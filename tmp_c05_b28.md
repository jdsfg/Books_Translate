#### 深入思考

**Q1.** _eval 设计纪律作为可迁移技能_。本章论证 eval 就是规范，设计 eval 是 AI 工程师的核心技能。考虑 eval 设计纪律是否迁移到其他工程背景（例如数据库工程、分布式系统、安全工程）。指出两个纪律有用迁移的背景，和一个不适用的背景。

**讨论：** 纪律迁移的两个背景：

1. **分布式系统测试**。_对照固定输入集合测量质量，带参考标准、阈值和 CI 门禁_ 的纪律直接映射到混沌工程和分布式系统测试。混沌工程测试固定一组故障场景（golden set），定义 _系统存活_ 意味着什么（参考标准），测量它（指标），并在回归时门禁部署（CI 门禁）。形态相同；领域不同。精通 eval 设计的工程师倾向于快速上手混沌工程，因为元模式是共享的。
2. **性能工程**。相同形态：golden 工作负载、定义的性能目标、自动化测量、PR 上的回归检测。_没有 PR 在超过阈值回归 eval 的情况下发布_ 的纪律与 _没有 PR 在超过阈值回归 p99 延迟的情况下发布_ 相同。指标不同；工作流相同。

一个纪律 _不_ 好迁移的背景：

* **对抗威胁的纯安全工程**。对抗复杂攻击者的安全工程不以相同方式被 _golden set_ 约束；攻击者的输入集是开放和对抗性生成的。安全测试借鉴 eval 纪律（已知漏洞的回归测试、模糊测试作为一种随机 eval），但核心安全姿态是 _假设输入集是敌意的且仅受攻击者创造力限制_，这与 _在固定代表性 golden set 上测量质量_ 是不同形态。安全工程和 AI 工程在 prompt 注入边界共享随机组件纪律（第 23 章），但核心方法论分叉。

通用模式：eval 纪律迁移到任何满足 (a) 质量可以对照代表性输入集合测量、(b) _足够好_ 的阈值可以操作化、(c) 工作流可以围绕它构建的工程领域。当其中一个崩溃时，纪律要么必须显著适配要么用不同方法论替代。

**Q2.** _eval 与工程判断_。本章论证 eval 使工程决策数据驱动。但许多工程决策不能简化为 eval 分数：关于架构、招聘、供应商关系、产品策略的选择。反思 _eval 驱动一切_ 的局限。指出 eval 驱动工程（本章）与工程判断（第 24 章）之间的关系。

**讨论：** eval 强大因为它将 _我认为_ 转换为 _数据显示_。这对有可测量结果的决策是变革性的：prompt 变更、模型升级、检索语料库更新、重排序器添加、结构化输出 schema、agent 设计。对于这些，eval 驱动工程是明确赢家。
