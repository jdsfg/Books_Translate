### 22.8 How This Breaks：未被维护的微调

**命名的故障模式**：未被维护的微调。团队为特定任务微调模型。基础模型改进；团队不重新调优；微调逐渐变得比（改进后的）基础模型更差。团队现在为一个无附加值的微调承担维护负担。

**真实或复合事件**：线索 B 失败 4。一个团队为特定结构化提取任务微调 GPT-4。微调在 2023 年发布时比基础 GPT-4 高出 8 分。到 2025 年中，GPT-5 发布；基础 GPT-5 比微调 GPT-4 高 4 分。团队的监控最终捕获了它，但仅在一个季度的退化生产行为之后；迁移的工程工作（为 GPT-5 基础重新调优；或切换到 GPT-5 基础配合 prompt 工程）花了 6-8 工程师周。

**工程缓解措施**：

1. **维护数据流水线，不仅是模型**。产生微调数据的数据流水线是持久资产；训练好的模型是短暂的。有流水线，重新调优简单；没有它，微调不可复现。
2. **每次基础模型更新时监控微调 vs 基础**。对两者运行 eval；如基础追上或超越，那是决策点：重新调优或迁移到基础。
3. **计划每次基础模型大版本发布时重新调优**。预算工程工作；如你不能持续，一开始就不要微调。
4. **先评估基础 + 良好 prompt**。许多团队过早微调；良好工程的 prompt + 结构化输出 + RAG 通常以一小部分维护成本匹配微调。

**本应捕获它的 eval**：持续在生产 eval 上比较微调 vs 当前基础模型。没有此比较，相对性能不可见。有了它，决策点是数据驱动的；团队主动迁移或重新调优，而非被动。
