### 9.6 Strata Research 的朴素 RAG 及前进路径

Strata Research 的第一版在 2024 年是朴素 RAG。他们有约 5M token 的案例法和法规语料库，OpenAI 的 `text-embedding-3-small` 作为嵌入模型，Pinecone 作为向量数据库，top-5 检索，无重排序器，GPT-4 作为 LLM。系统上线小规模 beta。

他们初始生产 golden set 的 eval 结果：

指标 | 朴素 RAG 基线
---|---
检索 recall@10 | 0.62
回答相关性 | 0.58
引用接地 | 0.51
管辖权正确性 | 0.74
总体通过率 | 0.43

这些数字不能发布。43% 通过率意味着超过一半答案在 Strata 的量表上不可接受。客户（法律研究员）需要 _高可靠性_ 因为他们的下游工作依赖答案正确；60% 的答案比没有答案更差因为他们无法分辨哪个是哪个。

前进路径，在 2025 年执行：

1. **文档结构感知分块**在段落级（提升：检索 recall 约 3 分）
2. **领域特定嵌入**通过 Voyage AI 的 `voyage-law-2`（Strata 法律语料库上检索 recall 约 8 分；见第 10 章 §10.8 仅嵌入 A/B 表）
3. **混合搜索**带 BM25 加向量（精确引用查询约 4 分）
4. **Cohere Rerank** 在 top-20 → top-5（相关性约 8 分；最大单项提升）
5. **查询重写**用于模糊查询（相关性约 3 分）
6. **Prompt 级引用要求**（第 6 章 V2；引用接地约 7 分）
7. **多轴 RAG eval** 驱动持续改进（第 11 章）

到 2025 年 Q4，生产 eval 非常不同：

指标 | 2025 Q4
---|---
检索 recall@10 | 0.85
回答相关性 | 0.85
引用接地 | 0.92
管辖权正确性 | 0.93
总体通过率 | 0.81

路径是一年跨所有四个杠杆的纪律工程，每个变更都对照 eval 验证。没有突破。没有单一技巧。有的是 _持续应用本章框架带 eval 驱动纪律_。这就是生产 RAG 工程的样子。

（关于算术的说明：上面每个杠杆的提升标注在不同指标上——一些对检索 recall、一些对相关性、一些对引用接地——它们针对多维量表上的重叠失败案例。它们不线性加总到总体通过率增量。上面七个杠杆的 +33 分总和与总体通过率的 +38 分变动（0.43 → 0.81）在此注意事项下一致；每个杠杆修复一片失败，在切片重叠处有递减回报。）

#### 常见误解

生产质量 RAG 不是冲刺交付物。Strata 团队花了整整一年从 43% 通过率到 81%；每个杠杆需要实验、评估、部署和监控。承诺 _两周 RAG MVP_ 的团队交付的东西能演示但在生产流量中无法存活。诚实的项目范围是月而非周。
