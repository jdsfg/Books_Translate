**Q3.** _更好的训练会消灭后处理器模式吗？_ §2.6 和上面 Q4/Q6 中的引用验证后处理器基于架构性声明：LLM 在不确定性下承诺输出而没有内部校准信号。一个合理的假设是到 2030 年，训练进步（更好的校准、检索增强训练、通过 API 暴露的内部置信度估计）将降低 hallucination 率到防御性后处理器成为不必要开销的程度。竞争假设是更好的训练将 hallucination 率推低但永远不会到零，而灾难性故障案例的工程经济学（Beacon 的一次 hallucinated 药物剂量）使后处理器模式永远必要。论证你的立场。如果你认为后处理器会消失，指出使其消失的具体训练进步；如果你认为它会留下，指出后处理器演变成什么。

**讨论：** **支持后处理器消失**：有一个具体的训练进步可以实质性改变局面，那就是通过 API 暴露可靠的逐 token 置信度估计。如果模型在输出的每个 span 旁边返回该 span 事实正确的校准概率，工程层可以直接使用该概率：高置信度声明通过，低置信度声明触发验证器或拒绝。后处理器不会消失，但它变成一个薄的编排层而非并行来源验证器。校准研究足够活跃，2030 年的前沿模型具有可靠置信度输出是合理的。届时独立验证的成本和延迟开销大幅缩小。

**支持后处理器留下**：永久性的理由基于两个事实。第一，模型的置信度估计，即使在训练分布上校准了，在最坏情况下的对抗性或分布外输入上也没有校准。模型可以在设计为引出自信错误（prompt 注入、jailbreak、领域偏移查询）的输入上自信地犯错。后处理器是防御模型自信地犯错，而不仅仅是防御模型不确定地犯错。第二，受监管行业的工程经济学（Beacon、Strata）要求的不是"低 hallucination 率"而是"可审计验证"。一个被告知"模型有 99.7% 的把握"的临床医生或律师不能像可以基于"引用已对照 PubMed 验证"那样为下游决策辩护。后处理器的价值部分是机械性的（捕获错误），部分是程序性的（产生审计轨迹）。

诚实的答案是后处理器模式演变而非消失。到 2030 年，架构可能看起来像：模型发射带置信度标签 span 的草稿输出；后处理器使用置信度作为路由信号（只验证低置信度声明）而非作为主要防御（验证一切）。这使延迟开销可控，同时保留受监管行业需要的可审计验证属性。本书关于防御性工程的章节（第 4、7、9、23 章）将需要 2030 年的更新，但底层原则（将模型输出视为不可信，在对其采取行动之前验证）不变。这个原则比 LLM 更古老；它追溯到一般的防御性编程。
