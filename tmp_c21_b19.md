* 基础 EOT 阈值为 350ms（Helios 为英语调优的默认值，按 §20.4 和工作流 #13）。
* 对受影响 locale，将阈值提高到 1000-1200ms。对照采样通话验证：在 1000ms 时，还剩多少错误打断？在 1200ms 时？选择最优点。
* 风险：更长阈值使 agent 在后续轮次上感觉更慢。权衡：locale 特定的对更慢 agent 响应的容忍，以换取不在思考中打断来电者。

(c) **按地区推出**：

* 按 locale 用功能开关标记新阈值。先对受影响 locale 5% 流量启用。
* 监控：打断率（应下降）、agent 响应延迟（会上升；验证可接受）、客户满意度（如有 CSAT 调查；非正式团队通话审查）。
* 5% 正面指标 1 周后，在接下来 2 周内逐步到 25%、50%、100%。
* 其他 locale：不动。全球流量的默认保持 350ms。

(d) **Eval 纪律**：

* 添加按 locale 的 eval 切片：每个受影响 locale 50 个采样通话，评分"适当轮次交替"。这成为回归套件的一部分。
* 未来的 EOT 调优变更对照此切片审查。
* 定期重新验证：随着受影响 locale 的用户群增长或典型停顿模式变化，阈值可能需要调整。

(e) **更广泛的教训**：语音系统服务异构用户群体。单一全局 EOT 设置很少对所有群体最优。纪律是 _细分用户群体_（按 locale、按人口统计、按用例）并按细分调优。支持调试的相同追踪数据（第 19 章）支持人口统计细分分析；相同的 eval 框架支持按细分验证；相同的运维仪表板测量按细分质量。

一个具体运维模式：团队构建"按细分健康仪表板"显示每个细分的打断率、EOT 校准和满意度。偏离基线的细分获得关注。这是语音领域 _生产 eval 采样_（第 21 章）专门应用于轮次交替维度。

一般原则，语音感知：**语音质量是按用户群体的属性，非按系统的属性**。在美国英语 eval 上得分好的语音系统可能在多样化全球流量上表现不佳。eval 和调优纪律必须反映系统服务的群体。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_来自特定口音群体的来电者从我们的语音 agent 那里经历 3-4 倍更高的打断率。设计检测、调优、推出、eval 纪律。_ 你验证 (a) LLM 提出按细分调优，非全局阈值变更（全局变更会对其他群体过度修正；LLM 可能默认全局；坚持细分），以及 (b) 推出包括超越仅打断率的监控（更长阈值提高 agent 响应延迟；这也应被追踪，否则团队以一个维度为代价优化另一维度）。
