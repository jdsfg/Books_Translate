**答案：** 初级工程师的设计让模型做对其架构来说完全错误的工作。在长 diff 中计数特定子串是一个字符级机械任务，模型不可靠：它无法直接计数 token 或子串，即使尝试也可能数错，尤其是在长输入中。

正确的设计是在代码中确定性地做计数，并让模型做它擅长的部分：

1. **代码级计数**：解析 diff，在添加的行中搜索字面字符串 `# TODO:`（通常是 diff 中以 `+` 开头的行），计数出现次数。这需要十行 Python。
2. **模型级解释**：如果计数超过 5，_然后_ 调用模型：给它有问题的 TODO 注释并要求它总结这些看起来是合理的进行中工作标记还是开发者在积累债务。这是模型擅长的判断任务。

架构原则是：**在代码中做确定性工作，将模型保留给需要语义判断的任务**。模型是一个概率性组件；将其预算花在代码可以可靠完成的任务上是浪费且不可靠的。第 13 章将把这个变成 _工具_ 模式：计数变成模型可以调用的确定性工具，模型编排但不执行计数。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_批评这个设计："让 LLM 在长 diff 中计数特定字符串的出现次数。"建议 LLM 和确定性代码之间更好的分工。_ LLM 可能产生类似"在代码中做计数；用 LLM 做判断"的合理答案。你通过检查提议的确定性计数是否实际处理你 diff 格式的边缘情况（多行注释、包含 TODO 作为子串的字符串等）来验证；LLM 的建议可能忽略实现细节，你必须填补这些。

**Q4.** _hallucination 诊断_。你的团队注意到模型偶尔在输出中产生不正确的引用，比如一个不存在的论文标题被归因于一个真实的作者。团队在争论是否 (a) 锐化 prompt 来阻止引用，(b) 添加一个后处理器来对照列表验证引用，(c) 切换到不同的模型。用 §2.6 的心智模型论证最佳方法（或组合）。

提示

hallucination 不是 prompt 可修复的。选择在于 _阻止模型处于可能 hallucinate 的位置_ 和 _事后捕获 hallucination_ 之间。

**答案：** 心智模型说 hallucination 是模型在高不确定性下仍承诺下一个 token 输出的产物，尤其是在事实引用领域。仅 prompt（选项 a）可以降低率但不能消除；模型对"不要引用你不确定的东西"的遵从本身就是训练行为，可靠性可变。所以选项 (a) 是小帮助，不是解决方案。

选项 (c)，切换模型，是创可贴。不同模型在不同任务上以不同率 hallucinate，但没有 2026 年的前沿模型在引用任务上是无 hallucination 的。切换可能转移故障模式而不消除它。
