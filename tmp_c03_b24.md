选项 (b)，后处理器，是架构性回答。后处理器取模型输出，提取被引用的参考文献，并对照确定性来源验证：已知论文的数据库、CrossRef DOI 查询或你自己的内部知识库。未验证的引用在输出到达用户之前被标记或移除。

这是正确答案因为它将概率性问题（模型可能 hallucinate）转化为确定性问题（这个被引用的参考文献是否存在？）。

最佳工程选择是 **(b) + (a)**：锐化 prompt 以降低率（免费赢，即使小）并添加后处理器来捕获漏网的（真正的防御）。只有当你的 eval 显示不同模型实质性降低率时才结合 (c)；否则，工程精力更好地花在后处理器上。

对于 Beacon Health AI 特别而言，这个原则有额外重量：一个 hallucinate 药物剂量或诊断的临床笔记摘要系统是灾难性的。后处理器模式（将模型输出视为不可信；在展示给临床医生之前验证；将低置信度输出标记供人工审查）是能在 §2.11 的 _将模型与产品混淆_ 故障模式中存活的设计。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我有一个偶尔 hallucinate 引用的基于 LLM 的系统。带我过一遍验证引用的对照 CrossRef 的后处理器架构，当引用未验证时怎么做，以及如何为已验证和未验证情况设计面向用户的输出。_ 你通过检查提议的 CrossRef API 用法是否匹配实际当前 CrossRef API 来验证（LLM 会 hallucinate API 细节，尤其是较少主流的 API），以及通过对面向用户流程的合理性检查（_标记并移除_ 是否隐藏了有用上下文，还是 _带警告显示_ 对你的用户更好？）。

**Q5.** _为真实功能设计 prompt 缓存友好的布局_。Strata Research 正在构建一个法律文档问答界面。每个查询携带：一个 600 token 的定义律师人格的 system prompt，一个 1800 token 的律所希望执行的"内部风格指南"，约 4000 token 的检索判例法上下文（每次查询不同），以及约 200 token 的用户问题。目前团队将 prompt 组装为：`[user_question] + [retrieved_context] + [house_style] + [system_prompt]`，因为"用户问题感觉最重要应该放前面"。重新设计布局以最大化 prompt 缓存命中，估算在 Sonnet 4（3.00 美元/百万新输入，0.30 美元/百万缓存读取）上每天 10,000 次查询的成本节省，并指出重新设计引入的一个团队应该 eval 的风险。

提示

缓存匹配前缀。从跨查询最稳定到最可变的顺序排列组件。然后问哪个组件可变但仍相对较大。

**答案：** 当前布局从缓存利用率角度看恰好反了：最可变的组件（`user_question`）在最前面，所以没有前缀可跨查询重用。几乎每个 token 都以新费率付费。
