2. **可观测性（第 5 层）**。没有带哪个模型服务调用的每调用追踪标签，团队无法将质量回归归因到路由决策。回归出现；是新 prompt、新模型还是新回退？无追踪路由不可诊断。成本追踪（第 6 层）类似地依赖可观测性：按层成本归因需每调用模型标签。

更深的要点：9 层架构不是独立功能的检查清单。层 _在特定方向上耦合_。高层依赖低层和相邻层功能正常。构建路由器而无 eval 的团队构建了无法安全运营的基础设施。架构的价值不在各层个体而在构建它们的顺序和纪律。

通用原则：当决定是否投资新层时，问哪些相邻层必须已工作新投资才能回报。9 层架构使这些依赖显式；这是将其视为参考而非菜单的价值。
