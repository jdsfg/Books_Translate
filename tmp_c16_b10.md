### 15.9 How This Breaks：丢失工作器跟踪的编排器

**命名的故障模式**：丢失工作器跟踪的编排器。多 agent 系统运行。编排器生成工作器；工作器完成子任务；编排器的聚合步骤遗漏一个工作器的输出（聚合逻辑中的异常；状态存储竞态条件；工作器在编排器等待超时后完成）。最终响应在缺少该工作器发现的情况下组合。用户得到不完整答案。

**真实或复合事件**：来自 2025-2026 多个生产部署的复合。故障模式 _不在_ LLM 层——每个单独 agent 正确工作——而在 _协调_ 层。状态存储丢弃事件；编排器在工作器完成前超时；聚合逻辑在特定边缘情况有 bug。agent 级 eval 通过；系统级行为错误。

**工程缓解措施**：

1. **系统级 eval，非仅 agent 级 eval**。eval 套件端到端运行完整轨迹并验证最终输出包含预期聚合。每 agent eval 必要但不充分。
2. **工作器完成保证**。编排器等待所有生成工作器（有界超时）；超时工作器被显式处理（要么以"不完整"标志包含，要么以响应中显式通知排除）。
3. **状态存储可靠性**。编排器-工作器通信通过可靠存储（Postgres、持久队列）；非可能丢失状态的内存哈希表。
4. **追踪级可观测性**。每个生成的工作器记录日志；每个工作器输出记录日志；聚合步骤的输入和输出记录日志。当丢失东西时，追踪告诉你丢了什么。

**本应捕获它的 eval**：系统级 eval 验证"对多问题工单，响应处理 _所有_ 问题，而非仅部分。"如 eval 有多问题案例其中参考输出覆盖问题 A、B 和 C，而系统输出仅覆盖 A 和 B，eval 标记。单 agent eval 错过此问题因为问题在协调中，不在任何 agent 的输出中。
