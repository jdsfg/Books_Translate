#### 概念检查

**Q1.** 为什么专用嵌入模型（Voyage `voyage-law-2` 等）在目标领域上优于通用嵌入？这对为新 RAG 项目评估嵌入意味着什么？

**答案：** 专用嵌入在领域特定语料库上训练，通常包含领域相关监督信号（例如法律引用对、代码-API 文档对）。训练目标显式优化领域中重要的相似性类型：法律文档中的引用共现、代码中的语法和语义相关性、医学文本中的病症-药物配对。通用嵌入优化广域相似性；在专用领域上，它们与相关语义对齐度较低。

对评估的含义：**承诺前总是在你的特定语料库和查询上评估**。供应商基准（MTEB、MIRACL）是通用基准上的相对排序；它们不预测你领域的性能。从基准排行榜选嵌入而不做领域特定 eval 的团队经常次优选择。eval 纪律（第 4 章）应用于检索层（第 9 章 §9.5）才产生正确选择。

**Q2.** 解释为什么混合搜索在生产 RAG 上优于纯向量搜索。给出两个纯向量失败但混合成功的查询示例。

**答案：** 纯向量搜索依赖嵌入模型将所有查询和所有块映射到统一语义空间。映射对改述查询好（嵌入跨表面变化捕获意义）但对依赖精确 token 匹配的查询弱。向量几何不很好地保留精确 token 匹配；罕见术语和特定标识符在纯相似性空间中可能远离包含它们的块。

两个示例：

1. **精确引用查询**："26 USC § 162(a) 说了什么？"用户想要特定节；向量搜索将语义相邻节（其他可扣除性节、相关案例法）排名高。BM25 在精确引用字符串上命中目标。混合捕获正确块。
2. **产品名查询**："如何使用 Stripe Atlas 税表生成器？"产品名"Stripe Atlas"如果文档使用不同表述可能不嵌入接近其文档。BM25 在精确产品名上呈现文档；纯向量搜索可能用通用税表页面稀释。

混合模式使用每种检索方法补偿对方的弱点。倒数排名融合混合分数；被任一方法排名高的块浮到顶部。

**Q3.** 比较 pgvector、Pinecone 和 Weaviate 用于新生产 RAG 项目。对每个，指出它是正确选择的场景。

**答案：**

* **pgvector**：一个 SaaS 初创公司有约 3M 块内部文档，已使用 Postgres 存储业务数据，无专职基础设施团队。pgvector 是正确选择：零新运维负担、与数据其余部分的事务一致性、在此规模性能充足。
* **Pinecone**：一个成熟企业 SaaS 有约 80M 块，有专职 AI 平台团队，运维便利和可预测扩展比供应商锁定更重要。Pinecone 的托管服务消除一类运维工作；SaaS 成本是合理的。
