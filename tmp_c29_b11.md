### M

**Matryoshka 嵌入**：训练为任何前缀本身也是有用的（低维）嵌入的嵌入。允许存储完整向量但以低维查询以提速（第 10 章，§10.3）。

**max_tokens**：限制生成响应长度的 API 参数。保守设置以限制成本和延迟（第 3 章，§3.3）。

**MCP**：模型上下文协议。开放标准（Anthropic 引入，现广泛采用）用于通过通用服务器接口将 LLM 连接到工具和数据源（第 13 章）。

**记忆架构**：agent 的草稿本、情景和长期记忆子系统的设计及其交互方式（第 14 章，§14.5）。

**消息轮次**：多轮对话中的一次交换（用户消息 + 助手响应）。对话 API 状态的单位（第 3 章，§3.1）。

**MIPRO**：DSPy 优化器，联合调优 prompt 指令和少样本示例。严肃团队的替代手动调优方案（第 6 章，§6.3）。

**Mistral**：法国开放权重模型家族，以高效中等规模模型和 Mixtral MoE 线闻名（第 22 章，§22.1）。

**MLX**：Apple 针对 Apple Silicon 优化的数组框架。Mac 和 iPhone 上的默认推理栈（第 22 章，§22.6）。

**ML 平台工程师**：为 AI 团队构建共享基础设施的工程师：训练集群、eval 平台、网关、可观测性。与 AI 工程师不同（第 24 章，§24.1）。

**Modal**：无服务器平台，流行用于批量推理、微调和沙箱化代码执行。混合构建/购买选择（第 22 章，§22.4）。

**模型卡**：提供商发布的文档，描述模型的训练、能力、限制和安全属性。采用任何新模型前的必读材料（第 2 章，§2.9）。

**模型上下文协议**：见 _MCP_（第 13 章）。

**模型升级工作流**：评估、A/B 测试和推出新模型快照的可重复流程。制度化，非临时（第 21 章，§21.6）。

**MRR**：平均倒数排名。在查询集上对 1/首个正确结果排名 取平均。用于"找到正确东西"任务的标准检索指标（第 9 章，§9.5）。

**多 agent**：由多个通过消息交互的 LLM agent 组成的系统。适用于良好分解的问题；通常过度设计（第 15 章）。

**多模态 RAG**：对非文本模态（图像、PDF 作为图像、视频、音频）的 RAG，使用多模态嵌入或 ColPali 等页面级检索器（第 12 章，§12.6）。

**混合专家**：每个 token 仅激活部分模型参数的架构，在固定参数量下显著改善推理经济性。被 Mixtral、DeepSeek-V3 等使用（第 22 章，§22.2）。

**MoE**：见 _混合专家_（第 22 章，§22.2）。


### N

**朴素 RAG**：基线模式：嵌入查询，按向量相似度检索 top-k，填入 prompt，生成。对生产不足但作为有用起点（第 9 章，§9.4）。

**NDCG**：归一化折损累积增益。奖励高相关结果在顶部排名并折扣靠后位置的检索指标（第 9 章，§9.5）。

**NER**：命名实体识别。LLM 前的实体标注技术（人、地点、日期）；仍用于 PII 脱敏和知识图谱构建（第 19 章，§19.7）。

**九层架构**：见"Z"/数字部分下的 _9 层架构_（第 16 章，§16.2）。
