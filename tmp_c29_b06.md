### E

**E2B**：为 agent 代码执行提供临时 Linux 容器的沙箱服务。"运行此 LLM 编写代码"工作负载的常见选择（第 13 章，§13.5）。

**边缘推理**：在靠近用户的设备（手机、笔记本、边缘服务器）上运行模型而非集中云中。以容量换延迟和隐私（第 22 章，§22.6）。

**ElevenLabs Conversational AI**：结合 ElevenLabs TTS 与 STT 和轮次检测逻辑的语音 agent 平台。语音质量强（第 20 章，§20.5）。

**嵌入**：文本（或其他模态）的稠密向量表示，语义相似性对应向量距离。检索的基础（第 10 章，§10.1）。

**嵌入模型**：产生嵌入的模型。选择重要：领域、维度、最大序列长度和许可证都需考虑（第 10 章，§10.2）。

**轮次结束检测**：决定用户何时说完。2026 年语音 agent 中最难的未解决问题（第 20 章，§20.6）。

**端到端语音模型**：直接处理音频输入并产生音频输出无需单独 STT/LLM/TTS 管道的模型。延迟更低，对单个阶段控制更少（第 20 章，§20.7）。

**EOT**：轮次结束。见 _轮次结束检测_（第 20 章，§20.6）。

**临时缓存**：提供商 prompt 缓存，在最后一次命中后存活几分钟（通常 5 分钟）。成本权衡：写入便宜，过期后零成本（第 18 章，§18.2）。

**情景记忆**：agent 对特定过去交互的记忆，按与当前情况的相似性检索。区别于长期语义记忆（第 14 章，§14.5）。

**Eval-as-CI**：在每个 PR 上运行评估并阻止回归关键指标的合并。区分严肃团队与原型者的纪律（第 5 章，§5.7）。

**Eval 驱动开发**：在写 prompt 或管道前定义 eval。反转通常流程并强制对成功的清晰（第 4 章 §4.1；第 24 章 §24.2 阶段 2）。

**Eval 存储**：持有 eval 定义、运行和结果的持久系统。让你无需重跑即可历史性比较任意两个版本（第 5 章，§5.8）。

**指数退避**：在尝试间加倍等待（带抖动）的重试策略。瞬时 API 失败的默认（第 3 章，§3.5）。

**评估框架**：运行 eval 项通过被测系统、收集输出并应用评分器的代码。区别于它执行的 eval _定义_（第 5 章，§5.7）。

**Eval 切片**：eval 集的命名子集（如"西班牙语查询"、"长上下文"、"含 PII"）。切片揭示聚合分数隐藏的回归（第 5 章，§5.8）。
