**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为临床笔记绘制合成 PHI 管道：输入、输出、转换步骤、验证和每步一个风险。_ 你验证 (a) 提议的替换策略确实打破重新识别（LLM 可能建议保留太多的替换，例如，姓名 → 同首字母姓名，这泄漏信息），以及 (b) HIPAA 合规推理匹配 Safe Harbor 和 Expert Determination 标准（LLM 在监管细节上不可靠；与隐私/合规团队交叉检查）。

**Q4.** _eval 套件增长管理_。你加入一个在单一套件中有 850 个 eval 案例的团队。套件运行需要 25 分钟；团队已停止在每个 PR 上运行因为它太慢。诊断问题并提出三个具体步骤的修复计划。

提示

§5.7 的 _季度审查_ 纪律。套件已无退休地增长。

**答案：** 诊断：套件随时间累积案例而无退休。850 个案例足够大，在现代 LLM 推理延迟约每案例 1 秒（带 LLM 作为评判者调用）的情况下，串行运行需要 15–30 分钟。团队理性地放弃了每 PR 运行因为延迟太高，但纪律崩溃（PR 上无 eval）比原始问题更糟。

三个修复步骤：

1. **审计和修剪**。审查所有 850 个案例。分类：(a) 活跃有用（最近 6 个月内为仍然活跃的真实故障模式添加的）；(b) 历史有用但代表不太可能再发生的已关闭故障模式；(c) 冗余（与其他案例重叠）；(d) 低质量（标注模糊，从未产生有用信号）。立即退休类别 (c) 和 (d)；退休大部分 (b) 但保留一组规范示例子集。目标：修剪至约 300 个活跃案例。
2. **并行化运行器**。300 案例 × 1 秒/案例 = 5 分钟串行。以 5 倍并发（远在大多数 provider 的速率限制内），降至约 1 分钟。以异步批处理，甚至更快。运行器级并行是廉价工程：`pytest-evals` 的 `pytest-xdist`，或运行器脚本中的异步模式。
3. **分层套件**。拆分为 _快速套件_（50–80 个案例在每个 PR 上运行；约 30 秒）和 _完整套件_（300 个案例在每夜节奏和发布前运行；并行化后 1–2 分钟）。快速套件门禁合并；完整套件捕获更微妙的问题。两者都进入 SQLite 存储；团队监控两者。

可选第四步：**将季度审查添加到团队的常设会议日历**。没有反复的纪律，套件会在下一年再次累积。将审查日历化是运营修复。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我们的 eval 套件增长到 850 个案例，运行需要 25 分钟。团队已停止在 PR 上运行。诊断并提出修复计划。_ 你验证 (a) 提议的修剪标准匹配你的领域（LLM 的通用 _退休低质量案例_ 需要针对你的量表具体化），以及 (b) 并行化在你的速率限制下可行（LLM 可能提议高于你的 provider 层级允许的并发；检查速率限制头）。
