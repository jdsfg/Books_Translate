#### 定义：Top-k

一个参数（正整数）限制每一步采样到概率最高的前 k 个 token。Top-k 为 50 意味着：在每一步，取概率最高的 50 个 token，重新归一化，从该集合中采样。

Top-k 比 top-p 更早，在现代 API 中已基本被 top-p 取代（Anthropic 和 OpenAI 都默认忽略或省略 top-k）。当两者都设置时，效果是取交集：概率最高的前 k 个 token，进一步限制到 top-p 质量内。

#### 随机性的组合学

为什么关心这个？因为 _同一个 prompt 跨调用产生不同输出_ 是你的产品继承的属性，你的 eval 纪律必须适应。如果你在 temperature 0.7 下运行同一个 prompt 100 次，你会得到 100 个不同的回答。有些会微妙地不同（一个词改变）；有些会实质性地不同（不同的推理路径、不同的结论）。这不是 bug。这是设计。

对于工程师，含义是：

1. **可复现性需要 temperature 0**（模 GPU 警告）。对于调试、回归测试、法律合规，将 temperature 设为 0 并将该输入的输出视为规范输出。
2. **eval 套件必须考虑变异性**。如果你在 temperature 0.7 下对模型输出评分，运行 eval N 次（通常 N=3 到 5）并报告分布，而非点估计。否则，一次运行中检测到的回归可能是噪声。
3. **某些故障模式是随机的**。prompt 注入攻击可能在 30% 的时间有效。安全过滤器可能在 1% 的时间失败。你不能通过抽查检测这些；你必须在足够大的样本量上测量它们以估计比率。
