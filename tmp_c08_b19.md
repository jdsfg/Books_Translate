* `instructor.from_anthropic` 包装客户端；`response_model=CodeReview` 启用 Anthropic 的工具使用结构化输出机制（§7.2 描述的 provider 侧强制路径；`instructor` 抽象 `response_format` 和工具使用之间的每 provider 差异）。
* `max_retries=3` 配置 `instructor` 的内部重试循环：在 `ValidationError` 上，库将错误追加到对话并重新调用；最多 3 次尝试。
* 重试耗尽后，`ValidationError` 带结构化错误详情被记录并重新抛出。调用者决定如何处理最终失败（通常：回退到 prompt 注入安全的错误路径；记录到 eval 存储；如果率超阈值则告警）。

要将重试频率作为指标跟踪：`instructor` 暴露钩子；将重试回调连接到递增计数器。计数器馈送你的监控（第 19 章）；如果重试率飙升，出问题了（schema 不匹配、模型行为变化、prompt 漂移）。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_重写这段 Anthropic + Pydantic 抽取代码以使用 `instructor` 的内置重试，3 次尝试，结构化日志和正确的错误传播。_ 你验证 (a) `instructor.from_anthropic` API 匹配当前库版本（它随时间变化；检查库文档），以及 (b) 重试行为：写一个带故意 schema 违规 mock 的测试确认重试启动且验证错误在最终失败时被记录。

**Q3.** _设计 schema 覆盖 eval_。schema 太刚性故障模式（§7.7）通过检查 schema 能否表示每个合法输出来捕获。设计一个检测这个的 eval。指定：(a) 输入集；(b) 测量什么；(c) 失败信号；(d) 如何集成到 CI。

提示

你需要 _期望输出无法适合 schema_ 的案例，以及测量模型是否被迫误分类或失败的 eval。

**答案：**

(a) **输入集**：30–50 个代表长尾的案例：与常见模式微妙不同的输入。对于 CodeReview Bot：产生 _注释级_ 发现（低于 minor）的输入、产生 _仅风格_ 发现（无严重性）的输入、产生 _跨文件_ 发现（file 字段不适合单一值）的输入。

(b) **测量什么**：

* **字段覆盖率**：对 schema 中每个字段，模型多常使用语义适合的值 vs. 不适合但符合的值？
* **兜底使用率**：模型多常使用 `unspecified` / `other` / 兜底值？高率暗示 schema 缺少类别；零率可能暗示兜底不可达。
* **强制误分类率**：人工标注者标记模型选择的值为"不适合但是最接近的可用选项"的案例。

(c) **失败信号**：如果强制误分类率在长尾切片上超过 5%，schema 太刚性。具体行动：添加类别、拓宽约束或将字段拆分为更多变体的判别联合。

(d) **CI 集成**：schema 覆盖 eval 在每个修改 schema 的 PR 上运行，加上每周在主分支上作为后台检查。当失败信号触发时，PR 被阻止（对 schema 变更）或标记被提升（对每周主分支运行）。
