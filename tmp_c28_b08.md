#### 模式 8：带 PII 脱敏可观测性的受监管 AI

**解决什么问题。** 你需要 LLM 调用的轨迹级可观测性——延迟、成本、错误率、质量代理——但将原始 prompt 和响应发送到 Datadog 或 Honeycomb 是合规违规：prompt 携带供应商不是业务伙伴的 PHI/PII。你仍需要轨迹。Beacon Health AI 生活在此约束中并构建了此模式。

**拓扑。** 每个 LLM 调用从 AI 服务发出 OpenTelemetry 轨迹。在轨迹离开网络边界前，它通过脱敏 sidecar——一个小型 Go 或 Rust 服务（低延迟重要；脱敏在摄取关键路径上）扫描 prompt 和响应 span。检测器是分层的：结构化标识符的正则（SSN、MRN、账号、邮箱、电话）；自由文本 PHI 的 NER（带 HIPAA 安全港实体集的 spaCy，或微调 BERT 类模型）；以及对抗案例的最终 LLM 通行，仅在先前层同意文本"可能干净"时运行使你不在大多数轨迹上付 LLM 税。检测到的实体替换为类型化 token（`[NAME_1]`、`[DOB_1]`）保留共指。脱敏轨迹发往轨迹存储——自托管 Langfuse 用于 LLM 可观测性功能，自托管 Tempo 或 Jaeger 仅用于 span。Grafana 仪表板显示延迟、成本、错误率和按租户质量代理。原始未脱敏轨迹存在 BAA 覆盖数据平面内单独的原始轨迹保险库中，短保留（24-72 小时）和访问审计读取——仅用于事件响应，从不用于常规仪表板。

**何时用。** HIPAA、PCI、GDPR 特殊类别，或任何 prompt 内容本身受监管的制度。脱敏层在法务第一次问"我们的可观测性供应商看到什么？"时就回本。

**何时不用。** 数据不受监管。脱敏给每次轨迹摄取增加 20-100 毫秒和有意义的运维面需监控；无合规理由不要承担。

**交叉引用。** 第 19 章（LLM 系统的可观测性，包括 §19.7 的 PHI 脱敏模式和 §19.6 的 HIPAA 紧张）；第 23 章（输出安全和四层 prompt 注入防御，复用同样的 PII 模式）。
