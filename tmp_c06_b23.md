(b) **CI 工作流变更**：现有的每 PR 工作流运行快速套件并根据其结果门禁合并。一个新的每夜工作流（cron 触发）对 main 分支当前提交运行完整套件。一个新的发布前工作流（在生产部署前手动触发）对候选发布分支运行完整套件。每夜和发布前都将结果写入与每 PR 工作流相同的 SQLite 存储，所以团队的仪表盘看到所有三个流。

(c) **呈现完整套件回归**：每夜运行仅在检测到快速套件未捕获的回归时发布到团队的 Slack 频道。这是正确的渠道因为它将差异呈现为学习信号：被完整而非快速捕获的回归告诉团队快速套件在回归触及的方面有覆盖差距，这正是季度快速套件审查的输入。团队将这些视为中优先级问题：在一周内调查，将失败案例（或类似案例）添加到快速套件，确保回归在发布前运行前修复。

(d) **避免仅关注快速套件**：三个纪律机制。(i) 发布前运行在任何生产部署前是强制性的；这保证完整套件是发布流程的一部分，而非仅是侧面实验。(ii) 季度快速套件审查（如上引用）显式检查完整套件历史以找出应提升到快速的案例；这创建了与完整套件的反复接触点。(iii) 仪表盘以相同显著性并排呈现快速和完整套件分数；团队的 eval 驱动文化（第 4 章 §4.6，_eval 作为纪律_ 点）将两者视为一等信号。§5.9 _没人读的 eval_ 故障模式对完整套件和对任何其他 eval 一样适用；仪表盘、Slack 告警和发布前门禁都设计为防止完整套件漂移到那个命运。

引用的第 4 章概念：**覆盖审查**（§4.6 / §4.11）。快速 vs 完整拆分服务于覆盖纪律。没有覆盖审查，快速套件向容易通过的案例漂移，团队的有效 eval 即使纸面上套件增长也在缩小。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_绘制将 eval 套件拆分为快速和完整套件的工程工作，快速门禁合并，完整在每夜和发布前运行。覆盖案例标记、CI 工作流变更、回归呈现和防止快速套件成为唯一有人看的套件的纪律。_ 你验证 (a) 提议的案例标记标准是具体和可操作化的（LLM 可能建议 _选择判别性案例_ 而不指定如何测量判别力；坚持一个指标，例如，最近 N 个 prompt 版本间维度分数的方差）；(b) 呈现机制是具体的（Slack 频道、仪表盘磁贴、每周审查会议）而非模糊的；以及 (c) 发布前门禁显式绑定到生产部署流程，而非仅是 _我们有时应该运行完整套件_。
