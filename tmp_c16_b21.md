* **上线前**：试点 eval (a) 必须通过才能有任何生产流量。人工在环审查 30 轨迹结果并签署。
* **每任务审查（前 4 周）**：每个任务的提取数据在使用前由人工操作员审查。操作员确认或更正；更正反馈为 eval 数据。4 周后，如准确性一致，减少审查为 10% 抽查。
* **失败时**：任何触发故障模式的任务排队人工审查。人工看到行动轨迹和截图；他们手动完成任务或更新 agent prompt 以处理新情况。
* **写行动时**：按 (b)，写行动需每任务显式人工批准。agent 不能自主提交表单、付款或修改门户状态。
* **季度审查**：审查 agent 整体性能；审查本季度出现的门户变更；更新 prompt 和工具目录。

(e) **审计轨迹要求**：

* 每个行动记录日志：时间戳、行动类型（点击、输入、滚动）、目标元素（CSS 选择器或坐标）、前后截图。
* 每个任务记录日志：task_id、供应商、agent 版本、prompt 版本、工具目录版本、结果（成功/失败/升级）、如有故障模式、提取数据、人工审查结果。
* 日志防篡改（仅追加；签名）。按合规保留（供应商数据操作通常 1-3 年）。
* 日志可查询：支持、安全和合规团队可搜索特定任务、agent 版本或故障模式。

**上线前门控**：仅在全部五个部分通过审查时部署。审计是结构化门控，非检查清单；任一部分的"失败"阻止上线直到解决。

交叉引用：

* §13.4 LLM 提议-应用处置：每个写行动经人工批准（应用处置）；LLM 的自主性限定为只读。
* §13.5 沙箱：专用 VM 是计算机使用版的代码执行沙箱；原则相同。
* §14.4 界：每任务资源上限和 30 行动限制是计算机使用版的 agent 界；相同纪律，不同领域。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_审计供应商门户数据提取的计算机使用部署：可靠性 eval、安全控制、故障模式映射、人工在环、审计轨迹。_ 你验证 (a) 安全控制包含沙箱 VM 和每任务范围化凭证（LLM 有时提议更弱隔离如"小心使用现有凭证"；坚持隔离），以及 (b) 人工在环检查点是渐进的（前 4 周重度审查，然后抽查；非永远固定水平）。
