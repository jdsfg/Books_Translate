### 章节小结

chat-completions API 的最小形式是六行代码。围绕那六行你封装：固定到日期模型版本；将 `max_tokens` 设为预算；选择 temperature 用于可复现性；在仓库外管理 API key；用指数退避处理 429 和 5xx；记录请求 ID、token 用量、持续时间和停止原因；以及读取每次调用生成的账单。Streaming 改变交互式 UI 的交付模型。采样参数（temperature、top-p、top-k、stop sequences）是你为特定任务画像转动的杠杆。速率限制和定价是运营现实。

2026 年的模型阵容分为前沿梯队（约 3 美元/15 美元，约 500 毫秒延迟）和工作马梯队（约 0.10 美元–0.80 美元输入 / 0.40 美元–4 美元输出，约 250–400 毫秒延迟）。梯队之间的选择是日常架构中最大的成本杠杆；第 17 章使其成为纪律。

Aiyana 在 Beacon Health AI 第一周的生产封装（§3.9）大约是生产级代码的下限。在那个下限之上，本书的其余部分构建层：eval（第 4–5 章）、prompt 作为版本化制品（第 6 章）、结构化输出（第 7 章）、上下文工程（第 8 章）、检索（第 9–12 章）、工具调用和 agent（第 13–15 章）、生产架构（第 16–19 章）、语音和运维（第 20–21 章）、安全和超越现成的路径（第 22–23 章）、团队和判断（第 24–25 章）。线索是连续的；你在本章写的调用在之后的每一章中。

第 2 章的心智模型（token 序列、采样循环、hallucination、prompt 敏感性）直接激发了 API 表面上几乎每个参数：`max_tokens` 约束循环，`temperature` 控制采样步骤，结构化输出（第 7 章）约束分布。如果你不确定一个参数为什么做它做的事，答案几乎总是在第 2 章。

第 4 章是下一章，它是将 API 调用变成纪律的章节。没有 eval，API 调用之上的每一层都是凭感觉。有了 eval，本书的其余部分有了立足之处。

### 3.X 练习题
