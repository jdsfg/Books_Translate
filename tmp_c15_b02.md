### 14.2 简要分类：自主 vs 监督；单轮 vs 多轮；ReAct vs 规划后执行 vs 反思

agent 设计空间有维度。侧栏级分类：

**自主性轴**：

* _监督_ agent 在高影响操作前与用户确认。用户批准每个主要步骤。安全；慢。
* _自主_ agent 无需确认即进行。快；如轨迹出错则危险。
* _混合_ 在生产中最常见：常规操作自主；高影响操作需用户确认。

**轮次计数轴**：

* _单轮_ agent 在一次推理 + 少量工具调用中完成任务。低风险；更简单。
* _多轮_ agent 有扩展轨迹（数分钟或数小时迭代）。高风险；螺旋故障模式在此。

**模式轴**（侧栏，非完整覆盖；第 13 章将工具使用作为构建块；agent 架构组合工具使用）：

* **ReAct**，Yao 等引入（2022）：推理与行动和观察交错。默认模式；简单、良好支持。
* **规划后执行**：agent 先产生计划，然后逐步执行。更结构化；当任务自然分解时有用。
* **反思**，Shinn 等引入（2023）：agent 审查其轨迹并改进方法。对有迭代任务有用；增加推理成本。
* **混合计划**：真实生产系统常组合。多轮客户支持 agent 对每轮循环用 ReAct；主要子任务间反思；当用户任务显式多步时规划后执行。

2026 年教训：**大多数生产 agent 使用 ReAct + 有界循环 + 结构化记忆；精心模式偶尔是正确答案但通常到货即过度**。从简单开始；让 eval 拉你向复杂而非猜测。

### 14.3 为什么 agent 失败：目录

五个故障模式几乎涵盖 2025–2026 所有 agent 失败。


#### 故障模式 1：无限循环

agent 持续调用工具而不朝目标推进。每次调用产生触发更多调用的输出。轨迹永不终止。无边界时，agent 永远运行（或直到速率限制触发）。

原因：

* agent 推理卡在矛盾或模糊上。
* 工具输出有噪声且 agent 持续重试。
* agent 忘记已尝试过什么。

缓解：硬迭代限制；轨迹监控；重复相似工具调用的断路器。

#### 故障模式 2：幻觉工具调用

agent 用不合理参数调用工具。不存在的客户 ID；错误格式的日期；不在目录中的工具名。工具报错；agent 读错误；有时 agent 重试相同错误调用；有时它试图修复没坏的东西而螺旋。

原因：

* agent 发明了实体（例如从上下文推断的订单 ID）。
* agent 未仔细读工具 schema。
* agent 混淆了多个工具。

缓解：工具 schema 验证（按第 7 章）；agent 可恢复的结构化错误响应；工具错误序列的重试上限。
