### T

**电话集成**：将 AI 语音 agent 连接到电话网络（SIP、Twilio、WebRTC 桥接）。为语音栈增加编解码器、网络和 PSTN 特定关注（第 20 章，§20.8）。

**温度**：缩放 logit 分布的采样参数。0 意味贪心；更高值产生更多样输出（第 2 章，§2.5）。

**TGI**：文本生成推理。Hugging Face 的高吞吐开放权重模型服务引擎。vLLM 的常见替代（第 22 章，§22.5）。

**首 token 时间**：从请求发送到首个生成 token 的延迟。用户感知的"思考时间"指标（第 20 章，§20.2）。

**Together AI**：提供托管开放权重模型和微调的云平台。常见的基于开放权重构建选择（第 22 章，§22.4）。

**Token**：LLM 输入/输出的单位：分词器产生的子词片段。定价、上下文限制和速率限制均以 token 计（第 2 章，§2.2）。

**Token 预算护栏**：发送前估计请求 token 数对照上限并拒绝或修剪的预检（第 23 章，§23.1）。

**分词器**：将文本映射到 token 并反向的算法。不同模型有不同分词器；一个拉丁字符 ≠ 一个 token（第 2 章，§2.2）。

**工具目录**：为给定任务暴露给 agent 的工具集合。更小的目录改善准确性；更大的改善能力（第 13 章，§13.4）。

**工具 schema**：工具名称、描述和参数的正式描述（通常 JSON-Schema 风格）。agent 看到的接口（第 13 章，§13.2）。

**工具使用**：模型在生成期间调用外部函数的能力。agent 和多数生产 LLM 集成背后的机制（第 13 章，§13.1）。

**top-k**：采样参数，将下一个 token 选择限制为 k 个最可能 token（第 2 章，§2.5）。

**top-p**：核采样。将下一个 token 选择限制为累积概率超过 p 的最小集合（第 2 章，§2.5）。

**有毒内容**：包含有害、骚扰或其他被禁止材料的输出。由输出安全层中的分类器检测（第 23 章，§23.2）。

**TPM**：每分钟 token 数。标准速率限制单位，通常在规模时是约束瓶颈（第 3 章，§3.5）。

**追踪**：一个逻辑请求通过系统的完整记录，由嵌套 span 组成。事后调试的单位（第 19 章，§19.2）。

**TTFT**：首 token 时间。见 _首 token 时间_（第 20 章，§20.2）。

**TTS**：文本转语音。经典语音管道的最终阶段。提供商选择主导感知语音质量（第 20 章，§20.4）。

**Transformer**：几乎所有现代 LLM 底层的神经架构，以序列上的自注意力为特征。AI 工程师很少需要知道数学；但需知道其成本形状（第 2 章，§2.1）。

**尾延迟**：第 95、99 或 99.9 百分位响应时间。用户实际感受的指标；平均延迟隐藏它（第 17 章，§17.5）。


### U

**不可信内容标记**：在 prompt 中将检索或工具获取的文本标记为不可信（如包裹在 `<untrusted>` 块中），使模型将其视为数据而非指令。对抗间接注入的部分防御（第 23 章，§23.3）。

**用户消息**：由终端用户产生的消息轮次。LLM 系统中主要的"不可信输入"面（第 3 章，§3.1）。
