* _引用正确性_：输出中所有引用是否经过验证为真实，没有 hallucinated 的到达用户？
* _引用完整性_：输出是否引用了应该引用的来源（需要引用的声明没有遗漏引用）？
* _独立于引用的答案质量_：无论引用如何，实质内容是否正确且有用？
* _验证器透明度_：当验证器行动（标记、移除、注释）时，面向用户的输出是否清楚发生了什么，还是用户看到静默变更？

(c) **每轴阈值**：引用正确性 ≥ 2.5（很高；hallucinated 引用到达用户对 Strata 是 Beacon 级故障）；完整性 ≥ 2.0（较低因为过度引用比不足引用危害小）；答案质量 ≥ 2.3；验证器透明度 ≥ 2.5（静默验证器行为随时间侵蚀用户信任）。

(d) **§4.11 主动检查的覆盖差距**：(1) _当用户问一个答案依赖于太新而不在验证器数据库中的论文的问题时会发生什么_——验证器标记为未验证；系统必须传达不确定性而非静默丢弃；(2) _当 LLM 引用真实论文但将声明错误归因于它时会发生什么_——引用验证为真实，但声明未出现在论文中；这是最难捕获的故障模式，需要基本引用存在验证器遗漏的额外验证器层（对论文文本的声明 grounding）。eval 应包含 20–30 个显式探测这些差距的案例。

(e) **CI 门禁**：PR 被阻止如果 (i) 250 个案例的平均分降至 10.0（满分 12）以下，或 (ii) 对抗性 50 案例分层上的引用正确性分数降至 2.4 以下，或 (iii) 验证器透明度分数在任何单一分层上降至 2.4 以下。分层特定门禁是必不可少的，因为仅平均分门禁可能掩盖对抗性输入上的灾难性回归。

这个练习说明第 4 章的 eval 纪律如何不仅适用于 LLM 功能而且适用于 LLM 加防御层功能。验证器是被测系统的一部分；eval 必须评分集成输出，量表必须设计为使验证器的失败不是不可见的（那将违背拥有验证器的全部意义）。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我在为 LLM 加引用验证器管道设计 eval。系统生成带引用的答案，验证器在展示给用户前检查引用。推荐 golden set 组合与分层、四轴量表、每轴阈值、两个显式探测的覆盖差距和 CI 门禁规则。_ 你验证 (a) 量表区分"验证器工作了"与"LLM 一开始就没有 hallucinate"（LLM 经常合并这些）；(b) 提议的对抗性分层是具体的（小众子领域、编造但合理的作者名、近似正确的标题）而非模糊的；以及 (c) CI 门禁包含分层特定下限，而非仅平均分下限（LLM 建议的常见错误是仅门禁平均分，这隐藏了切片特定回归）。
