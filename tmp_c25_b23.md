1. _分辨率覆盖_：eval 黄金集包含生产实际分辨率范围的图像，非仅一种分辨率。
2. _视角覆盖_：多视角图像（正面；斜面；部分遮挡）。
3. _光照覆盖_：高光、低光、混合光示例。
4. _模态覆盖_：如生产流包含多种成像模态（如 X 光、CT、MRI），eval 覆盖每种。
5. _标注质量_：参考标注由领域专家（此案例中放射科医生）产生并测量标注者间一致性。

_成本影响（视觉新增）_：

6. _每图像 token 估计_：视觉 token 取决于分辨率；PR 应指定每典型图像的 token 数和每调用成本。
7. _量预测_：预期每天图像数和由此产生的日成本。
8. _视觉上下文缓存潜力_：如同图像运行多次推理，缓存能否降低成本？PR 应评估。

_安全考虑（视觉新增）_：

9. _对分布外图像的自信错误_：视觉模型在训练分布外图像上自信幻觉。PR 应描述 OOD 检测层和回退路径。
10. _图像中的 PII_：医学图像可含识别信息（面部；纹身；首饰）。PR 应描述去标识步骤。
11. _图像来源溯源_：图像来源可信吗？元数据是否保留供审计？
12. _跨模态注入_：如工作流结合图像和文本输入，对抗图像内容是否引发文本侧注入（_视觉 prompt 注入_ 故障模式）？

_视觉特定验证（新部分）_：

13. _黄金集上的标注者间一致性_。Cohen's kappa 或类似；记录值。
14. _黄金集上的模型 vs 标注者一致性_。模型与专家标注的一致性，按图像类别。
15. _关键类别的混淆矩阵分析_。对医学标注，关键发现的假阴性与假阳性有本质不同；PR 应分别报告两种率。
16. _对抗图像样本_。一组 20-50 张已知对抗图像（合成扰动；边缘案例）包含在 eval 中并随时间追踪。

(c) **PR 应产生的产物**：

1. _Eval 报告_ 显式分列视觉特定维度（按分辨率、按视角、按光照、按模态）。
2. _成本预测_ 带每图像和每天数字。
3. _安全分析_ 覆盖上述安全扩展的每项。
4. _标注者一致性表_ 显示每图像类别的 kappa 值。
5. _故障模式目录_ 记录观察到的最差 10 个误分类，含示例图像和提议缓解。
6. _OOD 检测设计文档_ 或对现有文档的引用。

扩展模板比文本版本更长因为视觉功能本质上更复杂。纪律：不因感觉繁琐而跳过部分；故障模式真实且医学影像中的灾难性故障成本大。维持模板纪律的团队可靠发布；侵蚀模板的团队累积风险以事件浮现。
