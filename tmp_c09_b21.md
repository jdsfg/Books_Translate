**净预测。** 到 2030 年部分融合。_应用层_ 区别（RAG 架构 vs. 长上下文架构）变薄因为模型处理更多检索。_工程层_ 区别保持：索引语料库仍是索引语料库；分块器、嵌入模型和索引管道仍需工程关注。改变的是 _谁_ 在查询时做检索决策：从应用程序的检索编排到模型的工具使用。团队仍构建检索基础设施；团队停止手写检索然后生成循环。

更深的观察：**架构经常在用户看到的边界融合，同时在工程层保持独立**。RAG 和长上下文可能从应用开发者视角收敛而不从数据工程师视角收敛。词汇也可能转变；_RAG_ 作为术语可能消退即使底层工程存活，就像 _Web 应用_ 作为区分术语在几乎所有东西都变成 Web 应用后消退一样。
