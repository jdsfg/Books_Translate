* _竞态条件_：使用同步交接（工作器在完成时显式通知编排器而非编排器轮询）；或使用幂等读取加重试。
* _等待超时_：延长超时；如工作器确实比预算花更长时间，解决工作器性能。
* _聚合逻辑_：为聚合函数编写覆盖边缘情况的单元测试；隔离 eval 编排器的聚合。
* _工作器写入失败_：在工作器写入上添加重试逻辑；工作器写入失败时告警；确保编排器注意到缺失写入（而非静默继续）。

元推荐：**eval 必须捕获此问题**。添加系统级 eval 切片验证所有工作器的发现反映在编排器的最终响应中（按 §15.9）。无 eval，复发可能。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我们的编排器-工作器系统有 8% 不完整响应，工作器完成但发现未到达编排器。诊断并提出修复。_ 你验证 (a) 提议的诊断覆盖协调层（LLM 可能默认 _修复工作器_；坚持检查编排器-工作器交接），以及 (b) 提议的修复包含添加系统级 eval（LLM 可能仅建议点修复；坚持回归预防层）。

**Q5.** _何时添加第三个工作器 agent vs 扩展前两个_。你的编排器-工作器系统有两个工作器（研究工作器和综合工作器）。系统处理 80% 的传入任务良好，但 20% 的任务涉及第三个关注点（对照权威数据库进行事实核查），当前两个工作器都不做。你的团队在辩论：添加第三个工作器（fact_check_worker）还是扩展 synthesis_worker 也做事实核查。做决策。指定：(a) 你用来决策的标准；(b) 此案例倾向哪个选项；(c) 被拒选项的权衡；(d) 会改变决策的 eval 证据；(e) 上线计划。

提示

专业化 vs 协调开销；eval 驱动决策。

**答案：**

(a) **决策标准**：

§15.2 的三个相关标准：

1. **事实核查是否受益于专业化？** 专用 fact_check_worker 可有更紧的 prompt、更窄的工具目录（仅事实数据库工具）和聚焦事实核查准确性的 eval。与综合合并可能稀释两者。
2. **事实核查是否与综合并行？** 如事实核查可与研究同时运行且综合可纳入结果，并行性节省墙上时钟。如事实核查依赖综合（有声明后才能事实核查），则是串行的，移除并行性论点。
3. **第三个工作器的协调成本是什么？** 每个额外工作器增加编排器复杂性（更多生成和聚合逻辑）、更多系统级界簿记、更多失败模式（编排器也可能丢失此工作器）。成本真实但有界。

(b) **此案例倾向哪个选项**：**添加第三个工作器（fact_check_worker）**。

理由：
