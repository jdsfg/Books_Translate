1. **检索是有自身故障模式的可测量上游组件。** 通用回答质量量表评分最终响应。那将检索视为黑盒：如果答案错误，量表告诉你它错但不知道原因是 _检索遗漏了相关块_ 还是 _生成忽略了检索到的相关块_。RAG 特定量表将检索 recall 和 precision 作为独立轴添加，将故障分解到拥有它的层。没有那个分解，你盲目调试：回归可能是分块变更、嵌入变更、prompt 变更或模型变更，单一答案分数不给定位信号。

2. **忠实性和引用接地是通用回答量表中缺失的 RAG 特定质量维度。** 通用量表问 _答案正确吗？_ RAG 量表还问 _答案是否接地于检索块，有可追溯引用？_ 忠实性轴捕获一类故障（正确答案但编造理由；正确答案来自训练而非检索），仅正确性量表评分为通过。在受监管领域（医学、法律）未接地答案即使碰巧正确也不合规；RAG 量表使接地与否信号显式。

结构教训：量表特化于架构。纯 LLM 量表有关于回答质量的轴；RAG 量表添加检索和接地；agent 量表（第 14 章）添加轨迹和边界合规轴；多 agent 量表（第 15 章）添加协调轴。第 5 章的多轴模式是框架；每个架构用自己的轴实例化框架。
