#### 计算机使用 agent 如何工作

机制是 agent 循环中的视觉-语言模型：

1. agent 接收目标（"预订 6 月 12 日从 SFO 到 JFK 的航班"）。
2. agent 截取当前屏幕。
3. agent 推理下一步做什么，给定屏幕状态。
4. agent 发出行动（点击坐标 (x, y)；输入文本；按键）。
5. 行动执行；屏幕更新；回到步骤 2。

循环持续直到目标达成或 agent 停止。视觉模型处理屏幕理解；agent 的推理处理任务规划。

#### 计算机使用在 2026 年生产中的适用处

三个生产适配：

**一次性自动化**。用户有特定重复任务（"从这个网站下载我所有收据为 PDF"）不值得编写 API 集成。计算机使用是正确答案：它适用于任何有 UI 的网站；替代是手工工作。

**无障碍应用**。难以操作计算机的用户（视觉障碍、运动障碍）受益于代其行动的计算机使用 agent。可靠性门槛比高量生产低（替代是 _完全没有自动化_）；一定失败率可接受。

**长尾集成**。企业有 20 个内部系统。为全部 20 个构建 API 集成昂贵；计算机使用让 agent 直接操作系统的 UI。权衡：比 API 集成更低的可靠性，无系统更新破坏 UI 的维护开销。

#### 计算机使用在生产中不适用处

**可靠性重要的生产关键工作流**。2026 年计算机使用失败率通常每任务 10–30%（因任务复杂性和具体 UI 而异）；即使窄范围良好界定的用例也在 20–30%（见 §15.7 Helios 的工作示例：刻意窄范围的供应商门户任务 26% 失败率）。对于失败代价高的工作流（金融交易、面向客户的行动），失败率不可接受；真正 API 集成是正确答案。

**高量自动化**。计算机使用的每任务成本显著高于 API 集成的每任务成本（每步更多推理成本；需要更多步骤）。在规模上，成本差异主导。

**需要详细审计的任何事**。计算机使用轨迹难以精确重现；UI 元素移动；状态不同。对于需要审计轨迹的工作流，API 集成更干净。

2026 年生产 AI 启发式：**如能为任务构建真正 API，就做那个；计算机使用是最后手段**。启发式由失败率和成本现实塑造；它将随计算机使用模型改善而演化，但截至 2026 年规则成立。
