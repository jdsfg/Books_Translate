**答案：** 首先，数学。当前月度成本：40M×480/10⁶×0.30+40M×40/10⁶×1.50=5,760+2,400=8,160 美元。目标：从月度成本中削减约 2,660 美元；主导杠杆是输入 token 因为在此 prompt 形状下它们以约 2.4 倍超过输出。

**变体 A：激进输入压缩**（目标 260 输入 token）。削减角色框架、将指令折叠为项目符号、丢弃少样本示例并依赖模型的强基线分类器行为。
* 预测 eval：0.80 到 0.83（大部分损失来自移除少样本锚点；分类对简洁框架稳健但少样本示例携带边缘案例信号）。
* 预测成本：40M×260/10⁶×0.30+40M×40/10⁶×1.50=3,120+2,400=5,520 美元。在预算内。
* 风险：在 ≥0.82 下限边界。Eval 必须在提交前运行。

**变体 B：保留一个少样本，压缩散文**（目标 340 输入 token）。保留一个精心选择的少样本示例（来自你 eval 失败分析的最高信息边缘案例）；压缩周围指令。
* 预测 eval：0.83 到 0.85（一个精心选择的少样本恢复完整少样本的大部分提升；如果少样本做行为工作，指令压缩基本免费）。
* 预测成本：40M×340/10⁶×0.30+40M×40/10⁶×1.50=4,080+2,400=6,480 美元。_超预算 980 美元。_
* 杠杆：通过切换到结构化单 token 标签格式减少输出 token（将输出削减到约 5 token，回收 2,100）。合计：4,080+300=4,380。在预算内并有 1,120 美元余量。

**变体 C：用原始 prompt 换模型**（工作马到小模型）。从 Sonnet 级移到 Haiku 级使用相同 prompt；约前沿成本的 30%。
* 预测 eval：0.76 到 0.81 取决于任务复杂度。_低于典型分类的 ≥0.82 下限。_ 落出预算和质量可行区域。
* 预测成本：远低于预算但质量不达标。

**推荐：变体 B 加输出格式变更。** 最佳预期 eval（0.83 到 0.85），在输出 token 削减后舒适地在预算内，且少样本示例提供调试信号（当分类在类似输入上失败时，少样本本身是诊断锚点）。如果 B 的 eval 低于下限，变体 A 是后备；变体 C 是错误答案因为它为不需要的预算余量牺牲质量下限。模式：在伸手拿模型切换之前在 prompt 上做成本-质量优化（对比第 5 章的层级决策；这里我们有自由留在更高层级因为 prompt 重设计赚到了预算）。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
