2. _评判 prompt 变更需显式标签_。注册表提交必须标记 `[JUDGE-PROMPT]` 前缀；CI 检查此前缀并触发基线比较。无标签的变更被阻止。
3. _季度伪影演练_。每季度一次，在 staging eval 上故意引入仅测量漂移并验证值班团队在升级前运行伪影优先分诊。建立肌肉记忆。

教训：生产 eval 是一个系统；系统的测量层需与被测系统相同的纪律。没有此纪律，团队的质量信号以随时间侵蚀信任的方式变得不可靠。这连接到第 19 章 §19.5 的更广原则：_可观测性本身是一个可失败的系统_；eval 基础设施同样如此。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_调试一个结果是测量伪影的漂移告警（评判 prompt 被静默变更）。覆盖调查、纠正措施、流程变更。_ 你验证 (a) 调查从伪影优先分诊开始而非跳到系统漂移假设（LLM 可能默认 _最近部署导致的_；坚持先检查测量层），以及 (b) 流程变更防止复发而非仅道歉（CI 门控和标签约定，非仅"更小心"）。

**Q6.** _为新 agent 类型设计金丝雀标准_。你的团队准备部署一个新的 _合同审查 agent_（多步：提取条款；标记风险；建议编辑）到生产。agent 是全新的（非模型升级；非现有 agent 的 prompt 变更）。设计金丝雀标准：(a) 监控哪些信号；(b) 提升到下一流量步骤的阈值；(c) 立即回滚的阈值；(d) 金丝雀期间的人在回路组件。

> _提示_：新 agent 金丝雀比模型升级金丝雀更难因为没有先前基线。标准必须混合离线 eval 预测、运维健全性和随信心增长而衰减的人工抽查密度。

**答案：**

(a) **金丝雀期间监控的信号**：

1. _生产 eval 通过率_：按 agent 预建的 eval 评分标准（提取完整性、风险标记精确率/召回率、编辑建议接受率）。
2. _按维度分数_：分别追踪三个子任务。新 agent 可能总体通过而在对特定客户群体最重要的单一维度上回归。
3. _每轨迹成本_：新多步 agent 常有成本意外（循环；过度检索）。追踪成本分布而非仅均值；关注长尾高成本轨迹。
4. _延迟 p50 和 p99_：合同审查非实时，但审查者的工作流期望在可容忍窗口内响应。
5. _审查者接受率_：当人工审查者接受或修改 agent 的建议时，那是最强生产质量信号。追踪接受率和修改率。
6. _安全信号_：agent 是否尝试跨租户数据访问？是否泄漏 PII？是否在高风险条款上产生自信但错误的风险标记？
7. _运维异常_：工具调用失败率；重试率；agent 循环上限命中。
