#### 量表（六轴）

**检索轴**：

1. **检索 recall@10**（范围 0–1）：查询的标注相关块中有多少在 top-10 检索中？组件级指标；需要标注相关性。
2. **检索 precision@5**（范围 0–1）：top-5 检索块中有多少标注相关？捕获 _太多离题块_ 故障模式。

**回答轴**（由 LLM-as-judge 评分 0–3，对照人工校准）：

3. **忠实性**：答案是否从检索块得出？0 = 答案与检索块矛盾；1 = 答案超出检索块；2 = 答案主要接地但有少量不支持声明；3 = 答案完全接地。
4. **回答相关性**：答案是否回答用户问题？0 = 答案离题；1 = 答案勉强相关；2 = 答案回答了问题但不完整；3 = 答案完全回答问题。
5. **引用接地**：答案中的声明是否引用到具体块？0 = 无引用；1 = 引用存在但错误或模糊；2 = 大部分正确引用；3 = 每个声明都引用且引用精确。
6. **有害输出率**（二元，0–1）：答案是否包含幻觉事实、编造引用、不安全建议、或会让公司尴尬的内容？

聚合：total = retrieval_recall@10 + retrieval_precision@5 + (faithfulness + answer_relevance + citation_grounding) / 3 + (1 - harmful_output_rate)。前两轴各 0–1；三个回答轴各 0–3，平均为 0–3 贡献；有害率 0–1。范围 0–6；Strata 的生产阈值 ≥ 4.6（约 6 分上限的 0.77）。
