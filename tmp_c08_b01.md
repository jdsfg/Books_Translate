模型的默认输出是散文。有时是优美的散文；通常是格式良好的散文；对于聊天界面几乎总是有用的散文。但你的下游代码不消费散文。你的下游代码消费数据：字典、记录、表中的行、函数调用的参数。_模型产生散文_ 和 _我的代码需要数据_ 之间的差距就是结构化输出填补的差距。

这听起来像一个小问题。它不是。结构化输出，以大多数生产系统实际需要的形式，是 AI 工程中最密集的工程表面之一。它涉及 schema 设计、验证纪律、错误恢复、prompt 级指导、原生 API 与库包装器之间的选择，以及当模型产生可解析但错误的 JSON 时怎么办的问题。做对了你的管道坚如磐石；做错了你在生产中有一堆崩溃循环。

本章走现代技术栈：原生 JSON schema 模式、用于 Pydantic 类型输出的 `instructor`、用于 logit 级约束生成的 `outlines`、作为结构化输出特殊情况的工具调用，以及特定于此层的故障模式。本章的绑定交付物是工作流 #3：一个生产级 Pydantic + `instructor` + 原生 JSON schema 管道，带 `tenacity` 重试和判别联合 schema。

### 7.1 为什么结构化输出不是可选的

三个动机：

**程序化消费者。** 你的代码读取模型输出并基于它分发。如果输出是 _包含答案的散文_，你的代码做脆弱的字符串解析。如果输出是 _带命名字段的 JSON 对象_，你的代码做字典访问。前者在模型每次改变措辞时都断；后者仅在 schema 变化时断（而你可以用验证检测 schema 变化）。

**Eval 纪律。** 评估结构化输出是直接的（字段级比较、验证、特定元素的精确匹配）。评估散文是困难的（LLM 作为评判者、量表评分、第 4 章的所有复杂性）。强制结构进入输出简化 eval，这提高了你可以自信发布的门槛。

**成本和延迟。** 结构化输出通常比等效散文更短。一个逐字段 JSON 对象编码 _严重性：高；受影响文件：auth.py；建议修复：使用环境变量_ 比传达相同信息的代码审查散文段落更短。以 5 倍输出成本乘数（第 3 章），这是真实的成本杠杆。

发布 LLM 功能但不认真对待结构化输出的团队在解析层用工程时间、用 eval 复杂性、用每次调用的 token 成本来支付。这些都不规模化。
