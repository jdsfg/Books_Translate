### 3.3 采样参数：何时转动每个旋钮

从第 2 章你知道 temperature、top-p 和 top-k 做什么。以下是工程师实际何时从默认值改变每个参数。

**Temperature。** 大多数生产代码中的默认值是 **0.0 用于你想要一致性的任务**（CI 中的代码生成、结构化提取、分类、针对检索上下文的事实问答）和 **0.7 到 1.0 用于你想要多样性的任务**（创意写作、头脑风暴、生成多个候选以供选择）。中间范围（0.2–0.5）有时用于在不失去可靠性的情况下引入少量变化（例如，一个你不希望对相同 prompt 产生字面相同回答的聊天助手）。选择是任务特定的；默认为 0，只在有理由时改变。

**Top-p。** 默认 1.0（无截断）或 0.95（轻微截断）对几乎所有用途都合适。降到 0.5 或 0.8 减少模型发射极不可能 token 的机会；这偶尔作为 _安全_ 杠杆有用（更少意外输出），但它以不明显的方式与 temperature 交互。经验法则：一次只转一个旋钮（temperature）。

**Top-k。** 在 2026 年基本废弃。Anthropic 暴露它；OpenAI 不暴露；你很少会碰它。在探索性开发期间如果你想与 top-p 比较则设置一次；否则省略。

**Stop sequences。** 每当你有一个应终止生成的分隔符时设置这些。两种常见模式：

* _带闭合分隔符的结构化输出_：要求模型发射以 `}\n###END###` 结尾的 JSON 对象，并将 stop sequence 设为 `###END###`。模型在发射分隔符时立即终止；你不为尾部 token 付费。
* _带最终答案标记的 chain-of-thought_：让模型大声思考，然后发射 `Final answer:`，然后是答案；如果你只想要 _Final answer:_ 之前的所有内容，将 stop sequence 设为 `Final answer:` 并解析响应。

Stop sequences 是一个小但真实的成本杠杆。一个模型原本会填充到 500 token 的响应在正确的 stop sequence 下通常在 250 结束。5 倍的输出对输入成本比（§2.10）意味着 250 token 的输出节省比同样的输入节省更具成本效益。

**max_tokens。** 始终显式设置。大多数 provider 的默认值很慷慨（4K 或 8K），失控的生成会让你付出代价。代码审查响应不应需要超过 1000 token；设上限。结构化输出提取可能需要 2000；设上限。长篇论文可能需要 4000；设上限。max_tokens 是 _预算_，不是 _目标_；模型通常发射比上限更少的 token。
