#### 反模式 6：工作流足够时构建 agent

**模式**：对简单确定性工作流能更好处理的问题伸手多 agent 或 agentic 架构。将"agentic"视为本质上比"工作流"更高级。

**为何失败**：agent 增加工作流没有的复杂性（有界工程、可观测性、故障模式）。对可分解为已知步骤的任务，工作流比 agent 更可预测、更便宜、更易调试。两个线索 B 故障在不同规模锚定此反模式。5.2 万美元隔夜 agent 事件（线索 B 故障 5，第 14 章）是无界循环案例：一个无迭代上限、成本断路器或工具调用去重的单 agent 运行了 14 小时才被注意。2026 年 agentic 递归事件（线索 B 故障 6，第 15 章）是多 agent 案例：一个编排者加工作者系统收敛到错误中间答案并花重试循环翻倍 token 消耗才被注意。两者是同一类故障——一个无确定性工作流本会隐含的有界的 agentic 系统——在不同架构规模上浮现。

**论述章节**：第 14 章（有界 agent，以及是否构建 agent 的更广问题）。第 15 章（生产现实是编排者-工作者在大多数情况下胜过多 agent；许多任务两者都不需要）。

**终止标准**：如你能将任务指定为确定性步骤序列且每步可能一个 LLM 调用，你有工作流；不要 agent 化它。如你确实有真正 agent，{迭代上限、成本断路器、工具调用去重、观察验证}的每一个必须在 agent 见到生产流量前就位。
