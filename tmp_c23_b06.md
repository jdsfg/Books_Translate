### 22.6 设备端与边缘推理

一类特定的自托管：在 _用户设备_（手机、笔记本、边缘服务器）上运行推理。2026 年格局：

**Apple Intelligence**：Apple 的混合设备端 + Private Cloud Compute 栈，用于 iOS/macOS。较小的常规任务（摘要、改写、设备端 Siri）在 Apple Silicon 上本地运行；更重任务回退到 Apple 的隐私保护云（Private Cloud Compute）；更重或开放式请求可在用户明确同意下路由到第三方模型（如 ChatGPT）。架构以隐私为中心但非严格设备端；设备端部分是真实且大量的。

**用于边缘的小型开放权重模型**：Phi-4（Microsoft，约 14B 但为边缘重度优化）；Gemma 3（Google，2-27B 尺寸包括边缘友好型）；Llama 3.2（Meta，1B 和 3B 尺寸专为边缘）。

**边缘推理框架**：

* **MLX（Apple）**：为 Apple Silicon 优化；与 iOS/macOS 开发集成。
* **llama.cpp**：跨平台；在消费硬件上运行；主导的开源边缘推理工具。
* **Ollama**：llama.cpp 的开发者友好封装；一键本地模型部署。

边缘推理在四种形态中找到生产用途：

**隐私敏感应用**：发送到云不理想的个人数据任务（个人语音备忘转录；设备端助手）。Apple Intelligence 是领先的消费者示例。

**离线/不可靠连接**：必须在无互联网下工作的应用（野外工作者；车辆；远程位置）。边缘推理提供功能能力。

**延迟关键**：到云的往返增加 50-200ms；对一些实时应用（AR；游戏；语音反馈循环），边缘更快。

**消费者规模下的成本经济**：当应用有数百万用户且每用户多次调用，累积云成本大；边缘将成本移到用户硬件（他们反正已付费）。

边缘对特定用例是真实生产能力，非通用替代。大多数生产 AI 仍在云端；边缘是适合案例的专门选择，且能力增长足够快使 2030 年可能随模型效率改善看到更广泛的边缘部署。

#### 一对可分离的选择：模型家族 vs 部署

团队常将 _开放权重_ 等同于 _自托管_，但两个是独立决策。开放权重模型在托管 API 后运行良好（Together、Replicate、Anyscale）；闭源模型偶尔可在特定供应商协议下自托管（不常见；大多数提供商不允许）。因此实际上有两个按顺序的选择：_哪个模型家族_（开放权重 vs 闭源）和 _哪种部署_（托管 API vs 自托管）。各自按自身优点决策。
