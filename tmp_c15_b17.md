文档比 Helios 的短因为 agent 任务更受限（单轨迹、无状态变更工具）。结构转移；细节适合 Beacon 的临床安全需求。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为临床笔记摘要 agent 编写有界 agent 设计文档：界、记忆、策略门控、eval 套件。_ 你验证 (a) 界匹配同步临床医生等待的工作流（不接受交互用例的多分钟轨迹），以及 (b) 安全架构保守（无自动插入 EHR；需临床医生审查）。

**Q2.** _在生产中检测螺旋 agent_。你值班。一个 agent 运行了 8 分钟。正常轨迹在 30 秒内完成。设计告警和响应。指定：(a) 查询什么数据；(b) 决策树；(c) 响应行动；(d) 事后审查。

**答案：**

(a) **查询的数据**（通过第 19 章可观测性）：

* 当前轨迹 ID、agent 版本、客户/会话 ID。
* 到目前为止完成的迭代；到目前为止的成本。
* 最近工具调用（最后 10 个）：工具名、参数（净化）、结果。
* LLM 调用时间戳（看是卡在推理还是在进展）。

(b) **决策树**：

1. 轨迹是否触及界（迭代上限、成本上限）？如是，agent 应自行停止；检查为什么界强制未启动。
2. 最近工具调用是否重复（相同工具、相似参数）？如是，断路器应已触发；检查为什么没有。
3. 工具调用是否失败？如多次失败，工具依赖可能宕机；检查下游服务。
4. agent 是否在进展（不同工具、不同参数）但轨迹自然较长？如是，可能是合法但边缘的轨迹；让它完成但标记审查。

(c) **响应行动**：

* _手动停止_：如轨迹明显螺旋，发送停止信号（每个生产 agent 应有外部停止端点）。agent 以优雅错误退出。
* _调整界_：临时降低 agent 类的迭代上限，直到界强制被调查。
* _通知_：寻呼 AI 工程值班（如你不是他们）；开始事故工单。
* _客户影响缓解_：如客户受影响（其工单已挂起 8 分钟），升级到人工 agent 接管。

(d) **事后审查**：

* 根因分析：为什么界强制失败？是代码 bug、配置错误、还是与模型行为的未预见交互？
* Eval 覆盖：添加重现螺旋模式的回归测试。验证界在 eval 中启动。
* 更新设计文档以反映任何新界参数。
* 事故复盘按第 23 章纪律内部发布。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
