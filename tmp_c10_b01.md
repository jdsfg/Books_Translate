2024 年 5 月，Google 推出了 _AI Overviews_——搜索结果页面顶部的 LLM 生成摘要。几天内，截图流传显示系统告诉用户在披萨中加无毒胶水防止奶酪滑落，以及每天至少吃一块小石头。指令清晰、自信、荒谬。

来源最终被追踪。系统在做检索增强生成。它从开放 Web 检索；Web 包含 Reddit 上一篇帖子有人开玩笑建议在披萨酱中加胶水；模型将检索内容视为事实并合成了自信答案。

这是 2020 年代典型的 RAG 故障故事，它携带的教训是本章围绕构建的：**检索质量决定 RAG 质量。LLM 在下游。如果你检索错了，你生成错了。**

这是第四部分（第 9–12 章）的开始。第 9 章构建架构。第 10 章工程化杠杆（嵌入、向量数据库、混合搜索）。第 11 章添加高级模式（重排序、查询重写、评估）。第 12 章扩展到多模态。到最后，你应该能设计、构建、评估和运营 Strata Research 规模的生产 RAG 系统。你还将理解 2024 年 5 月 Google 出了什么问题以及什么会捕获它。

### 9.1 为什么 RAG：三个动机

RAG 解决的三个问题，每个基于第 2 章的心智模型。


#### 动机 1：知识截止

模型的训练有截止日期。之后发生的任何事（通过的法规、价格变更、发布的版本）模型不知道。对于答案依赖截止后信息的任何生产系统，模型单独不够。

RAG 通过 **在查询时从外部索引检索新鲜信息** 并将其插入模型上下文来解决此问题。索引独立于模型更新；模型的有效知识成为索引中的内容加上预训练中的内容。

对于 Strata Research：法律答案依赖法规和案例法的当前状态。2024 年训练的模型不知道 2025 年通过的法规。在新鲜度维护的法律语料库上的 RAG 弥合差距。

#### 动机 2：你的数据

模型的训练不包含你的私有数据：你的公司 wiki、你的客户记录、你的专有代码库、你的患者病历。模型对这些一无所知，除非你放入上下文。

RAG 通过 **在你的私有数据上构建索引** 并在查询时从中检索来解决此问题。模型在其上下文窗口中看到你的数据；模型的答案基于你的数据。

对于 Beacon Health AI：患者特定的临床摘要需要患者历史，模型在预训练中无从知晓。在患者病历上的 RAG（带 HIPAA 感知基础设施）提供它。
