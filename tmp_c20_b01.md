传统应用性能监控（APM）——Datadog、New Relic、Honeycomb——解决一个明确定义的问题：当请求到达后端时，它的生命周期是什么，时间花在哪里，遇到了什么错误？数十年来这些工具对后端工程足够。

LLM 系统打破了 APM。对 LLM 系统重要的问题不是 _请求在 HTTP 层面是否成功_ 而是 _prompt 是什么，模型输出了什么，答案质量是否可接受，agent 调用了什么工具，成本是多少？_ 传统 APM 不知道要追踪这些。这些问题需要新层。

这就是 LLM 系统的可观测性：追踪调试、监控和改进 LLM 驱动产品所需内容的纪律。2026 年的栈已围绕三个命名入选者稳定——Langfuse、Helicone、Phoenix（Arize）——与现有 APM 工具并存。绑定工作流 #12 是 Langfuse 追踪范例，展示生产 LLM 系统追踪实际样子。

Beacon Health AI 贯穿本章。HIPAA 可观测性张力是真实的：追踪工具存储 prompt 和响应，对临床流水线意味着存储 PHI。本章走查 Beacon 的解决方案：在 HIPAA 合规基础设施上自托管 Langfuse 部署，在追踪摄取层做 PHI 脱敏；保留调试信号而不不当暴露受监管数据的可观测性。

### 19.1 为什么传统 APM 不够

Datadog 告诉你请求耗时 800ms 并返回 HTTP 200。它告诉你请求依次命中端点 A、B、C。它告诉你端点 B 中的数据库调用耗时 80ms。这是真实有用的信息。

Datadog 不告诉你：

* _系统 prompt 是什么？_（prompt 是使响应成为其样子的东西）
* _实际调用了什么模型？_（在路由系统中，模型每调用不同）
* _模型输出了什么？_（文本内容；不仅是元数据）
* _模型调用了什么工具？_（请求内的中间工具调用）
* _成本是多少？_（token 计数和成本计算）
* _此调用的 eval 分数是什么？_（答案是否实际正确？）
* _通过 agent 循环的轨迹是什么？_（多轮行为持续多秒）

这些是调试 LLM 系统的重要问题。传统 APM 默认不捕获它们。团队必须要么：

1. 通过大量自定义插桩将 LLM 特定数据添加到 APM（技术上可能；通常不实际）。
2. 采用专为这些问题构建的专用 LLM 可观测性工具。

2026 年规范：专用 LLM 可观测性工具，通常与传统 APM 并存（每个工具处理其层）。专用工具自动包装 LLM 调用；追踪流入专用后端；UI 为 LLM 调试工作流设计。
