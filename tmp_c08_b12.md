### 章节小结

结构化输出是 LLM 默认散文行为和下游代码按需数据消费之间的工程层。2026 年，现代技术栈是：provider API 上的原生 JSON schema 模式（底层约束解码）；`instructor` 用于 Pydantic 类型 Python 包装器带验证自动重试；`outlines` 用于自托管或不寻常约束案例。工具调用在结构上与结构化输出相同；schema 是工具的参数签名。

判别联合是 _优雅失败模式_ 的基础：模型有类型化退出坡道（`kind: "error"`）而非对不可回答问题幻觉答案。Schema 应暴露模型不确定性（置信度分数、不清晰元素标志、错误变体）而非隐藏它。

特定于此层的三个故障模式：schema 太刚性（模型无法表示有效输出）；schema 太宽松（验证错误内容）；语义但非语法错误（可解析，但无意义）。前两个是 schema 设计问题；第三个需要超越验证器的 eval 纪律。

绑定工作流 #3（§7.9）是生产级制品：Pydantic v2 带字段验证器和判别联合；`instructor` 包装 provider 客户端；通过库的原生 JSON schema 模式；`tenacity` 在 API 错误上的外部重试；`instructor` 在验证错误上的内部重试；prompt 中的不可信内容标记用于基本注入防御。

Schema 级约束补充第 6 章的 prompt 级约束；工具使用（第 13 章）在结构上是相同问题；agent（第 14 章）是每步都是结构化的情况；输出验证在第 23 章作为安全层再次出现。

如果你从本章取一个综合建议：**`instructor` 加原生 JSON schema 模式加判别联合，schema 设计为显式暴露模型不确定性而非隐藏它。** 每个部分可替换；合在一起它们是 2026 年默认。

### 7.X 练习题
