前几章构建了生产 AI 的能力和运维层。本章覆盖保护这些层免受滥用、攻击和优雅失败的内容：安全和可靠性。AI 系统 _防御性工程_ 的纪律。

威胁是真实的。线索 B 失败 3（2025 年生产中的 prompt 注入事件）在本章锚定：一个企业客户服务 agent 通过间接 prompt 注入被诱骗泄漏客户数据。该事件塑造了全行业实践；涌现的四层防御模式是绑定工作流 #15 捕获的内容。

本章的工作是区分可发布给客户、部署在受监管行业、暴露于对抗输入的 AI 系统——与不能的系统。跳过此工作的生产 AI 最终会出事件；投资于此的生产 AI 吸收事件类别并随时间改进。

### 23.1 成本断路器和 token 预算护栏

第一道防线：有界资源消耗。即使不考虑对抗威胁，生产 AI 需要硬限制以防止失控行为。

**成本断路器**（第 14 章 §14.4 为 agent 引入，此处泛化）：

* _每调用成本上限_：单次 LLM 调用不能超过 X 美元（通常 0.50-2 美元取决于风险）。
* _每轨迹成本上限_：agent 轨迹在 X 美元时停止（第 14 章）。
* _每租户日上限_：租户日支出不能超过 X 美元（第 17 章）。
* _每功能月上限_：特定功能支出不能超过 X 美元。

实现：路由层中的中间件（第 17 章）在调用前估算成本，超上限则阻止，调用后记录实际成本。上限是硬限制；用户/agent 获得结构化错误并调整。

**Token 预算护栏**以绝对 token 限制补充成本上限：

* _每调用 max_tokens_：每次 API 调用有显式上限（第 3 章）。
* _每轨迹总 token_：跨 agent 轨迹的累积 token 不能超过 N。
* _每消息上下文预算_：总上下文窗口使用不能超过模型窗口的 N%。

这些是第一道防线。它们不解决对抗意图但防止任何原因（有意、意外或失控）的无界资源消耗。
