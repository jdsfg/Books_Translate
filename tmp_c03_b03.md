#### 工程师需要从中带走什么

有几件事对本书的其余部分是承重的。

**模型有一个知识截止日期。** 训练数据在某个日期之前收集，模型对那个日期之后的事情一无所知。（如果截止日期后的事件在后来的训练数据中被引用，模型可能吸收了一些信号，但你不能依赖它。）第 9 章（RAG）在某种意义上就是通过将截止日期后的信息直接放入输入序列来获取的学科。

**模型知道你的数据仅当你的数据在它的训练集中。** 你的内部公司 wiki 可能不在训练集中。你的专有代码库可能不在。Beacon Health AI 的临床笔记肯定不在（PHI 不在公共网络上）。这是 RAG 的第二个动机。

**模型的行为反映训练语料库的经验分布。** 互联网是主要来源。模型里有 Reddit；有 Wikipedia；有论坛帖子里的坏答案；有高质量教科书。没有进一步训练，模型会生成所有这些的统计加权平均。这就是为什么仅预训练不足以产生一个有用的聊天产品。下一步（RLHF / DPO）是让模型优先产生好教科书风格而非坏论坛风格的东西。

**模型没有 _现在_ 的概念。** 它不知道现在几点。它不知道自己是哪个模型版本。它不知道 Anthropic API 的价格是多少。那些看起来显而易见的 _依赖于模型运行时上下文_ 的世界事实必须通过 prompt 提供给它。（试着问模型现在是哪一年。诚实的答案是 _我不知道；这取决于你什么时候问我，而我无法感知那个_。许多模型会猜测，通常基于截止数据，而猜测会是错的。）
