**答案：** 有用的 prompt：_我在为 LLM 驱动的临床分诊助手设计 golden set。输出是三向紧急程度标签。推荐规模、分层轴、标注协议、标注者间一致性目标和审查节奏。_ 你通过 (a) 检查提议的分层是否覆盖你的临床环境中重要的东西（LLM 可能不知道你的特定专科分布；你提供它），以及 (b) 检查标注协议与监管环境的交互（如果数据来自研究背景的 IRB 审查；跨团队标注的 HIPAA 约束）是否可行来验证。LLM 擅长结构；你提供领域特定约束。

**Q2.** _校准 LLM 评判者_。你有一个 LLM 评判者（GPT-4o）在四轴量表上评分输出，总可能 12 分。你想验证评判者与人工一致后再在生产中使用其分数。设计校准：(a) 样本量；(b) 评分流程；(c) 一致性指标；(d) 可接受阈值；(e) 如果评判者未通过校准怎么办。

提示

你需要足够样本以获得统计信号但不要多到校准过于昂贵。对于序数分数，使用加权 kappa 或 Spearman 相关。

**答案：**

(a) **样本量：60 个案例**，作为生产 golden set 的分层子集抽取（覆盖每个量表维度的范围）。小于 40 统计上薄弱；大于 100 昂贵且无比例收益。60 足以在量表维度级别检测有意义的不一致。

(b) **评分流程**：每个案例由 (i) LLM 评判者使用生产量表 prompt 评分；(ii) 两位独立人工使用相同量表评分。人工在评分前不应看到 LLM 分数。LLM 评分对同一案例运行两次（如果适用，用不同顺序或随机化 prompt 顺序）以测量评判者自身一致性。

(c) **一致性指标**：

* _评判者 vs 人工（每维度）_：加权 Cohen's kappa 或 Spearman 相关，因为量表是序数的。
* _评判者 vs 聚合人工分数_：相同指标，对两个人工评分者的平均。
* _评判者自一致性_：两次评判者运行之间的 kappa 或相关。
* _人工 vs 人工_：加权 kappa，作为这个量表中 _良好一致性是什么样的_ 的基线。

(d) **可接受阈值**：

* 评判者 vs 人工 κ ≥ 0.7 每维度；≥ 0.6 可接受但带标记和改进计划。
* 评判者自一致性 ≥ 0.85（评判者应与自己一致）。
* 评判者 vs 人工不应比人工 vs 人工低 0.1 以上（评判者可以比人工稍嘈杂，但不能差太多）。

(e) **如果评判者未通过校准**：
