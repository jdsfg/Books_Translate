**答案：** 视觉 token 通常定价比文本 token 高（Anthropic Vision 2026 年约 3 美元/1M token；每页视觉 token 使用 1000–2000 token；仅视觉输入每页成本 0.003 美元–0.006 美元）。在数百万页的摄取规模下，累计成本达数万美元；在查询规模下（如检索时使用视觉），每查询成本累积。

三个管理举措：

1. **分辨率控制**：高分辨率页面图像使用更多视觉 token。许多文档 RAG 任务不需要最大分辨率；降采样到保持文本可读的级别（打印文本通常 200–300 DPI）显著减少 token 使用。从 600 DPI 降到 200 DPI 的页面可能使用一半视觉 token 而任务无质量损失。
2. **批处理**：API provider 提供约 50% 定价折扣的批端点。文档摄取通常异步（用户不等摄取）；使用批处理。为摄取密集工作流削减视觉成本一半。
3. **页面级缓存和增量处理**：如页面在文档版本间未变（法律文档经常以小幅修订重新版本化），跳过重新处理。哈希页面图像；如哈希匹配先前处理的页面，复用先前提取。同时节省视觉 token 成本和摄取延迟。

第四个举措：**按重要性分阶段摄取**。高价值文档（频繁查询的合同；近期法规）以全分辨率视觉优先处理；低优先级文档获得更低分辨率处理或经典 OCR 回退。成本分配到工程价值最高的地方。

**Q4.** 为什么 _签名_、_表格_ 和 _布局敏感_ 查询比标准文本查询对多模态处理敏感得多？参考 §12.5 的 Strata 数据。

**答案：** 标准文本查询（例如"此合同关于赔偿怎么说？"）检索作为文本存在的内容。纯文本 RAG 能胜任处理此因为相关内容存在于文本提取捕获的散文中。

三个敏感类别有不同结构特征：

* **签名**根本不在文本中。签名是视觉产物——页面上的手写标记。纯文本摄取要么完全遗漏签名，要么记为"[签名]"而无更多细节。像"此合同是否由双方签署？"或"找到 Jane Doe 签署的合同"的查询无法被纯文本 RAG 回答；多模态处理可识别和定位签名，恢复 Strata 看到的 +59 分提升。
* **表格**部分在文本中但丢失结构。OCR 的表格内容是没有列行结构的数字和词序列。像"此 MSA 的合同总值是多少？"的查询需要值与正确单元格关联；纯文本 RAG 返回关联丢失的扁平序列。视觉优先摄取将表格结构保留为 markdown 或结构化 JSON；查询可使用结构，恢复 +36 分提升。
* **布局敏感查询**依赖视觉组织：什么内容在页眉，什么在边距，什么在第 7.4 节。纯文本摄取丢失布局；一切变成扁平文本序列。视觉优先摄取保留布局；依赖"找到赔偿节中的条款"的查询可使用结构。
