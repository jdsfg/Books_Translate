本章中提到的每个开放权重模型名称（Llama、Mistral、Qwen、DeepSeek、Phi；7B/13B/70B 与 1B/3B）和每个推理引擎名称（vLLM、TGI、Modal、Together AI、Anyscale、Replicate；MLX、llama.cpp、Ollama）截至 2026 年末是当前的，到你做决策时需要对照开放权重排行榜做健全性检查。三个决策本身——是否微调、是否自托管、是否设备端 vs 云端——独立于哪个模型赢得今年的季度。

第 1-21 章假设了 2026 年的主导部署模式：调用托管前沿或工作模型的 API。对大多数生产 AI 团队，这是正确选择。但不总是。一些用例受益于超越现成方案：微调模型以适应特定任务、自托管开放权重模型、将推理部署到用户设备。

本章是这些选择的压缩决策框架。服务基础设施的深度（vLLM 内部；量化技术；生产 GPU 扩展）推迟到未来的 ML 平台书籍；_这些选择何时适用_ 的工程判断是本章覆盖的内容。

线索 B 失败 4（微调弃用级联）在此锚定。一个团队的 GPT-4 微调在第一天就变得比 GPT-5 基础模型更差；维护负担令他们惊讶。教训——微调有半衰期——塑造了本章大部分立场。

### 22.1 何时微调有意义

微调取一个预训练基础模型并在任务特定数据上继续训练以产生专用模型。2026 年，微调通常通过 LoRA（低秩适应）或相关技术完成，在冻结的基础权重之上更新小适配器。

微调适用的情况：

**1\. 特定领域或格式**：任务要求以基础模型持续出错的特定格式输出：例如，基础模型即使通过 prompt 工程也不可靠遵循的结构化医疗笔记格式。微调将格式内在地教给模型。

**2\. 特定领域语言**：基础模型处理薄弱的技术词汇领域任务。专业医疗、法律、科学上下文可能受益于领域调优模型。（通常：专用嵌入 + 基础模型 + RAG 无需微调即可处理此问题；先检查此路径。）

**3\. 延迟敏感应用配合更小模型**：更小的微调模型可能在特定任务上匹配更大的通用模型，而延迟和成本仅为一小部分。对高量专业任务有用。

**4\. 风格或人设一致性**：品牌特定语气、角色声音、正式语域。微调将风格烘焙到权重中比仅 prompt 方法更可靠。

微调不适用的情况：

**1\. 任务由基础模型配合良好 prompt 很好处理**：大多数通用任务。当 prompt 工程 + 结构化输出 + RAG 足够时不要微调。

**2\. 任务需要最新信息**：微调与基础模型有相同的知识截止（加你的训练数据）；它们不解决新鲜度问题。用 RAG 代替。

**3\. 团队缺乏持续维护能力**：微调有半衰期（§22.2）。没有对基础模型演进时持续重新调优的承诺，微调累积技术债务。
