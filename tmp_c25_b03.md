### 24.3 AI 代码的代码审查实践

AI 功能的代码审查不同于经典代码审查。经典审查关注正确性、安全、性能。AI 审查增加：

**Prompt 审查**：每个 prompt 变更审查清晰度、结构、eval 影响（如可用）。审查者寻找：可能混淆模型的模糊性；缺失的安全约束；成本影响（长度）；模型特定假设。

**Schema 审查**：结构化输出 schema 审查完整性、错误变体处理（第 7 章的可辨识联合）、下游消费者兼容性。

**Eval 覆盖审查**：当功能变更时，eval 是否覆盖变更？如添加新代码路径，是否被 eval 切片行使？如假设新故障模式，是否被测试？

**成本影响审查**：变更如何影响成本？更精细的 prompt 成本更高；额外工具调用使延迟和成本翻倍；模型升级可能改变每调用成本。成本影响估计伴随有意义的变更。

**安全审查**：触及 prompt、工具或输出处理的变更审查安全含义。新工具触发策略门控审查（第 13 章 §13.4）；检索内容处理变更触发注入防御审查（第 23 章 §23.3）。

审查者不必每个 PR 都是安全或 eval 专家；他们应知道何时升级。团队的审查文化浮现关注点；专家在需要的案例中加入。

**AI 功能的 PR 模板** 通常包括：


    ## 变更内容
    [描述]

    ## 变更类型
    - [ ] Prompt 变更
    - [ ] Schema 变更
    - [ ] 工具变更
    - [ ] 模型变更
    - [ ] 其他代码变更

    ## Eval 影响
    [变更前后 eval 分数；eval 运行链接]

    ## 成本影响
    [估计的每调用成本增量；如显著，月度增量]

    ## 安全考虑
    [影响的威胁类别；已验证的防御]

    ## 发布计划
    [金丝雀或直接发布；回滚流程]


模板使审查结构化；结构使遗漏更不可能。
