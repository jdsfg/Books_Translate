### 3.4 Token 限制、上下文窗口以及用完时会发生什么

chat-completions 调用中的 token 用量是以下各项的总和：

* **输入 token**：system prompt + 所有消息（每个角色标签增加几个 token）+ 工具定义（如有）。
* **输出 token**：模型的响应。

模型的上下文窗口是输入 + 输出的上限。对于 200K 的 Claude Sonnet 4，这意味着输入 + 输出 ≤ 200K。如果你发送 198K 输入并要求 `max_tokens=4000`，你会得到一个错误：`4000 > 200000 - 198000 = 2000`。错误很简单；bug 通常是你的输入比你以为的更大。

一个更有趣的故障：你发送 50K 输入并要求 `max_tokens=8000`。模型生成 7950 token 的输出，响应以 `stop_reason: "max_tokens"` 截断。你有一个部分响应。怎么办？

三种模式：

1. **提高上限并重试。** 适用于初步探索性工作。在生产中不好因为你无法控制成本。
2. **要求模型继续。** 发送一个后续 `messages` 数组，将部分输出作为 assistant 消息追加，加上一条 user 消息说 _从你停下的地方继续_。模型会恢复。这可行但脆弱，且对你被截断的块使 token 成本翻倍。
3. **设计任务以适应 `max_tokens`。** 这是生产中的正确答案。如果你在总结文档而输出比你的预算长，文档对一次调用来说太长了。将其分成部分，总结每个部分，然后总结摘要。（这是 _map-reduce summarization_；第 8 章将回到这个话题。）将每次调用限制在与任务匹配的预算内；相信你必须围绕上限做架构设计。

通用原则：`max_tokens` 用完是一个 _架构性_ 信号，表明你的任务不适合调用形状，而非 _操作性_ 信号表明你应该提高上限。将其视为设计警报。
