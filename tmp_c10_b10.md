#### 概念检查

**Q1.** 陈述 RAG 的三个动机并指出每个是主导动机的生产场景。

**答案：**

1. **知识截止**：新闻摘要应用。模型的预训练是几个月或几年前的；时事需要新鲜检索。知识截止是主导动机。
2. **你的数据**：Beacon Health AI 的患者特定临床摘要器。模型对特定患者零训练数据；在患者病历上的 RAG 是接地个性化摘要的唯一方式。
3. **幻觉**：模型训练编码知识会出错的高风险问答：法律研究、医学参考、法规合规。Strata 的法律技术是范式案例。幻觉是主导动机；RAG 是架构缓解。

许多生产 RAG 系统同时服务多个动机。Strata 的 RAG 同时解决新鲜度（法规变化）和幻觉（法律准确性要求接地）。Beacon 的同时解决你的数据（患者特定）和幻觉（临床准确性）。动机不互斥。

**Q2.** 走过七步 RAG 循环。对每步，指出团队做的一个工程决策及其为何重要。

**答案：**

1. **用户查询**：如何预处理（小写？展开缩写？检测语言？）。重要因为后续步骤依赖清理后的查询。
2. **嵌入查询**：哪个嵌入模型。重要因为模型决定语义相似性与你领域用户意图的对齐程度。
3. **向量搜索**：哪个 K（top-K 大小）。重要因为 K=5 vs K=50 改变敏感性（大 K 捕获更多）和噪声（大 K 稀释）之间的权衡。
4. **重排序**：是否添加重排序器，以及哪个。重要因为重排序器通常提升生产质量 10–20 分（Strata 示例）。
5. **构建 prompt**：如何围绕检索块结构化 prompt（引用指令、分隔符格式、来源标记）。对引用接地和 prompt 注入防御重要。
6. **LLM 生成**：哪个模型。重要因为前沿 vs 工作马影响质量和成本。
7. **后处理**：引用提取、schema 验证。对下游代码重要；没有验证，你发布解析脆弱的管道。

有意识地工程化七个层中每一层的团队在做生产 RAG。只工程化一两个层的团队在发布包装过的朴素 RAG。

**Q3.** _检索评估_ 和 _回答评估_ 的区别是什么？为什么生产 RAG 需要两者？

**答案：** 检索评估测量 _检索是否找到了正确的块？_ 指标：recall@K、precision@K、MRR、NDCG。输入：查询，带哪些块相关的标签。输出：仅检索层的分数。

回答评估测量 _给定检索，答案是否正确？_ 指标：忠实度（答案遵循块）、回答相关性（针对问题）、引用接地（正确引用）。输入：查询、检索块、LLM 的答案，加参考答案或 LLM 作为评判者。输出：端到端管道的分数。

生产 RAG 需要两者因为故障模式定位不同：
