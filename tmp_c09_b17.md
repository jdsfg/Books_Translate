**成本 vs. 质量权衡。** 摘要在每个阶段边界增加一次 Haiku 级调用：约 0.005 美元/摘要；有 4 到 5 个阶段边界的 agent 任务在摘要开销上花费 0.02 到 0.025 美元。对比替代方案（agent 的主模型上下文成本无界增长加最终溢出），权衡是有利的。

**质量保障。** Agent 应能在需要时 _展开摘要_：如果下一步行动需要较早工具调用的逐字内容，agent 可以从获取原始内容的工具请求回来（轨迹外部存储；摘要是上下文中的内容，但原始可恢复）。此模式处理摘要丢失了后来证明重要的细节的边缘案例。

**与第 13 章的联系。** 此模式对 agent（第 13 章）是基础性的；没有它，agent 受上下文窗口深度限制。§8.4 手段 1 的框架是基础；此 Q 将其专门扩展到 agent。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为长时间运行的编码 agent 设计轨迹内摘要：触发器、什么逐字保留 vs. 摘要、摘要格式、成本考虑。_ 你验证 (a) LLM 提议 _结构化_ 摘要格式（非散文；agent 对结构化状态工作更好，LLM 有时默认散文），以及 (b) LLM 包含 _摘要展开_ 机制（从存储轨迹恢复逐字的能力；没有这个，不可逆摘要可能丢失 agent 后来需要的信息）。

**Q6.** _跨章节：带 N 个工具的 agent 的上下文预算分配（第 13 章预览）_。你在设计客服 agent 的上下文窗口分配，它有以下工具：`get_customer`、`get_order`、`get_shipment_status`、`update_subscription`、`cancel_order`、`escalate_to_human`，加另外 8 个工具。每个工具的 JSON schema 定义 150 到 400 token。模型有 200K 上下文窗口；对话历史可增长到 30K；系统 prompt 2K；输出预算 4K。分配上下文预算：哪些工具总是驻留上下文中，哪些即时加载，延迟-成本权衡是什么？

提示

§8.4 手段 4（即时加载）是关键手段；第 13 章将回到此。本章的分层上下文模式（§8.4 手段 3）也适用于工具集。

**答案：**

**预算计算。** 总计：200K。固定：2K（系统）+ 30K（历史满）+ 4K（输出）= 36K 预留。可用于工具定义 + 检索上下文 + 工作数据：164K。14 个工具每个 150 到 400 token（平均约 250），完整工具集加载到上下文花费 3.5K token。这本身不贵。问题变成：你预加载全部 14 个，还是即时加载？

**分配策略：分层工具。**

_第一层：始终在上下文中（3 到 5 个工具）_。高频、低摩擦的工具，agent 在大多数调用上使用。对于客服：`get_customer`、`get_order`、`get_shipment_status`。Token 成本：约 800 到 1200。每次调用加载因为即时加载它们的延迟惩罚不值得节省 1K token。
