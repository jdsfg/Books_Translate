审计是纪律，非一次性交付物。跨季度跟踪问题构建历史上下文；多季度保持"黄色"的层获得显式投资决策。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_设计季度层完整性审计流程：频率、测量什么、参与者、输出。_ 你验证 (a) 审计在你团队日历中可实施（不接受如你团队无法维持每季度 50 工程师小时的流程），以及 (b) 输出驱动行动（带截止日期和所有者的行动项，而非仅归档遗忘的报告）。

**Q4.** _重构巨石_。你继承一个 AI 工程巨石：4,000 行 Python 模块处理 prompt 构造、模型调用、响应解析、eval、日志、成本追踪、业务逻辑。团队需要添加多提供商路由。勾画重构计划。

**答案：** 勾画：

**阶段 1：提取模型提供商调用（1 工程师周）**

识别巨石中所有调用 Anthropic/OpenAI 等的地方。提取为单一 `LLMClient` 接口带 `call(messages, model, max_tokens, ...)` 方法。巨石现在调用 `LLMClient`；实现初始硬编码单一提供商。无行为变化；仅接口。

**阶段 2：插入路由器（1-2 工程师周）**

用路由器（LiteLLM）替换硬编码的 `LLMClient`。路由器根据模型名路由到适当提供商。现在多提供商通过配置工作，非通过巨石修改。

**阶段 3：提取可观测性（1-2 工程师周）**

识别巨石中所有日志。标准化为结构化追踪格式（按第 19 章）。将追踪管道到 Langfuse。巨石现在使用 `tracer.log(...)` 接口；实现处理 Langfuse SDK 调用。

**阶段 4：提取 eval 和成本追踪（各 1-2 工程师周）**

相同模式：识别巨石中的调用；提取为聚焦模块；巨石依赖提取的模块。

**阶段 5：从基础设施中提取业务逻辑（2-4 工程师周）**

剩余巨石主要是业务逻辑。将其重构为产品特定模块；基础设施模块（LLMClient、tracer、eval、cost）跨产品可重用。

**总计**：完整重构约 6-12 工程师周。Helios 的花了 3 个工程师月。模式是 **增量重构**，每阶段交付自己的价值：阶段 1 启用多提供商路由器；阶段 3 启用适当可观测性；阶段 4 启用 eval 即 CI。团队每阶段获得能力而非等待完整重构。

需管理的风险：**不要无测试重构**。每阶段需回归套件；巨石的行为必须保留。如测试不存在，作为阶段 1 的一部分在提取前编写。构建测试是重构成本的一部分。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
