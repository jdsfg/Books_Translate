### 15.6 Helios Customer AI：生产中的多 agent 分解

Helios 的生产部署说明模式。客户支持 agent 有三种模式：

**模式 1：单 agent（75% 流量）**。标准 ReAct 循环配完整工具目录。用于常规工单：信息查找、简单操作、单问题对话。平均成本约 0.10 美元；延迟约 3 秒。

**模式 2：编排器-工作器（20% 流量）**。编排器将工单分类为有多个子问题（例如"我想退货这个产品同时更新我的配送地址"）；生成 2-3 个专业工作器（退货处理 agent；地址更新 agent）；聚合结果。平均成本约 0.30 美元；延迟约 6 秒。

**模式 3：人工升级（5% 流量）**。agent 确定工单超出其能力（高度情绪化客户；法律/政策灰色地带；模糊请求）。agent 收集上下文并以结构化交接文档交给人工。成本约 0.05 美元（agent 仅做交接工作）；交接前延迟约 5 秒（之后人工处理）。

（上面每模式平均值是中位数风味的——它们排除了有界但昂贵的长尾轨迹，那些将 Helios 的 _生产均值_ 拉到第 14 章 §14.9 的约 0.18 美元/轨迹。一起读：中位单 agent 约 0.10 美元，中位编排器约 0.30 美元，加权中位约 0.14 美元；含长尾均值约 0.18 美元；差距是 §14.4 的有界工程前沿。）

偏转率 KPI（无需人工交接解决的工单百分比）在 2026 年约 63%，较 2024 年系统上线时的约 42% 提升。提升来自：

* 更好的工具目录（第 13 章 §13.7）。
* 有界 agent 设计（第 14 章 §14.6）。
* 模式 2 编排器-工作器用于单 agent 难处理的多问题工单。
* 持续 eval 驱动改进（第 11 章引入的多轴 rubric 纪律扩展到第 14 章 §14.6 有界 agent 设计文档的 eval 套件 + §14.10 的界逃逸 eval）。

5% 的人工升级份额是有意的。Helios _不_ 追求 100% 偏转；一些工单确实受益于人工处理（降级、边缘情况、客户关系时刻）。编排器的分类 _应该_ 将一些工单路由到人工；eval 验证它做到了。

多 agent 分解是最仔细测量的架构选择。模式 2 在 5% 流量切片上试点 3 个月；eval 显示多问题工单的清晰质量提升（客户满意度 +8 分）；成本增加可接受；随后生产上线。纪律：**在承诺架构复杂性之前先 eval**。
