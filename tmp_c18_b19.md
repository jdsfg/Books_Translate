* "我们对 eval 门控通过的 8 个任务类有经测试的到 GPT-4o 的回退。在这些类上，故障转移交付相比 Sonnet 90%+ 的质量。"
* "对回退 eval 门控失败的 3 个任务类，我们在宕机期间承诺优雅错误路径。我们不会在这些类上静默降级质量。"
* "我们有每月重新 eval 纪律以使回退质量保持最新。"
* "15 分钟以内的提供商宕机对 8 个覆盖类透明吸收。"
* 如不属实，不要承诺"我们有完整回退覆盖"。对哪些任务类覆盖哪些不覆盖的诚实是与董事会可持续信任的基础。

框架很重要：董事会的要求"添加回退"是"停止宕机"的简写。实际交付物是宕机容忍的 _覆盖子集_。工程的工作是枚举什么覆盖、什么不覆盖、权衡是什么，以使董事会的期望有根据。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我们全 Anthropic 需要在 3 工程师周内跨提供商回退。约束：prompt 为 Sonnet 调优；200 案例 eval。设计回退含提供商选择、prompt 可移植、质量门控、流量拆分、董事会承诺。_ 你验证 (a) LLM 承认一些任务类不会有可行回退（回答不应在 eval 门控可能失败处承诺完整覆盖），以及 (b) 流量拆分计划在任何真实流量前从影子开始（推荐跳到 50% 流量拆分的 LLM 在推荐不安全变更）。

**Q6.** _为滥用租户的每租户预算强制_。你的 SaaS 有 200 租户；月度推理预算 40K 美元。一个租户（"租户 X"）过去两周调用增至三倍；成本归因显示租户 X 现消耗 28% 预算。调查发现租户将 API 用于非设计用途（脚本中循环调用；用 LLM 做简单 regex 更适合的任务）。租户合同上限 1500 美元/月推理；本月已达 4200 美元。设计强制行动计划。指定：(a) 已有的每租户策略；(b) 允许此情况的策略缺口；(c) 立即行动；(d) 策略级修复；(e) 客户沟通。

**答案：**

(a) **已有的每租户策略**（按 §17.6）：每租户调用计数器；月度限制 80% 时软上限告警；100% 时硬上限拒绝。基础设施存在；缺口在于 _租户配置的是什么限制_。

(b) **策略缺口**：租户 X 8 个月前上线时用默认预算设置（未做每租户配置因为团队尚未学会强制）。合同中的"预算"是销售侧数字从未传播到技术强制系统。结果：技术强制以平台默认（无限制）运行，非合同预算。

(c) **立即行动**（24 小时内）：
