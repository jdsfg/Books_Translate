#### 深入思考

**Q1.** _嵌入的未来_。推测 2030 年的嵌入格局。领域专用嵌入仍重要吗？混合搜索仍需要吗？识别两个可能改变答案的力量。

**讨论：** 两个力量：

1. **基础模型在通用语义相似性上改进**。到 2030 年，通用嵌入模型可能在常见领域上缩小与专用嵌入的大部分差距。专用嵌入可能在非常窄的领域（例如有专有词汇的特定行业）中持续，那里训练数据有限。
2. **端到端检索模型**。今天的检索是两阶段（嵌入，然后搜索）。端到端神经检索（基于 transformer、单阶段）在改进；到 2030 年，它可能是某些用例的主导模式，取代双编码器嵌入架构。混合搜索可能演化为多信号端到端模型。

净预测：2030 年，_通用嵌入_可能对今天专用嵌入服务的 80% 用例足够；长尾专用用例（法律、医学、代码）可能仍受益于专用模型。混合搜索因精确 token 案例而持续（这些是语言固有的；嵌入无法完全覆盖）。

承诺前评估的纪律（第 4 章应用于检索层）在架构转变中存活。具体选择变化；纪律不变。

**Q2.** _为什么 Postgres 赢得了大多数团队的向量数据库问题_。本章论证 pgvector 是 2026 年新生产 RAG 项目的默认。论证支持和反对此立场。指出 pgvector 是错误默认而专用向量数据库正确的特定场景。

**讨论：** **支持 pgvector 作为默认**：大多数生产团队已运维 Postgres；添加 pgvector 零新基础设施。按租户访问控制、与业务数据的事务一致性、Postgres 现有运维成熟度：全部免费。到约 10M 向量规模，性能充足。运维简单性是主导价值。

**反对 pgvector 作为默认**：专用向量数据库在规模上有优势：更好的 HNSW 调优、专用存储格式、原生混合搜索、更好的向量特定操作人机工程。需要在 100M+ 规模运维向量索引的团队发现 pgvector 的通用 Postgres 基础设施更难调优；专用数据库更直接。

**pgvector 错误的特定场景**：团队构建 _非常_ 大规模 RAG 产品，索引在生产成熟时达到 500M+ 向量。在此规模，pgvector 的通用架构在对抗你：索引构建时间、负载下查询延迟、内存特性都推离 Postgres 设计。Pinecone（或认真调优的自托管 Qdrant）优雅处理此规模。在此选 pgvector 增加专用数据库不需要的工程努力。

通用原则：**默认对中位案例正确；你案例的正确答案取决于你相对于中位的位置**。远低于中位规模的团队（小语料库、简单查询）可以选任何；远高于中位规模的团队需要专门选。本章的默认面向广泛中间。
