#### 应用题

**Q1.** _设计重排序器集成_。你的团队 RAG 系统当前无重排序器运行。管道是：查询 → 向量搜索 top-10 → LLM。设计重排序器集成。指定：(a) 哪个重排序器；(b) 向量搜索的 top-K；(c) 重排序后的 top-N；(d) 丢弃低相关块的分数阈值；(e) 批处理策略；(f) 预期质量提升和成本增加。

提示

§11.1 有参数。

**答案：**

(a) **重排序器**：Cohere Rerank 3 作为起点（良好支持、可预测性能）。替代：BGE-Reranker-v2 如果你有自托管基础设施且偏好 OSS。

(b) **向量搜索的 top-K**：30。比之前的 top-10 大以给重排序器更多候选；重排序器可以恢复向量搜索排名 11-30 的相关块。

(c) **重排序后的 top-N**：5。重排序器的工作是识别最相关的 5 个；LLM 获得比之前 top-10 更锐利的上下文。

(d) **分数阈值**：Cohere 的 0-1 标度上 0.4。低于 0.4 的块被丢弃；如果所有 5 个块低于阈值，系统返回 _无确信答案_（附置信度低的解释）而非幻觉。

(e) **批处理**：在单次 Cohere Rerank 调用中发送所有 30 个候选（API 支持批处理评分）。相比 30 次顺序调用的延迟收益大。

(f) **预期提升**：总体通过率 +12 分（向纯向量管道添加重排序器的典型值）。**成本增加**：约 0.001 美元/查询（30 个候选的 Cohere Rerank 定价）。**延迟增加**：约 300ms（重排序器调用）。净评估：明显值得的添加。

实现工作量：约 2–3 工程师天。对生产 golden 集 eval 变更；如提升至少 +5 分则发布。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为纯向量 RAG 管道设计重排序器集成：重排序器选择、top-K、top-N、分数阈值、批处理、预期提升。_ 你验证 (a) 提议的分数阈值匹配你重排序器的实际评分标度（不同重排序器使用不同标度；检查 API 文档），以及 (b) 预期提升对你的起始基线现实（如果你的纯向量已在 80% 通过率，+12 提升可能过于乐观；质量上升时提升压缩）。

**Q2.** _构建多轴 eval 量表_。为 Beacon Health AI 临床笔记摘要器构建 §11.4 的绑定工作流 #5 量表。指定六个轴各自适合临床摘要的操作定义。识别每个轴如何评分（确定性、LLM judge、人工）。

**答案：**

1. **检索 recall@10**（确定性，需标注相关性）：患者病历中与口述笔记真正相关的条目，有多少在 top-10 检索中？范围 0–1。
2. **检索 precision@5**（确定性，需标注相关性）：top-5 检索的病历条目中，有多少标注相关？范围 0–1。
3. **临床准确性**（LLM judge，校准）：摘要是否包含任何临床不准确（错误药物、错误剂量、错误诊断、错误检查发现）？0 = 不准确；1 = 轻微不准确；2 = 基本准确；3 = 临床精确。
