界取决于典型轨迹特征，带合法边缘情况的缓冲。

**答案：**

典型轨迹是先验；界为合法边缘情况添加余量而不允许灾难性螺旋。

(a) **迭代限制**：20。典型 8-12，为合法边缘情况（需交叉引用多来源的研究问题）留余量。超过 20，agent 可能迷失。

(b) **每调用输出上限**：4,096 token。综合步骤需要结构化输出空间；单个工具调用不需要更多。上限足够宽松不导致合法调用的人工截断，足够低以捕获失控生成。

(c) **累积 token 预算**：250K token。典型轨迹消耗 50-100K（输入含检索段落）；250K 给 2.5 倍余量。超过此，轨迹消耗的上下文远超综合价值所证明的。

(d) **成本上限**：每轨迹 2.00 美元。按 Sonnet 4 定价（约 3 美元/M 输入，15 美元/M 输出）和典型 50K 输入 + 8K 输出：约 0.27 美元/轨迹。2.00 美元上限允许约 7 倍典型成本：足够余量给合法边缘情况（特别长的综合），足够低使失控轨迹不烧 50 美元。

(e) **墙上时钟上限**：5 分钟。典型 60-90 秒；5 分钟约 3.5 倍余量。足够长给合法慢搜索（学术数据库延迟可变）；足够短使卡住的轨迹不影响其他用户或队列后续工作。

(f) **每工具断路器**：

* `search_arxiv`、`search_web`、`search_internal_docs`：每轨迹各 5 次调用。超过此，agent 可能在重复查询；应整合发现。
* `fetch_url`：每轨迹 20 次调用。agent 检索的 URL 多于搜索调用（每次搜索返回多个 URL）。
* `summarize_passage`：每轨迹 50 次调用。agent 摘要单个段落；50 覆盖彻底的研究轨迹。
* `compose_synthesis`：最多 2 次调用（一次草稿 + 一次修订）。超过此，agent 在不收敛的精炼循环中。
* **相同参数断路**：任何工具用相同参数调用两次触发对 agent 的软警告（在工具响应中返回）；第三次相同调用停止轨迹。

(g) **优雅降级行为**：

* 在任一界（迭代、成本、墙上时钟）的 70%：agent 通过系统消息被通知：_资源达 70%。考虑结束研究并组合综合。_ agent 决定是否收尾或继续。
* 在 85%：系统插入 _资源达 85%。现在用你已有的组合综合。不要发起新搜索。_ agent 的下一步行动应为 `compose_synthesis`。
* 在 100%：强制停止；系统用已检索的段落组合部分综合，带显式说明研究不完整。用户看到："研究不完整；这是前 N 次搜索中发现的。用更窄查询重新运行以获取更多细节。"

升级给 agent 在硬停止前优雅结束的机会；硬停止防止失控成本。用户总是得到有用的东西（部分综合），而非静默失败。
