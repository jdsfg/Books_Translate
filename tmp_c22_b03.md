### 21.4 LLM 功能的 A/B 测试

经典软件中的 A/B 测试成熟：将变体 A 发给一部分用户，变体 B 发给其余，测量结果，决定哪个胜出。对 LLM 功能，经典 A/B 测试以不明显的方式失效。

为什么 LLM A/B 更难：

1. **多维输出**：变体 A 可能在一个维度（正确性）更好而在另一个（语气）更差。单一指标不能捕获权衡；决策变得更难。
2. **Eval 噪声**：eval 作为评判的评分有噪声（第 4 章 §4.5）。200 次调用样本上变体间 0.2 分的差异可能统计无意义。
3. **用户适应**：用户调整到系统的行为方式。新变体初期可能看起来更差因为用户已校准到先前行为；几周后，他们适应；变体看起来更好。
4. **延迟诱导混淆**：不同延迟的变体（更快 vs 更慢模型）混淆参与度指标。更快的变体获得更多参与度仅因为它们更快，独立于质量。

实践中对 LLM A/B 有效的：

* **比经典运行更长的 A/B**（周而非天）以允许用户适应稳定。
* **同时测量多维度**；显式权衡取舍。
* **按用户细分分层**：新用户 vs 有经验用户对变体反应不同。
* **将 A/B 参与度信号与离线 eval 分数结合**：一个变体同时有 eval 提升和参与度提升是可信的发布。

A/B 测试是 LLM 功能的真实工具但需要比经典应用更多谨慎。
