#### 概念检查

**Q1.** 为什么传统 APM 对 LLM 系统不够？识别传统 APM 不回答的三个具体问题。

**答案：** 三个问题：

1. **模型输出了什么？** 传统 APM 记录请求/响应元数据（HTTP 码、大小、延迟）但不记录 LLM 响应的文本内容。没有响应内容，你无法调试 _为什么_ 答案错误：你只知道调用在技术上成功了。
2. **发送的 prompt 是什么？** Prompt 决定模型行为；传统 APM 不捕获系统 prompt + 消息结构。没有 prompt 调查质量问题是在没有最重要变量的情况下调查。
3. **agent 跨多个 LLM 调用和工具调用做了什么？** 轨迹可能跨 4-10 次调用持续数秒；传统 APM 将每个视为独立请求。轨迹结构（调用了什么工具、什么顺序、什么结果）对 APM 不可见。

值得提及的第四个问题：**此调用/轨迹的成本是多少？** 成本归因需要 APM 不原生理解的每调用 token 数据。

一般原则：APM 解决后端工程问题；LLM 系统有新问题（语义内容、多轮、按组件成本）需要新工具。专用 LLM 可观测性填补缺口。

**Q2.** LLM 轨迹追踪包括什么（按 §19.2）？为什么轨迹级结构重要？

**答案：** 轨迹追踪包括（来自 §19.2）：

* 轨迹中的所有 LLM 调用，带每调用数据（模型、prompt、输出、token、成本、延迟）。
* 轨迹内的所有工具调用。
* 总轨迹成本和延迟。
* 轨迹的最终结果（成功/升级/失败/放弃）。
* 元数据（用户、会话、功能、租户）。

轨迹级结构重要因为多调用 agent 行为与单调用请求行为有质的区别：

* **成本是每轨迹的，非每调用的**。0.30 美元的轨迹成本是工程指标；个别调用成本汇总成它。每调用追踪不自然聚合，除非显式分组。
* **质量是每轨迹的**。用户的 eval 信号（问题是否解决？）是关于整个轨迹的结果，非任何个别调用。每调用质量分数不捕获全貌。
* **故障模式是轨迹级的**。Agent 螺旋（第 14 章）只有跨多次调用才能看到；单次调用看起来正常；模式是问题。
* **调试遵循轨迹**。要理解什么出了错，团队需要按序看调用和工具调用的序列，非随机个体快照。

轨迹级结构是使 LLM 可观测性与 APM _不同_ 的东西。APM 以请求思考；LLM 可观测性以轨迹思考。

**Q3.** 为什么本章论证受监管行业常需自托管可观测性？为什么供应商 BAA 不总够？

**答案：** 供应商 BAA 建立与供应商存储 PHI 的法律框架：数据加密、访问控制、供应商同意 HIPAA 标准。对许多受监管团队，这足够。

但一些受监管团队需要更多：

1. **数据驻留**：PHI 必须留在团队的基础设施上以满足团队的特定合规姿态（一些医院合同要求；一些州法规比 HIPAA 更严）。
