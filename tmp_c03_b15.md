### 章节小结

LLM 在机制上是一个将 token 序列转化为下一个 token 概率分布的函数。其他一切（聊天产品、agent、RAG 系统、语音助手）都是围绕这一个函数封装的工程。token 是整数编码的输入和输出单位；模型从不看到文本。预训练通过在庞大语料上训练预测下一个 token 赋予模型语言和世界知识；偏好调优（RLHF、DPO）将基础模型变成乐于助人的、遵循指令的聊天模型。上下文窗口是模型的工作记忆，是单次推理调用中状态存在的唯一地方。hallucination 是模型在高不确定性下仍不得不承诺下一个 token 选择的可见产物。prompt 敏感性来自模型在特定 token 上做条件化而非解析抽象意图的事实。模型无法可靠地数字母因为它从未看到字母；tokenization 位于文本和模型之间。model card 是规格表；那样对待它。

第 1 章的三个团队会继续出现；第 3 章将把 API 表面交到你手中，使这个心智模型具体化。之后几乎每一章都依赖这里构建的模型。上下文工程（第 8 章）建立在 §2.4 上；RAG（第 9 章）是 §2.6 的架构性回答；工具调用（第 13 章）是 §2.8 的工程性回答；成本优化（第 18 章）是 §2.10 的工程性回答。四问题准则和 _How This Breaks_ 模式贯穿全书，每次重现都依赖你在这里内化的东西。

带入第 3 章最重要的东西是循环：_给定一个 token 序列，模型返回下一个 token 的概率分布，你采样，你追加，你重复直到停止。_ 如果那个循环在你脑中，第 3 章的 API 表面将立刻说得通，整本书将复合累积。

### 2.X 练习题
