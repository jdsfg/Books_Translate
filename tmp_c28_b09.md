#### 模式 9：带文本回退的语音 agent

**解决什么问题。** 产品是语音优先——临床抄写员、客户支持 agent、免手助手——但语音不可靠：STT 听错、TTS 在罕见音素上发错、EOT 检测打断用户、丢包使一轮脱轨。该模式是生产级语音循环带优雅文本回退。

**拓扑。** 客户端打开双向音频流（WebRTC，或 LiveKit 或 Daily 等供应商 SDK）到语音 agent 服务。STT 以流模式运行（Deepgram、Whisper-streaming、ElevenLabs Scribe）每 200-500 毫秒发出部分转录。EOT 检测器——部分上的小分类器（VAD、停顿长度、韵律线索）或专用 EOT 模型——决定用户何时说完。在 EOT 上，最终转录流向 LLM（模式 1 或 5 取决于工具使用）。LLM 响应逐 token 流入流式 TTS（ElevenLabs、Cartesia 或供应商原生流式 TTS）；音频帧流到客户端。中断处理器并行运行：如客户端在 TTS 播放时检测到用户语音，服务取消在途 TTS、刷新 LLM 流并开始新一轮。如任一阶段失败——STT 置信度低于阈值、TTS 错误、音频通道停滞——回退打开文本面板；用户看到转录并可打字，LLM 在纯文本模式继续。语音尝试在后台重连；成功时客户端提供"恢复语音"。

**何时用。** 语音是一等产品界面。团队有音频工程能力（或供应商合作）且准备调试 RTC 问题。延迟预算紧（自然对话低于 700 毫秒轮延迟，理想低于 300 毫秒）且你已测量。

**何时不用。** 语音是"锦上添花"或演示。语音 agent 比文本 agent 多一个数量级的运维工作；除非语音是产品否则不要承担。

**交叉引用。** 第 20 章（生产中的语音和多模态界面）。
