交叉引用：这是 §14.4 四界模式应用于特定用例。相同模式适合任何研究/综合 agent；只有具体数字变化。有界设计纪律是：典型轨迹 + 2-7 倍边缘情况余量 + 70/85/100% 阈值的优雅降级。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为研究 agent 设计界（典型 8-12 次迭代、60-90 秒、0.27 美元成本）：迭代、输出、token、成本、墙上时钟、断路器、优雅降级。_ 你验证 (a) 界是典型的 2-7 倍（LLM 可能设得太紧，导致合法轨迹失败；或太松，失去作用），以及 (b) 优雅降级阈值（70/85/100%）是显式的（LLM 可能默认"到限停止"而无早期警告结构）。

**Q6.** _给定追踪调试 X 美元隔夜事故_。你到工作岗位面对 342 美元成本事故：单一 agent 轨迹隔夜运行 4 小时，做了 847 次工具调用，超出预算 50 倍才被日支出 kill switch 终止。追踪显示：agent 被给客户服务任务（"查订单 #98765 并解释配送延迟"）；6 次正常迭代后，进入在 `lookup_order` 和 `check_shipping_status` 之间交替的循环，每次参数略有不同；成本断路器（设在 0.50 美元）从未触发。诊断有界工程出了什么问题并提出三个独立修复。

**答案：**

诊断：多个界同时失败。轨迹显示三层失败：

1. **成本断路器在 0.50 美元未触发**，尽管轨迹累积了 342 美元。这意味着成本核算代码损坏：要么未被调用，要么被调用但计数器未递增，要么阈值检查在比较错误值（例如每调用成本而非累积）。
2. **迭代上限未触发**（847 次迭代远超任何合理上限）。要么无迭代上限，要么上限设得过高（例如 10,000 因为工程师认为不重要），要么计数器在不应重置的条件下重置。
3. **相同工具相似参数断路器未触发**。轨迹在两个工具间交替，参数每次略有不同（所以严格相同参数检查错过了），但 agent 明显在语义循环中。断路器逻辑比较精确参数；agent 通过微小参数扰动逃避。

成本事故根因是 _界强制缺口_。四界模式（§14.4）是设计；实现有击败它的 bug。

**三个独立修复**：

**修复 1：仪表化和审计成本断路器。**

成本计数器必须：

* 在每次 LLM 调用和每次工具调用后递增，带显式单元测试验证递增发生。
* 在下次 LLM 调用前对照阈值比较，返回停止 agent 的错误响应（而非仅记录警告）。
* 在每次轨迹记录日志：起始成本、每迭代增量、结束成本。让值班工程师在追踪中看到成本轨迹。
