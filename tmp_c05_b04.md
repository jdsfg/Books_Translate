### 4.3 构建 golden set：规范背后的工程工作

golden set 是 eval 中最昂贵的部分，也是团队最常投入不足的部分。一个好的 golden set 对 eval 套件来说就像一个好的测试语料库对编译器一样：没有它，你无法信任结果。

可用 golden set 的一些特征：

**有代表性。** golden set 中输入的分布应匹配系统实际会看到的输入分布。如果你的生产流量是 70% 短客服查询和 30% 长多轮对话，你的 golden set 应该大约是 70/30，而非 50/50 因为在电子表格中看起来更好。golden set 分布和生产分布之间的漂移是 eval 无法预测生产的失败的最大单一来源。

**分层。** 有代表性但均匀的样本遗漏了长尾。你还需要显式类别：简单/中等/困难输入；主要/次要/边缘场景；常见语言/罕见语言；已知正常/已知对抗输入。每个类别获得单独分数，所以只影响困难类别的回归是可见的（而非在无差异化的总数中被平均掉）。

**标注到可辩护的标准。** 每个输入需要参考标准，参考标准需要是可辩护的——如果两个合理的标注者看输入和参考，他们应该同意参考是正确的（或者，对于量表评分，以相同方式评分）。标注者间一致性是告诉你标签是否可靠的指标。（我们将在 §4.6 中讨论这个。）

**版本化。** golden set 是版本化制品。当你添加案例、更改标签、移除过时案例时，变更在版本控制中并在代码审查中可见。一个没有历史地漂移的 golden set 无法支持随时间可靠的回归检测。

**为信号而设的规模。** 5 个案例的 golden set 告诉你什么都没有；50,000 个的 golden set 维护昂贵。生产团队通常在 100–2000 范围内为主 eval 定居，子集 eval（切片、每租户、每功能）在较小端。正确的大小是能给你关于你关心检测的变化幅度的统计有意义信号的最小大小。

**无污染。** 如果你的 golden set 输入泄漏到你的 prompt 调优循环中，你的 eval 不再测量它声称的东西。（你为 eval 优化了；eval 不再报告无偏性能。）生产团队保持 golden set 的 _留出_ 部分不被任何调优触及。这是经典 ML 中 _你不看的测试集_ 纪律的 AI 工程版本。

对于 Beacon Health AI 的临床笔记摘要器，构建 golden set 是三个工程师月的工作：200 份匿名口述，每份由经验丰富的护士执业者用结构化参考摘要标注，然后由第二位临床医生审查，然后通过合成 PHI 管道匿名化（第 5 章）。三个工程师月是大的；它是构建功能最昂贵的部分。它也是决定功能是否发布的部分，因为没有它团队无法测量质量，没有测量功能就不是工程。成本是真实的；它也是下限。
