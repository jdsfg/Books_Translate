#### 计算机使用的安全考量

计算机使用 agent 有屏幕访问权。它们看到 agent 会话能看到的一切：用户的打开标签页、邮件、银行应用、消息。安全面很大。

生产部署模式：

* **沙箱环境**：agent 在专用 VM 或容器中操作，非用户的实际计算机。VM 仅有任务所需的应用和凭证。
* **每任务凭证范围化**：agent 有特定任务的凭证（例如航班预订任务有航班预订账户；非用户的完整凭证库）。
* **高风险行动的人工在环**：agent 在最终承诺前与人类确认（例如在点击"购买"前）。
* **行动日志和回放**：每张截图、每个行动，记录用于审计。如出问题，轨迹可被检查。

这些模式在 2026 年非可选。无这些的计算机使用会产生事故。


#### 常见误解

看演示视频，容易假设 _计算机使用 agent 替代 API 集成_。它们不替代。API 更可靠、更快、更便宜、更易审计；它们是 2026 年的主导集成模式并将持续。计算机使用 _补充_ API，填补 API 不存在或构建成本超过价值的空白。正确心智模型：API 是默认；计算机使用是小众场景的专用工具。
