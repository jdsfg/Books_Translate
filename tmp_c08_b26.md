4. **超越 JSON schema 的约束。** `outlines` 式文法约束只要某些输出需要比 schema 更丰富的约束（SQL、正则、自定义文法）就保持有用。前沿模型不会原生约束到 _每个_ 可能的文法；库层提供这个。

**存活的函数。** 即使字面约束解码问题消失，库作为 LLM 调用和类型化应用代码之间的 _集成层_ 的角色持续。它们不再是"约束强制库"而成为"LLM 到应用集成库"，约束强制是几个函数之一。

**2030 年预测。** `instructor` 在 2030 年看起来与 2026 年形式相似，约束强制层变薄（因为 provider 原生处理大部分）但类型绑定、重试循环和 provider 抽象层变厚。`outlines` 在长尾存活（研究项目、自托管模型、文法约束输出）。Pydantic 保持作为类型基底。库的 _名字_ 甚至可能存活；它们的 _函数集_ 转变。

通用教训：**解决技术问题的库在问题消失时消亡；占据技术栈中有用位置的库即使最初解决的问题蒸发也存活**。`instructor` 的位置（LLM 和 Pydantic 之间）是持久的；约束强制角度是该位置的一个历史理由，不是唯一理由。
