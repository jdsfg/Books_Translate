分布式系统 — 全量残留英文词扫描
总块数: 364
独立词: 1676
总出现: 13702

=== leader (475 occurrences) ===
  c02_b04.md: ...据）。CockroachDB 在正常运营期间为一致性支付延迟成本：一个需要跨区域副本法定人数往返的提交，比只需写入本地 leader 的提交更慢。Spanner 支付著名的 7 ms commit-wait（第13章），通过 TrueTime 提供线性...
  c02_b11.md: ...何时重要：任何应用逻辑假设“我看到的是最新数据”的读，都必须使用 leader 上的新鲜读，而不是 follower 读或 `AS OF SYSTEM TIME`。在需要线性化读的地方使用过期读，是...
  c04_b06.md: ...年生产系统中，广播原语以不同名称出现，但语义相同：  * **Kafka** 在 topic 分区内提供全序广播。分区 leader 排序写入；副本按相同顺序应用。这是限定在一个分区内的原子广播。 * **NATS JetStream** 提供基于 s...
  c04_b12.md: ...多的吞吐。  第5章迈出下一步：既然我们知道了如何可靠广播和排序事件，就把这些原语应用到复制。同步、异步、半同步、基于 leader、无 leader、法定人数读/写。每种性质、失败模式，以及使用它们的生产系统。  ---...
  c04_b12.md: ...章迈出下一步：既然我们知道了如何可靠广播和排序事件，就把这些原语应用到复制。同步、异步、半同步、基于 leader、无 leader、法定人数读/写。每种性质、失败模式，以及使用它们的生产系统。  ---...
  c05_b01.md: ...衡不同。同步复制最大化持久性和故障后一致性，代价是写延迟。异步复制最小化写延迟，代价是故障后持久性。半同步在中间。基于 leader 的复制集中写入；无 leader 的复制分散写入。   ### 5.2 同步复制  最简单的策略。主库接受写入，把写发...
  c05_b01.md: ...后一致性，代价是写延迟。异步复制最小化写延迟，代价是故障后持久性。半同步在中间。基于 leader 的复制集中写入；无 leader 的复制分散写入。   ### 5.2 同步复制  最简单的策略。主库接受写入，把写发给每个副本，等待每个副本确认，然后...
  c05_b02.md: ...onous_commit = remote_write` 配多个 standby 行为类似。   ### 5.5 基于 Leader 的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader...
  c05_b02.md: ...Leader 的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader...
  c05_b02.md: ...的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader（保一致）或...
  c05_b02.md: ...leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader（保一致）或 follower（扩规模，但可能过期）。  用于：PostgreSQL、MySQL、MongoDB（rep...
  c05_b02.md: ...blet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death**...
  c05_b02.md: ...order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write...
  c05_b02.md: ...所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the lea...
  c05_b02.md: ...der 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；...
  c05_b02.md: ...onous_commit = remote_write` 配多个 standby 行为类似。   ### 5.5 基于 Leader 的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader...
  c05_b02.md: ...Leader 的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader...
  c05_b02.md: ...的复制  一种拓扑选择。一个节点是 _leader_（或称 primary、master）；所有写都发往 leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader（保一致）或...
  c05_b02.md: ...leader；leader 传播给 _followers_（或称 replicas、secondaries）。读可以走 leader（保一致）或 follower（扩规模，但可能过期）。  用于：PostgreSQL、MySQL、MongoDB（rep...
  c05_b02.md: ...blet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death**...
  c05_b02.md: ...order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write...
  c05_b02.md: ...所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the lea...
  c05_b02.md: ...der 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；...
  c05_b03.md: ...### 5.6 无 Leader 复制  没有指定 leader。写发向任意节点，该节点转发给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致...
  c05_b03.md: ...### 5.6 无 Leader 复制  没有指定 leader。写发向任意节点，该节点转发给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致答案。  用于：Dynamo（及...
  c05_b03.md: ...### 5.6 无 Leader 复制  没有指定 leader。写发向任意节点，该节点转发给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致...
  c05_b03.md: ...### 5.6 无 Leader 复制  没有指定 leader。写发向任意节点，该节点转发给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致答案。  用于：Dynamo（及...
  c05_b04.md: ...后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a) **将用户读在写后的一段时间内路由到 leader。** 某些会话中间件跟踪“该用户 30 秒前写过”，并把读钉在主库 30 秒。代价：受影响用户无法享受读副本扩展。...
  c05_b06.md: ...活动，读会观察到最近完全提交的写），但不是完整线性一致性。混淆它们会让真实工程师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（Postgr...
  c05_b06.md: ...完全提交的写），但不是完整线性一致性。混淆它们会让真实工程师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（PostgreSQL、Mongo...
  c05_b06.md: ...师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（PostgreSQL、MongoDB、CockroachDB）。Leaderless：任意...
  c05_b06.md: ...ckroachDB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传...
  c05_b06.md: ...ssandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数...
  c05_b06.md: ...）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、...
  c05_b06.md: ...案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB...
  c05_b06.md: ...e）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB replica sets、CockroachDB（per-ran...
  c05_b06.md: ...rless：任意节点接受写；系统依赖冲突解决（LWW、sibling、CRDT）收敛并发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 tr...
  c05_b06.md: ...发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故...
  c05_b06.md: ...derless 让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b06.md: ...价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b06.md: ...er 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b06.md: ...活动，读会观察到最近完全提交的写），但不是完整线性一致性。混淆它们会让真实工程师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（Postgr...
  c05_b06.md: ...完全提交的写），但不是完整线性一致性。混淆它们会让真实工程师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（PostgreSQL、Mongo...
  c05_b06.md: ...师损失真实数据。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（PostgreSQL、MongoDB、CockroachDB）。Leaderless：任意...
  c05_b06.md: ...ckroachDB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传...
  c05_b06.md: ...ssandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数...
  c05_b06.md: ...）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、...
  c05_b06.md: ...案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB...
  c05_b06.md: ...e）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB replica sets、CockroachDB（per-ran...
  c05_b06.md: ...rless：任意节点接受写；系统依赖冲突解决（LWW、sibling、CRDT）收敛并发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 tr...
  c05_b06.md: ...发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故...
  c05_b06.md: ...derless 让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b06.md: ...价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b06.md: ...er 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b08.md: ...tgres 故障切换（用 Patroni 等）通常 30 秒到数分钟；CockroachDB 自动 per-range leader 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 read-after-wri...
  c05_b10.md: ...久性 vs 吞吐；两者都是真实成本。  **Q2.** 一个 LLM 被要求实现“高可用”复制，写出一个用异步复制、无 leader 选举、无法定人数的读可去任意副本、写可去任意副本并后台传播的系统。指出三种失败模式，并解释为什么这并非真正高可用。...
  c05_b11.md: ...：Raft 复制 per-range 的多区域系统（CockroachDB）。Raft quorum 可承受单区域丢失；leader 可在另一区域重选；写在数秒内恢复。一致性全程保持。代价是跨区域 Raft 的延迟下限，但这是按每次写以毫秒计的成本，不...
  c05_b11.md: ...本章总结  复制服务于持久性、可用性和读扩展性的某种组合。同步、异步、半同步的选择在写延迟和故障切换后持久性之间权衡。leader-based 与 leaderless 的选择在协调成本和故障切换透明性之间权衡。$W + R > N$ 的法定人数规则...
  c06_b08.md: ...；你希望中位数成对往返低。地理聚类有帮助。  **答案：** 三个副本的 Raft：quorum 是任意 2 of 3。leader 自己算一个 ack；条目在 _最快_ follower 响应后提交。因此 US-East 发起的写，提交延迟是到其他两...
  c07_b01.md: ...现：  * **原子广播**（第4章）：每次广播是一个共识实例。 * **领导者选举** ：每个进程提议自己；决定值即 leader。 * **分布式锁** ：每次获取锁请求是一个共识实例。 * **成员管理** ：“谁在集群中”的变更通过共识决定。...
  c07_b04.md: ...，故障检测器用带超时的心跳实现。超时是 _怀疑阈值_ ：任何在超时内未响应的进程被怀疑崩溃。超时太短，误怀疑导致不必要 leader 变更。超时太长，真实故障检测变慢。  激进超时（快速故障检测）与保守超时（避免误怀疑）之间的张力，是运行共识集群的运维...
  c07_b05.md: ...ination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广播 | 稳定 leader（Multi-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条...
  c07_b05.md: ...议者广播 | 稳定 leader（Multi-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate...
  c07_b05.md: ...ti-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） |...
  c07_b05.md: ...te（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返...
  c07_b05.md: ...+ 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有...
  c07_b05.md: ...都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有用。几乎每种“新共识协议”只改变三者之一：新法定...
  c07_b05.md: ...ination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广播 | 稳定 leader（Multi-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条...
  c07_b05.md: ...议者广播 | 稳定 leader（Multi-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate...
  c07_b05.md: ...ti-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） |...
  c07_b05.md: ...te（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返...
  c07_b05.md: ...+ 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有...
  c07_b05.md: ...都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有用。几乎每种“新共识协议”只改变三者之一：新法定...
  c07_b08.md: ...跨三个数据中心运行 Raft 复制的 etcd 集群，注意到 200-500 ms 跨 DC 网络抖动期间，集群偶尔丢 leader 并重选。配置：心跳间隔 100 ms，选举超时 1000-2000 ms。诊断并建议调整。  提示  选举超时必须长于...
  c07_b08.md: ...最坏网络延迟。1000-2000 ms 对 500 ms 跨 DC 抖动来说紧。  **答案：** 诊断：网络抖动期间 leader 到 follower 的心跳耗时超过预期。若心跳延迟 500 ms，而选举超时 1000 ms，在心跳到达前 foll...
  c07_b08.md: ...，在心跳到达前 follower 的定时器就可能触发选举。选举成功（因为旧 leader 心跳仍在延迟），集群就有了新 leader。  这次新选举不必要，因为旧 leader 还活着；follower 只是无法判断。  调整：  (a) 将选举超时增...
  c07_b08.md: ...可能触发选举。选举成功（因为旧 leader 心跳仍在延迟），集群就有了新 leader。  这次新选举不必要，因为旧 leader 还活着；follower 只是无法判断。  调整：  (a) 将选举超时增加到 5000-10000 ms。这样可承受...
  c07_b08.md: ...：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre-vote（消除误怀疑扰动）并把选举超时调到约 5...
  c07_b10.md: ...不必想 FLP。协议自会处理。  反驳：理解 FLP 对理解共识集群为何如此行为至关重要。Raft 在分区期间无法选出 leader 时，就是 FLP 在起作用：在持续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视...
  c07_b11.md: ...分区期间可能暂停。  三条性质映射到真实协议的具体设计选择：agreement 用法定人数相交，validity 来自 leader 提议的结构，termination 用部分同步加心跳。用这副眼镜读任何新共识论文，结构都会显现。  第8章取第一个经典...
  c08_b01.md: ...定一个值（协议完成后，没有两个 acceptor 对选定值产生分歧）。 * **Termination** ：无故障且 leader 稳定时，协议终止。   ### 8.2 为什么朴素协议失败  在 Paxos 之前，先看朴素协议会怎么做。  **朴素...
  c08_b03.md: ...#### 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 propos...
  c08_b03.md: ...#### 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议...
  c08_b03.md: ...## 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互...
  c08_b03.md: ...poser 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos...
  c08_b03.md: ...干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos 就一个值达成一致...
  c08_b03.md: ...一个值）。朴素扩展是每个槽跑独立 Paxos 实例，但成本太高（每槽两阶段）。  Multi-Paxos：选举一个稳定 leader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leade...
  c08_b03.md: ...是每个槽跑独立 Paxos 实例，但成本太高（每槽两阶段）。  Multi-Paxos：选举一个稳定 leader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高...
  c08_b03.md: ...eader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREP...
  c08_b03.md: ...只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREPARE(b)`）。   3. Acceptors 对每个槽回复它们最高接受...
  c08_b03.md: ...始为槽跑第一阶段（`PREPARE(b)`）。   3. Acceptors 对每个槽回复它们最高接受的值。   4. Leader 决定任何“飞行中”槽（已有人接受的槽）的值，并正常继续处理新槽。   5. Leader 想提交每个新值时，跳过第一阶...
  c08_b03.md: ...它们最高接受的值。   4. Leader 决定任何“飞行中”槽（已有人接受的槽）的值，并正常继续处理新槽。   5. Leader 想提交每个新值时，跳过第一阶段，只跑第二阶段（`ACCEPT(b, v)`）填入下一个槽。  该优化每决定省一次往返：...
  c08_b03.md: ...cd-via-Raft）领导稳定数分钟到数小时，因此每决定成本是一次往返。   #### Multi-Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时）...
  c08_b03.md: ...）领导稳定数分钟到数小时，因此每决定成本是一次往返。   #### Multi-Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballo...
  c08_b03.md: ...Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到...
  c08_b03.md: ...#### 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 propos...
  c08_b03.md: ...#### 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议...
  c08_b03.md: ...## 常见误解  常见误解是认为 Paxos 选举 leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互...
  c08_b03.md: ...poser 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos...
  c08_b03.md: ...干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos 就一个值达成一致...
  c08_b03.md: ...一个值）。朴素扩展是每个槽跑独立 Paxos 实例，但成本太高（每槽两阶段）。  Multi-Paxos：选举一个稳定 leader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leade...
  c08_b03.md: ...是每个槽跑独立 Paxos 实例，但成本太高（每槽两阶段）。  Multi-Paxos：选举一个稳定 leader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高...
  c08_b03.md: ...eader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREP...
  c08_b03.md: ...只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREPARE(b)`）。   3. Acceptors 对每个槽回复它们最高接受...
  c08_b03.md: ...始为槽跑第一阶段（`PREPARE(b)`）。   3. Acceptors 对每个槽回复它们最高接受的值。   4. Leader 决定任何“飞行中”槽（已有人接受的槽）的值，并正常继续处理新槽。   5. Leader 想提交每个新值时，跳过第一阶...
  c08_b03.md: ...它们最高接受的值。   4. Leader 决定任何“飞行中”槽（已有人接受的槽）的值，并正常继续处理新槽。   5. Leader 想提交每个新值时，跳过第一阶段，只跑第二阶段（`ACCEPT(b, v)`）填入下一个槽。  该优化每决定省一次往返：...
  c08_b03.md: ...cd-via-Raft）领导稳定数分钟到数小时，因此每决定成本是一次往返。   #### Multi-Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时）...
  c08_b03.md: ...）领导稳定数分钟到数小时，因此每决定成本是一次往返。   #### Multi-Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballo...
  c08_b03.md: ...Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到...
  c08_b04.md: ...tor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化**...
  c08_b04.md: ...* **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、lea...
  c08_b04.md: ...der 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（2011）和 Spanner 论文（2...
  c08_b04.md: ...）和“辅助”的（只在故障时咨询）。降低稳态成本。 * **Egalitarian Paxos（EPaxos）** ：无 leader；每个副本都可提议，冲突按提议解决。吞吐更高；推理更复杂。 * **Flexible Paxos**（Heidi How...
  c08_b04.md: ...数只需 _相交_，不必都是多数。这让系统可调节权衡（更小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授...
  c08_b04.md: ...小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经...
  c08_b04.md: ...。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口...
  c08_b04.md: ...xos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）。Spanner 和 CockroachDB 都把...
  c08_b04.md: ...cceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）。Spanner 和 CockroachDB 都把基于 lease 的读作为主要性能优化。...
  c08_b04.md: ...tor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化**...
  c08_b04.md: ...* **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、lea...
  c08_b04.md: ...der 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（2011）和 Spanner 论文（2...
  c08_b04.md: ...）和“辅助”的（只在故障时咨询）。降低稳态成本。 * **Egalitarian Paxos（EPaxos）** ：无 leader；每个副本都可提议，冲突按提议解决。吞吐更高；推理更复杂。 * **Flexible Paxos**（Heidi How...
  c08_b04.md: ...数只需 _相交_，不必都是多数。这让系统可调节权衡（更小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授...
  c08_b04.md: ...小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经...
  c08_b04.md: ...。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口...
  c08_b04.md: ...xos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）。Spanner 和 CockroachDB 都把...
  c08_b04.md: ...cceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）。Spanner 和 CockroachDB 都把基于 lease 的读作为主要性能优化。...
  c08_b06.md: ...**答案：** Multi-Paxos 扩展单法令 Paxos，以就值序列达成一致（复制日志每槽一个值）。改进点：稳定 leader 为多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leade...
  c08_b06.md: ...eader 为多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leader。Leader 在其任期内持有该角色，直到崩溃或被怀疑。...
  c08_b06.md: ...多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leader。Leader 在其任期内持有该角色，直到崩溃或被怀疑。...
  c08_b06.md: ...**答案：** Multi-Paxos 扩展单法令 Paxos，以就值序列达成一致（复制日志每槽一个值）。改进点：稳定 leader 为多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leade...
  c08_b06.md: ...eader 为多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leader。Leader 在其任期内持有该角色，直到崩溃或被怀疑。...
  c08_b06.md: ...多个槽只跑一次第一阶段，然后每槽只跑第二阶段。  具体：  (a) 通过 Paxos 实例或等价机制选举 leader。Leader 在其任期内持有该角色，直到崩溃或被怀疑。...
  c08_b07.md: ...一阶段（“为槽 [current_slot, \infty) prepare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往...
  c08_b07.md: ...fty) prepare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（...
  c08_b07.md: ...TED），而非两次（PREPARE + PROMISE + ACCEPT + ACCEPTED）。对服务多个决定的稳定 leader，摊销效果显著。  若领导不稳定（频繁重选），每次重选都要跑第一阶段。实用 Multi-Paxos 系统追求领导稳定数分...
  c08_b07.md: ...一阶段（“为槽 [current_slot, \infty) prepare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往...
  c08_b07.md: ...fty) prepare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（...
  c08_b07.md: ...TED），而非两次（PREPARE + PROMISE + ACCEPT + ACCEPTED）。对服务多个决定的稳定 leader，摊销效果显著。  若领导不稳定（频繁重选），每次重选都要跑第一阶段。实用 Multi-Paxos 系统追求领导稳定数分...
  c08_b08.md: ...#### 应用问题  **Q1.** 一个团队为 leader 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 b...
  c08_b08.md: ...nya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集群有 5 个节点。当前 leader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accep...
  c08_b08.md: ...有 5 个节点。当前 leader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当...
  c08_b08.md: ...集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5...
  c08_b08.md: ...法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Lea...
  c08_b08.md: ...der 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到...
  c08_b08.md: ...需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ballot number 的...
  c08_b08.md: ...llot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶...
  c08_b08.md: ...follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leader 在少数方被“隔离”：它不能...
  c08_b08.md: ...omise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leader 在少数方被“隔离”：它不能提交、拿不到 accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且...
  c08_b08.md: ...少数方被“隔离”：它不能提交、拿不到 accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot 正在使用，下台成为 follower。...
  c08_b08.md: ...#### 应用问题  **Q1.** 一个团队为 leader 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 b...
  c08_b08.md: ...nya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集群有 5 个节点。当前 leader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accep...
  c08_b08.md: ...有 5 个节点。当前 leader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当...
  c08_b08.md: ...集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5...
  c08_b08.md: ...法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Lea...
  c08_b08.md: ...der 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到...
  c08_b08.md: ...需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ballot number 的...
  c08_b08.md: ...llot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶...
  c08_b08.md: ...follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leader 在少数方被“隔离”：它不能...
  c08_b08.md: ...omise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leader 在少数方被“隔离”：它不能提交、拿不到 accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且...
  c08_b08.md: ...少数方被“隔离”：它不能提交、拿不到 accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot 正在使用，下台成为 follower。...
  c08_b09.md: ...结果：短暂的不可用（选举期间，通常数秒），然后多数方新 leader 恢复服务。没有 safety 违反；最坏情况是短暂暂停。  **Q3.** 比较 Paxos 与 2PC。两者都有两阶...
  c08_b10.md: ...杂性。对新团队来说，Raft 是正确默认。  反驳：Paxos 有 Raft 没有的属性，视变体而定。EPaxos 无 leader；对低竞争工作负载，其吞吐高于 Raft 的 leader 瓶颈设计。Flexible Paxos 允许运维调节法定人数...
  c08_b10.md: ...os 有 Raft 没有的属性，视变体而定。EPaxos 无 leader；对低竞争工作负载，其吞吐高于 Raft 的 leader 瓶颈设计。Flexible Paxos 允许运维调节法定人数大小，Raft 不行。Google 的 Chubby 和...
  c08_b11.md: ...响应中最高已接受的值；这保证一旦值被选定，所有后续 ballot 都会提议它。  Multi-Paxos 通过选举稳定 leader 并把第一阶段摊销到多个决定，把单法令扩展为值序列。稳态下每决定成本是一次往返。  Paxos 以难实现著称：持久化纪律...
  c08_b11.md: ...garo 和 Ousterhout 2014 年为比 Paxos 更易理解而设计。Raft 使用相同骨架（法定人数 + leader + 持久状态），但教学、实现和运维推理都更容易。它是 2026 年新系统的首选共识协议。  ---...
  c09_b01.md: ...布式系统项目首先想到 Raft。   ### 9.1 三个子问题  Raft 把共识分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leade...
  c09_b01.md: ...个子问题  Raft 把共识分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followe...
  c09_b01.md: ...eader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **Safety** ：确保一旦日志条目提交，它保...
  c09_b01.md: ...心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对...
  c09_b01.md: ...中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 t...
  c09_b01.md: ...中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现...
  c09_b01.md: ...中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现自己已过期。...
  c09_b01.md: ...布式系统项目首先想到 Raft。   ### 9.1 三个子问题  Raft 把共识分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leade...
  c09_b01.md: ...个子问题  Raft 把共识分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followe...
  c09_b01.md: ...eader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **Safety** ：确保一旦日志条目提交，它保...
  c09_b01.md: ...心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对...
  c09_b01.md: ...中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 t...
  c09_b01.md: ...中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现...
  c09_b01.md: ...中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现自己已过期。...
  c09_b02.md: ...### 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 f...
  c09_b02.md: ...## 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机在 150 ms 到 300...
  c09_b02.md: ...若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导...
  c09_b02.md: ...idate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两...
  c09_b02.md: ...(b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的日志比某些已提交条目短，该条目可能“丢失”（leader 会覆盖它）。u...
  c09_b02.md: ...### 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 f...
  c09_b02.md: ...## 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机在 150 ms 到 300...
  c09_b02.md: ...若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导...
  c09_b02.md: ...idate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两...
  c09_b02.md: ...(b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的日志比某些已提交条目短，该条目可能“丢失”（leader 会覆盖它）。u...
  c09_b03.md: ...状态机，其中每个已提交条目在少数派故障下存活，且每个客户端请求在每个副本上以相同顺序恰好应用一次。机制是 Raft 的 leader-election + AppendEntries + commit-on-majority。变体：per-range...
  c09_b03.md: ...ft（TiKV）用于多组并行；learner 节点用于不影响法定人数的追赶。训练技能：实现（或运维）一个系统，通过任意 leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader...
  c09_b03.md: ...leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader 接受客户端请求。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `AppendEntries`...
  c09_b03.md: ...ed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prev...
  c09_b03.md: ...evLogTerm`，拒绝。这是 _consistency check_ ；它确保 follower 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consis...
  c09_b03.md: ...Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什...
  c09_b03.md: ...日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找...
  c09_b03.md: ...绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[foll...
  c09_b03.md: ...减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`...
  c09_b03.md: ...er 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条...
  c09_b03.md: ...的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选...
  c09_b03.md: ...他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选举多数）。 (b) 每个新 leader 拥有所有已提交条目（通过“日志最新”规则选举限制）。 (c) Leader...
  c09_b03.md: ...同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选举多数）。 (b) 每个新 leader 拥有所有已提交条目（通过“日志最新”规则选举限制）。 (c) Leader 从不覆盖或删除自己的日志条目。  合起来：...
  c09_b03.md: ...ader 从不覆盖或删除自己的日志条目。  合起来：索引 i、term T 的已提交条目在提交时已复制到多数。任何未来 leader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节点拥有该条目。根据 up-to-date 规则，新 leade...
  c09_b03.md: ...eader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节点拥有该条目。根据 up-to-date 规则，新 leader 至少拥有任何多数成员的所有条目，因此包括该已提交条目。于是新 leader 保留它，且索引 i 永远保留该条目。...
  c09_b03.md: ...条目。根据 up-to-date 规则，新 leader 至少拥有任何多数成员的所有条目，因此包括该已提交条目。于是新 leader 保留它，且索引 i 永远保留该条目。...
  c09_b03.md: ...状态机，其中每个已提交条目在少数派故障下存活，且每个客户端请求在每个副本上以相同顺序恰好应用一次。机制是 Raft 的 leader-election + AppendEntries + commit-on-majority。变体：per-range...
  c09_b03.md: ...ft（TiKV）用于多组并行；learner 节点用于不影响法定人数的追赶。训练技能：实现（或运维）一个系统，通过任意 leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader...
  c09_b03.md: ...leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader 接受客户端请求。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `AppendEntries`...
  c09_b03.md: ...ed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prev...
  c09_b03.md: ...evLogTerm`，拒绝。这是 _consistency check_ ；它确保 follower 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consis...
  c09_b03.md: ...Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什...
  c09_b03.md: ...日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找...
  c09_b03.md: ...绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[foll...
  c09_b03.md: ...减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`...
  c09_b03.md: ...er 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条...
  c09_b03.md: ...的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选...
  c09_b03.md: ...他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选举多数）。 (b) 每个新 leader 拥有所有已提交条目（通过“日志最新”规则选举限制）。 (c) Leader...
  c09_b03.md: ...同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选举多数）。 (b) 每个新 leader 拥有所有已提交条目（通过“日志最新”规则选举限制）。 (c) Leader 从不覆盖或删除自己的日志条目。  合起来：...
  c09_b03.md: ...ader 从不覆盖或删除自己的日志条目。  合起来：索引 i、term T 的已提交条目在提交时已复制到多数。任何未来 leader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节点拥有该条目。根据 up-to-date 规则，新 leade...
  c09_b03.md: ...eader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节点拥有该条目。根据 up-to-date 规则，新 leader 至少拥有任何多数成员的所有条目，因此包括该已提交条目。于是新 leader 保留它，且索引 i 永远保留该条目。...
  c09_b03.md: ...条目。根据 up-to-date 规则，新 leader 至少拥有任何多数成员的所有条目，因此包括该已提交条目。于是新 leader 保留它，且索引 i 永远保留该条目。...
  c09_b04.md: ...#### “Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previou...
  c09_b04.md: ...#### “Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带...
  c09_b04.md: ...是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常见误解  Raft 的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (...
  c09_b04.md: ...的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目且 (b) 一个自己 term 的条目也已复制到多数 来提交条目。两个条件一起防止图...
  c09_b04.md: ...(b) 一个自己 term 的条目也已复制到多数 来提交条目。两个条件一起防止图 8 问题。  实践中很少要紧，因为新 leader 通常很快提交新条目，但实现必须处理 corner case。   ### 9.6 日志压缩  Raft 日志无限增长。...
  c09_b04.md: ...身是必须通过共识完成的配置变更。朴素方法（突然改变配置）不安全：旧配置和新配置可能短暂地各自形成“多数”，从而选出不同 leader。  Raft 的解：_joint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过...
  c09_b04.md: ...#### “Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previou...
  c09_b04.md: ...#### “Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带...
  c09_b04.md: ...是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常见误解  Raft 的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (...
  c09_b04.md: ...的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目且 (b) 一个自己 term 的条目也已复制到多数 来提交条目。两个条件一起防止图...
  c09_b04.md: ...(b) 一个自己 term 的条目也已复制到多数 来提交条目。两个条件一起防止图 8 问题。  实践中很少要紧，因为新 leader 通常很快提交新条目，但实现必须处理 corner case。   ### 9.6 日志压缩  Raft 日志无限增长。...
  c09_b04.md: ...身是必须通过共识完成的配置变更。朴素方法（突然改变配置）不安全：旧配置和新配置可能短暂地各自形成“多数”，从而选出不同 leader。  Raft 的解：_joint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过...
  c09_b05.md: ...仍在某些细分领域获胜：Google 基础设施（Chubby、Spanner）、专门变体如 EPaxos 用于高吞吐无 leader 工作负载、Flexible Paxos 用于可调法定人数。但对 2026 年的新项目，默认是 Raft。   ###...
  c09_b05.md: ...变更** ：raft-rs 实现单服务器变更（Raft joint consensus 的安全子集）。  她放弃的：无 leader 写（CRDT 或 EPaxos 可提供）；per-tablet 性能调优（Spanner 的 Paxos-with-l...
  c09_b06.md: ...#### 概念检测  **Q1.** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader ele...
  c09_b06.md: ...提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选...
  c09_b06.md: ...ication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 l...
  c09_b06.md: ...(a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **L...
  c09_b06.md: ...r。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并...
  c09_b06.md: ...选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；...
  c09_b06.md: ...follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的 candidate 能赢）与 leader 只追加日志...
  c09_b06.md: ...，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的 candidate 能赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不...
  c09_b06.md: ...的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 ter...
  c09_b06.md: ...。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follow...
  c09_b06.md: ...aft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 le...
  c09_b06.md: ...te 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raf...
  c09_b06.md: ...m 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应...
  c09_b06.md: ...er”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase...
  c09_b06.md: ...不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 Raft 随机化选举超时？  提...
  c09_b06.md: ...？  提示  减少多个 follower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投...
  c09_b06.md: ...ower 的定时器显著提前触发，它在其他人定时器触发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 leader 选举在崩溃条件下会频繁活锁。有了它，leader 选举完成时间期望约为最大超时量级（生产通常几百毫秒）。  **Q4....
  c09_b06.md: ...发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 leader 选举在崩溃条件下会频繁活锁。有了它，leader 选举完成时间期望约为最大超时量级（生产通常几百毫秒）。  **Q4.** 描述 AppendEntries 一致性检查...
  c09_b06.md: ...。  **Q4.** 描述 AppendEntries 一致性检查。它保证什么？  提示  Follower 必须在与 leader 记录匹配的前一条目处才追加新条目。...
  c09_b06.md: ...#### 概念检测  **Q1.** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader ele...
  c09_b06.md: ...提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选...
  c09_b06.md: ...ication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 l...
  c09_b06.md: ...(a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **L...
  c09_b06.md: ...r。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并...
  c09_b06.md: ...选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；...
  c09_b06.md: ...follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的 candidate 能赢）与 leader 只追加日志...
  c09_b06.md: ...，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的 candidate 能赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不...
  c09_b06.md: ...的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 ter...
  c09_b06.md: ...。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follow...
  c09_b06.md: ...aft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 le...
  c09_b06.md: ...te 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raf...
  c09_b06.md: ...m 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应...
  c09_b06.md: ...er”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase...
  c09_b06.md: ...不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 Raft 随机化选举超时？  提...
  c09_b06.md: ...？  提示  减少多个 follower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投...
  c09_b06.md: ...ower 的定时器显著提前触发，它在其他人定时器触发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 leader 选举在崩溃条件下会频繁活锁。有了它，leader 选举完成时间期望约为最大超时量级（生产通常几百毫秒）。  **Q4....
  c09_b06.md: ...发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 leader 选举在崩溃条件下会频繁活锁。有了它，leader 选举完成时间期望约为最大超时量级（生产通常几百毫秒）。  **Q4.** 描述 AppendEntries 一致性检查...
  c09_b06.md: ...。  **Q4.** 描述 AppendEntries 一致性检查。它保证什么？  提示  Follower 必须在与 leader 记录匹配的前一条目处才追加新条目。...
  c09_b07.md: ...**答案：** Leader 发送 `AppendEntries(term, prevLogIndex, prevLogTerm, entries,...
  c09_b07.md: ...evLogIndex` 处的日志条目 term 不是 `prevLogTerm`，拒绝（follower 日志在此处与 leader 分歧）。  否则，追加新条目（覆盖任何冲突的后续条目），按 `leaderCommit` 推进 commit inde...
  c09_b07.md: ...ndex，回复成功。  这保证：一次成功 `AppendEntries` 后，follower 日志在新条目末尾之前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，leader 递减该 follower...
  c09_b07.md: ...r 日志在新条目末尾之前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配...
  c09_b07.md: ...查保持一致。  一致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 le...
  c09_b07.md: ...er 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 leader 日志的机制。...
  c09_b07.md: ...**答案：** Leader 发送 `AppendEntries(term, prevLogIndex, prevLogTerm, entries,...
  c09_b07.md: ...evLogIndex` 处的日志条目 term 不是 `prevLogTerm`，拒绝（follower 日志在此处与 leader 分歧）。  否则，追加新条目（覆盖任何冲突的后续条目），按 `leaderCommit` 推进 commit inde...
  c09_b07.md: ...ndex，回复成功。  这保证：一次成功 `AppendEntries` 后，follower 日志在新条目末尾之前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，leader 递减该 follower...
  c09_b07.md: ...r 日志在新条目末尾之前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配...
  c09_b07.md: ...查保持一致。  一致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 le...
  c09_b07.md: ...er 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 leader 日志的机制。...
  c09_b08.md: ...与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向...
  c09_b08.md: ...a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日志缺少 `pr...
  c09_b08.md: ...extIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常...
  c09_b08.md: ...er 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit`...
  c09_b08.md: ...是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追...
  c09_b08.md: ...ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `Appe...
  c09_b08.md: ...个节点。网络分区期间，2 个节点与另 3 个隔离。走一遍每侧发生什么，用户可见行为如何。  提示  多数侧（3）可以选 leader 并提交；少数侧（2）不能。...
  c09_b08.md: ...与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向...
  c09_b08.md: ...a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日志缺少 `pr...
  c09_b08.md: ...extIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常...
  c09_b08.md: ...er 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit`...
  c09_b08.md: ...是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追...
  c09_b08.md: ...ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `Appe...
  c09_b08.md: ...个节点。网络分区期间，2 个节点与另 3 个隔离。走一遍每侧发生什么，用户可见行为如何。  提示  多数侧（3）可以选 leader 并提交；少数侧（2）不能。...
  c09_b09.md: ...**答案：** 分区期间：  _多数侧（3 节点）：_  (a) 若原 leader 在多数侧，它继续运行。它的心跳到达 3 中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b)...
  c09_b09.md: ...侧，它继续运行。它的心跳到达 3 中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b) 若原 leader 在少数侧，多数侧 follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得...
  c09_b09.md: ...follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得投票（多数），成为 leader。新提交继续。  (c) 无论哪种，多数侧继续服务写和线性化读。  _少数侧（2 节点）：_  (a) 若原 leade...
  c09_b09.md: ...eader。新提交继续。  (c) 无论哪种，多数侧继续服务写和线性化读。  _少数侧（2 节点）：_  (a) 若原 leader 在少数侧，它试图提交但拿不到多数（它只有自己加可能一个可达节点；总共 2 < 3）。写阻塞。  (b) 若原 lead...
  c09_b09.md: ...ader 在少数侧，它试图提交但拿不到多数（它只有自己加可能一个可达节点；总共 2 < 3）。写阻塞。  (b) 若原 leader 在多数侧，少数侧 follower 等待心跳。它们定时器触发。其中一个成为 candidate，向少数侧另一节点请求投...
  c09_b09.md: ...一个成为 candidate，向少数侧另一节点请求投票。它得到 2 票（自己和另一节点），但 2 < 3，因此不能成为 leader。反复重选失败。  (c) 少数侧不能服务写。若应用接受过期，它可以服务过期读（最后已知提交条目）；否则读也失败。  _...
  c09_b09.md: ..._用户可见：_  * 连接到多数侧的客户端：正常运行，无感知分区。 * 连接到少数侧的客户端：写失败，返回 “no leader” 或超时；读失败或返回过期数据。  _恢复：_  * 分区愈合后，少数侧节点在多数侧心跳中发现更高 term。它们 r...
  c09_b09.md: ...到多数侧，重新加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Raft 的 leader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leade...
  c09_b09.md: ...；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Raft 的 leader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提...
  c09_b09.md: ...eader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序...
  c09_b09.md: ...EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 le...
  c09_b09.md: ...er 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Ra...
  c09_b09.md: ...换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。L...
  c09_b09.md: ...l。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每...
  c09_b09.md: ...r。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 leader 处理能力和到 followers 的网络带宽限制。分片（multi-r...
  c09_b09.md: ...wers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 leader 处理能力和到 followers 的网络带宽限制。分片（multi-raft per range，如 Cockroac...
  c09_b09.md: ...的网络带宽限制。分片（multi-raft per range，如 CockroachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换...
  c09_b09.md: ...aft per range，如 CockroachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时...
  c09_b09.md: ...roachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leade...
  c09_b09.md: ...重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某...
  c09_b09.md: ...eader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端...
  c09_b09.md: ...**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _...
  c09_b09.md: ...(d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 t...
  c09_b09.md: ...户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 trivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产...
  c09_b09.md: ...。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 trivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c...
  c09_b09.md: ...ivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c) **可预测性能**。Leader 行为可预测；延迟和吞吐特性已被充分理解。  (d) **运维...
  c09_b09.md: ...(b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c) **可预测性能**。Leader 行为可预测；延迟和吞吐特性已被充分理解。  (d) **运维工具**。几乎所有 Raft 系统都有可见性工具（etcd...
  c09_b09.md: ...所有 Raft 系统都有可见性工具（etcd status、CockroachDB raft inspector）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based...
  c09_b09.md: ...achDB raft inspector）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或...
  c09_b09.md: ...or）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中...
  c09_b09.md: ...权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 leader 协议获胜。  2026 年的主导...
  c09_b09.md: ...sed Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 leader 协议获胜。  2026 年的主导模式是 multi-Raft：多 Raft 组并行，每组处理一个分片。这结合了每组内...
  c09_b09.md: ...**答案：** 分区期间：  _多数侧（3 节点）：_  (a) 若原 leader 在多数侧，它继续运行。它的心跳到达 3 中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b)...
  c09_b09.md: ...侧，它继续运行。它的心跳到达 3 中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b) 若原 leader 在少数侧，多数侧 follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得...
  c09_b09.md: ...follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得投票（多数），成为 leader。新提交继续。  (c) 无论哪种，多数侧继续服务写和线性化读。  _少数侧（2 节点）：_  (a) 若原 leade...
  c09_b09.md: ...eader。新提交继续。  (c) 无论哪种，多数侧继续服务写和线性化读。  _少数侧（2 节点）：_  (a) 若原 leader 在少数侧，它试图提交但拿不到多数（它只有自己加可能一个可达节点；总共 2 < 3）。写阻塞。  (b) 若原 lead...
  c09_b09.md: ...ader 在少数侧，它试图提交但拿不到多数（它只有自己加可能一个可达节点；总共 2 < 3）。写阻塞。  (b) 若原 leader 在多数侧，少数侧 follower 等待心跳。它们定时器触发。其中一个成为 candidate，向少数侧另一节点请求投...
  c09_b09.md: ...一个成为 candidate，向少数侧另一节点请求投票。它得到 2 票（自己和另一节点），但 2 < 3，因此不能成为 leader。反复重选失败。  (c) 少数侧不能服务写。若应用接受过期，它可以服务过期读（最后已知提交条目）；否则读也失败。  _...
  c09_b09.md: ..._用户可见：_  * 连接到多数侧的客户端：正常运行，无感知分区。 * 连接到少数侧的客户端：写失败，返回 “no leader” 或超时；读失败或返回过期数据。  _恢复：_  * 分区愈合后，少数侧节点在多数侧心跳中发现更高 term。它们 r...
  c09_b09.md: ...到多数侧，重新加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Raft 的 leader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leade...
  c09_b09.md: ...；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Raft 的 leader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提...
  c09_b09.md: ...eader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序...
  c09_b09.md: ...EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 le...
  c09_b09.md: ...er 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Ra...
  c09_b09.md: ...换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。L...
  c09_b09.md: ...l。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每...
  c09_b09.md: ...r。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 leader 处理能力和到 followers 的网络带宽限制。分片（multi-r...
  c09_b09.md: ...wers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 leader 处理能力和到 followers 的网络带宽限制。分片（multi-raft per range，如 Cockroac...
  c09_b09.md: ...的网络带宽限制。分片（multi-raft per range，如 CockroachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换...
  c09_b09.md: ...aft per range，如 CockroachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时...
  c09_b09.md: ...roachDB）是绕过方法。  (b) **Leader 瓶颈**。慢或重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leade...
  c09_b09.md: ...重载的 leader 会拖慢整个集群。运维通过 leader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某...
  c09_b09.md: ...eader 放置调优避免热点。  (c) **故障切换不可用窗口**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端...
  c09_b09.md: ...**。Leader 死亡时集群暂停选举（通常数秒）。无 leader 协议没有这种窗口。  (d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _...
  c09_b09.md: ...(d) **地理不对称**。Leader 在某区域意味着所有写都付到该区域的往返；其他区域客户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 t...
  c09_b09.md: ...户端看到更高写延迟。无 leader 协议可让任何区域接受写。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 trivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产...
  c09_b09.md: ...。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 trivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c...
  c09_b09.md: ...ivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c) **可预测性能**。Leader 行为可预测；延迟和吞吐特性已被充分理解。  (d) **运维...
  c09_b09.md: ...(b) **实现成熟度**。许多生产级 Raft 库存在；无 leader 等价物很少。  (c) **可预测性能**。Leader 行为可预测；延迟和吞吐特性已被充分理解。  (d) **运维工具**。几乎所有 Raft 系统都有可见性工具（etcd...
  c09_b09.md: ...所有 Raft 系统都有可见性工具（etcd status、CockroachDB raft inspector）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based...
  c09_b09.md: ...achDB raft inspector）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或...
  c09_b09.md: ...or）；无 leader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中...
  c09_b09.md: ...权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 leader 协议获胜。  2026 年的主导...
  c09_b09.md: ...sed Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 leader 协议获胜。  2026 年的主导模式是 multi-Raft：多 Raft 组并行，每组处理一个分片。这结合了每组内...
  c09_b10.md: ...学和代码可读性收益真实。  反驳：完整 Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos...
  c09_b10.md: ...LM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上 apply 的混淆。** 条目在多数复制后 _提交_；只有在提交之后才 _应用_ 到状态机。许多 LLM 实现一追...
  c09_b10.md: ...多数复制后 _提交_；只有在提交之后才 _应用_ 到状态机。许多 LLM 实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader...
  c09_b10.md: ...，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 pr...
  c09_b10.md: ...会让不一致日志保持不一致。  (e) **NextIndex 回退缺失。** `AppendEntries` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LLM 实现有时不减，导致无限循环重试，看起来像 hang。  (f) **...
  c09_b11.md: ...LLM 生成的代码匹配高层描述但错过微妙不变式。Raft 论文的图 8 case 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous term 条目”翻译成正确代码，需要深入理解不变式，而这正...
  c10_b02.md: ...文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepa...
  c10_b03.md: ...empool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 A...
  c10_b03.md: ...在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuff 的三阶段结构与 PBFT...
  c10_b03.md: ...(N)$。  HotStuff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1 个投票组成 _Quorum Certificate（QC）_。 * QC 是...
  c10_b03.md: ...ff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1 个投票组成 _Quorum Certificate（QC）_。 * QC 是某阶段结果的支持证明，可传...
  c10_b03.md: ...段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotS...
  c10_b03.md: ...)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotStuff 把各阶段 pipeline。Leader 在为块 $k+1$ 收集第一阶段投票时，可以同时为块 $k$ 收集第二阶段投票、为块 $k-1$ 收集第三阶段投票。这...
  c10_b03.md: ...empool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 A...
  c10_b03.md: ...在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuff 的三阶段结构与 PBFT...
  c10_b03.md: ...(N)$。  HotStuff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1 个投票组成 _Quorum Certificate（QC）_。 * QC 是...
  c10_b03.md: ...ff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1 个投票组成 _Quorum Certificate（QC）_。 * QC 是某阶段结果的支持证明，可传...
  c10_b03.md: ...段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotS...
  c10_b03.md: ...)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotStuff 把各阶段 pipeline。Leader 在为块 $k+1$ 收集第一阶段投票时，可以同时为块 $k$ 收集第二阶段投票、为块 $k-1$ 收集第三阶段投票。这...
  c10_b04.md: ...blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：leader 的工作不再是“排序交易”（DAG 已经排好），而是...
  c10_b04.md: ...ni-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：leader 的工作不再是“排序交易”（DAG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 leader 的工作量...
  c10_b04.md: ...G 之上：leader 的工作不再是“排序交易”（DAG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 leader 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal D...
  c10_b04.md: ...n 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block，因为 DAG 本身即是“共识”机制（每个 block 都被签名并...
  c10_b04.md: ...ecommit），每阶段都需要 2f+1 of 3f+1 票。 * 基于轮次：若一轮未达成决定，下一轮以确定性轮转选新 leader。 * 锁定：一旦验证者 precommit 某值，它在后续轮次中被“锁定”到该值。  Tendermint 比 Hot...
  c10_b05.md: ...态提交快。  含义：Phase 1 很少运行的系统（稳定领导）可以让 accept 法定人数很小以换取快速提交，代价是 leader 变更需要更大的 prepare 法定人数。  这一洞见用于一些 2026 年分布式数据库设计，以降低 per-comm...
  c10_b07.md: ...ates，无需重播所有投票。  **答案：** HotStuff 的关键创新是 _线性 view change_ ：当 leader 被怀疑且新 leader 接管时，消息复杂度是 $O(N)$ 而非 PBFT 的 $O(N^2)$。...
  c10_b07.md: ...。  **答案：** HotStuff 的关键创新是 _线性 view change_ ：当 leader 被怀疑且新 leader 接管时，消息复杂度是 $O(N)$ 而非 PBFT 的 $O(N^2)$。...
  c10_b08.md: ...tes（QC）_。QC 是 2f+1 个签名在特定阶段结果上的集合。QC 是“该值已获支持”的紧凑、可传递证明。  当 leader 为提议的 phase k 收集到 2f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff...
  c10_b08.md: ...line HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change...
  c10_b08.md: ...即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让...
  c10_b08.md: ...et$。只要两个法定人数相交，两个法定人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1$），$Q_2$ 很小（如...
  c10_b08.md: ...re 法定人数与任意大小为 2 的 accept 法定人数都相交（因为 $(N - 1) + 2 > N$）。  代价：leader 变更昂贵（需要 $N - 1$ 个副本可达才能 prepare）。收益：每次提交只需 2 个副本 ack，稳态提交延迟...
  c10_b08.md: ...：每次提交只需 2 个副本 ack，稳态提交延迟非常低。  5 节点集群：$Q_1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  F...
  c10_b08.md: ...1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  FoundationDB 和一些新系统用此变体生产调优。权衡：用较慢但罕见的路径换...
  c10_b08.md: ...tes（QC）_。QC 是 2f+1 个签名在特定阶段结果上的集合。QC 是“该值已获支持”的紧凑、可传递证明。  当 leader 为提议的 phase k 收集到 2f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff...
  c10_b08.md: ...line HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change...
  c10_b08.md: ...即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让...
  c10_b08.md: ...et$。只要两个法定人数相交，两个法定人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1$），$Q_2$ 很小（如...
  c10_b08.md: ...re 法定人数与任意大小为 2 的 accept 法定人数都相交（因为 $(N - 1) + 2 > N$）。  代价：leader 变更昂贵（需要 $N - 1$ 个副本可达才能 prepare）。收益：每次提交只需 2 个副本 ack，稳态提交延迟...
  c10_b08.md: ...：每次提交只需 2 个副本 ack，稳态提交延迟非常低。  5 节点集群：$Q_1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  F...
  c10_b08.md: ...1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  FoundationDB 和一些新系统用此变体生产调优。权衡：用较慢但罕见的路径换...
  c10_b10.md: ...ipelining 与 Raft 的日志复制。两者都可有多个“飞行中的决定”——结构上有什么不同？  提示  Raft leader 串行化提交；多条目可同时在空中但各自独立发送。HotStuff pipeline 让不同提议的三个投票阶段重叠。  *...
  c10_b10.md: ...三个投票阶段重叠。  **答案：** 两个协议都支持多个在飞决定，但结构不同。  _Raft pipelining_ ：leader 可并行为多个日志条目发送 `AppendEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条...
  c10_b10.md: ...ing_ ：leader 可并行为多个日志条目发送 `AppendEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到...
  c10_b10.md: ...提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff pipelining_ ：HotStuff 每个决定...
  c10_b10.md: ...个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$...
  c10_b10.md: ...ipelining 与 Raft 的日志复制。两者都可有多个“飞行中的决定”——结构上有什么不同？  提示  Raft leader 串行化提交；多条目可同时在空中但各自独立发送。HotStuff pipeline 让不同提议的三个投票阶段重叠。  *...
  c10_b10.md: ...三个投票阶段重叠。  **答案：** 两个协议都支持多个在飞决定，但结构不同。  _Raft pipelining_ ：leader 可并行为多个日志条目发送 `AppendEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条...
  c10_b10.md: ...ing_ ：leader 可并行为多个日志条目发送 `AppendEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到...
  c10_b10.md: ...提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff pipelining_ ：HotStuff 每个决定...
  c10_b10.md: ...个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$...
  c11_b01.md: ...失败、为谁设计。   ### 11.1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Lead...
  c11_b01.md: ...### 11.1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）...
  c11_b01.md: ...1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）、基于 gossi...
  c11_b01.md: ...ader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）、基于 gossip 的收敛、人工干预。   3. **一致性模型是什么？** 线性化、可串行化、快照、因...
  c11_b01.md: ...sensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置...
  c11_b01.md: ...ability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：Cockroa...
  c11_b01.md: ...t。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、DynamoDB、Riak。任意...
  c11_b01.md: ...、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单 leader）；FoundationDB（sequencer 设计）。  协议决定写延迟、故障切换行为和冲突解决机制。它是系统最重要...
  c11_b01.md: ...失败、为谁设计。   ### 11.1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Lead...
  c11_b01.md: ...### 11.1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）...
  c11_b01.md: ...1 四个问题  对任何分布式数据库，问：    1. **复制协议是什么？** 单 leader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）、基于 gossi...
  c11_b01.md: ...ader、多 leader、无 leader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）、基于 gossip 的收敛、人工干预。   3. **一致性模型是什么？** 线性化、可串行化、快照、因...
  c11_b01.md: ...sensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置...
  c11_b01.md: ...ability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：Cockroa...
  c11_b01.md: ...t。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、DynamoDB、Riak。任意...
  c11_b01.md: ...、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单 leader）；FoundationDB（sequencer 设计）。  协议决定写延迟、故障切换行为和冲突解决机制。它是系统最重要...
  c11_b02.md: ...#### 问题 2：故障恢复语义  系统如何处理节点故障？具体：  * **Leader 重选时间** ：Raft 系统通常 1-5 秒；etcd 快（良好条件下亚秒）；Spanner 的 Paxos 带租约...
  c11_b02.md: ...* **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-...
  c11_b02.md: ...split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需...
  c11_b02.md: ...#### 问题 2：故障恢复语义  系统如何处理节点故障？具体：  * **Leader 重选时间** ：Raft 系统通常 1-5 秒；etcd 快（良好条件下亚秒）；Spanner 的 Paxos 带租约...
  c11_b02.md: ...* **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-...
  c11_b02.md: ...split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需...
  c11_b03.md: ...性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lease 读很快 Consiste...
  c11_b03.md: ...e-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition...
  c11_b03.md: ...MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable...
  c11_b04.md: ...AWS）  Question | Answer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Failure-recovery | 透明（存储层对用户不透明）；自动扩缩容与自动修复 Con...
  c11_b04.md: ...ssandra  Question | Answer ---|--- Replication protocol | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 gossip；超时后排除...
  c11_b07.md: ...Firecracker 数秒内重新启动，存储层独立处理副本故障）。一致性模型为强一致 serializable；读可走 leader（即时）或 follower（有界过期）。分区行为在存储层为 CP（丢失存储法定人数时写阻塞）。PACELC：分区下 P...
  c11_b09.md: ...议）：  Question | Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency m...
  c13_b10.md: ...200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。...
  c13_b14.md: ...者角色的节点死亡：  (a) 协调者状态 range 的 Raft group 检测到故障（心跳超时）并在数秒内选出新 leader。  (b) 新 leader 继承协调者状态（事务记录、参与者列表、当前提交阶段）。  (c) 新 leader 从停...
  c13_b14.md: ...协调者状态 range 的 Raft group 检测到故障（心跳超时）并在数秒内选出新 leader。  (b) 新 leader 继承协调者状态（事务记录、参与者列表、当前提交阶段）。  (c) 新 leader 从停止处恢复 2PC 协议。若前任...
  c13_b14.md: ...出新 leader。  (b) 新 leader 继承协调者状态（事务记录、参与者列表、当前提交阶段）。  (c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 lea...
  c13_b14.md: ...继承协调者状态（事务记录、参与者列表、当前提交阶段）。  (c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 a...
  c13_b14.md: ...der 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参...
  c13_b14.md: ...PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换具有鲁棒性：它们通过 Raft term/le...
  c14_b05.md: ...过期数据做欺诈决策），单区域部署，工作负载包含每晚重训欺诈模型的批处理作业。  步骤 1（第11章诊断）：复制 = 单 leader 或每分片共识。故障恢复：偏好亚秒（欺诈决策对延迟敏感）。一致性 = 读线性化（不能用过期数据做欺诈决策）。分区行为 =...
  c14_b07.md: ...ol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency model | Lineariza...
  c14_b07.md: ...每 range Raft Failure-recovery | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency model | Linearizable；提供过期读 | Serializab...
  c15_b03.md: ...秒。  对需要 read-your-writes 的操作，客户端可请求 `ConsistentRead=true`，对 leader 做同步读。代价：通常是最终读延迟的 2 倍，并消耗更多 capacity unit。  跨区域（Global Tabl...
  c15_b04.md: ...置控制持久性：当 `min.insync.replicas` 个副本 ack 后，partition 的写才算持久。若 leader 在传播前故障，写丢失。   ### 15.6 跨一致性模型的计数器工作负载  课程大纲中的线索 B：计数器 / 排行榜...
  c19_b08.md: ...last-known-good 注册表。  **答案：** 诊断：Consul 故障转移期间，集群重新选举 Raft leader。选举期间新写被阻塞（通常 5-15 秒）。此窗口查询 Consul 的客户端可能收到过期或错误响应。  若客户端配置为...
  c19_b08.md: ...) **配置客户端使用 stale 读**：Consul 支持从任意副本 stale 读（`?stale=true`）；leader 不可达时客户端回退到 stale 读。权衡：故障转移期间数据略过期；对服务发现可接受。  (b) **客户端缓存带宽限...
  c23_b04.md: ...令（优雅关闭）。 * 14:15 UTC：EU-West 节点下线。集群的 Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20...
  c23_b04.md: ...板添加区域健康。     * 故障转移窗口内有一笔支付被重复扣款（Raft 重选举完成前某 shard 有 30 秒双 leader 期）。行动项：调查并修复；这与第1章事故的同类 bug。     * EU-West API gateway 的重试预...
  c23_b04.md: ...* 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game...
  c23_b09.md: ...**注入什么故障？** 区域故障可多种建模：整个区域关闭（所有节点终止）；区域网络隔离（仍在运行但不可达）；区域数据库 leader 故障（特定子集）。选择最贴近真实风险的那种。  (b) **稳态假设是什么？** 具体 SLI：支付成功率保持在 X%...
  c27_b04.md: ...peration 行为。  现实：CockroachDB 默认读写事务是可串行化（serializable）；读可以在 leader（per-operation linearizable）或 follower（bounded-staleness）。这段...
  c27_b04.md: ...ount_balance_authoritative(account_id):         # 线性化读；显式强制 leader replica         with db.transaction() as tx:             re...
  c28_b10.md: ...；分区下 sloppy quorum | | 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求 → leader → follower | 复制状态机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派...
  c28_b10.md: ...+ 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下跳过 QC 验证；竞争新 leader 间视图变更竞态 | | 6 | 30 分钟读懂数据库 | Ch.11 §11.3 | 任何厂商文档 | 一页 PACE...
  c28_b14.md: ...Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-brain 可能需要人工干预。 * **一致...
  c28_b14.md: ...clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revisio...
  c30_b06.md: ...至少共享一个节点；共识安全基础。 * **Raft**：Ongaro-Ousterhout 2014 共识协议。分解为 leader 选举、日志复制、安全性。 * **Retry budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障...
  c30_b09.md: ...en；新主库不应能在旧主库仍自认为主库时写入。     * Ch.7（共识，防止 split-brain）：基于共识的 leader 选举可防止双主状态。     * Ch.5（复制，主库 failover 纪律）：无 fencing 的 failov...
  c30_b14.md: ...├── ch09_raft/     │   ├── raft_minimal.py   — ~300 lines: leader election + AppendEntries + commit     │   ├── kv_state_mach...
  c30_b16.md: ...FLP 说确定性共识在纯异步下有界终止不可能。真实系统用部分同步。 | Ch.7 | | 12 | "Paxos 选举 leader。" | 单法令 Paxos 无 leader。Multi-Paxos 的 leader 是优化，不是安全要求。 | C...
  c30_b16.md: ...真实系统用部分同步。 | Ch.7 | | 12 | "Paxos 选举 leader。" | 单法令 Paxos 无 leader。Multi-Paxos 的 leader 是优化，不是安全要求。 | Ch.8 | | 13 | "Raft 在多数派...
  c30_b16.md: ...12 | "Paxos 选举 leader。" | 单法令 Paxos 无 leader。Multi-Paxos 的 leader 是优化，不是安全要求。 | Ch.8 | | 13 | "Raft 在多数派复制后立刻提交条目。" | 几乎对；前任期...
  c30_b19.md: ...异步复制；主库在传播前死亡 | Ch.5 §5.3，Ch.5 §5.4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fenci...

=== token (316 occurrences) ===
  c01_b03.md: ...个系统在身份上说谎了吗？** 它是否假设某个节点是“领导者”，却没有协议能证明这一点？它是否假设锁是唯一的，却没有围栏 token？它是否假设它正在对话的节点就是它所认为的那个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happ...
  c01_b08.md: ...Adaeze。早上 6:00，Olamide 有了修复方案。到 9:00，她完成了事后复盘。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章），以及用于协调者状态的 Raft 支持提交日志（第9章）。复盘命名了系统一直讲述的三...
  c01_b09.md: ...接上的一次单一重试。对每个谎言分别走一遍，什么本可以预防它？为什么单一修复不够？  提示  每个谎言需要不同纪律。幂等 token 解决一个。fencing token 带来的协调者唯一性解决另一个。时钟偏移有界的时间戳解决第三个。  **答案：**...
  c01_b09.md: ...遍，什么本可以预防它？为什么单一修复不够？  提示  每个谎言需要不同纪律。幂等 token 解决一个。fencing token 带来的协调者唯一性解决另一个。时钟偏移有界的时间戳解决第三个。  **答案：** 对于失败谎言，请求上的幂等 toke...
  c01_b09.md: ...oken 带来的协调者唯一性解决另一个。时钟偏移有界的时间戳解决第三个。  **答案：** 对于失败谎言，请求上的幂等 token 会让第二个协调者的重试被识别为“我已经处理过这个事务 ID”并拒绝——第二次提交变成 no-op 而不是重复扣款。银行...
  c01_b09.md: ...C 时间中先发生，即使两者都在 NTP 时钟的 5 ms 窗口内完成。  对于身份谎言，协调者角色上的 fencing token（第18章）会让银行拒绝第二次提交，因为第二个协调者的 token 会低于第一个。即使两者都相信自己是活跃协调者，银行也...
  c01_b09.md: ...成。  对于身份谎言，协调者角色上的 fencing token（第18章）会让银行拒绝第二次提交，因为第二个协调者的 token 会低于第一个。即使两者都相信自己是活跃协调者，银行也会根据单调 token 顺序知道该信任哪一个。...
  c01_b09.md: ...会让银行拒绝第二次提交，因为第二个协调者的 token 会低于第一个。即使两者都相信自己是活跃协调者，银行也会根据单调 token 顺序知道该信任哪一个。...
  c01_b11.md: ...领导者被选出，分区恢复后，原领导者从自己的视角仍作为领导者行动。  第三，即使锁确实被唯一持有，也没有 fencing token。一条旧领导者在锁过期前发出、但尚未送达下游系统的动作，在新领导者发出自己的动作后到达下游。下游系统无法区分“由当前领导...
  c01_b12.md: ...修复方案是 fencing token，第18章会覆盖：每次获取锁都会增加一个单调计数器，每个下游动作都携带该 token。下游拒绝任何低于它所见过最高 to...
  c01_b12.md: ...修复方案是 fencing token，第18章会覆盖：每次获取锁都会增加一个单调计数器，每个下游动作都携带该 token。下游拒绝任何低于它所见过最高 token 的 token 的动作。即使两个节点短暂相信自己持有锁，也只有 token...
  c01_b12.md: ...en，第18章会覆盖：每次获取锁都会增加一个单调计数器，每个下游动作都携带该 token。下游拒绝任何低于它所见过最高 token 的 token 的动作。即使两个节点短暂相信自己持有锁，也只有 token 更高的那个能说服下游行动。  **Q3.*...
  c01_b12.md: ...覆盖：每次获取锁都会增加一个单调计数器，每个下游动作都携带该 token。下游拒绝任何低于它所见过最高 token 的 token 的动作。即使两个节点短暂相信自己持有锁，也只有 token 更高的那个能说服下游行动。  **Q3.** 一个微服务架...
  c01_b12.md: ...token。下游拒绝任何低于它所见过最高 token 的 token 的动作。即使两个节点短暂相信自己持有锁，也只有 token 更高的那个能说服下游行动。  **Q3.** 一个微服务架构有 14 个服务，单个请求通常触及其中 5 到 8 个。其...
  c01_b15.md: ...“系统整体经历了什么”之间的缺口里。  Crescent 事件在单一重试中同时利用了三个谎言。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章）和 Raft 支持的提交日志（第9章）。单一修复无法弥合这个缺口。  这本书训练的心...
  c03_b15.md: ...a m: m.timestamp)` 错误。  第1章提到 Crescent 多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；Raft 在...
  c03_b15.md: ...HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；Raft 在第9章；Spanner / CockroachDB / Calvin / TrueTime 在...
  c06_b06.md: ...部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-2...
  c06_b06.md: ...对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是 budget 内与超出...
  c06_b07.md: ...部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-2...
  c06_b07.md: ...对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是预算内与超出的差别。高频...
  c12_b10.md: ...希使用虚拟节点（每物理节点 256+）做负载分布。LLM 生成代码通常每个节点一个位置，导致负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确保同一键的多个副本不落同一物理节点。LLM 代码可能未正确处理多副本场景，...
  c12_b10.md: ...载分布。LLM 生成代码通常每个节点一个位置，导致负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确保同一键的多个副本不落同一物理节点。LLM 代码可能未正确处理多副本场景，让同一键的两个副本落到同一节点。  (c)...
  c12_b10.md: ...** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使...
  c12_b10.md: ...成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使分配在节点重启后保留并防止 flappin...
  c12_b10.md: ...希使用虚拟节点（每物理节点 256+）做负载分布。LLM 生成代码通常每个节点一个位置，导致负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确保同一键的多个副本不落同一物理节点。LLM 代码可能未正确处理多副本场景，...
  c12_b10.md: ...载分布。LLM 生成代码通常每个节点一个位置，导致负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确保同一键的多个副本不落同一物理节点。LLM 代码可能未正确处理多副本场景，让同一键的两个副本落到同一节点。  (c)...
  c12_b10.md: ...** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使...
  c12_b10.md: ...成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使分配在节点重启后保留并防止 flappin...
  c17_b11.md: ...原语。分布式缓存（Redis Cluster gossip；失效模式）和分布式锁（Redlock 辩论；fencing token 作为正确原语）。Crescent 支付系统使用 fencing token 保证协调者唯一性；本章解释这是防御 “身份...
  c17_b11.md: ...式锁（Redlock 辩论；fencing token 作为正确原语）。Crescent 支付系统使用 fencing token 保证协调者唯一性；本章解释这是防御 “身份谎言” 的正确手段。  ---...
  c18_b01.md: ...", node_id, nx=True, ex=30)   > 说明性伪代码；bug 在于原语选择（无 fencing token 的超时锁），而非语法。  这就是 Redlock 模式。代码很短。生产环境中，它偶尔会允许两个领导者同时行动。原因是分...
  c18_b01.md: ...为什么朴素锁会失败、正确原语长什么样）。第1章 Crescent 支付系统 “身份谎言” 的修复使用了 fencing token；本章解释原因。   ### 18.1 分布式缓存  缓存是一种性能模式：把昂贵操作结果存在快速查找存储中，后续请求避免...
  c18_b03.md: ...己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该 token...
  c18_b03.md: ...# Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该 token 伴随对受保护资源的每个操作。  受保护资源（存储系统、API 端点）...
  c18_b03.md: ...ns  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该 token 伴随对受保护资源的每个操作。  受保护资源（存储系统、API 端点）跟踪已接受的最高 token...
  c18_b03.md: ...token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该 token 伴随对受保护资源的每个操作。  受保护资源（存储系统、API 端点）跟踪已接受的最高 token。任何带更低 toke...
  c18_b03.md: ...token 高）。该 token 伴随对受保护资源的每个操作。  受保护资源（存储系统、API 端点）跟踪已接受的最高 token。任何带更低 token 的操作被拒绝。  如何修复 Redlock 的失败模式：  (a) 进程暂停：客户端 A 用...
  c18_b03.md: ...oken 伴随对受保护资源的每个操作。  受保护资源（存储系统、API 端点）跟踪已接受的最高 token。任何带更低 token 的操作被拒绝。  如何修复 Redlock 的失败模式：  (a) 进程暂停：客户端 A 用 token 100 获得...
  c18_b03.md: ...。任何带更低 token 的操作被拒绝。  如何修复 Redlock 的失败模式：  (a) 进程暂停：客户端 A 用 token 100 获得锁；暂停 60 秒（TTL 过期）；客户端 B 用 token 101 获得锁（锁服务单调递增）。A 恢复...
  c18_b03.md: ...模式：  (a) 进程暂停：客户端 A 用 token 100 获得锁；暂停 60 秒（TTL 过期）；客户端 B 用 token 101 获得锁（锁服务单调递增）。A 恢复后发送 “token 100 写”，存储拒绝（101 > 100）。锁服务视...
  c18_b03.md: ...获得锁；暂停 60 秒（TTL 过期）；客户端 B 用 token 101 获得锁（锁服务单调递增）。A 恢复后发送 “token 100 写”，存储拒绝（101 > 100）。锁服务视角已 “释放锁”；存储视角一致。  (b) 时钟漂移：token...
  c18_b03.md: ...token 100 写”，存储拒绝（101 > 100）。锁服务视角已 “释放锁”；存储视角一致。  (b) 时钟漂移：token 逻辑不涉及时钟。锁服务计数器是权威。即使客户端对时间看法 wildly 不同，资源的 “最高已见 token” 提供正...
  c18_b03.md: ...时钟漂移：token 逻辑不涉及时钟。锁服务计数器是权威。即使客户端对时间看法 wildly 不同，资源的 “最高已见 token” 提供正确排序。  代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis...
  c18_b03.md: ...看法 wildly 不同，资源的 “最高已见 token” 提供正确排序。  代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠...
  c18_b03.md: ...fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。” 这过于绝对。Redloc...
  c18_b03.md: ...effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：用 fencing token。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fe...
  c18_b03.md: ...坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁...
  c18_b04.md: ...顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing toke...
  c18_b04.md: ...oken。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短...
  c18_b04.md: ...复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有...
  c18_b04.md: ...获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有 token 更高的能行...
  c18_b04.md: ...任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有 token 更高的能行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fen...
  c18_b04.md: ...行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram...
  c18_b04.md: ...会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram         autonumber         par...
  c18_b04.md: ...101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank: commit (token=100)         Bank->>Ba...
  c18_b04.md: ...ired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank: commit (token=101)         Bank->>Ba...
  c18_b04.md: ...k-->>C1: STALE_TOKEN   即使 C1 醒来仍以为自己持有锁（经典的基于超时锁失败模式），银行的单调 token 检查拒绝其提交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 f...
  c18_b04.md: ...交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 fencing token）的组合是正确生产模式。朴素 Redlock 是错误模式；正确模式更复杂，但复杂性必要。...
  c18_b04.md: ...顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing toke...
  c18_b04.md: ...oken。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短...
  c18_b04.md: ...复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有...
  c18_b04.md: ...获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有 token 更高的能行...
  c18_b04.md: ...任何带更低 token 的提交被拒绝。  (c) 若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有 token 更高的能行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fen...
  c18_b04.md: ...行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram...
  c18_b04.md: ...会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram         autonumber         par...
  c18_b04.md: ...101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank: commit (token=100)         Bank->>Ba...
  c18_b04.md: ...ired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank: commit (token=101)         Bank->>Ba...
  c18_b04.md: ...k-->>C1: STALE_TOKEN   即使 C1 醒来仍以为自己持有锁（经典的基于超时锁失败模式），银行的单调 token 检查拒绝其提交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 f...
  c18_b04.md: ...交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 fencing token）的组合是正确生产模式。朴素 Redlock 是错误模式；正确模式更复杂，但复杂性必要。...
  c18_b05.md: ...### 18.4 缓存作为锁的反模式  一个特定反模式：没有 fencing token 时把缓存当分布式锁。       # WRONG     if cache.set("user_processing_...
  c18_b05.md: ...需要正确性的锁不要用缓存。用 etcd、Consul 或 ZooKeeper 做锁；在受保护资源上执行 fencing token。性能代价小（锁获取罕见）；正确性收益大。  对纯效率锁（job queue 去重、幂等提示），基于缓存的锁可以。纪律在...
  c18_b06.md: ...示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯一值作为 k...
  c18_b06.md: ...。  两者都是基于超时协议的知名问题，也适用于许多朴素分布式锁方案，不只是 Redlock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token 的操作被拒绝...
  c18_b06.md: ...于许多朴素分布式锁方案，不只是 Redlock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 to...
  c18_b06.md: ...ock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock...
  c18_b06.md: ...encing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing...
  c18_b06.md: ...en。受保护资源跟踪已接受的最高 token；任何更低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppman...
  c18_b06.md: ...的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fencing t...
  c18_b06.md: ...n 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fencing token，它如何修复分布式锁正确性问题？  提示  单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “锁”，只有更...
  c18_b06.md: ...Q2.** 什么是 fencing token，它如何修复分布式锁正确性问题？  提示  单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “锁”，只有更高 token 能行动。  **答案：** Fencing token 是锁服务在每...
  c18_b06.md: ...如何修复分布式锁正确性问题？  提示  单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “锁”，只有更高 token 能行动。  **答案：** Fencing token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比...
  c18_b06.md: ...存储拒绝更低 token；即使两个客户端持有 “锁”，只有更高 token 能行动。  **答案：** Fencing token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 token。...
  c18_b06.md: ...**答案：** Fencing token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 token。  该 token 伴随受保护资源的每个操作。资源跟踪已接受的最高 token；更低 tok...
  c18_b06.md: ...ng token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 token。  该 token 伴随受保护资源的每个操作。资源跟踪已接受的最高 token；更低 token 的操作被拒绝。  如...
  c18_b06.md: ...锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 token。  该 token 伴随受保护资源的每个操作。资源跟踪已接受的最高 token；更低 token 的操作被拒绝。  如何修复正确性问题：...
  c18_b06.md: ...之前任何已发 token 严格更高的新 token。  该 token 伴随受保护资源的每个操作。资源跟踪已接受的最高 token；更低 token 的操作被拒绝。  如何修复正确性问题：  * 若客户端 A 用 token 100 持有锁并暂停（T...
  c18_b06.md: ...ken 严格更高的新 token。  该 token 伴随受保护资源的每个操作。资源跟踪已接受的最高 token；更低 token 的操作被拒绝。  如何修复正确性问题：  * 若客户端 A 用 token 100 持有锁并暂停（TTL 过期），客户...
  c18_b06.md: ...作。资源跟踪已接受的最高 token；更低 token 的操作被拒绝。  如何修复正确性问题：  * 若客户端 A 用 token 100 持有锁并暂停（TTL 过期），客户端 B 用 token 101 获得锁。A 随后到达资源的 token 10...
  c18_b06.md: ...绝。  如何修复正确性问题：  * 若客户端 A 用 token 100 持有锁并暂停（TTL 过期），客户端 B 用 token 101 获得锁。A 随后到达资源的 token 100 操作被拒绝（资源已见过 101）。A 的过期锁状态无法伤害系统...
  c18_b06.md: ...用 token 100 持有锁并暂停（TTL 过期），客户端 B 用 token 101 获得锁。A 随后到达资源的 token 100 操作被拒绝（资源已见过 101）。A 的过期锁状态无法伤害系统。  * 时钟漂移不影响 token：锁服务的计...
  c18_b06.md: ...到达资源的 token 100 操作被拒绝（资源已见过 101）。A 的过期锁状态无法伤害系统。  * 时钟漂移不影响 token：锁服务的计数器是权威，而非任何客户端时钟。  * 分区愈合：若网络分区，两个客户端各自获得它们认为的锁（若锁服务用共识...
  c18_b06.md: ...户端时钟。  * 分区愈合：若网络分区，两个客户端各自获得它们认为的锁（若锁服务用共识则不可能，但对朴素设置可以想象），token 不同。资源只接受更高 token。  Fencing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护...
  c18_b06.md: ...分区，两个客户端各自获得它们认为的锁（若锁服务用共识则不可能，但对朴素设置可以想象），token 不同。资源只接受更高 token。  Fencing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：...
  c18_b06.md: ...的锁（若锁服务用共识则不可能，但对朴素设置可以想象），token 不同。资源只接受更高 token。  Fencing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：即使锁服务有 bug（对同一把锁...
  c18_b06.md: ...任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：即使锁服务有 bug（对同一把锁发了两个 token），资源的单调检查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统（数据库、消息队列）原生不支持...
  c18_b06.md: ...上更健壮：即使锁服务有 bug（对同一把锁发了两个 token），资源的单调检查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统（数据库、消息队列）原生不支持 fencing token；应用或中间件必须叠加。对正确性关键用例，...
  c18_b06.md: ...查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统（数据库、消息队列）原生不支持 fencing token；应用或中间件必须叠加。对正确性关键用例，这是必要工作。  **Q3.** 基于缓存的 Redlock 式锁何时可接受，...
  c18_b07.md: ...纪律：选择锁前先分类用例。正确性用例使用基于共识的锁（etcd、Consul、ZooKeeper）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide...
  c18_b07.md: ...ncing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent...
  c18_b07.md: ...是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operation 安全。...
  c18_b07.md: ...t 修复同时组合 Raft 锁服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operation 安全。  **答案：** 组合提供两层安全：  (a) **Raft 锁...
  c18_b07.md: ...致。没有它，朴素锁方案可能在分区或故障转移期间把同一把锁发给两个客户端。  (b) **受保护资源上的 fencing token** ：保证资源上的操作按 token 排序。即使锁服务出错（或锁持有者在别人获得锁后暂停恢复），资源强制执行单调 to...
  c18_b07.md: ...转移期间把同一把锁发给两个客户端。  (b) **受保护资源上的 fencing token** ：保证资源上的操作按 token 排序。即使锁服务出错（或锁持有者在别人获得锁后暂停恢复），资源强制执行单调 token 顺序。  两层覆盖不同失败模式...
  c18_b07.md: ...en** ：保证资源上的操作按 token 排序。即使锁服务出错（或锁持有者在别人获得锁后暂停恢复），资源强制执行单调 token 顺序。  两层覆盖不同失败模式：  * 只有 Raft：提供安全获取，但不防御客户端进程暂停。从 Raft 视角持有...
  c18_b07.md: ...两层覆盖不同失败模式：  * 只有 Raft：提供安全获取，但不防御客户端进程暂停。从 Raft 视角持有 “有效” token 的暂停客户端仍可在另一个客户端获得锁后操作资源。 * 只有 Fencing token：提供操作级排序，但不提供 “当...
  c18_b07.md: ...Raft 视角持有 “有效” token 的暂停客户端仍可在另一个客户端获得锁后操作资源。 * 只有 Fencing token：提供操作级排序，但不提供 “当前持有者” 概念。没有锁服务，每个操作都要求资源与所有潜在持有者协商。  组合是正确生产...
  c18_b07.md: ...组合是正确生产模式。实践中：etcd 或 Consul 提供锁服务（基于 Raft）；应用在存储层实现 fencing-token 检查（或依赖支持它的存储系统，如带版本列检查的数据库）。...
  c18_b08.md: ...b queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有...
  c18_b08.md: ...偶尔双处理浪费但无害）可接受。若任务有非幂等副作用（发邮件、扣款、发货）则不可接受。  (b) **带 fencing token 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fe...
  c18_b08.md: ...d 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fencing token 防止过期持有者问题。每次获取较慢（etcd 写是多数复制）。  (c) **数据库行级锁** ：数据库中的一行表示 “...
  c18_b08.md: ...绕专用队列基础设施设计。  **Q2.** Anya 正在为 Stratus DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 R...
  c18_b08.md: ....** Anya 正在为 Stratus DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低...
  c18_b08.md: ...现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB...
  c18_b08.md: ...行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的...
  c18_b08.md: ...顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的 etcd 锁。实...
  c18_b08.md: ...最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的 etcd 锁。实现：  (a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd 锁获取。每次...
  c18_b08.md: ...(a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd 锁获取。每次获取分配一个 fencing token（来自 etcd revision 号的单调递增计数器）。  (b) 领导者的后续操作（发起 range split、再...
  c18_b08.md: ...ion 号的单调递增计数器）。  (b) 领导者的后续操作（发起 range split、再平衡决策、配置变更）携带该 token。  (c) range 的 Raft 状态存储已接受的最高 token。Raft 领导者的提交机制检查：该操作 tok...
  c18_b08.md: ...ge split、再平衡决策、配置变更）携带该 token。  (c) range 的 Raft 状态存储已接受的最高 token。Raft 领导者的提交机制检查：该操作 token >= 已接受最高 token？若是，接受；否则拒绝。  (d) 拒...
  c18_b08.md: ...ken。  (c) range 的 Raft 状态存储已接受的最高 token。Raft 领导者的提交机制检查：该操作 token >= 已接受最高 token？若是，接受；否则拒绝。  (d) 拒绝：操作失败并返回 `STALE_LEADER`。提...
  c18_b08.md: ...的 Raft 状态存储已接受的最高 token。Raft 领导者的提交机制检查：该操作 token >= 已接受最高 token？若是，接受；否则拒绝。  (d) 拒绝：操作失败并返回 `STALE_LEADER`。提议客户端知道自己失去领导权并下...
  c18_b08.md: ...拒绝。  (d) 拒绝：操作失败并返回 `STALE_LEADER`。提议客户端知道自己失去领导权并下台。  实现细节：token 比较发生在 Raft AppendEntries 阶段，而非仅在 API 层。这确保即使过期领导者的 RPC 到达 R...
  c18_b09.md: ...对 Stratus 的具体架构，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 token；过期 token...
  c18_b09.md: ...tus 的具体架构，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “epoch number”，提供运维...
  c18_b09.md: ...，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “epoch number”，提供运维安全。即使在复杂故...
  c18_b09.md: ...ch number”，提供运维安全。即使在复杂故障转移期间（集群重选领导者；旧领导者在网络抖动后重新加入并尝试发命令），token 排序保证只有一位领导者的命令被接受。  代价：每条命令携带 token（几个字节）；每个条目 apply 时检查 to...
  c18_b09.md: ...者；旧领导者在网络抖动后重新加入并尝试发命令），token 排序保证只有一位领导者的命令被接受。  代价：每条命令携带 token（几个字节）；每个条目 apply 时检查 token（很快）。代价可忽略；安全收益巨大。  **Q3.** 团队缓存层...
  c18_b09.md: ...en 排序保证只有一位领导者的命令被接受。  代价：每条命令携带 token（几个字节）；每个条目 apply 时检查 token（很快）。代价可忽略；安全收益巨大。  **Q3.** 团队缓存层（Redis Cluster）出现热门 key 过期时...
  c18_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“2026 年所有分布式锁都应使用 fencing token。不提供它的锁服务已过时。”  **讨论：** 辩护：fencing token 是正确性的正确原语。任何不提供它的锁服...
  c18_b10.md: ...年所有分布式锁都应使用 fencing token。不提供它的锁服务已过时。”  **讨论：** 辩护：fencing token 是正确性的正确原语。任何不提供它的锁服务都有微妙安全洞。随着分布式系统成熟，领域应标准化在正确原语上。  反驳：许多锁...
  c18_b10.md: ...提供它的锁服务都有微妙安全洞。随着分布式系统成熟，领域应标准化在正确原语上。  反驳：许多锁用例纯效率；fencing token 对它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用 fencin...
  c18_b10.md: ...语上。  反驳：许多锁用例纯效率；fencing token 对它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合：锁库应 _...
  c18_b10.md: ...它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合：锁库应 _支持_ fencing token（作为选项）。正确性用例应用使...
  c18_b10.md: ...正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合：锁库应 _支持_ fencing token（作为选项）。正确性用例应用使用它。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etcd、Cons...
  c18_b10.md: ...；工程师必须理解用例并正确选择。  2026 现实：etcd、Consul、ZooKeeper 都支持 fencing token 等价物（revision 号、sequence 号、czxid）。应用代码才是需要纪律的地方。“正确性用例始终用 fe...
  c18_b10.md: ...evision 号、sequence 号、czxid）。应用代码才是需要纪律的地方。“正确性用例始终用 fencing token” 作为 _正确性用例的默认_ 是对的，但作为 _通用默认_ 是错的。  第二个考虑：存储系统对 fencing-tok...
  c18_b10.md: ...ken” 作为 _正确性用例的默认_ 是对的，但作为 _通用默认_ 是错的。  第二个考虑：存储系统对 fencing-token 检查的支持是限制因素。带乐观并发控制（版本列）的数据库天然支持。对象存储（S3）原生不支持；应用必须叠加。存储系统选择...
  c18_b10.md: ...乐观并发控制（版本列）的数据库天然支持。对象存储（S3）原生不支持；应用必须叠加。存储系统选择可能成为 fencing token 采用的瓶颈。  **Q2.** 一个 LLM 被要求 “为我们的微服务实现分布式锁”。它产出一个 Redlock 实现...
  c18_b10.md: ...(c) **受保护资源是什么**。若受保护资源自身有锁（数据库行锁），分布式锁冗余。若资源没有原生锁，fencing token 对锁安全至关重要。LLM 没问。  再三种（任一条都让 Redlock 不足）：  (d) **进程暂停行为**。GC...
  c18_b10.md: ...Redlock 不足）：  (d) **进程暂停行为**。GC 暂停、VM 迁移持有的暂停进程锁在没有 fencing token 时很危险。LLM 没问暂停预期。  (e) **跨区域部署**。5 个 Redis master 跨区域的 Redlo...
  c18_b11.md: ...两个客户端同时持有它的失败模式是什么？进程可能暂停多久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。   ###...
  c18_b11.md: ...失败。正确原语是共识支持的锁服务（etcd、Consul、ZooKeeper）加受保护资源上强制执行的 fencing token。  第1章 Crescent 修复结合基于 Raft 的协调者选举与银行端点的 fencing token。即使两个协...
  c18_b11.md: ...ncing token。  第1章 Crescent 修复结合基于 Raft 的协调者选举与银行端点的 fencing token。即使两个协调者短暂认为自己在位，只有 token 更高者能提交。重复扣款不可能复发。  缓存作为锁是正确性用例的反模式...
  c18_b11.md: ...t 修复结合基于 Raft 的协调者选举与银行端点的 fencing token。即使两个协调者短暂认为自己在位，只有 token 更高者能提交。重复扣款不可能复发。  缓存作为锁是正确性用例的反模式。用共识支持的锁；在资源处用 fencing to...
  c18_b11.md: ...en 更高者能提交。重复扣款不可能复发。  缓存作为锁是正确性用例的反模式。用共识支持的锁；在资源处用 fencing token。代价小；正确性收益大。  第19章讲服务发现与配置：etcd 作为现代默认（替代 ZooKeeper）、Consul、...
  c19_b04.md: ...cd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支...
  c24_b01.md: ...个阶段是 2025-2026 的突破。  (c) **KV cache 作为中心状态**。KV cache 是让模型逐 token 解码的工作内存。它很大（每个活跃请求 1-100 GB），短命（请求结束即释放），并且是内存和吞吐瓶颈。  (d) *...
  c24_b01.md: ...个活跃请求 1-100 GB），短命（请求结束即释放），并且是内存和吞吐瓶颈。  (d) **严格的延迟预算**。每个 token 生成延迟 10-50 ms。在此预算内，系统必须读取 KV cache、计算 next-token logits、采样...
  c24_b01.md: ...延迟预算**。每个 token 生成延迟 10-50 ms。在此预算内，系统必须读取 KV cache、计算 next-token logits、采样、推进状态。超过几毫秒的网络跳数都很重要。  (e) **突发工作负载模式**。有的请求短（单次问答...
  c24_b03.md: ...LLM 应用中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成**：SGLang 原生支持...
  c24_b03.md: ...nt 系统。它是 prompt 结构重复的工作负载的最佳选择。  **何时选 SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-...
  c24_b03.md: ...板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prompt token 中 30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高...
  c24_b03.md: ...affold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prompt token 中 30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 2000-tok...
  c24_b03.md: ...ken 中 30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 2000-token system prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。...
  c24_b05.md: ...SD，每月 86.4 万美元。加上网络、存储和运维。  每个请求成本：  * Llama 3 70B 上典型 1000-token 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求 GPU 成本 0.004-0.013 US...
  c24_b05.md: ...工作原理：  * 请求到达推理服务器。 * 服务器保持一个 “batch” 活跃请求；GPU 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * B...
  c24_b06.md: ...推理在结构上与典型微服务舰队不同？  提示  GPU 作为单元、两阶段计算、KV cache 作为状态、严格的 per-token 延迟、突发模式。  **答案：** 五个结构差异：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内...
  c24_b06.md: ...吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 token 必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per...
  c24_b06.md: ...st 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 token 必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per-request 预算更宽松；推理有...
  c24_b06.md: ...10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per-request 预算更宽松；推理有 per-token 预算。  (e) **突发工作负载模式**。请求从单次问答到大文档分析不等。集群必须处理混合而无线头阻塞。微服务通常...
  c24_b06.md: ...*答案：** PagedAttention（vLLM，2023）把 KV cache 分成固定大小块（如每块 16 个 token）。每个请求的 KV cache 是块列表，不必在内存中连续。块可独立分配和释放。  它解决的问题：经典 KV cach...
  c24_b07.md: ...：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；...
  c24_b07.md: ...完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒...
  c24_b07.md: ...tch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 to...
  c24_b07.md: ...s = 5120 tokens。  (c) **可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching...
  c24_b08.md: ...分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching（若尚未），然后 SGLang...
  c24_b09.md: ...inuous batching**（若尚未）：短请求可与长请求同 batch。每步并行处理所有请求；短请求生成完自己的 token 即完成，不受长请求影响。这消除大部分队头阻塞。  (b) **优先队列**：短请求和长请求分别走不同队列。短请求 ba...
  c24_b09.md: ...(c) **分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 p...
  c24_b09.md: ...decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请求大小**：把 prompt 限制为 N token（足够小以不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (...
  c24_b11.md: ...的关键差异：GPU 是单元；两阶段计算（prefill + decode）；KV cache 是中心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAttention 把 KV cache 分成固定大小块；消除...
  c25_b01.md: ...de 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU...
  c25_b01.md: ...fill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prom...
  c25_b01.md: ...GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算...
  c25_b01.md: ...。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一...
  c25_b01.md: ...Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV...
  c25_b01.md: ...PU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute p...
  c25_b01.md: ...不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-m...
  c25_b02.md: ...2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2         D1 --...
  c25_b02.md: ...-->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2         D1 -->|"streamed tokens"| LB         D2 -->|"s...
  c25_b02.md: ...内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：...
  c25_b02.md: ...Link 或 InfiniBand 的 RDMA）。  KV cache 传输是新的运维关注点：它消耗网络带宽并增加首 token 时间。处理得当，整体吞吐收益占主导。...
  c25_b03.md: ...arding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；...
  c25_b03.md: ...prompt 前缀被许多请求使用）被摊销；超长上下文无需单节点内存达到最大尺寸。  成本：缓存层网络带宽成为瓶颈；每 token 的 KV cache 传输必须非常快（100+ tokens/sec 需亚毫秒）。RDMA + 精心预取是让这可行的技...
  c25_b04.md: ...cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefil...
  c25_b04.md: ...prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型...
  c25_b04.md: ...6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限...
  c25_b04.md: ...ill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 KV cache（5-1...
  c25_b04.md: ...宽限制）。 * KV cache 传输：典型 KV cache（5-10 GB 经 RDMA）约 50 ms。 * 首 token 延迟：约 100-300 ms（prefill + 传输 + 首 token 生成）。 * 后续每 token 延迟：...
  c25_b04.md: ...经 RDMA）约 50 ms。 * 首 token 延迟：约 100-300 ms（prefill + 传输 + 首 token 生成）。 * 后续每 token 延迟：约 30 ms。  **成本影响**：  * GPU 利用率从 60% 提升到...
  c25_b04.md: ...* 首 token 延迟：约 100-300 ms（prefill + 传输 + 首 token 生成）。 * 后续每 token 延迟：约 30 ms。  **成本影响**：  * GPU 利用率从 60% 提升到约 85%。 * 有效集群容量提升...
  c25_b05.md: ...向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 toke...
  c25_b05.md: ...oken 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅新 token）...
  c25_b05.md: ...n：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅新 token），并采样。这是矩阵-向量乘法。...
  c25_b05.md: ...tion 风格），存储在集群级缓存层；decode 节点按需读取块。  它解决的问题：超长上下文（2026 年 1M+ token）每个请求产生数十 GB KV cache。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GP...
  c25_b05.md: ...作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) **LMCache**（UC Berkeley，2024）：开源等价物。为 vLLM 集成设计。提...
  c25_b05.md: ...它们负责。这简化故障处理（decode 节点故障不丢失 KV cache）。  成本：  (a) **网络带宽**：每 token 的 KV cache 读取必须非常快（>100 tokens/sec 需亚毫秒）。需要 RDMA + 精心预取。  (...
  c25_b06.md: ...一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt...
  c25_b06.md: ...列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输...
  c25_b06.md: ...提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输出）的延迟组件：  (a...
  c25_b06.md: ...de per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输出）的延迟组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）...
  c25_b06.md: ...首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输出）的延迟组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b) **Prefill*...
  c25_b06.md: ...组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b) **Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV ca...
  c25_b06.md: ...ache 传输**：5 GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 token 生成**：decode 节点上约 30 ms（一个 decode 步）。  (e) **首 token 总计**：约...
  c25_b06.md: ...(d) **首 token 生成**：decode 节点上约 30 ms（一个 decode 步）。  (e) **首 token 总计**：约 185 ms（网络 + prefill + 传输 + 首 decode）。  (f) **后续每 tok...
  c25_b06.md: ...ken 总计**：约 185 ms（网络 + prefill + 传输 + 首 decode）。  (f) **后续每 token**：约 30 ms。  (g) **200 输出 token 总计**：约 185 ms（首 token）+ 199...
  c25_b06.md: ...+ 传输 + 首 decode）。  (f) **后续每 token**：约 30 ms。  (g) **200 输出 token 总计**：约 185 ms（首 token）+ 199 × 30 ms（其余）= 约 6.2 秒。  对 co-loc...
  c25_b06.md: ...（首 token）+ 199 × 30 ms（其余）= 约 6.2 秒。  对 co-located（未分离）架构，首 token 延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-token 延迟类似。分离版为首...
  c25_b06.md: ...d（未分离）架构，首 token 延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-token 延迟类似。分离版为首 token 多付约 50 ms（传输）；吞吐收益来自集群整体更好的每 GPU 利用率。...
  c25_b06.md: ...延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-token 延迟类似。分离版为首 token 多付约 50 ms（传输）；吞吐收益来自集群整体更好的每 GPU 利用率。...
  c25_b07.md: ...对首 token 延迟敏感的工作负载（用户看到流式输出的交互式聊天），50 ms 惩罚是真实的。对批处理工作负载（文档分析、agent...
  c25_b07.md: ...50 ms 惩罚是真实的。对批处理工作负载（文档分析、agent 运行），惩罚不可见（用户只看到最终输出）。  减少首 token 延迟的优化：  (a) **KV cache 预取**：prefill 快完成时，开始把早期块传输到 decode 节...
  c25_b07.md: ...t 有已缓存前缀（来自之前请求），跳过该部分 prefill。  Tobias 的部署使用 (a) 和 (c)；典型首 token 延迟约 150 ms，接近 co-located 性能。...
  c25_b08.md: ...+ 个月）**  * 根据观测流量调整 prefill/decode 比例。 * 实现 KV cache 预取以减少首 token 延迟。 * 为常见 prompt 模式添加前缀复用逻辑。 * 监控任何回归；按需调整。  **每阶段验证门**：  *...
  c25_b08.md: ...节省。ROI 约 10 倍。  **Q2.** 某团队推理集群工作负载高度可变：某些时段以长 prompt（3000+ token）短输出为主；另一些时段以短 prompt 长输出为主。分离如何帮助，运维关注点是什么？  提示  分离允许独立扩缩 p...
  c25_b08.md: ...ry-bound，短输出不需要那么多）。 * 短 prompt 长输出时段：多扩 decode（需要更多内存和 per-token 吞吐）并少扩 prefill。  没有分离（co-located）时，GPU 同时做两个阶段；扩缩是一维的。集群对当前...
  c25_b11.md: ...定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-of-Experts）模型：总参数量很大，但每 token 只激活部分；成本低于 dense 大模型。  (f) **运维优化**：  * 根据观测利用率 right-size...
  c25_b11.md: ...；6 个月迁移；利用率从 60% 到 85%；年节省 400-500 万美元。投入巨大；ROI 巨大。  延迟成本：首 token 延迟增加约 50 ms（KV cache 传输）。对交互式工作负载可感知；缓解措施（同 chassis NVLink、...
  c26_b02.md: ...请求，利用缓存前缀。只有后缀被重新计算。  收益：  * prefill 成本大幅节省（缓存前缀 “免费”）。 * 首 token 延迟降低（更少 prefill 工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale...
  c26_b02.md: ...倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另...
  c26_b02.md: ...6.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。  预测问题：给定 prompt，会生成多少输出 token？有些信号很明显：  * 显式长度提示（API 请求中 max_tokens=100）。 * Prompt 结构（“是/...
  c26_b03.md: ...e Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，d...
  c26_b03.md: ...，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify...
  c26_b03.md: ...rify model（大模型，70B+）并行处理所有 K 个候选。这是一个 “verify 步”，一次处理 K 个候选 token。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 ver...
  c26_b03.md: ...model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K...
  c26_b03.md: ...缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 deco...
  c26_b03.md: ...数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步...
  c26_b03.md: ...decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2...
  c26_b03.md: ...K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify...
  c26_b03.md: ...载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 dra...
  c26_b03.md: ...decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节...
  c26_b03.md: ...fication。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟...
  c26_b03.md: ...每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 v...
  c26_b03.md: ...间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。...
  c26_b03.md: ...相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b05.md: ...描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** S...
  c26_b05.md: ...odel 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verif...
  c26_b05.md: ...模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型毫秒级。  (b) Verify model 并行处理 K 个候选（一个...
  c26_b05.md: ...很快：小模型毫秒级。  (b) Verify model 并行处理 K 个候选（一个 verify 步一起处理 K 个 token）。这与正常 decode 步成本大致相同。  (c) Verify model 检查 K 个候选中与其自身预测匹配的前...
  c26_b05.md: ...的前缀。匹配前缀被接受；第一个不匹配被替换为 verify model 的选择。  (d) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 v...
  c26_b05.md: ...) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 d...
  c26_b05.md: ...ive decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token...
  c26_b05.md: ...token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft mode...
  c26_b06.md: ...ecoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100...
  c26_b06.md: ...ated speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 token 延迟推到不可接受阈值的工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式...
  c26_b06.md: ...求排队，调度器也确保客户 2 和 3 得到 GPU 时间。没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户...
  c26_b06.md: ...没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-rob...
  c26_b06.md: ...死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每...
  c26_b06.md: ...(a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N...
  c26_b06.md: ...ecoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100...
  c26_b06.md: ...ated speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 token 延迟推到不可接受阈值的工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式...
  c26_b06.md: ...求排队，调度器也确保客户 2 和 3 得到 GPU 时间。没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户...
  c26_b06.md: ...没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-rob...
  c26_b06.md: ...死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每...
  c26_b06.md: ...(a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N...
  c26_b08.md: ...*Q1.** Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system p...
  c26_b08.md: ...e 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 tok...
  c26_b08.md: ...工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aware 路由（KV cach...
  c26_b08.md: ...ken system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aware 路由（KV cache locality）**：负载均衡器识...
  c26_b08.md: ...化：  (a) **Prefix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system prompt。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收...
  c26_b08.md: ...prompt。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（5...
  c26_b08.md: ...第25章）保存前缀；多个 decode 节点从缓存读取。  (c) **长度预测路由**：中等输出请求（200-500 token）去能支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculati...
  c26_b08.md: ...断：p99/p50 比 15:1 表明严重尾部延迟。可能原因：  (a) **长输出请求阻塞短请求**：单个 5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有...
  c26_b08.md: ...) **长输出请求阻塞短请求**：单个 5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：500...
  c26_b08.md: ...。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggregation（分离 prefill 节点）...
  c26_b09.md: ...speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify...
  c26_b09.md: ...on）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳数增加 0.5-2 ms）。  各何时更好：  * **Co-located**：小集群（< 10 节点）...
  c26_b09.md: ...）。  各何时更好：  * **Co-located**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分...
  c26_b11.md: ...请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布...
  c27_b01.md: ...3. **机器时钟说真话**。跨机器比较时间戳的代码。    4. **朴素分布式锁有效**。没有 fencing token 的 Redlock。假设锁持有者唯一的代码。    5. **部分故障罕见**。只有 happy path 的代码；不...
  c27_b03.md: ...有 Redis key 意味着唯一性。代码用 Redlock 风格锁保护正确性关键操作。  现实：没有 fencing token 的 Redlock 在进程暂停、时钟漂移和 Redis failover 下失败。两个客户端可能都相信自己持有锁；两者...
  c27_b03.md: ...被污染。  修复：用 consensus-backed 锁（etcd、Consul）并在被保护资源上加 fencing token。       def process_unique_action(action_id):         token...
  c27_b03.md: ...token。       def process_unique_action(action_id):         token = etcd_lock.acquire(f"action:{action_id}", ttl=30)...
  c27_b03.md: ...try:             do_critical_work(action_id, fencing_token=token)         finally:             etcd_lock.release(token)...
  c27_b03.md: ...tical_work(action_id, fencing_token):         # 存储层拒绝 stale token 的写         db.atomic_write(action_id, ..., max_token=fencin...
  c27_b05.md: ...或因果排序。  (f) **锁**：每个分布式锁对正确性场景应是 consensus-backed 加 fencing token。  (g) **部分故障处理**：每个多步操作应处理部分失败（saga、outbox、compensating act...
  c27_b09.md: ...时间戳做排序。    4. **朴素分布式锁有效**：Redlock 风格代码假设锁持有者唯一；缺少 fencing token。    5. **部分故障罕见**：代码只有 happy path；不考虑 “半成功” 结果；无 saga、无补偿动作...
  c27_b09.md: ...et(key, "1", nx=True, ex=30): do_critical_work()`。无 fencing token 的 Redlock；并发持有者造成污染。  （谎言 5）`db.update(user); cache.update(...
  c27_b10.md: ...(f) **锁**：分布式锁对正确性关键场景应是 consensus-backed（etcd 等）加 fencing token。  (g) **部分故障处理**：多步操作应处理部分失败（saga、outbox、compensating actio...
  c27_b12.md: ...on ledger 排序依赖机器时钟。    4. **谎言 4（锁）**：基于 Redis 的锁没有 fencing token。在 GC 暂停或分区下，两个协调器可能同时持有锁；两者都能处理同一笔支付。    5. **谎言 5（部分故障）**：...
  c27_b12.md: ...meout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5）；在误导性 CP 注释下读一致性模糊（谎言 6）。请用团队设计文档的...
  c27_b15.md: ...算重试、熔断器、排序原语（HLC 而非 wall clock）、consensus-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace context 传播、可观测性。每份分布式代码 P...
  c28_b02.md: ...us DB（现为 Crescent 组件）。管理分布式事务状态；经 Raft 复制；对安全关键操作使用 fencing token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (...
  c28_b02.md: ...luster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（Co...
  c28_b04.md: ...inator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze 余额（第11/13/14章）。A...
  c28_b04.md: ...on quorum 物理限制）。  (10) 事务的 coordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4 防御）。5 ms。  (11) API 服务向 Ka...
  c28_b04.md: ...oordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4 防御）。5 ms。  (11) API 服务向 Kafka 发布 “payment.completed” 事...
  c28_b05.md: ...| Redis 缓存（最终）、Yjs（CRDT） | n/a | | 18 | 锁 | etcd + fencing token | E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Consul + Cloudflare...
  c28_b07.md: ...律的投资，造就了一个 ‘刚刚好使’ 的系统。”  **Anya（Stratus DB 主管）**：“带 fencing token 的 Raft 复制协调器是系统安全的核心。集成到 Crescent 技术栈很直接，因为 Stratus DB 从一开始...
  c28_b10.md: ...复制协调器 + HLC 版 MVCC | 协调器状态在易失内存；竞争下不确定性重启；下游 API 缺少 fencing token | | 9 | Yuki 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定...
  c28_b10.md: ...成本 | prefill/decode 分离 + KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/decode failover |  每个工作流都可作为独立小手...
  c28_b12.md: ...设这些谎言正在发生，识别系统每个部分易受哪些谎言影响，并组合防御这些谎言的原语（时间用 HLC、身份用 fencing token、故障用重试/超时/熔断器/saga）。  工作工程师的姿态：对每个组件、每个连接、每个操作都假设最坏情况，直到被验证。...
  c28_b12.md: ...的系统；没有内化的读者产出有 bug 的系统。  **Q2.** 指出下列原语分别由哪一章引入：HLC、fencing token、Raft、原子广播、CRDT、重试预算、混沌工程、KV-cache-aware 路由。  提示  各章大致按顺序引入原...
  c28_b12.md: ...引入原语；查看目录。  **答案：**  * **HLC**：第3章（时间、序与说谎的时钟）。 * **Fencing token**：第18章（分布式锁）。 * **Raft**：第9章（实现友好的共识）。 * **Atomic broadcast...
  c28_b12.md: ...路由**：第26章（LLM 推理负载均衡）。  每个原语解决三个谎言某一类表现。HLC 防御时间谎言；fencing token 防御身份谎言；带预算的重试防御故障谎言的放大。完整防御需要多个原语共同使用。  第28章的 Crescent 系统使用...
  c28_b14.md: ...Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revision 在编排层起类似作用。 * **故障**：异步复制可能丢失主库传播前最近写入。团...
  c28_b17.md: ...C 量化这些谎言（第2章）。 * 如何用具体原语防御每个谎言——时间用 HLC（第3章），Raft 和 fencing token 用于身份（第9、18章），带预算的重试和 saga 用于故障（第13、21章）。 * 如何在 30 分钟内评估任何分布...
  c28_b18.md: ...C 分析；跨 region 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing token；追踪用 OpenTelemetry；韧性用重试预算和熔断器；验证用混沌工程；AI 辅助代码审查用六谎言清单。  心智模...
  c29_b02.md: ...#### LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典型工作负载首...
  c29_b02.md: ...LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典型工作负载首 token 延迟 100-300 ms。  2027-2030...
  c29_b02.md: ...延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典型工作负载首 token 延迟 100-300 ms。  2027-2030 轨迹：  * 生产交互式工作负载的 **inter-token l...
  c29_b02.md: ...首 token 延迟 100-300 ms。  2027-2030 轨迹：  * 生产交互式工作负载的 **inter-token latency（ITL）** 低于 10 ms 成为标准。这由投机解码改进（Medusa、EAGLE、Hydra 变体...
  c29_b02.md: ...ll + Hopper 继任者）驱动。 * 通过更激进的 KV cache 预取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 * **MoE（Mixture-of-Experts）**...
  c29_b02.md: ...r，可能需要不同的分布式系统处理。  心智模型（GPU 是单元；两阶段计算；KV cache 是中心状态；严格 per-token 延迟）可推广到 MoE，很可能也到扩散-LM。具体原语（PagedAttention、RadixAttention、d...
  c30_b06.md: ...LM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被保护资源强制执行令牌顺序。 * **FLP impossibility（...
  c30_b06.md: ...l time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **Linearizable（线性化）**：每次操作看起来在调用与响应之间某个瞬间生效，与实时...
  c30_b06.md: ...*Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层...
  c30_b07.md: ...因果/向量时钟；TrueTime | | 4 | 朴素分布式锁有效 | Ch.18 | 共识支持锁 + fencing token | | 5 | 部分故障罕见 | Ch.22 | Saga 模式；outbox；补偿动作 | | 6 | CAP 分类...
  c30_b09.md: ...*：大量手动数据协调，部分提交永久丢失。 * **章节映射防御**：     * Ch.18（分布式锁，fencing token）：failover 自动化缺少 fencing token；新主库不应能在旧主库仍自认为主库时写入。     * Ch...
  c30_b09.md: ...御**：     * Ch.18（分布式锁，fencing token）：failover 自动化缺少 fencing token；新主库不应能在旧主库仍自认为主库时写入。     * Ch.7（共识，防止 split-brain）：基于共识的 le...
  c30_b11.md: ...#### F.5 — 大规模分布式 LLM 推理（月推理预算 10 万美元以上）  **输入**：每日 10 亿+ token；多模型；延迟敏感的交互路径 + 批处理路径；成本优化是强制的。  **决策**：vLLM/SGLang + 分离式 p...
  c30_b16.md: ...缓存当锁用。" | 基于缓存的锁在进程暂停、时钟漂移、缓存驱逐和 failover 下失败。用共识 + fencing token。 | Ch.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的...
  c30_b19.md: ...扣款、重复写入） | 缺少幂等性 key；重试命中无保护路径 | Ch.4（RPC 语义），Ch.18（fencing token），Ch.27 §27.2   跨服务 span 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽...
  c30_b19.md: ...；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-token 检查 | 无 fencing 的朴素分布式锁 | Ch.18 §18.2（Redlock 批评），Ch.27 §27....
  c30_b19.md: ...入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用...
  c30_b19.md: ...| Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6   LLM 推理 trace 显示首 token 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分离延迟）   LLM...

=== llm (273 occurrences) ===
  c01_b03.md: ...Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不同，心智动作相同。...
  c01_b04.md: ...是当前正在撰写事故复盘的失败模式。  本书也是，刻意地，关于过去两年发生变化的一件事：AI 辅助代码生成。分布式代码是 LLM 生成最糟糕的类别，第27章会完整回归这一点。现在先记住这个观察：如果代码库中有哪个部分你不应该在未验证的情况下信任生成...
  c01_b06.md: ...分（第18到第19章）讲协调。第七部分（第20到第23章）讲规模化运行分布式系统。第八部分（第24到第26章）讲分布式 LLM 推理，这是 2026 年的一个子学科。第九部分（第27到第28章）对全书进行综合。第29章是关于 2027-2030...
  c02_b01.md: ...让 LLM 写一个分布式库存服务，它会产出 200 行看起来干净的 Python。代码旁的说明写道：“我们使用强一致性数据库（Co...
  c02_b01.md: ...AP 看是 CP 系统。”代码可信，注释也可信。两者都错了，而“可信”与“正确”之间的差距正是本章的主题。（与本书每次 LLM 冷启动代码引用一样，这里的 bug 是结构性的——一个口号级别的 CAP 论断掩盖了每个操作层面的问题——而非语法性的...
  c02_b13.md: ...绝对正确。对社交信息流产品，同样权衡错误；分区行为应该是“展示我们有的”，而不是“拒绝服务”。  **Q2.** 一个 LLM 被问到“我应该为我的在线商店选 CP 还是 AP 数据库？”，三段话内给出自信推荐。在不指明模型选了哪个的情况下，指出...
  c02_b14.md: ...负载变化时无法调试的价值。  更深观察：分布式系统决策不是那种可以外包推理的决策。推理 _就是_ 决策。一个碰巧正确的 LLM 生成答案不会给团队带来任何理解，无法评估未来变更，也无法在假设变化时为自己辩护。   ### 本章总结  Deutsc...
  c03_b01.md: ...一个 LLM 被要求写一个聊天消息排序函数，产出：       def display_in_order(messages):...
  c03_b08.md: ...（发送时发送者可见的消息 ID）。显示顺序是依赖图的任意拓扑排序。Yjs 和 Automerge 内部就是这样做的。  LLM 原版的 `sorted(messages, key=lambda m: m.timestamp)` 错在把 wall-...
  c03_b14.md: ...（commit-wait 自动增长）。HLC 的失败模式不那么可见，从运维纪律角度看可能更糟。  **Q2.** 一个 LLM 生成的日志系统用 `time.time()` 给每条日志行盖戳，然后合并 100 台服务器的日志并排序。指出该设计的三...
  c04_b01.md: ...一层都严格强于下一层；每一层都更接近复制状态机实际需要。  为了说明即使只向上迈一小步也重要，考虑原语用错会怎样。一个 LLM 被要求实现跨微服务舰队的事件分发，产出：       def broadcast(event):         fo...
  c04_b11.md: ...它们扩展了较弱广播就够用的操作集合。层次本身仍然描述成本结构：需要原子广播的操作仍然要付代价。  **Q2.** 一个 LLM 被要求设计“高可用”事件分发系统。它给出用尽力广播并在发送者侧重试的设计。指出该设计对后续使用它的工程师而言存在的三种...
  c04_b11.md: ...。若事件是独立的提示（缓存失效），丢失可接受，设计没问题。若事件构成序列（必须按序应用的状态变更），设计则严重不足。  LLM 生成的设计没有命名其假设，因此读者必须从代码中反推。这就是 AI 生成分布式代码的反复出现的问题（第27章）：缺少明确...
  c05_b10.md: ...制或批量方法可能 outperform Raft。权衡是持久性 vs 吞吐；两者都是真实成本。  **Q2.** 一个 LLM 被要求实现“高可用”复制，写出一个用异步复制、无 leader 选举、无法定人数的读可去任意副本、写可去任意副本并后台...
  c06_b03.md: ...L 分布式训练** 用 InfiniBand 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RD...
  c06_b06.md: ...uorum），让写保持 quorum 成本，但减少读的感知多区域延迟。  **Q4.** 为什么 RDMA 用于分布式 LLM 推理却很少用于应用层微服务？  提示  RDMA 的延迟收益在亚毫秒级有意义。应用服务延迟预算大得多。  **答案：*...
  c06_b06.md: ...求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在...
  c06_b07.md: ...求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在...
  c06_b11.md: ...或后端处理时间。网络通常比工程师假设的占比小。端到端剖析用户感知延迟通常能在网外找到更大收益。  **Q2.** 一个 LLM 被问“我们应该部署三个区域还是五个？”并回答“五个可用性更好。”指出模型没问的问题，并解释“更多区域 = 更好可用性”...
  c07_b10.md: ...设计者重要；其含义（safety vs liveness；部分同步；故障检测）对每个人都重要。  **Q2.** 一个 LLM 被问“为什么我们不能对所有东西都用两阶段提交？”它给出关于锁争用的回答。指出 LLM 关于协调者故障下 2PC 阻塞性...
  c07_b10.md: ...都重要。  **Q2.** 一个 LLM 被问“为什么我们不能对所有东西都用两阶段提交？”它给出关于锁争用的回答。指出 LLM 关于协调者故障下 2PC 阻塞性质的遗漏。  **讨论：** 2PC 有一个臭名昭著的阻塞失败模式：若协调者在发送 P...
  c07_b10.md: ...er / CockroachDB / Percolator 模式（第13章）。  只回答“锁争用”而不提阻塞失败模式的 LLM 错过了 2PC 不能作为通用共识原语的核心原因。锁争用是另一个（真实但较不根本）问题。...
  c07_b11.md: ...更深观察：许多 LLM 生成分布式协议解释只命名表面症状（锁争用、延迟），而不命名结构性原因（协调者故障阻塞、缺乏法定人数相交）。结构性原因决...
  c08_b10.md: ...标榜“用 Paxos”，Paxos 家族在运维上仍然活跃。协议家族的存在比简单名字匹配更广泛。  **Q2.** 一个 LLM 被要求用 Python 实现 Paxos，产出 200 行代码。在不运行的情况下，指出任何 LLM 生成的 Paxos...
  c08_b10.md: ...*Q2.** 一个 LLM 被要求用 Python 实现 Paxos，产出 200 行代码。在不运行的情况下，指出任何 LLM 生成的 Paxos 实现中可能存在的三类 bug。  **讨论：**  (a) **持久化遗漏。** Acceptor...
  c08_b10.md: ...ballot`、`accepted_value`）必须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并...
  c08_b10.md: ...ot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“...
  c08_b10.md: ...个节点的多数是 $\lceil (N+1)/2 \rceil$。$N=4$ 时多数是 3；$N=5$ 时多数也是 3。LLM 代码常硬编码错误值，与偶数节点一起使用时破坏法定人数相交。  还有三条（任一条都会造成静默 safety 违反）：...
  c08_b10.md: ...规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c08_b11.md: ...(e) **重配置。** 增减节点需要协议层谨慎协调。LLM 生成的代码通常根本不实现重配置，或以允许 split-brain 的天真方式实现。  (f) **网络 framing...
  c08_b11.md: ...*网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/ballot/acceptor。LLM 代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 acceptor 状态。  更深观察：Paxos 是...
  c08_b11.md: ...代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 acceptor 状态。  更深观察：Paxos 是 LLM 生成代码的最坏情况类别。Safety 性质在任何单次测试运行中不可见；它们需要对抗性调度（在特定时刻故障）才能揭示。一...
  c08_b11.md: ...不可见；它们需要对抗性调度（在特定时刻故障）才能揭示。一个只被训练过 Paxos 描述但未被训练过 Paxos 实现的 LLM 会产出看起来正确但静默不安全的代码。这是第27章核心教训：分布式代码需要 LLM 目前做得不好的推理。   ### 本...
  c08_b11.md: ...描述但未被训练过 Paxos 实现的 LLM 会产出看起来正确但静默不安全的代码。这是第27章核心教训：分布式代码需要 LLM 目前做得不好的推理。   ### 本章总结  Paxos 是 Lamport 1989 年提出的经典共识协议。单法令...
  c09_b10.md: ...对 20% 需要完整功能集的与 Paxos 持平。”这足以让 Raft 成为新系统的正确默认。  **Q2.** 一个 LLM 被要求“为分布式计数器实现 Raft”。输出 300 行 Python。指出除了第8章 Paxos 等价 bug 之外...
  c09_b10.md: ...“为分布式计数器实现 Raft”。输出 300 行 Python。指出除了第8章 Paxos 等价 bug 之外，任何 LLM 生成的 Raft 实现中可能存在的三类结构性 bug。  **讨论：** 除了持久化、ballot 唯一性、法定人数...
  c09_b10.md: ...bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 le...
  c09_b10.md: ...在 leader 上 apply 的混淆。** 条目在多数复制后 _提交_；只有在提交之后才 _应用_ 到状态机。许多 LLM 实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous...
  c09_b10.md: ...Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条...
  c09_b10.md: ...ex 回退缺失。** `AppendEntries` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LLM 实现有时不减，导致无限循环重试，看起来像 hang。  (f) **快照与应用竞争。** 快照捕获某时刻状态机状态；并...
  c09_b10.md: ...像 hang。  (f) **快照与应用竞争。** 快照捕获某时刻状态机状态；并发 apply 不能在快照期间修改状态。LLM 实现很少协调，产生不一致快照。...
  c09_b11.md: ...模式与 Paxos 相同：LLM 生成的代码匹配高层描述但错过微妙不变式。Raft 论文的图 8 case 就在论文里；LLM 训练也见过。但把“lea...
  c09_b11.md: ...模式与 Paxos 相同：LLM 生成的代码匹配高层描述但错过微妙不变式。Raft 论文的图 8 case 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous term 条目”翻译成正确...
  c09_b11.md: ...全的模式：使用经过验证的 Raft 库（etcd raft、raft-rs、hashicorp/raft）；不要从头用 LLM 生成共识代码。只在库之上用生成代码处理应用逻辑。   ### 本章总结  Raft 是 2026 年新分布式系统的首选...
  c10_b11.md: ...在云厂商看不见的情况下处理数据。这是同一威胁模型的不同解决方案，可能在云信任场景胜过 BFT。  **Q2.** 一个 LLM 被要求设计“高安全分布式系统”。它产出一个使用 BFT 共识的设计。指出 LLM 可能未考虑的三件事，并解释为什么“使...
  c10_b11.md: ...过 BFT。  **Q2.** 一个 LLM 被要求设计“高安全分布式系统”。它产出一个使用 BFT 共识的设计。指出 LLM 可能未考虑的三件事，并解释为什么“使用 BFT”很少是完整答案。  **讨论：**  (a) **BFT 副本放在哪里...
  c10_b11.md: ...b) **密钥管理。** BFT 依赖加密签名。密钥必须管理：轮换、撤销被破坏密钥、使其处于副本同一信任边界之外。多数 LLM 生成设计会忽略密钥管理，但这常是实际安全弱点。  (c) **侧信道抵抗。** 名义上诚实的副本若可被观测（时序侧信道...
  c10_b12.md: ...察：“高安全”是组织属性，而非协议属性。BFT 只是一个工具；必须与密钥管理、地理分布、侧信道抵抗、组织分离等实践结合。LLM 把“使用 BFT”作为“高安全”答案是部分答案；完整答案需要 LLM 所不具备的专业知识。   ### 本章总结  拜...
  c10_b12.md: ...钥管理、地理分布、侧信道抵抗、组织分离等实践结合。LLM 把“使用 BFT”作为“高安全”答案是部分答案；完整答案需要 LLM 所不具备的专业知识。   ### 本章总结  拜占庭容错处理任意节点不当行为，包括恶意行为、重复签名和损坏状态。代价是...
  c11_b10.md: ...（不锁 GCP、团队已有 Postgres 兼容技能）做出最终决定。两层都必要；单独都不充分。  **Q2.** 一个 LLM 被问“哪个数据库最好？”且没有更多上下文。它给出答案。为什么这个答案几乎肯定错，LLM 应该先问什么？  **讨论：*...
  c11_b10.md: ...充分。  **Q2.** 一个 LLM 被问“哪个数据库最好？”且没有更多上下文。它给出答案。为什么这个答案几乎肯定错，LLM 应该先问什么？  **讨论：** “最好”没有上下文无意义。LLM 被问时不知道：  * 工作负载的一致性需求（线性化...
  c11_b10.md: ...有更多上下文。它给出答案。为什么这个答案几乎肯定错，LLM 应该先问什么？  **讨论：** “最好”没有上下文无意义。LLM 被问时不知道：  * 工作负载的一致性需求（线性化、最终、介于两者之间）。 * 每次操作延迟预算。 * 部署范围（单区...
  c11_b10.md: ...他系统协作）。 * 监管约束（GDPR、数据驻留、审计）。 * 规模（TB 或 PB；每秒行数）。  没有任何这些信息，LLM 只是在按训练数据平均产出观点，这过度代表流行系统（PostgreSQL、MongoDB、DynamoDB），低估特定上...
  c11_b10.md: ...观点，这过度代表流行系统（PostgreSQL、MongoDB、DynamoDB），低估特定上下文下应选的专业系统。  LLM 应该问：“工作负载需要什么？约束是什么？你想优化什么？”任何人类数据库顾问也会问同样问题。  更深观察：“最好数据库”...
  c11_b10.md: ...束是什么？你想优化什么？”任何人类数据库顾问也会问同样问题。  更深观察：“最好数据库”这种问题没有上下文就没答案，而 LLM 若自信回答，正说明它不理解问题。更诚实的 LLM 会拒绝回答直到获得上下文，或产出“如果…那么…”答案，把工作负载特征...
  c11_b10.md: ...同样问题。  更深观察：“最好数据库”这种问题没有上下文就没答案，而 LLM 若自信回答，正说明它不理解问题。更诚实的 LLM 会拒绝回答直到获得上下文，或产出“如果…那么…”答案，把工作负载特征映射到数据库选择。  诊断方法是桥梁：一旦 LLM...
  c11_b10.md: ...LLM 会拒绝回答直到获得上下文，或产出“如果…那么…”答案，把工作负载特征映射到数据库选择。  诊断方法是桥梁：一旦 LLM（或人）知道工作负载，方法就把候选范围缩小到几个。然后领域专长（或进一步问题）在它们之间选择。...
  c12_b10.md: ...CockroachDB），元数据层成为重要运维关注点。哈希分片规避这一点，代价是失去灵活性。  **Q2.** 一个 LLM 被要求“为我们的分布式缓存实现一致哈希”，产出 50 行代码。指出 LLM 生成代码与生产实现相比的三个结构性问题。...
  c12_b10.md: ...代价是失去灵活性。  **Q2.** 一个 LLM 被要求“为我们的分布式缓存实现一致哈希”，产出 50 行代码。指出 LLM 生成代码与生产实现相比的三个结构性问题。  **讨论：**  (a) **Vnode 处理。** 生产一致哈希使用虚拟...
  c12_b10.md: ...。  **讨论：**  (a) **Vnode 处理。** 生产一致哈希使用虚拟节点（每物理节点 256+）做负载分布。LLM 生成代码通常每个节点一个位置，导致负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确...
  c12_b10.md: ...负载不均衡。  (b) **Token 分配。** 生产系统谨慎分配 token，确保同一键的多个副本不落同一物理节点。LLM 代码可能未正确处理多副本场景，让同一键的两个副本落到同一节点。  (c) **成员变更期间的路由。** 生产代码正确处...
  c12_b10.md: ...生产代码正确处理节点增删期间的并发读/写：写发往新 owner，读在迁移完成前同时检查旧 owner 和新 owner。LLM 代码通常把成员变更视为瞬时，在并发流量下错误。  还有三条（任一条都是生产 bug）：  (d) **哈希函数选择。*...
  c12_b10.md: ...都是生产 bug）：  (d) **哈希函数选择。** 生产使用高质量哈希（MurmurHash3、FNV）做负载分布。LLM 代码可能用 `hash()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vn...
  c12_b10.md: ...sh()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-t...
  c12_b10.md: ...o-node 映射持久化（在 etcd、ZooKeeper 或类似中），使分配在节点重启后保留并防止 flapping。LLM 代码通常在启动时计算映射，不同运行可能产生不同映射。...
  c12_b11.md: ...模式：一致哈希概念简单，但有很多运维微妙之处。LLM 生成实现可用于原型；生产环境应使用经过验证的库（Cassandra 的 Dynamo 风格一致哈希、简单场景用 jum...
  c13_b08.md: ...Raft group 是参与者；协调者状态本身被 Raft 复制）。 * **跨服务** ：涉及银行结算 API、风控 LLM、通知服务、审计日志归档的支付是一个 saga。Orchestrator（一个由 CockroachDB 保存状态的小服...
  c13_b16.md: ..._可预测延迟_ 而 favor TrueTime，即使平均成本相似，p99 对 SLA 重要。  **Q2.** 一个 LLM 被要求 “在我们的微服务中实现分布式事务”，产出一个 2PC 实现。指出 LLM 很可能未处理的三种失败模式。  **...
  c13_b16.md: ...LA 重要。  **Q2.** 一个 LLM 被要求 “在我们的微服务中实现分布式事务”，产出一个 2PC 实现。指出 LLM 很可能未处理的三种失败模式。  **讨论：**  (a) **协调者故障**。经典 2PC 若协调者在协议中途崩溃会无...
  c13_b16.md: ...*讨论：**  (a) **协调者故障**。经典 2PC 若协调者在协议中途崩溃会无限阻塞。修复（复制协调者状态）需要 LLM 很可能未实现的共识基础设施（Raft、Paxos）。没有它，系统存在活性单点。  (b) **跨服务异构性**。参与者...
  c13_b16.md: ...MongoDB、一个 Redis）。2PC 要求每个参与者原子、持久地实现 prepared 状态。大多数服务实现没有；LLM 生成代码可能假设统一参与者接口。  (c) **长周期事务**。跨微服务的 2PC 事务可能持续数秒到数分钟（每个服务...
  c13_b17.md: ...(d) **协议消息幂等性**。网络重试可能使 PREPARE 或 COMMIT 到达两次。参与者必须幂等处理。LLM 代码通常不会。  (e) **参与者崩溃后的恢复**。参与者在投 YES 后、收到 COMMIT 前崩溃，恢复时必须记...
  c13_b17.md: ...投 YES 后、收到 COMMIT 前崩溃，恢复时必须记住其投票并一致行动。这需要 prepared 状态的持久化存储。LLM 代码常用内存状态，崩溃后丢失投票。  (f) **超时纪律**。协调者必须对不响应的参与者超时，并决定是否中止整个事务...
  c13_b17.md: ...代码常用内存状态，崩溃后丢失投票。  (f) **超时纪律**。协调者必须对不响应的参与者超时，并决定是否中止整个事务。LLM 代码通常完全没有超时或使用不当值。  更深观察：跨服务 2PC 在 2026 年微服务架构中很少是正确模式。LLM 为...
  c13_b17.md: ...务。LLM 代码通常完全没有超时或使用不当值。  更深观察：跨服务 2PC 在 2026 年微服务架构中很少是正确模式。LLM 为 “跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 sa...
  c13_b17.md: ...“跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 saga；LLM 生成 2PC 反映的是旧教科书里突出的内容，而非当前最佳实践。   ### 本章总结  分布式事务是跨分片、跨区域原子...
  c14_b12.md: ...维 NewSQL。两者都以不同的扩展特性提供 “托管简单性”。选择取决于具体工作负载的扩展轴。  **Q2.** 一个 LLM 被要求为 “现代 Web 应用” 推荐数据库，它推荐了 MongoDB。为什么这个答案在没有更多上下文时是错误的，LL...
  c14_b12.md: ...LM 被要求为 “现代 Web 应用” 推荐数据库，它推荐了 MongoDB。为什么这个答案在没有更多上下文时是错误的，LLM 应该先问什么？  **讨论：** MongoDB 对某些工作负载合理，但对许多其他工作负载不合适。“现代 Web 应用...
  c14_b12.md: ...大规模下 schema-on-read 陷阱、生态比 Postgres 有限）使其对许多其他工作负载错误。  更诚实的 LLM 应该问：  * 应用实际做什么？ * 数据形态是什么（关系、文档、图、时序）？ * 规模多大？ * 团队专长如何？ *...
  c14_b12.md: ...、图、时序）？ * 规模多大？ * 团队专长如何？ * 部署目标是什么？  没有这些，“推荐 MongoDB” 只说明 LLM 在从训练数据中给出流行答案（MongoDB 是 2010 年代流行的 NoSQL，训练数据中占比高），而非基于工作负载...
  c14_b13.md: ...更深观察：数据库推荐是上下文主导答案的领域。LLM 在未问工作负载的情况下推荐特定数据库，相当于医生未经诊断就开处方。正确答案是 “告诉我更多信息”，而非 “用 Mong...
  c15_b10.md: ...最终一致” 与 “分区下数分钟最终一致” 非常不同。使用标签而不指定边界的工程师常对结果意外。  **Q2.** 一个 LLM 被要求实现 “一个全局一致的计数器”。它给出使用强一致数据库的代码。指出三个该答案错误的工作负载，正确答案应是 CRD...
  c15_b10.md: ...任何计数器 _值_ 变化频繁但 _消费者_ 容忍轻微不一致的工作负载，都是 CRDT/最终一致工作负载。默认用强一致的 LLM 给出的是安全但昂贵的答案，而非正确答案。...
  c15_b11.md: ...第二个观察：LLM 的选择暴露了架构盲点。正确答案取决于理解工作负载的流量画像、一致性需求和成本权衡。不询问这些就给出答案的 LLM，是在...
  c15_b11.md: ...观察：LLM 的选择暴露了架构盲点。正确答案取决于理解工作负载的流量画像、一致性需求和成本权衡。不询问这些就给出答案的 LLM，是在生成一个看似合理但未参与设计问题的答案。诚实的 LLM 会回答：“告诉我吞吐、一致性需求和成本约束，我才会在 CR...
  c15_b11.md: ...载的流量画像、一致性需求和成本权衡。不询问这些就给出答案的 LLM，是在生成一个看似合理但未参与设计问题的答案。诚实的 LLM 会回答：“告诉我吞吐、一致性需求和成本约束，我才会在 CRDT、最终一致或强一致中推荐。”   ### 本章总结  最...
  c16_b10.md: ...细设计。随着 local-first 软件更普遍，CRDT 相比 OT 在离线场景更具吸引力。  **Q2.** 一个 LLM 被要求 “实现一个协作文档编辑器”。它产出的代码使用简单 WebSocket 广播 “把文档设为这个字符串”。指出该设...
  c16_b11.md: ...更深观察：LLM 的设计是每个团队都会先尝试的朴素方法。CRDT 在结构上解决以上所有问题；重新发明它们不必要且错误。正确的 LLM 回...
  c16_b11.md: ...察：LLM 的设计是每个团队都会先尝试的朴素方法。CRDT 在结构上解决以上所有问题；重新发明它们不必要且错误。正确的 LLM 回答应该是 “用 Yjs 或 Automerge；不要从零实现协作编辑。” LLM 产出朴素代码说明它没学到这一课，或...
  c16_b11.md: ...；重新发明它们不必要且错误。正确的 LLM 回答应该是 “用 Yjs 或 Automerge；不要从零实现协作编辑。” LLM 产出朴素代码说明它没学到这一课，或它在不考虑需求的情况下生成最简代码。  对协作文辑来说，答案是 “用库”。这是一个...
  c17_b10.md: ...果作为默认更容易采用。2030 年 “默认应该是什么” 的答案可能比 2026 年更偏向因果。  **Q2.** 一个 LLM 被要求 “实现一个强一致的计数器”。它产出使用数据库事务每次递增的代码。指出三个该答案错误的工作负载，正确答案应是基于...
  c17_b10.md: ...2,447。  模式：任何计数器 _值_ 更新速度超过 _消费者_ 能据此行动的速度，都是 CRDT/最终一致工作负载。LLM 的 “用数据库事务” 是安全但昂贵的答案；它能工作但成本不必要。...
  c17_b11.md: ...第二个观察：LLM 对强一致的偏好反映了训练偏差。LLM 训练数据中大多数 “实现 X” 请求可能隐含答案是 “用数据库”，即使更专门的原...
  c17_b11.md: ...第二个观察：LLM 对强一致的偏好反映了训练偏差。LLM 训练数据中大多数 “实现 X” 请求可能隐含答案是 “用数据库”，即使更专门的原语（CRDT、队列、缓存）才是正确的。...
  c17_b11.md: ...训练数据中大多数 “实现 X” 请求可能隐含答案是 “用数据库”，即使更专门的原语（CRDT、队列、缓存）才是正确的。LLM 在安全默认；工程师应质疑该默认是否适合工作负载。  正确的工程反射：当被要求 “实现计数器” 时，先问吞吐。若吞吐高，...
  c18_b01.md: ...一个 LLM 被要求为领导者选举服务实现分布式锁，它产出：       def acquire_lock():         re...
  c18_b10.md: ...S3）原生不支持；应用必须叠加。存储系统选择可能成为 fencing token 采用的瓶颈。  **Q2.** 一个 LLM 被要求 “为我们的微服务实现分布式锁”。它产出一个 Redlock 实现。指出 LLM 可能没考虑的三件事，并解释为什...
  c18_b10.md: ...瓶颈。  **Q2.** 一个 LLM 被要求 “为我们的微服务实现分布式锁”。它产出一个 Redlock 实现。指出 LLM 可能没考虑的三件事，并解释为什么 “Redlock” 很少是完整答案。  **讨论：**  (a) **正确性 vs...
  c18_b10.md: ...件事，并解释为什么 “Redlock” 很少是完整答案。  **讨论：**  (a) **正确性 vs 效率的区别**。LLM 没问锁用途就给出一个方案。若用例是正确性（支付、库存），Redlock 错误。正确答案取决于用例；LLM 没问。  (...
  c18_b10.md: ...率的区别**。LLM 没问锁用途就给出一个方案。若用例是正确性（支付、库存），Redlock 错误。正确答案取决于用例；LLM 没问。  (b) **部署特定的失败模式**。Redlock 的安全假设 “5 个独立 Redis master” 与...
  c18_b10.md: ...特定网络条件。生产中团队可能 5 个 Redis 实例在同一 DC（单一故障域），或 3 个带主从复制（不同故障模型）。LLM 没问部署。  (c) **受保护资源是什么**。若受保护资源自身有锁（数据库行锁），分布式锁冗余。若资源没有原生锁，f...
  c18_b10.md: ...**。若受保护资源自身有锁（数据库行锁），分布式锁冗余。若资源没有原生锁，fencing token 对锁安全至关重要。LLM 没问。  再三种（任一条都让 Redlock 不足）：  (d) **进程暂停行为**。GC 暂停、VM 迁移持有的暂...
  c18_b10.md: ...：  (d) **进程暂停行为**。GC 暂停、VM 迁移持有的暂停进程锁在没有 fencing token 时很危险。LLM 没问暂停预期。  (e) **跨区域部署**。5 个 Redis master 跨区域的 Redlock 会增加锁获取...
  c18_b10.md: ...。  (e) **跨区域部署**。5 个 Redis master 跨区域的 Redlock 会增加锁获取的跨区域延迟。LLM 没问地理。  (f) **Redis 故障时发生什么**。若 Redis master 故障，集群从 5 变 4；多数...
  c18_b10.md: ...dis 故障时发生什么**。若 Redis master 故障，集群从 5 变 4；多数变成 4 中 3；安全分析改变。LLM 没处理运维场景。...
  c18_b11.md: ...更深观察：“实现分布式锁” 这类请求在回答前需要多个澄清问题。没问就产出单一产物（Redlock 代码）的 LLM 是在对训练数据做模式匹配，而非工程。正确回应是：“告诉我：这把锁保护什么？两个客户端同时持有它的失败模式是什么？进程可...
  c18_b11.md: ...久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。   ### 本章总结  分布式缓存（Redis Cluste...
  c18_b11.md: ...cent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。   ### 本章总结  分布式缓存（Redis Cluster 作为典型）用...
  c19_b10.md: ...替代（Cilium）更轻。随着 eBPF 网格成熟，服务网格运维成本下降，成本收益计算会改变。  **Q2.** 一个 LLM 被要求 “为我们的微服务舰队实现服务发现”。它产出使用 Kubernetes Services 的代码。该团队在 EC...
  c19_b10.md: ...它产出使用 Kubernetes Services 的代码。该团队在 EC2 上运行（非 Kubernetes）。指出 LLM 的失败和正确模式。  **讨论：** LLM 的失败：它产出的是它最熟悉部署（Kubernetes）的答案，而没有问实...
  c19_b10.md: ...es 的代码。该团队在 EC2 上运行（非 Kubernetes）。指出 LLM 的失败和正确模式。  **讨论：** LLM 的失败：它产出的是它最熟悉部署（Kubernetes）的答案，而没有问实际部署目标。EC2（或基于 VM）部署无法使用...
  c19_b10.md: ...取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出 Kubernetes Services 的 LLM 是...
  c19_b10.md: ...察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出 Kubernetes Services 的 LLM 是把流行模式套到不适用的上下文。...
  c19_b11.md: ...正确的 LLM 回应：“告诉我你的部署：Kubernetes？VM？混合？云？本地？你们团队目前用什么做编排？我可以根据匹配推荐 Ku...
  c20_b09.md: ...的认知负担；2026 年还处于早期且不可靠，但轨迹表明 2028-2030 年会成为标准工具。  **Q2.** 一个 LLM 被要求 “给我们的系统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tra...
  c20_b09.md: ...统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的失败：产出与特定厂商耦合的代码。团队现在被锁定在 DataDog...
  c20_b09.md: ...cific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的失败：产出与特定厂商耦合的代码。团队现在被锁定在 DataDog；换到另一追踪后端（Honeycomb、Tempo、...
  c20_b09.md: ...er 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练偏差。厂商博客和教程在 LLM 训练数据中过度代表特定产品；LLM 产出它见过的东西，...
  c20_b09.md: ...porter 在启动时配置；应用代码相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练偏差。厂商博客和教程在 LLM 训练数据中过度代表特定产品；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetr...
  c20_b09.md: ...相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练偏差。厂商博客和教程在 LLM 训练数据中过度代表特定产品；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel expo...
  c20_b09.md: ...DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTelemetry 基础之上再提供该后端的具体配置。...
  c20_b10.md: ...g、environment-based config）。Vendor lock-in 是真实且可避免的成本；忽略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布式追踪是请求在微服务系统中路径的取证记录。Trace 是...
  c21_b01.md: ...一个 LLM 被要求处理分布式调用中的 “瞬态错误”，它产出：       def call_with_retry(url, max...
  c21_b09.md: ...）或过于宽松（逐渐退化期间）。自适应熔断器（Netflix 近期工作）自我调参；这是发展方向。  **Q2.** 一个 LLM 被要求 “让我们的服务有韧性”。它对每个下游调用加了 5 次指数退避重试。指出为什么这是错的，正确答案是什么。  **...
  c21_b09.md: ...让我们的服务有韧性”。它对每个下游调用加了 5 次指数退避重试。指出为什么这是错的，正确答案是什么。  **讨论：** LLM 正好做了错的事：以 “韧性” 名义添加了最常见的放大机制（5 次重试策略）。这是经典反模式；LLM 训练于过时的 “失...
  c21_b09.md: ...**讨论：** LLM 正好做了错的事：以 “韧性” 名义添加了最常见的放大机制（5 次重试策略）。这是经典反模式；LLM 训练于过时的 “失败时重试” 指导，没有现代 “但要限制重试” 的上下文。  错在哪：  * 5 次指数退避重试对慢下...
  c21_b10.md: ...（装饰器是概念性的；具体库各异。）关键：组合式防御，每种都限制放大。  更深观察：“韧性” 不是单一属性；而是一组模式。LLM 产出 “重试 5 次” 而没有 surrounding context，是缺失架构。正确的 LLM 回应应是：“韧性需...
  c21_b10.md: ...属性；而是一组模式。LLM 产出 “重试 5 次” 而没有 surrounding context，是缺失架构。正确的 LLM 回应应是：“韧性需要重试预算、熔断器、超时、幂等和 fallback。以下是组合使用每种的模式。”   ### 本章总...
  c22_b01.md: ...，2021 年的形式化框架）是最难检测和恢复的生产故障模式。本章讲两者。  愚弄 Olamide 监控的失败，部分源于 LLM 对健康检查的默认建议：       @app.route('/healthz')     def health():...
  c22_b10.md: ...步减少亚稳态故障。2026 年尚处早期；2030 年可能成为标准。轨迹：更多自动化检测和干预。  **Q2.** 一个 LLM 被要求 “为我们的服务设计健康检查”。它产出一个简单 `/healthz` endpoint 返回 200。指出为什么...
  c22_b11.md: ...正确的 LLM 回应应问：“你的服务角色是什么？它有哪些依赖？健康检查的成本预算如何？有这上下文，我才能设计适合你服务的 livene...
  c22_b11.md: ...良的健康检查会导致脆弱部署（每次重启都失败检查；集群降级）或隐藏故障（真实问题期间检查通过）。没有上下文就产健康检查的 LLM 很可能产生其中一种失败模式；正确回应涉及设计对话。   ### 本章总结  灰色故障是进程通过健康检查但从应用视角看实...
  c23_b12.md: ...益权衡；称他们失职忽视了权衡。诚实框架是：对任务关键系统，混沌工程必要；对其他系统，评估权衡。  **Q2.** 一个 LLM 被要求 “为我们团队设计混沌工程实践”。它输出一个工具列表（Chaos Monkey、Chaos Mesh、Greml...
  c23_b12.md: ...我们团队设计混沌工程实践”。它输出一个工具列表（Chaos Monkey、Chaos Mesh、Gremlin）。指出 LLM 漏了什么，完整答案包含什么。  **讨论：** LLM 只给了 _工具_ 答案，没给 _实践_ 答案。工具必要但不充分...
  c23_b12.md: ...s Monkey、Chaos Mesh、Gremlin）。指出 LLM 漏了什么，完整答案包含什么。  **讨论：** LLM 只给了 _工具_ 答案，没给 _实践_ 答案。工具必要但不充分；组织实践（game day、无责复盘、记录学习、固定节...
  c23_b13.md: ...(g) **沟通与认同**：混沌工程可能遇到阻力（“你会搞坏生产”）；团队需要阐明价值并获得领导支持。  更深观察：LLM 的 “工具列表” 是部分答案。提问者可能需要完整实践，而非工具列表。只给工具答案的 LLM 遗漏了组织维度。  正确...
  c23_b13.md: ...获得领导支持。  更深观察：LLM 的 “工具列表” 是部分答案。提问者可能需要完整实践，而非工具列表。只给工具答案的 LLM 遗漏了组织维度。  正确 LLM 回应：“混沌工程既是技术也是组织。工具包括 [...]。实践包括带无责复盘的 gam...
  c23_b13.md: ...的 “工具列表” 是部分答案。提问者可能需要完整实践，而非工具列表。只给工具答案的 LLM 遗漏了组织维度。  正确 LLM 回应：“混沌工程既是技术也是组织。工具包括 [...]。实践包括带无责复盘的 game day。对起步团队，我推荐 [...
  c24_b01.md: ...hwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户运行 LLM 推理；每月基础设施账单 120 万美元。CFO 想知道钱花在哪；CTO 想知道如何在保证质量的前提下缩减账单。  这些...
  c24_b01.md: ...120 万美元。CFO 想知道钱花在哪；CTO 想知道如何在保证质量的前提下缩减账单。  这些问题有同一个答案：分布式 LLM 推理是 2026 年分布式系统的一个子学科。它有自己的协议（PagedAttention、RadixAttention...
  c24_b01.md: ...VIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。   ### 24.1 为什么分布式 LLM 推理不同  单机上推理大模型（Llama 3 70B、Mistral Large、Claude 3 Opus 级别）需...
  c24_b01.md: ...one 就是数十 GB；每个请求的 KV cache 又增加更多。推理必须跨 GPU 分布。  但分布式推理不是 “以 LLM 为负载的分布式系统”。它有与典型微服务舰队结构不同的属性：  (a) **GPU 是单元，而非节点**。昂贵资源是 G...
  c24_b02.md: ...erkeley 发布；2024-2026 年广泛部署）引入了 _PagedAttention_：把操作系统分页思想用于 LLM KV cache 管理的启发式应用。  经典问题：活跃请求的 KV cache 是连续内存。若请求需要 4 GB KV...
  c24_b04.md: ...nference Microservices）是 NVIDIA 的企业推理部署平台。约 2024 年 GA 发布；企业 LLM 部署的商业参考设计。  特性：  (a) **容器化微服务**：每个模型是一个 NIM 容器（含模型权重、推理引擎、A...
  c24_b04.md: ...部署；由 NVIDIA 工具管理。  (b) **多引擎支持**：NIM 容器可按模型使用 vLLM、TensorRT-LLM（NVIDIA 优化引擎）或其他引擎。面向用户的 API 一致。  (c) **硬件优化**：NIM 容器包含 NVID...
  c24_b06.md: ...#### 概念检测  **Q1.** 是什么让分布式 LLM 推理在结构上与典型微服务舰队不同？  提示  GPU 作为单元、两阶段计算、KV cache 作为状态、严格的 per...
  c24_b06.md: ...等。集群必须处理混合而无线头阻塞。微服务通常 per-request profile 更均匀。  这些差异证明把分布式 LLM 推理视为独立子学科。协议（PagedAttention、RadixAttention）、原语（KV cache、pre...
  c24_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“分布式 LLM 推理只是把 LLM 作为负载的分布式系统。通用分布式系统模式适用；不需要新理论。”  **讨论：** 辩护：分布式系统...
  c24_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“分布式 LLM 推理只是把 LLM 作为负载的分布式系统。通用分布式系统模式适用；不需要新理论。”  **讨论：** 辩护：分布式系统理论（共识、复制、分...
  c24_b10.md: ...saggregated prefill/decode）是新贡献；它们并非明显派生自通用分布式系统理论。  综合：分布式 LLM 推理是面向 LLM 负载的分布式系统 _专门化_。通用模式适用高层次；推理专用优化适用低层次。2026 年推理工程师需...
  c24_b10.md: ...d prefill/decode）是新贡献；它们并非明显派生自通用分布式系统理论。  综合：分布式 LLM 推理是面向 LLM 负载的分布式系统 _专门化_。通用模式适用高层次；推理专用优化适用低层次。2026 年推理工程师需要两者：通用分布式系...
  c24_b10.md: ...系统” 的工程师会错过领域实际进展。把它当作子学科（有自己的会议、库、最佳实践）才是正确框架。  **Q2.** 一个 LLM 被要求 “为我们的 Llama 3 70B 搭建推理集群”。它产出使用基础 Hugging Face TextGene...
  c24_b10.md: ...”。它产出使用基础 Hugging Face TextGenerationInference（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产部署包含什么。  **讨论：** LLM 的基础 TGI 部署是起点，但遗漏许多 2026...
  c24_b10.md: ...ionInference（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产部署包含什么。  **讨论：** LLM 的基础 TGI 部署是起点，但遗漏许多 2026 生产特性：  (a) **PagedAttention 引擎**：v...
  c24_b10.md: ...但 vLLM 对大多数工作负载吞吐更好。  (b) **Continuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张...
  c24_b10.md: ...需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-requ...
  c24_b10.md: ...(d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-request 延迟。LLM 应提供完整可观测性方案。  (e) **自动扩缩**：随负载变化，集群应扩缩（Kubernetes HPA、自定义控制...
  c24_b10.md: ...供完整可观测性方案。  (e) **自动扩缩**：随负载变化，集群应扩缩（Kubernetes HPA、自定义控制器）。LLM 应提供自动扩缩配置。  (f) **多租户隔离**：若集群服务多客户，需要隔离（rate limit、队列优先级、每客...
  c24_b10.md: ...” 答案空间很广。最小答案（基础部署）适合原型；完整答案（生产级）需要理解工作负载、运维要求和成本约束。给出最小答案的 LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM...
  c24_b10.md: ...LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM 最小答案的团队会构建脆弱、不可观测、昂贵的集群。正确路径是逐步成熟：先用最小答案做原型，再随工作负载需求添加生产特性。...
  c24_b11.md: ...” 答案空间很广。最小答案（基础部署）适合原型；完整答案（生产级）需要理解工作负载、运维要求和成本约束。给出最小答案的 LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM...
  c24_b11.md: ...LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM 最小答案的团队会构建脆弱、不可观测、昂贵的集群。正确路径是逐步成熟：先用最小答案做原型，再随工作负载需求添加生产特性。...
  c24_b11.md: ...测、昂贵的集群。正确路径是逐步成熟：先用最小答案做原型，再随工作负载需求添加生产特性。   ### 本章总结  分布式 LLM 推理是 2026 年分布式系统的一个子学科。与典型微服务舰队的关键差异：GPU 是单元；两阶段计算（prefill +...
  c25_b01.md: ...e）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU...
  c25_b10.md: ...等扩展到 MoE 模型。2026 年采用分离的团队应预期每 18 个月随新技术出现而重新架构。  **Q2.** 一个 LLM 被要求 “优化我们推理集群成本”。它建议 “用更小的模型”。指出 LLM 遗漏什么，更完整的答案包含什么。  **讨论...
  c25_b10.md: ...技术出现而重新架构。  **Q2.** 一个 LLM 被要求 “优化我们推理集群成本”。它建议 “用更小的模型”。指出 LLM 遗漏什么，更完整的答案包含什么。  **讨论：** “用更小的模型” 是一种成本削减策略，但会降低能力。LLM 给出了...
  c25_b10.md: ...。指出 LLM 遗漏什么，更完整的答案包含什么。  **讨论：** “用更小的模型” 是一种成本削减策略，但会降低能力。LLM 给出了最便宜的答案，没有考虑质量权衡或架构替代方案。  完整答案包括：  (a) **架构优化**（不降低模型质量）：...
  c25_b10.md: ...吞吐。 * Speculative decoding（第26章）：长生成 2-3 倍吞吐。 * 编译（TensorRT-LLM、ExecuTorch）：在支持硬件上 1.5-2 倍提升。  (c) **硬件优化**：  * 更新 GPU（H200...
  c25_b11.md: ...**：  * 根据观测利用率 right-size 集群。 * 用自动扩缩匹配需求。 * 多租户调度以最大化利用率。  LLM 的 “用更小模型” 答案最多带来 10-20% 成本降低（有时有质量损失）。以上完整答案可实现 50-80% 成本降低...
  c25_b11.md: ...。任何特定团队的最优答案取决于他们已应用哪些优化。  更深观察：2026 年推理成本优化是深度专业领域。给出单句答案的 LLM 遗漏了领域实际深度。正确答案应是：“推理成本优化有很多杠杆。最大杠杆是 [列表]。你的集群在每个上处于什么位置？我们针...
  c26_b01.md: ...LLM 请求路由与 Web 请求路由根本不同。Web 请求耗时 10-50 ms；路由到错误服务器的最坏成本是延迟翻倍。LLM...
  c26_b01.md: ...LLM 请求路由与 Web 请求路由根本不同。Web 请求耗时 10-50 ms；路由到错误服务器的最坏成本是延迟翻倍。LLM 请求耗时 1-30 秒；路由到错误服务器的最坏成本是用户看到 30 秒转圈等待。  本章讲 2024-2026 年出现...
  c26_b01.md: ...0 秒；路由到错误服务器的最坏成本是用户看到 30 秒转圈等待。  本章讲 2024-2026 年出现的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重...
  c26_b01.md: ...he-aware 路由（本章）、speculative decoding（本章）。这些部分组合。   ### 26.1 LLM 请求路由问题  典型设置：N 个节点的推理集群，每个运行多个模型实例。请求到达负载均衡器；负载均衡器决定哪个节点处理每...
  c26_b01.md: ...载均衡器；负载均衡器决定哪个节点处理每个请求。  朴素负载均衡（round-robin、least-loaded）错过 LLM 推理特有的关键机会：  (a) **KV cache locality**：若请求 prompt 的前缀已在某节点缓存...
  c26_b01.md: ...(c) **模型可用性**：许多集群服务多个模型。路由必须考虑哪些节点加载了哪些模型。  (d) **尾部延迟**：LLM 推理的尾部延迟比 Web 请求宽得多（p99 30 秒正常）。差的路由决策对用户可见。  机会：考虑这些属性的路由决策...
  c26_b05.md: ...#### 概念检测  **Q1.** 为什么 LLM 请求路由比 Web 请求路由更重要？  提示  LLM 请求耗时秒级 vs 毫秒级；路由决策后果更大。  **答案：*...
  c26_b05.md: ...#### 概念检测  **Q1.** 为什么 LLM 请求路由比 Web 请求路由更重要？  提示  LLM 请求耗时秒级 vs 毫秒级；路由决策后果更大。  **答案：** LLM 请求通常耗时 1-30 秒（取决于 prom...
  c26_b05.md: ...求路由比 Web 请求路由更重要？  提示  LLM 请求耗时秒级 vs 毫秒级；路由决策后果更大。  **答案：** LLM 请求通常耗时 1-30 秒（取决于 prompt 长度和输出长度）；Web 请求通常 10-50 ms。错误路由决策的...
  c26_b05.md: ...秒（取决于 prompt 长度和输出长度）；Web 请求通常 10-50 ms。错误路由决策的成本相应大得多。  具体 LLM 路由机会：  (a) **KV cache locality**：若某节点已缓存 prompt 前缀，路由到该节点节省...
  c26_b05.md: ...el A 请求路由到只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错误路由决策可导致用户等待 30 秒尾部；Web 等价是 100 ms 延迟。影响数量级不...
  c26_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“2026 年 LLM 推理已足够成熟，进一步优化只是增量。大的收益已过去。”  **讨论：** 辩护：主要架构收益（PagedAttenti...
  c26_b10.md: ...理成本上竞争。竞争推动持续优化。Continuum 的优化冲刺不是可选；它是保持竞争力所必需。  **Q2.** 一个 LLM 被要求 “为我们集群实现 KV-cache-aware 路由”。它产出一个带哈希检查的基本 least-loaded...
  c26_b10.md: ...e-aware 路由”。它产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** LLM 的基本方法：哈希 prompt，检查是否有节点缓存该哈希，若有则路由过去。  遗漏什么：  (a) **前缀匹配，而非...
  c26_b10.md: ...哈希 prompt，检查是否有节点缓存该哈希，若有则路由过去。  遗漏什么：  (a) **前缀匹配，而非精确匹配**。LLM 的哈希检查只匹配完全相同 prompt。真实工作负载的 prompt 共享 _前缀_（system prompt +...
  c26_b11.md: ...-and-route” 是原型级答案；生产需要更多。  更深观察：推理集群优化是 2026 专业领域。给出原型级答案的 LLM 遗漏了领域深度。正确 LLM 回应应问：“你的 prompt 结构是什么？多少租户？有缓存层吗？当前缓存命中率多少？我...
  c26_b11.md: ...级答案；生产需要更多。  更深观察：推理集群优化是 2026 专业领域。给出原型级答案的 LLM 遗漏了领域深度。正确 LLM 回应应问：“你的 prompt 结构是什么？多少租户？有缓存层吗？当前缓存命中率多少？我可以基于这些 specific...
  c26_b11.md: ...些 specifics 设计路由策略。”  对 Continuum 部署，路由逻辑是数千行手写代码，为工作负载精细调优。LLM 生成的朴素实现会低 50% 以上。在正确路由实现上的投资通过效率收益回报。   ### 本章总结  LLM 请求路由与...
  c26_b11.md: ...载精细调优。LLM 生成的朴素实现会低 50% 以上。在正确路由实现上的投资通过效率收益回报。   ### 本章总结  LLM 请求路由与 Web 请求路由有结构差异：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。...
  c26_b11.md: ...请求路由与 Web 请求路由有结构差异：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预测）带来显著效率收益。  KV-cache-aware 路由：负载均...
  c26_b11.md: ...请求成本降低 40%、年节省 500-600 万美元。  第27章开启第九部分（综合），整合 Thread C：为什么 LLM 系统性地不擅长分布式代码。第1章的六个谎言将以实例回归；本章教授审查 AI 生成分布式代码的纪律。  ---...
  c27_b01.md: ...Olamide 正在审查队友提交的 PR。该 PR “AI 辅助”；大部分代码由 LLM 生成。代码实现了一个跨 region 支付协调器。看起来正确。单元测试通过。集成测试通过。  她读了 30 行后拒绝了...
  c27_b01.md: ...。单元测试通过。集成测试通过。  她读了 30 行后拒绝了 PR。  前 30 行包含了 Thread C 的六个谎言。LLM 产出了看起来合理的分布式代码，但在生产中数周内就会重现第1章原始重复扣款失败。代码形状正确；设计错误。  本章整合 T...
  c27_b01.md: ...周内就会重现第1章原始重复扣款失败。代码形状正确；设计错误。  本章整合 Thread C。六个谎言被重述；给出每个的 LLM 生成示例；审查 AI 生成分布式代码的检查清单是本章贡献。  更深主张：分布式代码是 LLM 生成最糟糕的类别。不是因...
  c27_b01.md: ...个谎言被重述；给出每个的 LLM 生成示例；审查 AI 生成分布式代码的检查清单是本章贡献。  更深主张：分布式代码是 LLM 生成最糟糕的类别。不是因为 LLM 不能产出看起来像分布式代码的代码；它们能。问题是 LLM 生成分布式代码默认嵌入了...
  c27_b01.md: ...成示例；审查 AI 生成分布式代码的检查清单是本章贡献。  更深主张：分布式代码是 LLM 生成最糟糕的类别。不是因为 LLM 不能产出看起来像分布式代码的代码；它们能。问题是 LLM 生成分布式代码默认嵌入了第1章的谎言，而谎言对没有内化它们的...
  c27_b01.md: ...更深主张：分布式代码是 LLM 生成最糟糕的类别。不是因为 LLM 不能产出看起来像分布式代码的代码；它们能。问题是 LLM 生成分布式代码默认嵌入了第1章的谎言，而谎言对没有内化它们的人来说是不可见的。   ### 27.1 六个谎言，整合版...
  c27_b01.md: ...ELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM 默认产出嵌入全部六个的代码。本章用实例逐一走过。...
  c27_b02.md: ...### 27.2 谎言 1：RPC 有 TCP 式语义  > 本章每个 LLM 生成代码块都是说明性伪代码。重点是 _缺失的结构防御_，而非语法。  LLM 生成代码：       def subm...
  c27_b02.md: ...PC 有 TCP 式语义  > 本章每个 LLM 生成代码块都是说明性伪代码。重点是 _缺失的结构防御_，而非语法。  LLM 生成代码：       def submit_payment(payment):         result = p...
  c27_b02.md: ..., key=payment.idempotency_key)         # ... 无论发生多少次重试都安全   LLM 产出第一版是因为大部分训练数据显示简单的 “调用并检查结果” 模式。第二版需要识别简单模式遗漏的故障模式。   ###...
  c27_b02.md: ...示简单的 “调用并检查结果” 模式。第二版需要识别简单模式遗漏的故障模式。   ### 27.3 谎言 2：网络可靠  LLM 生成代码：       def get_user_profile(user_id):         return p...
  c27_b02.md: ...return cached_profile_or_default(user_id)   装饰器栈反映纪律。LLM 产出裸调用是因为训练数据充满裸调用；“每个网络调用都需要防御” 的工程智慧在代码生成训练中没有强体现。...
  c27_b03.md: ...### 27.4 谎言 3：机器时钟说真话  LLM 生成代码：       def merge_events(events_from_replicas):...
  c27_b03.md: ...replicas, key=lambda e: (e.hlc.l, e.hlc.c, e.replica_id))   LLM 产出 wall-clock 版本因为它是最简单的排序表达式。HLC 版本需要理解时间的谎言。   ### 27.5 谎...
  c27_b03.md: ...版本因为它是最简单的排序表达式。HLC 版本需要理解时间的谎言。   ### 27.5 谎言 4：朴素分布式锁有效  LLM 生成代码：       def process_unique_action(action_id):         i...
  c27_b03.md: ...db.atomic_write(action_id, ..., max_token=fencing_token)   LLM 产出 Redis 版本是因为 Redlock 风格代码在训练语料中（尤其旧博客）。修复需要理解第18章教训（Klepp...
  c27_b04.md: ...### 27.6 谎言 5：部分故障罕见  LLM 生成代码：       def update_user_across_systems(user):         p...
  c27_b04.md: ...er(user.id, 'updated'))      # 异步 worker 处理 outbox；失败时重试。   LLM 产出朴素版本是因为训练数据显示 “做 A；做 B；做 C” 是常见模式。“若 A 成功但 B 失败会怎样？” 的纪律是...
  c27_b04.md: ...产出朴素版本是因为训练数据显示 “做 A；做 B；做 C” 是常见模式。“若 A 成功但 B 失败会怎样？” 的纪律是 LLM 不会自然进行的工程思考。   ### 27.7 谎言 6：CAP 分类简单  LLM 生成代码：       # 注释...
  c27_b04.md: ...但 B 失败会怎样？” 的纪律是 LLM 不会自然进行的工程思考。   ### 27.7 谎言 6：CAP 分类简单  LLM 生成代码：       # 注释：我们用 CockroachDB，它是 CP，所以是一致的。     def get_...
  c27_b04.md: ...nts WHERE id = %s", account_id)   谎言：“CP” 意味着 “完全一致”。代码注释显示 LLM 在应用口号级 CAP，不理解 per-operation 行为。  现实：CockroachDB 默认读写事务是可串行...
  c27_b04.md: ...unts WHERE id = %s AS OF SYSTEM TIME '-10s'", account_id)   LLM 产出第一版加误导注释是因为口号级 CAP 陈述在训练数据中过度代表。修复需要理解 PACELC（第2章）和具体数据库的...
  c27_b05.md: ...可观测性**：每个操作应可观测（日志、trace、监控）。否则故障在客户投诉前不可见。  检查清单不穷尽，但覆盖最常见 LLM 生成失败模式。对每份 PR 走一遍，可捕获高频 bug。   ### 27.9 为什么 LLM 不擅长这个  结构性原...
  c27_b05.md: ...单不穷尽，但覆盖最常见 LLM 生成失败模式。对每份 PR 走一遍，可捕获高频 bug。   ### 27.9 为什么 LLM 不擅长这个  结构性原因：  (a) **训练数据偏差**。LLM 见过很多简单代码（单机推理）示例。分布式代码代表较...
  c27_b05.md: ...可捕获高频 bug。   ### 27.9 为什么 LLM 不擅长这个  结构性原因：  (a) **训练数据偏差**。LLM 见过很多简单代码（单机推理）示例。分布式代码代表较少；“每个网络调用都需要防御” 的工程智慧 LLM 吸收较少。  (...
  c27_b05.md: ...训练数据偏差**。LLM 见过很多简单代码（单机推理）示例。分布式代码代表较少；“每个网络调用都需要防御” 的工程智慧 LLM 吸收较少。  (b) **模式匹配，而非推理**。LLM 匹配模式：“我见过这种代码；给你。” 对分布式代码，正确模式...
  c27_b05.md: ...。分布式代码代表较少；“每个网络调用都需要防御” 的工程智慧 LLM 吸收较少。  (b) **模式匹配，而非推理**。LLM 匹配模式：“我见过这种代码；给你。” 对分布式代码，正确模式取决于故障模式（LLM 不会枚举）；LLM 选最常见的模式...
  c27_b05.md: ...，而非推理**。LLM 匹配模式：“我见过这种代码；给你。” 对分布式代码，正确模式取决于故障模式（LLM 不会枚举）；LLM 选最常见的模式，通常是朴素版。  (c) **不变量不可见**。分布式系统正确性依赖不变量（quorum inters...
  c27_b05.md: ...统正确性依赖不变量（quorum intersection、因果排序、幂等性）。这些不变量不在代码中表达；在周边设计中。LLM 看到代码；看不到不变量。  (d) **测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试...
  c27_b05.md: ...g 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。LLM 的代码 “运行”；bug 只有仔细生产分析才可见。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需...
  c27_b05.md: ...ug 只有仔细生产分析才可见。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人工用上述清单审查。LLM 是生产力工具；不是分布式系统思考的替代品。...
  c27_b06.md: ...### 27.10 AI 辅助审查 AI 生成代码  §27.9 解释了为什么 LLM 产出 buggy 分布式代码。自然跟进：若 AI 工具生成代码，AI 工具能否也 _审查_ 它？部分可以。这个部分答案...
  c27_b09.md: ...#### 概念检测  **Q1.** 说出 LLM 关于分布式系统的六个谎言。  提示  RPC 语义、网络可靠性、时钟、锁、部分故障、CAP 简单。  **答案：**...
  c27_b09.md: ...解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默认产出嵌入这些谎言的代码，因为训练数据过度代表简单模式（单机假设），而不足代表处理这些故障模式的分布式系统纪律。...
  c27_b09.md: ...训练数据过度代表简单模式（单机假设），而不足代表处理这些故障模式的分布式系统纪律。  **Q2.** 每个谎言给出一个 LLM 生成代码嵌入的例子。  提示  例子在 §27.2-27.7。  **答案：**  （谎言 1）`result = s...
  c27_b09.md: ...后接未指定的读。口号级 CAP，缺少 per-operation 一致性选择。  每个模式都在训练数据中，且常大量出现。LLM 把它们作为默认产出；识别它们是 bug 的工程纪律需要分布式系统训练。  **Q3.** 为什么单元测试不足以捕获 L...
  c27_b09.md: ...M 把它们作为默认产出；识别它们是 bug 的工程纪律需要分布式系统训练。  **Q3.** 为什么单元测试不足以捕获 LLM 生成分布式代码中的分布式系统 bug？  提示  单元测试不演练分布式故障模式（网络分区、并发重试、操作期间领导者切换...
  c27_b10.md: ...第23章的混沌工程和 Jepsen 传统是答案。真实分布式系统验证需要真实分布式系统测试。通过单元测试的 LLM 生成代码并未验证分布式行为；验证来自混沌工程和生产观察。  更深观察：“测试通过” 与 “系统正确” 之间的差距对分布...
  c27_b10.md: ...*可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助 PR）按此清单审查，可捕获高频 LLM 生成 bug。清单不穷尽；覆盖最常见故障模式。  清单是纪律；需要训练。没有内化六个谎言的审查者会漏掉清单能捕获的 b...
  c27_b11.md: ...all-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinato...
  c27_b11.md: ...ck、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use Coc...
  c27_b12.md: ...队已内化纪律；AI 辅助 PR 按清单审查；混沌测试是标准；生产中新分布式代码 bug 罕见。  **Q3.** 一个 LLM 生成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服务一致性。” 批判性审查该声明。  提示...
  c27_b13.md: ...某些补偿失败）有定义行为；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 saga 模式” 就是误导；LLM 产出了看起来像 saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计模式声明容易做出但难以验证...
  c27_b13.md: ...更深观察：代码（或 PR 描述）中的设计模式声明容易做出但难以验证。审查者必须检查 specifics，而非接受标签。LLM 经常产出使用模式 _词汇_ 但无 _语义_ 的代码。词汇在训练数据中；语义需要理解。  对 Crescent：每个 s...
  c27_b14.md: ...1.** 辩护或反驳：“AI 辅助代码生成应被禁止用于分布式代码。风险太高。”  **讨论：** 辩护：分布式代码需要 LLM 不擅长的推理；bug 沉默、昂贵且难检测。禁止 AI 辅助迫使人工工程师仔细推理；工程质量更高。  反驳：AI 辅助即...
  c27_b14.md: ...）严格审查；低风险代码（UI、内部工具）宽松。上述 Crescent 政策是一种表述；它不禁止 AI 辅助，但要求捕获 LLM 失败模式的纪律。  2026 现实：大多数团队在某种程度上使用 AI 辅助。很少有团队完全禁止；许多团队按代码类型设不...
  c27_b14.md: ...g 的团队有明确政策和训练有素的审查者；struggling 的团队采用 AI 辅助却没有考虑审查含义。  第二个考虑：LLM 生成代码质量随时间提升。2024 年 LLM 比 2026 年产出更 naive 的分布式代码；2028 年可能显著更...
  c27_b14.md: ...uggling 的团队采用 AI 辅助却没有考虑审查含义。  第二个考虑：LLM 生成代码质量随时间提升。2024 年 LLM 比 2026 年产出更 naive 的分布式代码；2028 年可能显著更好。政策应随技术演进；定期重新评估合理。  *...
  c27_b15.md: ...个谎言的新事故罕见。投资（审查时间、混沌测试、培训）巨大；事故成本更高。   ### 本章总结  Thread C 的 LLM 关于分布式系统的六个谎言：(1) RPC 有 TCP 式语义；(2) 网络可靠；(3) 机器时钟说真话；(4) 朴素分...
  c27_b15.md: ...网络可靠；(3) 机器时钟说真话；(4) 朴素分布式锁有效；(5) 部分故障罕见；(6) CAP 分类简单。每个都是 LLM 生成分布式代码的默认模式。  LLM 生成分布式代码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故...
  c27_b15.md: ...4) 朴素分布式锁有效；(5) 部分故障罕见；(6) CAP 分类简单。每个都是 LLM 生成分布式代码的默认模式。  LLM 生成分布式代码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故障场景下可见。成本高：重复扣款、丢失写...
  c27_b15.md: ...显式一致性选择、trace context 传播、可观测性。每份分布式代码 PR 按清单审查，捕获高频失败。  为什么 LLM 结构性地不擅长分布式代码：训练数据偏差（简单模式过度代表）；模式匹配而非推理；不变量在代码中不可见；测试抓不到 bug...
  c27_b15.md: ...测试抓不到 bug；bug 沉默。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人类用清单审查。LLM 是生产力工具；不是分布式系统思考的替代品。使用 AI 辅助的团队必须投资审查纪律；没有纪律的团队产出的分布式代码每月都...
  c28_b02.md: ...js 的协作文档编辑器（Yuki 的组件），用于商户入驻合同。  (i) **欺诈检测**：Continuum 风格的 LLM 推理（Tobias 的组件）。每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性**：OpenT...
  c28_b03.md: ...SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]         end         CF --> APIE...
  c28_b04.md: ...因此读是本地；线性化（从 leader 读；使用 HLC，第3章）。8 ms。  (7) 事务通过 Continuum LLM 推理检查欺诈（第24-26章）。欺诈模型在 p50 80 ms 内返回分数（sub-100-ms p99，按 Tobi...
  c28_b04.md: ...ms 跨赤道 + 5 ms 最后一公里。  总计：端到端 p50 约 360 ms。p99 约 580 ms（欺诈检测 LLM 尾部更宽，且跨 region 提交跳每跳付出约 95 ms 的最坏情况 Raft quorum 物理代价）。远低于 C...
  c28_b05.md: ...E.4（CrowdStrike 无 canary）、E.7（Fastly 无 canary） | | 24-26 | LLM 推理 | Continuum 风格欺诈检测集群 | n/a | | 27 | AI 生成代码审查 | PR 审查中的六...
  c28_b10.md: ...me day 而不修复 | | 12 | vLLM 分离集群 | Ch.25 §25.5 | 每月 100 万美元以上 LLM 推理工作负载 | 相同或更好延迟下降低 30-50% 成本 | prefill/decode 分离 + KV cach...
  c28_b12.md: ...章引入三个谎言，经过协议（共识、复制、事务、CRDT），经过运维纪律（追踪、级联故障、混沌工程），到第27章回顾为什么 LLM 不擅长这个，以及第28章的综合。内化纪律的读者能构建 Crescent 这样的系统；没有内化的读者产出有 bug 的系...
  c28_b13.md: ...年工程师职业生涯跨度 30 多年。心智模型始终服务；具体系统会变十几次。本书投资于持久知识。  **Q4.** 本书对 LLM 推理的覆盖（第24-26章）如何融入更广泛的分布式系统叙事？  提示  LLM 推理是 2026 子学科；同样的谎言适...
  c28_b13.md: ...于持久知识。  **Q4.** 本书对 LLM 推理的覆盖（第24-26章）如何融入更广泛的分布式系统叙事？  提示  LLM 推理是 2026 子学科；同样的谎言适用；具体原语是推理专用的。  **答案：** LLM 推理是 2026 年分布式...
  c28_b13.md: ...分布式系统叙事？  提示  LLM 推理是 2026 子学科；同样的谎言适用；具体原语是推理专用的。  **答案：** LLM 推理是 2026 年分布式系统的一个子学科。同样的三个谎言（时间、身份、故障）在推理集群和一般分布式系统中都适用。具体...
  c28_b13.md: ...、同样的审查清单、同样的混沌工程。Tobias 的优化推动就是把本书纪律应用于特定工作负载。  (d) **前瞻性**：LLM 推理是成长领域。2026 年进入该领域的工程师处于前沿；未来 5-10 年将显著发展。本书覆盖让他们为该领域快速演进做...
  c28_b17.md: ...评估任何分布式系统（第11章）。 * 如何在故障下运维分布式系统（第20-23章）。 * 如何处理 2026 年分布式 LLM 推理子学科（第24-26章）。 * 为什么 LLM 不擅长写分布式代码，以及如何审查它们的输出（第27章）。 * 工作...
  c28_b17.md: ...维分布式系统（第20-23章）。 * 如何处理 2026 年分布式 LLM 推理子学科（第24-26章）。 * 为什么 LLM 不擅长写分布式代码，以及如何审查它们的输出（第27章）。 * 工作工程师的心智模型在真实生产系统中是什么样子（第28章...
  c28_b18.md: ...第1章的论点句：_分布式系统在时间、身份、故障上自欺。2026 年工作工程师的纪律是组合会说真话的原语——并认识到 LLM 系统性地在这三者上都撒谎。_  这就是贯穿线。这就是本书。这就是 2026 年工作分布式系统工程师随身携带的东西。...
  c29_b02.md: ...#### LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-tok...
  c29_b02.md: ...成代码默认。 * **新模式处理**仍弱；人类在新模式和架构选择上的角色持续。 * **AI 辅助事后分析**成为标准；LLM 读取 trace+logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 P...
  c30_b03.md: ...Patel, P., et al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong,...
  c30_b03.md: ...: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KV...
  c30_b03.md: ..."Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (https://gi...
  c30_b03.md: ...章）  * "The Bitter Lesson" (Sutton, 2019) 及后续评论。 — 为什么模式匹配主导 LLM 行为。 * Crescent 事后复盘（本书全文）。 — 六个谎言的工作示例。   #### 前瞻（第29章 — 后记...
  c30_b06.md: ...aggregated prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing to...
  c30_b06.md: ...计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **Linearizable（...
  c30_b06.md: ...**：事务读取一致快照；存在写-写冲突（可能写偏）。 * **Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Th...
  c30_b07.md: ...* * *   ### 附录 D — LLM 关于分布式系统的六个谎言  整合参考卡。工作示例和审查清单见第27章。  | # | 谎言 | 反驳章节 | 防御 |...
  c30_b11.md: ...lize 用于物化视图式流 SQL；Beam 用于跨 runner 可移植性。   #### F.5 — 大规模分布式 LLM 推理（月推理预算 10 万美元以上）  **输入**：每日 10 亿+ token；多模型；延迟敏感的交互路径 + 批...
  c30_b16.md: ...故障时通过。用深健康检查走真实路径。 | Ch.22 |  模式：每个误解乍一看合理，有一个适合幻灯片的一词版本，正是 LLM 被要求写分布式代码时复现的版本。内化了这 25 条的审查者能在数秒内捕获高频 LLM 生成 bug。...
  c30_b16.md: ...有一个适合幻灯片的一词版本，正是 LLM 被要求写分布式代码时复现的版本。内化了这 25 条的审查者能在数秒内捕获高频 LLM 生成 bug。...
  c30_b19.md: ...；缺少 checkpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6   LLM 推理 trace 显示首 token 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.2...
  c30_b19.md: ...ken 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分离延迟）   LLM 推理 trace 显示长输出请求阻塞短请求 | 队头阻塞；无长度预测路由 | Ch.26 §26.3（长度预测路由）...

=== trace (266 occurrences) ===
  c01_b01.md: ...手机显示一张收据。她的银行账单却显示两笔扣款。  Olamide 在 3:04 被告警叫醒。到 3:30，她已经在查看 trace。两个协调者（coordinator）节点分别完成了支付：一个位于 Crescent 的 EU-West 区域（法兰克福...
  c01_b05.md: ...：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Series IV Vol 3（可观...
  c01_b11.md: ...#### 应用问题  **Q1.** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔 47 ms 到达支付服务的两个不同副本...
  c01_b11.md: ...#### 应用问题  **Q1.** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔 47 ms 到达支付服务的两个不同副本。两者都成功...
  c03_b14.md: ...序列号打破。  这就是现代可观测性栈（OpenTelemetry，配合正确的追踪上下文传播）实际做的事情。跨服务传播的 trace context 携带 HLC 等价信息的机制，trace ID + span ID + 序列号提供本地顺序的平手打破。...
  c03_b14.md: ...try，配合正确的追踪上下文传播）实际做的事情。跨服务传播的 trace context 携带 HLC 等价信息的机制，trace ID + span ID + 序列号提供本地顺序的平手打破。...
  c03_b15.md: ...二阶收益：如果请求已经在传播 trace context（用于分布式追踪，第20章），它们已经携带了足以跨服务正确排序日志事件的信息。用 wall clock...
  c19_b11.md: ...的领域；跨领域交互明确。  第20章开始第七部分：运营分布式系统。分布式追踪与调试：因果保持、分区下采样、跨共识边界的 trace context。本章将该主题与可观测性工具（Series IV Vol 3）范围隔离，聚焦追踪支持的分布式系统推理。...
  c20_b01.md: ...中的某处出了问题。找出原因就是分布式追踪问题。  本章讨论追踪的分布式系统特定方面：因果保持、分区下采样、跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vo...
  c20_b01.md: ...可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什么需要分布式追踪  单体应用中，stack trace 告诉你发生了什么：哪个函数调用了哪个，错误在哪里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布...
  c20_b01.md: ...诉你发生了什么：哪个函数调用了哪个，错误在哪里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有...
  c20_b01.md: ...ans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解...
  c20_b01.md: ...是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解）：       graph TD         R["...
  c20_b01.md: ...主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Pa...
  c20_b01.md: ...nsaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 nul...
  c20_b01.md: ...ts（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布式系统特有的关注点  分布式追踪与本地...
  c20_b01.md: ...型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布式系统特有的关注点  分布式追踪与本地性能分析有何不同：...
  c20_b01.md: ...中的某处出了问题。找出原因就是分布式追踪问题。  本章讨论追踪的分布式系统特定方面：因果保持、分区下采样、跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vo...
  c20_b01.md: ...可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什么需要分布式追踪  单体应用中，stack trace 告诉你发生了什么：哪个函数调用了哪个，错误在哪里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布...
  c20_b01.md: ...诉你发生了什么：哪个函数调用了哪个，错误在哪里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有...
  c20_b01.md: ...ans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解...
  c20_b01.md: ...是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解）：       graph TD         R["...
  c20_b01.md: ...主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Pa...
  c20_b01.md: ...nsaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 nul...
  c20_b01.md: ...ts（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布式系统特有的关注点  分布式追踪与本地...
  c20_b01.md: ...型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布式系统特有的关注点  分布式追踪与本地性能分析有何不同：...
  c20_b02.md: ...#### 关注点 1：因果保持  Trace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 spa...
  c20_b02.md: ...aceparent`、`tracestate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**...
  c20_b02.md: ...ns 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Ka...
  c20_b02.md: ...**异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message...
  c20_b02.md: ...交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message attributes）。  * **协议转换时 c...
  c20_b02.md: ...tributes）。  * **协议转换时 context 丢失**：服务从 HTTP 转换到自定义二进制协议可能丢失 trace context。修复：在协议边界显式传播逻辑。  * **重试时 context 损坏**：同一操作重试应产生独立 s...
  c20_b02.md: ...#### 关注点 2：分区下采样  Traces 通常被采样（只有 1% 或更少的请求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。...
  c20_b02.md: ...ce 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  T...
  c20_b02.md: ...e 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；...
  c20_b02.md: ...trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共...
  c20_b02.md: ...，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、...
  c20_b02.md: ...许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则...
  c20_b02.md: ...。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace co...
  c20_b02.md: ...）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 contex...
  c20_b02.md: ...应用的 Raft 库支持 context 传播；并非所有都支持。  对共识日志是不可变历史（Kafka、区块链）的系统，trace context 是事件数据的一部分。后续处理器使用嵌入的 context。  更深观察：分布式追踪是事件所发生事情的取...
  c20_b02.md: ...context。  更深观察：分布式追踪是事件所发生事情的取证记录。对历史记录重要的系统（审计、合规、调试数月前问题），trace context 是系统永久状态的一部分，不只是可观测性 artifact。...
  c20_b02.md: ...#### 关注点 1：因果保持  Trace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 spa...
  c20_b02.md: ...aceparent`、`tracestate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**...
  c20_b02.md: ...ns 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Ka...
  c20_b02.md: ...**异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message...
  c20_b02.md: ...交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message attributes）。  * **协议转换时 c...
  c20_b02.md: ...tributes）。  * **协议转换时 context 丢失**：服务从 HTTP 转换到自定义二进制协议可能丢失 trace context。修复：在协议边界显式传播逻辑。  * **重试时 context 损坏**：同一操作重试应产生独立 s...
  c20_b02.md: ...#### 关注点 2：分区下采样  Traces 通常被采样（只有 1% 或更少的请求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。...
  c20_b02.md: ...ce 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  T...
  c20_b02.md: ...e 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；...
  c20_b02.md: ...trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共...
  c20_b02.md: ...，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、...
  c20_b02.md: ...许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则...
  c20_b02.md: ...。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace co...
  c20_b02.md: ...）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 contex...
  c20_b02.md: ...应用的 Raft 库支持 context 传播；并非所有都支持。  对共识日志是不可变历史（Kafka、区块链）的系统，trace context 是事件数据的一部分。后续处理器使用嵌入的 context。  更深观察：分布式追踪是事件所发生事情的取...
  c20_b02.md: ...context。  更深观察：分布式追踪是事件所发生事情的取证记录。对历史记录重要的系统（审计、合规、调试数月前问题），trace context 是系统永久状态的一部分，不只是可观测性 artifact。...
  c20_b03.md: ...### 20.3 Trace 能告诉你什么  对一个 14 服务分布式请求，trace 显示：  * **关键路径**：最长串行依赖链；它决定请求总...
  c20_b03.md: ...### 20.3 Trace 能告诉你什么  对一个 14 服务分布式请求，trace 显示：  * **关键路径**：最长串行依赖链；它决定请求总延迟。 * **并行分支**：并行调用的服务；整体延迟由最...
  c20_b03.md: ...很快的服务突然变慢；往往是问题征兆。 * **意外依赖**：被调用但工程师未预料到的服务；通常是架构漂移征兆。  阅读 trace 是一项技能。模式：  (a) **先看最长 span**：它通常主导延迟。 (b) **看错误**：追踪第一个错误；后...
  c20_b03.md: ...缺失” 的 spans**：若应该被调用的服务没出现，可能是上游没调用它（bug 或路由问题）。   ### 20.4 Trace 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wa...
  c20_b03.md: ...询慢）。为此需要服务内性能分析。  (b) **资源竞争**：若变慢由接收服务的 CPU 竞争或磁盘 I/O 排队导致，trace 显示症状（span 慢）但不显示根因。为此需要基础设施指标。  (c) **配置漂移**：若服务因配置变更而变慢，tr...
  c20_b03.md: ...ce 显示症状（span 慢）但不显示根因。为此需要基础设施指标。  (c) **配置漂移**：若服务因配置变更而变慢，trace 不会显示 “配置与预期不同”。为此需要配置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，t...
  c20_b03.md: ...e 不会显示 “配置与预期不同”。为此需要配置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics...
  c20_b03.md: ...置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、D...
  c20_b03.md: ...* 可导出到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 olde...
  c20_b03.md: ...### 20.3 Trace 能告诉你什么  对一个 14 服务分布式请求，trace 显示：  * **关键路径**：最长串行依赖链；它决定请求总...
  c20_b03.md: ...### 20.3 Trace 能告诉你什么  对一个 14 服务分布式请求，trace 显示：  * **关键路径**：最长串行依赖链；它决定请求总延迟。 * **并行分支**：并行调用的服务；整体延迟由最...
  c20_b03.md: ...很快的服务突然变慢；往往是问题征兆。 * **意外依赖**：被调用但工程师未预料到的服务；通常是架构漂移征兆。  阅读 trace 是一项技能。模式：  (a) **先看最长 span**：它通常主导延迟。 (b) **看错误**：追踪第一个错误；后...
  c20_b03.md: ...缺失” 的 spans**：若应该被调用的服务没出现，可能是上游没调用它（bug 或路由问题）。   ### 20.4 Trace 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wa...
  c20_b03.md: ...询慢）。为此需要服务内性能分析。  (b) **资源竞争**：若变慢由接收服务的 CPU 竞争或磁盘 I/O 排队导致，trace 显示症状（span 慢）但不显示根因。为此需要基础设施指标。  (c) **配置漂移**：若服务因配置变更而变慢，tr...
  c20_b03.md: ...ce 显示症状（span 慢）但不显示根因。为此需要基础设施指标。  (c) **配置漂移**：若服务因配置变更而变慢，trace 不会显示 “配置与预期不同”。为此需要配置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，t...
  c20_b03.md: ...e 不会显示 “配置与预期不同”。为此需要配置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics...
  c20_b03.md: ...置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、D...
  c20_b03.md: ...* 可导出到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 olde...
  c20_b04.md: ...### 20.6 取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每...
  c20_b04.md: ...### 20.6 取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trac...
  c20_b04.md: ...取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b...
  c20_b04.md: ...race 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace*...
  c20_b04.md: ...操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求...
  c20_b04.md: ...trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持...
  c20_b04.md: ...付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取...
  c20_b04.md: ...(b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：...
  c20_b04.md: ...攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；...
  c20_b04.md: ...主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检...
  c20_b04.md: ...**客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检索。  对 Cres...
  c20_b04.md: ...定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检索。  对 Crescent：每笔支付 trace 保存 7 年（监管要求）。Trace 存储成本显著，但被视为合规成本。   ### 20.7 练习...
  c20_b04.md: ...而非几天）；按客户或交易 ID 索引以便检索。  对 Crescent：每笔支付 trace 保存 7 年（监管要求）。Trace 存储成本显著，但被视为合规成本。   ### 20.7 练习...
  c20_b04.md: ...### 20.6 取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每...
  c20_b04.md: ...### 20.6 取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trac...
  c20_b04.md: ...取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b...
  c20_b04.md: ...race 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace*...
  c20_b04.md: ...操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求...
  c20_b04.md: ...trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持...
  c20_b04.md: ...付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取...
  c20_b04.md: ...(b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：...
  c20_b04.md: ...攻击者请求流的 trace 是主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；...
  c20_b04.md: ...主要证据。  (c) **客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检...
  c20_b04.md: ...**客户投诉 trace**：客户投诉时，支持团队需要其特定请求的 trace 来诊断。  取证 trace 与采样 trace 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检索。  对 Cres...
  c20_b04.md: ...定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检索。  对 Crescent：每笔支付 trace 保存 7 年（监管要求）。Trace 存储成本显著，但被视为合规成本。   ### 20.7 练习...
  c20_b04.md: ...而非几天）；按客户或交易 ID 索引以便检索。  对 Crescent：每笔支付 trace 保存 7 年（监管要求）。Trace 存储成本显著，但被视为合规成本。   ### 20.7 练习...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、sp...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态...
  c20_b05.md: ...么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 t...
  c20_b05.md: ...e ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一...
  c20_b05.md: ...链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯...
  c20_b05.md: ...的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent...
  c20_b05.md: ...工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节...
  c20_b05.md: ...查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.*...
  c20_b05.md: ...vents**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace...
  c20_b05.md: ...trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答...
  c20_b05.md: ...为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是...
  c20_b05.md: ...ext 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。...
  c20_b05.md: ...pan ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 *...
  c20_b05.md: ...是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 s...
  c20_b05.md: ...传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相...
  c20_b05.md: ...道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。...
  c20_b05.md: ...失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而是许多断开的片段。调试变得不可能，因为你无...
  c20_b05.md: ...而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而是许多断开的片段。调试变得不可能，因为你无法跟随请求路径。  传播通常通过 HTTP headers（W3C 标准的...
  c20_b05.md: ...s）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 trace context。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化...
  c20_b05.md: ...ontext。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？...
  c20_b05.md: ...d 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点（根 spa...
  c20_b05.md: ...缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace...
  c20_b05.md: ..._Head-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b05.md: ...trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、sp...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态...
  c20_b05.md: ...么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 t...
  c20_b05.md: ...e ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一...
  c20_b05.md: ...链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯...
  c20_b05.md: ...的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent...
  c20_b05.md: ...工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节...
  c20_b05.md: ...查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.*...
  c20_b05.md: ...vents**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace...
  c20_b05.md: ...trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答...
  c20_b05.md: ...为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是...
  c20_b05.md: ...ext 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。...
  c20_b05.md: ...pan ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 *...
  c20_b05.md: ...是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 s...
  c20_b05.md: ...传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相...
  c20_b05.md: ...道请求属于哪个 trace。它创建新的 trace ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。...
  c20_b05.md: ...失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而是许多断开的片段。调试变得不可能，因为你无...
  c20_b05.md: ...而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而是许多断开的片段。调试变得不可能，因为你无法跟随请求路径。  传播通常通过 HTTP headers（W3C 标准的...
  c20_b05.md: ...s）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 trace context。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化...
  c20_b05.md: ...ontext。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？...
  c20_b05.md: ...d 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点（根 spa...
  c20_b05.md: ...缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace...
  c20_b05.md: ..._Head-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b05.md: ...trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b06.md: ..._Tail-based sampling_ ：spans 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 trace 采样 1%”...
  c20_b06.md: ..._Tail-based sampling_ ：spans 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 trace 采样 1%”）。只有被选中的 trace 被导...
  c20_b06.md: ...ng_ ：spans 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 trace 采样 1%”）。只有被选中的 trace 被导出到存储。  优点：可保留所有错误 trace；可保留...
  c20_b06.md: ...s 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 trace 采样 1%”）。只有被选中的 trace 被导出到存储。  优点：可保留所有错误 trace；可保留所有慢 trace...
  c20_b06.md: ...基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 trace 采样 1%”）。只有被选中的 trace 被导出到存储。  优点：可保留所有错误 trace；可保留所有慢 trace；可基于结果选择。缺点：需要缓冲基础设施（...
  c20_b06.md: ...错误的 trace；成功 trace 采样 1%”）。只有被选中的 trace 被导出到存储。  优点：可保留所有错误 trace；可保留所有慢 trace；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询...
  c20_b06.md: ...trace 采样 1%”）。只有被选中的 trace 被导出到存储。  优点：可保留所有错误 trace；可保留所有慢 trace；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-ba...
  c20_b06.md: ...。  优点：可保留所有错误 trace；可保留所有慢 trace；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部...
  c20_b06.md: ...sed 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 trace context 必须跨越共识边界持久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧...
  c20_b06.md: ...久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧因果上是下游。没有 context，trace 断裂。  **答案：** 流过共识协议（CockroachDB 事务、Kafka publish-with-confi...
  c20_b06.md: ...发生：当共识日志条目被应用到状态机时，下游效果产生（写磁盘、发送通知等）。  这些下游效果因果上是原始请求的一部分。若 trace context 没有通过共识日志传播，apply 侧的工作看起来是独立的 trace，与发起请求断开。  对调试：若请...
  c20_b06.md: ...效果因果上是原始请求的一部分。若 trace context 没有通过共识日志传播，apply 侧的工作看起来是独立的 trace，与发起请求断开。  对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条目），没有链接 apply 与原始请...
  c20_b06.md: ...起请求断开。  对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条目），没有链接 apply 与原始请求的 trace context，无法诊断。  对取证：支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链...
  c20_b06.md: ...原始请求的 trace context，无法诊断。  对取证：支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元数据中。apply 代码运行时...
  c20_b06.md: ...对取证：支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元数据中。apply 代码运行时提取 context，并为它创建的任何 spans...
  c20_b06.md: ...spans 使用。现代 Raft 库（etcd 的 Raft、raft-rs）直接支持；旧库可能不支持。  更深观察：trace 价值随分布式系统复杂度增加而增加。对简单两服务架构，trace 有用但非必需。对内含共识的 50 服务 mesh，tr...
  c20_b06.md: ...raft-rs）直接支持；旧库可能不支持。  更深观察：trace 价值随分布式系统复杂度增加而增加。对简单两服务架构，trace 有用但非必需。对内含共识的 50 服务 mesh，trace 是主要调试工具；缺失 context 传播是严重的运维缺...
  c20_b06.md: ...ce 价值随分布式系统复杂度增加而增加。对简单两服务架构，trace 有用但非必需。对内含共识的 50 服务 mesh，trace 是主要调试工具；缺失 context 传播是严重的运维缺口。...
  c20_b07.md: ...#### 应用问题  **Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ s...
  c20_b07.md: ...C 的 40 ms）。若 B 和 C 能合并成一个服务，节省的是 hop 开销，通常 10-20 ms。  更深观察：trace 是优化的输入。没有 trace，团队只能猜测哪里慢；有了 trace，他们知道。  **Q2.** 某团队 trace...
  c20_b07.md: ...和 C 能合并成一个服务，节省的是 hop 开销，通常 10-20 ms。  更深观察：trace 是优化的输入。没有 trace，团队只能猜测哪里慢；有了 trace，他们知道。  **Q2.** 某团队 trace 显示 1% 请求有以错误结束的...
  c20_b07.md: ...op 开销，通常 10-20 ms。  更深观察：trace 是优化的输入。没有 trace，团队只能猜测哪里慢；有了 trace，他们知道。  **Q2.** 某团队 trace 显示 1% 请求有以错误结束的 span。调查发现 80% 这些错误...
  c20_b07.md: ...trace 是优化的输入。没有 trace，团队只能猜测哪里慢；有了 trace，他们知道。  **Q2.** 某团队 trace 显示 1% 请求有以错误结束的 span。调查发现 80% 这些错误来自一个特定下游服务。走一遍从 trace 到修复...
  c20_b07.md: ...某团队 trace 显示 1% 请求有以错误结束的 span。调查发现 80% 这些错误来自一个特定下游服务。走一遍从 trace 到修复的诊断流程。  提示  按服务分组错误；看错误 attributes（状态码、错误消息）；找模式（特定时间窗口、...
  c20_b07.md: ...tes**：失败请求是否针对特定租户、特定 endpoint、特定输入模式？找输入相关性。  (e) **钻取一条失败 trace**：选一条失败 trace，看错误前的 spans。X 当时在做什么？它调用了什么？它调用的是否先失败？...
  c20_b07.md: ...特定租户、特定 endpoint、特定输入模式？找输入相关性。  (e) **钻取一条失败 trace**：选一条失败 trace，看错误前的 spans。X 当时在做什么？它调用了什么？它调用的是否先失败？...
  c20_b08.md: ...(f) **检查失败请求的日志**：trace 展示时间线；日志展示服务当时的想法。按 trace ID 关联。  (g) **检查 service X 的指标**：...
  c20_b08.md: ...(f) **检查失败请求的日志**：trace 展示时间线；日志展示服务当时的想法。按 trace ID 关联。  (g) **检查 service X 的指标**：失败时的错误率、延迟、资源使用。X 是否 CPU 受...
  c20_b08.md: ...否慢？  (h) **在测试环境复现**：用已知的坏输入模式复现失败。迭代修复。  诊断流程的技巧是知道每一步该看什么。Trace 是起点；诊断流程结合 traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0....
  c20_b08.md: ...aced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Tra...
  c20_b08.md: ....** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。...
  c20_b08.md: ...t OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Ka...
  c20_b08.md: ...ace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace c...
  c20_b08.md: ...Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把...
  c20_b08.md: ...ka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace...
  c20_b08.md: ...trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.propagate import inje...
  c20_b08.md: ...c, value=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为 `traceparent` 和（可选）`tracestate` headers 插入。消费端：...
  c20_b08.md: ...s 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客户端自动仪器库（`openteleme...
  c20_b08.md: ...way → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分布式追踪的全部意义。...
  c20_b08.md: ...(f) **检查失败请求的日志**：trace 展示时间线；日志展示服务当时的想法。按 trace ID 关联。  (g) **检查 service X 的指标**：...
  c20_b08.md: ...(f) **检查失败请求的日志**：trace 展示时间线；日志展示服务当时的想法。按 trace ID 关联。  (g) **检查 service X 的指标**：失败时的错误率、延迟、资源使用。X 是否 CPU 受...
  c20_b08.md: ...否慢？  (h) **在测试环境复现**：用已知的坏输入模式复现失败。迭代修复。  诊断流程的技巧是知道每一步该看什么。Trace 是起点；诊断流程结合 traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0....
  c20_b08.md: ...aced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Tra...
  c20_b08.md: ....** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。...
  c20_b08.md: ...t OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Ka...
  c20_b08.md: ...ace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace c...
  c20_b08.md: ...Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把...
  c20_b08.md: ...ka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace...
  c20_b08.md: ...trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.propagate import inje...
  c20_b08.md: ...c, value=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为 `traceparent` 和（可选）`tracestate` headers 插入。消费端：...
  c20_b08.md: ...s 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客户端自动仪器库（`openteleme...
  c20_b08.md: ...way → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分布式追踪的全部意义。...
  c20_b09.md: ...g、Tempo）和良好的工程纪律，分布式追踪提供调试大多数生产问题所需的可见性。挣扎的团队要么没投资追踪，要么没学会读 trace。  反驳：trace 覆盖工作的 _因果链_，但不覆盖 _服务内部行为_（CPU 竞争、锁竞争、GC、内存压力）。许多...
  c20_b09.md: ...的工程纪律，分布式追踪提供调试大多数生产问题所需的可见性。挣扎的团队要么没投资追踪，要么没学会读 trace。  反驳：trace 覆盖工作的 _因果链_，但不覆盖 _服务内部行为_（CPU 竞争、锁竞争、GC、内存压力）。许多生产问题根因在 tra...
  c20_b09.md: ...ace 覆盖工作的 _因果链_，但不覆盖 _服务内部行为_（CPU 竞争、锁竞争、GC、内存压力）。许多生产问题根因在 trace 之外。Tracing 是一种工具；完整调试工具集包括性能分析、日志分析、指标关联和复现。  综合：tracing 极大...
  c20_b09.md: ...个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关联 traces、logs、metrics。它们有望减轻读 trace 的认知负担；2026 年还处于早期且不可靠，但轨迹表明 2028-2030 年会成为标准工具。  **Q2.** 一个...
  c20_b09.md: ...elemetry 的 vendor-neutral 写法：     from opentelemetry import trace     tracer = trace.get_tracer(__name__)      with tracer.st...
  c20_b09.md: ...eutral 写法：     from opentelemetry import trace     tracer = trace.get_tracer(__name__)      with tracer.start_as_current_span...
  c20_b10.md: ...略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。...
  c20_b10.md: ...分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（跨服务边界传播 context）、分区...
  c20_b10.md: ...果保持（跨服务边界传播 context）、分区下采样（head-based vs tail-based）、跨共识边界的 trace context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有...
  c20_b10.md: ...什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客...
  c20_b10.md: ...；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客户或交易 ID 索引。它们是系统永久记录...
  c20_b10.md: ...r-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客户或交易 ID 索引。它们是系统永久记录的一部分，不只是可观测...
  c20_b10.md: ...略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。...
  c20_b10.md: ...分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（跨服务边界传播 context）、分区...
  c20_b10.md: ...果保持（跨服务边界传播 context）、分区下采样（head-based vs tail-based）、跨共识边界的 trace context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有...
  c20_b10.md: ...什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客...
  c20_b10.md: ...；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客户或交易 ID 索引。它们是系统永久记录...
  c20_b10.md: ...r-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是特殊的：对特定操作类型 100% 采样、保存数年、按客户或交易 ID 索引。它们是系统永久记录的一部分，不只是可观测...
  c22_b04.md: ...慢。触发器是一次增加 5% 内存使用的功能 rollout；系统通过了健康检查，但每个请求都被延迟尖刺。  恢复：通过 trace 识别 rollout；回滚；集群在数小时内恢复。   #### Roblox 2021 多天中断  灰色故障与亚稳态级...
  c22_b07.md: ...线程阻塞在同一把锁上。  (c) **下游抖动**：下游调用偶尔耗时 30 秒（可能是其自身 GC、网络问题等）。诊断：trace 分析；识别哪个下游调用慢。  (d) **网络抖动**：偶发包丢失或重传。诊断：TCP 层指标、网口计数器。  检测改...
  c25_b04.md: ...ight 请求的 KV cache 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；...
  c25_b06.md: ...性分配到 (prefill, decode) 对；分配可能需要 rebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill...
  c27_b05.md: ...action）。  (h) **一致性模型**：每个操作应有显式一致性选择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应...
  c27_b05.md: ...显式一致性选择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应可观测（日志、trace、监控）。否则故障在客户投诉前不可见。...
  c27_b05.md: ...action）。  (h) **一致性模型**：每个操作应有显式一致性选择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应...
  c27_b05.md: ...显式一致性选择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应可观测（日志、trace、监控）。否则故障在客户投诉前不可见。...
  c27_b10.md: ...ng action）。  (h) **一致性模型**：每个操作应有显式一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日...
  c27_b10.md: ...操作应有显式一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助...
  c27_b10.md: ...ontext 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助 PR）按此清单审查，可捕获高频 LLM 生成 bug。清单不穷尽；覆盖最常见...
  c27_b10.md: ...ng action）。  (h) **一致性模型**：每个操作应有显式一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日...
  c27_b10.md: ...操作应有显式一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助...
  c27_b10.md: ...ontext 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助 PR）按此清单审查，可捕获高频 LLM 生成 bug。清单不穷尽；覆盖最常见...
  c27_b15.md: ...nsensus-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace context 传播、可观测性。每份分布式代码 PR 按清单审查，捕获高频失败。  为什么 LLM 结构性地不擅长分布...
  c28_b04.md: ...跨赤道。  (3) US-East 的 regional 负载均衡器路由到健康的 API 服务实例。实例开始支付处理。Trace context（OpenTelemetry W3C `traceparent`）全程传播。  (4) API 服务通过...
  c28_b04.md: ...理代价）。远低于 Crescent 产品团队为跨 region 支付地理正确性协商的 750-ms p99 SLO。  Trace（第20章）捕获整个旅程。47 个 span，分布在 6 个服务。Trace 作为取证记录保存 7 年（合规要求）。...
  c28_b04.md: ...性协商的 750-ms p99 SLO。  Trace（第20章）捕获整个旅程。47 个 span，分布在 6 个服务。Trace 作为取证记录保存 7 年（合规要求）。...
  c29_b02.md: ...* **新模式处理**仍弱；人类在新模式和架构选择上的角色持续。 * **AI 辅助事后分析**成为标准；LLM 读取 trace+logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出...
  c30_b08.md: ...复**：通过追踪识别循环 worker 后回滚部署。 * **章节映射防御**：     * Ch.20（分布式追踪）：trace 数据揭示了循环；没有追踪，诊断会耗时更久。     * Ch.27（LLM 生成代码审查）：bug 模式未被 AI 辅...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，哪章有相应防御。...
  c30_b18.md: ...分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，哪章有相应防御。...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，...
  c30_b18.md: ...### 附录 I — Trace 到章节反向索引  调试生产事件看分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，哪章有相应防御。...
  c30_b18.md: ...分布式 trace 时，工程师常知道 _在 trace 里看到什么_，却不知道 _哪章解释它_。本附录是反向索引：给定 trace 症状，哪章有相应防御。...
  c30_b19.md: ...Trace 症状 | 可能原因 | 参考章节   |---|---|---   同一操作两个 span 显示不同效果（重复扣款、重...
  c30_b19.md: ...；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 regi...
  c30_b19.md: ...s | 跨 region Raft quorum；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-token 检查 | 无 fencing 的朴素分布式锁 | Ch.18 §18....
  c30_b19.md: ...| 无 fencing 的朴素分布式锁 | Ch.18 §18.2（Redlock 批评），Ch.27 §27.5   Trace 显示事务提交但读之后返回旧值 | 写后读失败；从没有 HLC 传播的 follower 读 | Ch.5 §5.8（写...
  c30_b19.md: ...C 传播的 follower 读 | Ch.5 §5.8（写后读），Ch.15 §15.2（session 保证）   Trace 显示两次写入成功但 failover 后只持久化一次 | 异步复制；主库在传播前死亡 | Ch.5 §5.3，Ch.5...
  c30_b19.md: ...fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace...
  c30_b19.md: ...trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §...
  c30_b19.md: ...ckpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6   LLM 推理 trace 显示首 token 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分...
  c30_b19.md: ...>300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分离延迟）   LLM 推理 trace 显示长输出请求阻塞短请求 | 队头阻塞；无长度预测路由 | Ch.26 §26.3（长度预测路由）   配置变更导致集...
  c30_b19.md: ...Trace 症状 | 可能原因 | 参考章节   |---|---|---   同一操作两个 span 显示不同效果（重复扣款、重...
  c30_b19.md: ...；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 regi...
  c30_b19.md: ...s | 跨 region Raft quorum；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-token 检查 | 无 fencing 的朴素分布式锁 | Ch.18 §18....
  c30_b19.md: ...| 无 fencing 的朴素分布式锁 | Ch.18 §18.2（Redlock 批评），Ch.27 §27.5   Trace 显示事务提交但读之后返回旧值 | 写后读失败；从没有 HLC 传播的 follower 读 | Ch.5 §5.8（写...
  c30_b19.md: ...C 传播的 follower 读 | Ch.5 §5.8（写后读），Ch.15 §15.2（session 保证）   Trace 显示两次写入成功但 failover 后只持久化一次 | 异步复制；主库在传播前死亡 | Ch.5 §5.3，Ch.5...
  c30_b19.md: ...fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace...
  c30_b19.md: ...trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §...
  c30_b19.md: ...ckpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6   LLM 推理 trace 显示首 token 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分...
  c30_b19.md: ...>300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分离延迟）   LLM 推理 trace 显示长输出请求阻塞短请求 | 队头阻塞；无长度预测路由 | Ch.26 §26.3（长度预测路由）   配置变更导致集...

=== per (202 occurrences) ===
  c01_b11.md: ...roachDB 表），第二个请求会违反约束，重复扣款被阻止。  如果两个副本各自查询本地缓存或尚未复制第一条请求条目的 per-region 存储，那么谁都没看到对方的先前提交。系统在存储层失败了身份谎言：系统相信存在一个“幂等记录存储”，但现实...
  c04_b09.md: ...理。幂等性处理此情况；缺乏幂等性会导致双重处理。  第1章的 Crescent 支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition key（通常是 transaction ID）幂...
  c04_b10.md: ...突发且按进程聚类时效果良好。  (b) **分层 / 分组因果广播** 。把进程分组（例如按区域或租户）。每条消息携带 per-group 向量（组大小条目）加一个 per-region 摘要。跨组因果以较粗粒度跟踪。可扩展 gossip 系统就...
  c04_b10.md: ...层 / 分组因果广播** 。把进程分组（例如按区域或租户）。每条消息携带 per-group 向量（组大小条目）加一个 per-region 摘要。跨组因果以较粗粒度跟踪。可扩展 gossip 系统就是这样做的；代价是跨组因果近似而非精确。  第...
  c04_b10.md: ...就是这样做的；代价是跨组因果近似而非精确。  第三种选择：对某些应用用 _causal+ consistency_ 加 per-key 版本向量替代因果广播。每个键有一个 contributors 向量；向量随键的写作者数增长，而非系统大小。这是...
  c04_b10.md: ...用这种方法。  对大多数 2026 系统，正确的问题是：应用是否真的需要 _所有_ 消息的因果广播，还是因果可以限定在 per-entity 或 per-room 操作？前者很少成立；后者通常够用。...
  c04_b10.md: ...2026 系统，正确的问题是：应用是否真的需要 _所有_ 消息的因果广播，还是因果可以限定在 per-entity 或 per-room 操作？前者很少成立；后者通常够用。...
  c05_b04.md: ...多：写 ack 之后开始的读必须看见该写，即使存在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassand...
  c05_b04.md: ...ollower 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB 和 YugabyteDB 通过 per-transaction HLC 时间戳使用 (c) 的变体。会话跟踪见过的最大 HLC；同一会话的后续读使用至少该 H...
  c05_b04.md: ...使用至少该 HLC。这是现代运维模式。   ### 5.9 五种会话保证  当 follower 可服务读时，应用可从 per-session 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Re...
  c05_b04.md: ...全局。两个会话可以看过不同顺序；保证只适用于单个会话的视图。  大多数现代数据库提供其中子集，常可配置。代价是跟踪成本：per-session HLC 或 LSN 必须存客户端或服务器，并在每次读时检查。...
  c05_b08.md: ...是多少？** Postgres 故障切换（用 Patroni 等）通常 30 秒到数分钟；CockroachDB 自动 per-range leader 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 r...
  c05_b09.md: ...无需显式跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c05_b09.md: ...无需显式跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c05_b10.md: ...复制，但从副本读是异步，远端读副本不影响写延迟。  综合：按操作的正确性需求选择复制模式，而非按系统默认。现代系统提供 per-operation 选择（Spanner 过期读、CockroachDB follower 读、MongoDB wri...
  c05_b11.md: ...真正同时给高可用与正确性的现代替代：Raft 复制 per-range 的多区域系统（CockroachDB）。Raft quorum 可承受单区域丢失；leader 可在另一区...
  c05_b11.md: ...Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-session HLC 跟踪（CockroachDB、YugabyteDB）或把受影响会话路由到主库（带应用逻辑的 P...
  c06_b02.md: ...*监管数据驻留。** 欧盟 GDPR、中国 PIPL、俄罗斯数据本地化法律等要求某些用户数据留在特定司法管辖区。按区域 per-region 存储的区域用户架构由此而来。  成本：  * **跨区域操作延迟税。** 任何跨区域操作都付往返成本。...
  c06_b02.md: ...属区域。  * **Latency** ：归属区域用户看到本地延迟；跨归属操作付全价。 * **Failover** ：per-region 故障切换；其他区域不受影响。 * **Right for** ：以用户为中心的工作负载（聊天、社交、生产...
  c06_b08.md: ...消息传递延迟，这是他们的心理模型）。Spanner 过度；中央 DB 让用户体验不对称。  这正是 Slack 的模式：per-region channels，跨区复制做 federation。  **Q2.** 一个团队为 Raft 复制数据库...
  c07_b05.md: ...心跳的故障检测。默认超时：心跳 100 ms，选举 1000-2000 ms。 * **CockroachDB** 用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-t...
  c07_b05.md: ...ms，选举 1000-2000 ms。 * **CockroachDB** 用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet Paxos。由于 Googl...
  c07_b05.md: ...用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet Paxos。由于 Google 数据中心网络纪律更好，超时更紧。 * **TiKV** 用 Raft。M...
  c08_b09.md: ...。这就是 Spanner 模式（第13章）：跨 shard 事务用 2PC，每个 shard 内部用 Paxos 保证 per-shard 持久性。...
  c09_b05.md: ...joint consensus 的安全子集）。  她放弃的：无 leader 写（CRDT 或 EPaxos 可提供）；per-tablet 性能调优（Spanner 的 Paxos-with-leases 可提供）。对 Stratus 场景（金...
  c10_b05.md: ...是 leader 变更需要更大的 prepare 法定人数。  这一洞见用于一些 2026 年分布式数据库设计，以降低 per-commit 延迟而不牺牲 safety。CockroachDB 和 TiDB 的路线图中已有变体；Foundatio...
  c10_b09.md: ...T 协议通常每决定需要多轮往返。跨 DC 延迟（50-200 ms）意味着每个决定 100-600 ms。元数据存储的 per-operation 延迟受此限制。...
  c10_b10.md: ...ning 是“不同决定在不同阶段同时进行”。  吞吐效果：稳态下两者都能达到每轮往返提交一个决定。HotStuff 的 per-decision 延迟是 3 轮往返，但因 pipelined 所以吞吐与 Raft 相近。差异体现在延迟：Raft...
  c10_b10.md: ...一轮往返；HotStuff 每次提交三轮往返，但 pipelined 使吞吐相同。  区块链含义：HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中是数百毫秒到数秒。若 per-decision 延迟关键，...
  c10_b10.md: ...链含义：HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中是数百毫秒到数秒。若 per-decision 延迟关键，这是成本。若吞吐关键（例如每块处理大量交易的区块链），pipelining 吸收成本。...
  c11_b01.md: ...eSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：CockroachDB、TiDB、YugabyteDB。每个分片（range、region...
  c11_b01.md: ...ugabyteDB。每个分片（range、region、key range）有自己的共识组，通常是 Raft。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租...
  c11_b02.md: ...entual** ：副本最终收敛；读可能过期。Cassandra（默认）、DynamoDB（默认）、Riak。 * **Per-operation 可调** ：应用按请求选择。Cassandra（consistency levels）、Dynam...
  c11_b03.md: ...默认 500 ms）实现线性化时间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上...
  c11_b03.md: ...确定性窗口读付 HLC-restart 成本） Strengths | 兼容 Postgres SQL；可部署于任何云；per-range Raft 水平扩展 Weaknesses | 比单区域 Postgres 写延迟更高；运维调优复杂 Rig...
  c11_b04.md: ...el | 默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 per-item 线性化 PACELC | 默认 PA / EL（低延迟、最终）；条件写操作 PC / EC Strength...
  c11_b05.md: ...从 Postgres 兼容代码出发更低。选 CockroachDB。 > > “DynamoDB 出局：一致性模型需要 per-operation 纪律，我们的团队还没准备好为金融工作负载维护。Cassandra 出局：可调一致性很强大，但运维负...
  c11_b05.md: ...每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serial...
  c11_b05.md: ...”只告诉你分区行为，没告诉你正常运营成本。总要问 PACELC 问题（无分区时每操作成本是多少毫秒？）。  (c) **per-operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per...
  c11_b05.md: ...per-operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per-operation 细分才重要。  (d) **营销级可用性声明。** “99.99% 可用性”是 SLA 声明；分区...
  c11_b05.md: ...从 Postgres 兼容代码出发更低。选 CockroachDB。 > > “DynamoDB 出局：一致性模型需要 per-operation 纪律，我们的团队还没准备好为金融工作负载维护。Cassandra 出局：可调一致性很强大，但运维负...
  c11_b05.md: ...每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serial...
  c11_b05.md: ...”只告诉你分区行为，没告诉你正常运营成本。总要问 PACELC 问题（无分区时每操作成本是多少毫秒？）。  (c) **per-operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per...
  c11_b05.md: ...per-operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per-operation 细分才重要。  (d) **营销级可用性声明。** “99.99% 可用性”是 SLA 声明；分区...
  c11_b06.md: ...nearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Lin...
  c11_b06.md: ...提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看...
  c11_b06.md: ...erializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作 B 开始前...
  c11_b06.md: ...间生效，且符合实时顺序。若操作 A 在操作 B 开始前完成返回，则 A 排在 B 前。  Serializable 是 per-transaction 属性：任何并发事务的执行都等价于某种串行顺序。该顺序不必符合实时；即使事务 A 在 B 开始前...
  c11_b09.md: ...诊断方法，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但用 C++ 编写，采用 shard-per-core。协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Quest...
  c11_b09.md: ...cyllaDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node t...
  c11_b09.md: ...（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operat...
  c11_b09.md: ...诊断方法，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但用 C++ 编写，采用 shard-per-core。协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Quest...
  c11_b09.md: ...cyllaDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node t...
  c11_b09.md: ...（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operat...
  c11_b11.md: ...陷阱：营销级“一致性”声明混淆 linearizable 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 benchmark。方法通过要求精确答案抵御这些陷阱。  第12章进入下...
  c12_b01.md: ...on 内实现局部范围查询）。  选择取决于工作负载：纯 OLTP 点查偏向哈希；分析扫描偏向范围；混合工作负载需要通过 per-table 配置获得两者。...
  c12_b06.md: ...时间戳范围分片使该查询成为单分区扫描。示例：HBase、ClickHouse、TimescaleDB。  许多现代系统 per-table 提供两者：CockroachDB 默认范围但支持哈希分片表；Cassandra 哈希分片但允许同一 par...
  c12_b08.md: ...**答案：**  (a) **按 tenant ID 哈希分片。** 每个租户数据在一个分区（可能一个节点）。优点：per-tenant 查询是单分区；租户数据有局部性；重分片可原子移动租户数据。缺点：大租户可能占满一个分区（热租户问题）；系...
  c12_b08.md: ...row ID) 范围分片。** 租户按序排列；每个租户内部行有序。优点：大租户数据可跨多个 range（太大时分裂）；per-tenant 范围扫描高效；小租户共置于同一 range。缺点：小租户相邻意味着热小租户会影响邻居；range 元数据...
  c12_b08.md: ...每租户一个数据库。** 每个租户有自己的数据库（或 schema、namespace、project）。优点：最大隔离；per-tenant 资源控制；删除租户 trivial。缺点：运维成本随租户数增长（管理成千上万个数据库）；跨租户聚合很难；...
  c12_b08.md: ...-tenant 资源控制；删除租户 trivial。缺点：运维成本随租户数增长（管理成千上万个数据库）；跨租户聚合很难；per-tenant 开销（连接池、schema 版本）。  建议：  * 10 到 100 个租户：选项 (c) 运维可管理...
  c12_b10.md: ...sandra 和 DynamoDB 不比 CockroachDB 不成熟；它们有不同设计优先级（大规模水平扩展、可预测 per-node 性能）。  综合：对大多数不可预测倾斜的 OLTP 工作负载，带自动分裂的范围分片是正确默认。对应用控制键分...
  c12_b10.md: ...TP 工作负载，带自动分裂的范围分片是正确默认。对应用控制键分布的工作负载（缓存、会话存储），哈希是正确默认。现代系统 per-table 提供两者；选择不是“哪个系统”，而是“哪张表用哪种方案”。  第二个考虑：范围分片的元数据开销真实。对拥有...
  c12_b11.md: ...节点分布数据。哈希实现均匀分布和简单路由，但不支持范围查询；范围提供查询局部性和自动分裂，但顺序写有热点风险。现代系统 per-table 提供两者。  一致哈希是哈希分片系统的基础：增删节点只移动 $O(1/N)$ 数据。Vnodes（每物理节...
  c13_b04.md: ...行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。...
  c13_b10.md: ...区域法定人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West / US-West），per-shard 提交最坏情况约 90 ms（第6章 AP Q2）。事务必须在所有三个分片提交后才会返回给应用；总提交延迟受...
  c14_b02.md: ...based MVCC，NTP 偏移上界（默认 500 ms）。 * Postgres 线缆兼容。 * 多区域部署，支持 per-table 区域配置。 * 开源核心（CockroachDB Community）；企业版附加高级功能。  优势：...
  c14_b02.md: ...优势：  * 可跑在任何云或本地。 * Postgres 兼容性意味着现有工具（psql、驱动、ORM）可用。 * Per-table 多区域配置（global、regional、follower reads）。 * 默认可串行化。  劣势：...
  c14_b02.md: ...based MVCC，NTP 偏移上界（默认 500 ms）。 * Postgres 线缆兼容。 * 多区域部署，支持 per-table 区域配置。 * 开源核心（CockroachDB Community）；企业版附加高级功能。  优势：...
  c14_b02.md: ...优势：  * 可跑在任何云或本地。 * Postgres 兼容性意味着现有工具（psql、驱动、ORM）可用。 * Per-table 多区域配置（global、regional、follower reads）。 * 默认可串行化。  劣势：...
  c14_b04.md: ...；“set it and forget it” 运维。  劣势：厂商锁定（专有格式，但 Iceberg 集成正在出现）；per-query 成本可能让调优差的工作负载意外。   #### BigQuery 的 Dremel  Google 的分析...
  c14_b05.md: ...载应用方法。** 假设你在构建实时欺诈检测服务：读率高（~50k reads/sec），写由事件驱动且突发，数据主要是 per-account 状态加一些跨账户图查询，读需要一致性（不能用过期数据做欺诈决策），单区域部署，工作负载包含每晚重训欺诈...
  c14_b08.md: ...在线节点）。OLAP 常是弹性计算（为查询启动 worker，结束后释放）。 * **一致性** ：NewSQL 提供 per-transaction ACID。OLAP 常提供快照一致（查询看到查询开始时的一致视图，查询期间写入不反映）。 *...
  c14_b10.md: ...100 倍。全球：多区域。  **答案：** 工作负载分析：  * **写**：10 万/秒 score 提交。需要 per-player 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7...
  c14_b10.md: ...过度；每次写成本太高（每个 score 提交都走共识往返）。能工作但贵。  (b) **DynamoDB（KV）** ：per-item 条件写做原子更新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载很 fit。  (c)...
  c14_b10.md: ...区域写亚毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。...
  c14_b10.md: ...on；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5...
  c14_b10.md: ...100 倍。全球：多区域。  **答案：** 工作负载分析：  * **写**：10 万/秒 score 提交。需要 per-player 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7...
  c14_b10.md: ...过度；每次写成本太高（每个 score 提交都走共识往返）。能工作但贵。  (b) **DynamoDB（KV）** ：per-item 条件写做原子更新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载很 fit。  (c)...
  c14_b10.md: ...区域写亚毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。...
  c14_b10.md: ...on；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5...
  c14_b11.md: ...为此设计；NewSQL 在没有积极缓存情况下难以达到每节点如此读量。混合（DynamoDB + Redis）给出正确的 per-operation 成本画像。...
  c15_b02.md: ...#### 实践实现  标准实现使用 per-session HLC（第3章）跟踪。客户端记录观察到的最大 HLC；后续操作要求服务器至少赶上该 HLC。服务器要么...
  c15_b03.md: ...ncy level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency level ALL...
  c15_b03.md: ...可能丢失近期写。  对需要更强保证的应用，Redis 提供 `WAIT N` 命令，阻塞直到 N 个副本 ack。这是 per-write 选择加入，非全局模式。...
  c15_b04.md: ...每个副本维护一个 “各副本贡献递增” 向量（每个副本一个条目）。总数是向量之和。合并两个 G-Counter 时取 per-entry 最大值（每个副本的贡献至少是已报告的最高值）。  成本：与最终一致相同，但有形式化合并语义。不可能丢失递增...
  c15_b05.md: ...购买使库存减一；绝不能超卖。   #### 需要线性化  若两个并发购买都想对最后一件商品减一，必须只成功一个。这需要 per-item 的线性化递减。  实现：可串行化数据库中的行级锁（Spanner、CockroachDB、Postgres）...
  c15_b06.md: ...要场景：跨会话的因果对应用重要。示例：聊天中同一用户的消息按输入顺序出现。  大多数生产系统只提供子集。应用代码可通过 per-session 跟踪 HLC 或版本号构建缺失的保证。  **Q2.** 为什么计数器工作负载（页面浏览计数器）适合最...
  c15_b08.md: ...覆盖的报告。诊断并给出修复建议。  提示  写节点间时钟偏移可导致 “逻辑上更晚” 的写输掉。检查 NTP 纪律并考虑 per-field 处理。  **答案：** 诊断：Cassandra 协调节点间时钟偏移。当用户从设备 A 编辑资料（通过协...
  c15_b08.md: ...(c) **轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。LWT 用 Paxos 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field...
  c15_b08.md: ...os 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field CRDT** ：对合并语义明确的字段（如 “tags” 字段可用 CRDT 集合；“last_update...
  c15_b08.md: ...覆盖的报告。诊断并给出修复建议。  提示  写节点间时钟偏移可导致 “逻辑上更晚” 的写输掉。检查 NTP 纪律并考虑 per-field 处理。  **答案：** 诊断：Cassandra 协调节点间时钟偏移。当用户从设备 A 编辑资料（通过协...
  c15_b08.md: ...(c) **轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。LWT 用 Paxos 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field...
  c15_b08.md: ...os 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field CRDT** ：对合并语义明确的字段（如 “tags” 字段可用 CRDT 集合；“last_update...
  c15_b09.md: ...生不支持 CRDT；应用必须自己实现。  (c) **迁移到多主系统 + 无冲突语义** ：CockroachDB 的 per-table 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoD...
  c15_b09.md: ...+ 无冲突语义** ：CockroachDB 的 per-table 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重...
  c15_b10.md: ...作_，而非系统。同一应用内，有些操作适合最终一致（计数器、浏览追踪），有些必须强一致（支付、唯一申领）。系统应同时支持 per-operation 选择；工程师的职责是正确分类每个操作。  2026 年现实：大多数新系统使用 NewSQL（默认强...
  c15_b10.md: ...ation 选择；工程师的职责是正确分类每个操作。  2026 年现实：大多数新系统使用 NewSQL（默认强一致）或 per-operation 可调系统（DynamoDB 条件写、Cassandra LWT）。“默认最终一致” 在 2010...
  c15_b11.md: ...follow-reads、monotonic writes、consistent prefix）定义了完整线性化的有用 per-session 弱化版本。CockroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话...
  c16_b02.md: ...R-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值...
  c17_b01.md: ...许的每个行为也都被模型 B 允许，则 A 强于 B。  从强到弱的经典层级：  * **Linearizable** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-opera...
  c17_b01.md: ...le** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consis...
  c17_b01.md: ...on；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-eventual** ：...
  c17_b01.md: ...需要越少协调）；模型越弱，应用成本越高（需要更多纪律处理更弱保证）。  2026 生产现实：大多数系统提供跨多个模型的 per-operation 选择。应用代码为每次操作选合适级别。   ### 17.2 因果一致性  经典表述（第3章 Lam...
  c17_b01.md: ...：从 follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with explicit per-session tracking** ：非原生；应用层实现。...
  c17_b02.md: ...排序；读者看到最近 HLC 截止前的所有提交。  成本：每次提交 HLC 计算和传播；定期重新计算排行榜。因为工作是 per-submission 而非协调，系统可跨区域每秒服务数百万提交。   #### 库存（第13章）：线性化  库存提交；...
  c17_b02.md: ...非协调，系统可跨区域每秒服务数百万提交。   #### 库存（第13章）：线性化  库存提交；“不能低于零” 需要协调；per-item 线性化通过事务或 Raft 复制的递减实现；每次 5-50 ms。  三个工作负载的选择展示了频谱。方向错误...
  c17_b03.md: ...ynamoDB** ：最终读（默认）；强一致读；条件写（per-item 线性化）。 * **Cassandra** ：per-operation 一致性级别（ONE、QUORUM、ALL）；LWT 线性化。  优点：单一运维足迹；跨操作分析容易...
  c17_b03.md: ...ion 一致性级别（ONE、QUORUM、ALL）；LWT 线性化。  优点：单一运维足迹；跨操作分析容易。缺点：需要 per-operation 纪律；难以通过系统规则强制 “该表所有写都走线性化”。   #### 模式 B：不同操作用不同系统...
  c17_b05.md: ...配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出 _相交_（第...
  c17_b05.md: ...晚” 由 Cassandra 的 LWW 冲突解决决定，存在第3章的时钟偏移问题。  QUORUM _不提供_：  * Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-...
  c17_b05.md: ...Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-key；多键一致性需要轻量事务（LWT，基于 Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUORUM 读独...
  c17_b05.md: ...践中：QUORUM 对许多工作负载是 “足够好” 的一致性（大多数数据库操作是单键，可容忍 LWW 解析）。对需要真正 per-key 线性化的工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，...
  c17_b05.md: ...配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出 _相交_（第...
  c17_b05.md: ...晚” 由 Cassandra 的 LWW 冲突解决决定，存在第3章的时钟偏移问题。  QUORUM _不提供_：  * Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-...
  c17_b05.md: ...Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-key；多键一致性需要轻量事务（LWT，基于 Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUORUM 读独...
  c17_b05.md: ...践中：QUORUM 对许多工作负载是 “足够好” 的一致性（大多数数据库操作是单键，可容忍 LWW 解析）。对需要真正 per-key 线性化的工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，...
  c17_b07.md: ...#### 应用问题  **Q1.** 一个团队要构建聊天应用。聊天是 per-room；用户发帖和回复。选择一致性模型并论证。  提示  回复必须跟随原帖（因果）；不同用户的并发消息可任意顺序（因...
  c17_b07.md: ...打到未收到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时携带其 HLC；服务按 HLC 排序消息；读查询 room 当前 HLC 前沿。大多数聊天...
  c17_b10.md: ...20% 用线性化，其余用最终，架构比到处跟踪因果更简单。  综合：因果一致是有用的中间点，但不是通用默认。正确默认是 per-operation：需要严格排序的操作用线性化；有因果关系的用因果；没有的用最终。现代系统支持这种 per-opera...
  c17_b10.md: ...正确默认是 per-operation：需要严格排序的操作用线性化；有因果关系的用因果；没有的用最终。现代系统支持这种 per-operation 选择；把所有操作塞进一个模型很少正确。  第二个考虑：历史上因果一致在专业系统（COPS、Eige...
  c17_b10.md: ...微服务实例每秒计数事件；计数聚合到中央仪表盘。事务计数器要求每次递增咨询中央系统；延迟和吞吐都不对。基于 CRDT 的 per-instance 计数器加定期聚合自然处理。  (c) **实时分析**。一个 “当前在线用户数” 计数器每秒更新 5...
  c17_b11.md: ...章）；库存是线性化（第13章）。每个都是其需求的正确一致性；选错级别要么导致正确性违规，要么浪费资源。  生产系统提供 per-operation 一致性选择：Spanner（线性化 + 过期读）、DynamoDB（最终 + 条件写）、Cassa...
  c18_b02.md: ...herd）** ：多个读者在 key 过期时同时请求；全部未命中并涌向 source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事故期间的缓存踩踏** ：truth source 变慢或宕机；所有缓存过期，所有读者击中...
  c18_b03.md: ...序。  代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；...
  c18_b07.md: ...服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operation 安全。  **答案：** 组合提供两层安全：  (a) **Raft 锁服务** ：保证锁获取本身...
  c18_b08.md: ...）：专用系统（Kafka with consumer groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次...
  c18_b08.md: ...nsumer groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮...
  c18_b09.md: ...ing herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示  概率性提前刷新、刷新期间 per-key 锁、请求合并、异常时提供过期数据。  **答案：** 三种缓解策略：  (a) **概率性提前刷新**：每个缓...
  c18_b09.md: ...过期临近增加。到期时，缓存已被某个读者刷新；后续读者看到新条目。Vattani 等人的模式显著减少踩踏。  (b) **Per-key 刷新锁**：缓存未命中时，读者获取该 key 的短期锁。其他并发读者阻塞在锁上；只有一个去数据库取；完成后回填...
  c18_b09.md: ...存未命中只发生在冷 key（不会发生踩踏）。  对高流量系统，常见组合 (a) + (b)：概率性提前刷新处理常见情况；per-key 锁处理最坏情况。(d) 处理数据库中断情况。  更深观察：惊群是缓存被要求做太多的征兆。若单个缓存 key 热...
  c18_b09.md: ...ing herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示  概率性提前刷新、刷新期间 per-key 锁、请求合并、异常时提供过期数据。  **答案：** 三种缓解策略：  (a) **概率性提前刷新**：每个缓...
  c18_b09.md: ...过期临近增加。到期时，缓存已被某个读者刷新；后续读者看到新条目。Vattani 等人的模式显著减少踩踏。  (b) **Per-key 刷新锁**：缓存未命中时，读者获取该 key 的短期锁。其他并发读者阻塞在锁上；只有一个去数据库取；完成后回填...
  c18_b09.md: ...存未命中只发生在冷 key（不会发生踩踏）。  对高流量系统，常见组合 (a) + (b)：概率性提前刷新处理常见情况；per-key 锁处理最坏情况。(d) 处理数据库中断情况。  更深观察：惊群是缓存被要求做太多的征兆。若单个缓存 key 热...
  c19_b04.md: ...LS**：服务间加密与认证。  (c) **流量策略**：金丝雀发布、蓝绿、A/B 测试。  (d) **可观测性**：per-request 追踪、延迟指标、错误率。  (e) **弹性**：熔断、重试预算、超时策略。  2026 年两大主导服...
  c20_b10.md: ...展示跨服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 t...
  c21_b07.md: ...hedding**：每个服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) *...
  c21_b09.md: ...游放大 6 倍负载。 * 退避（2^attempt 秒）让每个调用变慢（完全重试的调用耗时 31 秒）。 * 组合产生 per-call 延迟和下游放大；两者都坏。  正确答案结合多种技术：  (a) **调用者处的重试预算**：把重试限制为成功...
  c21_b09.md: ...熔断器**：当下游失败时，停止调用。快速失败而非缓慢失败。  (c) **超时策略**：基于下游预期 p99 延迟设置 per-call 超时；不要超时过长。  (d) **幂等性**：确保重试安全（同一操作可多次执行而无坏效果）。  (e) *...
  c22_b02.md: ...快）；10% 未命中（需要磁盘读取）。  触发器：流量尖峰。缓存命中率短暂降到 70%。磁盘读取率翻三倍；磁盘队列堆积；per-query 延迟增加。  反馈循环：延迟增加意味着每个查询持有内存更久（查询状态直到完成才释放）；内存压力增加；缓存缩...
  c22_b03.md: ...系统无法自行做到。   #### 防御亚稳态  (a) **有界反馈循环**。每个循环必须有阻尼机制。对缓存颠簸：限制 per-query 内存；内存耗尽时拒绝新查询（load shedding）。对重试风暴：重试预算。  (b) **滞后（hy...
  c22_b08.md: ...层级）的系统能更优雅地从亚稳态恢复。  **Q3.** 走一遍用基于延迟的路由防御灰色故障的负载均衡器设计。  提示  Per-backend p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a)...
  c22_b08.md: ...ackend p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a) **Per-backend 延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (...
  c22_b08.md: ...end 延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (b) **Per-backend p99 计算**：每几秒计算每个后端的 p99 延迟。  (c) **集群范围延迟参考**：计算所有后...
  c24_b05.md: ...uum 的客户定价：通常每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个请求的 GPU-秒。更好的 attention（PagedAtte...
  c24_b05.md: ...率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching...
  c24_b06.md: ...LLM 推理在结构上与典型微服务舰队不同？  提示  GPU 作为单元、两阶段计算、KV cache 作为状态、严格的 per-token 延迟、突发模式。  **答案：** 五个结构差异：  (a) **GPU 是单元，而非节点**。昂贵资源是...
  c24_b06.md: ...优化每个阶段是 2025-2026 架构突破。  (c) **KV cache 作为中心状态**。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 p...
  c24_b06.md: ...心状态**。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**...
  c24_b06.md: ...r-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 token 必须在 10-...
  c24_b06.md: ...是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 token 必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微...
  c24_b06.md: ...ken 延迟预算**。每个生成 token 必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per-request 预算更宽松；推理有 per-token 预算。  (e) **突发工作负载模式**。请求从单次问答到大...
  c24_b06.md: ...必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per-request 预算更宽松；推理有 per-token 预算。  (e) **突发工作负载模式**。请求从单次问答到大文档分析不等。集群必须处理混合而无线头阻塞。...
  c24_b06.md: ...预算。  (e) **突发工作负载模式**。请求从单次问答到大文档分析不等。集群必须处理混合而无线头阻塞。微服务通常 per-request profile 更均匀。  这些差异证明把分布式 LLM 推理视为独立子学科。协议（PagedAtte...
  c24_b07.md: ...，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 m...
  c24_b10.md: ...I 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-request 延迟。LLM 应提供完整可观测性方案。  (e) **自动扩缩**：随负载变化，集群应扩缩（Kuber...
  c24_b11.md: ...服务舰队的关键差异：GPU 是单元；两阶段计算（prefill + decode）；KV cache 是中心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAttention 把 KV cache 分成固定...
  c25_b01.md: ...）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Ma...
  c25_b02.md: ...P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2...
  c25_b02.md: ...K1 -->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2         D1 -->|"streamed tokens"| LB         D2...
  c25_b03.md: ...KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode...
  c25_b05.md: ...token）每个请求产生数十 GB KV cache。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GPU 内存。  实现：  (a) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV...
  c25_b05.md: ...M 集成设计。提供集群级 KV cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀...
  c25_b06.md: ....** 一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token...
  c25_b06.md: ...似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输出）的延迟组...
  c25_b06.md: ...cated（未分离）架构，首 token 延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-token 延迟类似。分离版为首 token 多付约 50 ms（传输）；吞吐收益来自集群整体更好的每 GPU 利用率...
  c25_b09.md: ...Continuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两阶段都保持 15% headroom；基于 per-phase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比...
  c25_b11.md: ...he）实现这些模式。采用阈值：约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用...
  c26_b03.md: ...(a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：dra...
  c26_b04.md: ...间调度请求。  朴素 FIFO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) *...
  c26_b04.md: ...为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调...
  c26_b04.md: ...驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运维关注点：每请求调度决策必须非常快（亚毫秒），以免增加 per-request 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ###...
  c26_b04.md: ...节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **硬件**：面向 disa...
  c26_b04.md: ...间调度请求。  朴素 FIFO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) *...
  c26_b04.md: ...为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调...
  c26_b04.md: ...驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运维关注点：每请求调度决策必须非常快（亚毫秒），以免增加 per-request 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ###...
  c26_b04.md: ...节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **硬件**：面向 disa...
  c26_b06.md: ...ve decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例...
  c26_b07.md: ...：SLA 客户有可预测延迟；低 tier 客户在峰值期间也有有界服务。  运维关注点：调度决策必须快（亚毫秒）以免增加 per-request 延迟。决策还必须考虑请求预期资源消耗（长输出请求比短请求消耗更多 “credit”）。这些因素使调度器...
  c26_b08.md: ...r-1 客户。  综合：该客户每请求成本降低约 5 倍（前缀复用），延迟显著改善。Continuum 能给出有竞争力的 per-request 定价；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户 onboarding 从第...
  c26_b09.md: ...长输出请求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部感知负载均衡器**：监控 per-node 延迟；若某节点异常高（灰色故障，第22章），绕行。Envoy outlier detection 或类似。...
  c26_b09.md: ...ated speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 v...
  c26_b09.md: ...cation）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳数增加 0.5-2 ms）。  各何时更好：  * **Co-located**：小集群（<...
  c26_b09.md: ...2 ms）。  各何时更好：  * **Co-located**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。...
  c26_b10.md: ...衡（least-loaded、round-robin 等）。  (g) **指标和可观测性**：缓存命中率、每请求节省、per-customer 缓存效率。...
  c27_b01.md: ...说 “我们用 CP 系统” 而不理解 PACELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM 默认产出嵌入全部六个的代码。本章用实例逐一走过。...
  c27_b04.md: ...account_id)   谎言：“CP” 意味着 “完全一致”。代码注释显示 LLM 在应用口号级 CAP，不理解 per-operation 行为。  现实：CockroachDB 默认读写事务是可串行化（serializable）；读可以...
  c27_b04.md: ...产出第一版加误导注释是因为口号级 CAP 陈述在训练数据中过度代表。修复需要理解 PACELC（第2章）和具体数据库的 per-operation 语义。...
  c27_b09.md: ...补偿动作。    6. **CAP 分类简单**：代码说 “我们用 CP 系统” 或 “我们用 AP 系统”，而不理解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默...
  c27_b09.md: ...ockroachDB is CP, so this is consistent` 后接未指定的读。口号级 CAP，缺少 per-operation 一致性选择。  每个模式都在训练数据中，且常大量出现。LLM 把它们作为默认产出；识别它们是 bu...
  c28_b10.md: ...诊断画像 | 四个诊断问题：复制、恢复、一致性、分区 | 接受营销级 “一致”；混淆 CAP 与 PACELC；遗漏 per-operation 一致性选择 | | 7 | Anya 的重分片迁移 | Ch.12 §12.5 | N 节点集群磁...
  c29_b02.md: ...#### LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典...
  c29_b02.md: ...### LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典型工作负载首 token 延迟 100-300 ms。  2027...
  c29_b02.md: ...ormer，可能需要不同的分布式系统处理。  心智模型（GPU 是单元；两阶段计算；KV cache 是中心状态；严格 per-token 延迟）可推广到 MoE，很可能也到扩散-LM。具体原语（PagedAttention、RadixAtten...
  c30_b07.md: ...2 | Saga 模式；outbox；补偿动作 | | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-operation 一致性选择 |  Crescent 支付系统对每个谎言的防御在第28章详述。  * * *   #...
  c30_b10.md: ...is Cluster 用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正...
  c30_b11.md: ...y 用于静态内容路径。 * **缓存**：区域 Redis 或 Memcached 用于热读。 * **一致性策略**：per-operation 选择；内容读用最终一致，唯一声明操作（用户名保留等）用条件写入。 * **为何这个组合**：读规模...
  c30_b11.md: ...a 管理**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 *...
  c30_b13.md: ...满足吞吐 / 可用性需求。   2. **故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分区行为** 基于应用能容忍什么。  然后通过第14章...
  c30_b13.md: ...2. **故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分区行为** 基于应用能容忍什么。  然后通过第14章矩阵映射到系统。再应用相关防御（重...
  c30_b14.md: ...all N)     │   └── hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/     │...
  c30_b16.md: ..., Ch.11 | | 5 | "我们用 CP 系统，所以一致。" | CP 命名分区行为；PACELC 命名日常成本。Per-operation 一致性才是正确问题。 | Ch.2 | | 6 | "向量时钟告诉你现在几点。" | 向量时钟推理...

=== cache (182 occurrences) ===
  c06_b03.md: ...**分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）、应用必须使用 RDMA 原语（one-sided...
  c06_b06.md: ...的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是 bu...
  c06_b07.md: ...的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是预算内...
  c18_b01.md: ...模式  Phil Karlton 的名言：“计算机科学中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **W...
  c18_b01.md: ...tion**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven in...
  c18_b01.md: ...事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-throu...
  c18_b01.md: ...模式  Phil Karlton 的名言：“计算机科学中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **W...
  c18_b01.md: ...tion**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven in...
  c18_b01.md: ...事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-throu...
  c18_b09.md: ...阻塞后从缓存读取。  (c) **请求合并**：在应用层（或缓存客户端库），同一 key 的并发请求去重。只有一条真实 cache-miss 请求去数据库；其他等待结果。  (d) **异常时提供过期数据**：缓存过期且数据库慢或宕机时，提供过期值而...
  c24_b01.md: ...e、prefill、decode），以及自己的部署模式（disaggregated prefill/decode、KV cache sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NV...
  c24_b01.md: ...laude 3 Opus 级别）需要的内存超过任何单张 GPU。模型权重 alone 就是数十 GB；每个请求的 KV cache 又增加更多。推理必须跨 GPU 分布。  但分布式推理不是 “以 LLM 为负载的分布式系统”。它有与典型微服务舰队结...
  c24_b01.md: ...nd；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV cache 作为中心状态**。KV cache 是让模型逐 token 解码的工作内存。它很大（每个活跃请求 1-100 GB），...
  c24_b01.md: ...-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV cache 作为中心状态**。KV cache 是让模型逐 token 解码的工作内存。它很大（每个活跃请求 1-100 GB），短命（请求结束即释放），并且是内存和...
  c24_b01.md: ...瓶颈。  (d) **严格的延迟预算**。每个 token 生成延迟 10-50 ms。在此预算内，系统必须读取 KV cache、计算 next-token logits、采样、推进状态。超过几毫秒的网络跳数都很重要。  (e) **突发工作负载模...
  c24_b02.md: ...发布；2024-2026 年广泛部署）引入了 _PagedAttention_：把操作系统分页思想用于 LLM KV cache 管理的启发式应用。  经典问题：活跃请求的 KV cache 是连续内存。若请求需要 4 GB KV cache，系统...
  c24_b02.md: ...ttention_：把操作系统分页思想用于 LLM KV cache 管理的启发式应用。  经典问题：活跃请求的 KV cache 是连续内存。若请求需要 4 GB KV cache，系统预留 4 GB。请求结束后 4 GB 释放。碎片累积；GPU...
  c24_b02.md: ...KV cache 管理的启发式应用。  经典问题：活跃请求的 KV cache 是连续内存。若请求需要 4 GB KV cache，系统预留 4 GB。请求结束后 4 GB 释放。碎片累积；GPU 内存无法被充分利用。  vLLM 的解决方案：把 K...
  c24_b02.md: ...统预留 4 GB。请求结束后 4 GB 释放。碎片累积；GPU 内存无法被充分利用。  vLLM 的解决方案：把 KV cache 切成固定大小块（例如每块 16 个 token）。每个请求的 KV cache 是块列表（不必连续）。块可独立分配和释...
  c24_b02.md: ...用。  vLLM 的解决方案：把 KV cache 切成固定大小块（例如每块 16 个 token）。每个请求的 KV cache 是块列表（不必连续）。块可独立分配和释放；碎片被消除。  收益：  (a) **更高内存利用率**：典型 vLLM 部...
  c24_b02.md: ...片被消除。  收益：  (a) **更高内存利用率**：典型 vLLM 部署使用 70-90% GPU 内存用于 KV cache（相比朴素系统 30-50%）。每张 GPU 能容纳更多请求。  (b) **跨请求共享**：共享 prompt 的请求...
  c24_b02.md: ...请求共享**：共享 prompt 的请求（system prompt + user message）可共享前缀的 KV cache 块。对聊天机器人风格工作负载，内存节省巨大。  (c) **更高吞吐**：每张 GPU 并发请求更多；每个请求成本更低...
  c24_b03.md: ...unction-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新请求到达时，系统找到最长缓存前缀并复用；只有后缀需要重新计算。  收益：  (a) **共享前缀的巨大节省**：对具...
  c24_b06.md: ...*Q1.** 是什么让分布式 LLM 推理在结构上与典型微服务舰队不同？  提示  GPU 作为单元、两阶段计算、KV cache 作为状态、严格的 per-token 延迟、突发模式。  **答案：** 五个结构差异：  (a) **GPU 是单元...
  c24_b06.md: ...ound）有不同最优硬件/软件 profile。独立优化每个阶段是 2025-2026 架构突破。  (c) **KV cache 作为中心状态**。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。...
  c24_b06.md: ...ofile。独立优化每个阶段是 2025-2026 架构突破。  (c) **KV cache 作为中心状态**。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态...
  c24_b06.md: ...推理专用。  **Q2.** 描述 PagedAttention。它解决什么问题？  提示  受 OS 分页启发。KV cache 分固定大小块；不连续；消除碎片。  **答案：** PagedAttention（vLLM，2023）把 KV cac...
  c24_b06.md: ...che 分固定大小块；不连续；消除碎片。  **答案：** PagedAttention（vLLM，2023）把 KV cache 分成固定大小块（如每块 16 个 token）。每个请求的 KV cache 是块列表，不必在内存中连续。块可独立分配...
  c24_b06.md: ...ntion（vLLM，2023）把 KV cache 分成固定大小块（如每块 16 个 token）。每个请求的 KV cache 是块列表，不必在内存中连续。块可独立分配和释放。  它解决的问题：经典 KV cache 管理为每个请求预留连续内存。...
  c24_b06.md: ...oken）。每个请求的 KV cache 是块列表，不必在内存中连续。块可独立分配和释放。  它解决的问题：经典 KV cache 管理为每个请求预留连续内存。若请求需要 4 GB，系统预留 4 GB；请求结束后释放。久而久之内存碎片化；即使总空闲内...
  c24_b06.md: ...50%）。  PagedAttention 收益：  * **更高内存利用率**：70-90% GPU 内存用于 KV cache，相比朴素系统 30-50%。 * **每张 GPU 更多并发请求**：更好内存利用率意味着更多请求 fit；每张 GP...
  c24_b06.md: ...用率意味着更多请求 fit；每张 GPU 吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同 system prompt 的聊天工作负载，内存节省巨大。  灵感来自操作系统虚拟内存：页让进程无需连...
  c24_b06.md: ...灵感来自操作系统虚拟内存：页让进程无需连续物理地址即可使用内存。PagedAttention 把同样思想用于 KV cache。  2026 年，vLLM（及 PagedAttention 衍生系统）是最广泛部署的开源推理引擎。该技术是基线；进一...
  c24_b06.md: ...是最广泛部署的开源推理引擎。该技术是基线；进一步优化（RadixAttention、disaggregation、KV cache sharding）在此基础上构建。  **Q3.** 比较 vLLM 和 SGLang。各适合什么场景？  提示  v...
  c24_b07.md: ...rving、无特定前缀复用模式。  _SGLang_：  * 核心创新：RadixAttention（基于前缀的 KV cache 复用）。 * 优势：对共享前缀工作负载节省巨大。 * 最适合：具有长共享 system prompt、结构化输出、函数...
  c24_b07.md: ...GI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 KV cache。复杂度值得吞吐收益。...
  c24_b08.md: ...预期最终集群利用率 80%+，在 120 万美元账单上每月节省 25-40 万美元。  其他技术（简述）：跨节点 KV cache sharding（第25章）；speculative decoding（第26章）；模型量化（8-bit、4-bit...
  c24_b10.md: ...式系统模式适用；不需要新理论。”  **讨论：** 辩护：分布式系统理论（共识、复制、分片、负载均衡）适用于推理。KV cache sharding 就是分片；推理节点间负载均衡就是负载均衡；故障转移就是故障转移。通用模式足够。  反驳：推理有结构属...
  c24_b10.md: ...就是故障转移。通用模式足够。  反驳：推理有结构属性（per-token 延迟预算、两阶段计算、GPU 作为单元、KV cache 作为状态），通用分布式系统不会为之优化。专用协议（PagedAttention、RadixAttention、disa...
  c24_b10.md: ...系统知识加推理专用技术。  第二个考虑：推理领域快速演进。2025-2026 突破（disaggregation、KV cache sharding、speculative decoding as distributed protocol）是近期；2...
  c24_b10.md: ...r parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-request 延迟。LLM 应提供完整可观测性方案。  (e) **自动扩缩**：随负...
  c24_b11.md: ...布式系统的一个子学科。与典型微服务舰队的关键差异：GPU 是单元；两阶段计算（prefill + decode）；KV cache 是中心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAttention 把...
  c24_b11.md: ...心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAttention 把 KV cache 分成固定大小块；消除碎片；GPU 内存利用率从 30-50% 提升到 70-90%。2026 年基线开源推理引擎。...
  c24_b11.md: ...升到 70-90%。2026 年基线开源推理引擎。  SGLang 的 RadixAttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA...
  c24_b11.md: ...用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated prefill/decode + KV cache sharding 图。  ---...
  c25_b01.md: ...章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Splitwise、DistServe）；生产部署在...
  c25_b01.md: ...：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不...
  c25_b01.md: ...的 KV cache，计算 attention，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency...
  c25_b01.md: ...| Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50...
  c25_b01.md: ...Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可能只用到 50% 计算和 50% 内存带宽：从未饱和任一。浪费巨大。  Mic...
  c25_b01.md: ...DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b02.md: ...2<br/>4× H100"]         end         subgraph CacheTier["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1...
  c25_b02.md: ...er["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         su...
  c25_b02.md: ...ke / LMCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tie...
  c25_b02.md: ...st"| P1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2...
  c25_b02.md: ...2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1...
  c25_b02.md: ...────┬───────┘  └──────┬───────┘                        │ KV cache         │ KV cache                        │ transfer...
  c25_b02.md: ...───┬───────┘                        │ KV cache         │ KV cache                        │ transfer         │ transfer...
  c25_b02.md: ...s 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：...
  c25_b02.md: ...ing prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保...
  c25_b02.md: ...Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优...
  c25_b02.md: ...de 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cach...
  c25_b02.md: ...个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过...
  c25_b02.md: ...ache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：...
  c25_b02.md: ...间：~10-100 ms（通过 200 Gbit/s NVLink 或 InfiniBand 的 RDMA）。  KV cache 传输是新的运维关注点：它消耗网络带宽并增加首 token 时间。处理得当，整体吞吐收益占主导。...
  c25_b02.md: ...2<br/>4× H100"]         end         subgraph CacheTier["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1...
  c25_b02.md: ...er["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         su...
  c25_b02.md: ...ke / LMCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tie...
  c25_b02.md: ...st"| P1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2...
  c25_b02.md: ...2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1...
  c25_b02.md: ...────┬───────┘  └──────┬───────┘                        │ KV cache         │ KV cache                        │ transfer...
  c25_b02.md: ...───┬───────┘                        │ KV cache         │ KV cache                        │ transfer         │ transfer...
  c25_b02.md: ...s 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：...
  c25_b02.md: ...ing prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保...
  c25_b02.md: ...Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优...
  c25_b02.md: ...de 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cach...
  c25_b02.md: ...个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过...
  c25_b02.md: ...ache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：...
  c25_b02.md: ...间：~10-100 ms（通过 200 Gbit/s NVLink 或 InfiniBand 的 RDMA）。  KV cache 传输是新的运维关注点：它消耗网络带宽并增加首 token 时间。处理得当，整体吞吐收益占主导。...
  c25_b03.md: ...ode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型；KV cache 传输逻辑；集群拓扑更复杂。对小集群，复杂度不值得；对大集群，收益值得。  2026 阈值：大约 50+ 节点是分离开始...
  c25_b03.md: ...26 阈值：大约 50+ 节点是分离开始划算的地方。低于此，运维开销超过效率收益。   ### 25.4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对...
  c25_b03.md: ...益。   ### 25.4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV...
  c25_b03.md: ...分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Moonc...
  c25_b03.md: ...文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在 2024 年论文中描述的推...
  c25_b03.md: ...。  Mooncake，Moonshot AI 在 2024 年论文中描述的推理系统，是 2026 年的典型实现。KV cache 以块形式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读...
  c25_b03.md: ...d 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node...
  c25_b03.md: ...开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cach...
  c25_b03.md: ...ache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一...
  c25_b03.md: ...独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被...
  c25_b03.md: ...层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被许多请求使用）被摊销；超长上下文无需单节点内存达到最大尺寸。  成本：缓存层网络带宽成...
  c25_b03.md: ...许多请求使用）被摊销；超长上下文无需单节点内存达到最大尺寸。  成本：缓存层网络带宽成为瓶颈；每 token 的 KV cache 传输必须非常快（100+ tokens/sec 需亚毫秒）。RDMA + 精心预取是让这可行的技术。...
  c25_b03.md: ...ode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型；KV cache 传输逻辑；集群拓扑更复杂。对小集群，复杂度不值得；对大集群，收益值得。  2026 阈值：大约 50+ 节点是分离开始...
  c25_b03.md: ...26 阈值：大约 50+ 节点是分离开始划算的地方。低于此，运维开销超过效率收益。   ### 25.4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对...
  c25_b03.md: ...益。   ### 25.4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV...
  c25_b03.md: ...分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Moonc...
  c25_b03.md: ...文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在 2024 年论文中描述的推...
  c25_b03.md: ...。  Mooncake，Moonshot AI 在 2024 年论文中描述的推理系统，是 2026 年的典型实现。KV cache 以块形式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读...
  c25_b03.md: ...d 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node...
  c25_b03.md: ...开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cach...
  c25_b03.md: ...ache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一...
  c25_b03.md: ...独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被...
  c25_b03.md: ...层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被许多请求使用）被摊销；超长上下文无需单节点内存达到最大尺寸。  成本：缓存层网络带宽成...
  c25_b03.md: ...许多请求使用）被摊销；超长上下文无需单节点内存达到最大尺寸。  成本：缓存层网络带宽成为瓶颈；每 token 的 KV cache 传输必须非常快（100+ tokens/sec 需亚毫秒）。RDMA + 精心预取是让这可行的技术。...
  c25_b04.md: ...30 个 decode 节点（每个 8x H200 = 240 张 H200）。针对内存带宽和容量优化。 * 10 个 cache 节点（Mooncake 风格缓存层；高带宽存储，RDMA 连接 serving）。 * 高带宽互联：每个 rack 内...
  c25_b04.md: ...请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Deco...
  c25_b04.md: ...点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token...
  c25_b04.md: ...de（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型 prompt（1000-5000...
  c25_b04.md: ...ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 KV cache（5-10 GB 经 RDMA）约 50 ms。 * 首 token 延迟：约 100-3...
  c25_b04.md: ...迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 KV cache（5-10 GB 经 RDMA）约 50 ms。 * 首 token 延迟：约 100-300 ms（prefill +...
  c25_b04.md: ...%。 * 年度节省：此前 1400 万美元年运行费中节省 400-500 万美元。  **运维关注点**：  * KV cache 传输是新瓶颈；带宽配置必须跟上。 * 故障模式：prefill 节点崩溃会让 in-flight 请求的 KV cac...
  c25_b04.md: ...che 传输是新瓶颈；带宽配置必须跟上。 * 故障模式：prefill 节点崩溃会让 in-flight 请求的 KV cache 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 pre...
  c25_b05.md: ...）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅新 token），并采样。这是矩阵-向量乘法。瓶颈是内存带宽（加载 KV cache）。...
  c25_b05.md: ...KV cache，计算 attention（仅新 token），并采样。这是矩阵-向量乘法。瓶颈是内存带宽（加载 KV cache）。  不对称意味着一张 GPU 同时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过...
  c25_b05.md: ...B200）。饱和内存带宽。  每个阶段 GPU 利用率接近 100%；集群整体利用率大幅提升。  成本是两阶段间 KV cache 传输（通常 50-100 ms）加架构复杂度。对大集群（50+ 节点），收益超过成本；对小集群不是。2026 年采用阈...
  c25_b05.md: ...0+ 节点），收益超过成本；对小集群不是。2026 年采用阈值大致是集群规模 50+。  **Q2.** 什么是 KV cache sharding，它解决什么问题？  提示  长上下文产生巨大 KV cache；跨多节点分布；缓存层抽象。  **答...
  c25_b05.md: ...+。  **Q2.** 什么是 KV cache sharding，它解决什么问题？  提示  长上下文产生巨大 KV cache；跨多节点分布；缓存层抽象。  **答案：** KV cache sharding 把单个请求的 KV cache 分布...
  c25_b05.md: ...g，它解决什么问题？  提示  长上下文产生巨大 KV cache；跨多节点分布；缓存层抽象。  **答案：** KV cache sharding 把单个请求的 KV cache 分布到多张 GPU（可能跨多个节点）。KV cache 被分成块（P...
  c25_b05.md: ...KV cache；跨多节点分布；缓存层抽象。  **答案：** KV cache sharding 把单个请求的 KV cache 分布到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAttention 风格），存储在集群级缓...
  c25_b05.md: ...：** KV cache sharding 把单个请求的 KV cache 分布到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAttention 风格），存储在集群级缓存层；decode 节点按需读取块。  它解决的问题：超...
  c25_b05.md: ...code 节点按需读取块。  它解决的问题：超长上下文（2026 年 1M+ token）每个请求产生数十 GB KV cache。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GPU 内存。  实现：  (a) **Moo...
  c25_b05.md: ...GPU 内存。  实现：  (a) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+...
  c25_b05.md: ...b) **LMCache**（UC Berkeley，2024）：开源等价物。为 vLLM 集成设计。提供集群级 KV cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache...
  c25_b05.md: ...cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt（system p...
  c25_b05.md: ...长上下文可行**：per-request KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt（system prompt、agent scaffold）共享缓存块。节省在多请求间摊销...
  c25_b05.md: ...agent scaffold）共享缓存块。节省在多请求间摊销。  (c) **Decode 节点可无状态**：按需读取 cache；KV cache 不由它们负责。这简化故障处理（decode 节点故障不丢失 KV cache）。  成本：  (a)...
  c25_b05.md: ...ffold）共享缓存块。节省在多请求间摊销。  (c) **Decode 节点可无状态**：按需读取 cache；KV cache 不由它们负责。这简化故障处理（decode 节点故障不丢失 KV cache）。  成本：  (a) **网络带宽**...
  c25_b05.md: ...可无状态**：按需读取 cache；KV cache 不由它们负责。这简化故障处理（decode 节点故障不丢失 KV cache）。  成本：  (a) **网络带宽**：每 token 的 KV cache 读取必须非常快（>100 tokens...
  c25_b05.md: ...理（decode 节点故障不丢失 KV cache）。  成本：  (a) **网络带宽**：每 token 的 KV cache 读取必须非常快（>100 tokens/sec 需亚毫秒）。需要 RDMA + 精心预取。  (b) **缓存层作为新...
  c25_b06.md: ...；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码；处理传输期间故障；与路由层协调。...
  c25_b06.md: ...ning and decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码；处理传输期间故障；与路由层协调。  (c) **更复杂的负载均衡**...
  c25_b06.md: ...efill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache 传输失败。每种都需处理。  对小集群（5-10 节点），运维开销占主导。效率收益（更好利用率）只转化为几个百分点的成本...
  c25_b06.md: ...更小集群可能不值得。  **Q4.** 一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型...
  c25_b06.md: ...*Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 toke...
  c25_b06.md: ...en prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 token 生成**：decode 节点上约...
  c25_b07.md: ...档分析、agent 运行），惩罚不可见（用户只看到最终输出）。  减少首 token 延迟的优化：  (a) **KV cache 预取**：prefill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把...
  c25_b08.md: ...50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由、KV cache 传输、故障处理。 * 用 mirror 生产流量模式的合成工作负载测试...
  c25_b08.md: ...4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由、KV cache 传输、故障处理。 * 用 mirror 生产流量模式的合成工作负载测试。 * 验证：延迟、吞吐、故障模式。  **Ph...
  c25_b08.md: ...部分离（5-6 个月）**  * 转换剩余节点。 * 最终拓扑：30 prefill + 30 decode + 10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-located 路径。  **Phase 4：...
  c25_b08.md: ...ase 4：调优与优化（7+ 个月）**  * 根据观测流量调整 prefill/decode 比例。 * 实现 KV cache 预取以减少首 token 延迟。 * 为常见 prompt 模式添加前缀复用逻辑。 * 监控任何回归；按需调整。  *...
  c25_b09.md: ...* Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum...
  c25_b09.md: ...hard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一...
  c25_b10.md: ...ireworks）已采用分离。对任何成本敏感部署，该技术是正确默认。  反驳：分离有真实运维复杂度（不同节点类型、KV cache 传输、更复杂编排）。对小部署（< 20 节点），运维开销超过效率收益。对没有推理专用平台工程师的 startup 团队...
  c25_b10.md: ...倍吞吐提升。 * 前缀共享（SGLang）：重复 prompt 工作负载 20-50% 降低。 * 超长上下文的 KV cache sharding：否则不可行的工作负载得以运行。  (b) **引擎优化**：  * 量化（8-bit 或 4-bit...
  c25_b11.md: ...系统（Mooncake、LMCache）实现这些模式。采用阈值：约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ tok...
  c25_b11.md: ...约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用。Mooncake（商业）和 LM...
  c25_b11.md: ...2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60% 到 85%；年节省 400-500 万美元。投入巨大；R...
  c25_b11.md: ...ulative decoding（draft model + verify model）作为多机协议；长度预测和 KV-cache-aware 路由。  ---...
  c26_b01.md: ...坏成本是用户看到 30 秒转圈等待。  本章讲 2024-2026 年出现的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculat...
  c26_b01.md: ...的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative decoding（用小 draft model 加速生成；可作为跨...
  c26_b01.md: ...八部分。Tobias 的 Continuum 集群完整优化故事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、speculative decoding（本...
  c26_b01.md: ...群完整优化故事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、speculative decoding（本章）。这些部分组合。   ### 26.1 LL...
  c26_b01.md: ...朴素负载均衡（round-robin、least-loaded）错过 LLM 推理特有的关键机会：  (a) **KV cache locality**：若请求 prompt 的前缀已在某节点缓存，路由到该节点可节省缓存部分的 prefill 成本。...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器...
  c26_b02.md: ...tem prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器...
  c26_b02.md: ...tem prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。...
  c26_b04.md: ...个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+...
  c26_b04.md: ...ed prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：spec...
  c26_b05.md: ...）；Web 请求通常 10-50 ms。错误路由决策的成本相应大得多。  具体 LLM 路由机会：  (a) **KV cache locality**：若某节点已缓存 prompt 前缀，路由到该节点节省 prefill 成本。路由别处浪费缓存。每...
  c26_b05.md: ...-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-aware 加长度预测路由，吞吐提升约 25%，p99 延迟降低约 30%。改动是算法级（无硬件改动）；收益巨大。...
  c26_b08.md: ...新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同...
  c26_b10.md: ...inuum 的优化冲刺不是可选；它是保持竞争力所必需。  **Q2.** 一个 LLM 被要求 “为我们集群实现 KV-cache-aware 路由”。它产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** L...
  c26_b10.md: ...prompt + 不同用户消息）。需要 radix tree 或类似前缀索引。  (b) **缓存新鲜度跟踪**。KV cache 会过期（内存压力、TTL）。路由器必须知道哪些缓存仍然有效；路由到缓存已被 evict 的节点无收益。  (c) **...
  c26_b11.md: ...在正确路由实现上的投资通过效率收益回报。   ### 本章总结  LLM 请求路由与 Web 请求路由有结构差异：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预...
  c26_b11.md: ...、模型可用性、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预测）带来显著效率收益。  KV-cache-aware 路由：负载均衡器维护缓存 KV cache 的前缀索引；把请求路由到有缓存的节点。对前缀重工作负载（sys...
  c26_b11.md: ...-cache-aware、长度预测）带来显著效率收益。  KV-cache-aware 路由：负载均衡器维护缓存 KV cache 的前缀索引；把请求路由到有缓存的节点。对前缀重工作负载（system prompt、agent scaffold、fu...
  c26_b11.md: ...eighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative dec...
  c26_b11.md: ...ing。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fai...
  c28_b07.md: ...欺诈检测推理是 sub-100-ms p99（p50 80 ms；与 §28.2 记录一致），得益于分离式架构和 KV-cache-aware 路由。集成只是一个简单 HTTP endpoint；Crescent 的 API 服务调用我们的 endp...
  c28_b10.md: ...LLM 推理工作负载 | 相同或更好延迟下降低 30-50% 成本 | prefill/decode 分离 + KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/...
  c28_b12.md: ...出下列原语分别由哪一章引入：HLC、fencing token、Raft、原子广播、CRDT、重试预算、混沌工程、KV-cache-aware 路由。  提示  各章大致按顺序引入原语；查看目录。  **答案：**  * **HLC**：第3章（时间...
  c28_b12.md: ...on）。 * **重试预算**：第21章（级联故障）。 * **混沌工程**：第23章（混沌作为学科）。 * **KV-cache-aware 路由**：第26章（LLM 推理负载均衡）。  每个原语解决三个谎言某一类表现。HLC 防御时间谎言；fe...
  c29_b02.md: ...体）和硬件（B100/B200 及下一代 Blackwell + Hopper 继任者）驱动。 * 通过更激进的 KV cache 预取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 *...
  c29_b02.md: ...特性不同于自回归 transformer，可能需要不同的分布式系统处理。  心智模型（GPU 是单元；两阶段计算；KV cache 是中心状态；严格 per-token 延迟）可推广到 MoE，很可能也到扩散-LM。具体原语（PagedAttenti...
  c30_b03.md: ...ggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (https://github.com/LMCache/LMCache). — 开源 KV...
  c30_b03.md: ...层。 * LMCache (https://github.com/LMCache/LMCache). — 开源 KV cache 层。 * Leviathan, Y., Kalman, M., & Matias, Y. (2023). "Fast...
  c30_b06.md: ...all-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **...
  c30_b11.md: ...理路径；成本优化是强制的。  **决策**：vLLM/SGLang + 分离式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；企业部署两者都通过 N...
  c30_b11.md: ...IDIA NIM。 * **架构**：集群超过约 50 节点后做 prefill/decode 分离（第25章）；KV cache 层（Mooncake / LMCache）用于前缀复用。 * **路由**：KV-cache-aware 负载均衡（第...
  c30_b11.md: ...分离（第25章）；KV cache 层（Mooncake / LMCache）用于前缀复用。 * **路由**：KV-cache-aware 负载均衡（第26章）加长度预测路由。 * **投机解码**：按模型为长输出工作负载启用。 * **监控**...
  c30_b11.md: ...*监控**：每客户 GPU 秒成本；每阶段利用率。 * **为何这个组合**：2025-2026 架构突破（分离、KV cache 层、投机解码）复合产生显著成本节省。 * **替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用...
  c30_b19.md: ...），Ch.4 §4.6   LLM 推理 trace 显示首 token 延迟 >300 ms | 分离式架构中 KV cache 传输成本 | Ch.25 §25.5（分离延迟）   LLM 推理 trace 显示长输出请求阻塞短请求 | 队头阻塞...

=== decode (179 occurrences) ===
  c06_b03.md: ...A 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）、应用必须使用 R...
  c06_b06.md: ...省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50...
  c06_b07.md: ...省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms 内生成。一次 50...
  c24_b01.md: ...自己的协议（PagedAttention、RadixAttention），自己的原语（KV cache、prefill、decode），以及自己的部署模式（disaggregated prefill/decode、KV cache sharding）。...
  c24_b01.md: ...原语（KV cache、prefill、decode），以及自己的部署模式（disaggregated prefill/decode、KV cache sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SG...
  c24_b01.md: ...GPU 之间移动，不亚于在节点之间。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memor...
  c24_b01.md: ...rompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV cache 作为中...
  c24_b06.md: ...GPU。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt，compute-bound）和 decode（生成 token，memory-bound）有不同最优硬件/软件 profile。独立优化每个阶段是 2025-202...
  c24_b06.md: ...独立子学科。协议（PagedAttention、RadixAttention）、原语（KV cache、prefill、decode）和部署模式（disaggregation、sharding）都是推理专用。  **Q2.** 描述 PagedAtte...
  c24_b08.md: ...Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若 Tobias...
  c24_b08.md: ...可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同...
  c24_b08.md: ...efill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发...
  c24_b08.md: ...ory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching...
  c24_b09.md: ...提出修复。  提示  队头阻塞。Continuous batching 有帮助；优先队列帮助更大；分离 prefill/decode 是架构修复。  **答案：** 双峰延迟是队头阻塞。长请求占用资源（GPU 内存、每步计算时间）；短请求在其后等待。...
  c24_b09.md: ...速处理；长请求 batch 按自己节奏处理。权衡：更难最优共享 GPU 资源。  (c) **分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 pref...
  c24_b09.md: ...**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请求大小**：把 p...
  c24_b09.md: ...l（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请求大小**：把 prompt 限制为 N token（...
  c24_b09.md: ...*Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous batching 可...
  c24_b09.md: ...ous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/decode（第25章）。架构改动大，但收益（消除双峰延迟）是用户可见的改善。...
  c24_b11.md: ...推理是 2026 年分布式系统的一个子学科。与典型微服务舰队的关键差异：GPU 是单元；两阶段计算（prefill + decode）；KV cache 是中心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAt...
  c24_b11.md: ...点利用率约每月 2 万美元。  第25章进入 2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬...
  c24_b11.md: ...gated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated prefill/de...
  c24_b11.md: ...de 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated prefill/decode + KV cache sharding 图。  ---...
  c25_b01.md: ...inuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（...
  c25_b01.md: ...80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非...
  c25_b01.md: .../decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-...
  c25_b01.md: ...prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上...
  c25_b01.md: ...GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Splitwise、D...
  c25_b01.md: ...plitwise、DistServe）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-to...
  c25_b01.md: ...。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）...
  c25_b01.md: ...atrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads...
  c25_b01.md: ...GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b01.md: ...inuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（...
  c25_b01.md: ...80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非...
  c25_b01.md: .../decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-...
  c25_b01.md: ...prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上...
  c25_b01.md: ...GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Splitwise、D...
  c25_b01.md: ...plitwise、DistServe）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-to...
  c25_b01.md: ...。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）...
  c25_b01.md: ...atrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads...
  c25_b01.md: ...GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b02.md: ...K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Nod...
  c25_b02.md: ...er["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H...
  c25_b02.md: ...D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H200"]         end         LB -->|"new reques...
  c25_b02.md: ...┌──────────────┐  ┌──────────────┐                 │  Decode      │  │  Decode      │                 │  Node 1      │...
  c25_b02.md: ...───┐  ┌──────────────┐                 │  Decode      │  │  Decode      │                 │  Node 1      │  │  Node 2      │...
  c25_b02.md: ...输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请...
  c25_b02.md: ...t 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。...
  c25_b02.md: ...的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV ca...
  c25_b02.md: ...为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：每个请求通常 1-10...
  c25_b02.md: ...K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Nod...
  c25_b02.md: ...er["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H...
  c25_b02.md: ...D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H200"]         end         LB -->|"new reques...
  c25_b02.md: ...┌──────────────┐  ┌──────────────┐                 │  Decode      │  │  Decode      │                 │  Node 1      │...
  c25_b02.md: ...───┐  ┌──────────────┐                 │  Decode      │  │  Decode      │                 │  Node 1      │  │  Node 2      │...
  c25_b02.md: ...输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请...
  c25_b02.md: ...t 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。...
  c25_b02.md: ...的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（HBM3、大内存）。 * 保存大量活跃 decode 请求的 KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV ca...
  c25_b02.md: ...为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：每个请求通常 1-10...
  c25_b03.md: ...两个复合收益：  (a) **每阶段硬件利用率更好**。Prefill 节点饱和 GPU 计算（只做计算重的工作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容...
  c25_b03.md: ...带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，...
  c25_b03.md: ...（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升...
  c25_b03.md: ...反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型；KV cac...
  c25_b03.md: ...4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 G...
  c25_b03.md: ...理系统，是 2026 年的典型实现。KV cache 以块形式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vL...
  c25_b03.md: ...Cache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode...
  c25_b03.md: ...decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并...
  c25_b03.md: ...只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被许多请求使用）被摊销；...
  c25_b03.md: ...两个复合收益：  (a) **每阶段硬件利用率更好**。Prefill 节点饱和 GPU 计算（只做计算重的工作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容...
  c25_b03.md: ...带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，...
  c25_b03.md: ...（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升...
  c25_b03.md: ...反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型；KV cac...
  c25_b03.md: ...4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 G...
  c25_b03.md: ...理系统，是 2026 年的典型实现。KV cache 以块形式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vL...
  c25_b03.md: ...Cache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode...
  c25_b03.md: ...decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并...
  c25_b03.md: ...只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共享。  收益：许多并发请求共享缓存层；KV cache 复用（同一 prompt 前缀被许多请求使用）被摊销；...
  c25_b04.md: ...* 30 个 prefill 节点（每个 4x H100 = 120 张 H100）。针对计算优化。 * 30 个 decode 节点（每个 8x H200 = 240 张 H200）。针对内存带宽和容量优化。 * 10 个 cache 节点（Mo...
  c25_b04.md: ...**请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 p...
  c25_b04.md: ...refill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV ca...
  c25_b04.md: ...ache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征...
  c25_b04.md: ...征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 K...
  c25_b04.md: ...复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集群 rollout...
  c25_b04.md: ...* 30 个 prefill 节点（每个 4x H100 = 120 张 H100）。针对计算优化。 * 30 个 decode 节点（每个 8x H200 = 240 张 H200）。针对内存带宽和容量优化。 * 10 个 cache 节点（Mo...
  c25_b04.md: ...**请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 p...
  c25_b04.md: ...refill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV ca...
  c25_b04.md: ...ache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征...
  c25_b04.md: ...征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 K...
  c25_b04.md: ...复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集群 rollout...
  c25_b05.md: ...#### 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Dec...
  c25_b05.md: ...ode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。G...
  c25_b05.md: ...prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅...
  c25_b05.md: ...时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill...
  c25_b05.md: ...0%）。  分离：  * **Prefill 节点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内存 GPU（H200、B100、B200）。饱和内存带宽。  每个阶段 GPU 利用率接...
  c25_b05.md: ...到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAttention 风格），存储在集群级缓存层；decode 节点按需读取块。  它解决的问题：超长上下文（2026 年 1M+ token）每个请求产生数十 GB KV cach...
  c25_b05.md: ...) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) *...
  c25_b05.md: ...che**（UC Berkeley，2024）：开源等价物。为 vLLM 集成设计。提供集群级 KV cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存...
  c25_b05.md: ...prompt（system prompt、agent scaffold）共享缓存块。节省在多请求间摊销。  (c) **Decode 节点可无状态**：按需读取 cache；KV cache 不由它们负责。这简化故障处理（decode 节点故障不丢失...
  c25_b05.md: ...亚毫秒）。需要 RDMA + 精心预取。  (b) **缓存层作为新故障模式**：缓存必须高可用；缓存故障影响所有读取 decode 节点。...
  c25_b05.md: ...#### 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Dec...
  c25_b05.md: ...ode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。G...
  c25_b05.md: ...prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅...
  c25_b05.md: ...时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill...
  c25_b05.md: ...0%）。  分离：  * **Prefill 节点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内存 GPU（H200、B100、B200）。饱和内存带宽。  每个阶段 GPU 利用率接...
  c25_b05.md: ...到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAttention 风格），存储在集群级缓存层；decode 节点按需读取块。  它解决的问题：超长上下文（2026 年 1M+ token）每个请求产生数十 GB KV cach...
  c25_b05.md: ...) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) *...
  c25_b05.md: ...che**（UC Berkeley，2024）：开源等价物。为 vLLM 集成设计。提供集群级 KV cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存...
  c25_b05.md: ...prompt（system prompt、agent scaffold）共享缓存块。节省在多请求间摊销。  (c) **Decode 节点可无状态**：按需读取 cache；KV cache 不由它们负责。这简化故障处理（decode 节点故障不丢失...
  c25_b05.md: ...亚毫秒）。需要 RDMA + 精心预取。  (b) **缓存层作为新故障模式**：缓存必须高可用；缓存故障影响所有读取 decode 节点。...
  c25_b06.md: ...）；对小集群，收益不占优。  **答案：** 分离有运维成本：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies...
  c25_b06.md: ...ning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码；处理传输期间故障；与路由层协调。  (c) **更复杂的负载均衡**：请求必须基于可用性分配到 (prefil...
  c25_b06.md: ...码；处理传输期间故障；与路由层协调。  (c) **更复杂的负载均衡**：请求必须基于可用性分配到 (prefill, decode) 对；分配可能需要 rebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区...
  c25_b06.md: ...ebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache 传输失败。每种都...
  c25_b06.md: ...跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache 传输失败。每种都需处理。  对小集群（5-10 节点），运维开销占主导。效率收益（更好利用率...
  c25_b06.md: ...阈值与工作负载相关：50 节点但 uniform 短 prompt 工作负载的集群可能不受益（prefill 成本相对 decode 小）。30 节点但重长 prompt 工作负载的集群可能受益（prefill 成本是瓶颈）。  Tobias 的 Co...
  c25_b06.md: ...。  **Q4.** 一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-to...
  c25_b06.md: ...GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 token 生成**：decode 节点上约 30 ms（一个 decode 步）。  (e) **首 token 总计**：约 185 ms（网络 +...
  c25_b06.md: ...code 步）。  (e) **首 token 总计**：约 185 ms（网络 + prefill + 传输 + 首 decode）。  (f) **后续每 token**：约 30 ms。  (g) **200 输出 token 总计**：约 18...
  c25_b06.md: ....2 秒。  对 co-located（未分离）架构，首 token 延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-token 延迟类似。分离版为首 token 多付约 50 ms（传输）；吞吐收益来自集群整体更...
  c25_b07.md: ...少首 token 延迟的优化：  (a) **KV cache 预取**：prefill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chas...
  c25_b07.md: ...ill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**...
  c25_b08.md: ...1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由、KV cache 传输、故障处理。 * 用 mirror 生...
  c25_b08.md: ...**Phase 3：全部分离（5-6 个月）**  * 转换剩余节点。 * 最终拓扑：30 prefill + 30 decode + 10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-located 路径。...
  c25_b08.md: ...o-located 路径。  **Phase 4：调优与优化（7+ 个月）**  * 根据观测流量调整 prefill/decode 比例。 * 实现 KV cache 预取以减少首 token 延迟。 * 为常见 prompt 模式添加前缀复用逻辑。...
  c25_b08.md: ...些时段以短 prompt 长输出为主。分离如何帮助，运维关注点是什么？  提示  分离允许独立扩缩 prefill 和 decode；可变性成为机会。运维关注点：rebalance。  **答案：** 有了分离，prefill 和 decode 容量可...
  c25_b08.md: ...l 和 decode；可变性成为机会。运维关注点：rebalance。  **答案：** 有了分离，prefill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile：  * 长 prompt 时段：多扩 prefill（comp...
  c25_b08.md: ...当前工作负载 profile：  * 长 prompt 时段：多扩 prefill（compute-bound）并少扩 decode（memory-bound，短输出不需要那么多）。 * 短 prompt 长输出时段：多扩 decode（需要更多内存和...
  c25_b08.md: ...und）并少扩 decode（memory-bound，短输出不需要那么多）。 * 短 prompt 长输出时段：多扩 decode（需要更多内存和 per-token 吞吐）并少扩 prefill。  没有分离（co-located）时，GPU 同时...
  c25_b08.md: ...久）。若工作负载在几分钟内从长 prompt 变为短 prompt：  * prefill 容量过度配置（闲置）。 * decode 容量不足（饱和）。 * 用户可见 decode 延迟上升。  缓解：...
  c25_b08.md: ...为短 prompt：  * prefill 容量过度配置（闲置）。 * decode 容量不足（饱和）。 * 用户可见 decode 延迟上升。  缓解：...
  c25_b09.md: ...如专用节点，但提供灵活性。  (d) **基于工作负载感知指标的自动扩缩**：根据每阶段指标（prefill 队列深度、decode token 吞吐）而非聚合指标扩缩。  对 Continuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两...
  c25_b09.md: ...需要 4+ GPU；405B 需要 16+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每...
  c25_b09.md: ...ill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输...
  c25_b09.md: ...r parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 mode...
  c25_b09.md: ...U。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段...
  c25_b09.md: ...如专用节点，但提供灵活性。  (d) **基于工作负载感知指标的自动扩缩**：根据每阶段指标（prefill 队列深度、decode token 吞吐）而非聚合指标扩缩。  对 Continuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两...
  c25_b09.md: ...需要 4+ GPU；405B 需要 16+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每...
  c25_b09.md: ...ill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输...
  c25_b09.md: ...r parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 mode...
  c25_b09.md: ...U。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段...
  c25_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“Disaggregated prefill/decode 是 2026 年任何生产推理部署的默认。Co-located 是遗留方案。”  **讨论：** 辩护：分离在典型工作负...
  c25_b10.md: ...。  完整答案包括：  (a) **架构优化**（不降低模型质量）：  * Disaggregated prefill/decode（第25章）：30-50% 成本降低。 * Continuous batching（第24章）：若尚未使用，2-5 倍吞...
  c25_b10.md: ...*：  * 更新 GPU（H200、B100、B200）：每美元更好内存带宽或计算。 * 混合部署（prefill 与 decode 用不同 GPU）。 * 对非关键工作负载用 spot/preemptible 实例。  (d) **工作负载优化**：...
  c25_b11.md: ...们针对你的具体 setup 确定下一步最高杠杆。”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是...
  c25_b11.md: ...ll/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优硬件；相同延迟下吞吐提升 2-4...
  c25_b11.md: ...）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60% 到 85%；年节省 400-...
  c26_b01.md: ...fill 成本。路由别处则浪费缓存。  (b) **长度预测**：请求的预期输出长度影响正确路由。长输出请求应去能持续 decode 的节点；短输出请求可去任何地方。  (c) **模型可用性**：许多集群服务多个模型。路由必须考虑哪些节点加载了哪些模...
  c26_b02.md: ...输出请求**：可去忙碌的节点（边际负载短暂）。节点很快完成短请求并再次空闲。  (b) **长输出请求**：应去有持续 decode 容量的节点。把长请求集中到少数节点可防止其他节点的尾部延迟放大。  实现：  (a) 一个轻量级长度预测器（可本身是小...
  c26_b02.md: ...(c) 误预测：若预测错误（本应短却长，或相反），请求 mid-flight 迁移到更合适节点（KV cache 传输；decode 在新节点恢复）。  误预测是运维关注点。80% 准确度的长度预测器已能带来大部分收益；完美不可实现。迁移机制处理其余。...
  c26_b03.md: ...decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=...
  c26_b03.md: ...” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model...
  c26_b03.md: ...oken（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被...
  c26_b04.md: ...tinuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀...
  c26_b04.md: ...路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多...
  c26_b04.md: ...tinuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀...
  c26_b04.md: ...路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多...
  c26_b05.md: ...省 prefill 成本。路由别处浪费缓存。每次请求可节省数百毫秒。  (b) **长度预测**：长输出请求应去有持续 decode 容量的节点。把长请求集中到少数节点可防止其他节点尾部延迟放大。  (c) **模型可用性**：集群服务多个模型；路由必...
  c26_b05.md: ...Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型...
  c26_b05.md: ...(b) Verify model 并行处理 K 个候选（一个 verify 步一起处理 K 个 token）。这与正常 decode 步成本大致相同。  (c) Verify model 检查 K 个候选中与其自身预测匹配的前缀。匹配前缀被接受；第一个...
  c26_b05.md: ...model 的选择。  (d) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative...
  c26_b05.md: ...产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接...
  c26_b08.md: ...量高（如 100 rps），单节点缓存无法服务所有。缓存层（Mooncake/LMCache，第25章）保存前缀；多个 decode 节点从缓存读取。  (c) **长度预测路由**：中等输出请求（200-500 token）去能支持约 10 秒 de...
  c26_b08.md: ...de 节点从缓存读取。  (c) **长度预测路由**：中等输出请求（200-500 token）去能支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出...
  c26_b08.md: ...的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5...
  c26_b08.md: ...ecoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5 秒。  (e) **客户 SLA 调度**：该客户是 tier-2（中优先级）。Weig...
  c26_b08.md: ...表明严重尾部延迟。可能原因：  (a) **长输出请求阻塞短请求**：单个 5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **...
  c26_b08.md: ...*：需要重试的请求更慢；若重试无预算，尾部可延长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefil...
  c26_b08.md: ...) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...5章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b09.md: ...节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部感知负载均衡器**：监控 per-node 延迟；若某...
  c26_b10.md: ...对使用 Mooncake/LMCache 的集群，第25章）：缓存层是共享的；任何节点可读。路由器应考虑这点：基于哪个 decode 节点有容量来路由，然后该节点从缓存层读取。  (e) **客户亲和性**：对 workload 可预测的客户（相同 s...
  c28_b10.md: ...| 每月 100 万美元以上 LLM 推理工作负载 | 相同或更好延迟下降低 30-50% 成本 | prefill/decode 分离 + KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新...
  c28_b10.md: ...层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/decode failover |  每个工作流都可作为独立小手册阅读：输入、机制、输出、陷阱。它们共同覆盖了 2026 年分布式系...
  c29_b02.md: ...将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟 10-50 ms；典型工作负载首 token 延迟 100-300 ms。  2027-2030 轨迹：  *...
  c30_b06.md: ...冲突复制数据类型）**：无论操作顺序如何都能确定性合并的数据结构。 * **Disaggregated prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 d...
  c30_b06.md: ...何都能确定性合并的数据结构。 * **Disaggregated prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU...
  c30_b06.md: ...e（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被...
  c30_b11.md: ...敏感的交互路径 + 批处理路径；成本优化是强制的。  **决策**：vLLM/SGLang + 分离式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；...
  c30_b11.md: ...重工作负载）；企业部署两者都通过 NVIDIA NIM。 * **架构**：集群超过约 50 节点后做 prefill/decode 分离（第25章）；KV cache 层（Mooncake / LMCache）用于前缀复用。 * **路由**：KV-...

=== prefill (171 occurrences) ===
  c06_b03.md: ...oCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）...
  c06_b06.md: ...ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms...
  c06_b07.md: ...ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV cache，每个 token 必须在 ~10-50 ms...
  c24_b01.md: ...一个子学科。它有自己的协议（PagedAttention、RadixAttention），自己的原语（KV cache、prefill、decode），以及自己的部署模式（disaggregated prefill/decode、KV cache sha...
  c24_b01.md: ...ion），自己的原语（KV cache、prefill、decode），以及自己的部署模式（disaggregated prefill/decode、KV cache sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：...
  c24_b01.md: ...点可能有 8 张 GPU；推理工作负载在 GPU 之间移动，不亚于在节点之间。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-b...
  c24_b01.md: ...个请求的两阶段计算**。Prefill（处理 prompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的...
  c24_b03.md: ...大节省**：对具有长共享 system prompt 的工作负载（生产 LLM 应用中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill...
  c24_b03.md: ...rompt 的工作负载（生产 LLM 应用中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成*...
  c24_b03.md: ...prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成**：SGLang 原生支持结构化输出（JSON schema、regex 约束...
  c24_b06.md: ...载在 GPU 之间移动不亚于在节点之间。微服务舰队关注节点；推理关注 GPU。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt，compute-bound）和 decode（生成 token，memory-bound）有不同最...
  c24_b06.md: ...LLM 推理视为独立子学科。协议（PagedAttention、RadixAttention）、原语（KV cache、prefill、decode）和部署模式（disaggregation、sharding）都是推理专用。  **Q2.** 描述 Pa...
  c24_b08.md: ...响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若...
  c24_b08.md: ...负载（客服机器人、agent scaffold），SGLang 的 RadixAttention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利...
  c24_b08.md: ...到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decod...
  c24_b08.md: ...(c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 p...
  c24_b08.md: ...ute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。...
  c24_b08.md: ...响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若...
  c24_b08.md: ...负载（客服机器人、agent scaffold），SGLang 的 RadixAttention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利...
  c24_b08.md: ...到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decod...
  c24_b08.md: ...(c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 p...
  c24_b08.md: ...ute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。...
  c24_b09.md: ...塞短请求。诊断并提出修复。  提示  队头阻塞。Continuous batching 有帮助；优先队列帮助更大；分离 prefill/decode 是架构修复。  **答案：** 双峰延迟是队头阻塞。长请求占用资源（GPU 内存、每步计算时间）；短请求...
  c24_b09.md: ...请求 batch 快速处理；长请求 batch 按自己节奏处理。权衡：更难最优共享 GPU 资源。  (c) **分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数...
  c24_b09.md: ...：更难最优共享 GPU 资源。  (c) **分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 d...
  c24_b09.md: ...code（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请...
  c24_b09.md: ...n prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请求大小**：把 prompt 限制为 N token（足够小以不阻塞其他请求）。拒绝更...
  c24_b09.md: ...：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/decode（第25章）。架构改动大，但收益（消除双峰延迟）是用户可见的改善。...
  c24_b11.md: ...间巨大；每个百分点利用率约每月 2 万美元。  第25章进入 2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个...
  c24_b11.md: ...2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 di...
  c24_b11.md: ...und decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated prefill/decode + KV cache sharding 图。  ---...
  c25_b01.md: ...60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和...
  c25_b01.md: ...isaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bou...
  c25_b01.md: ...。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认...
  c25_b01.md: ...阶段优化的不同 GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Spli...
  c25_b01.md: ...成为经典研究（Splitwise、DistServe）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对...
  c25_b01.md: ...-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 toke...
  c25_b01.md: ...Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV...
  c25_b01.md: ...和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b01.md: ...e 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b01.md: ...60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和...
  c25_b01.md: ...isaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bou...
  c25_b01.md: ...。  这个洞察事后看很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认...
  c25_b01.md: ...阶段优化的不同 GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Spli...
  c25_b01.md: ...成为经典研究（Splitwise、DistServe）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对...
  c25_b01.md: ...-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 toke...
  c25_b01.md: ...Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV...
  c25_b01.md: ...和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b01.md: ...e 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b02.md: ...LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node...
  c25_b02.md: ...ier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4×...
  c25_b02.md: ...P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4× H100"]         end         subgraph CacheTier...
  c25_b02.md: ...┌──────────────┐  ┌──────────────┐                 │  Prefill     │  │  Prefill     │                 │  Node 1      │  │...
  c25_b02.md: ...───┐  ┌──────────────┐                 │  Prefill     │  │  Prefill     │                 │  Node 1      │  │  Node 2      │...
  c25_b02.md: ...ed   │                 └──────────────┘  └──────────────┘   Prefill 节点：  * 针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。...
  c25_b02.md: ...* 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（H...
  c25_b02.md: ...。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVL...
  c25_b02.md: ...每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassi...
  c25_b02.md: ...LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node...
  c25_b02.md: ...ier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4×...
  c25_b02.md: ...P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4× H100"]         end         subgraph CacheTier...
  c25_b02.md: ...┌──────────────┐  ┌──────────────┐                 │  Prefill     │  │  Prefill     │                 │  Node 1      │  │...
  c25_b02.md: ...───┐  ┌──────────────┐                 │  Prefill     │  │  Prefill     │                 │  Node 1      │  │  Node 2      │...
  c25_b02.md: ...ed   │                 └──────────────┘  └──────────────┘   Prefill 节点：  * 针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。...
  c25_b02.md: ...* 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  Decode 节点：  * 针对内存带宽和容量优化（H...
  c25_b02.md: ...。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVL...
  c25_b02.md: ...每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassi...
  c25_b03.md: ...### 25.3 为什么分离获胜  两个复合收益：  (a) **每阶段硬件利用率更好**。Prefill 节点饱和 GPU 计算（只做计算重的工作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未...
  c25_b03.md: ...作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩...
  c25_b03.md: ...容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefi...
  c25_b03.md: ...efill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型...
  c25_b03.md: ...### 25.3 为什么分离获胜  两个复合收益：  (a) **每阶段硬件利用率更好**。Prefill 节点饱和 GPU 计算（只做计算重的工作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未...
  c25_b03.md: ...作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b) **独立扩缩**。Prefill 容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩...
  c25_b03.md: ...容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefi...
  c25_b03.md: ...efill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型...
  c25_b04.md: ...12：Continuum 的分离式部署  Tobias 的 80% 利用率目标计划：  **架构**：  * 30 个 prefill 节点（每个 4x H100 = 120 张 H100）。针对计算优化。 * 30 个 decode 节点（每个 8x...
  c25_b04.md: ...00 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. K...
  c25_b04.md: ...求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode...
  c25_b04.md: ...e 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从...
  c25_b04.md: ...token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 tok...
  c25_b04.md: ...00-500 万美元。  **运维关注点**：  * KV cache 传输是新瓶颈；带宽配置必须跟上。 * 故障模式：prefill 节点崩溃会让 in-flight 请求的 KV cache 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑...
  c25_b04.md: ...che 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集...
  c25_b04.md: ...12：Continuum 的分离式部署  Tobias 的 80% 利用率目标计划：  **架构**：  * 30 个 prefill 节点（每个 4x H100 = 120 张 H100）。针对计算优化。 * 30 个 decode 节点（每个 8x...
  c25_b04.md: ...00 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. K...
  c25_b04.md: ...求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode...
  c25_b04.md: ...e 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层）。   5. Decode 节点生成输出 token；每 token 从...
  c25_b04.md: ...token；每 token 从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 tok...
  c25_b04.md: ...00-500 万美元。  **运维关注点**：  * KV cache 传输是新瓶颈；带宽配置必须跟上。 * 故障模式：prefill 节点崩溃会让 in-flight 请求的 KV cache 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑...
  c25_b04.md: ...che 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trace 必须跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集...
  c25_b05.md: ...#### 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩...
  c25_b05.md: ...# 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。...
  c25_b05.md: ...-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是...
  c25_b05.md: ...是内存带宽（加载 KV cache）。  不对称意味着一张 GPU 同时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利...
  c25_b05.md: ...对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill 节点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内...
  c25_b05.md: ...#### 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩...
  c25_b05.md: ...# 概念检测  **Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。...
  c25_b05.md: ...-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是...
  c25_b05.md: ...是内存带宽（加载 KV cache）。  不对称意味着一张 GPU 同时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利...
  c25_b05.md: ...对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill 节点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内...
  c25_b06.md: ...型、传输逻辑、集群复杂度）；对小集群，收益不占优。  **答案：** 分离有运维成本：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scali...
  c25_b06.md: ...decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码；处理传输期间故障；与路由层协调。  (c) **更复杂的负载均衡**：请求必须基于可用性...
  c25_b06.md: ...对；分配可能需要 rebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache...
  c25_b06.md: ...*：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache 传输失败。每种都需处理。  对小集群（5-10 节点），运维开销占主...
  c25_b06.md: ...6 个月分离投资值得；更小集群可能不值得。  **Q4.** 一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  *...
  c25_b06.md: ...00-token 输出）的延迟组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b) **Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输...
  c25_b06.md: ...节点上约 30 ms（一个 decode 步）。  (e) **首 token 总计**：约 185 ms（网络 + prefill + 传输 + 首 decode）。  (f) **后续每 token**：约 30 ms。  (g) **200 输出...
  c25_b06.md: ...型、传输逻辑、集群复杂度）；对小集群，收益不占优。  **答案：** 分离有运维成本：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scali...
  c25_b06.md: ...decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码；处理传输期间故障；与路由层协调。  (c) **更复杂的负载均衡**：请求必须基于可用性...
  c25_b06.md: ...对；分配可能需要 rebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache...
  c25_b06.md: ...*：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工作。  (e) **故障模式**：prefill 中途失败；decode 生成期间失败；cache 传输失败。每种都需处理。  对小集群（5-10 节点），运维开销占主...
  c25_b06.md: ...6 个月分离投资值得；更小集群可能不值得。  **Q4.** 一个请求流过分离式架构。列出延迟组件和近似值。  提示  Prefill、KV cache 传输、decode per token。各自贡献首 token 和 per-token 延迟。  *...
  c25_b06.md: ...00-token 输出）的延迟组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b) **Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输...
  c25_b06.md: ...节点上约 30 ms（一个 decode 步）。  (e) **首 token 总计**：约 185 ms（网络 + prefill + 传输 + 首 decode）。  (f) **后续每 token**：约 30 ms。  (g) **200 输出...
  c25_b07.md: ...行），惩罚不可见（用户只看到最终输出）。  减少首 token 延迟的优化：  (a) **KV cache 预取**：prefill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 dec...
  c25_b07.md: ...预取**：prefill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) *...
  c25_b07.md: ...输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请求），跳过该部分 prefill。  Tobias 的部署使用 (a) 和 (c)；典型首 token 延迟约 150 ms，接近 co-located...
  c25_b08.md: ...**Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由、KV cache 传输、故障处...
  c25_b08.md: ...指标确认收益，继续转换。  **Phase 3：全部分离（5-6 个月）**  * 转换剩余节点。 * 最终拓扑：30 prefill + 30 decode + 10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-...
  c25_b08.md: ...。 * 退役 co-located 路径。  **Phase 4：调优与优化（7+ 个月）**  * 根据观测流量调整 prefill/decode 比例。 * 实现 KV cache 预取以减少首 token 延迟。 * 为常见 prompt 模式添加...
  c25_b08.md: ...n）短输出为主；另一些时段以短 prompt 长输出为主。分离如何帮助，运维关注点是什么？  提示  分离允许独立扩缩 prefill 和 decode；可变性成为机会。运维关注点：rebalance。  **答案：** 有了分离，prefill 和 d...
  c25_b08.md: ...立扩缩 prefill 和 decode；可变性成为机会。运维关注点：rebalance。  **答案：** 有了分离，prefill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile：  * 长 prompt 时段：多扩 pre...
  c25_b08.md: ...ill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile：  * 长 prompt 时段：多扩 prefill（compute-bound）并少扩 decode（memory-bound，短输出不需要那么多）。 * 短 promp...
  c25_b08.md: ...需要那么多）。 * 短 prompt 长输出时段：多扩 decode（需要更多内存和 per-token 吞吐）并少扩 prefill。  没有分离（co-located）时，GPU 同时做两个阶段；扩缩是一维的。集群对当前工作负载要么计算过多，要么内存...
  c25_b08.md: ...（云 GPU 置备需 5-15 分钟，本地更久）。若工作负载在几分钟内从长 prompt 变为短 prompt：  * prefill 容量过度配置（闲置）。 * decode 容量不足（饱和）。 * 用户可见 decode 延迟上升。  缓解：...
  c25_b09.md: ...集群两阶段都保持 15% headroom；基于 per-phase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多...
  c25_b09.md: ...ama 3 70B 需要 4+ GPU；405B 需要 16+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Pref...
  c25_b09.md: ...fill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Dec...
  c25_b09.md: ...：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节...
  c25_b09.md: ...从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 m...
  c25_b09.md: ...集群两阶段都保持 15% headroom；基于 per-phase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多...
  c25_b09.md: ...ama 3 70B 需要 4+ GPU；405B 需要 16+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Pref...
  c25_b09.md: ...fill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Dec...
  c25_b09.md: ...：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节...
  c25_b09.md: ...从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 m...
  c25_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“Disaggregated prefill/decode 是 2026 年任何生产推理部署的默认。Co-located 是遗留方案。”  **讨论：** 辩护：分...
  c25_b10.md: ...衡或架构替代方案。  完整答案包括：  (a) **架构优化**（不降低模型质量）：  * Disaggregated prefill/decode（第25章）：30-50% 成本降低。 * Continuous batching（第24章）：若尚未使用...
  c25_b11.md: ...处于什么位置？我们针对你的具体 setup 确定下一步最高杠杆。”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；de...
  c25_b11.md: ...”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到...
  c25_b11.md: ...前缀复用。Mooncake（商业）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60%...
  c25_b11.md: ...处于什么位置？我们针对你的具体 setup 确定下一步最高杠杆。”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；de...
  c25_b11.md: ...”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到...
  c25_b11.md: ...前缀复用。Mooncake（商业）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60%...
  c26_b01.md: ...**KV cache locality**：若请求 prompt 的前缀已在某节点缓存，路由到该节点可节省缓存部分的 prefill 成本。路由别处则浪费缓存。  (b) **长度预测**：请求的预期输出长度影响正确路由。长输出请求应去能持续 deco...
  c26_b02.md: ...择最长匹配前缀的节点；把请求路由过去。  (d) 所选节点服务请求，利用缓存前缀。只有后缀被重新计算。  收益：  * prefill 成本大幅节省（缓存前缀 “免费”）。 * 首 token 延迟降低（更少 prefill 工作）。 * 集群吞吐更高（...
  c26_b04.md: ...整优化  Continuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-awar...
  c26_b05.md: ...路由机会：  (a) **KV cache locality**：若某节点已缓存 prompt 前缀，路由到该节点节省 prefill 成本。路由别处浪费缓存。每次请求可节省数百毫秒。  (b) **长度预测**：长输出请求应去有持续 decode 容量...
  c26_b08.md: ...t。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）...
  c26_b08.md: ...到同一节点（或有缓存的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b)...
  c26_b08.md: ...的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**...
  c26_b08.md: ...只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**：若该客户流量高（如 100 rps），单节点缓存无法服务...
  c26_b08.md: ...nuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggregation（分离 prefill 节点）修复此问题。...
  c26_b08.md: ...抖动或瞬态故障**：需要重试的请求更慢；若重试无预算，尾部可延长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在...
  c26_b08.md: ...长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...gated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...t。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）...
  c26_b08.md: ...到同一节点（或有缓存的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b)...
  c26_b08.md: ...的节点）。  收益：5000-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**...
  c26_b08.md: ...只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**：若该客户流量高（如 100 rps），单节点缓存无法服务...
  c26_b08.md: ...nuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggregation（分离 prefill 节点）修复此问题。...
  c26_b08.md: ...抖动或瞬态故障**：需要重试的请求更慢；若重试无预算，尾部可延长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在...
  c26_b08.md: ...长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...gated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c26_b08.md: ...decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefill 请求隔离在 prefill 节点；decode 不受影响。...
  c28_b10.md: ...25 §25.5 | 每月 100 万美元以上 LLM 推理工作负载 | 相同或更好延迟下降低 30-50% 成本 | prefill/decode 分离 + KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓...
  c28_b10.md: ...KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/decode failover |  每个工作流都可作为独立小手册阅读：输入、机制、输出、陷阱。它们共同覆盖了 202...
  c30_b03.md: ...离论文。 * Zhong, Y., et al. (2024). "DistServe: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. *...
  c30_b06.md: ...**CRDT（无冲突复制数据类型）**：无论操作顺序如何都能确定性合并的数据结构。 * **Disaggregated prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计...
  c30_b06.md: ...d prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing token（围栏令牌）**：锁...
  c30_b11.md: ...n；多模型；延迟敏感的交互路径 + 批处理路径；成本优化是强制的。  **决策**：vLLM/SGLang + 分离式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀...
  c30_b11.md: ...GLang（前缀重工作负载）；企业部署两者都通过 NVIDIA NIM。 * **架构**：集群超过约 50 节点后做 prefill/decode 分离（第25章）；KV cache 层（Mooncake / LMCache）用于前缀复用。 * **路...

=== follower (157 occurrences) ===
  c02_b10.md: ...线性化的三种操作或配置，并解释何时重要。  提示  CockroachDB 有 `AS OF SYSTEM TIME`、follower 读，旧版本的默认 `SNAPSHOT` 隔离级别比 serializable 弱。每个都有一个非线性化角落。  **答...
  c02_b10.md: ...额读。  (b) 通过 `AS OF SYSTEM TIME follower_read_timestamp()` 的 follower 读从最近副本做有界过期读。与上述权衡相同。适用于全球低延迟读取缓慢变化的数据。  (c) 事务优先级与 `CONCUR...
  c02_b11.md: ...何时重要：任何应用逻辑假设“我看到的是最新数据”的读，都必须使用 leader 上的新鲜读，而不是 follower 读或 `AS OF SYSTEM TIME`。在需要线性化读的地方使用过期读，是一类默认线性化系统也不会自动防止的 b...
  c05_b02.md: ...传播给 _followers_（或称 replicas、secondaries）。读可以走 leader（保一致）或 follower（扩规模，但可能过期）。  用于：PostgreSQL、MySQL、MongoDB（replica sets）、Cock...
  c05_b04.md: ...开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a) **将用户读在写后的一段时间内路由到 leader。** 某些会话中间件跟踪“该用户 30...
  c05_b04.md: ...最小 LSN”。** 客户端知道刚写入记录的 LSN 为 12345，后续读包含 “LSN >= 12345”。落后的 follower 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB 和 YugabyteDB 通过 per-tra...
  c05_b04.md: ...跟踪见过的最大 HLC；同一会话的后续读使用至少该 HLC。这是现代运维模式。   ### 5.9 五种会话保证  当 follower 可服务读时，应用可从 per-session 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等...
  c05_b06.md: ...#### 概念检测  **Q1.** 一个团队用异步复制从主库到一个 follower 做灾难恢复。主库在写传播前死亡。走查团队会看到什么以及恢复如何进行。  提示  写丢失。选项取决于如何检测丢失以及 R...
  c05_b06.md: ...**答案：** 团队会观察到：  (a) 发起写的应用收到成功响应，因为异步复制在传播前 ack。(b) 被提升的 follower 没有该写。(c) 任何观察过该写的客户端（read-your-writes 或下游影响）现在看到的状态与它以为的不一致...
  c05_b06.md: ...sed：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB replica sets、Co...
  c05_b09.md: ...**Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max-HLC-observed。读请求带“最小所需 HLC”。服务器要么等待...
  c05_b09.md: ...return read_local(key, at_hlc=server.local_hlc)   当客户端从落后的 follower 读时：该 follower 的 `local_hlc` 小于 `min_hlc`。处理器要么等待（直到复制把它带到最新...
  c05_b09.md: ...ocal(key, at_hlc=server.local_hlc)   当客户端从落后的 follower 读时：该 follower 的 `local_hlc` 小于 `min_hlc`。处理器要么等待（直到复制把它带到最新）要么拒绝（返回 “stal...
  c06_b06.md: ...定工作负载可更快。  (c) 接受 85 ms 并调整预算。对人类可见操作，5 ms 通常不重要。  (d) 读操作用 follower 读（无需 quorum），让写保持 quorum 成本，但减少读的感知多区域延迟。  **Q4.** 为什么 RDMA...
  c06_b08.md: ...er commit-wait（~7 ms）加 quorum 往返；跨大陆 quorum 约 200 ms 提交。读可用 follower 区域做有界过期读。500 ms 预算满足；成本是 Spanner 基础设施开销。  推荐：大多数聊天应用选 (b)。异...
  c06_b08.md: ...案：** 三个副本的 Raft：quorum 是任意 2 of 3。leader 自己算一个 ack；条目在 _最快_ follower 响应后提交。因此 US-East 发起的写，提交延迟是到其他两个区域中较快者的往返。系统级最坏情况是所有可能发起区域中...
  c06_b08.md: ...0 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。 * US-Central 发起：最快 follower = US-East，3...
  c06_b08.md: ...-East 发起：最快 follower = US-Central，30 ms。 * US-Central 发起：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower = US-Central，50 m...
  c06_b08.md: ...US-Central 发起：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower = US-Central，50 ms。...
  c06_b09.md: ...t ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。...
  c06_b09.md: ...* US-East 发起：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US-East，200 ms。...
  c06_b09.md: ...ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US-East，200 ms。  最坏情况 Raft 提交：**~200 ms**（Tokyo 发起）。DR 更好...
  c06_b09.md: ...ast-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。...
  c06_b09.md: ...* US-East 发起：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower = US-East，80 ms。...
  c06_b09.md: ...。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower = US-East，80 ms。  最坏情况 Raft 提交：**~90 ms**（EU-West 发起）。跨两洲；单...
  c07_b03.md: ...tion_ 论证，出现在每个共识协议中。Paxos 选择一个多数 acceptor 已接受的值。Raft 选择一个多数 follower 已复制的日志条目。HotStuff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certific...
  c07_b08.md: ...00-2000 ms 对 500 ms 跨 DC 抖动来说紧。  **答案：** 诊断：网络抖动期间 leader 到 follower 的心跳耗时超过预期。若心跳延迟 500 ms，而选举超时 1000 ms，在心跳到达前 follower 的定时器就可...
  c07_b08.md: ...ader 到 follower 的心跳耗时超过预期。若心跳延迟 500 ms，而选举超时 1000 ms，在心跳到达前 follower 的定时器就可能触发选举。选举成功（因为旧 leader 心跳仍在延迟），集群就有了新 leader。  这次新选举不必...
  c07_b08.md: ...（因为旧 leader 心跳仍在延迟），集群就有了新 leader。  这次新选举不必要，因为旧 leader 还活着；follower 只是无法判断。  调整：  (a) 将选举超时增加到 5000-10000 ms。这样可承受 500-1000 ms...
  c07_b08.md: ...障最坏检测时间变长。  (c) 使用 Raft 的 “pre-vote” 扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 foll...
  c07_b08.md: ...ower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre-vote（消除误怀疑扰动）并把选举超时调到约 5 倍观察到的最坏跨 DC...
  c08_b08.md: ...个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多...
  c08_b08.md: ...能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ball...
  c08_b08.md: ...allot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对...
  c08_b08.md: ...afety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot 正在使用，下台成为 follower。...
  c08_b08.md: ...个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多...
  c08_b08.md: ...能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ball...
  c08_b08.md: ...allot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对...
  c08_b08.md: ...afety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot 正在使用，下台成为 follower。...
  c09_b01.md: ...）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，...
  c09_b02.md: ...### 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机...
  c09_b02.md: ...r Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机在 150 ms 到 300 ms 之间），它变为 _c...
  c09_b02.md: ...ate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两个 candidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重...
  c09_b02.md: ...### 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机...
  c09_b02.md: ...r Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太久未收到心跳（_election timeout_，通常随机在 150 ms 到 300 ms 之间），它变为 _c...
  c09_b02.md: ...ate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两个 candidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重...
  c09_b03.md: ...。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `AppendEntries` RPC 给所有 follower，包含新条目及上下文（前一个日志索引和 term）。   3. 一旦多数 follower 复制该条目，将其标记为 _c...
  c09_b03.md: ...tries` RPC 给所有 follower，包含新条目及上下文（前一个日志索引和 term）。   3. 一旦多数 follower 复制该条目，将其标记为 _committed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `...
  c09_b03.md: ...一旦多数 follower 复制该条目，将其标记为 _committed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `current...
  c09_b03.md: ...4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIndex` 处没有条目或该...
  c09_b03.md: ...ries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIndex` 处没有条目或该条目的 term 不是 `prevLogTerm`，拒绝。这是 _...
  c09_b03.md: ...目或该条目的 term 不是 `prevLogTerm`，拒绝。这是 _consistency check_ ；它确保 follower 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit ind...
  c09_b03.md: ...ommit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   ####...
  c09_b03.md: ...日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什么  若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（...
  c09_b03.md: ...aft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什么  若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。...
  c09_b03.md: ...若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直...
  c09_b03.md: ...ex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然...
  c09_b03.md: ...到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发...
  c09_b03.md: ...ader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）...
  c09_b03.md: ...簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft...
  c09_b03.md: ...tIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 le...
  c09_b03.md: ...。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `AppendEntries` RPC 给所有 follower，包含新条目及上下文（前一个日志索引和 term）。   3. 一旦多数 follower 复制该条目，将其标记为 _c...
  c09_b03.md: ...tries` RPC 给所有 follower，包含新条目及上下文（前一个日志索引和 term）。   3. 一旦多数 follower 复制该条目，将其标记为 _committed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `...
  c09_b03.md: ...一旦多数 follower 复制该条目，将其标记为 _committed_ 并应用到状态机。   4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `current...
  c09_b03.md: ...4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIndex` 处没有条目或该...
  c09_b03.md: ...ries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIndex` 处没有条目或该条目的 term 不是 `prevLogTerm`，拒绝。这是 _...
  c09_b03.md: ...目或该条目的 term 不是 `prevLogTerm`，拒绝。这是 _consistency check_ ；它确保 follower 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit ind...
  c09_b03.md: ...ommit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   ####...
  c09_b03.md: ...日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什么  若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（...
  c09_b03.md: ...aft 确保日志在集群中 _只追加_ 且 _一致_ 地增长。   #### Follower 日志落后时发生什么  若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。...
  c09_b03.md: ...若 follower 日志在 `prevLogIndex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直...
  c09_b03.md: ...ex` 处没有条目（太短），或包含不同条目（term 不匹配），follower 拒绝。Leader 收到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然...
  c09_b03.md: ...到拒绝后，将该 follower 的 `nextIndex` 减一并用更早条目重试。如此继续，直到 leader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发...
  c09_b03.md: ...ader 找到 follower 日志匹配点，然后追加所有后续条目。  簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）...
  c09_b03.md: ...簿记：leader 维护 `nextIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft...
  c09_b03.md: ...tIndex[follower]`（下一个要发送的条目）和 `matchIndex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 le...
  c09_b04.md: ...快照包含的最后索引和 term。 * 当前集群配置。  日志落后太多、无法通过 `AppendEntries` 追赶的 follower 通过 `InstallSnapshot` RPC 接收快照。Follower 用快照替换自己的状态并恢复。  运维考虑...
  c09_b04.md: ...`AppendEntries` 追赶的 follower 通过 `InstallSnapshot` RPC 接收快照。Follower 用快照替换自己的状态并恢复。  运维考虑：快照必须与状态机一致。许多实现的 bug 是：快照在某一时刻拍摄，但快照生成...
  c09_b04.md: ...快照包含的最后索引和 term。 * 当前集群配置。  日志落后太多、无法通过 `AppendEntries` 追赶的 follower 通过 `InstallSnapshot` RPC 接收快照。Follower 用快照替换自己的状态并恢复。  运维考虑...
  c09_b04.md: ...`AppendEntries` 追赶的 follower 通过 `InstallSnapshot` RPC 接收快照。Follower 用快照替换自己的状态并恢复。  运维考虑：快照必须与状态机一致。许多实现的 bug 是：快照在某一时刻拍摄，但快照生成...
  c09_b06.md: ...、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所...
  c09_b06.md: ...replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引...
  c09_b06.md: ...接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都...
  c09_b06.md: ...leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递增且全序。结构作用相似...
  c09_b06.md: ...e 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 Raft 随机化选举超时？  提示  减少多个 follower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 followe...
  c09_b06.md: ...ollower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投票，平分选票（没人获多数）。集...
  c09_b06.md: ...*答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投票，平分选票（没人获多数）。集群重复选举，很可能再次平分。  随机化超时（例如...
  c09_b06.md: ...随机化超时（例如 150 ms 到 300 ms 均匀随机）让同时 candidate 在统计上不可能。几轮后，一个 follower 的定时器显著提前触发，它在其他人定时器触发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 lead...
  c09_b06.md: ...超时量级（生产通常几百毫秒）。  **Q4.** 描述 AppendEntries 一致性检查。它保证什么？  提示  Follower 必须在与 leader 记录匹配的前一条目处才追加新条目。...
  c09_b06.md: ...、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所...
  c09_b06.md: ...replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引...
  c09_b06.md: ...接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都...
  c09_b06.md: ...leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递增且全序。结构作用相似...
  c09_b06.md: ...e 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 Raft 随机化选举超时？  提示  减少多个 follower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 followe...
  c09_b06.md: ...ollower 同时触发选举时的平分选票。  **答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投票，平分选票（没人获多数）。集...
  c09_b06.md: ...*答案：** 若选举超时固定为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投票，平分选票（没人获多数）。集群重复选举，很可能再次平分。  随机化超时（例如...
  c09_b06.md: ...随机化超时（例如 150 ms 到 300 ms 均匀随机）让同时 candidate 在统计上不可能。几轮后，一个 follower 的定时器显著提前触发，它在其他人定时器触发前获胜。  这个随机化是细小但重要的实践细节。没有它，Raft 的 lead...
  c09_b06.md: ...超时量级（生产通常几百毫秒）。  **Q4.** 描述 AppendEntries 一致性检查。它保证什么？  提示  Follower 必须在与 leader 记录匹配的前一条目处才追加新条目。...
  c09_b07.md: ...(term, prevLogIndex, prevLogTerm, entries, leaderCommit)` 时，follower 执行一致性检查：  (a) 若 `term < currentTerm`，拒绝（leader 过期）。  (b) 若...
  c09_b07.md: ...执行一致性检查：  (a) 若 `term < currentTerm`，拒绝（leader 过期）。  (b) 若 follower 日志在 `prevLogIndex` 处没有条目，拒绝（follower 太短）。  (c) 若 follower 在...
  c09_b07.md: ...若 follower 日志在 `prevLogIndex` 处没有条目，拒绝（follower 太短）。  (c) 若 follower 在 `prevLogIndex` 处的日志条目 term 不是 `prevLogTerm`，拒绝（follower 日...
  c09_b07.md: ...erCommit` 推进 commit index，回复成功。  这保证：一次成功 `AppendEntries` 后，follower 日志在新条目末尾之前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，l...
  c09_b07.md: ...前与 leader 日志一致。集群各节点的日志结构通过这种增量检查保持一致。  一致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配前缀，然后追加所有后续条目...
  c09_b07.md: ...致性检查拒绝时，leader 递减该 follower 的 `nextIndex` 并用更早条目重试。Leader 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 leader 日志的机制。...
  c09_b07.md: ...用更早条目重试。Leader 和 follower 收敛到匹配前缀，然后追加所有后续条目。  该协议是落后或分区恢复后 follower 赶上 leader 日志的机制。...
  c09_b08.md: ...应用问题  **Q1.** Anya 正在实现 Stratus DB 复制层。她有一个 3 节点 Raft 集群。一个 follower 与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示...
  c09_b08.md: ...有一个 3 节点 Raft 集群。一个 follower 与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower...
  c09_b08.md: ...follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：...
  c09_b08.md: ...；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：** Follower 重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达...
  c09_b08.md: ...重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日...
  c09_b08.md: ...tries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日志缺少 `prevLogIndex` 处条目（落后 100...
  c09_b08.md: ...向分区前追踪的条目。  (b) Follower 日志缺少 `prevLogIndex` 处条目（落后 1000 条）。Follower 拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `...
  c09_b08.md: ...处条目（落后 1000 条）。Follower 拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含...
  c09_b08.md: ...er 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextInde...
  c09_b08.md: ...]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，le...
  c09_b08.md: ...Index` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批...
  c09_b08.md: ...加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 100 条，追赶网络耗时约 500 ms，加上 fo...
  c09_b08.md: ...er 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 100 条，追赶网络耗时约 500 ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `Ins...
  c09_b08.md: ...RPC 100 条，追赶网络耗时约 500 ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换...
  c09_b08.md: ...日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 etcd / TiKV / 基...
  c09_b08.md: ...应用问题  **Q1.** Anya 正在实现 Stratus DB 复制层。她有一个 3 节点 Raft 集群。一个 follower 与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示...
  c09_b08.md: ...有一个 3 节点 Raft 集群。一个 follower 与集群分区 30 分钟；期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower...
  c09_b08.md: ...follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：...
  c09_b08.md: ...；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：** Follower 重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达...
  c09_b08.md: ...重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日...
  c09_b08.md: ...tries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日志缺少 `prevLogIndex` 处条目（落后 100...
  c09_b08.md: ...向分区前追踪的条目。  (b) Follower 日志缺少 `prevLogIndex` 处条目（落后 1000 条）。Follower 拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `...
  c09_b08.md: ...处条目（落后 1000 条）。Follower 拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含...
  c09_b08.md: ...er 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextInde...
  c09_b08.md: ...]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，le...
  c09_b08.md: ...Index` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批...
  c09_b08.md: ...加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 100 条，追赶网络耗时约 500 ms，加上 fo...
  c09_b08.md: ...er 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 100 条，追赶网络耗时约 500 ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `Ins...
  c09_b08.md: ...RPC 100 条，追赶网络耗时约 500 ms，加上 follower 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换...
  c09_b08.md: ...日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 etcd / TiKV / 基...
  c09_b09.md: ...中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b) 若原 leader 在少数侧，多数侧 follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得投票（多数），成为 leader。新...
  c09_b09.md: ...拿不到多数（它只有自己加可能一个可达节点；总共 2 < 3）。写阻塞。  (b) 若原 leader 在多数侧，少数侧 follower 等待心跳。它们定时器触发。其中一个成为 candidate，向少数侧另一节点请求投票。它得到 2 票（自己和另一节点）...
  c09_b09.md: ...败或返回过期数据。  _恢复：_  * 分区愈合后，少数侧节点在多数侧心跳中发现更高 term。它们 revert 为 follower，同步日志到多数侧，重新加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Ra...
  c09_b10.md: ...默 safety 违反）：  (d) **AppendEntries 一致性检查 off-by-one。** 检查比较 follower 在 `prevLogIndex` 处的条目与 `prevLogTerm`。索引或 term 比较差一都会让不一致日志保...
  c10_b10.md: ...可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff pipelining_ ：HotStuff 每个决定有三个投票阶段（pre...
  c11_b01.md: ...* **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：CockroachDB、TiDB、Yuga...
  c11_b03.md: ...（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停止接受写 PACELC...
  c11_b05.md: ...t 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serializable 事务；follower 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC | PC / EC Str...
  c11_b07.md: ...数秒内重新启动，存储层独立处理副本故障）。一致性模型为强一致 serializable；读可走 leader（即时）或 follower（有界过期）。分区行为在存储层为 CP（丢失存储法定人数时写阻塞）。PACELC：分区下 PC，无分区 EC（存储层法定...
  c14_b07.md: ...秒） Consistency model | Linearizable；提供过期读 | Serializable；提供 follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestam...
  c17_b01.md: ...3.6 起）：客户端跟踪见过的最高操作时间戳；同会话后续读要求服务器至少赶上该时间戳。 * **CockroachDB follower reads** ：从 follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with e...
  c17_b01.md: ...间戳；同会话后续读要求服务器至少赶上该时间戳。 * **CockroachDB follower reads** ：从 follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with explicit per-session...
  c17_b08.md: ...码必须处理轻微过期读。  (d) **切换到具有 HLC-aware 多区域语义的系统**：CockroachDB 的 follower reads 或 YugabyteDB 的类似特性；读可从更近副本做有界过期，无需因果会话等待。  对 MongoDB，...
  c18_b09.md: ...对 Stratus 的具体架构，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “...
  c20_b02.md: ...次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的 Raft 库支持 context 传播；...
  c27_b04.md: ...可串行化（serializable）；读可以在 leader（per-operation linearizable）或 follower（bounded-staleness）。这段代码中的查询，没有显式配置，可能线性化也可能不是，取决于连接设置。带 `AS...
  c27_b04.md: ...段代码中的查询，没有显式配置，可能线性化也可能不是，取决于连接设置。带 `AS OF SYSTEM TIME` 或通过 follower-read 设置的读可能返回旧数据。  修复：每个操作显式一致性选择。       def get_account_ba...
  c27_b12.md: ...istency” 暗示读是线性化的；没有显式 `WITH (LINEARIZABLE)` 或类似，读可能来自有旧数据的 follower。  Olamide 的审查反馈：“拒绝。这段代码包含 Thread C 全部六个谎言。具体：bank_service....
  c28_b06.md: ...EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100%...
  c28_b10.md: ...py quorum | | 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求 → leader → follower | 复制状态机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派提交 | append...
  c30_b04.md: ...| 每 range 一个 Raft；HLC + 偏移 | Raft 重新选举；每 range 亚秒级 | 可串行化 + follower read | CP | PC/EC | 多 region Postgres 兼容；任意云 | | **DynamoDB...
  c30_b19.md: ...批评），Ch.27 §27.5   Trace 显示事务提交但读之后返回旧值 | 写后读失败；从没有 HLC 传播的 follower 读 | Ch.5 §5.8（写后读），Ch.15 §15.2（session 保证）   Trace 显示两次写入成功...

=== saga (157 occurrences) ===
  c13_b02.md: ...XA 脆弱。现代系统使用 _saga 模式_（本章 §13.6.5 展开）：每一步独立提交；失败触发补偿操作而非回滚。saga 是应用层；数据库层的跨组织 2PC 已消失。...
  c13_b05.md: ...ike 能力带给云用户；未来几年可能看到 CockroachDB / TiDB 采用此路径。   ### 13.6.5 Saga：跨服务的 2PC 替代方案  §13.2-§13.6 的一切都假设事务 _发生在同一分布式数据库内部_。2PC + 复...
  c13_b05.md: ...、隔夜银行结算）。 * 没有共享事务基础设施来实现 “prepared 但未提交”。  对跨服务工作流，现代模式是 **saga**。...
  c13_b05.md: ...ike 能力带给云用户；未来几年可能看到 CockroachDB / TiDB 采用此路径。   ### 13.6.5 Saga：跨服务的 2PC 替代方案  §13.2-§13.6 的一切都假设事务 _发生在同一分布式数据库内部_。2PC + 复...
  c13_b05.md: ...、隔夜银行结算）。 * 没有共享事务基础设施来实现 “prepared 但未提交”。  对跨服务工作流，现代模式是 **saga**。...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺序执行已提交步骤的...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺序执行已提交步骤的补偿动作。系统从不在服务边界上持有锁；每一步提交都在本地持久化；整个系统最终收敛到 “所有...
  c13_b06.md: ...在服务边界上持有锁；每一步提交都在本地持久化；整个系统最终收敛到 “所有步骤成功” 或 “早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持...
  c13_b06.md: ...到 “所有步骤成功” 或 “早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orche...
  c13_b06.md: ...“早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身...
  c13_b06.md: ...调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复制或存在事务型 DB...
  c13_b06.md: ...estrator 状态本身持久化（通常 Raft 复制或存在事务型 DB）。  (b) **Choreographed saga** ：每一步完成时发布事件；后续步骤订阅事件。补偿通过 “回滚事件” 触发。无中央 orchestrator；步骤通过...
  c13_b06.md: ...” 触发。无中央 orchestrator；步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻...
  c13_b06.md: ...rated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻辑，但端到端调试更困难。...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺序执行已提交步骤的...
  c13_b06.md: ...#### Saga 结构  Saga 是一系列本地事务，每一步独立提交，并为每一步定义 _补偿动作_。若后续步骤失败，saga 会按相反顺序执行已提交步骤的补偿动作。系统从不在服务边界上持有锁；每一步提交都在本地持久化；整个系统最终收敛到 “所有...
  c13_b06.md: ...在服务边界上持有锁；每一步提交都在本地持久化；整个系统最终收敛到 “所有步骤成功” 或 “早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持...
  c13_b06.md: ...到 “所有步骤成功” 或 “早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orche...
  c13_b06.md: ...“早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身...
  c13_b06.md: ...调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复制或存在事务型 DB...
  c13_b06.md: ...estrator 状态本身持久化（通常 Raft 复制或存在事务型 DB）。  (b) **Choreographed saga** ：每一步完成时发布事件；后续步骤订阅事件。补偿通过 “回滚事件” 触发。无中央 orchestrator；步骤通过...
  c13_b06.md: ...” 触发。无中央 orchestrator；步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻...
  c13_b06.md: ...rated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻辑，但端到端调试更困难。...
  c13_b07.md: ...pensations for N-1, N-2, ..., 1   单一 orchestrator 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步...
  c13_b07.md: ...orchestrator 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步，最后动作 LedgerService.record，状态：in_pro...
  c13_b07.md: ...tor 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步，最后动作 LedgerService.record，状态：in_progress。” 完整...
  c13_b07.md: ...arketplace 订单处理” 子系统使用舞蹈化（不同团队拥有服务，独立演进）。两种模式在同一公司共存。  编排支付 saga 可视化：       sequenceDiagram         autonumber         parti...
  c13_b08.md: ...nsation idempotent** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs 2PC：何时选谁  Scenario | 正确模式 ---|--- 同一分布式数据库内跨分片 | 2PC + 复...
  c13_b08.md: ...kroachDB / FoundationDB 模式） 同一团队栈内跨服务、短生命周期 | 2PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本...
  c13_b08.md: ...B 模式） 同一团队栈内跨服务、短生命周期 | 2PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | S...
  c13_b08.md: ...PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工...
  c13_b08.md: ...a（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一...
  c13_b08.md: ...格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC...
  c13_b08.md: ...| Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC   #### Crescent 的混合方案  Cres...
  c13_b08.md: ...身被 Raft 复制）。 * **跨服务** ：涉及银行结算 API、风控 LLM、通知服务、审计日志归档的支付是一个 saga。Orchestrator（一个由 CockroachDB 保存状态的小服务）依次调用每一步，并为每一步定义补偿。  两...
  c13_b08.md: ...一步定义补偿。  两种模式共存；各自适用于自己的范围。数据库内跨分片原子性 → 2PC。含外部参与者的跨服务工作流 → saga。...
  c13_b08.md: ...nsation idempotent** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs 2PC：何时选谁  Scenario | 正确模式 ---|--- 同一分布式数据库内跨分片 | 2PC + 复...
  c13_b08.md: ...kroachDB / FoundationDB 模式） 同一团队栈内跨服务、短生命周期 | 2PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本...
  c13_b08.md: ...B 模式） 同一团队栈内跨服务、短生命周期 | 2PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | S...
  c13_b08.md: ...PC + 复制协调者，或 saga；选择取决于运维 跨团队跨服务 | Saga（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工...
  c13_b08.md: ...a（编排或舞蹈化） 跨组织（多家公司） | Saga；XA 风格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一...
  c13_b08.md: ...格 2PC 基本已被弃用 长周期工作流（分钟到小时） | Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC...
  c13_b08.md: ...| Saga；2PC 持锁在运维上不可行 含人工审批的工作流 | Saga；人无法参与 2PC 中间可见性可接受 | Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC   #### Crescent 的混合方案  Cres...
  c13_b08.md: ...身被 Raft 复制）。 * **跨服务** ：涉及银行结算 API、风控 LLM、通知服务、审计日志归档的支付是一个 saga。Orchestrator（一个由 CockroachDB 保存状态的小服务）依次调用每一步，并为每一步定义补偿。  两...
  c13_b08.md: ...一步定义补偿。  两种模式共存；各自适用于自己的范围。数据库内跨分片原子性 → 2PC。含外部参与者的跨服务工作流 → saga。...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用户资金（Cock...
  c13_b09.md: ...易已取消” 通知 6 | 更新商家分析聚合 | 减去该笔贡献  若第 5 步失败（通知服务宕机），第 1-4 步已提交。Saga 有两种有效恢复：(a) 带退避重试第 5 步（接受短暂通知延迟）；(b) 触发第 1-4 步补偿。选择取决于用户通知对...
  c13_b09.md: ...用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，带时间戳与 trace...
  c13_b09.md: ...rescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，带时间戳与 trace ID）本身存在分析仓库（第14章）中，以满足合规。  ---...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用户资金（Cock...
  c13_b09.md: ...易已取消” 通知 6 | 更新商家分析聚合 | 减去该笔贡献  若第 5 步失败（通知服务宕机），第 1-4 步已提交。Saga 有两种有效恢复：(a) 带退避重试第 5 步（接受短暂通知延迟）；(b) 触发第 1-4 步补偿。选择取决于用户通知对...
  c13_b09.md: ...用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，带时间戳与 trace...
  c13_b09.md: ...rescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，带时间戳与 trace ID）本身存在分析仓库（第14章）中，以满足合规。  ---...
  c13_b15.md: ...**Q3.** 一个团队为微服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提...
  c13_b15.md: ...**Q3.** 一个团队为微服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用...
  c13_b15.md: ...服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：...
  c13_b15.md: ...与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：** Saga 模式：事务被拆成一系列本地事务，每一步独立提交...
  c13_b15.md: ...步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：** Saga 模式：事务被拆成一系列本地事务，每一步独立提交。若后续步骤失败，_补偿动作_ 撤销前面步骤。系统从不在服务边界上持锁。...
  c13_b15.md: ...不在服务边界上持锁。  2PC：事务在所有参与者间保持，直到全部投 YES，然后原子提交。协议期间锁跨服务边界持有。  Saga 是正确答案的情况：  * **跨服务事务** ：事务跨不同团队拥有或不同存储技术的服务。2PC 需要共享事务基础设施；...
  c13_b15.md: ...是正确答案的情况：  * **跨服务事务** ：事务跨不同团队拥有或不同存储技术的服务。2PC 需要共享事务基础设施；saga 不需要。 * **长周期事务** ：事务耗时分钟到小时（订机票涉及预订、支付、出票，可能耗时）。2PC 持锁数小时在运...
  c13_b15.md: ...预订、支付、出票，可能耗时）。2PC 持锁数小时在运维上不可行。 * **松耦合** ：服务不应需要知道彼此的提交状态。saga 的补偿是应用层；2PC 要求参与者实现 prepared 状态。  2PC 是正确答案的情况：  * **同一数据库内...
  c13_b15.md: ...chDB），2PC + 复制协调者是正确协议。 * **需要强原子性** ：不允许中间状态被其他读者看到。2PC 提供；saga 不提供（每一步本地提交可见）。 * **短事务** ：事务在毫秒级完成，持锁可接受。  2026 年现实：分布式数据库...
  c13_b15.md: ...短事务** ：事务在毫秒级完成，持锁可接受。  2026 年现实：分布式数据库内部用 2PC（效果好）；跨微服务事务用 saga（2PC 太脆弱）。两种模式常在同一应用共存：订单处理服务在数据库内用 2PC（订单 + 库存原子更新），跨服务用 sa...
  c13_b15.md: ...ga（2PC 太脆弱）。两种模式常在同一应用共存：订单处理服务在数据库内用 2PC（订单 + 库存原子更新），跨服务用 saga（支付服务、物流服务、通知服务）。...
  c13_b15.md: ...**Q3.** 一个团队为微服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提...
  c13_b15.md: ...**Q3.** 一个团队为微服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用...
  c13_b15.md: ...服务架构实现 saga 模式（跨服务分布式事务，带补偿动作）。与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：...
  c13_b15.md: ...与 2PC 比较。何时 saga 是正确答案？  提示  Saga：每一步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：** Saga 模式：事务被拆成一系列本地事务，每一步独立提交...
  c13_b15.md: ...步独立提交；失败触发补偿。2PC：所有步骤原子。saga 用于跨服务；2PC 用于同一服务内跨分片。  **答案：** Saga 模式：事务被拆成一系列本地事务，每一步独立提交。若后续步骤失败，_补偿动作_ 撤销前面步骤。系统从不在服务边界上持锁。...
  c13_b15.md: ...不在服务边界上持锁。  2PC：事务在所有参与者间保持，直到全部投 YES，然后原子提交。协议期间锁跨服务边界持有。  Saga 是正确答案的情况：  * **跨服务事务** ：事务跨不同团队拥有或不同存储技术的服务。2PC 需要共享事务基础设施；...
  c13_b15.md: ...是正确答案的情况：  * **跨服务事务** ：事务跨不同团队拥有或不同存储技术的服务。2PC 需要共享事务基础设施；saga 不需要。 * **长周期事务** ：事务耗时分钟到小时（订机票涉及预订、支付、出票，可能耗时）。2PC 持锁数小时在运...
  c13_b15.md: ...预订、支付、出票，可能耗时）。2PC 持锁数小时在运维上不可行。 * **松耦合** ：服务不应需要知道彼此的提交状态。saga 的补偿是应用层；2PC 要求参与者实现 prepared 状态。  2PC 是正确答案的情况：  * **同一数据库内...
  c13_b15.md: ...chDB），2PC + 复制协调者是正确协议。 * **需要强原子性** ：不允许中间状态被其他读者看到。2PC 提供；saga 不提供（每一步本地提交可见）。 * **短事务** ：事务在毫秒级完成，持锁可接受。  2026 年现实：分布式数据库...
  c13_b15.md: ...短事务** ：事务在毫秒级完成，持锁可接受。  2026 年现实：分布式数据库内部用 2PC（效果好）；跨微服务事务用 saga（2PC 太脆弱）。两种模式常在同一应用共存：订单处理服务在数据库内用 2PC（订单 + 库存原子更新），跨服务用 sa...
  c13_b15.md: ...ga（2PC 太脆弱）。两种模式常在同一应用共存：订单处理服务在数据库内用 2PC（订单 + 库存原子更新），跨服务用 saga（支付服务、物流服务、通知服务）。...
  c13_b16.md: ...2PC 事务可能持续数秒到数分钟（每个服务做自身验证、网络调用等）。跨服务持锁那么久会导致其他事务排队，产生级联变慢。Saga 模式更适合跨服务工作流。  再三条（任一条都是生产 bug）：...
  c13_b17.md: ...观察：跨服务 2PC 在 2026 年微服务架构中很少是正确模式。LLM 为 “跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 saga；LLM 生成 2PC 反映的是旧教科书里突出的内容...
  c13_b17.md: ...LM 为 “跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 saga；LLM 生成 2PC 反映的是旧教科书里突出的内容，而非当前最佳实践。   ### 本章总结  分布式事务是跨分片、跨...
  c13_b17.md: ...域，serializable 隔离且完整 ACID 保证。协议成熟；运维调优（局部性、分片、重启预算）决定生产性能。  Saga 模式（跨服务补偿动作，§13.6.5）是跨微服务 “事务” 的正确模式；2PC 是分布式数据库内跨分片的正确模式。编排...
  c13_b17.md: ...偿动作，§13.6.5）是跨微服务 “事务” 的正确模式；2PC 是分布式数据库内跨分片的正确模式。编排 vs 舞蹈化 saga；正确性义务（独立可提交、幂等、可补偿、补偿幂等）；saga-vs-2PC 决策矩阵；Crescent 的混合使用都在...
  c13_b17.md: ...PC 是分布式数据库内跨分片的正确模式。编排 vs 舞蹈化 saga；正确性义务（独立可提交、幂等、可补偿、补偿幂等）；saga-vs-2PC 决策矩阵；Crescent 的混合使用都在 §13.6.5 展开。两种模式在现代应用中共存。  第14章...
  c13_b17.md: ...观察：跨服务 2PC 在 2026 年微服务架构中很少是正确模式。LLM 为 “跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 saga；LLM 生成 2PC 反映的是旧教科书里突出的内容...
  c13_b17.md: ...LM 为 “跨微服务事务” 生成 2PC 而未问及 saga、最终一致或异步补偿，说明它跳过了架构问题。正确答案通常是 saga；LLM 生成 2PC 反映的是旧教科书里突出的内容，而非当前最佳实践。   ### 本章总结  分布式事务是跨分片、跨...
  c13_b17.md: ...域，serializable 隔离且完整 ACID 保证。协议成熟；运维调优（局部性、分片、重启预算）决定生产性能。  Saga 模式（跨服务补偿动作，§13.6.5）是跨微服务 “事务” 的正确模式；2PC 是分布式数据库内跨分片的正确模式。编排...
  c13_b17.md: ...偿动作，§13.6.5）是跨微服务 “事务” 的正确模式；2PC 是分布式数据库内跨分片的正确模式。编排 vs 舞蹈化 saga；正确性义务（独立可提交、幂等、可补偿、补偿幂等）；saga-vs-2PC 决策矩阵；Crescent 的混合使用都在...
  c13_b17.md: ...PC 是分布式数据库内跨分片的正确模式。编排 vs 舞蹈化 saga；正确性义务（独立可提交、幂等、可补偿、补偿幂等）；saga-vs-2PC 决策矩阵；Crescent 的混合使用都在 §13.6.5 展开。两种模式在现代应用中共存。  第14章...
  c14_b09.md: ...stgres 兼容、细粒度控制。缺点：大量应用改动（分片感知查询、跨分片 join 变成应用层）、跨分片事务需要应用层 saga 或 2PC、运维复杂度倍增。  _选项 2：Aurora DSQL_。迁移到 AWS 托管 NewSQL。优点：ser...
  c17_b03.md: ...的线性化需求。  优点：每个系统针对其工作负载优化；架构分离清晰。缺点：跨系统一致性需要应用逻辑；跨系统 “事务” 用 saga 模式。  2026 趋势：初创公司小用模式 A（运维简单）；大系统用模式 B（per-component 优化）。两者...
  c27_b04.md: ...n_service.notify()` 之前，通知未发出。系统处于不一致状态。  修复：识别哪些调用对一致性关键，并用 saga 模式（第13章）或 transactional outbox。       def update_user_acros...
  c27_b06.md: ...中账户余额不能为负的规则？” 不变量存在于产品知识中，不在代码模式里。 * **跨 PR 一致性**：“这个 PR 的 saga 补偿需要与同一工作流 PR #4729 中已建立的补偿匹配。” 跨 PR 记忆在 2026 AI 审查者中薄弱。 *...
  c27_b06.md: ...R #4729 中已建立的补偿匹配。” 跨 PR 记忆在 2026 AI 审查者中薄弱。 * **架构判断**：“这是 saga；它是 _正确_ 的 saga 吗？应该用 2PC 吗？” 模式选择是上下文判断；AI 审查者倾向于接受选择并审查实现，...
  c27_b06.md: ...匹配。” 跨 PR 记忆在 2026 AI 审查者中薄弱。 * **架构判断**：“这是 saga；它是 _正确_ 的 saga 吗？应该用 2PC 吗？” 模式选择是上下文判断；AI 审查者倾向于接受选择并审查实现，而非质疑设计。 * **新模式...
  c27_b07.md: ...若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game...
  c27_b07.md: ...y 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证——步骤 2-5 的 saga 补偿已定义；orchestrator 状态经 Ra...
  c27_b07.md: ...partner-onboarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证——步骤 2-5 的 saga 补偿已定义；orchestrator 状态经 Raft 复制；步骤 4（外部 partner API）有 0.1 重试...
  c27_b09.md: ...cing token。    5. **部分故障罕见**：代码只有 happy path；不考虑 “半成功” 结果；无 saga、无补偿动作。    6. **CAP 分类简单**：代码说 “我们用 CP 系统” 或 “我们用 AP 系统”，而不理...
  c27_b12.md: ...time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5）；在误导性 CP 注释下读一致性模糊（谎言 6）。请用团队设计文档的纪律重新设计；不要提交未应用检查清单的...
  c27_b12.md: ...中新分布式代码 bug 罕见。  **Q3.** 一个 LLM 生成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？s...
  c27_b12.md: ...成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？saga 状态是否持久？...
  c27_b12.md: ...a 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？saga 状态是否持久？...
  c27_b12.md: ...time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5）；在误导性 CP 注释下读一致性模糊（谎言 6）。请用团队设计文档的纪律重新设计；不要提交未应用检查清单的...
  c27_b12.md: ...中新分布式代码 bug 罕见。  **Q3.** 一个 LLM 生成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？s...
  c27_b12.md: ...成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？saga 状态是否持久？...
  c27_b12.md: ...a 模式实现跨服务一致性。” 批判性审查该声明。  提示  Saga 声明需要验证：每个步骤是否独立？补偿动作是否定义？saga 状态是否持久？...
  c27_b13.md: ...**答案：** 批判性审查问题：  (a) **每个步骤是否可独立提交？** Saga 模式要求每个步骤的本地事务可在不依赖后续步骤的情况下提交。验证：每个步骤有自己的数据库事务；每个步骤的提交是持久的。...
  c27_b13.md: ...* 若第 N 步失败，必须能撤销第 1 至 N-1 步。验证：每个步骤有定义的补偿动作；该动作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日...
  c27_b13.md: ...销第 1 至 N-1 步。验证：每个步骤有定义的补偿动作；该动作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启...
  c27_b13.md: ...作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错...
  c27_b13.md: ...完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续...
  c27_b13.md: ...rvive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续。验证：orchestrator 能从任何步骤恢复；幂等重试安全。  (e) **补偿...
  c27_b13.md: ...*补偿本身是否幂等？** 补偿也可能失败（罕见但可能）；应可安全重试多次。验证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, fai...
  c27_b13.md: ...败（罕见但可能）；应可安全重试多次。验证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记...
  c27_b13.md: ...“started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功...
  c27_b13.md: ...rted, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不...
  c27_b13.md: ...记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 s...
  c27_b13.md: ...a（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 saga 模式” 就是误导；LLM 产出了看起来像 saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计...
  c27_b13.md: ...；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 saga 模式” 就是误导；LLM 产出了看起来像 saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计模式声明容易做出但难以验证。审查者必须检查 spec...
  c27_b13.md: ...M 经常产出使用模式 _词汇_ 但无 _语义_ 的代码。词汇在训练数据中；语义需要理解。  对 Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的（孤儿部分事务）...
  c27_b13.md: ...解。  对 Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的（孤儿部分事务）。...
  c27_b13.md: ...**答案：** 批判性审查问题：  (a) **每个步骤是否可独立提交？** Saga 模式要求每个步骤的本地事务可在不依赖后续步骤的情况下提交。验证：每个步骤有自己的数据库事务；每个步骤的提交是持久的。...
  c27_b13.md: ...* 若第 N 步失败，必须能撤销第 1 至 N-1 步。验证：每个步骤有定义的补偿动作；该动作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日...
  c27_b13.md: ...销第 1 至 N-1 步。验证：每个步骤有定义的补偿动作；该动作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启...
  c27_b13.md: ...作本身幂等。  (c) **Saga 状态是否持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错...
  c27_b13.md: ...完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续...
  c27_b13.md: ...rvive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续。验证：orchestrator 能从任何步骤恢复；幂等重试安全。  (e) **补偿...
  c27_b13.md: ...*补偿本身是否幂等？** 补偿也可能失败（罕见但可能）；应可安全重试多次。验证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, fai...
  c27_b13.md: ...败（罕见但可能）；应可安全重试多次。验证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记...
  c27_b13.md: ...“started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功...
  c27_b13.md: ...rted, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不...
  c27_b13.md: ...记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成 saga（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 s...
  c27_b13.md: ...a（某些步骤成功、某些失败、某些补偿失败）有定义行为；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 saga 模式” 就是误导；LLM 产出了看起来像 saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计...
  c27_b13.md: ...；系统不静默泄漏不一致状态。  若任一答案为 “否”，声明 “使用 saga 模式” 就是误导；LLM 产出了看起来像 saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计模式声明容易做出但难以验证。审查者必须检查 spec...
  c27_b13.md: ...M 经常产出使用模式 _词汇_ 但无 _语义_ 的代码。词汇在训练数据中；语义需要理解。  对 Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的（孤儿部分事务）...
  c27_b13.md: ...解。  对 Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的（孤儿部分事务）。...
  c28_b05.md: ...n/a | | 13 | 分布式事务 | CockroachDB 可串行化；Percolator 风格；§13.6.5 saga | n/a | | 14 | NewSQL + 查询处理器 | CockroachDB + Snowflake 用于分...
  c28_b17.md: ...个谎言——时间用 HLC（第3章），Raft 和 fencing token 用于身份（第9、18章），带预算的重试和 saga 用于故障（第13、21章）。 * 如何在 30 分钟内评估任何分布式系统（第11章）。 * 如何在故障下运维分布式系统...
  c30_b06.md: ...ding failure（级联故障）**：通过放大（如重试风暴）跨组件传播的故障。 * **Choreographed saga（编排式 saga）**：每一步发布完成事件；后续步骤订阅；无中央协调器。补偿通过 “rollback 事件” 发生。见...
  c30_b06.md: ...当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating action（补偿动作）**：saga 中语义撤销先前已提交步骤的前进动作。很少是 “精确撤销”；通常恢复不变量（退款、重新入库、取消通知）。自身必须幂等。...
  c30_b06.md: ...ilure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健康状态。 * **Orchestrated saga（协调式 saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与...
  c30_b06.md: ...移除触发因素不能恢复健康状态。 * **Orchestrated saga（协调式 saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：C...
  c30_b06.md: ...saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：CAP 的 PACELC 扩展。若分区：可用性或一致性；否则：延迟或一致性。 * *...
  c30_b06.md: ...把 AI 的审查当作审查。命名于第27章 §27.10。防御：每次批准必须阐明至少一项经人类验证的实质性内容。 * **Saga**：跨服务事务模式，用补偿动作代替锁。见第13章 §13.6.5 了解协调式与编排式变体。 * **Serializa...
  c30_b06.md: ...ding failure（级联故障）**：通过放大（如重试风暴）跨组件传播的故障。 * **Choreographed saga（编排式 saga）**：每一步发布完成事件；后续步骤订阅；无中央协调器。补偿通过 “rollback 事件” 发生。见...
  c30_b06.md: ...当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating action（补偿动作）**：saga 中语义撤销先前已提交步骤的前进动作。很少是 “精确撤销”；通常恢复不变量（退款、重新入库、取消通知）。自身必须幂等。...
  c30_b06.md: ...ilure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健康状态。 * **Orchestrated saga（协调式 saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与...
  c30_b06.md: ...移除触发因素不能恢复健康状态。 * **Orchestrated saga（协调式 saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：C...
  c30_b06.md: ...saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：CAP 的 PACELC 扩展。若分区：可用性或一致性；否则：延迟或一致性。 * *...
  c30_b06.md: ...把 AI 的审查当作审查。命名于第27章 §27.10。防御：每次批准必须阐明至少一项经人类验证的实质性内容。 * **Saga**：跨服务事务模式，用补偿动作代替锁。见第13章 §13.6.5 了解协调式与编排式变体。 * **Serializa...
  c30_b07.md: ...有效 | Ch.18 | 共识支持锁 + fencing token | | 5 | 部分故障罕见 | Ch.22 | Saga 模式；outbox；补偿动作 | | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-oper...
  c30_b14.md: ...├── ch13_saga/     │   ├── orchestrator.py   — orchestrated saga with durable state (SQLite)     │   ├── choreographed.py  —...
  c30_b14.md: ...tate (SQLite)     │   ├── choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_saga.p...
  c30_b19.md: ...器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触发但未全部完成 | 补偿幂等性违反或 saga 状态丢失 | Ch.13 §13.6.5（saga 正确性义务）...
  c30_b19.md: ...ver | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触发但未全部完成 | 补偿幂等性违反或 saga 状态丢失 | Ch.13 §13.6.5（saga 正确性义务）   跨 region 提交 p99 >150 ms...
  c30_b19.md: ...器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触发但未全部完成 | 补偿幂等性违反或 saga 状态丢失 | Ch.13 §13.6.5（saga 正确性义务）...
  c30_b19.md: ...ver | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触发但未全部完成 | 补偿幂等性违反或 saga 状态丢失 | Ch.13 §13.6.5（saga 正确性义务）   跨 region 提交 p99 >150 ms...

=== gpu (142 occurrences) ===
  c06_b03.md: ...迟做跨节点内存访问。 * **ML 分布式训练** 用 InfiniBand 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间...
  c24_b01.md: ...理大模型（Llama 3 70B、Mistral Large、Claude 3 Opus 级别）需要的内存超过任何单张 GPU。模型权重 alone 就是数十 GB；每个请求的 KV cache 又增加更多。推理必须跨 GPU 分布。  但分布式...
  c24_b01.md: ...的内存超过任何单张 GPU。模型权重 alone 就是数十 GB；每个请求的 KV cache 又增加更多。推理必须跨 GPU 分布。  但分布式推理不是 “以 LLM 为负载的分布式系统”。它有与典型微服务舰队结构不同的属性：  (a) **G...
  c24_b01.md: ...U 分布。  但分布式推理不是 “以 LLM 为负载的分布式系统”。它有与典型微服务舰队结构不同的属性：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点可能有 8 张 GPU；推理工作负载在 GPU 之间移动...
  c24_b01.md: ...M 为负载的分布式系统”。它有与典型微服务舰队结构不同的属性：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点可能有 8 张 GPU；推理工作负载在 GPU 之间移动，不亚于在节点之间。  (b) **每个请...
  c24_b01.md: ...队结构不同的属性：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点可能有 8 张 GPU；推理工作负载在 GPU 之间移动，不亚于在节点之间。  (b) **每个请求的两阶段计算**。Prefill（处理 p...
  c24_b01.md: ...a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点可能有 8 张 GPU；推理工作负载在 GPU 之间移动，不亚于在节点之间。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt）和 deco...
  c24_b02.md: ...ache 是连续内存。若请求需要 4 GB KV cache，系统预留 4 GB。请求结束后 4 GB 释放。碎片累积；GPU 内存无法被充分利用。  vLLM 的解决方案：把 KV cache 切成固定大小块（例如每块 16 个 token）。...
  c24_b02.md: ...。块可独立分配和释放；碎片被消除。  收益：  (a) **更高内存利用率**：典型 vLLM 部署使用 70-90% GPU 内存用于 KV cache（相比朴素系统 30-50%）。每张 GPU 能容纳更多请求。  (b) **跨请求共享**...
  c24_b02.md: ...率**：典型 vLLM 部署使用 70-90% GPU 内存用于 KV cache（相比朴素系统 30-50%）。每张 GPU 能容纳更多请求。  (b) **跨请求共享**：共享 prompt 的请求（system prompt + user...
  c24_b02.md: ...age）可共享前缀的 KV cache 块。对聊天机器人风格工作负载，内存节省巨大。  (c) **更高吞吐**：每张 GPU 并发请求更多；每个请求成本更低。  vLLM 采用迅速。到 2026 年，它是最广泛部署的开源推理 serving 系...
  c24_b03.md: ...0-token system prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。对 prompt 多样化（不同用户、不同任务、无共享前缀）的通用 API 流量，vLLM 的 PagedAtte...
  c24_b04.md: ...**：NIM 容器包含 NVIDIA 的硬件专属优化（H100 / H200 / B100 / B200 调优）。每代 GPU 都有调优容器。  (d) **企业特性**：SSO、RBAC、审计日志、监控集成。面向企业采购而建。  (e) **G...
  c24_b04.md: ...U 都有调优容器。  (d) **企业特性**：SSO、RBAC、审计日志、监控集成。面向企业采购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。...
  c24_b04.md: ...购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别是运维：NIM 提供 Kubernet...
  c24_b04.md: ...**GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别是运维：NIM 提供 Kubernetes-native...
  c24_b04.md: ...└─────────────┘           └─────────────┘   每个节点有 8 张 H100 GPU（2026 年标准配置）。模型跨 GPU 分布（单模型 tensor parallelism；超大模型 pipeline...
  c24_b04.md: ...└─────────────┘   每个节点有 8 张 H100 GPU（2026 年标准配置）。模型跨 GPU 分布（单模型 tensor parallelism；超大模型 pipeline parallelism）。负载均衡器基...
  c24_b04.md: ...点。  Continuum 具体拓扑：  * 50 个节点，每个 8x H100（或 H200，混合）：共 400 张 GPU。 * 服务 8 种不同模型（不同尺寸、微调、每客户自定义模型）。 * 平均 GPU 利用率：60%（目标：80%+）。...
  c24_b04.md: ...H200，混合）：共 400 张 GPU。 * 服务 8 种不同模型（不同尺寸、微调、每客户自定义模型）。 * 平均 GPU 利用率：60%（目标：80%+）。 * 成本：每月 120 万美元，包括 GPU 租赁、网络、存储和工程运维。  60...
  c24_b04.md: ...微调、每客户自定义模型）。 * 平均 GPU 利用率：60%（目标：80%+）。 * 成本：每月 120 万美元，包括 GPU 租赁、网络、存储和工程运维。  60% 利用率就是 Tobias 优化工作的空间。每个百分点的利用率约等于每月 2 万...
  c24_b05.md: ...### 24.6 成本模型  对推理集群，成本由 GPU 租赁主导。截至 2026 年：  * H100 80GB 云按需：每张 GPU 每小时约 3-4 USD。 * 1 年...
  c24_b05.md: ...4.6 成本模型  对推理集群，成本由 GPU 租赁主导。截至 2026 年：  * H100 80GB 云按需：每张 GPU 每小时约 3-4 USD。 * 1 年预留承诺：每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）...
  c24_b05.md: ...2026 年：  * H100 80GB 云按需：每张 GPU 每小时约 3-4 USD。 * 1 年预留承诺：每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。...
  c24_b05.md: ...约 3-4 USD。 * 1 年预留承诺：每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每...
  c24_b05.md: ...CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每小时 1200 USD，每月 86.4 万美元。加上网络、存储和运维。  每个请求成本：  *...
  c24_b05.md: ...络、存储和运维。  每个请求成本：  * Llama 3 70B 上典型 1000-token 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求 GPU 成本 0.004-0.013 USD。 * 每天 100 万请求：...
  c24_b05.md: ...3 70B 上典型 1000-token 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求 GPU 成本 0.004-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD，每月 12万-39万 U...
  c24_b05.md: ...模型大小、prompt 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个请求的 GPU-秒。更好的 attention（PagedAttention、RadixAttention）、更好的 batching...
  c24_b05.md: ...tinuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  * 请求到达推理服...
  c24_b05.md: ...SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  * 请求到达推理服务器。 * 服务器保持一个 “batch” 活...
  c24_b05.md: ...稳定（始终并行处理多个请求）。  工作原理：  * 请求到达推理服务器。 * 服务器保持一个 “batch” 活跃请求；GPU 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length limit），它离开 b...
  c24_b05.md: ...成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static bat...
  c24_b06.md: ...#### 概念检测  **Q1.** 是什么让分布式 LLM 推理在结构上与典型微服务舰队不同？  提示  GPU 作为单元、两阶段计算、KV cache 作为状态、严格的 per-token 延迟、突发模式。  **答案：** 五个...
  c24_b06.md: ...V cache 作为状态、严格的 per-token 延迟、突发模式。  **答案：** 五个结构差异：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点有 8 张 GPU；工作负载在 GPU 之间移动不亚于在...
  c24_b06.md: ...token 延迟、突发模式。  **答案：** 五个结构差异：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点有 8 张 GPU；工作负载在 GPU 之间移动不亚于在节点之间。微服务舰队关注节点；推理关注 G...
  c24_b06.md: ...：** 五个结构差异：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点有 8 张 GPU；工作负载在 GPU 之间移动不亚于在节点之间。微服务舰队关注节点；推理关注 GPU。  (b) **每个请求的两阶段计...
  c24_b06.md: ...：  (a) **GPU 是单元，而非节点**。昂贵资源是 GPU 内存和计算。一个节点有 8 张 GPU；工作负载在 GPU 之间移动不亚于在节点之间。微服务舰队关注节点；推理关注 GPU。  (b) **每个请求的两阶段计算**。Prefil...
  c24_b06.md: ...U 内存和计算。一个节点有 8 张 GPU；工作负载在 GPU 之间移动不亚于在节点之间。微服务舰队关注节点；推理关注 GPU。  (b) **每个请求的两阶段计算**。Prefill（处理 prompt，compute-bound）和 deco...
  c24_b06.md: ...需要 4 GB，系统预留 4 GB；请求结束后释放。久而久之内存碎片化；即使总空闲内存大，也没有足够大的连续块给新请求。GPU 内存利用率差（典型 30-50%）。  PagedAttention 收益：  * **更高内存利用率**：70-90...
  c24_b06.md: ...存利用率差（典型 30-50%）。  PagedAttention 收益：  * **更高内存利用率**：70-90% GPU 内存用于 KV cache，相比朴素系统 30-50%。 * **每张 GPU 更多并发请求**：更好内存利用率意味着...
  c24_b06.md: ...**更高内存利用率**：70-90% GPU 内存用于 KV cache，相比朴素系统 30-50%。 * **每张 GPU 更多并发请求**：更好内存利用率意味着更多请求 fit；每张 GPU 吞吐上升。 * **前缀共享**：共享 prom...
  c24_b06.md: ...he，相比朴素系统 30-50%。 * **每张 GPU 更多并发请求**：更好内存利用率意味着更多请求 fit；每张 GPU 吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同 system...
  c24_b07.md: ...衡器按工作负载把请求路由到最适合的引擎。  **Q4.** 什么是 continuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：**...
  c24_b07.md: ...适合的引擎。  **Q4.** 什么是 continuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous ba...
  c24_b07.md: ...atch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；...
  c24_b07.md: ...。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（static）batchin...
  c24_b07.md: ...：  (a) **Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token...
  c24_b07.md: ...间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens...
  c24_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群 GPU 利用率为 60%。找出三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前...
  c24_b09.md: ...*：短请求和长请求分别走不同队列。短请求 batch 快速处理；长请求 batch 按自己节奏处理。权衡：更难最优共享 GPU 资源。  (c) **分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理...
  c24_b10.md: ...衡就是负载均衡；故障转移就是故障转移。通用模式足够。  反驳：推理有结构属性（per-token 延迟预算、两阶段计算、GPU 作为单元、KV cache 作为状态），通用分布式系统不会为之优化。专用协议（PagedAttention、Radix...
  c24_b10.md: ...(b) **Continuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU sha...
  c24_b10.md: ...应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。...
  c24_b10.md: ...**多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性*...
  c24_b10.md: ...hard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-request 延迟。LLM 应提供完整可观测性方案。...
  c24_b10.md: ...：若集群服务多客户，需要隔离（rate limit、队列优先级、每客户模型分片）。  (g) **成本监控**：每客户 GPU-hour、每工作负载。真实生产部署会针对成本做仪器。  (h) **故障处理**：GPU 故障、节点故障、模型加载故障...
  c24_b10.md: ...) **成本监控**：每客户 GPU-hour、每工作负载。真实生产部署会针对成本做仪器。  (h) **故障处理**：GPU 故障、节点故障、模型加载故障。部署必须优雅处理每种。  正确答案：“基础部署用 TGI 或 vLLM 加 tensor...
  c24_b11.md: ...特性。   ### 本章总结  分布式 LLM 推理是 2026 年分布式系统的一个子学科。与典型微服务舰队的关键差异：GPU 是单元；两阶段计算（prefill + decode）；KV cache 是中心状态；严格 per-token 延迟预...
  c24_b11.md: ...预算；突发工作负载模式。  vLLM 的 PagedAttention 把 KV cache 分成固定大小块；消除碎片；GPU 内存利用率从 30-50% 提升到 70-90%。2026 年基线开源推理引擎。  SGLang 的 RadixAtt...
  c24_b11.md: ...微服务、多引擎支持、硬件优化、企业特性。企业团队的标准部署表面。  Continuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准...
  c24_b11.md: ...batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：50 节点、400 GPU、每月 120 万美元、60% 利用率。优化空间巨大；每个百分点利用率约每月 2 万美元。  第25章进入 2025-2...
  c25_b01.md: ...Tobias 的集群 GPU 利用率为 60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggre...
  c25_b01.md: ...计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。  本章是工作流 12：一个...
  c25_b01.md: ...ecode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/dec...
  c25_b01.md: ...LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。...
  c25_b01.md: ...oken prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 toke...
  c25_b01.md: ...少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一个 to...
  c25_b01.md: ...age | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix...
  c25_b01.md: ...r mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可能只用到 50% 计算和 50% 内存带宽：从未饱和任一。浪费巨大。  Microsoft Res...
  c25_b01.md: ...2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 dec...
  c25_b01.md: ...的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU...
  c25_b01.md: ...阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b01.md: ...GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 prefill GPU 传输到 decode GPU。...
  c25_b02.md: ...───┘  └──────────────┘   Prefill 节点：  * 针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算...
  c25_b02.md: ...KV cache。 * 为每个请求逐 token 生成。 * 瓶颈是内存带宽（每 token 读取 KV cache）；GPU 为此调优。  KV cache 传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给...
  c25_b03.md: ...### 25.3 为什么分离获胜  两个复合收益：  (a) **每阶段硬件利用率更好**。Prefill 节点饱和 GPU 计算（只做计算重的工作）。Decode 节点饱和内存带宽（只做内存重的工作）。集群整体利用率远高于未分离情况。  (b...
  c25_b03.md: ...1M token 上下文（2026 年正在成为常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在...
  c25_b03.md: ...常见工作负载），单个请求的 KV cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在 2024 年论文中描述的推理系统，是 2026...
  c25_b04.md: ...000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受 decode GPU 内存带宽限制）。 * KV cache 传输：典型 KV cache（5-10 GB 经 RDMA）约 50 ms。...
  c25_b04.md: ...l + 传输 + 首 token 生成）。 * 后续每 token 延迟：约 30 ms。  **成本影响**：  * GPU 利用率从 60% 提升到约 85%。 * 有效集群容量提升约 40%（利用率收益加每 GPU 效率提升）。 * 每个请...
  c25_b04.md: ...**成本影响**：  * GPU 利用率从 60% 提升到约 85%。 * 有效集群容量提升约 40%（利用率收益加每 GPU 效率提升）。 * 每个请求成本下降约 30%。 * 年度节省：此前 1400 万美元年运行费中节省 400-500 万...
  c25_b05.md: ...e：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很...
  c25_b05.md: ...阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅新 token），并采样。这是矩阵-向量乘法。瓶颈是内...
  c25_b05.md: ...ion（仅新 token），并采样。这是矩阵-向量乘法。瓶颈是内存带宽（加载 KV cache）。  不对称意味着一张 GPU 同时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 *...
  c25_b05.md: ...KV cache）。  不对称意味着一张 GPU 同时运行两个阶段时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（...
  c25_b05.md: ...时，对哪个阶段都没优化：  * 对 prefill，GPU 内存带宽过度配置（不是限制；浪费）。 * 对 decode，GPU 计算过度配置（不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill 节点**...
  c25_b05.md: ...不是限制；浪费）。  综合利用率差（典型 40-60%）。  分离：  * **Prefill 节点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内存 GPU（H200、B100、...
  c25_b05.md: ...点**：使用最高计算 GPU（H100、H200）。饱和计算。 * **Decode 节点**：使用高内存带宽和大内存 GPU（H200、B100、B200）。饱和内存带宽。  每个阶段 GPU 利用率接近 100%；集群整体利用率大幅提升。...
  c25_b05.md: ...**Decode 节点**：使用高内存带宽和大内存 GPU（H200、B100、B200）。饱和内存带宽。  每个阶段 GPU 利用率接近 100%；集群整体利用率大幅提升。  成本是两阶段间 KV cache 传输（通常 50-100 ms）加...
  c25_b05.md: ...点分布；缓存层抽象。  **答案：** KV cache sharding 把单个请求的 KV cache 分布到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAttention 风格），存储在集群级缓存层；decode...
  c25_b05.md: ...块。  它解决的问题：超长上下文（2026 年 1M+ token）每个请求产生数十 GB KV cache。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GPU 内存。  实现：  (a) **Mooncake**（M...
  c25_b05.md: ...en）每个请求产生数十 GB KV cache。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GPU 内存。  实现：  (a) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cac...
  c25_b05.md: ...decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt（system prompt、ag...
  c25_b06.md: ...5 ms（负载均衡器到推理节点）。  (b) **Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 400 Gbit/s...
  c25_b06.md: ...），但 per-token 延迟类似。分离版为首 token 多付约 50 ms（传输）；吞吐收益来自集群整体更好的每 GPU 利用率。...
  c25_b08.md: ...decode（需要更多内存和 per-token 吞吐）并少扩 prefill。  没有分离（co-located）时，GPU 同时做两个阶段；扩缩是一维的。集群对当前工作负载要么计算过多，要么内存带宽过多，无法同时匹配。  运维关注点：reba...
  c25_b09.md: ...峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分模型。正交维度；常组合...
  c25_b09.md: ...rding（tensor parallelism、pipeline parallelism）_：把模型权重和计算跨多张 GPU 拆分。当模型太大一张 GPU 放不下时必需（Llama 3 70B 需要 4+ GPU；405B 需要 16+）。...
  c25_b09.md: ...rallelism、pipeline parallelism）_：把模型权重和计算跨多张 GPU 拆分。当模型太大一张 GPU 放不下时必需（Llama 3 70B 需要 4+ GPU；405B 需要 16+）。  _Disaggregation...
  c25_b09.md: ...m）_：把模型权重和计算跨多张 GPU 拆分。当模型太大一张 GPU 放不下时必需（Llama 3 70B 需要 4+ GPU；405B 需要 16+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个...
  c25_b09.md: ...+）。  _Disaggregation_：把请求处理拆成 prefill 和 decode 两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每 shard 4 张 GPU（ten...
  c25_b09.md: ...在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张...
  c25_b09.md: ...* Prefill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * K...
  c25_b09.md: ...（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的...
  c25_b09.md: ...（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100...
  c25_b09.md: ...重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 d...
  c25_b09.md: ...分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一张 GPU 放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B）可放一张 GPU，不需要...
  c25_b09.md: ...大一张 GPU 放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B）可放一张 GPU，不需要 sharding。小集群部署（< 50 节点）可能不需要分离。对 Continuum 设置（70B+ 模型、5...
  c25_b10.md: ...orRT-LLM、ExecuTorch）：在支持硬件上 1.5-2 倍提升。  (c) **硬件优化**：  * 更新 GPU（H200、B100、B200）：每美元更好内存带宽或计算。 * 混合部署（prefill 与 decode 用不同 G...
  c25_b10.md: ...U（H200、B100、B200）：每美元更好内存带宽或计算。 * 混合部署（prefill 与 decode 用不同 GPU）。 * 对非关键工作负载用 spot/preemptible 实例。  (d) **工作负载优化**：  * 限制每请...
  c25_b11.md: ...compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优硬件；相同延迟下吞吐提升 2-4 倍。  Splitwise（Microsoft Research）...
  c25_b11.md: ...，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用。Mooncake（商业）和 LMCache（开源...
  c26_b03.md: ...约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Spe...
  c26_b03.md: ....5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。...
  c26_b04.md: ...多租户公平性  Continuum 服务多个客户。每客户付费 tier 不同；有些有延迟 SLA。公平性问题：给定有限 GPU 资源，如何在客户间调度请求。  朴素 FIFO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器...
  c26_b04.md: ...饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有...
  c26_b06.md: ...ng 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **V...
  c26_b06.md: ...更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的...
  c26_b06.md: ...做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。  (c) **集群足够...
  c26_b06.md: ...d-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verificati...
  c26_b06.md: ...s 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大...
  c26_b06.md: ...round-trip 会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始...
  c26_b06.md: ...体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何...
  c26_b06.md: ...张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculat...
  c26_b06.md: ...的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decodi...
  c26_b06.md: ...什么是多租户推理集群的 weighted fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按...
  c26_b06.md: ...**答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权重 5、3、2（共 10）。调度器目标让 GPU 花 50% 时间处理客户 1 的请求，30%...
  c26_b06.md: ...或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权重 5、3、2（共 10）。调度器目标让 GPU 花 50% 时间处理客户 1 的请求，30% 处理客户 2，20% 处理客户 3。  如何防止饥饿：即使客户 1 有很...
  c26_b06.md: ...处理客户 2，20% 处理客户 3。  如何防止饥饿：即使客户 1 有很多请求排队，调度器也确保客户 2 和 3 得到 GPU 时间。没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速...
  c26_b06.md: ...的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等下一轮。  (c) **Hierarchical scheduling**：客户可...
  c26_b08.md: ...延迟高（p50=2 秒，p99=30 秒）。诊断并提出修复。  提示  尾部延迟来自长请求阻塞、无长度预测路由、长请求 GPU 竞争、speculative decoding 有帮助。  **答案：** 诊断：p99/p50 比 15:1 表明严...
  c26_b08.md: ...5:1 表明严重尾部延迟。可能原因：  (a) **长输出请求阻塞短请求**：单个 5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。...
  c26_b08.md: ...秒以上；同节点其他请求等待它完成。Disaggregation（分离 prefill 节点）修复此问题。  (c) **GPU 竞争**：若多请求竞争同节点 GPU 内存带宽，最慢者拖累尾部。  (d) **网络抖动或瞬态故障**：需要重试的请求...
  c26_b08.md: ...saggregation（分离 prefill 节点）修复此问题。  (c) **GPU 竞争**：若多请求竞争同节点 GPU 内存带宽，最慢者拖累尾部。  (d) **网络抖动或瞬态故障**：需要重试的请求更慢；若重试无预算，尾部可延长。  修...
  c26_b09.md: ...**答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify...
  c26_b09.md: ...0 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；有推理平台工程师的团队；网络延迟有...
  c26_b09.md: ...）；有推理平台工程师的团队；网络延迟有界（intra-rack、NVLink、RDMA）；成本节省占主导（verify GPU-hour 非常贵的大模型）。  对 Continuum（50+ 节点、大 verify model）：分布式是正确答案...
  c26_b11.md: ...受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间...
  c26_b11.md: ...在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling...
  c30_b06.md: ...code）**：2025-2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被保护资源强制执行令牌顺序。...
  c30_b11.md: ...e 负载均衡（第26章）加长度预测路由。 * **投机解码**：按模型为长输出工作负载启用。 * **监控**：每客户 GPU 秒成本；每阶段利用率。 * **为何这个组合**：2025-2026 架构突破（分离、KV cache 层、投机解码）...

=== bug (133 occurrences) ===
  c01_b01.md: ...EU-West、US-West；本章使用城市名，因为事后复盘最初就是这样命名的，后来团队才把它重新框定为区域术语。）  bug 不在任何单段代码里。银行的 ledger 代码是对的。重试逻辑是对的。支付网关的幂等性检查对它所持有的世界模型而言也是...
  c01_b01.md: ...任何单段代码里。银行的 ledger 代码是对的。重试逻辑是对的。支付网关的幂等性检查对它所持有的世界模型而言也是对的。bug 在于每个组件对其他组件的“信念”。每个组件都相信了某种事实上不成立的东西。  这本书讨论的就是相信不实之物的系统。它讲...
  c01_b10.md: ...s 窗口本身就足以造成 Crescent 式失败。如果两个协调者在 10 ms 内提交，时间戳无法可靠排序。许多分布式 bug 住在小于时钟偏移上界的窗口里。  第二，NTP 可能静默失败。如果 NTP 服务器不可达，本地时钟以硬件速率漂移，典型...
  c01_b14.md: ...，响应者只修复了最明显的失败，却忽略了更深的谎言。这种情况的成本模式是什么？  **讨论：** 模式反复出现：一个可见 bug 被修复，六个月后同类 bug 在略有不同的条件下再次出现，直到第二或第三次发生才有人识别出更深的谎言。成本是累积的事故...
  c01_b14.md: ...却忽略了更深的谎言。这种情况的成本模式是什么？  **讨论：** 模式反复出现：一个可见 bug 被修复，六个月后同类 bug 在略有不同的条件下再次出现，直到第二或第三次发生才有人识别出更深的谎言。成本是累积的事故响应、客户影响和每次修复所花费...
  c01_b15.md: ...上说谎：每个节点把自己看作独一无二地扮演角色，而系统其余部分可能不同意。它们在失败上说谎：部分失败是默认模式，不是例外，bug 就住在“这个节点上发生了什么”与“系统整体经历了什么”之间的缺口里。  Crescent 事件在单一重试中同时利用了三...
  c02_b01.md: ...注释也可信。两者都错了，而“可信”与“正确”之间的差距正是本章的主题。（与本书每次 LLM 冷启动代码引用一样，这里的 bug 是结构性的——一个口号级别的 CAP 论断掩盖了每个操作层面的问题——而非语法性的。）  模型选择 CP，是因为网上关...
  c02_b01.md: ...6. **只有一个管理员。**   7. **传输成本为零。**   8. **网络是同构的。**  每条谬误对应一类 bug。我们快速过一遍，因为本书后续章节会深入展开其中大部分。   #### 网络是可靠的  数据包会丢。TCP 会重传，但...
  c02_b03.md: ...有用的练习：取你分布式代码里的任意一个函数，问它假设了八条中的哪一条。大多数函数至少假设一条。大多数生产中的分布式系统 bug 就是八条之一，只是略有伪装。   ### 2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PO...
  c02_b11.md: ...r 读或 `AS OF SYSTEM TIME`。在需要线性化读的地方使用过期读，是一类默认线性化系统也不会自动防止的 bug。  **Q3.** 走一遍 §2.4 的计数器-排行榜-库存工作负载，解释为什么每个的一致性需求是这样。然后提出一个可...
  c02_b14.md: ...未来变更，也无法在假设变化时为自己辩护。   ### 本章总结  Deutsch 的八大谬误命名了产生大多数分布式系统 bug 的单机直觉。谬误不是要记住的 checklist；它们是审计每段分布式代码时应检查的假设类别。第21章会作为级联失败重...
  c02_b14.md: ...t；它们是审计每段分布式代码时应检查的假设类别。第21章会作为级联失败重新审视网络可靠性的谬误；第18章会作为锁与协调 bug 重新审视安全和“只有一个管理员”的谬误。  精确陈述的 CAP 说，在网络分区下，你不能同时拥有线性化和可用性。口号版...
  c04_b09.md: ...ion ordering，应用必须按 partition key（通常是 transaction ID）幂等。重复扣款 bug 是因为幂等检查没有强一致。  **Q2.** 用 flooding 写可靠广播的伪代码。走查一个 4 节点示例，其中一...
  c05_b11.md: ...择在协调成本和故障切换透明性之间权衡。$W + R > N$ 的法定人数规则给相交，不是线性一致性；差别是许多真实复制 bug 的根源。  Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现...
  c07_b08.md: ...个副本（崩溃只需 2f+1）。  对单一可信团队运维的配置存储：  * 威胁模型是运维性的（节点崩溃、网络故障、软件 bug）。 * 拜占庭故障（节点恶意行为）需要主机被攻破或内部攻击；若团队可信且主机安全，风险低。 * 导致“类拜占庭”行为的...
  c07_b08.md: ...* 拜占庭故障（节点恶意行为）需要主机被攻破或内部攻击；若团队可信且主机安全，风险低。 * 导致“类拜占庭”行为的软件 bug（节点因 bug 发送错误响应）确实存在，但通常跨副本相关（相同 bug、相同错误输出），BFT 对此也无帮助。  结论...
  c08_b07.md: ...被移除”节点可能 resurfacing，导致配置不一致。  累积效应：Paxos 实现通常是数千行谨慎代码。上述任何 bug 都可能导致数月后才显现的静默 safety 违反。“正确 Paxos 实现很少”并非夸张。...
  c08_b10.md: ...实现 Paxos，产出 200 行代码。在不运行的情况下，指出任何 LLM 生成的 Paxos 实现中可能存在的三类 bug。  **讨论：**  (a) **持久化遗漏。** Acceptor 状态（$b_{max}$、`accepted_b...
  c09_b04.md: ...ot` RPC 接收快照。Follower 用快照替换自己的状态并恢复。  运维考虑：快照必须与状态机一致。许多实现的 bug 是：快照在某一时刻拍摄，但快照生成期间提交的日志条目未被正确计入。修复方法是在快照例程与应用例程之间做谨慎协调。...
  c09_b04.md: ...间任何决定都需要联合多数。过渡完成后，集群才切换到 $C_{new}$。  这是 Raft 较复杂的部分之一，常是实现 bug 的来源。生产 etcd 使用更简单的替代方案：一次只改一个服务器（single-server change），代价是同...
  c09_b05.md: ...rs` 库（Rust）。她在设计文档中记录的理由：  (a) **成熟度** ：TiKV 的 Raft 已生产运行多年，bug 已被发现并修复。 (b) **语言匹配** ：Stratus DB 是 Rust；raft-rs 原生。 (c) **...
  c09_b10.md: ...一个 LLM 被要求“为分布式计数器实现 Raft”。输出 300 行 Python。指出除了第8章 Paxos 等价 bug 之外，任何 LLM 生成的 Raft 实现中可能存在的三类结构性 bug。  **讨论：** 除了持久化、ballot...
  c09_b10.md: ...ython。指出除了第8章 Paxos 等价 bug 之外，任何 LLM 生成的 Raft 实现中可能存在的三类结构性 bug。  **讨论：** 除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug...
  c09_b10.md: ...bug。  **讨论：** 除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM...
  c10_b01.md: ...运维时，这个假设合理。当节点由互不信任的各方运维（区块链验证者）、节点可能被攻击者攻破（存在恶意租户的云环境），或软件 bug 产生“类拜占庭”行为（损坏状态、垃圾消息）时，这个假设不合理。  拜占庭容错（Byzantine Fault Tole...
  c10_b04.md: ...release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint fork 因此产生安全 bug。协议正确性已被证明；新实现应使用经过验证的库而不是重新实现。...
  c10_b07.md: ...uivocation）、假装诚实，或主动误导协议。拜占庭故障既模型恶意行为（被攻破节点、对抗性验证者），也模型任意软件 bug（损坏状态产生乱码响应）。  差异重要，因为：  (a) 崩溃容错协议（Paxos、Raft）假设无响应节点已崩溃；在拜...
  c11_b02.md: ...evels）、DynamoDB（按请求强一致读）、Spanner（提供过期读）。  一致性模型决定应用可以免费避免哪些 bug，必须显式处理哪些。...
  c14_b03.md: ...演练，沿袭 Tyler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * TigerStyle 编程规范：固定大小 128 字节消息、热路径无动态分配、显式状态机、设计口号是 “1000...
  c15_b06.md: ...成功。 * 违反不变式成本（退款、品牌损害）远高于协调成本。 * “漏掉” 递减（系统认为还有库存时实际已售完）是严重 bug。  结构差异：计数器的不变式是 “总和等于总递增数”，可交换且幂等。库存的不变式是 “值绝不低于零”，这是对操作顺序和...
  c15_b07.md: ...丢弃逻辑上更晚的写，而选择逻辑上更早的写。  Cassandra 的 “tombstone resurrection” bug（第5章）是经典示例：由于时钟偏移，delete 的时间戳早于后续写；后续写 “胜出” 并复活了已删记录。  抵御 LW...
  c15_b08.md: ...**严格 NTP 纪律** ：确保所有 Cassandra 节点保持小范围漂移（10-50 ms）。这减少但不能消除 bug；残差概率小但非零。  (b) **服务端时间戳 + 每个资料指定单一协调器** ：通过 `user_id` 的一致哈希...
  c15_b08.md: ...NewSQL** ：对新开发，选择默认提供正确一致性模型的系统。CockroachDB（可串行化）以更高写成本消除此类 bug。  建议：短期部署 (a) + (c)。长期若工作负载增长或用户投诉持续，评估 (e)。  **Q2.** 一个团队为...
  c16_b10.md: ...净。合并函数就是类型的定义；正确性是结构性的。OT 需要为每对操作写转换函数，随操作集增长而膨胀；实现庞大且容易出微妙 bug。Google Docs 的 OT 实现出了名的复杂；学术文献记录了大量 OT 正确性问题。  反驳：OT 已有数十年生...
  c17_b02.md: ...跟随原帖；线程结构保持。  (c) **协作文档编辑** ：会话内编辑因果有序；CRDT 处理并发编辑。  (d) **Bug tracker 评论** ：讨论线程中回复顺序重要，但跨线程并发可任意。  与线性化相比：因果协调更少（每次操作无需共...
  c17_b05.md: ...UM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...
  c17_b06.md: ...UM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...
  c18_b06.md: ...ing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：即使锁服务有 bug（对同一把锁发了两个 token），资源的单调检查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统...
  c18_b11.md: ...ncing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。   ### 本章总结  分布式缓存（Redis Cluster 作为典型）用 gossip 维护成员关系，可调失效模...
  c20_b03.md: ...更而变慢，trace 不会显示 “配置与预期不同”。为此需要配置审计。  (d) **过期代码**：若服务运行存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、l...
  c20_b07.md: ...tes**：错误码是什么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug。 * 503：service X 过载。 * 502/504：调用服务与 X 之间网关问题。 * 超时：X 挂起或慢。...
  c20_b08.md: ...traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低到随机抽查可能发现不了。Tracing  surfaced 这些客户支持量尚未检测到的长尾问题。  **Q3.**...
  c21_b03.md: ...年 8 月 14 日 16:10，俄亥俄州三条输电线路因树木长得太近而故障。FirstEnergy 的监控系统有软件 bug，阻止运维看到故障；系统实际上在盲目飞行。  放大：电力绕过故障线路流动时，其他线路过载。每条过载线路要么触发保护继电器...
  c21_b08.md: ...败（线路倒下）。等价：下游数据库因迁移变慢。  (b) **缺乏可观测性**：监控系统（FirstEnergy 的）有 bug；运维看不到故障。等价：团队监控面板过期；on-call 几分钟内没看到数据库变慢。  (c) **放大**：电力重新分...
  c23_b01.md: ...工程把信念转化为证据。  (b) **生产规模，而非实验室规模**。许多故障只在生产规模和流量模式下可见。实验室测试找 bug；混沌找只在负载下才出现的 bug。  (c) **稳态假设**。实验前，定义 “系统健康” 的含义（具体 SLI）。实...
  c23_b01.md: ...**生产规模，而非实验室规模**。许多故障只在生产规模和流量模式下可见。实验室测试找 bug；混沌找只在负载下才出现的 bug。  (c) **稳态假设**。实验前，定义 “系统健康” 的含义（具体 SLI）。实验若维持稳态则成功；否则失败。...
  c23_b03.md: ...发现结果很残酷。几乎每个主要分布式数据库首次被 Jepsen 测试时都违反了某些声明。这些发现推动了行业改进：厂商修复 bug；工程师学会验证什么；整个领域的正确性基线提高。  著名 Jepsen 发现：  * **Cassandra（多次）**...
  c23_b03.md: ...*：早期版本有可串行化违规；后续版本修复。 * **Redis**：Redlock 问题（第18章主题）；各种故障转移 bug。 * **etcd**：旧版本有轻微一致性 issue；已纳入修复。  Jepsen 报告是任何分布式系统工程师的必读...
  c23_b04.md: ...款（Raft 重选举完成前某 shard 有 30 秒双 leader 期）。行动项：调查并修复；这与第1章事故的同类 bug。     * EU-West API gateway 的重试预算配置正确；没有重试风暴。 * 学习：     * 故障...
  c23_b04.md: ...* 故障转移有效，但不如设计预期干净。     * 面向客户的影响（超时）比团队预期大。     * 发现一个真实 bug（重复扣款），生产流量在真实故障下可能遇到。  **行动项**：  * 将 Consul 健康检查频率调整到目标 10...
  c23_b07.md: ...**答案：** Game day 暴露问题：团队不知道的故障模式、运维缺口、潜伏的 bug。赛后讨论是捕获学习的机会。  若团队害怕（害怕责备、害怕显得无能、害怕对缺口负责），他们会隐藏问题。他们会淡化意外、缩...
  c23_b07.md: ...是 Jepsen 项目，为什么它对分布式系统领域重要？  提示  Jepsen：系统性地测试分布式数据库声明。发现许多 bug；提高领域正确性基线。  **答案：** Jepsen 是 Kyle Kingsbury 的项目（约 2013 年启动）...
  c23_b07.md: ...ncern、CockroachDB 早期可串行化、Redis 故障转移、etcd 旧版本——都有 Jepsen 发现的 bug。  为何重要：  (a) **经验证据**：Jepsen 报告是声明真实性的经验证据。没有它们，厂商声明只是营销；有了...
  c23_b07.md: ...验证据。没有它们，厂商声明只是营销；有了它们，声明可测试。  (b) **行业改进**：厂商修复 Jepsen 发现的 bug。领域正确性基线提高。  (c) **工程师教育**：读 Jepsen 报告的工程师学会验证什么、怀疑什么。报告是分布式...
  c23_b10.md: ...在增长。成熟混沌工程实践把它视为持续投资，而非一次性里程碑。  **Q3.** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继...
  c23_b10.md: ...day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game...
  c23_b10.md: ...在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；...
  c23_b10.md: ...发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行...
  c23_b10.md: ...day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行动项清单并按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game da...
  c23_b10.md: ...按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其...
  c23_b10.md: ...间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **...
  c23_b10.md: ...的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **Bug 严重程度**：若是数据损坏（如示例），属于高严重。立即暂停修复；不要继续可能让更多客户暴露于损坏的 game day。...
  c23_b10.md: ...若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条...
  c23_b10.md: ...周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条件下触发，更灵活。  (d) **团队带宽**：小团队带宽有限必须优...
  c23_b10.md: ...团队带宽有限必须优先；暂停一件事做另一件事。大团队可并行推进。  对 Olamide 的假设场景（故障转移下重复扣款）：bug 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 t...
  c23_b10.md: ...在增长。成熟混沌工程实践把它视为持续投资，而非一次性里程碑。  **Q3.** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继...
  c23_b10.md: ...day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game...
  c23_b10.md: ...在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；...
  c23_b10.md: ...发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行...
  c23_b10.md: ...day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行动项清单并按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game da...
  c23_b10.md: ...按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其...
  c23_b10.md: ...间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **...
  c23_b10.md: ...的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **Bug 严重程度**：若是数据损坏（如示例），属于高严重。立即暂停修复；不要继续可能让更多客户暴露于损坏的 game day。...
  c23_b10.md: ...若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条...
  c23_b10.md: ...周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条件下触发，更灵活。  (d) **团队带宽**：小团队带宽有限必须优...
  c23_b10.md: ...团队带宽有限必须优先；暂停一件事做另一件事。大团队可并行推进。  对 Olamide 的假设场景（故障转移下重复扣款）：bug 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 t...
  c23_b11.md: ...更深观察：game day 的价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在已知未修复 bug 的情况下继续...
  c23_b11.md: ...更深观察：game day 的价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在已知未修复 bug 的情况下继续混沌在运维上存...
  c23_b11.md: ...更深观察：game day 的价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在已知未修复 bug 的情况下继续混沌在运维上存疑。...
  c23_b11.md: ...价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在已知未修复 bug 的情况下继续混沌在运维上存疑。...
  c23_b13.md: ...de 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它在客户之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026...
  c27_b05.md: ...。否则故障在客户投诉前不可见。  检查清单不穷尽，但覆盖最常见 LLM 生成失败模式。对每份 PR 走一遍，可捕获高频 bug。   ### 27.9 为什么 LLM 不擅长这个  结构性原因：  (a) **训练数据偏差**。LLM 见过很多简...
  c27_b05.md: ...因果排序、幂等性）。这些不变量不在代码中表达；在周边设计中。LLM 看到代码；看不到不变量。  (d) **测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在...
  c27_b05.md: ...(d) **测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、spli...
  c27_b05.md: ...式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。L...
  c27_b05.md: ...试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。LLM 的代码 “运行”；bug...
  c27_b05.md: ...bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。LLM 的代码 “运行”；bug 只有仔细生产分析才可见。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人工用上述清单审查。LL...
  c27_b05.md: ...。否则故障在客户投诉前不可见。  检查清单不穷尽，但覆盖最常见 LLM 生成失败模式。对每份 PR 走一遍，可捕获高频 bug。   ### 27.9 为什么 LLM 不擅长这个  结构性原因：  (a) **训练数据偏差**。LLM 见过很多简...
  c27_b05.md: ...因果排序、幂等性）。这些不变量不在代码中表达；在周边设计中。LLM 看到代码；看不到不变量。  (d) **测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在...
  c27_b05.md: ...(d) **测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、spli...
  c27_b05.md: ...式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。L...
  c27_b05.md: ...试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。LLM 的代码 “运行”；bug...
  c27_b05.md: ...bug（重复扣款、丢失写、split-brain）通常不抛异常；它们产生看起来正确的错误结果。LLM 的代码 “运行”；bug 只有仔细生产分析才可见。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人工用上述清单审查。LL...
  c27_b09.md: ...er-operation 一致性选择。  每个模式都在训练数据中，且常大量出现。LLM 把它们作为默认产出；识别它们是 bug 的工程纪律需要分布式系统训练。  **Q3.** 为什么单元测试不足以捕获 LLM 生成分布式代码中的分布式系统 bu...
  c27_b09.md: ...ug 的工程纪律需要分布式系统训练。  **Q3.** 为什么单元测试不足以捕获 LLM 生成分布式代码中的分布式系统 bug？  提示  单元测试不演练分布式故障模式（网络分区、并发重试、操作期间领导者切换）。  **答案：** 单元测试通常：...
  c27_b09.md: ...常顺序到达）。(d) 不测试多副本场景（split-brain、partition、failover）。  分布式系统 bug 需要：  (a) 真实或带真实故障注入的模拟网络（Toxiproxy、Chaos Mesh，第23章）。(b) 多进程...
  c27_b09.md: ...实时序。(c) 对抗性调度（Jepsen 风格测试，第23章）。(d) 可变条件下的长时间运行测试。  单元测试遗漏的 bug：  * 网络抖动 + 重试下的重复扣款。 * 分区下的 split-brain。 * 领导者 failover 下的丢...
  c27_b10.md: ...正确” 之间的差距对分布式系统最大。对顺序单机代码，通过测试是正确性的良好证据。对分布式代码，通过测试只是弱证据；许多 bug 只在测试未构造的特定故障场景下可见。  **Q4.** 描述 AI 生成分布式代码的审查清单。  提示  10 项：幂...
  c27_b10.md: ...：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助 PR）按此清单审查，可捕获高频 LLM 生成 bug。清单不穷尽；覆盖最常见故障模式。  清单是纪律；需要训练。没有内化六个谎言的审查者会漏掉清单能捕获的 bug。本章（和...
  c27_b10.md: ...M 生成 bug。清单不穷尽；覆盖最常见故障模式。  清单是纪律；需要训练。没有内化六个谎言的审查者会漏掉清单能捕获的 bug。本章（和本书）是训练；清单是运营应用。...
  c27_b12.md: ...可行，因为每个谎言都可凭检查识别。受过六个谎言训练的审查者几分钟内捕获；未受训练的审查者会批准 PR，然后在生产中发现 bug。  **Q2.** 一个团队正在推广 AI 辅助代码生成。他们想保持分布式代码质量。提出一个政策。  提示  政策组成...
  c27_b12.md: ...**：高级工程师每季度审计已合并分布式代码 PR 样本。发现与团队共享；模式级问题导致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。...
  c27_b12.md: ...重复扣款事故后起草了类似政策。三年后，团队已内化纪律；AI 辅助 PR 按清单审查；混沌测试是标准；生产中新分布式代码 bug 罕见。  **Q3.** 一个 LLM 生成 PR 包含复杂分布式操作。PR 描述声称 “使用 saga 模式实现跨服...
  c27_b13.md: ...Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的（孤儿部分事务）。...
  c27_b14.md: ...“AI 辅助代码生成应被禁止用于分布式代码。风险太高。”  **讨论：** 辩护：分布式代码需要 LLM 不擅长的推理；bug 沉默、昂贵且难检测。禁止 AI 辅助迫使人工工程师仔细推理；工程质量更高。  反驳：AI 辅助即使对分布式代码也有真实...
  c27_b14.md: ...从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别遗漏模式；团队培训和政策更新。  要点...
  c27_b14.md: ...*：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别遗漏模式；团队培训和政策更新。  要点：“仔细审查” 是起点但不是系统。规模化的分布式系统质量...
  c27_b14.md: ...“AI 辅助代码生成应被禁止用于分布式代码。风险太高。”  **讨论：** 辩护：分布式代码需要 LLM 不擅长的推理；bug 沉默、昂贵且难检测。禁止 AI 辅助迫使人工工程师仔细推理；工程质量更高。  反驳：AI 辅助即使对分布式代码也有真实...
  c27_b14.md: ...从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别遗漏模式；团队培训和政策更新。  要点...
  c27_b14.md: ...*：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别遗漏模式；团队培训和政策更新。  要点：“仔细审查” 是起点但不是系统。规模化的分布式系统质量...
  c27_b15.md: ...故障罕见；(6) CAP 分类简单。每个都是 LLM 生成分布式代码的默认模式。  LLM 生成分布式代码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故障场景下可见。成本高：重复扣款、丢失写、split-brain、级联故障。...
  c27_b15.md: ...LLM 结构性地不擅长分布式代码：训练数据偏差（简单模式过度代表）；模式匹配而非推理；不变量在代码中不可见；测试抓不到 bug；bug 沉默。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人类用清单审查。LLM 是生产力工...
  c27_b15.md: ...结构性地不擅长分布式代码：训练数据偏差（简单模式过度代表）；模式匹配而非推理；不变量在代码中不可见；测试抓不到 bug；bug 沉默。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人类用清单审查。LLM 是生产力工具；不是...
  c27_b15.md: ...故障罕见；(6) CAP 分类简单。每个都是 LLM 生成分布式代码的默认模式。  LLM 生成分布式代码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故障场景下可见。成本高：重复扣款、丢失写、split-brain、级联故障。...
  c27_b15.md: ...LLM 结构性地不擅长分布式代码：训练数据偏差（简单模式过度代表）；模式匹配而非推理；不变量在代码中不可见；测试抓不到 bug；bug 沉默。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人类用清单审查。LLM 是生产力工...
  c27_b15.md: ...结构性地不擅长分布式代码：训练数据偏差（简单模式过度代表）；模式匹配而非推理；不变量在代码中不可见；测试抓不到 bug；bug 沉默。  2026 含义：AI 辅助代码生成对很多事有用；对分布式代码，需要人类用清单审查。LLM 是生产力工具；不是...
  c28_b08.md: ...到被验证。  2026 年内化这种姿态的工程师构建出 Crescent 这样的系统。没有内化者产出第1章的原始重复扣款 bug，以及许多类似 bug，并不断惊讶于 “测试通过的系统怎么在生产中坏了。”...
  c28_b08.md: ...年内化这种姿态的工程师构建出 Crescent 这样的系统。没有内化者产出第1章的原始重复扣款 bug，以及许多类似 bug，并不断惊讶于 “测试通过的系统怎么在生产中坏了。”...
  c28_b09.md: ...容忍用 Raft 复制协调器）。  * 如何预防（诊断方法、混沌工程纪律、LLM 代码审查清单）。 * 如何构建现在无 bug 处理 Adaeze 支付的系统（第28章架构）。  教学大纲中的 12 个核心工作流全部出现。每个都作为一张卡片摘要在...
  c28_b10.md: ...；记录学习；行动项 | 演练前设计 + 当天执行 + 无责复盘 | 跳过影响半径控制；无 rollback 计划；发现 bug 却继续后续 game day 而不修复 | | 12 | vLLM 分离集群 | Ch.25 §25.5 | 每月 1...
  c28_b12.md: ...什么 LLM 不擅长这个，以及第28章的综合。内化纪律的读者能构建 Crescent 这样的系统；没有内化的读者产出有 bug 的系统。  **Q2.** 指出下列原语分别由哪一章引入：HLC、fencing token、Raft、原子广播、CR...
  c28_b15.md: ...Crescent 系统健壮；这种纪律比任何单个工程师的具体知识更重要。  **Q3.** 反思你遇到过的一个分布式系统 bug（生产或课程中）。用三个谎言语言识别它是哪个谎言，以及什么防御能防止它。  提示  用三个谎言语言重构 bug；找出缺失...
  c28_b15.md: ...布式系统 bug（生产或课程中）。用三个谎言语言识别它是哪个谎言，以及什么防御能防止它。  提示  用三个谎言语言重构 bug；找出缺失的防御。  **答案：** 常见模式：一个 worker 池消费队列；每个 worker 处理一个 job；偶...
  c28_b15.md: ...时的 worker 池是标准模式；假设 “超时保护我们” 就是谎言。防御是下游幂等，无论运维模式看起来多可靠。  这类 bug 在分布式系统工程师职业生涯中反复出现。每次出现都是一课；三个谎言分析纪律能发现缺口；修复成为工程师标准实践的一部分。...
  c28_b18.md: ...设最坏情况；识别谎言；应用防御；用混沌工程验证。  2026 年内化这一点的工程师构建健壮系统。没有内化者产出重复扣款 bug。区别不在于具体算法知识；而在于一致应用的不信任纪律。  本书提供了模型、原语和运维纪律。读者完成各章后拥有工具包。下一...
  c30_b08.md: ...（分布式追踪）：trace 数据揭示了循环；没有追踪，诊断会耗时更久。     * Ch.27（LLM 生成代码审查）：bug 模式未被 AI 辅助审查流水线捕获，因为循环条件依赖输入数据而非代码形状。   #### E.4 — CrowdStr...
  c30_b09.md: ...loud BigQuery 存储故障，2023-08-08  * **触发**：BigQuery 存储写入 API 的 bug 导致存储层开始拒绝特定表类的写入。 * **放大**：客户端应用重试；重试放大拒绝率；拒绝指标越过告警阈值，触发自动区...
  c30_b09.md: ...*：客户端应用重试；重试放大拒绝率；拒绝指标越过告警阈值，触发自动区域 failover，但 failover 不包含 bug 修复。 * **时间线**：受影响工作负载的 BigQuery 写入部分中断 4 小时。 * **恢复**：部署 bu...
  c30_b09.md: ...ug 修复。 * **时间线**：受影响工作负载的 BigQuery 写入部分中断 4 小时。 * **恢复**：部署 bug 修复；手动重置区域 failover 状态。 * **章节映射防御**：     * Ch.21（级联故障，重试风暴）...
  c30_b09.md: ...式；更好的 failover 纪律（跨区域 failover 需人工介入）有帮助。     * Ch.27（审查清单）：bug 修复应被审查 "跨区域回滚时会发生什么"；未覆盖到 failover-without-the-fix 场景。   ##...
  c30_b09.md: ...* **触发**：客户推送了一个有效配置，触发了 Fastly 基于 Varnish 的 CDN 平台中此前未知的软件 bug。该 bug 导致配置解析器在特定输入模式上崩溃。 * **放大**：坏配置被分发到全球所有 Fastly CDN 节点...
  c30_b09.md: ...**：客户推送了一个有效配置，触发了 Fastly 基于 Varnish 的 CDN 平台中此前未知的软件 bug。该 bug 导致配置解析器在特定输入模式上崩溃。 * **放大**：坏配置被分发到全球所有 Fastly CDN 节点；每个节点崩...
  c30_b16.md: ...片的一词版本，正是 LLM 被要求写分布式代码时复现的版本。内化了这 25 条的审查者能在数秒内捕获高频 LLM 生成 bug。...

=== game (132 occurrences) ===
  c01_b08.md: ...。复盘命名了系统一直讲述的三个谎言。银行向 Adaeze 退款。Crescent 在内部发布了复盘，并在下周进行了一次 game day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya...
  c21_b04.md: ...(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证修复。  事后复盘模式：识别触发器、放大和防御缺失。每一项都是独立修复。防御缺失（上面 c）通常...
  c22_b11.md: ...第23章讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game...
  c22_b11.md: ...。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c22_b11.md: ...第23章讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game...
  c22_b11.md: ...。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御”...
  c23_b01.md: ...Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game...
  c23_b01.md: ...game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2...
  c23_b01.md: ...对不可避免故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习...
  c23_b01.md: ...y  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  Game day 结构：  (a) **赛前（1-4 周前）**：设计实验。定义要注入的故障、稳态假设、回滚计划、参与者和角色。...
  c23_b01.md: ...*恢复与复盘**：结束实验（回滚或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智...
  c23_b01.md: ...或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴露运维缺口。...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御”...
  c23_b01.md: ...Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game...
  c23_b01.md: ...game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2...
  c23_b01.md: ...对不可避免故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习...
  c23_b01.md: ...y  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  Game day 结构：  (a) **赛前（1-4 周前）**：设计实验。定义要注入的故障、稳态假设、回滚计划、参与者和角色。...
  c23_b01.md: ...*恢复与复盘**：结束实验（回滚或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智...
  c23_b01.md: ...或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴露运维缺口。...
  c23_b02.md: ...esiliency in Practice》）是混沌工程组织方面的权威声音。  关键实践：  (a) **心理安全**。Game day 必须让团队能无责地识别问题。若失败暴露运维缺口，讨论应是 “我们如何修复系统” 而非 “谁负责”。没有心理安全...
  c23_b02.md: ...因（防御缺失、runbook 错误）而非个人错误。责备产生防御行为；系统改进需要诚实分析。  (c) **多元参与**。Game day 不仅应包括 on-call 工程师，还应包括产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什...
  c23_b02.md: ...产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什么” 有不同视角。  (d) **记录学习**。每次 game day 都产生书面产物：测试了什么、发生了什么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构...
  c23_b02.md: ...、发生了什么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构记忆。  (e) **固定节奏**。Game day 不是一次性事件。每月或每季度节奏保持肌肉锻炼；新成员通过参与学习。  组织实践才是区分 “我们用 Chaos...
  c23_b02.md: ...esiliency in Practice》）是混沌工程组织方面的权威声音。  关键实践：  (a) **心理安全**。Game day 必须让团队能无责地识别问题。若失败暴露运维缺口，讨论应是 “我们如何修复系统” 而非 “谁负责”。没有心理安全...
  c23_b02.md: ...因（防御缺失、runbook 错误）而非个人错误。责备产生防御行为；系统改进需要诚实分析。  (c) **多元参与**。Game day 不仅应包括 on-call 工程师，还应包括产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什...
  c23_b02.md: ...产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什么” 有不同视角。  (d) **记录学习**。每次 game day 都产生书面产物：测试了什么、发生了什么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构...
  c23_b02.md: ...、发生了什么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构记忆。  (e) **固定节奏**。Game day 不是一次性事件。每月或每季度节奏保持肌肉锻炼；新成员通过参与学习。  组织实践才是区分 “我们用 Chaos...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群...
  c23_b04.md: ...标 10 秒检测。 * 在客服面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障...
  c23_b04.md: ...eader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群...
  c23_b04.md: ...标 10 秒检测。 * 在客服面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障...
  c23_b04.md: ...eader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b05.md: ...vel 2（Game days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey...
  c23_b06.md: ...境无法复制的属性（规模、流量模式、完整依赖图）。实验室测试必要但不充分。  (d) **自动化实验以持续运行**。手动 game day 有价值；Chaos Monkey 风格的持续自动混沌让系统永久被测试。  (e) **最小化爆炸半径**。从小...
  c23_b06.md: ...us、Gremlin）实现它们；组织实践（game day、无责复盘、记录学习）让它们落地。  **Q2.** 什么是 game day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day...
  c23_b06.md: ...什么是 game day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day 是一次 scheduled、故意的混沌练习。团队聚集；注入故障；观察系统反应；记录学习。  结构：  (a)...
  c23_b06.md: ...外。  (e) **赛后**：  * 提交改进行动项（修复 bug、更新 runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏...
  c23_b06.md: ...g、更新 runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏保持团队的故障响应肌肉锻炼。  **Q3.** 为什么心理安全在...
  c23_b06.md: ...团队的故障响应肌肉锻炼。  **Q3.** 为什么心理安全在混沌工程实践中很重要？  提示  没有安全，团队会隐藏问题。Game day 暴露问题；安全让团队诚实讨论。...
  c23_b06.md: ...境无法复制的属性（规模、流量模式、完整依赖图）。实验室测试必要但不充分。  (d) **自动化实验以持续运行**。手动 game day 有价值；Chaos Monkey 风格的持续自动混沌让系统永久被测试。  (e) **最小化爆炸半径**。从小...
  c23_b06.md: ...us、Gremlin）实现它们；组织实践（game day、无责复盘、记录学习）让它们落地。  **Q2.** 什么是 game day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day...
  c23_b06.md: ...什么是 game day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day 是一次 scheduled、故意的混沌练习。团队聚集；注入故障；观察系统反应；记录学习。  结构：  (a)...
  c23_b06.md: ...外。  (e) **赛后**：  * 提交改进行动项（修复 bug、更新 runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏...
  c23_b06.md: ...g、更新 runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏保持团队的故障响应肌肉锻炼。  **Q3.** 为什么心理安全在...
  c23_b06.md: ...团队的故障响应肌肉锻炼。  **Q3.** 为什么心理安全在混沌工程实践中很重要？  提示  没有安全，团队会隐藏问题。Game day 暴露问题；安全让团队诚实讨论。...
  c23_b07.md: ...**答案：** Game day 暴露问题：团队不知道的故障模式、运维缺口、潜伏的 bug。赛后讨论是捕获学习的机会。  若团队害怕（害怕责备、...
  c23_b07.md: ...习的机会。  若团队害怕（害怕责备、害怕显得无能、害怕对缺口负责），他们会隐藏问题。他们会淡化意外、缩小缺口、避免升级。Game day 的价值就被摧毁。  心理安全意味着：团队可以在不被个人责备的情况下指出问题。“重试预算太高，导致级联” 是系统...
  c23_b09.md: ...#### 应用问题  **Q1.** Olamide 正在策划一次测试区域故障转移的 game day。走一遍她在当天前需要回答的设计问题。  提示  稳态假设、爆炸半径、回滚计划、参与者、成功标准、runbook...
  c23_b09.md: ...所选 SLI 的实时面板；需要回滚时触发的告警；每个操作的日志。  (i) **会产生哪些 runbook 更新？** Game day 会暴露 runbook 缺口；捕获流程是什么？  (j) **沟通计划是什么？** 之前/期间/之后通知谁？如...
  c23_b09.md: ...#### 应用问题  **Q1.** Olamide 正在策划一次测试区域故障转移的 game day。走一遍她在当天前需要回答的设计问题。  提示  稳态假设、爆炸半径、回滚计划、参与者、成功标准、runbook...
  c23_b09.md: ...所选 SLI 的实时面板；需要回滚时触发的告警；每个操作的日志。  (i) **会产生哪些 runbook 更新？** Game day 会暴露 runbook 缺口；捕获流程是什么？  (j) **沟通计划是什么？** 之前/期间/之后通知谁？如...
  c23_b10.md: ...haos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加...
  c23_b10.md: ...s Monkey 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加网络分区混沌、慢节点混沌、为区域故障运行 game day。每次扩展都会发现新问题。  更深观察：混沌工程不是 “完成” 的状态。它是随系统成长的持续实践。随着系统增加功...
  c23_b10.md: ...成新依赖、扩展到新区域，混沌面也在增长。成熟混沌工程实践把它视为持续投资，而非一次性里程碑。  **Q3.** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复...
  c23_b10.md: ...**Q3.** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问...
  c23_b10.md: ...致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答...
  c23_b10.md: ...复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续...
  c23_b10.md: ...day 测试正确的东西。  **答案：** 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行动项清单并按优先级修复。 * 缺点：已知...
  c23_b10.md: ...* 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行动项清单并按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风...
  c23_b10.md: ...：已知 bug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。...
  c23_b10.md: ...：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **Bug 严重程度**：若是数据损坏（如示例）...
  c23_b10.md: ...(a) **Bug 严重程度**：若是数据损坏（如示例），属于高严重。立即暂停修复；不要继续可能让更多客户暴露于损坏的 game day。  (b) **修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要...
  c23_b10.md: ...继续可能让更多客户暴露于损坏的 game day。  (b) **修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c...
  c23_b10.md: ...(b) **修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是...
  c23_b10.md: ...暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条件下触发，更灵活。...
  c23_b10.md: ...场景（故障转移下重复扣款）：bug 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 targeted 实验验证修复；修复验证后恢复常规 game day 计划。...
  c23_b10.md: ...单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 targeted 实验验证修复；修复验证后恢复常规 game day 计划。...
  c23_b11.md: ...更深观察：game day 的价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在...
  c23_b12.md: ...；问题是失败是惊喜还是排练过。混沌工程把惊喜变成排练。没有混沌工程的系统，每次失败都是惊喜；团队从生产事故（昂贵）而非 game day（廉价）学习。对任务关键系统，这是运维失职。  反驳：混沌工程有成本（工程师时间、复杂度、练习期间偶尔的客户可见...
  c23_b12.md: ...适级别是什么”。  2026 现实：成熟 SRE 组织的混沌工程实践与系统关键性成比例。Crescent 支付系统每月 game day；内部 HR 系统没有。两者在其上下文中都正确。  第二个考虑：“运维失职” 的说法太强，因为它暗示跨越团队边界...
  c23_b12.md: ...具**：是的，列出工具（Chaos Mesh、Litmus、AWS FIS 等）及其对团队技术栈的适配。  (b) **Game day 结构**：赛前/当天/执行/复盘/赛后的模式。团队需要运营 playbook，不只是工具。  (c) **组织...
  c23_b12.md: ...；问题是失败是惊喜还是排练过。混沌工程把惊喜变成排练。没有混沌工程的系统，每次失败都是惊喜；团队从生产事故（昂贵）而非 game day（廉价）学习。对任务关键系统，这是运维失职。  反驳：混沌工程有成本（工程师时间、复杂度、练习期间偶尔的客户可见...
  c23_b12.md: ...适级别是什么”。  2026 现实：成熟 SRE 组织的混沌工程实践与系统关键性成比例。Crescent 支付系统每月 game day；内部 HR 系统没有。两者在其上下文中都正确。  第二个考虑：“运维失职” 的说法太强，因为它暗示跨越团队边界...
  c23_b12.md: ...具**：是的，列出工具（Chaos Mesh、Litmus、AWS FIS 等）及其对团队技术栈的适配。  (b) **Game day 结构**：赛前/当天/执行/复盘/赛后的模式。团队需要运营 playbook，不只是工具。  (c) **组织...
  c23_b13.md: ...(f) **混沌工程自身指标**：团队如何知道其混沌实践有效？指标如 “每次 game day 发现的问题数”、“混沌工程预防的事故数”、“真实事故中缓解时间（因排练而缩短）”。  (g) **沟通与认同*...
  c23_b13.md: ...LLM 遗漏了组织维度。  正确 LLM 回应：“混沌工程既是技术也是组织。工具包括 [...]。实践包括带无责复盘的 game day。对起步团队，我推荐 [...]。以下是 3 个月建立该计划 [...]。工具只是答案的一部分。”   ###...
  c23_b13.md: ...程是故意注入故障以验证系统故障下行为的实践。五条原则：稳态假设、变化真实事件、在生产中运行、自动化、最小化爆炸半径。  Game day 是 scheduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性...
  c23_b13.md: ...混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功...
  c23_b13.md: ...工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它在客户之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学...
  c23_b13.md: ...(f) **混沌工程自身指标**：团队如何知道其混沌实践有效？指标如 “每次 game day 发现的问题数”、“混沌工程预防的事故数”、“真实事故中缓解时间（因排练而缩短）”。  (g) **沟通与认同*...
  c23_b13.md: ...LLM 遗漏了组织维度。  正确 LLM 回应：“混沌工程既是技术也是组织。工具包括 [...]。实践包括带无责复盘的 game day。对起步团队，我推荐 [...]。以下是 3 个月建立该计划 [...]。工具只是答案的一部分。”   ###...
  c23_b13.md: ...程是故意注入故障以验证系统故障下行为的实践。五条原则：稳态假设、变化真实事件、在生产中运行、自动化、最小化爆炸半径。  Game day 是 scheduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性...
  c23_b13.md: ...混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功...
  c23_b13.md: ...工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它在客户之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学...
  c27_b07.md: ...ewed” 签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态...
  c27_b07.md: ...，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态审查的经验补充。 * 成熟度映射：第三层对应第23章 §...
  c27_b07.md: ...的经验补充。 * 成熟度映射：第三层对应第23章 §23.7 的混沌工程成熟度 Level 2-3（scheduled game days 和持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到 Level 3 意味着混沌节奏是持续而...
  c27_b07.md: ...节奏是持续而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查 → 人类审查 → 混沌验证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 A...
  c27_b07.md: ...→ 混沌验证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下...
  c27_b07.md: ...必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点...
  c27_b07.md: ...reaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23...
  c27_b07.md: ...“saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsif...
  c27_b07.md: ...rker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿...
  c27_b07.md: ...维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-...
  c27_b07.md: ...game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Ol...
  c27_b07.md: ...流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流...
  c27_b07.md: ...态经 Raft 复制；步骤 4（外部 partner API）有 0.1 重试预算和 50% 失败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢...
  c27_b07.md: ...ewed” 签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态...
  c27_b07.md: ...，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态审查的经验补充。 * 成熟度映射：第三层对应第23章 §...
  c27_b07.md: ...的经验补充。 * 成熟度映射：第三层对应第23章 §23.7 的混沌工程成熟度 Level 2-3（scheduled game days 和持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到 Level 3 意味着混沌节奏是持续而...
  c27_b07.md: ...节奏是持续而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查 → 人类审查 → 混沌验证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 A...
  c27_b07.md: ...→ 混沌验证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下...
  c27_b07.md: ...必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点...
  c27_b07.md: ...reaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23...
  c27_b07.md: ...“saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsif...
  c27_b07.md: ...rker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿...
  c27_b07.md: ...维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-...
  c27_b07.md: ...game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Ol...
  c27_b07.md: ...流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流...
  c27_b07.md: ...态经 Raft 复制；步骤 4（外部 partner API）有 0.1 重试预算和 50% 失败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢...
  c27_b12.md: ...。发现与团队共享；模式级问题导致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事...
  c27_b12.md: ...致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事后复盘识别六个谎言中哪些在起作...
  c27_b12.md: ...。发现与团队共享；模式级问题导致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事...
  c27_b12.md: ...致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事后复盘识别六个谎言中哪些在起作...
  c27_b14.md: ...(e) **合并后审计**：高级工程师重审已合并 PR 样本；遗漏被记录；团队从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后...
  c27_b14.md: ...重审已合并 PR 样本；遗漏被记录；团队从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别...
  c28_b05.md: ...| E.1（S3 亚稳态）、E.2（Roblox）、E.6（BigQuery） | | 23 | 混沌工程 | 每月 game day、Jepsen 风格验证 | E.4（CrowdStrike 无 canary）、E.7（Fastly 无 ca...
  c28_b06.md: ...。团队 06:00 UTC 集合（覆盖所有 region 的工作时间）。  (a) **发布前检查清单**：两周前混沌 game day；所有 action items 已关闭。容量测试通过。客户迁移计划已审。Rollback 计划已审。客服已通报...
  c28_b10.md: ...| 演练前设计 + 当天执行 + 无责复盘 | 跳过影响半径控制；无 rollback 计划；发现 bug 却继续后续 game day 而不修复 | | 12 | vLLM 分离集群 | Ch.25 §25.5 | 每月 100 万美元以上 LL...
  c28_b16.md: ...指标（第22章）给系统加仪表。没有可见性，安全变更无法做到。  (d) **把混沌工程作为检查点**：在当前系统上运行 game day（第23章）。记录什么会坏；什么令人意外。用发现来排序。  (e) **第一季度修复前 3 个缺口**：典型首批...
  c28_b16.md: ...作幂等性。每个修复是一个独立 PR；每个可测试。  (f) **持续增量改进**：每季度修复下一批缺口。每批后运行混沌 game day 验证。  (g) **教育团队**：修复进入时，告诉团队原因。每个 PR 都是传播纪律的机会。一年后，团队集体...
  c29_b02.md: ...logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出 game-day 场景；人类接受或扩展。  第27章 §27.10 的三层架构（AI 审查 → 人类审查 → 混沌验证）可能持续...
  c30_b07.md: ...）：多层级重试放大。防御：每个客户端设置重试预算。     * Ch.23（混沌工程）："操作者移除过多容量" 应作为 game-day 场景；恢复路径（限流 + 重启）应经过演练。...
  c30_b08.md: ...服务。     * Ch.23（混沌工程）：该具体故障模式（高 heartbeat 负载下的 Consul）部署前未做 game-day 测试。   #### E.3 — Cloudflare 邮件路由故障，2024-04  * **触发**：一次...
  c30_b08.md: ...手动恢复（启动到安全模式，删除坏 sensor 文件）。 * **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。...
  c30_b08.md: ...23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触发）：把部署视为触发；分阶段 rollout（1% → 10%...
  c30_b09.md: ...n）；全局原子 rollout 是反模式。     * Ch.23（混沌工程）："配置触发所有节点解析器崩溃" 应作为 game-day 场景。     * Ch.21（级联故障）：同时全球崩溃是无阶段 rollout 的最坏情况。...
  c30_b10.md: ...018 杠杆最高）。 * 对每个案例研究，问：六个谎言中哪些在起作用？缺失什么防御？Olamide 在此之前会运行什么 game day？  * * *   ### 附录 F — 生产决策树  针对五种常见工作负载模式，本附录提供端到端 “从需求到...

=== fencing (122 occurrences) ===
  c01_b08.md: ...试。  现在回到 Adaeze。早上 6:00，Olamide 有了修复方案。到 9:00，她完成了事后复盘。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章），以及用于协调者状态的 Raft 支持提交日志（第9章）。复盘命名了系统...
  c01_b09.md: ...每个谎言分别走一遍，什么本可以预防它？为什么单一修复不够？  提示  每个谎言需要不同纪律。幂等 token 解决一个。fencing token 带来的协调者唯一性解决另一个。时钟偏移有界的时间戳解决第三个。  **答案：** 对于失败谎言，请求上的幂...
  c01_b09.md: ...方的提交在 HLC 时间中先发生，即使两者都在 NTP 时钟的 5 ms 窗口内完成。  对于身份谎言，协调者角色上的 fencing token（第18章）会让银行拒绝第二次提交，因为第二个协调者的 token 会低于第一个。即使两者都相信自己是活跃协...
  c01_b11.md: ...一网络路径上的新领导者被选出，分区恢复后，原领导者从自己的视角仍作为领导者行动。  第三，即使锁确实被唯一持有，也没有 fencing token。一条旧领导者在锁过期前发出、但尚未送达下游系统的动作，在新领导者发出自己的动作后到达下游。下游系统无法区分...
  c01_b12.md: ...修复方案是 fencing token，第18章会覆盖：每次获取锁都会增加一个单调计数器，每个下游动作都携带该 token。下游拒绝任何低于它所见...
  c01_b15.md: ...上发生了什么”与“系统整体经历了什么”之间的缺口里。  Crescent 事件在单一重试中同时利用了三个谎言。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章）和 Raft 支持的提交日志（第9章）。单一修复无法弥合这个缺口。  这...
  c03_b15.md: ...ey=lambda m: m.timestamp)` 错误。  第1章提到 Crescent 多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；...
  c03_b15.md: ...多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；Raft 在第9章；Spanner / CockroachDB / Calvin / True...
  c03_b15.md: ...ey=lambda m: m.timestamp)` 错误。  第1章提到 Crescent 多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；...
  c03_b15.md: ...多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语。Fencing token 在第18章；Raft 在第9章；Spanner / CockroachDB / Calvin / True...
  c17_b11.md: ...启第六部分：协调原语。分布式缓存（Redis Cluster gossip；失效模式）和分布式锁（Redlock 辩论；fencing token 作为正确原语）。Crescent 支付系统使用 fencing token 保证协调者唯一性；本章解释这是...
  c17_b11.md: ...失效模式）和分布式锁（Redlock 辩论；fencing token 作为正确原语）。Crescent 支付系统使用 fencing token 保证协调者唯一性；本章解释这是防御 “身份谎言” 的正确手段。  ---...
  c18_b01.md: ...分布式锁（具体是为什么朴素锁会失败、正确原语长什么样）。第1章 Crescent 支付系统 “身份谎言” 的修复使用了 fencing token；本章解释原因。   ### 18.1 分布式缓存  缓存是一种性能模式：把昂贵操作结果存在快速查找存储中，...
  c18_b03.md: ...edis 侧看更早过期。两个客户端可能同时认为自己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前...
  c18_b03.md: ...端可能同时认为自己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该...
  c18_b03.md: ...即使客户端对时间看法 wildly 不同，资源的 “最高已见 token” 提供正确排序。  代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用...
  c18_b03.md: ...保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。” 这过于绝对。...
  c18_b03.md: ...任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：用 fencing token。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源...
  c18_b03.md: ...en。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-...
  c18_b03.md: ...edis 侧看更早过期。两个客户端可能同时认为自己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前...
  c18_b03.md: ...端可能同时认为自己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 token 高）。该...
  c18_b03.md: ...即使客户端对时间看法 wildly 不同，资源的 “最高已见 token” 提供正确排序。  代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用...
  c18_b03.md: ...保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。” 这过于绝对。...
  c18_b03.md: ...任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：用 fencing token。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源...
  c18_b03.md: ...en。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-...
  c18_b04.md: ...t 的修复  回顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencin...
  c18_b04.md: ...encing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若...
  c18_b04.md: ...ken 更高的能行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagr...
  c18_b04.md: ...cent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram         autonumber...
  c18_b04.md: ...n 检查拒绝其提交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 fencing token）的组合是正确生产模式。朴素 Redlock 是错误模式；正确模式更复杂，但复杂性必要。...
  c18_b04.md: ...t 的修复  回顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencin...
  c18_b04.md: ...encing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得协调者角色时分配一个 fencing token。  (b) 银行支付端点存储已接受的最高 token。任何带更低 token 的提交被拒绝。  (c) 若...
  c18_b04.md: ...ken 更高的能行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagr...
  c18_b04.md: ...cent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fencing-token 机制可视化：       sequenceDiagram         autonumber...
  c18_b04.md: ...n 检查拒绝其提交。两个协调者可以短暂共存；只有一个能修改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 fencing token）的组合是正确生产模式。朴素 Redlock 是错误模式；正确模式更复杂，但复杂性必要。...
  c18_b05.md: ...### 18.4 缓存作为锁的反模式  一个特定反模式：没有 fencing token 时把缓存当分布式锁。       # WRONG     if cache.set("user_proce...
  c18_b05.md: ...者。  修复：对需要正确性的锁不要用缓存。用 etcd、Consul 或 ZooKeeper 做锁；在受保护资源上执行 fencing token。性能代价小（锁获取罕见）；正确性收益大。  对纯效率锁（job queue 去重、幂等提示），基于缓存的锁...
  c18_b06.md: ...供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯...
  c18_b06.md: ...有效可能意见不一。  两者都是基于超时协议的知名问题，也适用于许多朴素分布式锁方案，不只是 Redlock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token...
  c18_b06.md: ...低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fen...
  c18_b06.md: ...ing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fencing token，它如何修复分布式锁正确性问题？  提示  单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “...
  c18_b06.md: ...单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “锁”，只有更高 token 能行动。  **答案：** Fencing token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 to...
  c18_b06.md: ...各自获得它们认为的锁（若锁服务用共识则不可能，但对朴素设置可以想象），token 不同。资源只接受更高 token。  Fencing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：即使锁服务有 bug...
  c18_b06.md: ...），资源的单调检查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统（数据库、消息队列）原生不支持 fencing token；应用或中间件必须叠加。对正确性关键用例，这是必要工作。  **Q3.** 基于缓存的 Redlock 式锁...
  c18_b06.md: ...供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯...
  c18_b06.md: ...有效可能意见不一。  两者都是基于超时协议的知名问题，也适用于许多朴素分布式锁方案，不只是 Redlock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 token；任何更低 token...
  c18_b06.md: ...低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fen...
  c18_b06.md: ...ing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fencing token，它如何修复分布式锁正确性问题？  提示  单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “...
  c18_b06.md: ...单调递增计数器；存储拒绝更低 token；即使两个客户端持有 “锁”，只有更高 token 能行动。  **答案：** Fencing token 是锁服务在每次锁获取时分配的单调递增计数器。每次获取获得一个比之前任何已发 token 严格更高的新 to...
  c18_b06.md: ...各自获得它们认为的锁（若锁服务用共识则不可能，但对朴素设置可以想象），token 不同。资源只接受更高 token。  Fencing token 把安全责任从 “锁服务保证唯一性” 转移到 “受保护资源强制执行排序”。结构上更健壮：即使锁服务有 bug...
  c18_b06.md: ...），资源的单调检查也能捕获。  代价：受保护资源必须实现 token 跟踪。许多存储系统（数据库、消息队列）原生不支持 fencing token；应用或中间件必须叠加。对正确性关键用例，这是必要工作。  **Q3.** 基于缓存的 Redlock 式锁...
  c18_b07.md: ...界的重复后果。  纪律：选择锁前先分类用例。正确性用例使用基于共识的锁（etcd、Consul、ZooKeeper）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token...
  c18_b07.md: ...per）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Cre...
  c18_b07.md: ...b queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operati...
  c18_b07.md: ...就谁持有锁达成一致。没有它，朴素锁方案可能在分区或故障转移期间把同一把锁发给两个客户端。  (b) **受保护资源上的 fencing token** ：保证资源上的操作按 token 排序。即使锁服务出错（或锁持有者在别人获得锁后暂停恢复），资源强制执...
  c18_b07.md: ...户端进程暂停。从 Raft 视角持有 “有效” token 的暂停客户端仍可在另一个客户端获得锁后操作资源。 * 只有 Fencing token：提供操作级排序，但不提供 “当前持有者” 概念。没有锁服务，每个操作都要求资源与所有潜在持有者协商。  组...
  c18_b07.md: ...持有者协商。  组合是正确生产模式。实践中：etcd 或 Consul 提供锁服务（基于 Raft）；应用在存储层实现 fencing-token 检查（或依赖支持它的存储系统，如带版本列检查的数据库）。...
  c18_b07.md: ...界的重复后果。  纪律：选择锁前先分类用例。正确性用例使用基于共识的锁（etcd、Consul、ZooKeeper）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token...
  c18_b07.md: ...per）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Cre...
  c18_b07.md: ...b queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operati...
  c18_b07.md: ...就谁持有锁达成一致。没有它，朴素锁方案可能在分区或故障转移期间把同一把锁发给两个客户端。  (b) **受保护资源上的 fencing token** ：保证资源上的操作按 token 排序。即使锁服务出错（或锁持有者在别人获得锁后暂停恢复），资源强制执...
  c18_b07.md: ...户端进程暂停。从 Raft 视角持有 “有效” token 的暂停客户端仍可在另一个客户端获得锁后操作资源。 * 只有 Fencing token：提供操作级排序，但不提供 “当前持有者” 概念。没有锁服务，每个操作都要求资源与所有潜在持有者协商。  组...
  c18_b07.md: ...持有者协商。  组合是正确生产模式。实践中：etcd 或 Consul 提供锁服务（基于 Raft）；应用在存储层实现 fencing-token 检查（或依赖支持它的存储系统，如带版本列检查的数据库）。...
  c18_b08.md: ...个团队实现 job queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。...
  c18_b08.md: ...施。若任务幂等（偶尔双处理浪费但无害）可接受。若任务有非幂等副作用（发邮件、扣款、发货）则不可接受。  (b) **带 fencing token 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性...
  c18_b08.md: ...en 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fencing token 防止过期持有者问题。每次获取较慢（etcd 写是多数复制）。  (c) **数据库行级锁** ：数据库中的...
  c18_b08.md: ...现代系统通常可围绕专用队列基础设施设计。  **Q2.** Anya 正在为 Stratus DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。ran...
  c18_b08.md: ...aft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的 etcd 锁。实现：  (a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd...
  c18_b08.md: ...锁。实现：  (a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd 锁获取。每次获取分配一个 fencing token（来自 etcd revision 号的单调递增计数器）。  (b) 领导者的后续操作（发起 range s...
  c18_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“2026 年所有分布式锁都应使用 fencing token。不提供它的锁服务已过时。”  **讨论：** 辩护：fencing token 是正确性的正确原语。任何不...
  c18_b10.md: ...驳：“2026 年所有分布式锁都应使用 fencing token。不提供它的锁服务已过时。”  **讨论：** 辩护：fencing token 是正确性的正确原语。任何不提供它的锁服务都有微妙安全洞。随着分布式系统成熟，领域应标准化在正确原语上。...
  c18_b10.md: ...正确原语。任何不提供它的锁服务都有微妙安全洞。随着分布式系统成熟，领域应标准化在正确原语上。  反驳：许多锁用例纯效率；fencing token 对它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用...
  c18_b10.md: ...应标准化在正确原语上。  反驳：许多锁用例纯效率；fencing token 对它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合...
  c18_b10.md: ...token 对它们增加复杂度却无收益。要求所有锁都带 fencing token 的库让简单情况变难。“正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合：锁库应 _支持_ fencing token（作为选项）。正确...
  c18_b10.md: ...简单情况变难。“正确性用例用 fencing token，否则用缓存锁” 的纪律是合适粒度。  综合：锁库应 _支持_ fencing token（作为选项）。正确性用例应用使用它。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etc...
  c18_b10.md: ...。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etcd、Consul、ZooKeeper 都支持 fencing token 等价物（revision 号、sequence 号、czxid）。应用代码才是需要纪律的地方。“正确性用例...
  c18_b10.md: ...en 等价物（revision 号、sequence 号、czxid）。应用代码才是需要纪律的地方。“正确性用例始终用 fencing token” 作为 _正确性用例的默认_ 是对的，但作为 _通用默认_ 是错的。  第二个考虑：存储系统对 fenci...
  c18_b10.md: ...ncing token” 作为 _正确性用例的默认_ 是对的，但作为 _通用默认_ 是错的。  第二个考虑：存储系统对 fencing-token 检查的支持是限制因素。带乐观并发控制（版本列）的数据库天然支持。对象存储（S3）原生不支持；应用必须叠加。...
  c18_b10.md: ...持是限制因素。带乐观并发控制（版本列）的数据库天然支持。对象存储（S3）原生不支持；应用必须叠加。存储系统选择可能成为 fencing token 采用的瓶颈。  **Q2.** 一个 LLM 被要求 “为我们的微服务实现分布式锁”。它产出一个 Redl...
  c18_b10.md: ...M 没问部署。  (c) **受保护资源是什么**。若受保护资源自身有锁（数据库行锁），分布式锁冗余。若资源没有原生锁，fencing token 对锁安全至关重要。LLM 没问。  再三种（任一条都让 Redlock 不足）：  (d) **进程暂停行...
  c18_b10.md: ...种（任一条都让 Redlock 不足）：  (d) **进程暂停行为**。GC 暂停、VM 迁移持有的暂停进程锁在没有 fencing token 时很危险。LLM 没问暂停预期。  (e) **跨区域部署**。5 个 Redis master 跨区域的...
  c18_b11.md: ...这把锁保护什么？两个客户端同时持有它的失败模式是什么？进程可能暂停多久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。...
  c18_b11.md: ...暂停和时钟漂移下失败。正确原语是共识支持的锁服务（etcd、Consul、ZooKeeper）加受保护资源上强制执行的 fencing token。  第1章 Crescent 修复结合基于 Raft 的协调者选举与银行端点的 fencing token...
  c18_b11.md: ...强制执行的 fencing token。  第1章 Crescent 修复结合基于 Raft 的协调者选举与银行端点的 fencing token。即使两个协调者短暂认为自己在位，只有 token 更高者能提交。重复扣款不可能复发。  缓存作为锁是正确性...
  c18_b11.md: ...位，只有 token 更高者能提交。重复扣款不可能复发。  缓存作为锁是正确性用例的反模式。用共识支持的锁；在资源处用 fencing token。代价小；正确性收益大。  第19章讲服务发现与配置：etcd 作为现代默认（替代 ZooKeeper）、C...
  c19_b04.md: ...*直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul...
  c27_b01.md: ...y 的代码。    3. **机器时钟说真话**。跨机器比较时间戳的代码。    4. **朴素分布式锁有效**。没有 fencing token 的 Redlock。假设锁持有者唯一的代码。    5. **部分故障罕见**。只有 happy path...
  c27_b03.md: ...)   谎言：持有 Redis key 意味着唯一性。代码用 Redlock 风格锁保护正确性关键操作。  现实：没有 fencing token 的 Redlock 在进程暂停、时钟漂移和 Redis failover 下失败。两个客户端可能都相信自己...
  c27_b03.md: ...rk`；关键工作被污染。  修复：用 consensus-backed 锁（etcd、Consul）并在被保护资源上加 fencing token。       def process_unique_action(action_id):...
  c27_b05.md: ...序。用 HLC 或因果排序。  (f) **锁**：每个分布式锁对正确性场景应是 consensus-backed 加 fencing token。  (g) **部分故障处理**：每个多步操作应处理部分失败（saga、outbox、compensati...
  c27_b09.md: ...ll-clock 时间戳做排序。    4. **朴素分布式锁有效**：Redlock 风格代码假设锁持有者唯一；缺少 fencing token。    5. **部分故障罕见**：代码只有 happy path；不考虑 “半成功” 结果；无 saga...
  c27_b09.md: ...redis.set(key, "1", nx=True, ex=30): do_critical_work()`。无 fencing token 的 Redlock；并发持有者造成污染。  （谎言 5）`db.update(user); cache.u...
  c27_b10.md: ...)` 是错的。  (f) **锁**：分布式锁对正确性关键场景应是 consensus-backed（etcd 等）加 fencing token。  (g) **部分故障处理**：多步操作应处理部分失败（saga、outbox、compensating...
  c27_b12.md: ...戳。跨 region ledger 排序依赖机器时钟。    4. **谎言 4（锁）**：基于 Redis 的锁没有 fencing token。在 GC 暂停或分区下，两个协调器可能同时持有锁；两者都能处理同一笔支付。    5. **谎言 5（部分...
  c27_b12.md: ...调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5）；在误导性 CP 注释下读一致性模糊（谎言 6）。请用团...
  c27_b15.md: ...等性、超时、带预算重试、熔断器、排序原语（HLC 而非 wall clock）、consensus-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace context 传播、可观测性。每份分...
  c28_b02.md: ...**：Stratus DB（现为 Crescent 组件）。管理分布式事务状态；经 Raft 复制；对安全关键操作使用 fencing token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 k...
  c28_b02.md: ...voy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional...
  c28_b04.md: ...务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze 余额（第11/13/...
  c28_b04.md: ...情况跨 region quorum 物理限制）。  (10) 事务的 coordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4 防御）。5 ms。  (11) API...
  c28_b05.md: ...、因果、CRDT | Redis 缓存（最终）、Yjs（CRDT） | n/a | | 18 | 锁 | etcd + fencing token | E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Consul + Cloud...
  c28_b07.md: ...测性和诊断方法纪律的投资，造就了一个 ‘刚刚好使’ 的系统。”  **Anya（Stratus DB 主管）**：“带 fencing token 的 Raft 复制协调器是系统安全的核心。集成到 Crescent 技术栈很直接，因为 Stratus D...
  c28_b10.md: ...+ Raft 复制协调器 + HLC 版 MVCC | 协调器状态在易失内存；竞争下不确定性重启；下游 API 缺少 fencing token | | 9 | Yuki 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离...
  c28_b12.md: ...系统工程纪律是假设这些谎言正在发生，识别系统每个部分易受哪些谎言影响，并组合防御这些谎言的原语（时间用 HLC、身份用 fencing token、故障用重试/超时/熔断器/saga）。  工作工程师的姿态：对每个组件、每个连接、每个操作都假设最坏情况，...
  c28_b12.md: ...scent 这样的系统；没有内化的读者产出有 bug 的系统。  **Q2.** 指出下列原语分别由哪一章引入：HLC、fencing token、Raft、原子广播、CRDT、重试预算、混沌工程、KV-cache-aware 路由。  提示  各章大致...
  c28_b12.md: ...各章大致按顺序引入原语；查看目录。  **答案：**  * **HLC**：第3章（时间、序与说谎的时钟）。 * **Fencing token**：第18章（分布式锁）。 * **Raft**：第9章（实现友好的共识）。 * **Atomic bro...
  c28_b12.md: ...he-aware 路由**：第26章（LLM 推理负载均衡）。  每个原语解决三个谎言某一类表现。HLC 防御时间谎言；fencing token 防御身份谎言；带预算的重试防御故障谎言的放大。完整防御需要多个原语共同使用。  第28章的 Crescen...
  c28_b12.md: ...系统工程纪律是假设这些谎言正在发生，识别系统每个部分易受哪些谎言影响，并组合防御这些谎言的原语（时间用 HLC、身份用 fencing token、故障用重试/超时/熔断器/saga）。  工作工程师的姿态：对每个组件、每个连接、每个操作都假设最坏情况，...
  c28_b12.md: ...scent 这样的系统；没有内化的读者产出有 bug 的系统。  **Q2.** 指出下列原语分别由哪一章引入：HLC、fencing token、Raft、原子广播、CRDT、重试预算、混沌工程、KV-cache-aware 路由。  提示  各章大致...
  c28_b12.md: ...各章大致按顺序引入原语；查看目录。  **答案：**  * **HLC**：第3章（时间、序与说谎的时钟）。 * **Fencing token**：第18章（分布式锁）。 * **Raft**：第9章（实现友好的共识）。 * **Atomic bro...
  c28_b12.md: ...he-aware 路由**：第26章（LLM 推理负载均衡）。  每个原语解决三个谎言某一类表现。HLC 防御时间谎言；fencing token 防御身份谎言；带预算的重试防御故障谎言的放大。完整防御需要多个原语共同使用。  第28章的 Crescen...
  c28_b14.md: ...**身份**：Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revision 在编排层起类似作用。 * **故障**：异步复制可能丢失主库传播前...
  c28_b17.md: ...和 PACELC 量化这些谎言（第2章）。 * 如何用具体原语防御每个谎言——时间用 HLC（第3章），Raft 和 fencing token 用于身份（第9、18章），带预算的重试和 saga 用于故障（第13、21章）。 * 如何在 30 分钟内...
  c28_b18.md: ...AP/PACELC 分析；跨 region 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing token；追踪用 OpenTelemetry；韧性用重试预算和熔断器；验证用混沌工程；AI 辅助代码审查用六谎言清单...
  c30_b06.md: ...2026 年 LLM 推理模式；prefill（计算密集）和 decode（内存密集）在不同 GPU 上运行。 * **Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被保护资源强制执行令牌顺序。 * **FLP impossib...
  c30_b07.md: ...3 | HLC；因果/向量时钟；TrueTime | | 4 | 朴素分布式锁有效 | Ch.18 | 共识支持锁 + fencing token | | 5 | 部分故障罕见 | Ch.22 | Saga 模式；outbox；补偿动作 | | 6 |...
  c30_b09.md: ...* **恢复**：大量手动数据协调，部分提交永久丢失。 * **章节映射防御**：     * Ch.18（分布式锁，fencing token）：failover 自动化缺少 fencing token；新主库不应能在旧主库仍自认为主库时写入。...
  c30_b09.md: ...**章节映射防御**：     * Ch.18（分布式锁，fencing token）：failover 自动化缺少 fencing token；新主库不应能在旧主库仍自认为主库时写入。     * Ch.7（共识，防止 split-brain）：基于...
  c30_b09.md: ...in）：基于共识的 leader 选举可防止双主状态。     * Ch.5（复制，主库 failover 纪律）：无 fencing 的 failover 模式正是第5章警告的。   #### E.6 — Google Cloud BigQuery 存...
  c30_b16.md: ...21 | "把缓存当锁用。" | 基于缓存的锁在进程暂停、时钟漂移、缓存驱逐和 failover 下失败。用共识 + fencing token。 | Ch.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是...
  c30_b19.md: ...t quorum；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-token 检查 | 无 fencing 的朴素分布式锁 | Ch.18 §18.2（Redlock 批评），Ch.2...
  c30_b19.md: ...§6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-token 检查 | 无 fencing 的朴素分布式锁 | Ch.18 §18.2（Redlock 批评），Ch.27 §27.5   Trace 显示事务提...

=== proposer (113 occurrences) ===
  c08_b01.md: ...第9章）更容易实现，但骨架相同。   ### 8.1 场景  一组 N 个进程，每个扮演三种角色之一或多种：  * **Proposer** ：试图让某个值被选定。 * **Acceptor** ：对提议投票；崩溃后持久化状态。 * **Learner**...
  c08_b01.md: ...### 8.2 为什么朴素协议失败  在 Paxos 之前，先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给...
  c08_b01.md: ...* ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **...
  c08_b01.md: ...ptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 pro...
  c08_b01.md: ...有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 pro...
  c08_b01.md: ...ser 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3**...
  c08_b01.md: ...ser 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看...
  c08_b01.md: ...（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有...
  c08_b01.md: ...。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有值拿到多数；无法达成一致。  Paxos 通过引入两阶段（prepare、accep...
  c08_b01.md: ...第9章）更容易实现，但骨架相同。   ### 8.1 场景  一组 N 个进程，每个扮演三种角色之一或多种：  * **Proposer** ：试图让某个值被选定。 * **Acceptor** ：对提议投票；崩溃后持久化状态。 * **Learner**...
  c08_b01.md: ...### 8.2 为什么朴素协议失败  在 Paxos 之前，先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给...
  c08_b01.md: ...* ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **...
  c08_b01.md: ...ptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 pro...
  c08_b01.md: ...有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 pro...
  c08_b01.md: ...ser 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3**...
  c08_b01.md: ...ser 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看...
  c08_b01.md: ...（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有...
  c08_b01.md: ...。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有值拿到多数；无法达成一致。  Paxos 通过引入两阶段（prepare、accep...
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（例如 (counter, propo...
  c08_b02.md: ...allot number 唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)`...
  c08_b02.md: ...acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在...
  c08_b02.md: ...t  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_val...
  c08_b02.md: ...ccept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，proposer 必须取 `accepted_ballot` _最高_ 的那个 promise 的值。 * 否则（所有 promise...
  c08_b02.md: ...的那个 promise 的值。 * 否则（所有 promise 的 `accepted_value` 都是 null），proposer 使用自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须...
  c08_b02.md: ...safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。...
  c08_b02.md: ...被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b,...
  c08_b02.md: ...`accepted_ballot, accepted_value`，并回复 `ACCEPTED(b, v)`。  当 proposer 收集到多数 `ACCEPTED` 时，值 v 被 _选定_。   #### Learning  一旦值被选定，lear...
  c08_b02.md: ...出的决定会被任何其他多数中的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过...
  c08_b02.md: ...同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就是 agreement 性质：任何已选定的值将永远是选定的值，无论后续跑多少 ballot。...
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（例如 (counter, propo...
  c08_b02.md: ...allot number 唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)`...
  c08_b02.md: ...acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在...
  c08_b02.md: ...t  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_val...
  c08_b02.md: ...ccept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，proposer 必须取 `accepted_ballot` _最高_ 的那个 promise 的值。 * 否则（所有 promise...
  c08_b02.md: ...的那个 promise 的值。 * 否则（所有 promise 的 `accepted_value` 都是 null），proposer 使用自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须...
  c08_b02.md: ...safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。...
  c08_b02.md: ...被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b,...
  c08_b02.md: ...`accepted_ballot, accepted_value`，并回复 `ACCEPTED(b, v)`。  当 proposer 收集到多数 `ACCEPTED` 时，值 v 被 _选定_。   #### Learning  一旦值被选定，lear...
  c08_b02.md: ...出的决定会被任何其他多数中的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过...
  c08_b02.md: ...同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就是 agreement 性质：任何已选定的值将永远是选定的值，无论后续跑多少 ballot。...
  c08_b03.md: ...leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。...
  c08_b03.md: ...os 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leade...
  c08_b03.md: ...leader 并由 leader 提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。...
  c08_b03.md: ...os 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leade...
  c08_b05.md: ...### 8.8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤...
  c08_b05.md: ...8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $...
  c08_b05.md: ...每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到 3 个 promise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v...
  c08_b05.md: ...ed_value` 都是 null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5**...
  c08_b05.md: ...ed_value = "hello"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 propose...
  c08_b05.md: ...roposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b...
  c08_b05.md: ...选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受...
  c08_b05.md: ...`PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，...
  c08_b05.md: ...3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送...
  c08_b05.md: ...第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hel...
  c08_b05.md: ...prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 saf...
  c08_b05.md: ...”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 safety 不变式。   ### 8.9 练习...
  c08_b05.md: ...### 8.8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤...
  c08_b05.md: ...8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $...
  c08_b05.md: ...每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到 3 个 promise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v...
  c08_b05.md: ...ed_value` 都是 null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5**...
  c08_b05.md: ...ed_value = "hello"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 propose...
  c08_b05.md: ...roposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b...
  c08_b05.md: ...选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受...
  c08_b05.md: ...`PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，...
  c08_b05.md: ...3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送...
  c08_b05.md: ...第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hel...
  c08_b05.md: ...prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 saf...
  c08_b05.md: ...”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 safety 不变式。   ### 8.9 练习...
  c08_b06.md: ...获得提议许可；了解任何先前接受的值。第二阶段：提议并让多数接受。  **答案：**  _第一阶段（Prepare）：_ Proposer 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ba...
  c08_b06.md: ...)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中...
  c08_b06.md: ...第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已接受的最高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：...
  c08_b06.md: ...在知道该多数中每个 acceptor 已接受的最高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：_ Proposer 选一个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给...
  c08_b06.md: ...T(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promis...
  c08_b06.md: ...一旦值被选定，所有后续 ballot 都会认同它。这就是 agreement 性质。  **Q2.** 为什么第二阶段 proposer 必须使用第一阶段响应中最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值...
  c08_b06.md: ...最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值，该值可能已被选定；新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已...
  c08_b06.md: ...它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少...
  c08_b06.md: ...数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少）。  若新 proposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$...
  c08_b06.md: ...获得提议许可；了解任何先前接受的值。第二阶段：提议并让多数接受。  **答案：**  _第一阶段（Prepare）：_ Proposer 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ba...
  c08_b06.md: ...)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中...
  c08_b06.md: ...第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已接受的最高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：...
  c08_b06.md: ...在知道该多数中每个 acceptor 已接受的最高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：_ Proposer 选一个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给...
  c08_b06.md: ...T(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promis...
  c08_b06.md: ...一旦值被选定，所有后续 ballot 都会认同它。这就是 agreement 性质。  **Q2.** 为什么第二阶段 proposer 必须使用第一阶段响应中最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值...
  c08_b06.md: ...最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值，该值可能已被选定；新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已...
  c08_b06.md: ...它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少...
  c08_b06.md: ...数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少）。  若新 proposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$...
  c08_b08.md: ...**Q1.** 一个团队为 leader 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 pro...
  c08_b08.md: ...ser（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答...
  c08_b08.md: ...allot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acce...
  c08_b08.md: ...epare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  *...
  c08_b08.md: ...ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给...
  c08_b08.md: ...**Q1.** 一个团队为 leader 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 pro...
  c08_b08.md: ...ser（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答...
  c08_b08.md: ...allot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acce...
  c08_b08.md: ...epare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  *...
  c08_b08.md: ...ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给...
  c08_b09.md: ...，它们在处理协调者故障方面的根本区别是什么？  提示  2PC 有一个协调者；若它崩溃，参与者阻塞。Paxos 有多个 proposer；一个崩溃，另一个可接管。  **答案：** 两个协议都有两阶段。结构性区别在于如何处理协调者 / proposer 的...
  c08_b09.md: ...个 proposer；一个崩溃，另一个可接管。  **答案：** 两个协议都有两阶段。结构性区别在于如何处理协调者 / proposer 的故障。  _2PC 结构_ ：一个协调者给所有参与者发 PREPARE（或 VOTE_REQUEST）。若所有参与者...
  c08_b09.md: ...道协调者的决定。它们不能单方面决定，因为协调者可能在崩溃前已提交（通知了下游客户端）。  _Paxos 结构_ ：任何 proposer 都可提议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 bal...
  c08_b09.md: ...为协调者可能在崩溃前已提交（通知了下游客户端）。  _Paxos 结构_ ：任何 proposer 都可提议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Pa...
  c08_b09.md: ...os 结构_ ：任何 proposer 都可提议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一...
  c08_b09.md: ...fety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高 ballot 接管。新 proposer 的第一阶段会学到是否有（什...
  c08_b09.md: ...可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高 ballot 接管。新 proposer 的第一阶段会学到是否有（什么）值已被接受，并完成前一个提议或重新开...
  c08_b09.md: ..._Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高 ballot 接管。新 proposer 的第一阶段会学到是否有（什么）值已被接受，并完成前一个提议或重新开始。没有参与者无限期阻塞。  结构性优势：Paxos...
  c08_b09.md: ...提议或重新开始。没有参与者无限期阻塞。  结构性优势：Paxos 对 liveness 没有单点故障。任何进程都可以当 proposer；任一单进程故障不会阻塞系统（只要多数存活）。  2PC 的阻塞失败模式是它不是通用共识原语的原因。实用 2PC 实现通...
  c08_b10.md: ...safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 p...
  c08_b10.md: ...现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 b...
  c08_b10.md: ...number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规...
  c08_b10.md: ...r 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-b...
  c08_b10.md: ...一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c08_b11.md: ...lit-brain 的天真方式实现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/ballot/acceptor。LLM 代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 accept...
  c08_b11.md: ...阶段（prepare、accept）加法定人数相交就一个值达成一致。Safety 性质由 accept 规则保留：任何 proposer 的 accept 必须提议第一阶段响应中最高已接受的值；这保证一旦值被选定，所有后续 ballot 都会提议它。  M...
  c09_b06.md: ...llot number 也单调递增且全序。结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos...

=== quorum (111 occurrences) ===
  c02_b08.md: ...陈述 PACELC。对 (a) Spanner、(b) CockroachDB、(c) DynamoDB、(d) 使用 QUORUM 读/写的 Cassandra，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Av...
  c02_b08.md: ...（DynamoDB 也支持按请求的强一致读，延迟更高，这是按操作的 PACELC 选择，不是全局的。）  (d) 使用 QUORUM 读/写的 Cassandra：对这些特定操作是 PC/EC。QUORUM 级别要求大多数副本 ack，因此分区时少数侧...
  c02_b08.md: ...ELC 选择，不是全局的。）  (d) 使用 QUORUM 读/写的 Cassandra：对这些特定操作是 PC/EC。QUORUM 级别要求大多数副本 ack，因此分区时少数侧拒绝写入，无分区时支付法定人数延迟。使用 LOCAL_QUORUM 情况变...
  c05_b03.md: ...有冲突值并选择（或合并）。 * **CRDT**（第16章）。冲突由数据类型的合并函数确定性解决。   ### 5.7 Quorum 读与写  对 N 个副本的读写法定人数。经典 Dynamo 论文引入规则：若 $W + R > N$，则任意读与任意已...
  c05_b03.md: ...点，因此读能看到写 _当且仅当_ 写已在该节点完成提交 _且_ 没有冲突要解决。  失败情况：  * **Sloppy quorum** ：分区下，写法定人数可能由不在目标副本集中的节点满足（Dynamo 的 “hinted handoff”）。相交保...
  c05_b03.md: ...有冲突值并选择（或合并）。 * **CRDT**（第16章）。冲突由数据类型的合并函数确定性解决。   ### 5.7 Quorum 读与写  对 N 个副本的读写法定人数。经典 Dynamo 论文引入规则：若 $W + R > N$，则任意读与任意已...
  c05_b03.md: ...点，因此读能看到写 _当且仅当_ 写已在该节点完成提交 _且_ 没有冲突要解决。  失败情况：  * **Sloppy quorum** ：分区下，写法定人数可能由不在目标副本集中的节点满足（Dynamo 的 “hinted handoff”）。相交保...
  c05_b05.md: ...pply` 用于单数据中心 RPO=0；MySQL 半同步用于较低成本下的中等持久性；CockroachDB Raft-quorum 用于自动 RPO=0 且多区域容错。训练技能：按操作的 RPO 需求选择复制模式，而非按系统默认。  System |...
  c05_b05.md: ...基于法定人数 CockroachDB | Raft-replicated（per-range）| 所有写都是 Raft-quorum（多数同步） Spanner | Paxos-replicated（per-tablet）| 所有写都是 Paxos-q...
  c05_b05.md: ...m（多数同步） Spanner | Paxos-replicated（per-tablet）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Le...
  c05_b05.md: ...let）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Leaderless | 默认最终一致，可按请求强一致 Redis（master-rep...
  c05_b05.md: ...a）| Async | `WAIT N` 命令实现临时同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Ra...
  c05_b05.md: ...| 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Raft-quorum  模式：现代分布式数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raf...
  c05_b05.md: ...数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避...
  c05_b05.md: ...pply` 用于单数据中心 RPO=0；MySQL 半同步用于较低成本下的中等持久性；CockroachDB Raft-quorum 用于自动 RPO=0 且多区域容错。训练技能：按操作的 RPO 需求选择复制模式，而非按系统默认。  System |...
  c05_b05.md: ...基于法定人数 CockroachDB | Raft-replicated（per-range）| 所有写都是 Raft-quorum（多数同步） Spanner | Paxos-replicated（per-tablet）| 所有写都是 Paxos-q...
  c05_b05.md: ...m（多数同步） Spanner | Paxos-replicated（per-tablet）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Le...
  c05_b05.md: ...let）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Leaderless | 默认最终一致，可按请求强一致 Redis（master-rep...
  c05_b05.md: ...a）| Async | `WAIT N` 命令实现临时同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Ra...
  c05_b05.md: ...| 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Raft-quorum  模式：现代分布式数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raf...
  c05_b05.md: ...数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避...
  c05_b06.md: ...看不到。不同并发读者看到不同状态。线性一致性要求所有读者按实时顺序要么都看见写要么都不见。  (b) **Sloppy quorum** ：主副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本（从未收到写）...
  c05_b07.md: ...阈值不同。 * 故障切换计划必须明确：同步副本死亡时，提升异步（知道可能丢写）或等待（知道写阻塞）。  现代 Raft-quorum 方法（CockroachDB 等）避免这种不对称：每次写走法定人数（3 中任意 2，而非特定 1），因此没有单节点在关...
  c05_b07.md: ...DB 等）避免这种不对称：每次写走法定人数（3 中任意 2，而非特定 1），因此没有单节点在关键路径上。代价是写总是付 quorum 延迟，即使某个副本本来更快。...
  c05_b08.md: ...的 2 个节点是第三个（不在写者集合）和写者集合中一个离线的节点，读可能看不到写。  (b) Cassandra 的 “QUORUM” 协调者 opportunistically 选择节点。读协调者可能选择与写协调者不同的节点集合。即使两个 quoru...
  c05_b08.md: ...UORUM” 协调者 opportunistically 选择节点。读协调者可能选择与写协调者不同的节点集合。即使两个 quorum 都有效，具体重叠可能不包括持有最新值的节点，如果写者 quorum 只是部分传播。...
  c05_b08.md: ...读协调者可能选择与写协调者不同的节点集合。即使两个 quorum 都有效，具体重叠可能不包括持有最新值的节点，如果写者 quorum 只是部分传播。...
  c05_b08.md: ...的 2 个节点是第三个（不在写者集合）和写者集合中一个离线的节点，读可能看不到写。  (b) Cassandra 的 “QUORUM” 协调者 opportunistically 选择节点。读协调者可能选择与写协调者不同的节点集合。即使两个 quoru...
  c05_b08.md: ...UORUM” 协调者 opportunistically 选择节点。读协调者可能选择与写协调者不同的节点集合。即使两个 quorum 都有效，具体重叠可能不包括持有最新值的节点，如果写者 quorum 只是部分传播。...
  c05_b08.md: ...读协调者可能选择与写协调者不同的节点集合。即使两个 quorum 都有效，具体重叠可能不包括持有最新值的节点，如果写者 quorum 只是部分传播。...
  c05_b09.md: ...时间内把读发到那里。Cassandra 的 “PER_REPLICA_DC” 或类似协调者感知模式能做到。  通用教训：quorum 读/写规则给的是相交，不是线性一致性。需要 read-your-writes 的应用必须显式实现它。  **Q3.**...
  c05_b10.md: ...入思考  **Q1.** 辩护或反驳：“2026 年所有分布式数据库都应该像 CockroachDB 一样用 Raft-quorum。异步复制是遗留反模式。”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，...
  c05_b10.md: ...像 CockroachDB 一样用 Raft-quorum。异步复制是遗留反模式。”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，按构造避免 split-brain。延迟成本真实但有界；对大多数工作负载可...
  c05_b10.md: ...复制仍适合许多工作负载。偶尔丢失可接受的日志流水线不需要 RPO = 0。写少读多的工作负载可用异步复制到读副本，不付 quorum 成本。Aurora 的设计（写走 6 副本 4-of-6 quorum 的存储层）是混合：写为持久性做 quorum...
  c05_b10.md: ...。写少读多的工作负载可用异步复制到读副本，不付 quorum 成本。Aurora 的设计（写走 6 副本 4-of-6 quorum 的存储层）是混合：写为持久性做 quorum 复制，但从副本读是异步，远端读副本不影响写延迟。  综合：按操作的正确性...
  c05_b10.md: ...quorum 成本。Aurora 的设计（写走 6 副本 4-of-6 quorum 的存储层）是混合：写为持久性做 quorum 复制，但从副本读是异步，远端读副本不影响写延迟。  综合：按操作的正确性需求选择复制模式，而非按系统默认。现代系统提供...
  c05_b10.md: ...DB write concerns）。纪律是问“这个操作需要什么？”而不是“系统默认是什么？”  第二个考虑：Raft-quorum 系统每 range/shard 有 _写吞吐上限_ ，由 Raft 往返成本决定。对每键极端写吞吐的工作负载，异步复制...
  c05_b11.md: ...正同时给高可用与正确性的现代替代：Raft 复制 per-range 的多区域系统（CockroachDB）。Raft quorum 可承受单区域丢失；leader 可在另一区域重选；写在数秒内恢复。一致性全程保持。代价是跨区域 Raft 的延迟下限，...
  c06_b04.md: ...ms（Crescent 与银行同区域）。 * CockroachDB 提交（多区域）：150 ms（三区域 Raft quorum）。 * 余量：90 ms。  预算之所以可行，是因为 Crescent 把 CockroachDB 节点放在彼此 15...
  c06_b04.md: ...、US-West、EU-West）。如果第三个区域是新加坡，预算将不成立：US-East 与新加坡之间跨区域 Raft quorum 单程 200 ms、往返 400 ms，会超支。  Olamide 的团队专门选择这三个区域，把 Raft quoru...
  c06_b04.md: ...uorum 单程 200 ms、往返 400 ms，会超支。  Olamide 的团队专门选择这三个区域，把 Raft quorum 延迟压在 100 ms 以下。这是延迟预算能显式化的那种运维决策；不写下来的话，系统部署后延迟会在生产里给团队惊喜。...
  c06_b06.md: ...**Q3.** 一个团队的多区域数据库部署在 US-East、US-West、Frankfurt。他们希望 Raft quorum 在 80 ms 内提交。可行吗？若不可行，如何改？  提示  Raft quorum 需要多数。三节点时 quorum...
  c06_b06.md: ...nkfurt。他们希望 Raft quorum 在 80 ms 内提交。可行吗？若不可行，如何改？  提示  Raft quorum 需要多数。三节点时 quorum 是任意 2。计算每对往返下限。  **答案：** 三副本 Raft 需 2 of 3...
  c06_b06.md: ...quorum 在 80 ms 内提交。可行吗？若不可行，如何改？  提示  Raft quorum 需要多数。三节点时 quorum 是任意 2。计算每对往返下限。  **答案：** 三副本 Raft 需 2 of 3 ack。相关成对往返延迟：  *...
  c06_b06.md: ...o），把第二快往返压到 80 ms 以下。  (b) 用 Spanner 风格按区域 leaderless 写，按区域 quorum 加全局提交时间戳，特定工作负载可更快。  (c) 接受 85 ms 并调整预算。对人类可见操作，5 ms 通常不重要。...
  c06_b06.md: ...预算。对人类可见操作，5 ms 通常不重要。  (d) 读操作用 follower 读（无需 quorum），让写保持 quorum 成本，但减少读的感知多区域延迟。  **Q4.** 为什么 RDMA 用于分布式 LLM 推理却很少用于应用层微服务？...
  c06_b08.md: ...nner）。** 每个聊天室数据驻留在某个区域并全球复制。写付 Spanner commit-wait（~7 ms）加 quorum 往返；跨大陆 quorum 约 200 ms 提交。读可用 follower 区域做有界过期读。500 ms 预算满足...
  c06_b08.md: ...数据驻留在某个区域并全球复制。写付 Spanner commit-wait（~7 ms）加 quorum 往返；跨大陆 quorum 约 200 ms 提交。读可用 follower 区域做有界过期读。500 ms 预算满足；成本是 Spanner 基...
  c06_b08.md: ...做 federation。  **Q2.** 一个团队为 Raft 复制数据库选三个区域，希望最小化最坏情况 Raft quorum 延迟。给定上述光速下限，提出一个三区域选择并论证。  提示  Raft quorum 是多数；你希望中位数成对往返低。...
  c06_b08.md: ...域，希望最小化最坏情况 Raft quorum 延迟。给定上述光速下限，提出一个三区域选择并论证。  提示  Raft quorum 是多数；你希望中位数成对往返低。地理聚类有帮助。  **答案：** 三个副本的 Raft：quorum 是任意 2 o...
  c06_b08.md: ...示  Raft quorum 是多数；你希望中位数成对往返低。地理聚类有帮助。  **答案：** 三个副本的 Raft：quorum 是任意 2 of 3。leader 自己算一个 ack；条目在 _最快_ follower 响应后提交。因此 US-E...
  c06_b11.md: ...害_ 强一致系统的可用性，因为任一区域中断都可能破坏法定人数。5 区域 Raft 系统可承受 2 区域丢失（5 中 3 quorum）；3 区域 Raft 可承受 1 区域丢失（3 中 2 quorum）。5 区域设置确实更可用，但每次写延迟更高（副本...
  c06_b11.md: ...Raft 系统可承受 2 区域丢失（5 中 3 quorum）；3 区域 Raft 可承受 1 区域丢失（3 中 2 quorum）。5 区域设置确实更可用，但每次写延迟更高（副本更分散）。 * 读/写比多少？读重工作负载从区域读受益；区域越多，本地...
  c06_b11.md: ...写延迟更高（副本更分散）。 * 读/写比多少？读重工作负载从区域读受益；区域越多，本地读路径越多。写重工作负载每次写付 quorum 成本；更多区域通常意味着更高写延迟。 * 成本？五个有意义规模的区域成本约是一个区域的五倍。边际可用性很少值 5 倍成...
  c06_b12.md: ...可行设计。Crescent 团队的三区域选择（US-East、US-West、EU-West）由 80 ms Raft quorum 预算驱动；若把副本放到新加坡，预算将不可行。  第7章开始第三部分：共识。既然已知如何广播和复制，我们需要一组协议让节...
  c07_b03.md: ...选择一个多数 follower 已复制的日志条目。HotStuff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certificate。三者都以法定人数作为 agreement 的安全机制。...
  c07_b05.md: ...条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view...
  c10_b03.md: ...制是 HotStuff 的三阶段投票（prepare / pre-commit / commit）及作为紧凑支持证明的 Quorum Certificates，外加 pipelining 提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；...
  c10_b05.md: ...exible Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisited” 推广了 Paxos 的法定人数要求。经典 Paxos 要求 prepa...
  c10_b07.md: ...3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certificates，无需重播所有投票。  **答案：** HotStuff 的关键创新是 _线性 view ch...
  c10_b12.md: ...BFT 是经典实用 BFT 协议；其 $O(N^2)$ 消息复杂度限制了小规模副本集。HotStuff（2019）通过 Quorum Certificates 和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数...
  c11_b01.md: ...、可串行化、快照、因果、最终、按操作可调。   4. **分区行为是什么？** 少数侧拒绝写、提供过期读、sloppy quorum、应用控制。  具体回答这四个问题，就能得到该系统的 PACELC 画像、运维特征和失败模式。任何问题 30 分钟都能回...
  c11_b03.md: ...andra、默认一致性下的 DynamoDB。写可能冲突；冲突解决（LWW、CRDT）决定结果。 * **Sloppy quorum**（Dynamo 特有行为）：主副本不可达时，写发往任何可达节点（hinted handoff）；读从任意法定人数。相...
  c11_b04.md: ...终，LWT（Paxos-backed）提供线性化 Partition behavior | 按操作可调；低一致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA / EL，quorum 时 PC / EC Strengths |...
  c11_b04.md: ...havior | 按操作可调；低一致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA / EL，quorum 时 PC / EC Strengths | 线性扩展到数千节点；可调权衡 Weaknesses | 运维复杂（tomb...
  c11_b09.md: ...ncy model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quorum PC/EC  差异在实现：  Aspect | Ca...
  c11_b09.md: ...tion behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quorum PC/EC  差异在实现：  Aspect | Cassandra | ScyllaDB ---|---|---- I...
  c14_b10.md: ...制。工作负载很 fit。  (c) **Cassandra/ScyllaDB** ：可调一致性。ONE 一致性读非常快；QUORUM 写提供一定持久性保证。线性扩展到多节点。  (d) **CRDT-based（自定义）** ：G-Counter 或...
  c15_b03.md: ...作可调。Consistency level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency...
  c15_b03.md: ...终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency level ALL：所有副本；只对特定一致性证明有用。  应用纪律：按需求为每次...
  c17_b05.md: ...定相同更新，我的视图与他人的收敛（强最终）。”  **Q4.** 一个团队为某工作负载选择 Cassandra，并配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-opera...
  c17_b05.md: ...Q4.** 一个团队为某工作负载选择 Cassandra，并配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。...
  c17_b05.md: ...read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出 _相交_（第5章）：任何读与任何已提交写至少在一个副本上重叠。这比最终一致强，但弱于线性化。  QUORUM...
  c17_b05.md: ...QUORUM 读写给出 _相交_（第5章）：任何读与任何已提交写至少在一个副本上重叠。这比最终一致强，但弱于线性化。  QUORUM 提供：  * 单会话 read-after-write：若写成功（Q 个写入副本 ack）且后续读用 QUORUM，相...
  c17_b05.md: ...QUORUM 提供：  * 单会话 read-after-write：若写成功（Q 个写入副本 ack）且后续读用 QUORUM，相交节点有该写；读能看到。 * 跨会话排序：若两会话写同一键，HLC 更晚的写在 QUORUM 读中胜出。“更晚” 由...
  c17_b05.md: ...本 ack）且后续读用 QUORUM，相交节点有该写；读能看到。 * 跨会话排序：若两会话写同一键，HLC 更晚的写在 QUORUM 读中胜出。“更晚” 由 Cassandra 的 LWW 冲突解决决定，存在第3章的时钟偏移问题。  QUORUM _不...
  c17_b05.md: ...写在 QUORUM 读中胜出。“更晚” 由 Cassandra 的 LWW 冲突解决决定，存在第3章的时钟偏移问题。  QUORUM _不提供_：  * Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作...
  c17_b05.md: ..._不提供_：  * Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-key；多键一致性需要轻量事务（LWT，基于 Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUO...
  c17_b05.md: ...RUM 是 per-key；多键一致性需要轻量事务（LWT，基于 Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUORUM 读独立；读集合可能反映系统非线性化快照。  实践中：QUORUM 对许多工作负载是 “足够好” 的一致性（大多数数据库...
  c17_b05.md: ...Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUORUM 读独立；读集合可能反映系统非线性化快照。  实践中：QUORUM 对许多工作负载是 “足够好” 的一致性（大多数数据库操作是单键，可容忍 LWW 解析）。对需要真正 per-key 线...
  c17_b05.md: ...-key 线性化的工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，QUORUM 和 LWT 都不够；需要事务型系统。  在 Cassandra 中选择 QUORUM 是 workhorse 模式；许...
  c17_b05.md: ...ckroachDB）。对多键事务，QUORUM 和 LWT 都不够；需要事务型系统。  在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bu...
  c17_b05.md: ...务型系统。  在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...
  c17_b06.md: ...在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bu...
  c17_b06.md: ...在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...
  c28_b10.md: ...伪代码 | Ch.4 §4.7 | 要交付给 N 个进程的消息 | 所有正确接收者以相同顺序交付（原子）或更弱变体 | Quorum 确认 + 重广播 | 为 “安全” 默认选原子广播，而可靠广播已足够；遗漏 uniformity（发送者和唯一接收者同...
  c28_b10.md: ...Ch.5 §5.10 | 写主库；存在副本 | 每次写入的 RPO + 每次写入的延迟（可配置权衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy quorum | | 4 | Ra...
  c28_b10.md: ...衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy quorum | | 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求 → leader → follower...
  c28_b10.md: ...置 | | 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1...
  c28_b10.md: ...| Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下...
  c28_b10.md: ...) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下跳过 QC 验证；竞争新 leader 间视图变更竞态 | | 6 | 30 分钟读懂数据库 | Ch...
  c28_b10.md: ...伪代码 | Ch.4 §4.7 | 要交付给 N 个进程的消息 | 所有正确接收者以相同顺序交付（原子）或更弱变体 | Quorum 确认 + 重广播 | 为 “安全” 默认选原子广播，而可靠广播已足够；遗漏 uniformity（发送者和唯一接收者同...
  c28_b10.md: ...Ch.5 §5.10 | 写主库；存在副本 | 每次写入的 RPO + 每次写入的延迟（可配置权衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy quorum | | 4 | Ra...
  c28_b10.md: ...衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy quorum | | 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求 → leader → follower...
  c28_b10.md: ...置 | | 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1...
  c28_b10.md: ...| Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下...
  c28_b10.md: ...) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下跳过 QC 验证；竞争新 leader 间视图变更竞态 | | 6 | 30 分钟读懂数据库 | Ch...
  c30_b02.md: ..., H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport,...
  c30_b02.md: ...xible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzan...
  c30_b02.md: ..., H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport,...
  c30_b02.md: ...xible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzan...
  c30_b04.md: ...| PC/EC | 多 region Postgres 兼容；任意云 | | **DynamoDB** | 无领导者；quorum | 透明 | 默认最终；请求时可强一致 | AP 默认 | PA/EL | AWS 原生 KV；可预测延迟 | | *...
  c30_b06.md: ...致性。 * **Paxos**：Lamport 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum intersection（quorum 交集）**：N 个节点中任意两个多数派至少共享一...
  c30_b06.md: ...：Lamport 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum intersection（quorum 交集）**：N 个节点中任意两个多数派至少共享一个节点；共识安全基础。 * *...
  c30_b06.md: ...致性。 * **Paxos**：Lamport 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum intersection（quorum 交集）**：N 个节点中任意两个多数派至少共享一...
  c30_b06.md: ...：Lamport 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum intersection（quorum 交集）**：N 个节点中任意两个多数派至少共享一个节点；共识安全基础。 * *...
  c30_b10.md: ...TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West + US-West（或类似）。 *...
  c30_b16.md: ...Ch.27 | | 9 | "W + R > N 给出线性化。" | 它给出 _交集_，更弱。并发写和 sloppy quorum 破坏线性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。" | 跨 reg...
  c30_b16.md: ...性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 |...
  c30_b16.md: ...on 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 | "FLP 说共识不可能。" | FLP 说确定性共识在纯异步下有界终止不可能。真实系...
  c30_b19.md: ...3.6.5（saga 正确性义务）   跨 region 提交 p99 >150 ms | 跨 region Raft quorum；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但下游无 fencing-...

=== span (106 occurrences) ===
  c03_b14.md: ...上下文传播）实际做的事情。跨服务传播的 trace context 携带 HLC 等价信息的机制，trace ID + span ID + 序列号提供本地顺序的平手打破。...
  c20_b01.md: ...里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 s...
  c20_b01.md: ...n 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格...
  c20_b01.md: ...到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §2...
  c20_b01.md: ...§28.2 有 Crescent 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network i...
  c20_b01.md: ...style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（非并行）；它们时长相加约等于根总时长（65...
  c20_b01.md: ...365 ms p50）。DB transaction 的四个子节点在事务内部也是串行的；总和（93 ms）接近 DB span 总时长（95 ms），2 ms 差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 D...
  c20_b01.md: ...总和（93 ms）接近 DB span 总时长（95 ms），2 ms 差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  *...
  c20_b01.md: ...立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 s...
  c20_b01.md: ...和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间...
  c20_b01.md: ...k）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和...
  c20_b01.md: ...个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * Attribu...
  c20_b01.md: ...里。分布式系统中，等价物是 _trace_：请求跨服务的因果链。  分布式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 s...
  c20_b01.md: ...n 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格...
  c20_b01.md: ...到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §2...
  c20_b01.md: ...§28.2 有 Crescent 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network i...
  c20_b01.md: ...style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（非并行）；它们时长相加约等于根总时长（65...
  c20_b01.md: ...365 ms p50）。DB transaction 的四个子节点在事务内部也是串行的；总和（93 ms）接近 DB span 总时长（95 ms），2 ms 差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 D...
  c20_b01.md: ...总和（93 ms）接近 DB span 总时长（95 ms），2 ms 差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  *...
  c20_b01.md: ...立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 s...
  c20_b01.md: ...和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间...
  c20_b01.md: ...k）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和...
  c20_b01.md: ...个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * Attribu...
  c20_b02.md: ...#### 关注点 1：因果保持  Trace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 span，跨服务边界。  标准传播：HTTP headers（W...
  c20_b02.md: ...点 1：因果保持  Trace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 span，跨服务边界。  标准传播：HTTP headers（W3C 标准的 `traceparent`...
  c20_b02.md: ...ace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 span，跨服务边界。  标准传播：HTTP headers（W3C 标准的 `traceparent`、`tracestate...
  c20_b02.md: ...e context。修复：在协议边界显式传播逻辑。  * **重试时 context 损坏**：同一操作重试应产生独立 span（trace 中显示重试），而不是与原 span 合并。修复：每次重试创建新的子 span。   #### 关注点 2：...
  c20_b02.md: ...。  * **重试时 context 损坏**：同一操作重试应产生独立 span（trace 中显示重试），而不是与原 span 合并。修复：每次重试创建新的子 span。   #### 关注点 2：分区下采样  Traces 通常被采样（只有 1...
  c20_b02.md: ...**：同一操作重试应产生独立 span（trace 中显示重试），而不是与原 span 合并。修复：每次重试创建新的子 span。   #### 关注点 2：分区下采样  Traces 通常被采样（只有 1% 或更少的请求被详细追踪），因为存储每条...
  c20_b02.md: ...Traces 通常被采样（只有 1% 或更少的请求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。这是 “head-based” 采样。...
  c20_b02.md: ...采样（只有 1% 或更少的请求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。这是 “head-based” 采样。  分区下，采样决策传...
  c20_b02.md: ...求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span...
  c20_b02.md: ...span 也被采样（trace 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码（在 trace context 加 per-span a...
  c20_b02.md: ...分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：span...
  c20_b03.md: ...*：被调用但工程师未预料到的服务；通常是架构漂移征兆。  阅读 trace 是一项技能。模式：  (a) **先看最长 span**：它通常主导延迟。 (b) **看错误**：追踪第一个错误；后续可能是级联。 (c) **与基线比较**：一个 sp...
  c20_b03.md: ...an**：它通常主导延迟。 (b) **看错误**：追踪第一个错误；后续可能是级联。 (c) **与基线比较**：一个 span 花 200 ms 可能是正常，也可能比平时慢 10 倍；比较揭示。 (d) **看 “缺失” 的 spans**：若应...
  c20_b03.md: ...### 20.4 Trace 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wall-clock 时长，但不拆解接收服务内部做了什么（哪些函数慢、哪些查询慢）。为此需要服务内性...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、pa...
  c20_b05.md: ...*Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案...
  c20_b05.md: ...ce？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个...
  c20_b05.md: ...信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发...
  c20_b05.md: ...布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；...
  c20_b05.md: ...果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Spa...
  c20_b05.md: ...；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID*...
  c20_b05.md: ...pan 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为...
  c20_b05.md: ...*Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **S...
  c20_b05.md: ...内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operat...
  c20_b05.md: ...标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服...
  c20_b05.md: ...**Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **...
  c20_b05.md: ...e 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes**：自定义键值...
  c20_b05.md: ...**Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 pare...
  c20_b05.md: ...时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？...
  c20_b05.md: ...显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、par...
  c20_b05.md: ...2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、...
  c20_b05.md: ...接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 tra...
  c20_b05.md: ...碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace I...
  c20_b05.md: ...答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与...
  c20_b05.md: ...e ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有...
  c20_b05.md: ...求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而...
  c20_b05.md: ..._ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b05.md: ...#### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、pa...
  c20_b05.md: ...*Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案...
  c20_b05.md: ...ce？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个...
  c20_b05.md: ...信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发...
  c20_b05.md: ...布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；...
  c20_b05.md: ...果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Spa...
  c20_b05.md: ...；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID*...
  c20_b05.md: ...pan 携带：  * **Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为...
  c20_b05.md: ...*Trace ID**：该 trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **S...
  c20_b05.md: ...内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operat...
  c20_b05.md: ...标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服...
  c20_b05.md: ...**Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **...
  c20_b05.md: ...e 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes**：自定义键值...
  c20_b05.md: ...**Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 pare...
  c20_b05.md: ...时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？...
  c20_b05.md: ...显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、par...
  c20_b05.md: ...2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、...
  c20_b05.md: ...接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 tra...
  c20_b05.md: ...碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace I...
  c20_b05.md: ...答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪个 trace。它创建新的 trace ID；工作看起来与...
  c20_b05.md: ...e ID；工作看起来与原始请求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有...
  c20_b05.md: ...求是分开的 trace。 * 父子关系丢失；trace 树碎片化。 * 采样决策不传播；子 span 可能未被采样而父 span 被采样（或相反），导致不完整 trace。  结果：不是一条展示完整因果链（原始请求和所有下游工作）的 trace，而...
  c20_b05.md: ..._ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b07.md: ...ice C：40 ms（8%）  优化优先级：  (a) **调查 service B**：200 ms 是最大的单一 span。B 在做什么？若是下游数据库查询，能否优化或缓存？若是复杂计算，能否批量、延迟或并行化？  (b) **调查 serv...
  c20_b07.md: ...团队只能猜测哪里慢；有了 trace，他们知道。  **Q2.** 某团队 trace 显示 1% 请求有以错误结束的 span。调查发现 80% 这些错误来自一个特定下游服务。走一遍从 trace 到修复的诊断流程。  提示  按服务分组错误；看...
  c20_b08.md: ...):         process(record)   `extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka...
  c20_b08.md: ...record)   `extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry...
  c20_b08.md: ...ct` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客户端自动仪...
  c20_b08.md: ...eaders 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客户端自动仪器库（`open...
  c20_b10.md: ...迁移能力。   ### 本章总结  分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（...
  c20_b10.md: ...路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（跨服务边界传播 context）、分区下采样（head-based...
  c28_b04.md: ...跨 region 支付地理正确性协商的 750-ms p99 SLO。  Trace（第20章）捕获整个旅程。47 个 span，分布在 6 个服务。Trace 作为取证记录保存 7 年（合规要求）。...
  c30_b19.md: ...Trace 症状 | 可能原因 | 参考章节   |---|---|---   同一操作两个 span 显示不同效果（重复扣款、重复写入） | 缺少幂等性 key；重试命中无保护路径 | Ch.4（RPC 语义），Ch.1...
  c30_b19.md: ...无保护路径 | Ch.4（RPC 语义），Ch.18（fencing token），Ch.27 §27.2   跨服务 span 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 w...
  c30_b19.md: ...用 wall-clock；时钟偏移 | Ch.3（HLC），Ch.20 §20.2（context 传播）   一个慢 span 主导请求延迟；被调用服务自身健康检查是绿色 | 灰色故障；廉价健康检查不走真实路径 | Ch.22（灰色故障、深健康检...
  c30_b19.md: ...下游变慢后上游突发重试 | 重试放大；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（...
  c30_b19.md: ...| 重试放大；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务...
  c30_b19.md: ...context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 region 出现多个协调器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触...
  c30_b19.md: ...前死亡 | Ch.5 §5.3，Ch.5 §5.4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token）...
  c30_b19.md: ...Trace 症状 | 可能原因 | 参考章节   |---|---|---   同一操作两个 span 显示不同效果（重复扣款、重复写入） | 缺少幂等性 key；重试命中无保护路径 | Ch.4（RPC 语义），Ch.1...
  c30_b19.md: ...无保护路径 | Ch.4（RPC 语义），Ch.18（fencing token），Ch.27 §27.2   跨服务 span 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 w...
  c30_b19.md: ...用 wall-clock；时钟偏移 | Ch.3（HLC），Ch.20 §20.2（context 传播）   一个慢 span 主导请求延迟；被调用服务自身健康检查是绿色 | 灰色故障；廉价健康检查不走真实路径 | Ch.22（灰色故障、深健康检...
  c30_b19.md: ...下游变慢后上游突发重试 | 重试放大；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（...
  c30_b19.md: ...| 重试放大；缺少重试预算 | Ch.21 §21.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务...
  c30_b19.md: ...context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 region 出现多个协调器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触...
  c30_b19.md: ...前死亡 | Ch.5 §5.3，Ch.5 §5.4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token）...

=== crescent (102 occurrences) ===
  c01_b01.md: ...凌晨 2:47（圣保罗时间），Crescent 在三个区域上线那晚，一位名叫 Adaeze Okafor 的客户——住在圣保罗 Vila Madalena 的尼日利亚...
  c01_b01.md: ...被告警叫醒。到 3:30，她已经在查看 trace。两个协调者（coordinator）节点分别完成了支付：一个位于 Crescent 的 EU-West 区域（法兰克福数据中心），另一个位于前置 Crescent US-East 区域的南美洲网关（网关...
  c01_b01.md: ...ator）节点分别完成了支付：一个位于 Crescent 的 EU-West 区域（法兰克福数据中心），另一个位于前置 Crescent US-East 区域的南美洲网关（网关设在圣保罗，负责把流量路由到 US-East 的运维数据面）。两者都记录了干净的...
  c01_b01.md: ...在第一份响应变慢后重试了一次请求，重试落到了圣保罗网关，而原始请求仍在通过法兰克福收尾。（第13章和第28章会完整展开 Crescent 的三区域架构——US-East、EU-West、US-West；本章使用城市名，因为事后复盘最初就是这样命名的，后来团...
  c01_b01.md: ...真话。时钟在走。变量保存你设的值。函数返回就是返回。调用两次就是两次。  当你在多台机器上写程序时，这些全都不成立。  Crescent 事件中的两个协调者彼此之间说了三个谎言，而这三个谎言并非偶然。它们是每个分布式系统都会说的三个谎言，也是你在这个领域读...
  c01_b08.md: ...，以及用于协调者状态的 Raft 支持提交日志（第9章）。复盘命名了系统一直讲述的三个谎言。银行向 Adaeze 退款。Crescent 在内部发布了复盘，并在下周进行了一次 game day（第23章），验证修复在模拟分区下是否有效。  Crescent...
  c01_b08.md: ...Crescent 在内部发布了复盘，并在下周进行了一次 game day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuki 和 Tobias 最终在第28章一起交付的系统。第1章到第2...
  c01_b09.md: ...络或传输层重试它，可能被送达两次。把成功视为默认、把可见失败视为例外，忽略了最大的一类：部分可见性。  **Q2.** Crescent 事件涉及在不稳定移动连接上的一次单一重试。对每个谎言分别走一遍，什么本可以预防它？为什么单一修复不够？  提示  每个...
  c01_b10.md: ...TP 中断、虚拟机暂停，以及 10 ms 窗口边界会发生什么。  **答案：** 第一，10 ms 窗口本身就足以造成 Crescent 式失败。如果两个协调者在 10 ms 内提交，时间戳无法可靠排序。许多分布式 bug 住在小于时钟偏移上界的窗口里。...
  c01_b14.md: ...），所以即使应用认为自己在和单个 Postgres 对话，存储也已经是分布式的，并继承所有三个谎言。  **Q2.** Crescent 复盘命名了三个谎言，并需要对三个都进行修复。在你的经验或阅读中，找出一个生产事故，响应者只修复了最明显的失败，却忽略了...
  c01_b15.md: ...败上说谎：部分失败是默认模式，不是例外，bug 就住在“这个节点上发生了什么”与“系统整体经历了什么”之间的缺口里。  Crescent 事件在单一重试中同时利用了三个谎言。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章）和 Raf...
  c03_b15.md: ...原版 `sorted(messages, key=lambda m: m.timestamp)` 错误。  第1章提到 Crescent 多区域支付系统用 HLC、fencing token 和 Raft 修复。现在你知道 HLC 的含义及为何它是正确原语...
  c04_b01.md: ...第1章的 Crescent 支付系统需要它的两个协调者节点就一个结果达成一致。它们没有。原因是：每个协调者独立做出提交决定，并把自己的决定下发给下...
  c04_b01.md: ...原语的正确名称，因为它捕捉了单一逻辑事件必须到达一组接收者的思想，即使底层网络只认识点对点链路。  原子广播正是第1章 Crescent 系统缺失的原语。两个协调者各自广播“我正在提交”，却不具备原子性质，结果是两次独立提交。如果系统使用原子广播（后来通过...
  c04_b09.md: ...平衡（例如部署期间），一条在途消息可能被另一个消费者重新处理。幂等性处理此情况；缺乏幂等性会导致双重处理。  第1章的 Crescent 支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition...
  c04_b12.md: ...的全序。  原子广播等价于共识，实现它的协议（Raft、Paxos、ZAB、HotStuff）是第三部分的主题。第1章 Crescent 多区域支付修复使用的 Raft 复制提交日志，就是把原子广播应用于协调者状态机。  每一层都比下一层更贵。纪律是：识别...
  c05_b08.md: ...#### 应用问题  **Q1.** Olamide 的团队正在决定 Crescent 支付系统用 Postgres + 读副本（异步）还是 CockroachDB。列出他们应先用 PACELC 框架（第2...
  c05_b08.md: ...tgres 可用逻辑复制做多区域，但失败模式复杂；CockroachDB 从一开始就按多区域设计，具有一致延迟模型。若 Crescent 运营三区域，CockroachDB 运维更干净。  (e) **团队运维成熟度如何？** 跑了十年 Postgres...
  c05_b11.md: ...，不是线性一致性；差别是许多真实复制 bug 的根源。  Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-session HLC 跟踪（CockroachDB、YugabyteD...
  c06_b04.md: ...受。   5. **若预算装不下** ，重新设计：把服务放到同区域、反规范化数据以避免跨区域跳，或通过缓存接受过期。  Crescent 支付系统示例：  * 目标：支付授权 500 ms p99。 * TLS、认证、应用逻辑：100 ms。 * 幂等性检...
  c06_b04.md: ...B 提交（多区域）：150 ms（三区域 Raft quorum）。 * 余量：90 ms。  预算之所以可行，是因为 Crescent 把 CockroachDB 节点放在彼此 150 ms 可达的三个区域（US-East、US-West、EU-West...
  c06_b05.md: ...条批量消息，会付每条消息开销（TLS framing、header bytes），小消息时这部分可能超过实际负载。  Crescent 示例：CockroachDB WAL 跨区域复制每天约 10 GB，很小。实际主导的跨区域成本是应用的指标和日志外送（...
  c06_b12.md: ...nel-bypass TCP 足够。  延迟预算方法（先定目标、减固定成本、把余量分配到每跳）能在部署前发现不可行设计。Crescent 团队的三区域选择（US-East、US-West、EU-West）由 80 ms Raft quorum 预算驱动；若...
  c07_b01.md: ...管理** ：“谁在集群中”的变更通过共识决定。 * **配置管理** ：任何必须全局一致的系统级设置都通过共识决定。  Crescent 支付系统的协调者状态机（第1章）正是如此：通过共识复制状态机，让所有协调者节点就当前状态达成一致。...
  c07_b11.md: ...fety 性质（“坏事永不发生”）由现代共识协议始终保证。Liveness 性质（“好事最终发生”）只在网络稳定后保证。Crescent 多区域提交日志使用 Raft，它在所有网络条件下 safe，但在分区期间可能暂停。  三条性质映射到真实协议的具体设计...
  c11_b01.md: ...Olamide 坐在她的 CTO 对面。CTO 距下个会议还有 30 分钟。议题：Crescent 是否应把支付账本从 PostgreSQL 迁移到 CockroachDB？  Olamide 没时间通读 Cockro...
  c11_b07.md: ...提交 80 ms”）。 (c) 暴露厂商不愿强调的权衡。 (d) 让你按应用关心的维度比较，而非厂商喜欢的维度。  对 Crescent 的决策：Spanner-vs-CockroachDB 博客不会处理 GCP 锁定问题（一家厂商不会强调另一家的锁定劣势...
  c11_b08.md: ...的研究很有趣：通过限制数据模型（仅复式记账），系统可比通用数据库快得多且更可验证。2026 年金融系统中真实架构模式是：Crescent 的支付账本实际转账记录可用 TigerBeetle（schema 合适），而用户可见元数据仍用 CockroachDB...
  c11_b10.md: ...团队既有知识、该技术的事故历史）、(iv) 战略考虑（厂商锁定、退出选项）。这些因素组织特定，需由评估团队自行添加。  Crescent 决策示例展示了两层：技术方法把选择缩到 Spanner vs CockroachDB；商业层（不锁 GCP、团队已有...
  c11_b11.md: ...ACELC 位置。方法可泛化；它适用于你没用过的系统，因为四个问题在任何厂商文档中都有确定答案。  Olamide 对 Crescent 的决定（CockroachDB 胜过 Spanner、DynamoDB、Cassandra、FoundationDB）...
  c13_b07.md: ...#### 每种风格实例  **Orchestrated** —— Crescent 支付工作流（后文详细展开）：       PaymentOrchestrator (CockroachDB-backe...
  c13_b07.md: ...看点）和弱点（单一知晓点）。舞蹈化分布了两点：任何服务可独立开发部署，但理解完整工作流需要阅读跨多个服务的订阅关系。  Crescent 对支付使用编排（受监管、审计重、工作流很少变）。其内部 “marketplace 订单处理” 子系统使用舞蹈化（不同团...
  c13_b08.md: ...2PC 中间可见性可接受 | Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC   #### Crescent 的混合方案  Crescent 同时使用两者：  * **CockroachDB 内部** ：更新 `accounts...
  c13_b08.md: ...Saga（每一步本地提交并可见） 需要原子性（无中间可见性） | 2PC   #### Crescent 的混合方案  Crescent 同时使用两者：  * **CockroachDB 内部** ：更新 `accounts`、`transactions`...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用...
  c13_b09.md: ...暂通知延迟）；(b) 触发第 1-4 步补偿。选择取决于用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计...
  c13_b10.md: ...西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务在 Crescent 内部 BRL 账本     -- 全程运行，FX 作为单独分录入账）。     BEGIN TRANSACTION;...
  c13_b10.md: ...;     COMMIT;   在 CockroachDB 下：    1. 事务以来自发起节点的 HLC 时间戳开始。Crescent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-D...
  c13_b10.md: ...intents 变成提交值。  跨区域延迟：每个 Raft commit 都涉及受影响分片在跨区域法定人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West / US-West），per-shard 提交最坏情况约 90...
  c13_b11.md: ...第28章 §28.2 会分解完整端到端，包括 TLS、geo-DNS、风控检查（Continuum LLM）和返回路径。Crescent 产品团队谈妥的 750 ms p99 SLO 轻松满足。无超卖、无双扣、无撕裂写。  这就是 2026 年跨区域分布式...
  c13_b17.md: ...模式。编排 vs 舞蹈化 saga；正确性义务（独立可提交、幂等、可补偿、补偿幂等）；saga-vs-2PC 决策矩阵；Crescent 的混合使用都在 §13.6.5 展开。两种模式在现代应用中共存。  第14章结束第四部分：NewSQL 全景。Span...
  c14_b03.md: ...a 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。Crescent 的实际转账记录可用 TigerBeetle；商家目录和用户画像则用 CockroachDB 或其他通用存储。   ##...
  c14_b06.md: ...### 14.11 实例：Crescent 的架构  2026 年，Olamide 的 Crescent 支付平台使用三种数据系统：  (a) **Cockroa...
  c14_b06.md: ...### 14.11 实例：Crescent 的架构  2026 年，Olamide 的 Crescent 支付平台使用三种数据系统：  (a) **CockroachDB** 用于运营存储：账户、交易、商家元数据、审计日志。...
  c14_b06.md: ...备。强可串行化事务保证支付完整性。  (b) **TigerBeetle** 用于实际转账账本：每笔资金变动的复式记账。Crescent 财务团队把 TigerBeetle 账本作为会计真相来源；CockroachDB 引用 TigerBeetle 转账...
  c14_b13.md: ...10 的决策矩阵将常见工作负载类别映射到首选系统。第11章的诊断方法缩小候选范围；第13章的事务协议视角区分候选者。  Crescent 的三系统架构（CockroachDB 运营存储、TigerBeetle 账本、Snowflake 分析）是金融级系统的...
  c15_b09.md: ...的工作负载（读重）很好。对多区域积极写同一 item 的工作负载不合适。系统文档说明了这点，但初始设计常被忽略。  对 Crescent：支付数据按用户归属区域分区，天然避免跨区域写冲突。São Paulo 用户总是写到南美区域；跨区域复制只用于灾备和分析...
  c17_b03.md: ...。   #### 模式 B：不同操作用不同系统  对需要强一致的操作用强一致系统；对其余用最终/CRDT。示例：  * Crescent（第14章）：CockroachDB 做运营存储（线性化）；独立计数器服务（CRDT）做页面浏览指标。 * Glide（...
  c17_b11.md: ...+ 条件写）、Cassandra（可调级别 + LWT）。应用工程师的任务是为每次操作正确选择。  混合架构组合系统：Crescent 用 CockroachDB（线性化）+ 自定义计数器服务（最终）+ Snowflake（分析）。Glide 用 Yjs...
  c17_b11.md: ...Cluster gossip；失效模式）和分布式锁（Redlock 辩论；fencing token 作为正确原语）。Crescent 支付系统使用 fencing token 保证协调者唯一性；本章解释这是防御 “身份谎言” 的正确手段。  ---...
  c18_b01.md: ...dis Cluster gossip 与缓存失效模式）和分布式锁（具体是为什么朴素锁会失败、正确原语长什么样）。第1章 Crescent 支付系统 “身份谎言” 的修复使用了 fencing token；本章解释原因。   ### 18.1 分布式缓存...
  c18_b02.md: ...性；缓存无形中成为记录系统。缓存故障（Redis 崩溃、内存驱逐）后数据丢失。缓解：纪律；把缓存当缓存，而不是存储。  Crescent 系统在热读路径（账户余额查询、幂等键检查）使用 Redis Cluster。每个缓存条目有短 TTL（60 秒）；未命...
  c18_b04.md: ...### 18.3 Crescent 的修复  回顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 f...
  c18_b04.md: ...### 18.3 Crescent 的修复  回顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：...
  c18_b04.md: ...若两个协调者短暂都认为自己持有角色（故障转移、分区愈合等期间），只有 token 更高的能行动。  有了这套机制，原始 Crescent 失败不会复发。即使两个协调者同时存活并尝试提交，银行的 fencing-token 检查保证至多一个成功。  Fenc...
  c18_b07.md: ...例使用基于共识的锁（etcd、Consul、ZooKeeper）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效...
  c18_b07.md: ...ing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；t...
  c18_b11.md: ...模式匹配，而非工程。正确回应是：“告诉我：这把锁保护什么？两个客户端同时持有它的失败模式是什么？进程可能暂停多久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM...
  c18_b11.md: ...的锁服务（etcd、Consul、ZooKeeper）加受保护资源上强制执行的 fencing token。  第1章 Crescent 修复结合基于 Raft 的协调者选举与银行端点的 fencing token。即使两个协调者短暂认为自己在位，只有 t...
  c19_b04.md: ...区域）的团队，服务网格过度。服务图复杂（50+ 服务、多区域、多协议）的团队，服务网格至关重要。   ### 19.7 Crescent 的发现架构  Olamide 的 Crescent 使用：  * **Kubernetes 原生服务发现** 用于区域...
  c19_b04.md: ...、多区域、多协议）的团队，服务网格至关重要。   ### 19.7 Crescent 的发现架构  Olamide 的 Crescent 使用：  * **Kubernetes 原生服务发现** 用于区域内服务间流量。每个服务是 Kubernetes Se...
  c19_b04.md: ...务发现** 用于区域内服务间流量。每个服务是 Kubernetes Service 对象；客户端解析 `payment.crescent.svc.cluster.local`；kube-proxy 负载均衡到健康 pod。  * **直接使用 etcd**...
  c19_b09.md: ...lag 为 true，执行中变 false）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存...
  c19_b11.md: ...发现与 mTLS、流量策略、可观测性打包。它适合多区域/多协议/复杂流量需求的团队；没有这些需求的团队则是过度工程。  Crescent 的发现使用四个分层系统：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul...
  c20_b04.md: ...race 存储方式不同：对特定操作类型 100% 采样；保留数年（而非几天）；按客户或交易 ID 索引以便检索。  对 Crescent：每笔支付 trace 保存 7 年（监管要求）。Trace 存储成本显著，但被视为合规成本。   ### 20.7 练...
  c21_b04.md: ...### 21.7 工作流 10：级联故障事后复盘  一个真实世界事后复盘（虚构但具有代表性），针对 Crescent：  **日期**：2026-03-14。  **影响**：服务降级 47 分钟；窗口期内 12% 支付交易失败。  *...
  c22_b06.md: ...不同；都需要。  正确深度取决于工作负载。无外部依赖的无状态服务可用廉价检查。有状态服务或有关键依赖的服务需要深度检查。Crescent 支付服务使用深度检查：每 30 秒一次真实的（低流量）测试交易；失败表示该节点不应接收生产流量。...
  c22_b08.md: ...坏性太大（流量高峰、高收入期）；缓存可在减少流量下重建；团队有工具应用限流。  推荐：取决于上下文。  * 对工作时间 Crescent 支付系统：限流。重启的短暂中断不可接受；限流部分流量更可取。  * 对非工作时间内部分析服务：重启。短暂中断可接受；更...
  c22_b09.md: ...对 Crescent：区域负载均衡器上使用带 outlier detection 的 Envoy。表现出集群中位数 5 倍延迟的后端被 “弹...
  c22_b10.md: ...DN）反馈循环少，很少表现出亚稳态。风险与复杂度相关；较不复杂的系统暴露更少。  综合：复杂分布式系统会发生亚稳态故障。Crescent 支付平台、Google Search、AWS S3 都足够复杂，已展示过亚稳态。简单系统可能确实避免它。准备包括 (a...
  c22_b11.md: ...2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day...
  c23_b05.md: ...纪律。  (c) **Level 2（Game days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Cha...
  c23_b08.md: ...对 Crescent：团队在采用任何新分布式数据库前都会咨询 Jepsen 报告。报告是评估候选者的诊断方法（第11章）的一部分。...
  c23_b12.md: ...做）；正确框架是 “这个系统的合适级别是什么”。  2026 现实：成熟 SRE 组织的混沌工程实践与系统关键性成比例。Crescent 支付系统每月 game day；内部 HR 系统没有。两者在其上下文中都正确。  第二个考虑：“运维失职” 的说法太强...
  c27_b07.md: ...#### 三层审查架构  Crescent 团队对 AI 生成分布式代码使用三层审查：  **第一层 —— 自动 AI 审查（秒级，每次 push 运行）**：...
  c27_b07.md: ...是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度...
  c27_b08.md: ...一项人类检查的实质性内容（工作负载、一致性要求、业务不变量）。若评论只是 AI 审查后 “LGTM”，就是橡皮图章。  Crescent 审查政策强制执行：每个分布式 PR 批准需要一句话解释人类在 AI 输出之外验证了什么。成本小（一句话）；纪律真实。...
  c27_b12.md: ...为错误分布式代码的成本高（数据污染、中断、客户影响）。对非分布式代码（UI、简单 CRUD 等），较轻政策即可。  对 Crescent：Olamide 在第1章原始重复扣款事故后起草了类似政策。三年后，团队已内化纪律；AI 辅助 PR 按清单审查；混沌测...
  c27_b13.md: ...cs，而非接受标签。LLM 经常产出使用模式 _词汇_ 但无 _语义_ 的代码。词汇在训练数据中；语义需要理解。  对 Crescent：每个 saga PR 都按以上七个问题审查。每次审查耗时 30 分钟以上；成本合理，因为 saga bug 是灾难性的...
  c27_b14.md: ...，而非二元。团队应根据风险容忍度设政策：高风险代码（分布式、安全敏感）严格审查；低风险代码（UI、内部工具）宽松。上述 Crescent 政策是一种表述；它不禁止 AI 辅助，但要求捕获 LLM 失败模式的纪律。  2026 现实：大多数团队在某种程度上使...
  c27_b15.md: ...对 Crescent：Olamide 事故后三年建立了以上每项。团队分布式代码质量显著改善；涉及六个谎言的新事故罕见。投资（审查时间、混沌测...
  c28_b02.md: ...### 28.1 架构  平台：Crescent 面向 enterprise 客户的多 region 支付处理器。全球接受支付，端到端 p99 延迟低于 750 ms（...
  c28_b03.md: ...完整的 Crescent 技术栈可视化：       graph TB         subgraph Edge["Edge (Cloudfl...
  c28_b04.md: ...daeze 的计算 home region，由 geo-DNS 决定；圣保罗到 US-East 单程约 55 ms，是 Crescent 三个 region 中最近的）。60 ms 跨赤道。  (3) US-East 的 regional 负载均衡器路由到...
  c28_b04.md: ...M 尾部更宽，且跨 region 提交跳每跳付出约 95 ms 的最坏情况 Raft quorum 物理代价）。远低于 Crescent 产品团队为跨 region 支付地理正确性协商的 750-ms p99 SLO。  Trace（第20章）捕获整个旅程...
  c28_b05.md: ...究” 列把每课与练习它的附录 E 真实世界事件关联；将案例研究与课程并读可让抽象具体化。  | 章节 | 课程 | 在 Crescent 中的位置 | 案例研究（附录 E） | |---|---|---|---| | 1 | 三个谎言（时间、身份、故障）...
  c28_b07.md: ...（Stratus DB 主管）**：“带 fencing token 的 Raft 复制协调器是系统安全的核心。集成到 Crescent 技术栈很直接，因为 Stratus DB 从一开始就用这些模式设计。诊断方法（第11章）是我们向 Crescent 团...
  c28_b07.md: ...到 Crescent 技术栈很直接，因为 Stratus DB 从一开始就用这些模式设计。诊断方法（第11章）是我们向 Crescent 团队推销集成的方式；他们对 Stratus 运行了该方法，验证了其 profile 匹配需求。”  **Yuki（从...
  c28_b07.md: ...Glide 借调）**：“商户入驻合同编辑器使用 Yjs。协作编辑 ‘刚好工作’，因为 CRDT 确定性地处理冲突。与 Crescent 的认证和审计追踪系统集成是难点；编辑器本身很容易。”  **Tobias（从 Continuum 借调）**：“欺诈检...
  c28_b07.md: ....2 记录一致），得益于分离式架构和 KV-cache-aware 路由。集成只是一个简单 HTTP endpoint；Crescent 的 API 服务调用我们的 endpoint；我们返回分数。复杂度在我们这边；Crescent 这边微不足道。”  四...
  c28_b07.md: ...TP endpoint；Crescent 的 API 服务调用我们的 endpoint；我们返回分数。复杂度在我们这边；Crescent 这边微不足道。”  四个视角说明 2026 现实：复杂分布式系统由组合组件构建，每个组件来自拥有深厚专长的团队。本书纪...
  c28_b08.md: ...种 _姿态_：愿意对每个组件、每个连接、每个操作都假设最坏情况，直到被验证。  2026 年内化这种姿态的工程师构建出 Crescent 这样的系统。没有内化者产出第1章的原始重复扣款 bug，以及许多类似 bug，并不断惊讶于 “测试通过的系统怎么在生产...
  c28_b12.md: ...维纪律（追踪、级联故障、混沌工程），到第27章回顾为什么 LLM 不擅长这个，以及第28章的综合。内化纪律的读者能构建 Crescent 这样的系统；没有内化的读者产出有 bug 的系统。  **Q2.** 指出下列原语分别由哪一章引入：HLC、fenci...
  c28_b12.md: ...fencing token 防御身份谎言；带预算的重试防御故障谎言的放大。完整防御需要多个原语共同使用。  第28章的 Crescent 系统使用了所有上述原语。本书路径就是积累工具包；综合就是一起使用工具包。...
  c28_b13.md: ...长领域。2026 年进入该领域的工程师处于前沿；未来 5-10 年将显著发展。本书覆盖让他们为该领域快速演进做准备。  Crescent 系统（第28章）把 Continuum 的推理组件作为更大系统的一部分。关于推理的章节不是独立的；它们融入更广泛的分布...
  c28_b14.md: ...pply` 收紧。  优势：成熟、文档丰富、SQL 强大。劣势：failover 不可用窗口；异步复制 RPO。  对 Crescent：Postgres + Patroni 不满足多 region 需求；CockroachDB 是正确选择。对单 regi...
  c28_b14.md: ...每个连接、系统中每个操作的最坏情况。  "没有这种姿态的工程师构建出测试通过、生产失败的系统。有这种姿态的工程师构建出 Crescent 这样的系统。区别不在于知识；而在于不信任的纪律。...
  c28_b15.md: ...。新工程师的投资应在模型，而非记忆算法。  对 Olamide 来说，这种指导风格是她打造团队的方式。团队的集体纪律让 Crescent 系统健壮；这种纪律比任何单个工程师的具体知识更重要。  **Q3.** 反思你遇到过的一个分布式系统 bug（生产或课...
  c28_b18.md: ...### 本章总结  Crescent 多 region 支付平台是综合。四位主角（Olamide、Anya、Yuki、Tobias）贡献了组件；系统使用了书...
  c30_b03.md: ...Bitter Lesson" (Sutton, 2019) 及后续评论。 — 为什么模式匹配主导 LLM 行为。 * Crescent 事后复盘（本书全文）。 — 六个谎言的工作示例。   #### 前瞻（第29章 — 后记）  * Amazon Web...
  c30_b07.md: ...| | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-operation 一致性选择 |  Crescent 支付系统对每个谎言的防御在第28章详述。  * * *   ### 附录 E — 案例研究  七个真实世界分布式系统事...

=== commit (98 occurrences) ===
  c01_b01.md: ...US-East 区域的南美洲网关（网关设在圣保罗，负责把流量路由到 US-East 的运维数据面）。两者都记录了干净的 commit。两者都相信自己是唯一处理该事务的协调者。客户的手机当时正通过一个不稳定的 3G 连接，在第一份响应变慢后重试了一次请求...
  c02_b04.md: ...：一个需要跨区域副本法定人数往返的提交，比只需写入本地 leader 的提交更慢。Spanner 支付著名的 7 ms commit-wait（第13章），通过 TrueTime 提供线性化时间戳。  DynamoDB 在分区期间是 AP（分区节点继续...
  c02_b04.md: ...系统 | 分区期间 | 否则（无分区） ---|---|--- Spanner | C（拒绝少数侧写入） | C，伴随 commit-wait 延迟 CockroachDB | C（拒绝少数侧写入） | C，伴随法定人数写入延迟 TiDB / Yuga...
  c02_b08.md: ...致性。  (a) Spanner：PC/EC。分区时偏好一致性（被分区出去的副本拒绝写入）。无分区时仍保持一致，代价是 commit-wait 延迟（TrueTime 带来的 7 ms 成本）。  (b) CockroachDB：PC/EC。分区时，少...
  c02_b08.md: ...是线性化需求。在 PACELC 下，两个选项都能满足需求，但代价不同。  Spanner：默认线性化。每次库存减量支付 commit-wait 延迟（约 7 ms 基线，加上法定人数往返）。在 100,000 请求/秒下，Spanner 需要足够分片（...
  c02_b11.md: ...言、一种访问控制模型。 * 优点：跨工作负载事务变得简单（原子地减量库存并更新排行榜）。 * 缺点：Spanner 的 commit-wait 延迟每次写入都要付，即使对工作负载并不需要。 * 缺点：每个分片吞吐受共识往返限制，计数器在 Spanner...
  c02_b14.md: ...Lamport、向量、HLC、TrueTime 等协议，让你能够在机器间推理顺序而不撒谎。本章提到的 Spanner commit-wait 延迟将在第3章完整解释；CockroachDB 基于 HLC 的 MVCC 将在第13章完整解释。  ---...
  c03_b07.md: ...加上一层监控并报告不确定性的软件来实现这一点。典型 TrueTime 不确定度在 1 到 7 毫秒量级。   #### commit-wait 协议  Spanner 使用 TrueTime 为每个事务分配一个提交时间戳，保证该时间戳大于系统过去任何已...
  c03_b07.md: ...置的最大时钟偏移上界（默认 500 ms）作为“给普通人的 TrueTime 替代方案”。Spanner 的 7 ms commit-wait 在 CockroachDB 中变成更大的不确定性窗口，表现为事务重启成本而非 commit-wait，但精神...
  c03_b07.md: ...r 的 7 ms commit-wait 在 CockroachDB 中变成更大的不确定性窗口，表现为事务重启成本而非 commit-wait，但精神相似：先界定时钟偏移，再设计容忍它的协议。   #### 常见误解  一个常见信念：“如果我跑 NTP...
  c03_b08.md: ...primitive | What it costs ---|---|--- Spanner | TrueTime + commit-wait | 每次写事务 7 ms + GPS 原子钟基础设施 CockroachDB / YugabyteDB /...
  c03_b12.md: ...构）还是只有 _一致排序_ （HLC 已提供）。大多数分布式应用代码只需要后者。  **Q3.** Spanner 的 commit-wait 是 7 ms。CockroachDB 的 HLC 带偏移上界默认 500 ms。为什么 CockroachD...
  c03_b14.md: ...启。第二，全局时间戳排序在任何分区下都能存活而不重启。对重启不可接受的工作负载（热键高竞争写入），TrueTime 的 commit-wait 比分摊到每次重启的成本更划算。  综合：HLC 加严格 NTP 对大多数团队是正确默认。TrueTime 对...
  c03_b14.md: ...：HLC 加严格 NTP 对大多数团队是正确默认。TrueTime 对能负担 GPS 基础设施且工作负载中重启成本超过 commit-wait 成本的团队是正确答案。AWS 的 clock-bound API（Amazon Time Sync Serv...
  c03_b15.md: ...应用暴露 `[earliest, latest]` 不确定性区间。Spanner 用 TrueTime 支付 7 ms commit-wait，换取线性化全局时间戳。AWS 的 clock-bound API 正在让 TrueTime 类原语在云厂商上...
  c05_b11.md: ...一步：跨区域复制。光速限制、数据中心拓扑（CLOS、leaf-spine）以及跨区域的延迟税。第3章的 Spanner commit-wait 会作为一致全局复制的基本成本重新出现；替代方案（区域分区、follower 读、geo-DNS）是在不每次操...
  c06_b08.md: ...理模型。  (c) **全局复制（Spanner）。** 每个聊天室数据驻留在某个区域并全球复制。写付 Spanner commit-wait（~7 ms）加 quorum 往返；跨大陆 quorum 约 200 ms 提交。读可用 follower...
  c07_b10.md: ...C 阻塞性质的遗漏。  **讨论：** 2PC 有一个臭名昭著的阻塞失败模式：若协调者在发送 PREPARE 后、发送 COMMIT 或 ABORT 前崩溃，参与者就卡住。它们已投票（并持有锁），无法自行决定（不知道其他参与者投票），也不能中止（协调者...
  c08_b09.md: ...结构_ ：一个协调者给所有参与者发 PREPARE（或 VOTE_REQUEST）。若所有参与者投 yes，协调者发 COMMIT；若有任何 no，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在 PREPARE 后、发...
  c08_b09.md: ...；若有任何 no，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在 PREPARE 后、发 COMMIT/ABORT 前崩溃，参与者被阻塞。它们已投 yes（并持有锁），但不知道协调者的决定。它们不能单方面决定，因为协调者可...
  c09_b03.md: ...副本上以相同顺序恰好应用一次。机制是 Raft 的 leader-election + AppendEntries + commit-on-majority。变体：per-range Raft（CockroachDB）用于水平扩展；multi-Raft...
  c09_b03.md: ...保 follower 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follo...
  c09_b07.md: ...在此处与 leader 分歧）。  否则，追加新条目（覆盖任何冲突的后续条目），按 `leaderCommit` 推进 commit index，回复成功。  这保证：一次成功 `AppendEntries` 后，follower 日志在新条目末尾之前...
  c09_b08.md: ...下一批条目（通常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追上当前日志末尾。1000 条目、RTT 50 ms、每...
  c10_b02.md: ...他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。...
  c10_b02.md: ...e 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？...
  c10_b02.md: ...” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时...
  c10_b02.md: ...播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副...
  c10_b02.md: ...prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 al...
  c10_b02.md: ...Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 all-to-all 广播，每个决定 $...
  c10_b02.md: ...副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 all-to-all 广播，每个决定 $O(N^2)$ 消息。$N=4$（...
  c10_b02.md: ...他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。...
  c10_b02.md: ...e 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？...
  c10_b02.md: ...” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时...
  c10_b02.md: ...播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副...
  c10_b02.md: ...prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 al...
  c10_b02.md: ...Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 all-to-all 广播，每个决定 $...
  c10_b02.md: ...副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 all-to-all 广播，每个决定 $O(N^2)$ 消息。$N=4$（...
  c10_b05.md: ...ader 变更需要更大的 prepare 法定人数。  这一洞见用于一些 2026 年分布式数据库设计，以降低 per-commit 延迟而不牺牲 safety。CockroachDB 和 TiDB 的路线图中已有变体；FoundationDB 多年来...
  c10_b10.md: ...返。  _HotStuff pipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票...
  c10_b10.md: ...tStuff pipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块...
  c10_b10.md: ...些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipeline 是“不同决定的阶段重叠”，每个决定总共需三轮，但吞吐可...
  c10_b10.md: ...$k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipeline 是“不同决定的阶段重叠”，每个决定总共需三轮，但吞吐可达每轮一个决定（而非每三轮一个决定）。...
  c11_b03.md: ...plication protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（...
  c11_b03.md: ...——少数侧停止接受写；TrueTime 确保分区后时间戳一致 PACELC | PC / EC（分区下一致；正常运营为 commit-wait 延迟买单） Strengths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses...
  c11_b03.md: ...启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性化的工作负载（广告、金融账本）；Google 自身基础设施   #...
  c12_b11.md: ...e 的 Percolator（真正的 Spanner 风格事务模式）、Calvin 的确定性排序、TrueTime 的 commit-wait、CockroachDB 的 HLC-based MVCC 作为实用 TrueTime 替代。四位主角的多区域...
  c13_b01.md: ...为 prepared（但未提交）状态。   3. 回复 `YES`（可提交）或 `NO`（必须中止）。  **阶段 2：Commit 或 Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finaliz...
  c13_b01.md: ...）或 `NO`（必须中止）。  **阶段 2：Commit 或 Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finalize prepared 数据。  若任一参与者投 NO（或协调者超时），协调...
  c13_b02.md: ...#### 2PC 为何阻塞  经典失败模式：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已经投票（并持有锁）。他们无法自行决定（协调者可能在崩溃前已提交）。只能等待。锁被持有。冲突...
  c13_b03.md: ...nner 风格” 数据库在底层实际采用的模式。  结构：在键值存储之上的快照隔离事务，使用两阶段（Prewrite + Commit），通过一个指定的 “primary” 行协调。无中心协调者。  **阶段 1：Prewrite**  事务客户端从要写...
  c13_b03.md: ...y keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 comm...
  c13_b03.md: ...mmit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 commit 记录（value, commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary...
  c13_b03.md: ...nner 风格” 数据库在底层实际采用的模式。  结构：在键值存储之上的快照隔离事务，使用两阶段（Prewrite + Commit），通过一个指定的 “primary” 行协调。无中心协调者。  **阶段 1：Prewrite**  事务客户端从要写...
  c13_b03.md: ...y keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 comm...
  c13_b03.md: ...mmit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 commit 记录（value, commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary...
  c13_b04.md: ...3. 事务 _等待_ 直到 `TT.now().earliest > commit_timestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任...
  c13_b04.md: ...mmit_timestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任何观察到此次提交的事务（以大于 commit_timestamp 的时间戳读）自...
  c13_b04.md: ...参与者是各 tablet（各跑 Paxos）。2PC 层增加 commit_timestamp 协调。TrueTime commit-wait 确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime co...
  c13_b04.md: ...it-wait 确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime commit-wait 加上 Paxos 法定人数往返决定。对 Spanner 典型的 7 ms TrueTime 不确定性与区域...
  c13_b04.md: ...3. 事务 _等待_ 直到 `TT.now().earliest > commit_timestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任...
  c13_b04.md: ...mmit_timestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任何观察到此次提交的事务（以大于 commit_timestamp 的时间戳读）自...
  c13_b04.md: ...参与者是各 tablet（各跑 Paxos）。2PC 层增加 commit_timestamp 协调。TrueTime commit-wait 确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime co...
  c13_b04.md: ...it-wait 确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime commit-wait 加上 Paxos 法定人数往返决定。对 Spanner 典型的 7 ms TrueTime 不确定性与区域...
  c13_b05.md: ...Cost ---|---|---- Spanner | TrueTime（GPS + 原子钟） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） |...
  c13_b10.md: ...1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';     COMMIT;   在 CockroachDB 下：    1. 事务以来自发起节点的 HLC 时间戳开始。Crescent 运营三...
  c13_b10.md: ...TE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取 write intent。   6. `COMMIT`：事务协调者（本身通过 Raft 复制）在选定的 commit_timestamp 标记事务提交。intents 变成...
  c13_b10.md: ...）在选定的 commit_timestamp 标记事务提交。intents 变成提交值。  跨区域延迟：每个 Raft commit 都涉及受影响分片在跨区域法定人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West...
  c13_b12.md: ...描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发...
  c13_b12.md: ...，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 P...
  c13_b12.md: ...若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修...
  c13_b12.md: ...Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它通过两阶段（Prewrite + Commit）在键值存储上实现快照隔离事务，无中心协调者。  如何避免协调者：事务指定其中一个键为 _primary_。原子提交点是...
  c13_b12.md: ...描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发...
  c13_b12.md: ...，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 P...
  c13_b12.md: ...若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修...
  c13_b12.md: ...Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它通过两阶段（Prewrite + Commit）在键值存储上实现快照隔离事务，无中心协调者。  如何避免协调者：事务指定其中一个键为 _primary_。原子提交点是...
  c13_b13.md: ...时间都在本事务提交之后。结果是全局线性化：跨越全球所有 Spanner 实例，时间戳形成与实时一致的全序。  7 ms commit-wait 在每次写上支付。成本是 TrueTime-based 线性化的代价，无需为时间戳做协调者往返。...
  c13_b14.md: ...(c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中...
  c13_b14.md: ...2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换...
  c13_b14.md: ...间为单热行高争用工作负载选择（如每个事件都递增的全局计数器）。比较两系统的预期延迟。  提示  Spanner：每次写 commit-wait（恒定 7 ms）。CockroachDB：争用时重启（可变成本）。  **答案：** 单行高争用是两者的最坏...
  c13_b14.md: ...r_ ：每次递增获取行级锁，等待提交时间戳（7 ms），然后提交。单热行吞吐受每行提交串行化限制：若瓶颈 purely commit-wait，每秒最多约 140 次递增，实际因锁获取开销约 50-100 次。  _CockroachDB_ ：每次递增...
  c13_b14.md: ...(c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中...
  c13_b14.md: ...2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换...
  c13_b14.md: ...间为单热行高争用工作负载选择（如每个事件都递增的全局计数器）。比较两系统的预期延迟。  提示  Spanner：每次写 commit-wait（恒定 7 ms）。CockroachDB：争用时重启（可变成本）。  **答案：** 单行高争用是两者的最坏...
  c13_b14.md: ...r_ ：每次递增获取行级锁，等待提交时间戳（7 ms），然后提交。单热行吞吐受每行提交串行化限制：若瓶颈 purely commit-wait，每秒最多约 140 次递增，实际因锁获取开销约 50-100 次。  _CockroachDB_ ：每次递增...
  c13_b16.md: ...TrueTime；若你在 AWS/Azure/多云且运维简单性重要，用 HLC。  更微妙考虑：TrueTime 的 commit-wait 是每次写 _无条件_ 支付；HLC 的重启成本在争用事件上 _有条件_ 支付。对低争用工作负载，HLC 均摊...
  c13_b17.md: ...(d) **协议消息幂等性**。网络重试可能使 PREPARE 或 COMMIT 到达两次。参与者必须幂等处理。LLM 代码通常不会。  (e) **参与者崩溃后的恢复**。参与者在投 YES 后、收...
  c13_b17.md: ...达两次。参与者必须幂等处理。LLM 代码通常不会。  (e) **参与者崩溃后的恢复**。参与者在投 YES 后、收到 COMMIT 前崩溃，恢复时必须记住其投票并一致行动。这需要 prepared 状态的持久化存储。LLM 代码常用内存状态，崩溃后丢...
  c13_b17.md: ...、Calvin（通过 sequencer 的确定性排序，无中止）、TrueTime/Spanner（硬件辅助时间戳 + commit-wait）。  现代 2PC 模式（Spanner、CockroachDB、FoundationDB 使用）通过共识复...
  c18_b03.md: ...Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：用 fencing token。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁...
  c18_b04.md: ...lock         L-->>C1: granted, token=100         C1->>Bank: commit (token=100)         Bank->>Bank: 100 >= highest (was 0); ac...
  c18_b04.md: ...lock         L-->>C2: granted, token=101         C2->>Bank: commit (token=101)         Bank->>Bank: 101 >= highest (100); acce...
  c18_b04.md: ...Note over C1: resumed after pause         C1->>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highest (101)...
  c20_b01.md: ...dger write<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network out)"]...
  c28_b04.md: ...merchant_day_rollup。跨 shard、跨 region 事务，带 HLC 时间戳；为可串行化一致性做 commit-wait。95 ms（受第6章所说的最坏情况跨 region quorum 物理限制）。  (10) 事务的 coor...
  c28_b10.md: ...机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 | | 5 | HotSt...
  c30_b12.md: ...tgres + 读副本足够时不要伸手选 Spanner。** Spanner 是最干净的多 region 系统，但它的 commit-wait 延迟每次写入都要支付。对单区域工作负载，Postgres 生态成熟度和每次写入延迟通常更好。只有真正需要其能...
  c30_b14.md: ...inimal.py   — ~300 lines: leader election + AppendEntries + commit     │   ├── kv_state_machine.py — replicated key-value stor...

=== day (97 occurrences) ===
  c01_b08.md: ...了系统一直讲述的三个谎言。银行向 Adaeze 退款。Crescent 在内部发布了复盘，并在下周进行了一次 game day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuk...
  c13_b10.md: ...lup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';     COMMIT;   在 CockroachDB 下：    1. 事务以来自发...
  c21_b04.md: ...进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证修复。  事后复盘模式：识别触发器、放大和防御缺失。每一项都是独立修复。防御缺失（上面 c）通常是最杠杆...
  c22_b11.md: ...days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “...
  c23_b01.md: ...ent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day...
  c23_b01.md: ...day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026...
  c23_b01.md: ...故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  G...
  c23_b01.md: ...ame day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  Game day 结构：  (a) **赛前（1-4 周前）**：设计实验。定义要注入的故障、稳态假设、回滚计划、参与者和角色。  (b...
  c23_b01.md: ...盘**：结束实验（回滚或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴...
  c23_b01.md: ...）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴露运维缺口。...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “...
  c23_b01.md: ...ent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障模式。混沌 game day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day...
  c23_b01.md: ...day 是弥合 “我们构建了防御” 与 “我们知道防御有效” 之间鸿沟的纪律。  本章是工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026...
  c23_b01.md: ...故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  G...
  c23_b01.md: ...ame day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  Game day 结构：  (a) **赛前（1-4 周前）**：设计实验。定义要注入的故障、稳态假设、回滚计划、参与者和角色。  (b...
  c23_b01.md: ...盘**：结束实验（回滚或自然恢复）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴...
  c23_b01.md: ...）；团队回顾；记录学习。  (e) **赛后**：提交改进行动项；安排下一次 game day 验证修复。  Game day 既是技术练习，也是 _社交_ 练习。它教会团队了解系统；建立故障模式的共享心智模型；暴露运维缺口。...
  c23_b02.md: ...ency in Practice》）是混沌工程组织方面的权威声音。  关键实践：  (a) **心理安全**。Game day 必须让团队能无责地识别问题。若失败暴露运维缺口，讨论应是 “我们如何修复系统” 而非 “谁负责”。没有心理安全，团队会...
  c23_b02.md: ...失、runbook 错误）而非个人错误。责备产生防御行为；系统改进需要诚实分析。  (c) **多元参与**。Game day 不仅应包括 on-call 工程师，还应包括产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什么” 有...
  c23_b02.md: ...客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什么” 有不同视角。  (d) **记录学习**。每次 game day 都产生书面产物：测试了什么、发生了什么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构记忆。...
  c23_b02.md: ...么、学到了什么、提交了哪些行动项。随着时间，这些产物形成系统故障模式的机构记忆。  (e) **固定节奏**。Game day 不是一次性事件。每月或每季度节奏保持肌肉锻炼；新成员通过参与学习。  组织实践才是区分 “我们用 Chaos Monk...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群 EU-W...
  c23_b04.md: ...秒检测。 * 在客服面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” ru...
  c23_b04.md: ...故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群 EU-W...
  c23_b04.md: ...秒检测。 * 在客服面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” ru...
  c23_b04.md: ...故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b04.md: ...障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并演练不同故障模式。...
  c23_b05.md: ...（Game days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey 风格：...
  c23_b06.md: ...的属性（规模、流量模式、完整依赖图）。实验室测试必要但不充分。  (d) **自动化实验以持续运行**。手动 game day 有价值；Chaos Monkey 风格的持续自动混沌让系统永久被测试。  (e) **最小化爆炸半径**。从小处开始（...
  c23_b06.md: ...可扩大。  这些原则是基础。具体工具（Chaos Mesh、Litmus、Gremlin）实现它们；组织实践（game day、无责复盘、记录学习）让它们落地。  **Q2.** 什么是 game day？描述其结构。  提示  赛前、当天、执行...
  c23_b06.md: ...emlin）实现它们；组织实践（game day、无责复盘、记录学习）让它们落地。  **Q2.** 什么是 game day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day 是一次...
  c23_b06.md: ...ame day？描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day 是一次 scheduled、故意的混沌练习。团队聚集；注入故障；观察系统反应；记录学习。  结构：  (a) **赛前...
  c23_b06.md: ...e) **赛后**：  * 提交改进行动项（修复 bug、更新 runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏保持团队...
  c23_b06.md: ...runbook、增加监控）。 * 安排下一次 game day 验证修复。 * 向更广泛工程组织传播学习。  Game day 是周期性实践，不是一次性事件。每月或每季度节奏保持团队的故障响应肌肉锻炼。  **Q3.** 为什么心理安全在混沌工程...
  c23_b06.md: ...响应肌肉锻炼。  **Q3.** 为什么心理安全在混沌工程实践中很重要？  提示  没有安全，团队会隐藏问题。Game day 暴露问题；安全让团队诚实讨论。...
  c23_b07.md: ...**答案：** Game day 暴露问题：团队不知道的故障模式、运维缺口、潜伏的 bug。赛后讨论是捕获学习的机会。  若团队害怕（害怕责备、害怕显得...
  c23_b07.md: ...若团队害怕（害怕责备、害怕显得无能、害怕对缺口负责），他们会隐藏问题。他们会淡化意外、缩小缺口、避免升级。Game day 的价值就被摧毁。  心理安全意味着：团队可以在不被个人责备的情况下指出问题。“重试预算太高，导致级联” 是系统观察；“...
  c23_b09.md: ...#### 应用问题  **Q1.** Olamide 正在策划一次测试区域故障转移的 game day。走一遍她在当天前需要回答的设计问题。  提示  稳态假设、爆炸半径、回滚计划、参与者、成功标准、runbook 更新。...
  c23_b09.md: ...I 的实时面板；需要回滚时触发的告警；每个操作的日志。  (i) **会产生哪些 runbook 更新？** Game day 会暴露 runbook 缺口；捕获流程是什么？  (j) **沟通计划是什么？** 之前/期间/之后通知谁？如何向更广...
  c23_b10.md: ...Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加网络分区...
  c23_b10.md: ...key 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加网络分区混沌、慢节点混沌、为区域故障运行 game day。每次扩展都会发现新问题。  更深观察：混沌工程不是 “完成” 的状态。它是随系统成长的持续实践。随着系统增加功能、集成...
  c23_b10.md: ...扩展到新区域，混沌面也在增长。成熟混沌工程实践把它视为持续投资，而非一次性里程碑。  **Q3.** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug...
  c23_b10.md: ...** 某团队 game day 发现一个真实 bug（导致数据损坏的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修...
  c23_b10.md: ...的竞态条件）。他们现在争论：继续计划的后续 game day，还是先暂停修复 bug。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：**...
  c23_b10.md: ...。分析。  提示  继续 game day 与暂停修复。Bug 真实；继续可能发现更多问题；先修复确保下一次 game day 测试正确的东西。  **答案：** 两种选择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game...
  c23_b10.md: ...择都有道理；正确答案取决于 bug 严重程度和团队带宽。  _继续 game day_ ：  * 优点：每次 game day 都揭示更多问题；团队建立行动项清单并按优先级修复。 * 缺点：已知 bug 仍在生产环境；间隙期间有客户影响风险。...
  c23_b10.md: ...ug 仍在生产环境；间隙期间有客户影响风险。  _暂停并修复_ ：  * 优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因...
  c23_b10.md: ...优点：已知 bug 快速修复；后续 game day 测试已移除 bug 的系统（更有用数据）。 * 缺点：game day 计划推迟；其他问题可能更久未被发现。  推荐因素：  (a) **Bug 严重程度**：若是数据损坏（如示例），属于高...
  c23_b10.md: ...*Bug 严重程度**：若是数据损坏（如示例），属于高严重。立即暂停修复；不要继续可能让更多客户暴露于损坏的 game day。  (b) **修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有...
  c23_b10.md: ...更多客户暴露于损坏的 game day。  (b) **修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **...
  c23_b10.md: ...**修复复杂度**：若修复小（几小时），暂停并修复；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧...
  c23_b10.md: ...；恢复 game day。若修复大（几周），不要阻塞所有 game day；继续不触发该 bug 的限定范围 game day。  (c) **生产影响**：bug 是否已影响客户？若是，修复紧迫性最高。若只在特定条件下触发，更灵活。  (d)...
  c23_b10.md: ...转移下重复扣款）：bug 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 targeted 实验验证修复；修复验证后恢复常规 game day 计划。...
  c23_b10.md: ...可解决）。推荐：暂停大型 game day；可继续小范围 targeted 实验验证修复；修复验证后恢复常规 game day 计划。...
  c23_b11.md: ...更深观察：game day 的价值在于发现 bug。发现 bug 是成功，不是失败。团队应庆祝发现 bug（比客户发现好），并优先修复。在已知未修...
  c23_b12.md: ...败是惊喜还是排练过。混沌工程把惊喜变成排练。没有混沌工程的系统，每次失败都是惊喜；团队从生产事故（昂贵）而非 game day（廉价）学习。对任务关键系统，这是运维失职。  反驳：混沌工程有成本（工程师时间、复杂度、练习期间偶尔的客户可见事故）。...
  c23_b12.md: ...么”。  2026 现实：成熟 SRE 组织的混沌工程实践与系统关键性成比例。Crescent 支付系统每月 game day；内部 HR 系统没有。两者在其上下文中都正确。  第二个考虑：“运维失职” 的说法太强，因为它暗示跨越团队边界的道德判...
  c23_b12.md: ...含什么。  **讨论：** LLM 只给了 _工具_ 答案，没给 _实践_ 答案。工具必要但不充分；组织实践（game day、无责复盘、记录学习、固定节奏）才是让混沌工程生效的东西。  完整答案包括：  (a) **工具**：是的，列出工具（C...
  c23_b12.md: ...的，列出工具（Chaos Mesh、Litmus、AWS FIS 等）及其对团队技术栈的适配。  (b) **Game day 结构**：赛前/当天/执行/复盘/赛后的模式。团队需要运营 playbook，不只是工具。  (c) **组织实践**...
  c23_b12.md: ...理安全、记录学习、固定节奏。没有这些，技术实验不产生学习。  (d) **成熟度进阶**：从 Level 2（game day）到 Level 3（持续混沌）之前。每一层需要前一层；跳过会坏。  (e) **具体第一次实验**：对 “先做什么”...
  c23_b13.md: ...(f) **混沌工程自身指标**：团队如何知道其混沌实践有效？指标如 “每次 game day 发现的问题数”、“混沌工程预防的事故数”、“真实事故中缓解时间（因排练而缩短）”。  (g) **沟通与认同**：混沌...
  c23_b13.md: ...漏了组织维度。  正确 LLM 回应：“混沌工程既是技术也是组织。工具包括 [...]。实践包括带无责复盘的 game day。对起步团队，我推荐 [...]。以下是 3 个月建立该计划 [...]。工具只是答案的一部分。”   ### 本章总结...
  c23_b13.md: ...入故障以验证系统故障下行为的实践。五条原则：稳态假设、变化真实事件、在生产中运行、自动化、最小化爆炸半径。  Game day 是 scheduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性事件。...
  c23_b13.md: ...故障下测试数据库正确性声明。  成熟度模型：Level 0（无混沌）、Level 1（手动）、Level 2（game day）、Level 3（持续）、Level 4（混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4...
  c23_b13.md: ...计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它...
  c23_b13.md: ...1 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它在客户之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学科；vL...
  c27_b07.md: ...签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态审查的经...
  c27_b07.md: ...行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 * 这是静态审查的经验补充。 * 成熟度映射：第三层对应第23章 §23.7...
  c27_b07.md: ...而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查 → 人类审查 → 混沌验证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 AI +...
  c27_b07.md: ...证循环闭合；game day 发现反馈为 AI 审查者的新模式，以便在未来 PR 中标记。  第三层纪律具体：game day 必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”...
  c27_b07.md: ...AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 cra...
  c27_b07.md: ...r 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧...
  c27_b07.md: ...补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确...
  c27_b07.md: ...审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在...
  c27_b07.md: ...的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day...
  c27_b07.md: ...day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamid...
  c27_b07.md: ...每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流添加 s...
  c27_b07.md: ...ft 复制；步骤 4（外部 partner API）有 0.1 重试预算和 50% 失败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢复；(b...
  c27_b12.md: ...队共享；模式级问题导致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事后复盘识...
  c27_b12.md: ...。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h) **事后复盘**：任何涉及分布式代码的事故，事后复盘识别六个谎言中哪些在起作用。修复...
  c27_b14.md: ...**合并后审计**：高级工程师重审已合并 PR 样本；遗漏被记录；团队从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**...
  c27_b14.md: ...PR 样本；遗漏被记录；团队从模式中学习。  (f) **发布前 game day**：新特性发布前，混沌 game day 验证该特性声称的防御。Bug 在客户看到前捕获。  (g) **事后学习**：bug 进入生产后，事后复盘识别遗漏模式...
  c28_b05.md: ...1（S3 亚稳态）、E.2（Roblox）、E.6（BigQuery） | | 23 | 混沌工程 | 每月 game day、Jepsen 风格验证 | E.4（CrowdStrike 无 canary）、E.7（Fastly 无 canary...
  c28_b06.md: ...6:00 UTC 集合（覆盖所有 region 的工作时间）。  (a) **发布前检查清单**：两周前混沌 game day；所有 action items 已关闭。容量测试通过。客户迁移计划已审。Rollback 计划已审。客服已通报。  (...
  c28_b10.md: ...设计 + 当天执行 + 无责复盘 | 跳过影响半径控制；无 rollback 计划；发现 bug 却继续后续 game day 而不修复 | | 12 | vLLM 分离集群 | Ch.25 §25.5 | 每月 100 万美元以上 LLM 推理...
  c28_b16.md: ...2章）给系统加仪表。没有可见性，安全变更无法做到。  (d) **把混沌工程作为检查点**：在当前系统上运行 game day（第23章）。记录什么会坏；什么令人意外。用发现来排序。  (e) **第一季度修复前 3 个缺口**：典型首批目标——...
  c28_b16.md: ...每个修复是一个独立 PR；每个可测试。  (f) **持续增量改进**：每季度修复下一批缺口。每批后运行混沌 game day 验证。  (g) **教育团队**：修复进入时，告诉团队原因。每个 PR 都是传播纪律的机会。一年后，团队集体知识提升...
  c29_b02.md: ...metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出 game-day 场景；人类接受或扩展。  第27章 §27.10 的三层架构（AI 审查 → 人类审查 → 混沌验证）可能持续；层间边...
  c30_b07.md: ...重试放大。防御：每个客户端设置重试预算。     * Ch.23（混沌工程）："操作者移除过多容量" 应作为 game-day 场景；恢复路径（限流 + 重启）应经过演练。...
  c30_b08.md: ...* Ch.23（混沌工程）：该具体故障模式（高 heartbeat 负载下的 Consul）部署前未做 game-day 测试。   #### E.3 — Cloudflare 邮件路由故障，2024-04  * **触发**：一次配置错误...
  c30_b08.md: ...启动到安全模式，删除坏 sensor 文件）。 * **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     *...
  c30_b08.md: ...工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触发）：把部署视为触发；分阶段 rollout（1% → 10% → 10...
  c30_b09.md: ...原子 rollout 是反模式。     * Ch.23（混沌工程）："配置触发所有节点解析器崩溃" 应作为 game-day 场景。     * Ch.21（级联故障）：同时全球崩溃是无阶段 rollout 的最坏情况。...
  c30_b10.md: ...杆最高）。 * 对每个案例研究，问：六个谎言中哪些在起作用？缺失什么防御？Olamide 在此之前会运行什么 game day？  * * *   ### 附录 F — 生产决策树  针对五种常见工作负载模式，本附录提供端到端 “从需求到架构”...

=== etcd (96 occurrences) ===
  c04_b06.md: ...**NATS JetStream** 提供基于 stream 的有序交付，至少一次。根据配置接近因果或更好。 * **etcd** 通过 Raft 提供全序复制。状态机命令的原子广播。 * **Redis Streams** 在 stream 内...
  c07_b05.md: ...于 DAG 的协议）。   ### 7.8 真实系统所在  生产共识部署对每种 FLP 逃逸都做出明确选择：  * **etcd** 用 Raft 加部分同步、基于心跳的故障检测。默认超时：心跳 100 ms，选举 1000-2000 ms。 *...
  c07_b08.md: ...#### 应用问题  **Q1.** 一个团队跨三个数据中心运行 Raft 复制的 etcd 集群，注意到 200-500 ms 跨 DC 网络抖动期间，集群偶尔丢 leader 并重选。配置：心跳间隔 100...
  c09_b04.md: ...数。过渡完成后，集群才切换到 $C_{new}$。  这是 Raft 较复杂的部分之一，常是实现 bug 的来源。生产 etcd 使用更简单的替代方案：一次只改一个服务器（single-server change），代价是同一逻辑变更需要更多步骤。...
  c09_b05.md: ...程师可以一次学一个。 * **实现友好** ：显式日志结构、显式角色转换、显式状态机 API。 * **生态成熟** ：etcd 的 Raft 实现（Go）被广泛使用；多种语言实现存在。 * **运维工具** ：etcdctl、consul mem...
  c09_b05.md: ...足够状态供观察。 * **社区知识** ：一代工程师把 Raft 当作第一个共识协议学习；知识库广阔。  生产用户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、Sp...
  c09_b08.md: ...t` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 etcd / TiKV / 基于 Raft 的系统里，这种追赶通常透明，数千条差距在数秒内完成。  **Q2.** 一个 Raf...
  c09_b10.md: ...法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego Ongaro 博士论文数百页；etcd Raft 实现数千行。  综合：Raft 在 _基本情况_ 和 _常见情况_ 更简单。完整功能集与完整 Paxos 相...
  c11_b02.md: ...：故障恢复语义  系统如何处理节点故障？具体：  * **Leader 重选时间** ：Raft 系统通常 1-5 秒；etcd 快（良好条件下亚秒）；Spanner 的 Paxos 带租约类似。PostgreSQL 逻辑故障切换需外部工具（Pat...
  c18_b03.md: ...扣款，灾难。  正确的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种...
  c18_b05.md: ...（RDB 或 AOF 可能没持久化最近变更）；另一个客户端获得锁；两个持有者。  修复：对需要正确性的锁不要用缓存。用 etcd、Consul 或 ZooKeeper 做锁；在受保护资源上执行 fencing token。性能代价小（锁获取罕见）；...
  c18_b07.md: ...序，但不提供 “当前持有者” 概念。没有锁服务，每个操作都要求资源与所有潜在持有者协商。  组合是正确生产模式。实践中：etcd 或 Consul 提供锁服务（基于 Raft）；应用在存储层实现 fencing-token 检查（或依赖支持它的存储...
  c18_b08.md: ...每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有不同成本/安全...
  c18_b08.md: ...无害）可接受。若任务有非幂等副作用（发邮件、扣款、发货）则不可接受。  (b) **带 fencing token 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fencing t...
  c18_b08.md: ...货）则不可接受。  (b) **带 fencing token 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fencing token 防止过期持有者问题。每次获取较慢（etcd...
  c18_b08.md: ...**带 fencing token 的 etcd 锁** ：对效率和正确性用例都正确。增加 etcd 依赖（若团队没跑 etcd）。锁具有线性化语义；fencing token 防止过期持有者问题。每次获取较慢（etcd 写是多数复制）。  (c)...
  c18_b08.md: ...。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的 etcd 锁。实现：  (a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd 锁获取。每次获取分配一个...
  c18_b08.md: ...ncing token 的 etcd 锁。实现：  (a) 集群的领导者角色（对给定 range 或集群范围协调）通过 etcd 锁获取。每次获取分配一个 fencing token（来自 etcd revision 号的单调递增计数器）。  (b...
  c18_b10.md: ...ing token（作为选项）。正确性用例应用使用它。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etcd、Consul、ZooKeeper 都支持 fencing token 等价物（revision 号、sequence...
  c18_b11.md: ...是：“告诉我：这把锁保护什么？两个客户端同时持有它的失败模式是什么？进程可能暂停多久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入...
  c18_b11.md: ...例的反模式。用共识支持的锁；在资源处用 fencing token。代价小；正确性收益大。  第19章讲服务发现与配置：etcd 作为现代默认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data +...
  c19_b01.md: ...、故障）。每天都反复出现的问题：服务 A 如何知道服务 B 的实例在哪里？  这就是服务发现问题。2026 年的答案是 etcd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with wa...
  c19_b01.md: ...（无 heartbeat）时从集合移除。  发现服务本身是分布式系统；它有自己的共识、复制和失败模式。2026 年选择：etcd、Consul 或基于 DNS 的发现（云厂商服务发现）。   ### 19.2 etcd  etcd 是 Kubern...
  c19_b01.md: ...和失败模式。2026 年选择：etcd、Consul 或基于 DNS 的发现（云厂商服务发现）。   ### 19.2 etcd  etcd 是 Kubernetes 控制平面使用的开源分布式键值存储。它是服务发现级别系统的现代参考设计。  关键性...
  c19_b01.md: ...2026 年选择：etcd、Consul 或基于 DNS 的发现（云厂商服务发现）。   ### 19.2 etcd  etcd 是 Kubernetes 控制平面使用的开源分布式键值存储。它是服务发现级别系统的现代参考设计。  关键性质：  *...
  c19_b02.md: ...#### etcd 为何替代 ZooKeeper  Apache ZooKeeper 是首个生产分布式协调系统（2000 年代末起源于 Y...
  c19_b02.md: ...提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：  (a) **更好的运维模型**：etcd 使用 Raft（更易理解...
  c19_b02.md: ...与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：  (a) **更好的运维模型**：etcd 使用 Raft（更易理解；工具更好），而 ZooKeeper 的 ZAB（Paxos 变体）更复杂。  (b) **g...
  c19_b02.md: ...易理解；工具更好），而 ZooKeeper 的 ZAB（Paxos 变体）更复杂。  (b) **gRPC API**：etcd 的 API 基于 gRPC；现代；与云原生生态契合。ZooKeeper 的 API 较老、不够精致。  (c) **K...
  c19_b02.md: ...oKeeper 的 API 较老、不够精致。  (c) **Kubernetes 采用**：Kubernetes 选择 etcd；Kubernetes 的引力把 etcd 推向主导地位。  (d) **更好的集群管理**：etcd 的 `etcdc...
  c19_b02.md: ...。  (c) **Kubernetes 采用**：Kubernetes 选择 etcd；Kubernetes 的引力把 etcd 推向主导地位。  (d) **更好的集群管理**：etcd 的 `etcdctl` 和基于 operator 的管理比...
  c19_b02.md: ...netes 选择 etcd；Kubernetes 的引力把 etcd 推向主导地位。  (d) **更好的集群管理**：etcd 的 `etcdctl` 和基于 operator 的管理比 ZooKeeper 的 CLI 和运维故事更友好。  Zo...
  c19_b02.md: ...ZooKeeper 仍在生产中使用（Kafka 旧元数据层、HDFS、较老的 Cassandra 集群）。新系统通常从 etcd 开始。Kafka 从 ZooKeeper 迁移到 KRaft（Kafka Raft）是近期大趋势的例子。   ###...
  c19_b02.md: ...势的例子。   ### 19.3 Consul  HashiCorp 的 Consul 是另一个流行的服务发现系统。与 etcd 的关键差异：  (a) **原生多 DC**：Consul 内置数据中心感知；跨 DC 服务发现是一等特性。  (b)...
  c19_b02.md: ...些场景下仅 Kubernetes 发现不够。对纯 Kubernetes 部署，通过 Kubernetes API 使用 etcd 通常更简单。...
  c19_b03.md: ...现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch...
  c19_b04.md: ...年两大主导服务网格：Istio（基于 envoy；Kubernetes 原生）和 Linkerd（更轻量）。两者都用 etcd/Kubernetes 做发现，用 envoy 做数据面。  服务网格采用已趋于平稳；许多团队发现运维开销大于收益。服务...
  c19_b04.md: ...escent.svc.cluster.local`；kube-proxy 负载均衡到健康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revision 号 fencing to...
  c19_b04.md: ...roxy 负载均衡到健康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网...
  c19_b04.md: ...球客户解析到最近的区域负载均衡。  这四个系统分层。每个处理自己的领域；跨领域交互通过明确定义的 API。这比 “全用 etcd” 更复杂，但匹配部署拓扑（Kubernetes + AWS + 跨区域），并让每个系统做其最擅长的。   ### 19...
  c19_b05.md: ...) 其他服务查询注册表找目标服务实例。(d) 实例未 heartbeat（崩溃、分区）时从注册表移除。  现代发现服务：etcd、Consul、ZooKeeper、云厂商基于 DNS 的服务、Kubernetes API server。  发现服务...
  c19_b05.md: ...所有客户端看到同一注册表）、高可用（不让服务间调用失败）和低延迟（不让调用被注册表查找延迟）。  **Q2.** 比较 etcd 与 ZooKeeper。为什么 etcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、g...
  c19_b05.md: ...务间调用失败）和低延迟（不让调用被注册表查找延迟）。  **Q2.** 比较 etcd 与 ZooKeeper。为什么 etcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、gRPC、Kubernetes 采用。Zoo...
  c19_b05.md: ....** 比较 etcd 与 ZooKeeper。为什么 etcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、gRPC、Kubernetes 采用。ZooKeeper：老 API、ZAB 共识、较小的现代生态。  **...
  c19_b05.md: ...gRPC、Kubernetes 采用。ZooKeeper：老 API、ZAB 共识、较小的现代生态。  **答案：** etcd 和 ZooKeeper 都是分布式协调服务，具有相似原语（键值存储、watch、lease/ephemeral nod...
  c19_b05.md: ...务，具有相似原语（键值存储、watch、lease/ephemeral node）。  差异：  | Aspect | etcd | ZooKeeper | |---|---|---| | Consensus | Raft | ZAB（Paxos...
  c19_b05.md: ...云原生 | zkCli.sh；较老模式 | | Performance | 针对现代硬件优化 | 成熟但较老优化 |  etcd 替代 ZooKeeper 的原因：  (a) **Kubernetes 采用**：Kubernetes 选择 etcd...
  c19_b05.md: ...etcd 替代 ZooKeeper 的原因：  (a) **Kubernetes 采用**：Kubernetes 选择 etcd；Kubernetes 的引力把 etcd 带到云原生基础设施的主导地位。  (b) **更好的运维模型**：Raft...
  c19_b05.md: ...：  (a) **Kubernetes 采用**：Kubernetes 选择 etcd；Kubernetes 的引力把 etcd 带到云原生基础设施的主导地位。  (b) **更好的运维模型**：Raft 比 ZAB 更易理解；etcd 的运维工具...
  c19_b05.md: ...的引力把 etcd 带到云原生基础设施的主导地位。  (b) **更好的运维模型**：Raft 比 ZAB 更易理解；etcd 的运维工具更精致。  (c) **gRPC 和云原生契合**：etcd 与现代云原生工具（Prometheus 指标、...
  c19_b05.md: ...的运维模型**：Raft 比 ZAB 更易理解；etcd 的运维工具更精致。  (c) **gRPC 和云原生契合**：etcd 与现代云原生工具（Prometheus 指标、Helm charts、Kubernetes operators）集成良...
  c19_b05.md: ...er 仍在生产中使用（KRaft 前的现有 Kafka 集群、HBase、一些旧 Apache 项目），但新系统通常从 etcd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as da...
  c19_b05.md: ...ka 集群、HBase、一些旧 Apache 项目），但新系统通常从 etcd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一...
  c19_b07.md: ...#### 应用问题  **Q1.** 一个团队从 ZooKeeper 迁移到 etcd。当前 ZooKeeper 部署承载服务发现、分布式锁和配置。走一遍迁移计划。  提示  迁移计划：识别用例，把 ZK...
  c19_b07.md: ...oKeeper 部署承载服务发现、分布式锁和配置。走一遍迁移计划。  提示  迁移计划：识别用例，把 ZK 原语映射到 etcd 原语，过渡期双写，同步后切换读。  **答案：** 迁移计划：  **Phase 1：清点与映射**。识别每个 Zoo...
  c19_b07.md: ...e 1：清点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recip...
  c19_b07.md: ...al nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK persistent zN...
  c19_b07.md: ...ecipe（lease + revision number）。 * 配置：ZK persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ et...
  c19_b07.md: ...cd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 *...
  c19_b07.md: ...meral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2...
  c19_b07.md: ...lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道...
  c19_b07.md: ...（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和...
  c19_b07.md: ...与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和 etcd。读仍来自 ZooKeeper。监控两者一致性（差异表示双写路径有 bug）。  **Phase 4：迁移读**。一次一...
  c19_b07.md: ...per。监控两者一致性（差异表示双写路径有 bug）。  **Phase 4：迁移读**。一次一个用例，将读路径切换到 etcd。先服务发现（风险低）；然后分布式锁（风险较高）；然后配置（风险最高，运营影响更大）。  **Phase 5：停止写 Z...
  c19_b07.md: ...（风险较高）；然后配置（风险最高，运营影响更大）。  **Phase 5：停止写 ZooKeeper**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper。  **Phase 6：下线 ZooKeeper**。经过...
  c19_b07.md: ...eeper**。经过足够时间确认无残留读之后，下线 ZK 集群。  关键风险与缓解：  * **语义差异**：ZK 和 etcd 在 watch 语义、原子操作等方面有微妙差异。映射必须小心。缓解：双写阶段充分测试。  * **性能差异**：etc...
  c19_b07.md: ...tcd 在 watch 语义、原子操作等方面有微妙差异。映射必须小心。缓解：双写阶段充分测试。  * **性能差异**：etcd 和 ZK 性能特征不同。某些工作负载可能需要重新调优。缓解：切换前做负载测试。  * **运维学习曲线**：团队需要学...
  c19_b07.md: ...ZK 性能特征不同。某些工作负载可能需要重新调优。缓解：切换前做负载测试。  * **运维学习曲线**：团队需要学习 etcd 运维。缓解：过渡期间配对运维值班、更新 runbook。  总时间线：有意义迁移 3-6 个月；较小部署更快。Kafk...
  c19_b08.md: ...nsul 刷新。  (e) **迁移到集群内发现（Kubernetes service）**：Kubernetes 用 etcd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Kubernetes API 中断影响新 se...
  c19_b08.md: ...(d) 或 (e)。30 秒故障转移导致的中断是运维痛点；实质降低它会明显改善用户体验。  **Q3.** 一个团队用 etcd 做 feature flags。flag `new_checkout_flow` 被切换为 `true`。走一遍每个服...
  c19_b09.md: ...**运维动作**：`etcdctl put feature_flags/new_checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_...
  c19_b09.md: ...的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好...
  c19_b09.md: ...ch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部...
  c19_b09.md: ...开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；...
  c19_b10.md: ...现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS 解析。  (c) **直接使用 etcd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Ma...
  c19_b10.md: ...ECS/Fargate 自动注册；客户端通过 DNS 解析。  (c) **直接使用 etcd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d)...
  c19_b10.md: ...动注册；客户端通过 DNS 解析。  (c) **直接使用 etcd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Net...
  c19_b11.md: ...目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的注册表。2026 年标准解是...
  c19_b11.md: ...或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的注册表。2026 年标准解是 etcd、Consul 和基于 DNS 的发现（AWS Cloud Map 等云厂商服务）。etcd 主导云原生细分市场；Con...
  c19_b11.md: ...册表。2026 年标准解是 etcd、Consul 和基于 DNS 的发现（AWS Cloud Map 等云厂商服务）。etcd 主导云原生细分市场；Consul 处理 VM 和多 DC 部署；DNS 对稳定服务足够。  etcd 在新系统中基本替...
  c19_b11.md: ...等云厂商服务）。etcd 主导云原生细分市场；Consul 处理 VM 和多 DC 部署；DNS 对稳定服务足够。  etcd 在新系统中基本替代 ZooKeeper，因为 Kubernetes 采用、Raft 与 ZAB 共识选择、gRPC A...
  c19_b11.md: ...求的团队则是过度工程。  Crescent 的发现使用四个分层系统：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每个处理自己的领域；跨领...
  c23_b03.md: ...行化违规；后续版本修复。 * **Redis**：Redlock 问题（第18章主题）；各种故障转移 bug。 * **etcd**：旧版本有轻微一致性 issue；已纳入修复。  Jepsen 报告是任何分布式系统工程师的必读。它们是声明真实性的...
  c23_b07.md: ...dra 轻量事务、MongoDB read concern、CockroachDB 早期可串行化、Redis 故障转移、etcd 旧版本——都有 Jepsen 发现的 bug。  为何重要：  (a) **经验证据**：Jepsen 报告是声明真实...
  c28_b02.md: ...esh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare...
  c28_b03.md: ...STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end...
  c28_b05.md: ...17 | 最终、因果、CRDT | Redis 缓存（最终）、Yjs（CRDT） | n/a | | 18 | 锁 | etcd + fencing token | E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Cons...
  c28_b13.md: ...进。1998 年 Paxos 是标准共识；2026 年 Raft 是标准。ZooKeeper 曾是标准协调服务；现在是 etcd。2026 年分布式系统工程包括 Spanner、CockroachDB、vLLM、Splitwise、Mooncake...
  c28_b14.md: ...例：  _诊断方法（第11章）_：  * **复制协议**：Postgres 逻辑复制；默认异步；Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障...
  c28_b14.md: ...制；默认异步；Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-brain 可能需要人工干...
  c28_b14.md: ...能需要人工干预。 * **一致性模型**：事务内快照隔离；副本读最终一致。 * **分区行为**：CP——少数派无法在 etcd 获得 leadership，拒绝写。  PACELC：PC/EL（分区时一致；正常操作时低延迟，因异步复制）。  _三...
  c28_b14.md: ...序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；et...
  c28_b14.md: ...序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revision 在编排层起类似...
  c28_b14.md: ...cd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revision 在编排层起类似作用。 * **故障**：异步复制可能丢失主库传播前最近写入。团队必须接受某 RPO；...
  c28_b18.md: ...每个组件做 CAP/PACELC 分析；跨 region 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing token；追踪用 OpenTelemetry；韧性用重试预算和熔断器；验证用混沌工程；AI 辅助...
  c30_b14.md: ...n     │   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo.py — protecte...

=== term (96 occurrences) ===
  c07_b08.md: ...-vote” 扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre...
  c09_b01.md: ...解性作为主要贡献。  本章按 Anya 学习 Raft 的方式教授：三个子问题（leader 选举、日志复制、安全性）、term 的作用、AppendEntries 协议，以及为什么 2026 年每个新分布式系统项目首先想到 Raft。   ###...
  c09_b01.md: ...；Raft 的拆分是核心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。...
  c09_b01.md: ...### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxo...
  c09_b01.md: ...每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保...
  c09_b01.md: ...至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `curr...
  c09_b01.md: ...ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为...
  c09_b01.md: ...每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 ca...
  c09_b01.md: ...vert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定...
  c09_b01.md: ...r 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停...
  c09_b01.md: ...* 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新...
  c09_b01.md: ...”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现自己已过期。...
  c09_b01.md: ...解性作为主要贡献。  本章按 Anya 学习 Raft 的方式教授：三个子问题（leader 选举、日志复制、安全性）、term 的作用、AppendEntries 协议，以及为什么 2026 年每个新分布式系统项目首先想到 Raft。   ###...
  c09_b01.md: ...；Raft 的拆分是核心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。...
  c09_b01.md: ...### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxo...
  c09_b01.md: ...每个 term 至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保...
  c09_b01.md: ...至多有一个 leader。Term 用单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `curr...
  c09_b01.md: ...ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为...
  c09_b01.md: ...每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 ca...
  c09_b01.md: ...vert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定...
  c09_b01.md: ...r 或 candidate）。 * 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停...
  c09_b01.md: ...* 服务器在 RPC 中看到较低 term 时，拒绝该 RPC，并在响应中附更高的当前 term。  “永远服从更高 term”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新...
  c09_b01.md: ...”规则阻止过期 leader 做决定。一个已被罢免但尚不知情的旧 leader（可能曾暂停或分区）一旦尝试与任何见过新 term 的节点通信，就发现自己已过期。...
  c09_b02.md: ...equestVote` RPC。  每个服务器收到 `RequestVote` 后：  * 若 candidate 的 term 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。 * 若 ca...
  c09_b02.md: ...`RequestVote` 后：  * 若 candidate 的 term 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），...
  c09_b02.md: ...若 candidate 的 term 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 can...
  c09_b02.md: ...则  Candidate 的日志被认为是“最新”的，当满足以下之一：  (a) candidate 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至...
  c09_b02.md: ...“最新”的，当满足以下之一：  (a) candidate 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保...
  c09_b02.md: ...) candidate 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条...
  c09_b03.md: ...节点用于不影响法定人数的追赶。训练技能：实现（或运维）一个系统，通过任意 leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader 接受客户端请求。对每个请求：    1....
  c09_b03.md: ...2. 并行发送 `AppendEntries` RPC 给所有 follower，包含新条目及上下文（前一个日志索引和 term）。   3. 一旦多数 follower 复制该条目，将其标记为 _committed_ 并应用到状态机。   4....
  c09_b03.md: ...到状态机。   4. 响应客户端。  Follower 处理 `AppendEntries`：  * 若 leader term 低于 follower 的 `currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIn...
  c09_b03.md: ...currentTerm`，拒绝。 * 若 follower 日志在 `prevLogIndex` 处没有条目或该条目的 term 不是 `prevLogTerm`，拒绝。这是 _consistency check_ ；它确保 follower 日志...
  c09_b03.md: ...成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条目。  论证依赖...
  c09_b03.md: ...ety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多...
  c09_b03.md: ...未来 term 的其他 leader 都不会在该索引 i 提交不同条目。  论证依赖已陈述的三条规则：  (a) 每个 term 至多一个 leader（选举多数）。 (b) 每个新 leader 拥有所有已提交条目（通过“日志最新”规则选举限制）...
  c09_b03.md: ...已提交条目（通过“日志最新”规则选举限制）。 (c) Leader 从不覆盖或删除自己的日志条目。  合起来：索引 i、term T 的已提交条目在提交时已复制到多数。任何未来 leader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节...
  c09_b04.md: ...#### “Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它...
  c09_b04.md: ...能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带提交 previous term 的条目（这有副作用：连带提...
  c09_b04.md: ...一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带提交 previous term 的条目（这有副作用：连带提交之前的所有条目）。  原因：没有这条规则...
  c09_b04.md: ...制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带提交 previous term 的条目（这有副作用：连带提交之前的所有条目）。  原因：没有这条规则，某个 corner case 会让已提交条目后来...
  c09_b04.md: ...某个 corner case 会让已提交条目后来被覆盖。Raft 论文的图 8 说明了该情况；修复就是“current term only”提交规则。这是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常见误解...
  c09_b04.md: ...常见误解  Raft 的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目且 (b) 一个自己 term 的条目也已复制到多数 来提...
  c09_b04.md: ...但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目且 (b) 一个自己 term 的条目也已复制到多数 来提交条目。两个条件一起防止图 8 问题。  实践中很少要紧，因为新 leader 通常很快提交...
  c09_b04.md: ...写入快照，并把日志截断到快照索引。  快照包含：  * 状态机完整状态（例如键值存储内容）。 * 快照包含的最后索引和 term。 * 当前集群配置。  日志落后太多、无法通过 `AppendEntries` 追赶的 follower 通过 `In...
  c09_b06.md: ...志足够新的 candidate 能赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；...
  c09_b06.md: ...的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft...
  c09_b06.md: ...* 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数...
  c09_b06.md: ...单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携...
  c09_b06.md: ...der；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 te...
  c09_b06.md: ...ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 rev...
  c09_b06.md: ...增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：...
  c09_b06.md: ...rm 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ball...
  c09_b06.md: ...结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概...
  c09_b06.md: ...ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 l...
  c09_b06.md: ...由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Ra...
  c09_b06.md: ...ti-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。...
  c09_b06.md: ...leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3...
  c09_b06.md: ...志足够新的 candidate 能赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；...
  c09_b06.md: ...的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft...
  c09_b06.md: ...* 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数...
  c09_b06.md: ...单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携...
  c09_b06.md: ...der；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 te...
  c09_b06.md: ...ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 rev...
  c09_b06.md: ...增整数，划分时间。每个 term 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：...
  c09_b06.md: ...rm 以选举开始；每 term 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ball...
  c09_b06.md: ...结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概...
  c09_b06.md: ...ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 l...
  c09_b06.md: ...由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Ra...
  c09_b06.md: ...ti-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。...
  c09_b06.md: ...leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3...
  c09_b07.md: ...ogTerm, entries, leaderCommit)` 时，follower 执行一致性检查：  (a) 若 `term < currentTerm`，拒绝（leader 过期）。  (b) 若 follower 日志在 `prevLogI...
  c09_b07.md: ...条目，拒绝（follower 太短）。  (c) 若 follower 在 `prevLogIndex` 处的日志条目 term 不是 `prevLogTerm`，拒绝（follower 日志在此处与 leader 分歧）。  否则，追加新条目（覆...
  c09_b09.md: ...no leader” 或超时；读失败或返回过期数据。  _恢复：_  * 分区愈合后，少数侧节点在多数侧心跳中发现更高 term。它们 revert 为 follower，同步日志到多数侧，重新加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提...
  c09_b10.md: ...除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 t...
  c09_b10.md: ...ff-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能...
  c09_b10.md: ...外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上...
  c09_b10.md: ...m 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上 apply 的混淆。**...
  c09_b10.md: ...实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous...
  c09_b10.md: ...ty。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在...
  c09_b10.md: ...提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safet...
  c09_b10.md: ...一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：...
  c09_b10.md: ...e。** 检查比较 follower 在 `prevLogIndex` 处的条目与 `prevLogTerm`。索引或 term 比较差一都会让不一致日志保持不一致。  (e) **NextIndex 回退缺失。** `AppendEntries`...
  c09_b10.md: ...除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 t...
  c09_b10.md: ...ff-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能...
  c09_b10.md: ...外：  (a) **Term 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上...
  c09_b10.md: ...m 重启时投票计数未清零。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上 apply 的混淆。**...
  c09_b10.md: ...实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous...
  c09_b10.md: ...ty。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在...
  c09_b10.md: ...提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safet...
  c09_b10.md: ...一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：...
  c09_b10.md: ...e。** 检查比较 follower 在 `prevLogIndex` 处的条目与 `prevLogTerm`。索引或 term 比较差一都会让不一致日志保持不一致。  (e) **NextIndex 回退缺失。** `AppendEntries`...
  c09_b11.md: ...错过微妙不变式。Raft 论文的图 8 case 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous term 条目”翻译成正确代码，需要深入理解不变式，而这正是模型做不到的地方。  20...
  c09_b11.md: ...ase 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous term 条目”翻译成正确代码，需要深入理解不变式，而这正是模型做不到的地方。  2026 年最安全的模式：使用经过验证的 Ra...
  c13_b14.md: ...leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换具有鲁棒性：它们通过 Raft term/leadership 识别新协调者权威并接受其指令。  事务的用户可见行为：故障切换期间短暂停顿（亚秒到数秒），然后提...
  c16_b05.md: ...(d) **Collaboration is seamless** ：多人可编辑；冲突自动解决。 (e) **Long-term archive** ：数据比提供服务的公司活得更久。 (f) **Privacy by default** ：数据不需...

=== ballot (85 occurrences) ===
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Propo...
  c08_b02.md: ...每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复...
  c08_b02.md: ...tor 最近 _接受_ 的值（可能为 null）。  该 promise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到...
  c08_b02.md: ...自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `...
  c08_b02.md: ...在协议消息上）。   ### 8.4 为什么 Paxos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序...
  c08_b02.md: ...xos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相...
  c08_b02.md: ...fety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acc...
  c08_b02.md: ...部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acceptor 的任意两个多数集合至少相交于...
  c08_b02.md: ...受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值...
  c08_b02.md: ...r 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ball...
  c08_b02.md: ...llot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受...
  c08_b02.md: ...在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携...
  c08_b02.md: ...；proposer 必须提议 v。  这就是 agreement 性质：任何已选定的值将永远是选定的值，无论后续跑多少 ballot。...
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Propo...
  c08_b02.md: ...每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复...
  c08_b02.md: ...tor 最近 _接受_ 的值（可能为 null）。  该 promise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到...
  c08_b02.md: ...自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `...
  c08_b02.md: ...在协议消息上）。   ### 8.4 为什么 Paxos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序...
  c08_b02.md: ...xos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相...
  c08_b02.md: ...fety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acc...
  c08_b02.md: ...部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acceptor 的任意两个多数集合至少相交于...
  c08_b02.md: ...受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值...
  c08_b02.md: ...r 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ball...
  c08_b02.md: ...llot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受...
  c08_b02.md: ...在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携...
  c08_b02.md: ...；proposer 必须提议 v。  这就是 agreement 性质：任何已选定的值将永远是选定的值，无论后续跑多少 ballot。...
  c08_b03.md: ...为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREPARE(b)`）。   3....
  c08_b03.md: ...eader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到某个 ballot “获胜”（先拿到第一阶...
  c08_b03.md: ...axos 实例（或退化等价）。当进程怀疑当前 leader 死亡（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到某个 ballot “获胜”（先拿到第一阶段多数）。  随机化超时模式（R...
  c08_b03.md: ...（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到某个 ballot “获胜”（先拿到第一阶段多数）。  随机化超时模式（Raft 使用）通过随机化心跳超时，使同时启动选举在统计上不可能。...
  c08_b05.md: ...响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acc...
  c08_b05.md: ...}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello”...
  c08_b05.md: ...**步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。...
  c08_b05.md: ...o” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。...
  c08_b05.md: ...PT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若...
  c08_b06.md: ...接受的值。第二阶段：提议并让多数接受。  **答案：**  _第一阶段（Prepare）：_ Proposer 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acce...
  c08_b06.md: ...er 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`...
  c08_b06.md: ...cepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b...
  c08_b06.md: ...不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已接受的最高值（如有）；...
  c08_b06.md: ...高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：_ Proposer 选一个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ball...
  c08_b06.md: ...llot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数...
  c08_b06.md: ...tor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promise 中见到这个已接受值，并被强制提议它。  两阶段合起来保证：一旦值被选定，所有...
  c08_b06.md: ...一阶段都会在该多数的 promise 中见到这个已接受值，并被强制提议它。  两阶段合起来保证：一旦值被选定，所有后续 ballot 都会认同它。这就是 agreement 性质。  **Q2.** 为什么第二阶段 proposer 必须使用第一阶段响...
  c08_b06.md: ...它。这就是 agreement 性质。  **Q2.** 为什么第二阶段 proposer 必须使用第一阶段响应中最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值，该值可能已被选定；新 proposer...
  c08_b06.md: ...二阶段 proposer 必须使用第一阶段响应中最高 ballot 的已接受值，而不是自己的原始值？  提示  若先前 ballot 已接受某值，该值可能已被选定；新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受...
  c08_b06.md: ...若先前 ballot 已接受某值，该值可能已被选定；新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。...
  c08_b06.md: ...被不同观察者视为都已选定，违反 agreement。  Accept 规则（proposer 必须采用第一阶段所见最高 ballot 的已接受值）防止这种情况。任何已被某 acceptor 接受的值（即使尚未多数）都“在飞行中”，可能已选定。新 bal...
  c08_b06.md: ...lot 的已接受值）防止这种情况。任何已被某 acceptor 接受的值（即使尚未多数）都“在飞行中”，可能已选定。新 ballot 必须采纳它以避免 safety 违反。  新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任...
  c08_b06.md: ...r 接受的值（即使尚未多数）都“在飞行中”，可能已选定。新 ballot 必须采纳它以避免 safety 违反。  新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任何未来 ballot 都会看到 $v_{prev}$ 并提议...
  c08_b06.md: ...纳它以避免 safety 违反。  新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任何未来 ballot 都会看到 $v_{prev}$ 并提议它。系统收敛到单一选定值。  **Q3.** 什么是 Multi-Paxos？它...
  c08_b07.md: ...epare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（PREPARE...
  c08_b08.md: ...r 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 propo...
  c08_b08.md: ...查两个 proposer（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ball...
  c08_b08.md: ...llot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Propose...
  c08_b08.md: ...eptors 4、5 的 2 个 promise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot 20。  **B 的 accept：**  * B 发送 `ACCEPT(20, "Olamide")` 给所有 5...
  c08_b08.md: ...。  **A 的重试：**  * A 的 `prepare(10)` 失败（只有 2 个 promise）。A 选更高 ballot，例如 30。 * A 发送 `PREPARE(b=30)`。 * 所有 5 个 acceptor 的 $b_{max}...
  c08_b08.md: ...ed_ballot 的值）。 * A 发送 `ACCEPT(30, "Olamide")`。全部 5 个接受（它们已有 ballot 20 的 “Olamide”；更新到 ballot 30 的相同值）。 * A “选定”了 “Olamide”——但该...
  c08_b08.md: ...EPT(30, "Olamide")`。全部 5 个接受（它们已有 ballot 20 的 “Olamide”；更新到 ballot 30 的相同值）。 * A “选定”了 “Olamide”——但该值已在 ballot 20 时被选定。  系统收敛到...
  c08_b08.md: ...的 “Olamide”；更新到 ballot 30 的相同值）。 * A “选定”了 “Olamide”——但该值已在 ballot 20 时被选定。  系统收敛到单一值 “Olamide”。A 原本想提议 “Anya” 的意愿被 safety 规则覆...
  c08_b08.md: ...个不可达 follower 不再收到 leader 心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ballot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼...
  c08_b08.md: ...发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ballot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 lead...
  c08_b08.md: ...accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot 正在使用，下台成为 follower。...
  c08_b09.md: ...ser 都可提议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高...
  c08_b09.md: ...number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高 ballot 接管。新 proposer 的第一阶段会学到是否有（什么）值已被接受，并完成前一个提议或重新开始。没有参与者无限期阻塞...
  c08_b10.md: ...acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LL...
  c08_b10.md: ...前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带...
  c08_b10.md: ...r 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点...
  c08_b10.md: ...带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N+1)/2 \...
  c08_b10.md: ...(d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol...
  c08_b10.md: ...acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LL...
  c08_b10.md: ...前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带...
  c08_b10.md: ...r 单调递增。LLM 代码常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点...
  c08_b10.md: ...带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N+1)/2 \...
  c08_b10.md: ...(d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol...
  c08_b11.md: ...的天真方式实现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/ballot/acceptor。LLM 代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 acceptor 状态。...
  c08_b11.md: ...规则保留：任何 proposer 的 accept 必须提议第一阶段响应中最高已接受的值；这保证一旦值被选定，所有后续 ballot 都会提议它。  Multi-Paxos 通过选举稳定 leader 并把第一阶段摊销到多个决定，把单法令扩展为值序列。...
  c08_b11.md: ...并把第一阶段摊销到多个决定，把单法令扩展为值序列。稳态下每决定成本是一次往返。  Paxos 以难实现著称：持久化纪律、ballot 唯一性、法定人数 off-by-one、重配置、快照。Spanner 和 Megastore 是经典生产实现。附录 A...
  c09_b01.md: ...单调递增整数编号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。...
  c09_b06.md: ...赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos 的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同...
  c09_b06.md: ...的 ballot 有何不同？  提示  Term 单调递增；每 term 至多一个 leader；作用与 Paxos ballot 相同。  **答案：** Raft term 是单调递增整数，划分时间。每个 term 以选举开始；每 term 至多...
  c09_b06.md: ...携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递增且全序。结构作用相似（防止过期提议被接受）。 * Pa...
  c09_b06.md: ...term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递增且全序。结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 prop...
  c09_b06.md: ...* Paxos ballot number 也单调递增且全序。结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列...
  c09_b06.md: ...* Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxo...
  c09_b10.md: ...等价 bug 之外，任何 LLM 生成的 Raft 实现中可能存在的三类结构性 bug。  **讨论：** 除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数...

=== prompt (85 occurrences) ===
  c24_b02.md: ...于 KV cache（相比朴素系统 30-50%）。每张 GPU 能容纳更多请求。  (b) **跨请求共享**：共享 prompt 的请求（system prompt + user message）可共享前缀的 KV cache 块。对聊天机器人风格...
  c24_b02.md: ...30-50%）。每张 GPU 能容纳更多请求。  (b) **跨请求共享**：共享 prompt 的请求（system prompt + user message）可共享前缀的 KV cache 块。对聊天机器人风格工作负载，内存节省巨大。  (c)...
  c24_b03.md: ...最长缓存前缀并复用；只有后缀需要重新计算。  收益：  (a) **共享前缀的巨大节省**：对具有长共享 system prompt 的工作负载（生产 LLM 应用中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefi...
  c24_b03.md: ...复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成**：SGLang 原生支持结构化输出（JSON sch...
  c24_b03.md: ...存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输出应用、agent 系统。它是 prompt 结构重复的工作负载的最佳选择。  **何时选 SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共...
  c24_b03.md: ...选择。  **何时选 SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAtt...
  c24_b03.md: ...ught scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prompt token 中 30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 20...
  c24_b03.md: ...来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 2000-token system prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。对 prompt 多样化（不...
  c24_b03.md: ...ystem prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。对 prompt 多样化（不同用户、不同任务、无共享前缀）的通用 API 流量，vLLM 的 PagedAttention 是更好选择—...
  c24_b06.md: ...GPU 更多并发请求**：更好内存利用率意味着更多请求 fit；每张 GPU 吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同 system prompt 的聊天工作负载，内存节省巨大。  灵感...
  c24_b06.md: ...吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同 system prompt 的聊天工作负载，内存节省巨大。  灵感来自操作系统虚拟内存：页让进程无需连续物理地址即可使用内存。PagedAtten...
  c24_b07.md: ...（基于前缀的 KV cache 复用）。 * 优势：对共享前缀工作负载节省巨大。 * 最适合：具有长共享 system prompt、结构化输出、函数调用的工作负载。  各适合何时：  * **vLLM**：任意用户聊天机器人；混合工作负载 API；希...
  c24_b07.md: ...意用户聊天机器人；混合工作负载 API；希望最广泛生产支持的团队。 * **SGLang**：客服机器人（system prompt 跨百万请求重复）、agent 框架（agent scaffold 共享）、结构化输出应用（函数调用、JSON 生成）。...
  c24_b08.md: ...一步。预期利用率提升：从 60% 到 80%+。  (b) **前缀共享（SGLang）**：对具有重复 system prompt 的工作负载（客服机器人、agent scaffold），SGLang 的 RadixAttention 可节省 30-...
  c24_b08.md: ...l 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous b...
  c24_b09.md: ...分离 prefill 和 decode（第25章）**：长请求问题常在于 prefill（处理 10000-token prompt 需要数秒）。若 prefill 与 decode 在不同节点，短请求的 decode 不会被长请求的 prefill...
  c24_b09.md: ...e 在不同节点，短请求的 decode 不会被长请求的 prefill 阻塞。  (d) **限制最大请求大小**：把 prompt 限制为 N token（足够小以不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同...
  c24_b09.md: ...塞。  (d) **限制最大请求大小**：把 prompt 限制为 N token（足够小以不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (e) **Speculative decoding...
  c24_b11.md: ...线开源推理引擎。  SGLang 的 RadixAttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA NIM 是企业部署平台：容器化微服...
  c25_b01.md: ...1 Prefill/Decode 不对称性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-...
  c25_b01.md: ...性  对单个 LLM 推理请求：  **Prefill 阶段**：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLO...
  c25_b01.md: ...oken 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **Decode 阶段**：逐 token 生成输出。对每个 token，GPU 读取整个上下文（pro...
  c25_b02.md: ...Prefill 节点：  * 针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分...
  c25_b02.md: ...针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cache 传输到分配的 decode 节点。  D...
  c25_b03.md: ...) **独立扩缩**。Prefill 容量（计算）和 decode 容量（内存+内存带宽）可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下...
  c25_b04.md: .... 负载均衡器基于可用性把请求分配到 prefill 节点和 decode 节点。   3. Prefill 节点处理 prompt；产生 KV cache。   4. KV cache 从 prefill 传输到 decode（若可能复用则存入缓存层...
  c25_b04.md: ...从缓存层读取 KV cache。   6. 输出流回客户端。  **运维特征**：  * Prefill 延迟：典型 prompt（1000-5000 token）约 50-200 ms。 * Decode 延迟：每 token 约 30 ms（受...
  c25_b05.md: ...法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内...
  c25_b05.md: ...und，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode...
  c25_b05.md: ...equest KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt（system prompt、agent scaffold）共享缓存块。节省在多请求间摊销。  (c) **Decode...
  c25_b05.md: ...e 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt（system prompt、agent scaffold）共享缓存块。节省在多请求间摊销。  (c) **Decode 节点可无状态**：按需读取...
  c25_b06.md: ...* 团队规模足以 justify 专门平台工程师做分离逻辑。  阈值与工作负载相关：50 节点但 uniform 短 prompt 工作负载的集群可能不受益（prefill 成本相对 decode 小）。30 节点但重长 prompt 工作负载的集群...
  c25_b06.md: ...iform 短 prompt 工作负载的集群可能不受益（prefill 成本相对 decode 小）。30 节点但重长 prompt 工作负载的集群可能受益（prefill 成本是瓶颈）。  Tobias 的 Continuum 集群（50 节点，混合...
  c25_b06.md: ...token。各自贡献首 token 和 per-token 延迟。  **答案：** 典型请求（1000-token prompt，200-token 输出）的延迟组件：  (a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b)...
  c25_b06.md: ...a) **网络入口**：约 5 ms（负载均衡器到推理节点）。  (b) **Prefill**：1000-token prompt 在计算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 4...
  c25_b07.md: ...同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请求），跳过该部分 prefill。  Tobias 的部署使用 (a) 和 (c)；典型首 t...
  c25_b08.md: ...量调整 prefill/decode 比例。 * 实现 KV cache 预取以减少首 token 延迟。 * 为常见 prompt 模式添加前缀复用逻辑。 * 监控任何回归；按需调整。  **每阶段验证门**：  * p99 延迟在 co-locat...
  c25_b08.md: ...400-500 万美元年节省。ROI 约 10 倍。  **Q2.** 某团队推理集群工作负载高度可变：某些时段以长 prompt（3000+ token）短输出为主；另一些时段以短 prompt 长输出为主。分离如何帮助，运维关注点是什么？  提示...
  c25_b08.md: ....** 某团队推理集群工作负载高度可变：某些时段以长 prompt（3000+ token）短输出为主；另一些时段以短 prompt 长输出为主。分离如何帮助，运维关注点是什么？  提示  分离允许独立扩缩 prefill 和 decode；可变性成为...
  c25_b08.md: ...：** 有了分离，prefill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile：  * 长 prompt 时段：多扩 prefill（compute-bound）并少扩 decode（memory-bound，短输出不需要那...
  c25_b08.md: ...efill（compute-bound）并少扩 decode（memory-bound，短输出不需要那么多）。 * 短 prompt 长输出时段：多扩 decode（需要更多内存和 per-token 吞吐）并少扩 prefill。  没有分离（co-...
  c25_b08.md: ...点：rebalance。  集群不能瞬间扩缩（云 GPU 置备需 5-15 分钟，本地更久）。若工作负载在几分钟内从长 prompt 变为短 prompt：  * prefill 容量过度配置（闲置）。 * decode 容量不足（饱和）。 * 用户可...
  c25_b08.md: ...。  集群不能瞬间扩缩（云 GPU 置备需 5-15 分钟，本地更久）。若工作负载在几分钟内从长 prompt 变为短 prompt：  * prefill 容量过度配置（闲置）。 * decode 容量不足（饱和）。 * 用户可见 decode 延迟...
  c25_b10.md: ...ontinuous batching（第24章）：若尚未使用，2-5 倍吞吐提升。 * 前缀共享（SGLang）：重复 prompt 工作负载 20-50% 降低。 * 超长上下文的 KV cache sharding：否则不可行的工作负载得以运行。...
  c26_b01.md: ...ast-loaded）错过 LLM 推理特有的关键机会：  (a) **KV cache locality**：若请求 prompt 的前缀已在某节点缓存，路由到该节点可节省缓存部分的 prefill 成本。路由别处则浪费缓存。  (b) **长度预测...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有节点缓存了该 prompt 的显著前缀？若有，路由过...
  c26_b02.md: ...：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有节点缓存了该 prompt 的显著前缀？若有，路由过去。  实现：  (a) 每个节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）...
  c26_b02.md: ...节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）。  (b) 对 incoming request 的 prompt 做哈希（或其前缀与索引比对）。  (c) 负载均衡器选择最长匹配前缀的节点；把请求路由过去。  (d) 所选节点服务请...
  c26_b02.md: ...案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（...
  c26_b02.md: ...% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复...
  c26_b02.md: ...工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-awar...
  c26_b02.md: ...nuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，...
  c26_b02.md: ...由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。  预测问题：给定 prompt，会生成多少输出 token？有些信号很明显：  * 显式长度提示（API 请求中 max_tokens=100）。 *...
  c26_b02.md: ...会生成多少输出 token？有些信号很明显：  * 显式长度提示（API 请求中 max_tokens=100）。 * Prompt 结构（“是/否” 问题通常输出短；“写一篇文章” 输出长）。 * 历史记录（客户平均输出长度是有用的先验）。  路由收...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有节点缓存了该 prompt 的显著前缀？若有，路由过...
  c26_b02.md: ...：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有节点缓存了该 prompt 的显著前缀？若有，路由过去。  实现：  (a) 每个节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）...
  c26_b02.md: ...节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）。  (b) 对 incoming request 的 prompt 做哈希（或其前缀与索引比对）。  (c) 负载均衡器选择最长匹配前缀的节点；把请求路由过去。  (d) 所选节点服务请...
  c26_b02.md: ...案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（...
  c26_b02.md: ...% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复...
  c26_b02.md: ...工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-awar...
  c26_b02.md: ...nuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，...
  c26_b02.md: ...由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。  预测问题：给定 prompt，会生成多少输出 token？有些信号很明显：  * 显式长度提示（API 请求中 max_tokens=100）。 *...
  c26_b02.md: ...会生成多少输出 token？有些信号很明显：  * 显式长度提示（API 请求中 max_tokens=100）。 * Prompt 结构（“是/否” 问题通常输出短；“写一篇文章” 输出长）。 * 历史记录（客户平均输出长度是有用的先验）。  路由收...
  c26_b05.md: ...策的成本相应大得多。  具体 LLM 路由机会：  (a) **KV cache locality**：若某节点已缓存 prompt 前缀，路由到该节点节省 prefill 成本。路由别处浪费缓存。每次请求可节省数百毫秒。  (b) **长度预测**：...
  c26_b08.md: ...的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。...
  c26_b08.md: ...n system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt...
  c26_b08.md: ...prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (...
  c26_b08.md: ...缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aw...
  c26_b08.md: ...fix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system prompt。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-token p...
  c26_b08.md: ...00-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**：若该客户流量高（如...
  c26_b08.md: ...容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggrega...
  c26_b08.md: ...的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。...
  c26_b08.md: ...n system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt...
  c26_b08.md: ...prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (...
  c26_b08.md: ...缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aw...
  c26_b08.md: ...fix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system prompt。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-token p...
  c26_b08.md: ...00-token prefill 只付一次（第一个请求）。后续请求跳过前缀 prefill；只 prefill 每请求 prompt（50 token）。Prefill 成本每请求降低约 100 倍。  (b) **缓存层复制**：若该客户流量高（如...
  c26_b08.md: ...容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggrega...
  c26_b10.md: ...产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** LLM 的基本方法：哈希 prompt，检查是否有节点缓存该哈希，若有则路由过去。  遗漏什么：  (a) **前缀匹配，而非精确匹配**。LLM 的哈希检查...
  c26_b10.md: ...缓存该哈希，若有则路由过去。  遗漏什么：  (a) **前缀匹配，而非精确匹配**。LLM 的哈希检查只匹配完全相同 prompt。真实工作负载的 prompt 共享 _前缀_（system prompt + 不同用户消息）。需要 radix tre...
  c26_b10.md: ...遗漏什么：  (a) **前缀匹配，而非精确匹配**。LLM 的哈希检查只匹配完全相同 prompt。真实工作负载的 prompt 共享 _前缀_（system prompt + 不同用户消息）。需要 radix tree 或类似前缀索引。  (b)...
  c26_b10.md: ...确匹配**。LLM 的哈希检查只匹配完全相同 prompt。真实工作负载的 prompt 共享 _前缀_（system prompt + 不同用户消息）。需要 radix tree 或类似前缀索引。  (b) **缓存新鲜度跟踪**。KV cache...
  c26_b10.md: ...来路由，然后该节点从缓存层读取。  (e) **客户亲和性**：对 workload 可预测的客户（相同 system prompt），把客户流量 pin 到特定节点以最大化缓存复用。  (f) **冷启动 fallback**：若没有节点有缓存，路由...
  c26_b11.md: ...更深观察：推理集群优化是 2026 专业领域。给出原型级答案的 LLM 遗漏了领域深度。正确 LLM 回应应问：“你的 prompt 结构是什么？多少租户？有缓存层吗？当前缓存命中率多少？我可以基于这些 specifics 设计路由策略。”  对 Co...
  c26_b11.md: ...re 路由：负载均衡器维护缓存 KV cache 的前缀索引；把请求路由到有缓存的节点。对前缀重工作负载（system prompt、agent scaffold、function-calling 模板）的重大收益。  长度预测路由：预测请求输出长度；...

=== verify (85 occurrences) ===
  c26_b03.md: ...g  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model...
  c26_b03.md: ...1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个候选。这是一个 “verify 步”，一次处理 K 个候选 token。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配...
  c26_b03.md: ...ken。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：v...
  c26_b03.md: ...y model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序...
  c26_b03.md: ...相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draf...
  c26_b03.md: ...个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative D...
  c26_b03.md: ...个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请...
  c26_b03.md: ...分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft...
  c26_b03.md: ...大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；...
  c26_b03.md: ...宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节点可用更便宜硬件（...
  c26_b03.md: ...*Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节点可用更便宜硬件（小 GPU，某些情况...
  c26_b03.md: ...draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0....
  c26_b03.md: ...：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步...
  c26_b03.md: ...n 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding...
  c26_b03.md: ...g  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model...
  c26_b03.md: ...1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个候选。这是一个 “verify 步”，一次处理 K 个候选 token。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配...
  c26_b03.md: ...ken。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：v...
  c26_b03.md: ...y model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序...
  c26_b03.md: ...相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draf...
  c26_b03.md: ...个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative D...
  c26_b03.md: ...个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请...
  c26_b03.md: ...分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft...
  c26_b03.md: ...大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；...
  c26_b03.md: ...宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节点可用更便宜硬件（...
  c26_b03.md: ...*Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节点可用更便宜硬件（小 GPU，某些情况...
  c26_b03.md: ...draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0....
  c26_b03.md: ...：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步...
  c26_b03.md: ...n 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding...
  c26_b04.md: ...测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **...
  c26_b05.md: ...ulative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculativ...
  c26_b05.md: ...提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft...
  c26_b05.md: ...token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-...
  c26_b05.md: ...。  (c) Verify model 检查 K 个候选中与其自身预测匹配的前缀。匹配前缀被接受；第一个不匹配被替换为 verify model 的选择。  (d) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常...
  c26_b05.md: ...n）输出。解码前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify...
  c26_b05.md: ...verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verif...
  c26_b05.md: ...aft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify m...
  c26_b05.md: ...erify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6...
  c26_b05.md: ...%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于...
  c26_b05.md: ...6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90%...
  c26_b05.md: ...取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b05.md: ...ulative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculativ...
  c26_b05.md: ...提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft...
  c26_b05.md: ...token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-...
  c26_b05.md: ...。  (c) Verify model 检查 K 个候选中与其自身预测匹配的前缀。匹配前缀被接受；第一个不匹配被替换为 verify model 的选择。  (d) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常...
  c26_b05.md: ...n）输出。解码前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify...
  c26_b05.md: ...verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verif...
  c26_b05.md: ...aft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify m...
  c26_b05.md: ...erify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6...
  c26_b05.md: ...%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于...
  c26_b05.md: ...6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90%...
  c26_b05.md: ...取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b06.md: ...peculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义...
  c26_b06.md: ...U（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请...
  c26_b06.md: ...硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。...
  c26_b06.md: ...el 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，...
  c26_b06.md: ...）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-tr...
  c26_b06.md: ...peculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义...
  c26_b06.md: ...U（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请...
  c26_b06.md: ...硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。...
  c26_b06.md: ...el 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，...
  c26_b06.md: ...）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-tr...
  c26_b09.md: ...工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答...
  c26_b09.md: ...低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-...
  c26_b09.md: ...在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decod...
  c26_b09.md: ...r-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify...
  c26_b09.md: ...verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处...
  c26_b09.md: ...draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（...
  c26_b09.md: ...GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳数增加 0.5-2...
  c26_b09.md: ...门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；有推理平台工程师的团队；网络延迟有界（intra-rack...
  c26_b09.md: ...MA）；成本节省占主导（verify GPU-hour 非常贵的大模型）。  对 Continuum（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚...
  c26_b09.md: ...um（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  2026 年，分布式 speculative decoding 处于早期采用。已发布案例（...
  c26_b09.md: ...工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答...
  c26_b09.md: ...低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-...
  c26_b09.md: ...在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decod...
  c26_b09.md: ...r-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify...
  c26_b09.md: ...verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处...
  c26_b09.md: ...draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（...
  c26_b09.md: ...GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳数增加 0.5-2...
  c26_b09.md: ...门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；有推理平台工程师的团队；网络延迟有界（intra-rack...
  c26_b09.md: ...MA）；成本节省占主导（verify GPU-hour 非常贵的大模型）。  对 Continuum（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚...
  c26_b09.md: ...um（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  2026 年，分布式 speculative decoding 处于早期采用。已发布案例（...
  c26_b11.md: ...致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft...
  c26_b11.md: ...小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群...
  c26_b11.md: ...并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配...
  c30_b06.md: ...tive decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2...

=== watch (83 occurrences) ===
  c18_b11.md: ...作为现代默认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data + watch stream 模式。这些是让其余分布式基础设施运行的元系统。  ---...
  c19_b01.md: ...cd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch streams” 模式。   ### 19.1 发现问题  服务网格有若干实例。实例：  * 启动并变为可用。 * 监...
  c19_b01.md: ...别系统的现代参考设计。  关键性质：  * 基于 Raft（第9章）实现共识。 * 键值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 *...
  c19_b01.md: ...lease（如 30 秒）；实例每 10 秒续期。 * 若实例未续期（崩溃、分区），key 过期并被删除。 * 客户端 watch `/services/payment/` 前缀；实例变更时收到通知。  模式很干净：发现服务无需知道具体服务类型；它提...
  c19_b01.md: ...nt/` 前缀；实例变更时收到通知。  模式很干净：发现服务无需知道具体服务类型；它提供通用键值存储加 lease 与 watch 原语。应用使用这些原语。...
  c19_b01.md: ...cd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch streams” 模式。   ### 19.1 发现问题  服务网格有若干实例。实例：  * 启动并变为可用。 * 监...
  c19_b01.md: ...别系统的现代参考设计。  关键性质：  * 基于 Raft（第9章）实现共识。 * 键值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 *...
  c19_b01.md: ...lease（如 30 秒）；实例每 10 秒续期。 * 若实例未续期（崩溃、分区），key 过期并被删除。 * 客户端 watch `/services/payment/` 前缀；实例变更时收到通知。  模式很干净：发现服务无需知道具体服务类型；它提...
  c19_b01.md: ...nt/` 前缀；实例变更时收到通知。  模式很干净：发现服务无需知道具体服务类型；它提供通用键值存储加 lease 与 watch 原语。应用使用这些原语。...
  c19_b03.md: ...**：DNS TTL 通常 30-300 秒；更新传播慢。失败实例可能在 TTL 过期前仍被返回给客户端。 * **无 watch 原语**：客户端在 TTL 边界轮询 DNS；无推送通知。 * **元数据有限**：DNS 记录只带 IP 和端口；更...
  c19_b03.md: ...明处理。  对成员稳定的服务（数据库、有状态服务），DNS 通常足够。对成员快速变化的服务（自动扩缩无状态服务），基于 watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch...
  c19_b03.md: ...watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：...
  c19_b03.md: ...Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch...
  c19_b03.md: ...在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更时 watch 触发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务...
  c19_b03.md: ...支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更时 watch 触发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务一致性的配置，配置存储使用共识。  示例：  *...
  c19_b03.md: ...*Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。...
  c19_b03.md: ...启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **重配置竞争**：若服务正在执...
  c19_b03.md: ...常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **重配置竞争**：若服务正在执行操作时配置变更，在操作中途应用新配置会导致不一致。解...
  c19_b03.md: ...**：DNS TTL 通常 30-300 秒；更新传播慢。失败实例可能在 TTL 过期前仍被返回给客户端。 * **无 watch 原语**：客户端在 TTL 边界轮询 DNS；无推送通知。 * **元数据有限**：DNS 记录只带 IP 和端口；更...
  c19_b03.md: ...明处理。  对成员稳定的服务（数据库、有状态服务），DNS 通常足够。对成员快速变化的服务（自动扩缩无状态服务），基于 watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch...
  c19_b03.md: ...watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：...
  c19_b03.md: ...Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch...
  c19_b03.md: ...在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更时 watch 触发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务...
  c19_b03.md: ...支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更时 watch 触发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务一致性的配置，配置存储使用共识。  示例：  *...
  c19_b03.md: ...*Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。...
  c19_b03.md: ...启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **重配置竞争**：若服务正在执...
  c19_b03.md: ...常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **重配置竞争**：若服务正在执行操作时配置变更，在操作中途应用新配置会导致不一致。解...
  c19_b05.md: ...共识、较小的现代生态。  **答案：** etcd 和 ZooKeeper 都是分布式协调服务，具有相似原语（键值存储、watch、lease/ephemeral node）。  差异：  | Aspect | etcd | ZooKeeper |...
  c19_b05.md: ...| | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Hadoop 生态（Kaf...
  c19_b05.md: ...eeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **...
  c19_b05.md: ...g as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，...
  c19_b05.md: ...数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _watch_ 相关 ke...
  c19_b06.md: ...lags/new_checkout_flow = true` 的 key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag（`etcdctl put feature_flags/n...
  c19_b06.md: ...`etcdctl put feature_flags/new_checkout_flow false`），每个服务收到 watch 事件并在毫秒级禁用该功能。  没有 watch 模式，替代是轮询（每个服务每 N 秒查询配置）或变更后重启（服务必须重...
  c19_b06.md: ...gs/new_checkout_flow false`），每个服务收到 watch 事件并在毫秒级禁用该功能。  没有 watch 模式，替代是轮询（每个服务每 N 秒查询配置）或变更后重启（服务必须重启才能取新配置）。两者都有缺点：轮询增加负载和延...
  c19_b06.md: ...每 N 秒查询配置）或变更后重启（服务必须重启才能取新配置）。两者都有缺点：轮询增加负载和延迟；重启导致短暂不可用。  Watch 模式是动态配置的现代默认。它行之有效是因为：  * 更新传播快（无轮询延迟）。 * 配置存储负载低（客户端只接收变更，...
  c19_b06.md: ...够，但对无状态自动扩缩服务不足？  提示  DNS TTL 30-300 秒；有状态服务变化少；无状态服务变化快。基于 watch 的发现响应更快。  **答案：** DNS 记录有 TTL，生产中通常 30-300 秒。客户端缓存 DNS 响应；更...
  c19_b06.md: ...60 秒 DNS TTL 意味着客户端在最多 60 秒内仍路由到已终止实例；失败请求、重试风暴、用户可见错误。  基于 watch 的发现（etcd、Consul）：  * 更新在数百毫秒内传播。 * 客户端收到推送通知；无轮询延迟。 * 失败实例在...
  c19_b06.md: ...户端收到推送通知；无轮询延迟。 * 失败实例在数秒内从注册表移除（heartbeat 超时）。  对快速变化的服务拓扑，watch 式发现足够快，能跟得上。DNS 太粗。  实践中，许多生产系统两者都用：DNS 用于稳定服务（数据库、缓存），watc...
  c19_b06.md: ...atch 式发现足够快，能跟得上。DNS 太粗。  实践中，许多生产系统两者都用：DNS 用于稳定服务（数据库、缓存），watch 式用于自动扩缩服务。组合匹配部署特征。...
  c19_b06.md: ...lags/new_checkout_flow = true` 的 key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag（`etcdctl put feature_flags/n...
  c19_b06.md: ...`etcdctl put feature_flags/new_checkout_flow false`），每个服务收到 watch 事件并在毫秒级禁用该功能。  没有 watch 模式，替代是轮询（每个服务每 N 秒查询配置）或变更后重启（服务必须重...
  c19_b06.md: ...gs/new_checkout_flow false`），每个服务收到 watch 事件并在毫秒级禁用该功能。  没有 watch 模式，替代是轮询（每个服务每 N 秒查询配置）或变更后重启（服务必须重启才能取新配置）。两者都有缺点：轮询增加负载和延...
  c19_b06.md: ...每 N 秒查询配置）或变更后重启（服务必须重启才能取新配置）。两者都有缺点：轮询增加负载和延迟；重启导致短暂不可用。  Watch 模式是动态配置的现代默认。它行之有效是因为：  * 更新传播快（无轮询延迟）。 * 配置存储负载低（客户端只接收变更，...
  c19_b06.md: ...够，但对无状态自动扩缩服务不足？  提示  DNS TTL 30-300 秒；有状态服务变化少；无状态服务变化快。基于 watch 的发现响应更快。  **答案：** DNS 记录有 TTL，生产中通常 30-300 秒。客户端缓存 DNS 响应；更...
  c19_b06.md: ...60 秒 DNS TTL 意味着客户端在最多 60 秒内仍路由到已终止实例；失败请求、重试风暴、用户可见错误。  基于 watch 的发现（etcd、Consul）：  * 更新在数百毫秒内传播。 * 客户端收到推送通知；无轮询延迟。 * 失败实例在...
  c19_b06.md: ...户端收到推送通知；无轮询延迟。 * 失败实例在数秒内从注册表移除（heartbeat 超时）。  对快速变化的服务拓扑，watch 式发现足够快，能跟得上。DNS 太粗。  实践中，许多生产系统两者都用：DNS 用于稳定服务（数据库、缓存），watc...
  c19_b06.md: ...atch 式发现足够快，能跟得上。DNS 太粗。  实践中，许多生产系统两者都用：DNS 用于稳定服务（数据库、缓存），watch 式用于自动扩缩服务。组合匹配部署特征。...
  c19_b07.md: ...。经过足够时间确认无残留读之后，下线 ZK 集群。  关键风险与缓解：  * **语义差异**：ZK 和 etcd 在 watch 语义、原子操作等方面有微妙差异。映射必须小心。缓解：双写阶段充分测试。  * **性能差异**：etcd 和 ZK 性...
  c19_b08.md: ...new_checkout_flow` 被切换为 `true`。走一遍每个服务会发生什么，以及可能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b08.md: ...什么，以及可能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b08.md: ...new_checkout_flow` 被切换为 `true`。走一遍每个服务会发生什么，以及可能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b08.md: ...什么，以及可能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b09.md: ...flags/new_checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十...
  c19_b09.md: ...flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. *...
  c19_b09.md: ...变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler...
  c19_b09.md: ...ight 请求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000...
  c19_b09.md: ...请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。et...
  c19_b09.md: ...地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watc...
  c19_b09.md: ...atch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件...
  c19_b09.md: ...00 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内...
  c19_b09.md: ...要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “ne...
  c19_b09.md: ...），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-appli...
  c19_b09.md: ...负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision...
  c19_b09.md: ...能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用...
  c19_b09.md: ...(d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转*...
  c19_b09.md: ...对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；上述边角情况由应用层纪律处理。...
  c19_b09.md: ...flags/new_checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十...
  c19_b09.md: ...flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. *...
  c19_b09.md: ...变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler...
  c19_b09.md: ...ight 请求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000...
  c19_b09.md: ...请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。et...
  c19_b09.md: ...地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watc...
  c19_b09.md: ...atch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件...
  c19_b09.md: ...00 服务舰队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内...
  c19_b09.md: ...要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “ne...
  c19_b09.md: ...），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-appli...
  c19_b09.md: ...负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision...
  c19_b09.md: ...能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用...
  c19_b09.md: ...(d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转*...
  c19_b09.md: ...对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；上述边角情况由应用层纪律处理。...
  c19_b11.md: ...eper 仍在生产中使用（现有 Kafka、Hadoop 等），但缓慢衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etc...
  c19_b11.md: ...务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul 中，watch 变更，动态重新配置）是 2026 年运行时配置的默认，不应要求服务重启。  服务网格（Istio、Linkerd）把服...

=== region (80 occurrences) ===
  c01_b11.md: ...hDB 表），第二个请求会违反约束，重复扣款被阻止。  如果两个副本各自查询本地缓存或尚未复制第一条请求条目的 per-region 存储，那么谁都没看到对方的先前提交。系统在存储层失败了身份谎言：系统相信存在一个“幂等记录存储”，但现实中存在两个短暂...
  c04_b10.md: ...分组因果广播** 。把进程分组（例如按区域或租户）。每条消息携带 per-group 向量（组大小条目）加一个 per-region 摘要。跨组因果以较粗粒度跟踪。可扩展 gossip 系统就是这样做的；代价是跨组因果近似而非精确。  第三种选择：对某...
  c06_b02.md: ...三个好处，按大多数团队优先级近似排序：  (a) **灾难恢复。** 单区域（一个 AWS AZ 组、一个 GCP region）可能因自然灾害、数据中心火灾、网络故障或区域监管行动而不可用。多区域部署能存活单区域丢失。  (b) **用户延迟。*...
  c06_b02.md: ...据驻留。** 欧盟 GDPR、中国 PIPL、俄罗斯数据本地化法律等要求某些用户数据留在特定司法管辖区。按区域 per-region 存储的区域用户架构由此而来。  成本：  * **跨区域操作延迟税。** 任何跨区域操作都付往返成本。 * **Egr...
  c06_b02.md: ...* **Latency** ：归属区域用户看到本地延迟；跨归属操作付全价。 * **Failover** ：per-region 故障切换；其他区域不受影响。 * **Right for** ：以用户为中心的工作负载（聊天、社交、生产力），大多数操...
  c06_b08.md: ...延迟，这是他们的心理模型）。Spanner 过度；中央 DB 让用户体验不对称。  这正是 Slack 的模式：per-region channels，跨区复制做 federation。  **Q2.** 一个团队为 Raft 复制数据库选三个区域，希...
  c14_b10.md: ...毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Pe...
  c15_b09.md: ...突语义** ：CockroachDB 的 per-table 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重）很好。对多区...
  c27_b01.md: ...Olamide 正在审查队友提交的 PR。该 PR “AI 辅助”；大部分代码由 LLM 生成。代码实现了一个跨 region 支付协调器。看起来正确。单元测试通过。集成测试通过。  她读了 30 行后拒绝了 PR。  前 30 行包含了 Thr...
  c27_b11.md: ...ide 的 30 行 PR 审查。她在前 30 行看到全部六个谎言。构造一个包含它们的合理代码片段。  提示  一个跨 region 支付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释...
  c27_b11.md: ...ale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use CockroachDB which is CP fo...
  c27_b12.md: ...ledger_service.record(payment.id, time.time())` 使用本机时钟时间戳。跨 region ledger 排序依赖机器时钟。    4. **谎言 4（锁）**：基于 Redis 的锁没有 fencing to...
  c27_b15.md: ...名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模型是贯穿线。  ---...
  c28_b01.md: ...时间是 2026-09-15。四位主角一起交付了多 region 支付平台。  Olamide 是 SRE 主管。Anya 是数据库主管（Stratus DB，现在内嵌为 Cresce...
  c28_b02.md: ...### 28.1 架构  平台：Crescent 面向 enterprise 客户的多 region 支付处理器。全球接受支付，端到端 p99 延迟低于 750 ms（按产品团队协商的 SLO；§28.2 会拆解完整 p...
  c28_b02.md: ...TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 region（US-East、EU-West、US-West）。每个 region 有完整技术栈。  (c) **Operation...
  c28_b02.md: ...(b) **Regional 集群**：3 个 region（US-East、EU-West、US-West）。每个 region 有完整技术栈。  (c) **Operational 存储**：CockroachDB（每 region 12 节点，...
  c28_b02.md: ...点，共 36）。存储账户、交易、商户数据。可串行化事务。  (d) **账本**：TigerBeetle（5 副本，多 region）。记录每笔资金流动。  (e) **协调器**：Stratus DB（现为 Crescent 组件）。管理分布式事务状...
  c28_b02.md: ...。管理分布式事务状态；经 Raft 复制；对安全关键操作使用 fencing token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (g) **队列**：每 region 一个...
  c28_b02.md: ...*：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (g) **队列**：每 region 一个 Kafka。异步工作流（通知、下游管道）。  (h) **商户协作**：基于 Yjs 的协作文档编辑器（Yuki...
  c28_b02.md: ...dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安...
  c28_b02.md: ...egional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region 异步（Kafka）、边缘缓存（Redis）。...
  c28_b03.md: ...uth / rate limit]         end         subgraph USE["US-East Region"]             APIE[API Service]             REDISE[Redis Cl...
  c28_b03.md: ...AE[Kafka brokers]         end         subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[Redis Cl...
  c28_b03.md: ...AW[Kafka brokers]         end         subgraph USW["US-West Region"]             APIW2[API Service]             CRDBW2[Cockroa...
  c28_b03.md: ...es"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]...
  c28_b03.md: ...a VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]...
  c28_b03.md: ...APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2...
  c28_b03.md: ...br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW         TB...
  c28_b03.md: ...uth / rate limit]         end         subgraph USE["US-East Region"]             APIE[API Service]             REDISE[Redis Cl...
  c28_b03.md: ...AE[Kafka brokers]         end         subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[Redis Cl...
  c28_b03.md: ...AW[Kafka brokers]         end         subgraph USW["US-West Region"]             APIW2[API Service]             CRDBW2[Cockroa...
  c28_b03.md: ...es"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]...
  c28_b03.md: ...a VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]...
  c28_b03.md: ...APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2...
  c28_b03.md: ...br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW         TB...
  c28_b04.md: ...r 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US-East region（Adaeze 的计算 home region，由 geo-DNS 决定；圣保罗到 US-East 单程约 55 ms，...
  c28_b04.md: ...率限制检查）。5 ms。  (2) Worker 路由到 US-East region（Adaeze 的计算 home region，由 geo-DNS 决定；圣保罗到 US-East 单程约 55 ms，是 Crescent 三个 region 中最...
  c28_b04.md: ...me region，由 geo-DNS 决定；圣保罗到 US-East 单程约 55 ms，是 Crescent 三个 region 中最近的）。60 ms 跨赤道。  (3) US-East 的 regional 负载均衡器路由到健康的 API 服务...
  c28_b04.md: ...mi 余额（EU-West shard，拉各斯商户），更新 merchant_day_rollup。跨 shard、跨 region 事务，带 HLC 时间戳；为可串行化一致性做 commit-wait。95 ms（受第6章所说的最坏情况跨 regio...
  c28_b04.md: ...egion 事务，带 HLC 时间戳；为可串行化一致性做 commit-wait。95 ms（受第6章所说的最坏情况跨 region quorum 物理限制）。  (10) 事务的 coordinator（Stratus DB）用 fencing to...
  c28_b04.md: ...s 最后一公里。  总计：端到端 p50 约 360 ms。p99 约 580 ms（欺诈检测 LLM 尾部更宽，且跨 region 提交跳每跳付出约 95 ms 的最坏情况 Raft quorum 物理代价）。远低于 Crescent 产品团队为跨...
  c28_b04.md: ...提交跳每跳付出约 95 ms 的最坏情况 Raft quorum 物理代价）。远低于 Crescent 产品团队为跨 region 支付地理正确性协商的 750-ms p99 SLO。  Trace（第20章）捕获整个旅程。47 个 span，分布在...
  c28_b05.md: ...个组件都经 PACELC 分类 | n/a | | 3 | 时间、HLC | CockroachDB 使用 HLC；跨 region 时间戳 | n/a | | 4 | 广播原语 | Kafka（原子）、Consul（gossip） | E.2（Con...
  c28_b05.md: ...wstamped（TigerBeetle） | E.5（无 fencing 的 failover） | | 6 | 多 region | 三 region 部署；每跳延迟预算 | n/a | | 7-10 | 共识 | Raft（Stratus、Coc...
  c28_b05.md: ...gerBeetle） | E.5（无 fencing 的 failover） | | 6 | 多 region | 三 region 部署；每跳延迟预算 | n/a | | 7-10 | 共识 | Raft（Stratus、CockroachDB）、V...
  c28_b06.md: ...### 28.4 发布  发布日：2026-09-15。团队 06:00 UTC 集合（覆盖所有 region 的工作时间）。  (a) **发布前检查清单**：两周前混沌 game day；所有 action items 已关闭...
  c28_b06.md: ...*10:30 UTC**：小异常。EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f)...
  c28_b10.md: ...；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 shard/region...
  c28_b10.md: ...未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 shard/region 原子提交或原子中止 | 2PC + Raft 复制协调器 + HLC 版...
  c28_b10.md: ...region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 shard/region 原子提交或原子中止 | 2PC + Raft 复制协调器 + HLC 版 MVCC | 协调器状态在易失内存；竞争下不...
  c28_b11.md: ...四位主角的系统在第28章的多 region 支付平台汇聚。本书结束于它开始的地方：Adaeze 的支付，现在被正确处理，因为工程师学会了纪律。  2026 年的分...
  c28_b14.md: ...ailover 不可用窗口；异步复制 RPO。  对 Crescent：Postgres + Patroni 不满足多 region 需求；CockroachDB 是正确选择。对单 region OLTP 工作负载，Postgres + Patroni...
  c28_b14.md: ...cent：Postgres + Patroni 不满足多 region 需求；CockroachDB 是正确选择。对单 region OLTP 工作负载，Postgres + Patroni 运维简洁且工作良好。  这个练习说明：诊断方法适用于任何系统...
  c28_b18.md: ...### 本章总结  Crescent 多 region 支付平台是综合。四位主角（Olamide、Anya、Yuki、Tobias）贡献了组件；系统使用了书中每个原语。发布是...
  c28_b18.md: ...应用：发布前混沌、渐进金丝雀发布、发布后复盘。  每章的课程都出现在架构中：每个组件做 CAP/PACELC 分析；跨 region 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing token；追踪用 Ope...
  c29_b04.md: ...算 fabric，改变数据中心设计）。本书框架仍适用；具体成本模型会变。  * **监管冲击**：重大数据主权法改变多 region 部署的权衡。诊断方法（第11章）仍是正确工具；应用它的约束会变。  * **AI 能力不连续性**：若 AI 在分布式...
  c30_b04.md: ...ft 重新选举；每 range 亚秒级 | 可串行化 + follower read | CP | PC/EC | 多 region Postgres 兼容；任意云 | | **DynamoDB** | 无领导者；quorum | 透明 | 默认最终；...
  c30_b10.md: ...NewSQL**：在中等单区域规模，PostgreSQL 的运维成熟度和生态系统胜过横向扩展的边际收益。直到真正需要多 region 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aur...
  c30_b10.md: ...IOPS 计费更高。   #### F.2 — 面向全球用户的多区域 OLTP  **输入**：全球用户；部分操作需跨 region 一致性（支付、库存）；p99 延迟低于 500ms；跨大洲灾难恢复。  **决策**：CockroachDB 或 Sp...
  c30_b10.md: ...eetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典...
  c30_b10.md: ...ational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West...
  c30_b10.md: ...U-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配...
  c30_b10.md: ...用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**...
  c30_b10.md: ...NewSQL**：在中等单区域规模，PostgreSQL 的运维成熟度和生态系统胜过横向扩展的边际收益。直到真正需要多 region 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aur...
  c30_b10.md: ...IOPS 计费更高。   #### F.2 — 面向全球用户的多区域 OLTP  **输入**：全球用户；部分操作需跨 region 一致性（支付、库存）；p99 延迟低于 500ms；跨大洲灾难恢复。  **决策**：CockroachDB 或 Sp...
  c30_b10.md: ...eetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典...
  c30_b10.md: ...ational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West...
  c30_b10.md: ...U-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配...
  c30_b10.md: ...用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**...
  c30_b12.md: ...尖峰。  **单区域 Postgres + 读副本足够时不要伸手选 Spanner。** Spanner 是最干净的多 region 系统，但它的 commit-wait 延迟每次写入都要支付。对单区域工作负载，Postgres 生态成熟度和每次写入延...
  c30_b12.md: ...sh。** Istio/Linkerd 增加运维复杂度。对小型服务图，Kubernetes 原生服务发现足够。只有在多 region、多协议、复杂流量需求真实存在时才采用 service mesh。  **崩溃故障共识足够时不要伸手选 BFT。** B...
  c30_b12.md: ...ner 风格全局线性化。** 大多数以用户为中心的工作负载（聊天、社交、生产力）可通过将每个用户数据 home 在一个 region 来服务。全局线性化是最昂贵的一致性级别；只有当工作负载真正需要所有用户的全局排序时才支付它。  统一模式：**系统的成...
  c30_b16.md: ...| 它给出 _交集_，更弱。并发写和 sloppy quorum 破坏线性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-o...
  c30_b16.md: ...并发写和 sloppy quorum 破坏线性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 qu...
  c30_b16.md: ...rum 破坏线性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 |...
  c30_b16.md: ...10 | "多 region HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 | "FLP 说共识不可能。" |...
  c30_b19.md: ...race 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 region 出现多个协调器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制...
  c30_b19.md: ...但未全部完成 | 补偿幂等性违反或 saga 状态丢失 | Ch.13 §13.6.5（saga 正确性义务）   跨 region 提交 p99 >150 ms | 跨 region Raft quorum；延迟底线 | Ch.6 §6.1（光速），...
  c30_b19.md: ...失 | Ch.13 §13.6.5（saga 正确性义务）   跨 region 提交 p99 >150 ms | 跨 region Raft quorum；延迟底线 | Ch.6 §6.1（光速），Ch.13 §13.7   Trace 中锁定获取但...

=== safety (77 occurrences) ===
  c07_b02.md: ...成立）与 _liveness_（必须最终成立）分开。Paxos 和 Raft 都具有这种结构：任何网络行为下都不会违反 safety，但网络足够差时可能不终止。一旦网络稳定，它们就恢复终止。...
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agree...
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不...
  c07_b03.md: ...eness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值...
  c07_b03.md: ...例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safe...
  c07_b03.md: ...两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Li...
  c07_b03.md: ...fety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  *...
  c07_b03.md: ...无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。...
  c07_b03.md: ...985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safe...
  c07_b03.md: ...fety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。...
  c07_b03.md: ...FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liveness：构建可能不终止但永不决定错误...
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agree...
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不...
  c07_b03.md: ...eness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值...
  c07_b03.md: ...例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safe...
  c07_b03.md: ...两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Li...
  c07_b03.md: ...fety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  *...
  c07_b03.md: ...无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。...
  c07_b03.md: ...985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safe...
  c07_b03.md: ...fety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。...
  c07_b03.md: ...FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liveness：构建可能不终止但永不决定错误...
  c07_b06.md: ...慢节点有界），协议可以推进。  实践中 GST 是隐含的：网络通常表现良好。不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示...
  c07_b06.md: ...的：网络通常表现良好。不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：...
  c07_b06.md: ...质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术不同；运维后果不同。 =====END=====...
  c07_b06.md: ...慢节点有界），协议可以推进。  实践中 GST 是隐含的：网络通常表现良好。不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示...
  c07_b06.md: ...的：网络通常表现良好。不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：...
  c07_b06.md: ...质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术不同；运维后果不同。 =====END=====...
  c07_b07.md: ...ion（每个正确进程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何...
  c07_b07.md: ...正确性问题。  (c) 运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b07.md: ...ion（每个正确进程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何...
  c07_b07.md: ...正确性问题。  (c) 运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b10.md: ...何如此行为至关重要。Raft 在分区期间无法选出 leader 时，就是 FLP 在起作用：在持续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视为“数据库坏了”，并可能采取破坏 safety 的反生产措施（强制 l...
  c07_b10.md: ...续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视为“数据库坏了”，并可能采取破坏 safety 的反生产措施（强制 leader、禁用 Raft）。  现代协议通过明确选择（始终 safe，liveness 有条件...
  c07_b11.md: ...述。FLP 不可能性说确定性协议在纯异步下一次崩溃下不能保证终止全部三条；现代逃生舱是部分同步、随机化和故障检测器。  Safety 性质（“坏事永不发生”）由现代共识协议始终保证。Liveness 性质（“好事最终发生”）只在网络稳定后保证。Cres...
  c08_b02.md: ...promise 的 `accepted_value` 都是 null），proposer 使用自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Propose...
  c08_b02.md: ...ers。也有优化（指定 learner；把信息捎带在协议消息上）。   ### 8.4 为什么 Paxos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ball...
  c08_b02.md: ...promise 的 `accepted_value` 都是 null），proposer 使用自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acceptor 接受）必须是新 ballot 提议的值。Propose...
  c08_b02.md: ...ers。也有优化（指定 learner；把信息捎带在协议消息上）。   ### 8.4 为什么 Paxos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ball...
  c08_b03.md: ...提议值。单法令 Paxos 没有 leader 概念。任何进程任何时候都可提议；多个 proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一...
  c08_b03.md: ...proposer 并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。...
  c08_b03.md: ...但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos 就一个值达成一致。状态机复制需要值序列（复制...
  c08_b04.md: ...间持久化 $b_{max}$、`accepted_ballot`、`accepted_value`。丢失这些状态会破坏 safety。 * **Reconfiguration** ：增减 acceptor 集合本身必须通过 Paxos 完成，以避免两个...
  c08_b04.md: ...cipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进...
  c08_b05.md: ...ser 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 safety 不变式。   ### 8.9 练习...
  c08_b06.md: ...。任何已被某 acceptor 接受的值（即使尚未多数）都“在飞行中”，可能已选定。新 ballot 必须采纳它以避免 safety 违反。  新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任何未来 ballot 都会看到...
  c08_b07.md: ...eptor 丢失该状态（磁盘故障、漏 fsync、文件系统损坏），它会“忘记”先前 promise 并接受冲突值，违反 safety。  (b) **重配置** ：改变 acceptor 集合（增删节点）不能随意，因为两个不同配置可能各自形成无重叠的“...
  c08_b07.md: ...g，导致配置不一致。  累积效应：Paxos 实现通常是数千行谨慎代码。上述任何 bug 都可能导致数月后才显现的静默 safety 违反。“正确 Paxos 实现很少”并非夸张。...
  c08_b08.md: ...值已在 ballot 20 时被选定。  系统收敛到单一值 “Olamide”。A 原本想提议 “Anya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集群有 5 个节点。当前 leader 到其余 4 个中...
  c08_b09.md: ...结果：短暂的不可用（选举期间，通常数秒），然后多数方新 leader 恢复服务。没有 safety 违反；最坏情况是短暂暂停。  **Q3.** 比较 Paxos 与 2PC。两者都有两阶段，它们在处理协调者故障方面的...
  c08_b09.md: ...下游客户端）。  _Paxos 结构_ ：任何 proposer 都可提议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 prop...
  c08_b10.md: ...论：** 辩护：Raft 由 Ongaro 和 Ousterhout 明确设计为比 Paxos 更易理解，同时提供相同 safety 保证。大多数新分布式系统（etcd、Consul、TiKV、CockroachDB、SpiceDB）用 Raft。Di...
  c08_b10.md: ...状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 propose...
  c08_b10.md: ...$ 时多数也是 3。LLM 代码常硬编码错误值，与偶数节点一起使用时破坏法定人数相交。  还有三条（任一条都会造成静默 safety 违反）：  (d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 acc...
  c08_b10.md: ...。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c08_b10.md: ...safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c08_b11.md: ...因此重试消息可能被处理两次，污染 acceptor 状态。  更深观察：Paxos 是 LLM 生成代码的最坏情况类别。Safety 性质在任何单次测试运行中不可见；它们需要对抗性调度（在特定时刻故障）才能揭示。一个只被训练过 Paxos 描述但未被训...
  c08_b11.md: ...9 年提出的经典共识协议。单法令 Paxos 通过两阶段（prepare、accept）加法定人数相交就一个值达成一致。Safety 性质由 accept 规则保留：任何 proposer 的 accept 必须提议第一阶段响应中最高已接受的值；这保证...
  c09_b01.md: ...g replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **Safety** ：确保一旦日志条目提交，它保持提交，且没有其他服务器在相同索引提交冲突条目。  这种分解在教学上重要：每个部分可独...
  c09_b03.md: ...运维）一个系统，通过任意 leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader 接受客户端请求。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `App...
  c09_b03.md: ...盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且...
  c09_b03.md: ...dex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 ter...
  c09_b03.md: ...已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不...
  c09_b03.md: ...运维）一个系统，通过任意 leader 变更和分区，维护“索引 i 上 term T 的已提交条目永远留在索引 i”的 safety 性质。  Leader 接受客户端请求。对每个请求：    1. 把条目追加到本地日志。   2. 并行发送 `App...
  c09_b03.md: ...盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保日志在集群中 _只追加_ 且...
  c09_b03.md: ...dex[follower]`（已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 ter...
  c09_b03.md: ...已知复制到该 follower 的最高条目）。成功追加后两者都推进。   ### 9.5 Safety  Raft 的 safety 性质：若某 leader 已在 term T 的索引 i 提交了条目，则任何未来 term 的其他 leader 都不...
  c09_b06.md: ...解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Followe...
  c09_b06.md: ...` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的...
  c09_b06.md: ...解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Followe...
  c09_b06.md: ...` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **Safety** ：确保索引 i 一旦提交，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的...
  c09_b10.md: ...在提交之后才 _应用_ 到状态机。许多 LLM 实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 te...
  c09_b10.md: ...term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendEntries 一致性检查 off...
  c09_b10.md: ...evious term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendEntries 一致性检查 off-by-one。** 检查比较 follower...
  c10_b05.md: ...要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 都是多数。Howard 的洞见：对 safety 只需要 $Q_1 \cap Q_2 \ne \emptyset$。  这意味着：$Q_1$ 和 $Q_2$ 可以调优。...
  c10_b05.md: ...这意味着：$Q_1$ 和 $Q_2$ 可以调优。只要任意 prepare 法定人数与任意 accept 法定人数相交，safety 就成立。  示例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rceil + 1$。两者都是多数。...
  c10_b05.md: ...repare 法定人数。  这一洞见用于一些 2026 年分布式数据库设计，以降低 per-commit 延迟而不牺牲 safety。CockroachDB 和 TiDB 的路线图中已有变体；FoundationDB 多年来一直可配置法定人数大小。...
  c10_b08.md: ...Q_2$ 各为 N 的多数。这确保 $Q_1 \cap Q_2 \ne \emptyset$（任意两个多数相交）以保证 safety。  Howard 的推广：只需要 $Q_1 \cap Q_2 \ne \emptyset$。只要两个法定人数相交，两个...

=== clock (75 occurrences) ===
  c01_b09.md: ...一点，但窗口太短；第二个请求刚好落在窗口外。  对于时间谎言，使用混合逻辑时钟（HLC）时间戳（第3章）而非 wall-clock 时间戳，会让两个协调者得到相同的可比较顺序。任一协调者都会看到另一方的提交在 HLC 时间中先发生，即使两者都在 NT...
  c01_b10.md: ...钟跳跃。被虚拟机监控器暂停 30 秒（快照、实时迁移、某些运行时的 GC STW 暂停）的 VM 恢复后，其 wall clock 会跳跃 30 秒。从外部看，该节点好像时间旅行到了未来。在节点内部，任何跨暂停前后两次时钟读取来推理持续时间的代码都会...
  c01_b12.md: ...一致”，会惊讶地发现 Postgres 中的 `now()` 返回的是 _事务开始时间_ ，而非调用时刻的 wall-clock 时间。同一事务中的两条语句看到同一个 `now()`。这是文档里写明的，但违背朴素的单机直觉。...
  c02_b03.md: ...作看起来都在其调用与响应之间的某个瞬间原子地生效。  “满足真实时间顺序”这一点让它很强。如果客户端 A 在 wall-clock 时间 t_1 完成一次写入，客户端 B 在 wall-clock 时间 t_2 > t_1 开始一次读取（两个客户端可...
  c02_b03.md: ...间顺序”这一点让它很强。如果客户端 A 在 wall-clock 时间 t_1 完成一次写入，客户端 B 在 wall-clock 时间 t_2 > t_1 开始一次读取（两个客户端可以在不同机器上），那么 B 的读取必须看到 A 的写入（或更晚的写...
  c03_b01.md: ...持续时间，应使用独立的 _单调_ 时钟。单调时钟永远不会回退（不被 NTP 调整），也不会跳跃前进。它们不与 wall-clock 时间绑定；其值只有在作差时才有意义。大多数现代语言同时暴露两者：Python 中 `time.time()` 是 wa...
  c03_b02.md: ..._ ，记作 a \parallel b。并发在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另一个）；有些...
  c03_b02.md: ...的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能...
  c03_b02.md: ...ns-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序...
  c03_b02.md: ...ll-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。...
  c03_b02.md: ...产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳——工作流 1：逻辑时钟实现  > **工作流 1（输入...
  c03_b04.md: ...#### 常见误解  常见混淆：向量时钟“告诉你现在几点”。不是。向量时钟是推理 _顺序_ 的结构；与 wall-clock 时间无关。向量 [2,2,2] 不表示“自某事以来 2 秒”，它表示“我已观察到三个进程各发生 2 个事件”。把向量时...
  c03_b04.md: ...2,2,2] 不表示“自某事以来 2 秒”，它表示“我已观察到三个进程各发生 2 个事件”。把向量时钟比较与 wall-clock 比较混在同一逻辑中会产生 nonsense。...
  c03_b05.md: ...### 3.5 混合逻辑时钟（HLC）  向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014...
  c03_b05.md: ...mirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一个逻辑计数器组件为同一 wall-clock 瞬间内的事件排序。  进程 p 上的 HLC...
  c03_b05.md: ...HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一个逻辑计数器组件为同一 wall-clock 瞬间内的事件排序。  进程 p 上的 HLC 是一个二元组 (l, c)，其中 l 是 wall-clock 时间（平...
  c03_b05.md: ...wall-clock 瞬间内的事件排序。  进程 p 上的 HLC 是一个二元组 (l, c)，其中 l 是 wall-clock 时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、...
  c03_b05.md: ...当下时刻的三条信息：p 的 HLC 对此的看法 (l_p)、发送者的 HLC 对此的看法 (l_m)、p 的 wall clock 对此的看法 (t)。新的 l' 必须是最大值，以保证 HLC 单调且尊重 happens-before；它不能小于其中...
  c03_b06.md: ...用发送者的 l_m 并扩展发送者计数器，以记录该事件发生在发送之后。c' = c_m + 1。 * **只有 wall clock 贡献了 (l' = t，严格大于 l_p 和 l_m 两者)** ：物理时间已前进过两个先前 HLC；计数器重置，因为...
  c03_b06.md: ...re** ：a \to b 蕴含 HLC(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \e...
  c03_b06.md: ...重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  CockroachDB、YugabyteDB、MongoDB（...
  c03_b06.md: ...有 GPS 原子钟的情况下获得 TrueTime 类行为？”   #### 工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|--...
  c03_b07.md: ...为一个 _区间_ ：每次调用 `TT.now()` 返回一对 `[earliest, latest]`，真实 wall-clock 时间保证落在该区间内。  Spanner 在每个数据中心部署 GPS 接收器和原子钟，加上一层监控并报告不确定性的软件...
  c03_b07.md: ...2. **等待** 直到 TT.now().earliest > s。这等待掉不确定性区间；一旦成立，真实 wall-clock 时间可证明已越过 s，因此不会有其他事务随后分配更小时间戳并声称更早提交。   3. 应用事务。  等待通常为 7 m...
  c03_b07.md: ...Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业等价物。  §3.5 描述的 HLC 路径...
  c03_b08.md: ...s + GPS 原子钟基础设施 CockroachDB / YugabyteDB / TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequ...
  c03_b08.md: ...原版的 `sorted(messages, key=lambda m: m.timestamp)` 错在把 wall-clock 时间当作 happens-before 的代理。三种修复任一都能让函数正确。选择哪种取决于你的运营容忍度和所需一致性。...
  c03_b09.md: ...2 上由 P_1 发送的消息接收，而 y 的写入又发生在同一进程该接收之后。  并发示例：进程 P_1 在某 wall-clock 时刻写入 x = 5，同时进程 P_2 写入 y = 10，两者未互相通信。没有 happens-before 链连接...
  c03_b10.md: ...**Q4.** 进程 P 的 HLC 是 (100, 3)。P 的 wall-clock 读数为 102。P 收到一条 HLC 为 (105, 1) 的消息。接收后 P 的 HLC 是多少？  提示  HLC...
  c03_b10.md: ...况：c' = c_m + 1 = 1 + 1 = 2。  新 HLC 为 (105, 2)。  HLC 贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接收 HLC...
  c03_b10.md: ...1 = 2。  新 HLC 为 (105, 2)。  HLC 贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接收 HLC (105, 2) 按字典序严格大于消息 H...
  c03_b11.md: ...了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cassandra 的 LWW 依赖每次写入的 wall-clock 时间戳。如果删除被盖戳早于后续写入的时间戳，会发生什么？  **答案：** Cassandra 的默认冲突解决是 la...
  c03_b11.md: ...：** Cassandra 的默认冲突解决是 last-write-wins（LWW），基于协调者节点分配的 wall-clock 时间戳。如果删除由时钟落后的协调者处理，它被盖戳早于另一个时钟超前的协调者上的后续写入。当删除和写入到达副本时，副本看...
  c03_b11.md: ...* 迁移到基于 HLC 的 MVCC 系统（CockroachDB、YugabyteDB），其冲突解决不依赖 wall clock。  对这个具体工作负载（三数据中心每秒 100k 写入），每次写入用 LWT 可能太贵；实际答案是要么严格 NTP 纪...
  c03_b11.md: ...写入），每次写入用 LWT 可能太贵；实际答案是要么严格 NTP 纪律加监控症状，要么迁移到写入路径不依赖 wall clock 正确性的系统。  **Q2.** 一位新工程师为 10,000 节点微服务系统实现向量时钟。向量增长到每个消息 10,...
  c03_b12.md: ...戳 t_w 满足 t_s < t_w < t_s + \text{max\_skew}，系统无法判断该写入在 wall-clock 时间中是否发生在事务开始之前，于是 _重启_ 事务，用新时间戳推到观察到的写入之后。这就是“uncertainty r...
  c03_b14.md: ...Time 对能负担 GPS 基础设施且工作负载中重启成本超过 commit-wait 成本的团队是正确答案。AWS 的 clock-bound API（Amazon Time Sync Service）首次在 Google 之外提供 TrueTime...
  c03_b14.md: ...比 B 快 50 ms，合并日志显示 B 在 A 收到之前就开始处理。取证追踪被破坏。  (b) **同毫秒 wall-clock 碰撞** ：多台服务器每秒写数千行日志，可能产生相同毫秒分辨率的时间戳。按时间戳排序会在每毫秒内任意排序，破坏每台服务...
  c03_b15.md: ...trace context（用于分布式追踪，第20章），它们已经携带了足以跨服务正确排序日志事件的信息。用 wall clock 的日志系统把这些取证证据白白浪费掉。   ### 本章总结  分布式系统讲的第一个谎言是时间。NTP 只给有界 _预期...
  c03_b15.md: ...## 本章总结  分布式系统讲的第一个谎言是时间。NTP 只给有界 _预期_ 漂移，不给有界 _保证_ 漂移。wall-clock 时间戳是事件偏序的有损代理。用跨机器时间戳比较来决定“哪个先发生”的代码是在对一个虚构概念做推理。  Lamport...
  c03_b15.md: ...但不能检测并发。向量时钟为每个进程每个事件分配一个整数，能检测并发，但扩展性差。混合逻辑时钟（HLC）结合 wall-clock 风格与逻辑计数器排序，保持恒定大小，是 CockroachDB、YugabyteDB、MongoDB 等 2026 系...
  c03_b15.md: ...性区间。Spanner 用 TrueTime 支付 7 ms commit-wait，换取线性化全局时间戳。AWS 的 clock-bound API 正在让 TrueTime 类原语在云厂商上可用。HLC 加已知偏移上界是该领域其余团队的实用替代。...
  c05_b03.md: ...两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **Sibling values** 保留到应...
  c13_b05.md: ...e（GPS + 原子钟） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC...
  c13_b05.md: ...迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到 CockroachDB / TiDB...
  c13_b13.md: ...；软件监控并报告时钟不确定性。`TT.now()` 返回区间 `[earliest, latest]`，真实 wall-clock 时间 provably 位于该区间内。典型不确定性：1-7 毫秒。  Spanner 通过 _commit-wait_...
  c13_b13.md: ...().earliest > commit_timestamp`。通常约 ~7 ms。   3. 等待后，真实 wall-clock 时间 provably 已过 commit_timestamp。   4. 提交释放；锁释放。  等待确保任何后续事务...
  c13_b16.md: ...际正确性收益对大多数工作负载很小。  综合：TrueTime 是更干净的方案；HLC 对大多数团队更实用。AWS 引入 clock-bound API 正在缩小差距；5 年内我们可能看到 HLC 系统迁移到 clock-bound API，差别变得无...
  c13_b16.md: ...大多数团队更实用。AWS 引入 clock-bound API 正在缩小差距；5 年内我们可能看到 HLC 系统迁移到 clock-bound API，差别变得无关紧要。目前选择是运维性的：若你在 GCP 且成本可接受，用 TrueTime；若你在...
  c13_b17.md: ...rueTime 替代。成本可变（争用时重启）而非恒定（每次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide 的跨区域支付事务在...
  c16_b03.md: ...vin Jahns 约 2014 年启动）使用 RGA 的优化变体。每个字符由一个小结构体表示（约 8 字节），包含其 clock、client ID 和指向前驱的指针。该结构对常见情况高度优化（顺序打字产生字符链，每个指向前一个）。  Yjs 在...
  c16_b04.md: ...#### 冲突解决  Yjs 自动处理所有冲突。若两个用户并发在同一位置插入字符，Yjs 按 client ID 和 clock 确定性排序。若一个用户删除段落时另一个将其加粗，粗体属性保留在幸存字符上（未被删除的字符）；被删除的字符消失。  结果...
  c16_b07.md: ...**答案：** LWW-Register 使用 wall-clock（或 HLC）时间戳决定哪个值胜出。若两个副本并发写入，时钟 _更快_ 的副本胜出（无论哪个写逻辑上更晚），系统就会丢失...
  c16_b07.md: ...看到所有存活值（作为 siblings）并决定。不丢数据；冲突解决在应用层。  (b) **用 HLC 代替 wall clock** ：HLC（第3章）可减少但不能消除偏移问题。HLC 尊重 happens-before；因果有序的两写会被正确打时...
  c20_b03.md: ...告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wall-clock 时长，但不拆解接收服务内部做了什么（哪些函数慢、哪些查询慢）。为此需要服务内性能分析。  (b) **资源竞争**：若...
  c27_b03.md: ...钟偏移或因果性。  现实：跨机器时钟最好情况下可能差数十毫秒，最坏情况差数秒。时间戳非常接近的事件无法可靠用 wall clock 排序。应由因果性排序的事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-before、H...
  c27_b03.md: ...事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-before、HLC 或 vector clock（第3章）排序。       def merge_events(events_from_replicas):...
  c27_b03.md: ...y=lambda e: (e.hlc.l, e.hlc.c, e.replica_id))   LLM 产出 wall-clock 版本因为它是最简单的排序表达式。HLC 版本需要理解时间的谎言。   ### 27.5 谎言 4：朴素分布式锁有效...
  c27_b09.md: ...circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall-clock 时间戳做排序。    4. **朴素分布式锁有效**：Redlock 风格代码假设锁持有者唯一；缺少 fencing...
  c27_b09.md: ...（谎言 3）`sorted(events, key=lambda e: e.timestamp)`。跨机器 wall-clock 排序；时钟偏移破坏排序。  （谎言 4）`if redis.set(key, "1", nx=True, ex=30)...
  c27_b11.md: ...一个包含它们的合理代码片段。  提示  一个跨 region 支付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：...
  c28_b10.md: ...ppens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长；HLC 计数无界溢出 | | 2 | 广播原语伪代...
  c28_b10.md: .../ Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长；HLC 计数无界溢出 | | 2 | 广播原语伪代码 | Ch.4 §4.7 | 要交付给 N 个进程的消息 |...
  c28_b14.md: ...区时一致；正常操作时低延迟，因异步复制）。  _三个谎言姿态_：  * **时间**：Postgres 使用 wall-clock 时间戳；事务按事务开始时间排序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **...
  c28_b14.md: ...态_：  * **时间**：Postgres 使用 wall-clock 时间戳；事务按事务开始时间排序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供 leader...
  c29_b01.md: ...许多工作负载的 HLC  2026 现实（第3、13章）：TrueTime 仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bo...
  c29_b01.md: ...ax-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；API 已可用但尚未被普遍消费。...
  c29_b01.md: ...0 轨迹：  * 预计 **CockroachDB、TiDB、YugabyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增...
  c29_b01.md: ...nd 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增加类似 clock-bound 服务；云厂商在基础设施上竞争。 * 本地 **GPS + PTP** 基础设施变便宜；大型金融公司正在为关...
  c29_b01.md: ...务工作负载部署自己的 TrueTime 等价基础设施。  含义：到 2029-2030 年，"使用 HLC + max-clock-skew" 可能像今天的 "仔细调 NTP" 一样显得过时。迁移路径已知；心智模型（时间不确定；系统必须界定不确定性）...
  c30_b06.md: ...通过健康检查但实际从应用视角已损坏的故障。 * **HLC（混合逻辑时钟）**：二元组 (l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV...
  c30_b14.md: ...ks (small N)     │   └── hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/...
  c30_b16.md: ...才是正确问题。 | Ch.2 | | 6 | "向量时钟告诉你现在几点。" | 向量时钟推理 _顺序_，不是 wall-clock 时间。值不是秒。 | Ch.3 | | 7 | "我运行 NTP 就有 TrueTime。" | TrueTime 给...
  c30_b19.md: ...（RPC 语义），Ch.18（fencing token），Ch.27 §27.2   跨服务 span 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 wall-clock；时钟偏...
  c30_b19.md: ...all-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 wall-clock；时钟偏移 | Ch.3（HLC），Ch.20 §20.2（context 传播）   一个慢 span 主导请求延迟；...

=== key (75 occurrences) ===
  c01_b11.md: ...领导者选举协议。一位资深工程师说：“我们用 Redis `SET NX EX 30` 选主。领导者每 10 秒刷新一次 key。如果领导者挂掉，key 过期，别人接管。”指出该设计未处理的三种失败模式。需要时可参考第18章。  提示  该模式是...
  c01_b11.md: ...工程师说：“我们用 Redis `SET NX EX 30` 选主。领导者每 10 秒刷新一次 key。如果领导者挂掉，key 过期，别人接管。”指出该设计未处理的三种失败模式。需要时可参考第18章。  提示  该模式是 Redlock。Jeps...
  c04_b09.md: ...代理只在一个分区内保证顺序；同一 topic 不同分区间的事件无序。应用必须将相关事件放在同一分区（partition key = entity ID），或接受跨分区顺序未定义。  (c) **消费者重平衡语义** 。若消费者重平衡（例如部署期间...
  c04_b09.md: ...支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition key（通常是 transaction ID）幂等。重复扣款 bug 是因为幂等检查没有强一致。  **Q2.** 用 flo...
  c04_b10.md: ...做的；代价是跨组因果近似而非精确。  第三种选择：对某些应用用 _causal+ consistency_ 加 per-key 版本向量替代因果广播。每个键有一个 contributors 向量；向量随键的写作者数增长，而非系统大小。这是 CRD...
  c12_b01.md: ...现代系统常同时提供两者。CockroachDB 默认范围分片，但表可显式按 `crdb_internal` shard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering columns...
  c12_b01.md: ...crdb_internal` shard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering columns，使同一 partition 内数据有序（在哈希 partition 内实现局...
  c13_b03.md: ...（及其时间戳）。   2. 在 start_timestamp 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）...
  c13_b03.md: ...art_timestamp 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite...
  c13_b03.md: ...败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 commit 记录（value, commit_timestamp）并清除锁。  若该原子...
  c13_b03.md: ...ondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 prima...
  c13_b03.md: ...沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 primary key 扮演了协调者角色。协调者故障被客户端故障取代，而客户端故障由读时恢复处理（任何后续 reader 都能清理）。   #...
  c13_b12.md: ...gle Percolator？它如何避免需要中心协调者？  提示  Percolator 用事务中的 “primary key” 作为原子提交点。恢复由读者而非协调者执行。  **答案：** Percolator 是 Google 的分布式事务协...
  c16_b02.md: ...t 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值的集合（...
  c17_b05.md: ...Cassandra 的 LWW 冲突解决决定，存在第3章的时钟偏移问题。  QUORUM _不提供_：  * Per-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-key；...
  c17_b05.md: ...-key 线性化：并发写仍竞争；LWW 解决；赢家可能不是逻辑上更晚的写。 * 原子多键操作：QUORUM 是 per-key；多键一致性需要轻量事务（LWT，基于 Paxos，更慢）。 * 跨多键的线性化读：每个键的 QUORUM 读独立；读集...
  c17_b05.md: ...UORUM 对许多工作负载是 “足够好” 的一致性（大多数数据库操作是单键，可容忍 LWW 解析）。对需要真正 per-key 线性化的工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，QUOR...
  c18_b01.md: ...后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 *...
  c18_b02.md: ...#### 常见陷阱  (a) **缓存过期时的惊群（thundering herd）** ：多个读者在 key 过期时同时请求；全部未命中并涌向 source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事...
  c18_b02.md: ...d）** ：多个读者在 key 过期时同时请求；全部未命中并涌向 source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事故期间的缓存踩踏** ：truth source 变慢或宕机；所有缓存过期，所有读者击中慢 so...
  c18_b02.md: ...” 事件；缓存主动失效。   ### 18.2 分布式锁  锁协调跨进程对共享资源的访问。朴素形式：在共享存储中放一个 key；key 持有者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis...
  c18_b02.md: ...；缓存主动失效。   ### 18.2 分布式锁  锁协调跨进程对共享资源的访问。朴素形式：在共享存储中放一个 key；key 持有者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis 作者）...
  c18_b02.md: ...端选一个唯一值（随机 nonce）。(b) 客户端尝试在 N 个独立 Redis master（通常 5 个）上设置锁 key。(c) 若多数（3/5）在超时内成功，则获得锁。(d) 客户端持有锁一段时间 TTL；可通过删除 key 释放（需验证...
  c18_b02.md: ...个）上设置锁 key。(c) 若多数（3/5）在超时内成功，则获得锁。(d) 客户端持有锁一段时间 TTL；可通过删除 key 释放（需验证 value 与 nonce 匹配）。  意图：容忍少数 Redis master 故障；在集群上提供互斥...
  c18_b03.md: ...代价：受保护资源必须实现 fencing-token 逻辑。并非所有系统原生支持；例如 Redis 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。...
  c18_b05.md: ...存特有原因：  (a) **缓存故障转移**：Redis Sentinel 提升副本；新 master 可能尚未复制锁 key；另一个客户端获得锁；两个持有者。  (b) **缓存内存驱逐**：内存压力下 Redis 驱逐 key；锁 key 被...
  c18_b05.md: ...可能尚未复制锁 key；另一个客户端获得锁；两个持有者。  (b) **缓存内存驱逐**：内存压力下 Redis 驱逐 key；锁 key 被驱逐；另一个客户端获得锁；两个持有者。  (c) **缓存崩溃**：Redis 崩溃；锁 key 在重启...
  c18_b05.md: ...锁 key；另一个客户端获得锁；两个持有者。  (b) **缓存内存驱逐**：内存压力下 Redis 驱逐 key；锁 key 被驱逐；另一个客户端获得锁；两个持有者。  (c) **缓存崩溃**：Redis 崩溃；锁 key 在重启后丢失（RD...
  c18_b05.md: ...驱逐 key；锁 key 被驱逐；另一个客户端获得锁；两个持有者。  (c) **缓存崩溃**：Redis 崩溃；锁 key 在重启后丢失（RDB 或 AOF 可能没持久化最近变更）；另一个客户端获得锁；两个持有者。  修复：对需要正确性的锁不...
  c18_b06.md: ...Kleppmann 的批评，它为什么不提供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个...
  c18_b06.md: ...n。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯一值作为 key 来获取锁。若多数（3/5）在超时内成功，则获得锁。锁有 TTL；客户端通过删除 key 释放（需验证 value 与...
  c18_b06.md: ...通常 5 个）上设置唯一值作为 key 来获取锁。若多数（3/5）在超时内成功，则获得锁。锁有 TTL；客户端通过删除 key 释放（需验证 value 与 nonce 匹配）。  Kleppmann 的批评：Redlock 不提供正确性，因为：...
  c18_b09.md: ...查 token（很快）。代价可忽略；安全收益巨大。  **Q3.** 团队缓存层（Redis Cluster）出现热门 key 过期时的 “thundering herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示...
  c18_b09.md: ...herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示  概率性提前刷新、刷新期间 per-key 锁、请求合并、异常时提供过期数据。  **答案：** 三种缓解策略：  (a) **概率性提前刷新**：每个缓存读者读...
  c18_b09.md: ...增加。到期时，缓存已被某个读者刷新；后续读者看到新条目。Vattani 等人的模式显著减少踩踏。  (b) **Per-key 刷新锁**：缓存未命中时，读者获取该 key 的短期锁。其他并发读者阻塞在锁上；只有一个去数据库取；完成后回填缓存并释...
  c18_b09.md: ...到新条目。Vattani 等人的模式显著减少踩踏。  (b) **Per-key 刷新锁**：缓存未命中时，读者获取该 key 的短期锁。其他并发读者阻塞在锁上；只有一个去数据库取；完成后回填缓存并释放锁。其他读者解除阻塞后从缓存读取。  (c)...
  c18_b09.md: ...；完成后回填缓存并释放锁。其他读者解除阻塞后从缓存读取。  (c) **请求合并**：在应用层（或缓存客户端库），同一 key 的并发请求去重。只有一条真实 cache-miss 请求去数据库；其他等待结果。  (d) **异常时提供过期数据**...
  c18_b09.md: ...约但保持可用性。  (e) **后台刷新**：后台任务在热门缓存条目过期前刷新它们。缓存始终温热；缓存未命中只发生在冷 key（不会发生踩踏）。  对高流量系统，常见组合 (a) + (b)：概率性提前刷新处理常见情况；per-key 锁处理最坏...
  c18_b09.md: ...只发生在冷 key（不会发生踩踏）。  对高流量系统，常见组合 (a) + (b)：概率性提前刷新处理常见情况；per-key 锁处理最坏情况。(d) 处理数据库中断情况。  更深观察：惊群是缓存被要求做太多的征兆。若单个缓存 key 热到过期会...
  c18_b09.md: ...况；per-key 锁处理最坏情况。(d) 处理数据库中断情况。  更深观察：惊群是缓存被要求做太多的征兆。若单个缓存 key 热到过期会引发踩踏，底层数据可能应放在更快 source（应用内存、Hazelcast 等分布式内存、CDN）。缓存是...
  c19_b01.md: ...基于 Raft（第9章）实现共识。 * 键值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 * Transactions：多键原子操...
  c19_b01.md: ...值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 * Transactions：多键原子操作。  服务发现用法：  * 每个服务实例写...
  c19_b01.md: ...TTL；不续期则自动过期。 * Transactions：多键原子操作。  服务发现用法：  * 每个服务实例写入一个 key（如 `/services/payment/instance-uuid`），包含地址和元数据。 * key 带 leas...
  c19_b01.md: ...实例写入一个 key（如 `/services/payment/instance-uuid`），包含地址和元数据。 * key 带 lease（如 30 秒）；实例每 10 秒续期。 * 若实例未续期（崩溃、分区），key 过期并被删除。 * 客...
  c19_b01.md: ...包含地址和元数据。 * key 带 lease（如 30 秒）；实例每 10 秒续期。 * 若实例未续期（崩溃、分区），key 过期并被删除。 * 客户端 watch `/services/payment/` 前缀；实例变更时收到通知。  模式很...
  c19_b03.md: ...一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务...
  c19_b03.md: ...nsul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更时 watch 触发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务一致性的配置，...
  c19_b03.md: ...后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **重配置竞争**：若服务正在执行操作时配置变...
  c19_b05.md: ...| Raft | ZAB（Paxos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kuber...
  c19_b05.md: ...axos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Ha...
  c19_b05.md: ...ch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _watch_ 相关 key 的变更。配置变更时，消费者收到通知并动态重新配置自己。...
  c19_b05.md: ...| Raft | ZAB（Paxos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kuber...
  c19_b05.md: ...axos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Ha...
  c19_b05.md: ...ch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _watch_ 相关 key 的变更。配置变更时，消费者收到通知并动态重新配置自己。...
  c19_b06.md: ...re flags。配置存储保存如 `feature_flags/new_checkout_flow = true` 的 key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag...
  c19_b09.md: ...之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是 1000 条同时通知。etcd 在小规模...
  c27_b02.md: ...客户端超时。客户端看到失败；服务端已扣款。重试造成重复扣款。  修复：幂等性。每笔支付必须携带 idempotency key。服务端检查：我之前见过这个 key 吗？若见过，返回之前计算的结果。重试安全。       def submit_pa...
  c27_b02.md: ...扣款。重试造成重复扣款。  修复：幂等性。每笔支付必须携带 idempotency key。服务端检查：我之前见过这个 key 吗？若见过，返回之前计算的结果。重试安全。       def submit_payment(payment):...
  c27_b02.md: ...t = retry_with_idempotency(payment_service.charge, payment, key=payment.idempotency_key)         # ... 无论发生多少次重试都安全   LLM 产...
  c27_b03.md: ...from_replicas):         return sorted(events_from_replicas, key=lambda e: e.timestamp)   谎言：时间戳跨机器可比较。代码用时间戳排序事件，不考虑时钟偏移或因果...
  c27_b03.md: ...re 传播的 HLC 时间戳）         return sorted(events_from_replicas, key=lambda e: (e.hlc.l, e.hlc.c, e.replica_id))   LLM 产出 wall-c...
  c27_b03.md: ...redis.delete(f"action_lock:{action_id}")   谎言：持有 Redis key 意味着唯一性。代码用 Redlock 风格锁保护正确性关键操作。  现实：没有 fencing token 的 Red...
  c27_b05.md: ...式代码（或压力下自写代码）时，检查清单：  (a) **幂等性**：每个网络调用必须幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联。  (c) **带预...
  c27_b07.md: ...做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false posi...
  c27_b10.md: ...、可观测性。  **答案：** 十项检查：  (a) **幂等性**：每个网络调用应幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联上游。  (c) **...
  c27_b12.md: ...1（RPC 语义）**：`bank_service.transfer(payment)` 没有 idempotency key。若超时重试，bank 可能转账两次。    2. **谎言 2（网络可靠）**：任何网络调用（bank、db、noti...
  c27_b12.md: ...码包含 Thread C 全部六个谎言。具体：bank_service.transfer 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）...
  c28_b02.md: ...g token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (g) **队列**：每 region 一个 Kafka。异步工作流（通知、下游管道）。  (h) **商户协作*...
  c28_b04.md: ...Telemetry W3C `traceparent`）全程传播。  (4) API 服务通过 Redis 检查幂等性 key（第18章缓存模式）。若 key 存在，返回之前计算的结果（谎言 1 防御）。2 ms。  (5) API 服务开始 S...
  c28_b04.md: ...raceparent`）全程传播。  (4) API 服务通过 Redis 检查幂等性 key（第18章缓存模式）。若 key 存在，返回之前计算的结果（谎言 1 防御）。2 ms。  (5) API 服务开始 Stratus DB 事务。该事务...
  c30_b06.md: ...pens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **Linearizable（线性化）**：每次操...
  c30_b07.md: ...--- | --- | --- | --- | | 1 | RPC 有 TCP 式语义 | Ch.4, 5 | 幂等性 key；显式重试语义 | | 2 | 网络可靠 | Ch.2, 21 | 超时；重试预算；熔断器 | | 3 | 机器时钟说真...
  c30_b14.md: ...tries + commit     │   ├── kv_state_machine.py — replicated key-value store on top     │   └── tests/            — split-br...
  c30_b16.md: ..."。 | Ch.3 | | 8 | "RPC 有 TCP 式语义。" | 即使请求已被处理，响应仍可能丢失。需要幂等性 key。 | Ch.4, Ch.27 | | 9 | "W + R > N 给出线性化。" | 它给出 _交集_，更弱。并发写...
  c30_b19.md: ...参考章节   |---|---|---   同一操作两个 span 显示不同效果（重复扣款、重复写入） | 缺少幂等性 key；重试命中无保护路径 | Ch.4（RPC 语义），Ch.18（fencing token），Ch.27 §27.2...

=== draft (75 occurrences) ===
  c26_b03.md: ...26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a...
  c26_b03.md: ...n、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B...
  c26_b03.md: ...机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并...
  c26_b03.md: ...对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个...
  c26_b03.md: ...间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型...
  c26_b03.md: ...rify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。...
  c26_b03.md: ...ken 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型...
  c26_b03.md: ...分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节...
  c26_b03.md: ...一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）...
  c26_b03.md: ...Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify...
  c26_b03.md: ...verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：...
  c26_b03.md: ...分布式收益：  (a) Draft model 节点可用更便宜硬件（小 GPU，某些情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 veri...
  c26_b03.md: ...情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verif...
  c26_b03.md: ...服务多个请求的 draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 roun...
  c26_b03.md: ...(c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽...
  c26_b03.md: ...per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative...
  c26_b03.md: ...26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a...
  c26_b03.md: ...n、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B...
  c26_b03.md: ...机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并...
  c26_b03.md: ...对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个...
  c26_b03.md: ...间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型...
  c26_b03.md: ...rify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。...
  c26_b03.md: ...ken 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型...
  c26_b03.md: ...分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节...
  c26_b03.md: ...一张 GPU。现代变体把它们分到不同机器：  (a) **Draft 在专用节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）...
  c26_b03.md: ...Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify...
  c26_b03.md: ...verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K 个候选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：...
  c26_b03.md: ...分布式收益：  (a) Draft model 节点可用更便宜硬件（小 GPU，某些情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 veri...
  c26_b03.md: ...情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verif...
  c26_b03.md: ...服务多个请求的 draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 roun...
  c26_b03.md: ...(c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽...
  c26_b03.md: ...per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative...
  c26_b04.md: ...收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling...
  c26_b05.md: ...件改动）；收益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K...
  c26_b05.md: ...erify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选...
  c26_b05.md: ...型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型毫秒级。  (b) Verify model 并行处理 K 个候选（一个 verify 步一起处理 K 个...
  c26_b05.md: ...n 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，ver...
  c26_b05.md: ...model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify mo...
  c26_b05.md: ...raft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b05.md: ...件改动）；收益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K...
  c26_b05.md: ...erify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选...
  c26_b05.md: ...型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型毫秒级。  (b) Verify model 并行处理 K 个候选（一个 verify 步一起处理 K 个...
  c26_b05.md: ...n 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，ver...
  c26_b05.md: ...model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify mo...
  c26_b05.md: ...raft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b06.md: ...现在已是生产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示...
  c26_b06.md: ...del，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 specu...
  c26_b06.md: ...请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬...
  c26_b06.md: ...tive decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。...
  c26_b06.md: ...(a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求...
  c26_b06.md: ...ion（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms...
  c26_b06.md: ...会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verificat...
  c26_b06.md: ...现在已是生产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示...
  c26_b06.md: ...del，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 specu...
  c26_b06.md: ...请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬...
  c26_b06.md: ...tive decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。...
  c26_b06.md: ...(a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求...
  c26_b06.md: ...ion（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms...
  c26_b06.md: ...会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verificat...
  c26_b09.md: ...更重要，但得到的工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，...
  c26_b09.md: ...peculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located specu...
  c26_b09.md: ...verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculativ...
  c26_b09.md: ...* 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用...
  c26_b09.md: ...。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 *...
  c26_b09.md: ...用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳...
  c26_b09.md: ...o-located**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；...
  c26_b09.md: ...更重要，但得到的工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，...
  c26_b09.md: ...peculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located specu...
  c26_b09.md: ...verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculativ...
  c26_b09.md: ...* 更低 per-token 延迟（无网络跳数）。 * 运维更简单（无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用...
  c26_b09.md: ...。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 *...
  c26_b09.md: ...用更便宜硬件（小 GPU 或 CPU）。 * Verify 节点利用率更高（始终处理 verification）。 * Draft 和 verify 容量独立扩缩。 * 更复杂（网络协议、同步、故障处理）。 * 更高 per-token 延迟（网络跳...
  c26_b09.md: ...o-located**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；...
  c26_b11.md: ...短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify...
  c30_b06.md: ...；存在写-写冲突（可能写偏）。 * **Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer rev...

=== wall (74 occurrences) ===
  c01_b09.md: ...本应做到这一点，但窗口太短；第二个请求刚好落在窗口外。  对于时间谎言，使用混合逻辑时钟（HLC）时间戳（第3章）而非 wall-clock 时间戳，会让两个协调者得到相同的可比较顺序。任一协调者都会看到另一方的提交在 HLC 时间中先发生，即使两...
  c01_b10.md: ...台机器的时钟跳跃。被虚拟机监控器暂停 30 秒（快照、实时迁移、某些运行时的 GC STW 暂停）的 VM 恢复后，其 wall clock 会跳跃 30 秒。从外部看，该节点好像时间旅行到了未来。在节点内部，任何跨暂停前后两次时钟读取来推理持续时...
  c01_b12.md: ...ELECT 一致”，会惊讶地发现 Postgres 中的 `now()` 返回的是 _事务开始时间_ ，而非调用时刻的 wall-clock 时间。同一事务中的两条语句看到同一个 `now()`。这是文档里写明的，但违背朴素的单机直觉。...
  c02_b03.md: ...，且每个操作看起来都在其调用与响应之间的某个瞬间原子地生效。  “满足真实时间顺序”这一点让它很强。如果客户端 A 在 wall-clock 时间 t_1 完成一次写入，客户端 B 在 wall-clock 时间 t_2 > t_1 开始一次读取（...
  c02_b03.md: ...满足真实时间顺序”这一点让它很强。如果客户端 A 在 wall-clock 时间 t_1 完成一次写入，客户端 B 在 wall-clock 时间 t_2 > t_1 开始一次读取（两个客户端可以在不同机器上），那么 B 的读取必须看到 A 的写入...
  c03_b01.md: ...台机器上的持续时间，应使用独立的 _单调_ 时钟。单调时钟永远不会回退（不被 NTP 调整），也不会跳跃前进。它们不与 wall-clock 时间绑定；其值只有在作差时才有意义。大多数现代语言同时暴露两者：Python 中 `time.time()...
  c03_b01.md: ...ck 时间绑定；其值只有在作差时才有意义。大多数现代语言同时暴露两者：Python 中 `time.time()` 是 wall，`time.monotonic()` 是单调；Go 中 `time.Now()` 内部同时携带两者。  规则：任何测量...
  c03_b01.md: ...` 内部同时携带两者。  规则：任何测量“X 花了多久”的代码都应使用单调时间。任何记录“X 何时发生”的代码必须使用 wall 时间，但要认识其局限。...
  c03_b02.md: ...为 _并发_ ，记作 a \parallel b。并发在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另...
  c03_b02.md: ...r 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100...
  c03_b02.md: ...happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-cl...
  c03_b02.md: ...。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有...
  c03_b02.md: ...ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳——工作流 1：逻辑时钟实现  > **工作流...
  c03_b04.md: ...#### 常见误解  常见混淆：向量时钟“告诉你现在几点”。不是。向量时钟是推理 _顺序_ 的结构；与 wall-clock 时间无关。向量 [2,2,2] 不表示“自某事以来 2 秒”，它表示“我已观察到三个进程各发生 2 个事件...
  c03_b04.md: ...。向量 [2,2,2] 不表示“自某事以来 2 秒”，它表示“我已观察到三个进程各发生 2 个事件”。把向量时钟比较与 wall-clock 比较混在同一逻辑中会产生 nonsense。...
  c03_b05.md: ...### 3.5 混合逻辑时钟（HLC）  向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone...
  c03_b05.md: ...ni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一个逻辑计数器组件为同一 wall-clock 瞬间内的事件排序。  进程 p 上...
  c03_b05.md: ...014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一个逻辑计数器组件为同一 wall-clock 瞬间内的事件排序。  进程 p 上的 HLC 是一个二元组 (l, c)，其中 l 是 wall-cloc...
  c03_b05.md: ...件为同一 wall-clock 瞬间内的事件排序。  进程 p 上的 HLC 是一个二元组 (l, c)，其中 l 是 wall-clock 时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Ma...
  c03_b05.md: ...s、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, t)；若 l' = l_p 则 c_p 加 1，否则...
  c03_b05.md: ...把发送作为本地事件处理。 * **接收** 带 HLC (l_m, c_m) 的消息在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, l_m, t)；然后更新计数器：     * 若 l...
  c03_b05.md: ...综合了关于当下时刻的三条信息：p 的 HLC 对此的看法 (l_p)、发送者的 HLC 对此的看法 (l_m)、p 的 wall clock 对此的看法 (t)。新的 l' 必须是最大值，以保证 HLC 单调且尊重 happens-before；它...
  c03_b06.md: ...进；p 采用发送者的 l_m 并扩展发送者计数器，以记录该事件发生在发送之后。c' = c_m + 1。 * **只有 wall clock 贡献了 (l' = t，严格大于 l_p 和 l_m 两者)** ：物理时间已前进过两个先前 HLC；计数...
  c03_b06.md: ...-before** ：a \to b 蕴含 HLC(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制...
  c03_b06.md: ...< HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \epsilon 是进程间最大时钟偏移。 * **...
  c03_b06.md: ...对，不像向量时钟随 N 扩展。 * **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  Cockroach...
  c03_b06.md: ...* **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  CockroachDB、YugabyteDB、Mo...
  c03_b06.md: ...数据中心没有 GPS 原子钟的情况下获得 TrueTime 类行为？”   #### 工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after...
  c03_b06.md: ...工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 | 100 | (100, 0) 2...
  c03_b06.md: ...00, 2) 5 | P_3 | 105，本地事件 | (105, 0)  P_2 的 HLC 即使本地时钟落后也贴近 wall 时间，因为传入消息把它拉向前。这个性质让 HLC 对小时钟偏移具有鲁棒性：时钟慢的节点不会因自己的慢时钟而被“排序更晚...
  c03_b06.md: ...进；p 采用发送者的 l_m 并扩展发送者计数器，以记录该事件发生在发送之后。c' = c_m + 1。 * **只有 wall clock 贡献了 (l' = t，严格大于 l_p 和 l_m 两者)** ：物理时间已前进过两个先前 HLC；计数...
  c03_b06.md: ...-before** ：a \to b 蕴含 HLC(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制...
  c03_b06.md: ...< HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \epsilon 是进程间最大时钟偏移。 * **...
  c03_b06.md: ...对，不像向量时钟随 N 扩展。 * **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  Cockroach...
  c03_b06.md: ...* **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  CockroachDB、YugabyteDB、Mo...
  c03_b06.md: ...数据中心没有 GPS 原子钟的情况下获得 TrueTime 类行为？”   #### 工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after...
  c03_b06.md: ...工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 | 100 | (100, 0) 2...
  c03_b06.md: ...00, 2) 5 | P_3 | 105，本地事件 | (105, 0)  P_2 的 HLC 即使本地时钟落后也贴近 wall 时间，因为传入消息把它拉向前。这个性质让 HLC 对小时钟偏移具有鲁棒性：时钟慢的节点不会因自己的慢时钟而被“排序更晚...
  c03_b07.md: ...将时间暴露为一个 _区间_ ：每次调用 `TT.now()` 返回一对 `[earliest, latest]`，真实 wall-clock 时间保证落在该区间内。  Spanner 在每个数据中心部署 GPS 接收器和原子钟，加上一层监控并报告不...
  c03_b07.md: ...”）。   2. **等待** 直到 TT.now().earliest > s。这等待掉不确定性区间；一旦成立，真实 wall-clock 时间可证明已越过 s，因此不会有其他事务随后分配更小时间戳并声称更早提交。   3. 应用事务。  等待通...
  c03_b08.md: ...LLM 原版的 `sorted(messages, key=lambda m: m.timestamp)` 错在把 wall-clock 时间当作 happens-before 的代理。三种修复任一都能让函数正确。选择哪种取决于你的运营容忍度和...
  c03_b09.md: ...应是 P_2 上由 P_1 发送的消息接收，而 y 的写入又发生在同一进程该接收之后。  并发示例：进程 P_1 在某 wall-clock 时刻写入 x = 5，同时进程 P_2 写入 y = 10，两者未互相通信。没有 happens-befo...
  c03_b10.md: ...**Q4.** 进程 P 的 HLC 是 (100, 3)。P 的 wall-clock 读数为 102。P 收到一条 HLC 为 (105, 1) 的消息。接收后 P 的 HLC 是多少？  提...
  c03_b10.md: ...的消息。接收后 P 的 HLC 是多少？  提示  HLC 接收规则（§3.5）：l' 取（旧 l_p、消息 l_m、wall now t）的最大值。计数器根据三者中哪一个贡献了 l' 来更新。  **答案：** 应用 §3.5 的四种情况。...
  c03_b10.md: ...l_m”情况：c' = c_m + 1 = 1 + 1 = 2。  新 HLC 为 (105, 2)。  HLC 贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接...
  c03_b10.md: ...1 + 1 = 2。  新 HLC 为 (105, 2)。  HLC 贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接收 HLC (105, 2) 按字典序严格...
  c03_b11.md: ...ne 复活了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cassandra 的 LWW 依赖每次写入的 wall-clock 时间戳。如果删除被盖戳早于后续写入的时间戳，会发生什么？  **答案：** Cassandra 的默认冲突...
  c03_b11.md: ...**答案：** Cassandra 的默认冲突解决是 last-write-wins（LWW），基于协调者节点分配的 wall-clock 时间戳。如果删除由时钟落后的协调者处理，它被盖戳早于另一个时钟超前的协调者上的后续写入。当删除和写入到达副...
  c03_b11.md: ...数量级。 * 迁移到基于 HLC 的 MVCC 系统（CockroachDB、YugabyteDB），其冲突解决不依赖 wall clock。  对这个具体工作负载（三数据中心每秒 100k 写入），每次写入用 LWT 可能太贵；实际答案是要么严格...
  c03_b11.md: ...100k 写入），每次写入用 LWT 可能太贵；实际答案是要么严格 NTP 纪律加监控症状，要么迁移到写入路径不依赖 wall clock 正确性的系统。  **Q2.** 一位新工程师为 10,000 节点微服务系统实现向量时钟。向量增长到每个...
  c03_b12.md: ...写入的时间戳 t_w 满足 t_s < t_w < t_s + \text{max\_skew}，系统无法判断该写入在 wall-clock 时间中是否发生在事务开始之前，于是 _重启_ 事务，用新时间戳推到观察到的写入之后。这就是“uncerta...
  c03_b14.md: ...A 的时钟比 B 快 50 ms，合并日志显示 B 在 A 收到之前就开始处理。取证追踪被破坏。  (b) **同毫秒 wall-clock 碰撞** ：多台服务器每秒写数千行日志，可能产生相同毫秒分辨率的时间戳。按时间戳排序会在每毫秒内任意排序，...
  c03_b15.md: ...已经在传播 trace context（用于分布式追踪，第20章），它们已经携带了足以跨服务正确排序日志事件的信息。用 wall clock 的日志系统把这些取证证据白白浪费掉。   ### 本章总结  分布式系统讲的第一个谎言是时间。NTP 只给...
  c03_b15.md: ...。   ### 本章总结  分布式系统讲的第一个谎言是时间。NTP 只给有界 _预期_ 漂移，不给有界 _保证_ 漂移。wall-clock 时间戳是事件偏序的有损代理。用跨机器时间戳比较来决定“哪个先发生”的代码是在对一个虚构概念做推理。  La...
  c03_b15.md: ...efore 但不能检测并发。向量时钟为每个进程每个事件分配一个整数，能检测并发，但扩展性差。混合逻辑时钟（HLC）结合 wall-clock 风格与逻辑计数器排序，保持恒定大小，是 CockroachDB、YugabyteDB、MongoDB 等...
  c05_b03.md: ...解决，因为两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **Sibling values*...
  c13_b13.md: ...器和原子钟；软件监控并报告时钟不确定性。`TT.now()` 返回区间 `[earliest, latest]`，真实 wall-clock 时间 provably 位于该区间内。典型不确定性：1-7 毫秒。  Spanner 通过 _commit...
  c13_b13.md: ...T.now().earliest > commit_timestamp`。通常约 ~7 ms。   3. 等待后，真实 wall-clock 时间 provably 已过 commit_timestamp。   4. 提交释放；锁释放。  等待确保...
  c16_b07.md: ...**答案：** LWW-Register 使用 wall-clock（或 HLC）时间戳决定哪个值胜出。若两个副本并发写入，时钟 _更快_ 的副本胜出（无论哪个写逻辑上更晚），...
  c16_b07.md: ...发写。应用看到所有存活值（作为 siblings）并决定。不丢数据；冲突解决在应用层。  (b) **用 HLC 代替 wall clock** ：HLC（第3章）可减少但不能消除偏移问题。HLC 尊重 happens-before；因果有序的两写...
  c20_b03.md: ...ce 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wall-clock 时长，但不拆解接收服务内部做了什么（哪些函数慢、哪些查询慢）。为此需要服务内性能分析。  (b) **资源...
  c27_b03.md: ...，不考虑时钟偏移或因果性。  现实：跨机器时钟最好情况下可能差数十毫秒，最坏情况差数秒。时间戳非常接近的事件无法可靠用 wall clock 排序。应由因果性排序的事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-be...
  c27_b03.md: ...s, key=lambda e: (e.hlc.l, e.hlc.c, e.replica_id))   LLM 产出 wall-clock 版本因为它是最简单的排序表达式。HLC 版本需要理解时间的谎言。   ### 27.5 谎言 4：朴素分布...
  c27_b09.md: ...etry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall-clock 时间戳做排序。    4. **朴素分布式锁有效**：Redlock 风格代码假设锁持有者唯一；缺少 fe...
  c27_b09.md: ...起级联。  （谎言 3）`sorted(events, key=lambda e: e.timestamp)`。跨机器 wall-clock 排序；时钟偏移破坏排序。  （谎言 4）`if redis.set(key, "1", nx=True,...
  c27_b11.md: ...谎言。构造一个包含它们的合理代码片段。  提示  一个跨 region 支付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM...
  c28_b10.md: ...尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长；HLC 计数无界溢出 | | 2 |...
  c28_b14.md: .../EL（分区时一致；正常操作时低延迟，因异步复制）。  _三个谎言姿态_：  * **时间**：Postgres 使用 wall-clock 时间戳；事务按事务开始时间排序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响...
  c28_b14.md: ...三个谎言姿态_：  * **时间**：Postgres 使用 wall-clock 时间戳；事务按事务开始时间排序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供...
  c30_b06.md: ...**：进程通过健康检查但实际从应用视角已损坏的故障。 * **HLC（混合逻辑时钟）**：二元组 (l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cac...
  c30_b06.md: ...*：二元组 (l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 t...
  c30_b16.md: ...n 一致性才是正确问题。 | Ch.2 | | 6 | "向量时钟告诉你现在几点。" | 向量时钟推理 _顺序_，不是 wall-clock 时间。值不是秒。 | Ch.3 | | 7 | "我运行 NTP 就有 TrueTime。" | True...
  c30_b19.md: ...Ch.4（RPC 语义），Ch.18（fencing token），Ch.27 §27.2   跨服务 span 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 wall-clo...
  c30_b19.md: ...n 的 wall-clock 顺序与因果预期矛盾（B 在 A 之后发生，尽管 B 是 A 的回复） | 跨机器排序使用 wall-clock；时钟偏移 | Ch.3（HLC），Ch.20 §20.2（context 传播）   一个慢 span 主...

=== consul (73 occurrences) ===
  c09_b05.md: ...成熟** ：etcd 的 Raft 实现（Go）被广泛使用；多种语言实现存在。 * **运维工具** ：etcdctl、consul members、CockroachDB 的 raft visualizer。协议暴露足够状态供观察。 * **社区知识...
  c09_b05.md: ...观察。 * **社区知识** ：一代工程师把 Raft 当作第一个共识协议学习；知识库广阔。  生产用户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、N...
  c09_b05.md: ...成熟** ：etcd 的 Raft 实现（Go）被广泛使用；多种语言实现存在。 * **运维工具** ：etcdctl、consul members、CockroachDB 的 raft visualizer。协议暴露足够状态供观察。 * **社区知识...
  c09_b05.md: ...观察。 * **社区知识** ：一代工程师把 Raft 当作第一个共识协议学习；知识库广阔。  生产用户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、N...
  c18_b03.md: ...的分布式锁库用 Raft（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种结构。...
  c18_b05.md: ...或 AOF 可能没持久化最近变更）；另一个客户端获得锁；两个持有者。  修复：对需要正确性的锁不要用缓存。用 etcd、Consul 或 ZooKeeper 做锁；在受保护资源上执行 fencing token。性能代价小（锁获取罕见）；正确性收益大。...
  c18_b07.md: ...款、发邮件、发货）。重复动作造成真实世界的重复后果。  纪律：选择锁前先分类用例。正确性用例使用基于共识的锁（etcd、Consul、ZooKeeper）加 fencing token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用...
  c18_b07.md: ...“当前持有者” 概念。没有锁服务，每个操作都要求资源与所有潜在持有者协商。  组合是正确生产模式。实践中：etcd 或 Consul 提供锁服务（基于 Raft）；应用在存储层实现 fencing-token 检查（或依赖支持它的存储系统，如带版本列检...
  c18_b10.md: ...oken（作为选项）。正确性用例应用使用它。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etcd、Consul、ZooKeeper 都支持 fencing token 等价物（revision 号、sequence 号、czxid...
  c18_b11.md: ...者）。Redlock 式缓存锁对效率可接受；对正确性，在进程暂停和时钟漂移下失败。正确原语是共识支持的锁服务（etcd、Consul、ZooKeeper）加受保护资源上强制执行的 fencing token。  第1章 Crescent 修复结合基于...
  c18_b11.md: ...g token。代价小；正确性收益大。  第19章讲服务发现与配置：etcd 作为现代默认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data + watch stream 模式。这些是让其余分布式基础设...
  c19_b01.md: ...每天都反复出现的问题：服务 A 如何知道服务 B 的实例在哪里？  这就是服务发现问题。2026 年的答案是 etcd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch str...
  c19_b01.md: ...artbeat）时从集合移除。  发现服务本身是分布式系统；它有自己的共识、复制和失败模式。2026 年选择：etcd、Consul 或基于 DNS 的发现（云厂商服务发现）。   ### 19.2 etcd  etcd 是 Kubernetes 控制...
  c19_b02.md: ...afka 从 ZooKeeper 迁移到 KRaft（Kafka Raft）是近期大趋势的例子。   ### 19.3 Consul  HashiCorp 的 Consul 是另一个流行的服务发现系统。与 etcd 的关键差异：  (a) **原生多...
  c19_b02.md: ...KRaft（Kafka Raft）是近期大趋势的例子。   ### 19.3 Consul  HashiCorp 的 Consul 是另一个流行的服务发现系统。与 etcd 的关键差异：  (a) **原生多 DC**：Consul 内置数据中心感知...
  c19_b02.md: ...hiCorp 的 Consul 是另一个流行的服务发现系统。与 etcd 的关键差异：  (a) **原生多 DC**：Consul 内置数据中心感知；跨 DC 服务发现是一等特性。  (b) **服务网格**：Consul 包含服务网格功能（服务间...
  c19_b02.md: ...a) **原生多 DC**：Consul 内置数据中心感知；跨 DC 服务发现是一等特性。  (b) **服务网格**：Consul 包含服务网格功能（服务间 mTLS、流量策略、intentions）。  (c) **基于 DNS 的发现接口**：服...
  c19_b02.md: ...s）。  (c) **基于 DNS 的发现接口**：服务可通过 DNS 查询发现（如 `payment.service.consul`），无需客户端库即可与任何语言配合。  (d) **内置健康检查**：Consul 轮询服务实例以验证健康（不依赖实例...
  c19_b02.md: ...`payment.service.consul`），无需客户端库即可与任何语言配合。  (d) **内置健康检查**：Consul 轮询服务实例以验证健康（不依赖实例侧 heartbeat）。  (e) **多租户**：Consul 企业版支持命名空...
  c19_b02.md: ...内置健康检查**：Consul 轮询服务实例以验证健康（不依赖实例侧 heartbeat）。  (e) **多租户**：Consul 企业版支持命名空间。  Consul 在异构环境（混合云、本地、基于 VM 的部署）中流行，这些场景下仅 Kubern...
  c19_b02.md: ...务实例以验证健康（不依赖实例侧 heartbeat）。  (e) **多租户**：Consul 企业版支持命名空间。  Consul 在异构环境（混合云、本地、基于 VM 的部署）中流行，这些场景下仅 Kubernetes 发现不够。对纯 Kubern...
  c19_b03.md: ...数据库、有状态服务），DNS 通常足够。对成员快速变化的服务（自动扩缩无状态服务），基于 watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发...
  c19_b03.md: ...模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。...
  c19_b04.md: ...。Raft 支撑的 etcd 集群提供带 revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支持找到 US-East 的服务。Consu...
  c19_b04.md: ...fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Clo...
  c19_b04.md: ...onsul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户...
  c19_b05.md: ...务查询注册表找目标服务实例。(d) 实例未 heartbeat（崩溃、分区）时从注册表移除。  现代发现服务：etcd、Consul、ZooKeeper、云厂商基于 DNS 的服务、Kubernetes API server。  发现服务本身是分布式系...
  c19_b05.md: ...** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _watch_ 相关 key 的变更。配置变更时，消费者收到通知并动态重新配置自己。...
  c19_b08.md: ...**Q2.** 一个团队有 47 个微服务使用 Consul 做发现。他们注意到 Consul 集群故障转移（3 副本 Consul，一个节点故障）期间，服务间流量中断 30 秒。...
  c19_b08.md: ...**Q2.** 一个团队有 47 个微服务使用 Consul 做发现。他们注意到 Consul 集群故障转移（3 副本 Consul，一个节点故障）期间，服务间流量中断 30 秒。诊断并给出修复方案。  提示  C...
  c19_b08.md: ...**Q2.** 一个团队有 47 个微服务使用 Consul 做发现。他们注意到 Consul 集群故障转移（3 副本 Consul，一个节点故障）期间，服务间流量中断 30 秒。诊断并给出修复方案。  提示  Consul 基于 gossip 的健康...
  c19_b08.md: ...l 集群故障转移（3 副本 Consul，一个节点故障）期间，服务间流量中断 30 秒。诊断并给出修复方案。  提示  Consul 基于 gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：**...
  c19_b08.md: ...gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：** 诊断：Consul 故障转移期间，集群重新选举 Raft leader。选举期间新写被阻塞（通常 5-15 秒）。此窗口查询 Consul...
  c19_b08.md: ...Consul 故障转移期间，集群重新选举 Raft leader。选举期间新写被阻塞（通常 5-15 秒）。此窗口查询 Consul 的客户端可能收到过期或错误响应。  若客户端配置为 “fail open”（Consul 不可用时使用 last-kn...
  c19_b08.md: ...sul 不可达时返回错误），每次服务调用在选举窗口内失败。  修复：  (a) **配置客户端使用 stale 读**：Consul 支持从任意副本 stale 读（`?stale=true`）；leader 不可达时客户端回退到 stale 读。权衡...
  c19_b08.md: ...期；对服务发现可接受。  (b) **客户端缓存带宽限期**：客户端本地缓存发现的服务注册表一段宽限期（如 60 秒）。Consul 故障转移期间，客户端使用缓存注册表。风险：故障转移期间死亡的实例可能仍被返回给调用者；应用看到连接拒绝错误并回退到其他...
  c19_b08.md: ...存注册表。风险：故障转移期间死亡的实例可能仍被返回给调用者；应用看到连接拒绝错误并回退到其他实例。  (c) **提高 Consul 冗余**：运行 5 节点 Consul 集群而非 3 节点；更大集群对故障容忍更高，重选更快。  (d) **使用带...
  c19_b08.md: ...能仍被返回给调用者；应用看到连接拒绝错误并回退到其他实例。  (c) **提高 Consul 冗余**：运行 5 节点 Consul 集群而非 3 节点；更大集群对故障容忍更高，重选更快。  (d) **使用带 sidecar 缓存的服务网格**：Is...
  c19_b08.md: ...格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidecar 异步从 Consul 刷新。  (e) **迁移到集群内发现（Kubernetes serv...
  c19_b08.md: ...r 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidecar 异步从 Consul 刷新。  (e) **迁移到集群内发现（Kubernetes service）**：Kubernetes 用 etcd...
  c19_b08.md: ...ubernetes API 中断影响新 service 定义，但不影响已有。对 Kubernetes 内服务，这通常比 Consul 更弹性。  推荐：短期缓解用 (a) + (b)；长期架构改进用 (d) 或 (e)。30 秒故障转移导致的中断是运维...
  c19_b10.md: ...基于 VM）部署无法使用 Kubernetes Services；需要不同机制。  EC2 的正确模式：  (a) **Consul**：HashiCorp 的 Consul 为 VM 和混合部署设计。EC2 实例启动时通过 Consul agent...
  c19_b10.md: ...es Services；需要不同机制。  EC2 的正确模式：  (a) **Consul**：HashiCorp 的 Consul 为 VM 和混合部署设计。EC2 实例启动时通过 Consul agent 注册；客户端通过 DNS 或 HTTP A...
  c19_b10.md: ...(a) **Consul**：HashiCorp 的 Consul 为 VM 和混合部署设计。EC2 实例启动时通过 Consul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AW...
  c19_b10.md: ...(c) **直接使用 etcd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Jav...
  c19_b10.md: ...心化服务发现。Netflix 使用；2026 年较不常见，但某些遗留系统仍在用。  对 2026 年的新 EC2 部署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS...
  c19_b11.md: ...netes？VM？混合？云？本地？你们团队目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健...
  c19_b11.md: ...项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的注册表。2026 年标准解是 etcd、Consul 和基于 DNS 的发现（AWS Cloud Map 等云厂商服务）。etcd 主导云原生细分市场；Consul 处理...
  c19_b11.md: ...tcd、Consul 和基于 DNS 的发现（AWS Cloud Map 等云厂商服务）。etcd 主导云原生细分市场；Consul 处理 VM 和多 DC 部署；DNS 对稳定服务足够。  etcd 在新系统中基本替代 ZooKeeper，因为 Ku...
  c19_b11.md: ...treams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul 中，watch 变更，动态重新配置）是 2026 年运行时配置的默认，不应要求服务重启。  服务网格（Istio、Li...
  c19_b11.md: ...rescent 的发现使用四个分层系统：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每个处理自己的领域；跨领域交互明确。  第20章开始第七...
  c23_b04.md: ...t 节点下线。集群的 Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20 UTC：系统稳定。EU-West 发起交易的 p99...
  c23_b04.md: ...统回到基线。  **复盘**：  * 稳态：维持（延迟在 SLO 内，成功率高于阈值）。 * 观察：     * 基于 Consul 的服务 mesh 故障转移耗时 30 秒；此窗口内 EU-West 用户看到超时。行动项：调整 Consul 健康检查...
  c23_b04.md: ...* 基于 Consul 的服务 mesh 故障转移耗时 30 秒；此窗口内 EU-West 用户看到超时。行动项：调整 Consul 健康检查频率。     * 客服面板没有清晰指示 “EU-West 区域下线”。行动项：在面板添加区域健康。...
  c23_b04.md: ...队预期大。     * 发现一个真实 bug（重复扣款），生产流量在真实故障下可能遇到。  **行动项**：  * 将 Consul 健康检查频率调整到目标 10 秒检测。 * 在客服面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 *...
  c23_b13.md: ...el 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bug。Game day 成功，因为它在客户之前发现问题。  第24章开启第八部...
  c28_b02.md: ...、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster...
  c28_b03.md: ...TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br...
  c28_b03.md: ...etle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analy...
  c28_b03.md: ...TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB...
  c28_b03.md: ...APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6...
  c28_b03.md: ...TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br...
  c28_b03.md: ...etle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analy...
  c28_b03.md: ...TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB...
  c28_b03.md: ...APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6...
  c28_b05.md: ...etcd + fencing token | E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Consul + Cloudflare Workers | E.2（Roblox Consul）、E.7（Fastly 配置 rol...
  c28_b05.md: ...主） | | 19 | 服务发现 | Consul + Cloudflare Workers | E.2（Roblox Consul）、E.7（Fastly 配置 rollout） | | 20 | 追踪 | OpenTelemetry 端到端 | E...
  c30_b08.md: ...### E.2 — Roblox 多日故障，2021-10-28 至 2021-11-02  * **触发**：部署新 Consul 版本，带有微妙行为变化（更长 heartbeat 给 Consul 集群带来负载）。 * **放大**：集群的 Has...
  c30_b08.md: ...2021-11-02  * **触发**：部署新 Consul 版本，带有微妙行为变化（更长 heartbeat 给 Consul 集群带来负载）。 * **放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 s...
  c30_b08.md: ...妙行为变化（更长 heartbeat 给 Consul 集群带来负载）。 * **放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积压又带来更多...
  c30_b08.md: ...streaming 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gossip 协议的 BoltDB 状态在该故障模式下无边界增长。 * **时间线**：73 小时客户可见中断（Rob...
  c30_b08.md: ...，streaming 子系统从干净状态重建。 * **章节映射防御**：     * Ch.19（服务发现）：单一依赖 Consul 且无优雅降级；缺少纵深防御。     * Ch.22（亚稳态故障）：自我维持的损坏状态需要外部干预才能打破。...
  c30_b08.md: ...ng 子系统故障传播到依赖服务。     * Ch.23（混沌工程）：该具体故障模式（高 heartbeat 负载下的 Consul）部署前未做 game-day 测试。   #### E.3 — Cloudflare 邮件路由故障，2024-04...

=== pacelc (72 occurrences) ===
  c01_b15.md: ...将遇到 Deutsch 的八大谬误（它们以工程假设命名这些谎言）、CAP（它量化分区下一致性与可用性之间的权衡），以及 PACELC（它把 CAP 扩展到无分区情况）。然后第3章抓住第一个谎言——时间——展开让你能够不撒谎地推理顺序的协议（Lampor...
  c02_b01.md: ...的一致性”指的是线性一致性（linearizability）——这比“事务性”强得多——也没有说明应用看到的延迟更多与 PACELC 有关（无分区时的 C 与 L 权衡），而非分区时的 C 或 A 选择。  本章教你区分“知道标签”（CP、AP、最终一...
  c02_b01.md: ...Deutsch 的八大谬误开始，因为它们命名了每个错误分布式直觉所依赖的假设。然后精确陈述 CAP（不是口号），再讲 PACELC——它把 CAP 忽略的无分区情况也纳入进来。   ### 2.1 Deutsch 的分布式计算八大谬误  20 世纪...
  c02_b04.md: ...“AP”的系统提供的保证比标签暗示的更强；许多标为“CP”的系统以标签未命名的小巧方式交易可用性。   ### 2.3 PACELC：诚实的扩展  Daniel Abadi 在 2010 年左右指出，CAP 只命名了分区情况，忽略了更常见的情况：网络健...
  c02_b04.md: ...2010 年左右指出，CAP 只命名了分区情况，忽略了更常见的情况：网络健康时。健康情况下的权衡在延迟与一致性之间。  PACELC：若发生 Partition，则 Availability 或 Consistency；否则（Else），则 Laten...
  c02_b04.md: ...迟很低，因为它不对读做任何协调。Cassandra 类似；其可调一致性让你在请求时选择速度还是严格顺序。   #### PACELC 实践  对任何系统有用的问题是：在 PACELC 下，它在分区时放弃什么，在正常运营时又放弃什么？以下是你在第四部分会...
  c02_b04.md: ...类似；其可调一致性让你在请求时选择速度还是严格顺序。   #### PACELC 实践  对任何系统有用的问题是：在 PACELC 下，它在分区时放弃什么，在正常运营时又放弃什么？以下是你在第四部分会遇到系统的 PACELC 摘要：  系统 | 分区...
  c02_b04.md: ...任何系统有用的问题是：在 PACELC 下，它在分区时放弃什么，在正常运营时又放弃什么？以下是你在第四部分会遇到系统的 PACELC 摘要：  系统 | 分区期间 | 否则（无分区） ---|---|--- Spanner | C（拒绝少数侧写入） |...
  c02_b04.md: ...tionDB | C | C，针对 OLTP 延迟优化  这张表比 CAP 表更有用。CAP 告诉你事情变坏时发生什么。PACELC 告诉你应用 _每天_ 付出什么代价，这通常决定了系统是否适合你的工作负载。   ### 2.4 计数器-排行榜-库存提...
  c02_b06.md: ...### 2.5 PACELC 决策流程  为工作负载选择系统时，PACELC 流程是：    1. **对工作负载分类。** 每个操作需要什么？线性...
  c02_b06.md: ...### 2.5 PACELC 决策流程  为工作负载选择系统时，PACELC 流程是：    1. **对工作负载分类。** 每个操作需要什么？线性化、因果、最终一致？   2. **计算延迟预算...
  c02_b06.md: ...过期读的代价各是什么？对库存系统，不可接受超卖，宁可不可用。对内容分发系统，过期读比空白页更可取。   5. **选择 PACELC 配置匹配的系统。** 针对该工作负载、延迟预算和分区时优先级。  这个流程就是第11章将深入教授的 30 分钟读懂任何...
  c02_b06.md: ...该工作负载、延迟预算和分区时优先级。  这个流程就是第11章将深入教授的 30 分钟读懂任何分布式数据库的“诊断方法”。PACELC 表是摘要。   ### 2.6 练习...
  c02_b08.md: ...致性”是数据库意义上的宽松词。CAP 的一致性是线性一致性，比事务一致性或最终一致性要强得多。  **Q3.** 陈述 PACELC。对 (a) Spanner、(b) CockroachDB、(c) DynamoDB、(d) 使用 QUORUM 读/...
  c02_b08.md: ...CockroachDB、(c) DynamoDB、(d) 使用 QUORUM 读/写的 Cassandra，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Availability vs Consistency；否...
  c02_b08.md: ...DynamoDB、(d) 使用 QUORUM 读/写的 Cassandra，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Availability vs Consistency；否则（Else），Latency...
  c02_b08.md: ...y vs Consistency。每个系统是 PA/EL、PA/EC、PC/EL、PC/EC 之一。  **答案：** PACELC：分区时选择可用性或一致性；否则（无分区）选择延迟或一致性。  (a) Spanner：PC/EC。分区时偏好一致性（被...
  c02_b08.md: ...可能过期的读。无分区时，读延迟低，因为它们不做协调。（DynamoDB 也支持按请求的强一致读，延迟更高，这是按操作的 PACELC 选择，不是全局的。）  (d) 使用 QUORUM 读/写的 Cassandra：对这些特定操作是 PC/EC。QUO...
  c02_b08.md: ...延迟。使用 LOCAL_QUORUM 情况变化；使用 ONE 则变成 PA/EL。Cassandra 是按请求的，所以 PACELC 分类也是按操作的。  **Q4.** 一个团队正在为一个秒杀库存系统在 Spanner 和 DynamoDB 之间做选...
  c02_b08.md: ...DynamoDB 之间做选择，大促期间每秒 100,000 次购买请求，持续 60 秒。需求是任何商品都不能超卖。用 PACELC 走一遍决策。  提示  “任何商品不能超卖”是库存减量操作上的线性化需求。DynamoDB 默认一致性是最终一致；能否...
  c02_b08.md: ...B 默认一致性是最终一致；能否让该操作线性化？代价是什么？  **答案：** 库存减量的“不超卖”需求是线性化需求。在 PACELC 下，两个选项都能满足需求，但代价不同。  Spanner：默认线性化。每次库存减量支付 commit-wait 延迟（...
  c02_b09.md: ...PACELC 说：对这个操作，两者在分区下都是 PC。Else 情况下，条件写入路径 favors DynamoDB 的延迟，尤其当...
  c02_b11.md: ...册的工程成本。正确答案取决于规模、团队规模和应用跨工作负载的程度。  **Q4.** 挑选你栈中的一个系统，为它写一页 PACELC 画像，具体说明分区时做什么、正常运营时付什么代价。要具体说明哪些操作是线性化的，哪些不是。  提示  使用 §2.3...
  c02_b11.md: ...么代价。要具体说明哪些操作是线性化的，哪些不是。  提示  使用 §2.3 的表格式。对系统支持的每种操作类型，给出其 PACELC 分类。  **答案：** 这是第四部分其余章节将训练你掌握的内容。一个 Redis Cluster 工作示例：...
  c02_b12.md: ...裂） | L，但分区下不安全（第18章） Lua 脚本 | PC（单主节点原子） | 每个主节点 L，但局限于一个槽  PACELC 画像让每种操作在什么条件下安全、系统默认会在哪里让你惊讶，变得清晰。第11章将训练你在 30 分钟内为任何系统产出这个...
  c02_b13.md: ...#### 深入思考  **Q1.** 辩护或反驳：“CAP 在 2026 年已经过时；PACELC 是工程师唯一需要的框架。”用至少两个生产系统的证据。  **讨论：** 两个框架都有位置。CAP 命名了系统设计者关于...
  c02_b13.md: ...两个生产系统的证据。  **讨论：** 两个框架都有位置。CAP 命名了系统设计者关于罕见但真实的分区情况必须做的选择。PACELC 命名了应用用户每天付出的代价。单独哪一个都不够。  “CAP 过时”的辩护：在现代云部署中，分区很短（秒级，不是小时级...
  c02_b13.md: ...”的辩护：在现代云部署中，分区很短（秒级，不是小时级），正常运营中延迟与一致性的选择主导用户体验。99.9% 的时间里 PACELC 才重要；CAP 只在事故响应时登场。  反驳：当分区真的发生，CAP 行为决定你的系统是静默损坏数据、干净拒绝请求，还...
  c02_b13.md: ...选择必须提前做。没有想过 CAP 的生产系统造成了足以以其名字命名的大规模中断。  有用的综合：CAP 是最坏情况行为；PACELC 是日常行为。两者都应出现在任何严肃的系统评估中，PACELC 因主导日常成本而获得更多设计关注，CAP 因在负载下分区...
  c02_b13.md: ...命名的大规模中断。  有用的综合：CAP 是最坏情况行为；PACELC 是日常行为。两者都应出现在任何严肃的系统评估中，PACELC 因主导日常成本而获得更多设计关注，CAP 因在负载下分区行为本身就是需求的安全关键工作负载而获得更多设计关注。  工作...
  c02_b14.md: ...号版本（“选两个”）有误导性，因为分区容错性不是可选项。CAP 的“一致性”是线性化，比该词的宽松数据库意义强得多。  PACELC 扩展了 CAP 忽略的情况：没有分区时。PACELC 命名了正常运营中延迟与一致性之间的权衡，这就是应用每天付出的代价...
  c02_b14.md: ...。CAP 的“一致性”是线性化，比该词的宽松数据库意义强得多。  PACELC 扩展了 CAP 忽略的情况：没有分区时。PACELC 命名了正常运营中延迟与一致性之间的权衡，这就是应用每天付出的代价。系统的 PACELC 画像比它的 CAP 标签更实用...
  c02_b14.md: ...CAP 忽略的情况：没有分区时。PACELC 命名了正常运营中延迟与一致性之间的权衡，这就是应用每天付出的代价。系统的 PACELC 画像比它的 CAP 标签更实用。  §2.4 的计数器-排行榜-库存工作负载会在全书反复出现。它们代表一致性光谱上的点...
  c04_b06.md: ...有序变更事件。  选择系统时正确的问题不是“它提供哪一层广播？”而是“它对我关心的操作提供的最强性质是什么？”第2章的 PACELC 框架可以推广：每个组件的代价与它提供的最强保证成正比，而从一个更强系统获得较弱保证通常不可能（成本已经内嵌）。   #...
  c05_b08.md: ...在决定 Crescent 支付系统用 Postgres + 读副本（异步）还是 CockroachDB。列出他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域...
  c10_b12.md: ...方法_ ：如何通过识别复制协议、故障恢复语义、一致性模型和分区行为，在 30 分钟内读懂任意分布式数据库的 CAP 和 PACELC 位置。  ---...
  c11_b01.md: ...是什么？** 少数侧拒绝写、提供过期读、sloppy quorum、应用控制。  具体回答这四个问题，就能得到该系统的 PACELC 画像、运维特征和失败模式。任何问题 30 分钟都能回答，因为每家厂商文档中都有确定答案。   ### 11.2 方法详...
  c11_b03.md: ...** ：Cassandra 可调一致性、DynamoDB 可选强一致读。应用按请求选择行为。  分区行为是最坏情况行为；PACELC 的 “P” 半部分。结合正常运营成本（PACELC 的 “EL/EC” 半部分），得到完整画像。   ### 11.3...
  c11_b03.md: ...分钟读懂一个数据库  > **工作流 6（输入 → 输出）** ：给定任何分布式数据库文档，在 30 分钟内产出一页 PACELC + 运维画像（复制协议、故障恢复语义、一致性模型、分区行为）。机制是 §11.1-§11.2 的四个问题诊断；输出是...
  c11_b03.md: ...有界过期读 Partition behavior | CP——少数侧停止接受写；TrueTime 确保分区后时间戳一致 PACELC | PC / EC（分区下一致；正常运营为 commit-wait 延迟买单） Strengths | 无需重启成本即...
  c11_b03.md: ...ollower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停止接受写 PACELC | PC / EC（分区下一致；无分区时为不确定性窗口读付 HLC-restart 成本） Strengths | 兼...
  c11_b04.md: ...请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 per-item 线性化 PACELC | 默认 PA / EL（低延迟、最终）；条件写操作 PC / EC Strengths | 免运维；延迟可预测；与...
  c11_b04.md: ...backed）提供线性化 Partition behavior | 按操作可调；低一致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA / EL，quorum 时 PC / EC Strengths | 线性扩展到数千节点；可调...
  c11_b04.md: ...lizable 事务；ACID 强可串行化保证 Partition behavior | CP——丢失法定人数则拒绝写 PACELC | PC / EC；面向 OLTP 延迟设计 Strengths | 久经考验的正确性（Snowflake 构建其上；...
  c11_b05.md: ...alizable 事务；follower 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC | PC / EC Strengths | 从一开始即为金融工作负载构建；深度可观测性 Weaknesses | 新系...
  c11_b05.md: ...一致”却不指明模型。“强一致”可能指线性化、可串行化、快照或某种自定义。要求精确模型。  (b) **混淆 CAP 与 PACELC。** 厂商说“CP”只告诉你分区行为，没告诉你正常运营成本。总要问 PACELC 问题（无分区时每操作成本是多少毫秒？...
  c11_b05.md: ...型。  (b) **混淆 CAP 与 PACELC。** 厂商说“CP”只告诉你分区行为，没告诉你正常运营成本。总要问 PACELC 问题（无分区时每操作成本是多少毫秒？）。  (c) **per-operation vs 系统级声明。** 系统可能对...
  c11_b05.md: ...(d) **营销级可用性声明。** “99.99% 可用性”是 SLA 声明；分区下的实际可用性是另一个数字。CAP/PACELC 分析才是预测器。  (e) **理想化条件 benchmark。** 厂商 benchmark 在最佳硬件、理想网络、...
  c11_b05.md: ...benchmark。** 厂商 benchmark 在最佳硬件、理想网络、单区域。真实生产性能差 2-10 倍。CAP/PACELC 画像告诉你网络变差时的预期。   ### 11.7 练习...
  c11_b06.md: ...4. 分区行为是什么？（拒绝少数、服务所有、sloppy quorum、应用控制）  回答这四个问题可得到任何系统的 PACELC 画像、运维特征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同...
  c11_b07.md: ...e；读可走 leader（即时）或 follower（有界过期）。分区行为在存储层为 CP（丢失存储法定人数时写阻塞）。PACELC：分区下 PC，无分区 EC（存储层法定人数写比本地 Postgres 逻辑多花约 5-10 ms）。优势：server...
  c11_b08.md: ...每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数 PACELC | PC / EC；优化金融 OLTP 吞吐（benchmark 宣称每秒 100 万+ 转账） Strengths...
  c11_b09.md: ...| 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quorum PC/EC  差异在实现：  Aspect | Cassandra |...
  c11_b11.md: ...诊断方法（四个问题：复制协议、故障恢复、一致性、分区行为）让你能在 30 分钟内读懂任何分布式数据库的 CAP 和 PACELC 位置。方法可泛化；它适用于你没用过的系统，因为四个问题在任何厂商文档中都有确定答案。  Olamide 对 Cresc...
  c11_b11.md: ...阅读文档的常见陷阱：营销级“一致性”声明混淆 linearizable 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 benchmark。方法通过要求精确答案抵御这些陷阱。  第12...
  c14_b07.md: ...拒绝写 Timestamp source | TrueTime（GPS + 原子钟） | HLC + NTP 偏移上界 PACELC | PC / EC（每次写 commit-wait） | PC / EC（不确定性的可变成本） SQL 兼容性 | S...
  c27_b01.md: ...ath 的代码；不考虑 “半成功” 结果。    6. **CAP 分类简单**。说 “我们用 CP 系统” 而不理解 PACELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM...
  c27_b04.md: ...account_id)   LLM 产出第一版加误导注释是因为口号级 CAP 陈述在训练数据中过度代表。修复需要理解 PACELC（第2章）和具体数据库的 per-operation 语义。...
  c27_b09.md: ...简单**：代码说 “我们用 CP 系统” 或 “我们用 AP 系统”，而不理解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默认产出嵌入这些谎言的代码，因为训练数据过度代表...
  c28_b05.md: ...身份、故障） | 架构防御每一种 | E.1、E.2、E.5、E.7（每个谎言都导致真实故障） | | 2 | CAP、PACELC | 每个组件都经 PACELC 分类 | n/a | | 3 | 时间、HLC | CockroachDB 使用 HL...
  c28_b05.md: ...| E.1、E.2、E.5、E.7（每个谎言都导致真实故障） | | 2 | CAP、PACELC | 每个组件都经 PACELC 分类 | n/a | | 3 | 时间、HLC | CockroachDB 使用 HLC；跨 region 时间戳 |...
  c28_b10.md: ...ader 间视图变更竞态 | | 6 | 30 分钟读懂数据库 | Ch.11 §11.3 | 任何厂商文档 | 一页 PACELC + 诊断画像 | 四个诊断问题：复制、恢复、一致性、分区 | 接受营销级 “一致”；混淆 CAP 与 PACELC；遗...
  c28_b10.md: ...页 PACELC + 诊断画像 | 四个诊断问题：复制、恢复、一致性、分区 | 接受营销级 “一致”；混淆 CAP 与 PACELC；遗漏 per-operation 一致性选择 | | 7 | Anya 的重分片迁移 | Ch.12 §12.5 |...
  c28_b14.md: ...照隔离；副本读最终一致。 * **分区行为**：CP——少数派无法在 etcd 获得 leadership，拒绝写。  PACELC：PC/EL（分区时一致；正常操作时低延迟，因异步复制）。  _三个谎言姿态_：  * **时间**：Postgres...
  c28_b17.md: ...在圣保罗被重复扣款。你现在知道：  * 为什么分布式系统在时间、身份、故障上自欺（第1章）。 * 如何通过 CAP 和 PACELC 量化这些谎言（第2章）。 * 如何用具体原语防御每个谎言——时间用 HLC（第3章），Raft 和 fencing t...
  c28_b18.md: ...个原语。发布是纪律的成功应用：发布前混沌、渐进金丝雀发布、发布后复盘。  每章的课程都出现在架构中：每个组件做 CAP/PACELC 分析；跨 region 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing t...
  c30_b01.md: ...Design: CAP is Only Part of the Story." _IEEE Computer_. — PACELC 论文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering o...
  c30_b04.md: ...### 附录 B — 五数据库 CAP 位置参考  | 系统 | 复制 | 故障恢复 | 一致性 | 分区 | PACELC | 最佳场景 | |---|---|---|---|---|---|---| | **Spanner** | 每 ta...
  c30_b06.md: ...器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：CAP 的 PACELC 扩展。若分区：可用性或一致性；否则：延迟或一致性。 * **Paxos**：Lampor...
  c30_b06.md: ...次调用每一步，失败时触发补偿。协调器状态本身是持久的。与编排式 saga 对照。 * **PACELC**：CAP 的 PACELC 扩展。若分区：可用性或一致性；否则：延迟或一致性。 * **Paxos**：Lamport 1989/1998 共识协...
  c30_b07.md: ...障罕见 | Ch.22 | Saga 模式；outbox；补偿动作 | | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-operation 一致性选择 |  Crescent 支付系统对每个谎言的防御在第28章详述。  *...
  c30_b16.md: ...e 严格更强。 | Ch.2, Ch.11 | | 5 | "我们用 CP 系统，所以一致。" | CP 命名分区行为；PACELC 命名日常成本。Per-operation 一致性才是正确问题。 | Ch.2 | | 6 | "向量时钟告诉你现在几点...

=== acceptor (72 occurrences) ===
  c07_b03.md: ...规则矛盾。  这就是 _quorum intersection_ 论证，出现在每个共识协议中。Paxos 选择一个多数 acceptor 已接受的值。Raft 选择一个多数 follower 已复制的日志条目。HotStuff 选择一个由 2f+1 of...
  c08_b01.md: ...场景  一组 N 个进程，每个扮演三种角色之一或多种：  * **Proposer** ：试图让某个值被选定。 * **Acceptor** ：对提议投票；崩溃后持久化状态。 * **Learner** ：发现已选定的值。  单个进程可扮演所有三种角色。协...
  c08_b01.md: ...alidity** ：只有被提议的值才能被选定。 * **Agreement** ：只选定一个值（协议完成后，没有两个 acceptor 对选定值产生分歧）。 * **Termination** ：无故障且 leader 稳定时，协议终止。   ### 8...
  c08_b01.md: ...在 Paxos 之前，先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分...
  c08_b01.md: ...先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值...
  c08_b01.md: ...的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 prop...
  c08_b01.md: ...所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ac...
  c08_b01.md: ...* ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。...
  c08_b01.md: ...中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有值拿到多数；无法达成一致。...
  c08_b01.md: ...场景  一组 N 个进程，每个扮演三种角色之一或多种：  * **Proposer** ：试图让某个值被选定。 * **Acceptor** ：对提议投票；崩溃后持久化状态。 * **Learner** ：发现已选定的值。  单个进程可扮演所有三种角色。协...
  c08_b01.md: ...alidity** ：只有被提议的值才能被选定。 * **Agreement** ：只选定一个值（协议完成后，没有两个 acceptor 对选定值产生分歧）。 * **Termination** ：无故障且 leader 稳定时，协议终止。   ### 8...
  c08_b01.md: ...在 Paxos 之前，先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分...
  c08_b01.md: ...先看朴素协议会怎么做。  **朴素协议 1** ：指定一个 proposer 把它的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值...
  c08_b01.md: ...的值发给所有 acceptor。所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 prop...
  c08_b01.md: ...所有 acceptor 存储该值。完成。  问题：proposer 可能在把值发给所有 acceptor 前崩溃。部分 acceptor 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ac...
  c08_b01.md: ...* ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。...
  c08_b01.md: ...中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可提议；acceptor 接受它看到的第一个提议。  问题：两个 proposer 可各自让不相交的子集接受其值；没有值拿到多数；无法达成一致。...
  c08_b02.md: ...唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小...
  c08_b02.md: ...按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{...
  c08_b02.md: ...`PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \left...
  c08_b02.md: ...accepted_value)`，其中 `accepted_ballot` 和 `accepted_value` 是该 acceptor 最近 _接受_ 的值（可能为 null）。  该 promise 让 acceptor 承诺：“我不再接受任何 bal...
  c08_b02.md: ...epted_value` 是该 acceptor 最近 _接受_ 的值（可能为 null）。  该 promise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer...
  c08_b02.md: ...b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise...
  c08_b02.md: ...受）必须是新 ballot 提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $...
  c08_b02.md: ...先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{max}$，忽略。 * 否则，把 $(b,...
  c08_b02.md: ...时，值 v 被 _选定_。   #### Learning  一旦值被选定，learners 必须学到它。最简单做法：acceptor 接受一个值且其支持达到多数时，就告诉 learners。也有优化（指定 learner；把信息捎带在协议消息上）。...
  c08_b02.md: ...lot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acceptor 的任意两个多数集合至少相交于一个 acceptor。（$N=3$ 时，任意两个 2 集合至少共享 1 个；$N=5$...
  c08_b02.md: ...，不依赖真实时间。  **第 2 部分：法定人数相交。** N 个 acceptor 的任意两个多数集合至少相交于一个 acceptor。（$N=3$ 时，任意两个 2 集合至少共享 1 个；$N=5$ 时，任意两个 3 集合至少共享 1 个。）这是基础性...
  c08_b02.md: ...1 个；$N=5$ 时，任意两个 3 集合至少共享 1 个。）这是基础性质：一个多数作出的决定会被任何其他多数中的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提...
  c08_b02.md: ...。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 b...
  c08_b04.md: ...e Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persistent state** ：acceptor 必须在崩溃间持久化 $b_{max}$、`accepted_ballot`、`accepted_value`。丢失这些...
  c08_b04.md: ...accepted_value`。丢失这些状态会破坏 safety。 * **Reconfiguration** ：增减 acceptor 集合本身必须通过 Paxos 完成，以避免两个不同配置各自形成无重叠的“多数”。 * **Snapshotting**...
  c08_b04.md: ...**Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 lea...
  c08_b04.md: ...Paxos** ：允许并发非冲突提议被排序。对可交换操作的工作负载吞吐更高。 * **Cheap Paxos** ：把 acceptor 分为“活跃”的（积极参与协议）和“辅助”的（只在故障时咨询）。降低稳态成本。 * **Egalitarian Paxo...
  c08_b04.md: ...要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）...
  c08_b05.md: ...### 8.8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPAR...
  c08_b05.md: ..._3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $...
  c08_b05.md: ...** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE...
  c08_b05.md: ...**步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `accepted_ballot = 10, acc...
  c08_b05.md: ...ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `accepted_ballot = 10, accepted_value = "hello"`，并...
  c08_b05.md: ...lot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`...
  c08_b05.md: ...的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hel...
  c08_b05.md: ...### 8.8 示例演算  三个 acceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPAR...
  c08_b05.md: ..._3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $...
  c08_b05.md: ...** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE...
  c08_b05.md: ...**步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `accepted_ballot = 10, acc...
  c08_b05.md: ...ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `accepted_ballot = 10, accepted_value = "hello"`，并...
  c08_b05.md: ...lot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`...
  c08_b05.md: ...的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hel...
  c08_b06.md: ...Prepare）：_ Proposer 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复 `PROMISE(b, accepted_ballot, a...
  c08_b06.md: ...llot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是...
  c08_b06.md: ...回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise...
  c08_b06.md: ...多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已接受的最高值（如有）；这约束了它能提议什么。  _第二阶段（Accept）：_ Proposer 选一个值（第一阶段...
  c08_b06.md: ...个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，...
  c08_b06.md: ...值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 bal...
  c08_b06.md: ...新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已...
  c08_b06.md: ...Accept 规则（proposer 必须采用第一阶段所见最高 ballot 的已接受值）防止这种情况。任何已被某 acceptor 接受的值（即使尚未多数）都“在飞行中”，可能已选定。新 ballot 必须采纳它以避免 safety 违反。  新 b...
  c08_b07.md: ...）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballot 的 acceptor 会接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（PREPARE + PROMISE...
  c08_b07.md: ...Multi-Paxos 优化。  **答案：** Paxos 难实现有多个运维原因：  (a) **持久状态纪律** ：acceptor 必须在回复 prepare 或 accept 消息 _之前_ 把 $b_{max}$、`accepted_ballot...
  c08_b07.md: ..._ 把 $b_{max}$、`accepted_ballot`、`accepted_value` 持久化到可靠存储。若 acceptor 丢失该状态（磁盘故障、漏 fsync、文件系统损坏），它会“忘记”先前 promise 并接受冲突值，违反 safet...
  c08_b07.md: ...文件系统损坏），它会“忘记”先前 promise 并接受冲突值，违反 safety。  (b) **重配置** ：改变 acceptor 集合（增删节点）不能随意，因为两个不同配置可能各自形成无重叠的“多数”。标准解是 “joint consensus”...
  c08_b08.md: ...#### 应用问题  **Q1.** 一个团队为 leader 选举服务实现单法令 Paxos。他们有 5 个 acceptor。走查两个 proposer（A 和 B）并发启动 ballot 的场景，跟踪协议以证明最终只选定一个值。  提示  更...
  c08_b08.md: ...oser 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prep...
  c08_b08.md: ...所有 5 个。 * B 发送 `PREPARE(b=20)` 给所有 5 个。  假设 B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(...
  c08_b08.md: ...omise）。A 选更高 ballot，例如 30。 * A 发送 `PREPARE(b=30)`。 * 所有 5 个 acceptor 的 $b_{max}=20$ 且 `accepted_value = "Olamide"`（`accepted_bal...
  c08_b08.md: ...repare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何...
  c08_b10.md: ...指出任何 LLM 生成的 Paxos 实现中可能存在的三类 bug。  **讨论：**  (a) **持久化遗漏。** Acceptor 状态（$b_{max}$、`accepted_ballot`、`accepted_value`）必须在回复 prepa...
  c08_b10.md: ...或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯...
  c08_b10.md: ...指出任何 LLM 生成的 Paxos 实现中可能存在的三类 bug。  **讨论：**  (a) **持久化遗漏。** Acceptor 状态（$b_{max}$、`accepted_ballot`、`accepted_value`）必须在回复 prepa...
  c08_b10.md: ...或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯...
  c08_b11.md: ...现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/ballot/acceptor。LLM 代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 acceptor 状态。  更深观察：Pax...
  c08_b11.md: ...oposer/ballot/acceptor。LLM 代码常用简单 HTTP 无幂等，因此重试消息可能被处理两次，污染 acceptor 状态。  更深观察：Paxos 是 LLM 生成代码的最坏情况类别。Safety 性质在任何单次测试运行中不可见；它们...

=== prepare (71 occurrences) ===
  c07_b10.md: ...M 关于协调者故障下 2PC 阻塞性质的遗漏。  **讨论：** 2PC 有一个臭名昭著的阻塞失败模式：若协调者在发送 PREPARE 后、发送 COMMIT 或 ABORT 前崩溃，参与者就卡住。它们已投票（并持有锁），无法自行决定（不知道其他参与者投...
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（...
  c08_b02.md: ...ter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor...
  c08_b02.md: ...增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复...
  c08_b02.md: ...：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值...
  c08_b02.md: ...新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，...
  c08_b02.md: ...值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就...
  c08_b02.md: ...任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就是 agreement 性质：任何已选定...
  c08_b02.md: ...#### 第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（...
  c08_b02.md: ...ter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor...
  c08_b02.md: ...增。  Proposer 向每个 acceptor 发送 `PREPARE(b)`。  每个 acceptor 收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复...
  c08_b02.md: ...：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值...
  c08_b02.md: ...新值必须与之匹配（必须采用最高 ballot 值）。这意味着：一旦某值被选定（被多数接受），任何后续 ballot 的 prepare 阶段都会在某个响应中见到该值（由法定人数相交保证），并必须提议它。  合起来：值 v 在 ballot b 被选定后，...
  c08_b02.md: ...值 v 在 ballot b 被选定后，任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就...
  c08_b02.md: ...任何更晚 ballot $b' > b$ 都不能选定不同值。任何 $b'$ 的 prepare 与接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就是 agreement 性质：任何已选定...
  c08_b04.md: ...* **Flexible Paxos**（Heidi Howard 2016 法定人数灵活性工作）：推广法定人数要求。prepare 法定人数和 accept 法定人数只需 _相交_，不必都是多数。这让系统可调节权衡（更小 accept 法定人数换更快...
  c08_b05.md: ...ceptor $A_1, A_2, A_3$；一个 proposer。  **步骤 1** ：Proposer 发送 `PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初...
  c08_b05.md: ...` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3**...
  c08_b05.md: ...proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7** ：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回...
  c08_b05.md: ...发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9**...
  c08_b05.md: ..."hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若第二个 proposer...
  c08_b07.md: ...(b) Leader 上任伊始对所有未来槽跑一次第一阶段（“为槽 [current_slot, \infty) prepare” 模式）。  (c) 对每个要提交的新值，leader 只跑第二阶段（ACCEPT）。承诺过 leader ballo...
  c08_b07.md: ...**答案：** Paxos 难实现有多个运维原因：  (a) **持久状态纪律** ：acceptor 必须在回复 prepare 或 accept 消息 _之前_ 把 $b_{max}$、`accepted_ballot`、`accepted_va...
  c08_b08.md: ...踪协议以证明最终只选定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Propo...
  c08_b08.md: ...ptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给所有 5 个。 * B 发送 `PREPARE(b=20)`...
  c08_b08.md: ...“Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给所有 5 个。 * B 发送 `PREPARE(b=20)` 给所有 5 个。  假设 B 的 pre...
  c08_b08.md: ...。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给所有 5 个。 * B 发送 `PREPARE(b=20)` 给所有 5 个。  假设 B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Ac...
  c08_b08.md: ...ARE(b=10)` 给所有 5 个。 * B 发送 `PREPARE(b=20)` 给所有 5 个。  假设 B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回...
  c08_b08.md: ...受。B 收到 5 个 accepted 消息。值 “Olamide” 被选定。  **A 的重试：**  * A 的 `prepare(10)` 失败（只有 2 个 promise）。A 选更高 ballot，例如 30。 * A 发送 `PREPARE...
  c08_b08.md: ...prepare(10)` 失败（只有 2 个 promise）。A 选更高 ballot，例如 30。 * A 发送 `PREPARE(b=30)`。 * 所有 5 个 acceptor 的 $b_{max}=20$ 且 `accepted_value...
  c08_b08.md: ...心跳。它们的选举定时器触发（通常几百毫秒到几秒）。其中一个启动新 ballot（更高 ballot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 accept...
  c08_b08.md: ...几秒）。其中一个启动新 ballot（更高 ballot number 的 prepare）。  新 ballot 的 prepare 到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个...
  c08_b09.md: ...有两阶段。结构性区别在于如何处理协调者 / proposer 的故障。  _2PC 结构_ ：一个协调者给所有参与者发 PREPARE（或 VOTE_REQUEST）。若所有参与者投 yes，协调者发 COMMIT；若有任何 no，协调者发 ABORT。...
  c08_b09.md: ...，协调者发 COMMIT；若有任何 no，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在 PREPARE 后、发 COMMIT/ABORT 前崩溃，参与者被阻塞。它们已投 yes（并持有锁），但不知道协调者的决定。它们不能单...
  c08_b10.md: ...eptor 状态（$b_{max}$、`accepted_ballot`、`accepted_value`）必须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后...
  c10_b02.md: ...v 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的...
  c10_b02.md: ...每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个...
  c10_b02.md: ...e-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “p...
  c10_b02.md: ...eader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (...
  c10_b02.md: ...** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 c...
  c10_b02.md: ...消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，...
  c10_b02.md: ...消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。...
  c10_b02.md: ...v 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的...
  c10_b02.md: ...每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个...
  c10_b02.md: ...e-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “p...
  c10_b02.md: ...eader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (...
  c10_b02.md: ...** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 c...
  c10_b02.md: ...消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，...
  c10_b02.md: ...消息的副本决定该值。  为什么需要两轮（prepare + commit）？Prepare 轮确保没有两个正确副本同时 prepare 不同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。...
  c10_b05.md: ...uorum Intersection Revisited” 推广了 Paxos 的法定人数要求。经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 都是多数。Howard 的洞见：对 safety 只需要...
  c10_b05.md: ...$Q_1 \cap Q_2 \ne \emptyset$。  这意味着：$Q_1$ 和 $Q_2$ 可以调优。只要任意 prepare 法定人数与任意 accept 法定人数相交，safety 就成立。  示例：  * 经典：$Q_1 = Q_2 = \...
  c10_b05.md: ...1 很少运行的系统（稳定领导）可以让 accept 法定人数很小以换取快速提交，代价是 leader 变更需要更大的 prepare 法定人数。  这一洞见用于一些 2026 年分布式数据库设计，以降低 per-commit 延迟而不牺牲 safety...
  c10_b08.md: ...f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。...
  c10_b08.md: ...\emptyset$；两者不必都是多数。更小的 $Q_2$ = 更快的提交。  **答案：** 经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 各为 N 的多数。这确保 $Q_1 \cap Q_2 \...
  c10_b08.md: ...2（每次提交）。让 $Q_1$ 很大（如 $N - 1$），$Q_2$ 很小（如 2）。任意大小为 $N - 1$ 的 prepare 法定人数与任意大小为 2 的 accept 法定人数都相交（因为 $(N - 1) + 2 > N$）。  代价：le...
  c10_b08.md: ...交（因为 $(N - 1) + 2 > N$）。  代价：leader 变更昂贵（需要 $N - 1$ 个副本可达才能 prepare）。收益：每次提交只需 2 个副本 ack，稳态提交延迟非常低。  5 节点集群：$Q_1 = 4, Q_2 = 2$。...
  c10_b10.md: ...pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipelin...
  c10_b12.md: ...s 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 prepare 与 accept 法定人数相交，而非各为多数。这让运维可以调优：快而常见的路径 vs 慢而罕见的路径。  非区块链 B...
  c13_b01.md: ...### 13.2 两阶段提交（2PC）  经典协议。协调者管理事务；参与者（各分片）就是否提交投票。  **阶段 1：Prepare**  协调者向每个参与者发送 `PREPARE(txn_id, operations)`。每个参与者：    1. 验...
  c13_b01.md: ...协议。协调者管理事务；参与者（各分片）就是否提交投票。  **阶段 1：Prepare**  协调者向每个参与者发送 `PREPARE(txn_id, operations)`。每个参与者：    1. 验证操作（如检查约束、获取锁）。   2. 将操作...
  c13_b02.md: ...#### 2PC 为何阻塞  经典失败模式：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已经投票（并持有锁）。他们无法自行决定（协调者可能在崩溃前已提交...
  c13_b12.md: ...#### 概念检测  **Q1.** 描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段...
  c13_b12.md: ...abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发送 PREPARE。每个参与者验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Com...
  c13_b12.md: ...T；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；...
  c13_b12.md: ...#### 概念检测  **Q1.** 描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段...
  c13_b12.md: ...abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发送 PREPARE。每个参与者验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Com...
  c13_b12.md: ...T；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；...
  c13_b14.md: ...录、参与者列表、当前提交阶段）。  (c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader...
  c13_b14.md: ...但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换具有鲁棒性：它们通过 Raft term/leadership 识别新...
  c13_b17.md: ...(d) **协议消息幂等性**。网络重试可能使 PREPARE 或 COMMIT 到达两次。参与者必须幂等处理。LLM 代码通常不会。  (e) **参与者崩溃后的恢复**。参与者在...
  c15_b08.md: ...**两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...

=== model (68 occurrences) ===
  c11_b03.md: ...~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition behavior | CP——少数侧停止接受写；True...
  c11_b03.md: ...ecovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition beha...
  c11_b04.md: ...步复制 Failure-recovery | 透明（存储层对用户不透明）；自动扩缩容与自动修复 Consistency model | 默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 pe...
  c11_b04.md: ...ecovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线性化 Partition behavior | 按操...
  c11_b04.md: ...e-recovery | 快（亚秒级 sequencer 切换）；自设计日起就以混沌工程为目标 Consistency model | Serializable 事务；ACID 强可串行化保证 Partition behavior | CP——丢失法...
  c11_b05.md: ...recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serializable 事务；follower 上有界过期读 Partition behavior | CP——...
  c11_b08.md: ...ry | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Partitio...
  c11_b09.md: ...r；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP...
  c14_b07.md: ...ry | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency model | Linearizable；提供过期读 | Serializable；提供 follower reads Parti...
  c19_b05.md: ...aft | ZAB（Paxos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes...
  c24_b04.md: ...│              │           │              │             │ Model A: 4   │           │ Model A: 2   │             │ Model B:...
  c24_b04.md: ...│              │             │ Model A: 4   │           │ Model A: 2   │             │ Model B: 2   │           │ Model B:...
  c24_b04.md: ...│ Model A: 4   │           │ Model A: 2   │             │ Model B: 2   │           │ Model B: 4   │             │ Model C:...
  c24_b04.md: ...│ Model A: 2   │             │ Model B: 2   │           │ Model B: 4   │             │ Model C: 2   │           │ Model C:...
  c24_b04.md: ...│ Model B: 2   │           │ Model B: 4   │             │ Model C: 2   │           │ Model C: 2   │             └──────────...
  c24_b04.md: ...│ Model B: 4   │             │ Model C: 2   │           │ Model C: 2   │             └─────────────┘           └───────────...
  c25_b09.md: ...模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sh...
  c25_b09.md: ...code shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 K...
  c25_b09.md: ...l 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026...
  c25_b09.md: ...模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sh...
  c25_b09.md: ...code shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 K...
  c25_b09.md: ...l 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026...
  c26_b03.md: ...Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个...
  c26_b03.md: ...化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K...
  c26_b03.md: ...erify model” 确认，来加速 decode 阶段。  机制：  (a) 对每个 decode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒...
  c26_b03.md: ...ecode 步，draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个候选。这是一...
  c26_b03.md: ...（通常 K=4-8）。Draft 很快：draft model 小（1-7B 参数），毫秒级。  (b) Verify model（大模型，70B+）并行处理所有 K 个候选。这是一个 “verify 步”，一次处理 K 个候选 token。  (c...
  c26_b03.md: ...）并行处理所有 K 个候选。这是一个 “verify 步”，一次处理 K 个候选 token。  (c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token...
  c26_b03.md: ...c) Verify model 检查 K 个候选中与前缀匹配的部分。匹配前缀被接受；第一个不匹配被替换为 verify model 偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify...
  c26_b03.md: ...偏好的 token。  (d) 被接受的 token（1-K 个）输出。解码前进相应数量。  为何更快：verify model 处理 K 个候选的时间与一次正常 decode 步大致相同（因为 K 个 token 并行处理远快于顺序 K 步）。若...
  c26_b03.md: ...个 token 并行处理远快于顺序 K 步）。若接受率高（如 80% draft token 被接受），verify model 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft mode...
  c26_b03.md: ...odel 每步产出约 0.8K 个 token 而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 2...
  c26_b03.md: ...而非 1 个。  吞吐提升：典型工作负载 2-3 倍。成本：draft model 增加 GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decodin...
  c26_b03.md: ...选 token 给 verify 节点；verify 节点确认或修正；修正流回。  分布式收益：  (a) Draft model 节点可用更便宜硬件（小 GPU，某些情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源...
  c26_b03.md: ...(a) Draft model 节点可用更便宜硬件（小 GPU，某些情况下甚至 CPU 用于 tiny draft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 verificati...
  c26_b05.md: ...点可防止其他节点尾部延迟放大。  (c) **模型可用性**：集群服务多个模型；路由必须考虑哪些节点加载了哪些模型。把 model A 请求路由到只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LL...
  c26_b05.md: ...(c) **模型可用性**：集群服务多个模型；路由必须考虑哪些节点加载了哪些模型。把 model A 请求路由到只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错...
  c26_b05.md: ...集群服务多个模型；路由必须考虑哪些节点加载了哪些模型。把 model A 请求路由到只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错误路由决策可导致用户等待 3...
  c26_b05.md: ...益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 toke...
  c26_b05.md: ...decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative deco...
  c26_b05.md: ...用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draft model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型毫秒级。  (b) Verify model...
  c26_b05.md: ...model 生成 K 个候选 token（通常 K=4-8）。Draft 很快：小模型毫秒级。  (b) Verify model 并行处理 K 个候选（一个 verify 步一起处理 K 个 token）。这与正常 decode 步成本大致相同。...
  c26_b05.md: ...一个 verify 步一起处理 K 个 token）。这与正常 decode 步成本大致相同。  (c) Verify model 检查 K 个候选中与其自身预测匹配的前缀。匹配前缀被接受；第一个不匹配被替换为 verify model 的选择。...
  c26_b05.md: ...Verify model 检查 K 个候选中与其自身预测匹配的前缀。匹配前缀被接受；第一个不匹配被替换为 verify model 的选择。  (d) 被接受前缀（1 到 K 个 token）输出。解码前进。  为何加速生成：  * 正常 decod...
  c26_b05.md: ...前进。  为何加速生成：  * 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处...
  c26_b05.md: ...）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 token。  若接受率 80%，verify model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层...
  c26_b05.md: ...y model 每 verify 步产出约 0.8 × K = 6.4 个 token，而非 1 个。在 verify model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft...
  c26_b05.md: ...oken，而非 1 个。在 verify model 层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft mode...
  c26_b05.md: ...层面吞吐约 5-6 倍。净吞吐提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏...
  c26_b05.md: ...提升：2-3 倍（扣除 draft model 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b05.md: ...odel 成本后）。  接受率取决于 draft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b05.md: ...ft model 近似 verify model 的程度。精选 draft model（同架构小版本、从 verify model 蒸馏）可达 70-90% 接受率。...
  c26_b06.md: ...产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 dra...
  c26_b06.md: ...）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative...
  c26_b06.md: ...ive decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a)...
  c26_b06.md: ...**答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵...
  c26_b06.md: ...U）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 ve...
  c26_b06.md: ...大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。  (c)...
  c26_b09.md: ...ted**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；有推理平台工...
  c26_b09.md: ...的团队；per-token 延迟要求紧的工作负载；draft model 小到能放在同 GPU 而不挤占 verify model。  * **分布式**：大集群（50+ 节点）；有推理平台工程师的团队；网络延迟有界（intra-rack、NVLin...
  c26_b09.md: ...省占主导（verify GPU-hour 非常贵的大模型）。  对 Continuum（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  202...
  c26_b11.md: ...burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model...
  c26_b11.md: ...。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft 在便宜硬件...
  c26_b11.md: ...model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在 verify model 层面 2-3 倍吞吐。分布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户...
  c30_b03.md: ...al. (2023). "Efficient Memory Management for Large Language Model Serving with PagedAttention." _SOSP_. — vLLM。 * Zheng, L.,...
  c30_b03.md: ...(2023). "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAttention。 * Patel, P., et al. (2023). "S...
  c30_b06.md: ...冲突（可能写偏）。 * **Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review ar...
  c30_b06.md: ...coding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2026 年...

=== speculative (65 occurrences) ===
  c24_b08.md: ...万美元账单上每月节省 25-40 万美元。  其他技术（简述）：跨节点 KV cache sharding（第25章）；speculative decoding（第26章）；模型量化（8-bit、4-bit serving 用于低精度模型）；MoE routin...
  c24_b09.md: ...不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍...
  c24_b09.md: ...须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：c...
  c24_b09.md: ...不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍...
  c24_b09.md: ...须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：c...
  c25_b10.md: ...优化**：  * 量化（8-bit 或 4-bit serving）： slight 质量损失下 2-4 倍吞吐。 * Speculative decoding（第26章）：长生成 2-3 倍吞吐。 * 编译（TensorRT-LLM、ExecuTorch）：在...
  c25_b11.md: ...6章结束第八部分，讲负载均衡和投机解码作为分布式协议。Anyscale/Together/Fireworks 路由问题；speculative decoding（draft model + verify model）作为多机协议；长度预测和 KV-cache-a...
  c26_b01.md: ...KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative decoding（用小 draft model 加速生成；可作为跨机分布式协议实现）。  本章结束第八部分。Tobia...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 t...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速...
  c26_b03.md: ...GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不...
  c26_b03.md: ...同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located...
  c26_b03.md: ...decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 t...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速...
  c26_b03.md: ...GPU 使用；verify model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不...
  c26_b03.md: ...同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located...
  c26_b03.md: ...decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b04.md: ...ache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighte...
  c26_b05.md: ...测路由，吞吐提升约 25%，p99 延迟降低约 30%。改动是算法级（无硬件改动）；收益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify...
  c26_b05.md: ...n；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：...
  c26_b05.md: ...* 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 tok...
  c26_b05.md: ...测路由，吞吐提升约 25%，p99 延迟降低约 30%。改动是算法级（无硬件改动）；收益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify...
  c26_b05.md: ...n；大 verify model 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：...
  c26_b05.md: ...* 正常 decode：产生 K 个 token 需要 K 步；每步都是完整 verify model 前向传播。 * Speculative decode：1 个 draft 步（快）+ 1 个 verify 步（并行处理 K）产生（接受率 × K）个 tok...
  c26_b06.md: ...Speculative decoding 现在已是生产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费...
  c26_b06.md: ...持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify mod...
  c26_b06.md: ...draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draf...
  c26_b06.md: ...昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Pe...
  c26_b06.md: ...on。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 token 延迟推到不可接受阈值...
  c26_b06.md: ...延迟推到不可接受阈值的工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获...
  c26_b06.md: ...Speculative decoding 现在已是生产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费...
  c26_b06.md: ...持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify mod...
  c26_b06.md: ...draft model 可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draf...
  c26_b06.md: ...昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Pe...
  c26_b06.md: ...on。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 token 延迟推到不可接受阈值...
  c26_b06.md: ...延迟推到不可接受阈值的工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获...
  c26_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示...
  c26_b08.md: ...500 token）去能支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请...
  c26_b08.md: ...释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5 秒。  (e) **客户 SL...
  c26_b08.md: ...=2 秒，p99=30 秒）。诊断并提出修复。  提示  尾部延迟来自长请求阻塞、无长度预测路由、长请求 GPU 竞争、speculative decoding 有帮助。  **答案：** 诊断：p99/p50 比 15:1 表明严重尾部延迟。可能原因：  (a...
  c26_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示...
  c26_b08.md: ...500 token）去能支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请...
  c26_b08.md: ...释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5 秒。  (e) **客户 SL...
  c26_b08.md: ...=2 秒，p99=30 秒）。诊断并提出修复。  提示  尾部延迟来自长请求阻塞、无长度预测路由、长请求 GPU 竞争、speculative decoding 有帮助。  **答案：** 诊断：p99/p50 比 15:1 表明严重尾部延迟。可能原因：  (a...
  c26_b09.md: ...到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部...
  c26_b09.md: ...LA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative decoding 有帮助，但更多是全面改善而非专门尾部。  更深观察：尾部延迟是用户-facing 指标。p50 2...
  c26_b09.md: ...），这 1% 可感知并损害产品。修复尾部延迟常比改善中位数更重要，但得到的工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬...
  c26_b09.md: ...draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络...
  c26_b09.md: ...无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU...
  c26_b09.md: ...答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  2026 年，分布式 speculative decoding 处于早期采用。已发布案例（Together、Anyscale、一些自定义实现）显示规模化下 1.5-...
  c26_b09.md: ...到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部...
  c26_b09.md: ...LA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative decoding 有帮助，但更多是全面改善而非专门尾部。  更深观察：尾部延迟是用户-facing 指标。p50 2...
  c26_b09.md: ...），这 1% 可感知并损害产品。修复尾部延迟常比改善中位数更重要，但得到的工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬...
  c26_b09.md: ...draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络...
  c26_b09.md: ...无跨网络 draft-verify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU...
  c26_b09.md: ...答案。基础设施成本节省（更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  2026 年，分布式 speculative decoding 处于早期采用。已发布案例（Together、Anyscale、一些自定义实现）显示规模化下 1.5-...
  c26_b10.md: ...论：** 辩护：主要架构收益（PagedAttention、RadixAttention、disaggregation、speculative decoding）已部署。剩余优化是增量：调优、硬件刷新、小型协议改进。2022-2024 年 5-10 倍收益不会再...
  c26_b10.md: ...刷新、小型协议改进。2022-2024 年 5-10 倍收益不会再来。  反驳：领域仍快速演进。新技术不断涌现：更好的 speculative decoding（multi-token speculation、EAGLE、Medusa）、MoE 专用优化（exp...
  c26_b11.md: ...请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个...
  c26_b11.md: ...tion + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果...
  c26_b11.md: ...请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个...
  c26_b11.md: ...tion + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果...
  c30_b03.md: ...& Matias, Y. (2023). "Fast Inference from Transformers via Speculative Decoding." _ICML_.   #### 综合与 AI 生成代码（第九部分 — 第27-28章）  * "T...
  c30_b06.md: ...* **Snapshot isolation（快照隔离）**：事务读取一致快照；存在写-写冲突（可能写偏）。 * **Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 veri...

=== tree (63 occurrences) ===
  c11_b11.md: ...跨节点分片数据。哈希分片、范围分片、一致哈希、Bigtable / Cassandra / RocksDB 的 LSM-tree 基础，以及 Anya 在 Stratus DB 集群上无停机重分片的实例。  ---...
  c12_b03.md: ...### 12.4 LSM-Tree：存储底层  大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数...
  c12_b03.md: ...LSM-Tree：存储底层  大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2...
  c12_b03.md: ...大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被...
  c12_b03.md: ...（删除键被丢弃）、减少读时要检查的文件数，并保持磁盘结构有序。   #### 为什么 LSM 在写重工作负载中击败 B-tree  B-tree 更新需要读取相关页（4-16 KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，...
  c12_b03.md: ...、减少读时要检查的文件数，并保持磁盘结构有序。   #### 为什么 LSM 在写重工作负载中击败 B-tree  B-tree 更新需要读取相关页（4-16 KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，但仍付写放大成本...
  c12_b03.md: ...n 开销。  权衡：  * **写吞吐** ：LSM 胜（通常持续写快 10-100 倍）。 * **读延迟** ：B-tree 点查胜（读一页即可；LSM 可能需检查多个 SSTable 和 memtable，Bloom filter 有帮助）。...
  c12_b03.md: ...和 memtable，Bloom filter 有帮助）。 * **范围扫描** ：可比拟；LSM 的有序合并与 B-tree 的叶遍历类似。 * **空间放大** ：LSM 可能有 1.5-3 倍空间开销（compaction 前多个 SSTa...
  c12_b03.md: ...**空间放大** ：LSM 可能有 1.5-3 倍空间开销（compaction 前多个 SSTable 共存）；B-tree 接近 1 倍。  2026 年，LSM 主导分布式数据库存储底层，因为大多数工作负载写重或写吞吐受限。Bigtable...
  c12_b03.md: ...### 12.4 LSM-Tree：存储底层  大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数...
  c12_b03.md: ...LSM-Tree：存储底层  大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2...
  c12_b03.md: ...大多数现代分布式数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被...
  c12_b03.md: ...（删除键被丢弃）、减少读时要检查的文件数，并保持磁盘结构有序。   #### 为什么 LSM 在写重工作负载中击败 B-tree  B-tree 更新需要读取相关页（4-16 KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，...
  c12_b03.md: ...、减少读时要检查的文件数，并保持磁盘结构有序。   #### 为什么 LSM 在写重工作负载中击败 B-tree  B-tree 更新需要读取相关页（4-16 KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，但仍付写放大成本...
  c12_b03.md: ...n 开销。  权衡：  * **写吞吐** ：LSM 胜（通常持续写快 10-100 倍）。 * **读延迟** ：B-tree 点查胜（读一页即可；LSM 可能需检查多个 SSTable 和 memtable，Bloom filter 有帮助）。...
  c12_b03.md: ...和 memtable，Bloom filter 有帮助）。 * **范围扫描** ：可比拟；LSM 的有序合并与 B-tree 的叶遍历类似。 * **空间放大** ：LSM 可能有 1.5-3 倍空间开销（compaction 前多个 SSTa...
  c12_b03.md: ...**空间放大** ：LSM 可能有 1.5-3 倍空间开销（compaction 前多个 SSTable 共存）；B-tree 接近 1 倍。  2026 年，LSM 主导分布式数据库存储底层，因为大多数工作负载写重或写吞吐受限。Bigtable...
  c12_b04.md: ...#### 常见误解  常见混淆：“LSM 适合写重；B-tree 适合读重。”部分正确但过于简化。LSM 的读性能对许多工作负载可与 B-tree 竞争（带 Bloom filter...
  c12_b04.md: ...常见混淆：“LSM 适合写重；B-tree 适合读重。”部分正确但过于简化。LSM 的读性能对许多工作负载可与 B-tree 竞争（带 Bloom filter 的点查；范围扫描受益于有序 SSTable）。真正区别在写路径：LSM 把随机写转...
  c12_b04.md: ...SSTable）。真正区别在写路径：LSM 把随机写转化为顺序写（通过 WAL 和 memtable 批量），这是 B-tree 无法比拟的结构优势。在现代 SSD 工作负载中，LSM 的优势是 _可预测_ 写吞吐；读性能通常够用。   ### 1...
  c12_b05.md: ...年移动略出问题的经验结晶。Anya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体...
  c12_b05.md: ...ya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍...
  c12_b05.md: ...逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认...
  c12_b05.md: ...B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解...
  c12_b05.md: ...M-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL In...
  c12_b05.md: ...节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Ora...
  c12_b05.md: ...**Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读...
  c12_b05.md: ...分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间...
  c12_b05.md: ...nnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间放大。 * 成熟运维工具（数...
  c12_b05.md: ...读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间放大。 * 成熟运维工具（数十年优化）。  B+ tree 劣势：  * 随机写成本（每次更新可能修改不同页）。 * 页级锁（或 MVCC 开销）。  现代结论：对读主导、写率较...
  c12_b05.md: ...）。 * 页级锁（或 MVCC 开销）。  现代结论：对读主导、写率较低的 OLTP 工作负载，单节点数据库中的 B+ tree 仍然优秀。对高写吞吐、尤其是分布式场景，LSM 是正确底层。   ### 12.7 练习...
  c12_b05.md: ...年移动略出问题的经验结晶。Anya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体...
  c12_b05.md: ...ya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍...
  c12_b05.md: ...逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认...
  c12_b05.md: ...B+ Tree 作为替代  LSM-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解...
  c12_b05.md: ...M-tree 主导现代分布式数据库存储底层，但 B+ tree（只在叶节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL In...
  c12_b05.md: ...节点存数据的 B-tree 变体）并未过时。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Ora...
  c12_b05.md: ...**Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读...
  c12_b05.md: ...分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间...
  c12_b05.md: ...nnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间放大。 * 成熟运维工具（数...
  c12_b05.md: ...读延迟（一条 root-to-leaf 路径；深度有界）。 * 低空间放大。 * 成熟运维工具（数十年优化）。  B+ tree 劣势：  * 随机写成本（每次更新可能修改不同页）。 * 页级锁（或 MVCC 开销）。  现代结论：对读主导、写率较...
  c12_b05.md: ...）。 * 页级锁（或 MVCC 开销）。  现代结论：对读主导、写率较低的 OLTP 工作负载，单节点数据库中的 B+ tree 仍然优秀。对高写吞吐、尤其是分布式场景，LSM 是正确底层。   ### 12.7 练习...
  c12_b06.md: ...拥有多个 vnode 散布在环上。这带来更均匀负载分布，并把重平衡成本分散到多个邻居。  **Q3.** 描述 LSM-tree 的写路径。为什么它比 B+ tree 写更快？  提示  LSM：先写 WAL（顺序），再写 memtable（内存）...
  c12_b06.md: ...来更均匀负载分布，并把重平衡成本分散到多个邻居。  **Q3.** 描述 LSM-tree 的写路径。为什么它比 B+ tree 写更快？  提示  LSM：先写 WAL（顺序），再写 memtable（内存）。B+ tree：读页、修改、写回（随...
  c12_b06.md: ...径。为什么它比 B+ tree 写更快？  提示  LSM：先写 WAL（顺序），再写 memtable（内存）。B+ tree：读页、修改、写回（随机 I/O）。  **答案：** LSM 写路径：    1. 写追加到磁盘 WAL（write-...
  c12_b06.md: ...ction 合并多个 SSTable。  用户可见写在第 1、2 步后完成；后续 compaction 异步。  B+ tree 写路径：    1. 写必须更新相关叶页（磁盘上 4-16 KB）。   2. 若页未缓存，先读（随机 I/O）。...
  c12_b07.md: ...上也快（但差距没这么大）。  (b) LSM 批量磁盘 I/O：很多写先在 memtable 累积，再一起刷盘。B+ tree 每次页更新都要触碰磁盘。  (c) LSM 把 compaction 成本摊销：合并异步发生，不在用户写路径上。  对...
  c12_b07.md: ...SM 把 compaction 成本摊销：合并异步发生，不在用户写路径上。  对持续高写吞吐，LSM 结构上优于 B+ tree。点查方面，B+ tree 延迟更低（一次 I/O）；LSM 需要检查多个 SSTable（Bloom filter 有...
  c12_b07.md: ...on 成本摊销：合并异步发生，不在用户写路径上。  对持续高写吞吐，LSM 结构上优于 B+ tree。点查方面，B+ tree 延迟更低（一次 I/O）；LSM 需要检查多个 SSTable（Bloom filter 有帮助）。  **Q4.**...
  c12_b09.md: ...访问、SSD 上 100 GB 总数据集。比较并推荐。  提示  写重 favor LSM。SSD 减小但未消除 B+ tree 随机写劣势。  **答案：** 工作负载特征：  * 80% 写、20% 读：写重。 * 随机访问：两种系统都无顺序局...
  c12_b09.md: ...ction 开销：周期后台 CPU 和 I/O 突增。  SQLite 画像：  * 写吞吐：中等。每次写需更新 B+ tree 页；随机更新需页级 I/O。 * 读延迟：非常低（一次点查读一页）。 * 空间放大：~1 倍（原地更新）。 * 运维简...
  c12_b09.md: ...推荐：该工作负载用 RocksDB。80% 写占比意味着 RocksDB 的写吞吐优势占主导；即便在 SSD 上，B+ tree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20% 读占比在 RocksDB 中配合 Bloom fil...
  c12_b11.md: ...nner）能处理哈希无法解决的热键问题，代价是元数据管理。系统会分裂过大的 range 并为负载重平衡副本。  LSM-tree 是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺序、快）；刷成不可变 SSTable；异步...
  c12_b11.md: ...e 和 WAL（顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ tree；B+ tree 在读重单节点 OLTP 中仍然占优。  Anya 的重分片（工作流 7）是在线重平衡：准备新节点、更新...
  c12_b11.md: ...顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ tree；B+ tree 在读重单节点 OLTP 中仍然占优。  Anya 的重分片（工作流 7）是在线重平衡：准备新节点、更新元数据、流式传输...
  c24_b03.md: ...t scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新请求到达时，系统找到最长缓存前缀并复用；只有后缀需要重新计算。  收益：  (a) **共...
  c24_b03.md: ...）。结构是请求规范的一部分；推理引擎知道如何处理。  (c) **函数调用**：原生支持 tool use；radix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输出应用、agent 系...
  c26_b10.md: ...mpt。真实工作负载的 prompt 共享 _前缀_（system prompt + 不同用户消息）。需要 radix tree 或类似前缀索引。  (b) **缓存新鲜度跟踪**。KV cache 会过期（内存压力、TTL）。路由器必须知道哪些缓...
  c30_b02.md: ...uted Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996)....
  c30_b02.md: ...Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & Dabek, F. (2...
  c30_b06.md: ...**Linearizable（线性化）**：每次操作看起来在调用与响应之间某个瞬间生效，与实时一致。 * **LSM-tree（日志结构合并树）**：写优化的存储底层，Bigtable、Cassandra、RocksDB 使用。 * **Meta...
  c30_b14.md: ...├── ch12_lsm/     │   └── tiny_lsm.py       — minimal LSM-tree (memtable + WAL + SSTable + compaction)     ├── ch13_saga/...
  c30_b16.md: ...系统的控制平面需要强一致。每个 AP 系统都有 CP 角落。 | Ch.11 | | 16 | "LSM 适合写重；B-tree 适合读重。" | 过于简化。LSM 的结构优势是把随机写转为顺序写；读性能有竞争力。 | Ch.12 | | 17 |...

=== range (61 occurrences) ===
  c05_b08.md: ...** Postgres 故障切换（用 Patroni 等）通常 30 秒到数分钟；CockroachDB 自动 per-range leader 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 read-af...
  c05_b10.md: ...ncerns）。纪律是问“这个操作需要什么？”而不是“系统默认是什么？”  第二个考虑：Raft-quorum 系统每 range/shard 有 _写吞吐上限_ ，由 Raft 往返成本决定。对每键极端写吞吐的工作负载，异步复制或批量方法可能 ou...
  c05_b11.md: ...真正同时给高可用与正确性的现代替代：Raft 复制 per-range 的多区域系统（CockroachDB）。Raft quorum 可承受单区域丢失；leader 可在另一区域重选；写在...
  c07_b05.md: ...障检测。默认超时：心跳 100 ms，选举 1000-2000 ms。 * **CockroachDB** 用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet...
  c07_b05.md: ...举 1000-2000 ms。 * **CockroachDB** 用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet Paxos。由于 Google 数据中心...
  c11_b03.md: ...roachDB  Question | Answer ---|--- Replication protocol | 每 range Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间戳 Failure-...
  c11_b03.md: ...00 ms）实现线性化时间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aw...
  c11_b03.md: ...口读付 HLC-restart 成本） Strengths | 兼容 Postgres SQL；可部署于任何云；per-range Raft 水平扩展 Weaknesses | 比单区域 Postgres 写延迟更高；运维调优复杂 Right for...
  c11_b05.md: ...stion | Answer（Stratus DB） ---|--- Replication protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-rang...
  c11_b05.md: ...ange Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serializable...
  c12_b02.md: ...B 的范围分片是现代参考设计。  * 键空间被划分为 _ranges_，每个通常 64-512 MB 数据。 * 每个 range 有自己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。...
  c12_b02.md: ...数据。 * 每个 range 有自己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新...
  c12_b02.md: ...己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩...
  c12_b02.md: ...覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数...
  c12_b02.md: ...* Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数据本身也是一个 ra...
  c12_b02.md: ...ge 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数据本身也是一个 range（特殊 range），因此它一致且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range...
  c12_b02.md: ...range（特殊 range），因此它一致且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range 分裂。  分裂机制是关键：热点 range（接收不成比例流量）自动分裂，分散负载。Cassandra 做不到这一点；其...
  c12_b02.md: ...且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range 分裂。  分裂机制是关键：热点 range（接收不成比例流量）自动分裂，分散负载。Cassandra 做不到这一点；其哈希分片意味着热键一直停留在同一分区，直到应...
  c12_b02.md: ...B 的范围分片是现代参考设计。  * 键空间被划分为 _ranges_，每个通常 64-512 MB 数据。 * 每个 range 有自己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。...
  c12_b02.md: ...数据。 * 每个 range 有自己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新...
  c12_b02.md: ...己的 Raft 组（3 副本部署中 3 个副本）。 * 协调者（系统元数据）跟踪哪个 range 覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩...
  c12_b02.md: ...覆盖哪个键区间。 * Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数...
  c12_b02.md: ...* Range 超过阈值时 _分裂_（一个 range 分成两个；各半成为带自己 Raft 组的新 range）。 * Range 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数据本身也是一个 ra...
  c12_b02.md: ...ge 缩太小时与邻居 _合并_。 * 负载不均时，系统通过在不同物理节点间移动副本 _重平衡_。  元数据本身也是一个 range（特殊 range），因此它一致且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range...
  c12_b02.md: ...range（特殊 range），因此它一致且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range 分裂。  分裂机制是关键：热点 range（接收不成比例流量）自动分裂，分散负载。Cassandra 做不到这一点；其...
  c12_b02.md: ...且复制。客户端缓存路由表，未命中时刷新。系统可在持续增长下每分钟处理数千次 range 分裂。  分裂机制是关键：热点 range（接收不成比例流量）自动分裂，分散负载。Cassandra 做不到这一点；其哈希分片意味着热键一直停留在同一分区，直到应...
  c12_b04.md: ...者服务被告知它们）。  新节点暂时不拥有任何数据；它们是备用容量。   #### 步骤 2：重平衡元数据  更新集群的 range 表：把过载节点上的 range 重新分配给新节点。重分配是惰性的：数据尚未移动。  对 Stratus 的 12 节点...
  c12_b04.md: ...时不拥有任何数据；它们是备用容量。   #### 步骤 2：重平衡元数据  更新集群的 range 表：把过载节点上的 range 重新分配给新节点。重分配是惰性的：数据尚未移动。  对 Stratus 的 12 节点加 3 节点，重平衡算法目标是每...
  c12_b04.md: ...惰性的：数据尚未移动。  对 Stratus 的 12 节点加 3 节点，重平衡算法目标是每个节点持有约 1/15 的 range。目前每节点 1/12。新目标大约移动现有数据的 20% 到新节点（总量的 3/15 = 20%，均匀来自旧 12 个节...
  c12_b04.md: ...总量的 3/15 = 20%，均匀来自旧 12 个节点）。   #### 步骤 3：流式传输数据  对每个被重新分配的 range，源节点向目标节点发送 _snapshot_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot...
  c12_b04.md: ...，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。...
  c12_b04.md: ...目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft group 领导权转移给目标节点，源节点的副本被移除。   #### 步骤 4：验证  重平衡完成后，集...
  c12_b04.md: ...* 每个节点的磁盘使用。 * Raft group 健康（无卡住副本、无 underreplication）。  若某 range “卡住”（例如移动期间 Raft group 落后），运维介入。Anya 之前三次重分片中有两次遇到；每次 10 分钟...
  c12_b05.md: ...# 做得对的地方  (a) 协议在线：客户无停机体验。  (b) 整个过程中集群事务一致性保持（Raft 处理 per-range 领导权转移；强一致性不中断）。  (c) 协议可逆：若中间检测到问题，可暂停并回滚飞行中状态。  (d) 协议可并行：...
  c12_b05.md: ...权转移；强一致性不中断）。  (c) 协议可逆：若中间检测到问题，可暂停并回滚飞行中状态。  (d) 协议可并行：多个 range 同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对...
  c12_b05.md: ...(d) 协议可并行：多个 range 同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对 range 大小的元数据过期，重平衡计划可能移动过多或过少数据。修复是定期重新估算，集群...
  c12_b05.md: ...同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对 range 大小的元数据过期，重平衡计划可能移动过多或过少数据。修复是定期重新估算，集群每小时执行。  (b) **流式期间复制滞...
  c12_b05.md: ...# 做得对的地方  (a) 协议在线：客户无停机体验。  (b) 整个过程中集群事务一致性保持（Raft 处理 per-range 领导权转移；强一致性不中断）。  (c) 协议可逆：若中间检测到问题，可暂停并回滚飞行中状态。  (d) 协议可并行：...
  c12_b05.md: ...权转移；强一致性不中断）。  (c) 协议可逆：若中间检测到问题，可暂停并回滚飞行中状态。  (d) 协议可并行：多个 range 同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对...
  c12_b05.md: ...(d) 协议可并行：多个 range 同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对 range 大小的元数据过期，重平衡计划可能移动过多或过少数据。修复是定期重新估算，集群...
  c12_b05.md: ...同时移动，受网络和磁盘吞吐限制。   #### 可能出错的地方  (a) **Range 大小估算错误** ：若系统对 range 大小的元数据过期，重平衡计划可能移动过多或过少数据。修复是定期重新估算，集群每小时执行。  (b) **流式期间复制滞...
  c12_b08.md: ...按 (tenant ID, row ID) 范围分片。** 租户按序排列；每个租户内部行有序。优点：大租户数据可跨多个 range（太大时分裂）；per-tenant 范围扫描高效；小租户共置于同一 range。缺点：小租户相邻意味着热小租户会影响邻...
  c12_b08.md: ...户内部行有序。优点：大租户数据可跨多个 range（太大时分裂）；per-tenant 范围扫描高效；小租户共置于同一 range。缺点：小租户相邻意味着热小租户会影响邻居；range 元数据更复杂。  (c) **每租户一个数据库。** 每个租户有...
  c12_b08.md: ...大时分裂）；per-tenant 范围扫描高效；小租户共置于同一 range。缺点：小租户相邻意味着热小租户会影响邻居；range 元数据更复杂。  (c) **每租户一个数据库。** 每个租户有自己的数据库（或 schema、namespace、p...
  c12_b10.md: ...辩护：带自动分裂的范围分片（CockroachDB、Spanner）能处理哈希分片无法解决的热键问题。系统可检测热点 range 并分裂；哈希分片系统中热键一直停在一个节点直到应用改键。这是运维成熟度。  反驳：哈希分片确实更简单。对工作负载键分布...
  c12_b10.md: ...e 提供两者；选择不是“哪个系统”，而是“哪张表用哪种方案”。  第二个考虑：范围分片的元数据开销真实。对拥有数百万小 range 的系统（大规模 CockroachDB），元数据层成为重要运维关注点。哈希分片规避这一点，代价是失去灵活性。  **Q...
  c12_b11.md: ...裂的范围分片（CockroachDB、Spanner）能处理哈希无法解决的热键问题，代价是元数据管理。系统会分裂过大的 range 并为负载重平衡副本。  LSM-tree 是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺...
  c13_b14.md: ...roup 选出）恢复协议。  **答案：** 在 CockroachDB 中，事务协调者状态作为记录存在 system range 中；该 range 本身被 Raft 复制。若运行协调者角色的节点死亡：  (a) 协调者状态 range 的 Raf...
  c13_b14.md: ...协议。  **答案：** 在 CockroachDB 中，事务协调者状态作为记录存在 system range 中；该 range 本身被 Raft 复制。若运行协调者角色的节点死亡：  (a) 协调者状态 range 的 Raft group 检测...
  c13_b14.md: ...ystem range 中；该 range 本身被 Raft 复制。若运行协调者角色的节点死亡：  (a) 协调者状态 range 的 Raft group 检测到故障（心跳超时）并在数秒内选出新 leader。  (b) 新 leader 继承协调...
  c14_b02.md: ...Spanner for the rest of us”，因为它提供类似保证而无 GCP 锁定。  关键属性：  * 每 range 使用 Raft 复制。 * HLC-based MVCC，NTP 偏移上界（默认 500 ms）。 * Postgre...
  c14_b07.md: ...* SQL（通常兼容 Postgres 或 MySQL 线缆）。 * 可串行化或线性化事务的 ACID。 * 跨分片/range 水平扩展。 * 多区域部署下的一致事务。  代价：比单节点 Postgres 更高的每写延迟（共识往返）；更多运维复杂...
  c14_b07.md: ...chDB ---|---|---- Replication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos leader 切换（~数秒） | Raft leader...
  c18_b08.md: ...us DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：...
  c18_b08.md: ...ing token。描述存储层如何强制执行 token 顺序。  提示  每次 range 写入包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举...
  c28_b05.md: ...p） | E.2（Consul gossip 过载） | | 5 | 复制 | Raft（CockroachDB 每个 range）、Viewstamped（TigerBeetle） | E.5（无 fencing 的 failover） | | 6...
  c28_b05.md: ...| 11 | 诊断方法 | 用于组件选择 | n/a | | 12 | 分片、LSM | CockroachDB 基于 range；底层 LSM | n/a | | 13 | 分布式事务 | CockroachDB 可串行化；Percolator 风...
  c28_b10.md: ...§12.5 | N 节点集群磁盘利用率高；需加 M 节点 | 再平衡集群，零停机，事务一致性保留 | 一致性哈希 + range streaming + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记...
  c28_b10.md: ...致性保留 | 一致性哈希 + range streaming + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | | 8 | Olam...
  c30_b04.md: ...数据读 | CP | PC/EC | 全局线性化工作负载；仅限 GCP | | **CockroachDB** | 每 range 一个 Raft；HLC + 偏移 | Raft 重新选举；每 range 亚秒级 | 可串行化 + follower...
  c30_b04.md: ...| **CockroachDB** | 每 range 一个 Raft；HLC + 偏移 | Raft 重新选举；每 range 亚秒级 | 可串行化 + follower read | CP | PC/EC | 多 region Postgres...

=== olamide (60 occurrences) ===
  c01_b01.md: ...亚软件工程师——用手机购买了一份 6 雷亚尔的数据包。她只付了一次款。手机显示一张收据。她的银行账单却显示两笔扣款。  Olamide 在 3:04 被告警叫醒。到 3:30，她已经在查看 trace。两个协调者（coordinator）节点分别完成了支...
  c01_b03.md: ...答不了，那你还不理解这个系统，无论你对它的 API 有多熟悉。  本书的四位主角会在不同系统、不同领域反复执行这个循环。Olamide 运行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobia...
  c01_b08.md: ...中作为完整演练出现；能否独立执行每一个，是检验你是否吸收对应章节的实操测试。  现在回到 Adaeze。早上 6:00，Olamide 有了修复方案。到 9:00，她完成了事后复盘。修复涉及 fencing token（第18章）、混合逻辑时钟（第3章）...
  c01_b08.md: ...下周进行了一次 game day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuki 和 Tobias 最终在第28章一起交付的系统。第1章到第28章之间的路径是本书正文；第29章则前...
  c05_b01.md: ...Olamide 团队的一名新工程师写道：  > “我加了一个读副本来分担主库读负载。延迟现在很棒。用户仪表盘会在存款后立即显示余额。完...
  c05_b08.md: ...#### 应用问题  **Q1.** Olamide 的团队正在决定 Crescent 支付系统用 Postgres + 读副本（异步）还是 CockroachDB。列出他...
  c06_b04.md: ...不成立：US-East 与新加坡之间跨区域 Raft quorum 单程 200 ms、往返 400 ms，会超支。  Olamide 的团队专门选择这三个区域，把 Raft quorum 延迟压在 100 ms 以下。这是延迟预算能显式化的那种运维决策...
  c08_b08.md: ...案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPARE(b=10)` 给所有 5 个。 * B 发送...
  c08_b08.md: ...个 promise，ballot 20。  **B 的 accept：**  * B 发送 `ACCEPT(20, "Olamide")` 给所有 5 个。 * 全部 5 个接受。B 收到 5 个 accepted 消息。值 “Olamide” 被选定...
  c08_b08.md: ...20, "Olamide")` 给所有 5 个。 * 全部 5 个接受。B 收到 5 个 accepted 消息。值 “Olamide” 被选定。  **A 的重试：**  * A 的 `prepare(10)` 失败（只有 2 个 promise）。A...
  c08_b08.md: ...30)`。 * 所有 5 个 acceptor 的 $b_{max}=20$ 且 `accepted_value = "Olamide"`（`accepted_ballot = 20`）。它们回复 `PROMISE(30, 20, "Olamide")`...
  c08_b08.md: ...= "Olamide"`（`accepted_ballot = 20`）。它们回复 `PROMISE(30, 20, "Olamide")`。 * A 收到多数 promise。根据 accept 规则，A 必须提议 “Olamide”（来自最高 acc...
  c08_b08.md: ...(30, 20, "Olamide")`。 * A 收到多数 promise。根据 accept 规则，A 必须提议 “Olamide”（来自最高 accepted_ballot 的值）。 * A 发送 `ACCEPT(30, "Olamide")`。全...
  c08_b08.md: ...须提议 “Olamide”（来自最高 accepted_ballot 的值）。 * A 发送 `ACCEPT(30, "Olamide")`。全部 5 个接受（它们已有 ballot 20 的 “Olamide”；更新到 ballot 30 的相同值）。...
  c08_b08.md: ...。 * A 发送 `ACCEPT(30, "Olamide")`。全部 5 个接受（它们已有 ballot 20 的 “Olamide”；更新到 ballot 30 的相同值）。 * A “选定”了 “Olamide”——但该值已在 ballot 20...
  c08_b08.md: ...（它们已有 ballot 20 的 “Olamide”；更新到 ballot 30 的相同值）。 * A “选定”了 “Olamide”——但该值已在 ballot 20 时被选定。  系统收敛到单一值 “Olamide”。A 原本想提议 “Anya”...
  c08_b08.md: ...相同值）。 * A “选定”了 “Olamide”——但该值已在 ballot 20 时被选定。  系统收敛到单一值 “Olamide”。A 原本想提议 “Anya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集...
  c11_b01.md: ...Olamide 坐在她的 CTO 对面。CTO 距下个会议还有 30 分钟。议题：Crescent 是否应把支付账本从 Postgre...
  c11_b01.md: ...还有 30 分钟。议题：Crescent 是否应把支付账本从 PostgreSQL 迁移到 CockroachDB？  Olamide 没时间通读 CockroachDB 文档。也不需要。她有一套诊断方法，对任何曾在生产环境部署过的分布式数据库，30 分...
  c11_b03.md: ...—四个问题可泛化。训练技能：从结构性属性而非营销声明的视角阅读厂商文档；对某工作负载是否适合该系统给出可辩护的判断。  Olamide 把方法用于五个系统。输出是每个系统的一页阅读笔记。   #### Spanner（Google）  Question...
  c11_b05.md: ...### 11.4 Olamide 的决定  Olamide 为 CTO 总结：  > “对 Crescent 支付工作负载，选择落在 Spanner 和...
  c11_b05.md: ...### 11.4 Olamide 的决定  Olamide 为 CTO 总结：  > “对 Crescent 支付工作负载，选择落在 Spanner 和 CockroachDB...
  c11_b11.md: ...库的 CAP 和 PACELC 位置。方法可泛化；它适用于你没用过的系统，因为四个问题在任何厂商文档中都有确定答案。  Olamide 对 Crescent 的决定（CockroachDB 胜过 Spanner、DynamoDB、Cassandra、Fo...
  c13_b01.md: ...Olamide 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Ad...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN...
  c13_b14.md: ...#### 应用问题  **Q1.** Olamide 的支付事务触及三个区域的三个分片。追踪若事务协调者在提交中途死亡会发生什么。  提示  协调者状态通过 Raft 复制...
  c13_b17.md: ...的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide 的跨区域支付事务在 CockroachDB 中约 120 ms 原子提交到三个区域，serializable 隔离且完...
  c14_b06.md: ...### 14.11 实例：Crescent 的架构  2026 年，Olamide 的 Crescent 支付平台使用三种数据系统：  (a) **CockroachDB** 用于运营存储：账户、交易、...
  c18_b11.md: ...它的失败模式是什么？进程可能暂停多久？”  Crescent 修复组合 etcd + fencing token，因为 Olamide 团队问了这些问题。LLM 没问，且 LLM 生成的代码会重新引入重复扣款 bug。   ### 本章总结  分布式缓存...
  c19_b01.md: ...Olamide 的微服务舰队跨三个区域运行 47 个服务。每个服务有 3-12 个实例。实例来来去去（部署、自动扩缩、故障）。每天都反...
  c19_b04.md: ...图复杂（50+ 服务、多区域、多协议）的团队，服务网格至关重要。   ### 19.7 Crescent 的发现架构  Olamide 的 Crescent 使用：  * **Kubernetes 原生服务发现** 用于区域内服务间流量。每个服务是 Ku...
  c22_b01.md: ...凌晨 2:17，on-call 告警响起：“面向客户的错误率飙升到 14%。” Olamide 打开监控面板。每个节点都是绿色。CPU 正常。内存正常。健康检查都返回 200 OK。面板是绿的；客户却遇到错误。...
  c22_b01.md: ...metastable failures，2021 年的形式化框架）是最难检测和恢复的生产故障模式。本章讲两者。  愚弄 Olamide 监控的失败，部分源于 LLM 对健康检查的默认建议：       @app.route('/healthz')...
  c22_b11.md: ...Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c23_b01.md: ...Olamide 正在策划 Crescent 首次正式混沌工程 game day。她的团队已运行两年精心设计的服務；他们尚未演练这些故障...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 Cockr...
  c23_b04.md: ...### 23.6 工作流 11：Olamide 的第一次 Game Day  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群 EU-West 区域故障。...
  c23_b04.md: ...* 14:00 UTC：团队集合；回顾回滚计划；观察员进入 on-call 室。 * 14:10 UTC：实验开始。Olamide 对 EU-West 节点调用 CockroachDB “drain” 命令（优雅关闭）。 * 14:15 UTC：EU...
  c23_b09.md: ...#### 应用问题  **Q1.** Olamide 正在策划一次测试区域故障转移的 game day。走一遍她在当天前需要回答的设计问题。  提示  稳态假设、爆炸半径、...
  c23_b10.md: ...件下触发，更灵活。  (d) **团队带宽**：小团队带宽有限必须优先；暂停一件事做另一件事。大团队可并行推进。  对 Olamide 的假设场景（故障转移下重复扣款）：bug 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂...
  c23_b13.md: ...l 3（持续）、Level 4（混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调优、面板缺口、真实重复扣款 bu...
  c27_b01.md: ...Olamide 正在审查队友提交的 PR。该 PR “AI 辅助”；大部分代码由 LLM 生成。代码实现了一个跨 region 支付协...
  c27_b07.md: ...me-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证...
  c27_b11.md: ...#### 应用问题  **Q1.** 走一遍 Olamide 的 30 行 PR 审查。她在前 30 行看到全部六个谎言。构造一个包含它们的合理代码片段。  提示  一个跨 reg...
  c27_b12.md: ...读是线性化的；没有显式 `WITH (LINEARIZABLE)` 或类似，读可能来自有旧数据的 follower。  Olamide 的审查反馈：“拒绝。这段代码包含 Thread C 全部六个谎言。具体：bank_service.transfer 缺...
  c27_b12.md: ...成本高（数据污染、中断、客户影响）。对非分布式代码（UI、简单 CRUD 等），较轻政策即可。  对 Crescent：Olamide 在第1章原始重复扣款事故后起草了类似政策。三年后，团队已内化纪律；AI 辅助 PR 按清单审查；混沌测试是标准；生产中...
  c27_b15.md: ...对 Crescent：Olamide 事故后三年建立了以上每项。团队分布式代码质量显著改善；涉及六个谎言的新事故罕见。投资（审查时间、混沌测试、培训）巨大；...
  c27_b15.md: ...纪律；跳过任何一层都会产生可预测缺口。对 AI 审查 PR 橡皮图章是被点名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模...
  c28_b01.md: ...时间是 2026-09-15。四位主角一起交付了多 region 支付平台。  Olamide 是 SRE 主管。Anya 是数据库主管（Stratus DB，现在内嵌为 Crescent 的 commit-log...
  c28_b07.md: ...### 28.5 四位主角的视角  每位主角都有贡献和收获：  **Olamide（SRE 主管）**：“架构健壮，因为我们提前支付了分布式系统正确性的成本。三年前，我们交付的系统内置了六个谎言；我们用...
  c28_b10.md: ...ange 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 sha...
  c28_b10.md: ...板 + 结构防御的五 Whys | “我们增加了超时” 却不改变类别；遗漏亚稳态反馈环；指责导向复盘 | | 11 | Olamide 的混沌演练日 | Ch.23 §23.6 | 假设故障模式 + 稳态 SLI | 验证防御；记录学习；行动项 | 演练...
  c28_b14.md: ...：诊断方法适用于任何系统；三个谎言姿态揭示防御（或缺失）；两者让工程师无需通读每页文档就能评估系统。  **Q2.** Olamide 正在指导新工程师。工程师问：“分布式系统最重要的是学什么？” 构造 Olamide 的回答。  提示  心智模型、纪律...
  c28_b14.md: ...档就能评估系统。  **Q2.** Olamide 正在指导新工程师。工程师问：“分布式系统最重要的是学什么？” 构造 Olamide 的回答。  提示  心智模型、纪律、愿意假设最坏情况。  **答案：** Olamide 可能的回答：  "最重要学会...
  c28_b14.md: ...统最重要的是学什么？” 构造 Olamide 的回答。  提示  心智模型、纪律、愿意假设最坏情况。  **答案：** Olamide 可能的回答：  "最重要学会的是心智动作：假设每个组件都在时间、身份、故障上说谎。然后问有什么防御。然后用混沌工程验证...
  c28_b15.md: ..."  这个回答反映本书中心论点：心智模型是持久知识；具体系统和算法是应用。新工程师的投资应在模型，而非记忆算法。  对 Olamide 来说，这种指导风格是她打造团队的方式。团队的集体纪律让 Crescent 系统健壮；这种纪律比任何单个工程师的具体知识...
  c29_b04.md: ...5 结语  你在 2026、2027 或更晚读这本书。具体年份影响哪些具名系统仍 current。它不影响心智模型。  Olamide、Anya、Yuki 和 Tobias 是复合人物；他们的系统和事件是作者工作和评审过的生产架构的真实组合。平台设计（分...
  c30_b10.md: ...blox 2021、GitHub 2018 杠杆最高）。 * 对每个案例研究，问：六个谎言中哪些在起作用？缺失什么防御？Olamide 在此之前会运行什么 game day？  * * *   ### 附录 F — 生产决策树  针对五种常见工作负载模式...
  c30_b12.md: ...常见反模式：何时不该伸手选显而易见的选择  上述决策树偏向 “选什么”。同样重要的是何时 _不_ 选诱人的系统。以下是 Olamide 团队在新晋工程师身上看到的最常见 2026 反模式：  **刚遇到扩展 pain 就不要伸手选 NewSQL。** 一...

=== aws (59 occurrences) ===
  c03_b07.md: ...Google 之外罕见  TrueTime 需要 GPS、原子钟、软件栈和运维纪律。大多数公司数据中心里没有 GPS。AWS 等云厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不...
  c03_b07.md: ...mazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业等价物。...
  c03_b14.md: ...认。TrueTime 对能负担 GPS 基础设施且工作负载中重启成本超过 commit-wait 成本的团队是正确答案。AWS 的 clock-bound API（Amazon Time Sync Service）首次在 Google 之外提供...
  c03_b15.md: ...]` 不确定性区间。Spanner 用 TrueTime 支付 7 ms commit-wait，换取线性化全局时间戳。AWS 的 clock-bound API 正在让 TrueTime 类原语在云厂商上可用。HLC 加已知偏移上界是该领域其余...
  c10_b10.md: ...C 沦陷”是否现实威胁？对大型云厂商控制面，可能确实（内部威胁、定向攻击）。对大多数其他系统，BFT 成本超过威胁概率。AWS、GCP、Azure 的元数据存储使用崩溃容错共识（加上强物理安全和访问控制）而非 BFT。威胁真实，但通过其他手段管理...
  c11_b04.md: ...| 默认 PA / EL（低延迟、最终）；条件写操作 PC / EC Strengths | 免运维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访...
  c11_b04.md: ...操作 PC / EC Strengths | 免运维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访问模式可预测的单租户键值工作负载；AWS 原生系统...
  c11_b04.md: ...| 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访问模式可预测的单租户键值工作负载；AWS 原生系统   #### Cassandra  Question | Answer ---|--- Replicatio...
  c11_b07.md: ...Aurora DSQL 是 AWS 的 serverless、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres 兼容 Spanner...
  c11_b07.md: ...Aurora DSQL 是 AWS 的 serverless、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres 兼容 Spanner 风格系统。复制协议基于 Firecracker 每租户微虚拟机与...
  c11_b07.md: ...tgres 逻辑多花约 5-10 ms）。优势：serverless 计费、Postgres 兼容、可跨区。劣势：仅限 AWS；较新（仍在实战中检验）；每租户 Firecracker 模型不常见，可能有意外运维特征。适合：希望多区域且 serve...
  c13_b05.md: ...多数非 Google 分布式数据库使用 HLC + 偏移上界。成本可变（取决于争用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 T...
  c13_b16.md: ...深入思考  **Q1.** 辩护或反驳：“2026 年所有金融系统都应使用 TrueTime 风格（Spanner 或 AWS Time Sync Service）。HLC 系统是一种妥协。”  **讨论：** 辩护：金融系统对事务排序错误零容忍...
  c13_b16.md: ...行多年，无需 TrueTime；经验证据表明 HLC 有效。TrueTime 的成本（GPS 基础设施、GCP 锁定或 AWS Time Sync Service 依赖）真实，而边际正确性收益对大多数工作负载很小。  综合：TrueTime 是更...
  c13_b16.md: ...赖）真实，而边际正确性收益对大多数工作负载很小。  综合：TrueTime 是更干净的方案；HLC 对大多数团队更实用。AWS 引入 clock-bound API 正在缩小差距；5 年内我们可能看到 HLC 系统迁移到 clock-bound...
  c13_b16.md: ...-bound API，差别变得无关紧要。目前选择是运维性的：若你在 GCP 且成本可接受，用 TrueTime；若你在 AWS/Azure/多云且运维简单性重要，用 HLC。  更微妙考虑：TrueTime 的 commit-wait 是每次写...
  c13_b17.md: ...可配置时钟偏移上界作为实用的 TrueTime 替代。成本可变（争用时重启）而非恒定（每次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。...
  c14_b01.md: ...wSQL；兼容 MySQL。 * **YugabyteDB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon...
  c14_b01.md: ...DB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：serverless Postgres，存储与...
  c14_b01.md: ...ime 的时间戳避免不确定性重启。 * 单一全球实例可跨多区域（或仅区域）。  劣势：  * 仅限 GCP。不能部署在 AWS 或 Azure。 * 昂贵（按节点小时 + 存储和 IO 计费）。 * 相比完整 Postgres，某些 SQL 功能...
  c14_b03.md: ...### 14.6 Aurora DSQL  AWS 2024 GA 的分布式 SQL 数据库。架构与上述其他系统不同。  关键属性：  * 每租户 Firecracker...
  c14_b03.md: ...计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 A...
  c14_b03.md: ...S 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 AWS。 * 较新（生产 track record 不如 CockroachDB 或 Spanner）。 * 每租户 Fire...
  c14_b03.md: ...r）。 * 每租户 Firecracker 开销可能让习惯多租户数据库引擎的团队意外。  Aurora DSQL 适合：AWS 原生工作负载，需要多区域 Postgres 又不想管理 CockroachDB 集群。新兴示例：AWS 客户从 Aur...
  c14_b03.md: ...DSQL 适合：AWS 原生工作负载，需要多区域 Postgres 又不想管理 CockroachDB 集群。新兴示例：AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Ser...
  c14_b09.md: ...#### 应用问题  **Q1.** 一个团队在 AWS 上运行 PostgreSQL，数据库已增长到 10 TB。单 Aurora Postgres 实例峰值 CPU 60%...
  c14_b09.md: ...是迁移到 CockroachDB。走一遍权衡。  提示  手动分片：工作量最大、控制最大。Aurora DSQL：托管、AWS 原生、较新。CockroachDB：托管或自托管、多云、成熟。  **答案：**  _选项 1：手动分片_。把 10...
  c14_b09.md: ...应用层）、跨分片事务需要应用层 saga 或 2PC、运维复杂度倍增。  _选项 2：Aurora DSQL_。迁移到 AWS 托管 NewSQL。优点：serverless 计费（无空闲容量）、兼容 Postgres（大部分）、多区域可用、AW...
  c14_b09.md: ...WS 托管 NewSQL。优点：serverless 计费（无空闲容量）、兼容 Postgres（大部分）、多区域可用、AWS 原生集成。缺点：AWS 锁定加深、较新且生产记录较少、每租户 Firecracker 开销可能意外。  _选项 3：C...
  c14_b09.md: ...。优点：serverless 计费（无空闲容量）、兼容 Postgres（大部分）、多区域可用、AWS 原生集成。缺点：AWS 锁定加深、较新且生产记录较少、每租户 Firecracker 开销可能意外。  _选项 3：CockroachDB_。...
  c14_b09.md: ...ra 分开运维、付费产品（企业功能需 license）。  推荐因素：  * 若团队可预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：Cockr...
  c14_b09.md: ...若团队可预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：CockroachDB。 * 若团队增长已放缓且希望最小架构改动：继续用 Aurora...
  c15_b09.md: ...namoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时钟偏移窗口内），一个会胜出，另一个被丢弃。  从一个区域视角，它自...
  c19_b03.md: ...口；更丰富元数据需要 SRV 或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务...
  c19_b04.md: ...Consul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53...
  c19_b04.md: ...己的领域；跨领域交互通过明确定义的 API。这比 “全用 etcd” 更复杂，但匹配部署拓扑（Kubernetes + AWS + 跨区域），并让每个系统做其最擅长的。   ### 19.8 练习...
  c19_b10.md: ...EC2 实例启动时通过 Consul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通...
  c19_b10.md: ...ul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS 解析。  (c) *...
  c19_b10.md: ...nsul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出 Kubernet...
  c19_b11.md: ...M？混合？云？本地？你们团队目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的...
  c19_b11.md: ...分层系统：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每个处理自己的领域；跨领域交互明确。  第20章开始第七部分：运营分布式系统。分...
  c22_b03.md: ...+ 重试预算 + 熔断器。每层防御不同层面；共同防止灰色到亚稳态的级联。   ### 22.5 真实案例   #### AWS S3 2017 中断  一次运维任务中的打字错误（删除的容量超过预期）让 S3 的索引子系统容量不足。恢复慢，因为索引...
  c22_b08.md: ...成请求，检测被动观察可能遗漏的灰色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由...
  c22_b10.md: ...不复杂的系统暴露更少。  综合：复杂分布式系统会发生亚稳态故障。Crescent 支付平台、Google Search、AWS S3 都足够复杂，已展示过亚稳态。简单系统可能确实避免它。准备包括 (a) 避免不必要的复杂度，(b) 为反馈循环加仪...
  c22_b11.md: ...vs 亚稳态：级联是故障跨组件扩散；亚稳态是故障在单个组件或系统内自我维持。两者可共存；都需要各自防御。  真实案例：AWS S3 2017、Google Magic Pony（非正式）、Roblox 2021。都特征为小触发器、反馈循环、自维...
  c23_b02.md: ...基于 Helm 部署；可扩展。  (c) **Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合...
  c23_b02.md: ...：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e) **Toxiproxy**：TC...
  c23_b02.md: ...Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e) **Toxiproxy**：TCP 层故障注入。慢连接、丢包、模拟下游变慢。与应用无关。  选择...
  c23_b02.md: ...模拟下游变慢。与应用无关。  选择取决于部署：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...
  c29_b01.md: ...Time 仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL...
  c29_b01.md: ...-2030 轨迹：  * 预计 **CockroachDB、TiDB、YugabyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2...
  c29_b01.md: ...型。云厂商被攻破对高 stakes 数据不再是假设。 * **机密计算**（Intel TDX、AMD SEV-SNP、AWS Nitro Enclaves）解决 “云中静态数据” 威胁，但不解决 “云被攻破下控制面完整性” 威胁。BFT 解决后...
  c30_b04.md: ...amoDB** | 无领导者；quorum | 透明 | 默认最终；请求时可强一致 | AP 默认 | PA/EL | AWS 原生 KV；可预测延迟 | | **Cassandra** | 无领导者；可调 | 基于 gossip；10-30 秒...
  c30_b07.md: ...有技术触发、放大机制、恢复和能防止它的章节映射防御。建议作为第21-23章后的深度延伸阅读。   #### E.1 — AWS S3 (US-EAST-1) 故障，2017-02-28  * **触发**：工程师为 S3 计费子系统运行例行调试命...
  c30_b07.md: ...又阻止系统追上。恢复需要重启索引子系统，但重启本身需要限制新流量。 * **时间线**：4 小时客户可见影响；许多依赖 AWS 的服务（Slack、Quora、Trello、Medium）中断。 * **恢复**：AWS 工程师限制入流量、重启索...
  c30_b07.md: ...时客户可见影响；许多依赖 AWS 的服务（Slack、Quora、Trello、Medium）中断。 * **恢复**：AWS 工程师限制入流量、重启索引子系统、逐步恢复容量。 * **章节映射防御**：     * Ch.22（亚稳态故障）：系...
  c30_b10.md: ...要多 region 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aurora PostgreSQL 用于托管简单，但每 IOPS 计费更高。   #### F.2 — 面向全球用...
  c30_b10.md: ...具。 * **替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DSQL。...
  c30_b11.md: ...容读用最终一致，唯一声明操作（用户名保留等）用条件写入。 * **为何这个组合**：读规模占主导；最终一致可接受；对仅 AWS 部署，可接受厂商锁定（DynamoDB）。 * **替代**：更高成本下 Spanner 提供更强一致性；Cassan...

=== happens (56 occurrences) ===
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Or...
  c03_b02.md: ...lel b。并发在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另一个）；有些没有（它们并发，任何顺序都同样有...
  c03_b02.md: ...个）；有些没有（它们并发，任何顺序都同样有效）。   #### 为什么这是正确的原语  本章开头的聊天消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消...
  c03_b02.md: ...消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之...
  c03_b02.md: ...一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-cl...
  c03_b02.md: ...来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happe...
  c03_b02.md: ...ppens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳...
  c03_b02.md: ...时钟实现  > **工作流 1（输入 → 输出）** ：给定跨 N 个进程的本地 + 发送 + 接收事件序列，产出尊重 happens-before 的逐事件时间戳。本节走 Lamport 时间戳；§3.4 扩展到向量时钟（还能检测并发）；§3.5 扩展...
  c03_b02.md: ....4 扩展到向量时钟（还能检测并发）；§3.5 扩展到 HLC（贴近 wall 时间）。该工作流的 _输出_ ——尊重 happens-before 的时间戳——三者相同；_成本_ 和 _检测能力_ 不同。变体：为小成本日志排序选 Lamport；为 C...
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Or...
  c03_b02.md: ...lel b。并发在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另一个）；有些没有（它们并发，任何顺序都同样有...
  c03_b02.md: ...个）；有些没有（它们并发，任何顺序都同样有效）。   #### 为什么这是正确的原语  本章开头的聊天消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消...
  c03_b02.md: ...消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之...
  c03_b02.md: ...一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-cl...
  c03_b02.md: ...来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happe...
  c03_b02.md: ...ppens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳...
  c03_b02.md: ...时钟实现  > **工作流 1（输入 → 输出）** ：给定跨 N 个进程的本地 + 发送 + 接收事件序列，产出尊重 happens-before 的逐事件时间戳。本节走 Lamport 时间戳；§3.4 扩展到向量时钟（还能检测并发）；§3.5 扩展...
  c03_b02.md: ....4 扩展到向量时钟（还能检测并发）；§3.5 扩展到 HLC（贴近 wall 时间）。该工作流的 _输出_ ——尊重 happens-before 的时间戳——三者相同；_成本_ 和 _检测能力_ 不同。变体：为小成本日志排序选 Lamport；为 C...
  c03_b05.md: ...法 (l_m)、p 的 wall clock 对此的看法 (t)。新的 l' 必须是最大值，以保证 HLC 单调且尊重 happens-before；它不能小于其中任何一个。因此 l' 至少等于其中之一。四种情况枚举了哪个（或哪些）做出贡献：...
  c03_b06.md: ...么规则有四种而不是三种或五种：每种对应三个可能“赢家”之一，加上平手时的特殊合并情况。  关键不变式：  * **尊重 happens-before** ：a \to b 蕴含 HLC(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近...
  c03_b06.md: ...正确原语：  * **恒定大小** ：每个 HLC 是一个 (l, c) 对，不像向量时钟随 N 扩展。 * **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间*...
  c03_b08.md: ...果消息经过不同服务器（区域分片），HLC 会在任何跨服务器交互中传播 HLC（例如回复携带原消息的 HLC），从而尊重 happens-before。  **修复 2** ：每个聊天室使用向量时钟。向量很小（每个参与者一个条目），能精确检测并发。并发发送...
  c03_b08.md: ...d(messages, key=lambda m: m.timestamp)` 错在把 wall-clock 时间当作 happens-before 的代理。三种修复任一都能让函数正确。选择哪种取决于你的运营容忍度和所需一致性。   ### 3.9 练习...
  c03_b09.md: ...#### 概念检测  **Q1.** 陈述 Lamport 的 happens-before 关系。给出一个因果有序事件的例子和一个并发事件的例子。  提示  三条规则：同进程排序、消息先发送后接收...
  c03_b09.md: ...一个并发事件的例子。  提示  三条规则：同进程排序、消息先发送后接收、传递性。并发是三条都不满足。  **答案：** Happens-before，记作 a \to b，由三条规则定义：(1) 若 a 和 b 在同一进程且 a 先发生，则 a \to...
  c03_b09.md: ...P_1 向键值存储写入 x = 5；P_2 读取含 x = 5 的响应，然后写入 y = 10。P_1 对 x 的写入 happens-before P_2 对 y 的写入，因为读响应是 P_2 上由 P_1 发送的消息接收，而 y 的写入又发生在同一进...
  c03_b09.md: ...P_1 在某 wall-clock 时刻写入 x = 5，同时进程 P_2 写入 y = 10，两者未互相通信。没有 happens-before 链连接它们。它们在技术意义上并发；任一排序都与偏序一致。  **Q2.** 两个事件的 Lamport...
  c03_b09.md: ...amport 时间戳分别为 5 和 7。关于它们的因果关系你能得出什么结论？  提示  Lamport 时间戳单向保持 happens-before，但逆命题不成立。  **答案：** 几乎得不出什么。Lamport 时间戳满足：若 a \to b，则...
  c03_b09.md: ...命题不成立。因此 L(a) = 5 < L(b) = 7 不蕴含 a \to b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），...
  c03_b09.md: ...b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），却不适合 _检测_ 并发的原因。要检测并发需要向量时钟。  **Q3.**...
  c03_b09.md: ...#### 概念检测  **Q1.** 陈述 Lamport 的 happens-before 关系。给出一个因果有序事件的例子和一个并发事件的例子。  提示  三条规则：同进程排序、消息先发送后接收...
  c03_b09.md: ...一个并发事件的例子。  提示  三条规则：同进程排序、消息先发送后接收、传递性。并发是三条都不满足。  **答案：** Happens-before，记作 a \to b，由三条规则定义：(1) 若 a 和 b 在同一进程且 a 先发生，则 a \to...
  c03_b09.md: ...P_1 向键值存储写入 x = 5；P_2 读取含 x = 5 的响应，然后写入 y = 10。P_1 对 x 的写入 happens-before P_2 对 y 的写入，因为读响应是 P_2 上由 P_1 发送的消息接收，而 y 的写入又发生在同一进...
  c03_b09.md: ...P_1 在某 wall-clock 时刻写入 x = 5，同时进程 P_2 写入 y = 10，两者未互相通信。没有 happens-before 链连接它们。它们在技术意义上并发；任一排序都与偏序一致。  **Q2.** 两个事件的 Lamport...
  c03_b09.md: ...amport 时间戳分别为 5 和 7。关于它们的因果关系你能得出什么结论？  提示  Lamport 时间戳单向保持 happens-before，但逆命题不成立。  **答案：** 几乎得不出什么。Lamport 时间戳满足：若 a \to b，则...
  c03_b09.md: ...命题不成立。因此 L(a) = 5 < L(b) = 7 不蕴含 a \to b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），...
  c03_b09.md: ...b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），却不适合 _检测_ 并发的原因。要检测并发需要向量时钟。  **Q3.**...
  c03_b10.md: ...2)。  HLC 贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接收 HLC (105, 2) 按字典序严格大于消息 HLC (105, 1)，也严格大于之前本地 HL...
  c03_b11.md: ...HLC。用 HLC 替换向量时钟。每条消息携带一个 (l, c) 对，而非 10,000 项向量。权衡：HLC 尊重 happens-before 但无法检测并发。如果应用需要并发检测（CRDT 合并、冲突解决），单用 HLC 不够，还需要每键版本向量...
  c03_b14.md: ...破坏本地顺序。  无需同步时钟的修复：用 HLC 加每服务器单调序列号给每条日志行盖戳。HLC 在服务器交换消息时尊重 happens-before（例如，携带 HLC 的请求 ID）。单调序列号保留每台服务器本地顺序。按 HLC 排序显示合并日志，平手...
  c03_b15.md: ...间戳是事件偏序的有损代理。用跨机器时间戳比较来决定“哪个先发生”的代码是在对一个虚构概念做推理。  Lamport 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-before 但不能检...
  c03_b15.md: ...Lamport 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-before 但不能检测并发。向量时钟为每个进程每个事件分配一个整数，能检测并发，但扩展性差。混合逻辑时钟（HLC）结...
  c04_b03.md: ...### 4.5 因果广播（Causal Broadcast）  可靠广播不保序。因果广播新增：_若消息 m_1 在 happens-before 关系下先于 m_2 广播（即 broadcast(m_1) \to broadcast(m_2)），则任...
  c04_b12.md: ...agreement（任一正确接收者交付蕴含所有正确接收者交付）。一致可靠广播修复接收者崩溃后的缺口。因果广播新增尊重 happens-before 的顺序。原子广播新增所有正确进程的全序。  原子广播等价于共识，实现它的协议（Raft、Paxos、ZA...
  c16_b07.md: ...。  (b) **用 HLC 代替 wall clock** ：HLC（第3章）可减少但不能消除偏移问题。HLC 尊重 happens-before；因果有序的两写会被正确打时间戳。并发写仍按 HLC 平票规则排序（本质上是并发集合内的 LWW）。  (...
  c17_b01.md: ...ation 选择。应用代码为每次操作选合适级别。   ### 17.2 因果一致性  经典表述（第3章 Lamport happens-before 在操作上的应用）：若操作 A happens-before 操作 B，则每个进程都先看到 A 再看到 B...
  c17_b01.md: ...17.2 因果一致性  经典表述（第3章 Lamport happens-before 在操作上的应用）：若操作 A happens-before 操作 B，则每个进程都先看到 A 再看到 B。并发操作可任意顺序。  为何有用：  * 会话内 **re...
  c17_b01.md: ...到 B。并发操作可任意顺序。  为何有用：  * 会话内 **read-your-writes** 自然满足（自己的写 happens-before 后续读）。 * **因果性保持** ：对消息的回复排在该消息之后；对评论的子评论排在其父评论之后。 *...
  c17_b04.md: ...作按因果顺序；并发操作可任意。线性化：实时顺序。最终：收敛过程中无顺序保证。  **答案：** 因果一致性：若操作 A happens-before 操作 B（Lamport 意义），则每个进程先看到 A 再看到 B。并发操作（无 happens-bef...
  c17_b04.md: ...所有观众最终应在排行榜上看到 A 在 B 前（或至少系统不能显示 B 的提交仿佛先于 A）。  因果关系：B 的提交 happens-after B 看到 A 的分数；因此 B 的提交 happens-after A 的提交。因果一致保持这一点：所有副...
  c17_b04.md: ...B 的提交仿佛先于 A）。  因果关系：B 的提交 happens-after B 看到 A 的分数；因此 B 的提交 happens-after A 的提交。因果一致保持这一点：所有副本都显示 A 在 B 前。  对两个并发提交（玩家 C 和 D 同时...
  c17_b07.md: ...A 发 “周末过得怎样？” 用户 B 回复 “很棒！”，每个观众都应先看 A 的帖再看 B 的回复。因果关系：B 的帖 happens-after B 看到 A 的帖。  (b) 并发消息可任意顺序：用户 C 和 D 同时发帖且未看到对方，观众可看到不同...
  c27_b03.md: ...无法可靠用 wall clock 排序。应由因果性排序的事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-before、HLC 或 vector clock（第3章）排序。       def merge_events(ev...
  c27_b03.md: ...e_events(events_from_replicas):         # 按 HLC 排序（假设事件带有正确 happens-before 传播的 HLC 时间戳）         return sorted(events_from_repli...
  c28_b10.md: ...-- | | 1 | 逻辑时钟实现 | Ch.3 §3.3 | N 个进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock...
  c30_b06.md: ...故障。 * **HLC（混合逻辑时钟）**：二元组 (l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-...

=== service (56 occurrences) ===
  c03_b07.md: ...S、原子钟、软件栈和运维纪律。大多数公司数据中心里没有 GPS。AWS 等云厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bo...
  c13_b05.md: ...偏移上界。成本可变（取决于争用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到...
  c18_b04.md: ...Coordinator A<br/>(token 100)         participant L as Lock Service<br/>(etcd; Raft)         participant Bank as Bank<br/>(high...
  c19_b03.md: ...* **Kubernetes headless services** ：“headless” Kubernetes service 没有 cluster IP；客户端解析服务名得到所有 pod IP。Kubernetes DNS 插件透明处理。  对...
  c19_b04.md: ...nt 使用：  * **Kubernetes 原生服务发现** 用于区域内服务间流量。每个服务是 Kubernetes Service 对象；客户端解析 `payment.crescent.svc.cluster.local`；kube-proxy 负载...
  c19_b08.md: ...nsul 中断影响。sidecar 异步从 Consul 刷新。  (e) **迁移到集群内发现（Kubernetes service）**：Kubernetes 用 etcd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Ku...
  c19_b08.md: ...netes service）**：Kubernetes 用 etcd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Kubernetes API 中断影响新 service 定义，但不影响已有。对 Kubernetes 内服务，...
  c19_b08.md: ...cd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Kubernetes API 中断影响新 service 定义，但不影响已有。对 Kubernetes 内服务，这通常比 Consul 更弹性。  推荐：短期缓解用 (a) +...
  c20_b05.md: ...n 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duratio...
  c20_b07.md: ...#### 应用问题  **Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（18...
  c20_b07.md: ...#### 应用问题  **Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms...
  c20_b07.md: ...*Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms。延迟预算 300 ms。诊断应把优化...
  c20_b07.md: ...e 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms。延迟预算 300 ms。诊断应把优化重点放在哪里。  提示  识别关键路...
  c20_b07.md: ...大的贡献者。  **答案：** 关键路径是 A → B → C → D，串行，共 470 ms。  最大贡献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 m...
  c20_b07.md: ...→ C → D，串行，共 470 ms。  最大贡献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%...
  c20_b07.md: ...献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 servic...
  c20_b07.md: ...（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 service B**：200 ms 是最大的单一 spa...
  c20_b07.md: ...ervice A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 service B**：200 ms 是最大的单一 span。B 在做什么？若是下游数据库查询，能否优化或缓存？若是复杂计算，能否批量...
  c20_b07.md: ...span。B 在做什么？若是下游数据库查询，能否优化或缓存？若是复杂计算，能否批量、延迟或并行化？  (b) **调查 service D**：180 ms 是第二大。同样问题。  (c) **考虑并行化**：若 A → B → C → D 当前是串行，...
  c20_b07.md: ...；找模式（特定时间窗口、特定请求类型）。  **答案：** 诊断流程：  (a) **按服务分组错误**：80% 来自 service X，是主导集群。聚焦那里。  (b) **检查错误 attributes**：错误码是什么？500 vs 503 vs...
  c20_b07.md: ...*检查错误 attributes**：错误码是什么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug。 * 503：service X 过载。 * 502/504：调用服务与 X 之间网关问题。 * 超...
  c20_b07.md: ...么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug。 * 503：service X 过载。 * 502/504：调用服务与 X 之间网关问题。 * 超时：X 挂起或慢。  (c) **看时间分布**...
  c20_b07.md: ...#### 应用问题  **Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（18...
  c20_b07.md: ...#### 应用问题  **Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms...
  c20_b07.md: ...*Q1.** 某团队分布式 trace 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms。延迟预算 300 ms。诊断应把优化...
  c20_b07.md: ...e 显示：service A（50 ms）→ service B（200 ms）→ service C（40 ms）→ service D（180 ms）。请求总延迟 470 ms。延迟预算 300 ms。诊断应把优化重点放在哪里。  提示  识别关键路...
  c20_b07.md: ...大的贡献者。  **答案：** 关键路径是 A → B → C → D，串行，共 470 ms。  最大贡献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 m...
  c20_b07.md: ...→ C → D，串行，共 470 ms。  最大贡献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%...
  c20_b07.md: ...献者：  * Service B：200 ms（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 servic...
  c20_b07.md: ...（占总 42%） * Service D：180 ms（占 38%） * Service A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 service B**：200 ms 是最大的单一 spa...
  c20_b07.md: ...ervice A：50 ms（11%） * Service C：40 ms（8%）  优化优先级：  (a) **调查 service B**：200 ms 是最大的单一 span。B 在做什么？若是下游数据库查询，能否优化或缓存？若是复杂计算，能否批量...
  c20_b07.md: ...span。B 在做什么？若是下游数据库查询，能否优化或缓存？若是复杂计算，能否批量、延迟或并行化？  (b) **调查 service D**：180 ms 是第二大。同样问题。  (c) **考虑并行化**：若 A → B → C → D 当前是串行，...
  c20_b07.md: ...；找模式（特定时间窗口、特定请求类型）。  **答案：** 诊断流程：  (a) **按服务分组错误**：80% 来自 service X，是主导集群。聚焦那里。  (b) **检查错误 attributes**：错误码是什么？500 vs 503 vs...
  c20_b07.md: ...*检查错误 attributes**：错误码是什么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug。 * 503：service X 过载。 * 502/504：调用服务与 X 之间网关问题。 * 超...
  c20_b07.md: ...么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug。 * 503：service X 过载。 * 502/504：调用服务与 X 之间网关问题。 * 超时：X 挂起或慢。  (c) **看时间分布**...
  c20_b08.md: ...查失败请求的日志**：trace 展示时间线；日志展示服务当时的想法。按 trace ID 关联。  (g) **检查 service X 的指标**：失败时的错误率、延迟、资源使用。X 是否 CPU 受限？内存受限？其下游是否慢？  (h) **在测试...
  c20_b08.md: ...默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 K...
  c20_b08.md: ...传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message he...
  c20_b08.md: ...ct` 原语；应用代码使用它们。  Kafka 传播修复后，团队将看到从 API gateway → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分布式追踪的全部意...
  c20_b08.md: ...复后，团队将看到从 API gateway → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分布式追踪的全部意义。...
  c21_b07.md: ...ing**：每个服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：...
  c28_b02.md: ...metheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务*...
  c28_b03.md: ...subgraph USE["US-East Region"]             APIE[API Service]             REDISE[Redis Cluster]             CRDBE[Cockro...
  c28_b03.md: ...USE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end         subg...
  c28_b03.md: ...subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[Redis Cluster]             CRDBW[Cockro...
  c28_b03.md: ...subgraph USW["US-West Region"]             APIW2[API Service]             CRDBW2[CockroachDB nodes]         end...
  c28_b03.md: ...subgraph USE["US-East Region"]             APIE[API Service]             REDISE[Redis Cluster]             CRDBE[Cockro...
  c28_b03.md: ...USE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end         subg...
  c28_b03.md: ...subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[Redis Cluster]             CRDBW[Cockro...
  c28_b03.md: ...subgraph USW["US-West Region"]             APIW2[API Service]             CRDBW2[CockroachDB nodes]         end...
  c29_b01.md: ...他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；API 已可用但尚未...
  c30_b03.md: ...后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，包括 2024 年在 EC2 上引入微秒级有界时间。 — TrueTime 风格...
  c30_b12.md: ...算。Cassandra 在 100+ 节点集群中才值得，此时其横向扩展是必需的。  **10 个服务不需要时不要伸手选 service mesh。** Istio/Linkerd 增加运维复杂度。对小型服务图，Kubernetes 原生服务发现足够。只有...
  c30_b12.md: ...度。对小型服务图，Kubernetes 原生服务发现足够。只有在多 region、多协议、复杂流量需求真实存在时才采用 service mesh。  **崩溃故障共识足够时不要伸手选 BFT。** BFT 需要 3 倍副本和运维成本。对操作者可信、节点...
  c30_b16.md: ...驱逐和 failover 下失败。用共识 + fencing token。 | Ch.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的一个功能；mesh 还增加 mTLS、流量策略、可观测性——以...
  c30_b19.md: ...rain；无共识的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败）...

=== context (56 occurrences) ===
  c03_b14.md: ...这就是现代可观测性栈（OpenTelemetry，配合正确的追踪上下文传播）实际做的事情。跨服务传播的 trace context 携带 HLC 等价信息的机制，trace ID + span ID + 序列号提供本地顺序的平手打破。...
  c03_b15.md: ...二阶收益：如果请求已经在传播 trace context（用于分布式追踪，第20章），它们已经携带了足以跨服务正确排序日志事件的信息。用 wall clock 的日志系统把这些...
  c19_b11.md: ...域交互明确。  第20章开始第七部分：运营分布式系统。分布式追踪与调试：因果保持、分区下采样、跨共识边界的 trace context。本章将该主题与可观测性工具（Series IV Vol 3）范围隔离，聚焦追踪支持的分布式系统推理。  ---...
  c20_b01.md: ...问题。找出原因就是分布式追踪问题。  本章讨论追踪的分布式系统特定方面：因果保持、分区下采样、跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vol 3（可观测性...
  c20_b02.md: ...#### 关注点 1：因果保持  Trace context（trace ID、parent span ID、采样决策）必须从每个父 span 传播到每个子 span，跨服务边界。...
  c20_b02.md: ...ent`、`tracestate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给...
  c20_b02.md: ...C metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，...
  c20_b02.md: ...ers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）...
  c20_b02.md: ...context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka head...
  c20_b02.md: ...但未传播 trace context，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message attributes）。  * **协议转换时 context 丢...
  c20_b02.md: ...e context（Kafka headers、SQS message attributes）。  * **协议转换时 context 丢失**：服务从 HTTP 转换到自定义二进制协议可能丢失 trace context。修复：在协议边界显式传播逻辑。...
  c20_b02.md: ...es）。  * **协议转换时 context 丢失**：服务从 HTTP 转换到自定义二进制协议可能丢失 trace context。修复：在协议边界显式传播逻辑。  * **重试时 context 损坏**：同一操作重试应产生独立 span（trac...
  c20_b02.md: ...HTTP 转换到自定义二进制协议可能丢失 trace context。修复：在协议边界显式传播逻辑。  * **重试时 context 损坏**：同一操作重试应产生独立 span（trace 中显示重试），而不是与原 span 合并。修复：每次重试创建新...
  c20_b02.md: ...下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  Tail-based sampling（trace 完成后再决策）...
  c20_b02.md: ...样（“保留所有错误 trace；成功 trace 采样 1%”）。   #### 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则共识 apply...
  c20_b02.md: ...## 关注点 3：跨共识边界的 trace context  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存...
  c20_b02.md: ...ce context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的...
  c20_b02.md: ...，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的 Raft 库支持 context 传播；并非所有都支持。  对共识日志是不可变历史（Kafka、区块链）...
  c20_b02.md: ...据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的 Raft 库支持 context 传播；并非所有都支持。  对共识日志是不可变历史（Kafka、区块链）的系统，trace context 是事件数据的...
  c20_b02.md: ...ft 库支持 context 传播；并非所有都支持。  对共识日志是不可变历史（Kafka、区块链）的系统，trace context 是事件数据的一部分。后续处理器使用嵌入的 context。  更深观察：分布式追踪是事件所发生事情的取证记录。对历史记...
  c20_b02.md: ...共识日志是不可变历史（Kafka、区块链）的系统，trace context 是事件数据的一部分。后续处理器使用嵌入的 context。  更深观察：分布式追踪是事件所发生事情的取证记录。对历史记录重要的系统（审计、合规、调试数月前问题），trace c...
  c20_b02.md: ...t。  更深观察：分布式追踪是事件所发生事情的取证记录。对历史记录重要的系统（审计、合规、调试数月前问题），trace context 是系统永久状态的一部分，不只是可观测性 artifact。...
  c20_b03.md: ...到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从...
  c20_b05.md: ...和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至关重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Tra...
  c20_b05.md: ...重要？  提示  没有传播，子 span 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  *...
  c20_b05.md: ...nTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 trace context。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，...
  c20_b05.md: ...辑。  纪律：每次跨服务交互都必须传播 trace context。异步交接（队列、定时任务）需要通过异步机制显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采...
  c20_b05.md: ...-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b06.md: ...主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 trace context 必须跨越共识边界持久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧因果上是下游。没...
  c20_b06.md: ...须跨越共识边界持久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧因果上是下游。没有 context，trace 断裂。  **答案：** 流过共识协议（CockroachDB 事务、Kafka publish-with...
  c20_b06.md: ...日志条目被应用到状态机时，下游效果产生（写磁盘、发送通知等）。  这些下游效果因果上是原始请求的一部分。若 trace context 没有通过共识日志传播，apply 侧的工作看起来是独立的 trace，与发起请求断开。  对调试：若请求因在 appl...
  c20_b06.md: ...对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条目），没有链接 apply 与原始请求的 trace context，无法诊断。  对取证：支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链接，审计链不完整...
  c20_b06.md: ...支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元数据中。apply 代码运行时提取 context，并为它创建的任何 spans 使用。现代 R...
  c20_b06.md: ...没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元数据中。apply 代码运行时提取 context，并为它创建的任何 spans 使用。现代 Raft 库（etcd 的 Raft、raft-rs）直接支持；旧库可能不支...
  c20_b06.md: ...增加。对简单两服务架构，trace 有用但非必需。对内含共识的 50 服务 mesh，trace 是主要调试工具；缺失 context 传播是严重的运维缺口。...
  c20_b08.md: ...正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提示  Trace context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当...
  c20_b08.md: ...e context 默认不会通过 Kafka 消息传播。  **答案：** 默认 Kafka 客户端不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回...
  c20_b08.md: ...不传播 trace context。当 service A 生产消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context...
  c20_b08.md: ...context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.propagate import inject...
  c20_b08.md: ...ue=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为 `traceparent` 和（可选）`tracestate` headers 插入。消费端：       fr...
  c20_b08.md: ...xtract      headers = {k: v for k, v in record.headers}     context = extract(headers)     with tracer.start_as_current_span("c...
  c20_b08.md: ...t(headers)     with tracer.start_as_current_span("consume", context=context):         process(record)   `extract` 读取 headers 并创...
  c20_b08.md: ...s)     with tracer.start_as_current_span("consume", context=context):         process(record)   `extract` 读取 headers 并创建链接到生产者...
  c20_b08.md: ...process(record)   `extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenT...
  c20_b10.md: ...元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（跨服务边界传播 context）、分区下采样（head-based vs tail-based）、跨共识边界的 trace context（Raft、...
  c20_b10.md: ...务边界传播 context）、分区下采样（head-based vs tail-based）、跨共识边界的 trace context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有 vendor-...
  c21_b10.md: ...大。  更深观察：“韧性” 不是单一属性；而是一组模式。LLM 产出 “重试 5 次” 而没有 surrounding context，是缺失架构。正确的 LLM 回应应是：“韧性需要重试预算、熔断器、超时、幂等和 fallback。以下是组合使用每种的...
  c27_b05.md: ...n）。  (h) **一致性模型**：每个操作应有显式一致性选择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应可观测（日志、t...
  c27_b05.md: ...择。验证选择匹配工作负载需求。  (i) **Trace context 传播**：每个跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应可观测（日志、trace、监控）。否则故障在客户投诉前不可见。  检查清单不穷...
  c27_b10.md: ...ion）。  (h) **一致性模型**：每个操作应有显式一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、...
  c27_b10.md: ...一致性选择；验证匹配工作负载需求。  (i) **Trace context 传播**：跨服务调用必须传播 trace context。  (j) **可观测性**：每个操作应被日志、trace、监控。  对每份 PR（尤其 AI 辅助 PR）按此清单审...
  c27_b15.md: ...s-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace context 传播、可观测性。每份分布式代码 PR 按清单审查，捕获高频失败。  为什么 LLM 结构性地不擅长分布式代码：训练数据...
  c28_b04.md: ...(3) US-East 的 regional 负载均衡器路由到健康的 API 服务实例。实例开始支付处理。Trace context（OpenTelemetry W3C `traceparent`）全程传播。  (4) API 服务通过 Redis 检...
  c30_b14.md: ...20_tracing/     │   └── propagation_demo.py — OpenTelemetry context propagation across HTTP + Kafka     ├── ch21_resilience/...
  c30_b16.md: ...| "我们有 OpenTelemetry；追踪已解决。" | 追踪需要在每个异步边界（队列、定时任务）和共识边界传播 context。默认 Kafka 客户端不传播。 | Ch.20 | | 24 | "带指数退避的重试就是韧性。" | 无预算的重试会...
  c30_b19.md: ...1.3（重试预算），附录 E.1、E.6   Span 存在但没有父 span；在 trace 树中孤立 | 异步边界 context 传播中断 | Ch.20 §20.2（因果保留）   同一事务在不同 region 出现多个协调器状态 span |...

=== based (56 occurrences) ===
  c05_b06.md: ...。  **Q3.** 基于 leader 与无 leader 复制有何区别？各举一个生产系统。  提示  Leader-based：一个节点排序写（PostgreSQL、MongoDB、CockroachDB）。Leaderless：任意节点接受写（...
  c05_b06.md: ...DB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往 leader，leader 排序后传播给 fol...
  c05_b06.md: ...切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故障透明，代价...
  c05_b06.md: ...让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的操作。...
  c05_b11.md: ...复制服务于持久性、可用性和读扩展性的某种组合。同步、异步、半同步的选择在写延迟和故障切换后持久性之间权衡。leader-based 与 leaderless 的选择在协调成本和故障切换透明性之间权衡。$W + R > N$ 的法定人数规则给相交，不是...
  c09_b09.md: ...加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提交，被放弃。  **Q3.** 比较 Raft 的 leader-based 设计与无 leader 方案（如 EPaxos、Cassandra LWT）。Raft 因为单 leader 放弃了什...
  c09_b09.md: ...颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的...
  c09_b09.md: ...eader 协议通常缺乏类似工具。  权衡：在简单性和运维成熟度超过 leader 路由延迟下限的系统中，leader-based Raft 是正确默认。在 leader 瓶颈占主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 lea...
  c11_b03.md: ...--- Replication protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTim...
  c11_b03.md: ...on | Answer ---|--- Replication protocol | 每 range Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间戳 Failure-recovery | Raft...
  c11_b05.md: ...---|--- Replication protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切...
  c12_b11.md: ...务模式）、Calvin 的确定性排序、TrueTime 的 commit-wait、CockroachDB 的 HLC-based MVCC 作为实用 TrueTime 替代。四位主角的多区域支付系统在事务层真正成型。  ---...
  c13_b01.md: ...绍 2026 年的现实：CockroachDB、TiDB、YugabyteDB 部署在 NTP 偏移上界之上的 HLC-based MVCC。   ### 13.1 分布式事务问题  _分布式事务_ 是一组跨多个分片（或分区、节点）的操作，必须整体满...
  c13_b05.md: ...### 13.6 CockroachDB 的 HLC-Based MVCC  CockroachDB（以及 TiDB、YugabyteDB）选择了一条不需要 TrueTime 基础设施...
  c13_b13.md: ...er 实例，时间戳形成与实时一致的全序。  7 ms commit-wait 在每次写上支付。成本是 TrueTime-based 线性化的代价，无需为时间戳做协调者往返。...
  c14_b02.md: ...us”，因为它提供类似保证而无 GCP 锁定。  关键属性：  * 每 range 使用 Raft 复制。 * HLC-based MVCC，NTP 偏移上界（默认 500 ms）。 * Postgres 线缆兼容。 * 多区域部署，支持 per-t...
  c14_b02.md: ...。  劣势：  * 运维成本高于单节点 Postgres。 * 某些 Postgres 扩展和功能不支持。 * HLC-based MVCC 需要严格的 NTP 纪律。  CockroachDB 适合：不能接受 Spanner GCP 锁定的多区域...
  c14_b02.md: ...L 线缆。  关键属性：  * TiKV 存储层（每 region/tablet 使用 Raft；Percolator-based 事务）。 * TiDB 计算层（无状态 SQL 前端；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 *...
  c14_b02.md: ...es 的 NewSQL；CockroachDB 的近亲。  关键属性：  * 每分片使用 Raft 复制。 * HLC-based 时间戳。 * 兼容 Postgres 线缆（直接使用 Postgres 解析器和计划器）。 * 多区域部署。  优势：...
  c14_b10.md: ...：可调一致性。ONE 一致性读非常快；QUORUM 写提供一定持久性保证。线性扩展到多节点。  (d) **CRDT-based（自定义）** ：G-Counter 或 CRDT 有序集做排行榜。每个区域有本地计数器；跨区域合并。每区域写亚毫秒；最...
  c15_b05.md: ...oDB 条件写。  成本：每次递减 5-50 ms；每件商品吞吐受共识限制。   #### 最终一致的问题  CRDT-based “PN-Counter” 可以表示库存（正向和负向贡献），但合并函数是求和。若两个副本并发减到 -1，合并后总数为 -...
  c15_b07.md: ...调者在数据库分配时间戳）而非客户端。 * 对正确性关键数据，使用轻量事务（Cassandra LWT）或迁移到 HLC-based MVCC（CockroachDB）。  更深层问题：LWW 在结构上是会丢失信息的。按设计，冲突写会丢失信息（被丢弃的...
  c15_b09.md: ...tem 只被一个区域拥有。其他区域的写路由到拥有区域。没有跨区域并发写，就没有 LWW 冲突。  (b) **CRDT-based 字段** ：对多区域可能更新的字段（计数器、标签），使用并发更新能干净合并的数据结构。DynamoDB 原生不支持 C...
  c15_b11.md: ...要因果一致）、库存（需要线性化）。应用工程师的任务是为每个操作选择正确的一致性。  第16章深入 CRDT：state-based vs op-based、典型类型（G-Counter、PN-Counter、OR-Set、LWW-Register）、...
  c15_b11.md: ...线性化）。应用工程师的任务是为每个操作选择正确的一致性。  第16章深入 CRDT：state-based vs op-based、典型类型（G-Counter、PN-Counter、OR-Set、LWW-Register）、2026 生产现实（No...
  c16_b01.md: ...” 是结构属性：数据类型的设计从构造上消除冲突。  形式上，设计 CRDT 有两种等价方式：  (a) **State-based CRDT（CvRDT）** ：每个副本持有完整状态。合并时，合并函数取两个状态并产生新状态。合并必须满足：  * **...
  c16_b01.md: ...个 join-semilattice。任意合并序列都会收敛到所有已见状态的最小上界。  (b) **Operation-based CRDT（CmRDT）** ：每个副本广播操作。广播必须保证投递（可靠广播，第4章）。操作必须可交换：不同顺序应用操作...
  c16_b01.md: ...可靠广播，第4章）。操作必须可交换：不同顺序应用操作产生相同结果。  两种表述表达能力等价；选择是运维性的。State-based 更易实现（只需算合并）但状态可能变大。Op-based 更高效（只发 diff）但要求广播层保证投递。...
  c16_b01.md: ...相同结果。  两种表述表达能力等价；选择是运维性的。State-based 更易实现（只需算合并）但状态可能变大。Op-based 更高效（只发 diff）但要求广播层保证投递。...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvR...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvRDT 合并状态；CmRDT 广播操作。  **答...
  c16_b06.md: ...Type）是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合并顺序如何都产生相同结果。  _State-based CRDT（CvRDT）_ ：每个副本持有完整状态。合并时，合并函数取两个状态并产生新状态。合并必须满足交换律、结合律、...
  c16_b06.md: ...投递外无要求；离线后合并容易。缺点：状态可能变大；发送完整状态带宽密集（delta 可缓解）。  _Operation-based CRDT（CmRDT）_ ：每个副本广播操作。广播必须保证恰好一次投递（或操作幂等，允许至少一次）。操作必须可交换：任...
  c16_b06.md: ...息更小（只发 op）；高频更新更高效。缺点：需要可靠广播层；离线操作需要小心 op-log 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Automerg...
  c16_b06.md: ...广播层；离线操作需要小心 op-log 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.**...
  c16_b11.md: ...flict-free Replicated Data Types）是为并发更新下确定性合并而设计的数据结构。State-based CRDT（CvRDT）合并完整状态；operation-based CRDT（CmRDT）广播操作。两者表达能力等价；...
  c16_b11.md: ...为并发更新下确定性合并而设计的数据结构。State-based CRDT（CvRDT）合并完整状态；operation-based CRDT（CmRDT）广播操作。两者表达能力等价；选择是运维性的。  典型 CRDT：G-Counter（只增计数器）...
  c17_b10.md: ...OPS、Eiger）外难以正确实现。MongoDB 的因果会话是首个广泛部署的实现；CockroachDB 的 HLC-based MVCC 副作用地提供 causal+。随着这些成熟，因果作为默认更容易采用。2030 年 “默认应该是什么” 的答案...
  c18_b01.md: ...ite-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d) **TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven inv...
  c20_b02.md: ...策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样决策冗余编码...
  c20_b02.md: ...。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retro...
  c20_b05.md: ...context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-b...
  c20_b05.md: ...分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，t...
  c20_b05.md: ...**Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-...
  c20_b05.md: ...d 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在...
  c20_b05.md: ...：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点（根 span）做出。决策（是否采样）通过 trace contex...
  c20_b06.md: ..._Tail-based sampling_ ：spans 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留...
  c20_b06.md: ...ce；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTe...
  c20_b06.md: ...常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开...
  c20_b06.md: ...d-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施...
  c20_b06.md: ...；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，...
  c20_b06.md: ...队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 tra...
  c20_b06.md: ...误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 trace context 必须跨越共...
  c21_b06.md: ...。互联电网（为共享发电而高效）成为级联路径。现代电网设计增加 “blackstart” 孤岛。分布式系统类比：cell-based 架构，一个 cell 故障不会级联到其他 cell；以部分效率换取遏制。  2003 年大停电是经典研究，因为规模前所...
  c21_b08.md: ...*电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blackstart 孤岛**：cell-based 架构；能够隔离部分系统并独立重启。  2003 年序列用分布式系统术语：  (a) **触发器**：三个 “服务连接”...
  c30_b02.md: ...nino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelm...

=== west (54 occurrences) ===
  c01_b01.md: ...她已经在查看 trace。两个协调者（coordinator）节点分别完成了支付：一个位于 Crescent 的 EU-West 区域（法兰克福数据中心），另一个位于前置 Crescent US-East 区域的南美洲网关（网关设在圣保罗，负责把流...
  c01_b01.md: ...而原始请求仍在通过法兰克福收尾。（第13章和第28章会完整展开 Crescent 的三区域架构——US-East、EU-West、US-West；本章使用城市名，因为事后复盘最初就是这样命名的，后来团队才把它重新框定为区域术语。）  bug 不在任...
  c01_b01.md: ...过法兰克福收尾。（第13章和第28章会完整展开 Crescent 的三区域架构——US-East、EU-West、US-West；本章使用城市名，因为事后复盘最初就是这样命名的，后来团队才把它重新框定为区域术语。）  bug 不在任何单段代码里。银...
  c06_b06.md: ...ine 提供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、US-West、Frankfurt。他们希望 Raft quorum 在 80 ms 内提交。可行吗？若不可行，如何改？  提示  R...
  c06_b06.md: ...限。  **答案：** 三副本 Raft 需 2 of 3 ack。相关成对往返延迟：  * US-East 到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到 Frankfurt...
  c06_b06.md: ...S-East 到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和...
  c06_b06.md: ...S-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和 Frankfurt 中先响应者约 80-90 ms 往返。因此 US-East 发起的写约 85 ms 往返提交，...
  c06_b06.md: ...。  预算勉强可行但很紧。选项：  (a) 把一个副本移近（如 Frankfurt 换 US-Central 或 US-West 换 Toronto），把第二快往返压到 80 ms 以下。  (b) 用 Spanner 风格按区域 leaderle...
  c06_b08.md: ...的往返。系统级最坏情况是所有可能发起区域中这个延迟的最大值。  聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50...
  c06_b08.md: ...区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-...
  c06_b08.md: ...l、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。 * US-...
  c06_b08.md: ...al，30 ms。 * US-Central 发起：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower = US-Central，50 ms。...
  c06_b09.md: ...*（US-West 发起）。延迟极好；但灾难恢复差（全在美国大陆，单一国家暴露）。  分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔...
  c06_b09.md: ...分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  *...
  c06_b09.md: ...o）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。...
  c06_b09.md: ...ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起...
  c06_b09.md: ...yo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US...
  c06_b09.md: ...~200 ms**（Tokyo 发起）。DR 更好（三大洲）；延迟更差，尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-Wes...
  c06_b09.md: ...*（Tokyo 发起）。DR 更好（三大洲）；延迟更差，尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 m...
  c06_b09.md: ...尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower...
  c06_b09.md: ...-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower = US-West，80 m...
  c06_b09.md: ...est、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower = US-West，80 ms。 * EU-We...
  c06_b09.md: ...s，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West...
  c06_b09.md: ...st ~180 ms。  * US-East 发起：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower =...
  c06_b09.md: ...-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower = US-East，80 ms。  最坏情况 Raft 提交：**~90 ms**（EU...
  c06_b09.md: ...。跨两洲；单区域 DR 可存活；所有发起区域延迟压在 ~90 ms。  对大多数工作负载，第三种（US-East、EU-West、US-West）是正确平衡：跨大陆 DR，最坏情况提交延迟低于 100 ms。  **Q3.** 一个微服务架构有 2...
  c06_b09.md: ...DR 可存活；所有发起区域延迟压在 ~90 ms。  对大多数工作负载，第三种（US-East、EU-West、US-West）是正确平衡：跨大陆 DR，最坏情况提交延迟低于 100 ms。  **Q3.** 一个微服务架构有 20 个服务。用户...
  c06_b09.md: ...~30 ms。 * 认证检查：~20 ms。 * 跨区域 hop 往返：~150 ms（假设 US-East 到 EU-West）。 * 固定总成本：~200 ms。  剩余预算 = 1000 - 200 = 800 ms，分给 7 个本地（区域内...
  c13_b10.md: ...-- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE a...
  c13_b10.md: ...B 下：    1. 事务以来自发起节点的 HLC 时间戳开始。Crescent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-DNS 路由选定的区域。按第6章 §6.1 方法（...
  c13_b10.md: ...1. 事务以来自发起节点的 HLC 时间戳开始。Crescent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-DNS 路由选定的区域。按第6章 §6.1 方法（光纤中光速下限约...
  c13_b10.md: ...→ US-East（经南美沿海电缆约 8,500 km）p99 RTT 约 110 ms；São Paulo → EU-West（经大西洋约 9,500 km）约 190 ms；São Paulo → US-West（经美国大陆路由约 11,000...
  c13_b10.md: ...；São Paulo → EU-West（经大西洋约 9,500 km）约 190 ms；São Paulo → US-West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELE...
  c13_b10.md: ...`UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取...
  c13_b10.md: ...ommit 都涉及受影响分片在跨区域法定人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West / US-West），per-shard 提交最坏情况约 90 ms（第6章 AP Q2）。事务必须在所有三个分片提交...
  c13_b10.md: ...影响分片在跨区域法定人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West / US-West），per-shard 提交最坏情况约 90 ms（第6章 AP Q2）。事务必须在所有三个分片提交后才会返回给应用；总...
  c19_b04.md: ...revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。...
  c23_b04.md: ...y  Olamide 的计划：  **赛前设计（3 周前）**：  * 实验：模拟 CockroachDB 集群 EU-West 区域故障。 * 稳态假设：US-East 的支付交易继续 p99 延迟 < 500 ms；支付成功率保持在 99.5%...
  c23_b04.md: ...；支付成功率保持在 99.5% 以上。 * 回滚计划：若实验导致超过 SLO 阈值的可见影响，在 5 分钟内恢复 EU-West 区域。 * 爆炸半径：仅 EU-West 区域；US-East 和 US-West 正常运行。 * 参与者：SRE 团...
  c23_b04.md: ...回滚计划：若实验导致超过 SLO 阈值的可见影响，在 5 分钟内恢复 EU-West 区域。 * 爆炸半径：仅 EU-West 区域；US-East 和 US-West 正常运行。 * 参与者：SRE 团队（4 人）、支付团队（3 名工程师）、客...
  c23_b04.md: ...可见影响，在 5 分钟内恢复 EU-West 区域。 * 爆炸半径：仅 EU-West 区域；US-East 和 US-West 正常运行。 * 参与者：SRE 团队（4 人）、支付团队（3 名工程师）、客服负责人（1 人）、产品经理（1 人）。...
  c23_b04.md: ...：团队集合；回顾回滚计划；观察员进入 on-call 室。 * 14:10 UTC：实验开始。Olamide 对 EU-West 节点调用 CockroachDB “drain” 命令（优雅关闭）。 * 14:15 UTC：EU-West 节点下线...
  c23_b04.md: ...对 EU-West 节点调用 CockroachDB “drain” 命令（优雅关闭）。 * 14:15 UTC：EU-West 节点下线。集群的 Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 C...
  c23_b04.md: ...14:15 UTC：EU-West 节点下线。集群的 Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20 UTC：系统稳定...
  c23_b04.md: ...支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20 UTC：系统稳定。EU-West 发起交易的 p99 延迟：480 ms（基线 180 ms；跨区故障转移成本）。成功率：99.7%。 * 14:30...
  c23_b04.md: ...80 ms（基线 180 ms；跨区故障转移成本）。成功率：99.7%。 * 14:30 UTC：实验结束。重启 EU-West 节点；集群恢复。 * 14:45 UTC：系统回到基线。  **复盘**：  * 稳态：维持（延迟在 SLO 内，成功...
  c23_b04.md: ...功率高于阈值）。 * 观察：     * 基于 Consul 的服务 mesh 故障转移耗时 30 秒；此窗口内 EU-West 用户看到超时。行动项：调整 Consul 健康检查频率。     * 客服面板没有清晰指示 “EU-West 区域下线...
  c23_b04.md: ...内 EU-West 用户看到超时。行动项：调整 Consul 健康检查频率。     * 客服面板没有清晰指示 “EU-West 区域下线”。行动项：在面板添加区域健康。     * 故障转移窗口内有一笔支付被重复扣款（Raft 重选举完成前某 s...
  c23_b04.md: ...shard 有 30 秒双 leader 期）。行动项：调查并修复；这与第1章事故的同类 bug。     * EU-West API gateway 的重试预算配置正确；没有重试风暴。 * 学习：     * 故障转移有效，但不如设计预期干净。...
  c28_b03.md: ...KAFKAE[Kafka brokers]         end         subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[R...
  c28_b03.md: ...KAFKAW[Kafka brokers]         end         subgraph USW["US-West Region"]             APIW2[API Service]             CRDBW2[...
  c28_b06.md: ...*：增加到 50%。关键观察期。团队紧盯每个 dashboard。  (e) **10:30 UTC**：小异常。EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 followe...
  c30_b10.md: ...gion，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局表做跨 region...
  c30_b10.md: ...Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局表做跨 region 复制。 * **为...

=== lamport (54 occurrences) ===
  c02_b14.md: ...个工作负载需要哪种一致性，是比知道哪个数据库是“CP”或“AP”更有用的技能。  第3章抓住第一个谎言——时间——展开 Lamport、向量、HLC、TrueTime 等协议，让你能够在机器间推理顺序而不撒谎。本章提到的 Spanner commit-w...
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distribute...
  c03_b02.md: ...戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳——工作流 1：逻辑时钟实现  > **工作流 1（输入 → 输出）** ：给定跨 N 个进程的本地 + 发送...
  c03_b02.md: ...：给定跨 N 个进程的本地 + 发送 + 接收事件序列，产出尊重 happens-before 的逐事件时间戳。本节走 Lamport 时间戳；§3.4 扩展到向量时钟（还能检测并发）；§3.5 扩展到 HLC（贴近 wall 时间）。该工作流的 _输出...
  c03_b02.md: ...—尊重 happens-before 的时间戳——三者相同；_成本_ 和 _检测能力_ 不同。变体：为小成本日志排序选 Lamport；为 CRDT 并发检测选向量；为系统级时间戳兼调试辅助选 HLC。  Lamport 的第一种算法很简单：每个进程 p...
  c03_b02.md: ...不同。变体：为小成本日志排序选 Lamport；为 CRDT 并发检测选向量；为系统级时间戳兼调试辅助选 HLC。  Lamport 的第一种算法很简单：每个进程 p 维护一个本地计数器 L_p，初始为 0。  * 任何本地事件，p 将 L_p 加 1...
  c03_b02.md: ...b，则 L(a) < L(b)。  逆命题不成立。L(a) < L(b) 不意味着 a \to b；两个事件可能并发。Lamport 时间戳给出 _一致排序_ （happens-before 的一个全序扩展），但不能让你 _检测_ 并发。...
  c03_b03.md: ...ies 4) | 4 6 | P_3 | receive n | \max(1, 4) + 1 = 5  事件 a 的 Lamport 时间戳为 1；事件 x 的时间戳为 1。两者并发（没有 happens-before 链连接它们）。Lamport 时...
  c03_b03.md: ...的 Lamport 时间戳为 1；事件 x 的时间戳为 1。两者并发（没有 happens-before 链连接它们）。Lamport 时间戳相等；我们无法排序，但一个全序扩展（按进程 ID 打破平手）会一致地让 a < x 或 x < a。  P_3...
  c03_b03.md: ...send(n) \to recv(n) 反映为递增的时间戳 (2, 3, 4, 5)。   ### 3.4 向量时钟  Lamport 时间戳保留顺序但失去检测并发能力。向量时钟由 Fidge 和 Mattern 在 1988 年独立提出，以更多空间为代...
  c03_b06.md: ...个 HLC 是一个 (l, c) 对，不像向量时钟随 N 扩展。 * **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间...
  c03_b09.md: ...#### 概念检测  **Q1.** 陈述 Lamport 的 happens-before 关系。给出一个因果有序事件的例子和一个并发事件的例子。  提示  三条规则：同进程排...
  c03_b09.md: ...happens-before 链连接它们。它们在技术意义上并发；任一排序都与偏序一致。  **Q2.** 两个事件的 Lamport 时间戳分别为 5 和 7。关于它们的因果关系你能得出什么结论？  提示  Lamport 时间戳单向保持 happen...
  c03_b09.md: ...**Q2.** 两个事件的 Lamport 时间戳分别为 5 和 7。关于它们的因果关系你能得出什么结论？  提示  Lamport 时间戳单向保持 happens-before，但逆命题不成立。  **答案：** 几乎得不出什么。Lamport 时间...
  c03_b09.md: ...示  Lamport 时间戳单向保持 happens-before，但逆命题不成立。  **答案：** 几乎得不出什么。Lamport 时间戳满足：若 a \to b，则 L(a) < L(b)。逆命题不成立。因此 L(a) = 5 < L(b) = 7...
  c03_b09.md: ...b) = 7 不蕴含 a \to b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），却不适合 _检测_ 并发的原因。要检测并发需...
  c03_b09.md: ...t\le V_m。两者互不支配，因此该发送事件与本次接收前 P_3 的最近事件 _并发_。  这种分析区分了向量时钟与 Lamport 时钟：我们能检测这两个事件并发，而非因果关系。...
  c03_b12.md: ...HLC 加偏移上界是实际默认； aggressively 调优 NTP 和偏移上界。  **Q4.** 用伪代码实现 Lamport 时间戳，系统允许每个进程产生本地事件、发送消息和接收消息。走一遍至少包含 6 个事件、至少一对发送-接收的三进程示例。...
  c03_b13.md: ...\to b \to send\,m \to recv\,m \to send\,n \to recv\,n \to y。Lamport 时间戳沿因果路径形成单调链：1, 2, 3, 4, 5, 6, 7。事件 x 的 L = 1（与 a 并发），全序会用...
  c03_b13.md: ...L = 1（与 a 并发），全序会用进程 ID 打破平手。  注意 x（L=1）和 a（L=1）并发（没有因果链连接）。Lamport 时间戳无法区分“并发”与“按平手排序”；这是 §3.4 引入向量时钟解决的问题。...
  c03_b15.md: ...ll-clock 时间戳是事件偏序的有损代理。用跨机器时间戳比较来决定“哪个先发生”的代码是在对一个虚构概念做推理。  Lamport 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-b...
  c03_b15.md: ...来决定“哪个先发生”的代码是在对一个虚构概念做推理。  Lamport 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-before 但不能检测并发。向量时钟为每个进程每个事件分配一个整...
  c07_b11.md: ...ion 用部分同步加心跳。用这副眼镜读任何新共识论文，结构都会显现。  第8章取第一个经典协议 Paxos，逐步走一遍。Lamport 1989 年论文（2001 年改写成通俗英语）是分布式系统最有影响力的论文之一。Multi-Paxos 把单法令 Pa...
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Ti...
  c08_b01.md: ...述了一座虚构希腊岛屿，兼职立法者通过涉及信使、账簿和法定人数的繁复仪式颁布法令。论文审稿不佳（一位审稿人建议它是玩笑），Lamport 撤回。1998 年他才最终发表。  论文描述的协议就是 Paxos，它是分布式系统史上最具影响力的共识算法。Googl...
  c08_b01.md: ...Chubby（Spanner 的基础）。ZooKeeper 的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport 2001 年改写成 “Paxos Made Simple”，仍然很难。  本章教授 Paxos 的常见形态：单法令 P...
  c08_b04.md: ...### 8.6 为什么 Paxos 难实现  Lamport 的 “Paxos Made Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persist...
  c08_b11.md: ...安全的代码。这是第27章核心教训：分布式代码需要 LLM 目前做得不好的推理。   ### 本章总结  Paxos 是 Lamport 1989 年提出的经典共识协议。单法令 Paxos 通过两阶段（prepare、accept）加法定人数相交就一个值达...
  c10_b01.md: ...BFT 家族树：       graph LR         BG["Byzantine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov 1999<br/>O(N...
  c10_b01.md: ...style Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport、Shostak 和 Pease 在 1982 年论文中形式化：N 个将军包围一座城市，必须就是否进攻达成一致。每个将军...
  c14_b13.md: ...作文档）是正确答案，强一致成本过高。CRDT 是以无冲突意外的方式实现最终一致性的原则性方法。本章还涵盖 Chandy-Lamport 分布式快照，这是理解 Flink/checkpoint 系统的基础。  ---...
  c15_b01.md: ...知道 _哪些_ 工作负载需求更弱，以及 _弱多少_。本章讲授实用分类法：最终一致何时正确、有哪些会话保证、Chandy-Lamport 分布式快照如何让你对异步系统的一致状态进行推理。   ### 15.1 最终一致何时是正确答案  三类最终一致性在结构...
  c15_b02.md: ...默认因为它最简单。代价是时钟偏移问题：一个逻辑上更晚的写可能因写节点时钟慢而输掉。   ### 15.4 Chandy-Lamport 分布式快照  一个基础原语：如何在系统仍在运行时拍下一致快照？  Chandy 和 Lamport 1985 年论文引...
  c15_b02.md: ....4 Chandy-Lamport 分布式快照  一个基础原语：如何在系统仍在运行时拍下一致快照？  Chandy 和 Lamport 1985 年论文引入了一个协议，无需全局时钟即可产生代表一致全局状态的快照。   #### 场景  一组进程通过 FI...
  c15_b03.md: ...致快照：它可能在某个真实时刻就是系统状态，尽管没有命名某个具体时刻。   #### 2026 年重要性  Chandy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing...
  c15_b03.md: ...pointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “checkpoint barrier”）流过数...
  c15_b03.md: ...nce 语义建立于此。  * **某些实现的分布式事务** ：特定时间戳处系统的 “快照” 概念上是一个 Chandy-Lamport 快照，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD linea...
  c15_b06.md: ...可最终一致。任何业务含义取决于基于操作发生时刻状态拒绝某些操作的操作需要线性化。  **Q3.** 描述 Chandy-Lamport 快照协议。记录的状态代表什么？  提示  每条通道 marker；每个进程首次收到 marker 时记录；捕获在途消息...
  c15_b07.md: ...heckpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last writ...
  c15_b08.md: ...到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 ch...
  c15_b08.md: ...Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactio...
  c15_b09.md: ...是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照，exactly-once 不可能：系统无法捕获重启的一致状态。该协议是状态流处理整个类别的基础。  **Q3...
  c15_b11.md: ...ckroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话跟踪提供它们。  Chandy-Lamport 分布式快照是在无全局时钟下捕获一致全局状态的基础原语。协议使用每条通道上的 marker 定义快照边界；记录的各进程状...
  c28_b10.md: ...§3.3 | N 个进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector...
  c30_b01.md: ...is Only Part of the Story." _IEEE Computer_. — PACELC 论文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering of Events in a...
  c30_b02.md: ...liveness." _Information Processing Letters_. — 安全性/活性分解。 * Lamport, L. (1998). "The Part-Time Parliament." _ACM TOCS_. — 原始 Pa...
  c30_b02.md: ...). "The Part-Time Parliament." _ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L. (2001). "Paxos Made Simple." _ACM SIGACT News_. — 必读改写。...
  c30_b02.md: ...s: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzantine Genera...
  c30_b02.md: ...OD_.   #### 最终一致性与 CRDT（第五部分 — 第15-17章）  * Chandy, K. M., & Lamport, L. (1985). "Distributed snapshots: determining global stat...
  c30_b06.md: ...PACELC**：CAP 的 PACELC 扩展。若分区：可用性或一致性；否则：延迟或一致性。 * **Paxos**：Lamport 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum i...
  c30_b14.md: ...ted-systems-2026-examples/     ├── ch03_clocks/     │   ├── lamport.py        — Lamport timestamps     │   ├── vector.py...
  c30_b14.md: ...mples/     ├── ch03_clocks/     │   ├── lamport.py        — Lamport timestamps     │   ├── vector.py         — Vector clocks (s...
  c30_b14.md: ...ted-systems-2026-examples/     ├── ch03_clocks/     │   ├── lamport.py        — Lamport timestamps     │   ├── vector.py...
  c30_b14.md: ...mples/     ├── ch03_clocks/     │   ├── lamport.py        — Lamport timestamps     │   ├── vector.py         — Vector clocks (s...

=== east (51 occurrences) ===
  c01_b01.md: ...支付：一个位于 Crescent 的 EU-West 区域（法兰克福数据中心），另一个位于前置 Crescent US-East 区域的南美洲网关（网关设在圣保罗，负责把流量路由到 US-East 的运维数据面）。两者都记录了干净的 commit。...
  c01_b01.md: ...数据中心），另一个位于前置 Crescent US-East 区域的南美洲网关（网关设在圣保罗，负责把流量路由到 US-East 的运维数据面）。两者都记录了干净的 commit。两者都相信自己是唯一处理该事务的协调者。客户的手机当时正通过一个不稳...
  c01_b01.md: ...到了圣保罗网关，而原始请求仍在通过法兰克福收尾。（第13章和第28章会完整展开 Crescent 的三区域架构——US-East、EU-West、US-West；本章使用城市名，因为事后复盘最初就是这样命名的，后来团队才把它重新框定为区域术语。）...
  c02_b02.md: ...可能造出云账单超过工资预算的系统。   #### 网络是同构的  网络的不同部分有不同的带宽、延迟和可靠性。一个 US-East 客户访问 US-East 服务器的请求很快。同一条代码路径上，一个新加坡客户访问 US-East 服务器会慢 200...
  c02_b02.md: ...的系统。   #### 网络是同构的  网络的不同部分有不同的带宽、延迟和可靠性。一个 US-East 客户访问 US-East 服务器的请求很快。同一条代码路径上，一个新加坡客户访问 US-East 服务器会慢 200 ms，而且失败率不同，因为...
  c02_b02.md: ...和可靠性。一个 US-East 客户访问 US-East 服务器的请求很快。同一条代码路径上，一个新加坡客户访问 US-East 服务器会慢 200 ms，而且失败率不同，因为新加坡的传输要经过更多跳。在测试环境表现良好的代码，在真实流量模式下可能...
  c06_b04.md: ...0 ms 可达的三个区域（US-East、US-West、EU-West）。如果第三个区域是新加坡，预算将不成立：US-East 与新加坡之间跨区域 Raft quorum 单程 200 ms、往返 400 ms，会超支。  Olamide 的团队...
  c06_b06.md: ...；leaf-spine 提供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、US-West、Frankfurt。他们希望 Raft quorum 在 80 ms 内提交。可行吗？若不可行，如何改...
  c06_b06.md: ...2。计算每对往返下限。  **答案：** 三副本 Raft 需 2 of 3 ack。相关成对往返延迟：  * US-East 到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到...
  c06_b06.md: ...ft 需 2 of 3 ack。相关成对往返延迟：  * US-East 到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  U...
  c06_b06.md: ...Frankfurt：~90 ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和 Frankfurt 中先响应者约 80-90 ms 往返。因此 US-...
  c06_b06.md: ...发起的写等待第二个 ack；US-West 和 Frankfurt 中先响应者约 80-90 ms 往返。因此 US-East 发起的写约 85 ms 往返提交，刚超 80 ms 预算。  预算勉强可行但很紧。选项：  (a) 把一个副本移近（如...
  c06_b08.md: ...** 一个团队设计全球聊天应用。任何区域的用户应在 500 ms 内看到来自任何区域的消息。团队在 (a) 中央 US-East 数据库供所有区域读，(b) 每区域数据库 + 异步跨区域复制，(c) 全局复制数据库（Spanner）之间选择。针对延...
  c06_b08.md: ...选项最坏情况的跨区域往返。500 ms 是阈值；每个选项最坏情况交付多少？  **答案：**  (a) **中央 US-East DB。** 东京用户写；消息必须到达 US-East（单程 ~100 ms），持久化（~10 ms），然后东京用户可读...
  c06_b08.md: ...项最坏情况交付多少？  **答案：**  (a) **中央 US-East DB。** 东京用户写；消息必须到达 US-East（单程 ~100 ms），持久化（~10 ms），然后东京用户可读（再 ~100 ms）。东京会话写后读往返约 210...
  c06_b08.md: ...m 是任意 2 of 3。leader 自己算一个 ack；条目在 _最快_ follower 响应后提交。因此 US-East 发起的写，提交延迟是到其他两个区域中较快者的往返。系统级最坏情况是所有可能发起区域中这个延迟的最大值。  聚类三区域：...
  c06_b08.md: ...的写，提交延迟是到其他两个区域中较快者的往返。系统级最坏情况是所有可能发起区域中这个延迟的最大值。  聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 m...
  c06_b08.md: ...况是所有可能发起区域中这个延迟的最大值。  聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-...
  c06_b08.md: ...聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower...
  c06_b08.md: ...-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。 * US-Central 发起：最快 follo...
  c06_b08.md: ...llower = US-Central，30 ms。 * US-Central 发起：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower = US-Central，50 ms。...
  c06_b09.md: ...*~50 ms**（US-West 发起）。延迟极好；但灾难恢复差（全在美国大陆，单一国家暴露）。  分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；U...
  c06_b09.md: ...单一国家暴露）。  分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~2...
  c06_b09.md: ...U-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 f...
  c06_b09.md: ...0 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower =...
  c06_b09.md: ...：最快 follower = EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US-East，200 ms。  最坏情况 Raft...
  c06_b09.md: ...发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US-East，200 ms。  最坏情况 Raft 提交：**~200 ms**（Tokyo 发起）。DR 更好（三大洲）；延迟更差...
  c06_b09.md: ...ft 提交：**~200 ms**（Tokyo 发起）。DR 更好（三大洲）；延迟更差，尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West...
  c06_b09.md: ...发起）。DR 更好（三大洲）；延迟更差，尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US...
  c06_b09.md: ...延迟更差，尤其对亚太用户。  折中：US-East、EU-West、US-West。成对：East-EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 fol...
  c06_b09.md: ...EU ~90 ms，East-West ~80 ms，EU-West 到 US-West ~180 ms。  * US-East 发起：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower =...
  c06_b09.md: ...：最快 follower = US-West，80 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower = US-East，80 ms。  最坏情况 Raft...
  c06_b09.md: ...：最快 follower = US-East，90 ms。 * US-West 发起：最快 follower = US-East，80 ms。  最坏情况 Raft 提交：**~90 ms**（EU-West 发起）。跨两洲；单区域 DR 可存活；...
  c06_b09.md: ...ep-alive）：~30 ms。 * 认证检查：~20 ms。 * 跨区域 hop 往返：~150 ms（假设 US-East 到 EU-West）。 * 固定总成本：~200 ms。  剩余预算 = 1000 - 200 = 800 ms，分给...
  c13_b10.md: ...ckroachDB 下：    1. 事务以来自发起节点的 HLC 时间戳开始。Crescent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-DNS 路由选定的区域。按第6章...
  c13_b10.md: ...（光纤中光速下限约 200,000 km/s，真实 RTT 的 2-3× 路由开销乘数）：São Paulo → US-East（经南美沿海电缆约 8,500 km）p99 RTT 约 110 ms；São Paulo → EU-West（经大西洋...
  c13_b10.md: ...约 190 ms；São Paulo → US-West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Ada...
  c13_b10.md: ...从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East...
  c19_b04.md: ...取。  * **Consul 做跨区域服务网格**：EU-West 的服务通过 Consul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gat...
  c23_b04.md: ...设计（3 周前）**：  * 实验：模拟 CockroachDB 集群 EU-West 区域故障。 * 稳态假设：US-East 的支付交易继续 p99 延迟 < 500 ms；支付成功率保持在 99.5% 以上。 * 回滚计划：若实验导致超过 S...
  c23_b04.md: ...超过 SLO 阈值的可见影响，在 5 分钟内恢复 EU-West 区域。 * 爆炸半径：仅 EU-West 区域；US-East 和 US-West 正常运行。 * 参与者：SRE 团队（4 人）、支付团队（3 名工程师）、客服负责人（1 人）、产...
  c23_b04.md: ...余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20 UTC：系统稳定。EU-West 发起交易的 p99 延迟：480 ms（基线 180 ms；跨区故...
  c23_b04.md: ...面板添加区域健康。 * 调查重复扣款案例；若可复现，提交 P0。 * 4 周后安排下一次 game day：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到...
  c28_b03.md: ...S / auth / rate limit]         end         subgraph USE["US-East Region"]             APIE[API Service]             REDISE[R...
  c28_b04.md: ...Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US-East region（Adaeze 的计算 home region，由 geo-DNS 决定；圣保罗到 US-East 单程约...
  c28_b04.md: ...US-East region（Adaeze 的计算 home region，由 geo-DNS 决定；圣保罗到 US-East 单程约 55 ms，是 Crescent 三个 region 中最近的）。60 ms 跨赤道。  (3) US-Eas...
  c28_b04.md: ...ast 单程约 55 ms，是 Crescent 三个 region 中最近的）。60 ms 跨赤道。  (3) US-East 的 regional 负载均衡器路由到健康的 API 服务实例。实例开始支付处理。Trace context（Open...
  c28_b04.md: ...）。2 ms。  (5) API 服务开始 Stratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（第18章）。5 ms。...
  c28_b04.md: ...事务从 CockroachDB 读取 Adaeze 余额（第11/13/14章）。Adaeze 账户按地域分区到 US-East，因此读是本地；线性化（从 leader 读；使用 HLC，第3章）。8 ms。  (7) 事务通过 Continuum...
  c30_b07.md: ...计费子系统运行例行调试命令时输入错误，从索引子系统移除了比预期更多的容量。 * **放大**：索引子系统负责管理 US-EAST-1 所有 S3 对象的元数据。容量减少后，索引跟不上流量；索引查询变慢；客户端重试放大负载；索引队列积压。放置子系统（...
  c30_b10.md: ...：选择 3 个 region，使最坏情况 Raft quorum 约 90ms（第6章 AP Q2 方法）。典型：US-East + EU-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局...

=== write (50 occurrences) ===
  c03_b11.md: ...题  **Q1.** 一个团队在三个数据中心运行 Cassandra，每秒 100,000 次写入，偶尔发现“last write wins” tombstone 复活了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cassandra...
  c03_b11.md: ...戳。如果删除被盖戳早于后续写入的时间戳，会发生什么？  **答案：** Cassandra 的默认冲突解决是 last-write-wins（LWW），基于协调者节点分配的 wall-clock 时间戳。如果删除由时钟落后的协调者处理，它被盖戳早于另...
  c03_b11.md: ...间戳。如果删除由时钟落后的协调者处理，它被盖戳早于另一个时钟超前的协调者上的后续写入。当删除和写入到达副本时，副本看到 write-timestamp > delete-timestamp，于是保留写入，实际上复活了记录。  这就是经典的 Cassa...
  c05_b01.md: ...面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_（分区后两个副本各自接受写入）和 _quoru...
  c05_b01.md: ...-failover consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica fai...
  c05_b01.md: ...面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_（分区后两个副本各自接受写入）和 _quoru...
  c05_b01.md: ...-failover consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica fai...
  c05_b02.md: ...### 5.3 异步复制  主库接受写入，本地提交，立即向客户端确认。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主...
  c05_b02.md: ...ver consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under repli...
  c05_b02.md: ...Paxos 的 per-tablet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on...
  c05_b02.md: ...leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-...
  c05_b02.md: ...consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...
  c05_b02.md: ...### 5.3 异步复制  主库接受写入，本地提交，立即向客户端确认。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主...
  c05_b02.md: ...ver consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under repli...
  c05_b02.md: ...Paxos 的 per-tablet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on...
  c05_b02.md: ...leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-...
  c05_b02.md: ...consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...
  c05_b03.md: ...中断系统。代价是冲突必须在读时或后台修复时解决，因为两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。...
  c05_b03.md: ...定人数可能由不在目标副本集中的节点满足（Dynamo 的 “hinted handoff”）。相交保证被破坏。 * **Write in progress read** ：若写正在 N 个节点中的 W 个上执行，读命中 R 个节点可能只看到写者集合中...
  c05_b03.md: ...中断系统。代价是冲突必须在读时或后台修复时解决，因为两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。...
  c05_b03.md: ...定人数可能由不在目标副本集中的节点满足（Dynamo 的 “hinted handoff”）。相交保证被破坏。 * **Write in progress read** ：若写正在 N 个节点中的 W 个上执行，读命中 R 个节点可能只看到写者集合中...
  c05_b04.md: ...人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到...
  c05_b04.md: ...读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a) **将用户读...
  c05_b04.md: ...人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到...
  c05_b04.md: ...读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a) **将用户读...
  c05_b05.md: ...er、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避免客户存款失败。  较老系统（Postg...
  c05_b08.md: ...他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？** 异步 Postgres 复制...
  c05_b08.md: ...der 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 read-after-write 保证？** 若仪表盘必须立即反映存款，两个系统都能提供（Postgres 走主库读；CockroachDB 用 HLC...
  c05_b09.md: ...nit__(self):             self.max_hlc = (0, 0)          def write(self, key, value):             result = db.write(key, value...
  c05_b09.md: ...def write(self, key, value):             result = db.write(key, value)             # 服务器返回写提交时的 HLC。             self....
  c05_b11.md: ...+ R > N$ 的法定人数规则给相交，不是线性一致性；差别是许多真实复制 bug 的根源。  Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-session HLC 跟...
  c12_b03.md: ...。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b) **Write-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL...
  c13_b10.md: ...的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi...
  c13_b10.md: ...US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPD...
  c13_b10.md: ...服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取 write intent。   6. `COMMIT`：事务协调者（本身通过 Raft 复制）在选定的 commit_timest...
  c15_b02.md: ...3 冲突解决策略  当两个并发写冲突（两个副本接受同一键的不同值）时，系统必须解决。三种策略：  (a) **Last write wins（LWW）** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。  (b)...
  c15_b03.md: ...t。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-write-wins。应用必须处理自己写入一个区域后被另一区域并发写覆盖的情况（罕见但可能）。   #### Cassandra...
  c15_b03.md: ...近期写。  对需要更强保证的应用，Redis 提供 `WAIT N` 命令，阻塞直到 N 个副本 ack。这是 per-write 选择加入，非全局模式。...
  c15_b07.md: ...port（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last write wins”（LWW）冲突解决，它的失败模式是什么？  提示  LWW：时间戳最高的写胜出。失败：时钟偏移可导致 “逻辑...
  c15_b09.md: ...会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时钟偏移窗口内），一个会胜出，...
  c17_b05.md: ...提交写至少在一个副本上重叠。这比最终一致强，但弱于线性化。  QUORUM 提供：  * 单会话 read-after-write：若写成功（Q 个写入副本 ack）且后续读用 QUORUM，相交节点有该写；读能看到。 * 跨会话排序：若两会话写同一...
  c18_b01.md: ...e-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c)...
  c18_b01.md: ...hrough**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d...
  c18_b01.md: ...读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...
  c18_b01.md: ...TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...
  c18_b01.md: ...e-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c)...
  c18_b01.md: ...hrough**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d...
  c18_b01.md: ...读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...
  c18_b01.md: ...TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...
  c20_b01.md: ...5 ms (cross-region, serial after fraud)"]         L["ledger write<br/>30 ms (serial after DB)"]         POST["post-commit + r...

=== accept (50 occurrences) ===
  c08_b02.md: ...romise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  P...
  c08_b02.md: ...收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，propo...
  c08_b02.md: ...提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{m...
  c08_b02.md: ...Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{max}$，忽略。 * 否则，把 $(b, v)$ 持久化为 `a...
  c08_b02.md: ...享 1 个。）这是基础性质：一个多数作出的决定会被任何其他多数中的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare...
  c08_b02.md: ...的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹...
  c08_b02.md: ...romise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  P...
  c08_b02.md: ...收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，propo...
  c08_b02.md: ...提议的值。Proposer 不能忽略先前接受的值；它必须采纳。  Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{m...
  c08_b02.md: ...Proposer 向所有 acceptor 发送 `ACCEPT(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{max}$，忽略。 * 否则，把 $(b, v)$ 持久化为 `a...
  c08_b02.md: ...享 1 个。）这是基础性质：一个多数作出的决定会被任何其他多数中的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare...
  c08_b02.md: ...的某个 acceptor“记住”。  **第 3 部分：accept 规则保留先前决定。** Proposer 发送 `ACCEPT` 时，它提议的值受第一阶段响应约束。若 prepare 法定人数中已有 acceptor 接受过先前值，新值必须与之匹...
  c08_b04.md: ...机快照并截断日志。快照必须与日志该点状态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network...
  c08_b04.md: ...Paxos**（Heidi Howard 2016 法定人数灵活性工作）：推广法定人数要求。prepare 法定人数和 accept 法定人数只需 _相交_，不必都是多数。这让系统可调节权衡（更小 accept 法定人数换更快提交）。  现代生产系统主...
  c08_b05.md: ...null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 accept...
  c08_b05.md: ...个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：...
  c08_b05.md: ...必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ba...
  c08_b06.md: ...ept）：_ Proposer 选一个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 p...
  c08_b06.md: ...cceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promise 中见到这个已接受值，...
  c08_b06.md: ...ew}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Accept 规则（proposer 必须采用第一阶段所见最高 ballot 的已接受值）防止这种情况。任何已被某 acceptor...
  c08_b06.md: ...ept）：_ Proposer 选一个值（第一阶段响应中最高 ballot 的已接受值，若都没有则用自己的值），发送 `ACCEPT(b, v)` 给所有 acceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 p...
  c08_b06.md: ...cceptor。未承诺更高 ballot 的 acceptor 接受。  第二阶段保证：若 proposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promise 中见到这个已接受值，...
  c08_b06.md: ...ew}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Accept 规则（proposer 必须采用第一阶段所见最高 ballot 的已接受值）防止这种情况。任何已被某 acceptor...
  c08_b07.md: ...接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（PREPARE + PROMISE + ACCEPT + ACCEPTED）。对服务多个决定的稳定 leader，摊销效果显著。  若领导不稳定（频繁重选），每次重选都要跑...
  c08_b07.md: ...Paxos 难实现有多个运维原因：  (a) **持久状态纪律** ：acceptor 必须在回复 prepare 或 accept 消息 _之前_ 把 $b_{max}$、`accepted_ballot`、`accepted_value` 持久化到...
  c08_b08.md: ...定一个值。  提示  更高 ballot 的 proposer 获胜。另一个 proposer 的 prepare 或 accept 失败，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议...
  c08_b08.md: ...romise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot 20。  **B 的 accept：**  * B 发送 `ACCEPT(20, "Olamide")` 给所有 5 个。 * 全部 5 个接受。B 收到...
  c08_b08.md: ...B 的情况：B 有 5 个 promise，ballot 20。  **B 的 accept：**  * B 发送 `ACCEPT(20, "Olamide")` 给所有 5 个。 * 全部 5 个接受。B 收到 5 个 accepted 消息。值...
  c08_b08.md: ...20`）。它们回复 `PROMISE(30, 20, "Olamide")`。 * A 收到多数 promise。根据 accept 规则，A 必须提议 “Olamide”（来自最高 accepted_ballot 的值）。 * A 发送 `ACCEP...
  c08_b08.md: ...ccept 规则，A 必须提议 “Olamide”（来自最高 accepted_ballot 的值）。 * A 发送 `ACCEPT(30, "Olamide")`。全部 5 个接受（它们已有 ballot 20 的 “Olamide”；更新到 bal...
  c08_b08.md: ...eader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎么做？  提示  Leader 无法拿到 accept 的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为...
  c08_b08.md: ...的多数。Follower 的选举定时器触发。多数方选出新 leader。  **答案：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。...
  c08_b08.md: ...：** 当前 leader 无法为 accept 消息拿到多数（它只能到达 1 个其他节点；多数需要 5 中 3）。新 accept 超时。Leader 不能提交。  同时，3 个不可达 follower 不再收到 leader 心跳。它们的选举定时器...
  c08_b08.md: ...槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leader 在少数方被“隔离”：它不能提交、拿不到 accept。它的状态保持有效（safety 得到保留），但不进展。网络愈合且旧 leader 重新加入时，它发现更高 ballot...
  c08_b10.md: ...b_{max}$、`accepted_ballot`、`accepted_value`）必须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor...
  c08_b10.md: ...起使用时破坏法定人数相交。  还有三条（任一条都会造成静默 safety 违反）：  (d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式...
  c08_b10.md: ...b_{max}$、`accepted_ballot`、`accepted_value`）必须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor...
  c08_b10.md: ...起使用时破坏法定人数相交。  还有三条（任一条都会造成静默 safety 违反）：  (d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式...
  c08_b11.md: ...总结  Paxos 是 Lamport 1989 年提出的经典共识协议。单法令 Paxos 通过两阶段（prepare、accept）加法定人数相交就一个值达成一致。Safety 性质由 accept 规则保留：任何 proposer 的 accept...
  c08_b11.md: ...议。单法令 Paxos 通过两阶段（prepare、accept）加法定人数相交就一个值达成一致。Safety 性质由 accept 规则保留：任何 proposer 的 accept 必须提议第一阶段响应中最高已接受的值；这保证一旦值被选定，所有后续...
  c08_b11.md: ...accept）加法定人数相交就一个值达成一致。Safety 性质由 accept 规则保留：任何 proposer 的 accept 必须提议第一阶段响应中最高已接受的值；这保证一旦值被选定，所有后续 ballot 都会提议它。  Multi-Paxo...
  c10_b05.md: ...visited” 推广了 Paxos 的法定人数要求。经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 都是多数。Howard 的洞见：对 safety 只需要 $Q_1 \cap Q_2 \ne \...
  c10_b05.md: ...e \emptyset$。  这意味着：$Q_1$ 和 $Q_2$ 可以调优。只要任意 prepare 法定人数与任意 accept 法定人数相交，safety 就成立。  示例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rcei...
  c10_b05.md: ...$Q_2$ = 一列。$N$ 是方阵；两个法定人数都是 $\sqrt{N}$。节省带宽，但故障时延迟更高。 * 更小的 accept 法定人数：$Q_1$ 更大（很少跑），$Q_2$ 更小（每次提交）。稳态提交快。  含义：Phase 1 很少运行的系...
  c10_b05.md: ...更大（很少跑），$Q_2$ 更小（每次提交）。稳态提交快。  含义：Phase 1 很少运行的系统（稳定领导）可以让 accept 法定人数很小以换取快速提交，代价是 leader 变更需要更大的 prepare 法定人数。  这一洞见用于一些 20...
  c10_b08.md: ...小的 $Q_2$ = 更快的提交。  **答案：** 经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 各为 N 的多数。这确保 $Q_1 \cap Q_2 \ne \emptyset$（任意两个多数...
  c10_b08.md: ...- 1$），$Q_2$ 很小（如 2）。任意大小为 $N - 1$ 的 prepare 法定人数与任意大小为 2 的 accept 法定人数都相交（因为 $(N - 1) + 2 > N$）。  代价：leader 变更昂贵（需要 $N - 1$ 个...
  c10_b12.md: ...工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 prepare 与 accept 法定人数相交，而非各为多数。这让运维可以调优：快而常见的路径 vs 慢而罕见的路径。  非区块链 BFT 用例包括受攻...
  c18_b04.md: ...it (token=100)         Bank->>Bank: 100 >= highest (was 0); accept         Bank-->>C1: ok         Note over C1: GC pause 60s...
  c18_b04.md: ...mmit (token=101)         Bank->>Bank: 101 >= highest (100); accept         Bank-->>C2: ok         Note over C1: resumed after...

=== hotstuff (49 occurrences) ===
  c07_b02.md: ...(a) **部分同步** ：假设网络在某个未知全局稳定时间后最终同步；此后消息在已知上界内交付。Paxos、Raft、HotStuff 等现代共识协议假设部分同步。  (b) **随机化** ：协议使用随机数生成器，使对手无法可靠构造无限延迟调度。Ben...
  c07_b03.md: ...中。Paxos 选择一个多数 acceptor 已接受的值。Raft 选择一个多数 follower 已复制的日志条目。HotStuff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certificate。三者都以法定人数作为 agr...
  c07_b05.md: ...止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 |...
  c10_b01.md: ...个副本（而崩溃容错只需 2f+1）。收益：即使最多 f 个节点主动作恶，协议仍正确工作。  本章覆盖现代 BFT 协议：HotStuff（2026 年所有 BFT 系统的基础）、Narwhal-Bullshark（DAG mempool 与共识分离）、Te...
  c10_b01.md: ...t<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et al. 2019<br/>O(N) view change"]...
  c10_b01.md: ...man 2016<br/>simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et al. 2019<br/>O(N) view change"]         HotStuff...
  c10_b01.md: ...HotStuff<br/>Yin et al. 2019<br/>O(N) view change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["My...
  c10_b01.md: ...change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]...
  c10_b01.md: ...--> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>...
  c10_b01.md: ...s2["Aptos 2.x"]         style BG fill:#f9f9f9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6   ### 10.1...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）**...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）** ：给定 N 个验证者的 BFT 集群（$N \ge 3f +...
  c10_b03.md: ...1$）和提议流，产出最多容忍 f 个拜占庭验证者的提交决定，且 view-change 成本为 $O(N)$。机制是 HotStuff 的三阶段投票（prepare / pre-commit / commit）及作为紧凑支持证明的 Quorum Cert...
  c10_b03.md: ...吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更...
  c10_b03.md: ...-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；le...
  c10_b03.md: ...ader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotStuff 把各阶段 pipeline。Leader 在为块 $k+1$ 收集第一阶段投票时，可以同时为块 $k$ 收集第二阶段投...
  c10_b03.md: ...同时为块 $k$ 收集第二阶段投票、为块 $k-1$ 收集第三阶段投票。这使吞吐远高于 PBFT 的串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用...
  c10_b03.md: ...高于 PBFT 的串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（202...
  c10_b03.md: ...用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotStuff 只用于区块链。”HotStu...
  c10_b03.md: ...Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotStuff 只用于区块链。”HotStuff 是 _共识协议_ ；区块链使用它是因为其威胁模型需要 BFT，但协议本身适用于任何...
  c10_b03.md: ...tStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotStuff 只用于区块链。”HotStuff 是 _共识协议_ ；区块链使用它是因为其威胁模型需要 BFT，但协议本身适用于任何 BFT 用例。非区块链 HotSt...
  c10_b03.md: ...Stuff 是 _共识协议_ ；区块链使用它是因为其威胁模型需要 BFT，但协议本身适用于任何 BFT 用例。非区块链 HotStuff 部署包括高金融价值控制面和安全关键配置存储。...
  c10_b04.md: ...因为 DAG 本身即是“共识”机制（每个 block 都被签名并引用）。  生产中：  * **Aptos** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024...
  c10_b04.md: ...ullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff。  Narwhal-Bullshark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚...
  c10_b04.md: ...n、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint 用部分 HotStuff 特性换取简单性：  * 两阶段投票（prevote + precommit），每阶段都需要 2f+1 of 3f+1...
  c10_b04.md: ...der。 * 锁定：一旦验证者 precommit 某值，它在后续轮次中被“锁定”到该值。  Tendermint 比 HotStuff 更易实现和调试。吞吐较低（因为每轮串行、无 pipelining），但对许多区块链用例足够。Cosmos 生态（Osm...
  c10_b07.md: ...需要额外副本的原因：给协议足够冗余，使任何法定人数的投票由诚实 peer 主导。  **Q3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certificates，无需...
  c10_b07.md: ...性 view change；可概括支持的 Quorum Certificates，无需重播所有投票。  **答案：** HotStuff 的关键创新是 _线性 view change_ ：当 leader 被怀疑且新 leader 接管时，消息复杂度是 $...
  c10_b08.md: ...机制：HotStuff 使用 _Quorum Certificates（QC）_。QC 是 2f+1 个签名在特定阶段结果上的集合。QC 是“...
  c10_b08.md: ...当 leader 为提议的 phase k 收集到 2f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本...
  c10_b08.md: ...change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让 view-change 成本主导。Pipelining（不...
  c10_b09.md: ...间结算构建许可链。21 家银行各运行一个验证者节点。威胁模型：任何一家银行可能被攻破，但最多 6 家同时。在 PBFT、HotStuff、Tendermint 之间选择并论证。  提示  $N = 21, f = 6$。检查 3f+1 要求。然后比较消息复...
  c10_b09.md: ...^2)$。21 节点下可工作，但会限制协议成熟后的吞吐。运维工具较旧（多数 PBFT 实现是研究代码）。  (b) **HotStuff** ：$O(N)$ view change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实...
  c10_b09.md: ...ge；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但多绑定特定区块链生态。为许可链实现 HotStuff 比直接拿库需要更多工程。  (c) **Tendermi...
  c10_b09.md: ...已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但多绑定特定区块链生态。为许可链实现 HotStuff 比直接拿库需要更多工程。  (c) **Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好...
  c10_b09.md: ...SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于 HotStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的数千）。  推荐：通过 Cosmos SDK 使用...
  c10_b09.md: ...dermint Core 是成熟、经过验证的库。吞吐低于 HotStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的数千）。  推荐：通过 Cosmos SDK 使用 Tendermint。理由：  * 成熟库和生态。 * 对该...
  c10_b09.md: ...* 对该用例吞吐足够。 * 运维工具和可观测性发展良好。 * 若该链未来需要互操作，Cosmos IBC 是加分项。  HotStuff 技术上“最佳”，但实现负担更重；对 21 节点许可链，Tendermint 的简单性占主导。  PBFT 在 2026...
  c10_b10.md: ...强物理安全和访问控制）而非 BFT。威胁真实，但通过其他手段管理（纵深防御、监控、访问控制）。  **Q3.** 比较 HotStuff 的 pipelining 与 Raft 的日志复制。两者都可有多个“飞行中的决定”——结构上有什么不同？  提示  R...
  c10_b10.md: ...“飞行中的决定”——结构上有什么不同？  提示  Raft leader 串行化提交；多条目可同时在空中但各自独立发送。HotStuff pipeline 让不同提议的三个投票阶段重叠。  **答案：** 两个协议都支持多个在飞决定，但结构不同。  _Ra...
  c10_b10.md: ...，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff pipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重...
  c10_b10.md: ...决定）。  结构差异：  * Raft：$O(1)$ 阶段每决定；pipelining 是“同阶段多个在飞决定”。 * HotStuff：$O(1)$ 阶段每决定（具体 3 个）；pipelining 是“不同决定在不同阶段同时进行”。  吞吐效果：稳态下...
  c10_b10.md: ...3 个）；pipelining 是“不同决定在不同阶段同时进行”。  吞吐效果：稳态下两者都能达到每轮往返提交一个决定。HotStuff 的 per-decision 延迟是 3 轮往返，但因 pipelined 所以吞吐与 Raft 相近。差异体现在延迟...
  c10_b10.md: ...延迟是 3 轮往返，但因 pipelined 所以吞吐与 Raft 相近。差异体现在延迟：Raft 每次提交一轮往返；HotStuff 每次提交三轮往返，但 pipelined 使吞吐相同。  区块链含义：HotStuff 的 per-decision...
  c10_b10.md: ...迟：Raft 每次提交一轮往返；HotStuff 每次提交三轮往返，但 pipelined 使吞吐相同。  区块链含义：HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中是数百毫秒到数秒。若 per-decision...
  c10_b11.md: ...#### 深入思考  **Q1.** 辩护或反驳：“到 2030 年，所有分布式数据库都应使用 HotStuff 风格 BFT，因为威胁模型包含被攻破的云厂商。”  **讨论：** 辩护：云厂商被攻破是真实且日益增长的威胁。Snow...
  c10_b12.md: ...，以及协议消息上的加密签名。  PBFT 是经典实用 BFT 协议；其 $O(N^2)$ 消息复杂度限制了小规模副本集。HotStuff（2019）通过 Quorum Certificates 和 pipelining 实现 $O(N)$ view cha...
  c28_b10.md: ...ommit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 | | 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视...
  c30_b02.md: ...hi, D., Reiter, M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with Linearity and Responsiveness." _PODC_....

=== dynamodb (48 occurrences) ===
  c02_b04.md: ...Spanner 支付著名的 7 ms commit-wait（第13章），通过 TrueTime 提供线性化时间戳。  DynamoDB 在分区期间是 AP（分区节点继续从副本提供读，可能过期）。DynamoDB 在正常运营期间读延迟很低，因为它不对读做任...
  c02_b04.md: ...过 TrueTime 提供线性化时间戳。  DynamoDB 在分区期间是 AP（分区节点继续从副本提供读，可能过期）。DynamoDB 在正常运营期间读延迟很低，因为它不对读做任何协调。Cassandra 类似；其可调一致性让你在请求时选择速度还是严格顺...
  c02_b04.md: ...绝少数侧写入） | C，伴随法定人数写入延迟 TiDB / YugabyteDB | C | C，伴随法定人数写入延迟 DynamoDB | A（可能提供过期读） | L（低延迟读，默认最终一致） Cassandra | 可调（按请求法定人数） | 可调（...
  c02_b08.md: ...请求的，所以 PACELC 分类也是按操作的。  **Q4.** 一个团队正在为一个秒杀库存系统在 Spanner 和 DynamoDB 之间做选择，大促期间每秒 100,000 次购买请求，持续 60 秒。需求是任何商品都不能超卖。用 PACELC 走一...
  c02_b08.md: ...。需求是任何商品都不能超卖。用 PACELC 走一遍决策。  提示  “任何商品不能超卖”是库存减量操作上的线性化需求。DynamoDB 默认一致性是最终一致；能否让该操作线性化？代价是什么？  **答案：** 库存减量的“不超卖”需求是线性化需求。在 P...
  c02_b08.md: ...需要足够分片（按商品分片库存）来分散负载。如果秒杀的是单个热品，所有减量命中一个分片，吞吐受限于该分片的提交吞吐。  DynamoDB：默认最终一致。为“不超卖”需要条件写入（`ConditionExpression`）加强一致读。条件写入在分区层是单项...
  c02_b09.md: ...PACELC 说：对这个操作，两者在分区下都是 PC。Else 情况下，条件写入路径 favors DynamoDB 的延迟，尤其当 Spanner 跨区域时。如果需要同一事务中做其他强一致读（如购物车与库存一起），Spanner 场景...
  c02_b09.md: ...他强一致读（如购物车与库存一起），Spanner 场景更干净。  决策：如果秒杀是单区域，且唯一一致性需求是按项目减量，DynamoDB 条件写入在延迟和成本上胜出。如果秒杀跨区域，或系统需要多项目事务（例如原子地减量库存并增加用户购买记录），Spanne...
  c02_b10.md: ...）。然后指出延迟成本。  提示  大多数“AP”系统都有一条需要强一致性的元数据路径：领导者选举、集群成员、配置。例如，DynamoDB 有条件写入。  **答案：** 大多数“AP”系统仅对数据平面是 AP；它们的控制平面需要强一致性才能正常工作。以 D...
  c02_b10.md: ...B 有条件写入。  **答案：** 大多数“AP”系统仅对数据平面是 AP；它们的控制平面需要强一致性才能正常工作。以 DynamoDB 为例的工作示例：  (a) 通过 `ConditionExpression` 的条件写入在分区层是线性化的。成本：每次...
  c02_b13.md: ...比是多少？1000:1 的读/写工作负载与 1:1 的容忍存储选择完全不同。 * 成本预算是多少？Spanner 昂贵；DynamoDB 小工作负载便宜、热键昂贵；CockroachDB 比 Spanner 便宜但需要运维专长。 * 团队需要与什么集成？现...
  c05_b05.md: ...Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Leaderless | 默认最终一致，可按请求强一致 Redis（master-replica）| Async...
  c05_b06.md: ...ostgreSQL、MongoDB、CockroachDB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片或每 range）是 leader。该分片所有写发往...
  c05_b06.md: ...WW、sibling、CRDT）收敛并发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可...
  c10_b12.md: ...错共识更合适。  第11章开始第四部分：分布式数据库。第7-10章的协议是基础；Spanner、CockroachDB、DynamoDB、Cassandra、FoundationDB 等是在基础上构建的生产系统。本章教授 _诊断方法_ ：如何通过识别复制协...
  c11_b01.md: ...30 分钟就够。  本章教授这套方法。方法有四个问题，按顺序。学完后，你能读懂 Spanner、CockroachDB、DynamoDB、Cassandra 或 FoundationDB，并产出一页阅读笔记，说明每个系统是什么、成本多少、何时失败、为谁设计...
  c11_b01.md: ...t 用 Paxos；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、DynamoDB、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单...
  c11_b02.md: ...**Per-operation 可调** ：应用按请求选择。Cassandra（consistency levels）、DynamoDB（按请求强一致读）、Spanner（提供过期读）。  一致性模型决定应用可以免费避免哪些 bug，必须显式处理哪些。...
  c11_b03.md: ...数侧继续。 * **任意侧服务读、任意处接受写**（分区下 AP）：默认一致性下的 Cassandra、默认一致性下的 DynamoDB。写可能冲突；冲突解决（LWW、CRDT）决定结果。 * **Sloppy quorum**（Dynamo 特有行为）：...
  c11_b03.md: ...ed handoff）；读从任意法定人数。相交保证可能被破坏。 * **应用控制** ：Cassandra 可调一致性、DynamoDB 可选强一致读。应用按请求选择行为。  分区行为是最坏情况行为；PACELC 的 “P” 半部分。结合正常运营成本（PA...
  c11_b03.md: ...制是 §11.1-§11.2 的四个问题诊断；输出是 §11.3 所示 Spanner / CockroachDB / DynamoDB / Cassandra / FoundationDB 的表格形式。变体：把同样方法用于消息队列、缓存、搜索引擎、ML...
  c11_b04.md: ...#### DynamoDB（AWS）  Question | Answer ---|--- Replication protocol | 区域内无...
  c12_b06.md: ...、顺序写热点风险。  哈希适合：用户会话键值存储，每个会话按 ID 访问且无范围查询。示例：Redis Cluster、DynamoDB。  范围适合：时序数据库常查询“T1 到 T2 之间的所有事件”。按时间戳范围分片使该查询成为单分区扫描。示例：HBa...
  c12_b10.md: ...哈希分片确实更简单。对工作负载键分布均匀（无倾斜）的情况，哈希提供更可预测性能和更少元数据开销。Cassandra 和 DynamoDB 不比 CockroachDB 不成熟；它们有不同设计优先级（大规模水平扩展、可预测 per-node 性能）。  综合...
  c14_b01.md: ...SQL、MySQL、Oracle。ACID，单节点或主从复制，纵向扩展。 * **NoSQL** ：Cassandra、DynamoDB、MongoDB。水平扩展，但历史上放弃 ACID 或使用非 SQL API。  NewSQL 系统目标是 “两全其美”...
  c14_b05.md: ...云）、Snowflake（OLAP 任何云） 大规模水平扩展，可接受最终一致 | Cassandra、ScyllaDB、DynamoDB  第11章的诊断方法缩小候选范围；此矩阵是把工作负载类别映射到系统的起点。  **对表中未列工作负载应用方法。** 假...
  c14_b10.md: ...** ：线性化事务。对此工作负载过度；每次写成本太高（每个 score 提交都走共识往返）。能工作但贵。  (b) **DynamoDB（KV）** ：per-item 条件写做原子更新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载...
  c14_b10.md: ...行榜。每个区域有本地计数器；跨区域合并。每区域写亚毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次...
  c14_b10.md: ...*Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5 ms（DynamoDB put）+ 1 ms（...
  c14_b11.md: ...选择由读规模需求（16.7 万 reads/sec）主导。DynamoDB 和 Cassandra 为此设计；NewSQL 在没有积极缓存情况下难以达到每节点如此读量。混合（DynamoDB +...
  c15_b02.md: ...本重试” 拒绝。  CockroachDB、MongoDB、YugabyteDB 都实现了变体。Cassandra 和 DynamoDB 历史上不自动提供会话保证；应用代码必须通过 “本会话路由到主库” 模式自行处理。   ### 15.3 冲突解决策略...
  c15_b03.md: ...Flink 风格系统的人都应知道它提供什么保证以及如何实现。   ### 15.5 生产中的最终一致性   #### DynamoDB  默认读最终一致：读返回最近成功复制的值，但复制滞后可能意味着稍旧的值。区域内滞后通常亚秒。  对需要 read-yo...
  c15_b03.md: ...。代价：通常是最终读延迟的 2 倍，并消耗更多 capacity unit。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-write-wins。应用必须处理自己写入一个区域后被另一区域并...
  c15_b05.md: ...可串行化数据库中的行级锁（Spanner、CockroachDB、Postgres），或 Raft 复制的减一操作，或 DynamoDB 条件写。  成本：每次递减 5-50 ms；每件商品吞吐受共识限制。   #### 最终一致的问题  CRDT-bas...
  c15_b09.md: ...amoDB Global Tables）。他们发现偶尔从一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Global Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **...
  c15_b09.md: ...l Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟...
  c15_b09.md: ...(b) **CRDT-based 字段** ：对多区域可能更新的字段（计数器、标签），使用并发更新能干净合并的数据结构。DynamoDB 原生不支持 CRDT；应用必须自己实现。  (c) **迁移到多主系统 + 无冲突语义** ：CockroachDB...
  c15_b09.md: ...B 的 per-table 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重）很好。对多区域积极写同一 item 的工作负载不合...
  c17_b03.md: ...一致性级别；应用按操作选择。示例：  * **Spanner** ：读写事务（线性化）；有界过期只读；过期读。 * **DynamoDB** ：最终读（默认）；强一致读；条件写（per-item 线性化）。 * **Cassandra** ：per-ope...
  c17_b11.md: ...确性违规，要么浪费资源。  生产系统提供 per-operation 一致性选择：Spanner（线性化 + 过期读）、DynamoDB（最终 + 条件写）、Cassandra（可调级别 + LWT）。应用工程师的任务是为每次操作正确选择。  混合架构组合...
  c29_b04.md: ...0 年系统时，问第11章训练你问的同样四个问题。问题会奏效。系统的答案会与 CockroachDB、Spanner 或 DynamoDB 不同。心智动作相同。  去构建系统。当本书过时时，写下下一本。  * * *  _第29章（后记）于 2026-05-...
  c30_b04.md: ...follower read | CP | PC/EC | 多 region Postgres 兼容；任意云 | | **DynamoDB** | 无领导者；quorum | 透明 | 默认最终；请求时可强一致 | AP 默认 | PA/EL | AWS 原...
  c30_b11.md: ...3 — 读重全球内容分发  **输入**：读主导工作负载；面向用户；延迟敏感；大多数读可接受最终一致。  **决策**：DynamoDB Global Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational...
  c30_b11.md: ...al Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB Global Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CD...
  c30_b12.md: ...和每次写入延迟通常更好。只有真正需要其能力时才支付 Spanner 成本。  **团队需要 SQL 灵活性时不要伸手选 DynamoDB。** DynamoDB 的可预测按请求成本和低延迟是真实好处，但代价是严格的数据建模纪律（PK 设计、GSI 设计、无...
  c30_b12.md: ...只有真正需要其能力时才支付 Spanner 成本。  **团队需要 SQL 灵活性时不要伸手选 DynamoDB。** DynamoDB 的可预测按请求成本和低延迟是真实好处，但代价是严格的数据建模纪律（PK 设计、GSI 设计、无 join）。习惯 “丢...
  c30_b12.md: ...代价是严格的数据建模纪律（PK 设计、GSI 设计、无 join）。习惯 “丢一个查询过去” SQL 灵活性的团队会与 DynamoDB 持续斗争。只有当数据模型真正匹配其访问模式时才选 DynamoDB。  **3 节点集群就能搞定时不要伸手选 Cass...
  c30_b12.md: ...习惯 “丢一个查询过去” SQL 灵活性的团队会与 DynamoDB 持续斗争。只有当数据模型真正匹配其访问模式时才选 DynamoDB。  **3 节点集群就能搞定时不要伸手选 Cassandra。** Cassandra 的运维故事（tombstone...

=== agreement (48 occurrences) ===
  c04_b02.md: ...upper_layer.deliver(msg)   性质（在尽力广播性质之外）：  * **Agreement** ：若任一正确进程交付，所有正确进程都交付。 * **Validity** ：正确发送者广播的消息被所有正确进程交付...
  c04_b02.md: ...upper_layer.deliver(msg)   性质（在尽力广播性质之外）：  * **Agreement** ：若任一正确进程交付，所有正确进程都交付。 * **Validity** ：正确发送者广播的消息被所有正确进程交付...
  c04_b05.md: ...A["Best-effort broadcast<br/>(TCP + for-loop)"] -->|"+ agreement<br/>(re-broadcasting)"| B[Reliable broadcast]         B -->...
  c04_b05.md: ...t broadcast       (TCP plus a for-loop)         |         + agreement (re-broadcasting on receipt)         v     Reliable broadca...
  c04_b06.md: ...（每个副本最终看到每个操作），但它们容忍 _任何顺序_（数据结构无论如何都会收敛）。因此 CRDT 是只需要可靠广播的 agreement 性质、不需要原子广播 ordering 性质的应用示例。成本节约是真实的：基于 CRDT 的计数器能达到 Raft 复...
  c04_b07.md: ...示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. *...
  c04_b07.md: ...付这些广播。每个进程决定其交付的第一个广播所包含的值。由总序，所有正确进程交付相同的第一个广播，因此决定相同值。这满足 agreement（都决定相同）、validity（值来自提议）和 termination（假设广播层可终止）。  _共识 → 原子广播_...
  c04_b07.md: ...CRDT 的 G-Counter 增量。要在 N 个副本上增量计数器，每个“增量”操作必须最终到达每个副本（可靠广播：agreement）。增量顺序不影响最终计数值（CRDT 合并可交换）。原子广播会迫使每个增量都走一轮共识，每个增量都付出法定人数往返。可...
  c04_b09.md: ...P4 仍直接从 P1 收到 m，并且它们会互相重广播（因已有，所以 no-op）。所有正确接收者（P3、P4）都已交付。Agreement 满足。...
  c04_b10.md: ...P2 等到收到多数进程的 ack 后才“向上交付”来防止；按此规则，P2 的本地交付不会发生，于是“要么全要么无”的 agreement 得以维持。  **Q3.** 因果广播用向量时钟实现。对 1000 节点系统，向量是每条消息 1000 项。指出两种让...
  c04_b11.md: ...者侧重试路径已死。没有一致可靠广播（ack 轮）或原子广播（共识），部分交付是永久的。  (b) **接收者失败时没有 agreement。** 若接收者 X 在广播期间崩溃并错过消息，发送者的重试逻辑不向恢复后的节点重发，X 恢复后永远不会得到它。Agre...
  c04_b11.md: ...ment。** 若接收者 X 在广播期间崩溃并错过消息，发送者的重试逻辑不向恢复后的节点重发，X 恢复后永远不会得到它。Agreement 需要恢复后重新交付，通常通过发送者持久化 ack 跟踪或基于 tombstone 的 gossip 层实现。  (c)...
  c04_b11.md: ...者侧重试路径已死。没有一致可靠广播（ack 轮）或原子广播（共识），部分交付是永久的。  (b) **接收者失败时没有 agreement。** 若接收者 X 在广播期间崩溃并错过消息，发送者的重试逻辑不向恢复后的节点重发，X 恢复后永远不会得到它。Agre...
  c04_b11.md: ...ment。** 若接收者 X 在广播期间崩溃并错过消息，发送者的重试逻辑不向恢复后的节点重发，X 恢复后永远不会得到它。Agreement 需要恢复后重新交付，通常通过发送者持久化 ack 跟踪或基于 tombstone 的 gossip 层实现。  (c)...
  c04_b12.md: ...布式系统工程师一个结构化词汇来回答“把一条消息发送给一组进程意味着什么？”尽力广播给你一个 for 循环。可靠广播新增 agreement（任一正确接收者交付蕴含所有正确接收者交付）。一致可靠广播修复接收者崩溃后的缺口。因果广播新增尊重 happens-be...
  c07_b01.md: ...### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一个值，并最终 _决定_ 单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Terminatio...
  c07_b01.md: ...**Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。不同变体会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准...
  c07_b01.md: ...是经典陈述。不同变体会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程...
  c07_b01.md: ...### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一个值，并最终 _决定_ 单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Terminatio...
  c07_b01.md: ...**Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。不同变体会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准...
  c07_b01.md: ...是经典陈述。不同变体会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程...
  c07_b02.md: ...### 7.3 FLP 不可能性  陈述：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能同时以 agreement、validity 和 termination 性质解决共识。  证明微妙，但直觉可接近。想象系统处于某状态 s。调度器...
  c07_b03.md: ...afety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不...
  c07_b03.md: ..._部分同步下 live_（GST 后终止）。网络差时集群不能进展；最坏情况是不可用，而非不一致。   ### 7.6 Agreement-Validity-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙...
  c07_b03.md: ...-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙。   #### Agreement  所有正确进程决定相同值。挑战：进程如何 _知道_ 自己的决定与其他人相同，而没有全局神谕？  经典答案：选择具有 _...
  c07_b03.md: ...uff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certificate。三者都以法定人数作为 agreement 的安全机制。...
  c07_b03.md: ...afety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不...
  c07_b03.md: ..._部分同步下 live_（GST 后终止）。网络差时集群不能进展；最坏情况是不可用，而非不一致。   ### 7.6 Agreement-Validity-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙...
  c07_b03.md: ...-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙。   #### Agreement  所有正确进程决定相同值。挑战：进程如何 _知道_ 自己的决定与其他人相同，而没有全局神谕？  经典答案：选择具有 _...
  c07_b03.md: ...uff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certificate。三者都以法定人数作为 agreement 的安全机制。...
  c07_b04.md: ...lidity 通过确保协议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细...
  c07_b05.md: ...### 7.7 三条性质映射到协议设计  现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人...
  c07_b05.md: ...revote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种...
  c07_b05.md: ...### 7.7 三条性质映射到协议设计  现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人...
  c07_b05.md: ...revote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议...
  c07_b06.md: ...nation。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Terminatio...
  c07_b06.md: ...在实践中不可能。  **答案：** FLP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议...
  c07_b06.md: ...nation。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Terminatio...
  c07_b06.md: ...在实践中不可能。  **答案：** FLP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满...
  c07_b07.md: ...**答案：** _Safety_ 性质说“坏事永不发生”。它被包含坏事件的有限执行违反。示例：agreement（没有两个进程决定不同值）、互斥（没有两个进程同时在临界区）。  _Liveness_ 性质说“好事最终会发生”。它只被...
  c07_b07.md: ...safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；没有任何执行违反 agreement 或 validity。  (b) 在持续坏网络条件下协议可能不进展；这是可用性问题，不是正确性问题。  (c) 运维共...
  c07_b11.md: ...域提交日志使用 Raft，它在所有网络条件下 safe，但在分区期间可能暂停。  三条性质映射到真实协议的具体设计选择：agreement 用法定人数相交，validity 来自 leader 提议的结构，termination 用部分同步加心跳。用这副眼镜...
  c08_b01.md: ...,f=2$。  目标：选定单个值。协议必须保证：  * **Validity** ：只有被提议的值才能被选定。 * **Agreement** ：只选定一个值（协议完成后，没有两个 acceptor 对选定值产生分歧）。 * **Termination**...
  c08_b02.md: ...接受 v 的多数相交；prepare 响应携带 v（及 ballot b）；proposer 必须提议 v。  这就是 agreement 性质：任何已选定的值将永远是选定的值，无论后续跑多少 ballot。...
  c08_b06.md: ...e 中见到这个已接受值，并被强制提议它。  两阶段合起来保证：一旦值被选定，所有后续 ballot 都会认同它。这就是 agreement 性质。  **Q2.** 为什么第二阶段 proposer 必须使用第一阶段响应中最高 ballot 的已接受值，而不...
  c08_b06.md: ...，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Accept 规则（proposer 必须采用第一阶段所见最高 ballot 的已接受值）防止这种情况。任何已被某...

=== read (48 occurrences) ===
  c05_b01.md: ...一位客户存了款，刷新页面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_（分区后两个副本各自接...
  c05_b01.md: ...器磁盘损坏，数据不会丢失。 * **Availability** ：一台机器不可达，系统仍能从另一台服务请求。 * **Read scalability** ：把读分散到副本，成倍提升读吞吐。  复制的成本有两部分：写入延迟增加（必须把写传播到副本...
  c05_b01.md: ...一位客户存了款，刷新页面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_（分区后两个副本各自接...
  c05_b01.md: ...器磁盘损坏，数据不会丢失。 * **Availability** ：一台机器不可达，系统仍能从另一台服务请求。 * **Read scalability** ：把读分散到副本，成倍提升读吞吐。  复制的成本有两部分：写入延迟增加（必须把写传播到副本...
  c05_b02.md: ...equired on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * *...
  c05_b02.md: ...after-write consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...
  c05_b03.md: ...足（Dynamo 的 “hinted handoff”）。相交保证被破坏。 * **Write in progress read** ：若写正在 N 个节点中的 W 个上执行，读命中 R 个节点可能只看到写者集合中少于 W 的节点（写者处于提交中途...
  c05_b04.md: ...写，即使存在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析...
  c05_b04.md: ...记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），...
  c05_b04.md: ...ury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：...
  c05_b04.md: ...er-session 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到...
  c05_b04.md: ...写，即使存在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析...
  c05_b04.md: ...记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），...
  c05_b04.md: ...ury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：...
  c05_b04.md: ...er-session 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到...
  c05_b05.md: ...achDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避免客户存款失败。...
  c05_b08.md: ...kroachDB。列出他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？** 异步...
  c05_b08.md: ...r-range leader 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 read-after-write 保证？** 若仪表盘必须立即反映存款，两个系统都能提供（Postgres 走主库读；Cockr...
  c05_b09.md: ...a 原生不支持；应用层封装可实现。或切换到原生支持的 CockroachDB/YugabyteDB。  (c) **把 read-your-writes 流量路由到主副本** ：若应用知道某键的“主”节点（Cassandra 有自然副本顺序；顺序中...
  c05_b09.md: ...PLICA_DC” 或类似协调者感知模式能做到。  通用教训：quorum 读/写规则给的是相交，不是线性一致性。需要 read-your-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-you...
  c05_b09.md: ...。需要 read-your-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max...
  c05_b09.md: ...max_hlc, result.hlc)             return result          def read(self, key):             # 请求数据的最小 HLC 为 self.max_hlc。...
  c05_b09.md: ...# 请求数据的最小 HLC 为 self.max_hlc。             result = db.read(key, min_hlc=self.max_hlc)             # 服务器返回数据及读取时的 HLC。...
  c05_b09.md: ...作为 SQL 连接会话状态的一部分自动传播；应用无需显式跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c05_b11.md: ...换透明性之间权衡。$W + R > N$ 的法定人数规则给相交，不是线性一致性；差别是许多真实复制 bug 的根源。  Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-s...
  c11_b02.md: ...lation** ：事务看到一致快照，但不防止写-写冲突（写偏可能）。PostgreSQL（实际 REPEATABLE READ；SERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：write...
  c11_b02.md: ...严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。 * **Eventu...
  c15_b01.md: ...erry、Theimer、Petersen、Demers、Spreitzer、Hauser 1994）：  (a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读...
  c15_b03.md: ...ynamoDB  默认读最终一致：读返回最近成功复制的值，但复制滞后可能意味着稍旧的值。区域内滞后通常亚秒。  对需要 read-your-writes 的操作，客户端可请求 `ConsistentRead=true`，对 leader 做同步读。...
  c15_b03.md: ...应用纪律：按需求为每次操作选择一致性级别。许多 Cassandra 生产故障可追溯为使用错误的一致性级别（例如，需要 read-your-writes 时却用 ONE 读）。   #### Redis 复制  主从复制异步。从副本读可能过期。Ma...
  c15_b06.md: ...#### 概念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic w...
  c17_b01.md: ...ns-before 操作 B，则每个进程都先看到 A 再看到 B。并发操作可任意顺序。  为何有用：  * 会话内 **read-your-writes** 自然满足（自己的写 happens-before 后续读）。 * **因果性保持** ：对...
  c17_b04.md: ...* 最终一致允许 “我看到 A 的分数，然后刷新排行榜却没有它” 的失败。B 提交分数后刷新，可能读到没有 B 的副本。Read-your-writes 是因果一致的特例。  排行榜位于频谱中间。因果是正确模型。  **Q3.** 强最终与纯最终一...
  c17_b05.md: ...载选择 Cassandra，并配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUOR...
  c17_b05.md: ...5章）：任何读与任何已提交写至少在一个副本上重叠。这比最终一致强，但弱于线性化。  QUORUM 提供：  * 单会话 read-after-write：若写成功（Q 个写入副本 ack）且后续读用 QUORUM，相交节点有该写；读能看到。 * 跨...
  c17_b07.md: ...不应需要 100ms 共识。  为何不用纯最终：  * 用户可能发帖后下次查看却没看到（若读打到未收到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时...
  c17_b07.md: ...不应需要 100ms 共识。  为何不用纯最终：  * 用户可能发帖后下次查看却没看到（若读打到未收到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时...
  c20_b01.md: ...> DB         R --> L         R --> POST         DB --> DB1["read balance<br/>8 ms"]         DB --> DB2["UPDATE Adaeze<br/>30...
  c23_b03.md: ...ssandra（多次）**：轻量事务违反线性化；计数器不一致；故障转移期间丢失写。 * **MongoDB（多次）**：read concern 问题；分片元数据不一致；故障转移数据丢失。 * **CockroachDB**：早期版本有可串行化违规...
  c23_b07.md: ...）是否成立。  发现：几乎所有主要分布式数据库首次测试时都违反了某些声明。Cassandra 轻量事务、MongoDB read concern、CockroachDB 早期可串行化、Redis 故障转移、etcd 旧版本——都有 Jepsen 发...
  c27_b04.md: ...有显式配置，可能线性化也可能不是，取决于连接设置。带 `AS OF SYSTEM TIME` 或通过 follower-read 设置的读可能返回旧数据。  修复：每个操作显式一致性选择。       def get_account_balance...
  c27_b09.md: ...款。 * 分区下的 split-brain。 * 领导者 failover 下的丢失写。 * 网络恢复后的 stale read。 * 负载下的级联故障。...
  c27_b11.md: ...朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region p...
  c27_b11.md: ...status='failed', reason='busy')          try:             # Read latest balance             balance = db.query(...
  c27_b11.md: ...朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region p...
  c27_b11.md: ...status='failed', reason='busy')          try:             # Read latest balance             balance = db.query(...
  c28_b06.md: ...p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100% 流量。La...
  c30_b04.md: ...一个 Raft；HLC + 偏移 | Raft 重新选举；每 range 亚秒级 | 可串行化 + follower read | CP | PC/EC | 多 region Postgres 兼容；任意云 | | **DynamoDB** |...

=== before (47 occurrences) ===
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering...
  c03_b02.md: ...在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另一个）；有些没有（它们并发，任何顺序都同样有效）。   #...
  c03_b02.md: ...它们并发，任何顺序都同样有效）。   #### 为什么这是正确的原语  本章开头的聊天消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果...
  c03_b02.md: ...在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳...
  c03_b02.md: ...复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-befo...
  c03_b02.md: ...fore。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳——工作流 1...
  c03_b02.md: ...**工作流 1（输入 → 输出）** ：给定跨 N 个进程的本地 + 发送 + 接收事件序列，产出尊重 happens-before 的逐事件时间戳。本节走 Lamport 时间戳；§3.4 扩展到向量时钟（还能检测并发）；§3.5 扩展到 HLC（贴...
  c03_b02.md: ...时钟（还能检测并发）；§3.5 扩展到 HLC（贴近 wall 时间）。该工作流的 _输出_ ——尊重 happens-before 的时间戳——三者相同；_成本_ 和 _检测能力_ 不同。变体：为小成本日志排序选 Lamport；为 CRDT 并发检...
  c03_b02.md: ...< L(b) 不意味着 a \to b；两个事件可能并发。Lamport 时间戳给出 _一致排序_ （happens-before 的一个全序扩展），但不能让你 _检测_ 并发。...
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering...
  c03_b02.md: ...在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  happens-before 关系是一种 _偏序_ 。有些事件有确定顺序（一个因果上先于另一个）；有些没有（它们并发，任何顺序都同样有效）。   #...
  c03_b02.md: ...它们并发，任何顺序都同样有效）。   #### 为什么这是正确的原语  本章开头的聊天消息排序问题，用 happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果...
  c03_b02.md: ...在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳...
  c03_b02.md: ...复保证出现在原消息之后，无论 wall-clock 时间戳如何。  wall-clock 时间戳不尊重 happens-before。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-befo...
  c03_b02.md: ...fore。两台机器时钟相差 100 ms 可能产生这样的 wall-clock 排序：回复的时间戳更早。Happens-before 是系统必须保留的结构；wall-clock 时间是有损代理。   ### 3.3 Lamport 时间戳——工作流 1...
  c03_b02.md: ...**工作流 1（输入 → 输出）** ：给定跨 N 个进程的本地 + 发送 + 接收事件序列，产出尊重 happens-before 的逐事件时间戳。本节走 Lamport 时间戳；§3.4 扩展到向量时钟（还能检测并发）；§3.5 扩展到 HLC（贴...
  c03_b02.md: ...时钟（还能检测并发）；§3.5 扩展到 HLC（贴近 wall 时间）。该工作流的 _输出_ ——尊重 happens-before 的时间戳——三者相同；_成本_ 和 _检测能力_ 不同。变体：为小成本日志排序选 Lamport；为 CRDT 并发检...
  c03_b02.md: ...< L(b) 不意味着 a \to b；两个事件可能并发。Lamport 时间戳给出 _一致排序_ （happens-before 的一个全序扩展），但不能让你 _检测_ 并发。...
  c03_b05.md: ...p 的 wall clock 对此的看法 (t)。新的 l' 必须是最大值，以保证 HLC 单调且尊重 happens-before；它不能小于其中任何一个。因此 l' 至少等于其中之一。四种情况枚举了哪个（或哪些）做出贡献：...
  c03_b06.md: ...是三种或五种：每种对应三个可能“赢家”之一，加上平手时的特殊合并情况。  关键不变式：  * **尊重 happens-before** ：a \to b 蕴含 HLC(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-cl...
  c03_b06.md: ...**恒定大小** ：每个 HLC 是一个 (l, c) 对，不像向量时钟随 N 扩展。 * **尊重 happens-before** ：像 Lamport 时钟，但额外贴近 wall 时间。 * **贴近 wall-clock 时间** ：对人类调...
  c03_b08.md: ...务器（区域分片），HLC 会在任何跨服务器交互中传播 HLC（例如回复携带原消息的 HLC），从而尊重 happens-before。  **修复 2** ：每个聊天室使用向量时钟。向量很小（每个参与者一个条目），能精确检测并发。并发发送的两条消息可以...
  c03_b08.md: ...es, key=lambda m: m.timestamp)` 错在把 wall-clock 时间当作 happens-before 的代理。三种修复任一都能让函数正确。选择哪种取决于你的运营容忍度和所需一致性。   ### 3.9 练习...
  c03_b09.md: ...#### 概念检测  **Q1.** 陈述 Lamport 的 happens-before 关系。给出一个因果有序事件的例子和一个并发事件的例子。  提示  三条规则：同进程排序、消息先发送后接收、传递性。并发...
  c03_b09.md: ...子。  提示  三条规则：同进程排序、消息先发送后接收、传递性。并发是三条都不满足。  **答案：** Happens-before，记作 a \to b，由三条规则定义：(1) 若 a 和 b 在同一进程且 a 先发生，则 a \to b；(2) 若...
  c03_b09.md: ...储写入 x = 5；P_2 读取含 x = 5 的响应，然后写入 y = 10。P_1 对 x 的写入 happens-before P_2 对 y 的写入，因为读响应是 P_2 上由 P_1 发送的消息接收，而 y 的写入又发生在同一进程该接收之后。...
  c03_b09.md: ...wall-clock 时刻写入 x = 5，同时进程 P_2 写入 y = 10，两者未互相通信。没有 happens-before 链连接它们。它们在技术意义上并发；任一排序都与偏序一致。  **Q2.** 两个事件的 Lamport 时间戳分别为...
  c03_b09.md: ...间戳分别为 5 和 7。关于它们的因果关系你能得出什么结论？  提示  Lamport 时间戳单向保持 happens-before，但逆命题不成立。  **答案：** 几乎得不出什么。Lamport 时间戳满足：若 a \to b，则 L(a) <...
  c03_b09.md: ...能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），却不适合 _检测_ 并发的原因。要检测并发需要向量时钟。  **Q3.** 三进程系统（编...
  c03_b10.md: ...贴近 wall-clock 时间（105 只比 wall-clock 102 早 3 ms），并尊重 happens-before：接收 HLC (105, 2) 按字典序严格大于消息 HLC (105, 1)，也严格大于之前本地 HLC (100,...
  c03_b11.md: ...LC 替换向量时钟。每条消息携带一个 (l, c) 对，而非 10,000 项向量。权衡：HLC 尊重 happens-before 但无法检测并发。如果应用需要并发检测（CRDT 合并、冲突解决），单用 HLC 不够，还需要每键版本向量或类似机制。...
  c03_b14.md: ...无需同步时钟的修复：用 HLC 加每服务器单调序列号给每条日志行盖戳。HLC 在服务器交换消息时尊重 happens-before（例如，携带 HLC 的请求 ID）。单调序列号保留每台服务器本地顺序。按 HLC 排序显示合并日志，平手用服务器 ID...
  c03_b15.md: ...有损代理。用跨机器时间戳比较来决定“哪个先发生”的代码是在对一个虚构概念做推理。  Lamport 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-before 但不能检测并发。向量时...
  c03_b15.md: ...t 的 happens-before 是正确的原语。Lamport 时间戳为每个事件分配一个整数，尊重 happens-before 但不能检测并发。向量时钟为每个进程每个事件分配一个整数，能检测并发，但扩展性差。混合逻辑时钟（HLC）结合 wall-...
  c04_b03.md: ...5 因果广播（Causal Broadcast）  可靠广播不保序。因果广播新增：_若消息 m_1 在 happens-before 关系下先于 m_2 广播（即 broadcast(m_1) \to broadcast(m_2)），则任何正确进程都不...
  c04_b12.md: ...nt（任一正确接收者交付蕴含所有正确接收者交付）。一致可靠广播修复接收者崩溃后的缺口。因果广播新增尊重 happens-before 的顺序。原子广播新增所有正确进程的全序。  原子广播等价于共识，实现它的协议（Raft、Paxos、ZAB、HotSt...
  c16_b02.md: ...\(x\)，合并结果必须决定谁赢。使用 tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Se...
  c16_b07.md: ...*用 HLC 代替 wall clock** ：HLC（第3章）可减少但不能消除偏移问题。HLC 尊重 happens-before；因果有序的两写会被正确打时间戳。并发写仍按 HLC 平票规则排序（本质上是并发集合内的 LWW）。  (c) **领域...
  c17_b01.md: ...。应用代码为每次操作选合适级别。   ### 17.2 因果一致性  经典表述（第3章 Lamport happens-before 在操作上的应用）：若操作 A happens-before 操作 B，则每个进程都先看到 A 再看到 B。并发操作可任...
  c17_b01.md: ...一致性  经典表述（第3章 Lamport happens-before 在操作上的应用）：若操作 A happens-before 操作 B，则每个进程都先看到 A 再看到 B。并发操作可任意顺序。  为何有用：  * 会话内 **read-your...
  c17_b01.md: ...可任意顺序。  为何有用：  * 会话内 **read-your-writes** 自然满足（自己的写 happens-before 后续读）。 * **因果性保持** ：对消息的回复排在该消息之后；对评论的子评论排在其父评论之后。 * **比线性化便...
  c17_b04.md: ...发操作可任意。线性化：实时顺序。最终：收敛过程中无顺序保证。  **答案：** 因果一致性：若操作 A happens-before 操作 B（Lamport 意义），则每个进程先看到 A 再看到 B。并发操作（无 happens-before 关系）...
  c17_b04.md: ...ens-before 操作 B（Lamport 意义），则每个进程先看到 A 再看到 B。并发操作（无 happens-before 关系）可任意顺序；不同副本可看到不同顺序。  与线性化相比：线性化额外要求操作按实时顺序排列（若 A 在 B 开始前完...
  c27_b03.md: ...ll clock 排序。应由因果性排序的事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-before、HLC 或 vector clock（第3章）排序。       def merge_events(events_fr...
  c27_b03.md: ...(events_from_replicas):         # 按 HLC 排序（假设事件带有正确 happens-before 传播的 HLC 时间戳）         return sorted(events_from_replicas, ke...
  c28_b10.md: ...| 逻辑时钟实现 | Ch.3 §3.3 | N 个进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比...
  c30_b06.md: ...HLC（混合逻辑时钟）**：二元组 (l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工...

=== max (47 occurrences) ===
  c03_b02.md: ...发送消息时，消息携带 L_p（当前计数器）。 * p 接收时间戳为 t 的消息时，令 L_p \leftarrow \max(L_p, t) + 1，并用新的 L_p 标记接收事件。  保证：若 a \to b，则 L(a) < L(b)。...
  c03_b03.md: ...s 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | send msg n to P_3 (carries 4) | 4 6...
  c03_b03.md: ...| send msg n to P_3 (carries 4) | 4 6 | P_3 | receive n | \max(1, 4) + 1 = 5  事件 a 的 Lamport 时间戳为 1；事件 x 的时间戳为 1。两者并发（没有 h...
  c03_b03.md: ...，消息携带当前 V_p。 * p 接收带向量 V_m 的消息时，对所有 i 设置 V_p[i] \leftarrow \max(V_p[i], V_m[i])，然后将 V_p[p] 加 1。  保证：a \to b 当且仅当 V(a) < V(b...
  c03_b05.md: ...* **本地事件** 在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, t)；若 l' = l_p 则 c_p 加 1，否则令 c_p \leftarrow 0；然后令 l_p \...
  c03_b05.md: ...l_m, c_m) 的消息在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, l_m, t)；然后更新计数器：     * 若 l' = l_p = l_m：c' \leftarrow...
  c03_b05.md: ...l_p, l_m, t)；然后更新计数器：     * 若 l' = l_p = l_m：c' \leftarrow \max(c_p, c_m) + 1     * 否则若 l' = l_p：c' \leftarrow c_p + 1...
  c03_b06.md: ..._m = l')** ：这是“合并”情况。两端逻辑时钟相同；计数器必须递增过两个先前计数器以保留顺序。因此 c' = \max(c_p, c_m) + 1。 * **只有 p 的先前 HLC 贡献了 (l' = l_p，严格大于 l_m 和 t)...
  c03_b06.md: ...，携带 (100, 1) | (100, 1) 4 | P_2 | 95（P_2 时钟落后 5），接收 | l' = \max(0, 100, 95) = 100 = l_m；应用“否则若 l' = l_m”情况：c' = c_m + 1 = 1...
  c03_b08.md: ...7 ms + GPS 原子钟基础设施 CockroachDB / YugabyteDB / TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB |...
  c03_b09.md: ...近事件是否并发？  提示  接收规则：逐元素取最大值，然后自己的条目加 1。  **答案：** 首先逐元素取最大值：[\max(3, 4), \max(5, 4), \max(2, 2)] = [4, 5, 2]。然后 P_3 把自己的条目（第三...
  c03_b09.md: ...接收规则：逐元素取最大值，然后自己的条目加 1。  **答案：** 首先逐元素取最大值：[\max(3, 4), \max(5, 4), \max(2, 2)] = [4, 5, 2]。然后 P_3 把自己的条目（第三个位置，因为进程编号 1...
  c03_b09.md: ...大值，然后自己的条目加 1。  **答案：** 首先逐元素取最大值：[\max(3, 4), \max(5, 4), \max(2, 2)] = [4, 5, 2]。然后 P_3 把自己的条目（第三个位置，因为进程编号 1-2-3）加 1：[4,...
  c03_b10.md: ...）的最大值。计数器根据三者中哪一个贡献了 l' 来更新。  **答案：** 应用 §3.5 的四种情况。  l' = \max(l_p, l_m, t) = \max(100, 105, 102) = 105。  比较哪个值做出贡献：l' = 1...
  c03_b10.md: ...l' 来更新。  **答案：** 应用 §3.5 的四种情况。  l' = \max(l_p, l_m, t) = \max(100, 105, 102) = 105。  比较哪个值做出贡献：l' = 105 = l_m，但 l' \ne l_...
  c03_b12.md: ...动时获取时间戳 t_s。如果读时观察到已提交写入的时间戳 t_w 满足 t_s < t_w < t_s + \text{max\_skew}，系统无法判断该写入在 wall-clock 时间中是否发生在事务开始之前，于是 _重启_ 事务，用新时间...
  c03_b12.md: ...至少包含 6 个事件、至少一对发送-接收的三进程示例。  提示  三条规则：本地事件 L 加 1；发送携带 L；接收取 max(L, 收到) 再加 1。...
  c03_b13.md: ...def receive(self, sender_L, msg):             self.L = max(self.L, sender_L) + 1             return self.L, msg   三进程走...
  c03_b13.md: ...| P1 发送 m 给 P2 | 3 | — | 1 | m 携带 L = 3 5 | P2 接收 m | 3 | \max(0, 3) + 1 = 4 | 1 | 6 | P2 发送 n 给 P3 | 3 | 5 | — | n 携带 L =...
  c03_b13.md: ...2 发送 n 给 P3 | 3 | 5 | — | n 携带 L = 5 7 | P3 接收 n | 3 | 5 | \max(1, 5) + 1 = 6 | 8 | P3 本地事件 y | 3 | 5 | 7 | y 的 L = 7  因果链：...
  c05_b09.md: ...ead-your-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max-HLC-observed。读请求带“最小所需 HLC”。服务器要么等待，要么返回该 HLC 的数据，要么拒绝。  **...
  c05_b09.md: ...e)             # 服务器返回写提交时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return result...
  c05_b09.md: ...# 服务器返回数据及读取时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return result.value...
  c06_b09.md: ...**：如果服务网格允许 7 跳中 4 跳作为一批并行（fan-out 模式），其余 3 跳串行：  * 并行批延迟 = max(4 个并行 hop 的 p99)。每个约 115 ms p99，批 p99 也是 ~115 ms（4 个中最大值，独立...
  c08_b02.md: ...收到 `PREPARE(b)` 后：  * 若 $b$ 小于等于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复 `PROMISE...
  c08_b02.md: ...于 acceptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复 `PROMISE(b, accepted_ballot, accepted_va...
  c08_b02.md: ...T(b, v)`。  每个 acceptor 收到 `ACCEPT(b, v)` 后：  * 若 $b$ 小于 $b_{max}$，忽略。 * 否则，把 $(b, v)$ 持久化为 `accepted_ballot, accepted_value...
  c08_b04.md: ...千行。差距在运维细节：  * **Persistent state** ：acceptor 必须在崩溃间持久化 $b_{max}$、`accepted_ballot`、`accepted_value`。丢失这些状态会破坏 safety。 * **...
  c08_b05.md: ...`PREPARE(b=10)` 给三个 acceptor。  **步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null,...
  c08_b05.md: ...**步骤 2** ：三个 acceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到...
  c08_b05.md: ...：第二个 proposer 发送 `PREPARE(b=20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`（先前接受的值及其...
  c08_b07.md: ...) **持久状态纪律** ：acceptor 必须在回复 prepare 或 accept 消息 _之前_ 把 $b_{max}$、`accepted_ballot`、`accepted_value` 持久化到可靠存储。若 acceptor 丢失...
  c08_b08.md: ...B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Acceptors 4、5 先收到 A 的；...
  c08_b08.md: ...B `PROMISE(20, null, null)`。Acceptors 4、5 先收到 A 的；它们设置 $b_{max}=10$ 并回复 A `PROMISE(10, null, null)`。随后它们收到 B 的，设置 $b_{max}...
  c08_b08.md: ...{max}=10$ 并回复 A `PROMISE(10, null, null)`。随后它们收到 B 的，设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。  A 的情况：A 只有 acceptors...
  c08_b08.md: ...llot，例如 30。 * A 发送 `PREPARE(b=30)`。 * 所有 5 个 acceptor 的 $b_{max}=20$ 且 `accepted_value = "Olamide"`（`accepted_ballot = 20`）...
  c13_b05.md: ...eTime（GPS + 原子钟） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB...
  c16_b02.md: ...\(\sum_i V[i]\)。 * 合并两个 G-Counter：\(V_{\text{merged}}[i] = \max(V_1[i], V_2[i])\)。  逐元素 max 合并满足交换律、结合律、幂等。值（总和）定义良好且单调递增。...
  c16_b02.md: ...unter：\(V_{\text{merged}}[i] = \max(V_1[i], V_2[i])\)。  逐元素 max 合并满足交换律、结合律、幂等。值（总和）定义良好且单调递增。  G-Counter 只能递增；PN-Counter 扩...
  c16_b02.md: ...值：\(\sum_i P[i] - \sum_i N[i]\)。 * 合并：对 \(P\) 和 \(N\) 分别逐元素 max。  PN-Counter 可表达任意整数值。它不强制 “值绝不低于零”，因此不能替代库存（第15章）。   ####...
  c16_b06.md: ...Counter 和 PN-Counter 的操作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。PN-Counter：两个 G-Counter（正、负）。  **答案：**  _G-Counter_ ：向量...
  c16_b06.md: ...i]\)。 * 合并 \(V_1, V_2\)：对每个 \(i\)，\(V_{\text{merged}}[i] = \max(V_1[i], V_2[i])\)。  示例：3 个副本。每个初始 \([0,0,0]\)。副本 1 递增两次：\([...
  c16_b06.md: ...值：\(\sum_i P[i] - \sum_i N[i]\)。 * 合并：对 \(P\) 和 \(N\) 分别逐元素 max。  示例：3 个副本。R1 递增两次、递减一次：\(P=[2,0,0], N=[1,0,0]\)。R2 递增一次：\(...
  c20_b07.md: ...C → D 当前是串行，能否把其中某些调用并行？例如，若 A 对 B 和 A 对 C 的调用独立，能否并行（总延迟变成 max(B,C) 而非 B + C）？  (d) **质疑是否所有跳转都必要**：4 个服务跳转对单个请求来说很多。能否通过反...
  c20_b07.md: ...* 若两者都降：总延迟降到 290 ms（在预算内）。  并行化：若 A → (B、C 并行) → D，总为 50 + max(200,40) + 180 = 430 ms；只有边际改善。  重构：若 C 的工作能在 B 内完成（消除一跳），总为...
  c29_b01.md: ...务将替代许多工作负载的 HLC  2026 现实（第3、13章）：TrueTime 仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 cl...
  c29_b01.md: ...为关键任务工作负载部署自己的 TrueTime 等价基础设施。  含义：到 2029-2030 年，"使用 HLC + max-clock-skew" 可能像今天的 "仔细调 NTP" 一样显得过时。迁移路径已知；心智模型（时间不确定；系统必须界...

=== open (46 occurrences) ===
  c01_b05.md: ...：Mohri 的 _Federated Learning_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork...
  c19_b08.md: ...被阻塞（通常 5-15 秒）。此窗口查询 Consul 的客户端可能收到过期或错误响应。  若客户端配置为 “fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail closed”（Co...
  c21_b02.md: ...stateDiagram-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)...
  c21_b02.md: ...lure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)...
  c21_b02.md: ...e calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/...
  c21_b02.md: ...ll calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contact         Hal...
  c21_b02.md: ...downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通...
  c21_b02.md: ...所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游失败时，上...
  c21_b02.md: ...。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复...
  c21_b02.md: ...sed；若失败，返回 Open。  收益：当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复。  权衡：Open 状态下，即使某些下游调用可能成功，请求也快速失败。对某些工作负载不可接受；对...
  c21_b02.md: ...当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复。  权衡：Open 状态下，即使某些下游调用可能成功，请求也快速失败。对某些工作负载不可接受；对其他工作负载，“快速失败” 比 “挂 30...
  c21_b02.md: ...stateDiagram-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)...
  c21_b02.md: ...lure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)...
  c21_b02.md: ...e calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/...
  c21_b02.md: ...ll calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contact         Hal...
  c21_b02.md: ...downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通...
  c21_b02.md: ...所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游失败时，上...
  c21_b02.md: ...。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复...
  c21_b02.md: ...sed；若失败，返回 Open。  收益：当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复。  权衡：Open 状态下，即使某些下游调用可能成功，请求也快速失败。对某些工作负载不可接受；对...
  c21_b02.md: ...当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-open 探测），正常流量恢复。  权衡：Open 状态下，即使某些下游调用可能成功，请求也快速失败。对某些工作负载不可接受；对其他工作负载，“快速失败” 比 “挂 30...
  c21_b05.md: ...y 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b05.md: ....** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b05.md: ...y 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b05.md: ....** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b06.md: ...再联系下游。  三状态：  (a) **Closed**：正常操作。所有调用都到下游。熔断器跟踪成功率。  (b) **Open**：失败阈值被超过（例如最近 30 秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一...
  c21_b06.md: ...**Open**：失败阈值被超过（例如最近 30 秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）...
  c21_b06.md: ...秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Clo...
  c21_b06.md: ...联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。  收益：当下...
  c21_b06.md: ...进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复...
  c21_b06.md: ...sed；若失败，返回 Open。  收益：当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复。  权衡：Open 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用...
  c21_b06.md: ...当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复。  权衡：Open 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用都比没有好），Closed-with-re...
  c21_b06.md: ...作负载（任何成功调用都比没有好），Closed-with-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netflix Hystrix（已弃用；被 Resilience4j 替代）；Resilie...
  c21_b06.md: ...再联系下游。  三状态：  (a) **Closed**：正常操作。所有调用都到下游。熔断器跟踪成功率。  (b) **Open**：失败阈值被超过（例如最近 30 秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一...
  c21_b06.md: ...**Open**：失败阈值被超过（例如最近 30 秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）...
  c21_b06.md: ...秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Clo...
  c21_b06.md: ...联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。  收益：当下...
  c21_b06.md: ...进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复...
  c21_b06.md: ...sed；若失败，返回 Open。  收益：当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复。  权衡：Open 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用...
  c21_b06.md: ...当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-open 探测），正常流量恢复。  权衡：Open 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用都比没有好），Closed-with-re...
  c21_b06.md: ...作负载（任何成功调用都比没有好），Closed-with-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netflix Hystrix（已弃用；被 Resilience4j 替代）；Resilie...
  c21_b10.md: ...常 10%）来限制重试驱动放大。持续失败下，上限随成功流量下降；放大保持有界。  熔断器监控下游成功率；超过阈值时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load she...
  c21_b10.md: ...续失败下，上限随成功流量下降；放大保持有界。  熔断器监控下游成功率；超过阈值时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load shedding 防止队列堆积。用 HT...
  c21_b10.md: ...常 10%）来限制重试驱动放大。持续失败下，上限随成功流量下降；放大保持有界。  熔断器监控下游成功率；超过阈值时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load she...
  c21_b10.md: ...续失败下，上限随成功流量下降；放大保持有界。  熔断器监控下游成功率；超过阈值时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load shedding 防止队列堆积。用 HT...
  c22_b06.md: ...后时，熔断器在 30 秒内 50% 失败率打开；以相同阈值重新关闭。有滞后时：30 秒内 50% 失败率打开；half-open 探测中 5 秒内 80% 成功就关闭。关闭是激进的：一旦检测到短暂恢复，熔断器就重开。  无滞后时，熔断器在可变条件下...
  c30_b19.md: ...），Ch.18（fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流...

=== validity (46 occurrences) ===
  c04_b02.md: ...TCP 加一个 for 循环。它有时够用（缓存失效提示，漏一个可接受），对状态复制几乎总是错的。  性质：  * **Validity** ：若发送者正确且接收者正确，接收者最终交付消息。 * **No-creation** ：未被发送的消息不会被交付。...
  c04_b02.md: ...性质（在尽力广播性质之外）：  * **Agreement** ：若任一正确进程交付，所有正确进程都交付。 * **Validity** ：正确发送者广播的消息被所有正确进程交付。  可靠广播 _不_ 保证：  * **顺序** ：两条消息可能在不同接...
  c04_b07.md: ...otal order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：validity（发送者正确、接收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增什么。   2. **R...
  c04_b07.md: ...一个广播所包含的值。由总序，所有正确进程交付相同的第一个广播，因此决定相同值。这满足 agreement（都决定相同）、validity（值来自提议）和 termination（假设广播层可终止）。  _共识 → 原子广播_ 。假设有一个决定单值的共识原语...
  c07_b01.md: ...于“异步 + 崩溃”。本章其余部分讨论这些更强假设是什么、为什么部分同步是现代逃生舱，以及三条性质（agreement、validity、termination）对任何协议的要求。   ### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一...
  c07_b01.md: ...提议_ 一个值，并最终 _决定_ 单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。...
  c07_b01.md: ...会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程间消息按发送顺序到...
  c07_b01.md: ...于“异步 + 崩溃”。本章其余部分讨论这些更强假设是什么、为什么部分同步是现代逃生舱，以及三条性质（agreement、validity、termination）对任何协议的要求。   ### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一...
  c07_b01.md: ...提议_ 一个值，并最终 _决定_ 单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。...
  c07_b01.md: ...会微调（例如 uniform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程间消息按发送顺序到...
  c07_b02.md: ...3 FLP 不可能性  陈述：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能同时以 agreement、validity 和 termination 性质解决共识。  证明微妙，但直觉可接近。想象系统处于某状态 s。调度器可选择下一条交付的...
  c07_b02.md: ...T 前系统完全异步（无界）；GST 后系统同步（有界延迟）。协议在异步下必须保持 _safe_（agreement 和 validity 永不违反），并在 GST 后最终 _live_（在 GST 后有限时间内保证终止）。  这种划分是现代共识设计模式：把...
  c07_b03.md: ...事永不发生”的性质。示例：  * Agreement 是 safety 性质：任何时刻两个正确进程不会决定不同值。 * Validity 是 safety 性质：任何进程不会决定未被提议的值。 * 互斥是 safety 性质：任何时刻不会两个进程同时进入临...
  c07_b03.md: ...ve_（GST 后终止）。网络差时集群不能进展；最坏情况是不可用，而非不一致。   ### 7.6 Agreement-Validity-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙。   ####...
  c07_b04.md: ...#### Validity  决定值必须是某个进程提议的值。挑战：如果每个提议者都提议同一个默认值？如果提议者在其值被看到前崩溃？  最简单形式下...
  c07_b04.md: ...决定值必须是某个进程提议的值。挑战：如果每个提议者都提议同一个默认值？如果提议者在其值被看到前崩溃？  最简单形式下，validity 是平凡的：提议者的值是唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广...
  c07_b04.md: ...者在其值被看到前崩溃？  最简单形式下，validity 是平凡的：提议者的值是唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreem...
  c07_b04.md: ...唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity...
  c07_b04.md: ...Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发...
  c07_b04.md: ...termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发送过的值。   #### Termination  所有正确进程最终...
  c07_b04.md: ...#### Validity  决定值必须是某个进程提议的值。挑战：如果每个提议者都提议同一个默认值？如果提议者在其值被看到前崩溃？  最简单形式下...
  c07_b04.md: ...决定值必须是某个进程提议的值。挑战：如果每个提议者都提议同一个默认值？如果提议者在其值被看到前崩溃？  最简单形式下，validity 是平凡的：提议者的值是唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广...
  c07_b04.md: ...者在其值被看到前崩溃？  最简单形式下，validity 是平凡的：提议者的值是唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreem...
  c07_b04.md: ...唯一候选。实用形式（多提议者、异步）中，validity 通过确保协议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity...
  c07_b04.md: ...Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发...
  c07_b04.md: ...termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发送过的值。   #### Termination  所有正确进程最终...
  c07_b05.md: ...条性质映射到协议设计  现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广...
  c07_b05.md: ...心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和...
  c07_b05.md: ...值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐...
  c07_b05.md: ...文很有用。几乎每种“新共识协议”只改变三者之一：新法定人数结构（HotStuff 的线性 view change）、新 validity 检查（拜占庭验证者）、新终止机制（Narwhal-Bullshark 等基于 DAG 的协议）。   ### 7.8...
  c07_b05.md: ...条性质映射到协议设计  现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广...
  c07_b05.md: ...心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和...
  c07_b05.md: ...值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐...
  c07_b05.md: ...文很有用。几乎每种“新共识协议”只改变三者之一：新法定人数结构（HotStuff 的线性 view change）、新 validity 检查（拜占庭验证者）、新终止机制（Narwhal-Bullshark 等基于 DAG 的协议）。   ### 7.8...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：...
  c07_b06.md: ...各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  进程通过点对点消息通信...
  c07_b06.md: ...**答案：** FLP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满足更强假设（部分同...
  c07_b06.md: ...（消息在未知但有限上界内交付）。  协议在任何条件下（包括 GST 前异步）必须 _safe_（agreement 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：...
  c07_b06.md: ...各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  进程通过点对点消息通信...
  c07_b06.md: ...**答案：** FLP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满足更强假设（部分同...
  c07_b06.md: ...（消息在未知但有限上界内交付）。  协议在任何条件下（包括 GST 前异步）必须 _safe_（agreement 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构...
  c07_b07.md: ...ss 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；没有任何执行违反 agreement 或 validity。  (b) 在持续坏网络条件下协议可能不进展；这是可用性问题，不是正确性问题。  (c) 运维共识集群的根本是确保有利...
  c07_b11.md: ...。   ### 本章总结  共识是让一组进程在部分进程崩溃的情况下就一个值达成一致的问题。三条性质（agreement、validity、termination）是经典陈述。FLP 不可能性说确定性协议在纯异步下一次崩溃下不能保证终止全部三条；现代逃生舱是...
  c07_b11.md: ...络条件下 safe，但在分区期间可能暂停。  三条性质映射到真实协议的具体设计选择：agreement 用法定人数相交，validity 来自 leader 提议的结构，termination 用部分同步加心跳。用这副眼镜读任何新共识论文，结构都会显现。...
  c08_b01.md: ...忍的崩溃故障数。经典设定是 $N=3,f=1$ 或 $N=5,f=2$。  目标：选定单个值。协议必须保证：  * **Validity** ：只有被提议的值才能被选定。 * **Agreement** ：只选定一个值（协议完成后，没有两个 accepto...

=== redlock (44 occurrences) ===
  c01_b09.md: ...leader_lock", node_id, nx=True, ex=30): become_leader()`。这是 Redlock 模式（第18章）。它假设持有锁 30 秒就意味着没有其他节点持有它。在网络分区或 GC 暂停下，两个节点可能都相信自己...
  c01_b11.md: ...。如果领导者挂掉，key 过期，别人接管。”指出该设计未处理的三种失败模式。需要时可参考第18章。  提示  该模式是 Redlock。Jepsen / Kleppmann 辩论至少指出三种失败模式。想想 GC 暂停、到 Redis 的网络分区、以及旧领...
  c18_b01.md: ...=30)   > 说明性伪代码；bug 在于原语选择（无 fencing token 的超时锁），而非语法。  这就是 Redlock 模式。代码很短。生产环境中，它偶尔会允许两个领导者同时行动。原因是分布式系统中最深刻的论证之一：Martin Klep...
  c18_b01.md: ...。生产环境中，它偶尔会允许两个领导者同时行动。原因是分布式系统中最深刻的论证之一：Martin Kleppmann 对 Redlock 的分析以及 _fencing token_ 的作用。  本章讲授两个相关原语：分布式缓存（具体是 Redis Clus...
  c18_b02.md: ...布式锁  锁协调跨进程对共享资源的访问。朴素形式：在共享存储中放一个 key；key 持有者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis 作者）提出 Redlock 作为分布式锁算法：  (a)...
  c18_b02.md: ...者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis 作者）提出 Redlock 作为分布式锁算法：  (a) 客户端选一个唯一值（随机 nonce）。(b) 客户端尝试在 N 个独立 Redis m...
  c18_b03.md: ...016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它为何没有提供表面上的安全性质。该文是分布式锁的奠基性读物。  核心问题：分布式锁有 _两个_ 常被混淆的目的。...
  c18_b03.md: ...正确性**：同一时间只有一个客户端修改受保护资源。若两个同时做，资源损坏。示例：支付提交，两个并发提交导致重复扣款。  Redlock 对效率场景提供合理保证。对正确性场景则不提供保证，原因有二：  (a) **进程暂停**。客户端获得锁后经历长时间 s...
  c18_b03.md: ...停）。锁 TTL 过期。另一个客户端获得锁。原客户端恢复，未意识到锁已过期，继续修改资源。  (b) **时钟漂移**。Redlock 的安全分析假设时钟大致同步。若客户端时钟偏快，其锁 TTL 从 Redis 侧看更早过期。两个客户端可能同时认为自己持...
  c18_b03.md: ...受保护资源（存储系统、API 端点）跟踪已接受的最高 token。任何带更低 token 的操作被拒绝。  如何修复 Redlock 的失败模式：  (a) 进程暂停：客户端 A 用 token 100 获得锁；暂停 60 秒（TTL 过期）；客户端...
  c18_b03.md: ...s 本身不支持 per-key fencing token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。” 这过于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  *...
  c18_b03.md: ...token。应用必须在其上叠加。   #### 常见误解  常见误读：“Redlock 错了；不要用。” 这过于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  * Job queue：用 Redlock（或任何...
  c18_b03.md: ...于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  * Job queue：用 Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：...
  c18_b04.md: ...改受保护资源。  （Raft 复制的锁服务）+（受保护资源上的 fencing token）的组合是正确生产模式。朴素 Redlock 是错误模式；正确模式更复杂，但复杂性必要。...
  c18_b05.md: ...cache.delete("user_processing_lock_42")   这种模式因简单而诱人。失败原因除了 Redlock（进程暂停、时钟漂移、缓存故障）还有缓存特有原因：  (a) **缓存故障转移**：Redis Sentinel 提升副...
  c18_b06.md: ...#### 概念检测  **Q1.** 描述 Redlock 模式。根据 Kleppmann 的批评，它为什么不提供正确性？  提示  Redlock：让多数 Redis mast...
  c18_b06.md: ...检测  **Q1.** 描述 Redlock 模式。根据 Kleppmann 的批评，它为什么不提供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **...
  c18_b06.md: ...edis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯一值作为 key 来获取锁。若多数（3/5）在超...
  c18_b06.md: ...锁有 TTL；客户端通过删除 key 释放（需验证 value 与 nonce 匹配）。  Kleppmann 的批评：Redlock 不提供正确性，因为：  (a) _进程暂停_ ：客户端获得锁后暂停（GC、VM 暂停）。从 Redis 看 TTL 过...
  c18_b06.md: ...看法可能不同。两者对锁是否仍有效可能意见不一。  两者都是基于超时协议的知名问题，也适用于许多朴素分布式锁方案，不只是 Redlock。  修复是 fencing token：锁服务在每次获取时颁发单调递增 token。受保护资源跟踪已接受的最高 tok...
  c18_b06.md: ...高 token；任何更低 token 的操作被拒绝。即使两个客户端都以为自己持有锁，只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2...
  c18_b06.md: ...支持 fencing token；应用或中间件必须叠加。对正确性关键用例，这是必要工作。  **Q3.** 基于缓存的 Redlock 式锁何时可接受，何时不可？  提示  效率用例可接受（重复只是浪费但不灾难）。正确性用例不可接受（重复是灾难）。...
  c18_b08.md: ...示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有不同成本/安全权衡。  **答案：**  (a) **Redlock** ：简单、快、除 Redis 外无需额外基础设施。若任务幂等（偶尔双处理浪费但无害）可接受。若任务有非幂等副作用（发...
  c18_b08.md: ...有 Postgres 的应用，运维干净：无额外基础设施。  推荐：  * 幂等任务（大多数 ETL 流水线、周期任务）：Redlock 即可。最简单最快。 * 非幂等任务：若已有数据库，选 (c)；若无，选 (b)。两者都提供正确性。 * 高吞吐 job...
  c18_b10.md: ...cing token 采用的瓶颈。  **Q2.** 一个 LLM 被要求 “为我们的微服务实现分布式锁”。它产出一个 Redlock 实现。指出 LLM 可能没考虑的三件事，并解释为什么 “Redlock” 很少是完整答案。  **讨论：**  (a)...
  c18_b10.md: ...求 “为我们的微服务实现分布式锁”。它产出一个 Redlock 实现。指出 LLM 可能没考虑的三件事，并解释为什么 “Redlock” 很少是完整答案。  **讨论：**  (a) **正确性 vs 效率的区别**。LLM 没问锁用途就给出一个方案。若...
  c18_b10.md: ...讨论：**  (a) **正确性 vs 效率的区别**。LLM 没问锁用途就给出一个方案。若用例是正确性（支付、库存），Redlock 错误。正确答案取决于用例；LLM 没问。  (b) **部署特定的失败模式**。Redlock 的安全假设 “5 个独...
  c18_b10.md: ...是正确性（支付、库存），Redlock 错误。正确答案取决于用例；LLM 没问。  (b) **部署特定的失败模式**。Redlock 的安全假设 “5 个独立 Redis master” 与特定网络条件。生产中团队可能 5 个 Redis 实例在同一...
  c18_b10.md: ...），分布式锁冗余。若资源没有原生锁，fencing token 对锁安全至关重要。LLM 没问。  再三种（任一条都让 Redlock 不足）：  (d) **进程暂停行为**。GC 暂停、VM 迁移持有的暂停进程锁在没有 fencing token 时...
  c18_b10.md: ...token 时很危险。LLM 没问暂停预期。  (e) **跨区域部署**。5 个 Redis master 跨区域的 Redlock 会增加锁获取的跨区域延迟。LLM 没问地理。  (f) **Redis 故障时发生什么**。若 Redis maste...
  c18_b11.md: ...ruth source）在各系统中反复出现。  分布式锁有两个目的：效率（避免重复工作）和正确性（资源只有一个写入者）。Redlock 式缓存锁对效率可接受；对正确性，在进程暂停和时钟漂移下失败。正确原语是共识支持的锁服务（etcd、Consul、Zoo...
  c23_b03.md: ...故障转移数据丢失。 * **CockroachDB**：早期版本有可串行化违规；后续版本修复。 * **Redis**：Redlock 问题（第18章主题）；各种故障转移 bug。 * **etcd**：旧版本有轻微一致性 issue；已纳入修复。  J...
  c27_b01.md: ...器时钟说真话**。跨机器比较时间戳的代码。    4. **朴素分布式锁有效**。没有 fencing token 的 Redlock。假设锁持有者唯一的代码。    5. **部分故障罕见**。只有 happy path 的代码；不考虑 “半成功” 结...
  c27_b03.md: ...te(f"action_lock:{action_id}")   谎言：持有 Redis key 意味着唯一性。代码用 Redlock 风格锁保护正确性关键操作。  现实：没有 fencing token 的 Redlock 在进程暂停、时钟漂移和 Re...
  c27_b03.md: ...key 意味着唯一性。代码用 Redlock 风格锁保护正确性关键操作。  现实：没有 fencing token 的 Redlock 在进程暂停、时钟漂移和 Redis failover 下失败。两个客户端可能都相信自己持有锁；两者都调用 `do_cr...
  c27_b03.md: ...tion_id, ..., max_token=fencing_token)   LLM 产出 Redis 版本是因为 Redlock 风格代码在训练语料中（尤其旧博客）。修复需要理解第18章教训（Kleppmann 对 Redlock 的批评）。...
  c27_b03.md: ...is 版本是因为 Redlock 风格代码在训练语料中（尤其旧博客）。修复需要理解第18章教训（Kleppmann 对 Redlock 的批评）。...
  c27_b09.md: .... **机器时钟说真话**：代码跨机器比较 wall-clock 时间戳做排序。    4. **朴素分布式锁有效**：Redlock 风格代码假设锁持有者唯一；缺少 fencing token。    5. **部分故障罕见**：代码只有 happy...
  c27_b09.md: ..."1", nx=True, ex=30): do_critical_work()`。无 fencing token 的 Redlock；并发持有者造成污染。  （谎言 5）`db.update(user); cache.update(user); not...
  c27_b11.md: ...代码片段。  提示  一个跨 region 支付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LL...
  c27_b12.md: ...谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5）；在误导性 CP 注释下读一致性模糊...
  c30_b03.md: ...M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2...
  c30_b16.md: ...忍任意顺序但需要交付。_冲突_ 操作（库存、唯一声明）仍需要原子广播。 | Ch.4, Ch.16 | | 20 | "Redlock 错了；别用。" | Redlock 对 _正确性_ 用例错了；对效率合适。纪律是分类用例。 | Ch.18 | | 2...
  c30_b16.md: ...（库存、唯一声明）仍需要原子广播。 | Ch.4, Ch.16 | | 20 | "Redlock 错了；别用。" | Redlock 对 _正确性_ 用例错了；对效率合适。纪律是分类用例。 | Ch.18 | | 21 | "把缓存当锁用。" | 基于...

=== liveness (43 occurrences) ===
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safet...
  c07_b03.md: ...ty 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定...
  c07_b03.md: ...：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairne...
  c07_b03.md: ...质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liven...
  c07_b03.md: ...个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alp...
  c07_b03.md: ...eness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每...
  c07_b03.md: ...必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要...
  c07_b03.md: ...个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 livene...
  c07_b03.md: ...veness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liv...
  c07_b03.md: ...ess。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liveness：构建可能不终止但永不决定错误值的协议。这正是网络差时 Paxos 和 Raft 的行为。  (c) 加强模型：假设部分...
  c07_b03.md: ...### 7.5 Safety vs Liveness  区分是基础性的。  **Safety**：说“坏事永不发生”的性质。示例：  * Agreement 是 safet...
  c07_b03.md: ...ty 性质：任何时刻不会两个进程同时进入临界区。  Safety 性质被 _有限_ 执行违反：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定...
  c07_b03.md: ...：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairne...
  c07_b03.md: ...质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liven...
  c07_b03.md: ...个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alp...
  c07_b03.md: ...eness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每...
  c07_b03.md: ...必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要...
  c07_b03.md: ...个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 livene...
  c07_b03.md: ...veness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liv...
  c07_b03.md: ...ess。选择是：  (a) 牺牲 safety：构建总是终止但有时决定错误值的协议。对大多数应用无用。  (b) 牺牲 liveness：构建可能不终止但永不决定错误值的协议。这正是网络差时 Paxos 和 Raft 的行为。  (c) 加强模型：假设部分...
  c07_b06.md: ...不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术...
  c07_b06.md: ...区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术不同；运维后果不同。 =====END=====...
  c07_b06.md: ...不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.** 区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术...
  c07_b06.md: ...区分 safety 性质与 liveness 性质。为何这种区分对共识重要？  提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术不同；运维后果不同。 =====END=====...
  c07_b07.md: ...程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；...
  c07_b07.md: ...重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；没有任何执行违反 agreement 或 va...
  c07_b07.md: ...(b) 在持续坏网络条件下协议可能不进展；这是可用性问题，不是正确性问题。  (c) 运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b07.md: ...运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b07.md: ...程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；...
  c07_b07.md: ...重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；没有任何执行违反 agreement 或 va...
  c07_b07.md: ...(b) 在持续坏网络条件下协议可能不进展；这是可用性问题，不是正确性问题。  (c) 运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b07.md: ...运维共识集群的根本是确保有利于 liveness 的条件：紧超时、健康网络、足够副本。协议负责 safety；运维负责 liveness 条件。...
  c07_b10.md: ...Raft 在分区期间无法选出 leader 时，就是 FLP 在起作用：在持续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视为“数据库坏了”，并可能采取破坏 safety 的反生产措施（强制 leader、禁用 Raf...
  c07_b10.md: ...能采取破坏 safety 的反生产措施（强制 leader、禁用 Raft）。  现代协议通过明确选择（始终 safe，liveness 有条件）来 _处理_ FLP。部署、运维、调试协议的工程师需要理解做了什么选择。FLP 就是解释该选择的框架。  第二...
  c07_b10.md: ...不是好奇心。应用工程师需要知道不可能性说了什么、逃生舱是什么。证明本身主要对协议设计者重要；其含义（safety vs liveness；部分同步；故障检测）对每个人都重要。  **Q2.** 一个 LLM 被问“为什么我们不能对所有东西都用两阶段提交？”...
  c07_b10.md: ...可能在崩溃前已提交）。  这种阻塞不只是“锁争用”。它是更深层的结构性问题：2PC 把共识归约到单一协调者，协调者成为 liveness 的单点故障。参与者在协调者恢复前被阻塞，而协调者可能永不恢复。  3PC 被提出修复，但 3PC 在网络分区下也失败；...
  c07_b11.md: ...止全部三条；现代逃生舱是部分同步、随机化和故障检测器。  Safety 性质（“坏事永不发生”）由现代共识协议始终保证。Liveness 性质（“好事最终发生”）只在网络稳定后保证。Crescent 多区域提交日志使用 Raft，它在所有网络条件下 saf...
  c08_b09.md: ...一阶段会学到是否有（什么）值已被接受，并完成前一个提议或重新开始。没有参与者无限期阻塞。  结构性优势：Paxos 对 liveness 没有单点故障。任何进程都可以当 proposer；任一单进程故障不会阻塞系统（只要多数存活）。  2PC 的阻塞失败模...
  c09_b09.md: ...dra LWT）。Raft 因为单 leader 放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时...
  c22_b06.md: ...败（短暂下游抖动导致检查失败；节点被标记不健康；集群容量下降）。缓解：标记不健康前需要多次连续失败。  生产模式：廉价 liveness 检查（让 kubelet 知道容器是否存活）加深度的 readiness 检查（让负载均衡器知道节点能否服务流量）。两...
  c22_b10.md: ...200 有灰色故障问题：即使服务坏了也通过。对非平凡服务，它提供虚假安全感。  生产中的正确健康检查：  (a) **Liveness 检查**（kubelet 的 `/healthz`）：进程是否在运行？廉价；若进程能响应则返回 200。  (b) *...
  c22_b11.md: ...确的 LLM 回应应问：“你的服务角色是什么？它有哪些依赖？健康检查的成本预算如何？有这上下文，我才能设计适合你服务的 liveness、readiness 和 startup 检查。” 单行 “return 200” 只适用于最简单的无状态服务。  更深...
  c30_b02.md: ...部分同步形式化。 * Alpern, B., & Schneider, F. B. (1985). "Defining liveness." _Information Processing Letters_. — 安全性/活性分解。 * Lamport,...

=== continuum (42 occurrences) ===
  c01_b03.md: ...平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不...
  c23_b13.md: ...026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  ---...
  c24_b01.md: ...Tobias Schwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户运行 LLM 推理；每月基础设施账单 12...
  c24_b04.md: ...接使用 vLLM 加自定义编排。   ### 24.5 推理集群拓扑  2026 年生产推理集群（如 Tobias 在 Continuum）如下：                        ┌──── Load Balancer ────┐...
  c24_b04.md: ...lism；超大模型 pipeline parallelism）。负载均衡器基于模型可用性和当前负载将请求路由到节点。  Continuum 具体拓扑：  * 50 个节点，每个 8x H100（或 H200，混合）：共 400 张 GPU。 * 服务 8 种...
  c24_b05.md: ...小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每小时 1200 USD，每月 86.4 万美元。加上网络、存储和运维。...
  c24_b05.md: ...04-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD，每月 12万-39万 USD。  对 Continuum 的客户定价：通常每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、客户等级）。利润来自 per...
  c24_b07.md: ...nt 框架（agent scaffold 共享）、结构化输出应用（函数调用、JSON 生成）。  对 Tobias 的 Continuum 集群：答案取决于每客户工作负载。一些客户是任意聊天工作负载（vLLM 合适）；一些是重结构化输出工作负载（SGLang...
  c24_b07.md: ...案取决于每客户工作负载。一些客户是任意聊天工作负载（vLLM 合适）；一些是重结构化输出工作负载（SGLang 合适）。Continuum 同时运行两者，按客户把工作负载路由到合适引擎。  决策很少是排他的 “vLLM 对 SGLang”；现代推理集群常同时...
  c24_b09.md: ...成优于灵活）。 * **成本敏感度**：非常敏感成本 → vLLM 加自定义集成；CapEx 灵活 → NIM。  对 Continuum（重度基础设施用户，有平台团队）：vLLM 加自定义企业特性合理。对大多数 enterprise IT 商店：NIM 是...
  c24_b09.md: ...speculative decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated...
  c24_b10.md: ...产级）需要理解工作负载、运维要求和成本约束。给出最小答案的 LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM 最小答案的团队会构建脆弱、不可观测、昂贵的集群。正确路径是逐步成熟：先用...
  c24_b11.md: ...产级）需要理解工作负载、运维要求和成本约束。给出最小答案的 LLM 产出的是原型级代码，对原型合适但对生产不足。  对 Continuum 部署，上述每个缺失特性都很重要。遵循 LLM 最小答案的团队会构建脆弱、不可观测、昂贵的集群。正确路径是逐步成熟：先用...
  c24_b11.md: ...持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：50 节点、400 GPU、每月 120 万美元、60% 利用率。优化空间巨大；每个百分点利用率约每月 2 万美...
  c25_b01.md: ...Tobias 的集群 GPU 利用率为 60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decod...
  c25_b04.md: ...### 25.5 工作流 12：Continuum 的分离式部署  Tobias 的 80% 利用率目标计划：  **架构**：  * 30 个 prefill 节点（每...
  c25_b06.md: ...对 Continuum 部署，缓存层是实现超长上下文和前缀复用的基础。对其工作负载需求，架构复杂度值得。  **Q3.** 为什么分离式架构的...
  c25_b06.md: ...de 小）。30 节点但重长 prompt 工作负载的集群可能受益（prefill 成本是瓶颈）。  Tobias 的 Continuum 集群（50 节点，混合工作负载）处于阈值。6 个月分离投资值得；更小集群可能不值得。  **Q4.** 一个请求流过分...
  c25_b09.md: ...指标的自动扩缩**：根据每阶段指标（prefill 队列深度、decode token 吞吐）而非聚合指标扩缩。  对 Continuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两阶段都保持 15% headroom；基于 per-phase...
  c25_b09.md: ...KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张...
  c25_b09.md: ...。  小模型（7B、13B）可放一张 GPU，不需要 sharding。小集群部署（< 50 节点）可能不需要分离。对 Continuum 设置（70B+ 模型、50+ 节点），两者都适用。...
  c26_b01.md: ...oding（用小 draft model 加速生成；可作为跨机分布式协议实现）。  本章结束第八部分。Tobias 的 Continuum 集群完整优化故事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、K...
  c26_b02.md: ...的类似特性**：发布指标显示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prom...
  c26_b04.md: ...### 26.6 多租户公平性  Continuum 服务多个客户。每客户付费 tier 不同；有些有延迟 SLA。公平性问题：给定有限 GPU 资源，如何在客户间调度请求...
  c26_b04.md: ...随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven prior...
  c26_b04.md: ...客户即使在尖峰也有有界服务。  运维关注点：每请求调度决策必须非常快（亚毫秒），以免增加 per-request 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ### 26.7 Tobias 的完整优化  Co...
  c26_b04.md: ...um 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ### 26.7 Tobias 的完整优化  Continuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode...
  c26_b05.md: ...构建显式考虑这些因素的路由优化器。朴素 round-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-aware 加长度预测路由，吞吐提升约 25%，p99 延迟降低约 30%。...
  c26_b06.md: ...主导。 * 网络延迟会把首 token 延迟推到不可接受阈值的工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fir...
  c26_b07.md: ...对 Continuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客...
  c26_b08.md: ...ng 确保其请求被处理但不饿死 tier-1 客户。  综合：该客户每请求成本降低约 5 倍（前缀复用），延迟显著改善。Continuum 能给出有竞争力的 per-request 定价；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户...
  c26_b08.md: ...；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优化让 Continuum 能与 Anyscale、Together 等竞争。  **Q2.** 某团队推理集群尾部延迟高（p50=2 秒，p99...
  c26_b09.md: ...ntra-rack、NVLink、RDMA）；成本节省占主导（verify GPU-hour 非常贵的大模型）。  对 Continuum（50+ 节点、大 verify model）：分布式是正确答案。基础设施成本节省（更便宜 draft 硬件、更高 ve...
  c26_b10.md: ...gether、Fireworks、AWS Bedrock、Azure OpenAI）在推理成本上竞争。竞争推动持续优化。Continuum 的优化冲刺不是可选；它是保持竞争力所必需。  **Q2.** 一个 LLM 被要求 “为我们集群实现 KV-cache...
  c26_b11.md: ...pt 结构是什么？多少租户？有缓存层吗？当前缓存命中率多少？我可以基于这些 specifics 设计路由策略。”  对 Continuum 部署，路由逻辑是数千行手写代码，为工作负载精细调优。LLM 生成的朴素实现会低 50% 以上。在正确路由实现上的投资通...
  c26_b11.md: ...平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregatio...
  c28_b01.md: ...的 commit-log 层）。Yuki 从 Glide 借调来负责协作文档编辑组件（商户入驻流程）。Tobias 从 Continuum Inference 借调来负责 AI 驱动的欺诈检测层。  他们交付的系统是全书的综合。每个原语都在其中。每章的课都被...
  c28_b02.md: ...**商户协作**：基于 Yjs 的协作文档编辑器（Yuki 的组件），用于商户入驻合同。  (i) **欺诈检测**：Continuum 风格的 LLM 推理（Tobias 的组件）。每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性...
  c28_b03.md: ...SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]         end         CF --> APIE...
  c28_b04.md: ...到 US-East，因此读是本地；线性化（从 leader 读；使用 HLC，第3章）。8 ms。  (7) 事务通过 Continuum LLM 推理检查欺诈（第24-26章）。欺诈模型在 p50 80 ms 内返回分数（sub-100-ms p99，按...
  c28_b05.md: ...dStrike 无 canary）、E.7（Fastly 无 canary） | | 24-26 | LLM 推理 | Continuum 风格欺诈检测集群 | n/a | | 27 | AI 生成代码审查 | PR 审查中的六谎言清单；§27.10 三层...
  c28_b13.md: ...处于前沿；未来 5-10 年将显著发展。本书覆盖让他们为该领域快速演进做准备。  Crescent 系统（第28章）把 Continuum 的推理组件作为更大系统的一部分。关于推理的章节不是独立的；它们融入更广泛的分布式系统叙事，因为推理是 2026 年分布...

=== yjs (42 occurrences) ===
  c03_b08.md: ...6章）。每条消息是一个 op，带唯一 ID 和依赖列表（发送时发送者可见的消息 ID）。显示顺序是依赖图的任意拓扑排序。Yjs 和 Automerge 内部就是这样做的。  LLM 原版的 `sorted(messages, key=lambda...
  c04_b10.md: ...tors 向量；向量随键的写作者数增长，而非系统大小。这是 CRDT 模式；广播层退化为“可靠”，因果性由数据类型承载。Yjs 和 Automerge（第16章）使用这种方法。  对大多数 2026 系统，正确的问题是：应用是否真的需要 _所有_...
  c04_b11.md: ...作使用 CRDT（计数器、浏览追踪、协作文档），对不适用的操作使用原子广播（库存、支付、领导者选举）。这样兼得两者之长。Yjs 协作文档编辑器用 CRDT 处理文档主体，用强一致服务器处理访问控制元数据。CockroachDB 用 Raft（原子...
  c16_b01.md: ...（Conflict-free Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于部分子产品）。本章是 Yuki 的工作流 9：用 CRDT 实现 Glide...
  c16_b03.md: ...扑排序重建。  同一位置的并发插入按 ID 顺序（或副本顺序作为平票）解析。结构类似树；线性化是树遍历。   #### Yjs 的实现  Yjs（Kevin Jahns 约 2014 年启动）使用 RGA 的优化变体。每个字符由一个小结构体表示（...
  c16_b03.md: ...一位置的并发插入按 ID 顺序（或副本顺序作为平票）解析。结构类似树；线性化是树遍历。   #### Yjs 的实现  Yjs（Kevin Jahns 约 2014 年启动）使用 RGA 的优化变体。每个字符由一个小结构体表示（约 8 字节），包...
  c16_b03.md: ...clock、client ID 和指向前驱的指针。该结构对常见情况高度优化（顺序打字产生字符链，每个指向前一个）。  Yjs 在 Notion、Linear 等众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAs...
  c16_b03.md: ...接受水平。早期版本在大规模下有内存问题；2.x 使用 Rust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 *...
  c16_b03.md: ...使用 Rust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更...
  c16_b03.md: ...merge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且其文档模型比 Automerge 完整历...
  c16_b03.md: ...## 16.4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const tex...
  c16_b04.md: ...它，存储会无界增长。有了它，旧编辑被合并进快照，任何重连恢复都使用快照加后续 update。   #### 冲突解决  Yjs 自动处理所有冲突。若两个用户并发在同一位置插入字符，Yjs 按 client ID 和 clock 确定性排序。若一个...
  c16_b04.md: ...都使用快照加后续 update。   #### 冲突解决  Yjs 自动处理所有冲突。若两个用户并发在同一位置插入字符，Yjs 按 client ID 和 clock 确定性排序。若一个用户删除段落时另一个将其加粗，粗体属性保留在幸存字符上（未被...
  c16_b04.md: ...a migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Yjs 的版本处理覆盖很多情况；应用以向后兼容层处理其余。  (c) **存储大小** ：重历史文档（编辑数月的文档）会累积状...
  c16_b06.md: ...实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.** 走一遍 G-Counter 和 PN-Coun...
  c16_b07.md: ...MV 或领域特定 CRDT。对需要拒绝并发写的字段，线性化。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  R...
  c16_b07.md: ...。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 b...
  c16_b07.md: ...list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size 优化。  **答案：** RGA（Replicated Growable A...
  c16_b07.md: ...* 同一位置并发插入被确定性解析（编辑器永远不会看到 “合并冲突”）。 * 结构保持因果性：字符总插入在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构...
  c16_b07.md: ...并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle size** ：Yjs ~80 KB，即使在慢网络下也足够小，适合浏览器。 (d) **垃圾回收** ：删除字符的 tombstone 最终在...
  c16_b07.md: ...ate。  早期 list CRDT（Logoot、Treedoc、WOOT）存在问题：标识符增长、合并慢、内存占用大。Yjs 是首个对生产环境浏览器协作编辑来说足够快、足够小的实用 RGA 实现。...
  c16_b08.md: ...### 应用问题  **Q1.** Yuki 的 Glide 团队想添加 “实时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awaren...
  c16_b08.md: ...时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文档内容，必须在...
  c16_b08.md: ...久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文档内容，必须在断连后保留）和 _awareness 状态_（临时信息如光标位置、在线状态、选择，...
  c16_b08.md: ...ess 通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端...
  c16_b09.md: ...型_（公式、范围操作、约束）通常需要额外层。  **Q3.** 一个团队在为协作文档项目管理应用（Jira 风格）选择 Yjs 与 Automerge。基于工作负载（大量小问题编辑、历史重要、偶尔批量导入）进行比较。  提示  Yjs：更快、更小...
  c16_b09.md: ...风格）选择 Yjs 与 Automerge。基于工作负载（大量小问题编辑、历史重要、偶尔批量导入）进行比较。  提示  Yjs：更快、更小。Automerge：历史更丰富。批量导入对两者都棘手。  **答案：**  _Yjs_ ：  * 优点：合...
  c16_b09.md: ...中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；Yjs 擅长。Automerge 的历史可启用 “给我看上周二的问题描述”。 * **状态变更**：离散状态转换（Open →...
  c16_b09.md: ...态转换（Open → In Progress → Done）。用寄存器或 LWW 字段建模比 list CRDT 容易；Yjs 的 Y.Map 或 Automerge 的结构化 doc 都可以。 * **评论**：只追加的评论列表。任一 CRDT...
  c16_b09.md: ...。 * **审计追踪**：项目管理应用常需要合规性的 “谁改了什么、何时”。Automerge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CSV。两个库都能处理，但导入是结构...
  c16_b09.md: ...非为批量加载优化。  推荐：  * 若审计/历史作为数据需求重要：Automerge。 * 若纯性能和生态成熟度占主导：Yjs。  对审计和历史重要的 Jira 风格应用，Automerge 是正确选择。对文档是焦点、历史是次要功能的 Notio...
  c16_b09.md: ...历史重要的 Jira 风格应用，Automerge 是正确选择。对文档是焦点、历史是次要功能的 Notion 风格应用，Yjs 是正确选择。决策总是由工作负载驱动。...
  c16_b10.md: ...语义（脚注、评论、依赖文档结构的格式）下大规模工作。CRDT 覆盖成熟协作编辑完整功能集的实践仍不如 OT 经充分验证；Yjs 覆盖常见情况，但当前 CRDT 库大多未覆盖 Google Docs OT 的边界情况。  综合：2026 年新设计默...
  c16_b10.md: ...T 库大多未覆盖 Google Docs OT 的边界情况。  综合：2026 年新设计默认用 CRDT。数学干净加上 Yjs 和 Automerge 的生产记录是强论据。对现有基于 OT 的系统（Google Docs、Microsoft Wo...
  c16_b11.md: ...队都会先尝试的朴素方法。CRDT 在结构上解决以上所有问题；重新发明它们不必要且错误。正确的 LLM 回答应该是 “用 Yjs 或 Automerge；不要从零实现协作编辑。” LLM 产出朴素代码说明它没学到这一课，或它在不考虑需求的情况下生成...
  c16_b11.md: ...uki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 compaction...
  c17_b02.md: ...同 “赢家”。 * 强最终：收敛状态由更新集合决定，而非策略。无不确定性。  CRDT（第16章）是典型强最终数据类型。Yjs、Automerge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT...
  c17_b03.md: ...：CockroachDB 做运营存储（线性化）；独立计数器服务（CRDT）做页面浏览指标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（per-object 线...
  c17_b11.md: ...ent 用 CockroachDB（线性化）+ 自定义计数器服务（最终）+ Snowflake（分析）。Glide 用 Yjs（CRDT/强最终）+ Cloudflare Durable Objects（per-object 线性化）做文档元数据...
  c28_b02.md: ...) **队列**：每 region 一个 Kafka。异步工作流（通知、下游管道）。  (h) **商户协作**：基于 Yjs 的协作文档编辑器（Yuki 的组件），用于商户入驻合同。  (i) **欺诈检测**：Continuum 风格的 LL...
  c28_b07.md: ...了该方法，验证了其 profile 匹配需求。”  **Yuki（从 Glide 借调）**：“商户入驻合同编辑器使用 Yjs。协作编辑 ‘刚好工作’，因为 CRDT 确定性地处理冲突。与 Crescent 的认证和审计追踪系统集成是难点；编辑器...
  c28_b10.md: ...协调器状态在易失内存；竞争下不确定性重启；下游 API 缺少 fencing token | | 9 | Yuki 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定性合并后的文档状态 | RGA 风格列...

=== decoding (42 occurrences) ===
  c24_b08.md: ...5-40 万美元。  其他技术（简述）：跨节点 KV cache sharding（第25章）；speculative decoding（第26章）；模型量化（8-bit、4-bit serving 用于低精度模型）；MoE routing（mixture...
  c24_b09.md: ...大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍；长请求完成更快，...
  c24_b09.md: ...point。  (e) **Speculative decoding（第26章）**：对长生成，speculative decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous...
  c25_b10.md: ...8-bit 或 4-bit serving）： slight 质量损失下 2-4 倍吞吐。 * Speculative decoding（第26章）：长生成 2-3 倍吞吐。 * 编译（TensorRT-LLM、ExecuTorch）：在支持硬件上 1.5...
  c25_b11.md: ...均衡和投机解码作为分布式协议。Anyscale/Together/Fireworks 路由问题；speculative decoding（draft model + verify model）作为多机协议；长度预测和 KV-cache-aware 路由。...
  c26_b01.md: ...re load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative decoding（用小 draft model 加速生成；可作为跨机分布式协议实现）。  本章结束第八部分。Tobias 的 Conti...
  c26_b01.md: ...）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、speculative decoding（本章）。这些部分组合。   ### 26.1 LLM 请求路由问题  典型设置：N 个节点的推理集群，每个运行多个模型...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “v...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶...
  c26_b03.md: ...y model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a)...
  c26_b03.md: ...与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculat...
  c26_b03.md: ...期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “v...
  c26_b03.md: ...### 26.4 Speculative Decoding  另一类优化：speculative decoding 通过小 “draft model” 预测 token、大 “verify model” 确认，来加速 decode 阶...
  c26_b03.md: ...y model 每步成本略高（处理 K 个 token 而非 1 个）。   ### 26.5 Speculative Decoding 作为分布式协议  标准实现把 draft 和 verify 放在同一张 GPU。现代变体把它们分到不同机器：  (a)...
  c26_b03.md: ...与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculat...
  c26_b03.md: ...期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b04.md: ...由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair sc...
  c26_b05.md: ...%，p99 延迟降低约 30%。改动是算法级（无硬件改动）；收益巨大。  **Q2.** 描述 speculative decoding。它如何加速生成？  提示  小 draft model 提出 K 个 token；大 verify model 检查；...
  c26_b05.md: ...odel 检查；接受前缀输出；每 “verify 步” K 个 token。  **答案：** Speculative decoding 使用两个模型：小 “draft” 模型和大 “verify” 模型。  每 decode 步流程：  (a) Draf...
  c26_b06.md: ...Speculative decoding 现在已是生产推理引擎标准（vLLM 支持；SGLang 支持；有专门库）。2-3 倍吞吐本质上是免费（附加成本是运行...
  c26_b06.md: ...倍吞吐本质上是免费（附加成本是运行 draft model，很小）。  **Q3.** 何时把 speculative decoding 分布到多机有意义？  提示  当 draft model 可放在更便宜硬件；当 verify model 被多请求共享...
  c26_b06.md: ...可放在更便宜硬件；当 verify model 被多请求共享。  **答案：** 在以下情况把 speculative decoding 分布到多机有意义：  (a) **Draft model 可放在更便宜硬件**。1B 参数 draft 可放在小 GP...
  c26_b06.md: ...) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token r...
  c26_b06.md: ...；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 token 延迟推到不可接受阈值的工作负载。 *...
  c26_b06.md: ...工作负载。 * 没有工程能力维护分布式协议的团队。  对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4....
  c26_b08.md: ...支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode...
  c26_b08.md: ...。  (d) **Speculative decoding**：中等输出 decode 受益于 speculative decoding。2 倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5 秒。  (e) **客户 SLA 调度**：该客...
  c26_b08.md: ...秒）。诊断并提出修复。  提示  尾部延迟来自长请求阻塞、无长度预测路由、长请求 GPU 竞争、speculative decoding 有帮助。  **答案：** 诊断：p99/p50 比 15:1 表明严重尾部延迟。可能原因：  (a) **长输出请求...
  c26_b09.md: ...ed” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部感知负载均衡器**...
  c26_b09.md: ...tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative decoding 有帮助，但更多是全面改善而非专门尾部。  更深观察：尾部延迟是用户-facing 指标。p50 2 秒且 p99 30...
  c26_b09.md: ...害产品。修复尾部延迟常比改善中位数更重要，但得到的工程关注更少。  **Q3.** 比较分布式 speculative decoding 与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co...
  c26_b09.md: ...独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-token 延迟（无网络跳数）。 * 运维...
  c26_b09.md: ...erify 协议）。 * Draft 和 verify 一起扩缩（资源比例固定）。  _分布式 speculative decoding（draft 在专用节点，verify 在其它节点）_：  * Draft 用更便宜硬件（小 GPU 或 CPU）。 *...
  c26_b09.md: ...更便宜 draft 硬件、更高 verify 利用率）大于小延迟惩罚。  2026 年，分布式 speculative decoding 处于早期采用。已发布案例（Together、Anyscale、一些自定义实现）显示规模化下 1.5-2.5 倍吞吐提升...
  c26_b10.md: ...收益（PagedAttention、RadixAttention、disaggregation、speculative decoding）已部署。剩余优化是增量：调优、硬件刷新、小型协议改进。2022-2024 年 5-10 倍收益不会再来。  反驳：领域...
  c26_b10.md: ...22-2024 年 5-10 倍收益不会再来。  反驳：领域仍快速演进。新技术不断涌现：更好的 speculative decoding（multi-token speculation、EAGLE、Medusa）、MoE 专用优化（expert routi...
  c26_b11.md: ...tained” 节点；短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model 提出 K 个 token；大 verify model 并行检查 K 个；接受前缀输出。在...
  c26_b11.md: ...che sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果：集群利用率 85...
  c30_b03.md: ..., Y., et al. (2024). "DistServe: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et a...
  c30_b03.md: .... (2023). "Fast Inference from Transformers via Speculative Decoding." _ICML_.   #### 综合与 AI 生成代码（第九部分 — 第27-28章）  * "The Bitter...
  c30_b06.md: ...t isolation（快照隔离）**：事务读取一致快照；存在写-写冲突（可能写偏）。 * **Speculative decoding（投机解码）**：LLM 推理加速；小 draft model 提出 K 个 token，大 verify model...

=== serializable (41 occurrences) ===
  c02_b01.md: ...ockroachDB 的论调说 CockroachDB 是 CP。模型没有说明 CockroachDB 默认隔离级别是 serializable，没有说明它的提交延迟取决于数据在单区域还是多区域，没有说明“CAP 里的一致性”指的是线性一致性（linearizab...
  c02_b10.md: ...DB 有 `AS OF SYSTEM TIME`、follower 读，旧版本的默认 `SNAPSHOT` 隔离级别比 serializable 弱。每个都有一个非线性化角落。  **答案：**  (a) `AS OF SYSTEM TIME` 读是有界过期读，来...
  c02_b10.md: ...RENTLY` 操作交互，如果误读隔离保证，可能在不同会话的语句间产生微妙的非线性化行为。CockroachDB 默认 serializable 隔离，这是 SQL 隔离级别中最强的，但 serializable 不等于跨会话的线性化；serializable 将...
  c02_b10.md: ...生微妙的非线性化行为。CockroachDB 默认 serializable 隔离，这是 SQL 隔离级别中最强的，但 serializable 不等于跨会话的线性化；serializable 将事务排序为某种有效顺序，而线性化要求排序与真实时间一致。对许多工作负...
  c02_b10.md: ...认 serializable 隔离，这是 SQL 隔离级别中最强的，但 serializable 不等于跨会话的线性化；serializable 将事务排序为某种有效顺序，而线性化要求排序与真实时间一致。对许多工作负载差异不重要；对“会话 B 的读在会话 A 的写...
  c11_b02.md: ...用与响应之间的某个瞬间原子生效，且符合实时顺序。Spanner、CockroachDB（事务默认）、TiDB。 * **Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable...
  c11_b02.md: ...Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。...
  c11_b02.md: ...行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到...
  c11_b02.md: ...n** ：事务看到一致快照，但不防止写-写冲突（写偏可能）。PostgreSQL（实际 REPEATABLE READ；SERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-read...
  c11_b02.md: ...用与响应之间的某个瞬间原子生效，且符合实时顺序。Spanner、CockroachDB（事务默认）、TiDB。 * **Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable...
  c11_b02.md: ...Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。...
  c11_b02.md: ...行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到...
  c11_b02.md: ...n** ：事务看到一致快照，但不防止写-写冲突（写偏可能）。PostgreSQL（实际 REPEATABLE READ；SERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-read...
  c11_b03.md: ...| Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停...
  c11_b04.md: ...ry | 快（亚秒级 sequencer 切换）；自设计日起就以混沌工程为目标 Consistency model | Serializable 事务；ACID 强可串行化保证 Partition behavior | CP——丢失法定人数则拒绝写 PACELC...
  c11_b05.md: ...| Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serializable 事务；follower 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC |...
  c11_b06.md: ...2. 故障恢复语义是什么？（重选时间、gossip、人工）   3. 一致性模型是什么？（linearizable、serializable、snapshot、causal、eventual、tunable）   4. 分区行为是什么？（拒绝少数、服务所有、s...
  c11_b06.md: ...个问题可得到任何系统的 PACELC 画像、运维特征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：...
  c11_b06.md: ...“serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operati...
  c11_b06.md: ...看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作 B 开始前完成返回，则 A 排在 B 前。  Serializable 是 per-transaction 属性：任何并发事务的执行都等价于某种串行顺序。该顺序不必符合实时；即使事务 A 在...
  c11_b06.md: ...行顺序。该顺序不必符合实时；即使事务 A 在 B 开始前提交，串行等价顺序仍可能把 B 排在 A 前。  例如，在一个 serializable 系统中：事务 A 在时刻 10 写入 $x = 1$ 并提交。事务 B 在时刻 20 读取 $x$。Serializa...
  c11_b06.md: ...ializable 系统中：事务 A 在时刻 10 写入 $x = 1$ 并提交。事务 B 在时刻 20 读取 $x$。Serializable 性质允许 B 读到 $x$ 的旧值（仿佛 B 在串行等价中排在 A 前），只要整体执行等价于某种串行顺序。  Line...
  c11_b06.md: ...rizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 serializable 付更多（每次读走共识、commit-wait 或 HLC restart）。许多工作负载需要 serializabil...
  c11_b06.md: ...2. 故障恢复语义是什么？（重选时间、gossip、人工）   3. 一致性模型是什么？（linearizable、serializable、snapshot、causal、eventual、tunable）   4. 分区行为是什么？（拒绝少数、服务所有、s...
  c11_b06.md: ...个问题可得到任何系统的 PACELC 画像、运维特征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：...
  c11_b06.md: ...“serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operati...
  c11_b06.md: ...看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作 B 开始前完成返回，则 A 排在 B 前。  Serializable 是 per-transaction 属性：任何并发事务的执行都等价于某种串行顺序。该顺序不必符合实时；即使事务 A 在...
  c11_b06.md: ...行顺序。该顺序不必符合实时；即使事务 A 在 B 开始前提交，串行等价顺序仍可能把 B 排在 A 前。  例如，在一个 serializable 系统中：事务 A 在时刻 10 写入 $x = 1$ 并提交。事务 B 在时刻 20 读取 $x$。Serializa...
  c11_b06.md: ...ializable 系统中：事务 A 在时刻 10 写入 $x = 1$ 并提交。事务 B 在时刻 20 读取 $x$。Serializable 性质允许 B 读到 $x$ 的旧值（仿佛 B 在串行等价中排在 A 前），只要整体执行等价于某种串行顺序。  Line...
  c11_b06.md: ...rizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 serializable 付更多（每次读走共识、commit-wait 或 HLC restart）。许多工作负载需要 serializabil...
  c11_b07.md: ...of 6 ack）。故障恢复透明（Firecracker 数秒内重新启动，存储层独立处理副本故障）。一致性模型为强一致 serializable；读可走 leader（即时）或 follower（有界过期）。分区行为在存储层为 CP（丢失存储法定人数时写阻塞）。P...
  c11_b11.md: ...加在技术阅读之上；单独任何一层都不充分。  阅读文档的常见陷阱：营销级“一致性”声明混淆 linearizable 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 benchmark。方法通过要求...
  c13_b10.md: ...销限制（跨区域事务通常 100-150 ms 端到端）。  若两笔事务竞用同一账户（罕见但可能），一笔获胜；另一笔拿到 serializable error 并重试。CockroachDB 的 serializable 隔离保证正确性；应用代码必须准备好重试（标准...
  c13_b10.md: ...同一账户（罕见但可能），一笔获胜；另一笔拿到 serializable error 并重试。CockroachDB 的 serializable 隔离保证正确性；应用代码必须准备好重试（标准模式是带指数退避的重试循环，最多 N 次）。...
  c13_b17.md: ...Google。  Olamide 的跨区域支付事务在 CockroachDB 中约 120 ms 原子提交到三个区域，serializable 隔离且完整 ACID 保证。协议成熟；运维调优（局部性、分片、重启预算）决定生产性能。  Saga 模式（跨服务补偿动...
  c14_b07.md: ...aft leader 切换（~数秒） Consistency model | Linearizable；提供过期读 | Serializable；提供 follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧...
  c27_b01.md: .... **CAP 分类简单**。说 “我们用 CP 系统” 而不理解 PACELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM 默认产出嵌入全部六个的代码。本章用实例逐一走过。...
  c27_b09.md: ...“我们用 AP 系统”，而不理解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默认产出嵌入这些谎言的代码，因为训练数据过度代表简单模式（单机假设），而不足代表处理这些故障模式的分布式系...
  c30_b06.md: ...* **Saga**：跨服务事务模式，用补偿动作代替锁。见第13章 §13.6.5 了解协调式与编排式变体。 * **Serializable（可串行化）**：每次事务执行等价于某种串行顺序。弱于线性化。 * **Snapshot isolation（快照隔离）...
  c30_b16.md: ...| 分区容错不是选择；分区下在一致性与可用性之间选择。 | Ch.2 | | 4 | "Linearizable = serializable。" | Linearizable 是每次操作的实时顺序；serializable 是每次事务的任意顺序。Lineari...
  c30_b16.md: ...| "Linearizable = serializable。" | Linearizable 是每次操作的实时顺序；serializable 是每次事务的任意顺序。Linearizable 严格更强。 | Ch.2, Ch.11 | | 5 | "我们用 CP...

=== adaeze (40 occurrences) ===
  c01_b01.md: ...凌晨 2:47（圣保罗时间），Crescent 在三个区域上线那晚，一位名叫 Adaeze Okafor 的客户——住在圣保罗 Vila Madalena 的尼日利亚软件工程师——用手机购买了一份 6 雷亚尔的...
  c01_b02.md: ...#### 第二个谎言：身份  法兰克福协调者相信自己是唯一处理 Adaeze 这笔事务的协调者。圣保罗协调者也这么认为。从各自的世界观内部看，两者都对。从外部看，两者都错。  这就是身份谎言：每个...
  c01_b02.md: ...分失败当作默认模式。如果你写分布式代码只处理 happy path、把失败当作不寻常的例外，那你写的代码终将在某一天让 Adaeze 被重复扣款。   ### 1.2 分布式推理为何不同  大多数工程师在一种“世界善意撒谎”的环境中学习编程。时钟准确。...
  c01_b08.md: ...复出现的模式。每个都在指定章节中作为完整演练出现；能否独立执行每一个，是检验你是否吸收对应章节的实操测试。  现在回到 Adaeze。早上 6:00，Olamide 有了修复方案。到 9:00，她完成了事后复盘。修复涉及 fencing token（第...
  c01_b08.md: ...合逻辑时钟（第3章），以及用于协调者状态的 Raft 支持提交日志（第9章）。复盘命名了系统一直讲述的三个谎言。银行向 Adaeze 退款。Crescent 在内部发布了复盘，并在下周进行了一次 game day（第23章），验证修复在模拟分区下是否有...
  c01_b15.md: ...——展开让你能够不撒谎地推理顺序的协议（Lamport 时钟、向量时钟、HLC、TrueTime）。  本书会不断回到 Adaeze。本章倒数第二段让她被重复扣款的系统，就是四位主角在第28章一起交付的系统。中间的一切是路径；第29章收尾，区分什么会继...
  c13_b01.md: ...Olamide 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Adaeze 的账户（一个分片）、商家的账户（另...
  c13_b01.md: ...de 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Adaeze 的账户（一个分片）、商家的账户（另一个分片）和商家的日汇总记录（第三个分片）。三处要么全提交，要么全不提交。  这就是...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结...
  c13_b10.md: ...-- 全程运行，FX 作为单独分录入账）。     BEGIN TRANSACTION;       -- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id =...
  c13_b10.md: ...余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240 USD；是一笔 substantial 交...
  c13_b10.md: ...lance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240 USD；是一笔 substantial 交易，不是       -- 第1章的 6...
  c13_b10.md: ...E accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到...
  c13_b10.md: ...s；São Paulo → US-West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被...
  c13_b10.md: ...路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南...
  c13_b10.md: ...ast 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adae...
  c13_b10.md: ...aeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结...
  c13_b10.md: ...-- 全程运行，FX 作为单独分录入账）。     BEGIN TRANSACTION;       -- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id =...
  c13_b10.md: ...余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240 USD；是一笔 substantial 交...
  c13_b10.md: ...lance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240 USD；是一笔 substantial 交易，不是       -- 第1章的 6...
  c13_b10.md: ...E accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到...
  c13_b10.md: ...s；São Paulo → US-West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被...
  c13_b10.md: ...路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南...
  c13_b10.md: ...ast 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adae...
  c13_b10.md: ...aeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额...
  c13_b11.md: ...结果：Adaeze 的支付在 ~120 ms 内原子提交到三个区域的三个分片（CockroachDB 跨区域提交成本）。用户在她的手机看到...
  c18_b04.md: ...### 18.3 Crescent 的修复  回顾第1章：Crescent 重复扣款是因为两个协调者都认为自己正在处理 Adaeze 的交易。修复涉及 fencing token。  架构：  (a) 协调者角色由 Raft 复制的锁服务管理。每次获得...
  c20_b01.md: ...--> DB1["read balance<br/>8 ms"]         DB --> DB2["UPDATE Adaeze<br/>30 ms (US-East shard)"]         DB --> DB3["UPDATE Femi...
  c28_b04.md: ...### 28.2 一笔支付的旅程  追踪一笔从圣保罗 Adaeze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge wor...
  c28_b04.md: ...inator 获得 fencing token（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze 余额（第11/13/14章）。Adaeze 账户按地域分区到 US-East，因此读是本地；线性化（从 leader...
  c28_b04.md: ...n（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze 余额（第11/13/14章）。Adaeze 账户按地域分区到 US-East，因此读是本地；线性化（从 leader 读；使用 HLC，第3章）。8 ms。  (...
  c28_b04.md: ...9，按 Tobias 的 SLO）；分数低于阈值；支付继续。  (8) 事务写入 TigerBeetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replication 在 30 ms...
  c28_b04.md: ...tamped Replication 在 30 ms 内提交。  (9) 事务在 CockroachDB 中提交：更新 Adaeze 余额（US-East shard），更新 Femi 余额（EU-West shard，拉各斯商户），更新 mercha...
  c28_b04.md: ...。下游服务（通知、分析、账本对账）异步消费。5 ms。  (12) API 服务通过 Cloudflare 返回成功给 Adaeze 的手机。60 ms 跨赤道 + 5 ms 最后一公里。  总计：端到端 p50 约 360 ms。p99 约 580...
  c28_b09.md: ...### 28.7 结辩  你在第1章翻开本书，读到 Adaeze 的重复扣款。你现在知道：  * 它为何发生（三个谎言，加上时钟偏移、双协调器、缺失幂等性的具体组合）。 * 如何修复（...
  c28_b09.md: ...t 复制协调器）。  * 如何预防（诊断方法、混沌工程纪律、LLM 代码审查清单）。 * 如何构建现在无 bug 处理 Adaeze 支付的系统（第28章架构）。  教学大纲中的 12 个核心工作流全部出现。每个都作为一张卡片摘要在此总结；完整讲解在对...
  c28_b11.md: ...四位主角的系统在第28章的多 region 支付平台汇聚。本书结束于它开始的地方：Adaeze 的支付，现在被正确处理，因为工程师学会了纪律。  2026 年的分布式系统工程师是内化了心智模型的人。他们不是 cod...
  c28_b17.md: ...统；它要求识别缺口并按优先级关闭。本书心智模型是分析和优先排序的框架。   ### 28.9 结语  你翻开本书时读到 Adaeze 凌晨 2:47 在圣保罗被重复扣款。你现在知道：  * 为什么分布式系统在时间、身份、故障上自欺（第1章）。 * 如何...
  c28_b17.md: ...是刚开始、继续，还是转型分布式系统工程师职业生涯，这个心智模型都会伴随你。具体系统会变；模型永恒。  在你构建的系统中，Adaeze 的支付不会再重复扣款。这就是本书的论点。  去构建系统吧。...

=== operation (39 occurrences) ===
  c05_b10.md: ...从副本读是异步，远端读副本不影响写延迟。  综合：按操作的正确性需求选择复制模式，而非按系统默认。现代系统提供 per-operation 选择（Spanner 过期读、CockroachDB follower 读、MongoDB write concern...
  c10_b09.md: ...通常每决定需要多轮往返。跨 DC 延迟（50-200 ms）意味着每个决定 100-600 ms。元数据存储的 per-operation 延迟受此限制。...
  c11_b02.md: ...al** ：副本最终收敛；读可能过期。Cassandra（默认）、DynamoDB（默认）、Riak。 * **Per-operation 可调** ：应用按请求选择。Cassandra（consistency levels）、DynamoDB（按请求强一致...
  c11_b05.md: ...stgres 兼容代码出发更低。选 CockroachDB。 > > “DynamoDB 出局：一致性模型需要 per-operation 纪律，我们的团队还没准备好为金融工作负载维护。Cassandra 出局：可调一致性很强大，但运维负担对支付系统不合适。...
  c11_b05.md: ...你分区行为，没告诉你正常运营成本。总要问 PACELC 问题（无分区时每操作成本是多少毫秒？）。  (c) **per-operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per-operation...
  c11_b05.md: ...operation vs 系统级声明。** 系统可能对某些操作线性化，对其他操作最终。系统级标签掩盖了这种差异；per-operation 细分才重要。  (d) **营销级可用性声明。** “99.99% 可用性”是 SLA 声明；分区下的实际可用性是另一...
  c11_b06.md: ...izable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable...
  c11_b06.md: ...lizable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作 B 开始前完成返回，则 A 排...
  c11_b11.md: ...销级“一致性”声明混淆 linearizable 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 benchmark。方法通过要求精确答案抵御这些陷阱。  第12章进入下一层：分布式数据库如...
  c14_b11.md: ...；NewSQL 在没有积极缓存情况下难以达到每节点如此读量。混合（DynamoDB + Redis）给出正确的 per-operation 成本画像。...
  c15_b03.md: ...level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency level ALL：所有副本；只对特定...
  c15_b10.md: ...非系统。同一应用内，有些操作适合最终一致（计数器、浏览追踪），有些必须强一致（支付、唯一申领）。系统应同时支持 per-operation 选择；工程师的职责是正确分类每个操作。  2026 年现实：大多数新系统使用 NewSQL（默认强一致）或 per-o...
  c15_b10.md: ...n 选择；工程师的职责是正确分类每个操作。  2026 年现实：大多数新系统使用 NewSQL（默认强一致）或 per-operation 可调系统（DynamoDB 条件写、Cassandra LWT）。“默认最终一致” 在 2010 年扩展强一致系统更难...
  c16_b01.md: ...使状态在合并下形成一个 join-semilattice。任意合并序列都会收敛到所有已见状态的最小上界。  (b) **Operation-based CRDT（CmRDT）** ：每个副本广播操作。广播必须保证投递（可靠广播，第4章）。操作必须可交换：不同...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvRDT 合并状态；CmRDT 广播操作...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvRDT 合并状态；CmRDT 广播操作...
  c16_b11.md: ...ta Types）是为并发更新下确定性合并而设计的数据结构。State-based CRDT（CvRDT）合并完整状态；operation-based CRDT（CmRDT）广播操作。两者表达能力等价；选择是运维性的。  典型 CRDT：G-Counter（...
  c17_b01.md: ...行为也都被模型 B 允许，则 A 强于 B。  从强到弱的经典层级：  * **Linearizable** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-operation；存在全序，...
  c17_b01.md: ...：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：pe...
  c17_b01.md: ...在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-eventual** ：给定相同操作集合，副...
  c17_b01.md: ...协调）；模型越弱，应用成本越高（需要更多纪律处理更弱保证）。  2026 生产现实：大多数系统提供跨多个模型的 per-operation 选择。应用代码为每次操作选合适级别。   ### 17.2 因果一致性  经典表述（第3章 Lamport happe...
  c17_b03.md: ...oDB** ：最终读（默认）；强一致读；条件写（per-item 线性化）。 * **Cassandra** ：per-operation 一致性级别（ONE、QUORUM、ALL）；LWT 线性化。  优点：单一运维足迹；跨操作分析容易。缺点：需要 per...
  c17_b03.md: ...一致性级别（ONE、QUORUM、ALL）；LWT 线性化。  优点：单一运维足迹；跨操作分析容易。缺点：需要 per-operation 纪律；难以通过系统规则强制 “该表所有写都走线性化”。   #### 模式 B：不同操作用不同系统  对需要强一致的操...
  c17_b05.md: ...UORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出 _相交_（第5章）：任何读与任何...
  c17_b10.md: ...用线性化，其余用最终，架构比到处跟踪因果更简单。  综合：因果一致是有用的中间点，但不是通用默认。正确默认是 per-operation：需要严格排序的操作用线性化；有因果关系的用因果；没有的用最终。现代系统支持这种 per-operation 选择；把所...
  c17_b10.md: ...是 per-operation：需要严格排序的操作用线性化；有因果关系的用因果；没有的用最终。现代系统支持这种 per-operation 选择；把所有操作塞进一个模型很少正确。  第二个考虑：历史上因果一致在专业系统（COPS、Eiger）外难以正确实现。...
  c17_b11.md: ...存是线性化（第13章）。每个都是其需求的正确一致性；选错级别要么导致正确性违规，要么浪费资源。  生产系统提供 per-operation 一致性选择：Spanner（线性化 + 过期读）、DynamoDB（最终 + 条件写）、Cassandra（可调级别...
  c18_b07.md: ...fencing token？而不是只用其一？  提示  Raft 提供安全锁获取；token 在资源处强制执行 per-operation 安全。  **答案：** 组合提供两层安全：  (a) **Raft 锁服务** ：保证锁获取本身是安全的。Raft...
  c20_b05.md: ...t span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多...
  c27_b01.md: ...们用 CP 系统” 而不理解 PACELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM 默认产出嵌入全部六个的代码。本章用实例逐一走过。...
  c27_b04.md: ...ount_id)   谎言：“CP” 意味着 “完全一致”。代码注释显示 LLM 在应用口号级 CAP，不理解 per-operation 行为。  现实：CockroachDB 默认读写事务是可串行化（serializable）；读可以在 leader（p...
  c27_b04.md: ...版加误导注释是因为口号级 CAP 陈述在训练数据中过度代表。修复需要理解 PACELC（第2章）和具体数据库的 per-operation 语义。...
  c27_b09.md: ...。    6. **CAP 分类简单**：代码说 “我们用 CP 系统” 或 “我们用 AP 系统”，而不理解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默认产出嵌入这些谎言的...
  c27_b09.md: ...oachDB is CP, so this is consistent` 后接未指定的读。口号级 CAP，缺少 per-operation 一致性选择。  每个模式都在训练数据中，且常大量出现。LLM 把它们作为默认产出；识别它们是 bug 的工程纪律需要分...
  c28_b10.md: ...像 | 四个诊断问题：复制、恢复、一致性、分区 | 接受营销级 “一致”；混淆 CAP 与 PACELC；遗漏 per-operation 一致性选择 | | 7 | Anya 的重分片迁移 | Ch.12 §12.5 | N 节点集群磁盘利用率高；需加 M...
  c30_b07.md: ...Saga 模式；outbox；补偿动作 | | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-operation 一致性选择 |  Crescent 支付系统对每个谎言的防御在第28章详述。  * * *   ### 附录 E —...
  c30_b11.md: ...静态内容路径。 * **缓存**：区域 Redis 或 Memcached 用于热读。 * **一致性策略**：per-operation 选择；内容读用最终一致，唯一声明操作（用户名保留等）用条件写入。 * **为何这个组合**：读规模占主导；最终一致可接...
  c30_b13.md: ...吐 / 可用性需求。   2. **故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分区行为** 基于应用能容忍什么。  然后通过第14章矩阵映射到系统。再应...
  c30_b16.md: ....11 | | 5 | "我们用 CP 系统，所以一致。" | CP 命名分区行为；PACELC 命名日常成本。Per-operation 一致性才是正确问题。 | Ch.2 | | 6 | "向量时钟告诉你现在几点。" | 向量时钟推理 _顺序_，不是 w...

=== zookeeper (39 occurrences) ===
  c08_b01.md: ...s，它是分布式系统史上最具影响力的共识算法。Google 用 Paxos 构建 Chubby（Spanner 的基础）。ZooKeeper 的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport 2001 年改写成 “Paxos Made...
  c08_b10.md: ...xos vs Raft” 辩论对常见情况已基本倒向 Raft，Paxos 在少数真正有其优势的领域存活。  第二个考虑：ZooKeeper 的 ZAB（原子广播）是 Paxos 变体，大量生产基础设施依赖 ZooKeeper。因此即使系统不标榜“用 Paxo...
  c08_b10.md: ...其优势的领域存活。  第二个考虑：ZooKeeper 的 ZAB（原子广播）是 Paxos 变体，大量生产基础设施依赖 ZooKeeper。因此即使系统不标榜“用 Paxos”，Paxos 家族在运维上仍然活跃。协议家族的存在比简单名字匹配更广泛。  **Q...
  c18_b03.md: ...状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种结构。...
  c18_b05.md: ...持久化最近变更）；另一个客户端获得锁；两个持有者。  修复：对需要正确性的锁不要用缓存。用 etcd、Consul 或 ZooKeeper 做锁；在受保护资源上执行 fencing token。性能代价小（锁获取罕见）；正确性收益大。  对纯效率锁（job...
  c18_b10.md: ...选项）。正确性用例应用使用它。默认可以是任一；工程师必须理解用例并正确选择。  2026 现实：etcd、Consul、ZooKeeper 都支持 fencing token 等价物（revision 号、sequence 号、czxid）。应用代码才是需要...
  c19_b02.md: ...#### etcd 为何替代 ZooKeeper  Apache ZooKeeper 是首个生产分布式协调系统（2000 年代末起源于 Yahoo；用于 Hadoop、...
  c19_b02.md: ...#### etcd 为何替代 ZooKeeper  Apache ZooKeeper 是首个生产分布式协调系统（2000 年代末起源于 Yahoo；用于 Hadoop、Kafka、Cassandra 等）...
  c19_b02.md: ...是首个生产分布式协调系统（2000 年代末起源于 Yahoo；用于 Hadoop、Kafka、Cassandra 等）。ZooKeeper 提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。...
  c19_b02.md: ...Nodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：  (a) **更好的运维模型**：etcd 使用 Raft（更易理解；工具更好），而 ZooKeeper 的...
  c19_b02.md: ...替代 ZooKeeper，因为：  (a) **更好的运维模型**：etcd 使用 Raft（更易理解；工具更好），而 ZooKeeper 的 ZAB（Paxos 变体）更复杂。  (b) **gRPC API**：etcd 的 API 基于 gRPC；现代...
  c19_b02.md: ...os 变体）更复杂。  (b) **gRPC API**：etcd 的 API 基于 gRPC；现代；与云原生生态契合。ZooKeeper 的 API 较老、不够精致。  (c) **Kubernetes 采用**：Kubernetes 选择 etcd；Ku...
  c19_b02.md: ...推向主导地位。  (d) **更好的集群管理**：etcd 的 `etcdctl` 和基于 operator 的管理比 ZooKeeper 的 CLI 和运维故事更友好。  ZooKeeper 仍在生产中使用（Kafka 旧元数据层、HDFS、较老的 Cas...
  c19_b02.md: ...cd 的 `etcdctl` 和基于 operator 的管理比 ZooKeeper 的 CLI 和运维故事更友好。  ZooKeeper 仍在生产中使用（Kafka 旧元数据层、HDFS、较老的 Cassandra 集群）。新系统通常从 etcd 开始。K...
  c19_b02.md: ...用（Kafka 旧元数据层、HDFS、较老的 Cassandra 集群）。新系统通常从 etcd 开始。Kafka 从 ZooKeeper 迁移到 KRaft（Kafka Raft）是近期大趋势的例子。   ### 19.3 Consul  HashiCor...
  c19_b03.md: ...为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以 key 形式存在 etcd/Consul/ZooKeeper（或支持 watch 的配置数据库）中。  (b) 消费配置的服务启动时 watch 相关 key。  (c) 配置变更...
  c19_b05.md: ...目标服务实例。(d) 实例未 heartbeat（崩溃、分区）时从注册表移除。  现代发现服务：etcd、Consul、ZooKeeper、云厂商基于 DNS 的服务、Kubernetes API server。  发现服务本身是分布式系统。它需要共识（让所...
  c19_b05.md: ...同一注册表）、高可用（不让服务间调用失败）和低延迟（不让调用被注册表查找延迟）。  **Q2.** 比较 etcd 与 ZooKeeper。为什么 etcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、gRPC、Kubernet...
  c19_b05.md: ...被注册表查找延迟）。  **Q2.** 比较 etcd 与 ZooKeeper。为什么 etcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、gRPC、Kubernetes 采用。ZooKeeper：老 API、ZAB 共识、较...
  c19_b05.md: ...tcd 在新系统中基本替代了 ZooKeeper？  提示  etcd：Raft、gRPC、Kubernetes 采用。ZooKeeper：老 API、ZAB 共识、较小的现代生态。  **答案：** etcd 和 ZooKeeper 都是分布式协调服务，具...
  c19_b05.md: ...bernetes 采用。ZooKeeper：老 API、ZAB 共识、较小的现代生态。  **答案：** etcd 和 ZooKeeper 都是分布式协调服务，具有相似原语（键值存储、watch、lease/ephemeral node）。  差异：  |...
  c19_b05.md: ...语（键值存储、watch、lease/ephemeral node）。  差异：  | Aspect | etcd | ZooKeeper | |---|---|---| | Consensus | Raft | ZAB（Paxos 变体） | | API...
  c19_b05.md: ...Cli.sh；较老模式 | | Performance | 针对现代硬件优化 | 成熟但较老优化 |  etcd 替代 ZooKeeper 的原因：  (a) **Kubernetes 采用**：Kubernetes 选择 etcd；Kubernetes 的...
  c19_b05.md: ...bernetes operators）集成良好。  (d) **Kafka 的 KRaft 迁移**：Kafka 作为 ZooKeeper 最大的单一用户，迁移到 KRaft（自己的 Raft 实现）。Raft 趋势广泛。  ZooKeeper 仍在生产中使...
  c19_b05.md: ...fka 作为 ZooKeeper 最大的单一用户，迁移到 KRaft（自己的 Raft 实现）。Raft 趋势广泛。  ZooKeeper 仍在生产中使用（KRaft 前的现有 Kafka 集群、HBase、一些旧 Apache 项目），但新系统通常从 et...
  c19_b05.md: ...使用（KRaft 前的现有 Kafka 集群、HBase、一些旧 Apache 项目），但新系统通常从 etcd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch...
  c19_b07.md: ...#### 应用问题  **Q1.** 一个团队从 ZooKeeper 迁移到 etcd。当前 ZooKeeper 部署承载服务发现、分布式锁和配置。走一遍迁移计划。  提示  迁移计划：识...
  c19_b07.md: ...#### 应用问题  **Q1.** 一个团队从 ZooKeeper 迁移到 etcd。当前 ZooKeeper 部署承载服务发现、分布式锁和配置。走一遍迁移计划。  提示  迁移计划：识别用例，把 ZK 原语映射到 etcd 原语...
  c19_b07.md: ...tcd 原语，过渡期双写，同步后切换读。  **答案：** 迁移计划：  **Phase 1：清点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布...
  c19_b07.md: ...）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和 etcd。读仍来自 Z...
  c19_b07.md: ...并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和 etcd。读仍来自 ZooKeeper。监控两者一致性（差异表示双写路径有 bug）。  **Phase 4：迁移...
  c19_b07.md: ...r 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和 etcd。读仍来自 ZooKeeper。监控两者一致性（差异表示双写路径有 bug）。  **Phase 4：迁移读**。一次一个用例，将读路径切换到 etc...
  c19_b07.md: ...d。先服务发现（风险低）；然后分布式锁（风险较高）；然后配置（风险最高，运营影响更大）。  **Phase 5：停止写 ZooKeeper**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper。  **Phase 6：下线...
  c19_b07.md: ...**Phase 5：停止写 ZooKeeper**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper。  **Phase 6：下线 ZooKeeper**。经过足够时间确认无残留读之后，下线 ZK 集群。  关键风险与缓...
  c19_b07.md: ...**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper。  **Phase 6：下线 ZooKeeper**。经过足够时间确认无残留读之后，下线 ZK 集群。  关键风险与缓解：  * **语义差异**：ZK 和 etcd...
  c19_b11.md: ...导云原生细分市场；Consul 处理 VM 和多 DC 部署；DNS 对稳定服务足够。  etcd 在新系统中基本替代 ZooKeeper，因为 Kubernetes 采用、Raft 与 ZAB 共识选择、gRPC API 和更好的云原生工具。ZooKeep...
  c19_b11.md: ...oKeeper，因为 Kubernetes 采用、Raft 与 ZAB 共识选择、gRPC API 和更好的云原生工具。ZooKeeper 仍在生产中使用（现有 Kafka、Hadoop 等），但缓慢衰退。  Config-as-data with watc...
  c28_b13.md: ...评估新系统。  **答案：** 算法和系统会演进。1998 年 Paxos 是标准共识；2026 年 Raft 是标准。ZooKeeper 曾是标准协调服务；现在是 etcd。2026 年分布式系统工程包括 Spanner、CockroachDB、vLLM、...
  c30_b03.md: ...Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENI...

=== reliable (36 occurrences) ===
  c04_b05.md: ...* ：给定一条应到达 N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓...
  c04_b05.md: ...息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort...
  c04_b05.md: ...mic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认...
  c04_b05.md: ...）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性...
  c04_b05.md: ...CP + for-loop)"] -->|"+ agreement<br/>(re-broadcasting)"| B[Reliable broadcast]         B -->|"+ uniformity<br/>(ack round befor...
  c04_b05.md: ...->|"+ uniformity<br/>(ack round before deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks...
  c04_b05.md: ...+ agreement (re-broadcasting on receipt)         v     Reliable broadcast         |         + uniformity (ack round before...
  c04_b05.md: ...niformity (ack round before delivery)         v     Uniform reliable broadcast         |         + causal order (vector clocks)...
  c04_b05.md: ...现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distributed Programming_（第2版，2011），该书逐层展开并给出完整伪代...
  c04_b05.md: ...最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交...
  c04_b05.md: ...务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器增量 | reliable | 顺序无关，只要交付  经济论证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，b...
  c04_b05.md: ...* ：给定一条应到达 N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓...
  c04_b05.md: ...息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort...
  c04_b05.md: ...mic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认...
  c04_b05.md: ...）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性...
  c04_b05.md: ...CP + for-loop)"] -->|"+ agreement<br/>(re-broadcasting)"| B[Reliable broadcast]         B -->|"+ uniformity<br/>(ack round befor...
  c04_b05.md: ...->|"+ uniformity<br/>(ack round before deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks...
  c04_b05.md: ...+ agreement (re-broadcasting on receipt)         v     Reliable broadcast         |         + uniformity (ack round before...
  c04_b05.md: ...niformity (ack round before delivery)         v     Uniform reliable broadcast         |         + causal order (vector clocks)...
  c04_b05.md: ...现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distributed Programming_（第2版，2011），该书逐层展开并给出完整伪代...
  c04_b05.md: ...最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交...
  c04_b05.md: ...务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器增量 | reliable | 顺序无关，只要交付  经济论证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，b...
  c04_b07.md: ...概念检测  **Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、ca...
  c04_b07.md: ...度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total...
  c04_b07.md: ...y（发送者正确、接收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增什么。   2. **Reliable broadcast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **U...
  c04_b07.md: ...ast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform reliable broadcast** ：新增 _uniformity_ ——若任何进程（正确或故障）交付，所有正确进程都交付。修复了...
  c04_b07.md: ...概念检测  **Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、ca...
  c04_b07.md: ...度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total...
  c04_b07.md: ...y（发送者正确、接收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增什么。   2. **Reliable broadcast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **U...
  c04_b07.md: ...ast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform reliable broadcast** ：新增 _uniformity_ ——若任何进程（正确或故障）交付，所有正确进程都交付。修复了...
  c30_b01.md: ...C., Guerraoui, R., & Rodrigues, L. (2011). _Introduction to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. —...
  c30_b01.md: .... D., & Toueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。...
  c30_b01.md: ...C., Guerraoui, R., & Rodrigues, L. (2011). _Introduction to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. —...
  c30_b01.md: .... D., & Toueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。...
  c30_b14.md: ....py    — best-effort broadcast (TCP + for-loop)     │   ├── reliable.py       — reliable broadcast via flooding     │   └── caus...
  c30_b14.md: ...broadcast (TCP + for-loop)     │   ├── reliable.py       — reliable broadcast via flooding     │   └── causal.py         — caus...

=== flp (36 occurrences) ===
  c06_b12.md: ...不可行。  第7章开始第三部分：共识。既然已知如何广播和复制，我们需要一组协议让节点组在任意故障下就一个值达成一致。从 FLP 不可能到部分同步，再到实用协议（Paxos、Raft、HotStuff）的旅程是接下来四章的主题。  ---...
  c07_b01.md: ...了一个让领域花数年才消化的结果：在即使只可能发生一次崩溃故障的完全异步系统中，任何确定性共识协议都无法保证终止。这就是 FLP 不可能性结果。  FLP 并不意味着共识在实践中不可能；它意味着共识可证明可解所需的假设强于“异步 + 崩溃”。本章其...
  c07_b01.md: ...结果：在即使只可能发生一次崩溃故障的完全异步系统中，任何确定性共识协议都无法保证终止。这就是 FLP 不可能性结果。  FLP 并不意味着共识在实践中不可能；它意味着共识可证明可解所需的假设强于“异步 + 崩溃”。本章其余部分讨论这些更强假设是什...
  c07_b02.md: ...### 7.3 FLP 不可能性  陈述：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能同时以 agreement、valid...
  c07_b02.md: ...为 X 可能只是慢。如果协议不等 X 就决定，可能出错（X 有不同信息）。如果协议等待，可能永远等下去。   #### FLP 没说什么  FLP 不是说共识在实践中不可能。它说确定性协议在“纯异步 + 一次崩溃”下不能保证终止。真实系统以三种方...
  c07_b02.md: ...如果协议不等 X 就决定，可能出错（X 有不同信息）。如果协议等待，可能永远等下去。   #### FLP 没说什么  FLP 不是说共识在实践中不可能。它说确定性协议在“纯异步 + 一次崩溃”下不能保证终止。真实系统以三种方式逃脱：  (a)...
  c07_b03.md: ...性质的合取。这不仅是理论趣味；它让设计师分别推理 safety 和 liveness。   #### 为何对共识重要  FLP 说：在纯异步下一次崩溃下，没有协议能同时满足 safety 和 liveness。选择是：  (a) 牺牲 safet...
  c07_b04.md: ...。  激进超时（快速故障检测）与保守超时（避免误怀疑）之间的张力，是运行共识集群的运维核心。   #### 常见误解  FLP 的常见误读：“FLP 说共识不可能。”不是。FLP 说确定性、有界终止的共识在 _特定假设_（纯异步、一次崩溃）下不可...
  c07_b04.md: ...障检测）与保守超时（避免误怀疑）之间的张力，是运行共识集群的运维核心。   #### 常见误解  FLP 的常见误读：“FLP 说共识不可能。”不是。FLP 说确定性、有界终止的共识在 _特定假设_（纯异步、一次崩溃）下不可能。生产系统满足更强条...
  c07_b04.md: ...）之间的张力，是运行共识集群的运维核心。   #### 常见误解  FLP 的常见误读：“FLP 说共识不可能。”不是。FLP 说确定性、有界终止的共识在 _特定假设_（纯异步、一次崩溃）下不可能。生产系统满足更强条件；共识每天在生产中解决。正确...
  c07_b04.md: ...确定性、有界终止的共识在 _特定假设_（纯异步、一次崩溃）下不可能。生产系统满足更强条件；共识每天在生产中解决。正确读 FLP 的方式是把它看作关于所需假设的定理：你需要 _某种_ 部分同步、随机化或故障检测。...
  c07_b05.md: ...Narwhal-Bullshark 等基于 DAG 的协议）。   ### 7.8 真实系统所在  生产共识部署对每种 FLP 逃逸都做出明确选择：  * **etcd** 用 Raft 加部分同步、基于心跳的故障检测。默认超时：心跳 100 m...
  c07_b06.md: ...进程通过点对点消息通信。协议必须能容忍最多 f 个崩溃故障（f 是协议参数）。  **Q2.** 用你自己的话陈述 FLP 不可能性。它 _没_ 说什么？  提示  FLP：纯异步 + 一次崩溃下，确定性共识不能保证终止。不是说共识在实践中不...
  c07_b06.md: ...个崩溃故障（f 是协议参数）。  **Q2.** 用你自己的话陈述 FLP 不可能性。它 _没_ 说什么？  提示  FLP：纯异步 + 一次崩溃下，确定性共识不能保证终止。不是说共识在实践中不可能。  **答案：** FLP 不可能性：在即使...
  c07_b06.md: ...什么？  提示  FLP：纯异步 + 一次崩溃下，确定性共识不能保证终止。不是说共识在实践中不可能。  **答案：** FLP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、te...
  c07_b06.md: ...分同步假设下，真实协议（Paxos、Raft）在网络稳定后保证终止。  (c) 真实系统坏了。真实系统正确；它们满足比 FLP 纯异步更强的条件。  正确读 FLP：它界定了你可做的假设。你不能同时 _完全异步_ 、_确定性_ 、_终止_ 三者兼...
  c07_b06.md: ...aft）在网络稳定后保证终止。  (c) 真实系统坏了。真实系统正确；它们满足比 FLP 纯异步更强的条件。  正确读 FLP：它界定了你可做的假设。你不能同时 _完全异步_ 、_确定性_ 、_终止_ 三者兼得。选择放弃哪个。  **Q3.**...
  c07_b06.md: ..._完全异步_ 、_确定性_ 、_终止_ 三者兼得。选择放弃哪个。  **Q3.** 定义部分同步。它如何让共识协议逃脱 FLP？  提示  部分同步：假设未知 GST 之后消息延迟有界。  **答案：** 部分同步（Dwork、Lynch、Sto...
  c07_b06.md: ...eement 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分同步断言 GST 后不存在这种调度。GST 后协议基于心跳的故障检测器变...
  c07_b06.md: ...nt 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分同步断言 GST 后不存在这种调度。GST 后协议基于心跳的故障检测器变得可靠（...
  c07_b07.md: ...事件永不发生的无限执行违反。示例：termination（每个正确进程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，livene...
  c07_b09.md: ...统分布式系统用例（配置存储、数据库、协调），Raft 仍是运维默认。  **Q3.** 为一个 2 进程共识示例走一遍 FLP 不可能性证明直觉。证明中对手起什么作用？  提示  对手控制消息交付。在纯异步中，对手可无限延迟一条消息。证明存在“二...
  c07_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“2026 年 FLP 只是历史好奇心；现代系统都已处理它，工程师无需了解。”  **讨论：** “历史好奇心”观点：每个生产共识协议都假设部...
  c07_b10.md: ...，工程师无需了解。”  **讨论：** “历史好奇心”观点：每个生产共识协议都假设部分同步。工程师只需选个协议，不必想 FLP。协议自会处理。  反驳：理解 FLP 对理解共识集群为何如此行为至关重要。Raft 在分区期间无法选出 leader...
  c07_b10.md: ...“历史好奇心”观点：每个生产共识协议都假设部分同步。工程师只需选个协议，不必想 FLP。协议自会处理。  反驳：理解 FLP 对理解共识集群为何如此行为至关重要。Raft 在分区期间无法选出 leader 时，就是 FLP 在起作用：在持续恶劣...
  c07_b10.md: ...处理。  反驳：理解 FLP 对理解共识集群为何如此行为至关重要。Raft 在分区期间无法选出 leader 时，就是 FLP 在起作用：在持续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视为“数据库坏了”，...
  c07_b10.md: ...选出 leader 时，就是 FLP 在起作用：在持续恶劣条件下，协议优先 safety 而非 liveness。不懂 FLP 的运维会视为“数据库坏了”，并可能采取破坏 safety 的反生产措施（强制 leader、禁用 Raft）。  现代...
  c07_b10.md: ...强制 leader、禁用 Raft）。  现代协议通过明确选择（始终 safe，liveness 有条件）来 _处理_ FLP。部署、运维、调试协议的工程师需要理解做了什么选择。FLP 就是解释该选择的框架。  第二个考虑：2026 年仍在设计新...
  c07_b10.md: ...选择（始终 safe，liveness 有条件）来 _处理_ FLP。部署、运维、调试协议的工程师需要理解做了什么选择。FLP 就是解释该选择的框架。  第二个考虑：2026 年仍在设计新共识协议（Narwhal-Bullshark、Mystic...
  c07_b10.md: ...年仍在设计新共识协议（Narwhal-Bullshark、Mysticeti、HotStuff 变体）。每种都做出与 FLP 相关的明确选择。工程师在它们之间选择需要比较的词汇。  综合：FLP 是基础，不是好奇心。应用工程师需要知道不可能性说...
  c07_b10.md: ...ceti、HotStuff 变体）。每种都做出与 FLP 相关的明确选择。工程师在它们之间选择需要比较的词汇。  综合：FLP 是基础，不是好奇心。应用工程师需要知道不可能性说了什么、逃生舱是什么。证明本身主要对协议设计者重要；其含义（safet...
  c07_b11.md: ...崩溃的情况下就一个值达成一致的问题。三条性质（agreement、validity、termination）是经典陈述。FLP 不可能性说确定性协议在纯异步下一次崩溃下不能保证终止全部三条；现代逃生舱是部分同步、随机化和故障检测器。  Safet...
  c30_b02.md: ...consensus with one faulty process." _Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Con...
  c30_b06.md: ...* **Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被保护资源强制执行令牌顺序。 * **FLP impossibility（FLP 不可能性）**：Fischer-Lynch-Paterson 1985。确定性共识...
  c30_b16.md: ...每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 | "FLP 说共识不可能。" | FLP 说确定性共识在纯异步下有界终止不可能。真实系统用部分同步。 | Ch.7 | | 12...
  c30_b16.md: ...成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 | "FLP 说共识不可能。" | FLP 说确定性共识在纯异步下有界终止不可能。真实系统用部分同步。 | Ch.7 | | 12 | "Paxos 选举 lea...

=== batching (35 occurrences) ===
  c08_b04.md: ...区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（2011）和 Spann...
  c24_b05.md: ...个请求的 GPU-秒。更好的 attention（PagedAttention、RadixAttention）、更好的 batching（continuous batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。...
  c24_b05.md: ...tion（PagedAttention、RadixAttention）、更好的 batching（continuous batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuou...
  c24_b05.md: ...更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空...
  c24_b05.md: ...### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  *...
  c24_b05.md: ...，它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求...
  c24_b05.md: ...GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batc...
  c24_b05.md: ...hing per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提...
  c24_b05.md: ...个请求的 GPU-秒。更好的 attention（PagedAttention、RadixAttention）、更好的 batching（continuous batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。...
  c24_b05.md: ...tion（PagedAttention、RadixAttention）、更好的 batching（continuous batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuou...
  c24_b05.md: ...更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空...
  c24_b05.md: ...### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  *...
  c24_b05.md: ...，它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求...
  c24_b05.md: ...GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batc...
  c24_b05.md: ...hing per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提...
  c24_b07.md: ...推理集群常同时运行两者，负载均衡器按工作负载把请求路由到最适合的引擎。  **Q4.** 什么是 continuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲...
  c24_b07.md: ...PU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit...
  c24_b07.md: ...续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请...
  c24_b07.md: ...00 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous batching...
  c24_b07.md: ...batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状...
  c24_b08.md: ...Tobias 的集群 GPU 利用率为 60%。找出三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案...
  c24_b08.md: ...ated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常...
  c24_b08.md: ...** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）...
  c24_b08.md: ...us batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching...
  c24_b08.md: ...batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching；若 Tobias 未用现代引擎，这是第一步。预期利用率提升：从 60% 到 80%+。  (b) **前缀共享（SGL...
  c24_b08.md: ...t；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching（若尚未），然后 SGLang 用于前缀复用工作负载，然后 disaggregation。预期最终集群利用率 80%+，...
  c24_b09.md: ...* 某团队推理集群出现双峰延迟：短请求快，长请求阻塞短请求。诊断并提出修复。  提示  队头阻塞。Continuous batching 有帮助；优先队列帮助更大；分离 prefill/decode 是架构修复。  **答案：** 双峰延迟是队头阻塞。长请...
  c24_b09.md: ...求占用资源（GPU 内存、每步计算时间）；短请求在其后等待。  修复，按影响排序：  (a) **Continuous batching**（若尚未）：短请求可与长请求同 batch。每步并行处理所有请求；短请求生成完自己的 token 即完成，不受长请求...
  c24_b09.md: ...可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/decode（第25章）...
  c24_b10.md: ...作负载）是 2026 默认。TGI 合理，但 vLLM 对大多数工作负载吞吐更好。  (b) **Continuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：...
  c24_b11.md: ...NIM 是企业部署平台：容器化微服务、多引擎支持、硬件优化、企业特性。企业团队的标准部署表面。  Continuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-...
  c24_b11.md: ...nuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：50 节点、400 GPU、每月 12...
  c25_b10.md: ...isaggregated prefill/decode（第25章）：30-50% 成本降低。 * Continuous batching（第24章）：若尚未使用，2-5 倍吞吐提升。 * 前缀共享（SGLang）：重复 prompt 工作负载 20-50%...
  c26_b08.md: ...5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefill 需 10 秒...
  c30_b14.md: ...│   └── continuous_batching_toy.py — minimal continuous-batching scheduler     └── ch27_review/         ├── ai_review_checkl...

=== batch (35 occurrences) ===
  c24_b05.md: ...。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  * 请求到达推理服务器。 * 服务器保持一个 “batch” 活跃请求；GPU 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length li...
  c24_b05.md: ...U 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等...
  c24_b05.md: ...n。 * 当请求完成（生成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。...
  c24_b05.md: ...。有了它，GPU 利用率稳定（始终并行处理多个请求）。  工作原理：  * 请求到达推理服务器。 * 服务器保持一个 “batch” 活跃请求；GPU 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length li...
  c24_b05.md: ...U 每步为每个请求处理一个 token。 * 当请求完成（生成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等...
  c24_b05.md: ...n。 * 当请求完成（生成 EOS 或 hit length limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。...
  c24_b07.md: ...tinuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batc...
  c24_b07.md: ...s batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU...
  c24_b07.md: ...atch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 ba...
  c24_b07.md: ...ch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无...
  c24_b07.md: ...求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（stati...
  c24_b07.md: ...离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间...
  c24_b07.md: ...**Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每...
  c24_b07.md: ...tatic）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 tok...
  c24_b07.md: ...闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 tokens。...
  c24_b07.md: ...**可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果：相比朴素...
  c24_b07.md: ...tinuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 KV cache。复杂度值得吞吐收益。...
  c24_b07.md: ...LLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 KV cache。复杂度值得吞吐收益。...
  c24_b07.md: ...tinuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batc...
  c24_b07.md: ...s batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU...
  c24_b07.md: ...atch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 ba...
  c24_b07.md: ...ch。GPU 每步为每个请求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无...
  c24_b07.md: ...求处理一个 token。请求完成（生成 EOS 或 hit length limit）后离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（stati...
  c24_b07.md: ...离开 batch；等待请求加入。Batch 持续补充；GPU 始终并行处理多个请求。  如何提高利用率：  (a) **Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间...
  c24_b07.md: ...**Batch 间无空闲**。经典（static）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每...
  c24_b07.md: ...tatic）batching 等 N 个请求，一起处理，等全部完成。GPU 在 batch 之间空闲（等待新请求）且在 batch 内空闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 tok...
  c24_b07.md: ...闲（等待慢请求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 tokens。...
  c24_b07.md: ...**可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果：相比朴素...
  c24_b07.md: ...tinuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 KV cache。复杂度值得吞吐收益。...
  c24_b07.md: ...LLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 KV cache。复杂度值得吞吐收益。...
  c24_b09.md: ...后等待。  修复，按影响排序：  (a) **Continuous batching**（若尚未）：短请求可与长请求同 batch。每步并行处理所有请求；短请求生成完自己的 token 即完成，不受长请求影响。这消除大部分队头阻塞。  (b) **优...
  c24_b09.md: ...en 即完成，不受长请求影响。这消除大部分队头阻塞。  (b) **优先队列**：短请求和长请求分别走不同队列。短请求 batch 快速处理；长请求 batch 按自己节奏处理。权衡：更难最优共享 GPU 资源。  (c) **分离 prefill...
  c24_b09.md: ...这消除大部分队头阻塞。  (b) **优先队列**：短请求和长请求分别走不同队列。短请求 batch 快速处理；长请求 batch 按自己节奏处理。权衡：更难最优共享 GPU 资源。  (c) **分离 prefill 和 decode（第25章）*...
  c24_b11.md: ...队的标准部署表面。  Continuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：...
  c26_b09.md: ...。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative...

=== continuous (35 occurrences) ===
  c24_b05.md: ...batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率...
  c24_b05.md: ...）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。...
  c24_b05.md: ...Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 se...
  c24_b05.md: ...batching）、更好的硬件利用率（disaggregation，第25章）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率...
  c24_b05.md: ...）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用率稳定（始终并行处理多个请求）。...
  c24_b05.md: ...Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 se...
  c24_b07.md: ...SGLang”；现代推理集群常同时运行两者，负载均衡器按工作负载把请求路由到最适合的引擎。  **Q4.** 什么是 continuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；b...
  c24_b07.md: ...利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成...
  c24_b07.md: ...kens。  (c) **可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果...
  c24_b07.md: ...求继续。Static batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管...
  c24_b07.md: ...SGLang”；现代推理集群常同时运行两者，负载均衡器按工作负载把请求路由到最适合的引擎。  **Q4.** 什么是 continuous batching？它如何提高 GPU 利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；b...
  c24_b07.md: ...利用率？  提示  GPU 并行处理多个请求；请求持续加入和离开 batch；batch 间无空闲。  **答案：** Continuous batching：推理服务器保持一个活跃请求 batch。GPU 每步为每个请求处理一个 token。请求完成（生成...
  c24_b07.md: ...kens。  (c) **可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果...
  c24_b07.md: ...求继续。Static batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管...
  c24_b08.md: ...问题  **Q1.** Tobias 的集群 GPU 利用率为 60%。找出三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25...
  c24_b08.md: ...）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous ba...
  c24_b08.md: ...**Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous...
  c24_b08.md: ...continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching；若 Tobias 未用现代引擎，这是第一步。预期利用率提升：从 60% 到 80%+。  (b) *...
  c24_b08.md: ...批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching（若尚未），然后 SGLang 用于前缀复用工作负载，然后 disaggregation。预期最终集群...
  c24_b08.md: ...问题  **Q1.** Tobias 的集群 GPU 利用率为 60%。找出三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25...
  c24_b08.md: ...）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous ba...
  c24_b08.md: ...**Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous...
  c24_b08.md: ...continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching；若 Tobias 未用现代引擎，这是第一步。预期利用率提升：从 60% 到 80%+。  (b) *...
  c24_b08.md: ...批量处理大 prompt；decode 节点服务大量并发 token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching（若尚未），然后 SGLang 用于前缀复用工作负载，然后 disaggregation。预期最终集群...
  c24_b09.md: ...答案。  **Q3.** 某团队推理集群出现双峰延迟：短请求快，长请求阻塞短请求。诊断并提出修复。  提示  队头阻塞。Continuous batching 有帮助；优先队列帮助更大；分离 prefill/decode 是架构修复。  **答案：** 双峰延...
  c24_b09.md: ...峰延迟是队头阻塞。长请求占用资源（GPU 内存、每步计算时间）；短请求在其后等待。  修复，按影响排序：  (a) **Continuous batching**（若尚未）：短请求可与长请求同 batch。每步并行处理所有请求；短请求生成完自己的 token...
  c24_b09.md: ...e decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/dec...
  c24_b09.md: ...答案。  **Q3.** 某团队推理集群出现双峰延迟：短请求快，长请求阻塞短请求。诊断并提出修复。  提示  队头阻塞。Continuous batching 有帮助；优先队列帮助更大；分离 prefill/decode 是架构修复。  **答案：** 双峰延...
  c24_b09.md: ...峰延迟是队头阻塞。长请求占用资源（GPU 内存、每步计算时间）；短请求在其后等待。  修复，按影响排序：  (a) **Continuous batching**（若尚未）：短请求可与长请求同 batch。每步并行处理所有请求；短请求生成完自己的 token...
  c24_b09.md: ...e decoding 可提速 decode 2-3 倍；长请求完成更快，缩短短请求等待时间。  对 Continuum：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/dec...
  c24_b10.md: ...SGLang 适合工作负载）是 2026 默认。TGI 合理，但 vLLM 对大多数工作负载吞吐更好。  (b) **Continuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor paral...
  c24_b11.md: ...出。  NVIDIA NIM 是企业部署平台：容器化微服务、多引擎支持、硬件优化、企业特性。企业团队的标准部署表面。  Continuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batch...
  c25_b10.md: ...模型质量）：  * Disaggregated prefill/decode（第25章）：30-50% 成本降低。 * Continuous batching（第24章）：若尚未使用，2-5 倍吞吐提升。 * 前缀共享（SGLang）：重复 prompt 工作...
  c26_b08.md: ...阻塞短请求**：单个 5000-token 输出请求阻塞 GPU decode 容量。其他请求的尾部 token 等待。Continuous batching 有帮助但不能完全消除。  (b) **Prompt 长度可变**：50000-token prefi...
  c30_b14.md: ...inference/     │   └── continuous_batching_toy.py — minimal continuous-batching scheduler     └── ch27_review/         ├── ai_revi...

=== opentelemetry (34 occurrences) ===
  c01_b05.md: ...tt 的 CMU 15-721 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为...
  c01_b05.md: ...ed Systems Observability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTelemetry Collector 文档。 * **联邦学习与隐私保护计算** ：差分隐私、安全多方计算、分布式 ML 中的同态加密。...
  c20_b01.md: ...本章讨论追踪的分布式系统特定方面：因果保持、分区下采样、跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vol 3（可观测性）。本章聚焦追踪所支持的分布式系统推理。   ##...
  c20_b03.md: ...置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准  2026 年，OpenTelemetry 是分布式追踪仪器的事实标准。关键特性：  * 每种主流语言都有 ve...
  c20_b03.md: ...g、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准  2026 年，OpenTelemetry 是分布式追踪仪器的事实标准。关键特性：  * 每种主流语言都有 vendor-neutral API。 * 常见库自动...
  c20_b03.md: ...dog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开...
  c20_b03.md: ...从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeline 细节（Series IV Vol 3 会讲）。要点：Op...
  c20_b03.md: ...older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeline 细节（Series IV Vol 3 会讲）。要点：OpenTelemetry 是现代默认；使用它可...
  c20_b03.md: ...ry。  本章不展开 OpenTelemetry pipeline 细节（Series IV Vol 3 会讲）。要点：OpenTelemetry 是现代默认；使用它可消除旧追踪系统的 vendor lock-in 顾虑。...
  c20_b05.md: ...准的 `traceparent`）或消息队列元数据（Kafka headers、AWS SQS attributes）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 trace context。异步...
  c20_b06.md: ...-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1%...
  c20_b08.md: ...。Tracing  surfaced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 trace 连接。诊断。  提...
  c20_b08.md: ...在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.propagate import inject      headers = {}     inject(header...
  c20_b08.md: ...`traceparent` 和（可选）`tracestate` headers 插入。消费端：       from opentelemetry.propagate import extract      headers = {k: v for k, v in r...
  c20_b08.md: ...ntext。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客户端自动仪器库（`opentelemetry-instrumentation-kafka-pyt...
  c20_b08.md: ...afka 客户端自动仪器库（`opentelemetry-instrumentation-kafka-python`、`opentelemetry-instrumentation-confluent-kafka`）可自动处理传播。若团队使用这些，传播透明。若使用未自...
  c20_b08.md: ...如上手动传播。  该模式可泛化：任何异步边界（队列、定时任务、webhooks、event buses）都需要显式传播。OpenTelemetry 提供 `inject`/`extract` 原语；应用代码使用它们。  Kafka 传播修复后，团队将看到从 API...
  c20_b09.md: ...“2026 年，分布式追踪已经解决了微服务架构中的调试问题。挣扎的团队只是追踪做错了。”  **讨论：** 辩护：有了 OpenTelemetry、成熟的追踪后端（Honeycomb、Datadog、Tempo）和良好的工程纪律，分布式追踪提供调试大多数生产问题所需...
  c20_b09.md: ...实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelemetry" 是运维标准。  第二个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关联 traces、logs、metr...
  c20_b09.md: ...taDog；换到另一追踪后端（Honeycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetr...
  c20_b09.md: ...eycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetry，然后导出到配置的后端。后端可通过...
  c20_b09.md: ...penTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetry，然后导出到配置的后端。后端可通过配置切换，不用改代码。       # 使用 OpenTelemetry 的 vend...
  c20_b09.md: ...团队代码调用 OpenTelemetry，然后导出到配置的后端。后端可通过配置切换，不用改代码。       # 使用 OpenTelemetry 的 vendor-neutral 写法：     from opentelemetry import trace...
  c20_b09.md: ...改代码。       # 使用 OpenTelemetry 的 vendor-neutral 写法：     from opentelemetry import trace     tracer = trace.get_tracer(__name__)      w...
  c20_b09.md: ...do_work()   上述代码可与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练...
  c20_b09.md: ...厂商博客和教程在 LLM 训练数据中过度代表特定产品；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样...
  c20_b09.md: ...、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTelemetry 基础之上再提供该后端的具体配置。...
  c20_b10.md: ...d-based vs tail-based）、跨共识边界的 trace context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采...
  c20_b10.md: ...2026 年事实标准：每种主流语言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展...
  c28_b02.md: ...的 LLM 推理（Tobias 的组件）。每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty aler...
  c28_b05.md: ...s | E.2（Roblox Consul）、E.7（Fastly 配置 rollout） | | 20 | 追踪 | OpenTelemetry 端到端 | E.3（Cloudflare 邮件循环通过 trace 诊断） | | 21 | 级联故障 | 重试预算、...
  c28_b18.md: ...ion 排序用 HLC；共识用 Raft；协作编辑用 CRDT；锁用 etcd + fencing token；追踪用 OpenTelemetry；韧性用重试预算和熔断器；验证用混沌工程；AI 辅助代码审查用六谎言清单。  心智模型：分布式系统在时间、身份、故障上撒...
  c30_b14.md: ...ens     ├── ch20_tracing/     │   └── propagation_demo.py — OpenTelemetry context propagation across HTTP + Kafka     ├── ch21_resili...
  c30_b16.md: ...mesh 还增加 mTLS、流量策略、可观测性——以及运维开销。按需选择。 | Ch.19 | | 23 | "我们有 OpenTelemetry；追踪已解决。" | 追踪需要在每个异步边界（队列、定时任务）和共识边界传播 context。默认 Kafka 客户端不...

=== anya (33 occurrences) ===
  c01_b03.md: ...作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不同，心智动作相同。...
  c01_b08.md: ...game day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuki 和 Tobias 最终在第28章一起交付的系统。第1章到第28章之间的路径是本书正文；第29章则前瞻这个系统...
  c08_b08.md: ...，必须用更高 ballot 重试。  **答案：** 设定：5 个 acceptor。Proposer A 想提议值 “Anya”；Proposer B 想提议 “Olamide”。  **并发 prepare：**  * A 发送 `PREPAR...
  c08_b08.md: ...lamide”——但该值已在 ballot 20 时被选定。  系统收敛到单一值 “Olamide”。A 原本想提议 “Anya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集群有 5 个节点。当前 lea...
  c09_b01.md: ...Anya Reddy 两个月前加入 Stratus DB。她的第一个项目：为复制层选个共识库。她读了 Paxos 论文。她读了...
  c09_b01.md: ...Algorithm”，目标是：至少和 Paxos 一样正确，但 _可理解_。摘要明确把可理解性作为主要贡献。  本章按 Anya 学习 Raft 的方式教授：三个子问题（leader 选举、日志复制、安全性）、term 的作用、AppendEntr...
  c09_b05.md: ...载、Flexible Paxos 用于可调法定人数。但对 2026 年的新项目，默认是 Raft。   ### 9.9 Anya 的决定  Anya 为 Stratus DB 复制层选择了 `tikv/raft-rs` 库（Rust）。她在设计文档...
  c09_b05.md: ...Paxos 用于可调法定人数。但对 2026 年的新项目，默认是 Raft。   ### 9.9 Anya 的决定  Anya 为 Stratus DB 复制层选择了 `tikv/raft-rs` 库（Rust）。她在设计文档中记录的理由：  (...
  c09_b05.md: ...leases 可提供）。对 Stratus 场景（金融级持久性、亚 100 ms 提交、三区域部署），这些权衡正确。  Anya 遵循的模式是 2026 标准：选一个成熟的 Raft 库，依赖其工具，接受运维默认值直到需要调优。后续章节（第11-1...
  c09_b08.md: ...#### 应用问题  **Q1.** Anya 正在实现 Stratus DB 复制层。她有一个 3 节点 Raft 集群。一个 follower 与集群分区 30...
  c09_b11.md: ...afety），并基于 terms、elections 和 AppendEntries 一致性检查提供清晰的运维模型。  Anya 为 Stratus DB 选择 raft-rs、依赖库工具、接受运维默认值，这是 2026 年的标准模式。大多数团队不...
  c11_b05.md: ...这就是方法。在采用任何新数据库前使用它；在阅读任何声称某系统“比 X 更好”的博客前使用它。   ### 11.5 Anya 的 Stratus DB 阅读  Anya 把同样方法用于她正在开发的系统。阅读结果：  Question | Ans...
  c11_b05.md: ...在阅读任何声称某系统“比 X 更好”的博客前使用它。   ### 11.5 Anya 的 Stratus DB 阅读  Anya 把同样方法用于她正在开发的系统。阅读结果：  Question | Answer（Stratus DB） ---|--...
  c11_b05.md: ...在开发 Right for | CockroachDB 风格工作负载，Stratus 合作公司内部使用  这份阅读帮助 Anya 向潜在客户沟通系统画像。客户不需要读 CockroachDB-vs-Stratus 比较；他们需要方法，根据自己的工作...
  c11_b11.md: ...片、范围分片、一致哈希、Bigtable / Cassandra / RocksDB 的 LSM-tree 基础，以及 Anya 在 Stratus DB 集群上无停机重分片的实例。  ---...
  c12_b01.md: ...Anya 盯着团队 on-call 发来的 Slack 消息：“Stratus 集群 7 磁盘使用率 87%，每天增长 2%。下...
  c12_b01.md: ...有节点移到新节点，零停机，同时集群继续服务每秒 50,000 笔事务。  这是课程大纲里的工作流 7：无停机重分片迁移。Anya 之前做过三次。协议在她脑中；运维纪律在她的 runbook 里。本章将走一遍，并介绍使之可能的底层基础（哈希 vs 范...
  c12_b04.md: ...。在现代 SSD 工作负载中，LSM 的优势是 _可预测_ 写吞吐；读性能通常够用。   ### 12.5 工作流 7：Anya 的重分片迁移  现在看 Anya 的具体问题：12 节点集群磁盘 87%，需下周前加 3 节点，同时服务 50,000...
  c12_b04.md: ...M 的优势是 _可预测_ 写吞吐；读性能通常够用。   ### 12.5 工作流 7：Anya 的重分片迁移  现在看 Anya 的具体问题：12 节点集群磁盘 87%，需下周前加 3 节点，同时服务 50,000 TPS 且无停机。  Anya...
  c12_b04.md: ...Anya 的具体问题：12 节点集群磁盘 87%，需下周前加 3 节点，同时服务 50,000 TPS 且无停机。  Anya 的协议：   #### 步骤 1：准备并加入新节点  3 个新节点以相同软件、配置部署。它们作为成员加入集群（集群协调...
  c12_b04.md: ...nderreplication）。  若某 range “卡住”（例如移动期间 Raft group 落后），运维介入。Anya 之前三次重分片中有两次遇到；每次 10 分钟解决。   #### 步骤 5：退役旧硬件（如适用）  本次重平衡不涉及退...
  c12_b04.md: ...pprox 5.8 \text{ 小时} \]  实践中，移动被节流以避免影响客户流量；实际挂钟时间 12-18 小时。Anya 安排在夜间进行；第二天早上集群重平衡完毕，每节点磁盘使用率约 70%（42 TB 分布在 15 × 4 TB = 60...
  c12_b05.md: ...ge 多数不受影响），因此用户可见影响只是短暂降级。  纪律是运维层面的：runbook 是多年移动略出问题的经验结晶。Anya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tr...
  c12_b08.md: ...#### 应用问题  **Q1.** Anya 的集群是 12 × 4 TB 节点，磁盘使用 87%。她要加 3 个节点（也是 4 TB）。走一遍重分片时间预算，假设...
  c12_b08.md: ...总吞吐（理论值的 50%）：需 11.7 小时。工作日夜间快速跑、白天进一步节流：实际挂钟可能 15-24 小时。  Anya 安排在工作日晚上 8 点开始。到第二天早上约完成 70%；剩余部分第二天以较低速率完成。从开始约 36 小时后集群重平...
  c12_b08.md: ...SM compaction 开销、Raft snapshot 流）。 * 客户可见：p99 延迟可能上升 10-30%，Anya 会监控并在超过 SLA 时节流。  **Q2.** 一个团队为多租户 SaaS 实现数据库。每个租户有自己的行集合。他...
  c12_b11.md: ...mpaction。LSM 在写重工作负载中击败 B+ tree；B+ tree 在读重单节点 OLTP 中仍然占优。  Anya 的重分片（工作流 7）是在线重平衡：准备新节点、更新元数据、流式传输数据、验证。12 到 15 节点移动了 8 TB，...
  c18_b08.md: ...一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现代系统通常可围绕专用队列基础设施设计。  **Q2.** Anya 正在为 Stratus DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序...
  c27_b15.md: ...I 审查 PR 橡皮图章是被点名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模型是贯穿线。  ---...
  c28_b01.md: ...时间是 2026-09-15。四位主角一起交付了多 region 支付平台。  Olamide 是 SRE 主管。Anya 是数据库主管（Stratus DB，现在内嵌为 Crescent 的 commit-log 层）。Yuki 从 Gli...
  c28_b07.md: ...置了六个谎言；我们用事故为之买单。对混沌工程、可观测性和诊断方法纪律的投资，造就了一个 ‘刚刚好使’ 的系统。”  **Anya（Stratus DB 主管）**：“带 fencing token 的 Raft 复制协调器是系统安全的核心。集成到...
  c28_b10.md: ...| 接受营销级 “一致”；混淆 CAP 与 PACELC；遗漏 per-operation 一致性选择 | | 7 | Anya 的重分片迁移 | Ch.12 §12.5 | N 节点集群磁盘利用率高；需加 M 节点 | 再平衡集群，零停机，事务一...
  c29_b04.md: ...2026、2027 或更晚读这本书。具体年份影响哪些具名系统仍 current。它不影响心智模型。  Olamide、Anya、Yuki 和 Tobias 是复合人物；他们的系统和事件是作者工作和评审过的生产架构的真实组合。平台设计（分层架构、每...

=== distributed (33 occurrences) ===
  c03_b02.md: ...ie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distributed System"（1978），必读。关键洞见是：给分布式系统中的事件排序不需要同步时钟，只需要 _happens-bef...
  c04_b05.md: ...uerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distributed Programming_（第2版，2011），该书逐层展开并给出完整伪代码与正确性证明。   ### 4.8 你的应用...
  c18_b03.md: ...Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它为何没有提供表面上的安全性质。该文是分布式锁的奠基性读物。  核心问题...
  c27_b11.md: ...stency      def process_payment(payment):         # Acquire distributed lock for the user's account         lock_key = f"user_lock:...
  c30_b01.md: ...— 学术处理的教材基础。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S.,...
  c30_b01.md: ...AP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IE...
  c30_b01.md: ..., L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System." _Communications of the ACM_. — 第3章必读。 * Kulkarni,...
  c30_b01.md: ...Corbett, J. C., et al. (2012). "Spanner: Google's Globally-Distributed Database." _OSDI_. — TrueTime 描述。   #### 广播与复制（第二部分 — 第4-6章...
  c30_b01.md: ...Rodrigues, L. (2011). _Introduction to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. — 广播层级参考。 * Chandra, T. D...
  c30_b01.md: ...oueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K....
  c30_b01.md: ...— 学术处理的教材基础。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S.,...
  c30_b01.md: ...AP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IE...
  c30_b01.md: ..., L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System." _Communications of the ACM_. — 第3章必读。 * Kulkarni,...
  c30_b01.md: ...Corbett, J. C., et al. (2012). "Spanner: Google's Globally-Distributed Database." _OSDI_. — TrueTime 描述。   #### 广播与复制（第二部分 — 第4-6章...
  c30_b01.md: ...Rodrigues, L. (2011). _Introduction to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. — 广播层级参考。 * Chandra, T. D...
  c30_b01.md: ...oueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K....
  c30_b02.md: ..., Lynch, N. A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. —...
  c30_b02.md: ...om Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree...
  c30_b02.md: ...Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications." _OSDI_. — Percolator。 * Th...
  c30_b02.md: .... — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_....
  c30_b02.md: ...DT（第五部分 — 第15-17章）  * Chandy, K. M., & Lamport, L. (1985). "Distributed snapshots: determining global states of distributed systems...
  c30_b02.md: ...1985). "Distributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Peter...
  c30_b02.md: ..., Lynch, N. A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. —...
  c30_b02.md: ...om Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree...
  c30_b02.md: ...Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications." _OSDI_. — Percolator。 * Th...
  c30_b02.md: .... — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_....
  c30_b02.md: ...DT（第五部分 — 第15-17章）  * Chandy, K. M., & Lamport, L. (1985). "Distributed snapshots: determining global states of distributed systems...
  c30_b02.md: ...1985). "Distributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Peter...
  c30_b03.md: ...协调与运维（第六、七部分 — 第18-23章）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M....
  c30_b03.md: ..., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Ch...
  c30_b03.md: ...协调与运维（第六、七部分 — 第18-23章）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M....
  c30_b03.md: ..., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Ch...
  c30_b14.md: ...#### 预期仓库结构               distributed-systems-2026-examples/     ├── ch03_clocks/     │   ├── lam...

=== wait (33 occurrences) ===
  c02_b04.md: ...域副本法定人数往返的提交，比只需写入本地 leader 的提交更慢。Spanner 支付著名的 7 ms commit-wait（第13章），通过 TrueTime 提供线性化时间戳。  DynamoDB 在分区期间是 AP（分区节点继续从副本提供...
  c02_b04.md: ...期间 | 否则（无分区） ---|---|--- Spanner | C（拒绝少数侧写入） | C，伴随 commit-wait 延迟 CockroachDB | C（拒绝少数侧写入） | C，伴随法定人数写入延迟 TiDB / YugabyteD...
  c02_b08.md: ...) Spanner：PC/EC。分区时偏好一致性（被分区出去的副本拒绝写入）。无分区时仍保持一致，代价是 commit-wait 延迟（TrueTime 带来的 7 ms 成本）。  (b) CockroachDB：PC/EC。分区时，少数侧拒绝受...
  c02_b08.md: ...在 PACELC 下，两个选项都能满足需求，但代价不同。  Spanner：默认线性化。每次库存减量支付 commit-wait 延迟（约 7 ms 基线，加上法定人数往返）。在 100,000 请求/秒下，Spanner 需要足够分片（按商品分片...
  c02_b11.md: ...制模型。 * 优点：跨工作负载事务变得简单（原子地减量库存并更新排行榜）。 * 缺点：Spanner 的 commit-wait 延迟每次写入都要付，即使对工作负载并不需要。 * 缺点：每个分片吞吐受共识往返限制，计数器在 Spanner 上无法像...
  c02_b14.md: ...t、向量、HLC、TrueTime 等协议，让你能够在机器间推理顺序而不撒谎。本章提到的 Spanner commit-wait 延迟将在第3章完整解释；CockroachDB 基于 HLC 的 MVCC 将在第13章完整解释。  ---...
  c03_b07.md: ...报告不确定性的软件来实现这一点。典型 TrueTime 不确定度在 1 到 7 毫秒量级。   #### commit-wait 协议  Spanner 使用 TrueTime 为每个事务分配一个提交时间戳，保证该时间戳大于系统过去任何已提交事务。...
  c03_b07.md: ...移上界（默认 500 ms）作为“给普通人的 TrueTime 替代方案”。Spanner 的 7 ms commit-wait 在 CockroachDB 中变成更大的不确定性窗口，表现为事务重启成本而非 commit-wait，但精神相似：先界...
  c03_b07.md: ...s commit-wait 在 CockroachDB 中变成更大的不确定性窗口，表现为事务重启成本而非 commit-wait，但精神相似：先界定时钟偏移，再设计容忍它的协议。   #### 常见误解  一个常见信念：“如果我跑 NTP 并与 p...
  c03_b08.md: ...ive | What it costs ---|---|--- Spanner | TrueTime + commit-wait | 每次写事务 7 ms + GPS 原子钟基础设施 CockroachDB / YugabyteDB / TiDB...
  c03_b12.md: ..._一致排序_ （HLC 已提供）。大多数分布式应用代码只需要后者。  **Q3.** Spanner 的 commit-wait 是 7 ms。CockroachDB 的 HLC 带偏移上界默认 500 ms。为什么 CockroachDB 的数字...
  c03_b14.md: ...时间戳排序在任何分区下都能存活而不重启。对重启不可接受的工作负载（热键高竞争写入），TrueTime 的 commit-wait 比分摊到每次重启的成本更划算。  综合：HLC 加严格 NTP 对大多数团队是正确默认。TrueTime 对能负担 G...
  c03_b14.md: ...格 NTP 对大多数团队是正确默认。TrueTime 对能负担 GPS 基础设施且工作负载中重启成本超过 commit-wait 成本的团队是正确答案。AWS 的 clock-bound API（Amazon Time Sync Service）首...
  c03_b14.md: ...。TrueTime 的 `[earliest, latest]` API 迫使系统对不确定性变宽作出反应（commit-wait 自动增长）。HLC 的失败模式不那么可见，从运维纪律角度看可能更糟。  **Q2.** 一个 LLM 生成的日志系统用...
  c03_b15.md: ...earliest, latest]` 不确定性区间。Spanner 用 TrueTime 支付 7 ms commit-wait，换取线性化全局时间戳。AWS 的 clock-bound API 正在让 TrueTime 类原语在云厂商上可用。HL...
  c05_b11.md: ...制。光速限制、数据中心拓扑（CLOS、leaf-spine）以及跨区域的延迟税。第3章的 Spanner commit-wait 会作为一致全局复制的基本成本重新出现；替代方案（区域分区、follower 读、geo-DNS）是在不每次操作都付税的...
  c06_b08.md: ...c) **全局复制（Spanner）。** 每个聊天室数据驻留在某个区域并全球复制。写付 Spanner commit-wait（~7 ms）加 quorum 往返；跨大陆 quorum 约 200 ms 提交。读可用 follower 区域做有界...
  c11_b03.md: ...on protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS +...
  c11_b03.md: ...接受写；TrueTime 确保分区后时间戳一致 PACELC | PC / EC（分区下一致；正常运营为 commit-wait 延迟买单） Strengths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限...
  c11_b03.md: ...局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性化的工作负载（广告、金融账本）；Google 自身基础设施   #### C...
  c12_b11.md: ...colator（真正的 Spanner 风格事务模式）、Calvin 的确定性排序、TrueTime 的 commit-wait、CockroachDB 的 HLC-based MVCC 作为实用 TrueTime 替代。四位主角的多区域支付系统在...
  c13_b04.md: ...务 _等待_ 直到 `TT.now().earliest > commit_timestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任何观察到此...
  c13_b04.md: ...mestamp`。这就是 commit-wait，通常 7 ms。   4. 事务释放锁；提交持久化。  Commit-wait 确保时间戳顺序与实时一致：任何观察到此次提交的事务（以大于 commit_timestamp 的时间戳读）自己一定会被...
  c13_b04.md: ...ablet（各跑 Paxos）。2PC 层增加 commit_timestamp 协调。TrueTime commit-wait 确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime commit-...
  c13_b04.md: ...确保时间戳在系统范围内与实时一致。  结果：可串行化、线性化、多区域事务，提交延迟由 TrueTime commit-wait 加上 Paxos 法定人数往返决定。对 Spanner 典型的 7 ms TrueTime 不确定性与区域 Paxos...
  c13_b05.md: ...--|---|---- Spanner | TrueTime（GPS + 原子钟） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性...
  c13_b13.md: ...提交之后。结果是全局线性化：跨越全球所有 Spanner 实例，时间戳形成与实时一致的全序。  7 ms commit-wait 在每次写上支付。成本是 TrueTime-based 线性化的代价，无需为时间戳做协调者往返。...
  c13_b14.md: ...用工作负载选择（如每个事件都递增的全局计数器）。比较两系统的预期延迟。  提示  Spanner：每次写 commit-wait（恒定 7 ms）。CockroachDB：争用时重启（可变成本）。  **答案：** 单行高争用是两者的最坏情况。...
  c13_b14.md: ...增获取行级锁，等待提交时间戳（7 ms），然后提交。单热行吞吐受每行提交串行化限制：若瓶颈 purely commit-wait，每秒最多约 140 次递增，实际因锁获取开销约 50-100 次。  _CockroachDB_ ：每次递增乐观地读行...
  c13_b16.md: ...me；若你在 AWS/Azure/多云且运维简单性重要，用 HLC。  更微妙考虑：TrueTime 的 commit-wait 是每次写 _无条件_ 支付；HLC 的重启成本在争用事件上 _有条件_ 支付。对低争用工作负载，HLC 均摊成本更低。...
  c28_b04.md: ...t_day_rollup。跨 shard、跨 region 事务，带 HLC 时间戳；为可串行化一致性做 commit-wait。95 ms（受第6章所说的最坏情况跨 region quorum 物理限制）。  (10) 事务的 coordinat...
  c30_b03.md: ...Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_...
  c30_b12.md: ...读副本足够时不要伸手选 Spanner。** Spanner 是最干净的多 region 系统，但它的 commit-wait 延迟每次写入都要支付。对单区域工作负载，Postgres 生态成熟度和每次写入延迟通常更好。只有真正需要其能力时才支付...

=== tigerbeetle (33 occurrences) ===
  c02_b14.md: ...的正确答案可能是 PostgreSQL + 读副本；Stripe 风格全球支付系统的正确答案可能是 Spanner 或 TigerBeetle。正确的推理路径要经过这些问题。没有推理路径的答案是一个团队无法辩护、无法扩展、在工作负载变化时无法调试的价值。  更深...
  c11_b06.md: ...Q3.** 用诊断方法给一个你没用过的系统产出一段阅读。可选：MongoDB、ScyllaDB、Aurora DSQL、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：** 示例 Aurora DSQL（A...
  c11_b08.md: ...#### 应用问题  **Q1.** 把诊断方法用于 TigerBeetle。充分阅读其文档以回答四个问题，并产出一页阅读。  提示  TigerBeetle 是面向确定性模拟测试和 TigerS...
  c11_b08.md: ...题  **Q1.** 把诊断方法用于 TigerBeetle。充分阅读其文档以回答四个问题，并产出一页阅读。  提示  TigerBeetle 是面向确定性模拟测试和 TigerStyle 编程范式的金融级账本数据库。找其复制协议、故障模型、一致性和分区行为。...
  c11_b08.md: ...测试和 TigerStyle 编程范式的金融级账本数据库。找其复制协议、故障模型、一致性和分区行为。  **答案：** TigerBeetle 阅读：  Question | Answer ---|--- Replication protocol | 自定义 V...
  c11_b08.md: ...专用于复式记账（非通用）；生态小 Right for | 银行账本、支付系统、金融 OLTP，且 schema 符合 TigerBeetle 的会计模型  TigerBeetle 作为领域特定设计的研究很有趣：通过限制数据模型（仅复式记账），系统可比通用数据库...
  c11_b08.md: ...ight for | 银行账本、支付系统、金融 OLTP，且 schema 符合 TigerBeetle 的会计模型  TigerBeetle 作为领域特定设计的研究很有趣：通过限制数据模型（仅复式记账），系统可比通用数据库快得多且更可验证。2026 年金融系统...
  c11_b08.md: ...系统可比通用数据库快得多且更可验证。2026 年金融系统中真实架构模式是：Crescent 的支付账本实际转账记录可用 TigerBeetle（schema 合适），而用户可见元数据仍用 CockroachDB。  **Q2.** 一个团队为新微服务评估 Mon...
  c13_b09.md: ...um LLM） | 无需补偿（只读） 3 | 提交银行结算 API | 调用银行 reversal API 4 | 在 TigerBeetle 账本记录 | 记入反向双分录 5 | 发送确认通知 | 发送 “交易已取消” 通知 6 | 更新商家分析聚合 | 减去...
  c13_b17.md: ...SQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Presto、Snowflake、BigQuery）。本章讲授如何用第11章的诊断方...
  c14_b01.md: ...QL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（Trino/Presto、Snowflake、BigQuery 的 Dremel...
  c14_b01.md: ...erless NewSQL。 * **Neon** ：serverless Postgres，存储与计算分离。 * **TigerBeetle** ：金融级账本，采用确定性模拟测试和 TigerStyle 规范（固定大小消息、显式状态机枚举）。  该类别已成熟到...
  c14_b03.md: ...环境、低容量 SaaS）并从 serverless 计费受益。不适合大规模高吞吐生产 OLTP。   ### 14.8 TigerBeetle  领域特定：专为金融复式记账优化的数据库。  关键属性：  * 5 副本 Viewstamped Replicatio...
  c14_b03.md: ...hema 范围内吞吐极高。 * 确定性执行；可复现调试。  劣势：  * Schema 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。Crescent 的实际转账记录可用 TigerBeetl...
  c14_b03.md: ...igerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。Crescent 的实际转账记录可用 TigerBeetle；商家目录和用户画像则用 CockroachDB 或其他通用存储。   ### 14.9 分布式查询处理器  与 New...
  c14_b05.md: ...B、Spanner（若在 GCP）、Aurora DSQL（若在 AWS）、TiDB（若用 MySQL） 金融账本 | TigerBeetle（账本）+ Postgres/CockroachDB（元数据） HTAP（混合 OLTP/OLAP） | TiDB，或...
  c14_b06.md: ...域（US-East、EU-West、US-West）以提供全球服务和灾备。强可串行化事务保证支付完整性。  (b) **TigerBeetle** 用于实际转账账本：每笔资金变动的复式记账。Crescent 财务团队把 TigerBeetle 账本作为会计真相来...
  c14_b06.md: ...性。  (b) **TigerBeetle** 用于实际转账账本：每笔资金变动的复式记账。Crescent 财务团队把 TigerBeetle 账本作为会计真相来源；CockroachDB 引用 TigerBeetle 转账 ID 但不重复余额状态。  (c)...
  c14_b06.md: ...动的复式记账。Crescent 财务团队把 TigerBeetle 账本作为会计真相来源；CockroachDB 引用 TigerBeetle 转账 ID 但不重复余额状态。  (c) **Snowflake** 用于分析：数据每晚从 CockroachDB 和...
  c14_b06.md: ...转账 ID 但不重复余额状态。  (c) **Snowflake** 用于分析：数据每晚从 CockroachDB 和 TigerBeetle 复制到 Snowflake。BI 团队出报表、监管方审计、ML 团队训练欺诈检测模型，都在 Snowflake 而不影...
  c14_b08.md: ...（若仅限 AWS 可接受，但生产记录较新）；单区域 Postgres（因缺乏多区域灾备被否）。  (b) **账本**：TigerBeetle。理由：金融级正确性；极高吞吐；显式错误状态让审计和对账容易。账本与运营存储通过应用耦合：处理支付时，运营存储记录元数据...
  c14_b08.md: ...）；强并发支持多团队（BI、财务、合规、ML）；时间旅行便于审计。数据通过 CDC 每晚从 CockroachDB 和 TigerBeetle 复制。  可选补充：  (d) **缓存**：Redis Cluster 用于热读路径（账户余额查询、幂等键检查）。多...
  c14_b08.md: ...earch 用于交易搜索（合规、客户支持、欺诈调查）。  架构分层：热路径（Redis + CockroachDB + TigerBeetle）处理每笔事务；温路径（Kafka + 下游服务）异步处理；冷路径（Snowflake）用于分析。每层都有适合其访问模式...
  c28_b02.md: ...achDB（每 region 12 节点，共 36）。存储账户、交易、商户数据。可串行化事务。  (d) **账本**：TigerBeetle（5 副本，多 region）。记录每笔资金流动。  (e) **协调器**：Stratus DB（现为 Crescen...
  c28_b02.md: ...缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region 异步（Kafka）、边缘缓存（Redis）。...
  c28_b03.md: ...d         subgraph Shared["Shared Services"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Cons...
  c28_b04.md: ...内返回分数（sub-100-ms p99，按 Tobias 的 SLO）；分数低于阈值；支付继续。  (8) 事务写入 TigerBeetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replic...
  c28_b04.md: ...O）；分数低于阈值；支付继续。  (8) 事务写入 TigerBeetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replication 在 30 ms 内提交。  (9) 事务在 Cockro...
  c30_b04.md: ...C | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...
  c30_b10.md: ...p99 延迟低于 500ms；跨大洲灾难恢复。  **决策**：CockroachDB 或 Spanner；财务账本用 TigerBeetle 补充。  * **Operational 存储**：CockroachDB（任意云）或 Spanner（仅限 GCP）...
  c30_b10.md: ...储**：CockroachDB（任意云）或 Spanner（仅限 GCP）。按云承诺选择。 * **账本**（如适用）：TigerBeetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Regio...
  c30_b10.md: ...择。 * **账本**（如适用）：TigerBeetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况 Raft quorum 约 90...
  c30_b10.md: ...**为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postg...

=== failover (33 occurrences) ===
  c05_b01.md: ...fter ack** ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Post-failover consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **Write lat...
  c05_b02.md: ...。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Avail...
  c05_b02.md: ...。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Wri...
  c05_b02.md: ...ngle source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 *...
  c05_b02.md: ...。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Avail...
  c05_b02.md: ...。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Wri...
  c05_b02.md: ...ngle source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 *...
  c05_b03.md: ...装一致答案。  用于：Dynamo（及其后代 Cassandra、Riak、ScyllaDB）。  关键性质是 _no failover required_ ：任意节点任何时候都能接受写，因此任一单节点死亡不会中断系统。代价是冲突必须在读时或后台修复时解决...
  c06_b02.md: ...m standby。读可去任何区域（可能过期）。  * **Latency** ：写只付区域内成本；读可本地。 * **Failover** ：切换到 standby 需数分钟；可能丢失部分写。 * **Right for** ：单区域可用性足够、DR 是...
  c06_b02.md: ...忍过期）或把请求代理到归属区域。  * **Latency** ：归属区域用户看到本地延迟；跨归属操作付全价。 * **Failover** ：per-region 故障切换；其他区域不受影响。 * **Right for** ：以用户为中心的工作负载（聊...
  c06_b02.md: ...个区域都能服务任何操作。写跨区域协调以保证全局一致。  * **Latency** ：每次写付跨区域往返成本。 * **Failover** ：区域可互换；丢失任一区域透明。 * **Right for** ：任何操作都可能触及任意数据的全局共享状态（罕见...
  c19_b06.md: ...息队列、缓存）：实例寿命长。故障转移很少（可能一月一次）。60 秒 TTL 意味着缓存 DNS 的客户端最多 60 秒 failover 延迟；对大多数工作负载可接受。  对无状态自动扩缩服务：实例快速增减。负载增加后数秒内出现新实例；负载降低后数秒内旧实...
  c27_b03.md: ...保护正确性关键操作。  现实：没有 fencing token 的 Redlock 在进程暂停、时钟漂移和 Redis failover 下失败。两个客户端可能都相信自己持有锁；两者都调用 `do_critical_work`；关键工作被污染。  修复：用...
  c27_b09.md: ...故障（时钟偏移、GC 暂停、重试以异常顺序到达）。(d) 不测试多副本场景（split-brain、partition、failover）。  分布式系统 bug 需要：  (a) 真实或带真实故障注入的模拟网络（Toxiproxy、Chaos Mesh，...
  c27_b09.md: ...单元测试遗漏的 bug：  * 网络抖动 + 重试下的重复扣款。 * 分区下的 split-brain。 * 领导者 failover 下的丢失写。 * 网络恢复后的 stale read。 * 负载下的级联故障。...
  c28_b10.md: ...adership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨...
  c28_b10.md: ...MA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/decode failover |  每个工作流都可作为独立小手册阅读：输入、机制、输出、陷阱。它们共同覆盖了 2026 年分布式系统工程的操作弧线。...
  c28_b14.md: ...法（第11章）_：  * **复制协议**：Postgres 逻辑复制；默认异步；Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；...
  c28_b14.md: ...ynchronous_commit = remote_apply` 收紧。  优势：成熟、文档丰富、SQL 强大。劣势：failover 不可用窗口；异步复制 RPO。  对 Crescent：Postgres + Patroni 不满足多 region...
  c30_b09.md: ...21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分区导致 MySQL Orchestrator failover 自动化在东海岸设施选出新主库，而西海岸原主库仍活着。 * **放大**：分区愈合后，系统有两个主库且数据分歧。恢复需要...
  c30_b09.md: ...据协调，部分提交永久丢失。 * **章节映射防御**：     * Ch.18（分布式锁，fencing token）：failover 自动化缺少 fencing token；新主库不应能在旧主库仍自认为主库时写入。     * Ch.7（共识，防止 s...
  c30_b09.md: ...共识，防止 split-brain）：基于共识的 leader 选举可防止双主状态。     * Ch.5（复制，主库 failover 纪律）：无 fencing 的 failover 模式正是第5章警告的。   #### E.6 — Google Cl...
  c30_b09.md: ...leader 选举可防止双主状态。     * Ch.5（复制，主库 failover 纪律）：无 fencing 的 failover 模式正是第5章警告的。   #### E.6 — Google Cloud BigQuery 存储故障，2023-08...
  c30_b09.md: ...致存储层开始拒绝特定表类的写入。 * **放大**：客户端应用重试；重试放大拒绝率；拒绝指标越过告警阈值，触发自动区域 failover，但 failover 不包含 bug 修复。 * **时间线**：受影响工作负载的 BigQuery 写入部分中断 4...
  c30_b09.md: ...类的写入。 * **放大**：客户端应用重试；重试放大拒绝率；拒绝指标越过告警阈值，触发自动区域 failover，但 failover 不包含 bug 修复。 * **时间线**：受影响工作负载的 BigQuery 写入部分中断 4 小时。 * **恢复...
  c30_b09.md: ...**：受影响工作负载的 BigQuery 写入部分中断 4 小时。 * **恢复**：部署 bug 修复；手动重置区域 failover 状态。 * **章节映射防御**：     * Ch.21（级联故障，重试风暴）：客户端层重试预算可限制放大。...
  c30_b09.md: ...Ch.21（级联故障，重试风暴）：客户端层重试预算可限制放大。     * Ch.22（灰色故障）："拒绝率飙升触发 failover" 循环是经典亚稳态模式；更好的 failover 纪律（跨区域 failover 需人工介入）有帮助。     * C...
  c30_b09.md: ...算可限制放大。     * Ch.22（灰色故障）："拒绝率飙升触发 failover" 循环是经典亚稳态模式；更好的 failover 纪律（跨区域 failover 需人工介入）有帮助。     * Ch.27（审查清单）：bug 修复应被审查 "跨区...
  c30_b09.md: ...需人工介入）有帮助。     * Ch.27（审查清单）：bug 修复应被审查 "跨区域回滚时会发生什么"；未覆盖到 failover-without-the-fix 场景。   #### E.7 — Fastly 全球 CDN 故障，2021-06-0...
  c30_b16.md: ...律是分类用例。 | Ch.18 | | 21 | "把缓存当锁用。" | 基于缓存的锁在进程暂停、时钟漂移、缓存驱逐和 failover 下失败。用共识 + fencing token。 | Ch.18 | | 22 | "我们用 service mesh...
  c30_b19.md: ...20 §20.2（因果保留）   同一事务在不同 region 出现多个协调器状态 span | 复制协调器；事务中途 failover | Ch.13 §13.2（2PC + 复制协调器）   Saga 补偿触发但未全部完成 | 补偿幂等性违反或 sag...
  c30_b19.md: ...| Ch.5 §5.8（写后读），Ch.15 §15.2（session 保证）   Trace 显示两次写入成功但 failover 后只持久化一次 | 异步复制；主库在传播前死亡 | Ch.5 §5.3，Ch.5 §5.4（同步 vs 异步）   分...
  c30_b19.md: ...4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Service mesh sidec...

=== writes (33 occurrences) ===
  c05_b03.md: ...合中少于 W 的节点（写者处于提交中途）。根据读修复逻辑，读可能返回旧值、混合值或新值。 * **Concurrent writes** ：两个写者可在相交集合上各自满足 W 要求，但写冲突值。冲突解决（LWW、sibling、CRDT）决定哪个“赢”...
  c05_b04.md: ...。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人...
  c05_b04.md: ...视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到比先前读 _更旧_ 的状...
  c05_b04.md: ...onic reads** ：后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writes-follow-reads** ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。...
  c05_b04.md: ...* ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。 * **Monotonic writes** ：你的写按你发出顺序应用。 * **Consistent prefix** ：你看到全局写顺序的一个前缀，即使没看...
  c05_b04.md: ...。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人...
  c05_b04.md: ...视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到比先前读 _更旧_ 的状...
  c05_b04.md: ...onic reads** ：后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writes-follow-reads** ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。...
  c05_b04.md: ...* ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。 * **Monotonic writes** ：你的写按你发出顺序应用。 * **Consistent prefix** ：你看到全局写顺序的一个前缀，即使没看...
  c05_b09.md: ...层封装可实现。或切换到原生支持的 CockroachDB/YugabyteDB。  (c) **把 read-your-writes 流量路由到主副本** ：若应用知道某键的“主”节点（Cassandra 有自然副本顺序；顺序中第一个常是写协调者），在...
  c05_b09.md: ...或类似协调者感知模式能做到。  通用教训：quorum 读/写规则给的是相交，不是线性一致性。需要 read-your-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-writes 伪代码...
  c05_b09.md: ...our-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max-HLC-observe...
  c05_b09.md: ...会话状态的一部分自动传播；应用无需显式跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c11_b02.md: ...READ；SERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 Mong...
  c11_b02.md: ...chDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。 * **Eventual** ：副本最终收敛...
  c15_b01.md: ...er、Petersen、Demers、Spreitzer、Hauser 1994）：  (a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读不会看到 _更旧_ 状态...
  c15_b01.md: ...otonic reads** ：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes-follow-reads** ：会话发出的任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X...
  c15_b01.md: ...任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X 之后。  (d) **Monotonic writes** ：会话的写按发出顺序应用。  (e) **Consistent prefix** ：即使看不到最新，也能看到全局写...
  c15_b01.md: ...er、Petersen、Demers、Spreitzer、Hauser 1994）：  (a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读不会看到 _更旧_ 状态...
  c15_b01.md: ...otonic reads** ：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes-follow-reads** ：会话发出的任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X...
  c15_b01.md: ...任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X 之后。  (d) **Monotonic writes** ：会话的写按发出顺序应用。  (e) **Consistent prefix** ：即使看不到最新，也能看到全局写...
  c15_b03.md: ...认读最终一致：读返回最近成功复制的值，但复制滞后可能意味着稍旧的值。区域内滞后通常亚秒。  对需要 read-your-writes 的操作，客户端可请求 `ConsistentRead=true`，对 leader 做同步读。代价：通常是最终读延迟的...
  c15_b03.md: ...每次操作选择一致性级别。许多 Cassandra 生产故障可追溯为使用错误的一致性级别（例如，需要 read-your-writes 时却用 ONE 读）。   #### Redis 复制  主从复制异步。从副本读可能过期。Master 上 ack 的...
  c15_b06.md: ...#### 概念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consis...
  c15_b06.md: ...种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**...
  c15_b06.md: ...d-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写...
  c15_b06.md: ...#### 概念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consis...
  c15_b06.md: ...种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**...
  c15_b06.md: ...d-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写...
  c17_b01.md: ...操作 B，则每个进程都先看到 A 再看到 B。并发操作可任意顺序。  为何有用：  * 会话内 **read-your-writes** 自然满足（自己的写 happens-before 后续读）。 * **因果性保持** ：对消息的回复排在该消息之后...
  c17_b04.md: ...我看到 A 的分数，然后刷新排行榜却没有它” 的失败。B 提交分数后刷新，可能读到没有 B 的副本。Read-your-writes 是因果一致的特例。  排行榜位于频谱中间。因果是正确模型。  **Q3.** 强最终与纯最终一致有何不同？  提示...
  c17_b07.md: ...D 同时发帖且未看到对方，观众可看到不同顺序。这没问题；没人关心同时发的帖子的精确顺序。  (c) Read-your-writes：用户发的消息应在下次查看会话时看到。  为何不用线性化：  * 线性化要求每条消息全局排序；聊天服务会在共识吞吐处瓶颈...
  c17_b07.md: ...共识。  为何不用纯最终：  * 用户可能发帖后下次查看却没看到（若读打到未收到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时携带其 HLC；服务按...

=== checkpoint (33 occurrences) ===
  c14_b13.md: ...无冲突意外的方式实现最终一致性的原则性方法。本章还涵盖 Chandy-Lamport 分布式快照，这是理解 Flink/checkpoint 系统的基础。  ---...
  c15_b08.md: ...ostgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flin...
  c15_b08.md: ...rt 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：...
  c15_b08.md: ...nce 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Chec...
  c15_b08.md: ...从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。...
  c15_b08.md: ...ink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 ch...
  c15_b08.md: ...nt 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 *...
  c15_b08.md: ...：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoi...
  c15_b08.md: ...eckpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...
  c15_b08.md: ...ostgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flin...
  c15_b08.md: ...rt 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：...
  c15_b08.md: ...nce 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Chec...
  c15_b08.md: ...从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。...
  c15_b08.md: ...ink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 ch...
  c15_b08.md: ...nt 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 *...
  c15_b08.md: ...：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoi...
  c15_b08.md: ...eckpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint...
  c15_b09.md: ...：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义...
  c15_b09.md: ...通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint...
  c15_b09.md: ...失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体...
  c15_b09.md: ...checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件...
  c15_b09.md: ...界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件到达与持久提交之间的时间）就是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作...
  c15_b09.md: ...rce 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件到达与持久提交之间的时间）就是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint...
  c15_b09.md: ...：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义...
  c15_b09.md: ...通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint...
  c15_b09.md: ...失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体...
  c15_b09.md: ...checkpoint 重启。  结果：端到端意义上每个事件恰好处理一次。Checkpoint 定义 “事务边界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件...
  c15_b09.md: ...界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件到达与持久提交之间的时间）就是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作...
  c15_b09.md: ...rce 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件到达与持久提交之间的时间）就是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照...
  c30_b11.md: ...g。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等价物。 * **为何这...
  c30_b19.md: ...21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6   LLM 推理 trace 显...

=== consistency (32 occurrences) ===
  c02_b04.md: ...时。健康情况下的权衡在延迟与一致性之间。  PACELC：若发生 Partition，则 Availability 或 Consistency；否则（Else），则 Latency 或 Consistency。  这个缩写很别扭，但观点尖锐。CockroachD...
  c02_b04.md: ...Partition，则 Availability 或 Consistency；否则（Else），则 Latency 或 Consistency。  这个缩写很别扭，但观点尖锐。CockroachDB 在分区期间是 CP（它拒绝分区侧的写入，而不是提供过期数据）。...
  c02_b08.md: ...a，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Availability vs Consistency；否则（Else），Latency vs Consistency。每个系统是 PA/EL、PA/EC、PC/EL、PC/...
  c02_b08.md: ...Partition，则 Availability vs Consistency；否则（Else），Latency vs Consistency。每个系统是 PA/EL、PA/EC、PC/EL、PC/EC 之一。  **答案：** PACELC：分区时选择可用性或...
  c05_b01.md: ...* ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Post-failover consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **Write latency** ：下界是最...
  c05_b02.md: ...。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Availability unde...
  c05_b02.md: ...**Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Write latency**...
  c05_b02.md: ...death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-write on fol...
  c09_b03.md: ...leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，R...
  c09_b03.md: ...leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，R...
  c09_b11.md: ...涵盖 Flexible Paxos（Heidi Howard 的重配置洞见），让运维可以调节固定法定人数协议无法提供的 consistency-availability 权衡。  ---...
  c11_b01.md: ...方法详述   #### 问题 1：复制协议  阅读系统文档中的 “replication”、“consensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL...
  c11_b03.md: ...s leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition behavior | CP——少数侧停止接受...
  c11_b03.md: ...间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partitio...
  c11_b04.md: ...der；跨区域全局表用异步复制 Failure-recovery | 透明（存储层对用户不透明）；自动扩缩容与自动修复 Consistency model | 默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件...
  c11_b04.md: ...L） Failure-recovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线性化 Partition behavio...
  c11_b04.md: ...）；强一致 Failure-recovery | 快（亚秒级 sequencer 切换）；自设计日起就以混沌工程为目标 Consistency model | Serializable 事务；ACID 强可串行化保证 Partition behavior | C...
  c11_b05.md: ...VCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model | Serializable 事务；follower 上有界过期读 Partition behavior...
  c11_b08.md: ...ilure-recovery | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Pa...
  c11_b09.md: ...ol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quo...
  c13_b01.md: ...点）的操作，必须整体满足 ACID：  * **Atomicity（原子性）** ：所有操作提交，或都不提交。 * **Consistency（一致性）** ：数据库从一个有效状态转换到另一个有效状态。 * **Isolation（隔离性）** ：并发事务看起来...
  c14_b07.md: ...ilure-recovery | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency model | Linearizable；提供过期读 | Serializable；提供 follower reads...
  c15_b03.md: ...用必须处理自己写入一个区域后被另一区域并发写覆盖的情况（罕见但可能）。   #### Cassandra  按操作可调。Consistency level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per...
  c15_b03.md: ...#### Cassandra  按操作可调。Consistency level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）...
  c15_b03.md: ...level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency level ALL：所有副本；只对特定一致性证明有用。  应用纪律：按需求为每次操作选择一致性级别。许多 Cassan...
  c17_b01.md: ...、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带一个依赖向量：写者本次写之前已观察到的写集合。副本在应用某写前会延迟，直到其所有依赖都已应用。  Lloy...
  c17_b11.md: ...### 本章总结  一致性格在 Linearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CRDT）提供确定性收敛。两者都比线性化便宜；两者都比纯最终更强。...
  c27_b11.md: ...ayment coordinator     # We use CockroachDB which is CP for consistency      def process_payment(payment):         # Acquire distri...
  c27_b12.md: ...后续重试会重复扣款。    6. **谎言 6（CAP 简单）**：注释 “CockroachDB is CP for consistency” 暗示读是线性化的；没有显式 `WITH (LINEARIZABLE)` 或类似，读可能来自有旧数据的 followe...
  c30_b01.md: ...ces." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP...
  c30_b02.md: ..., D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et a...
  c30_b06.md: ...价于共识。 * **BFT（拜占庭容错）**：容忍发送任意或恶意消息节点，而不仅是崩溃故障节点。 * **Causal consistency（因果一致性）**：每次操作的一致性模型，因果相关操作按因果顺序可见；并发操作可任意排序。 * **Cascading...

=== ack (32 occurrences) ===
  c02_b08.md: ...d) 使用 QUORUM 读/写的 Cassandra：对这些特定操作是 PC/EC。QUORUM 级别要求大多数副本 ack，因此分区时少数侧拒绝写入，无分区时支付法定人数延迟。使用 LOCAL_QUORUM 情况变化；使用 ONE 则变成 P...
  c04_b03.md: ...息的接收者在重广播前崩溃，消息仍会被送达。  简单实现：一个进程只有在确认多数进程已经 _收到_ 该消息后才向上层交付。ack 往返增加延迟，但确保如果消息消失（发送者崩溃、某个接收者崩溃），仍有存活的正确进程持有它并重广播。       on...
  c04_b03.md: ...for peer in all_processes:                 send(peer, ACK(msg))      on receive(ACK(msg), sender):         acks[msg]....
  c04_b10.md: ...2 在重广播前也崩溃。P3 和 P4 就收不到 m。可靠广播在此失败。一致可靠广播会通过要求 P2 等到收到多数进程的 ack 后才“向上交付”来防止；按此规则，P2 的本地交付不会发生，于是“要么全要么无”的 agreement 得以维持。...
  c04_b11.md: ...的重试逻辑不向恢复后的节点重发，X 恢复后永远不会得到它。Agreement 需要恢复后重新交付，通常通过发送者持久化 ack 跟踪或基于 tombstone 的 gossip 层实现。  (c) **跨发送者没有顺序。** 若两个发送者广播应排...
  c05_b01.md: ...入，把写发给每个副本，等待每个副本确认，然后才向客户端确认写入。  性质：  * **Durability after ack** ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Post-failover...
  c05_b02.md: ...，但不等待所有副本。确认过的副本保证拥有该写；其他副本异步接收。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已...
  c05_b02.md: ...** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Write latency** ：受最快 _合格_ 副本的往...
  c05_b02.md: ...er replica failure** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑第一个 ack。这比同步快、比异步持久。  MySQL 的 `rpl_semi_sync_master_wait_for_slave_...
  c05_b03.md: ...$ 模式相反：写总成功，读必须咨询所有人。  最常见的生产模式是 $N = 3, W = 2, R = 2$。两个节点 ack 写成功；两个节点响应读成功。$W + R = 4 > N = 3$，相交成立。单节点故障不阻塞读也不阻塞写。...
  c05_b04.md: ...之后，读如果命中相交节点会看见该写 _只要_ 没有其他并发写 _且_ 写者的提交已到达相交节点。线性一致性要求更多：写 ack 之后开始的读必须看见该写，即使存在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-ses...
  c05_b06.md: ...丢失以及 RPO 是什么。  **答案：** 团队会观察到：  (a) 发起写的应用收到成功响应，因为异步复制在传播前 ack。(b) 被提升的 follower 没有该写。(c) 任何观察过该写的客户端（read-your-writes 或下游...
  c05_b06.md: ...一致性要求更多：实时顺序和并发期间无异常。  **答案：** $W + R > N$：若写必须被至少 W 个 N 副本 ack，读必须被至少 R 个 N 副本响应，则任意读集合与任意写集合至少共享一个副本。读因此“看到”写（因为至少一个响应读的副...
  c05_b06.md: ...须被至少 R 个 N 副本响应，则任意读集合与任意写集合至少共享一个副本。读因此“看到”写（因为至少一个响应读的副本也 ack 了写）。  为什么不是线性一致性：  (a) **进行中的写被读** ：写还在进行（写者已联系部分副本但未满 W）时，...
  c05_b08.md: ...3, W = 2, R = 2$ 相交保证写者集合与读者集合至少有一个共同节点。但：  (a) 写可能在 2 个节点 ack 后就被确认，即使第三个尚未应用。若读者的 2 个节点是第三个（不在写者集合）和写者集合中一个离线的节点，读可能看不到写...
  c06_b06.md: ...需要多数。三节点时 quorum 是任意 2。计算每对往返下限。  **答案：** 三副本 Raft 需 2 of 3 ack。相关成对往返延迟：  * US-East 到 US-West：~80 ms。 * US-East 到 Frankfur...
  c06_b06.md: ...ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和 Frankfurt 中先响应者约 80-90 ms 往返。因此 US-East 发起的写约 85...
  c06_b08.md: ...地理聚类有帮助。  **答案：** 三个副本的 Raft：quorum 是任意 2 of 3。leader 自己算一个 ack；条目在 _最快_ follower 响应后提交。因此 US-East 发起的写，提交延迟是到其他两个区域中较快者的往返...
  c08_b01.md: ...or 有值，部分没有。没有就“选定”达成一致。  **朴素协议 2** ：指定一个 proposer 发送值；等待多数 ack。完成。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户...
  c08_b01.md: ...。  问题：proposer 可能在协议中途崩溃。剩余 acceptor 不知道 proposer 已提交（给客户端发 ack）还是中止。新 proposer 不知道应该提议新值还是重试旧值。  **朴素协议 3** ：任意 proposer 可...
  c09_b04.md: ...tle 的安全条件；没有它，协议不安全。   #### 常见误解  Raft 的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目...
  c10_b08.md: ...代价：leader 变更昂贵（需要 $N - 1$ 个副本可达才能 prepare）。收益：每次提交只需 2 个副本 ack，稳态提交延迟非常低。  5 节点集群：$Q_1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2...
  c10_b08.md: ...提交延迟非常低。  5 节点集群：$Q_1 = 4, Q_2 = 2$。提交在 leader + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  FoundationDB 和一些新系...
  c10_b10.md: ...t pipelining_ ：leader 可并行为多个日志条目发送 `AppendEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条...
  c11_b08.md: ...若主库在复制前崩溃可能丢失。 * _Majority 写_ ：`writeConcern: majority`。写在被 ack 前已持久化到多数；主库故障后存活。 * _默认读_ ：`readConcern: local` 从主库读。主库读线性化...
  c11_b08.md: ...condary` 时）。 * _Majority 读_ ：`readConcern: majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern: linearizable` 提供线性化读...
  c13_b14.md: ...当前提交阶段）。  (c) 新 leader 从停止处恢复 2PC 协议。若前任 leader 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发...
  c13_b14.md: ...r 已收到 PREPARE ack 但尚未发送 COMMIT，新 leader 发送 COMMIT。若前任尚未收到全部 ack，新 leader 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换具有鲁棒性：它们通过 Raf...
  c15_b03.md: ...rites 时却用 ONE 读）。   #### Redis 复制  主从复制异步。从副本读可能过期。Master 上 ack 的写尚未在副本上；故障切换可能丢失近期写。  对需要更强保证的应用，Redis 提供 `WAIT N` 命令，阻塞直到...
  c15_b03.md: ...本上；故障切换可能丢失近期写。  对需要更强保证的应用，Redis 提供 `WAIT N` 命令，阻塞直到 N 个副本 ack。这是 per-write 选择加入，非全局模式。...
  c15_b04.md: ...和 `min.insync.replicas` 配置控制持久性：当 `min.insync.replicas` 个副本 ack 后，partition 的写才算持久。若 leader 在传播前故障，写丢失。   ### 15.6 跨一致性模型的计...
  c19_b03.md: ...性化读。  (d) **配置漂移**：未能应用配置变更的服务（因 bug、网络故障）静默漂移出同步。解决：每个服务显式 ack，监控过期。...

=== causal (32 occurrences) ===
  c04_b03.md: ...upper_layer.deliver(deliverable.msg)   性质（在可靠广播之外）：  * **Causal order** ：若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1...
  c04_b05.md: ...性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op...
  c04_b05.md: ...e deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -...
  c04_b05.md: ...cast]         C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -->|"+ total order<br/>(consensus)"| E...
  c04_b05.md: ...v     Uniform reliable broadcast         |         + causal order (vector clocks)         v     Causal broadcast...
  c04_b05.md: ...|         + causal order (vector clocks)         v     Causal broadcast         |         + total order (consensus)...
  c04_b05.md: ...| 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器增量 | reliable | 顺序无关，只要交付  经济论证很重...
  c04_b05.md: ...性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op...
  c04_b05.md: ...e deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -...
  c04_b05.md: ...cast]         C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -->|"+ total order<br/>(consensus)"| E...
  c04_b05.md: ...v     Uniform reliable broadcast         |         + causal order (vector clocks)         v     Causal broadcast...
  c04_b05.md: ...|         + causal order (vector clocks)         v     Causal broadcast         |         + total order (consensus)...
  c04_b05.md: ...| 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器增量 | reliable | 顺序无关，只要交付  经济论证很重...
  c04_b07.md: ...，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。...
  c04_b07.md: ...le、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broad...
  c04_b07.md: ...ty_ ——若任何进程（正确或故障）交付，所有正确进程都交付。修复了单个接收者收到后重广播前崩溃的缺口。   4. **Causal broadcast** ：新增 _causal order_ ——若 broadcast(m_1) \to broad...
  c04_b07.md: ...，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。...
  c04_b07.md: ...le、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broad...
  c04_b07.md: ...ty_ ——若任何进程（正确或故障）交付，所有正确进程都交付。修复了单个接收者收到后重广播前崩溃的缺口。   4. **Causal broadcast** ：新增 _causal order_ ——若 broadcast(m_1) \to broad...
  c11_b02.md: ...REPEATABLE READ；SERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra...
  c17_b01.md: ...istent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **S...
  c17_b01.md: ...现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带一个依赖向量：写者本次写之前已观察到的写集合。副本在应用某写前会延迟，直到其所有...
  c17_b01.md: ...istent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **S...
  c17_b01.md: ...现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带一个依赖向量：写者本次写之前已观察到的写集合。副本在应用某写前会延迟，直到其所有...
  c17_b02.md: ...因果顺序看到操作），但某种意义上 _更强_（收敛状态唯一确定）。关系微妙；对大多数实践，基于 CRDT 的系统同时提供 causal+ 和强最终。   ### 17.4 计数器 / 排行榜 / 库存的最终处理  线索 B 在此结束。每个工作负载映射到正...
  c17_b05.md: ...果。一个系统可以强最终但不保持因果顺序（并发 op 确定合并而不保持时间顺序）。大多数生产 CRDT 系统同时提供两者：causal+（保持 happens-before）和 strong-eventual（确定性收敛）。两者结合给出有用保证：“我看到...
  c17_b10.md: ...MongoDB 的因果会话是首个广泛部署的实现；CockroachDB 的 HLC-based MVCC 副作用地提供 causal+。随着这些成熟，因果作为默认更容易采用。2030 年 “默认应该是什么” 的答案可能比 2026 年更偏向因果。  *...
  c17_b11.md: ...据库事务。   ### 本章总结  一致性格在 Linearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CRDT）提供确定性收敛。两者都比线性化便...
  c30_b02.md: ...ndersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Llo...
  c30_b06.md: ...消息的广播。等价于共识。 * **BFT（拜占庭容错）**：容忍发送任意或恶意消息节点，而不仅是崩溃故障节点。 * **Causal consistency（因果一致性）**：每次操作的一致性模型，因果相关操作按因果顺序可见；并发操作可任意排序。 *...
  c30_b14.md: ...able.py       — reliable broadcast via flooding     │   └── causal.py         — causal broadcast with vector clocks     ├── ch...
  c30_b14.md: ...able broadcast via flooding     │   └── causal.py         — causal broadcast with vector clocks     ├── ch09_raft/     │   ├──...

=== def (31 occurrences) ===
  c03_b01.md: ...一个 LLM 被要求写一个聊天消息排序函数，产出：       def display_in_order(messages):         return sorted(messages,...
  c03_b13.md: ...**答案：**       class LamportProcess:         def __init__(self, pid):             self.pid = pid...
  c03_b13.md: ...self.pid = pid             self.L = 0          def local_event(self, description):             self.L += 1...
  c03_b13.md: ...self.L += 1             return self.L, description          def send(self, dest, msg):             self.L += 1...
  c03_b13.md: ...est.receive(self.L, msg)             return self.L          def receive(self, sender_L, msg):             self.L = max(self...
  c04_b03.md: ...msg, V_msg, sender))         try_deliver_from_buffer()      def try_deliver_from_buffer():         while True:...
  c04_b09.md: ...收到并重广播，消息就会到达全部。  **答案：** 伪代码：       delivered = set()      def broadcast(msg):         flood(msg, sender=self)      def fl...
  c04_b09.md: ...def broadcast(msg):         flood(msg, sender=self)      def flood(msg, sender):         if msg in delivered:...
  c04_b09.md: ...!= self:                 send(peer, msg, sender=self)      def on_receive(msg, sender):         flood(msg, sender)   四节点示例...
  c05_b09.md: ...待，要么返回该 HLC 的数据，要么拒绝。  **答案：**       class Session:         def __init__(self):             self.max_hlc = (0, 0)...
  c05_b09.md: ...__init__(self):             self.max_hlc = (0, 0)          def write(self, key, value):             result = db.write(key,...
  c05_b09.md: ...elf.max_hlc, result.hlc)             return result          def read(self, key):             # 请求数据的最小 HLC 为 self.max_hlc。...
  c05_b09.md: ...t.hlc)             return result.value      # 服务器端读处理器：     def handle_read(key, min_hlc):         while server.local_hlc <...
  c18_b01.md: ...一个 LLM 被要求为领导者选举服务实现分布式锁，它产出：       def acquire_lock():         return redis.set("leader_lock", nod...
  c21_b01.md: ...一个 LLM 被要求处理分布式调用中的 “瞬态错误”，它产出：       def call_with_retry(url, max_retries=5):         for attempt in...
  c21_b09.md: ...koff=ExponentialBackoff(max=5))     @timeout(seconds=2)     def call_downstream():         return http.get(downstream_url)...
  c27_b02.md: ...本章每个 LLM 生成代码块都是说明性伪代码。重点是 _缺失的结构防御_，而非语法。  LLM 生成代码：       def submit_payment(payment):         result = payment_service.c...
  c27_b02.md: ...empotency key。服务端检查：我之前见过这个 key 吗？若见过，返回之前计算的结果。重试安全。       def submit_payment(payment):         payment.idempotency_key =...
  c27_b02.md: ...式。第二版需要识别简单模式遗漏的故障模式。   ### 27.3 谎言 2：网络可靠  LLM 生成代码：       def get_user_profile(user_id):         return profile_service.g...
  c27_b02.md: ...etry(max_retries=3, budget=0.1)     @timeout(seconds=2)     def get_user_profile(user_id):         try:             return...
  c27_b03.md: ...### 27.4 谎言 3：机器时钟说真话  LLM 生成代码：       def merge_events(events_from_replicas):         return sorted(e...
  c27_b03.md: ...偏移颠倒。  修复：用 happens-before、HLC 或 vector clock（第3章）排序。       def merge_events(events_from_replicas):         # 按 HLC 排序（假设事件...
  c27_b03.md: ...。HLC 版本需要理解时间的谎言。   ### 27.5 谎言 4：朴素分布式锁有效  LLM 生成代码：       def process_unique_action(action_id):         if redis.set(f"ac...
  c27_b03.md: ...nsensus-backed 锁（etcd、Consul）并在被保护资源上加 fencing token。       def process_unique_action(action_id):         token = etcd_lock...
  c27_b03.md: ...finally:             etcd_lock.release(token)      def do_critical_work(action_id, fencing_token):         # 存储层拒绝...
  c27_b04.md: ...### 27.6 谎言 5：部分故障罕见  LLM 生成代码：       def update_user_across_systems(user):         primary_db.update...
  c27_b04.md: ...：识别哪些调用对一致性关键，并用 saga 模式（第13章）或 transactional outbox。       def update_user_across_systems(user):         # 关键：DB 更新。失败即中止。...
  c27_b04.md: ...类简单  LLM 生成代码：       # 注释：我们用 CockroachDB，它是 CP，所以是一致的。     def get_account_balance(account_id):         return db.query("S...
  c27_b04.md: ...TIME` 或通过 follower-read 设置的读可能返回旧数据。  修复：每个操作显式一致性选择。       def get_account_balance_authoritative(account_id):         # 线性...
  c27_b04.md: ...LECT balance FROM accounts WHERE id = %s", account_id)      def get_account_balance_cached(account_id):         # 展示用途有界旧数据...
  c27_b11.md: ...r     # We use CockroachDB which is CP for consistency      def process_payment(payment):         # Acquire distributed loc...

=== flag (31 occurrences) ===
  c19_b06.md: ...key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag（`etcdctl put feature_flags/new_checkout_flow false`），每个服务收到...
  c19_b08.md: ...中断是运维痛点；实质降低它会明显改善用户体验。  **Q3.** 一个团队用 etcd 做 feature flags。flag `new_checkout_flow` 被切换为 `true`。走一遍每个服务会发生什么，以及可能出错的地方。  提示...
  c19_b09.md: ...的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：fl...
  c19_b09.md: ...常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始的请求继续使用旧值；之后...
  c19_b09.md: ...ag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**...
  c19_b09.md: ...(b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解...
  c19_b09.md: ...分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 fl...
  c19_b09.md: ...ag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag...
  c19_b09.md: ...w-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感...
  c19_b09.md: ...flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。...
  c19_b09.md: ...服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间...
  c19_b09.md: ...cd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓...
  c19_b09.md: ...on 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓解：服务在请求开始时快照 flag...
  c19_b09.md: ...flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revis...
  c19_b09.md: ...alse）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级...
  c19_b09.md: ...中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；上述边角情况由应用层纪律处理。...
  c19_b09.md: ...的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：fl...
  c19_b09.md: ...常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始的请求继续使用旧值；之后...
  c19_b09.md: ...ag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**...
  c19_b09.md: ...(b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解...
  c19_b09.md: ...分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看到不同 flag 值。若 flag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 fl...
  c19_b09.md: ...ag 效果跨服务（一个服务期望与另一 “new-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag...
  c19_b09.md: ...w-checkout” 版本通信），这种不一致会导致错误。缓解：跨服务功能 flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感...
  c19_b09.md: ...flag 的切换需显式协调。  (c) **Flag 应用慢**：服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。...
  c19_b09.md: ...服务收到 watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间...
  c19_b09.md: ...cd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓...
  c19_b09.md: ...on 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓解：服务在请求开始时快照 flag...
  c19_b09.md: ...flag 翻转可导致请求行为不一致（开始时 flag 为 true，执行中变 false）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revis...
  c19_b09.md: ...alse）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级...
  c19_b09.md: ...中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；上述边角情况由应用层纪律处理。...
  c23_b10.md: ...力 / GC 暂停**：节点活着但慢。专门故障。  (h) **配置变更**：部署、schema 变更、feature flag。本身不是故障，但与系统交互的运营事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整混沌工程学...

=== rdma (30 occurrences) ===
  c06_b03.md: ...应用层协调通常是。 * 跨机架与同机架 RPC 延迟相近（差几微秒）。网络局部性不如应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标...
  c06_b03.md: ...上，用 PFC（Priority Flow Control）保证无损交付。 * **iWARP** ：基于 TCP 的 RDMA；较少见，但因容忍有损网络而更容易部署。  性能：RDMA 延迟约 1-2 微秒（同数据中心），而 kernel-byp...
  c06_b03.md: ...）保证无损交付。 * **iWARP** ：基于 TCP 的 RDMA；较少见，但因容忍有损网络而更容易部署。  性能：RDMA 延迟约 1-2 微秒（同数据中心），而 kernel-bypass TCP 约 50-100 微秒。内核不参与其中；N...
  c06_b03.md: ...用途：  * **分布式内存数据库**（Microsoft FaRM，曾是 Azure Cosmos DB 的基础）用 RDMA 以类似内存的延迟做跨节点内存访问。 * **ML 分布式训练** 用 InfiniBand 或 RoCE 上的 RDM...
  c06_b03.md: ...DMA 以类似内存的延迟做跨节点内存访问。 * **ML 分布式训练** 用 InfiniBand 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 d...
  c06_b03.md: ...and 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损...
  c06_b03.md: ...LM 推理**（第24-26章）用 RDMA 在 prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）、应用必须使用 RDMA 原语（one-sided puts 和...
  c06_b03.md: ...e 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）、应用必须使用 RDMA 原语（one-sided puts 和 gets），且失败模式与 TCP 不同。对大多数应用层分布式系统，RDMA 过...
  c06_b03.md: ...用 RDMA 原语（one-sided puts 和 gets），且失败模式与 TCP 不同。对大多数应用层分布式系统，RDMA 过度；它存在于存储和 ML 层，那里的延迟提升 justify 运维成本。...
  c06_b06.md: ...ower 读（无需 quorum），让写保持 quorum 成本，但减少读的感知多区域延迟。  **Q4.** 为什么 RDMA 用于分布式 LLM 推理却很少用于应用层微服务？  提示  RDMA 的延迟收益在亚毫秒级有意义。应用服务延迟预算大得...
  c06_b06.md: ...少读的感知多区域延迟。  **Q4.** 为什么 RDMA 用于分布式 LLM 推理却很少用于应用层微服务？  提示  RDMA 的延迟收益在亚毫秒级有意义。应用服务延迟预算大得多。  **答案：** RDMA 把网络延迟从 ~50 微秒（kern...
  c06_b06.md: ...理却很少用于应用层微服务？  提示  RDMA 的延迟收益在亚毫秒级有意义。应用服务延迟预算大得多。  **答案：** RDMA 把网络延迟从 ~50 微秒（kernel-bypass TCP）降到 ~1-2 微秒（RoCE 上的 RDMA）。对总...
  c06_b06.md: ...：** RDMA 把网络延迟从 ~50 微秒（kernel-bypass TCP）降到 ~1-2 微秒（RoCE 上的 RDMA）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。...
  c06_b06.md: ...~1-2 微秒（RoCE 上的 RDMA）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都...
  c06_b06.md: ...s 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是 budget 内与超出的差别。高频率和紧预算让 RDMA 值得运维成本。  同样理由适用于 ML 训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫...
  c06_b07.md: ...**答案：** RDMA 把网络延迟从 ~50 微秒（kernel-bypass TCP）降到 ~1-2 微秒（RDMA over RoCE）。...
  c06_b07.md: ...1-2 微秒（RDMA over RoCE）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都...
  c06_b07.md: ...0-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是预算内与超出的差别。高频率和紧预算让 RDMA 值得运维成本。  同样理由适用于 ML 训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫...
  c06_b12.md: ...选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工作负载有用。对大多数应用层服务，网络不是瓶颈；kernel-...
  c25_b02.md: ...LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1 -->|...
  c25_b02.md: ...P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1         K2 --...
  c25_b02.md: ...传输：  * Prefill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：每个请求通常 1-10 GB（取决于 prompt...
  c25_b02.md: ...长度和模型）。 * 时间：~10-100 ms（通过 200 Gbit/s NVLink 或 InfiniBand 的 RDMA）。  KV cache 传输是新的运维关注点：它消耗网络带宽并增加首 token 时间。处理得当，整体吞吐收益占主导。...
  c25_b03.md: ...存层网络带宽成为瓶颈；每 token 的 KV cache 传输必须非常快（100+ tokens/sec 需亚毫秒）。RDMA + 精心预取是让这可行的技术。...
  c25_b05.md: ...**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) **LMCache**...
  c25_b05.md: ...*网络带宽**：每 token 的 KV cache 读取必须非常快（>100 tokens/sec 需亚毫秒）。需要 RDMA + 精心预取。  (b) **缓存层作为新故障模式**：缓存必须高可用；缓存故障影响所有读取 decode 节点。...
  c25_b06.md: ...上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 token 生成**：decode 节点上约 30 ms（一个 decode 步）...
  c25_b07.md: ...nk**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请求），跳过该部分 prefill。...
  c25_b11.md: ...bias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60% 到 85%；年节省 400-500 万美元。投入巨大；ROI 巨大。  延迟成本...
  c28_b10.md: ...载 | 相同或更好延迟下降低 30-50% 成本 | prefill/decode 分离 + KV cache 层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合 prefill/decode fa...

=== promise (30 occurrences) ===
  c08_b02.md: ...$b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复 `PROMISE(b, accepted_ballot, accepted_value)`，其中 `accepted_ballot` 和...
  c08_b02.md: ...ot` 和 `accepted_value` 是该 acceptor 最近 _接受_ 的值（可能为 null）。  该 promise 让 acceptor 承诺：“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept...
  c08_b02.md: ...“我不再接受任何 ballot 小于 b 的提议。”   #### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的...
  c08_b02.md: ...#### 第二阶段：Accept  Proposer 收集 `PROMISE` 响应。若收到多数 acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null...
  c08_b02.md: ...acceptor 的 promise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，proposer 必须取 `accepted_ballot...
  c08_b02.md: ...的 `accepted_value`，proposer 必须取 `accepted_ballot` _最高_ 的那个 promise 的值。 * 否则（所有 promise 的 `accepted_value` 都是 null），proposer 使用...
  c08_b05.md: ...cceptor 的 $b_{max}=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到 3 个 promise（多数）。所...
  c08_b05.md: ...并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到 3 个 promise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v = \text{"hello"...
  c08_b05.md: ...20)`。  **步骤 8** ：Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer...
  c08_b05.md: ..."hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提...
  c08_b06.md: ...，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺...
  c08_b06.md: ...的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收...
  c08_b06.md: ...cceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已...
  c08_b06.md: ...oposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promise 中见到这个已接受值，并被强制提议它。  两阶段合起来保证：一旦值被选定，所有后续 ballot 都会认同它。这就是 a...
  c08_b06.md: ...，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺...
  c08_b06.md: ...的 acceptor 回复 `PROMISE(b, accepted_ballot, accepted_value)`。Promise 是 acceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收...
  c08_b06.md: ...cceptor 的承诺：不再接受 ballot 小于 b 的未来提议。  第一阶段保证：若 proposer 收到多数 promise，则没有 ballot 小于 b 的未来提议能被选定。此外，proposer 现在知道该多数中每个 acceptor 已...
  c08_b06.md: ...oposer 收到多数 accept，值被 _选定_。由法定人数相交，任何后续 ballot 的第一阶段都会在该多数的 promise 中见到这个已接受值，并被强制提议它。  两阶段合起来保证：一旦值被选定，所有后续 ballot 都会认同它。这就是 a...
  c08_b07.md: ...acceptor 会接受。  每决定成本是一次往返（ACCEPT + ACCEPTED），而非两次（PREPARE + PROMISE + ACCEPT + ACCEPTED）。对服务多个决定的稳定 leader，摊销效果显著。  若领导不稳定（频繁重选...
  c08_b07.md: ...ue` 持久化到可靠存储。若 acceptor 丢失该状态（磁盘故障、漏 fsync、文件系统损坏），它会“忘记”先前 promise 并接受冲突值，违反 safety。  (b) **重配置** ：改变 acceptor 集合（增删节点）不能随意，因为...
  c08_b08.md: ...acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Acceptors 4、5 先收到 A 的；它们设置 $b_{max}=10$ 并回...
  c08_b08.md: ...null, null)`。Acceptors 4、5 先收到 A 的；它们设置 $b_{max}=10$ 并回复 A `PROMISE(10, null, null)`。随后它们收到 B 的，设置 $b_{max}=20$ 并回复 B `PROMISE(...
  c08_b08.md: ...`PROMISE(10, null, null)`。随后它们收到 B 的，设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。  A 的情况：A 只有 acceptors 4、5 的 2 个 promise，不...
  c08_b08.md: ...`PROMISE(20, null, null)`。  A 的情况：A 只有 acceptors 4、5 的 2 个 promise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot 20。  **B 的 accept...
  c08_b08.md: ...A 只有 acceptors 4、5 的 2 个 promise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot 20。  **B 的 accept：**  * B 发送 `ACCEPT(20, "Olamide")`...
  c08_b08.md: ...值 “Olamide” 被选定。  **A 的重试：**  * A 的 `prepare(10)` 失败（只有 2 个 promise）。A 选更高 ballot，例如 30。 * A 发送 `PREPARE(b=30)`。 * 所有 5 个 accep...
  c08_b08.md: ...`accepted_value = "Olamide"`（`accepted_ballot = 20`）。它们回复 `PROMISE(30, 20, "Olamide")`。 * A 收到多数 promise。根据 accept 规则，A 必须提议 “...
  c08_b08.md: ...d_ballot = 20`）。它们回复 `PROMISE(30, 20, "Olamide")`。 * A 收到多数 promise。根据 accept 规则，A 必须提议 “Olamide”（来自最高 accepted_ballot 的值）。 * A...
  c08_b08.md: ...到达多数方（3 个彼此可达的 follower）。这 3 个 acceptor 承诺。新 leader 拿到 3 个 promise（多数）。  新 leader 对飞行中日志槽跑第一阶段，学习任何部分提交的值，并跑第二阶段完成它们。  旧 leade...
  c08_b10.md: ..._ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot nu...

=== awareness (30 occurrences) ===
  c16_b08.md: ...lide 团队想添加 “实时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文...
  c16_b08.md: ...何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文档内容，必须在断连后保留）和 _awareness 状态_（临...
  c16_b08.md: ...，必须在断连后保留）和 _awareness 状态_（临时信息如光标位置、在线状态、选择，只对连接中的用户有意义）。  Awareness 协议：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发...
  c16_b08.md: ...临时信息如光标位置、在线状态、选择，只对连接中的用户有意义）。  Awareness 协议：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发布到 awareness 通道。  (b) aware...
  c16_b08.md: ...：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发布到 awareness 通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs...
  c16_b08.md: ...通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 aw...
  c16_b08.md: ...bSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后...
  c16_b08.md: ...ss 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.ge...
  c16_b08.md: ...端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalS...
  c16_b08.md: ...const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalStateField('cursor', { x: ..., y: ..., position: .....
  c16_b08.md: ...or', { x: ..., y: ..., position: ... })      // 渲染他人光标：     awareness.on('change', (changes) => {       for (const [clientId, sta...
  c16_b08.md: ...ange', (changes) => {       for (const [clientId, state] of awareness.getStates()) {         renderPresence(clientId, state)...
  c16_b08.md: ...{         renderPresence(clientId, state)       }     })   awareness 状态 _不是_ 严格意义上的 CRDT（不需要跨副本合并；每个客户端的状态是权威的）。它是临时信息的独立通道。  这种...
  c16_b08.md: ...是有意的设计：把光标位置存到文档 CRDT 会让文档因临时数据膨胀，并产生不想要的历史（每次光标移动都是一次文档编辑）。Awareness 协议是 “临时协作状态” 的正确架构。  **Q2.** 一个团队要构建协作文档表格。他们考虑��单元格值使用 CRD...
  c16_b08.md: ...lide 团队想添加 “实时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文...
  c16_b08.md: ...何用 Yjs 实现？  提示  光标位置不是持久状态；是 awareness 数据。Yjs 有独立于文档 CRDT 的 awareness 协议。  **答案：** Yjs 区分 _持久状态_（文档内容，必须在断连后保留）和 _awareness 状态_（临...
  c16_b08.md: ...，必须在断连后保留）和 _awareness 状态_（临时信息如光标位置、在线状态、选择，只对连接中的用户有意义）。  Awareness 协议：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发...
  c16_b08.md: ...临时信息如光标位置、在线状态、选择，只对连接中的用户有意义）。  Awareness 协议：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发布到 awareness 通道。  (b) aware...
  c16_b08.md: ...：  (a) 每个客户端将其 awareness 状态（一个 JSON 对象：光标位置、用户名、颜色、当前选择）发布到 awareness 通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs...
  c16_b08.md: ...通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 aw...
  c16_b08.md: ...bSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后...
  c16_b08.md: ...ss 对象中。  (c) 客户端根据收到的 awareness 状态渲染在线状态指示。  (d) 客户端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.ge...
  c16_b08.md: ...端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalS...
  c16_b08.md: ...const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalStateField('cursor', { x: ..., y: ..., position: .....
  c16_b08.md: ...or', { x: ..., y: ..., position: ... })      // 渲染他人光标：     awareness.on('change', (changes) => {       for (const [clientId, sta...
  c16_b08.md: ...ange', (changes) => {       for (const [clientId, state] of awareness.getStates()) {         renderPresence(clientId, state)...
  c16_b08.md: ...{         renderPresence(clientId, state)       }     })   awareness 状态 _不是_ 严格意义上的 CRDT（不需要跨副本合并；每个客户端的状态是权威的）。它是临时信息的独立通道。  这种...
  c16_b08.md: ...是有意的设计：把光标位置存到文档 CRDT 会让文档因临时数据膨胀，并产生不想要的历史（每次光标移动都是一次文档编辑）。Awareness 协议是 “临时协作状态” 的正确架构。  **Q2.** 一个团队要构建协作文档表格。他们考虑��单元格值使用 CRD...
  c16_b11.md: ...rkers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Softwa...
  c28_b10.md: ...客户端并发编辑，部分离线 | 确定性合并后的文档状态 | RGA 风格列表 CRDT + WebSocket 同步 + awareness 协议 | 在 CRDT 中存储临时光标位置；缺少定期压缩；分片逻辑与合并逻辑混淆 | | 10 | 级联故障事后分析...

=== tidb (29 occurrences) ===
  c02_b04.md: ...C，伴随 commit-wait 延迟 CockroachDB | C（拒绝少数侧写入） | C，伴随法定人数写入延迟 TiDB / YugabyteDB | C | C，伴随法定人数写入延迟 DynamoDB | A（可能提供过期读） | L（低...
  c03_b08.md: ...-wait | 每次写事务 7 ms + GPS 原子钟基础设施 CockroachDB / YugabyteDB / TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin...
  c03_b14.md: ...在没有 GPS 基础设施的情况下实现了 TrueTime 的大部分价值。CockroachDB、YugabyteDB、TiDB、MongoDB 都选择 HLC 并已规模化生产。经验证据表明 HLC 已足够好。  反驳：TrueTime 给 Spa...
  c05_b05.md: ...命令实现临时同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Raft-quorum  模式：现代分布...
  c05_b05.md: ...icated | 所有写都是 Raft-quorum  模式：现代分布式数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗...
  c10_b05.md: ...些 2026 年分布式数据库设计，以降低 per-commit 延迟而不牺牲 safety。CockroachDB 和 TiDB 的路线图中已有变体；FoundationDB 多年来一直可配置法定人数大小。   ### 10.7 非区块链 BFT...
  c11_b01.md: ...；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：CockroachDB、TiDB、YugabyteDB。每个分片（range、region、key range）有自己的共识组，通常是 Raft。 *...
  c11_b02.md: ...：每个操作看起来在调用与响应之间的某个瞬间原子生效，且符合实时顺序。Spanner、CockroachDB（事务默认）、TiDB。 * **Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、Ti...
  c11_b02.md: ...DB。 * **Serializable**（最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linear...
  c11_b03.md: ...络分区发生时，分区两侧分别做什么？  * **少数侧拒绝写**（分区下 CP）：Spanner、CockroachDB、TiDB、YugabyteDB。少数侧停止接受写；多数侧继续。 * **任意侧服务读、任意处接受写**（分区下 AP）：默认一致...
  c13_b01.md: ...替代）、TrueTime（Spanner 的硬件辅助时间戳）。本章最后介绍 2026 年的现实：CockroachDB、TiDB、YugabyteDB 部署在 NTP 偏移上界之上的 HLC-based MVCC。   ### 13.1 分布式事务...
  c13_b03.md: ...e Bigtable 的事务层（Bigtable 的 “单行事务” 是原子的；Percolator 增加了多行）。 * TiDB 使用 Percolator 派生协议（Percolator 是 TiDB 设计文档的引用）。 * 大多数 “键值存储上...
  c13_b03.md: ...的；Percolator 增加了多行）。 * TiDB 使用 Percolator 派生协议（Percolator 是 TiDB 设计文档的引用）。 * 大多数 “键值存储上的快照隔离” 设计都遵循此模式。  该协议的优雅之处在于：它无需存储层本身...
  c13_b05.md: ...achDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / Fa...
  c13_b05.md: ...clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到 CockroachDB / TiDB 采用此路径。   ### 13.6.5 Saga：跨服务的 2PC 替代方案  §13.2-§13.6 的一切都假设事...
  c13_b12.md: ...若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清理由读者摊销；无需单独恢复进程。  用于：TiDB（Percolator 派生），以及概念上大多数 KV 快照隔离设计。...
  c13_b17.md: ...ationDB 使用）通过共识复制协调者状态，消除经典阻塞。阻塞版 2PC 已基本过时。  CockroachDB 和 TiDB 使用 HLC 加可配置时钟偏移上界作为实用的 TrueTime 替代。成本可变（争用时重启）而非恒定（每次写 comm...
  c13_b17.md: ...展开。两种模式在现代应用中共存。  第14章结束第四部分：NewSQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Pr...
  c14_b01.md: ...题之一。答案取决于工作负载、部署范围和团队。本章讲授全景：NewSQL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（...
  c14_b01.md: ...er** ：开创者；仅限 GCP。 * **CockroachDB** ：开源 NewSQL；可跑在任何云上。 * **TiDB** ：另一开源 NewSQL；兼容 MySQL。 * **YugabyteDB** ：开源 NewSQL；兼容 Pos...
  c14_b02.md: ...生产示例：众多 fintech（DoorDash、Comcast 部分工作负载、若干大型银行）。   ### 14.4 TiDB  PingCAP 的 NewSQL；兼容 MySQL 线缆。  关键属性：  * TiKV 存储层（每 region/...
  c14_b02.md: ...* TiKV 存储层（每 region/tablet 使用 Raft；Percolator-based 事务）。 * TiDB 计算层（无状态 SQL 前端；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列...
  c14_b02.md: ...久经考验：微信、阿里巴巴等）。  劣势：  * 英语社区比 CockroachDB 小。 * 多组件架构复杂（PD + TiDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MyS...
  c14_b02.md: ...ckroachDB 小。 * 多组件架构复杂（PD + TiDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MySQL 工作负载。生产示例：许多中国科技公司、部分美国公司（Squ...
  c14_b05.md: ...多区域 OLTP | CockroachDB、Spanner（若在 GCP）、Aurora DSQL（若在 AWS）、TiDB（若用 MySQL） 金融账本 | TigerBeetle（账本）+ Postgres/CockroachDB（元数据）...
  c14_b05.md: ...rBeetle（账本）+ Postgres/CockroachDB（元数据） HTAP（混合 OLTP/OLAP） | TiDB，或 CockroachDB + 独立 OLAP 存储 纯 OLAP | Snowflake、BigQuery、数据湖上...
  c29_b01.md: ...是早期采用者；API 已可用但尚未被普遍消费。  2027-2030 轨迹：  * 预计 **CockroachDB、TiDB、YugabyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。...
  c30_b04.md: ...切换 | 可串行化 | CP | PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...
  c30_b10.md: ...分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DS...

=== broadcast (29 occurrences) ===
  c04_b03.md: ...### 4.4 一致可靠广播（Uniform Reliable Broadcast）  再上一层修复“发送者崩溃后只有一个接收者收到消息”缺口。一致可靠广播新增：_若任一进程（正确或已故障）交付了一条消...
  c04_b03.md: ...往返增加延迟，但确保如果消息消失（发送者崩溃、某个接收者崩溃），仍有存活的正确进程持有它并重广播。       on broadcast(msg):         for peer in all_processes:             send(p...
  c04_b03.md: ...程都交付它。  这就是防止普通可靠广播中“唯一接收者崩溃”失败模式的性质。   ### 4.5 因果广播（Causal Broadcast）  可靠广播不保序。因果广播新增：_若消息 m_1 在 happens-before 关系下先于 m_2 广播（即 b...
  c04_b03.md: ..._若消息 m_1 在 happens-before 关系下先于 m_2 广播（即 broadcast(m_1) \to broadcast(m_2)），则任何正确进程都不会在 m_1 之前交付 m_2。_  标准实现使用向量时钟（第3章）。每次广播携带发送者...
  c04_b03.md: ...交付消息，直到所有因果上先于它的消息都已交付。       # 每个进程维护 V[1..N]，初始全零。     on broadcast(msg):         V[self] += 1         send_to_all(msg, V)...
  c04_b03.md: ...iver(deliverable.msg)   性质（在可靠广播之外）：  * **Causal order** ：若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。  代价：每次广播携带一...
  c04_b03.md: ...g)   性质（在可靠广播之外）：  * **Causal order** ：若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。  代价：每次广播携带一个 N 项向量。与向量时钟一样，大 N...
  c04_b05.md: ...再选最弱的足够层。  广播层次作为分层架构：      graph TD         A["Best-effort broadcast<br/>(TCP + for-loop)"] -->|"+ agreement<br/>(re-broadcastin...
  c04_b05.md: ...loop)"] -->|"+ agreement<br/>(re-broadcasting)"| B[Reliable broadcast]         B -->|"+ uniformity<br/>(ack round before deliver)...
  c04_b05.md: ...formity<br/>(ack round before deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks)"| D[Caus...
  c04_b05.md: ...C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -->|"+ total order<br/>(consensus)"| E["Atomic b...
  c04_b05.md: ...t]         D -->|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9         styl...
  c04_b05.md: ...yle E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast       (TCP plus a for-loop)         |         + agreement (...
  c04_b05.md: ...reement (re-broadcasting on receipt)         v     Reliable broadcast         |         + uniformity (ack round before delivery)...
  c04_b05.md: ...(ack round before delivery)         v     Uniform reliable broadcast         |         + causal order (vector clocks)         v...
  c04_b05.md: ...+ causal order (vector clocks)         v     Causal broadcast         |         + total order (consensus)         v     A...
  c04_b05.md: ...|         + total order (consensus)         v     Atomic broadcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Cachin、Guerr...
  c04_b07.md: ...ausal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：validity（发送者正确、接收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增...
  c04_b07.md: ...收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增什么。   2. **Reliable broadcast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform rel...
  c04_b07.md: ..._agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform reliable broadcast** ：新增 _uniformity_ ——若任何进程（正确或故障）交付，所有正确进程都交付。修复了单个接收者收到后重广...
  c04_b07.md: ...任何进程（正确或故障）交付，所有正确进程都交付。修复了单个接收者收到后重广播前崩溃的缺口。   4. **Causal broadcast** ：新增 _causal order_ ——若 broadcast(m_1) \to broadcast(m_2)，...
  c04_b07.md: ...重广播前崩溃的缺口。   4. **Causal broadcast** ：新增 _causal order_ ——若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。   5. **Atom...
  c04_b07.md: ...ausal broadcast** ：新增 _causal order_ ——若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。   5. **Atomic broadcast** ：新增...
  c04_b07.md: ...1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。   5. **Atomic broadcast** ：新增 _total order_ ——所有正确进程以相同总顺序交付消息（且与 causal order 一致）。...
  c28_b12.md: ...ing token**：第18章（分布式锁）。 * **Raft**：第9章（实现友好的共识）。 * **Atomic broadcast**：第4章（广播层级）。 * **CRDT**：第16章（CRDTs in production）。 * **重试预算...
  c30_b06.md: ...40%（上下文判断、业务不变量、架构选择、新模式、考虑后保留的推理）。见第27章 §27.10。 * **Atomic broadcast（原子广播）**：所有正确进程按相同全序交付消息的广播。等价于共识。 * **BFT（拜占庭容错）**：容忍发送任意或恶...
  c30_b14.md: ...ch04_broadcast/     │   ├── best_effort.py    — best-effort broadcast (TCP + for-loop)     │   ├── reliable.py       — reliable b...
  c30_b14.md: ...t (TCP + for-loop)     │   ├── reliable.py       — reliable broadcast via flooding     │   └── causal.py         — causal broadca...
  c30_b14.md: ...oadcast via flooding     │   └── causal.py         — causal broadcast with vector clocks     ├── ch09_raft/     │   ├── raft_mini...

=== aurora (29 occurrences) ===
  c05_b05.md: ...致，可按请求强一致 Redis（master-replica）| Async | `WAIT N` 命令实现临时同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB...
  c05_b10.md: ...载。偶尔丢失可接受的日志流水线不需要 RPO = 0。写少读多的工作负载可用异步复制到读副本，不付 quorum 成本。Aurora 的设计（写走 6 副本 4-of-6 quorum 的存储层）是混合：写为持久性做 quorum 复制，但从副本读是异...
  c11_b01.md: ...** ：Cassandra、DynamoDB、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单 leader）；FoundationDB（sequencer 设计）。  协议决定写延迟...
  c11_b06.md: ...可避免过度付费。  **Q3.** 用诊断方法给一个你没用过的系统产出一段阅读。可选：MongoDB、ScyllaDB、Aurora DSQL、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：**...
  c11_b06.md: ...L、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：** 示例 Aurora DSQL（AWS，2024 GA）：...
  c11_b07.md: ...Aurora DSQL 是 AWS 的 serverless、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres...
  c13_b17.md: ...。  第14章结束第四部分：NewSQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Presto、Snowflake、Big...
  c14_b01.md: ...范围和团队。本章讲授全景：NewSQL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（Trino/Presto、Snowf...
  c14_b01.md: ...；兼容 MySQL。 * **YugabyteDB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：ser...
  c14_b03.md: ...### 14.6 Aurora DSQL  AWS 2024 GA 的分布式 SQL 数据库。架构与上述其他系统不同。  关键属性：  * 每租户 F...
  c14_b03.md: ...oachDB 或 Spanner）。 * 每租户 Firecracker 开销可能让习惯多租户数据库引擎的团队意外。  Aurora DSQL 适合：AWS 原生工作负载，需要多区域 Postgres 又不想管理 CockroachDB 集群。新兴示例...
  c14_b03.md: ...AWS 原生工作负载，需要多区域 Postgres 又不想管理 CockroachDB 集群。新兴示例：AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Serverless Pos...
  c14_b05.md: ...读副本，或单区域 CockroachDB 多区域 OLTP | CockroachDB、Spanner（若在 GCP）、Aurora DSQL（若在 AWS）、TiDB（若用 MySQL） 金融账本 | TigerBeetle（账本）+ Postgre...
  c14_b05.md: ...nowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | CockroachDB（任何云）、Snowflake（OLAP 任何云） 大规模水平扩展...
  c14_b08.md: ...；多区域以支持全球服务；可串行化事务保证支付完整性；可跑在任何云上。曾考虑替代：Spanner（因 GCP 锁定被否）；Aurora DSQL（若仅限 AWS 可接受，但生产记录较新）；单区域 Postgres（因缺乏多区域灾备被否）。  (b) **...
  c14_b09.md: ...#### 应用问题  **Q1.** 一个团队在 AWS 上运行 PostgreSQL，数据库已增长到 10 TB。单 Aurora Postgres 实例峰值 CPU 60%，延迟在下降。他们需要决定：手动分片、迁移到 Aurora DSQL，还是迁...
  c14_b09.md: ...10 TB。单 Aurora Postgres 实例峰值 CPU 60%，延迟在下降。他们需要决定：手动分片、迁移到 Aurora DSQL，还是迁移到 CockroachDB。走一遍权衡。  提示  手动分片：工作量最大、控制最大。Aurora D...
  c14_b09.md: ...到 Aurora DSQL，还是迁移到 CockroachDB。走一遍权衡。  提示  手动分片：工作量最大、控制最大。Aurora DSQL：托管、AWS 原生、较新。CockroachDB：托管或自托管、多云、成熟。  **答案：**  _选项 1...
  c14_b09.md: ...片感知查询、跨分片 join 变成应用层）、跨分片事务需要应用层 saga 或 2PC、运维复杂度倍增。  _选项 2：Aurora DSQL_。迁移到 AWS 托管 NewSQL。优点：serverless 计费（无空闲容量）、兼容 Postgres...
  c14_b09.md: ...roachDB Serverless 或自托管。优点：多云、成熟、Postgres 兼容、强可串行化事务。缺点：与现有 Aurora 分开运维、付费产品（企业功能需 license）。  推荐因素：  * 若团队可预见未来仍 commitment AW...
  c14_b09.md: ...（企业功能需 license）。  推荐因素：  * 若团队可预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：CockroachDB。 * 若团队增长...
  c14_b09.md: ...选性或 AWS commitment 不确定：CockroachDB。 * 若团队增长已放缓且希望最小架构改动：继续用 Aurora Postgres 并纵向扩展；推迟决策。  10 TB / 60% CPU 尚未临界（还可进一步纵向扩展）。关键问题是...
  c14_b12.md: ...靠。直接上 NewSQL 会增加前期成本，而收益可能永远不会兑现。  第二个考虑：serverless 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL 让你无需运维 NewSQL。...
  c14_b12.md: ...：serverless 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL 让你无需运维 NewSQL。两者都以不同的扩展特性提供 “托管简单性”。选择取决于具体工作负载的扩展轴。...
  c29_b01.md: ...26 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；API 已可用但尚未被普遍消费。  2027-2030 轨迹：  * 预计 **Cockroa...
  c29_b03.md: ...026 年系统仍在生产；2026 年协议仍是标准。通读。  * **2028**：部分具名系统可能已弃用或被收购/更名。Aurora DSQL 演进；vLLM 可能已被继任者替代。以 “它现在叫什么？” 的态度阅读；底层原语未变。用本书诊断方法（第11...
  c30_b04.md: ...级 sequencer 切换 | 可串行化 | CP | PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...
  c30_b10.md: ...ion 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aurora PostgreSQL 用于托管简单，但每 IOPS 计费更高。   #### F.2 — 面向全球用户的多区域 OLT...
  c30_b10.md: ...兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DSQL。...

=== leaf (29 occurrences) ===
  c06_b03.md: ...### 6.4 CLOS 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles Clos 命名，...
  c06_b03.md: ...为 _leaf-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 lea...
  c06_b03.md: ...eaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务...
  c06_b03.md: ...：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一...
  c06_b03.md: ...。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-pat...
  c06_b03.md: ...bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服...
  c06_b03.md: ...path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产...
  c06_b03.md: ...### 6.4 CLOS 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles Clos 命名，...
  c06_b03.md: ...为 _leaf-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 lea...
  c06_b03.md: ...eaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务...
  c06_b03.md: ...：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一...
  c06_b03.md: ...。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-pat...
  c06_b03.md: ...bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服...
  c06_b03.md: ...path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产...
  c06_b06.md: ...99，无论工程多强。承诺跨太平洋操作 30 ms p99 的团队在承诺不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三...
  c06_b06.md: ...团队在承诺不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答...
  c06_b06.md: ....** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine...
  c06_b06.md: ...e 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只...
  c06_b06.md: ...spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，...
  c06_b06.md: ...egation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负...
  c06_b06.md: ...），因为三层的 aggregation 交换机对东西向（服务器到服务器）流量成为瓶颈，而现代分布式工作负载以东西向为主；leaf-spine 提供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、...
  c06_b06.md: ...99，无论工程多强。承诺跨太平洋操作 30 ms p99 的团队在承诺不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三...
  c06_b06.md: ...团队在承诺不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答...
  c06_b06.md: ....** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine...
  c06_b06.md: ...e 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只...
  c06_b06.md: ...spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，...
  c06_b06.md: ...egation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负...
  c06_b06.md: ...），因为三层的 aggregation 交换机对东西向（服务器到服务器）流量成为瓶颈，而现代分布式工作负载以东西向为主；leaf-spine 提供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、...
  c06_b12.md: ...titioned、globally consistent）成本画像不同；选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和...

=== linearizable (29 occurrences) ===
  c11_b02.md: ...troni 切换的停机画像不同。   #### 问题 3：一致性模型  系统对单个操作保证什么？模型可能是：  * **Linearizable**（最强单操作属性）：每个操作看起来在调用与响应之间的某个瞬间原子生效，且符合实时顺序。Spanner、Cockroa...
  c11_b02.md: ...最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot i...
  c11_b02.md: ...）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到一致快照，但不防止写-写冲突（写偏可能...
  c11_b02.md: ...troni 切换的停机画像不同。   #### 问题 3：一致性模型  系统对单个操作保证什么？模型可能是：  * **Linearizable**（最强单操作属性）：每个操作看起来在调用与响应之间的某个瞬间原子生效，且符合实时顺序。Spanner、Cockroa...
  c11_b02.md: ...最强事务属性）：事务等价于某种串行顺序。CockroachDB（事务级）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot i...
  c11_b02.md: ...）、TiDB。注意：serializable 与 linearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到一致快照，但不防止写-写冲突（写偏可能...
  c11_b06.md: ...uorum、应用控制）  回答这四个问题可得到任何系统的 PACELC 画像、运维特征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operat...
  c11_b06.md: ...征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。...
  c11_b06.md: ...per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作...
  c11_b06.md: ...able 性质允许 B 读到 $x$ 的旧值（仿佛 B 在串行等价中排在 A 前），只要整体执行等价于某种串行顺序。  Linearizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 seriali...
  c11_b06.md: ...等价于某种串行顺序。  Linearizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 serializable 付更多（每次读走共识、commit-wait 或 HLC restart）。许多工作...
  c11_b06.md: ...uorum、应用控制）  回答这四个问题可得到任何系统的 PACELC 画像、运维特征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operat...
  c11_b06.md: ...征和失败模式。  **Q2.** “linearizable” 与 “serializable” 有何不同？  提示  Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。...
  c11_b06.md: ...per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看起来在调用与响应之间的某个瞬间生效，且符合实时顺序。若操作 A 在操作...
  c11_b06.md: ...able 性质允许 B 读到 $x$ 的旧值（仿佛 B 在串行等价中排在 A 前），只要整体执行等价于某种串行顺序。  Linearizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 seriali...
  c11_b06.md: ...等价于某种串行顺序。  Linearizable 禁止：B 在 A 完成后开始，因此 B 必须看到 A 的写。  成本：linearizable 系统比 serializable 付更多（每次读走共识、commit-wait 或 HLC restart）。许多工作...
  c11_b08.md: ...演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数 PACELC | PC / EC；...
  c11_b08.md: ...majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern: linearizable` 提供线性化读。自 MongoDB 3.4 起可用。  分片集群：每个分片是一个副本集；上述按分片适用。跨分片事务可用...
  c11_b08.md: ...演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数 PACELC | PC / EC；...
  c11_b08.md: ...majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern: linearizable` 提供线性化读。自 MongoDB 3.4 起可用。  分片集群：每个分片是一个副本集；上述按分片适用。跨分片事务可用...
  c11_b11.md: ...生态、运维经验。这些商业因素叠加在技术阅读之上；单独任何一层都不充分。  阅读文档的常见陷阱：营销级“一致性”声明混淆 linearizable 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 b...
  c14_b07.md: ...os leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency model | Linearizable；提供过期读 | Serializable；提供 follower reads Partition behavior |...
  c17_b01.md: ...致性模型形成偏序：若模型 A 允许的每个行为也都被模型 B 允许，则 A 强于 B。  从强到弱的经典层级：  * **Linearizable** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：pe...
  c17_b11.md: ...的工程反射：当被要求 “实现计数器” 时，先问吞吐。若吞吐高，答案不是数据库事务。   ### 本章总结  一致性格在 Linearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eve...
  c27_b09.md: ...说 “我们用 CP 系统” 或 “我们用 AP 系统”，而不理解 per-operation 一致性选择、PACELC、linearizable vs serializable。  LLM 默认产出嵌入这些谎言的代码，因为训练数据过度代表简单模式（单机假设），而不...
  c30_b06.md: ...（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **Linearizable（线性化）**：每次操作看起来在调用与响应之间某个瞬间生效，与实时一致。 * **LSM-tree（日志结构合并树）**...
  c30_b16.md: ...、可用性、分区容错中选两个。" | 分区容错不是选择；分区下在一致性与可用性之间选择。 | Ch.2 | | 4 | "Linearizable = serializable。" | Linearizable 是每次操作的实时顺序；serializable 是每次...
  c30_b16.md: ...致性与可用性之间选择。 | Ch.2 | | 4 | "Linearizable = serializable。" | Linearizable 是每次操作的实时顺序；serializable 是每次事务的任意顺序。Linearizable 严格更强。 | Ch....
  c30_b16.md: ...lizable。" | Linearizable 是每次操作的实时顺序；serializable 是每次事务的任意顺序。Linearizable 严格更强。 | Ch.2, Ch.11 | | 5 | "我们用 CP 系统，所以一致。" | CP 命名分区行为；P...

=== tobias (28 occurrences) ===
  c01_b03.md: ...amide 运行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行...
  c01_b08.md: ...章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuki 和 Tobias 最终在第28章一起交付的系统。第1章到第28章之间的路径是本书正文；第29章则前瞻这个系统将如何在 2027-2030...
  c23_b13.md: ...4章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  ---...
  c24_b01.md: ...Tobias Schwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户...
  c24_b01.md: ...介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NVIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。   ### 24.1 为什么分布式 LLM 推理不同  单机上推理大模型（L...
  c24_b04.md: ...（目标：80%+）。 * 成本：每月 120 万美元，包括 GPU 租赁、网络、存储和工程运维。  60% 利用率就是 Tobias 优化工作的空间。每个百分点的利用率约等于每月 2 万美元节省。接下来两章讲解能起作用的技术（disaggregated...
  c24_b07.md: ...请求重复）、agent 框架（agent scaffold 共享）、结构化输出应用（函数调用、JSON 生成）。  对 Tobias 的 Continuum 集群：答案取决于每客户工作负载。一些客户是任意聊天工作负载（vLLM 合适）；一些是重结构化输...
  c24_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群 GPU 利用率为 60%。找出三种提高利用率的技术及预期影响。  提示  Continuous batchin...
  c24_b08.md: ...decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提...
  c24_b08.md: ...带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching；若 Tobias 未用现代引擎，这是第一步。预期利用率提升：从 60% 到 80%+。  (b) **前缀共享（SGLang）**：对具...
  c24_b08.md: ...t scaffold），SGLang 的 RadixAttention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。...
  c24_b11.md: ...处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：50 节点、400 GPU、每月 120 万美元、60% 利用率。优化空间巨大；每个百分...
  c25_b01.md: ...Tobias 的集群 GPU 利用率为 60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：...
  c25_b04.md: ...### 25.5 工作流 12：Continuum 的分离式部署  Tobias 的 80% 利用率目标计划：  **架构**：  * 30 个 prefill 节点（每个 4x H100 = 120...
  c25_b06.md: ...成本相对 decode 小）。30 节点但重长 prompt 工作负载的集群可能受益（prefill 成本是瓶颈）。  Tobias 的 Continuum 集群（50 节点，混合工作负载）处于阈值。6 个月分离投资值得；更小集群可能不值得。  **Q...
  c25_b07.md: ...快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请求），跳过该部分 prefill。  Tobias 的部署使用 (a) 和 (c)；典型首 token 延迟约 150 ms，接近 co-located 性能。...
  c25_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout...
  c25_b11.md: ...1M+ token）必需；实现跨请求前缀复用。Mooncake（商业）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点 + 10 cache 节点；高带宽 RDMA...
  c26_b01.md: ...ative decoding（用小 draft model 加速生成；可作为跨机分布式协议实现）。  本章结束第八部分。Tobias 的 Continuum 集群完整优化故事至此完成：disaggregation（第25章）、KV cache shar...
  c26_b04.md: ...est 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ### 26.7 Tobias 的完整优化  Continuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregat...
  c26_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system...
  c26_b08.md: ...（前缀复用），延迟显著改善。Continuum 能给出有竞争力的 per-request 定价；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优化让 Continuum 能与 Anysca...
  c26_b11.md: ...。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware...
  c27_b15.md: ...可预测缺口。对 AI 审查 PR 橡皮图章是被点名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模型是贯穿线。  ---...
  c28_b01.md: ...Crescent 的 commit-log 层）。Yuki 从 Glide 借调来负责协作文档编辑组件（商户入驻流程）。Tobias 从 Continuum Inference 借调来负责 AI 驱动的欺诈检测层。  他们交付的系统是全书的综合。每个原...
  c28_b07.md: ...’，因为 CRDT 确定性地处理冲突。与 Crescent 的认证和审计追踪系统集成是难点；编辑器本身很容易。”  **Tobias（从 Continuum 借调）**：“欺诈检测推理是 sub-100-ms p99（p50 80 ms；与 §28.2...
  c28_b13.md: ...。第24-26章开发专门原语。  (c) **同样纪律**：推理工程使用同样的诊断方法、同样的审查清单、同样的混沌工程。Tobias 的优化推动就是把本书纪律应用于特定工作负载。  (d) **前瞻性**：LLM 推理是成长领域。2026 年进入该领域...
  c29_b04.md: ...更晚读这本书。具体年份影响哪些具名系统仍 current。它不影响心智模型。  Olamide、Anya、Yuki 和 Tobias 是复合人物；他们的系统和事件是作者工作和评审过的生产架构的真实组合。平台设计（分层架构、每个组件的诊断、混沌纪律、六谎...

=== yugabytedb (28 occurrences) ===
  c02_b04.md: ...mmit-wait 延迟 CockroachDB | C（拒绝少数侧写入） | C，伴随法定人数写入延迟 TiDB / YugabyteDB | C | C，伴随法定人数写入延迟 DynamoDB | A（可能提供过期读） | L（低延迟读，默认最终一致） C...
  c03_b06.md: ...。 * **贴近 wall-clock 时间** ：对人类调试、日志关联和推理持续时间有用。  CockroachDB、YugabyteDB、MongoDB（自 3.6 起）等都用 HLC 作为事务时间戳基础。HLC 回答了“如何在每个数据中心没有 GPS 原...
  c03_b08.md: ...Time + commit-wait | 每次写事务 7 ms + GPS 原子钟基础设施 CockroachDB / YugabyteDB / TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内）...
  c03_b14.md: ...论：** 辩护：HLC 在没有 GPS 基础设施的情况下实现了 TrueTime 的大部分价值。CockroachDB、YugabyteDB、TiDB、MongoDB 都选择 HLC 并已规模化生产。经验证据表明 HLC 已足够好。  反驳：TrueTime...
  c03_b15.md: ...性差。混合逻辑时钟（HLC）结合 wall-clock 风格与逻辑计数器排序，保持恒定大小，是 CockroachDB、YugabyteDB、MongoDB 等 2026 系统的现代默认。  TrueTime 走硬件路线：每个数据中心 GPS 加原子钟，向应用...
  c05_b04.md: ...= 12345”。落后的 follower 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB 和 YugabyteDB 通过 per-transaction HLC 时间戳使用 (c) 的变体。会话跟踪见过的最大 HLC；同一会话的后续读...
  c05_b05.md: ...同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Raft-quorum  模式：现代分布式数据库（Cockroac...
  c05_b05.md: ...d | 所有写都是 Raft-quorum  模式：现代分布式数据库（CockroachDB、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的...
  c05_b09.md: ...绝返回早于写时间戳的数据。Cassandra 原生不支持；应用层封装可实现。或切换到原生支持的 CockroachDB/YugabyteDB。  (c) **把 read-your-writes 流量路由到主副本** ：若应用知道某键的“主”节点（Cassan...
  c05_b09.md: ...回 “stale replica” 错误）。拒绝后客户端要么重试不同副本，要么重试主库。  CockroachDB 和 YugabyteDB 使用这种模式的变体。HLC 作为 SQL 连接会话状态的一部分自动传播；应用无需显式跟踪。  代价：慢读路径上的等待或...
  c09_b09.md: ...多 Raft 组并行，每组处理一个分片。这结合了每组内 Raft 的简单性与水平扩展。CockroachDB、TiKV、YugabyteDB 都用此模式生产。...
  c09_b11.md: ...维工具。Multi-Raft 模式（多 Raft 组并行，每组一个分片）恢复水平扩展。CockroachDB、TiKV、YugabyteDB 等都在生产中用此模式。  第10章迈出崩溃故障共识的下一步：若某些节点撒谎怎么办？拜占庭容错（HotStuff、Nar...
  c11_b01.md: ...ower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：CockroachDB、TiDB、YugabyteDB。每个分片（range、region、key range）有自己的共识组，通常是 Raft。 * **Per-table...
  c11_b03.md: ...时，分区两侧分别做什么？  * **少数侧拒绝写**（分区下 CP）：Spanner、CockroachDB、TiDB、YugabyteDB。少数侧停止接受写；多数侧继续。 * **任意侧服务读、任意处接受写**（分区下 AP）：默认一致性下的 Cassand...
  c13_b01.md: ...rueTime（Spanner 的硬件辅助时间戳）。本章最后介绍 2026 年的现实：CockroachDB、TiDB、YugabyteDB 部署在 NTP 偏移上界之上的 HLC-based MVCC。   ### 13.1 分布式事务问题  _分布式事务_...
  c13_b05.md: ...### 13.6 CockroachDB 的 HLC-Based MVCC  CockroachDB（以及 TiDB、YugabyteDB）选择了一条不需要 TrueTime 基础设施的路径：HLC（见第3章）加上可配置的最大时钟偏移上界。  协议：...
  c13_b05.md: ...HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Seque...
  c13_b17.md: ...种模式在现代应用中共存。  第14章结束第四部分：NewSQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Presto、Snowfl...
  c14_b01.md: ...案取决于工作负载、部署范围和团队。本章讲授全景：NewSQL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（Trino/Prest...
  c14_b01.md: ...* ：开源 NewSQL；可跑在任何云上。 * **TiDB** ：另一开源 NewSQL；兼容 MySQL。 * **YugabyteDB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AW...
  c14_b02.md: ...能力的 MySQL 工作负载。生产示例：许多中国科技公司、部分美国公司（Square 历史上）。   ### 14.5 YugabyteDB  兼容 Postgres 的 NewSQL；CockroachDB 的近亲。  关键属性：  * 每分片使用 Raft...
  c14_b02.md: ...track record 比 CockroachDB 小。 * 多区域配置复杂（与 CockroachDB 类似）。  YugabyteDB 和 CockroachDB 是画像相似的竞争者。选择常归结为具体功能需求和生态偏好。...
  c15_b02.md: ...LC。服务器要么等待、返回该 HLC 的数据，要么以 “换副本重试” 拒绝。  CockroachDB、MongoDB、YugabyteDB 都实现了变体。Cassandra 和 DynamoDB 历史上不自动提供会话保证；应用代码必须通过 “本会话路由到主库...
  c15_b11.md: ...ent prefix）定义了完整线性化的有用 per-session 弱化版本。CockroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话跟踪提供它们。  Chandy-Lamport 分布式快照是在无全局时钟下捕获一致全局状...
  c17_b08.md: ...**切换到具有 HLC-aware 多区域语义的系统**：CockroachDB 的 follower reads 或 YugabyteDB 的类似特性；读可从更近副本做有界过期，无需因果会话等待。  对 MongoDB，(a) 和 (b) 是常见模式。因果会...
  c29_b01.md: ...用者；API 已可用但尚未被普遍消费。  2027-2030 轨迹：  * 预计 **CockroachDB、TiDB、YugabyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure**...
  c30_b04.md: ...可串行化 | CP | PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...
  c30_b10.md: ...型是财务完整性的正确工具。 * **替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DSQL。...

=== termination (28 occurrences) ===
  c04_b07.md: ...确进程交付相同的第一个广播，因此决定相同值。这满足 agreement（都决定相同）、validity（值来自提议）和 termination（假设广播层可终止）。  _共识 → 原子广播_ 。假设有一个决定单值的共识原语。要实现原子广播，每个消息槽运行一次共识...
  c07_b01.md: ...”。本章其余部分讨论这些更强假设是什么、为什么部分同步是现代逃生舱，以及三条性质（agreement、validity、termination）对任何协议的要求。   ### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一个值，并最终 _决定_...
  c07_b01.md: ...*Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。不同变体会微调（例如 uniform agreement 要求即...
  c07_b01.md: ...iform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程间消息按发送顺序到达），但时序 _异步_...
  c07_b01.md: ...”。本章其余部分讨论这些更强假设是什么、为什么部分同步是现代逃生舱，以及三条性质（agreement、validity、termination）对任何协议的要求。   ### 7.1 问题精确定义  共识问题是：一组进程各自 _提议_ 一个值，并最终 _决定_...
  c07_b01.md: ...*Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  这三条性质是经典陈述。不同变体会微调（例如 uniform agreement 要求即...
  c07_b01.md: ...iform agreement 要求即使已故障的进程若决定也必须决定相同值），但 agreement-validity-termination 是标准。  进程通过点对点消息通信。通信可靠（消息不丢失）且有序（同一对进程间消息按发送顺序到达），但时序 _异步_...
  c07_b02.md: ...陈述：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能同时以 agreement、validity 和 termination 性质解决共识。  证明微妙，但直觉可接近。想象系统处于某状态 s。调度器可选择下一条交付的消息。证明构造了一条无限调度...
  c07_b03.md: ...性质被 _有限_ 执行违反：单个坏事件就够了。  **Liveness**：说“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服...
  c07_b03.md: ...终止）。网络差时集群不能进展；最坏情况是不可用，而非不一致。   ### 7.6 Agreement-Validity-Termination 三重奏  共识三条性质各有不同“形状”。理解每条是理解协议为何如此设计的钥匙。   #### Agreement  所...
  c07_b04.md: ...议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为...
  c07_b04.md: ...（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发送过的值。   #### Termination  所有正确进程最终决定。挑战：在纯异步下，正确进程无法判断另一进程是崩溃还是只是慢。如果协议等所有人，可能永远等下去（...
  c07_b04.md: ...议的“决定值”来自提议者的广播而非其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为...
  c07_b04.md: ...（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议”任何值，包括它从未发送过的值。   #### Termination  所有正确进程最终决定。挑战：在纯异步下，正确进程无法判断另一进程是崩溃还是只是慢。如果协议等所有人，可能永远等下去（...
  c07_b05.md: ...现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广播 | 稳定 leader（...
  c07_b05.md: ...四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有用。几乎每种“新共识协议”...
  c07_b05.md: ...现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos | 被选定值的多数法定人数 | 值来自提议者广播 | 稳定 leader（...
  c07_b05.md: ...四个协议中 agreement 都由法定人数相交提供。validity 是结构性的（值来自指定提议者或 leader）。termination 由某种 leader 选举加超时提供，超时即隐式故障检测器。  这种分解对阅读新共识论文很有用。几乎每种“新共识协议”...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreem...
  c07_b06.md: ...*Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  进程通过点对点消息通信。协议必须能容忍最多 f 个崩溃故障（f 是协议参数）。  **...
  c07_b06.md: ...LP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满足更强假设（部分同步、随机化或故障检测器）...
  c07_b06.md: ...#### 概念检测  **Q1.** 精确陈述共识问题，包括三条性质。  提示  Agreement、validity、termination。每条都有精确含义。  **答案：** 一组进程各提议一个值。共识问题要求协议决定单个值，使得：  * **Agreem...
  c07_b06.md: ...*Agreement** ：所有正确进程决定相同值。 * **Validity** ：决定值曾被某个进程提议。 * **Termination** ：所有正确进程最终决定。  进程通过点对点消息通信。协议必须能容忍最多 f 个崩溃故障（f 是协议参数）。  **...
  c07_b06.md: ...LP 不可能性：在即使只可能发生一次崩溃故障的异步系统中，没有任何确定性协议能以 agreement、validity、termination 全部三条性质解决共识。  它没说什么：  (a) 共识不可能。生产系统通过满足更强假设（部分同步、随机化或故障检测器）...
  c07_b07.md: ...有两个进程同时在临界区）。  _Liveness_ 性质说“好事最终会发生”。它只被好事件永不发生的无限执行违反。示例：termination（每个正确进程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 saf...
  c07_b11.md: ...章总结  共识是让一组进程在部分进程崩溃的情况下就一个值达成一致的问题。三条性质（agreement、validity、termination）是经典陈述。FLP 不可能性说确定性协议在纯异步下一次崩溃下不能保证终止全部三条；现代逃生舱是部分同步、随机化和故障检...
  c07_b11.md: ...性质映射到真实协议的具体设计选择：agreement 用法定人数相交，validity 来自 leader 提议的结构，termination 用部分同步加心跳。用这副眼镜读任何新共识论文，结构都会显现。  第8章取第一个经典协议 Paxos，逐步走一遍。Lam...
  c08_b01.md: ...* **Agreement** ：只选定一个值（协议完成后，没有两个 acceptor 对选定值产生分歧）。 * **Termination** ：无故障且 leader 稳定时，协议终止。   ### 8.2 为什么朴素协议失败  在 Paxos 之前，先看...

=== system (27 occurrences) ===
  c01_b05.md: ...& Cesati _Understanding the Linux Kernel_（第3版）；Love _Linux System Programming_ ；Brendan Gregg 的 _Systems Performance_（第2版，202...
  c03_b02.md: ..."Time, Clocks, and the Ordering of Events in a Distributed System"（1978），必读。关键洞见是：给分布式系统中的事件排序不需要同步时钟，只需要 _happens-before_ 关系...
  c03_b08.md: ...### 3.7 综合：分布式数据库中的排序  现代分布式数据库对事务排序使用三种模式之一：  System | Ordering primitive | What it costs ---|---|--- Spanner |...
  c05_b05.md: ...t-quorum 用于自动 RPO=0 且多区域容错。训练技能：按操作的 RPO 需求选择复制模式，而非按系统默认。  System | Default replication | Configuration options ---|---|--- P...
  c08_b11.md: ...de Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ousterhout 20...
  c13_b05.md: ...重启；高争用最近写入键的工作负载会看到频繁重启，并通过重试延迟有效支付 TrueTime 成本。   #### 对比  System | Timestamp source | Cost ---|---|---- Spanner | TrueTime（G...
  c13_b14.md: ...Raft group 选出）恢复协议。  **答案：** 在 CockroachDB 中，事务协调者状态作为记录存在 system range 中；该 range 本身被 Raft 复制。若运行协调者角色的节点死亡：  (a) 协调者状态 range...
  c24_b03.md: ...达时，系统找到最长缓存前缀并复用；只有后缀需要重新计算。  收益：  (a) **共享前缀的巨大节省**：对具有长共享 system prompt 的工作负载（生产 LLM 应用中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必...
  c24_b03.md: ...中常见），前缀复用节省了共享部分的 prefill 成本。之前每个请求都必须 prefill 的 2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成**：SGLang 原生支持结构化输出（J...
  c24_b03.md: ...工作负载的最佳选择。  **何时选 SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——R...
  c24_b03.md: ...30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 2000-token system prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。对 promp...
  c24_b06.md: ...每张 GPU 吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同 system prompt 的聊天工作负载，内存节省巨大。  灵感来自操作系统虚拟内存：页让进程无需连续物理地址即可使用内存。Pag...
  c24_b07.md: ...tention（基于前缀的 KV cache 复用）。 * 优势：对共享前缀工作负载节省巨大。 * 最适合：具有长共享 system prompt、结构化输出、函数调用的工作负载。  各适合何时：  * **vLLM**：任意用户聊天机器人；混合工作负...
  c24_b08.md: ...代引擎，这是第一步。预期利用率提升：从 60% 到 80%+。  (b) **前缀共享（SGLang）**：对具有重复 system prompt 的工作负载（客服机器人、agent scaffold），SGLang 的 RadixAttention...
  c24_b11.md: ...2026 年基线开源推理引擎。  SGLang 的 RadixAttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA NIM 是企业部署平...
  c26_b02.md: ...户看到约 30% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人...
  c26_b02.md: ...示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cac...
  c26_b02.md: ...对 Continuum 具有 system prompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测...
  c26_b08.md: ...Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针...
  c26_b08.md: ...00-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system...
  c26_b08.md: ...system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-token system prompt 开头；每请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路...
  c26_b08.md: ...) **Prefix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system prompt。第一个请求后，该前缀缓存在某节点。该客户后续请求被路由到同一节点（或有缓存的节点）。  收益：5000-...
  c30_b01.md: ...012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IEEE Computer_. —...
  c30_b01.md: ..."Time, Clocks, and the Ordering of Events in a Distributed System." _Communications of the ACM_. — 第3章必读。 * Kulkarni, S. S.,...
  c30_b02.md: ...Chang, F., et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P.,...
  c30_b03.md: ..._. * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C.,...
  c30_b13.md: ...*故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分区行为** 基于应用能容忍什么。  然后通过第14章矩阵映射到系统。再应用相关防御（重试预算、熔断器...

=== schema (27 occurrences) ===
  c01_b05.md: ...I 设计（在 _Software at Scale_ Series IV Vol 1 _APIs_ 中覆盖）、gRPC schema 演进、OpenAPI 工具链。第19章会提到服务发现；协议本身超出范围。参考：Newman _Building Mic...
  c11_b08.md: ...aknesses | 专用于复式记账（非通用）；生态小 Right for | 银行账本、支付系统、金融 OLTP，且 schema 符合 TigerBeetle 的会计模型  TigerBeetle 作为领域特定设计的研究很有趣：通过限制数据模型（仅...
  c12_b08.md: ...应用层处理热租户。  Salesforce / Workday 模式（大规模多租户）通常是 (b) 加 careful schema 设计。初创 SaaS 模式（几百租户）常为简单选 (c)。...
  c13_b14.md: ...（金融系统 p99 重要）；CockroachDB 适合中低争用下更高峰值吞吐。对真正的高争用，两者都不合适；重新设计 schema。...
  c14_b01.md: ...me 用于线性化全局时间戳。 * 支持跨多行、多 tablet 事务的 SQL。 * 区域或多区域部署配置。 * 在线 schema 变更。  优势：  * 最成熟的多区域事务数据库；在 Google 规模下久经考验。 * 基于 TrueTime 的时...
  c14_b03.md: ...字节消息、热路径无动态分配、显式状态机、设计口号是 “1000-year debugging”。 * 账户和转账的固定 schema；无通用 SQL。 * 极高吞吐设计（宣称每秒 100 万+ 转账）。  优势：  * 通过确定性模拟测试获得金融级正确...
  c14_b03.md: ...L。 * 极高吞吐设计（宣称每秒 100 万+ 转账）。  优势：  * 通过确定性模拟测试获得金融级正确性。 * 在 schema 范围内吞吐极高。 * 确定性执行；可复现调试。  劣势：  * Schema 专用；非通用。 * 生态小。  Tige...
  c14_b03.md: ...确定性模拟测试获得金融级正确性。 * 在 schema 范围内吞吐极高。 * 确定性执行；可复现调试。  劣势：  * Schema 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。C...
  c14_b03.md: ...：  * Schema 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。Crescent 的实际转账记录可用 TigerBeetle；商家目录和用户画像则用 CockroachDB 或...
  c14_b03.md: ...字节消息、热路径无动态分配、显式状态机、设计口号是 “1000-year debugging”。 * 账户和转账的固定 schema；无通用 SQL。 * 极高吞吐设计（宣称每秒 100 万+ 转账）。  优势：  * 通过确定性模拟测试获得金融级正确...
  c14_b03.md: ...L。 * 极高吞吐设计（宣称每秒 100 万+ 转账）。  优势：  * 通过确定性模拟测试获得金融级正确性。 * 在 schema 范围内吞吐极高。 * 确定性执行；可复现调试。  劣势：  * Schema 专用；非通用。 * 生态小。  Tige...
  c14_b03.md: ...确定性模拟测试获得金融级正确性。 * 在 schema 范围内吞吐极高。 * 确定性执行；可复现调试。  劣势：  * Schema 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。C...
  c14_b03.md: ...：  * Schema 专用；非通用。 * 生态小。  TigerBeetle 适合：支付账本、结算系统、会计系统，且 schema 适合。Crescent 的实际转账记录可用 TigerBeetle；商家目录和用户画像则用 CockroachDB 或...
  c15_b09.md: ...戳更晚）送来时，本地写被覆盖。该区域的用户看到自己的写 “消失”。  架构修复：  (a) **按区域分区** ：设计 schema，使每个 item 只被一个区域拥有。其他区域的写路由到拥有区域。没有跨区域并发写，就没有 LWW 冲突。  (b) *...
  c16_b04.md: ...显式唯一动作语义，通常在服务器以轻量事务实现（Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Y...
  c16_b04.md: ...Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Yjs 的版本处理覆盖很多情况；应用以向后兼容层...
  c16_b04.md: ...显式唯一动作语义，通常在服务器以轻量事务实现（Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Y...
  c16_b04.md: ...Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Yjs 的版本处理覆盖很多情况；应用以向后兼容层...
  c16_b08.md: ...一致应用（op-based CRDT，注意顺序），或 (ii) 分解为单个单元格 op（丢失语义意图）。  (c) **Schema 变更**。新增列、修改单元格类型、定义命名范围。这些是 CRDT 必须支持的结构操作。...
  c17_b01.md: ...2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB causally-consistent sessions**（3...
  c17_b08.md: ...ra 中的修复是把每个物品作为单独一行存储（键 = (user_id, item_id)），这样添加不冲突。可行但需要 schema 设计纪律。“清空购物车” 或 “设置数量” 等操作若仍用单个购物车状态行仍会冲突。  _Riak with sibli...
  c17_b08.md: ...发添加保留；并发移除被 tombstone；最终购物车是确定性合并。  若团队现有系统是 Cassandra：重新设计 schema（每物品一行，而非每购物车一行），并对 “清空购物车” 操作使用 LWT。比 Riak CRDT 方法复杂但可行。...
  c21_b04.md: ...12% 支付交易失败。  **触发器**：2026-03-14 14:23 UTC 部署了 merchant 数据库 schema 变更。该变更引入的索引触发了一次长时间迁移；迁移把一张热表锁定 90 秒。  **放大**：  * 90 秒锁定期间，...
  c21_b10.md: ...ug）、互联作为级联路径。教训适用于每个分布式系统。  工作流 10（Crescent 事后复盘）展示微服务领域的模式：schema 变更触发器、重试风暴放大、缺失熔断器、自动扩缩慢。修复：linter 规则、降低重试预算、加熔断器、预热容量、更新 r...
  c23_b10.md: ...入。专门故障。  (g) **内存压力 / GC 暂停**：节点活着但慢。专门故障。  (h) **配置变更**：部署、schema 变更、feature flag。本身不是故障，但与系统交互的运营事件。  Chaos Monkey 测试一个切片（“实...
  c30_b11.md: ...reSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 p...
  c30_b11.md: ...写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；F...

=== consensus (27 occurrences) ===
  c04_b05.md: ...|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9         style E fill:#fff4e6...
  c04_b05.md: ...+ total order (consensus)         v     Atomic broadcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrig...
  c08_b07.md: ...改变 acceptor 集合（增删节点）不能随意，因为两个不同配置可能各自形成无重叠的“多数”。标准解是 “joint consensus” 或 “alpha-paxos” 模式，但正确实现微妙。  (c) **快照与日志截断** ：Paxos 日志无限增长...
  c08_b11.md: ...，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ousterhout 2014 年为比 Paxos 更易理...
  c09_b01.md: ...和 Ousterhout 设计，2014 年论文题为 “In Search of an Understandable Consensus Algorithm”，目标是：至少和 Paxos 一样正确，但 _可理解_。摘要明确把可理解性作为主要贡献。  本章按...
  c09_b10.md: ...### 深入思考  **Q1.** 辩护或反驳：“Raft 的简单性只是营销说法；一旦你实现重配置、快照和 joint consensus，Raft 就和 Paxos 一样复杂。”  **讨论：** 辩护：核心 Raft 协议（leader election...
  c09_b10.md: ...基本 Raft；基本 Paxos 需要更久。教学和代码可读性收益真实。  反驳：完整 Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优...
  c11_b01.md: ...。   ### 11.2 方法详述   #### 问题 1：复制协议  阅读系统文档中的 “replication”、“consensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader...
  c19_b05.md: ...node）。  差异：  | Aspect | etcd | ZooKeeper | |---|---|---| | Consensus | Raft | ZAB（Paxos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | |...
  c27_b03.md: ...下失败。两个客户端可能都相信自己持有锁；两者都调用 `do_critical_work`；关键工作被污染。  修复：用 consensus-backed 锁（etcd、Consul）并在被保护资源上加 fencing token。       def pro...
  c27_b05.md: ...`time.Now()` 做跨机器排序。用 HLC 或因果排序。  (f) **锁**：每个分布式锁对正确性场景应是 consensus-backed 加 fencing token。  (g) **部分故障处理**：每个多步操作应处理部分失败（saga、...
  c27_b10.md: ...排序应使用 HLC 或因果排序。`time.Now()` 是错的。  (f) **锁**：分布式锁对正确性关键场景应是 consensus-backed（etcd 等）加 fencing token。  (g) **部分故障处理**：多步操作应处理部分失败（...
  c27_b15.md: ...、级联故障。  审查清单（10 项）：幂等性、超时、带预算重试、熔断器、排序原语（HLC 而非 wall clock）、consensus-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace c...
  c28_b10.md: ...多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 | | 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票...
  c30_b02.md: ...A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. — FLP 不可能性。...
  c30_b02.md: ...FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the presence of partial synchrony." _Journal of the ACM_...
  c30_b02.md: ...., & Ousterhout, J. (2014). "In Search of an Understandable Consensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi,...
  c30_b02.md: ..., M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with Linearity and Responsiveness." _PODC_. * Danezis, G.,...
  c30_b02.md: ...). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelman, A., et al. (2022). "Bullshark: D...
  c30_b02.md: ...Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT consensus." _arXiv:1807.04938_. — Tendermint。   #### 分布式存储（第四部分 — 第11...
  c30_b02.md: ...A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. — FLP 不可能性。...
  c30_b02.md: ...FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the presence of partial synchrony." _Journal of the ACM_...
  c30_b02.md: ...., & Ousterhout, J. (2014). "In Search of an Understandable Consensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi,...
  c30_b02.md: ..., M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with Linearity and Responsiveness." _PODC_. * Danezis, G.,...
  c30_b02.md: ...). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelman, A., et al. (2022). "Bullshark: D...
  c30_b02.md: ...Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT consensus." _arXiv:1807.04938_. — Tendermint。   #### 分布式存储（第四部分 — 第11...
  c30_b14.md: ...tokens) — for demonstration     │   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_tok...

=== worker (27 occurrences) ===
  c13_b03.md: ...2. **Scheduler** ：每个分片接收排序后的批次并按顺序执行。锁按约定顺序获取；无死锁。   3. **Worker** ：在本地分片上执行事务操作。  约定顺序就是规范执行顺序。每个分片独立处理每笔事务的一部分，但按全局顺序；结果等价...
  c16_b04.md: ...ctively 编辑时，其 update 被发送到服务器（带 Durable Objects 的 Cloudflare Worker）并广播给其他连接客户端。每次编辑延迟亚秒级。  (b) **重连时追赶** ：客户端离线后重连，与服务器交换 _sta...
  c16_b04.md: ...ate vectors_（“我有哪些 update” 的压缩表示），并接收缺失 update。  Cloudflare Worker 持有文档权威副本。它应用进入的 update 并广播给所有连接客户端。重连时，它计算客户端与服务器状态差并只发送所需内...
  c17_b03.md: ...计数器服务（CRDT）做页面浏览指标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（per-object 线性化）做文档元数据。注意：Cloudflare KV 在这...
  c18_b03.md: ...率**：同一时间只有一个客户端做昂贵工作。若两个同时做，工作重复但无损害。示例：job queue 中每个任务应由一个 worker 处理。  (b) **正确性**：同一时间只有一个客户端修改受保护资源。若两个同时做，资源损坏。示例：支付提交，两个并...
  c18_b07.md: ...**答案：** 缓存锁（Redlock 或类似）在以下情况可接受：  (a) 锁用于 _效率_ ：只有一个 worker 应同时做昂贵工作。若偶尔两个 worker 都做（因锁失败），重复工作浪费但无害。  示例：job queue 去重、...
  c18_b07.md: ...ock 或类似）在以下情况可接受：  (a) 锁用于 _效率_ ：只有一个 worker 应同时做昂贵工作。若偶尔两个 worker 都做（因锁失败），重复工作浪费但无害。  示例：job queue 去重、周期批处理任务调度、昂贵计算记忆化。  (b...
  c18_b07.md: ...但无害。  示例：job queue 去重、周期批处理任务调度、昂贵计算记忆化。  (b) 下游影响是幂等的：即使两个 worker 都执行，效果与一个相同。示例：worker 写入带幂等键的数据库 “user_id seen”。  缓存锁在以下情况...
  c18_b07.md: ...周期批处理任务调度、昂贵计算记忆化。  (b) 下游影响是幂等的：即使两个 worker 都执行，效果与一个相同。示例：worker 写入带幂等键的数据库 “user_id seen”。  缓存锁在以下情况 _不可接受_ ：  (a) 锁用于 _正确性...
  c18_b08.md: ...#### 应用问题  **Q1.** 一个团队实现 job queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁...
  c18_b08.md: ...。每次获取较慢（etcd 写是多数复制）。  (c) **数据库行级锁** ：数据库中的一行表示 “正在处理任务 X”。worker 用 `SELECT ... FOR UPDATE` 获取锁；行的存在阻止其他 worker 申领。完成后 worker...
  c18_b08.md: ...表示 “正在处理任务 X”。worker 用 `SELECT ... FOR UPDATE` 获取锁；行的存在阻止其他 worker 申领。完成后 worker 更新行（如 status = "done"）。对已有 Postgres 的应用，运维干净：...
  c18_b08.md: ...worker 用 `SELECT ... FOR UPDATE` 获取锁；行的存在阻止其他 worker 申领。完成后 worker 更新行（如 status = "done"）。对已有 Postgres 的应用，运维干净：无额外基础设施。  推荐：...
  c27_b04.md: ...outbox.enqueue(NotifyUser(user.id, 'updated'))      # 异步 worker 处理 outbox；失败时重试。   LLM 产出朴素版本是因为训练数据显示 “做 A；做 B；做 C” 是常见模式。...
  c27_b07.md: ...包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game...
  c28_b04.md: ...eze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US...
  c28_b04.md: ...斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US-East r...
  c28_b04.md: ...eze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US...
  c28_b04.md: ...斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker 路由到 US-East r...
  c28_b15.md: ...谎言，以及什么防御能防止它。  提示  用三个谎言语言重构 bug；找出缺失的防御。  **答案：** 常见模式：一个 worker 池消费队列；每个 worker 处理一个 job；偶尔一个 job 被处理两次。  _谎言_：身份谎言（worker...
  c28_b15.md: ...提示  用三个谎言语言重构 bug；找出缺失的防御。  **答案：** 常见模式：一个 worker 池消费队列；每个 worker 处理一个 job；偶尔一个 job 被处理两次。  _谎言_：身份谎言（worker 假设自己唯一处理这个 job）结...
  c28_b15.md: ...er 假设自己唯一处理这个 job）结合故障谎言（worker 假设 “我没崩溃，所以我在处理”）。  _发生了什么_：worker A 拉出 job 42，开始处理，然后暂停（GC pause）。队列的可见性超时到期；worker B 拉出 job...
  c28_b15.md: ..._发生了什么_：worker A 拉出 job 42，开始处理，然后暂停（GC pause）。队列的可见性超时到期；worker B 拉出 job 42，开始处理。两者都完成。job 被处理两次；下游副作用发生两次。  _缺失的防御_：下游副作用的...
  c28_b15.md: ...没有幂等，两次处理都产生效果。  _我学到的_："假设最坏情况" 的纪律甚至适用于看起来安全的运维模式。带可见性超时的 worker 池是标准模式；假设 “超时保护我们” 就是谎言。防御是下游幂等，无论运维模式看起来多可靠。  这类 bug 在分布式系...
  c30_b08.md: ...Cloudflare 邮件路由故障，2024-04  * **触发**：一次配置错误的部署推出代码变更，导致邮件路由 worker 对某些消息类型进入无限循环。 * **放大**：循环 worker 保持连接池打开；新邮件流量排队；队列满；整个邮件路...
  c30_b08.md: ...**：一次配置错误的部署推出代码变更，导致邮件路由 worker 对某些消息类型进入无限循环。 * **放大**：循环 worker 保持连接池打开；新邮件流量排队；队列满；整个邮件路由服务降级。 * **时间线**：Cloudflare 邮件转发服务...
  c30_b08.md: ...务降级。 * **时间线**：Cloudflare 邮件转发服务中断约 2 小时。 * **恢复**：通过追踪识别循环 worker 后回滚部署。 * **章节映射防御**：     * Ch.20（分布式追踪）：trace 数据揭示了循环；没有追踪，...

=== sink (27 occurrences) ===
  c15_b08.md: ...che Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕...
  c15_b08.md: ...inting 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **So...
  c15_b08.md: ...记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink...
  c15_b08.md: ...算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件...
  c15_b08.md: ...sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkp...
  c15_b08.md: ...须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Pos...
  c15_b08.md: ...**幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Fli...
  c15_b08.md: ...复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...
  c15_b08.md: ...che Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕...
  c15_b08.md: ...inting 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **So...
  c15_b08.md: ...记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink...
  c15_b08.md: ...算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件...
  c15_b08.md: ...sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkp...
  c15_b08.md: ...须参与 checkpoint。两种策略：  * **幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Pos...
  c15_b08.md: ...**幂等写** ：sink 记录每个唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Fli...
  c15_b08.md: ...复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端意义上每个事件恰好处理...
  c15_b09.md: ...一次。Checkpoint 定义 “事务边界”；一个 checkpoint 内所有内容要么整体提交（source 推进、sink 提交），要么整体回滚。  代价：checkpoint 延迟（事件到达与持久提交之间的时间）就是 checkpoint...
  c30_b11.md: ...延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Strea...
  c30_b11.md: ...rt 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint...
  c30_b11.md: ...ured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Reg...
  c30_b11.md: ...个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQ...
  c30_b11.md: ...延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Strea...
  c30_b11.md: ...rt 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint...
  c30_b11.md: ...ured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Reg...
  c30_b11.md: ...个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQ...

=== sglang (27 occurrences) ===
  c23_b13.md: ...，因为它在客户之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集...
  c24_b01.md: ...de、KV cache sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NVIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。   ### 24.1 为什么...
  c24_b01.md: ...文档分析），有的突发（多轮并发对话）。集群必须处理这种混合而没有队头阻塞。  2026 年处理这些挑战的系统：vLLM、SGLang、NVIDIA NIM。每种有不同优化；适合不同工作负载。...
  c24_b03.md: ...### 24.3 SGLang 与 RadixAttention  SGLang（2023 年底由 LMSYS 发布；2025 年广泛部署）走了另一条...
  c24_b03.md: ...### 24.3 SGLang 与 RadixAttention  SGLang（2023 年底由 LMSYS 发布；2025 年广泛部署）走了另一条优化路径：_RadixAttention_。vLL...
  c24_b03.md: ...LMSYS 发布；2025 年广泛部署）走了另一条优化路径：_RadixAttention_。vLLM 优化内存布局，SGLang 优化请求路由布局。  洞察：许多请求共享前缀结构（system prompts、chain-of-thought sc...
  c24_b03.md: ...2000-token system prompt，现在只 prefill 一次并缓存。  (b) **结构化生成**：SGLang 原生支持结构化输出（JSON schema、regex 约束）。结构是请求规范的一部分；推理引擎知道如何处理。  (c...
  c24_b03.md: ...道如何处理。  (c) **函数调用**：原生支持 tool use；radix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输出应用、agent 系统。它是 prompt 结构重复的工作负载...
  c24_b03.md: ...中流行：客服机器人、结构化输出应用、agent 系统。它是 prompt 结构重复的工作负载的最佳选择。  **何时选 SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 functio...
  c24_b03.md: ...存可避免重新计算这些 token。阈值大致：若平均 prompt token 中 30% 或更多来自跨请求重复固定前缀，SGLang 的缓存命中率足够高，值得切换。对每个请求都以 2000-token system prompt 开始的客服机器人，SG...
  c24_b03.md: ...ng 的缓存命中率足够高，值得切换。对每个请求都以 2000-token system prompt 开始的客服机器人，SGLang 在缓存命中时直接消除该计算，同时降低延迟和 GPU 成本。对 prompt 多样化（不同用户、不同任务、无共享前缀）的...
  c24_b05.md: ...regation，第25章）。每种技术复合。   ### 24.7 Continuous Batching  vLLM/SGLang 的关键技术：continuous batching。没有它，GPU 利用率是突发的（请求间空闲）。有了它，GPU 利用...
  c24_b05.md: ...低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提升通常 2-5 倍。   ### 24.8 练习...
  c24_b06.md: ...isaggregation、KV cache sharding）在此基础上构建。  **Q3.** 比较 vLLM 和 SGLang。各适合什么场景？  提示  vLLM：PagedAttention；通用。SGLang：RadixAttention；...
  c24_b06.md: ...Q3.** 比较 vLLM 和 SGLang。各适合什么场景？  提示  vLLM：PagedAttention；通用。SGLang：RadixAttention；前缀复用重的工作负载。  **答案：**...
  c24_b07.md: ...各适合何时：  * **vLLM**：任意用户聊天机器人；混合工作负载 API；希望最广泛生产支持的团队。 * **SGLang**：客服机器人（system prompt 跨百万请求重复）、agent 框架（agent scaffold 共享）、...
  c24_b07.md: ...ang 合适）。Continuum 同时运行两者，按客户把工作负载路由到合适引擎。  决策很少是排他的 “vLLM 对 SGLang”；现代推理集群常同时运行两者，负载均衡器按工作负载把请求路由到最适合的引擎。  **Q4.** 什么是 continu...
  c24_b07.md: ...比朴素 serving 吞吐提升 2-5 倍。Continuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI 等）。  实现复杂度在于动态 batch 管理：跟踪每个请求状态、处理加入/离开事件、跨 batch 管理 K...
  c24_b08.md: ...atching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLLM、SGLang）默认使用 continuous batching；若 Tobias 未用现代引擎，这是第一步。预期利用率提升：从 60...
  c24_b08.md: ...享（SGLang）**：对具有重复 system prompt 的工作负载（客服机器人、agent scaffold），SGLang 的 RadixAttention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀...
  c24_b08.md: ...ention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/...
  c24_b08.md: ...token 生成。整体预期利用率提升：20-40%。  叠加：continuous batching（若尚未），然后 SGLang 用于前缀复用工作负载，然后 disaggregation。预期最终集群利用率 80%+，在 120 万美元账单上每月节...
  c24_b11.md: ...分成固定大小块；消除碎片；GPU 内存利用率从 30-50% 提升到 70-90%。2026 年基线开源推理引擎。  SGLang 的 RadixAttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。...
  c26_b04.md: ...fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **硬件**：面向 disaggregation 优化的 H100/H200 混合。  结果：  * 集群...
  c30_b03.md: ...gedAttention." _SOSP_. — vLLM。 * Zheng, L., et al. (2023). "SGLang: Efficient Execution of Structured Language Model Programs....
  c30_b11.md: ...每日 10 亿+ token；多模型；延迟敏感的交互路径 + 批处理路径；成本优化是强制的。  **决策**：vLLM/SGLang + 分离式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（...
  c30_b11.md: ...式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；企业部署两者都通过 NVIDIA NIM。 * **架构**：集群超过约 50 节点后做 prefi...

=== counter (26 occurrences) ===
  c03_b03.md: ...例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1...
  c04_b07.md: ...证成本节约。  提示  CRDT 需要每个副本看到每个操作，但容忍任何顺序。  **答案：** 基于 CRDT 的 G-Counter 增量。要在 N 个副本上增量计数器，每个“增量”操作必须最终到达每个副本（可靠广播：agreement）。增量顺序不影...
  c04_b11.md: ...现代高吞吐系统太贵；CRDT 让整个广播层次过时。”  **讨论：** 对适用 CRDT 的工作负载，该主张有道理。G-Counter 示例说明，一整类操作（可交换的状态更新）不需要原子广播，配合可靠广播和 CRDT 语义可以扩展到更高吞吐。  反驳：C...
  c14_b10.md: ...非常快；QUORUM 写提供一定持久性保证。线性扩展到多节点。  (d) **CRDT-based（自定义）** ：G-Counter 或 CRDT 有序集做排行榜。每个区域有本地计数器；跨区域合并。每区域写亚毫秒；最终收敛。  推荐：混合架构。  *...
  c15_b04.md: ...（G-Counter）  每个副本维护一个 “各副本贡献递增” 向量（每个副本一个条目）。总数是向量之和。合并两个 G-Counter 时取 per-entry 最大值（每个副本的贡献至少是已报告的最高值）。  成本：与最终一致相同，但有形式化合并语义。...
  c15_b04.md: ...化合并语义。不可能丢失递增（一旦某副本记录递增，它就在该副本向量中，最终会合并到其它）。  第16章深入 CRDT。G-Counter 是最简单示例；集合、寄存器、映射随后。   #### 线性化  计数器放在强一致存储（Spanner、Cockroac...
  c15_b05.md: ...成本：每次递减 5-50 ms；每件商品吞吐受共识限制。   #### 最终一致的问题  CRDT-based “PN-Counter” 可以表示库存（正向和负向贡献），但合并函数是求和。若两个副本并发减到 -1，合并后总数为 -2，表示负两件（即最后一...
  c16_b02.md: ...### 16.2 典型 CRDT   #### G-Counter（只增计数器）  最简单的非平凡 CRDT。每个副本有一个向量 \(V[1..N]\)，\(V[i]\) 是副本 \(i...
  c16_b02.md: ...\(V[j] \leftarrow V[j] + 1\)。 * 值：\(\sum_i V[i]\)。 * 合并两个 G-Counter：\(V_{\text{merged}}[i] = \max(V_1[i], V_2[i])\)。  逐元素 max 合...
  c16_b02.md: ...1[i], V_2[i])\)。  逐元素 max 合并满足交换律、结合律、幂等。值（总和）定义良好且单调递增。  G-Counter 只能递增；PN-Counter 扩展为支持递减。   #### PN-Counter（正负计数器）  两个 G-Cou...
  c16_b02.md: ...逐元素 max 合并满足交换律、结合律、幂等。值（总和）定义良好且单调递增。  G-Counter 只能递增；PN-Counter 扩展为支持递减。   #### PN-Counter（正负计数器）  两个 G-Counter：\(P[1..N]\)...
  c16_b02.md: ...总和）定义良好且单调递增。  G-Counter 只能递增；PN-Counter 扩展为支持递减。   #### PN-Counter（正负计数器）  两个 G-Counter：\(P[1..N]\) 记录正向递增，\(N[1..N]\) 记录负向递增。...
  c16_b02.md: ...ter 只能递增；PN-Counter 扩展为支持递减。   #### PN-Counter（正负计数器）  两个 G-Counter：\(P[1..N]\) 记录正向递增，\(N[1..N]\) 记录负向递增。  * 在副本 \(j\) 递增：\(P[...
  c16_b02.md: ...i P[i] - \sum_i N[i]\)。 * 合并：对 \(P\) 和 \(N\) 分别逐元素 max。  PN-Counter 可表达任意整数值。它不强制 “值绝不低于零”，因此不能替代库存（第15章）。   #### LWW-Register（...
  c16_b06.md: ...（list、map）更高效。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.** 走一遍 G-Counter 和 PN-Counter 的操作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。...
  c16_b06.md: ...。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.** 走一遍 G-Counter 和 PN-Counter 的操作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。PN-Counter：两个...
  c16_b06.md: ...优势的混合方案。  **Q2.** 走一遍 G-Counter 和 PN-Counter 的操作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。PN-Counter：两个 G-Counter（正、负）。  **答案：...
  c16_b06.md: ...PN-Counter 的操作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。PN-Counter：两个 G-Counter（正、负）。  **答案：**  _G-Counter_ ：向量 \(V[1..N]\)。每个...
  c16_b06.md: ...作与合并。  提示  G-Counter：贡献向量；递增自己的槽；按元素 max 合并。PN-Counter：两个 G-Counter（正、负）。  **答案：**  _G-Counter_ ：向量 \(V[1..N]\)。每个条目 \(V[i]\) 是...
  c16_b06.md: ...2 递增一次：\([0,1,0]\)。合并：\([2,1,0]\)。值：3。  _PN-Counter_ ：两个 G-Counter，\(P[1..N]\) 和 \(N[1..N]\)。\(P\) 记录正向递增，\(N\) 记录负向递增。  * 在副本...
  c16_b11.md: ...peration-based CRDT（CmRDT）广播操作。两者表达能力等价；选择是运维性的。  典型 CRDT：G-Counter（只增计数器）、PN-Counter（支持递减的计数器）、LWW-Register（最后写胜出）、MV-Register...
  c16_b11.md: ...T（CmRDT）广播操作。两者表达能力等价；选择是运维性的。  典型 CRDT：G-Counter（只增计数器）、PN-Counter（支持递减的计数器）、LWW-Register（最后写胜出）、MV-Register（多值）、OR-Set（观察删除集合...
  c30_b14.md: ...xample     ├── ch16_crdt/     │   ├── g_counter.py      — G-Counter (grow-only)     │   ├── pn_counter.py     — PN-Counter...
  c30_b14.md: ...— G-Counter (grow-only)     │   ├── pn_counter.py     — PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │...
  c30_b14.md: ...xample     ├── ch16_crdt/     │   ├── g_counter.py      — G-Counter (grow-only)     │   ├── pn_counter.py     — PN-Counter...
  c30_b14.md: ...— G-Counter (grow-only)     │   ├── pn_counter.py     — PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │...

=== multi (26 occurrences) ===
  c06_b03.md: ...理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。...
  c07_b05.md: ...r-tablet Paxos。由于 Google 数据中心网络纪律更好，超时更紧。 * **TiKV** 用 Raft。Multi-Raft 架构：多 Raft 组并行。 * **Tendermint**（Cosmos 链）用显式轮次超时；生产 1...
  c07_b11.md: ...Paxos，逐步走一遍。Lamport 1989 年论文（2001 年改写成通俗英语）是分布式系统最有影响力的论文之一。Multi-Paxos 把单法令 Paxos 扩展到决策序列，这是生产部署（Spanner、Megastore）的真实形式。第9章...
  c08_b01.md: ...s Made Simple”，仍然很难。  本章教授 Paxos 的常见形态：单法令 Paxos（就一个值达成一致）和 Multi-Paxos（就值序列达成一致）。重点不是背消息，而是理解使 Paxos safe 的结构不变式。Raft（第9章）更容...
  c08_b03.md: ...并发运行时协议 safety 仍成立。Proposer 会相互干扰（导致重启和终止延迟），但无法违反 safety。  Multi-Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5...
  c08_b03.md: ...Paxos（下一节）为效率引入 leader，但 leader 是优化，不是 safety 要求。   ### 8.5 Multi-Paxos  单法令 Paxos 就一个值达成一致。状态机复制需要值序列（复制日志中每个槽一个值）。朴素扩展是每个槽跑...
  c08_b03.md: ...状态机复制需要值序列（复制日志中每个槽一个值）。朴素扩展是每个槽跑独立 Paxos 实例，但成本太高（每槽两阶段）。  Multi-Paxos：选举一个稳定 leader，让 leader 为整个槽范围只跑一次第一阶段，然后每个后续槽只跑第二阶段。...
  c08_b03.md: ...现代系统（Spanner、etcd-via-Raft）领导稳定数分钟到数小时，因此每决定成本是一次往返。   #### Multi-Paxos 中的 Leader 选举  Leader 选举本身就是一个 Paxos 实例（或退化等价）。当进程怀疑当前...
  c08_b04.md: ...dling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Go...
  c08_b04.md: ...。这让系统可调节权衡（更小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续...
  c08_b06.md: ...定。任何未来 ballot 都会看到 $v_{prev}$ 并提议它。系统收敛到单一选定值。  **Q3.** 什么是 Multi-Paxos？它如何比单法令 Paxos 更适合状态机复制？  提示  单法令 Paxos 就一个值达成一致；SMR 需...
  c08_b06.md: ...os？它如何比单法令 Paxos 更适合状态机复制？  提示  单法令 Paxos 就一个值达成一致；SMR 需要序列。Multi-Paxos 把第一阶段在多决定上摊销。  **答案：** Multi-Paxos 扩展单法令 Paxos，以就值序列达...
  c08_b06.md: ...Paxos 就一个值达成一致；SMR 需要序列。Multi-Paxos 把第一阶段在多决定上摊销。  **答案：** Multi-Paxos 扩展单法令 Paxos，以就值序列达成一致（复制日志每槽一个值）。改进点：稳定 leader 为多个槽只跑...
  c08_b07.md: ...TED）。对服务多个决定的稳定 leader，摊销效果显著。  若领导不稳定（频繁重选），每次重选都要跑第一阶段。实用 Multi-Paxos 系统追求领导稳定数分钟到数小时。  **Q4.** 为什么 Paxos 以难正确实现著称？说出三个运维关注...
  c08_b07.md: ....** 为什么 Paxos 以难正确实现著称？说出三个运维关注点。  提示  持久状态、重配置、快照、fsync 纪律、Multi-Paxos 优化。  **答案：** Paxos 难实现有多个运维原因：  (a) **持久状态纪律** ：accep...
  c08_b07.md: ...截断太激进会让恢复中的副本没有足够日志追赶。  还有三条（任一条都曾引发生产故障）：  (d) **稳定领导维护** ：Multi-Paxos 依赖稳定领导；若网络不稳定导致领导频繁更替，每次更替都花第一阶段往返并可能暂停集群。  (e) **读一致...
  c08_b08.md: ...到单一值 “Olamide”。A 原本想提议 “Anya” 的意愿被 safety 规则覆盖。  **Q2.** 一个 Multi-Paxos 集群有 5 个节点。当前 leader 到其余 4 个中 3 个的网络连接降级，只剩 1 个可到达。集群怎...
  c08_b11.md: ...r 的 accept 必须提议第一阶段响应中最高已接受的值；这保证一旦值被选定，所有后续 ballot 都会提议它。  Multi-Paxos 通过选举稳定 leader 并把第一阶段摊销到多个决定，把单法令扩展为值序列。稳态下每决定成本是一次往返。...
  c09_b06.md: ...由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 leader，但 leader 角色与 ballot 的耦合不如 Raft 的 leader 与 te...
  c09_b06.md: ...角色与 ballot 的耦合不如 Raft 的 leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 R...
  c09_b09.md: ...主导的高吞吐工作负载，或需要任意区域写的地理分布式工作负载中，无 leader 协议获胜。  2026 年的主导模式是 multi-Raft：多 Raft 组并行，每组处理一个分片。这结合了每组内 Raft 的简单性与水平扩展。CockroachDB...
  c09_b10.md: ...ion + AppendEntries + commit）确实比核心 Paxos（prepare + accept + Multi-Paxos 优化）简单。学生可以周末实现一个能工作的基本 Raft；基本 Paxos 需要更久。教学和代码可读性收益真...
  c09_b10.md: ...导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego Ongaro 博士论文数百页；etcd Raft 实现数千行。  综合：Raft 在...
  c09_b11.md: ...作的边界情况（重配置、快照、领导权转移）。  Raft 用每集群吞吐上限（leader 串行化写）换取简单性和运维工具。Multi-Raft 模式（多 Raft 组并行，每组一个分片）恢复水平扩展。CockroachDB、TiKV、YugabyteD...
  c28_b03.md: ...Services"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]...
  c30_b16.md: ...同步。 | Ch.7 | | 12 | "Paxos 选举 leader。" | 单法令 Paxos 无 leader。Multi-Paxos 的 leader 是优化，不是安全要求。 | Ch.8 | | 13 | "Raft 在多数派复制后立刻提...

=== aware (26 occurrences) ===
  c11_b03.md: ...ge 领导权转移 Consistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停止接受写 PACELC | PC / EC（分区...
  c15_b10.md: ...传感器以每传感器每秒 10,000 事件计数。回调到中央强一致系统太慢。正确答案是传感器本地累加，批量上传到 CRDT-aware 聚合器。  模式：任何计数器 _值_ 变化频繁但 _消费者_ 容忍轻微不一致的工作负载，都是 CRDT/最终一致工作负...
  c15_b11.md: ...化的有用 per-session 弱化版本。CockroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话跟踪提供它们。  Chandy-Lamport 分布式快照是在无全局时钟下捕获一致全局状态的基础原语。协议使用每条...
  c17_b08.md: ...（从任意副本读而不等待）。这以牺牲因果正确性换取可用性；应用代码必须处理轻微过期读。  (d) **切换到具有 HLC-aware 多区域语义的系统**：CockroachDB 的 follower reads 或 YugabyteDB 的类似特性；...
  c25_b11.md: ...e decoding（draft model + verify model）作为多机协议；长度预测和 KV-cache-aware 路由。  ---...
  c26_b01.md: ...看到 30 秒转圈等待。  本章讲 2024-2026 年出现的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative de...
  c26_b01.md: ...事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、speculative decoding（本章）。这些部分组合。   ### 26.1 LLM 请求路由...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有...
  c26_b02.md: ...efill 工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **T...
  c26_b02.md: ...ompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。  预测问题：给...
  c26_b02.md: ...### 26.2 KV-Cache-Aware 路由  模式：负载均衡器维护 “哪些 prompt 前缀缓存在哪些节点” 的映射。新请求到达时，负载均衡器检查：是否有...
  c26_b02.md: ...efill 工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **T...
  c26_b02.md: ...ompt 的工作负载（客服机器人跨百万请求重复约 2000-token system prompt），KV-cache-aware 路由是重大胜利。   ### 26.3 长度预测路由  另一种信号：预测请求输出长度，并基于预测路由。  预测问题：给...
  c26_b04.md: ...fill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长尾感知调度）。  (c) **Decode**：speculativ...
  c26_b05.md: ...或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-aware 加长度预测路由，吞吐提升约 25%，p99 延迟降低约 30%。改动是算法级（无硬件改动）；收益巨大。  **Q2.*...
  c26_b08.md: ...rkload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware 路由处理 system prompt；针对该客户前缀。  **答案：** 该客户工作负载：每个请求以相同 5000-t...
  c26_b08.md: ...pt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system prompt。第一...
  c26_b10.md: ...的优化冲刺不是可选；它是保持竞争力所必需。  **Q2.** 一个 LLM 被要求 “为我们集群实现 KV-cache-aware 路由”。它产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** LLM 的基本...
  c26_b11.md: ...：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预测）带来显著效率收益。  KV-cache-aware 路由：负载均衡器维护缓存 KV cache 的前缀索引；...
  c26_b11.md: ...、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预测）带来显著效率收益。  KV-cache-aware 路由：负载均衡器维护缓存 KV cache 的前缀索引；把请求路由到有缓存的节点。对前缀重工作负载（system pr...
  c26_b11.md: ...Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair sche...
  c28_b07.md: ...是 sub-100-ms p99（p50 80 ms；与 §28.2 记录一致），得益于分离式架构和 KV-cache-aware 路由。集成只是一个简单 HTTP endpoint；Crescent 的 API 服务调用我们的 endpoint；我...
  c28_b12.md: ...别由哪一章引入：HLC、fencing token、Raft、原子广播、CRDT、重试预算、混沌工程、KV-cache-aware 路由。  提示  各章大致按顺序引入原语；查看目录。  **答案：**  * **HLC**：第3章（时间、序与说谎的...
  c28_b12.md: ...**重试预算**：第21章（级联故障）。 * **混沌工程**：第23章（混沌作为学科）。 * **KV-cache-aware 路由**：第26章（LLM 推理负载均衡）。  每个原语解决三个谎言某一类表现。HLC 防御时间谎言；fencing...
  c30_b11.md: ...优化是强制的。  **决策**：vLLM/SGLang + 分离式 prefill/decode + KV-cache-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；企业部署两者都通过 NVIDIA...
  c30_b11.md: ...5章）；KV cache 层（Mooncake / LMCache）用于前缀复用。 * **路由**：KV-cache-aware 负载均衡（第26章）加长度预测路由。 * **投机解码**：按模型为长输出工作负载启用。 * **监控**：每客户 G...

=== update (26 occurrences) ===
  c13_b07.md: ...send(user, "completed")       step 6: call AnalyticsService.update(merchant, day)       on failure at step N: invoke compensat...
  c13_b07.md: ...->>N: send confirmation         N-->>O: sent         O->>A: update merchant aggregate         A-->>O: updated         O-->>C:...
  c13_b10.md: ...40 USD；是一笔 substantial 交易，不是       -- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'a...
  c13_b10.md: ...emi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'f...
  c13_b10.md: ...1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1200 WHERE merchant...
  c13_b10.md: ...o-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在...
  c13_b10.md: ...的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非...
  c13_b10.md: ...ite intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取 write intent。   6. `CO...
  c13_b10.md: ...的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取 write intent。   6. `COMMIT`：事务协调者（本身通过 Raft 复制）在选定...
  c16_b04.md: ...同步  两种同步模式共存：  (a) **通过 WebSocket 实时同步** ：用户 actively 编辑时，其 update 被发送到服务器（带 Durable Objects 的 Cloudflare Worker）并广播给其他连接客户端。每...
  c16_b04.md: ...迟亚秒级。  (b) **重连时追赶** ：客户端离线后重连，与服务器交换 _state vectors_（“我有哪些 update” 的压缩表示），并接收缺失 update。  Cloudflare Worker 持有文档权威副本。它应用进入的 up...
  c16_b04.md: ...：客户端离线后重连，与服务器交换 _state vectors_（“我有哪些 update” 的压缩表示），并接收缺失 update。  Cloudflare Worker 持有文档权威副本。它应用进入的 update 并广播给所有连接客户端。重连时，...
  c16_b04.md: ...te” 的压缩表示），并接收缺失 update。  Cloudflare Worker 持有文档权威副本。它应用进入的 update 并广播给所有连接客户端。重连时，它计算客户端与服务器状态差并只发送所需内容。   #### 持久化  权威文档以 up...
  c16_b04.md: ...te 并广播给所有连接客户端。重连时，它计算客户端与服务器状态差并只发送所需内容。   #### 持久化  权威文档以 update 序列形式存在 Durable Object 存储中。定期地（约每小时），update 被 _compacted_ 成快...
  c16_b04.md: ...#### 持久化  权威文档以 update 序列形式存在 Durable Object 存储中。定期地（约每小时），update 被 _compacted_ 成快照：文档被序列化，历史 update 被丢弃。  快照是 CRDT 历史的 “垃圾回收...
  c16_b04.md: ...ble Object 存储中。定期地（约每小时），update 被 _compacted_ 成快照：文档被序列化，历史 update 被丢弃。  快照是 CRDT 历史的 “垃圾回收”。没有它，存储会无界增长。有了它，旧编辑被合并进快照，任何重连恢复都...
  c16_b04.md: ...照是 CRDT 历史的 “垃圾回收”。没有它，存储会无界增长。有了它，旧编辑被合并进快照，任何重连恢复都使用快照加后续 update。   #### 冲突解决  Yjs 自动处理所有冲突。若两个用户并发在同一位置插入字符，Yjs 按 client ID...
  c16_b07.md: ...mbstone 最终在所有副本观察到删除后 GC。 (e) **持久化格式** ：高效的二进制格式存储和传输 CRDT update。  早期 list CRDT（Logoot、Treedoc、WOOT）存在问题：标识符增长、合并慢、内存占用大。Yjs...
  c20_b01.md: ...DB --> DB1["read balance<br/>8 ms"]         DB --> DB2["UPDATE Adaeze<br/>30 ms (US-East shard)"]         DB --> DB3["UPDA...
  c20_b01.md: ...DATE Adaeze<br/>30 ms (US-East shard)"]         DB --> DB3["UPDATE Femi<br/>40 ms (EU-West shard, cross-region)"]         DB -...
  c20_b01.md: .../>40 ms (EU-West shard, cross-region)"]         DB --> DB4["UPDATE rollup<br/>15 ms"]         style R fill:#fff4e6         sty...
  c27_b04.md: ...def update_user_across_systems(user):         primary_db.update(user)         cache.update(user)         notification_servi...
  c27_b04.md: ...ystems(user):         primary_db.update(user)         cache.update(user)         notification_service.notify(user.id, 'updated...
  c27_b04.md: ...成功。代码只有 happy path；部分故障是隐含的。  现实：调用相互独立；任何都可能失败。`primary_db.update()` 成功但 `cache.update()` 之前，缓存已过期。两者都成功但 `notification_servi...
  c27_b04.md: ...故障是隐含的。  现实：调用相互独立；任何都可能失败。`primary_db.update()` 成功但 `cache.update()` 之前，缓存已过期。两者都成功但 `notification_service.notify()` 之前，通知未发出...
  c27_b04.md: ...systems(user):         # 关键：DB 更新。失败即中止。         primary_db.update(user)          # 尽力：缓存和通知。用 outbox 模式。         outbox.enque...

=== load (26 occurrences) ===
  c21_b03.md: ...### 21.5 Load Shedding  当服务过载时，两种响应：  (a) **排队并延迟**：入站请求在队列中等待，直到服务能处理。最终...
  c21_b03.md: ...弃**：超出服务容量的入站请求立即被拒绝，返回 “太忙” 响应。调用者快速知道，并可决定重试、故障转移或向用户失败。  Load shedding（选项 b）在级联故障条件下结构上更好，因为它避免导致放大的队列堆积。  实现：每个服务跟踪当前并发度...
  c21_b03.md: ...调用者优雅处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值。延迟升高时阈值降低；延迟正常时阈值升高...
  c21_b03.md: ...看到扩散时，已来不及干预。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸。继电器就是 load shedding 机制。没有它们，级联会更糟（整个发电厂受损）。  (d) **系统设计必须遏制故障**。电网为效率互...
  c21_b03.md: ...tart” 孤岛，可在级联期间隔离。  分布式系统类比：微服务舰队为功能互联；互联成为级联路径。防御（重试预算、熔断器、load shedding）必须在每个节点运行；仅集中监控无法防止级联。...
  c21_b03.md: ...### 21.5 Load Shedding  当服务过载时，两种响应：  (a) **排队并延迟**：入站请求在队列中等待，直到服务能处理。最终...
  c21_b03.md: ...弃**：超出服务容量的入站请求立即被拒绝，返回 “太忙” 响应。调用者快速知道，并可决定重试、故障转移或向用户失败。  Load shedding（选项 b）在级联故障条件下结构上更好，因为它避免导致放大的队列堆积。  实现：每个服务跟踪当前并发度...
  c21_b03.md: ...调用者优雅处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值。延迟升高时阈值降低；延迟正常时阈值升高...
  c21_b03.md: ...看到扩散时，已来不及干预。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸。继电器就是 load shedding 机制。没有它们，级联会更糟（整个发电厂受损）。  (d) **系统设计必须遏制故障**。电网为效率互...
  c21_b03.md: ...tart” 孤岛，可在级联期间隔离。  分布式系统类比：微服务舰队为功能互联；互联成为级联路径。防御（重试预算、熔断器、load shedding）必须在每个节点运行；仅集中监控无法防止级联。...
  c21_b04.md: ...触发，但新节点需要 5 分钟才能就绪。  **恢复**：  * 14:35，运维手动在 merchant 数据库上调用 load shedding（只接受最高优先级流量）。 * 这让已在 flight 的查询完成；数据库开始恢复。 * 14:42，...
  c21_b05.md: ...延迟增加，进而产生更多放大，进而产生更多延迟。  防御针对放大：重试预算限制重试驱动的放大；熔断器停止向故障组件加负载；load shedding 防止队列堆积。  **Q2.** 什么是重试预算，它如何限制放大？  提示  重试预算：把重试流量限...
  c21_b06.md: ...**防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸隔离故障。分布式系统类比：每个服务应有自己的熔断器、load shedding、重试预算；集中式防御在网络分区时会失效。  (d) **系统设计必须遏制故障**。互联电网（为共享发...
  c21_b07.md: ...使用无预算的重试，没有熔断器。他们想逐步增加弹性。改变顺序是什么？  提示  最高杠杆优先：重试预算，然后熔断器，然后 load shedding。  **答案：** 推荐顺序：  (a) **每个客户端加重试预算**：实现重试预算可防止最常见的放...
  c21_b07.md: ...ence4j（Java）、其他语言 Hystrix-like 库，或 Envoy 代理。  (c) **每个服务入口加 load shedding**：每个服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工...
  c21_b07.md: ...确保每个服务按 2 倍正常负载配置。缓冲吸收尖峰而不饱和。  (e) **为以上加可观测性**：监控重试率、熔断器状态、load-shedding 计数器。任何异常时告警；告警表明正在发生事故。...
  c21_b08.md: ...电厂**：产生工作供下游消费的源服务。等价：产生交易事件的支付服务。  * **发电厂跳闸（离线自保）**：过载服务的 load shedding。  * **电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blacks...
  c21_b10.md: ...时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load shedding 防止队列堆积。用 HTTP 503 拒绝超额请求；调用者快速知道并可故障转移。  2003 年美国东...
  c22_b06.md: ...有滞后时，熔断器在每个状态稳定，但对真实转换响应迅速。  该模式可泛化：任何翻转行为的信号都应有双向不同阈值。自动扩缩、load shedding、领导者选举都受益于滞后。没有它，系统会在状态间 “拍动”；有它，系统稳定但响应迅速。  **Q4.*...
  c22_b08.md: ...接受；更快恢复值得。  * 对没有良好限流工具的系统：重启（因为限流无法有效完成）。  比二选一更好：让系统不需要选择。load shedding（自动 per-node 限流）、shard 隔离（一个 shard 的亚稳态不扩散）、优雅降级（高负...
  c22_b08.md: ...色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的...
  c22_b08.md: ...接受；更快恢复值得。  * 对没有良好限流工具的系统：重启（因为限流无法有效完成）。  比二选一更好：让系统不需要选择。load shedding（自动 per-node 限流）、shard 隔离（一个 shard 的亚稳态不扩散）、优雅降级（高负...
  c22_b08.md: ...色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的...
  c24_b04.md: ...产推理集群（如 Tobias 在 Continuum）如下：                        ┌──── Load Balancer ────┐                      │...
  c25_b02.md: ...### 25.2 分离式架构  拓扑可视化：       graph TB         LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Ti...
  c26_b01.md: ...秒转圈等待。  本章讲 2024-2026 年出现的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative decodin...

=== bound (25 occurrences) ===
  c03_b07.md: ...ce（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业等价物。  §3.5 描述的 HLC 路径是大多数非...
  c03_b07.md: ...），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业等价物。  §3.5 描述的 HLC 路径是大多数非 Google 系统的选择。C...
  c03_b14.md: ...能负担 GPS 基础设施且工作负载中重启成本超过 commit-wait 成本的团队是正确答案。AWS 的 clock-bound API（Amazon Time Sync Service）首次在 Google 之外提供 TrueTime 风格能力，...
  c03_b15.md: ...anner 用 TrueTime 支付 7 ms commit-wait，换取线性化全局时间戳。AWS 的 clock-bound API 正在让 TrueTime 类原语在云厂商上可用。HLC 加已知偏移上界是该领域其余团队的实用替代。  本章开头...
  c13_b05.md: ...） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + skew bou...
  c13_b05.md: ...und（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Sequencer...
  c13_b05.md: ...ueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到 CockroachDB / TiDB 采用此路径。...
  c13_b16.md: ...对大多数工作负载很小。  综合：TrueTime 是更干净的方案；HLC 对大多数团队更实用。AWS 引入 clock-bound API 正在缩小差距；5 年内我们可能看到 HLC 系统迁移到 clock-bound API，差别变得无关紧要。目前...
  c13_b16.md: ...实用。AWS 引入 clock-bound API 正在缩小差距；5 年内我们可能看到 HLC 系统迁移到 clock-bound API，差别变得无关紧要。目前选择是运维性的：若你在 GCP 且成本可接受，用 TrueTime；若你在 AWS/Az...
  c13_b17.md: ...e 替代。成本可变（争用时重启）而非恒定（每次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide 的跨区域支付事务在 Cockro...
  c24_b01.md: ...l（处理 prompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV ca...
  c24_b01.md: ...token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV cache 作为中心状态**。KV cache...
  c24_b08.md: ...-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节...
  c24_b08.md: ...ted prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；decode 节点...
  c24_b11.md: ...25章进入 2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLL...
  c24_b11.md: ...saggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated pre...
  c25_b01.md: ...ill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不...
  c25_b01.md: ...（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。  本章是工...
  c25_b05.md: ...解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefil...
  c25_b05.md: ...明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 pro...
  c25_b11.md: ...ggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优...
  c25_b11.md: ...-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优硬件；相同延迟下吞吐提升 2-4 倍。  Splitwise（...
  c29_b01.md: ...ck-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；API 已可用但尚未被普遍消费。  2027-2...
  c29_b01.md: ...* 预计 **CockroachDB、TiDB、YugabyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增加类似 cl...
  c29_b01.md: ...作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增加类似 clock-bound 服务；云厂商在基础设施上竞争。 * 本地 **GPS + PTP** 基础设施变便宜；大型金融公司正在为关键任务工作负...

=== candidate (25 occurrences) ===
  c09_b02.md: ...ection timeout_，通常随机在 150 ms 到 300 ms 之间），它变为 _candidate_。  Candidate：    1. 递增 `currentTerm`。   2. 给自己投票。   3. 重置选举定时器。   4. 向所有...
  c09_b02.md: ...所有其他服务器发送 `RequestVote` RPC。  每个服务器收到 `RequestVote` 后：  * 若 candidate 的 term 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。...
  c09_b02.md: ...rm 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leade...
  c09_b02.md: ...erm 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 fol...
  c09_b02.md: ...第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两个 candidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重选在统计上不可能；几轮后某个 candidat...
  c09_b02.md: ...andidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重选在统计上不可能；几轮后某个 candidate 的定时器显著提前触发并获胜。   #### “日志最新”规则  Candidate 的日志被认为是“最新”的，当满足以...
  c09_b02.md: ...同时重选在统计上不可能；几轮后某个 candidate 的定时器显著提前触发并获胜。   #### “日志最新”规则  Candidate 的日志被认为是“最新”的，当满足以下之一：  (a) candidate 最后一条日志条目的 term 高于 vote...
  c09_b02.md: ...e 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的...
  c09_b02.md: ...且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的日志比某些已提交条目短，该条目可能“丢失”（leader 会覆盖它）。up-to-date 规则防止这种情况。...
  c09_b02.md: ...ection timeout_，通常随机在 150 ms 到 300 ms 之间），它变为 _candidate_。  Candidate：    1. 递增 `currentTerm`。   2. 给自己投票。   3. 重置选举定时器。   4. 向所有...
  c09_b02.md: ...所有其他服务器发送 `RequestVote` RPC。  每个服务器收到 `RequestVote` 后：  * 若 candidate 的 term 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。...
  c09_b02.md: ...rm 低于自己的，拒绝。 * 若该服务器已在此 term 投过票，拒绝（每个服务器每 term 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leade...
  c09_b02.md: ...erm 最多投一票）。 * 若 candidate 的日志至少与自己的一样新（见下文），授予投票。  收集到多数投票的 candidate 成为 leader。Leader 的第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 fol...
  c09_b02.md: ...第一个动作是发送心跳（空的 `AppendEntries` RPC）给所有 follower，宣示领导地位。  若两个 candidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重选在统计上不可能；几轮后某个 candidat...
  c09_b02.md: ...andidate 平分选票（都未获多数），两者等待下一次选举超时并重试。随机化超时让同时重选在统计上不可能；几轮后某个 candidate 的定时器显著提前触发并获胜。   #### “日志最新”规则  Candidate 的日志被认为是“最新”的，当满足以...
  c09_b02.md: ...同时重选在统计上不可能；几轮后某个 candidate 的定时器显著提前触发并获胜。   #### “日志最新”规则  Candidate 的日志被认为是“最新”的，当满足以下之一：  (a) candidate 最后一条日志条目的 term 高于 vote...
  c09_b02.md: ...e 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的...
  c09_b02.md: ...且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的日志比某些已提交条目短，该条目可能“丢失”（leader 会覆盖它）。up-to-date 规则防止这种情况。...
  c09_b06.md: ...** ：确保索引 i 一旦提交，未来任何 leader 都不会在同一索引提交不同条目。机制是选举限制（只有日志足够新的 candidate 能赢）与 leader 只追加日志纪律的结合。  **Q2.** 什么是 Raft 的“term”？它与 Paxos...
  c09_b06.md: ...被接受）。 * Paxos ballot 可由任何 proposer 随时自由创建。Raft term 只由启动选举的 candidate 创建，term 序列因此更有序。 * Paxos 没有“term leader”概念；Multi-Paxos 有 le...
  c09_b06.md: ...为 200 ms，leader 故障后所有 follower 的定时器大约在同时触发。多个 follower 同时成为 candidate，同时请求投票，平分选票（没人获多数）。集群重复选举，很可能再次平分。  随机化超时（例如 150 ms 到 300 m...
  c09_b06.md: ...票（没人获多数）。集群重复选举，很可能再次平分。  随机化超时（例如 150 ms 到 300 ms 均匀随机）让同时 candidate 在统计上不可能。几轮后，一个 follower 的定时器显著提前触发，它在其他人定时器触发前获胜。  这个随机化是细小...
  c09_b09.md: ...正常进行。  (b) 若原 leader 在少数侧，多数侧 follower 的定时器触发（收不到心跳）。其中一个成为 candidate，从 3 中 2 获得投票（多数），成为 leader。新提交继续。  (c) 无论哪种，多数侧继续服务写和线性化读。...
  c09_b09.md: ...）。写阻塞。  (b) 若原 leader 在多数侧，少数侧 follower 等待心跳。它们定时器触发。其中一个成为 candidate，向少数侧另一节点请求投票。它得到 2 票（自己和另一节点），但 2 < 3，因此不能成为 leader。反复重选失败。...
  c09_b10.md: ...。** 每个新 term 开始时必须清空投票收集状态。若 LLM 跨 term 缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上 apply 的混淆。** 条目在多数复制后 _提交_；只有在...

=== marker (25 occurrences) ===
  c15_b02.md: ...记录自身状态。(b) 向每条出通道发送一个 _marker_。(c) 开始记录每条入通道上收到的所有消息（直到该通道的 marker 到达）。  其他每个进程首次收到 marker 时：  (a) 记录自身状态。(b) 记录收到 marker 的通道为...
  c15_b02.md: ...arker_。(c) 开始记录每条入通道上收到的所有消息（直到该通道的 marker 到达）。  其他每个进程首次收到 marker 时：  (a) 记录自身状态。(b) 记录收到 marker 的通道为 “快照期间为空”（该通道消息不属于快照）。(c...
  c15_b02.md: ...该通道的 marker 到达）。  其他每个进程首次收到 marker 时：  (a) 记录自身状态。(b) 记录收到 marker 的通道为 “快照期间为空”（该通道消息不属于快照）。(c) 向每条出通道发送 marker。(d) 开始在每条 _其他...
  c15_b02.md: ...自身状态。(b) 记录收到 marker 的通道为 “快照期间为空”（该通道消息不属于快照）。(c) 向每条出通道发送 marker。(d) 开始在每条 _其他_ 入通道上记录消息，直到各通道都收到 marker。  当进程在已经开始记录的通道上收到...
  c15_b02.md: ...不属于快照）。(c) 向每条出通道发送 marker。(d) 开始在每条 _其他_ 入通道上记录消息，直到各通道都收到 marker。  当进程在已经开始记录的通道上收到 marker 时，停止在该通道上记录；已记录的消息就是该通道的在途集合。...
  c15_b02.md: ...。(d) 开始在每条 _其他_ 入通道上记录消息，直到各通道都收到 marker。  当进程在已经开始记录的通道上收到 marker 时，停止在该通道上记录；已记录的消息就是该通道的在途集合。...
  c15_b03.md: ...#### 为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息...
  c15_b03.md: ...#### 为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到...
  c15_b03.md: ...为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送...
  c15_b03.md: ...令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送之后到达）。  记录的状态（各进程...
  c15_b03.md: ...，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送之后到达）。  记录的状态（各进程状态 + 各通道在途消息...
  c15_b03.md: ...he Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “checkpoint barrier”）流过数据流图。每个算子在 barrier 经过时记录状...
  c15_b03.md: ...#### 为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息...
  c15_b03.md: ...#### 为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到...
  c15_b03.md: ...为何有效  Marker 是同步令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送...
  c15_b03.md: ...令牌。Marker 之后，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送之后到达）。  记录的状态（各进程...
  c15_b03.md: ...，快照前发送的消息不可能到达（因为 FIFO 顺序保证任何在 marker 前发送的消息都会在 marker 前到达）。Marker 之前，快照后发送的消息不可能到达（因为 marker 在发送之后到达）。  记录的状态（各进程状态 + 各通道在途消息...
  c15_b03.md: ...he Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “checkpoint barrier”）流过数据流图。每个算子在 barrier 经过时记录状...
  c15_b06.md: ...需要线性化。  **Q3.** 描述 Chandy-Lamport 快照协议。记录的状态代表什么？  提示  每条通道 marker；每个进程首次收到 marker 时记录；捕获在途消息。  **答案：** 协议：...
  c15_b06.md: ...描述 Chandy-Lamport 快照协议。记录的状态代表什么？  提示  每条通道 marker；每个进程首次收到 marker 时记录；捕获在途消息。  **答案：** 协议：...
  c15_b07.md: ...状态并向每条出通道发送 _marker_。发起者开始在每条入通道上记录收到的所有消息。  (b) 其他每个进程首次收到 marker 时，记录自身状态，将收到 marker 的通道标记为 “快照期间为空”，向每条出通道发送 marker，并开始在每条其...
  c15_b07.md: ...。发起者开始在每条入通道上记录收到的所有消息。  (b) 其他每个进程首次收到 marker 时，记录自身状态，将收到 marker 的通道标记为 “快照期间为空”，向每条出通道发送 marker，并开始在每条其他入通道上记录消息。  (c) 当进程在...
  c15_b07.md: ...个进程首次收到 marker 时，记录自身状态，将收到 marker 的通道标记为 “快照期间为空”，向每条出通道发送 marker，并开始在每条其他入通道上记录消息。  (c) 当进程在已开始记录的通道上收到 marker 时，停止在该通道记录；已记...
  c15_b07.md: ...间为空”，向每条出通道发送 marker，并开始在每条其他入通道上记录消息。  (c) 当进程在已开始记录的通道上收到 marker 时，停止在该通道记录；已记录的消息就是在途集合。  记录的状态代表什么：系统的 _一致全局状态_。各进程状态加上各通道...
  c15_b11.md: ...提供它们。  Chandy-Lamport 分布式快照是在无全局时钟下捕获一致全局状态的基础原语。协议使用每条通道上的 marker 定义快照边界；记录的各进程状态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly...

=== job (25 occurrences) ===
  c18_b03.md: ...个_ 常被混淆的目的。  (a) **效率**：同一时间只有一个客户端做昂贵工作。若两个同时做，工作重复但无损害。示例：job queue 中每个任务应由一个 worker 处理。  (b) **正确性**：同一时间只有一个客户端修改受保护资源。...
  c18_b03.md: ...错了；不要用。” 这过于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  * Job queue：用 Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 *...
  c18_b03.md: ...个_ 常被混淆的目的。  (a) **效率**：同一时间只有一个客户端做昂贵工作。若两个同时做，工作重复但无损害。示例：job queue 中每个任务应由一个 worker 处理。  (b) **正确性**：同一时间只有一个客户端修改受保护资源。...
  c18_b03.md: ...错了；不要用。” 这过于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  * Job queue：用 Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 *...
  c18_b07.md: ...：只有一个 worker 应同时做昂贵工作。若偶尔两个 worker 都做（因锁失败），重复工作浪费但无害。  示例：job queue 去重、周期批处理任务调度、昂贵计算记忆化。  (b) 下游影响是幂等的：即使两个 worker 都执行，效...
  c18_b07.md: ...锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 f...
  c18_b08.md: ...#### 应用问题  **Q1.** 一个团队实现 job queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing t...
  c18_b08.md: ...a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有不同成本/安全权衡。  **答案：**  (a) **Redloc...
  c18_b08.md: ...ock 即可。最简单最快。 * 非幂等任务：若已有数据库，选 (c)；若无，选 (b)。两者都提供正确性。 * 高吞吐 job queue（每秒数千任务）：专用系统（Kafka with consumer groups、Cloud Pub/Sub...
  c18_b08.md: ...系统（Kafka with consumer groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原...
  c18_b08.md: ...er groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现...
  c18_b08.md: ...b/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现代系统通常可围绕专用队列基础设施设计...
  c18_b08.md: ...#### 应用问题  **Q1.** 一个团队实现 job queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing t...
  c18_b08.md: ...a) Redlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有不同成本/安全权衡。  **答案：**  (a) **Redloc...
  c18_b08.md: ...ock 即可。最简单最快。 * 非幂等任务：若已有数据库，选 (c)；若无，选 (b)。两者都提供正确性。 * 高吞吐 job queue（每秒数千任务）：专用系统（Kafka with consumer groups、Cloud Pub/Sub...
  c18_b08.md: ...系统（Kafka with consumer groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原...
  c18_b08.md: ...er groups、Cloud Pub/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现...
  c18_b08.md: ...b/Sub）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现代系统通常可围绕专用队列基础设施设计...
  c28_b15.md: ...构 bug；找出缺失的防御。  **答案：** 常见模式：一个 worker 池消费队列；每个 worker 处理一个 job；偶尔一个 job 被处理两次。  _谎言_：身份谎言（worker 假设自己唯一处理这个 job）结合故障谎言（wor...
  c28_b15.md: ...失的防御。  **答案：** 常见模式：一个 worker 池消费队列；每个 worker 处理一个 job；偶尔一个 job 被处理两次。  _谎言_：身份谎言（worker 假设自己唯一处理这个 job）结合故障谎言（worker 假设 “我...
  c28_b15.md: ...orker 处理一个 job；偶尔一个 job 被处理两次。  _谎言_：身份谎言（worker 假设自己唯一处理这个 job）结合故障谎言（worker 假设 “我没崩溃，所以我在处理”）。  _发生了什么_：worker A 拉出 job 4...
  c28_b15.md: ...个 job）结合故障谎言（worker 假设 “我没崩溃，所以我在处理”）。  _发生了什么_：worker A 拉出 job 42，开始处理，然后暂停（GC pause）。队列的可见性超时到期；worker B 拉出 job 42，开始处理。两...
  c28_b15.md: ...rker A 拉出 job 42，开始处理，然后暂停（GC pause）。队列的可见性超时到期；worker B 拉出 job 42，开始处理。两者都完成。job 被处理两次；下游副作用发生两次。  _缺失的防御_：下游副作用的幂等性。若下游是幂...
  c28_b15.md: ...始处理，然后暂停（GC pause）。队列的可见性超时到期；worker B 拉出 job 42，开始处理。两者都完成。job 被处理两次；下游副作用发生两次。  _缺失的防御_：下游副作用的幂等性。若下游是幂等的（使用 idempotency...
  c30_b19.md: ...示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §15.4（Ch...

=== sharding (25 occurrences) ===
  c24_b08.md: ...利用率 80%+，在 120 万美元账单上每月节省 25-40 万美元。  其他技术（简述）：跨节点 KV cache sharding（第25章）；speculative decoding（第26章）；模型量化（8-bit、4-bit serving 用...
  c24_b10.md: ...用；不需要新理论。”  **讨论：** 辩护：分布式系统理论（共识、复制、分片、负载均衡）适用于推理。KV cache sharding 就是分片；推理节点间负载均衡就是负载均衡；故障转移就是故障转移。通用模式足够。  反驳：推理有结构属性（per-tok...
  c24_b10.md: ...理专用技术。  第二个考虑：推理领域快速演进。2025-2026 突破（disaggregation、KV cache sharding、speculative decoding as distributed protocol）是近期；2027-2030...
  c24_b11.md: ...本章将走工作流 12：vLLM 集群加 disaggregated prefill/decode + KV cache sharding 图。  ---...
  c25_b01.md: ...12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Splitwise、DistServe）；生产部署在 2025-2026...
  c25_b03.md: ...大约 50+ 节点是分离开始划算的地方。低于此，运维开销超过效率收益。   ### 25.4 跨节点 KV Cache Sharding  一个独立但相关的技术：对超长上下文，把 KV cache 分布到多个 decode 节点。  对 1M token...
  c25_b05.md: ...，收益超过成本；对小集群不是。2026 年采用阈值大致是集群规模 50+。  **Q2.** 什么是 KV cache sharding，它解决什么问题？  提示  长上下文产生巨大 KV cache；跨多节点分布；缓存层抽象。  **答案：** KV c...
  c25_b05.md: ...么问题？  提示  长上下文产生巨大 KV cache；跨多节点分布；缓存层抽象。  **答案：** KV cache sharding 把单个请求的 KV cache 分布到多张 GPU（可能跨多个节点）。KV cache 被分成块（PagedAtten...
  c25_b09.md: ...报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分...
  c25_b09.md: ...el sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sharding（tensor p...
  c25_b09.md: ...ion：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sharding（tensor parallelism、pipeline parallelism）_：把模型权重和计算跨多张 GPU 拆...
  c25_b09.md: ...有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一张 GPU 放不下；分离因为阶段有不同最...
  c25_b09.md: ...模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一张 GPU 放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B...
  c25_b09.md: ...放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B）可放一张 GPU，不需要 sharding。小集群部署（< 50 节点）可能不需要分离。对 Continuum 设置（70B+ 模型、50+ 节点），两者都适用。...
  c25_b09.md: ...报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分...
  c25_b09.md: ...el sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sharding（tensor p...
  c25_b09.md: ...ion：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sharding（tensor parallelism、pipeline parallelism）_：把模型权重和计算跨多张 GPU 拆...
  c25_b09.md: ...有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一张 GPU 放不下；分离因为阶段有不同最...
  c25_b09.md: ...模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两者都需要：sharding 因为模型太大一张 GPU 放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B...
  c25_b09.md: ...放不下；分离因为阶段有不同最优硬件。两种技术组合；工程复杂度也复合。  小模型（7B、13B）可放一张 GPU，不需要 sharding。小集群部署（< 50 节点）可能不需要分离。对 Continuum 设置（70B+ 模型、50+ 节点），两者都适用。...
  c25_b10.md: ...* 前缀共享（SGLang）：重复 prompt 工作负载 20-50% 降低。 * 超长上下文的 KV cache sharding：否则不可行的工作负载得以运行。  (b) **引擎优化**：  * 量化（8-bit 或 4-bit serving）...
  c25_b11.md: ...ncake、LMCache）实现这些模式。采用阈值：约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨...
  c26_b01.md: ...bias 的 Continuum 集群完整优化故事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、speculative decoding（本章）。这些部分组合...
  c26_b04.md: ...后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-cache-aware 路由（前缀重工作负载重大收益）+ 长度预测路由（长...
  c26_b11.md: ...d fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + w...

=== jepsen (24 occurrences) ===
  c01_b11.md: ...，key 过期，别人接管。”指出该设计未处理的三种失败模式。需要时可参考第18章。  提示  该模式是 Redlock。Jepsen / Kleppmann 辩论至少指出三种失败模式。想想 GC 暂停、到 Redis 的网络分区、以及旧领导者醒来时会发...
  c03_b11.md: ...e-timestamp，于是保留写入，实际上复活了记录。  这就是经典的 Cassandra “复活删除”失败模式，在 Jepsen 分析中有详尽记录。修复方案：  * 确保严格 NTP 纪律；当 NTP 漂移超过小上界（10 到 50 ms）时告警。...
  c05_b04.md: ...记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5...
  c23_b03.md: ...### 23.5 Jepsen 传统  Kyle Kingsbury 的 Jepsen 项目是混沌的另一种风味：不是 “向生产注入故障”，而是 “通过...
  c23_b03.md: ...### 23.5 Jepsen 传统  Kyle Kingsbury 的 Jepsen 项目是混沌的另一种风味：不是 “向生产注入故障”，而是 “通过注入故障系统性地测试分布式数据库的声明”。  Jepse...
  c23_b03.md: ...epsen 项目是混沌的另一种风味：不是 “向生产注入故障”，而是 “通过注入故障系统性地测试分布式数据库的声明”。  Jepsen 测试：  * 在可控条件下运行分布式数据库。 * 注入特定故障（网络分区、时钟偏移、慢节点）。 * 观察数据库的正确性...
  c23_b03.md: ...节点）。 * 观察数据库的正确性声明（线性化、快照隔离等）是否成立。  发现结果很残酷。几乎每个主要分布式数据库首次被 Jepsen 测试时都违反了某些声明。这些发现推动了行业改进：厂商修复 bug；工程师学会验证什么；整个领域的正确性基线提高。  著...
  c23_b03.md: ...试时都违反了某些声明。这些发现推动了行业改进：厂商修复 bug；工程师学会验证什么；整个领域的正确性基线提高。  著名 Jepsen 发现：  * **Cassandra（多次）**：轻量事务违反线性化；计数器不一致；故障转移期间丢失写。 * **Mo...
  c23_b03.md: ...k 问题（第18章主题）；各种故障转移 bug。 * **etcd**：旧版本有轻微一致性 issue；已纳入修复。  Jepsen 报告是任何分布式系统工程师的必读。它们是声明真实性的经验证据。...
  c23_b07.md: ...工程同时测试系统和团队。系统防御在赛后数据中可见；团队防御在赛后讨论中可见。两者都需要强大。  **Q4.** 什么是 Jepsen 项目，为什么它对分布式系统领域重要？  提示  Jepsen：系统性地测试分布式数据库声明。发现许多 bug；提高领域...
  c23_b07.md: ...后讨论中可见。两者都需要强大。  **Q4.** 什么是 Jepsen 项目，为什么它对分布式系统领域重要？  提示  Jepsen：系统性地测试分布式数据库声明。发现许多 bug；提高领域正确性基线。  **答案：** Jepsen 是 Kyle K...
  c23_b07.md: ...重要？  提示  Jepsen：系统性地测试分布式数据库声明。发现许多 bug；提高领域正确性基线。  **答案：** Jepsen 是 Kyle Kingsbury 的项目（约 2013 年启动），在故障条件下系统性地测试分布式数据库的正确性声明。...
  c23_b07.md: ...oDB read concern、CockroachDB 早期可串行化、Redis 故障转移、etcd 旧版本——都有 Jepsen 发现的 bug。  为何重要：  (a) **经验证据**：Jepsen 报告是声明真实性的经验证据。没有它们，厂商声...
  c23_b07.md: ...edis 故障转移、etcd 旧版本——都有 Jepsen 发现的 bug。  为何重要：  (a) **经验证据**：Jepsen 报告是声明真实性的经验证据。没有它们，厂商声明只是营销；有了它们，声明可测试。  (b) **行业改进**：厂商修复...
  c23_b07.md: ...报告是声明真实性的经验证据。没有它们，厂商声明只是营销；有了它们，声明可测试。  (b) **行业改进**：厂商修复 Jepsen 发现的 bug。领域正确性基线提高。  (c) **工程师教育**：读 Jepsen 报告的工程师学会验证什么、怀疑什...
  c23_b07.md: ...b) **行业改进**：厂商修复 Jepsen 发现的 bug。领域正确性基线提高。  (c) **工程师教育**：读 Jepsen 报告的工程师学会验证什么、怀疑什么。报告是分布式系统工程师必读。  (d) **标准方法**：Jepsen 的方法（形...
  c23_b07.md: ...育**：读 Jepsen 报告的工程师学会验证什么、怀疑什么。报告是分布式系统工程师必读。  (d) **标准方法**：Jepsen 的方法（形式化模型、随机测试、历史验证）已成为测试分布式系统正确性的标准方法。  Jepsen 项目是过去十年对分布式...
  c23_b07.md: ...d) **标准方法**：Jepsen 的方法（形式化模型、随机测试、历史验证）已成为测试分布式系统正确性的标准方法。  Jepsen 项目是过去十年对分布式系统工程最重要的贡献之一。它让正确性在该领域变得可测试，而以前验证只是愿景。...
  c23_b08.md: ...对 Crescent：团队在采用任何新分布式数据库前都会咨询 Jepsen 报告。报告是评估候选者的诊断方法（第11章）的一部分。...
  c23_b13.md: ...（Chaos Mesh、Litmus、Gremlin、AWS FIS、Toxiproxy）实现故障注入；选择取决于部署。Jepsen 项目是另一种风味：系统性地在故障下测试数据库正确性声明。  成熟度模型：Level 0（无混沌）、Level 1（手动...
  c27_b10.md: ...第23章的混沌工程和 Jepsen 传统是答案。真实分布式系统验证需要真实分布式系统测试。通过单元测试的 LLM 生成代码并未验证分布式行为；验证来自混沌...
  c28_b05.md: ...亚稳态）、E.2（Roblox）、E.6（BigQuery） | | 23 | 混沌工程 | 每月 game day、Jepsen 风格验证 | E.4（CrowdStrike 无 canary）、E.7（Fastly 无 canary） | | 2...
  c30_b03.md: ...Reliability Engineering_. O'Reilly. — 重试预算和熔断器作为生产纪律。 * The Jepsen reports (https://jepsen.io) — 分布式数据库正确性的经验测试；必读。 * 2003 Nor...
  c30_b03.md: .... O'Reilly. — 重试预算和熔断器作为生产纪律。 * The Jepsen reports (https://jepsen.io) — 分布式数据库正确性的经验测试；必读。 * 2003 Northeast Blackout final NE...

=== timeout (24 occurrences) ===
  c02_b01.md: ...并报告连接断开。HTTP 构建在 TCP 之上，继承了这一点。当你的代码调用 `http.post(url)` 并收到 timeout 时，你不知道请求是否已被处理；你只知道你没收到响应。  仅此一条谬误就是第1章第三个谎言（失败谎言）的根源。所有假设“...
  c02_b07.md: ...response = http.get(url); parse(response)`，没有 try/except、没有 timeout、没有重试。假设调用总是返回可解析响应。  _延迟为零。_ 示例：`for item in million_items:...
  c05_b09.md: ...制追上，或超时后拒绝。             sleep(1ms)             if elapsed > timeout:                 raise StaleReplicaError         return rea...
  c21_b09.md: ...意义的用户错误。  “韧性” 代码应类似：       @circuit_breaker(threshold=0.5, timeout=30)     @retry(max_retries=3, budget=0.1, backoff=Exponenti...
  c21_b09.md: ...ries=3, budget=0.1, backoff=ExponentialBackoff(max=5))     @timeout(seconds=2)     def call_downstream():         return http.g...
  c27_b01.md: ...码，遗漏了 “请求在服务端已成功但响应丢失” 的情况。    2. **网络可靠**。Deutsch 的第一谬误。没有 timeout、retry、idempotency 的代码。    3. **机器时钟说真话**。跨机器比较时间戳的代码。    4....
  c27_b02.md: ...er_id):         return profile_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fallback。代码假设调用最终会返回。  现实：网络...
  c27_b02.md: ...游有时会失败。没有防御，这一行可能让调用服务无限挂起，促成级联故障，造成 p99 延迟灾难。  修复：每个网络调用应有 timeout、retry budget、circuit breaker，以及（若可能）fallback。       @circui...
  c27_b02.md: ...aker，以及（若可能）fallback。       @circuit_breaker(threshold=0.5, timeout=30)     @retry(max_retries=3, budget=0.1)     @timeout(seco...
  c27_b02.md: ...0.5, timeout=30)     @retry(max_retries=3, budget=0.1)     @timeout(seconds=2)     def get_user_profile(user_id):         try:...
  c27_b05.md: ...调用必须幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联。  (c) **带预算的重试**：每个重试必须有预算（占成功流量的百分比上限）。否则重试风暴。  (d)...
  c27_b06.md: ...ry budget；`time.time()` 用在看起来像跨机器上下文的地方；`requests.get()` 没有 timeout（库默认 _没有_ timeout）。 * **库习惯不匹配**：不需要的地方用 legacy Hystrix 装饰器；...
  c27_b06.md: ...ime()` 用在看起来像跨机器上下文的地方；`requests.get()` 没有 timeout（库默认 _没有_ timeout）。 * **库习惯不匹配**：不需要的地方用 legacy Hystrix 装饰器；`redis.set("...",...
  c27_b07.md: ...）**：  * 使用 Greptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动...
  c27_b08.md: ...需要领域知识的部分。  价值增长的技能：_阐明为什么_ 检查清单项适用或不适用。AI 标记 “这个 HTTP 调用没有 timeout”，人类工作要么是确认 “是的，加上 timeout，正确值是 X”，要么是反驳 “这是故意的；这是不应 timeout...
  c27_b08.md: ..._ 检查清单项适用或不适用。AI 标记 “这个 HTTP 调用没有 timeout”，人类工作要么是确认 “是的，加上 timeout，正确值是 X”，要么是反驳 “这是故意的；这是不应 timeout 的 streaming endpoint；AI 没...
  c27_b08.md: ...timeout”，人类工作要么是确认 “是的，加上 timeout，正确值是 X”，要么是反驳 “这是故意的；这是不应 timeout 的 streaming endpoint；AI 没看到上下文。” 这种阐明是审查者的贡献。  价值下降的技能：做 AI...
  c27_b08.md: ...endpoint；AI 没看到上下文。” 这种阐明是审查者的贡献。  价值下降的技能：做 AI 的工作（肉眼扫描缺失 timeout）。这部分自动化了；让高级工程师花时间做它是错配。   #### 失败模式：对 AI 审查 PR 橡皮图章  特定反模式...
  c27_b09.md: ...CP 式语义**：代码假设响应表示请求结果；不处理 “服务端成功但响应丢失”。    2. **网络可靠**：代码没有 timeout、retry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比...
  c27_b09.md: ...: handle()`。无幂等；重试造成重复工作。  （谎言 2）`result = http.get(url)`。无 timeout、retry、circuit breaker；挂起级联。  （谎言 3）`sorted(events, key=lamb...
  c27_b10.md: ...络调用应幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联上游。  (c) **带预算的重试**：重试应受预算约束（占成功流量百分比上限）。否则重试风暴。  (d)...
  c27_b11.md: ...行看到全部六个谎言。构造一个包含它们的合理代码片段。  提示  一个跨 region 支付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合...
  c27_b12.md: ...2. **谎言 2（网络可靠）**：任何网络调用（bank、db、notification、ledger）都没有 timeout。一个慢调用挂起整个函数。    3. **谎言 3（时钟）**：`ledger_service.record(paym...
  c27_b12.md: ...具体：bank_service.transfer 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token...

=== partition (24 occurrences) ===
  c02_b04.md: ...CAP 只命名了分区情况，忽略了更常见的情况：网络健康时。健康情况下的权衡在延迟与一致性之间。  PACELC：若发生 Partition，则 Availability 或 Consistency；否则（Else），则 Latency 或 Consisten...
  c02_b08.md: ...、(d) 使用 QUORUM 读/写的 Cassandra，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Availability vs Consistency；否则（Else），Latency vs Consisten...
  c04_b09.md: ...性处理此情况；缺乏幂等性会导致双重处理。  第1章的 Crescent 支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition key（通常是 transaction ID）幂等。重复扣款 bug...
  c04_b09.md: ...的 Crescent 支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition key（通常是 transaction ID）幂等。重复扣款 bug 是因为幂等检查没有强一致。  **Q2.** 用...
  c08_b04.md: ...后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。...
  c11_b03.md: ...钟）使 leader-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition behavior | CP——少数侧停止接受写；TrueTime 确保分区后时间戳一致 PACELC | PC / E...
  c11_b03.md: ...sistency model | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停止接受写 PACELC | PC / EC（分区下一致；无分区时为不确定性窗口读...
  c11_b04.md: ...存储层对用户不透明）；自动扩缩容与自动修复 Consistency model | 默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 per-item 线性化 PACELC | 默认 PA / EL...
  c11_b04.md: ...需人工处理 Consistency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线性化 Partition behavior | 按操作可调；低一致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA /...
  c11_b04.md: ...起就以混沌工程为目标 Consistency model | Serializable 事务；ACID 强可串行化保证 Partition behavior | CP——丢失法定人数则拒绝写 PACELC | PC / EC；面向 OLTP 延迟设计 Str...
  c11_b05.md: ...现区域故障切换 Consistency model | Serializable 事务；follower 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC | PC / EC Strengths | 从一开始即为金融...
  c11_b08.md: ...cy model | 复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数 PACELC | PC / EC；优化金融 OLTP...
  c11_b09.md: ...ery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quor...
  c12_b01.md: ...片，但表可显式按 `crdb_internal` shard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering columns，使同一 partition 内数据有序（在哈希 partition...
  c12_b01.md: ...。Cassandra 是哈希分片，但提供 partition key + clustering columns，使同一 partition 内数据有序（在哈希 partition 内实现局部范围查询）。  选择取决于工作负载：纯 OLTP 点查偏向哈希；分析...
  c12_b06.md: ...per-table 提供两者：CockroachDB 默认范围但支持哈希分片表；Cassandra 哈希分片但允许同一 partition 内按 clustering 列排序做局部范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash...
  c13_b10.md: ...2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的...
  c14_b07.md: ...model | Linearizable；提供过期读 | Serializable；提供 follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestamp source | TrueT...
  c15_b04.md: ...#### Kafka  每个 partition 内部顺序强（partition 内原子广播）。跨 partition 顺序不提供；应用必须处理。  复制因子和 `mi...
  c15_b04.md: ...#### Kafka  每个 partition 内部顺序强（partition 内原子广播）。跨 partition 顺序不提供；应用必须处理。  复制因子和 `min.insync.replicas` 配置控制持久性：当 `min.i...
  c15_b04.md: ....insync.replicas` 配置控制持久性：当 `min.insync.replicas` 个副本 ack 后，partition 的写才算持久。若 leader 在传播前故障，写丢失。   ### 15.6 跨一致性模型的计数器工作负载  课程大纲...
  c30_b01.md: ...'s conjecture and the feasibility of consistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 *...
  c30_b11.md: ...**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pu...
  c30_b14.md: ...e store on top     │   └── tests/            — split-brain, partition, recovery scenarios     ├── ch12_lsm/     │   └── tiny_lsm....

=== cluster (24 occurrences) ===
  c02_b11.md: ...每种操作类型，给出其 PACELC 分类。  **答案：** 这是第四部分其余章节将训练你掌握的内容。一个 Redis Cluster 工作示例：...
  c02_b12.md: ...Redis Cluster：分区时，被分区出去的节点对其不再拥有的哈希槽拒绝写入（可通过 `cluster-require-full-covera...
  c02_b12.md: ...Redis Cluster：分区时，被分区出去的节点对其不再拥有的哈希槽拒绝写入（可通过 `cluster-require-full-coverage` 配置）。正常运营下，从副本读不是线性化的；它们最终一致于主节点。对主节点...
  c12_b01.md: ...于：Dynamo、Cassandra（默认 partitioner：Murmur3Partitioner）、Redis Cluster。   #### 范围分片  键排序；每个分区拥有一个连续范围。分区边界存在元数据里（协调者、路由表）。  优点：  *...
  c12_b06.md: ...、相关键局部性好、顺序写热点风险。  哈希适合：用户会话键值存储，每个会话按 ID 访问且无范围查询。示例：Redis Cluster、DynamoDB。  范围适合：时序数据库常查询“T1 到 T2 之间的所有事件”。按时间戳范围分片使该查询成为单分区...
  c14_b08.md: ...每晚从 CockroachDB 和 TigerBeetle 复制。  可选补充：  (d) **缓存**：Redis Cluster 用于热读路径（账户余额查询、幂等键检查）。多区域，各区域缓存。  (e) **队列**：Kafka 用于跨服务事件分发...
  c18_b01.md: ...lock 的分析以及 _fencing token_ 的作用。  本章讲授两个相关原语：分布式缓存（具体是 Redis Cluster gossip 与缓存失效模式）和分布式锁（具体是为什么朴素锁会失败、正确原语长什么样）。第1章 Crescent 支付...
  c18_b01.md: ...，后续请求避免重复计算。分布式缓存把这一模式扩展到多个缓存实例，被多个服务访问。  2026 年典型系统是 Redis Cluster（及其商业变体：Redis Enterprise、Amazon ElastiCache for Redis、Redis...
  c18_b01.md: ...prise、Amazon ElastiCache for Redis、Redis Labs）。下面讨论聚焦 Redis Cluster。   #### Redis Cluster gossip  Redis Cluster 是分片、复制的键值存储。键被映...
  c18_b01.md: ...he for Redis、Redis Labs）。下面讨论聚焦 Redis Cluster。   #### Redis Cluster gossip  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot...
  c18_b01.md: ...s）。下面讨论聚焦 Redis Cluster。   #### Redis Cluster gossip  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 slot 属于一个 master...
  c18_b01.md: ...自己的视图。随时间推移，集群对自身的视图收敛。  gossip 不是共识；分区下，集群两半可能有不一致视图。Redis Cluster 要求大多数 master 节点同意配置变更（slot 重分配、主从切换）来处理。分区期间的少数派写失败并返回 `CLU...
  c18_b02.md: ...缓解：纪律；把缓存当缓存，而不是存储。  Crescent 系统在热读路径（账户余额查询、幂等键检查）使用 Redis Cluster。每个缓存条目有短 TTL（60 秒）；未命中时查询 truth source（CockroachDB）。事件驱动失效在...
  c19_b03.md: ...netes headless services** ：“headless” Kubernetes service 没有 cluster IP；客户端解析服务名得到所有 pod IP。Kubernetes DNS 插件透明处理。  对成员稳定的服务（数据库...
  c28_b02.md: ...t 复制；对安全关键操作使用 fencing token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (g) **队列**：每 region 一个 Kafka。异步工作流（通知、下游...
  c28_b02.md: ...alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）...
  c28_b02.md: ...：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、region...
  c28_b02.md: ...t 复制；对安全关键操作使用 fencing token。  (f) **缓存**：每 region 一个 Redis Cluster。热路径：账户余额、幂等性 key。  (g) **队列**：每 region 一个 Kafka。异步工作流（通知、下游...
  c28_b02.md: ...alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）...
  c28_b02.md: ...：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、region...
  c28_b03.md: ...on"]             APIE[API Service]             REDISE[Redis Cluster]             CRDBE[CockroachDB nodes]             STRATUSE[...
  c28_b03.md: ...on"]             APIW[API Service]             REDISW[Redis Cluster]             CRDBW[CockroachDB nodes]             KAFKAW[Ka...
  c30_b10.md: ...eSQL 16，带一或两个 hot-standby 读副本（同步到一个，异步到另一个）。 * **缓存**：Redis Cluster 用于热读路径（TTL 60s；写入时事件驱动失效）。 * **复制**：同步到一个 standby，异步到另一个。RP...
  c30_b10.md: ...方法）。典型：US-East + EU-West + US-West（或类似）。 * **缓存**：区域 Redis Cluster 用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table...

=== text (24 occurrences) ===
  c03_b05.md: ...mirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, t)；若 l' = l_p 则 c_p 加...
  c03_b05.md: ..._p)；先把发送作为本地事件处理。 * **接收** 带 HLC (l_m, c_m) 的消息在进程 p：令 t = \text{wall\_clock\_now}()；计算 l' = \max(l_p, l_m, t)；然后更新计数器：...
  c03_b06.md: ...C(a) < HLC(b)，按 (l, c) 的字典序。 * **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \epsilon 是进程间最大时钟偏移。...
  c03_b12.md: ...是：事务启动时获取时间戳 t_s。如果读时观察到已提交写入的时间戳 t_w 满足 t_s < t_w < t_s + \text{max\_skew}，系统无法判断该写入在 wall-clock 时间中是否发生在事务开始之前，于是 _重启_ 事务，...
  c08_b05.md: ...3 个 promise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"he...
  c08_b05.md: ...= \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `a...
  c12_b04.md: ...20%（8.4 TB），每流 100 MB/s（4 条流并行；总 400 MB/s）：  \[ \frac{8.4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx...
  c12_b04.md: ...100 MB/s（4 条流并行；总 400 MB/s）：  \[ \frac{8.4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \...
  c12_b04.md: ...[ \frac{8.4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  实践中，移动被节流以避免影响客户流量；实际挂钟时间 12...
  c12_b04.md: ...}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  实践中，移动被节流以避免影响客户流量；实际挂钟时间 12-18 小时。Anya 安排在夜间进行；第二...
  c12_b08.md: ...7 TB = 8.4 TB。  400 MB/s 总吞吐（4 条流 × 100 MB/s）下的时间：  \[ 8.4 \text{ TB} = 8400 \text{ GB} = 8{,}400{,}000 \text{ MB} \] \[ \fr...
  c12_b08.md: ...0 MB/s 总吞吐（4 条流 × 100 MB/s）下的时间：  \[ 8.4 \text{ TB} = 8400 \text{ GB} = 8{,}400{,}000 \text{ MB} \] \[ \frac{8{,}400{,}000 \...
  c12_b08.md: ...下的时间：  \[ 8.4 \text{ TB} = 8400 \text{ GB} = 8{,}400{,}000 \text{ MB} \] \[ \frac{8{,}400{,}000 \text{ MB}}{400 \text{ MB/s}...
  c12_b08.md: ...{ GB} = 8{,}400{,}000 \text{ MB} \] \[ \frac{8{,}400{,}000 \text{ MB}}{400 \text{ MB/s}} = 21{,}000 \text{ s} \approx 5.8 \t...
  c12_b08.md: ...,}000 \text{ MB} \] \[ \frac{8{,}400{,}000 \text{ MB}}{400 \text{ MB/s}} = 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  生产中...
  c12_b08.md: ...rac{8{,}400{,}000 \text{ MB}}{400 \text{ MB/s}} = 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  生产中，移动被节流以避免影响客户流量。节流至 200 M...
  c12_b08.md: ...t{ MB}}{400 \text{ MB/s}} = 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  生产中，移动被节流以避免影响客户流量。节流至 200 MB/s 总吞吐（理论值的 50%）：需 11...
  c13_b05.md: ...得），$t_w$ 为事务在读期间观察到的某个已提交写的提交 HLC 时间戳。若 $t_s < t_w < t_s + \text{max_clock_skew}$，系统无法判断 $t_w$ 在真实时间上究竟在 $t_s$ 之前还是之后——该写的提交...
  c16_b01.md: ...，合并函数取两个状态并产生新状态。合并必须满足：  * **交换律** ：\(\text{merge}(a,b) = \text{merge}(b,a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c...
  c16_b01.md: ...e}(b,a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c) = \text{merge}(a,\text{merge}(b,c))\) * **幂等律** ：\(\text{merge}(a,a...
  c16_b03.md: ...Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta =...
  c16_b03.md: ....Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标...
  c16_b03.md: ...Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta =...
  c16_b03.md: ....Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标...

=== automerge (24 occurrences) ===
  c03_b08.md: ...消息是一个 op，带唯一 ID 和依赖列表（发送时发送者可见的消息 ID）。显示顺序是依赖图的任意拓扑排序。Yjs 和 Automerge 内部就是这样做的。  LLM 原版的 `sorted(messages, key=lambda m: m.timest...
  c04_b10.md: ...量；向量随键的写作者数增长，而非系统大小。这是 CRDT 模式；广播层退化为“可靠”，因果性由数据类型承载。Yjs 和 Automerge（第16章）使用这种方法。  对大多数 2026 系统，正确的问题是：应用是否真的需要 _所有_ 消息的因果广播，还是因...
  c16_b01.md: ...e Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于部分子产品）。本章是 Yuki 的工作流 9：用 CRDT 实现 Glide 协作文档编辑器。  CRDT 是过去...
  c16_b03.md: ...众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAssembly 实现。   #### Automerge 的实现  Automerge（Martin Kleppmann、Adam Wiggins 等）使用类似的 RGA-li...
  c16_b03.md: ...80 KB），用 TypeScript / WebAssembly 实现。   #### Automerge 的实现  Automerge（Martin Kleppmann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Aut...
  c16_b03.md: ...rge（Martin Kleppmann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间旅行和丰富的合并 UI。  Automerge...
  c16_b03.md: ...。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间旅行和丰富的合并 UI。  Automerge 2.x（2023 年发布）是重大重写，将性能提升到生产可接受水平。早期版本在大规模下有内存问题；2.x 使用 Rust...
  c16_b03.md: ...期版本在大规模下有内存问题；2.x 使用 Rust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰...
  c16_b03.md: ...与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因...
  c16_b03.md: ...器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且其文档模型比 Automerge 完整历史图更简单。   ### 16.4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   ####...
  c16_b06.md: ...te-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.** 走一遍 G-Counter 和 PN-Counter 的操作与合并。...
  c16_b09.md: ...范围操作、约束）通常需要额外层。  **Q3.** 一个团队在为协作文档项目管理应用（Jira 风格）选择 Yjs 与 Automerge。基于工作负载（大量小问题编辑、历史重要、偶尔批量导入）进行比较。  提示  Yjs：更快、更小。Automerge：历...
  c16_b09.md: ...与 Automerge。基于工作负载（大量小问题编辑、历史重要、偶尔批量导入）进行比较。  提示  Yjs：更快、更小。Automerge：历史更丰富。批量导入对两者都棘手。  **答案：**  _Yjs_ ：  * 优点：合并快、bundle 小、生产使用...
  c16_b09.md: ...何时改了什么” UI；为 git-like 文档工作流设计。 * 缺点：每文档内存占用更大（存储历史）；历史上较慢，但 Automerge 2.x 在 2026 生产 benchmark 中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：...
  c16_b09.md: ...；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；Yjs 擅长。Automerge 的历史可启用 “给我看上周二的问题描述”。 * **状态变更**：离散状态转换（Open → In Progress...
  c16_b09.md: ...Progress → Done）。用寄存器或 LWW 字段建模比 list CRDT 容易；Yjs 的 Y.Map 或 Automerge 的结构化 doc 都可以。 * **评论**：只追加的评论列表。任一 CRDT 都能处理。 * **审计追踪**：项目...
  c16_b09.md: ...：只追加的评论列表。任一 CRDT 都能处理。 * **审计追踪**：项目管理应用常需要合规性的 “谁改了什么、何时”。Automerge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CS...
  c16_b09.md: ...处理，但导入是结构操作，必须小心协调；CRDT 机制并非为批量加载优化。  推荐：  * 若审计/历史作为数据需求重要：Automerge。 * 若纯性能和生态成熟度占主导：Yjs。  对审计和历史重要的 Jira 风格应用，Automerge 是正确选择。...
  c16_b09.md: ...数据需求重要：Automerge。 * 若纯性能和生态成熟度占主导：Yjs。  对审计和历史重要的 Jira 风格应用，Automerge 是正确选择。对文档是焦点、历史是次要功能的 Notion 风格应用，Yjs 是正确选择。决策总是由工作负载驱动。...
  c16_b10.md: ...覆盖 Google Docs OT 的边界情况。  综合：2026 年新设计默认用 CRDT。数学干净加上 Yjs 和 Automerge 的生产记录是强论据。对现有基于 OT 的系统（Google Docs、Microsoft Word Online），迁...
  c16_b11.md: ...的朴素方法。CRDT 在结构上解决以上所有问题；重新发明它们不必要且错误。正确的 LLM 回答应该是 “用 Yjs 或 Automerge；不要从零实现协作编辑。” LLM 产出朴素代码说明它没学到这一课，或它在不考虑需求的情况下生成最简代码。  对协作文辑...
  c16_b11.md: ...写胜出）、MV-Register（多值）、OR-Set（观察删除集合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Du...
  c17_b02.md: ...家”。 * 强最终：收敛状态由更新集合决定，而非策略。无不确定性。  CRDT（第16章）是典型强最终数据类型。Yjs、Automerge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT bucket ty...
  c30_b02.md: ..."A Conflict-Free Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McG...

=== atomic (24 occurrences) ===
  c04_b05.md: ...的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 rel...
  c04_b05.md: ...t-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性质，再选最弱的足够层。  广...
  c04_b05.md: ...置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性质，再选最弱的足够层。  广播层次作为分层架构：      graph TD...
  c04_b05.md: ...roadcast]         D -->|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9...
  c04_b05.md: ...t         |         + total order (consensus)         v     Atomic broadcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Ca...
  c04_b05.md: ...读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 |...
  c04_b05.md: ...每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器...
  c04_b05.md: ...证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，best-effort（无协调）与 atomic（每事件共识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性性质_，再选最弱的足够广播层。不要为了安...
  c04_b05.md: ...识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性性质_，再选最弱的足够广播层。不要为了安全默认选 atomic。代价会由每次操作永久支付。...
  c04_b05.md: ...的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 rel...
  c04_b05.md: ...t-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性质，再选最弱的足够层。  广...
  c04_b05.md: ...置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用真正需要的正确性性质，再选最弱的足够层。  广播层次作为分层架构：      graph TD...
  c04_b05.md: ...roadcast]         D -->|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9...
  c04_b05.md: ...t         |         + total order (consensus)         v     Atomic broadcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Ca...
  c04_b05.md: ...读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 |...
  c04_b05.md: ...每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺序相同 库存提交 | atomic（或等价事务） | 超卖不可接受 房间内聊天消息 | causal（或更强） | 回复必须跟在原文之后 CRDT 计数器...
  c04_b05.md: ...证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，best-effort（无协调）与 atomic（每事件共识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性性质_，再选最弱的足够广播层。不要为了安...
  c04_b05.md: ...识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性性质_，再选最弱的足够广播层。不要为了安全默认选 atomic。代价会由每次操作永久支付。...
  c04_b07.md: ...前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：*...
  c04_b07.md: ...cast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。   5. **Atomic broadcast** ：新增 _total order_ ——所有正确进程以相同总顺序交付消息（且与 causal...
  c04_b07.md: ...前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：*...
  c04_b07.md: ...cast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m_2。   5. **Atomic broadcast** ：新增 _total order_ ——所有正确进程以相同总顺序交付消息（且与 causal...
  c28_b12.md: ...**Fencing token**：第18章（分布式锁）。 * **Raft**：第9章（实现友好的共识）。 * **Atomic broadcast**：第4章（广播层级）。 * **CRDT**：第16章（CRDTs in production）...
  c30_b06.md: ...；人类捕获约 40%（上下文判断、业务不变量、架构选择、新模式、考虑后保留的推理）。见第27章 §27.10。 * **Atomic broadcast（原子广播）**：所有正确进程按相同全序交付消息的广播。等价于共识。 * **BFT（拜占庭容错）*...

=== source (24 occurrences) ===
  c05_b02.md: ...Spanner（通过 Paxos 的 per-tablet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover re...
  c13_b05.md: ...频繁重启，并通过重试延迟有效支付 TrueTime 成本。   #### 对比  System | Timestamp source | Cost ---|---|---- Spanner | TrueTime（GPS + 原子钟） | 每次写 7 m...
  c14_b07.md: ...eads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestamp source | TrueTime（GPS + 原子钟） | HLC + NTP 偏移上界 PACELC | PC / EC（每次写...
  c15_b08.md: ...一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？...
  c15_b08.md: ...nk 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读...
  c15_b08.md: ...**Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed ope...
  c15_b08.md: ...fka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的...
  c15_b08.md: ...一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？...
  c15_b08.md: ...nk 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读...
  c15_b08.md: ...**Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed ope...
  c15_b08.md: ...fka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的...
  c18_b01.md: ...事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth...
  c18_b01.md: ...读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓...
  c18_b01.md: ...缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source...
  c18_b01.md: ...source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d) **TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下...
  c18_b01.md: ...始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d) **TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新...
  c18_b01.md: ...后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside +...
  c18_b02.md: ...) **缓存过期时的惊群（thundering herd）** ：多个读者在 key 过期时同时请求；全部未命中并涌向 source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事故期间的缓存踩踏** ：truth source...
  c18_b02.md: ...source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事故期间的缓存踩踏** ：truth source 变慢或宕机；所有缓存过期，所有读者击中慢 source，使其更慢。缓解：异常时提供过期数据（serve stale-o...
  c18_b02.md: ...ey 锁。  (b) **事故期间的缓存踩踏** ：truth source 变慢或宕机；所有缓存过期，所有读者击中慢 source，使其更慢。缓解：异常时提供过期数据（serve stale-on-error）。  (c) **失效竞争的过期数据**...
  c18_b02.md: ...数据（serve stale-on-error）。  (c) **失效竞争的过期数据** ：缓存被失效，但 truth source 写尚未提交；在失效与提交之间的一次读会用旧值刷新缓存。缓解：提交后再失效，失败时重试。  (d) **把缓存当作 tr...
  c18_b02.md: ...提交；在失效与提交之间的一次读会用旧值刷新缓存。缓解：提交后再失效，失败时重试。  (d) **把缓存当作 truth source** ：开发者开始把缓存用于正确性；缓存无形中成为记录系统。缓存故障（Redis 崩溃、内存驱逐）后数据丢失。缓解：纪律...
  c18_b02.md: ...余额查询、幂等键检查）使用 Redis Cluster。每个缓存条目有短 TTL（60 秒）；未命中时查询 truth source（CockroachDB）。事件驱动失效在每次提交时发布 “账户已变” 事件；缓存主动失效。   ### 18.2 分布...
  c18_b09.md: ...库中断情况。  更深观察：惊群是缓存被要求做太多的征兆。若单个缓存 key 热到过期会引发踩踏，底层数据可能应放在更快 source（应用内存、Hazelcast 等分布式内存、CDN）。缓存是一层工具；组合工具通常能更好处理最坏工作负载。...

=== serverless (24 occurrences) ===
  c11_b07.md: ...Aurora DSQL 是 AWS 的 serverless、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres 兼容 Spanner 风格系统。复制协议基于...
  c11_b07.md: ...PACELC：分区下 PC，无分区 EC（存储层法定人数写比本地 Postgres 逻辑多花约 5-10 ms）。优势：serverless 计费、Postgres 兼容、可跨区。劣势：仅限 AWS；较新（仍在实战中检验）；每租户 Firecracker 模型...
  c11_b07.md: ...限 AWS；较新（仍在实战中检验）；每租户 Firecracker 模型不常见，可能有意外运维特征。适合：希望多区域且 serverless、又不想管理 CockroachDB 集群的 Postgres 工作负载。  方法可泛化：读厂商文档，找到四个答案，写出...
  c14_b01.md: ...NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：serverless Postgres，存储与计算分离。 * **TigerB...
  c14_b01.md: ...ora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：serverless Postgres，存储与计算分离。 * **TigerBeetle** ：金融级账本，采用确定性模拟测试和 Tiger...
  c14_b03.md: ...证计算隔离。 * 解耦存储层，6 副本法定人数（写需 4 of 6 ack）。 * 兼容 Postgres 线缆。 * Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费...
  c14_b03.md: ...* Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postg...
  c14_b03.md: ...AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Serverless Postgres，存储与计算分离。严格说不是多主意义上的 “NewSQL”，但属于同一运维类别。  关键属性：  *...
  c14_b03.md: ...旅行。 * 单主（每个数据库同时只有一个写入者）。 * 分支（为 dev/test 即时克隆数据库）。  优势：  * Serverless：适合负载可变。 * 分支让 dev/test 很便宜（每个分支是存储的写时复制）。 * 真正 Postgres（非克隆...
  c14_b03.md: ...。 * 较新；某些运维细节仍在打磨。  Neon 适合：负载可变（开发、dev/test 环境、低容量 SaaS）并从 serverless 计费受益。不适合大规模高吞吐生产 OLTP。   ### 14.8 TigerBeetle  领域特定：专为金融复式记...
  c14_b03.md: ...证计算隔离。 * 解耦存储层，6 副本法定人数（写需 4 of 6 ack）。 * 兼容 Postgres 线缆。 * Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费...
  c14_b03.md: ...* Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postg...
  c14_b03.md: ...AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Serverless Postgres，存储与计算分离。严格说不是多主意义上的 “NewSQL”，但属于同一运维类别。  关键属性：  *...
  c14_b03.md: ...旅行。 * 单主（每个数据库同时只有一个写入者）。 * 分支（为 dev/test 即时克隆数据库）。  优势：  * Serverless：适合负载可变。 * 分支让 dev/test 很便宜（每个分支是存储的写时复制）。 * 真正 Postgres（非克隆...
  c14_b03.md: ...。 * 较新；某些运维细节仍在打磨。  Neon 适合：负载可变（开发、dev/test 环境、低容量 SaaS）并从 serverless 计费受益。不适合大规模高吞吐生产 OLTP。   ### 14.8 TigerBeetle  领域特定：专为金融复式记...
  c14_b04.md: ...过元数据层（构建于 FoundationDB 之上）实现强一致。 * 时间旅行：可在保留期内查询任何先前状态。  优势：serverless 计算；可预测成本；并发能力极强；“set it and forget it” 运维。  劣势：厂商锁定（专有格式，但...
  c14_b05.md: ...oachDB + 独立 OLAP 存储 纯 OLAP | Snowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | CockroachDB（任何云）、S...
  c14_b08.md: ...数据，账本记录资金变动；两者必须提交（跨两系统的 saga 模式）。  (c) **分析**：Snowflake。理由：serverless 计算（分析工作负载突发）；强并发支持多团队（BI、财务、合规、ML）；时间旅行便于审计。数据通过 CDC 每晚从 Co...
  c14_b09.md: ...aga 或 2PC、运维复杂度倍增。  _选项 2：Aurora DSQL_。迁移到 AWS 托管 NewSQL。优点：serverless 计费（无空闲容量）、兼容 Postgres（大部分）、多区域可用、AWS 原生集成。缺点：AWS 锁定加深、较新且生产...
  c14_b09.md: ...每租户 Firecracker 开销可能意外。  _选项 3：CockroachDB_。迁移到 CockroachDB Serverless 或自托管。优点：多云、成熟、Postgres 兼容、强可串行化事务。缺点：与现有 Aurora 分开运维、付费产品（企...
  c14_b09.md: ...aga 或 2PC、运维复杂度倍增。  _选项 2：Aurora DSQL_。迁移到 AWS 托管 NewSQL。优点：serverless 计费（无空闲容量）、兼容 Postgres（大部分）、多区域可用、AWS 原生集成。缺点：AWS 锁定加深、较新且生产...
  c14_b09.md: ...每租户 Firecracker 开销可能意外。  _选项 3：CockroachDB_。迁移到 CockroachDB Serverless 或自托管。优点：多云、成熟、Postgres 兼容、强可串行化事务。缺点：与现有 Aurora 分开运维、付费产品（企...
  c14_b12.md: ...ckroachDB” 的模式在运维上可靠。直接上 NewSQL 会增加前期成本，而收益可能永远不会兑现。  第二个考虑：serverless 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL...
  c30_b10.md: ...替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DSQL。...

=== chaos (24 occurrences) ===
  c21_b04.md: ...据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证修复。  事后复盘模式：识别触发器、放大和防御缺失。每一项都是...
  c22_b11.md: ...、错误预算。对变化率告警与对绝对值告警同等重要。  第23章讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。...
  c23_b01.md: ...工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026 年已成熟为标准 SRE 实践，包含组织和技术组件。   ### 23.1 什么是混沌工程  混...
  c23_b01.md: ...(e) **最小化爆炸半径**。从小处开始（一个节点、一个 shard）；建立信心后再扩大。  Netflix 起源：Chaos Monkey（随机 kill 生产实例的工具）约 2010 年引入。Netflix 的理由：实例反正会在生产环境中失败...
  c23_b02.md: ...### 23.3 Nora Jones 的组织实践  Nora Jones（与 Casey Rosenthal 合著《Chaos Engineering: System Resiliency in Practice》）是混沌工程组织方面的权威声音。...
  c23_b02.md: ...*。Game day 不是一次性事件。每月或每季度节奏保持肌肉锻炼；新成员通过参与学习。  组织实践才是区分 “我们用 Chaos Monkey” 与 “我们有混沌工程学科” 的关键。工具容易；文化难。   ### 23.4 工具  现代混沌工程工具...
  c23_b02.md: ...与 “我们有混沌工程学科” 的关键。工具容易；文化难。   ### 23.4 工具  现代混沌工程工具：  (a) **Chaos Mesh**：Kubernetes 原生混沌工程。注入 pod 故障、网络故障、磁盘故障、时间偏移。广泛采用。  (b...
  c23_b02.md: ...**：TCP 层故障注入。慢连接、丢包、模拟下游变慢。与应用无关。  选择取决于部署：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiprox...
  c23_b05.md: ...ent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey 风格：工作时间持续随机注入故障。系统一直在被测试。  (e) **Level 4（混沌作为设计输入）**...
  c23_b06.md: ...正确的实践。目标是把信念转化为证据：不假设防御有效，而是证明它们有效。  五条原则（来自 “Principles of Chaos Engineering” 网站，源自 Netflix 团队）：  (a) **围绕稳态行为建立假设**。用可测量输出（...
  c23_b06.md: ...量模式、完整依赖图）。实验室测试必要但不充分。  (d) **自动化实验以持续运行**。手动 game day 有价值；Chaos Monkey 风格的持续自动混沌让系统永久被测试。  (e) **最小化爆炸半径**。从小处开始（一个节点、一个 sh...
  c23_b09.md: ...。投入是真实的（几天高级工程师时间），但替代方案（在真实事故中发现这些问题）成本更高。  **Q2.** 一个团队运行 Chaos Monkey 两年了。他们没有发生重大中断。他们是否 “完成” 了混沌工程？  提示  Chaos Monkey 随机...
  c23_b09.md: ...** 一个团队运行 Chaos Monkey 两年了。他们没有发生重大中断。他们是否 “完成” 了混沌工程？  提示  Chaos Monkey 随机 kill 实例。它测试一种故障模式；系统可能还有许多其他。一个层面的持续混沌不等于完整学科。  *...
  c23_b09.md: ...kill 实例。它测试一种故障模式；系统可能还有许多其他。一个层面的持续混沌不等于完整学科。  **答案：** 没有。Chaos Monkey 测试一种故障模式（随机实例终止）。系统还有许多其他 Chaos Monkey 不测试的故障模式：  (a...
  c23_b09.md: ...不等于完整学科。  **答案：** 没有。Chaos Monkey 测试一种故障模式（随机实例终止）。系统还有许多其他 Chaos Monkey 不测试的故障模式：  (a) **网络分区**：节点仍在运行但彼此隔离。Chaos Monkey kil...
  c23_b09.md: ...）。系统还有许多其他 Chaos Monkey 不测试的故障模式：  (a) **网络分区**：节点仍在运行但彼此隔离。Chaos Monkey kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos...
  c23_b09.md: ...。Chaos Monkey kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机...
  c23_b09.md: ...*：节点仍活着但降级。Chaos Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机 kill 单个节点，不是协调的区域事件。  (d) **级联依赖**：服务 X 故障导致服务 Y...
  c23_b09.md: ...nkey 随机 kill 单个节点，不是协调的区域事件。  (d) **级联依赖**：服务 X 故障导致服务 Y 问题。Chaos Monkey 测试实例故障；级联关于传播。  (e) **时钟偏移**：节点活着但时钟错误。专门故障。...
  c23_b10.md: ...(h) **配置变更**：部署、schema 变更、feature flag。本身不是故障，但与系统交互的运营事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基...
  c23_b10.md: ...统交互的运营事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game...
  c23_b10.md: ...测试一个切片（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos M...
  c23_b10.md: ...性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加网络分区混沌、慢节点混沌、为区域故障运行 g...
  c27_b07.md: ...t reviewed” 签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，chaos game day（第23章）演练该特性声称的防御。 * Game day 验证防御 _有效_，而非仅存在于代码中。 *...

=== nim (24 occurrences) ===
  c23_b13.md: ...第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  -...
  c24_b01.md: ...ding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NVIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。   ### 24.1 为什么分布式 LLM 推理不同...
  c24_b01.md: ...发对话）。集群必须处理这种混合而没有队头阻塞。  2026 年处理这些挑战的系统：vLLM、SGLang、NVIDIA NIM。每种有不同优化；适合不同工作负载。...
  c24_b04.md: ...### 24.4 NVIDIA NIM  NIM（NVIDIA Inference Microservices）是 NVIDIA 的企业推理部署平台。约 20...
  c24_b04.md: ...### 24.4 NVIDIA NIM  NIM（NVIDIA Inference Microservices）是 NVIDIA 的企业推理部署平台。约 2024 年...
  c24_b04.md: ...2024 年 GA 发布；企业 LLM 部署的商业参考设计。  特性：  (a) **容器化微服务**：每个模型是一个 NIM 容器（含模型权重、推理引擎、API 表面）。通过 Kubernetes 部署；由 NVIDIA 工具管理。  (b)...
  c24_b04.md: ...推理引擎、API 表面）。通过 Kubernetes 部署；由 NVIDIA 工具管理。  (b) **多引擎支持**：NIM 容器可按模型使用 vLLM、TensorRT-LLM（NVIDIA 优化引擎）或其他引擎。面向用户的 API 一致。...
  c24_b04.md: ...、TensorRT-LLM（NVIDIA 优化引擎）或其他引擎。面向用户的 API 一致。  (c) **硬件优化**：NIM 容器包含 NVIDIA 的硬件专属优化（H100 / H200 / B100 / B200 调优）。每代 GPU 都有...
  c24_b04.md: ...特性**：SSO、RBAC、审计日志、监控集成。面向企业采购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是...
  c24_b04.md: ...共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别是运维：NIM 提供 Kubernetes-native 部署、可观...
  c24_b04.md: ...ti-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别是运维：NIM 提供 Kubernetes-native 部署、可观测性和企业集成；vLLM 提供推理优化。  对企业团队：NIM 是...
  c24_b04.md: ...维：NIM 提供 Kubernetes-native 部署、可观测性和企业集成；vLLM 提供推理优化。  对企业团队：NIM 是标准。对开源/成本优化团队：直接使用 vLLM 加自定义编排。   ### 24.5 推理集群拓扑  2026 年生...
  c24_b08.md: ...以上推理预算每年节省数百万美元。ROI 很直接。  **Q2.** 某团队在企业部署中选择 vLLM 与 NVIDIA NIM。他们在 Kubernetes 上；有 SOC 2 合规要求。比较。  提示  vLLM：开源，更多控制，更少集成。NI...
  c24_b08.md: ...IM。他们在 Kubernetes 上；有 SOC 2 合规要求。比较。  提示  vLLM：开源，更多控制，更少集成。NIM：企业特性、SOC 2、更少优化控制。  **答案：**  _vLLM_：  * 开源；团队完全控制部署。 * 最佳原始...
  c24_b08.md: ...、RBAC、审计日志、监控集成。 * SOC 2 合规：平台提供构建块；团队的特定合规更快实现。 * 推理引擎控制较少：NIM 容器使用特定引擎（通常底层仍是 vLLM），但团队离调优隔了一层。 * 成本：NVIDIA 收取企业 tier 费用。...
  c24_b08.md: ...。 * 成本：NVIDIA 收取企业 tier 费用。  对 SOC 2 合规 Kubernetes 部署：  * **NIM** 是运维上更快的路径。合规和企业特性已预建；团队认证工作量减少。成本是 NVIDIA license 溢价。 * *...
  c24_b09.md: ...* **团队规模**：小团队 → NIM（现成企业）；有平台工程师的大团队 → vLLM（自建企业特性）。 * **合规紧迫性**：3 个月内 SOC 2 审计...
  c24_b09.md: ...企业）；有平台工程师的大团队 → vLLM（自建企业特性）。 * **合规紧迫性**：3 个月内 SOC 2 审计 → NIM（更快）；12 个月 timeline → 两者都可。 * **定制化需求**：重度定制 → vLLM（完全控制）；标准...
  c24_b09.md: ...timeline → 两者都可。 * **定制化需求**：重度定制 → vLLM（完全控制）；标准 serving → NIM（集成优于灵活）。 * **成本敏感度**：非常敏感成本 → vLLM 加自定义集成；CapEx 灵活 → NIM。...
  c24_b09.md: ...→ NIM（集成优于灵活）。 * **成本敏感度**：非常敏感成本 → vLLM 加自定义集成；CapEx 灵活 → NIM。  对 Continuum（重度基础设施用户，有平台团队）：vLLM 加自定义企业特性合理。对大多数 enterpri...
  c24_b09.md: ...inuum（重度基础设施用户，有平台团队）：vLLM 加自定义企业特性合理。对大多数 enterprise IT 商店：NIM 是正确答案。  **Q3.** 某团队推理集群出现双峰延迟：短请求快，长请求阻塞短请求。诊断并提出修复。  提示  队...
  c24_b11.md: ...对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA NIM 是企业部署平台：容器化微服务、多引擎支持、硬件优化、企业特性。企业团队的标准部署表面。  Continuous bat...
  c30_b11.md: ...路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；企业部署两者都通过 NVIDIA NIM。 * **架构**：集群超过约 50 节点后做 prefill/decode 分离（第25章）；KV cache 层（...
  c30_b11.md: ...6 架构突破（分离、KV cache 层、投机解码）复合产生显著成本节省。 * **替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用 Anyscale / Together / Fireworks 等托管替代。...

=== tier (24 occurrences) ===
  c24_b08.md: ...控制较少：NIM 容器使用特定引擎（通常底层仍是 vLLM），但团队离调优隔了一层。 * 成本：NVIDIA 收取企业 tier 费用。  对 SOC 2 合规 Kubernetes 部署：  * **NIM** 是运维上更快的路径。合规和企业特性...
  c25_b02.md: ...ad Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node 1<br...
  c25_b02.md: ...>4× H100"]         end         subgraph CacheTier["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1]...
  c25_b02.md: ...che Node 2]         end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Node 1<b...
  c25_b03.md: ...成设计；提供集群级 KV cache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在...
  c25_b05.md: ...：  (a) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。...
  c25_b10.md: ...**工作负载优化**：  * 限制每请求最大上下文长度（防止超大上下文拖慢集群）。 * 把不同工作负载路由到不同模型 tier（只在质量可接受处用小模型）。 * 在 API 层缓存常见响应。  (e) **模型优化**（LLM 的建议，但更 nu...
  c26_b04.md: ...### 26.6 多租户公平性  Continuum 服务多个客户。每客户付费 tier 不同；有些有延迟 SLA。公平性问题：给定有限 GPU 资源，如何在客户间调度请求。  朴素 FIFO 调度失败：一个...
  c26_b04.md: ...**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduli...
  c26_b04.md: ...被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器...
  c26_b04.md: ...负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更...
  c26_b04.md: ...ighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运维关注点：每请...
  c26_b04.md: ...er SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运维关注点：每请求调度决策必须非常快（亚毫秒），以免增加 per-request 延迟...
  c26_b04.md: ...须非常快（亚毫秒），以免增加 per-request 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ### 26.7 Tobias 的完整优化  Continuum 集群 6 个月优化冲刺后的完整图景...
  c26_b07.md: ...对 Continuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 we...
  c26_b07.md: ...tinuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低 tier 客户在峰值期间也有...
  c26_b07.md: ...fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低 tier 客户在峰值期间也有有界服务。  运维关注点：调度决策必须快（亚毫秒）以免增加 per-request 延迟。决策还必须...
  c26_b08.md: ...倍吞吐提升下，每请求 decode 时间从约 10 秒降到约 5 秒。  (e) **客户 SLA 调度**：该客户是 tier-2（中优先级）。Weighted fair scheduling 确保其请求被处理但不饿死 tier-1 客户。  综...
  c26_b08.md: ...度**：该客户是 tier-2（中优先级）。Weighted fair scheduling 确保其请求被处理但不饿死 tier-1 客户。  综合：该客户每请求成本降低约 5 倍（前缀复用），延迟显著改善。Continuum 能给出有竞争力的 p...
  c26_b09.md: ...求最大输出长度**：限制 max_tokens；拒绝极端限制请求或路由到特殊 endpoint。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同...
  c26_b09.md: ...制请求或路由到特殊 endpoint。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(...
  c26_b09.md: ...**多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative decod...
  c26_b09.md: ...*：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)、(b)、(d) 影响最大。Speculative decoding 有帮助，但更多...
  c26_b11.md: ...多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：dis...

=== stratus (23 occurrences) ===
  c01_b03.md: ...wartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不同，心智动作相同。...
  c09_b01.md: ...Anya Reddy 两个月前加入 Stratus DB。她的第一个项目：为复制层选个共识库。她读了 Paxos 论文。她读了 Megastore 论文。她试图在周末实现...
  c09_b05.md: ...用于可调法定人数。但对 2026 年的新项目，默认是 Raft。   ### 9.9 Anya 的决定  Anya 为 Stratus DB 复制层选择了 `tikv/raft-rs` 库（Rust）。她在设计文档中记录的理由：  (a) **成熟度**...
  c09_b05.md: ...(a) **成熟度** ：TiKV 的 Raft 已生产运行多年，bug 已被发现并修复。 (b) **语言匹配** ：Stratus DB 是 Rust；raft-rs 原生。 (c) **运维工具** ：该库以 Stratus 现有可观测性栈可摄取的...
  c09_b05.md: ...) **语言匹配** ：Stratus DB 是 Rust；raft-rs 原生。 (c) **运维工具** ：该库以 Stratus 现有可观测性栈可摄取的形式暴露状态机、日志和配置。 (d) **成员变更** ：raft-rs 实现单服务器变更（Ra...
  c09_b05.md: ...xos 可提供）；per-tablet 性能调优（Spanner 的 Paxos-with-leases 可提供）。对 Stratus 场景（金融级持久性、亚 100 ms 提交、三区域部署），这些权衡正确。  Anya 遵循的模式是 2026 标准：选...
  c09_b05.md: ...是 2026 标准：选一个成熟的 Raft 库，依赖其工具，接受运维默认值直到需要调优。后续章节（第11-14章）展示 Stratus 和 CockroachDB 如何在这个 Raft 基础上构建分布式数据库。   ### 9.10 练习...
  c09_b08.md: ...#### 应用问题  **Q1.** Anya 正在实现 Stratus DB 复制层。她有一个 3 节点 Raft 集群。一个 follower 与集群分区 30 分钟；期间集群提交了 10...
  c09_b11.md: ...并基于 terms、elections 和 AppendEntries 一致性检查提供清晰的运维模型。  Anya 为 Stratus DB 选择 raft-rs、依赖库工具、接受运维默认值，这是 2026 年的标准模式。大多数团队不应从头实现 Raft...
  c11_b05.md: ...。在采用任何新数据库前使用它；在阅读任何声称某系统“比 X 更好”的博客前使用它。   ### 11.5 Anya 的 Stratus DB 阅读  Anya 把同样方法用于她正在开发的系统。阅读结果：  Question | Answer（Stratu...
  c11_b05.md: ...aknesses | 新系统；生态小；SQL 层仍在开发 Right for | CockroachDB 风格工作负载，Stratus 合作公司内部使用  这份阅读帮助 Anya 向潜在客户沟通系统画像。客户不需要读 CockroachDB-vs-Str...
  c11_b05.md: ...tus 合作公司内部使用  这份阅读帮助 Anya 向潜在客户沟通系统画像。客户不需要读 CockroachDB-vs-Stratus 比较；他们需要方法，根据自己的工作负载评估两者。   ### 11.6 阅读文档的常见陷阱  (a) **营销级“一致...
  c11_b11.md: ...一致哈希、Bigtable / Cassandra / RocksDB 的 LSM-tree 基础，以及 Anya 在 Stratus DB 集群上无停机重分片的实例。  ---...
  c12_b01.md: ...Anya 盯着团队 on-call 发来的 Slack 消息：“Stratus 集群 7 磁盘使用率 87%，每天增长 2%。下周末前必须扩容。”  集群有 12 节点，每节点 4 TB（原始容量...
  c12_b04.md: ...数据  更新集群的 range 表：把过载节点上的 range 重新分配给新节点。重分配是惰性的：数据尚未移动。  对 Stratus 的 12 节点加 3 节点，重平衡算法目标是每个节点持有约 1/15 的 range。目前每节点 1/12。新目标大约...
  c18_b08.md: ...）原生去重。锁模式通常是 “自己造轮子”；现代系统通常可围绕专用队列基础设施设计。  **Q2.** Anya 正在为 Stratus DB 领导者选举服务实现 fencing token。描述存储层如何强制执行 token 顺序。  提示  每次 ra...
  c18_b08.md: ...包含领导者 token。range 的 Raft 状态跟踪最高 token。低 token 被拒绝。  **答案：** Stratus DB 的领导者选举使用带 fencing token 的 etcd 锁。实现：  (a) 集群的领导者角色（对给定 r...
  c18_b09.md: ...对 Stratus 的具体架构，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 tok...
  c28_b02.md: ...d) **账本**：TigerBeetle（5 副本，多 region）。记录每笔资金流动。  (e) **协调器**：Stratus DB（现为 Crescent 组件）。管理分布式事务状态；经 Raft 复制；对安全关键操作使用 fencing to...
  c28_b03.md: ...]             CRDBE[CockroachDB nodes]             STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]...
  c28_b04.md: ...y（第18章缓存模式）。若 key 存在，返回之前计算的结果（谎言 1 防御）。2 ms。  (5) API 服务开始 Stratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordi...
  c28_b07.md: ...带 fencing token 的 Raft 复制协调器是系统安全的核心。集成到 Crescent 技术栈很直接，因为 Stratus DB 从一开始就用这些模式设计。诊断方法（第11章）是我们向 Crescent 团队推销集成的方式；他们对 Strat...
  c28_b07.md: ...ratus DB 从一开始就用这些模式设计。诊断方法（第11章）是我们向 Crescent 团队推销集成的方式；他们对 Stratus 运行了该方法，验证了其 profile 匹配需求。”  **Yuki（从 Glide 借调）**：“商户入驻合同编辑器...

=== cpu (23 occurrences) ===
  c02_b07.md: ...每条日志行作为单独的 gRPC 消息发送到集中聚合器。序列化开销和每条消息的头成本远超实际日志负载，聚合器网络远未饱和 CPU 就已先饱和。  _网络是安全的。_ 示例：内部服务接受来自 VPC 的任意调用者，因为“VPC 是私有的”。集群中一个...
  c06_b03.md: ...网络。在 CLOS 内，延迟下限是微秒级。 * 网络 bisection bandwidth 很少是瓶颈。磁盘 I/O、CPU 和应用层协调通常是。 * 跨机架与同机架 RPC 延迟相近（差几微秒）。网络局部性不如应用设计重要。   ### 6....
  c06_b03.md: ...Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实现：  * **RoCE（RDMA over Converged Ethernet）** ：202...
  c10_b07.md: ...副本（崩溃容错只需 2f+1），反映问题更难。  (c) 拜占庭协议通常要求每条消息加密签名（以证明谁说了什么），增加 CPU 成本。  2026 年的实践界线：若运维者可信且节点安全 reasonably，崩溃容错假设正确。若运维者不可信（区块...
  c10_b10.md: ...(c) **加密开销** ：每条消息都需签名；验证成本。对中等吞吐元数据存储（每秒数百操作），CPU 成本可承受。  (d) **成员管理** ：增删验证者本身必须通过 BFT 完成，否则拜占庭攻击者可能引入额外验证者。...
  c12_b07.md: ...”（从它到下一个顺时针节点）。随机放置意味着某些节点的弧远大于其他节点；弧大 2 倍的节点得到 2 倍查询，带来磁盘和 CPU 压力。  12 个随机放置节点中，最大弧通常是平均值的 2-3 倍。即最忙节点 QPS 是平均节点的 2-3 倍，对应...
  c12_b07.md: ...12 个随机放置节点中，最大弧通常是平均值的 2-3 倍。即最忙节点 QPS 是平均节点的 2-3 倍，对应磁盘和 CPU 压力。  每个物理节点 256 个 vnode 时，每个物理节点在环上有 256 个随机位置。一个物理节点拥有的总弧是...
  c12_b08.md: ...络吞吐：200 MB/s 持续 24 小时 = 17 TB 传输（跨 AZ 可能产生 egress 费用）。 * 集群 CPU：移动期间每节点额外 10-20%（LSM compaction 开销、Raft snapshot 流）。 * 客户可见...
  c12_b09.md: ...放大：典型 1.5-2 倍（compaction 前多个 SSTable）。 * Compaction 开销：周期后台 CPU 和 I/O 突增。  SQLite 画像：  * 写吞吐：中等。每次写需更新 B+ tree 页；随机更新需页级 I/...
  c14_b09.md: ...团队在 AWS 上运行 PostgreSQL，数据库已增长到 10 TB。单 Aurora Postgres 实例峰值 CPU 60%，延迟在下降。他们需要决定：手动分片、迁移到 Aurora DSQL，还是迁移到 CockroachDB。走一遍...
  c14_b09.md: ...增长已放缓且希望最小架构改动：继续用 Aurora Postgres 并纵向扩展；推迟决策。  10 TB / 60% CPU 尚未临界（还可进一步纵向扩展）。关键问题是未来 12-24 个月是否一定需要多区域或进一步扩展。若是，现在就选 Coc...
  c19_b10.md: ...Kubernetes 部署、几十个服务以内，它无需 Istio 或 Linkerd 的运维开销就能处理发现。网格增加 CPU、内存、延迟和巨大运维面。  反驳：服务网格对多集群、多区域或非 Kubernetes 部署增加真正价值。mTLS、流量...
  c20_b03.md: ...收服务内部做了什么（哪些函数慢、哪些查询慢）。为此需要服务内性能分析。  (b) **资源竞争**：若变慢由接收服务的 CPU 竞争或磁盘 I/O 排队导致，trace 显示症状（span 慢）但不显示根因。为此需要基础设施指标。  (c) **...
  c20_b08.md: ...trace ID 关联。  (g) **检查 service X 的指标**：失败时的错误率、延迟、资源使用。X 是否 CPU 受限？内存受限？其下游是否慢？  (h) **在测试环境复现**：用已知的坏输入模式复现失败。迭代修复。  诊断流程的...
  c21_b04.md: ...支付服务的重试逻辑（指数退避 5 次重试）把 merchant 数据库负载放大 3 倍。 * merchant 数据库 CPU 饱和；甚至无法处理非 merchant 表查询。 * 依赖 merchant 数据库的其他服务（通知服务、分析聚合器）...
  c21_b04.md: ...触发器、放大和防御缺失。每一项都是独立修复。防御缺失（上面 c）通常是最杠杆的；若有熔断器，merchant 数据库的 CPU 尖峰不会扩散到其他服务。   ### 21.8 练习...
  c22_b01.md: ...:17，on-call 告警响起：“面向客户的错误率飙升到 14%。” Olamide 打开监控面板。每个节点都是绿色。CPU 正常。内存正常。健康检查都返回 200 OK。面板是绿的；客户却遇到错误。  二十分钟后她找到问题：一个节点的磁盘静默...
  c22_b01.md: ...、网卡问题）。健康检查（从监控接收，返回值未严格验证）通过；需要下游调用的应用请求挂起。  (e) **嘈杂邻居导致的 CPU 竞争**。共享环境（云、Kubernetes）中的节点与另一租户争夺 CPU；延迟剧烈波动。健康检查在嘈杂邻居发威前完...
  c22_b01.md: ...请求挂起。  (e) **嘈杂邻居导致的 CPU 竞争**。共享环境（云、Kubernetes）中的节点与另一租户争夺 CPU；延迟剧烈波动。健康检查在嘈杂邻居发威前完成；通过。  模式：故障真实存在（用户看到问题）但未被检测（监控是绿的）。运维...
  c22_b04.md: ...；集群在数小时内恢复。   #### Roblox 2021 多天中断  灰色故障与亚稳态级联的结合。触发器是一次导致 CPU 过度使用的部署；级联是重试风暴和连接池耗尽。亚稳态：系统的流量模式和资源使用稳定在一种损坏状态，没有干预无法恢复。耗时...
  c22_b05.md: ...秒以上（GC 期间）。节点 “活着” 但不可靠。  其他示例：返回错误数据的过期缓存；网络黑洞（能收不能发）；嘈杂邻居 CPU 竞争；通过结构检查但状态损坏。  模式：故障真实存在但标准监控未检测到。防御：深度健康检查锻炼与真实请求相同路径；基于...
  c30_b08.md: ...eat 给 Consul 集群带来负载）。 * **放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 +...
  c30_b08.md: ...服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gossip 协议的 BoltDB 状态在该故障模式下无边界增长。 * *...

=== after (23 occurrences) ===
  c03_b02.md: ...happens-before 语言表达就是：一条回复本质上是在发送者看到原消息之后发出的。回复的发送 happens-after 原消息的接收。如果我们用一种尊重 happens-before 的结构来给事件排序，回复保证出现在原消息之后，无论 w...
  c03_b03.md: ...P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send...
  c03_b03.md: ...### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P_1 | send m, ca...
  c03_b06.md: ...的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 | 100 | (100, 0) 2 | P_1 | 100（同一毫秒...
  c03_b09.md: ...L(a) = 5 < L(b) = 7 不蕴含 a \to b；两个事件可能并发。我们只知道 a 不 happens-after b。  这就是 Lamport 时间戳适合产生 happens-before 的全序扩展（例如日志排序），却不适合 _...
  c05_b01.md: ...了款，刷新页面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_（分区后两个副本各自接受写入）和...
  c05_b01.md: ...。主库接受写入，把写发给每个副本，等待每个副本确认，然后才向客户端确认写入。  性质：  * **Durability after ack** ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Post-fail...
  c05_b02.md: ...再响应客户端，但不等待所有副本。确认过的副本保证拥有该写；其他副本异步接收。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死...
  c05_b02.md: ...ed on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-...
  c05_b02.md: ...-write consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...
  c05_b04.md: ...团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表...
  c05_b04.md: ...经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a)...
  c05_b04.md: ...团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表...
  c05_b04.md: ...经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 read-after-write 问题。客户写（存款），然后读（仪表盘）。读走到尚未收到该写的 follower。  三种方法：  (a)...
  c05_b05.md: ...、Spanner、TiDB、YugabyteDB）让每个写都走 Raft 或 Paxos quorum，于是 read-after-write 窗口坍缩为“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避免客户存款失败。  较老系统...
  c05_b08.md: ...hDB。列出他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？** 异步 Postg...
  c05_b08.md: ...ge leader 转移只需数秒。如果支付处理的用户可见中断不可接受，差别重要。  (c) **应用需要什么 read-after-write 保证？** 若仪表盘必须立即反映存款，两个系统都能提供（Postgres 走主库读；CockroachDB...
  c05_b11.md: ...间权衡。$W + R > N$ 的法定人数规则给相交，不是线性一致性；差别是许多真实复制 bug 的根源。  Read-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-session...
  c17_b04.md: ...在排行榜上看到 A 在 B 前（或至少系统不能显示 B 的提交仿佛先于 A）。  因果关系：B 的提交 happens-after B 看到 A 的分数；因此 B 的提交 happens-after A 的提交。因果一致保持这一点：所有副本都显示 A...
  c17_b04.md: ...于 A）。  因果关系：B 的提交 happens-after B 看到 A 的分数；因此 B 的提交 happens-after A 的提交。因果一致保持这一点：所有副本都显示 A 在 B 前。  对两个并发提交（玩家 C 和 D 同时提交且互未看...
  c17_b05.md: ...何读与任何已提交写至少在一个副本上重叠。这比最终一致强，但弱于线性化。  QUORUM 提供：  * 单会话 read-after-write：若写成功（Q 个写入副本 ack）且后续读用 QUORUM，相交节点有该写；读能看到。 * 跨会话排序：若...
  c17_b07.md: ...得怎样？” 用户 B 回复 “很棒！”，每个观众都应先看 A 的帖再看 B 的回复。因果关系：B 的帖 happens-after B 看到 A 的帖。  (b) 并发消息可任意顺序：用户 C 和 D 同时发帖且未看到对方，观众可看到不同顺序。这没问...
  c18_b04.md: ...accept         Bank-->>C2: ok         Note over C1: resumed after pause         C1->>Bank: commit (token=100, stale)...

=== map (23 occurrences) ===
  c04_b11.md: ...CRDT 语义可以扩展到更高吞吐。  反驳：CRDT 只对真正可交换的数据类型和操作有效。集合、计数器、寄存器、某些 map 有 CRDT 形式化。冲突操作（库存提交、唯一用户名声明、支付提交）无法表达为 CRDT，因为冲突解决是应用的业务逻辑...
  c16_b02.md: ...ombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register...
  c16_b02.md: ...累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每...
  c16_b02.md: ...做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值的集合（类似 OR-Set）。  对协作文辑，相关的...
  c16_b02.md: ...R-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值的集合（类似 OR-Set）。  对协作文辑，相关的 CRDT 是 _list_（或数组、序列），允许...
  c16_b03.md: ....Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y...
  c16_b06.md: ...og 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Automerge 采用结合两者优势的混合方案。  **Q2.** 走一遍 G-Counter 和...
  c16_b06.md: ...影响，有哪些替代方案？  提示  LWW 依赖时间戳；时钟偏移导致错误的 “赢家”。替代：MV-Register、OR-Map 等更丰富的类型。...
  c16_b08.md: ...值使用 CRDT。该用什么 CRDT 结构，有哪些坑？  提示  表格：cell ID 到值的映射。简单情况用 LWW-Map；同一单元格并发编辑是坑。公式更难。  **答案：** 基本结构：LWW-Map（或 OR-Map），键是 cell I...
  c16_b08.md: ...ID 到值的映射。简单情况用 LWW-Map；同一单元格并发编辑是坑。公式更难。  **答案：** 基本结构：LWW-Map（或 OR-Map），键是 cell ID（如 “A1”、“B2”），值是单元格内容。  对简单单元格值（文本、数字），...
  c16_b08.md: ...。简单情况用 LWW-Map；同一单元格并发编辑是坑。公式更难。  **答案：** 基本结构：LWW-Map（或 OR-Map），键是 cell ID（如 “A1”、“B2”），值是单元格内容。  对简单单元格值（文本、数字），带 HLC 时间戳...
  c16_b08.md: ...ell ID（如 “A1”、“B2”），值是单元格内容。  对简单单元格值（文本、数字），带 HLC 时间戳的 LWW-Map 可用。两个用户并发编辑 A1 时，HLC 更高的写胜出。  坑：  (a) **公式**。单元格包含 `=SUM(A1...
  c16_b09.md: ...→ In Progress → Done）。用寄存器或 LWW 字段建模比 list CRDT 容易；Yjs 的 Y.Map 或 Automerge 的结构化 doc 都可以。 * **评论**：只追加的评论列表。任一 CRDT 都能处理。 *...
  c19_b03.md: ...SRV 或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kub...
  c19_b04.md: ...DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Ma...
  c19_b04.md: ...ap 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Map 暴露；全球客户解析到最近的区域负载均衡。  这四个系统分层。每个处理自己的领域；跨领域交互通过明确定义的 API。这比...
  c19_b10.md: ...过 Consul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS 解析。...
  c19_b10.md: ...cd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Java 中心化服务发现。Ne...
  c19_b10.md: ...使用；2026 年较不常见，但某些遗留系统仍在用。  对 2026 年的新 EC2 部署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深...
  c19_b10.md: ...署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出 Kube...
  c19_b11.md: ...你们团队目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的注册表。2026 年...
  c19_b11.md: ...哪些实例现在健康” 的注册表。2026 年标准解是 etcd、Consul 和基于 DNS 的发现（AWS Cloud Map 等云厂商服务）。etcd 主导云原生细分市场；Consul 处理 VM 和多 DC 部署；DNS 对稳定服务足够。...
  c19_b11.md: ...netes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每个处理自己的领域；跨领域交互明确。  第20章开始第七部分：运营分布式系统。分布式追踪与调试：因果...

=== shard (23 occurrences) ===
  c05_b10.md: ...）。纪律是问“这个操作需要什么？”而不是“系统默认是什么？”  第二个考虑：Raft-quorum 系统每 range/shard 有 _写吞吐上限_ ，由 Raft 往返成本决定。对每键极端写吞吐的工作负载，异步复制或批量方法可能 outperfo...
  c08_b09.md: ...Paxos（或 Raft）复制协调者状态，使协调者故障后可恢复协调者状态。这就是 Spanner 模式（第13章）：跨 shard 事务用 2PC，每个 shard 内部用 Paxos 保证 per-shard 持久性。...
  c08_b09.md: ...者状态，使协调者故障后可恢复协调者状态。这就是 Spanner 模式（第13章）：跨 shard 事务用 2PC，每个 shard 内部用 Paxos 保证 per-shard 持久性。...
  c08_b09.md: ...Spanner 模式（第13章）：跨 shard 事务用 2PC，每个 shard 内部用 Paxos 保证 per-shard 持久性。...
  c11_b01.md: ...、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard 基于共识** ：CockroachDB、TiDB、YugabyteDB。每个分片（range、region、key r...
  c11_b09.md: ...。对两者应用诊断方法，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但用 C++ 编写，采用 shard-per-core。协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Q...
  c12_b01.md: ...混合方案  现代系统常同时提供两者。CockroachDB 默认范围分片，但表可显式按 `crdb_internal` shard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering col...
  c13_b04.md: ...中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin...
  c13_b10.md: ...人数中的往返。对 Crescent 的 3 区域部署（US-East / EU-West / US-West），per-shard 提交最坏情况约 90 ms（第6章 AP Q2）。事务必须在所有三个分片提交后才会返回给应用；总提交延迟受最慢分片加上...
  c22_b08.md: ...因为限流无法有效完成）。  比二选一更好：让系统不需要选择。load shedding（自动 per-node 限流）、shard 隔离（一个 shard 的亚稳态不扩散）、优雅降级（高负载下禁用部分功能）等机制避免这种困境。  更深教训：亚稳态故障...
  c24_b10.md: ...GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU...
  c25_b03.md: ...V cache 以块形式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV c...
  c25_b09.md: ...两个阶段，每个阶段在不同 GPU。优化集群硬件利用率。  组合：70B 模型加分离：  * Prefill 节点：每 shard 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 sh...
  c25_b09.md: ...rd 4 张 GPU（tensor parallel）；模型权重分布在这 4 张 GPU。 * Decode 节点：每 shard 4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill...
  c25_b09.md: ...4 张 GPU（tensor parallel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张...
  c25_b09.md: ...lel）；模型权重同样分布。 * KV cache 传输：从 prefill shard 的 GPU 到 decode shard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shar...
  c25_b09.md: ...hard 的 GPU。  对 Continuum：每个 prefill 节点有 4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cach...
  c25_b09.md: ...4 张 H100 服务一个 model shard；每个 decode 节点有 8 张 H200 服务一个 model shard（4 张给模型，4 张给 KV cache）。分离在请求阶段层面；sharding 在模型层面。  2026 年大模型两...
  c28_b04.md: ...在 30 ms 内提交。  (9) 事务在 CockroachDB 中提交：更新 Adaeze 余额（US-East shard），更新 Femi 余额（EU-West shard，拉各斯商户），更新 merchant_day_rollup。跨 s...
  c28_b04.md: ...kroachDB 中提交：更新 Adaeze 余额（US-East shard），更新 Femi 余额（EU-West shard，拉各斯商户），更新 merchant_day_rollup。跨 shard、跨 region 事务，带 HLC 时间戳...
  c28_b04.md: ...d），更新 Femi 余额（EU-West shard，拉各斯商户），更新 merchant_day_rollup。跨 shard、跨 region 事务，带 HLC 时间戳；为可串行化一致性做 commit-wait。95 ms（受第6章所说的最坏...
  c28_b10.md: ...failover 未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 shard/region 原子提交或原子中止 | 2PC + Raft 复制协调...
  c28_b10.md: ...ide 的跨 region 支付 | Ch.13 §13.7 | 多 shard 跨 region 支付事务 | 所有 shard/region 原子提交或原子中止 | 2PC + Raft 复制协调器 + HLC 版 MVCC | 协调器状态在易失...

=== spine (23 occurrences) ===
  c06_b03.md: ...### 6.4 CLOS 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles Clos 命名，1953），...
  c06_b03.md: ..._（以 Charles Clos 命名，1953），2026 年实现为 _leaf-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接...
  c06_b03.md: ...tches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any...
  c06_b03.md: ...提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理...
  c06_b03.md: ...器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）*...
  c06_b03.md: ...宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、ac...
  c06_b03.md: ...ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产生带宽瓶颈，...
  c06_b03.md: ...### 6.4 CLOS 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles Clos 命名，1953），...
  c06_b03.md: ..._（以 Charles Clos 命名，1953），2026 年实现为 _leaf-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接...
  c06_b03.md: ...tches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any...
  c06_b03.md: ...提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理...
  c06_b03.md: ...器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）*...
  c06_b03.md: ...宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、ac...
  c06_b03.md: ...ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产生带宽瓶颈，...
  c06_b06.md: ...工程多强。承诺跨太平洋操作 30 ms p99 的团队在承诺不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 agg...
  c06_b06.md: ...不可能。  **Q2.** 用 2-3 句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** L...
  c06_b06.md: ...句话描述 leaf-spine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交...
  c06_b06.md: ...leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 s...
  c06_b06.md: ...成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（co...
  c06_b06.md: ...e 有两层：leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三...
  c06_b06.md: ...务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三层的 aggregation 交换...
  c06_b06.md: ...层的 aggregation 交换机对东西向（服务器到服务器）流量成为瓶颈，而现代分布式工作负载以东西向为主；leaf-spine 提供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、US-Wes...
  c06_b12.md: ...ned、globally consistent）成本画像不同；选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工...

=== femi (23 occurrences) ===
  c13_b01.md: ...Olamide 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Adaeze 的账户（一个分片）、商家的账户（另一个分片）和商家的日汇总记录（第三个分片）。三处要么全提交...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     --...
  c13_b10.md: ...Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务在 Crescent 内部 B...
  c13_b10.md: ...ce = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPD...
  c13_b10.md: ...0 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET b...
  c13_b10.md: ...E accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SE...
  c13_b10.md: ...nce = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1...
  c13_b10.md: ...nt_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';     COMMIT;   在 CockroachDB 下：...
  c13_b10.md: ...Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到...
  c13_b10.md: ...rite intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。...
  c13_b10.md: ...### 13.7 工作流 8：Olamide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     --...
  c13_b10.md: ...Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务在 Crescent 内部 B...
  c13_b10.md: ...ce = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPD...
  c13_b10.md: ...0 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET b...
  c13_b10.md: ...E accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SE...
  c13_b10.md: ...nce = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1...
  c13_b10.md: ...nt_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';     COMMIT;   在 CockroachDB 下：...
  c13_b10.md: ...Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到...
  c13_b10.md: ...rite intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。...
  c20_b01.md: ...aeze<br/>30 ms (US-East shard)"]         DB --> DB3["UPDATE Femi<br/>40 ms (EU-West shard, cross-region)"]         DB --> DB...
  c28_b04.md: ...### 28.2 一笔支付的旅程  追踪一笔从圣保罗 Adaeze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验...
  c28_b04.md: ...的 SLO）；分数低于阈值；支付继续。  (8) 事务写入 TigerBeetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replication 在 30 ms 内提交。  (9...
  c28_b04.md: ...提交。  (9) 事务在 CockroachDB 中提交：更新 Adaeze 余额（US-East shard），更新 Femi 余额（EU-West shard，拉各斯商户），更新 merchant_day_rollup。跨 shard、跨 re...

=== level (23 occurrences) ===
  c15_b03.md: ...域后被另一区域并发写覆盖的情况（罕见但可能）。   #### Cassandra  按操作可调。Consistency level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-opera...
  c15_b03.md: ...ra  按操作可调。Consistency level ONE：一个副本响应即可；非常快；最终。Consistency level QUORUM：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consi...
  c15_b03.md: ...：多数响应；per-operation 线性化（关于 sloppy quorum 有注意事项）。Consistency level ALL：所有副本；只对特定一致性证明有用。  应用纪律：按需求为每次操作选择一致性级别。许多 Cassandra 生产...
  c15_b08.md: ...的一致哈希把某用户所有写路由到同一协调器。单一协调器时钟能正确排序该用户的写。Cassandra 通过 session-level 路由支持。  (c) **轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。LWT 用 P...
  c23_b05.md: ...### 23.7 混沌工程成熟度模型  团队的混沌实践处于哪一层？粗略成熟度模型：  (a) **Level 0（无混沌）**：不故意注入故障。团队从真实事故中了解故障模式。  (b) **Level 1（手动混沌）**：偶尔的...
  c23_b05.md: ...成熟度模型：  (a) **Level 0（无混沌）**：不故意注入故障。团队从真实事故中了解故障模式。  (b) **Level 1（手动混沌）**：偶尔的即兴实验。团队 kill 一个节点看会发生什么。没有结构化纪律。  (c) **Level...
  c23_b05.md: ...*Level 1（手动混沌）**：偶尔的即兴实验。团队 kill 一个节点看会发生什么。没有结构化纪律。  (c) **Level 2（Game days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 gam...
  c23_b05.md: ...days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey 风格：工作时间持续随机...
  c23_b05.md: ...d 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey 风格：工作时间持续随机注入故障。系统一直在被测试。...
  c23_b05.md: ...沌）**：生产中的自动混沌。Chaos Monkey 风格：工作时间持续随机注入故障。系统一直在被测试。  (e) **Level 4（混沌作为设计输入）**：混沌发现驱动架构决策。团队用混沌数据确定优先级；新功能发布前通过混沌测试。  2026 年...
  c23_b05.md: ...沌发现驱动架构决策。团队用混沌数据确定优先级；新功能发布前通过混沌测试。  2026 年，大多数成熟 SRE 组织处于 Level 2-3。Level 4 是愿景；很少有团队持续达到。  进阶是从 “我们被动应对故障” 到 “故障是设计约束，而非惊喜...
  c23_b05.md: ...团队用混沌数据确定优先级；新功能发布前通过混沌测试。  2026 年，大多数成熟 SRE 组织处于 Level 2-3。Level 4 是愿景；很少有团队持续达到。  进阶是从 “我们被动应对故障” 到 “故障是设计约束，而非惊喜”。每一步都提升运维...
  c23_b12.md: ...组织实践**：无责复盘、心理安全、记录学习、固定节奏。没有这些，技术实验不产生学习。  (d) **成熟度进阶**：从 Level 2（game day）到 Level 3（持续混沌）之前。每一层需要前一层；跳过会坏。  (e) **具体第一次实验*...
  c23_b12.md: ...学习、固定节奏。没有这些，技术实验不产生学习。  (d) **成熟度进阶**：从 Level 2（game day）到 Level 3（持续混沌）之前。每一层需要前一层；跳过会坏。  (e) **具体第一次实验**：对 “先做什么” 的具体建议。对新...
  c23_b13.md: ...y）实现故障注入；选择取决于部署。Jepsen 项目是另一种风味：系统性地在故障下测试数据库正确性声明。  成熟度模型：Level 0（无混沌）、Level 1（手动）、Level 2（game day）、Level 3（持续）、Level 4（混沌...
  c23_b13.md: ...于部署。Jepsen 项目是另一种风味：系统性地在故障下测试数据库正确性声明。  成熟度模型：Level 0（无混沌）、Level 1（手动）、Level 2（game day）、Level 3（持续）、Level 4（混沌作为设计输入）。大多数成熟...
  c23_b13.md: ...数据库正确性声明。  成熟度模型：Level 0（无混沌）、Level 1（手动）、Level 2（game day）、Level 3（持续）、Level 4（混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  O...
  c23_b13.md: ...）、Level 2（game day）、Level 3（持续）、Level 4（混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Co...
  c23_b13.md: ...game day）、Level 3（持续）、Level 4（混沌作为设计输入）。大多数成熟组织处于 Level 2-3；Level 4 是愿景。  Olamide 的工作流 11 game day（区域故障转移）发现真实问题：Consul 健康检查调...
  c27_b07.md: ...，而非仅存在于代码中。 * 这是静态审查的经验补充。 * 成熟度映射：第三层对应第23章 §23.7 的混沌工程成熟度 Level 2-3（scheduled game days 和持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到...
  c27_b07.md: ...工程成熟度 Level 2-3（scheduled game days 和持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到 Level 3 意味着混沌节奏是持续而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查...
  c27_b07.md: ...2-3（scheduled game days 和持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到 Level 3 意味着混沌节奏是持续而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查 → 人类审查 → 混...
  c27_b07.md: ...持续自动混沌）。良好运行第三层的团队至少处于 Level 2；达到 Level 3 意味着混沌节奏是持续而非仅在发布前。Level 4（混沌发现作为设计输入）是愿景：AI 审查 → 人类审查 → 混沌验证循环闭合；game day 发现反馈为 AI...

=== shedding (23 occurrences) ===
  c21_b03.md: ...### 21.5 Load Shedding  当服务过载时，两种响应：  (a) **排队并延迟**：入站请求在队列中等待，直到服务能处理。最终，请求在调用者处超...
  c21_b03.md: ...出服务容量的入站请求立即被拒绝，返回 “太忙” 响应。调用者快速知道，并可决定重试、故障转移或向用户失败。  Load shedding（选项 b）在级联故障条件下结构上更好，因为它避免导致放大的队列堆积。  实现：每个服务跟踪当前并发度；超过阈值时拒绝请...
  c21_b03.md: ...加 `Retry-After` header）让调用者优雅处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值...
  c21_b03.md: ...处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值。延迟升高时阈值降低；延迟正常时阈值升高。   ### 2...
  c21_b03.md: ...，已来不及干预。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸。继电器就是 load shedding 机制。没有它们，级联会更糟（整个发电厂受损）。  (d) **系统设计必须遏制故障**。电网为效率互联（跨区域共享发电...
  c21_b03.md: ...孤岛，可在级联期间隔离。  分布式系统类比：微服务舰队为功能互联；互联成为级联路径。防御（重试预算、熔断器、load shedding）必须在每个节点运行；仅集中监控无法防止级联。...
  c21_b03.md: ...### 21.5 Load Shedding  当服务过载时，两种响应：  (a) **排队并延迟**：入站请求在队列中等待，直到服务能处理。最终，请求在调用者处超...
  c21_b03.md: ...出服务容量的入站请求立即被拒绝，返回 “太忙” 响应。调用者快速知道，并可决定重试、故障转移或向用户失败。  Load shedding（选项 b）在级联故障条件下结构上更好，因为它避免导致放大的队列堆积。  实现：每个服务跟踪当前并发度；超过阈值时拒绝请...
  c21_b03.md: ...加 `Retry-After` header）让调用者优雅处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值...
  c21_b03.md: ...处理。  艺术在于设置阈值。太低会拒绝本可处理的流量；太高则 shedding 生效前队列已堆积。  自适应 load shedding（Google、AWS 等使用）：服务基于观测延迟动态调整阈值。延迟升高时阈值降低；延迟正常时阈值升高。   ### 2...
  c21_b03.md: ...，已来不及干预。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸。继电器就是 load shedding 机制。没有它们，级联会更糟（整个发电厂受损）。  (d) **系统设计必须遏制故障**。电网为效率互联（跨区域共享发电...
  c21_b03.md: ...孤岛，可在级联期间隔离。  分布式系统类比：微服务舰队为功能互联；互联成为级联路径。防御（重试预算、熔断器、load shedding）必须在每个节点运行；仅集中监控无法防止级联。...
  c21_b04.md: ...节点需要 5 分钟才能就绪。  **恢复**：  * 14:35，运维手动在 merchant 数据库上调用 load shedding（只接受最高优先级流量）。 * 这让已在 flight 的查询完成；数据库开始恢复。 * 14:42，上游服务重试预算耗...
  c21_b05.md: ...进而产生更多放大，进而产生更多延迟。  防御针对放大：重试预算限制重试驱动的放大；熔断器停止向故障组件加负载；load shedding 防止队列堆积。  **Q2.** 什么是重试预算，它如何限制放大？  提示  重试预算：把重试流量限制为成功流量的一部...
  c21_b06.md: ...须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸隔离故障。分布式系统类比：每个服务应有自己的熔断器、load shedding、重试预算；集中式防御在网络分区时会失效。  (d) **系统设计必须遏制故障**。互联电网（为共享发电而高效）成为级联...
  c21_b07.md: ...的重试，没有熔断器。他们想逐步增加弹性。改变顺序是什么？  提示  最高杠杆优先：重试预算，然后熔断器，然后 load shedding。  **答案：** 推荐顺序：  (a) **每个客户端加重试预算**：实现重试预算可防止最常见的放大机制（重试风暴）...
  c21_b07.md: ...j（Java）、其他语言 Hystrix-like 库，或 Envoy 代理。  (c) **每个服务入口加 load shedding**：每个服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-ser...
  c21_b07.md: ...务按 2 倍正常负载配置。缓冲吸收尖峰而不饱和。  (e) **为以上加可观测性**：监控重试率、熔断器状态、load-shedding 计数器。任何异常时告警；告警表明正在发生事故。...
  c21_b08.md: ...产生工作供下游消费的源服务。等价：产生交易事件的支付服务。  * **发电厂跳闸（离线自保）**：过载服务的 load shedding。  * **电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blackstart 孤岛**...
  c21_b08.md: ...对分布式系统工程师的教训：模式是普遍的。任何具有放大和缺乏遏制的互联系统都会发生级联故障。相同防御（熔断器、load shedding、可观测性）适用。...
  c21_b10.md: ...pen 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load shedding 防止队列堆积。用 HTTP 503 拒绝超额请求；调用者快速知道并可故障转移。  2003 年美国东北部大停电（500...
  c22_b06.md: ...熔断器在每个状态稳定，但对真实转换响应迅速。  该模式可泛化：任何翻转行为的信号都应有双向不同阈值。自动扩缩、load shedding、领导者选举都受益于滞后。没有它，系统会在状态间 “拍动”；有它，系统稳定但响应迅速。  **Q4.** 为什么深度健康...
  c22_b08.md: ...恢复值得。  * 对没有良好限流工具的系统：重启（因为限流无法有效完成）。  比二选一更好：让系统不需要选择。load shedding（自动 per-node 限流）、shard 隔离（一个 shard 的亚稳态不扩散）、优雅降级（高负载下禁用部分功能）...

=== gossip (22 occurrences) ===
  c02_b05.md: ...性一致性是过度设计，会把吞吐限制很多倍。实现为 CRDT 的计数器（G-Counter，第16章）放在每个副本上，通过 gossip 增量收敛，可以水平扩展而无需协调。   #### 排行榜  游戏中前 10 名分数，几秒钟内为所有观众刷新。每次分数提...
  c04_b02.md: ...息就丢失了。  通过 flooding 实现可靠广播的代价是在全连接拓扑下每条消息 $O(N^2)$ 条消息。存在基于 gossip 的变体，平均以 $O(N \log N)$ 条消息达到概率可靠；Bimodal Multicast（Birman 等）...
  c04_b10.md: ...每条消息携带 per-group 向量（组大小条目）加一个 per-region 摘要。跨组因果以较粗粒度跟踪。可扩展 gossip 系统就是这样做的；代价是跨组因果近似而非精确。  第三种选择：对某些应用用 _causal+ consistency_...
  c04_b11.md: ...远不会得到它。Agreement 需要恢复后重新交付，通常通过发送者持久化 ack 跟踪或基于 tombstone 的 gossip 层实现。  (c) **跨发送者没有顺序。** 若两个发送者广播应排序的事件（例如同一实体的“创建”和“删除”），接收...
  c11_b01.md: ...eader、基于法定人数、基于共识、自定义。   2. **故障恢复语义是什么？** Leader 重选（多久）、基于 gossip 的收敛、人工干预。   3. **一致性模型是什么？** 线性化、可串行化、快照、因果、最终、按操作可调。   4....
  c11_b02.md: ...s 带租约类似。PostgreSQL 逻辑故障切换需外部工具（Patroni、repmgr），10-60 秒。 * **Gossip 收敛** ：Cassandra 通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外...
  c11_b02.md: ...外部工具（Patroni、repmgr），10-60 秒。 * **Gossip 收敛** ：Cassandra 通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brai...
  c11_b02.md: ...s 带租约类似。PostgreSQL 逻辑故障切换需外部工具（Patroni、repmgr），10-60 秒。 * **Gossip 收敛** ：Cassandra 通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外...
  c11_b02.md: ...外部工具（Patroni、repmgr），10-60 秒。 * **Gossip 收敛** ：Cassandra 通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brai...
  c11_b04.md: ...l | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistency model | 按操作可调；默认最终，...
  c11_b09.md: ...lication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behav...
  c15_b04.md: ...量文章的页面浏览计数器。每次浏览是一次递增；总数展示给读者。   #### 最终一致性  每个副本维护本地计数器，通过 gossip 传给其他副本。显示总数是所有副本之和（或离读者最近副本的值）。  成本：每次递增零协调；亚毫秒写延迟。  权衡：显示总...
  c18_b01.md: ...以及 _fencing token_ 的作用。  本章讲授两个相关原语：分布式缓存（具体是 Redis Cluster gossip 与缓存失效模式）和分布式锁（具体是为什么朴素锁会失败、正确原语长什么样）。第1章 Crescent 支付系统 “身份谎...
  c18_b01.md: ...edis、Redis Labs）。下面讨论聚焦 Redis Cluster。   #### Redis Cluster gossip  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 s...
  c18_b01.md: ...16,384 个 hash slot 之一；每个 slot 属于一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向随机选择的其他节点子集发送 PING；PING 携带发送者对集群的视图。接收者基于 PING 更...
  c18_b01.md: ...PING；PING 携带发送者对集群的视图。接收者基于 PING 更新自己的视图。随时间推移，集群对自身的视图收敛。  gossip 不是共识；分区下，集群两半可能有不一致视图。Redis Cluster 要求大多数 master 节点同意配置变更（s...
  c18_b11.md: ...生成的代码会重新引入重复扣款 bug。   ### 本章总结  分布式缓存（Redis Cluster 作为典型）用 gossip 维护成员关系，可调失效模式（cache-aside、write-through、TTL、event-driven）。缓...
  c19_b08.md: ...副本 Consul，一个节点故障）期间，服务间流量中断 30 秒。诊断并给出修复方案。  提示  Consul 基于 gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：** 诊断：Consul...
  c30_b01.md: ...n, K., et al. (1999). "Bimodal multicast." _ACM TOCS_. — 基于 gossip 的可靠广播。...
  c30_b02.md: ...Buchman, E., Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT consensus." _arXiv:1807.04938_. — Tendermint。   ####...
  c30_b04.md: ...| PA/EL | AWS 原生 KV；可预测延迟 | | **Cassandra** | 无领导者；可调 | 基于 gossip；10-30 秒 | 默认最终；LWT 用于线性化 | 可调 | 可调 | 写重；大规模横向扩展 | | **Found...
  c30_b08.md: ...ing 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gossip 协议的 BoltDB 状态在该故障模式下无边界增长。 * **时间线**：73 小时客户可见中断（Roblox 史上最...

=== peer (22 occurrences) ===
  c03_b01.md: ...界 _预期_ 漂移；除非你主动测量并反应，否则它不会给你有界 _保证_ 漂移。一台 NTP 配置错误一周的服务器可能与 peer 偏差数秒，却仍然像健康节点一样服务请求。  还有两个锋利的棱角：  * **NTP 可以回退时间。** 当检测到较大漂...
  c03_b06.md: ...它拉向前。这个性质让 HLC 对小时钟偏移具有鲁棒性：时钟慢的节点不会因自己的慢时钟而被“排序更晚”；任何来自更快时钟 peer 的传入消息都会把它的 HLC 拉向前。...
  c03_b07.md: ...t，但精神相似：先界定时钟偏移，再设计容忍它的协议。   #### 常见误解  一个常见信念：“如果我跑 NTP 并与 peer 保持在 10 ms 内，我就有 TrueTime。”不。TrueTime 有两条 NTP 没有的性质：(a) 每次调用...
  c04_b02.md: ...n delivered:             delivered.add(msg)             for peer in all_processes:                 if peer != sender:...
  c04_b02.md: ...)             for peer in all_processes:                 if peer != sender:                     send(peer, msg)...
  c04_b03.md: ...者崩溃），仍有存活的正确进程持有它并重广播。       on broadcast(msg):         for peer in all_processes:             send(peer, msg)      on recei...
  c04_b03.md: ...in received:             received.add(msg)             for peer in all_processes:                 send(peer, ACK(msg))...
  c04_b09.md: ...其中一个接收者收到消息后、重广播前崩溃。消息还能到达其余正确接收者吗？  提示  flooding 下，发送者发给所有 peer。只要崩溃节点之外至少有一个 peer 收到并重广播，消息就会到达全部。  **答案：** 伪代码：       del...
  c04_b09.md: ...。消息还能到达其余正确接收者吗？  提示  flooding 下，发送者发给所有 peer。只要崩溃节点之外至少有一个 peer 收到并重广播，消息就会到达全部。  **答案：** 伪代码：       delivered = set()...
  c04_b09.md: ...vered.add(msg)         upper_layer.deliver(msg)         for peer in all_processes:             if peer != sender and peer !=...
  c04_b09.md: ...iver(msg)         for peer in all_processes:             if peer != sender and peer != self:                 send(peer, msg,...
  c04_b09.md: ...or peer in all_processes:             if peer != sender and peer != self:                 send(peer, msg, sender=self)...
  c07_b08.md: ...(c) 使用 Raft 的 “pre-vote” 扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发...
  c07_b08.md: ...扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre-vote（消...
  c10_b07.md: ...s 和 Raft 假设崩溃故障。  _Byzantine fault_ 是节点任意行为的故障：可能发送错误消息、向不同 peer 发送冲突消息（equivocation）、假装诚实，或主动误导协议。拜占庭故障既模型恶意行为（被攻破节点、对抗性验证者...
  c10_b07.md: ...必须在最多 f 个节点为拜占庭时正确工作。  一个正确副本收到来自其他节点的消息。为做决定，它需要识别哪些消息来自诚实 peer，哪些来自拜占庭 peer。它无法直接区分（拜占庭 peer 可声称任何内容）。  考虑场景：f 个拜占庭 peer 都...
  c10_b07.md: ...占庭时正确工作。  一个正确副本收到来自其他节点的消息。为做决定，它需要识别哪些消息来自诚实 peer，哪些来自拜占庭 peer。它无法直接区分（拜占庭 peer 可声称任何内容）。  考虑场景：f 个拜占庭 peer 都说“值 A 正确”；f 个...
  c10_b07.md: ...实 peer，哪些来自拜占庭 peer。它无法直接区分（拜占庭 peer 可声称任何内容）。  考虑场景：f 个拜占庭 peer 都说“值 A 正确”；f 个诚实 peer 说“值 B 正确”。其余 f 个诚实 peer 看到冲突，必须决定。从它们...
  c10_b07.md: ...法直接区分（拜占庭 peer 可声称任何内容）。  考虑场景：f 个拜占庭 peer 都说“值 A 正确”；f 个诚实 peer 说“值 B 正确”。其余 f 个诚实 peer 看到冲突，必须决定。从它们视角：f 个说 A，f 个说 B。它们无法区...
  c10_b07.md: ...考虑场景：f 个拜占庭 peer 都说“值 A 正确”；f 个诚实 peer 说“值 B 正确”。其余 f 个诚实 peer 看到冲突，必须决定。从它们视角：f 个说 A，f 个说 B。它们无法区分哪组是拜占庭的。  $N = 3f + 1$...
  c10_b07.md: ...说 B。它们无法区分哪组是拜占庭的。  $N = 3f + 1$ 时：任意 2f+1 法定人数包含至少 f+1 个诚实 peer。2f+1 的“超过 f 个诚实同意”可以压倒任何大小为 f 的拜占庭群体。两个这样的法定人数在至少 f+1 个副本上相...
  c10_b07.md: ...个经法定人数验证的决定在一位诚实见证上重叠。  BFT 需要额外副本的原因：给协议足够冗余，使任何法定人数的投票由诚实 peer 主导。  **Q3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view chang...

=== sequencer (22 occurrences) ===
  c03_b08.md: ...lock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequencer 确定性排序 | 到 sequencer 的延迟；不涉及时钟  正确选择取决于你的运营 footprint。如果你数据中...
  c03_b08.md: ...未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequencer 确定性排序 | 到 sequencer 的延迟；不涉及时钟  正确选择取决于你的运营 footprint。如果你数据中心有 GPS（或使用有云 TrueTim...
  c03_b08.md: ...ueTime 等价物的云厂商），TrueTime 风格最干净。如果没有，HLC 加已知偏移上界是实际默认。如果你能容忍 sequencer（一个排序事务的单点）的架构开销，Calvin 的方法完全消除时钟依赖。第13章会在事务深度再次回到这三种。   ###...
  c13_b03.md: ...事先按确定性顺序对所有事务排序，然后让每个分片按约定顺序执行。无冲突，无因锁冲突导致的中止。  结构：    1. **Sequencer** ：每个区域的服务接收事务请求。它批量处理并分配全局顺序（通过跨区域共识）。   2. **Scheduler**...
  c13_b04.md: ...#### Calvin 为何快  Sequencer 是唯一的同步点；执行跨分片并行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 se...
  c13_b04.md: ...er 是唯一的同步点；执行跨分片并行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequen...
  c13_b04.md: ...提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片...
  c13_b04.md: ...uencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   ###...
  c13_b04.md: ...。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处  * **FaunaDB...
  c13_b04.md: ...#### Calvin 为何快  Sequencer 是唯一的同步点；执行跨分片并行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 se...
  c13_b04.md: ...er 是唯一的同步点；执行跨分片并行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequen...
  c13_b04.md: ...提交；没有单独的提交阶段）。跨分片事务成本为一次 sequencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片...
  c13_b04.md: ...uencer 往返加上并行 per-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   ###...
  c13_b04.md: ...。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处  * **FaunaDB...
  c13_b05.md: ...yteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Sequencer 往返  2026 年的现实：大多数非 Google 分布式数据库使用 HLC + 偏移上...
  c13_b05.md: ...kew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Sequencer 往返  2026 年的现实：大多数非 Google 分布式数据库使用 HLC + 偏移上界。成本可变（取决于争用），但...
  c13_b13.md: ...排序再执行；无因锁冲突导致的中止。CockroachDB：先执行，冲突时重试。  **答案：**  _Calvin_ ：sequencer 在执行前全局排序所有事务。每个分片按约定顺序执行事务。无冲突（顺序预先决定）。无因锁冲突导致的中止。  优点：无重试成...
  c13_b13.md: ...先决定）。无因锁冲突导致的中止。  优点：无重试成本；确定性、可预测延迟；高争用下无 abort storm。  缺点：sequencer 是全局同步点；每笔事务付 sequencer 往返；sequencer 是跨区域瓶颈。  _CockroachDB（乐...
  c13_b13.md: ...试成本；确定性、可预测延迟；高争用下无 abort storm。  缺点：sequencer 是全局同步点；每笔事务付 sequencer 往返；sequencer 是跨区域瓶颈。  _CockroachDB（乐观快照隔离 + 可串行化）_ ：每个事务启动并...
  c13_b13.md: ...；高争用下无 abort storm。  缺点：sequencer 是全局同步点；每笔事务付 sequencer 往返；sequencer 是跨区域瓶颈。  _CockroachDB（乐观快照隔离 + 可串行化）_ ：每个事务启动并执行；提交时检测冲突。若检...
  c13_b13.md: ...hDB（乐观快照隔离 + 可串行化）_ ：每个事务启动并执行；提交时检测冲突。若检测到冲突，事务重启。  优点：无全局 sequencer；事务独立推进；低争用下吞吐很高。  缺点：争用导致重启；病态争用下产生 abort storm；高争用下延迟不可预测。...
  c30_b04.md: ...| | **FoundationDB** | 定制（sequencer + log + storage） | 亚秒级 sequencer 切换 | 可串行化 | CP | PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL...

=== change (22 occurrences) ===
  c06_b05.md: ...复制 100 TB 的系统，年费六位数。  架构含义：  * **复制 delta，而非全量。** 大多数跨区域复制是 change-data-capture，不是周期全量快照。 * **线上压缩。** 现代系统用 Zstd 等；对典型结构化数据可省...
  c07_b05.md: ...certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 prop...
  c07_b05.md: ...种分解对阅读新共识论文很有用。几乎每种“新共识协议”只改变三者之一：新法定人数结构（HotStuff 的线性 view change）、新 validity 检查（拜占庭验证者）、新终止机制（Narwhal-Bullshark 等基于 DAG 的协议）...
  c10_b01.md: ...FT --> HotStuff["HotStuff<br/>Yin et al. 2019<br/>O(N) view change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuf...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）** ：给定 N 个验证者的 BFT...
  c10_b03.md: ...证者的 BFT 集群（$N \ge 3f + 1$）和提议流，产出最多容忍 f 个拜占庭验证者的提交决定，且 view-change 成本为 $O(N)$。机制是 HotStuff 的三阶段投票（prepare / pre-commit / commi...
  c10_b03.md: ...与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham...
  c10_b03.md: ...ueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuf...
  c10_b03.md: ...）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）** ：给定 N 个验证者的 BFT...
  c10_b03.md: ...证者的 BFT 集群（$N \ge 3f + 1$）和提议流，产出最多容忍 f 个拜占庭验证者的提交决定，且 view-change 成本为 $O(N)$。机制是 HotStuff 的三阶段投票（prepare / pre-commit / commi...
  c10_b03.md: ...与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham...
  c10_b03.md: ...ueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuf...
  c10_b03.md: ...）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议...
  c10_b07.md: ...peer 主导。  **Q3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certificates，无需重播所有投票。  **答案：** HotStuff 的关键创...
  c10_b08.md: ...并广播（或在 pipeline HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 v...
  c10_b08.md: ...leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义：HotStu...
  c10_b08.md: ...(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让 view-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi Howard 的...
  c10_b09.md: ...障。  (a) **PBFT** ：被充分理解、成熟。消息复杂度 $O(N^2) = 441$ 条消息每决定，view change 也是 $O(N^2)$。21 节点下可工作，但会限制协议成熟后的吞吐。运维工具较旧（多数 PBFT 实现是研究代码）。...
  c10_b09.md: ...的吞吐。运维工具较旧（多数 PBFT 实现是研究代码）。  (b) **HotStuff** ：$O(N)$ view change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 Ho...
  c10_b12.md: ...uff（2019）通过 Quorum Certificates 和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG...
  c11_b08.md: ...eplication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事务的 stri...

=== dsql (22 occurrences) ===
  c11_b06.md: ...。  **Q3.** 用诊断方法给一个你没用过的系统产出一段阅读。可选：MongoDB、ScyllaDB、Aurora DSQL、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：** 示例 A...
  c11_b06.md: ...Beetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：** 示例 Aurora DSQL（AWS，2024 GA）：...
  c11_b07.md: ...Aurora DSQL 是 AWS 的 serverless、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres 兼容 S...
  c13_b17.md: ...结束第四部分：NewSQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Presto、Snowflake、BigQuery...
  c14_b01.md: ...章讲授全景：NewSQL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（Trino/Presto、Snowflake、...
  c14_b01.md: ...QL。 * **YugabyteDB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：serverle...
  c14_b03.md: ...### 14.6 Aurora DSQL  AWS 2024 GA 的分布式 SQL 数据库。架构与上述其他系统不同。  关键属性：  * 每租户 Firecr...
  c14_b03.md: ...或 Spanner）。 * 每租户 Firecracker 开销可能让习惯多租户数据库引擎的团队意外。  Aurora DSQL 适合：AWS 原生工作负载，需要多区域 Postgres 又不想管理 CockroachDB 集群。新兴示例：AWS...
  c14_b03.md: ...res 又不想管理 CockroachDB 集群。新兴示例：AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Serverless Postgres，存储与计算分离。严格说不是多...
  c14_b05.md: ...域 CockroachDB 多区域 OLTP | CockroachDB、Spanner（若在 GCP）、Aurora DSQL（若在 AWS）、TiDB（若用 MySQL） 金融账本 | TigerBeetle（账本）+ Postgres/Coc...
  c14_b05.md: ...e、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | CockroachDB（任何云）、Snowflake（OLAP 任何云） 大规模水平扩展，可接受最...
  c14_b08.md: ...全球服务；可串行化事务保证支付完整性；可跑在任何云上。曾考虑替代：Spanner（因 GCP 锁定被否）；Aurora DSQL（若仅限 AWS 可接受，但生产记录较新）；单区域 Postgres（因缺乏多区域灾备被否）。  (b) **账本**：...
  c14_b09.md: ...单 Aurora Postgres 实例峰值 CPU 60%，延迟在下降。他们需要决定：手动分片、迁移到 Aurora DSQL，还是迁移到 CockroachDB。走一遍权衡。  提示  手动分片：工作量最大、控制最大。Aurora DSQL：托...
  c14_b09.md: ...a DSQL，还是迁移到 CockroachDB。走一遍权衡。  提示  手动分片：工作量最大、控制最大。Aurora DSQL：托管、AWS 原生、较新。CockroachDB：托管或自托管、多云、成熟。  **答案：**  _选项 1：手动分片...
  c14_b09.md: ...license）。  推荐因素：  * 若团队可预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：CockroachDB。 * 若团队增长已放缓且希...
  c14_b09.md: ...纵向扩展）。关键问题是未来 12-24 个月是否一定需要多区域或进一步扩展。若是，现在就选 CockroachDB 或 DSQL 并增量迁移；若否，推迟。  许多团队在这种情况下选择 “纵向扩展 + 读副本” 作为下一步，只在被迫时才迁移到 New...
  c14_b12.md: ...ewSQL 会增加前期成本，而收益可能永远不会兑现。  第二个考虑：serverless 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL 让你无需运维 NewSQL。两者都以不...
  c14_b12.md: ...less 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL 让你无需运维 NewSQL。两者都以不同的扩展特性提供 “托管简单性”。选择取决于具体工作负载的扩展轴。  **Q2....
  c29_b01.md: ...：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；API 已可用但尚未被普遍消费。  2027-2030 轨迹：  * 预计 **CockroachDB、...
  c29_b03.md: ...仍在生产；2026 年协议仍是标准。通读。  * **2028**：部分具名系统可能已弃用或被收购/更名。Aurora DSQL 演进；vLLM 可能已被继任者替代。以 “它现在叫什么？” 的态度阅读；底层原语未变。用本书诊断方法（第11章）评估...
  c30_b04.md: ...ncer 切换 | 可串行化 | CP | PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...
  c30_b10.md: ...DB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选 Aurora DSQL。...

=== secondary (22 occurrences) ===
  c11_b08.md: ...持久化到多数；主库故障后存活。 * _默认读_ ：`readConcern: local` 从主库读。主库读线性化，从 secondary 读则最终一致（MongoDB 客户端默认 `readPreference: secondary` 时）。 * _Maj...
  c11_b08.md: ...库读。主库读线性化，从 secondary 读则最终一致（MongoDB 客户端默认 `readPreference: secondary` 时）。 * _Majority 读_ ：`readConcern: majority` 返回已被多数 ack 的数据...
  c13_b03.md: ...p 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：C...
  c13_b03.md: ...：写入 commit 记录（value, commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是...
  c13_b03.md: ...tamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的...
  c13_b03.md: ...2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞见：primary key 充当...
  c13_b03.md: ...r 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 pri...
  c13_b03.md: ...p 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：C...
  c13_b03.md: ...：写入 commit 记录（value, commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是...
  c13_b03.md: ...tamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的...
  c13_b03.md: ...2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞见：primary key 充当...
  c13_b03.md: ...r 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 pri...
  c13_b12.md: ...其中一个键为 _primary_。原子提交点是 primary 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端...
  c13_b12.md: ...ry 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈...
  c13_b12.md: ...在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清理由读者摊销；无需单独恢复进程。  用于：TiDB（Percolator 派生），以及概...
  c13_b12.md: ...其中一个键为 _primary_。原子提交点是 primary 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端...
  c13_b12.md: ...ry 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈...
  c13_b12.md: ...在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清理由读者摊销；无需单独恢复进程。  用于：TiDB（Percolator 派生），以及概...
  c17_b07.md: ...查：本地 oplog 是否已应用到该 clusterTime？若是，服务读；若否，等待赶上（或超时）。  网络抖动期间，secondary 复制滞后。对滞后 secondary 的因果会话读会阻塞，直到：  (a) secondary 赶上（可能数秒，取决于...
  c17_b07.md: ...clusterTime？若是，服务读；若否，等待赶上（或超时）。  网络抖动期间，secondary 复制滞后。对滞后 secondary 的因果会话读会阻塞，直到：  (a) secondary 赶上（可能数秒，取决于滞后）。(b) 读超时。  用户可见：...
  c17_b07.md: ...。  修复：  (a) **读路由到主库**：因果会话可配置 `readPreference: primary`，消除 secondary 滞后问题。代价：失去读扩展。...
  c17_b08.md: ...(b) **降低复制滞后**：为 secondary 调优以降低滞后（更多内存、更快网络、更多并行应用线程）。缩小等待窗口但不消除。  (c) **缩短超时并回退**：配置...

=== request (22 occurrences) ===
  c19_b04.md: ...：服务间加密与认证。  (c) **流量策略**：金丝雀发布、蓝绿、A/B 测试。  (d) **可观测性**：per-request 追踪、延迟指标、错误率。  (e) **弹性**：熔断、重试预算、超时策略。  2026 年两大主导服务网格：Isti...
  c24_b05.md: ...的客户定价：通常每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个请求的 GPU-秒。更好的 attention（PagedAttention、Ra...
  c24_b05.md: ...经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理...
  c24_b06.md: ...阶段是 2025-2026 架构突破。  (c) **KV cache 作为中心状态**。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-reque...
  c24_b06.md: ...*。KV cache 是 per-request 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 to...
  c24_b06.md: ...quest 工作内存（1-100 GB），短命，是内存和吞吐瓶颈。微服务 per-request 状态小；推理 per-request 状态大。  (d) **严格的 per-token 延迟预算**。每个生成 token 必须在 10-50 ms 内完...
  c24_b06.md: ...延迟预算**。每个生成 token 必须在 10-50 ms 内完成。网络跳数超过几毫秒会显著影响吞吐。微服务 per-request 预算更宽松；推理有 per-token 预算。  (e) **突发工作负载模式**。请求从单次问答到大文档分析不等。集...
  c24_b06.md: ...(e) **突发工作负载模式**。请求从单次问答到大文档分析不等。集群必须处理混合而无线头阻塞。微服务通常 per-request profile 更均匀。  这些差异证明把分布式 LLM 推理视为独立子学科。协议（PagedAttention、Ra...
  c24_b10.md: ...；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、per-request 延迟。LLM 应提供完整可观测性方案。  (e) **自动扩缩**：随负载变化，集群应扩缩（Kubernetes HP...
  c25_b02.md: ...Decode Node 2<br/>8× H200"]         end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 -->|"KV ca...
  c25_b02.md: ...end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|...
  c25_b02.md: ...er fill:#f0f9e8   ASCII 回退：                           ┌──── Request ───┐                         │                │...
  c25_b02.md: ...Decode Node 2<br/>8× H200"]         end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 -->|"KV ca...
  c25_b02.md: ...end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|...
  c25_b02.md: ...er fill:#f0f9e8   ASCII 回退：                           ┌──── Request ───┐                         │                │...
  c25_b05.md: ...设计。提供集群级 KV cache，任何 decode 节点可读。  收益：  (a) **超长上下文可行**：per-request KV cache 不受单 GPU 内存限制。  (b) **跨请求复用 KV cache**：共享前缀的 prompt...
  c25_b11.md: ...现这些模式。采用阈值：约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 per-request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用。Mooncak...
  c26_b02.md: ...现：  (a) 每个节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）。  (b) 对 incoming request 的 prompt 做哈希（或其前缀与索引比对）。  (c) 负载均衡器选择最长匹配前缀的节点；把请求路由过去。  (d...
  c26_b03.md: ...（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发布案例（Toge...
  c26_b04.md: ...器偏好。低 tier 客户即使在尖峰也有有界服务。  运维关注点：每请求调度决策必须非常快（亚毫秒），以免增加 per-request 延迟。Continuum 调度器在内存中，基于决策树，输入为客户 tier 和近期用量。   ### 26.7 Tob...
  c26_b07.md: ...客户有可预测延迟；低 tier 客户在峰值期间也有有界服务。  运维关注点：调度决策必须快（亚毫秒）以免增加 per-request 延迟。决策还必须考虑请求预期资源消耗（长输出请求比短请求消耗更多 “credit”）。这些因素使调度器实现非平凡。...
  c26_b08.md: ...客户。  综合：该客户每请求成本降低约 5 倍（前缀复用），延迟显著改善。Continuum 能给出有竞争力的 per-request 定价；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优...

=== calvin (21 occurrences) ===
  c03_b08.md: .../ TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequencer 确定性排序 | 到 sequencer 的延迟；不涉及时钟  正确选择...
  c03_b08.md: ...格最干净。如果没有，HLC 加已知偏移上界是实际默认。如果你能容忍 sequencer（一个排序事务的单点）的架构开销，Calvin 的方法完全消除时钟依赖。第13章会在事务深度再次回到这三种。   ### 3.8 回到聊天应用  本章开头的聊天消息函...
  c03_b15.md: ...是正确原语。Fencing token 在第18章；Raft 在第9章；Spanner / CockroachDB / Calvin / TrueTime 在事务上下文中的完整比较在第13章。  第4章迈出下一步：既然我们能在机器间排序事件，就需要把事...
  c12_b11.md: ...层：分布式事务。2PC 的阻塞失败模式、Google 的 Percolator（真正的 Spanner 风格事务模式）、Calvin 的确定性排序、TrueTime 的 commit-wait、CockroachDB 的 HLC-based MVCC...
  c13_b01.md: ...（带阻塞失败模式的经典协议）、Google Percolator（大多数生产系统实际采用的 Spanner 克隆模式）、Calvin（确定性排序作为乐观协议的替代）、TrueTime（Spanner 的硬件辅助时间戳）。本章最后介绍 2026 年的现实...
  c13_b03.md: ...优雅之处在于：它无需存储层本身了解事务，就能把分布式事务加到键值存储上。事务层完全在存储层之上。   ### 13.4 Calvin：确定性排序  Calvin 由 Thomson 等人在 2012 年论文中提出，采用不同路径：事先按确定性顺序对所有事...
  c13_b03.md: ...身了解事务，就能把分布式事务加到键值存储上。事务层完全在存储层之上。   ### 13.4 Calvin：确定性排序  Calvin 由 Thomson 等人在 2012 年论文中提出，采用不同路径：事先按确定性顺序对所有事务排序，然后让每个分片按约定...
  c13_b04.md: ...#### Calvin 为何快  Sequencer 是唯一的同步点；执行跨分片并行。没有中止重试（约定顺序就是协议的提交；没有单独的提交阶段...
  c13_b04.md: ...-shard 执行。  代价：sequencer 是全局序列化点。对极高吞吐工作负载，sequencer 可能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处...
  c13_b04.md: ...能成为瓶颈。Calvin 将 sequencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处  * **FaunaDB** ：基于 Calvin 的商业数据库；面向全球分布的可串行化事务。 * **研究...
  c13_b04.md: ...并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处  * **FaunaDB** ：基于 Calvin 的商业数据库；面向全球分布的可串行化事务。 * **研究系统** ：Calvin 的确定性执行思想影响了大量后续系统。...
  c13_b04.md: ...* **FaunaDB** ：基于 Calvin 的商业数据库；面向全球分布的可串行化事务。 * **研究系统** ：Calvin 的确定性执行思想影响了大量后续系统。  2026 年 Calvin 是少数派；大多数生产系统是 2PC + 复制协调者...
  c13_b04.md: ...向全球分布的可串行化事务。 * **研究系统** ：Calvin 的确定性执行思想影响了大量后续系统。  2026 年 Calvin 是少数派；大多数生产系统是 2PC + 复制协调者 + 快照隔离。但 Calvin 无中止重试，使其在乐观协议挣扎的高...
  c13_b04.md: ...响了大量后续系统。  2026 年 Calvin 是少数派；大多数生产系统是 2PC + 复制协调者 + 快照隔离。但 Calvin 无中止重试，使其在乐观协议挣扎的高争用工作负载中具有吸引力。   ### 13.5 Spanner 与 TrueTim...
  c13_b05.md: ...窗口读时重启 TiDB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Sequencer 往返  2026 年的现实：大多数非 Goo...
  c13_b13.md: ...**Q3.** 比较 Calvin 的确定性排序与 CockroachDB 的乐观并发控制。  提示  Calvin：先排序再执行；无因锁冲突导致的中止。...
  c13_b13.md: ...**Q3.** 比较 Calvin 的确定性排序与 CockroachDB 的乐观并发控制。  提示  Calvin：先排序再执行；无因锁冲突导致的中止。CockroachDB：先执行，冲突时重试。  **答案：**  _Calvin_...
  c13_b13.md: ...推进；低争用下吞吐很高。  缺点：争用导致重启；病态争用下产生 abort storm；高争用下延迟不可预测。  权衡：Calvin 赢在高争用工作负载（计数器递增、热行更新）；CockroachDB 赢在低争用工作负载（大多数 OLTP）。2026...
  c13_b13.md: ...hDB 赢在低争用工作负载（大多数 OLTP）。2026 年分布式数据库市场由乐观方法主导，因为真实工作负载大多低争用；Calvin 在专业应用（FaunaDB）中存活，那里确定性语义重要。  **Q4.** 什么是 TrueTime，Spanner...
  c13_b17.md: ...典，存在协调者故障时阻塞的弱点）、Percolator（带 primary-key 原子提交的快照隔离，无中心协调者）、Calvin（通过 sequencer 的确定性排序，无中止）、TrueTime/Spanner（硬件辅助时间戳 + commit-...
  c30_b02.md: ...ions." _OSDI_. — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Sys...

=== phase (21 occurrences) ===
  c09_b06.md: ...leader 与 term 紧。  概念映射：Raft term 对应 Multi-Paxos 中“leader 单次 Phase 1 的任期”。差异主要是记号和人体工学。  **Q3.** 为什么 Raft 随机化选举超时？  提示  减少多个 f...
  c10_b05.md: ...。 * 更小的 accept 法定人数：$Q_1$ 更大（很少跑），$Q_2$ 更小（每次提交）。稳态提交快。  含义：Phase 1 很少运行的系统（稳定领导）可以让 accept 法定人数很小以换取快速提交，代价是 leader 变更需要更大的...
  c10_b08.md: ...是 2f+1 个签名在特定阶段结果上的集合。QC 是“该值已获支持”的紧凑、可传递证明。  当 leader 为提议的 phase k 收集到 2f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下...
  c10_b08.md: ...2 \ne \emptyset$。只要两个法定人数相交，两个法定人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1...
  c10_b08.md: ...人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1$），$Q_2$ 很小（如 2）。任意大小为 $N - 1$ 的...
  c10_b08.md: ...是 2f+1 个签名在特定阶段结果上的集合。QC 是“该值已获支持”的紧凑、可传递证明。  当 leader 为提议的 phase k 收集到 2f+1 个投票后，它组成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下...
  c10_b08.md: ...2 \ne \emptyset$。只要两个法定人数相交，两个法定人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1...
  c10_b08.md: ...人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 leader 任期一次），但频繁运行 Phase 2（每次提交）。让 $Q_1$ 很大（如 $N - 1$），$Q_2$ 很小（如 2）。任意大小为 $N - 1$ 的...
  c19_b07.md: ...移计划：识别用例，把 ZK 原语映射到 etcd 原语，过渡期双写，同步后切换读。  **答案：** 迁移计划：  **Phase 1：清点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → e...
  c19_b07.md: ...）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时...
  c19_b07.md: ...TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phase 3：双写**。应用同时写入 ZooKeeper 和 etcd。读仍来自 ZooKeeper。监控两者一致性（差异表示双...
  c19_b07.md: ...ZooKeeper 和 etcd。读仍来自 ZooKeeper。监控两者一致性（差异表示双写路径有 bug）。  **Phase 4：迁移读**。一次一个用例，将读路径切换到 etcd。先服务发现（风险低）；然后分布式锁（风险较高）；然后配置（风险...
  c19_b07.md: ...，将读路径切换到 etcd。先服务发现（风险低）；然后分布式锁（风险较高）；然后配置（风险最高，运营影响更大）。  **Phase 5：停止写 ZooKeeper**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper...
  c19_b07.md: ...写 ZooKeeper**。所有读都来自 etcd 且双写已验证足够长（数周）后，停止写入 ZooKeeper。  **Phase 6：下线 ZooKeeper**。经过足够时间确认无残留读之后，下线 ZK 集群。  关键风险与缓解：  * **语义...
  c25_b01.md: ...ntion，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory...
  c25_b08.md: ...分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill...
  c25_b08.md: ...he 传输、故障处理。 * 用 mirror 生产流量模式的合成工作负载测试。 * 验证：延迟、吞吐、故障模式。  **Phase 2：逐步转换（3-4 个月）**  * 更多节点从 co-located 转成分离角色。 * 运行 50/50 分割（...
  c25_b08.md: ...节点 co-located）两周。 * 测量：利用率、每请求成本、p99 延迟。 * 若指标确认收益，继续转换。  **Phase 3：全部分离（5-6 个月）**  * 转换剩余节点。 * 最终拓扑：30 prefill + 30 decode +...
  c25_b08.md: ...10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-located 路径。  **Phase 4：调优与优化（7+ 个月）**  * 根据观测流量调整 prefill/decode 比例。 * 实现 KV cac...
  c25_b09.md: ...tinuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两阶段都保持 15% headroom；基于 per-phase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disa...
  c30_b03.md: ...2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024). "Di...

=== pbft (21 occurrences) ===
  c10_b01.md: ...BG["Byzantine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov 1999<br/>O(N²) messages"]         P...
  c10_b01.md: ...BG["Byzantine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov 1999<br/>O(N²) messages"]         PBFT --...
  c10_b01.md: ...T["PBFT<br/>Castro-Liskov 1999<br/>O(N²) messages"]         PBFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler wor...
  c10_b01.md: ...Tendermint<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et al. 2019<br/>O(N) view ch...
  c10_b02.md: ...### 10.2 PBFT：经典协议  实用拜占庭容错（Practical Byzantine Fault Tolerance，PBFT）由 Ca...
  c10_b02.md: ...10.2 PBFT：经典协议  实用拜占庭容错（Practical Byzantine Fault Tolerance，PBFT）由 Castro 和 Liskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分...
  c10_b02.md: ...rance，PBFT）由 Castro 和 Liskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) *...
  c10_b02.md: ...同值（法定人数相交）。Commit 轮确保若任何正确副本 commit，所有正确副本最终都 commit（一致性）。  PBFT 的复杂度：每阶段是 all-to-all 广播，每个决定 $O(N^2)$ 消息。$N=4$（容忍 1 个拜占庭）时每...
  c10_b02.md: ...1 个拜占庭）时每决定 16 条消息，可管理。$N=100$ 时 10,000 条消息， prohibitive。  PBFT 二十年间是经典 BFT 协议，但因二次消息复杂度很少大规模部署。2018-2020 年区块链研究浪潮产生了解决该复杂度...
  c10_b03.md: ...BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuff 的三阶段结构与 PBFT 类似，但有关键差异：  *...
  c10_b03.md: ...ader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。  HotStuff 的三阶段结构与 PBFT 类似，但有关键差异：  * 每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1...
  c10_b03.md: ...+1$ 收集第一阶段投票时，可以同时为块 $k$ 收集第二阶段投票、为块 $k-1$ 收集第三阶段投票。这使吞吐远高于 PBFT 的串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuf...
  c10_b04.md: ...m**（已停运）直接使用 HotStuff。  Narwhal-Bullshark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚秒级提交）。复杂度在于 DAG mempool 的正确性及其与共识层的集成。   ##...
  c10_b07.md: ...BFT 需要额外副本的原因：给协议足够冗余，使任何法定人数的投票由诚实 peer 主导。  **Q3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certif...
  c10_b07.md: ...线性 view change_ ：当 leader 被怀疑且新 leader 接管时，消息复杂度是 $O(N)$ 而非 PBFT 的 $O(N^2)$。...
  c10_b08.md: ...w change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条...
  c10_b09.md: ...公司为银行间结算构建许可链。21 家银行各运行一个验证者节点。威胁模型：任何一家银行可能被攻破，但最多 6 家同时。在 PBFT、HotStuff、Tendermint 之间选择并论证。  提示  $N = 21, f = 6$。检查 3f+1 要...
  c10_b09.md: ...：** 阈值检查：$3f + 1 = 19 \le N = 21$。三个协议都能容忍 6 个拜占庭故障。  (a) **PBFT** ：被充分理解、成熟。消息复杂度 $O(N^2) = 441$ 条消息每决定，view change 也是 $O(N...
  c10_b09.md: ...。  HotStuff 技术上“最佳”，但实现负担更重；对 21 节点许可链，Tendermint 的简单性占主导。  PBFT 在 2026 年是错误选择：吞吐限制和过时工具使其在新部署中劣于两者。  **Q2.** 一家云厂商为其控制面设计元数...
  c10_b12.md: ...行为，包括恶意行为、重复签名和损坏状态。代价是 3f+1 副本（崩溃容错只需 2f+1），以及协议消息上的加密签名。  PBFT 是经典实用 BFT 协议；其 $O(N^2)$ 消息复杂度限制了小规模副本集。HotStuff（2019）通过 Quo...
  c30_b02.md: ...B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abra...

=== compaction (21 occurrences) ===
  c12_b03.md: ...le）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收空间（删除键被丢弃）、减少读时要检查的...
  c12_b03.md: ...SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效率换取周期性 compaction 开销。  权衡：  * **...
  c12_b03.md: ...追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效率换取周期性 compaction 开销。  权衡：  * **写吞吐** ：LSM 胜（通常持续写快 10-100 倍）。 * **读延迟** ：B-t...
  c12_b03.md: ...、MongoDB WiredTiger 可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够...
  c12_b03.md: ...可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成...
  c12_b03.md: ...#### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **...
  c12_b03.md: ...多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快...
  c12_b03.md: ...le）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收空间（删除键被丢弃）、减少读时要检查的...
  c12_b03.md: ...SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效率换取周期性 compaction 开销。  权衡：  * **...
  c12_b03.md: ...追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效率换取周期性 compaction 开销。  权衡：  * **写吞吐** ：LSM 胜（通常持续写快 10-100 倍）。 * **读延迟** ：B-t...
  c12_b03.md: ...、MongoDB WiredTiger 可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够...
  c12_b03.md: ...可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成...
  c12_b03.md: ...#### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **...
  c12_b03.md: ...多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快...
  c12_b06.md: ...ble（内存有序结构）。这很快。   3. memtable 满时，刷盘成不可变 SSTable。   4. 定期通过 compaction 合并多个 SSTable。  用户可见写在第 1、2 步后完成；后续 compaction 异步。  B+ tree...
  c12_b06.md: ...le。   4. 定期通过 compaction 合并多个 SSTable。  用户可见写在第 1、2 步后完成；后续 compaction 异步。  B+ tree 写路径：    1. 写必须更新相关叶页（磁盘上 4-16 KB）。   2. 若页未缓存，...
  c12_b07.md: ...I/O：很多写先在 memtable 累积，再一起刷盘。B+ tree 每次页更新都要触碰磁盘。  (c) LSM 把 compaction 成本摊销：合并异步发生，不在用户写路径上。  对持续高写吞吐，LSM 结构上优于 B+ tree。点查方面，B+ tr...
  c12_b09.md: ...p99 可能 1-5 ms。 * 空间放大：典型 1.5-2 倍（compaction 前多个 SSTable）。 * Compaction 开销：周期后台 CPU 和 I/O 突增。  SQLite 画像：  * 写吞吐：中等。每次写需更新 B+ tree...
  c12_b11.md: ...是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ tree；B+ tree 在读重单节点 OLTP 中仍然占优。  Anya 的重分...
  c16_b04.md: ...理覆盖很多情况；应用以向后兼容层处理其余。  (c) **存储大小** ：重历史文档（编辑数月的文档）会累积状态。定期 compaction 必不可少；否则存储无界增长。  (d) **大量并发用户性能** ：单个文档超过约 100 个并发编辑者时，广播流量成...
  c16_b11.md: ...cts 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Software——Kleppmann 等人在 201...

=== orchestrator (21 occurrences) ===
  c13_b06.md: ...骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复...
  c13_b06.md: ...saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复制或存在事务型 DB）。  (b) **Choreographed saga** ：...
  c13_b06.md: ...horeographed saga** ：每一步完成时发布事件；后续步骤订阅事件。补偿通过 “回滚事件” 触发。无中央 orchestrator；步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入...
  c13_b06.md: ...步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻辑，但端到端调试更困难。...
  c13_b06.md: ...骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复...
  c13_b06.md: ...saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复制或存在事务型 DB）。  (b) **Choreographed saga** ：...
  c13_b06.md: ...horeographed saga** ：每一步完成时发布事件；后续步骤订阅事件。补偿通过 “回滚事件” 触发。无中央 orchestrator；步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入...
  c13_b06.md: ...步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻辑，但端到端调试更困难。...
  c13_b07.md: ...e at step N: invoke compensations for N-1, N-2, ..., 1   单一 orchestrator 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“sag...
  c13_b07.md: ...er         participant C as Client         participant O as Orchestrator<br/>(state in CRDB)         participant R as Reservation...
  c13_b07.md: ...e at step N: invoke compensations for N-1, N-2, ..., 1   单一 orchestrator 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“sag...
  c13_b07.md: ...er         participant C as Client         participant O as Orchestrator<br/>(state in CRDB)         participant R as Reservation...
  c13_b08.md: ...ft 复制）。 * **跨服务** ：涉及银行结算 API、风控 LLM、通知服务、审计日志归档的支付是一个 saga。Orchestrator（一个由 CockroachDB 保存状态的小服务）依次调用每一步，并为每一步定义补偿。  两种模式共存；各自适用于自己...
  c27_b07.md: ...boarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证——步骤 2-5 的 saga 补偿已定义；orchestrator 状态经 Raft 复制；步骤 4（外部 partner API）有 0.1 重试预算和 50% 失败率熔断器。” 下一...
  c27_b07.md: ...有 0.1 重试预算和 50% 失败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢复；(b) 让 partner API 返回 50% 错误，验证熔断器打开；(...
  c27_b07.md: ...败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢复；(b) 让 partner API 返回 50% 错误，验证熔断器打开；(c) 在第 4 步重试期间注入网络...
  c27_b13.md: ...补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续。验证：orchestra...
  c27_b13.md: ...数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续。验证：orchestrator 能从任何步骤恢复；幂等重试安全。  (...
  c27_b13.md: ...hestrator 是否容错？** 若 orchestrator 在 saga 中途崩溃，恢复必须从保存状态继续。验证：orchestrator 能从任何步骤恢复；幂等重试安全。  (e) **补偿本身是否幂等？** 补偿也可能失败（罕见但可能）；应可安全重试多次...
  c30_b09.md: ...小时降级，2018-10-21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分区导致 MySQL Orchestrator failover 自动化在东海岸设施选出新主库，而西海岸原主库仍活着。 * **放大**：分区愈合后，系统有两个主库且...
  c30_b14.md: ...WAL + SSTable + compaction)     ├── ch13_saga/     │   ├── orchestrator.py   — orchestrated saga with durable state (SQLite)     │...

=== local (20 occurrences) ===
  c03_b03.md: ...## 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1...
  c03_b03.md: ...ss | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 |...
  c03_b03.md: ...a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_...
  c03_b03.md: ...## 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1...
  c03_b03.md: ...ss | Event | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 |...
  c03_b03.md: ...a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranag...
  c16_b05.md: ...是 (c)、(d) 及部分 (b)、(e) 的技术使能者。没有 CRDT，“离线且自动解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（...
  c16_b05.md: ...库）、Skiff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...e）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranag...
  c16_b05.md: ...是 (c)、(d) 及部分 (b)、(e) 的技术使能者。没有 CRDT，“离线且自动解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（...
  c16_b05.md: ...库）、Skiff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...e）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b10.md: ...用 OT。  第二个考虑：CRDT 对离线协作处理干净（离线更改可在重连后合并）；OT 对离线支持需要更仔细设计。随着 local-first 软件更普遍，CRDT 相比 OT 在离线场景更具吸引力。  **Q2.** 一个 LLM 被要求 “实现一...
  c16_b11.md: ...ket 实时同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Software——Kleppmann 等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计...
  c30_b02.md: ...Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., K...
  c30_b03.md: ...Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud."...

=== failure (20 occurrences) ===
  c05_b01.md: ...ite latency** ：下界是最慢副本的往返时间。 * **Availability under replica failure** ：若任何副本不可达，写入阻塞，直到副本恢复或被移出集合。  慢副本阻塞写入这一性质是致命的。磁盘降级、嘈杂邻居或网...
  c05_b02.md: ...播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Availability under replica failure** ：写不受副本健康影响。  丢失写窗口是异步复制的风险。对大多数工作负载（分析、内容元数据、浏览量），这个窗口可接受...
  c05_b02.md: ...tency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under replica failure** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑第一个 ack。这比同步快、比异步持久。...
  c11_b03.md: ...otocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 l...
  c11_b03.md: ...每 range Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistenc...
  c11_b04.md: ...swer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Failure-recovery | 透明（存储层对用户不透明）；自动扩缩容与自动修复 Consistency model | 默认最...
  c11_b04.md: ...-- Replication protocol | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistenc...
  c11_b04.md: ...n protocol | 自定义（sequencer + transaction-log + storage）；强一致 Failure-recovery | 快（亚秒级 sequencer 切换）；自设计日起就以混沌工程为目标 Consistency m...
  c11_b05.md: ...lication protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency...
  c11_b08.md: ...protocol | 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consi...
  c11_b09.md: ...Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LW...
  c13_b07.md: ...tep 6: call AnalyticsService.update(merchant, day)       on failure at step N: invoke compensations for N-1, N-2, ..., 1   单一 o...
  c13_b07.md: ...>O: updated         O-->>C: success         Note over O: On failure at step N:<br/>invoke compensations<br/>for N-1, N-2, ...,...
  c14_b07.md: ...-|---- Replication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consi...
  c21_b02.md: ...eDiagram-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open...
  c21_b02.md: ...ail         Closed: All calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream c...
  c30_b01.md: ...— 广播层级参考。 * Chandra, T. D., & Toueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of th...
  c30_b06.md: ...（因果一致性）**：每次操作的一致性模型，因果相关操作按因果顺序可见；并发操作可任意排序。 * **Cascading failure（级联故障）**：通过放大（如重试风暴）跨组件传播的故障。 * **Choreographed saga（编排式 sag...
  c30_b06.md: ...cher-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不能保证终止。 * **Gray failure（灰色故障）**：进程通过健康检查但实际从应用视角已损坏的故障。 * **HLC（混合逻辑时钟）**：二元组 (l, c...
  c30_b06.md: ...树）**：写优化的存储底层，Bigtable、Cassandra、RocksDB 使用。 * **Metastable failure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健康状态。 * **Orchestrated saga（...

=== view (20 occurrences) ===
  c07_b05.md: ...orum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必...
  c07_b05.md: ...器。  这种分解对阅读新共识论文很有用。几乎每种“新共识协议”只改变三者之一：新法定人数结构（HotStuff 的线性 view change）、新 validity 检查（拜占庭验证者）、新终止机制（Narwhal-Bullshark 等基于 D...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）** ：给定 N 个验...
  c10_b03.md: ...N 个验证者的 BFT 集群（$N \ge 3f + 1$）和提议流，产出最多容忍 f 个拜占庭验证者的提交决定，且 view-change 成本为 $O(N)$。机制是 HotStuff 的三阶段投票（prepare / pre-commit...
  c10_b03.md: ...ter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。...
  c10_b03.md: ...te（QC）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息...
  c10_b03.md: ...### 10.3 HotStuff：线性 View Change——工作流 5：HotStuff 三阶段走查  > **工作流 5（输入 → 输出）** ：给定 N 个验...
  c10_b03.md: ...N 个验证者的 BFT 集群（$N \ge 3f + 1$）和提议流，产出最多容忍 f 个拜占庭验证者的提交决定，且 view-change 成本为 $O(N)$。机制是 HotStuff 的三阶段投票（prepare / pre-commit...
  c10_b03.md: ...ter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次 leader 变更的消息复杂度从 PBFT 的 $O(N^2)$ 降到 $O(N)$。...
  c10_b03.md: ...te（QC）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息...
  c10_b07.md: ...投票由诚实 peer 主导。  **Q3.** 相对于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certificates，无需重播所有投票。  **答案：** HotStu...
  c10_b07.md: ...Quorum Certificates，无需重播所有投票。  **答案：** HotStuff 的关键创新是 _线性 view change_ ：当 leader 被怀疑且新 leader 接管时，消息复杂度是 $O(N)$ 而非 PBFT 的...
  c10_b08.md: ...成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，P...
  c10_b08.md: ...e 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义...
  c10_b08.md: ...产生 $O(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让 view-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi H...
  c10_b08.md: ...成 QC 并广播（或在 pipeline HotStuff 中，把 QC 包含在下一阶段 pre-prepare 里）。View change 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，P...
  c10_b08.md: ...e 时，新 leader 只需每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态。  相比之下，PBFT 的 view change 要求每个副本把其见过的全部消息（完整状态）发给新 leader，产生 $O(N^2)$ 条消息。  含义...
  c10_b08.md: ...产生 $O(N^2)$ 条消息。  含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让 view-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi H...
  c10_b09.md: ...个拜占庭故障。  (a) **PBFT** ：被充分理解、成熟。消息复杂度 $O(N^2) = 441$ 条消息每决定，view change 也是 $O(N^2)$。21 节点下可工作，但会限制协议成熟后的吞吐。运维工具较旧（多数 PBFT 实现...
  c11_b08.md: ...ped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事...

=== vnode (20 occurrences) ===
  c12_b02.md: ...负载。修复：每个物理节点拥有多个“虚拟节点”（vnode）散布在环上。Cassandra 默认每个物理节点 256 个 vnode；这使负载分布更均匀。  Vnodes 也有助于再平衡：新节点加入时从多个现有节点取 vnode，把重平衡成本分散到整个...
  c12_b02.md: ...个物理节点 256 个 vnode；这使负载分布更均匀。  Vnodes 也有助于再平衡：新节点加入时从多个现有节点取 vnode，把重平衡成本分散到整个集群，而非集中在一个邻居。   ### 12.3 实践中的范围分片  CockroachDB 的...
  c12_b06.md: ...。每个现代分布式键值系统都使用某种形式的一致哈希或其衍生。  虚拟节点（vnodes）进一步完善：每个物理节点拥有多个 vnode 散布在环上。这带来更均匀负载分布，并把重平衡成本分散到多个邻居。  **Q3.** 描述 LSM-tree 的写路径。...
  c12_b07.md: ...le（Bloom filter 有帮助）。  **Q4.** 为什么 Cassandra 默认每个物理节点 256 个 vnode？它解决了什么问题？  提示  负载分布。没有 vnode 时，单个物理节点可能落在环上稀疏区域，从而承担不成比例负载。...
  c12_b07.md: ...* 为什么 Cassandra 默认每个物理节点 256 个 vnode？它解决了什么问题？  提示  负载分布。没有 vnode 时，单个物理节点可能落在环上稀疏区域，从而承担不成比例负载。  **答案：** 朴素一致哈希（每个物理节点一个位置）中...
  c12_b07.md: ...值的 2-3 倍。即最忙节点 QPS 是平均节点的 2-3 倍，对应磁盘和 CPU 压力。  每个物理节点 256 个 vnode 时，每个物理节点在环上有 256 个随机位置。一个物理节点拥有的总弧是 256 个随机弧之和；依大数定律，它接近期望值...
  c12_b07.md: ...和；依大数定律，它接近期望值（环的 1/N）。负载分布均匀得多。  第二个好处：新物理节点加入时，从多个现有物理节点取 vnode（因为新节点的 256 个 vnode 分散）。数据移动分散到整个集群，不集中在少数邻居。这使再平衡更快、更平缓。  2...
  c12_b07.md: .../N）。负载分布均匀得多。  第二个好处：新物理节点加入时，从多个现有物理节点取 vnode（因为新节点的 256 个 vnode 分散）。数据移动分散到整个集群，不集中在少数邻居。这使再平衡更快、更平缓。  256 是参数；合适值取决于集群规模。大...
  c12_b07.md: ...动分散到整个集群，不集中在少数邻居。这使再平衡更快、更平缓。  256 是参数；合适值取决于集群规模。大集群可每节点少 vnode（100 节点集群用 1024 vnode 每节点即每节点 10 个 vnode，通常足够）。小集群需要更多 vnode...
  c12_b07.md: ...平衡更快、更平缓。  256 是参数；合适值取决于集群规模。大集群可每节点少 vnode（100 节点集群用 1024 vnode 每节点即每节点 10 个 vnode，通常足够）。小集群需要更多 vnode 每节点以获得好分布。Cassandra...
  c12_b07.md: ...合适值取决于集群规模。大集群可每节点少 vnode（100 节点集群用 1024 vnode 每节点即每节点 10 个 vnode，通常足够）。小集群需要更多 vnode 每节点以获得好分布。Cassandra 默认 256 是因为它在典型集群规模下...
  c12_b07.md: ...vnode（100 节点集群用 1024 vnode 每节点即每节点 10 个 vnode，通常足够）。小集群需要更多 vnode 每节点以获得好分布。Cassandra 默认 256 是因为它在典型集群规模下平衡了均匀性与令牌管理开销。...
  c12_b10.md: ...一致哈希”，产出 50 行代码。指出 LLM 生成代码与生产实现相比的三个结构性问题。  **讨论：**  (a) **Vnode 处理。** 生产一致哈希使用虚拟节点（每物理节点 256+）做负载分布。LLM 生成代码通常每个节点一个位置，导致负载...
  c12_b10.md: ...哈希（MurmurHash3、FNV）做负载分布。LLM 代码可能用 `hash()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。...
  c12_b10.md: ...LM 代码可能用 `hash()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系...
  c12_b10.md: ...**Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、Z...
  c12_b10.md: ...一致哈希”，产出 50 行代码。指出 LLM 生成代码与生产实现相比的三个结构性问题。  **讨论：**  (a) **Vnode 处理。** 生产一致哈希使用虚拟节点（每物理节点 256+）做负载分布。LLM 生成代码通常每个节点一个位置，导致负载...
  c12_b10.md: ...哈希（MurmurHash3、FNV）做负载分布。LLM 代码可能用 `hash()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。...
  c12_b10.md: ...LM 代码可能用 `hash()`（未设计为均匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系...
  c12_b10.md: ...**Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、Z...

=== glide (19 occurrences) ===
  c01_b03.md: ...不同领域反复执行这个循环。Olamide 运行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理...
  c15_b11.md: ...tion 和 Linear 中的 Yjs、Ink & Switch 项目中的 Automerge）。Yuki 的实例是 Glide 协作文档编辑器实现。  ---...
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人...
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人可同时编辑同一份文档，使用不同设备，有时...
  c16_b01.md: ...tanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人可同时编辑同一份文档，使用不同设备，有时离线，文档会收敛到一致状态，而无需冲突解决弹窗。  使这成为可...
  c16_b01.md: ...是 Yjs（团队主要选择）和 Automerge（用于部分子产品）。本章是 Yuki 的工作流 9：用 CRDT 实现 Glide 协作文档编辑器。  CRDT 是过去十年分布式状态系统中最具运维重要性的进展。CRDT 出现前，“协作编辑” 需要复杂...
  c16_b03.md: ...功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且...
  c16_b03.md: ...行，包括慢网络），且其文档模型比 Automerge 完整历史图更简单。   ### 16.4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：...
  c16_b04.md: ...符上（未被删除的字符）；被删除的字符消失。  结果在运维上令人惊叹：用户独立编辑，有时离线数小时，合并却正确且无需弹窗。Glide 生产两年没有出现 “合并冲突对话框”。   #### 失败模式  系统不是魔法。失败模式：  (a) **并发相同动作...
  c16_b04.md: ...少；否则存储无界增长。  (d) **大量并发用户性能** ：单个文档超过约 100 个并发编辑者时，广播流量成为瓶颈。Glide 的方案是分片文档（1000 用户的文档被分成 per-section 子文档）。...
  c16_b05.md: ...Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT）、Replicache（CRDT-like 库）、Skiff...
  c16_b07.md: ...。对冲突有意义的数据，用 MV 或领域特定 CRDT。对需要拒绝并发写的字段，线性化。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到...
  c16_b08.md: ...#### 应用问题  **Q1.** Yuki 的 Glide 团队想添加 “实时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状态；是 awar...
  c16_b11.md: ...合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.D...
  c16_b11.md: ...9 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear、Notion、Glide、Logseq、Skiff 是示例。  2026 年新协作编辑器设计默认用 CRDT。现有基于 OT 的系统（Googl...
  c16_b11.md: ...COPS/Eiger 家族）、强最终（CRDT 作为原型），以及把它们放在线性化和最终一致之间的一致性格。Yuki 的 Glide 系统同时使用了这三者的元素。  ---...
  c17_b03.md: ...escent（第14章）：CockroachDB 做运营存储（线性化）；独立计数器服务（CRDT）做页面浏览指标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（pe...
  c18_b07.md: ...token。效率用例可用缓存锁。  Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合...
  c28_b01.md: ...nya 是数据库主管（Stratus DB，现在内嵌为 Crescent 的 commit-log 层）。Yuki 从 Glide 借调来负责协作文档编辑组件（商户入驻流程）。Tobias 从 Continuum Inference 借调来负责 AI...

=== mysql (19 occurrences) ===
  c05_b02.md: ...多数工作负载（分析、内容元数据、浏览量），这个窗口可接受。对丢失写会灾难性的工作负载（支付、账户余额），纯异步错误。  MySQL 默认复制是异步。PostgreSQL 默认 `synchronous_commit = on` 是本地磁盘 flush...
  c05_b02.md: ...** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑第一个 ack。这比同步快、比异步持久。  MySQL 的 `rpl_semi_sync_master_wait_for_slave_count = 1` 是经典实现。Pos...
  c05_b02.md: ...ries）。读可以走 leader（保一致）或 follower（扩规模，但可能过期）。  用于：PostgreSQL、MySQL、MongoDB（replica sets）、CockroachDB（通过 Raft 的 per-range leade...
  c05_b05.md: ...ostgreSQL `synchronous_commit = remote_apply` 用于单数据中心 RPO=0；MySQL 半同步用于较低成本下的中等持久性；CockroachDB Raft-quorum 用于自动 RPO=0 且多区域容错。...
  c05_b05.md: ...Async（一个或多个副本） | `synchronous_commit = remote_apply` 实现按写同步 MySQL | Async | `rpl_semi_sync` 插件实现半同步 MongoDB（replica set）| Asy...
  c05_b05.md: ...“本会话已写的都有已知 HLC；本会话读尊重它”。代价是延迟，用来避免客户存款失败。  较老系统（PostgreSQL、MySQL、MongoDB）把同步作为可选项，默认异步。应用代码负责路由或等待。   ### 5.11 练习...
  c11_b01.md: ...或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复制或通过可配置法定人数。 * **Per-shard...
  c12_b05.md: ...。B+ tree 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B...
  c14_b01.md: ...D 事务的分布式数据库。原始定义将 NewSQL 与以下两类对比：  * **传统 SQL** ：PostgreSQL、MySQL、Oracle。ACID，单节点或主从复制，纵向扩展。 * **NoSQL** ：Cassandra、DynamoDB、...
  c14_b01.md: ...ockroachDB** ：开源 NewSQL；可跑在任何云上。 * **TiDB** ：另一开源 NewSQL；兼容 MySQL。 * **YugabyteDB** ：开源 NewSQL；兼容 Postgres。 * **AWS Aurora DS...
  c14_b02.md: ...omcast 部分工作负载、若干大型银行）。   ### 14.4 TiDB  PingCAP 的 NewSQL；兼容 MySQL 线缆。  关键属性：  * TiKV 存储层（每 region/tablet 使用 Raft；Percolator-b...
  c14_b02.md: ...olator-based 事务）。 * TiDB 计算层（无状态 SQL 前端；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大...
  c14_b02.md: ...。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和 OLAP 无需 ETL。 *...
  c14_b02.md: ...iDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MySQL 工作负载。生产示例：许多中国科技公司、部分美国公司（Square 历史上）。   ### 14.5 YugabyteD...
  c30_b09.md: ...#### E.5 — GitHub MySQL 24 小时降级，2018-10-21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分区导致 My...
  c30_b09.md: ...QL 24 小时降级，2018-10-21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分区导致 MySQL Orchestrator failover 自动化在东海岸设施选出新主库，而西海岸原主库仍活着。 * **放大**：分...
  c30_b10.md: ...度和生态系统胜过横向扩展的边际收益。直到真正需要多 region 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aurora PostgreSQL 用于托管简单，但每 IOPS 计费更...
  c30_b10.md: ...。直到真正需要多 region 再迁移 NewSQL。 * **替代**：若团队偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aurora PostgreSQL 用于托管简单，但每 IOPS 计费更高。   #### F.2 — 面...
  c30_b10.md: ...inity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**：若需 MySQL 兼容选 TiDB；若需更接近 Postgres 兼容选 YugabyteDB；AWS 原生 serverless 选...

=== rpo (19 occurrences) ===
  c05_b06.md: ...r 做灾难恢复。主库在写传播前死亡。走查团队会看到什么以及恢复如何进行。  提示  写丢失。选项取决于如何检测丢失以及 RPO 是什么。  **答案：** 团队会观察到：  (a) 发起写的应用收到成功响应，因为异步复制在传播前 ack。(b)...
  c05_b06.md: ...客户）。 * 团队的 _Recovery Point Objective_（RPO）是允许的最大数据丢失时间。异步复制 RPO = 复制延迟，可能是数秒到数分钟。同步复制 RPO = 0。  纪律：按 RPO 需求选择复制模式。支付数据 RPO...
  c05_b06.md: ...Objective_（RPO）是允许的最大数据丢失时间。异步复制 RPO = 复制延迟，可能是数秒到数分钟。同步复制 RPO = 0。  纪律：按 RPO 需求选择复制模式。支付数据 RPO = 0（用同步或半同步）。分析数据 RPO = 数分...
  c05_b06.md: ...）是允许的最大数据丢失时间。异步复制 RPO = 复制延迟，可能是数秒到数分钟。同步复制 RPO = 0。  纪律：按 RPO 需求选择复制模式。支付数据 RPO = 0（用同步或半同步）。分析数据 RPO = 数分钟可接受（异步加更便宜硬件）。...
  c05_b06.md: ...RPO = 复制延迟，可能是数秒到数分钟。同步复制 RPO = 0。  纪律：按 RPO 需求选择复制模式。支付数据 RPO = 0（用同步或半同步）。分析数据 RPO = 数分钟可接受（异步加更便宜硬件）。  **Q2.** 为法定人数系统定...
  c05_b06.md: ...同步复制 RPO = 0。  纪律：按 RPO 需求选择复制模式。支付数据 RPO = 0（用同步或半同步）。分析数据 RPO = 数分钟可接受（异步加更便宜硬件）。  **Q2.** 为法定人数系统定义 $W + R > N$ 规则。为什么它给...
  c05_b08.md: ...步）还是 CockroachDB。列出他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO...
  c05_b08.md: ...RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？** 异步 Postgres 复制 RPO = 复制延迟（正常负载通常亚秒，压力大时数分钟）。Cockroac...
  c05_b08.md: ...需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？** 异步 Postgres 复制 RPO = 复制延迟（正常负载通常亚秒，压力大时数分钟）。CockroachDB 的 Raft 复制对已提交写 RPO = 0...
  c05_b08.md: ...复制 RPO = 复制延迟（正常负载通常亚秒，压力大时数分钟）。CockroachDB 的 Raft 复制对已提交写 RPO = 0。支付数据通常 RPO = 0 是硬性要求。  (b) **RTO 是多少？** Postgres 故障切换（用...
  c05_b08.md: ...正常负载通常亚秒，压力大时数分钟）。CockroachDB 的 Raft 复制对已提交写 RPO = 0。支付数据通常 RPO = 0 是硬性要求。  (b) **RTO 是多少？** Postgres 故障切换（用 Patroni 等）通常 3...
  c05_b10.md: ...一样用 Raft-quorum。异步复制是遗留反模式。”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，按构造避免 split-brain。延迟成本真实但有界；对大多数工作负载可接受。  反驳：异步复...
  c05_b10.md: ...n。延迟成本真实但有界；对大多数工作负载可接受。  反驳：异步复制仍适合许多工作负载。偶尔丢失可接受的日志流水线不需要 RPO = 0。写少读多的工作负载可用异步复制到读副本，不付 quorum 成本。Aurora 的设计（写走 6 副本 4-o...
  c28_b10.md: ...者同时崩溃） | | 3 | 同步 vs 异步复制追踪 | Ch.5 §5.10 | 写主库；存在副本 | 每次写入的 RPO + 每次写入的延迟（可配置权衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即...
  c28_b14.md: ...etcd revision 在编排层起类似作用。 * **故障**：异步复制可能丢失主库传播前最近写入。团队必须接受某 RPO；可通过 `synchronous_commit = remote_apply` 收紧。  优势：成熟、文档丰富、SQL...
  c28_b14.md: ...remote_apply` 收紧。  优势：成熟、文档丰富、SQL 强大。劣势：failover 不可用窗口；异步复制 RPO。  对 Crescent：Postgres + Patroni 不满足多 region 需求；CockroachDB...
  c30_b10.md: ...er 用于热读路径（TTL 60s；写入时事件驱动失效）。 * **复制**：同步到一个 standby，异步到另一个。RPO = 0 对同步副本；RPO 为秒级对异步副本。 * **为何不选 NewSQL**：在中等单区域规模，PostgreS...
  c30_b10.md: ...60s；写入时事件驱动失效）。 * **复制**：同步到一个 standby，异步到另一个。RPO = 0 对同步副本；RPO 为秒级对异步副本。 * **为何不选 NewSQL**：在中等单区域规模，PostgreSQL 的运维成熟度和生态系统...
  c30_b13.md: ...诊断方法：    1. **复制协议** 满足吞吐 / 可用性需求。   2. **故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分区行为**...

=== tendermint (19 occurrences) ===
  c07_b05.md: ...+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数...
  c07_b05.md: ...络纪律更好，超时更紧。 * **TiKV** 用 Raft。Multi-Raft 架构：多 Raft 组并行。 * **Tendermint**（Cosmos 链）用显式轮次超时；生产 1 秒一轮。 * **Aptos / Sui** 用 Narwhal-Bu...
  c10_b01.md: ...ff（2026 年所有 BFT 系统的基础）、Narwhal-Bullshark（DAG mempool 与共识分离）、Tendermint（许多区块链生态的工作马）、Flexible Paxos（Heidi Howard 的重配置洞见），以及非区块链 BFT...
  c10_b01.md: ...r/>Castro-Liskov 1999<br/>O(N²) messages"]         PBFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler workhorse"]...
  c10_b01.md: ...skov 1999<br/>O(N²) messages"]         PBFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> H...
  c10_b04.md: ...迟声明也有竞争力（亚秒级提交）。复杂度在于 DAG mempool 的正确性及其与共识层的集成。   ### 10.5 Tendermint：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、...
  c10_b04.md: ...复杂度在于 DAG mempool 的正确性及其与共识层的集成。   ### 10.5 Tendermint：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 C...
  c10_b04.md: ...年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint 用部分 HotStuff 特性换取简单性：  * 两阶段投票（prevote + precommit），每阶段都需要...
  c10_b04.md: ...轮以确定性轮转选新 leader。 * 锁定：一旦验证者 precommit 某值，它在后续轮次中被“锁定”到该值。  Tendermint 比 HotStuff 更易实现和调试。吞吐较低（因为每轮串行、无 pipelining），但对许多区块链用例足够。Co...
  c10_b04.md: ...、无 pipelining），但对许多区块链用例足够。Cosmos 生态（Osmosis、Juno、Akash）都使用 Tendermint。  Tendermint 的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几...
  c10_b04.md: ...），但对许多区块链用例足够。Cosmos 生态（Osmosis、Juno、Akash）都使用 Tendermint。  Tendermint 的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint...
  c10_b04.md: ...Tendermint 的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint fork 因此产生安全 bug。协议正确性已被证明；新实现应使用经过验证的库而不是重新实现。...
  c10_b09.md: ...21 家银行各运行一个验证者节点。威胁模型：任何一家银行可能被攻破，但最多 6 家同时。在 PBFT、HotStuff、Tendermint 之间选择并论证。  提示  $N = 21, f = 6$。检查 3f+1 要求。然后比较消息复杂度、吞吐和生态成熟度...
  c10_b09.md: ...HotStuff）存在，但多绑定特定区块链生态。为许可链实现 HotStuff 比直接拿库需要更多工程。  (c) **Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint...
  c10_b09.md: ...*Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于 HotStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuf...
  c10_b09.md: ...但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的数千）。  推荐：通过 Cosmos SDK 使用 Tendermint。理由：  * 成熟库和生态。 * 对该用例吞吐足够。 * 运维工具和可观测性发展良好。 * 若该链未来需要互操作，Co...
  c10_b09.md: ...需要互操作，Cosmos IBC 是加分项。  HotStuff 技术上“最佳”，但实现负担更重；对 21 节点许可链，Tendermint 的简单性占主导。  PBFT 在 2026 年是错误选择：吞吐限制和过时工具使其在新部署中劣于两者。  **Q2.**...
  c10_b12.md: ...验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人...
  c30_b02.md: ..."The latest gossip on BFT consensus." _arXiv:1807.04938_. — Tendermint。   #### 分布式存储（第四部分 — 第11-14章）  * Karger, D., et al. (1997)....

=== node (19 occurrences) ===
  c11_b09.md: ...rd-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational...
  c12_b10.md: ...ra 和 DynamoDB 不比 CockroachDB 不成熟；它们有不同设计优先级（大规模水平扩展、可预测 per-node 性能）。  综合：对大多数不可预测倾斜的 OLTP 工作负载，带自动分裂的范围分片是正确默认。对应用控制键分布的工作负...
  c12_b10.md: ...码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使分配在节点重启后保留并防止 flapping。LLM 代码...
  c18_b03.md: ...token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种结构。...
  c24_b04.md: ...│  Inference   │           │  Inference   │             │   Node 1     │           │   Node 2     │             │  (8x H100)...
  c24_b04.md: ...│  Inference   │             │   Node 1     │           │   Node 2     │             │  (8x H100)   │           │  (8x H100)...
  c25_b02.md: ...fill Tier<br/>(compute-optimized)"]             P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4× H100"...
  c25_b02.md: ...P1["Prefill Node 1<br/>4× H100"]             P2["Prefill Node 2<br/>4× H100"]         end         subgraph CacheTier["KV...
  c25_b02.md: ...Cache Tier<br/>(Mooncake / LMCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         subgrap...
  c25_b02.md: ...MCache)"]             K1[Cache Node 1]             K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tier<br/...
  c25_b02.md: ...ode Tier<br/>(memory-BW optimized)"]             D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H200"]...
  c25_b02.md: ...D1["Decode Node 1<br/>8× H200"]             D2["Decode Node 2<br/>8× H200"]         end         LB -->|"new request"| P...
  c25_b02.md: ...│  Prefill     │  │  Prefill     │                 │  Node 1      │  │  Node 2      │                 │  (4x H100)   │...
  c25_b02.md: ...│  │  Prefill     │                 │  Node 1      │  │  Node 2      │                 │  (4x H100)   │  │  (4x H100)   │...
  c25_b02.md: ...│  Decode      │  │  Decode      │                 │  Node 1      │  │  Node 2      │                 │  (8x H200)   │...
  c25_b02.md: ...│  │  Decode      │                 │  Node 1      │  │  Node 2      │                 │  (8x H200)   │  │  (8x H200)   │...
  c25_b03.md: ...ache，任何 decode 节点都可读取。  架构模式：KV cache 成为集群的独立 tier，而不只是 per-node 资源。Decode 节点无状态（按需从缓存层读取 KV cache）；KV cache 在许多 decode 节点间共...
  c25_b06.md: ...：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and d...
  c26_b09.md: ...求加速 decode 阶段。这些请求 p99 延迟降低约 2 倍。  (d) **尾部感知负载均衡器**：监控 per-node 延迟；若某节点异常高（灰色故障，第22章），绕行。Envoy outlier detection 或类似。  (e)...

=== memtable (19 occurrences) ===
  c12_b03.md: ...，后被 Cassandra、RocksDB、ScyllaDB、LevelDB 等采用。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b) **Write-ahead log（WAL）** ：每次...
  c12_b03.md: ...新写先到这里。  (b) **Write-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtabl...
  c12_b03.md: ...emtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定...
  c12_b03.md: ...Sorted String Table）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收...
  c12_b03.md: ...KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效...
  c12_b03.md: ...0-100 倍）。 * **读延迟** ：B-tree 点查胜（读一页即可；LSM 可能需检查多个 SSTable 和 memtable，Bloom filter 有帮助）。 * **范围扫描** ：可比拟；LSM 的有序合并与 B-tree 的叶遍历类似...
  c12_b03.md: ...，后被 Cassandra、RocksDB、ScyllaDB、LevelDB 等采用。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b) **Write-ahead log（WAL）** ：每次...
  c12_b03.md: ...新写先到这里。  (b) **Write-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtabl...
  c12_b03.md: ...emtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定...
  c12_b03.md: ...Sorted String Table）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收...
  c12_b03.md: ...KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效...
  c12_b03.md: ...0-100 倍）。 * **读延迟** ：B-tree 点查胜（读一页即可；LSM 可能需检查多个 SSTable 和 memtable，Bloom filter 有帮助）。 * **范围扫描** ：可比拟；LSM 的有序合并与 B-tree 的叶遍历类似...
  c12_b06.md: ...描述 LSM-tree 的写路径。为什么它比 B+ tree 写更快？  提示  LSM：先写 WAL（顺序），再写 memtable（内存）。B+ tree：读页、修改、写回（随机 I/O）。  **答案：** LSM 写路径：    1. 写追加到磁...
  c12_b06.md: ...LSM 写路径：    1. 写追加到磁盘 WAL（write-ahead log）。这是顺序写。   2. 写插入 memtable（内存有序结构）。这很快。   3. memtable 满时，刷盘成不可变 SSTable。   4. 定期通过 com...
  c12_b06.md: ...write-ahead log）。这是顺序写。   2. 写插入 memtable（内存有序结构）。这很快。   3. memtable 满时，刷盘成不可变 SSTable。   4. 定期通过 compaction 合并多个 SSTable。  用户可见...
  c12_b07.md: ...(a) LSM 写是 _顺序_ 的（WAL 追加；把整个 memtable 刷成新 SSTable）。传统存储上顺序写比随机写快 10-100 倍；在 SSD 上也快（但差距没这么大）。  (b...
  c12_b07.md: ...写比随机写快 10-100 倍；在 SSD 上也快（但差距没这么大）。  (b) LSM 批量磁盘 I/O：很多写先在 memtable 累积，再一起刷盘。B+ tree 每次页更新都要触碰磁盘。  (c) LSM 把 compaction 成本摊销：合并...
  c12_b09.md: ...写吞吐：非常高（memtable + WAL 顺序）。80% 写工作负载下表现好。 * 读延迟：每次读可能 touch memtable + Bloom filter + 多个 SSTable。冷读 p99 可能 1-5 ms。 * 空间放大：典型 1.5...
  c12_b11.md: ...理。系统会分裂过大的 range 并为负载重平衡副本。  LSM-tree 是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ t...

=== primary (19 occurrences) ===
  c13_b03.md: ...采用的模式。  结构：在键值存储之上的快照隔离事务，使用两阶段（Prewrite + Commit），通过一个指定的 “primary” 行协调。无中心协调者。  **阶段 1：Prewrite**  事务客户端从要写的键中选一个作为 _primary_...
  c13_b03.md: ...1. 读取当前值（及其时间戳）。   2. 在 start_timestamp 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁...
  c13_b03.md: ...标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 c...
  c13_b03.md: ...ewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primary key：    1. 原子地：写入 commit 记录（value, commit_timestamp）并清除锁。...
  c13_b03.md: ...commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是否提交。  若客户端在阶段 2 崩溃...
  c13_b03.md: ...事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primar...
  c13_b03.md: ...rimary，从而知道事务是否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percol...
  c13_b03.md: ...否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞...
  c13_b03.md: ...提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 p...
  c13_b03.md: ...。   #### Percolator 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真...
  c13_b03.md: ...y key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 primary key 扮演了协调者角色...
  c13_b03.md: ..._原子提交点_。提交与否是 primary 行状态的属性。遇到 secondary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 primary key 扮演了协调者角色。协调者故障被客...
  c13_b03.md: ...ary 锁的读者沿指针到 primary；primary 的状态（已提交或未提交）是真相来源。  无需中心协调者，因为 primary key 扮演了协调者角色。协调者故障被客户端故障取代，而客户端故障由读时恢复处理（任何后续 reader 都能清理）。...
  c13_b12.md: ...什么是 Google Percolator？它如何避免需要中心协调者？  提示  Percolator 用事务中的 “primary key” 作为原子提交点。恢复由读者而非协调者执行。  **答案：** Percolator 是 Google 的分布...
  c13_b12.md: ...在键值存储上实现快照隔离事务，无中心协调者。  如何避免协调者：事务指定其中一个键为 _primary_。原子提交点是 primary 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 se...
  c13_b12.md: ...。原子提交点是 primary 行上的单次变更（在时间戳处提交 value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：...
  c13_b12.md: ...value 并清除锁）。Secondary 键的锁指向 primary；任何遇到 secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。...
  c13_b12.md: ...指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清理由读者摊销；无需单独恢复进程。...
  c13_b12.md: ...解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清理由读者摊销；无需单独恢复进程。  用于：TiDB（Perc...

=== snowflake (19 occurrences) ===
  c14_b03.md: ...：分布式查询处理器处理分析工作负载（TB-PB 数据上的大扫描、聚合、跨源 join）。示例：Trino/Presto、Snowflake、BigQuery、Spark SQL。...
  c14_b04.md: ...优势：跨异构数据源的联邦查询；无需数据移动（查询下推）。  劣势：性能依赖源系统的索引和分区；调优复杂。   #### Snowflake  云原生数据仓库，计算与存储分离。  * 存储在类 S3 云对象存储中。 * 通过独立 virtual warehou...
  c14_b04.md: ...ogle ML 栈集成。  劣势：仅限 GCP；写得差的查询可能成本主导预算。   #### 模式  三者（Trino、Snowflake、BigQuery）遵循同一架构模式：存储与计算分离； aggressively 并行化；分析扫描使用列式布局；暴露 S...
  c14_b05.md: ...AP（混合 OLTP/OLAP） | TiDB，或 CockroachDB + 独立 OLAP 存储 纯 OLAP | Snowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora D...
  c14_b05.md: ...s / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | CockroachDB（任何云）、Snowflake（OLAP 任何云） 大规模水平扩展，可接受最终一致 | Cassandra、ScyllaDB、DynamoDB  第1...
  c14_b05.md: ...P（批处理作业）。矩阵建议：CockroachDB 单区域做 OLTP 路径（线性化、每分片 Raft、快速故障切换），Snowflake 或 Trino 做批处理重训路径。图查询组件是例外——若跨账户图稠密，图数据库（Neo4j、Amazon Neptun...
  c14_b05.md: ...这些特定查询，而 CockroachDB 保存图索引的账户状态。结果：三系统架构——CockroachDB（OLTP）、Snowflake（批处理/OLAP）、Neptune（图查询）。  该矩阵提供每个组件的起点；第11章诊断确认适配性；最终架构是跨组件的...
  c14_b06.md: ...本作为会计真相来源；CockroachDB 引用 TigerBeetle 转账 ID 但不重复余额状态。  (c) **Snowflake** 用于分析：数据每晚从 CockroachDB 和 TigerBeetle 复制到 Snowflake。BI 团队出...
  c14_b06.md: ...(c) **Snowflake** 用于分析：数据每晚从 CockroachDB 和 TigerBeetle 复制到 Snowflake。BI 团队出报表、监管方审计、ML 团队训练欺诈检测模型，都在 Snowflake 而不影响 OLTP 路径。  三系...
  c14_b06.md: ...和 TigerBeetle 复制到 Snowflake。BI 团队出报表、监管方审计、ML 团队训练欺诈检测模型，都在 Snowflake 而不影响 OLTP 路径。  三系统架构是金融级系统的现代模式。每个系统是各自工作负载的正确工具；集成通过定义良好的...
  c14_b08.md: ...处理支付时，运营存储记录元数据，账本记录资金变动；两者必须提交（跨两系统的 saga 模式）。  (c) **分析**：Snowflake。理由：serverless 计算（分析工作负载突发）；强并发支持多团队（BI、财务、合规、ML）；时间旅行便于审计。数...
  c14_b09.md: ...2.** 一个团队正在构建全球 SaaS 分析产品。客户通过产品 UI 查询自己的数据；查询可以是即席 SQL。团队在 Snowflake（托管）和数据湖上自托管 Trino 之间选择。比较。  提示  Snowflake：托管、成本可预测、控制少。Trin...
  c14_b09.md: ...据；查询可以是即席 SQL。团队在 Snowflake（托管）和数据湖上自托管 Trino 之间选择。比较。  提示  Snowflake：托管、成本可预测、控制少。Trino：自托管、更多控制、需要运维专长。  **答案：**  _Snowflake_ ：...
  c14_b10.md: ...* 若团队小（无专职数据平台工程师）：Snowflake。自托管 Trino 的运维负担会主导成本方程。 * 若团队有数据平台专长且产品在规模上对成本敏感：Trino。规模化后...
  c14_b10.md: ...若产品需要跨多个客户数据源查询（联邦查询到客户控制的数据库）：Trino 的联邦是正确工具。  混合：团队内部分析用 Snowflake；客户-facing 联邦查询用 Trino（因为 Snowflake 无法联邦到客户控制的源）。许多真实 SaaS 平...
  c14_b10.md: ...。许多真实 SaaS 平台两者都跑。  2026 趋势：随着开源 Trino 生态成熟（更好的优化器、缓存、运维工具），Snowflake 与 Trino 的差距在缩小。决策越来越依赖团队能力而非技术能力。  **Q3.** 用诊断方法（第11章）+ 事务协...
  c17_b11.md: ...作正确选择。  混合架构组合系统：Crescent 用 CockroachDB（线性化）+ 自定义计数器服务（最终）+ Snowflake（分析）。Glide 用 Yjs（CRDT/强最终）+ Cloudflare Durable Objects（per-o...
  c28_b03.md: ...CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud det...
  c28_b05.md: ...；§13.6.5 saga | n/a | | 14 | NewSQL + 查询处理器 | CockroachDB + Snowflake 用于分析 | n/a | | 15-17 | 最终、因果、CRDT | Redis 缓存（最终）、Yjs（CRDT）...

=== mesh (19 occurrences) ===
  c20_b06.md: ...察：trace 价值随分布式系统复杂度增加而增加。对简单两服务架构，trace 有用但非必需。对内含共识的 50 服务 mesh，trace 是主要调试工具；缺失 context 传播是严重的运维缺口。...
  c21_b08.md: ...护继电器 = 熔断器。发电厂 = 关键服务。  **答案：** 映射：  * **输电线路**：服务之间的连接。微服务 mesh 中，就是服务间的 API 调用。  * **电力流**：请求 / 负载。流过电网的电力就是流过服务 mesh 的请求。...
  c21_b08.md: ...。微服务 mesh 中，就是服务间的 API 调用。  * **电力流**：请求 / 负载。流过电网的电力就是流过服务 mesh 的请求。  * **线路上的保护继电器**：服务间连接上的熔断器。过载时跳闸；隔离故障。  * **发电厂**：产生工...
  c21_b08.md: ...付服务。  * **发电厂跳闸（离线自保）**：过载服务的 load shedding。  * **电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blackstart 孤岛**：cell-based 架构；能够隔离部分...
  c21_b08.md: ...在数据库上超时并重试，然后在彼此上超时并重试；级联扩散。  (e) **完全失败**：整个东北地区电网崩溃。等价：服务 mesh 无法服务任何请求；全面中断。  (f) **恢复需要干预**：电力运维必须手动一片片重启电网。等价：SRE 团队必须限...
  c21_b08.md: ...。  (f) **恢复需要干预**：电力运维必须手动一片片重启电网。等价：SRE 团队必须限流、重启服务、手动重建服务 mesh。  对分布式系统工程师的教训：模式是普遍的。任何具有放大和缺乏遏制的互联系统都会发生级联故障。相同防御（熔断器、loa...
  c23_b02.md: ...混沌工程学科” 的关键。工具容易；文化难。   ### 23.4 工具  现代混沌工程工具：  (a) **Chaos Mesh**：Kubernetes 原生混沌工程。注入 pod 故障、网络故障、磁盘故障、时间偏移。广泛采用。  (b) **L...
  c23_b02.md: ...层故障注入。慢连接、丢包、模拟下游变慢。与应用无关。  选择取决于部署：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...
  c23_b04.md: ...Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到 US-East。 * 14:20 UTC：系统稳定。EU-West 发起交易的 p99 延迟：480 ms...
  c23_b04.md: ...盘**：  * 稳态：维持（延迟在 SLO 内，成功率高于阈值）。 * 观察：     * 基于 Consul 的服务 mesh 故障转移耗时 30 秒；此窗口内 EU-West 用户看到超时。行动项：调整 Consul 健康检查频率。     *...
  c23_b10.md: ...（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey...
  c28_b02.md: ...metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etc...
  c28_b03.md: ...r/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]             CONT...
  c30_b12.md: ...dra 在 100+ 节点集群中才值得，此时其横向扩展是必需的。  **10 个服务不需要时不要伸手选 service mesh。** Istio/Linkerd 增加运维复杂度。对小型服务图，Kubernetes 原生服务发现足够。只有在多 re...
  c30_b12.md: ...，Kubernetes 原生服务发现足够。只有在多 region、多协议、复杂流量需求真实存在时才采用 service mesh。  **崩溃故障共识足够时不要伸手选 BFT。** BFT 需要 3 倍副本和运维成本。对操作者可信、节点 reaso...
  c30_b16.md: ...over 下失败。用共识 + fencing token。 | Ch.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的一个功能；mesh 还增加 mTLS、流量策略、可观测性——以及运维开销...
  c30_b16.md: ...token。 | Ch.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的一个功能；mesh 还增加 mTLS、流量策略、可观测性——以及运维开销。按需选择。 | Ch.19 | | 23...
  c30_b16.md: ...h.18 | | 22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的一个功能；mesh 还增加 mTLS、流量策略、可观测性——以及运维开销。按需选择。 | Ch.19 | | 23 | "我们有 Open...
  c30_b19.md: ...的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch...

=== best (18 occurrences) ===
  c04_b05.md: ...流 2（输入 → 输出）** ：给定一条应到达 N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代...
  c04_b05.md: ...rm reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 a...
  c04_b05.md: ...用真正需要的正确性性质，再选最弱的足够层。  广播层次作为分层架构：      graph TD         A["Best-effort broadcast<br/>(TCP + for-loop)"] -->|"+ agreement<br...
  c04_b05.md: ...9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast       (TCP plus a for-loop)         |...
  c04_b05.md: ...pplication | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期...
  c04_b05.md: ...-|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须...
  c04_b05.md: ...e | 顺序无关，只要交付  经济论证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，best-effort（无协调）与 atomic（每事件共识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性...
  c04_b05.md: ...流 2（输入 → 输出）** ：给定一条应到达 N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代...
  c04_b05.md: ...rm reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 a...
  c04_b05.md: ...用真正需要的正确性性质，再选最弱的足够层。  广播层次作为分层架构：      graph TD         A["Best-effort broadcast<br/>(TCP + for-loop)"] -->|"+ agreement<br...
  c04_b05.md: ...9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast       (TCP plus a for-loop)         |...
  c04_b05.md: ...pplication | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期...
  c04_b05.md: ...-|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须...
  c04_b05.md: ...e | 顺序无关，只要交付  经济论证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，best-effort（无协调）与 atomic（每事件共识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性...
  c04_b07.md: ...#### 概念检测  **Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreeme...
  c04_b07.md: ...uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：validity（发送者正确、接收者正确 → 交付）和 no-creation...
  c13_b09.md: ...-4 步补偿。选择取决于用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，...
  c30_b14.md: ...ule     ├── ch04_broadcast/     │   ├── best_effort.py    — best-effort broadcast (TCP + for-loop)     │   ├── reliable.py...

=== set (18 occurrences) ===
  c04_b09.md: ...至少有一个 peer 收到并重广播，消息就会到达全部。  **答案：** 伪代码：       delivered = set()      def broadcast(msg):         flood(msg, sender=self)...
  c11_b08.md: ...答案。  提示  MongoDB 的一致性取决于配置：writeConcern、readConcern、replica set vs sharded。默认与最强可用不同。  **答案：** MongoDB 的一致性高度依赖配置。精确答案必须说明哪...
  c13_b10.md: ...antial 交易，不是       -- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';...
  c13_b10.md: ...：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -...
  c13_b10.md: ...mi';       -- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '...
  c14_b04.md: ...强一致。 * 时间旅行：可在保留期内查询任何先前状态。  优势：serverless 计算；可预测成本；并发能力极强；“set it and forget it” 运维。  劣势：厂商锁定（专有格式，但 Iceberg 集成正在出现）；per-q...
  c16_b02.md: ...ster 把冲突抛给应用：读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove...
  c16_b02.md: ...值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 remove...
  c16_b02.md: ...型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 remove 的 G-Set）失败：若 A 添加元素 \(x\)，B 并发移除 \(x\)，合并结果必须决定谁赢。使用 tombstone（被移除元...
  c16_b02.md: ...re-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 d...
  c16_b02.md: ...air 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombston...
  c16_b02.md: ...中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-k...
  c16_b02.md: ...的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值的集合（类似 OR-Set）。  对协作文辑，相关的 CRDT 是 _list_（或数组、序列），允许在任意位置插入删除。这是难的。...
  c16_b11.md: ...PN-Counter（支持递减的计数器）、LWW-Register（最后写胜出）、MV-Register（多值）、OR-Set（观察删除集合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Y...
  c17_b08.md: ...并函数取并集。  _Riak 的 CRDT bucket types_（较新的 Riak 特性）：购物车可建模为 OR-Set CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，si...
  c17_b08.md: ...ing values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set 语义匹配购物车的 “物品并集” 行为。并发添加保留；并发移除被 tombstone；最终购物车是确定性合并。  若团队...
  c30_b14.md: ...PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT...
  c30_b14.md: ...PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT...

=== lock (18 occurrences) ===
  c10_b04.md: ...。Cosmos 生态（Osmosis、Juno、Akash）都使用 Tendermint。  Tendermint 的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint fork 因此...
  c13_b03.md: ..._。对事务要写的每个键：    1. 读取当前值（及其时间戳）。   2. 在 start_timestamp 处的 “lock” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary...
  c18_b04.md: ...1 as Coordinator A<br/>(token 100)         participant L as Lock Service<br/>(etcd; Raft)         participant Bank as Bank<b...
  c18_b04.md: ...C2 as Coordinator B<br/>(token 101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank: comm...
  c18_b04.md: ...ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired         C2->>L: acquire lock         L-->>C2: g...
  c18_b04.md: ...Note over L: Lock TTL expired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank: comm...
  c18_b04.md: ...1 as Coordinator A<br/>(token 100)         participant L as Lock Service<br/>(etcd; Raft)         participant Bank as Bank<b...
  c18_b04.md: ...C2 as Coordinator B<br/>(token 101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank: comm...
  c18_b04.md: ...ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired         C2->>L: acquire lock         L-->>C2: g...
  c18_b04.md: ...Note over L: Lock TTL expired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank: comm...
  c19_b07.md: ...des → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK persistent zNodes...
  c20_b03.md: ...ies IV Vol 3 会讲）。要点：OpenTelemetry 是现代默认；使用它可消除旧追踪系统的 vendor lock-in 顾虑。...
  c20_b09.md: ...el exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTelemetry 基础之上再提供该后端的具体配置。...
  c20_b10.md: ...metheus、structured logging、environment-based config）。Vendor lock-in 是真实且可避免的成本；忽略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布式追踪是请...
  c20_b10.md: ...dor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；profi...
  c27_b11.md: ...def process_payment(payment):         # Acquire distributed lock for the user's account         lock_key = f"user_lock:{paym...
  c28_b03.md: ...STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end...
  c30_b14.md: ...onstration     │   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo.py —...

=== gcp (18 occurrences) ===
  c10_b10.md: ...”是否现实威胁？对大型云厂商控制面，可能确实（内部威胁、定向攻击）。对大多数其他系统，BFT 成本超过威胁概率。AWS、GCP、Azure 的元数据存储使用崩溃容错共识（加上强物理安全和访问控制）而非 BFT。威胁真实，但通过其他手段管理（纵深防...
  c11_b03.md: ...延迟买单） Strengths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性化的工作负载（广告...
  c11_b07.md: ...比较，而非厂商喜欢的维度。  对 Crescent 的决策：Spanner-vs-CockroachDB 博客不会处理 GCP 锁定问题（一家厂商不会强调另一家的锁定劣势，另一家会过度强调）。方法自然 surfaced，因为“需要什么云”是优势/...
  c13_b16.md: ...金融级公司生产运行多年，无需 TrueTime；经验证据表明 HLC 有效。TrueTime 的成本（GPS 基础设施、GCP 锁定或 AWS Time Sync Service 依赖）真实，而边际正确性收益对大多数工作负载很小。  综合：Tru...
  c13_b16.md: ...5 年内我们可能看到 HLC 系统迁移到 clock-bound API，差别变得无关紧要。目前选择是运维性的：若你在 GCP 且成本可接受，用 TrueTime；若你在 AWS/Azure/多云且运维简单性重要，用 HLC。  更微妙考虑：Tr...
  c14_b01.md: ...。  2026 年，NewSQL 类别已成熟。主要竞争者：  * **Google Spanner** ：开创者；仅限 GCP。 * **CockroachDB** ：开源 NewSQL；可跑在任何云上。 * **TiDB** ：另一开源 New...
  c14_b01.md: ...* 基于 TrueTime 的时间戳避免不确定性重启。 * 单一全球实例可跨多区域（或仅区域）。  劣势：  * 仅限 GCP。不能部署在 AWS 或 Azure。 * 昂贵（按节点小时 + 存储和 IO 计费）。 * 相比完整 Postgres...
  c14_b01.md: ...tgres，某些 SQL 功能（子查询等）曾有限制，虽已逐步改善。  Spanner 适合：能 justify 成本和 GCP 承诺的全球规模工作负载。生产示例：Google 自身基础设施（Ads、Cloud、Workspace）、Spotify...
  c14_b02.md: ...的开源 NewSQL。有时被描述为 “Spanner for the rest of us”，因为它提供类似保证而无 GCP 锁定。  关键属性：  * 每 range 使用 Raft 复制。 * HLC-based MVCC，NTP 偏移上界...
  c14_b02.md: ...* HLC-based MVCC 需要严格的 NTP 纪律。  CockroachDB 适合：不能接受 Spanner GCP 锁定的多区域 Postgres 工作负载。生产示例：众多 fintech（DoorDash、Comcast 部分工作负...
  c14_b04.md: ...化器。 * TB 级数据秒级扫描。  优势：即席分析非常快；按查询付费；与 Google ML 栈集成。  劣势：仅限 GCP；写得差的查询可能成本主导预算。   #### 模式  三者（Trino、Snowflake、BigQuery）遵循同一...
  c14_b07.md: ...hDB 和 Spanner。  提示  两者都是 PC/EC NewSQL。差异：TrueTime vs HLC、仅限 GCP vs 任何云、专有 SQL vs Postgres 兼容。  **答案：**  Question | Spanner...
  c14_b07.md: ...定性的可变成本） SQL 兼容性 | Spanner SQL（专有） | 兼容 Postgres 线缆 部署 | 仅限 GCP | 任何云、本地 成熟度 | 2012 年生产；2017 年对外 | ~2019 年生产  两者都是 PC/EC Ne...
  c14_b07.md: ...EC NewSQL，高层保证相似。关键差异：Spanner 有 TrueTime 优势（一致延迟、无疑虑性重启）但仅限 GCP；CockroachDB 有 Postgres 兼容优势且可跑在任何地方，但争用下付可变成本。  新工作负载选择：若在...
  c14_b07.md: ...；CockroachDB 有 Postgres 兼容优势且可跑在任何地方，但争用下付可变成本。  新工作负载选择：若在 GCP 且成本可接受，选 Spanner；若需要多云、本地或 Postgres 兼容，选 CockroachDB。  **Q3...
  c19_b03.md: ...或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kuberne...
  c29_b01.md: ...会变   #### 硬件时间服务将替代许多工作负载的 HLC  2026 现实（第3、13章）：TrueTime 仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sy...
  c30_b04.md: ...rueTime | 基于租约；亚秒级 | 线性化 + 旧数据读 | CP | PC/EC | 全局线性化工作负载；仅限 GCP | | **CockroachDB** | 每 range 一个 Raft；HLC + 偏移 | Raft 重新选举；...

=== bank (18 occurrences) ===
  c13_b07.md: ...ion         participant F as Fraud         participant B as Bank         participant L as Ledger         participant N as No...
  c18_b04.md: ...pant L as Lock Service<br/>(etcd; Raft)         participant Bank as Bank<br/>(highest_seen_token)         participant C2 as...
  c18_b04.md: ...s Lock Service<br/>(etcd; Raft)         participant Bank as Bank<br/>(highest_seen_token)         participant C2 as Coordina...
  c18_b04.md: ...quire lock         L-->>C1: granted, token=100         C1->>Bank: commit (token=100)         Bank->>Bank: 100 >= highest (wa...
  c18_b04.md: ...ed, token=100         C1->>Bank: commit (token=100)         Bank->>Bank: 100 >= highest (was 0); accept         Bank-->>C1:...
  c18_b04.md: ...en=100         C1->>Bank: commit (token=100)         Bank->>Bank: 100 >= highest (was 0); accept         Bank-->>C1: ok...
  c18_b04.md: ...Bank->>Bank: 100 >= highest (was 0); accept         Bank-->>C1: ok         Note over C1: GC pause 60s         Note o...
  c18_b04.md: ...quire lock         L-->>C2: granted, token=101         C2->>Bank: commit (token=101)         Bank->>Bank: 101 >= highest (10...
  c18_b04.md: ...ed, token=101         C2->>Bank: commit (token=101)         Bank->>Bank: 101 >= highest (100); accept         Bank-->>C2: ok...
  c18_b04.md: ...en=101         C2->>Bank: commit (token=101)         Bank->>Bank: 101 >= highest (100); accept         Bank-->>C2: ok...
  c18_b04.md: ...)         Bank->>Bank: 101 >= highest (100); accept         Bank-->>C2: ok         Note over C1: resumed after pause...
  c18_b04.md: ...: ok         Note over C1: resumed after pause         C1->>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highe...
  c18_b04.md: ...pause         C1->>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highest (101); reject         Bank-->>C1: STA...
  c18_b04.md: ...C1->>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highest (101); reject         Bank-->>C1: STALE_TOKE...
  c18_b04.md: ...e)         Bank->>Bank: 100 < highest (101); reject         Bank-->>C1: STALE_TOKEN   即使 C1 醒来仍以为自己持有锁（经典的基于超时锁失败模式），银行的单调 t...
  c27_b12.md: ...*：`bank_service.transfer(payment)` 没有 idempotency key。若超时重试，bank 可能转账两次。    2. **谎言 2（网络可靠）**：任何网络调用（bank、db、notification、le...
  c27_b12.md: ...5（部分故障）**：四个顺序调用（bank、db、notification、ledger）没有 rollback。若 bank 成功后 db 失败，用户已在 bank 扣款但 DB 未扣；后续重试会重复扣款。    6. **谎言 6（CAP 简...
  c27_b12.md: ...nk、db、notification、ledger）没有 rollback。若 bank 成功后 db 失败，用户已在 bank 扣款但 DB 未扣；后续重试会重复扣款。    6. **谎言 6（CAP 简单）**：注释 “CockroachDB...

=== spans (18 occurrences) ===
  c20_b01.md: ...式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 tra...
  c20_b01.md: ...成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解）：       gr...
  c20_b01.md: ...tes（自定义键值对）。 * Events（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布...
  c20_b01.md: ...式 trace 由 _spans_ 组成。每个 span 代表一个工作单元（接收到的请求、被调用的函数、下游 RPC）。Spans 有父子关系，形成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 tra...
  c20_b01.md: ...成一棵树（或 DAG）。根 span 是初始请求；子 span 是父请求触发的下游工作。  一棵 trace，可视化为 spans 树（这是说明性支付风格 trace；第28章 §28.2 有 Crescent 系统对应完整拆解）：       gr...
  c20_b01.md: ...tes（自定义键值对）。 * Events（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 trace 在大规模下不现实；采样决定保留哪些 trace。   ### 20.2 三个分布...
  c20_b02.md: ...ate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 context 丢失**：服务把工作交给后台队列但未传播 tr...
  c20_b02.md: ...span attribute 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“...
  c20_b03.md: ...**：一个 span 花 200 ms 可能是正常，也可能比平时慢 10 倍；比较揭示。 (d) **看 “缺失” 的 spans**：若应该被调用的服务没出现，可能是上游没调用它（bug 或路由问题）。   ### 20.4 Trace 不能告诉你...
  c20_b05.md: ...### 概念检测  **Q1.** 什么是分布式 trace？每个 span 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attribu...
  c20_b05.md: ...ributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的树（或 DAG）；每个 span 代表一个工作单元。  每个 span 携带：  * **Trace ID**：该...
  c20_b05.md: ...据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界...
  c20_b05.md: ...l-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_ ：采样决策在 trace 起点...
  c20_b05.md: ...（根 span）做出。决策（是否采样）通过 trace context 传播给所有子 span。要么 trace 所有 spans 都被采样，要么都没有。...
  c20_b06.md: ..._Tail-based sampling_ ：spans 被缓冲到 trace 完成（或超时）。基于 trace 整体属性做决策（例如 “保留所有含错误的 trace；成功 t...
  c20_b06.md: ...trace context 包含在共识日志条目的元数据中。apply 代码运行时提取 context，并为它创建的任何 spans 使用。现代 Raft 库（etcd 的 Raft、raft-rs）直接支持；旧库可能不支持。  更深观察：trace...
  c20_b07.md: ...oint、特定输入模式？找输入相关性。  (e) **钻取一条失败 trace**：选一条失败 trace，看错误前的 spans。X 当时在做什么？它调用了什么？它调用的是否先失败？...
  c20_b10.md: ...生成代码会让团队失去迁移能力。   ### 本章总结  分布式追踪是请求在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特...

=== tracing (18 occurrences) ===
  c20_b08.md: ...s 和复现。常见模式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低到随机抽查可能发现不了。Tracing  surfaced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTele...
  c20_b09.md: ..._因果链_，但不覆盖 _服务内部行为_（CPU 竞争、锁竞争、GC、内存压力）。许多生产问题根因在 trace 之外。Tracing 是一种工具；完整调试工具集包括性能分析、日志分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试...
  c20_b09.md: ...题根因在 trace 之外。Tracing 是一种工具；完整调试工具集包括性能分析、日志分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 traci...
  c20_b09.md: ...分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具...
  c20_b09.md: ...acing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing...
  c20_b09.md: ...整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracin...
  c20_b09.md: ...tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelem...
  c20_b09.md: ...racing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelemetry" 是运维标准。  第二个考虑：AI 辅助调试工...
  c20_b09.md: ..._因果链_，但不覆盖 _服务内部行为_（CPU 竞争、锁竞争、GC、内存压力）。许多生产问题根因在 trace 之外。Tracing 是一种工具；完整调试工具集包括性能分析、日志分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试...
  c20_b09.md: ...题根因在 trace 之外。Tracing 是一种工具；完整调试工具集包括性能分析、日志分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 traci...
  c20_b09.md: ...分析、指标关联和复现。  综合：tracing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具...
  c20_b09.md: ...acing 极大改善了分布式系统的调试体验，但没有解决整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing...
  c20_b09.md: ...整个问题。挣扎的团队要么缺少 tracing（基本门槛），要么把 tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracin...
  c20_b09.md: ...tracing 当作唯一工具（过度依赖）。成熟团队把 tracing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelem...
  c20_b09.md: ...racing 作为入口，必要时钻取其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelemetry" 是运维标准。  第二个考虑：AI 辅助调试工...
  c21_b06.md: ...重要**。FirstEnergy 运维看不到初始故障（监控软件 bug）；等他们看到扩散时，干预已太晚。分布式系统类比：tracing、metrics、alerting 必须在系统降级时仍工作。  (c) **防御必须本地运行**。每条输电线路有自己的保...
  c22_b07.md: ...若节点过去一分钟 p99 延迟超过阈值（如 5 秒），健康检查失败。负载均衡器降低失败节点优先级。  (b) **添加 tracing**：对每次慢请求做分布式追踪，揭示瓶颈。即使只采样 1% 的请求，也足以识别模式。  (c) **添加详细 GC 监控...
  c27_b10.md: ...* 描述 AI 生成分布式代码的审查清单。  提示  10 项：幂等性、超时、重试、熔断器、排序、锁、部分故障、一致性、tracing、可观测性。  **答案：** 十项检查：  (a) **幂等性**：每个网络调用应幂等或携带 idempotency...

=== located (18 occurrences) ===
  c25_b03.md: ...负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂...
  c25_b06.md: ...ken 总计**：约 185 ms（首 token）+ 199 × 30 ms（其余）= 约 6.2 秒。  对 co-located（未分离）架构，首 token 延迟可能约 150 ms（prefill + 首 decode，无传输），但 per-t...
  c25_b07.md: ...prefill。  Tobias 的部署使用 (a) 和 (c)；典型首 token 延迟约 150 ms，接近 co-located 性能。...
  c25_b08.md: ...载测试。 * 验证：延迟、吞吐、故障模式。  **Phase 2：逐步转换（3-4 个月）**  * 更多节点从 co-located 转成分离角色。 * 运行 50/50 分割（25 节点分离，25 节点 co-located）两周。 * 测量：利用率...
  c25_b08.md: ...* 更多节点从 co-located 转成分离角色。 * 运行 50/50 分割（25 节点分离，25 节点 co-located）两周。 * 测量：利用率、每请求成本、p99 延迟。 * 若指标确认收益，继续转换。  **Phase 3：全部分离（...
  c25_b08.md: ...+ 30 decode + 10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-located 路径。  **Phase 4：调优与优化（7+ 个月）**  * 根据观测流量调整 prefill/decode 比例...
  c25_b08.md: ...rompt 模式添加前缀复用逻辑。 * 监控任何回归；按需调整。  **每阶段验证门**：  * p99 延迟在 co-located 基线 10% 内。 * 吞吐至少比 co-located 基线高 20%（目标收益）。 * 没有超过团队事故响应能力的...
  c25_b08.md: ...调整。  **每阶段验证门**：  * p99 延迟在 co-located 基线 10% 内。 * 吞吐至少比 co-located 基线高 20%（目标收益）。 * 没有超过团队事故响应能力的新故障模式。  **风险缓解**：  * 每阶段都有 ro...
  c25_b08.md: ...过团队事故响应能力的新故障模式。  **风险缓解**：  * 每阶段都有 rollback 计划：指标回退则回到 co-located。 * 客户沟通：通知大客户架构变化；为需要稳定延迟的客户提供 opt-out。 * 工程投入：2 名高级工程师全职做分...
  c25_b10.md: ...辩护或反驳：“Disaggregated prefill/decode 是 2026 年任何生产推理部署的默认。Co-located 是遗留方案。”  **讨论：** 辩护：分离在典型工作负载相同延迟下提供 2-4 倍吞吐提升。Splitwise 和...
  c25_b10.md: ...更复杂编排）。对小部署（< 20 节点），运维开销超过效率收益。对没有推理专用平台工程师的 startup 团队，co-located 运维更简单。强制分离会惩罚小而简单的场景。  综合：分离是大型生产部署（50+ 节点、每月 10 万美元以上推理预算）...
  c25_b10.md: ...小而简单的场景。  综合：分离是大型生产部署（50+ 节点、每月 10 万美元以上推理预算）的正确默认。对小部署，co-located 运维更简单、成本可接受。选择取决于规模。  2026 现实：所有主要推理厂商都采用分离，但在大型 enterprise...
  c25_b11.md: ...cache 传输）。对交互式工作负载可感知；缓解措施（同 chassis NVLink、预取、前缀缓存）使其接近 co-located 性能。  第26章结束第八部分，讲负载均衡和投机解码作为分布式协议。Anyscale/Together/Firewor...
  c26_b03.md: ...ulative decoding 处于早期生产部署。已发布案例（Together 的栈、一些自定义实现）显示相比 co-located speculative decoding 1.5-2.5 倍吞吐，代价是额外网络跳数增加首 token 延迟。...
  c26_b06.md: ...rification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decoding 更合适：  * 小集群，分布式运维开销占主导。 * 网络延迟会把首 toke...
  c26_b09.md: ...与 “同一机器 draft + verify”。各何时更好？  提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft...
  c26_b09.md: ...提示  分布式：draft 更便宜硬件，独立扩缩。Co-located：更低延迟，更简单。  **答案：**  _Co-located speculative decoding（draft 和 verify 在同 GPU/节点）_：  * 更低 per-...
  c26_b09.md: ...、故障处理）。 * 更高 per-token 延迟（网络跳数增加 0.5-2 ms）。  各何时更好：  * **Co-located**：小集群（< 10 节点）；没有专门推理平台工程的团队；per-token 延迟要求紧的工作负载；draft mod...

=== yuki (17 occurrences) ===
  c01_b03.md: ...多熟悉。  本书的四位主角会在不同系统、不同领域反复执行这个循环。Olamide 运行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continu...
  c01_b08.md: ...day（第23章），验证修复在模拟分区下是否有效。  Crescent 现在运行的系统，就是 Olamide、Anya、Yuki 和 Tobias 最终在第28章一起交付的系统。第1章到第28章之间的路径是本书正文；第29章则前瞻这个系统将如何在...
  c15_b11.md: ...26 生产现实（Notion 和 Linear 中的 Yjs、Ink & Switch 项目中的 Automerge）。Yuki 的实例是 Glide 协作文档编辑器实现。  ---...
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作...
  c16_b01.md: ...为可能的数学是 CRDT（Conflict-free Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于部分子产品）。本章是 Yuki 的工作流 9：用 CR...
  c16_b01.md: ...，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于部分子产品）。本章是 Yuki 的工作流 9：用 CRDT 实现 Glide 协作文档编辑器。  CRDT 是过去十年分布式状态系统中最具运维重要性的...
  c16_b03.md: ...。 * Automerge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且其文档模型比 Autome...
  c16_b03.md: ...（Glide 在浏览器运行，包括慢网络），且其文档模型比 Automerge 完整历史图更简单。   ### 16.4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档...
  c16_b07.md: ...，LWW 可用。对冲突有意义的数据，用 MV 或领域特定 CRDT。对需要拒绝并发写的字段，线性化。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list...
  c16_b08.md: ...#### 应用问题  **Q1.** Yuki 的 Glide 团队想添加 “实时在线状态”（显示其他用户光标）。如何用 Yjs 实现？  提示  光标位置不是持久状...
  c16_b11.md: ...t（观察删除集合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs...
  c16_b11.md: ...案。因果一致（COPS/Eiger 家族）、强最终（CRDT 作为原型），以及把它们放在线性化和最终一致之间的一致性格。Yuki 的 Glide 系统同时使用了这三者的元素。  ---...
  c27_b15.md: ...层都会产生可预测缺口。对 AI 审查 PR 橡皮图章是被点名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模型是贯穿线...
  c28_b01.md: ...RE 主管。Anya 是数据库主管（Stratus DB，现在内嵌为 Crescent 的 commit-log 层）。Yuki 从 Glide 借调来负责协作文档编辑组件（商户入驻流程）。Tobias 从 Continuum Inference...
  c28_b07.md: ...rescent 团队推销集成的方式；他们对 Stratus 运行了该方法，验证了其 profile 匹配需求。”  **Yuki（从 Glide 借调）**：“商户入驻合同编辑器使用 Yjs。协作编辑 ‘刚好工作’，因为 CRDT 确定性地处理冲突...
  c28_b10.md: ...MVCC | 协调器状态在易失内存；竞争下不确定性重启；下游 API 缺少 fencing token | | 9 | Yuki 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定性合并后的文档状态 | R...
  c29_b04.md: ...、2027 或更晚读这本书。具体年份影响哪些具名系统仍 current。它不影响心智模型。  Olamide、Anya、Yuki 和 Tobias 是复合人物；他们的系统和事件是作者工作和评审过的生产架构的真实组合。平台设计（分层架构、每个组件的诊...

=== usd (17 occurrences) ===
  c02_b07.md: ...据同步任务每五分钟跑一次“以保持区域同步”。收到云账单后，团队发现每次同步传输 50 GB 跨区域数据，单价 0.09 USD/GB。月账单：数千美元，用于一个没人主动使用的功能。  _网络是同构的。_ 示例：某服务在测试中 p50 延迟 50...
  c06_b05.md: ...价下，区域间移动数据的美元成本大致：  * 同一区域内：免费或几乎免费。 * 区域间（同一云）：0.01 到 0.09 USD / GB。 * 云间：0.08 到 0.15 USD / GB。  对每天区域间复制 1 TB 的系统，仅 egres...
  c06_b05.md: ...：免费或几乎免费。 * 区域间（同一云）：0.01 到 0.09 USD / GB。 * 云间：0.08 到 0.15 USD / GB。  对每天区域间复制 1 TB 的系统，仅 egress 就是 10-90 USD/天，或 300-2,70...
  c06_b05.md: ...：0.08 到 0.15 USD / GB。  对每天区域间复制 1 TB 的系统，仅 egress 就是 10-90 USD/天，或 300-2,700 USD/月。对每天复制 100 TB 的系统，年费六位数。  架构含义：  * **复制...
  c06_b05.md: ...GB。  对每天区域间复制 1 TB 的系统，仅 egress 就是 10-90 USD/天，或 300-2,700 USD/月。对每天复制 100 TB 的系统，年费六位数。  架构含义：  * **复制 delta，而非全量。** 大多数跨...
  c23_b13.md: ...集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  ---...
  c24_b05.md: ...集群，成本由 GPU 租赁主导。截至 2026 年：  * H100 80GB 云按需：每张 GPU 每小时约 3-4 USD。 * 1 年预留承诺：每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效...
  c24_b05.md: ...100 80GB 云按需：每张 GPU 每小时约 3-4 USD。 * 1 年预留承诺：每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的...
  c24_b05.md: ...每张 GPU 每小时约 2-3 USD。 * 本地（CapEx 摊销）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每小时 1200 USD，每月 86.4 万...
  c24_b05.md: ...）：每张 GPU 每小时有效约 1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每小时 1200 USD，每月 86.4 万美元。加上网络、存储和运维。  每个请求成本：  * Llama 3...
  c24_b05.md: ...1.50-2.50 USD。  对 Continuum 的 400 张 GPU，按 3 USD/小时：每小时 1200 USD，每月 86.4 万美元。加上网络、存储和运维。  每个请求成本：  * Llama 3 70B 上典型 1000-to...
  c24_b05.md: ...求成本：  * Llama 3 70B 上典型 1000-token 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求 GPU 成本 0.004-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD...
  c24_b05.md: ...en 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求 GPU 成本 0.004-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD，每月 12万-39万 USD。  对 Continuum 的客...
  c24_b05.md: ...USD/小时，即每个请求 GPU 成本 0.004-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD，每月 12万-39万 USD。  对 Continuum 的客户定价：通常每个请求 0.01-0.02 USD（取决于...
  c24_b05.md: ...U 成本 0.004-0.013 USD。 * 每天 100 万请求：每天 4k-13k USD，每月 12万-39万 USD。  对 Continuum 的客户定价：通常每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、...
  c24_b05.md: ...13k USD，每月 12万-39万 USD。  对 Continuum 的客户定价：通常每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个...
  c25_b06.md: ...转化为几个百分点的成本节省，但运维复杂度增加工程时间和风险。  对大集群（50+ 节点），效率收益可观：  * 1M+ USD/月成本下 40% 容量提升即每月 40 万美元节省。 * 运维复杂度在更多节点上摊销。 * 团队规模足以 justif...

=== riak (17 occurrences) ===
  c02_b10.md: ...xos 实现线性化。成本：远高于非 LWT 写入（常 4 到 10 倍），因为每次写入都发生 Paxos 往返。  对 Riak：启用后的强一致 bucket 是线性化的。成本：每次操作支付一次协调往返；吞吐比最终一致 bucket 低一个数量级。...
  c03_b03.md: ...长，除非垃圾回收旧条目，而这有自身的正确性考量。  实践中，完整向量时钟用于小 N 的 CRDT 风格系统（第16章）。Riak 使用 dotted version vectors。现代系统常使用下面的混合替代方案。...
  c03_b11.md: ...version vectors（DVV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃），Riak 使用过。对活跃消息系统，DVV 没有直接帮助，因为每个活跃服务都会贡献。...
  c04_b10.md: ...version vectors（DVV）** 或稀疏向量。若大多数条目为零（大多数进程最近未贡献消息），只存非零条目。Riak 使用过类似技术。成本是检测条目何时可丢弃（该进程“跟上”到足以让其条目隐含）的 bookkeeping。当贡献呈突发且...
  c05_b03.md: ...给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致答案。  用于：Dynamo（及其后代 Cassandra、Riak、ScyllaDB）。  关键性质是 _no failover required_ ：任意节点任何时候都能接受写，因此任...
  c05_b03.md: ...。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **Sibling values** 保留到应用解决。Riak 的方法。应用看到所有冲突值并选择（或合并）。 * **CRDT**（第16章）。冲突由数据类型的合并函数确定性解决。...
  c05_b04.md: ...ssion 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必...
  c05_b06.md: ...g、CRDT）收敛并发写。无需故障切换，因为没有指定 leader 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。l...
  c11_b01.md: ...；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、DynamoDB、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单 leade...
  c15_b02.md: ...andra 默认。  (b) **Sibling values** ：系统保留所有冲突值；应用看到它们并选择（或合并）。Riak 的做法。需要应用逻辑，但从不静默丢失数据。  (c) **CRDT** ：数据类型被设计成冲突有确定性合并函数。系统自...
  c16_b02.md: ...其他版本支配的条目。  MV-Register 把冲突抛给应用：读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能...
  c17_b02.md: ...强最终数据类型。Yjs、Automerge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT bucket types 都是生产示例。  强最终某种意义上 _弱于_ 因果一致（不保证收敛过程中所有副...
  c17_b08.md: ...许多工作负载是正确模型，但会以更严格读不暴露的方式揭示底层复制滞后。  **Q3.** 一个团队在购物车工作负载中选择 Riak（sibling-value 冲突解决）与 Cassandra（LWW）。分析。  提示  购物车：不同设备并发更新；合...
  c17_b08.md: ...[X]}, {cart: [Y]}]` 并决定。标准购物车合并是 “物品并集”，由应用实现（Riak 不自动合并）。  Riak 的方法干净地处理购物车工作负载：并发添加作为 sibling 保留；应用的合并函数取并集。  _Riak 的 CRDT...
  c17_b08.md: ..._Riak 的 CRDT bucket types_（较新的 Riak 特性）：购物车可建模为 OR-Set CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling valu...
  c17_b08.md: ...。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set 语义匹配购物车的 “物品并集” 行为。并发添加保留；并发移除被...
  c17_b08.md: ...Cassandra：重新设计 schema（每物品一行，而非每购物车一行），并对 “清空购物车” 操作使用 LWT。比 Riak CRDT 方法复杂但可行。...

=== time (17 occurrences) ===
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distributed System...
  c03_b07.md: ...Time 需要 GPS、原子钟、软件栈和运维纪律。大多数公司数据中心里没有 GPS。AWS 等云厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 Time...
  c05_b06.md: ...为的不一致。  恢复选项：  * 若主库磁盘可恢复，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入永久丢失。团队必...
  c07_b06.md: ...rk、Lynch、Stockmeyer 1988 形式化）：系统有一个未知的 Global Stabilization Time（GST）。GST 前系统完全异步（消息交付和进程速度无界）。GST 后系统同步（消息在未知但有限上界内交付）。  协议...
  c08_b01.md: ...rt 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。它描述了一座虚构希腊岛屿，兼职立法者通过涉及信使、账簿和法定人数的繁复仪式颁布法令。论...
  c13_b05.md: ...库使用 HLC + 偏移上界。成本可变（取决于争用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给...
  c13_b17.md: ...移上界作为实用的 TrueTime 替代。成本可变（争用时重启）而非恒定（每次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide...
  c20_b05.md: ...l。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **...
  c27_b12.md: ...整个函数。    3. **谎言 3（时钟）**：`ledger_service.record(payment.id, time.time())` 使用本机时钟时间戳。跨 region ledger 排序依赖机器时钟。    4. **谎言 4（锁...
  c27_b12.md: ...3. **谎言 3（时钟）**：`ledger_service.record(payment.id, time.time())` 使用本机时钟时间戳。跨 region ledger 排序依赖机器时钟。    4. **谎言 4（锁）**：基...
  c27_b12.md: ...r 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga...
  c27_b12.md: ...idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有事务/saga（谎言 5...
  c29_b01.md: ...仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采...
  c30_b01.md: ...tory." _IEEE Computer_. — PACELC 论文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System...
  c30_b02.md: ...ssing Letters_. — 安全性/活性分解。 * Lamport, L. (1998). "The Part-Time Parliament." _ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L. (2001...
  c30_b03.md: ...前瞻（第29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，包括 2024 年在 EC2 上引入微秒级有界时间。...
  c30_b06.md: ...(l, c)，l 类 wall-clock，c 为逻辑计数器；尊重 happens-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token...

=== self (17 occurrences) ===
  c03_b13.md: ...amportProcess:         def __init__(self, pid):             self.pid = pid             self.L = 0          def local_event(s...
  c03_b13.md: ...__init__(self, pid):             self.pid = pid             self.L = 0          def local_event(self, description):...
  c03_b13.md: ...0          def local_event(self, description):             self.L += 1             return self.L, description          def...
  c03_b13.md: ...f, description):             self.L += 1             return self.L, description          def send(self, dest, msg):...
  c03_b13.md: ...description          def send(self, dest, msg):             self.L += 1             # 消息携带 (L, msg)。dest 是另一进程。...
  c03_b13.md: ...程。             dest.receive(self.L, msg)             return self.L          def receive(self, sender_L, msg):             se...
  c03_b13.md: ...lf.L          def receive(self, sender_L, msg):             self.L = max(self.L, sender_L) + 1             return self.L, ms...
  c03_b13.md: ...self.L = max(self.L, sender_L) + 1             return self.L, msg   三进程走查：  Step | Action | L_1 | L_2 | L_3 | 注释 ---|-...
  c04_b03.md: ...# 每个进程维护 V[1..N]，初始全零。     on broadcast(msg):         V[self] += 1         send_to_all(msg, V)      on receive(msg, V_ms...
  c04_b09.md: ...= set()      def broadcast(msg):         flood(msg, sender=self)      def flood(msg, sender):         if msg in delivered:...
  c04_b09.md: ...in all_processes:             if peer != sender and peer != self:                 send(peer, msg, sender=self)      def on_r...
  c04_b09.md: ...er and peer != self:                 send(peer, msg, sender=self)      def on_receive(msg, sender):         flood(msg, sende...
  c05_b09.md: ...class Session:         def __init__(self):             self.max_hlc = (0, 0)          def write(self, key, value):...
  c05_b09.md: ...write(key, value)             # 服务器返回写提交时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return...
  c05_b09.md: ...t          def read(self, key):             # 请求数据的最小 HLC 为 self.max_hlc。             result = db.read(key, min_hlc=self.max...
  c05_b09.md: ...C 为 self.max_hlc。             result = db.read(key, min_hlc=self.max_hlc)             # 服务器返回数据及读取时的 HLC。             self.m...
  c05_b09.md: ...c=self.max_hlc)             # 服务器返回数据及读取时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return...

=== sender (17 occurrences) ===
  c04_b02.md: ...到了消息，该消息就会通过重广播到达所有正确接收者。  最简单版本的伪代码：       on receive(msg, sender):         if msg not in delivered:             delivered.ad...
  c04_b02.md: ...for peer in all_processes:                 if peer != sender:                     send(peer, msg)             upper_laye...
  c04_b03.md: ...processes:             send(peer, msg)      on receive(msg, sender):         if msg not in received:             received.add(...
  c04_b03.md: ...send(peer, ACK(msg))      on receive(ACK(msg), sender):         acks[msg].add(sender)         if len(acks[msg]) >...
  c04_b03.md: ...= 1         send_to_all(msg, V)      on receive(msg, V_msg, sender):         buffer.add((msg, V_msg, sender))         try_deli...
  c04_b03.md: ...eceive(msg, V_msg, sender):         buffer.add((msg, V_msg, sender))         try_deliver_from_buffer()      def try_deliver_fr...
  c04_b03.md: ...deliverable = None             for (msg, V_msg, sender) in buffer:                 # 当所有因果上先于它的消息都已交付时才可交付。...
  c04_b03.md: ...# 当所有因果上先于它的消息都已交付时才可交付。                 if V_msg[sender] == V[sender] + 1 and all(V_msg[i] <= V[i] for i in others)...
  c04_b03.md: ...所有因果上先于它的消息都已交付时才可交付。                 if V_msg[sender] == V[sender] + 1 and all(V_msg[i] <= V[i] for i in others):...
  c04_b03.md: ...in others):                     deliverable = (msg, V_msg, sender)                     break             if deliverable is No...
  c04_b03.md: ...buffer.remove(deliverable)             V[deliverable.sender] += 1             upper_layer.deliver(deliverable.msg)   性质...
  c04_b09.md: ...livered = set()      def broadcast(msg):         flood(msg, sender=self)      def flood(msg, sender):         if msg in delive...
  c04_b09.md: ...t(msg):         flood(msg, sender=self)      def flood(msg, sender):         if msg in delivered:             return         d...
  c04_b09.md: ...)         for peer in all_processes:             if peer != sender and peer != self:                 send(peer, msg, sender=se...
  c04_b09.md: ...!= sender and peer != self:                 send(peer, msg, sender=self)      def on_receive(msg, sender):         flood(msg,...
  c04_b09.md: ...send(peer, msg, sender=self)      def on_receive(msg, sender):         flood(msg, sender)   四节点示例：P1、P2、P3、P4。P1 广播 m。...
  c04_b09.md: ...=self)      def on_receive(msg, sender):         flood(msg, sender)   四节点示例：P1、P2、P3、P4。P1 广播 m。  * P1 将 m 发给 P2、P3、P4。 * P2 收...

=== split (17 occurrences) ===
  c05_b10.md: ...遗留反模式。”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，按构造避免 split-brain。延迟成本真实但有界；对大多数工作负载可接受。  反驳：异步复制仍适合许多工作负载。偶尔丢失可接受的日志流水...
  c05_b10.md: ...副本、写可去任意副本并后台传播的系统。指出三种失败模式，并解释为什么这并非真正高可用。  **讨论：**  (a) **Split-brain 写。** 两个写者可向不同副本写冲突值。没有协调者排序，没有法定人数强制相交。副本分歧；冲突解决（如有）会...
  c05_b10.md: ...遗留反模式。”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，按构造避免 split-brain。延迟成本真实但有界；对大多数工作负载可接受。  反驳：异步复制仍适合许多工作负载。偶尔丢失可接受的日志流水...
  c05_b10.md: ...副本、写可去任意副本并后台传播的系统。指出三种失败模式，并解释为什么这并非真正高可用。  **讨论：**  (a) **Split-brain 写。** 两个写者可向不同副本写冲突值。没有协调者排序，没有法定人数强制相交。副本分歧；冲突解决（如有）会...
  c08_b11.md: ...(e) **重配置。** 增减节点需要协议层谨慎协调。LLM 生成的代码通常根本不实现重配置，或以允许 split-brain 的天真方式实现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 pro...
  c11_b02.md: ...通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通...
  c11_b02.md: ...。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader...
  c11_b02.md: ...leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需要人工提升副本。RTO 以小时计。  故障恢复时间决...
  c11_b02.md: ...通过 gossip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通...
  c11_b02.md: ...。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader...
  c11_b02.md: ...leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需要人工提升副本。RTO 以小时计。  故障恢复时间决...
  c27_b09.md: ...(d) 可变条件下的长时间运行测试。  单元测试遗漏的 bug：  * 网络抖动 + 重试下的重复扣款。 * 分区下的 split-brain。 * 领导者 failover 下的丢失写。 * 网络恢复后的 stale read。 * 负载下的级联故...
  c27_b15.md: ...生成分布式代码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故障场景下可见。成本高：重复扣款、丢失写、split-brain、级联故障。  审查清单（10 项）：幂等性、超时、带预算重试、熔断器、排序原语（HLC 而非 wall c...
  c28_b14.md: ...*：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-brain 可能需要人工干预。 * **一致性模型**：事务内快照隔离；副本读最终一致。 * **分区行为**：CP—...
  c30_b06.md: ...* **AI/human review split (60/40)**：2026 年 AI 生成代码审查职责的粗略划分。AI 捕获约 60%（语法模式、习语不匹配、测试覆...
  c30_b14.md: ...ated key-value store on top     │   └── tests/            — split-brain, partition, recovery scenarios     ├── ch12_lsm/...
  c30_b19.md: ...h.5 §5.3，Ch.5 §5.4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5...

=== your (17 occurrences) ===
  c05_b04.md: ...在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数...
  c05_b04.md: ...ssion 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到比先前读...
  c05_b09.md: ...支持；应用层封装可实现。或切换到原生支持的 CockroachDB/YugabyteDB。  (c) **把 read-your-writes 流量路由到主副本** ：若应用知道某键的“主”节点（Cassandra 有自然副本顺序；顺序中第一个常是...
  c05_b09.md: ..._DC” 或类似协调者感知模式能做到。  通用教训：quorum 读/写规则给的是相交，不是线性一致性。需要 read-your-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-wri...
  c05_b09.md: ...ead-your-writes 的应用必须显式实现它。  **Q3.** 为一个客户端跟踪 HLC 的系统写 read-your-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max-HLC-...
  c05_b09.md: ...QL 连接会话状态的一部分自动传播；应用无需显式跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c11_b02.md: ...ckroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。 * **Eventual**...
  c15_b01.md: ...Theimer、Petersen、Demers、Spreitzer、Hauser 1994）：  (a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读不会看到...
  c15_b03.md: ...DB  默认读最终一致：读返回最近成功复制的值，但复制滞后可能意味着稍旧的值。区域内滞后通常亚秒。  对需要 read-your-writes 的操作，客户端可请求 `ConsistentRead=true`，对 leader 做同步读。代价：通常...
  c15_b03.md: ...：按需求为每次操作选择一致性级别。许多 Cassandra 生产故障可追溯为使用错误的一致性级别（例如，需要 read-your-writes 时却用 ONE 读）。   #### Redis 复制  主从复制异步。从副本读可能过期。Master...
  c15_b06.md: ...#### 概念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes...
  c15_b06.md: ...w-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写。重要场景：用户期望自己的动作立即反映。示例：客户更新资料；下一页显示新资料。...
  c17_b01.md: ...fore 操作 B，则每个进程都先看到 A 再看到 B。并发操作可任意顺序。  为何有用：  * 会话内 **read-your-writes** 自然满足（自己的写 happens-before 后续读）。 * **因果性保持** ：对消息的回复...
  c17_b04.md: ...致允许 “我看到 A 的分数，然后刷新排行榜却没有它” 的失败。B 提交分数后刷新，可能读到没有 B 的副本。Read-your-writes 是因果一致的特例。  排行榜位于频谱中间。因果是正确模型。  **Q3.** 强最终与纯最终一致有何不同...
  c17_b07.md: ...C 和 D 同时发帖且未看到对方，观众可看到不同顺序。这没问题；没人关心同时发的帖子的精确顺序。  (c) Read-your-writes：用户发的消息应在下次查看会话时看到。  为何不用线性化：  * 线性化要求每条消息全局排序；聊天服务会在...
  c17_b07.md: ...100ms 共识。  为何不用纯最终：  * 用户可能发帖后下次查看却没看到（若读打到未收到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时携带其 H...
  c30_b03.md: ...., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，作为 2027-2...

=== replication (17 occurrences) ===
  c05_b05.md: ...=0 且多区域容错。训练技能：按操作的 RPO 需求选择复制模式，而非按系统默认。  System | Default replication | Configuration options ---|---|--- PostgreSQL | Async（一个或多...
  c09_b01.md: ...个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **Safety** ：确保一...
  c09_b06.md: ...** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。...
  c09_b06.md: ...并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目...
  c11_b01.md: ...为每家厂商文档中都有确定答案。   ### 11.2 方法详述   #### 问题 1：复制协议  阅读系统文档中的 “replication”、“consensus”、“consistency” 或 “high availability”。寻找：  * **S...
  c11_b03.md: ...统的一页阅读笔记。   #### Spanner（Google）  Question | Answer ---|--- Replication protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wa...
  c11_b03.md: ...Google 自身基础设施   #### CockroachDB  Question | Answer ---|--- Replication protocol | 每 range Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 m...
  c11_b04.md: ...#### DynamoDB（AWS）  Question | Answer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Failure-recovery | 透明（存储...
  c11_b04.md: ...键值工作负载；AWS 原生系统   #### Cassandra  Question | Answer ---|--- Replication protocol | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recove...
  c11_b04.md: ...求每节点简单的大规模系统   #### FoundationDB  Question | Answer ---|--- Replication protocol | 自定义（sequencer + transaction-log + storage）；强一致 F...
  c11_b05.md: ...同样方法用于她正在开发的系统。阅读结果：  Question | Answer（Stratus DB） ---|--- Replication protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-...
  c11_b08.md: ...性和分区行为。  **答案：** TigerBeetle 阅读：  Question | Answer ---|--- Replication protocol | 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序...
  c11_b08.md: ...ion | Answer ---|--- Replication protocol | 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 view-change；面向确...
  c11_b09.md: ...特征。  **答案：** 两者诊断画像相同（共享协议）：  Question | Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超...
  c14_b03.md: ...igerBeetle  领域特定：专为金融复式记账优化的数据库。  关键属性：  * 5 副本 Viewstamped Replication 共识。 * 确定性模拟测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，沿袭 Tyler Nee...
  c14_b07.md: ...兼容。  **答案：**  Question | Spanner | CockroachDB ---|---|---- Replication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery |...
  c28_b04.md: ...Beetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replication 在 30 ms 内提交。  (9) 事务在 CockroachDB 中提交：更新 Adaeze 余额（US-East...

=== memory (17 occurrences) ===
  c06_b03.md: ...RPC 延迟相近（差几微秒）。网络局部性不如应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实现：  * *...
  c11_b09.md: ...-|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~...
  c24_b01.md: ...ecode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) **KV cache 作为中心状态**。KV...
  c24_b08.md: ...aggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Prefill 节点批量处理大 prompt；dec...
  c24_b11.md: ...架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregat...
  c25_b01.md: ...decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。...
  c25_b01.md: ...Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul...
  c25_b01.md: ...-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可...
  c25_b01.md: ...decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。...
  c25_b01.md: ...Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul...
  c25_b01.md: ...-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可...
  c25_b02.md: ...│  (8x H200)   │  │  (8x H200)   │                 │  Memory BW   │  │  Memory BW   │                 │  optimized   │...
  c25_b02.md: ...│  │  (8x H200)   │                 │  Memory BW   │  │  Memory BW   │                 │  optimized   │  │  optimized   │...
  c25_b05.md: ...性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算...
  c25_b11.md: ...是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优硬件；相同延迟下吞吐提升 2-4 倍。  Spli...
  c30_b03.md: ...* Kwon, W., Li, Z., Zhuang, S., et al. (2023). "Efficient Memory Management for Large Language Model Serving with PagedAtten...
  c30_b14.md: ...)     │   ├── choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_saga.py — the Ch.13...

=== narwhal (17 occurrences) ===
  c07_b05.md: ...Tendermint**（Cosmos 链）用显式轮次超时；生产 1 秒一轮。 * **Aptos / Sui** 用 Narwhal-Bullshark（基于 DAG）获得比经典 BFT 更高吞吐；轮复杂度与消息延迟解耦。  这些系统都在 _任何条件下...
  c10_b01.md: ...节点主动作恶，协议仍正确工作。  本章覆盖现代 BFT 协议：HotStuff（2026 年所有 BFT 系统的基础）、Narwhal-Bullshark（DAG mempool 与共识分离）、Tendermint（许多区块链生态的工作马）、Flexib...
  c10_b01.md: ...["Mysticeti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"...
  c10_b01.md: ...ti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]...
  c10_b01.md: ...Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]...
  c10_b01.md: ...22<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]         style BG fill:#f9f9f9...
  c10_b01.md: ...l:#f9f9f9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport、Shostak...
  c10_b03.md: ...Certificates，外加 pipelining 提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的 mempool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪...
  c10_b03.md: ...行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识...
  c10_b04.md: ...### 10.4 Narwhal-Bullshark：基于 DAG 的共识  2022 年 Narwhal 论文（Danezis、Kokoris-Kog...
  c10_b04.md: ...### 10.4 Narwhal-Bullshark：基于 DAG 的共识  2022 年 Narwhal 论文（Danezis、Kokoris-Kogias、Sonnino、Spiegelman）做出结构性创新：把 _mem...
  c10_b04.md: ...pool_（在交易到达共识前对它们排序的层）与 _consensus_（决定提交哪些 mempool 块的层）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名...
  c10_b04.md: ...轻了 leader 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 b...
  c10_b04.md: ...中：  * **Aptos** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运...
  c10_b04.md: ...024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff。  Narwhal-Bullshark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚秒级提交）。复杂度在...
  c10_b12.md: ...ificates 和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint...
  c30_b02.md: ...Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus."...

=== sstable (17 occurrences) ===
  c12_b03.md: ...g（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtable 满时，被刷写成磁盘上的不可变有序文件。新写开始填充新...
  c12_b03.md: ...刷写成磁盘上的不可变有序文件。新写开始填充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收空间（删除键被丢弃）、减少读时要检查的文件数，并保持磁盘结构有序。...
  c12_b03.md: ...充新 memtable。  (d) **Compaction** ：定期把多个 SSTable 合并成数量更少、更大的 SSTable。这回收空间（删除键被丢弃）、减少读时要检查的文件数，并保持磁盘结构有序。   #### 为什么 LSM 在写重工作负载...
  c12_b03.md: ...胜（通常持续写快 10-100 倍）。 * **读延迟** ：B-tree 点查胜（读一页即可；LSM 可能需检查多个 SSTable 和 memtable，Bloom filter 有帮助）。 * **范围扫描** ：可比拟；LSM 的有序合并与 B-...
  c12_b03.md: ...tree 的叶遍历类似。 * **空间放大** ：LSM 可能有 1.5-3 倍空间开销（compaction 前多个 SSTable 共存）；B-tree 接近 1 倍。  2026 年，LSM 主导分布式数据库存储底层，因为大多数工作负载写重或写吞吐...
  c12_b03.md: ...主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：...
  c12_b03.md: ...* **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的...
  c12_b04.md: ...简化。LSM 的读性能对许多工作负载可与 B-tree 竞争（带 Bloom filter 的点查；范围扫描受益于有序 SSTable）。真正区别在写路径：LSM 把随机写转化为顺序写（通过 WAL 和 memtable 批量），这是 B-tree 无法...
  c12_b04.md: ...ot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft...
  c12_b04.md: ...t 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通...
  c12_b06.md: ...顺序写。   2. 写插入 memtable（内存有序结构）。这很快。   3. memtable 满时，刷盘成不可变 SSTable。   4. 定期通过 compaction 合并多个 SSTable。  用户可见写在第 1、2 步后完成；后续 co...
  c12_b06.md: ...3. memtable 满时，刷盘成不可变 SSTable。   4. 定期通过 compaction 合并多个 SSTable。  用户可见写在第 1、2 步后完成；后续 compaction 异步。  B+ tree 写路径：    1. 写必...
  c12_b07.md: ...(a) LSM 写是 _顺序_ 的（WAL 追加；把整个 memtable 刷成新 SSTable）。传统存储上顺序写比随机写快 10-100 倍；在 SSD 上也快（但差距没这么大）。  (b) LSM 批量磁盘 I...
  c12_b07.md: ...高写吞吐，LSM 结构上优于 B+ tree。点查方面，B+ tree 延迟更低（一次 I/O）；LSM 需要检查多个 SSTable（Bloom filter 有帮助）。  **Q4.** 为什么 Cassandra 默认每个物理节点 256 个 vn...
  c12_b09.md: ...% 写工作负载下表现好。 * 读延迟：每次读可能 touch memtable + Bloom filter + 多个 SSTable。冷读 p99 可能 1-5 ms。 * 空间放大：典型 1.5-2 倍（compaction 前多个 SSTable）...
  c12_b09.md: ...SSTable。冷读 p99 可能 1-5 ms。 * 空间放大：典型 1.5-2 倍（compaction 前多个 SSTable）。 * Compaction 开销：周期后台 CPU 和 I/O 突增。  SQLite 画像：  * 写吞吐：中等。...
  c12_b11.md: ...LSM-tree 是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ tree；B+ tree 在读重单节点 OLTP 中仍...

=== monkey (17 occurrences) ===
  c22_b11.md: ...对变化率告警与对绝对值告警同等重要。  第23章讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide...
  c23_b01.md: ...：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026 年已成熟为标准 SRE 实践，包含组织和技术组件。   ### 23.1 什么是混沌工程  混沌工程：故意向...
  c23_b01.md: ...*最小化爆炸半径**。从小处开始（一个节点、一个 shard）；建立信心后再扩大。  Netflix 起源：Chaos Monkey（随机 kill 生产实例的工具）约 2010 年引入。Netflix 的理由：实例反正会在生产环境中失败，不如在工程师...
  c23_b02.md: ...day 不是一次性事件。每月或每季度节奏保持肌肉锻炼；新成员通过参与学习。  组织实践才是区分 “我们用 Chaos Monkey” 与 “我们有混沌工程学科” 的关键。工具容易；文化难。   ### 23.4 工具  现代混沌工程工具：  (a)...
  c23_b05.md: ...次 game day 是 Level 2。  (d) **Level 3（持续混沌）**：生产中的自动混沌。Chaos Monkey 风格：工作时间持续随机注入故障。系统一直在被测试。  (e) **Level 4（混沌作为设计输入）**：混沌发现驱动...
  c23_b06.md: ...依赖图）。实验室测试必要但不充分。  (d) **自动化实验以持续运行**。手动 game day 有价值；Chaos Monkey 风格的持续自动混沌让系统永久被测试。  (e) **最小化爆炸半径**。从小处开始（一个节点、一个 shard）；建立...
  c23_b09.md: ...的（几天高级工程师时间），但替代方案（在真实事故中发现这些问题）成本更高。  **Q2.** 一个团队运行 Chaos Monkey 两年了。他们没有发生重大中断。他们是否 “完成” 了混沌工程？  提示  Chaos Monkey 随机 kill 实...
  c23_b09.md: ...队运行 Chaos Monkey 两年了。他们没有发生重大中断。他们是否 “完成” 了混沌工程？  提示  Chaos Monkey 随机 kill 实例。它测试一种故障模式；系统可能还有许多其他。一个层面的持续混沌不等于完整学科。  **答案：**...
  c23_b09.md: ...实例。它测试一种故障模式；系统可能还有许多其他。一个层面的持续混沌不等于完整学科。  **答案：** 没有。Chaos Monkey 测试一种故障模式（随机实例终止）。系统还有许多其他 Chaos Monkey 不测试的故障模式：  (a) **网络分...
  c23_b09.md: ...科。  **答案：** 没有。Chaos Monkey 测试一种故障模式（随机实例终止）。系统还有许多其他 Chaos Monkey 不测试的故障模式：  (a) **网络分区**：节点仍在运行但彼此隔离。Chaos Monkey kill 节点（网络...
  c23_b09.md: ...许多其他 Chaos Monkey 不测试的故障模式：  (a) **网络分区**：节点仍在运行但彼此隔离。Chaos Monkey kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos Monkey...
  c23_b09.md: ...Monkey kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机 kill 单个...
  c23_b09.md: ...着但降级。Chaos Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机 kill 单个节点，不是协调的区域事件。  (d) **级联依赖**：服务 X 故障导致服务 Y 问题。Chao...
  c23_b09.md: ...机 kill 单个节点，不是协调的区域事件。  (d) **级联依赖**：服务 X 故障导致服务 Y 问题。Chaos Monkey 测试实例故障；级联关于传播。  (e) **时钟偏移**：节点活着但时钟错误。专门故障。...
  c23_b10.md: ...配置变更**：部署、schema 变更、feature flag。本身不是故障，但与系统交互的运营事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性...
  c23_b10.md: ...事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于...
  c23_b10.md: ...s Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey 两年没有重大中断的团队只建立了一个韧性切片。他们应扩展：增加网络分区混沌、慢节点混沌、为区域故障运行 game day...

=== kleppmann (16 occurrences) ===
  c01_b04.md: ...论文的综述。它不是任何单一数据库的重新实现教程。它不是 Kubernetes 书，也不是区块链书。基础材料的参考读物是 Kleppmann 的 _Designing Data-Intensive Applications_ ；如果你还没读过，可以先读这本，或...
  c01_b05.md: ...写放大优化、页缓存行为、fsync 顺序。我们会在影响分布式推理的地方触及这些（第12章），但不会展开单节点深度。参考：Kleppmann 的 _DDIA_ 第3-4章（基础）；RocksDB 调优指南（运维深度）；Pavlo & Aslett 的 CMU...
  c01_b11.md: ...人接管。”指出该设计未处理的三种失败模式。需要时可参考第18章。  提示  该模式是 Redlock。Jepsen / Kleppmann 辩论至少指出三种失败模式。想想 GC 暂停、到 Redis 的网络分区、以及旧领导者醒来时会发生什么。  **答案：*...
  c16_b05.md: ...rst Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (...
  c16_b11.md: ...理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Software——Kleppmann 等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear...
  c18_b01.md: ...lock 模式。代码很短。生产环境中，它偶尔会允许两个领导者同时行动。原因是分布式系统中最深刻的论证之一：Martin Kleppmann 对 Redlock 的分析以及 _fencing token_ 的作用。  本章讲授两个相关原语：分布式缓存（具体是...
  c18_b03.md: ...#### Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locki...
  c18_b03.md: ...#### Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它为何没有提供表面上的...
  c18_b06.md: ...#### 概念检测  **Q1.** 描述 Redlock 模式。根据 Kleppmann 的批评，它为什么不提供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程...
  c18_b06.md: ...5）在超时内成功，则获得锁。锁有 TTL；客户端通过删除 key 释放（需验证 value 与 nonce 匹配）。  Kleppmann 的批评：Redlock 不提供正确性，因为：  (a) _进程暂停_ ：客户端获得锁后暂停（GC、VM 暂停）。从 R...
  c18_b06.md: ...只有 token 更高者能行动。  Redlock vs fencing token 的争论是分布式锁正确性的经典读物。Kleppmann 的博客文章是必读。  **Q2.** 什么是 fencing token，它如何修复分布式锁正确性问题？  提示  单...
  c30_b01.md: ...清单为工作工程师精选，并非穷尽；每个主题只包含最直接相关的来源。   #### 基础（第一部分 — 第1-3章）  * Kleppmann, M. (2017). _Designing Data-Intensive Applications_. O'Reil...
  c30_b02.md: ..."Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M., & Beresford, A. R. (2017). "A Conflict-Free Replicated...
  c30_b02.md: ...e Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (20...
  c30_b03.md: ...#### 协调与运维（第六、七部分 — 第18-23章）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对...
  c30_b03.md: ...微秒级有界时间。 — TrueTime 风格有界时钟正离开 Google、进入公有云基线的最强信号（§29.1）。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (20...

=== post (16 occurrences) ===
  c01_b10.md: ...部分失败是不完整且静默的。部分工作已发生；部分没有；分界至少对一个参与者不可观测。示例：客户端向服务发送 HTTP POST。服务收到请求，执行了副作用（信用卡扣款），但在发送响应前崩溃。客户端超时、重试，重启后的服务收到重复请求。从客户端视角...
  c01_b11.md: ...题  **Q1.** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔 47 ms 到达支付服务的两个不同副本。两者都成功了。走一遍三个谎言，...
  c05_b01.md: ...ity after ack** ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Post-failover consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **...
  c05_b02.md: ...客户端确认。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。...
  c05_b02.md: ...本异步接收。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失...
  c20_b01.md: ...Crescent 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network in + TLS<b...
  c20_b01.md: ...L["ledger write<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network ou...
  c20_b01.md: ...L["ledger write<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network out)"]...
  c20_b01.md: ...R --> F         R --> DB         R --> L         R --> POST         DB --> DB1["read balance<br/>8 ms"]         DB -->...
  c20_b01.md: ...Crescent 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network in + TLS<b...
  c20_b01.md: ...L["ledger write<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network ou...
  c20_b01.md: ...L["ledger write<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network out)"]...
  c20_b01.md: ...R --> F         R --> DB         R --> L         R --> POST         DB --> DB1["read balance<br/>8 ms"]         DB -->...
  c29_b04.md: ...动作相同。  去构建系统。当本书过时时，写下下一本。  * * *  _第29章（后记）于 2026-05-20 作为 post-pipeline 润色的一部分添加。_ _前瞻是故意预测；2028+ 读者被邀请评估哪些部分老得好。_ 这篇后记是预测...
  c30_b03.md: ...Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P.,...
  c30_b19.md: ...这个模式叫什么？” 的调试工程师使用。章节指针是入口；章节的防御是修复。  * * *  _2026-05-20 作为 post-pipeline 润色的一部分生成。_ _QBooks · A Socratopia Learning Series...

=== call (16 occurrences) ===
  c02_b09.md: ...要多项目事务（例如原子地减量库存并增加用户购买记录），Spanner 是更干净的答案。真实团队还会考虑运维成熟度、on-call 经验，以及公司其余部分已经运行什么。...
  c02_b11.md: ...nner 做库存）能在每个工作负载上达到更高峰值吞吐，代价是运维复杂度、跨系统一致性问题，以及三套独立可观测性和 on-call 手册的工程成本。正确答案取决于规模、团队规模和应用跨工作负载的程度。  **Q4.** 挑选你栈中的一个系统，为它写一...
  c06_b11.md: ...本？五个有意义规模的区域成本约是一个区域的五倍。边际可用性很少值 5 倍成本。 * 运维成熟度？每个区域需要监控、on-call、部署流水线。三个工程师运维五个区域可能比运维一个区域出更多事故，无论理论可用性优势如何。...
  c12_b01.md: ...Anya 盯着团队 on-call 发来的 Slack 消息：“Stratus 集群 7 磁盘使用率 87%，每天增长 2%。下周末前必须扩容。”  集群...
  c13_b07.md: ...ymentOrchestrator (CockroachDB-backed state):       step 1: call ReservationService.reserve(user, amount)       step 2: call...
  c13_b07.md: ...call ReservationService.reserve(user, amount)       step 2: call FraudService.check(transaction)         → score       step...
  c13_b07.md: ...audService.check(transaction)         → score       step 3: call BankSettlement.submit(transaction)      → bank_ref       st...
  c13_b07.md: ...ettlement.submit(transaction)      → bank_ref       step 4: call LedgerService.record(transaction, bank_ref)       step 5: c...
  c13_b07.md: ...l LedgerService.record(transaction, bank_ref)       step 5: call NotificationService.send(user, "completed")       step 6: c...
  c13_b07.md: ...l NotificationService.send(user, "completed")       step 6: call AnalyticsService.update(merchant, day)       on failure at...
  c21_b08.md: ...**缺乏可观测性**：监控系统（FirstEnergy 的）有 bug；运维看不到故障。等价：团队监控面板过期；on-call 几分钟内没看到数据库变慢。  (c) **放大**：电力重新分配到剩余线路；每条线路现在更接近容量。等价：慢数据库的调...
  c21_b09.md: ...6 倍负载。 * 退避（2^attempt 秒）让每个调用变慢（完全重试的调用耗时 31 秒）。 * 组合产生 per-call 延迟和下游放大；两者都坏。  正确答案结合多种技术：  (a) **调用者处的重试预算**：把重试限制为成功流量的 1...
  c21_b09.md: ...*：当下游失败时，停止调用。快速失败而非缓慢失败。  (c) **超时策略**：基于下游预期 p99 延迟设置 per-call 超时；不要超时过长。  (d) **幂等性**：确保重试安全（同一操作可多次执行而无坏效果）。  (e) **Fall...
  c22_b01.md: ...凌晨 2:17，on-call 告警响起：“面向客户的错误率飙升到 14%。” Olamide 打开监控面板。每个节点都是绿色。CPU 正常。内存正常...
  c23_b02.md: ...而非个人错误。责备产生防御行为；系统改进需要诚实分析。  (c) **多元参与**。Game day 不仅应包括 on-call 工程师，还应包括产品经理、客服代表、业务利益相关者。每个角色对 “系统崩溃意味着什么” 有不同视角。  (d) **记...
  c23_b04.md: ...（1 人）、产品经理（1 人）。  **当天**：  * 14:00 UTC：团队集合；回顾回滚计划；观察员进入 on-call 室。 * 14:10 UTC：实验开始。Olamide 对 EU-West 节点调用 CockroachDB “dra...

=== event (16 occurrences) ===
  c03_b03.md: ...#### 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local even...
  c03_b03.md: ...vent | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 |...
  c03_b03.md: ...2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | se...
  c03_b03.md: ...都不成立，则事件并发。   #### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P...
  c03_b03.md: ...#### 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | local even...
  c03_b03.md: ...vent | Local counter after |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 |...
  c03_b03.md: ...2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | se...
  c03_b03.md: ...都不成立，则事件并发。   #### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P...
  c13_b07.md: ...or）：       OrderService            → publishes OrderCreated event     PaymentService          listens to OrderCreated      →...
  c18_b01.md: ...(d) **TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取...
  c18_b01.md: ...：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺...
  c18_b01.md: ...(d) **TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取...
  c18_b01.md: ...：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺...
  c20_b10.md: ...服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace、客户投诉 trace）是...
  c30_b11.md: ...**：Kafka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor*...
  c30_b14.md: ...├── choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_saga.py — the Ch.13 §13.6....

=== send (16 occurrences) ===
  c03_b03.md: ...fter |---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4...
  c03_b03.md: ...nt x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | send msg n to P_3 (carries 4) | 4 6 | P_3 | receive n | \max(1,...
  c03_b03.md: ...手）会一致地让 a < x 或 x < a。  P_3 接收 n 的时间戳为 5。P_1 发送 m 的时间戳为 2。链 send(m) \to recv(m) \to send(n) \to recv(n) 反映为递增的时间戳 (2, 3, 4,...
  c03_b03.md: ...P_3 接收 n 的时间戳为 5。P_1 发送 m 的时间戳为 2。链 send(m) \to recv(m) \to send(n) \to recv(n) 反映为递增的时间戳 (2, 3, 4, 5)。   ### 3.4 向量时钟  Lamp...
  c03_b03.md: ...ctor after |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P_1 | send m, carries [2,0,0] | [2,0,0] 3 | P_3 | x | [0,0,1] 4 | P_2...
  c03_b03.md: ...3 | P_3 | x | [0,0,1] 4 | P_2 | recv m | [2,1,0] 5 | P_2 | send n, carries [2,2,0] | [2,2,0] 6 | P_3 | recv n | [2,2,2]  现在...
  c03_b03.md: ...parallel x（并发）。V(send m) = [2,0,0] < V(recv n) = [2,2,2]，所以 send(m) \to recv(n)，与链一致。   #### 代价  向量时钟随进程数扩展。10 节点系统每个消息携带 10...
  c03_b13.md: ....L += 1             return self.L, description          def send(self, dest, msg):             self.L += 1             # 消息携...
  c03_b13.md: ...6 | 8 | P3 本地事件 y | 3 | 5 | 7 | y 的 L = 7  因果链：a \to b \to send\,m \to recv\,m \to send\,n \to recv\,n \to y。Lamport 时间戳沿因果...
  c03_b13.md: ...5 | 7 | y 的 L = 7  因果链：a \to b \to send\,m \to recv\,m \to send\,n \to recv\,n \to y。Lamport 时间戳沿因果路径形成单调链：1, 2, 3, 4, 5, 6...
  c04_b03.md: ...adcast(msg):         for peer in all_processes:             send(peer, msg)      on receive(msg, sender):         if msg not...
  c04_b03.md: ...msg)             for peer in all_processes:                 send(peer, ACK(msg))      on receive(ACK(msg), sender):...
  c13_b07.md: ...ansaction, bank_ref)       step 5: call NotificationService.send(user, "completed")       step 6: call AnalyticsService.upda...
  c13_b07.md: ...saction, bank_ref)         L-->>O: ledger_id         O->>N: send confirmation         N-->>O: sent         O->>A: update mer...
  c20_b08.md: ...t inject      headers = {}     inject(headers)     producer.send(topic, value=message, headers=headers)   `inject` 函数按 W3C 标...
  c20_b08.md: ...extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；trace 树跨越 Kafka 边界。  OpenTelemetry 有许多 Kafka 客...

=== uniform (16 occurrences) ===
  c04_b05.md: ...N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 be...
  c04_b05.md: ...4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用...
  c04_b05.md: ...B -->|"+ uniformity<br/>(ack round before deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vect...
  c04_b05.md: ...+ uniformity (ack round before delivery)         v     Uniform reliable broadcast         |         + causal order (vector...
  c04_b05.md: ...受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺...
  c04_b05.md: ...N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 be...
  c04_b05.md: ...4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（= 共识）。训练的技能：永远不要为了安全默认选 atomic；先识别应用...
  c04_b05.md: ...B -->|"+ uniformity<br/>(ack round before deliver)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vect...
  c04_b05.md: ...+ uniformity (ack round before delivery)         v     Uniform reliable broadcast         |         + causal order (vector...
  c04_b05.md: ...受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分布式状态机复制 | atomic | 每个副本必须执行相同命令且顺...
  c04_b07.md: ...Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal ord...
  c04_b07.md: ...e broadcast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform reliable broadcast** ：新增 _uniformity_ ——若任何进程（正确或故障）交付，所有正确...
  c04_b07.md: ...Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal ord...
  c04_b07.md: ...e broadcast** ：新增 _agreement_ ——若任一正确进程交付，所有正确进程都交付。   3. **Uniform reliable broadcast** ：新增 _uniformity_ ——若任何进程（正确或故障）交付，所有正确...
  c12_b10.md: ...匀）。  (e) **Vnode 数量边缘情况。** 物理节点容量不同时，vnode 数应成比例。LLM 代码通常假设 uniform vnode 数。  (f) **Token 分配持久化。** 生产系统将 token-to-node 映射持久化（在...
  c25_b06.md: ...度在更多节点上摊销。 * 团队规模足以 justify 专门平台工程师做分离逻辑。  阈值与工作负载相关：50 节点但 uniform 短 prompt 工作负载的集群可能不受益（prefill 成本相对 decode 小）。30 节点但重长 promp...

=== sibling (16 occurrences) ===
  c05_b03.md: ...（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **Sibling values** 保留到应用解决。Riak 的方法。应用看到所有冲突值并选择（或合并）。 * **CRDT**（第16...
  c15_b02.md: ...ins（LWW）** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。  (b) **Sibling values** ：系统保留所有冲突值；应用看到它们并选择（或合并）。Riak 的做法。需要应用逻辑，但从不静默丢失数...
  c15_b02.md: ...深入讲解。  正确策略取决于工作负载：  * 应用有明确 “赢家” 语义的数据：LWW。 * 应用能推理所有候选的数据：sibling values。 * 具有数学结构的数据（计数器、集合、寄存器）：CRDT。  LWW 是最常见默认因为它最简单。代价是...
  c15_b02.md: ...ins（LWW）** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。  (b) **Sibling values** ：系统保留所有冲突值；应用看到它们并选择（或合并）。Riak 的做法。需要应用逻辑，但从不静默丢失数...
  c15_b02.md: ...深入讲解。  正确策略取决于工作负载：  * 应用有明确 “赢家” 语义的数据：LWW。 * 应用能推理所有候选的数据：sibling values。 * 具有数学结构的数据（计数器、集合、寄存器）：CRDT。  LWW 是最常见默认因为它最简单。代价是...
  c15_b07.md: ...可接受的数据（如资料字段最后更新），LWW 没问题。对 “丢失” 不可接受的数据（如交易日志），LWW 错误；应用需要 sibling values 或 CRDT。...
  c17_b08.md: ...chema 设计纪律。“清空购物车” 或 “设置数量” 等操作若仍用单个购物车状态行仍会冲突。  _Riak with sibling values_ ：两个写冲突时，两个值都作为 sibling 保留。应用看到 `[{cart: [X]}, {cart...
  c17_b08.md: ...若仍用单个购物车状态行仍会冲突。  _Riak with sibling values_ ：两个写冲突时，两个值都作为 sibling 保留。应用看到 `[{cart: [X]}, {cart: [Y]}]` 并决定。标准购物车合并是 “物品并集”，由应...
  c17_b08.md: ...车合并是 “物品并集”，由应用实现（Riak 不自动合并）。  Riak 的方法干净地处理购物车工作负载：并发添加作为 sibling 保留；应用的合并函数取并集。  _Riak 的 CRDT bucket types_（较新的 Riak 特性）：购物车...
  c17_b08.md: ...ypes_（较新的 Riak 特性）：购物车可建模为 OR-Set CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用...
  c17_b08.md: ...et CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set...
  c17_b08.md: ...chema 设计纪律。“清空购物车” 或 “设置数量” 等操作若仍用单个购物车状态行仍会冲突。  _Riak with sibling values_ ：两个写冲突时，两个值都作为 sibling 保留。应用看到 `[{cart: [X]}, {cart...
  c17_b08.md: ...若仍用单个购物车状态行仍会冲突。  _Riak with sibling values_ ：两个写冲突时，两个值都作为 sibling 保留。应用看到 `[{cart: [X]}, {cart: [Y]}]` 并决定。标准购物车合并是 “物品并集”，由应...
  c17_b08.md: ...车合并是 “物品并集”，由应用实现（Riak 不自动合并）。  Riak 的方法干净地处理购物车工作负载：并发添加作为 sibling 保留；应用的合并函数取并集。  _Riak 的 CRDT bucket types_（较新的 Riak 特性）：购物车...
  c17_b08.md: ...ypes_（较新的 Riak 特性）：购物车可建模为 OR-Set CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用...
  c17_b08.md: ...et CRDT。Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set...

=== abort (16 occurrences) ===
  c07_b10.md: ...。  **讨论：** 2PC 有一个臭名昭著的阻塞失败模式：若协调者在发送 PREPARE 后、发送 COMMIT 或 ABORT 前崩溃，参与者就卡住。它们已投票（并持有锁），无法自行决定（不知道其他参与者投票），也不能中止（协调者可能在崩溃前已提...
  c08_b09.md: ...REPARE（或 VOTE_REQUEST）。若所有参与者投 yes，协调者发 COMMIT；若有任何 no，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在 PREPARE 后、发 COMMIT/ABORT 前崩溃，参与...
  c08_b09.md: ...o，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在 PREPARE 后、发 COMMIT/ABORT 前崩溃，参与者被阻塞。它们已投 yes（并持有锁），但不知道协调者的决定。它们不能单方面决定，因为协调者可能在崩溃前已...
  c13_b01.md: ...d（但未提交）状态。   3. 回复 `YES`（可提交）或 `NO`（必须中止）。  **阶段 2：Commit 或 Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finalize prepar...
  c13_b01.md: ...id)`；每个参与者 finalize prepared 数据。  若任一参与者投 NO（或协调者超时），协调者发送 `ABORT(txn_id)`；每个参与者丢弃 prepared 数据并释放锁。...
  c13_b02.md: ...#### 2PC 为何阻塞  经典失败模式：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已经投票（并持有锁）。他们无法自行决定（协调者可能在崩溃前已提交）。只能等待。锁被持有。冲突事务前向推进...
  c13_b12.md: ...两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发送 PREPAR...
  c13_b12.md: ...验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABOR...
  c13_b12.md: ...bort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 Y...
  c13_b12.md: ...（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修复：通过共识...
  c13_b12.md: ...两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prepare）：协调者向所有参与者发送 PREPAR...
  c13_b12.md: ...验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABOR...
  c13_b12.md: ...bort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 Y...
  c13_b12.md: ...（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修复：通过共识...
  c13_b13.md: ...按约定顺序执行事务。无冲突（顺序预先决定）。无因锁冲突导致的中止。  优点：无重试成本；确定性、可预测延迟；高争用下无 abort storm。  缺点：sequencer 是全局同步点；每笔事务付 sequencer 往返；sequencer 是跨...
  c13_b13.md: ...事务重启。  优点：无全局 sequencer；事务独立推进；低争用下吞吐很高。  缺点：争用导致重启；病态争用下产生 abort storm；高争用下延迟不可预测。  权衡：Calvin 赢在高争用工作负载（计数器递增、热行更新）；Cockroac...

=== retry (16 occurrences) ===
  c21_b05.md: ...；计算预算；抑制超额重试。重试在客户端侧被抑制；原始失败返回应用代码，由它决定高层重试还是向用户失败。  gRPC 的 retry policy 支持预算；Envoy 的 retry policy 也支持。这是生产级库的标准模式。  **Q3.**...
  c21_b05.md: ...失败返回应用代码，由它决定高层重试还是向用户失败。  gRPC 的 retry policy 支持预算；Envoy 的 retry policy 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。  提示  Clos...
  c21_b07.md: ...常见的放大机制（重试风暴）。修改每个重试感知库或 sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、HTTP 客户端自定义逻辑。  (b) **每对上下游之间加熔断器...
  c21_b07.md: ...试感知库或 sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、HTTP 客户端自定义逻辑。  (b) **每对上下游之间加熔断器**：尤其应用服务与共享基础设施（如数...
  c21_b07.md: ...**每个服务入口加 load shedding**：每个服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency li...
  c21_b09.md: ...应类似：       @circuit_breaker(threshold=0.5, timeout=30)     @retry(max_retries=3, budget=0.1, backoff=ExponentialBackoff(max=5...
  c27_b01.md: ...求在服务端已成功但响应丢失” 的情况。    2. **网络可靠**。Deutsch 的第一谬误。没有 timeout、retry、idempotency 的代码。    3. **机器时钟说真话**。跨机器比较时间戳的代码。    4. **朴素分...
  c27_b02.md: ...return profile_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fallback。代码假设调用最终会返回。  现实：网络会抖动。下游有时慢...
  c27_b02.md: ...有防御，这一行可能让调用服务无限挂起，促成级联故障，造成 p99 延迟灾难。  修复：每个网络调用应有 timeout、retry budget、circuit breaker，以及（若可能）fallback。       @circuit_brea...
  c27_b02.md: ...ack。       @circuit_breaker(threshold=0.5, timeout=30)     @retry(max_retries=3, budget=0.1)     @timeout(seconds=2)     def...
  c27_b06.md: ...查清单的 _模式识别_ 一半上可靠：  * **语法模式**：`def call_with_retry()` 函数没有 retry budget；`time.time()` 用在看起来像跨机器上下文的地方；`requests.get()` 没有 ti...
  c27_b07.md: ...使用 Greptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约...
  c27_b09.md: ...：代码假设响应表示请求结果；不处理 “服务端成功但响应丢失”。    2. **网络可靠**：代码没有 timeout、retry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall...
  c27_b09.md: ...()`。无幂等；重试造成重复工作。  （谎言 2）`result = http.get(url)`。无 timeout、retry、circuit breaker；挂起级联。  （谎言 3）`sorted(events, key=lambda e:...
  c30_b06.md: ...t**：Ongaro-Ousterhout 2014 共识协议。分解为 leader 选举、日志复制、安全性。 * **Retry budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping...
  c30_b14.md: ...ka     ├── ch21_resilience/     │   ├── retry_budget.py   — retry budget implementation (gRPC-style)     │   ├── circuit_brea...

=== disaggregated (16 occurrences) ===
  c24_b08.md: ...三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batc...
  c24_b08.md: ...有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bou...
  c24_b08.md: ...三种提高利用率的技术及预期影响。  提示  Continuous batching（若尚未）、前缀共享（SGLang）、disaggregated prefill/decode（第25章）。  **答案：** 三种技术：  (a) **Continuous batc...
  c24_b08.md: ...有共享前缀，把相关工作负载切到 SGLang 可减少浪费计算。受影响工作负载预期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bou...
  c24_b09.md: ...。  对 Continuum：continuous batching 可能已经就位（现代引擎都有）。下一步影响最大的是 disaggregated prefill/decode（第25章）。架构改动大，但收益（消除双峰延迟）是用户可见的改善。...
  c24_b11.md: ...美元、60% 利用率。优化空间巨大；每个百分点利用率约每月 2 万美元。  第25章进入 2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decod...
  c24_b11.md: ...ll 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流 12：vLLM 集群加 disaggregated prefill/decode + KV cache sharding 图。  ---...
  c25_b01.md: ...的集群 GPU 利用率为 60%。Continuum CTO 把目标定为 80%。2026 年实现这一目标的架构变化：disaggregated prefill/decode（prefill/decode 分离）。  这个洞察事后看很明显：prefill（处理 p...
  c25_b01.md: ...次优默认；把它们放到针对不同阶段优化的不同 GPU 上，可解锁显著收益。  本章是工作流 12：一个 vLLM 集群加 disaggregated prefill/decode，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经...
  c25_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“Disaggregated prefill/decode 是 2026 年任何生产推理部署的默认。Co-located 是遗留方案。”  **讨论...
  c25_b10.md: ...最便宜的答案，没有考虑质量权衡或架构替代方案。  完整答案包括：  (a) **架构优化**（不降低模型质量）：  * Disaggregated prefill/decode（第25章）：30-50% 成本降低。 * Continuous batching（第24...
  c25_b11.md: ...[列表]。你的集群在每个上处于什么位置？我们针对你的具体 setup 确定下一步最高杠杆。”   ### 本章总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-...
  c26_b04.md: ...26.7 Tobias 的完整优化  Continuum 集群 6 个月优化冲刺后的完整图景：  (a) **架构**：disaggregated prefill/decode + KV cache sharding（第25章）。  (b) **路由**：KV-ca...
  c26_b08.md: ...累尾部。  (d) **网络抖动或瞬态故障**：需要重试的请求更慢；若重试无预算，尾部可延长。  修复：  (a) **Disaggregated prefill/decode**（第25章）：若未就位，解决 prefill 阻塞 decode 问题。长 prefi...
  c30_b03.md: ...DI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。...
  c30_b06.md: ...通知）。自身必须幂等。 * **CRDT（无冲突复制数据类型）**：无论操作顺序如何都能确定性合并的数据结构。 * **Disaggregated prefill/decode（分离式 prefill/decode）**：2025-2026 年 LLM 推理模式；p...

=== weighted (16 occurrences) ===
  c26_b04.md: ...iority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **De...
  c26_b04.md: ...t”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客...
  c26_b04.md: ...ulative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用...
  c26_b04.md: ...iority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **De...
  c26_b04.md: ...t”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客...
  c26_b04.md: ...ulative decoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用...
  c26_b06.md: ...le、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：**...
  c26_b06.md: ...fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权...
  c26_b06.md: ...le、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：**...
  c26_b06.md: ...fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权...
  c26_b07.md: ...对 Continuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低...
  c26_b07.md: ...er 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低 tier 客户在峰值期间也有有界服务。  运维关注点：调度决策必须快（亚...
  c26_b08.md: ...code 时间从约 10 秒降到约 5 秒。  (e) **客户 SLA 调度**：该客户是 tier-2（中优先级）。Weighted fair scheduling 确保其请求被处理但不饿死 tier-1 客户。  综合：该客户每请求成本降低约 5 倍...
  c26_b11.md: ...2-3 倍吞吐。分布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使...
  c26_b11.md: ...r scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache s...
  c26_b11.md: ...g + KV-cache-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果：集群利用率 85%（从 60%）、每请...

=== data (15 occurrences) ===
  c01_b04.md: ...。它不是 Kubernetes 书，也不是区块链书。基础材料的参考读物是 Kleppmann 的 _Designing Data-Intensive Applications_ ；如果你还没读过，可以先读这本，或并行阅读。  本书是：2026 年给...
  c06_b05.md: ...TB 的系统，年费六位数。  架构含义：  * **复制 delta，而非全量。** 大多数跨区域复制是 change-data-capture，不是周期全量快照。 * **线上压缩。** 现代系统用 Zstd 等；对典型结构化数据可省 50-90...
  c16_b01.md: ...敛到一致状态，而无需冲突解决弹窗。  使这成为可能的数学是 CRDT（Conflict-free Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于部分子产品...
  c16_b01.md: ...突解决；实现小一个数量级。   ### 16.1 CRDT 是什么  Conflict-free Replicated Data Type 是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合并顺序如何都产生相同结果。“无冲突” 是结...
  c18_b11.md: ...配置：etcd 作为现代默认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data + watch stream 模式。这些是让其余分布式基础设施运行的元系统。  ---...
  c19_b01.md: ...6 年的答案是 etcd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch streams” 模式。   ### 19.1 发现问题  服务网格有若干实例。实例：  * 启...
  c19_b03.md: ...缩无状态服务），基于 watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch...
  c19_b05.md: ...cd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并...
  c19_b05.md: ...提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _w...
  c19_b11.md: ...原生工具。ZooKeeper 仍在生产中使用（现有 Kafka、Hadoop 等），但缓慢衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secre...
  c30_b01.md: ...#### 基础（第一部分 — 第1-3章）  * Kleppmann, M. (2017). _Designing Data-Intensive Applications_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanen...
  c30_b02.md: ...06). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick,...
  c30_b02.md: ...1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N., Baq...
  c30_b02.md: ...quero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M., & Beresford, A....
  c30_b03.md: ...McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，作为 2027-2030 主...

=== lwt (15 occurrences) ===
  c02_b10.md: ...用 `IF NOT EXISTS` 或 `IF` 子句的轻量事务（LWT）通过 Paxos 实现线性化。成本：远高于非 LWT 写入（常 4 到 10 倍），因为每次写入都发生 Paxos 往返。  对 Riak：启用后的强一致 bucket 是...
  c03_b11.md: ...SING TIMESTAMP` 的服务端时间戳。 * 对复活不可接受的数据，使用带 `IF` 子句的轻量事务（LWT）；LWT 使用 Paxos，提供线性化语义。代价：每次操作延迟增加一个数量级。 * 迁移到基于 HLC 的 MVCC 系统（Co...
  c03_b11.md: ...eDB），其冲突解决不依赖 wall clock。  对这个具体工作负载（三数据中心每秒 100k 写入），每次写入用 LWT 可能太贵；实际答案是要么严格 NTP 纪律加监控症状，要么迁移到写入路径不依赖 wall clock 正确性的系统。...
  c11_b04.md: ...；超时后排除节点；coordinator 故障需人工处理 Consistency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线性化 Partition behavior | 按操作可调；低一致性 AP，quoru...
  c11_b04.md: ...性扩展到数千节点；可调权衡 Weaknesses | 运维复杂（tombstone 管理、compaction 调优）；LWT 慢 Right for | 写重且可放宽一致性的工作负载；追求每节点简单的大规模系统   #### Foundatio...
  c11_b09.md: ...re-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低...
  c15_b08.md: ...n-level 路由支持。  (c) **轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。LWT 用 Paxos 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。...
  c15_b08.md: ...t_updated” 可用带 HLC 的 LWW），使用 CRDT 语义。对简单 LWW 字段（name、email），LWT 路径处理。  (e) **迁移到 CRDT 感知系统或 NewSQL** ：对新开发，选择默认提供正确一致性模型的系统...
  c17_b03.md: ...线性化）。 * **Cassandra** ：per-operation 一致性级别（ONE、QUORUM、ALL）；LWT 线性化。  优点：单一运维足迹；跨操作分析容易。缺点：需要 per-operation 纪律；难以通过系统规则强制 “...
  c17_b05.md: ...性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出 _相交_（第5章）：任何读与任何已提交写至少在一个副本上...
  c17_b05.md: ...“足够好” 的一致性（大多数数据库操作是单键，可容忍 LWW 解析）。对需要真正 per-key 线性化的工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，QUORUM 和 LWT 都不够；需要...
  c17_b05.md: ...工作负载，用 LWT（Cassandra）或迁移到可串行化系统（CockroachDB）。对多键事务，QUORUM 和 LWT 都不够；需要事务型系统。  在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正...
  c17_b08.md: ...现有系统是 Cassandra：重新设计 schema（每物品一行，而非每购物车一行），并对 “清空购物车” 操作使用 LWT。比 Riak CRDT 方法复杂但可行。...
  c30_b04.md: ...预测延迟 | | **Cassandra** | 无领导者；可调 | 基于 gossip；10-30 秒 | 默认最终；LWT 用于线性化 | 可调 | 可调 | 写重；大规模横向扩展 | | **FoundationDB** | 定制（sequ...
  c30_b11.md: ...受厂商锁定（DynamoDB）。 * **替代**：更高成本下 Spanner 提供更强一致性；Cassandra 用 LWT 可选线性化。   #### F.4 — 高吞吐流处理 / 事件处理  **输入**：100K-1M 事件/秒；分区内有...

=== fill (15 occurrences) ===
  c04_b05.md: ...us)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：...
  c04_b05.md: ...>= Consensus"]         style A fill:#f9f9f9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast...
  c10_b01.md: ..."]         Narwhal --> Aptos2["Aptos 2.x"]         style BG fill:#f9f9f9         style HotStuff fill:#fff4e6         style N...
  c10_b01.md: ...2.x"]         style BG fill:#f9f9f9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁...
  c10_b01.md: ...9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport、Shostak 和 Pe...
  c20_b01.md: ...DB --> DB4["UPDATE rollup<br/>15 ms"]         style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 par...
  c20_b01.md: ...p<br/>15 ms"]         style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（...
  c25_b02.md: ...D2 -->|"streamed tokens"| LB         style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4e6         style...
  c25_b02.md: ...style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII 回退：...
  c25_b02.md: ...style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII 回退：                           ┌──── Request...
  c28_b03.md: ...covery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         style EUW fi...
  c28_b03.md: ...SE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fi...
  c28_b03.md: ...ll:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         style Shared...
  c28_b03.md: ...ll:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...
  c28_b03.md: ...#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...

=== style (15 occurrences) ===
  c04_b05.md: ...(consensus)"| E["Atomic broadcast<br/>= Consensus"]         style A fill:#f9f9f9         style E fill:#fff4e6  等效 ASCII 回退（用于...
  c04_b05.md: ...cast<br/>= Consensus"]         style A fill:#f9f9f9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort bro...
  c10_b01.md: ...Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]         style BG fill:#f9f9f9         style HotStuff fill:#fff4e6...
  c10_b01.md: ...> Aptos2["Aptos 2.x"]         style BG fill:#f9f9f9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6...
  c10_b01.md: ...BG fill:#f9f9f9         style HotStuff fill:#fff4e6         style Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport...
  c20_b01.md: ...on)"]         DB --> DB4["UPDATE rollup<br/>15 ms"]         style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；...
  c20_b01.md: ...ATE rollup<br/>15 ms"]         style R fill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是...
  c25_b02.md: ...ed tokens"| LB         D2 -->|"streamed tokens"| LB         style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4...
  c25_b02.md: ...tokens"| LB         style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e...
  c25_b02.md: ...r fill:#e6f3ff         style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII 回退：...
  c28_b03.md: ...APIW -.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         st...
  c28_b03.md: ...STRATUSE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         sty...
  c28_b03.md: ...le Edge fill:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         sty...
  c28_b03.md: ...yle USE fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...
  c28_b03.md: ...yle EUW fill:#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...

=== percolator (15 occurrences) ===
  c07_b10.md: ...t 或 Paxos）复制，因此任何协调者状态都可从共识日志恢复。这就是 Spanner / CockroachDB / Percolator 模式（第13章）。  只回答“锁争用”而不提阻塞失败模式的 LLM 错过了 2PC 不能作为通用共识原语的核心原因。锁...
  c12_b11.md: ...B/s，24-36 小时完成，无客户停机。  第13章进入下一层：分布式事务。2PC 的阻塞失败模式、Google 的 Percolator（真正的 Spanner 风格事务模式）、Calvin 的确定性排序、TrueTime 的 commit-wait、Co...
  c13_b01.md: ...是 2026 年分布式数据库的核心运维挑战。  本章讲授四种经典方法：2PC（带阻塞失败模式的经典协议）、Google Percolator（大多数生产系统实际采用的 Spanner 克隆模式）、Calvin（确定性排序作为乐观协议的替代）、TrueTime（...
  c13_b03.md: ...### 13.3 Google Percolator  Percolator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事...
  c13_b03.md: ...### 13.3 Google Percolator  Percolator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事务协议，构建于 Bigt...
  c13_b03.md: ...rimary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。   #### Percolator 为何有效  关键洞见：primary key 充当 _原子提交点_。提交与否是 primary 行状态的属性。遇到 s...
  c13_b03.md: ...协调者角色。协调者故障被客户端故障取代，而客户端故障由读时恢复处理（任何后续 reader 都能清理）。   #### Percolator 用于何处  * Google Bigtable 的事务层（Bigtable 的 “单行事务” 是原子的；Percola...
  c13_b03.md: ...colator 用于何处  * Google Bigtable 的事务层（Bigtable 的 “单行事务” 是原子的；Percolator 增加了多行）。 * TiDB 使用 Percolator 派生协议（Percolator 是 TiDB 设计文档的引用...
  c13_b03.md: ...le 的事务层（Bigtable 的 “单行事务” 是原子的；Percolator 增加了多行）。 * TiDB 使用 Percolator 派生协议（Percolator 是 TiDB 设计文档的引用）。 * 大多数 “键值存储上的快照隔离” 设计都遵循此模...
  c13_b12.md: ...Spanner、CockroachDB、FoundationDB 都用此模式。  **Q2.** 什么是 Google Percolator？它如何避免需要中心协调者？  提示  Percolator 用事务中的 “primary key” 作为原子提交点。恢...
  c13_b12.md: ...DB 都用此模式。  **Q2.** 什么是 Google Percolator？它如何避免需要中心协调者？  提示  Percolator 用事务中的 “primary key” 作为原子提交点。恢复由读者而非协调者执行。  **答案：** Percolat...
  c13_b12.md: ...rcolator 用事务中的 “primary key” 作为原子提交点。恢复由读者而非协调者执行。  **答案：** Percolator 是 Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它通过两阶段（Prewrite...
  c13_b17.md: ...### 本章总结  分布式事务是跨分片、跨区域原子性问题。四种经典方法：2PC（经典，存在协调者故障时阻塞的弱点）、Percolator（带 primary-key 原子提交的快照隔离，无中心协调者）、Calvin（通过 sequencer 的确定性排序，...
  c28_b05.md: ...hDB 基于 range；底层 LSM | n/a | | 13 | 分布式事务 | CockroachDB 可串行化；Percolator 风格；§13.6.5 saga | n/a | | 14 | NewSQL + 查询处理器 | CockroachDB...
  c30_b02.md: ...sing Distributed Transactions and Notifications." _OSDI_. — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Tr...

=== apply (15 occurrences) ===
  c09_b10.md: ...缓存投票数或在 term 增加时未重置，candidate 可能把过期票计入。  (b) **只在 leader 上 apply 的混淆。** 条目在多数复制后 _提交_；只有在提交之后才 _应用_ 到状态机。许多 LLM 实现一追加就应用，导致未...
  c09_b10.md: ...现有时不减，导致无限循环重试，看起来像 hang。  (f) **快照与应用竞争。** 快照捕获某时刻状态机状态；并发 apply 不能在快照期间修改状态。LLM 实现很少协调，产生不一致快照。...
  c18_b09.md: ...对 Stratus 的具体架构，token 存储在 Raft 条目本身（作为条目元数据）。Follower 在 apply 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “epoch nu...
  c18_b09.md: ...尝试发命令），token 排序保证只有一位领导者的命令被接受。  代价：每条命令携带 token（几个字节）；每个条目 apply 时检查 token（很快）。代价可忽略；安全收益巨大。  **Q3.** 团队缓存层（Redis Cluster）出现...
  c20_b02.md: ...ntext  当操作流过共识协议（Raft、Paxos）时，trace context 必须持久通过共识轮次。否则共识 apply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower app...
  c20_b02.md: ...ply 侧与发起请求无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的 Raft 库支持 context 传播；并非所有都支...
  c20_b02.md: ...无关联。  在 Raft 中，trace context 可存在日志条目元数据中。Follower apply 条目时，apply 代码使用存储的 context。这要求应用的 Raft 库支持 context 传播；并非所有都支持。  对共识日志是...
  c20_b06.md: ...大化覆盖同时控制成本。  **Q4.** 为什么 trace context 必须跨越共识边界持久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧因果上是下游。没有 context，trace 断裂。  **答案：*...
  c20_b06.md: ...么 trace context 必须跨越共识边界持久（例如跨越 Raft apply）？  提示  请求流过共识；共识 apply 侧因果上是下游。没有 context，trace 断裂。  **答案：** 流过共识协议（CockroachDB 事务...
  c20_b06.md: ...oachDB 事务、Kafka publish-with-confirm、Raft 复制状态变更）的请求，其工作在共识 apply 侧发生：当共识日志条目被应用到状态机时，下游效果产生（写磁盘、发送通知等）。  这些下游效果因果上是原始请求的一部分。...
  c20_b06.md: ...（写磁盘、发送通知等）。  这些下游效果因果上是原始请求的一部分。若 trace context 没有通过共识日志传播，apply 侧的工作看起来是独立的 trace，与发起请求断开。  对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条...
  c20_b06.md: ...text 没有通过共识日志传播，apply 侧的工作看起来是独立的 trace，与发起请求断开。  对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条目），没有链接 apply 与原始请求的 trace context，无法诊断。  对取...
  c20_b06.md: ...是独立的 trace，与发起请求断开。  对调试：若请求因在 apply 侧出问题而失败（如状态机拒绝条目），没有链接 apply 与原始请求的 trace context，无法诊断。  对取证：支付系统中，原始支付请求与 apply 侧放款必须链接...
  c20_b06.md: ...目），没有链接 apply 与原始请求的 trace context，无法诊断。  对取证：支付系统中，原始支付请求与 apply 侧放款必须链接在 trace 中。没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元...
  c20_b06.md: ...必须链接在 trace 中。没有链接，审计链不完整。  实现：trace context 包含在共识日志条目的元数据中。apply 代码运行时提取 context，并为它创建的任何 spans 使用。现代 Raft 库（etcd 的 Raft、raf...

=== dag (15 occurrences) ===
  c10_b01.md: ...--> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --...
  c10_b03.md: ...ining 提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的 mempool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要...
  c10_b03.md: ...入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotStuff 只用于区块链。”HotStuff 是 _共识...
  c10_b04.md: ...### 10.4 Narwhal-Bullshark：基于 DAG 的共识  2022 年 Narwhal 论文（Danezis、Kokoris-Kogias、Sonnino、Spieg...
  c10_b04.md: ...图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：leader 的工作不再是...
  c10_b04.md: ...引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：leader 的工作不再是“排序交易”（DAG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 l...
  c10_b04.md: ...行扩展图。  共识运行在 DAG 之上：leader 的工作不再是“排序交易”（DAG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 leader 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022...
  c10_b04.md: ...r 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block...
  c10_b04.md: ...rk 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block，因为 DAG 本身即是“共识”...
  c10_b04.md: ...在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block，因为 DAG 本身即是“共识”机制（每个 block 都被签名并引用）。  生产中：  * **A...
  c10_b04.md: ...共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block，因为 DAG 本身即是“共识”机制（每个 block 都被签名并引用）。  生产中：  * **Aptos** 使用受 HotStu...
  c10_b04.md: ...ullshark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚秒级提交）。复杂度在于 DAG mempool 的正确性及其与共识层的集成。   ### 10.5 Tendermint：工作马  Tendermin...
  c10_b12.md: ...ange，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard...
  c30_b02.md: ...Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Sp...
  c30_b02.md: ...s." _EuroSys_. * Spiegelman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon,...

=== item (15 occurrences) ===
  c11_b04.md: ...默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 per-item 线性化 PACELC | 默认 PA / EL（低延迟、最终）；条件写操作 PC / EC Strengths | 免...
  c14_b10.md: ...次写成本太高（每个 score 提交都走共识往返）。能工作但贵。  (b) **DynamoDB（KV）** ：per-item 条件写做原子更新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载很 fit。  (c) **Ca...
  c15_b03.md: ...capacity unit。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-write-wins。应用必须处理自己写入一个区域后被另一区域并发写覆盖的情况（罕见但可能）。   ##...
  c15_b05.md: ...存减一；绝不能超卖。   #### 需要线性化  若两个并发购买都想对最后一件商品减一，必须只成功一个。这需要 per-item 的线性化递减。  实现：可串行化数据库中的行级锁（Spanner、CockroachDB、Postgres），或 Ra...
  c15_b09.md: ...一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Global Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Ta...
  c15_b09.md: ...消失”。诊断。  提示  DynamoDB Global Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 l...
  c15_b09.md: ...域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时...
  c15_b09.md: ...域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时钟偏移窗口内），一个会胜出，另一个被丢弃。  从一个区域视角，它自己的写成功了（收到本地区域 ack）。当跨区域复...
  c15_b09.md: ...被覆盖。该区域的用户看到自己的写 “消失”。  架构修复：  (a) **按区域分区** ：设计 schema，使每个 item 只被一个区域拥有。其他区域的写路由到拥有区域。没有跨区域并发写，就没有 LWW 冲突。  (b) **CRDT-bas...
  c15_b09.md: ...深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重）很好。对多区域积极写同一 item 的工作负载不合适。系统文档说明了这点，但初始设计常被忽略。  对 Crescent：支付数据按用户归属区域分区，天然避...
  c16_b07.md: ...（编辑器永远不会看到 “合并冲突”）。 * 结构保持因果性：字符总插入在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAsse...
  c16_b07.md: ...在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能** ：合并操作用 Rust 编译为 WebAssemb...
  c16_b07.md: ...（编辑器永远不会看到 “合并冲突”）。 * 结构保持因果性：字符总插入在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAsse...
  c16_b07.md: ...在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能** ：合并操作用 Rust 编译为 WebAssemb...
  c17_b02.md: ...系统可跨区域每秒服务数百万提交。   #### 库存（第13章）：线性化  库存提交；“不能低于零” 需要协调；per-item 线性化通过事务或 Raft 复制的递减实现；每次 5-50 ms。  三个工作负载的选择展示了频谱。方向错误代价高：...

=== balance (15 occurrences) ===
  c13_b10.md: ...BEGIN TRANSACTION;       -- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaez...
  c13_b10.md: ...al 交易，不是       -- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Fe...
  c13_b10.md: ...-- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片...
  c13_b10.md: ...i 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -- 更新 Fem...
  c13_b10.md: ...-- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总...
  c20_b01.md: ...R --> L         R --> POST         DB --> DB1["read balance<br/>8 ms"]         DB --> DB2["UPDATE Adaeze<br/>30 ms (US-...
  c27_b04.md: ...ccount_balance(account_id):         return db.query("SELECT balance FROM accounts WHERE id = %s", account_id)   谎言：“CP” 意味着 “完全...
  c27_b04.md: ...db.transaction() as tx:             return tx.query("SELECT balance FROM accounts WHERE id = %s", account_id)      def get_acco...
  c27_b04.md: ...id):         # 展示用途有界旧数据可接受         return db.query("SELECT balance FROM accounts WHERE id = %s AS OF SYSTEM TIME '-10s'", acco...
  c27_b11.md: ...ed', reason='busy')          try:             # Read latest balance             balance = db.query(                 "SELECT bal...
  c27_b11.md: ...try:             # Read latest balance             balance = db.query(                 "SELECT balance FROM accounts W...
  c27_b11.md: ...nce             balance = db.query(                 "SELECT balance FROM accounts WHERE id = %s",                 payment.user_...
  c27_b11.md: ...payment.user_id             )              if balance < payment.amount:                 return PaymentResult(stat...
  c27_b11.md: ...db.update(                 "UPDATE accounts SET balance = balance - %s WHERE id = %s",                 (payment.amo...
  c27_b11.md: ...db.update(                 "UPDATE accounts SET balance = balance - %s WHERE id = %s",                 (payment.amount, payme...

=== flink (15 occurrences) ===
  c14_b13.md: ...RDT 是以无冲突意外的方式实现最终一致性的原则性方法。本章还涵盖 Chandy-Lamport 分布式快照，这是理解 Flink/checkpoint 系统的基础。  ---...
  c15_b03.md: ...年重要性  Chandy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在...
  c15_b03.md: ...oint barrier”）流过数据流图。每个算子在 barrier 经过时记录状态；合在一起，这些状态是一个一致快照。Flink 的 exactly-once 语义建立于此。  * **某些实现的分布式事务** ：特定时间戳处系统的 “快照” 概念...
  c15_b03.md: ...基于快照的重启。  该协议已有 40 多年历史，仍是思考一致分布式状态的标准方式。任何从事流处理的人都应了解；任何评估 Flink 风格系统的人都应知道它提供什么保证以及如何实现。   ### 15.5 生产中的最终一致性   #### Dynamo...
  c15_b07.md: ...ck 时刻），但快照是系统的一个 _合法_ 状态。  这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等...
  c15_b08.md: ...(c)。长期若工作负载增长或用户投诉持续，评估 (e)。  **Q2.** 一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Cha...
  c15_b08.md: ...oint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知...
  c15_b08.md: ...source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 che...
  c15_b08.md: ...ink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交。...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个...
  c15_b11.md: ...每条通道上的 marker 定义快照边界；记录的各进程状态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly-once 语义和类似分布式 checkpointing 系统的基础。  计数器 / 排行榜 / 库存工...
  c30_b11.md: ...分区内有序处理；端到端 exactly-once；分钟到小时级延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Kafka（或更...
  c30_b11.md: ...：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured...
  c30_b11.md: ...reaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等...
  c30_b11.md: ...a Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；...

=== remove (15 occurrences) ===
  c16_b02.md: ...读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 rem...
  c16_b02.md: ...OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 remove 的 G-Set）失败：若 A 添加元素 \(x\)，B 并发移除 \(x\)，合...
  c16_b02.md: ...，B 并发移除 \(x\)，合并结果必须决定谁赢。使用 tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。...
  c16_b02.md: ...tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一...
  c16_b02.md: ...某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) *...
  c16_b02.md: ...add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove element x**：找到集合中所有 \((x,d)\)；将 \(d\) 记入 tombstone 集合。 * **...
  c16_b02.md: ...stone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #...
  c16_b02.md: ...读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 rem...
  c16_b02.md: ...OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 remove 的 G-Set）失败：若 A 添加元素 \(x\)，B 并发移除 \(x\)，合...
  c16_b02.md: ...，B 并发移除 \(x\)，合并结果必须决定谁赢。使用 tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。...
  c16_b02.md: ...tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一...
  c16_b02.md: ...某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) *...
  c16_b02.md: ...add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove element x**：找到集合中所有 \((x,d)\)；将 \(d\) 记入 tombstone 集合。 * **...
  c16_b02.md: ...stone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #...
  c30_b14.md: ...y     — PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list C...

=== doc (15 occurrences) ===
  c16_b03.md: ...4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = do...
  c16_b03.md: ...实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')...
  c16_b03.md: ...# 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text...
  c16_b03.md: ...oc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.g...
  c16_b03.md: ...= doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comme...
  c16_b03.md: ...p('meta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端拥有文档自己的副本。编辑更新本...
  c16_b03.md: ...4 Yuki 的工作流 9：Glide 协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = do...
  c16_b03.md: ...实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')...
  c16_b03.md: ...# 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text...
  c16_b03.md: ...oc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.g...
  c16_b03.md: ...= doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comme...
  c16_b03.md: ...p('meta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端拥有文档自己的副本。编辑更新本...
  c16_b08.md: ...reness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalStateFi...
  c16_b09.md: ...）。用寄存器或 LWW 字段建模比 list CRDT 容易；Yjs 的 Y.Map 或 Automerge 的结构化 doc 都可以。 * **评论**：只追加的评论列表。任一 CRDT 都能处理。 * **审计追踪**：项目管理应用常需要合规...
  c16_b11.md: ...e 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动...

=== first (15 occurrences) ===
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kle...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在...
  c16_b05.md: ...、(d) 及部分 (b)、(e) 的技术使能者。没有 CRDT，“离线且自动解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的...
  c16_b05.md: ...iff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kle...
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在...
  c16_b05.md: ...、(d) 及部分 (b)、(e) 的技术使能者。没有 CRDT，“离线且自动解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的...
  c16_b05.md: ...iff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b05.md: ...026 趋势：随着 CRDT 成熟，更多应用可成为 local-first。5 年展望可能出现一代默认符合 local-first 原则的消费应用。   ### 16.6 练习...
  c16_b10.md: ...第二个考虑：CRDT 对离线协作处理干净（离线更改可在重连后合并）；OT 对离线支持需要更仔细设计。随着 local-first 软件更普遍，CRDT 相比 OT 在离线场景更具吸引力。  **Q2.** 一个 LLM 被要求 “实现一个协作文档编...
  c16_b11.md: ...同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Software——Kleppmann 等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工...
  c29_b02.md: ...lackwell + Hopper 继任者）驱动。 * 通过更激进的 KV cache 预取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 * **MoE（Mixture-of-Exper...
  c30_b02.md: ...s, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsk...
  c30_b03.md: ...s, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onwar...

=== rollout (15 occurrences) ===
  c20_b08.md: ...抽查可能发现不了。Tracing  surfaced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTelemetry。Trace 工作正常，但他们发现 Kafka 消费服务的 trace 没有与生产服务的 t...
  c22_b04.md: ...垃圾回收器进入持续收集状态（因为内存几乎总是满的）。系统继续 “工作” 但极慢。触发器是一次增加 5% 内存使用的功能 rollout；系统通过了健康检查，但每个请求都被延迟尖刺。  恢复：通过 trace 识别 rollout；回滚；集群在数小时内恢复...
  c22_b04.md: ...加 5% 内存使用的功能 rollout；系统通过了健康检查，但每个请求都被延迟尖刺。  恢复：通过 trace 识别 rollout；回滚；集群在数小时内恢复。   #### Roblox 2021 多天中断  灰色故障与亚稳态级联的结合。触发器是一次...
  c25_b04.md: ...decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集群 rollout。最终达到 80% 目标。  更深教训：2026 年推理成本优化是架构机会。这些模式是 2023-2024 年研究论文在...
  c25_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollo...
  c25_b08.md: ...Tobias 的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pil...
  c25_b08.md: ...llout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 *...
  c25_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollo...
  c25_b08.md: ...Tobias 的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pil...
  c25_b08.md: ...llout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 *...
  c30_b08.md: ...机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触发）：把部署视为触发；分阶段 rollout（1% → 10% → 100%）是标准防御。     * Ch.27（LLM 辅助审查架构）：内核模式 sensor...
  c30_b09.md: ...* **章节映射防御**：     * Ch.19（config-as-data，watch streams）：配置 rollout 需要分阶段（canary region）；全局原子 rollout 是反模式。     * Ch.23（混沌工程）："...
  c30_b09.md: ...as-data，watch streams）：配置 rollout 需要分阶段（canary region）；全局原子 rollout 是反模式。     * Ch.23（混沌工程）："配置触发所有节点解析器崩溃" 应作为 game-day 场景。...
  c30_b09.md: ...置触发所有节点解析器崩溃" 应作为 game-day 场景。     * Ch.21（级联故障）：同时全球崩溃是无阶段 rollout 的最坏情况。...
  c30_b19.md: ...队头阻塞；无长度预测路由 | Ch.26 §26.3（长度预测路由）   配置变更导致集群范围突发故障 | 全局原子 rollout；缺少分阶段 | Ch.19 §19.5，附录 E.7（Fastly）    反向索引供凌晨 2 点看火焰图并问 “这个...

=== systems (14 occurrences) ===
  c01_b05.md: ...流水线机械机制是 Series IV Vol 3（可观测性）的主题。参考：Sridharan _Distributed Systems Observability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTeleme...
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。它描述了一座虚构希腊岛屿，兼职立法者通过...
  c29_b01.md: ...本书名为 _Distributed Systems: The 2026 Operating Manual_。具名系统（Spanner、CockroachDB、vLLM、M...
  c30_b01.md: ...96). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K., et al....
  c30_b01.md: ...96). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K., et al....
  c30_b02.md: ...vin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_.   #### 最终一致性与 CRDT（第五部分 — 第15-17章）  * Chandy, K...
  c30_b02.md: ...ributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K.,...
  c30_b02.md: ...vin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_.   #### 最终一致性与 CRDT（第五部分 — 第15-17章）  * Chandy, K...
  c30_b02.md: ...ributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K.,...
  c30_b03.md: ...010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu, X., Yan...
  c30_b03.md: ...n, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Chaos Engi...
  c30_b03.md: ...010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu, X., Yan...
  c30_b03.md: ...n, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Chaos Engi...
  c30_b14.md: ...#### 预期仓库结构               distributed-systems-2026-examples/     ├── ch03_clocks/     │   ├── lamport.py...

=== web (14 occurrences) ===
  c01_b05.md: ...uilding Microservices_（第2版，2021）；Richardson & Ruby _RESTful Web Services_ ；gRPC 与 OpenAPI 官方规范。  _保持范围_ 的纪律本身就是一项分布式系统技能。一本...
  c12_b02.md: ...节点集群加 1 节点，12/13 的数据必须移动，运维不可行。  一致哈希由 Karger 等人在 1997 年关于 Web 缓存分布的论文中提出，解决了这个问题。思想：  * 把节点和键都映射到一个环形哈希空间（例如 64 位整数空间，视为一...
  c14_b12.md: ...不同的扩展特性提供 “托管简单性”。选择取决于具体工作负载的扩展轴。  **Q2.** 一个 LLM 被要求为 “现代 Web 应用” 推荐数据库，它推荐了 MongoDB。为什么这个答案在没有更多上下文时是错误的，LLM 应该先问什么？  **...
  c14_b12.md: ...误的，LLM 应该先问什么？  **讨论：** MongoDB 对某些工作负载合理，但对许多其他工作负载不合适。“现代 Web 应用” 这个框架没有具体内容；正确数据库取决于：  * 应用做什么操作（文档 CRUD？复杂查询？Join？）。 *...
  c26_b01.md: ...LLM 请求路由与 Web 请求路由根本不同。Web 请求耗时 10-50 ms；路由到错误服务器的最坏成本是延迟翻倍。LLM 请求耗时 1-30...
  c26_b01.md: ...LLM 请求路由与 Web 请求路由根本不同。Web 请求耗时 10-50 ms；路由到错误服务器的最坏成本是延迟翻倍。LLM 请求耗时 1-30 秒；路由到错误服务器的最...
  c26_b01.md: ...**：许多集群服务多个模型。路由必须考虑哪些节点加载了哪些模型。  (d) **尾部延迟**：LLM 推理的尾部延迟比 Web 请求宽得多（p99 30 秒正常）。差的路由决策对用户可见。  机会：考虑这些属性的路由决策可带来显著效率提升。Any...
  c26_b05.md: ...#### 概念检测  **Q1.** 为什么 LLM 请求路由比 Web 请求路由更重要？  提示  LLM 请求耗时秒级 vs 毫秒级；路由决策后果更大。  **答案：** LLM 请求通常...
  c26_b05.md: ...秒级；路由决策后果更大。  **答案：** LLM 请求通常耗时 1-30 秒（取决于 prompt 长度和输出长度）；Web 请求通常 10-50 ms。错误路由决策的成本相应大得多。  具体 LLM 路由机会：  (a) **KV cache...
  c26_b05.md: ...只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错误路由决策可导致用户等待 30 秒尾部；Web 等价是 100 ms 延迟。影响数量级不同。  现代推理平台...
  c26_b05.md: ...慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错误路由决策可导致用户等待 30 秒尾部；Web 等价是 100 ms 延迟。影响数量级不同。  现代推理平台（Anyscale、Together、Fireworks）...
  c26_b11.md: ...生成的朴素实现会低 50% 以上。在正确路由实现上的投资通过效率收益回报。   ### 本章总结  LLM 请求路由与 Web 请求路由有结构差异：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。LLM 专用路由优化...
  c30_b01.md: ...he feasibility of consistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D. (20...
  c30_b03.md: ...ent 事后复盘（本书全文）。 — 六个谎言的工作示例。   #### 前瞻（第29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockB...

=== cloudflare (14 occurrences) ===
  c03_b01.md: ...到负值。 * **闰秒。** UTC 每隔几年插入一秒闰秒。不同系统处理方式不同（smear、step、repeat）。Cloudflare 2017 年闰秒事故是经典案例；其 DNS 服务出现负持续时间并传播到排序比较，导致数据结构破坏。  现代分布式系统的...
  c16_b04.md: ...时同步** ：用户 actively 编辑时，其 update 被发送到服务器（带 Durable Objects 的 Cloudflare Worker）并广播给其他连接客户端。每次编辑延迟亚秒级。  (b) **重连时追赶** ：客户端离线后重连，与服务器...
  c16_b04.md: ...，与服务器交换 _state vectors_（“我有哪些 update” 的压缩表示），并接收缺失 update。  Cloudflare Worker 持有文档权威副本。它应用进入的 update 并广播给所有连接客户端。重连时，它计算客户端与服务器状态差...
  c16_b11.md: ...Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时...
  c17_b03.md: ...营存储（线性化）；独立计数器服务（CRDT）做页面浏览指标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（per-object 线性化）做文档元数据。注意：Cloudflar...
  c17_b03.md: ...loudflare Worker + Durable Objects（per-object 线性化）做文档元数据。注意：Cloudflare KV 在这里是错误原语——它全局最终一致，会破坏元数据的线性化需求。  优点：每个系统针对其工作负载优化；架构分离清晰...
  c17_b11.md: ...DB（线性化）+ 自定义计数器服务（最终）+ Snowflake（分析）。Glide 用 Yjs（CRDT/强最终）+ Cloudflare Durable Objects（per-object 线性化）做文档元数据。“每个工作负载的正确系统” 是 2026...
  c28_b02.md: .../ p99 ~580 ms），结算到 50+ 国家银行，并提供实时欺诈检测。  组件：  (a) **API 网关**：Cloudflare Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Re...
  c28_b04.md: ...旅程  追踪一笔从圣保罗 Adaeze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) W...
  c28_b04.md: ...ent.completed” 事件。下游服务（通知、分析、账本对账）异步消费。5 ms。  (12) API 服务通过 Cloudflare 返回成功给 Adaeze 的手机。60 ms 跨赤道 + 5 ms 最后一公里。  总计：端到端 p50 约 360...
  c28_b05.md: ...ncing token | E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Consul + Cloudflare Workers | E.2（Roblox Consul）、E.7（Fastly 配置 rollout） | | 20...
  c30_b08.md: ...障模式（高 heartbeat 负载下的 Consul）部署前未做 game-day 测试。   #### E.3 — Cloudflare 邮件路由故障，2024-04  * **触发**：一次配置错误的部署推出代码变更，导致邮件路由 worker 对某些消...
  c30_b08.md: ...**放大**：循环 worker 保持连接池打开；新邮件流量排队；队列满；整个邮件路由服务降级。 * **时间线**：Cloudflare 邮件转发服务中断约 2 小时。 * **恢复**：通过追踪识别循环 worker 后回滚部署。 * **章节映射防御*...
  c30_b11.md: ...bal Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CDN**：Cloudflare 或 Fastly 用于静态内容路径。 * **缓存**：区域 Redis 或 Memcached 用于热读。 * **...

=== once (14 occurrences) ===
  c04_b06.md: ...的原子广播。 * **Redis Streams** 在 stream 内提供有序交付；消费者组提供 at-least-once。 * **CockroachDB 的 Rangefeed** 提供带 closed-timestamp 保证的因果有序...
  c04_b09.md: ...#### 应用问题  **Q1.** 一个团队用消息代理实现微服务舰队的事件分发，该代理提供 at-least-once 交付和 topic 内有序。这给他们哪一层广播？应用代码还需要哪些纪律？  提示  代理在 topic 分区内提供的是...
  c04_b09.md: ...纪律？  提示  代理在 topic 分区内提供的是原子广播。跨 topic 或分区需要应用自己强制。at-least-once 意味着消费者可能看到重复。  **答案：** 代理在 topic 分区内提供原子广播（per-topic orderi...
  c04_b09.md: ...内提供原子广播（per-topic ordering）。跨 topic 或分区只提供应用强制出的性质。at-least-once 交付意味着重试情况下每个消费者可能多次收到同一条消息。  应用需要纪律：  (a) 每个消息处理函数必须具备 **幂等...
  c15_b03.md: ...数据流图。每个算子在 barrier 经过时记录状态；合在一起，这些状态是一个一致快照。Flink 的 exactly-once 语义建立于此。  * **某些实现的分布式事务** ：特定时间戳处系统的 “快照” 概念上是一个 Chandy-Lam...
  c15_b07.md: ..._合法_ 状态。  这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  *...
  c15_b08.md: ...，评估 (e)。  **Q2.** 一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 chec...
  c15_b08.md: ...上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Che...
  c15_b09.md: ...30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照，exactly-once 不可能：系统无法捕获重启的一致状态。该协议是状态流处理整个类别的基础。  **Q3.** 一个团队有全球多区域数据库（...
  c15_b11.md: ...定义快照边界；记录的各进程状态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly-once 语义和类似分布式 checkpointing 系统的基础。  计数器 / 排行榜 / 库存工作负载展示了频谱：计数器（...
  c30_b11.md: ....4 — 高吞吐流处理 / 事件处理  **输入**：100K-1M 事件/秒；分区内有序处理；端到端 exactly-once；分钟到小时级延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或 Cockr...
  c30_b11.md: ...eam processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或...
  c30_b11.md: ...* **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物...
  c30_b19.md: ...） | Ch.21 §21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4 §4.6...

=== session (14 occurrences) ===
  c05_b04.md: ...ack 之后开始的读必须看见该写，即使存在并发操作。法定人数系统只有附加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak...
  c05_b04.md: ...该 HLC。这是现代运维模式。   ### 5.9 五种会话保证  当 follower 可服务读时，应用可从 per-session 视角请求越来越强的一致性。经典五条（来自 Terry、Theimer 等，1994）：  * **Read-your-...
  c05_b04.md: ...个会话可以看过不同顺序；保证只适用于单个会话的视图。  大多数现代数据库提供其中子集，常可配置。代价是跟踪成本：per-session HLC 或 LSN 必须存客户端或服务器，并在每次读时检查。...
  c05_b11.md: ...d-after-write 一致性是最常见的应用层关切。Crescent 客户存款失败是经典案例。现代系统通过 per-session HLC 跟踪（CockroachDB、YugabyteDB）或把受影响会话路由到主库（带应用逻辑的 PostgreSQ...
  c15_b02.md: ...#### 实践实现  标准实现使用 per-session HLC（第3章）跟踪。客户端记录观察到的最大 HLC；后续操作要求服务器至少赶上该 HLC。服务器要么等待、返回该 H...
  c15_b06.md: ...跨会话的因果对应用重要。示例：聊天中同一用户的消息按输入顺序出现。  大多数生产系统只提供子集。应用代码可通过 per-session 跟踪 HLC 或版本号构建缺失的保证。  **Q2.** 为什么计数器工作负载（页面浏览计数器）适合最终一致性，而库存...
  c15_b08.md: ...ser_id` 的一致哈希把某用户所有写路由到同一协调器。单一协调器时钟能正确排序该用户的写。Cassandra 通过 session-level 路由支持。  (c) **轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。L...
  c15_b11.md: ...ow-reads、monotonic writes、consistent prefix）定义了完整线性化的有用 per-session 弱化版本。CockroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话跟踪提供它们。...
  c17_b01.md: ...follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with explicit per-session tracking** ：非原生；应用层实现。...
  c18_b03.md: ...t（或等价共识）管理锁状态，并在受保护资源上执行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种结构。...
  c30_b02.md: ..., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_....
  c30_b02.md: ...antees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N., Baquero, C., & Zawirski, M...
  c30_b02.md: ..., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_....
  c30_b02.md: ...antees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N., Baquero, C., & Zawirski, M...

=== hop (14 occurrences) ===
  c06_b03.md: ...性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：...
  c06_b06.md: ...leaf 交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三层的 a...
  c06_b09.md: ...20 个服务。用户请求延迟预算 p99 1 秒。一次请求触及 8 个服务，其中一个在另一区域。走一遍延迟预算：每个本地 hop 能分多少？  提示  跨区域至少加 100-200 ms。从总数中扣除后，余数分配给本地 hop。  **答案：**...
  c06_b09.md: ...遍延迟预算：每个本地 hop 能分多少？  提示  跨区域至少加 100-200 ms。从总数中扣除后，余数分配给本地 hop。  **答案：** 预算拆分，先把 8 个服务 hop 按串行处理。  固定成本：  * TLS 握手（首次连接，常复...
  c06_b09.md: ...域至少加 100-200 ms。从总数中扣除后，余数分配给本地 hop。  **答案：** 预算拆分，先把 8 个服务 hop 按串行处理。  固定成本：  * TLS 握手（首次连接，常复用 keep-alive）：~30 ms。 * 认证检查...
  c06_b09.md: ...* TLS 握手（首次连接，常复用 keep-alive）：~30 ms。 * 认证检查：~20 ms。 * 跨区域 hop 往返：~150 ms（假设 US-East 到 EU-West）。 * 固定总成本：~200 ms。  剩余预算 =...
  c06_b09.md: ...~200 ms。  剩余预算 = 1000 - 200 = 800 ms，分给 7 个本地（区域内）hop（第 8 个 hop 是跨区域，已计入固定）。  **情况 1——7 跳完全串行**：800 / 7 ≈ 115 ms p99 每跳。可行但...
  c06_b09.md: ...许 7 跳中 4 跳作为一批并行（fan-out 模式），其余 3 跳串行：  * 并行批延迟 = max(4 个并行 hop 的 p99)。每个约 115 ms p99，批 p99 也是 ~115 ms（4 个中最大值，独立且相似）。 * 串行...
  c06_b09.md: ...- 115 = 685 ms 分给 3 串行；每跳 ~228 ms p99。  因此加一次 fan-out 后，单个 hop 有 ~228 ms p99，舒服得多。并行批“买回”预算给串行路径。  **情况 3——2 批各 2 跳并行 + 3...
  c06_b09.md: ...并行 + 3 串行**：更宽松；3 串行每跳约 190 ms。  优化优先级：  (a) **质疑是否所有 7 个本地 hop 都必要。** 服务网格图常包含历史原因存在的 hop；实际数据依赖可能只有 4-5 个。  (b) **识别可并行 f...
  c06_b09.md: ...ms。  优化优先级：  (a) **质疑是否所有 7 个本地 hop 都必要。** 服务网格图常包含历史原因存在的 hop；实际数据依赖可能只有 4-5 个。  (b) **识别可并行 fan-out。** 互不依赖的 hop（如同时取用户资...
  c06_b09.md: ...历史原因存在的 hop；实际数据依赖可能只有 4-5 个。  (b) **识别可并行 fan-out。** 互不依赖的 hop（如同时取用户资料、检查认证、加载定价）应并行。...
  c06_b10.md: ...(c) **质疑跨区域 hop 是否必要。** 数据能否以可接受过期本地缓存？请求能否完全路由到用户归属区域？  (d) **收紧尾部。** 115...
  c20_b07.md: ...50 + 200 + 180 = 430 ms（省 C 的 40 ms）。若 B 和 C 能合并成一个服务，节省的是 hop 开销，通常 10-20 ms。  更深观察：trace 是优化的输入。没有 trace，团队只能猜测哪里慢；有了 tr...

=== yes (14 occurrences) ===
  c08_b09.md: ...故障。  _2PC 结构_ ：一个协调者给所有参与者发 PREPARE（或 VOTE_REQUEST）。若所有参与者投 yes，协调者发 COMMIT；若有任何 no，协调者发 ABORT。协调者是唯一决策者。  _2PC 故障_ ：若协调者在...
  c08_b09.md: ...。  _2PC 故障_ ：若协调者在 PREPARE 后、发 COMMIT/ABORT 前崩溃，参与者被阻塞。它们已投 yes（并持有锁），但不知道协调者的决定。它们不能单方面决定，因为协调者可能在崩溃前已提交（通知了下游客户端）。  _Paxo...
  c13_b01.md: ...验证操作（如检查约束、获取锁）。   2. 将操作数据持久化为 prepared（但未提交）状态。   3. 回复 `YES`（可提交）或 `NO`（必须中止）。  **阶段 2：Commit 或 Abort**  若所有参与者投 YES，协调...
  c13_b01.md: ...复 `YES`（可提交）或 `NO`（必须中止）。  **阶段 2：Commit 或 Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finalize prepared 数据。  若任一参与者投...
  c13_b12.md: ...念检测  **Q1.** 描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prep...
  c13_b12.md: ...Prepare）：协调者向所有参与者发送 PREPARE。每个参与者验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；...
  c13_b12.md: ...repared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。...
  c13_b12.md: ...T；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修复：通过共识（Raft、Paxos）复制...
  c13_b12.md: ...念检测  **Q1.** 描述 2PC 的两个阶段。协议的经典失败模式是什么？  提示  阶段 1：prepare；投 yes/no。阶段 2：commit 或 abort。失败：协调者在阶段 1 后崩溃。  **答案：**  阶段 1（Prep...
  c13_b12.md: ...Prepare）：协调者向所有参与者发送 PREPARE。每个参与者验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；...
  c13_b12.md: ...repared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。...
  c13_b12.md: ...T；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送 COMMIT/ABORT 前崩溃。参与者已投 YES 并持锁；无法自行决定（协调者可能在崩溃前已提交）；无限期等待协调者。  现代修复：通过共识（Raft、Paxos）复制...
  c13_b15.md: ...。若后续步骤失败，_补偿动作_ 撤销前面步骤。系统从不在服务边界上持锁。  2PC：事务在所有参与者间保持，直到全部投 YES，然后原子提交。协议期间锁跨服务边界持有。  Saga 是正确答案的情况：  * **跨服务事务** ：事务跨不同团队拥...
  c13_b17.md: ...COMMIT 到达两次。参与者必须幂等处理。LLM 代码通常不会。  (e) **参与者崩溃后的恢复**。参与者在投 YES 后、收到 COMMIT 前崩溃，恢复时必须记住其投票并一致行动。这需要 prepared 状态的持久化存储。LLM 代...

=== appendentries (14 occurrences) ===
  c09_b01.md: ...本章按 Anya 学习 Raft 的方式教授：三个子问题（leader 选举、日志复制、安全性）、term 的作用、AppendEntries 协议，以及为什么 2026 年每个新分布式系统项目首先想到 Raft。   ### 9.1 三个子问题  Raft 把...
  c09_b06.md: ...有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给 follower。条目一旦多数复制即提交；follower 将已提交条目应用到自己的状态机。  (c) **...
  c09_b06.md: ...条件下会频繁活锁。有了它，leader 选举完成时间期望约为最大超时量级（生产通常几百毫秒）。  **Q4.** 描述 AppendEntries 一致性检查。它保证什么？  提示  Follower 必须在与 leader 记录匹配的前一条目处才追加新条目。...
  c09_b08.md: ...期间集群提交了 1000 个条目。该 follower 重新加入。走一遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSn...
  c09_b08.md: ...**答案：** Follower 重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (...
  c09_b08.md: ...ader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 etcd / TiKV / 基于 Raft 的系统里，这种追赶通常透明，数千条差距在数秒内完成。...
  c09_b09.md: ...：  _多数侧（3 节点）：_  (a) 若原 leader 在多数侧，它继续运行。它的心跳到达 3 中 2 个；它有 AppendEntries 的多数。提交正常进行。  (b) 若原 leader 在少数侧，多数侧 follower 的定时器触发（收不到心跳）。...
  c09_b10.md: ...ft 就和 Paxos 一样复杂。”  **讨论：** 辩护：核心 Raft 协议（leader election + AppendEntries + commit）确实比核心 Paxos（prepare + accept + Multi-Paxos 优化）简单。学...
  c09_b10.md: ...corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendEntries 一致性检查 off-by-one。** 检查比较 follower 在 `prevLogIndex` 处的条目与 `p...
  c09_b10.md: ...Term`。索引或 term 比较差一都会让不一致日志保持不一致。  (e) **NextIndex 回退缺失。** `AppendEntries` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LLM 实现有时不减，导致无限循环重试，看起来像...
  c09_b11.md: ...ader election、log replication、safety），并基于 terms、elections 和 AppendEntries 一致性检查提供清晰的运维模型。  Anya 为 Stratus DB 选择 raft-rs、依赖库工具、接受运维默认值...
  c18_b08.md: ...返回 `STALE_LEADER`。提议客户端知道自己失去领导权并下台。  实现细节：token 比较发生在 Raft AppendEntries 阶段，而非仅在 API 层。这确保即使过期领导者的 RPC 到达 Raft group，条目在共识层也被拒绝；应用逻辑...
  c28_b10.md: ...h.9 §9.4 | 客户端请求 → leader → follower | 复制状态机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节...
  c30_b14.md: ...│   ├── raft_minimal.py   — ~300 lines: leader election + AppendEntries + commit     │   ├── kv_state_machine.py — replicated key-v...

=== cloud (14 occurrences) ===
  c11_b09.md: ...中；Cassandra 兼容 API Cost | 开源；众多云托管 offering | 开源 + ScyllaDB Cloud  对分析工作负载（通常高吞吐、可容忍最终一致）的综合：  若 (a) 每节点吞吐重要（希望更少但更强大的节点）、(b)...
  c14_b01.md: ...14.2 Spanner  参考实现。Google Spanner 自 2012 年在生产运行，2017 年左右作为 Cloud Spanner 对外提供。  关键属性：  * 每 tablet 使用 Paxos 复制。 * TrueTime 用于...
  c19_b03.md: ...富元数据需要 SRV 或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * *...
  c19_b03.md: ...XT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kubernetes he...
  c19_b04.md: ...sul 多 DC 支持找到 US-East 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Clou...
  c19_b04.md: ...loud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Map 暴露；全球客户解析到最近的区域负载均衡。  这四个系统分层。每个处理自己的领域；跨领域交互通过明确定义的 AP...
  c19_b10.md: ...实例启动时通过 Consul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS...
  c19_b10.md: ...接使用 etcd**：在 EC2 上部署 etcd 集群；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Java 中心化服务发...
  c19_b10.md: ...tflix 使用；2026 年较不常见，但某些遗留系统仍在用。  对 2026 年的新 EC2 部署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。...
  c19_b10.md: ...EC2 部署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出...
  c19_b11.md: ...？云？本地？你们团队目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的哪些实例现在健康” 的注册表。20...
  c19_b11.md: ...：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每个处理自己的领域；跨领域交互明确。  第20章开始第七部分：运营分布式系统。分布式追踪与调...
  c30_b03.md: .... "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，作为 2027-2030 主流采用预测重读（§29.1）。 * Castr...
  c30_b09.md: ...er 纪律）：无 fencing 的 failover 模式正是第5章警告的。   #### E.6 — Google Cloud BigQuery 存储故障，2023-08-08  * **触发**：BigQuery 存储写入 API 的 bug...

=== payment (14 occurrences) ===
  c13_b07.md: ...participant A as Analytics         C->>O: initiate payment         O->>R: reserve funds         R-->>O: reserved...
  c18_b03.md: ...queue：用 Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payment commit：用 fencing token。不用的话最坏情况：重复扣款，灾难。  正确的分布式锁库用 Raft（或等...
  c20_b01.md: ...nt 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network in + TLS<br/>65 ms...
  c20_b04.md: ...### 20.6 取证 Trace  某些 trace 是特殊的：它们是特定操作的 _取证记录_。示例：  (a) **Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifac...
  c27_b02.md: ...，返回之前计算的结果。重试安全。       def submit_payment(payment):         payment.idempotency_key = generate_unique_key()         result = re...
  c27_b02.md: ...result = retry_with_idempotency(payment_service.charge, payment, key=payment.idempotency_key)         # ... 无论发生多少次重试都安全...
  c27_b02.md: ...retry_with_idempotency(payment_service.charge, payment, key=payment.idempotency_key)         # ... 无论发生多少次重试都安全   LLM 产出第一版是因为大...
  c27_b11.md: ...d。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use CockroachDB which is CP for consis...
  c27_b11.md: ...lock for the user's account         lock_key = f"user_lock:{payment.user_id}"         if not redis.set(lock_key, "1", nx=True,...
  c27_b11.md: ...ELECT balance FROM accounts WHERE id = %s",                 payment.user_id             )              if balance < payment.amo...
  c27_b11.md: ...payment.user_id             )              if balance < payment.amount:                 return PaymentResult(status='failed...
  c27_b11.md: ...lance - %s WHERE id = %s",                 (payment.amount, payment.user_id)             )             notification_service.not...
  c27_b11.md: ...)             notification_service.notify(payment.user_id, "Payment processed")             ledger_service.record(payment.id, t...
  c28_b04.md: ...下游 endpoint 验证 token（谎言 4 防御）。5 ms。  (11) API 服务向 Kafka 发布 “payment.completed” 事件。下游服务（通知、分析、账本对账）异步消费。5 ms。  (12) API 服务通过 Clo...

=== trino (14 occurrences) ===
  c14_b03.md: ...与 NewSQL 不同类别：分布式查询处理器处理分析工作负载（TB-PB 数据上的大扫描、聚合、跨源 join）。示例：Trino/Presto、Snowflake、BigQuery、Spark SQL。...
  c14_b04.md: ...#### Trino/Presto  源自 Facebook 的 Presto（2013）；2020 年分叉为 Trino，PrestoDB...
  c14_b04.md: ...#### Trino/Presto  源自 Facebook 的 Presto（2013）；2020 年分叉为 Trino，PrestoDB 由 Facebook 继续。  * 分布式查询引擎；将工作下推给源系统。 * 跨多种数据源查询（S3...
  c14_b05.md: ...CockroachDB + 独立 OLAP 存储 纯 OLAP | Snowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | Cockroa...
  c14_b05.md: ...议：CockroachDB 单区域做 OLTP 路径（线性化、每分片 Raft、快速故障切换），Snowflake 或 Trino 做批处理重训路径。图查询组件是例外——若跨账户图稠密，图数据库（Neo4j、Amazon Neptune）更适合这些特...
  c14_b09.md: ...析产品。客户通过产品 UI 查询自己的数据；查询可以是即席 SQL。团队在 Snowflake（托管）和数据湖上自托管 Trino 之间选择。比较。  提示  Snowflake：托管、成本可预测、控制少。Trino：自托管、更多控制、需要运维专长。...
  c14_b09.md: ...lake（托管）和数据湖上自托管 Trino 之间选择。比较。  提示  Snowflake：托管、成本可预测、控制少。Trino：自托管、更多控制、需要运维专长。  **答案：**  _Snowflake_ ：  * 优点：零运维负担；按查询和存储...
  c14_b09.md: ...制；完全调优控制；跨多源联邦查询；无锁定。 * 缺点：重大运维负担（计算集群管理、查询优化、缓存）；需要 JVM 调优、Trino 内部知识；成本包含运维团队。  对 SaaS 分析产品：...
  c14_b10.md: ...* 若团队小（无专职数据平台工程师）：Snowflake。自托管 Trino 的运维负担会主导成本方程。 * 若团队有数据平台专长且产品在规模上对成本敏感：Trino。规模化后成本节省可能很大。...
  c14_b10.md: ...：Snowflake。自托管 Trino 的运维负担会主导成本方程。 * 若团队有数据平台专长且产品在规模上对成本敏感：Trino。规模化后成本节省可能很大。 * 若产品需要跨多个客户数据源查询（联邦查询到客户控制的数据库）：Trino 的联邦是正确...
  c14_b10.md: ...上对成本敏感：Trino。规模化后成本节省可能很大。 * 若产品需要跨多个客户数据源查询（联邦查询到客户控制的数据库）：Trino 的联邦是正确工具。  混合：团队内部分析用 Snowflake；客户-facing 联邦查询用 Trino（因为 Sn...
  c14_b10.md: ...的数据库）：Trino 的联邦是正确工具。  混合：团队内部分析用 Snowflake；客户-facing 联邦查询用 Trino（因为 Snowflake 无法联邦到客户控制的源）。许多真实 SaaS 平台两者都跑。  2026 趋势：随着开源 T...
  c14_b10.md: ...o（因为 Snowflake 无法联邦到客户控制的源）。许多真实 SaaS 平台两者都跑。  2026 趋势：随着开源 Trino 生态成熟（更好的优化器、缓存、运维工具），Snowflake 与 Trino 的差距在缩小。决策越来越依赖团队能力而非...
  c14_b10.md: ...台两者都跑。  2026 趋势：随着开源 Trino 生态成熟（更好的优化器、缓存、运维工具），Snowflake 与 Trino 的差距在缩小。决策越来越依赖团队能力而非技术能力。  **Q3.** 用诊断方法（第11章）+ 事务协议视角（第13章...

=== list (14 occurrences) ===
  c16_b03.md: ...### 16.3 List CRDT：协作文辑的核心  List（文档正文）是最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必...
  c16_b03.md: ...### 16.3 List CRDT：协作文辑的核心  List（文档正文）是最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必须产生一致的全序。   #### Lo...
  c16_b03.md: ...独立在不同位置插入字符；合并必须产生一致的全序。   #### Logoot / Treedoc / WOOT  早期 list CRDT（2000 年代中期）。每个字符被分配一个 _position identifier_，位于相邻字符之间。Po...
  c16_b03.md: ...1.4。1.3 与 1.4 之间再插入则取 1.35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers...
  c16_b03.md: ...### 16.3 List CRDT：协作文辑的核心  List（文档正文）是最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必...
  c16_b03.md: ...### 16.3 List CRDT：协作文辑的核心  List（文档正文）是最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必须产生一致的全序。   #### Lo...
  c16_b03.md: ...独立在不同位置插入字符；合并必须产生一致的全序。   #### Logoot / Treedoc / WOOT  早期 list CRDT（2000 年代中期）。每个字符被分配一个 _position identifier_，位于相邻字符之间。Po...
  c16_b03.md: ...1.4。1.3 与 1.4 之间再插入则取 1.35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers...
  c16_b07.md: ...并发写的字段，线性化。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针...
  c16_b07.md: ...Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size...
  c16_b07.md: ...有副本观察到删除后 GC。 (e) **持久化格式** ：高效的二进制格式存储和传输 CRDT update。  早期 list CRDT（Logoot、Treedoc、WOOT）存在问题：标识符增长、合并慢、内存占用大。Yjs 是首个对生产环境浏...
  c16_b09.md: ...**状态变更**：离散状态转换（Open → In Progress → Done）。用寄存器或 LWW 字段建模比 list CRDT 容易；Yjs 的 Y.Map 或 Automerge 的结构化 doc 都可以。 * **评论**：只追加的...
  c16_b11.md: ...持递减的计数器）、LWW-Register（最后写胜出）、MV-Register（多值）、OR-Set（观察删除集合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协...
  c30_b14.md: ...Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT     ├── ch18_locks/     │   ├── redlock_naive.py  — th...

=== nvidia (14 occurrences) ===
  c23_b13.md: ...之前发现问题。  第24章开启第八部分：分布式 AI 推理。推理集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例...
  c24_b01.md: ...he sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NVIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。   ### 24.1 为什么分布式 LLM 推...
  c24_b01.md: ...的突发（多轮并发对话）。集群必须处理这种混合而没有队头阻塞。  2026 年处理这些挑战的系统：vLLM、SGLang、NVIDIA NIM。每种有不同优化；适合不同工作负载。...
  c24_b04.md: ...### 24.4 NVIDIA NIM  NIM（NVIDIA Inference Microservices）是 NVIDIA 的企业推理部署平台。...
  c24_b04.md: ...### 24.4 NVIDIA NIM  NIM（NVIDIA Inference Microservices）是 NVIDIA 的企业推理部署平台。约 2024 年 GA 发布；企业 LLM 部署的商业参考设计。  特性：  (a) **容器化微...
  c24_b04.md: ...化微服务**：每个模型是一个 NIM 容器（含模型权重、推理引擎、API 表面）。通过 Kubernetes 部署；由 NVIDIA 工具管理。  (b) **多引擎支持**：NIM 容器可按模型使用 vLLM、TensorRT-LLM（NVIDIA...
  c24_b04.md: ...-LLM（NVIDIA 优化引擎）或其他引擎。面向用户的 API 一致。  (c) **硬件优化**：NIM 容器包含 NVIDIA 的硬件专属优化（H100 / H200 / B100 / B200 调优）。每代 GPU 都有调优容器。  (d) *...
  c24_b08.md: ...100 万美元以上推理预算每年节省数百万美元。ROI 很直接。  **Q2.** 某团队在企业部署中选择 vLLM 与 NVIDIA NIM。他们在 Kubernetes 上；有 SOC 2 合规要求。比较。  提示  vLLM：开源，更多控制，更少集...
  c24_b08.md: ...更快实现。 * 推理引擎控制较少：NIM 容器使用特定引擎（通常底层仍是 vLLM），但团队离调优隔了一层。 * 成本：NVIDIA 收取企业 tier 费用。  对 SOC 2 合规 Kubernetes 部署：  * **NIM** 是运维上更快的...
  c24_b08.md: ...ubernetes 部署：  * **NIM** 是运维上更快的路径。合规和企业特性已预建；团队认证工作量减少。成本是 NVIDIA license 溢价。 * **vLLM** 是成本有效的路径，若团队有工程能力构建缺失的企业特性。长期控制更多；短期...
  c24_b11.md: ...cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA NIM 是企业部署平台：容器化微服务、多引擎支持、硬件优化、企业特性。企业团队的标准部署表面。  Continuous...
  c30_b03.md: ...Tolerance." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控制平面重读（§29.1）。 * NVIDIA Blackwell (B100 / B200) 及 Hopper 继任者产品简介（2024-2026 发布）。 — s...
  c30_b11.md: ...e-aware 路由。  * **推理引擎**：vLLM（通用）或 SGLang（前缀重工作负载）；企业部署两者都通过 NVIDIA NIM。 * **架构**：集群超过约 50 节点后做 prefill/decode 分离（第25章）；KV cach...
  c30_b11.md: ...025-2026 架构突破（分离、KV cache 层、投机解码）复合产生显著成本节省。 * **替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用 Anyscale / Together / Fireworks 等托管替代。...

=== latency (13 occurrences) ===
  c02_b04.md: ...ACELC：若发生 Partition，则 Availability 或 Consistency；否则（Else），则 Latency 或 Consistency。  这个缩写很别扭，但观点尖锐。CockroachDB 在分区期间是 CP（它拒绝分区侧的...
  c02_b08.md: ...PACELC：若 Partition，则 Availability vs Consistency；否则（Else），Latency vs Consistency。每个系统是 PA/EL、PA/EC、PC/EL、PC/EC 之一。  **答案：** P...
  c05_b01.md: ...ver consistency** ：若主库死亡，任意副本被提升为新主库，新主库拥有所有已确认写。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica failure** ：...
  c05_b02.md: ...## 5.3 异步复制  主库接受写入，本地提交，立即向客户端确认。写后台发送给副本。  性质：  * **Write latency** ：只有本地提交加网络确认。 * **Post-failover consistency** ：若主库在写传播前死亡...
  c05_b02.md: ...nsistency** ：若主库死亡，已 ack 的副本拥有该写。若提升的是其他副本，写可能丢失。 * **Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under replica failu...
  c06_b02.md: ...写发往一个区域（active）。其他区域是异步复制的 warm standby。读可去任何区域（可能过期）。  * **Latency** ：写只付区域内成本；读可本地。 * **Failover** ：切换到 standby 需数分钟；可能丢失部分写。...
  c06_b02.md: ...属”一个区域（按用户居住地选择）。该用户的读写去那个区域。其他区域缓存读（容忍过期）或把请求代理到归属区域。  * **Latency** ：归属区域用户看到本地延迟；跨归属操作付全价。 * **Failover** ：per-region 故障切换；其...
  c06_b02.md: ...。   #### 模式 3：全局一致（真正的多主）  每个区域都能服务任何操作。写跨区域协调以保证全局一致。  * **Latency** ：每次写付跨区域往返成本。 * **Failover** ：区域可互换；丢失任一区域透明。 * **Right f...
  c19_b09.md: ...（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这...
  c25_b01.md: ...KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill...
  c29_b02.md: ...n 延迟 100-300 ms。  2027-2030 轨迹：  * 生产交互式工作负载的 **inter-token latency（ITL）** 低于 10 ms 成为标准。这由投机解码改进（Medusa、EAGLE、Hydra 变体）和硬件（B10...
  c29_b02.md: ...opper 继任者）驱动。 * 通过更激进的 KV cache 预取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 * **MoE（Mixture-of-Experts）** 模型成为 >10...
  c30_b02.md: ...P_. * Lloyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== step (13 occurrences) ===
  c03_b03.md: ...#### 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1...
  c03_b03.md: ...b) 和 V(b) < V(a) 都不成立，则事件并发。   #### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 |...
  c03_b06.md: ...ime 类行为？”   #### 工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 |...
  c03_b13.md: ...f.L, sender_L) + 1             return self.L, msg   三进程走查：  Step | Action | L_1 | L_2 | L_3 | 注释 ---|---|---|---|---|--- 1 |...
  c13_b07.md: ...PaymentOrchestrator (CockroachDB-backed state):       step 1: call ReservationService.reserve(user, amount)       step...
  c13_b07.md: ...step 1: call ReservationService.reserve(user, amount)       step 2: call FraudService.check(transaction)         → score...
  c13_b07.md: ...call FraudService.check(transaction)         → score       step 3: call BankSettlement.submit(transaction)      → bank_ref...
  c13_b07.md: ...ll BankSettlement.submit(transaction)      → bank_ref       step 4: call LedgerService.record(transaction, bank_ref)       s...
  c13_b07.md: ...p 4: call LedgerService.record(transaction, bank_ref)       step 5: call NotificationService.send(user, "completed")       s...
  c13_b07.md: ...p 5: call NotificationService.send(user, "completed")       step 6: call AnalyticsService.update(merchant, day)       on fai...
  c13_b07.md: ...AnalyticsService.update(merchant, day)       on failure at step N: invoke compensations for N-1, N-2, ..., 1   单一 orchestra...
  c13_b07.md: ...O-->>C: success         Note over O: On failure at step N:<br/>invoke compensations<br/>for N-1, N-2, ..., 1...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用户资金（CockroachDB...

=== vector (13 occurrences) ===
  c03_b03.md: ...并发。   #### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P_1 | send...
  c25_b01.md: ...TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 G...
  c27_b03.md: ...由因果性排序的事件（回复发生在其父事件之后）可能被时钟偏移颠倒。  修复：用 happens-before、HLC 或 vector clock（第3章）排序。       def merge_events(events_from_replicas):...
  c28_b10.md: ...进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长...
  c28_b10.md: ...amport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长；HLC 计数无界溢出 | | 2 | 广播原语伪代码 | Ch.4 §4.7 | 要交付给 N 个进...
  c28_b10.md: ...进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长...
  c28_b10.md: ...amport / Vector / HLC 计数规则 | 将 wall clock 与逻辑时钟比较；10k 节点舰队的 vector clock 增长；HLC 计数无界溢出 | | 2 | 广播原语伪代码 | Ch.4 §4.7 | 要交付给 N 个进...
  c30_b14.md: ...│   ├── lamport.py        — Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)     │   └── hlc.py...
  c30_b14.md: ...— Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)     │   └── hlc.py            — Hybrid Log...
  c30_b14.md: ...oding     │   └── causal.py         — causal broadcast with vector clocks     ├── ch09_raft/     │   ├── raft_minimal.py   — ~...
  c30_b14.md: ...│   ├── lamport.py        — Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)     │   └── hlc.py...
  c30_b14.md: ...— Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)     │   └── hlc.py            — Hybrid Log...
  c30_b14.md: ...oding     │   └── causal.py         — causal broadcast with vector clocks     ├── ch09_raft/     │   ├── raft_minimal.py   — ~...

=== tombstone (13 occurrences) ===
  c03_b11.md: ...个团队在三个数据中心运行 Cassandra，每秒 100,000 次写入，偶尔发现“last write wins” tombstone 复活了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cassandra 的 LWW 依赖每次写入的 wa...
  c04_b11.md: ...的节点重发，X 恢复后永远不会得到它。Agreement 需要恢复后重新交付，通常通过发送者持久化 ack 跟踪或基于 tombstone 的 gossip 层实现。  (c) **跨发送者没有顺序。** 若两个发送者广播应排序的事件（例如同一实体的“创建”...
  c15_b07.md: ...标上更早时间戳（因其写入节点时钟慢），LWW 会丢弃逻辑上更晚的写，而选择逻辑上更早的写。  Cassandra 的 “tombstone resurrection” bug（第5章）是经典示例：由于时钟偏移，delete 的时间戳早于后续写；后续写 “胜出...
  c16_b02.md: ...remove 的 G-Set）失败：若 A 添加元素 \(x\)，B 并发移除 \(x\)，合并结果必须决定谁赢。使用 tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在...
  c16_b02.md: ...：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：生成新唯一 dot \(d\)；将 \...
  c16_b02.md: ...air 集合。 * **Remove element x**：找到集合中所有 \((x,d)\)；将 \(d\) 记入 tombstone 集合。 * **集合值**：若存在某个 \((x,d)\) 的 \(d\) 未被 tombstone，则 \(x\)...
  c16_b02.md: ...\(d\) 记入 tombstone 集合。 * **集合值**：若存在某个 \((x,d)\) 的 \(d\) 未被 tombstone，则 \(x\) 在集合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 to...
  c16_b02.md: ...((x,d)\) 的 \(d\) 未被 tombstone，则 \(x\) 在集合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remov...
  c16_b02.md: ...ne，则 \(x\) 在集合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-...
  c16_b02.md: ...后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR...
  c16_b02.md: ...OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾回收。   #### LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值...
  c16_b07.md: ...e size** ：Yjs ~80 KB，即使在慢网络下也足够小，适合浏览器。 (d) **垃圾回收** ：删除字符的 tombstone 最终在所有副本观察到删除后 GC。 (e) **持久化格式** ：高效的二进制格式存储和传输 CRDT update。...
  c17_b08.md: ...的 CRDT bucket types。OR-Set 语义匹配购物车的 “物品并集” 行为。并发添加保留；并发移除被 tombstone；最终购物车是确定性合并。  若团队现有系统是 Cassandra：重新设计 schema（每物品一行，而非每购物车一行...

=== tablet (13 occurrences) ===
  c05_b06.md: ...用窗口。leaderless 让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-based 但 leader 切换开销低；Cassandra 用轻量事务处理需要 leader 类语义的...
  c07_b05.md: ...r-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet Paxos。由于 Google 数据中心网络纪律更好，超时更紧。 * **TiKV** 用 Raft。Multi-Ra...
  c09_b05.md: ...t consensus 的安全子集）。  她放弃的：无 leader 写（CRDT 或 EPaxos 可提供）；per-tablet 性能调优（Spanner 的 Paxos-with-leases 可提供）。对 Stratus 场景（金融级持久性、亚...
  c11_b01.md: ...yteDB。每个分片（range、region、key range）有自己的共识组，通常是 Raft。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线...
  c11_b01.md: ...有自己的共识组，通常是 Raft。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、D...
  c11_b03.md: ...Google）  Question | Answer ---|--- Replication protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery...
  c13_b04.md: ...协调者往返即可分配线性化提交时间戳。  Spanner 如何在提交中使用 TrueTime：    1. 事务获取参与 tablet 的读/写锁。   2. 事务的 `commit_timestamp = TT.now().latest`（最晚可能的...
  c13_b04.md: ...ner 事务都 provably 更晚或更早，而不会 “模糊”。   #### Spanner 的完整提交协议  对多 tablet 事务，Spanner 使用带复制协调者状态的 2PC。参与者是各 tablet（各跑 Paxos）。2PC 层增加 c...
  c13_b04.md: ...Spanner 的完整提交协议  对多 tablet 事务，Spanner 使用带复制协调者状态的 2PC。参与者是各 tablet（各跑 Paxos）。2PC 层增加 commit_timestamp 协调。TrueTime commit-wait...
  c14_b01.md: ...er 自 2012 年在生产运行，2017 年左右作为 Cloud Spanner 对外提供。  关键属性：  * 每 tablet 使用 Paxos 复制。 * TrueTime 用于线性化全局时间戳。 * 支持跨多行、多 tablet 事务的 SQ...
  c14_b01.md: ...：  * 每 tablet 使用 Paxos 复制。 * TrueTime 用于线性化全局时间戳。 * 支持跨多行、多 tablet 事务的 SQL。 * 区域或多区域部署配置。 * 在线 schema 变更。  优势：  * 最成熟的多区域事务数据库...
  c14_b07.md: ...Spanner | CockroachDB ---|---|---- Replication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos leader 切换（~数秒...
  c30_b04.md: ...LC | 最佳场景 | |---|---|---|---|---|---|---| | **Spanner** | 每 tablet 一个 Paxos；TrueTime | 基于租约；亚秒级 | 线性化 + 旧数据读 | CP | PC/EC | 全局...

=== hello (13 occurrences) ===
  c08_b05.md: ...romise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})`...
  c08_b05.md: ...xt{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCEPT(b=10, v=\text{"hello"})` 给三个 acceptor。  **步骤 5** ：三个 acceptor 都接受。它们设置 `accepted...
  c08_b05.md: ...acceptor 都接受。它们设置 `accepted_ballot = 10, accepted_value = "hello"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 a...
  c08_b05.md: ...d_ballot = 10, accepted_value = "hello"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在...
  c08_b05.md: ...0, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新 ballot：  **步骤 7*...
  c08_b05.md: ...Acceptor 的 $b_{max}=10$。它们接受 prepare，每个回复 `PROMISE(20, 10, "hello")`（先前接受的值及其 ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promis...
  c08_b05.md: ...ballot）。  **步骤 9** ：第二个 proposer 收集到 3 个 promise。所有都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 b...
  c08_b05.md: ...都携带已接受值 “hello” 及 ballot 10。根据 accept 规则，第二个 proposer 必须提议 “hello”（最高已接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20...
  c08_b05.md: ...接受 ballot 的值）。  **步骤 10** ：第二个 proposer 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepar...
  c08_b05.md: ...ser 发送 `ACCEPT(b=20, v="hello")`。Acceptor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “he...
  c08_b05.md: ...tor 接受，更新状态。  选定的值保持 “hello”。任何未来 ballot 也会在 prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若第二个 proposer 试图提议不同值（如 “w...
  c08_b05.md: ...lo”。任何未来 ballot 也会在 prepare 响应中看到 “hello”（由法定人数相交保证），并必须提议 “hello”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。...
  c08_b05.md: ...“hello”。协议安全。  若第二个 proposer 试图提议不同值（如 “world”），它会被协议强制改为 “hello”。Proposer 的“意愿”不能凌驾 safety 不变式。   ### 8.9 练习...

=== pipeline (13 occurrences) ===
  c10_b03.md: ...高 QC（$O(N)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotStuff 把各阶段 pipeline。Leader 在为块 $k+1$ 收集第一阶段投票时，可以同时为块 $k$ 收集第二阶段投票、为块 $k-1$ 收集第...
  c10_b10.md: ...—结构上有什么不同？  提示  Raft leader 串行化提交；多条目可同时在空中但各自独立发送。HotStuff pipeline 让不同提议的三个投票阶段重叠。  **答案：** 两个协议都支持多个在飞决定，但结构不同。  _Raft pipeli...
  c10_b10.md: ...endEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff p...
  c10_b10.md: ...ipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 p...
  c10_b10.md: ...prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipeline 是“不同决定的阶段重叠”，每个决定总共需三轮，但吞吐可达每轮一个决定（而非每三轮一个决定）。  结构差异：  * Ra...
  c10_b10.md: ...—结构上有什么不同？  提示  Raft leader 串行化提交；多条目可同时在空中但各自独立发送。HotStuff pipeline 让不同提议的三个投票阶段重叠。  **答案：** 两个协议都支持多个在飞决定，但结构不同。  _Raft pipeli...
  c10_b10.md: ...endEntries`。每个条目需要多数 ack 才能提交。Leader 可让多个条目挂起，不必等每个提交再启动下一个。Pipeline 是“网络上的在飞条目”，每个条目从 leader 到 follower 再返回只需单轮往返。  _HotStuff p...
  c10_b10.md: ...ipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 p...
  c10_b10.md: ...prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipeline 是“不同决定的阶段重叠”，每个决定总共需三轮，但吞吐可达每轮一个决定（而非每三轮一个决定）。  结构差异：  * Ra...
  c20_b01.md: ...方面：因果保持、分区下采样、跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vol 3（可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什...
  c20_b03.md: ...vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeline 细节（Series IV Vol 3 会讲）。要点：OpenTelemetry 是现代默认；使用它可消除旧追踪系统的...
  c29_b04.md: ...去构建系统。当本书过时时，写下下一本。  * * *  _第29章（后记）于 2026-05-20 作为 post-pipeline 润色的一部分添加。_ _前瞻是故意预测；2028+ 读者被邀请评估哪些部分老得好。_ 这篇后记是预测；对技术 5 年的...
  c30_b19.md: ...什么？” 的调试工程师使用。章节指针是入口；章节的防御是修复。  * * *  _2026-05-20 作为 post-pipeline 润色的一部分生成。_ _QBooks · A Socratopia Learning Series Publicati...

=== scylladb (13 occurrences) ===
  c11_b06.md: ...下正确）。知道差别可避免过度付费。  **Q3.** 用诊断方法给一个你没用过的系统产出一段阅读。可选：MongoDB、ScyllaDB、Aurora DSQL、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。...
  c11_b09.md: ...致”毫无意义；说“MongoDB 用此配置可线性化”可执行。  **Q3.** 一家初创公司在 Cassandra 和 ScyllaDB 之间为分析工作负载选择。对两者应用诊断方法，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但...
  c11_b09.md: ...公司在 Cassandra 和 ScyllaDB 之间为分析工作负载选择。对两者应用诊断方法，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但用 C++ 编写，采用 shard-per-core。协议家族相同，实现不同；比较性能、...
  c11_b09.md: ...| 可调；低一致性 PA/EL，quorum PC/EC  差异在实现：  Aspect | Cassandra | ScyllaDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-cor...
  c11_b09.md: ...目更老） | 成长中；Cassandra 兼容 API Cost | 开源；众多云托管 offering | 开源 + ScyllaDB Cloud  对分析工作负载（通常高吞吐、可容忍最终一致）的综合：  若 (a) 每节点吞吐重要（希望更少但更强大的节...
  c11_b09.md: ...吞吐重要（希望更少但更强大的节点）、(b) GC 停顿是问题（延迟敏感分析）、(c) 运维团队熟悉 C++ 栈调试，选 ScyllaDB。若 (a) 团队已深度熟悉 Cassandra、(b) 更广泛的生态重要（现有 Cassandra-aware 工具）...
  c11_b09.md: ...) 每节点吞吐不是瓶颈，选 Cassandra。  对没有历史包袱的新创公司，2026 年新分析工作负载的默认很可能是 ScyllaDB。10 倍每节点吞吐优势意味着更少节点、更简单拓扑、更低成本。诊断方法显示（两系统逻辑画像相同）切换实现不会丢失任何东西...
  c12_b03.md: ...年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被 Cassandra、RocksDB、ScyllaDB、LevelDB 等采用。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先...
  c12_b03.md: ...ocksDB（CockroachDB、TiKV、YugabyteDB、MongoDB WiredTiger 可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：...
  c14_b05.md: ...oachDB（任何云）、Snowflake（OLAP 任何云） 大规模水平扩展，可接受最终一致 | Cassandra、ScyllaDB、DynamoDB  第11章的诊断方法缩小候选范围；此矩阵是把工作负载类别映射到系统的起点。  **对表中未列工作负载...
  c14_b10.md: ...新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载很 fit。  (c) **Cassandra/ScyllaDB** ：可调一致性。ONE 一致性读非常快；QUORUM 写提供一定持久性保证。线性扩展到多节点。  (d) **CRD...
  c30_b11.md: ...迟敏感；大多数读可接受最终一致。  **决策**：DynamoDB Global Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB Global Tables（AWS 原生、...
  c30_b11.md: ...erational 存储**：DynamoDB Global Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CDN**：Cloudflare 或 Fastly 用于静态内容路径。 * **缓存**：区域...

=== runbook (13 occurrences) ===
  c12_b01.md: ...000 笔事务。  这是课程大纲里的工作流 7：无停机重分片迁移。Anya 之前做过三次。协议在她脑中；运维纪律在她的 runbook 里。本章将走一遍，并介绍使之可能的底层基础（哈希 vs 范围分片、一致哈希、LSM-tree）。   ### 12.1...
  c12_b05.md: ...Raft group 继续服务（per-range 多数不受影响），因此用户可见影响只是短暂降级。  纪律是运维层面的：runbook 是多年移动略出问题的经验结晶。Anya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6...
  c12_b05.md: ...影响），因此用户可见影响只是短暂降级。  纪律是运维层面的：runbook 是多年移动略出问题的经验结晶。Anya 的 runbook 有 47 项检查清单；她逐项勾选。   ### 12.6 B+ Tree 作为替代  LSM-tree 主导现代分布式...
  c19_b07.md: ...。缓解：切换前做负载测试。  * **运维学习曲线**：团队需要学习 etcd 运维。缓解：过渡期间配对运维值班、更新 runbook。  总时间线：有意义迁移 3-6 个月；较小部署更快。Kafka KRaft 迁移（Confluent 的经验）是大型...
  c21_b04.md: ...) 预热 merchant 数据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证修复。  事后复盘模式：识别触发器、...
  c21_b10.md: ...a 变更触发器、重试风暴放大、缺失熔断器、自动扩缩慢。修复：linter 规则、降低重试预算、加熔断器、预热容量、更新 runbook。  第22章进入更难的事故类别：亚稳态故障与灰色故障。Bronson 和 Aguilera 2021 年的论文形式化了...
  c22_b10.md: ...践围绕亚稳态成熟。Bronson-Aguilera 论文提供框架；公司有内部亚稳态 playbook；“系统卡住” 的 runbook 是标准。随着意识提高，灾难性亚稳态故障（Roblox 规模的多天中断）频率已下降。  第二个考虑：AI 辅助运维（LL...
  c23_b04.md: ...ay：模拟 US-East 数据库 leader 故障（区域内故障转移，更小爆炸半径）。 * 更新 “区域数据库故障” runbook，加入观察到的行为。  Game day 是成功的。它发现真实问题；团队学到东西。下一次 game day 将验证修复并...
  c23_b07.md: ...。  Nora Jones 的无责复盘实践是运营体现：讨论聚焦系统性原因，而非个人错误。“配置为什么是这样？” 导向 “runbook 不清楚” 或 “默认值错了”——可行动。“谁设置的配置？” 导向防御性。  文化改变很难。习惯责备文化的团队需要时间来...
  c23_b09.md: ...移的 game day。走一遍她在当天前需要回答的设计问题。  提示  稳态假设、爆炸半径、回滚计划、参与者、成功标准、runbook 更新。  **答案：** 设计问题：  (a) **注入什么故障？** 区域故障可多种建模：整个区域关闭（所有节点终止...
  c23_b09.md: ...) **监控是否到位？** 所选 SLI 的实时面板；需要回滚时触发的告警；每个操作的日志。  (i) **会产生哪些 runbook 更新？** Game day 会暴露 runbook 缺口；捕获流程是什么？  (j) **沟通计划是什么？** 之前...
  c23_b09.md: ...需要回滚时触发的告警；每个操作的日志。  (i) **会产生哪些 runbook 更新？** Game day 会暴露 runbook 缺口；捕获流程是什么？  (j) **沟通计划是什么？** 之前/期间/之后通知谁？如何向更广泛工程组织分享学习？...
  c28_b10.md: ...分析 | Ch.21 §21.7 | 事件：触发 + 放大 + 传播 + 恢复 | 行动项：重试预算、熔断器、容量缓冲、runbook | 事后分析模板 + 结构防御的五 Whys | “我们增加了超时” 却不改变类别；遗漏亚稳态反馈环；指责导向复盘 |...

=== merchant (13 occurrences) ===
  c13_b07.md: ...end confirmation         N-->>O: sent         O->>A: update merchant aggregate         A-->>O: updated         O-->>C: success...
  c21_b04.md: ...降级 47 分钟；窗口期内 12% 支付交易失败。  **触发器**：2026-03-14 14:23 UTC 部署了 merchant 数据库 schema 变更。该变更引入的索引触发了一次长时间迁移；迁移把一张热表锁定 90 秒。  **放大**：...
  c21_b04.md: ...。该变更引入的索引触发了一次长时间迁移；迁移把一张热表锁定 90 秒。  **放大**：  * 90 秒锁定期间，触及 merchant 表的查询超时。 * 支付服务的重试逻辑（指数退避 5 次重试）把 merchant 数据库负载放大 3 倍。 * me...
  c21_b04.md: ...*：  * 90 秒锁定期间，触及 merchant 表的查询超时。 * 支付服务的重试逻辑（指数退避 5 次重试）把 merchant 数据库负载放大 3 倍。 * merchant 数据库 CPU 饱和；甚至无法处理非 merchant 表查询。 *...
  c21_b04.md: ...nt 表的查询超时。 * 支付服务的重试逻辑（指数退避 5 次重试）把 merchant 数据库负载放大 3 倍。 * merchant 数据库 CPU 饱和；甚至无法处理非 merchant 表查询。 * 依赖 merchant 数据库的其他服务（通知服...
  c21_b04.md: ...5 次重试）把 merchant 数据库负载放大 3 倍。 * merchant 数据库 CPU 饱和；甚至无法处理非 merchant 表查询。 * 依赖 merchant 数据库的其他服务（通知服务、分析聚合器）开始超时并重试。 * merchant...
  c21_b04.md: ...库负载放大 3 倍。 * merchant 数据库 CPU 饱和；甚至无法处理非 merchant 表查询。 * 依赖 merchant 数据库的其他服务（通知服务、分析聚合器）开始超时并重试。 * merchant 数据库总负载峰值达正常 8 倍；集群自...
  c21_b04.md: ...merchant 表查询。 * 依赖 merchant 数据库的其他服务（通知服务、分析聚合器）开始超时并重试。 * merchant 数据库总负载峰值达正常 8 倍；集群自动扩缩触发，但新节点需要 5 分钟才能就绪。  **恢复**：  * 14:35...
  c21_b04.md: ...峰值达正常 8 倍；集群自动扩缩触发，但新节点需要 5 分钟才能就绪。  **恢复**：  * 14:35，运维手动在 merchant 数据库上调用 load shedding（只接受最高优先级流量）。 * 这让已在 flight 的查询完成；数据库开始...
  c21_b04.md: ...*：  (1) 为非常规索引创建添加 linter 规则。(2) 把所有服务重试预算降到 10%。(3) 在所有服务与 merchant 数据库之间添加熔断器。(4) 预热 merchant 数据库 spare capacity。(5) 改进 “merch...
  c21_b04.md: ...则。(2) 把所有服务重试预算降到 10%。(3) 在所有服务与 merchant 数据库之间添加熔断器。(4) 预热 merchant 数据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进...
  c21_b04.md: ...chant 数据库之间添加熔断器。(4) 预热 merchant 数据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证...
  c21_b04.md: ...修复。  事后复盘模式：识别触发器、放大和防御缺失。每一项都是独立修复。防御缺失（上面 c）通常是最杠杆的；若有熔断器，merchant 数据库的 CPU 尖峰不会扩散到其他服务。   ### 21.8 练习...

=== chandy (13 occurrences) ===
  c14_b13.md: ...、浏览追踪、协作文档）是正确答案，强一致成本过高。CRDT 是以无冲突意外的方式实现最终一致性的原则性方法。本章还涵盖 Chandy-Lamport 分布式快照，这是理解 Flink/checkpoint 系统的基础。  ---...
  c15_b01.md: ...。  艺术在于知道 _哪些_ 工作负载需求更弱，以及 _弱多少_。本章讲授实用分类法：最终一致何时正确、有哪些会话保证、Chandy-Lamport 分布式快照如何让你对异步系统的一致状态进行推理。   ### 15.1 最终一致何时是正确答案  三类...
  c15_b02.md: ...WW 是最常见默认因为它最简单。代价是时钟偏移问题：一个逻辑上更晚的写可能因写节点时钟慢而输掉。   ### 15.4 Chandy-Lamport 分布式快照  一个基础原语：如何在系统仍在运行时拍下一致快照？  Chandy 和 Lamport 1...
  c15_b02.md: ...### 15.4 Chandy-Lamport 分布式快照  一个基础原语：如何在系统仍在运行时拍下一致快照？  Chandy 和 Lamport 1985 年论文引入了一个协议，无需全局时钟即可产生代表一致全局状态的快照。   #### 场景...
  c15_b03.md: ...消息）是一个一致快照：它可能在某个真实时刻就是系统状态，尽管没有命名某个具体时刻。   #### 2026 年重要性  Chandy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 check...
  c15_b03.md: ...式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “checkpoint barr...
  c15_b03.md: ...actly-once 语义建立于此。  * **某些实现的分布式事务** ：特定时间戳处系统的 “快照” 概念上是一个 Chandy-Lamport 快照，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark R...
  c15_b06.md: ...绝哪些）的操作可最终一致。任何业务含义取决于基于操作发生时刻状态拒绝某些操作的操作需要线性化。  **Q3.** 描述 Chandy-Lamport 快照协议。记录的状态代表什么？  提示  每条通道 marker；每个进程首次收到 marker 时记...
  c15_b07.md: ...这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “l...
  c15_b08.md: ...ink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障...
  c15_b09.md: ...之间的时间）就是 checkpoint 间隔（通常 1-30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照，exactly-once 不可能：系统无法捕获重启的一致状态。该协议是状态流处理整个类别的基...
  c15_b11.md: ...弱化版本。CockroachDB、MongoDB、YugabyteDB 通过 HLC-aware 会话跟踪提供它们。  Chandy-Lamport 分布式快照是在无全局时钟下捕获一致全局状态的基础原语。协议使用每条通道上的 marker 定义快照边界...
  c30_b02.md: ...e Systems." _SIGMOD_.   #### 最终一致性与 CRDT（第五部分 — 第15-17章）  * Chandy, K. M., & Lamport, L. (1985). "Distributed snapshots: deter...

=== endpoint (13 occurrences) ===
  c19_b04.md: ...的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Map 暴露；全球客户解析到最近的区域负载均衡。  这四个系统分层。每个处理自...
  c20_b07.md: ...设施变更）。若不是，问题是持续性的。  (d) **检查请求 attributes**：失败请求是否针对特定租户、特定 endpoint、特定输入模式？找输入相关性。  (e) **钻取一条失败 trace**：选一条失败 trace，看错误前的 span...
  c22_b01.md: ...二十分钟后她找到问题：一个节点的磁盘静默降级。原本 1 ms 的读取现在要花 100 ms。健康检查是廉价的仅 RAM endpoint——从不触碰磁盘，所以它在 1 毫秒内返回 200。负载均衡器信任 200，继续把流量路由到慢节点，客户看到挂起。  这...
  c22_b01.md: ...示例：  (a) **慢节点**。节点磁盘降级；原本 1 ms 的读取现在 100 ms。健康检查是廉价的仅 RAM endpoint；快速返回 200。应用请求触及磁盘时变慢；用户体验挂起。负载均衡器继续路由流量到慢节点。  (b) **内存压力**。...
  c22_b05.md: ...窗口。  示例：  (a) **廉价健康检查的慢节点**：节点磁盘降级；读取比正常慢 100 倍。健康检查是仅 RAM endpoint；快速返回 200。负载均衡器继续路由流量；用户体验慢请求。  (b) **双峰延迟的内存压力**：节点内存 90%；G...
  c22_b10.md: ...测和干预。  **Q2.** 一个 LLM 被要求 “为我们的服务设计健康检查”。它产出一个简单 `/healthz` endpoint 返回 200。指出为什么这在生产中是错的，正确的健康检查应包含什么。  **讨论：** 简单 `/healthz` 返...
  c22_b10.md: ...30-60 秒；区分 “启动中” 与 “坏了”。允许启动期间更长超时。  (d) **Detailed health endpoint**（给运维的 `/health/details`）：每个子系统状态的 JSON 输出。用于诊断；不用于自动路由。  (...
  c24_b09.md: ...限制为 N token（足够小以不阻塞其他请求）。拒绝更大 prompt 并返回结构化错误；调用者必须分块或使用不同 endpoint。  (e) **Speculative decoding（第26章）**：对长生成，speculative decod...
  c26_b09.md: ...tion 或类似。  (e) **限制每请求最大输出长度**：限制 max_tokens；拒绝极端限制请求或路由到特殊 endpoint。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “...
  c27_b08.md: ...是的，加上 timeout，正确值是 X”，要么是反驳 “这是故意的；这是不应 timeout 的 streaming endpoint；AI 没看到上下文。” 这种阐明是审查者的贡献。  价值下降的技能：做 AI 的工作（肉眼扫描缺失 timeout）。...
  c28_b04.md: ...(10) 事务的 coordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4 防御）。5 ms。  (11) API 服务向 Kafka 发布 “payment.com...
  c28_b07.md: ...ms；与 §28.2 记录一致），得益于分离式架构和 KV-cache-aware 路由。集成只是一个简单 HTTP endpoint；Crescent 的 API 服务调用我们的 endpoint；我们返回分数。复杂度在我们这边；Crescent 这边...
  c28_b07.md: ...ache-aware 路由。集成只是一个简单 HTTP endpoint；Crescent 的 API 服务调用我们的 endpoint；我们返回分数。复杂度在我们这边；Crescent 这边微不足道。”  四个视角说明 2026 现实：复杂分布式系统由组...

=== parent (13 occurrences) ===
  c20_b01.md: ...ill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（非并行）；它们时长相加约等于根总时长（65 + 22 + 80 +...
  c20_b01.md: ...ce ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * At...
  c20_b01.md: ...ill:#fff4e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（非并行）；它们时长相加约等于根总时长（65 + 22 + 80 +...
  c20_b01.md: ...ce ID（每个请求唯一；整个 trace 所有 span 共享）。 * Span ID（每个 span 唯一）。 * Parent span ID（根为 null）。 * 时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * At...
  c20_b05.md: ...an 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录...
  c20_b05.md: ...trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 o...
  c20_b05.md: ...span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至...
  c20_b05.md: ...pan 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪...
  c20_b05.md: ...an 携带哪些信息？  提示  Trace 是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录...
  c20_b05.md: ...trace 内所有 span 共享；全局标识 trace。 * **Span ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 o...
  c20_b05.md: ...span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace 和 parent-span ID 组装，形成可视化显示的树结构。  **Q2.** 为什么跨服务边界传播 trace context 至...
  c20_b05.md: ...pan 无法链接到父 span；trace 会碎片化。  **答案：** Trace context（trace ID、parent span ID、采样决策）是父 span 和子 span 之间的链接。没有传播：  * 接收请求的服务不知道请求属于哪...
  c20_b10.md: ...在微服务系统中路径的取证记录。Trace 是 spans 树；每个 span 代表一个工作单元；树通过 trace 和 parent-span ID 组装。  三个分布式系统特有关注点：因果保持（跨服务边界传播 context）、分区下采样（head-...

=== head (13 occurrences) ===
  c20_b02.md: ...采样决策通常在根 span 做出：若根 span 被采样，则所有子 span 也被采样（trace 完整）。这是 “head-based” 采样。  分区下，采样决策传播可能丢失。部分 span 被采样，其余没有；trace 不完整。修复：采样...
  c20_b05.md: ...显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。...
  c20_b05.md: ...于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：**...
  c20_b05.md: ...显式传播 context。没有这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。...
  c20_b05.md: ...于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：**...
  c20_b06.md: ...慢 trace；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数...
  c20_b06.md: ...础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-b...
  c20_b06.md: ...**Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误...
  c20_b06.md: ...部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为...
  c20_b06.md: ...慢 trace；可基于结果选择。缺点：需要缓冲基础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数...
  c20_b06.md: ...础设施（通常几分钟的 trace 数据）；更复杂；数据可查询的延迟高于 head-based。  何时适用：  * **Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-b...
  c20_b06.md: ...**Head-based**：简单部署；统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误...
  c20_b06.md: ...部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为...

=== headers (13 occurrences) ===
  c20_b02.md: ...rent span ID、采样决策）必须从每个父 span 传播到每个子 span，跨服务边界。  标准传播：HTTP headers（W3C 标准的 `traceparent`、`tracestate`）或 gRPC metadata。服务接收请求时从...
  c20_b02.md: ...W3C 标准的 `traceparent`、`tracestate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。  陷阱：  * **异步边界 con...
  c20_b02.md: ...text，会产生 “断裂” trace（下游工作无父节点）。修复：通过队列传播 trace context（Kafka headers、SQS message attributes）。  * **协议转换时 context 丢失**：服务从 HTTP 转...
  c20_b05.md: ...和所有下游工作）的 trace，而是许多断开的片段。调试变得不可能，因为你无法跟随请求路径。  传播通常通过 HTTP headers（W3C 标准的 `traceparent`）或消息队列元数据（Kafka headers、AWS SQS attrib...
  c20_b05.md: ...。  传播通常通过 HTTP headers（W3C 标准的 `traceparent`）或消息队列元数据（Kafka headers、AWS SQS attributes）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。...
  c20_b08.md: ...ce B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.propagate...
  c20_b08.md: ...。生产端：       from opentelemetry.propagate import inject      headers = {}     inject(headers)     producer.send(topic, value=mes...
  c20_b08.md: ...inject(headers)     producer.send(topic, value=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为 `tracepa...
  c20_b08.md: ...ct(headers)     producer.send(topic, value=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为 `traceparent` 和（...
  c20_b08.md: ...W3C 标准将当前 trace context 作为 `traceparent` 和（可选）`tracestate` headers 插入。消费端：       from opentelemetry.propagate import extract...
  c20_b08.md: ...消费端：       from opentelemetry.propagate import extract      headers = {k: v for k, v in record.headers}     context = extract(h...
  c20_b08.md: ...gate import extract      headers = {k: v for k, v in record.headers}     context = extract(headers)     with tracer.start_as_cu...
  c20_b08.md: ...", context=context):         process(record)   `extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send span 的子 span；t...

=== vendor (13 occurrences) ===
  c20_b03.md: ...ry：标准  2026 年，OpenTelemetry 是分布式追踪仪器的事实标准。关键特性：  * 每种主流语言都有 vendor-neutral API。 * 常见库自动仪器（HTTP 客户端、数据库驱动、消息队列）。 * 可导出到任意后端（Jae...
  c20_b03.md: ...标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeli...
  c20_b03.md: ...细节（Series IV Vol 3 会讲）。要点：OpenTelemetry 是现代默认；使用它可消除旧追踪系统的 vendor lock-in 顾虑。...
  c20_b09.md: ...28-2030 年会成为标准工具。  **Q2.** 一个 LLM 被要求 “给我们的系统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论...
  c20_b09.md: ...er）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetry，然后导出到配置的后端。后端可通过配置切换，不用改代码...
  c20_b09.md: ...etry，然后导出到配置的后端。后端可通过配置切换，不用改代码。       # 使用 OpenTelemetry 的 vendor-neutral 写法：     from opentelemetry import trace     tracer...
  c20_b09.md: ...K；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTelemetry 基础之上再提供该后端的具体配置。...
  c20_b10.md: ...try、Prometheus、structured logging、environment-based config）。Vendor lock-in 是真实且可避免的成本；忽略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布...
  c20_b10.md: ...context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor loc...
  c20_b10.md: ...言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；...
  c20_b10.md: ...try、Prometheus、structured logging、environment-based config）。Vendor lock-in 是真实且可避免的成本；忽略这点的 LLM 生成代码会让团队失去迁移能力。   ### 本章总结  分布...
  c20_b10.md: ...context（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor loc...
  c20_b10.md: ...言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；...

=== path (12 occurrences) ===
  c01_b02.md: ...的每三个请求丢一个。  单机模型把失败当作异常。分布式模型把部分失败当作默认模式。如果你写分布式代码只处理 happy path、把失败当作不寻常的例外，那你写的代码终将在某一天让 Adaeze 被重复扣款。   ### 1.2 分布式推理为何不同...
  c01_b03.md: ...是否假设它正在对话的节点就是它所认为的那个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否有任何代码...
  c01_b03.md: ...是它所认为的那个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否有任何代码路径假设“这不会发生”？...
  c01_b03.md: ...3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否有任何代码路径假设“这不会发生”？  如果你能回答这三个问题，...
  c01_b09.md: ...它。在网络分区或 GC 暂停下，两个节点可能都相信自己持有锁，两者都会作为领导者行动。  _失败_ ：只有 happy path、把失败当作异常的代码。示例：`result = remote_service.call(); update_local...
  c06_b01.md: ...倍）。真实光缆要绕行障碍物，洲际路线的有效速度更接近 150,000 km/s。  单程延迟下限示例（往返翻倍）：  Path | Distance (km) | Floor (ms) ---|---|--- 同一机架 | 0.001 | < 0...
  c06_b03.md: ...eaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做负载均衡。  Leaf...
  c27_b01.md: ...角色。假设 “领导者” 而不经协议强制唯一性的代码是脆弱的。  (c) **故障**：部分故障是例外。只有 happy path 和 error path 的代码把故障当作罕见；现实是部分故障是默认。  Thread C 的六个谎言，作为贯穿第1-...
  c27_b01.md: ...不经协议强制唯一性的代码是脆弱的。  (c) **故障**：部分故障是例外。只有 happy path 和 error path 的代码把故障当作罕见；现实是部分故障是默认。  Thread C 的六个谎言，作为贯穿第1-2章的前瞻引用（冷钩子章节...
  c27_b01.md: ...cing token 的 Redlock。假设锁持有者唯一的代码。    5. **部分故障罕见**。只有 happy path 的代码；不考虑 “半成功” 结果。    6. **CAP 分类简单**。说 “我们用 CP 系统” 而不理解 PAC...
  c27_b04.md: ...service.notify(user.id, 'updated')   谎言：三个调用一起成功。代码只有 happy path；部分故障是隐含的。  现实：调用相互独立；任何都可能失败。`primary_db.update()` 成功但 `cac...
  c27_b09.md: ...风格代码假设锁持有者唯一；缺少 fencing token。    5. **部分故障罕见**：代码只有 happy path；不考虑 “半成功” 结果；无 saga、无补偿动作。    6. **CAP 分类简单**：代码说 “我们用 CP 系...

=== order (12 occurrences) ===
  c04_b03.md: ...r_layer.deliver(deliverable.msg)   性质（在可靠广播之外）：  * **Causal order** ：若 broadcast(m_1) \to broadcast(m_2)，则每个正确进程先交付 m_1 再交付 m...
  c04_b04.md: ...息都可能交互的应用（更新共享状态），需要原子广播。   ### 4.6 原子（全序）广播（Atomic / Total-Order Broadcast）  四层中最强。原子广播新增：_所有正确进程以相同的总顺序交付消息。_  该总顺序与因果顺序一致（...
  c04_b04.md: ...（Paxos、Raft、ZAB、HotStuff），第三部分再展开。  性质（在因果广播之外）：  * **Total order** ：所有正确进程以相同顺序交付消息。  代价：任何原子广播协议的下界至少是共识的代价，在部分同步假设下每个决定需要多...
  c04_b05.md: ...er)"| C[Uniform reliable broadcast]         C -->|"+ causal order<br/>(vector clocks)"| D[Causal broadcast]         D -->|"+...
  c04_b05.md: ...vector clocks)"| D[Causal broadcast]         D -->|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus"]...
  c04_b05.md: ...v     Uniform reliable broadcast         |         + causal order (vector clocks)         v     Causal broadcast         |...
  c04_b05.md: ...s)         v     Causal broadcast         |         + total order (consensus)         v     Atomic broadcast == Consensus   每...
  c04_b07.md: ...orm reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast**...
  c04_b07.md: ...、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：validity（发...
  c04_b07.md: ...broadcast** ：新增 _total order_ ——所有正确进程以相同总顺序交付消息（且与 causal order 一致）。  原子广播等价于共识。  **Q2.** 为什么原子广播等价于共识？简述两个方向的归约。  提示  原子广播...
  c04_b07.md: ...槽运行一次共识。  **答案：** _原子广播 → 共识_ 。假设有原子广播。每个进程广播自己的提议值。由 total-order 性质，所有正确进程以相同顺序交付这些广播。每个进程决定其交付的第一个广播所包含的值。由总序，所有正确进程交付相同的第一...
  c05_b02.md: ...的 per-tablet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leade...

=== add (12 occurrences) ===
  c04_b03.md: ...nder):         if msg not in received:             received.add(msg)             for peer in all_processes:...
  c04_b03.md: ...(msg))      on receive(ACK(msg), sender):         acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg not in deli...
  c04_b03.md: ...msg]) > N/2 and msg not in delivered:             delivered.add(msg)             upper_layer.deliver(msg)   性质（在可靠广播之外）：  *...
  c04_b03.md: ...msg, V)      on receive(msg, V_msg, sender):         buffer.add((msg, V_msg, sender))         try_deliver_from_buffer()...
  c16_b02.md: ...#### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑战：如何支持能正确收敛的 add 和 remove 操作。  朴素方法（带 remove 的 G-Set）失败：若 A 添加元素 \(x\)，B 并发移...
  c16_b02.md: ...合并结果必须决定谁赢。使用 tombstone（被移除元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解...
  c16_b02.md: ...元素被标记而非删除）有帮助，但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot...
  c16_b02.md: ...但产生 “remove-before-add” 问题：若 B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定...
  c16_b02.md: ...B 的 remove 在 A 的 add 到达某副本前先到达，add 会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombsto...
  c16_b02.md: ...OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：...
  c16_b02.md: ...针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Rem...
  c16_b02.md: ...tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在 tombstone 全局已知后做垃圾...

=== closed (12 occurrences) ===
  c04_b06.md: ...序交付；消费者组提供 at-least-once。 * **CockroachDB 的 Rangefeed** 提供带 closed-timestamp 保证的因果有序变更事件。  选择系统时正确的问题不是“它提供哪一层广播？”而是“它对我关心的操作提...
  c19_b08.md: ...“fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail closed”（Consul 不可达时返回错误），每次服务调用在选举窗口内失败。  修复：  (a) **配置客户端使用 stale...
  c21_b02.md: ...即失败（不再联系下游）。  三状态可视化：       stateDiagram-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/...
  c21_b02.md: ...状态可视化：       stateDiagram-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over...
  c21_b02.md: ...cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --> Closed: probe calls succeed<br/>(e.g. 80% over 5s)         HalfOpe...
  c21_b02.md: ...ver 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/>track success/failure rate...
  c21_b02.md: ...obe calls allowed;<br/>test if downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c)...
  c21_b02.md: ...有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止增加负载。下游有机会恢复。当检测到恢复（通过 half-ope...
  c21_b05.md: ...etry policy 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b06.md: ...对下游服务调用的成功率。若失败率超过阈值，熔断器 “打开”，后续调用立即失败，不再联系下游。  三状态：  (a) **Closed**：正常操作。所有调用都到下游。熔断器跟踪成功率。  (b) **Open**：失败阈值被超过（例如最近 30 秒 5...
  c21_b06.md: ...pen**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。  收益：当下游失败时，上游停止加负载。这给下游恢复机会。当检测到恢复（通过 Half-ope...
  c21_b06.md: ...流量恢复。  权衡：Open 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用都比没有好），Closed-with-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netfl...

=== brain (12 occurrences) ===
  c05_b10.md: ...”  **讨论：** 辩护：Raft-quorum 对已提交写给 RPO = 0，故障切换自动，按构造避免 split-brain。延迟成本真实但有界；对大多数工作负载可接受。  反驳：异步复制仍适合许多工作负载。偶尔丢失可接受的日志流水线不需要 R...
  c05_b10.md: ...任意副本并后台传播的系统。指出三种失败模式，并解释为什么这并非真正高可用。  **讨论：**  (a) **Split-brain 写。** 两个写者可向不同副本写冲突值。没有协调者排序，没有法定人数强制相交。副本分歧；冲突解决（如有）会静默丢失其中...
  c08_b11.md: ...(e) **重配置。** 增减节点需要协议层谨慎协调。LLM 生成的代码通常根本不实现重配置，或以允许 split-brain 的天真方式实现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/...
  c11_b02.md: ...ssip 检测节点故障（默认 10-30 秒）。故障窗口内被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防...
  c11_b02.md: ...被排除在法定人数外；系统用存活节点继续。 * **Split-brain 防止** ：多数共识系统自动防止 split-brain（少数侧拿不到法定人数）。无 leader 系统通过法定人数防止（$W + R > N$）。单 leader 系统（Po...
  c11_b02.md: ...系统通过法定人数防止（$W + R > N$）。单 leader 系统（Postgres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需要人工提升副本。RTO 以小时计。  故障恢复时间决定可用性 S...
  c27_b09.md: ...网络行为。(c) 不引入时间相关故障（时钟偏移、GC 暂停、重试以异常顺序到达）。(d) 不测试多副本场景（split-brain、partition、failover）。  分布式系统 bug 需要：  (a) 真实或带真实故障注入的模拟网络（To...
  c27_b09.md: ...条件下的长时间运行测试。  单元测试遗漏的 bug：  * 网络抖动 + 重试下的重复扣款。 * 分区下的 split-brain。 * 领导者 failover 下的丢失写。 * 网络恢复后的 stale read。 * 负载下的级联故障。...
  c27_b15.md: ...码默认嵌入谎言。Bug 沉默（不抛异常），单元测试不可见，只在生产故障场景下可见。成本高：重复扣款、丢失写、split-brain、级联故障。  审查清单（10 项）：幂等性、超时、带预算重试、熔断器、排序原语（HLC 而非 wall clock）、...
  c28_b14.md: ...oni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-brain 可能需要人工干预。 * **一致性模型**：事务内快照隔离；副本读最终一致。 * **分区行为**：CP——少数派无法...
  c30_b14.md: ...ey-value store on top     │   └── tests/            — split-brain, partition, recovery scenarios     ├── ch12_lsm/     │   └─...
  c30_b19.md: ....3，Ch.5 §5.4（同步 vs 异步）   分区愈合期间可见两个 leader 的写入 span | Split-brain；无共识的 failover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Serv...

=== reads (12 occurrences) ===
  c05_b04.md: ...1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writes-fol...
  c05_b04.md: ...后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writes-follow-reads** ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。 * **Monotonic...
  c11_b02.md: ...ABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。 * **E...
  c14_b07.md: ...tency model | Linearizable；提供过期读 | Serializable；提供 follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestamp sour...
  c14_b10.md: ...layer 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7 万 reads/sec。大多是 “top-N 排行榜”，偶尔 “我的分数”。 * **一致性**：因果足够（若玩家 A 提交分数后玩家...
  c15_b01.md: ...(a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes-follo...
  c15_b01.md: ...：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes-follow-reads** ：会话发出的任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X 之后。  (d) **M...
  c15_b06.md: ...** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **...
  c15_b06.md: ...要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-wr...
  c17_b01.md: ...端跟踪见过的最高操作时间戳；同会话后续读要求服务器至少赶上该时间戳。 * **CockroachDB follower reads** ：从 follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with explici...
  c17_b08.md: ...读。  (d) **切换到具有 HLC-aware 多区域语义的系统**：CockroachDB 的 follower reads 或 YugabyteDB 的类似特性；读可从更近副本做有界过期，无需因果会话等待。  对 MongoDB，(a) 和...
  c25_b01.md: ...Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可能只用到 50% 计算和 50% 内存带宽：从未饱和任一。浪...

=== recovery (12 occurrences) ===
  c05_b06.md: ...。  恢复选项：  * 若主库磁盘可恢复，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入永久丢失。团队必须调和下游影响（退...
  c05_b06.md: ...。  恢复选项：  * 若主库磁盘可恢复，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入永久丢失。团队必须调和下游影响（退...
  c11_b03.md: ...每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failure-recovery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lea...
  c11_b03.md: ...Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间戳 Failure-recovery | Raft leader 重选（亚秒到数秒）；per-range 领导权转移 Consistency model |...
  c11_b04.md: ...|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Failure-recovery | 透明（存储层对用户不透明）；自动扩缩容与自动修复 Consistency model | 默认最终一致；按请求可提...
  c11_b04.md: ...cation protocol | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistency model |...
  c11_b04.md: ...ol | 自定义（sequencer + transaction-log + storage）；强一致 Failure-recovery | 快（亚秒级 sequencer 切换）；自设计日起就以混沌工程为目标 Consistency model | Se...
  c11_b05.md: ...protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery | Per-range Raft 重选（~1 秒）；通过共识实现区域故障切换 Consistency model |...
  c11_b08.md: ...| 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 view-change；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency mo...
  c11_b09.md: ...者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Par...
  c14_b07.md: ...eplication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos leader 切换（~数秒） | Raft leader 切换（~数秒） Consistency mo...
  c30_b14.md: ...top     │   └── tests/            — split-brain, partition, recovery scenarios     ├── ch12_lsm/     │   └── tiny_lsm.py       —...

=== gst (12 occurrences) ===
  c07_b02.md: ...tabilization Time_（GST），一个未知时刻，此后消息交付被某个未知但有限的延迟限制。  非对称很重要：GST 前系统完全异步（无界）；GST 后系统同步（有界延迟）。协议在异步下必须保持 _safe_（agreement 和 v...
  c07_b02.md: ...后系统同步（有界延迟）。协议在异步下必须保持 _safe_（agreement 和 validity 永不违反），并在 GST 后最终 _live_（在 GST 后有限时间内保证终止）。  这种划分是现代共识设计模式：把 _safety_（必须始...
  c07_b05.md: ...-- Paxos | 被选定值的多数法定人数 | 值来自提议者广播 | 稳定 leader（Multi-Paxos）在 GST 后终止 Raft | 日志条目的多数法定人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳...
  c07_b06.md: ...三者兼得。选择放弃哪个。  **Q3.** 定义部分同步。它如何让共识协议逃脱 FLP？  提示  部分同步：假设未知 GST 之后消息延迟有界。  **答案：** 部分同步（Dwork、Lynch、Stockmeyer 1988 形式化）：系统...
  c07_b06.md: ...tockmeyer 1988 形式化）：系统有一个未知的 Global Stabilization Time（GST）。GST 前系统完全异步（消息交付和进程速度无界）。GST 后系统同步（消息在未知但有限上界内交付）。  协议在任何条件下（包括...
  c07_b06.md: ...未知的 Global Stabilization Time（GST）。GST 前系统完全异步（消息交付和进程速度无界）。GST 后系统同步（消息在未知但有限上界内交付）。  协议在任何条件下（包括 GST 前异步）必须 _safe_（agreem...
  c07_b06.md: ...。  协议在任何条件下（包括 GST 前异步）必须 _safe_（agreement 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分...
  c07_b06.md: ...括 GST 前异步）必须 _safe_（agreement 和 validity 永不违反）。GST 前可能不终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分同步断言 GST 后不存在...
  c07_b06.md: ...终止，但 GST 后保证在有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分同步断言 GST 后不存在这种调度。GST 后协议基于心跳的故障检测器变得可靠（慢节点有界），协议可以推进。  实践中 GST 是隐含的...
  c07_b06.md: ...有限时间内终止。  如何逃脱 FLP：FLP 不可能性依赖对手构造无限延迟调度。部分同步断言 GST 后不存在这种调度。GST 后协议基于心跳的故障检测器变得可靠（慢节点有界），协议可以推进。  实践中 GST 是隐含的：网络通常表现良好。不好时...
  c07_b06.md: ...同步断言 GST 后不存在这种调度。GST 后协议基于心跳的故障检测器变得可靠（慢节点有界），协议可以推进。  实践中 GST 是隐含的：网络通常表现良好。不好时（分区、拥塞），协议暂停。稳定后恢复。Safety 性质全程成立。  **Q4.**...
  c07_b09.md: ...“P1 消息被永远延迟”，因此不能在没有 P1 输入的情况下安全决定。但等 P1 可能永远等不到。  逃脱：部分同步中 GST 后对手有界。协议可在超时后声明怀疑某进程并无需它做出决定；超时是有界的，因此终止有保证。...

=== bullshark (12 occurrences) ===
  c07_b05.md: ...tStuff 的线性 view change）、新 validity 检查（拜占庭验证者）、新终止机制（Narwhal-Bullshark 等基于 DAG 的协议）。   ### 7.8 真实系统所在  生产共识部署对每种 FLP 逃逸都做出明确选择：  *...
  c07_b05.md: ...nt**（Cosmos 链）用显式轮次超时；生产 1 秒一轮。 * **Aptos / Sui** 用 Narwhal-Bullshark（基于 DAG）获得比经典 BFT 更高吞吐；轮复杂度与消息延迟解耦。  这些系统都在 _任何条件下 safe_。没有一...
  c10_b01.md: ...议仍正确工作。  本章覆盖现代 BFT 协议：HotStuff（2026 年所有 BFT 系统的基础）、Narwhal-Bullshark（DAG mempool 与共识分离）、Tendermint（许多区块链生态的工作马）、Flexible Paxos（H...
  c10_b01.md: ...br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]         Narwhal -...
  c10_b03.md: ...cates，外加 pipelining 提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的 mempool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理...
  c10_b03.md: ...变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基...
  c10_b04.md: ...### 10.4 Narwhal-Bullshark：基于 DAG 的共识  2022 年 Narwhal 论文（Danezis、Kokoris-Kogias、Sonnin...
  c10_b04.md: ...作不再是“排序交易”（DAG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 leader 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DA...
  c10_b04.md: ...s** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff...
  c10_b04.md: ...Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff。  Narwhal-Bullshark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚秒级提交）。复杂度在于 DAG memp...
  c10_b12.md: ...和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生...
  c30_b02.md: ...FT Consensus." _EuroSys_. * Spiegelman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., K...

=== state (12 occurrences) ===
  c08_b04.md: ...Paxos Made Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persistent state** ：acceptor 必须在崩溃间持久化 $b_{max}$、`accepted_ballot`、`accepted...
  c15_b08.md: ...ffset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子状态。重启后...
  c15_b11.md: ...、排行榜（需要因果一致）、库存（需要线性化）。应用工程师的任务是为每个操作选择正确的一致性。  第16章深入 CRDT：state-based vs op-based、典型类型（G-Counter、PN-Counter、OR-Set、LWW-Regi...
  c16_b01.md: ...果。“无冲突” 是结构属性：数据类型的设计从构造上消除冲突。  形式上，设计 CRDT 有两种等价方式：  (a) **State-based CRDT（CvRDT）** ：每个副本持有完整状态。合并时，合并函数取两个状态并产生新状态。合并必须满足：...
  c16_b01.md: ...须保证投递（可靠广播，第4章）。操作必须可交换：不同顺序应用操作产生相同结果。  两种表述表达能力等价；选择是运维性的。State-based 更易实现（只需算合并）但状态可能变大。Op-based 更高效（只发 diff）但要求广播层保证投递。...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并...
  c16_b06.md: ...优点：消息更小（只发 op）；高频更新更高效。缺点：需要可靠广播层；离线操作需要小心 op-log 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Au...
  c16_b06.md: ...#### 概念检测  **Q1.** 定义 CRDT。State-based（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并...
  c16_b06.md: ...优点：消息更小（只发 op）；高频更新更高效。缺点：需要可靠广播层；离线操作需要小心 op-log 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、map）更高效。Yjs 和 Au...
  c16_b11.md: ...DT（Conflict-free Replicated Data Types）是为并发更新下确定性合并而设计的数据结构。State-based CRDT（CvRDT）合并完整状态；operation-based CRDT（CmRDT）广播操作。两者表...
  c30_b14.md: ...│   ├── orchestrator.py   — orchestrated saga with durable state (SQLite)     │   ├── choreographed.py  — choreographed saga...
  c30_b14.md: ...ntation (gRPC-style)     │   ├── circuit_breaker.py — three-state circuit breaker     │   └── load_shedder.py   — adaptive co...

=== prepared (12 occurrences) ===
  c10_b02.md: ...e 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个...
  c10_b02.md: ...2f+1 个 prepare 消息后，进入该值的 “prepared” 状态。 (c) **Commit** ：每个 prepared 副本广播 commit 消息。收集到 2f+1 个 commit 消息的副本决定该值。  为什么需要两轮（prepar...
  c13_b01.md: ..., operations)`。每个参与者：    1. 验证操作（如检查约束、获取锁）。   2. 将操作数据持久化为 prepared（但未提交）状态。   3. 回复 `YES`（可提交）或 `NO`（必须中止）。  **阶段 2：Commit 或 A...
  c13_b01.md: ...Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finalize prepared 数据。  若任一参与者投 NO（或协调者超时），协调者发送 `ABORT(txn_id)`；每个参与者丢弃 prepa...
  c13_b01.md: ...pared 数据。  若任一参与者投 NO（或协调者超时），协调者发送 `ABORT(txn_id)`；每个参与者丢弃 prepared 数据并释放锁。...
  c13_b05.md: ...+ 复制协调者模式之所以有效，是因为每个参与者（分片、tablet）属于同一系统，运行同一协议，并能原子、持久地实现 “prepared” 状态。  当事务跨越 _服务_ 边界时——支付服务调用风控服务、通知服务、合作银行 API——这些假设不成立。参与者...
  c13_b05.md: ...完全不同的公司）运维。 * 某一步可能耗时数分钟或数小时（人工审批、隔夜银行结算）。 * 没有共享事务基础设施来实现 “prepared 但未提交”。  对跨服务工作流，现代模式是 **saga**。...
  c13_b12.md: ...**答案：**  阶段 1（Prepare）：协调者向所有参与者发送 PREPARE。每个参与者验证、将操作持久化为 prepared 状态，并投 YES（就绪）或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者...
  c13_b15.md: ...运维上不可行。 * **松耦合** ：服务不应需要知道彼此的提交状态。saga 的补偿是应用层；2PC 要求参与者实现 prepared 状态。  2PC 是正确答案的情况：  * **同一数据库内跨分片** ：事务在分布式数据库内（Spanner、Coc...
  c13_b16.md: ...不同存储技术（一个 Postgres、一个 MongoDB、一个 Redis）。2PC 要求每个参与者原子、持久地实现 prepared 状态。大多数服务实现没有；LLM 生成代码可能假设统一参与者接口。  (c) **长周期事务**。跨微服务的 2PC...
  c13_b17.md: ...**参与者崩溃后的恢复**。参与者在投 YES 后、收到 COMMIT 前崩溃，恢复时必须记住其投票并一致行动。这需要 prepared 状态的持久化存储。LLM 代码常用内存状态，崩溃后丢失投票。  (f) **超时纪律**。协调者必须对不响应的参与者超...
  c15_b09.md: ...两阶段提交模式：checkpoint 完成时，Flink 通知所有 sink 提交 prepared 事务。若有 sink 提交失败，checkpoint 无效，系统从上一个 checkpoint 重启。  结果：端到端...

=== operational (12 occurrences) ===
  c11_b09.md: ...顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compaction 调优、GC 调优） | 中等（架构已处理大部分） Ecosyste...
  c16_b01.md: ...作文档编辑器。  CRDT 是过去十年分布式状态系统中最具运维重要性的进展。CRDT 出现前，“协作编辑” 需要复杂的 Operational Transformation（OT）算法，出了名的难实现正确（Google Docs 有 10 万行 OT 实现）。C...
  c16_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“CRDT 让 Operational Transformation 过时。2026 年所有新的协作编辑器设计都应使用 CRDT。”  **讨论：** 辩护：...
  c19_b05.md: ...Ecosystem | Kubernetes 原生使用 | Hadoop 生态（Kafka、HBase 历史） | | Operational tooling | etcdctl；云原生 | zkCli.sh；较老模式 | | Performance | 针对现...
  c28_b02.md: ...个 region（US-East、EU-West、US-West）。每个 region 有完整技术栈。  (c) **Operational 存储**：CockroachDB（每 region 12 节点，共 36）。存储账户、交易、商户数据。可串行化事务。...
  c30_b10.md: ...部署；p99 延迟低于 100ms。  **决策**：PostgreSQL 加读副本 + Redis 缓存。  * **Operational 存储**：PostgreSQL 16，带一或两个 hot-standby 读副本（同步到一个，异步到另一个）。 * *...
  c30_b10.md: ...复。  **决策**：CockroachDB 或 Spanner；财务账本用 TigerBeetle 补充。  * **Operational 存储**：CockroachDB（任意云）或 Spanner（仅限 GCP）。按云承诺选择。 * **账本**（如适用...
  c30_b10.md: ...Spanner（仅限 GCP）。按云承诺选择。 * **账本**（如适用）：TigerBeetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，...
  c30_b10.md: ...部署；p99 延迟低于 100ms。  **决策**：PostgreSQL 加读副本 + Redis 缓存。  * **Operational 存储**：PostgreSQL 16，带一或两个 hot-standby 读副本（同步到一个，异步到另一个）。 * *...
  c30_b10.md: ...复。  **决策**：CockroachDB 或 Spanner；财务账本用 TigerBeetle 补充。  * **Operational 存储**：CockroachDB（任意云）或 Spanner（仅限 GCP）。按云承诺选择。 * **账本**（如适用...
  c30_b10.md: ...Spanner（仅限 GCP）。按云承诺选择。 * **账本**（如适用）：TigerBeetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，...
  c30_b11.md: ...策**：DynamoDB Global Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB Global Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（...

=== participant (12 occurrences) ===
  c13_b07.md: ...saga 可视化：       sequenceDiagram         autonumber         participant C as Client         participant O as Orchestrator<br/>(stat...
  c13_b07.md: ...autonumber         participant C as Client         participant O as Orchestrator<br/>(state in CRDB)         participant R...
  c13_b07.md: ...participant O as Orchestrator<br/>(state in CRDB)         participant R as Reservation         participant F as Fraud         par...
  c13_b07.md: ...state in CRDB)         participant R as Reservation         participant F as Fraud         participant B as Bank         participan...
  c13_b07.md: ...ant R as Reservation         participant F as Fraud         participant B as Bank         participant L as Ledger         participa...
  c13_b07.md: ...articipant F as Fraud         participant B as Bank         participant L as Ledger         participant N as Notification         p...
  c13_b07.md: ...rticipant B as Bank         participant L as Ledger         participant N as Notification         participant A as Analytics...
  c13_b07.md: ...t L as Ledger         participant N as Notification         participant A as Analytics         C->>O: initiate payment         O->>...
  c18_b04.md: ...ken 机制可视化：       sequenceDiagram         autonumber         participant C1 as Coordinator A<br/>(token 100)         participant L a...
  c18_b04.md: ...participant C1 as Coordinator A<br/>(token 100)         participant L as Lock Service<br/>(etcd; Raft)         participant Bank...
  c18_b04.md: ...participant L as Lock Service<br/>(etcd; Raft)         participant Bank as Bank<br/>(highest_seen_token)         participant C...
  c18_b04.md: ...participant Bank as Bank<br/>(highest_seen_token)         participant C2 as Coordinator B<br/>(token 101)         C1->>L: acquire...

=== register (12 occurrences) ===
  c16_b02.md: ...N-Counter 可表达任意整数值。它不强制 “值绝不低于零”，因此不能替代库存（第15章）。   #### LWW-Register（Last-Write-Wins 寄存器）  每个值带时间戳（通常用 HLC）。寄存器保存时间戳最高的值。  * 在副本...
  c16_b02.md: ...xt{current timestamp}\)，替换。 * 合并：选时间戳更高的值；平票按副本 ID 打破。  LWW-Register 继承 LWW 的所有问题：时钟偏移可导致 “逻辑上更晚” 的写输掉。适合 “最后更新胜出” 语义正确的字段；不适合必须...
  c16_b02.md: ...“逻辑上更晚” 的写输掉。适合 “最后更新胜出” 语义正确的字段；不适合必须保留每次更新的字段。   #### MV-Register（Multi-Value Register）  不选赢家，而是保留所有并发写。每次写带版本向量；并发写（版本向量互不支配...
  c16_b02.md: ...新胜出” 语义正确的字段；不适合必须保留每次更新的字段。   #### MV-Register（Multi-Value Register）  不选赢家，而是保留所有并发写。每次写带版本向量；并发写（版本向量互不支配）作为 siblings 保留。  * 在...
  c16_b02.md: ...)\}\)；任何版本被 \(V\) 支配的现有条目被移除。 * 合并：保留所有版本不被任何其他版本支配的条目。  MV-Register 把冲突抛给应用：读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（...
  c16_b02.md: ...LWW-Map / OR-Map  由 per-key 寄存器构建的映射（键到值）。LWW-Map：每个值是 LWW-Register。OR-Map：每个键对应一个值的集合（类似 OR-Set）。  对协作文辑，相关的 CRDT 是 _list_（或数组...
  c16_b06.md: ...)。值：3 - 1 = 2。  合并满足交换律、结合律、幂等。任何合并顺序结果相同。  **Q3.** 为什么 LWW-Register 会受到时钟偏移问题影响，有哪些替代方案？  提示  LWW 依赖时间戳；时钟偏移导致错误的 “赢家”。替代：MV-Re...
  c16_b06.md: ...er 会受到时钟偏移问题影响，有哪些替代方案？  提示  LWW 依赖时间戳；时钟偏移导致错误的 “赢家”。替代：MV-Register、OR-Map 等更丰富的类型。...
  c16_b07.md: ...**答案：** LWW-Register 使用 wall-clock（或 HLC）时间戳决定哪个值胜出。若两个副本并发写入，时钟 _更快_ 的副本胜出（无论哪个...
  c16_b07.md: ...dra 的 LWW 冲突解决相同：依赖独立运行时钟的时间戳在偏移下会结构性丢失信息。  替代方案：  (a) **MV-Register（多值寄存器）** ：不选单一赢家，保留所有并发写。应用看到所有存活值（作为 siblings）并决定。不丢数据；冲突解...
  c16_b11.md: ...选择是运维性的。  典型 CRDT：G-Counter（只增计数器）、PN-Counter（支持递减的计数器）、LWW-Register（最后写胜出）、MV-Register（多值）、OR-Set（观察删除集合）、list CRDT（Yjs 使用 RGA...
  c16_b11.md: ...-Counter（只增计数器）、PN-Counter（支持递减的计数器）、LWW-Register（最后写胜出）、MV-Register（多值）、OR-Set（观察删除集合）、list CRDT（Yjs 使用 RGA 衍生；Automerge 使用带完整历...

=== rocksdb (11 occurrences) ===
  c11_b11.md: ...进入下一层：分布式数据库如何跨节点分片数据。哈希分片、范围分片、一致哈希、Bigtable / Cassandra / RocksDB 的 LSM-tree 基础，以及 Anya 在 Stratus DB 集群上无停机重分片的实例。  ---...
  c12_b03.md: ...人于 1996 年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被 Cassandra、RocksDB、ScyllaDB、LevelDB 等采用。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表...
  c12_b03.md: ...026 年，LSM 主导分布式数据库存储底层，因为大多数工作负载写重或写吞吐受限。Bigtable、Cassandra、RocksDB（CockroachDB、TiKV、YugabyteDB、MongoDB WiredTiger 可选使用）、Scylla...
  c12_b03.md: ...认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空...
  c12_b09.md: ...**Q3.** 一个团队为键值存储选择嵌入式存储：RocksDB（LSM）与 SQLite（B+ tree）。工作负载：80% 写、20% 读、随机访问、SSD 上 100 GB 总数...
  c12_b09.md: ...访问：两种系统都无顺序局部性收益。 * 100 GB on SSD：大但还不算巨大；SSD 降低随机 I/O 成本。  RocksDB 画像：  * 写吞吐：非常高（memtable + WAL 顺序）。80% 写工作负载下表现好。 * 读延迟：每次读可...
  c12_b09.md: ...。 * 空间放大：~1 倍（原地更新）。 * 运维简单：SQLite 是最好运维的嵌入式数据库。  推荐：该工作负载用 RocksDB。80% 写占比意味着 RocksDB 的写吞吐优势占主导；即便在 SSD 上，B+ tree 随机写也比 LSM 顺序...
  c12_b09.md: ...。 * 运维简单：SQLite 是最好运维的嵌入式数据库。  推荐：该工作负载用 RocksDB。80% 写占比意味着 RocksDB 的写吞吐优势占主导；即便在 SSD 上，B+ tree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20...
  c12_b09.md: ...占主导；即便在 SSD 上，B+ tree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20% 读占比在 RocksDB 中配合 Bloom filter 做点查仍可接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正...
  c12_b09.md: ...接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正确选择。SQLite “开箱即用”是真实优势；RocksDB 需要调优（compaction 策略、write-buffer 大小、block cache 大小），小团队可能不愿投...
  c30_b06.md: ...，与实时一致。 * **LSM-tree（日志结构合并树）**：写优化的存储底层，Bigtable、Cassandra、RocksDB 使用。 * **Metastable failure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健...

=== log (11 occurrences) ===
  c01_b05.md: ...网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Serie...
  c09_b01.md: ...分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **S...
  c09_b06.md: ...*Q1.** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服...
  c09_b06.md: ...r 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给...
  c09_b06.md: ...*Q1.** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服...
  c09_b06.md: ...r 缺失并启动选举。随机化超时降低平分选票概率。日志最新规则确保当选 leader 拥有所有已提交条目。  (b) **Log replication** ：leader 接受客户端请求，追加到日志，并通过 `AppendEntries` 复制给...
  c12_b03.md: ...(a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b) **Write-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c) **S...
  c12_b04.md: ...拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft group 领导权转移给目标节点，源...
  c12_b05.md: ...少数据。修复是定期重新估算，集群每小时执行。  (b) **流式期间复制滞后** ：若网络饱和，目标节点通过 Raft log replay 追赶耗时超过预期。修复是在移动期间节流其他流量。  (c) **移动期间节点故障** ：源或目标节点故障...
  c16_b06.md: ...何顺序应用产生相同结果。  优点：消息更小（只发 op）；高频更新更高效。缺点：需要可靠广播层；离线操作需要小心 op-log 管理。  实践：state-based 对简单类型（计数器、寄存器）更易；op-based 对复杂类型（list、ma...
  c30_b02.md: ...Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Acta Informatica_. * Pe...

=== sre (11 occurrences) ===
  c01_b07.md: ...LLM 推理）、第27-29章（综合 + 2027-2030 前瞻）。计划每章 2-3 次会话；约 6 周。  * **SRE / 运维工程师** ：优先读第七部分（第20-23章），然后第27章 §27.10（AI 辅助审查分布式代码），然后第...
  c21_b08.md: ...价：服务 mesh 无法服务任何请求；全面中断。  (f) **恢复需要干预**：电力运维必须手动一片片重启电网。等价：SRE 团队必须限流、重启服务、手动重建服务 mesh。  对分布式系统工程师的教训：模式是普遍的。任何具有放大和缺乏遏制的互...
  c22_b03.md: ...团队观察到亚稳态并应用手动限流）。  Bronson 和 Aguilera 的论文列举了模式和防御；它是 2026 年 SRE 风格工程师的必读。   ### 22.4 灰色与亚稳态结合  灰色故障经常先于亚稳态故障。序列：  * 灰色故障开始：...
  c22_b10.md: ...不必要的复杂度，(b) 为反馈循环加仪器以获得可见性，(c) 准备干预机制（限流、重启、故障转移）。  2026 现实：SRE 实践围绕亚稳态成熟。Bronson-Aguilera 论文提供框架；公司有内部亚稳态 playbook；“系统卡住”...
  c23_b01.md: ...以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026 年已成熟为标准 SRE 实践，包含组织和技术组件。   ### 23.1 什么是混沌工程  混沌工程：故意向生产（或类生产）系统注入故障，以验...
  c23_b01.md: ...败，不如在工程师能响应的工作时间故意 kill，使系统对不可避免故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故...
  c23_b04.md: ...-West 区域。 * 爆炸半径：仅 EU-West 区域；US-East 和 US-West 正常运行。 * 参与者：SRE 团队（4 人）、支付团队（3 名工程师）、客服负责人（1 人）、产品经理（1 人）。  **当天**：  * 14:0...
  c23_b05.md: ...设计输入）**：混沌发现驱动架构决策。团队用混沌数据确定优先级；新功能发布前通过混沌测试。  2026 年，大多数成熟 SRE 组织处于 Level 2-3。Level 4 是愿景；很少有团队持续达到。  进阶是从 “我们被动应对故障” 到 “故...
  c23_b09.md: ...于 99%，p99 超过 1000 ms），在 X 分钟内恢复区域。谁触发回滚？多久完成？  (e) **谁参与？** SRE 团队（执行实验）；支付团队（熟悉应用逻辑）；客服（监控用户可见影响）；产品经理（业务背景）；观察员。  (f) **时...
  c23_b12.md: ...动实验）或没有。错误是把它当作二元（全做或全不做）；正确框架是 “这个系统的合适级别是什么”。  2026 现实：成熟 SRE 组织的混沌工程实践与系统关键性成比例。Crescent 支付系统每月 game day；内部 HR 系统没有。两者在其...
  c28_b01.md: ...时间是 2026-09-15。四位主角一起交付了多 region 支付平台。  Olamide 是 SRE 主管。Anya 是数据库主管（Stratus DB，现在内嵌为 Crescent 的 commit-log 层）。Yu...

=== effort (11 occurrences) ===
  c04_b05.md: ...入 → 输出）** ：给定一条应到达 N 个进程的消息，产出满足应用正确性要求且成本最低的广播原语。输出是 {best-effort, reliable, uniform reliable, causal, atomic} 之一及对应伪代码（§4.2-...
  c04_b05.md: ...liable, causal, atomic} 之一及对应伪代码（§4.2-§4.6）。变体：缓存失效提示用 best-effort；CRDT op 交付用 reliable；配置传播用 uniform reliable；状态机复制用 atomic（=...
  c04_b05.md: ...的正确性性质，再选最弱的足够层。  广播层次作为分层架构：      graph TD         A["Best-effort broadcast<br/>(TCP + for-loop)"] -->|"+ agreement<br/>(re-b...
  c04_b05.md: ...style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast       (TCP plus a for-loop)         |         + a...
  c04_b05.md: ...ation | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数...
  c04_b05.md: ...DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存失效提示 | best-effort | 丢失只导致过期读，不会损坏数据 配置传播 | uniform reliable | 每个副本最终必须有相同配置 分...
  c04_b05.md: ...序无关，只要交付  经济论证很重要。原子广播每条消息至少花费一轮共识。对每秒处理 100,000 事件的系统，best-effort（无协调）与 atomic（每事件共识）的差别就是可行与不可行的差别。  纪律：先识别应用需要的 _正确性性质_，再选最...
  c04_b07.md: ...#### 概念检测  **Q1.** 按强度列出五种广播原语，并说明每个比前一个新增的性质。  提示  Best-effort、reliable、uniform reliable、causal、atomic。新增性质：agreement、unif...
  c04_b07.md: ...rmity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：validity（发送者正确、接收者正确 → 交付）和 no-creation。比 for...
  c13_b09.md: ...偿。选择取决于用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复核。  Saga 审计日志（每一步提交和补偿，带时间戳与 t...
  c30_b14.md: ...├── ch04_broadcast/     │   ├── best_effort.py    — best-effort broadcast (TCP + for-loop)     │   ├── reliable.py       —...

=== consistent (11 occurrences) ===
  c05_b04.md: ...于 X 写 Y，Y 排在 X 之后。 * **Monotonic writes** ：你的写按你发出顺序应用。 * **Consistent prefix** ：你看到全局写顺序的一个前缀，即使没看最新写。  这些保证是 _per-session_ 而非全局。...
  c15_b01.md: ...则 Y 排在 X 之后。  (d) **Monotonic writes** ：会话的写按发出顺序应用。  (e) **Consistent prefix** ：即使看不到最新，也能看到全局写顺序的一个前缀。  这些保证是 _per-session_，不是全局...
  c15_b06.md: ...writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写。重要场景：用户期望自...
  c15_b06.md: ...writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写。重要场景：用户期望自...
  c17_b01.md: ...* **Linearizable** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal...
  c17_b01.md: ...* ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。 * **Causal consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-event...
  c17_b01.md: ...-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual consistent** ：无新更新时副本收敛；冲突按某种（可能非确定性）策略解决。  越弱的模型越快（需要越少协调）；模型越弱，应用成本越...
  c17_b01.md: ...和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB causally-consistent sessions**（3.6 起）：客户端跟踪见过的最高操作时间戳；同会话后续读要求服务器至少赶上该时间戳。 * **...
  c30_b01.md: ...nch, N. (2002). "Brewer's conjecture and the feasibility of consistent, available, partition-tolerant web services." _ACM SIGACT N...
  c30_b02.md: ...#### 分布式存储（第四部分 — 第11-14章）  * Karger, D., et al. (1997). "Consistent Hashing and Random Trees." _STOC_. * Chang, F., et al. (200...
  c30_b02.md: ...J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., P...

=== leaderless (11 occurrences) ===
  c05_b05.md: ...axos-replicated（per-tablet）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Leaderless | 默认最终一致，可按请求强...
  c05_b05.md: ...um Cassandra | Leaderless | 可调一致性：ONE、QUORUM、ALL DynamoDB | Leaderless | 默认最终一致，可按请求强一致 Redis（master-replica）| Async | `WAIT N` 命令...
  c05_b06.md: ...。  提示  Leader-based：一个节点排序写（PostgreSQL、MongoDB、CockroachDB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片...
  c05_b06.md: ...B replica sets、CockroachDB（per-range）、Spanner（per-tablet）。  Leaderless：任意节点接受写；系统依赖冲突解决（LWW、sibling、CRDT）收敛并发写。无需故障切换，因为没有指定 leade...
  c05_b06.md: ...k。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-...
  c05_b06.md: ...。  提示  Leader-based：一个节点排序写（PostgreSQL、MongoDB、CockroachDB）。Leaderless：任意节点接受写（Cassandra、DynamoDB）。  **答案：** Leader-based：一个节点（每分片...
  c05_b06.md: ...B replica sets、CockroachDB（per-range）、Spanner（per-tablet）。  Leaderless：任意节点接受写；系统依赖冲突解决（LWW、sibling、CRDT）收敛并发写。无需故障切换，因为没有指定 leade...
  c05_b06.md: ...k。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故障透明，代价是更复杂的冲突解决和更弱的每键排序。现代系统模糊界限：Spanner 每 tablet leader-...
  c05_b11.md: ...、可用性和读扩展性的某种组合。同步、异步、半同步的选择在写延迟和故障切换后持久性之间权衡。leader-based 与 leaderless 的选择在协调成本和故障切换透明性之间权衡。$W + R > N$ 的法定人数规则给相交，不是线性一致性；差别是许多真实...
  c06_b06.md: ...US-West 换 Toronto），把第二快往返压到 80 ms 以下。  (b) 用 Spanner 风格按区域 leaderless 写，按区域 quorum 加全局提交时间戳，特定工作负载可更快。  (c) 接受 85 ms 并调整预算。对人类可见操...
  c11_b01.md: ...ease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线性化读。 * **Leaderless** ：Cassandra、DynamoDB、Riak。任意节点接受写；读时或后台修复解决冲突。 * **自定义** ：...

=== right (11 occurrences) ===
  c06_b02.md: ...只付区域内成本；读可本地。 * **Failover** ：切换到 standby 需数分钟；可能丢失部分写。 * **Right for** ：单区域可用性足够、DR 是唯一多区域目标的工作负载。   #### 模式 2：按用户地理分区  每个用户...
  c06_b02.md: ...地延迟；跨归属操作付全价。 * **Failover** ：per-region 故障切换；其他区域不受影响。 * **Right for** ：以用户为中心的工作负载（聊天、社交、生产力），大多数操作围绕单个用户数据。  Slack、许多游戏公司这...
  c06_b02.md: ...Latency** ：每次写付跨区域往返成本。 * **Failover** ：区域可互换；丢失任一区域透明。 * **Right for** ：任何操作都可能触及任意数据的全局共享状态（罕见、昂贵）。  Spanner 针对这种模式。Cockroa...
  c11_b03.md: ...事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性化的工作负载（广告、金融账本）；Google 自身基础设施   #### CockroachD...
  c11_b03.md: ...per-range Raft 水平扩展 Weaknesses | 比单区域 Postgres 写延迟更高；运维调优复杂 Right for | 多区域 Postgres 替代；需要 serializability 的金融工作负载...
  c11_b04.md: ...维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访问模式可预测的单租户键值工作负载；AWS 原生系统   #### Cassandra  Question...
  c11_b04.md: ...节点；可调权衡 Weaknesses | 运维复杂（tombstone 管理、compaction 调优）；LWT 慢 Right for | 写重且可放宽一致性的工作负载；追求每节点简单的大规模系统   #### FoundationDB  Que...
  c11_b04.md: ...）；经混沌工程验证 Weaknesses | 底层 API（SQL 层是独立项目）；生态比 Postgres 兼容系小 Right for | 在其上构建分布式数据库（Snowflake 元数据层）；需要高可靠性的 ACID 工作负载...
  c11_b05.md: ...gths | 从一开始即为金融工作负载构建；深度可观测性 Weaknesses | 新系统；生态小；SQL 层仍在开发 Right for | CockroachDB 风格工作负载，Stratus 合作公司内部使用  这份阅读帮助 Anya 向潜在客...
  c11_b08.md: ...试 + TigerStyle 编程）；确定性执行；易于调试 Weaknesses | 专用于复式记账（非通用）；生态小 Right for | 银行账本、支付系统、金融 OLTP，且 schema 符合 TigerBeetle 的会计模型  Tige...
  c25_b11.md: ...很大，但每 token 只激活部分；成本低于 dense 大模型。  (f) **运维优化**：  * 根据观测利用率 right-size 集群。 * 用自动扩缩匹配需求。 * 多租户调度以最大化利用率。  LLM 的 “用更小模型” 答案最多带来...

=== protocol (11 occurrences) ===
  c07_b05.md: ...### 7.7 三条性质映射到协议设计  现代共识协议可通过它们如何实现三条性质来分析：  Protocol | Agreement | Validity | Termination ---|---|---|--- Paxos...
  c08_b10.md: ...d ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c11_b03.md: ...#### Spanner（Google）  Question | Answer ---|--- Replication protocol | 每 tablet Paxos；线性化时间戳使用 TrueTime-based commit-wait Failur...
  c11_b03.md: ...施   #### CockroachDB  Question | Answer ---|--- Replication protocol | 每 range Raft；HLC-based MVCC 通过 NTP 偏移上界（默认 500 ms）实现线性化时间...
  c11_b04.md: ...#### DynamoDB（AWS）  Question | Answer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Failure-recovery | 透明（存储层对用户不透明）；...
  c11_b04.md: ...生系统   #### Cassandra  Question | Answer ---|--- Replication protocol | 无 leader；按请求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 g...
  c11_b04.md: ...#### FoundationDB  Question | Answer ---|--- Replication protocol | 自定义（sequencer + transaction-log + storage）；强一致 Failure-re...
  c11_b05.md: ...系统。阅读结果：  Question | Answer（Stratus DB） ---|--- Replication protocol | 每 range Raft（使用 raft-rs）；HLC-based MVCC Failure-recovery...
  c11_b08.md: ...案：** TigerBeetle 阅读：  Question | Answer ---|--- Replication protocol | 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-...
  c11_b09.md: ...两者诊断画像相同（共享协议）：  Question | Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failure-recovery | 基于 gossip；超时后排除节点 Co...
  c14_b07.md: ...Question | Spanner | CockroachDB ---|---|---- Replication protocol | 每 tablet Paxos | 每 range Raft Failure-recovery | Paxos le...

=== tikv (11 occurrences) ===
  c07_b05.md: ...Spanner** 用 per-tablet Paxos。由于 Google 数据中心网络纪律更好，超时更紧。 * **TiKV** 用 Raft。Multi-Raft 架构：多 Raft 组并行。 * **Tendermint**（Cosmos...
  c09_b05.md: ...程师把 Raft 当作第一个共识协议学习；知识库广阔。  生产用户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、Ret...
  c09_b05.md: ...新项目，默认是 Raft。   ### 9.9 Anya 的决定  Anya 为 Stratus DB 复制层选择了 `tikv/raft-rs` 库（Rust）。她在设计文档中记录的理由：  (a) **成熟度** ：TiKV 的 Raft 已生...
  c09_b05.md: ...B 复制层选择了 `tikv/raft-rs` 库（Rust）。她在设计文档中记录的理由：  (a) **成熟度** ：TiKV 的 Raft 已生产运行多年，bug 已被发现并修复。 (b) **语言匹配** ：Stratus DB 是 Rust...
  c09_b08.md: ...Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 etcd / TiKV / 基于 Raft 的系统里，这种追赶通常透明，数千条差距在数秒内完成。  **Q2.** 一个 Raft 集群有 5...
  c09_b09.md: ...Raft：多 Raft 组并行，每组处理一个分片。这结合了每组内 Raft 的简单性与水平扩展。CockroachDB、TiKV、YugabyteDB 都用此模式生产。...
  c09_b11.md: ...简单性和运维工具。Multi-Raft 模式（多 Raft 组并行，每组一个分片）恢复水平扩展。CockroachDB、TiKV、YugabyteDB 等都在生产中用此模式。  第10章迈出崩溃故障共识的下一步：若某些节点撒谎怎么办？拜占庭容错（H...
  c12_b01.md: ...（HBase 的存在理由就是对宽行做范围扫描）、CockroachDB（默认）、Spanner（per-tablet）、TiKV。   #### 2026 年混合方案  现代系统常同时提供两者。CockroachDB 默认范围分片，但表可显式按 `...
  c14_b02.md: ...）。   ### 14.4 TiDB  PingCAP 的 NewSQL；兼容 MySQL 线缆。  关键属性：  * TiKV 存储层（每 region/tablet 使用 Raft；Percolator-based 事务）。 * TiDB 计算...
  c14_b02.md: ...et 使用 Raft；Percolator-based 事务）。 * TiDB 计算层（无状态 SQL 前端；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：...
  c14_b02.md: ...、阿里巴巴等）。  劣势：  * 英语社区比 CockroachDB 小。 * 多组件架构复杂（PD + TiDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MySQL 工作负载...

=== pre (11 occurrences) ===
  c07_b08.md: ...ms）。这减少心跳流量，但降低活性信号频率；更长间隔意味着真实故障最坏检测时间变长。  (c) 使用 Raft 的 “pre-vote” 扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增...
  c07_b08.md: ...erm。若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre-vote（消除误怀疑扰动）并把选举超时调到约 5 倍观察到的最坏跨 DC RTT。  **Q2.** 一个团队为复制配...
  c09_b10.md: ...实。  反驳：完整 Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。D...
  c10_b02.md: ...iskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-p...
  c10_b02.md: ...**Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，...
  c10_b02.md: ...iskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-p...
  c10_b02.md: ...**Pre-prepare** ：leader 提议一个值，发给所有副本。 (b) **Prepare** ：每个接受 pre-prepare 的副本广播 prepare 消息给其他所有副本。某个副本收集到 2f+1 个 prepare 消息后，...
  c10_b10.md: ...需单轮往返。  _HotStuff pipelining_ ：HotStuff 每个决定有三个投票阶段（prepare、pre-commit、commit）。Pipeline 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 pre...
  c10_b10.md: ...e 让这些阶段对不同提议重叠。Leader 为块 $k+2$ 收集 prepare 投票的同时，为块 $k+1$ 收集 pre-commit 投票、为块 $k$ 收集 commit 投票。Pipeline 是“不同决定的阶段重叠”，每个决定总共需...
  c27_b06.md: ...ion-test 分数低。  这些是可分解为 “在 diff 中找这个正则” 的检查清单项。AI 审查者几秒处理。作为 pre-PR 检查，在人类审查者看到 diff 前过滤掉明显失败。   #### AI 审查者可靠遗漏什么  检查清单其余项需...
  c27_b07.md: ...自动 AI 审查（秒级，每次 push 运行）**：  * 使用 Greptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参...

=== lease (11 occurrences) ===
  c08_b04.md: ...多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（2011）和 Spanner 论文（2012）描述...
  c08_b04.md: ..._，不必都是多数。这让系统可调节权衡（更小 accept 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“...
  c08_b04.md: ...t 法定人数换更快提交）。  现代生产系统主要用带 leader lease 的 Multi-Paxos。Leader lease 是额外机制：leader 持有 acceptor 授予的有时限“租约”；租约存续期间，leader 可不经过共识轮次服...
  c08_b04.md: ...经过共识轮次服务线性化读（租约保证该窗口内无其他 leader）。Spanner 和 CockroachDB 都把基于 lease 的读作为主要性能优化。...
  c08_b07.md: ...次更替都花第一阶段往返并可能暂停集群。  (e) **读一致性** ：提供线性化读要么需要一轮共识（慢），要么需要基于 lease 的捷径（快但 lease 实现微妙）。  (f) **故障期间成员变更** ：分区期间移除故障节点危险；分区愈合后“被...
  c11_b01.md: ...ge、region、key range）有自己的共识组，通常是 Raft。 * **Per-tablet 基于共识 + lease** ：Spanner。每个 tablet 用 Paxos；leader 持有租约以支持快速线性化读。 * **Lead...
  c11_b03.md: ...ery | Paxos leader 重选（~数秒）；TrueTime 基础设施（GPS + 原子钟）使 leader-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition behavi...
  c19_b01.md: ...key（如 `/services/payment/instance-uuid`），包含地址和元数据。 * key 带 lease（如 30 秒）；实例每 10 秒续期。 * 若实例未续期（崩溃、分区），key 过期并被删除。 * 客户端 watch...
  c19_b01.md: ...es/payment/` 前缀；实例变更时收到通知。  模式很干净：发现服务无需知道具体服务类型；它提供通用键值存储加 lease 与 watch 原语。应用使用这些原语。...
  c19_b07.md: ...个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revisi...
  c19_b07.md: ...eys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd...

=== eventual (11 occurrences) ===
  c11_b02.md: ...s、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。 * **Eventual** ：副本最终收敛；读可能过期。Cassandra（默认）、DynamoDB（默认）、Riak。 * **Per-op...
  c17_b01.md: ...stent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual consisten...
  c17_b01.md: ...**Strong-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual consistent** ：无新更新时副本收敛；冲突按某种（可能非确定性）策略解决。  越弱的模型越快（需要越少协调）...
  c17_b01.md: ...stent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual consisten...
  c17_b01.md: ...**Strong-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual consistent** ：无新更新时副本收敛；冲突按某种（可能非确定性）策略解决。  越弱的模型越快（需要越少协调）...
  c17_b05.md: ...间顺序）。大多数生产 CRDT 系统同时提供两者：causal+（保持 happens-before）和 strong-eventual（确定性收敛）。两者结合给出有用保证：“我看到所有先前写（因果），且给定相同更新，我的视图与他人的收敛（强最终）。”...
  c17_b11.md: ...数器” 时，先问吞吐。若吞吐高，答案不是数据库事务。   ### 本章总结  一致性格在 Linearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CR...
  c17_b11.md: ...ble 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CRDT）提供确定性收敛。两者都比线性化便宜；两者都比纯最终更强。  计数器 / 排行榜 / 库存工作负载最终定...
  c17_b11.md: ...数器” 时，先问吞吐。若吞吐高，答案不是数据库事务。   ### 本章总结  一致性格在 Linearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CR...
  c17_b11.md: ...ble 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CRDT）提供确定性收敛。两者都比线性化便宜；两者都比纯最终更强。  计数器 / 排行榜 / 库存工作负载最终定...
  c30_b02.md: ..., Kaminsky, M., & Andersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COP...

=== coordinator (11 occurrences) ===
  c11_b04.md: ...求可调一致性（ONE、QUORUM、ALL） Failure-recovery | 基于 gossip；超时后排除节点；coordinator 故障需人工处理 Consistency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线...
  c18_b04.md: ...equenceDiagram         autonumber         participant C1 as Coordinator A<br/>(token 100)         participant L as Lock Service<br/...
  c18_b04.md: ...as Bank<br/>(highest_seen_token)         participant C2 as Coordinator B<br/>(token 101)         C1->>L: acquire lock         L-->...
  c27_b11.md: ...：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use CockroachDB which is CP for consistency      d...
  c28_b03.md: ...RDBE[CockroachDB nodes]             STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Ka...
  c28_b04.md: ...在，返回之前计算的结果（谎言 1 防御）。2 ms。  (5) API 服务开始 Stratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（...
  c28_b04.md: ...tratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze...
  c28_b04.md: ...mmit-wait。95 ms（受第6章所说的最坏情况跨 region quorum 物理限制）。  (10) 事务的 coordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4...
  c28_b04.md: ...在，返回之前计算的结果（谎言 1 防御）。2 ms。  (5) API 服务开始 Stratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（...
  c28_b04.md: ...tratus DB 事务。该事务的 coordinator 状态在 US-East 节点间经 Raft 复制（第9章）。Coordinator 获得 fencing token（第18章）。5 ms。  (6) 事务从 CockroachDB 读取 Adaeze...
  c28_b04.md: ...mmit-wait。95 ms（受第6章所说的最坏情况跨 region quorum 物理限制）。  (10) 事务的 coordinator（Stratus DB）用 fencing token 记录完成；银行下游 endpoint 验证 token（谎言 4...

=== size (11 occurrences) ===
  c12_b03.md: ...DB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Ca...
  c12_b03.md: ...led compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空间（合并更积极）但写...
  c12_b03.md: ...多 SSTable 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 = leveled。...
  c12_b03.md: ...DB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Ca...
  c12_b03.md: ...led compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空间（合并更积极）但写...
  c12_b03.md: ...多 SSTable 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 = leveled。...
  c16_b03.md: ...、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且其文档模型比 Automerge 完整历史图更简单。   ### 16...
  c16_b07.md: ...list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size 优化。  **答案：** RGA（Replicated Growable Array）把列表表示为图：每个字符有唯一...
  c16_b07.md: ...性能** ：合并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle size** ：Yjs ~80 KB，即使在慢网络下也足够小，适合浏览器。 (d) **垃圾回收** ：删除字符的 tombst...
  c24_b07.md: ...求完成）。  (b) **Per-token 吞吐优化**。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 tokens。  (c)...
  c25_b11.md: ...token 只激活部分；成本低于 dense 大模型。  (f) **运维优化**：  * 根据观测利用率 right-size 集群。 * 用自动扩缩匹配需求。 * 多租户调度以最大化利用率。  LLM 的 “用更小模型” 答案最多带来 10-2...

=== master (11 occurrences) ===
  c15_b03.md: ...ad-your-writes 时却用 ONE 读）。   #### Redis 复制  主从复制异步。从副本读可能过期。Master 上 ack 的写尚未在副本上；故障切换可能丢失近期写。  对需要更强保证的应用，Redis 提供 `WAIT N` 命...
  c18_b01.md: ...luster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 slot 属于一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向随机选择的其他节点子集发送 PING；PING...
  c18_b01.md: ...对自身的视图收敛。  gossip 不是共识；分区下，集群两半可能有不一致视图。Redis Cluster 要求大多数 master 节点同意配置变更（slot 重分配、主从切换）来处理。分区期间的少数派写失败并返回 `CLUSTERDOWN`。...
  c18_b02.md: ...k 作为分布式锁算法：  (a) 客户端选一个唯一值（随机 nonce）。(b) 客户端尝试在 N 个独立 Redis master（通常 5 个）上设置锁 key。(c) 若多数（3/5）在超时内成功，则获得锁。(d) 客户端持有锁一段时间 TTL；...
  c18_b02.md: ...一段时间 TTL；可通过删除 key 释放（需验证 value 与 nonce 匹配）。  意图：容忍少数 Redis master 故障；在集群上提供互斥。...
  c18_b05.md: ...停、时钟漂移、缓存故障）还有缓存特有原因：  (a) **缓存故障转移**：Redis Sentinel 提升副本；新 master 可能尚未复制锁 key；另一个客户端获得锁；两个持有者。  (b) **缓存内存驱逐**：内存压力下 Redis 驱逐...
  c18_b06.md: ...lock 模式。根据 Kleppmann 的批评，它为什么不提供正确性？  提示  Redlock：让多数 Redis master 设置锁 key。失败：进程暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端...
  c18_b06.md: ...暂停、时钟漂移。修复：fencing token。  **答案：** Redlock：客户端通过在 N 个 Redis master（通常 5 个）上设置唯一值作为 key 来获取锁。若多数（3/5）在超时内成功，则获得锁。锁有 TTL；客户端通过删除...
  c18_b10.md: ...取决于用例；LLM 没问。  (b) **部署特定的失败模式**。Redlock 的安全假设 “5 个独立 Redis master” 与特定网络条件。生产中团队可能 5 个 Redis 实例在同一 DC（单一故障域），或 3 个带主从复制（不同故障模...
  c18_b10.md: ...在没有 fencing token 时很危险。LLM 没问暂停预期。  (e) **跨区域部署**。5 个 Redis master 跨区域的 Redlock 会增加锁获取的跨区域延迟。LLM 没问地理。  (f) **Redis 故障时发生什么**。...
  c18_b10.md: ...dlock 会增加锁获取的跨区域延迟。LLM 没问地理。  (f) **Redis 故障时发生什么**。若 Redis master 故障，集群从 5 变 4；多数变成 4 中 3；安全分析改变。LLM 没处理运维场景。...

=== traces (11 occurrences) ===
  c20_b02.md: ...中显示重试），而不是与原 span 合并。修复：每次重试创建新的子 span。   #### 关注点 2：分区下采样  Traces 通常被采样（只有 1% 或更少的请求被详细追踪），因为存储每条 trace 很贵。  采样决策通常在根 span 做出...
  c20_b03.md: ...应该被调用的服务没出现，可能是上游没调用它（bug 或路由问题）。   ### 20.4 Trace 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wall-clock 时长，但不拆解...
  c20_b03.md: ...存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成...
  c20_b03.md: ...应该被调用的服务没出现，可能是上游没调用它（bug 或路由问题）。   ### 20.4 Trace 不能告诉你什么  Traces 是强大工具但有局限：  (a) **服务内部性能**：span 显示 RPC 的 wall-clock 时长，但不拆解...
  c20_b03.md: ...存在已知 bug 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成...
  c20_b08.md: ...现**：用已知的坏输入模式复现失败。迭代修复。  诊断流程的技巧是知道每一步该看什么。Trace 是起点；诊断流程结合 traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低...
  c20_b09.md: ...默认 OpenTelemetry" 是运维标准。  第二个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关联 traces、logs、metrics。它们有望减轻读 trace 的认知负担；2026 年还处于早期且不可靠，但轨迹表明 2028...
  c20_b10.md: ...库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；profiling 展示服务内部发生了什么；met...
  c20_b10.md: ...elemetry 避免 vendor lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示...
  c27_b12.md: ...测试的分布式代码 PR 被拒绝。  (e) **监控要求**：每个分布式操作必须可观测。没有 metrics、logs、traces 的操作 PR 被拒绝。  (f) **周期性审计**：高级工程师每季度审计已合并分布式代码 PR 样本。发现与团队共享...
  c28_b02.md: ...s 的组件）。每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k...

=== compute (11 occurrences) ===
  c24_b01.md: ...*。Prefill（处理 prompt）和 decode（逐 token 生成）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。独立优化每个阶段是 2025-2026 的突破。  (c) *...
  c24_b08.md: ...期利用率提升：5-15%。  (c) **Disaggregated prefill/decode（第25章）**：把 compute-bound prefill 和 memory-bound decode 分离到不同硬件，让每个阶段使用最优硬件。Pre...
  c24_b11.md: ...万美元。  第25章进入 2025-2026 架构突破：disaggregated prefill/decode。把 compute-bound prefill 与 memory-bound decode 分离，让每个阶段使用最优硬件。本章将走工作流...
  c25_b01.md: ...很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同...
  c25_b01.md: ...下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |...
  c25_b01.md: ...很明显：prefill（处理 prompt）和 decode（生成 token）的计算特征非常不同。Prefill 是 compute-bound；decode 是 memory-bound。在同一张 GPU 上同时运行两者是次优默认；把它们放到针对不同...
  c25_b01.md: ...下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |...
  c25_b02.md: ...│  (4x H100)   │  │  (4x H100)   │                 │  Compute     │  │  Compute     │                 │  optimized   │  │...
  c25_b02.md: ...│  │  (4x H100)   │                 │  Compute     │  │  Compute     │                 │  optimized   │  │  optimized   │...
  c25_b05.md: ...**Q1.** 解释 prefill/decode 不对称性。为什么它证明分离两个阶段合理？  提示  Prefill：compute-bound，矩阵-矩阵乘法。Decode：memory-bound，矩阵-向量乘法。不同最优硬件。  **答案：**...
  c25_b11.md: ...总结  Disaggregated prefill/decode 是 2025-2026 架构突破。Prefill 是 compute-bound（矩阵-矩阵乘法）；decode 是 memory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个...

=== pagedattention (11 occurrences) ===
  c24_b02.md: ...### 24.2 vLLM 与 PagedAttention  vLLM（2023 年由 UC Berkeley 发布；2024-2026 年广泛部署）引入了 _PagedAtte...
  c24_b03.md: ...降低延迟和 GPU 成本。对 prompt 多样化（不同用户、不同任务、无共享前缀）的通用 API 流量，vLLM 的 PagedAttention 是更好选择——它为多样性优化内存布局，而非复用。...
  c24_b06.md: ...ll、decode）和部署模式（disaggregation、sharding）都是推理专用。  **Q2.** 描述 PagedAttention。它解决什么问题？  提示  受 OS 分页启发。KV cache 分固定大小块；不连续；消除碎片。  **答案：**...
  c24_b06.md: ...。它解决什么问题？  提示  受 OS 分页启发。KV cache 分固定大小块；不连续；消除碎片。  **答案：** PagedAttention（vLLM，2023）把 KV cache 分成固定大小块（如每块 16 个 token）。每个请求的 KV cache...
  c24_b06.md: ...放。久而久之内存碎片化；即使总空闲内存大，也没有足够大的连续块给新请求。GPU 内存利用率差（典型 30-50%）。  PagedAttention 收益：  * **更高内存利用率**：70-90% GPU 内存用于 KV cache，相比朴素系统 30-50%。...
  c24_b06.md: ...tem prompt 的聊天工作负载，内存节省巨大。  灵感来自操作系统虚拟内存：页让进程无需连续物理地址即可使用内存。PagedAttention 把同样思想用于 KV cache。  2026 年，vLLM（及 PagedAttention 衍生系统）是最广泛部署...
  c24_b06.md: ...rding）在此基础上构建。  **Q3.** 比较 vLLM 和 SGLang。各适合什么场景？  提示  vLLM：PagedAttention；通用。SGLang：RadixAttention；前缀复用重的工作负载。  **答案：**...
  c24_b07.md: ..._vLLM_：  * 核心创新：PagedAttention（KV cache 内存管理）。 * 优势：通用，多种工作负载下高吞吐。 * 最适合：任意 serving、无特定前缀复...
  c24_b10.md: ...产部署包含什么。  **讨论：** LLM 的基础 TGI 部署是起点，但遗漏许多 2026 生产特性：  (a) **PagedAttention 引擎**：vLLM（或 SGLang 适合工作负载）是 2026 默认。TGI 合理，但 vLLM 对大多数工作负载吞...
  c24_b11.md: ...decode）；KV cache 是中心状态；严格 per-token 延迟预算；突发工作负载模式。  vLLM 的 PagedAttention 把 KV cache 分成固定大小块；消除碎片；GPU 内存利用率从 30-50% 提升到 70-90%。2026 年...
  c30_b03.md: ...ent Memory Management for Large Language Model Serving with PagedAttention." _SOSP_. — vLLM。 * Zheng, L., et al. (2023). "SGLang: Effi...

=== fair (11 occurrences) ===
  c26_b04.md: ...eues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit...
  c26_b04.md: ...redit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高...
  c26_b04.md: ...ecoding 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLa...
  c26_b06.md: ...er、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weig...
  c26_b06.md: ...duling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权重 5、3...
  c26_b07.md: ...对 Continuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低 tier...
  c26_b07.md: ...合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内部也 weighted fair。结果：SLA 客户有可预测延迟；低 tier 客户在峰值期间也有有界服务。  运维关注点：调度决策必须快（亚毫秒）以免...
  c26_b08.md: ...10 秒降到约 5 秒。  (e) **客户 SLA 调度**：该客户是 tier-2（中优先级）。Weighted fair scheduling 确保其请求被处理但不饿死 tier-1 客户。  综合：该客户每请求成本降低约 5 倍（前缀复用...
  c26_b11.md: ...布式变体（draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 w...
  c26_b11.md: ...ing 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache shardi...
  c26_b11.md: ...che-aware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果：集群利用率 85%（从 60%）、每请求成本降低...

=== services (10 occurrences) ===
  c19_b03.md: ...ure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kubernetes headless services** ：“headless” Kubernetes service 没有 cluster IP；客户端解析服务名得到所有...
  c19_b10.md: ...会改变。  **Q2.** 一个 LLM 被要求 “为我们的微服务舰队实现服务发现”。它产出使用 Kubernetes Services 的代码。该团队在 EC2 上运行（非 Kubernetes）。指出 LLM 的失败和正确模式。  **讨论：** LL...
  c19_b10.md: ...熟悉部署（Kubernetes）的答案，而没有问实际部署目标。EC2（或基于 VM）部署无法使用 Kubernetes Services；需要不同机制。  EC2 的正确模式：  (a) **Consul**：HashiCorp 的 Consul 为 VM...
  c19_b10.md: ...、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求需要上下文。未问就产出 Kubernetes Services 的 LLM 是把流行模式套到不适用的上下文。...
  c19_b11.md: ...的部署：Kubernetes？VM？混合？云？本地？你们团队目前用什么做编排？我可以根据匹配推荐 Kubernetes Services、Consul、AWS Cloud Map、etcd 或其他选项。”   ### 本章总结  服务发现是 “服务 X 的...
  c19_b11.md: ...复杂流量需求的团队；没有这些需求的团队则是过度工程。  Crescent 的发现使用四个分层系统：Kubernetes Services 用于区域内、etcd 用于领导者选举锁、Consul 用于跨区域网格、AWS Cloud Map 用于外部 API。每...
  c27_b11.md: ...iled', reason='insufficient')              # Process across services             bank_result = bank_service.transfer(payment)...
  c28_b03.md: ...kroachDB nodes]         end         subgraph Shared["Shared Services"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-re...
  c30_b01.md: ...easibility of consistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D. (2012). "Con...
  c30_b03.md: ...事后复盘（本书全文）。 — 六个谎言的工作示例。   #### 前瞻（第29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，...

=== foundationdb (10 occurrences) ===
  c02_b04.md: ...能提供过期读） | L（低延迟读，默认最终一致） Cassandra | 可调（按请求法定人数） | 可调（默认 L） FoundationDB | C | C，针对 OLTP 延迟优化  这张表比 CAP 表更有用。CAP 告诉你事情变坏时发生什么。PACELC...
  c10_b05.md: ...计，以降低 per-commit 延迟而不牺牲 safety。CockroachDB 和 TiDB 的路线图中已有变体；FoundationDB 多年来一直可配置法定人数大小。   ### 10.7 非区块链 BFT 用例  BFT 不只是区块链。三个生产相关用例...
  c10_b08.md: ...r + 另一节点（共 2 个）ack 即可，远快于经典 3-of-5。Leader 变更需要 4 of 5（罕见）。  FoundationDB 和一些新系统用此变体生产调优。权衡：用较慢但罕见的路径换取较快且频繁的路径。...
  c10_b12.md: ...：分布式数据库。第7-10章的协议是基础；Spanner、CockroachDB、DynamoDB、Cassandra、FoundationDB 等是在基础上构建的生产系统。本章教授 _诊断方法_ ：如何通过识别复制协议、故障恢复语义、一致性模型和分区行为，在 3...
  c11_b01.md: ...有四个问题，按顺序。学完后，你能读懂 Spanner、CockroachDB、DynamoDB、Cassandra 或 FoundationDB，并产出一页阅读笔记，说明每个系统是什么、成本多少、何时失败、为谁设计。   ### 11.1 四个问题  对任何分布式...
  c11_b01.md: ...节点接受写；读时或后台修复解决冲突。 * **自定义** ：Aurora（存储层自定义协议；数据库层单 leader）；FoundationDB（sequencer 设计）。  协议决定写延迟、故障切换行为和冲突解决机制。它是系统最重要的属性。...
  c11_b04.md: ...n 调优）；LWT 慢 Right for | 写重且可放宽一致性的工作负载；追求每节点简单的大规模系统   #### FoundationDB  Question | Answer ---|--- Replication protocol | 自定义（seque...
  c13_b12.md: ...Paxos）复制协调者状态。协调者不再是活性单点；任何节点可接管并查询复制状态。Spanner、CockroachDB、FoundationDB 都用此模式。  **Q2.** 什么是 Google Percolator？它如何避免需要中心协调者？  提示  Pe...
  c14_b03.md: ...测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，沿袭 Tyler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * TigerStyle 编程规范：固定大小 128 字节消息、热路...
  c30_b04.md: ...ossip；10-30 秒 | 默认最终；LWT 用于线性化 | 可调 | 可调 | 写重；大规模横向扩展 | | **FoundationDB** | 定制（sequencer + log + storage） | 亚秒级 sequencer 切换 | 可串行化...

=== leftarrow (10 occurrences) ===
  c03_b02.md: ...为事件时间戳。 * p 发送消息时，消息携带 L_p（当前计数器）。 * p 接收时间戳为 t 的消息时，令 L_p \leftarrow \max(L_p, t) + 1，并用新的 L_p 标记接收事件。  保证：若 a \to b，则 L(a) < L(...
  c03_b03.md: ...。 * p 发送消息时，消息携带当前 V_p。 * p 接收带向量 V_m 的消息时，对所有 i 设置 V_p[i] \leftarrow \max(V_p[i], V_m[i])，然后将 V_p[p] 加 1。  保证：a \to b 当且仅当 V(a)...
  c03_b05.md: ...\_now}()；计算 l' = \max(l_p, t)；若 l' = l_p 则 c_p 加 1，否则令 c_p \leftarrow 0；然后令 l_p \leftarrow l'，并用 (l_p, c_p) 标记事件。 * **发送** ：消息中携带...
  c03_b05.md: ...(l_p, t)；若 l' = l_p 则 c_p 加 1，否则令 c_p \leftarrow 0；然后令 l_p \leftarrow l'，并用 (l_p, c_p) 标记事件。 * **发送** ：消息中携带 (l_p, c_p)；先把发送作为本地事...
  c03_b05.md: ...l' = \max(l_p, l_m, t)；然后更新计数器：     * 若 l' = l_p = l_m：c' \leftarrow \max(c_p, c_m) + 1     * 否则若 l' = l_p：c' \leftarrow c_p + 1...
  c03_b05.md: ...l_m：c' \leftarrow \max(c_p, c_m) + 1     * 否则若 l' = l_p：c' \leftarrow c_p + 1     * 否则若 l' = l_m：c' \leftarrow c_m + 1     * 否则（l...
  c03_b05.md: ...* 否则若 l' = l_p：c' \leftarrow c_p + 1     * 否则若 l' = l_m：c' \leftarrow c_m + 1     * 否则（l' = t，严格新于 l_p 和 l_m 两者）：c' \leftarrow 0...
  c03_b05.md: ...' \leftarrow c_m + 1     * 否则（l' = t，严格新于 l_p 和 l_m 两者）：c' \leftarrow 0     * 设置 (l_p, c_p) \leftarrow (l', c')；用新二元组标记接收事件。  这四种...
  c03_b05.md: ...= t，严格新于 l_p 和 l_m 两者）：c' \leftarrow 0     * 设置 (l_p, c_p) \leftarrow (l', c')；用新二元组标记接收事件。  这四种情况穷举且互斥：l' 恰好等于 l_p、l_m、t 中的一个（平局...
  c08_b02.md: ...ptor 见过的最高 ballot $b_{max}$，忽略（或回复 NACK）。 * 否则，更新 $b_{max} \leftarrow b$，并回复 `PROMISE(b, accepted_ballot, accepted_value)`，其中 `ac...

=== replicated (10 occurrences) ===
  c05_b05.md: ...ries | `writeConcern: majority` 实现基于法定人数 CockroachDB | Raft-replicated（per-range）| 所有写都是 Raft-quorum（多数同步） Spanner | Paxos-replica...
  c05_b05.md: ...licated（per-range）| 所有写都是 Raft-quorum（多数同步） Spanner | Paxos-replicated（per-tablet）| 所有写都是 Paxos-quorum Cassandra | Leaderless | 可调...
  c05_b05.md: ...层；写以 4-of-6 存储 quorum 确认 | 读副本最终一致 TiDB / YugabyteDB | Raft-replicated | 所有写都是 Raft-quorum  模式：现代分布式数据库（CockroachDB、Spanner、TiDB、Y...
  c16_b01.md: ...备，有时离线，文档会收敛到一致状态，而无需冲突解决弹窗。  使这成为可能的数学是 CRDT（Conflict-free Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 Automerge（用于...
  c16_b01.md: ...T 出现后，数学处理冲突解决；实现小一个数量级。   ### 16.1 CRDT 是什么  Conflict-free Replicated Data Type 是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合并顺序如何都产生相同结果。“无冲...
  c30_b02.md: ...er, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N....
  c30_b02.md: ...iça, N., Baquero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M., & Beresford...
  c30_b02.md: ...Kleppmann, M., & Beresford, A. R. (2017). "A Conflict-Free Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础。 * Kleppmann, M...
  c30_b02.md: ...W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...
  c30_b14.md: ...+ AppendEntries + commit     │   ├── kv_state_machine.py — replicated key-value store on top     │   └── tests/            — spli...

=== flexible (10 occurrences) ===
  c08_b04.md: ...Paxos（EPaxos）** ：无 leader；每个副本都可提议，冲突按提议解决。吞吐更高；推理更复杂。 * **Flexible Paxos**（Heidi Howard 2016 法定人数灵活性工作）：推广法定人数要求。prepare 法定人数和...
  c08_b10.md: ...的属性，视变体而定。EPaxos 无 leader；对低竞争工作负载，其吞吐高于 Raft 的 leader 瓶颈设计。Flexible Paxos 允许运维调节法定人数大小，Raft 不行。Google 的 Chubby 和 Spanner 基于 Pax...
  c09_b05.md: ...Google 基础设施（Chubby、Spanner）、专门变体如 EPaxos 用于高吞吐无 leader 工作负载、Flexible Paxos 用于可调法定人数。但对 2026 年的新项目，默认是 Raft。   ### 9.9 Anya 的决定...
  c09_b11.md: ...扩展到对抗性环境。成本更高（3f+1 副本而非 2f+1），但威胁模型覆盖区块链、不可信云和高风险金融系统。该章还涵盖 Flexible Paxos（Heidi Howard 的重配置洞见），让运维可以调节固定法定人数协议无法提供的 consistency...
  c10_b01.md: ...arwhal-Bullshark（DAG mempool 与共识分离）、Tendermint（许多区块链生态的工作马）、Flexible Paxos（Heidi Howard 的重配置洞见），以及非区块链 BFT 用例（对抗性多租户系统的控制面）。  BF...
  c10_b05.md: ...### 10.6 Flexible Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum...
  c10_b05.md: ...### 10.6 Flexible Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisited” 推广了 Paxos 的法定人数要求。经典...
  c10_b08.md: ...成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi Howard 的 Flexible Paxos 推广了法定人数要求。陈述该推广并给出一个实践应用示例。  提示  只需要 $Q_1 \cap Q_2 \n...
  c10_b12.md: ...分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 prepare 与 accept 法定人数相交，而非各为多数。这让运维可以调优...
  c30_b02.md: ...aft 论文。 * Howard, H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quoru...

=== pipelining (10 occurrences) ===
  c08_b04.md: ...与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi-Paxos 优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（201...
  c10_b03.md: ...are / pre-commit / commit）及作为紧凑支持证明的 Quorum Certificates，外加 pipelining 提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的...
  c10_b03.md: ...是线性的，因为新 leader 只需要每个副本的最高 QC（$O(N)$ 条消息）即可确定协议状态，而不用所有消息。  Pipelining：HotStuff 把各阶段 pipeline。Leader 在为块 $k+1$ 收集第一阶段投票时，可以同时为块 $k...
  c10_b08.md: ...含义：HotStuff 可扩展到更大的副本集合（某些区块链部署数百验证者），而不让 view-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi Howard 的 Flexible Paxos...
  c10_b09.md: ...具较旧（多数 PBFT 实现是研究代码）。  (b) **HotStuff** ：$O(N)$ view change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但...
  c10_b10.md: ...而非 BFT。威胁真实，但通过其他手段管理（纵深防御、监控、访问控制）。  **Q3.** 比较 HotStuff 的 pipelining 与 Raft 的日志复制。两者都可有多个“飞行中的决定”——结构上有什么不同？  提示  Raft leader 串行...
  c10_b10.md: ...决定总共需三轮，但吞吐可达每轮一个决定（而非每三轮一个决定）。  结构差异：  * Raft：$O(1)$ 阶段每决定；pipelining 是“同阶段多个在飞决定”。 * HotStuff：$O(1)$ 阶段每决定（具体 3 个）；pipelining 是“...
  c10_b10.md: ...决定；pipelining 是“同阶段多个在飞决定”。 * HotStuff：$O(1)$ 阶段每决定（具体 3 个）；pipelining 是“不同决定在不同阶段同时进行”。  吞吐效果：稳态下两者都能达到每轮往返提交一个决定。HotStuff 的 per-...
  c10_b10.md: ...者集合中是数百毫秒到数秒。若 per-decision 延迟关键，这是成本。若吞吐关键（例如每块处理大量交易的区块链），pipelining 吸收成本。...
  c10_b12.md: ...N^2)$ 消息复杂度限制了小规模副本集。HotStuff（2019）通过 Quorum Certificates 和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（202...

=== previous (10 occurrences) ===
  c09_b04.md: ...“Leader 只能提交自己 term 的条目”微妙之处  一条微妙规则：leader 不会 _直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带提交 previous term 的条目（这有副作...
  c09_b04.md: ..._直接_ 通过复制来提交 previous term 的条目。它只有在复制了一个自己 term 的条目后，才顺带提交 previous term 的条目（这有副作用：连带提交之前的所有条目）。  原因：没有这条规则，某个 corner case 会让已提...
  c09_b04.md: ...。   #### 常见误解  Raft 的常见误读：“leader 复制，多数一 ack，就算提交。”几乎对，但需注意 previous-term 提交微妙性。Leader 通过确保 (a) 多数复制了该条目且 (b) 一个自己 term 的条目也已复制到...
  c09_b10.md: ...机。许多 LLM 实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 pre...
  c09_b10.md: ...ous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反...
  c09_b10.md: ...er 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety...
  c09_b10.md: ...机。许多 LLM 实现一追加就应用，导致未提交条目在状态机可见，leader 故障时破坏 safety。  (c) **Previous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 pre...
  c09_b10.md: ...ous-term 提交微妙性。** Raft 规则：leader 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反...
  c09_b10.md: ...er 只有同时提交一个自己 term 的条目时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety...
  c09_b11.md: ...论文的图 8 case 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous term 条目”翻译成正确代码，需要深入理解不变式，而这正是模型做不到的地方。  2026 年最安全的模式：使用经过验...

=== snapshot (10 occurrences) ===
  c11_b02.md: ...nearizable 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到一致快照，但不防止写-写冲突（写偏可能）。PostgreSQL（实际 REPEATA...
  c12_b04.md: ...### 步骤 3：流式传输数据  对每个被重新分配的 range，源节点向目标节点发送 _snapshot_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SS...
  c12_b04.md: ...配的 range，源节点向目标节点发送 _snapshot_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 S...
  c12_b04.md: ...t_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点...
  c12_b04.md: ...储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft...
  c12_b04.md: ...### 步骤 3：流式传输数据  对每个被重新分配的 range，源节点向目标节点发送 _snapshot_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SS...
  c12_b04.md: ...配的 range，源节点向目标节点发送 _snapshot_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 S...
  c12_b04.md: ...t_。源节点在 snapshot 期间继续服务读写；新写被缓冲，snapshot 完成后发送。  对 LSM 后端存储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点...
  c12_b04.md: ...储，snapshot 是高效的 SSTable 拷贝：源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft...
  c30_b06.md: ...与编排式变体。 * **Serializable（可串行化）**：每次事务执行等价于某种串行顺序。弱于线性化。 * **Snapshot isolation（快照隔离）**：事务读取一致快照；存在写-写冲突（可能写偏）。 * **Speculative d...

=== score (10 occurrences) ===
  c13_b07.md: ...step 2: call FraudService.check(transaction)         → score       step 3: call BankSettlement.submit(transaction)...
  c13_b07.md: ...R-->>O: reserved         O->>F: check fraud         F-->>O: score (OK)         O->>B: submit settlement         B-->>O: bank_...
  c14_b10.md: ...而非技术能力。  **Q3.** 用诊断方法（第11章）+ 事务协议视角（第13章）为全球游戏排行榜设计系统：10 万 score updates/秒，1000 万并发用户查看，读写 p99 都低于 200 ms。  提示  排行榜：因果一致。高写吞...
  c14_b10.md: ...典 OLTP。读规模：写 100 倍。全球：多区域。  **答案：** 工作负载分析：  * **写**：10 万/秒 score 提交。需要 per-player 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/...
  c14_b10.md: ...CRDT 有序集做排行榜。每个区域有本地计数器；跨区域合并。每区域写亚毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N...
  c14_b10.md: ...中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5 ms（DynamoDB put）+ 1 ms（缓存更新）= 6 ms。p99 ~50 ms。远低于预算。 *...
  c14_b10.md: ...而非技术能力。  **Q3.** 用诊断方法（第11章）+ 事务协议视角（第13章）为全球游戏排行榜设计系统：10 万 score updates/秒，1000 万并发用户查看，读写 p99 都低于 200 ms。  提示  排行榜：因果一致。高写吞...
  c14_b10.md: ...典 OLTP。读规模：写 100 倍。全球：多区域。  **答案：** 工作负载分析：  * **写**：10 万/秒 score 提交。需要 per-player 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/...
  c14_b10.md: ...CRDT 有序集做排行榜。每个区域有本地计数器；跨区域合并。每区域写亚毫秒；最终收敛。  推荐：混合架构。  * **Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N...
  c14_b10.md: ...中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5 ms（DynamoDB put）+ 1 ms（缓存更新）= 6 ms。p99 ~50 ms。远低于预算。 *...

=== exactly (10 occurrences) ===
  c15_b03.md: ...rier”）流过数据流图。每个算子在 barrier 经过时记录状态；合在一起，这些状态是一个一致快照。Flink 的 exactly-once 语义建立于此。  * **某些实现的分布式事务** ：特定时间戳处系统的 “快照” 概念上是一个 Chand...
  c15_b07.md: ...快照是系统的一个 _合法_ 状态。  这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思...
  c15_b08.md: ...长或用户投诉持续，评估 (e)。  **Q2.** 一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的...
  c15_b08.md: ...致状态；故障时从上一个 checkpoint 重启。Sink 必须幂等或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消...
  c15_b09.md: ...间隔（通常 1-30 秒）。对需要更低端到端延迟的工作负载，这是权衡。  没有 Chandy-Lamport 风格快照，exactly-once 不可能：系统无法捕获重启的一致状态。该协议是状态流处理整个类别的基础。  **Q3.** 一个团队有全球多区...
  c15_b11.md: ...marker 定义快照边界；记录的各进程状态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly-once 语义和类似分布式 checkpointing 系统的基础。  计数器 / 排行榜 / 库存工作负载展示了频谱...
  c30_b11.md: ...#### F.4 — 高吞吐流处理 / 事件处理  **输入**：100K-1M 事件/秒；分区内有序处理；端到端 exactly-once；分钟到小时级延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或...
  c30_b11.md: ...* **Stream processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreS...
  c30_b11.md: ...ry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materializ...
  c30_b19.md: ...误（未检测到失败） | Ch.21 §21.4（熔断器状态）   流处理 trace 显示 job 重启后重复事件 | Exactly-once 破坏；缺少 checkpoint 协调 | Ch.15 §15.4（Chandy-Lamport），Ch.4...

=== rga (10 occurrences) ===
  c16_b03.md: ...sition identifiers 可能变长（交替插入可使 id 长度翻倍）。删除字符的垃圾回收微妙。   #### RGA（Replicated Growable Array）  另一种方法：每个字符带一个 ID（通常是 (timestamp...
  c16_b03.md: ...构类似树；线性化是树遍历。   #### Yjs 的实现  Yjs（Kevin Jahns 约 2014 年启动）使用 RGA 的优化变体。每个字符由一个小结构体表示（约 8 字节），包含其 clock、client ID 和指向前驱的指针。该结...
  c16_b03.md: ...omerge 的实现  Automerge（Martin Kleppmann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间...
  c16_b07.md: ...定 CRDT。对需要拒绝并发写的字段，线性化。  **Q4.** Yuki 的 Glide 编辑器使用 Yjs。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有...
  c16_b07.md: ...s。为什么 RGA 是该用例的正确 list CRDT，Yjs 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size 优化。  **答案：** RGA（Repl...
  c16_b07.md: ...提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size 优化。  **答案：** RGA（Replicated Growable Array）把列表表示为图：每个字符有唯一 ID 和指向前驱（插入位置后的字符...
  c16_b07.md: ...字符有唯一 ID 和指向前驱（插入位置后的字符）的指针。列表顺序由该图的确定性遍历决定；并发插入按 ID 顺序解析。  RGA 适合协作文本编辑的原因：  * 插入是本地的（只记录前驱）；无需全局同步。 * 同一位置并发插入被确定性解析（编辑器永...
  c16_b07.md: ...c、WOOT）存在问题：标识符增长、合并慢、内存占用大。Yjs 是首个对生产环境浏览器协作编辑来说足够快、足够小的实用 RGA 实现。...
  c28_b10.md: ...i 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定性合并后的文档状态 | RGA 风格列表 CRDT + WebSocket 同步 + awareness 协议 | 在 CRDT 中存储临时光标位置；...
  c30_b14.md: ...Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT     ├── ch18_locks/     │   ├── redlock_naiv...

=== envoy (10 occurrences) ===
  c19_b04.md: ...voy；Kubernetes 原生）和 Linkerd（更轻量）。两者都用 etcd/Kubernetes 做发现，用 envoy 做数据面。  服务网格采用已趋于平稳；许多团队发现运维开销大于收益。服务图简单（10 个服务、单区域）的团队，服务网格...
  c19_b10.md: ...量的替代（Linkerd）或完全不用网格。  第二个考虑：服务网格价值很大程度上取决于数据面技术。Istio 的数据面 Envoy 很重；基于 eBPF 的替代（Cilium）更轻。随着 eBPF 网格成熟，服务网格运维成本下降，成本收益计算会改变。...
  c21_b05.md: ...端侧被抑制；原始失败返回应用代码，由它决定高层重试还是向用户失败。  gRPC 的 retry policy 支持预算；Envoy 的 retry policy 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。...
  c21_b06.md: ...ix（已弃用；被 Resilience4j 替代）；Resilience4j（现代 Java）；polly（.NET）；Envoy 和 HAProxy 作为反向代理。该模式在成熟分布式系统中是标准。  **Q4.** 2003 年美国东北部大停电教给...
  c21_b07.md: ...。修改每个重试感知库或 sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、HTTP 客户端自定义逻辑。  (b) **每对上下游之间加熔断器**：尤其应用服务与共享基...
  c21_b07.md: ...与共享基础设施（如数据库）之间。工具：Resilience4j（Java）、其他语言 Hystrix-like 库，或 Envoy 代理。  (c) **每个服务入口加 load shedding**：每个服务拒绝超出其并发容量的入站请求。返回 HT...
  c22_b08.md: ...(h) **主动探测**：除被动观察外，负载均衡器可向每个后端发合成请求，检测被动观察可能遗漏的灰色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer...
  c22_b09.md: ...对 Crescent：区域负载均衡器上使用带 outlier detection 的 Envoy。表现出集群中位数 5 倍延迟的后端被 “弹出” 活动池；通过探测定期测试；恢复后重新加入。...
  c26_b09.md: ...倍。  (d) **尾部感知负载均衡器**：监控 per-node 延迟；若某节点异常高（灰色故障，第22章），绕行。Envoy outlier detection 或类似。  (e) **限制每请求最大输出长度**：限制 max_tokens；拒...
  c28_b02.md: ...agerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluster 内。  (l) **锁服务**：etcd cluster 提供分布式锁（安全关键操作使用 fen...

=== attributes (10 occurrences) ===
  c20_b01.md: ...nt span ID（根为 null）。 * 时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * Attributes（自定义键值对）。 * Events（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spa...
  c20_b05.md: ...是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的...
  c20_b05.md: ...和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志...
  c20_b05.md: ...eaders（W3C 标准的 `traceparent`）或消息队列元数据（Kafka headers、AWS SQS attributes）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 t...
  c20_b05.md: ...是 spans 树。每个 span 携带 trace ID、span ID、parent span ID、时间戳、状态、attributes。  **答案：** 分布式 trace 是单个分布式请求的因果记录：请求触发的跨服务工作链。它结构化为 spans 的...
  c20_b05.md: ...和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志...
  c20_b05.md: ...eaders（W3C 标准的 `traceparent`）或消息队列元数据（Kafka headers、AWS SQS attributes）。OpenTelemetry 库对常见框架自动处理；自定义协议需要显式传播逻辑。  纪律：每次跨服务交互都必须传播 t...
  c20_b07.md: ...发现 80% 这些错误来自一个特定下游服务。走一遍从 trace 到修复的诊断流程。  提示  按服务分组错误；看错误 attributes（状态码、错误消息）；找模式（特定时间窗口、特定请求类型）。  **答案：** 诊断流程：  (a) **按服务分组错误...
  c20_b07.md: ...：  (a) **按服务分组错误**：80% 来自 service X，是主导集群。聚焦那里。  (b) **检查错误 attributes**：错误码是什么？500 vs 503 vs 超时？每个暗示不同根因：  * 500：service X 逻辑 bug...
  c20_b07.md: ...集中在特定时间窗口？若是，找相关事件（部署、流量尖峰、基础设施变更）。若不是，问题是持续性的。  (d) **检查请求 attributes**：失败请求是否针对特定租户、特定 endpoint、特定输入模式？找输入相关性。  (e) **钻取一条失败 tra...

=== serving (10 occurrences) ===
  c24_b01.md: ...ted prefill/decode、KV cache sharding）。本章介绍推理集群和三个典型的 2026 年 serving 系统：vLLM、SGLang 和 NVIDIA NIM。  对 Tobias 的账单，答案在接下来两章。本章建立基线。...
  c24_b02.md: ...**：每张 GPU 并发请求更多；每个请求成本更低。  vLLM 采用迅速。到 2026 年，它是最广泛部署的开源推理 serving 系统。Anyscale、Together、Fireworks 和许多其他推理公司使用 vLLM（或 vLLM 衍生系统...
  c24_b05.md: ...延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提升通常 2-5 倍。   ###...
  c24_b05.md: ...us batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提升通常 2-5 倍。   ### 24.8 练习...
  c24_b07.md: ...PagedAttention（KV cache 内存管理）。 * 优势：通用，多种工作负载下高吞吐。 * 最适合：任意 serving、无特定前缀复用模式。  _SGLang_：  * 核心创新：RadixAttention（基于前缀的 KV cache...
  c24_b07.md: ...中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous batching 现在是生产推理引擎默认（vLLM、SGLang、TGI...
  c24_b09.md: ...更快）；12 个月 timeline → 两者都可。 * **定制化需求**：重度定制 → vLLM（完全控制）；标准 serving → NIM（集成优于灵活）。 * **成本敏感度**：非常敏感成本 → vLLM 加自定义集成；CapEx 灵活 →...
  c30_b03.md: ...023). "Efficient Memory Management for Large Language Model Serving with PagedAttention." _SOSP_. — vLLM。 * Zheng, L., et al. (...
  c30_b03.md: ...saggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-ce...
  c30_b03.md: ...ncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (https://github.com...

=== disaggregation (10 occurrences) ===
  c24_b08.md: ...0-40%。  叠加：continuous batching（若尚未），然后 SGLang 用于前缀复用工作负载，然后 disaggregation。预期最终集群利用率 80%+，在 120 万美元账单上每月节省 25-40 万美元。  其他技术（简述）：跨节点 KV...
  c25_b09.md: ...hase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation...
  c25_b09.md: ...disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sha...
  c25_b09.md: ...hase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **Q3.** 比较 disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation...
  c25_b09.md: ...disaggregation 与 model sharding（把模型跨多张 GPU 拆分）。何时需要两者？  提示  Disaggregation：拆分阶段。Sharding：拆分模型。正交维度；常组合使用。  **答案：** 两种正交技术：  _Model sha...
  c26_b01.md: ...生成；可作为跨机分布式协议实现）。  本章结束第八部分。Tobias 的 Continuum 集群完整优化故事至此完成：disaggregation（第25章）、KV cache sharding（第25章）、KV-cache-aware 路由（本章）、specula...
  c26_b04.md: ...*Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **硬件**：面向 disaggregation 优化的 H100/H200 混合。  结果：  * 集群利用率：85%（从 60% 提升）。 * 每个请求成本：比基线...
  c26_b06.md: ...rification（每请求一个 verify 步）。昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggregation（第25章）类似，分布式 speculative decoding 增加运维复杂度；大规模才划算。  (d) **Dra...
  c26_b08.md: ...) **Prompt 长度可变**：50000-token prefill 需 10 秒以上；同节点其他请求等待它完成。Disaggregation（分离 prefill 节点）修复此问题。  (c) **GPU 竞争**：若多请求竞争同节点 GPU 内存带宽，最慢者...
  c26_b11.md: ...ier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cache-aware 路由 + 长度预测路由 + 分布式 spec...

=== scheduling (10 occurrences) ===
  c26_b04.md: ...*：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robi...
  c26_b04.md: ...；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 S...
  c26_b04.md: ...ng 跨 draft 和 verify 节点分布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **...
  c26_b06.md: ...reworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fair scheduling？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair s...
  c26_b06.md: ...g？  提示  每客户有权重；调度器按比例分配 GPU 时间；防止饥饿。  **答案：** Weighted fair scheduling：每客户（或租户）有权重；调度器按权重比例在客户间分配 GPU 时间。  示例：三客户权重 5、3、2（共 10）。调度...
  c26_b06.md: ...每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等下一轮。  (c) **Hierarchical scheduling**：客户可分组（如按 tier）；调度器分层（tier 内公平；tier 间有优先级）。...
  c26_b08.md: ...降到约 5 秒。  (e) **客户 SLA 调度**：该客户是 tier-2（中优先级）。Weighted fair scheduling 确保其请求被处理但不饿死 tier-1 客户。  综合：该客户每请求成本降低约 5 倍（前缀复用），延迟显著改善。Co...
  c26_b11.md: ...draft 在便宜硬件，verify 在大 GPU）独立扩缩；适合大集群。  多租户公平性：weighted fair scheduling 按客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fai...
  c26_b11.md: ...客户权重分配 GPU 时间。防止饥饿；支持 SLA tier。Continuum 使用分层 weighted fair scheduling。  Tobias 的完整优化栈：disaggregation + KV cache sharding + KV-cac...
  c26_b11.md: ...ware 路由 + 长度预测路由 + 分布式 speculative decoding + weighted fair scheduling + 按引擎选择（vLLM/SGLang）。结果：集群利用率 85%（从 60%）、每请求成本降低 40%、年节省 50...

=== sensor (10 occurrences) ===
  c30_b08.md: ...审查流水线捕获，因为循环条件依赖输入数据而非代码形状。   #### E.4 — CrowdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更...
  c30_b08.md: ...lcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏。 * **放大**：更...
  c30_b08.md: ...机器同时变砖；多日恢复；航空、医院、金融服务严重受影响。 * **恢复**：每台机器手动恢复（启动到安全模式，删除坏 sensor 文件）。 * **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canar...
  c30_b08.md: ...* **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触发）：把部署视为触发...
  c30_b08.md: ...rollout（1% → 10% → 100%）是标准防御。     * Ch.27（LLM 辅助审查架构）：内核模式 sensor 的输入处理正是审查清单必须捕获缺失验证的代码类型。...
  c30_b08.md: ...审查流水线捕获，因为循环条件依赖输入数据而非代码形状。   #### E.4 — CrowdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更...
  c30_b08.md: ...lcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏。 * **放大**：更...
  c30_b08.md: ...机器同时变砖；多日恢复；航空、医院、金融服务严重受影响。 * **恢复**：每台机器手动恢复（启动到安全模式，删除坏 sensor 文件）。 * **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canar...
  c30_b08.md: ...* **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触发）：把部署视为触发...
  c30_b08.md: ...rollout（1% → 10% → 100%）是标准防御。     * Ch.27（LLM 辅助审查架构）：内核模式 sensor 的输入处理正是审查清单必须捕获缺失验证的代码类型。...

=== inference (9 occurrences) ===
  c01_b03.md: ...tanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不同，心智动作相同。...
  c23_b13.md: ...vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  ---...
  c24_b01.md: ...Tobias Schwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户运行 LLM 推理；每月基础设施账单 120 万美元。CFO...
  c24_b04.md: ...### 24.4 NVIDIA NIM  NIM（NVIDIA Inference Microservices）是 NVIDIA 的企业推理部署平台。约 2024 年 GA 发布；企业 LLM 部署的商...
  c24_b04.md: ...┌─────────────┐           ┌─────────────┐             │  Inference   │           │  Inference   │             │   Node 1     │...
  c24_b04.md: ...┌─────────────┐             │  Inference   │           │  Inference   │             │   Node 1     │           │   Node 2     │...
  c28_b01.md: ...og 层）。Yuki 从 Glide 借调来负责协作文档编辑组件（商户入驻流程）。Tobias 从 Continuum Inference 借调来负责 AI 驱动的欺诈检测层。  他们交付的系统是全书的综合。每个原语都在其中。每章的课都被运营化。这个系统回答...
  c30_b03.md: ...el, P., et al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al....
  c30_b03.md: ...层。 * Leviathan, Y., Kalman, M., & Matias, Y. (2023). "Fast Inference from Transformers via Speculative Decoding." _ICML_.   ####...

=== tls (9 occurrences) ===
  c02_b02.md: ...在足够的带宽把它送到需要去的地方。   #### 网络是安全的  默认情况下每个数据包都是裸露的。加密是 opt-in。TLS 加密两个端点之间的通信；它并不验证端点，除非证书校验正确，而且它也无法抵御某个已被攻陷的已认证端点。你的流量“走 HT...
  c06_b04.md: ...见延迟目标。** 交互应用常 100-200 ms p99；异步工作流 1-2 秒。   2. **减去固定成本。** TLS 握手（首次连接 10-100 ms）、CDN edge 到 origin（5-50 ms）、认证（5-20 ms）。...
  c06_b04.md: ...免跨区域跳，或通过缓存接受过期。  Crescent 支付系统示例：  * 目标：支付授权 500 ms p99。 * TLS、认证、应用逻辑：100 ms。 * 幂等性检查：10 ms（本地缓存 + 5 ms DB 查询）。 * 风险评分：50...
  c06_b09.md: ...除后，余数分配给本地 hop。  **答案：** 预算拆分，先把 8 个服务 hop 按串行处理。  固定成本：  * TLS 握手（首次连接，常复用 keep-alive）：~30 ms。 * 认证检查：~20 ms。 * 跨区域 hop 往返...
  c13_b11.md: ...结果，典型 p50 延迟约 ~360 ms，p99 约 ~580 ms——第28章 §28.2 会分解完整端到端，包括 TLS、geo-DNS、风控检查（Continuum LLM）和返回路径。Crescent 产品团队谈妥的 750 ms p9...
  c19_b07.md: ...watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。  **Phas...
  c20_b01.md: ...>POST /payment<br/>~365 ms total"]         NW["network in + TLS<br/>65 ms (edge + cross-equator)"]         A["auth + idempo...
  c28_b02.md: ...行，并提供实时欺诈检测。  组件：  (a) **API 网关**：Cloudflare Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 reg...
  c28_b03.md: ..."Edge (Cloudflare Workers)"]             CF[API Gateway<br/>TLS / auth / rate limit]         end         subgraph USE["US-E...

=== merge (9 occurrences) ===
  c02_b11.md: ...展示浏览量晚几秒）远低于线性化计数（每次增量必须协调）的系统成本。基于 CRDT 的计数器或最终一致 put-with-merge 可在几乎无协调的情况下服务该工作负载。  _排行榜_：因果一致，因为如果玩家 B 提交前看到了玩家 A 的分数，则两个...
  c16_b01.md: ...：每个副本持有完整状态。合并时，合并函数取两个状态并产生新状态。合并必须满足：  * **交换律** ：\(\text{merge}(a,b) = \text{merge}(b,a)\) * **结合律** ：\(\text{merge}(\text...
  c16_b01.md: ...取两个状态并产生新状态。合并必须满足：  * **交换律** ：\(\text{merge}(a,b) = \text{merge}(b,a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c) = \t...
  c16_b01.md: ...(\text{merge}(a,b) = \text{merge}(b,a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c) = \text{merge}(a,\text{merge}(b,c))\)...
  c16_b01.md: ...(a,b) = \text{merge}(b,a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c) = \text{merge}(a,\text{merge}(b,c))\) * **幂等律** ：\...
  c16_b01.md: ...a)\) * **结合律** ：\(\text{merge}(\text{merge}(a,b),c) = \text{merge}(a,\text{merge}(b,c))\) * **幂等律** ：\(\text{merge}(a,a) = a\...
  c16_b01.md: ...：\(\text{merge}(\text{merge}(a,b),c) = \text{merge}(a,\text{merge}(b,c))\) * **幂等律** ：\(\text{merge}(a,a) = a\)  这三条性质使状态在合并下...
  c16_b01.md: ...c) = \text{merge}(a,\text{merge}(b,c))\) * **幂等律** ：\(\text{merge}(a,a) = a\)  这三条性质使状态在合并下形成一个 join-semilattice。任意合并序列都会收敛到所...
  c30_b02.md: ..., E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & Dabek,...

=== last (9 occurrences) ===
  c03_b11.md: ...# 应用问题  **Q1.** 一个团队在三个数据中心运行 Cassandra，每秒 100,000 次写入，偶尔发现“last write wins” tombstone 复活了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cass...
  c03_b11.md: ...ck 时间戳。如果删除被盖戳早于后续写入的时间戳，会发生什么？  **答案：** Cassandra 的默认冲突解决是 last-write-wins（LWW），基于协调者节点分配的 wall-clock 时间戳。如果删除由时钟落后的协调者处理，它...
  c05_b03.md: ...点死亡不会中断系统。代价是冲突必须在读时或后台修复时解决，因为两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏...
  c15_b02.md: ...# 15.3 冲突解决策略  当两个并发写冲突（两个副本接受同一键的不同值）时，系统必须解决。三种策略：  (a) **Last write wins（LWW）** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。...
  c15_b03.md: ...y unit。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-write-wins。应用必须处理自己写入一个区域后被另一区域并发写覆盖的情况（罕见但可能）。   #### Cass...
  c15_b07.md: ...y-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last write wins”（LWW）冲突解决，它的失败模式是什么？  提示  LWW：时间戳最高的写胜出。失败：时钟偏移可...
  c15_b09.md: ...m 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时钟偏移窗口内），...
  c19_b08.md: ...中断 30 秒。诊断并给出修复方案。  提示  Consul 基于 gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：** 诊断：Consul 故障转移期间，集群重新选举 Raft leade...
  c19_b08.md: ...Consul 的客户端可能收到过期或错误响应。  若客户端配置为 “fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail closed”（Consul 不可达时返回错误），每次服务调...

=== like (9 occurrences) ===
  c03_b14.md: ...Google 之外提供 TrueTime 风格能力，我们可能会看到高利害工作负载缓慢迁移到云厂商的 TrueTime-like 模型。  第二个考虑：HLC 的“最大时钟偏移”上界是一个配置参数。如果实际偏移超过该上界，数据库会静默产生非线性化结...
  c13_b05.md: ...azon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到 CockroachDB / TiDB 采用此路径。   ### 13.6.5 Sag...
  c13_b17.md: ...次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide 的跨区域支付事务在 CockroachDB 中约 120 ms 原子提交到...
  c16_b03.md: ...ge 的实现  Automerge（Martin Kleppmann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间旅行和丰富...
  c16_b03.md: ...ann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间旅行和丰富的合并 UI。  Automerge 2.x（2023 年发布...
  c16_b03.md: ...le 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在...
  c16_b09.md: ..._Automerge_ ：  * 优点：完整文档历史是数据结构的一部分；自然的 “谁在何时改了什么” UI；为 git-like 文档工作流设计。 * 缺点：每文档内存占用更大（存储历史）；历史上较慢，但 Automerge 2.x 在 2026...
  c21_b07.md: ...**：尤其应用服务与共享基础设施（如数据库）之间。工具：Resilience4j（Java）、其他语言 Hystrix-like 库，或 Envoy 代理。  (c) **每个服务入口加 load shedding**：每个服务拒绝超出其并发容量的...
  c30_b14.md: ...rved-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT     ├── ch18_locks/     │   ├── redlock_naive.py...

=== total (9 occurrences) ===
  c04_b04.md: ...对任意两条消息都可能交互的应用（更新共享状态），需要原子广播。   ### 4.6 原子（全序）广播（Atomic / Total-Order Broadcast）  四层中最强。原子广播新增：_所有正确进程以相同的总顺序交付消息。_  该总顺序与因...
  c04_b04.md: ...里先命名算法（Paxos、Raft、ZAB、HotStuff），第三部分再展开。  性质（在因果广播之外）：  * **Total order** ：所有正确进程以相同顺序交付消息。  代价：任何原子广播协议的下界至少是共识的代价，在部分同步假设下每...
  c04_b05.md: ...<br/>(vector clocks)"| D[Causal broadcast]         D -->|"+ total order<br/>(consensus)"| E["Atomic broadcast<br/>= Consensus...
  c04_b05.md: ...clocks)         v     Causal broadcast         |         + total order (consensus)         v     Atomic broadcast == Consens...
  c04_b07.md: ...liable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effort broadcast** ：vali...
  c04_b07.md: ...播：每个消息槽运行一次共识。  **答案：** _原子广播 → 共识_ 。假设有原子广播。每个进程广播自己的提议值。由 total-order 性质，所有正确进程以相同顺序交付这些广播。每个进程决定其交付的第一个广播所包含的值。由总序，所有正确进程交...
  c13_b10.md: ...-- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-0...
  c13_b10.md: ...- 更新 Femi 的日汇总       UPDATE merchant_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';...
  c20_b01.md: ...graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["network in + TLS<br/>65 ms (edge + cross-equa...

=== group (9 occurrences) ===
  c04_b10.md: ...进程聚类时效果良好。  (b) **分层 / 分组因果广播** 。把进程分组（例如按区域或租户）。每条消息携带 per-group 向量（组大小条目）加一个 per-region 摘要。跨组因果以较粗粒度跟踪。可扩展 gossip 系统就是这样做的；...
  c12_b04.md: ...点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft group 领导权转移给目标节点，源节点的副本被移除。   #### 步骤 4：验证  重平衡完成后，集群监控：  * 每个节点的...
  c12_b04.md: ...4：验证  重平衡完成后，集群监控：  * 每个节点的负载分布（每秒查询数）。 * 每个节点的磁盘使用。 * Raft group 健康（无卡住副本、无 underreplication）。  若某 range “卡住”（例如移动期间 Raft gr...
  c12_b04.md: ...up 健康（无卡住副本、无 underreplication）。  若某 range “卡住”（例如移动期间 Raft group 落后），运维介入。Anya 之前三次重分片中有两次遇到；每次 10 分钟解决。   #### 步骤 5：退役旧硬件（如...
  c12_b05.md: ...复是在移动期间节流其他流量。  (c) **移动期间节点故障** ：源或目标节点故障需要重新规划移动。集群的 Raft group 继续服务（per-range 多数不受影响），因此用户可见影响只是短暂降级。  纪律是运维层面的：runbook 是多...
  c13_b14.md: ...三个分片。追踪若事务协调者在提交中途死亡会发生什么。  提示  协调者状态通过 Raft 复制。新协调者（由 Raft group 选出）恢复协议。  **答案：** 在 CockroachDB 中，事务协调者状态作为记录存在 system rang...
  c13_b14.md: ...；该 range 本身被 Raft 复制。若运行协调者角色的节点死亡：  (a) 协调者状态 range 的 Raft group 检测到故障（心跳超时）并在数秒内选出新 leader。  (b) 新 leader 继承协调者状态（事务记录、参与者列...
  c18_b08.md: ...生在 Raft AppendEntries 阶段，而非仅在 API 层。这确保即使过期领导者的 RPC 到达 Raft group，条目在共识层也被拒绝；应用逻辑永远看不到它们。...
  c19_b09.md: ...etcdctl put feature_flags/new_checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务...

=== room (9 occurrences) ===
  c04_b10.md: ...系统，正确的问题是：应用是否真的需要 _所有_ 消息的因果广播，还是因果可以限定在 per-entity 或 per-room 操作？前者很少成立；后者通常够用。...
  c17_b07.md: ...#### 应用问题  **Q1.** 一个团队要构建聊天应用。聊天是 per-room；用户发帖和回复。选择一致性模型并论证。  提示  回复必须跟随原帖（因果）；不同用户的并发消息可任意顺序（因果足够）；...
  c17_b07.md: ...息应在下次查看会话时看到。  为何不用线性化：  * 线性化要求每条消息全局排序；聊天服务会在共识吞吐处瓶颈。 * 跨 room 顺序无关；单 room 内线性化过度。 * 每次发帖的延迟成本（共识往返）不必要；“你的帖子 100ms 内可见” 可...
  c17_b07.md: ...。  为何不用线性化：  * 线性化要求每条消息全局排序；聊天服务会在共识吞吐处瓶颈。 * 跨 room 顺序无关；单 room 内线性化过度。 * 每次发帖的延迟成本（共识往返）不必要；“你的帖子 100ms 内可见” 可接受，但不应需要 100...
  c17_b07.md: ...到帖的副本）。这是 read-your-writes 失败。 * 回复可能在某些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时携带其 HLC；服务按 HLC 排序消息；读查询 room 当前 HLC 前沿。大多数聊天系统（Sl...
  c17_b07.md: ...些视图中出现在原帖之前。  实现：per-room HLC；消息发帖时携带其 HLC；服务按 HLC 排序消息；读查询 room 当前 HLC 前沿。大多数聊天系统（Slack、Discord）底层大致如此。  扩展：room 跨节点分片；每个 r...
  c17_b07.md: ...C 排序消息；读查询 room 当前 HLC 前沿。大多数聊天系统（Slack、Discord）底层大致如此。  扩展：room 跨节点分片；每个 room 的 HLC 是本地的；跨 room 流量不协调。Slack 架构大致如此。  **Q2.*...
  c17_b07.md: ...m 当前 HLC 前沿。大多数聊天系统（Slack、Discord）底层大致如此。  扩展：room 跨节点分片；每个 room 的 HLC 是本地的；跨 room 流量不协调。Slack 架构大致如此。  **Q2.** 一个团队使用 Mongo...
  c17_b07.md: ...统（Slack、Discord）底层大致如此。  扩展：room 跨节点分片；每个 room 的 HLC 是本地的；跨 room 流量不协调。Slack 架构大致如此。  **Q2.** 一个团队使用 MongoDB 因果一致会话。他们注意到网络抖...

=== monotonic (9 occurrences) ===
  c05_b04.md: ...Theimer 等，1994）：  * **Read-your-writes** ：任何后续读都能看见此前写。 * **Monotonic reads** ：后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writ...
  c05_b04.md: ...low-reads** ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。 * **Monotonic writes** ：你的写按你发出顺序应用。 * **Consistent prefix** ：你看到全局写顺序的一个...
  c15_b01.md: ...ser 1994）：  (a) **Read-your-writes** ：同一会话后续读能看到先前写。  (b) **Monotonic reads** ：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes...
  c15_b01.md: ...s** ：会话发出的任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X 之后。  (d) **Monotonic writes** ：会话的写按发出顺序应用。  (e) **Consistent prefix** ：即使看不到最新，...
  c15_b06.md: ...念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefi...
  c15_b06.md: ...。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的...
  c15_b06.md: ...念检测  **Q1.** 列出五种会话保证。为每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefi...
  c15_b06.md: ...。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的...
  c30_b14.md: ...│   └── fencing_token_demo.py — protected resource enforces monotonic tokens     ├── ch20_tracing/     │   └── propagation_demo.p...

=== result (9 occurrences) ===
  c05_b09.md: ...= (0, 0)          def write(self, key, value):             result = db.write(key, value)             # 服务器返回写提交时的 HLC。...
  c05_b09.md: ...务器返回写提交时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return result          def read(self, key)...
  c05_b09.md: ....max_hlc = max(self.max_hlc, result.hlc)             return result          def read(self, key):             # 请求数据的最小 HLC 为 s...
  c05_b09.md: ...key):             # 请求数据的最小 HLC 为 self.max_hlc。             result = db.read(key, min_hlc=self.max_hlc)             # 服务器返回数据及...
  c05_b09.md: ...返回数据及读取时的 HLC。             self.max_hlc = max(self.max_hlc, result.hlc)             return result.value      # 服务器端读处理器：     d...
  c05_b09.md: ....max_hlc = max(self.max_hlc, result.hlc)             return result.value      # 服务器端读处理器：     def handle_read(key, min_hlc):...
  c27_b02.md: ...而非语法。  LLM 生成代码：       def submit_payment(payment):         result = payment_service.charge(payment)         if result.success...
  c27_b02.md: ...result = payment_service.charge(payment)         if result.success:             db.insert(payment.id, status='complete...
  c27_b02.md: ...payment.idempotency_key = generate_unique_key()         result = retry_with_idempotency(payment_service.charge, payment, k...

=== return (9 occurrences) ===
  c05_b09.md: ...self.max_hlc = max(self.max_hlc, result.hlc)             return result          def read(self, key):             # 请求数据的最小...
  c05_b09.md: ...self.max_hlc = max(self.max_hlc, result.hlc)             return result.value      # 服务器端读处理器：     def handle_read(key, min_...
  c05_b09.md: ...> timeout:                 raise StaleReplicaError         return read_local(key, at_hlc=server.local_hlc)   当客户端从落后的 followe...
  c20_b01.md: ...e<br/>30 ms (serial after DB)"]         POST["post-commit + return<br/>73 ms (Kafka publish + network out)"]         R --> NW...
  c22_b01.md: ...认建议：       @app.route('/healthz')     def health():         return 'OK', 200   > 说明性伪代码；bug 在于检查 _没有_ 锻炼的内容（真正的 I/O 路径），而非语法。...
  c22_b11.md: ...算如何？有这上下文，我才能设计适合你服务的 liveness、readiness 和 startup 检查。” 单行 “return 200” 只适用于最简单的无状态服务。  更深观察：健康检查是系统可靠性故事的一部分。设计不良的健康检查会导致脆弱部署...
  c27_b02.md: ...络可靠  LLM 生成代码：       def get_user_profile(user_id):         return profile_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 ci...
  c27_b02.md: ...def get_user_profile(user_id):         try:             return profile_service.get(user_id)         except CircuitBreakerO...
  c27_b02.md: ...get(user_id)         except CircuitBreakerOpen:             return cached_profile_or_default(user_id)   装饰器栈反映纪律。LLM 产出裸调用是因为训...

=== number (9 occurrences) ===
  c08_b02.md: ...第一阶段：Prepare  想提议一个值的 proposer 选一个 _ballot number_ b。Ballot number 唯一分配（例如 (counter, proposer_id) 按字典序）且随时间单调递增。  Proposer 向每个...
  c08_b02.md: ...。   ### 8.4 为什么 Paxos 是安全的  Safety 论证分三部分。  **第 1 部分：ballot number 是全序的。** 每个 ballot 唯一，且 ballot 单调发放。协议行为只依赖 ballot 顺序，不依赖真实时...
  c08_b03.md: ...跑一次第一阶段，然后每个后续槽只跑第二阶段。  结构：    1. 想成为 leader 的进程选一个高 ballot number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREPARE(b)`）。   3. Accepto...
  c08_b06.md: ...阶段：提议并让多数接受。  **答案：**  _第一阶段（Prepare）：_ Proposer 选一个 ballot number b，发送 `PREPARE(b)` 给所有 acceptor。未承诺任何更高 ballot 的 acceptor 回复...
  c08_b09.md: ...议。即使多个 proposer 并发运行，协议 safety 仍成立。新 proposer 可通过选更高 ballot number 接管。  _Paxos 故障_ ：若 proposer 在协议中途崩溃，另一 proposer 可选更高 ballot...
  c08_b10.md: ...se 并可接受冲突值，违反 safety。单进程测试无法发现。  (b) **Ballot 唯一性。** Ballot number 必须在所有 proposer 间唯一且每个 proposer 单调递增。LLM 代码常用简单计数器而不带 propos...
  c09_b01.md: ...号。Raft 的每个 RPC 消息都携带发送者当前 term。  Term 是 Raft 对 Paxos ballot number 的类比。规则：  * 每个服务器在稳定存储上保存 `currentTerm`（已知的最高 term）。 * 服务器在任...
  c09_b06.md: ...新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递增且全序。结构作用相似（防止过期提议被接受）。 * Paxos ballot 可由任何 proposer 随时...
  c18_b09.md: ...ly 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “epoch number”，提供运维安全。即使在复杂故障转移期间（集群重选领导者；旧领导者在网络抖动后重新加入并尝试发命令），token 排序保...

=== new (9 occurrences) ===
  c08_b06.md: ...数恰好已接受）也可能没有（若接受者更少）。  若新 proposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违...
  c08_b06.md: ...者更少）。  若新 proposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Ac...
  c08_b06.md: ...{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Accept 规则（proposer 必须采用第一阶段...
  c09_b04.md: ...选出不同 leader。  Raft 的解：_joint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过中间配置 $C_{old,new}$，该配置要求 _同时_ 得到旧配置和新配置的多数。过渡期间任何决定都...
  c09_b04.md: ...oint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过中间配置 $C_{old,new}$，该配置要求 _同时_ 得到旧配置和新配置的多数。过渡期间任何决定都需要联合多数。过渡完成后，集群才切换到 $C_{...
  c09_b04.md: ...}$，该配置要求 _同时_ 得到旧配置和新配置的多数。过渡期间任何决定都需要联合多数。过渡完成后，集群才切换到 $C_{new}$。  这是 Raft 较复杂的部分之一，常是实现 bug 的来源。生产 etcd 使用更简单的替代方案：一次只改一个...
  c16_b03.md: ...#### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文...
  c25_b02.md: ...D2["Decode Node 2<br/>8× H200"]         end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 --...
  c25_b02.md: ...end         LB -->|"new request"| P1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1...

=== mempool (9 occurrences) ===
  c10_b01.md: ...Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --> Aptos2...
  c10_b03.md: ...提高吞吐。变体：Tendermint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的 mempool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader...
  c10_b03.md: ...otStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotStuff 只用于区块链。”HotStuff 是 _共识协议_ ；区块链...
  c10_b04.md: ...an）做出结构性创新：把 _mempool_（在交易到达共识前对它们排序的层）与 _consensus_（决定提交哪些 mempool 块的层）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成...
  c10_b04.md: ...易到达共识前对它们排序的层）与 _consensus_（决定提交哪些 mempool 块的层）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mi...
  c10_b04.md: ...个 block 都被签名并引用）。  生产中：  * **Aptos** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一...
  c10_b04.md: ...hark 的吞吐声明惊人：每秒数万笔交易，而 PBFT 只有数百。延迟声明也有竞争力（亚秒级提交）。复杂度在于 DAG mempool 的正确性及其与共识层的集成。   ### 10.5 Tendermint：工作马  Tendermint 在 Buch...
  c10_b12.md: ...$O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-Bullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  He...
  c30_b02.md: ...A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelman, A.,...

=== question (9 occurrences) ===
  c11_b03.md: ...Olamide 把方法用于五个系统。输出是每个系统的一页阅读笔记。   #### Spanner（Google）  Question | Answer ---|--- Replication protocol | 每 tablet Paxos；线性化时...
  c11_b03.md: ...r | 需要全局线性化的工作负载（广告、金融账本）；Google 自身基础设施   #### CockroachDB  Question | Answer ---|--- Replication protocol | 每 range Raft；HLC-ba...
  c11_b04.md: ...#### DynamoDB（AWS）  Question | Answer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用...
  c11_b04.md: ...约束 Right for | 访问模式可预测的单租户键值工作负载；AWS 原生系统   #### Cassandra  Question | Answer ---|--- Replication protocol | 无 leader；按请求可调一致性（O...
  c11_b04.md: ...ght for | 写重且可放宽一致性的工作负载；追求每节点简单的大规模系统   #### FoundationDB  Question | Answer ---|--- Replication protocol | 自定义（sequencer + tra...
  c11_b05.md: ...### 11.5 Anya 的 Stratus DB 阅读  Anya 把同样方法用于她正在开发的系统。阅读结果：  Question | Answer（Stratus DB） ---|--- Replication protocol | 每 range...
  c11_b08.md: ...范式的金融级账本数据库。找其复制协议、故障模型、一致性和分区行为。  **答案：** TigerBeetle 阅读：  Question | Answer ---|--- Replication protocol | 自定义 Viewstamped Rep...
  c11_b09.md: ...d-per-core。协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Question | Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一...
  c14_b07.md: ...eTime vs HLC、仅限 GCP vs 任何云、专有 SQL vs Postgres 兼容。  **答案：**  Question | Spanner | CockroachDB ---|---|---- Replication protocol |...

=== behavior (9 occurrences) ===
  c11_b03.md: ...-lease 读很快 Consistency model | 读写事务线性化；只读可提供有界过期读 Partition behavior | CP——少数侧停止接受写；TrueTime 确保分区后时间戳一致 PACELC | PC / EC（分区下一致；正...
  c11_b03.md: ...odel | Serializable 事务；follower 上 HLC-aware 有界过期读 Partition behavior | CP——少数侧停止接受写 PACELC | PC / EC（分区下一致；无分区时为不确定性窗口读付 HLC-res...
  c11_b04.md: ...；自动扩缩容与自动修复 Consistency model | 默认最终一致；按请求可提供强一致读 Partition behavior | 默认读分区下 AP；条件写提供 per-item 线性化 PACELC | 默认 PA / EL（低延迟、最终）；...
  c11_b04.md: ...istency model | 按操作可调；默认最终，LWT（Paxos-backed）提供线性化 Partition behavior | 按操作可调；低一致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA / EL，quorum...
  c11_b04.md: ...Consistency model | Serializable 事务；ACID 强可串行化保证 Partition behavior | CP——丢失法定人数则拒绝写 PACELC | PC / EC；面向 OLTP 延迟设计 Strengths |...
  c11_b05.md: ...nsistency model | Serializable 事务；follower 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC | PC / EC Strengths | 从一开始即为金融工作负载构建；深度...
  c11_b08.md: ...复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数 PACELC | PC / EC；优化金融 OLTP 吞吐（benchm...
  c11_b09.md: ...ossip；超时后排除节点 Consistency model | 可调：默认最终，LWT 线性化 Partition behavior | 可调；低一致性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quorum PC/EC...
  c14_b07.md: ...nearizable；提供过期读 | Serializable；提供 follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestamp source | TrueTime（GPS +...

=== neon (9 occurrences) ===
  c11_b06.md: ...一个你没用过的系统产出一段阅读。可选：MongoDB、ScyllaDB、Aurora DSQL、TigerBeetle、Neon。  提示  查阅厂商文档；把答案映射到四问题表格；综合。  **答案：** 示例 Aurora DSQL（AWS，20...
  c13_b17.md: ...分：NewSQL 全景。Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle，以及分布式查询处理器（Trino/Presto、Snowflake、BigQuery）。本章讲...
  c14_b01.md: ...：NewSQL 竞争者（Spanner、CockroachDB、TiDB、YugabyteDB、Aurora DSQL、Neon、TigerBeetle）和处理分析工作负载的分布式查询处理器（Trino/Presto、Snowflake、BigQu...
  c14_b01.md: ...*AWS Aurora DSQL**（2024 GA）：AWS 上托管的 serverless NewSQL。 * **Neon** ：serverless Postgres，存储与计算分离。 * **TigerBeetle** ：金融级账本，采用...
  c14_b03.md: ...。新兴示例：AWS 客户从 Aurora Postgres 整合迁移到 DSQL 以实现多区域。   ### 14.7 Neon  Serverless Postgres，存储与计算分离。严格说不是多主意义上的 “NewSQL”，但属于同一运维类别...
  c14_b03.md: ...Postgres（非克隆）；所有扩展可用。  劣势：  * 单主；写不能水平扩展。 * 较新；某些运维细节仍在打磨。  Neon 适合：负载可变（开发、dev/test 环境、低容量 SaaS）并从 serverless 计费受益。不适合大规模高吞...
  c14_b05.md: ...纯 OLAP | Snowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域） 多云 | CockroachDB（任何云）、Snowflake（OLAP...
  c14_b12.md: ...本，而收益可能永远不会兑现。  第二个考虑：serverless 选项（Neon、Aurora DSQL）正在模糊界线。Neon 让你无需运维 Postgres；Aurora DSQL 让你无需运维 NewSQL。两者都以不同的扩展特性提供 “托管...
  c30_b04.md: ...PC/EC | OLTP 基础；在其上构建数据库 |  关于 Aurora DSQL、TiDB、YugabyteDB、Neon、TigerBeetle：见第14章。  * * *...

=== idempotency (9 occurrences) ===
  c20_b01.md: ...+ TLS<br/>65 ms (edge + cross-equator)"]         A["auth + idempotency<br/>22 ms"]         F["fraud check<br/>80 ms (serial after...
  c27_b01.md: ...成功但响应丢失” 的情况。    2. **网络可靠**。Deutsch 的第一谬误。没有 timeout、retry、idempotency 的代码。    3. **机器时钟说真话**。跨机器比较时间戳的代码。    4. **朴素分布式锁有效**。没有 f...
  c27_b02.md: ...用可能在服务端已处理后从客户端超时。客户端看到失败；服务端已扣款。重试造成重复扣款。  修复：幂等性。每笔支付必须携带 idempotency key。服务端检查：我之前见过这个 key 吗？若见过，返回之前计算的结果。重试安全。       def submi...
  c27_b05.md: ...审查 AI 生成分布式代码（或压力下自写代码）时，检查清单：  (a) **幂等性**：每个网络调用必须幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联。  (c)...
  c27_b07.md: ...deRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false...
  c27_b09.md: ...成功但响应丢失”。    2. **网络可靠**：代码没有 timeout、retry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall-clock 时间戳做排序。    4. **朴素分布式...
  c27_b10.md: ...、一致性、tracing、可观测性。  **答案：** 十项检查：  (a) **幂等性**：每个网络调用应幂等或携带 idempotency key。否则重试造成重复工作。  (b) **超时**：每个网络调用必须有 timeout。否则挂起级联上游。  (c...
  c27_b12.md: ...1. **谎言 1（RPC 语义）**：`bank_service.transfer(payment)` 没有 idempotency key。若超时重试，bank 可能转账两次。    2. **谎言 2（网络可靠）**：任何网络调用（bank、db、...
  c27_b12.md: ...审查反馈：“拒绝。这段代码包含 Thread C 全部六个谎言。具体：bank_service.transfer 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎...

=== tail (9 occurrences) ===
  c20_b02.md: ...e 不完整。修复：采样决策冗余编码（在 trace context 加 per-span attribute 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后...
  c20_b05.md: ...这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 s...
  c20_b05.md: ...-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_...
  c20_b05.md: ...这种纪律，分布式追踪产生碎片化的 trace，误导性大于帮助。  **Q3.** 什么是 head-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 s...
  c20_b05.md: ...-based 采样与 tail-based 采样？各自何时适用？  提示  Head-based：在根处决策；传播决策。Tail-based：缓冲 spans，trace 完成后决策。  **答案：** _Head-based sampling_...
  c20_b06.md: ...统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。...
  c20_b06.md: ...漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 trace context...
  c20_b06.md: ...统一采样率；对延迟敏感的基础设施。绝大多数 OpenTelemetry 部署从 head-based 开始。  * **Tail-based**：聚焦错误和异常值的部署；大舰队中统一 1% 采样会漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。...
  c20_b06.md: ...漏掉太多错误；有基础设施支持大规模缓冲和决策的团队。  实践中，许多大规模系统两者都用：head-based 处理主体；tail-based 在之上捕获错误/异常值。混合最大化覆盖同时控制成本。  **Q4.** 为什么 trace context...

=== metrics (9 occurrences) ===
  c20_b03.md: ...本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20....
  c20_b08.md: ...式复现失败。迭代修复。  诊断流程的技巧是知道每一步该看什么。Trace 是起点；诊断流程结合 traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低到随机抽查可能发现不了。T...
  c20_b09.md: ...etry" 是运维标准。  第二个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关联 traces、logs、metrics。它们有望减轻读 trace 的认知负担；2026 年还处于早期且不可靠，但轨迹表明 2028-2030 年会成为标准工...
  c20_b10.md: ...教训泛化：2026 年，对横切关注点（tracing、metrics、logging、configuration）的正确答案通常是 “用开放标准”（OpenTelemetry、Promet...
  c20_b10.md: ...ces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 t...
  c21_b06.md: ...stEnergy 运维看不到初始故障（监控软件 bug）；等他们看到扩散时，干预已太晚。分布式系统类比：tracing、metrics、alerting 必须在系统降级时仍工作。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时...
  c27_b12.md: ...单处理的故障模式。没有混沌测试的分布式代码 PR 被拒绝。  (e) **监控要求**：每个分布式操作必须可观测。没有 metrics、logs、traces 的操作 PR 被拒绝。  (f) **周期性审计**：高级工程师每季度审计已合并分布式代码 P...
  c28_b02.md: ...b-100-ms p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：...
  c29_b02.md: ...仍弱；人类在新模式和架构选择上的角色持续。 * **AI 辅助事后分析**成为标准；LLM 读取 trace+logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出 game-day 场景；...

=== rps (9 occurrences) ===
  c21_b01.md: ...若每个调用者失败时重试 N 次，负载放大为 N+1（原始加 N 次重试）。N=5 时，负载放大 6 倍；额定 1000 rps 的服务在重试启动后可能被 6000 rps 击中。  防御：重试预算。...
  c21_b01.md: ...N+1（原始加 N 次重试）。N=5 时，负载放大 6 倍；额定 1000 rps 的服务在重试启动后可能被 6000 rps 击中。  防御：重试预算。...
  c21_b07.md: ...调用者就收到 6 倍正常负载。 * 其他调用者也这么做。 * 总放大：所有调用者 6 倍。  若下游正常容量 1000 rps，正常负载 800 rps，放大后负载为 800 × 6 = 4800 rps。下游现在处于 4.8 倍容量；完全倒下。...
  c21_b07.md: ...载。 * 其他调用者也这么做。 * 总放大：所有调用者 6 倍。  若下游正常容量 1000 rps，正常负载 800 rps，放大后负载为 800 × 6 = 4800 rps。下游现在处于 4.8 倍容量；完全倒下。  有 10% 重试预算：...
  c21_b07.md: ...者 6 倍。  若下游正常容量 1000 rps，正常负载 800 rps，放大后负载为 800 × 6 = 4800 rps。下游现在处于 4.8 倍容量；完全倒下。  有 10% 重试预算：  * 调用者跟踪近期成功率；计算每秒重试上限（成功...
  c21_b07.md: ...败下，成功流量下降；上限按比例下降；总流量被限制在约成功流量的 1.1 倍。  下游看到：  * 正常成功流量：800 rps。 * 预算允许最多 80 次重试/秒（800 的 10%）。 * 总量：~880 rps。 * 当下游变慢，成功流量降...
  c21_b07.md: ...* 正常成功流量：800 rps。 * 预算允许最多 80 次重试/秒（800 的 10%）。 * 总量：~880 rps。 * 当下游变慢，成功流量降到 500 rps；预算降到 50；总量：~550。 * 下游不会过载；有机会恢复。  该...
  c21_b07.md: ...允许最多 80 次重试/秒（800 的 10%）。 * 总量：~880 rps。 * 当下游变慢，成功流量降到 500 rps；预算降到 50；总量：~550。 * 下游不会过载；有机会恢复。  该修复是结构性的：重试预算无论配置多少次重试都能限...
  c22_b06.md: ...。  权衡：  * 深度检查给依赖增加负载（每次检查都是真实调用）。100 个节点每 5 秒检查一次，下游会收到 20 RPS 的健康检查流量。 * 深度检查本身可能间歇性失败（短暂下游抖动导致检查失败；节点被标记不健康；集群容量下降）。缓解：标...

=== breaker (9 occurrences) ===
  c27_b02.md: ...le_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fallback。代码假设调用最终会返回。  现实：网络会抖动。下游有时慢。下游有时会失败。没有防御，这一行可能...
  c27_b02.md: ...级联故障，造成 p99 延迟灾难。  修复：每个网络调用应有 timeout、retry budget、circuit breaker，以及（若可能）fallback。       @circuit_breaker(threshold=0.5, time...
  c27_b05.md: ...重试必须有预算（占成功流量的百分比上限）。否则重试风暴。  (d) **熔断器**：每个跨服务调用应有 circuit breaker。否则级联故障。  (e) **排序原语**：绝不用 `time.Now()` 做跨机器排序。用 HLC 或因果排序。...
  c27_b07.md: ...三层纪律具体：game day 必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game d...
  c27_b09.md: ...不处理 “服务端成功但响应丢失”。    2. **网络可靠**：代码没有 timeout、retry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall-clock 时间戳做排序。...
  c27_b09.md: ...工作。  （谎言 2）`result = http.get(url)`。无 timeout、retry、circuit breaker；挂起级联。  （谎言 3）`sorted(events, key=lambda e: e.timestamp)`。跨机...
  c27_b10.md: ...*：重试应受预算约束（占成功流量百分比上限）。否则重试风暴。  (d) **熔断器**：跨服务调用应有 circuit breaker。否则级联故障。  (e) **排序原语**：跨机器排序应使用 HLC 或因果排序。`time.Now()` 是错的。...
  c30_b06.md: ...骤订阅；无中央协调器。补偿通过 “rollback 事件” 发生。见第13章 §13.6.5。 * **Circuit breaker（熔断器）**：当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating action（...
  c30_b14.md: ...style)     │   ├── circuit_breaker.py — three-state circuit breaker     │   └── load_shedder.py   — adaptive concurrency limite...

=== circuit (9 occurrences) ===
  c27_b02.md: ...rn profile_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fallback。代码假设调用最终会返回。  现实：网络会抖动。下游有时慢。下游有时会失败。没有...
  c27_b02.md: ...务无限挂起，促成级联故障，造成 p99 延迟灾难。  修复：每个网络调用应有 timeout、retry budget、circuit breaker，以及（若可能）fallback。       @circuit_breaker(threshold=0...
  c27_b05.md: ...的重试**：每个重试必须有预算（占成功流量的百分比上限）。否则重试风暴。  (d) **熔断器**：每个跨服务调用应有 circuit breaker。否则级联故障。  (e) **排序原语**：绝不用 `time.Now()` 做跨机器排序。用 HLC...
  c27_b07.md: ...中标记。  第三层纪律具体：game day 必须注入 AI + 人类审查声称要防御的具体故障模式。若人类审查者写 “circuit breaker 防止下游变慢”，game day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败...
  c27_b09.md: ...应表示请求结果；不处理 “服务端成功但响应丢失”。    2. **网络可靠**：代码没有 timeout、retry、circuit breaker、idempotency。    3. **机器时钟说真话**：代码跨机器比较 wall-clock 时...
  c27_b09.md: ...等；重试造成重复工作。  （谎言 2）`result = http.get(url)`。无 timeout、retry、circuit breaker；挂起级联。  （谎言 3）`sorted(events, key=lambda e: e.timest...
  c27_b10.md: ...*带预算的重试**：重试应受预算约束（占成功流量百分比上限）。否则重试风暴。  (d) **熔断器**：跨服务调用应有 circuit breaker。否则级联故障。  (e) **排序原语**：跨机器排序应使用 HLC 或因果排序。`time.Now(...
  c30_b06.md: ...完成事件；后续步骤订阅；无中央协调器。补偿通过 “rollback 事件” 发生。见第13章 §13.6.5。 * **Circuit breaker（熔断器）**：当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating...
  c30_b14.md: ...n (gRPC-style)     │   ├── circuit_breaker.py — three-state circuit breaker     │   └── load_shedder.py   — adaptive concurrenc...

=== jones (9 occurrences) ===
  c22_b11.md: ...etflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。  ---...
  c23_b02.md: ...### 23.3 Nora Jones 的组织实践  Nora Jones（与 Casey Rosenthal 合著《Chaos Engineering: S...
  c23_b02.md: ...### 23.3 Nora Jones 的组织实践  Nora Jones（与 Casey Rosenthal 合著《Chaos Engineering: System Resiliency i...
  c23_b07.md: ...是系统观察；“Alice 配置了重试预算，她错了” 是个人责备。前者导向修复；后者导向防御行为且无修复。  Nora Jones 的无责复盘实践是运营体现：讨论聚焦系统性原因，而非个人错误。“配置为什么是这样？” 导向 “runbook 不清楚”...
  c23_b13.md: ...eduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性事件。  Nora Jones 的组织实践强调心理安全、无责复盘、多元参与、记录学习、固定节奏。没有文化基础，技术练习不产生学习。  工具（Chaos...
  c29_b03.md: ...复杂（多 region、多云、AI 增强、混合 BFT/崩溃故障组件），唯一知道防御是否有效的方式是演练它们。Nora Jones 的组织实践（无责复盘、game day、记录学习）随着技术面增长变得更相关，而非更不相关。   #### 12 个核心...
  c30_b03.md: ...ilures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering: System Resiliency in Practi...
  c30_b03.md: ...ing: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J., & Murphy, N. R. (2016). _Site Reliability...
  c30_b03.md: ...迟到 _Software at Scale_ Series IV Vol 4）。 * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering_. O'Reilly. — 已在运维部分列出，此处再次引...

=== radixattention (9 occurrences) ===
  c24_b03.md: ...### 24.3 SGLang 与 RadixAttention  SGLang（2023 年底由 LMSYS 发布；2025 年广泛部署）走了另一条优化路径：_RadixAttent...
  c24_b03.md: ...stem prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新请求到达时，系统找到最长缓存前缀并复用；只有后缀需要重...
  c24_b03.md: ...m prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prompt token 中 30% 或更多来自跨请求重复...
  c24_b06.md: ...st profile 更均匀。  这些差异证明把分布式 LLM 推理视为独立子学科。协议（PagedAttention、RadixAttention）、原语（KV cache、prefill、decode）和部署模式（disaggregation、sharding）都...
  c24_b06.md: ...较 vLLM 和 SGLang。各适合什么场景？  提示  vLLM：PagedAttention；通用。SGLang：RadixAttention；前缀复用重的工作负载。  **答案：**...
  c24_b07.md: ...，多种工作负载下高吞吐。 * 最适合：任意 serving、无特定前缀复用模式。  _SGLang_：  * 核心创新：RadixAttention（基于前缀的 KV cache 复用）。 * 优势：对共享前缀工作负载节省巨大。 * 最适合：具有长共享 system...
  c24_b08.md: ...**：对具有重复 system prompt 的工作负载（客服机器人、agent scaffold），SGLang 的 RadixAttention 可节省 30-70% prefill 成本。若 Tobias 显著比例流量有共享前缀，把相关工作负载切到 SGLang...
  c24_b11.md: ...消除碎片；GPU 内存利用率从 30-50% 提升到 70-90%。2026 年基线开源推理引擎。  SGLang 的 RadixAttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架...
  c30_b03.md: ...ficient Execution of Structured Language Model Programs." — RadixAttention。 * Patel, P., et al. (2023). "Splitwise: Efficient Generati...

=== burst (9 occurrences) ===
  c26_b02.md: ...身是小模型，或基于规则的启发式）把 incoming 请求分类为短/中/长。  (b) 负载均衡器相应路由：短请求去 “burst” 节点；长请求去 “sustained” 节点。  (c) 误预测：若预测错误（本应短却长，或相反），请求 mid-f...
  c26_b08.md: ...测路由**：中等输出请求（200-500 token）去能支持约 10 秒 decode 的节点。避免放在应快速释放的 burst 节点。  (d) **Speculative decoding**：中等输出 decode 受益于 speculati...
  c26_b09.md: ...(b) **长度预测路由**：长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加...
  c26_b09.md: ...(b) **长度预测路由**：长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶...
  c26_b09.md: ...s；拒绝极端限制请求或路由到特殊 endpoint。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)...
  c26_b09.md: ...(b) **长度预测路由**：长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加...
  c26_b09.md: ...(b) **长度预测路由**：长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative decoding**：对长输出请求加速 decode 阶...
  c26_b09.md: ...s；拒绝极端限制请求或路由到特殊 endpoint。  (f) **多部署 tier**：短请求严格延迟 SLA 的 “burst” tier；长请求宽松 SLA 的 “batch” tier。客户为不同 tier 付费不同。  对大多数集群，(a)...
  c26_b11.md: ...模板）的重大收益。  长度预测路由：预测请求输出长度；长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decoding：小 draft model...

=== regional (9 occurrences) ===
  c28_b02.md: ...**API 网关**：Cloudflare Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 region（US-East、EU-West、US-West...
  c28_b02.md: ...re Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 region（US-East、EU-West、US-West）。每个 region 有完整技术栈。...
  c28_b02.md: ...luster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region...
  c28_b02.md: ...用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region 异步（Kafka）、边缘缓存（Redis）...
  c28_b02.md: ...**API 网关**：Cloudflare Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 region（US-East、EU-West、US-West...
  c28_b02.md: ...re Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集群**：3 个 region（US-East、EU-West、US-West）。每个 region 有完整技术栈。...
  c28_b02.md: ...luster 提供分布式锁（安全关键操作使用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region...
  c28_b02.md: ...用 fencing token）。  架构分层：边缘（Cloudflare）、regional 无状态（API 服务）、regional 有状态（CockroachDB、TigerBeetle）、跨 region 异步（Kafka）、边缘缓存（Redis）...
  c28_b04.md: ...55 ms，是 Crescent 三个 region 中最近的）。60 ms 跨赤道。  (3) US-East 的 regional 负载均衡器路由到健康的 API 服务实例。实例开始支付处理。Trace context（OpenTelemetry W...

=== apie (9 occurrences) ===
  c28_b03.md: ...end         subgraph USE["US-East Region"]             APIE[API Service]             REDISE[Redis Cluster]...
  c28_b03.md: ...ntinuum LLM<br/>fraud detection]         end         CF --> APIE         CF --> APIW         CF --> APIW2         APIE --> R...
  c28_b03.md: ...F --> APIE         CF --> APIW         CF --> APIW2         APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE...
  c28_b03.md: ...> APIW         CF --> APIW2         APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE...
  c28_b03.md: ...PIW2         APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE...
  c28_b03.md: ...SE         APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> C...
  c28_b03.md: ...BE         APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|...
  c28_b03.md: ...TUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"|...
  c28_b03.md: ...nightly"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL...

=== ledger (8 occurrences) ===
  c01_b01.md: ...使用城市名，因为事后复盘最初就是这样命名的，后来团队才把它重新框定为区域术语。）  bug 不在任何单段代码里。银行的 ledger 代码是对的。重试逻辑是对的。支付网关的幂等性检查对它所持有的世界模型而言也是对的。bug 在于每个组件对其他组件的“信...
  c01_b02.md: ...系统自始至终健康。  圣保罗同样没有观察到失败。从它的日志看，系统同样健康。  但系统整体失败了。客户被重复扣款。银行 ledger 出现了两笔本应为一次的借记。  这是第三个谎言，也是最深的一个。在单机程序中，失败是响亮的。进程崩溃。磁盘写满。异常沿...
  c13_b07.md: ...raud         participant B as Bank         participant L as Ledger         participant N as Notification         participant A...
  c20_b01.md: ...n<br/>95 ms (cross-region, serial after fraud)"]         L["ledger write<br/>30 ms (serial after DB)"]         POST["post-comm...
  c27_b12.md: ...nk 可能转账两次。    2. **谎言 2（网络可靠）**：任何网络调用（bank、db、notification、ledger）都没有 timeout。一个慢调用挂起整个函数。    3. **谎言 3（时钟）**：`ledger_service...
  c27_b12.md: ...service.record(payment.id, time.time())` 使用本机时钟时间戳。跨 region ledger 排序依赖机器时钟。    4. **谎言 4（锁）**：基于 Redis 的锁没有 fencing token。在 G...
  c27_b12.md: ...都能处理同一笔支付。    5. **谎言 5（部分故障）**：四个顺序调用（bank、db、notification、ledger）没有 rollback。若 bank 成功后 db 失败，用户已在 bank 扣款但 DB 未扣；后续重试会重复扣款。...
  c27_b12.md: ...e.transfer 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 time.time() 排序（谎言 3）；Redlock 无 fencing token（谎言 4）；四步操作没有...

=== timestamp (8 occurrences) ===
  c01_b03.md: ...1. **这个系统在时间上说谎了吗？** 它是否假设 `time.Now()` 能给事件排序？它是否假设跨机器的 timestamp 可比较？它是否假设会话在明确定义的时刻开始和结束？   2. **这个系统在身份上说谎了吗？** 它是否假设某个节点是...
  c01_b12.md: ...now()`，再 `INSERT INTO log (created_at) VALUES (now())`，插入行的 timestamp 会与 SELECT 一致”，会惊讶地发现 Postgres 中的 `now()` 返回的是 _事务开始时间_ ，而非调...
  c03_b11.md: ...除由时钟落后的协调者处理，它被盖戳早于另一个时钟超前的协调者上的后续写入。当删除和写入到达副本时，副本看到 write-timestamp > delete-timestamp，于是保留写入，实际上复活了记录。  这就是经典的 Cassandra “复活删除...
  c03_b11.md: ...另一个时钟超前的协调者上的后续写入。当删除和写入到达副本时，副本看到 write-timestamp > delete-timestamp，于是保留写入，实际上复活了记录。  这就是经典的 Cassandra “复活删除”失败模式，在 Jepsen 分析中有...
  c03_b11.md: ...上界（10 到 50 ms）时告警。这只会让失败更罕见，不能消除它。 * 只有当你有可靠单调来源时才使用 `USING TIMESTAMP` 的服务端时间戳。 * 对复活不可接受的数据，使用带 `IF` 子句的轻量事务（LWT）；LWT 使用 Paxos，提...
  c04_b06.md: ...组提供 at-least-once。 * **CockroachDB 的 Rangefeed** 提供带 closed-timestamp 保证的因果有序变更事件。  选择系统时正确的问题不是“它提供哪一层广播？”而是“它对我关心的操作提供的最强性质是什么？...
  c13_b05.md: ...入键的工作负载会看到频繁重启，并通过重试延迟有效支付 TrueTime 成本。   #### 对比  System | Timestamp source | Cost ---|---|---- Spanner | TrueTime（GPS + 原子钟） |...
  c14_b07.md: ...follower reads Partition behavior | CP——少数侧拒绝写 | CP——少数侧拒绝写 Timestamp source | TrueTime（GPS + 原子钟） | HLC + NTP 偏移上界 PACELC | PC /...

=== deutsch (8 occurrences) ===
  c01_b15.md: ...失败模式。如果你不能，无论你对它的 API 多么熟悉，这个系统对你都是黑箱。  接下来的第2章把这些谎言量化。你将遇到 Deutsch 的八大谬误（它们以工程假设命名这些谎言）、CAP（它量化分区下一致性与可用性之间的权衡），以及 PACELC（它把 C...
  c02_b01.md: ...的 C 或 A 选择。  本章教你区分“知道标签”（CP、AP、最终一致性、强一致性）和“推理它们的实际代价”。我们从 Deutsch 的八大谬误开始，因为它们命名了每个错误分布式直觉所依赖的假设。然后精确陈述 CAP（不是口号），再讲 PACELC——...
  c02_b01.md: ...然后精确陈述 CAP（不是口号），再讲 PACELC——它把 CAP 忽略的无分区情况也纳入进来。   ### 2.1 Deutsch 的分布式计算八大谬误  20 世纪 90 年代初，Sun Microsystems 的 Peter Deutsch 写...
  c02_b01.md: ...1 Deutsch 的分布式计算八大谬误  20 世纪 90 年代初，Sun Microsystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 James Gosling 补充了第八条。这份清单经得起时间考验，...
  c02_b07.md: ...#### 概念检测  **Q1.** 用你自己的话陈述 Deutsch 的八大谬误，并各举一个 quietly 假设该谬误为真的代码示例。  提示  为每个谬误找一个代码模式。对“网络是可靠...
  c02_b14.md: ...确的 LLM 生成答案不会给团队带来任何理解，无法评估未来变更，也无法在假设变化时为自己辩护。   ### 本章总结  Deutsch 的八大谬误命名了产生大多数分布式系统 bug 的单机直觉。谬误不是要记住的 checklist；它们是审计每段分布式代...
  c27_b01.md: ...求成功” 来自 “响应到达” 的代码，遗漏了 “请求在服务端已成功但响应丢失” 的情况。    2. **网络可靠**。Deutsch 的第一谬误。没有 timeout、retry、idempotency 的代码。    3. **机器时钟说真话**。跨...
  c30_b01.md: ...M. (2017). _Distributed Systems_ (3rd ed.). — 学术处理的教材基础。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing."...

=== availability (8 occurrences) ===
  c02_b04.md: ...，忽略了更常见的情况：网络健康时。健康情况下的权衡在延迟与一致性之间。  PACELC：若发生 Partition，则 Availability 或 Consistency；否则（Else），则 Latency 或 Consistency。  这个缩写很别扭，但观...
  c02_b08.md: ...UM 读/写的 Cassandra，分别给出 PACELC 分类。  提示  PACELC：若 Partition，则 Availability vs Consistency；否则（Else），Latency vs Consistency。每个系统是 PA/EL、...
  c05_b01.md: ...目标，任何给定系统都追求其中某种组合：  * **Durability** ：一台机器磁盘损坏，数据不会丢失。 * **Availability** ：一台机器不可达，系统仍能从另一台服务请求。 * **Read scalability** ：把读分散到副本，成倍...
  c05_b01.md: ...被提升为新主库，新主库拥有所有已确认写。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica failure** ：若任何副本不可达，写入阻塞，直到副本恢复或被移出集合。  慢副本阻塞...
  c05_b02.md: ...lover consistency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Availability under replica failure** ：写不受副本健康影响。  丢失写窗口是异步复制的风险。对大多数工作负载...
  c05_b02.md: ...升的是其他副本，写可能丢失。 * **Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under replica failure** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑...
  c09_b11.md: ...Paxos（Heidi Howard 的重配置洞见），让运维可以调节固定法定人数协议无法提供的 consistency-availability 权衡。  ---...
  c11_b01.md: ...议  阅读系统文档中的 “replication”、“consensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader...

=== msg (8 occurrences) ===
  c03_b03.md: ...|---|---|---|--- 1 | P_1 | local event a | 1 2 | P_1 | send msg m to P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_...
  c03_b03.md: ...| 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | send msg n to P_3 (carries 4) | 4 6 | P_3 | receive n | \max(1, 4) +...
  c04_b03.md: ...:         for peer in all_processes:             send(peer, msg)      on receive(msg, sender):         if msg not in receiv...
  c04_b03.md: ...send(peer, msg)      on receive(msg, sender):         if msg not in received:             received.add(msg)...
  c04_b03.md: ..., ACK(msg))      on receive(ACK(msg), sender):         acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg not in...
  c04_b03.md: ...sender):         acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg not in delivered:             delivered.add...
  c04_b03.md: ...acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg not in delivered:             delivered.add(msg)...
  c04_b03.md: ...le.sender] += 1             upper_layer.deliver(deliverable.msg)   性质（在可靠广播之外）：  * **Causal order** ：若 broadcast(m_1) \to b...

=== least (8 occurrences) ===
  c04_b01.md: ...，要么失败且调用者知道。在 TCP 术语中，若调用者不重试，这是 _at-most-once_ ；若重试，则是 _at-least-once_ 。应用想要的是 _所有副本按相同顺序全有或全无_ 。这就是原子广播，它等价于共识。   ### 4.1 为...
  c04_b06.md: ...。状态机命令的原子广播。 * **Redis Streams** 在 stream 内提供有序交付；消费者组提供 at-least-once。 * **CockroachDB 的 Rangefeed** 提供带 closed-timestamp 保证...
  c04_b09.md: ...#### 应用问题  **Q1.** 一个团队用消息代理实现微服务舰队的事件分发，该代理提供 at-least-once 交付和 topic 内有序。这给他们哪一层广播？应用代码还需要哪些纪律？  提示  代理在 topic 分区...
  c04_b09.md: ...码还需要哪些纪律？  提示  代理在 topic 分区内提供的是原子广播。跨 topic 或分区需要应用自己强制。at-least-once 意味着消费者可能看到重复。  **答案：** 代理在 topic 分区内提供原子广播（per-topic o...
  c04_b09.md: ...pic 分区内提供原子广播（per-topic ordering）。跨 topic 或分区只提供应用强制出的性质。at-least-once 交付意味着重试情况下每个消费者可能多次收到同一条消息。  应用需要纪律：  (a) 每个消息处理函数必须具备...
  c22_b08.md: ...“outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已弃用但历史上重要）...
  c26_b05.md: ...scale、Together、Fireworks）已构建显式考虑这些因素的路由优化器。朴素 round-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-aware 加长...
  c26_b10.md: ...*Q2.** 一个 LLM 被要求 “为我们集群实现 KV-cache-aware 路由”。它产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** LLM 的基本方法：哈希 prompt，检查是否有节点缓存该哈...

=== topic (8 occurrences) ===
  c04_b06.md: ...2026 年的生产现实  2026 年生产系统中，广播原语以不同名称出现，但语义相同：  * **Kafka** 在 topic 分区内提供全序广播。分区 leader 排序写入；副本按相同顺序应用。这是限定在一个分区内的原子广播。 * **NAT...
  c04_b09.md: ...应用问题  **Q1.** 一个团队用消息代理实现微服务舰队的事件分发，该代理提供 at-least-once 交付和 topic 内有序。这给他们哪一层广播？应用代码还需要哪些纪律？  提示  代理在 topic 分区内提供的是原子广播。跨 top...
  c04_b09.md: ...at-least-once 交付和 topic 内有序。这给他们哪一层广播？应用代码还需要哪些纪律？  提示  代理在 topic 分区内提供的是原子广播。跨 topic 或分区需要应用自己强制。at-least-once 意味着消费者可能看到重复。...
  c04_b09.md: ...pic 内有序。这给他们哪一层广播？应用代码还需要哪些纪律？  提示  代理在 topic 分区内提供的是原子广播。跨 topic 或分区需要应用自己强制。at-least-once 意味着消费者可能看到重复。  **答案：** 代理在 topic...
  c04_b09.md: ...topic 或分区需要应用自己强制。at-least-once 意味着消费者可能看到重复。  **答案：** 代理在 topic 分区内提供原子广播（per-topic ordering）。跨 topic 或分区只提供应用强制出的性质。at-lea...
  c04_b09.md: ...者可能看到重复。  **答案：** 代理在 topic 分区内提供原子广播（per-topic ordering）。跨 topic 或分区只提供应用强制出的性质。at-least-once 交付意味着重试情况下每个消费者可能多次收到同一条消息。  应...
  c04_b09.md: ...一条消息两次应产生与收到一次相同的最终状态。通常通过每条消息带幂等键，在处理前检查数据库实现。  (b) 若应用需要跨 topic 顺序，则要自己保证。代理只在一个分区内保证顺序；同一 topic 不同分区间的事件无序。应用必须将相关事件放在同一分区...
  c04_b09.md: ...键，在处理前检查数据库实现。  (b) 若应用需要跨 topic 顺序，则要自己保证。代理只在一个分区内保证顺序；同一 topic 不同分区间的事件无序。应用必须将相关事件放在同一分区（partition key = entity ID），或接受跨分...

=== replica (8 occurrences) ===
  c05_b01.md: ...。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica failure** ：若任何副本不可达，写入阻塞，直到副本恢复或被移出集合。  慢副本阻塞写入这一性质是致命的。磁盘降...
  c05_b02.md: ...：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Availability under replica failure** ：写不受副本健康影响。  丢失写窗口是异步复制的风险。对大多数工作负载（分析、内容元数据、浏览量）...
  c05_b02.md: ...Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under replica failure** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑第一个 ack。这比同步快、...
  c05_b06.md: ...排序后传播给 follower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB replica sets、CockroachDB（per-range）、Spanner（per-tablet）。  Leaderles...
  c05_b09.md: ...`local_hlc` 小于 `min_hlc`。处理器要么等待（直到复制把它带到最新）要么拒绝（返回 “stale replica” 错误）。拒绝后客户端要么重试不同副本，要么重试主库。  CockroachDB 和 YugabyteDB 使用这种模...
  c11_b08.md: ...出精确、可辩护的答案。  提示  MongoDB 的一致性取决于配置：writeConcern、readConcern、replica set vs sharded。默认与最强可用不同。  **答案：** MongoDB 的一致性高度依赖配置。精确答案必...
  c27_b04.md: ...lance_authoritative(account_id):         # 线性化读；显式强制 leader replica         with db.transaction() as tx:             return tx....
  c28_b03.md: ...Shared["Shared Services"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-r...

=== wal (8 occurrences) ===
  c05_b06.md: ...，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入永久丢失。团队必须调和下游影响（退款、重试、通知客户）。 * 团...
  c06_b05.md: ...ing、header bytes），小消息时这部分可能超过实际负载。  Crescent 示例：CockroachDB WAL 跨区域复制每天约 10 GB，很小。实际主导的跨区域成本是应用的指标和日志外送（每个区域把可观测数据送到中央聚合器），...
  c08_b10.md: ...须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程测试无法发现...
  c12_b03.md: ...ite-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c) **SSTable（Sorted String Table）** ：memtable 满时，被刷写成磁盘上...
  c12_b03.md: ...机写 I/O 成本高；SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Compaction 工作异步、批量进行。对写重工作负载，LSM 以即时写效率换取周期性 compact...
  c12_b06.md: ...居。  **Q3.** 描述 LSM-tree 的写路径。为什么它比 B+ tree 写更快？  提示  LSM：先写 WAL（顺序），再写 memtable（内存）。B+ tree：读页、修改、写回（随机 I/O）。  **答案：** LSM...
  c12_b06.md: ...存）。B+ tree：读页、修改、写回（随机 I/O）。  **答案：** LSM 写路径：    1. 写追加到磁盘 WAL（write-ahead log）。这是顺序写。   2. 写插入 memtable（内存有序结构）。这很快。   3....
  c12_b11.md: ...range 并为负载重平衡副本。  LSM-tree 是大多数现代分布式数据库的存储底层。写进入 memtable 和 WAL（顺序、快）；刷成不可变 SSTable；异步 compaction。LSM 在写重工作负载中击败 B+ tree；B+...

=== justify (8 occurrences) ===
  c05_b08.md: ...问：成本？自托管 Postgres 比 CockroachDB 许可证和基础设施开销低。对规模化支付系统，运维收益通常 justify；对小初创公司，成本可能不 justify。  **Q2.** 一个团队运行 Cassandra，$N = 3, W =...
  c05_b08.md: ...ockroachDB 许可证和基础设施开销低。对规模化支付系统，运维收益通常 justify；对小初创公司，成本可能不 justify。  **Q2.** 一个团队运行 Cassandra，$N = 3, W = 2, R = 2$。他们观察到偶发读异常...
  c06_b03.md: ...ts），且失败模式与 TCP 不同。对大多数应用层分布式系统，RDMA 过度；它存在于存储和 ML 层，那里的延迟提升 justify 运维成本。...
  c10_b05.md: ...不会产生错误配置。  成本收益：BFT 成本是 2-3 倍（更多副本、更多消息、更多加密）。对大多数系统，威胁模型无法 justify 成本。对少数需要它的系统，成本不可避免。...
  c10_b11.md: ...真实。大多数数据库将继续使用崩溃容错共识。  综合：BFT 会扩散到特定高价值垂直领域（金融、医疗、国防），其威胁模型 justify 成本。主流分布式数据库不会广泛采用 BFT。中间路线（给崩溃容错系统增加某些 BFT 属性，如签名 AppendEnt...
  c14_b01.md: ...。 * 相比完整 Postgres，某些 SQL 功能（子查询等）曾有限制，虽已逐步改善。  Spanner 适合：能 justify 成本和 GCP 承诺的全球规模工作负载。生产示例：Google 自身基础设施（Ads、Cloud、Workspace）...
  c25_b06.md: ...M+ USD/月成本下 40% 容量提升即每月 40 万美元节省。 * 运维复杂度在更多节点上摊销。 * 团队规模足以 justify 专门平台工程师做分离逻辑。  阈值与工作负载相关：50 节点但 uniform 短 prompt 工作负载的集群可能不...
  c27_b06.md: ...CRDT）。AI 审查者在新模式上自信地错误。 * **考虑后保留的推理**：资深审查者决定不标记某事，因为周边上下文 justify 它。AI 审查者标记模式；没有上下文。  粗略 60/40 分：AI 捕获高容量常规；人类捕获高判断边缘案例。这一分直...

=== saas (8 occurrences) ===
  c06_b02.md: ...：以用户为中心的工作负载（聊天、社交、生产力），大多数操作围绕单个用户数据。  Slack、许多游戏公司这样做，大多数 SaaS 产品在达到多区域规模后都会悄悄演化成这样。   #### 模式 3：全局一致（真正的多主）  每个区域都能服务任何操作...
  c12_b08.md: ...见：p99 延迟可能上升 10-30%，Anya 会监控并在超过 SLA 时节流。  **Q2.** 一个团队为多租户 SaaS 实现数据库。每个租户有自己的行集合。他们在 (a) 按 tenant ID 哈希分片，(b) 按 (tenant ID...
  c12_b08.md: ...lesforce / Workday 模式（大规模多租户）通常是 (b) 加 careful schema 设计。初创 SaaS 模式（几百租户）常为简单选 (c)。...
  c14_b09.md: ...QL（12-24 个月后）。这在运维上理性；只有当收益明确时才应支付迁移成本。  **Q2.** 一个团队正在构建全球 SaaS 分析产品。客户通过产品 UI 查询自己的数据；查询可以是即席 SQL。团队在 Snowflake（托管）和数据湖上自托...
  c14_b09.md: ...缺点：重大运维负担（计算集群管理、查询优化、缓存）；需要 JVM 调优、Trino 内部知识；成本包含运维团队。  对 SaaS 分析产品：...
  c14_b10.md: ...wflake；客户-facing 联邦查询用 Trino（因为 Snowflake 无法联邦到客户控制的源）。许多真实 SaaS 平台两者都跑。  2026 趋势：随着开源 Trino 生态成熟（更好的优化器、缓存、运维工具），Snowflake...
  c29_b02.md: ...权转移到用户，减少公司监管面）。 * 预计到 2030 年，20-40% 的新消费级生产力应用将是本地优先；网络依赖型 SaaS 模式在这一细分市场失去份额。 * B2B SaaS（Salesforce、Workday 等）仍是以网络为中心，因为数...
  c29_b02.md: ...0 年，20-40% 的新消费级生产力应用将是本地优先；网络依赖型 SaaS 模式在这一细分市场失去份额。 * B2B SaaS（Salesforce、Workday 等）仍是以网络为中心，因为数据真正在组织层面共享。   #### AI 辅助分布...

=== paulo (8 occurrences) ===
  c06_b06.md: ...#### 概念检测  **Q1.** 计算以下单程光速下限：(a) Chicago 到 São Paulo，(b) Tokyo 到 Mumbai，(c) 赤道上相距 180 度的任意两点。  提示  光纤中光速约 200,00...
  c06_b06.md: ...约 200,000 km/s。地球周长约 40,000 km。  **答案：**  (a) Chicago 到 São Paulo：大圆距离约 8,500 km。按直线光纤 200,000 km/s 计算，下限 8,500 / 200,000 = 4...
  c13_b01.md: ...Olamide 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Adaeze 的账户（一个分片）、商家的账户（另一个分片）和商家的日汇总...
  c13_b10.md: ...HLC 时间戳开始。Crescent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-DNS 路由选定的区域。按第6章 §6.1 方法（光纤中光速下限约 200,000 km/s，...
  c13_b10.md: ...第6章 §6.1 方法（光纤中光速下限约 200,000 km/s，真实 RTT 的 2-3× 路由开销乘数）：São Paulo → US-East（经南美沿海电缆约 8,500 km）p99 RTT 约 110 ms；São Paulo → EU...
  c13_b10.md: ...：São Paulo → US-East（经南美沿海电缆约 8,500 km）p99 RTT 约 110 ms；São Paulo → EU-West（经大西洋约 9,500 km）约 190 ms；São Paulo → US-West（经美国大陆...
  c13_b10.md: ...TT 约 110 ms；São Paulo → EU-West（经大西洋约 9,500 km）约 190 ms；São Paulo → US-West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。...
  c15_b09.md: ...文档说明了这点，但初始设计常被忽略。  对 Crescent：支付数据按用户归属区域分区，天然避免跨区域写冲突。São Paulo 用户总是写到南美区域；跨区域复制只用于灾备和分析，不是活跃多主写入。...

=== global (8 occurrences) ===
  c07_b06.md: ...有界。  **答案：** 部分同步（Dwork、Lynch、Stockmeyer 1988 形式化）：系统有一个未知的 Global Stabilization Time（GST）。GST 前系统完全异步（消息交付和进程速度无界）。GST 后系统同步（...
  c15_b09.md: ...捕获重启的一致状态。该协议是状态流处理整个类别的基础。  **Q3.** 一个团队有全球多区域数据库（DynamoDB Global Tables）。他们发现偶尔从一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Glo...
  c15_b09.md: ...bal Tables）。他们发现偶尔从一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Global Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** D...
  c15_b09.md: ...跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两...
  c15_b09.md: ...able 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重）很好。对多区域积极写同一 item 的工作负载不合适。系统文档说...
  c30_b02.md: ..., & Lamport, L. (1985). "Distributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B.,...
  c30_b11.md: ...容分发  **输入**：读主导工作负载；面向用户；延迟敏感；大多数读可接受最终一致。  **决策**：DynamoDB Global Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：Dy...
  c30_b11.md: ...或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB Global Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CDN**：Clo...

=== aptos (8 occurrences) ===
  c07_b05.md: ...多 Raft 组并行。 * **Tendermint**（Cosmos 链）用显式轮次超时；生产 1 秒一轮。 * **Aptos / Sui** 用 Narwhal-Bullshark（基于 DAG）获得比经典 BFT 更高吞吐；轮复杂度与消息延迟...
  c10_b01.md: ...19<br/>O(N) view change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's...
  c10_b01.md: ...Narwhal --> Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]         style BG fill:#f9f9f9         style HotStuff...
  c10_b03.md: ...段投票。这使吞吐远高于 PBFT 的串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；M...
  c10_b04.md: ...G 中已有 block，因为 DAG 本身即是“共识”机制（每个 block 都被签名并引用）。  生产中：  * **Aptos** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 B...
  c10_b06.md: ...) **BFT 是任何涉及区块链系统的运维现实。** 无论你在集成 Ethereum、构建 Cosmos 链，还是评估 Aptos 产品，BFT 模型决定了底层系统能做什么。  2026 年的分布式系统工程师应能读懂 BFT 论文，而不是从头设计 B...
  c10_b09.md: ...(b) **HotStuff** ：$O(N)$ view change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但多绑定特定区块链生态。为许可链实现...
  c10_b09.md: ...iew change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但多绑定特定区块链生态。为许可链实现 HotStuff 比直接拿库需要更多工程。  (c)...

=== ongaro (8 occurrences) ===
  c07_b11.md: ...axos 扩展到决策序列，这是生产部署（Spanner、Megastore）的真实形式。第9章讲 Raft，Diego Ongaro 和 John Ousterhout 2014 年明确为比 Paxos 更易理解而设计。  ---...
  c08_b10.md: ...26 年 Paxos 已过时。Raft 同样好、更简单，每个新系统都应该用它。”  **讨论：** 辩护：Raft 由 Ongaro 和 Ousterhout 明确设计为比 Paxos 更易理解，同时提供相同 safety 保证。大多数新分布式系统（e...
  c08_b10.md: ...大多数新分布式系统（etcd、Consul、TiKV、CockroachDB、SpiceDB）用 Raft。Diego Ongaro 的博士论文 “Designing for Understandability” 直接针对 Paxos 的复杂性。对新团...
  c08_b11.md: ...ighly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ousterhout 2014 年为比 Paxos 更易理解而设计。Raft 使用相同骨架（法定人数 + lead...
  c09_b01.md: ...早上她说：“我脑子里装不下这些。一定有更好的办法。”  她的技术 lead 把 Raft 论文递给她。  Raft 由 Ongaro 和 Ousterhout 设计，2014 年论文题为 “In Search of an Understandable...
  c09_b10.md: ...、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego Ongaro 博士论文数百页；etcd Raft 实现数千行。  综合：Raft 在 _基本情况_ 和 _常见情况_ 更简单。完整功...
  c30_b02.md: .... (2001). "Paxos Made Simple." _ACM SIGACT News_. — 必读改写。 * Ongaro, D., & Ousterhout, J. (2014). "In Search of an Understandab...
  c30_b06.md: ...ction（quorum 交集）**：N 个节点中任意两个多数派至少共享一个节点；共识安全基础。 * **Raft**：Ongaro-Ousterhout 2014 共识协议。分解为 leader 选举、日志复制、安全性。 * **Retry budg...

=== hash (8 occurrences) ===
  c10_b03.md: ...个投票组成 _Quorum Certificate（QC）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * View change（怀疑 leader 时）是线性的，因为新 leader 只...
  c12_b06.md: ...ition 内按 clustering 列排序做局部范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash mod N” 更好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案...
  c12_b06.md: ...排序做局部范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash mod N” 更好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个...
  c12_b06.md: ...N” 更好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个键的分区由 `hash(key) mod N` 决定。若 N 变化（增删节点），几乎所有键会映...
  c12_b06.md: ...增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个键的分区由 `hash(key) mod N` 决定。若 N 变化（增删节点），几乎所有键会映射到不同分区。$N=12 \to 13$ 时，1...
  c12_b06.md: ...13$ 时，约 1/13 的数据移动（新节点拥有的那段弧）。  运维含义：一致哈希下集群扩缩容可行（移动 8% 数据），hash-mod-N 不可行（移动 92% 数据）。每个现代分布式键值系统都使用某种形式的一致哈希或其衍生。  虚拟节点（vno...
  c18_b01.md: ...is Cluster gossip  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 slot 属于一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节...
  c26_b11.md: ...正确答案应讨论前缀索引方法、缓存新鲜度跟踪、负载感知，以及若有缓存层如何集成。单行 “hash-and-route” 是原型级答案；生产需要更多。  更深观察：推理集群优化是 2026 专业领域。给出原型级答案的...

=== cosmos (8 occurrences) ===
  c10_b04.md: ...t 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint 用部分 HotStuff 特性换取简单性：  * 两阶...
  c10_b04.md: ...nt 比 HotStuff 更易实现和调试。吞吐较低（因为每轮串行、无 pipelining），但对许多区块链用例足够。Cosmos 生态（Osmosis、Juno、Akash）都使用 Tendermint。  Tendermint 的 lock-an...
  c10_b06.md: ...BFT 启发的优化。  (c) **BFT 是任何涉及区块链系统的运维现实。** 无论你在集成 Ethereum、构建 Cosmos 链，还是评估 Aptos 产品，BFT 模型决定了底层系统能做什么。  2026 年的分布式系统工程师应能读懂 BFT...
  c10_b09.md: ...为许可链实现 HotStuff 比直接拿库需要更多工程。  (c) **Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于...
  c10_b09.md: ...接拿库需要更多工程。  (c) **Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于 HotStuff，但对银行间结算足...
  c10_b09.md: ...吞吐低于 HotStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的数千）。  推荐：通过 Cosmos SDK 使用 Tendermint。理由：  * 成熟库和生态。 * 对该用例吞吐足够。 * 运维工具和可观测性发展良...
  c10_b09.md: ...nt。理由：  * 成熟库和生态。 * 对该用例吞吐足够。 * 运维工具和可观测性发展良好。 * 若该链未来需要互操作，Cosmos IBC 是加分项。  HotStuff 技术上“最佳”，但实现负担更重；对 21 节点许可链，Tendermint 的...
  c10_b12.md: ...ullshark（2022）将 mempool 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 p...

=== answer (8 occurrences) ===
  c11_b03.md: ...方法用于五个系统。输出是每个系统的一页阅读笔记。   #### Spanner（Google）  Question | Answer ---|--- Replication protocol | 每 tablet Paxos；线性化时间戳使用 True...
  c11_b03.md: ...的工作负载（广告、金融账本）；Google 自身基础设施   #### CockroachDB  Question | Answer ---|--- Replication protocol | 每 range Raft；HLC-based MVCC...
  c11_b04.md: ...#### DynamoDB（AWS）  Question | Answer ---|--- Replication protocol | 区域内无 leader；跨区域全局表用异步复制 Fail...
  c11_b04.md: ...r | 访问模式可预测的单租户键值工作负载；AWS 原生系统   #### Cassandra  Question | Answer ---|--- Replication protocol | 无 leader；按请求可调一致性（ONE、QUORUM...
  c11_b04.md: ...重且可放宽一致性的工作负载；追求每节点简单的大规模系统   #### FoundationDB  Question | Answer ---|--- Replication protocol | 自定义（sequencer + transaction-...
  c11_b05.md: ...nya 的 Stratus DB 阅读  Anya 把同样方法用于她正在开发的系统。阅读结果：  Question | Answer（Stratus DB） ---|--- Replication protocol | 每 range Raft（使用...
  c11_b08.md: ...。找其复制协议、故障模型、一致性和分区行为。  **答案：** TigerBeetle 阅读：  Question | Answer ---|--- Replication protocol | 自定义 Viewstamped Replication...
  c11_b09.md: ...协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Question | Answer（两者） ---|--- Replication protocol | 无 leader；按请求可调一致性 Failur...

=== bigtable (8 occurrences) ===
  c11_b11.md: ...方法通过要求精确答案抵御这些陷阱。  第12章进入下一层：分布式数据库如何跨节点分片数据。哈希分片、范围分片、一致哈希、Bigtable / Cassandra / RocksDB 的 LSM-tree 基础，以及 Anya 在 Stratus DB 集群...
  c12_b03.md: ...Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被 Cassandra、RocksDB、ScyllaDB、Level...
  c12_b03.md: ...）；B-tree 接近 1 倍。  2026 年，LSM 主导分布式数据库存储底层，因为大多数工作负载写重或写吞吐受限。Bigtable、Cassandra、RocksDB（CockroachDB、TiKV、YugabyteDB、MongoDB Wired...
  c13_b03.md: ...ator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它是大多数 “Spanner 风格” 数据库在底层实际采用的模式。  结构：在键值存储之上的...
  c13_b03.md: ...由读时恢复处理（任何后续 reader 都能清理）。   #### Percolator 用于何处  * Google Bigtable 的事务层（Bigtable 的 “单行事务” 是原子的；Percolator 增加了多行）。 * TiDB 使用 Pe...
  c13_b12.md: ...提交点。恢复由读者而非协调者执行。  **答案：** Percolator 是 Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它通过两阶段（Prewrite + Commit）在键值存储上实现快照隔离事务，无中心协调者...
  c30_b02.md: ...ing and Random Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_....
  c30_b06.md: ...操作看起来在调用与响应之间某个瞬间生效，与实时一致。 * **LSM-tree（日志结构合并树）**：写优化的存储底层，Bigtable、Cassandra、RocksDB 使用。 * **Metastable failure（亚稳态故障）**：损坏状态自...

=== table (8 occurrences) ===
  c12_b01.md: ...实现局部范围查询）。  选择取决于工作负载：纯 OLTP 点查偏向哈希；分析扫描偏向范围；混合工作负载需要通过 per-table 配置获得两者。...
  c12_b06.md: ...围分片使该查询成为单分区扫描。示例：HBase、ClickHouse、TimescaleDB。  许多现代系统 per-table 提供两者：CockroachDB 默认范围但支持哈希分片表；Cassandra 哈希分片但允许同一 partition...
  c12_b10.md: ...作负载，带自动分裂的范围分片是正确默认。对应用控制键分布的工作负载（缓存、会话存储），哈希是正确默认。现代系统 per-table 提供两者；选择不是“哪个系统”，而是“哪张表用哪种方案”。  第二个考虑：范围分片的元数据开销真实。对拥有数百万小 r...
  c12_b11.md: ...数据。哈希实现均匀分布和简单路由，但不支持范围查询；范围提供查询局部性和自动分裂，但顺序写有热点风险。现代系统 per-table 提供两者。  一致哈希是哈希分片系统的基础：增删节点只移动 $O(1/N)$ 数据。Vnodes（每物理节点 256+...
  c14_b02.md: ...d MVCC，NTP 偏移上界（默认 500 ms）。 * Postgres 线缆兼容。 * 多区域部署，支持 per-table 区域配置。 * 开源核心（CockroachDB Community）；企业版附加高级功能。  优势：  * 可跑在任...
  c14_b02.md: ...：  * 可跑在任何云或本地。 * Postgres 兼容性意味着现有工具（psql、驱动、ORM）可用。 * Per-table 多区域配置（global、regional、follower reads）。 * 默认可串行化。  劣势：  * 运维...
  c15_b09.md: ...CRDT；应用必须自己实现。  (c) **迁移到多主系统 + 无冲突语义** ：CockroachDB 的 per-table 区域亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Glob...
  c30_b10.md: ...luster 用于热读；全局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 *...

=== ssd (8 occurrences) ===
  c12_b03.md: ...B-tree  B-tree 更新需要读取相关页（4-16 KB）、修改、写回。传统机械硬盘上随机写 I/O 成本高；SSD 好些，但仍付写放大成本。  LSM 更新只需追加到 memtable（内存；快）和追加到 WAL（顺序写；快）。Com...
  c12_b04.md: ...SM 把随机写转化为顺序写（通过 WAL 和 memtable 批量），这是 B-tree 无法比拟的结构优势。在现代 SSD 工作负载中，LSM 的优势是 _可预测_ 写吞吐；读性能通常够用。   ### 12.5 工作流 7：Anya 的重分...
  c12_b07.md: ...的（WAL 追加；把整个 memtable 刷成新 SSTable）。传统存储上顺序写比随机写快 10-100 倍；在 SSD 上也快（但差距没这么大）。  (b) LSM 批量磁盘 I/O：很多写先在 memtable 累积，再一起刷盘。B+...
  c12_b09.md: ...选择嵌入式存储：RocksDB（LSM）与 SQLite（B+ tree）。工作负载：80% 写、20% 读、随机访问、SSD 上 100 GB 总数据集。比较并推荐。  提示  写重 favor LSM。SSD 减小但未消除 B+ tree 随...
  c12_b09.md: ...80% 写、20% 读、随机访问、SSD 上 100 GB 总数据集。比较并推荐。  提示  写重 favor LSM。SSD 减小但未消除 B+ tree 随机写劣势。  **答案：** 工作负载特征：  * 80% 写、20% 读：写重。 *...
  c12_b09.md: ...作负载特征：  * 80% 写、20% 读：写重。 * 随机访问：两种系统都无顺序局部性收益。 * 100 GB on SSD：大但还不算巨大；SSD 降低随机 I/O 成本。  RocksDB 画像：  * 写吞吐：非常高（memtable +...
  c12_b09.md: ...% 写、20% 读：写重。 * 随机访问：两种系统都无顺序局部性收益。 * 100 GB on SSD：大但还不算巨大；SSD 降低随机 I/O 成本。  RocksDB 画像：  * 写吞吐：非常高（memtable + WAL 顺序）。80%...
  c12_b09.md: ...嵌入式数据库。  推荐：该工作负载用 RocksDB。80% 写占比意味着 RocksDB 的写吞吐优势占主导；即便在 SSD 上，B+ tree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20% 读占比在 RocksDB 中配合...

=== top (8 occurrences) ===
  c14_b10.md: ...玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7 万 reads/sec。大多是 “top-N 排行榜”，偶尔 “我的分数”。 * **一致性**：因果足够（若玩家 A 提交分数后玩家 B 在看到 A 的分数后...
  c14_b10.md: ...*Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 Dyna...
  c14_b10.md: ...玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7 万 reads/sec。大多是 “top-N 排行榜”，偶尔 “我的分数”。 * **一致性**：因果足够（若玩家 A 提交分数后玩家 B 在看到 A 的分数后...
  c14_b10.md: ...*Score 存储**：DynamoDB（或 Cassandra）per-region；跨区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 Dyna...
  c15_b04.md: ...；最终或 CRDT 计数器是正合适。   ### 15.7 排行榜工作负载  同一工作负载，不同一致性需求。游戏排行榜：top-10 分数，对所有观众几秒刷新一次。   #### 因果一致  若玩家 A 提交分数后，玩家 B 在看到 A 的分数出...
  c17_b02.md: ...致  页面浏览计数器；吞吐主导正确性；CRDT 或简单最终；每次递增亚毫秒。   #### 排行榜（本章）：因果一致  Top-N 分数；因果排序保持 “B 在看到 A 后提交”；读者每会话看到一致前缀；每次提交亚秒级。  实现：每次分数提交携带...
  c26_b07.md: ...对 Continuum：分层调度。Top-tier 客户（SLA 合同）有优先级；他们内部 weighted fair 分配。低 tier 客户获得剩余；他们内...
  c30_b14.md: ...│   ├── kv_state_machine.py — replicated key-value store on top     │   └── tests/            — split-brain, partition, rec...

=== queue (8 occurrences) ===
  c18_b03.md: ...被混淆的目的。  (a) **效率**：同一时间只有一个客户端做昂贵工作。若两个同时做，工作重复但无损害。示例：job queue 中每个任务应由一个 worker 处理。  (b) **正确性**：同一时间只有一个客户端修改受保护资源。若两个同时做...
  c18_b03.md: ...不要用。” 这过于绝对。Redlock 对 _正确性用例_ 是错的；对 _效率用例_ 可以。区别很重要：  * Job queue：用 Redlock（或任何 best-effort 锁）。最坏情况：一个任务被处理两次，浪费但不灾难。 * Payme...
  c18_b07.md: ...一个 worker 应同时做昂贵工作。若偶尔两个 worker 都做（因锁失败），重复工作浪费但无害。  示例：job queue 去重、周期批处理任务调度、昂贵计算记忆化。  (b) 下游影响是幂等的：即使两个 worker 都执行，效果与一个相同...
  c18_b07.md: ...Crescent 支付协调者角色是正确性用例；修复使用 fencing token。Glide 处理用户上传的 job queue 是效率用例；缓存锁即可。  **Q4.** 为什么 Crescent 修复同时组合 Raft 锁服务和 fencing...
  c18_b08.md: ...#### 应用问题  **Q1.** 一个团队实现 job queue，每个任务应被恰好一个 worker 处理。他们考虑 (a) Redlock，(b) 带 fencing token 的...
  c18_b08.md: ...edlock，(b) 带 fencing token 的 etcd 锁，(c) 数据库行级锁。比较。  提示  Job queue 可以是纯效率（若任务幂等）。每种锁方法有不同成本/安全权衡。  **答案：**  (a) **Redlock** ：简...
  c18_b08.md: ...即可。最简单最快。 * 非幂等任务：若已有数据库，选 (c)；若无，选 (b)。两者都提供正确性。 * 高吞吐 job queue（每秒数千任务）：专用系统（Kafka with consumer groups、Cloud Pub/Sub）原生处理投...
  c18_b08.md: ...b）原生处理投递语义，避免 per-job 锁模式。  更深观察：”per-job 锁“ 往往是错误架构。专用 job queue（内置至少一次或恰好一次语义）原生去重。锁模式通常是 “自己造轮子”；现代系统通常可围绕专用队列基础设施设计。  **Q...

=== config (8 occurrences) ===
  c18_b11.md: ...第19章讲服务发现与配置：etcd 作为现代默认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data + watch stream 模式。这些是让其余分布式基础设施运行的元系统。  ---...
  c19_b01.md: ...服务发现问题。2026 年的答案是 etcd、Consul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch streams” 模式。   ### 19.1 发现问题  服务网格有若干实例。...
  c19_b03.md: ...速变化的服务（自动扩缩无状态服务），基于 watch 的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客...
  c19_b05.md: ...但新系统通常从 etcd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 w...
  c19_b05.md: ...ms”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，...
  c19_b05.md: ...但新系统通常从 etcd 开始。ZooKeeper 生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 w...
  c19_b05.md: ...ms”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，...
  c19_b11.md: ...API 和更好的云原生工具。ZooKeeper 仍在生产中使用（现有 Kafka、Hadoop 等），但缓慢衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、...

=== netflix (8 occurrences) ===
  c19_b10.md: ...ap 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Java 中心化服务发现。Netflix 使用；2026 年较不常见，但某些遗留系统仍在用。  对 2026 年的新 EC2 部署：Consul 或 Cloud...
  c21_b02.md: ...可能成功，请求也快速失败。对某些工作负载不可接受；对其他工作负载，“快速失败” 比 “挂 30 秒” 更可取。  实现：Netflix Hystrix（经典原型；已弃用，转而推荐 Resilience4j）；Resilience4j（Java）；poll...
  c21_b06.md: ...losed-with-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netflix Hystrix（已弃用；被 Resilience4j 替代）；Resilience4j（现代 Java）；polly（...
  c22_b08.md: ...tion Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已弃用但历史上重要）有类似功能。  上述设计无需每个后端都做深度健康检查即可防御灰色故障。负载均衡器...
  c22_b11.md: ...重试率、饱和指标、错误预算。对变化率告警与对绝对值告警同等重要。  第23章讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones...
  c23_b01.md: ...沟的纪律。  本章是工作流 11：混沌 game day 的设计与执行，以及它所属于的更广泛混沌工程学科。该学科起源于 Netflix 的 Chaos Monkey；2026 年已成熟为标准 SRE 实践，包含组织和技术组件。   ### 23.1 什么...
  c23_b01.md: ...下游变慢、区域中断。  (e) **最小化爆炸半径**。从小处开始（一个节点、一个 shard）；建立信心后再扩大。  Netflix 起源：Chaos Monkey（随机 kill 生产实例的工具）约 2010 年引入。Netflix 的理由：实例反正...
  c23_b01.md: ...信心后再扩大。  Netflix 起源：Chaos Monkey（随机 kill 生产实例的工具）约 2010 年引入。Netflix 的理由：实例反正会在生产环境中失败，不如在工程师能响应的工作时间故意 kill，使系统对不可避免故障更健壮。该学科传播...

=== backend (8 occurrences) ===
  c22_b08.md: ...系统能更优雅地从亚稳态恢复。  **Q3.** 走一遍用基于延迟的路由防御灰色故障的负载均衡器设计。  提示  Per-backend p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a) **Per-ba...
  c22_b08.md: ...nd p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a) **Per-backend 延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (b) **Per...
  c22_b08.md: ...延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (b) **Per-backend p99 计算**：每几秒计算每个后端的 p99 延迟。  (c) **集群范围延迟参考**：计算所有后端 p99 的中...
  c22_b08.md: ...99 延迟。  (c) **集群范围延迟参考**：计算所有后端 p99 的中位数。这是 “预期” 延迟。  (d) **Backend weight**：根据每个后端 p99 相对于集群中位数的反比分配权重。比中位数快的后端权重 1.0；慢的后端按比例降...
  c22_b08.md: ...系统能更优雅地从亚稳态恢复。  **Q3.** 走一遍用基于延迟的路由防御灰色故障的负载均衡器设计。  提示  Per-backend p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a) **Per-ba...
  c22_b08.md: ...nd p99 延迟跟踪；降低高 p99 后端优先级；渐进而非突然移除。  **答案：** 设计：  (a) **Per-backend 延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (b) **Per...
  c22_b08.md: ...延迟跟踪**：负载均衡器跟踪每个后端每个请求的延迟。维护滑动窗口（如最近 60 秒）的延迟观测。  (b) **Per-backend p99 计算**：每几秒计算每个后端的 p99 延迟。  (c) **集群范围延迟参考**：计算所有后端 p99 的中...
  c22_b08.md: ...99 延迟。  (c) **集群范围延迟参考**：计算所有后端 p99 的中位数。这是 “预期” 延迟。  (d) **Backend weight**：根据每个后端 p99 相对于集群中位数的反比分配权重。比中位数快的后端权重 1.0；慢的后端按比例降...

=== subgraph (8 occurrences) ===
  c25_b02.md: ...：       graph TB         LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]...
  c25_b02.md: ...P2["Prefill Node 2<br/>4× H100"]         end         subgraph CacheTier["KV Cache Tier<br/>(Mooncake / LMCache)"]...
  c25_b02.md: ...he Node 1]             K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]...
  c28_b03.md: ...完整的 Crescent 技术栈可视化：       graph TB         subgraph Edge["Edge (Cloudflare Workers)"]             CF[API Gatewa...
  c28_b03.md: ...PI Gateway<br/>TLS / auth / rate limit]         end         subgraph USE["US-East Region"]             APIE[API Service]...
  c28_b03.md: ...vice]             KAFKAE[Kafka brokers]         end         subgraph EUW["EU-West Region"]             APIW[API Service]...
  c28_b03.md: ...odes]             KAFKAW[Kafka brokers]         end         subgraph USW["US-West Region"]             APIW2[API Service]...
  c28_b03.md: ...]             CRDBW2[CockroachDB nodes]         end         subgraph Shared["Shared Services"]             TB[TigerBeetle<br/>5-...

=== lmcache (8 occurrences) ===
  c25_b03.md: ...式（PagedAttention 风格）分布在集群级缓存层。Decode 节点需要某块时，从对应 shard 读取。  LMCache（UC Berkeley，2024）是开源等价物。为 vLLM 集成设计；提供集群级 KV cache，任何 decod...
  c25_b05.md: ...节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) **LMCache**（UC Berkeley，2024）：开源等价物。为 vLLM 集成设计。提供集群级 KV cache，任何 dec...
  c25_b10.md: ...技术快速演进。Splitwise（2023）引入基本思想；DistServe（2024）精化调度；Mooncake 和 LMCache（2024-2025）添加缓存层；Mysticeti 等扩展到 MoE 模型。2026 年采用分离的团队应预期每 18...
  c25_b11.md: ...ise（Microsoft Research）和 DistServe（UCSD）是经典研究；生产系统（Mooncake、LMCache）实现这些模式。采用阈值：约 50+ 节点集群，效率收益超过运维复杂度。  KV cache sharding 把 pe...
  c25_b11.md: ...he 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用。Mooncake（商业）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 + 30 decode 节点...
  c30_b03.md: ...rchitecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (https://github.com/LMCache/LMCache). — 开源 KV cache 层。 * Le...
  c30_b03.md: ..." — Moonshot AI 的 KV cache 层。 * LMCache (https://github.com/LMCache/LMCache). — 开源 KV cache 层。 * Leviathan, Y., Kalman, M., & M...
  c30_b03.md: ...shot AI 的 KV cache 层。 * LMCache (https://github.com/LMCache/LMCache). — 开源 KV cache 层。 * Leviathan, Y., Kalman, M., & Matias, Y...

=== streaming (8 occurrences) ===
  c27_b08.md: ...类工作要么是确认 “是的，加上 timeout，正确值是 X”，要么是反驳 “这是故意的；这是不应 timeout 的 streaming endpoint；AI 没看到上下文。” 这种阐明是审查者的贡献。  价值下降的技能：做 AI 的工作（肉眼扫描缺失...
  c28_b10.md: ...| N 节点集群磁盘利用率高；需加 M 节点 | 再平衡集群，零停机，事务一致性保留 | 一致性哈希 + range streaming + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 str...
  c28_b10.md: ...ing + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | | 8 | Olamide 的跨 region 支付 | Ch.13 §13.7...
  c30_b08.md: ...l 集群带来负载）。 * **放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gos...
  c30_b08.md: ...**放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gossip 协议的 BoltD...
  c30_b08.md: ...响。 * **恢复**：需要多厂商事件响应（Roblox + HashiCorp）；最终 BoltDB 需要从快照恢复，streaming 子系统从干净状态重建。 * **章节映射防御**：     * Ch.19（服务发现）：单一依赖 Consul 且无优...
  c30_b08.md: ...。     * Ch.22（亚稳态故障）：自我维持的损坏状态需要外部干预才能打破。     * Ch.21（级联故障）：streaming 子系统故障传播到依赖服务。     * Ch.23（混沌工程）：该具体故障模式（高 heartbeat 负载下的 Co...
  c30_b11.md: ...通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink 与 Flink c...

=== happy (7 occurrences) ===
  c01_b02.md: ...却把来自别处的每三个请求丢一个。  单机模型把失败当作异常。分布式模型把部分失败当作默认模式。如果你写分布式代码只处理 happy path、把失败当作不寻常的例外，那你写的代码终将在某一天让 Adaeze 被重复扣款。   ### 1.2 分布式推...
  c01_b03.md: ...oken？它是否假设它正在对话的节点就是它所认为的那个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否...
  c01_b09.md: ...其他节点持有它。在网络分区或 GC 暂停下，两个节点可能都相信自己持有锁，两者都会作为领导者行动。  _失败_ ：只有 happy path、把失败当作异常的代码。示例：`result = remote_service.call(); update_...
  c27_b01.md: ...唯一地扮演其角色。假设 “领导者” 而不经协议强制唯一性的代码是脆弱的。  (c) **故障**：部分故障是例外。只有 happy path 和 error path 的代码把故障当作罕见；现实是部分故障是默认。  Thread C 的六个谎言，作为...
  c27_b01.md: ...没有 fencing token 的 Redlock。假设锁持有者唯一的代码。    5. **部分故障罕见**。只有 happy path 的代码；不考虑 “半成功” 结果。    6. **CAP 分类简单**。说 “我们用 CP 系统” 而不理...
  c27_b04.md: ...ation_service.notify(user.id, 'updated')   谎言：三个调用一起成功。代码只有 happy path；部分故障是隐含的。  现实：调用相互独立；任何都可能失败。`primary_db.update()` 成功但...
  c27_b09.md: ...edlock 风格代码假设锁持有者唯一；缺少 fencing token。    5. **部分故障罕见**：代码只有 happy path；不考虑 “半成功” 结果；无 saga、无补偿动作。    6. **CAP 分类简单**：代码说 “我们用...

=== socratopia (7 occurrences) ===
  c01_b06.md: ...之外，但建议在任何阅读路径最后读；它会指出哪些命名系统和原语可能演进，以及心智模型的哪些方面是持久的。  每章设计为与 Socratopia 伴侣一起进行大约四到五次工作会话。你先通读一遍把握结构。伴侣和你一起读，提出问题、看着你完成示例，并探查每章记录的错误...
  c01_b07.md: ...。  * **分布式系统完全新手** ：按顺序阅读，第1章 → 第29章（第29章是前瞻性后记）。计划每章 4-5 次 Socratopia 会话；完整弧线预计 12-16 周。每章后做概念检测练习；第三部分后做应用问题；每部分末尾做深入思考问题。  * **...
  c01_b08.md: ...#### 练习说明  每章末尾的练习是为让 Socratopia 伴侣引导你完成，而不是作为总结性测试。概念检测项测试回忆 + 理解；应用问题练习迁移（把概念应用到新场景）；深入思考项...
  c30_b13.md: ...*   ### 附录 G — 可运行代码仓库（计划配套）  配套代码仓库 **计划** 位于 `github.com/socratopia/distributed-systems-2026-examples`（状态：截至本书出版日期 2026-05-20 仍...
  c30_b13.md: ...5-20 仍在开发中）。下面记录的结构是预期布局；查阅本附录的读者应将其视为设计规范，而非完整交付的 artifact。Socratopia 团队和贡献者将在出版后数月内填充仓库；想跟踪进度（或贡献）的读者可咨询 Socratopia 社区渠道。  完成后，仓...
  c30_b13.md: ...交付的 artifact。Socratopia 团队和贡献者将在出版后数月内填充仓库；想跟踪进度（或贡献）的读者可咨询 Socratopia 社区渠道。  完成后，仓库将包含按章节组织的本书核心原语可运行实现。意图不是生产级分布式系统库——已有经过验证的库（e...
  c30_b19.md: ...* * *  _2026-05-20 作为 post-pipeline 润色的一部分生成。_ _QBooks · A Socratopia Learning Series Publication_  * * *  ---...

=== lynch (7 occurrences) ===
  c02_b03.md: ...ric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 2002 年证明了它。口号的 CAP 说“在一致性、可用性、分区容错性里选两个”。这种表述有两处误导。  首先，分...
  c07_b01.md: ...“到底有多难” 的精确陈述是分布式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即使只可能发生一次崩溃故障的完全异步系统中...
  c07_b02.md: ...目的等价。现代协议通常用部分同步加隐式故障检测器（心跳）。   ### 7.4 部分同步  经典形式化来自 Dwork、Lynch 和 Stockmeyer 1988 年关于部分同步下共识的论文。模型引入 _Global Stabilization...
  c30_b01.md: ...omputing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (2002). "Brewer's conjecture and the feasibility of con...
  c30_b02.md: ...#### 共识（第三部分 — 第7-10章）  * Fischer, M. J., Lynch, N. A., & Paterson, M. S. (1985). "Impossibility of distrib...
  c30_b02.md: ...ty process." _Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the presence of...
  c30_b06.md: ...数器；被保护资源强制执行令牌顺序。 * **FLP impossibility（FLP 不可能性）**：Fischer-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不能保证终止。 * **Gray failure（灰色...

=== recv (7 occurrences) ===
  c03_b03.md: ...x 或 x < a。  P_3 接收 n 的时间戳为 5。P_1 发送 m 的时间戳为 2。链 send(m) \to recv(m) \to send(n) \to recv(n) 反映为递增的时间戳 (2, 3, 4, 5)。   ### 3....
  c03_b03.md: ...戳为 5。P_1 发送 m 的时间戳为 2。链 send(m) \to recv(m) \to send(n) \to recv(n) 反映为递增的时间戳 (2, 3, 4, 5)。   ### 3.4 向量时钟  Lamport 时间戳保留顺序但...
  c03_b03.md: ..., carries [2,0,0] | [2,0,0] 3 | P_3 | x | [0,0,1] 4 | P_2 | recv m | [2,1,0] 5 | P_2 | send n, carries [2,2,0] | [2,2,0] 6 |...
  c03_b03.md: ...,1,0] 5 | P_2 | send n, carries [2,2,0] | [2,2,0] 6 | P_3 | recv n | [2,2,2]  现在我们能检测并发。V(a) = [1,0,0] 与 V(x) = [0,0,1] 互不小于...
  c03_b03.md: ...发）。V(send m) = [2,0,0] < V(recv n) = [2,2,2]，所以 send(m) \to recv(n)，与链一致。   #### 代价  向量时钟随进程数扩展。10 节点系统每个消息携带 10 项向量；1000 节点...
  c03_b13.md: ...本地事件 y | 3 | 5 | 7 | y 的 L = 7  因果链：a \to b \to send\,m \to recv\,m \to send\,n \to recv\,n \to y。Lamport 时间戳沿因果路径形成单调链：1, 2...
  c03_b13.md: ...L = 7  因果链：a \to b \to send\,m \to recv\,m \to send\,n \to recv\,n \to y。Lamport 时间戳沿因果路径形成单调链：1, 2, 3, 4, 5, 6, 7。事件 x 的 L...

=== wins (7 occurrences) ===
  c03_b11.md: ...1.** 一个团队在三个数据中心运行 Cassandra，每秒 100,000 次写入，偶尔发现“last write wins” tombstone 复活了一条已删除记录。用时钟偏移问题诊断，并推荐修复。  提示  Cassandra 的 LWW...
  c03_b11.md: ...被盖戳早于后续写入的时间戳，会发生什么？  **答案：** Cassandra 的默认冲突解决是 last-write-wins（LWW），基于协调者节点分配的 wall-clock 时间戳。如果删除由时钟落后的协调者处理，它被盖戳早于另一个时钟超...
  c05_b03.md: ...价是冲突必须在读时或后台修复时解决，因为两个写入者可能并发写冲突值。  冲突解决策略：  * **Last write wins（LWW）** 基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **...
  c15_b02.md: ...策略  当两个并发写冲突（两个副本接受同一键的不同值）时，系统必须解决。三种策略：  (a) **Last write wins（LWW）** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。  (b) **Sib...
  c15_b03.md: ...域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-write-wins。应用必须处理自己写入一个区域后被另一区域并发写覆盖的情况（罕见但可能）。   #### Cassandra  按操作可...
  c15_b07.md: ...其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last write wins”（LWW）冲突解决，它的失败模式是什么？  提示  LWW：时间戳最高的写胜出。失败：时钟偏移可导致 “逻辑上更晚”...
  c15_b09.md: ...覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入同一 item（在时钟偏移窗口内），一个会胜出，另一个被丢...

=== delivered (7 occurrences) ===
  c04_b03.md: ....add(sender)         if len(acks[msg]) > N/2 and msg not in delivered:             delivered.add(msg)             upper_layer.del...
  c04_b03.md: ...len(acks[msg]) > N/2 and msg not in delivered:             delivered.add(msg)             upper_layer.deliver(msg)   性质（在可靠广播之外）...
  c04_b09.md: ...eer。只要崩溃节点之外至少有一个 peer 收到并重广播，消息就会到达全部。  **答案：** 伪代码：       delivered = set()      def broadcast(msg):         flood(msg, sender=...
  c04_b09.md: ...sender=self)      def flood(msg, sender):         if msg in delivered:             return         delivered.add(msg)         uppe...
  c04_b09.md: ...r):         if msg in delivered:             return         delivered.add(msg)         upper_layer.deliver(msg)         for peer...
  c04_b09.md: ...示例：P1、P2、P3、P4。P1 广播 m。  * P1 将 m 发给 P2、P3、P4。 * P2 收到 m，加入 delivered，发给 P3、P4（跳过 P1）。 * P3 收到 m（假设先从 P1），加入 delivered，发给 P2、P4。...
  c04_b09.md: ...P2 收到 m，加入 delivered，发给 P3、P4（跳过 P1）。 * P3 收到 m（假设先从 P1），加入 delivered，发给 P2、P4。 * P4 收到 m，依此类推。  现在假设 P2 收到 m 后在重广播前崩溃。P3 和 P4 仍直...

=== streams (7 occurrences) ===
  c04_b06.md: ...据配置接近因果或更好。 * **etcd** 通过 Raft 提供全序复制。状态机命令的原子广播。 * **Redis Streams** 在 stream 内提供有序交付；消费者组提供 at-least-once。 * **CockroachDB 的...
  c19_b01.md: ...sul 或云厂商 DNS。本章讲授发现问题、标准解，以及更广泛的 “config as data with watch streams” 模式。   ### 19.1 发现问题  服务网格有若干实例。实例：  * 启动并变为可用。 * 监听特定端口。 *...
  c19_b01.md: ...参考设计。  关键性质：  * 基于 Raft（第9章）实现共识。 * 键值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 * Transac...
  c19_b03.md: ...的发现（etcd、Consul）响应更快。   ### 19.5 Config as Data with Watch Streams  一个超越服务发现的更广泛模式：把配置作为数据存在协调服务中，客户端 watch 变更。  模式：  (a) 配置以...
  c19_b05.md: ...生态逐渐萎缩；etcd 生态逐渐增长。  **Q3.** 什么是 “config as data with watch streams”？举一个用例。  提示  配置作为数据存在协调服务中；客户端 watch 变更并动态重新配置。  **答案：** Co...
  c19_b05.md: ...服务中；客户端 watch 变更并动态重新配置。  **答案：** Config-as-data with watch streams：一种模式，配置作为数据存在协调服务（etcd、Consul）中，配置消费者 _watch_ 相关 key 的变更。配置...
  c19_b11.md: ...在生产中使用（现有 Kafka、Hadoop 等），但缓慢衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul...

=== standby (7 occurrences) ===
  c05_b02.md: ...是经典实现。PostgreSQL 的 `synchronous_commit = remote_write` 配多个 standby 行为类似。   ### 5.5 基于 Leader 的复制  一种拓扑选择。一个节点是 _leader_（或称 pri...
  c05_b07.md: ...无限等待，要么放弃同步要求。PostgreSQL 的 `synchronous_commit` 会阻塞；运维必须介入把 standby 从 `synchronous_standby_names` 中移除。  (c) **异步副本可能落后。** 若主库和同...
  c06_b02.md: ...模式 1：单区域 active-passive  所有写发往一个区域（active）。其他区域是异步复制的 warm standby。读可去任何区域（可能过期）。  * **Latency** ：写只付区域内成本；读可本地。 * **Failover*...
  c06_b02.md: ...域（可能过期）。  * **Latency** ：写只付区域内成本；读可本地。 * **Failover** ：切换到 standby 需数分钟；可能丢失部分写。 * **Right for** ：单区域可用性足够、DR 是唯一多区域目标的工作负载。...
  c28_b14.md: ...failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-brain 可能需要人工干预。 * **一致性模型**：事务内快照隔离；副本读最终一...
  c30_b10.md: ...本 + Redis 缓存。  * **Operational 存储**：PostgreSQL 16，带一或两个 hot-standby 读副本（同步到一个，异步到另一个）。 * **缓存**：Redis Cluster 用于热读路径（TTL 60s；写入...
  c30_b10.md: ...存**：Redis Cluster 用于热读路径（TTL 60s；写入时事件驱动失效）。 * **复制**：同步到一个 standby，异步到另一个。RPO = 0 对同步副本；RPO 为秒级对异步副本。 * **为何不选 NewSQL**：在中等单区域...

=== transaction (7 occurrences) ===
  c05_b04.md: ...wer 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB 和 YugabyteDB 通过 per-transaction HLC 时间戳使用 (c) 的变体。会话跟踪见过的最大 HLC；同一会话的后续读使用至少该 HLC。这是现代运维模式。...
  c11_b06.md: ...Linearizable：遵守实时顺序；per-operation。Serializable：某种串行顺序；per-transaction。  **答案：** Linearizable 是 per-operation 属性：每个操作看起来在调用与响应之间的某...
  c11_b06.md: ...且符合实时顺序。若操作 A 在操作 B 开始前完成返回，则 A 排在 B 前。  Serializable 是 per-transaction 属性：任何并发事务的执行都等价于某种串行顺序。该顺序不必符合实时；即使事务 A 在 B 开始前提交，串行等价顺序仍可能...
  c14_b08.md: ...）。OLAP 常是弹性计算（为查询启动 worker，结束后释放）。 * **一致性** ：NewSQL 提供 per-transaction ACID。OLAP 常提供快照一致（查询看到查询开始时的一致视图，查询期间写入不反映）。 * **成本模型** ：Ne...
  c20_b01.md: ..."fraud check<br/>80 ms (serial after auth)"]         DB["DB transaction<br/>95 ms (cross-region, serial after fraud)"]         L["l...
  c20_b01.md: ...；它们时长相加约等于根总时长（65 + 22 + 80 + 95 + 30 + 73 ≈ 365 ms p50）。DB transaction 的四个子节点在事务内部也是串行的；总和（93 ms）接近 DB span 总时长（95 ms），2 ms 差异反映事务...
  c20_b01.md: ...时长（95 ms），2 ms 差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有...

=== budget (7 occurrences) ===
  c06_b06.md: ...he，每个 token 必须在 ~10-50 ms 内生成。一次 50 微秒网络跳占预算 5-25%；降到 2 微秒是 budget 内与超出的差别。高频率和紧预算让 RDMA 值得运维成本。  同样理由适用于 ML 训练（每步梯度交换）和分布式内存数...
  c27_b02.md: ...行可能让调用服务无限挂起，促成级联故障，造成 p99 延迟灾难。  修复：每个网络调用应有 timeout、retry budget、circuit breaker，以及（若可能）fallback。       @circuit_breaker(thr...
  c27_b02.md: ...reaker(threshold=0.5, timeout=30)     @retry(max_retries=3, budget=0.1)     @timeout(seconds=2)     def get_user_profile(user_...
  c27_b06.md: ...模式识别_ 一半上可靠：  * **语法模式**：`def call_with_retry()` 函数没有 retry budget；`time.time()` 用在看起来像跨机器上下文的地方；`requests.get()` 没有 timeout（库...
  c27_b07.md: ...ptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确...
  c30_b06.md: ...garo-Ousterhout 2014 共识协议。分解为 leader 选举、日志复制、安全性。 * **Retry budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping anti-p...
  c30_b14.md: ...├── ch21_resilience/     │   ├── retry_budget.py   — retry budget implementation (gRPC-style)     │   ├── circuit_breaker.py...

=== sui (7 occurrences) ===
  c07_b05.md: ...并行。 * **Tendermint**（Cosmos 链）用显式轮次超时；生产 1 秒一轮。 * **Aptos / Sui** 用 Narwhal-Bullshark（基于 DAG）获得比经典 BFT 更高吞吐；轮复杂度与消息延迟解耦。  这...
  c10_b01.md: ...Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullsha...
  c10_b01.md: .../>Danezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]         styl...
  c10_b01.md: ...ezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"]         Narwhal --> Aptos2["Aptos 2.x"]         style BG...
  c10_b03.md: ...串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年...
  c10_b03.md: ...在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解  常见混淆：“HotS...
  c10_b04.md: ...引用）。  生产中：  * **Aptos** 使用受 HotStuff 启发的共识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 *...

=== zab (7 occurrences) ===
  c08_b01.md: ...具影响力的共识算法。Google 用 Paxos 构建 Chubby（Spanner 的基础）。ZooKeeper 的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport 2001 年改写成 “Paxos Made Simple...
  c08_b10.md: ...辩论对常见情况已基本倒向 Raft，Paxos 在少数真正有其优势的领域存活。  第二个考虑：ZooKeeper 的 ZAB（原子广播）是 Paxos 变体，大量生产基础设施依赖 ZooKeeper。因此即使系统不标榜“用 Paxos”，Pax...
  c19_b02.md: ...，因为：  (a) **更好的运维模型**：etcd 使用 Raft（更易理解；工具更好），而 ZooKeeper 的 ZAB（Paxos 变体）更复杂。  (b) **gRPC API**：etcd 的 API 基于 gRPC；现代；与云原生生...
  c19_b05.md: ...ooKeeper？  提示  etcd：Raft、gRPC、Kubernetes 采用。ZooKeeper：老 API、ZAB 共识、较小的现代生态。  **答案：** etcd 和 ZooKeeper 都是分布式协调服务，具有相似原语（键值存储...
  c19_b05.md: ...ect | etcd | ZooKeeper | |---|---|---| | Consensus | Raft | ZAB（Paxos 变体） | | API | 基于 gRPC，现代 | 较老的自定义协议 | | Key model | 平...
  c19_b05.md: ...ubernetes 的引力把 etcd 带到云原生基础设施的主导地位。  (b) **更好的运维模型**：Raft 比 ZAB 更易理解；etcd 的运维工具更精致。  (c) **gRPC 和云原生契合**：etcd 与现代云原生工具（Prom...
  c19_b11.md: ...对稳定服务足够。  etcd 在新系统中基本替代 ZooKeeper，因为 Kubernetes 采用、Raft 与 ZAB 共识选择、gRPC API 和更好的云原生工具。ZooKeeper 仍在生产中使用（现有 Kafka、Hadoop 等...

=== acceptors (7 occurrences) ===
  c08_b03.md: ...number b。   2. Leader 从当前日志末尾开始为槽跑第一阶段（`PREPARE(b)`）。   3. Acceptors 对每个槽回复它们最高接受的值。   4. Leader 决定任何“飞行中”槽（已有人接受的槽）的值，并正常继续处理新槽...
  c08_b08.md: ...RE(b=20)` 给所有 5 个。  假设 B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Accep...
  c08_b08.md: ...ptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Acceptors 4、5 先收到 A 的；它们设置 $b_{max}=10$ 并回复 A `PROMISE(10, null, null...
  c08_b08.md: ...置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。  A 的情况：A 只有 acceptors 4、5 的 2 个 promise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot...
  c08_b08.md: ...RE(b=20)` 给所有 5 个。  假设 B 的 prepare 先到达 acceptor 1、2、3（网络竞速）。Acceptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Accep...
  c08_b08.md: ...ptors 1、2、3 设置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。Acceptors 4、5 先收到 A 的；它们设置 $b_{max}=10$ 并回复 A `PROMISE(10, null, null...
  c08_b08.md: ...置 $b_{max}=20$ 并回复 B `PROMISE(20, null, null)`。  A 的情况：A 只有 acceptors 4、5 的 2 个 promise，不够多数（需要 3）。  B 的情况：B 有 5 个 promise，ballot...

=== nextindex (7 occurrences) ===
  c09_b08.md: ...遍它如何追上。  提示  Leader 通过重复 AppendEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：** Follower 重新加入时...
  c09_b08.md: ...的下一次心跳（`entries=[]` 的 `AppendEntries`）到达 follower。Leader 的 `nextIndex[follower]` 仍指向分区前追踪的条目。  (b) Follower 日志缺少 `prevLogIndex` 处...
  c09_b08.md: ...vLogIndex` 处条目（落后 1000 条）。Follower 拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现...
  c09_b08.md: ...拒绝。  (c) Leader 收到拒绝，递减 `nextIndex[follower]` 并重试。如此继续，直到 `nextIndex` 指向 follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可...
  c09_b08.md: ...follower 拥有的条目。  高效实现的拒绝响应会包含 follower 的最后日志索引，leader 可直接把 `nextIndex` 跳到该值，而不是一次减一。  (d) 找到匹配后，leader 发送下一批条目（通常批量，数十或数百条）。Follo...
  c09_b10.md: ...dex` 处的条目与 `prevLogTerm`。索引或 term 比较差一都会让不一致日志保持不一致。  (e) **NextIndex 回退缺失。** `AppendEntries` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LL...
  c09_b10.md: ...(e) **NextIndex 回退缺失。** `AppendEntries` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LLM 实现有时不减，导致无限循环重试，看起来像 hang。  (f) **快照与应用竞争。** 快照捕获某...

=== patroni (7 occurrences) ===
  c11_b02.md: ...部分复制配置需要人工提升副本。RTO 以小时计。  故障恢复时间决定可用性 SLA。5 秒 Raft 重选与 60 秒 Patroni 切换的停机画像不同。   #### 问题 3：一致性模型  系统对单个操作保证什么？模型可能是：  * **Linea...
  c28_b14.md: ...）。  提示  选系统；回答四个诊断问题；指出它如何防御时间、身份、故障。  **答案：** 以 Postgres + Patroni 高可用部署为例：  _诊断方法（第11章）_：  * **复制协议**：Postgres 逻辑复制；默认异步；Patr...
  c28_b14.md: ...roni 高可用部署为例：  _诊断方法（第11章）_：  * **复制协议**：Postgres 逻辑复制；默认异步；Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader...
  c28_b14.md: ...：Postgres 逻辑复制；默认异步；Patroni 通过 etcd 管理 failover。 * **故障恢复**：Patroni 通过基于 etcd 的 leader 选举检测主库故障；提升 standby；耗时 30-60 秒。split-bra...
  c28_b14.md: ...事务按事务开始时间排序，非 wall clock。跨副本顺序由复制决定；单副本内部不受时钟偏移影响。 * **身份**：Patroni + etcd 提供 leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未...
  c28_b14.md: ...富、SQL 强大。劣势：failover 不可用窗口；异步复制 RPO。  对 Crescent：Postgres + Patroni 不满足多 region 需求；CockroachDB 是正确选择。对单 region OLTP 工作负载，Postgr...
  c28_b14.md: ...region 需求；CockroachDB 是正确选择。对单 region OLTP 工作负载，Postgres + Patroni 运维简洁且工作良好。  这个练习说明：诊断方法适用于任何系统；三个谎言姿态揭示防御（或缺失）；两者让工程师无需通读每页...

=== strengths (7 occurrences) ===
  c11_b03.md: ...e 确保分区后时间戳一致 PACELC | PC / EC（分区下一致；正常运营为 commit-wait 延迟买单） Strengths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 bak...
  c11_b03.md: ...侧停止接受写 PACELC | PC / EC（分区下一致；无分区时为不确定性窗口读付 HLC-restart 成本） Strengths | 兼容 Postgres SQL；可部署于任何云；per-range Raft 水平扩展 Weaknesses |...
  c11_b04.md: ...件写提供 per-item 线性化 PACELC | 默认 PA / EL（低延迟、最终）；条件写操作 PC / EC Strengths | 免运维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据...
  c11_b04.md: ...致性 AP，quorum 时 CP PACELC | 可调；低一致性 PA / EL，quorum 时 PC / EC Strengths | 线性扩展到数千节点；可调权衡 Weaknesses | 运维复杂（tombstone 管理、compaction...
  c11_b04.md: ...ion behavior | CP——丢失法定人数则拒绝写 PACELC | PC / EC；面向 OLTP 延迟设计 Strengths | 久经考验的正确性（Snowflake 构建其上；Apple 大量使用）；经混沌工程验证 Weaknesses |...
  c11_b05.md: ...wer 上有界过期读 Partition behavior | CP——少数侧拒绝写 PACELC | PC / EC Strengths | 从一开始即为金融工作负载构建；深度可观测性 Weaknesses | 新系统；生态小；SQL 层仍在开发 Righ...
  c11_b08.md: ...本多数 PACELC | PC / EC；优化金融 OLTP 吞吐（benchmark 宣称每秒 100 万+ 转账） Strengths | 金融级正确性模型（确定性模拟测试 + TigerStyle 编程）；确定性执行；易于调试 Weaknesses |...

=== weaknesses (7 occurrences) ===
  c11_b03.md: ...运营为 commit-wait 延迟买单） Strengths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性...
  c11_b03.md: ...成本） Strengths | 兼容 Postgres SQL；可部署于任何云；per-range Raft 水平扩展 Weaknesses | 比单区域 Postgres 写延迟更高；运维调优复杂 Right for | 多区域 Postgres 替代；需要...
  c11_b04.md: .../ EL（低延迟、最终）；条件写操作 PC / EC Strengths | 免运维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访问模式可预测的单租户键值工作负载...
  c11_b04.md: ...可调；低一致性 PA / EL，quorum 时 PC / EC Strengths | 线性扩展到数千节点；可调权衡 Weaknesses | 运维复杂（tombstone 管理、compaction 调优）；LWT 慢 Right for | 写重且可放宽...
  c11_b04.md: ...迟设计 Strengths | 久经考验的正确性（Snowflake 构建其上；Apple 大量使用）；经混沌工程验证 Weaknesses | 底层 API（SQL 层是独立项目）；生态比 Postgres 兼容系小 Right for | 在其上构建分布式...
  c11_b05.md: ...——少数侧拒绝写 PACELC | PC / EC Strengths | 从一开始即为金融工作负载构建；深度可观测性 Weaknesses | 新系统；生态小；SQL 层仍在开发 Right for | CockroachDB 风格工作负载，Stratus...
  c11_b08.md: ...账） Strengths | 金融级正确性模型（确定性模拟测试 + TigerStyle 编程）；确定性执行；易于调试 Weaknesses | 专用于复式记账（非通用）；生态小 Right for | 银行账本、支付系统、金融 OLTP，且 schema 符...

=== fraud (7 occurrences) ===
  c13_b07.md: ...participant R as Reservation         participant F as Fraud         participant B as Bank         participant L as Ledg...
  c13_b07.md: ...reserve funds         R-->>O: reserved         O->>F: check fraud         F-->>O: score (OK)         O->>B: submit settlement...
  c20_b01.md: ...tor)"]         A["auth + idempotency<br/>22 ms"]         F["fraud check<br/>80 ms (serial after auth)"]         DB["DB transa...
  c20_b01.md: ...DB["DB transaction<br/>95 ms (cross-region, serial after fraud)"]         L["ledger write<br/>30 ms (serial after DB)"]...
  c20_b01.md: ...差异反映事务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享...
  c28_b03.md: ...Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]         end         CF --> APIE         CF --> A...
  c28_b03.md: ...APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW...

=== via (7 occurrences) ===
  c14_b02.md: ...ash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和 OLAP 无需 ETL。 * 运维成熟（在中国科技规模下久经考验：微信、阿里...
  c25_b02.md: ...1         LB -->|"new request"| P2         P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1...
  c25_b02.md: ...P1 -->|"KV cache via RDMA"| K1         P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1...
  c30_b03.md: ...M., & Matias, Y. (2023). "Fast Inference from Transformers via Speculative Decoding." _ICML_.   #### 综合与 AI 生成代码（第九部分 — 第2...
  c30_b14.md: ...or-loop)     │   ├── reliable.py       — reliable broadcast via flooding     │   └── causal.py         — causal broadcast w...
  c30_b14.md: ...(SQLite)     │   ├── choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_saga.py —...
  c30_b14.md: ...ation     │   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo.py — pro...

=== bigquery (7 occurrences) ===
  c14_b03.md: ...理分析工作负载（TB-PB 数据上的大扫描、聚合、跨源 join）。示例：Trino/Presto、Snowflake、BigQuery、Spark SQL。...
  c14_b04.md: ...锁定（专有格式，但 Iceberg 集成正在出现）；per-query 成本可能让调优差的工作负载意外。   #### BigQuery 的 Dremel  Google 的分析服务；基于 2010 年的 Dremel 研究。  * 在 Google Co...
  c14_b04.md: ...成。  劣势：仅限 GCP；写得差的查询可能成本主导预算。   #### 模式  三者（Trino、Snowflake、BigQuery）遵循同一架构模式：存储与计算分离； aggressively 并行化；分析扫描使用列式布局；暴露 SQL。  运维类别...
  c14_b05.md: .../OLAP） | TiDB，或 CockroachDB + 独立 OLAP 存储 纯 OLAP | Snowflake、BigQuery、数据湖上的 Trino Serverless / 可变负载 | Neon（单区域）、Aurora DSQL（多区域）...
  c30_b09.md: ...：无 fencing 的 failover 模式正是第5章警告的。   #### E.6 — Google Cloud BigQuery 存储故障，2023-08-08  * **触发**：BigQuery 存储写入 API 的 bug 导致存储层开始拒绝...
  c30_b09.md: ...#### E.6 — Google Cloud BigQuery 存储故障，2023-08-08  * **触发**：BigQuery 存储写入 API 的 bug 导致存储层开始拒绝特定表类的写入。 * **放大**：客户端应用重试；重试放大拒绝率；拒...
  c30_b09.md: ...值，触发自动区域 failover，但 failover 不包含 bug 修复。 * **时间线**：受影响工作负载的 BigQuery 写入部分中断 4 小时。 * **恢复**：部署 bug 修复；手动重置区域 failover 状态。 * **章节映...

=== apache (7 occurrences) ===
  c15_b03.md: ...## 2026 年重要性  Chandy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 ma...
  c15_b07.md: ...all-clock 时刻），但快照是系统的一个 _合法_ 状态。  这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapRe...
  c15_b08.md: ...署 (a) + (c)。长期若工作负载增长或用户投诉持续，评估 (e)。  **Q2.** 一个团队为流分析流水线构建 Apache Flink，需要端到端 “exactly-once” 保证（从 Kafka source 到 Postgres sin...
  c15_b11.md: ...原语。协议使用每条通道上的 marker 定义快照边界；记录的各进程状态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly-once 语义和类似分布式 checkpointing 系统的基础。  计数器 / 排行榜...
  c19_b02.md: ...#### etcd 为何替代 ZooKeeper  Apache ZooKeeper 是首个生产分布式协调系统（2000 年代末起源于 Yahoo；用于 Hadoop、Kafka、Ca...
  c30_b11.md: ...M 事件/秒；分区内有序处理；端到端 exactly-once；分钟到小时级延迟预算。  **决策**：Kafka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Ka...
  c30_b11.md: ...roker**：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Stru...

=== truth (7 occurrences) ===
  c15_b10.md: ...内累积，精确点赞数会压垮强一致。正确答案是近似计数器（CRDT 或 HyperLogLog），定期由分析 ground-truth。  (c) **遥测 / 指标计数器。** 边缘部署的传感器以每传感器每秒 10,000 事件计数。回调到中央强一致系...
  c18_b01.md: ...中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入...
  c18_b01.md: ...source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：...
  c18_b02.md: ...命中并涌向 source。缓解：概率性提前刷新；刷新期间 per-key 锁。  (b) **事故期间的缓存踩踏** ：truth source 变慢或宕机；所有缓存过期，所有读者击中慢 source，使其更慢。缓解：异常时提供过期数据（serve...
  c18_b02.md: ...常时提供过期数据（serve stale-on-error）。  (c) **失效竞争的过期数据** ：缓存被失效，但 truth source 写尚未提交；在失效与提交之间的一次读会用旧值刷新缓存。缓解：提交后再失效，失败时重试。  (d) **把...
  c18_b02.md: ...ce 写尚未提交；在失效与提交之间的一次读会用旧值刷新缓存。缓解：提交后再失效，失败时重试。  (d) **把缓存当作 truth source** ：开发者开始把缓存用于正确性；缓存无形中成为记录系统。缓存故障（Redis 崩溃、内存驱逐）后数据丢...
  c18_b02.md: ...读路径（账户余额查询、幂等键检查）使用 Redis Cluster。每个缓存条目有短 TTL（60 秒）；未命中时查询 truth source（CockroachDB）。事件驱动失效在每次提交时发布 “账户已变” 事件；缓存主动失效。   ###...

=== tokens (7 occurrences) ===
  c18_b03.md: ...早过期。两个客户端可能同时认为自己持有锁。  修复方案：_fencing token_。   #### Fencing tokens  Fencing token 是由锁服务分配的单调递增计数器。每次获得锁得到一个最新 token（比之前任何已发 to...
  c24_b07.md: ...。每个 GPU 步为每个请求处理一个 token。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 tokens。  (c) **可变请求长度优雅处理**。短请求（50 token）和长请...
  c24_b07.md: ...en。Batch size 256、每步 50 ms，系统每秒生成 256 tokens / 50 ms = 5120 tokens。  (c) **可变请求长度优雅处理**。短请求（50 token）和长请求（5000 token）可在同一 cont...
  c25_b02.md: ...K2 -->|"KV blocks per token"| D2         D1 -->|"streamed tokens"| LB         D2 -->|"streamed tokens"| LB         style Pre...
  c25_b02.md: ...D1 -->|"streamed tokens"| LB         D2 -->|"streamed tokens"| LB         style PrefillTier fill:#e6f3ff         style C...
  c30_b14.md: .../     │   ├── redlock_naive.py  — the wrong way (no fencing tokens) — for demonstration     │   ├── etcd_lock.py      — consen...
  c30_b14.md: ...ncing_token_demo.py — protected resource enforces monotonic tokens     ├── ch20_tracing/     │   └── propagation_demo.py — Ope...

=== stale (7 occurrences) ===
  c18_b04.md: ...: resumed after pause         C1->>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highest (101); reject         B...
  c19_b08.md: ...losed”（Consul 不可达时返回错误），每次服务调用在选举窗口内失败。  修复：  (a) **配置客户端使用 stale 读**：Consul 支持从任意副本 stale 读（`?stale=true`）；leader 不可达时客户端回退到...
  c19_b08.md: ...次服务调用在选举窗口内失败。  修复：  (a) **配置客户端使用 stale 读**：Consul 支持从任意副本 stale 读（`?stale=true`）；leader 不可达时客户端回退到 stale 读。权衡：故障转移期间数据略过期；对...
  c19_b08.md: ...读**：Consul 支持从任意副本 stale 读（`?stale=true`）；leader 不可达时客户端回退到 stale 读。权衡：故障转移期间数据略过期；对服务发现可接受。  (b) **客户端缓存带宽限期**：客户端本地缓存发现的服务注...
  c27_b03.md: ...do_critical_work(action_id, fencing_token):         # 存储层拒绝 stale token 的写         db.atomic_write(action_id, ..., max_token=...
  c27_b09.md: ...试下的重复扣款。 * 分区下的 split-brain。 * 领导者 failover 下的丢失写。 * 网络恢复后的 stale read。 * 负载下的级联故障。...
  c27_b11.md: ...付协调器，含朴素 RPC、无 timeout、wall-clock 排序、Redlock、无部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-reg...

=== kill (7 occurrences) ===
  c19_b01.md: ...19.1 发现问题  服务网格有若干实例。实例：  * 启动并变为可用。 * 监听特定端口。 * 有时崩溃或被编排器 kill。 * 在节点间迁移（容器移动）。 * 基于负载上下扩。  客户端服务需要调用目标服务的任意健康实例。客户端不控制目标部...
  c23_b01.md: ...实例的工具）约 2010 年引入。Netflix 的理由：实例反正会在生产环境中失败，不如在工程师能响应的工作时间故意 kill，使系统对不可避免故障更健壮。该学科传播开来；到 2026 年，每个成熟 SRE 组织都有某种混沌实践。   ### 2...
  c23_b05.md: ...*：不故意注入故障。团队从真实事故中了解故障模式。  (b) **Level 1（手动混沌）**：偶尔的即兴实验。团队 kill 一个节点看会发生什么。没有结构化纪律。  (c) **Level 2（Game days）**：scheduled 混...
  c23_b09.md: ...Monkey 两年了。他们没有发生重大中断。他们是否 “完成” 了混沌工程？  提示  Chaos Monkey 随机 kill 实例。它测试一种故障模式；系统可能还有许多其他。一个层面的持续混沌不等于完整学科。  **答案：** 没有。Chaos...
  c23_b09.md: ...aos Monkey 不测试的故障模式：  (a) **网络分区**：节点仍在运行但彼此隔离。Chaos Monkey kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos Monkey kill（...
  c23_b09.md: ...kill 节点（网络不受影响）。  (b) **慢节点 / 灰色故障**：节点仍活着但降级。Chaos Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机 kill 单个节点，不是...
  c23_b09.md: ...Monkey kill（二元的）而非降级。  (c) **区域故障**：整个区域离线。Chaos Monkey 随机 kill 单个节点，不是协调的区域事件。  (d) **级联依赖**：服务 X 故障导致服务 Y 问题。Chaos Monkey...

=== nodes (7 occurrences) ===
  c19_b02.md: ...andra 等）。ZooKeeper 提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：  (a) **更好的运维模型**...
  c19_b07.md: ...**Phase 1：清点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd loc...
  c19_b07.md: ...ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK persis...
  c19_b07.md: ...t zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watche...
  c28_b03.md: ...REDISE[Redis Cluster]             CRDBE[CockroachDB nodes]             STRATUSE[Stratus DB<br/>coordinator]...
  c28_b03.md: ...REDISW[Redis Cluster]             CRDBW[CockroachDB nodes]             KAFKAW[Kafka brokers]         end         subg...
  c28_b03.md: ...APIW2[API Service]             CRDBW2[CockroachDB nodes]         end         subgraph Shared["Shared Services"]...

=== feature (7 occurrences) ===
  c19_b03.md: ...发；服务接收新配置并重新配置（无需重启）。  (d) 对需要跨服务一致性的配置，配置存储使用共识。  示例：  * **Feature flags**：跨所有服务切换功能，无需重新部署。 * **Rate limits**：根据负载动态调整限速。 * *...
  c19_b06.md: ...示例：feature flags。配置存储保存如 `feature_flags/new_checkout_flow = true` 的 ke...
  c19_b06.md: ...配置存储保存如 `feature_flags/new_checkout_flow = true` 的 key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag（`etcdctl put...
  c19_b08.md: ...e)。30 秒故障转移导致的中断是运维痛点；实质降低它会明显改善用户体验。  **Q3.** 一个团队用 etcd 做 feature flags。flag `new_checkout_flow` 被切换为 `true`。走一遍每个服务会发生什么，以及可...
  c19_b09.md: ...e，执行中变 false）。缓解：服务在请求开始时快照 flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，...
  c19_b11.md: ...p 等），但缓慢衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul 中，watch 变更，动态重新配置）是...
  c23_b10.md: ...g) **内存压力 / GC 暂停**：节点活着但慢。专门故障。  (h) **配置变更**：部署、schema 变更、feature flag。本身不是故障，但与系统交互的运营事件。  Chaos Monkey 测试一个切片（“实例故障” 切片）。完整...

=== thread (7 occurrences) ===
  c22_b07.md: ...**锁竞争**：服务中偶尔把许多请求串行化的临界区。第一个请求因锁等待而耗时；其后排队的请求也耗时。诊断：尖刺期间的 thread dump；找许多线程阻塞在同一把锁上。  (c) **下游抖动**：下游调用偶尔耗时 30 秒（可能是其自身 GC、网...
  c26_b11.md: ...85%（从 60%）、每请求成本降低 40%、年节省 500-600 万美元。  第27章开启第九部分（综合），整合 Thread C：为什么 LLM 系统性地不擅长分布式代码。第1章的六个谎言将以实例回归；本章教授审查 AI 生成分布式代码的纪律。...
  c27_b01.md: ...ion 支付协调器。看起来正确。单元测试通过。集成测试通过。  她读了 30 行后拒绝了 PR。  前 30 行包含了 Thread C 的六个谎言。LLM 产出了看起来合理的分布式代码，但在生产中数周内就会重现第1章原始重复扣款失败。代码形状正确；设...
  c27_b01.md: ...M 产出了看起来合理的分布式代码，但在生产中数周内就会重现第1章原始重复扣款失败。代码形状正确；设计错误。  本章整合 Thread C。六个谎言被重述；给出每个的 LLM 生成示例；审查 AI 生成分布式代码的检查清单是本章贡献。  更深主张：分布式...
  c27_b01.md: ...：部分故障是例外。只有 happy path 和 error path 的代码把故障当作罕见；现实是部分故障是默认。  Thread C 的六个谎言，作为贯穿第1-2章的前瞻引用（冷钩子章节 2-4、18、21-22）并在此整合：    1. **RP...
  c27_b12.md: ...IZABLE)` 或类似，读可能来自有旧数据的 follower。  Olamide 的审查反馈：“拒绝。这段代码包含 Thread C 全部六个谎言。具体：bank_service.transfer 缺少 idempotency key（谎言 1）；...
  c27_b15.md: ...码质量显著改善；涉及六个谎言的新事故罕见。投资（审查时间、混沌测试、培训）巨大；事故成本更高。   ### 本章总结  Thread C 的 LLM 关于分布式系统的六个谎言：(1) RPC 有 TCP 式语义；(2) 网络可靠；(3) 机器时钟说真话...

=== anyscale (7 occurrences) ===
  c24_b02.md: ...发请求更多；每个请求成本更低。  vLLM 采用迅速。到 2026 年，它是最广泛部署的开源推理 serving 系统。Anyscale、Together、Fireworks 和许多其他推理公司使用 vLLM（或 vLLM 衍生系统）作为核心引擎。...
  c25_b11.md: ...k、预取、前缀缓存）使其接近 co-located 性能。  第26章结束第八部分，讲负载均衡和投机解码作为分布式协议。Anyscale/Together/Fireworks 路由问题；speculative decoding（draft model +...
  c26_b01.md: ...Web 请求宽得多（p99 30 秒正常）。差的路由决策对用户可见。  机会：考虑这些属性的路由决策可带来显著效率提升。Anyscale、Together、Fireworks 都发布过相关内容。...
  c26_b02.md: ...首 token 延迟降低（更少 prefill 工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约...
  c26_b06.md: ...布式协议的团队。  对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 we...
  c26_b08.md: ...Tobias：这正是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优化让 Continuum 能与 Anyscale、Together 等竞争。  **Q2.** 某团队推理集群尾部延迟高（p50=2 秒，p99=30 秒）。诊断并提出...
  c30_b11.md: ...层、投机解码）复合产生显著成本节省。 * **替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用 Anyscale / Together / Fireworks 等托管替代。...

=== together (7 occurrences) ===
  c24_b02.md: ...求成本更低。  vLLM 采用迅速。到 2026 年，它是最广泛部署的开源推理 serving 系统。Anyscale、Together、Fireworks 和许多其他推理公司使用 vLLM（或 vLLM 衍生系统）作为核心引擎。...
  c25_b11.md: ...）使其接近 co-located 性能。  第26章结束第八部分，讲负载均衡和投机解码作为分布式协议。Anyscale/Together/Fireworks 路由问题；speculative decoding（draft model + verify mo...
  c26_b01.md: ...（p99 30 秒正常）。差的路由决策对用户可见。  机会：考虑这些属性的路由决策可带来显著效率提升。Anyscale、Together、Fireworks 都发布过相关内容。...
  c26_b02.md: ...e routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **Together 的类似特性**：发布指标显示在高前缀复用工作负载（如聊天机器人 system prompt）上 2-3 倍吞吐。  对...
  c26_b06.md: ...对 Continuum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fa...
  c26_b08.md: ...是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优化让 Continuum 能与 Anyscale、Together 等竞争。  **Q2.** 某团队推理集群尾部延迟高（p50=2 秒，p99=30 秒）。诊断并提出修复。  提示...
  c30_b11.md: ...显著成本节省。 * **替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用 Anyscale / Together / Fireworks 等托管替代。...

=== static (7 occurrences) ===
  c24_b05.md: ...limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高...
  c24_b05.md: ...持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Contin...
  c24_b05.md: ...limit），它离开 batch；等待中的请求加入。 * Batch 持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高...
  c24_b05.md: ...持续被补充；GPU 保持高利用率。  经典替代：static batching。等 N 个请求；一起处理；等全部完成。Static batching per-request 延迟高（慢请求阻塞快请求），利用率低（batch 间空闲）。  Contin...
  c24_b07.md: ...和长请求（5000 token）可在同一 continuous batch 中运行。短请求 50 步后离开；长请求继续。Static batching 会让短请求等待长请求。  结果：相比朴素 serving 吞吐提升 2-5 倍。Continuous...
  c24_b08.md: ...**答案：** 三种技术：  (a) **Continuous batching**：若 Tobias 的集群使用 static batching，切换到 continuous batching 通常带来 2-3 倍吞吐提升。大多数现代引擎（vLL...
  c24_b11.md: ...Continuous batching 是吞吐模式：GPU 并行处理多个请求；请求持续加入和离开 batch；相比 static batching 提升 2-5 倍。现代引擎标准。  Tobias 的 Continuum 集群：50 节点、400...

=== moe (7 occurrences) ===
  c24_b08.md: ...peculative decoding（第26章）；模型量化（8-bit、4-bit serving 用于低精度模型）；MoE routing（mixture-of-experts 模型高效路由）。每种都复合。  更深观察：推理成本优化是 202...
  c25_b10.md: ...2024）精化调度；Mooncake 和 LMCache（2024-2025）添加缓存层；Mysticeti 等扩展到 MoE 模型。2026 年采用分离的团队应预期每 18 个月随新技术出现而重新架构。  **Q2.** 一个 LLM 被要求...
  c25_b11.md: ...处）。 * 用 task-specific fine-tunes（针对特定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-of-Experts）模型：总参数量很大，但每 token 只激活部分；成本低于 dense 大模型。...
  c26_b10.md: ...speculative decoding（multi-token speculation、EAGLE、Medusa）、MoE 专用优化（expert routing、expert parallelism）、更激进量化（NF4、FP4 servi...
  c29_b02.md: ...取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 * **MoE（Mixture-of-Experts）** 模型成为 >100B 参数模型的默认，需要 expert-parallel...
  c29_b02.md: ...处理。  心智模型（GPU 是单元；两阶段计算；KV cache 是中心状态；严格 per-token 延迟）可推广到 MoE，很可能也到扩散-LM。具体原语（PagedAttention、RadixAttention、disaggregatio...
  c30_b03.md: ...meter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基础，作为前瞻话题引用（§29.1；深度覆盖推迟到 _Software at Scale_ Series IV Vol...

=== distserve (7 occurrences) ===
  c25_b01.md: ...e，以及跨节点的 KV cache sharding。该技术在 2023-2024 年成为经典研究（Splitwise、DistServe）；生产部署在 2025-2026 年成熟。   ### 25.1 Prefill/Decode 不对称性  对单个 L...
  c25_b01.md: ...费巨大。  Microsoft Research 2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefill 完成后，KV cache 从 p...
  c25_b03.md: ...litwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistServe 数字：吞吐提升 2-4 倍。  成本：架构复杂度。两种节点类型；KV cache 传输逻辑；集群拓扑更复杂。对小集群，...
  c25_b10.md: ...是遗留方案。”  **讨论：** 辩护：分离在典型工作负载相同延迟下提供 2-4 倍吞吐提升。Splitwise 和 DistServe 论文很明确。成熟推理厂商（Anyscale、Together、Fireworks）已采用分离。对任何成本敏感部署，该技...
  c25_b10.md: ...28-2030 年分离可能成为所有规模的默认。  第二个考虑：技术快速演进。Splitwise（2023）引入基本思想；DistServe（2024）精化调度；Mooncake 和 LMCache（2024-2025）添加缓存层；Mysticeti 等扩展到...
  c25_b11.md: ...每个阶段使用最优硬件；相同延迟下吞吐提升 2-4 倍。  Splitwise（Microsoft Research）和 DistServe（UCSD）是经典研究；生产系统（Mooncake、LMCache）实现这些模式。采用阈值：约 50+ 节点集群，效率收...
  c30_b03.md: ...se Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024). "DistServe: Disaggregating Prefill and Decoding for Goodput-optimized...

=== splitwise (7 occurrences) ===
  c25_b01.md: ...到 50% 计算和 50% 内存带宽：从未饱和任一。浪费巨大。  Microsoft Research 2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段...
  c25_b03.md: ...可根据工作负载独立扩缩。对长 prompt 短输出的工作负载，多扩 prefill；对相反情况，多扩 decode。  Splitwise 公布数字：在相同延迟下，相比 co-located prefill/decode，成本效率提升 2 倍。DistSer...
  c25_b10.md: ...认。Co-located 是遗留方案。”  **讨论：** 辩护：分离在典型工作负载相同延迟下提供 2-4 倍吞吐提升。Splitwise 和 DistServe 论文很明确。成熟推理厂商（Anyscale、Together、Fireworks）已采用分离。...
  c25_b10.md: ...saggregation），阈值会降低；2028-2030 年分离可能成为所有规模的默认。  第二个考虑：技术快速演进。Splitwise（2023）引入基本思想；DistServe（2024）精化调度；Mooncake 和 LMCache（2024-202...
  c25_b11.md: ...mory-bound（矩阵-向量乘法）。分离到不同 GPU 让每个阶段使用最优硬件；相同延迟下吞吐提升 2-4 倍。  Splitwise（Microsoft Research）和 DistServe（UCSD）是经典研究；生产系统（Mooncake、LMC...
  c28_b13.md: ...曾是标准协调服务；现在是 etcd。2026 年分布式系统工程包括 Spanner、CockroachDB、vLLM、Splitwise、Mooncake——它们在该领域奠基工作时都不存在。  心智模型是持久的。理解分布式语境下时间、身份、故障的含义在 1...
  c30_b03.md: ...l Programs." — RadixAttention。 * Patel, P., et al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting."...

=== optimized (7 occurrences) ===
  c25_b02.md: ...er]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]             P1["Prefill Node 1<br/>4× H100"]...
  c25_b02.md: ...end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]             D1["Decode Node 1<br/>8× H200"]...
  c25_b02.md: ...│  Compute     │  │  Compute     │                 │  optimized   │  │  optimized   │                 └──────┬───────┘  └──...
  c25_b02.md: ...│  │  Compute     │                 │  optimized   │  │  optimized   │                 └──────┬───────┘  └──────┬───────┘...
  c25_b02.md: ...│  Memory BW   │  │  Memory BW   │                 │  optimized   │  │  optimized   │                 └──────────────┘  └──...
  c25_b02.md: ...│  │  Memory BW   │                 │  optimized   │  │  optimized   │                 └──────────────┘  └──────────────┘   Pr...
  c30_b03.md: ..."DistServe: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake:...

=== pilot (7 occurrences) ===
  c25_b04.md: ...跟随请求从 prefill 到 decode 到输出。  **实现时间线**：架构改动 6 个月。团队构建分离层；在小 pilot 集群测试；全集群 rollout。最终达到 80% 目标。  更深教训：2026 年推理成本优化是架构机会。这些模式是...
  c25_b08.md: ...的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2...
  c25_b08.md: ...out：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转...
  c25_b08.md: ...Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由...
  c25_b08.md: ...的集群 50 节点，混合工作负载。他正在规划分离迁移。走一遍 rollout 计划。  提示  分阶段 rollout：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2...
  c25_b08.md: ...out：pilot、逐步转换节点类型、可观测性、验证。  **答案：** Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转...
  c25_b08.md: ...Rollout 计划：  **Phase 1：Pilot（1-2 个月）**  * 从 50 节点中划出 10 个做 pilot。 * 4 个转 prefill 角色，4 个转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由...

=== fsync (6 occurrences) ===
  c01_b05.md: ...书焦点。  * **存储引擎内部细节** ：LSM 树压实调优、Bloom 过滤器大小、B+ 树写放大优化、页缓存行为、fsync 顺序。我们会在影响分布式推理的地方触及这些（第12章），但不会展开单节点深度。参考：Kleppmann 的 _DDIA...
  c08_b04.md: ...快照必须与日志该点状态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partitio...
  c08_b04.md: ...态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling...
  c08_b07.md: ...小时。  **Q4.** 为什么 Paxos 以难正确实现著称？说出三个运维关注点。  提示  持久状态、重配置、快照、fsync 纪律、Multi-Paxos 优化。  **答案：** Paxos 难实现有多个运维原因：  (a) **持久状态纪律...
  c08_b07.md: ...d_ballot`、`accepted_value` 持久化到可靠存储。若 acceptor 丢失该状态（磁盘故障、漏 fsync、文件系统损坏），它会“忘记”先前 promise 并接受冲突值，违反 safety。  (b) **重配置** ：改变...
  c08_b10.md: ...value`）必须在回复 prepare 或 accept 消息 _之前_ 持久化。LLM 生成的代码常用内存状态，无 fsync、无 WAL、无恢复。崩溃后 acceptor “忘记”先前 promise 并可接受冲突值，违反 safety。单进程...

=== bucket (6 occurrences) ===
  c02_b10.md: ...远高于非 LWT 写入（常 4 到 10 倍），因为每次写入都发生 Paxos 往返。  对 Riak：启用后的强一致 bucket 是线性化的。成本：每次操作支付一次协调往返；吞吐比最终一致 bucket 低一个数量级。  教训：每个“AP”系统都有...
  c02_b10.md: ...os 往返。  对 Riak：启用后的强一致 bucket 是线性化的。成本：每次操作支付一次协调往返；吞吐比最终一致 bucket 低一个数量级。  教训：每个“AP”系统都有一个 CP 角落。知道哪些操作落在哪个角落，比系统级标签更有用。  **Q...
  c17_b02.md: ...Automerge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT bucket types 都是生产示例。  强最终某种意义上 _弱于_ 因果一致（不保证收敛过程中所有副本按因果顺序看到操作），但某...
  c17_b08.md: ...的方法干净地处理购物车工作负载：并发添加作为 sibling 保留；应用的合并函数取并集。  _Riak 的 CRDT bucket types_（较新的 Riak 特性）：购物车可建模为 OR-Set CRDT。Riak 自动处理合并；无需应用逻辑合...
  c17_b08.md: ...数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set 语义匹配购物车的 “物品并集” 行为。并发添加保留；并发移除被 tombstone；最终购物...
  c26_b06.md: ...器也确保客户 2 和 3 得到 GPU 时间。没有客户被另一个客户的突发饿死。  实现模式：  (a) **Token bucket**：每客户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (...

=== nosql (6 occurrences) ===
  c02_b13.md: ...tgres 工具生态系统使兼容 Postgres 的系统（Aurora DSQL、CockroachDB、Neon）比 NoSQL 替代更便宜地采用。...
  c14_b01.md: ...**传统 SQL** ：PostgreSQL、MySQL、Oracle。ACID，单节点或主从复制，纵向扩展。 * **NoSQL** ：Cassandra、DynamoDB、MongoDB。水平扩展，但历史上放弃 ACID 或使用非 SQL API...
  c14_b07.md: ...#### 概念检测  **Q1.** 什么是 “NewSQL”？它与传统 SQL 和 NoSQL 有何不同？  提示  NewSQL：SQL + ACID + 水平扩展。传统 SQL：单节点或少数节点。NoSQL：水...
  c14_b07.md: ...和 NoSQL 有何不同？  提示  NewSQL：SQL + ACID + 水平扩展。传统 SQL：单节点或少数节点。NoSQL：水平扩展但历史上放弃 ACID。  **答案：** NewSQL 是在水平扩展的同时保留 SQL 和 ACID 事务的...
  c14_b07.md: ...**（PostgreSQL、MySQL、Oracle）：ACID、完整 SQL，但单节点或主从架构，纵向扩展。 * **NoSQL**（Cassandra、DynamoDB、MongoDB）：水平扩展，但历史上放弃部分 ACID、SQL 或事务一致性...
  c17_b09.md: ...更深观察：购物车是经典 CRDT 用例。早期 NoSQL 系统（Riak、Cassandra）尝试过不同方法；现代结论是该类工作负载的正确原语是 CRDT。...

=== ordering (6 occurrences) ===
  c03_b02.md: ...ns-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distributed System"（1978），必读。关键洞见是：给分布式系统中的事...
  c03_b08.md: ...### 3.7 综合：分布式数据库中的排序  现代分布式数据库对事务排序使用三种模式之一：  System | Ordering primitive | What it costs ---|---|--- Spanner | TrueTime +...
  c04_b06.md: ..._任何顺序_（数据结构无论如何都会收敛）。因此 CRDT 是只需要可靠广播的 agreement 性质、不需要原子广播 ordering 性质的应用示例。成本节约是真实的：基于 CRDT 的计数器能达到 Raft 复制计数器无法企及的吞吐。   ### 4...
  c04_b09.md: ...t-once 意味着消费者可能看到重复。  **答案：** 代理在 topic 分区内提供原子广播（per-topic ordering）。跨 topic 或分区只提供应用强制出的性质。at-least-once 交付意味着重试情况下每个消费者可能多次收到...
  c04_b09.md: ...等性会导致双重处理。  第1章的 Crescent 支付系统正是这种模式：Kafka 提供 per-partition ordering，应用必须按 partition key（通常是 transaction ID）幂等。重复扣款 bug 是因为幂等检查没...
  c30_b01.md: .... — PACELC 论文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System." _Communications of the...

=== process (6 occurrences) ===
  c03_b03.md: ...#### 工作示例  三个进程 P_1, P_2, P_3。事件序列：  Step | Process | Event | Local counter after |---|---|---|--- 1 | P_1 | lo...
  c03_b03.md: ...b) < V(a) 都不成立，则事件并发。   #### 工作示例  三个进程，与之前相同的事件序列：  Step | Process | Event | Vector after |---|---|---|--- 1 | P_1 | a | [1,0,...
  c03_b06.md: ...？”   #### 工作示例  三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 | 100 | (100...
  c27_b11.md: ...sult(status='failed', reason='insufficient')              # Process across services             bank_result = bank_service.tran...
  c27_b11.md: ...sult(status='failed', reason='insufficient')              # Process across services             bank_result = bank_service.tran...
  c30_b02.md: ...5). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Lynch, N.,...

=== sloppy (6 occurrences) ===
  c05_b03.md: ...至少共享一个节点，因此读能看到写 _当且仅当_ 写已在该节点完成提交 _且_ 没有冲突要解决。  失败情况：  * **Sloppy quorum** ：分区下，写法定人数可能由不在目标副本集中的节点满足（Dynamo 的 “hinted handof...
  c05_b06.md: ...它，未命中的读看不到。不同并发读者看到不同状态。线性一致性要求所有读者按实时顺序要么都看见写要么都不见。  (b) **Sloppy quorum** ：主副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本...
  c11_b01.md: ...？** 线性化、可串行化、快照、因果、最终、按操作可调。   4. **分区行为是什么？** 少数侧拒绝写、提供过期读、sloppy quorum、应用控制。  具体回答这四个问题，就能得到该系统的 PACELC 画像、运维特征和失败模式。任何问题 3...
  c11_b03.md: ...下的 Cassandra、默认一致性下的 DynamoDB。写可能冲突；冲突解决（LWW、CRDT）决定结果。 * **Sloppy quorum**（Dynamo 特有行为）：主副本不可达时，写发往任何可达节点（hinted handoff）；读从任...
  c28_b10.md: ...延迟（可配置权衡） | 每次写入的 quorum 成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy quorum | | 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求 → leader → fo...
  c30_b16.md: ...| Ch.4, Ch.27 | | 9 | "W + R > N 给出线性化。" | 它给出 _交集_，更弱。并发写和 sloppy quorum 破坏线性化。 | Ch.5 | | 10 | "多 region HA 却不付多 region 税。"...

=== values (6 occurrences) ===
  c05_b03.md: ...基于 wall-clock 时间戳。Cassandra 的经典默认。受第3章时钟偏移问题影响。 * **Sibling values** 保留到应用解决。Riak 的方法。应用看到所有冲突值并选择（或合并）。 * **CRDT**（第16章）。冲突由数...
  c15_b02.md: ...** ：时间戳最高的写胜出。简单，但受第3章时钟偏移问题影响。Cassandra 默认。  (b) **Sibling values** ：系统保留所有冲突值；应用看到它们并选择（或合并）。Riak 的做法。需要应用逻辑，但从不静默丢失数据。  (c)...
  c15_b02.md: ...确策略取决于工作负载：  * 应用有明确 “赢家” 语义的数据：LWW。 * 应用能推理所有候选的数据：sibling values。 * 具有数学结构的数据（计数器、集合、寄存器）：CRDT。  LWW 是最常见默认因为它最简单。代价是时钟偏移问题：...
  c15_b07.md: ...资料字段最后更新），LWW 没问题。对 “丢失” 不可接受的数据（如交易日志），LWW 错误；应用需要 sibling values 或 CRDT。...
  c17_b08.md: ...）与 Cassandra（LWW）。分析。  提示  购物车：不同设备并发更新；合并物品有意义（并集）。Sibling values 让应用合并；LWW 会丢失物品。  **答案：** 购物车特性：  * 用户可从多个设备（手机、笔记本）并发添加商品。...
  c17_b08.md: ...Riak 自动处理合并；无需应用逻辑合并 sibling。这是现代路径：对需要的数据类型使用 CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set 语义匹配购物...

=== lsn (6 occurrences) ===
  c05_b04.md: ...法享受读副本扩展。  (b) **等写传播完成再确认。** 同步复制路径。代价：写延迟。  (c) **读请求带“最小 LSN”。** 客户端知道刚写入记录的 LSN 为 12345，后续读包含 “LSN >= 12345”。落后的 follow...
  c05_b04.md: ...播完成再确认。** 同步复制路径。代价：写延迟。  (c) **读请求带“最小 LSN”。** 客户端知道刚写入记录的 LSN 为 12345，后续读包含 “LSN >= 12345”。落后的 follower 要么等待要么拒绝。代价：客户端跟踪...
  c05_b04.md: ...：写延迟。  (c) **读请求带“最小 LSN”。** 客户端知道刚写入记录的 LSN 为 12345，后续读包含 “LSN >= 12345”。落后的 follower 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB...
  c05_b04.md: ...为 12345，后续读包含 “LSN >= 12345”。落后的 follower 要么等待要么拒绝。代价：客户端跟踪 LSN。  CockroachDB 和 YugabyteDB 通过 per-transaction HLC 时间戳使用 (c)...
  c05_b04.md: ...只适用于单个会话的视图。  大多数现代数据库提供其中子集，常可配置。代价是跟踪成本：per-session HLC 或 LSN 必须存客户端或服务器，并在每次读时检查。...
  c05_b09.md: ...IF` 子句强制 Paxos 轮，是线性化的。代价：受影响操作延迟 4-10 倍。  (b) **用 HLC 做客户端 LSN 跟踪** ：跟踪写的时间戳；同一会话的后续读拒绝返回早于写时间戳的数据。Cassandra 原生不支持；应用层封装可实...

=== prefix (6 occurrences) ===
  c05_b04.md: ...在 X 之后。 * **Monotonic writes** ：你的写按你发出顺序应用。 * **Consistent prefix** ：你看到全局写顺序的一个前缀，即使没看最新写。  这些保证是 _per-session_ 而非全局。两个会话可以看...
  c15_b01.md: ...。  (d) **Monotonic writes** ：会话的写按发出顺序应用。  (e) **Consistent prefix** ：即使看不到最新，也能看到全局写顺序的一个前缀。  这些保证是 _per-session_，不是全局。两个会话可以...
  c15_b06.md: ...tonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-your-writes_ ：会话的读能看到先前写。重要场景：用户期望自己的动作立即反...
  c26_b02.md: ...低（更少 prefill 工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。...
  c26_b08.md: ...请求 prompt 短（可能 50 token）；输出中等（200-500 token）。  路由优化：  (a) **Prefix-aware 路由（KV cache locality）**：负载均衡器识别 5000-token system pro...
  c29_b02.md: .../B200 及下一代 Blackwell + Hopper 继任者）驱动。 * 通过更激进的 KV cache 预取和 prefix 共享，**first-token latency（TTFT）** 降到 50 ms 以下。 * **MoE（Mixtu...

=== any (6 occurrences) ===
  c06_b03.md: ...ine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间...
  c06_b03.md: ...**Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisec...
  c06_b03.md: ...ine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间...
  c06_b03.md: ...**Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisec...
  c06_b12.md: ...istent）成本画像不同；选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工作负载有用。对大多数应用层服务...
  c06_b12.md: ...成本画像不同；选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工作负载有用。对大多数应用层服务，网络不是瓶颈...

=== bypass (6 occurrences) ===
  c06_b03.md: ...DMA；较少见，但因容忍有损网络而更容易部署。  性能：RDMA 延迟约 1-2 微秒（同数据中心），而 kernel-bypass TCP 约 50-100 微秒。内核不参与其中；NIC 处理数据移动。  用途：  * **分布式内存数据库**（Mi...
  c06_b06.md: ...迟收益在亚毫秒级有意义。应用服务延迟预算大得多。  **答案：** RDMA 把网络延迟从 ~50 微秒（kernel-bypass TCP）降到 ~1-2 微秒（RoCE 上的 RDMA）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算...
  c06_b06.md: ...训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫秒级或更高的通用应用服务，kernel-bypass + TCP 已足够。...
  c06_b07.md: ...**答案：** RDMA 把网络延迟从 ~50 微秒（kernel-bypass TCP）降到 ~1-2 微秒（RDMA over RoCE）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5%...
  c06_b07.md: ...训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫秒级或更高的通用应用服务，kernel-bypass + TCP 已足够。...
  c06_b12.md: ...把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工作负载有用。对大多数应用层服务，网络不是瓶颈；kernel-bypass TCP 足够。  延迟预算方法（先定目标、减固定成本、把余量分配到每跳）能在部署前发现不可行设计。Crescent 团...

=== central (6 occurrences) ===
  c06_b08.md: ...到其他两个区域中较快者的往返。系统级最坏情况是所有可能发起区域中这个延迟的最大值。  聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-W...
  c06_b08.md: ...能发起区域中这个延迟的最大值。  聚类三区域：US-East、US-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：...
  c06_b08.md: ...S-Central、US-West。成对往返：East-Central ~30 ms，East-West ~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。...
  c06_b08.md: ...~80 ms，Central-West ~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。 * US-Central 发起：最快 follower = US-East，30 ms。 * US-We...
  c06_b08.md: ...~50 ms。  * US-East 发起：最快 follower = US-Central，30 ms。 * US-Central 发起：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower =...
  c06_b08.md: ...：最快 follower = US-East，30 ms。 * US-West 发起：最快 follower = US-Central，50 ms。...

=== out (6 occurrences) ===
  c06_b09.md: ...跳完全串行**：800 / 7 ≈ 115 ms p99 每跳。可行但紧；没有余量。  **情况 2——并行 fan-out**：如果服务网格允许 7 跳中 4 跳作为一批并行（fan-out 模式），其余 3 跳串行：  * 并行批延迟 =...
  c06_b09.md: ...+ 并行批：800 - 115 = 685 ms 分给 3 串行；每跳 ~228 ms p99。  因此加一次 fan-out 后，单个 hop 有 ~228 ms p99，舒服得多。并行批“买回”预算给串行路径。  **情况 3——2 批各 2...
  c06_b09.md: ...要。** 服务网格图常包含历史原因存在的 hop；实际数据依赖可能只有 4-5 个。  (b) **识别可并行 fan-out。** 互不依赖的 hop（如同时取用户资料、检查认证、加载定价）应并行。...
  c19_b09.md: ...checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；受网络限...
  c19_b09.md: ...1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服务可能看...
  c25_b08.md: ...ck 计划：指标回退则回到 co-located。 * 客户沟通：通知大客户架构变化；为需要稳定延迟的客户提供 opt-out。 * 工程投入：2 名高级工程师全职做分离层 6 个月。  总投入：约 50 万美元工程时间换取约 400-500 万...

=== ousterhout (6 occurrences) ===
  c07_b11.md: ...是生产部署（Spanner、Megastore）的真实形式。第9章讲 Raft，Diego Ongaro 和 John Ousterhout 2014 年明确为比 Paxos 更易理解而设计。  ---...
  c08_b10.md: ...s 已过时。Raft 同样好、更简单，每个新系统都应该用它。”  **讨论：** 辩护：Raft 由 Ongaro 和 Ousterhout 明确设计为比 Paxos 更易理解，同时提供相同 safety 保证。大多数新分布式系统（etcd、Consul、Ti...
  c08_b11.md: ...ilable System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ousterhout 2014 年为比 Paxos 更易理解而设计。Raft 使用相同骨架（法定人数 + leader + 持久状态），但教...
  c09_b01.md: ...里装不下这些。一定有更好的办法。”  她的技术 lead 把 Raft 论文递给她。  Raft 由 Ongaro 和 Ousterhout 设计，2014 年论文题为 “In Search of an Understandable Consensus Alg...
  c30_b02.md: ...os Made Simple." _ACM SIGACT News_. — 必读改写。 * Ongaro, D., & Ousterhout, J. (2014). "In Search of an Understandable Consensus Algor...
  c30_b06.md: ...uorum 交集）**：N 个节点中任意两个多数派至少共享一个节点；共识安全基础。 * **Raft**：Ongaro-Ousterhout 2014 共识协议。分解为 leader 选举、日志复制、安全性。 * **Retry budget（重试预算）**：...

=== prev (6 occurrences) ===
  c08_b06.md: ...，该值可能已被选定；新 proposer 不能覆盖它。  **答案：** 假设先前 ballot 已部分接受值 $v_{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。从系统视角，$v_{prev}$...
  c08_b06.md: ..._{prev}$：某些 acceptor 已接受，但 proposer 在知道是否多数已接受前崩溃。从系统视角，$v_{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少）。  若新 proposer 忽略 $v_{prev}$...
  c08_b06.md: ..._{prev}$ 可能已被选定（若多数恰好已接受）也可能没有（若接受者更少）。  若新 proposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$...
  c08_b06.md: ...oposer 忽略 $v_{prev}$ 并提议自己的 $v_{new}$，且协议选定 $v_{new}$，则 $v_{prev}$ 和 $v_{new}$ 可能被不同观察者视为都已选定，违反 agreement。  Accept 规则（propo...
  c08_b06.md: ...飞行中”，可能已选定。新 ballot 必须采纳它以避免 safety 违反。  新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任何未来 ballot 都会看到 $v_{prev}$ 并提议它。系统收敛到单一选定值。...
  c08_b06.md: ...新 ballot 第二阶段用 $v_{prev}$ 完成后，值被明确选定。任何未来 ballot 都会看到 $v_{prev}$ 并提议它。系统收敛到单一选定值。  **Q3.** 什么是 Multi-Paxos？它如何比单法令 Paxos 更...

=== hashicorp (6 occurrences) ===
  c09_b05.md: ...识库广阔。  生产用户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxo...
  c19_b02.md: ...ooKeeper 迁移到 KRaft（Kafka Raft）是近期大趋势的例子。   ### 19.3 Consul  HashiCorp 的 Consul 是另一个流行的服务发现系统。与 etcd 的关键差异：  (a) **原生多 DC**：Consul...
  c19_b10.md: ...法使用 Kubernetes Services；需要不同机制。  EC2 的正确模式：  (a) **Consul**：HashiCorp 的 Consul 为 VM 和混合部署设计。EC2 实例启动时通过 Consul agent 注册；客户端通过 DNS...
  c19_b10.md: ...仍在用。  对 2026 年的新 EC2 部署：Consul 或 Cloud Map。选择取决于团队偏好（Consul：HashiCorp 生态、多云；Cloud Map：AWS 原生、更简单）。  更深观察：LLM 没问部署目标。“实现服务发现” 这类请求...
  c30_b08.md: ...sul 版本，带有微妙行为变化（更长 heartbeat 给 Consul 集群带来负载）。 * **放大**：集群的 HashiCorp Consul 服务发现层过载；CPU 饱和导致逐渐 streaming 失败；streaming 失败导致队列积压；积...
  c30_b08.md: ...小时客户可见中断（Roblox 史上最大）；数百万用户受影响。 * **恢复**：需要多厂商事件响应（Roblox + HashiCorp）；最终 BoltDB 需要从快照恢复，streaming 子系统从干净状态重建。 * **章节映射防御**：...

=== mysticeti (6 occurrences) ===
  c10_b01.md: ...HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]         HotStuff -->...
  c10_b01.md: ...ff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Na...
  c10_b03.md: ...mint 以较低吞吐换取更简单实现；Narwhal-Bullshark 将基于 DAG 的 mempool 与共识分离；Mysticeti 进一步优化吞吐。训练技能：跟踪提议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发...
  c10_b03.md: ...s 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff 思想）；Mysticeti（2024 年 Sui 的高吞吐共识）扩展 HotStuff 并基于 DAG mempool。   #### 常见误解...
  c10_b04.md: ...识配合分离 mempool。 * **Sui** 在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff。  Narwhal-Bullshark...
  c25_b10.md: ...基本思想；DistServe（2024）精化调度；Mooncake 和 LMCache（2024-2025）添加缓存层；Mysticeti 等扩展到 MoE 模型。2026 年采用分离的团队应预期每 18 个月随新技术出现而重新架构。  **Q2.** 一个...

=== howard (6 occurrences) ===
  c10_b05.md: ...### 10.6 Flexible Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisited” 推广...
  c10_b05.md: ...数要求。经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept 法定人数 $Q_2$ 都是多数。Howard 的洞见：对 safety 只需要 $Q_1 \cap Q_2 \ne \emptyset$。  这意味着：$Q_1$...
  c10_b08.md: ...w-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi Howard 的 Flexible Paxos 推广了法定人数要求。陈述该推广并给出一个实践应用示例。  提示  只需要 $Q_1...
  c10_b08.md: ...的多数。这确保 $Q_1 \cap Q_2 \ne \emptyset$（任意两个多数相交）以保证 safety。  Howard 的推广：只需要 $Q_1 \cap Q_2 \ne \emptyset$。只要两个法定人数相交，两个法定人数可以大小不...
  c10_b12.md: ...通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 prepare 与 accept 法定人数相交，而非各为...
  c30_b02.md: ...rstandable Consensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos:...

=== sdk (6 occurrences) ===
  c10_b09.md: ...HotStuff 比直接拿库需要更多工程。  (c) **Tendermint** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于 Hot...
  c10_b09.md: ...tStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的数千）。  推荐：通过 Cosmos SDK 使用 Tendermint。理由：  * 成熟库和生态。 * 对该用例吞吐足够。 * 运维工具和可观测性发展良好。 *...
  c20_b03.md: ...：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeline 细节（Series...
  c20_b09.md: ...。  **Q2.** 一个 LLM 被要求 “给我们的系统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的失败：产...
  c20_b09.md: ...（Honeycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetry，然后...
  c20_b09.md: ...练数据中过度代表特定产品；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 v...

=== profile (6 occurrences) ===
  c11_b09.md: ...--- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 o...
  c24_b06.md: ...mpt，compute-bound）和 decode（生成 token，memory-bound）有不同最优硬件/软件 profile。独立优化每个阶段是 2025-2026 架构突破。  (c) **KV cache 作为中心状态**。KV cache...
  c24_b06.md: ...突发工作负载模式**。请求从单次问答到大文档分析不等。集群必须处理混合而无线头阻塞。微服务通常 per-request profile 更均匀。  这些差异证明把分布式 LLM 推理视为独立子学科。协议（PagedAttention、RadixAtten...
  c25_b08.md: ...剩余节点。 * 最终拓扑：30 prefill + 30 decode + 10 cache（或根据前期测得的工作负载 profile 类似调整）。 * 退役 co-located 路径。  **Phase 4：调优与优化（7+ 个月）**  * 根据观...
  c25_b08.md: ...balance。  **答案：** 有了分离，prefill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile：  * 长 prompt 时段：多扩 prefill（compute-bound）并少扩 decode（memory-...
  c28_b07.md: ...计。诊断方法（第11章）是我们向 Crescent 团队推销集成的方式；他们对 Stratus 运行了该方法，验证了其 profile 匹配需求。”  **Yuki（从 Glide 借调）**：“商户入驻合同编辑器使用 Yjs。协作编辑 ‘刚好工作’，因...

=== leveled (6 occurrences) ===
  c12_b03.md: ...** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Siz...
  c12_b03.md: ...lDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 =...
  c12_b03.md: ...更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 = leveled。...
  c12_b03.md: ...** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Siz...
  c12_b03.md: ...lDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 =...
  c12_b03.md: ...更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 = leveled。...

=== tenant (6 occurrences) ===
  c12_b08.md: ...SLA 时节流。  **Q2.** 一个团队为多租户 SaaS 实现数据库。每个租户有自己的行集合。他们在 (a) 按 tenant ID 哈希分片，(b) 按 (tenant ID, row ID) 范围分片，(c) 每租户一个数据库之间选择。分析权...
  c12_b08.md: ...易。按 (租户, 行) 范围：租户数据本地有序。每租户一库：最大隔离，运维成本高。  **答案：**  (a) **按 tenant ID 哈希分片。** 每个租户数据在一个分区（可能一个节点）。优点：per-tenant 查询是单分区；租户数据有局部...
  c12_b08.md: ...答案：**  (a) **按 tenant ID 哈希分片。** 每个租户数据在一个分区（可能一个节点）。优点：per-tenant 查询是单分区；租户数据有局部性；重分片可原子移动租户数据。缺点：大租户可能占满一个分区（热租户问题）；系统不能把单个租...
  c12_b08.md: ...ID) 范围分片。** 租户按序排列；每个租户内部行有序。优点：大租户数据可跨多个 range（太大时分裂）；per-tenant 范围扫描高效；小租户共置于同一 range。缺点：小租户相邻意味着热小租户会影响邻居；range 元数据更复杂。  (...
  c12_b08.md: ...个数据库。** 每个租户有自己的数据库（或 schema、namespace、project）。优点：最大隔离；per-tenant 资源控制；删除租户 trivial。缺点：运维成本随租户数增长（管理成千上万个数据库）；跨租户聚合很难；per-ten...
  c12_b08.md: ...ant 资源控制；删除租户 trivial。缺点：运维成本随租户数增长（管理成千上万个数据库）；跨租户聚合很难；per-tenant 开销（连接池、schema 版本）。  建议：  * 10 到 100 个租户：选项 (c) 运维可管理，隔离最好。...

=== sqlite (6 occurrences) ===
  c12_b09.md: ...**Q3.** 一个团队为键值存储选择嵌入式存储：RocksDB（LSM）与 SQLite（B+ tree）。工作负载：80% 写、20% 读、随机访问、SSD 上 100 GB 总数据集。比较并推荐。  提示...
  c12_b09.md: ...ompaction 前多个 SSTable）。 * Compaction 开销：周期后台 CPU 和 I/O 突增。  SQLite 画像：  * 写吞吐：中等。每次写需更新 B+ tree 页；随机更新需页级 I/O。 * 读延迟：非常低（一次点查读...
  c12_b09.md: ...；随机更新需页级 I/O。 * 读延迟：非常低（一次点查读一页）。 * 空间放大：~1 倍（原地更新）。 * 运维简单：SQLite 是最好运维的嵌入式数据库。  推荐：该工作负载用 RocksDB。80% 写占比意味着 RocksDB 的写吞吐优势占...
  c12_b09.md: ...读占比在 RocksDB 中配合 Bloom filter 做点查仍可接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正确选择。SQLite “开箱即用”是真实优势；RocksDB 需要调优（compaction 策略、writ...
  c12_b09.md: ...Bloom filter 做点查仍可接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正确选择。SQLite “开箱即用”是真实优势；RocksDB 需要调优（compaction 策略、write-buffer 大小、bloc...
  c12_b09.md: ...write-buffer 大小、block cache 大小），小团队可能不愿投入。对增长缓慢的 100 GB 数据集，SQLite 较慢的写路径可能可容忍。...

=== keys (6 occurrences) ===
  c13_b03.md: ...” 列写入新值，并标记 primary key。   3.（primary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit...
  c13_b03.md: ...记录（value, commit_timestamp）并清除锁。  若该原子步骤成功，事务即提交。Secondary keys 的锁指向 primary；任何遇到 secondary 锁的读者会沿指针找到 primary，从而知道事务是否提交。...
  c19_b02.md: ...末起源于 Yahoo；用于 Hadoop、Kafka、Cassandra 等）。ZooKeeper 提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替...
  c19_b07.md: ...点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lea...
  c19_b07.md: ...vision number）。 * 配置：ZK persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lea...
  c19_b07.md: ...rsistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK A...

=== backed (6 occurrences) ===
  c13_b05.md: ...用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未来几年可能看到 CockroachDB /...
  c27_b03.md: ...能都相信自己持有锁；两者都调用 `do_critical_work`；关键工作被污染。  修复：用 consensus-backed 锁（etcd、Consul）并在被保护资源上加 fencing token。       def process_un...
  c27_b05.md: ...()` 做跨机器排序。用 HLC 或因果排序。  (f) **锁**：每个分布式锁对正确性场景应是 consensus-backed 加 fencing token。  (g) **部分故障处理**：每个多步操作应处理部分失败（saga、outbox、...
  c27_b10.md: ...或因果排序。`time.Now()` 是错的。  (f) **锁**：分布式锁对正确性关键场景应是 consensus-backed（etcd 等）加 fencing token。  (g) **部分故障处理**：多步操作应处理部分失败（saga、ou...
  c27_b15.md: ...清单（10 项）：幂等性、超时、带预算重试、熔断器、排序原语（HLC 而非 wall clock）、consensus-backed 锁加 fencing token、部分故障处理（saga/outbox）、显式一致性选择、trace context...
  c30_b14.md: ...for demonstration     │   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo...

=== choreographed (6 occurrences) ===
  c13_b06.md: ...步，失败时触发补偿。Orchestrator 状态本身持久化（通常 Raft 复制或存在事务型 DB）。  (b) **Choreographed saga** ：每一步完成时发布事件；后续步骤订阅事件。补偿通过 “回滚事件” 触发。无中央 orchestrator...
  c13_b06.md: ...S）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga 分布协调逻辑，但端到端调试更困难。...
  c13_b07.md: ...步，最后动作 LedgerService.record，状态：in_progress。” 完整生命周期就是一行。  **Choreographed** —— 典型电商订单工作流（无中央 orchestrator）：       OrderService...
  c30_b06.md: ...意排序。 * **Cascading failure（级联故障）**：通过放大（如重试风暴）跨组件传播的故障。 * **Choreographed saga（编排式 saga）**：每一步发布完成事件；后续步骤订阅；无中央协调器。补偿通过 “rollback 事件”...
  c30_b14.md: ...— orchestrated saga with durable state (SQLite)     │   ├── choreographed.py  — choreographed saga via in-memory event bus     │   └─...
  c30_b14.md: ...with durable state (SQLite)     │   ├── choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_s...

=== record (6 occurrences) ===
  c13_b07.md: ...ansaction)      → bank_ref       step 4: call LedgerService.record(transaction, bank_ref)       step 5: call NotificationServi...
  c13_b07.md: ...每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步，最后动作 LedgerService.record，状态：in_progress。” 完整生命周期就是一行。  **Choreographed** —— 典型电商订单工作...
  c13_b07.md: ...: submit settlement         B-->>O: bank_ref         O->>L: record (transaction, bank_ref)         L-->>O: ledger_id         O...
  c14_b02.md: ...Postgres API 外还提供 Cassandra 兼容 API（YCQL）。  劣势：  * 生产 track record 比 CockroachDB 小。 * 多区域配置复杂（与 CockroachDB 类似）。  YugabyteDB 和...
  c20_b08.md: ...y.propagate import extract      headers = {k: v for k, v in record.headers}     context = extract(headers)     with tracer.sta...
  c27_b11.md: ...nt.user_id, "Payment processed")             ledger_service.record(payment.id, time.time())              return PaymentResult(...

=== test (6 occurrences) ===
  c14_b03.md: ...立服务（Pageserver），支持快照和时间旅行。 * 单主（每个数据库同时只有一个写入者）。 * 分支（为 dev/test 即时克隆数据库）。  优势：  * Serverless：适合负载可变。 * 分支让 dev/test 很便宜（每个分...
  c14_b03.md: ...支（为 dev/test 即时克隆数据库）。  优势：  * Serverless：适合负载可变。 * 分支让 dev/test 很便宜（每个分支是存储的写时复制）。 * 真正 Postgres（非克隆）；所有扩展可用。  劣势：  * 单主；写不...
  c14_b03.md: ...。  劣势：  * 单主；写不能水平扩展。 * 较新；某些运维细节仍在打磨。  Neon 适合：负载可变（开发、dev/test 环境、低容量 SaaS）并从 serverless 计费受益。不适合大规模高吞吐生产 OLTP。   ### 14.8...
  c21_b02.md: ...contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b)...
  c27_b06.md: ...Redlock 反模式）。 * **测试覆盖提示**：新的分布式代码函数没有相关测试文件；关键路径 mutation-test 分数低。  这些是可分解为 “在 diff 中找这个正则” 的检查清单项。AI 审查者几秒处理。作为 pre-PR 检...
  c27_b07.md: ...未演练的防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合...

=== checkpointing (6 occurrences) ===
  c15_b03.md: ...刻就是系统状态，尽管没有命名某个具体时刻。   #### 2026 年重要性  Chandy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport...
  c15_b03.md: ...handy-Lamport 是分布式 checkpointing 系统的基础：  * **Apache Flink 的 checkpointing** ：Chandy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “check...
  c15_b03.md: ...基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD lineage、MapReduce 的 checkpointing、各种 ML 训练系统使用基于快照的重启。  该协议已有 40 多年历史，仍是思考一致分布式状态的标准方式。任何从事流处...
  c15_b07.md: ...具体何时（实际上它可能不对应任何单一 wall-clock 时刻），但快照是系统的一个 _合法_ 状态。  这是分布式 checkpointing 的基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体...
  c15_b08.md: ...once” 保证（从 Kafka source 到 Postgres sink）。Chandy-Lamport 风格的 checkpointing 如何使之可能？  提示  Checkpoint 捕获一致状态；故障时从上一个 checkpoint 重启。Sink 必...
  c15_b11.md: ...态加上在途消息形成一个可能真实发生过的状态。这是 Apache Flink exactly-once 语义和类似分布式 checkpointing 系统的基础。  计数器 / 排行榜 / 库存工作负载展示了频谱：计数器（最终一致即可）、排行榜（需要因果一致）、库存（...

=== tables (6 occurrences) ===
  c15_b09.md: ...状态。该协议是状态流处理整个类别的基础。  **Q3.** 一个团队有全球多区域数据库（DynamoDB Global Tables）。他们发现偶尔从一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Global Tab...
  c15_b09.md: ...les）。他们发现偶尔从一个区域写入的数据在另一个区域看会 “消失”。诊断。  提示  DynamoDB Global Tables 跨区域按 item 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB...
  c15_b09.md: ...em 做 LWW。不同区域对同一 item 并发写会导致一个被覆盖。  **答案：** DynamoDB Global Tables 跨区域按 item 使用 last-write-wins（LWW），时间戳由 AWS 时钟服务决定。当两个区域并发写入...
  c15_b09.md: ...亲和多区域，或 Cassandra 上自定义 per-region 分区。  更深观察：DynamoDB Global Tables 对写少或自然按区域分区的工作负载（读重）很好。对多区域积极写同一 item 的工作负载不合适。系统文档说明了这点，但初...
  c30_b11.md: ...输入**：读主导工作负载；面向用户；延迟敏感；大多数读可接受最终一致。  **决策**：DynamoDB Global Tables 或 Cassandra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB...
  c30_b11.md: ...andra/ScyllaDB + CDN。  * **Operational 存储**：DynamoDB Global Tables（AWS 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CDN**：Cloudflare...

=== position (6 occurrences) ===
  c16_b03.md: ...st CRDT（2000 年代中期）。每个字符被分配一个 _position identifier_，位于相邻字符之间。Position identifiers 长度任意（通常是整数数组）；A 和 B 之间（id 1.2 与 1.5）插入的新字符获得 id...
  c16_b03.md: ...2 与 1.5）插入的新字符获得 id 1.3 或 1.4。1.3 与 1.4 之间再插入则取 1.35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下...
  c16_b03.md: ...identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers 可能变长（交替插入可使 id 长度翻倍）。删除字符的垃圾回收微妙。   #### RGA（Re...
  c16_b03.md: ...st CRDT（2000 年代中期）。每个字符被分配一个 _position identifier_，位于相邻字符之间。Position identifiers 长度任意（通常是整数数组）；A 和 B 之间（id 1.2 与 1.5）插入的新字符获得 id...
  c16_b03.md: ...2 与 1.5）插入的新字符获得 id 1.3 或 1.4。1.3 与 1.4 之间再插入则取 1.35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下...
  c16_b03.md: ...identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers 可能变长（交替插入可使 id 长度翻倍）。删除字符的垃圾回收微妙。   #### RGA（Re...

=== cell (6 occurrences) ===
  c16_b08.md: ...一个团队要构建协作文档表格。他们考虑��单元格值使用 CRDT。该用什么 CRDT 结构，有哪些坑？  提示  表格：cell ID 到值的映射。简单情况用 LWW-Map；同一单元格并发编辑是坑。公式更难。  **答案：** 基本结构：LWW-...
  c16_b08.md: ...WW-Map；同一单元格并发编辑是坑。公式更难。  **答案：** 基本结构：LWW-Map（或 OR-Map），键是 cell ID（如 “A1”、“B2”），值是单元格内容。  对简单单元格值（文本、数字），带 HLC 时间戳的 LWW-Map...
  c21_b06.md: ...制故障**。互联电网（为共享发电而高效）成为级联路径。现代电网设计增加 “blackstart” 孤岛。分布式系统类比：cell-based 架构，一个 cell 故障不会级联到其他 cell；以部分效率换取遏制。  2003 年大停电是经典研究，...
  c21_b06.md: ...高效）成为级联路径。现代电网设计增加 “blackstart” 孤岛。分布式系统类比：cell-based 架构，一个 cell 故障不会级联到其他 cell；以部分效率换取遏制。  2003 年大停电是经典研究，因为规模前所未有（5000 万人；...
  c21_b06.md: ...计增加 “blackstart” 孤岛。分布式系统类比：cell-based 架构，一个 cell 故障不会级联到其他 cell；以部分效率换取遏制。  2003 年大停电是经典研究，因为规模前所未有（5000 万人；40-100 亿美元损失），教...
  c21_b08.md: ...* **电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blackstart 孤岛**：cell-based 架构；能够隔离部分系统并独立重启。  2003 年序列用分布式系统术语：  (a) **触发器**：三个...

=== rate (6 occurrences) ===
  c19_b03.md: ...置，配置存储使用共识。  示例：  * **Feature flags**：跨所有服务切换功能，无需重新部署。 * **Rate limits**：根据负载动态调整限速。 * **路由规则**：流量路由策略（金丝雀发布、A/B 测试）。 * **S...
  c21_b02.md: ...-v2         [*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open -->...
  c21_b02.md: ...Closed: All calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contac...
  c21_b07.md: ...服务拒绝超出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：确保每个服...
  c26_b04.md: ...FO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queu...
  c28_b03.md: ...lare Workers)"]             CF[API Gateway<br/>TLS / auth / rate limit]         end         subgraph USE["US-East Region"]...

=== secret (6 occurrences) ===
  c19_b03.md: ...e limits**：根据负载动态调整限速。 * **路由规则**：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch s...
  c19_b03.md: ...*：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变...
  c19_b03.md: ...e limits**：根据负载动态调整限速。 * **路由规则**：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch s...
  c19_b03.md: ...*：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变...
  c19_b06.md: ...变更，而非全量重读）。 * 运维变更即时生效；无需重启。  其他示例用例：流量路由规则（金丝雀发布）、限速（动态调整）、secret rotation（无需服务重启）、监控阈值（无需代码部署即可调整告警）。  **Q4.** 为什么基于 DNS 的服务...
  c19_b11.md: ...-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul 中，watch 变更，动态重新配置）是 2026 年运行时配置的默认，不应要求服务...

=== istio (6 occurrences) ===
  c19_b04.md: ...uest 追踪、延迟指标、错误率。  (e) **弹性**：熔断、重试预算、超时策略。  2026 年两大主导服务网格：Istio（基于 envoy；Kubernetes 原生）和 Linkerd（更轻量）。两者都用 etcd/Kubernetes...
  c19_b08.md: ...ul 集群而非 3 节点；更大集群对故障容忍更高，重选更快。  (d) **使用带 sidecar 缓存的服务网格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断...
  c19_b10.md: ...e 对象、kube-proxy、DNS）快速、可靠、运维简单。对纯 Kubernetes 部署、几十个服务以内，它无需 Istio 或 Linkerd 的运维开销就能处理发现。网格增加 CPU、内存、延迟和巨大运维面。  反驳：服务网格对多集群、多区...
  c19_b10.md: ...左右达到顶峰；许多团队随后因运维开销弃用服务网格，转向更简单的替代。趋势是 “需要时才用服务网格，不要因为 FOMO”。Istio 的复杂性尤其驱使团队转向更轻量的替代（Linkerd）或完全不用网格。  第二个考虑：服务网格价值很大程度上取决于数据...
  c19_b10.md: ...性尤其驱使团队转向更轻量的替代（Linkerd）或完全不用网格。  第二个考虑：服务网格价值很大程度上取决于数据面技术。Istio 的数据面 Envoy 很重；基于 eBPF 的替代（Cilium）更轻。随着 eBPF 网格成熟，服务网格运维成本下降...
  c30_b12.md: ...0+ 节点集群中才值得，此时其横向扩展是必需的。  **10 个服务不需要时不要伸手选 service mesh。** Istio/Linkerd 增加运维复杂度。对小型服务图，Kubernetes 原生服务发现足够。只有在多 region、多协议、...

=== flight (6 occurrences) ===
  c19_b08.md: ...走一遍每个服务会发生什么，以及可能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b09.md: ...置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watc...
  c21_b04.md: ...:35，运维手动在 merchant 数据库上调用 load shedding（只接受最高优先级流量）。 * 这让已在 flight 的查询完成；数据库开始恢复。 * 14:42，上游服务重试预算耗尽，重试风暴开始消退。 * 14:50，正常运行恢复；...
  c22_b07.md: ..._选项 1：重启数据库_  优点：重置缓存（从头重建）；清除卡住的查询；“干净 slate” 恢复。  缺点：in-flight 请求失败（应用看到错误）；缓存必须重建（最初很多分钟慢，因为冷缓存）；短暂完全中断（通常 30-120 秒）。  何时...
  c25_b04.md: ...注点**：  * KV cache 传输是新瓶颈；带宽配置必须跟上。 * 故障模式：prefill 节点崩溃会让 in-flight 请求的 KV cache 搁浅；需要恢复逻辑。 * 负载均衡更复杂；分配必须同时考虑两个阶段。 * 可观测性：trac...
  c26_b02.md: ...t” 节点；长请求去 “sustained” 节点。  (c) 误预测：若预测错误（本应短却长，或相反），请求 mid-flight 迁移到更合适节点（KV cache 传输；decode 在新节点恢复）。  误预测是运维关注点。80% 准确度的长度预...

=== sidecar (6 occurrences) ===
  c19_b08.md: ...冗余**：运行 5 节点 Consul 集群而非 3 节点；更大集群对故障容忍更高，重选更快。  (d) **使用带 sidecar 缓存的服务网格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是...
  c19_b08.md: ...大集群对故障容忍更高，重选更快。  (d) **使用带 sidecar 缓存的服务网格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidecar 异步从 C...
  c19_b08.md: ...*使用带 sidecar 缓存的服务网格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidecar 异步从 Consul 刷新。  (e) **迁移到集群内...
  c19_b08.md: ...nkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidecar 异步从 Consul 刷新。  (e) **迁移到集群内发现（Kubernetes service）**：Kubern...
  c21_b07.md: ...荐顺序：  (a) **每个客户端加重试预算**：实现重试预算可防止最常见的放大机制（重试风暴）。修改每个重试感知库或 sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、...
  c30_b19.md: ...lover | Ch.7（共识），Ch.18（fencing token），附录 E.5   Service mesh sidecar 显示熔断器 "Open" 但 trace 仍显示尝试调用 | 熔断器配置错误（未检测到失败） | Ch.21 §21....

=== tempo (6 occurrences) ===
  c20_b03.md: ...团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准  2026 年，OpenTelemetry 是...
  c20_b03.md: ...I。 * 常见库自动仪器（HTTP 客户端、数据库驱动、消息队列）。 * 可导出到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：...
  c20_b09.md: ...错了。”  **讨论：** 辩护：有了 OpenTelemetry、成熟的追踪后端（Honeycomb、Datadog、Tempo）和良好的工程纪律，分布式追踪提供调试大多数生产问题所需的可见性。挣扎的团队要么没投资追踪，要么没学会读 trace。...
  c20_b09.md: ...** LLM 的失败：产出与特定厂商耦合的代码。团队现在被锁定在 DataDog；换到另一追踪后端（Honeycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetr...
  c20_b09.md: ...("operation"):         do_work()   上述代码可与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。...
  c20_b09.md: ...enTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTelemetry 基础...

=== logs (6 occurrences) ===
  c20_b03.md: ...g 的旧版本，trace 不会标记。为此需要部署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。...
  c20_b08.md: ...的坏输入模式复现失败。迭代修复。  诊断流程的技巧是知道每一步该看什么。Trace 是起点；诊断流程结合 traces、logs、metrics 和复现。常见模式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低到随机抽查...
  c20_b09.md: ...Telemetry" 是运维标准。  第二个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关联 traces、logs、metrics。它们有望减轻读 trace 的认知负担；2026 年还处于早期且不可靠，但轨迹表明 2028-2030...
  c20_b10.md: ...具。Traces 展示跨服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关联这四者。  取证 trace（支付 trace、安全事件 trace...
  c27_b12.md: ...。没有混沌测试的分布式代码 PR 被拒绝。  (e) **监控要求**：每个分布式操作必须可观测。没有 metrics、logs、traces 的操作 PR 被拒绝。  (f) **周期性审计**：高级工程师每季度审计已合并分布式代码 PR 样本。...
  c29_b02.md: ...式处理**仍弱；人类在新模式和架构选择上的角色持续。 * **AI 辅助事后分析**成为标准；LLM 读取 trace+logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出 game...

=== half (6 occurrences) ===
  c21_b02.md: ...正常操作。所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即失败（不联系下游）。  (c) **Half-open**：冷却期后，少量调用被允许通过。若成功，熔断器返回 Closed；若失败，返回 Open。  收益：当下游...
  c21_b05.md: ...**Q3.** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。Open：失败阈值超过；所有调用失败。Half-open：冷却后退火探测。...
  c21_b06.md: ...近 30 秒 50% 失败率）。所有后续调用立即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返...
  c21_b06.md: ...即失败（不联系下游）。熔断器保持 Open 一段冷却时间。  (c) **Half-open**：冷却结束后熔断器进入 Half-open。少量调用（如 1/N）被允许通过，测试下游是否恢复。若成功，返回 Closed；若失败，返回 Open。...
  c21_b10.md: ...动放大。持续失败下，上限随成功流量下降；放大保持有界。  熔断器监控下游成功率；超过阈值时打开；Open 状态快速失败；Half-open 探测恢复。该模式停止向故障组件加负载，给它恢复机会。  服务入口的 load shedding 防止队列堆积...
  c22_b06.md: ...断器。无滞后时，熔断器在 30 秒内 50% 失败率打开；以相同阈值重新关闭。有滞后时：30 秒内 50% 失败率打开；half-open 探测中 5 秒内 80% 成功就关闭。关闭是激进的：一旦检测到短暂恢复，熔断器就重开。  无滞后时，熔断器在...

=== readiness (6 occurrences) ===
  c22_b06.md: ...记不健康前需要多次连续失败。  生产模式：廉价 liveness 检查（让 kubelet 知道容器是否存活）加深度的 readiness 检查（让负载均衡器知道节点能否服务流量）。两者语义不同；都需要。  正确深度取决于工作负载。无外部依赖的无状态服务可用...
  c22_b10.md: ...检查**（kubelet 的 `/healthz`）：进程是否在运行？廉价；若进程能响应则返回 200。  (b) **Readiness 检查**（负载均衡器的 `/readyz`）：服务是否准备好服务真实请求？  * 锻炼数据库连接（一次简单的 SELE...
  c22_b10.md: ...用于自动路由。  (e) **基于延迟的指标**：单独暴露服务最近 p99 延迟的指标。负载均衡器（或自动扩缩器）结合 readiness 检查用它做路由决策。...
  c22_b10.md: ...检查**（kubelet 的 `/healthz`）：进程是否在运行？廉价；若进程能响应则返回 200。  (b) **Readiness 检查**（负载均衡器的 `/readyz`）：服务是否准备好服务真实请求？  * 锻炼数据库连接（一次简单的 SELE...
  c22_b10.md: ...用于自动路由。  (e) **基于延迟的指标**：单独暴露服务最近 p99 延迟的指标。负载均衡器（或自动扩缩器）结合 readiness 检查用它做路由决策。...
  c22_b11.md: ...应问：“你的服务角色是什么？它有哪些依赖？健康检查的成本预算如何？有这上下文，我才能设计适合你服务的 liveness、readiness 和 startup 检查。” 单行 “return 200” 只适用于最简单的无状态服务。  更深观察：健康检查是系统...

=== nora (6 occurrences) ===
  c22_b11.md: ...程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍实践。...
  c23_b02.md: ...### 23.3 Nora Jones 的组织实践  Nora Jones（与 Casey Rosenthal 合著《Chaos Engineer...
  c23_b02.md: ...### 23.3 Nora Jones 的组织实践  Nora Jones（与 Casey Rosenthal 合著《Chaos Engineering: System Resili...
  c23_b07.md: ...导致级联” 是系统观察；“Alice 配置了重试预算，她错了” 是个人责备。前者导向修复；后者导向防御行为且无修复。  Nora Jones 的无责复盘实践是运营体现：讨论聚焦系统性原因，而非个人错误。“配置为什么是这样？” 导向 “runbook...
  c23_b13.md: ...是 scheduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性事件。  Nora Jones 的组织实践强调心理安全、无责复盘、多元参与、记录学习、固定节奏。没有文化基础，技术练习不产生学习。  工具...
  c29_b03.md: ...随着系统更复杂（多 region、多云、AI 增强、混合 BFT/崩溃故障组件），唯一知道防御是否有效的方式是演练它们。Nora Jones 的组织实践（无责复盘、game day、记录学习）随着技术面增长变得更相关，而非更不相关。   ####...

=== soc (6 occurrences) ===
  c24_b08.md: ...。  **Q2.** 某团队在企业部署中选择 vLLM 与 NVIDIA NIM。他们在 Kubernetes 上；有 SOC 2 合规要求。比较。  提示  vLLM：开源，更多控制，更少集成。NIM：企业特性、SOC 2、更少优化控制。  *...
  c24_b08.md: ...bernetes 上；有 SOC 2 合规要求。比较。  提示  vLLM：开源，更多控制，更少集成。NIM：企业特性、SOC 2、更少优化控制。  **答案：**  _vLLM_：  * 开源；团队完全控制部署。 * 最佳原始推理性能（引擎本身...
  c24_b08.md: ...全控制部署。 * 最佳原始推理性能（引擎本身）。 * 没有原生 SSO、RBAC、审计日志；团队自建或集成第三方。 * SOC 2 合规：可行，但需团队处理审计跟踪、访问控制等。  _NVIDIA NIM_：  * 商业企业产品。 * 包含开箱即...
  c24_b08.md: ...。  _NVIDIA NIM_：  * 商业企业产品。 * 包含开箱即用的 SSO、RBAC、审计日志、监控集成。 * SOC 2 合规：平台提供构建块；团队的特定合规更快实现。 * 推理引擎控制较少：NIM 容器使用特定引擎（通常底层仍是 vL...
  c24_b08.md: ...用特定引擎（通常底层仍是 vLLM），但团队离调优隔了一层。 * 成本：NVIDIA 收取企业 tier 费用。  对 SOC 2 合规 Kubernetes 部署：  * **NIM** 是运维上更快的路径。合规和企业特性已预建；团队认证工作量...
  c24_b09.md: ...团队 → NIM（现成企业）；有平台工程师的大团队 → vLLM（自建企业特性）。 * **合规紧迫性**：3 个月内 SOC 2 审计 → NIM（更快）；12 个月 timeline → 两者都可。 * **定制化需求**：重度定制 → vL...

=== matrix (6 occurrences) ===
  c25_b01.md: ...er token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache |...
  c25_b01.md: ...n | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decod...
  c25_b01.md: ...| GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache...
  c25_b01.md: ...er token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache |...
  c25_b01.md: ...n | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decod...
  c25_b01.md: ...| GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache...

=== transfer (6 occurrences) ===
  c25_b02.md: ...│ KV cache         │ KV cache                        │ transfer         │ transfer                        ▼...
  c25_b02.md: ...│ KV cache                        │ transfer         │ transfer                        ▼                  ▼...
  c27_b11.md: ...cess across services             bank_result = bank_service.transfer(payment)             db.update(                 "UPDATE acc...
  c27_b12.md: ...这 25 行代码中的六个谎言：    1. **谎言 1（RPC 语义）**：`bank_service.transfer(payment)` 没有 idempotency key。若超时重试，bank 可能转账两次。    2. **谎言...
  c27_b12.md: ...。  Olamide 的审查反馈：“拒绝。这段代码包含 Thread C 全部六个谎言。具体：bank_service.transfer 缺少 idempotency key（谎言 1）；任何网络调用都没有 timeout（谎言 2）；ledger 用 t...
  c28_b10.md: ...衡集群，零停机，事务一致性保留 | 一致性哈希 + range streaming + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover 未处理 | |...

=== mooncake (6 occurrences) ===
  c25_b03.md: ...cache 可达数十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在 2024 年论文中描述的推理系统，是 2026 年的典型实现。KV cache 以块形式（...
  c25_b05.md: ...che。没有单张 GPU 内存能放下一个。不分片，最大上下文长度受限于 per-GPU 内存。  实现：  (a) **Mooncake**（Moonshot AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过...
  c25_b10.md: ...默认。  第二个考虑：技术快速演进。Splitwise（2023）引入基本思想；DistServe（2024）精化调度；Mooncake 和 LMCache（2024-2025）添加缓存层；Mysticeti 等扩展到 MoE 模型。2026 年采用分离的...
  c25_b11.md: ...request KV cache 跨多张 GPU 和节点分布。超长上下文（1M+ token）必需；实现跨请求前缀复用。Mooncake（商业）和 LMCache（开源）是 2026 实现。  Tobias 的工作流 12：30 prefill 节点 +...
  c28_b13.md: ...现在是 etcd。2026 年分布式系统工程包括 Spanner、CockroachDB、vLLM、Splitwise、Mooncake——它们在该领域奠基工作时都不存在。  心智模型是持久的。理解分布式语境下时间、身份、故障的含义在 1985 年（FLP...
  c30_b03.md: ...-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-centric Disaggregated Architecture for LLM Servi...

=== separate (6 occurrences) ===
  c25_b06.md: ...占优。  **答案：** 分离有运维成本：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate pr...
  c25_b06.md: ...节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning...
  c25_b06.md: ...节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻...
  c25_b06.md: ...占优。  **答案：** 分离有运维成本：  (a) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate pr...
  c25_b06.md: ...节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning...
  c25_b06.md: ...节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻...

=== round (6 occurrences) ===
  c26_b03.md: ...raft 和 verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），...
  c26_b04.md: ...scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从...
  c26_b05.md: ...同。  现代推理平台（Anyscale、Together、Fireworks）已构建显式考虑这些因素的路由优化器。朴素 round-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-...
  c26_b06.md: ...g 增加运维复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 G...
  c26_b06.md: ...与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所...
  c26_b06.md: ...率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等下一轮。...

=== verification (6 occurrences) ===
  c26_b03.md: ...节点**：一小模型节点池并行为多个请求运行 draft。  (b) **Verify 在大模型节点**：大模型节点只做 verification（比完整 decode 便宜）。  (c) **Draft 与 verify 间的网络协议**：draft 节点发送 K...
  c26_b03.md: ...raft model）。  (b) 更好资源利用率：小模型节点并行服务多个请求的 draft；大模型节点以更高吞吐处理 verification。  (c) 独立扩缩：根据工作负载扩缩 draft 和 verify 容量。  成本：  (a) 每 token 网络...
  c26_b06.md: ...*。1B 参数 draft 可放在小 GPU（甚至 CPU）。把 draft 放在便宜硬件上，让昂贵大模型 GPU 做 verification。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的...
  c26_b06.md: ...。  (b) **Verify model 被多请求共享**。Verify model 的 GPU 可并行处理多请求的 verification（每请求一个 verify 步）。昂贵资源被摊销。  (c) **集群足够大以支持角色分离**。与 disaggrega...
  c26_b06.md: ...0 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located sp...
  c26_b06.md: ...请求做 draft；大模型 GPU 做 verification。小 GPU 更便宜；大 GPU 利用率更高（始终处理 verification）。  何时 co-located speculative decoding 更合适：  * 小集群，分布式运维开销占主...

=== outbox (6 occurrences) ===
  c27_b04.md: ...系统处于不一致状态。  修复：识别哪些调用对一致性关键，并用 saga 模式（第13章）或 transactional outbox。       def update_user_across_systems(user):         # 关键：D...
  c27_b04.md: ...败即中止。         primary_db.update(user)          # 尽力：缓存和通知。用 outbox 模式。         outbox.enqueue(InvalidateCache(user.id))...
  c27_b04.md: ...ry_db.update(user)          # 尽力：缓存和通知。用 outbox 模式。         outbox.enqueue(InvalidateCache(user.id))         outbox.enqueue(No...
  c27_b04.md: ...式。         outbox.enqueue(InvalidateCache(user.id))         outbox.enqueue(NotifyUser(user.id, 'updated'))      # 异步 worker 处理...
  c27_b04.md: ...enqueue(NotifyUser(user.id, 'updated'))      # 异步 worker 处理 outbox；失败时重试。   LLM 产出朴素版本是因为训练数据显示 “做 A；做 B；做 C” 是常见模式。“若 A 成功但 B...
  c30_b07.md: ...18 | 共识支持锁 + fencing token | | 5 | 部分故障罕见 | Ch.22 | Saga 模式；outbox；补偿动作 | | 6 | CAP 分类简单 | Ch.2 | PACELC 分析；per-operation 一致性选...

=== lag (6 occurrences) ===
  c28_b06.md: ...**：小异常。EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00...
  c28_b06.md: ...30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100% 流量。Lag 已解决；延迟正...
  c28_b06.md: ...ad 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100% 流量。Lag 已解决；延迟正常。团队继续观察。  (g) **18:00 UTC**：完整流量 12 小时。延迟 p50：365 m...
  c28_b06.md: ...**：小异常。EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00...
  c28_b06.md: ...30 ms。调查：前一天晚上的数据库更新导致跨 region 复制 lag，部分 follower read 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100% 流量。Lag 已解决；延迟正...
  c28_b06.md: ...ad 比预期慢。Lag 在范围内但可见。决定：继续监控；不回滚。  (f) **12:00 UTC**：100% 流量。Lag 已解决；延迟正常。团队继续观察。  (g) **18:00 UTC**：完整流量 12 小时。延迟 p50：365 m...

=== structured (6 occurrences) ===
  c30_b02.md: ...et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gaw...
  c30_b02.md: ..., P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & D...
  c30_b02.md: ...et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gaw...
  c30_b02.md: ..., P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & D...
  c30_b03.md: ...* Zheng, L., et al. (2023). "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAttention。 * Patel, P., et...
  c30_b11.md: ...ache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂等写入；事务 sink...

=== charge (5 occurrences) ===
  c01_b11.md: ...#### 应用问题  **Q1.** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔 47 ms 到...
  c01_b11.md: ....** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔 47 ms 到达支付服务的两个不同副本。两者都成功了。走一遍三个谎言，指出系统未能防御的...
  c27_b02.md: ...f submit_payment(payment):         result = payment_service.charge(payment)         if result.success:             db.insert(p...
  c27_b02.md: ...ert(payment.id, status='failed')   谎言：代码假设 `payment_service.charge()` 要么成功（返回 success），要么失败（返回 failure）。第三种选项（RPC 在服务端已处理但响应丢失...
  c27_b02.md: ...y()         result = retry_with_idempotency(payment_service.charge, payment, key=payment.idempotency_key)         # ... 无论发生多少...

=== clocks (5 occurrences) ===
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distributed System"（1978），...
  c30_b01.md: ..._IEEE Computer_. — PACELC 论文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System." _Comm...
  c30_b01.md: ...adappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks." _OPODIS_. — HLC 论文。 * Corbett, J. C., et al. (2012). "Spa...
  c30_b14.md: ...— Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)     │   └── hlc.py            — Hybrid Logical Cl...
  c30_b14.md: ...│   └── causal.py         — causal broadcast with vector clocks     ├── ch09_raft/     │   ├── raft_minimal.py   — ~300 lin...

=== sync (5 occurrences) ===
  c03_b07.md: ...需要 GPS、原子钟、软件栈和运维纪律。大多数公司数据中心里没有 GPS。AWS 等云厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync...
  c13_b05.md: ...LC + 偏移上界。成本可变（取决于争用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like 能力带给云用户；未...
  c13_b17.md: ...实用的 TrueTime 替代。成本可变（争用时重启）而非恒定（每次写 commit-wait）。AWS 的 Time Sync clock-bound API 正把 TrueTime-like 能力带出 Google。  Olamide 的跨区域...
  c29_b01.md: ...CP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在 2024-2025 年成熟；Aurora DSQL 是早期采用者；AP...
  c30_b03.md: ...29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，包括 2024 年在 EC2 上引入微秒级有界时间。 — Tru...

=== skew (5 occurrences) ===
  c03_b08.md: ...S 原子钟基础设施 CockroachDB / YugabyteDB / TiDB | HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequencer...
  c13_b05.md: ...+ 原子钟） | 每次写 7 ms commit-wait CockroachDB | HLC + max-clock-skew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + sk...
  c13_b05.md: ...ew bound（默认 500 ms） | 仅不确定性窗口读时重启 TiDB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Seq...
  c29_b01.md: ...的 HLC  2026 现实（第3、13章）：TrueTime 仅限 GCP；对其他人，HLC + max-clock-skew 是实用替代。  轨迹从 2026 基线开始：AWS Time Sync Service 的 clock-bound 在...
  c29_b01.md: ...署自己的 TrueTime 等价基础设施。  含义：到 2029-2030 年，"使用 HLC + max-clock-skew" 可能像今天的 "仔细调 NTP" 一样显得过时。迁移路径已知；心智模型（时间不确定；系统必须界定不确定性）不变。...

=== action (5 occurrences) ===
  c03_b13.md: ...nder_L) + 1             return self.L, msg   三进程走查：  Step | Action | L_1 | L_2 | L_3 | 注释 ---|---|---|---|---|--- 1 | P1 本地事件...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用户资金（CockroachDB 事务） | 释放...
  c28_b06.md: ...C 集合（覆盖所有 region 的工作时间）。  (a) **发布前检查清单**：两周前混沌 game day；所有 action items 已关闭。容量测试通过。客户迁移计划已审。Rollback 计划已审。客服已通报。  (b) **06:00...
  c28_b06.md: ...错误率：0.02%（在预期内）。无客户投诉。发布宣告成功。  (h) **24:00 UTC**：发布后复盘。教训记录；action items 归档。团队第二天休息（前一个月是每周 80 小时）。  发布是本书纪律的成功应用。没有意外；混沌工程提前抓...
  c30_b06.md: ...breaker（熔断器）**：当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating action（补偿动作）**：saga 中语义撤销先前已提交步骤的前进动作。很少是 “精确撤销”；通常恢复不变量（退款、重新入库、取...

=== uniformity (5 occurrences) ===
  c04_b03.md: ...g)             upper_layer.deliver(msg)   性质（在可靠广播之外）：  * **Uniformity** ：若 _任何_ 进程交付了一条消息（正确或故障），所有正确进程都交付它。  这就是防止普通可靠广播中“唯一接收者崩...
  c04_b05.md: ...>(re-broadcasting)"| B[Reliable broadcast]         B -->|"+ uniformity<br/>(ack round before deliver)"| C[Uniform reliable broadca...
  c04_b05.md: ...ceipt)         v     Reliable broadcast         |         + uniformity (ack round before delivery)         v     Uniform reliable...
  c04_b07.md: ...fort、reliable、uniform reliable、causal、atomic。新增性质：agreement、uniformity、causal order、total order。  **答案：** 由弱到强：    1. **Best-effor...
  c28_b10.md: ...序交付（原子）或更弱变体 | Quorum 确认 + 重广播 | 为 “安全” 默认选原子广播，而可靠广播已足够；遗漏 uniformity（发送者和唯一接收者同时崩溃） | | 3 | 同步 vs 异步复制追踪 | Ch.5 §5.10 | 写主库；存在副本...

=== dynamo (5 occurrences) ===
  c05_b03.md: ...定 leader。写发向任意节点，该节点转发给所有副本。读也发向任意节点，可能需要咨询多个副本以拼装一致答案。  用于：Dynamo（及其后代 Cassandra、Riak、ScyllaDB）。  关键性质是 _no failover required...
  c05_b03.md: ...。冲突由数据类型的合并函数确定性解决。   ### 5.7 Quorum 读与写  对 N 个副本的读写法定人数。经典 Dynamo 论文引入规则：若 $W + R > N$，则任意读与任意已提交写在至少一个副本上相交。  这有时被说成提供线性一致性。...
  c05_b06.md: ...性一致性要求所有读者按实时顺序要么都看见写要么都不见。  (b) **Sloppy quorum** ：主副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本（从未收到写）而非持有 hint 的副本。相交被破...
  c12_b01.md: ...间所有键”需要咨询每个分区。 * **无范围扫描局部性** ：逻辑相关的键（同一用户的数据）可能落到不同分区。  用于：Dynamo、Cassandra（默认 partitioner：Murmur3Partitioner）、Redis Cluster。...
  c16_b02.md: ...留所有版本不被任何其他版本支配的条目。  MV-Register 把冲突抛给应用：读返回所有存活值，应用选择（或合并）。Dynamo 和 Riak 使用。   #### OR-Set（Observed-Remove Set）  典型的集合 CRDT。挑...

=== follow (5 occurrences) ===
  c05_b04.md: ...ads** ：后续读不会看到比先前读 _更旧_ 的状态。一旦看到版本 5，就不会再看到版本 3。 * **Writes-follow-reads** ：任何写都在此前读之后因果发生。若你先读 X 再基于 X 写 Y，Y 排在 X 之后。 * **Mon...
  c11_b02.md: ...ERIALIZABLE 更严）。CockroachDB 2.0 默认改变前。 * **Causal** ：writes-follow-reads、read-your-writes 跨会话保持。部分 Cassandra 配置、部分 MongoDB 配置。...
  c15_b01.md: ...reads** ：后续读不会看到 _更旧_ 状态。一旦看到版本 5，就不会再看到版本 3。  (c) **Writes-follow-reads** ：会话发出的任何写因果上在其先前读之后。若读了 X，再基于 X 写 Y，则 Y 排在 X 之后。  (...
  c15_b06.md: ...每个举一个它重要的工作负载。  提示  Read-your-writes、monotonic reads、writes-follow-reads、monotonic writes、consistent prefix。  **答案：**  _Read-y...
  c15_b06.md: ...ds_ ：后续读不会看到更旧状态。重要场景：用户监控进度。示例：仪表盘显示任务状态，不应看起来回退。  _Writes-follow-reads_ ：会话的写因果上在先前读之后。重要场景：读-然后-更新模式。示例：读计数器，加 1，写回；读和写应因果有...

=== server (5 occurrences) ===
  c05_b09.md: ...服务器端读处理器：     def handle_read(key, min_hlc):         while server.local_hlc < min_hlc:             # 等待复制追上，或超时后拒绝。...
  c05_b09.md: ...ise StaleReplicaError         return read_local(key, at_hlc=server.local_hlc)   当客户端从落后的 follower 读时：该 follower 的 `local_hlc`...
  c12_b05.md: ...默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-le...
  c13_b02.md: ...XA 事务）。事务跨多个数据库，可能由不同团队或公司运维。XA 在 2000 年代初广泛用于跨 Oracle、SQL Server、DB2 的事务。  这种 2PC 用法在现代微服务架构中已基本被弃用。运维复杂度（跨组织协调者、事务恢复语义、跨组织死...
  c19_b05.md: ...现代发现服务：etcd、Consul、ZooKeeper、云厂商基于 DNS 的服务、Kubernetes API server。  发现服务本身是分布式系统。它需要共识（让所有客户端看到同一注册表）、高可用（不让服务间调用失败）和低延迟（不让调用...

=== egress (5 occurrences) ===
  c06_b02.md: ...ion 存储的区域用户架构由此而来。  成本：  * **跨区域操作延迟税。** 任何跨区域操作都付往返成本。 * **Egress 费用。** 云厂商对跨区域（和跨云）数据移动收费。账单可能占主导基础设施成本。 * **运维复杂度。** 三个区域意味...
  c06_b05.md: ...### 6.7 跨区域 Egress 成本  2026 年云定价下，区域间移动数据的美元成本大致：  * 同一区域内：免费或几乎免费。 * 区域间（同一云）...
  c06_b05.md: ...9 USD / GB。 * 云间：0.08 到 0.15 USD / GB。  对每天区域间复制 1 TB 的系统，仅 egress 就是 10-90 USD/天，或 300-2,700 USD/月。对每天复制 100 TB 的系统，年费六位数。  架...
  c06_b05.md: ...### 6.7 跨区域 Egress 成本  2026 年云定价下，区域间移动数据的美元成本大致：  * 同一区域内：免费或几乎免费。 * 区域间（同一云）...
  c06_b05.md: ...9 USD / GB。 * 云间：0.08 到 0.15 USD / GB。  对每天区域间复制 1 TB 的系统，仅 egress 就是 10-90 USD/天，或 300-2,700 USD/月。对每天复制 100 TB 的系统，年费六位数。  架...

=== edge (5 occurrences) ===
  c06_b04.md: ...99；异步工作流 1-2 秒。   2. **减去固定成本。** TLS 握手（首次连接 10-100 ms）、CDN edge 到 origin（5-50 ms）、认证（5-20 ms）。   3. **把剩余预算分配给应用跳数。** 每个下游服...
  c28_b03.md: ...完整的 Crescent 技术栈可视化：       graph TB         subgraph Edge["Edge (Cloudflare Workers)"]             CF[API Gateway<br/...
  c28_b03.md: ...完整的 Crescent 技术栈可视化：       graph TB         subgraph Edge["Edge (Cloudflare Workers)"]             CF[API Gateway<br/>TLS /...
  c28_b03.md: ...-.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         style E...
  c28_b04.md: ...罗 Adaeze 到拉各斯商户 Femi 的支付：  (1) Adaeze 的手机发送支付请求到 Cloudflare edge worker。Worker 验证请求（HMAC 签名；按客户计数器做速率限制检查）。5 ms。  (2) Worker...

=== fan (5 occurrences) ===
  c06_b09.md: ...1——7 跳完全串行**：800 / 7 ≈ 115 ms p99 每跳。可行但紧；没有余量。  **情况 2——并行 fan-out**：如果服务网格允许 7 跳中 4 跳作为一批并行（fan-out 模式），其余 3 跳串行：  * 并行批延...
  c06_b09.md: ...3 跳 + 并行批：800 - 115 = 685 ms 分给 3 串行；每跳 ~228 ms p99。  因此加一次 fan-out 后，单个 hop 有 ~228 ms p99，舒服得多。并行批“买回”预算给串行路径。  **情况 3——2...
  c06_b09.md: ...p 都必要。** 服务网格图常包含历史原因存在的 hop；实际数据依赖可能只有 4-5 个。  (b) **识别可并行 fan-out。** 互不依赖的 hop（如同时取用户资料、检查认证、加载定价）应并行。...
  c19_b09.md: ...new_checkout_flow true`。etcd Raft group 提交变更。    2. **Watch fan-out**：每个 watch `feature_flags/` 的服务收到 watch 事件和新值（通常在几十毫秒内；...
  c19_b09.md: ...队，就是 1000 条同时通知。etcd 在小规模下处理良好；超大规模下可能需要对 watch 分片或使用 watch fan-out 服务。  (b) **部分传播**：有些服务比其他服务晚收到 watch 事件（网络延迟）。短暂窗口内，两个服...

=== made (5 occurrences) ===
  c08_b01.md: ...eeper 的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport 2001 年改写成 “Paxos Made Simple”，仍然很难。  本章教授 Paxos 的常见形态：单法令 Paxos（就一个值达成一致）和 Multi-...
  c08_b04.md: ...### 8.6 为什么 Paxos 难实现  Lamport 的 “Paxos Made Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persistent state** ：a...
  c08_b11.md: ...r 和 Megastore 是经典生产实现。附录 A 的阅读列表包括原始 1998 年论文、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available Sy...
  c30_b02.md: ...t." _ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L. (2001). "Paxos Made Simple." _ACM SIGACT News_. — 必读改写。 * Ongaro, D., & Ousterh...
  c30_b02.md: ...piegelman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevic, Z....

=== simple (5 occurrences) ===
  c08_b01.md: ...的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport 2001 年改写成 “Paxos Made Simple”，仍然很难。  本章教授 Paxos 的常见形态：单法令 Paxos（就一个值达成一致）和 Multi-Paxos（就...
  c08_b04.md: ...### 8.6 为什么 Paxos 难实现  Lamport 的 “Paxos Made Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persistent state** ：acceptor...
  c08_b11.md: ...egastore 是经典生产实现。附录 A 的阅读列表包括原始 1998 年论文、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Us...
  c30_b02.md: ...ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L. (2001). "Paxos Made Simple." _ACM SIGACT News_. — 必读改写。 * Ongaro, D., & Ousterhout, J....
  c30_b03.md: ...tch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基础，作为前瞻话题引用（§29.1；深度...

=== null (5 occurrences) ===
  c08_b02.md: ...mise，即可继续。  Proposer 在 accept 阶段使用的值：  * 若收到的 promise 中有任何非 null 的 `accepted_value`，proposer 必须取 `accepted_ballot` _最高_ 的那个...
  c08_b02.md: ..._最高_ 的那个 promise 的值。 * 否则（所有 promise 的 `accepted_value` 都是 null），proposer 使用自己的原始值。  这就是 safety 性质在起作用：任何已在“飞行中”的值（已被某 acce...
  c08_b05.md: ...=0$（初始），接受 prepare。每个设置 $b_{max}=10$ 并回复 `PROMISE(10, null, null)`。  **步骤 3** ：Proposer 收集到 3 个 promise（多数）。所有 `accepted_val...
  c08_b05.md: ...步骤 3** ：Proposer 收集到 3 个 promise（多数）。所有 `accepted_value` 都是 null，于是使用自己的值 $v = \text{"hello"}$。  **步骤 4** ：Proposer 发送 `ACCE...
  c20_b05.md: ...ID**：该 span 唯一。 * **Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start t...

=== accepted (5 occurrences) ===
  c08_b05.md: ...。它们设置 `accepted_ballot = 10, accepted_value = "hello"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值...
  c08_b05.md: ...o"`，并回复 `ACCEPTED(10, "hello")`。  **步骤 6** ：Proposer 收到 3 个 accepted 响应（多数）。值 “hello” 现在被 _选定_。  现在假设第二个 proposer（可能因为第一个崩溃）启动新...
  c08_b08.md: ...* B 发送 `ACCEPT(20, "Olamide")` 给所有 5 个。 * 全部 5 个接受。B 收到 5 个 accepted 消息。值 “Olamide” 被选定。  **A 的重试：**  * A 的 `prepare(10)` 失败（只有...
  c08_b10.md: ...常用简单计数器而不带 proposer-ID 消歧，于是两个 proposer 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N...
  c08_b10.md: ...ety 违反）：  (d) **第二阶段值选择。** Accept 规则（proposer 必须提议第一阶段响应中最高 accepted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-p...

=== election (5 occurrences) ===
  c09_b01.md: ...先想到 Raft。   ### 9.1 三个子问题  Raft 把共识分解为三个正交部分：  (a) **Leader election** ：选一个服务器作为 leader。 (b) **Log replication** ：leader 接受客户端请求...
  c09_b02.md: ...### 9.3 Leader Election  每个服务器初始为 _follower_。Follower 期望定期收到 leader 心跳。若 follower 太...
  c09_b06.md: ...# 概念检测  **Q1.** Raft 把共识分解为三个子问题。说出它们并说明各自处理什么。  提示  Leader election、log replication、safety。  **答案：**  (a) **Leader election** ：...
  c09_b06.md: ...der election、log replication、safety。  **答案：**  (a) **Leader election** ：选单个服务器作为 leader。Follower 通过心跳超时检测 leader 缺失并启动选举。随机化超时降低...
  c30_b14.md: ...09_raft/     │   ├── raft_minimal.py   — ~300 lines: leader election + AppendEntries + commit     │   ├── kv_state_machine.py —...

=== check (5 occurrences) ===
  c09_b03.md: ...* 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志在新追加之前与 leader 匹配，Raft 确保...
  c13_b07.md: ...rvice.reserve(user, amount)       step 2: call FraudService.check(transaction)         → score       step 3: call BankSettlem...
  c13_b07.md: ...->>R: reserve funds         R-->>O: reserved         O->>F: check fraud         F-->>O: score (OK)         O->>B: submit sett...
  c20_b01.md: ...A["auth + idempotency<br/>22 ms"]         F["fraud check<br/>80 ms (serial after auth)"]         DB["DB transaction<...
  c20_b01.md: ...务协调开销。该图让工程师立即看出哪个 span 主导延迟（本例中是跨区域 DB transaction 和 fraud check）。  每个 span 携带：  * Trace ID（每个请求唯一；整个 trace 所有 span 共享）。 * S...

=== case (5 occurrences) ===
  c09_b04.md: ...previous term 的条目（这有副作用：连带提交之前的所有条目）。  原因：没有这条规则，某个 corner case 会让已提交条目后来被覆盖。Raft 论文的图 8 说明了该情况；修复就是“current term only”提交规则...
  c09_b04.md: ...个条件一起防止图 8 问题。  实践中很少要紧，因为新 leader 通常很快提交新条目，但实现必须处理 corner case。   ### 9.6 日志压缩  Raft 日志无限增长。生产系统使用快照：定期把状态机完整状态写入快照，并把日志截断...
  c09_b10.md: ...previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendEntrie...
  c09_b11.md: ...模式与 Paxos 相同：LLM 生成的代码匹配高层描述但错过微妙不变式。Raft 论文的图 8 case 就在论文里；LLM 训练也见过。但把“leader 必须提交一个当前 term 条目才能提交 previous ter...
  c30_b14.md: ...py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/     │   ├── best_effort.py...

=== diem (5 occurrences) ===
  c10_b01.md: ...Yin et al. 2019<br/>O(N) view change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2...
  c10_b01.md: ...al. 2019<br/>O(N) view change"]         HotStuff --> Diem["Diem / Aptos"]         HotStuff --> Mysticeti["Mysticeti 2024<br...
  c10_b03.md: ...收集第三阶段投票。这使吞吐远高于 PBFT 的串行三阶段执行。  HotStuff 变体是现代每个 BFT 系统的基础：Diem/Aptos 使用 HotStuff；Sui 在 Narwhal 之上使用 Bullshark（融入了 HotStuff...
  c10_b04.md: ...在 Narwhal 上使用 Bullshark——2024 年升级为 Mysticeti 以进一步提高吞吐。 * **Diem**（已停运）直接使用 HotStuff。  Narwhal-Bullshark 的吞吐声明惊人：每秒数万笔交易，而 P...
  c10_b09.md: ...码）。  (b) **HotStuff** ：$O(N)$ view change；pipelining 允许更高吞吐。Diem/Aptos 已在生产使用。实现库（LibraBFT、Aptos 的 HotStuff）存在，但多绑定特定区块链生态。为...

=== spiegelman (5 occurrences) ===
  c10_b04.md: ...于 DAG 的共识  2022 年 Narwhal 论文（Danezis、Kokoris-Kogias、Sonnino、Spiegelman）做出结构性创新：把 _mempool_（在交易到达共识前对它们排序的层）与 _consensus_（决定提交哪些 me...
  c10_b04.md: ...AG 已经排好），而是“挑选要提交的 DAG 前沿”。这极大减轻了 leader 的工作量。  Bullshark 由 Spiegelman 与同事设计，2022 年发表，是运行在 Narwhal DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：l...
  c30_b02.md: ...ithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited....
  c30_b02.md: ..." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Effi...
  c30_b02.md: ...AG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Prac...

=== fail (5 occurrences) ===
  c10_b07.md: ...、可能恶意的消息。  **答案：** _Crash fault_ 是节点停止运行的故障：停止发消息、停止响应消息，且在 fail-stop 模型中直到重启才恢复。Paxos 和 Raft 假设崩溃故障。  _Byzantine fault_ 是节点...
  c19_b08.md: ...举期间新写被阻塞（通常 5-15 秒）。此窗口查询 Consul 的客户端可能收到过期或错误响应。  若客户端配置为 “fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail close...
  c19_b08.md: ...端配置为 “fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail closed”（Consul 不可达时返回错误），每次服务调用在选举窗口内失败。  修复：  (a) **配置客户端使...
  c21_b02.md: .../>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/>track success/fai...
  c21_b02.md: ...ugh;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contact         HalfOpen: Limited p...

=== azure (5 occurrences) ===
  c10_b10.md: ...实威胁？对大型云厂商控制面，可能确实（内部威胁、定向攻击）。对大多数其他系统，BFT 成本超过威胁概率。AWS、GCP、Azure 的元数据存储使用崩溃容错共识（加上强物理安全和访问控制）而非 BFT。威胁真实，但通过其他手段管理（纵深防御、监控、访...
  c13_b16.md: ...nd API，差别变得无关紧要。目前选择是运维性的：若你在 GCP 且成本可接受，用 TrueTime；若你在 AWS/Azure/多云且运维简单性重要，用 HLC。  更微妙考虑：TrueTime 的 commit-wait 是每次写 _无条件_...
  c14_b01.md: ...间戳避免不确定性重启。 * 单一全球实例可跨多区域（或仅区域）。  劣势：  * 仅限 GCP。不能部署在 AWS 或 Azure。 * 昂贵（按节点小时 + 存储和 IO 计费）。 * 相比完整 Postgres，某些 SQL 功能（子查询等）曾有...
  c19_b03.md: ...客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cloud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kubernetes headless ser...
  c29_b01.md: ...abyteDB** 到 2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增加类似 clock-bound 服务；云厂商在基础设施上竞争。 *...

=== firecracker (5 occurrences) ===
  c11_b07.md: ...、分布式 SQL 数据库，设计为 AWS 基础设施上的 Postgres 兼容 Spanner 风格系统。复制协议基于 Firecracker 每租户微虚拟机与解耦存储层（写走 6 副本存储法定人数，4 of 6 ack）。故障恢复透明（Firecracker...
  c11_b07.md: ...。优势：serverless 计费、Postgres 兼容、可跨区。劣势：仅限 AWS；较新（仍在实战中检验）；每租户 Firecracker 模型不常见，可能有意外运维特征。适合：希望多区域且 serverless、又不想管理 CockroachDB 集群的...
  c14_b03.md: ...a DSQL  AWS 2024 GA 的分布式 SQL 数据库。架构与上述其他系统不同。  关键属性：  * 每租户 Firecracker 微虚拟机保证计算隔离。 * 解耦存储层，6 副本法定人数（写需 4 of 6 ack）。 * 兼容 Postgres...
  c14_b03.md: ...AWS。 * 较新（生产 track record 不如 CockroachDB 或 Spanner）。 * 每租户 Firecracker 开销可能让习惯多租户数据库引擎的团队意外。  Aurora DSQL 适合：AWS 原生工作负载，需要多区域 Post...
  c14_b09.md: ...）、兼容 Postgres（大部分）、多区域可用、AWS 原生集成。缺点：AWS 锁定加深、较新且生产记录较少、每租户 Firecracker 开销可能意外。  _选项 3：CockroachDB_。迁移到 CockroachDB Serverless 或自托管...

=== mod (5 occurrences) ===
  c12_b06.md: ...内按 clustering 列排序做局部范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash mod N” 更好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：**...
  c12_b06.md: ...范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash mod N” 更好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个键的分区...
  c12_b06.md: ...好？  提示  hash-mod-N 增删节点会移动大部分数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个键的分区由 `hash(key) mod N` 决定。若 N 变化（增删节点），几乎所有键会映射到不同...
  c12_b06.md: ...数据；一致哈希只移动约 1/N。  **答案：** hash-mod-N 分片中，每个键的分区由 `hash(key) mod N` 决定。若 N 变化（增删节点），几乎所有键会映射到不同分区。$N=12 \to 13$ 时，12/13 的键会换...
  c12_b06.md: ...，约 1/13 的数据移动（新节点拥有的那段弧）。  运维含义：一致哈希下集群扩缩容可行（移动 8% 数据），hash-mod-N 不可行（移动 92% 数据）。每个现代分布式键值系统都使用某种形式的一致哈希或其衍生。  虚拟节点（vnodes）...

=== orchestrated (5 occurrences) ===
  c13_b06.md: ...地持久化；整个系统最终收敛到 “所有步骤成功” 或 “早期步骤已被补偿”。  两种 saga 协调模式：  (a) **Orchestrated saga** ：专门的 saga orchestrator 服务持有 saga 状态，依次调用每一步，失败时触发补偿。...
  c13_b06.md: ...事件。补偿通过 “回滚事件” 触发。无中央 orchestrator；步骤通过事件总线（Kafka、NATS）协调。  Orchestrated saga 更容易调试（单一真相位），但引入 orchestrator 作为焦点。Choreographed saga...
  c13_b07.md: ...#### 每种风格实例  **Orchestrated** —— Crescent 支付工作流（后文详细展开）：       PaymentOrchestrator (Coc...
  c30_b06.md: ...Metastable failure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健康状态。 * **Orchestrated saga（协调式 saga）**：专用协调器服务持有 saga 状态，依次调用每一步，失败时触发补偿。协调器状态本身是...
  c30_b14.md: ...paction)     ├── ch13_saga/     │   ├── orchestrator.py   — orchestrated saga with durable state (SQLite)     │   ├── choreographed....

=== notion (5 occurrences) ===
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人可同时编辑同一份文档，使用不同设备，有时离线，文档会收敛到一...
  c16_b03.md: ...、client ID 和指向前驱的指针。该结构对常见情况高度优化（顺序打字产生字符链，每个指向前一个）。  Yjs 在 Notion、Linear 等众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAssembly 实现...
  c16_b05.md: ...解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT）、Replicache（CRDT...
  c16_b09.md: ...导：Yjs。  对审计和历史重要的 Jira 风格应用，Automerge 是正确选择。对文档是焦点、历史是次要功能的 Notion 风格应用，Yjs 是正确选择。决策总是由工作负载驱动。...
  c16_b11.md: ...等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear、Notion、Glide、Logseq、Skiff 是示例。  2026 年新协作编辑器设计默认用 CRDT。现有基于 OT 的系统...

=== free (5 occurrences) ===
  c16_b01.md: ...使用不同设备，有时离线，文档会收敛到一致状态，而无需冲突解决弹窗。  使这成为可能的数学是 CRDT（Conflict-free Replicated Data Types，无冲突复制数据类型）。Yuki 使用的实现是 Yjs（团队主要选择）和 A...
  c16_b01.md: ...）。CRDT 出现后，数学处理冲突解决；实现小一个数量级。   ### 16.1 CRDT 是什么  Conflict-free Replicated Data Type 是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合并顺序如何...
  c30_b02.md: ...Preguiça, N., Baquero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M.,...
  c30_b02.md: ...框架。 * Kleppmann, M., & Beresford, A. R. (2017). "A Conflict-Free Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础。 * K...
  c30_b03.md: ..., M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * B...

=== bundle (5 occurrences) ===
  c16_b03.md: ...ust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更 “git-li...
  c16_b03.md: ...丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Glide 在浏览器运行，包括慢网络），且其文档模型比 Automerge 完整历史图更简单。   #...
  c16_b07.md: ...s 做到了早期 list CRDT 没做到什么？  提示  RGA：每个字符有前驱指针；树状。Yjs 针对顺序打字和小 bundle size 优化。  **答案：** RGA（Replicated Growable Array）把列表表示为图：每个字...
  c16_b07.md: ...ssembly 性能** ：合并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle size** ：Yjs ~80 KB，即使在慢网络下也足够小，适合浏览器。 (d) **垃圾回收** ：删除字符的 t...
  c16_b09.md: ...、更小。Automerge：历史更丰富。批量导入对两者都棘手。  **答案：**  _Yjs_ ：  * 优点：合并快、bundle 小、生产使用成熟、生态大（Notion、Linear）。 * 缺点：历史不透明（可以 replay update，但不...

=== const (5 occurrences) ===
  c16_b03.md: ...协作编辑器  实现如下：   #### 数据结构  每个文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')...
  c16_b03.md: ...文档是一个 Yjs Y.Doc。文档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const m...
  c16_b03.md: ...const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）...
  c16_b03.md: ...meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端...
  c16_b08.md: ...(d) 客户端断开连接后，其 awareness 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.getAwareness()      // 光标移动时：     awareness...

=== durable (5 occurrences) ===
  c16_b04.md: ...。重连时，它计算客户端与服务器状态差并只发送所需内容。   #### 持久化  权威文档以 update 序列形式存在 Durable Object 存储中。定期地（约每小时），update 被 _compacted_ 成快照：文档被序列化，历史 upd...
  c16_b11.md: ...ge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光...
  c17_b03.md: ...T）做页面浏览指标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（per-object 线性化）做文档元数据。注意：Cloudflare KV 在这里是错误原语——它全...
  c17_b11.md: ...义计数器服务（最终）+ Snowflake（分析）。Glide 用 Yjs（CRDT/强最终）+ Cloudflare Durable Objects（per-object 线性化）做文档元数据。“每个工作负载的正确系统” 是 2026 年复杂系统的默认...
  c30_b14.md: ...aga/     │   ├── orchestrator.py   — orchestrated saga with durable state (SQLite)     │   ├── choreographed.py  — choreographe...

=== websocket (5 occurrences) ===
  c16_b04.md: ...#### 同步  两种同步模式共存：  (a) **通过 WebSocket 实时同步** ：用户 actively 编辑时，其 update 被发送到服务器（带 Durable Objects...
  c16_b08.md: ...象：光标位置、用户名、颜色、当前选择）发布到 awareness 通道。  (b) awareness 状态通过同一个 WebSocket 传播给所有连接客户端，但存储在独立的 Yjs Awareness 对象中。  (c) 客户端根据收到的 awarene...
  c16_b10.md: ...OT 在离线场景更具吸引力。  **Q2.** 一个 LLM 被要求 “实现一个协作文档编辑器”。它产出的代码使用简单 WebSocket 广播 “把文档设为这个字符串”。指出该设计相比 CRDT 设计的三种失败模式。  **讨论：**  (a) **并发编...
  c16_b11.md: ...在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Loc...
  c28_b10.md: ...Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定性合并后的文档状态 | RGA 风格列表 CRDT + WebSocket 同步 + awareness 协议 | 在 CRDT 中存储临时光标位置；缺少定期压缩；分片逻辑与合并逻辑混淆 | |...

=== software (5 occurrences) ===
  c16_b05.md: ...### 16.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wi...
  c16_b05.md: ...6.5 Local-First Software  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中...
  c16_b11.md: ...reness 协议处理光标；定期 compaction 为快照。冲突解决自动；无合并对话框。  Local-First Software——Kleppmann 等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户...
  c30_b02.md: ...van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsky, M., &...
  c30_b03.md: ...van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 201...

=== issue (5 occurrences) ===
  c16_b09.md: ...在 2026 生产 benchmark 中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；Yjs 擅长。Automerge 的历史可启用 “给我看上周二的问题描述”。 * **状态变...
  c16_b09.md: ...。Automerge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CSV。两个库都能处理，但导入是结构操作，必须小心协调；CRDT 机制并非为批量加载优化。  推荐：  * 若审计...
  c16_b09.md: ...在 2026 生产 benchmark 中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；Yjs 擅长。Automerge 的历史可启用 “给我看上周二的问题描述”。 * **状态变...
  c16_b09.md: ...。Automerge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CSV。两个库都能处理，但导入是结构操作，必须小心协调；CRDT 机制并非为批量加载优化。  推荐：  * 若审计...
  c23_b03.md: ...Redis**：Redlock 问题（第18章主题）；各种故障转移 bug。 * **etcd**：旧版本有轻微一致性 issue；已纳入修复。  Jepsen 报告是任何分布式系统工程师的必读。它们是声明真实性的经验证据。...

=== flags (5 occurrences) ===
  c19_b03.md: ...置并重新配置（无需重启）。  (d) 对需要跨服务一致性的配置，配置存储使用共识。  示例：  * **Feature flags**：跨所有服务切换功能，无需重新部署。 * **Rate limits**：根据负载动态调整限速。 * **路由规则*...
  c19_b06.md: ...示例：feature flags。配置存储保存如 `feature_flags/new_checkout_flow = true` 的 key。所有尊重...
  c19_b06.md: ...`feature_flags/new_checkout_flow = true` 的 key。所有尊重 feature flags 的服务 watch `feature_flags/` 前缀。当运维更新 flag（`etcdctl put featu...
  c19_b08.md: ...障转移导致的中断是运维痛点；实质降低它会明显改善用户体验。  **Q3.** 一个团队用 etcd 做 feature flags。flag `new_checkout_flow` 被切换为 `true`。走一遍每个服务会发生什么，以及可能出错的地方...
  c19_b11.md: ...衰退。  Config-as-data with watch streams 把服务发现推广到动态配置：feature flags、流量规则、限速、secret。模式（存在 etcd/Consul 中，watch 变更，动态重新配置）是 2026 年...

=== revision (5 occurrences) ===
  c19_b04.md: ...康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revision 号 fencing token 的线性化锁获取。  * **Consul 做跨区域服务网格**：EU-West 的服务...
  c19_b09.md: ...错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 fl...
  c19_b09.md: ...事件期间崩溃会错过通知。重启时它读取当前状态（这是 etcd watch + revision 模式）。实现必须使用 `revision` 干净恢复。  (e) **请求中途 flag 翻转**：请求执行中途 flag 翻转可导致请求行为不一致（开始时 f...
  c19_b09.md: ...flag 值并在整个请求中使用该快照。  对 Crescent feature-flag 系统：每个服务用 etcd revision 号缓存 flag 值；watch 事件更新缓存。请求使用缓存，最多毫秒级过期。该模式对常见情况稳健；上述边角情况由应用...
  c28_b14.md: ...leader 唯一性；etcd 的 Raft 确保一次只有一个主库。Fencing token 未原生使用；etcd revision 在编排层起类似作用。 * **故障**：异步复制可能丢失主库传播前最近写入。团队必须接受某 RPO；可通过 `sync...

=== datadog (5 occurrences) ===
  c20_b03.md: ...e 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准  2026 年，O...
  c20_b03.md: ...P 客户端、数据库驱动、消息队列）。 * 可导出到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 Ope...
  c20_b09.md: ...的团队只是追踪做错了。”  **讨论：** 辩护：有了 OpenTelemetry、成熟的追踪后端（Honeycomb、Datadog、Tempo）和良好的工程纪律，分布式追踪提供调试大多数生产问题所需的可见性。挣扎的团队要么没投资追踪，要么没学会读 t...
  c20_b09.md: ...。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的失败：产出与特定厂商耦合的代码。团队现在被锁定在 DataDog；换到另一追踪后端（Honeycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenT...
  c20_b09.md: ...rt_as_current_span("operation"):         do_work()   上述代码可与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter...

=== calls (5 occurrences) ===
  c21_b02.md: ...ses<br/>(e.g. 30s timer)         HalfOpen --> Closed: probe calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: p...
  c21_b02.md: ...eed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/>track succes...
  c21_b02.md: ...HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/>track success/failure rate         Open: Al...
  c21_b02.md: ...o through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contact         HalfOpen: Limi...
  c21_b02.md: ...;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Closed**：...

=== aguilera (5 occurrences) ===
  c21_b10.md: ...算、加熔断器、预热容量、更新 runbook。  第22章进入更难的事故类别：亚稳态故障与灰色故障。Bronson 和 Aguilera 2021 年的论文形式化了亚稳态；灰色故障（通过健康检查但变慢的节点）是成熟分布式系统的运维噩梦。  ---...
  c22_b02.md: ...康检查应匹配标准，而不是最低门槛。   ### 22.3 亚稳态故障  Nathan Bronson 和 Marcos Aguilera 2021 年的论文引入 _metastable failure_ 的形式概念：一种小触发器把系统推入自维持故障状态的故...
  c22_b03.md: ...系统处于损坏状态并限流以允许恢复。这可能需要外部协调（SRE 团队观察到亚稳态并应用手动限流）。  Bronson 和 Aguilera 的论文列举了模式和防御；它是 2026 年 SRE 风格工程师的必读。   ### 22.4 灰色与亚稳态结合  灰色...
  c22_b10.md: ...得可见性，(c) 准备干预机制（限流、重启、故障转移）。  2026 现实：SRE 实践围绕亚稳态成熟。Bronson-Aguilera 论文提供框架；公司有内部亚稳态 playbook；“系统卡住” 的 runbook 是标准。随着意识提高，灾难性亚稳态...
  c30_b03.md: ...n for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failu...

=== bronson (5 occurrences) ===
  c21_b10.md: ...r 规则、降低重试预算、加熔断器、预热容量、更新 runbook。  第22章进入更难的事故类别：亚稳态故障与灰色故障。Bronson 和 Aguilera 2021 年的论文形式化了亚稳态；灰色故障（通过健康检查但变慢的节点）是成熟分布式系统的运维噩梦...
  c22_b02.md: ...健康，而不是在能回答廉价探测时。健康检查应匹配标准，而不是最低门槛。   ### 22.3 亚稳态故障  Nathan Bronson 和 Marcos Aguilera 2021 年的论文引入 _metastable failure_ 的形式概念：一种...
  c22_b03.md: ...载感知限流**。识别系统处于损坏状态并限流以允许恢复。这可能需要外部协调（SRE 团队观察到亚稳态并应用手动限流）。  Bronson 和 Aguilera 的论文列举了模式和防御；它是 2026 年 SRE 风格工程师的必读。   ### 22.4 灰...
  c22_b10.md: ...馈循环加仪器以获得可见性，(c) 准备干预机制（限流、重启、故障转移）。  2026 现实：SRE 实践围绕亚稳态成熟。Bronson-Aguilera 论文提供框架；公司有内部亚稳态 playbook；“系统卡住” 的 runbook 是标准。随着意识...
  c30_b03.md: ...e coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu, X., Yan, B., & Xu, L. (2021). "Me...

=== fireworks (5 occurrences) ===
  c24_b02.md: ...LLM 采用迅速。到 2026 年，它是最广泛部署的开源推理 serving 系统。Anyscale、Together、Fireworks 和许多其他推理公司使用 vLLM（或 vLLM 衍生系统）作为核心引擎。...
  c25_b11.md: ...located 性能。  第26章结束第八部分，讲负载均衡和投机解码作为分布式协议。Anyscale/Together/Fireworks 路由问题；speculative decoding（draft model + verify model）作为多机协议...
  c26_b01.md: ...正常）。差的路由决策对用户可见。  机会：考虑这些属性的路由决策可带来显著效率提升。Anyscale、Together、Fireworks 都发布过相关内容。...
  c26_b06.md: ...uum：他们规模下分布式 speculative decoding 合理。已发布的 Anyscale、Together、Fireworks 部署确认：在规模上，分布式获胜。  **Q4.** 什么是多租户推理集群的 weighted fair schedul...
  c30_b11.md: ...*替代**：企业团队用 NVIDIA NIM；若团队想避免运维复杂度，可用 Anyscale / Together / Fireworks 等托管替代。...

=== tgi (5 occurrences) ===
  c24_b05.md: ...间空闲）。  Continuous batching 已成为生产推理 serving 的默认。vLLM、SGLang、TGI 等都使用变体。相比朴素 serving，吞吐提升通常 2-5 倍。   ### 24.8 练习...
  c24_b10.md: ...ence（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产部署包含什么。  **讨论：** LLM 的基础 TGI 部署是起点，但遗漏许多 2026 生产特性：  (a) **PagedAttention 引擎**：vLLM（或 SG...
  c24_b10.md: ...：  (a) **PagedAttention 引擎**：vLLM（或 SGLang 适合工作负载）是 2026 默认。TGI 合理，但 vLLM 对大多数工作负载吞吐更好。  (b) **Continuous batching**：应启用（现代...
  c24_b10.md: ...a 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队列深度、p...
  c24_b10.md: ...。  (h) **故障处理**：GPU 故障、节点故障、模型加载故障。部署必须优雅处理每种。  正确答案：“基础部署用 TGI 或 vLLM 加 tensor parallelism。2026 生产还要包含 [以上列表]。以下是更完整配置……”...

=== attention (5 occurrences) ===
  c24_b05.md: ...t 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个请求的 GPU-秒。更好的 attention（PagedAttention、RadixAttention）、更好的 batching（continuous batc...
  c25_b01.md: ...*：处理整个输入 prompt。对 1000-token prompt，GPU 计算所有 1000 个 token 的 attention。这是矩阵-矩阵乘法；瓶颈是 GPU 计算（TFLOPs）。内存带宽很少是限制；延迟随 prompt 长度缩放。  **...
  c25_b01.md: ...生成输出。对每个 token，GPU 读取整个上下文（prompt + 已生成 token）的 KV cache，计算 attention，采样下一个 token。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase...
  c25_b05.md: ...优硬件。  **答案：** Prefill 处理输入 prompt。GPU 并行计算所有 prompt token 的 attention：矩阵-矩阵乘法。瓶颈是计算（TFLOPs）。内存带宽很少是限制。  Decode 逐 token 生成输出。对每个 t...
  c25_b05.md: ...限制。  Decode 逐 token 生成输出。对每个 token，GPU 读取整个上下文的 KV cache，计算 attention（仅新 token），并采样。这是矩阵-向量乘法。瓶颈是内存带宽（加载 KV cache）。  不对称意味着一张 GPU...

=== roi (5 occurrences) ===
  c24_b08.md: ...推理成本优化是 2026 年的专业领域。一个小团队投入 6 个月可为每月 100 万美元以上推理预算每年节省数百万美元。ROI 很直接。  **Q2.** 某团队在企业部署中选择 vLLM 与 NVIDIA NIM。他们在 Kubernetes...
  c25_b08.md: ...投入：2 名高级工程师全职做分离层 6 个月。  总投入：约 50 万美元工程时间换取约 400-500 万美元年节省。ROI 约 10 倍。  **Q2.** 某团队推理集群工作负载高度可变：某些时段以长 prompt（3000+ token）...
  c25_b11.md: ...e 节点；高带宽 RDMA 网络；6 个月迁移；利用率从 60% 到 85%；年节省 400-500 万美元。投入巨大；ROI 巨大。  延迟成本：首 token 延迟增加约 50 ms（KV cache 传输）。对交互式工作负载可感知；缓解措施...
  c26_b04.md: ...省：原 1400 万美元年运行费中节省 500-600 万美元。工程投入：薪水和基础设施改动约 200 万美元。第一年 ROI 约 3 倍；后续持续节省。  更深观察：2026 年推理成本优化相当于 2000 年代的数据库查询优化。两者都是深度专...
  c26_b04.md: ...省。  更深观察：2026 年推理成本优化相当于 2000 年代的数据库查询优化。两者都是深度专业领域；都有可衡量的大 ROI；都需要持续投入。未来 5 年还会进一步演进；保持前沿的团队继续捕获收益。   ### 26.8 练习...

=== rollback (5 occurrences) ===
  c25_b08.md: ...ed 基线高 20%（目标收益）。 * 没有超过团队事故响应能力的新故障模式。  **风险缓解**：  * 每阶段都有 rollback 计划：指标回退则回到 co-located。 * 客户沟通：通知大客户架构变化；为需要稳定延迟的客户提供 opt-ou...
  c27_b12.md: ...5. **谎言 5（部分故障）**：四个顺序调用（bank、db、notification、ledger）没有 rollback。若 bank 成功后 db 失败，用户已在 bank 扣款但 DB 未扣；后续重试会重复扣款。    6. **谎言...
  c28_b06.md: ...布前检查清单**：两周前混沌 game day；所有 action items 已关闭。容量测试通过。客户迁移计划已审。Rollback 计划已审。客服已通报。  (b) **06:00 UTC**：流量逐步切到新系统。按金丝雀计划：1% 流量到新系统；观...
  c28_b10.md: ...+ 稳态 SLI | 验证防御；记录学习；行动项 | 演练前设计 + 当天执行 + 无责复盘 | 跳过影响半径控制；无 rollback 计划；发现 bug 却继续后续 game day 而不修复 | | 12 | vLLM 分离集群 | Ch.25 §2...
  c30_b06.md: ...horeographed saga（编排式 saga）**：每一步发布完成事件；后续步骤订阅；无中央协调器。补偿通过 “rollback 事件” 发生。见第13章 §13.6.5。 * **Circuit breaker（熔断器）**：当下游失败率超过阈值...

=== diff (5 occurrences) ===
  c27_b06.md: ...盖提示**：新的分布式代码函数没有相关测试文件；关键路径 mutation-test 分数低。  这些是可分解为 “在 diff 中找这个正则” 的检查清单项。AI 审查者几秒处理。作为 pre-PR 检查，在人类审查者看到 diff 前过滤掉明显...
  c27_b06.md: ...分解为 “在 diff 中找这个正则” 的检查清单项。AI 审查者几秒处理。作为 pre-PR 检查，在人类审查者看到 diff 前过滤掉明显失败。   #### AI 审查者可靠遗漏什么  检查清单其余项需要 _判断_，而非模式识别。这些是人类分...
  c27_b07.md: ...项：timeout、retry-budget 注解、idempotency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false positive，作者用一行理由 dismiss。...
  c27_b07.md: ...人类按清单审查（分钟级，每个分布式 PR）**：  * 受过六个谎言训练的人类审查者打开 PR。 * AI 批注已在 diff 中；人类阅读并决定哪些要处理。 * 人类应用 AI 无法做的上下文判断：哪把锁用于正确性、哪个不变量必须成立、哪个设计...
  c29_b02.md: ...取 trace+logs+metrics 并为人类验证提出根因。 * **混沌工程与 AI 集成**：AI 基于 PR diff 提出 game-day 场景；人类接受或扩展。  第27章 §27.10 的三层架构（AI 审查 → 人类审查 → 混...

=== crdbe (5 occurrences) ===
  c28_b03.md: ...[API Service]             REDISE[Redis Cluster]             CRDBE[CockroachDB nodes]             STRATUSE[Stratus DB<br/>coor...
  c28_b03.md: ...CF --> APIW2         APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE         AP...
  c28_b03.md: ...APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raf...
  c28_b03.md: ...T         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CD...
  c28_b03.md: ...CRDBE <-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW         TB -.->|"CDC nightly"| SNO...

=== efficient (5 occurrences) ===
  c30_b02.md: ...lman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys_. * Spiegelman, A., et al. (2022)....
  c30_b03.md: ...— 第24-26章）  * Kwon, W., Li, Z., Zhuang, S., et al. (2023). "Efficient Memory Management for Large Language Model Serving with Pag...
  c30_b03.md: ...tion." _SOSP_. — vLLM。 * Zheng, L., et al. (2023). "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAt...
  c30_b03.md: ..." — RadixAttention。 * Patel, P., et al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. —...
  c30_b03.md: ...rmers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基础，作为前瞻话题引用（§29.1；深度覆盖推迟到 _Softwar...

=== fastly (5 occurrences) ===
  c30_b09.md: ...跨区域回滚时会发生什么"；未覆盖到 failover-without-the-fix 场景。   #### E.7 — Fastly 全球 CDN 故障，2021-06-08  * **触发**：客户推送了一个有效配置，触发了 Fastly 基于 Va...
  c30_b09.md: ...E.7 — Fastly 全球 CDN 故障，2021-06-08  * **触发**：客户推送了一个有效配置，触发了 Fastly 基于 Varnish 的 CDN 平台中此前未知的软件 bug。该 bug 导致配置解析器在特定输入模式上崩溃。 *...
  c30_b09.md: ...台中此前未知的软件 bug。该 bug 导致配置解析器在特定输入模式上崩溃。 * **放大**：坏配置被分发到全球所有 Fastly CDN 节点；每个节点崩溃；整个 CDN 同时离线。 * **时间线**：全球中断约 1 小时；主要网站（Reddit...
  c30_b09.md: ...；主要网站（Reddit、Stack Overflow、Amazon、BBC）受影响。 * **恢复**：客户回滚配置；Fastly 节点恢复。 * **章节映射防御**：     * Ch.19（config-as-data，watch stream...
  c30_b11.md: ...S 原生、托管）或 Cassandra/ScyllaDB（开源、多云）。 * **CDN**：Cloudflare 或 Fastly 用于静态内容路径。 * **缓存**：区域 Redis 或 Memcached 用于热读。 * **一致性策略**：p...

=== error (4 occurrences) ===
  c01_b03.md: ...对话的节点就是它所认为的那个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否有任何代码路径假设“这不会...
  c01_b03.md: ...个节点？   3. **这个系统在失败上说谎了吗？** 它是否只有 happy path 和 error path，且 error path 是不常见的情况？它是否把重试视为幂等，却没有证明？它是否有任何代码路径假设“这不会发生”？  如果你能回答这...
  c13_b10.md: ...00-150 ms 端到端）。  若两笔事务竞用同一账户（罕见但可能），一笔获胜；另一笔拿到 serializable error 并重试。CockroachDB 的 serializable 隔离保证正确性；应用代码必须准备好重试（标准模式是带指数...
  c27_b01.md: ...领导者” 而不经协议强制唯一性的代码是脆弱的。  (c) **故障**：部分故障是例外。只有 happy path 和 error path 的代码把故障当作罕见；现实是部分故障是默认。  Thread C 的六个谎言，作为贯穿第1-2章的前瞻引用（...

=== collector (4 occurrences) ===
  c01_b05.md: ...1 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机...
  c01_b05.md: ...ervability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTelemetry Collector 文档。 * **联邦学习与隐私保护计算** ：差分隐私、安全多方计算、分布式 ML 中的同态加密。它们在 2026 年...
  c01_b05.md: ...1 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机...
  c01_b05.md: ...ervability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTelemetry Collector 文档。 * **联邦学习与隐私保护计算** ：差分隐私、安全多方计算、分布式 ML 中的同态加密。它们在 2026 年...

=== ddia (4 occurrences) ===
  c01_b07.md: ...话；完整弧线预计 12-16 周。每章后做概念检测练习；第三部分后做应用问题；每部分末尾做深入思考问题。  * **有 DDIA 背景的后端 / 基础设施工程师** ：略读第2章（CAP/PACELC 重述）和第5章（复制），因为它们与 DDIA...
  c01_b07.md: ...DDIA 背景的后端 / 基础设施工程师** ：略读第2章（CAP/PACELC 重述）和第5章（复制），因为它们与 DDIA 重叠。精读第3章（HLC 补充 DDIA 的时钟章节）、第6章（DDIA 未深入的多区域）、第9章（DDIA 之上进一...
  c01_b07.md: ...** ：略读第2章（CAP/PACELC 重述）和第5章（复制），因为它们与 DDIA 重叠。精读第3章（HLC 补充 DDIA 的时钟章节）、第6章（DDIA 未深入的多区域）、第9章（DDIA 之上进一步展开的 Raft）、第10章（BFT，超...
  c01_b07.md: ...时钟章节）、第6章（DDIA 未深入的多区域）、第9章（DDIA 之上进一步展开的 Raft）、第10章（BFT，超出 DDIA）、第11章（诊断方法）、第13章（包括 saga §13.6.5 的现代事务）、第14章（NewSQL 全景）、第24...

=== kernel (4 occurrences) ===
  c06_b03.md: ...TCP 的 RDMA；较少见，但因容忍有损网络而更容易部署。  性能：RDMA 延迟约 1-2 微秒（同数据中心），而 kernel-bypass TCP 约 50-100 微秒。内核不参与其中；NIC 处理数据移动。  用途：  * **分布式内存数...
  c06_b06.md: ...由适用于 ML 训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫秒级或更高的通用应用服务，kernel-bypass + TCP 已足够。...
  c06_b07.md: ...由适用于 ML 训练（每步梯度交换）和分布式内存数据库（关键路径远程内存访问）。对延迟预算为毫秒级或更高的通用应用服务，kernel-bypass + TCP 已足够。...
  c06_b12.md: ...带宽。RDMA 把同数据中心延迟降到个位数微秒，对存储系统和 ML/推理工作负载有用。对大多数应用层服务，网络不是瓶颈；kernel-bypass TCP 足够。  延迟预算方法（先定目标、减固定成本、把余量分配到每跳）能在部署前发现不可行设计。Cre...

=== series (4 occurrences) ===
  c01_b05.md: ...优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Series IV Vol 3（可观测性）的主题。参考：Sridharan _Distributed Systems Observa...
  c01_b05.md: ...第2版，2020）。 * **应用层协议深度** ：REST API 设计（在 _Software at Scale_ Series IV Vol 1 _APIs_ 中覆盖）、gRPC schema 演进、OpenAPI 工具链。第19章会提到服务发现...
  c20_b01.md: ...跨共识边界的 trace context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vol 3（可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什么需要分布式追踪  单体...
  c30_b19.md: ...作为 post-pipeline 润色的一部分生成。_ _QBooks · A Socratopia Learning Series Publication_  * * *  ---...

=== select (4 occurrences) ===
  c01_b12.md: ...之间的差距。  **答案：** 这里的纪律比任何具体答案都重要。一个工作示例：Postgres 读者若假设“如果我先 `SELECT now()`，再 `INSERT INTO log (created_at) VALUES (now())`，插入行的...
  c01_b12.md: ...ERT INTO log (created_at) VALUES (now())`，插入行的 timestamp 会与 SELECT 一致”，会惊讶地发现 Postgres 中的 `now()` 返回的是 _事务开始时间_ ，而非调用时刻的 wall-...
  c13_b10.md: ...分录入账）。     BEGIN TRANSACTION;       -- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       --...
  c13_b10.md: ...West（经美国大陆路由约 11,000 km）约 200 ms。US-East 对 Adaeze 最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 U...

=== python (4 occurrences) ===
  c02_b01.md: ...让 LLM 写一个分布式库存服务，它会产出 200 行看起来干净的 Python。代码旁的说明写道：“我们使用强一致性数据库（CockroachDB），以确保任何商品都不会超卖。这让我们按 CAP 看...
  c03_b01.md: ...调整），也不会跳跃前进。它们不与 wall-clock 时间绑定；其值只有在作差时才有意义。大多数现代语言同时暴露两者：Python 中 `time.time()` 是 wall，`time.monotonic()` 是单调；Go 中 `time.No...
  c08_b10.md: ...s”，Paxos 家族在运维上仍然活跃。协议家族的存在比简单名字匹配更广泛。  **Q2.** 一个 LLM 被要求用 Python 实现 Paxos，产出 200 行代码。在不运行的情况下，指出任何 LLM 生成的 Paxos 实现中可能存在的三类...
  c09_b10.md: ...ft 成为新系统的正确默认。  **Q2.** 一个 LLM 被要求“为分布式计数器实现 Raft”。输出 300 行 Python。指出除了第8章 Paxos 等价 bug 之外，任何 LLM 生成的 Raft 实现中可能存在的三类结构性 bug。...

=== pod (4 occurrences) ===
  c02_b07.md: ...和。  _网络是安全的。_ 示例：内部服务接受来自 VPC 的任意调用者，因为“VPC 是私有的”。集群中一个被攻破的 pod 现在可以作为任意调用者行动，因为没有按调用者认证。  _拓扑不会变。_ 示例：配置文件中硬编码服务依赖的 IP 地址，...
  c19_b03.md: ...* ：“headless” Kubernetes service 没有 cluster IP；客户端解析服务名得到所有 pod IP。Kubernetes DNS 插件透明处理。  对成员稳定的服务（数据库、有状态服务），DNS 通常足够。对成员...
  c19_b04.md: ...端解析 `payment.crescent.svc.cluster.local`；kube-proxy 负载均衡到健康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd 集群提供带 revis...
  c23_b02.md: ...23.4 工具  现代混沌工程工具：  (a) **Chaos Mesh**：Kubernetes 原生混沌工程。注入 pod 故障、网络故障、磁盘故障、时间偏移。广泛采用。  (b) **Litmus**：另一个 Kubernetes 混沌框架...

=== events (4 occurrences) ===
  c03_b02.md: ...里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Distributed System"（1978），必读。关键洞见是：给分布式系统中的事件排序不需要同步时钟...
  c20_b01.md: ...时间戳和时长。 * 服务名和操作名。 * 状态（成功、错误、代码）。 * Attributes（自定义键值对）。 * Events（span 内的时间戳日志条目）。  一个典型请求可能产生 50-500 个 spans，取决于服务深度。存储所有 tr...
  c20_b05.md: ...可带错误码。 * **Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元数据）。 * **Events**：span 内的时间戳日志条目（状态变化、错误、里程碑）。  单个 trace 的所有 spans 通过 trace...
  c30_b01.md: ...文。 * Lamport, L. (1978). "Time, Clocks, and the Ordering of Events in a Distributed System." _Communications of the ACM_. — 第3...

=== dotted (4 occurrences) ===
  c03_b03.md: ...旧条目，而这有自身的正确性考量。  实践中，完整向量时钟用于小 N 的 CRDT 风格系统（第16章）。Riak 使用 dotted version vectors。现代系统常使用下面的混合替代方案。...
  c03_b11.md: ...。向量增长到每个消息 10,000 个条目，带宽成本不可接受。提出两种替代方案并讨论权衡。  提示  HLC 恒定扩展。dotted version vectors 压缩稀疏向量。若跨租户交互罕见，每会话或每租户向量有效。  **答案：** 两种替代方...
  c03_b11.md: ...据的服务数，通常几十而非 10,000。权衡：协议层需要“租户”或“会话”概念，对真正跨租户的操作会失效。  第三条路，dotted version vectors（DVV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃）...
  c04_b10.md: ...实践的策略。  提示  压缩稀疏向量。按更小组划分，向量大小受组规模限制。  **答案：** 两种策略：  (a) **Dotted version vectors（DVV）** 或稀疏向量。若大多数条目为零（大多数进程最近未贡献消息），只存非零条目。...

=== vectors (4 occurrences) ===
  c03_b03.md: ...。  实践中，完整向量时钟用于小 N 的 CRDT 风格系统（第16章）。Riak 使用 dotted version vectors。现代系统常使用下面的混合替代方案。...
  c03_b11.md: ...00 个条目，带宽成本不可接受。提出两种替代方案并讨论权衡。  提示  HLC 恒定扩展。dotted version vectors 压缩稀疏向量。若跨租户交互罕见，每会话或每租户向量有效。  **答案：** 两种替代方案：  (a) HLC。用 HL...
  c03_b11.md: ...,000。权衡：协议层需要“租户”或“会话”概念，对真正跨租户的操作会失效。  第三条路，dotted version vectors（DVV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃），Riak 使用过。对活跃消息系...
  c04_b10.md: ...疏向量。按更小组划分，向量大小受组规模限制。  **答案：** 两种策略：  (a) **Dotted version vectors（DVV）** 或稀疏向量。若大多数条目为零（大多数进程最近未贡献消息），只存非零条目。Riak 使用过类似技术。成本是...

=== version (4 occurrences) ===
  c03_b03.md: ...自身的正确性考量。  实践中，完整向量时钟用于小 N 的 CRDT 风格系统（第16章）。Riak 使用 dotted version vectors。现代系统常使用下面的混合替代方案。...
  c03_b11.md: ...个消息 10,000 个条目，带宽成本不可接受。提出两种替代方案并讨论权衡。  提示  HLC 恒定扩展。dotted version vectors 压缩稀疏向量。若跨租户交互罕见，每会话或每租户向量有效。  **答案：** 两种替代方案：  (a)...
  c03_b11.md: ...常几十而非 10,000。权衡：协议层需要“租户”或“会话”概念，对真正跨租户的操作会失效。  第三条路，dotted version vectors（DVV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃），Riak 使用...
  c04_b10.md: ...提示  压缩稀疏向量。按更小组划分，向量大小受组规模限制。  **答案：** 两种策略：  (a) **Dotted version vectors（DVV）** 或稀疏向量。若大多数条目为零（大多数进程最近未贡献消息），只存非零条目。Riak 使用过...

=== kulkarni (4 occurrences) ===
  c03_b05.md: ...### 3.5 混合逻辑时钟（HLC）  向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wa...
  c03_b05.md: ...组 (l, c)，其中 l 是 wall-clock 时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t...
  c30_b01.md: ...istributed System." _Communications of the ACM_. — 第3章必读。 * Kulkarni, S. S., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2...
  c30_b14.md: ...N)     │   └── hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/     │   ├── bes...

=== now (4 occurrences) ===
  c03_b06.md: ...三个进程；假设它们的 wall clock 彼此在 10 ms 以内。  Step | Process | Wall now | HLC after ---|---|---|--- 1 | P_1 | 100 | (100, 0) 2 | P_...
  c03_b07.md: ...除它。TrueTime API（2012 年 Spanner 论文描述）将时间暴露为一个 _区间_ ：每次调用 `TT.now()` 返回一对 `[earliest, latest]`，真实 wall-clock 时间保证落在该区间内。  Spa...
  c03_b07.md: ...个事务分配一个提交时间戳，保证该时间戳大于系统过去任何已提交事务。协议如下：    1. 选择提交时间戳 s = TT.now().latest（尽可能晚的“现在”）。   2. **等待** 直到 TT.now().earliest > s。这...
  c03_b07.md: .... 选择提交时间戳 s = TT.now().latest（尽可能晚的“现在”）。   2. **等待** 直到 TT.now().earliest > s。这等待掉不确定性区间；一旦成立，真实 wall-clock 时间可证明已越过 s，因此不...

=== amazon (4 occurrences) ===
  c03_b07.md: ...见  TrueTime 需要 GPS、原子钟、软件栈和运维纪律。大多数公司数据中心里没有 GPS。AWS 等云厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的...
  c13_b05.md: ...e 分布式数据库使用 HLC + 偏移上界。成本可变（取决于争用），但运维足迹小于 TrueTime。AWS 现已提供 Amazon Time Sync Service 的 GPS-backed clock-bound，正把 TrueTime-like...
  c30_b03.md: ...* Crescent 事后复盘（本书全文）。 — 六个谎言的工作示例。   #### 前瞻（第29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and Cl...
  c30_b03.md: ...#### 前瞻（第29章 — 后记）  * Amazon Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，包括 2024 年在 EC2 上引入微秒级有...

=== latest (4 occurrences) ===
  c03_b07.md: ...panner 论文描述）将时间暴露为一个 _区间_ ：每次调用 `TT.now()` 返回一对 `[earliest, latest]`，真实 wall-clock 时间保证落在该区间内。  Spanner 在每个数据中心部署 GPS 接收器和原子钟，...
  c03_b07.md: ...，而不是忽略它。NTP 给你“大概 10 ms 内”；TrueTime 给你“真实时间可证明在 `[earliest, latest]` 内”。差别就是偶尔出现线性化违反的系统与永不出现的系统之间的差别。...
  c27_b11.md: ...s='failed', reason='busy')          try:             # Read latest balance             balance = db.query(                 "SE...
  c30_b02.md: ...CCS_. * Buchman, E., Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT consensus." _arXiv:1807.04938_. — Tendermint。...

=== deliver (4 occurrences) ===
  c04_b02.md: ...send(peer, msg)             upper_layer.deliver(msg)   性质（在尽力广播性质之外）：  * **Agreement** ：若任一正确进程交付，所有正确进程都交付...
  c04_b03.md: ...red:             delivered.add(msg)             upper_layer.deliver(msg)   性质（在可靠广播之外）：  * **Uniformity** ：若 _任何_ 进程交付了一条消息（正确或...
  c04_b03.md: ...V[deliverable.sender] += 1             upper_layer.deliver(deliverable.msg)   性质（在可靠广播之外）：  * **Causal order** ：若 broa...
  c04_b09.md: ...return         delivered.add(msg)         upper_layer.deliver(msg)         for peer in all_processes:             if peer...

=== flooding (4 occurrences) ===
  c04_b02.md: ...溃，该消息只有原始接收者重广播时才会到达所有正确接收者。如果唯一收到它的接收者在重广播前也崩溃，消息就丢失了。  通过 flooding 实现可靠广播的代价是在全连接拓扑下每条消息 $O(N^2)$ 条消息。存在基于 gossip 的变体，平均以 $O(N...
  c04_b09.md: ...key（通常是 transaction ID）幂等。重复扣款 bug 是因为幂等检查没有强一致。  **Q2.** 用 flooding 写可靠广播的伪代码。走查一个 4 节点示例，其中一个接收者收到消息后、重广播前崩溃。消息还能到达其余正确接收者吗？...
  c04_b09.md: ...广播的伪代码。走查一个 4 节点示例，其中一个接收者收到消息后、重广播前崩溃。消息还能到达其余正确接收者吗？  提示  flooding 下，发送者发给所有 peer。只要崩溃节点之外至少有一个 peer 收到并重广播，消息就会到达全部。  **答案：**...
  c30_b14.md: ...oop)     │   ├── reliable.py       — reliable broadcast via flooding     │   └── causal.py         — causal broadcast with vecto...

=== deliverable (4 occurrences) ===
  c04_b03.md: ...try_deliver_from_buffer():         while True:             deliverable = None             for (msg, V_msg, sender) in buffer:...
  c04_b03.md: ...all(V_msg[i] <= V[i] for i in others):                     deliverable = (msg, V_msg, sender)                     break...
  c04_b03.md: ...sg, V_msg, sender)                     break             if deliverable is None:                 return             # 交付...
  c04_b03.md: ...# 交付             buffer.remove(deliverable)             V[deliverable.sender] += 1             upper_layer.deliver(deliverable.ms...

=== chandra (4 occurrences) ===
  c04_b04.md: ...播可解共识（每个进程广播自己的提议；第一个交付的提议即为决定值）。给定共识可实现原子广播（每个消息槽运行一次共识实例）。Chandra 和 Toueg 在形式化证明了这一等价性。  因此原子广播的实现就是共识的实现。这里先命名算法（Paxos、Raft、...
  c04_b07.md: ...消息槽运行一次共识实例。要广播某消息，在下一个可用槽提议它。所有进程按槽顺序看到相同的决定值序列，从而得到全序交付。  Chandra 和 Toueg 在 1996 年关于故障检测器的论文中形式化证明了这一等价性，意味着原子广播“和共识一样难”，并继承其...
  c07_b02.md: ...有限；多数生产系统用部分同步。  (c) **故障检测器** ：假设有一个神谕告诉你哪些进程已故障。协议用神谕裁决推进。Chandra-Toueg 的故障检测器层次形式化；能解共识的最弱故障检测器叫 \Omega（最终某个正确进程被标识为 leader）...
  c30_b01.md: ...e Distributed Programming_ (2nd ed.). Springer. — 广播层级参考。 * Chandra, T. D., & Toueg, S. (1996). "Unreliable failure detectors f...

=== toueg (4 occurrences) ===
  c04_b04.md: ...广播自己的提议；第一个交付的提议即为决定值）。给定共识可实现原子广播（每个消息槽运行一次共识实例）。Chandra 和 Toueg 在形式化证明了这一等价性。  因此原子广播的实现就是共识的实现。这里先命名算法（Paxos、Raft、ZAB、HotS...
  c04_b07.md: ...例。要广播某消息，在下一个可用槽提议它。所有进程按槽顺序看到相同的决定值序列，从而得到全序交付。  Chandra 和 Toueg 在 1996 年关于故障检测器的论文中形式化证明了这一等价性，意味着原子广播“和共识一样难”，并继承其极限（纯异步下...
  c07_b02.md: ...统用部分同步。  (c) **故障检测器** ：假设有一个神谕告诉你哪些进程已故障。协议用神谕裁决推进。Chandra-Toueg 的故障检测器层次形式化；能解共识的最弱故障检测器叫 \Omega（最终某个正确进程被标识为 leader）。  三种逃...
  c30_b01.md: ...ramming_ (2nd ed.). Springer. — 广播层级参考。 * Chandra, T. D., & Toueg, S. (1996). "Unreliable failure detectors for reliable dist...

=== application (4 occurrences) ===
  c04_b05.md: ...确性证明。   ### 4.8 你的应用需要哪一层？  正确层是满足应用正确性要求的最弱层，因为每一层更强层都更贵。  Application | Required layer | Reason ---|---|--- DNS 更新传播 | best-effor...
  c05_b09.md: ...跟踪。  代价：慢读路径上的等待或拒绝延迟。收益：任何会话的 read-your-writes 自动生效，无需 per-application 路由逻辑。...
  c19_b09.md: ...watch 事件但应用慢（如正处理重请求）。flag 效果延迟。对多数工作负载可接受；对时间敏感功能，监控 “flag-application latency” 有用。  (d) **服务错过 watch**：服务在 watch 事件期间崩溃会错过通知。重启时它...
  c22_b08.md: ...检测被动观察可能遗漏的灰色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netf...

=== stream (4 occurrences) ===
  c04_b06.md: ...r 排序写入；副本按相同顺序应用。这是限定在一个分区内的原子广播。 * **NATS JetStream** 提供基于 stream 的有序交付，至少一次。根据配置接近因果或更好。 * **etcd** 通过 Raft 提供全序复制。状态机命令的原子广...
  c04_b06.md: ...* **etcd** 通过 Raft 提供全序复制。状态机命令的原子广播。 * **Redis Streams** 在 stream 内提供有序交付；消费者组提供 at-least-once。 * **CockroachDB 的 Rangefeed**...
  c18_b11.md: ...认（替代 ZooKeeper）、Consul、基于 DNS 的发现，以及 config-as-data + watch stream 模式。这些是让其余分布式基础设施运行的元系统。  ---...
  c30_b11.md: ...sink。  * **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-onc...

=== durability (4 occurrences) ===
  c05_b01.md: ...的复制策略及其失败模式。   ### 5.1 为什么复制  复制有三个目标，任何给定系统都追求其中某种组合：  * **Durability** ：一台机器磁盘损坏，数据不会丢失。 * **Availability** ：一台机器不可达，系统仍能从另一台服务请...
  c05_b01.md: ...步复制  最简单的策略。主库接受写入，把写发给每个副本，等待每个副本确认，然后才向客户端确认写入。  性质：  * **Durability after ack** ：若客户端收到“OK”，写已在每个副本上。之后任意单副本磁盘故障不会丢失该写。 * **Pos...
  c05_b02.md: ...至少等待一个副本确认后再响应客户端，但不等待所有副本。确认过的副本保证拥有该写；其他副本异步接收。  性质：  * **Durability after ack** ：写入在主库和至少一个副本上。 * **Post-failover consistency**...
  c13_b01.md: ...库从一个有效状态转换到另一个有效状态。 * **Isolation（隔离性）** ：并发事务看起来按顺序执行。 * **Durability（持久性）** ：已提交事务在崩溃后存活。  挑战是 _跨分片原子性_ 。若各分片独立（各跑自己的 Raft group...

=== followers (4 occurrences) ===
  c05_b02.md: ...ncy on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...
  c09_b01.md: ...leader。 (b) **Log replication** ：leader 接受客户端请求，追加到日志，并复制给 followers。 (c) **Safety** ：确保一旦日志条目提交，它保持提交，且没有其他服务器在相同索引提交冲突条目。  这种分...
  c09_b09.md: ...案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 leader；followers 复制。  _Raft 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 lead...
  c09_b09.md: ...t 放弃的：_  (a) **吞吐上限**。Leader 串行化所有提交，因此每集群吞吐受 leader 处理能力和到 followers 的网络带宽限制。分片（multi-raft per range，如 CockroachDB）是绕过方法。  (b) *...

=== kingsbury (4 occurrences) ===
  c05_b04.md: ...实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。...
  c05_b04.md: ...对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人数给线性一致性却发现并非如此的情况。Kingsbury 的经典分析是必读。   ### 5.8 Read-After-Write 一致性  本章开头的客户存款案例就是 rea...
  c23_b03.md: ...### 23.5 Jepsen 传统  Kyle Kingsbury 的 Jepsen 项目是混沌的另一种风味：不是 “向生产注入故障”，而是 “通过注入故障系统性地测试分布式数据库的声明...
  c23_b07.md: ...n：系统性地测试分布式数据库声明。发现许多 bug；提高领域正确性基线。  **答案：** Jepsen 是 Kyle Kingsbury 的项目（约 2013 年启动），在故障条件下系统性地测试分布式数据库的正确性声明。  方法：  * 在可控条件下运行数...

=== async (4 occurrences) ===
  c05_b05.md: ...eplication | Configuration options ---|---|--- PostgreSQL | Async（一个或多个副本） | `synchronous_commit = remote_apply` 实现按写同步 MySQL...
  c05_b05.md: ...或多个副本） | `synchronous_commit = remote_apply` 实现按写同步 MySQL | Async | `rpl_semi_sync` 插件实现半同步 MongoDB（replica set）| Async to se...
  c05_b05.md: ...SQL | Async | `rpl_semi_sync` 插件实现半同步 MongoDB（replica set）| Async to secondaries | `writeConcern: majority` 实现基于法定人数 Cockroac...
  c05_b05.md: ...namoDB | Leaderless | 默认最终一致，可按请求强一致 Redis（master-replica）| Async | `WAIT N` 命令实现临时同步写 Aurora | 自定义存储层；写以 4-of-6 存储 quorum 确认...

=== point (4 occurrences) ===
  c05_b06.md: ...在看到的状态与它以为的不一致。  恢复选项：  * 若主库磁盘可恢复，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入...
  c05_b06.md: ...上次复制检查点到故障之间的写入永久丢失。团队必须调和下游影响（退款、重试、通知客户）。 * 团队的 _Recovery Point Objective_（RPO）是允许的最大数据丢失时间。异步复制 RPO = 复制延迟，可能是数秒到数分钟。同步复制...
  c05_b06.md: ...在看到的状态与它以为的不一致。  恢复选项：  * 若主库磁盘可恢复，把其日志中上次复制点之后的部分回放给新主库。这是 point-in-time-recovery 工具的做法；要求主库 WAL 完整。 * 若磁盘丢失，上次复制检查点到故障之间的写入...
  c05_b06.md: ...上次复制检查点到故障之间的写入永久丢失。团队必须调和下游影响（退款、重试、通知客户）。 * 团队的 _Recovery Point Objective_（RPO）是允许的最大数据丢失时间。异步复制 RPO = 复制延迟，可能是数秒到数分钟。同步复制...

=== trivial (4 occurrences) ===
  c05_b06.md: ...er 可失去。示例：Cassandra、DynamoDB、Riak。  权衡：leader-based 让每分片写排序 trivial（leader 排序），代价是故障切换不可用窗口。leaderless 让故障透明，代价是更复杂的冲突解决和更弱的每键排...
  c09_b09.md: ...放弃了什么，又获得了什么？  提示  单 leader 是吞吐瓶颈和 liveness 单点故障；换取的是更简单且排序 trivial。  **答案：** Raft 的 leader-based 设计任何时刻只有一个 leader。所有客户端请求都经 l...
  c09_b09.md: ...r 协议可让任何区域接受写。  _Raft 获得的：_  (a) **简单性**。单 leader 串行化所有写；排序 trivial。协议比无 leader 替代方案简单得多。  (b) **实现成熟度**。许多生产级 Raft 库存在；无 leade...
  c12_b08.md: ...据库（或 schema、namespace、project）。优点：最大隔离；per-tenant 资源控制；删除租户 trivial。缺点：运维成本随租户数增长（管理成千上万个数据库）；跨租户聚合很难；per-tenant 开销（连接池、schema...

=== rto (4 occurrences) ===
  c05_b08.md: ...CockroachDB。列出他们应先用 PACELC 框架（第2章）和本章复制权衡回答的五个问题。  提示  RPO、RTO、read-after-write 需求、多区域需求、团队运维成熟度。  **答案：**  (a) **RPO 是多少？...
  c05_b08.md: ...roachDB 的 Raft 复制对已提交写 RPO = 0。支付数据通常 RPO = 0 是硬性要求。  (b) **RTO 是多少？** Postgres 故障切换（用 Patroni 等）通常 30 秒到数分钟；CockroachDB 自动...
  c11_b02.md: ...gres）无外部协调可能 split-brain。 * **需要人工干预** ：遗留系统和部分复制配置需要人工提升副本。RTO 以小时计。  故障恢复时间决定可用性 SLA。5 秒 Raft 重选与 60 秒 Patroni 切换的停机画像不同。...
  c30_b13.md: ...应用第11章诊断方法：    1. **复制协议** 满足吞吐 / 可用性需求。   2. **故障恢复语义** 满足 RTO / RPO。   3. **一致性模型** per operation，而非 per system。   4. **分...

=== slack (4 occurrences) ===
  c06_b02.md: ...影响。 * **Right for** ：以用户为中心的工作负载（聊天、社交、生产力），大多数操作围绕单个用户数据。  Slack、许多游戏公司这样做，大多数 SaaS 产品在达到多区域规模后都会悄悄演化成这样。   #### 模式 3：全局一致（真...
  c06_b08.md: ...把数秒内跨区送达当作消息传递延迟，这是他们的心理模型）。Spanner 过度；中央 DB 让用户体验不对称。  这正是 Slack 的模式：per-region channels，跨区复制做 federation。  **Q2.** 一个团队为 Ra...
  c12_b01.md: ...Anya 盯着团队 on-call 发来的 Slack 消息：“Stratus 集群 7 磁盘使用率 87%，每天增长 2%。下周末前必须扩容。”  集群有 12 节点，每节...
  c17_b07.md: ...cord）底层大致如此。  扩展：room 跨节点分片；每个 room 的 HLC 是本地的；跨 room 流量不协调。Slack 架构大致如此。  **Q2.** 一个团队使用 MongoDB 因果一致会话。他们注意到网络抖动期间，读偶尔会挂好几秒...

=== active (4 occurrences) ===
  c06_b02.md: ...三套部署、三套监控栈。   ### 6.3 三种多区域模式  选择由你的数据需求决定。   #### 模式 1：单区域 active-passive  所有写发往一个区域（active）。其他区域是异步复制的 warm standby。读可去任何区域（...
  c06_b04.md: ...ness 要求法定人数跨区域（Raft、Paxos、Spanner），每次写都付跨区成本。  “多区域为 DR、单区域 active”和“多区域为 HA、跨区法定人数”两种模式成本差异很大。混淆它们会产生：支付了法定人数成本却没得到法定人数收益，或只得...
  c14_b03.md: ...f 6 ack）。 * 兼容 Postgres 线缆。 * Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KM...
  c14_b03.md: ...）。 * 兼容 Postgres 线缆。 * Serverless：无需实例选型，按用量计费。 * 可选 active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、C...

=== clos (4 occurrences) ===
  c06_b03.md: ...### 6.4 CLOS 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles C...
  c06_b03.md: ...S 与 Leaf-Spine 拓扑  现代数据中心内部，网络不是交换机树，而是 _CLOS 网络_（以 Charles Clos 命名，1953），2026 年实现为 _leaf-spine_。  结构：  * **Spine switches**...
  c06_b03.md: ...是分布式系统产生的。  对分布式系统工程师的实践含义：  * 同数据中心 RPC 延迟由序列化和排队主导，而非网络。在 CLOS 内，延迟下限是微秒级。 * 网络 bisection bandwidth 很少是瓶颈。磁盘 I/O、CPU 和应用层协...
  c06_b12.md: ...o-partitioned、globally consistent）成本画像不同；选择由数据需求决定。  数据中心内部，CLOS leaf-spine 拓扑以低延迟提供扁平 any-to-any 带宽。RDMA 把同数据中心延迟降到个位数微秒，对存...

=== bandwidth (4 occurrences) ===
  c06_b03.md: ...rvers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandw...
  c06_b03.md: ...width** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的...
  c06_b03.md: ...数据中心 RPC 延迟由序列化和排队主导，而非网络。在 CLOS 内，延迟下限是微秒级。 * 网络 bisection bandwidth 很少是瓶颈。磁盘 I/O、CPU 和应用层协调通常是。 * 跨机架与同机架 RPC 延迟相近（差几微秒）。网络局部性不...
  c25_b01.md: ...Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同时运行两个阶段，可能只用到 50% 计...

=== core (4 occurrences) ===
  c06_b03.md: ...Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产生带宽瓶颈，限制了东西向（服务器到服务器）流量，而这正是分布式系统产生的。  对分布式系统工程师的实践含义：  *...
  c10_b09.md: ...** ：三者中最简单。通过 Cosmos SDK 得到良好支持；Cosmos 生态有数十条生产链。Tendermint Core 是成熟、经过验证的库。吞吐低于 HotStuff，但对银行间结算足够（每秒几百 TPS，而非 HotStuff 可达的...
  c11_b09.md: ...，然后综合建议。  提示  ScyllaDB 与 Cassandra 兼容但用 C++ 编写，采用 shard-per-core。协议家族相同，实现不同；比较性能、运维特征。  **答案：** 两者诊断画像相同（共享协议）：  Question |...
  c11_b09.md: ...aDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throug...

=== frankfurt (4 occurrences) ===
  c06_b06.md: ...供任意服务器间扁平、低超额订阅的带宽。  **Q3.** 一个团队的多区域数据库部署在 US-East、US-West、Frankfurt。他们希望 Raft quorum 在 80 ms 内提交。可行吗？若不可行，如何改？  提示  Raft quorum...
  c06_b06.md: ...of 3 ack。相关成对往返延迟：  * US-East 到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等...
  c06_b06.md: ...到 US-West：~80 ms。 * US-East 到 Frankfurt：~90 ms。 * US-West 到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和 Frankfurt 中...
  c06_b06.md: ...到 Frankfurt：~160 ms（最差一对）。  US-East 发起的写等待第二个 ack；US-West 和 Frankfurt 中先响应者约 80-90 ms 往返。因此 US-East 发起的写约 85 ms 往返提交，刚超 80 ms 预算。...

=== leadership (4 occurrences) ===
  c07_b05.md: ...-2000 ms。 * **CockroachDB** 用 per-range Raft。类似超时；per-range leadership。 * **Spanner** 用 per-tablet Paxos。由于 Google 数据中心网络纪律更好，超时更紧...
  c13_b14.md: ...er 可重发 PREPARE（幂等）或中止。  (d) 参与者对协调者故障切换具有鲁棒性：它们通过 Raft term/leadership 识别新协调者权威并接受其指令。  事务的用户可见行为：故障切换期间短暂停顿（亚秒到数秒），然后提交（或中止）继续。参与...
  c28_b10.md: ...加 M 节点 | 再平衡集群，零停机，事务一致性保留 | 一致性哈希 + range streaming + Raft leadership transfer | range 大小估计漂移；业务高峰期忘记限流；源节点 streaming 中途 failover...
  c28_b14.md: ...* **一致性模型**：事务内快照隔离；副本读最终一致。 * **分区行为**：CP——少数派无法在 etcd 获得 leadership，拒绝写。  PACELC：PC/EL（分区时一致；正常操作时低延迟，因异步复制）。  _三个谎言姿态_：  * **时...

=== rtt (4 occurrences) ===
  c07_b08.md: ...不触发集群扰动。  推荐修复：启用 pre-vote（消除误怀疑扰动）并把选举超时调到约 5 倍观察到的最坏跨 DC RTT。  **Q2.** 一个团队为复制配置存储在 Raft 与拜占庭容错协议（HotStuff）之间选择。该存储由单一可信...
  c09_b08.md: ...` 推进 commit index，回复成功。  (e) 一批批地，follower 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 100 条，追赶网络耗时约 500 ms，加上 follower 磁盘写入时间。  若差距极大...
  c13_b10.md: ...的会话落在 geo-DNS 路由选定的区域。按第6章 §6.1 方法（光纤中光速下限约 200,000 km/s，真实 RTT 的 2-3× 路由开销乘数）：São Paulo → US-East（经南美沿海电缆约 8,500 km）p99 RT...
  c13_b10.md: ...TT 的 2-3× 路由开销乘数）：São Paulo → US-East（经南美沿海电缆约 8,500 km）p99 RTT 约 110 ms；São Paulo → EU-West（经大西洋约 9,500 km）约 190 ms；São Pa...

=== transactions (4 occurrences) ===
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。...
  c19_b01.md: ...streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 * Transactions：多键原子操作。  服务发现用法：  * 每个服务实例写入一个 key（如 `/services/payment/ins...
  c30_b02.md: ...010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications." _OSDI_. — Percolator。 * Thomson, A., et...
  c30_b02.md: ...or。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_.   #### 最终一致性与...

=== fast (4 occurrences) ===
  c08_b04.md: ...团队因同样原因选择 Raft。   ### 8.7 Paxos 家族  许多协议是“带扩展的 Paxos”：  * **Fast Paxos** ：常见情况下跳过第一阶段；使用更大的法定人数（3/4 而非过半）。无竞争时更快；竞争下可能更慢或不安全...
  c21_b02.md: ...br/>track success/failure rate         Open: All calls fail fast;<br/>no downstream contact         HalfOpen: Limited probe...
  c30_b02.md: ...OSDI_. — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Systems."...
  c30_b03.md: ...cache 层。 * Leviathan, Y., Kalman, M., & Matias, Y. (2023). "Fast Inference from Transformers via Speculative Decoding." _ICM...

=== network (4 occurrences) ===
  c08_b04.md: ...次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多...
  c16_b05.md: ...运行。 (b) **My work is not trapped** ：数据在本地；用户拥有比特。 (c) **The network is optional** ：应用可离线工作。 (d) **Collaboration is seamless** ：...
  c20_b01.md: ...root span<br/>POST /payment<br/>~365 ms total"]         NW["network in + TLS<br/>65 ms (edge + cross-equator)"]         A["auth...
  c20_b01.md: ...POST["post-commit + return<br/>73 ms (Kafka publish + network out)"]         R --> NW         R --> A         R --> F...

=== storage (4 occurrences) ===
  c08_b04.md: ...tting** ：日志无限增长；生产系统必须给状态机快照并截断日志。快照必须与日志该点状态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值...
  c30_b02.md: ...STOC_. * Chang, F., et al. (2006). "Bigtable: A Distributed Storage System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Ne...
  c30_b02.md: ...tle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013). "Stronger S...
  c30_b02.md: ...(2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== joint (4 occurrences) ===
  c08_b07.md: ...配置** ：改变 acceptor 集合（增删节点）不能随意，因为两个不同配置可能各自形成无重叠的“多数”。标准解是 “joint consensus” 或 “alpha-paxos” 模式，但正确实现微妙。  (c) **快照与日志截断** ：Pa...
  c09_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“Raft 的简单性只是营销说法；一旦你实现重配置、快照和 joint consensus，Raft 就和 Paxos 一样复杂。”  **讨论：** 辩护：核心 Raft 协议（leade...
  c09_b10.md: ...一个能工作的基本 Raft；基本 Paxos 需要更久。教学和代码可读性收益真实。  反驳：完整 Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人...
  c28_b10.md: ...性检查 + 多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 | | 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案...

=== off (4 occurrences) ===
  c08_b10.md: ...poser 可能使用相同 ballot，破坏“最高 accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N+1)/2 \rceil$。$N=4$ 时多数是 3；$N...
  c08_b11.md: ...法令扩展为值序列。稳态下每决定成本是一次往返。  Paxos 以难实现著称：持久化纪律、ballot 唯一性、法定人数 off-by-one、重配置、快照。Spanner 和 Megastore 是经典生产实现。附录 A 的阅读列表包括原始 19...
  c09_b10.md: ...生成的 Raft 实现中可能存在的三类结构性 bug。  **讨论：** 除了持久化、ballot 唯一性、法定人数 off-by-one 这些第8章 Paxos bug 之外：  (a) **Term 重启时投票计数未清零。** 每个新 te...
  c09_b10.md: ...ety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendEntries 一致性检查 off-by-one。** 检查比较 follower 在 `prevLogIndex` 处的条目与 `prevLogTerm...

=== buchman (4 occurrences) ===
  c10_b01.md: ...(N²) messages"]         PBFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> HotStuff["Hot...
  c10_b04.md: ...pool 的正确性及其与共识层的集成。   ### 10.5 Tendermint：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其...
  c10_b04.md: ....5 Tendermint：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint...
  c30_b02.md: ...2). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevic, Z. (2018). "The latest gossip o...

=== byzantine (4 occurrences) ===
  c10_b01.md: ...BFT 用例（对抗性多租户系统的控制面）。  BFT 家族树：       graph LR         BG["Byzantine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castr...
  c30_b02.md: ...uorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzantine Generals Problem." _ACM TOPLAS_. — BFT 基础。 * Castro, M., &...
  c30_b02.md: ...S_. — BFT 基础。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Re...
  c30_b03.md: ...用预测重读（§29.1）。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控制平面重读（§29....

=== castro (4 occurrences) ===
  c10_b01.md: ...ntine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov 1999<br/>O(N²) messages"]         PBFT --> Tendermin...
  c10_b02.md: ...FT：经典协议  实用拜占庭容错（Practical Byzantine Fault Tolerance，PBFT）由 Castro 和 Liskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a...
  c30_b02.md: ..."The Byzantine Generals Problem." _ACM TOPLAS_. — BFT 基础。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolera...
  c30_b03.md: ...Cloud." _Onward!_ — 2019 文章，作为 2027-2030 主流采用预测重读（§29.1）。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolera...

=== liskov (4 occurrences) ===
  c10_b01.md: ...enerals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov 1999<br/>O(N²) messages"]         PBFT --> Tendermint["Tend...
  c10_b02.md: ...实用拜占庭容错（Practical Byzantine Fault Tolerance，PBFT）由 Castro 和 Liskov 在 1999 年论文中提出，是首个可实际部署的 BFT 协议。PBFT 每个决定分三个阶段：  (a) **Pre-p...
  c30_b02.md: ...Generals Problem." _ACM TOPLAS_. — BFT 基础。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_....
  c30_b03.md: ...d!_ — 2019 文章，作为 2027-2030 主流采用预测重读（§29.1）。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_....

=== blocks (4 occurrences) ===
  c10_b04.md: ...些 mempool 块的层）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader...
  c10_b04.md: ...个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：leader 的工...
  c25_b02.md: ...1         P2 -->|"KV cache via RDMA"| K2         K1 -->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2...
  c25_b02.md: ...K1 -->|"KV blocks per token"| D1         K2 -->|"KV blocks per token"| D2         D1 -->|"streamed tokens"| LB...

=== emptyset (4 occurrences) ===
  c10_b05.md: ...法定人数 $Q_2$ 都是多数。Howard 的洞见：对 safety 只需要 $Q_1 \cap Q_2 \ne \emptyset$。  这意味着：$Q_1$ 和 $Q_2$ 可以调优。只要任意 prepare 法定人数与任意 accept 法定人数...
  c10_b08.md: ...xos 推广了法定人数要求。陈述该推广并给出一个实践应用示例。  提示  只需要 $Q_1 \cap Q_2 \ne \emptyset$；两者不必都是多数。更小的 $Q_2$ = 更快的提交。  **答案：** 经典 Paxos 要求 prepare 法...
  c10_b08.md: ...数 $Q_1$ 和 accept 法定人数 $Q_2$ 各为 N 的多数。这确保 $Q_1 \cap Q_2 \ne \emptyset$（任意两个多数相交）以保证 safety。  Howard 的推广：只需要 $Q_1 \cap Q_2 \ne \em...
  c10_b08.md: ...et$（任意两个多数相交）以保证 safety。  Howard 的推广：只需要 $Q_1 \cap Q_2 \ne \emptyset$。只要两个法定人数相交，两个法定人数可以大小不同。  实践示例：稳定领导的系统很少运行 Phase 1（例如每个 le...

=== certificates (4 occurrences) ===
  c10_b07.md: ...于 PBFT，HotStuff 的关键创新是什么？  提示  线性 view change；可概括支持的 Quorum Certificates，无需重播所有投票。  **答案：** HotStuff 的关键创新是 _线性 view change_ ：当 lead...
  c10_b08.md: ...机制：HotStuff 使用 _Quorum Certificates（QC）_。QC 是 2f+1 个签名在特定阶段结果上的集合。QC 是“该值已获支持”的紧凑、可传递证明。  当 lea...
  c10_b12.md: ...实用 BFT 协议；其 $O(N^2)$ 消息复杂度限制了小规模副本集。HotStuff（2019）通过 Quorum Certificates 和 pipelining 实现 $O(N)$ view change，使 BFT 可扩展至数百验证者。Narwhal-...
  c28_b10.md: ...0 §10.3 | 验证者提案 + 2f+1 轮投票 | Quorum 证书决定；O(N) 视图变更 | Quorum Certificates + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 quorum 混淆；截止压力下跳过 QC 验证；竞争新...

=== benchmark (4 occurrences) ===
  c11_b05.md: ...性”是 SLA 声明；分区下的实际可用性是另一个数字。CAP/PACELC 分析才是预测器。  (e) **理想化条件 benchmark。** 厂商 benchmark 在最佳硬件、理想网络、单区域。真实生产性能差 2-10 倍。CAP/PACELC 画像...
  c11_b05.md: ...际可用性是另一个数字。CAP/PACELC 分析才是预测器。  (e) **理想化条件 benchmark。** 厂商 benchmark 在最佳硬件、理想网络、单区域。真实生产性能差 2-10 倍。CAP/PACELC 画像告诉你网络变差时的预期。   #...
  c11_b11.md: ...e 与 serializable；混淆 CAP 与 PACELC；per-operation vs 系统级声明；理想化 benchmark。方法通过要求精确答案抵御这些陷阱。  第12章进入下一层：分布式数据库如何跨节点分片数据。哈希分片、范围分片、一致哈希...
  c16_b09.md: ...工作流设计。 * 缺点：每文档内存占用更大（存储历史）；历史上较慢，但 Automerge 2.x 在 2026 生产 benchmark 中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；...

=== tigerstyle (4 occurrences) ===
  c11_b08.md: ...Beetle。充分阅读其文档以回答四个问题，并产出一页阅读。  提示  TigerBeetle 是面向确定性模拟测试和 TigerStyle 编程范式的金融级账本数据库。找其复制协议、故障模型、一致性和分区行为。  **答案：** TigerBeetle 阅读...
  c11_b08.md: ...吞吐（benchmark 宣称每秒 100 万+ 转账） Strengths | 金融级正确性模型（确定性模拟测试 + TigerStyle 编程）；确定性执行；易于调试 Weaknesses | 专用于复式记账（非通用）；生态小 Right for | 银行...
  c14_b01.md: ...rless Postgres，存储与计算分离。 * **TigerBeetle** ：金融级账本，采用确定性模拟测试和 TigerStyle 规范（固定大小消息、显式状态机枚举）。  该类别已成熟到问题不再是 “是否用 NewSQL？”，而是 “用哪个 New...
  c14_b03.md: ...yler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * TigerStyle 编程规范：固定大小 128 字节消息、热路径无动态分配、显式状态机、设计口号是 “1000-year debuggin...

=== viewstamped (4 occurrences) ===
  c11_b08.md: ...e 阅读：  Question | Answer ---|--- Replication protocol | 自定义 Viewstamped Replication 变体；5 副本部署是经典；集群范围写排序 Failure-recovery | 亚秒级 vie...
  c14_b03.md: ...### 14.8 TigerBeetle  领域特定：专为金融复式记账优化的数据库。  关键属性：  * 5 副本 Viewstamped Replication 共识。 * 确定性模拟测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，...
  c28_b04.md: ...) 事务写入 TigerBeetle（账本）：借记 Adaeze，贷记 Femi。TigerBeetle 的 5 副本 Viewstamped Replication 在 30 ms 内提交。  (9) 事务在 CockroachDB 中提交：更新 Adaeze...
  c28_b05.md: ...2（Consul gossip 过载） | | 5 | 复制 | Raft（CockroachDB 每个 range）、Viewstamped（TigerBeetle） | E.5（无 fencing 的 failover） | | 6 | 多 region |...

=== readconcern (4 occurrences) ===
  c11_b08.md: ...一致吗？”用诊断方法给出精确、可辩护的答案。  提示  MongoDB 的一致性取决于配置：writeConcern、readConcern、replica set vs sharded。默认与最强可用不同。  **答案：** MongoDB 的一致性高度依赖...
  c11_b08.md: ...writeConcern: majority`。写在被 ack 前已持久化到多数；主库故障后存活。 * _默认读_ ：`readConcern: local` 从主库读。主库读线性化，从 secondary 读则最终一致（MongoDB 客户端默认 `readP...
  c11_b08.md: ...goDB 客户端默认 `readPreference: secondary` 时）。 * _Majority 读_ ：`readConcern: majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern...
  c11_b08.md: ...readConcern: majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern: linearizable` 提供线性化读。自 MongoDB 3.4 起可用。  分片集群：每个分片是一个副本集；上...

=== prewrite (4 occurrences) ===
  c13_b03.md: ...阶段（Prewrite + Commit），通过一个指定的 “primary” 行协调。无中心协调者。  **阶段 1：Prewrite**  事务客户端从要写的键中选一个作为 _primary_。对事务要写的每个键：    1. 读取当前值（及其时间戳）...
  c13_b03.md: ...imary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primar...
  c13_b03.md: ...阶段（Prewrite + Commit），通过一个指定的 “primary” 行协调。无中心协调者。  **阶段 1：Prewrite**  事务客户端从要写的键中选一个作为 _primary_。对事务要写的每个键：    1. 读取当前值（及其时间戳）...
  c13_b03.md: ...imary key 存指向自己的锁；secondary keys 存指向 primary 的锁。）  若任何键有冲突锁，prewrite 失败，事务中止。  **阶段 2：Commit**  客户端选一个 commit_timestamp。对 primar...

=== provably (4 occurrences) ===
  c13_b04.md: ...时间戳读）自己一定会被分配更晚的时间戳。  为何有效：TrueTime 保证 `TT.now().earliest`  provably 晚于任何更早带 TrueTime 时间戳事件的实时时刻。等待弥合了 gap；等待后，系统中任何其他 Spanner 事...
  c13_b04.md: ...任何更早带 TrueTime 时间戳事件的实时时刻。等待弥合了 gap；等待后，系统中任何其他 Spanner 事务都 provably 更晚或更早，而不会 “模糊”。   #### Spanner 的完整提交协议  对多 tablet 事务，Spanne...
  c13_b13.md: ...钟不确定性。`TT.now()` 返回区间 `[earliest, latest]`，真实 wall-clock 时间 provably 位于该区间内。典型不确定性：1-7 毫秒。  Spanner 通过 _commit-wait_ 协议用 TrueTim...
  c13_b13.md: ...st > commit_timestamp`。通常约 ~7 ms。   3. 等待后，真实 wall-clock 时间 provably 已过 commit_timestamp。   4. 提交释放；锁释放。  等待确保任何后续事务（系统任何地方）以大于...

=== note (4 occurrences) ===
  c13_b07.md: ...ate         A-->>O: updated         O-->>C: success         Note over O: On failure at step N:<br/>invoke compensations<br/>...
  c18_b04.md: ...0 >= highest (was 0); accept         Bank-->>C1: ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired...
  c18_b04.md: ...Bank-->>C1: ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired         C2->>L: acquire lock...
  c18_b04.md: ...101 >= highest (100); accept         Bank-->>C2: ok         Note over C1: resumed after pause         C1->>Bank: commit (tok...

=== listens (4 occurrences) ===
  c13_b07.md: ...→ publishes OrderCreated event     PaymentService          listens to OrderCreated      → publishes PaymentCompleted     Inven...
  c13_b07.md: ...→ publishes PaymentCompleted     InventoryService        listens to PaymentCompleted  → publishes InventoryReserved     Ship...
  c13_b07.md: ...→ publishes InventoryReserved     ShippingService         listens to InventoryReserved → publishes ShippingScheduled     Noti...
  c13_b07.md: ...d → publishes ShippingScheduled     NotificationService     listens to ShippingScheduled → sends confirmation   每个服务响应前一个服务的事件。...

=== over (4 occurrences) ===
  c13_b07.md: ...A-->>O: updated         O-->>C: success         Note over O: On failure at step N:<br/>invoke compensations<br/>for N...
  c18_b04.md: ...highest (was 0); accept         Bank-->>C1: ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired...
  c18_b04.md: ...k-->>C1: ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired         C2->>L: acquire lock         L-...
  c18_b04.md: ...= highest (100); accept         Bank-->>C2: ok         Note over C1: resumed after pause         C1->>Bank: commit (token=10...

=== publishes (4 occurrences) ===
  c13_b07.md: ...型电商订单工作流（无中央 orchestrator）：       OrderService            → publishes OrderCreated event     PaymentService          listens to O...
  c13_b07.md: ...PaymentService          listens to OrderCreated      → publishes PaymentCompleted     InventoryService        listens to Pay...
  c13_b07.md: ...InventoryService        listens to PaymentCompleted  → publishes InventoryReserved     ShippingService         listens to In...
  c13_b07.md: ...ShippingService         listens to InventoryReserved → publishes ShippingScheduled     NotificationService     listens to Sh...

=== idempotent (4 occurrences) ===
  c13_b08.md: ...* **Independently committable** ：本地事务成功或原子中止，不依赖后续步骤。 * **Idempotent** ：重执行（如 saga 重试后）产生相同结果。 * **Compensable** ：存在语义上撤销该步骤的补偿动...
  c13_b08.md: ...知）；通常是恢复不变量的前向动作（发送 “您的请求已取消” 通知；退款；重新入库）。 * **Compensation idempotent** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs 2PC：何时选谁...
  c13_b08.md: ...* **Independently committable** ：本地事务成功或原子中止，不依赖后续步骤。 * **Idempotent** ：重执行（如 saga 重试后）产生相同结果。 * **Compensable** ：存在语义上撤销该步骤的补偿动...
  c13_b08.md: ...知）；通常是恢复不变量的前向动作（发送 “您的请求已取消” 通知；退款；重新入库）。 * **Compensation idempotent** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs 2PC：何时选谁...

=== brl (4 occurrences) ===
  c13_b10.md: ...amide 的跨区域支付事务  Olamide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务...
  c13_b10.md: ...i NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务在 Crescent 内部 BRL 账本     -- 全程运行，FX 作为单独分录入账）。     BEGIN TRANSACTION;       -...
  c13_b10.md: ...unts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240 USD；是一笔 substantial 交易，不是       -- 第1章的 6-BRL 数据包）...
  c13_b10.md: ...款项（1200 BRL ≈ 240 USD；是一笔 substantial 交易，不是       -- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHE...

=== where (4 occurrences) ===
  c13_b10.md: ...-- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL ≈ 240...
  c13_b10.md: ...BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在...
  c13_b10.md: ...EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'femi';       -- 更新 Femi 的日汇总       UPDATE mer...
  c13_b10.md: ...总       UPDATE merchant_day_rollup SET total = total + 1200 WHERE merchant_id = 'femi' AND day = '2026-05-19';     COMMIT;...

=== geo (4 occurrences) ===
  c13_b10.md: ...scent 运营三个区域——US-East、EU-West、US-West——因此 São Paulo 客户的会话落在 geo-DNS 路由选定的区域。按第6章 §6.1 方法（光纤中光速下限约 200,000 km/s，真实 RTT 的 2-3...
  c13_b10.md: ...最优。   2. `SELECT` 从 Adaeze 分片 Raft leader 读取余额。Adaeze 账户行被 geo-partition 到 US-East（服务南美的区域），因此读留在区域内。   3. 对 Adaeze 余额的 `U...
  c13_b11.md: ...型 p50 延迟约 ~360 ms，p99 约 ~580 ms——第28章 §28.2 会分解完整端到端，包括 TLS、geo-DNS、风控检查（Continuum LLM）和返回路径。Crescent 产品团队谈妥的 750 ms p99 SL...
  c30_b02.md: ...oyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== htap (4 occurrences) ===
  c14_b02.md: ...；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和...
  c14_b02.md: ...TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和 OLAP 无需 ETL。 * 运维成熟（在中国科技规模下久经考验：微...
  c14_b02.md: ...复杂（PD + TiDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MySQL 工作负载。生产示例：许多中国科技公司、部分美国公司（Square 历史上）。   ### 14.5...
  c14_b05.md: ...若用 MySQL） 金融账本 | TigerBeetle（账本）+ Postgres/CockroachDB（元数据） HTAP（混合 OLTP/OLAP） | TiDB，或 CockroachDB + 独立 OLAP 存储 纯 OLAP | Sn...

=== spark (4 occurrences) ===
  c14_b03.md: ...B-PB 数据上的大扫描、聚合、跨源 join）。示例：Trino/Presto、Snowflake、BigQuery、Spark SQL。...
  c15_b03.md: ...Chandy-Lamport 快照，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD lineage、MapReduce 的 checkpointing、各种 ML 训练系统使用基于快照的重启。...
  c15_b07.md: ...基础结果。Apache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last write wins”（LWW）...
  c30_b11.md: ...r**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 Spark Structured Streaming。 * **Sink**：PostgreSQL 或 CockroachDB，幂...

=== query (4 occurrences) ===
  c14_b04.md: ...t it and forget it” 运维。  劣势：厂商锁定（专有格式，但 Iceberg 集成正在出现）；per-query 成本可能让调优差的工作负载意外。   #### BigQuery 的 Dremel  Google 的分析服务；基于...
  c22_b02.md: ...0% 未命中（需要磁盘读取）。  触发器：流量尖峰。缓存命中率短暂降到 70%。磁盘读取率翻三倍；磁盘队列堆积；per-query 延迟增加。  反馈循环：延迟增加意味着每个查询持有内存更久（查询状态直到完成才释放）；内存压力增加；缓存缩小以腾出空间...
  c22_b03.md: ...自行做到。   #### 防御亚稳态  (a) **有界反馈循环**。每个循环必须有阻尼机制。对缓存颠簸：限制 per-query 内存；内存耗尽时拒绝新查询（load shedding）。对重试风暴：重试预算。  (b) **滞后（hysteres...
  c27_b11.md: ...# Read latest balance             balance = db.query(                 "SELECT balance FROM accounts WHERE id = %...

=== linear (4 occurrences) ===
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人可同时编辑同一份文档，使用不同设备，有时离线，文档会收敛到一致状态，而无需...
  c16_b03.md: ...ID 和指向前驱的指针。该结构对常见情况高度优化（顺序打字产生字符链，每个指向前一个）。  Yjs 在 Notion、Linear 等众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAssembly 实现。   ###...
  c16_b05.md: ...能者。没有 CRDT，“离线且自动解决冲突的协作” 无法实现。  Local-First 是一种新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT...
  c16_b11.md: ...ppmann 等人在 2019 年论文中命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear、Notion、Glide、Logseq、Skiff 是示例。  2026 年新协作编辑器设计默认用 CRDT。现有基于...

=== dot (4 occurrences) ===
  c16_b02.md: ...会被静默丢失。  OR-Set 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add elem...
  c16_b02.md: ...t 的解决方案：每次 add 打一个唯一 ID（一个 “dot”）。remove 针对特定 dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：生成新唯一...
  c16_b02.md: ...未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove element x**：找到集合中所...
  c16_b02.md: ...ombstone，则 \(x\) 在集合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombsto...

=== pair (4 occurrences) ===
  c16_b02.md: ...在副本 \(j\) **add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove element x**：找到集合中所有 \((x,d)\)；将 \(d\) 记入 tom...
  c16_b02.md: ...值**：若存在某个 \((x,d)\) 的 \(d\) 未被 tombstone，则 \(x\) 在集合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set...
  c16_b02.md: ...合中。 * 合并：pair 集合取并集，tombstone 集合取并集；然后排除 dot 在 tombstone 中的 pair。  OR-Set 干净地处理 add-remove 并发。代价：tombstone 累积。生产 OR-Set 实现会在...
  c16_b03.md: ....35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers 可能变长（交替插入可使 id 长度翻倍）。删除...

=== webassembly (4 occurrences) ===
  c16_b03.md: ...Yjs 在 Notion、Linear 等众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAssembly 实现。   #### Automerge 的实现  Automerge（Martin Kleppmann、Adam W...
  c16_b03.md: ...x（2023 年发布）是重大重写，将性能提升到生产可接受水平。早期版本在大规模下有内存问题；2.x 使用 Rust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bundle 更小、高频编辑更快、...
  c16_b07.md: ...**Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能** ：合并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle...
  c16_b07.md: ...绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能** ：合并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle size** ：Yjs ~80 KB，即使在慢网络下也足够小，适合...

=== van (4 occurrences) ===
  c16_b05.md: ...运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (a) **No spin...
  c30_b01.md: ...ications_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanenbaum, A. S., & Van Steen, M. (2017). _Distributed Systems_ (3rd ed.). — 学术处理的教...
  c30_b02.md: ..._IEEE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Soft...
  c30_b03.md: ...开 Google、进入公有云基线的最强信号（§29.1）。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Soft...

=== docs (4 occurrences) ===
  c16_b10.md: ...定义；正确性是结构性的。OT 需要为每对操作写转换函数，随操作集增长而膨胀；实现庞大且容易出微妙 bug。Google Docs 的 OT 实现出了名的复杂；学术文献记录了大量 OT 正确性问题。  反驳：OT 已有数十年生产打磨。Google D...
  c16_b10.md: ...s 的 OT 实现出了名的复杂；学术文献记录了大量 OT 正确性问题。  反驳：OT 已有数十年生产打磨。Google Docs 的 OT 在丰富语义（脚注、评论、依赖文档结构的格式）下大规模工作。CRDT 覆盖成熟协作编辑完整功能集的实践仍不如...
  c16_b10.md: ...熟协作编辑完整功能集的实践仍不如 OT 经充分验证；Yjs 覆盖常见情况，但当前 CRDT 库大多未覆盖 Google Docs OT 的边界情况。  综合：2026 年新设计默认用 CRDT。数学干净加上 Yjs 和 Automerge 的生产记...
  c16_b10.md: ...用 CRDT。数学干净加上 Yjs 和 Automerge 的生产记录是强论据。对现有基于 OT 的系统（Google Docs、Microsoft Word Online），迁移成本高，现有 OT 足够好；它们可能继续用 OT。  第二个考虑：C...

=== lloyd (4 occurrences) ===
  c17_b01.md: ...之后。 * **比线性化便宜** ：无需全局协调；只要满足因果依赖，副本可独立应用因果有序操作。   #### 实现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+...
  c17_b01.md: ...ency”。每次写携带一个依赖向量：写者本次写之前已观察到的写集合。副本在应用某写前会延迟，直到其所有依赖都已应用。  Lloyd 等人在 2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到...
  c30_b02.md: ...cGranaghan, M. (2019). "Local-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsky, M., & Andersen, D. G. (2011...
  c30_b02.md: ...sal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo...

=== acquire (4 occurrences) ===
  c18_b04.md: ...icipant C2 as Coordinator B<br/>(token 101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank:...
  c18_b04.md: ...e 60s         Note over L: Lock TTL expired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank:...
  c27_b03.md: ...process_unique_action(action_id):         token = etcd_lock.acquire(f"action:{action_id}", ttl=30)         try:             do_...
  c27_b11.md: ...or consistency      def process_payment(payment):         # Acquire distributed lock for the user's account         lock_key =...

=== kraft (4 occurrences) ===
  c19_b02.md: ...HDFS、较老的 Cassandra 集群）。新系统通常从 etcd 开始。Kafka 从 ZooKeeper 迁移到 KRaft（Kafka Raft）是近期大趋势的例子。   ### 19.3 Consul  HashiCorp 的 Consul...
  c19_b05.md: ...us 指标、Helm charts、Kubernetes operators）集成良好。  (d) **Kafka 的 KRaft 迁移**：Kafka 作为 ZooKeeper 最大的单一用户，迁移到 KRaft（自己的 Raft 实现）。Raft...
  c19_b05.md: ...好。  (d) **Kafka 的 KRaft 迁移**：Kafka 作为 ZooKeeper 最大的单一用户，迁移到 KRaft（自己的 Raft 实现）。Raft 趋势广泛。  ZooKeeper 仍在生产中使用（KRaft 前的现有 Kafka...
  c19_b07.md: ...维。缓解：过渡期间配对运维值班、更新 runbook。  总时间线：有意义迁移 3-6 个月；较小部署更快。Kafka KRaft 迁移（Confluent 的经验）是大型迁移的典范；他们的公开文档是进行类似迁移的团队必读。...

=== linkerd (4 occurrences) ===
  c19_b04.md: ...断、重试预算、超时策略。  2026 年两大主导服务网格：Istio（基于 envoy；Kubernetes 原生）和 Linkerd（更轻量）。两者都用 etcd/Kubernetes 做发现，用 envoy 做数据面。  服务网格采用已趋于平稳；许多...
  c19_b08.md: ...非 3 节点；更大集群对故障容忍更高，重选更快。  (d) **使用带 sidecar 缓存的服务网格**：Istio/Linkerd sidecar 本地缓存服务注册表；客户端到 sidecar 的查找是本地的，不受 Consul 中断影响。sidec...
  c19_b10.md: ...e-proxy、DNS）快速、可靠、运维简单。对纯 Kubernetes 部署、几十个服务以内，它无需 Istio 或 Linkerd 的运维开销就能处理发现。网格增加 CPU、内存、延迟和巨大运维面。  反驳：服务网格对多集群、多区域或非 Kubern...
  c30_b12.md: ...群中才值得，此时其横向扩展是必需的。  **10 个服务不需要时不要伸手选 service mesh。** Istio/Linkerd 增加运维复杂度。对小型服务图，Kubernetes 原生服务发现足够。只有在多 region、多协议、复杂流量需求真实...

=== gateway (4 occurrences) ===
  c19_b04.md: ...ast 的服务。Consul 处理跨区域的 DNS 解析与路由。  * **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Map 暴露；全球客户解析到最近的区...
  c20_b08.md: ...提供 `inject`/`extract` 原语；应用代码使用它们。  Kafka 传播修复后，团队将看到从 API gateway → producer service → Kafka → consumer service → 下游服务的完整 tra...
  c23_b04.md: ...30 秒双 leader 期）。行动项：调查并修复；这与第1章事故的同类 bug。     * EU-West API gateway 的重试预算配置正确；没有重试风暴。 * 学习：     * 故障转移有效，但不如设计预期干净。     * 面向客户的...
  c28_b03.md: ...bgraph Edge["Edge (Cloudflare Workers)"]             CF[API Gateway<br/>TLS / auth / rate limit]         end         subgraph U...

=== mtls (4 occurrences) ===
  c19_b04.md: ...发现通常是更广泛服务网格实现的一部分。服务网格提供：  (a) **服务发现**：实例注册表；动态路由。  (b) **mTLS**：服务间加密与认证。  (c) **流量策略**：金丝雀发布、蓝绿、A/B 测试。  (d) **可观测性**：pe...
  c19_b10.md: ...格增加 CPU、内存、延迟和巨大运维面。  反驳：服务网格对多集群、多区域或非 Kubernetes 部署增加真正价值。mTLS、流量策略和可观测性功能替代了团队否则需要自建的工做。有这类需求的团队中，服务网格比替代方案更便宜。  综合：服务网格适...
  c19_b11.md: ...态重新配置）是 2026 年运行时配置的默认，不应要求服务重启。  服务网格（Istio、Linkerd）把服务发现与 mTLS、流量策略、可观测性打包。它适合多区域/多协议/复杂流量需求的团队；没有这些需求的团队则是过度工程。  Crescent...
  c30_b16.md: ...22 | "我们用 service mesh；服务发现已解决。" | 发现只是 mesh 的一个功能；mesh 还增加 mTLS、流量策略、可观测性——以及运维开销。按需选择。 | Ch.19 | | 23 | "我们有 OpenTelemetry...

=== rbac (4 occurrences) ===
  c19_b07.md: ...。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行部署；应用知道两者。...
  c24_b04.md: ...0 / H200 / B100 / B200 调优）。每代 GPU 都有调优容器。  (d) **企业特性**：SSO、RBAC、审计日志、监控集成。面向企业采购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG...
  c24_b08.md: ...答案：**  _vLLM_：  * 开源；团队完全控制部署。 * 最佳原始推理性能（引擎本身）。 * 没有原生 SSO、RBAC、审计日志；团队自建或集成第三方。 * SOC 2 合规：可行，但需团队处理审计跟踪、访问控制等。  _NVIDIA N...
  c24_b08.md: ...行，但需团队处理审计跟踪、访问控制等。  _NVIDIA NIM_：  * 商业企业产品。 * 包含开箱即用的 SSO、RBAC、审计日志、监控集成。 * SOC 2 合规：平台提供构建块；团队的特定合规更快实现。 * 推理引擎控制较少：NIM 容...

=== agent (4 occurrences) ===
  c19_b10.md: ...Consul**：HashiCorp 的 Consul 为 VM 和混合部署设计。EC2 实例启动时通过 Consul agent 注册；客户端通过 DNS 或 HTTP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务...
  c24_b03.md: ...ix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输出应用、agent 系统。它是 prompt 结构重复的工作负载的最佳选择。  **何时选 SGLang 而非 vLLM**：若提示 to...
  c24_b07.md: ...API；希望最广泛生产支持的团队。 * **SGLang**：客服机器人（system prompt 跨百万请求重复）、agent 框架（agent scaffold 共享）、结构化输出应用（函数调用、JSON 生成）。  对 Tobias 的 Co...
  c24_b11.md: ...ttention 按前缀组织 KV cache；对共享 system prompt 工作负载节省巨大。最适合客服机器人、agent 框架、结构化输出。  NVIDIA NIM 是企业部署平台：容器化微服务、多引擎支持、硬件优化、企业特性。企业团队的标...

=== artifact (4 occurrences) ===
  c20_b02.md: ...对历史记录重要的系统（审计、合规、调试数月前问题），trace context 是系统永久状态的一部分，不只是可观测性 artifact。...
  c20_b04.md: ...Payment trace**：每笔支付的全 trace 为合规和审计存储。Trace 是支付记录的一部分，不只是调试 artifact。  (b) **安全事件 trace**：调查安全事件时，攻击者请求流的 trace 是主要证据。  (c) **客户...
  c20_b10.md: ...特殊的：对特定操作类型 100% 采样、保存数年、按客户或交易 ID 索引。它们是系统永久记录的一部分，不只是可观测性 artifact。  第21章进入下一步：级联故障与重试风暴。2003 年美国东北部大停电是经典案例研究。重试预算、熔断器和分布式系统如...
  c30_b13.md: ...日期 2026-05-20 仍在开发中）。下面记录的结构是预期布局；查阅本附录的读者应将其视为设计规范，而非完整交付的 artifact。Socratopia 团队和贡献者将在出版后数月内填充仓库；想跟踪进度（或贡献）的读者可咨询 Socratopia 社...

=== honeycomb (4 occurrences) ===
  c20_b03.md: ...署跟踪。  Trace 只是众多数据源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准...
  c20_b03.md: ...见库自动仪器（HTTP 客户端、数据库驱动、消息队列）。 * 可导出到任意后端（Jaeger、Zipkin、Tempo、Honeycomb、Datadog 等）。 * W3C trace context 标准用于跨服务传播。  采用情况：大多数新微服务从第一...
  c20_b09.md: ...rrent_span("operation"):         do_work()   上述代码可与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代...
  c20_b09.md: ...确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若团队已选定特定后端，LLM 可在 OpenTeleme...

=== neutral (4 occurrences) ===
  c20_b03.md: ...2026 年，OpenTelemetry 是分布式追踪仪器的事实标准。关键特性：  * 每种主流语言都有 vendor-neutral API。 * 常见库自动仪器（HTTP 客户端、数据库驱动、消息队列）。 * 可导出到任意后端（Jaeger、Zipk...
  c20_b09.md: ...码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 vendor-neutral API；团队代码调用 OpenTelemetry，然后导出到配置的后端。后端可通过配置切换，不用改代码。...
  c20_b09.md: ...导出到配置的后端。后端可通过配置切换，不用改代码。       # 使用 OpenTelemetry 的 vendor-neutral 写法：     from opentelemetry import trace     tracer = trace....
  c20_b10.md: ...（Raft、Kafka 等）。  OpenTelemetry 是 2026 年事实标准：每种主流语言都有 vendor-neutral API、常见库自动仪器、可导出到任意后端。采用 OpenTelemetry 避免 vendor lock-in 并提供...

=== extract (4 occurrences) ===
  c20_b08.md: ...` headers 插入。消费端：       from opentelemetry.propagate import extract      headers = {k: v for k, v in record.headers}     contex...
  c20_b08.md: ...headers = {k: v for k, v in record.headers}     context = extract(headers)     with tracer.start_as_current_span("consume", c...
  c20_b08.md: ...pan("consume", context=context):         process(record)   `extract` 读取 headers 并创建链接到生产者 span 的 context。消费端 span 成为生产者 send sp...
  c20_b08.md: ...时任务、webhooks、event buses）都需要显式传播。OpenTelemetry 提供 `inject`/`extract` 原语；应用代码使用它们。  Kafka 传播修复后，团队将看到从 API gateway → producer se...

=== tracer (4 occurrences) ===
  c20_b08.md: ...in record.headers}     context = extract(headers)     with tracer.start_as_current_span("consume", context=context):...
  c20_b09.md: ...LLM 被要求 “给我们的系统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的失败：产出与特定厂商耦合的代码。团队现在被...
  c20_b09.md: ...vendor-neutral 写法：     from opentelemetry import trace     tracer = trace.get_tracer(__name__)      with tracer.start_as_curr...
  c20_b09.md: ...ort trace     tracer = trace.get_tracer(__name__)      with tracer.start_as_current_span("operation"):         do_work()   上述代...

=== exporter (4 occurrences) ===
  c20_b09.md: ...与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练偏差。厂商博客和教程在 LLM 训练...
  c20_b09.md: ...；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若...
  c20_b09.md: ...与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。  更深观察：LLM 倾向于推荐特定厂商，这暴露了训练偏差。厂商博客和教程在 LLM 训练...
  c20_b09.md: ...；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lock-in。" 若...

=== halfopen (4 occurrences) ===
  c21_b02.md: ...exceeds threshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --...
  c21_b02.md: ...-> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --> Closed: probe calls succeed<br/>(e.g. 80% over 5s)...
  c21_b02.md: ...Closed: probe calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go thr...
  c21_b02.md: ...pen: All calls fail fast;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recove...

=== hystrix (4 occurrences) ===
  c21_b02.md: ...快速失败。对某些工作负载不可接受；对其他工作负载，“快速失败” 比 “挂 30 秒” 更可取。  实现：Netflix Hystrix（经典原型；已弃用，转而推荐 Resilience4j）；Resilience4j（Java）；polly（.NET）；...
  c21_b06.md: ...th-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netflix Hystrix（已弃用；被 Resilience4j 替代）；Resilience4j（现代 Java）；polly（.NET）；En...
  c21_b07.md: ...下游之间加熔断器**：尤其应用服务与共享基础设施（如数据库）之间。工具：Resilience4j（Java）、其他语言 Hystrix-like 库，或 Envoy 代理。  (c) **每个服务入口加 load shedding**：每个服务拒绝超出其...
  c27_b06.md: ...` 没有 timeout（库默认 _没有_ timeout）。 * **库习惯不匹配**：不需要的地方用 legacy Hystrix 装饰器；`redis.set("...", nx=True, ex=...)` 模式用于正确性关键锁（§27.5 的...

=== policy (4 occurrences) ===
  c21_b05.md: ...抑制超额重试。重试在客户端侧被抑制；原始失败返回应用代码，由它决定高层重试还是向用户失败。  gRPC 的 retry policy 支持预算；Envoy 的 retry policy 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？...
  c21_b05.md: ...代码，由它决定高层重试还是向用户失败。  gRPC 的 retry policy 支持预算；Envoy 的 retry policy 也支持。这是生产级库的标准模式。  **Q3.** 什么是熔断器？描述它的三个状态。  提示  Closed：正常。O...
  c21_b07.md: ...制（重试风暴）。修改每个重试感知库或 sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、HTTP 客户端自定义逻辑。  (b) **每对上下游之间加熔断器**：尤其应用...
  c21_b07.md: ...sidecar，把重试限制为成功流量的 10-20%。工具：gRPC retry policy、Envoy retry policy、HTTP 客户端自定义逻辑。  (b) **每对上下游之间加熔断器**：尤其应用服务与共享基础设施（如数据库）之间。工...

=== fallback (4 occurrences) ===
  c21_b09.md: ...call 超时；不要超时过长。  (d) **幂等性**：确保重试安全（同一操作可多次执行而无坏效果）。  (e) **Fallback**：当下游不可用时，返回缓存结果、默认值或有意义的用户错误。  “韧性” 代码应类似：       @circuit_...
  c21_b10.md: ...surrounding context，是缺失架构。正确的 LLM 回应应是：“韧性需要重试预算、熔断器、超时、幂等和 fallback。以下是组合使用每种的模式。”   ### 本章总结  级联故障有特征形状：触发器、放大、饱和、扩散、完全失败、恢复需要...
  c26_b10.md: ...预测的客户（相同 system prompt），把客户流量 pin 到特定节点以最大化缓存复用。  (f) **冷启动 fallback**：若没有节点有缓存，路由器回退到默认负载均衡（least-loaded、round-robin 等）。  (g) *...
  c27_b02.md: ...get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fallback。代码假设调用最终会返回。  现实：网络会抖动。下游有时慢。下游有时会失败。没有防御，这一行可能让调用服务无限挂起，促成...

=== detection (4 occurrences) ===
  c22_b08.md: ...动观察外，负载均衡器可向每个后端发合成请求，检测被动观察可能遗漏的灰色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-...
  c22_b09.md: ...对 Crescent：区域负载均衡器上使用带 outlier detection 的 Envoy。表现出集群中位数 5 倍延迟的后端被 “弹出” 活动池；通过探测定期测试；恢复后重新加入。...
  c26_b09.md: ...知负载均衡器**：监控 per-node 延迟；若某节点异常高（灰色故障，第22章），绕行。Envoy outlier detection 或类似。  (e) **限制每请求最大输出长度**：限制 max_tokens；拒绝极端限制请求或路由到特殊 endp...
  c28_b03.md: ...ake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]         end         CF --> APIE         CF --> APIW...

=== scheduled (4 occurrences) ===
  c23_b01.md: ...年，每个成熟 SRE 组织都有某种混沌实践。   ### 23.2 Game Day  _Game day_ 是一次 scheduled、故意的混沌演练。团队聚集；注入故障；观察系统反应；记录学习。  Game day 结构：  (a) **赛前（1-4...
  c23_b05.md: ...兴实验。团队 kill 一个节点看会发生什么。没有结构化纪律。  (c) **Level 2（Game days）**：scheduled 混沌练习。预先计划、记录、团队参与。Crescent 第一次 game day 是 Level 2。  (d) **L...
  c23_b06.md: ...描述其结构。  提示  赛前、当天、执行、复盘、赛后。每个阶段有具体活动。  **答案：** Game day 是一次 scheduled、故意的混沌练习。团队聚集；注入故障；观察系统反应；记录学习。  结构：  (a) **赛前（1-4 周前）**：  *...
  c23_b13.md: ...系统故障下行为的实践。五条原则：稳态假设、变化真实事件、在生产中运行、自动化、最小化爆炸半径。  Game day 是 scheduled 混沌练习，流程结构化：赛前设计、当天执行、复盘、赛后行动项。它是周期性实践，不是一次性事件。  Nora Jones...

=== enterprise (4 occurrences) ===
  c24_b01.md: ...Tobias Schwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户运行 LLM 推理；每月基础设施账单 120 万美元。CFO 想知道钱花在哪；CTO 想知道如何在保证质量的前提下...
  c24_b09.md: ...灵活 → NIM。  对 Continuum（重度基础设施用户，有平台团队）：vLLM 加自定义企业特性合理。对大多数 enterprise IT 商店：NIM 是正确答案。  **Q3.** 某团队推理集群出现双峰延迟：短请求快，长请求阻塞短请求。诊断并提出...
  c25_b10.md: ...co-located 运维更简单、成本可接受。选择取决于规模。  2026 现实：所有主要推理厂商都采用分离，但在大型 enterprise 以下规模内部署中仍少见。随着工具成熟（Kubernetes operator for disaggregation、v...
  c28_b02.md: ...### 28.1 架构  平台：Crescent 面向 enterprise 客户的多 region 支付处理器。全球接受支付，端到端 p99 延迟低于 750 ms（按产品团队协商的 SLO；§...

=== use (4 occurrences) ===
  c24_b03.md: ...a、regex 约束）。结构是请求规范的一部分；推理引擎知道如何处理。  (c) **函数调用**：原生支持 tool use；radix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输...
  c27_b11.md: ...# LLM-generated cross-region payment coordinator     # We use CockroachDB which is CP for consistency      def process_pa...
  c28_b03.md: ...y<br/>TLS / auth / rate limit]         end         subgraph USE["US-East Region"]             APIE[API Service]...
  c28_b03.md: ...RATUSE --> TB         style Edge fill:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         style U...

=== nvlink (4 occurrences) ===
  c25_b02.md: ...ill 完成后，prefill 节点把 KV cache 发送给 decode 节点。 * 通过 RDMA（第6章）或 NVLink（同 chassis 内）传输。 * 大小：每个请求通常 1-10 GB（取决于 prompt 长度和模型）。 * 时间...
  c25_b02.md: ...通常 1-10 GB（取决于 prompt 长度和模型）。 * 时间：~10-100 ms（通过 200 Gbit/s NVLink 或 InfiniBand 的 RDMA）。  KV cache 传输是新的运维关注点：它消耗网络带宽并增加首 toke...
  c25_b07.md: ...**KV cache 预取**：prefill 快完成时，开始把早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RD...
  c25_b07.md: ...(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请求），跳过该部...

=== priority (4 occurrences) ===
  c26_b04.md: ...customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weight...
  c26_b04.md: ...inuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运...
  c26_b04.md: ...customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weight...
  c26_b04.md: ...inuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 tier 客户即使在尖峰也有有界服务。  运...

=== cross (4 occurrences) ===
  c27_b11.md: ...注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use CockroachDB which i...
  c28_b03.md: ...replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]...
  c28_b03.md: ...E         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"|...
  c28_b03.md: ..."Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW...

=== sub (4 occurrences) ===
  c28_b02.md: ...合同。  (i) **欺诈检测**：Continuum 风格的 LLM 推理（Tobias 的组件）。每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus m...
  c28_b07.md: ...审计追踪系统集成是难点；编辑器本身很容易。”  **Tobias（从 Continuum 借调）**：“欺诈检测推理是 sub-100-ms p99（p50 80 ms；与 §28.2 记录一致），得益于分离式架构和 KV-cache-aware...
  c29_b02.md: ...#### LLM 推理将向规模化 sub-100 ms per-token 延迟演进  2026 现实（第24-26章）：per-token decode 延迟...
  c30_b03.md: ...A Blackwell (B100 / B200) 及 Hopper 继任者产品简介（2024-2026 发布）。 — sub-100-ms ITL 声明背后的硬件轨迹（§29.1，对回顾准确性有保留）。 * Fedus, W., Zoph, B...

=== reilly (4 occurrences) ===
  c30_b01.md: ...mann, M. (2017). _Designing Data-Intensive Applications_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanenbaum, A. S., & Van Steen, M. (2017...
  c30_b03.md: ...020). _Chaos Engineering: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J., & Murphy, N. R. (2016)...
  c30_b03.md: ..., & Murphy, N. R. (2016). _Site Reliability Engineering_. O'Reilly. — 重试预算和熔断器作为生产纪律。 * The Jepsen reports (https://jepsen.io)...
  c30_b03.md: ...* Rosenthal, C., & Jones, N. (2020). _Chaos Engineering_. O'Reilly. — 已在运维部分列出，此处再次引用，因为混沌工程被预测会变得更加重要而非更小（§29.2）。  * * *...

=== schwartz (3 occurrences) ===
  c01_b03.md: ...行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Strat...
  c23_b13.md: ...分：分布式 AI 推理。推理集群作为 2026 年的子学科；vLLM、SGLang、NVIDIA NIM；Tobias Schwartz 的 1M USD/月 Continuum Inference 集群作为工作示例。  ---...
  c24_b01.md: ...Tobias Schwartz 是 Continuum Inference 的 ML 平台工程师。公司为 enterprise 客户运行 LLM 推理...

=== bloom (3 occurrences) ===
  c01_b05.md: ...存在且值得研究；每个都有自己的经典参考；若纳入都会扭曲本书焦点。  * **存储引擎内部细节** ：LSM 树压实调优、Bloom 过滤器大小、B+ 树写放大优化、页缓存行为、fsync 顺序。我们会在影响分布式推理的地方触及这些（第12章），但不会...
  c12_b09.md: ...table + WAL 顺序）。80% 写工作负载下表现好。 * 读延迟：每次读可能 touch memtable + Bloom filter + 多个 SSTable。冷读 p99 可能 1-5 ms。 * 空间放大：典型 1.5-2 倍（com...
  c12_b09.md: ...上，B+ tree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20% 读占比在 RocksDB 中配合 Bloom filter 做点查仍可接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正确选择。SQLite...

=== dwork (3 occurrences) ===
  c01_b05.md: ...d Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Differential Privacy...
  c07_b02.md: ...上对许多实践目的等价。现代协议通常用部分同步加隐式故障检测器（心跳）。   ### 7.4 部分同步  经典形式化来自 Dwork、Lynch 和 Stockmeyer 1988 年关于部分同步下共识的论文。模型引入 _Global Stabiliz...
  c30_b02.md: ...th one faulty process." _Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the...

=== learning (3 occurrences) ===
  c01_b05.md: ...g_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundat...
  c08_b02.md: ...D(b, v)`。  当 proposer 收集到多数 `ACCEPTED` 时，值 v 被 _选定_。   #### Learning  一旦值被选定，learners 必须学到它。最简单做法：acceptor 接受一个值且其支持达到多数时，就告诉 le...
  c30_b19.md: ...26-05-20 作为 post-pipeline 润色的一部分生成。_ _QBooks · A Socratopia Learning Series Publication_  * * *  ---...

=== vol (3 occurrences) ===
  c01_b05.md: ...ic/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Series IV Vol 3（可观测性）的主题。参考：Sridharan _Distributed Systems Observability_...
  c01_b05.md: ...* **应用层协议深度** ：REST API 设计（在 _Software at Scale_ Series IV Vol 1 _APIs_ 中覆盖）、gRPC schema 演进、OpenAPI 工具链。第19章会提到服务发现；协议本身超出...
  c20_b01.md: ...ce context。本章范围受限：深入的 OpenTelemetry pipeline 细节属于 Series IV Vol 3（可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什么需要分布式追踪  单体应用中，sta...

=== ebpf (3 occurrences) ===
  c01_b05.md: ...度）；Pavlo & Aslett 的 CMU 15-721 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储...
  c19_b10.md: ...）或完全不用网格。  第二个考虑：服务网格价值很大程度上取决于数据面技术。Istio 的数据面 Envoy 很重；基于 eBPF 的替代（Cilium）更轻。随着 eBPF 网格成熟，服务网格运维成本下降，成本收益计算会改变。  **Q2.** 一...
  c19_b10.md: ...值很大程度上取决于数据面技术。Istio 的数据面 Envoy 很重；基于 eBPF 的替代（Cilium）更轻。随着 eBPF 网格成熟，服务网格运维成本下降，成本收益计算会改变。  **Q2.** 一个 LLM 被要求 “为我们的微服务舰队实现...

=== checklist (3 occurrences) ===
  c02_b03.md: ...#### 常见误解  许多读者第一次看到八大谬误时，把它们当作 checklist：“我读过了；我吸收了；我可以写分布式代码了。”谬误不是这样用的。教训不是让你记住清单。教训是：单机直觉默认错误，每个分...
  c02_b14.md: ...。   ### 本章总结  Deutsch 的八大谬误命名了产生大多数分布式系统 bug 的单机直觉。谬误不是要记住的 checklist；它们是审计每段分布式代码时应检查的假设类别。第21章会作为级联失败重新审视网络可靠性的谬误；第18章会作为锁与协调 b...
  c27_b07.md: ...* 人类应用 AI 无法做的上下文判断：哪把锁用于正确性、哪个不变量必须成立、哪个设计选择正确。 * 人类用显式短语 “checklist reviewed” 签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，c...

=== vpc (3 occurrences) ===
  c02_b07.md: ...条消息的头成本远超实际日志负载，聚合器网络远未饱和 CPU 就已先饱和。  _网络是安全的。_ 示例：内部服务接受来自 VPC 的任意调用者，因为“VPC 是私有的”。集群中一个被攻破的 pod 现在可以作为任意调用者行动，因为没有按调用者认证。...
  c02_b07.md: ...载，聚合器网络远未饱和 CPU 就已先饱和。  _网络是安全的。_ 示例：内部服务接受来自 VPC 的任意调用者，因为“VPC 是私有的”。集群中一个被攻破的 pod 现在可以作为任意调用者行动，因为没有按调用者认证。  _拓扑不会变。_ 示例：...
  c14_b03.md: ...ctive 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 AWS。 * 较新（生产 track...

=== versus (3 occurrences) ===
  c02_b11.md: ...4 的计数器-排行榜-库存工作负载，解释为什么每个的一致性需求是这样。然后提出一个可以服务三者的单一系统。选择一个系统 versus 三个专用系统，各自的权衡是什么？  提示  Spanner 可以服务三者（库存用线性化、计数器用过期读、排行榜用会话读...
  c02_b11.md: ...么？  提示  Spanner 可以服务三者（库存用线性化、计数器用过期读、排行榜用会话读实现类因果）。权衡是运维简单 versus 延迟成本。  **答案：**  _计数器_：最终一致即可，因为用户可见的不一致成本（展示浏览量晚几秒）远低于线性化计数...
  c02_b13.md: ...店执行什么操作？库存提交和支付提交需要线性化；商品浏览和评论阅读不需要。 * 部署 footprint 是什么？单区域 versus 多区域会剧烈改变延迟画像。 * 团队的运维成熟度如何？一个没有分布式数据库经验的团队从托管 PostgreSQL 获得...

=== parallel (3 occurrences) ===
  c03_b02.md: ...o b 且 b \to c，则 a \to c（传递性）。  不满足上述三条规则的两个事件称为 _并发_ ，记作 a \parallel b。并发在这个技术意义中指“我们无法确定哪个先发生”，而不是“它们在同一 wall-clock 瞬间发生”。  hap...
  c03_b03.md: ...2,2,2]  现在我们能检测并发。V(a) = [1,0,0] 与 V(x) = [0,0,1] 互不小于对方；a \parallel x（并发）。V(send m) = [2,0,0] < V(recv n) = [2,2,2]，所以 send(m)...
  c29_b02.md: ...* **MoE（Mixture-of-Experts）** 模型成为 >100B 参数模型的默认，需要 expert-parallel routing 作为新原语（本书未深入覆盖；预计 Series IV Vol 4 会讲）。 * **基于扩散的语言模型...

=== avva (3 occurrences) ===
  c03_b05.md: ...向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一...
  c03_b05.md: ...时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{wall\_clock\_n...
  c30_b01.md: ...CM_. — 第3章必读。 * Kulkarni, S. S., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks." _OPODIS...

=== demirbas (3 occurrences) ===
  c03_b05.md: ...3.5 混合逻辑时钟（HLC）  向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock...
  c03_b05.md: ...其中 l 是 wall-clock 时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{w...
  c30_b01.md: ...." _Communications of the ACM_. — 第3章必读。 * Kulkarni, S. S., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Ph...

=== leone (3 occurrences) ===
  c03_b05.md: ...差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物理时间，一个逻辑计数器组件...
  c03_b05.md: ...钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{wall\_clock\_now}()；计算...
  c30_b01.md: ...。 * Kulkarni, S. S., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks." _OPODIS_. — HLC 论文。...

=== madappa (3 occurrences) ===
  c03_b05.md: ...钟（HLC）  向量时钟扩展性差；wall-clock 时间戳撒谎。混合逻辑时钟由 Kulkarni、Demirbas、Madappa、Avva 和 Leone 在 2014 年 HLC 论文中提出，结合两者：一个 wall-clock 风格的组件贴近物...
  c03_b05.md: ...ll-clock 时间（平台时钟分辨率），c 是一个小整数计数器。以下规则直接遵循 Kulkarni、Demirbas、Madappa、Avva 和 Leone（2014）：  * **本地事件** 在进程 p：令 t = \text{wall\_clo...
  c30_b01.md: ...ions of the ACM_. — 第3章必读。 * Kulkarni, S. S., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks...

=== faunadb (3 occurrences) ===
  c03_b08.md: ...HLC + max-clock-skew 窗口 | 读观察到“未来”写入时事务重启（在偏移窗口内） Calvin / FaunaDB | 由 sequencer 确定性排序 | 到 sequencer 的延迟；不涉及时钟  正确选择取决于你的运营 fo...
  c13_b04.md: ...quencer 跨区域分片，并通过跨区域共识将区域排序合并为全局排序。   #### Calvin 用于何处  * **FaunaDB** ：基于 Calvin 的商业数据库；面向全球分布的可串行化事务。 * **研究系统** ：Calvin 的确定性执...
  c13_b05.md: ...DB / YugabyteDB | HLC + skew bound | 同 CockroachDB Calvin / FaunaDB | Sequencer 分配 | Sequencer 往返  2026 年的现实：大多数非 Google 分布式数据库...

=== uncertainty (3 occurrences) ===
  c03_b12.md: ...入在 wall-clock 时间中是否发生在事务开始之前，于是 _重启_ 事务，用新时间戳推到观察到的写入之后。这就是“uncertainty restart”路径。  实际含义：  * Spanner 的成本是每个事务恒定的。CockroachDB 的成本只在...
  c03_b12.md: ...务（命中不确定性窗口的）付，其他不付。 * 在 CockroachDB 中，对最近写入键有高竞争的工作负载可能遭受大量 uncertainty restarts。500 ms 上界是最坏情况；收紧它（通过严格 NTP 纪律）会成比例减少重启。 * Spanner...
  c13_b05.md: ...口_ 内，其真实时间位置模糊。为保持可串行化，事务 _重启_，取新的开始时间戳，并严格推到 $t_w$ 之后。这就是 “uncertainty restart”。  代价按重启支付，而非按提交支付。低争用工作负载很少触发重启；高争用最近写入键的工作负载会看到频繁...

=== dest (3 occurrences) ===
  c03_b13.md: ...return self.L, description          def send(self, dest, msg):             self.L += 1             # 消息携带 (L, msg)。...
  c03_b13.md: ..., msg):             self.L += 1             # 消息携带 (L, msg)。dest 是另一进程。             dest.receive(self.L, msg)             re...
  c03_b13.md: ....L += 1             # 消息携带 (L, msg)。dest 是另一进程。             dest.receive(self.L, msg)             return self.L          def...

=== pid (3 occurrences) ===
  c03_b13.md: ...案：**       class LamportProcess:         def __init__(self, pid):             self.pid = pid             self.L = 0...
  c03_b13.md: ...tProcess:         def __init__(self, pid):             self.pid = pid             self.L = 0          def local_event(self,...
  c03_b13.md: ...ss:         def __init__(self, pid):             self.pid = pid             self.L = 0          def local_event(self, descr...

=== buffer (3 occurrences) ===
  c04_b03.md: ...to_all(msg, V)      on receive(msg, V_msg, sender):         buffer.add((msg, V_msg, sender))         try_deliver_from_buffer()...
  c04_b03.md: ...deliverable = None             for (msg, V_msg, sender) in buffer:                 # 当所有因果上先于它的消息都已交付时才可交付。                 i...
  c04_b03.md: ...s None:                 return             # 交付             buffer.remove(deliverable)             V[deliverable.sender] += 1...

=== under (3 occurrences) ===
  c05_b01.md: ...所有已确认写。 * **Write latency** ：下界是最慢副本的往返时间。 * **Availability under replica failure** ：若任何副本不可达，写入阻塞，直到副本恢复或被移出集合。  慢副本阻塞写入这一性质...
  c05_b02.md: ...ency** ：若主库在写传播前死亡，该写 _丢失_ 。被提升为新主库的副本没有它。 * **Availability under replica failure** ：写不受副本健康影响。  丢失写窗口是异步复制的风险。对大多数工作负载（分析、内容...
  c05_b02.md: ...。 * **Write latency** ：受最快 _合格_ 副本的往返时间限制。 * **Availability under replica failure** ：若没有合格副本可达，写入阻塞。  “合格”副本通常是配置集合；主库挑第一个 ac...

=== intersection (3 occurrences) ===
  c10_b05.md: ...Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisited” 推广了 Paxos 的法定人数要求。经典 Paxos 要求 prepare 法定人数 $Q_1$...
  c30_b02.md: ...alkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak,...
  c30_b06.md: ...t 1989/1998 共识协议。两阶段（prepare、accept）加 quorum 交集。 * **Quorum intersection（quorum 交集）**：N 个节点中任意两个多数派至少共享一个节点；共识安全基础。 * **Raft**：Ongar...

=== progress (3 occurrences) ===
  c05_b03.md: ...标副本集中的节点满足（Dynamo 的 “hinted handoff”）。相交保证被破坏。 * **Write in progress read** ：若写正在 N 个节点中的 W 个上执行，读命中 R 个节点可能只看到写者集合中少于 W 的节点（写者处...
  c07_b03.md: ...“好事最终会发生”的性质。示例：  * Termination 是 liveness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获...
  c07_b07.md: ..._ 性质说“好事最终会发生”。它只被好事件永不发生的无限执行违反。示例：termination（每个正确进程最终决定）、progress。  为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择...

=== kyle (3 occurrences) ===
  c05_b04.md: ...加纪律下才实现（例如返回前读修复，或通过 per-session 写入记忆实现 read-your-writes）。  Kyle Kingsbury 对 Cassandra、Riak 等的 Jepsen 分析记录了无数团队期望法定人数给线性一致性却...
  c23_b03.md: ...### 23.5 Jepsen 传统  Kyle Kingsbury 的 Jepsen 项目是混沌的另一种风味：不是 “向生产注入故障”，而是 “通过注入故障系统性地测...
  c23_b07.md: ...Jepsen：系统性地测试分布式数据库声明。发现许多 bug；提高领域正确性基线。  **答案：** Jepsen 是 Kyle Kingsbury 的项目（约 2013 年启动），在故障条件下系统性地测试分布式数据库的正确性声明。  方法：  *...

=== cost (3 occurrences) ===
  c06_b03.md: ...订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在它们之间做...
  c11_b09.md: ...| 中等（架构已处理大部分） Ecosystem | 更大（项目更老） | 成长中；Cassandra 兼容 API Cost | 开源；众多云托管 offering | 开源 + ScyllaDB Cloud  对分析工作负载（通常高吞吐、可容...
  c13_b05.md: ...试延迟有效支付 TrueTime 成本。   #### 对比  System | Timestamp source | Cost ---|---|---- Spanner | TrueTime（GPS + 原子钟） | 每次写 7 ms commi...

=== tokyo (3 occurrences) ===
  c06_b09.md: ...Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower...
  c06_b09.md: ...：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 ms；EU-West ↔ Tokyo ~250 ms。  * US-East 发起：最快 follower = EU-West，90 ms。 * EU-We...
  c06_b09.md: ...EU-West，90 ms。 * EU-West 发起：最快 follower = US-East，90 ms。 * Tokyo 发起：最快 follower = US-East，200 ms。  最坏情况 Raft 提交：**~200 ms**（...

=== aggregation (3 occurrences) ===
  c06_b06.md: ...ine 拓扑。它为何取代三层网络？  提示  Leaf-spine 中每个 leaf 连到每个 spine；经典三层中 aggregation 交换机成为带宽瓶颈。  **答案：** Leaf-spine 有两层：leaf 交换机连接服务器，每个 leaf 都连...
  c06_b06.md: ...机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三层的 aggregation 交换机对东西向（服务器到服务器）流量成为瓶颈，而现代分布式工作负载...
  c06_b06.md: ...p，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三层的 aggregation 交换机对东西向（服务器到服务器）流量成为瓶颈，而现代分布式工作负载以东西向为主；leaf-spine 提供任意服务器间...

=== fischer (3 occurrences) ===
  c07_b01.md: ...共识问题。它听起来不难，但 “到底有多难” 的精确陈述是分布式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即使只可能发生一次...
  c30_b02.md: ...#### 共识（第三部分 — 第7-10章）  * Fischer, M. J., Lynch, N. A., & Paterson, M. S. (1985). "Impossibil...
  c30_b06.md: ...颁发的单调递增计数器；被保护资源强制执行令牌顺序。 * **FLP impossibility（FLP 不可能性）**：Fischer-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不能保证终止。 * **Gray fail...

=== paterson (3 occurrences) ===
  c07_b01.md: ...式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即使只可能发生一次崩溃故障的完全异步系统中，任何确定性共识协议都无法保证终止。这...
  c30_b02.md: ...#### 共识（第三部分 — 第7-10章）  * Fischer, M. J., Lynch, N. A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with...
  c30_b06.md: ...资源强制执行令牌顺序。 * **FLP impossibility（FLP 不可能性）**：Fischer-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不能保证终止。 * **Gray failure（灰色故障）**：进程通...

=== safe (3 occurrences) ===
  c07_b07.md: ...为何对共识重要：FLP 说在纯异步下一次崩溃下，协议不能同时满足 safety 和 liveness。现代协议选择始终 safe，liveness 以部分同步为条件。这意味着：  (a) 协议在任何网络条件下都可证正确；没有任何执行违反 agree...
  c07_b11.md: ...ess 性质（“好事最终发生”）只在网络稳定后保证。Crescent 多区域提交日志使用 Raft，它在所有网络条件下 safe，但在分区期间可能暂停。  三条性质映射到真实协议的具体设计选择：agreement 用法定人数相交，validity...
  c08_b01.md: ...Paxos（就一个值达成一致）和 Multi-Paxos（就值序列达成一致）。重点不是背消息，而是理解使 Paxos safe 的结构不变式。Raft（第9章）更容易实现，但骨架相同。   ### 8.1 场景  一组 N 个进程，每个扮演三种角...

=== certificate (3 occurrences) ===
  c07_b03.md: ...follower 已复制的日志条目。HotStuff 选择一个由 2f+1 of 3f+1 验证者签名的 quorum certificate。三者都以法定人数作为 agreement 的安全机制。...
  c07_b05.md: ...人数 | Leader 只追加自己提议的条目 | Leader 选举 + 心跳超时 HotStuff | Quorum certificate（由 3f+1 中 2f+1 签名） | 对提议做 validity 检查 | 超时后 view change；新 le...
  c10_b03.md: ...每阶段是一轮 _投票_ ：副本对 leader 的提议投票；leader 收集 2f+1 个投票组成 _Quorum Certificate（QC）_。 * QC 是某阶段结果的支持证明，可传递；后续阶段通过 hash 引用 QC，而非重播所有投票。 * Vi...

=== precommit (3 occurrences) ===
  c07_b05.md: ...检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交...
  c10_b04.md: ...BFT 协议。Tendermint 用部分 HotStuff 特性换取简单性：  * 两阶段投票（prevote + precommit），每阶段都需要 2f+1 of 3f+1 票。 * 基于轮次：若一轮未达成决定，下一轮以确定性轮转选新 leader。...
  c10_b04.md: ...1 of 3f+1 票。 * 基于轮次：若一轮未达成决定，下一轮以确定性轮转选新 leader。 * 锁定：一旦验证者 precommit 某值，它在后续轮次中被“锁定”到该值。  Tendermint 比 HotStuff 更易实现和调试。吞吐较低（因为每...

=== vote (3 occurrences) ===
  c07_b08.md: ...。这减少心跳流量，但降低活性信号频率；更长间隔意味着真实故障最坏检测时间变长。  (c) 使用 Raft 的 “pre-vote” 扩展（etcd 近期版本已实现）：考虑选举的 follower 先问 peer “你会投我吗？” 再递增 term。...
  c07_b08.md: ...若 peer 回答“不，我已有当前 leader”，则该 follower 不触发集群扰动。  推荐修复：启用 pre-vote（消除误怀疑扰动）并把选举超时调到约 5 倍观察到的最坏跨 DC RTT。  **Q2.** 一个团队为复制配置存储在...
  c09_b10.md: ...反驳：完整 Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego...

=== diego (3 occurrences) ===
  c07_b11.md: ...把单法令 Paxos 扩展到决策序列，这是生产部署（Spanner、Megastore）的真实形式。第9章讲 Raft，Diego Ongaro 和 John Ousterhout 2014 年明确为比 Paxos 更易理解而设计。  ---...
  c08_b10.md: ...ty 保证。大多数新分布式系统（etcd、Consul、TiKV、CockroachDB、SpiceDB）用 Raft。Diego Ongaro 的博士论文 “Designing for Understandability” 直接针对 Paxos 的...
  c09_b10.md: ...e-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego Ongaro 博士论文数百页；etcd Raft 实现数千行。  综合：Raft 在 _基本情况_ 和 _常见情况_...

=== chubby (3 occurrences) ===
  c08_b01.md: ...终发表。  论文描述的协议就是 Paxos，它是分布式系统史上最具影响力的共识算法。Google 用 Paxos 构建 Chubby（Spanner 的基础）。ZooKeeper 的 ZAB 是 Paxos 变体。该协议以难正确实现著称；Lamport...
  c08_b04.md: ...anner、etcd），要么实现 Raft。  民间智慧：“世界上正确的 Paxos 实现寥寥无几。”Google 从 Chubby（基于 Paxos）迁移到后继系统时，之所以保留 Paxos 是因为工程师积累了深厚运维经验；新团队因同样原因选择 Ra...
  c08_b10.md: ...的 leader 瓶颈设计。Flexible Paxos 允许运维调节法定人数大小，Raft 不行。Google 的 Chubby 和 Spanner 基于 Paxos，有数十年生产伤痕沉淀。  综合：对 2026 年新通用系统，Raft 是正确选择...

=== part (3 occurrences) ===
  c08_b01.md: ...Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。它描述了一座虚构希腊岛屿，兼职立法者通过涉及信使、账簿和法定人数的繁复仪式颁...
  c30_b01.md: ...s in Modern Distributed Database System Design: CAP is Only Part of the Story." _IEEE Computer_. — PACELC 论文。 * Lamport, L....
  c30_b02.md: ...Processing Letters_. — 安全性/活性分解。 * Lamport, L. (1998). "The Part-Time Parliament." _ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L....

=== megastore (3 occurrences) ===
  c08_b04.md: ...优化** ：pipelining、batching、leader lease——每个都有微妙交互。  Google 的 Megastore 论文（2011）和 Spanner 论文（2012）描述了生产级 Paxos 的样子；细节令人清醒。libpaxos...
  c08_b11.md: ...os 以难实现著称：持久化纪律、ballot 唯一性、法定人数 off-by-one、重配置、快照。Spanner 和 Megastore 是经典生产实现。附录 A 的阅读列表包括原始 1998 年论文、2001 年 “Paxos Made Simple”...
  c09_b01.md: ...Reddy 两个月前加入 Stratus DB。她的第一个项目：为复制层选个共识库。她读了 Paxos 论文。她读了 Megastore 论文。她试图在周末实现单法令 Paxos。周一早上她说：“我脑子里装不下这些。一定有更好的办法。”  她的技术 lea...

=== persistent (3 occurrences) ===
  c08_b04.md: ...Lamport 的 “Paxos Made Simple” 论文 14 页。实际实现数千行。差距在运维细节：  * **Persistent state** ：acceptor 必须在崩溃间持久化 $b_{max}$、`accepted_ballot`、`ac...
  c19_b07.md: ...nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral n...
  c19_b07.md: ...（lease + revision number）。 * 配置：ZK persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys wit...

=== epaxos (3 occurrences) ===
  c08_b10.md: ...Paxos 的复杂性。对新团队来说，Raft 是正确默认。  反驳：Paxos 有 Raft 没有的属性，视变体而定。EPaxos 无 leader；对低竞争工作负载，其吞吐高于 Raft 的 leader 瓶颈设计。Flexible Paxos 允...
  c09_b05.md: ...内部公司系统。  Paxos 仍在某些细分领域获胜：Google 基础设施（Chubby、Spanner）、专门变体如 EPaxos 用于高吞吐无 leader 工作负载、Flexible Paxos 用于可调法定人数。但对 2026 年的新项目，默认...
  c09_b05.md: ...单服务器变更（Raft joint consensus 的安全子集）。  她放弃的：无 leader 写（CRDT 或 EPaxos 可提供）；per-tablet 性能调优（Spanner 的 Paxos-with-leases 可提供）。对 Str...

=== mid (3 occurrences) ===
  c08_b10.md: ...epted ballot 的值）是核心 safety 不变式。LLM 代码常无条件使用自己的值，proposer 崩溃 mid-protocol 时立即违反 safety。...
  c26_b02.md: ...burst” 节点；长请求去 “sustained” 节点。  (c) 误预测：若预测错误（本应短却长，或相反），请求 mid-flight 迁移到更合适节点（KV cache 传输；decode 在新节点恢复）。  误预测是运维关注点。80%...
  c26_b03.md: ...宽：不大（每请求每步几个 token），但真实存在。  (c) 同步复杂度：draft 与 verify 必须保持对齐；mid-request 迁移非平凡。  2026 年，分布式 speculative decoding 处于早期生产部署。已发...

=== how (3 occurrences) ===
  c08_b11.md: ...列表包括原始 1998 年论文、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角...
  c18_b03.md: ...#### Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它为何没有提供表面上的安全性质。该文...
  c30_b03.md: ...#### 协调与运维（第六、七部分 — 第18-23章）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * H...

=== using (3 occurrences) ===
  c08_b11.md: ...le” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ousterhout 2014 年为比...
  c30_b02.md: ...D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications." _OSDI_. — Perc...
  c30_b03.md: ...al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024...

=== search (3 occurrences) ===
  c09_b01.md: ...把 Raft 论文递给她。  Raft 由 Ongaro 和 Ousterhout 设计，2014 年论文题为 “In Search of an Understandable Consensus Algorithm”，目标是：至少和 Paxos 一样正...
  c22_b10.md: ...复杂度相关；较不复杂的系统暴露更少。  综合：复杂分布式系统会发生亚稳态故障。Crescent 支付平台、Google Search、AWS S3 都足够复杂，已展示过亚稳态。简单系统可能确实避免它。准备包括 (a) 避免不必要的复杂度，(b) 为反馈...
  c30_b02.md: ...T News_. — 必读改写。 * Ongaro, D., & Ousterhout, J. (2014). "In Search of an Understandable Consensus Algorithm." _USENIX ATC_. —...

=== terms (3 occurrences) ===
  c09_b01.md: ...要：每个部分可独立理解（和测试）。Paxos 的整块结构让这更难；Raft 的拆分是核心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。...
  c09_b01.md: ...要：每个部分可独立理解（和测试）。Paxos 的整块结构让这更难；Raft 的拆分是核心设计决策。   ### 9.2 Terms  Raft 中的时间被划分为 _terms_。每个 term 至多有一个 leader。Term 用单调递增整数编号。...
  c09_b11.md: ...。它把共识分解为三个正交子问题（leader election、log replication、safety），并基于 terms、elections 和 AppendEntries 一致性检查提供清晰的运维模型。  Anya 为 Stratus D...

=== revert (3 occurrences) ===
  c09_b01.md: ...知的最高 term）。 * 服务器在任何 RPC 中看到更高 term 时，更新自己的 `currentTerm` 并 revert 为 follower（若它之前是 leader 或 candidate）。 * 服务器在 RPC 中看到较低 term...
  c09_b06.md: ...erm 至多一个 leader。每个 Raft RPC 携带发送者 term，任何节点看到更高 term 就更新自己并 revert 为 follower。  与 Paxos ballot 对比：  * Paxos ballot number 也单调递...
  c09_b09.md: ...r” 或超时；读失败或返回过期数据。  _恢复：_  * 分区愈合后，少数侧节点在多数侧心跳中发现更高 term。它们 revert 为 follower，同步日志到多数侧，重新加入。 * 无数据丢失；无不一致。少数侧挂起的写从未提交，被放弃。  **...

=== index (3 occurrences) ===
  c09_b03.md: ...wer 日志在该点之前与 leader 日志一致。 * 否则，追加新条目（覆盖任何冲突的后续条目），更新 commit index，返回成功。  Consistency check 是 Raft safety 的核心。通过要求 follower 日志...
  c09_b07.md: ...ader 分歧）。  否则，追加新条目（覆盖任何冲突的后续条目），按 `leaderCommit` 推进 commit index，回复成功。  这保证：一次成功 `AppendEntries` 后，follower 日志在新条目末尾之前与 lead...
  c09_b08.md: ...常批量，数十或数百条）。Follower 追加，按 leader 的 `leaderCommit` 推进 commit index，回复成功。  (e) 一批批地，follower 追上当前日志末尾。1000 条目、RTT 50 ms、每 RPC 1...

=== corner (3 occurrences) ===
  c09_b04.md: ...后，才顺带提交 previous term 的条目（这有副作用：连带提交之前的所有条目）。  原因：没有这条规则，某个 corner case 会让已提交条目后来被覆盖。Raft 论文的图 8 说明了该情况；修复就是“current term only...
  c09_b04.md: ...来提交条目。两个条件一起防止图 8 问题。  实践中很少要紧，因为新 leader 通常很快提交新条目，但实现必须处理 corner case。   ### 9.6 日志压缩  Raft 日志无限增长。生产系统使用快照：定期把状态机完整状态写入快照，并...
  c09_b10.md: ...时，才顺带提交 previous term 的条目。LLM 实现常直接提交 previous term 条目，在图 8 corner case 中违反 safety。  还有三条（任一条都会造成静默 safety 违反）：  (d) **AppendE...

=== current (3 occurrences) ===
  c09_b04.md: ...：没有这条规则，某个 corner case 会让已提交条目后来被覆盖。Raft 论文的图 8 说明了该情况；修复就是“current term only”提交规则。这是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常...
  c29_b03.md: ...是当下生产现实。成本、延迟底线、系统选择都是今年的快照。通读；这是正典层。  * **2027**：本书大部分内容仍是 current。2026 年系统仍在生产；2026 年协议仍是标准。通读。  * **2028**：部分具名系统可能已弃用或被收购/更...
  c29_b04.md: ...故障模式会被添加。   ### 29.5 结语  你在 2026、2027 或更晚读这本书。具体年份影响哪些具名系统仍 current。它不影响心智模型。  Olamide、Anya、Yuki 和 Tobias 是复合人物；他们的系统和事件是作者工作和评...

=== only (3 occurrences) ===
  c09_b04.md: ...rner case 会让已提交条目后来被覆盖。Raft 论文的图 8 说明了该情况；修复就是“current term only”提交规则。这是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常见误解  Raft...
  c30_b01.md: ...deoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IEEE Computer_. — PACELC 论文。 * Lamport...
  c30_b16.md: ...n HA 却不付多 region 税。" | 跨 region quorum 每次写入都付跨 region 成本。DR-only 是异步；HA 是 quorum。 | Ch.6 | | 11 | "FLP 说共识不可能。" | FLP 说确定性共识...

=== rust (3 occurrences) ===
  c09_b05.md: ...TiKV 的 Raft 已生产运行多年，bug 已被发现并修复。 (b) **语言匹配** ：Stratus DB 是 Rust；raft-rs 原生。 (c) **运维工具** ：该库以 Stratus 现有可观测性栈可摄取的形式暴露状态机、日志...
  c16_b03.md: ...erge 2.x（2023 年发布）是重大重写，将性能提升到生产可接受水平。早期版本在大规模下有内存问题；2.x 使用 Rust + WebAssembly，效率更高。  2026 年 Yjs 与 Automerge 的选择：  * Yjs：bun...
  c16_b07.md: ...顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能** ：合并操作用 Rust 编译为 WebAssembly，比纯 JS 快得多。 (c) **小 bundle size** ：Yjs ~80 K...

=== yin (3 occurrences) ===
  c10_b01.md: ...simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et al. 2019<br/>O(N) view change"]         HotStuff --> Die...
  c10_b03.md: ...议通过三个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的...
  c30_b02.md: ...). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abraham, I....

=== workhorse (3 occurrences) ===
  c10_b01.md: ...BFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et al. 2019<b...
  c17_b05.md: ...）。对多键事务，QUORUM 和 LWT 都不够；需要事务型系统。  在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...
  c17_b06.md: ...在 Cassandra 中选择 QUORUM 是 workhorse 模式；许多生产部署正是这样用。理解 QUORUM 只给相交（不是线性化）对避免意外正确性 bug 至关重要。...

=== malkhi (3 occurrences) ===
  c10_b03.md: ...个阶段；理解每阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新...
  c30_b02.md: ...onsensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi, D., & Spiegelman, A. (2016). "Flexible Paxos: Quorum Inter...
  c30_b02.md: ...ical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abraham, I. (2019). "Ho...

=== heidi (3 occurrences) ===
  c10_b05.md: ...### 10.6 Flexible Paxos：可调法定人数  Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisi...
  c10_b08.md: ...不让 view-change 成本主导。Pipelining（不同提议的三阶段重叠）进一步提高吞吐。  **Q4.** Heidi Howard 的 Flexible Paxos 推广了法定人数要求。陈述该推广并给出一个实践应用示例。  提示  只需...
  c10_b12.md: ...ol 与共识通过 DAG 结构分离，进一步提高吞吐。Tendermint 是 Cosmos 生态使用的更简单工作马。  Heidi Howard 的 Flexible Paxos 推广了法定人数要求：只需要 prepare 与 accept 法定人数...

=== fault (3 occurrences) ===
  c23_b02.md: ...elm 部署；可扩展。  (c) **Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重...
  c30_b02.md: ...基础。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter,...
  c30_b03.md: ...1）。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控制平面重读（§29.1）。 *...

=== decision (3 occurrences) ===
  c10_b10.md: ...是“不同决定在不同阶段同时进行”。  吞吐效果：稳态下两者都能达到每轮往返提交一个决定。HotStuff 的 per-decision 延迟是 3 轮往返，但因 pipelined 所以吞吐与 Raft 相近。差异体现在延迟：Raft 每次提交一轮往返；...
  c10_b10.md: ...；HotStuff 每次提交三轮往返，但 pipelined 使吞吐相同。  区块链含义：HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中是数百毫秒到数秒。若 per-decision 延迟关键，这是成本。若吞吐关...
  c10_b10.md: ...HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中是数百毫秒到数秒。若 per-decision 延迟关键，这是成本。若吞吐关键（例如每块处理大量交易的区块链），pipelining 吸收成本。...

=== isolation (3 occurrences) ===
  c11_b02.md: ...e 不同；serializable 给事务排序，linearizable 按实时给操作排序。 * **Snapshot isolation** ：事务看到一致快照，但不防止写-写冲突（写偏可能）。PostgreSQL（实际 REPEATABLE READ；S...
  c13_b01.md: ...提交，或都不提交。 * **Consistency（一致性）** ：数据库从一个有效状态转换到另一个有效状态。 * **Isolation（隔离性）** ：并发事务看起来按顺序执行。 * **Durability（持久性）** ：已提交事务在崩溃后存活。...
  c30_b06.md: ...**Serializable（可串行化）**：每次事务执行等价于某种串行顺序。弱于线性化。 * **Snapshot isolation（快照隔离）**：事务读取一致快照；存在写-写冲突（可能写偏）。 * **Speculative decoding（投机...

=== serializability (3 occurrences) ===
  c11_b03.md: ...| 比单区域 Postgres 写延迟更高；运维调优复杂 Right for | 多区域 Postgres 替代；需要 serializability 的金融工作负载...
  c11_b06.md: ...serializable 付更多（每次读走共识、commit-wait 或 HLC restart）。许多工作负载需要 serializability（并发下正确）但不需要 linearizability（实时下正确）。知道差别可避免过度付费。  **Q3.** 用诊断...
  c11_b08.md: ...确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——少数侧拒绝写；法定人数为副本多数...

=== writeconcern (3 occurrences) ===
  c11_b08.md: ...。他们问：“MongoDB 一致吗？”用诊断方法给出精确、可辩护的答案。  提示  MongoDB 的一致性取决于配置：writeConcern、readConcern、replica set vs sharded。默认与最强可用不同。  **答案：** Mong...
  c11_b08.md: ...oDB 的一致性高度依赖配置。精确答案必须说明哪种操作、哪种配置。  副本集（典型 3 节点）：  * _默认写_ ：`writeConcern: 1`（单节点 ack）。复制最终一致；若主库在复制前崩溃可能丢失。 * _Majority 写_ ：`writeCo...
  c11_b08.md: ...Concern: 1`（单节点 ack）。复制最终一致；若主库在复制前崩溃可能丢失。 * _Majority 写_ ：`writeConcern: majority`。写在被 ack 前已持久化到多数；主库故障后存活。 * _默认读_ ：`readConcern:...

=== sec (3 occurrences) ===
  c11_b09.md: ...受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compaction 调优...
  c11_b09.md: ...GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compaction 调优、GC 调优） | 中等（架构已处理大...
  c14_b10.md: ...原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7 万 reads/sec。大多是 “top-N 排行榜”，偶尔 “我的分数”。 * **一致性**：因果足够（若玩家 A 提交分数后玩家 B 在...

=== leveldb (3 occurrences) ===
  c12_b03.md: ...table 论文（Chang 等，2006 OSDI）推广，后被 Cassandra、RocksDB、ScyllaDB、LevelDB 等采用。  LSM 结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b...
  c12_b03.md: ...ckroachDB、TiKV、YugabyteDB、MongoDB WiredTiger 可选使用）、ScyllaDB、LevelDB 等都使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Si...
  c12_b03.md: ...sandra 默认使用。 * **Leveled compaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Lev...

=== neil (3 occurrences) ===
  c12_b03.md: ...数据库用 _LSM-tree_（Log-Structured Merge-tree）存储数据。LSM-tree 由 O'Neil 等人于 1996 年提出，由 Bigtable 论文（Chang 等，2006 OSDI）推广，后被 Cassandr...
  c30_b02.md: ...ge System for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-...
  c30_b02.md: .... — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (LSM-tree)." _Ac...

=== tiered (3 occurrences) ===
  c12_b03.md: ...使用 LSM。   #### Compaction 策略  两种主要 compaction 策略：  * **Size-tiered compaction** ：某层有够多大小相近的 SSTable 时，合并成更大的 SSTable。Cassandra...
  c12_b03.md: ...ompaction** ：维护大小分层的层；每层是前一层的 N 倍。LevelDB、RocksDB 使用。  Size-tiered 更快（写放大低）但空间放大更高（更多 SSTable 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择...
  c12_b03.md: ...able 共存）。Leveled 更省空间（合并更积极）但写放大更高。选择取决于工作负载：磁盘便宜、写重 = size-tiered；磁盘贵、读重 = leveled。...

=== approx (3 occurrences) ===
  c12_b04.md: ...行；总 400 MB/s）：  \[ \frac{8.4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  实践中，移动被节流以避免影...
  c12_b04.md: ...4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  实践中，移动被节流以避免影响客户流量；实际挂钟时间 12-18 小时。Anya...
  c12_b08.md: ...0{,}000 \text{ MB}}{400 \text{ MB/s}} = 21{,}000 \text{ s} \approx 5.8 \text{ 小时} \]  生产中，移动被节流以避免影响客户流量。节流至 200 MB/s 总吞吐（理论值的...

=== replay (3 occurrences) ===
  c12_b04.md: ...源节点把该 range 的 SSTable 文件流式传给目标节点。Snapshot 点之后的新写通过 Raft log replay 应用。  目标节点追上后，通知源节点。该 range 的 Raft group 领导权转移给目标节点，源节点的副本被移...
  c12_b05.md: ...修复是定期重新估算，集群每小时执行。  (b) **流式期间复制滞后** ：若网络饱和，目标节点通过 Raft log replay 追赶耗时超过预期。修复是在移动期间节流其他流量。  (c) **移动期间节点故障** ：源或目标节点故障需要重新规划移...
  c15_b08.md: ...或两阶段提交。  **答案：** Flink 的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offse...

=== oracle (3 occurrences) ===
  c12_b05.md: ...ree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree 优势：  * 可预测读延迟（一条 root-to-leaf 路径；深...
  c13_b02.md: ...务（JDBC 标准中的 XA 事务）。事务跨多个数据库，可能由不同团队或公司运维。XA 在 2000 年代初广泛用于跨 Oracle、SQL Server、DB2 的事务。  这种 2PC 用法在现代微服务架构中已基本被弃用。运维复杂度（跨组织协调者、...
  c14_b01.md: ...布式数据库。原始定义将 NewSQL 与以下两类对比：  * **传统 SQL** ：PostgreSQL、MySQL、Oracle。ACID，单节点或主从复制，纵向扩展。 * **NoSQL** ：Cassandra、DynamoDB、MongoDB...

=== owner (3 occurrences) ===
  c12_b10.md: ...键的两个副本落到同一节点。  (c) **成员变更期间的路由。** 生产代码正确处理节点增删期间的并发读/写：写发往新 owner，读在迁移完成前同时检查旧 owner 和新 owner。LLM 代码通常把成员变更视为瞬时，在并发流量下错误。  还有...
  c12_b10.md: ...**成员变更期间的路由。** 生产代码正确处理节点增删期间的并发读/写：写发往新 owner，读在迁移完成前同时检查旧 owner 和新 owner。LLM 代码通常把成员变更视为瞬时，在并发流量下错误。  还有三条（任一条都是生产 bug）：  (...
  c12_b10.md: ...路由。** 生产代码正确处理节点增删期间的并发读/写：写发往新 owner，读在迁移完成前同时检查旧 owner 和新 owner。LLM 代码通常把成员变更视为瞬时，在并发流量下错误。  还有三条（任一条都是生产 bug）：  (d) **哈希函数...

=== lagos (3 occurrences) ===
  c13_b01.md: ...Olamide 的支付账本需要跨分片原子事务。Adaeze 在 São Paulo 向 Lagos 的商家 Femi 付款，涉及 Adaeze 的账户（一个分片）、商家的账户（另一个分片）和商家的日汇总记录（第三个分片...
  c13_b10.md: ...account_id = 'adaeze';       -- 给 Femi 入账（不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance =...
  c13_b10.md: ...tent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日...

=== survive (3 occurrences) ===
  c13_b02.md: ...时才是非阻塞的_。真实生产部署通过以下方式绕过：  * **通过共识复制协调者**（Raft、Paxos）。协调者状态 survive 任何单点故障。这是 Spanner / CockroachDB 模式：每笔事务的协调者状态本身被复制。 * **使用...
  c27_b05.md: ...**测试抓不到 bug**。单元测试通常不演练分布式故障模式。实验室环境的集成测试看不到罕见但灾难性的故障。Bug  survive 测试，在生产中出现。  (e) **Bug 沉默**。分布式系统 bug（重复扣款、丢失写、split-brain）通...
  c27_b13.md: ...持久？** Saga 的进度（哪些步骤完成、哪些补偿需要）必须持久。验证：saga 状态存储在数据库或分布式日志中；能 survive 重启。  (d) **Saga orchestrator 是否容错？** 若 orchestrator 在 saga...

=== reader (3 occurrences) ===
  c13_b03.md: ...锁的读者会沿指针找到 primary，从而知道事务是否提交。  若客户端在阶段 2 崩溃，恢复可行：任何看到陈旧锁的 reader 可通过查看 primary 来解析。若 primary 已提交，则 secondary 锁代表已提交的写；否则可丢弃。...
  c13_b03.md: ...调者，因为 primary key 扮演了协调者角色。协调者故障被客户端故障取代，而客户端故障由读时恢复处理（任何后续 reader 都能清理）。   #### Percolator 用于何处  * Google Bigtable 的事务层（Bigta...
  c13_b12.md: ...secondary 锁的读者沿指针到 primary 了解事务结果。  若客户端在协议中途崩溃，由读者恢复：任何后续 reader 遇到陈旧锁时检查 primary 状态。若 primary 已提交，secondary 锁代表已提交写；否则可丢弃。清...

=== accounts (3 occurrences) ===
  c13_b10.md: ...NSACTION;       -- 检查 Adaeze 余额充足       SELECT balance FROM accounts WHERE account_id = 'adaeze';       -- 扣 Adaeze 款项（1200 BRL...
  c13_b10.md: ...是一笔 substantial 交易，不是       -- 第1章的 6-BRL 数据包）       UPDATE accounts SET balance = balance - 1200 WHERE account_id = 'adaeze';...
  c13_b10.md: ...不同分片、不同区域：Femi 在 Lagos，       -- 被划分到 EU-West）       UPDATE accounts SET balance = balance + 1200 WHERE account_id = 'femi';...

=== intent (3 occurrences) ===
  c13_b10.md: ...此读留在区域内。   3. 对 Adaeze 余额的 `UPDATE` 在她的分片（US-East）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lago...
  c13_b10.md: ...t）上获取 write intent。   4. 对 Femi 余额的 `UPDATE` 在他的分片上获取 write intent。Femi 是 Lagos 商家；他的数据被划分到 EU-West（服务非洲和欧洲的区域）。这次 `UPDATE` 是跨...
  c13_b10.md: ...洲的区域）。这次 `UPDATE` 是跨区域跳转。   5. 对日汇总的 `UPDATE` 在该分片上获取 write intent。   6. `COMMIT`：事务协调者（本身通过 Raft 复制）在选定的 commit_timestamp 标记事...

=== etl (3 occurrences) ===
  c14_b02.md: ...MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和 OLAP 无需 ETL。 * 运维成熟（在中国科技规模下久经考验：微信、阿里巴巴等）。  劣势：  * 英语社区比 CockroachDB 小...
  c14_b06.md: ...而不影响 OLTP 路径。  三系统架构是金融级系统的现代模式。每个系统是各自工作负载的正确工具；集成通过定义良好的 ETL 或 CDC 流水线，而非跨系统分布式事务。   ### 14.12 练习...
  c14_b13.md: ...gerBeetle 账本、Snowflake 分析）是金融级系统的现代模式。每个系统是各自工作负载的正确工具；集成通过 ETL 或 CDC 流水线。  2026 趋势是整合：许多初创公司可从托管 Postgres（Neon、Aurora Post...

=== tiflash (3 occurrences) ===
  c14_b02.md: ...）。 * TiDB 计算层（无状态 SQL 前端；可独立于 TiKV 扩展）。 * 兼容 MySQL 线缆。 * 通过 TiFlash（列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via...
  c14_b02.md: ...列式存储引擎用于分析查询）支持 HTAP。  优势：  * MySQL 兼容性（庞大现有生态）。 * HTAP via TiFlash：同一数据上 OLTP 和 OLAP 无需 ETL。 * 运维成熟（在中国科技规模下久经考验：微信、阿里巴巴等）。  劣...
  c14_b02.md: ...。  劣势：  * 英语社区比 CockroachDB 小。 * 多组件架构复杂（PD + TiDB + TiKV + TiFlash）；运维学习曲线。  TiDB 适合：已超出单节点规模、需要 HTAP 能力的 MySQL 工作负载。生产示例：许多中国...

=== presto (3 occurrences) ===
  c14_b03.md: ...QL 不同类别：分布式查询处理器处理分析工作负载（TB-PB 数据上的大扫描、聚合、跨源 join）。示例：Trino/Presto、Snowflake、BigQuery、Spark SQL。...
  c14_b04.md: ...#### Trino/Presto  源自 Facebook 的 Presto（2013）；2020 年分叉为 Trino，PrestoDB 由 Face...
  c14_b04.md: ...#### Trino/Presto  源自 Facebook 的 Presto（2013）；2020 年分叉为 Trino，PrestoDB 由 Facebook 继续。  * 分布式查询引擎；将工...

=== workload (3 occurrences) ===
  c14_b05.md: ...### 14.10 2026 决策矩阵  对新工作负载，选择映射为：  Workload | 首选系统 ---|--- 单区域 OLTP，规模适中 | Postgres 单区域 OLTP，高规模 | Post...
  c26_b08.md: ...#### 应用问题  **Q1.** Tobias 的集群现已分离且分布式 speculative。一位新客户 workload 有 5000-token system prompt。走一遍适用的路由优化。  提示  KV-cache-aware...
  c26_b10.md: ...路由器应考虑这点：基于哪个 decode 节点有容量来路由，然后该节点从缓存层读取。  (e) **客户亲和性**：对 workload 可预测的客户（相同 system prompt），把客户流量 pin 到特定节点以最大化缓存复用。  (f) **冷启...

=== fit (3 occurrences) ===
  c14_b10.md: ...V）** ：per-item 条件写做原子更新；最终一致读用于排行榜。单区域读写快；多区域表提供跨区域复制。工作负载很 fit。  (c) **Cassandra/ScyllaDB** ：可调一致性。ONE 一致性读非常快；QUORUM 写提供一...
  c16_b09.md: ...都能处理。 * **审计追踪**：项目管理应用常需要合规性的 “谁改了什么、何时”。Automerge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CSV。两个库都能处理，但导...
  c24_b06.md: ...KV cache，相比朴素系统 30-50%。 * **每张 GPU 更多并发请求**：更好内存利用率意味着更多请求 fit；每张 GPU 吞吐上升。 * **前缀共享**：共享 prompt 的请求共享前缀的 KV cache 块。对具有共同...

=== barrier (3 occurrences) ===
  c15_b03.md: ...andy-Lamport 的变体，用于流计算。定期地，一个 marker（在 Flink 中叫 “checkpoint barrier”）流过数据流图。每个算子在 barrier 经过时记录状态；合在一起，这些状态是一个一致快照。Flink 的 exac...
  c15_b03.md: ...。定期地，一个 marker（在 Flink 中叫 “checkpoint barrier”）流过数据流图。每个算子在 barrier 经过时记录状态；合在一起，这些状态是一个一致快照。Flink 的 exactly-once 语义建立于此。  * **...
  c15_b08.md: ...唯一事件 ID 并忽略重复。本质上 sink 可安全重处理。 * **两阶段提交 sink** ：checkpoint barrier 到达时 sink 在 Postgres 事务中 prepare 写；Flink 确认 checkpoint 完成后提交...

=== capacity (3 occurrences) ===
  c15_b03.md: ...`ConsistentRead=true`，对 leader 做同步读。代价：通常是最终读延迟的 2 倍，并消耗更多 capacity unit。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 l...
  c21_b04.md: ...务之间没有熔断器。已添加。  (d) merchant 数据库自动扩缩太慢（5 分钟置备时间）。现在预热的 spare capacity 保持热备。  **行动项**：  (1) 为非常规索引创建添加 linter 规则。(2) 把所有服务重试预算降到 1...
  c21_b04.md: ...0%。(3) 在所有服务与 merchant 数据库之间添加熔断器。(4) 预热 merchant 数据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering...

=== offset (3 occurrences) ===
  c15_b08.md: ...的 exactly-once 依赖三个组件：  (a) **Source replay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset...
  c15_b08.md: ...eplay** ：Kafka 允许从已知 offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state...
  c15_b08.md: ...offset 重放消息。Checkpoint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoin...

=== conflict (3 occurrences) ===
  c16_b01.md: ...万行 OT 实现）。CRDT 出现后，数学处理冲突解决；实现小一个数量级。   ### 16.1 CRDT 是什么  Conflict-free Replicated Data Type 是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合...
  c30_b02.md: ...iro, M., Preguiça, N., Baquero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann,...
  c30_b02.md: .... — CRDT 框架。 * Kleppmann, M., & Beresford, A. R. (2017). "A Conflict-Free Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础...

=== client (3 occurrences) ===
  c16_b03.md: ...hns 约 2014 年启动）使用 RGA 的优化变体。每个字符由一个小结构体表示（约 8 字节），包含其 clock、client ID 和指向前驱的指针。该结构对常见情况高度优化（顺序打字产生字符链，每个指向前一个）。  Yjs 在 Notion、...
  c16_b04.md: ...后续 update。   #### 冲突解决  Yjs 自动处理所有冲突。若两个用户并发在同一位置插入字符，Yjs 按 client ID 和 clock 确定性排序。若一个用户删除段落时另一个将其加粗，粗体属性保留在幸存字符上（未被删除的字符）；被删...
  c30_b14.md: ...│   ├── etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo.py — protected resou...

=== git (3 occurrences) ===
  c16_b03.md: ...eppmann、Adam Wiggins 等）使用类似的 RGA-like 方法，但优化不同。Automerge 注重 git-like 的文档历史；数据结构包含完整变更历史，支持时间旅行和丰富的合并 UI。  Automerge 2.x（202...
  c16_b03.md: ...bundle 更小、高频编辑更快、功能较少（无内置历史 UI）。 * Automerge：更丰富的历史模型、稍重、更 “git-like” 语义。  对 Glide 协作文档编辑器，Yuki 选择 Yjs，因为 bundle size 重要（Gl...
  c16_b09.md: ...少。  _Automerge_ ：  * 优点：完整文档历史是数据结构的一部分；自然的 “谁在何时改了什么” UI；为 git-like 文档工作流设计。 * 缺点：每文档内存占用更大（存储历史）；历史上较慢，但 Automerge 2.x 在...

=== hardenberg (3 occurrences) ===
  c16_b05.md: ...响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (a) **No spinners** ：本地应...
  c30_b02.md: ...EE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software." _Onw...
  c30_b03.md: ...ogle、进入公有云基线的最强信号（§29.1）。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software: You O...

=== mcgranaghan (3 occurrences) ===
  c16_b05.md: ...念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (a) **No spinners** ：本地应用以本地交互速度运行。...
  c30_b02.md: ...rge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software." _Onward!_. * Lloyd, W.,...
  c30_b03.md: ...§29.1）。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Sp...

=== wiggins (3 occurrences) ===
  c16_b05.md: ...re  CRDT 运动影响了一种更广泛的设计理念：”Local-First Software“，由 Kleppmann、Wiggins、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (a) **No...
  c30_b02.md: ...ON Datatype." _IEEE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-F...
  c30_b03.md: ...Time 风格有界时钟正离开 Google、进入公有云基线的最强信号（§29.1）。 * Kleppmann, M., Wiggins, A., van Hardenberg, P., & McGranaghan, M. (2019). "Local-F...

=== objects (3 occurrences) ===
  c16_b11.md: ...历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；awareness 协议处理光标；定期 com...
  c17_b03.md: ...标。 * Glide（第16章）：Yjs（CRDT）做文档正文；Cloudflare Worker + Durable Objects（per-object 线性化）做文档元数据。注意：Cloudflare KV 在这里是错误原语——它全局最终一致，会破...
  c17_b11.md: ...终）+ Snowflake（分析）。Glide 用 Yjs（CRDT/强最终）+ Cloudflare Durable Objects（per-object 线性化）做文档元数据。“每个工作负载的正确系统” 是 2026 年复杂系统的默认架构模式。  第...

=== workers (3 occurrences) ===
  c16_b11.md: ...衍生；Automerge 使用带完整历史的类似结构）。  Yuki 的 Glide 协作编辑器在 Cloudflare Workers + Durable Objects 上使用 Yjs。文档是 Y.Doc；通过 WebSocket 实时同步；aware...
  c28_b02.md: ...ms），结算到 50+ 国家银行，并提供实时欺诈检测。  组件：  (a) **API 网关**：Cloudflare Workers 在边缘。处理 TLS、认证、速率限制。路由到最近的健康的 regional 集群。  (b) **Regional 集...
  c28_b05.md: ...| E.5（GitHub MySQL 双主） | | 19 | 服务发现 | Consul + Cloudflare Workers | E.2（Roblox Consul）、E.7（Fastly 配置 rollout） | | 20 | 追踪 | O...

=== cops (3 occurrences) ===
  c17_b01.md: ...本可独立应用因果有序操作。   #### 实现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带一个依赖向量：写者本次写之前已观...
  c17_b01.md: ...集合。副本在应用某写前会延迟，直到其所有依赖都已应用。  Lloyd 等人在 2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **...
  c30_b02.md: ...ual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013). "Stronger Semantics f...

=== eiger (3 occurrences) ===
  c17_b01.md: ...最终一致对许多其他工作负载又太弱。中间还有什么？  一致性格有许多点；2026 年实用的是 _因果一致_（COPS / Eiger 家族）和 _强最终一致_（CRDT，第16章）。本章把它们放到频谱上，并展示各自何时正确。  本章结束线索 B 的三一...
  c17_b01.md: ...写之前已观察到的写集合。副本在应用某写前会延迟，直到其所有依赖都已应用。  Lloyd 等人在 2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致...
  c30_b02.md: ...emantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== strong (3 occurrences) ===
  c17_b01.md: ...l consistent** ：per-operation；因果相关操作按因果顺序被看到；并发操作可任意顺序。 * **Strong-eventual** ：给定相同操作集合，副本收敛到同一状态；并发操作通过确定性合并解决。 * **Eventual...
  c17_b05.md: ...合并而不保持时间顺序）。大多数生产 CRDT 系统同时提供两者：causal+（保持 happens-before）和 strong-eventual（确定性收敛）。两者结合给出有用保证：“我看到所有先前写（因果），且给定相同更新，我的视图与他人的收敛...
  c17_b11.md: ...nearizable 和 Eventual 之间有许多有用的中间点。Causal consistency 保持因果顺序；strong-eventual（基于 CRDT）提供确定性收敛。两者都比线性化便宜；两者都比纯最终更强。  计数器 / 排行榜 /...

=== types (3 occurrences) ===
  c17_b02.md: ...ge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT bucket types 都是生产示例。  强最终某种意义上 _弱于_ 因果一致（不保证收敛过程中所有副本按因果顺序看到操作），但某种意义上 _...
  c17_b08.md: ...CRDT，sibling values 用于应用控制的合并。  推荐：对购物车用 Riak 的 CRDT bucket types。OR-Set 语义匹配购物车的 “物品并集” 行为。并发添加保留；并发移除被 tombstone；最终购物车是确定性合...
  c30_b02.md: ..., C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M., & Beresford, A. R. (20...

=== martin (3 occurrences) ===
  c18_b01.md: ...这就是 Redlock 模式。代码很短。生产环境中，它偶尔会允许两个领导者同时行动。原因是分布式系统中最深刻的论证之一：Martin Kleppmann 对 Redlock 的分析以及 _fencing token_ 的作用。  本章讲授两个相关原语：...
  c18_b03.md: ...#### Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distrib...
  c18_b03.md: ...#### Martin Kleppmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它...

=== ping (3 occurrences) ===
  c18_b01.md: ...一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向随机选择的其他节点子集发送 PING；PING 携带发送者对集群的视图。接收者基于 PING 更新自己的视图。随时间推移，集群对自身的视图收敛。  goss...
  c18_b01.md: ...ster，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向随机选择的其他节点子集发送 PING；PING 携带发送者对集群的视图。接收者基于 PING 更新自己的视图。随时间推移，集群对自身的视图收敛。  gossip 不是...
  c18_b01.md: ...gossip 维护：每个节点周期性向随机选择的其他节点子集发送 PING；PING 携带发送者对集群的视图。接收者基于 PING 更新自己的视图。随时间推移，集群对自身的视图收敛。  gossip 不是共识；分区下，集群两半可能有不一致视图。Red...

=== aside (3 occurrences) ===
  c18_b01.md: ...il Karlton 的名言：“计算机科学中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-t...
  c18_b01.md: ...：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalida...
  c18_b01.md: ...阅并失效。  选择取决于工作负载：  * 读多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一...

=== driven (3 occurrences) ===
  c18_b01.md: ...*TTL-based**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：...
  c18_b01.md: ...多写少：cache-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度...
  c26_b04.md: ...。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 tier 客户即使在尖峰也...

=== through (3 occurrences) ===
  c18_b01.md: ...e**：应用先查缓存；未命中则从 truth source 读取并回填缓存。写时使缓存失效。  (b) **Write-through**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-...
  c18_b01.md: ...cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...
  c21_b02.md: ...pen --> Open: probe calls fail         Closed: All calls go through;<br/>track success/failure rate         Open: All calls fai...

=== highest (3 occurrences) ===
  c18_b04.md: ...C1->>Bank: commit (token=100)         Bank->>Bank: 100 >= highest (was 0); accept         Bank-->>C1: ok         Note over C1...
  c18_b04.md: ...C2->>Bank: commit (token=101)         Bank->>Bank: 101 >= highest (100); accept         Bank-->>C2: ok         Note over C1:...
  c18_b04.md: ...>Bank: commit (token=100, stale)         Bank->>Bank: 100 < highest (101); reject         Bank-->>C1: STALE_TOKEN   即使 C1 醒来仍以为...

=== heartbeat (3 occurrences) ===
  c19_b01.md: ...控制目标部署；它必须动态发现实例。  发现流程：    1. 每个实例启动时向发现服务注册自己。   2. 实例周期性 heartbeat 以确认健康。   3. 发现服务跟踪健康实例集合。   4. 客户端查询发现服务找到实例。   5. 实例失败（无 h...
  c19_b05.md: ...当前健康可达。  发现服务维护注册表：  (a) 每个服务实例启动时注册自己（地址、端口、元数据）。(b) 实例周期性 heartbeat 确认存活。(c) 其他服务查询注册表找目标服务实例。(d) 实例未 heartbeat（崩溃、分区）时从注册表移除。...
  c19_b05.md: ...、元数据）。(b) 实例周期性 heartbeat 确认存活。(c) 其他服务查询注册表找目标服务实例。(d) 实例未 heartbeat（崩溃、分区）时从注册表移除。  现代发现服务：etcd、Consul、ZooKeeper、云厂商基于 DNS 的服务、...

=== ephemeral (3 occurrences) ===
  c19_b02.md: ...Kafka、Cassandra 等）。ZooKeeper 提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：  (a) **更好的...
  c19_b07.md: ...案：** 迁移计划：  **Phase 1：清点与映射**。识别每个 ZooKeeper 用法：  * 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → et...
  c19_b07.md: ...persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd...

=== watches (3 occurrences) ===
  c19_b02.md: ...Hadoop、Kafka、Cassandra 等）。ZooKeeper 提供类似原语：层级 keys（zNodes）、watches、ephemeral nodes（与会话生命周期绑定）。  etcd 在新系统中已基本替代 ZooKeeper，因为：...
  c19_b07.md: ...语义：  * ZK ephemeral nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。...
  c19_b07.md: ...nodes（绑定会话）→ etcd keys with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。...

=== storms (3 occurrences) ===
  c19_b03.md: ...t rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若很多服务 watch 很多 key，一次配置变更可同时触发数千 watch 事件，压垮发现服务。  (b) **...
  c19_b08.md: ...能出错的地方。  提示  Watch 事件在每个服务触发；服务重新配置；潜在问题：in-flight 请求、watch storms、部分传播。...
  c19_b09.md: ...求**：flag 翻转前开始的请求继续使用旧值；之后开始的请求看到新值。  可能出错的地方：  (a) **Watch storms**：若很多服务 watch 同一 key，etcd 集群会同时发送大量 watch 通知。对 1000 服务舰队，就是...

=== route (3 occurrences) ===
  c19_b04.md: ...* **AWS Cloud Map 做外部 API gateway**：面向客户的 API endpoint 通过 Route 53 + Cloud Map 暴露；全球客户解析到最近的区域负载均衡。  这四个系统分层。每个处理自己的领域；跨领域交...
  c22_b01.md: ...模式。本章讲两者。  愚弄 Olamide 监控的失败，部分源于 LLM 对健康检查的默认建议：       @app.route('/healthz')     def health():         return 'OK', 200   >...
  c26_b11.md: ...正确答案应讨论前缀索引方法、缓存新鲜度跟踪、负载感知，以及若有缓存层如何集成。单行 “hash-and-route” 是原型级答案；生产需要更多。  更深观察：推理集群优化是 2026 专业领域。给出原型级答案的 LLM 遗漏了领域深...

=== auth (3 occurrences) ===
  c20_b01.md: ...work in + TLS<br/>65 ms (edge + cross-equator)"]         A["auth + idempotency<br/>22 ms"]         F["fraud check<br/>80 ms...
  c20_b01.md: ...<br/>22 ms"]         F["fraud check<br/>80 ms (serial after auth)"]         DB["DB transaction<br/>95 ms (cross-region, seri...
  c28_b03.md: ...(Cloudflare Workers)"]             CF[API Gateway<br/>TLS / auth / rate limit]         end         subgraph USE["US-East Reg...

=== specific (3 occurrences) ===
  c20_b03.md: ...传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTelemetry pipeline 细节（Ser...
  c20_b09.md: ...年会成为标准工具。  **Q2.** 一个 LLM 被要求 “给我们的系统加分布式追踪”，并产出了使用 vendor-specific SDK（如 DataDog tracer）的代码。指出 LLM 错在哪，正确答案是什么。  **讨论：** LLM 的...
  c25_b11.md: ...* 对某些工作负载用更小模型（质量可接受处）。 * 用 task-specific fine-tunes（针对特定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-of-Expert...

=== engineering (3 occurrences) ===
  c21_b02.md: ...制。  实现：每个客户端跟踪近期成功率；计算预算；抑制超额重试。  Google 的《Site Reliability Engineering》一书有经典讨论。该模式现在已是生产级库的标准（gRPC、Envoy、Istio）。   ### 21.4 熔断器  另...
  c21_b04.md: ...re capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos engineering game day（第23章）验证修复。  事后复盘模式：识别触发器、放大和防御缺失。每一项都是独立修复。防御缺失（上面...
  c30_b03.md: ...tems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jon...

=== probe (3 occurrences) ===
  c21_b02.md: ...n elapses<br/>(e.g. 30s timer)         HalfOpen --> Closed: probe calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> O...
  c21_b02.md: ...s succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe calls fail         Closed: All calls go through;<br/>track...
  c21_b02.md: ...l fast;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Clo...

=== firstenergy (3 occurrences) ===
  c21_b03.md: ...中最大级联故障之一）。  触发器：2003 年 8 月 14 日 16:10，俄亥俄州三条输电线路因树木长得太近而故障。FirstEnergy 的监控系统有软件 bug，阻止运维看到故障；系统实际上在盲目飞行。  放大：电力绕过故障线路流动时，其他线路过载。每条...
  c21_b03.md: ...(a) **触发器 + 放大 = 崩溃**。许多级联故障触发器很小；放大才是杀手。  (b) **可观测性至关重要**。FirstEnergy 运维看不到初始故障；等他们看到扩散时，已来不及干预。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电...
  c21_b06.md: ...配导致更多线路故障）才是杀手。工程努力应聚焦放大机制，而非阻止每一个可能的触发器。  (b) **可观测性至关重要**。FirstEnergy 运维看不到初始故障（监控软件 bug）；等他们看到扩散时，干预已太晚。分布式系统类比：tracing、metrics、...

=== blackstart (3 occurrences) ===
  c21_b03.md: ...损）。  (d) **系统设计必须遏制故障**。电网为效率互联（跨区域共享发电）；互联成为级联路径。现代电网设计增加 “blackstart” 孤岛，可在级联期间隔离。  分布式系统类比：微服务舰队为功能互联；互联成为级联路径。防御（重试预算、熔断器、load...
  c21_b06.md: ...络分区时会失效。  (d) **系统设计必须遏制故障**。互联电网（为共享发电而高效）成为级联路径。现代电网设计增加 “blackstart” 孤岛。分布式系统类比：cell-based 架构，一个 cell 故障不会级联到其他 cell；以部分效率换取遏制。...
  c21_b08.md: ...的 load shedding。  * **电网互联**：服务 mesh；允许服务跨区域/数据中心相互调用。  * **Blackstart 孤岛**：cell-based 架构；能够隔离部分系统并独立重启。  2003 年序列用分布式系统术语：  (a) *...

=== linter (3 occurrences) ===
  c21_b04.md: ...CONCURRENTLY`（Postgres），它不会锁表。迁移工具使用了会锁表的 `CREATE INDEX`。一条 linter 规则本可捕获。  (b) 支付服务的重试预算设得太高（允许 50% 重试开销）。降到 10%。  (c) mercha...
  c21_b04.md: ...钟置备时间）。现在预热的 spare capacity 保持热备。  **行动项**：  (1) 为非常规索引创建添加 linter 规则。(2) 把所有服务重试预算降到 10%。(3) 在所有服务与 merchant 数据库之间添加熔断器。(4) 预...
  c21_b10.md: ...Crescent 事后复盘）展示微服务领域的模式：schema 变更触发器、重试风暴放大、缺失熔断器、自动扩缩慢。修复：linter 规则、降低重试预算、加熔断器、预热容量、更新 runbook。  第22章进入更难的事故类别：亚稳态故障与灰色故障。B...

=== ram (3 occurrences) ===
  c22_b01.md: ...误。  二十分钟后她找到问题：一个节点的磁盘静默降级。原本 1 ms 的读取现在要花 100 ms。健康检查是廉价的仅 RAM endpoint——从不触碰磁盘，所以它在 1 毫秒内返回 200。负载均衡器信任 200，继续把流量路由到慢节点，客...
  c22_b01.md: ...窗口。  示例：  (a) **慢节点**。节点磁盘降级；原本 1 ms 的读取现在 100 ms。健康检查是廉价的仅 RAM endpoint；快速返回 200。应用请求触及磁盘时变慢；用户体验挂起。负载均衡器继续路由流量到慢节点。  (b)...
  c22_b05.md: ...灰色故障窗口。  示例：  (a) **廉价健康检查的慢节点**：节点磁盘降级；读取比正常慢 100 倍。健康检查是仅 RAM endpoint；快速返回 200。负载均衡器继续路由流量；用户体验慢请求。  (b) **双峰延迟的内存压力**：节...

=== roblox (3 occurrences) ===
  c22_b04.md: ...查，但每个请求都被延迟尖刺。  恢复：通过 trace 识别 rollout；回滚；集群在数小时内恢复。   #### Roblox 2021 多天中断  灰色故障与亚稳态级联的结合。触发器是一次导致 CPU 过度使用的部署；级联是重试风暴和连接池耗尽...
  c22_b11.md: ...我维持。两者可共存；都需要各自防御。  真实案例：AWS S3 2017、Google Magic Pony（非正式）、Roblox 2021。都特征为小触发器、反馈循环、自维持的损坏状态。恢复耗时数小时到数天。  防御：有界反馈循环（上限、预算、限制...
  c30_b08.md: ...#### E.2 — Roblox 多日故障，2021-10-28 至 2021-11-02  * **触发**：部署新 Consul 版本，带有微妙行为...

=== balancer (3 occurrences) ===
  c22_b08.md: ...实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已...
  c24_b04.md: ...（如 Tobias 在 Continuum）如下：                        ┌──── Load Balancer ────┐                      │                       │...
  c25_b02.md: ...### 25.2 分离式架构  拓扑可视化：       graph TB         LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(c...

=== outlier (3 occurrences) ===
  c22_b08.md: ...动探测**：除被动观察外，负载均衡器可向每个后端发合成请求，检测被动观察可能遗漏的灰色故障。  实现：Envoy 的 “outlier detection” 实现了大部分；AWS Application Load Balancer 的 “least-ou...
  c22_b09.md: ...对 Crescent：区域负载均衡器上使用带 outlier detection 的 Envoy。表现出集群中位数 5 倍延迟的后端被 “弹出” 活动池；通过探测定期测试；恢复后重...
  c26_b09.md: ...d) **尾部感知负载均衡器**：监控 per-node 延迟；若某节点异常高（灰色故障，第22章），绕行。Envoy outlier detection 或类似。  (e) **限制每请求最大输出长度**：限制 max_tokens；拒绝极端限制请求或...

=== startup (3 occurrences) ===
  c22_b10.md: ...* 检查内部状态（缓存已填充、队列未卡住、无关键资源耗尽）。 * 仅当服务所需一切正常时才返回 200。  (c) **Startup 检查**（`/startupz`）：服务是否仍在初始化？用于生命最初 30-60 秒；区分 “启动中” 与 “坏了”。...
  c22_b11.md: ...么？它有哪些依赖？健康检查的成本预算如何？有这上下文，我才能设计适合你服务的 liveness、readiness 和 startup 检查。” 单行 “return 200” 只适用于最简单的无状态服务。  更深观察：健康检查是系统可靠性故事的一部分。...
  c25_b10.md: ...型、KV cache 传输、更复杂编排）。对小部署（< 20 节点），运维开销超过效率收益。对没有推理专用平台工程师的 startup 团队，co-located 运维更简单。强制分离会惩罚小而简单的场景。  综合：分离是大型生产部署（50+ 节点、每月...

=== toxiproxy (3 occurrences) ===
  c23_b02.md: ...imulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e) **Toxiproxy**：TCP 层故障注入。慢连接、丢包、模拟下游变慢。与应用无关。  选择取决于部署：Kubernetes 原生部署用...
  c23_b02.md: ...署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...
  c23_b10.md: ...障” 切片）。完整混沌工程学科测试其他切片。进阶：Chaos Monkey 用于基本实例故障韧性；Chaos Mesh、Toxiproxy 等专业混沌工具用于其他故障模式；game day 用于协调场景。  运行 Chaos Monkey 两年没有重大中断的...

=== radix (3 occurrences) ===
  c24_b03.md: ...thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新请求到达时，系统找到最长缓存前缀并复用；只有后缀需要重新计算。  收益：  (a...
  c24_b03.md: ...gex 约束）。结构是请求规范的一部分；推理引擎知道如何处理。  (c) **函数调用**：原生支持 tool use；radix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、结构化输出应用、ag...
  c26_b10.md: ...相同 prompt。真实工作负载的 prompt 共享 _前缀_（system prompt + 不同用户消息）。需要 radix tree 或类似前缀索引。  (b) **缓存新鲜度跟踪**。KV cache 会过期（内存压力、TTL）。路由器必须...

=== sso (3 occurrences) ===
  c24_b04.md: ...（H100 / H200 / B100 / B200 调优）。每代 GPU 都有调优容器。  (d) **企业特性**：SSO、RBAC、审计日志、监控集成。面向企业采购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通...
  c24_b08.md: ...**答案：**  _vLLM_：  * 开源；团队完全控制部署。 * 最佳原始推理性能（引擎本身）。 * 没有原生 SSO、RBAC、审计日志；团队自建或集成第三方。 * SOC 2 合规：可行，但需团队处理审计跟踪、访问控制等。  _NVI...
  c24_b08.md: ...合规：可行，但需团队处理审计跟踪、访问控制等。  _NVIDIA NIM_：  * 商业企业产品。 * 包含开箱即用的 SSO、RBAC、审计日志、监控集成。 * SOC 2 合规：平台提供构建块；团队的特定合规更快实现。 * 推理引擎控制较少：...

=== llama (3 occurrences) ===
  c24_b05.md: ...按 3 USD/小时：每小时 1200 USD，每月 86.4 万美元。加上网络、存储和运维。  每个请求成本：  * Llama 3 70B 上典型 1000-token 输出请求：约 5-15 GPU-秒。 * 按 3 USD/小时，即每个请求...
  c24_b10.md: ...际进展。把它当作子学科（有自己的会议、库、最佳实践）才是正确框架。  **Q2.** 一个 LLM 被要求 “为我们的 Llama 3 70B 搭建推理集群”。它产出使用基础 Hugging Face TextGenerationInference（...
  c24_b10.md: ...**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。T...

=== routing (3 occurrences) ===
  c24_b08.md: ...lative decoding（第26章）；模型量化（8-bit、4-bit serving 用于低精度模型）；MoE routing（mixture-of-experts 模型高效路由）。每种都复合。  更深观察：推理成本优化是 2026 年的专业领域...
  c26_b02.md: ...工作）。 * 集群吞吐更高（减少冗余计算）。  真实案例：  * **Anyscale 的 “Prefix-aware routing”**：其推理平台的生产特性。对重复 prompt 工作负载，客户看到约 30% 吞吐提升。 * **Together...
  c29_b02.md: ...Mixture-of-Experts）** 模型成为 >100B 参数模型的默认，需要 expert-parallel routing 作为新原语（本书未深入覆盖；预计 Series IV Vol 4 会讲）。 * **基于扩散的语言模型**（2024-...

=== parallelism (3 occurrences) ===
  c24_b10.md: ...nuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parall...
  c24_b10.md: ...lelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（tensor parallelism）。TGI 支持；LLM 应配置。  (d) **生产可观测性**：GPU 利用率指标、KV cache 利用率、请求队...
  c24_b10.md: ...故障、节点故障、模型加载故障。部署必须优雅处理每种。  正确答案：“基础部署用 TGI 或 vLLM 加 tensor parallelism。2026 生产还要包含 [以上列表]。以下是更完整配置……”  更深观察：“搭建推理集群” 答案空间很广。最小答案（基...

=== incoming (3 occurrences) ===
  c25_b02.md: ...──────┘   Prefill 节点：  * 针对计算优化（TFLOPs 最高的最新 GPU）。 * 并行处理大量 incoming prompt。 * 输出：每个 prompt 的 KV cache。 * 计算完 prefill 后，将 KV cac...
  c26_b02.md: ...，路由过去。  实现：  (a) 每个节点向负载均衡器报告其缓存前缀索引（周期性地，或通过订阅缓存层）。  (b) 对 incoming request 的 prompt 做哈希（或其前缀与索引比对）。  (c) 负载均衡器选择最长匹配前缀的节点；把请求路...
  c26_b02.md: ...数节点可防止其他节点的尾部延迟放大。  实现：  (a) 一个轻量级长度预测器（可本身是小模型，或基于规则的启发式）把 incoming 请求分类为短/中/长。  (b) 负载均衡器相应路由：短请求去 “burst” 节点；长请求去 “sustained”...

=== moonshot (3 occurrences) ===
  c25_b03.md: ...十 GB。没有单张 GPU 内存足够；KV cache 必须跨多张 GPU（可能跨多个节点）分片。  Mooncake，Moonshot AI 在 2024 年论文中描述的推理系统，是 2026 年的典型实现。KV cache 以块形式（PagedAtte...
  c25_b05.md: ...AI，2024 论文）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) **LMCache**（UC Berkeley，20...
  c30_b03.md: ...ache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (https://github.com/LMCache/LMCa...

=== gbit (3 occurrences) ===
  c25_b04.md: ...ooncake 风格缓存层；高带宽存储，RDMA 连接 serving）。 * 高带宽互联：每个 rack 内 800 Gbit/s InfiniBand，rack 间 400 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。...
  c25_b04.md: ...erving）。 * 高带宽互联：每个 rack 内 800 Gbit/s InfiniBand，rack 间 400 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求分配到 prefill...
  c25_b06.md: ...算优化 GPU 上约 100 ms。  (c) **KV cache 传输**：5 GB KV cache 经 400 Gbit/s RDMA 约 50 ms。  (d) **首 token 生成**：decode 节点上约 30 ms（一个 de...

=== rebalance (3 occurrences) ===
  c25_b06.md: ...。  (c) **更复杂的负载均衡**：请求必须基于可用性分配到 (prefill, decode) 对；分配可能需要 rebalance。  (d) **可观测性复杂度**：trace 必须跨阶段跟踪请求；指标必须区分 prefill 和 decode 工...
  c25_b08.md: ...如何帮助，运维关注点是什么？  提示  分离允许独立扩缩 prefill 和 decode；可变性成为机会。运维关注点：rebalance。  **答案：** 有了分离，prefill 和 decode 容量可独立扩缩。集群可匹配当前工作负载 profile...
  c25_b08.md: ...，GPU 同时做两个阶段；扩缩是一维的。集群对当前工作负载要么计算过多，要么内存带宽过多，无法同时匹配。  运维关注点：rebalance。  集群不能瞬间扩缩（云 GPU 置备需 5-15 分钟，本地更久）。若工作负载在几分钟内从长 prompt 变为短...

=== locality (3 occurrences) ===
  c26_b01.md: ...（round-robin、least-loaded）错过 LLM 推理特有的关键机会：  (a) **KV cache locality**：若请求 prompt 的前缀已在某节点缓存，路由到该节点可节省缓存部分的 prefill 成本。路由别处则浪费缓存...
  c26_b05.md: ...请求通常 10-50 ms。错误路由决策的成本相应大得多。  具体 LLM 路由机会：  (a) **KV cache locality**：若某节点已缓存 prompt 前缀，路由到该节点节省 prefill 成本。路由别处浪费缓存。每次请求可节省数百毫...
  c26_b11.md: ...现上的投资通过效率收益回报。   ### 本章总结  LLM 请求路由与 Web 请求路由有结构差异：KV cache locality、长度相关放置、模型可用性、更宽的尾部延迟。LLM 专用路由优化（KV-cache-aware、长度预测）带来显著效率收...

=== sustained (3 occurrences) ===
  c26_b02.md: ...把 incoming 请求分类为短/中/长。  (b) 负载均衡器相应路由：短请求去 “burst” 节点；长请求去 “sustained” 节点。  (c) 误预测：若预测错误（本应短却长，或相反），请求 mid-flight 迁移到更合适节点（KV ca...
  c26_b09.md: ...(b) **长度预测路由**：长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。Burst 节点尾部延迟显著改善。  (c) **Speculative de...
  c26_b11.md: ...old、function-calling 模板）的重大收益。  长度预测路由：预测请求输出长度；长输出请求路由到专用 “sustained” 节点；短请求去 “burst” 节点。防止长请求阻塞短请求导致尾部延迟放大。  Speculative decodi...

=== trip (3 occurrences) ===
  c26_b03.md: ...verify 容量。  成本：  (a) 每 token 网络延迟：draft 与 verify 节点间 round-trip 0.5-2 ms。增加 per-token 延迟。  (b) 网络带宽：不大（每请求每步几个 token），但真实存在...
  c26_b06.md: ...复杂度；大规模才划算。  (d) **Draft 与 verify 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 GPU +...
  c26_b06.md: ...fy 间网络延迟有界**。Per-token round-trip 0.5-2 ms 可容忍；50+ ms round-trip 会抵消加速。  具体例子：100 张大模型 GPU + 20 张小模型 GPU 的集群。小模型 GPU 为所有请求做...

=== robin (3 occurrences) ===
  c26_b04.md: ...ling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit...
  c26_b05.md: ...推理平台（Anyscale、Together、Fireworks）已构建显式考虑这些因素的路由优化器。朴素 round-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-...
  c26_b06.md: ...ken 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等下一轮。  (c) **...

=== credit (3 occurrences) ===
  c26_b04.md: ...更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：w...
  c26_b04.md: ...eficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair...
  c26_b04.md: ...-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加...

=== customer (3 occurrences) ===
  c26_b04.md: ...求。  朴素 FIFO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority...
  c26_b04.md: ...户中处理。提供有界延迟保证。  Continuum 方法：weighted fair scheduling 加 per-customer SLA-driven priority。高 tier 客户权重更高；其 p99 延迟 SLA 驱动调度器偏好。低 ti...
  c26_b10.md: ...ast-loaded、round-robin 等）。  (g) **指标和可观测性**：缓存命中率、每请求节省、per-customer 缓存效率。...

=== loaded (3 occurrences) ===
  c26_b05.md: ...Together、Fireworks）已构建显式考虑这些因素的路由优化器。朴素 round-robin 或 least-loaded 路由遗留显著性能。  对 Continuum 集群：从朴素路由切换到 KV-cache-aware 加长度预测路由，吞...
  c26_b10.md: ...一个 LLM 被要求 “为我们集群实现 KV-cache-aware 路由”。它产出一个带哈希检查的基本 least-loaded 均衡器。指出遗漏什么。  **讨论：** LLM 的基本方法：哈希 prompt，检查是否有节点缓存该哈希，若有则路由...
  c26_b10.md: ...以最大化缓存复用。  (f) **冷启动 fallback**：若没有节点有缓存，路由器回退到默认负载均衡（least-loaded、round-robin 等）。  (g) **指标和可观测性**：缓存命中率、每请求节省、per-customer 缓...

=== notify (3 occurrences) ===
  c27_b04.md: ...er)         cache.update(user)         notification_service.notify(user.id, 'updated')   谎言：三个调用一起成功。代码只有 happy path；部分故障是隐含的。...
  c27_b04.md: ...成功但 `cache.update()` 之前，缓存已过期。两者都成功但 `notification_service.notify()` 之前，通知未发出。系统处于不一致状态。  修复：识别哪些调用对一致性关键，并用 saga 模式（第13章）或 t...
  c27_b11.md: ...ent.user_id)             )             notification_service.notify(payment.user_id, "Payment processed")             ledger_se...

=== apiw (3 occurrences) ===
  c28_b03.md: ...end         subgraph EUW["EU-West Region"]             APIW[API Service]             REDISW[Redis Cluster]...
  c28_b03.md: ...d detection]         end         CF --> APIE         CF --> APIW         CF --> APIW2         APIE --> REDISE         APIE -...
  c28_b03.md: ...C nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB         style...

=== snow (3 occurrences) ===
  c28_b03.md: ...CONSUL[Consul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/...
  c28_b03.md: ...br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.discover...
  c28_b03.md: ...DBE -.->|"CDC nightly"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery...

=== stratuse (3 occurrences) ===
  c28_b03.md: ...s Cluster]             CRDBE[CockroachDB nodes]             STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock ser...
  c28_b03.md: ...APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE...
  c28_b03.md: ...covery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#fff4e6         style USE fi...

=== shared (3 occurrences) ===
  c28_b03.md: ...CRDBW2[CockroachDB nodes]         end         subgraph Shared["Shared Services"]             TB[TigerBeetle<br/>5-replica...
  c28_b03.md: ...BW2[CockroachDB nodes]         end         subgraph Shared["Shared Services"]             TB[TigerBeetle<br/>5-replica VSR<br/...
  c28_b03.md: ...W fill:#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...

=== database (3 occurrences) ===
  c30_b01.md: ...di, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IEEE Comput...
  c30_b01.md: ...C., et al. (2012). "Spanner: Google's Globally-Distributed Database." _OSDI_. — TrueTime 描述。   #### 广播与复制（第二部分 — 第4-6章）  * Cach...
  c30_b02.md: ...12). "Calvin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_.   #### 最终一致性与 CRDT（第五部分 — 第15-17章）  * C...

=== practical (3 occurrences) ===
  c30_b02.md: ..._ACM TOPLAS_. — BFT 基础。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malk...
  c30_b02.md: ...lman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevic, Z. (2018). "...
  c30_b03.md: ...7-2030 主流采用预测重读（§29.1）。 * Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控...

=== windows (3 occurrences) ===
  c30_b08.md: ...触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏。 * **放大**：更新同时推送到数百万台 Windows 机器（无 canary）；...
  c30_b08.md: ...lformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏。 * **放大**：更新同时推送到数百万台 Windows 机器（无 canary）；受影响机器进入蓝屏循环，需要手动恢复。 * **时间线**：850 万台 Windows 机...
  c30_b08.md: ...台 Windows 机器（无 canary）；受影响机器进入蓝屏循环，需要手动恢复。 * **时间线**：850 万台 Windows 机器同时变砖；多日恢复；航空、医院、金融服务严重受影响。 * **恢复**：每台机器手动恢复（启动到安全模式，删除坏...

=== examples (3 occurrences) ===
  c30_b14.md: ...#### 预期仓库结构               distributed-systems-2026-examples/     ├── ch03_clocks/     │   ├── lamport.py        — Lampo...
  c30_b14.md: ...py — automated checks for the six-lies patterns         └── examples/             ├── bad_distributed_code.py  — code with all s...
  c30_b14.md: ...tributed_code.py  — code with all six lies (the §27.2-§27.7 examples)             └── good_distributed_code.py — same workflow w...

=== minimal (3 occurrences) ===
  c30_b14.md: ...scenarios     ├── ch12_lsm/     │   └── tiny_lsm.py       — minimal LSM-tree (memtable + WAL + SSTable + compaction)     ├── ch...
  c30_b14.md: ...— Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like list CRDT     ├── ch18_locks/     │   ├── redlock_...
  c30_b14.md: ...── ch24_inference/     │   └── continuous_batching_toy.py — minimal continuous-batching scheduler     └── ch27_review/...

=== reddy (2 occurrences) ===
  c01_b03.md: ...器。Tobias Schwartz 运行它处理 Continuum Inference 的 LLM 推理集群。Anya Reddy 运行它处理 Stratus DB 的数据库引擎。系统不同，心智动作相同。...
  c09_b01.md: ...Anya Reddy 两个月前加入 Stratus DB。她的第一个项目：为复制层选个共识库。她读了 Paxos 论文。她读了 Megast...

=== watanabe (2 occurrences) ===
  c01_b03.md: ...本书的四位主角会在不同系统、不同领域反复执行这个循环。Olamide 运行它处理你刚刚遇到的多区域支付平台。Yuki Watanabe 运行它处理 Glide 的协作文档编辑器。Tobias Schwartz 运行它处理 Continuum Infere...
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide...

=== intensive (2 occurrences) ===
  c01_b04.md: ...Kubernetes 书，也不是区块链书。基础材料的参考读物是 Kleppmann 的 _Designing Data-Intensive Applications_ ；如果你还没读过，可以先读这本，或并行阅读。  本书是：2026 年给在职分布式系统工程师...
  c30_b01.md: ...# 基础（第一部分 — 第1-3章）  * Kleppmann, M. (2017). _Designing Data-Intensive Applications_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanenbaum, A. S...

=== linux (2 occurrences) ===
  c01_b05.md: ...the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告。 * **单系统管理** ：Linux 内核调优、容器运行时内部、虚拟机监控器选择。这些是分布式系统运行的底层基板，但基板细节超出范围。参考：Bovet &...
  c01_b05.md: ...分布式系统运行的底层基板，但基板细节超出范围。参考：Bovet & Cesati _Understanding the Linux Kernel_（第3版）；Love _Linux System Programming_ ；Brendan Gregg...

=== openapi (2 occurrences) ===
  c01_b05.md: ...ftware at Scale_ Series IV Vol 1 _APIs_ 中覆盖）、gRPC schema 演进、OpenAPI 工具链。第19章会提到服务发现；协议本身超出范围。参考：Newman _Building Microservices_...
  c01_b05.md: ...（第2版，2021）；Richardson & Ruby _RESTful Web Services_ ；gRPC 与 OpenAPI 官方规范。  _保持范围_ 的纪律本身就是一项分布式系统技能。一本试图面面俱到的书在生产中无益；一本聚焦在职工程师必须...

=== rest (2 occurrences) ===
  c01_b05.md: ...dan Gregg 的 _Systems Performance_（第2版，2020）。 * **应用层协议深度** ：REST API 设计（在 _Software at Scale_ Series IV Vol 1 _APIs_ 中覆盖）、gR...
  c14_b02.md: ...kroachDB  Cockroach Labs 的开源 NewSQL。有时被描述为 “Spanner for the rest of us”，因为它提供类似保证而无 GCP 锁定。  关键属性：  * 每 range 使用 Raft 复制。 *...

=== ahead (2 occurrences) ===
  c01_b05.md: ...ner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告。 * **单系统管理** ：Linux 内核调优、容器运...
  c12_b03.md: ...结构：  (a) **Memtable** ：内存有序结构（跳表、红黑树）。新写先到这里。  (b) **Write-ahead log（WAL）** ：每次写也先追加到磁盘上的持久日志，再进入 memtable。崩溃恢复时重放 WAL。  (c)...

=== internet (2 occurrences) ===
  c01_b05.md: ...不是生产现实；本书读者在未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告...
  c30_b03.md: ..., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K...

=== nonsense (2 occurrences) ===
  c01_b10.md: ...跃 30 秒。从外部看，该节点好像时间旅行到了未来。在节点内部，任何跨暂停前后两次时钟读取来推理持续时间的代码都会得到 nonsense。  **Q4.** 用你自己的话定义部分失败（partial failure），并与单机的失败概念对比。各举一例。...
  c03_b04.md: ...秒”，它表示“我已观察到三个进程各发生 2 个事件”。把向量时钟比较与 wall-clock 比较混在同一逻辑中会产生 nonsense。...

=== microsystems (2 occurrences) ===
  c02_b01.md: ...的无分区情况也纳入进来。   ### 2.1 Deutsch 的分布式计算八大谬误  20 世纪 90 年代初，Sun Microsystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 James Gosling 补充了...
  c30_b01.md: ...(1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (2002). "Brewer's...

=== sun (2 occurrences) ===
  c02_b01.md: ...P 忽略的无分区情况也纳入进来。   ### 2.1 Deutsch 的分布式计算八大谬误  20 世纪 90 年代初，Sun Microsystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 Jame...
  c30_b01.md: ...P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (200...

=== opt (2 occurrences) ===
  c02_b02.md: ...，而是因为不存在足够的带宽把它送到需要去的地方。   #### 网络是安全的  默认情况下每个数据包都是裸露的。加密是 opt-in。TLS 加密两个端点之间的通信；它并不验证端点，除非证书校验正确，而且它也无法抵御某个已被攻陷的已认证端点。你的...
  c25_b08.md: ...llback 计划：指标回退则回到 co-located。 * 客户沟通：通知大客户架构变化；为需要稳定延迟的客户提供 opt-out。 * 工程投入：2 名高级工程师全职做分离层 6 个月。  总投入：约 50 万美元工程时间换取约 400-5...

=== brewer (2 occurrences) ===
  c02_b03.md: ...条。大多数生产中的分布式系统 bug 就是八条之一，只是略有伪装。   ### 2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 20...
  c30_b01.md: ...osystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (2002). "Brewer's conjecture and the feasibility of consistent, available,...

=== gilbert (2 occurrences) ===
  c02_b03.md: ...2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 2002 年证明了它。口号的 CAP 说“在一致性、可用性、分区容错性里选两个”。这种...
  c30_b01.md: ...f Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (2002). "Brewer's conjecture and the feasi...

=== nancy (2 occurrences) ===
  c02_b03.md: ...CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 2002 年证明了它。口号的 CAP 说“在一致性、可用性、分区容错性里选两个”。这种表述有两处误导。...
  c07_b01.md: ...来不难，但 “到底有多难” 的精确陈述是分布式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即使只可能发生一次崩溃故障的完...

=== abadi (2 occurrences) ===
  c02_b04.md: ...许多标为“CP”的系统以标签未命名的小巧方式交易可用性。   ### 2.3 PACELC：诚实的扩展  Daniel Abadi 在 2010 年左右指出，CAP 只命名了分区情况，忽略了更常见的情况：网络健康时。健康情况下的权衡在延迟与一致性之间...
  c30_b01.md: ...-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Da...

=== put (2 occurrences) ===
  c02_b10.md: ...onExpression` 的条件写入在分区层是线性化的。成本：每次写入延迟包含一次同步法定人数检查，高于默认最终一致 put。  (b) 通过 `ConsistentRead=true` 的强一致读是线性化的。成本：比最终一致读延迟更高（Dyn...
  c02_b11.md: ...可见的不一致成本（展示浏览量晚几秒）远低于线性化计数（每次增量必须协调）的系统成本。基于 CRDT 的计数器或最终一致 put-with-merge 可在几乎无协调的情况下服务该工作负载。  _排行榜_：因果一致，因为如果玩家 B 提交前看到了玩...

=== footprint (2 occurrences) ===
  c02_b13.md: ...* 模型没有问的问题包括：  * 商店执行什么操作？库存提交和支付提交需要线性化；商品浏览和评论阅读不需要。 * 部署 footprint 是什么？单区域 versus 多区域会剧烈改变延迟画像。 * 团队的运维成熟度如何？一个没有分布式数据库经验的团队从托...
  c03_b08.md: ...DB | 由 sequencer 确定性排序 | 到 sequencer 的延迟；不涉及时钟  正确选择取决于你的运营 footprint。如果你数据中心有 GPS（或使用有云 TrueTime 等价物的云厂商），TrueTime 风格最干净。如果没有，HL...

=== leslie (2 occurrences) ===
  c03_b02.md: ...### 3.2 Happens-Before  这里的经典论文是 Leslie Lamport 的 "Time, Clocks, and the Ordering of Events in a Di...
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The...

=== carries (2 occurrences) ===
  c03_b03.md: ...er |---|---|---|--- 1 | P_1 | a | [1,0,0] 2 | P_1 | send m, carries [2,0,0] | [2,0,0] 3 | P_3 | x | [0,0,1] 4 | P_2 | recv m |...
  c03_b03.md: ...| x | [0,0,1] 4 | P_2 | recv m | [2,1,0] 5 | P_2 | send n, carries [2,2,0] | [2,2,0] 6 | P_3 | recv n | [2,2,2]  现在我们能检测并发。V(a...

=== receive (2 occurrences) ===
  c03_b03.md: ...o P_2 (carries 2) | 2 3 | P_3 | local event x | 1 4 | P_2 | receive m | \max(0, 2) + 1 = 3 5 | P_2 | send msg n to P_3 (carries...
  c03_b03.md: ...1 = 3 5 | P_2 | send msg n to P_3 (carries 4) | 4 6 | P_3 | receive n | \max(1, 4) + 1 = 5  事件 a 的 Lamport 时间戳为 1；事件 x 的时间戳为 1。...

=== epsilon (2 occurrences) ===
  c03_b06.md: ...* **贴近 wall-clock 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \epsilon 是进程间最大时钟偏移。 * **有界计数器** ：c_p 在部分同步假设下被常数限制...
  c03_b06.md: ...k 时间** ：l_p 被 \text{wall\_clock\_now}() + \epsilon 上界限制，其中 \epsilon 是进程间最大时钟偏移。 * **有界计数器** ：c_p 在部分同步假设下被常数限制；实践中 16 位足够。   ##...

=== earliest (2 occurrences) ===
  c03_b07.md: ...I（2012 年 Spanner 论文描述）将时间暴露为一个 _区间_ ：每次调用 `TT.now()` 返回一对 `[earliest, latest]`，真实 wall-clock 时间保证落在该区间内。  Spanner 在每个数据中心部署 GPS...
  c03_b07.md: ..._等待掉_ 不确定性，而不是忽略它。NTP 给你“大概 10 ms 内”；TrueTime 给你“真实时间可证明在 `[earliest, latest]` 内”。差别就是偶尔出现线性化违反的系统与永不出现的系统之间的差别。...

=== dvv (2 occurrences) ===
  c03_b11.md: ...的操作会失效。  第三条路，dotted version vectors（DVV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃），Riak 使用过。对活跃消息系统，DVV 没有直接帮助，因为每个活跃服务都会贡献。...
  c03_b11.md: ...VV），适合存储数据而非每条消息的逻辑时钟。DVV 压缩稀疏向量（大多数条目不活跃），Riak 使用过。对活跃消息系统，DVV 没有直接帮助，因为每个活跃服务都会贡献。...

=== delete (2 occurrences) ===
  c03_b11.md: ...，它被盖戳早于另一个时钟超前的协调者上的后续写入。当删除和写入到达副本时，副本看到 write-timestamp > delete-timestamp，于是保留写入，实际上复活了记录。  这就是经典的 Cassandra “复活删除”失败模式，在 J...
  c15_b07.md: ...Cassandra 的 “tombstone resurrection” bug（第5章）是经典示例：由于时钟偏移，delete 的时间戳早于后续写；后续写 “胜出” 并复活了已删记录。  抵御 LWW 失败模式：  * 严格 NTP 纪律；漂移超...

=== aggressively (2 occurrences) ===
  c03_b12.md: ...ime 等价物，Spanner 风格运维更干净。如果不能，CockroachDB 风格 HLC 加偏移上界是实际默认； aggressively 调优 NTP 和偏移上界。  **Q4.** 用伪代码实现 Lamport 时间戳，系统允许每个进程产生本地事件、发送...
  c14_b04.md: ...算。   #### 模式  三者（Trino、Snowflake、BigQuery）遵循同一架构模式：存储与计算分离； aggressively 并行化；分析扫描使用列式布局；暴露 SQL。  运维类别与 NewSQL 不同：分布式查询处理器面向 _分析_（读重、...

=== restart (2 occurrences) ===
  c03_b12.md: ...k 时间中是否发生在事务开始之前，于是 _重启_ 事务，用新时间戳推到观察到的写入之后。这就是“uncertainty restart”路径。  实际含义：  * Spanner 的成本是每个事务恒定的。CockroachDB 的成本只在与其他节点近期写...
  c13_b05.md: ...模糊。为保持可串行化，事务 _重启_，取新的开始时间戳，并严格推到 $t_w$ 之后。这就是 “uncertainty restart”。  代价按重启支付，而非按提交支付。低争用工作负载很少触发重启；高争用最近写入键的工作负载会看到频繁重启，并通过重试...

=== bimodal (2 occurrences) ===
  c04_b02.md: ...消息 $O(N^2)$ 条消息。存在基于 gossip 的变体，平均以 $O(N \log N)$ 条消息达到概率可靠；Bimodal Multicast（Birman 等）是经典例子。...
  c30_b01.md: ...l of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K., et al. (1999). "Bimodal multicast." _ACM TOCS_. — 基于 gossip 的可靠广播。...

=== multicast (2 occurrences) ===
  c04_b02.md: ...2)$ 条消息。存在基于 gossip 的变体，平均以 $O(N \log N)$ 条消息达到概率可靠；Bimodal Multicast（Birman 等）是经典例子。...
  c30_b01.md: ...ACM_. — 原子广播 ↔ 共识等价。 * Birman, K., et al. (1999). "Bimodal multicast." _ACM TOCS_. — 基于 gossip 的可靠广播。...

=== creation (2 occurrences) ===
  c04_b02.md: ...乎总是错的。  性质：  * **Validity** ：若发送者正确且接收者正确，接收者最终交付消息。 * **No-creation** ：未被发送的消息不会被交付。 * **No-duplication** ：每条消息至多交付一次。  缺失什么：关于...
  c04_b07.md: .... **Best-effort broadcast** ：validity（发送者正确、接收者正确 → 交付）和 no-creation。比 for 循环点对点发送没新增什么。   2. **Reliable broadcast** ：新增 _agreem...

=== layer (2 occurrences) ===
  c04_b05.md: ...一层？  正确层是满足应用正确性要求的最弱层，因为每一层更强层都更贵。  Application | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TT...
  c30_b06.md: ...速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2026 年 AI 生成分布式代码生产纪律。第1层 = 自...

=== none (2 occurrences) ===
  c04_b03.md: ...rom_buffer():         while True:             deliverable = None             for (msg, V_msg, sender) in buffer:...
  c04_b03.md: ...er)                     break             if deliverable is None:                 return             # 交付             buffer...

=== ascii (2 occurrences) ===
  c04_b05.md: ...style A fill:#f9f9f9         style E fill:#fff4e6  等效 ASCII 回退（用于纯文本环境）：       Best-effort broadcast       (TCP plus a...
  c25_b02.md: ...heTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII 回退：                           ┌──── Request ───┐...

=== cachin (2 occurrences) ===
  c04_b05.md: ...ic broadcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secur...
  c30_b01.md: ...base." _OSDI_. — TrueTime 描述。   #### 广播与复制（第二部分 — 第4-6章）  * Cachin, C., Guerraoui, R., & Rodrigues, L. (2011). _Introduction t...

=== guerraoui (2 occurrences) ===
  c04_b05.md: ...dcast == Consensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distribu...
  c30_b01.md: ..._. — TrueTime 描述。   #### 广播与复制（第二部分 — 第4-6章）  * Cachin, C., Guerraoui, R., & Rodrigues, L. (2011). _Introduction to Reliable and...

=== required (2 occurrences) ===
  c04_b05.md: ...8 你的应用需要哪一层？  正确层是满足应用正确性要求的最弱层，因为每一层更强层都更贵。  Application | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受...
  c05_b02.md: ...ce of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-af...

=== rodrigues (2 occurrences) ===
  c04_b05.md: ...sensus   每一层新增一条性质。每一层都基于下一层实现。这里的经典参考书是 Cachin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distributed Programm...
  c30_b01.md: ...#### 广播与复制（第二部分 — 第4-6章）  * Cachin, C., Guerraoui, R., & Rodrigues, L. (2011). _Introduction to Reliable and Secure Distribute...

=== secure (2 occurrences) ===
  c04_b05.md: ...achin、Guerraoui 和 Rodrigues 的 _Introduction to Reliable and Secure Distributed Programming_（第2版，2011），该书逐层展开并给出完整伪代码与正确性证明。...
  c30_b01.md: ..., R., & Rodrigues, L. (2011). _Introduction to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. — 广播层级参考。 * C...

=== jetstream (2 occurrences) ===
  c04_b06.md: ...区内提供全序广播。分区 leader 排序写入；副本按相同顺序应用。这是限定在一个分区内的原子广播。 * **NATS JetStream** 提供基于 stream 的有序交付，至少一次。根据配置接近因果或更好。 * **etcd** 通过 Raft 提供...
  c09_b05.md: ...kroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxos 仍在某些细分领域获胜：Google 基础设施（Chub...

=== nats (2 occurrences) ===
  c04_b06.md: ...pic 分区内提供全序广播。分区 leader 排序写入；副本按相同顺序应用。这是限定在一个分区内的原子广播。 * **NATS JetStream** 提供基于 stream 的有序交付，至少一次。根据配置接近因果或更好。 * **etcd**...
  c09_b05.md: ...l、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxos 仍在某些细分领域获胜：Google...

=== single (2 occurrences) ===
  c05_b02.md: ...rship）、Spanner（通过 Paxos 的 per-tablet leadership）。  性质：  * **Single source of write order** ：leader 将写串行化。所有副本按相同顺序应用。 * **Fail...
  c11_b01.md: ...n”、“consensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、MongoDB。Leader 串行化写；follower 异步复...

=== default (2 occurrences) ===
  c05_b05.md: ...用于自动 RPO=0 且多区域容错。训练技能：按操作的 RPO 需求选择复制模式，而非按系统默认。  System | Default replication | Configuration options ---|---|--- PostgreSQL...
  c16_b05.md: ...(e) **Long-term archive** ：数据比提供服务的公司活得更久。 (f) **Privacy by default** ：数据不需要云账户。 (g) **Ownership** ：用户控制数据。  CRDT 是 (c)、(d) 及部分...

=== observed (2 occurrences) ===
  c05_b09.md: ...-writes 伪代码。走查当客户端从落后的 follower 读时会发生什么。  提示  客户端跟踪 max-HLC-observed。读请求带“最小所需 HLC”。服务器要么等待，要么返回该 HLC 的数据，要么拒绝。  **答案：**       c...
  c30_b14.md: ...counter.py     — PN-Counter     │   ├── or_set.py         — Observed-Remove Set     │   └── tiny_yjs.py       — minimal RGA-like...

=== access (2 occurrences) ===
  c06_b03.md: ...ne 路径之一，网络在它们之间做负载均衡。  Leaf-spine 取代经典三层网络（core、aggregation、access），因为三层的超额订阅模式在 core 产生带宽瓶颈，限制了东西向（服务器到服务器）流量，而这正是分布式系统产生的。...
  c06_b03.md: ...近（差几微秒）。网络局部性不如应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实现：  * **RoCE（R...

=== infiniband (2 occurrences) ===
  c06_b03.md: ...ure Cosmos DB 的基础）用 RDMA 以类似内存的延迟做跨节点内存访问。 * **ML 分布式训练** 用 InfiniBand 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDM...
  c25_b04.md: ...风格缓存层；高带宽存储，RDMA 连接 serving）。 * 高带宽互联：每个 rack 内 800 Gbit/s InfiniBand，rack 间 400 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可...

=== nic (2 occurrences) ===
  c06_b03.md: ...应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实现：  * **RoCE（RDMA over Con...
  c06_b03.md: ...A 延迟约 1-2 微秒（同数据中心），而 kernel-bypass TCP 约 50-100 微秒。内核不参与其中；NIC 处理数据移动。  用途：  * **分布式内存数据库**（Microsoft FaRM，曾是 Azure Cosmos...

=== pfc (2 occurrences) ===
  c06_b03.md: ...**RoCE（RDMA over Converged Ethernet）** ：2026 年最常见；跑在以太网上，用 PFC（Priority Flow Control）保证无损交付。 * **iWARP** ：基于 TCP 的 RDMA；较少...
  c06_b03.md: ...prefill 与 decode 节点之间移动 KV cache。  RDMA 不是银弹。它需要精心网络配置（无损保证、PFC 调优）、应用必须使用 RDMA 原语（one-sided puts 和 gets），且失败模式与 TCP 不同。对大多...

=== roce (2 occurrences) ===
  c06_b03.md: ...Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实现：  * **RoCE（RDMA over Converged Ethernet）** ：2026 年最常见；跑在以太网上，用 PFC（Pri...
  c06_b03.md: ...的基础）用 RDMA 以类似内存的延迟做跨节点内存访问。 * **ML 分布式训练** 用 InfiniBand 或 RoCE 上的 RDMA 跨节点移动 GPU 梯度。 * **分布式 LLM 推理**（第24-26章）用 RDMA 在 pre...

=== bisection (2 occurrences) ===
  c06_b03.md: ...o-any bandwidth** ：任意服务器到另一服务器的流量只经过一个 spine hop。任意划分服务器之间的 bisection bandwidth 远高于经典树形拓扑。 * **无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与...
  c06_b03.md: ...实践含义：  * 同数据中心 RPC 延迟由序列化和排队主导，而非网络。在 CLOS 内，延迟下限是微秒级。 * 网络 bisection bandwidth 很少是瓶颈。磁盘 I/O、CPU 和应用层协调通常是。 * 跨机架与同机架 RPC 延迟相近（差几...

=== switches (2 occurrences) ===
  c06_b03.md: ...arles Clos 命名，1953），2026 年实现为 _leaf-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine...
  c06_b03.md: ...af-spine_。  结构：  * **Spine switches** ：顶部少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多...

=== mumbai (2 occurrences) ===
  c06_b06.md: ...念检测  **Q1.** 计算以下单程光速下限：(a) Chicago 到 São Paulo，(b) Tokyo 到 Mumbai，(c) 赤道上相距 180 度的任意两点。  提示  光纤中光速约 200,000 km/s。地球周长约 40,000...
  c06_b06.md: ...ms。真实电缆绕行加勒比和巴西海岸，有效距离约 10,000 km，即 50 ms 单程。  (b) Tokyo 到 Mumbai：大圆约 6,750 km。下限：34 ms。真实电缆经香港、新加坡，距离增加；预计单程 50-60 ms。  (c)...

=== fabric (2 occurrences) ===
  c06_b06.md: ...微秒（RoCE 上的 RDMA）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV...
  c06_b07.md: ...秒（RDMA over RoCE）。对总请求延迟 ~10 ms 的应用，这只省 ~0.5% 预算——不值部署 RDMA fabric 的运维复杂度。  对分布式 LLM 推理，prefill 与 decode 节点之间每次 token 都要移动 KV...

=== michael (2 occurrences) ===
  c07_b01.md: ...是最小形式陈述的共识问题。它听起来不难，但 “到底有多难” 的精确陈述是分布式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即...
  c07_b01.md: ...的精确陈述是分布式系统理论的核心结果之一。  1985 年，Michael Fischer、Nancy Lynch 和 Michael Paterson 发表了一个让领域花数年才消化的结果：在即使只可能发生一次崩溃故障的完全异步系统中，任何确定性共识协议...

=== stabilization (2 occurrences) ===
  c07_b02.md: ...来自 Dwork、Lynch 和 Stockmeyer 1988 年关于部分同步下共识的论文。模型引入 _Global Stabilization Time_（GST），一个未知时刻，此后消息交付被某个未知但有限的延迟限制。  非对称很重要：GST 前系统完全异步（...
  c07_b06.md: ...答案：** 部分同步（Dwork、Lynch、Stockmeyer 1988 形式化）：系统有一个未知的 Global Stabilization Time（GST）。GST 前系统完全异步（消息交付和进程速度无界）。GST 后系统同步（消息在未知但有限上界内交付）...

=== stockmeyer (2 occurrences) ===
  c07_b02.md: ...议通常用部分同步加隐式故障检测器（心跳）。   ### 7.4 部分同步  经典形式化来自 Dwork、Lynch 和 Stockmeyer 1988 年关于部分同步下共识的论文。模型引入 _Global Stabilization Time_（GST），一个...
  c30_b02.md: ..._Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the presence of partial synchrony...

=== alpern (2 occurrences) ===
  c07_b03.md: ...ess 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是...
  c30_b02.md: ...e of partial synchrony." _Journal of the ACM_. — 部分同步形式化。 * Alpern, B., & Schneider, F. B. (1985). "Defining liveness." _Infor...

=== schneider (2 occurrences) ===
  c07_b03.md: ...每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发生才算违反。  Alpern-Schneider 分解（1985）表明系统每个性质都是 safety 性质与 liveness 性质的合取。这不仅是理论趣味；它让设计师...
  c30_b02.md: ...ynchrony." _Journal of the ACM_. — 部分同步形式化。 * Alpern, B., & Schneider, F. B. (1985). "Defining liveness." _Information Processing...

=== subtle (2 occurrences) ===
  c07_b04.md: ...其他来源实现。  Validity 在实践中很少是最难的性质；agreement 和 termination 才是。但 subtle 的 validity 违反存在：拜占庭共识协议（第10章）必须仔细定义 validity，因为拜占庭进程可以声称“提议...
  c09_b04.md: ...。Raft 论文的图 8 说明了该情况；修复就是“current term only”提交规则。这是 Raft 中唯一 subtle 的安全条件；没有它，协议不安全。   #### 常见误解  Raft 的常见误读：“leader 复制，多数一 ack...

=== learner (2 occurrences) ===
  c08_b01.md: ...*Proposer** ：试图让某个值被选定。 * **Acceptor** ：对提议投票；崩溃后持久化状态。 * **Learner** ：发现已选定的值。  单个进程可扮演所有三种角色。协议对任意 $N \ge 2f + 1$ 有效，其中 f 是容忍...
  c09_b10.md: ...Raft 实现，包括 joint consensus 重配置、快照、领导权转移、leader 下台、pre-vote、learner 节点（不影响法定人数的追赶）以及各类优化，复杂度与完整 Multi-Paxos 实现相当。Diego Ongaro 博...

=== parliament (2 occurrences) ===
  c08_b01.md: ...ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。它描述了一座虚构希腊岛屿，兼职立法者通过涉及信使、账簿和法定人数的繁复仪式颁布法令。论文审稿不佳（一位审稿人...
  c30_b02.md: ...Letters_. — 安全性/活性分解。 * Lamport, L. (1998). "The Part-Time Parliament." _ACM TOCS_. — 原始 Paxos 论文。 * Lamport, L. (2001). "Paxos M...

=== learners (2 occurrences) ===
  c08_b02.md: ...oser 收集到多数 `ACCEPTED` 时，值 v 被 _选定_。   #### Learning  一旦值被选定，learners 必须学到它。最简单做法：acceptor 接受一个值且其支持达到多数时，就告诉 learners。也有优化（指定 le...
  c08_b02.md: ...ng  一旦值被选定，learners 必须学到它。最简单做法：acceptor 接受一个值且其支持达到多数时，就告诉 learners。也有优化（指定 learner；把信息捎带在协议消息上）。   ### 8.4 为什么 Paxos 是安全的  Saf...

=== lceil (2 occurrences) ===
  c08_b10.md: ...accepted ballot”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N+1)/2 \rceil$。$N=4$ 时多数是 3；$N=5$ 时多数也是 3。LLM 代码常硬编码错误值，与偶...
  c10_b05.md: ...法定人数与任意 accept 法定人数相交，safety 就成立。  示例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rceil + 1$。两者都是多数。 * Grid quorums：$Q_1$ = 一行，$Q_2$ = 一...

=== rceil (2 occurrences) ===
  c08_b10.md: ...”决胜规则。  (c) **法定人数 off-by-one。** N 个节点的多数是 $\lceil (N+1)/2 \rceil$。$N=4$ 时多数是 3；$N=5$ 时多数也是 3。LLM 代码常硬编码错误值，与偶数节点一起使用时破坏法定人数相...
  c10_b05.md: ...cept 法定人数相交，safety 就成立。  示例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rceil + 1$。两者都是多数。 * Grid quorums：$Q_1$ = 一行，$Q_2$ = 一列。$N$ 是方阵；两...

=== available (2 occurrences) ===
  c08_b11.md: ...“Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongaro 和 Ouster...
  c30_b01.md: ...2). "Brewer's conjecture and the feasibility of consistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化...

=== algorithm (2 occurrences) ===
  c09_b01.md: ...out 设计，2014 年论文题为 “In Search of an Understandable Consensus Algorithm”，目标是：至少和 Paxos 一样正确，但 _可理解_。摘要明确把可理解性作为主要贡献。  本章按 Anya 学习 R...
  c30_b02.md: ...rhout, J. (2014). "In Search of an Understandable Consensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H., Malkhi, D., & Spie...

=== understandable (2 occurrences) ===
  c09_b01.md: ...Raft 由 Ongaro 和 Ousterhout 设计，2014 年论文题为 “In Search of an Understandable Consensus Algorithm”，目标是：至少和 Paxos 一样正确，但 _可理解_。摘要明确把可理解性作为...
  c30_b02.md: ...改写。 * Ongaro, D., & Ousterhout, J. (2014). "In Search of an Understandable Consensus Algorithm." _USENIX ATC_. — Raft 论文。 * Howard, H....

=== date (2 occurrences) ===
  c09_b02.md: ...已提交条目。若 candidate 的日志比某些已提交条目短，该条目可能“丢失”（leader 会覆盖它）。up-to-date 规则防止这种情况。...
  c09_b03.md: ...到多数。任何未来 leader 必须由多数选举产生。两个多数相交（法定人数相交）；相交节点拥有该条目。根据 up-to-date 规则，新 leader 至少拥有任何多数成员的所有条目，因此包括该已提交条目。于是新 leader 保留它，且索引 i...

=== voter (2 occurrences) ===
  c09_b02.md: ...date 的日志被认为是“最新”的，当满足以下之一：  (a) candidate 最后一条日志条目的 term 高于 voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter...
  c09_b02.md: ...voter 最后一条的 term；或 (b) 两者最后条目 term 相同，且 candidate 的日志长度至少与 voter 一样长。  该规则确保当选 leader 拥有所有已提交条目。若 candidate 的日志比某些已提交条目短，该条目...

=== old (2 occurrences) ===
  c09_b04.md: ...地各自形成“多数”，从而选出不同 leader。  Raft 的解：_joint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过中间配置 $C_{old,new}$，该配置要求 _同时_ 得到旧配置和新配置的...
  c09_b04.md: ...解：_joint consensus_。从配置 $C_{old}$ 到 $C_{new}$ 的过渡经过中间配置 $C_{old,new}$，该配置要求 _同时_ 得到旧配置和新配置的多数。过渡期间任何决定都需要联合多数。过渡完成后，集群才切换到...

=== etcdctl (2 occurrences) ===
  c09_b05.md: ...。 * **生态成熟** ：etcd 的 Raft 实现（Go）被广泛使用；多种语言实现存在。 * **运维工具** ：etcdctl、consul members、CockroachDB 的 raft visualizer。协议暴露足够状态供观察。 *...
  c19_b05.md: ...原生使用 | Hadoop 生态（Kafka、HBase 历史） | | Operational tooling | etcdctl；云原生 | zkCli.sh；较老模式 | | Performance | 针对现代硬件优化 | 成熟但较老优化 |...

=== installsnapshot (2 occurrences) ===
  c09_b08.md: ...ndEntries 拒绝检测 follower 落后；它回退 nextIndex 直到匹配，然后前向追加。差距极大时用 InstallSnapshot。  **答案：** Follower 重新加入时：  (a) Leader 的下一次心跳（`entries=[]` 的...
  c09_b08.md: ...wer 磁盘写入时间。  若差距极大（例如日志已快照，follower 的最后条目在快照点之前），leader 发送 `InstallSnapshot` RPC。Follower 用快照替换状态，然后从快照末尾继续正常 `AppendEntries` 复制。  生产 e...

=== danezis (2 occurrences) ===
  c10_b01.md: ...ution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danezis et al. 2022<br/>DAG mempool"]         Narwhal --> Sui["Sui"...
  c30_b02.md: ...BFT Consensus with Linearity and Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (202...

=== generals (2 occurrences) ===
  c10_b01.md: ...性多租户系统的控制面）。  BFT 家族树：       graph LR         BG["Byzantine Generals<br/>Lamport et al. 1982"] --> PBFT["PBFT<br/>Castro-Liskov...
  c30_b02.md: ...amport, L., Shostak, R., & Pease, M. (1982). "The Byzantine Generals Problem." _ACM TOPLAS_. — BFT 基础。 * Castro, M., & Liskov, B...

=== pease (2 occurrences) ===
  c10_b01.md: ...ll:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport、Shostak 和 Pease 在 1982 年论文中形式化：N 个将军包围一座城市，必须就是否进攻达成一致。每个将军有意见。某些是叛徒，可能向不同将...
  c30_b02.md: ...ited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzantine Generals Problem." _ACM TOPLAS_....

=== shostak (2 occurrences) ===
  c10_b01.md: ...Narwhal fill:#fff4e6   ### 10.1 拜占庭威胁模型  经典拜占庭将军问题由 Lamport、Shostak 和 Pease 在 1982 年论文中形式化：N 个将军包围一座城市，必须就是否进攻达成一致。每个将军有意见。某些是叛...
  c30_b02.md: ...ersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Pease, M. (1982). "The Byzantine Generals Problem."...

=== abraham (2 occurrences) ===
  c10_b03.md: ...r change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-change 协议，把每次...
  c30_b02.md: ...PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with Linearity and Res...

=== gueta (2 occurrences) ===
  c10_b03.md: ...推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 view-chan...
  c30_b02.md: ...nce." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with...

=== reiter (2 occurrences) ===
  c10_b03.md: ...阶段为何必要；推理 leader change 时飞行中提议会发生什么。  HotStuff 由 Yin、Malkhi、Reiter、Gueta 和 Abraham 在 2019 年论文中提出，是使 BFT 在更大规模实用的突破。关键创新：线性 vie...
  c30_b02.md: ...ne Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gueta, G. G., & Abraham, I. (2019). "HotStuff: BFT...

=== kwon (2 occurrences) ===
  c10_b04.md: ...rmint：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint 用部分 H...
  c30_b02.md: ...k: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT cons...

=== milosevic (2 occurrences) ===
  c10_b04.md: ...：工作马  Tendermint 在 Buchman 2016 年论文中描述，2018 年由 Buchman、Kwon、Milosevic 扩展，是 Cosmos 及许多其他区块链生态使用的 BFT 协议。Tendermint 用部分 HotStuff 特性...
  c30_b02.md: ...rotocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevic, Z. (2018). "The latest gossip on BFT consensus." _arXiv:18...

=== block (2 occurrences) ===
  c10_b04.md: ...）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图...
  c10_b04.md: ...l DAG 之上的共识协议。它利用 DAG 结构实现低延迟提交：leader 可以无需进一步通信就提交 DAG 中已有 block，因为 DAG 本身即是“共识”机制（每个 block 都被签名并引用）。  生产中：  * **Aptos** 使用受...

=== mini (2 occurrences) ===
  c10_b04.md: ...决定提交哪些 mempool 块的层）分离。  Narwhal mempool 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无...
  c10_b04.md: ...ol 是一个 _有向无环图（DAG）_ ，由 mini-blocks 组成，每个 block 由其生产者签名并引用先前 mini-blocks。DAG 结构允许许多生产者在无 leader 排序的情况下并行扩展图。  共识运行在 DAG 之上：le...

=== release (2 occurrences) ===
  c10_b04.md: ...态（Osmosis、Juno、Akash）都使用 Tendermint。  Tendermint 的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint fork 因此产生安全 bug。协议正...
  c27_b03.md: ...fencing_token=token)         finally:             etcd_lock.release(token)      def do_critical_work(action_id, fencing_token):...

=== revisited (2 occurrences) ===
  c10_b05.md: ...Heidi Howard 2016 年论文 “Flexible Paxos: Quorum Intersection Revisited” 推广了 Paxos 的法定人数要求。经典 Paxos 要求 prepare 法定人数 $Q_1$ 和 accept...
  c30_b02.md: ...Spiegelman, A. (2016). "Flexible Paxos: Quorum Intersection Revisited." _OPODIS_. — 可调 quorum。 * Lamport, L., Shostak, R., & Peas...

=== crash (2 occurrences) ===
  c27_b07.md: ...day 必须包含下游变慢场景。若审查者写 “saga 补偿处理第 4 步失败”，game day 必须在第 4 步中点 crash worker。审查的主张成为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御...
  c27_b07.md: ...r API）有 0.1 重试预算和 50% 失败率熔断器。” 下一次 game day 计划：(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢复；(b) 让 partner API 返回 50%...

=== reasonably (2 occurrences) ===
  c10_b07.md: ...议通常要求每条消息加密签名（以证明谁说了什么），增加 CPU 成本。  2026 年的实践界线：若运维者可信且节点安全 reasonably，崩溃容错假设正确。若运维者不可信（区块链验证者、合资）或攻击面包含节点本身，需要 BFT。  **Q2.** 为什么...
  c30_b12.md: ...mesh。  **崩溃故障共识足够时不要伸手选 BFT。** BFT 需要 3 倍副本和运维成本。对操作者可信、节点 reasonably 安全的团队，崩溃故障共识（Raft）是正确选择。BFT 属于特定高 stakes 垂直领域（区块链、跨组织金融结算、国...

=== stop (2 occurrences) ===
  c10_b07.md: ...的消息。  **答案：** _Crash fault_ 是节点停止运行的故障：停止发消息、停止响应消息，且在 fail-stop 模型中直到重启才恢复。Paxos 和 Raft 假设崩溃故障。  _Byzantine fault_ 是节点任意行为的...
  c18_b03.md: ...k 对效率场景提供合理保证。对正确性场景则不提供保证，原因有二：  (a) **进程暂停**。客户端获得锁后经历长时间 stop-the-world 暂停（垃圾回收、虚拟机暂停）。锁 TTL 过期。另一个客户端获得锁。原客户端恢复，未意识到锁已过期...

=== incident (2 occurrences) ===
  c10_b10.md: ...。  (e) **运维复杂度** ：4 个 DC 的 12 个验证者意味着 12 台服务器运维、4 个网络管理、4 个 incident response 团队协调。运维面是单区域 Raft 设置的 3-4 倍。  权衡：“一个 DC 沦陷”是否现实威胁？...
  c28_b09.md: ...作为一张卡片摘要在此总结；完整讲解在对应章节。"Pitfalls" 列列出事件响应最常查阅的调试时失败模式；这是读者在 incident 期间最常参考的列。  > 下表故意较宽（7 列）。视口较窄的读者也可参考附录 F.1-F.5 的叙事形式决策走查，以及...

=== pipelined (2 occurrences) ===
  c10_b10.md: ...效果：稳态下两者都能达到每轮往返提交一个决定。HotStuff 的 per-decision 延迟是 3 轮往返，但因 pipelined 所以吞吐与 Raft 相近。差异体现在延迟：Raft 每次提交一轮往返；HotStuff 每次提交三轮往返，但 pip...
  c10_b10.md: ...ned 所以吞吐与 Raft 相近。差异体现在延迟：Raft 每次提交一轮往返；HotStuff 每次提交三轮往返，但 pipelined 使吞吐相同。  区块链含义：HotStuff 的 per-decision 延迟为 3 轮往返，在地理分布验证者集合中...

=== solarwinds (2 occurrences) ===
  c10_b11.md: ...，因为威胁模型包含被攻破的云厂商。”  **讨论：** 辩护：云厂商被攻破是真实且日益增长的威胁。Snowden 披露、SolarWinds 攻击以及各国行为体都表明“云可信”对高价值数据不是安全假设。BFT 正是针对这种威胁模型。  反驳：BFT 的运维成本...
  c29_b01.md: ...面  2026 现实（第10章）：BFT 主要用于区块链；非区块链 BFT 罕见。  2027-2030 轨迹：  * SolarWinds 式供应链攻击 2020-2024 年已改变行业威胁模型。云厂商被攻破对高 stakes 数据不再是假设。 * **机密...

=== linearizability (2 occurrences) ===
  c11_b06.md: ...mit-wait 或 HLC restart）。许多工作负载需要 serializability（并发下正确）但不需要 linearizability（实时下正确）。知道差别可避免过度付费。  **Q3.** 用诊断方法给一个你没用过的系统产出一段阅读。可选：Mongo...
  c27_b01.md: ...；不考虑 “半成功” 结果。    6. **CAP 分类简单**。说 “我们用 CP 系统” 而不理解 PACELC、linearizability vs serializable 或 per-operation 选择的代码。  LLM 默认产出嵌入全部六个的代码。本...

=== surfaced (2 occurrences) ===
  c11_b07.md: ...CockroachDB 博客不会处理 GCP 锁定问题（一家厂商不会强调另一家的锁定劣势，另一家会过度强调）。方法自然 surfaced，因为“需要什么云”是优势/劣势综合的一部分。...
  c20_b08.md: ...式：1% 中的 80%（即所有请求的 0.8%）足够高，表明真实 bug，但低到随机抽查可能发现不了。Tracing  surfaced 这些客户支持量尚未检测到的长尾问题。  **Q3.** 某团队已 rollout OpenTelemetry。Trac...

=== majority (2 occurrences) ===
  c11_b08.md: ...点 ack）。复制最终一致；若主库在复制前崩溃可能丢失。 * _Majority 写_ ：`writeConcern: majority`。写在被 ack 前已持久化到多数；主库故障后存活。 * _默认读_ ：`readConcern: local` 从主...
  c11_b08.md: ...eadPreference: secondary` 时）。 * _Majority 读_ ：`readConcern: majority` 返回已被多数 ack 的数据。 * _Linearizable 读_ ：`readConcern: lineariz...

=== aspect (2 occurrences) ===
  c11_b09.md: ...性 AP，quorum CP PACELC | 可调；低一致性 PA/EL，quorum PC/EC  差异在实现：  Aspect | Cassandra | ScyllaDB ---|---|---- Implementation | Java（J...
  c19_b05.md: ...都是分布式协调服务，具有相似原语（键值存储、watch、lease/ephemeral node）。  差异：  | Aspect | etcd | ZooKeeper | |---|---|---| | Consensus | Raft | ZAB...

=== ecosystem (2 occurrences) ===
  c11_b09.md: ...rational complexity | 高（compaction 调优、GC 调优） | 中等（架构已处理大部分） Ecosystem | 更大（项目更老） | 成长中；Cassandra 兼容 API Cost | 开源；众多云托管 offering...
  c19_b05.md: ...较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Hadoop 生态（Kafka、HBase 历史） | | Operation...

=== implementation (2 occurrences) ===
  c11_b09.md: ...m PC/EC  差异在实现：  Aspect | Cassandra | ScyllaDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC...
  c30_b14.md: ...21_resilience/     │   ├── retry_budget.py   — retry budget implementation (gRPC-style)     │   ├── circuit_breaker.py — three-state c...

=== java (2 occurrences) ===
  c11_b09.md: ...Aspect | Cassandra | ScyllaDB ---|---|---- Implementation | Java（JVM） | C++（shard-per-core） Memory profile | 受堆限制（GC 停顿） | 无...
  c19_b10.md: ...sul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Java 中心化服务发现。Netflix 使用；2026 年较不常见，但某些遗留系统仍在用。  对 2026 年的新 EC2 部...

=== ops (2 occurrences) ===
  c11_b09.md: ...e | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compactio...
  c11_b09.md: ...堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compaction 调优、GC 调优） | 中等（架构...

=== hbase (2 occurrences) ===
  c12_b01.md: ...承担所有写。 * **路由需要元数据查找** ：客户端必须知道每个分区持有哪个范围；元数据必须复制并保持最新。  用于：HBase（HBase 的存在理由就是对宽行做范围扫描）、CockroachDB（默认）、Spanner（per-tablet）、...
  c12_b06.md: ...oDB。  范围适合：时序数据库常查询“T1 到 T2 之间的所有事件”。按时间戳范围分片使该查询成为单分区扫描。示例：HBase、ClickHouse、TimescaleDB。  许多现代系统 per-table 提供两者：CockroachDB...

=== clustering (2 occurrences) ===
  c12_b01.md: ...nternal` shard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering columns，使同一 partition 内数据有序（在哈希 partition 内实现局部范围查询）。  选择取决...
  c12_b06.md: ...者：CockroachDB 默认范围但支持哈希分片表；Cassandra 哈希分片但允许同一 partition 内按 clustering 列排序做局部范围查询。  **Q2.** 为什么一致哈希比分布式键值存储中的 “hash mod N” 更好？  提示...

=== karger (2 occurrences) ===
  c12_b02.md: ...新哈希几乎所有键，导致所有数据移动。12 节点集群加 1 节点，12/13 的数据必须移动，运维不可行。  一致哈希由 Karger 等人在 1997 年关于 Web 缓存分布的论文中提出，解决了这个问题。思想：  * 把节点和键都映射到一个环形哈希空...
  c30_b02.md: ...:1807.04938_. — Tendermint。   #### 分布式存储（第四部分 — 第11-14章）  * Karger, D., et al. (1997). "Consistent Hashing and Random Trees."...

=== vnodes (2 occurrences) ===
  c12_b02.md: ...拟节点”（vnode）散布在环上。Cassandra 默认每个物理节点 256 个 vnode；这使负载分布更均匀。  Vnodes 也有助于再平衡：新节点加入时从多个现有节点取 vnode，把重平衡成本分散到整个集群，而非集中在一个邻居。   ###...
  c12_b11.md: ...风险。现代系统 per-table 提供两者。  一致哈希是哈希分片系统的基础：增删节点只移动 $O(1/N)$ 数据。Vnodes（每物理节点 256+）实现均匀负载分布。  带自动分裂的范围分片（CockroachDB、Spanner）能处理哈希无...

=== frac (2 occurrences) ===
  c12_b04.md: ...计 42 TB，移动 20%（8.4 TB），每流 100 MB/s（4 条流并行；总 400 MB/s）：  \[ \frac{8.4 \text{ TB}}{400 \text{ MB/s}} \approx 21{,}000 \text{ s...
  c12_b08.md: ...xt{ TB} = 8400 \text{ GB} = 8{,}400{,}000 \text{ MB} \] \[ \frac{8{,}400{,}000 \text{ MB}}{400 \text{ MB/s}} = 21{,}000 \tex...

=== qps (2 occurrences) ===
  c12_b07.md: ...点得到 2 倍查询，带来磁盘和 CPU 压力。  12 个随机放置节点中，最大弧通常是平均值的 2-3 倍。即最忙节点 QPS 是平均节点的 2-3 倍，对应磁盘和 CPU 压力。  每个物理节点 256 个 vnode 时，每个物理节点在环上有...
  c30_b12.md: ...6 反模式：  **刚遇到扩展 pain 就不要伸手选 NewSQL。** 一个团队 PostgreSQL 在 50k QPS 出现 p99 延迟问题，通常不需要 CockroachDB；它需要的是读副本、更好的索引、连接池和 Redis 缓存。...

=== favor (2 occurrences) ===
  c12_b09.md: ...ree）。工作负载：80% 写、20% 读、随机访问、SSD 上 100 GB 总数据集。比较并推荐。  提示  写重 favor LSM。SSD 减小但未消除 B+ tree 随机写劣势。  **答案：** 工作负载特征：  * 80% 写、20%...
  c13_b16.md: ...。对高争用工作负载（正是金融系统在市场事件期间的处境），HLC 可变成本可能占主导。金融论证可能因 _可预测延迟_ 而 favor TrueTime，即使平均成本相似，p99 对 SLA 重要。  **Q2.** 一个 LLM 被要求 “在我们的微服...

=== filter (2 occurrences) ===
  c12_b09.md: ...+ WAL 顺序）。80% 写工作负载下表现好。 * 读延迟：每次读可能 touch memtable + Bloom filter + 多个 SSTable。冷读 p99 可能 1-5 ms。 * 空间放大：典型 1.5-2 倍（compaction...
  c12_b09.md: ...ree 随机写也比 LSM 顺序写慢 5-10 倍（持续高吞吐）。20% 读占比在 RocksDB 中配合 Bloom filter 做点查仍可接受。  例外：若团队更看重运维简单而非峰值性能，SQLite 仍可能是正确选择。SQLite “开箱即用”...

=== finalize (2 occurrences) ===
  c13_b01.md: ...：Commit 或 Abort**  若所有参与者投 YES，协调者发送 `COMMIT(txn_id)`；每个参与者 finalize prepared 数据。  若任一参与者投 NO（或协调者超时），协调者发送 `ABORT(txn_id)`；每个参与...
  c13_b12.md: ...或 NO（必须中止）。  阶段 2（Commit 或 Abort）：若全部投 YES，协调者发送 COMMIT；参与者 finalize。若任一投 NO（或超时），协调者发送 ABORT；参与者丢弃。  经典失败：协调者在发送 PREPARE 后、尚未发送...

=== dabek (2 occurrences) ===
  c13_b03.md: ...### 13.3 Google Percolator  Percolator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流...
  c30_b02.md: ...d merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distr...

=== peng (2 occurrences) ===
  c13_b03.md: ...### 13.3 Google Percolator  Percolator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事务协议，构建于 Bigtable 之上...
  c30_b02.md: ...og-structured merge-tree (LSM-tree)." _Acta Informatica_. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processin...

=== scheduler (2 occurrences) ===
  c13_b03.md: ...**Sequencer** ：每个区域的服务接收事务请求。它批量处理并分配全局顺序（通过跨区域共识）。   2. **Scheduler** ：每个分片接收排序后的批次并按顺序执行。锁按约定顺序获取；无死锁。   3. **Worker** ：在本地分片上...
  c30_b14.md: ...── continuous_batching_toy.py — minimal continuous-batching scheduler     └── ch27_review/         ├── ai_review_checklist.py — a...

=== thomson (2 occurrences) ===
  c13_b03.md: ...分布式事务加到键值存储上。事务层完全在存储层之上。   ### 13.4 Calvin：确定性排序  Calvin 由 Thomson 等人在 2012 年论文中提出，采用不同路径：事先按确定性顺序对所有事务排序，然后让每个分片按约定顺序执行。无冲突，无...
  c30_b02.md: ...ed Transactions and Notifications." _OSDI_. — Percolator。 * Thomson, A., et al. (2012). "Calvin: Fast Distributed Transactions...

=== inventoryreserved (2 occurrences) ===
  c13_b07.md: ...toryService        listens to PaymentCompleted  → publishes InventoryReserved     ShippingService         listens to InventoryReserved →...
  c13_b07.md: ...es InventoryReserved     ShippingService         listens to InventoryReserved → publishes ShippingScheduled     NotificationService     l...

=== ledgerservice (2 occurrences) ===
  c13_b07.md: ...ment.submit(transaction)      → bank_ref       step 4: call LedgerService.record(transaction, bank_ref)       step 5: call Notificati...
  c13_b07.md: ...CockroachDB，驱动每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步，最后动作 LedgerService.record，状态：in_progress。” 完整生命周期就是一行。  **Choreographed** —— 典...

=== notificationservice (2 occurrences) ===
  c13_b07.md: ...gerService.record(transaction, bank_ref)       step 5: call NotificationService.send(user, "completed")       step 6: call AnalyticsService...
  c13_b07.md: ...tens to InventoryReserved → publishes ShippingScheduled     NotificationService     listens to ShippingScheduled → sends confirmation   每个服...

=== ordercreated (2 occurrences) ===
  c13_b07.md: ...中央 orchestrator）：       OrderService            → publishes OrderCreated event     PaymentService          listens to OrderCreated...
  c13_b07.md: ...s OrderCreated event     PaymentService          listens to OrderCreated      → publishes PaymentCompleted     InventoryService...

=== paymentcompleted (2 occurrences) ===
  c13_b07.md: ...ntService          listens to OrderCreated      → publishes PaymentCompleted     InventoryService        listens to PaymentCompleted  →...
  c13_b07.md: ...hes PaymentCompleted     InventoryService        listens to PaymentCompleted  → publishes InventoryReserved     ShippingService...

=== shippingscheduled (2 occurrences) ===
  c13_b07.md: ...ingService         listens to InventoryReserved → publishes ShippingScheduled     NotificationService     listens to ShippingScheduled →...
  c13_b07.md: ...es ShippingScheduled     NotificationService     listens to ShippingScheduled → sends confirmation   每个服务响应前一个服务的事件。补偿通过反向事件触发：`PaymentFa...

=== compensations (2 occurrences) ===
  c13_b07.md: ...ce.update(merchant, day)       on failure at step N: invoke compensations for N-1, N-2, ..., 1   单一 orchestrator 服务知道工作流，把 saga 状态存在...
  c13_b07.md: ...ccess         Note over O: On failure at step N:<br/>invoke compensations<br/>for N-1, N-2, ..., 1...

=== confirmation (2 occurrences) ===
  c13_b07.md: ...otificationService     listens to ShippingScheduled → sends confirmation   每个服务响应前一个服务的事件。补偿通过反向事件触发：`PaymentFailed → OrderCancelled...
  c13_b07.md: ...on, bank_ref)         L-->>O: ledger_id         O->>N: send confirmation         N-->>O: sent         O->>A: update merchant aggrega...

=== invoke (2 occurrences) ===
  c13_b07.md: ...csService.update(merchant, day)       on failure at step N: invoke compensations for N-1, N-2, ..., 1   单一 orchestrator 服务知道工作...
  c13_b07.md: ...>>C: success         Note over O: On failure at step N:<br/>invoke compensations<br/>for N-1, N-2, ..., 1...

=== reserve (2 occurrences) ===
  c13_b07.md: ...oachDB-backed state):       step 1: call ReservationService.reserve(user, amount)       step 2: call FraudService.check(transac...
  c13_b07.md: ...as Analytics         C->>O: initiate payment         O->>R: reserve funds         R-->>O: reserved         O->>F: check fraud...

=== submit (2 occurrences) ===
  c13_b07.md: ...nsaction)         → score       step 3: call BankSettlement.submit(transaction)      → bank_ref       step 4: call LedgerServi...
  c13_b07.md: ...->>F: check fraud         F-->>O: score (OK)         O->>B: submit settlement         B-->>O: bank_ref         O->>L: record (...

=== success (2 occurrences) ===
  c13_b07.md: ...merchant aggregate         A-->>O: updated         O-->>C: success         Note over O: On failure at step N:<br/>invoke compe...
  c21_b02.md: ...calls fail         Closed: All calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/>no down...

=== compensation (2 occurrences) ===
  c13_b08.md: ...撤销”（你无法取消已发送通知）；通常是恢复不变量的前向动作（发送 “您的请求已取消” 通知；退款；重新入库）。 * **Compensation idempotent** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs...
  c13_b09.md: ...#### 补偿表示例  对 Crescent 支付 saga：  Step | Action | Compensation ---|---|---- 1 | 预留用户资金（CockroachDB 事务） | 释放预留 2 | 运行风控检查（C...

=== account (2 occurrences) ===
  c14_b05.md: ...法。** 假设你在构建实时欺诈检测服务：读率高（~50k reads/sec），写由事件驱动且突发，数据主要是 per-account 状态加一些跨账户图查询，读需要一致性（不能用过期数据做欺诈决策），单区域部署，工作负载包含每晚重训欺诈模型的批处理作业...
  c27_b11.md: ...payment):         # Acquire distributed lock for the user's account         lock_key = f"user_lock:{payment.user_id}"         i...

=== storm (2 occurrences) ===
  c13_b13.md: ...行事务。无冲突（顺序预先决定）。无因锁冲突导致的中止。  优点：无重试成本；确定性、可预测延迟；高争用下无 abort storm。  缺点：sequencer 是全局同步点；每笔事务付 sequencer 往返；sequencer 是跨区域瓶颈。...
  c13_b13.md: ...优点：无全局 sequencer；事务独立推进；低争用下吞吐很高。  缺点：争用导致重启；病态争用下产生 abort storm；高争用下延迟不可预测。  权衡：Calvin 赢在高争用工作负载（计数器递增、热行更新）；CockroachDB 赢在...

=== fintech (2 occurrences) ===
  c14_b02.md: ...ockroachDB 适合：不能接受 Spanner GCP 锁定的多区域 Postgres 工作负载。生产示例：众多 fintech（DoorDash、Comcast 部分工作负载、若干大型银行）。   ### 14.4 TiDB  PingCAP 的...
  c14_b08.md: ...AP 在衍生数据集（从运营存储 ETL 或 CDC）上做分析查询。  **Q4.** 对 2026 年构建支付处理器的 fintech 初创公司，你会推荐哪些数据系统？为每个给出理由。  提示  运营存储 + 账本 + 分析。对每种应用诊断方法。  **...

=== track (2 occurrences) ===
  c14_b02.md: ...）。 * 除 Postgres API 外还提供 Cassandra 兼容 API（YCQL）。  劣势：  * 生产 track record 比 CockroachDB 小。 * 多区域配置复杂（与 CockroachDB 类似）。  Yugab...
  c21_b02.md: ...probe calls fail         Closed: All calls go through;<br/>track success/failure rate         Open: All calls fail fast;<br/...

=== dremel (2 occurrences) ===
  c14_b04.md: ...ceberg 集成正在出现）；per-query 成本可能让调优差的工作负载意外。   #### BigQuery 的 Dremel  Google 的分析服务；基于 2010 年的 Dremel 研究。  * 在 Google Colossus 文件...
  c14_b04.md: ...差的工作负载意外。   #### BigQuery 的 Dremel  Google 的分析服务；基于 2010 年的 Dremel 研究。  * 在 Google Colossus 文件系统上使用列式存储。 * 通过动态资源分配大规模并行执行。 *...

=== warehouse (2 occurrences) ===
  c14_b04.md: ...owflake  云原生数据仓库，计算与存储分离。  * 存储在类 S3 云对象存储中。 * 通过独立 virtual warehouse 计算；多个 warehouse 可同时读同一数据。 * 通过元数据层（构建于 FoundationDB 之上）实现强一...
  c14_b04.md: ...，计算与存储分离。  * 存储在类 S3 云对象存储中。 * 通过独立 virtual warehouse 计算；多个 warehouse 可同时读同一数据。 * 通过元数据层（构建于 FoundationDB 之上）实现强一致。 * 时间旅行：可在保留期内...

=== neptune (2 occurrences) ===
  c14_b05.md: ...wflake 或 Trino 做批处理重训路径。图查询组件是例外——若跨账户图稠密，图数据库（Neo4j、Amazon Neptune）更适合这些特定查询，而 CockroachDB 保存图索引的账户状态。结果：三系统架构——CockroachDB（OL...
  c14_b05.md: ...保存图索引的账户状态。结果：三系统架构——CockroachDB（OLTP）、Snowflake（批处理/OLAP）、Neptune（图查询）。  该矩阵提供每个组件的起点；第11章诊断确认适配性；最终架构是跨组件的组合而非单一系统。...

=== join (2 occurrences) ===
  c14_b07.md: ...ke、BigQuery）面向 OLAP（在线分析处理）：少量但扫描 TB-PB 数据的大查询、每查询频率低、复杂聚合和 join。优化目标是大扫描上的查询吞吐，通常每查询数秒到数分钟。...
  c16_b01.md: ...))\) * **幂等律** ：\(\text{merge}(a,a) = a\)  这三条性质使状态在合并下形成一个 join-semilattice。任意合并序列都会收敛到所有已见状态的最小上界。  (b) **Operation-based...

=== jvm (2 occurrences) ===
  c14_b09.md: ...；成本由团队控制；完全调优控制；跨多源联邦查询；无锁定。 * 缺点：重大运维负担（计算集群管理、查询优化、缓存）；需要 JVM 调优、Trino 内部知识；成本包含运维团队。  对 SaaS 分析产品：...
  c22_b07.md: ...模式（间歇性 30 秒尖刺、健康检查通过）是双峰延迟灰色故障的特征。常见原因：  (a) **垃圾回收暂停**：基于 JVM 或其他 GC 语言的服务出现内存压力。GC 暂停在极端情况下可达 30 秒。诊断：GC 日志；找与延迟尖刺相关的长暂停...

=== commitment (2 occurrences) ===
  c14_b09.md: ...缺点：与现有 Aurora 分开运维、付费产品（企业功能需 license）。  推荐因素：  * 若团队可预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：C...
  c14_b09.md: ...预见未来仍 commitment AWS 且看重托管运维：Aurora DSQL。 * 若团队看重多云可选性或 AWS commitment 不确定：CockroachDB。 * 若团队增长已放缓且希望最小架构改动：继续用 Aurora Postgres 并纵...

=== facing (2 occurrences) ===
  c14_b10.md: ...源查询（联邦查询到客户控制的数据库）：Trino 的联邦是正确工具。  混合：团队内部分析用 Snowflake；客户-facing 联邦查询用 Trino（因为 Snowflake 无法联邦到客户控制的源）。许多真实 SaaS 平台两者都跑。  20...
  c26_b09.md: ...影响最大。Speculative decoding 有帮助，但更多是全面改善而非专门尾部。  更深观察：尾部延迟是用户-facing 指标。p50 2 秒且 p99 30 秒意味着 1% 用户看到中位数 15 倍延迟。对交互式工作负载（聊天），这 1%...

=== player (2 occurrences) ===
  c14_b10.md: ...倍。全球：多区域。  **答案：** 工作负载分析：  * **写**：10 万/秒 score 提交。需要 per-player 原子更新；不需要跨玩家原子性。 * **读**：1000 万并发用户 × ~1 次/分钟 = 16.7 万 reads...
  c14_b10.md: ...区域复制做全局视图。 * **Top-N 排行榜**：每几秒预计算一次；按区域缓存在 Redis 中。 * **Per-player 分数**：从 DynamoDB 读；在玩家归属区域强一致读。  延迟预算：  * Score 提交：5 ms（Dyna...

=== fifo (2 occurrences) ===
  c15_b02.md: ...rt 1985 年论文引入了一个协议，无需全局时钟即可产生代表一致全局状态的快照。   #### 场景  一组进程通过 FIFO 通道通信（每条通道消息按发送顺序投递）。系统状态是各进程状态加上每条通道上正在传输的消息。  _一致全局状态_ 是指一...
  c26_b04.md: ...户。每客户付费 tier 不同；有些有延迟 SLA。公平性问题：给定有限 GPU 资源，如何在客户间调度请求。  朴素 FIFO 调度失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer ra...

=== mapreduce (2 occurrences) ===
  c15_b03.md: ...，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD lineage、MapReduce 的 checkpointing、各种 ML 训练系统使用基于快照的重启。  该协议已有 40 多年历史，仍是思考一致分...
  c15_b07.md: ...pache Flink 的 exactly-once 语义建立于 Chandy-Lamport（或其变体）；Spark、MapReduce 等批处理系统使用相关思想。  **Q4.** 什么是 “last write wins”（LWW）冲突解决，它的失败模...

=== transactional (2 occurrences) ===
  c15_b08.md: ...dy-Lamport 变体）记录数据流中每个算子状态。重启后每个算子从 checkpoint 恢复状态。  (c) **Transactional sink** ：sink（此处 Postgres）必须参与 checkpoint。两种策略：  * **幂等写** ：...
  c27_b04.md: ...y()` 之前，通知未发出。系统处于不一致状态。  修复：识别哪些调用对一致性关键，并用 saga 模式（第13章）或 transactional outbox。       def update_user_across_systems(user):...

=== field (2 occurrences) ===
  c15_b08.md: ...告。诊断并给出修复建议。  提示  写节点间时钟偏移可导致 “逻辑上更晚” 的写输掉。检查 NTP 纪律并考虑 per-field 处理。  **答案：** 诊断：Cassandra 协调节点间时钟偏移。当用户从设备 A 编辑资料（通过协调器 A），...
  c15_b08.md: ...供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field CRDT** ：对合并语义明确的字段（如 “tags” 字段可用 CRDT 集合；“last_updated” 可用带...

=== operator (2 occurrences) ===
  c15_b08.md: ...rce 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记录数据流中每个算子...
  c19_b02.md: ...tes 的引力把 etcd 推向主导地位。  (d) **更好的集群管理**：etcd 的 `etcdctl` 和基于 operator 的管理比 ZooKeeper 的 CLI 和运维故事更友好。  ZooKeeper 仍在生产中使用（Kafka 旧元数...

=== transformation (2 occurrences) ===
  c16_b01.md: ...T 是过去十年分布式状态系统中最具运维重要性的进展。CRDT 出现前，“协作编辑” 需要复杂的 Operational Transformation（OT）算法，出了名的难实现正确（Google Docs 有 10 万行 OT 实现）。CRDT 出现后，数学处理冲突解...
  c16_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“CRDT 让 Operational Transformation 过时。2026 年所有新的协作编辑器设计都应使用 CRDT。”  **讨论：** 辩护：CRDT 在数学上比 OT 更...

=== element (2 occurrences) ===
  c16_b02.md: ...dot。若任何 add 的 dot 未被 tombstone，元素就在集合中。  * 在副本 \(j\) **add element x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove elem...
  c16_b02.md: ...ment x**：生成新唯一 dot \(d\)；将 \((x,d)\) 加入 pair 集合。 * **Remove element x**：找到集合中所有 \((x,d)\)；将 \(d\) 记入 tombstone 集合。 * **集合值**：若存...

=== array (2 occurrences) ===
  c16_b03.md: ...交替插入可使 id 长度翻倍）。删除字符的垃圾回收微妙。   #### RGA（Replicated Growable Array）  另一种方法：每个字符带一个 ID（通常是 (timestamp, replica)）和一个指向其 “前驱”（插入位...
  c16_b03.md: ...标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端拥有文档自己的副本。编辑更新本地副本并产生一个小的 _update_（二进制 blob，每次编辑通...

=== identifiers (2 occurrences) ===
  c16_b03.md: ...000 年代中期）。每个字符被分配一个 _position identifier_，位于相邻字符之间。Position identifiers 长度任意（通常是整数数组）；A 和 B 之间（id 1.2 与 1.5）插入的新字符获得 id 1.3 或 1.4。1...
  c16_b03.md: ...r 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position identifiers 可能变长（交替插入可使 id 长度翻倍）。删除字符的垃圾回收微妙。   #### RGA（Replicated Gro...

=== object (2 occurrences) ===
  c16_b04.md: ...客户端与服务器状态差并只发送所需内容。   #### 持久化  权威文档以 update 序列形式存在 Durable Object 存储中。定期地（约每小时），update 被 _compacted_ 成快照：文档被序列化，历史 update 被丢弃...
  c16_b04.md: ...：两个用户同时按 “删除文档”。CRDT 不帮忙；应用需要显式唯一动作语义，通常在服务器以轻量事务实现（Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增...

=== logseq (2 occurrences) ===
  c16_b05.md: ...新兴设计理念。生产示例：Linear（大量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT）、Replicache（CRDT-like 库）、Skiff（隐私导向文档，使用 Autome...
  c16_b11.md: ...命名这场运动——是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear、Notion、Glide、Logseq、Skiff 是示例。  2026 年新协作编辑器设计默认用 CRDT。现有基于 OT 的系统（Google Docs、...

=== skiff (2 occurrences) ===
  c16_b05.md: ...Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT）、Replicache（CRDT-like 库）、Skiff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成熟，更多应用可成为 local-fir...
  c16_b11.md: ...—是 CRDT 使能的设计理念：离线工作、无缝同步、用户拥有数据。Linear、Notion、Glide、Logseq、Skiff 是示例。  2026 年新协作编辑器设计默认用 CRDT。现有基于 OT 的系统（Google Docs、Micros...

=== excel (2 occurrences) ===
  c16_b09.md: ...上下文中的 A1），约束会破。对硬约束需要更强的一致性（每单元格线性化）。  真实协作表格（Google Sheets、Excel Online）对基本单元格值使用 CRDT/OT 组合，对结构操作使用 OT，对公式重新求值使用服务端协调。纯 CRD...
  c16_b09.md: .../OT 组合，对结构操作使用 OT，对公式重新求值使用服务端协调。纯 CRDT 表格存在（一些研究原型），但很少扩展到 Excel 的完整功能集。  模式：CRDT 擅长 _数据模型_（单元格）；_交互模型_（公式、范围操作、约束）通常需要额外层。...

=== jira (2 occurrences) ===
  c16_b09.md: ...上较慢，但 Automerge 2.x 在 2026 生产 benchmark 中已具竞争力；生产应用生态较小。  对 Jira 风格项目管理应用：  * **Issue 正文**：长期大量小编辑；Yjs 擅长。Automerge 的历史可启用 “...
  c16_b09.md: ...* 若审计/历史作为数据需求重要：Automerge。 * 若纯性能和生态成熟度占主导：Yjs。  对审计和历史重要的 Jira 风格应用，Automerge 是正确选择。对文档是焦点、历史是次要功能的 Notion 风格应用，Yjs 是正确选择。...

=== andersen (2 occurrences) ===
  c17_b01.md: ...；只要满足因果依赖，副本可独立应用因果有序操作。   #### 实现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带一个依赖向量：写者...
  c30_b02.md: ...." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsky, M., & Andersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal...

=== freedman (2 occurrences) ===
  c17_b01.md: ...**比线性化便宜** ：无需全局协调；只要满足因果依赖，副本可独立应用因果有序操作。   #### 实现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consisten...
  c30_b02.md: ...M. (2019). "Local-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsky, M., & Andersen, D. G. (2011). "Don't Sett...

=== kaminsky (2 occurrences) ===
  c17_b01.md: ...* ：无需全局协调；只要满足因果依赖，副本可独立应用因果有序操作。   #### 实现  Lloyd、Freedman、Kaminsky、Andersen 的 COPS 论文（2011）为键值存储引入 “causal+ consistency”。每次写携带...
  c30_b02.md: ...l-First Software." _Onward!_. * Lloyd, W., Freedman, M. J., Kaminsky, M., & Andersen, D. G. (2011). "Don't Settle for Eventual:...

=== feed (2 occurrences) ===
  c17_b02.md: ...#### 用例  (a) **社交媒体 feed** ：帖子和回复因果有序；跨用户 feed 可显示并发帖子的不同顺序。  (b) **评论和线程** ：回复跟随原帖；...
  c17_b02.md: ...#### 用例  (a) **社交媒体 feed** ：帖子和回复因果有序；跨用户 feed 可显示并发帖子的不同顺序。  (b) **评论和线程** ：回复跟随原帖；线程结构保持。  (c) **协作文档编辑*...

=== clustertime (2 occurrences) ===
  c17_b07.md: ...现：  (a) 每个会话跟踪见过的最高操作时间戳（来自自己的写或已执行的读）。(b) 会话上的后续读包含该时间戳作为 “clusterTime”。(c) 服务器（副本或主库）检查：本地 oplog 是否已应用到该 clusterTime？若是，服务读；若否，等待...
  c17_b07.md: ...后续读包含该时间戳作为 “clusterTime”。(c) 服务器（副本或主库）检查：本地 oplog 是否已应用到该 clusterTime？若是，服务读；若否，等待赶上（或超时）。  网络抖动期间，secondary 复制滞后。对滞后 secondary 的...

=== back (2 occurrences) ===
  c18_b01.md: ...**：写穿过缓存（缓存同步写入 truth source）。缓存与 source 始终一致。  (c) **Write-back（write-behind）**：写先写缓存；缓存异步写回 source。source 可能暂时过期。  (d) **T...
  c18_b01.md: ...event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...

=== invalidation (2 occurrences) ===
  c18_b01.md: ...sed**：缓存条目在指定时间后过期。隐式失效；TTL 后的下一次读会取最新。  (e) **Event-driven invalidation**：source 发布 “该 key 变了” 事件；缓存订阅并失效。  选择取决于工作负载：  * 读多写少：cach...
  c18_b01.md: ...he-aside + 长 TTL。 * 读多写多：cache-aside + 短 TTL 或 event-driven invalidation。 * 写多：write-through（保持一致）或 write-back（牺牲持久性换速度）。...

=== slot (2 occurrences) ===
  c18_b01.md: ...uster gossip  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 slot 属于一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向...
  c18_b01.md: ...p  Redis Cluster 是分片、复制的键值存储。键被映射到 16,384 个 hash slot 之一；每个 slot 属于一个 master，可有可选副本。  集群成员关系通过 gossip 维护：每个节点周期性向随机选择的其他节点子集...

=== locking (2 occurrences) ===
  c18_b03.md: ...pmann 的批评  2016 年，Martin Kleppmann 发表《How to do distributed locking》，批评 Redlock 并解释它为何没有提供表面上的安全性质。该文是分布式锁的奠基性读物。  核心问题：分布式锁有 _...
  c30_b03.md: ...— 第18-23章）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junque...

=== sequence (2 occurrences) ===
  c18_b03.md: ...行 fencing token。etcd 的锁原语、Consul 的基于 session 的锁、ZooKeeper 的 sequence-node 锁都提供这种结构。...
  c19_b07.md: ...* 服务发现：ZK ephemeral nodes → etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK...

=== granted (2 occurrences) ===
  c18_b04.md: .../>(token 101)         C1->>L: acquire lock         L-->>C1: granted, token=100         C1->>Bank: commit (token=100)         Ba...
  c18_b04.md: ...k TTL expired         C2->>L: acquire lock         L-->>C2: granted, token=101         C2->>Bank: commit (token=101)         Ba...

=== pause (2 occurrences) ===
  c18_b04.md: ...0); accept         Bank-->>C1: ok         Note over C1: GC pause 60s         Note over L: Lock TTL expired         C2->>L: a...
  c18_b04.md: ...Bank-->>C2: ok         Note over C1: resumed after pause         C1->>Bank: commit (token=100, stale)         Bank->...

=== wrong (2 occurrences) ===
  c18_b05.md: ...18.4 缓存作为锁的反模式  一个特定反模式：没有 fencing token 时把缓存当分布式锁。       # WRONG     if cache.set("user_processing_lock_42", "1", nx=True, e...
  c30_b14.md: ...RDT     ├── ch18_locks/     │   ├── redlock_naive.py  — the wrong way (no fencing tokens) — for demonstration     │   ├── etc...

=== headless (2 occurrences) ===
  c19_b03.md: ...ud DNS、Azure DNS 提供托管的 DNS 后端发现。适合成员变化少的服务。  * **Kubernetes headless services** ：“headless” Kubernetes service 没有 cluster IP；客户端...
  c19_b03.md: ...DNS 后端发现。适合成员变化少的服务。  * **Kubernetes headless services** ：“headless” Kubernetes service 没有 cluster IP；客户端解析服务名得到所有 pod IP。Kuber...

=== limits (2 occurrences) ===
  c19_b03.md: ...储使用共识。  示例：  * **Feature flags**：跨所有服务切换功能，无需重新部署。 * **Rate limits**：根据负载动态调整限速。 * **路由规则**：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret r...
  c26_b04.md: ...失败：一个客户突发大工作负载可垄断集群，饿死他人。  更公平调度器：  (a) **Per-customer rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高...

=== rotation (2 occurrences) ===
  c19_b03.md: ...s**：根据负载动态调整限速。 * **路由规则**：流量路由策略（金丝雀发布、A/B 测试）。 * **Secret rotation**：不重启服务即可分发轮换后的 secret。   #### 常见陷阱  (a) **Watch storms**：若...
  c19_b06.md: ...重读）。 * 运维变更即时生效；无需重启。  其他示例用例：流量路由规则（金丝雀发布）、限速（动态调整）、secret rotation（无需服务重启）、监控阈值（无需代码部署即可调整告警）。  **Q4.** 为什么基于 DNS 的服务发现对 State...

=== kube (2 occurrences) ===
  c19_b04.md: ...netes Service 对象；客户端解析 `payment.crescent.svc.cluster.local`；kube-proxy 负载均衡到健康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑...
  c19_b08.md: ...迁移到集群内发现（Kubernetes service）**：Kubernetes 用 etcd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Kubernetes API 中断影响新 service 定义，但不影响已有。...

=== proxy (2 occurrences) ===
  c19_b04.md: ...Service 对象；客户端解析 `payment.crescent.svc.cluster.local`；kube-proxy 负载均衡到健康 pod。  * **直接使用 etcd** 做领导者选举锁服务（第18章）。Raft 支撑的 etcd...
  c19_b08.md: ...内发现（Kubernetes service）**：Kubernetes 用 etcd 作为后端；每个节点的 kube-proxy 缓存 service 定义；Kubernetes API 中断影响新 service 定义，但不影响已有。对 Kube...

=== znodes (2 occurrences) ===
  c19_b05.md: ...于 gRPC，现代 | 较老的自定义协议 | | Key model | 平铺 key + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Hadoop 生态（Kafka、HBase 历史）...
  c19_b07.md: ...cd lock recipe（lease + revision number）。 * 配置：ZK persistent zNodes → etcd persistent keys。  映射每个原语语义：  * ZK ephemeral nodes（绑定...

=== known (2 occurrences) ===
  c19_b08.md: ...秒。诊断并给出修复方案。  提示  Consul 基于 gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：** 诊断：Consul 故障转移期间，集群重新选举 Raft leader。选举期间...
  c19_b08.md: ...ul 的客户端可能收到过期或错误响应。  若客户端配置为 “fail open”（Consul 不可用时使用 last-known 服务注册表），影响很小。若配置为 “fail closed”（Consul 不可达时返回错误），每次服务调用在选举窗口...

=== grafana (2 occurrences) ===
  c20_b03.md: ...源之一。成熟运维团队将 traces、logs、metrics、配置关联起来找根因。Honeycomb、Datadog、Grafana Tempo 提供集成视图。   ### 20.5 OpenTelemetry：标准  2026 年，OpenTelem...
  c28_b02.md: ...p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨...

=== name (2 occurrences) ===
  c20_b05.md: ...**Parent span ID**：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：s...
  c20_b05.md: ...*：标识该 span 的父节点；根 span 为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 *...

=== message (2 occurrences) ===
  c20_b08.md: ...消息、service B 消费时，B 没有 trace context 把它的工作链接回 A。  修复：在 Kafka message headers 中显式传播 trace context。生产端：       from opentelemetry.p...
  c20_b08.md: ...ers = {}     inject(headers)     producer.send(topic, value=message, headers=headers)   `inject` 函数按 W3C 标准将当前 trace context 作为...

=== producer (2 occurrences) ===
  c20_b08.md: ...ate import inject      headers = {}     inject(headers)     producer.send(topic, value=message, headers=headers)   `inject` 函数按...
  c20_b08.md: ...t`/`extract` 原语；应用代码使用它们。  Kafka 传播修复后，团队将看到从 API gateway → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分...

=== jaeger (2 occurrences) ===
  c20_b09.md: ...的失败：产出与特定厂商耦合的代码。团队现在被锁定在 DataDog；换到另一追踪后端（Honeycomb、Tempo、Jaeger）需要在代码库各处改代码。  正确答案：使用 OpenTelemetry SDK。OpenTelemetry 提供 ve...
  c20_b09.md: ...ation"):         do_work()   上述代码可与 DataDog、Honeycomb、Tempo、Jaeger 或任何 OpenTelemetry 兼容后端一起工作。Exporter 在启动时配置；应用代码相同。  更深观察：LL...

=== get (2 occurrences) ===
  c27_b02.md: ...f get_user_profile(user_id):         return profile_service.get(user_id)   谎言：没有 timeout、没有 retry、没有 circuit breaker、没有 fal...
  c27_b02.md: ...e(user_id):         try:             return profile_service.get(user_id)         except CircuitBreakerOpen:             ret...

=== reliability (2 occurrences) ===
  c21_b02.md: ...10% 也下降。放大被限制。  实现：每个客户端跟踪近期成功率；计算预算；抑制超额重试。  Google 的《Site Reliability Engineering》一书有经典讨论。该模式现在已是生产级库的标准（gRPC、Envoy、Istio）。   ###...
  c30_b03.md: ...r, B., Jones, C., Petoff, J., & Murphy, N. R. (2016). _Site Reliability Engineering_. O'Reilly. — 重试预算和熔断器作为生产纪律。 * The Jepsen repo...

=== downstream (2 occurrences) ===
  c21_b02.md: ...cess/failure rate         Open: All calls fail fast;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>...
  c21_b02.md: ...HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b) **Open**：失败阈值...

=== polly (2 occurrences) ===
  c21_b02.md: ...flix Hystrix（经典原型；已弃用，转而推荐 Resilience4j）；Resilience4j（Java）；polly（.NET）；各种反向代理（Envoy、HAProxy）在代理层实现熔断器。...
  c21_b06.md: ...etflix Hystrix（已弃用；被 Resilience4j 替代）；Resilience4j（现代 Java）；polly（.NET）；Envoy 和 HAProxy 作为反向代理。该模式在成熟分布式系统中是标准。  **Q4.** 2003...

=== spare (2 occurrences) ===
  c21_b04.md: ...库与其上游服务之间没有熔断器。已添加。  (d) merchant 数据库自动扩缩太慢（5 分钟置备时间）。现在预热的 spare capacity 保持热备。  **行动项**：  (1) 为非常规索引创建添加 linter 规则。(2) 把所有服...
  c21_b04.md: ...预算降到 10%。(3) 在所有服务与 merchant 数据库之间添加熔断器。(4) 预热 merchant 数据库 spare capacity。(5) 改进 “merchant DB 过载” 场景 runbook。(6) 进行 chaos en...

=== adaptive (2 occurrences) ===
  c21_b07.md: ...请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：确保每个服务按 2 倍正常负载配置。缓冲吸收尖...
  c30_b14.md: ...three-state circuit breaker     │   └── load_shedder.py   — adaptive concurrency limiter     ├── ch24_inference/     │   └── con...

=== concurrency (2 occurrences) ===
  c21_b07.md: ...P 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：确保每个服务按 2 倍正常负载配置。缓冲吸收尖峰而不饱和。  (e)...
  c30_b14.md: ...te circuit breaker     │   └── load_shedder.py   — adaptive concurrency limiter     ├── ch24_inference/     │   └── continuous_batc...

=== limiters (2 occurrences) ===
  c21_b07.md: ...出其并发容量的入站请求。返回 HTTP 503 加 `Retry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：确保每个服务按 2 倍正常负...
  c21_b07.md: ...ry-After`。工具：per-service rate limiters、adaptive concurrency limiters。  (d) **容量缓冲**：确保每个服务按 2 倍正常负载配置。缓冲吸收尖峰而不饱和。  (e) **为以上加可观测...

=== metastable (2 occurrences) ===
  c30_b03.md: ...on, N., Aguilera, M. K., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C.,...
  c30_b06.md: ...tree（日志结构合并树）**：写优化的存储底层，Bigtable、Cassandra、RocksDB 使用。 * **Metastable failure（亚稳态故障）**：损坏状态自我维持的故障；移除触发因素不能恢复健康状态。 * **Orchestrat...

=== magic (2 occurrences) ===
  c22_b04.md: ...#### Google 的 Magic Pony 中断（内部传说转述）  一个广泛部署的 Google 服务经历亚稳态故障：垃圾回收器进入持续收集状态（因为内...
  c22_b11.md: ...亚稳态是故障在单个组件或系统内自我维持。两者可共存；都需要各自防御。  真实案例：AWS S3 2017、Google Magic Pony（非正式）、Roblox 2021。都特征为小触发器、反馈循环、自维持的损坏状态。恢复耗时数小时到数天。  防...

=== pony (2 occurrences) ===
  c22_b04.md: ...#### Google 的 Magic Pony 中断（内部传说转述）  一个广泛部署的 Google 服务经历亚稳态故障：垃圾回收器进入持续收集状态（因为内存几乎总是...
  c22_b11.md: ...在单个组件或系统内自我维持。两者可共存；都需要各自防御。  真实案例：AWS S3 2017、Google Magic Pony（非正式）、Roblox 2021。都特征为小触发器、反馈循环、自维持的损坏状态。恢复耗时数小时到数天。  防御：有界反...

=== full (2 occurrences) ===
  c22_b07.md: ...式追踪，揭示瓶颈。即使只采样 1% 的请求，也足以识别模式。  (c) **添加详细 GC 监控**：GC 暂停直方图、full GC 次数、分配速率。对超过 1 秒的 full GC 告警。  (d) **添加下游依赖��控**：每个下游服务的...
  c22_b07.md: ...识别模式。  (c) **添加详细 GC 监控**：GC 暂停直方图、full GC 次数、分配速率。对超过 1 秒的 full GC 告警。  (d) **添加下游依赖��控**：每个下游服务的 SLI；当任何下游超过正常延迟时告警。  诊断流程...

=== health (2 occurrences) ===
  c22_b10.md: ...？用于生命最初 30-60 秒；区分 “启动中” 与 “坏了”。允许启动期间更长超时。  (d) **Detailed health endpoint**（给运维的 `/health/details`）：每个子系统状态的 JSON 输出。用于诊断；不用...
  c22_b10.md: ...与 “坏了”。允许启动期间更长超时。  (d) **Detailed health endpoint**（给运维的 `/health/details`）：每个子系统状态的 JSON 输出。用于诊断；不用于自动路由。  (e) **基于延迟的指标**：单...

=== playbook (2 occurrences) ===
  c22_b10.md: ...移）。  2026 现实：SRE 实践围绕亚稳态成熟。Bronson-Aguilera 论文提供框架；公司有内部亚稳态 playbook；“系统卡住” 的 runbook 是标准。随着意识提高，灾难性亚稳态故障（Roblox 规模的多天中断）频率已下降。...
  c23_b12.md: ...）及其对团队技术栈的适配。  (b) **Game day 结构**：赛前/当天/执行/复盘/赛后的模式。团队需要运营 playbook，不只是工具。  (c) **组织实践**：无责复盘、心理安全、记录学习、固定节奏。没有这些，技术实验不产生学习。  (...

=== gremlin (2 occurrences) ===
  c23_b02.md: ...) **Litmus**：另一个 Kubernetes 混沌框架。云原生；基于 Helm 部署；可扩展。  (c) **Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator*...
  c23_b02.md: ...：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...

=== litmus (2 occurrences) ===
  c23_b02.md: ...h**：Kubernetes 原生混沌工程。注入 pod 故障、网络故障、磁盘故障、时间偏移。广泛采用。  (b) **Litmus**：另一个 Kubernetes 混沌框架。云原生；基于 Helm 部署；可扩展。  (c) **Gremlin**：...
  c23_b02.md: ...慢连接、丢包、模拟下游变慢。与应用无关。  选择取决于部署：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...

=== concern (2 occurrences) ===
  c23_b03.md: ...ra（多次）**：轻量事务违反线性化；计数器不一致；故障转移期间丢失写。 * **MongoDB（多次）**：read concern 问题；分片元数据不一致；故障转移数据丢失。 * **CockroachDB**：早期版本有可串行化违规；后续版本修复。...
  c23_b07.md: ...。  发现：几乎所有主要分布式数据库首次测试时都违反了某些声明。Cassandra 轻量事务、MongoDB read concern、CockroachDB 早期可串行化、Redis 故障转移、etcd 旧版本——都有 Jepsen 发现的 bug。...

=== calling (2 occurrences) ===
  c24_b03.md: ...多请求共享前缀结构（system prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新请求到达时，系统...
  c24_b03.md: ...非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这...

=== chain (2 occurrences) ===
  c24_b03.md: ...。vLLM 优化内存布局，SGLang 优化请求路由布局。  洞察：许多请求共享前缀结构（system prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些...
  c24_b03.md: ...token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值...

=== function (2 occurrences) ===
  c24_b03.md: ...布局。  洞察：许多请求共享前缀结构（system prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 KV cache。新...
  c24_b03.md: ...SGLang 而非 vLLM**：若提示 token 中显著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存...

=== scaffold (2 occurrences) ===
  c24_b03.md: ...ng 优化请求路由布局。  洞察：许多请求共享前缀结构（system prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix tree 中组织 K...
  c24_b03.md: ...——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prompt token...

=== thought (2 occurrences) ===
  c24_b03.md: ...存布局，SGLang 优化请求路由布局。  洞察：许多请求共享前缀结构（system prompts、chain-of-thought scaffold、function-calling 模板）。RadixAttention 按这些前缀在 radix t...
  c24_b03.md: ...著比例跨请求共享——共同 system prompt、固定 function-calling 模板或 chain-of-thought scaffold——RadixAttention 的前缀缓存可避免重新计算这些 token。阈值大致：若平均 prom...

=== timeline (2 occurrences) ===
  c24_b09.md: ...→ vLLM（自建企业特性）。 * **合规紧迫性**：3 个月内 SOC 2 审计 → NIM（更快）；12 个月 timeline → 两者都可。 * **定制化需求**：重度定制 → vLLM（完全控制）；标准 serving → NIM（集成优于...
  c27_b08.md: ...系统判断仍是承重技能——不取决于 2026 年 AI 工具状态。模式匹配部分将进一步自动化；判断部分在任何可合理预测的 timeline 上不会自动化。   ### 27.11 练习...

=== tensor (2 occurrences) ===
  c24_b10.md: ...**Continuous batching**：应启用（现代引擎有；LLM 应验证已开启）。  (c) **多 GPU tensor parallelism**：Llama 3 70B 需要多张 GPU 放权重；部署必须跨 GPU shard 模型（t...
  c24_b10.md: ...**：GPU 故障、节点故障、模型加载故障。部署必须优雅处理每种。  正确答案：“基础部署用 TGI 或 vLLM 加 tensor parallelism。2026 生产还要包含 [以上列表]。以下是更完整配置……”  更深观察：“搭建推理集群” 答...

=== mul (2 occurrences) ===
  c25_b01.md: ...ory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode |...
  c25_b01.md: ...| ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth | ~10-50 ms | Reads KV cache |  一张 GPU 同...

=== pattern (2 occurrences) ===
  c25_b01.md: ...n。这是矩阵-向量乘法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|...
  c30_b06.md: ...t（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping anti-pattern（橡皮图章反模式）**：人类审查者未加思考就批准 AI 审查过的 PR——把 AI 的审查当作审查。命名于第27章 §2...

=== cachetier (2 occurrences) ===
  c25_b02.md: ...["Prefill Node 2<br/>4× H100"]         end         subgraph CacheTier["KV Cache Tier<br/>(Mooncake / LMCache)"]             K1[Ca...
  c25_b02.md: ...s"| LB         style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII...

=== decodetier (2 occurrences) ===
  c25_b02.md: ...]             K2[Cache Node 2]         end         subgraph DecodeTier["Decode Tier<br/>(memory-BW optimized)"]             D1["De...
  c25_b02.md: ...:#e6f3ff         style CacheTier fill:#fff4e6         style DecodeTier fill:#f0f9e8   ASCII 回退：                           ┌──── Re...

=== prefilltier (2 occurrences) ===
  c25_b02.md: ...raph TB         LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-optimized)"]             P1["Pre...
  c25_b02.md: ...ens"| LB         D2 -->|"streamed tokens"| LB         style PrefillTier fill:#e6f3ff         style CacheTier fill:#fff4e6         s...

=== streamed (2 occurrences) ===
  c25_b02.md: ...D1         K2 -->|"KV blocks per token"| D2         D1 -->|"streamed tokens"| LB         D2 -->|"streamed tokens"| LB         st...
  c25_b02.md: ...n"| D2         D1 -->|"streamed tokens"| LB         D2 -->|"streamed tokens"| LB         style PrefillTier fill:#e6f3ff...

=== rack (2 occurrences) ===
  c25_b04.md: ...cache 节点（Mooncake 风格缓存层；高带宽存储，RDMA 连接 serving）。 * 高带宽互联：每个 rack 内 800 Gbit/s InfiniBand，rack 间 400 Gbit/s。  **请求流**：    1....
  c25_b04.md: ...储，RDMA 连接 serving）。 * 高带宽互联：每个 rack 内 800 Gbit/s InfiniBand，rack 间 400 Gbit/s。  **请求流**：    1. 请求到达负载均衡器。   2. 负载均衡器基于可用性把请求...

=== scaling (2 occurrences) ===
  c25_b06.md: ...efill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning logic。...
  c30_b03.md: ..., W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Spar...

=== physical (2 occurrences) ===
  c25_b07.md: ...早期块传输到 decode 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存...
  c30_b01.md: ...as, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks." _OPODIS_. — HLC 论文。 * Corbett, J. C., et al. (2012...

=== headroom (2 occurrences) ===
  c25_b09.md: ...*：预测工作负载变化（例如基于时间、客户模式）；在变化前预扩。  (b) **余量缓冲**：两阶段都保持 10-20% headroom；吸收短期可变性。  (c) **多用途节点**：保留一小池可在两种角色间切换的节点；按需切换。这些节点效率不如专用节点...
  c25_b09.md: ...而非聚合指标扩缩。  对 Continuum 混合工作负载，团队使用 (a)、(b)、(d)。集群两阶段都保持 15% headroom；基于 per-phase 指标自动扩缩；对已知客户模式（如每日报告生成造成 prefill 尖峰）做预测扩缩。  **...

=== deficit (2 occurrences) ===
  c26_b04.md: ...ed fair scheduling**：每客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit...
  c26_b06.md: ...户有一个按权重速率累积 token 的桶。请求消耗 token；调度器从 token 最多的客户中挑选。  (b) **Deficit round-robin**：每客户轮流处理；每轮每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等...

=== onboarding (2 occurrences) ===
  c26_b08.md: ...能给出有竞争力的 per-request 定价；客户对延迟满意。  对 Tobias：这正是这些优化针对的场景。客户 onboarding 从第一天起就盈利；优化让 Continuum 能与 Anyscale、Together 等竞争。  **Q2.** 某...
  c27_b07.md: ...的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证——步骤 2-5 的 saga 补偿已定义；orchestr...

=== specifics (2 occurrences) ===
  c26_b11.md: ...度。正确 LLM 回应应问：“你的 prompt 结构是什么？多少租户？有缓存层吗？当前缓存命中率多少？我可以基于这些 specifics 设计路由策略。”  对 Continuum 部署，路由逻辑是数千行手写代码，为工作负载精细调优。LLM 生成的朴素实现...
  c27_b13.md: ...saga 但不是正确实现的东西。  更深观察：代码（或 PR 描述）中的设计模式声明容易做出但难以验证。审查者必须检查 specifics，而非接受标签。LLM 经常产出使用模式 _词汇_ 但无 _语义_ 的代码。词汇在训练数据中；语义需要理解。  对 Cr...

=== across (2 occurrences) ===
  c27_b11.md: ...tus='failed', reason='insufficient')              # Process across services             bank_result = bank_service.transfer(pa...
  c30_b14.md: ...└── propagation_demo.py — OpenTelemetry context propagation across HTTP + Kafka     ├── ch21_resilience/     │   ├── retry_bud...

=== false (2 occurrences) ===
  c27_b07.md: ...otency-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false positive，作者用一行理由 dismiss。  **第二层 —— 人类按清单审查（分钟级，每个分布式 PR）**...
  c27_b08.md: ...判断；第三层处理涌现行为。跳过任何一层都会产生可预测的缺口：跳过第一层，审查者浪费时间在常规捕获；跳过第二层，AI 的 false negative 进入生产；跳过第三层，防御只在纸面上存在，在负载下失败。   #### 这对人类审查者角色意味着什么...

=== partner (2 occurrences) ===
  c27_b07.md: ...录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为新的 partner-onboarding 工作流添加 saga 的 PR。人类审查者评论说：“已验证——步骤 2-5 的 saga 补偿已...
  c27_b07.md: ...(a) 在步骤 3 中点 crash orchestrator，验证新 orchestrator 正确恢复；(b) 让 partner API 返回 50% 错误，验证熔断器打开；(c) 在第 4 步重试期间注入网络分区，验证预算限制放大。每个场景 1:...

=== amount (2 occurrences) ===
  c27_b11.md: ...ent.user_id             )              if balance < payment.amount:                 return PaymentResult(status='failed', reas...
  c27_b11.md: ...nce = balance - %s WHERE id = %s",                 (payment.amount, payment.user_id)             )             notification_se...

=== started (2 occurrences) ===
  c27_b13.md: ...可安全重试多次。验证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周...
  c27_b13.md: ...(f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga...

=== cont (2 occurrences) ===
  c28_b03.md: ...mesh]             SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]         end         CF -...
  c28_b03.md: ...E --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         C...

=== crdbw (2 occurrences) ===
  c28_b03.md: ...[API Service]             REDISW[Redis Cluster]             CRDBW[CockroachDB nodes]             KAFKAW[Kafka brokers]...
  c28_b03.md: ...-.fraud.-> CONT         CRDBE <-->|"Raft<br/>cross-region"| CRDBW         CRDBE <-->|"Raft<br/>cross-region"| CRDBW2...

=== etcde (2 occurrences) ===
  c28_b03.md: ...STRATUSE[Stratus DB<br/>coordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]...
  c28_b03.md: ...APIE --> CRDBE         APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT...

=== euw (2 occurrences) ===
  c28_b03.md: ...KAFKAE[Kafka brokers]         end         subgraph EUW["EU-West Region"]             APIW[API Service]...
  c28_b03.md: ...e fill:#fff4e6         style USE fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         style S...

=== kafkae (2 occurrences) ===
  c28_b03.md: ...ordinator]             ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end         subgraph EUW["EU-West Re...
  c28_b03.md: ...APIE --> STRATUSE         APIE --> ETCDE         APIE --> KAFKAE         APIE -.fraud.-> CONT         CRDBE <-->|"Raft<br/>c...

=== redise (2 occurrences) ===
  c28_b03.md: ..."US-East Region"]             APIE[API Service]             REDISE[Redis Cluster]             CRDBE[CockroachDB nodes]...
  c28_b03.md: ...E         CF --> APIW         CF --> APIW2         APIE --> REDISE         APIE --> CRDBE         APIE --> STRATUSE         AP...

=== usw (2 occurrences) ===
  c28_b03.md: ...KAFKAW[Kafka brokers]         end         subgraph USW["US-West Region"]             APIW2[API Service]...
  c28_b03.md: ...E fill:#e6f3ff         style EUW fill:#e6f3ff         style USW fill:#e6f3ff         style Shared fill:#f0f9e8...

=== vsr (2 occurrences) ===
  c28_b03.md: ..."Shared Services"]             TB[TigerBeetle<br/>5-replica VSR<br/>multi-region]             CONSUL[Consul<br/>cross-regio...
  c28_b05.md: ...n 部署；每跳延迟预算 | n/a | | 7-10 | 共识 | Raft（Stratus、CockroachDB）、VSR（TigerBeetle） | E.5（防止 split-brain） | | 11 | 诊断方法 | 用于组件选择 |...

=== brokers (2 occurrences) ===
  c28_b03.md: ...ETCDE[etcd lock service]             KAFKAE[Kafka brokers]         end         subgraph EUW["EU-West Region"]...
  c28_b03.md: ...CRDBW[CockroachDB nodes]             KAFKAW[Kafka brokers]         end         subgraph USW["US-West Region"]...

=== discovery (2 occurrences) ===
  c28_b03.md: ...y"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATU...
  c28_b03.md: ...ly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.discovery.-> CONSUL         STRATUSE --> TB         style Edge fill:#...

=== nightly (2 occurrences) ===
  c28_b03.md: ...-->|"Raft<br/>cross-region"| CRDBW2         CRDBE -.->|"CDC nightly"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.d...
  c28_b03.md: ...CRDBE -.->|"CDC nightly"| SNOW         TB -.->|"CDC nightly"| SNOW         APIE -.discovery.-> CONSUL         APIW -.di...

=== items (2 occurrences) ===
  c28_b06.md: ...所有 region 的工作时间）。  (a) **发布前检查清单**：两周前混沌 game day；所有 action items 已关闭。容量测试通过。客户迁移计划已审。Rollback 计划已审。客服已通报。  (b) **06:00 UTC**...
  c28_b06.md: ...2%（在预期内）。无客户投诉。发布宣告成功。  (h) **24:00 UTC**：发布后复盘。教训记录；action items 归档。团队第二天休息（前一个月是每周 80 小时）。  发布是本书纪律的成功应用。没有意外；混沌工程提前抓住了本会在生...

=== stakes (2 occurrences) ===
  c29_b01.md: ...2030 轨迹：  * SolarWinds 式供应链攻击 2020-2024 年已改变行业威胁模型。云厂商被攻破对高 stakes 数据不再是假设。 * **机密计算**（Intel TDX、AMD SEV-SNP、AWS Nitro Enclave...
  c30_b12.md: ...运维成本。对操作者可信、节点 reasonably 安全的团队，崩溃故障共识（Raft）是正确选择。BFT 属于特定高 stakes 垂直领域（区块链、跨组织金融结算、国防），而非主流。  **按用户地理分区数据足够时不要伸手选 Spanner 风格全...

=== recurring (2 occurrences) ===
  c29_b03.md: ...§28.7 目录中的工作流（包括实现逻辑时钟、走查 Raft 状态机、30 分钟读懂数据库、设计混沌演练日）是该学科 recurring 模式。它们的具体实例会变（2030 年你 30 分钟读懂的数据库可能不是 CockroachDB 或 Spanner）...
  c30_b06.md: ...**Workflow（编号）**：第28章 §28.7 目录的 12 个绑定模式之一。每个工作流是某章核心操作技能的 recurring “输入 → 机制 → 输出” 框架（如工作流 4 = Raft 状态机走查；工作流 7 = Anya 的重分片迁移；工...

=== logical (2 occurrences) ===
  c30_b01.md: ..., Demirbas, M., Madappa, D., Avva, B., & Leone, M. (2014). "Logical Physical Clocks." _OPODIS_. — HLC 论文。 * Corbett, J. C., et...
  c30_b14.md: ...tor clocks (small N)     │   └── hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadca...

=== sigact (2 occurrences) ===
  c30_b01.md: ...nsistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D. (2012). "Consistency Trade...
  c30_b02.md: ...始 Paxos 论文。 * Lamport, L. (2001). "Paxos Made Simple." _ACM SIGACT News_. — 必读改写。 * Ongaro, D., & Ousterhout, J. (2014). "In S...

=== impossibility (2 occurrences) ===
  c30_b02.md: ...* Fischer, M. J., Lynch, N. A., & Paterson, M. S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal...
  c30_b06.md: ...*Fencing token（围栏令牌）**：锁服务颁发的单调递增计数器；被保护资源强制执行令牌顺序。 * **FLP impossibility（FLP 不可能性）**：Fischer-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不...

=== large (2 occurrences) ===
  c30_b02.md: ...ree)." _Acta Informatica_. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions...
  c30_b03.md: ...Zhuang, S., et al. (2023). "Efficient Memory Management for Large Language Model Serving with PagedAttention." _SOSP_. — vLLM...

=== processing (2 occurrences) ===
  c30_b02.md: ...Schneider, F. B. (1985). "Defining liveness." _Information Processing Letters_. — 安全性/活性分解。 * Lamport, L. (1998). "The Part-Time...
  c30_b02.md: ..._. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications." _OSDI_....

=== tolerance (2 occurrences) ===
  c30_b02.md: ...Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — PBFT。 * Yin, M., Malkhi, D., Reiter, M. K., Gue...
  c30_b03.md: ...Castro, M., & Liskov, B. (1999). "Practical Byzantine Fault Tolerance." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控制平面重读（§29.1）。 * NVIDIA Bla...

=== scale (2 occurrences) ===
  c30_b02.md: ..._Acta Informatica_. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and N...
  c30_b03.md: ...B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu,...

=== architecture (2 occurrences) ===
  c30_b03.md: ..., et al. (2024). "Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI 的 KV cache 层。 * LMCache (ht...
  c30_b06.md: ...el 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2026 年 AI 生成分布式代码生产纪律。第1层 = 自动 AI 审查（秒级）；第2层 = 人类...

=== language (2 occurrences) ===
  c30_b03.md: ..., S., et al. (2023). "Efficient Memory Management for Large Language Model Serving with PagedAttention." _SOSP_. — vLLM。 * Zheng...
  c30_b03.md: ..., et al. (2023). "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAttention。 * Patel, P., et al. (202...

=== rosenthal (2 occurrences) ===
  c30_b03.md: ...). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering: System Resilie...
  c30_b03.md: ...瞻话题引用（§29.1；深度覆盖推迟到 _Software at Scale_ Series IV Vol 4）。 * Rosenthal, C., & Jones, N. (2020). _Chaos Engineering_. O'Reilly. — 已...

=== transformers (2 occurrences) ===
  c30_b03.md: ..., Y., Kalman, M., & Matias, Y. (2023). "Fast Inference from Transformers via Speculative Decoding." _ICML_.   #### 综合与 AI 生成代码（第九部分...
  c30_b03.md: ...性有保留）。 * Fedus, W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Effic...

=== three (2 occurrences) ===
  c30_b06.md: ...LM 推理加速；小 draft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2026 年 AI 生成分布式代码生产纪律。第...
  c30_b14.md: ...mplementation (gRPC-style)     │   ├── circuit_breaker.py — three-state circuit breaker     │   └── load_shedder.py   — adapt...

=== workflow (2 occurrences) ===
  c30_b06.md: ...10。 * **TrueTime**：Spanner 的硬件辅助时间服务。返回 [最早, 最晚] 不确定区间。 * **Workflow（编号）**：第28章 §28.7 目录的 12 个绑定模式之一。每个工作流是某章核心操作技能的 recurring “...
  c30_b14.md: ...7 examples)             └── good_distributed_code.py — same workflow with defenses applied        #### 使用仓库  每章目录包含：  * 一个 `READ...

=== review (2 occurrences) ===
  c30_b06.md: ...* **AI/human review split (60/40)**：2026 年 AI 生成代码审查职责的粗略划分。AI 捕获约 60%（语法模式、习语不...
  c30_b06.md: ...aft model 提出 K 个 token，大 verify model 并行检查。 * **Three-layer review architecture（三层审查架构）**：2026 年 AI 生成分布式代码生产纪律。第1层 = 自动 AI 审查...

=== value (2 occurrences) ===
  c30_b06.md: ...-before，保持接近 wall time。 * **KV cache（KV 缓存）**：LLM 推理的内存 key-value 工作状态；让模型逐 token 解码的每请求内存。 * **Linearizable（线性化）**：每次操作看起来在调...
  c30_b14.md: ...s + commit     │   ├── kv_state_machine.py — replicated key-value store on top     │   └── tests/            — split-brain, p...

=== boltdb (2 occurrences) ===
  c30_b08.md: ...aming 失败导致队列积压；积压又带来更多 CPU 压力（灰色 + 亚稳态组合）。Consul gossip 协议的 BoltDB 状态在该故障模式下无边界增长。 * **时间线**：73 小时客户可见中断（Roblox 史上最大）；数百万用户受影响...
  c30_b08.md: ...x 史上最大）；数百万用户受影响。 * **恢复**：需要多厂商事件响应（Roblox + HashiCorp）；最终 BoltDB 需要从快照恢复，streaming 子系统从干净状态重建。 * **章节映射防御**：     * Ch.19（服务发...

=== crowdstrike (2 occurrences) ===
  c30_b08.md: ...码审查）：bug 模式未被 AI 辅助审查流水线捕获，因为循环条件依赖输入数据而非代码形状。   #### E.4 — CrowdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核...
  c30_b08.md: ...### E.4 — CrowdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取...

=== falcon (2 occurrences) ===
  c30_b08.md: ...被 AI 辅助审查流水线捕获，因为循环条件依赖输入数据而非代码形状。   #### E.4 — CrowdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sens...
  c30_b08.md: ...owdStrike Falcon Sensor 崩溃，2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏...

=== github (2 occurrences) ===
  c30_b09.md: ...#### E.5 — GitHub MySQL 24 小时降级，2018-10-21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分...
  c30_b09.md: ...#### E.5 — GitHub MySQL 24 小时降级，2018-10-21  * **触发**：GitHub 两个主数据中心之间 43 秒网络分区导致 MySQL Orchestrator failover 自动化在东海岸设施选...

=== store (2 occurrences) ===
  c30_b10.md: ...GCP）。按云承诺选择。 * **账本**（如适用）：TigerBeetle 用于复式记账；在 operational store 中引用 TigerBeetle 转账 ID。 * **Region 放置**：选择 3 个 region，使最坏情况...
  c30_b14.md: ...mmit     │   ├── kv_state_machine.py — replicated key-value store on top     │   └── tests/            — split-brain, partiti...

=== lies (2 occurrences) ===
  c30_b14.md: ...├── ai_review_checklist.py — automated checks for the six-lies patterns         └── examples/             ├── bad_distribu...
  c30_b14.md: ...├── bad_distributed_code.py  — code with all six lies (the §27.2-§27.7 examples)             └── good_distributed...

=== six (2 occurrences) ===
  c30_b14.md: ...├── ai_review_checklist.py — automated checks for the six-lies patterns         └── examples/             ├── bad_dis...
  c30_b14.md: ...s/             ├── bad_distributed_code.py  — code with all six lies (the §27.2-§27.7 examples)             └── good_distri...

=== madalena (1 occurrences) ===
  c01_b01.md: ...保罗时间），Crescent 在三个区域上线那晚，一位名叫 Adaeze Okafor 的客户——住在圣保罗 Vila Madalena 的尼日利亚软件工程师——用手机购买了一份 6 雷亚尔的数据包。她只付了一次款。手机显示一张收据。她的银行账单却显示两笔...

=== okafor (1 occurrences) ===
  c01_b01.md: ...凌晨 2:47（圣保罗时间），Crescent 在三个区域上线那晚，一位名叫 Adaeze Okafor 的客户——住在圣保罗 Vila Madalena 的尼日利亚软件工程师——用手机购买了一份 6 雷亚尔的数据包。她只付...

=== vila (1 occurrences) ===
  c01_b01.md: ...:47（圣保罗时间），Crescent 在三个区域上线那晚，一位名叫 Adaeze Okafor 的客户——住在圣保罗 Vila Madalena 的尼日利亚软件工程师——用手机购买了一份 6 雷亚尔的数据包。她只付了一次款。手机显示一张收据。她的...

=== designing (1 occurrences) ===
  c08_b10.md: ...、Consul、TiKV、CockroachDB、SpiceDB）用 Raft。Diego Ongaro 的博士论文 “Designing for Understandability” 直接针对 Paxos 的复杂性。对新团队来说，Raft 是正确默认。...

=== advances (1 occurrences) ===
  c01_b05.md: ...相邻，但构成独立学科。参考：Mohri 的 _Federated Learning_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区...

=== algorithmic (1 occurrences) ===
  c01_b05.md: ...Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Differential Privacy_（2014）。 * **量子网络协议** ：量...

=== aslett (1 occurrences) ===
  c01_b05.md: ...。参考：Kleppmann 的 _DDIA_ 第3-4章（基础）；RocksDB 调优指南（运维深度）；Pavlo & Aslett 的 CMU 15-721 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、Op...

=== bovet (1 occurrences) ===
  c01_b05.md: ...* ：Linux 内核调优、容器运行时内部、虚拟机监控器选择。这些是分布式系统运行的底层基板，但基板细节超出范围。参考：Bovet & Cesati _Understanding the Linux Kernel_（第3版）；Love _Linux...

=== brendan (1 occurrences) ===
  c01_b05.md: ...ing the Linux Kernel_（第3版）；Love _Linux System Programming_ ；Brendan Gregg 的 _Systems Performance_（第2版，2020）。 * **应用层协议深度** ：RES...

=== cmu (1 occurrences) ===
  c01_b05.md: ...mann 的 _DDIA_ 第3-4章（基础）；RocksDB 调优指南（运维深度）；Pavlo & Aslett 的 CMU 15-721 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务网格可观测性、OpenTele...

=== cesati (1 occurrences) ===
  c01_b05.md: ...内核调优、容器运行时内部、虚拟机监控器选择。这些是分布式系统运行的底层基板，但基板细节超出范围。参考：Bovet & Cesati _Understanding the Linux Kernel_（第3版）；Love _Linux System Pr...

=== cilium (1 occurrences) ===
  c01_b05.md: ...ridharan _Distributed Systems Observability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTelemetry Collector 文档。 * **联邦学习与隐私保护计算** ：...

=== differential (1 occurrences) ===
  c01_b05.md: ...OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Differential Privacy_（2014）。 * **量子网络协议** ：量子密钥分发、纠缠分发协议、量子-经典混合共识。截至 20...

=== elkouss (1 occurrences) ===
  c01_b05.md: ...混合共识。截至 2026 年，这些仍是研究课题，不是生产现实；本书读者在未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Sci...

=== federated (1 occurrences) ===
  c01_b05.md: ...ed Learning_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmi...

=== foundations (1 occurrences) ===
  c01_b05.md: ...earning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Differential Privacy_（2014）。 * **量子网络协议** ：量子密钥分发、纠缠分发协议...

=== gregg (1 occurrences) ===
  c01_b05.md: ...Linux Kernel_（第3版）；Love _Linux System Programming_ ；Brendan Gregg 的 _Systems Performance_（第2版，2020）。 * **应用层协议深度** ：REST API...

=== hanson (1 occurrences) ===
  c01_b05.md: ...2026 年，这些仍是研究课题，不是生产现实；本书读者在未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，...

=== hubble (1 occurrences) ===
  c01_b05.md: ..._Distributed Systems Observability_（O'Reilly，2018）；Cilium / Hubble 文档；OpenTelemetry Collector 文档。 * **联邦学习与隐私保护计算** ：差分隐私、安全多方...

=== kairouz (1 occurrences) ===
  c01_b05.md: ...026 年与分布式系统相邻，但构成独立学科。参考：Mohri 的 _Federated Learning_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；...

=== love (1 occurrences) ===
  c01_b05.md: ...出范围。参考：Bovet & Cesati _Understanding the Linux Kernel_（第3版）；Love _Linux System Programming_ ；Brendan Gregg 的 _Systems Perfor...

=== mohri (1 occurrences) ===
  c01_b05.md: ...** ：差分隐私、安全多方计算、分布式 ML 中的同态加密。它们在 2026 年与分布式系统相邻，但构成独立学科。参考：Mohri 的 _Federated Learning_（2023）；Kairouz 等 "Advances and Open P...

=== newman (1 occurrences) ===
  c01_b05.md: ...Is_ 中覆盖）、gRPC schema 演进、OpenAPI 工具链。第19章会提到服务发现；协议本身超出范围。参考：Newman _Building Microservices_（第2版，2021）；Richardson & Ruby _RESTf...

=== openmined (1 occurrences) ===
  c01_b05.md: ...等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Different...

=== pavlo (1 occurrences) ===
  c01_b05.md: ...会展开单节点深度。参考：Kleppmann 的 _DDIA_ 第3-4章（基础）；RocksDB 调优指南（运维深度）；Pavlo & Aslett 的 CMU 15-721 讲义（学术深度）。 * **网络可观测性深度** ：基于 eBPF 的服务...

=== performance (1 occurrences) ===
  c19_b05.md: ...） | | Operational tooling | etcdctl；云原生 | zkCli.sh；较老模式 | | Performance | 针对现代硬件优化 | 成熟但较老优化 |  etcd 替代 ZooKeeper 的原因：  (a) **Kuber...

=== privacy (1 occurrences) ===
  c16_b05.md: ...编辑；冲突自动解决。 (e) **Long-term archive** ：数据比提供服务的公司活得更久。 (f) **Privacy by default** ：数据不需要云账户。 (g) **Ownership** ：用户控制数据。  CRDT 是...

=== problems (1 occurrences) ===
  c01_b05.md: ...i 的 _Federated Learning_（2023）；Kairouz 等 "Advances and Open Problems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _T...

=== qed (1 occurrences) ===
  c01_b05.md: ...tum internet: A vision for the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告。 * **单系统管理** ：Linux 内核调优、容器运行时内部、虚拟机监控器选择。这些是分布式系统...

=== quantum (1 occurrences) ===
  c01_b05.md: ...些仍是研究课题，不是生产现实；本书读者在未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，2018）；QED...

=== richardson (1 occurrences) ===
  c01_b05.md: ...到服务发现；协议本身超出范围。参考：Newman _Building Microservices_（第2版，2021）；Richardson & Ruby _RESTful Web Services_ ；gRPC 与 OpenAPI 官方规范。  _保持范围_...

=== roth (1 occurrences) ===
  c01_b05.md: ...roblems in Federated Learning"（2021）；OpenMined 社区资料；Dwork & Roth _The Algorithmic Foundations of Differential Privacy_（2014）...

=== ruby (1 occurrences) ===
  c01_b05.md: ...围。参考：Newman _Building Microservices_（第2版，2021）；Richardson & Ruby _RESTful Web Services_ ；gRPC 与 OpenAPI 官方规范。  _保持范围_ 的纪律本身就...

=== sridharan (1 occurrences) ===
  c01_b05.md: ...涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Series IV Vol 3（可观测性）的主题。参考：Sridharan _Distributed Systems Observability_（O'Reilly，2018）；Cilium /...

=== wehner (1 occurrences) ===
  c01_b05.md: ...议、量子-经典混合共识。截至 2026 年，这些仍是研究课题，不是生产现实；本书读者在未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahe...

=== metric (1 occurrences) ===
  c01_b05.md: ...测性深度** ：基于 eBPF 的服务网格可观测性、OpenTelemetry collector 流水线调优、log/metric/trace 存储后端。第20章涵盖追踪能告诉你关于分布式系统行为的哪些内容；流水线机械机制是 Series IV Vo...

=== road (1 occurrences) ===
  c01_b05.md: ...考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告。 * **单系统管理** ：Linux 内核...

=== vision (1 occurrences) ===
  c01_b05.md: ...未来 5 年内不应需要它们。参考：Wehner、Elkouss、Hanson "Quantum internet: A vision for the road ahead"（_Science_ ，2018）；QED-C 行业联盟报告。 * **单系统管...

=== reviewer (1 occurrences) ===
  c01_b07.md: ...诊断方法）和第14章（NewSQL 矩阵）是最常被重新查阅的正文章节。  * **审查 AI 生成分布式代码的 PR  reviewer** ：先读第27章（六个谎言与检查清单），然后第18章（fencing token）、第21章（重试预算与断路器）、第...

=== enospc (1 occurrences) ===
  c01_b10.md: ...中的典型失败是“我让 X 做某事，但我不知道结果如何”。  **答案：** 单机失败是响亮且完整的。进程崩溃；磁盘返回 ENOSPC；函数抛出异常。你能看见它，因为失败是本地且可观测的。示例：`open("/tmp/file", "w")` 因磁盘满而...

=== double (1 occurrences) ===
  c01_b11.md: ...#### 应用问题  **Q1.** 你正在阅读一个支付服务 double-charge 客户的堆栈 trace。trace 显示两个 POST `/charge` 请求，带有相同幂等键，相隔...

=== gosling (1 occurrences) ===
  c02_b01.md: ...icrosystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 James Gosling 补充了第八条。这份清单经得起时间考验，因为它们不是关于具体技术的谬误；它们关乎单机直觉与分布式现实之间的差距。  八大...

=== james (1 occurrences) ===
  c02_b01.md: ...，Sun Microsystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 James Gosling 补充了第八条。这份清单经得起时间考验，因为它们不是关于具体技术的谬误；它们关乎单机直觉与分布式现实之间...

=== peter (1 occurrences) ===
  c02_b01.md: ...### 2.1 Deutsch 的分布式计算八大谬误  20 世纪 90 年代初，Sun Microsystems 的 Peter Deutsch 写下了新手进入分布式计算时常犯的错误假设清单。后来 James Gosling 补充了第八条。这份清单...

=== eric (1 occurrences) ===
  c02_b03.md: ...至少假设一条。大多数生产中的分布式系统 bug 就是八条之一，只是略有伪装。   ### 2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lyn...

=== podc (1 occurrences) ===
  c02_b03.md: ...ug 就是八条之一，只是略有伪装。   ### 2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 2002 年证明了它。口号的 C...

=== seth (1 occurrences) ===
  c02_b03.md: ...### 2.2 精确陈述的 CAP  Eric Brewer 在 2000 年 PODC 上把 CAP 猜想提了出来。Seth Gilbert 和 Nancy Lynch 在 2002 年证明了它。口号的 CAP 说“在一致性、可用性、分区容错性...

=== ish (1 occurrences) ===
  c02_b03.md: ...可用性之间做选择。其余时间，分区容错性不会让你付出任何代价。  其次，CAP 中的“一致性”不是数据库常说的“ACID-ish 行为”那个宽松词。它指的是 _线性一致性（linearizability）_ ，一种具体且非常强的性质。   ####...

=== daniel (1 occurrences) ===
  c02_b04.md: ...签暗示的更强；许多标为“CP”的系统以标签未命名的小巧方式交易可用性。   ### 2.3 PACELC：诚实的扩展  Daniel Abadi 在 2010 年左右指出，CAP 只命名了分区情况，忽略了更常见的情况：网络健康时。健康情况下的权衡在延迟...

=== except (1 occurrences) ===
  c02_b07.md: ...可靠的。_ 示例：`response = http.get(url); parse(response)`，没有 try/except、没有 timeout、没有重试。假设调用总是返回可解析响应。  _延迟为零。_ 示例：`for item in mil...

=== quietly (1 occurrences) ===
  c02_b07.md: ...#### 概念检测  **Q1.** 用你自己的话陈述 Deutsch 的八大谬误，并各举一个 quietly 假设该谬误为真的代码示例。  提示  为每个谬误找一个代码模式。对“网络是可靠的”，是没有重试的代码。对“延迟为零”，...

=== try (1 occurrences) ===
  c02_b07.md: ..._网络是可靠的。_ 示例：`response = http.get(url); parse(response)`，没有 try/except、没有 timeout、没有重试。假设调用总是返回可解析响应。  _延迟为零。_ 示例：`for item...

=== else (1 occurrences) ===
  c02_b09.md: ...PACELC 说：对这个操作，两者在分区下都是 PC。Else 情况下，条件写入路径 favors DynamoDB 的延迟，尤其当 Spanner 跨区域时。如果需要同一事务中做其...

=== favors (1 occurrences) ===
  c02_b09.md: ...PACELC 说：对这个操作，两者在分区下都是 PC。Else 情况下，条件写入路径 favors DynamoDB 的延迟，尤其当 Spanner 跨区域时。如果需要同一事务中做其他强一致读（如购物车与库存一起），S...

=== lua (1 occurrences) ===
  c02_b12.md: ...过期数据） | L（非常快） `SET NX EX`（锁） | PA（锁可能分裂） | L，但分区下不安全（第18章） Lua 脚本 | PC（单主节点原子） | 每个主节点 L，但局限于一个槽  PACELC 画像让每种操作在什么条件下安全、系...

=== shopify (1 occurrences) ===
  c02_b14.md: ...一个不回答这些问题就给出答案的模型，充其量是幸运。单区域 Shopify 风格商店的正确答案可能是 PostgreSQL + 读副本；Stripe 风格全球支付系统的正确答案可能是 Spann...

=== stripe (1 occurrences) ===
  c02_b14.md: ...问题就给出答案的模型，充其量是幸运。单区域 Shopify 风格商店的正确答案可能是 PostgreSQL + 读副本；Stripe 风格全球支付系统的正确答案可能是 Spanner 或 TigerBeetle。正确的推理路径要经过这些问题。没有推理路...

=== lan (1 occurrences) ===
  c03_b01.md: ...是让机器时钟大致同步的机制。NTP 通过查询少量时间服务器、测量往返延迟，并让本地时钟缓缓向共识靠近来工作。  在健康 LAN 且附近有 Stratum-2 NTP 服务器时，机器同步到几毫秒以内。在广域网或嘈杂链路上，上界接近几十毫秒。在 NT...

=== stratum (1 occurrences) ===
  c03_b01.md: ...步的机制。NTP 通过查询少量时间服务器、测量往返延迟，并让本地时钟缓缓向共识靠近来工作。  在健康 LAN 且附近有 Stratum-2 NTP 服务器时，机器同步到几毫秒以内。在广域网或嘈杂链路上，上界接近几十毫秒。在 NTP 守护进程静默失败的机器...

=== fidge (1 occurrences) ===
  c03_b03.md: ...2, 3, 4, 5)。   ### 3.4 向量时钟  Lamport 时间戳保留顺序但失去检测并发能力。向量时钟由 Fidge 和 Mattern 在 1988 年独立提出，以更多空间为代价修复这一点。  每个进程 p 维护一个向量 V_p[1....

=== mattern (1 occurrences) ===
  c03_b03.md: ...5)。   ### 3.4 向量时钟  Lamport 时间戳保留顺序但失去检测并发能力。向量时钟由 Fidge 和 Mattern 在 1988 年独立提出，以更多空间为代价修复这一点。  每个进程 p 维护一个向量 V_p[1..n]（每个进程一个...

=== timesync (1 occurrences) ===
  c03_b07.md: ...Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业等价物。  §3.5 描述的...

=== crisp (1 occurrences) ===
  c03_b07.md: ...厂商提供 Amazon Time Sync Service（带 GPS 支撑的时钟），但不确定度更宽，API 也不那么 crisp。AWS 的 TimeSync clock-bound（为 clock-bound-aware 应用引入）是最接近的商业...

=== what (1 occurrences) ===
  c03_b08.md: ...中的排序  现代分布式数据库对事务排序使用三种模式之一：  System | Ordering primitive | What it costs ---|---|--- Spanner | TrueTime + commit-wait | 每次写...

=== costs (1 occurrences) ===
  c03_b08.md: ...分布式数据库对事务排序使用三种模式之一：  System | Ordering primitive | What it costs ---|---|--- Spanner | TrueTime + commit-wait | 每次写事务 7 ms +...

=== primitive (1 occurrences) ===
  c03_b08.md: ....7 综合：分布式数据库中的排序  现代分布式数据库对事务排序使用三种模式之一：  System | Ordering primitive | What it costs ---|---|--- Spanner | TrueTime + commit-wai...

=== restarts (1 occurrences) ===
  c03_b12.md: ...付，其他不付。 * 在 CockroachDB 中，对最近写入键有高竞争的工作负载可能遭受大量 uncertainty restarts。500 ms 上界是最坏情况；收紧它（通过严格 NTP 纪律）会成比例减少重启。 * Spanner 跨区域部署仍可用...

=== most (1 occurrences) ===
  c04_b01.md: ...lient.send()` 要么成功且消息到达，要么失败且调用者知道。在 TCP 术语中，若调用者不重试，这是 _at-most-once_ ；若重试，则是 _at-least-once_ 。应用想要的是 _所有副本按相同顺序全有或全无_ 。这就是...

=== duplication (1 occurrences) ===
  c04_b02.md: ...正确且接收者正确，接收者最终交付消息。 * **No-creation** ：未被发送的消息不会被交付。 * **No-duplication** ：每条消息至多交付一次。  缺失什么：关于顺序、失败下交付、或接收者之间一致的任何保证。   ### 4.3 可靠...

=== acks (1 occurrences) ===
  c04_b03.md: ...(peer, ACK(msg))      on receive(ACK(msg), sender):         acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg no...

=== len (1 occurrences) ===
  c04_b03.md: ...ACK(msg), sender):         acks[msg].add(sender)         if len(acks[msg]) > N/2 and msg not in delivered:             deli...

=== reason (1 occurrences) ===
  c04_b05.md: ...是满足应用正确性要求的最弱层，因为每一层更强层都更贵。  Application | Required layer | Reason ---|---|--- DNS 更新传播 | best-effort | 丢失一个可接受；通过 TTL 最终收敛 缓存...

=== rangefeed (1 occurrences) ===
  c04_b06.md: ...** 在 stream 内提供有序交付；消费者组提供 at-least-once。 * **CockroachDB 的 Rangefeed** 提供带 closed-timestamp 保证的因果有序变更事件。  选择系统时正确的问题不是“它提供哪一层广播？...

=== bookkeeping (1 occurrences) ===
  c04_b10.md: ...近未贡献消息），只存非零条目。Riak 使用过类似技术。成本是检测条目何时可丢弃（该进程“跟上”到足以让其条目隐含）的 bookkeeping。当贡献呈突发且按进程聚类时效果良好。  (b) **分层 / 分组因果广播** 。把进程分组（例如按区域或租户）。每条...

=== contributors (1 occurrences) ===
  c04_b10.md: ...选择：对某些应用用 _causal+ consistency_ 加 per-key 版本向量替代因果广播。每个键有一个 contributors 向量；向量随键的写作者数增长，而非系统大小。这是 CRDT 模式；广播层退化为“可靠”，因果性由数据类型承载。Yjs...

=== entity (1 occurrences) ===
  c04_b10.md: ...法。  对大多数 2026 系统，正确的问题是：应用是否真的需要 _所有_ 消息的因果广播，还是因果可以限定在 per-entity 或 per-room 操作？前者很少成立；后者通常够用。...

=== divergence (1 occurrences) ===
  c05_b01.md: ...：把读分散到副本，成倍提升读吞吐。  复制的成本有两部分：写入延迟增加（必须把写传播到副本后才认为持久）和读者可见的 Divergence（从过期副本读可能看不到最新写入）。  不同策略的权衡不同。同步复制最大化持久性和故障后一致性，代价是写延迟。异步复制最...

=== chargeback (1 occurrences) ===
  c05_b01.md: ...。”  下周，客服收到第一个投诉：一位客户存了款，刷新页面看到旧余额。十秒后再刷新看到新余额。客户以为存款失败，发起了 chargeback。  这就是 read-after-write 失败，它是复制带来的三类失败之一。另外两类是 _split brain_...

=== scalability (1 occurrences) ===
  c05_b01.md: ...，数据不会丢失。 * **Availability** ：一台机器不可达，系统仍能从另一台服务请求。 * **Read scalability** ：把读分散到副本，成倍提升读吞吐。  复制的成本有两部分：写入延迟增加（必须把写传播到副本后才认为持久）和读者可见...

=== death (1 occurrences) ===
  c05_b02.md: ...：leader 将写串行化。所有副本按相同顺序应用。 * **Failover required on leader death** ：必须选出新 leader 才能恢复写。选举通常需要数秒。 * **Read-after-write consis...

=== flush (1 occurrences) ===
  c05_b02.md: ...MySQL 默认复制是异步。PostgreSQL 默认 `synchronous_commit = on` 是本地磁盘 flush 后的异步；跨机器同步需要 `remote_apply` 或 `remote_write`。   ### 5.4 半同步...

=== trivially (1 occurrences) ===
  c05_b02.md: ...举通常需要数秒。 * **Read-after-write consistency on the leader** ： trivially 满足。 * **Read-after-write on followers** ：不保证；取决于复制延迟。...

=== concurrent (1 occurrences) ===
  c05_b03.md: ...个节点可能只看到写者集合中少于 W 的节点（写者处于提交中途）。根据读修复逻辑，读可能返回旧值、混合值或新值。 * **Concurrent writes** ：两个写者可在相交集合上各自满足 W 要求，但写冲突值。冲突解决（LWW、sibling、CRDT）...

=== configuration (1 occurrences) ===
  c05_b05.md: ...：按操作的 RPO 需求选择复制模式，而非按系统默认。  System | Default replication | Configuration options ---|---|--- PostgreSQL | Async（一个或多个副本） | `synchron...

=== options (1 occurrences) ===
  c05_b05.md: ...复制模式，而非按系统默认。  System | Default replication | Configuration options ---|---|--- PostgreSQL | Async（一个或多个副本） | `synchronous_comm...

=== secondaries (1 occurrences) ===
  c05_b05.md: ...nc | `rpl_semi_sync` 插件实现半同步 MongoDB（replica set）| Async to secondaries | `writeConcern: majority` 实现基于法定人数 CockroachDB | Raft-repl...

=== handoff (1 occurrences) ===
  c05_b06.md: ...序要么都看见写要么都不见。  (b) **Sloppy quorum** ：主副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本（从未收到写）而非持有 hint 的副本。相交被破坏。  (c) **并发冲突写...

=== hint (1 occurrences) ===
  c05_b06.md: ...副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本（从未收到写）而非持有 hint 的副本。相交被破坏。  (c) **并发冲突写** ：两个写者在不同但相交集合上各自满足 W。读看到两个写；冲突解决策...

=== hinted (1 occurrences) ===
  c05_b06.md: ...有读者按实时顺序要么都看见写要么都不见。  (b) **Sloppy quorum** ：主副本不可达时，Dynamo hinted handoff 把写发到非主副本。下一次读可能去主副本（从未收到写）而非持有 hint 的副本。相交被破坏。  (c)...

=== sets (1 occurrences) ===
  c05_b06.md: ...ollower。故障切换需要选出新 leader，耗时数秒。示例：PostgreSQL、MongoDB replica sets、CockroachDB（per-range）、Spanner（per-tablet）。  Leaderless：任意节...

=== opportunistically (1 occurrences) ===
  c05_b08.md: ...不在写者集合）和写者集合中一个离线的节点，读可能看不到写。  (b) Cassandra 的 “QUORUM” 协调者 opportunistically 选择节点。读协调者可能选择与写协调者不同的节点集合。即使两个 quorum 都有效，具体重叠可能不包括持有最新值的节点...

=== elapsed (1 occurrences) ===
  c05_b09.md: ...# 等待复制追上，或超时后拒绝。             sleep(1ms)             if elapsed > timeout:                 raise StaleReplicaError...

=== while (1 occurrences) ===
  c05_b09.md: ...# 服务器端读处理器：     def handle_read(key, min_hlc):         while server.local_hlc < min_hlc:             # 等待复制追上，或超时后拒绝。...

=== outperform (1 occurrences) ===
  c05_b10.md: ...ge/shard 有 _写吞吐上限_ ，由 Raft 往返成本决定。对每键极端写吞吐的工作负载，异步复制或批量方法可能 outperform Raft。权衡是持久性 vs 吞吐；两者都是真实成本。  **Q2.** 一个 LLM 被要求实现“高可用”复制，写出...

=== distance (1 occurrences) ===
  c06_b01.md: ...缆要绕行障碍物，洲际路线的有效速度更接近 150,000 km/s。  单程延迟下限示例（往返翻倍）：  Path | Distance (km) | Floor (ms) ---|---|--- 同一机架 | 0.001 | < 0.001 同一数据中心...

=== floor (1 occurrences) ===
  c06_b01.md: ...度更接近 150,000 km/s。  单程延迟下限示例（往返翻倍）：  Path | Distance (km) | Floor (ms) ---|---|--- 同一机架 | 0.001 | < 0.001 同一数据中心 | 1 | 0.01 同...

=== metro (1 occurrences) ===
  c06_b01.md: ...ms) ---|---|--- 同一机架 | 0.001 | < 0.001 同一数据中心 | 1 | 0.01 同一 metro 区域（如 US-East 到 US-East-2） | 100 | 0.7 跨大陆美国（NY 到 LA） | 4,00...

=== times (1 occurrences) ===
  c06_b01.md: ...5 ms 算健康；p50 75 ms 不可能。  含义：任何需要纽约到新加坡之间 k 次往返的分布式协议至少要 $k \times 150$ ms。Spanner 提交需要一次 TrueTime 等待加一次 Paxos 往返，所以 7 + 150 =...

=== gdpr (1 occurrences) ===
  c06_b02.md: ...0-30 ms。对延迟敏感工作负载（游戏、实时协作、语音），差别就是产品成败。  (c) **监管数据驻留。** 欧盟 GDPR、中国 PIPL、俄罗斯数据本地化法律等要求某些用户数据留在特定司法管辖区。按区域 per-region 存储的区域用户...

=== pipl (1 occurrences) ===
  c06_b02.md: ...对延迟敏感工作负载（游戏、实时协作、语音），差别就是产品成败。  (c) **监管数据驻留。** 欧盟 GDPR、中国 PIPL、俄罗斯数据本地化法律等要求某些用户数据留在特定司法管辖区。按区域 per-region 存储的区域用户架构由此而来。...

=== passive (1 occurrences) ===
  c06_b02.md: ...监控栈。   ### 6.3 三种多区域模式  选择由你的数据需求决定。   #### 模式 1：单区域 active-passive  所有写发往一个区域（active）。其他区域是异步复制的 warm standby。读可去任何区域（可能过期）。...

=== warm (1 occurrences) ===
  c06_b02.md: ...#### 模式 1：单区域 active-passive  所有写发往一个区域（active）。其他区域是异步复制的 warm standby。读可去任何区域（可能过期）。  * **Latency** ：写只付区域内成本；读可本地。 * **F...

=== direct (1 occurrences) ===
  c06_b03.md: ...机架与同机架 RPC 延迟相近（差几微秒）。网络局部性不如应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。数据中心两种实...

=== equal (1 occurrences) ===
  c06_b03.md: ...**无超额订阅**（理想版）：leaf 到 spine 的总上行带宽与 leaf 到服务器的总下行带宽匹配。 * **Equal-cost multi-path（ECMP）** ：一台服务器到另一台服务器的流量可走多条 spine 路径之一，网络在...

=== remote (1 occurrences) ===
  c06_b03.md: ...常是。 * 跨机架与同机架 RPC 延迟相近（差几微秒）。网络局部性不如应用设计重要。   ### 6.5 RDMA  Remote Direct Memory Access 让一台服务器的 NIC 直接写入另一台服务器的内存，无需目标 CPU 参与。...

=== servers (1 occurrences) ===
  c06_b03.md: ...少量高带宽交换机。 * **Leaf switches** ：许多低成本交换机，每个都连接到每个 spine。 * **Servers** ：连接到 leaf；每台服务器有多个 leaf 以提供冗余。  性质：  * **Any-to-any bandw...

=== iwarp (1 occurrences) ===
  c06_b03.md: ...* ：2026 年最常见；跑在以太网上，用 PFC（Priority Flow Control）保证无损交付。 * **iWARP** ：基于 TCP 的 RDMA；较少见，但因容忍有损网络而更容易部署。  性能：RDMA 延迟约 1-2 微秒（同数...

=== correctness (1 occurrences) ===
  c06_b04.md: ...为多区域高可用’，但期望正常运营的读写都在本地区域。”言下之意是多区域税在正常运营不付。这只在多区域仅异步时成立；一旦 correctness 要求法定人数跨区域（Raft、Paxos、Spanner），每次写都付跨区成本。  “多区域为 DR、单区域 acti...

=== origin (1 occurrences) ===
  c06_b04.md: ...流 1-2 秒。   2. **减去固定成本。** TLS 握手（首次连接 10-100 ms）、CDN edge 到 origin（5-50 ms）、认证（5-20 ms）。   3. **把剩余预算分配给应用跳数。** 每个下游服务分一块。   4...

=== zstd (1 occurrences) ===
  c06_b05.md: ...* 大多数跨区域复制是 change-data-capture，不是周期全量快照。 * **线上压缩。** 现代系统用 Zstd 等；对典型结构化数据可省 50-90% 字节。 * **避免跨区域话痨协议。** 一个协议发 100 条小消息而不是...

=== capture (1 occurrences) ===
  c06_b05.md: ...统，年费六位数。  架构含义：  * **复制 delta，而非全量。** 大多数跨区域复制是 change-data-capture，不是周期全量快照。 * **线上压缩。** 现代系统用 Zstd 等；对典型结构化数据可省 50-90% 字节。 *...

=== delta (1 occurrences) ===
  c06_b05.md: .../天，或 300-2,700 USD/月。对每天复制 100 TB 的系统，年费六位数。  架构含义：  * **复制 delta，而非全量。** 大多数跨区域复制是 change-data-capture，不是周期全量快照。 * **线上压缩。**...

=== ecmp (1 occurrences) ===
  c06_b06.md: ...交换机连接服务器，每个 leaf 都连到每个 spine 交换机。任意两台服务器间流量只经过一个 spine hop，ECMP 跨 spine 路径做负载均衡。它取代三层（core、aggregation、access），因为三层的 aggreg...

=== channels (1 occurrences) ===
  c06_b08.md: ...的心理模型）。Spanner 过度；中央 DB 让用户体验不对称。  这正是 Slack 的模式：per-region channels，跨区复制做 federation。  **Q2.** 一个团队为 Raft 复制数据库选三个区域，希望最小化最坏情况...

=== federation (1 occurrences) ===
  c06_b08.md: ...过度；中央 DB 让用户体验不对称。  这正是 Slack 的模式：per-region channels，跨区复制做 federation。  **Q2.** 一个团队为 Raft 复制数据库选三个区域，希望最小化最坏情况 Raft quorum 延迟。给定...

=== asia (1 occurrences) ===
  c06_b09.md: ...West 发起）。延迟极好；但灾难恢复差（全在美国大陆，单一国家暴露）。  分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Toky...

=== pacific (1 occurrences) ===
  c06_b09.md: ...发起）。延迟极好；但灾难恢复差（全在美国大陆，单一国家暴露）。  分散三区域：US-East、EU-West、Asia-Pacific（Tokyo）。成对往返：US-East ↔ EU-West ~90 ms；US-East ↔ Tokyo ~200 m...

=== javascript (1 occurrences) ===
  c06_b11.md: ...UI 加后端调和）。每个都是真实工程胜利，但都有局限。  更深观察：许多用户对“慢应用”的抱怨根本不在网络延迟。而是 JavaScript 解析时间、阻塞渲染资源或后端处理时间。网络通常比工程师假设的占比小。端到端剖析用户感知延迟通常能在网外找到更大收益。...

=== ben (1 occurrences) ===
  c07_b02.md: ...uff 等现代共识协议假设部分同步。  (b) **随机化** ：协议使用随机数生成器，使对手无法可靠构造无限延迟调度。Ben-Or 随机化共识是经典例子。实践意义有限；多数生产系统用部分同步。  (c) **故障检测器** ：假设有一个神谕告诉...

=== omega (1 occurrences) ===
  c07_b02.md: ...你哪些进程已故障。协议用神谕裁决推进。Chandra-Toueg 的故障检测器层次形式化；能解共识的最弱故障检测器叫 \Omega（最终某个正确进程被标识为 leader）。  三种逃逸在理论上对许多实践目的等价。现代协议通常用部分同步加隐式故障检测...

=== fairness (1 occurrences) ===
  c07_b03.md: ...veness 性质：每个正确进程最终决定。 * Progress 是 liveness 性质：每个请求最终被服务。 * Fairness 是 liveness 性质：每个竞争请求最终获得机会。  Liveness 性质被 _无限_ 执行违反：好事必须永不发...

=== prevote (1 occurrences) ===
  c07_b05.md: ...做 validity 检查 | 超时后 view change；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreem...

=== propose (1 occurrences) ===
  c07_b05.md: ...ange；新 leader Tendermint | 2/3+1 prevote 和 precommit | 值必须经 propose 阶段往返 | 基于轮次 + 指数退避  四个协议中 agreement 都由法定人数相交提供。validity 是结构...

=== end (1 occurrences) ===
  c07_b06.md: ...提示  Safety：“坏事永不发生。”Liveness：“好事最终发生。”证明技术不同；运维后果不同。 =====END=====...

=== john (1 occurrences) ===
  c07_b11.md: ...策序列，这是生产部署（Spanner、Megastore）的真实形式。第9章讲 Raft，Diego Ongaro 和 John Ousterhout 2014 年明确为比 Paxos 更易理解而设计。  ---...

=== acm (1 occurrences) ===
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Par...

=== computer (1 occurrences) ===
  c08_b01.md: ...1989 年，Leslie Lamport 向 ACM Transactions on Computer Systems 提交了一篇题为 “The Part-Time Parliament” 的论文。它描述了一座虚构希腊岛屿...

=== livelock (1 occurrences) ===
  c08_b03.md: ...怀疑当前 leader 死亡（心跳超时），它启动新 ballot。多个进程同时启动 ballot 会相互干扰，协议可能 livelock，直到某个 ballot “获胜”（先拿到第一阶段多数）。  随机化超时模式（Raft 使用）通过随机化心跳超时，使同时...

=== cheap (1 occurrences) ===
  c08_b04.md: ...* **Generalized Paxos** ：允许并发非冲突提议被排序。对可交换操作的工作负载吞吐更高。 * **Cheap Paxos** ：把 acceptor 分为“活跃”的（积极参与协议）和“辅助”的（只在故障时咨询）。降低稳态成本。...

=== egalitarian (1 occurrences) ===
  c08_b04.md: ...os** ：把 acceptor 分为“活跃”的（积极参与协议）和“辅助”的（只在故障时咨询）。降低稳态成本。 * **Egalitarian Paxos（EPaxos）** ：无 leader；每个副本都可提议，冲突按提议解决。吞吐更高；推理更复杂。 * **...

=== generalized (1 occurrences) ===
  c08_b04.md: ...** ：常见情况下跳过第一阶段；使用更大的法定人数（3/4 而非过半）。无竞争时更快；竞争下可能更慢或不安全。 * **Generalized Paxos** ：允许并发非冲突提议被排序。对可交换操作的工作负载吞吐更高。 * **Cheap Paxos** ：把...

=== reconfiguration (1 occurrences) ===
  c08_b04.md: ...}$、`accepted_ballot`、`accepted_value`。丢失这些状态会破坏 safety。 * **Reconfiguration** ：增减 acceptor 集合本身必须通过 Paxos 完成，以避免两个不同配置各自形成无重叠的“多数”。 * *...

=== snapshotting (1 occurrences) ===
  c08_b04.md: ...* ：增减 acceptor 集合本身必须通过 Paxos 完成，以避免两个不同配置各自形成无重叠的“多数”。 * **Snapshotting** ：日志无限增长；生产系统必须给状态机快照并截断日志。快照必须与日志该点状态一致。 * **Stable stora...

=== stable (1 occurrences) ===
  c08_b04.md: ...Snapshotting** ：日志无限增长；生产系统必须给状态机快照并截断日志。快照必须与日志该点状态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor...

=== discipline (1 occurrences) ===
  c08_b04.md: ...：日志无限增长；生产系统必须给状态机快照并截断日志。快照必须与日志该点状态一致。 * **Stable storage discipline** ：每次 accept 后 fsync；若跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。...

=== handling (1 occurrences) ===
  c08_b04.md: ...跳过 fsync，acceptor 可能丢失已接受值并破坏 safety。 * **Network partition handling** ：若 leader 与多数分区隔离，少数分区不能进展。Leader 在失去多数联系时必须下台。 * **Multi...

=== libpaxos (1 occurrences) ===
  c08_b04.md: ...Megastore 论文（2011）和 Spanner 论文（2012）描述了生产级 Paxos 的样子；细节令人清醒。libpaxos 等实现存在但未被广泛使用；多数团队要么使用更高层系统（Spanner、etcd），要么实现 Raft。  民间智慧：“...

=== smr (1 occurrences) ===
  c08_b06.md: ...Multi-Paxos？它如何比单法令 Paxos 更适合状态机复制？  提示  单法令 Paxos 就一个值达成一致；SMR 需要序列。Multi-Paxos 把第一阶段在多决定上摊销。  **答案：** Multi-Paxos 扩展单法令 P...

=== alpha (1 occurrences) ===
  c08_b07.md: ...（增删节点）不能随意，因为两个不同配置可能各自形成无重叠的“多数”。标准解是 “joint consensus” 或 “alpha-paxos” 模式，但正确实现微妙。  (c) **快照与日志截断** ：Paxos 日志无限增长；生产系统必须给状态...

=== resurfacing (1 occurrences) ===
  c08_b07.md: ...lease 实现微妙）。  (f) **故障期间成员变更** ：分区期间移除故障节点危险；分区愈合后“被移除”节点可能 resurfacing，导致配置不一致。  累积效应：Paxos 实现通常是数千行谨慎代码。上述任何 bug 都可能导致数月后才显现的静默 s...

=== understandability (1 occurrences) ===
  c08_b10.md: ...ockroachDB、SpiceDB）用 Raft。Diego Ongaro 的博士论文 “Designing for Understandability” 直接针对 Paxos 的复杂性。对新团队来说，Raft 是正确默认。  反驳：Paxos 有 Raft 没有的属性，...

=== build (1 occurrences) ===
  c08_b11.md: ...1998 年论文、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 R...

=== highly (1 occurrences) ===
  c08_b11.md: ...、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus” 以获取运维视角。  第9章讲 Raft，Ongar...

=== lampson (1 occurrences) ===
  c08_b11.md: ...实现。附录 A 的阅读列表包括原始 1998 年论文、2001 年 “Paxos Made Simple” 改写，以及 Lampson 的 “How to Build a Highly Available System Using Consensus”...

=== framing (1 occurrences) ===
  c08_b11.md: ...慎协调。LLM 生成的代码通常根本不实现重配置，或以允许 split-brain 的天真方式实现。  (f) **网络 framing。** Paxos 消息必须可靠地匹配到正确的 proposer/ballot/acceptor。LLM 代码常用简单...

=== lead (1 occurrences) ===
  c09_b01.md: ...ore 论文。她试图在周末实现单法令 Paxos。周一早上她说：“我脑子里装不下这些。一定有更好的办法。”  她的技术 lead 把 Raft 论文递给她。  Raft 由 Ongaro 和 Ousterhout 设计，2014 年论文题为 “In...

=== rethinkdb (1 occurrences) ===
  c09_b05.md: ...iKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxos 仍在某些细分领域获胜：Google 基础设施（Chubby、Spanner...

=== spicedb (1 occurrences) ===
  c09_b05.md: ...cd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxos 仍在某些细分领域获胜：G...

=== vault (1 occurrences) ===
  c09_b05.md: ...户包括：etcd、Consul、CockroachDB、TiKV、MongoDB（自 3.6 起）、HashiCorp Vault、SpiceDB、NATS JetStream、RethinkDB（历史）以及许多内部公司系统。  Paxos 仍在某些...

=== members (1 occurrences) ===
  c09_b05.md: ...tcd 的 Raft 实现（Go）被广泛使用；多种语言实现存在。 * **运维工具** ：etcdctl、consul members、CockroachDB 的 raft visualizer。协议暴露足够状态供观察。 * **社区知识** ：一代工程...

=== visualizer (1 occurrences) ===
  c09_b05.md: ...实现存在。 * **运维工具** ：etcdctl、consul members、CockroachDB 的 raft visualizer。协议暴露足够状态供观察。 * **社区知识** ：一代工程师把 Raft 当作第一个共识协议学习；知识库广阔。  生产...

=== hang (1 occurrences) ===
  c09_b10.md: ...` 被拒绝时，leader 必须递减 `nextIndex` 并重试。LLM 实现有时不减，导致无限循环重试，看起来像 hang。  (f) **快照与应用竞争。** 快照捕获某时刻状态机状态；并发 apply 不能在快照期间修改状态。LLM 实现...

=== elections (1 occurrences) ===
  c09_b11.md: ...解为三个正交子问题（leader election、log replication、safety），并基于 terms、elections 和 AppendEntries 一致性检查提供清晰的运维模型。  Anya 为 Stratus DB 选择 raft-...

=== evolution (1 occurrences) ===
  c10_b01.md: ..."]         HotStuff --> Mysticeti["Mysticeti 2024<br/>Sui's evolution"]         HotStuff --> Narwhal["Narwhal-Bullshark<br/>Danez...

=== simpler (1 occurrences) ===
  c10_b01.md: ...PBFT --> Tendermint["Tendermint<br/>Buchman 2016<br/>simpler workhorse"]         PBFT --> HotStuff["HotStuff<br/>Yin et...

=== prohibitive (1 occurrences) ===
  c10_b02.md: ...)$ 消息。$N=4$（容忍 1 个拜占庭）时每决定 16 条消息，可管理。$N=100$ 时 10,000 条消息， prohibitive。  PBFT 二十年间是经典 BFT 协议，但因二次消息复杂度很少大规模部署。2018-2020 年区块链研究浪潮产生...

=== fork (1 occurrences) ===
  c10_b04.md: ...的 lock-and-release 机制（验证者在某些条件下可解锁并切换值）微妙，历史上几个 Tendermint fork 因此产生安全 bug。协议正确性已被证明；新实现应使用经过验证的库而不是重新实现。...

=== grid (1 occurrences) ===
  c10_b05.md: ...立。  示例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rceil + 1$。两者都是多数。 * Grid quorums：$Q_1$ = 一行，$Q_2$ = 一列。$N$ 是方阵；两个法定人数都是 $\sqrt{N}$。节...

=== quorums (1 occurrences) ===
  c10_b05.md: ...例：  * 经典：$Q_1 = Q_2 = \lceil N/2 \rceil + 1$。两者都是多数。 * Grid quorums：$Q_1$ = 一行，$Q_2$ = 一列。$N$ 是方阵；两个法定人数都是 $\sqrt{N}$。节省带宽，但故障时...

=== sqrt (1 occurrences) ===
  c10_b05.md: ...多数。 * Grid quorums：$Q_1$ = 一行，$Q_2$ = 一列。$N$ 是方阵；两个法定人数都是 $\sqrt{N}$。节省带宽，但故障时延迟更高。 * 更小的 accept 法定人数：$Q_1$ 更大（很少跑），$Q_2$ 更小...

=== ethereum (1 occurrences) ===
  c10_b06.md: ...exiRaft）已融入 BFT 启发的优化。  (c) **BFT 是任何涉及区块链系统的运维现实。** 无论你在集成 Ethereum、构建 Cosmos 链，还是评估 Aptos 产品，BFT 模型决定了底层系统能做什么。  2026 年的分布式系统工...

=== ibc (1 occurrences) ===
  c10_b09.md: ...* 成熟库和生态。 * 对该用例吞吐足够。 * 运维工具和可观测性发展良好。 * 若该链未来需要互操作，Cosmos IBC 是加分项。  HotStuff 技术上“最佳”，但实现负担更重；对 21 节点许可链，Tendermint 的简单性占...

=== cdot (1 occurrences) ===
  c10_b09.md: ...$。分布于 4 个 DC（其中一个可能被攻破），每个 DC 贡献 3 个节点；总共 12。$N = 12 \ge 3 \cdot 3 + 1 = 10$，满足。  运维影响：  (a) **4 个 DC** ：系统必须跨至少 4 个 DC 才能容忍...

=== response (1 occurrences) ===
  c10_b10.md: ...运维复杂度** ：4 个 DC 的 12 个验证者意味着 12 台服务器运维、4 个网络管理、4 个 incident response 团队协调。运维面是单区域 Raft 设置的 3-4 倍。  权衡：“一个 DC 沦陷”是否现实威胁？对大型云厂商控制面...

=== snowden (1 occurrences) ===
  c10_b11.md: ...tuff 风格 BFT，因为威胁模型包含被攻破的云厂商。”  **讨论：** 辩护：云厂商被攻破是真实且日益增长的威胁。Snowden 披露、SolarWinds 攻击以及各国行为体都表明“云可信”对高价值数据不是安全假设。BFT 正是针对这种威胁模型。...

=== high (1 occurrences) ===
  c11_b01.md: ...1：复制协议  阅读系统文档中的 “replication”、“consensus”、“consistency” 或 “high availability”。寻找：  * **Single-leader** ：PostgreSQL、MySQL、Mo...

=== baked (1 occurrences) ===
  c11_b03.md: ...ths | 无需重启成本即可获全局线性化；运维故事成熟；多区域 Weaknesses | 仅限 GCP；昂贵；每次写都 baked in commit-wait 延迟 Right for | 需要全局线性化的工作负载（广告、金融账本）；Google...

=== pricing (1 occurrences) ===
  c11_b04.md: .../ EC Strengths | 免运维；延迟可预测；与 AWS 生态集成 Weaknesses | 仅限 AWS； pricing 惩罚热键；数据建模约束 Right for | 访问模式可预测的单租户键值工作负载；AWS 原生系统   #### C...

=== sharded (1 occurrences) ===
  c11_b08.md: ...MongoDB 的一致性取决于配置：writeConcern、readConcern、replica set vs sharded。默认与最强可用不同。  **答案：** MongoDB 的一致性高度依赖配置。精确答案必须说明哪种操作、哪种配置。...

=== strict (1 occurrences) ===
  c11_b08.md: ...ange；面向确定性模拟测试（每个可达状态都被显式枚举和演练） Consistency model | 复式记账事务的 strict serializability；每次转账 linearizable Partition behavior | CP——...

=== complexity (1 occurrences) ===
  c11_b09.md: ...throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity | 高（compaction 调优、GC 调优） | 中等（架构已处理大部分） Ecosystem | 更大（项目更老...

=== offering (1 occurrences) ===
  c11_b09.md: ...Ecosystem | 更大（项目更老） | 成长中；Cassandra 兼容 API Cost | 开源；众多云托管 offering | 开源 + ScyllaDB Cloud  对分析工作负载（通常高吞吐、可容忍最终一致）的综合：  若 (a) 每节...

=== throughput (1 occurrences) ===
  c11_b09.md: ...r-core） Memory profile | 受堆限制（GC 停顿） | 无堆（无 GC 停顿） Per-node throughput | ~50,000 ops/sec | ~500,000 ops/sec Operational complexity...

=== columns (1 occurrences) ===
  c12_b01.md: ...ard key 哈希分片。Cassandra 是哈希分片，但提供 partition key + clustering columns，使同一 partition 内数据有序（在哈希 partition 内实现局部范围查询）。  选择取决于工作负载：纯...

=== tps (1 occurrences) ===
  c12_b04.md: ...的重分片迁移  现在看 Anya 的具体问题：12 节点集群磁盘 87%，需下周前加 3 节点，同时服务 50,000 TPS 且无停机。  Anya 的协议：   #### 步骤 1：准备并加入新节点  3 个新节点以相同软件、配置部署。它们作...

=== innodb (1 occurrences) ===
  c12_b05.md: ...ee 仍占优之处：  * **Postgres** ：默认 B+ tree 索引。成熟、理解充分。 * **MySQL InnoDB** ：B+ tree。 * **SQL Server、Oracle** ：B+ tree 主存储。  B+ tree...

=== clickhouse (1 occurrences) ===
  c12_b06.md: ...范围适合：时序数据库常查询“T1 到 T2 之间的所有事件”。按时间戳范围分片使该查询成为单分区扫描。示例：HBase、ClickHouse、TimescaleDB。  许多现代系统 per-table 提供两者：CockroachDB 默认范围但支持哈希分片...

=== timescaledb (1 occurrences) ===
  c12_b06.md: ...查询“T1 到 T2 之间的所有事件”。按时间戳范围分片使该查询成为单分区扫描。示例：HBase、ClickHouse、TimescaleDB。  许多现代系统 per-table 提供两者：CockroachDB 默认范围但支持哈希分片表；Cassandra...

=== salesforce (1 occurrences) ===
  c12_b08.md: ...尾分布（小租户共置、大租户按需分裂）。 * 10 万+ 租户：选项 (a) 提供最简单分片，但需要应用层处理热租户。  Salesforce / Workday 模式（大规模多租户）通常是 (b) 加 careful schema 设计。初创 SaaS 模式（...

=== workday (1 occurrences) ===
  c12_b08.md: ...按需分裂）。 * 10 万+ 租户：选项 (a) 提供最简单分片，但需要应用层处理热租户。  Salesforce / Workday 模式（大规模多租户）通常是 (b) 加 careful schema 设计。初创 SaaS 模式（几百租户）常为简单选...

=== careful (1 occurrences) ===
  c12_b08.md: ...简单分片，但需要应用层处理热租户。  Salesforce / Workday 模式（大规模多租户）通常是 (b) 加 careful schema 设计。初创 SaaS 模式（几百租户）常为简单选 (c)。...

=== touch (1 occurrences) ===
  c12_b09.md: ...像：  * 写吞吐：非常高（memtable + WAL 顺序）。80% 写工作负载下表现好。 * 读延迟：每次读可能 touch memtable + Bloom filter + 多个 SSTable。冷读 p99 可能 1-5 ms。 * 空间...

=== flapping (1 occurrences) ===
  c12_b10.md: ...将 token-to-node 映射持久化（在 etcd、ZooKeeper 或类似中），使分配在节点重启后保留并防止 flapping。LLM 代码通常在启动时计算映射，不同运行可能产生不同映射。...

=== atomicity (1 occurrences) ===
  c13_b01.md: ...13.1 分布式事务问题  _分布式事务_ 是一组跨多个分片（或分区、节点）的操作，必须整体满足 ACID：  * **Atomicity（原子性）** ：所有操作提交，或都不提交。 * **Consistency（一致性）** ：数据库从一个有效状态转换到...

=== osdi (1 occurrences) ===
  c13_b03.md: ...## 13.3 Google Percolator  Percolator 由 Peng 和 Dabek 在 2010 OSDI 论文中描述，是 Google 的分布式事务协议，构建于 Bigtable 之上，用于网页索引流水线。它是大多数 “Sp...

=== start (1 occurrences) ===
  c20_b05.md: ...为 null。 * **Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。...

=== gap (1 occurrences) ===
  c13_b04.md: ...now().earliest`  provably 晚于任何更早带 TrueTime 时间戳事件的实时时刻。等待弥合了 gap；等待后，系统中任何其他 Spanner 事务都 provably 更晚或更早，而不会 “模糊”。   #### Spa...

=== analyticsservice (1 occurrences) ===
  c13_b07.md: ...ificationService.send(user, "completed")       step 6: call AnalyticsService.update(merchant, day)       on failure at step N: invoke co...

=== banksettlement (1 occurrences) ===
  c13_b07.md: ...rvice.check(transaction)         → score       step 3: call BankSettlement.submit(transaction)      → bank_ref       step 4: call Ledg...

=== fraudservice (1 occurrences) ===
  c13_b07.md: ...ReservationService.reserve(user, amount)       step 2: call FraudService.check(transaction)         → score       step 3: call BankS...

=== inventoryservice (1 occurrences) ===
  c13_b07.md: ...stens to OrderCreated      → publishes PaymentCompleted     InventoryService        listens to PaymentCompleted  → publishes InventoryRe...

=== orderservice (1 occurrences) ===
  c13_b07.md: ...行。  **Choreographed** —— 典型电商订单工作流（无中央 orchestrator）：       OrderService            → publishes OrderCreated event     PaymentServic...

=== paymentservice (1 occurrences) ===
  c13_b07.md: ...OrderService            → publishes OrderCreated event     PaymentService          listens to OrderCreated      → publishes PaymentCo...

=== reservationservice (1 occurrences) ===
  c13_b07.md: ...Orchestrator (CockroachDB-backed state):       step 1: call ReservationService.reserve(user, amount)       step 2: call FraudService.check...

=== shippingservice (1 occurrences) ===
  c13_b07.md: ...tens to PaymentCompleted  → publishes InventoryReserved     ShippingService         listens to InventoryReserved → publishes ShippingSc...

=== aggregate (1 occurrences) ===
  c13_b07.md: ...rmation         N-->>O: sent         O->>A: update merchant aggregate         A-->>O: updated         O-->>C: success         Not...

=== funds (1 occurrences) ===
  c13_b07.md: ...tics         C->>O: initiate payment         O->>R: reserve funds         R-->>O: reserved         O->>F: check fraud...

=== initiate (1 occurrences) ===
  c13_b07.md: ...ification         participant A as Analytics         C->>O: initiate payment         O->>R: reserve funds         R-->>O: reserv...

=== marketplace (1 occurrences) ===
  c13_b07.md: ...完整工作流需要阅读跨多个服务的订阅关系。  Crescent 对支付使用编排（受监管、审计重、工作流很少变）。其内部 “marketplace 订单处理” 子系统使用舞蹈化（不同团队拥有服务，独立演进）。两种模式在同一公司共存。  编排支付 saga 可视化：...

=== reserved (1 occurrences) ===
  c13_b07.md: ...itiate payment         O->>R: reserve funds         R-->>O: reserved         O->>F: check fraud         F-->>O: score (OK)...

=== sends (1 occurrences) ===
  c13_b07.md: ...NotificationService     listens to ShippingScheduled → sends confirmation   每个服务响应前一个服务的事件。补偿通过反向事件触发：`PaymentFailed → O...

=== sent (1 occurrences) ===
  c13_b07.md: ...ledger_id         O->>N: send confirmation         N-->>O: sent         O->>A: update merchant aggregate         A-->>O: up...

=== settlement (1 occurrences) ===
  c13_b07.md: ...heck fraud         F-->>O: score (OK)         O->>B: submit settlement         B-->>O: bank_ref         O->>L: record (transaction...

=== updated (1 occurrences) ===
  c13_b07.md: ...nt         O->>A: update merchant aggregate         A-->>O: updated         O-->>C: success         Note over O: On failure at...

=== compensable (1 occurrences) ===
  c13_b08.md: ...或原子中止，不依赖后续步骤。 * **Idempotent** ：重执行（如 saga 重试后）产生相同结果。 * **Compensable** ：存在语义上撤销该步骤的补偿动作。补偿很少是 “精确撤销”（你无法取消已发送通知）；通常是恢复不变量的前向动作（发...

=== independently (1 occurrences) ===
  c13_b08.md: ...#### 正确性义务  每一步必须：  * **Independently committable** ：本地事务成功或原子中止，不依赖后续步骤。 * **Idempotent** ：重执行（如...

=== scenario (1 occurrences) ===
  c13_b08.md: ...** ：补偿可安全执行多次。补偿期间的网路抖动常见；幂等不是可选项。   #### Saga vs 2PC：何时选谁  Scenario | 正确模式 ---|--- 同一分布式数据库内跨分片 | 2PC + 复制协调者（Spanner / Cockroa...

=== committable (1 occurrences) ===
  c13_b08.md: ...#### 正确性义务  每一步必须：  * **Independently committable** ：本地事务成功或原子中止，不依赖后续步骤。 * **Idempotent** ：重执行（如 saga 重试后）产生...

=== informational (1 occurrences) ===
  c13_b09.md: ...) 带退避重试第 5 步（接受短暂通知延迟）；(b) 触发第 1-4 步补偿。选择取决于用户通知对一致性是否关键，或仅 informational。对 Crescent 产品，通知是 best-effort；saga 以 1 小时预算重试，放弃后记录不一致性供人工复...

=== reversal (1 occurrences) ===
  c13_b09.md: ...2 | 运行风控检查（Continuum LLM） | 无需补偿（只读） 3 | 提交银行结算 API | 调用银行 reversal API 4 | 在 TigerBeetle 账本记录 | 记入反向双分录 5 | 发送确认通知 | 发送 “交易已取消...

=== ngn (1 occurrences) ===
  c13_b10.md: ...mide 从 Adaeze 到商家 Femi 的支付：       -- 金额以 BRL（巴西雷亚尔）计；到 Femi NGN 计价账户的跨币种结算     -- 由银行端 FX 层处理（此处范围外——本事务在 Crescent 内部 BRL 账...

=== intents (1 occurrences) ===
  c13_b10.md: ...6. `COMMIT`：事务协调者（本身通过 Raft 复制）在选定的 commit_timestamp 标记事务提交。intents 变成提交值。  跨区域延迟：每个 Raft commit 都涉及受影响分片在跨区域法定人数中的往返。对 Crescen...

=== rollup (1 occurrences) ===
  c20_b01.md: ...(EU-West shard, cross-region)"]         DB --> DB4["UPDATE rollup<br/>15 ms"]         style R fill:#fff4e6         style DB f...

=== purely (1 occurrences) ===
  c13_b14.md: ..._Spanner_ ：每次递增获取行级锁，等待提交时间戳（7 ms），然后提交。单热行吞吐受每行提交串行化限制：若瓶颈 purely commit-wait，每秒最多约 140 次递增，实际因锁获取开销约 50-100 次。  _CockroachDB...

=== spotify (1 occurrences) ===
  c14_b01.md: ...成本和 GCP 承诺的全球规模工作负载。生产示例：Google 自身基础设施（Ads、Cloud、Workspace）、Spotify、部分大型金融客户。...

=== cockroach (1 occurrences) ===
  c14_b02.md: ...### 14.3 CockroachDB  Cockroach Labs 的开源 NewSQL。有时被描述为 “Spanner for the rest of us”，因为它提供类似...

=== labs (1 occurrences) ===
  c14_b02.md: ...### 14.3 CockroachDB  Cockroach Labs 的开源 NewSQL。有时被描述为 “Spanner for the rest of us”，因为它提供类似保证而无...

=== pingcap (1 occurrences) ===
  c14_b02.md: ...多 fintech（DoorDash、Comcast 部分工作负载、若干大型银行）。   ### 14.4 TiDB  PingCAP 的 NewSQL；兼容 MySQL 线缆。  关键属性：  * TiKV 存储层（每 region/tablet 使用...

=== cloudwatch (1 occurrences) ===
  c14_b03.md: ...e 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 AWS。 * 较新（生产 track record 不如...

=== iam (1 occurrences) ===
  c14_b03.md: ...active-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 AWS。 * 较新（...

=== kms (1 occurrences) ===
  c14_b03.md: ...ve-active 多区域。  优势：  * Serverless 计费（无空闲容量）。 * AWS 原生：与 IAM、KMS、VPC、CloudWatch 集成。 * 兼容 Postgres。  劣势：  * 仅限 AWS。 * 较新（生产 t...

=== neely (1 occurrences) ===
  c14_b03.md: ...ion 共识。 * 确定性模拟测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，沿袭 Tyler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * TigerStyle...

=== tyler (1 occurrences) ===
  c14_b03.md: ...plication 共识。 * 确定性模拟测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，沿袭 Tyler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * Tige...

=== debugging (1 occurrences) ===
  c14_b03.md: ...gerStyle 编程规范：固定大小 128 字节消息、热路径无动态分配、显式状态机、设计口号是 “1000-year debugging”。 * 账户和转账的固定 schema；无通用 SQL。 * 极高吞吐设计（宣称每秒 100 万+ 转账）。  优势：...

=== dev (1 occurrences) ===
  c14_b03.md: ...* 分支（为 dev/test 即时克隆数据库）。  优势：  * Serverless：适合负载可变。 * 分支让 dev/test 很便宜（每个分支是存储的写时复制）。 * 真正 Postgres（非克隆）；所有扩展可用。  劣势：  *...

=== sled (1 occurrences) ===
  c14_b03.md: ...* 确定性模拟测试（DST）——协议的每个可达状态都在确定性模拟器中被显式枚举和演练，沿袭 Tyler Neely 的 sled 和 FoundationDB 的模拟器。能捕捉随机注入混沌工程遗漏的 bug。 * TigerStyle 编程规范：固...

=== year (1 occurrences) ===
  c14_b03.md: ...* TigerStyle 编程规范：固定大小 128 字节消息、热路径无动态分配、显式状态机、设计口号是 “1000-year debugging”。 * 账户和转账的固定 schema；无通用 SQL。 * 极高吞吐设计（宣称每秒 100 万+...

=== colossus (1 occurrences) ===
  c14_b04.md: ...ry 的 Dremel  Google 的分析服务；基于 2010 年的 Dremel 研究。  * 在 Google Colossus 文件系统上使用列式存储。 * 通过动态资源分配大规模并行执行。 * 标准 SQL；基于成本的优化器。 * TB 级数据...

=== prestodb (1 occurrences) ===
  c14_b04.md: ...## Trino/Presto  源自 Facebook 的 Presto（2013）；2020 年分叉为 Trino，PrestoDB 由 Facebook 继续。  * 分布式查询引擎；将工作下推给源系统。 * 跨多种数据源查询（S3、Hive、HDF...

=== forget (1 occurrences) ===
  c14_b04.md: ...：可在保留期内查询任何先前状态。  优势：serverless 计算；可预测成本；并发能力极强；“set it and forget it” 运维。  劣势：厂商锁定（专有格式，但 Iceberg 集成正在出现）；per-query 成本可能让调优差的...

=== virtual (1 occurrences) ===
  c14_b04.md: ...#### Snowflake  云原生数据仓库，计算与存储分离。  * 存储在类 S3 云对象存储中。 * 通过独立 virtual warehouse 计算；多个 warehouse 可同时读同一数据。 * 通过元数据层（构建于 Foundation...

=== elasticsearch (1 occurrences) ===
  c14_b08.md: ...域，各区域缓存。  (e) **队列**：Kafka 用于跨服务事件分发（通知、下游流水线）。  (f) **搜索**：Elasticsearch / OpenSearch 用于交易搜索（合规、客户支持、欺诈调查）。  架构分层：热路径（Redis + Cockro...

=== opensearch (1 occurrences) ===
  c14_b08.md: ...队列**：Kafka 用于跨服务事件分发（通知、下游流水线）。  (f) **搜索**：Elasticsearch / OpenSearch 用于交易搜索（合规、客户支持、欺诈调查）。  架构分层：热路径（Redis + CockroachDB + Tiger...

=== updates (1 occurrences) ===
  c14_b10.md: ...。  **Q3.** 用诊断方法（第11章）+ 事务协议视角（第13章）为全球游戏排行榜设计系统：10 万 score updates/秒，1000 万并发用户查看，读写 p99 都低于 200 ms。  提示  排行榜：因果一致。高写吞吐：不是经典 O...

=== rdd (1 occurrences) ===
  c15_b03.md: ...y-Lamport 快照，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD lineage、MapReduce 的 checkpointing、各种 ML 训练系统使用基于快照的重启。  该协议...

=== lineage (1 occurrences) ===
  c15_b03.md: ...mport 快照，尽管大多数生产系统使用基于时间戳的 MVCC。  * **可重启分布式计算** ：Spark RDD lineage、MapReduce 的 checkpointing、各种 ML 训练系统使用基于快照的重启。  该协议已有 40 多年...

=== unit (1 occurrences) ===
  c15_b03.md: ...entRead=true`，对 leader 做同步读。代价：通常是最终读延迟的 2 倍，并消耗更多 capacity unit。  跨区域（Global Tables）时，DynamoDB 使用异步多主复制。冲突解决按 item 做 last-w...

=== break (1 occurrences) ===
  c15_b04.md: ...OPS、Eiger，见第17章）。每次分数提交携带提交客户端看到的最新排行榜视图的 HLC；系统按 HLC 加 tie-break 排序。  成本：每操作成本低；秒内最终收敛。...

=== entry (1 occurrences) ===
  c15_b04.md: ...副本维护一个 “各副本贡献递增” 向量（每个副本一个条目）。总数是向量之和。合并两个 G-Counter 时取 per-entry 最大值（每个副本的贡献至少是已报告的最高值）。  成本：与最终一致相同，但有形式化合并语义。不可能丢失递增（一旦某副本...

=== tie (1 occurrences) ===
  c15_b04.md: ...系统（COPS、Eiger，见第17章）。每次分数提交携带提交客户端看到的最新排行榜视图的 HLC；系统按 HLC 加 tie-break 排序。  成本：每操作成本低；秒内最终收敛。...

=== resurrection (1 occurrences) ===
  c15_b07.md: ...写入节点时钟慢），LWW 会丢弃逻辑上更晚的写，而选择逻辑上更早的写。  Cassandra 的 “tombstone resurrection” bug（第5章）是经典示例：由于时钟偏移，delete 的时间戳早于后续写；后续写 “胜出” 并复活了已删记录。...

=== checkpointed (1 occurrences) ===
  c15_b08.md: ...oint 记录每个 source 已读的 offset；重启后 source 从该 offset 恢复。  (b) **Checkpointed operator state** ：Flink 的 checkpoint 协议（Chandy-Lamport 变体）记...

=== row (1 occurrences) ===
  c15_b08.md: ...**轻量事务（LWT）** ：资料更新使用 `UPDATE ... IF` 子句。LWT 用 Paxos 提供 per-row 线性化语义。代价：每次写延迟 4-10 倍。对资料编辑（不频繁操作）可接受。  (d) **Per-field CRD...

=== ground (1 occurrences) ===
  c15_b10.md: ...百万点赞在数秒内累积，精确点赞数会压垮强一致。正确答案是近似计数器（CRDT 或 HyperLogLog），定期由分析 ground-truth。  (c) **遥测 / 指标计数器。** 边缘部署的传感器以每传感器每秒 10,000 事件计数。回调到...

=== type (1 occurrences) ===
  c16_b01.md: ...现小一个数量级。   ### 16.1 CRDT 是什么  Conflict-free Replicated Data Type 是这样设计的数据类型：来自不同副本的并发更新可被确定性合并，无论合并顺序如何都产生相同结果。“无冲突” 是结构属性：数...

=== semilattice (1 occurrences) ===
  c16_b01.md: ...* **幂等律** ：\(\text{merge}(a,a) = a\)  这三条性质使状态在合并下形成一个 join-semilattice。任意合并序列都会收敛到所有已见状态的最小上界。  (b) **Operation-based CRDT（CmRDT）*...

=== staff (1 occurrences) ===
  c16_b01.md: ...Yuki Watanabe 是 Glide 的一名 staff 工程师，Glide 是与 Notion/Linear 同类的协作产品。Glide 的核心特性是多人可同时编辑同一份文档...

=== siblings (1 occurrences) ===
  c16_b02.md: ...ti-Value Register）  不选赢家，而是保留所有并发写。每次写带版本向量；并发写（版本向量互不支配）作为 siblings 保留。  * 在副本 \(j\) 用值 \(v\) 和版本 \(V\) 写入：将寄存器设为 \(\{(v,V)\}\)...

=== logoot (1 occurrences) ===
  c16_b03.md: ...st（文档正文）是最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必须产生一致的全序。   #### Logoot / Treedoc / WOOT  早期 list CRDT（2000 年代中期）。每个字符被分配一个 _positi...

=== treedoc (1 occurrences) ===
  c16_b03.md: ...最复杂的常见 CRDT。挑战：两个副本独立在不同位置插入字符；合并必须产生一致的全序。   #### Logoot / Treedoc / WOOT  早期 list CRDT（2000 年代中期）。每个字符被分配一个 _position identif...

=== typescript (1 occurrences) ===
  c16_b03.md: ...字符链，每个指向前一个）。  Yjs 在 Notion、Linear 等众多系统生产中运行。库很小（~80 KB），用 TypeScript / WebAssembly 实现。   #### Automerge 的实现  Automerge（Martin Kl...

=== woot (1 occurrences) ===
  c16_b03.md: ...T。挑战：两个副本独立在不同位置插入字符；合并必须产生一致的全序。   #### Logoot / Treedoc / WOOT  早期 list CRDT（2000 年代中期）。每个字符被分配一个 _position identifier_，位于...

=== comments (1 occurrences) ===
  c16_b03.md: ...= doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端拥有文档自己的副本...

=== getarray (1 occurrences) ===
  c16_b03.md: ...eta')          // 元数据 Y.Map（标题、标签）     const comments = doc.getArray('comments')// 评论 Y.Array   每个用户的客户端拥有文档自己的副本。编辑更新本地副本并产生一个小...

=== getmap (1 occurrences) ===
  c16_b03.md: ...c.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const comments = d...

=== gettext (1 occurrences) ===
  c16_b03.md: ...档包含命名类型：       const doc = new Y.Doc()     const text = doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('m...

=== identifier (1 occurrences) ===
  c16_b03.md: ...入的新字符获得 id 1.3 或 1.4。1.3 与 1.4 之间再插入则取 1.35；递归继续。  Position identifier 给字符排序；list 就是排序序列。插入是本地的；合并做 pair 并集。  局限：对抗性编辑下 position i...

=== meta (1 occurrences) ===
  c16_b03.md: ...t text = doc.getText('content')      // 正文 Y.Text     const meta = doc.getMap('meta')          // 元数据 Y.Map（标题、标签）     const...

=== actively (1 occurrences) ===
  c16_b04.md: ...#### 同步  两种同步模式共存：  (a) **通过 WebSocket 实时同步** ：用户 actively 编辑时，其 update 被发送到服务器（带 Durable Objects 的 Cloudflare Worker）...

=== edit (1 occurrences) ===
  c16_b04.md: ...(b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Yjs 的版本处理覆盖很多情况；应用以向后兼容层处理其余。  (c) **存储大小** ：重历史文档（...

=== migration (1 occurrences) ===
  c16_b04.md: ...义，通常在服务器以轻量事务实现（Durable Object 接受第一次删除，拒绝后续）。  (b) **Schema migration** ：文档 schema 变更时（如新增字段类型），旧客户端的飞行中 edit 可能解析不正确。Yjs 的版本处理覆盖...

=== collaboration (1 occurrences) ===
  c16_b05.md: ...在本地；用户拥有比特。 (c) **The network is optional** ：应用可离线工作。 (d) **Collaboration is seamless** ：多人可编辑；冲突自动解决。 (e) **Long-term archive** ：数据比...

=== long (1 occurrences) ===
  c16_b05.md: ...线工作。 (d) **Collaboration is seamless** ：多人可编辑；冲突自动解决。 (e) **Long-term archive** ：数据比提供服务的公司活得更久。 (f) **Privacy by default**...

=== ownership (1 occurrences) ===
  c16_b05.md: ...数据比提供服务的公司活得更久。 (f) **Privacy by default** ：数据不需要云账户。 (g) **Ownership** ：用户控制数据。  CRDT 是 (c)、(d) 及部分 (b)、(e) 的技术使能者。没有 CRDT，“离线且自...

=== replicache (1 occurrences) ===
  c16_b05.md: ...量使用 Yjs）、Notion（使用 Yjs）、Glide（Yuki 的产品）、Logseq（个人笔记使用 CRDT）、Replicache（CRDT-like 库）、Skiff（隐私导向文档，使用 Automerge）。  2026 趋势：随着 CRDT 成...

=== archive (1 occurrences) ===
  c16_b05.md: ...*Collaboration is seamless** ：多人可编辑；冲突自动解决。 (e) **Long-term archive** ：数据比提供服务的公司活得更久。 (f) **Privacy by default** ：数据不需要云账户。 (g...

=== optional (1 occurrences) ===
  c16_b05.md: ...y work is not trapped** ：数据在本地；用户拥有比特。 (c) **The network is optional** ：应用可离线工作。 (d) **Collaboration is seamless** ：多人可编辑；冲突自动解决...

=== seamless (1 occurrences) ===
  c16_b05.md: ...*The network is optional** ：应用可离线工作。 (d) **Collaboration is seamless** ：多人可编辑；冲突自动解决。 (e) **Long-term archive** ：数据比提供服务的公司活得更久。...

=== spinners (1 occurrences) ===
  c16_b05.md: ...、van Hardenberg、McGranaghan 在 2019 年论文中阐述。  七条属性：  (a) **No spinners** ：本地应用以本地交互速度运行。 (b) **My work is not trapped** ：数据在本地；用户拥...

=== trapped (1 occurrences) ===
  c16_b05.md: ...：  (a) **No spinners** ：本地应用以本地交互速度运行。 (b) **My work is not trapped** ：数据在本地；用户拥有比特。 (c) **The network is optional** ：应用可离线工作。...

=== work (1 occurrences) ===
  c16_b05.md: ...论文中阐述。  七条属性：  (a) **No spinners** ：本地应用以本地交互速度运行。 (b) **My work is not trapped** ：数据在本地；用户拥有比特。 (c) **The network is option...

=== cmrdt (1 occurrences) ===
  c16_b06.md: ...operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvRDT 合并状态；CmRDT 广播操作。  **答案：** CRDT（Conflict-free Replicated Data Type）是这样设...

=== cvrdt (1 occurrences) ===
  c16_b06.md: ...sed（CvRDT）与 operation-based（CmRDT）有何不同？  提示  CRDT：无论顺序，合并确定。CvRDT 合并状态；CmRDT 广播操作。  **答案：** CRDT（Conflict-free Replicated Dat...

=== bundling (1 occurrences) ===
  c16_b07.md: ...远不会看到 “合并冲突”）。 * 结构保持因果性：字符总插入在其前驱之后。  Yjs 的优化：  (a) **Item bundling** ：同一用户顺序输入的字符被捆绑成单个 item；常见情况下结构紧凑。 (b) **WebAssembly 性能**...

=== sheets (1 occurrences) ===
  c16_b08.md: ...A11，公式必须重新解释。CRDT 能表示公式文本，但不能在没有应用逻辑的情况下解决 “公式含义是什么”。Google Sheets 在协作公式语义上投入了数十年；这不只是 CRDT 问题。  (b) **范围操作**。“排序 A 列” 或 “删除 3...

=== getawareness (1 occurrences) ===
  c16_b08.md: ...ss 状态从其他客户端视图中移除（通常在短超时后）。  实现：       const awareness = doc.getAwareness()      // 光标移动时：     awareness.setLocalStateField('cursor',...

=== csv (1 occurrences) ===
  c16_b09.md: ...ge 内置历史是天然 fit；Yjs 需要单独审计日志。 * **批量导入**：导入 10,000 个 issue 的 CSV。两个库都能处理，但导入是结构操作，必须小心协调；CRDT 机制并非为批量加载优化。  推荐：  * 若审计/历史作为数...

=== diy (1 occurrences) ===
  c16_b11.md: ...产出朴素代码说明它没学到这一课，或它在不考虑需求的情况下生成最简代码。  对协作文辑来说，答案是 “用库”。这是一个 DIY 成本远高于库成本的领域。   ### 本章总结  CRDT（Conflict-free Replicated Data...

=== sequential (1 occurrences) ===
  c17_b01.md: ...强到弱的经典层级：  * **Linearizable** ：per-operation；尊重实时顺序。最贵。 * **Sequential consistent** ：per-operation；存在全序，与每个进程的本地顺序一致，但跨进程不一定与实时一致。...

=== causally (1 occurrences) ===
  c17_b01.md: ...mn family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB causally-consistent sessions**（3.6 起）：客户端跟踪见过的最高操作时间戳；同会话后续读要求服务器至少赶...

=== column (1 occurrences) ===
  c17_b01.md: ...某写前会延迟，直到其所有依赖都已应用。  Lloyd 等人在 2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB ca...

=== explicit (1 occurrences) ===
  c17_b01.md: ...r reads** ：从 follower 做有界过期读，过期由 HLC 界定。 * **Cassandra with explicit per-session tracking** ：非原生；应用层实现。...

=== family (1 occurrences) ===
  c17_b01.md: ...直到其所有依赖都已应用。  Lloyd 等人在 2013 年的后续论文 Eiger 扩展 COPS，增加 column family 和原子批次，把技术扩展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB causally-...

=== sessions (1 occurrences) ===
  c17_b01.md: ...展到更丰富 schema。  生产中提供因果一致的：  * **MongoDB causally-consistent sessions**（3.6 起）：客户端跟踪见过的最高操作时间戳；同会话后续读要求服务器至少赶上该时间戳。 * **Cockroach...

=== tracking (1 occurrences) ===
  c17_b01.md: ...做有界过期读，过期由 HLC 界定。 * **Cassandra with explicit per-session tracking** ：非原生；应用层实现。...

=== crdb (1 occurrences) ===
  c17_b02.md: ...新集合决定，而非策略。无不确定性。  CRDT（第16章）是典型强最终数据类型。Yjs、Automerge、Redis CRDB（不太知名的 Redis Enterprise 特性）、Riak 的 CRDT bucket types 都是生产示例。...

=== submission (1 occurrences) ===
  c17_b02.md: ...读者看到最近 HLC 截止前的所有提交。  成本：每次提交 HLC 计算和传播；定期重新计算排行榜。因为工作是 per-submission 而非协调，系统可跨区域每秒服务数百万提交。   #### 库存（第13章）：线性化  库存提交；“不能低于零” 需要协...

=== tracker (1 occurrences) ===
  c17_b02.md: ...；线程结构保持。  (c) **协作文档编辑** ：会话内编辑因果有序；CRDT 处理并发编辑。  (d) **Bug tracker 评论** ：讨论线程中回复顺序重要，但跨线程并发可任意。  与线性化相比：因果协调更少（每次操作无需共识），但应用可能...

=== repair (1 occurrences) ===
  c17_b05.md: ...assandra，并配置 QUORUM 读和写。这给他们什么一致性？  提示  QUORUM 给出相交；配合 read repair 接近 per-operation 线性化；但没有 LWT 就不是真正线性化。  **答案：** QUORUM 读写给出...

=== oplog (1 occurrences) ===
  c17_b07.md: ...执行的读）。(b) 会话上的后续读包含该时间戳作为 “clusterTime”。(c) 服务器（副本或主库）检查：本地 oplog 是否已应用到该 clusterTime？若是，服务读；若否，等待赶上（或超时）。  网络抖动期间，secondary...

=== instance (1 occurrences) ===
  c17_b10.md: ...例每秒计数事件；计数聚合到中央仪表盘。事务计数器要求每次递增咨询中央系统；延迟和吞吐都不对。基于 CRDT 的 per-instance 计数器加定期聚合自然处理。  (c) **实时分析**。一个 “当前在线用户数” 计数器每秒更新 5 万次，跨越数百万...

=== karlton (1 occurrences) ===
  c18_b01.md: ...、主从切换）来处理。分区期间的少数派写失败并返回 `CLUSTERDOWN`。   #### 缓存失效模式  Phil Karlton 的名言：“计算机科学中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-aside**：应用...

=== phil (1 occurrences) ===
  c18_b01.md: ...t 重分配、主从切换）来处理。分区期间的少数派写失败并返回 `CLUSTERDOWN`。   #### 缓存失效模式  Phil Karlton 的名言：“计算机科学中只有两件难事：缓存失效和命名。” 缓存失效模式：  (a) **Cache-as...

=== salvatore (1 occurrences) ===
  c18_b02.md: ...共享资源的访问。朴素形式：在共享存储中放一个 key；key 持有者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis 作者）提出 Redlock 作为分布式锁算法：  (a) 客户端选一个唯一值（随机...

=== sanfilippo (1 occurrences) ===
  c18_b02.md: ...形式：在共享存储中放一个 key；key 持有者是锁拥有者。   #### Redlock 模式  Salvatore Sanfilippo（Redis 作者）提出 Redlock 作为分布式锁算法：  (a) 客户端选一个唯一值（随机 nonce）。(b)...

=== wildly (1 occurrences) ===
  c18_b03.md: ...“释放锁”；存储视角一致。  (b) 时钟漂移：token 逻辑不涉及时钟。锁服务计数器是权威。即使客户端对时间看法 wildly 不同，资源的 “最高已见 token” 提供正确排序。  代价：受保护资源必须实现 fencing-token 逻辑。...

=== world (1 occurrences) ===
  c18_b03.md: ...合理保证。对正确性场景则不提供保证，原因有二：  (a) **进程暂停**。客户端获得锁后经历长时间 stop-the-world 暂停（垃圾回收、虚拟机暂停）。锁 TTL 过期。另一个客户端获得锁。原客户端恢复，未意识到锁已过期，继续修改资源。...

=== reject (1 occurrences) ===
  c18_b04.md: ...token=100, stale)         Bank->>Bank: 100 < highest (101); reject         Bank-->>C1: STALE_TOKEN   即使 C1 醒来仍以为自己持有锁（经典的基于超时锁...

=== resumed (1 occurrences) ===
  c18_b04.md: ...(100); accept         Bank-->>C2: ok         Note over C1: resumed after pause         C1->>Bank: commit (token=100, stale)...

=== sentinel (1 occurrences) ===
  c18_b05.md: ...原因除了 Redlock（进程暂停、时钟漂移、缓存故障）还有缓存特有原因：  (a) **缓存故障转移**：Redis Sentinel 提升副本；新 master 可能尚未复制锁 key；另一个客户端获得锁；两个持有者。  (b) **缓存内存驱逐**：...

=== user (1 occurrences) ===
  c27_b11.md: ...ayment(payment):         # Acquire distributed lock for the user's account         lock_key = f"user_lock:{payment.user_id}"...

=== seen (1 occurrences) ===
  c18_b07.md: ...响是幂等的：即使两个 worker 都执行，效果与一个相同。示例：worker 写入带幂等键的数据库 “user_id seen”。  缓存锁在以下情况 _不可接受_ ：  (a) 锁用于 _正确性_ ：只有一个客户端应同时修改资源。若两个都做，资...

=== vattani (1 occurrences) ===
  c18_b09.md: ...读者读取接近过期的条目时，以一定概率提前刷新缓存。概率随过期临近增加。到期时，缓存已被某个读者刷新；后续读者看到新条目。Vattani 等人的模式显著减少踩踏。  (b) **Per-key 刷新锁**：缓存未命中时，读者获取该 key 的短期锁。其他并...

=== epoch (1 occurrences) ===
  c18_b09.md: ...在 apply 时验证 token；过期 token 的条目是 no-op。  这种模式在分布式系统文献中有时称为 “epoch number”，提供运维安全。即使在复杂故障转移期间（集群重选领导者；旧领导者在网络抖动后重新加入并尝试发命令），to...

=== herd (1 occurrences) ===
  c18_b09.md: ...益巨大。  **Q3.** 团队缓存层（Redis Cluster）出现热门 key 过期时的 “thundering herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示  概率性提前刷新、刷新期间 per-key...

=== miss (1 occurrences) ===
  c18_b09.md: ...读取。  (c) **请求合并**：在应用层（或缓存客户端库），同一 key 的并发请求去重。只有一条真实 cache-miss 请求去数据库；其他等待结果。  (d) **异常时提供过期数据**：缓存过期且数据库慢或宕机时，提供过期值而非失败。这...

=== thundering (1 occurrences) ===
  c18_b09.md: ...）。代价可忽略；安全收益巨大。  **Q3.** 团队缓存层（Redis Cluster）出现热门 key 过期时的 “thundering herd”：流行缓存条目过期时，数百读者同时击中数据库。提出三种缓解策略。  提示  概率性提前刷新、刷新期间 per...

=== leases (1 occurrences) ===
  c19_b01.md: ...共识。 * 键值 API 提供线性化读写。 * Watch streams：客户端可订阅 key 变更并接收通知。 * Leases：key 可设 TTL；不续期则自动过期。 * Transactions：多键原子操作。  服务发现用法：  * 每个服...

=== cli (1 occurrences) ===
  c19_b02.md: ...**更好的集群管理**：etcd 的 `etcdctl` 和基于 operator 的管理比 ZooKeeper 的 CLI 和运维故事更友好。  ZooKeeper 仍在生产中使用（Kafka 旧元数据层、HDFS、较老的 Cassandra...

=== srv (1 occurrences) ===
  c19_b03.md: ...端在 TTL 边界轮询 DNS；无推送通知。 * **元数据有限**：DNS 记录只带 IP 和端口；更丰富元数据需要 SRV 或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、...

=== txt (1 occurrences) ===
  c19_b03.md: ...边界轮询 DNS；无推送通知。 * **元数据有限**：DNS 记录只带 IP 和端口；更丰富元数据需要 SRV 或 TXT 记录，并非所有客户端库都能优雅处理。  用例：  * **云厂商服务发现**：AWS Cloud Map、GCP Cl...

=== hadoop (1 occurrences) ===
  c19_b05.md: ...ey + 前缀 watch | 层级 zNodes | | Ecosystem | Kubernetes 原生使用 | Hadoop 生态（Kafka、HBase 历史） | | Operational tooling | etcdctl；云原生 |...

=== tooling (1 occurrences) ===
  c19_b05.md: ...Kubernetes 原生使用 | Hadoop 生态（Kafka、HBase 历史） | | Operational tooling | etcdctl；云原生 | zkCli.sh；较老模式 | | Performance | 针对现代硬件优化 |...

=== zkcli (1 occurrences) ===
  c19_b05.md: ...生态（Kafka、HBase 历史） | | Operational tooling | etcdctl；云原生 | zkCli.sh；较老模式 | | Performance | 针对现代硬件优化 | 成熟但较老优化 |  etcd 替代 Zoo...

=== stateful (1 occurrences) ===
  c19_b06.md: ...ation（无需服务重启）、监控阈值（无需代码部署即可调整告警）。  **Q4.** 为什么基于 DNS 的服务发现对 Stateful 服务常足够，但对无状态自动扩缩服务不足？  提示  DNS TTL 30-300 秒；有状态服务变化少；无状态服务变化...

=== acls (1 occurrences) ===
  c19_b07.md: ...s with lease。 * ZK watches（一次性）→ etcd watches（流式，更高效）。 * ZK ACLs → etcd RBAC + TLS 认证。  **Phase 2：并行部署**。etcd 与 ZooKeeper 并行...

=== recipe (1 occurrences) ===
  c19_b07.md: ...etcd keys with lease。 * 分布式锁：ZK sequence nodes → etcd lock recipe（lease + revision number）。 * 配置：ZK persistent zNodes → etcd...

=== good (1 occurrences) ===
  c19_b08.md: ...给出修复方案。  提示  Consul 基于 gossip 的健康检查；故障转移行为；客户端缓存 last-known-good 注册表。  **答案：** 诊断：Consul 故障转移期间，集群重新选举 Raft leader。选举期间新写被阻塞...

=== handler (1 occurrences) ===
  c19_b09.md: ...到 watch 事件和新值（通常在几十毫秒内；受网络限制）。    3. **服务端重新配置**：每个服务的 flag handler 更新内存中的 flag 状态。后续请求评估为新值。    4. **In-flight 请求**：flag 翻转前开始...

=== ecs (1 occurrences) ===
  c19_b10.md: ...TP API 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS 解析。  (c) **直接使用 etcd**：在 EC2 上部署 etc...

=== eureka (1 occurrences) ===
  c19_b10.md: ...；服务通过 etcd API 注册和发现。比 Consul 或 Cloud Map 工作量更大，但可控。  (d) **Eureka（Netflix OSS）**：较老的 Java 中心化服务发现。Netflix 使用；2026 年较不常见，但某些遗留...

=== fomo (1 occurrences) ===
  c19_b10.md: ...2023 年左右达到顶峰；许多团队随后因运维开销弃用服务网格，转向更简单的替代。趋势是 “需要时才用服务网格，不要因为 FOMO”。Istio 的复杂性尤其驱使团队转向更轻量的替代（Linkerd）或完全不用网格。  第二个考虑：服务网格价值很大程...

=== fargate (1 occurrences) ===
  c19_b10.md: ...PI 解析。  (b) **AWS Cloud Map**：AWS 托管服务发现。实例通过 API 注册，或与 ECS/Fargate 自动注册；客户端通过 DNS 解析。  (c) **直接使用 etcd**：在 EC2 上部署 etcd 集群；服务通...

=== plain (1 occurrences) ===
  c19_b10.md: ...#### 深入思考  **Q1.** 辩护或反驳：“2026 年服务网格对大多数团队过度工程。Plain Kubernetes 服务发现对 90% 的微服务部署足够。”  **讨论：** 辩护：Kubernetes 服务发现...

=== child (1 occurrences) ===
  c20_b01.md: ...e6         style DB fill:#e6f3ff   每个框是一个 span；箭头是 parent → child 关系。根的子节点是 _串行_ 阶段（非并行）；它们时长相加约等于根总时长（65 + 22 + 80 + 95 + 30...

=== root (1 occurrences) ===
  c20_b01.md: ...；第28章 §28.2 有 Crescent 系统对应完整拆解）：       graph TD         R["root span<br/>POST /payment<br/>~365 ms total"]         NW["netw...

=== stack (1 occurrences) ===
  c20_b01.md: ...Vol 3（可观测性）。本章聚焦追踪所支持的分布式系统推理。   ### 20.1 为什么需要分布式追踪  单体应用中，stack trace 告诉你发生了什么：哪个函数调用了哪个，错误在哪里。分布式系统中，等价物是 _trace_：请求跨服务的因果...

=== metadata (1 occurrences) ===
  c20_b02.md: ...标准传播：HTTP headers（W3C 标准的 `traceparent`、`tracestate`）或 gRPC metadata。服务接收请求时从 headers 提取 trace context；它创建的 spans 使用该 context。...

=== retroactively (1 occurrences) ===
  c20_b02.md: ...based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retroactively 应用决策。这需要更多基础设施，但允许基于结果采样（“保留所有错误 trace；成功 trace 采样 1%”）。...

=== sampling (1 occurrences) ===
  c20_b02.md: ...决策冗余编码（在 trace context 加 per-span attribute 中）。  Tail-based sampling（trace 完成后再决策）更复杂但更强：spans 必须被缓冲到 trace 完成，然后 retroactively...

=== older (1 occurrences) ===
  c20_b03.md: ...race context 标准用于跨服务传播。  采用情况：大多数新微服务从第一天就使用 OpenTelemetry。 older 系统随时间从 vendor-specific SDK 迁移到 OpenTelemetry。  本章不展开 OpenTe...

=== status (1 occurrences) ===
  c20_b05.md: ...：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes**：自定义键值对（HTTP 状态、数据库查询、应用自定义元...

=== duration (1 occurrences) ===
  c20_b05.md: ...Service name 和 operation name**：哪个服务、哪个操作。 * **Start time 和 duration**：span 何时开始、持续多久。 * **Status**：成功、错误，可带错误码。 * **Attributes*...

=== consumer (1 occurrences) ===
  c20_b08.md: ...Kafka 传播修复后，团队将看到从 API gateway → producer service → Kafka → consumer service → 下游服务的完整 trace。端到端可见性是分布式追踪的全部意义。...

=== otel (1 occurrences) ===
  c20_b09.md: ...表特定产品；LLM 产出它见过的东西，而非对用户最好的。  正确答案："使用 OpenTelemetry SDK；通过 OTel exporter 配置你喜欢的后端（DataDog、Honeycomb、Tempo 等）。这样避免 vendor lo...

=== retrofit (1 occurrences) ===
  c20_b09.md: ...其他工具。  2026 现实：tracing 采用已成熟但未普及。新微服务部署通常从 tracing 开始；旧系统经常 retrofit。"默认 OpenTelemetry" 是运维标准。  第二个考虑：AI 辅助调试工具（LLM 驱动）正在出现，可自动关...

=== profiling (1 occurrences) ===
  c20_b10.md: ...lock-in 并提供生态契合。  Traces 是强大的调试工具但不是唯一工具。Traces 展示跨服务发生了什么；profiling 展示服务内部发生了什么；metrics 展示资源和速率行为；logs 展示 per-event 叙事。成熟运维团队会关...

=== limited (1 occurrences) ===
  c21_b02.md: ...alls fail fast;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a)...

=== site (1 occurrences) ===
  c21_b02.md: ...降流量的 10% 也下降。放大被限制。  实现：每个客户端跟踪近期成功率；计算预算；抑制超额重试。  Google 的《Site Reliability Engineering》一书有经典讨论。该模式现在已是生产级库的标准（gRPC、Envoy、I...

=== allowed (1 occurrences) ===
  c21_b02.md: ...no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Closed**：正常操作。所有调...

=== contact (1 occurrences) ===
  c21_b02.md: ...e rate         Open: All calls fail fast;<br/>no downstream contact         HalfOpen: Limited probe calls allowed;<br/>test if...

=== cool (1 occurrences) ===
  c21_b02.md: ...hreshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --> Clos...

=== down (1 occurrences) ===
  c21_b02.md: ...old<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --> Closed: p...

=== elapses (1 occurrences) ===
  c21_b02.md: ...r/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-down elapses<br/>(e.g. 30s timer)         HalfOpen --> Closed: probe cal...

=== exceeds (1 occurrences) ===
  c21_b02.md: ...[*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen...

=== recovered (1 occurrences) ===
  c21_b02.md: ...lfOpen: Limited probe calls allowed;<br/>test if downstream recovered   (a) **Closed**：正常操作。所有调用都通过。  (b) **Open**：失败阈值被超过。所有调用立即...

=== statediagram (1 occurrences) ===
  c21_b02.md: ...下游调用的成功率；若失败率超过阈值，熔断器 “打开”，后续调用立即失败（不再联系下游）。  三状态可视化：       stateDiagram-v2         [*] --> Closed         Closed --> Open: failure...

=== succeed (1 occurrences) ===
  c21_b02.md: .../>(e.g. 30s timer)         HalfOpen --> Closed: probe calls succeed<br/>(e.g. 80% over 5s)         HalfOpen --> Open: probe cal...

=== threshold (1 occurrences) ===
  c21_b02.md: ...*] --> Closed         Closed --> Open: failure rate exceeds threshold<br/>(e.g. 50% over 30s)         Open --> HalfOpen: cool-dow...

=== haproxy (1 occurrences) ===
  c21_b06.md: ...Resilience4j 替代）；Resilience4j（现代 Java）；polly（.NET）；Envoy 和 HAProxy 作为反向代理。该模式在成熟分布式系统中是标准。  **Q4.** 2003 年美国东北部大停电教给分布式系统工程师什么...

=== nerc (1 occurrences) ===
  c21_b06.md: ...5000 万人；40-100 亿美元损失），教训清晰（小触发器、放大主导、防御缺失）。每个分布式系统工程师都应阅读官方 NERC 事后复盘，并问：“我的系统中对应的是什么？”...

=== alerting (1 occurrences) ===
  c21_b06.md: ...运维看不到初始故障（监控软件 bug）；等他们看到扩散时，干预已太晚。分布式系统类比：tracing、metrics、alerting 必须在系统降级时仍工作。  (c) **防御必须本地运行**。每条输电线路有自己的保护继电器；过载时跳闸隔离故障。分布...

=== retries (1 occurrences) ===
  c21_b06.md: ...en 状态下，本可能成功的调用也失败（上游无法判断）。对某些工作负载（任何成功调用都比没有好），Closed-with-retries 更可取；对其他工作负载（级联失败是更大风险），Open 是正确行为。  生产实现：Netflix Hystrix（已弃...

=== surrounding (1 occurrences) ===
  c21_b10.md: ...组合式防御，每种都限制放大。  更深观察：“韧性” 不是单一属性；而是一组模式。LLM 产出 “重试 5 次” 而没有 surrounding context，是缺失架构。正确的 LLM 回应应是：“韧性需要重试预算、熔断器、超时、幂等和 fallback。以下...

=== marcos (1 occurrences) ===
  c22_b02.md: ...廉价探测时。健康检查应匹配标准，而不是最低门槛。   ### 22.3 亚稳态故障  Nathan Bronson 和 Marcos Aguilera 2021 年的论文引入 _metastable failure_ 的形式概念：一种小触发器把系统推入...

=== nathan (1 occurrences) ===
  c22_b02.md: ...务真实请求时才健康，而不是在能回答廉价探测时。健康检查应匹配标准，而不是最低门槛。   ### 22.3 亚稳态故障  Nathan Bronson 和 Marcos Aguilera 2021 年的论文引入 _metastable failure_...

=== dump (1 occurrences) ===
  c22_b07.md: ...*：服务中偶尔把许多请求串行化的临界区。第一个请求因锁等待而耗时；其后排队的请求也耗时。诊断：尖刺期间的 thread dump；找许多线程阻塞在同一把锁上。  (c) **下游抖动**：下游调用偶尔耗时 30 秒（可能是其自身 GC、网络问题等）...

=== slate (1 occurrences) ===
  c22_b07.md: ...慢；需要手动协调。  **答案：**  _选项 1：重启数据库_  优点：重置缓存（从头重建）；清除卡住的查询；“干净 slate” 恢复。  缺点：in-flight 请求失败（应用看到错误）；缓存必须重建（最初很多分钟慢，因为冷缓存）；短暂完全中...

=== ribbon (1 occurrences) ===
  c22_b08.md: ...Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已弃用但历史上重要）有类似功能。  上述设计无需每个后端都做深度健康检查即可防御灰色故障。负载均衡器是天然瓶颈点；负载...

=== outstanding (1 occurrences) ===
  c22_b08.md: ...er detection” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已弃用但历史上重要）有类似功能。  上述设计...

=== requests (1 occurrences) ===
  c22_b08.md: ...” 实现了大部分；AWS Application Load Balancer 的 “least-outstanding-requests” 路由是更简单的近似；Netflix 的 Ribbon（已弃用但历史上重要）有类似功能。  上述设计无需每个后端都做深...

=== weight (1 occurrences) ===
  c22_b08.md: ...(c) **集群范围延迟参考**：计算所有后端 p99 的中位数。这是 “预期” 延迟。  (d) **Backend weight**：根据每个后端 p99 相对于集群中位数的反比分配权重。比中位数快的后端权重 1.0；慢的后端按比例降低。  例如，...

=== detailed (1 occurrences) ===
  c22_b10.md: ...服务是否仍在初始化？用于生命最初 30-60 秒；区分 “启动中” 与 “坏了”。允许启动期间更长超时。  (d) **Detailed health endpoint**（给运维的 `/health/details`）：每个子系统状态的 JSON 输出。...

=== blast (1 occurrences) ===
  c22_b11.md: ...这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流...

=== days (1 occurrences) ===
  c22_b11.md: ...讲主动解决这些故障模式的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项...

=== radius (1 occurrences) ===
  c22_b11.md: ...的纪律：混沌工程。从 Netflix Chaos Monkey 到 2026 生产实践。Game days、blast radius、Nora Jones 的组织实践。Olamide 的 Crescent game day 项目（工作流 11）将走一遍...

=== fis (1 occurrences) ===
  c23_b02.md: ...无关。  选择取决于部署：Kubernetes 原生部署用 Chaos Mesh 或 Litmus；AWS 原生部署用 FIS；需要商业支持用 Gremlin；跨平台团队用 Toxiproxy。...

=== helm (1 occurrences) ===
  c23_b02.md: ...、磁盘故障、时间偏移。广泛采用。  (b) **Litmus**：另一个 Kubernetes 混沌框架。云原生；基于 Helm 部署；可扩展。  (c) **Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fau...

=== injection (1 occurrences) ===
  c23_b02.md: ...；可扩展。  (c) **Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e)...

=== rds (1 occurrences) ===
  c23_b02.md: ...团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e) **Toxiproxy**：TCP 层故障注入。慢连接、丢包、模拟...

=== simulator (1 occurrences) ===
  c23_b02.md: ...**Gremlin**：商业混沌工程平台。易用；面向企业团队。  (d) **AWS Fault Injection Simulator**：AWS 原生；注入 EC2 故障、RDS 故障、网络故障。适合 AWS 重度团队。  (e) **Toxiprox...

=== drain (1 occurrences) ===
  c23_b04.md: ...all 室。 * 14:10 UTC：实验开始。Olamide 对 EU-West 节点调用 CockroachDB “drain” 命令（优雅关闭）。 * 14:15 UTC：EU-West 节点下线。集群的 Raft groups 在剩余区域重新...

=== groups (1 occurrences) ===
  c23_b04.md: ...roachDB “drain” 命令（优雅关闭）。 * 14:15 UTC：EU-West 节点下线。集群的 Raft groups 在剩余区域重新选举 leader。EU-West 发起的支付交易通过基于 Consul 的服务 mesh 故障转移到...

=== alice (1 occurrences) ===
  c23_b07.md: ...的价值就被摧毁。  心理安全意味着：团队可以在不被个人责备的情况下指出问题。“重试预算太高，导致级联” 是系统观察；“Alice 配置了重试预算，她错了” 是个人责备。前者导向修复；后者导向防御行为且无修复。  Nora Jones 的无责复盘实践...

=== targeted (1 occurrences) ===
  c23_b10.md: ...g 严重高（客户可见数据损坏）；修复复杂度可能中等（单团队几天可解决）。推荐：暂停大型 game day；可继续小范围 targeted 实验验证修复；修复验证后恢复常规 game day 计划。...

=== alone (1 occurrences) ===
  c24_b01.md: ...a 3 70B、Mistral Large、Claude 3 Opus 级别）需要的内存超过任何单张 GPU。模型权重 alone 就是数十 GB；每个请求的 KV cache 又增加更多。推理必须跨 GPU 分布。  但分布式推理不是 “以 LLM...

=== logits (1 occurrences) ===
  c24_b01.md: ...。每个 token 生成延迟 10-50 ms。在此预算内，系统必须读取 KV cache、计算 next-token logits、采样、推进状态。超过几毫秒的网络跳数都很重要。  (e) **突发工作负载模式**。有的请求短（单次问答），有的长（大...

=== next (1 occurrences) ===
  c24_b01.md: ...**严格的延迟预算**。每个 token 生成延迟 10-50 ms。在此预算内，系统必须读取 KV cache、计算 next-token logits、采样、推进状态。超过几毫秒的网络跳数都很重要。  (e) **突发工作负载模式**。有的请求...

=== tool (1 occurrences) ===
  c24_b03.md: ...schema、regex 约束）。结构是请求规范的一部分；推理引擎知道如何处理。  (c) **函数调用**：原生支持 tool use；radix tree 跨请求缓存函数定义部分。  SGLang 在具有高前缀复用的工作负载中流行：客服机器人、...

=== mig (1 occurrences) ===
  c24_b04.md: ...BAC、审计日志、监控集成。面向企业采购而建。  (e) **GPU 共享**：在支持硬件上，多个 NIM 容器可通过 MIG（Multi-Instance GPU）共享一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别...

=== tensorrt (1 occurrences) ===
  c24_b04.md: ...bernetes 部署；由 NVIDIA 工具管理。  (b) **多引擎支持**：NIM 容器可按模型使用 vLLM、TensorRT-LLM（NVIDIA 优化引擎）或其他引擎。面向用户的 API 一致。  (c) **硬件优化**：NIM 容器包含...

=== native (1 occurrences) ===
  c24_b04.md: ...一张 GPU。  NIM 是部署表面；vLLM（通常）是内部的推理引擎。区别是运维：NIM 提供 Kubernetes-native 部署、可观测性和企业集成；vLLM 提供推理优化。  对企业团队：NIM 是标准。对开源/成本优化团队：直接使用 vL...

=== markup (1 occurrences) ===
  c24_b05.md: ...每个请求 0.01-0.02 USD（取决于模型大小、prompt 长度、客户等级）。利润来自 per-request markup。  优化机会：减少每个请求的 GPU-秒。更好的 attention（PagedAttention、RadixAtte...

=== license (1 occurrences) ===
  c24_b08.md: ...es 部署：  * **NIM** 是运维上更快的路径。合规和企业特性已预建；团队认证工作量减少。成本是 NVIDIA license 溢价。 * **vLLM** 是成本有效的路径，若团队有工程能力构建缺失的企业特性。长期控制更多；短期工作量更大。...

=== capex (1 occurrences) ===
  c24_b09.md: ...；标准 serving → NIM（集成优于灵活）。 * **成本敏感度**：非常敏感成本 → vLLM 加自定义集成；CapEx 灵活 → NIM。  对 Continuum（重度基础设施用户，有平台团队）：vLLM 加自定义企业特性合理。对大多数...

=== face (1 occurrences) ===
  c24_b10.md: ...*Q2.** 一个 LLM 被要求 “为我们的 Llama 3 70B 搭建推理集群”。它产出使用基础 Hugging Face TextGenerationInference（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产部署包含什...

=== hugging (1 occurrences) ===
  c24_b10.md: ...正确框架。  **Q2.** 一个 LLM 被要求 “为我们的 Llama 3 70B 搭建推理集群”。它产出使用基础 Hugging Face TextGenerationInference（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产...

=== textgenerationinference (1 occurrences) ===
  c24_b10.md: ...* 一个 LLM 被要求 “为我们的 Llama 3 70B 搭建推理集群”。它产出使用基础 Hugging Face TextGenerationInference（TGI）部署的代码。指出 LLM 遗漏什么，2026 年生产部署包含什么。  **讨论：** LLM 的基础 TGI...

=== hour (1 occurrences) ===
  c24_b10.md: ...服务多客户，需要隔离（rate limit、队列优先级、每客户模型分片）。  (g) **成本监控**：每客户 GPU-hour、每工作负载。真实生产部署会针对成本做仪器。  (h) **故障处理**：GPU 故障、节点故障、模型加载故障。部署必须...

=== allocates (1 occurrences) ===
  c25_b01.md: ...-| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector mul | Memory bandwidth...

=== bottleneck (1 occurrences) ===
  c25_b01.md: ...法；瓶颈是内存带宽（加载 KV cache）。  不对称性：  | Phase | Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---|...

=== microsoft (1 occurrences) ===
  c25_b01.md: ...che |  一张 GPU 同时运行两个阶段，可能只用到 50% 计算和 50% 内存带宽：从未饱和任一。浪费巨大。  Microsoft Research 2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两...

=== research (1 occurrences) ===
  c25_b01.md: ...GPU 同时运行两个阶段，可能只用到 50% 计算和 50% 内存带宽：从未饱和任一。浪费巨大。  Microsoft Research 2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同...

=== tflops (1 occurrences) ===
  c25_b01.md: ...| |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU TFLOPs | ~0.1-1 ms | Allocates KV cache | | Decode | Matrix-vector...

=== ucsd (1 occurrences) ===
  c25_b01.md: ...内存带宽：从未饱和任一。浪费巨大。  Microsoft Research 2023 年的 Splitwise 论文和 UCSD 2024 年的 DistServe 提出：把两个阶段分离到不同 GPU。每个 GPU 针对自己的阶段优化。Prefil...

=== usage (1 occurrences) ===
  c25_b01.md: ...| Compute pattern | Bottleneck | Latency per token | Memory usage | |---|---|---|---|---| | Prefill | Matrix-matrix mul | GPU...

=== router (1 occurrences) ===
  c25_b02.md: ...5.2 分离式架构  拓扑可视化：       graph TB         LB[Load Balancer / Router]         subgraph PrefillTier["Prefill Tier<br/>(compute-op...

=== kimi (1 occurrences) ===
  c25_b05.md: ...）：商业实现。KV cache 作为独立 tier；decode 节点通过 RDMA 读取。用于 Moonshot 的 Kimi 产品，服务 1M+ token 上下文。  (b) **LMCache**（UC Berkeley，2024）：开源等...

=== decommissioning (1 occurrences) ===
  c25_b06.md: ...e pools；separate scaling policies；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 deco...

=== logic (1 occurrences) ===
  c25_b06.md: ...scaling policies；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cache 从 prefill 传输到 decode 的代码...

=== policies (1 occurrences) ===
  c25_b06.md: ...和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning logic。  (b) **KV...

=== pools (1 occurrences) ===
  c25_b06.md: ...) **两种节点类型**：prefill 节点和 decode 节点。Separate Kubernetes node pools；separate scaling policies；separate provisioning and decommi...

=== provisioning (1 occurrences) ===
  c25_b06.md: ...te Kubernetes node pools；separate scaling policies；separate provisioning and decommissioning logic。  (b) **KV cache 传输逻辑**：把 KV cach...

=== chassis (1 occurrences) ===
  c25_b07.md: ...code 节点。(b) **节点内 NVLink**：把 prefill 和 decode 放在同一 physical chassis；NVLink 传输比跨节点 RDMA 快得多。(c) **共享前缀缓存**：若 prompt 有已缓存前缀（来自之前请...

=== mirror (1 occurrences) ===
  c25_b08.md: ...转 decode 角色，2 个做 cache 节点。 * 构建分离层：路由、KV cache 传输、故障处理。 * 用 mirror 生产流量模式的合成工作负载测试。 * 验证：延迟、吞吐、故障模式。  **Phase 2：逐步转换（3-4 个月）**...

=== preemptible (1 occurrences) ===
  c25_b10.md: ...内存带宽或计算。 * 混合部署（prefill 与 decode 用不同 GPU）。 * 对非关键工作负载用 spot/preemptible 实例。  (d) **工作负载优化**：  * 限制每请求最大上下文长度（防止超大上下文拖慢集群）。 * 把不同工作负...

=== slight (1 occurrences) ===
  c25_b10.md: ...则不可行的工作负载得以运行。  (b) **引擎优化**：  * 量化（8-bit 或 4-bit serving）： slight 质量损失下 2-4 倍吞吐。 * Speculative decoding（第26章）：长生成 2-3 倍吞吐。 *...

=== spot (1 occurrences) ===
  c25_b10.md: ...每美元更好内存带宽或计算。 * 混合部署（prefill 与 decode 用不同 GPU）。 * 对非关键工作负载用 spot/preemptible 实例。  (d) **工作负载优化**：  * 限制每请求最大上下文长度（防止超大上下文拖慢集...

=== dense (1 occurrences) ===
  c25_b11.md: ...）。 * 用 MoE（Mixture-of-Experts）模型：总参数量很大，但每 token 只激活部分；成本低于 dense 大模型。  (f) **运维优化**：  * 根据观测利用率 right-size 集群。 * 用自动扩缩匹配需求。...

=== fine (1 occurrences) ===
  c25_b11.md: ...* 对某些工作负载用更小模型（质量可接受处）。 * 用 task-specific fine-tunes（针对特定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-of-Experts）模型：...

=== setup (1 occurrences) ===
  c25_b11.md: ...实际深度。正确答案应是：“推理成本优化有很多杠杆。最大杠杆是 [列表]。你的集群在每个上处于什么位置？我们针对你的具体 setup 确定下一步最高杠杆。”   ### 本章总结  Disaggregated prefill/decode 是 2025...

=== task (1 occurrences) ===
  c25_b11.md: ...* 对某些工作负载用更小模型（质量可接受处）。 * 用 task-specific fine-tunes（针对特定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-...

=== tunes (1 occurrences) ===
  c25_b11.md: ...* 对某些工作负载用更小模型（质量可接受处）。 * 用 task-specific fine-tunes（针对特定任务调优的小模型可媲美大通用模型）。 * 用 MoE（Mixture-of-Experts）模型：总参数量很大...

=== balancing (1 occurrences) ===
  c26_b01.md: ...。  本章讲 2024-2026 年出现的两个技术，处理 LLM 专用路由问题：KV-cache-aware load balancing（把请求路由到有相关 cache 的服务器；重大效率收益）和 speculative decoding（用小 draft...

=== drr (1 occurrences) ===
  c26_b04.md: ...客户有权重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户...

=== derived (1 occurrences) ===
  c26_b04.md: ...重；调度器按比例分配 GPU 时间。更复杂；更公平。  (d) **Deficit Round-Robin / DRR-derived**：每客户有随时间累积的 “credit”；请求消耗 credit；调度器从 credit 为正的客户中处理。提供有界...

=== engine (1 occurrences) ===
  c26_b04.md: ...布。  (d) **调度**：多租户公平性的 weighted fair scheduling。  (e) **Per-engine**：通用工作负载用 vLLM，前缀复用工作负载用 SGLang。  (f) **硬件**：面向 disaggregat...

=== queues (1 occurrences) ===
  c26_b04.md: ...rate limits**：每客户有配额；超配额请求被限流。简单；可预测；不适应负载。  (b) **Priority queues**：高 tier 客户请求先处理。高 tier 可预测；可饿死低 tier。  (c) **Weighted fair...

=== swap (1 occurrences) ===
  c26_b05.md: ...模型；路由必须考虑哪些节点加载了哪些模型。把 model A 请求路由到只服务 model B 的节点需要 model swap（慢）或拒绝。  (d) **尾部延迟**：LLM 尾部延迟比 Web 宽得多。错误路由决策可导致用户等待 30 秒尾部...

=== hierarchical (1 occurrences) ===
  c26_b06.md: ...**：每客户轮流处理；每轮每客户最多获得 N 单位 GPU 时间（N 与权重成正比）；超额工作等下一轮。  (c) **Hierarchical scheduling**：客户可分组（如按 tier）；调度器分层（tier 内公平；tier 间有优先级）。...

=== evict (1 occurrences) ===
  c26_b10.md: ...**缓存新鲜度跟踪**。KV cache 会过期（内存压力、TTL）。路由器必须知道哪些缓存仍然有效；路由到缓存已被 evict 的节点无收益。  (c) **负载感知**。即使某节点有缓存，它可能已超载。路由器必须平衡缓存局部性与当前负载。  (...

=== pin (1 occurrences) ===
  c26_b10.md: ...取。  (e) **客户亲和性**：对 workload 可预测的客户（相同 system prompt），把客户流量 pin 到特定节点以最大化缓存复用。  (f) **冷启动 fallback**：若没有节点有缓存，路由器回退到默认负载均衡（...

=== buggy (1 occurrences) ===
  c27_b06.md: ...### 27.10 AI 辅助审查 AI 生成代码  §27.9 解释了为什么 LLM 产出 buggy 分布式代码。自然跟进：若 AI 工具生成代码，AI 工具能否也 _审查_ 它？部分可以。这个部分答案决定如何组织审查流...

=== legacy (1 occurrences) ===
  c27_b06.md: ...s.get()` 没有 timeout（库默认 _没有_ timeout）。 * **库习惯不匹配**：不需要的地方用 legacy Hystrix 装饰器；`redis.set("...", nx=True, ex=...)` 模式用于正确性关键锁（...

=== mutation (1 occurrences) ===
  c27_b06.md: ...锁（§27.5 的 Redlock 反模式）。 * **测试覆盖提示**：新的分布式代码函数没有相关测试文件；关键路径 mutation-test 分数低。  这些是可分解为 “在 diff 中找这个正则” 的检查清单项。AI 审查者几秒处理。作为 pre...

=== coderabbit (1 occurrences) ===
  c27_b07.md: ...三层审查：  **第一层 —— 自动 AI 审查（秒级，每次 push 运行）**：  * 使用 Greptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budget 注解、idempote...

=== greptile (1 occurrences) ===
  c27_b07.md: ...I 生成分布式代码使用三层审查：  **第一层 —— 自动 AI 审查（秒级，每次 push 运行）**：  * 使用 Greptile 或 CodeRabbit 类服务做 pre-PR 检查。 * 检查清单的语法项：timeout、retry-budge...

=== dismiss (1 occurrences) ===
  c27_b07.md: ...* 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false positive，作者用一行理由 dismiss。  **第二层 —— 人类按清单审查（分钟级，每个分布式 PR）**：  * 受过六个谎言训练的人类审查者打开 PR。...

=== falsify (1 occurrences) ===
  c27_b07.md: ...为 game day 的测试计划。  这是与第23章运维紧密连接的地方：审查记录一份声明防御列表；game day 是 falsify（或确认）每个声明的实验。审查者声称但 game day 未演练的防御不是已验证防御；只是愿景。在 Crescent 流...

=== plan (1 occurrences) ===
  c27_b07.md: ...防御不是已验证防御；只是愿景。在 Crescent 流程中，每个新分布式代码 PR 附带 “game-day test plan” 附录，逐条列出声明的防御；下一次月度 game day 包含这些场景。  具体例子：Olamide 团队合并了一个为...

=== positive (1 occurrences) ===
  c27_b07.md: ...-key 参数使用、排序原语使用。 * 输出：diff 上的自动批注列表；约 80% 准确；约 20% 是 false positive，作者用一行理由 dismiss。  **第二层 —— 人类按清单审查（分钟级，每个分布式 PR）**：  * 受过六个...

=== reviewed (1 occurrences) ===
  c27_b07.md: ...无法做的上下文判断：哪把锁用于正确性、哪个不变量必须成立、哪个设计选择正确。 * 人类用显式短语 “checklist reviewed” 签批。  **第三层 —— 混沌验证（小时级，发布前运行）**：  * 任何新分布式特性发布前，chaos game...

=== lgtm (1 occurrences) ===
  c27_b08.md: ...“人类审查” 评论必须阐明至少一项人类检查的实质性内容（工作负载、一致性要求、业务不变量）。若评论只是 AI 审查后 “LGTM”，就是橡皮图章。  Crescent 审查政策强制执行：每个分布式 PR 批准需要一句话解释人类在 AI 输出之外验证...

=== negative (1 occurrences) ===
  c27_b08.md: ...处理涌现行为。跳过任何一层都会产生可预测的缺口：跳过第一层，审查者浪费时间在常规捕获；跳过第二层，AI 的 false negative 进入生产；跳过第三层，防御只在纸面上存在，在负载下失败。   #### 这对人类审查者角色意味着什么  2026 年人...

=== stub (1 occurrences) ===
  c27_b09.md: ...重试、操作期间领导者切换）。  **答案：** 单元测试通常：  (a) 在受控环境的单机上运行。(b) mock 或 stub 网络调用；不演练真实网络行为。(c) 不引入时间相关故障（时钟偏移、GC 暂停、重试以异常顺序到达）。(d) 不测试多...

=== generated (1 occurrences) ===
  c27_b11.md: ...部分故障处理、CP 注释但 stale read。  **答案：** 合理 LLM 生成代码：       # LLM-generated cross-region payment coordinator     # We use CockroachDB w...

=== which (1 occurrences) ===
  c27_b11.md: ...d cross-region payment coordinator     # We use CockroachDB which is CP for consistency      def process_payment(payment):...

=== explicitly (1 occurrences) ===
  c27_b12.md: ...辅助 PR 配对审查**：被标记为 “AI 辅助” 的 PR 需要两名工程师审查，至少一名是资深分布式系统工程师。配对 Explicitly 走一遍六个谎言。  (d) **测试要求**：分布式代码必须包含混沌测试（第23章），演练清单处理的故障模式。没有混沌...

=== bash (1 occurrences) ===
  c27_b12.md: ...级工程师每季度审计已合并分布式代码 PR 样本。发现与团队共享；模式级问题导致额外培训。  (g) **发布前 bug bash**：任何新分布式特性发布前进行混沌 game day（第23章）。Game day 演练该特性声称的防御。  (h)...

=== capped (1 occurrences) ===
  c27_b12.md: ...培训更新。  (i) **AI 辅助预算**：分布式代码允许但限制 AI 辅助。团队 “AI 辅助代码” 合并提交比例 capped（如 30%）。超过上限，团队被迫回到人工编写代码。  (j) **文档**：团队维护一份活的谎言、清单和实例文档。新员...

=== compensated (1 occurrences) ===
  c27_b13.md: ...可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部分失败？** 验证：半完成...

=== failed (1 occurrences) ===
  c27_b13.md: ...aga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。  (g) **Saga 是否正确处理部...

=== succeeded (1 occurrences) ===
  c27_b13.md: ...证：每个补偿可安全应用多次。  (f) **Saga 结果是否可观测？** 每个 saga 应产生 “started, succeeded” 或 “started, failed, compensated” 记录。验证：saga 生命周期被日志记录和可观测。...

=== naive (1 occurrences) ===
  c27_b14.md: ...辅助却没有考虑审查含义。  第二个考虑：LLM 生成代码质量随时间提升。2024 年 LLM 比 2026 年产出更 naive 的分布式代码；2028 年可能显著更好。政策应随技术演进；定期重新评估合理。  **Q2.** 一个工程师说 “我总是...

=== struggling (1 occurrences) ===
  c27_b14.md: ...AI 辅助。很少有团队完全禁止；许多团队按代码类型设不同政策。 thriving 的团队有明确政策和训练有素的审查者；struggling 的团队采用 AI 辅助却没有考虑审查含义。  第二个考虑：LLM 生成代码质量随时间提升。2024 年 LLM 比 2...

=== thriving (1 occurrences) ===
  c27_b14.md: ...的纪律。  2026 现实：大多数团队在某种程度上使用 AI 辅助。很少有团队完全禁止；许多团队按代码类型设不同政策。 thriving 的团队有明确政策和训练有素的审查者；struggling 的团队采用 AI 辅助却没有考虑审查含义。  第二个考虑：L...

=== ship (1 occurrences) ===
  c27_b15.md: ...橡皮图章是被点名的反模式。  第28章以四位主角的综合收尾：Olamide、Yuki、Tobias 和 Anya 一起 ship 多 region 支付平台。每章原语都会出现；工作工程师的心智模型是贯穿线。  ---...

=== pagerduty (1 occurrences) ===
  c28_b02.md: ...：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于...

=== prometheus (1 occurrences) ===
  c28_b02.md: ...每次交易欺诈检查 sub-100-ms p99。  (j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service...

=== alerts (1 occurrences) ===
  c28_b02.md: ...etry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发现；Envoy 用于 cluste...

=== dashboards (1 occurrences) ===
  c28_b02.md: ...j) **可观测性**：OpenTelemetry traces、Prometheus metrics、Grafana dashboards、PagerDuty alerts。  (k) **Service mesh**：Consul 跨 region 服务发...

=== kafkaw (1 occurrences) ===
  c28_b03.md: ...s Cluster]             CRDBW[CockroachDB nodes]             KAFKAW[Kafka brokers]         end         subgraph USW["US-West Re...

=== redisw (1 occurrences) ===
  c28_b03.md: ..."EU-West Region"]             APIW[API Service]             REDISW[Redis Cluster]             CRDBW[CockroachDB nodes]...

=== analytics (1 occurrences) ===
  c28_b03.md: ...onsul<br/>cross-region mesh]             SNOW[Snowflake<br/>analytics]             CONT[Continuum LLM<br/>fraud detection]...

=== limit (1 occurrences) ===
  c28_b03.md: ...Workers)"]             CF[API Gateway<br/>TLS / auth / rate limit]         end         subgraph USE["US-East Region"]...

=== completed (1 occurrences) ===
  c28_b04.md: ...int 验证 token（谎言 4 防御）。5 ms。  (11) API 服务向 Kafka 发布 “payment.completed” 事件。下游服务（通知、分析、账本对账）异步消费。5 ms。  (12) API 服务通过 Cloudflare 返回...

=== dashboard (1 occurrences) ===
  c28_b06.md: ...**：增加到 10%。继续 2 小时。  (d) **09:00 UTC**：增加到 50%。关键观察期。团队紧盯每个 dashboard。  (e) **10:30 UTC**：小异常。EU-West 的 p99 延迟上升 30 ms。调查：前一天晚上的数...

=== pitfalls (1 occurrences) ===
  c28_b09.md: ...统（第28章架构）。  教学大纲中的 12 个核心工作流全部出现。每个都作为一张卡片摘要在此总结；完整讲解在对应章节。"Pitfalls" 列列出事件响应最常查阅的调试时失败模式；这是读者在 incident 期间最常参考的列。  > 下表故意较宽（7 列...

=== figure (1 occurrences) ===
  c28_b10.md: ...| AppendEntries 一致性检查 + 多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 | | 5 | HotStuff 三阶段追踪 | Ch...

=== whys (1 occurrences) ===
  c28_b10.md: ...放大 + 传播 + 恢复 | 行动项：重试预算、熔断器、容量缓冲、runbook | 事后分析模板 + 结构防御的五 Whys | “我们增加了超时” 却不改变类别；遗漏亚稳态反馈环；指责导向复盘 | | 11 | Olamide 的混沌演练日...

=== append (1 occurrences) ===
  c28_b10.md: ...llower | 复制状态机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派提交 | append 时应用而非 commit 时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置...

=== coder (1 occurrences) ===
  c28_b11.md: ...eze 的支付，现在被正确处理，因为工程师学会了纪律。  2026 年的分布式系统工程师是内化了心智模型的人。他们不是 coder；他们是用会说谎的组件构建可信系统的架构师。他们是本书要培养的工作工程师。   ### 28.8 练习...

=== ide (1 occurrences) ===
  c28_b16.md: ...的防御’。”  **讨论：** 辩护：书中模式检测（六个谎言、诊断方法、审查清单）很适合 AI 工具。到 2030 年，IDE 插件或 PR 审查机器人可能标记模式（"这段代码有故障谎言；缺少幂等性"）。工程师角色从产出防御转向验证防御。  反驳...

=== oci (1 occurrences) ===
  c29_b01.md: ...2027 年会增加 AWS clock-bound 支持，作为 HLC 的可配置替代。 * **Azure** 和 **OCI** 可能在未来 2-3 年内增加类似 clock-bound 服务；云厂商在基础设施上竞争。 * 本地 **GPS +...

=== operating (1 occurrences) ===
  c29_b01.md: ...本书名为 _Distributed Systems: The 2026 Operating Manual_。具名系统（Spanner、CockroachDB、vLLM、Mooncake 等）、具名协议（Raft...

=== ptp (1 occurrences) ===
  c29_b01.md: ...* 可能在未来 2-3 年内增加类似 clock-bound 服务；云厂商在基础设施上竞争。 * 本地 **GPS + PTP** 基础设施变便宜；大型金融公司正在为关键任务工作负载部署自己的 TrueTime 等价基础设施。  含义：到 202...

=== expert (1 occurrences) ===
  c29_b02.md: ...ms 以下。 * **MoE（Mixture-of-Experts）** 模型成为 >100B 参数模型的默认，需要 expert-parallel routing 作为新原语（本书未深入覆盖；预计 Series IV Vol 4 会讲）。 * **...

=== inter (1 occurrences) ===
  c29_b02.md: ...典型工作负载首 token 延迟 100-300 ms。  2027-2030 轨迹：  * 生产交互式工作负载的 **inter-token latency（ITL）** 低于 10 ms 成为标准。这由投机解码改进（Medusa、EAGLE、Hy...

=== transformer (1 occurrences) ===
  c29_b02.md: ...* **基于扩散的语言模型**（2024-2025 研究前沿）到 2028 年可能成为生产现实；其推理特性不同于自回归 transformer，可能需要不同的分布式系统处理。  心智模型（GPU 是单元；两阶段计算；KV cache 是中心状态；严格 per-t...

=== birman (1 occurrences) ===
  c30_b01.md: ...istributed systems." _Journal of the ACM_. — 原子广播 ↔ 共识等价。 * Birman, K., et al. (1999). "Bimodal multicast." _ACM TOCS_. — 基于 g...

=== computing (1 occurrences) ===
  c30_b01.md: ...。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N...

=== corbett (1 occurrences) ===
  c30_b01.md: ...M. (2014). "Logical Physical Clocks." _OPODIS_. — HLC 论文。 * Corbett, J. C., et al. (2012). "Spanner: Google's Globally-Distribu...

=== design (1 occurrences) ===
  c30_b01.md: ...Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the Story." _IEEE Computer_. — PACELC...

=== eight (1 occurrences) ===
  c30_b01.md: ...Systems_ (3rd ed.). — 学术处理的教材基础。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2...

=== fallacies (1 occurrences) ===
  c30_b01.md: ...s_ (3rd ed.). — 学术处理的教材基础。 * Deutsch, P. (1994). "The Eight Fallacies of Distributed Computing." Sun Microsystems. — 第2章八大谬误的来源。...

=== globally (1 occurrences) ===
  c30_b01.md: ...HLC 论文。 * Corbett, J. C., et al. (2012). "Spanner: Google's Globally-Distributed Database." _OSDI_. — TrueTime 描述。   #### 广播与复制（...

=== modern (1 occurrences) ===
  c30_b01.md: ...— 形式化 CAP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only Part of the...

=== springer (1 occurrences) ===
  c30_b01.md: ...to Reliable and Secure Distributed Programming_ (2nd ed.). Springer. — 广播层级参考。 * Chandra, T. D., & Toueg, S. (1996). "Unreliabl...

=== steen (1 occurrences) ===
  c30_b01.md: ...ions_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanenbaum, A. S., & Van Steen, M. (2017). _Distributed Systems_ (3rd ed.). — 学术处理的教材基础。 *...

=== story (1 occurrences) ===
  c30_b01.md: ...Distributed Database System Design: CAP is Only Part of the Story." _IEEE Computer_. — PACELC 论文。 * Lamport, L. (1978). "Time...

=== tanenbaum (1 occurrences) ===
  c30_b01.md: ...Data-Intensive Applications_. O'Reilly. — 基础材料参考；建议并行阅读。 * Tanenbaum, A. S., & Van Steen, M. (2017). _Distributed Systems_ (3rd...

=== tradeoffs (1 occurrences) ===
  c30_b01.md: ...IGACT News_. — 形式化 CAP 证明。 * Abadi, D. (2012). "Consistency Tradeoffs in Modern Distributed Database System Design: CAP is Only P...

=== unreliable (1 occurrences) ===
  c30_b01.md: ...Springer. — 广播层级参考。 * Chandra, T. D., & Toueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journ...

=== conjecture (1 occurrences) ===
  c30_b01.md: ...— 第2章八大谬误的来源。 * Gilbert, S., & Lynch, N. (2002). "Brewer's conjecture and the feasibility of consistent, available, partition-tol...

=== detectors (1 occurrences) ===
  c30_b01.md: ...。 * Chandra, T. D., & Toueg, S. (1996). "Unreliable failure detectors for reliable distributed systems." _Journal of the ACM_. —...

=== feasibility (1 occurrences) ===
  c30_b01.md: ...lbert, S., & Lynch, N. (2002). "Brewer's conjecture and the feasibility of consistent, available, partition-tolerant web services."...

=== tolerant (1 occurrences) ===
  c30_b01.md: ...ure and the feasibility of consistent, available, partition-tolerant web services." _ACM SIGACT News_. — 形式化 CAP 证明。 * Abadi, D....

=== area (1 occurrences) ===
  c30_b02.md: ...t Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013). "St...

=== baquero (1 occurrences) ===
  c30_b02.md: ...ata." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N., Baquero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data...

=== beresford (1 occurrences) ===
  c30_b02.md: ...eplicated Data Types." _SSS_. — CRDT 框架。 * Kleppmann, M., & Beresford, A. R. (2017). "A Conflict-Free Replicated JSON Datatype."...

=== chang (1 occurrences) ===
  c30_b02.md: ...l. (1997). "Consistent Hashing and Random Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Distributed Storage System...

=== cheng (1 occurrences) ===
  c30_b02.md: ...for Structured Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured...

=== datatype (1 occurrences) ===
  c30_b02.md: ...& Beresford, A. R. (2017). "A Conflict-Free Replicated JSON Datatype." _IEEE TPDS_. — Automerge 基础。 * Kleppmann, M., Wiggins, A....

=== defining (1 occurrences) ===
  c30_b02.md: ...ACM_. — 部分同步形式化。 * Alpern, B., & Schneider, F. B. (1985). "Defining liveness." _Information Processing Letters_. — 安全性/活性分解。 *...

=== demers (1 occurrences) ===
  c30_b02.md: ..." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session...

=== don (1 occurrences) ===
  c30_b02.md: ..., Freedman, M. J., Kaminsky, M., & Andersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide...

=== gawlick (1 occurrences) ===
  c30_b02.md: ...red Data." _OSDI_. — 生产级 LSM-tree。 * O'Neil, P., Cheng, E., Gawlick, D., & O'Neil, E. (1996). "The log-structured merge-tree (L...

=== guarantees (1 occurrences) ===
  c30_b02.md: ..., A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_. — 五种 sessio...

=== hashing (1 occurrences) ===
  c30_b02.md: ...储（第四部分 — 第11-14章）  * Karger, D., et al. (1997). "Consistent Hashing and Random Trees." _STOC_. * Chang, F., et al. (2006). "Big...

=== hauser (1 occurrences) ===
  c30_b02.md: ...er, M. M., Petersen, K., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Re...

=== incremental (1 occurrences) ===
  c30_b02.md: ...Informatica_. * Peng, D., & Dabek, F. (2010). "Large-scale Incremental Processing Using Distributed Transactions and Notifications...

=== kogias (1 occurrences) ===
  c30_b02.md: ...nearity and Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tus...

=== kokoris (1 occurrences) ===
  c30_b02.md: ...with Linearity and Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal...

=== linearity (1 occurrences) ===
  c30_b02.md: ...G. G., & Abraham, I. (2019). "HotStuff: BFT Consensus with Linearity and Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias,...

=== low (1 occurrences) ===
  c30_b02.md: ..._SOSP_. * Lloyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== notifications (1 occurrences) ===
  c30_b02.md: ...e Incremental Processing Using Distributed Transactions and Notifications." _OSDI_. — Percolator。 * Thomson, A., et al. (2012). "Calv...

=== partitioned (1 occurrences) ===
  c30_b02.md: ..., et al. (2012). "Calvin: Fast Distributed Transactions for Partitioned Database Systems." _SIGMOD_.   #### 最终一致性与 CRDT（第五部分 — 第15-...

=== petersen (1 occurrences) ===
  c30_b02.md: ...buted systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994...

=== problem (1 occurrences) ===
  c30_b02.md: ...., Shostak, R., & Pease, M. (1982). "The Byzantine Generals Problem." _ACM TOPLAS_. — BFT 基础。 * Castro, M., & Liskov, B. (1999)...

=== protocols (1 occurrences) ===
  c30_b02.md: ...oSys_. * Spiegelman, A., et al. (2022). "Bullshark: DAG BFT Protocols Made Practical." _CCS_. * Buchman, E., Kwon, J., & Milosevi...

=== random (1 occurrences) ===
  c30_b02.md: ...-14章）  * Karger, D., et al. (1997). "Consistent Hashing and Random Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Di...

=== responsiveness (1 occurrences) ===
  c30_b02.md: ...ham, I. (2019). "HotStuff: BFT Consensus with Linearity and Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., &...

=== scalable (1 occurrences) ===
  c30_b02.md: ..., M., & Andersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_...

=== semantics (1 occurrences) ===
  c30_b02.md: ...e with COPS." _SOSP_. * Lloyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_. — Eiger。...

=== settle (1 occurrences) ===
  c30_b02.md: ...dman, M. J., Kaminsky, M., & Andersen, D. G. (2011). "Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Sto...

=== shapiro (1 occurrences) ===
  c30_b02.md: ...kly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Shapiro, M., Preguiça, N., Baquero, C., & Zawirski, M. (2011). "Con...

=== sonnino (1 occurrences) ===
  c30_b02.md: ...Responsiveness." _PODC_. * Danezis, G., Kokoris-Kogias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-base...

=== spreitzer (1 occurrences) ===
  c30_b02.md: ...Terry, D. B., Theimer, M. M., Petersen, K., Demers, A. J., Spreitzer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Wea...

=== stronger (1 occurrences) ===
  c30_b02.md: ...ea Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013). "Stronger Semantics for Low-Latency Geo-Replicated Storage." _NSDI_....

=== terry (1 occurrences) ===
  c30_b02.md: ...mining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K., Demers, A. J., Spreit...

=== theimer (1 occurrences) ===
  c30_b02.md: ...states of distributed systems." _ACM TOCS_. * Terry, D. B., Theimer, M. M., Petersen, K., Demers, A. J., Spreitzer, M. J., & Ha...

=== trees (1 occurrences) ===
  c30_b02.md: ...* Karger, D., et al. (1997). "Consistent Hashing and Random Trees." _STOC_. * Chang, F., et al. (2006). "Bigtable: A Distribu...

=== tusk (1 occurrences) ===
  c30_b02.md: ...ias, L., Sonnino, A., & Spiegelman, A. (2022). "Narwhal and Tusk: A DAG-based Mempool and Efficient BFT Consensus." _EuroSys...

=== weakly (1 occurrences) ===
  c30_b02.md: ...zer, M. J., & Hauser, C. H. (1994). "Session Guarantees for Weakly Consistent Replicated Data." _PDIS_. — 五种 session 保证。 * Sha...

=== wide (1 occurrences) ===
  c30_b02.md: ..."Don't Settle for Eventual: Scalable Causal Consistency for Wide-Area Storage with COPS." _SOSP_. * Lloyd, W., et al. (2013)...

=== zawirski (1 occurrences) ===
  c30_b02.md: ...五种 session 保证。 * Shapiro, M., Preguiça, N., Baquero, C., & Zawirski, M. (2011). "Conflict-Free Replicated Data Types." _SSS_. —...

=== determining (1 occurrences) ===
  c30_b02.md: ...handy, K. M., & Lamport, L. (1985). "Distributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry,...

=== faulty (1 occurrences) ===
  c30_b02.md: ...S. (1985). "Impossibility of distributed consensus with one faulty process." _Journal of the ACM_. — FLP 不可能性。 * Dwork, C., Ly...

=== partial (1 occurrences) ===
  c30_b02.md: ...N., & Stockmeyer, L. (1988). "Consensus in the presence of partial synchrony." _Journal of the ACM_. — 部分同步形式化。 * Alpern, B.,...

=== presence (1 occurrences) ===
  c30_b02.md: ..., C., Lynch, N., & Stockmeyer, L. (1988). "Consensus in the presence of partial synchrony." _Journal of the ACM_. — 部分同步形式化。 * A...

=== snapshots (1 occurrences) ===
  c30_b02.md: ...5-17章）  * Chandy, K. M., & Lamport, L. (1985). "Distributed snapshots: determining global states of distributed systems." _ACM TO...

=== states (1 occurrences) ===
  c30_b02.md: ...port, L. (1985). "Distributed snapshots: determining global states of distributed systems." _ACM TOCS_. * Terry, D. B., Theime...

=== synchrony (1 occurrences) ===
  c30_b02.md: ...tockmeyer, L. (1988). "Consensus in the presence of partial synchrony." _Journal of the ACM_. — 部分同步形式化。 * Alpern, B., & Schneide...

=== beyer (1 occurrences) ===
  c30_b03.md: ...os Engineering: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J., & Murphy, N. R. (2016). _Site R...

=== bitter (1 occurrences) ===
  c30_b03.md: ...coding." _ICML_.   #### 综合与 AI 生成代码（第九部分 — 第27-28章）  * "The Bitter Lesson" (Sutton, 2019) 及后续评论。 — 为什么模式匹配主导 LLM 行为。 * Crescen...

=== blackwell (1 occurrences) ===
  c30_b03.md: ...nce." _OSDI_. — 基础 BFT，结合 BFT 预测扩散到云控制平面重读（§29.1）。 * NVIDIA Blackwell (B100 / B200) 及 Hopper 继任者产品简介（2024-2026 发布）。 — sub-100-ms...

=== blog (1 occurrences) ===
  c30_b03.md: ...）  * Kleppmann, M. (2016). "How to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F....

=== clockbound (1 occurrences) ===
  c30_b03.md: ...on Web Services. (2024-2025). "Amazon Time Sync Service and ClockBound" 文档，包括 2024 年在 EC2 上引入微秒级有界时间。 — TrueTime 风格有界时钟正离开 Google、...

=== disaggregating (1 occurrences) ===
  c30_b03.md: ...g." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024). "DistServe: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _O...

=== execution (1 occurrences) ===
  c30_b03.md: ...SP_. — vLLM。 * Zheng, L., et al. (2023). "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAttention。 *...

=== failures (1 occurrences) ===
  c30_b03.md: ...ilera, M. K., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failures in Distributed Systems." _HotOS_. * Rosenthal, C., & Jones,...

=== fedus (1 occurrences) ===
  c30_b03.md: ...24-2026 发布）。 — sub-100-ms ITL 声明背后的硬件轨迹（§29.1，对回顾准确性有保留）。 * Fedus, W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers:...

=== generative (1 occurrences) ===
  c30_b03.md: ...ttention。 * Patel, P., et al. (2023). "Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zho...

=== goodput (1 occurrences) ===
  c30_b03.md: ...(2024). "DistServe: Disaggregating Prefill and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "...

=== hopper (1 occurrences) ===
  c30_b03.md: ...BFT 预测扩散到云控制平面重读（§29.1）。 * NVIDIA Blackwell (B100 / B200) 及 Hopper 继任者产品简介（2024-2026 发布）。 — sub-100-ms ITL 声明背后的硬件轨迹（§29.1，对回顾...

=== hunt (1 occurrences) ===
  c30_b03.md: ...w to do distributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKe...

=== itl (1 occurrences) ===
  c30_b03.md: ...(B100 / B200) 及 Hopper 继任者产品简介（2024-2026 发布）。 — sub-100-ms ITL 声明背后的硬件轨迹（§29.1，对回顾准确性有保留）。 * Fedus, W., Zoph, B., & Shazee...

=== junqueira (1 occurrences) ===
  c30_b03.md: ...ocking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordinati...

=== kvcache (1 occurrences) ===
  c30_b03.md: ...LM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moon...

=== konar (1 occurrences) ===
  c30_b03.md: ...stributed locking." Blog post. — 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-...

=== lesson (1 occurrences) ===
  c30_b03.md: ..." _ICML_.   #### 综合与 AI 生成代码（第九部分 — 第27-28章）  * "The Bitter Lesson" (Sutton, 2019) 及后续评论。 — 为什么模式匹配主导 LLM 行为。 * Crescent 事后复盘（...

=== management (1 occurrences) ===
  c30_b03.md: ...n, W., Li, Z., Zhuang, S., et al. (2023). "Efficient Memory Management for Large Language Model Serving with PagedAttention." _SOS...

=== models (1 occurrences) ===
  c30_b03.md: ...(2022). "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基础，作为前瞻话...

=== murphy (1 occurrences) ===
  c30_b03.md: ...Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J., & Murphy, N. R. (2016). _Site Reliability Engineering_. O'Reilly. —...

=== own (1 occurrences) ===
  c30_b03.md: ...g, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，作为 2...

=== parameter (1 occurrences) ===
  c30_b03.md: ...azeer, N. (2022). "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基...

=== patel (1 occurrences) ===
  c30_b03.md: ...of Structured Language Model Programs." — RadixAttention。 * Patel, P., et al. (2023). "Splitwise: Efficient Generative LLM In...

=== petoff (1 occurrences) ===
  c30_b03.md: ...Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J., & Murphy, N. R. (2016). _Site Reliability Engineering_...

=== programs (1 occurrences) ===
  c30_b03.md: .... "SGLang: Efficient Execution of Structured Language Model Programs." — RadixAttention。 * Patel, P., et al. (2023). "Splitwise:...

=== qin (1 occurrences) ===
  c30_b03.md: ...and Decoding for Goodput-optimized LLM Serving." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-centric Disaggrega...

=== reed (1 occurrences) ===
  c30_b03.md: ...— 对 Redlock 的批评。 * Hunt, P., Konar, M., Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet...

=== resiliency (1 occurrences) ===
  c30_b03.md: ...senthal, C., & Jones, N. (2020). _Chaos Engineering: System Resiliency in Practice_. O'Reilly. * Beyer, B., Jones, C., Petoff, J.,...

=== shazeer (1 occurrences) ===
  c30_b03.md: ...ms ITL 声明背后的硬件轨迹（§29.1，对回顾准确性有保留）。 * Fedus, W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers: Scaling to Trillion Param...

=== sparsity (1 occurrences) ===
  c30_b03.md: ...ling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR_. — MoE 基础，作为前瞻话题引用（§29.1；深度覆盖推迟到 _Software at Scal...

=== spite (1 occurrences) ===
  c30_b03.md: ...an, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，作为 2027-2030 主流采用预测重读（§2...

=== splitting (1 occurrences) ===
  c30_b03.md: ..."Splitwise: Efficient Generative LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024). "DistServe: D...

=== switch (1 occurrences) ===
  c30_b03.md: ...1，对回顾准确性有保留）。 * Fedus, W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers: Scaling to Trillion Parameter Models with Sim...

=== trillion (1 occurrences) ===
  c30_b03.md: ...B., & Shazeer, N. (2022). "Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity." _JMLR...

=== yan (1 occurrences) ===
  c30_b03.md: ...ems." _USENIX ATC_. * Bronson, N., Aguilera, M. K., Lu, X., Yan, B., & Xu, L. (2021). "Metastable Failures in Distributed S...

=== you (1 occurrences) ===
  c30_b03.md: ...nberg, P., & McGranaghan, M. (2019). "Local-First Software: You Own Your Data, in Spite of the Cloud." _Onward!_ — 2019 文章，...

=== zheng (1 occurrences) ===
  c30_b03.md: ...guage Model Serving with PagedAttention." _SOSP_. — vLLM。 * Zheng, L., et al. (2023). "SGLang: Efficient Execution of Structu...

=== zhong (1 occurrences) ===
  c30_b03.md: ...ive LLM Inference Using Phase Splitting." _ISCA_. — 分离论文。 * Zhong, Y., et al. (2024). "DistServe: Disaggregating Prefill and...

=== zoph (1 occurrences) ===
  c30_b03.md: ...。 — sub-100-ms ITL 声明背后的硬件轨迹（§29.1，对回顾准确性有保留）。 * Fedus, W., Zoph, B., & Shazeer, N. (2022). "Switch Transformers: Scaling to...

=== centric (1 occurrences) ===
  c30_b03.md: ...ng." _OSDI_. * Qin, R., et al. (2024). "Mooncake: A KVCache-centric Disaggregated Architecture for LLM Serving." — Moonshot AI...

=== coordination (1 occurrences) ===
  c30_b03.md: ...Junqueira, F. P., & Reed, B. (2010). "ZooKeeper: Wait-free coordination for Internet-scale systems." _USENIX ATC_. * Bronson, N., A...

=== reports (1 occurrences) ===
  c30_b03.md: ...lity Engineering_. O'Reilly. — 重试预算和熔断器作为生产纪律。 * The Jepsen reports (https://jepsen.io) — 分布式数据库正确性的经验测试；必读。 * 2003 Northeast B...

=== cascading (1 occurrences) ===
  c30_b06.md: ...onsistency（因果一致性）**：每次操作的一致性模型，因果相关操作按因果顺序可见；并发操作可任意排序。 * **Cascading failure（级联故障）**：通过放大（如重试风暴）跨组件传播的故障。 * **Choreographed saga...

=== compensating (1 occurrences) ===
  c30_b06.md: ...* **Circuit breaker（熔断器）**：当下游失败率超过阈值时打开（快速拒绝调用）的防御模式。 * **Compensating action（补偿动作）**：saga 中语义撤销先前已提交步骤的前进动作。很少是 “精确撤销”；通常恢复不变量（退款...

=== gray (1 occurrences) ===
  c30_b06.md: ...*：Fischer-Lynch-Paterson 1985。确定性共识协议在纯异步下有一个崩溃时不能保证终止。 * **Gray failure（灰色故障）**：进程通过健康检查但实际从应用视角已损坏的故障。 * **HLC（混合逻辑时钟）**：二...

=== rubber (1 occurrences) ===
  c30_b06.md: ...、安全性。 * **Retry budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping anti-pattern（橡皮图章反模式）**：人类审查者未加思考就批准 AI 审查过的 PR——把...

=== anti (1 occurrences) ===
  c30_b06.md: ...budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping anti-pattern（橡皮图章反模式）**：人类审查者未加思考就批准 AI 审查过的 PR——把 AI 的审查当作审查。命名...

=== human (1 occurrences) ===
  c30_b06.md: ...* **AI/human review split (60/40)**：2026 年 AI 生成代码审查职责的粗略划分。AI 捕获约 60%（语...

=== stamping (1 occurrences) ===
  c30_b06.md: ...**Retry budget（重试预算）**：重试流量占成功流量百分比上限；限制级联故障放大。 * **Rubber-stamping anti-pattern（橡皮图章反模式）**：人类审查者未加思考就批准 AI 审查过的 PR——把 AI 的审查当作...

=== canary (1 occurrences) ===
  c30_b08.md: ...ensor 文件）。 * **章节映射防御**：     * Ch.23（混沌工程，game-day 纪律）：部署应做 canary；"若 sensor 在数百万机器上崩溃" 场景应作为 game day。     * Ch.21（级联故障，部署作为触...

=== malformed (1 occurrences) ===
  c30_b08.md: ...2024-07-19  * **触发**：CrowdStrike Falcon 内核模式 sensor 的内容更新包含 malformed 数据，触发 Windows 内核越界内存读取；读取导致蓝屏。 * **放大**：更新同时推送到数百万台 Windows...

=== varnish (1 occurrences) ===
  c30_b09.md: ...ly 全球 CDN 故障，2021-06-08  * **触发**：客户推送了一个有效配置，触发了 Fastly 基于 Varnish 的 CDN 平台中此前未知的软件 bug。该 bug 导致配置解析器在特定输入模式上崩溃。 * **放大**：坏配置被...

=== fix (1 occurrences) ===
  c30_b09.md: ...27（审查清单）：bug 修复应被审查 "跨区域回滚时会发生什么"；未覆盖到 failover-without-the-fix 场景。   #### E.7 — Fastly 全球 CDN 故障，2021-06-08  * **触发**：客户推送...

=== without (1 occurrences) ===
  c30_b09.md: ...助。     * Ch.27（审查清单）：bug 修复应被审查 "跨区域回滚时会发生什么"；未覆盖到 failover-without-the-fix 场景。   #### E.7 — Fastly 全球 CDN 故障，2021-06-08  * **触...

=== iops (1 occurrences) ===
  c30_b10.md: ...偏好 MySQL 生态，可用类似复制的 MySQL；AWS 上 Aurora PostgreSQL 用于托管简单，但每 IOPS 计费更高。   #### F.2 — 面向全球用户的多区域 OLTP  **输入**：全球用户；部分操作需跨 regi...

=== affinity (1 occurrences) ===
  c30_b10.md: ...局表做跨 region 复制。 * **为何这个组合**：CockroachDB 的 per-table region affinity 匹配地理分区数据；TigerBeetle 的专用正确性模型是财务完整性的正确工具。 * **替代**：若需 MySQL...

=== hot (1 occurrences) ===
  c30_b10.md: ...加读副本 + Redis 缓存。  * **Operational 存储**：PostgreSQL 16，带一或两个 hot-standby 读副本（同步到一个，异步到另一个）。 * **缓存**：Redis Cluster 用于热读路径（TT...

=== beam (1 occurrences) ===
  c30_b11.md: ...择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQL；Beam 用于跨 runner 可移植性。   #### F.5 — 大规模分布式 LLM 推理（月推理预算 10 万美元以上）...

=== confluent (1 occurrences) ===
  c30_b11.md: ...roachDB，幂等写入；事务 sink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序...

=== materialize (1 occurrences) ===
  c30_b11.md: ...的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQL；Beam 用于跨 runner 可移植性。   #### F.5 — 大规模分布式 LLM...

=== memcached (1 occurrences) ===
  c30_b11.md: ...* **CDN**：Cloudflare 或 Fastly 用于静态内容路径。 * **缓存**：区域 Redis 或 Memcached 用于热读。 * **一致性策略**：per-operation 选择；内容读用最终一致，唯一声明操作（用户名保留等）用...

=== pulsar (1 occurrences) ===
  c30_b11.md: ...on 排序是正确原语；Flink 的 exactly-once 成熟；sink 选择取决于下游需求。 * **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQL；Beam 用于跨 runner 可移植性。   #...

=== registry (1 occurrences) ===
  c30_b11.md: ...ink 与 Flink checkpoint 协调。 * **Schema 管理**：Confluent Schema Registry 或等价物。 * **为何这个组合**：Kafka 的 per-partition 排序是正确原语；Flink 的 ex...

=== broker (1 occurrences) ===
  c30_b11.md: ...ka + Apache Flink + Postgres 或 CockroachDB sink。  * **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor**：Apach...

=== processor (1 occurrences) ===
  c30_b11.md: ...* **Event broker**：Kafka（或更轻量的 NATS JetStream）。 * **Stream processor**：Apache Flink（通过 Chandy-Lamport 快照实现 exactly-once，第15章）或 S...

=== runner (1 occurrences) ===
  c30_b11.md: ...* **替代**：Pulsar 替代 Kafka；Materialize 用于物化视图式流 SQL；Beam 用于跨 runner 可移植性。   #### F.5 — 大规模分布式 LLM 推理（月推理预算 10 万美元以上）  **输入**：每日...

=== home (1 occurrences) ===
  c30_b12.md: ...要伸手选 Spanner 风格全局线性化。** 大多数以用户为中心的工作负载（聊天、社交、生产力）可通过将每个用户数据 home 在一个 region 来服务。全局线性化是最昂贵的一致性级别；只有当工作负载真正需要所有用户的全局排序时才支付它。...

=== pain (1 occurrences) ===
  c30_b12.md: ..._不_ 选诱人的系统。以下是 Olamide 团队在新晋工程师身上看到的最常见 2026 反模式：  **刚遇到扩展 pain 就不要伸手选 NewSQL。** 一个团队 PostgreSQL 在 50k QPS 出现 p99 延迟问题，通常不需...

=== hybrid (1 occurrences) ===
  c30_b14.md: ...— Vector clocks (small N)     │   └── hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04...

=== applied (1 occurrences) ===
  c30_b14.md: ...└── good_distributed_code.py — same workflow with defenses applied        #### 使用仓库  每章目录包含：  * 一个 `README.md` 链接回本书章节，并命名代码演示...

=== automated (1 occurrences) ===
  c30_b14.md: ...r     └── ch27_review/         ├── ai_review_checklist.py — automated checks for the six-lies patterns         └── examples/...

=== bus (1 occurrences) ===
  c30_b14.md: ...choreographed.py  — choreographed saga via in-memory event bus     │   └── crescent_payment_saga.py — the Ch.13 §13.6.5 wo...

=== checks (1 occurrences) ===
  c30_b14.md: ...ch27_review/         ├── ai_review_checklist.py — automated checks for the six-lies patterns         └── examples/...

=== code (1 occurrences) ===
  c30_b14.md: ...└── examples/             ├── bad_distributed_code.py  — code with all six lies (the §27.2-§27.7 examples)             └─...

=== defenses (1 occurrences) ===
  c30_b14.md: ...└── good_distributed_code.py — same workflow with defenses applied        #### 使用仓库  每章目录包含：  * 一个 `README.md` 链接回本书章节...

=== demonstration (1 occurrences) ===
  c30_b14.md: ...redlock_naive.py  — the wrong way (no fencing tokens) — for demonstration     │   ├── etcd_lock.py      — consensus-backed lock via e...

=== enforces (1 occurrences) ===
  c30_b14.md: ...rary     │   └── fencing_token_demo.py — protected resource enforces monotonic tokens     ├── ch20_tracing/     │   └── propagat...

=== example (1 occurrences) ===
  c30_b14.md: ...│   └── crescent_payment_saga.py — the Ch.13 §13.6.5 worked example     ├── ch16_crdt/     │   ├── g_counter.py      — G-Counte...

=== four (1 occurrences) ===
  c30_b14.md: ...hlc.py            — Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/     │   ├── best_effort.p...

=== library (1 occurrences) ===
  c30_b14.md: ...─ etcd_lock.py      — consensus-backed lock via etcd client library     │   └── fencing_token_demo.py — protected resource enfo...

=== limiter (1 occurrences) ===
  c30_b14.md: ...reaker     │   └── load_shedder.py   — adaptive concurrency limiter     ├── ch24_inference/     │   └── continuous_batching_toy...

=== lines (1 occurrences) ===
  c30_b14.md: ...cks     ├── ch09_raft/     │   ├── raft_minimal.py   — ~300 lines: leader election + AppendEntries + commit     │   ├── kv_st...

=== patterns (1 occurrences) ===
  c30_b14.md: ...ai_review_checklist.py — automated checks for the six-lies patterns         └── examples/             ├── bad_distributed_code....

=== propagation (1 occurrences) ===
  c30_b14.md: ...ng/     │   └── propagation_demo.py — OpenTelemetry context propagation across HTTP + Kafka     ├── ch21_resilience/     │   ├── re...

=== protected (1 occurrences) ===
  c30_b14.md: ...via etcd client library     │   └── fencing_token_demo.py — protected resource enforces monotonic tokens     ├── ch20_tracing/...

=== resource (1 occurrences) ===
  c30_b14.md: ...lient library     │   └── fencing_token_demo.py — protected resource enforces monotonic tokens     ├── ch20_tracing/     │   └──...

=== rule (1 occurrences) ===
  c30_b14.md: ...— Hybrid Logical Clock per Kulkarni 2014 four-case rule     ├── ch04_broadcast/     │   ├── best_effort.py    — bes...

=== same (1 occurrences) ===
  c30_b14.md: ...-§27.7 examples)             └── good_distributed_code.py — same workflow with defenses applied        #### 使用仓库  每章目录包含：  *...

=== scenarios (1 occurrences) ===
  c30_b14.md: ...└── tests/            — split-brain, partition, recovery scenarios     ├── ch12_lsm/     │   └── tiny_lsm.py       — minimal L...

=== tests (1 occurrences) ===
  c30_b14.md: ..._machine.py — replicated key-value store on top     │   └── tests/            — split-brain, partition, recovery scenarios...

=== timestamps (1 occurrences) ===
  c30_b14.md: ...├── ch03_clocks/     │   ├── lamport.py        — Lamport timestamps     │   ├── vector.py         — Vector clocks (small N)...

=== way (1 occurrences) ===
  c30_b14.md: ...├── ch18_locks/     │   ├── redlock_naive.py  — the wrong way (no fencing tokens) — for demonstration     │   ├── etcd_lo...

=== worked (1 occurrences) ===
  c30_b14.md: ...us     │   └── crescent_payment_saga.py — the Ch.13 §13.6.5 worked example     ├── ch16_crdt/     │   ├── g_counter.py      —...

=== common (1 occurrences) ===
  c30_b15.md: ...### 附录 H — 二十五种常见误解  每个正文章节都有一个 “Common Misconception” 标注。本附录整合其中 25 种最常见且操作性强的误解，各附一句反驳。在批准分布式代码 P...

=== misconception (1 occurrences) ===
  c30_b15.md: ...### 附录 H — 二十五种常见误解  每个正文章节都有一个 “Common Misconception” 标注。本附录整合其中 25 种最常见且操作性强的误解，各附一句反驳。在批准分布式代码 PR 前，用它作为预审查清单。...

=== applications (0 occurrences) ===

=== building (0 occurrences) ===

=== microservices (0 occurrences) ===

=== observability (0 occurrences) ===

=== programming (0 occurrences) ===

=== restful (0 occurrences) ===

=== understanding (0 occurrences) ===

=== display (0 occurrences) ===

=== init (0 occurrences) ===

=== processes (0 occurrences) ===

=== upper (0 occurrences) ===

=== introduction (0 occurrences) ===

=== fff (0 occurrences) ===

=== objective (0 occurrences) ===

=== handle (0 occurrences) ===

=== min (0 occurrences) ===

=== chicago (0 occurrences) ===

=== live (0 occurrences) ===

=== committed (0 occurrences) ===

=== messages (0 occurrences) ===

=== ranges (0 occurrences) ===

=== abc (0 occurrences) ===

=== ref (0 occurrences) ===

=== eastp (0 occurrences) ===

=== dynamodbper (0 occurrences) ===

=== compacted (0 occurrences) ===

=== resilience (0 occurrences) ===

=== cached (0 occurrences) ===

=== generate (0 occurrences) ===

=== unique (0 occurrences) ===

=== critical (0 occurrences) ===

=== authoritative (0 occurrences) ===

=== notification (0 occurrences) ===

=== mock (0 occurrences) ===

=== manual (0 occurrences) ===

=== communications (0 occurrences) ===

=== ieee (0 occurrences) ===

=== journal (0 occurrences) ===

=== news (0 occurrences) ===

=== opodis (0 occurrences) ===

=== tocs (0 occurrences) ===

=== atc (0 occurrences) ===

=== acta (0 occurrences) ===

=== ccs (0 occurrences) ===

=== eurosys (0 occurrences) ===

=== informatica (0 occurrences) ===

=== information (0 occurrences) ===

=== letters (0 occurrences) ===

=== nsdi (0 occurrences) ===

=== onward (0 occurrences) ===

=== pdis (0 occurrences) ===

=== pregui (0 occurrences) ===

=== sigmod (0 occurrences) ===

=== sosp (0 occurrences) ===

=== sss (0 occurrences) ===

=== stoc (0 occurrences) ===

=== toplas (0 occurrences) ===

=== tpds (0 occurrences) ===

=== usenix (0 occurrences) ===

=== arxiv (0 occurrences) ===

=== hotos (0 occurrences) ===

=== icml (0 occurrences) ===

=== isca (0 occurrences) ===

=== jmlr (0 occurrences) ===

=== practice (0 occurrences) ===

=== bad (0 occurrences) ===

=== demo (0 occurrences) ===

=== locks (0 occurrences) ===

=== machine (0 occurrences) ===

=== shedder (0 occurrences) ===

=== tiny (0 occurrences) ===

=== toy (0 occurrences) ===

=== publication (0 occurrences) ===

=== qbooks (0 occurrences) ===

