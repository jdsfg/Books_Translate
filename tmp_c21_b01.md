随着模型和网络改进而改善）和供应商阵容——OpenAI Realtime、Gemini Live、ElevenLabs Conversational AI、Cartesia、Deepgram。底层的交互模式（流式、轮次交替、打断处理）是人类对话的属性，不是你用哪个提供商的属性，将比两者都持久。

语音 AI 在 2024 年是 LLM 生态的一个专业角落；到 2026 年，它是真实的生产能力。Helios Customer AI 正在推出语音增强客户支持：客户可以拨打一个电话号码并与由支持文本 agent 的同一基础设施支撑的 AI agent 交谈。架构在根本上不同于基于文本的 AI：从文本 AI 角度看似不可能的延迟预算；不再是可选的流式；完全不适用于文本的轮次交替和打断处理。

本章走查生产语音栈。语音有自己的工程面；本章是面向来自文本工作的 AI 工程师对该面的介绍。绑定工作流 #13 是带轮次交替、打断和回退到文本的语音 agent 循环。

### 20.1 2026 年语音栈

2026 年的语音栈有几种架构模式和清晰的供应商格局。

**模式 1：STT → LLM → TTS 流水线**（传统流水线）。三个独立模型：

* **STT（语音转文本）**：将用户口语音频转为文本。示例：Deepgram、AssemblyAI、OpenAI Whisper、Google Speech-to-Text。
* **LLM**：基于文本的 LLM（Sonnet、GPT-4o、Gemini）处理文本并产生文本响应。
* **TTS（文本转语音）**：将 LLM 的文本响应转为音频。示例：ElevenLabs、Cartesia、Azure TTS、Google TTS。

每步是独立 API 调用；延迟跨三步累积；集成是工程工作。

**模式 2：端到端语音模型**（2024-2026 年的转向）。单一模型接收音频输入并产生音频输出，推理在统一表示上内部发生：

* **OpenAI Realtime**（2024 年发布；2025 年成熟）。端到端语音，亚 500ms 首音频时间。
* **Google Gemini Live**。类似能力；与 Gemini 家族集成。

端到端语音模型简化架构（一次调用而非三次）但降低灵活性（你不能替换个别组件）。权衡到 2026 年已稳定：延迟关键的对话层用端到端模型；需要组件级控制时用 STT-LLM-TTS 流水线。

**模式 3：混合**。一些团队用端到端语音做对话但用显式 STT 做转录和归档、用显式文本 LLM 做工具增强推理，按需重组。

2026 年新语音 AI 部署的默认选择：**对话层用端到端（OpenAI Realtime 或 Gemini Live）；转录归档用 STT；工具执行用文本 LLM；仅端到端不可用时用 TTS**。
