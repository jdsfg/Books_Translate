1. **失败会发生**。即使可靠的语音服务也偶尔有事故。无回退，每次事故在对话中途失去客户。
2. **一些边缘案例语音模型处理不好**。长检索、复杂工具序列、仔细策略决策由文本 LLM 路径更好处理。将这些路由到文本+TTS 给系统优雅降级路径。
3. **成本优化**。回退路径可对一些流量用更便宜模型；团队可基于通话复杂度分类路由。

回退不保持与主语音相同的延迟（文本+TTS 比端到端语音慢），但它保持 _某事_：用户得到答案而非坏对话。权衡可接受。

**Q4.** 为什么本章论证语音 AI 是与文本 AI "不同的工程学科"？识别三个具体差异。

**答案：** 三个差异：

1. **延迟预算紧一个数量级**。文本 AI 容忍 1-3 秒 TTFT；语音要求亚 500ms。满足文本延迟预算的架构对语音不工作；语音需要一切流式、并行工具执行、预生成和端到端模型。
2. **轮次交替和打断是一等关注**。文本 AI 是轮次制且干净的（用户消息 → agent 响应 → 用户消息）。语音是重叠的、可打断的、韵律的。agent 必须处理停顿、打断、修复序列（"抱歉，我是说……"）。这些在文本中不存在。
3. **流式是强制的，非可选的**。文本 AI 可非流式（一些应用批处理响应）；语音必须从输入的第一个 token 到输出的最后一个 token 流式。架构在根本上是流式优先的。

值得提及的第四个差异：**模态切换重要**。语音用户措辞不同于文本用户（更短、更口语、更易打断）；prompt 和行为必须适配。文本 AI prompt 不会不经调整就翻译到语音。

一般教训：语音和文本 AI 共享原语（LLM、工具目录、eval 纪律）但在架构层不同。将语音视为新工程工作，非文本 AI 的配置变更。

**Q5.** 第 16 章的 9 层参考架构在文本系统上下文中呈现。论证同样 9 层适用于语音系统，识别语音类比与文本类比实质不同的任何层。用第 16 章的词汇框架答案。

**答案：** 9 层以扩展迁移，非替换。逐层比较：

1. **模型提供商**：语音扩展到 STT 提供商（Deepgram、AssemblyAI）、TTS 提供商（ElevenLabs、OpenAI TTS）和端到端语音模型（OpenAI Realtime、Gemini Live）。相同"购买"推荐；相同商品化。提供商列表更长。
2. **路由器/网关**：语音系统需跨 STT/TTS 提供商路由（延迟层、语言覆盖）以及 LLM 路由。路由器层职责扩展；层概念相同。
3. **编排**：语音编排添加轮次交替、打断和模态切换逻辑，文本编排没有。编排模式是语音特定的（见 §20.6 工作流 #13）；层位置不变。
