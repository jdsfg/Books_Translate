### 11.6 Triangle Trade-off 侧栏：高级 RAG 决策（经典格式）

模式 | 质量提升（典型） | 成本增量 | 延迟增量 | 最适合
---|---|---|---|---
重排序器（Cohere/BGE） | +10 到 +20 分 | +0.001 美元/查询 | +200–400ms | 几乎所有生产 RAG
查询扩展 | +3 到 +5 分 | +小 LLM 调用（0.0001 美元） | +200–500ms | 改述查询
HyDE | +3 到 +7 分 | +小 LLM 调用（0.0001 美元） | +200–500ms | 问题型查询
查询分解 | 复杂查询 +5 到 +10 分 | +1 LLM 调用 + N 次检索 | +500–1000ms | 多部分查询
自查询检索器 | 元数据丰富查询 +5 到 +15 分 | +小 LLM 调用 | +200–500ms | 有丰富元数据的语料库
KG-RAG | 领域依赖（关系密集领域常 +5–10） | 大量（图谱维护） | +200–500ms | 有结构化关系的领域

三件要解读的事：

1. **重排序器是高级 RAG 中最高 ROI 的举措**：+10–20 分，低成本适中延迟。如果你没用重排序器，那是首先要加的。
2. **查询模式（扩展、HyDE、分解、自查询）便宜且可选应用。** 使用路由层将特定模式应用到特定查询类型；不要对所有查询应用所有模式。
3. **KG-RAG 是最重投资**；仅在关系结构确实是价值主张时采用。

### 11.7 法律技术细节：引用接地、管辖权过滤、时间有效性

Strata Research 的领域有三个特定约束，映射到通用 RAG 模式但值得命名。

#### 带段落哈希匹配的引用接地

在法律 RAG 中，答案的引用必须指向源文档的 _确切段落_。引用正确的 _案例_ 但错误的 _段落_ 是真实故障——依赖引用的律师可能验证错误文本。

实现：索引中每个块有 `paragraph_hash`（段落文本的确定性哈希）。LLM 按 chunk-id 引用（第 6 章）；后处理器将 chunk-id 映射到段落哈希并解析为规范 URL。系统从不信任 LLM 从散文构造引用 URL。

#### 管辖权过滤

加州案例法在纽约法院无约束力。联邦先例通常适用；州先例不跨管辖权。检索必须尊重此。

实现：每个块有 `jurisdiction` 元数据。自查询检索器（§11.2 模式 4）解析用户查询的管辖权；检索在向量搜索前过滤到适当管辖权。LLM 的 prompt 显式声明适用管辖权并要求答案管辖权正确。
