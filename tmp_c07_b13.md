1. **eval 集是什么样的，评分函数有多稳定？** DSPy 的价值来自 MIPRO 对评分函数优化。如果 Strata 的 eval 是 §6.6 的多轴量表（准确率、引用基础、管辖区正确性、时间有效性），评分丰富但主要是 LLM 作为评判者；MIPRO 可以对它优化但每次优化运行花费许多评估器调用。如果 eval 小（比如 80 个查询）且稳定，DSPy 可以做有意义的工作。如果 eval 每个 sprint 变化，优化器输出很快过时且编译成本摊销不佳。_推向 DSPy_：稳定的、良好定义的 200+ 查询 eval。_推向注册表_：频繁演变的 eval，人工 prompt 作者轻松跟上。

2. **生产调试中谁读 prompt？** 当客户投诉答案时，工程师拉出产生它的 prompt。用注册表，那个 prompt 是特定版本的人类可读文件。用 DSPy，_编译的_ prompt 可能不透明（机器生成的措辞，可能有任意的少样本示例选择）；_签名_ 可读但 LLM 看到的实际文本距一次编译步骤。_推向 DSPy_：对阅读编译制品并将签名视为真相来源感到舒适的工程文化。_推向注册表_：需要按需获取确切 prompt 文本的调试工作流，或对所写 prompt 的利益相关者审查（合规、法律）。

3. **编译循环在你的开发周期中成本是多少？** DSPy 的编译步骤通常需要几分钟到几小时，取决于优化器设置。如果 prompt 变更需要在一小时内发布（热修复领域），编译开销是阻碍；你无论如何会回退到手动编辑热修复，削弱 DSPy 投资。_推向 DSPy_：每周或每月 prompt 改进节奏，编译适配 CI。_推向注册表_：当天迭代节奏，手动编辑是主导模式。

对 Strata 的具体净指导：注册表以已知成本满足质量目标。迁移到 DSPy 添加基础设施并改变调试表面；迁移的理由需要注册表无法达到的特定提升（例如，在 Strata 租户数量规模上的每客户 prompt 优化）。没有那个提升，_不要迁移正在工作的东西_。注册表继续；DSPy 可能因具有正确形态的未来功能而赢得位置。
