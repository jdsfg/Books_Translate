* 仅检索故障：检索 recall@10 为 0.62（低）；模型答错因为它从未有正确上下文。修检索。
* 仅 LLM 故障：检索 recall@10 为 0.90（好）；模型仍幻觉因为 prompt 不强制接地。修 prompt 或模型。
* 组合故障：检索平庸且 LLM 不补偿。需要修两者。

没有组件级 eval（检索），你无法判断该修哪层。没有端到端 eval，你不知道检索改进是否实际转化为更好答案。两层都需要用于诊断。

**Q4.** 为什么本章论证 Google AI Overviews 故障根本上是关于 _检索_，而非关于 _LLM_？在检索层什么会阻止它？

**答案：** 故障是：模型产生了自信、语法正确、语义连贯的答案，合成了检索内容。模型做了它的工作；它将答案基于检索块，完全按指令。块恰好包含一个笑话。模型无法知道笑话是笑话；它将块视为事实。

如果 LLM 更有能力，故障模式会相同。块是问题。前沿模型拿到 _Reddit 笑话内容_ 并被要求合成答案会从笑话合成答案。模型在检索下游；如果检索错误，模型完全自信地产生错误输出。

在检索层的预防意味着：来源质量加权（论坛帖子对事实查询降权 vs 权威来源）；来源允许列表（不对高风险输出从低信任来源检索）；检索内容审计 eval（捕获检索内容来自不应存在的来源的情况）。三者现在在生产 RAG 中是标准的；Google 案例使它们成为行业基线。

这是 RAG 工程的核心教训：**LLM 在检索下游。检索是工程努力赚取回报的地方。**

**Q5.** _为特定语料库类型选择分块策略_。对每个语料库，指出正确的分块策略并解释为什么。然后指出最常见的 _错误_ 策略及其产生的故障模式。

(a) Markdown 格式的内部 API 参考文档（1500 个端点，每个有参数、返回、示例文档）。(b) 法律案例法（PDF，多页意见带引用、批注、异议）。(c) 客服聊天记录（客户和 agent 间的多轮对话）。(d) 科学论文（PDF 带章节、图表、方程、参考文献）。

提示

§9.3 杠杆 1 说正确答案是结构感知。对每个，存在什么结构，用户查询的目标单元是什么？

**答案：**

(a) **每个端点一个块**（在 markdown 标题上结构感知）。每个端点的文档形成自包含单元；用户查询特定端点并希望完整端点文档作为检索单元。块包含端点名、参数、返回和示例一起；拆分它们无益。典型块大小：800 到 2500 token 取决于端点复杂性。
