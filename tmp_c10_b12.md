_错误策略_：固定大小 500 token 块，忽略 markdown 结构。这将参数从返回从示例中拆分；检索呈现部分块；LLM 获取参数但不获取示例并合成不完整答案。修复是按标题结构感知分块。

(b) **每节一个块带重叠，加批注的单独块**（在法律文档约定上结构感知）。批注是应成为自己可检索单元的摘要元数据；章节（意见、异议、协同意见）是用户查询的实质单元。章节内的引用保持内联；引用提取层可选地为每个引用案例产生单独块，带回链到其来源章节。

_错误策略_：固定大小块忽略意见 vs 异议结构。关于 _多数意见持什么_ 的查询可能因为异议引用多数意见进行反驳而检索到异议中的块；LLM 对谁说了什么困惑。修复：按章节角色分块；用 `section_role` 元数据标记每个块。

(c) **短对话每对话一个块，否则每个主题边界段一个块**（在对话主题转变上结构感知）。典型 5 轮对话适合一个块；长对话（20+ 轮覆盖多主题）在主题边界拆分，由轻量分类器或说话者轮次变化模式检测。每个块保留足够轮次上下文使对话自解释。

_错误策略_：每轮一块。每轮单独无意义；检索呈现脱离上下文的单轮；LLM 无法重建对话。修复：对话或段级分块。

(d) **章节感知分块带图表和方程块分离**（在论文结构加内容类型上结构感知）。摘要、引言、方法、结果、讨论形成各自块（或长章节的块组）。图表和方程成为各自块带其标题，使图表相关查询专门检索图表块。参考文献是单独块类型。章节间交叉引用保留为元数据，允许后续检索（第 13 章工具使用模式）。

_错误策略_：段落级分块将方程从其上下文设置散开。查询 _他们如何推导方程 3？_ 可能仅检索方程而无设置段落；LLM 无法重建推导。修复：将方程与其介绍和解释段落作为单元保持。

通用原则（值得跨所有四个案例携带）：**块边界应匹配文档中的意义单元，而非任意 token 计数**。Token 计数是约束；文档结构是信号。将分块器调到文档，而非将分块器调到平均块大小。
