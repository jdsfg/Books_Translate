### L

**Langfuse**：开源 LLM 可观测性平台，追踪、eval 集成和 prompt 管理强。自托管可观测性的常见默认（第 19 章，§19.3）。

**LangGraph**：LangChain 的基于图的 agent 编排框架。agent 工作流的状态机（第 15 章，§15.4）。

**后期交互**：检索方法（如 ColBERT、ColPali），将相似性计算推迟到查询时的 token 级对。比双编码器更准确、更贵（第 12 章，§12.3）。

**延迟预算**：面向用户响应允许的端到端时间，按阶段（网络、检索、LLM、后处理）分解。显式分配（第 20 章，§20.2）。

**长度偏差**：LLM judge 系统性偏好更长响应。通过长度控制评分标准或成对 prompt 缓解（第 5 章，§5.6）。

**LiteLLM**：开源代理，将 100+ 提供商 API 规范化为 OpenAI 兼容接口。自建网关基线（第 17 章，§17.3）。

**Llama**：Meta 的开放权重模型家族，2026 年开源 AI 的事实参考（第 22 章，§22.1）。

**llama.cpp**：为 CPU 和消费级 GPU 优化的 C++ 推理引擎。驱动多数本地 AI 工具（第 22 章，§22.6）。

**LLM-as-judge**：使用 LLM 对照评分标准评分输出。比人工 eval 便宜，但受位置、长度和家族偏差影响（第 5 章，§5.5）。

**Logit**：模型分配给每个可能下一 token 的 pre-softmax 分数。直接 logit 访问启用自定义采样器和置信度代理（第 2 章，§2.5）。

**长期记忆**：跨会话持久化的 agent 记忆，通常外部存储（向量存储、KG、SQL）并按需检索（第 14 章，§14.5）。

**LoRA**：低秩适配。在冻结基础权重旁学习小秩分解矩阵的微调。主导的成本高效微调方法（第 22 章，§22.3）。

**中间丢失**：LLM 对长上下文中间内容关注较少的经验发现。论证将最重要材料放在开头或结尾（第 8 章，§8.5）。

**潜空间**：嵌入所在的高维向量空间。运营上，"在潜空间中移动"意味着通过查询重写或微调改变嵌入（第 10 章，§10.1）。
