#### 概念检查

**Q1.** 一位初级工程师说："我会把 temperature 设为 0，这样我的 eval 套件在跨运行时完全可复现。"你会给这个计划加什么两个警告，你如何缓解它们？

提示

一个警告是关于模型在恰好 temperature 0 时的行为。另一个是关于 _可复现_ 在底层基础设施的不同版本间意味着什么。

**答案：** 两个警告：

1. **temperature 0 是近确定性的，不是严格确定性的。** 浮点操作的 GPU 级非结合性可能导致模型在两个候选非常接近时偶尔选择不同的最高概率 token。缓解措施是在 temperature 0 下多次运行 eval（比如 N=3）并验证输出在几乎所有情况下都匹配；将罕见分歧视为预期行为，要么报告一个小的方差预算，要么使用一个容忍不改变语义内容的 token 级交换的比较指标。
2. **模型版本在你底下变化。** provider 可以发布 Sonnet 4 的一个小更新（比如从 Sonnet 4 到 Sonnet 4-2026-04-15）而不在你的 API 调用中重命名模型。如果你不固定到特定日期版本的模型，你的"可复现"eval 可能不同日期产生不同结果。缓解措施是在 eval 配置中显式固定到日期版本模型，将任何模型版本变更视为触发完整 eval 重运行的发布事件，并在版本控制中与 prompt 一起快照日期版本。第 21 章将把这个变成完整的模型升级流程。

将 temperature 设为 0 的计划是必要的但不充分的。加上：(a) eval 级别的多次运行平均；(b) 显式模型版本固定；(c) 将 eval 视为（prompt、模型版本、系统上下文）的函数，并对三者进行版本控制。

**Q2.** 一位队友解释说模型"知道"某件事因为被问到时它回答正确了。用 §2.1–2.3 的心智模型重新框架这件事。实际发生了什么，这对你是否应该在生产中依赖那个知识有什么含义？

**答案：** 队友的框架是 _知识即检索_ 的误解。实际发生的是模型发射了一串构成正确答案的 token，因为以问题为条件的下一个 token 分布尖锐地峰化在拼出正确答案的 token 上。这 _不_ 等同于模型有一个它查询的已验证事实数据库。模型的"知识"是统计性的：它是训练语料库对相关查询产生足够强预测所强化的任何东西。

对生产依赖的三个含义：
