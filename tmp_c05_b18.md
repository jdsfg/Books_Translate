一个具体后果：在 Beacon Health AI，_药物处理适当性_ 维度的第一轮标注 kappa = 0.38，百分比一致性约 80%。按百分比一致性，标签看起来没问题；按 kappa，它们不可用。团队测量 kappa 的纪律捕获了差距并触发了最终达到 kappa = 0.72 的重新标注工作。没有 kappa，他们会在嘈杂标签上发布，eval 会比团队以为的更不可靠。

**Q3.** 指出 §4.5 中覆盖的两个 LLM 作为评判者的故障模式，并为每个提出具体缓解措施。

**答案：** 两个故障模式和缓解措施：

1. **相关家族失败**。如果评判者和系统都是 Sonnet 4，评判者倾向于同意系统因为它们共享偏见。评判者将包含评判者自己会产生的模式的输出标记为正确。eval 过度乐观。_缓解_：使用不同模型家族的评判者（在 Anthropic 上运行系统；使用 GPT-4o 或 Gemini 2.5 Pro 作为评判者）。成本溢价很小；eval 诚实性收益是真实的。
2. **长度偏见**。LLM 评判者在量表未指定长度时倾向于偏好更长输出。这偏置 eval 朝向冗长系统，即使简洁输出更可取。_缓解_：(a) 在量表中显式包含长度中立性；(b) 按输出长度四分位数测量平均分数并检查单调性（如果单调，偏见存在）；(c) 可选地，按输出长度分层 eval 并报告每层分数。组合捕获偏见并要么纠正它要么透明地暴露它。

值得提及的第三个故障模式：**偏好对的位置偏见**。评判者因位置而非内容偏好第一或第二候选。缓解：将每对在两种顺序下各运行一次并平均。这是 LLM 作为评判者偏好对 eval 的标准纪律；不要跳过它。

**Q4.** §4.7 的收益递减框架何时适用，何时不适用？指出一个团队可能错误援引框架以避免构建 eval 的功能，并解释为什么框架不适用。

**答案：** 收益递减框架在 §4.7 的四个场景之一确实成立时适用：(a) 没有预期生产生命周期的一次性原型；(b) eval 会以违背目的的方式约束多样性的创意任务；(c) 确实挑战 golden set 构建的 agent 轨迹；(d) 参与指标提供足够信号的极低风险任务。

团队可能错误援引框架的常见功能：**HR 问题的内部员工聊天机器人**。团队争辩 _这是低风险的；只有我们的员工使用；我们就根据反馈迭代_。框架不适用因为：(a) HR 问题有时有合规和政策影响（员工询问福利、休假政策、便利设施），错误答案为公司创造法律风险；(b) 员工通常不对微妙错误提供良好反馈（他们可能不知道答案是错的直到它引起下游问题）；(c) 功能旨在无限期在生产中运行，所以构建 eval 随时间良好摊销；(d) eval 成本相对于已投入的工程很小。正确答案是 _构建 eval_，即使团队更愿意跳过它。
