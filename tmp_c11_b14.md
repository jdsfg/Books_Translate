* _部分失败_：如果嵌入 API 在批处理中途失败，索引不一致。使用幂等设计："待处理更新"表被排空；重试循环；最终一致性。
* _Schema 变更_：如果元数据 schema 变更，索引管道必须重新处理所有文档以应用新元数据。作为主版本升级跟踪。
* _回填_：首次开启管道时（或长时间中断后），从当前状态回填索引，然后用提交日志追赶。

健壮实现的代码量：约 300–500 行 Python，加一个小 SQLite 或 Postgres 表用于跟踪状态。工程工作量：首版约 1 工程师周；含监控、告警、回填、schema 迁移约 1 工程师月。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为索引管道设计基于 git 的变更检测层：触发器、检测、重新索引逻辑、删除处理。_ 你验证 (a) 提议的 git 钩子或轮询设计在你的部署环境中工作（你的运行时有 git 访问吗？触发器应该是仓库上的 CI 吗？），以及 (b) 部分失败处理健壮：写一个嵌入 API 在批处理中途失败的测试并确认管道正确恢复无重复索引或遗漏块。

**Q4.** _零停机迁移嵌入模型_。你的生产 RAG 运行在 `text-embedding-3-small` 上。你评估了 `text-embedding-3-large` 且 eval 显示 +5 分质量提升。设计零停机迁移。识别步骤、时间尺度和回滚策略。

提示

你需要在旧索引继续服务流量时用新模型构建新索引。

**答案：** 步骤：

1. **并行构建新索引**（大型语料库通常多日批处理作业）。新索引填充 `text-embedding-3-large` 的向量；旧索引继续在 `text-embedding-3-small` 上服务生产流量。两个索引通过索引管道保持同步（现在写入两者）。
2. **影子 eval 新索引**：将小部分（5–10%）生产查询同时路由到两个索引，比较检索质量。比较数据进入仪表板审查。在面向客户切换前捕获意外。
3. **按租户或按功能金丝雀**：将小部分客户（或特定功能）完全路由到新索引；观察生产行为。通常 1 周金丝雀在 1-5% 后更广泛推出。
4. **渐进推出**：在新索引上增加流量百分比（10%、25%、50%、100%）在 2–4 周期间。每次增量以观察无质量回归和可接受延迟为门控。
5. **退役旧索引**在稳定期后（通常 100% 新流量 2–4 周）。保留旧嵌入模型可用于短期重新索引以防问题。

**回滚策略**：

* _快速回滚_：将 100% 路由回旧索引。回滚时间：分钟。旧索引在迁移期间显式保持热以启用此。
* _取证回滚_：如果特定查询类显示回归，仅将该类路由到旧索引同时其他保持新。需要在路由时查询分类。
