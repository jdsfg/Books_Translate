### 2.4 上下文窗口：它实际是什么，以及为什么重要

每个现代 LLM 都有一个 _上下文窗口_：模型在单次调用中可以处理的输入序列的最大长度，以 token 为单位。截至 2026 年，前沿模型的上下文窗口从 128K token（GPT-4o）到 200K token（Claude Sonnet 4 家族）到 2M token（Gemini 2.5 Pro 长上下文配置）。

上下文窗口是一个硬架构限制。你不能超过它。如果你的输入超过上下文窗口，API 要么报错要么静默截断（取决于 provider 和端点）。围绕上下文窗口的工程（放什么进去、留什么出来、如何压缩、何时切换到 retrieval）是第 8 章的主题。现在，重要的是上下文窗口 _是什么_。

上下文窗口是模型的工作记忆。在窗口内，模型可以 attend 到任何 token。窗口外，模型无法访问。模型中没有内置的 _长期记忆_，不像人类那样；模型在生成下一个 token 时考虑的一切来自 (a) 它的训练，编码在权重中，以及 (b) 上下文窗口内的当前输入序列。如果你想让模型记住昨天的对话，你必须把昨天的对话放回今天的输入中。如果你想让模型了解你的内部文档，你必须把文档放回输入中。

这是 _上下文工程_（第 8 章）领域的基础，也是 RAG（第 9-12 章）的基础，也是 agent 记忆架构（第 14 章）的基础。上下文窗口是单次推理调用中状态存在的唯一地方。

#### 为什么"lost in the middle"是真实现象

2023 年的一篇论文（Liu et al.，非正式地称为 _lost in the middle_ 论文）表明，在检索式任务中，模型被给予一个长上下文，其中某处包含相关片段，模型提取正确信息的准确性取决于片段出现在 _上下文中的哪个位置_。上下文开头和结尾的信息比中间的信息更可靠地被 attend 到。

这一现象在较新的模型中已有所缓解（2024 和 2025 年的前沿模型在长上下文检索方面明显优于该论文研究的 2023 年代模型），但它没有消失。它是一种倾向，而非硬性失败，第 8 章将回到这个话题：在 2026 年，实际含义是你应该将最关键的指令放在 prompt 的 _末尾_（最接近生成开始的地方），将最关键的参考材料放在上下文的 _开头_ 和 _末尾_，将不太关键的材料放在中间。

这种细节在没有盒子内部知识时是不可见的（把 LLM 当作黑盒的人不会想到这一点），但它在操作上很重要。你将在第 8 章围绕它做工程。
