### P

**段落哈希**：用于源文本块的稳定标识符，用于在重新索引间附加和验证引用（第 11 章，§11.7）。

**按租户预算**：在网关处强制执行的每客户支出上限。防止一个租户耗尽你的利润率的成本纪律（第 17 章，§17.6）。

**百分比一致性**：评分者一致的项目比例，忽略偶然性。简单但偏高；严肃分析用 kappa（第 4 章，§4.5）。

**PEFT**：参数高效微调。方法族（LoRA、QLoRA、前缀调优、适配器）仅更新小部分参数（第 22 章，§22.3）。

**pgvector**：PostgreSQL 扩展，添加向量类型和索引。已在 Postgres 上的团队的最低摩擦向量存储（第 10 章，§10.4）。

**Phoenix**：Arize 的开源 AI 可观测性和 eval 工具。在笔记本驱动工作流上表现强（第 19 章，§19.3）。

**PHI**：受保护健康信息。HIPAA 定义的可识别健康数据类别；如你处理它，需 BAA 和严格控制（第 19 章，§19.6）。

**PII 脱敏**：从 prompt、日志或输出中检测和移除个人可识别信息。正则、NER 和 LLM 分类的组合（第 19 章，§19.7）。

**Pinecone**：托管向量数据库，有强生产声誉。向量存储的"无聊选择"（第 10 章，§10.4）。

**计划后执行**：agent 模式，先产生完整计划，然后以有限重规划执行步骤。比 ReAct 更可预测，适应性更低（第 14 章，§14.2）。

**Portkey**：多提供商 AI 网关，专注可观测性和 prompt 版本化。LiteLLM 的 SaaS 替代（第 17 章，§17.3）。

**位置偏差**：LLM judge 偏好特定位置的输出（通常第一个）。通过随机化呈现顺序缓解（第 5 章，§5.6）。

**事后复盘文化**：对 AI 故障（错误回答、停机、成本飙升）进行无指责审查的实践，行动项追踪到完成（第 23 章 §23.7；第 25 章 §25.2）。

**预训练**：在大型无标签语料上对基础模型的初始训练。作为 AI 工程师，你几乎不做这个；你消费输出（第 2 章，§2.2）。

**私有云计算**：Apple 为超过设备端能力的 AI 工作负载设计的隐私保护云架构。机密 AI 推理的参考设计（第 22 章，§22.6）。

**Prompt 缓存**：提供商侧缓存请求前缀，使后续相同前缀的调用跳过重计算。2026 年最大的单一成本杠杆（第 18 章，§18.2）。

**Prompt 压缩**：通过摘要、改写或学习压缩器减少 prompt 的 token 数同时保持含义。在缓存后使用，非之前（第 18 章，§18.7）。

**Prompt 工程**：编写产生期望模型行为的 prompt 的技艺。2026 年仍相关但被 _上下文工程_ 作为更广纪律包含（第 7 章，§7.1）。

**Prompt 注入**：通过 prompt 中的不可信文本操纵模型，使其忽略指令或外泄数据的攻击。定义性 LLM 安全威胁（第 23 章，§23.3）。

**Prompt 注册表**：带元数据（作者、eval 结果、部署状态）的 prompt 版本化存储。真相之源，非代码中的字符串（第 7 章，§7.4）。

**Prompt 版本**：特定 prompt 文本的不可变、可寻址标识符。可复现性和回滚所需（第 7 章，§7.4）。
