> **截至 2026 年。** 2027 年后阅读？以怀疑的眼光浏览 §6.3；那些低层 prompt 模式过时最快。§6.5（prompt 作为版本化代码）是应该仍然成立的部分。

2022 年你可以通过擅长 _让我们一步一步思考_ 成为 prompt 工程师。那句话，附在数学题前面，使 GPT-3.5 在数学上明显更好。这个技巧成了梗。人们写文章论述 _prompt 工程_ 是一门新学科。

2026 年，那句话在某些任务上仍然有一点帮助，但差距足够小以至于你在 eval 上不会注意到。前沿模型——Claude Sonnet 4、GPT-4o、Gemini 2.5 Pro——无需脚手架处理基本推理。_让我们一步一步思考_ 已被吸收进训练；模型在被要求之前就做了。大多数 2022–2023 年的 _prompt 工程技巧_ 走了同样的路：进训练，出工具箱。

那么还剩什么？四件事，它们是 2026 年 prompt 工程的实际工作：

1. **通过 prompt 设计优化成本和延迟。** 更短的 prompt 和更短的输出成本更低且响应更快。设计一个用 60% 的 token 完成相同工作的 prompt 是真实的工程胜利。
2. **约束输出结构。** 使模型可靠地产生 JSON、XML、特定 markdown 形状或函数调用签名。这是第 7 章（结构化输出）的主题，是 prompt 工程最常见的生产用途。
3. **引出工具使用。** 告诉模型何时调用哪个工具、传什么参数、如何解释工具的响应。这是第 13 章（工具使用）的主题，是 agent 设计的基础。
4. **安全加固。** 拒绝模式；PII 脱敏；prompt 注入防御；防止有害输出的输出格式约束。这是第 23 章（安全）的主题，是大多数生产 prompt 工作所在。

本章是关于与 prompt 打交道的 _纪律_：将它们视为版本化代码、对照 golden set 评估它们、将它们设计为发布和演变的制品。具体策略是时间戳的（2026 年命名的模式到 2028 年将部分过时）；纪律是持久的。

### 6.1 prompt 工程仍在做的四件事

让我们展开每一个。
