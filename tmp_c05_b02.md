### 4.1 为什么 eval 不是可选的

关于 AI 工程中 eval 必要性的三个论点。不同团队通过不同门进入这个纪律；其中一个论点会打动你。

**论点 1：Air Canada 原则，递归地。** 第 1 章介绍了 LLM 输出是你公司正在做的陈述这个规则。如果你的公司在做陈述，你的工程流程应该在陈述到达用户之前有某种方式知道它在做什么陈述。_某种方式知道_ 就是 eval。没有 eval，你发布功能然后通过阅读头条新闻、法庭裁决、支持工单、愤怒的推文来发现你的公司在做什么陈述。有了 eval，你在功能发布前发现它，并调整。_之前_ 和 _之后_ 之间的选择是工程和赌博之间的选择。

**论点 2：变更检测论点。** 一个生产 LLM 系统有许多独立变化的移动部件：prompt、检索语料库、模型版本、周围业务逻辑、用户群体、上游数据格式、工具目录。其中任何一个变化都可能改变系统行为。没有 eval，你无法检测一个部分的变化是否降低了系统。你发布一个 prompt 微调；微调在你测试的案例上无害；它在没想到的一类输入上静默回归；回归累积两周直到客户注意到。有了 eval，prompt 微调作为 CI 的一部分对 golden set 运行；回归显示为特定 eval 切片上的 4 个点下降；工程师要么修复 prompt 要么升级。eval 是经典软件工程以回归测试形式拥有的变更检测传感器，AI 工程以 eval 形式拥有——但前提是团队已经构建了一个。

**论点 3：经济论点。** 没有 eval，每个工程决策都变成猜测。从 Sonnet 4 切换到 Sonnet 4.5、用 Voyage embedding 替换 Cohere、加一个重排序器、调 chunk 大小——每一个都是真金白银的问题。没有 eval，你通过阅读供应商营销、询问团队、凭直觉来回答。有了 eval，你通过在 golden set 上运行候选变更并读取 delta 来回答。_让每个决策数据驱动_ 的经济学优于 _猜测_ 的经济学，尤其当金额变大时。Helios 每月 1M 美元的推理账单意味着 5% 的路由改进是每月 50K 美元，或每年 600K 美元——超过一个高级工程师的年总薪酬，持续的，代价只是拥有一个 eval 套件。

如果你没有 eval，你无法防御性地发布，你无法检测回归，你无法做出经济决策。你可以 _在没有 eval 的情况下运营_（很多团队这样做），但你在凭感觉经营业务。感觉在不行之前都行，当不行时，故障模式通常很大且可见，并被命名为本书的一个章节标题。

### 4.2 eval 的解剖

一个 eval 有六个组件。如果缺少任何一个，eval 就不完整。
