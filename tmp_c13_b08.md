### 12.7 Triangle Trade-off 侧栏：多模态摄取成本（成本分解格式）

Strata 合同一页通过视觉优先管道的成本分解：

* **视觉输入**（页面图像，约 1500 视觉 token，Sonnet 4 Vision）：1500 × 3 美元/1M = **0.0045 美元**
* **文本输入**（提取 prompt，约 200 token）：200 × 3 美元/1M = **0.0006 美元**
* **输出**（结构化提取，约 1200 token）：1200 × 15 美元/1M = **0.018 美元**
* **对象存储**（页面图像 + 提取 JSON，合计约 500KB）：可忽略（约 0.0001 美元）
* **索引存储**（块、嵌入、元数据）：约 0.0001 美元
* **每页总计**：**约 0.0234 美元**

在 Strata 规模（50 万页初始语料库）：

* 一次性摄取：500,000 × 0.0234 美元 = **11,700 美元**
* 每日增量（200 个新合同 × 平均 50 页 = 10,000 页/天）：10,000 × 0.0234 美元 = **234 美元/天**
* 每月增量：约 7,000 美元/月

要解读的：

1. **输出主导每页成本**（输出是每页总成本的 75%）。5 倍输出乘数再次出现。减少提取输出冗长——更简洁的提取 prompt；不需要长散文描述的结构化输出——直接削减成本。
2. **视觉输入约 20% 成本**。分辨率控制和批处理是两个杠杆。
3. **存储在此规模基本免费**。廉价对象存储意味着一旦摄取就没有理由丢弃页面。
4. **以 7K 美元/月持续摄取**高价值法律文档，成本相对于工程价值（§12.5 中整个查询类别上约 30 分质量提升）小。模式对高价值内容高度成本有效。
