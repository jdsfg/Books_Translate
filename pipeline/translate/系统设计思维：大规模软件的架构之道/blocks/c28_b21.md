**答案：** 1. 它解决什么问题？PostgreSQL 在高基数（100+ 指标 × 1000+ 服务器 × 1 秒分辨率）下查询 18 个月指标数据时吃力。“API 端点 X 过去 30 天 p99 延迟”查询要花 30 秒。时序数据库正是为这种访问模式优化：带高写入速率的时序数据范围查询。是的——这是真实观察到的问题。2. 它与 PostgreSQL 有何不同？时序数据库用面向列的存储和基于时间的压缩（指标重复模式；常见 10–50 倍压缩比）。它们内置降采样（自动保留过去 1 周的 1 秒数据、过去 3 个月的 1 分钟数据、过去 2 年的 1 小时数据）。原生支持时间窗口函数。PostgreSQL 需要 TimescaleDB 扩展或手动分区才能做到这些。3. 故障模式？InfluxDB v1 在多节点配置下有已知稳定性问题。TimescaleDB 是 PostgreSQL 扩展——故障模式已被充分理解。为熟悉度偏好 TimescaleDB。4. 运营模型？TimescaleDB 运行在 PostgreSQL 上——SocialSnap 团队已经在运维 PostgreSQL。所需新运营专长最少。InfluxDB 需要新工具链。TimescaleDB 在运营连续性上胜出。5. 退出策略？TimescaleDB 数据存在 PostgreSQL 中；可通过标准 SQL 访问。迁移出去需要 ETL，但是可行的。退出策略良好。6. 还有谁在规模上运行？TimescaleDB 已被多家大公司大规模使用。公开的案例研究和事后复盘可获得。建议：采纳 TimescaleDB。它解决真实问题，故障模式可接受，运营 familiar，退出策略清晰。

**Q3.** SocialSnap 经历了一次 45 分钟（45-minute）故障：数据库迁移意外删除了 `posts` 表上的索引，导致每个 feed 查询执行全表扫描。用五个为什么技术追溯根本原因。然后撰写事后复盘的行动项，确保每项都处理系统性条件而非归咎于个人。