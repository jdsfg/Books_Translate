1. **这列是否频繁出现在生产查询的 WHERE、JOIN 或 ORDER BY 子句中？** 如果不是，索引代价（写入变慢）可能不值得。
2. **表是否足够大，以至于全表扫描明显慢？** 对小于 10,000（1 万）行的表，索引帮助很小——全表扫描已经够快。
3. **读/写比例如何？** 对 mostly-read 的表（如国家代码查询表），可以大胆加索引。对 mostly-write 的表（如事件日志），应谨慎加索引。
4. **列是否选择性高？** 对布尔列（true/false）建索引几乎没用——数据库平均仍要读 50% 的表。索引在基数高（很多不同值）的列上效果最好。