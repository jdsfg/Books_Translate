**Q4.** 什么是提示移交（hinted handoff），它有什么局限？

**答案：** 提示移交是处理对暂时不可用节点写入的机制。当写到达而目标节点不可达时，另一个节点（一致哈希环中的“邻居”）接受该写并带一个“提示”，标记它原是给不可用节点的。当不可用节点恢复，提示节点把累积的写投递过去，使恢复节点追上来。

局限：提示只保留有限时间（可配置，通常数小时）。如果目标节点在窗口期内仍未恢复，提示会过期并被丢弃。恢复节点随后必须依赖反熵（repair 进程）发现并补齐缺失数据。这更慢，对大数据集可能需要显著时间。