* **Command + Queue** 是典型的异步作业模式。命令是工作单元；队列是传输；两者一起让动作可传输。
* **Command + Flag** 是条件执行模式。开关选择*执行哪个*命令；命令做工作。
* **Flag + Queue** 单独是渐进灰度模式（第 11 章）；开关控制多大比例请求走哪条路径。

三者结合就是 2026 年“带灰度的后台作业”模式：命令表示工作单元；队列传输它们；开关为给定单元选择运行哪个版本的工作。Sidekiq 等后台作业框架的版本支持正是如此实现。