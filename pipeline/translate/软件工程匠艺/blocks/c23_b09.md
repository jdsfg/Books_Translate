这个失败案例被收录是因为否则本书读起来像一连串成功故事。纪律有效；跳过它们的团队会产生 Veridian 式的故事。两半都真实；两半都应入书。

* * *


### 22.8 完整示例：Helix 一个下午发布的功能

开头小品的详细版。Aki 的任务：为 Helix 添加一种新定价规则类型（基于使用量的阶梯，带 rollover credits）。


#### 步骤 1：写测试（30 分钟）

Aki 打开 `tests/test_usage_based_pricing.py`，写八个 failing tests：

    def test_usage_below_tier_charges_at_unit_price(): ...
    def test_usage_at_tier_boundary_uses_lower_price(): ...
    def test_rollover_credit_consumed_before_new_charges(): ...
    def test_rollover_credit_expires_after_12_months(): ...
    def test_negative_usage_raises_error(): ...
    def test_zero_usage_returns_zero_charge(): ...
    def test_currency_consistency_with_customer(): ...
    def test_property_based_total_equals_charge_minus_credits(): ...

这些测试精确描述规则行为。Aki 运行它们；全部失败，因为实现不存在。


#### 步骤 2：调用智能体（5 分钟）

Aki 打开 Claude Code agent mode。提示：

“实现基于使用量的阶梯定价规则类型，带 rollover credits。Spec：`tests/test_usage_based_pricing.py` 中的 failing tests。约定：CLAUDE.md。代码在 `core/pricing/rules/`。用 Decimal 不用 float。适用时用基于属性的测试。让所有测试通过。”


#### 步骤 3：智能体工作（90 分钟智能体时间，Aki 0 分钟）

智能体读取 CLAUDE.md、failing tests 和 `core/pricing/rules/` 现有规则。它实现：

* `core/pricing/rules/usage_based.py` 中的 `UsageBasedRule` 类。
* 纯函数 helper `apply_rollover_credits(usage, credits, current_date)`。
* 在规则注册表中注册新规则类型。
* 12 个 commit，每个原子，使用 Conventional Commits 消息。

智能体运行测试；全部八个通过。智能体报告完成。


#### 步骤 4：Aki 审查（45 分钟）

Aki 读每个 commit。实现看起来合理；测试通过；风格匹配代码库。Aki 应用线索 C checklist：

* 未测试边界情况：智能体没有测 rollover credits 超过使用量时会发生什么（负有效使用量）。Aki 加了一个测试；测试通过（实现正确处理，但测试缺失）。
* 过度抽象：没有观察到。
* 风格不一致：一个理解式中变量名 `c` 以清晰；Aki 保留（短作用域可接受）。
* 错误处理错误：智能体对负使用量抛 `ValueError`；代码库约定是 `InvalidUsageError`。Aki 让智能体修复；智能体一个 commit 修复。
* Off-by-one：rollover-credit-expires-after-12-months 测试用 `<`，实现用 `>=`。Aki 调查：实现正确，测试用 `<` 测正好 12 个月的边界。语义一致。
* 幻觉 API：没有观察到。

Aki 用一个额外不变量加强基于属性的测试。让智能体加测试；智能体照做。本步骤总工作：45 分钟。


#### 步骤 5：合并（5 分钟）

Aki 再运行一次测试；开 PR；AI 审查工具评论；Aki 处理容易的；合并。