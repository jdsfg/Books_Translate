它也让 orchestrator 的结构显式且可预测。读 orchestrator 的工程师知道寻找三个阶段；新 orchestrator 容易写，因为形状熟悉。

该模式在编排真正交错时失败（循环中反复 decide-and-act，每次决策依赖前一次动作结果）。对这些情况，orchestrator 更大、更命令式；FC/IS 纪律可用状态机式纯函数（返回下一状态的纯函数）来容纳，但形状不那么干净。