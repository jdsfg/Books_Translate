* 迁移开关回答“哪个实现运行？”用户可见行为应该无论哪个实现都不应有差异。“正确行为”是“没有分歧”。删除是迁移的预定里程碑，不是可选。
* 产品开关回答“这个用户得到什么体验？”不同用户故意得到不同行为。删除发生在实验结束后。

两类开关忽视后都会腐烂。缓解办法相同：带 `kill_by_date` 的文档生命周期（第 4 章 §4.4）。不同的是*类别特定的失效模式*：迁移开关在源码中累积死代码；产品开关在团队心智模型中累积认知负担（“这个用户又在哪个实验里？”）。两者都真实；两者都可预防；两者在咬人前都不可见。