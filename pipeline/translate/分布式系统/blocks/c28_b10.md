| # | 工作流 | 章节 | 输入 | 输出 | 核心原语 | 常见陷阱 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 逻辑时钟实现 | Ch.3 §3.3 | N 个进程上本地 + 发送 + 接收事件序列 | 尊重 happens-before 的每事件时间戳 | Lamport / 向量 / HLC 计数规则 | 将物理时钟与逻辑时钟比较；10k 节点舰队的向量时钟增长；HLC 计数无界溢出 |
| 2 | 广播原语伪代码 | Ch.4 §4.7 | 要交付给 N 个进程的消息 | 所有正确接收者以相同顺序交付（原子）或更弱变体 | 法定人数确认 + 重广播 | 为 “安全” 默认选原子广播，而可靠广播已足够；遗漏统一性（发送者和唯一接收者同时崩溃） |
| 3 | 同步 vs 异步复制追踪 | Ch.5 §5.10 | 写主库；存在副本 | 每次写入的 RPO + 每次写入的延迟（可配置权衡） | 每次写入的法定人数成员 | 同步副本成为变慢单点；误以为 W+R>N 即线性化；分区下 sloppy 法定人数 |
| 4 | Raft 状态机走查 | Ch.9 §9.4 | 客户端请求→领导者→跟随者 | 复制状态机；已提交条目在多数派上持久 | AppendEntries 一致性检查 + 多数派提交 | append 时应用而非提交时应用；忽略 Figure-8 前任期提交细节；无 joint consensus 重配置 |
| 5 | HotStuff 三阶段追踪 | Ch.10 §10.3 | 验证者提案 + 2f+1 轮投票 | 法定人数证书决定；O(N) 视图变更 | 法定人数证书 + 流水线三阶段投票 | 把 N≥3f+1 阈值与 2f+1 法定人数混淆；截止压力下跳过 QC 验证；竞争新领导者间视图变更竞态 |
| 6 | 30 分钟读懂数据库 | Ch.11 §11.3 | 任何厂商文档 | 一页 PACELC + 诊断画像 | 四个诊断问题：复制、恢复、一致性、分区 | 接受营销级 “一致”；混淆 CAP 与 PACELC；遗漏每次操作一致性选择 |
| 7 | Anya 的重分片迁移 | Ch.12 §12.5 | N 节点集群磁盘利用率高；需加 M 节点 | 再平衡集群，零停机，事务一致性保留 | 一致性哈希 + 范围流 + Raft 领导权转移 | 范围大小估计漂移；业务高峰期忘记限流；源节点流式处理中途 failover 未处理 |
| 8 | Olamide 的跨区域支付 | Ch.13 §13.7 | 多 shard 跨区域支付事务 | 所有 shard/区域原子提交或原子中止 | 2PC + Raft 复制协调器 + HLC 版 MVCC | 协调器状态在易失内存；竞争下不确定性重启；下游 API 缺少围栏 token |
| 9 | Yuki 的 Yjs 协作编辑器 | Ch.16 §16.4 | 多客户端并发编辑，部分离线 | 确定性合并后的文档状态 | RGA 风格列表 CRDT + WebSocket 同步 + awareness 协议 | 在 CRDT 中存储临时光标位置；缺少定期压缩；分片逻辑与合并逻辑混淆 |
| 10 | 级联故障事后分析 | Ch.21 §21.7 | 事件：触发 + 放大 + 传播 + 恢复 | 行动项：重试预算、熔断器、容量缓冲、运维手册 | 事后分析模板 + 结构防御的五 Whys | “我们增加了超时” 却不改变类别；遗漏亚稳态反馈环；指责导向复盘 |
| 11 | Olamide 的混沌演练日 | Ch.23 §23.6 | 假设故障模式 + 稳态 SLI | 验证防御；记录学习；行动项 | 演练前设计 + 当天执行 + 无责复盘 | 跳过影响半径控制；无回滚计划；发现缺陷却继续后续演练日而不修复 |
| 12 | vLLM 分离集群 | Ch.25 §25.5 | 每月 100 万美元以上 LLM 推理工作负载 | 相同或更好延迟下降低 30-50% 成本 | 预填充/解码分离 + KV 缓存层 + RDMA 网络 | 跨机架 KV 传输的首 token 延迟惩罚；缓存层带宽成为新瓶颈；混合预填充/解码 failover |

每个工作流都可作为独立小手册阅读：输入、机制、输出、陷阱。它们共同覆盖了 2026 年分布式系统工程的操作弧线。