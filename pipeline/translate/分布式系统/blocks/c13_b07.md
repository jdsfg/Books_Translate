#### 每种风格实例

**Orchestrated** —— Crescent 支付工作流（后文详细展开）：


    PaymentOrchestrator (CockroachDB-backed 状态):
      步骤 1: 调用 ReservationService.reserve(用户, amount)
      步骤 2: 调用 FraudService.check(事务)         → 分数
      步骤 3: 调用 BankSettlement.submit(事务)      → bank_ref
      步骤 4: 调用 LedgerService.record(事务, bank_ref)
      步骤 5: 调用 NotificationService.send(用户, "completed")
      步骤 6: 调用 AnalyticsService.update(商家, 天)
      on 故障 at 步骤 N: 调用 compensations for N-1, N-2, ..., 1


单一 orchestrator 服务知道工作流，把 saga 状态存在 CockroachDB，驱动每一步。调试：直接查询 saga 状态——“saga ABC123 在第 4 步，最后动作 LedgerService.record，状态：in_progress。” 完整生命周期就是一行。

**Choreographed** —— 典型电商订单工作流（无中央 orchestrator）：


    OrderService            → publishes OrderCreated 事件
    PaymentService          listens to OrderCreated      → publishes PaymentCompleted
    InventoryService        listens to PaymentCompleted  → publishes InventoryReserved
    ShippingService         listens to InventoryReserved → publishes ShippingScheduled
    NotificationService     listens to ShippingScheduled → 发送 confirmation


每个服务响应前一个服务的事件。补偿通过反向事件触发：`PaymentFailed → OrderCancelled`；`InventoryUnavailable → PaymentRefundRequested → PaymentRefunded → OrderCancelled`。无中央状态。调试：通过 Kafka 追踪事件链（第20章的追踪上下文是唯一的端到端可见性）。

权衡鲜明。编排集中了优势（单一查看点）和弱点（单一知晓点）。舞蹈化分布了两点：任何服务可独立开发部署，但理解完整工作流需要阅读跨多个服务的订阅关系。

Crescent 对支付使用编排（受监管、审计重、工作流很少变）。其内部 “marketplace 订单处理” 子系统使用舞蹈化（不同团队拥有服务，独立演进）。两种模式在同一公司共存。

编排支付 saga 可视化：


    sequenceDiagram
        autonumber
        参与者 C as 客户端
        参与者 O as Orchestrator<br/>(状态 in CRDB)
        参与者 R as Reservation
        参与者 F as 欺诈
        参与者 B as 银行
        参与者 L as Ledger
        参与者 N as 通知
        参与者 A as Analytics
        C->>O: initiate payment
        O->>R: reserve funds
        R-->>O: reserved
        O->>F: 检查欺诈
        F-->>O: 分数 (OK)
        O->>B: submit settlement
        B-->>O: bank_ref
        O->>L: 记录 (事务, bank_ref)
        L-->>O: ledger_id
        O->>N: 发送 confirmation
        N-->>O: 已发送
        O->>A: 更新商家聚合
        A-->>O: 已更新
        O-->>C: 成功
        Note over O: On 故障 at 步骤 N:<br/>调用 compensations<br/>for N-1, N-2, ..., 1