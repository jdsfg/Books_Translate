(c) **更容易优化：** SQL，优势明显。如果 DBMS 在 `enrollments.student_id` 上有索引，它会利用该索引加速计数；而二级索引和预排序统计信息甚至可能让它跳过排序。Python 版本没有这些知识，只能线性读取两个文件的每一行。当数据从 3 名学生增长到 300 万时，SQL 版本仍保持快速，而 Python 版本线性变慢。这就是声明式查询给我们的优化自由度，也是第 12 章的主题。