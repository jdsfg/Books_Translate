**答案：** 会——这正是第 5.7 节“属性成长为实体”的模式。一旦关系有了三个属性，每个都有自己的语义和更新模式，它就变成一个值得命名的东西。把它作为名为 `Enrollment` 的实体（而不是无名的关系）会让模型更清晰，并能自然地附加更多属性或关系（例如把选课与付款记录关联，或跟踪成绩变更历史）。

在关系 Schema 中，最终结果一样：一张名为 `Enrollments` 的表，包含四列。但在 ER 图中——以及与其他工程师的讨论中——“Enrollment 是一个实体”比“Enrolls 是一个碰巧带三个属性的关系”更诚实地反映了领域。