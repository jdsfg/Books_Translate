#### 解码规则

解码器声明 \hat{m} = m 当且仅当：

1. (x^n(m), y^n) 是联合典型的（按 §3.7 的定义），且
2. 不存在其他 m' \neq m 使得 (x^n(m'), y^n) 是联合典型的。

如果两个条件都不满足（没有联合典型码字，或多于一个），声明错误。

此规则是次优的；最优规则是最大似然解码，对于等概率消息为 \hat{m} = \arg\max_m p(y^n \mid x^n(m))。但联合典型性解码_更易于分析_且在极限下达到容量，这对可达性证明来说已经足够。