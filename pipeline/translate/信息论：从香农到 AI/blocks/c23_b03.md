### D.4 SmallTransformer（从第 18 章复制）


    class SmallTransformer(nn.Module):
        def __init__(self, vocab_size=256, embed_dim=256, n_heads=8,
                     n_layers=6, context_len=256):
            super().__init__()
            self.token_emb = nn.Embedding(vocab_size, embed_dim)
            self.pos_emb = nn.Embedding(context_len, embed_dim)
            self.layers = nn.ModuleList([
                TransformerLayer(embed_dim, n_heads) for _ in range(n_layers)
            ])
            self.ln_final = nn.LayerNorm(embed_dim)
            self.head = nn.Linear(embed_dim, vocab_size)

        def forward(self, x):
            b, t = x.shape
            positions = torch.arange(t, device=x.device).unsqueeze(0).expand(b, -1)
            z = self.token_emb(x) + self.pos_emb(positions)
            for layer in self.layers:
                z = layer(z)
            z = self.ln_final(z)
            return self.head(z)

        def hidden_states(self, x):
            """Return per-layer hidden states. Used by the MI probe in §D.7."""
            b, t = x.shape
            positions = torch.arange(t, device=x.device).unsqueeze(0).expand(b, -1)
            z = self.token_emb(x) + self.pos_emb(positions)
            states = [z]
            for layer in self.layers:
                z = layer(z)
                states.append(z)
            states.append(self.ln_final(z))
            return states


参数计数：约 n_{\mathrm{layers}} \cdot (4 e^2 + 8 e^2) + 2 \cdot e \cdot V，其中 e = \mathrm{embed\_dim}, V = \mathrm{vocab}。代入：6 \cdot 12 \cdot 65536 + 131072 \approx 4.85 百万——匹配第 18 章的"4M 参数"数字。