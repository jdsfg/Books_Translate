**深入思考 Q2.** 一个"完美"的通用压缩器会在不知道 p 的情况下以速率 H(X) 编码任何序列。LZ77 等近似地渐近实现此目标。什么阻止了*非渐近*通用压缩器的存在？

鸽巢原理。压缩器是从序列到位串的映射；如果它将*某些*序列缩短 k 比特，由单射性它必须将至少一个其他序列加长 k 比特。对固定序列长度 n，不可能有压缩器缩短*每个*输入。唯一的出路是知道哪些输入"常见"，使常见输入上的节省超过稀有输入上的损失——而此知识就是信源分布 p。通用性仅渐近可达：当 n \to \infty 时，LZ77 式编码器从输入本身学到足够的 p 以速率 H 压缩。

* * *

---