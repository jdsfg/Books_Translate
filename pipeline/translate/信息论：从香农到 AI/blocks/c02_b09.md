#### 性质 2：有限集上的最大熵

对于在有 nnn 个结果的集合上的离散随机变量 XXX，

H(X)≤log⁡n,H(X) \leq \log n,H(X)≤logn,

等号成立当且仅当 XXX 是均匀分布的。

**证明**：由 Jensen 不等式应用于凹函数 log⁡\loglog 得出。对任意概率分布 ppp，

H(X)=∑xp(x)log⁡1p(x)≤log⁡∑xp(x)⋅1p(x)=log⁡n,H(X) = \sum_x p(x) \log \frac{1}{p(x)} \leq \log \sum_x p(x) \cdot \frac{1}{p(x)} = \log n,H(X)=x∑​p(x)logp(x)1​≤logx∑​p(x)⋅p(x)1​=logn,

其中我们使用了凹函数 log⁡\loglog 上的 Jensen 不等式。Jensen 不等式中的等号在自变量为常数时成立，即 1/p(x)1/p(x)1/p(x) 对所有 xxx 相同，这意味着对所有 xxx 有 p(x)=1/np(x) = 1/np(x)=1/n。□\square□

#### 性质 3：关于分布的凹性

H(p)H(p)H(p) 是 ppp 的凹函数。即，对于两个概率分布 ppp 和 qqq 以及 λ∈[0,1]\lambda \in [0, 1]λ∈[0,1]，

H(λp+(1−λ)q)≥λH(p)+(1−λ)H(q).H(\lambda p + (1 - \lambda) q) \geq \lambda H(p) + (1 - \lambda) H(q).H(λp+(1−λ)q)≥λH(p)+(1−λ)H(q).

**证明**：函数 f(t)=−tlog⁡tf(t) = -t \log tf(t)=−tlogt 在 [0,1][0, 1][0,1] 上是凹的（通过求导验证：f′′(t)=−1/(tln⁡2)<0f''(t) = -1/(t \ln 2) < 0f′′(t)=−1/(tln2)<0）。熵是 f(p(x))f(p(x))f(p(x)) 的和，而凹函数之和是凹的。□\square□

这种凹性有一个优美的解释：混合两个分布只会增加熵。直觉上，混合产生了一个更不确定的联合分布；你必须先决定你是从哪个分布中采样的。

#### 性质 4：联合熵分解

对于一对 (X,Y)(X, Y)(X,Y)，**联合熵**（joint entropy）为

H(X,Y)=−∑x,yp(x,y)log⁡p(x,y).H(X, Y) = -\sum_{x, y} p(x, y) \log p(x, y).H(X,Y)=−x,y∑​p(x,y)logp(x,y).

这只是联合分布的熵，将其视为乘积空间上的单个随机变量。它满足

max⁡(H(X),H(Y))≤H(X,Y)≤H(X)+H(Y),\max(H(X), H(Y)) \leq H(X, Y) \leq H(X) + H(Y),max(H(X),H(Y))≤H(X,Y)≤H(X)+H(Y),

其中右边不等式等号成立当且仅当 XXX 和 YYY 独立。

我们将在第 2 章回到联合熵及其更丰富的衍生概念——条件熵和互信息（mutual information）。