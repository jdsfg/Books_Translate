更深层原则：**架构不是一组组件；它是一组组件加它们之间的契约**。本章的 9 层模型必要但不充分。完整架构纪律是组件完整性 _加_ 契约完整性。跨层审计是在契约缺口变成事故前浮现它们的实践。

一个具体运维提示：为每个项目维护"数据血缘"图，显示哪些字段在哪层产生并通过哪些其他层传播。图是跨层契约的显式产物。需要新字段的新功能更新图；审计验证图声明的字段在每个下游层实际存在。

本章教训，泛化：逐层思考是入口；跨层思考是成熟度。在生产中可靠上线的团队有两种纪律。