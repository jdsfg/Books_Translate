**Q2.** _阅读 model card_。选择任何你可以访问的 2026 年前沿模型（Claude Sonnet 4、GPT-4o、Gemini 2.5 Pro 等）。找到它的 model card 或产品文档。按照 §2.9 提取以下九个值：上下文窗口、知识截止日期、支持的功能、定价、基准结果、安全评估、延迟特征、速率限制、弃用时间线（如果披露）。对于每个值，注明它是 _直接声明_ 在 model card 上还是 _推断_ 的。

**答案：** Claude Sonnet 4 的示例（你的答案应来自当前 model card；这里的数字是 2026 年的代表性数字，会过时）：

值 | 声明或推断 | 备注
---|---|---
上下文窗口 | 声明 | 200K token 输入；某些配置为长上下文高级版提供 1M
知识截止日期 | 声明 | 具体月/年
功能 | 声明 | Streaming、工具调用、vision、JSON-schema 输出、prompt 缓存
定价 | 声明 | 3.00 美元/1M 新输入；0.30 美元/1M 缓存读取；15.00 美元/1M 输出；vision 和长上下文附加费单独披露
基准 | 声明 | MMLU、HumanEval、MATH、GPQA（provider 报告的分数）
安全评估 | 声明 | 拒绝率评估；red-team 摘要；一些 jailbreak 抵抗数字
延迟 | 部分声明 | TTFT（首 token 时间）p50 通常引用；吞吐量有时
速率限制 | 声明 | 基于层级；默认层级速率限制公开，更高层级通过账户升级
弃用 | 有时声明 | Anthropic 通常发布弃用政策；先前模型的具体时间线有声明

对于你的实际答案，用你今天有的 model card 做；值会变。练习是培养将 model card 当作规格表而非营销材料读的 _习惯_。当值未声明时，从 API 文档、定价页面和你自己的生产测量中推断。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我在构建 Anthropic Claude Sonnet 4 model card 的规格表摘要。列出九个属性（上下文窗口、知识截止日期、支持的功能、定价、基准结果、安全评估、延迟特征、速率限制、弃用时间线）及 2026 年当前值，并引用 model card 或定价页面中每个值出现的部分。_ 你通过与实际当前定价页面和 model card 交叉检查来验证（LLM 会 hallucinate model card 细节，尤其是具体基准数字和版本特定的功能标志）。LLM 在 _结构化摘要_ 方面有用；你必须验证具体数字。

**Q3.** _为 tokenization 限制的任务做设计_。Beacon Health CodeReview Bot 需要标记包含字面字符串 `# TODO:` 超过 5 次的 Python 文件的 PR。一位初级工程师提议 prompt 模型："看这个 diff 并告诉我它是否添加了超过 5 个 TODO 注释。"批评这个设计并提出替代方案。

提示

这正是 §2.8 警告的那种任务。模型不擅长计数。计数应该实际发生在哪里？