推荐可以合理地对所有内容用 C，也可以合理地对高量查询用 B（节省约 0.004 美元/调用 × 量）且仅对系统分类为法律敏感的查询用 C。选择取决于 Strata 的流量组合和经济学。第 17 章将让你在 prompt 版本间干净地路由。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我有一个带基线 prompt 的 RAG 系统。设计三个针对不同 eval 维度的 prompt 变体，预测每个的提升，并列出结果格式。_ 你验证 (a) 预测提升是合理的（LLM 经常预测过于乐观的增益；对照 V1→V4 Strata 弧做合理性检查），以及 (b) 结果格式覆盖对 _你的_ eval 重要的维度（非通用仅准确率表）。

**Q3.** _为成本压缩 prompt_。以下是一个 380 token 的系统 prompt。将其压缩到 230 token 或更少而不改变模型在代表性 eval 上的行为。指出你做的具体削减及每个的理由。（对于这个练习，假设一个典型的临床摘要器 prompt 带冗余指令和冗长框架。）

提示

寻找：冗余框架（多句话说同一件事）、可以用项目符号的解释性散文、不驱动行为变化的示例。

**答案：** 一个代表性压缩练习（具体输入 prompt 是示意性的；_技术_ 才是重要的）：

原始（冗长）：

> 你是一位高度胜任且经验丰富的 Beacon Health AI 临床笔记摘要器，Beacon Health AI 是一家为医院系统临床医生构建工具的领先医疗技术公司。你的主要角色和职责是获取医生在患者就诊后的口述笔记，并产生可插入电子健康记录（EHR）系统的结构化摘要。你必须仔细遵循这些指南。首先，你应始终产生包含以下部分的结构化摘要：主诉、现病史、检查发现和计划。其次，你绝不能编造任何信息，包括口述中不存在的药物、剂量、诊断或值。第三，如果你对口述的任何元素不确定或口述包含模糊信息，你应用标签 [不确定] 标记不确定元素而非猜测。第四，请使用系统文档中描述的 EHR schema，它使用带必填和可选字段的特定 JSON 格式。

压缩后：

> 你为 Beacon Health AI 将口述临床笔记摘要为结构化 EHR 摘要。
>
> 必需部分：主诉、现病史、检查发现、计划。
>
> 约束：
>
>   * 绝不编造药物、剂量、诊断或值
>   * 用 [不确定] 标记不确定性而非猜测
>   * 使用 EHR JSON schema
>

削减和理由：