这应该改变的一个 prompt 设计决策：**将长的、稳定的、系统级材料放在 prompt 的 _前部_，将短的、按请求的材料放在 _后部_**。反过来排序你就浪费了缓存，因为缓存匹配前缀。具体来说：system prompt，然后 few-shot 示例，然后长的检索上下文，最后用户的查询。这是规范布局，KV cache 的解释是为什么它是规范布局而非任意风格偏好的原因。第 8 章将在形式化上下文工程时回到这个话题。