**答案：** 有用的 prompt：_将四问题准则（模式、eval、故障模式、成本）应用于架构模式"在展示给用户之前对照确定性引用数据库验证 LLM 生成的引用"。对于四个问题中的每一个，要具体：命名 eval 指标和阈值，命名故障模式和缓解措施，给出验证器 API 和延迟开销的 2026 年成本估算。_ 你通过检查 eval 指标是否可操作化来验证（你能否实际构建一个引用密集输出的 golden set 并标注它们？），故障模式是否特定于引用验证（而非通用的"模型可能是错的"），以及成本数字是否来自当前 API 定价页面。

四问题准则在答案足够具体可以行动时最有用；对任何通用答案推回去。