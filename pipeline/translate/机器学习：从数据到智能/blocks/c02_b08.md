**(b) 高方差（过拟合）。** 模型对训练数据拟合完美（100%），但泛化很差（55%）。一个100层神经网络拥有巨大容量，可以有效地记住训练集——包括噪声和特异性细节。100% 训练准确率与 55% 测试准确率之间的巨大差距是过拟合的典型标志。解决方案包括：添加正则化（dropout、weight decay）、增加训练数据、使用更简单的架构，或加入数据增强（data augmentation）。