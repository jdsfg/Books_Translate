1. **带行级锁的数据库事务：** 在单个原子事务中读取和写入余额。
  2. **乐观锁：** 在余额中包含版本号。只有当版本自读取以来没有变化时才写入。如果变化了就重试。
  3. **原子操作：** 使用 `UPDATE accounts SET balance = balance - $amount WHERE id = $id AND balance >= $amount`。这会在数据库中原子地完成读取-检查-写入。