指标 | 每小时值 | 趋势
---|---|---
总输入 token | 450 万 | 平稳
缓存读取 token | 380 万 | 上升
缓存命中率 | 84% | 上升
无缓存成本（估算） | 13.50 美元 | 基线
有缓存成本（实际） | 4.30 美元 | -68%

缓存命中率是运维指标。稳定前缀上命中率 > 80% 表示缓存工作正常。命中率下降（例如从 84% 降到 50%）表示配置回归：也许缓存 TTL 被改变，也许 prompt 被修改以使缓存失效。

仪表板是成本追踪层（第 16 章 §16.1，第 6 层）的一部分。数据来自追踪存储（Langfuse 或等价物）；聚合是 Postgres 视图或仪表板工具查询。

### 18.3 OpenAI 隐式缓存和其他缓存策略

OpenAI 提供 **隐式缓存**（2024 年宣布）——对跨调用的前缀匹配自动生效；无需 `cache_control` 标记。折扣类似（缓存 token 上约 50-80%，因调用频率而异）。权衡：更少显式控制；团队不管理什么被缓存。

其他策略：

**应用层精确缓存**：如相同请求带相同输入反复进来，在应用层缓存响应。完全避免 API 调用。对带确定性输出的常见查询有用。模式：哈希 (prompt, model, parameters) 元组；将响应存储在 Redis/memcached；为缓存命中提供缓存响应。

**语义缓存**：更复杂。见 §18.4（绑定工作流 #10）。

2026 年 Helios 的缓存栈：

* Anthropic prompt 缓存用于系统 prompt 和工具定义（工作流 #9）。
* OpenAI 隐式缓存用于命中 OpenAI 的调用。
* 应用层精确匹配缓存用于频繁复发的 FAQ 式查询。
* 语义缓存用于释义等价查询（工作流 #10）。

每层节省成本的不同部分。组合起来，该栈在路由节省之上将 Helios 的有效每调用成本降低约 30%。
