### 12.2 多模态嵌入

对于某些应用，"什么与此查询相似？"的答案自然是图像，而非文本。示例：
* 用户问"显示包含支付系统架构图的页面"：答案是特定页面图像。
* 用户问"找到与此条款法律结构相同的合同条款"：答案可能涉及结构相似性部分是视觉的条款（缩进、编号、格式）。
* 用户问"找到签名是 [姓名] 的页面"：答案需要匹配签名外观。

纯文本嵌入无法直接索引页面图像。多模态嵌入可以：

* **CLIP**（及其开源后继 **OpenCLIP**）：原始跨模态嵌入；将图像和文本映射到共享嵌入空间。在图像-标题对上训练。在自然图像上强；在文档特定内容上弱。
* **ColPali**：2024 年文档检索专用。在页面图像块和查询文本上使用后交互架构（§12.3 中描述）。对文档 RAG 有生产相关性。
* **Voyage multimodal-3**：Voyage AI 2025 年多模态嵌入；连贯处理混合内容（文本 + 图像）。与其领域特定模型自然配对。
* **Cohere embed-v3-image**（其文本嵌入的扩展）：用于统一文本和图像检索的多模态变体。

2026 年的拆分：

* **对于混合内容的文档 RAG**：使用视觉优先摄取（§12.1）提取结构化文本，然后在提取文本上使用纯文本嵌入。页面图像本身上的多模态嵌入是图像相似性查询的可选层。
* **对于纯图像搜索应用**（找相似产品照片、找有特定视觉布局的页面）：使用多模态嵌入作为主要检索机制。

2026 年大多数生产文档 RAG 使用 **视觉优先摄取 + 提取文本上的纯文本嵌入**。多模态嵌入层是更罕见的专用。
