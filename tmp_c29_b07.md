### F

**回退链**：失败时按序尝试的模型或提供商有序列表。与断路器结合，标准可靠性模式（第 17 章，§17.5）。

**可行性冲刺**：1-3 天时间盒原型，测试 AI 功能是否可实现。几乎总比争论便宜（第 24 章，§24.6）。

**微调**：在任务特定数据上继续训练。2026 年通常指开放权重上的 LoRA/QLoRA 或基础模型的托管微调（第 2 章，§2.3，第 22 章，§22.3）。

**微调半衰期**：微调模型被更新基础模型在零样本模式下超越的时间。当前对多数任务为 6-12 个月（第 22 章，§22.3）。

**Firecracker**：AWS 微 VM 技术，被 Lambda 和某些沙箱服务使用。比容器强得多的隔离，启动成本略高（第 13 章，§13.5）。

**函数调用**：提供商对工具使用的特定名称，模型返回对命名函数的结构化调用。机制上等同于工具使用（第 13 章，§13.1）。

**少样本**：在 prompt 中包含少量输入-输出示例以演示任务。大体量时常被微调超越，但迭代更便宜（第 6 章，§6.2）。

### G

**Gemini Live**：Google 的实时多模态 API，支持语音和视频输入带低延迟输出。OpenAI Realtime 的直接竞争者（第 20 章，§20.5）。

**Gemma**：Google 的开放权重模型家族。适用于设备端和小型服务器部署（第 22 章，§22.1）。

**黄金集**：小型（50-500 示例）手工策划带参考答案的数据集。eval 套件的基石；其他一切在其上构建（第 4 章，§4.3）。

**优雅降级**：设计系统使部分失败（模型不可用、检索为空、工具出错）产生有用响应而非堆栈跟踪（第 23 章，§23.6）。

**gVisor**：Google 的用户空间内核，拦截系统调用，提供容器级隔离加 VM 级安全。常见沙箱选择（第 13 章，§13.5）。

**GPU 小时**：训练和自托管推理成本的标准单位。以 GPU 小时而非美元推理使供应商 vs 自建比较诚实（第 22 章，§22.5）。

**护栏**：执行策略（长度、格式、内容、成本）的生成前或后检查。输出安全 + token 预算控制的统称（第 23 章，§23.1）。
