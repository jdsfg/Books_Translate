5. _按工具断路器_。独立于总成本，单个工具有自己的上限（第 14 章 §14.5）。搜索工具可有每轨迹 10 次调用上限；代码执行工具可有 5 次调用上限。即使总预算宽松，聚合成本保持有界。

生产中有效的设计组合这些：按类别预算带软/硬上限；合理例外日志；工具级上限；合理超支的人工批准续期。agent 有做真实工作的自由；团队保留对失控循环的保护。纯静态断路器太粗糙；纯信任 agent 太危险；混合是工程答案。

此模式连接到第 14 章的有界 agent 设计：边界不是对能力的约束而是 _使能力可部署_ 的架构。没有边界，没有团队能信任 agent 处理重要任务；有了适当边界，agent 可被信任做更多。
