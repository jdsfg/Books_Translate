1. **你在使用主要 provider 的 API**（Anthropic、OpenAI、Google）带原生 JSON schema 支持。Provider 侧强制经过良好优化并与 provider 的计费、可观测性和工具生态集成；库包装器添加 Python 人体工程学而不增加推理成本。
2. **你的代码库是 Pydantic 优先的**。Provider 抽象（`instructor.from_anthropic`、`instructor.from_openai`）和类型化对象返回值对已在 Pydantic 范式中的团队是大的生产力收益。

2026 年分割：provider 侧原生 + `instructor` 用于托管 API 上的大多数生产工作；`outlines` 用于长尾案例。两者可在代码库中共存；为每个管道选正确工具。

**Q4.** §7.7 中命名的结构化输出三个故障模式（schema 太刚性、schema 太宽松、语义但非语法错误）。对每个，提出捕获它的具体设计或 eval 纪律。

**答案：**

1. **schema 太刚性**。用以下捕获：schema 覆盖 eval——对模型在 golden set 中产生的每个输出，验证 schema 能否表示它。如果真实输出无法表示（模型被迫误分类），eval 失败且你重新设计 schema。预防：为长尾重要的有限分类字段包含显式兜底选项（`unspecified`、`other`）。
2. **schema 太宽松**。用以下捕获：通过 Pydantic 约束（`Literal`、`Field(pattern=...)`、`Field(ge=..., le=...)`）更紧的字段类型。为内容规则添加自定义验证器（例如本章的 `no_fabricated_placeholder` 验证器）。验证器成为运行时门；eval 验证验证器捕获它应捕获的。
3. **语义但非语法错误**。用以下捕获：对结构化输出的 _内容_ 做量表或 LLM 作为评判者 eval（第 4 章）。结构验证器必要但不充分；LLM 评判者可以评分填充字段是否实际匹配输入语义。与此配对 eval 中的负面测试切片（§7.11）包含 _正确_ 输出是错误变体的输入——确认模型适当地使用错误路径。

**Q5.** 一个新项目要求你从你从未工作过的领域提取结构化输出（比如地质调查报告或保险承保备忘录）。走一遍你如何在写任何 schema 代码之前决定 _判别联合变体集_。你收集什么信息，从谁那里？

**答案：** 没有领域知识设计变体是陷阱；正确做法是在收集了正确输入之前推迟 schema。过程：

1. **从实际生产输入分布中采样 30 到 50 个真实文档**（非合成、非精选、真实的）。对每个，在任何 schema 约束之前用散文写下 _理想提取输出_ 会是什么样子。这是你的行为规范。
2. **按形状而非内容分桶输出**。理想输出是 _字段 A、B、C 的干净抽取_ 的文档形成一个桶。某些字段不确定或不可解析的文档形成另一个。提取器被要求编造（源中无相关信息）的形成第三个。这些桶是 _自然变体_。
