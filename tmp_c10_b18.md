* 输入：LLM 原始文本包含 `[doc_id:para_id]` 标记。
* 输出：引用对象列表带 `(doc_id, para_id, anchor_position_in_text)`。
* 失败处理：如果 LLM 发出格式错误的引用（doc_id 拼写错误、缺少冒号），记录并要么丢弃引用（保留周围文本）要么向 eval 管道报告警告。不崩溃；遗留 LLM 输出有时有噪声。

**步骤 2：对照检索块集解析引用。**

* 输入：引用列表 + 本次调用传给 LLM 的块集。
* 输出：验证的引用，每个绑定到特定检索块。
* 失败处理：如果引用引用了不在检索集中的 `(doc_id, para_id)`，LLM 幻觉了引用。标记引用为 `fabricated`；不作为有效来源呈现给用户。记录为质量信号。

**步骤 3：用稳定 URL 和片段丰富。**

* 输入：验证的引用 + 源文档元数据存储。
* 输出：引用对象带 `(stable_url, snippet, source_title, effective_date, jurisdiction)`。
* 稳定 URL 从文档的永久标识符计算（例如基于内容哈希的 URL 或每文档 slug），非从瞬态检索时 URL。片段是该段落的文本，如可能高亮引用声明的相关跨度。
* 失败处理：如果源文档元数据存储没有 `doc_id` 的条目（例如文档在检索后从语料库删除），标记为 `stale`；要么丢弃引用或显示 _来源不再可用_ 注释。

**步骤 4：评分引用置信度。**

* 输入：验证的引用 + LLM 在每个引用锚点附近的声明文本。
* 输出：每个引用的置信度分数（高/中/低）和低分的 _建议用户验证_ 标记。
* 评分信号：
    * _高_：引用段落包含声明的直接短语或接近改述。来自小模型的 NLI（自然语言推理）分数高。
    * _中_：引用段落包含相关信息但声明涉及跨段落合成。
    * _低_：引用段落包含关于主题的信息但不清楚支持特定声明。LLM 可能过度引用。
    * 分类器本身是小 LLM 调用或微调 NLI 模型；选择取决于量和延迟预算。

**步骤 5：渲染为响应格式。**

* 输入：丰富的、评分的引用 + LLM 原始答案文本。
* 输出：最终响应对象带答案文本、引用对象列表（每个包含 URL、片段、置信度、管辖权、effective_date）和任何质量标记。
* UI 中每个引用链接到稳定 URL，悬停显示片段，置信度低时显示建议验证徽章。

**步骤 6：记录用于持续 eval。**

* 输入：完整管道输出。
* 输出：分析管道的事件记录引用计数、置信度分布、编造率（步骤 2 中标记为 `fabricated` 的引用）和陈旧来源率。
* 此日志馈送第 19 章的监控；编造或陈旧率飙升触发告警。

**跨管道的质量保障：**
