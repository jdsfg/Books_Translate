### 13.2 工具 schema 作为给模型的文档

工具 schema 被模型读取并影响它何时及如何调用工具。欠规范的 schema 产生混乱行为：选错工具、错误参数、不需要时调用工具。好的 schema 清晰、具体且完整。

设计良好的工具 schema：

* **描述性名称**：`lookup_order` 而非 `get_data`。模型部分按名称选工具；具体名称减少歧义。
* **动作和目的描述**："按订单 ID 查询订单。返回订单详情：状态、商品、总额、配送。"描述告诉模型何时使用工具及它返回什么。
* **约束参数**：`"pattern": "^ORD-\\d{10}$"` 记录期望格式并拒绝格式错误的调用。
* **必选 vs 可选清晰标记**：`required` 字段告诉模型哪些参数是强制的。
* **类型安全**：数字为数字，日期为日期字符串，枚举为 schema 中的 `Literal` 等价物。

设计不良的工具 schema：

* 通用名称（`run`、`execute`、`do`）
* 模糊描述（"执行一个操作"）
* 适合结构类型处用无类型字符串参数
* 缺少使用示例

纪律：**写工具描述如同你在向一个聪明但不熟悉的工程师简报，他需要知道何时调用工具、传什么参数、期望返回什么**。那也是模型需要的。
