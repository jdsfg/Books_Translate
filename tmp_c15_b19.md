**Q4.** _为多 agent 系统定义系统级界_。重读 §14.7 失败 6。为规划器+工作器架构设计系统级界。指定：(a) 每系统轨迹总 agent 数；(b) 跨所有 agent 的成本上限；(c) 递归限制；(d) 界接近时的升级路径。

**答案：**

(a) **每系统轨迹总 agent 数**：上限 12。大多数合理分解需 1 个规划器 + 3–8 个工作器；12 是安全天花板。上限计算轨迹中的每次 agent 调用（规划器 + 工作器 + 任何子规划器 + 其工作器）。

(b) **跨所有 agent 的成本上限**：每系统轨迹 1.00 美元。保守。组合规划器成本 + 工作器成本之和。规划器负责跟踪运行总计并在上限接近时拒绝生成新工作器。

(c) **递归限制**：2 层 agent 生成。规划器可生成工作器；工作器最多生成 1 代子工作器；子工作器不能进一步生成。几何爆炸在架构上被防止。

(d) **升级路径**：

* 在 80% 成本预算时：规划器被通知；应考虑结束轨迹或停止工作并报告部分结果。
* 在 100% 成本预算时：规划器被迫停止并报告；进行中的工作器完成当前调用但不生成新的。
* 在 100% agent 数量限制时：同上。
* 如需升级（任务确实需要超出界允许的）：规划器发出结构化"需人工批准"输出；系统暂停；人工审查并批准更多预算或停止。

升级路径显式且结构化。系统不能透明地超出界；界覆盖需显式人工授权，记录日志。

与单 agent 界相比：系统级界使用 _跨所有 agent 的总计数器_，非每 agent。实现需要共享状态存储（Postgres 或 Redis）跟踪系统资源消耗；每个 agent 在工作前后调用此存储。簿记增加少量延迟（每次调用约 10ms）且对安全至关重要。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为规划器+工作器多 agent 架构设计系统级界：总 agent 数、成本上限、递归限制、升级。_ 你验证 (a) 界捕获 _系统级_ 计数器而非每 agent（LLM 可能默认每 agent 界；坚持全局），以及 (b) 升级是结构化的（非静默失败；显式人工批准流程）。

**Q5.** _为研究 agent 设计界_。你的团队正在为数据科学组构建内部研究 agent。agent 接受问题（"LLM 置信度分数校准的文献有哪些？"），搜索学术来源、内部文档和网络，并产生带引用的综合。典型成功轨迹需 8-12 次迭代，检索 30-50 个来源段落，运行 60-90 秒。agent 有工具：`search_arxiv`、`search_internal_docs`、`search_web`、`fetch_url`、`summarize_passage`、`compose_synthesis`。设计界。指定：(a) 迭代限制；(b) 每调用输出上限；(c) 累积 token 预算；(d) 成本上限；(e) 墙上时钟上限；(f) 每工具断路器；(g) 界接近时的优雅降级行为。

提示
