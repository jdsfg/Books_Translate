### 20.10 How This Breaks：抢话的 agent

**命名的故障模式**：抢话的 agent。轮次结束检测误触发；agent 以为用户说完了但用户只是在停顿；agent 开始说话；用户在思考中被打断。对话崩溃。用户可能挂断、重复自己或变得沮丧。

**真实或复合事件**：复合。一个语音 agent 部署 EOT 阈值设得太激进（200ms 静音）。在通话质量差的电话中，短暂音频掉线触发 EOT；agent 打断用户；投诉累积。团队提高了阈值；投诉下降；延迟成本很小。

**工程缓解措施**：

1. **按用例调优 EOT**。客户支持容忍 350-400ms；团队应对照代表性通话的 eval 验证。
2. **基于置信度的 EOT**。使用对轮次结束产生置信分数的模型；低置信触发更长等待。大多数现代语音模型原生做此。
3. **连接质量感知**。检测通话质量问题（抖动、掉线）并在质量差时增加 EOT 阈值。
4. **优雅打断恢复**。如 agent 确实在思考中间打断，模型应从用户响应中检测到（"我还没说完，我正要说……"）并优雅道歉/恢复。

**本应捕获它的 eval**：_语音对话 eval_ 包含带停顿、犹豫和连接质量问题的录音对话。eval 评分 agent 是否适当处理了它们（无错误打断；打断时优雅恢复）。没有此 eval 切片，团队优化正常路径对话并在生产中发现故障模式。
