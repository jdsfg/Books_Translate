### 12.8 How This Breaks：未处理的页面

**命名的故障模式**：未处理的页面。摄取管道运行；大多数页面成功；一页失败（损坏扫描、不支持语言、VLM API 错误未重试）。失败被记录但未浮现给系统。用户查询返回基于检索块的答案；其查询的相关内容实际在失败页面上；答案因遗漏而错误。

**真实或复合事件**：Strata 的复合。某合同在第 23 页有关键条款。第 23 页扫描质量差；VLM 未能干净提取；管道记录失败并跳过该页。合同索引了第 1-22 和 24-50 页。用户查询该合同的支付条款（在第 23 页上）从合同其他地方检索块；答案因缺少信息而错误。客户在将答案与原始文档交叉引用时注意到。

**工程缓解措施**：

1. **失败页面跟踪**：每次页面摄取尝试有状态（成功/失败/部分）；失败是一等数据，非仅日志行。仪表板显示每文档的失败页面数。
2. **查询时对不完整文档的感知**：当查询从有失败页面的文档检索时，答案包含标记："此合同有 3 个未处理页面；答案可能不完整。"用户知道要验证。
3. **失败页面恢复**：后台作业定期用更强模型或不同 prompt 重试失败页面。恢复率通常 30-60%（一些失败可恢复；一些是永久的如真正损坏的扫描）。
4. **文档级完整性检查**：在文档加入生产索引前，管道验证可接受完整性比率（例如 >95% 页面成功处理）。低于阈值的文档被隔离以供人工审查。

**本应捕获它的 eval**：_覆盖 eval_——一个查询切片，标注相关内容已知在特定页面上；eval 检查检索是否实际返回了那些页面的内容。如页面从索引中静默缺失，覆盖 eval 下降。没有此切片，故障模式不可见。
