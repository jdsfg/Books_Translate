#### 反模式 3：将 LLM 输出视为确定性

**模式。** 代码读取 LLM 响应并如同稳定函数返回一样分派。无 schema 验证。无错误变体。无置信度校准。99% 的情况工作；1% 的情况崩溃应用或更糟，静默产生坏结果。

**团队如何落入。** 工程师写的第一个 prompt 碰巧产生一致 JSON。他们在此基础上构建。他们不加验证因为开发中总是工作。生产流量首次命中 1% 情况——异常用户输入、模型更新、拒绝——代码以难以归因的方式爆炸。

**症状。** `json.loads(llm_response)` 周围的 try/except 块。注释说 _模型通常返回正确格式_。生产日志满是 `KeyError` 和 `ValidationError`。客户报告功能"就是什么都没做"——LLM 发出拒绝，代码未处理，功能静默无操作。

**纪律。** 对随机组件工程（第 2 章）。带可辨识联合的结构化输出：成功、错误、拒绝作为类型系统强制你处理的独立变体（第 7 章）。边界处 schema 验证。决策分支基于 LLM 确定性的置信度校准。

**锚定。** Google AI Overviews（线索 B 故障 2）。"胶水涂披萨"答案是检索层将 Reddit 内容喂给模型且模型将其视为权威——无验证、无来源质量门控、无荒谬检测器。同一根因故障：系统假设其输入和输出比实际更好行为。

**终止标准。** 如你的代码无法处理 LLM 发出意外响应——错误 schema、拒绝、空答案、幻觉字段——代码未达生产就绪。生产就绪由 1% 情况的行为定义，非 99% 情况。
