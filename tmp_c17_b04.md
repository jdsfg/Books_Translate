### 16.3 Helios 主任工程师案例：2026 年架构

Helios Customer AI 的主任工程师（称她 Ada）在 2026 年的一次行业演讲中走过了生产架构。幻灯片结构映射 9 层：

**第 1 层：模型提供商**。Anthropic（Sonnet 4 + Haiku 4）；OpenAI（GPT-4o + GPT-4o-mini）；Google（Gemini 2.5 Flash 用于高量成本优化）。全部通过 API 访问；无自托管。

**第 2 层：路由器 / 网关**。LiteLLM 作为路由器。成本感知回退链（第 17 章，绑定工作流 #8）。每租户预算强制。提供商故障转移。约 5K 行 LiteLLM 配置覆盖所有生产路由。

**第 3 层：编排**。基于 Temporal 的自定义 Python 编排器。Temporal 处理持久工作流侧（重试、调度、状态）；Helios 的自定义代码处理 agent 循环逻辑。团队在 2024 年评估后拒绝了 LangGraph：Temporal 的持久性和可观测性更强；LangGraph 的可视化图吸引力不证明抽象税合理。

**第 4 层：Eval 平台**。pytest-evals + 自定义 Postgres 后端 eval 存储。Golden 集有约 2,000 个案例覆盖各种工单类型。按第 5 章的 CI 门控。生产 eval 采样 5% 轨迹。

**第 5 层：可观测性**。Langfuse 自托管在 Helios 基础设施上。每次 API 调用、每次工具执行、每条轨迹追踪。保留：90 天热，1 年冷。

**第 6 层：成本追踪**。基于 Langfuse 数据的自定义仪表板。按功能、按租户、按模型、按天的成本视图。预算阈值实时告警（第 18 章，绑定工作流 #11）。

**第 7 层：特性开关**。LaunchDarkly（Helios 现有工具）。AI 特定开关：prompt 版本、模型版本、agent 配置版本。用于金丝雀部署和每租户路由变体。

**第 8 层：安全 / 护栏**。按 agent 的自定义输入/输出验证。Prompt 注入防御（第 23 章，绑定工作流 #15）。输出 schema 强制（第 7 章）。无第三方护栏工具：Helios 的策略关注足够特定值得自定义代码。

**第 9 层：应用层**。Helios 的客户支持产品。人工 agent 的 Web UI；终端客户的嵌入式聊天界面；企业集成的 API。

Ada 的演讲强调三个最重要的工程选择：

1. **从第一天起多提供商路由**。即使在 2024 年单提供商运维更简单时，路由层已构建。到 2026 年，路由层是使 Helios 的成本纪律（第 18 章）成为可能的关键。
2. **自托管可观测性**。Langfuse 在 Helios 基础设施上（非 SaaS）给了他们追踪数据控制权并避免了数据供应商锁定。运维成本小；数据控制收益大。
3. **在 Temporal 上自定义编排**。团队选择构建而非购买因为 Temporal 的持久性故事匹配其需求（长时间运行的客户轨迹在进程重启后存活）；LangGraph 在 2024-2025 年无法匹配。

团队会做得不同的两个工程选择：

1. **比应该的更晚构建 eval 平台**。前 18 个月有临时 eval；生产质量衰退在一段时间内未被检测。现在的纪律是 _先建 eval，再扩展_。
