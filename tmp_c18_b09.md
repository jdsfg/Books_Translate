### 17.10 How This Breaks：未经测试的回退

**命名的故障模式**：未经测试的回退。路由器有回退链配置。主模型在生产中首次失败；回退被调用；回退要么失败（不同于已处理的错误类型），要么成功但质量更低（回退模型未经 eval 测试），要么成功但遇到成本问题（回退昂贵且预算未检查）。

**真实或复合事件**：复合。团队配置 `gpt-4o-mini` 为主模型，`claude-haiku-4` 为回退。当 OpenAI 有速率限制问题时，回退启动。回退技术上工作（调用成功），但 prompt 为 GPT 的行为调优并在 Claude 上产生微妙错误输出。团队仅在聚合质量仪表板显示与 OpenAI 事故同时的下降时注意到。

**工程缓解措施**：

1. **对回退链中每个模型运行 eval**。主模型可能在 eval 上最好；回退应通过质量标准即使不在主模型水平。文档化每个模型的 eval 分数。
2. **周期性混沌测试**：故意将一小部分生产流量通过回退模型路由。在真实事故前捕获 _为一个模型调优的 prompt 在另一个上不工作_ 的问题。
3. **层感知回退 eval**：简单层回退应达到或超过简单层质量；标准层回退达到或超过标准层；等等。不要在未显式接受质量下降的情况下从前沿模型回退到工作马模型。
4. **生产 eval 采样**（第 21 章）：生产流量样本持续被 eval 评分。如质量下降与提供商故障转移同时发生，告警浮现关联。

**本应捕获它的 eval**：_跨模型 eval_ 对回退链中所有模型运行每个候选 prompt。如 prompt 的 eval 分数在回退上下降 >X 分，prompt 需修订或回退链需重新配置。无此 eval，回退盲目上线。
