**答案：** 有用的 prompt：_规划 LLM prompt 变更的 A/B 测试：样本量、持续时间、指标、决策标准、提前停止条件。_ 你验证 (a) 样本量考虑 eval 噪声（非仅网页式点击率噪声；LLM 评判噪声更大），以及 (b) 指标包含多维度（LLM A/B 非单指标）。

**Q5.** _调试测量伪影漂移信号_。你的漂移检测器触发：客户支持 agent 的生产 eval 均值 24 小时内下降 0.45 分。调查后，下降原来是测量伪影：评判 prompt 被队友重构 prompt 注册表时静默更新。走查 (a) 发现伪影的调查；(b) 纠正措施；(c) 防止复发的流程变更。

> _提示_：围绕概念检查 Q5 的伪影优先分诊列表组织调查。纠正措施有两层（数据完整性和信任恢复）；流程变更必须关闭产生静默更新的人工路径。

**答案：**

(a) **发现伪影的调查**：

步骤 1（伪影优先分诊，按概念检查 Q5 纪律）：在深入研究系统漂移前，走查伪影列表。

* _评判模型变更_：评判模型版本已固定，当日无提供商发布说明。干净。
* _采样变更_：配置中按功能采样率未变。干净。
* _评分标准或黄金集变更_：伪影在此浮现。eval prompt 注册表 26 小时前有一个提交：_"重构：合并评判 prompt。"_ 提交者意图是无操作重构但无意中改变了评分标准 _引用_ 维度的评分方式，使相同输出在该维度上降低约 0.4 分。
* _追踪存储 schema_：干净。
* _时间窗口比较_：基线期正常；下降在提交后窗口中。

步骤 2（验证假设）：用 _新_ 评判 prompt 对基线期的 100 条轨迹重新运行 eval。结果：那些基线轨迹在引用上也降低约 0.4 分。系统未漂移；测量漂移了。

(b) **纠正措施**：

1. _通过 prompt 注册表回滚评判 prompt 变更_（注册表的纪律支持此操作；回退清单版本）。
2. _用原始评判 prompt 对受影响期重新评分_ 以恢复趋势线。仪表板将在重新评分完成后显示假下降消失。
3. _在事后复盘模板中文档化事件_，即使无客户受影响。目的是使伪影恢复工作可见以便团队下次更快识别模式。
4. _更新告警消息_ 提醒值班在呼叫工程领导层前运行伪影优先分诊。告警路由正确；措辞可改进。

(c) **防止复发的流程变更**：

1. _eval prompt 注册表变更需对照固定基线的 eval CI_。任何触及评判 prompt 的提交必须对 50 条冻结参考轨迹运行评判并生成差异报告。如任何维度分数偏移超过 0.1 分，提交被阻止直到作者确认意图。
