* **> 10M token（RAG 胜）**：Strata 的法律语料库（数亿 token 的案例法和法规）。长上下文放不下；即使 Gemini 2.5 Pro 上限 2M。RAG 是唯一选项；问题变成如何工程化好 RAG（第 9–12 章）。

层级边界假设 2026 年模型上下文大小。随着上下文窗口增长，边界移动。决策树结构（基于语料库大小的 3 层）是持久制品。

**Q4.** 为什么 _升级模式_（§8.7 Beacon 示例）是常见上下文工程方法，什么决定是否应用它？

**答案：** 升级模式使用便宜上下文作为默认，仅在便宜版本信号不确定时切换到昂贵上下文。好处是大多数调用（通常 >80%）支付便宜成本；只有困难案例支付昂贵成本。平均成本被便宜路径主导。

它适用于：

* 便宜上下文充分覆盖大多数案例。没有这个，升级率太高且平均成本接近昂贵版本。
* 模型能可靠地信号不确定。第 7 章的判别联合模式给模型类型化的方式说 _我不确定_；升级模式依赖此信号有意义。
* 便宜和昂贵上下文之间的成本差距大。如果差距小，升级逻辑不值得工程开销。

它不适用于：

* 每次调用都需要完整上下文（无升级节省可能）。
* 模型的不确定信号不可靠（升级率要么太高要么太低）。
* 昂贵路径的延迟即使对小升级部分也不可接受（某些实时系统不能容忍）。

对于 Beacon，模式适合因为大多数临床笔记是典型的，模型的置信信号与实际临床复杂性相关，长上下文的延迟惩罚仅命中约 15% 值得的案例。模式为 Beacon 节省了约 70% 的平均推理成本 vs 总是昂贵。

**Q5.** 为你在第 8 和 9 章中现在见到的三个语料库大小带重新锚定长上下文 vs RAG 决策（§8.6）。对语料库大小 (a) 80K token、(b) 1.2M token、(c) 40M token，陈述决策、识别主导决定因素、并命名该带最常见的 _错误_ 答案。

**答案：**

(a) **80K token：长上下文胜。** 主导决定因素：舒适适合有余量；构建 RAG 管道增加分块、嵌入、向量数据库、检索 eval 开销而无质量增益。最常见的错误答案：仍然构建 RAG 因为 _真正的生产用 RAG_。这是过度工程；本章第一层明确反对它。团队应将完整语料库放入系统 prompt 并对稳定部分使用 prompt 缓存（可缓存部分成本保持低），一周内发布。
