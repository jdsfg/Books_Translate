* **(a) 数 CSV 项目**：不要依赖模型。模型不擅长计数，尤其是当计数是主要请求的操作时。在代码中做计数：在逗号上分割字符串（如果相关则处理转义逗号）并取长度。模型在 _理解_ 列表方面有用，但在 _计数_ 列表方面不行。
* **(b) Slugify**：不要依赖模型做规范 slug。使用代码库（例如 Python 的 `python-slugify`）。模型会为英文文本产生合理的 slug，但在边缘情况（Unicode、非常长的标题、大部分是标点符号的标题）上不一致，而库在这些情况下可靠。如果你真的想要 LLM 参与，用它来 _提议_ 一个 slug，然后由确定性后处理器规范化。
* **(c) URL 提取**：这个是边界情况。模型可以从短散文中合理好地提取 URL。对于生产可靠性，尤其是 URL 可能格式错误或以不寻常方式嵌入时，优先使用正则表达式（或解析库）来提取候选 URL，只有在任务是 _这些 URL 中哪个与用户意图最相关？_ 时才使用模型，这是一个模型处理得好的判断任务。
* **(d) SQL 生成**：这是模型的强项，不是弱项。从自然语言生成 SQL 是模型在训练中大量见过并擅长的任务，_只要你给它 schema_。在 prompt 中提供表定义。模型可以产生有用的 SQL；在生产中，你可能想加一个验证器（解析 SQL，检查它不包含如删表等不安全操作，限制为只读）。模型是高级任务的正确工具；代码是安全后处理的正确工具。

通用原则：将模型用于涉及 _理解语义_ 或 _生成流畅文本_ 的任务；将代码用于涉及 _对字节/字符的精确机制操作_ 的任务；当两者都需要时组合使用。第 13 章将用工具调用来形式化这一点。

**Q4.** 为什么 prompt 注入攻击"更偏向架构性而非可通过 prompt 修复"？在答案中引用 §2.3 和 §2.7 的心智模型。

**答案：** prompt 注入攻击利用了一个事实：在架构层面，模型在整个输入序列上做条件化并相应地计算下一个 token 分布。没有内部机制将"来自开发者 system prompt 的可信指令"与"来自用户消息或检索文档的不可信内容"分开。system 角色的特权通道状态是 RLHF/DPO 期间安装的 _训练行为_（§2.3）：模型被展示了许多应该优先 system 角色指令而非冲突 user 角色指令的示例，所以它倾向于这样做。但这种偏好是统计性的，不是架构性的。一个足够聪明的用户消息或检索文档中的一段注入内容可以将模型的下一个 token 分布偏移到足以覆盖训练偏好。
