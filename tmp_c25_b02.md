### 24.2 开发工作流：规格 → eval → prompt → 部署 → 监控

AI 功能的开发工作流有五个阶段。每个有产物；每个有门控。

**阶段 1：规格**。AI 产品工程师（和 PM）起草功能规格。超越经典产品规格元素，AI 规格包括：

* _AI 特定成功标准_：_AI 工作_ 意味着什么？操作化。
* _Eval 设计初步_：这能被评估吗？如能，用什么黄金集和评分标准？如不能——_我们能 eval 这个吗？_ 门控（§24.4）——在此终止功能。
* _成本信封_：发布时和规模时每用户活跃日的预期成本。
* _安全考虑_：哪些威胁类别适用？防御是什么？

**阶段 2：eval**。在构建功能前，构建 eval 套件。黄金集；评分标准；CI 集成。这是第 4 章的纪律在功能发布边界应用。

eval 优先纪律对严肃生产工作不可商量。先构建功能后构建 eval 的团队发现自己无法安全发布；eval 是门控，没有 eval 就无法门控。

**阶段 3：带 prompt 迭代的构建**。有了 eval，针对 eval 迭代 prompt 和架构。每次迭代被测量；改进被保留；回归被回退。

Helios 的典型模式：针对 eval 的 2-4 周 prompt 迭代后功能才准备好生产灰度。到结束时，功能通过 eval 阈值。

**阶段 4：带金丝雀部署**。生产发布遵循金丝雀模式（第 21 章 §21.6）：2% → 10% → 50% → 100% 在 2-4 周内。每步由观察到的生产行为匹配离线 eval 预测来门控。

**阶段 5：监控和迭代**。一旦达到 100%，功能进入持续监控（第 21 章）。生产 eval 采样；漂移检测；用户反馈信号。迭代继续，prompt 演进、工具添加、agent 变得更好。

工作流从规格到 100% 发布大约 6-12 周（对实质性功能）。更小功能压缩到 2-4 周。跳过阶段是故障模式；通过跳过 eval 或金丝雀压缩的团队发现自己发布后有质量或可靠性事件。
