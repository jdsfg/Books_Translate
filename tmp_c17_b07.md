### 16.6 How This Breaks：无人拥有的层

**命名的故障模式**：无人拥有的层。架构草图存在；团队认为他们有全部 9 层。实际上，一层（通常是可观测性或成本追踪）半建成；无人拥有；无人监控。麻烦的第一个迹象是生产发生缺失层本应捕获的事故。

**真实或复合事件**：来自 2025-2026 多个团队的复合。团队的架构图显示通过 Langfuse 的可观测性。实际上，Langfuse 集成覆盖了一些 agent 路径但不覆盖其他；一些关键工作流零追踪。当客户报告间歇性质量问题时，团队没有生产数据可调查：受影响工作流的追踪不存在。修复花了数周：为缺失工作流插桩；回填历史追踪（对过去事件不可能）；添加监控以检测未插桩工作流。

**工程缓解措施**：

1. **每层显式所有权**。每层有命名所有者对其运维健康负责。跨团队层（成本追踪跨工程+财务+运维）有多个所有者带显式职责。
2. **层完整性审计**。季度审查：参考架构中每层是否在运行？覆盖率是多少（正确触及层的工作流百分比）？缺口浮现并分配。
3. **自动化覆盖检查**。特别对可观测性：合成追踪检查运行通过每条工作流路径并验证追踪产生。自动检测未插桩路径。

**本应捕获它的 eval**：_架构覆盖 eval_ 通过每个生产工作流运行合成交易并验证每个工作流的交互命中预期层（路由器、可观测性、成本追踪）。绕过层的工作流被标记。这在 _实际未插桩_ 问题变成面向客户的事故前捕获它。
