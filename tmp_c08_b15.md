3. **就样本未覆盖的边缘案例采访领域专家**。_什么样的保险备忘录看起来正常但实际上需要专家判断来解读？什么样的备忘录承保人会拒绝做决定并升级？_ 这些罕见案例成为候选变体；并非所有都成为变体（如果下游代码以相同方式处理，你可以将罕见案例折叠进现有变体）。
4. **决定哪些桶是一等变体 vs. 单一变体内的字段。** 成功变体上的 `confidence: float` 是字段；`HumanReviewRequired` 变体是独立的一等案例。决策：下游代码是否不同分支？如果是，变体。如果否，字段。重读第 7 章 §7.4 Q1 在新领域的背景下；原则（下游分支驱动判别）是可移植的。
5. **在任何 Pydantic 代码之前在纸上勾勒变体集**。真实领域通常 3 到 5 个变体；10 个变体暗示过度判别（你的下游代码可能将几个变体折叠到同一处理器，那种情况下合并它们）。
6. **通过重放样本验证草图**。对原始 30 到 50 个采样文档的每个，哪个变体捕获理想输出？如果文档不适合任何变体，你有差距；要么添加变体要么拓宽一个。如果许多文档歧义地适合多个变体，你的判别器不清晰；精炼。
7. **然后写 Pydantic 代码**。变体集对照真实数据验证后，schema 直截了当实现。

整个过程对真实领域需要 1 到 3 天。跳过它产生 §7.7 的故障模式（schema 太刚性或太宽松），成本远超前期设计工作。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我在为一个不熟悉的领域设计判别联合 schema。走一遍如何在写 schema 代码之前决定变体集，以及我需要收集什么数据。_ 你验证 (a) LLM 强调 _先采样真实文档_ 而非跳到变体类型（LLM 回答经常从 schema 开始并将采样视为可选；反转顺序），以及 (b) LLM 提议的变体集不仅是 _成功/失败_；真实领域通常有一个中间的 _需人工审查_ 变体，LLM 除非被提示否则会省略（见 §7.4 的三变体模式）。

**Q6.** _从生产追踪诊断 schema 太刚性 vs. schema 太宽松故障_。你有一个运行 §7.4 判别联合 schema 的生产提取器。上周出现两个故障集群：（集群 A）模型在上周 18% 的调用上返回兜底 `unspecified` 值，比上月 4% 上升。（集群 B）下游验证接受了模型输出，但人工标注正确率从 0.91 降到 0.79。对每个集群，识别 schema 是太刚性还是太宽松，什么变了（可能原因），以及修复。

提示

§7.7 的两个故障模式是诊断类别。集群签名指向哪个模式适用。

**答案：**
