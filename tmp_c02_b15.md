#### 应用题

**Q1.** _将四问题准则映射到你心中的一个功能_。选择你的团队正在交付或考虑的一个真实 AI 功能。为它写四问题准则：

1. 模式是什么？
2. eval 是什么样的？
3. 它在生产环境中可能如何失败？
4. 成本是多少？

然后指出四个答案中哪个目前最弱。概述能加强它的工程工作。

提示

大多数团队对 (1) 有清晰答案，对 (2) 和 (3) 有模糊答案。最弱的通常是 (2)，即 eval，因为团队还没有把它写下来。

**答案：** 一个使用 Beacon Health AI 的 CodeReview Bot 的示例（你的真实答案应该来自你自己的工作）：

1. **模式**：结构化输出提取。bot 读取 pull-request diff 并生成结构化审查对象：评论列表，每条包含文件、行范围、严重性和理由。
2. **eval**：一个约 200 个历史 PR 的 golden set，每个由资深工程师手动标注应该生成哪些审查评论。eval 指标是针对标注的每条评论的 precision 和 recall，加上一个 _无虚假评论_ 阈值（误报比遗漏更糟糕，因为审查者时间是瓶颈）。
3. **故障模式**：(a) bot 建议的修改是错误的，因为它误读了周围上下文（距离 diff 超过约 50 行）；(b) bot 建议的修改与不在 prompt 中的团队风格约定冲突；(c) bot 生成的 JSON schema 格式错误，导致 GitHub Actions 步骤崩溃。
4. **成本**：使用 Sonnet 4 按 3 美元/15 美元每百万 token 计算，一个典型的 PR 约 2K token 上下文和约 1K token 输出，每个 PR 约 0.021 美元。团队每天约 50 个 PR，所以每天约 1 美元推理成本。成本主要由释放的人力时间主导，而非模型支出。

目前最弱的答案是 (2)：团队对"这条评论有用吗？"有模糊感觉，但还没有构建带手动标注的 200-PR golden set。加强它的工程工作大约是两名资深工程师两天的标签编写加上一名工程师一周将 `pytest-evals` 接入生成的 golden set 并集成到 CI 工作流中。之后，功能有了可辩护的交付门槛，团队可以检测跨 prompt 变更和模型升级的回归。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
