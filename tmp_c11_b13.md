元数据过滤流程：


    1. 查询分析（小 LLM 调用或基于规则）提取：
       - 管辖权（CA、NY、联邦、未指定）
       - 日期约束（effective_as_of、未指定）
    2. Pinecone 过滤：jurisdiction == 提取值 OR jurisdiction == 联邦
    3. BM25 过滤：管辖权匹配（相同逻辑）
    4. 日期过滤：effective_date <= 提取日期 AND
       (deprecation_date IS NULL OR deprecation_date > 提取日期)
    5. 两个检索对过滤子集运行
    6. RRF 合并
    7. 重排序器 top-20 → top-5
    8. LLM 带检索块 + 引用要求生成


架构处理所有三种查询类型：改述查询依赖向量；精确引用依赖 BM25；日期限定查询依赖元数据过滤。混合在查询分布上产生一致质量。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为法律 RAG 设计混合搜索覆盖改述问题、精确引用和日期限定查询。指定信号、融合和元数据过滤。_ 你验证 (a) 融合算法（RRF）被正确描述（LLM 有时描述加权平均融合在规模上次优），以及 (b) 元数据过滤逻辑正确处理"未指定约束"的边界情况（关于"加州消费者保护法"无日期的查询应默认为 _当前_ 生效日期，而非无日期）。

**Q3.** _构建索引管道的变更检测层_。你在索引存储为 git 仓库中 Markdown 文件的策略文档语料库。设计变更检测机制。指定：(a) 触发器；(b) 检测什么；(c) 什么触发哪些内容的重新索引；(d) 如何处理删除。

**答案：**

(a) **触发器**：policy-docs 仓库上的 git post-commit 钩子，或轮询 `git log` 获取上次索引 SHA 之后新提交的守护进程。钩子更响应（亚秒）；轮询更简单（无需仓库写权限）。

(b) **检测什么**：对每个提交，计算 `git diff --name-only commit_a..commit_b` 获取变更文件列表。对每个变更文件，通过路径匹配确定是否为策略文件（vs README、CI 配置等）。

(c) **什么触发重新索引**：

* _文件添加_：解析、分块、嵌入、插入。
* _文件修改_：解析、分块、嵌入；替换现有块（按 `source_id`）。
* _文件重命名_：作为 `删除 + 插入` 处理（旧 `source_id` 消失；新块在新 `source_id` 下）。
* _文件删除_：从索引删除该 `source_id` 的现有块。

(d) **删除处理**：从向量索引和 BM25 索引硬删除已删除文件的块。如为审计目的使用软删除，标记块 `deleted_at` 并在检索时过滤掉，后台作业在保留窗口后硬删除。

值得处理的边界情况：
