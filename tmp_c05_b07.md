6. **检查偏好对的位置偏见。** 当比较两个候选 A 和 B 时，LLM 评判者通常有位置偏好（有些偏好 A；有些偏好 B；偏见不总是直觉的）。缓解是将每对运行两次，一次作为 (A, B) 一次作为 (B, A)，然后平均。评判者成本翻倍但消除位置偏见。

LLM 作为评判者足够强大，已成为 2026 年细微 eval 的主导评分方法。它也足够危险，没有上述校准纪律就部署它的团队产生看起来好但系统性误导的 eval。使用它；校准它；永远不要信任未验证的版本。

#### 常见误解

LLM 作为评判者并不消除对人工标注数据的需求，尽管这个说法经常被重复。它消除的是 _每次 eval 运行时的人工评分_。你仍然需要在 _校准_ 时使用人工标签——你用来验证评判者与人工一致的那一小批人工评分输出。没有校准集，你不知道评判者是在产生有意义的分数还是在每个输出上以相同信心 hallucinate。将人工标签视为校准能力的战略投资，而非每次 eval 的经常性成本。成本摊销；校准持久；eval 变得可信。
