4. **如回归持续**：考虑 _查询分类器_ 按查询决定 top-N（一次小 LLM 调用）。综合查询获 top-N=7；查找查询获 top-N=3。更多工程但更高效。

通用教训：重排序器的工作是 _精度_；如果你还需要 _recall_（综合查询），重排序器的缩窄必须按查询类型调优。设低 top-N（更干净上下文、更低成本）的诱惑是真实工程陷阱。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我们的重排序器集成改善了检索 recall 但损害了总体通过率；答案感觉更薄。诊断并提出修复。_ 你验证 (a) 诊断显式命名 _top-N 缩窄_ 机制（LLM 可能模糊诊断为"重排序器质量问题"；坚持具体 top-N 原因），以及 (b) 提议的修复提到向 eval 添加完整性轴（LLM 可能只给 top-N 调整；eval 轴差距是持久教训）。

**Q6.** _为模糊查询设计 HyDE_。你的法律研究产品收到短而模糊的查询如"Smith 责任案"——用户指特定案例但查询缺少引用、管辖权或年份。标准向量搜索返回跨管辖权的数十个 _Smith_ 匹配。设计基于 HyDE 的预检索步骤以改善此模糊查询切片的精度。指定：(a) HyDE prompt；(b) HyDE 输出如何馈入检索；(c) HyDE 选错管辖权时的处理；(d) 确认提升的 eval 切片。

**答案：**

(a) **HyDE prompt**：聚焦法律领域指令，请 LLM 生成假设案例摘要而非通用答案。

    你是法律研究助手。用户询问了一个案例但他们的查询是模糊的："{query}"。

    生成一个 *假设* 案例摘要，如同你在为用户最可能指的案例写一段 200 词的
    百科条目。包括：
    - 涉及当事方（在查询中给出的名字处使用）。
    - 可能的管辖权（美国联邦、美国州、英国、欧盟；选择查询框架最常见的
      匹配）。
    - 可能的法律领域（合同、侵权、刑事、监管）。
    - 可能的程序状态和结果。

    以自然散文输出摘要。不要包含免责声明；这是检索增强步骤，
    不是面向客户的答案。


输出是一段看起来像真实案例摘要的段落。其词法和语义特征比用户的短查询更接近实际案例文档。

(b) **HyDE 输出馈入检索**：嵌入假设案例摘要（非原始查询）；用该嵌入做向量搜索。top-K 结果然后交给重排序器，重排序器现在对原始用户查询评分（非 HyDE 输出，以避免放大任何 HyDE 幻觉）。

关键管道顺序：

* 用户查询 → HyDE LLM 调用 → 假设摘要 → 嵌入 → 向量搜索 top-30 → 重排序器（对原始查询评分）→ top-5 → LLM 生成。

(c) **HyDE 选错管辖权时的处理**：HyDE 可能猜美国联邦而用户指英国；检索然后为英国问题返回美国案例。两个保障：
