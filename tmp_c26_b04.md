#### 反模式 7：无安全架构就发布

**模式**：部署多租户或 RAG 支持的 AI 而无四层 prompt 注入防御（第 23 章 §23.3）。系统在良性流量上工作。第一个复杂对抗输入——通常作为检索文档、工具输出或用户上传中的不可信内容到达——通过并外泄数据、执行未授权行动或揭示系统 prompt。

**为何失败**：通过检索或工具来源内容的间接 prompt 注入是 2025-2026 主导的 agent 系统威胁。现代模型训练安装 _一_ 层防御（模型会拒绝某些攻击）；生产系统需要四层（上下文内防御、输出验证、不可信内容标记、间接注入缓解）。2025 年生产中 prompt 注入事件（线索 B 故障 3，第 23 章）是经典案例：一个恶意客户上传的文档劫持了一个企业客户服务 agent 发出另一个客户的数据；公司的 BAA、模型自身的拒绝和 eval 套件都未能捕获它因为每层都是单一层。四层防御模式从行业对此类事件的响应中浮现。

**论述章节**：第 23 章（四层 prompt 注入防御）。第 13 章（工具执行沙箱和策略门控）。第 19 章（浮现异常跨租户模式的可观测性）。

**终止标准**：如你有租户系统、RAG 系统或任何处理不可信内容的 agent，且你没有 §23.3 的全部四层就位，你有一个已知未解决的威胁类别。带层就位发布或不发布。

### 25.2 速查表

AI 工程团队验证不在反模式中的简短检查清单。从团队角度，每个工程师能对这些回答 _是_ 吗？

* **对每个生产 AI 功能，我们有每次变更都运行的 eval。**
* **我们能说出按功能月度成本；按租户月度成本；按调用成本。**
* **我们的 LLM 输出用可辨识联合结构化处理成功/错误/拒绝变体。**
* **每个 LLM 调用被追踪；我们的 agent 有轨迹级可观测性。**
* **我们的 agent 有迭代上限、成本断路器和按工具断路器。**
* **我们的 prompt 在注册表中版本化；非埋为内联字符串。**
* **我们的 LLM-as-judge 已在有意义集合上对照人类校准。**
* **我们有提供商路由层；非单提供商锁定。**
* **我们有在工具执行层强制执行的跨租户数据隔离。**
* **我们在功能范围界定时有 _能否 eval_ 门控；我们在此终止过功能。**
* **我们有至少执行过一次的模型升级工作流。**
* **我们有事件事后复盘文化；教训反馈到 eval 套件。**

全部 12 项回答 _是_ 的团队处于 2026 生产 AI 工程能力水平。多项回答 _否_ 的团队有具体缺口需弥合。
