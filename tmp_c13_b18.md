**月度总支出**：3K 美元 + 5K 美元 + 2K 美元 = 10K 美元，舒适地在 2 万美元预算下。剩余 1 万美元留作缓冲用于：
* 激增摄取（新文档批次；并购尽职调查；新客户入驻）。
* 质量 eval 重跑（定期以更高质量重新摄取以测量漂移）。
* 应对意外视觉 token 价格变化的成本储备。

分层和升级方法是关键思想：热文档获得最佳处理因为其查询价值证明支出合理；冷文档获得最小处理但可在实际重要时按需升级。此模式泛化到法律语料库之外：任何有偏斜查询分布的领域都受益。

交叉引用：这是第 9 章渐进披露模式的多模态版本：在价值集中的地方花最多。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_在 100 万页法律语料库的热/温/冷层间分配 2 万美元/月视觉 token 预算：处理方法、分辨率、模型、每层支出。_ 你验证 (a) 分配是价值加权非量加权（LLM 可能默认按页数比例；那是错误启发式），以及 (b) 冷页面的查询时升级机制是显式的（LLM 可能忘记升级路径并将冷页面视为永久无视觉处理）。

**Q6.** _诊断签名块提取失败_。Strata 的多模态 RAG 整体处理签名查询良好（"是否已签署？"查询约 85% recall）但在一种特定文档类型上持续失败：授权委托书表格，签名是印章而非手写标记。VLM 要么遗漏印章要么报告"未找到签名"。诊断故障模式并提出不降低标准手写签名性能的修复。

**答案：**

诊断：VLM 的训练分布强调 _手写_ 签名（其训练语料库中的主导签名类型）。橡皮印章签名和盖章的授权委托书是分布外的。模型要么 (i) 不识别印章为签名标记，要么 (ii) 识别为文本并转录而不标记签名语义。

这是长尾提取失败。如 eval 切片包含授权委托书文档，eval 量表（上面的 Q5）会捕获它；没有该切片，它保持不可见。

修复，按优先级排序：

1. **用显式印章签名指令丰富 prompt。** 更新提取 prompt 以枚举签名类型："识别此页面上的所有签名标记。签名标记包括：手写签名、橡皮印章、浮雕印章、电子签名块和公证印章。对每个，报告位置、类型和任何可见标识文本。"prompt 级修复便宜且通常足够。
2. **印章签名的少样本示例。** 在 prompt 中添加 2-3 个标注示例：带印章签名的授权委托书，带正确提取。少样本示例显著改善 VLM 在分布外类型上的性能（第 6 章教训在此也适用）。
