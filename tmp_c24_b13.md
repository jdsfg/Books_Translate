**第四层：间接注入缓解**。来源归因和信任评分；基于模式的预筛；跨租户隔离；输出-行动隔离。_捕获_：防止注入试图诱导的行动，即使模型推理被部分攻破。硬屏障在此。_不捕获_：不匹配已知模式的新型攻击；非常复杂的攻击可能规避信任评分。

复合效应：每层提高攻击者所需的复杂度。单层防御可绕过；四层组合大幅提高门槛。

**Q3.** 为什么 _工具执行层的跨租户数据隔离_ 是防御模式中最硬的屏障（§23.7）？

**答案：** 工具执行层强制执行的跨租户隔离是最硬的屏障因为：

1. **它是确定性的，非概率性的**。其他层依赖 LLM 的行为（训练拒绝；遵循 prompt 指令）。LLM 是概率性的；攻击者的特定输入可能在平均输入失败处成功。工具执行中的跨租户隔离是确定性代码：`if user.tenant_id != requested.tenant_id: raise PermissionDenied`。代码没有"状态不佳的日子"。
2. **它在 LLM 决策之后执行**。即使 LLM 被注入说服尝试跨租户数据访问，工具执行层的策略检查在实际数据访问前触发。LLM 的攻破不传播到数据层。
3. **它是标准工程，非 AI 工程**。多租户数据库访问控制是充分理解的后端工程；模式成熟；失败模式有界。
4. **它可审计**。跨租户访问尝试在工具层记录；调查直接；合规可验证控制。

在近似失误事件（§23.7）中，四层防御阻止了攻击。如前三层失败且 LLM 尝试跨租户查询，第四层的确定性隔离仍会阻止数据访问。这是 _纵深防御_ 原则：硬屏障捕获软屏障遗漏的。

**设计含义**：多租户 AI 中最重要的安全投资不在 LLM 层防御；而在工具执行层的确定性数据范围。即使 LLM 层防御失败，安全数据层的团队被攻破的难度大得多。

**Q4.** "我不知道"作为质量信号意味着什么（§23.5）？为什么它比自信但错误的答案更可取？

**答案：** "我不知道"作为质量信号：良好工程的 AI 系统应 _偏好拒绝_ 而非 _虚构_。当模型不确定时，正确输出是 _"我没有关于此的可靠信息；让我为您转接人工"_ 而非自信的编造。

它比自信但错误的答案更可取因为：

1. **用户可基于拒绝行动**。他们知道需要验证或寻求其他帮助。自信的错误答案导致他们基于错误信息行动；基于错误信息行动的成本通常远高于需要验证的成本。
2. **Air Canada 原则（第 1 章）**。你的 AI 的输出是你公司的声明。自信的错误声明是公司责任；优雅拒绝是服务决策。
