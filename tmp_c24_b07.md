### 23.6 回退链和优雅降级

生产系统需要优雅的失败路径。当主路径失败时，回退选项保留部分用户价值。

三类失败模式：

**模型不可用**：提供商宕机；速率限制；超时。路由器中的回退链（第 17 章 §17.5）。

**能力不匹配**：agent 遇到超出其范围的任务。回退：升级到人工（或更适合该任务的不同 agent）。

**基于置信度的降级**：agent 的置信度太低无法自主行动。回退：拒绝自主行动；向用户呈现情况和选项（"我无法确定此事。您是否希望我为您转接人工客服？"）。

纪律：**每个行动路径都有定义的失败路径**。用户总是获得 _某种_ 响应，即使不是预期的。

Helios 的升级流程：当客户支持 agent 无法自信解决时，它组合一个结构化交接文档（客户上下文；到目前为止的对话；具体问题或议题；agent 的初步评估）并路由到人工客服。人工接手时是温暖的；客户不需重新开始。
