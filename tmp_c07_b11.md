#### 概念检查

**Q1.** 一位队友争辩说 _2026 年 prompt 工程已死；现代模型直接就能工作_。回应这个论点。指出两个 prompt 工程仍然承重的特定用例，以及一个论点部分正确的用例。

提示

§6.1 的四个用例。

**答案：** 论点部分正确（基本 prompt 在 2026 年工作；2022 年的许多技巧现已过时）部分错误（prompt 工程的特定用例仍然承重）。两个仍然必不可少的用例：

1. **约束输出结构**。即使有原生 JSON schema 模式，结构化输出的 _内容语义_（每个字段应包含什么、何时使用联合类型的哪个变体、如何处理不确定字段）由 prompt 设定。一个构建结构化抽取管道但不注意 prompt 级别的团队产生验证通过但语义错误的 JSON。第 7 章构建这个。
2. **引出工具使用**。工具 schema 描述可用工具，但何时调用哪个工具、带什么参数、如何组合工具输出的 _策略_ 是 prompt 级决策。Agent 系统的生死取决于这个 prompt 工作。第 13 章。

论点部分正确的地方：**基本推理任务**。对于前沿模型上的常规分析问题，_让我们一步一步思考_ 不必要；模型自己做了。坚持每个 prompt 都需要 2022 年咒语的队友在用过时的心智模型操作。

综合：2026 年的 prompt 工程比 2022 年更具体更聚焦。工作量减少了；剩余的工作更集中更重要。

**Q2.** 解释 _思维链_、_ReAct_ 和 _Self-Refine_ 的区别。指出各自是正确工具的场景和各自是过度的场景。

**答案：**

* **思维链**：要求模型在最终答案前产生中间推理步骤。_正确工具_：工作马梯队模型上的复杂数学问题，默认推理太压缩。_过度_：模型首轮输出已经正确的简单抽取任务。
* **ReAct**：交替推理、行动（工具调用）和观察。_正确工具_：任何工具增强 agent；这是现代工具使用 API 原生编码的模式。_过度_：无工具使用的单调用任务；ReAct 在模型无需行动即可处理的任务上是浪费。
* **Self-Refine**：模型批评自己的输出并修订。_正确工具_：推理成本翻倍值得质量提升的高风险输出（法律起草、临床摘要、正式通信）。_过度_：成本翻倍只为边际质量提升的高量、低风险任务。

三种模式不互斥。生产系统经常分层：Self-Refine 包装使用 CoT 推理步骤的 ReAct 循环。成本累加；当 eval 证明成本合理时使用模式。

**Q3.** 什么是 _prompt 注册表_，它解决什么具体问题？指出两个它 _不_ 解决的问题。

**答案：** Prompt 注册表是将 prompt 视为一等版本化制品的系统：存储在各自文件中、独立评估、通过清单部署。它解决的问题：
