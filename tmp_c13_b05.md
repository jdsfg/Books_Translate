### 12.4 图表、表格和结构化数据提取

文档不仅包含散文还有 **结构化数据**：数字表格、图表和图形、原理图。在这些上的 RAG 需要将结构化数据提取为可用形式。

2026 年方法：


    def extract_table_from_image(page_image: bytes) -> StructuredTable:
        response = vision_model.complete(
            model="claude-sonnet-4-20260315",  # Sonnet 4 在同一模型中处理视觉；
            # 不存在单独的视觉变体
            messages=[{
                "role": "user",
                "content": [
                    {"type": "image", "source": ...},
                    {"type": "text", "text": "将此图像中的任何表格提取为 JSON "
                    "对象数组。包括列名和行值。如存在多个表格，"
                    "以列表返回。"},
                ],
            }],
            response_model=StructuredTable,
        )
        return response


VLM 产生结构化数据：行列表，每行是列到值的字典。这进入索引作为两者：
* **结构化可查询数据**（在关系表或 JSON 文档存储中）用于按数值或分类值过滤的查询。
* **Markdown 格式文本**（作为文档块的一部分嵌入）用于通过内容语义相似度检索表格的查询。

图表和图形经过类似模式：VLM 产生图表的 _描述_（轴、趋势、关键数据点）加可能的话底层数据。两者都被索引。

2026 年基于 VLM 的表格提取准确性对简单表格高（干净文档上 >95% 单元格级准确率）对复杂表格低（有合并单元格、不规则布局、脚注行的文档上约 80%）。正确纪律：**在依赖之前在你的特定文档语料库上 eval 提取准确性**。领域特定怪癖（法律合同表格；财务报表表格）通常有在通用基准上不出现的故障模式。
