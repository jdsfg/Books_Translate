通用原则：**工具应暴露特定能力，非任意执行**。约束表面积是工程工作。

**Q5.** 第 7 章引入结构化输出作为从 LLM 获取可靠、验证数据的纪律。工具使用 _作为模式_ 如何与结构化输出关联？识别底层联系和一个重要区别。

**答案：**

底层联系：**两者都关于将 LLM 输出约束到 schema**。第 7 章中，schema 是答案的形状（LLM 填写的 Pydantic 模型）。工具使用中，schema 是工具函数的输入参数（LLM 用值填充的 JSON schema）。两种情况中，LLM 的自由形式生成被包装在应用可验证、解析和确定性行动的结构化信封中。schema 设计、验证失败重试和结构化错误的纪律从一个带到另一个。

事实上，工具使用在底层 _实现为_ 结构化输出：模型的工具调用是 `{tool_name, arguments}` 形式的结构化输出。provider 的工具使用 API 是结构化输出之上的开发者面向层，带工具有关响应如何反馈到下一轮的约定。

重要区别：**工具使用闭合循环；结构化输出不闭合**。结构化输出是一次性的：LLM 产生结构化答案，应用使用它，完成。工具使用是迭代的：LLM 产生工具调用，应用执行工具，结果反馈到 LLM，LLM 决定下一步做什么（调用另一个工具，或产生最终答案）。此循环使工具使用成为 agent（第 14 章）的基础而结构化输出仍是构建块。

含义：结构化输出的故障模式（验证失败、幻觉字段）也适用于工具使用，加上工具使用添加自己的故障模式（选错工具、无限循环、执行时策略违规）是一次性结构化输出没有的。第 7 章纪律是地板；第 13 章在其上构建。

交叉引用：§13.4 的"LLM 提议，应用处置"将第 7 章"LLM 生成，应用验证"模式泛化到迭代情况。两者都是更广泛原则的实例：将概率输出与确定性强制分离。
