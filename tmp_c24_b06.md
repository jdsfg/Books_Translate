此实现的内容：

* **第一层（上下文内防御）**：SYSTEM_PROMPT_PREAMBLE 显式训练模型将标记内容视为数据。
* **第二层（输出验证）**：`validate_output` 方法检查 schema 一致性和数据范围（无合法范围外的 PII）。
* **第三层（不可信内容标记）**：每个不可信来源在传递给模型前被包裹在结构化标签中。
* **第四层（间接注入缓解）**：基于模式的筛查；按来源信任级别；跨租户数据范围强制执行。

这是真实工程工作。完整生产版本约 300-500 行（包括安全事件日志、数据范围计算、测试套件）。成本：约 1 工程师月的专注安全工程。价值：防止 2025 年成为头条的 prompt 注入类事件。

### 23.5 置信度校准：知道 LLM 何时在胡说

按第 2 章 §2.6，LLM 自信地幻觉。模型没有校准的不确定性信号；_我确定_ 和 _我错了_ 对消费者看起来一样。

生产 AI 需要检测模型在低置信度区域运行的方法：

**模式 1：显式置信度引导**。prompt 要求模型以数值表达置信度。模型所述的置信度本身是 LLM 输出（不可靠作为校准概率）但 _与_ 实际可靠性 _相关_。作为众多信号之一有用。

    回答后，在单独一行上，说明你对答案的置信度：
    - HIGH：我确定答案基于检索证据是正确的。
    - MEDIUM：答案有支持但我注意到一些模糊性。
    - LOW：我不确定；这应由人工审查。


**模式 2：结构性置信度**。输出的结构携带置信度信息。Beacon 临床摘要器（第 7 章 §7.9）有 `confidence: float` 和 `unclear_elements: list[str]` 字段。下游代码可将低置信度输出路由到人工审查。

**模式 3：基于一致性的置信度**。多次运行同一 prompt（不同温度或不同采样）；如输出显著变化，模型不确定。如输出一致，更可靠。成本 N 倍推理但提供校准信号。

**模式 4：基于接地的置信度**（用于 RAG）。可评估输出对检索内容的忠实度。如答案做出检索不支持的声明，那是一个标志。

**"我不知道"答案作为质量信号**：良好工程的系统应 _偏好_ 优雅拒绝而非幻觉答案。说"我没有相关信息；让我为你转接人工"的客户支持系统比自信编造的系统质量更高。纪律：训练系统适当拒绝；在 eval 中奖励拒绝而非虚构。
