#### 概念检查

**Q1.** 陈述四界模式（§14.4）并解释为什么每个界独立必要。

**答案：**

1. **最大迭代**：限制轨迹长度。独立必要因为 agent 可在不触发任何其他限制的情况下螺旋（短的单次调用、低的每次调用成本，但无限多迭代）。
2. **每调用 token 上限**：限制单次推理输出。必要因为模型偶尔产生过长输出（冗长推理、生成代码）消耗上下文预算；每调用限制即使在迭代限制内也防止单次调用爆炸。
3. **总轨迹预算**：限制累积 token 使用。必要因为 agent 可在迭代限制和每调用上限内但仍累积巨大成本：15 次迭代 × 0.10 美元 = 每轨迹 1.50 美元在规模上累积。
4. **每工具断路器**：限制工具调用重复。必要因为 agent 可能在迭代计数器上看似进展但卡在相同工具调用的循环中；轨迹看起来在动但实际没有。断路器捕获 _卡住但计数_ 模式。

四界合在一起覆盖正交故障维度。任一单独留缺口。生产 agent 实现全部四界。

**Q2.** 解释 agent 记忆的三层（工作、情景、长期）。对每层，给出一个 Helios 场景说明其重要性。

**答案：**

1. **工作记忆**：当前轨迹的对话历史。_Helios 场景_：处理单个工单的 agent 有用户问题、agent 推理、工具调用和工具结果全在工作记忆中。当 agent 决定下一步调用什么工具时，它依赖此工作记忆。
2. **情景记忆**：会话内跨多次 agent 调用持久化的状态。_Helios 场景_：长支持会话中的客户可能多次与 agent 交互（agent 先帮查订单，后帮退款问题，再帮配送状态）。情景记忆捕获此会话中发生的事——已发出的退款、已做的决策——使后续调用不重复或矛盾早期调用。
3. **长期记忆**：跨会话的状态。_Helios 场景_：有数月历史的客户。当他们今天与 agent 交互时，长期记忆提供其典型问题、偏好（例如总是配送到特定地址）、过去投诉模式。agent 个性化其响应。

三层服务不同需求并有不同实现（工作记忆的上下文内；情景的结构化+摘要；长期的向量+结构化）。生产 agent 通常有全部三层；最小 agent 仅有工作记忆。

**Q3.** 为什么本章论证有界设计 _不_ 降低 agent 能力？回应"更多界 = 更低能力 agent"的常见直觉。

**答案：** 直觉是界限制 agent 可尝试什么；因此有界 agent 更低能力。本章论证界不降低 _有用_ 任务的能力；它们防止 agent 尝试 _无用或危险_ 的任务。
