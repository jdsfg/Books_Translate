#### 模式 4：视觉优先文档摄取

**解决什么问题。** PDF 不是文本。文本提取库（pdfminer、PyMuPDF）产生无布局、无表格、无图表且不知双栏期刊文章应逐栏阅读的字符流。结果是在垃圾上做 RAG。视觉优先摄取将每页视为图像并用 VLM 直接提取结构化内容。

**拓扑。** PDF 落入对象存储（S3、GCS）带作业标签。一个 worker 拉取作业，将 PDF 拆分为 200 DPI 的 PNG 或 JPEG 渲染（PyMuPDF 或 pdf2image），并按页向队列发出一张图像。一组 VLM worker（Claude vision、GPT-4-class vision、Gemini Pro Vision 或自托管 Qwen-VL）消费队列。每页，worker 用结构化提取 prompt 调用 VLM：阅读顺序中的叙事文本、JSON 格式表格、带标题的图表、带层级级别的章节标题，以及页面元数据（页码、文档 ID、布局类别）。输出是每页一个 JSON 文档，对照 Pydantic schema 验证。规范化阶段将页面连接为单个文档树（章节连续性、跨页表格、脚注解析）。规范化文档按语义分块（在章节边界，从不拆分表格，将图表标题附于图表），块如模式 3 那样索引用于检索——带更丰富元数据（章节路径、页码、表格或散文标记）供重排序和 prompt 构建阶段利用。

**何时用。** 源语料是非平凡布局的 PDF——学术论文、监管文件、医疗记录、合同、带表格的财务报表。Strata Research 的法律技术平台是经典：判例法通过朴素文本提取不可用但 VLM 管道产出干净、可引用的结构。

**何时不用。** 文档已是 HTML、Markdown 或其他结构化格式。或足够简单使 pdfminer 加清理达到 95%。VLM 摄取每页比文本提取贵 10-50 倍；量很重要。

**交叉引用。** 第 12 章（多模态摄取）。
