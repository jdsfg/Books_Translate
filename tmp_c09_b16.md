**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_设计多轮 agent 上下文溢出的压力测试 eval：输入集、测量、阈值、CI 集成。_ 你验证 (a) 合成对话锻炼现实模式（不只是简单来回；包含工具调用、检索、变化的消息长度），以及 (b) 阈值值对你的延迟预算现实（不要接受 LLM 的通用 4 秒数字而不对照你产品的实际预算检查）。

**Q5.** _为长时间运行的编码 agent 设计轨迹内摘要_。Agent 解决多步工程任务：读代码、改代码、运行测试、迭代。单个任务可能需要 30 到 60 次工具调用并在完成前将轨迹增长到 80K+ token。设计轨迹内摘要策略：何时摘要、什么逐字保留、什么摘要、以及如何保留足够状态使 agent 能连贯继续。

提示

这是 §8.4 手段 1 应用于 agent 轨迹而非对话。Agent 的状态不同于聊天历史；保留的状态必须支持继续行动，而非仅继续对话。

**答案：**

**何时摘要。** 两个触发器，一起使用：

* _Token 阈值_：当轨迹超过 40K token 时，摘要运行。40K 以下不摘要；agent 从完整上下文中受益。
* _阶段边界_：在逻辑子任务完成时（例如 agent 写完测试即将开始运行它们），摘要刚完成的阶段。这产生比阶段中间切割更干净的摘要。

组合规则：在 40K 阈值跨越后的 _下一个_ 阶段边界摘要。

**什么逐字保留。**

* _当前目标_：原始任务陈述，加用户的任何细化。
* _最近 5 到 8 次工具调用_ 及其完整输出：agent 的近期上下文驱动下一步行动；近因性重要。
* _任何未解决的错误_：开放失败是 agent 必须处理的状态；不要将它们摘要掉。
* _固定上下文_：agent 当前编辑的文件、当前工作假设。这些通常需要逐字引用以继续。

**什么摘要。**

* _较早工具调用_（超出近因窗口）：读了哪些文件、发现了什么、尝试了什么变更及其结果。
* _已完成子任务_：每个的一段摘要（做了什么、代码库中现在有什么、验证了什么）。
* _放弃的方法_：agent 应记住 _尝试并拒绝了的什么_ 使它不重试；在摘要中作为 _尝试：X。拒绝因为：Y._ 行包含。

**摘要格式。** 结构化而非散文；agent 对结构化状态工作更好。大致：

    已完成子任务：
    - [任务]：[结果]，验证通过 [检查]
    - [任务]：[结果]，验证通过 [检查]

    至今修改的文件：
    - path/to/file.py：[变更的一行摘要]
    - path/to/other.py：[变更的一行摘要]

    尝试并拒绝的：
    - [方法]：拒绝因为 [原因]

    待解决问题：
    - [问题]：[描述]

    当前假设：
    [agent 当前对问题的理解的简短陈述]
