* 适用于：有平台工程能力的 30 工程师创业公司，无即时内置 prompt 管理需求，偏好 OSS + 自托管，对 SaaS 定价成本敏感。
* 原因：OSS 不增加 SaaS 成本；自托管保持数据内部；配置驱动方法对熟悉 YAML 的工程师直接。

**Portkey**（SaaS）：

* 适用于：无平台工程能力的 5 工程师创业公司，想要集成 prompt 管理和可观测性而不自己构建，愿意为生产力支付 SaaS 定价。
* 原因：团队没有工程师来自托管好 LiteLLM；集成套件（路由器 + 可观测性 + prompt）比组装 OSS 部件更高效；SaaS 成本相对于节省的工程成本小。

通用模式：**有平台工程处用 OSS；没有处用 SaaS**。相同的路由器 vs SaaS 决策贯穿栈逐层适用（第 16 章）；团队结构比层技术性更决定答案。

**Q4.** 什么是 _未经测试的回退_ 故障模式？为什么特别重要的是对回退模型有 eval 覆盖？

**答案：** 故障模式：路由器有回退配置；当主模型在生产中首次失败时，回退技术上成功（API 调用返回响应）但响应质量低于主模型本应产生的。团队不会立即注意到因为回退"工作了"；但生产用户在故障转移窗口期间得到了更差的答案。

回退模型的 eval 覆盖重要因为：

1. **Prompt 常为模型调优**。在 GPT-4o 上产生好输出的 prompt 可能在 Claude Haiku 上产生不同输出；回退的质量不是主模型质量减去已知 delta。
2. **团队在正常运营中看不到回退行为**。没有专门 eval 测试，回退仅在实际事故中演练（正是发现质量问题的错误时机）。
3. **事故期间质量下降叠加事故**。主模型失败（问题 1）；现在回退产生更差质量（问题 2）；客户体验双重糟糕。经 eval 测试的回退将损害限制在问题 1。

缓解：对回退链中每个模型运行 eval。文档化每个模型分数。周期性混沌测试通过故意经回退路由。在生产 eval 采样中浮现质量下降（第 21 章）。

**Q5.** 第 16 章论证路由器层是最普遍"购买"的层之一。但本章主张任何支出超过 50K 美元/月的团队应有内部路由基础设施。调和这些立场。2026 年路由器层的"购买"意味着什么？

**答案：** 表面矛盾在精确阅读"购买"时消解。第 16 章的论证是团队不应 _从头构建路由器引擎_：底层技术（模型抽象、重试逻辑、回退分发、回调钩子）已商品化且被 LiteLLM、Portkey、Cloudflare AI Gateway、Kong AI Gateway 等工具（§17.3 列举的四个入选者）良好服务。在内部构建此引擎是 4-8 工程师月的工作且不返回产品差异化。
