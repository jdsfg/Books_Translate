#### 概念检查

**Q1.** 你公司的一位资深工程师说："我们不是在做 AI 工程。我们只是在后端路由中调用 OpenAI API。这就是普通的后端工程。"你会如何回应？指出 _普通后端工程_ 默认不具备、但 AI 工程要求的三项工程纪律。

提示

想想当你的后端调用的组件每次返回不同的答案、每次调用都花钱、而且可以被用户输入诱导产生对抗性行为时，有什么是不同的。

**答案：** 普通后端工程默认不具备的三项纪律：

1. **eval 驱动开发**。传统后端路由要么返回正确的值，要么返回错误。除了单元测试没有什么需要评估的。LLM 驱动的路由可以返回语法正确但语义错误的答案，而知道这种情况发生的唯一方法是维护一个 golden dataset，将路由的输出与之评分。这是一门完整的学科；第 4 章涵盖它。
2. **请求级成本工程**。传统后端路由的每次调用边际成本主要由你的基础设施（CPU 周期、数据库 I/O、网络带宽）决定，且可预测地有界。LLM 驱动的路由的边际成本以 token 计量（也就是输入和输出的字符数），请求之间可以变化 100 倍。成本仪表盘、按租户预算和熔断器（第 17 章、第 18 章）是一等工程关注点，不是事后想法。
3. **prompt 层的对抗性输入防御**。后端路由有经典攻击面（SQL 注入、XSS、认证绕过）。LLM 驱动的路由有一个全新的攻击面叫做 prompt 注入，其中输入中的不可信内容（检索到的文档、用户消息、工具输出）劫持模型的行为。防御层与经典安全工具提供的不同；第 23 章涵盖它。

所以这位资深工程师是错的，不是因为他们轻视，而是因为上面列出的三项纪律是不可选的。在没有这些纪律的情况下从后端路由调用 OpenAI API 的团队将会花比预期更多的钱，交付悄悄退化的功能，最终遭遇安全事件。这位资深工程师的框架是该领域最常见的入门级误解。

**Q2.** 从以下方面区分 _AI 工程师（产品侧）_ 和 _ML 平台工程师_：(a) 他们交付什么；(b) 他们的值班是什么样的；(c) 两个角色中哪个更需要在日常工作中读研究论文。

**答案：**
