#### 通过 prompt 设计的成本和延迟

你 prompt 中的每个 token 都花钱。模型发出的每个 token 花更多钱（通常每 token 4–5 倍）。减少 token 是工程师可用的最直接成本杠杆，不需要切换模型、切换 provider 或重新架构。

实践模式：

* **截断系统 prompt。** 许多生产系统 prompt 超过 1500 token 因为它们有机增长。坐下来压缩的高级工程师通常可以在不改变模型行为的情况下削减 30–40%。纪律：针对模型需要被告知的清晰列表编写系统 prompt，然后删除其他一切。
* **将稳定内容移至缓存 prompt。** 如果你的系统 prompt 和工具定义稳定，Anthropic 的 prompt 缓存（第 18 章）将读取成本从 3 美元/1M 降至 0.30 美元/1M。成本节省在该部分是 90%。资格测试：内容跨调用是否相同？如果是，缓存。
* **用 `max_tokens` 和停止序列约束输出长度。** 限制在正确长度的响应在每次调用上节省输出成本。5 倍输出乘数意味着小的输出节省主导小的输入节省。
* **为 token 效率构建结构。** 紧凑列表格式通常比包含相同信息的散文段落产生更短输出。_三个问题：(1) X，(2) Y，(3) Z。_ 比说同样事情的段落更短。

#### 约束输出结构

模型的默认输出是自由格式散文。如果你的下游代码需要 JSON，你必须要求 JSON；如果你必须要求 JSON，你必须设计 prompt 使模型可靠地产生可解析的 JSON。即使 1% 的结构化输出失败率在规模化时也转化为真实的生产问题。

现代最佳实践是使用 provider 的原生结构化输出模式（Anthropic 和 OpenAI 在 2026 年都暴露了这个），它在底层使用约束解码来 _强制_ 模型发出符合 schema 的有效 JSON。prompt 然后变成 _什么_ 字段应包含的描述，而 JSON 形态的 _那个_ 被机械地强制。第 7 章构建这个。

prompt 仍然重要的地方：schema 判别器（联合类型使用哪个变体）、字段语义（_严重性_ 在代码审查上下文中是什么意思？）和条件输出（何时省略可选字段）。这些是模型仍然从 prompt 上下文中做出的内容决策。

#### 引出工具使用

工具使用（第 13 章）是模型调用开发者定义的函数。模型对 _何时调用工具、哪个工具、带什么参数_ 的选择由 prompt 和工具 schema 控制。

两个 prompt 模式最重要：

* **工具定义作为文档。** 工具的名称、描述和参数文档字符串被模型读取。不够具体的工具 schema 产生混乱的模型行为：选错工具、错误参数、不需要时调用了工具。纪律：像为从未见过代码库的初级工程师写文档一样写工具文档。
* **系统 prompt 中的工具策略指导。** 即使有好的工具 schema，模型也受益于 prompt 级指导：_事实性问题优先使用数据库查询工具；仅当问题需要 2024 年后的信息时才使用网络搜索工具；没有明确的用户确认消息时绝不调用删除工具。_ 这些是放入系统 prompt 的策略决策。
