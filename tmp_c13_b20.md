#### 深入思考

**Q1.** _经典 OCR 的终结_。本章论证视觉优先摄取在 2026 年已在通用文档理解中大致取代经典 OCR。识别两个 2030 年经典 OCR 仍是正确选择的场景，并解释为什么基于 VLM 的摄取不会取代它们。

**讨论：** 经典 OCR 持续的两个场景：

1. **极大量、稳定格式文档在极端成本敏感下**。每天百万收据的打印收据处理；布局已知且标准化的结构化发票处理。在此量下，经典 OCR 的每页成本（一旦 Tesseract 在你自己的硬件上运行基本免费）胜过 VLM API 成本。格式稳定性让自定义解析器在 OCR 输出上可靠运行。到 2030 年，VLM API 成本可能降到此场景缩小，但极大量专用案例持续。
2. **实时/亚 100ms 延迟要求**。VLM 处理延迟在秒级（视觉输入编码和处理需要时间）。对于实时用例（用户期望即时反馈的实时摄像头文档扫描；移动应用中亚秒预算的支票存款扫描），经典 OCR 的更快执行胜出。即使到 2030 年，在设备或边缘硬件上运行的延迟优势将在某些专用案例中持续。

第三个值得注意的场景：**模型必须可审计的受监管环境**。某些受监管行业（某些政府、某些国防、某些金融）要求文档处理模型在算法级可检查和可审计。VLM 内部不易审计；经典 OCR 可以。到 2030 年，这是缩小但真实的专用。

大多数通用文档理解转向基于 VLM；长尾专用案例（高量 + 低成本；实时；可审计）仍是经典 OCR 领地。

**Q2.** _多模态 RAG 作为地平线_。到 2030 年，多模态模型将启用哪些纯文本 RAG 无法做到的新 RAG 能力？智能推测两个新应用类别。

**讨论：** 随强多模态 RAG 涌现的两个应用类别：

1. **生产规模的视觉相似性驱动搜索**。今天（2026 年），CLIP 家族嵌入充分处理图像相似性搜索。到 2030 年，随更强多模态嵌入和更快检索基础设施，"找看起来像此照片的产品"或"找我们存档中有类似视觉结构的页面"成为流畅的生产能力。应用：电商视觉搜索；医学影像存档搜索；设计资产检索。今天这些是专用；2030 年它们是主流。
2. **实时上下文多模态 QA**。配备摄像头的设备（智能手机、AR 眼镜、机器人系统）看到真实世界上下文并回答基于它的问题。检索从相关内容的知识库拉取；多模态模型将视觉上下文与检索内容组合。应用：AR 辅助维修（指向设备，检索相关手册章节，获得引导维修）；无障碍（通过实时场景理解的视障辅助）；工业检查（指向机械，检索已知故障模式，诊断）。2026 年原型存在；2030 年生产部署将广泛。
