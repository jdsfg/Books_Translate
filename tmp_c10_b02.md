#### 动机 3：幻觉

按第 2 章 §2.6，幻觉来自模型在高不确定性下承诺下一 token 预测。如果模型必须回答一个在预训练中稀疏表示的事实问题，模型插值，插值可能错误同时听起来自信。

RAG 通过 **将相关事实放入上下文** 来解决此问题，使模型不再插值；它在阅读。当答案就在输入中时下一 token 分布更锐利；幻觉概率下降。

这是最深的动机。RAG 不仅是 bolted 到 LLM 上的搜索引擎；它是将 _不确定下猜测_ 转变为 _从可验证来源阅读_ 的架构模式。模型答对因为答案在它面前。

### 9.2 RAG 循环，七步

标准 RAG 循环简短且众所周知。详述如下：

1. **用户查询**到达。
2. **嵌入查询**为向量表示。
3. **向量搜索**对索引语料库返回 top-K 候选块。
4. **重排序** top-K 块使用更强的相关性模型；保留 top-N。（朴素 RAG 中可选；生产中实际强制——见 §9.3 杠杆 4 和 §11.1。）
5. **构建 prompt** 带检索到的块加用户查询。
6. **LLM 生成**答案，基于检索上下文。
7. **可选后处理**：引用提取、schema 验证、格式化。



    def rag_query(user_question: str) -> str:
        query_vec = embed(user_question)
        candidates = vector_db.search(query_vec, top_k=20)
        top_chunks = rerank(user_question, candidates, top_n=5)
        prompt = build_prompt(user_question, top_chunks)
        answer = llm.complete(prompt)
        return answer


这就是 RAG 的核心。每个生产 RAG 系统都是此循环的精炼。复杂性来自每步有工程深度：如何为索引分块、用哪个嵌入模型、部署什么向量数据库、如何重排序、如何构建 prompt、如何评估整个管道。

#### 定义：检索增强生成（RAG）

一种模式，其中生成式 LLM 被给予来自外部语料库的相关检索内容作为其输入上下文的一部分，允许模型将答案基于检索信息而非仅依赖训练编码的知识。

用通俗语言：模型不从记忆回答，模型从你递给它的文档回答。记忆可能出错；文档（如果经过策展和良好检索）不会。

### 9.3 四个杠杆：分块、嵌入、检索、排序

每个生产 RAG 系统调谐四个杠杆。每个是子学科。
