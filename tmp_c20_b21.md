#### 深入思考

**Q1.** _可观测性作为基础_。论证可观测性是生产 AI 工程中 _最重要的_ 非功能关注。用两个类比支持论证。

**讨论：** 两个类比：

1. **数据库性能**：数据库工程师知道查询计划分析（EXPLAIN）和慢查询日志是基础。没有它们，数据库在运维上不透明；生产问题无法解决；性能优化是猜测。LLM 系统没有可观测性同样如此。

2. **分布式系统**：分布式追踪（Jaeger、Zipkin、OpenTelemetry）是调试分布式服务的前提。没有追踪，服务 B 中由服务 A 引起的问题不可见。LLM 系统在时间上分布（跨多次调用的轨迹）且在结构上分布（LLM + 工具）；相同的追踪纪律适用。

两个类比共享结构性洞察：**系统的行为是数据；没有数据，你没有工程**。后端工程师在 2010 年代通过分布式追踪学到此点；LLM 工程师在 2024-2026 年为 LLM 系统学习它。

可观测性是生产 AI 工程中 _最重要的_ 非功能关注的论证：其他每个纪律（调试、监控、成本、eval、安全、质量）依赖有数据来操作。没有可观测性数据，纪律是理论的；有它，它们是可操作的。对可观测性的投资是使能其他每个投资的投资。

一个实践含义：**在扩展前尽早投资可观测性**。在构建功能后附加可观测性的团队发现运维缺口持续数月。从第一天将可观测性构建入架构的团队在需要时有数据。

**Q2.** _可观测性的未来_。到 2030 年，LLM 可观测性可能需要捕获哪些 2026 年没有的新维度？推测两个新维度及其所需工程工作。

**讨论：** 两个维度：

1. **多 agent 轨迹可观测性**。到 2030 年，多 agent 系统更常见；追踪结构需捕获跨 agent 通信。超越 2026 年捕获的（每 agent 追踪），2030 年需要：谁委托给谁；agent 间交换了什么消息；跨 agent 协调如何影响结果。工程工作：将追踪 schema 扩展为一等公民的多 agent 拓扑；可视化跨 agent 流；调试协调失败。
2. **作为每条追踪一部分的持续质量评估**。到 2030 年，生产中的 eval 可能足够便宜使每条追踪都被 eval（不仅是采样）。这需要：更便宜/更快的 LLM 作为评判；可观测性与 eval 平台更紧密耦合；持续质量监控的新仪表板。工程工作：将生产 eval 集成到追踪流水线；每调用级的质量归因；识别哪个模型/prompt/工具变更退化特定追踪类。
