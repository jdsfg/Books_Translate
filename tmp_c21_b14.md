#### 应用题

**Q1.** _选择语音架构_。为每个场景推荐架构（端到端 / STT+LLM+TTS / 混合）：

(a) 处理高量呼入的客户服务电话 agent (b) 会议后 AI 摘要的会议转录服务 (c) 盲人通过语音导航网站的辅助工具 (d) 面向开发者的语音驱动编程助手

**答案：**

(a) **端到端（OpenAI Realtime 或 Gemini Live）带文本 LLM 回退**。客户服务电话有亚 500ms 延迟要求；端到端是唯一路径。Helios 的确切模式。
(b) **STT + 文本 LLM + TTS（如需语音输出）**。转录是异步的；延迟不关键。用最好的 STT（Deepgram）和最好的文本 LLM（Sonnet 4 做摘要）；语音输出可选，需要时可用 TTS。
(c) **导航用端到端语音；朗读用文本 + TTS**。导航需快速轮次交替（点击链接、返回等）；端到端提供延迟。朗读内容（如用户导航到的文章）可用文本 LLM 驱动的 TTS，因为用户在听，不在交互。
(d) **混合**。编程助手接收语音输入（端到端做低延迟监听），通过文本 LLM 处理（代码生成受益于完整文本 AI 能力包括工具使用），可能返回也可能不返回语音输出（开发者通常更愿在屏幕上看代码而非听它）。架构基于每个任务的模态需求混合。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为每个语音场景推荐架构并论证。_ 你验证 (a) 延迟分析匹配用例（实时对话需端到端；异步转录不需要），以及 (b) LLM 不对一切默认端到端；一些场景确实需要 STT-LLM-TTS 流水线灵活性。

**Q2.** _设计 EOT 调优_。Helios 的语音 agent 服务多样化来电者。设计 EOT 调优策略。指定：(a) 基础阈值；(b) 按条件调整；(c) 如何测量质量；(d) 如何随时间演化。

**答案：**

(a) **基础阈值**：350ms 静音。基于行业数据和 Helios 试点数据，这是客户服务式对话的合理起点。

(b) **按条件调整**：

* _通话质量_：如检测到抖动、丢包或背景噪声，增加到 450-500ms（避免音频掉线导致的错误 EOT）。
* _对话状态_：如用户刚问了复杂问题（模型分类为需要思考），用 400ms（用户可能停顿思考）。如用户在回答简单确认问题，用 300ms（不需太多思考）。
* _客户历史_：一些客户持续以更长停顿说话（老年客户；非母语者）；系统可基于客户先前通话按客户适配。调整后阈值通常 +50-100ms。

(c) **质量测量**：
