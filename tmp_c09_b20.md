组织含义：**在规模上，上下文工程需要平台团队**。平台团队拥有共享基础设施（第 16 章的参考架构明确有此层）；产品团队拥有功能特定的上下文形态；界限类似经典软件中的 _应用工程师 vs 平台工程师_。

对本书大多数读者，上下文即平台纪律是 2027–2028 年地平线。现在要带走的问题是：你团队的上下文工程工作何时变成平台问题而非按功能问题？当它变成时，相应组织。

**Q3.** _RAG 和长上下文会收敛为单一架构吗？_ 本章和第 9 章将 RAG 和长上下文视为按层选择的替代方案。推测：到 2030 年，两种架构会保持独立，还是会融合为统一的检索加上下文架构使边界消失？论证双方。指出融合成为现实必须发生的技术创新。

**讨论：** **独立架构论证。** RAG 的价值来自检索是显式的、可审计的、与模型解耦的：团队选择嵌入模型、调分块器、拥有索引。长上下文的价值来自简单性：模型看到你放入的，无中间检索逻辑。这些是不同的价值主张；团队将继续在它们之间做显式选择。决策树变得更锐利但不消解。

**融合论证。** 如果模型发展 _原生_ 检索能力，模型自己决定从知识存储中获取什么并集成到生成步骤中，RAG 和长上下文的区别变得模糊。模型在推理时有效进行上下文内检索；应用程序不在架构间选择因为模型处理它。2026 年的一些早期迹象：Anthropic 的 Web 搜索工具、OpenAI 的文件搜索工具、Google 的接地搜索响应都是此模式的早期版本。到 2030 年，模式可能更成熟，_模型_ 可能拥有检索纪律而非应用程序。

**融合所需的技术创新。** 两个部分：

1. **模型原生检索用户提供的索引。** 模型被授予对嵌入索引知识存储的访问并自己决定何时检索。应用程序提供存储和访问；模型拥有 _何时_ 和 _什么_。这要求模型在比当今工具使用训练更高保真度上训练检索即工具模式。
2. **连续上下文推理**，模型可以在生成期间摄取上下文 token 流而非预先。这将让模型开始生成、遇到无法回答的问题、检索、继续，全在一次推理调用中。此基础设施存在（流式输入、KV 缓存扩展）但模型行为尚未干净支持它。
