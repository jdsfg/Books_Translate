### 章节小结

RAG 解决三个问题：知识截止、你的数据、不确定下的幻觉。架构是七步循环：嵌入查询 → 向量搜索 → 可选重排序 → 构建 prompt → LLM 生成 → 后处理。四个杠杆（分块、嵌入、检索、排序）各有工程深度，接下来三章展开。

朴素 RAG 演示好但不发布。朴素 RAG 质量（50–65% 通过率）和生产质量 RAG（80%+）之间的差距是跨所有四个杠杆的一年纪律工程，每个变更由 eval 验证。Strata Research 的 V1→V4 路径是典型弧线。

RAG 评估有两层：检索评估（recall@K、MRR、NDCG，需要标注相关性）和回答评估（忠实度、回答相关性、引用接地，需要回答量表）。两层都需要；任一单独都遗漏故障模式。

Google AI Overviews 故障（Thread B 故障 2）是典型 RAG 故障故事：低信任检索来源产生了自信的错误答案。教训——_检索来源质量比 LLM 质量更重要_——是生产 RAG 工程的核心。缓解措施（来源溯源、允许列表、检索审计）现在是标准的。

动机追溯到幻觉（第 2 章）；方法论是第 4 章的 eval 纪律应用于两层；prompt 级接地要求来自第 6 章；上下文窗口框架来自第 8 章。第 10 章深入工程化四个杠杆；第 11 章覆盖高级模式和多轴 eval；第 12 章扩展到多模态。

本章一句话论证：**RAG 不是 bolted 到 LLM 上的搜索——它是将模型基于可检索事实的架构模式。** 检索质量是主导杠杆；四个杠杆都需要纪律；评估是严谨的且迭代是耐心的。内化此点的团队发布；寻找捷径的团队以艰难方式重访。

### 9.X 练习题
