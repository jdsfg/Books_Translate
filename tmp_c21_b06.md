关键特性：

1. **双向流**：用户音频流入；agent 音频流出。无批处理；延迟最小化。
2. **服务端 VAD（语音活动检测）**：轮次结束检测由 Realtime 模型在服务端完成。阈值和静音持续时间可按用例调优。
3. **打断处理**：当用户开始说话时，agent 停止发送音频（`interrupted` 标志）并取消任何进行中的响应。模型被通知并可适当响应用户打断。
4. **工具集成**：模型可在对话中调用工具。工具调用不中断音频流；它们异步发生且结果反馈到对话中。
5. **回退到文本 LLM**：如 Realtime API 出错（提供商中断、速率限制等），agent 回退到文本 LLM 调用 + TTS 合成。对话继续；用户只感知到小延迟停顿。

此循环是生产级的。它处理语音连接的故障模式（打断、模型错误、瞬态网络问题）同时维持亚 500ms 目标延迟。

### 20.7 流式、并行和语音架构

为满足延迟预算，语音架构将流式推得比文本更远：

**流式 TTS**：语音合成在第一个文本 token 上开始，而非等完整响应准备好。用户在 TTFT 预算内听到第一个词，即使响应继续生成。

**并行工具执行**：如 agent 决定调用工具，工具可在 agent 继续说话时执行（"让我为您查一下……"同时 lookup_order 工具并行运行）。音频填充是用户感知为低延迟的东西；实际工作被并行化。

**预生成常见响应**：对高度可预测的响应前缀（"我需要验证您的身份……"），音频可预生成并缓存。按需回放；节省 TTS 延迟。

**推测响应**：模型在仍听用户时生成候选响应；如用户说完预期的内容，响应已部分合成。

这些模式是语音独有的延迟纪律。学习语音的文本 AI 工程师需内化：**流式不是可选的；并行是默认的；预生成是真实杠杆**。
