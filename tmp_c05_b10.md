#### 场景 3：挑战 golden set 的 agent 轨迹

你在构建一个解决多步任务的 agent：预订航班、调试代码库、规划行程。agent 的行为是一系列工具调用。同一个问题可以有许多正确的解决路径。带参考 _轨迹_（特定工具调用序列）的 golden set 会因为 agent 以不同于参考的方式解决问题而惩罚 agent，即使 agent 的解决方案是正确的。

这里正确的 eval 是形态 5（端到端任务成功）：_agent 最终是否预订了有效航班？_ 而非 _agent 的工具调用序列是否匹配参考？_ 但形态 5 的 eval 构建昂贵（需要可工作的 agent 环境）且运行慢（一个 agent 每个任务可能需要 30 秒到几分钟）。很多团队在这个时候放弃并在没有严格 eval 的情况下发布。

替代 eval 的东西：_结构属性的部分学分 eval_（agent 最终是否使用了预订工具？agent 在购买前是否与用户确认？agent 的总支出是否保持在成本断路器内？）；加上 _生产遥测_ 关于真实用户（成功率、升级率、每任务成本）；加上 _对抗性抽查_ 在已知困难的轨迹上。组合弱于 golden set eval 但强于无测量。第 14 章和第 21 章将回到这个话题。

#### 场景 4：非常低风险的任务，错误输出的代价可恢复且很小

你在构建一个建议在聊天消息中添加 emoji 的功能。模型选几个 emoji。用户接受或拒绝。错误建议的代价是 _用户耸耸肩不接受_。没有合规风险、没有品牌风险、没有金钱风险。

在这里构建 eval 套件是过度杀。你需要的信号（用户是否接受建议？）可以从生产参与指标中廉价获得。回归的代价是可恢复的（下一次 prompt 迭代捕获它）。eval 会是没人行动的基础设施。

替代 eval 的东西：_在真实用户中进行 A/B 测试_，以参与指标作为度量。纪律从 _发布前 eval 作为门禁_ 转移到 _扩展前 A/B 作为门禁_。

#### 当收益递减节是错误框架时

一个警告。上述四个场景都是真实和有界的。它们适用的频率也是有界的。大多数生产 AI 功能不是创意的、不是纯原型、不是以挑战 golden set 的方式 agentic、不是低风险的。_大多数生产 AI 功能应该有 eval。_ 收益递减节的存在是为了防止 eval 纪律滑入教条，而非为不想做工作的团队提供逃生舱。当一个团队对一个中等风险、有可识别成功标准、即将发布给客户的功能援引收益递减框架时，反驳。默认是 _写 eval_；例外是真实的但比看起来更小。
