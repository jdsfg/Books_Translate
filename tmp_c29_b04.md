**上下文窗口**：模型一次调用中可关注的最大 token 数。引用的限制是最佳情况；因中间丢失效应有效限制更小（第 2 章，§2.4）。

**同族偏差**：LLM judge 偏好来自同族（同供应商或训练血统）模型的输出。通过 judge 轮换缓解（第 5 章，§5.6）。

**成本断路器**：每请求或每会话支出的硬上限，超过时中止执行。agent 必需（第 14 章，§14.4）。

**交叉编码器**：联合编码查询和候选文档以产生相关性分数的模型。比双编码器更准确但对首轮检索太慢；用作重排序器（第 11 章，§11.1）。

**每任务成本**：完成一个用户可见工作单元（一个答案、一个工单、一份文档）的全负载 LLM 支出。产品决策的正确成本指标（第 18 章，§18.1）。

**思维链**：见 _CoT_（第 6 章，§6.2）。

**Claude**：Anthropic 的模型家族。本书中 prompt 缓存、MCP 和长上下文用例的参考平台（第 3 章）。

**连续批处理**：推理服务器在 token 级别交错请求的技术，大幅提升 GPU 利用率。vLLM 和 TGI 优于朴素服务的原因（第 22 章，§22.5）。
