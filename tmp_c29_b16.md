**检索管道**：从查询到排名上下文的完整链：解析、重写、检索、重排序和填入 prompt（第 11 章）。


### S

**采样**：从模型概率分布中选择下一个 token 而非总是选最可能的。由温度、top-p、top-k 控制（第 2 章，§2.5）。

**沙箱**：用于运行不可信代码（LLM 生成或用户提供）的隔离执行环境。任何运行代码的工具所需（第 13 章，§13.5）。

**草稿本**：agent 在任务期间写入的短期工作记忆。通常是模型自身的可见推理或专用草稿缓冲区（第 14 章，§14.5）。

**自我精炼**：prompt 模式，模型产生答案、批评它并修改。反思的特定实例（第 6 章，§6.2）。

**语义缓存**：按嵌入相似度而非精确前缀匹配的缓存查找。更高命中率，有返回错误答案的风险；谨慎部署（第 18 章，§18.4）。

**语义分块**：按语义边界（主题转换）而非固定 token 数拆分文档。改善检索连贯性（第 9 章，§9.3）。

**静默重路由**：提供商在无通知下服务不同于请求别名的模型。使用带日期版本的理由（第 3 章，§3.11）。

**快照**：模型的不变更的特定带日期构建。生产中固定到快照（第 3 章，§3.1）。

**Span**：追踪内的定时段，代表一个操作（模型调用、检索、工具执行）。结构化可观测性的单位（第 19 章，§19.2）。

**停止序列**：当生成时停止生成的字符串。用于强制输出边界（第 3 章，§3.3）。

**STT**：语音转文本。经典语音管道的第一阶段。此处延迟直接影响首 token 时间（第 20 章，§20.3）。

**流式**：在生成时增量接收 token。UX 延迟所需；使重试和解析复杂化（第 3 章，§3.2）。

**结构化输出**：符合声明 schema（JSON、函数调用等）的输出。LLM 与下游代码之间的契约（第 7 章，§7.1）。

**系统 prompt**：每次对话开始时发送的高优先级指令消息。设定角色、规则和工具。积极缓存（第 3 章，§3.1）。

**Schema 验证**：验证 LLM 产生的对象是否符合声明 schema。在代码中完成，非在 prompt 中（第 7 章，§7.3）。

**探针**：见 _可行性探针_（第 24 章，§24.6）。

**副作用**：工具产生的改变外部状态的操作（文件写入、支付、邮件）。副作用必须由验证保护且常需人在回路（第 13 章，§13.6）。

**子问题**：分解的用户查询的一部分，在综合前独立检索或回答。多跳 RAG 背后的机制（第 11 章，§11.5）。
