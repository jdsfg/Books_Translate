### 11.5 Strata Research：V1→V4 质量旅程（43% → 81% 通过率）

Strata 跨四个杠杆的完整进展，详细走过。以下所有分数以 0–1 标度报告以便比较：通过率和检索 recall 自然 0–1；回答轴分数（忠实性、回答相关性、引用接地）是 §11.4 的原始 0–3 LLM-judge 分数除以 3 归一化。

**V1（基线）**：纯向量搜索；无重排序器；通用嵌入；朴素 prompt。

* 检索 recall@10：0.62
* 回答相关性：0.58
* 总体通过率：0.43

**V2（加重排序器 + 领域嵌入）**：在 top-20 → top-5 上引入 Cohere Rerank；切换到 Voyage `voyage-law-2` 嵌入。

* 检索 recall@10：0.74（+12）
* 回答相关性：0.72（+14）
* 总体通过率：0.62（+19）

**V3（加混合搜索 + 查询重写）**：混合搜索（向量 + BM25）；改述查询用 HyDE；元数据约束查询用自查询检索器。

* 检索 recall@10：0.82（+8）
* 回答相关性：0.79（+7）
* 总体通过率：0.74（+12）

**V4（高级 prompt + 引用强制）**：prompt 级引用要求（第 6 章）；基于 chunk-id 的引用后处理；prompt 中显式管辖权和时间有效性纪律。

* 检索 recall@10：0.85（+3）
* 回答相关性：0.85（+6）
* 引用接地：0.92（比早期版本 +25）
* 总体通过率：0.81（+7）

旅程花了 12 个月。最大单次提升是 V1→V2 的重排序器 + 领域嵌入组合步骤（通过率 +19 分来自同时引入 Cohere Rerank 和 Voyage `voyage-law-2`；对照研究会拆分功劳，但 Strata 发布了这对）。累计工程工作量：约 6 工程师月聚焦 RAG 工作，加持续 eval 和基础设施。客户价值：在受监管领域可辩护发布的企业法律技术。

从 Strata 经验的 2026 年要点：**每个杠杆都有贡献；增益复合；顺序不如应用每个杠杆并测量的纪律重要**。没有魔法举措；有的是跨技术栈的一致工程。
