按照 Helios 每月 1000 万工单、平均每个工单 4 次模型调用（一次理解、一次检索增强响应、一次工具调用、一次后续），即每月 4000 万次调用——按 0.017 美元的单位成本约 **68 万美元/月**，或按 0.02 美元取整单位成本约 **80 万美元/月**。两个下限估计都接近 Helios 报告的 100 万美元以上月推理支出。与报告数字的差距由偶尔的更长响应、超过四次调用平均的多调用工单流程，以及偶尔为 Sonnet 质量不够的敏感升级路由到更贵的前沿模型（Opus 级别）来覆盖。

在这个分解中要注意两件事：

1. **输出是每次调用的主导成本线**，即使它是较小的 token 计数之一。5 倍的输出 vs 输入定价乘数意味着 600 token 的输出比 2000 token 的输入更贵。这就是为什么产生更短、更结构化输出的 prompt 设计（第 7 章）是一个真正的成本杠杆，而不仅仅是质量问题。
2. **prompt 缓存在适用时是一个大杠杆**。system prompt 和工具定义都被缓存，每次调用成本比不缓存时低 10 倍。对于 system prompt 和工具定义稳定的高流量系统，prompt 缓存可以将每次调用成本降低 30–60%。第 18 章将把这个变成绑定工作流 #9。
