#### 概念检查

**Q1.** 解释为什么 _输出 token 成本主导_ 大多数生产 AI 系统，尽管输出 token 数量比输入 token 少。

**答案：** 输出 token 通常按输入 token 的 4-5 倍费率计费（例如 Sonnet 4：3 美元/100 万输入 vs 15 美元/100 万输出；GPT-4o：2.50 美元/100 万输入 vs 10 美元/100 万输出）。即使典型调用的输出是 200-600 token vs 1000-5000 输入，成本计算常偏向输出：

一次典型 Sonnet 4 调用带 2000 输入 + 500 输出：

* 输入成本：2000 × 3 美元/100 万 = 0.006 美元
* 输出成本：500 × 15 美元/100 万 = 0.0075 美元

输出在绝对美元上更贵尽管 token 少 4 倍。随着输入增长（长上下文、检索 RAG 片段），平衡可偏移，但对大多数生产调用，输出是有意义的或主导的。

含义：**减少输出长度是真实成本杠杆**。要求"简洁、结构化"输出的 prompt 比产生冗长响应的 prompt 节省更多，即使在同一模型。结构化输出（第 7 章）常比等价散文更短；停止序列限制不必要续写；prompt 中显式长度限制减少输出。每个节省的输出 token 乘以 5 倍输出乘数。

**Q2.** Anthropic prompt 缓存的盈亏平衡是什么，为什么阈值如此低？

**答案：** Anthropic prompt 缓存的盈亏平衡在首次复用时即达到。严格表述：写入溢价足够小使得任何在 5 分钟 TTL 内至少读取一次的缓存前缀远超回本写入成本。一次读取的节省（0.00405 美元）远超写入溢价（0.0011 美元），当前缀大小非平凡时。

计算：

* 写入溢价：0.25 倍输入费率（首次调用支付 1.25 倍而非 1.0 倍）。
* 读取节省：0.9 倍输入费率（每次后续调用支付 0.1 倍而非 1.0 倍）。
* 比率：写入溢价 / 读取节省 = 0.25 / 0.9 ≈ 0.28。

所以一次缓存写入 + 约 0.28 次缓存读取 = 盈亏平衡。超出此的任何使用都是净正。对于在调用 1 缓存并在调用 2 读取的系统 prompt，节省（调用 2 上输入成本的 90%）远超写入溢价（调用 1 上多 25%）。

低阈值意味着：**应对任何稳定内容启用缓存**。将内容标记为可缓存的工程成本极小；节省立即开始。唯一不应缓存的内容是真正每调用动态的内容（用户消息、新鲜检索）。

**Q3.** 生产 AI 团队通常部署的三个缓存层是什么，各自捕获什么？

**答案：** 三层：

1. **提供商侧 prompt 缓存（Anthropic）或隐式缓存（OpenAI）**。在提供商缓存 TTL（默认 5 分钟）内捕获稳定前缀。系统 prompt、工具定义、稳定 few-shot 示例上 30-60% 成本降低。
2. **应用层精确缓存**。捕获跨用户重复的相同查询+配置。哈希输入；将响应存储在 Redis/memcached。FAQ 式流量上 5-15% 降低，精确重复常见处。
3. **语义缓存**。通过嵌入相似度捕获释义等价查询。类似问题复发的流量上 10-30% 降低。比其他两层有更高复杂性和假阳性风险。
