此案例的预期结果：具体性可能通过行引用指令达到 0.75 到 0.80；误报率可能通过严重性标记变更达到 0.78 到 0.82。总分在 3 到 4 次约半天每次的迭代中从 0.73 提升到 0.78 到 0.81 区域。Eval 是方向盘；没有它，你在猜测。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_代码审查 bot prompt 在四个维度上得分 0.73（正确性 0.78、具体性 0.65、误报率 0.71、语气 0.79）。设计一个 prompt 迭代循环提升两个弱维度而不回归其他维度，使用多轴 eval 纪律。_ 你验证 (a) LLM 不推荐针对一个维度而忽略跨维度影响的变更（LLM 生成 prompt 建议的最常见故障模式；它优化被问维度并静默回归相邻维度），以及 (b) 提议的循环包含 _拒绝_ 步骤（一些迭代不奏效必须回滚；LLM 生成的循环经常跳过回滚步骤并将每个变更视为改进）。
