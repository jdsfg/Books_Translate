#### 时间有效性

法规被修订；案例被推翻。检索块必须在查询日期时是当前的。

实现：每个块有 `effective_date` 和 `deprecation_date` 元数据。自查询检索器解析查询的 effective-as-of 日期（默认：当前）；检索过滤到在该日期时当前的块。LLM 的 prompt 注明日期并要求答案时间正确。

三者都是分层在标准 RAG 管道上的工程纪律。没有它们，Strata 的 RAG 大致正确但对实际用例（律师在特定日期为特定客户在特定管辖权检查特定法规）不可靠。有了它们，系统可辩护地达到生产级。

#### 常见误解

管辖权过滤、时间有效性和引用精度看似 _法律技术专属_ 关注。它们不是——它们泛化。医学 RAG 有自己的版本（药物交互安全：关于药物 X 的检索块必须对照患者其他药物检查）；金融 RAG 有监管管辖权（美国证券法不同于欧盟）；企业 RAG 有访问控制（检索块必须对查询用户可访问）。_模式_是：识别领域的硬约束；将它们编码为元数据；工程检索以尊重它们。约束不同；模式是通用的。

### 11.8 How This Breaks：遗漏长尾的 eval

**命名的故障模式**：遗漏长尾的 eval。团队的 eval 有 200 个代表典型生产流量的查询。eval 得分好；生产在不寻常查询的小部分上得分差。碰巧问不寻常问题的客户遇到差答案；团队的 eval 显示无问题。

**真实或复合事件**：Strata 早期生产的复合。200 查询 eval 代表 _常见_ 法律问题：合同解释、监管合规、标准案例法查找。约 10% 生产查询是不寻常的：跨管辖权比较、关于语料库中尚无的最近通过立法的查询、混合多个子领域的查询（例如税法 + 知识产权法）。eval 不测量这些；这些上的生产行为比标题指标暗示的差 20+ 分。

**工程缓解措施**：**对抗/长尾 eval 切片**。策划 50–100 个刻意从长尾提取的查询：罕见查询类型、边界情况、命中系统已知弱点的查询。单独评分这些；在仪表板中跟踪；独立于主切片对长尾切片的回归门控发布。

第二个缓解措施：**生产查询采样**。1-5% 的真实生产查询样本被标注并随时间加入 eval。eval 增长以覆盖实际查询分布；长尾 vs 常见的拆分可见。

**本应捕获它的 eval**：生产 eval 中的长尾切片，在每个 PR 上与主 eval 一起运行。纪律是 _刻意策划_ 长尾案例，因为它们不自然出现；它们罕见。团队必须寻找它们，通常通过审查生产日志中的不寻常查询并添加它们。
