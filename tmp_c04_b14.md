### 3.11 How This Breaks：静默重路由

**命名的故障模式**：静默版本漂移。provider 对日期版本的弃用处理方式不同。OpenAI 在过去的案例中 _静默重路由_ 弃用的别名到更新版本（著名的 `gpt-3.5-turbo` 案例）：相同字符串继续工作但背后的模型已经变了。Anthropic 相反，在弃用的日期版本于切换日期后被调用时返回 404——调用大声失败而非重路由。两种故障模式都伤害你：静默重路由让你的行为无警告地变化；大声 404 意味着错过的弃用通知产生中断。一个团队在代码中固定到 `claude-sonnet-4-20260315` 并发布。两个月后，provider 弃用该日期版本。团队没有注意到弃用通知。在切换日期，行为微妙地变化（静默重路由）或集成开始失败（404）。一个在旧版本上干净运行的 eval 开始产生不同结果——或根本没有结果。团队将变更解释为模型不当行为或自己的中断；花一周调试；最终追溯到版本切换。

**真实或复合事件**：一个来自整个行业的复合例子。多个团队在 2024–2025 年经历过这个故事，尤其是在主要版本升级前后。教训已被广泛宣传但在弃用通知是三个月前的一封安静邮件时很容易忘记。

**工程缓解措施**：三层。

1. **订阅你的 provider 的弃用公告**，并使它们路由到团队别名，而非单个工程师的收件箱。provider 公告是规范信号。
2. **定期扫描你的代码库中的硬编码模型版本**，验证每个是否仍然有效。一个每周运行的简单脚本就足够。
3. **在切换之前对候选替换版本运行你的 eval 套件**。这是模型升级流程（第 21 章）。如果 eval 结果匹配，切换是安全的；如果不匹配，你有时间调查。

**本应捕获它的 eval**：一个 _模型版本回归 eval_，对当前版本和公告的替换版本都运行，带有标记任何分数退化的 delta 检查。如果团队在弃用通知到达的那天运行了这个，他们会看到替换模型上 4% 的拒绝率偏移，并 (a) 找到一个缩小差距的 prompt 调整，或 (b) 带着具体数据升级到 provider，或 (c) 做出迁移到完全不同模型的明智决策。这里的故障模式是 _运营纪律_，而非任何个别工程 bug，而 eval 纪律是捕获它的东西。第 21 章的绑定工作流 #14 构建了告警。
