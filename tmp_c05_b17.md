#### 概念检查

**Q1.** 一位队友争辩说："我们的 LLM 功能不需要 eval；我们对所有周围代码都有单元测试。"指出这个论点遗漏 eval 要点的两个不同方式。

提示

单元测试验证函数在你知道正确输出的案例上的行为。一个 LLM 驱动的函数在 _知道正确输出_ 方面是什么样的？

**答案：** 论点遗漏要点的两种方式：

1. **单元测试验证确定性正确性；eval 测量概率性质量。** 对函数 `parse_user_input(text) -> dict` 的单元测试确认，对于固定输入，函数返回特定输出。输出是唯一且正确的。对于 LLM 驱动的函数 `summarize_clinical_note(text) -> str`，没有唯一正确输出；许多摘要是可接受的，可接受程度各异。单元测试无法捕获这个；带量表和参考标准组合的 eval 可以。
2. **单元测试验证不变的东西；eval 检测 _非代码_ 东西变化时。** 即使所有代码未变，系统行为也可能漂移，因为 LLM provider 静默更新了模型，因为检索语料库添加了模式微妙不同的新源，因为用户群体的输入分布偏移了，因为 prompt 注入技术开始流传。这些都不改变周围代码；都不被周围代码的单元测试捕获。eval，定期对稳定 golden set 运行，检测这些代码外变化。两层（代码的单元测试、系统行为的 eval）是互补的，而非冗余的。

队友的立场在单元测试是必要的且它们与 eval 不同这一点上是对的。eval 是额外层；它们不是可选的。

**Q2.** 解释 _百分比一致性_ 和 _Cohen's kappa_ 作为标注者间一致性指标的区别。为什么 kappa 对构建 golden set 的纪律很重要？

**答案：** 百分比一致性是两个标注者给相同标签的案例的原始比例：如果两个标注者在 100 个案例中有 90 个标注相同，百分比一致性是 0.90。Cohen's kappa 对偶然一致性进行校正：它问 _这个一致性比偶然好多少？_ 在一个 95% 案例标注为 _是_ 的二元任务中，两个在剩余 5% 上抛硬币的标注者仍然会偶然在 95% 的时间一致，即使他们在困难案例上做无意义的工作。百分比一致性将此报告为 0.95（看起来很好）；kappa 将此报告为约 0（正确识别为不比偶然更好）。

kappa 对 golden set 纪律重要因为它告诉你标签是否在 _测量真实的东西_。如果 kappa 低于 0.4，标签足够嘈杂以至于不是可靠信号；将它们用作 golden set 意味着你的 eval 部分在测量标注者困惑，而非系统质量。生产团队以 kappa ≥ 0.6 为可用信号目标，kappa ≥ 0.8 为自信信号。没有 kappa，你无法判断你的 golden set 是坚实的还是建立在你没看到的不一致之上。
