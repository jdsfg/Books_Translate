### 章节小结

Token 经济学决定钱花在哪里：输出是输入费率的 4-5 倍；缓存输入比未缓存输入便宜约 90%；工作马模型比前沿模型便宜 10-25 倍。原则：缓存稳定内容；批量非时间敏感工作；减少输出长度。

Anthropic prompt 缓存（绑定工作流 #9）是 2026 年的主导缓存机制。盈亏平衡低于 1 次调用；任何在 TTL 内使用两次的稳定内容都是净正。系统 prompt、工具定义、few-shot 示例应始终缓存。缓存命中率是运维指标（目标：稳定前缀上 >80%）。

语义缓存（绑定工作流 #10）将缓存扩展到释义等价查询。相似度阈值 0.90-0.95；TTL 按领域；可选 LLM 验证步骤控制假阳性。Helios 在客户查询上 22% 缓存命中率节省约 80K 美元/月。假阳性故障模式是核心风险；缓解包括保守阈值、验证、按领域调优、漂移检测。

批量推理对异步工作负载提供 50% 折扣。文档摄取、eval 运行、回填、批量摘要适合批量；交互式 UI 和语音不适合。审计工作负载；路由合格的。

2026 年的蒸馏很大程度上被路由捕获（用工作马带 eval 验证）；基于微调的蒸馏增加复杂性（训练、维护、半衰期）对大多数团队很少值得。

绑定工作流 #11（§18.8）是成本仪表板：每功能、每租户、每模型、每天成本视图带预算告警和异常检测。工程量 2-4 周；价值是持续支出可见性和问责。

Helios 的 FinOps 故事（§18.9）：从 2024 年 20 万美元/月到 2026 年 100 万美元+/月由量驱动，每调用成本通过路由 + 缓存 + 每租户治理下降。纪律持续；仪表板每周审查；告警在重大影响前捕获回归。

成本追踪是第 16 章的第 6 层，第 17 章的路由器是其最大杠杆，后续内容在其上构建：第 19 章使可观测性成为数据基础，第 21 章将成本纳入生产 eval，第 24 章将 FinOps 转为团队纪律。

生产 AI 中的成本优化是工程，非财务。路由、缓存和批量是真实的、复合的杠杆；仪表板是使节省持续的纪律。

### 18.X 练习题
