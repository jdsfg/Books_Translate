**Q6.** _工具目录的跨租户隔离审计_。你的平台向约 80 个企业客户提供共享 AI agent。agent 工具目录中有 22 个工具（数据查找、文档搜索、集成触发、分析查询）。你怀疑某些工具的租户隔离强制执行可能比其他弱。设计审计：(a) 工具清单；(b) 按工具隔离审查；(c) 发现分类；(d) 修复计划。

> _提示_：工具目录审计是系统性工程练习；纪律是盘点每个工具，按同一强制执行标准检查每个，不跳过任何因为 _它肯定没问题_。

**答案：**

(a) **工具清单**：

构建电子表格，每工具一行，列：

* _工具名称_
* _类别_（数据读、数据写、集成触发、分析）
* _负责人_（负责的工程师或团队）
* _需租户上下文_（是/否/部分）
* _数据范围_（工具可触及哪些数据类别）
* _权限检查位置_（代码中租户检查发生处）
* _上次审计日期_

对 22 个工具，此清单需 1-2 天。电子表格成为审计的工作文档和持久记录。

(b) **按工具隔离审查**：

对每个工具，验证以下（_隔离检查清单_）：

1. _租户 ID 在请求上下文中_。工具从已认证会话接收请求租户 ID，而非从 LLM 的工具参数。
2. _目标租户 ID 对照请求租户 ID 检查_。工具的数据层知道哪个租户拥有目标数据；比较 `request.tenant_id == target.tenant_id`（或 `target.tenant_id IN allowed_tenants(request)`）被强制执行。
3. _基于过滤的查询在 SQL 或 API 层租户限定_。无工具可执行无租户过滤的查询；如查询以某种方式在无过滤下构造，数据库层拒绝它。
4. _权限拒绝被记录_。跨租户访问尝试产生路由到安全可观测性的结构化日志条目。
5. _权限拒绝统一_。工具向 LLM 返回通用 _权限拒绝_，非揭示目标是否存在的诊断。
6. _单元测试覆盖跨租户案例_。每个工具有至少一个模拟跨租户尝试并验证拒绝的测试。
7. _集成测试端到端覆盖跨租户案例_。完整 agent 管道用尝试诱导跨租户访问的对抗输入测试。

对每个工具，标记每个检查清单项：通过、失败或部分。对部分案例记录笔记。

(c) **发现分类**：

* _关键_：任何在生产中允许跨租户数据访问的工具。停止一切；必要时轮换密钥；启动安全事件响应。修复：立即（发现后 24 小时内）。
* _重大_：任何缺少项 1-3（确定性强制执行层）之一的工具。工具可能当前未泄漏，但强制执行不在纵深防御姿态。修复：30 天内。
* _次要_：任何缺少项 4-7（可观测性和测试层）的工具。确定性强制执行存在，但团队检测或测试能力不完整。修复：90 天内。
* _文档_：任何清单行不完整的工具（如负责人不明；数据范围模糊）。作为审计的一部分解决；严格说非安全发现而是纪律缺口。
