3. **模态适当内容**。一些内容适合语音（解释、对话响应）；其他内容适合文本（URL、长列表、代码、结构化表单）。编排必须选择；LLM 可被要求不仅发出语音还发出"结构化旁内容"路由到文本。输出 schema 变为多模态。
4. **每渠道延迟预算**。语音有亚 500ms TTFT 预算；文本有宽松得多的容忍。编排不能为等待文本渠道内容而阻塞语音；渠道必须独立分发。
5. **Eval 和可观测性 schema 扩展**。按第 19 章 Q5，schema 扩展以捕获跨模态事件。按 eval 纪律，评分标准评分跨模态一致性（渠道是否保持同步？）。

延续的纪律：上述每个工程挑战从单模态章节中可识别。同步是编排关注；冲突解决是 agent 循环关注；模态路由是分发关注；延迟预算是成本和质量关注；schema 扩展是可观测性关注。框架吸收新模态组合；团队构建扩展，非从零开始。

一个具体的 2028 年预测：双模态调度成为客户服务 AI、金融建议 AI、医疗 AI 和任何语音承载对话线索且文本承载结构化产物（表单、文档、代码）的高价值交互的标准。本章（第 20 章）的工程纪律是基础；与文本渠道机制的集成是扩展。

更广泛的教训，超越语音：随着 AI 系统服务更复杂的交互，模态空间扩展。语音加文本加视觉加工具使用加计算机使用都在菜单上。每个组合是多模态轨迹。内化了本章纪律（轨迹思维、eval 驱动工程、有界编排、完整可观测性）的团队可将新模态组合作为添加吸收；将每个模态视为独立栈的团队构建不互操作的孤立产品。框架的价值随模态增多而复合。
