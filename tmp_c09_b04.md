#### 手段 4：通过工具即时加载

最高上下文效率的手段是 _不_ 预加载所有可能有用的上下文，而是给模型工具来获取它需要的。模型决定查什么，工具获取它，工具输出进入下一次推理轮次的上下文。

对于 Helios 的客服 agent：不预加载客户完整账户历史（可能 20K token），而是给 agent 一个 `get_customer_field(field)` 工具。agent 仅获取 _它需要的字段_。每次调用的上下文急剧缩小；权衡是更多推理轮次（每次工具调用一轮）。

工具模式是上下文工程最激进的形式；第 13 章展开它。

### 8.5 中间丢失：真实现象，部分缓解

2023 年 Liu 等人的论文（"Lost in the Middle: How Language Models Use Long Contexts"）确立了，当相关信息放置在长上下文中的不同位置时，模型准确性随位置变化。上下文开头和结尾的信息比中间的信息更可靠地被关注。

到 2026 年，前沿模型在此方面有所改进：它们在长上下文检索上明显优于 2023 年代模型。但现象未被消除。对上下文工程的实践含义：
* **将关键指令放在 prompt 末尾**（最接近生成开始处）。
* **将最相关的检索内容放在检索上下文块的开头和结尾**。
* **按相关性排序检索块**（最高在前，最低在倒数第二；大块检索块的绝对中间是最弱位置）。
* **对于非常长的上下文（>50K token 检索材料），预期一些中间丢失损失**并考虑是否可以通过更强检索或分块减少语料库。

这些是战术，不是定律。前沿模型处理长上下文足够好使放置不如 2023 年重要，但边际效应仍然真实且值得为高量或高风险系统工程化。

### 8.6 长上下文 vs RAG 决策树（绑定工作流 #4）

2026 年的核心架构问题是：何时将所有内容放入长上下文，何时构建 RAG 管道？

答案取决于三件事：

1. **相关语料库的大小。** 多少 token 的文本可能与典型查询相关？
2. **延迟容忍度。** 长上下文调用比 RAG 增强调用慢（检索已缩小输入）。
3. **成本。** 长上下文调用为每次调用中包含语料库的每个 token 付费；RAG 为检索基础设施加每次调用仅检索到的块付费。
