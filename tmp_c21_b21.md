产品），以文本结束（agent 邮发确认）。架构必须优雅处理模态切换；这是新的工程前沿。

本书教授的纪律——eval 驱动工程、有界设计、可观测性、成本纪律——对多模态和单模态同样适用。纪律是持久的；模态特定模式演化。

**Q3.** _语音-文本双模态调度_。本章的工作流 #13 包含回退到文本但将语音和文本视为系统在其间选择的独立模式。推测语音和文本并发操作的未来：客户在语音通话中，对话中途系统同时打开文本渠道（发送链接、文档预览、结构化表单）供客户并行交互。论证此双模态调度是本章框架的自然扩展，并识别出现的工程挑战。

**讨论：** 双模态调度（语音 + 文本并发运行，非互为回退）是可信的 2027-2029 生产模式。客户服务是一个驱动因素：语音 agent 引导客户完成流程可同时向文本渠道推送结构化内容（这是表单链接；这是您的订单预览；这是确认码）补充语音交互。语音和文本渠道不是替代；它们是同一轨迹中的协调渠道。

本章的框架自然支持此点：

* **轨迹模型**（工作流 #13 和 §19.2）已是事件序列。双模态在多渠道上添加事件；轨迹捕获所有事件。
* **编排层**将事件分发到适当渠道。语音分发到 TTS；结构化内容分发到文本渠道；工具执行可能产生两者。
* **Eval 纪律**将轨迹作为整体评分：客户的请求是否被处理了？处理跨多模态是轨迹的实现细节。
* **可观测性层**在同一追踪下捕获语音和文本事件。跨模态调试（客户被口头要求输入确认码，但文本渠道从未显示码字段）成为可能。

工程挑战：

1. **同步**。语音和文本渠道必须就状态达成一致。如语音说"我正在给您发送表单链接"而文本渠道尚未发送链接，客户困惑。编排必须协调分发；文本渠道到达必须先于或同时于口头提及。
2. **冲突解决**。客户可打断任一渠道。他们可能说"等等，那是错误的表单"同时在表单中输入数据。系统必须整合两个信号；LLM 调用必须将语音转录和文本渠道交互都作为输入。
