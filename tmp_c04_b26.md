**答案：** 有用的 prompt：_编写一个计算重试等待时间的 Python 函数，遵循 HTTP `retry-after` 头（可能是整秒或 HTTP 日期形式），回退到有上限的指数退避，在两种情况下都应用抖动。然后解释没有抖动时出现的 bug。_ 你通过 (a) 测试两种头形式（整数和 HTTP 日期）以及格式错误头路径来验证；LLM 编写的解析器经常遗漏 HTTP 日期形式，因为整秒是其训练数据中更常见的情况；以及 (b) 检查抖动是否实际分散了重试：在单元测试中，调用 `compute_retry_wait("30", 1)` 1000 次并确认结果分布开来，而非全部是 30.0。惊群解释应具体说明为什么同步重试比分散重试更糟；如果 LLM 给出模糊的答案（"它过载了服务器"），追问机制。

**Q6.** _通过第 2 章的心智模型阅读 2026 年事件_。想象 Strata Research 的以下复合事件：法律科技 RAG 平台已在 `claude-sonnet-4`（别名，非日期版本）上运行三个月。在一个周二早上，客户投诉激增：在一类合同条款查询上输出微妙地变差，措辞不符合公司的内部风格。团队的 eval（他们一直在每天运行）显示相关切片隔夜下降 7 个点。借鉴第 2 章的心智模型（§2.3 关于 RLHF/DPO、§2.6 关于 hallucination、§2.7 关于 prompt 敏感性）和第 3 章的 §3.11 _静默重路由_ 来诊断事件。最可能的根因是什么，第二可能的根因是什么，即时缓解措施是什么？

提示

别名正在使用这一事实，加上隔夜在特定切片上的回归，强烈指向两个架构原因之一。§3.11 显式命名了一个。

**答案：** 最可能的根因恰好是 §3.11 警告的：**静默重路由**。Anthropic 隔夜发布了 Sonnet 4 的小更新（例如 `claude-sonnet-4-20260415` 替换 `claude-sonnet-4-20260315` 作为别名的目标）。新日期版本有略微不同的 RLHF/DPO 行为（§2.3）：训练后更新以影响 Strata 内部风格密集切片的方式移动了模型在风格和措辞上的偏好。7 个点的下降不是随机的；它集中在新训练以不同方式强调的切片上。从工程师级心智模型来看：模型的 _训练行为_ 是其特定微调语料库的函数，一个重新训练但同名的模型可以在不在上游 eval 套件中的切片上有不同的训练行为。团队自己的 eval 捕获了它；没有每日 eval，他们会从客户投诉中得知。

第二可能的根因是 **prompt 敏感性偏移**（§2.7）与一个小变更的交互。也许 Strata 的 prompt 加载器隔夜拾取了一个配置变更（空白差异、重新排序的 few-shot 示例、system prompt 中的新行）。新 prompt 功能上相似但产生略微不同的下一个 token 分布；在 prompt 最关键的切片上，质量下降。这更难发现因为它需要 diff 实际命中 API 的 prompt，而非仅 git 中的 prompt 模板。

即时缓解措施：
