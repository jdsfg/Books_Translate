#### 深入思考

**Q1.** _工具调用的未来_。2026 年的工具调用本质是函数调用：LLM 发出结构化参数；应用执行；结果反馈。推测 2030 年：工具调用会演化为更复杂的东西（例如更丰富的 agent 到工具协议、原生多步规划），还是核心仍是函数调用？

**讨论：** 两种力量塑造演化：

1. **模型能力增长**：LLM 对工具序列的推理改善。2026 年，模型偶尔做差工具选择决策；到 2030 年，很少做。对精心规划后执行架构的需求缩小；ReAct 式循环对更多情况足够。
2. **工具生态系统形式化**：Anthropic 的 Model Context Protocol（MCP，2024）和类似标准建立工具发现和工具组合协议。工具变得更像有自己 API 的软件服务，LLM 可发现和使用，而非每个应用的自定义函数。这使工具使用跨系统更 _可互操作_。

到 2030 年：工具调用可能在 _结构上_ 仍是函数调用，但周围基础设施（发现协议、组合模式、能力协商）更丰富。2030 年的 agent 可能从第三方 provider 发现和使用工具（类似 2026 年应用使用第三方 API），LLM 作为编排器。

一个具体 2030 预测：**agent 到 agent 通信标准涌现**。今天的 agent 循环是单体的（一个 agent 与工具对话）；2030 年可能看到 agent 间通信的标准化协议，带自己的发现和信任机制。"agent 团队"模式（第 15 章）变为基础设施级，非应用级。

本章教的纪律——工具 schema 作为文档、策略门控、沙箱——在架构转变中存活。具体工具、协议和标准演化；模式保留。

**Q2.** _沙箱作为长期必需_。到 2030 年，LLM 生成代码可能比 2026 年可靠得多。沙箱仍然必要吗？论证双方。

**讨论：** **沙箱存活的论点**：即使生成可靠性改善到 99.99%，规模化下单次安全事件的成本巨大。沙箱相对于该成本便宜。_纵深防御_纪律说：不依赖任何单层保障安全。沙箱是地板。到 2030 年，沙箱基础设施可能更轻（更快启动；更好资源效率）但不更不必要。

**沙箱可能转移的论点**：随代码生成可靠性上升，某些低风险用例可能在更轻量隔离中运行代码（例如进程级隔离而非容器级）。最极端隔离可能留给高风险用例；低风险用轻隔离。沙箱作为 _光谱_ 而非 _单一技术_。

**最可能的 2030 现实**：沙箱对代码执行保持普遍；具体隔离技术演化（Firecracker 式 microVM 可能是 2030 主导模式）；成本缩小（每次执行成本从美分 → 美分分数）；运维成熟度上升。
