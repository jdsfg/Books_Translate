### 章节小结

2026 年文档理解的转向是 **视觉优先摄取**：而非 OCR-然后-解析，视觉语言模型直接处理页面图像并产生保留布局、表格、签名和图表的结构化表示。OCR 替代故事对通用文档 RAG 大体真实；经典 OCR 在特定高量、稳定格式案例中持续。

多模态嵌入——CLIP、OpenCLIP、ColPali、Voyage multimodal-3——处理图像相似性重要的查询。ColPali 的后交互架构是布局重要的文档图像检索的参考设计；它比双编码器嵌入更重但在布局敏感任务上更准确。

通过 VLM 的表格和图表提取对干净文档足够可靠用于生产（>95% 准确率）；更杂乱的文档需要 eval 驱动的纪律以验证提取质量。

Strata Research 的视觉优先合同管道（§12.5）是案例研究：表格、签名和布局敏感查询上 30 分质量提升；适中成本（生产规模约 7K 美元/月；完整分解在 §12.7）；架构模式（PDF 拆分 → VLM 提取 → schema 归一化 → 分块 → 多模态索引）泛化到大多数文档 RAG 项目。

生产模式包括视觉 token 成本意识（分辨率控制、批处理、缓存）、手写和降质质量处理（置信度标记、多遍提取）和不可处理内容的优雅失败（故障隔离、跟踪、用户可见标记）。此处构建的多模态 RAG 扩展了第 9–11 章的检索架构；同一视觉能力在第 13 章成为 agent 工具，批处理在第 18 章作为成本杠杆回归，摄取管道可观测性在第 19 章出现，提取故障模式需要第 23 章开发的 eval 覆盖。

两个转变要带向前。第一，2026 年文档理解已从 OCR 优先跨越到视觉优先：模型现在就是通用文档 RAG 的 OCR。第二，多模态 RAG 已不再是专用——它是任何触及真实世界文档的系统的有效模式。

### 12.X 练习题
