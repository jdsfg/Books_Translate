### 24.11 How This Breaks：未经 eval 门控就发布的功能

**命名的故障模式**：未经 eval 门控就发布的功能。团队在范围界定时跳过能否 eval 此功能门控。功能被构建。eval 在构建后尝试发现不可行：评分标准不产生可靠标签；阈值无法辩护。团队现在处于有已构建功能但无可辩护质量测量的尴尬位置。

**真实或复合事件**：Beacon 终止的反面。一个团队构建 AI 功能；发布前 eval 设计失败；团队以模糊的质量保证（"我们非正式测试了"）发布。六个月后，客户投诉揭示质量问题；团队无法量化问题多广泛因为他们没有 eval；恢复无结构。功能最终被静默弃用。

**工程缓解措施**：

1. **eval 门控在范围界定时不可商量**。无可行 eval 设计的功能不通过规格。
2. **早期试点标注**。标注者间一致性应在承诺工程构建周期前测量。如标签无法可靠，eval 无法可靠。
3. **eval 设计的高级工程审查**。不让初级工程师自批准他们的 eval；高级审查在早期捕获 _这实际上行不通_ 的案例。
4. **_在范围界定时终止_ 的纪律作为值得庆祝的技能，非失败**。在范围界定时终止功能的团队应被赞扬；终止 10 个功能在范围界定并发布 5 个带可靠质量的团队比发布 15 个未知质量功能的团队更强。

**本应捕获它的 eval**：范围界定时的 eval 设计审查。纪律是流程，非具体代码。制度化审查的团队在范围界定时捕获故障模式；未制度化的团队发布后在生产中发现问题。
