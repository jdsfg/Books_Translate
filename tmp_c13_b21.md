第三个值得提及的能力：**视频 RAG**。今天的 RAG 最多基于图像；视频理解是 2027–2028 地平线。到 2030 年，视频存档上的 RAG（教育内容、监控录像、存档广播）将成熟。应用：企业会议搜索（"我们上季度会议中何时讨论了 X？"）；教育内容导航（"找到讲师讲解 Y 的部分"）；合规审查（"标记任何员工与竞争对手讨论定价策略的会议片段"）。

这些类别共享共同架构：丰富多模态检索 + 强大多模态生成。2026 年技术栈——视觉优先摄取、多模态嵌入、ColPali 式检索——是基础；2030 年扩展到视频、3D、传感器数据及更远。_你的上下文中有什么，由你的 eval 评估，接地于检索源_的纪律泛化。

**Q3.** _ColPali vs 文本 RAG 作为语料库特定决策_。本章引入 ColPali（后交互文档图像检索）作为布局敏感语料库的正确答案。ColPali 和标准文本 RAG 之间的决策应按语料库做出，非按公司。考虑三个具体语料库：(i) 5 万页企业年报存档（10-K 备案，混合文本和大量表格/图表）；(ii) 1 万页软件 API 参考（主要是代码块和散文）；(iii) 20 万页专利语料库（文本带嵌入技术图纸和化学结构）。对每个，推荐 ColPali、文本 RAG 或混合，并识别会改变你推荐的具体 eval 证据。

**讨论：**

**企业年报（10-K 备案）**：**混合（ColPali + 文本 RAG）**。

年报有布局密集结构："风险因素"节、"MD&A"节、财务报表和脚注，各有自己的视觉布局和表格约定。布局敏感查询（"最新 10-K 中披露的风险因素是什么？"）受益于 ColPali 的区域级检索。文本密集查询（"公司如何描述其竞争壁垒？"）用标准文本 RAG 工作良好。混合让每种查询类型使用最佳检索模式。

会改变推荐的 eval 证据：

* _纯文本到处赢_：如仔细 eval 显示带好分块的文本 RAG 充分处理布局查询（例如财务报表表格被文本提取步骤可靠捕获），ColPali 复杂性不必要；降到纯文本。
* _成本主导_：如 ColPali 的存储和查询成本超过质量价值（例如质量提升 +3 分但成本 3 倍），权衡偏向纯文本。

**软件 API 参考**：**标准文本 RAG**。

API 参考是文本和代码；布局常规且一致（函数签名、参数列表、返回类型、示例）。页面图像表示不添加文本已有的东西。带代码感知分块的标准文本 RAG（保持函数块完整；不在函数中间断开）充分处理此语料库。

会改变推荐的 eval 证据：
