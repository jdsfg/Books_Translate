1. **对抗 eval 切片**：故意精选设计用于浮现灾难性失败模式的输入。对每个模型版本和 prompt 变更测试。
2. **输出分类器**：每个生产输出通过分类器检查灾难性类别（PII 泄漏；偏题；安全违规；被拒主题）。LLM 作为评判或基于规则的系统按输出运行。
3. **人工抽查**：小部分输出由人工审查，特别是在高风险路径。慢且贵；安全网。
4. **用户报告渠道**：用户可标记有问题输出；被标记输出进入即时审查。
5. **来源质量控制**（对 RAG）：检索来源经审查；低信任来源排除（Google 案例因从低质量 Reddit 帖子检索引起）。

没有单一层足够；层叠加。团队的纵深防御是捕获罕见但灾难性输出的关键。

**Q4.** 为什么 LLM 功能的 A/B 测试比经典 A/B 测试更难？识别两个具体挑战。

**答案：** 两个挑战：

1. **多维输出**：LLM 响应在多维度上变化（正确性、语气、完整性、长度、格式）。变体 A 可能在正确性上更好；变体 B 在语气上更好。单一 A/B 测试结果指标不捕获权衡。经典 A/B 是单指标（转化率；点击率）；LLM A/B 需要显式多维分析。
2. **Eval 噪声淹没小效应量**。LLM 作为评判的评分有固有噪声（第 4 章）；对小变体差异（如 10 分尺度上 0.1 分差异），噪声可能比效应大。统计显著性需要比经典 A/B 更大的样本和更长的实验。

第三个挑战：**用户适应混淆信号**。用户调整行为以适应系统响应。新变体初期可能看起来更差（用户已校准到旧变体）几周后更好（用户适应）。经典 A/B 通常不需考虑此点。

缓解措施：运行更长时间；显式测量多维度；按用户细分分层（新用户 vs 有经验用户反应不同）；将 A/B 参与度信号与离线 eval 分数结合。可信决策通常需要多信号对齐。

**Q5.** 当漂移检测系统触发告警时，为什么 _测量伪影_ 总在鉴别诊断列表上，什么具体检查将其排除或确认？

**答案：** 漂移告警可有两个广义原因：(a) 系统质量实际变化了；(b) _测量_ 变化了。两者产生相同仪表板症状（eval 均值下降）；响应非常不同。测量伪影必须总在鉴别上因为将其当作真实漂移处理浪费工程周期、可能导致不必要回滚、并损害团队对 eval 信号的信任。

排除或确认测量伪影的具体检查：

1. _评判模型变更_。评判模型最近是否更新、重新版本化或重新 prompt？提供商对评判的静默更新产生与系统漂移无关的 eval 漂移。检查：评判版本固定；最近评判 prompt 提交；评判校准基线。
