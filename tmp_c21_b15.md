* _打断率_：agent 多常打断用户？通过通话后审查定向。
* _客户满意度调查_：通话后评分；关于 agent 是否适当等待的特定问题。
* _通话放弃率_：通话中途挂断的客户可能指示对 EOT 问题的沮丧。
* _语音对话 eval_（生产采样）：按包含轮次交替质量的评分标准评分。

(d) **随时间演化**：

* 每周审查 EOT 相关投诉模式。
* 阈值调整的 A/B 测试：将 10% 流量路由到不同阈值；比较质量指标。
* 随 Helios 扩展到新客户细分的按垂直或按语言调优。

纪律：EOT 调优是持续工程，非一次性配置。像 prompt 调优一样对待：可测量、迭代、基于证据。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为客户服务语音 agent 设计 EOT 调优：基础阈值、按条件调整、质量测量、演化。_ 你验证 (a) 质量指标可操作化（打断率可测量；仅"客户满意度"不可；特定调查问题可），以及 (b) A/B 测试方法考虑噪声（小流量 A/B 测试可能统计无意义）。

**Q3.** _工程化回退路径_。草拟 §20.6 的回退到文本路径。指定：(a) 失败检测；(b) 状态保留；(c) 延迟预算；(d) 优雅恢复。

**答案：**

(a) **失败检测**：

* 语音提供商返回错误（速率限制、5xx、超时）。
* 语音连接断开（网络问题、模型过载）。
* 语音质量严重退化（TTFT 高延迟；音频质量问题）。
* 语音模型无法取得进展（短时间内多次连续错误）。

(b) **状态保留**：

* 到目前为止的对话转录（来自语音模型的先前输出）被保留。
* 当前工具执行上下文（如有进行中的工具）被保留。
* 用户认证会话和客户数据上下文被保留。
* 回退路径用此上下文继续；对话不重启。

(c) **延迟预算**：

* 回退比主语音慢（首音频约 1.5-3 秒 vs 主亚 500ms）。
* 用户感知到增加的延迟但对话继续。
* 可选：在回退转换期间插入简短填充音频（"请稍等……"）。

(d) **优雅恢复**：

* 一旦回退启动，通话其余部分留在回退上（不尝试切回主；转换是突兀的）。
* 如主恢复时通话仍活跃（如下次通话），恢复正常主路由。
* 团队监控回退率作为运维指标；激增指示主问题。

工程量：干净回退实现约 2-4 工程师周。价值是生产语音系统所需的故障模式处理。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
