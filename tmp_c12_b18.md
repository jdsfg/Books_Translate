* **用元数据过滤检索对冲**：交叉引用第 11 章的自查询检索（§11.2 模式 4）。如果用户有 _任何_ 管辖权信号（显式或在先前轮次中），使用它作为过滤；HyDE 的管辖权猜测被覆盖。对于无管辖权信号的模糊查询，运行两次并行检索（HyDE 猜测的管辖权 + 次可能）并让重排序器决定。
* **HyDE 管辖权的置信阈值**：prompt LLM 表达对其管辖权猜测的置信度。低置信案例触发澄清用户问题而非猜测。

(d) **eval 切片**：50 查询"模糊查询"切片，标注用户实际意图的案例（从生产查询日志配对用户后续澄清收集）。有 HyDE 和无 HyDE 评分检索 recall@10。此切片上的提升是指标；生产决策取决于提升是否证明额外 LLM 调用成本（约 0.0005 美元/查询）合理。

交叉引用：此练习将 §11.2 查询重写模式特化到高模糊领域。相同逻辑适用于任何短查询领域（医学"狼疮关节痛"；技术支持"登录失败"）。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_为模糊法律查询设计 HyDE 预检索步骤：HyDE prompt、检索馈入、错误管辖权处理、eval 切片。_ 你验证 (a) 重排序器对原始查询评分，非 HyDE 输出（常见 LLM 错误：通过整个管道放大 HyDE），以及 (b) eval 切片具体（50 个标注查询，非模糊的"在模糊查询上测试"）。
