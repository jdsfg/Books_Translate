以下是 Helios 的客户支持 agent 设计文档。格式可移植。


    # Agent 设计文档: Helios 客户支持 Agent v3

    ## 目的
    在可能时自主处理客户支持工单；在适当时升级到人工。

    ## 轨迹边界
    - 每轨迹最大迭代次数: 15
    - 单次推理调用最大 token: 4,096（输出）
    - 每轨迹最大累积 token: 80,000
    - 每轨迹最大美元成本: $0.50（含 $5.00 的覆盖路径）
    - 最大墙上时钟时长: 90 秒（交互）/ 5 分钟（后台）

    ## 工具目录版本: v7
    - 跨信息、计算、操作、升级类别的 40 个工具
    - 完整规范见 tools/catalog/v7.yaml

    ## 记忆架构
    - **工作记忆**: 当前轨迹的对话历史；如超过 30K token
      通过摘要压缩以保留用户目标 + 最后 5 轮。
    - **情景记忆**（每会话）: 客户 ID、工单 ID、所做决策（发出的退款、
      执行的升级）、运行摘要。通过 `get_session_state` 工具可用。
    - **长期记忆**（每客户）: 客户的先前工单历史（最近 10 个
      工单摘要 + 结构化关键事实）。会话开始时通过
      `load_customer_profile` 工具检索。

    ## 断路器
    - 同一工具用相同参数调用 3 次: 停止并报"检测到循环"。
    - 同一工具连续失败 4 次: 停止并报"工具不健康"。
    - 成本断路器在 $0.40 触发（距预算留 $0.10 缓冲）。
    - 迭代计数器在 12 触发（留 3 次迭代缓冲）。

    ## 策略门控（按第 13 章）
    - 退款工具: 每租户金额上限；需客户确认。
    - 修改客户记录: 字段允许列表；强制审计日志。
    - 发送邮件: 模板允许列表；冷却期防止邮件风暴。
    - 升级到人工: 始终允许。

    ## 处理的故障模式
    - 无限循环: 迭代上限 + 断路器限制。
    - 幻觉工具调用: 工具 schema 验证 + 结构化错误 + 工具断路器。
    - 丢失目标: 用户目标通过系统 prompt 每次迭代重新注入；摘要压缩保留它。
    - 上下文溢出: 30K 阈值的轨迹内摘要。
    - 成本超支: 成本断路器 + 预算跟踪。

    ## 可观测性
    - 每次迭代的工具调用、延迟、成本和结果记录日志（第 19 章）。
    - 轨迹追踪可在 Langfuse 中查看，含完整对话、工具调用和工具结果。
    - 告警: 轨迹 > $0.30，轨迹 > 10 次迭代，轨迹失败率 > 每小时 2%。

    ## Eval 套件
    - 200 个带参考结果的 golden 轨迹。
    - 每轨迹指标: 偏转（agent 是否无需人工解决？）；客户
      满意度（LLM 评判 + 会话后信号）；每次解决成本；每次解决延迟。
    - 对抗切片（50 个轨迹）: 包含 prompt 注入尝试、
      模糊请求、多问题对话。测试断路器激活。

    ## 回滚程序
    - 如 v3 生产上线显示 >1.5 倍失败率，清单固定到 v2 agent。
    - 支持每租户回滚（如特定关注，某些租户留在 v2）。

    ## 签署
    - AI 工程负责人: [姓名]
    - 平台工程师: [姓名]
    - 客户支持运营负责人: [姓名]
    - 合规审查: [姓名]
    - 日期: [日期]
