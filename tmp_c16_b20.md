**答案：** 有用的 prompt：_我们的系统有两个工作器；20% 任务需第三个关注。决策：添加第三个工作器还是扩展第二个。标准、权衡、eval、上线。_ 你验证 (a) 决策标准涉及专业化、并行性 _和_ 协调成本（LLM 可能仅强调专业化；协调成本真实且常被遗漏），以及 (b) 上线计划包含影子模式和显式回滚触发器（无这些，错误决策在生产中累积）。

**Q6.** _新供应商门户的计算机使用审计_。你的团队正在考虑部署计算机使用 agent 从新供应商客户门户提取周数据（无 API 可用）。上线前，你需要审计部署。交叉引用第 13 章 §13.5 沙箱和 §13.4 LLM 提议-应用处置。指定：(a) 可靠性 eval；(b) 安全控制；(c) 故障模式映射；(d) 人工在环检查点；(e) 审计轨迹要求。

**答案：**

(a) **可靠性 eval**：

* 30 轨迹试点：agent 在真实门户会话上尝试提取任务 30 次，已知正确输出（由手动做任务的人工操作员标注）。
* 指标：任务完成率（目标 ≥80%）；每任务延迟（目标 ≤90 秒）；每任务成本（目标 ≤0.50 美元）；提取数据准确性（目标每字段 ≥95%）。
* 故障模式分解：按原因分类失败（UI 元素混淆、多步流程错误、门户自上次运行后变更、凭证问题、超时）。
* 通过标准：全部四个指标达阈值；故障模式被充分理解。

(b) **安全控制**（按第 13 章 §13.5）：

* **沙箱 VM** 专用于此任务；无其他应用、无其他凭证。VM 在每次任务后销毁。
* **每任务范围化凭证**：VM 仅有此供应商账户的凭证；无 SSO；无其他企业系统访问。
* **默认只读**：agent 的工具目录包括屏幕读取和点击行动但无修改门户数据的行动（无表单提交、无删除、无支付）。如需写行动，需显式人工批准（见 (d)）。
* **网络出口受限**：VM 可连接供应商门户域名和无其他外部目的地。防止数据外泄。
* **资源上限**：每任务 CPU、内存、墙上时钟。超限强制停止。

(c) **故障模式映射**（按 §15.7 计算机使用故障模式）：

故障模式 | 检测 | 恢复
---|---|---
UI 元素混淆（点错按钮） | 行动结果不匹配预期下一步屏幕 | 停止；标记人工
多步流程错误（导航中迷失） | 轨迹超过预期 ≤20 步任务的 30 次行动 | 停止；记录；标记
门户变更（出现 CAPTCHA、UI 重新设计） | 基于视觉的 CAPTCHA 模式检测；或意外布局 | 停止；告警；排队重新试点
凭证问题（登录失败） | 登录尝试响应 | 停止；告警安全
超时（页面加载慢） | 墙上时钟上限 | 停止；用更长超时重试一次；如仍超时，标记
数据损坏（提取数据不符合 schema 验证） | 提取字段的 Pydantic schema 验证 | 停止；标记人工审查

每个失败优雅停止（失败时不继续）；每个失败记录日志且可审查。

(d) **人工在环检查点**：
