### 章节小结

传统 APM 不捕获 LLM 系统可调试所需的内容。生产 LLM 可观测性需要专用工具追踪 prompt、模型版本、输出、工具调用、轨迹、成本和 eval 分数。2026 年的栈：Langfuse、Helicone、Phoenix（Arize）作为命名入选者；Langfuse 自托管是受监管行业的默认；通用场景用 SaaS。

完整 LLM 追踪包括每 LLM 调用数据（模型、prompt、输出、token、成本、延迟）、每工具调用数据、每轨迹聚合、每会话连续性、以及生产中的 eval 分数。追踪是调试产物；它使调查结构化而非考古式。

绑定工作流 #12（§19.5）是真实 Langfuse 追踪范例：Helios 客户支持轨迹按 span 分解，带成本归因和 eval 分数。工程量初始集成 2-3 周；持续每功能插桩。

HIPAA 可观测性张力（§19.6）：追踪工具为临床流水线存储 PHI。选项：供应商 BAA（对一些团队可行）；自托管（对要求更严的受监管行业可行）。Beacon 的分叉方法——PHI 路径自托管；非 PHI 用 SaaS——是代表性的。

PHI 脱敏（§19.7）减少团队内敏感数据暴露。分层：正则捕获明显模式；NER 捕获长尾；定期审计验证质量。权衡：脱敏降低调试保真度；分层访问通过适当协议为深入调查提供未脱敏视图。

告警类别：成本激增、错误率、延迟、eval 分数、工具故障、轨迹长度、租户预算、缓存命中率、schema 验证、拒绝率。告警路由遵循层所有权；调优阈值以避免告警疲劳。

不在的追踪故障模式（§19.10）是运维风险：插桩缺口。缓解包括 CI 覆盖检查、合成监控、覆盖指标、每功能发布检查清单。

可观测性是第 16 章的第 5 层，第 17 章路由器层插桩所坐的基底，第 18 章在其上构建成本追踪的基础；第 14 章已使用 agent 追踪做轨迹调试。展望未来，第 21 章用生产 eval 质量信号扩展该层，第 23 章将其用于 prompt 注入检测，第 24 章将其保持健康的团队实践编纂成文。

可观测性是其他一切所站的数据层——调试、监控、成本追踪、质量测量都依赖它。尽早投资，完整插桩，为受监管数据分叉路径。

### 19.X 练习题
