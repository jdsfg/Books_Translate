#### 手段 1：摘要对话历史

对于多轮对话，历史随轮次线性增长。20 轮后，历史单独可能 10K–30K token。每新一轮，你重新发送整个历史，为 token 付费，占用预算，增加中间丢失问题的机会。

模式：**将较早轮次摘要为紧凑回顾；保留最近轮次逐字**。最常见形态：
* 最近 N 轮（通常 N=4–8）：逐字
* 较早轮次：摘要为模型维护的 _滚动摘要_
* 很早的上下文（用户初始设置、agent 声明的目标）：在滚动摘要部分之外逐字保留

摘要本身是一次 LLM 调用。常见模式是每 K 轮（K=5–10）作为后台或异步步骤执行此操作，使主推理调用看到新鲜压缩的历史。摘要保留对话的 _什么_ 并丢弃 _确切措辞_。

成本权衡：每个摘要步骤花费一次推理调用。对于繁忙会话，摘要开销是真实的但小（约总推理成本的 5–15%），它在主调用更便宜更快上回报许多倍。

#### 手段 2：积极分块检索上下文

当 RAG 检索文档添加到上下文时，文档通常比必要的长。一个检索到的文档可能 2K token，其中 300 token 相关。包含整个文档浪费预算并稀释焦点。

模式：**在检索时分块文档，检索块而非整个文档**。200–500 token 的块是标准的；叙事内容（法律意见、长篇散文）更大，技术参考材料（API 文档、代码）更小。

第二个模式：**重排序检索到的块**（第 11 章）并仅包含 top-N（通常 3–10）在上下文中。重排序模型对块与查询的相关性评分；最低分的块被丢弃。这锐化上下文。

#### 手段 3：分层上下文（高层然后下钻）

对于非常长的源材料，不将整个材料放入上下文，而是将工作流结构化为：
1. 模型看到源材料的 _目录_ 或 _摘要_（便宜，适合小预算）。
2. 模型决定哪些部分需要详细查看。
3. 模型仅看到所选部分的完整细节。

这是 _通过模型判断检索_ 的模式：模型本身是包含什么的相关性评分器。当源材料有自然分层结构（带章节的长 PDF、多文件代码库、文档站点）时效果良好。它需要 agent 循环（多次推理调用）或工具调用（模型调用 `read_section` 工具）。
