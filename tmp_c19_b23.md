每周几分钟）但其存在与否是有后果的。

本章教训：成本纪律不仅是工具和仪表板；它是将成本视为工程的文化。工具使纪律成为可能；文化使其持续。

**Q3.** _语义缓存驱逐策略_。工作流 #10（§18.4）引入语义缓存但对驱逐策略着墨不多。在生产中，语义缓存持续增长；无策略时，它最终包含过时条目（过时答案）、租户交叉污染风险，或简单地说太多条目使相似度搜索变慢。论证为何驱逐是语义缓存工程的难点，并提出一个平衡缓存命中率与过时性、隔离和搜索成本的策略。

**讨论：** 驱逐比填充更难有三个原因：

1. **过时性无法仅从缓存条目检测**。精确缓存可用 TTL 因为输入-输出映射是确定性的；如底层数据在 T 小时内未变，缓存输出仍有效。语义缓存返回数周前查询的"我们的退款政策是什么"的答案。政策是否仍最新？缓存不知道，除非底层真相源通知了它。

2. **命中率与过时性是可调权衡**。长 TTL 产生高命中率但更多过时答案；短 TTL 产生低命中率但始终新鲜答案。没有通用正确设置；取决于回答领域的波动性。

3. **租户隔离必须在缓存层强制**。来自两个不同租户的相同语义相似查询可能合理地有不同答案（他们的数据不同）。朴素语义缓存跨越此边界并将租户 A 的答案提供给租户 B 的查询，是隐私和正确性失败。

一个平衡的驱逐策略：

* **按内容类型分层 TTL**。缓存条目标记其反映的源数据。FAQ 式内容用长 TTL（24-48 小时）；策略文档派生内容用与文档最后修改日期绑定的 TTL；用户特定内容完全不缓存。TTL 是内容感知的，非统一的。

* **租户作用域命名空间**。每个租户有自己的语义缓存命名空间；租户 A 的查询仅匹配租户 A 的缓存条目。这在架构层面消除了隔离故障模式。代价是更多缓存条目（更少跨租户共享），作为正确性的代价接受。

* **带大小上限的 LRU 驱逐**。在每个命名空间内，当命名空间超过大小上限（比如 10 万条目）时驱逐最近最少使用的条目。这保持相似度搜索快速；向量搜索延迟随索引大小增长，所以无界缓存最终使系统变慢超过节省所值得。

* **失效钩子**。当源文档变更（策略更新、知识库编辑），从该文档派生的缓存条目被显式失效。这要求应用层追踪缓存答案的来源；工程成本是真实的但新鲜度保证也是真实的。
