Aiyana Carter 做了九年软件工程师。两个月前，她的头衔变了。现在全员大会的幻灯片上写的是 _Senior AI Engineer_。她的工作也变了，但并非如头衔所暗示的那样。

她不是在训练模型。她在 Beacon Health AI 的团队正在交付的临床笔记摘要系统调用的是 Anthropic 的 API。开发工具团队正在构建的 CodeReview Bot 调用的是 OpenAI 的 API。办公室里没有 GPU 集群。她日常接触的代码库里没有 PyTorch。她不会发表任何论文。

取而代之的是一张清单。清单上有 prompt。有 eval 分数。有一列是每次查询的成本。有一列是 p50 延迟（跨请求的中位响应时间）。还有一列写着 _故障模式_，其中的条目是这样的句子：_模型有时会编造一种不在医生口述记录中的药物，而我们交付的 eval 集没有捕获到这个问题_。这张清单就是规范。这张清单就是工程。这张清单就是工作。

这就是 2025 年交给十万名软件工程师的工作，2026 年还将再交给二十万人。他们中大多数人都在四处张望，就像 Aiyana 两个月前那样，然后问：_这是工程，还是别的什么？_

这是工程。这是一种新型工程，它有名字，有形态，而这个形态就是本书的主题。

### 1.1 分裂：研究不是工程

一个有用的起点是这个分裂。大约三十年来，用机器学习构建系统的人就是训练模型的人。你写模型，你训练模型，你部署模型，你看着模型在生产环境中失败，你回去训练一个新模型。这个管道是垂直的，覆盖整个管道的头衔是 _机器学习工程师_，有时是 _应用科学家_。研究与工程之间的分裂很小，因为你正在构建的工件从一篇论文开始，到前端的 API 调用结束。

大语言模型打破了这个模式。模型现在太大了，一个团队无法训练。一个有 700 亿参数的模型预训练成本高达数千万美元。预训练前沿模型的团队数量以打计（算上基础设施人员有时以百计），它们集中在少数几个实验室：Anthropic、OpenAI、Google DeepMind、Meta、Mistral，以及其他几家。他们发表论文。他们发布权重或 API。他们训练下一个模型。

然后是其他所有人。其他所有人拿这些实验室产出的模型来构建产品。产品层现在非常庞大：每家有软件在生产中的公司都被要求为其添加 LLM 功能。每个创业公司的融资演示文稿里都有 LLM 的身影。每个内部工具团队都被要求_在这个上面试试 AI_。

因此，分裂是这样的：**研究构建更好的模型；工程用它们构建产品。** 它们共享一套词汇。它们不共享一个工作日。
