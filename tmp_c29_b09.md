### J

**越狱**：设计来使模型绕过安全训练的 prompt 或输入。2026 年基线是假设越狱成功并在输出和工具层防御（第 23 章，§23.3）。

**JSON schema**：预期 JSON 形状的正式描述，用于驱动约束解码。prompt 与下游代码之间的契约（第 7 章，§7.2）。

**Judge 模型**：在 LLM-as-judge eval 中用于评分输出的 LLM。应不同于（通常更强）被评模型（第 5 章，§5.5）。

**Judge 轮换**：定期更换 judge 模型以避免同族偏差并检测评分漂移（第 5 章，§5.6）。

### K

**Kappa**：Cohen's kappa。测量按机会调整的评分者间一致性。用于验证人工评分者（或 LLM judge）在评分标准上一致（第 4 章，§4.5）。

**KG-RAG**：知识图谱 RAG。用实体和关系上结构化图的遍历增强的检索。查询需多跳推理时胜出（第 11 章，§11.3）。

**知识图谱 RAG**：见 _KG-RAG_（第 11 章，§11.3）。

**Kong AI Gateway**：带 AI 特定功能（模型路由、prompt 模板、语义缓存）的企业 API 网关。已在 Kong 上的组织的强选择（第 17 章，§17.3）。

**Krippendorff's alpha**：跨评分者数量和量表类型泛化的评分者间一致系数。当评分者或条目变化时优于 kappa（第 4 章，§4.5）。

**KV 缓存**：注意力计算中先前 token 的存储键/值张量。跨解码步骤复用；使流式便宜的原因（第 2 章，§2.5）。

**密钥管理**：存储、轮换和限定提供商 API 密钥。AI 系统中凭证泄漏的最常见来源；在网关集中化（第 17 章，§17.6）。
