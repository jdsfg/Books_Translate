#### 概念检查

**Q1.** 一位队友问："我们已经有 pytest 了，为什么还需要像 `pytest-evals` 这样的单独 eval 框架？"从以下方面回答他们：(a) `pytest-evals` 在原始 pytest 之外添加了什么；(b) 如果你试图仅用原始 pytest 做 eval 会缺少什么。

提示

Pytest 用于确定性函数的二元通过/失败正确性。eval 需要什么 pytest 原生不提供的东西？

**答案：** `pytest-evals` 在原始 pytest 之外添加的：

* **跨案例聚合评分**：一份汇总 golden set 上分数的单一 eval 报告，而非仅每案例通过/失败。
* **每案例结构化分数**：每个案例可以返回维度分数字典而非仅 True/False。
* **对基线的比较**：pytest 原生不支持 _对前一次运行的分数 delta_；`pytest-evals` 添加了这个。
* **golden set 管理**：从版本化数据集加载案例，带元数据、分层感知。
* **结果持久化和历史**：将分数写入存储（如 §5.4 的 SQLite 存储）用于后续趋势分析。

仅用原始 pytest 缺少的：你必须自己构建以上所有。很多团队已经这样做了。关键在于这是一个已解决的问题；你不需要重建它。

一个更微妙的点：pytest 的二元通过/失败模型与 eval 的概率分数模型冲突。一个断言 `score >= 8.0` 的 pytest 测试要么通过要么失败；你无法在 CI 输出中轻松看到分数本身，无法轻松跨运行比较分数，无法轻松检测分数随时间的 _趋势_。`pytest-evals` 通过将每个案例的结构化分数视为制品、断言作为其上的薄层来桥接这个。

**Q2.** 比较 `ragas` 和 `inspect-ai`：在什么条件下各自是正确工具？指出一个两者都不是正确工具的场景并解释原因。

**答案：** `ragas` 在你的 eval 是 **RAG 形态** 时正确：你有一个检索增强系统，你关心四个规范指标（忠实度、答案相关性、上下文精确率、上下文召回率），你想要一个开箱编码标准 RAG eval 纪律的库。Strata Research 的法律科技 RAG 平台完美适合这个。

`inspect-ai` 在你的 eval 是 **安全形态或审计形态** 时正确：你在评估能力危险行为、prompt 注入稳健性、拒绝模式合规，或你需要完整审计跟踪以符合监管合规。英国政府工作、国防工作和 Beacon 的合规约束临床管道 eval 适合这个。

两者都不正确的场景：**agent 评估，其中任务是多轮的且成功标准是任务完成而非文本质量**。`ragas`（RAG 特定）和 `inspect-ai`（安全特定）都不是为此构建的；你会使用 `pytest-evals` 或 `langsmith`（如果你的 agent 基于 LangChain/LangGraph）或构建自定义测试框架。agent eval 需要沙箱执行、多轮重放和任务成功评估：安全和 RAG 专家不随附的工具。

**Q3.** 在什么条件下 _自建_ 优于 _购买_ eval 基础设施？指出三个不同条件及各自的反驳。

**答案：** 自建优于购买的三个条件：
