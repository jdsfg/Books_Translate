具体对此事故：添加回归测试模拟长轨迹并断言成本断路器在确切配置阈值处触发。先在测试环境中重现事故；验证修复前测试失败；验证修复后通过。

**修复 2：强制硬迭代上限与独立预算跟踪。**

迭代上限必须：

* 简单整数计数器（非可能被误算的复杂表达式）。
* 在不可绕过代码路径中每次迭代前检查。
* 设为校准到用例的值（典型 agent 15-20；如 Q5 的研究 agent 20）。

对于此客户服务轨迹，15 的迭代上限将在第 15 次迭代停止循环，即使有成本计数器 bug 也将成本限制在约 0.75 美元。

具体地：生产中，迭代上限是 _承重_ 界因为最简单正确实现。把这个做对；其他是纵深防御。

**修复 3：添加语义相同工具断路器。**

严格相同参数检查错过了此循环因为参数略有变化。修复：检测 _语义相似_ 的工具调用。实现选项：

* 哈希 (tool_name, normalized_args) 元组，其中规范化去除微小变化（例如尾随空格、等价字段顺序）。3 次命中 → 对 agent 软警告；5 次命中 → 停止。
* 基于 LLM：每 5 次迭代，小 LLM 调用检查轨迹并回答 _agent 是否卡在循环中？_ 如是，以"卡住循环"状态停止。
* 基于模式：检测交替工具模式（A→B→A→B→A→B），相同工具名不论参数。5 次交替迭代 → 警告；8 次 → 停止。

对于事故的具体模式（lookup_order 和 check_shipping_status 交替），交替工具检测将捕获它。

**事故中涌现的额外纪律**：

* **每 agent 日支出上限带寻呼**：独立的更高级 kill switch，当 agent 跨所有轨迹的累积支出超过日预算时停止 agent。团队有此（它最终终止了轨迹），但级别太高未能防止事故。降低到触发寻呼而非仅终止的级别。
* **事故后 eval 切片**：构建包含失败模式的对抗 eval（诱惑模型进入循环的 agent 任务）。eval 必须验证全部四界启动且轨迹干净停止。
* **界覆盖审计**：审查每个生产 agent 的界实现是否有同类缺口。_界已配置但实现有 bug_ 的模式常跨服务重复直到团队显式审计。

交叉引用：此事故说明为什么 §14.4 强调 _四界一起_。任一界的实现可能有 bug；其他提供纵深防御。仅依赖成本断路器的团队在该界有 bug 时被咬；四界都正确工作的团队将在成本螺旋发生前于迭代上限停止。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？
