### 2.5 生成循环：随机性从何而来

回忆 §2.1 的循环。模型返回下一个 token 的概率分布；你从分布中采样；你追加采样结果；你再来一次。采样步骤是随机性的来源。给定固定输入，分布是确定性的（模某些实现中 GPU 级别的少量非确定性）；选择 _发射哪个 token_ 是随机的，由分布加权。

采样步骤由 API 暴露的参数控制。这些是工程师转动的杠杆，第 3 章将详细练习它们。现在，让我们把心智模型搞对。

#### 定义：Temperature

一个标量参数（通常在 [0.0, 2.0]）控制采样前下一个 token 分布的锐度。在 temperature 0 时，模型在每一步确定性地选择最高概率的 token（这叫做 _greedy decoding_）。在 temperature 1 时，模型从原始分布中采样。在 temperature > 1 时，分布被软化，低概率 token 变得更有可能。在 temperature < 1 但 > 0 时，分布被锐化，高概率 token 变得更有可能。

用大白话说：temperature 是 _创造力旋钮_。低 temperature 用于你想要一致、可靠、保守输出的任务（结构化提取、生产中的代码生成、分类）。高 temperature 用于你想要多样性的任务（头脑风暴、创意写作）。大多数生产任务的默认值在 0.0–0.3 范围；许多生产系统以字面上的 0 运行以获得可复现性。

一个在生产中会咬人的微妙点：即使在 temperature 0，你也可能在实践中看到非确定性。这是因为用于计算分布的 GPU 级别操作在浮点运算下不是完全结合的，操作调度方式的微小差异（例如跨实例重启）可能改变当两个候选非常接近时哪个 token 具有最高概率。方差很小，但它存在。正确的思考方式：temperature 0 给你 _近确定性_ 行为，而非严格确定性，你不应依赖跨调用的位精确可复现性。

#### 定义：Top-p（nucleus sampling）

一个参数（在 [0.0, 1.0]）限制采样到累积概率至少为 p 的最小 token 集合。Top-p 为 0.9 意味着：在每一步，按概率对 token 排序，取概率总和至少为 0.9 的最小集合，重新归一化，从该集合中采样。

用大白话说：top-p 是 _截断_ 旋钮。它裁剪低概率 token 的长尾，防止模型仅仅因为分布在那里有非零质量就发射一个极不可能的 token。默认值（top-p ~0.9 到 1.0）对大多数用途都合适；降低它（例如 0.5）使输出更保守，但不像 temperature 0 那样使它们确定性。
