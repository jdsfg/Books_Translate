#### 深入思考

**Q1.** _正则解析的终结_。论证到 2030 年，散文 + 正则解析模式将被视为 AI 工程中的反模式，与经典 Web 开发中的 `eval()` 用户输入相当。然后论证相反：某些特定用例将使散文+正则模式存活。以每个论证各自正确的特定用例作结。

**讨论：** **终结论证**：通过约束解码的结构化输出现在可靠且人体工程学良好。成本差异可忽略；维护差异大（正则在每次 prompt 风格变化时断）。随着工具成熟和工程师获得结构化优先设计的反射，散文然后正则管道成为遗留模式，可识别为 2026 年代码库有但 2030 年代码库替换的那种东西。_调用 LLM、正则响应_ 的模式将成为新人在第一个月被告诉重构的那种代码。

**未完全终结论证**：某些用例需要自由格式散文输出作为主要制品（创意写作、起草通信、生成面向用户的散文）。对于这些，_结构化输出_ 是错误框架——散文 _就是_ 输出。需要从散文中提取一个特定字段的团队（例如从生成的文章中提取建议标题）可能用小正则或对散文的小结构化输出调用作为下游步骤。散文加抽取模式是持久的。

**以用例作结**：_散文然后正则_ 管道对 _信息抽取_ 任务（从已知输入中提取特定数据）是反模式。到 2030 年，这应总是结构化的。_散文作为输出加二次抽取_ 模式对 _生成_ 任务（散文是交付物）存活。区别是：散文是产品（那散文没问题），还是散文只是数据的传递机制（那结构化是正确的）？混淆两者的团队发布反模式；区分它们的团队发布可维护代码。

**Q2.** _schema 作为 API 契约_。本章论证 schema 应暴露模型不确定性而非隐藏它。扩展这个原则：论证 schema 不仅是内部数据定义，也是塑造 _下游系统_ 如何处理失败的 API 契约。指出 schema 设计中两个对远超 schema 本身的下游有后果的设计选择。

**讨论：** Schema 不仅是对模型输出的约束；它是下游系统可以期望什么的契约，schema 中的设计选择传播到下面每一层。

设计选择 1：**是否有显式错误变体**。只有 `SuccessSummary` 的 schema 强制每个模型输出进入成功形态，这迫使下游代码进入 _异常_ 错误路径（try/except、哨兵值、重试直到成功）。有判别 `ErrorSummary` 的 schema 使错误成为 API 的 _一等_ 部分；下游代码在变体上分支；日志、监控、重试策略都建立在类型化错误变体上而非异常处理。这是系统化处理失败的管道和将失败视为偏差的管道之间的区别。
