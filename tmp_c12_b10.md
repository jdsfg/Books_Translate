#### 概念检查

**Q1.** 为什么重排序器是高级 RAG 中最高 ROI 的单一举措？用双编码器和交叉编码器之间的底层架构差异解释。

**答案：** 向量搜索中使用的嵌入是双编码器：查询和块分别编码为独立向量；相似度是向量间余弦。双编码器无法直接捕获查询-块交互：每侧独立编码。这使向量搜索快（预计算块向量；常数时间相似度）但粗糙。

重排序器是交叉编码器：查询和块 _一起_ 输入一个 transformer，联合关注两者。模型可以直接权衡查询中的特定词如何与块中的特定词相关。这对相关性更准确：模型捕获双编码器遗漏的交互；但每对慢得多（你必须对每个查询-块对运行模型；无法预计算）。

架构差异解释了为什么重排序器大幅提升质量：它添加了嵌入步骤无法提供的注意力层。向量搜索快而广；重排序器慢而锐。两者一起形成"宽检索、窄重排序"模式，最大化 recall 和 precision。重排序器只在向量搜索的 top-K（通常 20–50）上运行，所以较慢的每对成本可管理。

**Q2.** 比较 HyDE 和查询扩展作为查询重写模式。各自何时是正确工具？

**答案：** 查询扩展向查询添加同义词和相关术语。扩展后的查询然后被嵌入并用于检索。最适合：使用与语料库不同语言的查询；罕见术语 vs 同义词不匹配的查询（用户说"汽车"但文档说"机动车"）。

HyDE（假设文档嵌入）生成查询的假设答案，然后嵌入假设答案。假设比查询更类似于检索目标（它们也是答案/段落）。最适合：用户问题在词法上不匹配答案的问题型查询；查询和答案有不同表面形式的技术或正式领域的查询。

各自何时正确：

* **查询扩展**：词汇不匹配问题。用户日常语言不同于文档技术语言的已建立行话领域。
* **HyDE**：问题-答案不匹配。查询是短问题而答案是不同框架的长段落的领域。

两者可组合（先扩展再 HyDE；两者馈入检索），但组合成本是两次额外 LLM 调用。评估哪个在你的 eval 上给出最大提升；通常一个足够。

**Q3.** 什么是 _自查询检索_ 以及为什么对有丰富元数据的语料库必不可少？

**答案：** 自查询检索使用 LLM 将用户的自然语言查询解析为结构化搜索参数：语义查询加元数据过滤（管辖权、日期、类型等）。结构化参数然后应用于检索：过滤缩小语料库；语义查询驱动向量搜索；结果是精确检索。
