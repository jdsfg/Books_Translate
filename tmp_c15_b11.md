### 章节小结

agent 是 LLM + 工具 + 循环 + 记忆。到 2026 年，agent 是大多数 AI 工程师花最多时间的架构模式；它们也是最可能灾难性失败的架构。五个故障模式——无限循环、幻觉工具调用、丢失目标、上下文溢出、成本超支——几乎涵盖所有生产 agent 失败。

核心工程美德是 _有界性_：防止失控行为的结构约束，独立于 LLM 的推理质量。四界模式（最大迭代、每调用 token 上限、总轨迹预算、每工具断路器）合在一起将无界循环转为有优雅故障模式的有界循环。

agent 记忆有三层：工作记忆（轨迹内暂存区）、情景记忆（会话内跨轨迹）、长期记忆（跨会话）。每层有实现模式：工作记忆的摘要；情景的结构化决策日志和运行摘要；长期的向量记忆和结构化事实。长轨迹中的工作记忆问题通过周期性轨迹内摘要解决（第 8 章 §8.4 的应用）。

绑定工作流 #7（§14.6）是有界 agent 设计文档：一页制品指定 agent 的界、记忆架构、策略门控、eval 套件和回滚程序。文档在发布前审查并随 agent 演化更新；它是团队对安全属性的显式承诺。

线程 B 失败 5（52K 美元隔夜 agent，2024）和 6（多 agent 递归成本超支，2026）是警示锚点。两个故障模式都是有界工程失败；两者都可通过本章模式防止（失败 5 通过每轨迹界；失败 6 通过第 15 章构建的系统级界）。

Helios 的 v1 → v3 路径（§14.8）是工作示例：18 个月的纪律工作；失败率从约 3% 到约 0.2%；成本方差大幅收紧；有界工程的成本通过避免异常值轨迹多次回收。第 13 章的工具使用是构建块；第 15 章将界扩展到系统级多 agent 协调；第 17–18 章接续 agent 层成本纪律和优化；第 19 章将可观测性带到轨迹级追踪；第 21 章在 agent 层监控漂移。

要内化的模式极其简单：有界设计是区分生产级 agent 和隔夜账单 52K 美元的那种的关键。工程界是便宜的。跳过它们没有上限。

### 14.X 练习题
