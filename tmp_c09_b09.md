### 章节小结

从 _prompt 工程_ 到 _上下文工程_ 的转变是 2024–2026 年领域的转向，反映了工程表面不再只是系统 prompt 而是上下文窗口的完整分配：系统 prompt、工具定义、对话历史、检索内容、当前消息、输出预算。上下文窗口是有限资源；工程它就是同时工程成本、延迟和质量。

主要手段是：摘要较早对话历史；积极分块检索内容；用高层摘要结构分层上下文再下钻；用工具即时加载上下文。中间丢失是真实的但被 2026 年模型部分缓解；放置在边际上仍然重要。

绑定工作流 #4（§8.6）是长上下文 vs RAG 决策树：< 500K 语料库 → 长上下文；500K–10M → 判断决策（通常混合）；> 10M → RAG。数字标注日期；结构持久。Beacon Health AI 的升级模式（默认便宜上下文；当模型信号不确定时路由到昂贵上下文）跨系统可移植。

延迟权衡（§8.8）尖锐：200K 输入的长上下文被 LLM 首 token 时间主导（约 4 秒）；RAG 通过减少输入将其削减到一秒以下。对于交互式产品，这通常迫使 RAG 即使长上下文可以适合；对于批处理，权衡放松。

上下文工程建立在第 6 章的 prompt 级工作和第 7 章的结构化输出（预算中的一项）之上。它为第 9–12 章（当长上下文不是答案时的 RAG）、第 13 章（工具使用作为即时上下文加载）和第 18 章（prompt 缓存，使上下文稳定部分便宜）打开大门。

将上下文窗口视为它的本质：**你在系统 prompt、工具定义、历史、检索和输出间分配的有限预算。** 跟踪分配。优化动态部分；缓存稳定部分；仅当 eval 信号证明合理时升级到昂贵上下文。

### 8.X 练习题
