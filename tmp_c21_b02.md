### 20.2 供应商专业选项

除主要 LLM 提供商的语音产品外，三个专业供应商在 2026 年值得命名：

**ElevenLabs Conversational AI**：构建在 ElevenLabs 高质量 TTS 之上。平台作为集成包处理 STT、LLM（通过你的或他们的提供商）和 TTS。优势：语音质量（ElevenLabs 语音明显比替代方案更自然）。适用：语音质量差异化的面向客户语音。

**Cartesia**：新兴 2025 年时代供应商，极低延迟语音模型。其 Sonic 系列模型可实现亚 100ms TTFT。适用：延迟关键语音应用，预算比行业一般规范更紧。

**Deepgram**：主要是 STT 供应商；2026 年扩展到完整对话 AI。在带口音语音、技术词汇、嘈杂环境上强 STT 准确率。适用：STT 重度用例（转录、归档、混合环境语音）。

2026 年选择：**供应商根据差异化你产品的维度选择**。Helios 选 OpenAI Realtime 作为主要（通用延迟和能力）；保留 Deepgram 做 STT 归档（转录可搜索性更好准确率）；探索 ElevenLabs 做高级层客户界面的语音质量。

### 20.3 延迟预算问题

语音 AI 的核心工程现实：**延迟预算很紧**。用户以比文本高得多的分辨率感知语音对话延迟。聊天中 2 秒延迟可接受；语音对话中 2 秒延迟感觉像连接中断。

生产语音预算：

* **首音频时间**（TTFT，语音等价于 TTT）：目标亚 500ms；竞争力在亚 300ms。用户大脑将更长延迟解释为"agent 没听到我"或"agent 坏了"。
* **总响应持续时间**：取决于响应长度；响应应在 TTFT 预算内 _开始_，然后流式。
* **轮次结束检测**：检测用户何时停止说话。目标：200-400ms。太激进（agent 打断用户）；太慢（agent 感觉无响应）。

对比基于文本的 AI：文本 TTFT 预算通常 1-3 秒；总响应可耗时 5-15 秒。语音紧一个数量级。

含义：**标准文本 AI 架构不能支持语音**。典型 agent 循环（第 14 章）带多次 LLM 调用和工具调用耗时 3-5 秒：对文本可以，对语音致命。语音架构需要不同。
