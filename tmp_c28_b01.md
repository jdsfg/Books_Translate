两部分。第一部分用散文描述十个参考架构模式；本书不附渲染图，因此描述写成仅从文本即可构建——每个框、每条箭头、每个队列都有命名。第二部分是 2026 年格局中出现但刻意不属于第 15 章正文推荐的 agent 框架目录，正文保留 LangGraph、AutoGen、Pydantic AI 和 Anthropic MCP。

模式是持久的。框架列表会更替。相应阅读。

* * *


### 第一部分 — 十个参考架构模式

每个模式遵循相同形状：它解决的问题、散文拓扑、何时用、何时不用、以及本书哪些章节深入论述该模式。

#### 模式 1：单提供商生产聊天界面

**解决什么问题。** 一个要发布的 AI 功能——现有产品内的聊天界面，或小型独立工具——尚不需要多提供商路由或独立可观测性栈。地板模式：多数团队发布的第一个 AI 功能，且在 2026 年悄悄驱动大多数生产中的 LLM 功能。

**拓扑。** 客户端通过 WebSocket 或 SSE 流连接到产品现有 API 网关（Envoy、Kong 或云供应商）。网关通过现有会话 cookie 或 JWT 认证，应用按租户速率限制（Redis 令牌桶），并转发到与其他后端服务一起部署的薄 AI 服务。AI 服务组合 prompt——来自版本化配置文件的系统 prompt、来自请求的用户消息、可选来自应用数据库的历史——并调用一个提供商的流端点（Anthropic、OpenAI、Gemini）。Token 通过 AI 服务流回网关到客户端。每个请求记录到现有可观测性栈（Datadog、Honeycomb 或自托管 OpenTelemetry），带团队后来需要的三个字段：提供商、模型、总 token 数。成本标记在日志摄取时通过与已发布价格表连接完成。

**何时用。** 第一个 AI 功能。单团队。单提供商六个月可接受。流量低于每月几百万请求。PHI/PII 脱敏由现有数据层处理或不适用。"只调 API"在此正确。

**何时不用。** 多个 AI 功能竞争同一预算信封（转向模式 2 或 10）。受监管数据在 prompt 中且无脱敏层（模式 8）。eval-as-CI 未就位且界面面向大规模用户——未来模型替换将静默回归质量（模式 7）。

**交叉引用。** 第 2-4 章（基础架构），第 17 章（何时超越它），第 21 章（首次部署）。
