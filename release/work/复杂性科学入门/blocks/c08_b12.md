#### 应用题

**Q4.** 从头实现 Barabási-Albert 模型（不使用 NetworkX 的内置函数）。构建 n=5000、m=3 的网络。在双对数轴上绘制度分布并验证 P(k) ~ k^{-3} 标度。

提示

在每一步，你需要以与度成正比的概率选择一个现有顶点。一种常见实现：维护一个"边端点"列表，其中每个顶点出现次数等于其度，然后从列表中均匀随机选取一个元素。

**答案。** 示例代码：

    import numpy as np
    import matplotlib.pyplot as plt

    n = 5000; m = 3
    # Initialize: small fully-connected starter graph
    degrees = [m] * (m + 1)  # m+1 nodes, each connected to all others
    endpoints = []
    for i in range(m + 1):
        endpoints.extend([i] * m)

    for i in range(m + 1, n):
        # Choose m existing vertices proportional to degree
        targets = set()
        while len(targets) < m:
            targets.add(endpoints[np.random.randint(len(endpoints))])
        for t in targets:
            degrees[t] += 1
            endpoints.append(t)
        degrees.append(m)
        endpoints.extend([i] * m)

    # Plot degree distribution on log-log axes
    bins = np.logspace(0, np.log10(max(degrees)), 30)
    hist, edges = np.histogram(degrees, bins=bins, density=True)
    centers = 0.5 * (edges[:-1] + edges[1:])
    mask = hist > 0
    plt.loglog(centers[mask], hist[mask], 'o')
    # Reference line k^-3
    ks = np.logspace(0.5, 2.5, 100)
    plt.loglog(ks, ks**(-3) * hist[mask][0] / centers[mask][0]**(-3), '--', label='k^-3')
    plt.xlabel('degree k'); plt.ylabel('P(k)'); plt.legend()
    plt.show()


经验度分布应在度的中间范围近似遵循 k^{-3} 参考线。偏差出现在低度端（模型有最小度 m）和高度端（有限尺寸效应截断尾部）。中间范围的吻合是稳健的，确认了解析预测。

使用 Clauset、Shalizi 和 Newman 的最大似然估计量进行更严格的拟合，对于 n=5000 的典型实现给出 \hat{γ} ≈ 2.95 到 3.05，接近理论值 3。

**Q5.** 使用公开的 Mathematical Reviews 数据计算三位随机选择的数学家的 Erdős 数。（或者，如果不可用，使用好莱坞演员合作网络并计算三位随机选择演员的"Bacon 数"。）评论你找到的值。

提示

开放数据：Erdős 数列在 Erdős Number Project https://oakland.edu/enp/。Bacon 数在 Oracle of Bacon https://oracleofbacon.org/。

**答案。** Bacon 数的示例答案（因为更容易查找）。从奥斯卡获奖者列表中随机选择的三位演员：Frances McDormand、Mahershala Ali、Anthony Hopkins。