### 17.8 Helios 案例：100 万美元/月推理，每租户成本治理

Helios 路由器层的完整故事：

**规模**：每月 1000 万+ 工单；约 500 万涉及 LLM 调用的轨迹；每月约 3000 万总 LLM 调用包括 agent 中间步骤。100 万美元+/月推理支出。

**架构**：LiteLLM 自托管，作为服务部署在 Kubernetes 上。约 5 个路由器实例在负载均衡器后；每个实例处理约 1K 调用/秒。后端用 Postgres 做租户预算追踪和密钥管理。

**租户管理**：200+ 企业客户，每个有自己的月度推理预算（合同协商条款：每租户 1K 到 250K 美元/月）。通过预算中间件（§17.6）强制每租户预算。

**路由逻辑**：

* 小型分类器（Haiku 调用）在每个工单开始时运行以识别层级（简单/标准/困难）。
* agent 在该轨迹的所有调用中使用分配的层级。
* 如 agent 在轨迹中途升级（例如简单层分类器判断错误；工单实际是困难的），路由器将轨迹升级到更高层。
* 对不论工单复杂度都签约了高级层的租户存在每租户覆盖。

**故障处理**：

* 2024 年：Anthropic 4 小时部分宕机。路由器将受影响调用故障转移到 OpenAI；客户看到正常服务。成本：窗口期间额外约 30K 美元 OpenAI 调用；保留收入：估计 200K 美元+ 本会失败的工单。
* 2025 年：OpenAI 速率限制问题影响 Helios 部分流量 90 分钟。路由器重定向到 Anthropic 和 Google。服务连续性保持；受影响调用有轻微延迟峰值。
* 2025 年：Google Gemini Flash 有质量回归影响 Helios 生产 eval 分数。路由器在数小时内重新配置以跳过 Flash；服务质量恢复。

**成本故事**：从 §17.4 的讨论，路由层相比始终用 Sonnet 基线节省约 65%。在 3000 万调用/月规模，这是每天数万美元的推理成本节省。路由器自身成本（运行 LiteLLM 服务、追踪用 Postgres、分类器模型）在每月低五位数（节省的一小部分）。

在 Helios 的规模路由器物超所值：架构、路由逻辑和每租户治理共同使商业模式成为可能。没有它，单位经济学会崩溃。
