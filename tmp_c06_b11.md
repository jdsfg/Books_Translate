这个工作流逐步做什么：

1. **在 PR 或 main push 时触发**，当相关路径变更时。跳过不相关的 PR 节省 CI 分钟。
2. **检出两个提交**：PR 的 HEAD 和合并基线。两次检出是 _delta 比较_ 的基础。
3. **运行 eval 套件两次**（一次候选，一次基线）到分开的结果文件。每次运行将结果记录到 SQLite 存储并附适当的 `git_sha` 和 `prompt_version`。
4. **比较运行**使用自定义脚本（`scripts/compare_evals.py`）应用阈值：摘要分数下降超过 0.1，或任何单一维度降至 2.5 以下，比较失败。
5. **在 PR 上发布比较评论**，带分数 delta。团队在合并前看到 eval 结果。
6. **上传 eval 结果作为制品**，以便审查者可以拉取并检查特定失败案例。

这个工作流故意不做的事：

* **它不在每个 PR 上运行**。如果 PR 不触及 prompt、源代码或 golden set，eval 跳过。这是刻意的成本 trade-off：eval 运行既花 CI 分钟又花 LLM API token。
* **它不因特定维度改善而阻止**。门禁是 _无回归_。改善在评论中注明但不要求。
* **它不要求人工审查每份报告**。默认流程是：如果 eval 通过门禁，PR 可以无需进一步 eval 讨论就合并。团队将注意力集中在未通过门禁的 PR 上。

每次运行成本：在 Beacon 的 CodeReview Bot eval（200 案例、约 600 token 输入、约 150 token 输出，加 LLM 作为评判者调用），每次运行约 1.50 美元。PR 大约运行两次（候选 + 基线），所以每个有 eval 相关变更的 PR 约 3 美元。以每月 100 个这样的 PR 计算，月度 eval API 成本约 300 美元。

### 5.7 eval 套件随时间扩展

一个值得命名的微妙模式。一个工作的 eval 套件会 _增长_。每次生产事件添加案例。每次模型升级练习新维度。每个新功能引入要测量的新行为。

健康增长模式是 **缓慢、刻意、带过时案例退休**。Beacon CodeReview Bot 的 eval 在发布时（此复合时间线的 2024 年 Q1）从 50 个案例开始；到 Q3 达到 200 个案例（几次事件添加案例后）；到 2026 年 Q1 达到 280 个案例；当团队退休了一些不再代表当前生产流量的案例时被修剪回 240 个。总维护成本：每季度约 4 个工程师日。

不健康增长模式是 **持续膨胀的案例袋无退休**。eval 套件数年膨胀到数千案例；案例不平衡（偏重于团队最近注意到的）；运行时间变得痛苦；套件因为太慢而不再在每个 PR 上运行；纪律崩溃。缓解是 _季度审查_：退休不再有价值的案例（代表当前流量或测试值得防范的特定故障模式），重新平衡分层，如果 LLM 评判者漂移则重新校准。

纪律小但真实。将 eval 套件视为活制品，而非一次性交付物。
