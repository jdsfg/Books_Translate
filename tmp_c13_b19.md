3. **授权委托书文档的专用工作器。** 如这些文档的量证明合理，在上游添加文档类型分类器；分类为授权委托书的文档通过为印章签名调优的专用提取 prompt 路由。以工程复杂性换取专用上的准确性。
4. **定向 eval 切片。** 构建 30 文档的授权委托书表格 eval 切片，带标注签名位置。验证每个修复提升切片的 recall。切片成为永久回归预防测试。

验证标准签名上无退化：在每次变更前后运行现有手写签名 eval 切片。如更广泛的 prompt 或少样本示例退化标准切片，撤销变更或拆分 prompt 路径。

通用教训：VLM 有长尾分布；生产多模态 RAG 必须显式为长尾仪表化。prompt 级修复对已知长尾案例通常足够；eval 纪律在它们成为客户投诉前捕获。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我们基于 VLM 的签名提取对手写签名工作良好但对印章签名失败。诊断并提出不退化标准性能的修复。_ 你验证 (a) 诊断显式命名分布外机制（LLM 可能诊断为"模型不够聪明"；OOD 框架更具可操作性），以及 (b) 修复包括 eval 切片保障（没有前后 eval 比较，prompt 丰富可能静默退化其他切片）。
