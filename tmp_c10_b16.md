**Q5.** _从追踪调试 RAG 质量回归_。你的生产 RAG 监控触发告警：回答通过率在过去 72 小时从 0.81 降到 0.68。你拉取 30 个最近失败案例的追踪。每个追踪包含：用户查询、检索结果（top-10 块及其分数）、LLM 的答案、以及人工标注的正确性判定。走过诊断过程。你首先看追踪的哪些特征、它们提示什么假设、你如何确认每个假设？

提示

§9.5（检索 vs 回答 eval）、§9.9（检索内容审计）、以及上面概念检查 Q3 的故障定位框架。追踪包含检索证据和生成证据；隔离哪层回归。

**答案：**

诊断手段：**在提出修复前将回归定位到层。** 三层可能回归；阅读追踪告诉你哪层。

**步骤 1：在失败追踪上计算仅检索指标。**

对 30 个失败追踪中的每个，检查 _正确_ 块是否在 top-10 检索中（这需要每个查询的人工标注 golden 块，或与人工编写的正确答案重叠等代理信号）。如果失败集上的检索 recall 远低于总体基线（比如 0.55 vs 0.92），回归在检索。如果失败集上检索 recall 正常，回归在生成或 prompt。

**步骤 2：可视化检查检索结果。**

对于检索确实获取了相关块的失败追踪，阅读块。要寻找的模式：
* _块是陈旧的_：检索内容引用旧策略/旧代码/过时日期。提示索引管道延迟或部分索引更新失败。
* _块来自意外来源_：论坛帖子、博客评论、语料库外内容。提示来源允许列表漂移或语料库摄取 bug。
* _块技术上正确但排名顺序意外_：正确块在排名 8 而非排名 2，LLM 更看重前面的块。提示嵌入模型行为变化或重排序器退化。

**步骤 3：检查 LLM 答案相对于块。**

对于检索看起来正常的追踪，检查 LLM 的答案：

* _答案忽略检索块_：答案来自先验知识，非来自块。提示 prompt 回归（引用强制减弱）或模型行为偏移（provider 部署了新模型版本）。
* _答案引用块但曲解_：块说 X；答案说 Y。提示 prompt 问题（指令允许过于自由地改述）或模型合成退化。
* _答案为声明引用了错误块_：模型选择了不支持声明的引用。提示 prompt 层引用接地下回归。

**步骤 4：识别时间模式。**

绘制失败追踪的时间戳。三种模式有诊断性：
