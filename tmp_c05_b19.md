通用测试：如果功能有（合规影响、无限期生产生命周期、团队可以表达的可识别故障模式）中的任何一个，收益递减框架不适用。默认是 _写 eval_；例外是真实的但比团队通常想要的更窄。

**Q5.** §4.4 区分三种参考标准：_gold standard_（唯一正确答案）、_参考答案_（带显式量表的代表性好答案）和 _仅量表_（无参考答案；仅量表）。对于以下每个任务，命名适当的参考标准类型并简要论证。(a) 从临床笔记中提取患者药物列表为结构化 JSON；(b) 将临床笔记摘要为一段入院摘要；(c) 起草退款请求邮件的客服回复；(d) 将客户消息路由到正确的支持队列。

提示

Gold standard 在有一个正确答案时有效。参考带量表在有好的代表性答案且量表解释原因时有效。仅量表在许多答案都好且展示一个会过度锚定标注者时有效。

**答案：**

(a) **药物列表提取：gold standard**。正确答案是笔记中命名的药物字面列表，按指定 schema 格式化。有一个正确答案（模排序，schema 固定了）。eval 检查对 gold 列表的每药物精确率和召回率。参考带量表是过度杀；仅量表会丢失精确信号。

(b) **入院摘要：参考带量表**。许多一段摘要都是可接受的；参考展示一个代表性好答案（仔细的临床医生会写的）；量表评分维度（正确性、完整性、语气、长度、无关细节的省略）。Gold standard 不行因为没有唯一正确段落。仅量表可行但标注者受益于在评分前看到 _好_ 的样子的锚定。

(c) **客服退款回复草稿：仅量表**。好的回复可以有多种形式（先道歉、先解释政策、先提议替代等），展示参考答案会过度锚定标注者于形式而非底层质量。量表应覆盖：针对客户具体请求、应用正确政策、语气适合客户情绪状态、不包含超出 agent 权限的承诺。参考带量表会推动标注者朝参考的形式；仅量表避免这个。

(d) **路由：gold standard**。正确队列是唯一的（假设良好定义的队列分类法）；eval 是简单的分类准确率。要避免的陷阱：队列分类法通常有合理模糊的案例（一条关于账单错误同时提及产品缺陷的消息可以进账单或产品队列）。通过 (i) 从 gold set 中排除，(ii) 用两个可接受队列标注并将任一视为正确，或 (iii) 添加 _队列理由_ 字段到 gold standard 并部分评分来处理。

通用教训：参考标准类型是 _存在多少好答案_ 和 _参考形式会在多大程度上扭曲标注者判断_ 的函数。选择不是审美的；它决定你的 eval 测量你意图的还是测量对一种特定答案形态的符合度。
