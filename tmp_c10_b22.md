**2030 年图景。** 七步循环可能是 _五步_ 循环：分块、检索（融合）、构建 prompt、生成、后处理。嵌入-搜索-重排序融合为 _检索_ 是最高确信变化。其他融合可能以较低概率发生（例如生成 + 后处理如果模型处理自己的引用渲染，这似乎更远）。

**更深的教训。** 此领域的抽象在 3 到 5 年时间尺度上是 _过渡性的_。词汇比实现存活更久；2030 年的工程师仍将七步循环识别为 _概念_ 模型即使它已融合为更少工程步骤。今天教七步循环（它映射到真实组件）；预期组件随时间整合而概念模型不变错误。
