### 20.9 Triangle Trade-off 侧栏：语音架构选择（延迟分解格式）

单次语音交互的延迟分解：

**架构 A：STT → 文本 LLM → TTS**（传统 2023 流水线）

* 音频输入（流式）：0ms（持续）
* STT 处理：400ms（等待轮次结束 + 转录）
* 文本 LLM 调用：800ms（Sonnet 4 带完整上下文）
* TTS 合成：600ms（等待完整响应 + 音频生成）
* 音频输出：50ms（首个音频块）
* **总感知 TTFT：约 1850ms**

**架构 B：端到端语音（OpenAI Realtime）**（2026 默认）

* 音频输入（流式）：0ms
* 轮次结束检测：350ms
* 模型处理（音频到音频）：150ms
* 音频输出：50ms（首个音频块）
* **总感知 TTFT：约 550ms**

**架构 C：端到端语音 + 并行工具执行**（Helios 生产）

* 音频输入（流式）：0ms
* 轮次结束检测：350ms
* 模型处理：100ms（填充音频："让我查一下……"）
* 工具执行（并行）：后台运行；不阻塞
* 音频输出：50ms
* （工具结果在填充音频后馈入）
* **总感知 TTFT：约 500ms**

要解读的：

1. **端到端语音比 STT+LLM+TTS 快得多**（约 3 倍改进）。架构选择是最大的延迟杠杆。
2. **填充音频 + 并行工具执行**进一步减少感知延迟。用户在真实数据到达前听到 agent 说话。
3. **亚 500ms 用 2026 栈可实现**，需仔细工程的边际。亚 300ms 有竞争力；Cartesia 的专业模型可实现。

对 Helios 的客户支持量，架构 A 和架构 B 之间的延迟差异是"用户沮丧挂断"和"用户有对话"之间的差异。架构选择是产品关键的，不仅是工程优雅的。
