### 19.8 对什么告警

可观测性数据流入仪表板和告警。仪表板是持续感知；告警是运维触发。生产中应该对什么告警？

**成本激增**：总支出比 7 天均值上升 >25%。从成本仪表板驱动（第 18 章）。

**错误率增加**：每功能错误率比基线上升 >X%。可能指示提供商问题或代码回归。

**延迟退化**：p99 延迟比基线高 >X%。常是提供商问题或上游变慢的第一个信号。

**Eval 分数下降**：生产 eval 采样低于阈值（第 21 章）。漂移检测。

**工具故障率**：特定工具以升高比率失败。常指示下游依赖问题。

**轨迹长度异常**：agent 运行异常长；可能螺旋（第 14 章）。

**每租户支出**：租户接近预算上限（第 17 章）。

**缓存命中率**：缓存按预期工作（第 18 章）。

**Schema 验证失败**：结构化输出以升高比率验证失败（第 7 章）；常是模型行为变更。

**拒绝率变化**：模型以不同于基线的比率拒绝；可能指示模型版本更新或 prompt 回归。

告警路由遵循层所有权（第 16 章 §16.4）：

* 成本告警 → 工程领导 + AI 运维。
* 质量告警（eval 下降）→ AI 工程团队。
* 提供商问题（延迟、错误率）→ AI 平台团队 + 提供商支持。
* 租户预算告警 → 客户成功 + AI 运维。

告警疲劳是故障模式：太多告警，团队开始忽略，真实问题漏过。纪律：仔细调优阈值；很少每分钟超过一条告警；告警应是可操作的，不仅是信息性的。

### 19.9 Triangle Trade-off 侧栏：可观测性投资（延迟分解格式）

插桩 vs 未插桩请求的延迟分解：

组件 | 未插桩 | 带 Langfuse（异步） | 带 Langfuse + PHI 脱敏
---|---|---|---
网络出（请求） | 50ms | 50ms | 50ms
LLM 调用（Sonnet 4） | 800ms | 800ms | 800ms
网络入（响应） | 50ms | 50ms | 50ms
追踪开销 | 0ms | 5ms（异步写入） | 25ms（脱敏 + 写入）
总感知延迟 | ~900ms | ~905ms | ~925ms

要解读的：

1. **可观测性在异步模式下增加可忽略延迟**。5ms 远低于噪声。
2. **PHI 脱敏增加 20ms**。真实但小；对需要脱敏的受监管行业可接受。
3. **系统可见性是价值**。延迟成本 < 请求的 3%；运维收益大得多（调试、监控、eval）。

三个要追踪的可观测性投资：

* **追踪覆盖率**：生产请求有追踪的百分比。目标：生产路径 >95%。
* **追踪完整性**：有所有预期 span（LLM 调用、工具调用、eval 分数）的追踪百分比。目标：>90%。
* **平均解决时间（MTTR）**：生产问题的。追踪数据应使 MTTR 短——分钟到小时，非天。（SRE 文献区分 MTTD = 平均检测时间和 MTTR；追踪数据主要压缩 MTTR，因为检测常通过用户报告或独立触发的告警发生。）
