#### 应用题

**Q1.** _为新 RAG 系统映射从朴素到生产的进展_。你在一家 50 人 SaaS 公司开始 RAG 项目。语料库：约 3M token 内部产品文档。初始朴素 eval：recall@10 = 0.66，回答通过率 = 0.51。提出 6 个月工程计划达到 0.80 通过率目标。指出五个具体杠杆变更及预测改进。

提示

§9.6 有 Strata 的路径；适配到文档 RAG 场景。

**答案：**

第 1 个月：**文档结构感知分块**。内部文档可能有 markdown 结构（章节、代码块、表格）。按章节带重叠分块。预测提升：检索 recall +3 分。

第 2 个月：**混合搜索（向量 + BM25）**。产品文档有许多精确术语查询（函数名、错误码）。混合捕获这些。预测提升：精确术语查询检索 recall +4 分。

第 3 个月：**添加 Cohere Rerank**。从向量+BM25 的 top-20 → 通过重排序器 top-5。大多数 RAG 进展中最大单项提升。预测提升：回答通过率 +8 分。

第 4 个月：**引用强制 prompt**。LLM 必须为每个声明引用 chunk-id。捕获模型超出检索内容幻觉的案例。预测提升：回答忠实度 +5 分。

第 5 个月：**模糊查询的查询重写**。用更小 LLM 扩展模糊查询（例如，_如何做认证？_ → _认证 API 方法 OAuth JWT_）。预测提升：长尾查询 +2–3 分。

第 6 个月：**多轴 eval 和持续迭代**。接入绑定工作流 #5 eval；识别残余故障模式；迭代。预测提升：可变，但纪律确保持续改进。

累计目标：从 0.51 到约 0.78。最后 2 分到 0.80 可能需要领域特定嵌入或更强生成模型。计划对有专职工程师的 50 人公司现实。

**AI 协作子问题**：你会如何请 LLM 帮助完成这个练习，你会在它的回答中验证什么？

**答案：** 有用的 prompt：_我有一个内部文档 RAG 上 0.51 通过率的朴素 RAG 系统。提出 6 个月工程计划带 5 个杠杆变更和预测改进以达到 0.80。_ 你验证 (a) 预测提升在现实范围内（非单次变更 20 分增益），以及 (b) 杠杆顺序合理（重排序器在查询重写前因为重排序器是最大单项提升；分块在一切之前因为所有后续杠杆依赖它）。

**Q2.** _设计检索内容审计_。勾勒 §9.9 的检索内容审计 eval。指定：(a) 审计看什么数据；(b) 测量什么；(c) 失败信号；(d) 运行频率。

**答案：**

(a) **数据**：生产检索调用样本（每周约 200 查询），每个带 top-K 检索块的来源 ID 和来源类型。加生产批准来源类型允许列表。

(b) **测量**：

* _允许列表一致率_：检索块来源类型在允许列表上的比例。
* _来源类型分布_：检索中每种来源类型的块计数（捕获一种来源意外成为主导的漂移）。
* _新来源率_：命中最近 30 天未见来源 ID 的检索比例（捕获无意中的语料库漂移）。
